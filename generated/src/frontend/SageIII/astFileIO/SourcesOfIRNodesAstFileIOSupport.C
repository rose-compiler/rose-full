// MACHINE GENERATED SOURCE FILE WITH ROSE (Grammar.h)--- DO NOT MODIFY!

#include "sage3basic.h"

#ifndef ROSE_USE_INTERNAL_FRONTEND_DEVELOPMENT
   #include "AST_FILE_IO.h"
#endif 
// The header file ("rose_config.h") should only be included by source files that require it.
#include "rose_config.h"

#if _MSC_VER
#define USE_CPP_NEW_DELETE_OPERATORS 0
#endif

#define ROSE_ALLOC_TRACE 0
#undef mprintf
#define mprintf Rose::Diagnostics::mfprintf(Rose::ir_node_mlog[Rose::Diagnostics::DEBUG])

#include <Rose/BinaryAnalysis/RegisterDictionary.h>

using namespace std;
#include "Cxx_GrammarMemoryPoolSupport.h"


/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNode that takes its corresponding StorageClass as parameter
 */
SgNode::SgNode(const SgNodeStorageClass& storageSource)   {

/* #line 32 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    p_freepointer = AST_FileIO::IS_VALID_POINTER();
    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_parent = (SgNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parent));
    p_isModified = storageSource.storageOf_isModified;
    p_containsTransformation = storageSource.storageOf_containsTransformation;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNode*
SgNode::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNode)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNode)};
        unsigned long positionInPool{localIndex % SgNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNode::pool_size};
        returnPointer = &(((SgNode*)(SgNode::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNode*
SgNode::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNode)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNode)};
        unsigned long positionInPool{localIndex % SgNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNode::pool_size};
        returnPointer = &(((SgNode*)(SgNode::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNode::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNode::pools.begin(); block != SgNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNode*>(*block)};
        for (unsigned i = 0; i < SgNode::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNode*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNode::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNode* pointerOfLinkedList{nullptr};
    for (auto block = SgNode::pools.begin(); block != SgNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNode*>(*block)};
        for (unsigned i = 0; i < SgNode::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNode::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNode::clearMemoryPool() {
    SgNode* tempPointer{nullptr};
    if (SgNode::pools.empty() == false) {
        auto block{SgNode::pools.begin()};
        SgNode::next_node = reinterpret_cast<SgNode*>(*block);
        for (; block != SgNode::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNode*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNode::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNode::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNode::pool_size-1]);
        }
    }
}

void
SgNode::deleteMemoryPool() {
    for (auto p: SgNode::pools) {
        ROSE_FREE(p);
    }
    SgNode::next_node = nullptr;
    SgNode::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNode::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNode) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNode)};
    for (size_t blockIndex = SgNode::pools.size(); (blockIndex * SgNode::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNode) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNode) = %" PRIuPTR " SgNode::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNode),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNode),SgNode::pool_size);
        }
#endif

        SgNode* pointer{static_cast<SgNode*>(ROSE_MALLOC(SgNode::pool_size * sizeof(SgNode)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNode::pool_size * sizeof(SgNode));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNode::pool_size * sizeof(SgNode));
#endif
        SgNode::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNode::pool_size * sizeof(SgNode), V_SgNode));

        if (SgNode::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNode* blkptr{reinterpret_cast<SgNode*>(SgNode::pools[blockIndex-1])};
                blkptr[SgNode::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNode::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNode::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNode::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNode::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNode::pool_size - 1};
    SgNode* testPointer{reinterpret_cast<SgNode*>(SgNode::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNode::pool_size * (SgNode::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNodeStroageClass
 * from its counterpart of SgNode. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNode::initializeStorageClassArray(SgNodeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNode::pools.begin(); block != SgNode::pools.end(); ++block) {
        SgNode* pointer{reinterpret_cast<SgNode*>(*block)};
        for (unsigned i = 0; i < SgNode::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 253 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSupport that takes its corresponding StorageClass as parameter
 */
SgSupport::SgSupport(const SgSupportStorageClass& storageSource)  : SgNode (storageSource) {

/* #line 263 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSupport*
SgSupport::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSupport* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSupport)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSupport + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSupport)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSupport)};
        unsigned long positionInPool{localIndex % SgSupport::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSupport::pool_size};
        returnPointer = &(((SgSupport*)(SgSupport::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSupport*
SgSupport::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSupport* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSupport)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSupport + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSupport)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSupport)};
        unsigned long positionInPool{localIndex % SgSupport::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSupport::pool_size};
        returnPointer = &(((SgSupport*)(SgSupport::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSupport::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSupport::pools.begin(); block != SgSupport::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSupport*>(*block)};
        for (unsigned i = 0; i < SgSupport::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSupport*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSupport::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSupport* pointerOfLinkedList{nullptr};
    for (auto block = SgSupport::pools.begin(); block != SgSupport::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSupport*>(*block)};
        for (unsigned i = 0; i < SgSupport::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSupport::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSupport::clearMemoryPool() {
    SgSupport* tempPointer{nullptr};
    if (SgSupport::pools.empty() == false) {
        auto block{SgSupport::pools.begin()};
        SgSupport::next_node = reinterpret_cast<SgSupport*>(*block);
        for (; block != SgSupport::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSupport*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSupport::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSupport::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSupport::pool_size-1]);
        }
    }
}

void
SgSupport::deleteMemoryPool() {
    for (auto p: SgSupport::pools) {
        ROSE_FREE(p);
    }
    SgSupport::next_node = nullptr;
    SgSupport::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSupport::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSupport) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSupport)};
    for (size_t blockIndex = SgSupport::pools.size(); (blockIndex * SgSupport::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSupport) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSupport) = %" PRIuPTR " SgSupport::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSupport),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSupport),SgSupport::pool_size);
        }
#endif

        SgSupport* pointer{static_cast<SgSupport*>(ROSE_MALLOC(SgSupport::pool_size * sizeof(SgSupport)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSupport::pool_size * sizeof(SgSupport));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSupport::pool_size * sizeof(SgSupport));
#endif
        SgSupport::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSupport::pool_size * sizeof(SgSupport), V_SgSupport));

        if (SgSupport::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSupport* blkptr{reinterpret_cast<SgSupport*>(SgSupport::pools[blockIndex-1])};
                blkptr[SgSupport::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSupport::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSupport::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSupport::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSupport::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSupport::pool_size - 1};
    SgSupport* testPointer{reinterpret_cast<SgSupport*>(SgSupport::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSupport::pool_size * (SgSupport::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSupportStroageClass
 * from its counterpart of SgSupport. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSupport::initializeStorageClassArray(SgSupportStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSupport::pools.begin(); block != SgSupport::pools.end(); ++block) {
        SgSupport* pointer{reinterpret_cast<SgSupport*>(*block)};
        for (unsigned i = 0; i < SgSupport::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 480 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgModifier that takes its corresponding StorageClass as parameter
 */
SgModifier::SgModifier(const SgModifierStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 490 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgModifier*
SgModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgModifier)};
        unsigned long positionInPool{localIndex % SgModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModifier::pool_size};
        returnPointer = &(((SgModifier*)(SgModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgModifier*
SgModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgModifier)};
        unsigned long positionInPool{localIndex % SgModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModifier::pool_size};
        returnPointer = &(((SgModifier*)(SgModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgModifier::pools.begin(); block != SgModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModifier*>(*block)};
        for (unsigned i = 0; i < SgModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgModifier::pools.begin(); block != SgModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModifier*>(*block)};
        for (unsigned i = 0; i < SgModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgModifier::clearMemoryPool() {
    SgModifier* tempPointer{nullptr};
    if (SgModifier::pools.empty() == false) {
        auto block{SgModifier::pools.begin()};
        SgModifier::next_node = reinterpret_cast<SgModifier*>(*block);
        for (; block != SgModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgModifier::pool_size-1]);
        }
    }
}

void
SgModifier::deleteMemoryPool() {
    for (auto p: SgModifier::pools) {
        ROSE_FREE(p);
    }
    SgModifier::next_node = nullptr;
    SgModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifier)};
    for (size_t blockIndex = SgModifier::pools.size(); (blockIndex * SgModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifier) = %" PRIuPTR " SgModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifier),SgModifier::pool_size);
        }
#endif

        SgModifier* pointer{static_cast<SgModifier*>(ROSE_MALLOC(SgModifier::pool_size * sizeof(SgModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgModifier::pool_size * sizeof(SgModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgModifier::pool_size * sizeof(SgModifier));
#endif
        SgModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgModifier::pool_size * sizeof(SgModifier), V_SgModifier));

        if (SgModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgModifier* blkptr{reinterpret_cast<SgModifier*>(SgModifier::pools[blockIndex-1])};
                blkptr[SgModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgModifier::pool_size - 1};
    SgModifier* testPointer{reinterpret_cast<SgModifier*>(SgModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgModifier::pool_size * (SgModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgModifierStroageClass
 * from its counterpart of SgModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgModifier::initializeStorageClassArray(SgModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgModifier::pools.begin(); block != SgModifier::pools.end(); ++block) {
        SgModifier* pointer{reinterpret_cast<SgModifier*>(*block)};
        for (unsigned i = 0; i < SgModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 707 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgModifierNodes that takes its corresponding StorageClass as parameter
 */
SgModifierNodes::SgModifierNodes(const SgModifierNodesStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 717 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_nodes = storageSource.storageOf_nodes.rebuildDataStoredInEasyStorageClass();
    SgModifierTypePtrVector::iterator i_nodes = p_nodes.begin();
    for ( ; i_nodes != p_nodes.end(); ++i_nodes) {
        (*i_nodes) = (SgModifierTypePtrVector::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_nodes)) );
    }
     p_next = (SgModifierNodes*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_next));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgModifierNodes*
SgModifierNodes::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgModifierNodes* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifierNodes)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifierNodes + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifierNodes)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierNodes)};
        unsigned long positionInPool{localIndex % SgModifierNodes::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModifierNodes::pool_size};
        returnPointer = &(((SgModifierNodes*)(SgModifierNodes::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgModifierNodes*
SgModifierNodes::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgModifierNodes* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifierNodes)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifierNodes + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifierNodes)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgModifierNodes)};
        unsigned long positionInPool{localIndex % SgModifierNodes::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModifierNodes::pool_size};
        returnPointer = &(((SgModifierNodes*)(SgModifierNodes::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgModifierNodes::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgModifierNodes::pools.begin(); block != SgModifierNodes::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModifierNodes*>(*block)};
        for (unsigned i = 0; i < SgModifierNodes::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgModifierNodes*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgModifierNodes::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgModifierNodes* pointerOfLinkedList{nullptr};
    for (auto block = SgModifierNodes::pools.begin(); block != SgModifierNodes::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModifierNodes*>(*block)};
        for (unsigned i = 0; i < SgModifierNodes::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgModifierNodes::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgModifierNodes::clearMemoryPool() {
    SgModifierNodes* tempPointer{nullptr};
    if (SgModifierNodes::pools.empty() == false) {
        auto block{SgModifierNodes::pools.begin()};
        SgModifierNodes::next_node = reinterpret_cast<SgModifierNodes*>(*block);
        for (; block != SgModifierNodes::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgModifierNodes*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgModifierNodes::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgModifierNodes::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgModifierNodes::pool_size-1]);
        }
    }
}

void
SgModifierNodes::deleteMemoryPool() {
    for (auto p: SgModifierNodes::pools) {
        ROSE_FREE(p);
    }
    SgModifierNodes::next_node = nullptr;
    SgModifierNodes::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgModifierNodes::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierNodes) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifierNodes)};
    for (size_t blockIndex = SgModifierNodes::pools.size(); (blockIndex * SgModifierNodes::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierNodes) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifierNodes) = %" PRIuPTR " SgModifierNodes::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierNodes),AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifierNodes),SgModifierNodes::pool_size);
        }
#endif

        SgModifierNodes* pointer{static_cast<SgModifierNodes*>(ROSE_MALLOC(SgModifierNodes::pool_size * sizeof(SgModifierNodes)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgModifierNodes::pool_size * sizeof(SgModifierNodes));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgModifierNodes::pool_size * sizeof(SgModifierNodes));
#endif
        SgModifierNodes::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgModifierNodes::pool_size * sizeof(SgModifierNodes), V_SgModifierNodes));

        if (SgModifierNodes::next_node != nullptr) {
            if (blockIndex > 0) {
                SgModifierNodes* blkptr{reinterpret_cast<SgModifierNodes*>(SgModifierNodes::pools[blockIndex-1])};
                blkptr[SgModifierNodes::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgModifierNodes::next_node = pointer;
        }

        for (unsigned i = 0; i < SgModifierNodes::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgModifierNodes::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgModifierNodes::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgModifierNodes::pool_size - 1};
    SgModifierNodes* testPointer{reinterpret_cast<SgModifierNodes*>(SgModifierNodes::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgModifierNodes::pool_size * (SgModifierNodes::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgModifierNodesStroageClass
 * from its counterpart of SgModifierNodes. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgModifierNodes::initializeStorageClassArray(SgModifierNodesStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgModifierNodes::pools.begin(); block != SgModifierNodes::pools.end(); ++block) {
        SgModifierNodes* pointer{reinterpret_cast<SgModifierNodes*>(*block)};
        for (unsigned i = 0; i < SgModifierNodes::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 940 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgConstVolatileModifier that takes its corresponding StorageClass as parameter
 */
SgConstVolatileModifier::SgConstVolatileModifier(const SgConstVolatileModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 950 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgConstVolatileModifier*
SgConstVolatileModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgConstVolatileModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConstVolatileModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConstVolatileModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConstVolatileModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgConstVolatileModifier)};
        unsigned long positionInPool{localIndex % SgConstVolatileModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConstVolatileModifier::pool_size};
        returnPointer = &(((SgConstVolatileModifier*)(SgConstVolatileModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgConstVolatileModifier*
SgConstVolatileModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgConstVolatileModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConstVolatileModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConstVolatileModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConstVolatileModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgConstVolatileModifier)};
        unsigned long positionInPool{localIndex % SgConstVolatileModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConstVolatileModifier::pool_size};
        returnPointer = &(((SgConstVolatileModifier*)(SgConstVolatileModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgConstVolatileModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgConstVolatileModifier::pools.begin(); block != SgConstVolatileModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConstVolatileModifier*>(*block)};
        for (unsigned i = 0; i < SgConstVolatileModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgConstVolatileModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgConstVolatileModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgConstVolatileModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgConstVolatileModifier::pools.begin(); block != SgConstVolatileModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConstVolatileModifier*>(*block)};
        for (unsigned i = 0; i < SgConstVolatileModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgConstVolatileModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgConstVolatileModifier::clearMemoryPool() {
    SgConstVolatileModifier* tempPointer{nullptr};
    if (SgConstVolatileModifier::pools.empty() == false) {
        auto block{SgConstVolatileModifier::pools.begin()};
        SgConstVolatileModifier::next_node = reinterpret_cast<SgConstVolatileModifier*>(*block);
        for (; block != SgConstVolatileModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgConstVolatileModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgConstVolatileModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgConstVolatileModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgConstVolatileModifier::pool_size-1]);
        }
    }
}

void
SgConstVolatileModifier::deleteMemoryPool() {
    for (auto p: SgConstVolatileModifier::pools) {
        ROSE_FREE(p);
    }
    SgConstVolatileModifier::next_node = nullptr;
    SgConstVolatileModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgConstVolatileModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgConstVolatileModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgConstVolatileModifier)};
    for (size_t blockIndex = SgConstVolatileModifier::pools.size(); (blockIndex * SgConstVolatileModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgConstVolatileModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgConstVolatileModifier) = %" PRIuPTR " SgConstVolatileModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgConstVolatileModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgConstVolatileModifier),SgConstVolatileModifier::pool_size);
        }
#endif

        SgConstVolatileModifier* pointer{static_cast<SgConstVolatileModifier*>(ROSE_MALLOC(SgConstVolatileModifier::pool_size * sizeof(SgConstVolatileModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgConstVolatileModifier::pool_size * sizeof(SgConstVolatileModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgConstVolatileModifier::pool_size * sizeof(SgConstVolatileModifier));
#endif
        SgConstVolatileModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgConstVolatileModifier::pool_size * sizeof(SgConstVolatileModifier), V_SgConstVolatileModifier));

        if (SgConstVolatileModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgConstVolatileModifier* blkptr{reinterpret_cast<SgConstVolatileModifier*>(SgConstVolatileModifier::pools[blockIndex-1])};
                blkptr[SgConstVolatileModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgConstVolatileModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgConstVolatileModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgConstVolatileModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgConstVolatileModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgConstVolatileModifier::pool_size - 1};
    SgConstVolatileModifier* testPointer{reinterpret_cast<SgConstVolatileModifier*>(SgConstVolatileModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgConstVolatileModifier::pool_size * (SgConstVolatileModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgConstVolatileModifierStroageClass
 * from its counterpart of SgConstVolatileModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgConstVolatileModifier::initializeStorageClassArray(SgConstVolatileModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgConstVolatileModifier::pools.begin(); block != SgConstVolatileModifier::pools.end(); ++block) {
        SgConstVolatileModifier* pointer{reinterpret_cast<SgConstVolatileModifier*>(*block)};
        for (unsigned i = 0; i < SgConstVolatileModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 1168 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStorageModifier that takes its corresponding StorageClass as parameter
 */
SgStorageModifier::SgStorageModifier(const SgStorageModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 1178 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;
    p_thread_local_storage = storageSource.storageOf_thread_local_storage;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStorageModifier*
SgStorageModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStorageModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStorageModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStorageModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStorageModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStorageModifier)};
        unsigned long positionInPool{localIndex % SgStorageModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStorageModifier::pool_size};
        returnPointer = &(((SgStorageModifier*)(SgStorageModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStorageModifier*
SgStorageModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStorageModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStorageModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStorageModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStorageModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStorageModifier)};
        unsigned long positionInPool{localIndex % SgStorageModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStorageModifier::pool_size};
        returnPointer = &(((SgStorageModifier*)(SgStorageModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStorageModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStorageModifier::pools.begin(); block != SgStorageModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStorageModifier*>(*block)};
        for (unsigned i = 0; i < SgStorageModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStorageModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStorageModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStorageModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgStorageModifier::pools.begin(); block != SgStorageModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStorageModifier*>(*block)};
        for (unsigned i = 0; i < SgStorageModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStorageModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStorageModifier::clearMemoryPool() {
    SgStorageModifier* tempPointer{nullptr};
    if (SgStorageModifier::pools.empty() == false) {
        auto block{SgStorageModifier::pools.begin()};
        SgStorageModifier::next_node = reinterpret_cast<SgStorageModifier*>(*block);
        for (; block != SgStorageModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStorageModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStorageModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStorageModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStorageModifier::pool_size-1]);
        }
    }
}

void
SgStorageModifier::deleteMemoryPool() {
    for (auto p: SgStorageModifier::pools) {
        ROSE_FREE(p);
    }
    SgStorageModifier::next_node = nullptr;
    SgStorageModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStorageModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStorageModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStorageModifier)};
    for (size_t blockIndex = SgStorageModifier::pools.size(); (blockIndex * SgStorageModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStorageModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStorageModifier) = %" PRIuPTR " SgStorageModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStorageModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStorageModifier),SgStorageModifier::pool_size);
        }
#endif

        SgStorageModifier* pointer{static_cast<SgStorageModifier*>(ROSE_MALLOC(SgStorageModifier::pool_size * sizeof(SgStorageModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStorageModifier::pool_size * sizeof(SgStorageModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStorageModifier::pool_size * sizeof(SgStorageModifier));
#endif
        SgStorageModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStorageModifier::pool_size * sizeof(SgStorageModifier), V_SgStorageModifier));

        if (SgStorageModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStorageModifier* blkptr{reinterpret_cast<SgStorageModifier*>(SgStorageModifier::pools[blockIndex-1])};
                blkptr[SgStorageModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStorageModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStorageModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStorageModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStorageModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStorageModifier::pool_size - 1};
    SgStorageModifier* testPointer{reinterpret_cast<SgStorageModifier*>(SgStorageModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStorageModifier::pool_size * (SgStorageModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStorageModifierStroageClass
 * from its counterpart of SgStorageModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStorageModifier::initializeStorageClassArray(SgStorageModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStorageModifier::pools.begin(); block != SgStorageModifier::pools.end(); ++block) {
        SgStorageModifier* pointer{reinterpret_cast<SgStorageModifier*>(*block)};
        for (unsigned i = 0; i < SgStorageModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 1397 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAccessModifier that takes its corresponding StorageClass as parameter
 */
SgAccessModifier::SgAccessModifier(const SgAccessModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 1407 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAccessModifier*
SgAccessModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAccessModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAccessModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAccessModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAccessModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAccessModifier)};
        unsigned long positionInPool{localIndex % SgAccessModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAccessModifier::pool_size};
        returnPointer = &(((SgAccessModifier*)(SgAccessModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAccessModifier*
SgAccessModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAccessModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAccessModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAccessModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAccessModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAccessModifier)};
        unsigned long positionInPool{localIndex % SgAccessModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAccessModifier::pool_size};
        returnPointer = &(((SgAccessModifier*)(SgAccessModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAccessModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAccessModifier::pools.begin(); block != SgAccessModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAccessModifier*>(*block)};
        for (unsigned i = 0; i < SgAccessModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAccessModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAccessModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAccessModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgAccessModifier::pools.begin(); block != SgAccessModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAccessModifier*>(*block)};
        for (unsigned i = 0; i < SgAccessModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAccessModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAccessModifier::clearMemoryPool() {
    SgAccessModifier* tempPointer{nullptr};
    if (SgAccessModifier::pools.empty() == false) {
        auto block{SgAccessModifier::pools.begin()};
        SgAccessModifier::next_node = reinterpret_cast<SgAccessModifier*>(*block);
        for (; block != SgAccessModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAccessModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAccessModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAccessModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAccessModifier::pool_size-1]);
        }
    }
}

void
SgAccessModifier::deleteMemoryPool() {
    for (auto p: SgAccessModifier::pools) {
        ROSE_FREE(p);
    }
    SgAccessModifier::next_node = nullptr;
    SgAccessModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAccessModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAccessModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAccessModifier)};
    for (size_t blockIndex = SgAccessModifier::pools.size(); (blockIndex * SgAccessModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAccessModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAccessModifier) = %" PRIuPTR " SgAccessModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAccessModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAccessModifier),SgAccessModifier::pool_size);
        }
#endif

        SgAccessModifier* pointer{static_cast<SgAccessModifier*>(ROSE_MALLOC(SgAccessModifier::pool_size * sizeof(SgAccessModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAccessModifier::pool_size * sizeof(SgAccessModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAccessModifier::pool_size * sizeof(SgAccessModifier));
#endif
        SgAccessModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAccessModifier::pool_size * sizeof(SgAccessModifier), V_SgAccessModifier));

        if (SgAccessModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAccessModifier* blkptr{reinterpret_cast<SgAccessModifier*>(SgAccessModifier::pools[blockIndex-1])};
                blkptr[SgAccessModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAccessModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAccessModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAccessModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAccessModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAccessModifier::pool_size - 1};
    SgAccessModifier* testPointer{reinterpret_cast<SgAccessModifier*>(SgAccessModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAccessModifier::pool_size * (SgAccessModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAccessModifierStroageClass
 * from its counterpart of SgAccessModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAccessModifier::initializeStorageClassArray(SgAccessModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAccessModifier::pools.begin(); block != SgAccessModifier::pools.end(); ++block) {
        SgAccessModifier* pointer{reinterpret_cast<SgAccessModifier*>(*block)};
        for (unsigned i = 0; i < SgAccessModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 1625 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionModifier that takes its corresponding StorageClass as parameter
 */
SgFunctionModifier::SgFunctionModifier(const SgFunctionModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 1635 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifierVector = storageSource.storageOf_modifierVector.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_constructor_destructor_priority = storageSource.storageOf_gnu_attribute_constructor_destructor_priority;
    p_gnu_attribute_named_weak_reference = storageSource.storageOf_gnu_attribute_named_weak_reference.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_named_alias = storageSource.storageOf_gnu_attribute_named_alias.rebuildDataStoredInEasyStorageClass();
     p_opencl_vec_type = (SgType *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_opencl_vec_type));
    p_opencl_work_group_size = storageSource.storageOf_opencl_work_group_size;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionModifier*
SgFunctionModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionModifier)};
        unsigned long positionInPool{localIndex % SgFunctionModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionModifier::pool_size};
        returnPointer = &(((SgFunctionModifier*)(SgFunctionModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionModifier*
SgFunctionModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionModifier)};
        unsigned long positionInPool{localIndex % SgFunctionModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionModifier::pool_size};
        returnPointer = &(((SgFunctionModifier*)(SgFunctionModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionModifier::pools.begin(); block != SgFunctionModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionModifier*>(*block)};
        for (unsigned i = 0; i < SgFunctionModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionModifier::pools.begin(); block != SgFunctionModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionModifier*>(*block)};
        for (unsigned i = 0; i < SgFunctionModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionModifier::clearMemoryPool() {
    SgFunctionModifier* tempPointer{nullptr};
    if (SgFunctionModifier::pools.empty() == false) {
        auto block{SgFunctionModifier::pools.begin()};
        SgFunctionModifier::next_node = reinterpret_cast<SgFunctionModifier*>(*block);
        for (; block != SgFunctionModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionModifier::pool_size-1]);
        }
    }
}

void
SgFunctionModifier::deleteMemoryPool() {
    for (auto p: SgFunctionModifier::pools) {
        ROSE_FREE(p);
    }
    SgFunctionModifier::next_node = nullptr;
    SgFunctionModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionModifier)};
    for (size_t blockIndex = SgFunctionModifier::pools.size(); (blockIndex * SgFunctionModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionModifier) = %" PRIuPTR " SgFunctionModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionModifier),SgFunctionModifier::pool_size);
        }
#endif

        SgFunctionModifier* pointer{static_cast<SgFunctionModifier*>(ROSE_MALLOC(SgFunctionModifier::pool_size * sizeof(SgFunctionModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionModifier::pool_size * sizeof(SgFunctionModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionModifier::pool_size * sizeof(SgFunctionModifier));
#endif
        SgFunctionModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionModifier::pool_size * sizeof(SgFunctionModifier), V_SgFunctionModifier));

        if (SgFunctionModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionModifier* blkptr{reinterpret_cast<SgFunctionModifier*>(SgFunctionModifier::pools[blockIndex-1])};
                blkptr[SgFunctionModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionModifier::pool_size - 1};
    SgFunctionModifier* testPointer{reinterpret_cast<SgFunctionModifier*>(SgFunctionModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionModifier::pool_size * (SgFunctionModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionModifierStroageClass
 * from its counterpart of SgFunctionModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionModifier::initializeStorageClassArray(SgFunctionModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionModifier::pools.begin(); block != SgFunctionModifier::pools.end(); ++block) {
        SgFunctionModifier* pointer{reinterpret_cast<SgFunctionModifier*>(*block)};
        for (unsigned i = 0; i < SgFunctionModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 1858 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUPC_AccessModifier that takes its corresponding StorageClass as parameter
 */
SgUPC_AccessModifier::SgUPC_AccessModifier(const SgUPC_AccessModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 1868 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;
    p_isShared = storageSource.storageOf_isShared;
    p_layout = storageSource.storageOf_layout;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUPC_AccessModifier*
SgUPC_AccessModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUPC_AccessModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUPC_AccessModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUPC_AccessModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUPC_AccessModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUPC_AccessModifier)};
        unsigned long positionInPool{localIndex % SgUPC_AccessModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUPC_AccessModifier::pool_size};
        returnPointer = &(((SgUPC_AccessModifier*)(SgUPC_AccessModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUPC_AccessModifier*
SgUPC_AccessModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUPC_AccessModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUPC_AccessModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUPC_AccessModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUPC_AccessModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUPC_AccessModifier)};
        unsigned long positionInPool{localIndex % SgUPC_AccessModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUPC_AccessModifier::pool_size};
        returnPointer = &(((SgUPC_AccessModifier*)(SgUPC_AccessModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUPC_AccessModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUPC_AccessModifier::pools.begin(); block != SgUPC_AccessModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUPC_AccessModifier*>(*block)};
        for (unsigned i = 0; i < SgUPC_AccessModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUPC_AccessModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUPC_AccessModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUPC_AccessModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgUPC_AccessModifier::pools.begin(); block != SgUPC_AccessModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUPC_AccessModifier*>(*block)};
        for (unsigned i = 0; i < SgUPC_AccessModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUPC_AccessModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUPC_AccessModifier::clearMemoryPool() {
    SgUPC_AccessModifier* tempPointer{nullptr};
    if (SgUPC_AccessModifier::pools.empty() == false) {
        auto block{SgUPC_AccessModifier::pools.begin()};
        SgUPC_AccessModifier::next_node = reinterpret_cast<SgUPC_AccessModifier*>(*block);
        for (; block != SgUPC_AccessModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUPC_AccessModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUPC_AccessModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUPC_AccessModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUPC_AccessModifier::pool_size-1]);
        }
    }
}

void
SgUPC_AccessModifier::deleteMemoryPool() {
    for (auto p: SgUPC_AccessModifier::pools) {
        ROSE_FREE(p);
    }
    SgUPC_AccessModifier::next_node = nullptr;
    SgUPC_AccessModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUPC_AccessModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUPC_AccessModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUPC_AccessModifier)};
    for (size_t blockIndex = SgUPC_AccessModifier::pools.size(); (blockIndex * SgUPC_AccessModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUPC_AccessModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUPC_AccessModifier) = %" PRIuPTR " SgUPC_AccessModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUPC_AccessModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUPC_AccessModifier),SgUPC_AccessModifier::pool_size);
        }
#endif

        SgUPC_AccessModifier* pointer{static_cast<SgUPC_AccessModifier*>(ROSE_MALLOC(SgUPC_AccessModifier::pool_size * sizeof(SgUPC_AccessModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUPC_AccessModifier::pool_size * sizeof(SgUPC_AccessModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUPC_AccessModifier::pool_size * sizeof(SgUPC_AccessModifier));
#endif
        SgUPC_AccessModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUPC_AccessModifier::pool_size * sizeof(SgUPC_AccessModifier), V_SgUPC_AccessModifier));

        if (SgUPC_AccessModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUPC_AccessModifier* blkptr{reinterpret_cast<SgUPC_AccessModifier*>(SgUPC_AccessModifier::pools[blockIndex-1])};
                blkptr[SgUPC_AccessModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUPC_AccessModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUPC_AccessModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUPC_AccessModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUPC_AccessModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUPC_AccessModifier::pool_size - 1};
    SgUPC_AccessModifier* testPointer{reinterpret_cast<SgUPC_AccessModifier*>(SgUPC_AccessModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUPC_AccessModifier::pool_size * (SgUPC_AccessModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUPC_AccessModifierStroageClass
 * from its counterpart of SgUPC_AccessModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUPC_AccessModifier::initializeStorageClassArray(SgUPC_AccessModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUPC_AccessModifier::pools.begin(); block != SgUPC_AccessModifier::pools.end(); ++block) {
        SgUPC_AccessModifier* pointer{reinterpret_cast<SgUPC_AccessModifier*>(*block)};
        for (unsigned i = 0; i < SgUPC_AccessModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 2088 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSpecialFunctionModifier that takes its corresponding StorageClass as parameter
 */
SgSpecialFunctionModifier::SgSpecialFunctionModifier(const SgSpecialFunctionModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 2098 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifierVector = storageSource.storageOf_modifierVector.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSpecialFunctionModifier*
SgSpecialFunctionModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSpecialFunctionModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpecialFunctionModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpecialFunctionModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpecialFunctionModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSpecialFunctionModifier)};
        unsigned long positionInPool{localIndex % SgSpecialFunctionModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSpecialFunctionModifier::pool_size};
        returnPointer = &(((SgSpecialFunctionModifier*)(SgSpecialFunctionModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSpecialFunctionModifier*
SgSpecialFunctionModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSpecialFunctionModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpecialFunctionModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpecialFunctionModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpecialFunctionModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSpecialFunctionModifier)};
        unsigned long positionInPool{localIndex % SgSpecialFunctionModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSpecialFunctionModifier::pool_size};
        returnPointer = &(((SgSpecialFunctionModifier*)(SgSpecialFunctionModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSpecialFunctionModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSpecialFunctionModifier::pools.begin(); block != SgSpecialFunctionModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSpecialFunctionModifier*>(*block)};
        for (unsigned i = 0; i < SgSpecialFunctionModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSpecialFunctionModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSpecialFunctionModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSpecialFunctionModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgSpecialFunctionModifier::pools.begin(); block != SgSpecialFunctionModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSpecialFunctionModifier*>(*block)};
        for (unsigned i = 0; i < SgSpecialFunctionModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSpecialFunctionModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSpecialFunctionModifier::clearMemoryPool() {
    SgSpecialFunctionModifier* tempPointer{nullptr};
    if (SgSpecialFunctionModifier::pools.empty() == false) {
        auto block{SgSpecialFunctionModifier::pools.begin()};
        SgSpecialFunctionModifier::next_node = reinterpret_cast<SgSpecialFunctionModifier*>(*block);
        for (; block != SgSpecialFunctionModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSpecialFunctionModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSpecialFunctionModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSpecialFunctionModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSpecialFunctionModifier::pool_size-1]);
        }
    }
}

void
SgSpecialFunctionModifier::deleteMemoryPool() {
    for (auto p: SgSpecialFunctionModifier::pools) {
        ROSE_FREE(p);
    }
    SgSpecialFunctionModifier::next_node = nullptr;
    SgSpecialFunctionModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSpecialFunctionModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSpecialFunctionModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpecialFunctionModifier)};
    for (size_t blockIndex = SgSpecialFunctionModifier::pools.size(); (blockIndex * SgSpecialFunctionModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSpecialFunctionModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpecialFunctionModifier) = %" PRIuPTR " SgSpecialFunctionModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSpecialFunctionModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpecialFunctionModifier),SgSpecialFunctionModifier::pool_size);
        }
#endif

        SgSpecialFunctionModifier* pointer{static_cast<SgSpecialFunctionModifier*>(ROSE_MALLOC(SgSpecialFunctionModifier::pool_size * sizeof(SgSpecialFunctionModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSpecialFunctionModifier::pool_size * sizeof(SgSpecialFunctionModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSpecialFunctionModifier::pool_size * sizeof(SgSpecialFunctionModifier));
#endif
        SgSpecialFunctionModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSpecialFunctionModifier::pool_size * sizeof(SgSpecialFunctionModifier), V_SgSpecialFunctionModifier));

        if (SgSpecialFunctionModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSpecialFunctionModifier* blkptr{reinterpret_cast<SgSpecialFunctionModifier*>(SgSpecialFunctionModifier::pools[blockIndex-1])};
                blkptr[SgSpecialFunctionModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSpecialFunctionModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSpecialFunctionModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSpecialFunctionModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSpecialFunctionModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSpecialFunctionModifier::pool_size - 1};
    SgSpecialFunctionModifier* testPointer{reinterpret_cast<SgSpecialFunctionModifier*>(SgSpecialFunctionModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSpecialFunctionModifier::pool_size * (SgSpecialFunctionModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSpecialFunctionModifierStroageClass
 * from its counterpart of SgSpecialFunctionModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSpecialFunctionModifier::initializeStorageClassArray(SgSpecialFunctionModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSpecialFunctionModifier::pools.begin(); block != SgSpecialFunctionModifier::pools.end(); ++block) {
        SgSpecialFunctionModifier* pointer{reinterpret_cast<SgSpecialFunctionModifier*>(*block)};
        for (unsigned i = 0; i < SgSpecialFunctionModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 2316 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElaboratedTypeModifier that takes its corresponding StorageClass as parameter
 */
SgElaboratedTypeModifier::SgElaboratedTypeModifier(const SgElaboratedTypeModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 2326 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElaboratedTypeModifier*
SgElaboratedTypeModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElaboratedTypeModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElaboratedTypeModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElaboratedTypeModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElaboratedTypeModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElaboratedTypeModifier)};
        unsigned long positionInPool{localIndex % SgElaboratedTypeModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElaboratedTypeModifier::pool_size};
        returnPointer = &(((SgElaboratedTypeModifier*)(SgElaboratedTypeModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElaboratedTypeModifier*
SgElaboratedTypeModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElaboratedTypeModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElaboratedTypeModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElaboratedTypeModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElaboratedTypeModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElaboratedTypeModifier)};
        unsigned long positionInPool{localIndex % SgElaboratedTypeModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElaboratedTypeModifier::pool_size};
        returnPointer = &(((SgElaboratedTypeModifier*)(SgElaboratedTypeModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElaboratedTypeModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElaboratedTypeModifier::pools.begin(); block != SgElaboratedTypeModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElaboratedTypeModifier*>(*block)};
        for (unsigned i = 0; i < SgElaboratedTypeModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElaboratedTypeModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElaboratedTypeModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElaboratedTypeModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgElaboratedTypeModifier::pools.begin(); block != SgElaboratedTypeModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElaboratedTypeModifier*>(*block)};
        for (unsigned i = 0; i < SgElaboratedTypeModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElaboratedTypeModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElaboratedTypeModifier::clearMemoryPool() {
    SgElaboratedTypeModifier* tempPointer{nullptr};
    if (SgElaboratedTypeModifier::pools.empty() == false) {
        auto block{SgElaboratedTypeModifier::pools.begin()};
        SgElaboratedTypeModifier::next_node = reinterpret_cast<SgElaboratedTypeModifier*>(*block);
        for (; block != SgElaboratedTypeModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElaboratedTypeModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElaboratedTypeModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElaboratedTypeModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElaboratedTypeModifier::pool_size-1]);
        }
    }
}

void
SgElaboratedTypeModifier::deleteMemoryPool() {
    for (auto p: SgElaboratedTypeModifier::pools) {
        ROSE_FREE(p);
    }
    SgElaboratedTypeModifier::next_node = nullptr;
    SgElaboratedTypeModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElaboratedTypeModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElaboratedTypeModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElaboratedTypeModifier)};
    for (size_t blockIndex = SgElaboratedTypeModifier::pools.size(); (blockIndex * SgElaboratedTypeModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElaboratedTypeModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElaboratedTypeModifier) = %" PRIuPTR " SgElaboratedTypeModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElaboratedTypeModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElaboratedTypeModifier),SgElaboratedTypeModifier::pool_size);
        }
#endif

        SgElaboratedTypeModifier* pointer{static_cast<SgElaboratedTypeModifier*>(ROSE_MALLOC(SgElaboratedTypeModifier::pool_size * sizeof(SgElaboratedTypeModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElaboratedTypeModifier::pool_size * sizeof(SgElaboratedTypeModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElaboratedTypeModifier::pool_size * sizeof(SgElaboratedTypeModifier));
#endif
        SgElaboratedTypeModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElaboratedTypeModifier::pool_size * sizeof(SgElaboratedTypeModifier), V_SgElaboratedTypeModifier));

        if (SgElaboratedTypeModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElaboratedTypeModifier* blkptr{reinterpret_cast<SgElaboratedTypeModifier*>(SgElaboratedTypeModifier::pools[blockIndex-1])};
                blkptr[SgElaboratedTypeModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElaboratedTypeModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElaboratedTypeModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElaboratedTypeModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElaboratedTypeModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElaboratedTypeModifier::pool_size - 1};
    SgElaboratedTypeModifier* testPointer{reinterpret_cast<SgElaboratedTypeModifier*>(SgElaboratedTypeModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElaboratedTypeModifier::pool_size * (SgElaboratedTypeModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElaboratedTypeModifierStroageClass
 * from its counterpart of SgElaboratedTypeModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElaboratedTypeModifier::initializeStorageClassArray(SgElaboratedTypeModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElaboratedTypeModifier::pools.begin(); block != SgElaboratedTypeModifier::pools.end(); ++block) {
        SgElaboratedTypeModifier* pointer{reinterpret_cast<SgElaboratedTypeModifier*>(*block)};
        for (unsigned i = 0; i < SgElaboratedTypeModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 2544 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLinkageModifier that takes its corresponding StorageClass as parameter
 */
SgLinkageModifier::SgLinkageModifier(const SgLinkageModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 2554 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLinkageModifier*
SgLinkageModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLinkageModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLinkageModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLinkageModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLinkageModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLinkageModifier)};
        unsigned long positionInPool{localIndex % SgLinkageModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLinkageModifier::pool_size};
        returnPointer = &(((SgLinkageModifier*)(SgLinkageModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLinkageModifier*
SgLinkageModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLinkageModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLinkageModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLinkageModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLinkageModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLinkageModifier)};
        unsigned long positionInPool{localIndex % SgLinkageModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLinkageModifier::pool_size};
        returnPointer = &(((SgLinkageModifier*)(SgLinkageModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLinkageModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLinkageModifier::pools.begin(); block != SgLinkageModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLinkageModifier*>(*block)};
        for (unsigned i = 0; i < SgLinkageModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLinkageModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLinkageModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLinkageModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgLinkageModifier::pools.begin(); block != SgLinkageModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLinkageModifier*>(*block)};
        for (unsigned i = 0; i < SgLinkageModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLinkageModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLinkageModifier::clearMemoryPool() {
    SgLinkageModifier* tempPointer{nullptr};
    if (SgLinkageModifier::pools.empty() == false) {
        auto block{SgLinkageModifier::pools.begin()};
        SgLinkageModifier::next_node = reinterpret_cast<SgLinkageModifier*>(*block);
        for (; block != SgLinkageModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLinkageModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLinkageModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLinkageModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLinkageModifier::pool_size-1]);
        }
    }
}

void
SgLinkageModifier::deleteMemoryPool() {
    for (auto p: SgLinkageModifier::pools) {
        ROSE_FREE(p);
    }
    SgLinkageModifier::next_node = nullptr;
    SgLinkageModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLinkageModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLinkageModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLinkageModifier)};
    for (size_t blockIndex = SgLinkageModifier::pools.size(); (blockIndex * SgLinkageModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLinkageModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLinkageModifier) = %" PRIuPTR " SgLinkageModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLinkageModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLinkageModifier),SgLinkageModifier::pool_size);
        }
#endif

        SgLinkageModifier* pointer{static_cast<SgLinkageModifier*>(ROSE_MALLOC(SgLinkageModifier::pool_size * sizeof(SgLinkageModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLinkageModifier::pool_size * sizeof(SgLinkageModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLinkageModifier::pool_size * sizeof(SgLinkageModifier));
#endif
        SgLinkageModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLinkageModifier::pool_size * sizeof(SgLinkageModifier), V_SgLinkageModifier));

        if (SgLinkageModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLinkageModifier* blkptr{reinterpret_cast<SgLinkageModifier*>(SgLinkageModifier::pools[blockIndex-1])};
                blkptr[SgLinkageModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLinkageModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLinkageModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLinkageModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLinkageModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLinkageModifier::pool_size - 1};
    SgLinkageModifier* testPointer{reinterpret_cast<SgLinkageModifier*>(SgLinkageModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLinkageModifier::pool_size * (SgLinkageModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLinkageModifierStroageClass
 * from its counterpart of SgLinkageModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLinkageModifier::initializeStorageClassArray(SgLinkageModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLinkageModifier::pools.begin(); block != SgLinkageModifier::pools.end(); ++block) {
        SgLinkageModifier* pointer{reinterpret_cast<SgLinkageModifier*>(*block)};
        for (unsigned i = 0; i < SgLinkageModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 2772 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBaseClassModifier that takes its corresponding StorageClass as parameter
 */
SgBaseClassModifier::SgBaseClassModifier(const SgBaseClassModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 2782 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;
    p_accessModifier = SgAccessModifier ( storageSource.storageOf_accessModifier);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBaseClassModifier*
SgBaseClassModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBaseClassModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBaseClassModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBaseClassModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBaseClassModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClassModifier)};
        unsigned long positionInPool{localIndex % SgBaseClassModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBaseClassModifier::pool_size};
        returnPointer = &(((SgBaseClassModifier*)(SgBaseClassModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBaseClassModifier*
SgBaseClassModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBaseClassModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBaseClassModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBaseClassModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBaseClassModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBaseClassModifier)};
        unsigned long positionInPool{localIndex % SgBaseClassModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBaseClassModifier::pool_size};
        returnPointer = &(((SgBaseClassModifier*)(SgBaseClassModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBaseClassModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBaseClassModifier::pools.begin(); block != SgBaseClassModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBaseClassModifier*>(*block)};
        for (unsigned i = 0; i < SgBaseClassModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBaseClassModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBaseClassModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBaseClassModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgBaseClassModifier::pools.begin(); block != SgBaseClassModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBaseClassModifier*>(*block)};
        for (unsigned i = 0; i < SgBaseClassModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBaseClassModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBaseClassModifier::clearMemoryPool() {
    SgBaseClassModifier* tempPointer{nullptr};
    if (SgBaseClassModifier::pools.empty() == false) {
        auto block{SgBaseClassModifier::pools.begin()};
        SgBaseClassModifier::next_node = reinterpret_cast<SgBaseClassModifier*>(*block);
        for (; block != SgBaseClassModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBaseClassModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBaseClassModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBaseClassModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBaseClassModifier::pool_size-1]);
        }
    }
}

void
SgBaseClassModifier::deleteMemoryPool() {
    for (auto p: SgBaseClassModifier::pools) {
        ROSE_FREE(p);
    }
    SgBaseClassModifier::next_node = nullptr;
    SgBaseClassModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBaseClassModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClassModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBaseClassModifier)};
    for (size_t blockIndex = SgBaseClassModifier::pools.size(); (blockIndex * SgBaseClassModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClassModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBaseClassModifier) = %" PRIuPTR " SgBaseClassModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClassModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBaseClassModifier),SgBaseClassModifier::pool_size);
        }
#endif

        SgBaseClassModifier* pointer{static_cast<SgBaseClassModifier*>(ROSE_MALLOC(SgBaseClassModifier::pool_size * sizeof(SgBaseClassModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBaseClassModifier::pool_size * sizeof(SgBaseClassModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBaseClassModifier::pool_size * sizeof(SgBaseClassModifier));
#endif
        SgBaseClassModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBaseClassModifier::pool_size * sizeof(SgBaseClassModifier), V_SgBaseClassModifier));

        if (SgBaseClassModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBaseClassModifier* blkptr{reinterpret_cast<SgBaseClassModifier*>(SgBaseClassModifier::pools[blockIndex-1])};
                blkptr[SgBaseClassModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBaseClassModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBaseClassModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBaseClassModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBaseClassModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBaseClassModifier::pool_size - 1};
    SgBaseClassModifier* testPointer{reinterpret_cast<SgBaseClassModifier*>(SgBaseClassModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBaseClassModifier::pool_size * (SgBaseClassModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBaseClassModifierStroageClass
 * from its counterpart of SgBaseClassModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBaseClassModifier::initializeStorageClassArray(SgBaseClassModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBaseClassModifier::pools.begin(); block != SgBaseClassModifier::pools.end(); ++block) {
        SgBaseClassModifier* pointer{reinterpret_cast<SgBaseClassModifier*>(*block)};
        for (unsigned i = 0; i < SgBaseClassModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 3001 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStructureModifier that takes its corresponding StorageClass as parameter
 */
SgStructureModifier::SgStructureModifier(const SgStructureModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 3011 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;
    p_bits_per_entry = storageSource.storageOf_bits_per_entry;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStructureModifier*
SgStructureModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStructureModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStructureModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStructureModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStructureModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStructureModifier)};
        unsigned long positionInPool{localIndex % SgStructureModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStructureModifier::pool_size};
        returnPointer = &(((SgStructureModifier*)(SgStructureModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStructureModifier*
SgStructureModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStructureModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStructureModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStructureModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStructureModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStructureModifier)};
        unsigned long positionInPool{localIndex % SgStructureModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStructureModifier::pool_size};
        returnPointer = &(((SgStructureModifier*)(SgStructureModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStructureModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStructureModifier::pools.begin(); block != SgStructureModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStructureModifier*>(*block)};
        for (unsigned i = 0; i < SgStructureModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStructureModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStructureModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStructureModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgStructureModifier::pools.begin(); block != SgStructureModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStructureModifier*>(*block)};
        for (unsigned i = 0; i < SgStructureModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStructureModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStructureModifier::clearMemoryPool() {
    SgStructureModifier* tempPointer{nullptr};
    if (SgStructureModifier::pools.empty() == false) {
        auto block{SgStructureModifier::pools.begin()};
        SgStructureModifier::next_node = reinterpret_cast<SgStructureModifier*>(*block);
        for (; block != SgStructureModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStructureModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStructureModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStructureModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStructureModifier::pool_size-1]);
        }
    }
}

void
SgStructureModifier::deleteMemoryPool() {
    for (auto p: SgStructureModifier::pools) {
        ROSE_FREE(p);
    }
    SgStructureModifier::next_node = nullptr;
    SgStructureModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStructureModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStructureModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStructureModifier)};
    for (size_t blockIndex = SgStructureModifier::pools.size(); (blockIndex * SgStructureModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStructureModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStructureModifier) = %" PRIuPTR " SgStructureModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStructureModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStructureModifier),SgStructureModifier::pool_size);
        }
#endif

        SgStructureModifier* pointer{static_cast<SgStructureModifier*>(ROSE_MALLOC(SgStructureModifier::pool_size * sizeof(SgStructureModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStructureModifier::pool_size * sizeof(SgStructureModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStructureModifier::pool_size * sizeof(SgStructureModifier));
#endif
        SgStructureModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStructureModifier::pool_size * sizeof(SgStructureModifier), V_SgStructureModifier));

        if (SgStructureModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStructureModifier* blkptr{reinterpret_cast<SgStructureModifier*>(SgStructureModifier::pools[blockIndex-1])};
                blkptr[SgStructureModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStructureModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStructureModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStructureModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStructureModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStructureModifier::pool_size - 1};
    SgStructureModifier* testPointer{reinterpret_cast<SgStructureModifier*>(SgStructureModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStructureModifier::pool_size * (SgStructureModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStructureModifierStroageClass
 * from its counterpart of SgStructureModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStructureModifier::initializeStorageClassArray(SgStructureModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStructureModifier::pools.begin(); block != SgStructureModifier::pools.end(); ++block) {
        SgStructureModifier* pointer{reinterpret_cast<SgStructureModifier*>(*block)};
        for (unsigned i = 0; i < SgStructureModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 3230 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeModifier that takes its corresponding StorageClass as parameter
 */
SgTypeModifier::SgTypeModifier(const SgTypeModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 3240 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifierVector = storageSource.storageOf_modifierVector.rebuildDataStoredInEasyStorageClass();
    p_upcModifier = SgUPC_AccessModifier ( storageSource.storageOf_upcModifier);
    p_structureModifier = SgStructureModifier ( storageSource.storageOf_structureModifier);
    p_constVolatileModifier = SgConstVolatileModifier ( storageSource.storageOf_constVolatileModifier);
    p_elaboratedTypeModifier = SgElaboratedTypeModifier ( storageSource.storageOf_elaboratedTypeModifier);
    p_gnu_extension_machine_mode = storageSource.storageOf_gnu_extension_machine_mode;
    p_gnu_attribute_alignment = storageSource.storageOf_gnu_attribute_alignment;
    p_gnu_attribute_sentinel = storageSource.storageOf_gnu_attribute_sentinel;
    p_address_space_value = storageSource.storageOf_address_space_value;
    p_vector_size = storageSource.storageOf_vector_size;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeModifier*
SgTypeModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeModifier)};
        unsigned long positionInPool{localIndex % SgTypeModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeModifier::pool_size};
        returnPointer = &(((SgTypeModifier*)(SgTypeModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeModifier*
SgTypeModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeModifier)};
        unsigned long positionInPool{localIndex % SgTypeModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeModifier::pool_size};
        returnPointer = &(((SgTypeModifier*)(SgTypeModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeModifier::pools.begin(); block != SgTypeModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeModifier*>(*block)};
        for (unsigned i = 0; i < SgTypeModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeModifier::pools.begin(); block != SgTypeModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeModifier*>(*block)};
        for (unsigned i = 0; i < SgTypeModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeModifier::clearMemoryPool() {
    SgTypeModifier* tempPointer{nullptr};
    if (SgTypeModifier::pools.empty() == false) {
        auto block{SgTypeModifier::pools.begin()};
        SgTypeModifier::next_node = reinterpret_cast<SgTypeModifier*>(*block);
        for (; block != SgTypeModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeModifier::pool_size-1]);
        }
    }
}

void
SgTypeModifier::deleteMemoryPool() {
    for (auto p: SgTypeModifier::pools) {
        ROSE_FREE(p);
    }
    SgTypeModifier::next_node = nullptr;
    SgTypeModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeModifier)};
    for (size_t blockIndex = SgTypeModifier::pools.size(); (blockIndex * SgTypeModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeModifier) = %" PRIuPTR " SgTypeModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeModifier),SgTypeModifier::pool_size);
        }
#endif

        SgTypeModifier* pointer{static_cast<SgTypeModifier*>(ROSE_MALLOC(SgTypeModifier::pool_size * sizeof(SgTypeModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeModifier::pool_size * sizeof(SgTypeModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeModifier::pool_size * sizeof(SgTypeModifier));
#endif
        SgTypeModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeModifier::pool_size * sizeof(SgTypeModifier), V_SgTypeModifier));

        if (SgTypeModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeModifier* blkptr{reinterpret_cast<SgTypeModifier*>(SgTypeModifier::pools[blockIndex-1])};
                blkptr[SgTypeModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeModifier::pool_size - 1};
    SgTypeModifier* testPointer{reinterpret_cast<SgTypeModifier*>(SgTypeModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeModifier::pool_size * (SgTypeModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeModifierStroageClass
 * from its counterpart of SgTypeModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeModifier::initializeStorageClassArray(SgTypeModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeModifier::pools.begin(); block != SgTypeModifier::pools.end(); ++block) {
        SgTypeModifier* pointer{reinterpret_cast<SgTypeModifier*>(*block)};
        for (unsigned i = 0; i < SgTypeModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 3467 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDeclarationModifier that takes its corresponding StorageClass as parameter
 */
SgDeclarationModifier::SgDeclarationModifier(const SgDeclarationModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 3477 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifierVector = storageSource.storageOf_modifierVector.rebuildDataStoredInEasyStorageClass();
    p_typeModifier = SgTypeModifier ( storageSource.storageOf_typeModifier);
    p_accessModifier = SgAccessModifier ( storageSource.storageOf_accessModifier);
    p_storageModifier = SgStorageModifier ( storageSource.storageOf_storageModifier);
    p_gnu_attribute_section_name = storageSource.storageOf_gnu_attribute_section_name.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_visability = storageSource.storageOf_gnu_attribute_visability;
    p_microsoft_uuid_string = storageSource.storageOf_microsoft_uuid_string.rebuildDataStoredInEasyStorageClass();
    p_microsoft_property_get_function_name = storageSource.storageOf_microsoft_property_get_function_name.rebuildDataStoredInEasyStorageClass();
    p_microsoft_property_put_function_name = storageSource.storageOf_microsoft_property_put_function_name.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDeclarationModifier*
SgDeclarationModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDeclarationModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationModifier)};
        unsigned long positionInPool{localIndex % SgDeclarationModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclarationModifier::pool_size};
        returnPointer = &(((SgDeclarationModifier*)(SgDeclarationModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDeclarationModifier*
SgDeclarationModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDeclarationModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDeclarationModifier)};
        unsigned long positionInPool{localIndex % SgDeclarationModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclarationModifier::pool_size};
        returnPointer = &(((SgDeclarationModifier*)(SgDeclarationModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDeclarationModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDeclarationModifier::pools.begin(); block != SgDeclarationModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclarationModifier*>(*block)};
        for (unsigned i = 0; i < SgDeclarationModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDeclarationModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDeclarationModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDeclarationModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgDeclarationModifier::pools.begin(); block != SgDeclarationModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclarationModifier*>(*block)};
        for (unsigned i = 0; i < SgDeclarationModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDeclarationModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDeclarationModifier::clearMemoryPool() {
    SgDeclarationModifier* tempPointer{nullptr};
    if (SgDeclarationModifier::pools.empty() == false) {
        auto block{SgDeclarationModifier::pools.begin()};
        SgDeclarationModifier::next_node = reinterpret_cast<SgDeclarationModifier*>(*block);
        for (; block != SgDeclarationModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDeclarationModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDeclarationModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDeclarationModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDeclarationModifier::pool_size-1]);
        }
    }
}

void
SgDeclarationModifier::deleteMemoryPool() {
    for (auto p: SgDeclarationModifier::pools) {
        ROSE_FREE(p);
    }
    SgDeclarationModifier::next_node = nullptr;
    SgDeclarationModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDeclarationModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationModifier)};
    for (size_t blockIndex = SgDeclarationModifier::pools.size(); (blockIndex * SgDeclarationModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationModifier) = %" PRIuPTR " SgDeclarationModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationModifier),SgDeclarationModifier::pool_size);
        }
#endif

        SgDeclarationModifier* pointer{static_cast<SgDeclarationModifier*>(ROSE_MALLOC(SgDeclarationModifier::pool_size * sizeof(SgDeclarationModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDeclarationModifier::pool_size * sizeof(SgDeclarationModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDeclarationModifier::pool_size * sizeof(SgDeclarationModifier));
#endif
        SgDeclarationModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDeclarationModifier::pool_size * sizeof(SgDeclarationModifier), V_SgDeclarationModifier));

        if (SgDeclarationModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDeclarationModifier* blkptr{reinterpret_cast<SgDeclarationModifier*>(SgDeclarationModifier::pools[blockIndex-1])};
                blkptr[SgDeclarationModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDeclarationModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDeclarationModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDeclarationModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDeclarationModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDeclarationModifier::pool_size - 1};
    SgDeclarationModifier* testPointer{reinterpret_cast<SgDeclarationModifier*>(SgDeclarationModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDeclarationModifier::pool_size * (SgDeclarationModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDeclarationModifierStroageClass
 * from its counterpart of SgDeclarationModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDeclarationModifier::initializeStorageClassArray(SgDeclarationModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDeclarationModifier::pools.begin(); block != SgDeclarationModifier::pools.end(); ++block) {
        SgDeclarationModifier* pointer{reinterpret_cast<SgDeclarationModifier*>(*block)};
        for (unsigned i = 0; i < SgDeclarationModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 3703 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOpenclAccessModeModifier that takes its corresponding StorageClass as parameter
 */
SgOpenclAccessModeModifier::SgOpenclAccessModeModifier(const SgOpenclAccessModeModifierStorageClass& storageSource)  : SgModifier (storageSource) {

/* #line 3713 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_modifier = storageSource.storageOf_modifier;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOpenclAccessModeModifier*
SgOpenclAccessModeModifier::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOpenclAccessModeModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOpenclAccessModeModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOpenclAccessModeModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOpenclAccessModeModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenclAccessModeModifier)};
        unsigned long positionInPool{localIndex % SgOpenclAccessModeModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOpenclAccessModeModifier::pool_size};
        returnPointer = &(((SgOpenclAccessModeModifier*)(SgOpenclAccessModeModifier::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOpenclAccessModeModifier*
SgOpenclAccessModeModifier::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOpenclAccessModeModifier* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOpenclAccessModeModifier)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOpenclAccessModeModifier + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOpenclAccessModeModifier)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOpenclAccessModeModifier)};
        unsigned long positionInPool{localIndex % SgOpenclAccessModeModifier::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOpenclAccessModeModifier::pool_size};
        returnPointer = &(((SgOpenclAccessModeModifier*)(SgOpenclAccessModeModifier::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOpenclAccessModeModifier::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOpenclAccessModeModifier::pools.begin(); block != SgOpenclAccessModeModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOpenclAccessModeModifier*>(*block)};
        for (unsigned i = 0; i < SgOpenclAccessModeModifier::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOpenclAccessModeModifier*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOpenclAccessModeModifier::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOpenclAccessModeModifier* pointerOfLinkedList{nullptr};
    for (auto block = SgOpenclAccessModeModifier::pools.begin(); block != SgOpenclAccessModeModifier::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOpenclAccessModeModifier*>(*block)};
        for (unsigned i = 0; i < SgOpenclAccessModeModifier::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOpenclAccessModeModifier::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOpenclAccessModeModifier::clearMemoryPool() {
    SgOpenclAccessModeModifier* tempPointer{nullptr};
    if (SgOpenclAccessModeModifier::pools.empty() == false) {
        auto block{SgOpenclAccessModeModifier::pools.begin()};
        SgOpenclAccessModeModifier::next_node = reinterpret_cast<SgOpenclAccessModeModifier*>(*block);
        for (; block != SgOpenclAccessModeModifier::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOpenclAccessModeModifier*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOpenclAccessModeModifier::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOpenclAccessModeModifier::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOpenclAccessModeModifier::pool_size-1]);
        }
    }
}

void
SgOpenclAccessModeModifier::deleteMemoryPool() {
    for (auto p: SgOpenclAccessModeModifier::pools) {
        ROSE_FREE(p);
    }
    SgOpenclAccessModeModifier::next_node = nullptr;
    SgOpenclAccessModeModifier::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOpenclAccessModeModifier::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenclAccessModeModifier) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOpenclAccessModeModifier)};
    for (size_t blockIndex = SgOpenclAccessModeModifier::pools.size(); (blockIndex * SgOpenclAccessModeModifier::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenclAccessModeModifier) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOpenclAccessModeModifier) = %" PRIuPTR " SgOpenclAccessModeModifier::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenclAccessModeModifier),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOpenclAccessModeModifier),SgOpenclAccessModeModifier::pool_size);
        }
#endif

        SgOpenclAccessModeModifier* pointer{static_cast<SgOpenclAccessModeModifier*>(ROSE_MALLOC(SgOpenclAccessModeModifier::pool_size * sizeof(SgOpenclAccessModeModifier)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOpenclAccessModeModifier::pool_size * sizeof(SgOpenclAccessModeModifier));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOpenclAccessModeModifier::pool_size * sizeof(SgOpenclAccessModeModifier));
#endif
        SgOpenclAccessModeModifier::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOpenclAccessModeModifier::pool_size * sizeof(SgOpenclAccessModeModifier), V_SgOpenclAccessModeModifier));

        if (SgOpenclAccessModeModifier::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOpenclAccessModeModifier* blkptr{reinterpret_cast<SgOpenclAccessModeModifier*>(SgOpenclAccessModeModifier::pools[blockIndex-1])};
                blkptr[SgOpenclAccessModeModifier::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOpenclAccessModeModifier::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOpenclAccessModeModifier::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOpenclAccessModeModifier::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOpenclAccessModeModifier::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOpenclAccessModeModifier::pool_size - 1};
    SgOpenclAccessModeModifier* testPointer{reinterpret_cast<SgOpenclAccessModeModifier*>(SgOpenclAccessModeModifier::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOpenclAccessModeModifier::pool_size * (SgOpenclAccessModeModifier::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOpenclAccessModeModifierStroageClass
 * from its counterpart of SgOpenclAccessModeModifier. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOpenclAccessModeModifier::initializeStorageClassArray(SgOpenclAccessModeModifierStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOpenclAccessModeModifier::pools.begin(); block != SgOpenclAccessModeModifier::pools.end(); ++block) {
        SgOpenclAccessModeModifier* pointer{reinterpret_cast<SgOpenclAccessModeModifier*>(*block)};
        for (unsigned i = 0; i < SgOpenclAccessModeModifier::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 3931 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgName that takes its corresponding StorageClass as parameter
 */
SgName::SgName(const SgNameStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 3941 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_char = storageSource.storageOf_char.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgName*
SgName::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgName* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgName)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgName + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgName)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgName)};
        unsigned long positionInPool{localIndex % SgName::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgName::pool_size};
        returnPointer = &(((SgName*)(SgName::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgName*
SgName::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgName* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgName)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgName + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgName)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgName)};
        unsigned long positionInPool{localIndex % SgName::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgName::pool_size};
        returnPointer = &(((SgName*)(SgName::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgName::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgName::pools.begin(); block != SgName::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgName*>(*block)};
        for (unsigned i = 0; i < SgName::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgName*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgName::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgName* pointerOfLinkedList{nullptr};
    for (auto block = SgName::pools.begin(); block != SgName::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgName*>(*block)};
        for (unsigned i = 0; i < SgName::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgName::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgName::clearMemoryPool() {
    SgName* tempPointer{nullptr};
    if (SgName::pools.empty() == false) {
        auto block{SgName::pools.begin()};
        SgName::next_node = reinterpret_cast<SgName*>(*block);
        for (; block != SgName::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgName*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgName::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgName::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgName::pool_size-1]);
        }
    }
}

void
SgName::deleteMemoryPool() {
    for (auto p: SgName::pools) {
        ROSE_FREE(p);
    }
    SgName::next_node = nullptr;
    SgName::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgName::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgName) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgName)};
    for (size_t blockIndex = SgName::pools.size(); (blockIndex * SgName::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgName) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgName) = %" PRIuPTR " SgName::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgName),AST_FILE_IO::getPoolSizeOfNewAst(V_SgName),SgName::pool_size);
        }
#endif

        SgName* pointer{static_cast<SgName*>(ROSE_MALLOC(SgName::pool_size * sizeof(SgName)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgName::pool_size * sizeof(SgName));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgName::pool_size * sizeof(SgName));
#endif
        SgName::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgName::pool_size * sizeof(SgName), V_SgName));

        if (SgName::next_node != nullptr) {
            if (blockIndex > 0) {
                SgName* blkptr{reinterpret_cast<SgName*>(SgName::pools[blockIndex-1])};
                blkptr[SgName::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgName::next_node = pointer;
        }

        for (unsigned i = 0; i < SgName::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgName::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgName::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgName::pool_size - 1};
    SgName* testPointer{reinterpret_cast<SgName*>(SgName::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgName::pool_size * (SgName::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNameStroageClass
 * from its counterpart of SgName. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgName::initializeStorageClassArray(SgNameStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgName::pools.begin(); block != SgName::pools.end(); ++block) {
        SgName* pointer{reinterpret_cast<SgName*>(*block)};
        for (unsigned i = 0; i < SgName::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 4159 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSymbolTable that takes its corresponding StorageClass as parameter
 */
SgSymbolTable::SgSymbolTable(const SgSymbolTableStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 4169 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_no_name = storageSource.storageOf_no_name;
    p_table = storageSource.storageOf_table.rebuildDataStoredInEasyStorageClass();
    rose_hash_multimap::iterator it;
    for (it = p_table->begin(); it != p_table->end(); ++it) {
        it->second = (SgSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex( (unsigned long)(it->second)));
    }
    p_symbolSet = storageSource.storageOf_symbolSet.rebuildDataStoredInEasyStorageClass();
    p_case_insensitive = storageSource.storageOf_case_insensitive;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSymbolTable*
SgSymbolTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSymbolTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSymbolTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSymbolTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSymbolTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbolTable)};
        unsigned long positionInPool{localIndex % SgSymbolTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSymbolTable::pool_size};
        returnPointer = &(((SgSymbolTable*)(SgSymbolTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSymbolTable*
SgSymbolTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSymbolTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSymbolTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSymbolTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSymbolTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSymbolTable)};
        unsigned long positionInPool{localIndex % SgSymbolTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSymbolTable::pool_size};
        returnPointer = &(((SgSymbolTable*)(SgSymbolTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSymbolTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSymbolTable::pools.begin(); block != SgSymbolTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSymbolTable*>(*block)};
        for (unsigned i = 0; i < SgSymbolTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSymbolTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSymbolTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSymbolTable* pointerOfLinkedList{nullptr};
    for (auto block = SgSymbolTable::pools.begin(); block != SgSymbolTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSymbolTable*>(*block)};
        for (unsigned i = 0; i < SgSymbolTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSymbolTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSymbolTable::clearMemoryPool() {
    SgSymbolTable* tempPointer{nullptr};
    if (SgSymbolTable::pools.empty() == false) {
        auto block{SgSymbolTable::pools.begin()};
        SgSymbolTable::next_node = reinterpret_cast<SgSymbolTable*>(*block);
        for (; block != SgSymbolTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSymbolTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSymbolTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSymbolTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSymbolTable::pool_size-1]);
        }
    }
}

void
SgSymbolTable::deleteMemoryPool() {
    for (auto p: SgSymbolTable::pools) {
        ROSE_FREE(p);
    }
    SgSymbolTable::next_node = nullptr;
    SgSymbolTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSymbolTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbolTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSymbolTable)};
    for (size_t blockIndex = SgSymbolTable::pools.size(); (blockIndex * SgSymbolTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbolTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSymbolTable) = %" PRIuPTR " SgSymbolTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbolTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSymbolTable),SgSymbolTable::pool_size);
        }
#endif

        SgSymbolTable* pointer{static_cast<SgSymbolTable*>(ROSE_MALLOC(SgSymbolTable::pool_size * sizeof(SgSymbolTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSymbolTable::pool_size * sizeof(SgSymbolTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSymbolTable::pool_size * sizeof(SgSymbolTable));
#endif
        SgSymbolTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSymbolTable::pool_size * sizeof(SgSymbolTable), V_SgSymbolTable));

        if (SgSymbolTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSymbolTable* blkptr{reinterpret_cast<SgSymbolTable*>(SgSymbolTable::pools[blockIndex-1])};
                blkptr[SgSymbolTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSymbolTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSymbolTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSymbolTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSymbolTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSymbolTable::pool_size - 1};
    SgSymbolTable* testPointer{reinterpret_cast<SgSymbolTable*>(SgSymbolTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSymbolTable::pool_size * (SgSymbolTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSymbolTableStroageClass
 * from its counterpart of SgSymbolTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSymbolTable::initializeStorageClassArray(SgSymbolTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSymbolTable::pools.begin(); block != SgSymbolTable::pools.end(); ++block) {
        SgSymbolTable* pointer{reinterpret_cast<SgSymbolTable*>(*block)};
        for (unsigned i = 0; i < SgSymbolTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 4395 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAttribute that takes its corresponding StorageClass as parameter
 */
SgAttribute::SgAttribute(const SgAttributeStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 4405 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAttribute*
SgAttribute::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAttribute)};
        unsigned long positionInPool{localIndex % SgAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAttribute::pool_size};
        returnPointer = &(((SgAttribute*)(SgAttribute::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAttribute*
SgAttribute::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAttribute)};
        unsigned long positionInPool{localIndex % SgAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAttribute::pool_size};
        returnPointer = &(((SgAttribute*)(SgAttribute::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAttribute::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAttribute::pools.begin(); block != SgAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAttribute*>(*block)};
        for (unsigned i = 0; i < SgAttribute::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAttribute*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAttribute::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAttribute* pointerOfLinkedList{nullptr};
    for (auto block = SgAttribute::pools.begin(); block != SgAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAttribute*>(*block)};
        for (unsigned i = 0; i < SgAttribute::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAttribute::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAttribute::clearMemoryPool() {
    SgAttribute* tempPointer{nullptr};
    if (SgAttribute::pools.empty() == false) {
        auto block{SgAttribute::pools.begin()};
        SgAttribute::next_node = reinterpret_cast<SgAttribute*>(*block);
        for (; block != SgAttribute::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAttribute*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAttribute::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAttribute::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAttribute::pool_size-1]);
        }
    }
}

void
SgAttribute::deleteMemoryPool() {
    for (auto p: SgAttribute::pools) {
        ROSE_FREE(p);
    }
    SgAttribute::next_node = nullptr;
    SgAttribute::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAttribute::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAttribute) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAttribute)};
    for (size_t blockIndex = SgAttribute::pools.size(); (blockIndex * SgAttribute::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAttribute) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAttribute) = %" PRIuPTR " SgAttribute::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAttribute),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAttribute),SgAttribute::pool_size);
        }
#endif

        SgAttribute* pointer{static_cast<SgAttribute*>(ROSE_MALLOC(SgAttribute::pool_size * sizeof(SgAttribute)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAttribute::pool_size * sizeof(SgAttribute));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAttribute::pool_size * sizeof(SgAttribute));
#endif
        SgAttribute::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAttribute::pool_size * sizeof(SgAttribute), V_SgAttribute));

        if (SgAttribute::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAttribute* blkptr{reinterpret_cast<SgAttribute*>(SgAttribute::pools[blockIndex-1])};
                blkptr[SgAttribute::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAttribute::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAttribute::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAttribute::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAttribute::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAttribute::pool_size - 1};
    SgAttribute* testPointer{reinterpret_cast<SgAttribute*>(SgAttribute::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAttribute::pool_size * (SgAttribute::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAttributeStroageClass
 * from its counterpart of SgAttribute. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAttribute::initializeStorageClassArray(SgAttributeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAttribute::pools.begin(); block != SgAttribute::pools.end(); ++block) {
        SgAttribute* pointer{reinterpret_cast<SgAttribute*>(*block)};
        for (unsigned i = 0; i < SgAttribute::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 4623 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPragma that takes its corresponding StorageClass as parameter
 */
SgPragma::SgPragma(const SgPragmaStorageClass& storageSource)  : SgAttribute (storageSource) {

/* #line 4633 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_startOfConstruct = (Sg_File_Info*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_startOfConstruct));
     p_endOfConstruct = (Sg_File_Info*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_endOfConstruct));
    p_printed = storageSource.storageOf_printed;
     p_args = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_args));
     p_associatedStatement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_associatedStatement));
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPragma*
SgPragma::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPragma* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPragma)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPragma + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPragma)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPragma)};
        unsigned long positionInPool{localIndex % SgPragma::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPragma::pool_size};
        returnPointer = &(((SgPragma*)(SgPragma::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPragma*
SgPragma::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPragma* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPragma)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPragma + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPragma)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPragma)};
        unsigned long positionInPool{localIndex % SgPragma::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPragma::pool_size};
        returnPointer = &(((SgPragma*)(SgPragma::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPragma::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPragma::pools.begin(); block != SgPragma::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPragma*>(*block)};
        for (unsigned i = 0; i < SgPragma::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPragma*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPragma::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPragma* pointerOfLinkedList{nullptr};
    for (auto block = SgPragma::pools.begin(); block != SgPragma::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPragma*>(*block)};
        for (unsigned i = 0; i < SgPragma::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPragma::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPragma::clearMemoryPool() {
    SgPragma* tempPointer{nullptr};
    if (SgPragma::pools.empty() == false) {
        auto block{SgPragma::pools.begin()};
        SgPragma::next_node = reinterpret_cast<SgPragma*>(*block);
        for (; block != SgPragma::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPragma*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPragma::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPragma::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPragma::pool_size-1]);
        }
    }
}

void
SgPragma::deleteMemoryPool() {
    for (auto p: SgPragma::pools) {
        ROSE_FREE(p);
    }
    SgPragma::next_node = nullptr;
    SgPragma::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPragma::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPragma) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPragma)};
    for (size_t blockIndex = SgPragma::pools.size(); (blockIndex * SgPragma::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPragma) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPragma) = %" PRIuPTR " SgPragma::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPragma),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPragma),SgPragma::pool_size);
        }
#endif

        SgPragma* pointer{static_cast<SgPragma*>(ROSE_MALLOC(SgPragma::pool_size * sizeof(SgPragma)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPragma::pool_size * sizeof(SgPragma));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPragma::pool_size * sizeof(SgPragma));
#endif
        SgPragma::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPragma::pool_size * sizeof(SgPragma), V_SgPragma));

        if (SgPragma::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPragma* blkptr{reinterpret_cast<SgPragma*>(SgPragma::pools[blockIndex-1])};
                blkptr[SgPragma::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPragma::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPragma::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPragma::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPragma::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPragma::pool_size - 1};
    SgPragma* testPointer{reinterpret_cast<SgPragma*>(SgPragma::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPragma::pool_size * (SgPragma::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPragmaStroageClass
 * from its counterpart of SgPragma. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPragma::initializeStorageClassArray(SgPragmaStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPragma::pools.begin(); block != SgPragma::pools.end(); ++block) {
        SgPragma* pointer{reinterpret_cast<SgPragma*>(*block)};
        for (unsigned i = 0; i < SgPragma::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 4856 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBitAttribute that takes its corresponding StorageClass as parameter
 */
SgBitAttribute::SgBitAttribute(const SgBitAttributeStorageClass& storageSource)  : SgAttribute (storageSource) {

/* #line 4866 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_bitflag = storageSource.storageOf_bitflag;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBitAttribute*
SgBitAttribute::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBitAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAttribute)};
        unsigned long positionInPool{localIndex % SgBitAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitAttribute::pool_size};
        returnPointer = &(((SgBitAttribute*)(SgBitAttribute::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBitAttribute*
SgBitAttribute::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBitAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBitAttribute)};
        unsigned long positionInPool{localIndex % SgBitAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitAttribute::pool_size};
        returnPointer = &(((SgBitAttribute*)(SgBitAttribute::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBitAttribute::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBitAttribute::pools.begin(); block != SgBitAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitAttribute*>(*block)};
        for (unsigned i = 0; i < SgBitAttribute::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBitAttribute*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBitAttribute::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBitAttribute* pointerOfLinkedList{nullptr};
    for (auto block = SgBitAttribute::pools.begin(); block != SgBitAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitAttribute*>(*block)};
        for (unsigned i = 0; i < SgBitAttribute::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBitAttribute::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBitAttribute::clearMemoryPool() {
    SgBitAttribute* tempPointer{nullptr};
    if (SgBitAttribute::pools.empty() == false) {
        auto block{SgBitAttribute::pools.begin()};
        SgBitAttribute::next_node = reinterpret_cast<SgBitAttribute*>(*block);
        for (; block != SgBitAttribute::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBitAttribute*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBitAttribute::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBitAttribute::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBitAttribute::pool_size-1]);
        }
    }
}

void
SgBitAttribute::deleteMemoryPool() {
    for (auto p: SgBitAttribute::pools) {
        ROSE_FREE(p);
    }
    SgBitAttribute::next_node = nullptr;
    SgBitAttribute::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBitAttribute::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAttribute) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitAttribute)};
    for (size_t blockIndex = SgBitAttribute::pools.size(); (blockIndex * SgBitAttribute::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAttribute) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitAttribute) = %" PRIuPTR " SgBitAttribute::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAttribute),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitAttribute),SgBitAttribute::pool_size);
        }
#endif

        SgBitAttribute* pointer{static_cast<SgBitAttribute*>(ROSE_MALLOC(SgBitAttribute::pool_size * sizeof(SgBitAttribute)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBitAttribute::pool_size * sizeof(SgBitAttribute));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBitAttribute::pool_size * sizeof(SgBitAttribute));
#endif
        SgBitAttribute::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBitAttribute::pool_size * sizeof(SgBitAttribute), V_SgBitAttribute));

        if (SgBitAttribute::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBitAttribute* blkptr{reinterpret_cast<SgBitAttribute*>(SgBitAttribute::pools[blockIndex-1])};
                blkptr[SgBitAttribute::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBitAttribute::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBitAttribute::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBitAttribute::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBitAttribute::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBitAttribute::pool_size - 1};
    SgBitAttribute* testPointer{reinterpret_cast<SgBitAttribute*>(SgBitAttribute::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBitAttribute::pool_size * (SgBitAttribute::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBitAttributeStroageClass
 * from its counterpart of SgBitAttribute. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBitAttribute::initializeStorageClassArray(SgBitAttributeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBitAttribute::pools.begin(); block != SgBitAttribute::pools.end(); ++block) {
        SgBitAttribute* pointer{reinterpret_cast<SgBitAttribute*>(*block)};
        for (unsigned i = 0; i < SgBitAttribute::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 5084 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFuncDecl_attr that takes its corresponding StorageClass as parameter
 */
SgFuncDecl_attr::SgFuncDecl_attr(const SgFuncDecl_attrStorageClass& storageSource)  : SgBitAttribute (storageSource) {

/* #line 5094 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFuncDecl_attr*
SgFuncDecl_attr::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFuncDecl_attr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFuncDecl_attr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFuncDecl_attr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFuncDecl_attr)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFuncDecl_attr)};
        unsigned long positionInPool{localIndex % SgFuncDecl_attr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFuncDecl_attr::pool_size};
        returnPointer = &(((SgFuncDecl_attr*)(SgFuncDecl_attr::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFuncDecl_attr*
SgFuncDecl_attr::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFuncDecl_attr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFuncDecl_attr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFuncDecl_attr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFuncDecl_attr)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFuncDecl_attr)};
        unsigned long positionInPool{localIndex % SgFuncDecl_attr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFuncDecl_attr::pool_size};
        returnPointer = &(((SgFuncDecl_attr*)(SgFuncDecl_attr::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFuncDecl_attr::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFuncDecl_attr::pools.begin(); block != SgFuncDecl_attr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFuncDecl_attr*>(*block)};
        for (unsigned i = 0; i < SgFuncDecl_attr::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFuncDecl_attr*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFuncDecl_attr::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFuncDecl_attr* pointerOfLinkedList{nullptr};
    for (auto block = SgFuncDecl_attr::pools.begin(); block != SgFuncDecl_attr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFuncDecl_attr*>(*block)};
        for (unsigned i = 0; i < SgFuncDecl_attr::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFuncDecl_attr::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFuncDecl_attr::clearMemoryPool() {
    SgFuncDecl_attr* tempPointer{nullptr};
    if (SgFuncDecl_attr::pools.empty() == false) {
        auto block{SgFuncDecl_attr::pools.begin()};
        SgFuncDecl_attr::next_node = reinterpret_cast<SgFuncDecl_attr*>(*block);
        for (; block != SgFuncDecl_attr::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFuncDecl_attr*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFuncDecl_attr::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFuncDecl_attr::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFuncDecl_attr::pool_size-1]);
        }
    }
}

void
SgFuncDecl_attr::deleteMemoryPool() {
    for (auto p: SgFuncDecl_attr::pools) {
        ROSE_FREE(p);
    }
    SgFuncDecl_attr::next_node = nullptr;
    SgFuncDecl_attr::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFuncDecl_attr::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFuncDecl_attr) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFuncDecl_attr)};
    for (size_t blockIndex = SgFuncDecl_attr::pools.size(); (blockIndex * SgFuncDecl_attr::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFuncDecl_attr) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFuncDecl_attr) = %" PRIuPTR " SgFuncDecl_attr::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFuncDecl_attr),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFuncDecl_attr),SgFuncDecl_attr::pool_size);
        }
#endif

        SgFuncDecl_attr* pointer{static_cast<SgFuncDecl_attr*>(ROSE_MALLOC(SgFuncDecl_attr::pool_size * sizeof(SgFuncDecl_attr)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFuncDecl_attr::pool_size * sizeof(SgFuncDecl_attr));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFuncDecl_attr::pool_size * sizeof(SgFuncDecl_attr));
#endif
        SgFuncDecl_attr::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFuncDecl_attr::pool_size * sizeof(SgFuncDecl_attr), V_SgFuncDecl_attr));

        if (SgFuncDecl_attr::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFuncDecl_attr* blkptr{reinterpret_cast<SgFuncDecl_attr*>(SgFuncDecl_attr::pools[blockIndex-1])};
                blkptr[SgFuncDecl_attr::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFuncDecl_attr::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFuncDecl_attr::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFuncDecl_attr::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFuncDecl_attr::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFuncDecl_attr::pool_size - 1};
    SgFuncDecl_attr* testPointer{reinterpret_cast<SgFuncDecl_attr*>(SgFuncDecl_attr::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFuncDecl_attr::pool_size * (SgFuncDecl_attr::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFuncDecl_attrStroageClass
 * from its counterpart of SgFuncDecl_attr. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFuncDecl_attr::initializeStorageClassArray(SgFuncDecl_attrStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFuncDecl_attr::pools.begin(); block != SgFuncDecl_attr::pools.end(); ++block) {
        SgFuncDecl_attr* pointer{reinterpret_cast<SgFuncDecl_attr*>(*block)};
        for (unsigned i = 0; i < SgFuncDecl_attr::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 5311 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClassDecl_attr that takes its corresponding StorageClass as parameter
 */
SgClassDecl_attr::SgClassDecl_attr(const SgClassDecl_attrStorageClass& storageSource)  : SgBitAttribute (storageSource) {

/* #line 5321 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClassDecl_attr*
SgClassDecl_attr::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClassDecl_attr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDecl_attr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDecl_attr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDecl_attr)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDecl_attr)};
        unsigned long positionInPool{localIndex % SgClassDecl_attr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassDecl_attr::pool_size};
        returnPointer = &(((SgClassDecl_attr*)(SgClassDecl_attr::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClassDecl_attr*
SgClassDecl_attr::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClassDecl_attr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDecl_attr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDecl_attr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDecl_attr)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClassDecl_attr)};
        unsigned long positionInPool{localIndex % SgClassDecl_attr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassDecl_attr::pool_size};
        returnPointer = &(((SgClassDecl_attr*)(SgClassDecl_attr::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClassDecl_attr::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClassDecl_attr::pools.begin(); block != SgClassDecl_attr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassDecl_attr*>(*block)};
        for (unsigned i = 0; i < SgClassDecl_attr::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClassDecl_attr*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClassDecl_attr::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClassDecl_attr* pointerOfLinkedList{nullptr};
    for (auto block = SgClassDecl_attr::pools.begin(); block != SgClassDecl_attr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassDecl_attr*>(*block)};
        for (unsigned i = 0; i < SgClassDecl_attr::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClassDecl_attr::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClassDecl_attr::clearMemoryPool() {
    SgClassDecl_attr* tempPointer{nullptr};
    if (SgClassDecl_attr::pools.empty() == false) {
        auto block{SgClassDecl_attr::pools.begin()};
        SgClassDecl_attr::next_node = reinterpret_cast<SgClassDecl_attr*>(*block);
        for (; block != SgClassDecl_attr::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClassDecl_attr*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClassDecl_attr::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClassDecl_attr::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClassDecl_attr::pool_size-1]);
        }
    }
}

void
SgClassDecl_attr::deleteMemoryPool() {
    for (auto p: SgClassDecl_attr::pools) {
        ROSE_FREE(p);
    }
    SgClassDecl_attr::next_node = nullptr;
    SgClassDecl_attr::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClassDecl_attr::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDecl_attr) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDecl_attr)};
    for (size_t blockIndex = SgClassDecl_attr::pools.size(); (blockIndex * SgClassDecl_attr::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDecl_attr) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDecl_attr) = %" PRIuPTR " SgClassDecl_attr::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDecl_attr),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDecl_attr),SgClassDecl_attr::pool_size);
        }
#endif

        SgClassDecl_attr* pointer{static_cast<SgClassDecl_attr*>(ROSE_MALLOC(SgClassDecl_attr::pool_size * sizeof(SgClassDecl_attr)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClassDecl_attr::pool_size * sizeof(SgClassDecl_attr));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClassDecl_attr::pool_size * sizeof(SgClassDecl_attr));
#endif
        SgClassDecl_attr::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClassDecl_attr::pool_size * sizeof(SgClassDecl_attr), V_SgClassDecl_attr));

        if (SgClassDecl_attr::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClassDecl_attr* blkptr{reinterpret_cast<SgClassDecl_attr*>(SgClassDecl_attr::pools[blockIndex-1])};
                blkptr[SgClassDecl_attr::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClassDecl_attr::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClassDecl_attr::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClassDecl_attr::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClassDecl_attr::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClassDecl_attr::pool_size - 1};
    SgClassDecl_attr* testPointer{reinterpret_cast<SgClassDecl_attr*>(SgClassDecl_attr::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClassDecl_attr::pool_size * (SgClassDecl_attr::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClassDecl_attrStroageClass
 * from its counterpart of SgClassDecl_attr. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClassDecl_attr::initializeStorageClassArray(SgClassDecl_attrStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClassDecl_attr::pools.begin(); block != SgClassDecl_attr::pools.end(); ++block) {
        SgClassDecl_attr* pointer{reinterpret_cast<SgClassDecl_attr*>(*block)};
        for (unsigned i = 0; i < SgClassDecl_attr::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 5538 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node Sg_File_Info that takes its corresponding StorageClass as parameter
 */
Sg_File_Info::Sg_File_Info(const Sg_File_InfoStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 5548 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_file_id = storageSource.storageOf_file_id;
    p_line = storageSource.storageOf_line;
    p_col = storageSource.storageOf_col;
    p_classificationBitField = storageSource.storageOf_classificationBitField;
    p_physical_file_id = storageSource.storageOf_physical_file_id;
    p_physical_line = storageSource.storageOf_physical_line;
    p_source_sequence_number = storageSource.storageOf_source_sequence_number;
    p_fileIDsToUnparse = storageSource.storageOf_fileIDsToUnparse.rebuildDataStoredInEasyStorageClass();
    p_fileLineNumbersToUnparse = storageSource.storageOf_fileLineNumbersToUnparse.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
Sg_File_Info*
Sg_File_Info::getPointerFromGlobalIndex(unsigned long globalIndex) {
    Sg_File_Info* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_Sg_File_Info)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_Sg_File_Info + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_Sg_File_Info)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_Sg_File_Info)};
        unsigned long positionInPool{localIndex % Sg_File_Info::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / Sg_File_Info::pool_size};
        returnPointer = &(((Sg_File_Info*)(Sg_File_Info::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
Sg_File_Info*
Sg_File_Info::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    Sg_File_Info* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_Sg_File_Info)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_Sg_File_Info + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_Sg_File_Info)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_Sg_File_Info)};
        unsigned long positionInPool{localIndex % Sg_File_Info::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / Sg_File_Info::pool_size};
        returnPointer = &(((Sg_File_Info*)(Sg_File_Info::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
Sg_File_Info::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = Sg_File_Info::pools.begin(); block != Sg_File_Info::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<Sg_File_Info*>(*block)};
        for (unsigned i = 0; i < Sg_File_Info::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((Sg_File_Info*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
Sg_File_Info::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    Sg_File_Info* pointerOfLinkedList{nullptr};
    for (auto block = Sg_File_Info::pools.begin(); block != Sg_File_Info::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<Sg_File_Info*>(*block)};
        for (unsigned i = 0; i < Sg_File_Info::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    Sg_File_Info::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
Sg_File_Info::clearMemoryPool() {
    Sg_File_Info* tempPointer{nullptr};
    if (Sg_File_Info::pools.empty() == false) {
        auto block{Sg_File_Info::pools.begin()};
        Sg_File_Info::next_node = reinterpret_cast<Sg_File_Info*>(*block);
        for (; block != Sg_File_Info::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<Sg_File_Info*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < Sg_File_Info::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[Sg_File_Info::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[Sg_File_Info::pool_size-1]);
        }
    }
}

void
Sg_File_Info::deleteMemoryPool() {
    for (auto p: Sg_File_Info::pools) {
        ROSE_FREE(p);
    }
    Sg_File_Info::next_node = nullptr;
    Sg_File_Info::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
Sg_File_Info::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_Sg_File_Info) + AST_FILE_IO::getPoolSizeOfNewAst(V_Sg_File_Info)};
    for (size_t blockIndex = Sg_File_Info::pools.size(); (blockIndex * Sg_File_Info::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_Sg_File_Info) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_Sg_File_Info) = %" PRIuPTR " Sg_File_Info::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_Sg_File_Info),AST_FILE_IO::getPoolSizeOfNewAst(V_Sg_File_Info),Sg_File_Info::pool_size);
        }
#endif

        Sg_File_Info* pointer{static_cast<Sg_File_Info*>(ROSE_MALLOC(Sg_File_Info::pool_size * sizeof(Sg_File_Info)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, Sg_File_Info::pool_size * sizeof(Sg_File_Info));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, Sg_File_Info::pool_size * sizeof(Sg_File_Info));
#endif
        Sg_File_Info::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), Sg_File_Info::pool_size * sizeof(Sg_File_Info), V_Sg_File_Info));

        if (Sg_File_Info::next_node != nullptr) {
            if (blockIndex > 0) {
                Sg_File_Info* blkptr{reinterpret_cast<Sg_File_Info*>(Sg_File_Info::pools[blockIndex-1])};
                blkptr[Sg_File_Info::pool_size-1].set_freepointer(pointer);
            }
        } else {
            Sg_File_Info::next_node = pointer;
        }

        for (unsigned i = 0; i < Sg_File_Info::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[Sg_File_Info::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
Sg_File_Info::getNumberOfLastValidPointer() {
    unsigned long localIndex{Sg_File_Info::pool_size - 1};
    Sg_File_Info* testPointer{reinterpret_cast<Sg_File_Info*>(Sg_File_Info::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + Sg_File_Info::pool_size * (Sg_File_Info::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class Sg_File_InfoStroageClass
 * from its counterpart of Sg_File_Info. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
Sg_File_Info::initializeStorageClassArray(Sg_File_InfoStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = Sg_File_Info::pools.begin(); block != Sg_File_Info::pools.end(); ++block) {
        Sg_File_Info* pointer{reinterpret_cast<Sg_File_Info*>(*block)};
        for (unsigned i = 0; i < Sg_File_Info::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 5774 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFile that takes its corresponding StorageClass as parameter
 */
SgFile::SgFile(const SgFileStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 5784 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_startOfConstruct = (Sg_File_Info*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_startOfConstruct));
    p_originalCommandLineArgumentList = storageSource.storageOf_originalCommandLineArgumentList.rebuildDataStoredInEasyStorageClass();
    p_verbose = storageSource.storageOf_verbose;
    p_output_warnings = storageSource.storageOf_output_warnings;
    p_C_only = storageSource.storageOf_C_only;
    p_Cxx_only = storageSource.storageOf_Cxx_only;
    p_Fortran_only = storageSource.storageOf_Fortran_only;
    p_CoArrayFortran_only = storageSource.storageOf_CoArrayFortran_only;
    p_upc_threads = storageSource.storageOf_upc_threads;
    p_Java_only = storageSource.storageOf_Java_only;
    p_Jvm_only = storageSource.storageOf_Jvm_only;
    p_Jovial_only = storageSource.storageOf_Jovial_only;
    p_PHP_only = storageSource.storageOf_PHP_only;
    p_Python_only = storageSource.storageOf_Python_only;
    p_Cuda_only = storageSource.storageOf_Cuda_only;
    p_OpenCL_only = storageSource.storageOf_OpenCL_only;
    p_Csharp_only = storageSource.storageOf_Csharp_only;
    p_Ada_only = storageSource.storageOf_Ada_only;
    p_requires_C_preprocessor = storageSource.storageOf_requires_C_preprocessor;
    p_binary_only = storageSource.storageOf_binary_only;
    p_inputFormat = storageSource.storageOf_inputFormat;
    p_outputFormat = storageSource.storageOf_outputFormat;
    p_backendCompileFormat = storageSource.storageOf_backendCompileFormat;
    p_fortran_implicit_none = storageSource.storageOf_fortran_implicit_none;
    p_openmp = storageSource.storageOf_openmp;
    p_openmp_parse_only = storageSource.storageOf_openmp_parse_only;
    p_openmp_ast_only = storageSource.storageOf_openmp_ast_only;
    p_openmp_lowering = storageSource.storageOf_openmp_lowering;
    p_openacc = storageSource.storageOf_openacc;
    p_openacc_parse_only = storageSource.storageOf_openacc_parse_only;
    p_openacc_ast_only = storageSource.storageOf_openacc_ast_only;
    p_cray_pointer_support = storageSource.storageOf_cray_pointer_support;
    p_failsafe = storageSource.storageOf_failsafe;
    p_output_parser_actions = storageSource.storageOf_output_parser_actions;
    p_exit_after_parser = storageSource.storageOf_exit_after_parser;
    p_skip_syntax_check = storageSource.storageOf_skip_syntax_check;
    p_skip_parser = storageSource.storageOf_skip_parser;
    p_relax_syntax_check = storageSource.storageOf_relax_syntax_check;
    p_skip_translation_from_edg_ast_to_rose_ast = storageSource.storageOf_skip_translation_from_edg_ast_to_rose_ast;
    p_skip_transformation = storageSource.storageOf_skip_transformation;
    p_skip_unparse = storageSource.storageOf_skip_unparse;
    p_skipfinalCompileStep = storageSource.storageOf_skipfinalCompileStep;
    p_unparse_includes = storageSource.storageOf_unparse_includes;
    p_unparse_line_directives = storageSource.storageOf_unparse_line_directives;
    p_unparse_function_calls_using_operator_syntax = storageSource.storageOf_unparse_function_calls_using_operator_syntax;
    p_unparse_function_calls_using_operator_names = storageSource.storageOf_unparse_function_calls_using_operator_names;
    p_unparse_instruction_addresses = storageSource.storageOf_unparse_instruction_addresses;
    p_unparse_raw_memory_contents = storageSource.storageOf_unparse_raw_memory_contents;
    p_unparse_binary_file_format = storageSource.storageOf_unparse_binary_file_format;
    p_outputLanguage = storageSource.storageOf_outputLanguage;
    p_inputLanguage = storageSource.storageOf_inputLanguage;
    p_sourceFileNameWithPath = storageSource.storageOf_sourceFileNameWithPath.rebuildDataStoredInEasyStorageClass();
    p_sourceFileNameWithoutPath = storageSource.storageOf_sourceFileNameWithoutPath.rebuildDataStoredInEasyStorageClass();
    p_unparse_output_filename = storageSource.storageOf_unparse_output_filename.rebuildDataStoredInEasyStorageClass();
    p_objectFileNameWithPath = storageSource.storageOf_objectFileNameWithPath.rebuildDataStoredInEasyStorageClass();
    p_objectFileNameWithoutPath = storageSource.storageOf_objectFileNameWithoutPath.rebuildDataStoredInEasyStorageClass();
    p_useBackendOnly = storageSource.storageOf_useBackendOnly;
    p_compileOnly = storageSource.storageOf_compileOnly;
    p_savedFrontendCommandLine = storageSource.storageOf_savedFrontendCommandLine.rebuildDataStoredInEasyStorageClass();
    p_no_implicit_templates = storageSource.storageOf_no_implicit_templates;
    p_no_implicit_inline_templates = storageSource.storageOf_no_implicit_inline_templates;
    p_skip_commentsAndDirectives = storageSource.storageOf_skip_commentsAndDirectives;
    p_collectAllCommentsAndDirectives = storageSource.storageOf_collectAllCommentsAndDirectives;
    p_translateCommentsAndDirectivesIntoAST = storageSource.storageOf_translateCommentsAndDirectivesIntoAST;
    p_unparseHeaderFiles = storageSource.storageOf_unparseHeaderFiles;
    p_preprocessorDirectivesAndCommentsList = storageSource.storageOf_preprocessorDirectivesAndCommentsList.rebuildDataStoredInEasyStorageClass();
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();
    p_KCC_frontend = storageSource.storageOf_KCC_frontend;
    p_new_frontend = storageSource.storageOf_new_frontend;
    p_disable_edg_backend = storageSource.storageOf_disable_edg_backend;
    p_disable_sage_backend = storageSource.storageOf_disable_sage_backend;
    p_testingLevel = storageSource.storageOf_testingLevel;
    p_preinit_il = storageSource.storageOf_preinit_il;
    p_enable_cp_backend = storageSource.storageOf_enable_cp_backend;
    p_markGeneratedFiles = storageSource.storageOf_markGeneratedFiles;
    p_negative_test = storageSource.storageOf_negative_test;
    p_strict_language_handling = storageSource.storageOf_strict_language_handling;
    p_wave = storageSource.storageOf_wave;
    p_embedColorCodesInGeneratedCode = storageSource.storageOf_embedColorCodesInGeneratedCode;
    p_generateSourcePositionCodes = storageSource.storageOf_generateSourcePositionCodes;
    p_sourceFileUsesCppFileExtension = storageSource.storageOf_sourceFileUsesCppFileExtension;
    p_sourceFileUsesFortranFileExtension = storageSource.storageOf_sourceFileUsesFortranFileExtension;
    p_sourceFileUsesFortran77FileExtension = storageSource.storageOf_sourceFileUsesFortran77FileExtension;
    p_sourceFileUsesFortran90FileExtension = storageSource.storageOf_sourceFileUsesFortran90FileExtension;
    p_sourceFileUsesFortran95FileExtension = storageSource.storageOf_sourceFileUsesFortran95FileExtension;
    p_sourceFileUsesFortran2003FileExtension = storageSource.storageOf_sourceFileUsesFortran2003FileExtension;
    p_sourceFileUsesFortran2008FileExtension = storageSource.storageOf_sourceFileUsesFortran2008FileExtension;
    p_sourceFileUsesCoArrayFortranFileExtension = storageSource.storageOf_sourceFileUsesCoArrayFortranFileExtension;
    p_sourceFileUsesPHPFileExtension = storageSource.storageOf_sourceFileUsesPHPFileExtension;
    p_sourceFileUsesPythonFileExtension = storageSource.storageOf_sourceFileUsesPythonFileExtension;
    p_sourceFileUsesJavaFileExtension = storageSource.storageOf_sourceFileUsesJavaFileExtension;
    p_sourceFileUsesJvmFileExtension = storageSource.storageOf_sourceFileUsesJvmFileExtension;
    p_sourceFileUsesBinaryFileExtension = storageSource.storageOf_sourceFileUsesBinaryFileExtension;
    p_sourceFileTypeIsUnknown = storageSource.storageOf_sourceFileTypeIsUnknown;
    p_sourceFileUsesCsharpFileExtension = storageSource.storageOf_sourceFileUsesCsharpFileExtension;
    p_sourceFileUsesAdaFileExtension = storageSource.storageOf_sourceFileUsesAdaFileExtension;
    p_sourceFileUsesJovialFileExtension = storageSource.storageOf_sourceFileUsesJovialFileExtension;
    p_detect_dangling_pointers = storageSource.storageOf_detect_dangling_pointers;
    p_experimental_flang_frontend = storageSource.storageOf_experimental_flang_frontend;
    p_read_executable_file_format_only = storageSource.storageOf_read_executable_file_format_only;
    p_visualize_executable_file_format_skip_symbols = storageSource.storageOf_visualize_executable_file_format_skip_symbols;
    p_visualize_dwarf_only = storageSource.storageOf_visualize_dwarf_only;
    p_read_instructions_only = storageSource.storageOf_read_instructions_only;
    p_libraryArchiveObjectFileNameList = storageSource.storageOf_libraryArchiveObjectFileNameList.rebuildDataStoredInEasyStorageClass();
    p_isLibraryArchive = storageSource.storageOf_isLibraryArchive;
    p_isObjectFile = storageSource.storageOf_isObjectFile;
    p_unparse_tokens = storageSource.storageOf_unparse_tokens;
    p_unparse_tokens_testing = storageSource.storageOf_unparse_tokens_testing;
    p_unparse_using_leading_and_trailing_token_mappings = storageSource.storageOf_unparse_using_leading_and_trailing_token_mappings;
    p_unparse_template_ast = storageSource.storageOf_unparse_template_ast;
    p_skipAstConsistancyTests = storageSource.storageOf_skipAstConsistancyTests;
    p_multifile_support = storageSource.storageOf_multifile_support;
    p_optimization = storageSource.storageOf_optimization;
    p_use_token_stream_to_improve_source_position_info = storageSource.storageOf_use_token_stream_to_improve_source_position_info;
    p_suppress_variable_declaration_normalization = storageSource.storageOf_suppress_variable_declaration_normalization;
    p_edg_il_to_graphviz = storageSource.storageOf_edg_il_to_graphviz;
    p_clang_il_to_graphviz = storageSource.storageOf_clang_il_to_graphviz;
    p_no_optimize_flag_for_frontend = storageSource.storageOf_no_optimize_flag_for_frontend;
    p_unparse_edg_normalized_method_ROSE_1392 = storageSource.storageOf_unparse_edg_normalized_method_ROSE_1392;
    p_standard = storageSource.storageOf_standard;
    p_gnu_standard = storageSource.storageOf_gnu_standard;
    p_frontendErrorCode = storageSource.storageOf_frontendErrorCode;
    p_javacErrorCode = storageSource.storageOf_javacErrorCode;
    p_ecjErrorCode = storageSource.storageOf_ecjErrorCode;
    p_midendErrorCode = storageSource.storageOf_midendErrorCode;
    p_unparserErrorCode = storageSource.storageOf_unparserErrorCode;
    p_backendCompilerErrorCode = storageSource.storageOf_backendCompilerErrorCode;
    p_unparsedFileFailedCompilation = storageSource.storageOf_unparsedFileFailedCompilation;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFile*
SgFile::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFile)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFile)};
        unsigned long positionInPool{localIndex % SgFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFile::pool_size};
        returnPointer = &(((SgFile*)(SgFile::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFile*
SgFile::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFile)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFile)};
        unsigned long positionInPool{localIndex % SgFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFile::pool_size};
        returnPointer = &(((SgFile*)(SgFile::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFile::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFile::pools.begin(); block != SgFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFile*>(*block)};
        for (unsigned i = 0; i < SgFile::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFile*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFile::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFile* pointerOfLinkedList{nullptr};
    for (auto block = SgFile::pools.begin(); block != SgFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFile*>(*block)};
        for (unsigned i = 0; i < SgFile::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFile::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFile::clearMemoryPool() {
    SgFile* tempPointer{nullptr};
    if (SgFile::pools.empty() == false) {
        auto block{SgFile::pools.begin()};
        SgFile::next_node = reinterpret_cast<SgFile*>(*block);
        for (; block != SgFile::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFile*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFile::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFile::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFile::pool_size-1]);
        }
    }
}

void
SgFile::deleteMemoryPool() {
    for (auto p: SgFile::pools) {
        ROSE_FREE(p);
    }
    SgFile::next_node = nullptr;
    SgFile::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFile::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFile) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFile)};
    for (size_t blockIndex = SgFile::pools.size(); (blockIndex * SgFile::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFile) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFile) = %" PRIuPTR " SgFile::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFile),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFile),SgFile::pool_size);
        }
#endif

        SgFile* pointer{static_cast<SgFile*>(ROSE_MALLOC(SgFile::pool_size * sizeof(SgFile)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFile::pool_size * sizeof(SgFile));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFile::pool_size * sizeof(SgFile));
#endif
        SgFile::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFile::pool_size * sizeof(SgFile), V_SgFile));

        if (SgFile::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFile* blkptr{reinterpret_cast<SgFile*>(SgFile::pools[blockIndex-1])};
                blkptr[SgFile::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFile::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFile::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFile::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFile::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFile::pool_size - 1};
    SgFile* testPointer{reinterpret_cast<SgFile*>(SgFile::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFile::pool_size * (SgFile::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFileStroageClass
 * from its counterpart of SgFile. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFile::initializeStorageClassArray(SgFileStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFile::pools.begin(); block != SgFile::pools.end(); ++block) {
        SgFile* pointer{reinterpret_cast<SgFile*>(*block)};
        for (unsigned i = 0; i < SgFile::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 6129 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSourceFile that takes its corresponding StorageClass as parameter
 */
SgSourceFile::SgSourceFile(const SgSourceFileStorageClass& storageSource)  : SgFile (storageSource) {

/* #line 6139 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_globalScope = (SgGlobal*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_globalScope));
    p_module_list = storageSource.storageOf_module_list.rebuildDataStoredInEasyStorageClass();
    SgModuleStatementPtrList::iterator i_module_list = p_module_list.begin();
    for ( ; i_module_list != p_module_list.end(); ++i_module_list) {
        (*i_module_list) = (SgModuleStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_module_list)) );
    }
    p_token_list = storageSource.storageOf_token_list.rebuildDataStoredInEasyStorageClass();
    SgTokenPtrList::iterator i_token_list = p_token_list.begin();
    for ( ; i_token_list != p_token_list.end(); ++i_token_list) {
        (*i_token_list) = (SgTokenPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_token_list)) );
    }
     p_temp_holding_scope = (SgGlobal*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_temp_holding_scope));
     p_package = (SgJavaPackageStatement *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_package));
     p_import_list = (SgJavaImportStatementList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_import_list));
     p_class_list = (SgJavaClassDeclarationList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_class_list));
    p_isHeaderFile = storageSource.storageOf_isHeaderFile;
    p_isHeaderFileIncludedMoreThanOnce = storageSource.storageOf_isHeaderFileIncludedMoreThanOnce;
     p_headerFileReport = (SgHeaderFileReport*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_headerFileReport));
    p_extraIncludeDirectorySpecifierBeforeList = storageSource.storageOf_extraIncludeDirectorySpecifierBeforeList.rebuildDataStoredInEasyStorageClass();
    p_extraIncludeDirectorySpecifierAfterList = storageSource.storageOf_extraIncludeDirectorySpecifierAfterList.rebuildDataStoredInEasyStorageClass();
     p_associated_include_file = (SgIncludeFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_associated_include_file));
    p_processedToIncludeCppDirectivesAndComments = storageSource.storageOf_processedToIncludeCppDirectivesAndComments;
    p_extra_nodes_for_namequal_init = storageSource.storageOf_extra_nodes_for_namequal_init.rebuildDataStoredInEasyStorageClass();
    SgNodePtrList::iterator i_extra_nodes_for_namequal_init = p_extra_nodes_for_namequal_init.begin();
    for ( ; i_extra_nodes_for_namequal_init != p_extra_nodes_for_namequal_init.end(); ++i_extra_nodes_for_namequal_init) {
        (*i_extra_nodes_for_namequal_init) = (SgNodePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_extra_nodes_for_namequal_init)) );
    }
    p_isDynamicLibrary = storageSource.storageOf_isDynamicLibrary;
     p_firstStatement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_firstStatement));
     p_lastStatement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lastStatement));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSourceFile*
SgSourceFile::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSourceFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSourceFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSourceFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSourceFile)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSourceFile)};
        unsigned long positionInPool{localIndex % SgSourceFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSourceFile::pool_size};
        returnPointer = &(((SgSourceFile*)(SgSourceFile::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSourceFile*
SgSourceFile::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSourceFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSourceFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSourceFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSourceFile)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSourceFile)};
        unsigned long positionInPool{localIndex % SgSourceFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSourceFile::pool_size};
        returnPointer = &(((SgSourceFile*)(SgSourceFile::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSourceFile::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSourceFile::pools.begin(); block != SgSourceFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSourceFile*>(*block)};
        for (unsigned i = 0; i < SgSourceFile::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSourceFile*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSourceFile::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSourceFile* pointerOfLinkedList{nullptr};
    for (auto block = SgSourceFile::pools.begin(); block != SgSourceFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSourceFile*>(*block)};
        for (unsigned i = 0; i < SgSourceFile::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSourceFile::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSourceFile::clearMemoryPool() {
    SgSourceFile* tempPointer{nullptr};
    if (SgSourceFile::pools.empty() == false) {
        auto block{SgSourceFile::pools.begin()};
        SgSourceFile::next_node = reinterpret_cast<SgSourceFile*>(*block);
        for (; block != SgSourceFile::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSourceFile*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSourceFile::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSourceFile::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSourceFile::pool_size-1]);
        }
    }
}

void
SgSourceFile::deleteMemoryPool() {
    for (auto p: SgSourceFile::pools) {
        ROSE_FREE(p);
    }
    SgSourceFile::next_node = nullptr;
    SgSourceFile::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSourceFile::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSourceFile) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSourceFile)};
    for (size_t blockIndex = SgSourceFile::pools.size(); (blockIndex * SgSourceFile::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSourceFile) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSourceFile) = %" PRIuPTR " SgSourceFile::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSourceFile),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSourceFile),SgSourceFile::pool_size);
        }
#endif

        SgSourceFile* pointer{static_cast<SgSourceFile*>(ROSE_MALLOC(SgSourceFile::pool_size * sizeof(SgSourceFile)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSourceFile::pool_size * sizeof(SgSourceFile));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSourceFile::pool_size * sizeof(SgSourceFile));
#endif
        SgSourceFile::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSourceFile::pool_size * sizeof(SgSourceFile), V_SgSourceFile));

        if (SgSourceFile::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSourceFile* blkptr{reinterpret_cast<SgSourceFile*>(SgSourceFile::pools[blockIndex-1])};
                blkptr[SgSourceFile::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSourceFile::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSourceFile::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSourceFile::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSourceFile::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSourceFile::pool_size - 1};
    SgSourceFile* testPointer{reinterpret_cast<SgSourceFile*>(SgSourceFile::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSourceFile::pool_size * (SgSourceFile::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSourceFileStroageClass
 * from its counterpart of SgSourceFile. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSourceFile::initializeStorageClassArray(SgSourceFileStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSourceFile::pools.begin(); block != SgSourceFile::pools.end(); ++block) {
        SgSourceFile* pointer{reinterpret_cast<SgSourceFile*>(*block)};
        for (unsigned i = 0; i < SgSourceFile::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 6386 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnknownFile that takes its corresponding StorageClass as parameter
 */
SgUnknownFile::SgUnknownFile(const SgUnknownFileStorageClass& storageSource)  : SgFile (storageSource) {

/* #line 6396 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_globalScope = (SgGlobal*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_globalScope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnknownFile*
SgUnknownFile::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnknownFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnknownFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnknownFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnknownFile)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownFile)};
        unsigned long positionInPool{localIndex % SgUnknownFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnknownFile::pool_size};
        returnPointer = &(((SgUnknownFile*)(SgUnknownFile::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnknownFile*
SgUnknownFile::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnknownFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnknownFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnknownFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnknownFile)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnknownFile)};
        unsigned long positionInPool{localIndex % SgUnknownFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnknownFile::pool_size};
        returnPointer = &(((SgUnknownFile*)(SgUnknownFile::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnknownFile::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnknownFile::pools.begin(); block != SgUnknownFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnknownFile*>(*block)};
        for (unsigned i = 0; i < SgUnknownFile::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnknownFile*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnknownFile::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnknownFile* pointerOfLinkedList{nullptr};
    for (auto block = SgUnknownFile::pools.begin(); block != SgUnknownFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnknownFile*>(*block)};
        for (unsigned i = 0; i < SgUnknownFile::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnknownFile::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnknownFile::clearMemoryPool() {
    SgUnknownFile* tempPointer{nullptr};
    if (SgUnknownFile::pools.empty() == false) {
        auto block{SgUnknownFile::pools.begin()};
        SgUnknownFile::next_node = reinterpret_cast<SgUnknownFile*>(*block);
        for (; block != SgUnknownFile::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnknownFile*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnknownFile::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnknownFile::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnknownFile::pool_size-1]);
        }
    }
}

void
SgUnknownFile::deleteMemoryPool() {
    for (auto p: SgUnknownFile::pools) {
        ROSE_FREE(p);
    }
    SgUnknownFile::next_node = nullptr;
    SgUnknownFile::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnknownFile::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownFile) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnknownFile)};
    for (size_t blockIndex = SgUnknownFile::pools.size(); (blockIndex * SgUnknownFile::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownFile) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnknownFile) = %" PRIuPTR " SgUnknownFile::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownFile),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnknownFile),SgUnknownFile::pool_size);
        }
#endif

        SgUnknownFile* pointer{static_cast<SgUnknownFile*>(ROSE_MALLOC(SgUnknownFile::pool_size * sizeof(SgUnknownFile)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnknownFile::pool_size * sizeof(SgUnknownFile));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnknownFile::pool_size * sizeof(SgUnknownFile));
#endif
        SgUnknownFile::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnknownFile::pool_size * sizeof(SgUnknownFile), V_SgUnknownFile));

        if (SgUnknownFile::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnknownFile* blkptr{reinterpret_cast<SgUnknownFile*>(SgUnknownFile::pools[blockIndex-1])};
                blkptr[SgUnknownFile::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnknownFile::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnknownFile::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnknownFile::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnknownFile::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnknownFile::pool_size - 1};
    SgUnknownFile* testPointer{reinterpret_cast<SgUnknownFile*>(SgUnknownFile::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnknownFile::pool_size * (SgUnknownFile::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnknownFileStroageClass
 * from its counterpart of SgUnknownFile. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnknownFile::initializeStorageClassArray(SgUnknownFileStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnknownFile::pools.begin(); block != SgUnknownFile::pools.end(); ++block) {
        SgUnknownFile* pointer{reinterpret_cast<SgUnknownFile*>(*block)};
        for (unsigned i = 0; i < SgUnknownFile::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 6614 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBinaryComposite that takes its corresponding StorageClass as parameter
 */
SgBinaryComposite::SgBinaryComposite(const SgBinaryCompositeStorageClass& storageSource)  : SgFile (storageSource) {

/* #line 6624 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_genericFileList = (SgAsmGenericFileList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_genericFileList));
     p_interpretations = (SgAsmInterpretationList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_interpretations));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBinaryComposite*
SgBinaryComposite::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBinaryComposite* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBinaryComposite)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBinaryComposite + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBinaryComposite)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryComposite)};
        unsigned long positionInPool{localIndex % SgBinaryComposite::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBinaryComposite::pool_size};
        returnPointer = &(((SgBinaryComposite*)(SgBinaryComposite::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBinaryComposite*
SgBinaryComposite::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBinaryComposite* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBinaryComposite)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBinaryComposite + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBinaryComposite)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBinaryComposite)};
        unsigned long positionInPool{localIndex % SgBinaryComposite::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBinaryComposite::pool_size};
        returnPointer = &(((SgBinaryComposite*)(SgBinaryComposite::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBinaryComposite::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBinaryComposite::pools.begin(); block != SgBinaryComposite::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBinaryComposite*>(*block)};
        for (unsigned i = 0; i < SgBinaryComposite::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBinaryComposite*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBinaryComposite::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBinaryComposite* pointerOfLinkedList{nullptr};
    for (auto block = SgBinaryComposite::pools.begin(); block != SgBinaryComposite::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBinaryComposite*>(*block)};
        for (unsigned i = 0; i < SgBinaryComposite::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBinaryComposite::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBinaryComposite::clearMemoryPool() {
    SgBinaryComposite* tempPointer{nullptr};
    if (SgBinaryComposite::pools.empty() == false) {
        auto block{SgBinaryComposite::pools.begin()};
        SgBinaryComposite::next_node = reinterpret_cast<SgBinaryComposite*>(*block);
        for (; block != SgBinaryComposite::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBinaryComposite*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBinaryComposite::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBinaryComposite::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBinaryComposite::pool_size-1]);
        }
    }
}

void
SgBinaryComposite::deleteMemoryPool() {
    for (auto p: SgBinaryComposite::pools) {
        ROSE_FREE(p);
    }
    SgBinaryComposite::next_node = nullptr;
    SgBinaryComposite::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBinaryComposite::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryComposite) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBinaryComposite)};
    for (size_t blockIndex = SgBinaryComposite::pools.size(); (blockIndex * SgBinaryComposite::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryComposite) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBinaryComposite) = %" PRIuPTR " SgBinaryComposite::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryComposite),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBinaryComposite),SgBinaryComposite::pool_size);
        }
#endif

        SgBinaryComposite* pointer{static_cast<SgBinaryComposite*>(ROSE_MALLOC(SgBinaryComposite::pool_size * sizeof(SgBinaryComposite)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBinaryComposite::pool_size * sizeof(SgBinaryComposite));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBinaryComposite::pool_size * sizeof(SgBinaryComposite));
#endif
        SgBinaryComposite::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBinaryComposite::pool_size * sizeof(SgBinaryComposite), V_SgBinaryComposite));

        if (SgBinaryComposite::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBinaryComposite* blkptr{reinterpret_cast<SgBinaryComposite*>(SgBinaryComposite::pools[blockIndex-1])};
                blkptr[SgBinaryComposite::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBinaryComposite::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBinaryComposite::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBinaryComposite::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBinaryComposite::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBinaryComposite::pool_size - 1};
    SgBinaryComposite* testPointer{reinterpret_cast<SgBinaryComposite*>(SgBinaryComposite::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBinaryComposite::pool_size * (SgBinaryComposite::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBinaryCompositeStroageClass
 * from its counterpart of SgBinaryComposite. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBinaryComposite::initializeStorageClassArray(SgBinaryCompositeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBinaryComposite::pools.begin(); block != SgBinaryComposite::pools.end(); ++block) {
        SgBinaryComposite* pointer{reinterpret_cast<SgBinaryComposite*>(*block)};
        for (unsigned i = 0; i < SgBinaryComposite::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 6843 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJvmComposite that takes its corresponding StorageClass as parameter
 */
SgJvmComposite::SgJvmComposite(const SgJvmCompositeStorageClass& storageSource)  : SgBinaryComposite (storageSource) {

/* #line 6853 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJvmComposite*
SgJvmComposite::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJvmComposite* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJvmComposite)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJvmComposite + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJvmComposite)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJvmComposite)};
        unsigned long positionInPool{localIndex % SgJvmComposite::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJvmComposite::pool_size};
        returnPointer = &(((SgJvmComposite*)(SgJvmComposite::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJvmComposite*
SgJvmComposite::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJvmComposite* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJvmComposite)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJvmComposite + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJvmComposite)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJvmComposite)};
        unsigned long positionInPool{localIndex % SgJvmComposite::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJvmComposite::pool_size};
        returnPointer = &(((SgJvmComposite*)(SgJvmComposite::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJvmComposite::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJvmComposite::pools.begin(); block != SgJvmComposite::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJvmComposite*>(*block)};
        for (unsigned i = 0; i < SgJvmComposite::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJvmComposite*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJvmComposite::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJvmComposite* pointerOfLinkedList{nullptr};
    for (auto block = SgJvmComposite::pools.begin(); block != SgJvmComposite::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJvmComposite*>(*block)};
        for (unsigned i = 0; i < SgJvmComposite::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJvmComposite::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJvmComposite::clearMemoryPool() {
    SgJvmComposite* tempPointer{nullptr};
    if (SgJvmComposite::pools.empty() == false) {
        auto block{SgJvmComposite::pools.begin()};
        SgJvmComposite::next_node = reinterpret_cast<SgJvmComposite*>(*block);
        for (; block != SgJvmComposite::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJvmComposite*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJvmComposite::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJvmComposite::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJvmComposite::pool_size-1]);
        }
    }
}

void
SgJvmComposite::deleteMemoryPool() {
    for (auto p: SgJvmComposite::pools) {
        ROSE_FREE(p);
    }
    SgJvmComposite::next_node = nullptr;
    SgJvmComposite::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJvmComposite::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJvmComposite) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJvmComposite)};
    for (size_t blockIndex = SgJvmComposite::pools.size(); (blockIndex * SgJvmComposite::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJvmComposite) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJvmComposite) = %" PRIuPTR " SgJvmComposite::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJvmComposite),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJvmComposite),SgJvmComposite::pool_size);
        }
#endif

        SgJvmComposite* pointer{static_cast<SgJvmComposite*>(ROSE_MALLOC(SgJvmComposite::pool_size * sizeof(SgJvmComposite)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJvmComposite::pool_size * sizeof(SgJvmComposite));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJvmComposite::pool_size * sizeof(SgJvmComposite));
#endif
        SgJvmComposite::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJvmComposite::pool_size * sizeof(SgJvmComposite), V_SgJvmComposite));

        if (SgJvmComposite::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJvmComposite* blkptr{reinterpret_cast<SgJvmComposite*>(SgJvmComposite::pools[blockIndex-1])};
                blkptr[SgJvmComposite::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJvmComposite::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJvmComposite::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJvmComposite::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJvmComposite::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJvmComposite::pool_size - 1};
    SgJvmComposite* testPointer{reinterpret_cast<SgJvmComposite*>(SgJvmComposite::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJvmComposite::pool_size * (SgJvmComposite::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJvmCompositeStroageClass
 * from its counterpart of SgJvmComposite. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJvmComposite::initializeStorageClassArray(SgJvmCompositeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJvmComposite::pools.begin(); block != SgJvmComposite::pools.end(); ++block) {
        SgJvmComposite* pointer{reinterpret_cast<SgJvmComposite*>(*block)};
        for (unsigned i = 0; i < SgJvmComposite::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 7070 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgProject that takes its corresponding StorageClass as parameter
 */
SgProject::SgProject(const SgProjectStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 7080 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_fileList_ptr = (SgFileList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_fileList_ptr));
    p_originalCommandLineArgumentList = storageSource.storageOf_originalCommandLineArgumentList.rebuildDataStoredInEasyStorageClass();
    p_frontendErrorCode = storageSource.storageOf_frontendErrorCode;
    p_javacErrorCode = storageSource.storageOf_javacErrorCode;
    p_ecjErrorCode = storageSource.storageOf_ecjErrorCode;
    p_midendErrorCode = storageSource.storageOf_midendErrorCode;
    p_backendErrorCode = storageSource.storageOf_backendErrorCode;
    p_keep_going = storageSource.storageOf_keep_going;
    p_unparser__clobber_input_file = storageSource.storageOf_unparser__clobber_input_file;
    p_outputFileName = storageSource.storageOf_outputFileName.rebuildDataStoredInEasyStorageClass();
    p_sourceFileNameList = storageSource.storageOf_sourceFileNameList.rebuildDataStoredInEasyStorageClass();
    p_objectFileNameList = storageSource.storageOf_objectFileNameList.rebuildDataStoredInEasyStorageClass();
    p_libraryFileList = storageSource.storageOf_libraryFileList.rebuildDataStoredInEasyStorageClass();
    p_librarySpecifierList = storageSource.storageOf_librarySpecifierList.rebuildDataStoredInEasyStorageClass();
    p_libraryDirectorySpecifierList = storageSource.storageOf_libraryDirectorySpecifierList.rebuildDataStoredInEasyStorageClass();
    p_includeDirectorySpecifierList = storageSource.storageOf_includeDirectorySpecifierList.rebuildDataStoredInEasyStorageClass();
    p_macroSpecifierList = storageSource.storageOf_macroSpecifierList.rebuildDataStoredInEasyStorageClass();
    p_preincludeFileList = storageSource.storageOf_preincludeFileList.rebuildDataStoredInEasyStorageClass();
    p_preincludeDirectoryList = storageSource.storageOf_preincludeDirectoryList.rebuildDataStoredInEasyStorageClass();
    p_compileOnly = storageSource.storageOf_compileOnly;
    p_wave = storageSource.storageOf_wave;
    p_prelink = storageSource.storageOf_prelink;
    p_template_instantiation_mode = storageSource.storageOf_template_instantiation_mode;
    p_ast_merge = storageSource.storageOf_ast_merge;
    p_projectSpecificDatabaseFile = storageSource.storageOf_projectSpecificDatabaseFile.rebuildDataStoredInEasyStorageClass();
    p_C_PreprocessorOnly = storageSource.storageOf_C_PreprocessorOnly;
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();
    p_compilationPerformanceFile = storageSource.storageOf_compilationPerformanceFile.rebuildDataStoredInEasyStorageClass();
    p_includePathList = storageSource.storageOf_includePathList.rebuildDataStoredInEasyStorageClass();
    p_excludePathList = storageSource.storageOf_excludePathList.rebuildDataStoredInEasyStorageClass();
    p_includeFileList = storageSource.storageOf_includeFileList.rebuildDataStoredInEasyStorageClass();
    p_excludeFileList = storageSource.storageOf_excludeFileList.rebuildDataStoredInEasyStorageClass();
    p_binary_only = storageSource.storageOf_binary_only;
    p_dataBaseFilename = storageSource.storageOf_dataBaseFilename.rebuildDataStoredInEasyStorageClass();
     p_directoryList = (SgDirectoryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_directoryList));
    p_C_only = storageSource.storageOf_C_only;
    p_Cxx_only = storageSource.storageOf_Cxx_only;
    p_C11_only = storageSource.storageOf_C11_only;
    p_Cxx0x_only = storageSource.storageOf_Cxx0x_only;
    p_Cxx11_only = storageSource.storageOf_Cxx11_only;
    p_C14_only = storageSource.storageOf_C14_only;
    p_Cxx14_only = storageSource.storageOf_Cxx14_only;
    p_Fortran_only = storageSource.storageOf_Fortran_only;
    p_Fortran_ofp_jvm_options = storageSource.storageOf_Fortran_ofp_jvm_options.rebuildDataStoredInEasyStorageClass();
    p_Java_only = storageSource.storageOf_Java_only;
    p_Jvm_only = storageSource.storageOf_Jvm_only;
    p_Jovial_only = storageSource.storageOf_Jovial_only;
    p_Ada_only = storageSource.storageOf_Ada_only;
    p_openmp_linking = storageSource.storageOf_openmp_linking;
    p_Java_ecj_jvm_options = storageSource.storageOf_Java_ecj_jvm_options.rebuildDataStoredInEasyStorageClass();
    p_Java_batch_mode = storageSource.storageOf_Java_batch_mode;
    p_Java_classpath = storageSource.storageOf_Java_classpath.rebuildDataStoredInEasyStorageClass();
    p_Java_sourcepath = storageSource.storageOf_Java_sourcepath.rebuildDataStoredInEasyStorageClass();
    p_Java_destdir = storageSource.storageOf_Java_destdir.rebuildDataStoredInEasyStorageClass();
    p_Java_source_destdir = storageSource.storageOf_Java_source_destdir.rebuildDataStoredInEasyStorageClass();
    p_Java_s = storageSource.storageOf_Java_s.rebuildDataStoredInEasyStorageClass();
    p_Java_source = storageSource.storageOf_Java_source.rebuildDataStoredInEasyStorageClass();
    p_Java_target = storageSource.storageOf_Java_target.rebuildDataStoredInEasyStorageClass();
    p_Java_encoding = storageSource.storageOf_Java_encoding.rebuildDataStoredInEasyStorageClass();
    p_Java_g = storageSource.storageOf_Java_g.rebuildDataStoredInEasyStorageClass();
    p_Java_nowarn = storageSource.storageOf_Java_nowarn;
    p_Java_verbose = storageSource.storageOf_Java_verbose;
    p_Java_deprecation = storageSource.storageOf_Java_deprecation;
    p_Java_bootclasspath = storageSource.storageOf_Java_bootclasspath.rebuildDataStoredInEasyStorageClass();
    p_addCppDirectivesToAST = storageSource.storageOf_addCppDirectivesToAST;
    p_includingPreprocessingInfosMap = storageSource.storageOf_includingPreprocessingInfosMap.rebuildDataStoredInEasyStorageClass();
    p_quotedIncludesSearchPaths = storageSource.storageOf_quotedIncludesSearchPaths.rebuildDataStoredInEasyStorageClass();
    p_bracketedIncludesSearchPaths = storageSource.storageOf_bracketedIncludesSearchPaths.rebuildDataStoredInEasyStorageClass();
    p_unparseHeaderFilesRootFolder = storageSource.storageOf_unparseHeaderFilesRootFolder.rebuildDataStoredInEasyStorageClass();
    p_frontendConstantFolding = storageSource.storageOf_frontendConstantFolding;
     p_globalScopeAcrossFiles = (SgGlobal*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_globalScopeAcrossFiles));
    p_unparse_in_same_directory_as_input_file = storageSource.storageOf_unparse_in_same_directory_as_input_file;
    p_stop_after_compilation_do_not_assemble_file = storageSource.storageOf_stop_after_compilation_do_not_assemble_file;
    p_gnuOptionForUndefinedSymbol = storageSource.storageOf_gnuOptionForUndefinedSymbol.rebuildDataStoredInEasyStorageClass();
    p_mode_32_bit = storageSource.storageOf_mode_32_bit;
    p_noclobber_output_file = storageSource.storageOf_noclobber_output_file;
    p_noclobber_if_different_output_file = storageSource.storageOf_noclobber_if_different_output_file;
    p_suppressConstantFoldingPostProcessing = storageSource.storageOf_suppressConstantFoldingPostProcessing;
    p_suppressNameQualificationAcrossWholeTranslationUnit = storageSource.storageOf_suppressNameQualificationAcrossWholeTranslationUnit;
    p_appendPID = storageSource.storageOf_appendPID;
    p_reportOnHeaderFileUnparsing = storageSource.storageOf_reportOnHeaderFileUnparsing;
    p_applicationRootDirectory = storageSource.storageOf_applicationRootDirectory.rebuildDataStoredInEasyStorageClass();
    p_usingApplicationRootDirectory = storageSource.storageOf_usingApplicationRootDirectory;
    p_usingDeferredTransformations = storageSource.storageOf_usingDeferredTransformations;
    p_astfile_out = storageSource.storageOf_astfile_out.rebuildDataStoredInEasyStorageClass();
    p_astfiles_in = storageSource.storageOf_astfiles_in.rebuildDataStoredInEasyStorageClass();
    p_extraIncludeDirectorySpecifierBeforeList = storageSource.storageOf_extraIncludeDirectorySpecifierBeforeList.rebuildDataStoredInEasyStorageClass();
    p_extraIncludeDirectorySpecifierAfterList = storageSource.storageOf_extraIncludeDirectorySpecifierAfterList.rebuildDataStoredInEasyStorageClass();
    p_skip_post_processing = storageSource.storageOf_skip_post_processing;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgProject*
SgProject::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgProject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProject)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgProject)};
        unsigned long positionInPool{localIndex % SgProject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProject::pool_size};
        returnPointer = &(((SgProject*)(SgProject::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgProject*
SgProject::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgProject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProject)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgProject)};
        unsigned long positionInPool{localIndex % SgProject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProject::pool_size};
        returnPointer = &(((SgProject*)(SgProject::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgProject::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgProject::pools.begin(); block != SgProject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProject*>(*block)};
        for (unsigned i = 0; i < SgProject::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgProject*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgProject::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgProject* pointerOfLinkedList{nullptr};
    for (auto block = SgProject::pools.begin(); block != SgProject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProject*>(*block)};
        for (unsigned i = 0; i < SgProject::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgProject::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgProject::clearMemoryPool() {
    SgProject* tempPointer{nullptr};
    if (SgProject::pools.empty() == false) {
        auto block{SgProject::pools.begin()};
        SgProject::next_node = reinterpret_cast<SgProject*>(*block);
        for (; block != SgProject::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgProject*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgProject::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgProject::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgProject::pool_size-1]);
        }
    }
}

void
SgProject::deleteMemoryPool() {
    for (auto p: SgProject::pools) {
        ROSE_FREE(p);
    }
    SgProject::next_node = nullptr;
    SgProject::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgProject::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgProject) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgProject)};
    for (size_t blockIndex = SgProject::pools.size(); (blockIndex * SgProject::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgProject) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgProject) = %" PRIuPTR " SgProject::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgProject),AST_FILE_IO::getPoolSizeOfNewAst(V_SgProject),SgProject::pool_size);
        }
#endif

        SgProject* pointer{static_cast<SgProject*>(ROSE_MALLOC(SgProject::pool_size * sizeof(SgProject)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgProject::pool_size * sizeof(SgProject));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgProject::pool_size * sizeof(SgProject));
#endif
        SgProject::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgProject::pool_size * sizeof(SgProject), V_SgProject));

        if (SgProject::next_node != nullptr) {
            if (blockIndex > 0) {
                SgProject* blkptr{reinterpret_cast<SgProject*>(SgProject::pools[blockIndex-1])};
                blkptr[SgProject::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgProject::next_node = pointer;
        }

        for (unsigned i = 0; i < SgProject::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgProject::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgProject::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgProject::pool_size - 1};
    SgProject* testPointer{reinterpret_cast<SgProject*>(SgProject::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgProject::pool_size * (SgProject::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgProjectStroageClass
 * from its counterpart of SgProject. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgProject::initializeStorageClassArray(SgProjectStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgProject::pools.begin(); block != SgProject::pools.end(); ++block) {
        SgProject* pointer{reinterpret_cast<SgProject*>(*block)};
        for (unsigned i = 0; i < SgProject::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 7386 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOptions that takes its corresponding StorageClass as parameter
 */
SgOptions::SgOptions(const SgOptionsStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 7396 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_debug_level = storageSource.storageOf_debug_level;
    p_logging_level = storageSource.storageOf_logging_level;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOptions*
SgOptions::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOptions* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOptions)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOptions + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOptions)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOptions)};
        unsigned long positionInPool{localIndex % SgOptions::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOptions::pool_size};
        returnPointer = &(((SgOptions*)(SgOptions::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOptions*
SgOptions::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOptions* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOptions)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOptions + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOptions)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOptions)};
        unsigned long positionInPool{localIndex % SgOptions::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOptions::pool_size};
        returnPointer = &(((SgOptions*)(SgOptions::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOptions::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOptions::pools.begin(); block != SgOptions::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOptions*>(*block)};
        for (unsigned i = 0; i < SgOptions::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOptions*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOptions::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOptions* pointerOfLinkedList{nullptr};
    for (auto block = SgOptions::pools.begin(); block != SgOptions::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOptions*>(*block)};
        for (unsigned i = 0; i < SgOptions::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOptions::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOptions::clearMemoryPool() {
    SgOptions* tempPointer{nullptr};
    if (SgOptions::pools.empty() == false) {
        auto block{SgOptions::pools.begin()};
        SgOptions::next_node = reinterpret_cast<SgOptions*>(*block);
        for (; block != SgOptions::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOptions*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOptions::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOptions::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOptions::pool_size-1]);
        }
    }
}

void
SgOptions::deleteMemoryPool() {
    for (auto p: SgOptions::pools) {
        ROSE_FREE(p);
    }
    SgOptions::next_node = nullptr;
    SgOptions::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOptions::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOptions) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOptions)};
    for (size_t blockIndex = SgOptions::pools.size(); (blockIndex * SgOptions::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOptions) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOptions) = %" PRIuPTR " SgOptions::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOptions),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOptions),SgOptions::pool_size);
        }
#endif

        SgOptions* pointer{static_cast<SgOptions*>(ROSE_MALLOC(SgOptions::pool_size * sizeof(SgOptions)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOptions::pool_size * sizeof(SgOptions));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOptions::pool_size * sizeof(SgOptions));
#endif
        SgOptions::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOptions::pool_size * sizeof(SgOptions), V_SgOptions));

        if (SgOptions::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOptions* blkptr{reinterpret_cast<SgOptions*>(SgOptions::pools[blockIndex-1])};
                blkptr[SgOptions::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOptions::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOptions::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOptions::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOptions::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOptions::pool_size - 1};
    SgOptions* testPointer{reinterpret_cast<SgOptions*>(SgOptions::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOptions::pool_size * (SgOptions::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOptionsStroageClass
 * from its counterpart of SgOptions. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOptions::initializeStorageClassArray(SgOptionsStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOptions::pools.begin(); block != SgOptions::pools.end(); ++block) {
        SgOptions* pointer{reinterpret_cast<SgOptions*>(*block)};
        for (unsigned i = 0; i < SgOptions::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 7615 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnparse_Info that takes its corresponding StorageClass as parameter
 */
SgUnparse_Info::SgUnparse_Info(const SgUnparse_InfoStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 7625 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_unparse_attribute = storageSource.storageOf_unparse_attribute.rebuildDataStoredInEasyStorageClass();
    p_access_attribute = storageSource.storageOf_access_attribute;
    p_nested_expression = storageSource.storageOf_nested_expression;
    p_operator_name = storageSource.storageOf_operator_name.rebuildDataStoredInEasyStorageClass();
    p_var_name = SgName ( storageSource.storageOf_var_name);
     p_declstatement_ptr = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declstatement_ptr));
     p_declaration_of_context = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration_of_context));
     p_current_context = (SgNamedType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_current_context));
    p_array_index_list = SgName ( storageSource.storageOf_array_index_list);
     p_current_namespace = (SgNamespaceDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_current_namespace));
    p_outputCodeGenerationFormatDelimiters = storageSource.storageOf_outputCodeGenerationFormatDelimiters;
    p_qualifiedNameList = storageSource.storageOf_qualifiedNameList.rebuildDataStoredInEasyStorageClass();
    SgQualifiedNamePtrList::iterator i_qualifiedNameList = p_qualifiedNameList.begin();
    for ( ; i_qualifiedNameList != p_qualifiedNameList.end(); ++i_qualifiedNameList) {
        (*i_qualifiedNameList) = (SgQualifiedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_qualifiedNameList)) );
    }
     p_current_function_call = (SgFunctionCallExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_current_function_call));
     p_current_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_current_scope));
     p_reference_node_for_qualification = (SgNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_reference_node_for_qualification));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_nestingLevel = storageSource.storageOf_nestingLevel;
    p_language = storageSource.storageOf_language;
     p_current_source_file = (SgSourceFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_current_source_file));
    p_use_generated_name_for_template_arguments = storageSource.storageOf_use_generated_name_for_template_arguments;
    p_user_defined_literal = storageSource.storageOf_user_defined_literal;
     p_declstatement_associated_with_type = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declstatement_associated_with_type));
    p_context_for_added_parentheses = storageSource.storageOf_context_for_added_parentheses;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnparse_Info*
SgUnparse_Info::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnparse_Info* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnparse_Info)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnparse_Info + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnparse_Info)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnparse_Info)};
        unsigned long positionInPool{localIndex % SgUnparse_Info::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnparse_Info::pool_size};
        returnPointer = &(((SgUnparse_Info*)(SgUnparse_Info::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnparse_Info*
SgUnparse_Info::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnparse_Info* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnparse_Info)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnparse_Info + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnparse_Info)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnparse_Info)};
        unsigned long positionInPool{localIndex % SgUnparse_Info::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnparse_Info::pool_size};
        returnPointer = &(((SgUnparse_Info*)(SgUnparse_Info::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnparse_Info::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnparse_Info::pools.begin(); block != SgUnparse_Info::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnparse_Info*>(*block)};
        for (unsigned i = 0; i < SgUnparse_Info::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnparse_Info*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnparse_Info::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnparse_Info* pointerOfLinkedList{nullptr};
    for (auto block = SgUnparse_Info::pools.begin(); block != SgUnparse_Info::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnparse_Info*>(*block)};
        for (unsigned i = 0; i < SgUnparse_Info::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnparse_Info::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnparse_Info::clearMemoryPool() {
    SgUnparse_Info* tempPointer{nullptr};
    if (SgUnparse_Info::pools.empty() == false) {
        auto block{SgUnparse_Info::pools.begin()};
        SgUnparse_Info::next_node = reinterpret_cast<SgUnparse_Info*>(*block);
        for (; block != SgUnparse_Info::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnparse_Info*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnparse_Info::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnparse_Info::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnparse_Info::pool_size-1]);
        }
    }
}

void
SgUnparse_Info::deleteMemoryPool() {
    for (auto p: SgUnparse_Info::pools) {
        ROSE_FREE(p);
    }
    SgUnparse_Info::next_node = nullptr;
    SgUnparse_Info::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnparse_Info::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnparse_Info) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnparse_Info)};
    for (size_t blockIndex = SgUnparse_Info::pools.size(); (blockIndex * SgUnparse_Info::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnparse_Info) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnparse_Info) = %" PRIuPTR " SgUnparse_Info::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnparse_Info),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnparse_Info),SgUnparse_Info::pool_size);
        }
#endif

        SgUnparse_Info* pointer{static_cast<SgUnparse_Info*>(ROSE_MALLOC(SgUnparse_Info::pool_size * sizeof(SgUnparse_Info)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnparse_Info::pool_size * sizeof(SgUnparse_Info));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnparse_Info::pool_size * sizeof(SgUnparse_Info));
#endif
        SgUnparse_Info::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnparse_Info::pool_size * sizeof(SgUnparse_Info), V_SgUnparse_Info));

        if (SgUnparse_Info::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnparse_Info* blkptr{reinterpret_cast<SgUnparse_Info*>(SgUnparse_Info::pools[blockIndex-1])};
                blkptr[SgUnparse_Info::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnparse_Info::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnparse_Info::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnparse_Info::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnparse_Info::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnparse_Info::pool_size - 1};
    SgUnparse_Info* testPointer{reinterpret_cast<SgUnparse_Info*>(SgUnparse_Info::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnparse_Info::pool_size * (SgUnparse_Info::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnparse_InfoStroageClass
 * from its counterpart of SgUnparse_Info. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnparse_Info::initializeStorageClassArray(SgUnparse_InfoStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnparse_Info::pools.begin(); block != SgUnparse_Info::pools.end(); ++block) {
        SgUnparse_Info* pointer{reinterpret_cast<SgUnparse_Info*>(*block)};
        for (unsigned i = 0; i < SgUnparse_Info::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 7871 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBaseClass that takes its corresponding StorageClass as parameter
 */
SgBaseClass::SgBaseClass(const SgBaseClassStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 7881 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_class = (SgClassDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_class));
    p_isDirectBaseClass = storageSource.storageOf_isDirectBaseClass;
     p_baseClassModifier = (SgBaseClassModifier*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_baseClassModifier));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBaseClass*
SgBaseClass::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBaseClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBaseClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBaseClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBaseClass)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClass)};
        unsigned long positionInPool{localIndex % SgBaseClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBaseClass::pool_size};
        returnPointer = &(((SgBaseClass*)(SgBaseClass::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBaseClass*
SgBaseClass::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBaseClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBaseClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBaseClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBaseClass)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBaseClass)};
        unsigned long positionInPool{localIndex % SgBaseClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBaseClass::pool_size};
        returnPointer = &(((SgBaseClass*)(SgBaseClass::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBaseClass::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBaseClass::pools.begin(); block != SgBaseClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBaseClass*>(*block)};
        for (unsigned i = 0; i < SgBaseClass::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBaseClass*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBaseClass::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBaseClass* pointerOfLinkedList{nullptr};
    for (auto block = SgBaseClass::pools.begin(); block != SgBaseClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBaseClass*>(*block)};
        for (unsigned i = 0; i < SgBaseClass::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBaseClass::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBaseClass::clearMemoryPool() {
    SgBaseClass* tempPointer{nullptr};
    if (SgBaseClass::pools.empty() == false) {
        auto block{SgBaseClass::pools.begin()};
        SgBaseClass::next_node = reinterpret_cast<SgBaseClass*>(*block);
        for (; block != SgBaseClass::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBaseClass*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBaseClass::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBaseClass::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBaseClass::pool_size-1]);
        }
    }
}

void
SgBaseClass::deleteMemoryPool() {
    for (auto p: SgBaseClass::pools) {
        ROSE_FREE(p);
    }
    SgBaseClass::next_node = nullptr;
    SgBaseClass::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBaseClass::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClass) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBaseClass)};
    for (size_t blockIndex = SgBaseClass::pools.size(); (blockIndex * SgBaseClass::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClass) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBaseClass) = %" PRIuPTR " SgBaseClass::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBaseClass),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBaseClass),SgBaseClass::pool_size);
        }
#endif

        SgBaseClass* pointer{static_cast<SgBaseClass*>(ROSE_MALLOC(SgBaseClass::pool_size * sizeof(SgBaseClass)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBaseClass::pool_size * sizeof(SgBaseClass));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBaseClass::pool_size * sizeof(SgBaseClass));
#endif
        SgBaseClass::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBaseClass::pool_size * sizeof(SgBaseClass), V_SgBaseClass));

        if (SgBaseClass::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBaseClass* blkptr{reinterpret_cast<SgBaseClass*>(SgBaseClass::pools[blockIndex-1])};
                blkptr[SgBaseClass::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBaseClass::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBaseClass::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBaseClass::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBaseClass::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBaseClass::pool_size - 1};
    SgBaseClass* testPointer{reinterpret_cast<SgBaseClass*>(SgBaseClass::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBaseClass::pool_size * (SgBaseClass::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBaseClassStroageClass
 * from its counterpart of SgBaseClass. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBaseClass::initializeStorageClassArray(SgBaseClassStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBaseClass::pools.begin(); block != SgBaseClass::pools.end(); ++block) {
        SgBaseClass* pointer{reinterpret_cast<SgBaseClass*>(*block)};
        for (unsigned i = 0; i < SgBaseClass::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 8104 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExpBaseClass that takes its corresponding StorageClass as parameter
 */
SgExpBaseClass::SgExpBaseClass(const SgExpBaseClassStorageClass& storageSource)  : SgBaseClass (storageSource) {

/* #line 8114 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_class_exp = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_class_exp));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExpBaseClass*
SgExpBaseClass::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExpBaseClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpBaseClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpBaseClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpBaseClass)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExpBaseClass)};
        unsigned long positionInPool{localIndex % SgExpBaseClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExpBaseClass::pool_size};
        returnPointer = &(((SgExpBaseClass*)(SgExpBaseClass::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExpBaseClass*
SgExpBaseClass::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExpBaseClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpBaseClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpBaseClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpBaseClass)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExpBaseClass)};
        unsigned long positionInPool{localIndex % SgExpBaseClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExpBaseClass::pool_size};
        returnPointer = &(((SgExpBaseClass*)(SgExpBaseClass::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExpBaseClass::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExpBaseClass::pools.begin(); block != SgExpBaseClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExpBaseClass*>(*block)};
        for (unsigned i = 0; i < SgExpBaseClass::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExpBaseClass*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExpBaseClass::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExpBaseClass* pointerOfLinkedList{nullptr};
    for (auto block = SgExpBaseClass::pools.begin(); block != SgExpBaseClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExpBaseClass*>(*block)};
        for (unsigned i = 0; i < SgExpBaseClass::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExpBaseClass::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExpBaseClass::clearMemoryPool() {
    SgExpBaseClass* tempPointer{nullptr};
    if (SgExpBaseClass::pools.empty() == false) {
        auto block{SgExpBaseClass::pools.begin()};
        SgExpBaseClass::next_node = reinterpret_cast<SgExpBaseClass*>(*block);
        for (; block != SgExpBaseClass::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExpBaseClass*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExpBaseClass::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExpBaseClass::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExpBaseClass::pool_size-1]);
        }
    }
}

void
SgExpBaseClass::deleteMemoryPool() {
    for (auto p: SgExpBaseClass::pools) {
        ROSE_FREE(p);
    }
    SgExpBaseClass::next_node = nullptr;
    SgExpBaseClass::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExpBaseClass::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExpBaseClass) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpBaseClass)};
    for (size_t blockIndex = SgExpBaseClass::pools.size(); (blockIndex * SgExpBaseClass::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExpBaseClass) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpBaseClass) = %" PRIuPTR " SgExpBaseClass::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExpBaseClass),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpBaseClass),SgExpBaseClass::pool_size);
        }
#endif

        SgExpBaseClass* pointer{static_cast<SgExpBaseClass*>(ROSE_MALLOC(SgExpBaseClass::pool_size * sizeof(SgExpBaseClass)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExpBaseClass::pool_size * sizeof(SgExpBaseClass));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExpBaseClass::pool_size * sizeof(SgExpBaseClass));
#endif
        SgExpBaseClass::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExpBaseClass::pool_size * sizeof(SgExpBaseClass), V_SgExpBaseClass));

        if (SgExpBaseClass::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExpBaseClass* blkptr{reinterpret_cast<SgExpBaseClass*>(SgExpBaseClass::pools[blockIndex-1])};
                blkptr[SgExpBaseClass::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExpBaseClass::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExpBaseClass::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExpBaseClass::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExpBaseClass::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExpBaseClass::pool_size - 1};
    SgExpBaseClass* testPointer{reinterpret_cast<SgExpBaseClass*>(SgExpBaseClass::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExpBaseClass::pool_size * (SgExpBaseClass::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExpBaseClassStroageClass
 * from its counterpart of SgExpBaseClass. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExpBaseClass::initializeStorageClassArray(SgExpBaseClassStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExpBaseClass::pools.begin(); block != SgExpBaseClass::pools.end(); ++block) {
        SgExpBaseClass* pointer{reinterpret_cast<SgExpBaseClass*>(*block)};
        for (unsigned i = 0; i < SgExpBaseClass::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 8332 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNonrealBaseClass that takes its corresponding StorageClass as parameter
 */
SgNonrealBaseClass::SgNonrealBaseClass(const SgNonrealBaseClassStorageClass& storageSource)  : SgBaseClass (storageSource) {

/* #line 8342 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_class_nonreal = (SgNonrealDecl*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_class_nonreal));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNonrealBaseClass*
SgNonrealBaseClass::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNonrealBaseClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealBaseClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealBaseClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealBaseClass)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealBaseClass)};
        unsigned long positionInPool{localIndex % SgNonrealBaseClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealBaseClass::pool_size};
        returnPointer = &(((SgNonrealBaseClass*)(SgNonrealBaseClass::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNonrealBaseClass*
SgNonrealBaseClass::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNonrealBaseClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealBaseClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealBaseClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealBaseClass)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNonrealBaseClass)};
        unsigned long positionInPool{localIndex % SgNonrealBaseClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealBaseClass::pool_size};
        returnPointer = &(((SgNonrealBaseClass*)(SgNonrealBaseClass::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNonrealBaseClass::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNonrealBaseClass::pools.begin(); block != SgNonrealBaseClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealBaseClass*>(*block)};
        for (unsigned i = 0; i < SgNonrealBaseClass::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNonrealBaseClass*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNonrealBaseClass::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNonrealBaseClass* pointerOfLinkedList{nullptr};
    for (auto block = SgNonrealBaseClass::pools.begin(); block != SgNonrealBaseClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealBaseClass*>(*block)};
        for (unsigned i = 0; i < SgNonrealBaseClass::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNonrealBaseClass::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNonrealBaseClass::clearMemoryPool() {
    SgNonrealBaseClass* tempPointer{nullptr};
    if (SgNonrealBaseClass::pools.empty() == false) {
        auto block{SgNonrealBaseClass::pools.begin()};
        SgNonrealBaseClass::next_node = reinterpret_cast<SgNonrealBaseClass*>(*block);
        for (; block != SgNonrealBaseClass::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNonrealBaseClass*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNonrealBaseClass::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNonrealBaseClass::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNonrealBaseClass::pool_size-1]);
        }
    }
}

void
SgNonrealBaseClass::deleteMemoryPool() {
    for (auto p: SgNonrealBaseClass::pools) {
        ROSE_FREE(p);
    }
    SgNonrealBaseClass::next_node = nullptr;
    SgNonrealBaseClass::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNonrealBaseClass::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealBaseClass) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealBaseClass)};
    for (size_t blockIndex = SgNonrealBaseClass::pools.size(); (blockIndex * SgNonrealBaseClass::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealBaseClass) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealBaseClass) = %" PRIuPTR " SgNonrealBaseClass::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealBaseClass),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealBaseClass),SgNonrealBaseClass::pool_size);
        }
#endif

        SgNonrealBaseClass* pointer{static_cast<SgNonrealBaseClass*>(ROSE_MALLOC(SgNonrealBaseClass::pool_size * sizeof(SgNonrealBaseClass)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNonrealBaseClass::pool_size * sizeof(SgNonrealBaseClass));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNonrealBaseClass::pool_size * sizeof(SgNonrealBaseClass));
#endif
        SgNonrealBaseClass::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNonrealBaseClass::pool_size * sizeof(SgNonrealBaseClass), V_SgNonrealBaseClass));

        if (SgNonrealBaseClass::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNonrealBaseClass* blkptr{reinterpret_cast<SgNonrealBaseClass*>(SgNonrealBaseClass::pools[blockIndex-1])};
                blkptr[SgNonrealBaseClass::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNonrealBaseClass::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNonrealBaseClass::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNonrealBaseClass::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNonrealBaseClass::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNonrealBaseClass::pool_size - 1};
    SgNonrealBaseClass* testPointer{reinterpret_cast<SgNonrealBaseClass*>(SgNonrealBaseClass::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNonrealBaseClass::pool_size * (SgNonrealBaseClass::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNonrealBaseClassStroageClass
 * from its counterpart of SgNonrealBaseClass. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNonrealBaseClass::initializeStorageClassArray(SgNonrealBaseClassStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNonrealBaseClass::pools.begin(); block != SgNonrealBaseClass::pools.end(); ++block) {
        SgNonrealBaseClass* pointer{reinterpret_cast<SgNonrealBaseClass*>(*block)};
        for (unsigned i = 0; i < SgNonrealBaseClass::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 8560 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypedefSeq that takes its corresponding StorageClass as parameter
 */
SgTypedefSeq::SgTypedefSeq(const SgTypedefSeqStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 8570 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_typedefs = storageSource.storageOf_typedefs.rebuildDataStoredInEasyStorageClass();
    SgTypePtrList::iterator i_typedefs = p_typedefs.begin();
    for ( ; i_typedefs != p_typedefs.end(); ++i_typedefs) {
        (*i_typedefs) = (SgTypePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_typedefs)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypedefSeq*
SgTypedefSeq::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypedefSeq* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefSeq)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefSeq + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefSeq)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSeq)};
        unsigned long positionInPool{localIndex % SgTypedefSeq::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefSeq::pool_size};
        returnPointer = &(((SgTypedefSeq*)(SgTypedefSeq::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypedefSeq*
SgTypedefSeq::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypedefSeq* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefSeq)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefSeq + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefSeq)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypedefSeq)};
        unsigned long positionInPool{localIndex % SgTypedefSeq::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefSeq::pool_size};
        returnPointer = &(((SgTypedefSeq*)(SgTypedefSeq::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypedefSeq::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypedefSeq::pools.begin(); block != SgTypedefSeq::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefSeq*>(*block)};
        for (unsigned i = 0; i < SgTypedefSeq::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypedefSeq*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypedefSeq::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypedefSeq* pointerOfLinkedList{nullptr};
    for (auto block = SgTypedefSeq::pools.begin(); block != SgTypedefSeq::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefSeq*>(*block)};
        for (unsigned i = 0; i < SgTypedefSeq::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypedefSeq::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypedefSeq::clearMemoryPool() {
    SgTypedefSeq* tempPointer{nullptr};
    if (SgTypedefSeq::pools.empty() == false) {
        auto block{SgTypedefSeq::pools.begin()};
        SgTypedefSeq::next_node = reinterpret_cast<SgTypedefSeq*>(*block);
        for (; block != SgTypedefSeq::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypedefSeq*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypedefSeq::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypedefSeq::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypedefSeq::pool_size-1]);
        }
    }
}

void
SgTypedefSeq::deleteMemoryPool() {
    for (auto p: SgTypedefSeq::pools) {
        ROSE_FREE(p);
    }
    SgTypedefSeq::next_node = nullptr;
    SgTypedefSeq::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypedefSeq::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSeq) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefSeq)};
    for (size_t blockIndex = SgTypedefSeq::pools.size(); (blockIndex * SgTypedefSeq::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSeq) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefSeq) = %" PRIuPTR " SgTypedefSeq::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSeq),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefSeq),SgTypedefSeq::pool_size);
        }
#endif

        SgTypedefSeq* pointer{static_cast<SgTypedefSeq*>(ROSE_MALLOC(SgTypedefSeq::pool_size * sizeof(SgTypedefSeq)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypedefSeq::pool_size * sizeof(SgTypedefSeq));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypedefSeq::pool_size * sizeof(SgTypedefSeq));
#endif
        SgTypedefSeq::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypedefSeq::pool_size * sizeof(SgTypedefSeq), V_SgTypedefSeq));

        if (SgTypedefSeq::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypedefSeq* blkptr{reinterpret_cast<SgTypedefSeq*>(SgTypedefSeq::pools[blockIndex-1])};
                blkptr[SgTypedefSeq::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypedefSeq::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypedefSeq::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypedefSeq::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypedefSeq::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypedefSeq::pool_size - 1};
    SgTypedefSeq* testPointer{reinterpret_cast<SgTypedefSeq*>(SgTypedefSeq::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypedefSeq::pool_size * (SgTypedefSeq::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypedefSeqStroageClass
 * from its counterpart of SgTypedefSeq. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypedefSeq::initializeStorageClassArray(SgTypedefSeqStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypedefSeq::pools.begin(); block != SgTypedefSeq::pools.end(); ++block) {
        SgTypedefSeq* pointer{reinterpret_cast<SgTypedefSeq*>(*block)};
        for (unsigned i = 0; i < SgTypedefSeq::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 8792 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateParameter that takes its corresponding StorageClass as parameter
 */
SgTemplateParameter::SgTemplateParameter(const SgTemplateParameterStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 8802 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_parameterType = storageSource.storageOf_parameterType;
     p_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_defaultTypeParameter = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_defaultTypeParameter));
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_defaultExpressionParameter = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_defaultExpressionParameter));
     p_templateDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
     p_defaultTemplateDeclarationParameter = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_defaultTemplateDeclarationParameter));
     p_initializedName = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initializedName));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateParameter*
SgTemplateParameter::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameter)};
        unsigned long positionInPool{localIndex % SgTemplateParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateParameter::pool_size};
        returnPointer = &(((SgTemplateParameter*)(SgTemplateParameter::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateParameter*
SgTemplateParameter::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateParameter)};
        unsigned long positionInPool{localIndex % SgTemplateParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateParameter::pool_size};
        returnPointer = &(((SgTemplateParameter*)(SgTemplateParameter::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateParameter::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateParameter::pools.begin(); block != SgTemplateParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateParameter*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameter::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateParameter*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateParameter::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateParameter* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateParameter::pools.begin(); block != SgTemplateParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateParameter*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameter::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateParameter::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateParameter::clearMemoryPool() {
    SgTemplateParameter* tempPointer{nullptr};
    if (SgTemplateParameter::pools.empty() == false) {
        auto block{SgTemplateParameter::pools.begin()};
        SgTemplateParameter::next_node = reinterpret_cast<SgTemplateParameter*>(*block);
        for (; block != SgTemplateParameter::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateParameter*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateParameter::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateParameter::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateParameter::pool_size-1]);
        }
    }
}

void
SgTemplateParameter::deleteMemoryPool() {
    for (auto p: SgTemplateParameter::pools) {
        ROSE_FREE(p);
    }
    SgTemplateParameter::next_node = nullptr;
    SgTemplateParameter::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateParameter::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameter) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameter)};
    for (size_t blockIndex = SgTemplateParameter::pools.size(); (blockIndex * SgTemplateParameter::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameter) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameter) = %" PRIuPTR " SgTemplateParameter::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameter),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameter),SgTemplateParameter::pool_size);
        }
#endif

        SgTemplateParameter* pointer{static_cast<SgTemplateParameter*>(ROSE_MALLOC(SgTemplateParameter::pool_size * sizeof(SgTemplateParameter)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateParameter::pool_size * sizeof(SgTemplateParameter));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateParameter::pool_size * sizeof(SgTemplateParameter));
#endif
        SgTemplateParameter::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateParameter::pool_size * sizeof(SgTemplateParameter), V_SgTemplateParameter));

        if (SgTemplateParameter::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateParameter* blkptr{reinterpret_cast<SgTemplateParameter*>(SgTemplateParameter::pools[blockIndex-1])};
                blkptr[SgTemplateParameter::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateParameter::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateParameter::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateParameter::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateParameter::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateParameter::pool_size - 1};
    SgTemplateParameter* testPointer{reinterpret_cast<SgTemplateParameter*>(SgTemplateParameter::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateParameter::pool_size * (SgTemplateParameter::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateParameterStroageClass
 * from its counterpart of SgTemplateParameter. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateParameter::initializeStorageClassArray(SgTemplateParameterStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateParameter::pools.begin(); block != SgTemplateParameter::pools.end(); ++block) {
        SgTemplateParameter* pointer{reinterpret_cast<SgTemplateParameter*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameter::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 9027 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateArgument that takes its corresponding StorageClass as parameter
 */
SgTemplateArgument::SgTemplateArgument(const SgTemplateArgumentStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 9037 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_argumentType = storageSource.storageOf_argumentType;
    p_isArrayBoundUnknownType = storageSource.storageOf_isArrayBoundUnknownType;
     p_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_unparsable_type_alias = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_unparsable_type_alias));
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_templateDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
     p_initializedName = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initializedName));
    p_explicitlySpecified = storageSource.storageOf_explicitlySpecified;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_requiresGlobalNameQualificationOnType = storageSource.storageOf_requiresGlobalNameQualificationOnType;
    p_name_qualification_length_for_type = storageSource.storageOf_name_qualification_length_for_type;
    p_type_elaboration_required_for_type = storageSource.storageOf_type_elaboration_required_for_type;
    p_global_qualification_required_for_type = storageSource.storageOf_global_qualification_required_for_type;
     p_previous_instance = (SgTemplateArgument*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_previous_instance));
     p_next_instance = (SgTemplateArgument*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_next_instance));
    p_is_pack_element = storageSource.storageOf_is_pack_element;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateArgument*
SgTemplateArgument::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateArgument* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateArgument)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateArgument + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateArgument)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgument)};
        unsigned long positionInPool{localIndex % SgTemplateArgument::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateArgument::pool_size};
        returnPointer = &(((SgTemplateArgument*)(SgTemplateArgument::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateArgument*
SgTemplateArgument::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateArgument* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateArgument)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateArgument + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateArgument)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateArgument)};
        unsigned long positionInPool{localIndex % SgTemplateArgument::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateArgument::pool_size};
        returnPointer = &(((SgTemplateArgument*)(SgTemplateArgument::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateArgument::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateArgument::pools.begin(); block != SgTemplateArgument::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateArgument*>(*block)};
        for (unsigned i = 0; i < SgTemplateArgument::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateArgument*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateArgument::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateArgument* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateArgument::pools.begin(); block != SgTemplateArgument::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateArgument*>(*block)};
        for (unsigned i = 0; i < SgTemplateArgument::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateArgument::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateArgument::clearMemoryPool() {
    SgTemplateArgument* tempPointer{nullptr};
    if (SgTemplateArgument::pools.empty() == false) {
        auto block{SgTemplateArgument::pools.begin()};
        SgTemplateArgument::next_node = reinterpret_cast<SgTemplateArgument*>(*block);
        for (; block != SgTemplateArgument::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateArgument*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateArgument::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateArgument::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateArgument::pool_size-1]);
        }
    }
}

void
SgTemplateArgument::deleteMemoryPool() {
    for (auto p: SgTemplateArgument::pools) {
        ROSE_FREE(p);
    }
    SgTemplateArgument::next_node = nullptr;
    SgTemplateArgument::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateArgument::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgument) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateArgument)};
    for (size_t blockIndex = SgTemplateArgument::pools.size(); (blockIndex * SgTemplateArgument::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgument) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateArgument) = %" PRIuPTR " SgTemplateArgument::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgument),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateArgument),SgTemplateArgument::pool_size);
        }
#endif

        SgTemplateArgument* pointer{static_cast<SgTemplateArgument*>(ROSE_MALLOC(SgTemplateArgument::pool_size * sizeof(SgTemplateArgument)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateArgument::pool_size * sizeof(SgTemplateArgument));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateArgument::pool_size * sizeof(SgTemplateArgument));
#endif
        SgTemplateArgument::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateArgument::pool_size * sizeof(SgTemplateArgument), V_SgTemplateArgument));

        if (SgTemplateArgument::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateArgument* blkptr{reinterpret_cast<SgTemplateArgument*>(SgTemplateArgument::pools[blockIndex-1])};
                blkptr[SgTemplateArgument::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateArgument::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateArgument::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateArgument::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateArgument::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateArgument::pool_size - 1};
    SgTemplateArgument* testPointer{reinterpret_cast<SgTemplateArgument*>(SgTemplateArgument::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateArgument::pool_size * (SgTemplateArgument::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateArgumentStroageClass
 * from its counterpart of SgTemplateArgument. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateArgument::initializeStorageClassArray(SgTemplateArgumentStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateArgument::pools.begin(); block != SgTemplateArgument::pools.end(); ++block) {
        SgTemplateArgument* pointer{reinterpret_cast<SgTemplateArgument*>(*block)};
        for (unsigned i = 0; i < SgTemplateArgument::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 9272 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDirectory that takes its corresponding StorageClass as parameter
 */
SgDirectory::SgDirectory(const SgDirectoryStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 9282 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
     p_fileList = (SgFileList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_fileList));
     p_directoryList = (SgDirectoryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_directoryList));
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDirectory*
SgDirectory::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDirectory* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectory)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectory + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectory)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectory)};
        unsigned long positionInPool{localIndex % SgDirectory::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDirectory::pool_size};
        returnPointer = &(((SgDirectory*)(SgDirectory::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDirectory*
SgDirectory::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDirectory* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectory)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectory + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectory)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDirectory)};
        unsigned long positionInPool{localIndex % SgDirectory::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDirectory::pool_size};
        returnPointer = &(((SgDirectory*)(SgDirectory::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDirectory::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDirectory::pools.begin(); block != SgDirectory::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDirectory*>(*block)};
        for (unsigned i = 0; i < SgDirectory::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDirectory*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDirectory::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDirectory* pointerOfLinkedList{nullptr};
    for (auto block = SgDirectory::pools.begin(); block != SgDirectory::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDirectory*>(*block)};
        for (unsigned i = 0; i < SgDirectory::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDirectory::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDirectory::clearMemoryPool() {
    SgDirectory* tempPointer{nullptr};
    if (SgDirectory::pools.empty() == false) {
        auto block{SgDirectory::pools.begin()};
        SgDirectory::next_node = reinterpret_cast<SgDirectory*>(*block);
        for (; block != SgDirectory::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDirectory*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDirectory::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDirectory::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDirectory::pool_size-1]);
        }
    }
}

void
SgDirectory::deleteMemoryPool() {
    for (auto p: SgDirectory::pools) {
        ROSE_FREE(p);
    }
    SgDirectory::next_node = nullptr;
    SgDirectory::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDirectory::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectory) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectory)};
    for (size_t blockIndex = SgDirectory::pools.size(); (blockIndex * SgDirectory::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectory) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectory) = %" PRIuPTR " SgDirectory::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectory),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectory),SgDirectory::pool_size);
        }
#endif

        SgDirectory* pointer{static_cast<SgDirectory*>(ROSE_MALLOC(SgDirectory::pool_size * sizeof(SgDirectory)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDirectory::pool_size * sizeof(SgDirectory));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDirectory::pool_size * sizeof(SgDirectory));
#endif
        SgDirectory::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDirectory::pool_size * sizeof(SgDirectory), V_SgDirectory));

        if (SgDirectory::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDirectory* blkptr{reinterpret_cast<SgDirectory*>(SgDirectory::pools[blockIndex-1])};
                blkptr[SgDirectory::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDirectory::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDirectory::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDirectory::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDirectory::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDirectory::pool_size - 1};
    SgDirectory* testPointer{reinterpret_cast<SgDirectory*>(SgDirectory::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDirectory::pool_size * (SgDirectory::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDirectoryStroageClass
 * from its counterpart of SgDirectory. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDirectory::initializeStorageClassArray(SgDirectoryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDirectory::pools.begin(); block != SgDirectory::pools.end(); ++block) {
        SgDirectory* pointer{reinterpret_cast<SgDirectory*>(*block)};
        for (unsigned i = 0; i < SgDirectory::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 9503 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFileList that takes its corresponding StorageClass as parameter
 */
SgFileList::SgFileList(const SgFileListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 9513 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_listOfFiles = storageSource.storageOf_listOfFiles.rebuildDataStoredInEasyStorageClass();
    SgFilePtrList::iterator i_listOfFiles = p_listOfFiles.begin();
    for ( ; i_listOfFiles != p_listOfFiles.end(); ++i_listOfFiles) {
        (*i_listOfFiles) = (SgFilePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_listOfFiles)) );
    }
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFileList*
SgFileList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFileList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFileList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFileList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFileList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFileList)};
        unsigned long positionInPool{localIndex % SgFileList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFileList::pool_size};
        returnPointer = &(((SgFileList*)(SgFileList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFileList*
SgFileList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFileList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFileList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFileList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFileList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFileList)};
        unsigned long positionInPool{localIndex % SgFileList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFileList::pool_size};
        returnPointer = &(((SgFileList*)(SgFileList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFileList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFileList::pools.begin(); block != SgFileList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFileList*>(*block)};
        for (unsigned i = 0; i < SgFileList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFileList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFileList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFileList* pointerOfLinkedList{nullptr};
    for (auto block = SgFileList::pools.begin(); block != SgFileList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFileList*>(*block)};
        for (unsigned i = 0; i < SgFileList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFileList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFileList::clearMemoryPool() {
    SgFileList* tempPointer{nullptr};
    if (SgFileList::pools.empty() == false) {
        auto block{SgFileList::pools.begin()};
        SgFileList::next_node = reinterpret_cast<SgFileList*>(*block);
        for (; block != SgFileList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFileList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFileList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFileList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFileList::pool_size-1]);
        }
    }
}

void
SgFileList::deleteMemoryPool() {
    for (auto p: SgFileList::pools) {
        ROSE_FREE(p);
    }
    SgFileList::next_node = nullptr;
    SgFileList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFileList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFileList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFileList)};
    for (size_t blockIndex = SgFileList::pools.size(); (blockIndex * SgFileList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFileList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFileList) = %" PRIuPTR " SgFileList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFileList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFileList),SgFileList::pool_size);
        }
#endif

        SgFileList* pointer{static_cast<SgFileList*>(ROSE_MALLOC(SgFileList::pool_size * sizeof(SgFileList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFileList::pool_size * sizeof(SgFileList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFileList::pool_size * sizeof(SgFileList));
#endif
        SgFileList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFileList::pool_size * sizeof(SgFileList), V_SgFileList));

        if (SgFileList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFileList* blkptr{reinterpret_cast<SgFileList*>(SgFileList::pools[blockIndex-1])};
                blkptr[SgFileList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFileList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFileList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFileList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFileList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFileList::pool_size - 1};
    SgFileList* testPointer{reinterpret_cast<SgFileList*>(SgFileList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFileList::pool_size * (SgFileList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFileListStroageClass
 * from its counterpart of SgFileList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFileList::initializeStorageClassArray(SgFileListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFileList::pools.begin(); block != SgFileList::pools.end(); ++block) {
        SgFileList* pointer{reinterpret_cast<SgFileList*>(*block)};
        for (unsigned i = 0; i < SgFileList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 9736 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDirectoryList that takes its corresponding StorageClass as parameter
 */
SgDirectoryList::SgDirectoryList(const SgDirectoryListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 9746 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_listOfDirectories = storageSource.storageOf_listOfDirectories.rebuildDataStoredInEasyStorageClass();
    SgDirectoryPtrList::iterator i_listOfDirectories = p_listOfDirectories.begin();
    for ( ; i_listOfDirectories != p_listOfDirectories.end(); ++i_listOfDirectories) {
        (*i_listOfDirectories) = (SgDirectoryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_listOfDirectories)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDirectoryList*
SgDirectoryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDirectoryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectoryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectoryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectoryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectoryList)};
        unsigned long positionInPool{localIndex % SgDirectoryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDirectoryList::pool_size};
        returnPointer = &(((SgDirectoryList*)(SgDirectoryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDirectoryList*
SgDirectoryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDirectoryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectoryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectoryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectoryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDirectoryList)};
        unsigned long positionInPool{localIndex % SgDirectoryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDirectoryList::pool_size};
        returnPointer = &(((SgDirectoryList*)(SgDirectoryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDirectoryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDirectoryList::pools.begin(); block != SgDirectoryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDirectoryList*>(*block)};
        for (unsigned i = 0; i < SgDirectoryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDirectoryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDirectoryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDirectoryList* pointerOfLinkedList{nullptr};
    for (auto block = SgDirectoryList::pools.begin(); block != SgDirectoryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDirectoryList*>(*block)};
        for (unsigned i = 0; i < SgDirectoryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDirectoryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDirectoryList::clearMemoryPool() {
    SgDirectoryList* tempPointer{nullptr};
    if (SgDirectoryList::pools.empty() == false) {
        auto block{SgDirectoryList::pools.begin()};
        SgDirectoryList::next_node = reinterpret_cast<SgDirectoryList*>(*block);
        for (; block != SgDirectoryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDirectoryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDirectoryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDirectoryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDirectoryList::pool_size-1]);
        }
    }
}

void
SgDirectoryList::deleteMemoryPool() {
    for (auto p: SgDirectoryList::pools) {
        ROSE_FREE(p);
    }
    SgDirectoryList::next_node = nullptr;
    SgDirectoryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDirectoryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectoryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectoryList)};
    for (size_t blockIndex = SgDirectoryList::pools.size(); (blockIndex * SgDirectoryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectoryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectoryList) = %" PRIuPTR " SgDirectoryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectoryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectoryList),SgDirectoryList::pool_size);
        }
#endif

        SgDirectoryList* pointer{static_cast<SgDirectoryList*>(ROSE_MALLOC(SgDirectoryList::pool_size * sizeof(SgDirectoryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDirectoryList::pool_size * sizeof(SgDirectoryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDirectoryList::pool_size * sizeof(SgDirectoryList));
#endif
        SgDirectoryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDirectoryList::pool_size * sizeof(SgDirectoryList), V_SgDirectoryList));

        if (SgDirectoryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDirectoryList* blkptr{reinterpret_cast<SgDirectoryList*>(SgDirectoryList::pools[blockIndex-1])};
                blkptr[SgDirectoryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDirectoryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDirectoryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDirectoryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDirectoryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDirectoryList::pool_size - 1};
    SgDirectoryList* testPointer{reinterpret_cast<SgDirectoryList*>(SgDirectoryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDirectoryList::pool_size * (SgDirectoryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDirectoryListStroageClass
 * from its counterpart of SgDirectoryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDirectoryList::initializeStorageClassArray(SgDirectoryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDirectoryList::pools.begin(); block != SgDirectoryList::pools.end(); ++block) {
        SgDirectoryList* pointer{reinterpret_cast<SgDirectoryList*>(*block)};
        for (unsigned i = 0; i < SgDirectoryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 9968 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionParameterTypeList that takes its corresponding StorageClass as parameter
 */
SgFunctionParameterTypeList::SgFunctionParameterTypeList(const SgFunctionParameterTypeListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 9978 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_arguments = storageSource.storageOf_arguments.rebuildDataStoredInEasyStorageClass();
    SgTypePtrList::iterator i_arguments = p_arguments.begin();
    for ( ; i_arguments != p_arguments.end(); ++i_arguments) {
        (*i_arguments) = (SgTypePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_arguments)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionParameterTypeList*
SgFunctionParameterTypeList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionParameterTypeList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterTypeList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterTypeList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterTypeList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterTypeList)};
        unsigned long positionInPool{localIndex % SgFunctionParameterTypeList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterTypeList::pool_size};
        returnPointer = &(((SgFunctionParameterTypeList*)(SgFunctionParameterTypeList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionParameterTypeList*
SgFunctionParameterTypeList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionParameterTypeList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterTypeList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterTypeList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterTypeList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionParameterTypeList)};
        unsigned long positionInPool{localIndex % SgFunctionParameterTypeList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterTypeList::pool_size};
        returnPointer = &(((SgFunctionParameterTypeList*)(SgFunctionParameterTypeList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionParameterTypeList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionParameterTypeList::pools.begin(); block != SgFunctionParameterTypeList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterTypeList*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterTypeList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionParameterTypeList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionParameterTypeList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionParameterTypeList* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionParameterTypeList::pools.begin(); block != SgFunctionParameterTypeList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterTypeList*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterTypeList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionParameterTypeList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionParameterTypeList::clearMemoryPool() {
    SgFunctionParameterTypeList* tempPointer{nullptr};
    if (SgFunctionParameterTypeList::pools.empty() == false) {
        auto block{SgFunctionParameterTypeList::pools.begin()};
        SgFunctionParameterTypeList::next_node = reinterpret_cast<SgFunctionParameterTypeList*>(*block);
        for (; block != SgFunctionParameterTypeList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionParameterTypeList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionParameterTypeList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionParameterTypeList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionParameterTypeList::pool_size-1]);
        }
    }
}

void
SgFunctionParameterTypeList::deleteMemoryPool() {
    for (auto p: SgFunctionParameterTypeList::pools) {
        ROSE_FREE(p);
    }
    SgFunctionParameterTypeList::next_node = nullptr;
    SgFunctionParameterTypeList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionParameterTypeList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterTypeList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterTypeList)};
    for (size_t blockIndex = SgFunctionParameterTypeList::pools.size(); (blockIndex * SgFunctionParameterTypeList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterTypeList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterTypeList) = %" PRIuPTR " SgFunctionParameterTypeList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterTypeList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterTypeList),SgFunctionParameterTypeList::pool_size);
        }
#endif

        SgFunctionParameterTypeList* pointer{static_cast<SgFunctionParameterTypeList*>(ROSE_MALLOC(SgFunctionParameterTypeList::pool_size * sizeof(SgFunctionParameterTypeList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionParameterTypeList::pool_size * sizeof(SgFunctionParameterTypeList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionParameterTypeList::pool_size * sizeof(SgFunctionParameterTypeList));
#endif
        SgFunctionParameterTypeList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionParameterTypeList::pool_size * sizeof(SgFunctionParameterTypeList), V_SgFunctionParameterTypeList));

        if (SgFunctionParameterTypeList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionParameterTypeList* blkptr{reinterpret_cast<SgFunctionParameterTypeList*>(SgFunctionParameterTypeList::pools[blockIndex-1])};
                blkptr[SgFunctionParameterTypeList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionParameterTypeList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionParameterTypeList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionParameterTypeList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionParameterTypeList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionParameterTypeList::pool_size - 1};
    SgFunctionParameterTypeList* testPointer{reinterpret_cast<SgFunctionParameterTypeList*>(SgFunctionParameterTypeList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionParameterTypeList::pool_size * (SgFunctionParameterTypeList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionParameterTypeListStroageClass
 * from its counterpart of SgFunctionParameterTypeList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionParameterTypeList::initializeStorageClassArray(SgFunctionParameterTypeListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionParameterTypeList::pools.begin(); block != SgFunctionParameterTypeList::pools.end(); ++block) {
        SgFunctionParameterTypeList* pointer{reinterpret_cast<SgFunctionParameterTypeList*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterTypeList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 10200 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgQualifiedName that takes its corresponding StorageClass as parameter
 */
SgQualifiedName::SgQualifiedName(const SgQualifiedNameStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 10210 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgQualifiedName*
SgQualifiedName::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgQualifiedName* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgQualifiedName)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgQualifiedName + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgQualifiedName)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedName)};
        unsigned long positionInPool{localIndex % SgQualifiedName::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgQualifiedName::pool_size};
        returnPointer = &(((SgQualifiedName*)(SgQualifiedName::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgQualifiedName*
SgQualifiedName::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgQualifiedName* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgQualifiedName)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgQualifiedName + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgQualifiedName)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgQualifiedName)};
        unsigned long positionInPool{localIndex % SgQualifiedName::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgQualifiedName::pool_size};
        returnPointer = &(((SgQualifiedName*)(SgQualifiedName::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgQualifiedName::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgQualifiedName::pools.begin(); block != SgQualifiedName::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgQualifiedName*>(*block)};
        for (unsigned i = 0; i < SgQualifiedName::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgQualifiedName*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgQualifiedName::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgQualifiedName* pointerOfLinkedList{nullptr};
    for (auto block = SgQualifiedName::pools.begin(); block != SgQualifiedName::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgQualifiedName*>(*block)};
        for (unsigned i = 0; i < SgQualifiedName::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgQualifiedName::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgQualifiedName::clearMemoryPool() {
    SgQualifiedName* tempPointer{nullptr};
    if (SgQualifiedName::pools.empty() == false) {
        auto block{SgQualifiedName::pools.begin()};
        SgQualifiedName::next_node = reinterpret_cast<SgQualifiedName*>(*block);
        for (; block != SgQualifiedName::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgQualifiedName*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgQualifiedName::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgQualifiedName::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgQualifiedName::pool_size-1]);
        }
    }
}

void
SgQualifiedName::deleteMemoryPool() {
    for (auto p: SgQualifiedName::pools) {
        ROSE_FREE(p);
    }
    SgQualifiedName::next_node = nullptr;
    SgQualifiedName::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgQualifiedName::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedName) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgQualifiedName)};
    for (size_t blockIndex = SgQualifiedName::pools.size(); (blockIndex * SgQualifiedName::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedName) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgQualifiedName) = %" PRIuPTR " SgQualifiedName::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedName),AST_FILE_IO::getPoolSizeOfNewAst(V_SgQualifiedName),SgQualifiedName::pool_size);
        }
#endif

        SgQualifiedName* pointer{static_cast<SgQualifiedName*>(ROSE_MALLOC(SgQualifiedName::pool_size * sizeof(SgQualifiedName)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgQualifiedName::pool_size * sizeof(SgQualifiedName));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgQualifiedName::pool_size * sizeof(SgQualifiedName));
#endif
        SgQualifiedName::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgQualifiedName::pool_size * sizeof(SgQualifiedName), V_SgQualifiedName));

        if (SgQualifiedName::next_node != nullptr) {
            if (blockIndex > 0) {
                SgQualifiedName* blkptr{reinterpret_cast<SgQualifiedName*>(SgQualifiedName::pools[blockIndex-1])};
                blkptr[SgQualifiedName::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgQualifiedName::next_node = pointer;
        }

        for (unsigned i = 0; i < SgQualifiedName::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgQualifiedName::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgQualifiedName::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgQualifiedName::pool_size - 1};
    SgQualifiedName* testPointer{reinterpret_cast<SgQualifiedName*>(SgQualifiedName::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgQualifiedName::pool_size * (SgQualifiedName::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgQualifiedNameStroageClass
 * from its counterpart of SgQualifiedName. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgQualifiedName::initializeStorageClassArray(SgQualifiedNameStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgQualifiedName::pools.begin(); block != SgQualifiedName::pools.end(); ++block) {
        SgQualifiedName* pointer{reinterpret_cast<SgQualifiedName*>(*block)};
        for (unsigned i = 0; i < SgQualifiedName::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 10428 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateArgumentList that takes its corresponding StorageClass as parameter
 */
SgTemplateArgumentList::SgTemplateArgumentList(const SgTemplateArgumentListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 10438 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_args = storageSource.storageOf_args.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_args = p_args.begin();
    for ( ; i_args != p_args.end(); ++i_args) {
        (*i_args) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_args)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateArgumentList*
SgTemplateArgumentList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateArgumentList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateArgumentList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateArgumentList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateArgumentList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgumentList)};
        unsigned long positionInPool{localIndex % SgTemplateArgumentList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateArgumentList::pool_size};
        returnPointer = &(((SgTemplateArgumentList*)(SgTemplateArgumentList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateArgumentList*
SgTemplateArgumentList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateArgumentList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateArgumentList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateArgumentList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateArgumentList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateArgumentList)};
        unsigned long positionInPool{localIndex % SgTemplateArgumentList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateArgumentList::pool_size};
        returnPointer = &(((SgTemplateArgumentList*)(SgTemplateArgumentList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateArgumentList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateArgumentList::pools.begin(); block != SgTemplateArgumentList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateArgumentList*>(*block)};
        for (unsigned i = 0; i < SgTemplateArgumentList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateArgumentList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateArgumentList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateArgumentList* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateArgumentList::pools.begin(); block != SgTemplateArgumentList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateArgumentList*>(*block)};
        for (unsigned i = 0; i < SgTemplateArgumentList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateArgumentList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateArgumentList::clearMemoryPool() {
    SgTemplateArgumentList* tempPointer{nullptr};
    if (SgTemplateArgumentList::pools.empty() == false) {
        auto block{SgTemplateArgumentList::pools.begin()};
        SgTemplateArgumentList::next_node = reinterpret_cast<SgTemplateArgumentList*>(*block);
        for (; block != SgTemplateArgumentList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateArgumentList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateArgumentList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateArgumentList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateArgumentList::pool_size-1]);
        }
    }
}

void
SgTemplateArgumentList::deleteMemoryPool() {
    for (auto p: SgTemplateArgumentList::pools) {
        ROSE_FREE(p);
    }
    SgTemplateArgumentList::next_node = nullptr;
    SgTemplateArgumentList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateArgumentList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgumentList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateArgumentList)};
    for (size_t blockIndex = SgTemplateArgumentList::pools.size(); (blockIndex * SgTemplateArgumentList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgumentList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateArgumentList) = %" PRIuPTR " SgTemplateArgumentList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateArgumentList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateArgumentList),SgTemplateArgumentList::pool_size);
        }
#endif

        SgTemplateArgumentList* pointer{static_cast<SgTemplateArgumentList*>(ROSE_MALLOC(SgTemplateArgumentList::pool_size * sizeof(SgTemplateArgumentList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateArgumentList::pool_size * sizeof(SgTemplateArgumentList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateArgumentList::pool_size * sizeof(SgTemplateArgumentList));
#endif
        SgTemplateArgumentList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateArgumentList::pool_size * sizeof(SgTemplateArgumentList), V_SgTemplateArgumentList));

        if (SgTemplateArgumentList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateArgumentList* blkptr{reinterpret_cast<SgTemplateArgumentList*>(SgTemplateArgumentList::pools[blockIndex-1])};
                blkptr[SgTemplateArgumentList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateArgumentList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateArgumentList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateArgumentList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateArgumentList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateArgumentList::pool_size - 1};
    SgTemplateArgumentList* testPointer{reinterpret_cast<SgTemplateArgumentList*>(SgTemplateArgumentList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateArgumentList::pool_size * (SgTemplateArgumentList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateArgumentListStroageClass
 * from its counterpart of SgTemplateArgumentList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateArgumentList::initializeStorageClassArray(SgTemplateArgumentListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateArgumentList::pools.begin(); block != SgTemplateArgumentList::pools.end(); ++block) {
        SgTemplateArgumentList* pointer{reinterpret_cast<SgTemplateArgumentList*>(*block)};
        for (unsigned i = 0; i < SgTemplateArgumentList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 10660 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateParameterList that takes its corresponding StorageClass as parameter
 */
SgTemplateParameterList::SgTemplateParameterList(const SgTemplateParameterListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 10670 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_args = storageSource.storageOf_args.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_args = p_args.begin();
    for ( ; i_args != p_args.end(); ++i_args) {
        (*i_args) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_args)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateParameterList*
SgTemplateParameterList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateParameterList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameterList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameterList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameterList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterList)};
        unsigned long positionInPool{localIndex % SgTemplateParameterList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateParameterList::pool_size};
        returnPointer = &(((SgTemplateParameterList*)(SgTemplateParameterList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateParameterList*
SgTemplateParameterList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateParameterList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameterList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameterList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameterList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateParameterList)};
        unsigned long positionInPool{localIndex % SgTemplateParameterList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateParameterList::pool_size};
        returnPointer = &(((SgTemplateParameterList*)(SgTemplateParameterList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateParameterList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateParameterList::pools.begin(); block != SgTemplateParameterList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateParameterList*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameterList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateParameterList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateParameterList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateParameterList* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateParameterList::pools.begin(); block != SgTemplateParameterList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateParameterList*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameterList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateParameterList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateParameterList::clearMemoryPool() {
    SgTemplateParameterList* tempPointer{nullptr};
    if (SgTemplateParameterList::pools.empty() == false) {
        auto block{SgTemplateParameterList::pools.begin()};
        SgTemplateParameterList::next_node = reinterpret_cast<SgTemplateParameterList*>(*block);
        for (; block != SgTemplateParameterList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateParameterList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateParameterList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateParameterList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateParameterList::pool_size-1]);
        }
    }
}

void
SgTemplateParameterList::deleteMemoryPool() {
    for (auto p: SgTemplateParameterList::pools) {
        ROSE_FREE(p);
    }
    SgTemplateParameterList::next_node = nullptr;
    SgTemplateParameterList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateParameterList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameterList)};
    for (size_t blockIndex = SgTemplateParameterList::pools.size(); (blockIndex * SgTemplateParameterList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameterList) = %" PRIuPTR " SgTemplateParameterList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameterList),SgTemplateParameterList::pool_size);
        }
#endif

        SgTemplateParameterList* pointer{static_cast<SgTemplateParameterList*>(ROSE_MALLOC(SgTemplateParameterList::pool_size * sizeof(SgTemplateParameterList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateParameterList::pool_size * sizeof(SgTemplateParameterList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateParameterList::pool_size * sizeof(SgTemplateParameterList));
#endif
        SgTemplateParameterList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateParameterList::pool_size * sizeof(SgTemplateParameterList), V_SgTemplateParameterList));

        if (SgTemplateParameterList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateParameterList* blkptr{reinterpret_cast<SgTemplateParameterList*>(SgTemplateParameterList::pools[blockIndex-1])};
                blkptr[SgTemplateParameterList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateParameterList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateParameterList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateParameterList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateParameterList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateParameterList::pool_size - 1};
    SgTemplateParameterList* testPointer{reinterpret_cast<SgTemplateParameterList*>(SgTemplateParameterList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateParameterList::pool_size * (SgTemplateParameterList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateParameterListStroageClass
 * from its counterpart of SgTemplateParameterList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateParameterList::initializeStorageClassArray(SgTemplateParameterListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateParameterList::pools.begin(); block != SgTemplateParameterList::pools.end(); ++block) {
        SgTemplateParameterList* pointer{reinterpret_cast<SgTemplateParameterList*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameterList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 10892 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGraph that takes its corresponding StorageClass as parameter
 */
SgGraph::SgGraph(const SgGraphStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 10902 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
    p_node_index_to_node_map = storageSource.storageOf_node_index_to_node_map.rebuildDataStoredInEasyStorageClass();
    rose_graph_integer_node_hash_map::iterator integer_node_it;
    for (integer_node_it = p_node_index_to_node_map.begin(); integer_node_it != p_node_index_to_node_map.end(); ++integer_node_it) {
        integer_node_it->second = (SgGraphNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex( (unsigned long)(integer_node_it->second)));
    }
    p_edge_index_to_edge_map = storageSource.storageOf_edge_index_to_edge_map.rebuildDataStoredInEasyStorageClass();
    rose_graph_integer_edge_hash_map::iterator integer_edge_it;
    for (integer_edge_it = p_edge_index_to_edge_map.begin(); integer_edge_it != p_edge_index_to_edge_map.end(); ++integer_edge_it) {
        integer_edge_it->second = (SgGraphEdge*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex((unsigned long)(integer_edge_it->second)));
    }
     p_node_index_pair_to_edge_multimap = storageSource.storageOf_node_index_pair_to_edge_multimap.rebuildDataStoredInEasyStorageClass();
     rose_graph_integerpair_edge_hash_multimap::iterator integerpair_edge_it;
     for (integerpair_edge_it = p_node_index_pair_to_edge_multimap.begin(); integerpair_edge_it != p_node_index_pair_to_edge_multimap.end(); ++integerpair_edge_it) {
         integerpair_edge_it->second = nullptr; printf ("Error: not implemented support for rose_graph_integerpair_edge_hash_multimap \n"); ASSERT_require(false);
     }
   {
     p_string_to_node_index_multimap = storageSource.storageOf_string_to_node_index_multimap.rebuildDataStoredInEasyStorageClass();
     rose_graph_string_integer_hash_multimap::iterator string_integer_it;
     for (string_integer_it = p_string_to_node_index_multimap.begin(); string_integer_it != p_string_to_node_index_multimap.end(); ++string_integer_it) {
         string_integer_it->second = 0; printf("Unimplemented support for rose_graph_string_integer_hash_multimap: Fixme! \n"); ASSERT_require(false);
     }
   }
   {
     p_string_to_edge_index_multimap = storageSource.storageOf_string_to_edge_index_multimap.rebuildDataStoredInEasyStorageClass();
     rose_graph_string_integer_hash_multimap::iterator string_integer_it;
     for (string_integer_it = p_string_to_edge_index_multimap.begin(); string_integer_it != p_string_to_edge_index_multimap.end(); ++string_integer_it) {
         string_integer_it->second = 0; printf("Unimplemented support for rose_graph_string_integer_hash_multimap: Fixme! \n"); ASSERT_require(false);
     }
   }
   {
     p_node_index_to_edge_multimap = storageSource.storageOf_node_index_to_edge_multimap.rebuildDataStoredInEasyStorageClass();
     rose_graph_integer_edge_hash_multimap::iterator integer_edge_it2;
     for (integer_edge_it2 = p_node_index_to_edge_multimap.begin(); integer_edge_it2 != p_node_index_to_edge_multimap.end(); ++integer_edge_it2) {
         integer_edge_it2->second = (SgGraphEdge*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex( (unsigned long)(integer_edge_it2->second)));
     }
   }
    p_index = storageSource.storageOf_index;
    p_boost_edges = storageSource.storageOf_boost_edges.rebuildDataStoredInEasyStorageClass();
    p_boost_edge_weights = storageSource.storageOf_boost_edge_weights.rebuildDataStoredInEasyStorageClass();
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();
    p_properties = storageSource.storageOf_properties.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGraph*
SgGraph::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGraph)};
        unsigned long positionInPool{localIndex % SgGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraph::pool_size};
        returnPointer = &(((SgGraph*)(SgGraph::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGraph*
SgGraph::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGraph)};
        unsigned long positionInPool{localIndex % SgGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraph::pool_size};
        returnPointer = &(((SgGraph*)(SgGraph::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGraph::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGraph::pools.begin(); block != SgGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraph*>(*block)};
        for (unsigned i = 0; i < SgGraph::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGraph*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGraph::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGraph* pointerOfLinkedList{nullptr};
    for (auto block = SgGraph::pools.begin(); block != SgGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraph*>(*block)};
        for (unsigned i = 0; i < SgGraph::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGraph::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGraph::clearMemoryPool() {
    SgGraph* tempPointer{nullptr};
    if (SgGraph::pools.empty() == false) {
        auto block{SgGraph::pools.begin()};
        SgGraph::next_node = reinterpret_cast<SgGraph*>(*block);
        for (; block != SgGraph::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGraph*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGraph::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGraph::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGraph::pool_size-1]);
        }
    }
}

void
SgGraph::deleteMemoryPool() {
    for (auto p: SgGraph::pools) {
        ROSE_FREE(p);
    }
    SgGraph::next_node = nullptr;
    SgGraph::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGraph::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGraph) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraph)};
    for (size_t blockIndex = SgGraph::pools.size(); (blockIndex * SgGraph::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGraph) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraph) = %" PRIuPTR " SgGraph::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGraph),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraph),SgGraph::pool_size);
        }
#endif

        SgGraph* pointer{static_cast<SgGraph*>(ROSE_MALLOC(SgGraph::pool_size * sizeof(SgGraph)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGraph::pool_size * sizeof(SgGraph));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGraph::pool_size * sizeof(SgGraph));
#endif
        SgGraph::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGraph::pool_size * sizeof(SgGraph), V_SgGraph));

        if (SgGraph::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGraph* blkptr{reinterpret_cast<SgGraph*>(SgGraph::pools[blockIndex-1])};
                blkptr[SgGraph::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGraph::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGraph::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGraph::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGraph::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGraph::pool_size - 1};
    SgGraph* testPointer{reinterpret_cast<SgGraph*>(SgGraph::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGraph::pool_size * (SgGraph::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGraphStroageClass
 * from its counterpart of SgGraph. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGraph::initializeStorageClassArray(SgGraphStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGraph::pools.begin(); block != SgGraph::pools.end(); ++block) {
        SgGraph* pointer{reinterpret_cast<SgGraph*>(*block)};
        for (unsigned i = 0; i < SgGraph::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 11161 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIncidenceDirectedGraph that takes its corresponding StorageClass as parameter
 */
SgIncidenceDirectedGraph::SgIncidenceDirectedGraph(const SgIncidenceDirectedGraphStorageClass& storageSource)  : SgGraph (storageSource) {

/* #line 11171 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
   {
     p_node_index_to_edge_multimap_edgesOut = storageSource.storageOf_node_index_to_edge_multimap_edgesOut.rebuildDataStoredInEasyStorageClass();
     rose_graph_integer_edge_hash_multimap::iterator integer_edge_it2;
     for (integer_edge_it2 = p_node_index_to_edge_multimap_edgesOut.begin(); integer_edge_it2 != p_node_index_to_edge_multimap_edgesOut.end(); ++integer_edge_it2) {
         integer_edge_it2->second = (SgGraphEdge*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex( (unsigned long)(integer_edge_it2->second)));
     }
   }
   {
     p_node_index_to_edge_multimap_edgesIn = storageSource.storageOf_node_index_to_edge_multimap_edgesIn.rebuildDataStoredInEasyStorageClass();
     rose_graph_integer_edge_hash_multimap::iterator integer_edge_it2;
     for (integer_edge_it2 = p_node_index_to_edge_multimap_edgesIn.begin(); integer_edge_it2 != p_node_index_to_edge_multimap_edgesIn.end(); ++integer_edge_it2) {
         integer_edge_it2->second = (SgGraphEdge*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex( (unsigned long)(integer_edge_it2->second)));
     }
   }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIncidenceDirectedGraph*
SgIncidenceDirectedGraph::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIncidenceDirectedGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncidenceDirectedGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncidenceDirectedGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncidenceDirectedGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceDirectedGraph)};
        unsigned long positionInPool{localIndex % SgIncidenceDirectedGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncidenceDirectedGraph::pool_size};
        returnPointer = &(((SgIncidenceDirectedGraph*)(SgIncidenceDirectedGraph::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIncidenceDirectedGraph*
SgIncidenceDirectedGraph::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIncidenceDirectedGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncidenceDirectedGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncidenceDirectedGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncidenceDirectedGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIncidenceDirectedGraph)};
        unsigned long positionInPool{localIndex % SgIncidenceDirectedGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncidenceDirectedGraph::pool_size};
        returnPointer = &(((SgIncidenceDirectedGraph*)(SgIncidenceDirectedGraph::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIncidenceDirectedGraph::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIncidenceDirectedGraph::pools.begin(); block != SgIncidenceDirectedGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncidenceDirectedGraph*>(*block)};
        for (unsigned i = 0; i < SgIncidenceDirectedGraph::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIncidenceDirectedGraph*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIncidenceDirectedGraph::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIncidenceDirectedGraph* pointerOfLinkedList{nullptr};
    for (auto block = SgIncidenceDirectedGraph::pools.begin(); block != SgIncidenceDirectedGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncidenceDirectedGraph*>(*block)};
        for (unsigned i = 0; i < SgIncidenceDirectedGraph::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIncidenceDirectedGraph::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIncidenceDirectedGraph::clearMemoryPool() {
    SgIncidenceDirectedGraph* tempPointer{nullptr};
    if (SgIncidenceDirectedGraph::pools.empty() == false) {
        auto block{SgIncidenceDirectedGraph::pools.begin()};
        SgIncidenceDirectedGraph::next_node = reinterpret_cast<SgIncidenceDirectedGraph*>(*block);
        for (; block != SgIncidenceDirectedGraph::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIncidenceDirectedGraph*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIncidenceDirectedGraph::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIncidenceDirectedGraph::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIncidenceDirectedGraph::pool_size-1]);
        }
    }
}

void
SgIncidenceDirectedGraph::deleteMemoryPool() {
    for (auto p: SgIncidenceDirectedGraph::pools) {
        ROSE_FREE(p);
    }
    SgIncidenceDirectedGraph::next_node = nullptr;
    SgIncidenceDirectedGraph::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIncidenceDirectedGraph::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceDirectedGraph) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncidenceDirectedGraph)};
    for (size_t blockIndex = SgIncidenceDirectedGraph::pools.size(); (blockIndex * SgIncidenceDirectedGraph::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceDirectedGraph) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncidenceDirectedGraph) = %" PRIuPTR " SgIncidenceDirectedGraph::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceDirectedGraph),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncidenceDirectedGraph),SgIncidenceDirectedGraph::pool_size);
        }
#endif

        SgIncidenceDirectedGraph* pointer{static_cast<SgIncidenceDirectedGraph*>(ROSE_MALLOC(SgIncidenceDirectedGraph::pool_size * sizeof(SgIncidenceDirectedGraph)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIncidenceDirectedGraph::pool_size * sizeof(SgIncidenceDirectedGraph));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIncidenceDirectedGraph::pool_size * sizeof(SgIncidenceDirectedGraph));
#endif
        SgIncidenceDirectedGraph::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIncidenceDirectedGraph::pool_size * sizeof(SgIncidenceDirectedGraph), V_SgIncidenceDirectedGraph));

        if (SgIncidenceDirectedGraph::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIncidenceDirectedGraph* blkptr{reinterpret_cast<SgIncidenceDirectedGraph*>(SgIncidenceDirectedGraph::pools[blockIndex-1])};
                blkptr[SgIncidenceDirectedGraph::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIncidenceDirectedGraph::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIncidenceDirectedGraph::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIncidenceDirectedGraph::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIncidenceDirectedGraph::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIncidenceDirectedGraph::pool_size - 1};
    SgIncidenceDirectedGraph* testPointer{reinterpret_cast<SgIncidenceDirectedGraph*>(SgIncidenceDirectedGraph::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIncidenceDirectedGraph::pool_size * (SgIncidenceDirectedGraph::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIncidenceDirectedGraphStroageClass
 * from its counterpart of SgIncidenceDirectedGraph. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIncidenceDirectedGraph::initializeStorageClassArray(SgIncidenceDirectedGraphStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIncidenceDirectedGraph::pools.begin(); block != SgIncidenceDirectedGraph::pools.end(); ++block) {
        SgIncidenceDirectedGraph* pointer{reinterpret_cast<SgIncidenceDirectedGraph*>(*block)};
        for (unsigned i = 0; i < SgIncidenceDirectedGraph::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 11402 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBidirectionalGraph that takes its corresponding StorageClass as parameter
 */
SgBidirectionalGraph::SgBidirectionalGraph(const SgBidirectionalGraphStorageClass& storageSource)  : SgIncidenceDirectedGraph (storageSource) {

/* #line 11412 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBidirectionalGraph*
SgBidirectionalGraph::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBidirectionalGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBidirectionalGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBidirectionalGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBidirectionalGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBidirectionalGraph)};
        unsigned long positionInPool{localIndex % SgBidirectionalGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBidirectionalGraph::pool_size};
        returnPointer = &(((SgBidirectionalGraph*)(SgBidirectionalGraph::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBidirectionalGraph*
SgBidirectionalGraph::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBidirectionalGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBidirectionalGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBidirectionalGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBidirectionalGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBidirectionalGraph)};
        unsigned long positionInPool{localIndex % SgBidirectionalGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBidirectionalGraph::pool_size};
        returnPointer = &(((SgBidirectionalGraph*)(SgBidirectionalGraph::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBidirectionalGraph::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBidirectionalGraph::pools.begin(); block != SgBidirectionalGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgBidirectionalGraph::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBidirectionalGraph*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBidirectionalGraph::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBidirectionalGraph* pointerOfLinkedList{nullptr};
    for (auto block = SgBidirectionalGraph::pools.begin(); block != SgBidirectionalGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgBidirectionalGraph::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBidirectionalGraph::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBidirectionalGraph::clearMemoryPool() {
    SgBidirectionalGraph* tempPointer{nullptr};
    if (SgBidirectionalGraph::pools.empty() == false) {
        auto block{SgBidirectionalGraph::pools.begin()};
        SgBidirectionalGraph::next_node = reinterpret_cast<SgBidirectionalGraph*>(*block);
        for (; block != SgBidirectionalGraph::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBidirectionalGraph*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBidirectionalGraph::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBidirectionalGraph::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBidirectionalGraph::pool_size-1]);
        }
    }
}

void
SgBidirectionalGraph::deleteMemoryPool() {
    for (auto p: SgBidirectionalGraph::pools) {
        ROSE_FREE(p);
    }
    SgBidirectionalGraph::next_node = nullptr;
    SgBidirectionalGraph::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBidirectionalGraph::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBidirectionalGraph) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBidirectionalGraph)};
    for (size_t blockIndex = SgBidirectionalGraph::pools.size(); (blockIndex * SgBidirectionalGraph::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBidirectionalGraph) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBidirectionalGraph) = %" PRIuPTR " SgBidirectionalGraph::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBidirectionalGraph),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBidirectionalGraph),SgBidirectionalGraph::pool_size);
        }
#endif

        SgBidirectionalGraph* pointer{static_cast<SgBidirectionalGraph*>(ROSE_MALLOC(SgBidirectionalGraph::pool_size * sizeof(SgBidirectionalGraph)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBidirectionalGraph::pool_size * sizeof(SgBidirectionalGraph));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBidirectionalGraph::pool_size * sizeof(SgBidirectionalGraph));
#endif
        SgBidirectionalGraph::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBidirectionalGraph::pool_size * sizeof(SgBidirectionalGraph), V_SgBidirectionalGraph));

        if (SgBidirectionalGraph::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBidirectionalGraph* blkptr{reinterpret_cast<SgBidirectionalGraph*>(SgBidirectionalGraph::pools[blockIndex-1])};
                blkptr[SgBidirectionalGraph::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBidirectionalGraph::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBidirectionalGraph::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBidirectionalGraph::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBidirectionalGraph::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBidirectionalGraph::pool_size - 1};
    SgBidirectionalGraph* testPointer{reinterpret_cast<SgBidirectionalGraph*>(SgBidirectionalGraph::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBidirectionalGraph::pool_size * (SgBidirectionalGraph::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBidirectionalGraphStroageClass
 * from its counterpart of SgBidirectionalGraph. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBidirectionalGraph::initializeStorageClassArray(SgBidirectionalGraphStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBidirectionalGraph::pools.begin(); block != SgBidirectionalGraph::pools.end(); ++block) {
        SgBidirectionalGraph* pointer{reinterpret_cast<SgBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgBidirectionalGraph::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 11629 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStringKeyedBidirectionalGraph that takes its corresponding StorageClass as parameter
 */
SgStringKeyedBidirectionalGraph::SgStringKeyedBidirectionalGraph(const SgStringKeyedBidirectionalGraphStorageClass& storageSource)  : SgBidirectionalGraph (storageSource) {

/* #line 11639 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStringKeyedBidirectionalGraph*
SgStringKeyedBidirectionalGraph::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStringKeyedBidirectionalGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringKeyedBidirectionalGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringKeyedBidirectionalGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringKeyedBidirectionalGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStringKeyedBidirectionalGraph)};
        unsigned long positionInPool{localIndex % SgStringKeyedBidirectionalGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStringKeyedBidirectionalGraph::pool_size};
        returnPointer = &(((SgStringKeyedBidirectionalGraph*)(SgStringKeyedBidirectionalGraph::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStringKeyedBidirectionalGraph*
SgStringKeyedBidirectionalGraph::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStringKeyedBidirectionalGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringKeyedBidirectionalGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringKeyedBidirectionalGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringKeyedBidirectionalGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStringKeyedBidirectionalGraph)};
        unsigned long positionInPool{localIndex % SgStringKeyedBidirectionalGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStringKeyedBidirectionalGraph::pool_size};
        returnPointer = &(((SgStringKeyedBidirectionalGraph*)(SgStringKeyedBidirectionalGraph::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStringKeyedBidirectionalGraph::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStringKeyedBidirectionalGraph::pools.begin(); block != SgStringKeyedBidirectionalGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStringKeyedBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgStringKeyedBidirectionalGraph::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStringKeyedBidirectionalGraph*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStringKeyedBidirectionalGraph::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStringKeyedBidirectionalGraph* pointerOfLinkedList{nullptr};
    for (auto block = SgStringKeyedBidirectionalGraph::pools.begin(); block != SgStringKeyedBidirectionalGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStringKeyedBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgStringKeyedBidirectionalGraph::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStringKeyedBidirectionalGraph::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStringKeyedBidirectionalGraph::clearMemoryPool() {
    SgStringKeyedBidirectionalGraph* tempPointer{nullptr};
    if (SgStringKeyedBidirectionalGraph::pools.empty() == false) {
        auto block{SgStringKeyedBidirectionalGraph::pools.begin()};
        SgStringKeyedBidirectionalGraph::next_node = reinterpret_cast<SgStringKeyedBidirectionalGraph*>(*block);
        for (; block != SgStringKeyedBidirectionalGraph::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStringKeyedBidirectionalGraph*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStringKeyedBidirectionalGraph::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStringKeyedBidirectionalGraph::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStringKeyedBidirectionalGraph::pool_size-1]);
        }
    }
}

void
SgStringKeyedBidirectionalGraph::deleteMemoryPool() {
    for (auto p: SgStringKeyedBidirectionalGraph::pools) {
        ROSE_FREE(p);
    }
    SgStringKeyedBidirectionalGraph::next_node = nullptr;
    SgStringKeyedBidirectionalGraph::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStringKeyedBidirectionalGraph::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStringKeyedBidirectionalGraph) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringKeyedBidirectionalGraph)};
    for (size_t blockIndex = SgStringKeyedBidirectionalGraph::pools.size(); (blockIndex * SgStringKeyedBidirectionalGraph::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStringKeyedBidirectionalGraph) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringKeyedBidirectionalGraph) = %" PRIuPTR " SgStringKeyedBidirectionalGraph::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStringKeyedBidirectionalGraph),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringKeyedBidirectionalGraph),SgStringKeyedBidirectionalGraph::pool_size);
        }
#endif

        SgStringKeyedBidirectionalGraph* pointer{static_cast<SgStringKeyedBidirectionalGraph*>(ROSE_MALLOC(SgStringKeyedBidirectionalGraph::pool_size * sizeof(SgStringKeyedBidirectionalGraph)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStringKeyedBidirectionalGraph::pool_size * sizeof(SgStringKeyedBidirectionalGraph));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStringKeyedBidirectionalGraph::pool_size * sizeof(SgStringKeyedBidirectionalGraph));
#endif
        SgStringKeyedBidirectionalGraph::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStringKeyedBidirectionalGraph::pool_size * sizeof(SgStringKeyedBidirectionalGraph), V_SgStringKeyedBidirectionalGraph));

        if (SgStringKeyedBidirectionalGraph::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStringKeyedBidirectionalGraph* blkptr{reinterpret_cast<SgStringKeyedBidirectionalGraph*>(SgStringKeyedBidirectionalGraph::pools[blockIndex-1])};
                blkptr[SgStringKeyedBidirectionalGraph::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStringKeyedBidirectionalGraph::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStringKeyedBidirectionalGraph::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStringKeyedBidirectionalGraph::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStringKeyedBidirectionalGraph::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStringKeyedBidirectionalGraph::pool_size - 1};
    SgStringKeyedBidirectionalGraph* testPointer{reinterpret_cast<SgStringKeyedBidirectionalGraph*>(SgStringKeyedBidirectionalGraph::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStringKeyedBidirectionalGraph::pool_size * (SgStringKeyedBidirectionalGraph::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStringKeyedBidirectionalGraphStroageClass
 * from its counterpart of SgStringKeyedBidirectionalGraph. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStringKeyedBidirectionalGraph::initializeStorageClassArray(SgStringKeyedBidirectionalGraphStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStringKeyedBidirectionalGraph::pools.begin(); block != SgStringKeyedBidirectionalGraph::pools.end(); ++block) {
        SgStringKeyedBidirectionalGraph* pointer{reinterpret_cast<SgStringKeyedBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgStringKeyedBidirectionalGraph::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 11856 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIntKeyedBidirectionalGraph that takes its corresponding StorageClass as parameter
 */
SgIntKeyedBidirectionalGraph::SgIntKeyedBidirectionalGraph(const SgIntKeyedBidirectionalGraphStorageClass& storageSource)  : SgBidirectionalGraph (storageSource) {

/* #line 11866 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIntKeyedBidirectionalGraph*
SgIntKeyedBidirectionalGraph::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIntKeyedBidirectionalGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntKeyedBidirectionalGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntKeyedBidirectionalGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntKeyedBidirectionalGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIntKeyedBidirectionalGraph)};
        unsigned long positionInPool{localIndex % SgIntKeyedBidirectionalGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntKeyedBidirectionalGraph::pool_size};
        returnPointer = &(((SgIntKeyedBidirectionalGraph*)(SgIntKeyedBidirectionalGraph::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIntKeyedBidirectionalGraph*
SgIntKeyedBidirectionalGraph::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIntKeyedBidirectionalGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntKeyedBidirectionalGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntKeyedBidirectionalGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntKeyedBidirectionalGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIntKeyedBidirectionalGraph)};
        unsigned long positionInPool{localIndex % SgIntKeyedBidirectionalGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntKeyedBidirectionalGraph::pool_size};
        returnPointer = &(((SgIntKeyedBidirectionalGraph*)(SgIntKeyedBidirectionalGraph::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIntKeyedBidirectionalGraph::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIntKeyedBidirectionalGraph::pools.begin(); block != SgIntKeyedBidirectionalGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntKeyedBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgIntKeyedBidirectionalGraph::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIntKeyedBidirectionalGraph*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIntKeyedBidirectionalGraph::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIntKeyedBidirectionalGraph* pointerOfLinkedList{nullptr};
    for (auto block = SgIntKeyedBidirectionalGraph::pools.begin(); block != SgIntKeyedBidirectionalGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntKeyedBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgIntKeyedBidirectionalGraph::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIntKeyedBidirectionalGraph::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIntKeyedBidirectionalGraph::clearMemoryPool() {
    SgIntKeyedBidirectionalGraph* tempPointer{nullptr};
    if (SgIntKeyedBidirectionalGraph::pools.empty() == false) {
        auto block{SgIntKeyedBidirectionalGraph::pools.begin()};
        SgIntKeyedBidirectionalGraph::next_node = reinterpret_cast<SgIntKeyedBidirectionalGraph*>(*block);
        for (; block != SgIntKeyedBidirectionalGraph::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIntKeyedBidirectionalGraph*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIntKeyedBidirectionalGraph::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIntKeyedBidirectionalGraph::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIntKeyedBidirectionalGraph::pool_size-1]);
        }
    }
}

void
SgIntKeyedBidirectionalGraph::deleteMemoryPool() {
    for (auto p: SgIntKeyedBidirectionalGraph::pools) {
        ROSE_FREE(p);
    }
    SgIntKeyedBidirectionalGraph::next_node = nullptr;
    SgIntKeyedBidirectionalGraph::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIntKeyedBidirectionalGraph::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIntKeyedBidirectionalGraph) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntKeyedBidirectionalGraph)};
    for (size_t blockIndex = SgIntKeyedBidirectionalGraph::pools.size(); (blockIndex * SgIntKeyedBidirectionalGraph::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIntKeyedBidirectionalGraph) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntKeyedBidirectionalGraph) = %" PRIuPTR " SgIntKeyedBidirectionalGraph::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIntKeyedBidirectionalGraph),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntKeyedBidirectionalGraph),SgIntKeyedBidirectionalGraph::pool_size);
        }
#endif

        SgIntKeyedBidirectionalGraph* pointer{static_cast<SgIntKeyedBidirectionalGraph*>(ROSE_MALLOC(SgIntKeyedBidirectionalGraph::pool_size * sizeof(SgIntKeyedBidirectionalGraph)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIntKeyedBidirectionalGraph::pool_size * sizeof(SgIntKeyedBidirectionalGraph));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIntKeyedBidirectionalGraph::pool_size * sizeof(SgIntKeyedBidirectionalGraph));
#endif
        SgIntKeyedBidirectionalGraph::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIntKeyedBidirectionalGraph::pool_size * sizeof(SgIntKeyedBidirectionalGraph), V_SgIntKeyedBidirectionalGraph));

        if (SgIntKeyedBidirectionalGraph::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIntKeyedBidirectionalGraph* blkptr{reinterpret_cast<SgIntKeyedBidirectionalGraph*>(SgIntKeyedBidirectionalGraph::pools[blockIndex-1])};
                blkptr[SgIntKeyedBidirectionalGraph::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIntKeyedBidirectionalGraph::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIntKeyedBidirectionalGraph::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIntKeyedBidirectionalGraph::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIntKeyedBidirectionalGraph::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIntKeyedBidirectionalGraph::pool_size - 1};
    SgIntKeyedBidirectionalGraph* testPointer{reinterpret_cast<SgIntKeyedBidirectionalGraph*>(SgIntKeyedBidirectionalGraph::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIntKeyedBidirectionalGraph::pool_size * (SgIntKeyedBidirectionalGraph::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIntKeyedBidirectionalGraphStroageClass
 * from its counterpart of SgIntKeyedBidirectionalGraph. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIntKeyedBidirectionalGraph::initializeStorageClassArray(SgIntKeyedBidirectionalGraphStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIntKeyedBidirectionalGraph::pools.begin(); block != SgIntKeyedBidirectionalGraph::pools.end(); ++block) {
        SgIntKeyedBidirectionalGraph* pointer{reinterpret_cast<SgIntKeyedBidirectionalGraph*>(*block)};
        for (unsigned i = 0; i < SgIntKeyedBidirectionalGraph::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 12083 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIncidenceUndirectedGraph that takes its corresponding StorageClass as parameter
 */
SgIncidenceUndirectedGraph::SgIncidenceUndirectedGraph(const SgIncidenceUndirectedGraphStorageClass& storageSource)  : SgGraph (storageSource) {

/* #line 12093 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIncidenceUndirectedGraph*
SgIncidenceUndirectedGraph::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIncidenceUndirectedGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncidenceUndirectedGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncidenceUndirectedGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncidenceUndirectedGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceUndirectedGraph)};
        unsigned long positionInPool{localIndex % SgIncidenceUndirectedGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncidenceUndirectedGraph::pool_size};
        returnPointer = &(((SgIncidenceUndirectedGraph*)(SgIncidenceUndirectedGraph::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIncidenceUndirectedGraph*
SgIncidenceUndirectedGraph::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIncidenceUndirectedGraph* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncidenceUndirectedGraph)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncidenceUndirectedGraph + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncidenceUndirectedGraph)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIncidenceUndirectedGraph)};
        unsigned long positionInPool{localIndex % SgIncidenceUndirectedGraph::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncidenceUndirectedGraph::pool_size};
        returnPointer = &(((SgIncidenceUndirectedGraph*)(SgIncidenceUndirectedGraph::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIncidenceUndirectedGraph::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIncidenceUndirectedGraph::pools.begin(); block != SgIncidenceUndirectedGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncidenceUndirectedGraph*>(*block)};
        for (unsigned i = 0; i < SgIncidenceUndirectedGraph::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIncidenceUndirectedGraph*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIncidenceUndirectedGraph::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIncidenceUndirectedGraph* pointerOfLinkedList{nullptr};
    for (auto block = SgIncidenceUndirectedGraph::pools.begin(); block != SgIncidenceUndirectedGraph::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncidenceUndirectedGraph*>(*block)};
        for (unsigned i = 0; i < SgIncidenceUndirectedGraph::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIncidenceUndirectedGraph::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIncidenceUndirectedGraph::clearMemoryPool() {
    SgIncidenceUndirectedGraph* tempPointer{nullptr};
    if (SgIncidenceUndirectedGraph::pools.empty() == false) {
        auto block{SgIncidenceUndirectedGraph::pools.begin()};
        SgIncidenceUndirectedGraph::next_node = reinterpret_cast<SgIncidenceUndirectedGraph*>(*block);
        for (; block != SgIncidenceUndirectedGraph::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIncidenceUndirectedGraph*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIncidenceUndirectedGraph::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIncidenceUndirectedGraph::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIncidenceUndirectedGraph::pool_size-1]);
        }
    }
}

void
SgIncidenceUndirectedGraph::deleteMemoryPool() {
    for (auto p: SgIncidenceUndirectedGraph::pools) {
        ROSE_FREE(p);
    }
    SgIncidenceUndirectedGraph::next_node = nullptr;
    SgIncidenceUndirectedGraph::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIncidenceUndirectedGraph::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceUndirectedGraph) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncidenceUndirectedGraph)};
    for (size_t blockIndex = SgIncidenceUndirectedGraph::pools.size(); (blockIndex * SgIncidenceUndirectedGraph::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceUndirectedGraph) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncidenceUndirectedGraph) = %" PRIuPTR " SgIncidenceUndirectedGraph::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIncidenceUndirectedGraph),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncidenceUndirectedGraph),SgIncidenceUndirectedGraph::pool_size);
        }
#endif

        SgIncidenceUndirectedGraph* pointer{static_cast<SgIncidenceUndirectedGraph*>(ROSE_MALLOC(SgIncidenceUndirectedGraph::pool_size * sizeof(SgIncidenceUndirectedGraph)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIncidenceUndirectedGraph::pool_size * sizeof(SgIncidenceUndirectedGraph));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIncidenceUndirectedGraph::pool_size * sizeof(SgIncidenceUndirectedGraph));
#endif
        SgIncidenceUndirectedGraph::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIncidenceUndirectedGraph::pool_size * sizeof(SgIncidenceUndirectedGraph), V_SgIncidenceUndirectedGraph));

        if (SgIncidenceUndirectedGraph::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIncidenceUndirectedGraph* blkptr{reinterpret_cast<SgIncidenceUndirectedGraph*>(SgIncidenceUndirectedGraph::pools[blockIndex-1])};
                blkptr[SgIncidenceUndirectedGraph::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIncidenceUndirectedGraph::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIncidenceUndirectedGraph::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIncidenceUndirectedGraph::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIncidenceUndirectedGraph::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIncidenceUndirectedGraph::pool_size - 1};
    SgIncidenceUndirectedGraph* testPointer{reinterpret_cast<SgIncidenceUndirectedGraph*>(SgIncidenceUndirectedGraph::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIncidenceUndirectedGraph::pool_size * (SgIncidenceUndirectedGraph::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIncidenceUndirectedGraphStroageClass
 * from its counterpart of SgIncidenceUndirectedGraph. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIncidenceUndirectedGraph::initializeStorageClassArray(SgIncidenceUndirectedGraphStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIncidenceUndirectedGraph::pools.begin(); block != SgIncidenceUndirectedGraph::pools.end(); ++block) {
        SgIncidenceUndirectedGraph* pointer{reinterpret_cast<SgIncidenceUndirectedGraph*>(*block)};
        for (unsigned i = 0; i < SgIncidenceUndirectedGraph::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 12310 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGraphNode that takes its corresponding StorageClass as parameter
 */
SgGraphNode::SgGraphNode(const SgGraphNodeStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 12320 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
     p_SgNode = (SgNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_SgNode));
    p_index = storageSource.storageOf_index;
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();
    p_properties = storageSource.storageOf_properties.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGraphNode*
SgGraphNode::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGraphNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphNode)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNode)};
        unsigned long positionInPool{localIndex % SgGraphNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphNode::pool_size};
        returnPointer = &(((SgGraphNode*)(SgGraphNode::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGraphNode*
SgGraphNode::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGraphNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphNode)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGraphNode)};
        unsigned long positionInPool{localIndex % SgGraphNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphNode::pool_size};
        returnPointer = &(((SgGraphNode*)(SgGraphNode::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGraphNode::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGraphNode::pools.begin(); block != SgGraphNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphNode*>(*block)};
        for (unsigned i = 0; i < SgGraphNode::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGraphNode*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGraphNode::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGraphNode* pointerOfLinkedList{nullptr};
    for (auto block = SgGraphNode::pools.begin(); block != SgGraphNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphNode*>(*block)};
        for (unsigned i = 0; i < SgGraphNode::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGraphNode::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGraphNode::clearMemoryPool() {
    SgGraphNode* tempPointer{nullptr};
    if (SgGraphNode::pools.empty() == false) {
        auto block{SgGraphNode::pools.begin()};
        SgGraphNode::next_node = reinterpret_cast<SgGraphNode*>(*block);
        for (; block != SgGraphNode::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGraphNode*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGraphNode::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGraphNode::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGraphNode::pool_size-1]);
        }
    }
}

void
SgGraphNode::deleteMemoryPool() {
    for (auto p: SgGraphNode::pools) {
        ROSE_FREE(p);
    }
    SgGraphNode::next_node = nullptr;
    SgGraphNode::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGraphNode::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNode) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphNode)};
    for (size_t blockIndex = SgGraphNode::pools.size(); (blockIndex * SgGraphNode::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNode) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphNode) = %" PRIuPTR " SgGraphNode::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNode),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphNode),SgGraphNode::pool_size);
        }
#endif

        SgGraphNode* pointer{static_cast<SgGraphNode*>(ROSE_MALLOC(SgGraphNode::pool_size * sizeof(SgGraphNode)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGraphNode::pool_size * sizeof(SgGraphNode));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGraphNode::pool_size * sizeof(SgGraphNode));
#endif
        SgGraphNode::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGraphNode::pool_size * sizeof(SgGraphNode), V_SgGraphNode));

        if (SgGraphNode::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGraphNode* blkptr{reinterpret_cast<SgGraphNode*>(SgGraphNode::pools[blockIndex-1])};
                blkptr[SgGraphNode::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGraphNode::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGraphNode::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGraphNode::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGraphNode::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGraphNode::pool_size - 1};
    SgGraphNode* testPointer{reinterpret_cast<SgGraphNode*>(SgGraphNode::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGraphNode::pool_size * (SgGraphNode::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGraphNodeStroageClass
 * from its counterpart of SgGraphNode. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGraphNode::initializeStorageClassArray(SgGraphNodeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGraphNode::pools.begin(); block != SgGraphNode::pools.end(); ++block) {
        SgGraphNode* pointer{reinterpret_cast<SgGraphNode*>(*block)};
        for (unsigned i = 0; i < SgGraphNode::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 12542 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGraphEdge that takes its corresponding StorageClass as parameter
 */
SgGraphEdge::SgGraphEdge(const SgGraphEdgeStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 12552 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_node_A = (SgGraphNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_node_A));
     p_node_B = (SgGraphNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_node_B));
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
    p_index = storageSource.storageOf_index;
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();
    p_properties = storageSource.storageOf_properties.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGraphEdge*
SgGraphEdge::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGraphEdge* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphEdge)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphEdge + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphEdge)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdge)};
        unsigned long positionInPool{localIndex % SgGraphEdge::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphEdge::pool_size};
        returnPointer = &(((SgGraphEdge*)(SgGraphEdge::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGraphEdge*
SgGraphEdge::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGraphEdge* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphEdge)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphEdge + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphEdge)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGraphEdge)};
        unsigned long positionInPool{localIndex % SgGraphEdge::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphEdge::pool_size};
        returnPointer = &(((SgGraphEdge*)(SgGraphEdge::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGraphEdge::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGraphEdge::pools.begin(); block != SgGraphEdge::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgGraphEdge::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGraphEdge*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGraphEdge::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGraphEdge* pointerOfLinkedList{nullptr};
    for (auto block = SgGraphEdge::pools.begin(); block != SgGraphEdge::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgGraphEdge::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGraphEdge::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGraphEdge::clearMemoryPool() {
    SgGraphEdge* tempPointer{nullptr};
    if (SgGraphEdge::pools.empty() == false) {
        auto block{SgGraphEdge::pools.begin()};
        SgGraphEdge::next_node = reinterpret_cast<SgGraphEdge*>(*block);
        for (; block != SgGraphEdge::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGraphEdge*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGraphEdge::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGraphEdge::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGraphEdge::pool_size-1]);
        }
    }
}

void
SgGraphEdge::deleteMemoryPool() {
    for (auto p: SgGraphEdge::pools) {
        ROSE_FREE(p);
    }
    SgGraphEdge::next_node = nullptr;
    SgGraphEdge::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGraphEdge::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdge) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphEdge)};
    for (size_t blockIndex = SgGraphEdge::pools.size(); (blockIndex * SgGraphEdge::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdge) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphEdge) = %" PRIuPTR " SgGraphEdge::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdge),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphEdge),SgGraphEdge::pool_size);
        }
#endif

        SgGraphEdge* pointer{static_cast<SgGraphEdge*>(ROSE_MALLOC(SgGraphEdge::pool_size * sizeof(SgGraphEdge)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGraphEdge::pool_size * sizeof(SgGraphEdge));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGraphEdge::pool_size * sizeof(SgGraphEdge));
#endif
        SgGraphEdge::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGraphEdge::pool_size * sizeof(SgGraphEdge), V_SgGraphEdge));

        if (SgGraphEdge::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGraphEdge* blkptr{reinterpret_cast<SgGraphEdge*>(SgGraphEdge::pools[blockIndex-1])};
                blkptr[SgGraphEdge::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGraphEdge::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGraphEdge::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGraphEdge::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGraphEdge::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGraphEdge::pool_size - 1};
    SgGraphEdge* testPointer{reinterpret_cast<SgGraphEdge*>(SgGraphEdge::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGraphEdge::pool_size * (SgGraphEdge::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGraphEdgeStroageClass
 * from its counterpart of SgGraphEdge. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGraphEdge::initializeStorageClassArray(SgGraphEdgeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGraphEdge::pools.begin(); block != SgGraphEdge::pools.end(); ++block) {
        SgGraphEdge* pointer{reinterpret_cast<SgGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgGraphEdge::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 12775 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDirectedGraphEdge that takes its corresponding StorageClass as parameter
 */
SgDirectedGraphEdge::SgDirectedGraphEdge(const SgDirectedGraphEdgeStorageClass& storageSource)  : SgGraphEdge (storageSource) {

/* #line 12785 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDirectedGraphEdge*
SgDirectedGraphEdge::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDirectedGraphEdge* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectedGraphEdge)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectedGraphEdge + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDirectedGraphEdge)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectedGraphEdge)};
        unsigned long positionInPool{localIndex % SgDirectedGraphEdge::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDirectedGraphEdge::pool_size};
        returnPointer = &(((SgDirectedGraphEdge*)(SgDirectedGraphEdge::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDirectedGraphEdge*
SgDirectedGraphEdge::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDirectedGraphEdge* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectedGraphEdge)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectedGraphEdge + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDirectedGraphEdge)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDirectedGraphEdge)};
        unsigned long positionInPool{localIndex % SgDirectedGraphEdge::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDirectedGraphEdge::pool_size};
        returnPointer = &(((SgDirectedGraphEdge*)(SgDirectedGraphEdge::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDirectedGraphEdge::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDirectedGraphEdge::pools.begin(); block != SgDirectedGraphEdge::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDirectedGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgDirectedGraphEdge::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDirectedGraphEdge*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDirectedGraphEdge::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDirectedGraphEdge* pointerOfLinkedList{nullptr};
    for (auto block = SgDirectedGraphEdge::pools.begin(); block != SgDirectedGraphEdge::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDirectedGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgDirectedGraphEdge::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDirectedGraphEdge::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDirectedGraphEdge::clearMemoryPool() {
    SgDirectedGraphEdge* tempPointer{nullptr};
    if (SgDirectedGraphEdge::pools.empty() == false) {
        auto block{SgDirectedGraphEdge::pools.begin()};
        SgDirectedGraphEdge::next_node = reinterpret_cast<SgDirectedGraphEdge*>(*block);
        for (; block != SgDirectedGraphEdge::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDirectedGraphEdge*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDirectedGraphEdge::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDirectedGraphEdge::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDirectedGraphEdge::pool_size-1]);
        }
    }
}

void
SgDirectedGraphEdge::deleteMemoryPool() {
    for (auto p: SgDirectedGraphEdge::pools) {
        ROSE_FREE(p);
    }
    SgDirectedGraphEdge::next_node = nullptr;
    SgDirectedGraphEdge::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDirectedGraphEdge::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectedGraphEdge) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectedGraphEdge)};
    for (size_t blockIndex = SgDirectedGraphEdge::pools.size(); (blockIndex * SgDirectedGraphEdge::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectedGraphEdge) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectedGraphEdge) = %" PRIuPTR " SgDirectedGraphEdge::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDirectedGraphEdge),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDirectedGraphEdge),SgDirectedGraphEdge::pool_size);
        }
#endif

        SgDirectedGraphEdge* pointer{static_cast<SgDirectedGraphEdge*>(ROSE_MALLOC(SgDirectedGraphEdge::pool_size * sizeof(SgDirectedGraphEdge)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDirectedGraphEdge::pool_size * sizeof(SgDirectedGraphEdge));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDirectedGraphEdge::pool_size * sizeof(SgDirectedGraphEdge));
#endif
        SgDirectedGraphEdge::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDirectedGraphEdge::pool_size * sizeof(SgDirectedGraphEdge), V_SgDirectedGraphEdge));

        if (SgDirectedGraphEdge::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDirectedGraphEdge* blkptr{reinterpret_cast<SgDirectedGraphEdge*>(SgDirectedGraphEdge::pools[blockIndex-1])};
                blkptr[SgDirectedGraphEdge::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDirectedGraphEdge::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDirectedGraphEdge::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDirectedGraphEdge::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDirectedGraphEdge::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDirectedGraphEdge::pool_size - 1};
    SgDirectedGraphEdge* testPointer{reinterpret_cast<SgDirectedGraphEdge*>(SgDirectedGraphEdge::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDirectedGraphEdge::pool_size * (SgDirectedGraphEdge::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDirectedGraphEdgeStroageClass
 * from its counterpart of SgDirectedGraphEdge. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDirectedGraphEdge::initializeStorageClassArray(SgDirectedGraphEdgeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDirectedGraphEdge::pools.begin(); block != SgDirectedGraphEdge::pools.end(); ++block) {
        SgDirectedGraphEdge* pointer{reinterpret_cast<SgDirectedGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgDirectedGraphEdge::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 13002 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUndirectedGraphEdge that takes its corresponding StorageClass as parameter
 */
SgUndirectedGraphEdge::SgUndirectedGraphEdge(const SgUndirectedGraphEdgeStorageClass& storageSource)  : SgGraphEdge (storageSource) {

/* #line 13012 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUndirectedGraphEdge*
SgUndirectedGraphEdge::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUndirectedGraphEdge* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUndirectedGraphEdge)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUndirectedGraphEdge + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUndirectedGraphEdge)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUndirectedGraphEdge)};
        unsigned long positionInPool{localIndex % SgUndirectedGraphEdge::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUndirectedGraphEdge::pool_size};
        returnPointer = &(((SgUndirectedGraphEdge*)(SgUndirectedGraphEdge::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUndirectedGraphEdge*
SgUndirectedGraphEdge::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUndirectedGraphEdge* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUndirectedGraphEdge)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUndirectedGraphEdge + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUndirectedGraphEdge)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUndirectedGraphEdge)};
        unsigned long positionInPool{localIndex % SgUndirectedGraphEdge::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUndirectedGraphEdge::pool_size};
        returnPointer = &(((SgUndirectedGraphEdge*)(SgUndirectedGraphEdge::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUndirectedGraphEdge::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUndirectedGraphEdge::pools.begin(); block != SgUndirectedGraphEdge::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUndirectedGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgUndirectedGraphEdge::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUndirectedGraphEdge*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUndirectedGraphEdge::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUndirectedGraphEdge* pointerOfLinkedList{nullptr};
    for (auto block = SgUndirectedGraphEdge::pools.begin(); block != SgUndirectedGraphEdge::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUndirectedGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgUndirectedGraphEdge::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUndirectedGraphEdge::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUndirectedGraphEdge::clearMemoryPool() {
    SgUndirectedGraphEdge* tempPointer{nullptr};
    if (SgUndirectedGraphEdge::pools.empty() == false) {
        auto block{SgUndirectedGraphEdge::pools.begin()};
        SgUndirectedGraphEdge::next_node = reinterpret_cast<SgUndirectedGraphEdge*>(*block);
        for (; block != SgUndirectedGraphEdge::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUndirectedGraphEdge*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUndirectedGraphEdge::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUndirectedGraphEdge::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUndirectedGraphEdge::pool_size-1]);
        }
    }
}

void
SgUndirectedGraphEdge::deleteMemoryPool() {
    for (auto p: SgUndirectedGraphEdge::pools) {
        ROSE_FREE(p);
    }
    SgUndirectedGraphEdge::next_node = nullptr;
    SgUndirectedGraphEdge::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUndirectedGraphEdge::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUndirectedGraphEdge) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUndirectedGraphEdge)};
    for (size_t blockIndex = SgUndirectedGraphEdge::pools.size(); (blockIndex * SgUndirectedGraphEdge::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUndirectedGraphEdge) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUndirectedGraphEdge) = %" PRIuPTR " SgUndirectedGraphEdge::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUndirectedGraphEdge),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUndirectedGraphEdge),SgUndirectedGraphEdge::pool_size);
        }
#endif

        SgUndirectedGraphEdge* pointer{static_cast<SgUndirectedGraphEdge*>(ROSE_MALLOC(SgUndirectedGraphEdge::pool_size * sizeof(SgUndirectedGraphEdge)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUndirectedGraphEdge::pool_size * sizeof(SgUndirectedGraphEdge));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUndirectedGraphEdge::pool_size * sizeof(SgUndirectedGraphEdge));
#endif
        SgUndirectedGraphEdge::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUndirectedGraphEdge::pool_size * sizeof(SgUndirectedGraphEdge), V_SgUndirectedGraphEdge));

        if (SgUndirectedGraphEdge::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUndirectedGraphEdge* blkptr{reinterpret_cast<SgUndirectedGraphEdge*>(SgUndirectedGraphEdge::pools[blockIndex-1])};
                blkptr[SgUndirectedGraphEdge::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUndirectedGraphEdge::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUndirectedGraphEdge::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUndirectedGraphEdge::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUndirectedGraphEdge::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUndirectedGraphEdge::pool_size - 1};
    SgUndirectedGraphEdge* testPointer{reinterpret_cast<SgUndirectedGraphEdge*>(SgUndirectedGraphEdge::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUndirectedGraphEdge::pool_size * (SgUndirectedGraphEdge::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUndirectedGraphEdgeStroageClass
 * from its counterpart of SgUndirectedGraphEdge. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUndirectedGraphEdge::initializeStorageClassArray(SgUndirectedGraphEdgeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUndirectedGraphEdge::pools.begin(); block != SgUndirectedGraphEdge::pools.end(); ++block) {
        SgUndirectedGraphEdge* pointer{reinterpret_cast<SgUndirectedGraphEdge*>(*block)};
        for (unsigned i = 0; i < SgUndirectedGraphEdge::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 13229 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGraphNodeList that takes its corresponding StorageClass as parameter
 */
SgGraphNodeList::SgGraphNodeList(const SgGraphNodeListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 13239 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGraphNodeList*
SgGraphNodeList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGraphNodeList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphNodeList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphNodeList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphNodeList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNodeList)};
        unsigned long positionInPool{localIndex % SgGraphNodeList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphNodeList::pool_size};
        returnPointer = &(((SgGraphNodeList*)(SgGraphNodeList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGraphNodeList*
SgGraphNodeList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGraphNodeList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphNodeList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphNodeList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphNodeList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGraphNodeList)};
        unsigned long positionInPool{localIndex % SgGraphNodeList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphNodeList::pool_size};
        returnPointer = &(((SgGraphNodeList*)(SgGraphNodeList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGraphNodeList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGraphNodeList::pools.begin(); block != SgGraphNodeList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphNodeList*>(*block)};
        for (unsigned i = 0; i < SgGraphNodeList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGraphNodeList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGraphNodeList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGraphNodeList* pointerOfLinkedList{nullptr};
    for (auto block = SgGraphNodeList::pools.begin(); block != SgGraphNodeList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphNodeList*>(*block)};
        for (unsigned i = 0; i < SgGraphNodeList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGraphNodeList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGraphNodeList::clearMemoryPool() {
    SgGraphNodeList* tempPointer{nullptr};
    if (SgGraphNodeList::pools.empty() == false) {
        auto block{SgGraphNodeList::pools.begin()};
        SgGraphNodeList::next_node = reinterpret_cast<SgGraphNodeList*>(*block);
        for (; block != SgGraphNodeList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGraphNodeList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGraphNodeList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGraphNodeList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGraphNodeList::pool_size-1]);
        }
    }
}

void
SgGraphNodeList::deleteMemoryPool() {
    for (auto p: SgGraphNodeList::pools) {
        ROSE_FREE(p);
    }
    SgGraphNodeList::next_node = nullptr;
    SgGraphNodeList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGraphNodeList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNodeList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphNodeList)};
    for (size_t blockIndex = SgGraphNodeList::pools.size(); (blockIndex * SgGraphNodeList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNodeList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphNodeList) = %" PRIuPTR " SgGraphNodeList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphNodeList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphNodeList),SgGraphNodeList::pool_size);
        }
#endif

        SgGraphNodeList* pointer{static_cast<SgGraphNodeList*>(ROSE_MALLOC(SgGraphNodeList::pool_size * sizeof(SgGraphNodeList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGraphNodeList::pool_size * sizeof(SgGraphNodeList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGraphNodeList::pool_size * sizeof(SgGraphNodeList));
#endif
        SgGraphNodeList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGraphNodeList::pool_size * sizeof(SgGraphNodeList), V_SgGraphNodeList));

        if (SgGraphNodeList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGraphNodeList* blkptr{reinterpret_cast<SgGraphNodeList*>(SgGraphNodeList::pools[blockIndex-1])};
                blkptr[SgGraphNodeList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGraphNodeList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGraphNodeList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGraphNodeList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGraphNodeList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGraphNodeList::pool_size - 1};
    SgGraphNodeList* testPointer{reinterpret_cast<SgGraphNodeList*>(SgGraphNodeList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGraphNodeList::pool_size * (SgGraphNodeList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGraphNodeListStroageClass
 * from its counterpart of SgGraphNodeList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGraphNodeList::initializeStorageClassArray(SgGraphNodeListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGraphNodeList::pools.begin(); block != SgGraphNodeList::pools.end(); ++block) {
        SgGraphNodeList* pointer{reinterpret_cast<SgGraphNodeList*>(*block)};
        for (unsigned i = 0; i < SgGraphNodeList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 13456 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGraphEdgeList that takes its corresponding StorageClass as parameter
 */
SgGraphEdgeList::SgGraphEdgeList(const SgGraphEdgeListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 13466 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGraphEdgeList*
SgGraphEdgeList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGraphEdgeList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphEdgeList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphEdgeList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGraphEdgeList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdgeList)};
        unsigned long positionInPool{localIndex % SgGraphEdgeList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphEdgeList::pool_size};
        returnPointer = &(((SgGraphEdgeList*)(SgGraphEdgeList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGraphEdgeList*
SgGraphEdgeList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGraphEdgeList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphEdgeList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphEdgeList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGraphEdgeList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGraphEdgeList)};
        unsigned long positionInPool{localIndex % SgGraphEdgeList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGraphEdgeList::pool_size};
        returnPointer = &(((SgGraphEdgeList*)(SgGraphEdgeList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGraphEdgeList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGraphEdgeList::pools.begin(); block != SgGraphEdgeList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphEdgeList*>(*block)};
        for (unsigned i = 0; i < SgGraphEdgeList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGraphEdgeList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGraphEdgeList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGraphEdgeList* pointerOfLinkedList{nullptr};
    for (auto block = SgGraphEdgeList::pools.begin(); block != SgGraphEdgeList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGraphEdgeList*>(*block)};
        for (unsigned i = 0; i < SgGraphEdgeList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGraphEdgeList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGraphEdgeList::clearMemoryPool() {
    SgGraphEdgeList* tempPointer{nullptr};
    if (SgGraphEdgeList::pools.empty() == false) {
        auto block{SgGraphEdgeList::pools.begin()};
        SgGraphEdgeList::next_node = reinterpret_cast<SgGraphEdgeList*>(*block);
        for (; block != SgGraphEdgeList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGraphEdgeList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGraphEdgeList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGraphEdgeList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGraphEdgeList::pool_size-1]);
        }
    }
}

void
SgGraphEdgeList::deleteMemoryPool() {
    for (auto p: SgGraphEdgeList::pools) {
        ROSE_FREE(p);
    }
    SgGraphEdgeList::next_node = nullptr;
    SgGraphEdgeList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGraphEdgeList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdgeList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphEdgeList)};
    for (size_t blockIndex = SgGraphEdgeList::pools.size(); (blockIndex * SgGraphEdgeList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdgeList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphEdgeList) = %" PRIuPTR " SgGraphEdgeList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGraphEdgeList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGraphEdgeList),SgGraphEdgeList::pool_size);
        }
#endif

        SgGraphEdgeList* pointer{static_cast<SgGraphEdgeList*>(ROSE_MALLOC(SgGraphEdgeList::pool_size * sizeof(SgGraphEdgeList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGraphEdgeList::pool_size * sizeof(SgGraphEdgeList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGraphEdgeList::pool_size * sizeof(SgGraphEdgeList));
#endif
        SgGraphEdgeList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGraphEdgeList::pool_size * sizeof(SgGraphEdgeList), V_SgGraphEdgeList));

        if (SgGraphEdgeList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGraphEdgeList* blkptr{reinterpret_cast<SgGraphEdgeList*>(SgGraphEdgeList::pools[blockIndex-1])};
                blkptr[SgGraphEdgeList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGraphEdgeList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGraphEdgeList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGraphEdgeList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGraphEdgeList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGraphEdgeList::pool_size - 1};
    SgGraphEdgeList* testPointer{reinterpret_cast<SgGraphEdgeList*>(SgGraphEdgeList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGraphEdgeList::pool_size * (SgGraphEdgeList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGraphEdgeListStroageClass
 * from its counterpart of SgGraphEdgeList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGraphEdgeList::initializeStorageClassArray(SgGraphEdgeListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGraphEdgeList::pools.begin(); block != SgGraphEdgeList::pools.end(); ++block) {
        SgGraphEdgeList* pointer{reinterpret_cast<SgGraphEdgeList*>(*block)};
        for (unsigned i = 0; i < SgGraphEdgeList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 13683 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeTable that takes its corresponding StorageClass as parameter
 */
SgTypeTable::SgTypeTable(const SgTypeTableStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 13693 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_type_table = (SgSymbolTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type_table));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeTable*
SgTypeTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTable)};
        unsigned long positionInPool{localIndex % SgTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeTable::pool_size};
        returnPointer = &(((SgTypeTable*)(SgTypeTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeTable*
SgTypeTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeTable)};
        unsigned long positionInPool{localIndex % SgTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeTable::pool_size};
        returnPointer = &(((SgTypeTable*)(SgTypeTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeTable::pools.begin(); block != SgTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeTable*>(*block)};
        for (unsigned i = 0; i < SgTypeTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeTable* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeTable::pools.begin(); block != SgTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeTable*>(*block)};
        for (unsigned i = 0; i < SgTypeTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeTable::clearMemoryPool() {
    SgTypeTable* tempPointer{nullptr};
    if (SgTypeTable::pools.empty() == false) {
        auto block{SgTypeTable::pools.begin()};
        SgTypeTable::next_node = reinterpret_cast<SgTypeTable*>(*block);
        for (; block != SgTypeTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeTable::pool_size-1]);
        }
    }
}

void
SgTypeTable::deleteMemoryPool() {
    for (auto p: SgTypeTable::pools) {
        ROSE_FREE(p);
    }
    SgTypeTable::next_node = nullptr;
    SgTypeTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTable)};
    for (size_t blockIndex = SgTypeTable::pools.size(); (blockIndex * SgTypeTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTable) = %" PRIuPTR " SgTypeTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTable),SgTypeTable::pool_size);
        }
#endif

        SgTypeTable* pointer{static_cast<SgTypeTable*>(ROSE_MALLOC(SgTypeTable::pool_size * sizeof(SgTypeTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeTable::pool_size * sizeof(SgTypeTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeTable::pool_size * sizeof(SgTypeTable));
#endif
        SgTypeTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeTable::pool_size * sizeof(SgTypeTable), V_SgTypeTable));

        if (SgTypeTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeTable* blkptr{reinterpret_cast<SgTypeTable*>(SgTypeTable::pools[blockIndex-1])};
                blkptr[SgTypeTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeTable::pool_size - 1};
    SgTypeTable* testPointer{reinterpret_cast<SgTypeTable*>(SgTypeTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeTable::pool_size * (SgTypeTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeTableStroageClass
 * from its counterpart of SgTypeTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeTable::initializeStorageClassArray(SgTypeTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeTable::pools.begin(); block != SgTypeTable::pools.end(); ++block) {
        SgTypeTable* pointer{reinterpret_cast<SgTypeTable*>(*block)};
        for (unsigned i = 0; i < SgTypeTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 13911 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNameGroup that takes its corresponding StorageClass as parameter
 */
SgNameGroup::SgNameGroup(const SgNameGroupStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 13921 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_group_name = storageSource.storageOf_group_name.rebuildDataStoredInEasyStorageClass();
    p_name_list = storageSource.storageOf_name_list.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNameGroup*
SgNameGroup::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNameGroup* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNameGroup)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNameGroup + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNameGroup)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNameGroup)};
        unsigned long positionInPool{localIndex % SgNameGroup::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNameGroup::pool_size};
        returnPointer = &(((SgNameGroup*)(SgNameGroup::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNameGroup*
SgNameGroup::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNameGroup* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNameGroup)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNameGroup + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNameGroup)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNameGroup)};
        unsigned long positionInPool{localIndex % SgNameGroup::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNameGroup::pool_size};
        returnPointer = &(((SgNameGroup*)(SgNameGroup::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNameGroup::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNameGroup::pools.begin(); block != SgNameGroup::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNameGroup*>(*block)};
        for (unsigned i = 0; i < SgNameGroup::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNameGroup*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNameGroup::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNameGroup* pointerOfLinkedList{nullptr};
    for (auto block = SgNameGroup::pools.begin(); block != SgNameGroup::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNameGroup*>(*block)};
        for (unsigned i = 0; i < SgNameGroup::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNameGroup::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNameGroup::clearMemoryPool() {
    SgNameGroup* tempPointer{nullptr};
    if (SgNameGroup::pools.empty() == false) {
        auto block{SgNameGroup::pools.begin()};
        SgNameGroup::next_node = reinterpret_cast<SgNameGroup*>(*block);
        for (; block != SgNameGroup::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNameGroup*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNameGroup::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNameGroup::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNameGroup::pool_size-1]);
        }
    }
}

void
SgNameGroup::deleteMemoryPool() {
    for (auto p: SgNameGroup::pools) {
        ROSE_FREE(p);
    }
    SgNameGroup::next_node = nullptr;
    SgNameGroup::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNameGroup::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNameGroup) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNameGroup)};
    for (size_t blockIndex = SgNameGroup::pools.size(); (blockIndex * SgNameGroup::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNameGroup) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNameGroup) = %" PRIuPTR " SgNameGroup::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNameGroup),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNameGroup),SgNameGroup::pool_size);
        }
#endif

        SgNameGroup* pointer{static_cast<SgNameGroup*>(ROSE_MALLOC(SgNameGroup::pool_size * sizeof(SgNameGroup)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNameGroup::pool_size * sizeof(SgNameGroup));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNameGroup::pool_size * sizeof(SgNameGroup));
#endif
        SgNameGroup::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNameGroup::pool_size * sizeof(SgNameGroup), V_SgNameGroup));

        if (SgNameGroup::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNameGroup* blkptr{reinterpret_cast<SgNameGroup*>(SgNameGroup::pools[blockIndex-1])};
                blkptr[SgNameGroup::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNameGroup::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNameGroup::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNameGroup::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNameGroup::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNameGroup::pool_size - 1};
    SgNameGroup* testPointer{reinterpret_cast<SgNameGroup*>(SgNameGroup::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNameGroup::pool_size * (SgNameGroup::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNameGroupStroageClass
 * from its counterpart of SgNameGroup. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNameGroup::initializeStorageClassArray(SgNameGroupStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNameGroup::pools.begin(); block != SgNameGroup::pools.end(); ++block) {
        SgNameGroup* pointer{reinterpret_cast<SgNameGroup*>(*block)};
        for (unsigned i = 0; i < SgNameGroup::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 14140 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDimensionObject that takes its corresponding StorageClass as parameter
 */
SgDimensionObject::SgDimensionObject(const SgDimensionObjectStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 14150 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_array = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_array));
     p_shape = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_shape));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDimensionObject*
SgDimensionObject::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDimensionObject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDimensionObject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDimensionObject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDimensionObject)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDimensionObject)};
        unsigned long positionInPool{localIndex % SgDimensionObject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDimensionObject::pool_size};
        returnPointer = &(((SgDimensionObject*)(SgDimensionObject::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDimensionObject*
SgDimensionObject::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDimensionObject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDimensionObject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDimensionObject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDimensionObject)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDimensionObject)};
        unsigned long positionInPool{localIndex % SgDimensionObject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDimensionObject::pool_size};
        returnPointer = &(((SgDimensionObject*)(SgDimensionObject::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDimensionObject::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDimensionObject::pools.begin(); block != SgDimensionObject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDimensionObject*>(*block)};
        for (unsigned i = 0; i < SgDimensionObject::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDimensionObject*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDimensionObject::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDimensionObject* pointerOfLinkedList{nullptr};
    for (auto block = SgDimensionObject::pools.begin(); block != SgDimensionObject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDimensionObject*>(*block)};
        for (unsigned i = 0; i < SgDimensionObject::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDimensionObject::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDimensionObject::clearMemoryPool() {
    SgDimensionObject* tempPointer{nullptr};
    if (SgDimensionObject::pools.empty() == false) {
        auto block{SgDimensionObject::pools.begin()};
        SgDimensionObject::next_node = reinterpret_cast<SgDimensionObject*>(*block);
        for (; block != SgDimensionObject::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDimensionObject*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDimensionObject::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDimensionObject::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDimensionObject::pool_size-1]);
        }
    }
}

void
SgDimensionObject::deleteMemoryPool() {
    for (auto p: SgDimensionObject::pools) {
        ROSE_FREE(p);
    }
    SgDimensionObject::next_node = nullptr;
    SgDimensionObject::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDimensionObject::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDimensionObject) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDimensionObject)};
    for (size_t blockIndex = SgDimensionObject::pools.size(); (blockIndex * SgDimensionObject::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDimensionObject) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDimensionObject) = %" PRIuPTR " SgDimensionObject::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDimensionObject),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDimensionObject),SgDimensionObject::pool_size);
        }
#endif

        SgDimensionObject* pointer{static_cast<SgDimensionObject*>(ROSE_MALLOC(SgDimensionObject::pool_size * sizeof(SgDimensionObject)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDimensionObject::pool_size * sizeof(SgDimensionObject));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDimensionObject::pool_size * sizeof(SgDimensionObject));
#endif
        SgDimensionObject::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDimensionObject::pool_size * sizeof(SgDimensionObject), V_SgDimensionObject));

        if (SgDimensionObject::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDimensionObject* blkptr{reinterpret_cast<SgDimensionObject*>(SgDimensionObject::pools[blockIndex-1])};
                blkptr[SgDimensionObject::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDimensionObject::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDimensionObject::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDimensionObject::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDimensionObject::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDimensionObject::pool_size - 1};
    SgDimensionObject* testPointer{reinterpret_cast<SgDimensionObject*>(SgDimensionObject::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDimensionObject::pool_size * (SgDimensionObject::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDimensionObjectStroageClass
 * from its counterpart of SgDimensionObject. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDimensionObject::initializeStorageClassArray(SgDimensionObjectStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDimensionObject::pools.begin(); block != SgDimensionObject::pools.end(); ++block) {
        SgDimensionObject* pointer{reinterpret_cast<SgDimensionObject*>(*block)};
        for (unsigned i = 0; i < SgDimensionObject::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 14369 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFormatItem that takes its corresponding StorageClass as parameter
 */
SgFormatItem::SgFormatItem(const SgFormatItemStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 14379 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_repeat_specification = storageSource.storageOf_repeat_specification;
     p_data = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_data));
     p_format_item_list = (SgFormatItemList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_format_item_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFormatItem*
SgFormatItem::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFormatItem* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatItem)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatItem + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatItem)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItem)};
        unsigned long positionInPool{localIndex % SgFormatItem::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFormatItem::pool_size};
        returnPointer = &(((SgFormatItem*)(SgFormatItem::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFormatItem*
SgFormatItem::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFormatItem* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatItem)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatItem + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatItem)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFormatItem)};
        unsigned long positionInPool{localIndex % SgFormatItem::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFormatItem::pool_size};
        returnPointer = &(((SgFormatItem*)(SgFormatItem::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFormatItem::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFormatItem::pools.begin(); block != SgFormatItem::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFormatItem*>(*block)};
        for (unsigned i = 0; i < SgFormatItem::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFormatItem*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFormatItem::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFormatItem* pointerOfLinkedList{nullptr};
    for (auto block = SgFormatItem::pools.begin(); block != SgFormatItem::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFormatItem*>(*block)};
        for (unsigned i = 0; i < SgFormatItem::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFormatItem::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFormatItem::clearMemoryPool() {
    SgFormatItem* tempPointer{nullptr};
    if (SgFormatItem::pools.empty() == false) {
        auto block{SgFormatItem::pools.begin()};
        SgFormatItem::next_node = reinterpret_cast<SgFormatItem*>(*block);
        for (; block != SgFormatItem::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFormatItem*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFormatItem::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFormatItem::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFormatItem::pool_size-1]);
        }
    }
}

void
SgFormatItem::deleteMemoryPool() {
    for (auto p: SgFormatItem::pools) {
        ROSE_FREE(p);
    }
    SgFormatItem::next_node = nullptr;
    SgFormatItem::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFormatItem::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItem) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatItem)};
    for (size_t blockIndex = SgFormatItem::pools.size(); (blockIndex * SgFormatItem::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItem) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatItem) = %" PRIuPTR " SgFormatItem::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItem),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatItem),SgFormatItem::pool_size);
        }
#endif

        SgFormatItem* pointer{static_cast<SgFormatItem*>(ROSE_MALLOC(SgFormatItem::pool_size * sizeof(SgFormatItem)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFormatItem::pool_size * sizeof(SgFormatItem));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFormatItem::pool_size * sizeof(SgFormatItem));
#endif
        SgFormatItem::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFormatItem::pool_size * sizeof(SgFormatItem), V_SgFormatItem));

        if (SgFormatItem::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFormatItem* blkptr{reinterpret_cast<SgFormatItem*>(SgFormatItem::pools[blockIndex-1])};
                blkptr[SgFormatItem::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFormatItem::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFormatItem::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFormatItem::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFormatItem::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFormatItem::pool_size - 1};
    SgFormatItem* testPointer{reinterpret_cast<SgFormatItem*>(SgFormatItem::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFormatItem::pool_size * (SgFormatItem::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFormatItemStroageClass
 * from its counterpart of SgFormatItem. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFormatItem::initializeStorageClassArray(SgFormatItemStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFormatItem::pools.begin(); block != SgFormatItem::pools.end(); ++block) {
        SgFormatItem* pointer{reinterpret_cast<SgFormatItem*>(*block)};
        for (unsigned i = 0; i < SgFormatItem::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 14599 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFormatItemList that takes its corresponding StorageClass as parameter
 */
SgFormatItemList::SgFormatItemList(const SgFormatItemListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 14609 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_format_item_list = storageSource.storageOf_format_item_list.rebuildDataStoredInEasyStorageClass();
    SgFormatItemPtrList::iterator i_format_item_list = p_format_item_list.begin();
    for ( ; i_format_item_list != p_format_item_list.end(); ++i_format_item_list) {
        (*i_format_item_list) = (SgFormatItemPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_format_item_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFormatItemList*
SgFormatItemList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFormatItemList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatItemList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatItemList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatItemList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItemList)};
        unsigned long positionInPool{localIndex % SgFormatItemList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFormatItemList::pool_size};
        returnPointer = &(((SgFormatItemList*)(SgFormatItemList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFormatItemList*
SgFormatItemList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFormatItemList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatItemList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatItemList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatItemList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFormatItemList)};
        unsigned long positionInPool{localIndex % SgFormatItemList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFormatItemList::pool_size};
        returnPointer = &(((SgFormatItemList*)(SgFormatItemList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFormatItemList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFormatItemList::pools.begin(); block != SgFormatItemList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFormatItemList*>(*block)};
        for (unsigned i = 0; i < SgFormatItemList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFormatItemList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFormatItemList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFormatItemList* pointerOfLinkedList{nullptr};
    for (auto block = SgFormatItemList::pools.begin(); block != SgFormatItemList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFormatItemList*>(*block)};
        for (unsigned i = 0; i < SgFormatItemList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFormatItemList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFormatItemList::clearMemoryPool() {
    SgFormatItemList* tempPointer{nullptr};
    if (SgFormatItemList::pools.empty() == false) {
        auto block{SgFormatItemList::pools.begin()};
        SgFormatItemList::next_node = reinterpret_cast<SgFormatItemList*>(*block);
        for (; block != SgFormatItemList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFormatItemList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFormatItemList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFormatItemList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFormatItemList::pool_size-1]);
        }
    }
}

void
SgFormatItemList::deleteMemoryPool() {
    for (auto p: SgFormatItemList::pools) {
        ROSE_FREE(p);
    }
    SgFormatItemList::next_node = nullptr;
    SgFormatItemList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFormatItemList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItemList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatItemList)};
    for (size_t blockIndex = SgFormatItemList::pools.size(); (blockIndex * SgFormatItemList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItemList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatItemList) = %" PRIuPTR " SgFormatItemList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatItemList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatItemList),SgFormatItemList::pool_size);
        }
#endif

        SgFormatItemList* pointer{static_cast<SgFormatItemList*>(ROSE_MALLOC(SgFormatItemList::pool_size * sizeof(SgFormatItemList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFormatItemList::pool_size * sizeof(SgFormatItemList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFormatItemList::pool_size * sizeof(SgFormatItemList));
#endif
        SgFormatItemList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFormatItemList::pool_size * sizeof(SgFormatItemList), V_SgFormatItemList));

        if (SgFormatItemList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFormatItemList* blkptr{reinterpret_cast<SgFormatItemList*>(SgFormatItemList::pools[blockIndex-1])};
                blkptr[SgFormatItemList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFormatItemList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFormatItemList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFormatItemList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFormatItemList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFormatItemList::pool_size - 1};
    SgFormatItemList* testPointer{reinterpret_cast<SgFormatItemList*>(SgFormatItemList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFormatItemList::pool_size * (SgFormatItemList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFormatItemListStroageClass
 * from its counterpart of SgFormatItemList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFormatItemList::initializeStorageClassArray(SgFormatItemListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFormatItemList::pools.begin(); block != SgFormatItemList::pools.end(); ++block) {
        SgFormatItemList* pointer{reinterpret_cast<SgFormatItemList*>(*block)};
        for (unsigned i = 0; i < SgFormatItemList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 14831 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDataStatementGroup that takes its corresponding StorageClass as parameter
 */
SgDataStatementGroup::SgDataStatementGroup(const SgDataStatementGroupStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 14841 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_object_list = storageSource.storageOf_object_list.rebuildDataStoredInEasyStorageClass();
    SgDataStatementObjectPtrList::iterator i_object_list = p_object_list.begin();
    for ( ; i_object_list != p_object_list.end(); ++i_object_list) {
        (*i_object_list) = (SgDataStatementObjectPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_object_list)) );
    }
    p_value_list = storageSource.storageOf_value_list.rebuildDataStoredInEasyStorageClass();
    SgDataStatementValuePtrList::iterator i_value_list = p_value_list.begin();
    for ( ; i_value_list != p_value_list.end(); ++i_value_list) {
        (*i_value_list) = (SgDataStatementValuePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_value_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDataStatementGroup*
SgDataStatementGroup::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDataStatementGroup* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementGroup)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementGroup + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementGroup)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementGroup)};
        unsigned long positionInPool{localIndex % SgDataStatementGroup::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDataStatementGroup::pool_size};
        returnPointer = &(((SgDataStatementGroup*)(SgDataStatementGroup::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDataStatementGroup*
SgDataStatementGroup::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDataStatementGroup* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementGroup)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementGroup + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementGroup)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDataStatementGroup)};
        unsigned long positionInPool{localIndex % SgDataStatementGroup::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDataStatementGroup::pool_size};
        returnPointer = &(((SgDataStatementGroup*)(SgDataStatementGroup::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDataStatementGroup::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDataStatementGroup::pools.begin(); block != SgDataStatementGroup::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDataStatementGroup*>(*block)};
        for (unsigned i = 0; i < SgDataStatementGroup::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDataStatementGroup*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDataStatementGroup::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDataStatementGroup* pointerOfLinkedList{nullptr};
    for (auto block = SgDataStatementGroup::pools.begin(); block != SgDataStatementGroup::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDataStatementGroup*>(*block)};
        for (unsigned i = 0; i < SgDataStatementGroup::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDataStatementGroup::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDataStatementGroup::clearMemoryPool() {
    SgDataStatementGroup* tempPointer{nullptr};
    if (SgDataStatementGroup::pools.empty() == false) {
        auto block{SgDataStatementGroup::pools.begin()};
        SgDataStatementGroup::next_node = reinterpret_cast<SgDataStatementGroup*>(*block);
        for (; block != SgDataStatementGroup::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDataStatementGroup*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDataStatementGroup::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDataStatementGroup::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDataStatementGroup::pool_size-1]);
        }
    }
}

void
SgDataStatementGroup::deleteMemoryPool() {
    for (auto p: SgDataStatementGroup::pools) {
        ROSE_FREE(p);
    }
    SgDataStatementGroup::next_node = nullptr;
    SgDataStatementGroup::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDataStatementGroup::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementGroup) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementGroup)};
    for (size_t blockIndex = SgDataStatementGroup::pools.size(); (blockIndex * SgDataStatementGroup::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementGroup) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementGroup) = %" PRIuPTR " SgDataStatementGroup::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementGroup),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementGroup),SgDataStatementGroup::pool_size);
        }
#endif

        SgDataStatementGroup* pointer{static_cast<SgDataStatementGroup*>(ROSE_MALLOC(SgDataStatementGroup::pool_size * sizeof(SgDataStatementGroup)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDataStatementGroup::pool_size * sizeof(SgDataStatementGroup));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDataStatementGroup::pool_size * sizeof(SgDataStatementGroup));
#endif
        SgDataStatementGroup::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDataStatementGroup::pool_size * sizeof(SgDataStatementGroup), V_SgDataStatementGroup));

        if (SgDataStatementGroup::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDataStatementGroup* blkptr{reinterpret_cast<SgDataStatementGroup*>(SgDataStatementGroup::pools[blockIndex-1])};
                blkptr[SgDataStatementGroup::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDataStatementGroup::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDataStatementGroup::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDataStatementGroup::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDataStatementGroup::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDataStatementGroup::pool_size - 1};
    SgDataStatementGroup* testPointer{reinterpret_cast<SgDataStatementGroup*>(SgDataStatementGroup::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDataStatementGroup::pool_size * (SgDataStatementGroup::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDataStatementGroupStroageClass
 * from its counterpart of SgDataStatementGroup. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDataStatementGroup::initializeStorageClassArray(SgDataStatementGroupStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDataStatementGroup::pools.begin(); block != SgDataStatementGroup::pools.end(); ++block) {
        SgDataStatementGroup* pointer{reinterpret_cast<SgDataStatementGroup*>(*block)};
        for (unsigned i = 0; i < SgDataStatementGroup::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 15068 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDataStatementObject that takes its corresponding StorageClass as parameter
 */
SgDataStatementObject::SgDataStatementObject(const SgDataStatementObjectStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 15078 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_variableReference_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_variableReference_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDataStatementObject*
SgDataStatementObject::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDataStatementObject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementObject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementObject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementObject)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementObject)};
        unsigned long positionInPool{localIndex % SgDataStatementObject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDataStatementObject::pool_size};
        returnPointer = &(((SgDataStatementObject*)(SgDataStatementObject::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDataStatementObject*
SgDataStatementObject::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDataStatementObject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementObject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementObject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementObject)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDataStatementObject)};
        unsigned long positionInPool{localIndex % SgDataStatementObject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDataStatementObject::pool_size};
        returnPointer = &(((SgDataStatementObject*)(SgDataStatementObject::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDataStatementObject::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDataStatementObject::pools.begin(); block != SgDataStatementObject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDataStatementObject*>(*block)};
        for (unsigned i = 0; i < SgDataStatementObject::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDataStatementObject*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDataStatementObject::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDataStatementObject* pointerOfLinkedList{nullptr};
    for (auto block = SgDataStatementObject::pools.begin(); block != SgDataStatementObject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDataStatementObject*>(*block)};
        for (unsigned i = 0; i < SgDataStatementObject::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDataStatementObject::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDataStatementObject::clearMemoryPool() {
    SgDataStatementObject* tempPointer{nullptr};
    if (SgDataStatementObject::pools.empty() == false) {
        auto block{SgDataStatementObject::pools.begin()};
        SgDataStatementObject::next_node = reinterpret_cast<SgDataStatementObject*>(*block);
        for (; block != SgDataStatementObject::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDataStatementObject*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDataStatementObject::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDataStatementObject::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDataStatementObject::pool_size-1]);
        }
    }
}

void
SgDataStatementObject::deleteMemoryPool() {
    for (auto p: SgDataStatementObject::pools) {
        ROSE_FREE(p);
    }
    SgDataStatementObject::next_node = nullptr;
    SgDataStatementObject::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDataStatementObject::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementObject) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementObject)};
    for (size_t blockIndex = SgDataStatementObject::pools.size(); (blockIndex * SgDataStatementObject::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementObject) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementObject) = %" PRIuPTR " SgDataStatementObject::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementObject),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementObject),SgDataStatementObject::pool_size);
        }
#endif

        SgDataStatementObject* pointer{static_cast<SgDataStatementObject*>(ROSE_MALLOC(SgDataStatementObject::pool_size * sizeof(SgDataStatementObject)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDataStatementObject::pool_size * sizeof(SgDataStatementObject));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDataStatementObject::pool_size * sizeof(SgDataStatementObject));
#endif
        SgDataStatementObject::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDataStatementObject::pool_size * sizeof(SgDataStatementObject), V_SgDataStatementObject));

        if (SgDataStatementObject::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDataStatementObject* blkptr{reinterpret_cast<SgDataStatementObject*>(SgDataStatementObject::pools[blockIndex-1])};
                blkptr[SgDataStatementObject::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDataStatementObject::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDataStatementObject::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDataStatementObject::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDataStatementObject::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDataStatementObject::pool_size - 1};
    SgDataStatementObject* testPointer{reinterpret_cast<SgDataStatementObject*>(SgDataStatementObject::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDataStatementObject::pool_size * (SgDataStatementObject::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDataStatementObjectStroageClass
 * from its counterpart of SgDataStatementObject. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDataStatementObject::initializeStorageClassArray(SgDataStatementObjectStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDataStatementObject::pools.begin(); block != SgDataStatementObject::pools.end(); ++block) {
        SgDataStatementObject* pointer{reinterpret_cast<SgDataStatementObject*>(*block)};
        for (unsigned i = 0; i < SgDataStatementObject::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 15296 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIncludeFile that takes its corresponding StorageClass as parameter
 */
SgIncludeFile::SgIncludeFile(const SgIncludeFileStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 15306 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_filename = SgName ( storageSource.storageOf_filename);
     p_source_file = (SgSourceFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_source_file));
    p_include_file_list = storageSource.storageOf_include_file_list.rebuildDataStoredInEasyStorageClass();
    SgIncludeFilePtrList::iterator i_include_file_list = p_include_file_list.begin();
    for ( ; i_include_file_list != p_include_file_list.end(); ++i_include_file_list) {
        (*i_include_file_list) = (SgIncludeFilePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_include_file_list)) );
    }
    p_first_source_sequence_number = storageSource.storageOf_first_source_sequence_number;
    p_last_source_sequence_number = storageSource.storageOf_last_source_sequence_number;
    p_isIncludedMoreThanOnce = storageSource.storageOf_isIncludedMoreThanOnce;
    p_isPrimaryUse = storageSource.storageOf_isPrimaryUse;
    p_file_hash = storageSource.storageOf_file_hash.rebuildDataStoredInEasyStorageClass();
    p_name_used_in_include_directive = SgName ( storageSource.storageOf_name_used_in_include_directive);
     p_source_file_of_translation_unit = (SgSourceFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_source_file_of_translation_unit));
     p_including_source_file = (SgSourceFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_including_source_file));
     p_parent_include_file = (SgIncludeFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parent_include_file));
    p_isSystemInclude = storageSource.storageOf_isSystemInclude;
    p_isPreinclude = storageSource.storageOf_isPreinclude;
    p_requires_explict_path_for_unparsed_headers = storageSource.storageOf_requires_explict_path_for_unparsed_headers;
    p_can_be_supported_using_token_based_unparsing = storageSource.storageOf_can_be_supported_using_token_based_unparsing;
    p_directory_prefix = SgName ( storageSource.storageOf_directory_prefix);
    p_name_without_path = SgName ( storageSource.storageOf_name_without_path);
    p_applicationRootDirectory = SgName ( storageSource.storageOf_applicationRootDirectory);
    p_will_be_unparsed = storageSource.storageOf_will_be_unparsed;
    p_isRoseSystemInclude = storageSource.storageOf_isRoseSystemInclude;
    p_from_system_include_dir = storageSource.storageOf_from_system_include_dir;
    p_preinclude_macros_only = storageSource.storageOf_preinclude_macros_only;
    p_isApplicationFile = storageSource.storageOf_isApplicationFile;
    p_isRootSourceFile = storageSource.storageOf_isRootSourceFile;
     p_firstStatement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_firstStatement));
     p_lastStatement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lastStatement));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIncludeFile*
SgIncludeFile::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIncludeFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeFile)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeFile)};
        unsigned long positionInPool{localIndex % SgIncludeFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncludeFile::pool_size};
        returnPointer = &(((SgIncludeFile*)(SgIncludeFile::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIncludeFile*
SgIncludeFile::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIncludeFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeFile)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIncludeFile)};
        unsigned long positionInPool{localIndex % SgIncludeFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncludeFile::pool_size};
        returnPointer = &(((SgIncludeFile*)(SgIncludeFile::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIncludeFile::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIncludeFile::pools.begin(); block != SgIncludeFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncludeFile*>(*block)};
        for (unsigned i = 0; i < SgIncludeFile::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIncludeFile*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIncludeFile::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIncludeFile* pointerOfLinkedList{nullptr};
    for (auto block = SgIncludeFile::pools.begin(); block != SgIncludeFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncludeFile*>(*block)};
        for (unsigned i = 0; i < SgIncludeFile::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIncludeFile::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIncludeFile::clearMemoryPool() {
    SgIncludeFile* tempPointer{nullptr};
    if (SgIncludeFile::pools.empty() == false) {
        auto block{SgIncludeFile::pools.begin()};
        SgIncludeFile::next_node = reinterpret_cast<SgIncludeFile*>(*block);
        for (; block != SgIncludeFile::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIncludeFile*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIncludeFile::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIncludeFile::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIncludeFile::pool_size-1]);
        }
    }
}

void
SgIncludeFile::deleteMemoryPool() {
    for (auto p: SgIncludeFile::pools) {
        ROSE_FREE(p);
    }
    SgIncludeFile::next_node = nullptr;
    SgIncludeFile::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIncludeFile::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeFile) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeFile)};
    for (size_t blockIndex = SgIncludeFile::pools.size(); (blockIndex * SgIncludeFile::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeFile) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeFile) = %" PRIuPTR " SgIncludeFile::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeFile),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeFile),SgIncludeFile::pool_size);
        }
#endif

        SgIncludeFile* pointer{static_cast<SgIncludeFile*>(ROSE_MALLOC(SgIncludeFile::pool_size * sizeof(SgIncludeFile)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIncludeFile::pool_size * sizeof(SgIncludeFile));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIncludeFile::pool_size * sizeof(SgIncludeFile));
#endif
        SgIncludeFile::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIncludeFile::pool_size * sizeof(SgIncludeFile), V_SgIncludeFile));

        if (SgIncludeFile::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIncludeFile* blkptr{reinterpret_cast<SgIncludeFile*>(SgIncludeFile::pools[blockIndex-1])};
                blkptr[SgIncludeFile::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIncludeFile::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIncludeFile::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIncludeFile::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIncludeFile::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIncludeFile::pool_size - 1};
    SgIncludeFile* testPointer{reinterpret_cast<SgIncludeFile*>(SgIncludeFile::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIncludeFile::pool_size * (SgIncludeFile::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIncludeFileStroageClass
 * from its counterpart of SgIncludeFile. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIncludeFile::initializeStorageClassArray(SgIncludeFileStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIncludeFile::pools.begin(); block != SgIncludeFile::pools.end(); ++block) {
        SgIncludeFile* pointer{reinterpret_cast<SgIncludeFile*>(*block)};
        for (unsigned i = 0; i < SgIncludeFile::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 15554 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDataStatementValue that takes its corresponding StorageClass as parameter
 */
SgDataStatementValue::SgDataStatementValue(const SgDataStatementValueStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 15564 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_data_initialization_format = storageSource.storageOf_data_initialization_format;
     p_initializer_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initializer_list));
     p_repeat_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_repeat_expression));
     p_constant_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_constant_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDataStatementValue*
SgDataStatementValue::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDataStatementValue* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementValue)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementValue + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDataStatementValue)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementValue)};
        unsigned long positionInPool{localIndex % SgDataStatementValue::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDataStatementValue::pool_size};
        returnPointer = &(((SgDataStatementValue*)(SgDataStatementValue::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDataStatementValue*
SgDataStatementValue::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDataStatementValue* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementValue)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementValue + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDataStatementValue)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDataStatementValue)};
        unsigned long positionInPool{localIndex % SgDataStatementValue::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDataStatementValue::pool_size};
        returnPointer = &(((SgDataStatementValue*)(SgDataStatementValue::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDataStatementValue::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDataStatementValue::pools.begin(); block != SgDataStatementValue::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDataStatementValue*>(*block)};
        for (unsigned i = 0; i < SgDataStatementValue::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDataStatementValue*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDataStatementValue::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDataStatementValue* pointerOfLinkedList{nullptr};
    for (auto block = SgDataStatementValue::pools.begin(); block != SgDataStatementValue::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDataStatementValue*>(*block)};
        for (unsigned i = 0; i < SgDataStatementValue::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDataStatementValue::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDataStatementValue::clearMemoryPool() {
    SgDataStatementValue* tempPointer{nullptr};
    if (SgDataStatementValue::pools.empty() == false) {
        auto block{SgDataStatementValue::pools.begin()};
        SgDataStatementValue::next_node = reinterpret_cast<SgDataStatementValue*>(*block);
        for (; block != SgDataStatementValue::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDataStatementValue*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDataStatementValue::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDataStatementValue::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDataStatementValue::pool_size-1]);
        }
    }
}

void
SgDataStatementValue::deleteMemoryPool() {
    for (auto p: SgDataStatementValue::pools) {
        ROSE_FREE(p);
    }
    SgDataStatementValue::next_node = nullptr;
    SgDataStatementValue::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDataStatementValue::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementValue) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementValue)};
    for (size_t blockIndex = SgDataStatementValue::pools.size(); (blockIndex * SgDataStatementValue::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementValue) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementValue) = %" PRIuPTR " SgDataStatementValue::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDataStatementValue),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDataStatementValue),SgDataStatementValue::pool_size);
        }
#endif

        SgDataStatementValue* pointer{static_cast<SgDataStatementValue*>(ROSE_MALLOC(SgDataStatementValue::pool_size * sizeof(SgDataStatementValue)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDataStatementValue::pool_size * sizeof(SgDataStatementValue));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDataStatementValue::pool_size * sizeof(SgDataStatementValue));
#endif
        SgDataStatementValue::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDataStatementValue::pool_size * sizeof(SgDataStatementValue), V_SgDataStatementValue));

        if (SgDataStatementValue::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDataStatementValue* blkptr{reinterpret_cast<SgDataStatementValue*>(SgDataStatementValue::pools[blockIndex-1])};
                blkptr[SgDataStatementValue::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDataStatementValue::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDataStatementValue::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDataStatementValue::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDataStatementValue::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDataStatementValue::pool_size - 1};
    SgDataStatementValue* testPointer{reinterpret_cast<SgDataStatementValue*>(SgDataStatementValue::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDataStatementValue::pool_size * (SgDataStatementValue::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDataStatementValueStroageClass
 * from its counterpart of SgDataStatementValue. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDataStatementValue::initializeStorageClassArray(SgDataStatementValueStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDataStatementValue::pools.begin(); block != SgDataStatementValue::pools.end(); ++block) {
        SgDataStatementValue* pointer{reinterpret_cast<SgDataStatementValue*>(*block)};
        for (unsigned i = 0; i < SgDataStatementValue::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 15785 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaImportStatementList that takes its corresponding StorageClass as parameter
 */
SgJavaImportStatementList::SgJavaImportStatementList(const SgJavaImportStatementListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 15795 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_java_import_list = storageSource.storageOf_java_import_list.rebuildDataStoredInEasyStorageClass();
    SgJavaImportStatementPtrList::iterator i_java_import_list = p_java_import_list.begin();
    for ( ; i_java_import_list != p_java_import_list.end(); ++i_java_import_list) {
        (*i_java_import_list) = (SgJavaImportStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_java_import_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaImportStatementList*
SgJavaImportStatementList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaImportStatementList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaImportStatementList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaImportStatementList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaImportStatementList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatementList)};
        unsigned long positionInPool{localIndex % SgJavaImportStatementList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaImportStatementList::pool_size};
        returnPointer = &(((SgJavaImportStatementList*)(SgJavaImportStatementList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaImportStatementList*
SgJavaImportStatementList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaImportStatementList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaImportStatementList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaImportStatementList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaImportStatementList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaImportStatementList)};
        unsigned long positionInPool{localIndex % SgJavaImportStatementList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaImportStatementList::pool_size};
        returnPointer = &(((SgJavaImportStatementList*)(SgJavaImportStatementList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaImportStatementList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaImportStatementList::pools.begin(); block != SgJavaImportStatementList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaImportStatementList*>(*block)};
        for (unsigned i = 0; i < SgJavaImportStatementList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaImportStatementList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaImportStatementList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaImportStatementList* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaImportStatementList::pools.begin(); block != SgJavaImportStatementList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaImportStatementList*>(*block)};
        for (unsigned i = 0; i < SgJavaImportStatementList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaImportStatementList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaImportStatementList::clearMemoryPool() {
    SgJavaImportStatementList* tempPointer{nullptr};
    if (SgJavaImportStatementList::pools.empty() == false) {
        auto block{SgJavaImportStatementList::pools.begin()};
        SgJavaImportStatementList::next_node = reinterpret_cast<SgJavaImportStatementList*>(*block);
        for (; block != SgJavaImportStatementList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaImportStatementList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaImportStatementList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaImportStatementList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaImportStatementList::pool_size-1]);
        }
    }
}

void
SgJavaImportStatementList::deleteMemoryPool() {
    for (auto p: SgJavaImportStatementList::pools) {
        ROSE_FREE(p);
    }
    SgJavaImportStatementList::next_node = nullptr;
    SgJavaImportStatementList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaImportStatementList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatementList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaImportStatementList)};
    for (size_t blockIndex = SgJavaImportStatementList::pools.size(); (blockIndex * SgJavaImportStatementList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatementList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaImportStatementList) = %" PRIuPTR " SgJavaImportStatementList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatementList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaImportStatementList),SgJavaImportStatementList::pool_size);
        }
#endif

        SgJavaImportStatementList* pointer{static_cast<SgJavaImportStatementList*>(ROSE_MALLOC(SgJavaImportStatementList::pool_size * sizeof(SgJavaImportStatementList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaImportStatementList::pool_size * sizeof(SgJavaImportStatementList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaImportStatementList::pool_size * sizeof(SgJavaImportStatementList));
#endif
        SgJavaImportStatementList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaImportStatementList::pool_size * sizeof(SgJavaImportStatementList), V_SgJavaImportStatementList));

        if (SgJavaImportStatementList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaImportStatementList* blkptr{reinterpret_cast<SgJavaImportStatementList*>(SgJavaImportStatementList::pools[blockIndex-1])};
                blkptr[SgJavaImportStatementList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaImportStatementList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaImportStatementList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaImportStatementList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaImportStatementList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaImportStatementList::pool_size - 1};
    SgJavaImportStatementList* testPointer{reinterpret_cast<SgJavaImportStatementList*>(SgJavaImportStatementList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaImportStatementList::pool_size * (SgJavaImportStatementList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaImportStatementListStroageClass
 * from its counterpart of SgJavaImportStatementList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaImportStatementList::initializeStorageClassArray(SgJavaImportStatementListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaImportStatementList::pools.begin(); block != SgJavaImportStatementList::pools.end(); ++block) {
        SgJavaImportStatementList* pointer{reinterpret_cast<SgJavaImportStatementList*>(*block)};
        for (unsigned i = 0; i < SgJavaImportStatementList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 16017 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaClassDeclarationList that takes its corresponding StorageClass as parameter
 */
SgJavaClassDeclarationList::SgJavaClassDeclarationList(const SgJavaClassDeclarationListStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 16027 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_java_class_list = storageSource.storageOf_java_class_list.rebuildDataStoredInEasyStorageClass();
    SgClassDeclarationPtrList::iterator i_java_class_list = p_java_class_list.begin();
    for ( ; i_java_class_list != p_java_class_list.end(); ++i_java_class_list) {
        (*i_java_class_list) = (SgClassDeclarationPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_java_class_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaClassDeclarationList*
SgJavaClassDeclarationList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaClassDeclarationList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaClassDeclarationList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaClassDeclarationList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaClassDeclarationList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaClassDeclarationList)};
        unsigned long positionInPool{localIndex % SgJavaClassDeclarationList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaClassDeclarationList::pool_size};
        returnPointer = &(((SgJavaClassDeclarationList*)(SgJavaClassDeclarationList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaClassDeclarationList*
SgJavaClassDeclarationList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaClassDeclarationList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaClassDeclarationList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaClassDeclarationList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaClassDeclarationList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaClassDeclarationList)};
        unsigned long positionInPool{localIndex % SgJavaClassDeclarationList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaClassDeclarationList::pool_size};
        returnPointer = &(((SgJavaClassDeclarationList*)(SgJavaClassDeclarationList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaClassDeclarationList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaClassDeclarationList::pools.begin(); block != SgJavaClassDeclarationList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaClassDeclarationList*>(*block)};
        for (unsigned i = 0; i < SgJavaClassDeclarationList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaClassDeclarationList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaClassDeclarationList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaClassDeclarationList* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaClassDeclarationList::pools.begin(); block != SgJavaClassDeclarationList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaClassDeclarationList*>(*block)};
        for (unsigned i = 0; i < SgJavaClassDeclarationList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaClassDeclarationList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaClassDeclarationList::clearMemoryPool() {
    SgJavaClassDeclarationList* tempPointer{nullptr};
    if (SgJavaClassDeclarationList::pools.empty() == false) {
        auto block{SgJavaClassDeclarationList::pools.begin()};
        SgJavaClassDeclarationList::next_node = reinterpret_cast<SgJavaClassDeclarationList*>(*block);
        for (; block != SgJavaClassDeclarationList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaClassDeclarationList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaClassDeclarationList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaClassDeclarationList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaClassDeclarationList::pool_size-1]);
        }
    }
}

void
SgJavaClassDeclarationList::deleteMemoryPool() {
    for (auto p: SgJavaClassDeclarationList::pools) {
        ROSE_FREE(p);
    }
    SgJavaClassDeclarationList::next_node = nullptr;
    SgJavaClassDeclarationList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaClassDeclarationList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaClassDeclarationList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaClassDeclarationList)};
    for (size_t blockIndex = SgJavaClassDeclarationList::pools.size(); (blockIndex * SgJavaClassDeclarationList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaClassDeclarationList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaClassDeclarationList) = %" PRIuPTR " SgJavaClassDeclarationList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaClassDeclarationList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaClassDeclarationList),SgJavaClassDeclarationList::pool_size);
        }
#endif

        SgJavaClassDeclarationList* pointer{static_cast<SgJavaClassDeclarationList*>(ROSE_MALLOC(SgJavaClassDeclarationList::pool_size * sizeof(SgJavaClassDeclarationList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaClassDeclarationList::pool_size * sizeof(SgJavaClassDeclarationList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaClassDeclarationList::pool_size * sizeof(SgJavaClassDeclarationList));
#endif
        SgJavaClassDeclarationList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaClassDeclarationList::pool_size * sizeof(SgJavaClassDeclarationList), V_SgJavaClassDeclarationList));

        if (SgJavaClassDeclarationList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaClassDeclarationList* blkptr{reinterpret_cast<SgJavaClassDeclarationList*>(SgJavaClassDeclarationList::pools[blockIndex-1])};
                blkptr[SgJavaClassDeclarationList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaClassDeclarationList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaClassDeclarationList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaClassDeclarationList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaClassDeclarationList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaClassDeclarationList::pool_size - 1};
    SgJavaClassDeclarationList* testPointer{reinterpret_cast<SgJavaClassDeclarationList*>(SgJavaClassDeclarationList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaClassDeclarationList::pool_size * (SgJavaClassDeclarationList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaClassDeclarationListStroageClass
 * from its counterpart of SgJavaClassDeclarationList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaClassDeclarationList::initializeStorageClassArray(SgJavaClassDeclarationListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaClassDeclarationList::pools.begin(); block != SgJavaClassDeclarationList::pools.end(); ++block) {
        SgJavaClassDeclarationList* pointer{reinterpret_cast<SgJavaClassDeclarationList*>(*block)};
        for (unsigned i = 0; i < SgJavaClassDeclarationList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 16249 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgHeaderFileReport that takes its corresponding StorageClass as parameter
 */
SgHeaderFileReport::SgHeaderFileReport(const SgHeaderFileReportStorageClass& storageSource)  : SgSupport (storageSource) {

/* #line 16259 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_source_file = (SgSourceFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_source_file));
    p_include_file_list = storageSource.storageOf_include_file_list.rebuildDataStoredInEasyStorageClass();
    SgSourceFilePtrList::iterator i_include_file_list = p_include_file_list.begin();
    for ( ; i_include_file_list != p_include_file_list.end(); ++i_include_file_list) {
        (*i_include_file_list) = (SgSourceFilePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_include_file_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgHeaderFileReport*
SgHeaderFileReport::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgHeaderFileReport* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHeaderFileReport)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHeaderFileReport + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHeaderFileReport)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileReport)};
        unsigned long positionInPool{localIndex % SgHeaderFileReport::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgHeaderFileReport::pool_size};
        returnPointer = &(((SgHeaderFileReport*)(SgHeaderFileReport::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgHeaderFileReport*
SgHeaderFileReport::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgHeaderFileReport* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHeaderFileReport)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHeaderFileReport + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHeaderFileReport)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgHeaderFileReport)};
        unsigned long positionInPool{localIndex % SgHeaderFileReport::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgHeaderFileReport::pool_size};
        returnPointer = &(((SgHeaderFileReport*)(SgHeaderFileReport::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgHeaderFileReport::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgHeaderFileReport::pools.begin(); block != SgHeaderFileReport::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgHeaderFileReport*>(*block)};
        for (unsigned i = 0; i < SgHeaderFileReport::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgHeaderFileReport*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgHeaderFileReport::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgHeaderFileReport* pointerOfLinkedList{nullptr};
    for (auto block = SgHeaderFileReport::pools.begin(); block != SgHeaderFileReport::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgHeaderFileReport*>(*block)};
        for (unsigned i = 0; i < SgHeaderFileReport::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgHeaderFileReport::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgHeaderFileReport::clearMemoryPool() {
    SgHeaderFileReport* tempPointer{nullptr};
    if (SgHeaderFileReport::pools.empty() == false) {
        auto block{SgHeaderFileReport::pools.begin()};
        SgHeaderFileReport::next_node = reinterpret_cast<SgHeaderFileReport*>(*block);
        for (; block != SgHeaderFileReport::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgHeaderFileReport*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgHeaderFileReport::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgHeaderFileReport::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgHeaderFileReport::pool_size-1]);
        }
    }
}

void
SgHeaderFileReport::deleteMemoryPool() {
    for (auto p: SgHeaderFileReport::pools) {
        ROSE_FREE(p);
    }
    SgHeaderFileReport::next_node = nullptr;
    SgHeaderFileReport::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgHeaderFileReport::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileReport) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgHeaderFileReport)};
    for (size_t blockIndex = SgHeaderFileReport::pools.size(); (blockIndex * SgHeaderFileReport::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileReport) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgHeaderFileReport) = %" PRIuPTR " SgHeaderFileReport::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileReport),AST_FILE_IO::getPoolSizeOfNewAst(V_SgHeaderFileReport),SgHeaderFileReport::pool_size);
        }
#endif

        SgHeaderFileReport* pointer{static_cast<SgHeaderFileReport*>(ROSE_MALLOC(SgHeaderFileReport::pool_size * sizeof(SgHeaderFileReport)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgHeaderFileReport::pool_size * sizeof(SgHeaderFileReport));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgHeaderFileReport::pool_size * sizeof(SgHeaderFileReport));
#endif
        SgHeaderFileReport::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgHeaderFileReport::pool_size * sizeof(SgHeaderFileReport), V_SgHeaderFileReport));

        if (SgHeaderFileReport::next_node != nullptr) {
            if (blockIndex > 0) {
                SgHeaderFileReport* blkptr{reinterpret_cast<SgHeaderFileReport*>(SgHeaderFileReport::pools[blockIndex-1])};
                blkptr[SgHeaderFileReport::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgHeaderFileReport::next_node = pointer;
        }

        for (unsigned i = 0; i < SgHeaderFileReport::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgHeaderFileReport::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgHeaderFileReport::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgHeaderFileReport::pool_size - 1};
    SgHeaderFileReport* testPointer{reinterpret_cast<SgHeaderFileReport*>(SgHeaderFileReport::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgHeaderFileReport::pool_size * (SgHeaderFileReport::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgHeaderFileReportStroageClass
 * from its counterpart of SgHeaderFileReport. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgHeaderFileReport::initializeStorageClassArray(SgHeaderFileReportStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgHeaderFileReport::pools.begin(); block != SgHeaderFileReport::pools.end(); ++block) {
        SgHeaderFileReport* pointer{reinterpret_cast<SgHeaderFileReport*>(*block)};
        for (unsigned i = 0; i < SgHeaderFileReport::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 16482 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgType that takes its corresponding StorageClass as parameter
 */
SgType::SgType(const SgTypeStorageClass& storageSource)  : SgNode (storageSource) {

/* #line 16492 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_isCoArray = storageSource.storageOf_isCoArray;
    p_substitutedForTemplateParam = storageSource.storageOf_substitutedForTemplateParam;
     p_ref_to = (SgReferenceType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ref_to));
     p_ptr_to = (SgPointerType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ptr_to));
     p_modifiers = (SgModifierNodes*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_modifiers));
     p_typedefs = (SgTypedefSeq*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_typedefs));
     p_rvalue_ref_to = (SgRvalueReferenceType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rvalue_ref_to));
     p_decltype_ref_to = (SgDeclType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_decltype_ref_to));
     p_typeof_ref_to = (SgTypeOfType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_typeof_ref_to));
     p_type_kind = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type_kind));
    p_hasTypeKindStar = storageSource.storageOf_hasTypeKindStar;
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgType*
SgType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgType)};
        unsigned long positionInPool{localIndex % SgType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgType::pool_size};
        returnPointer = &(((SgType*)(SgType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgType*
SgType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgType)};
        unsigned long positionInPool{localIndex % SgType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgType::pool_size};
        returnPointer = &(((SgType*)(SgType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgType::pools.begin(); block != SgType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgType*>(*block)};
        for (unsigned i = 0; i < SgType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgType* pointerOfLinkedList{nullptr};
    for (auto block = SgType::pools.begin(); block != SgType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgType*>(*block)};
        for (unsigned i = 0; i < SgType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgType::clearMemoryPool() {
    SgType* tempPointer{nullptr};
    if (SgType::pools.empty() == false) {
        auto block{SgType::pools.begin()};
        SgType::next_node = reinterpret_cast<SgType*>(*block);
        for (; block != SgType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgType::pool_size-1]);
        }
    }
}

void
SgType::deleteMemoryPool() {
    for (auto p: SgType::pools) {
        ROSE_FREE(p);
    }
    SgType::next_node = nullptr;
    SgType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgType)};
    for (size_t blockIndex = SgType::pools.size(); (blockIndex * SgType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgType) = %" PRIuPTR " SgType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgType),SgType::pool_size);
        }
#endif

        SgType* pointer{static_cast<SgType*>(ROSE_MALLOC(SgType::pool_size * sizeof(SgType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgType::pool_size * sizeof(SgType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgType::pool_size * sizeof(SgType));
#endif
        SgType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgType::pool_size * sizeof(SgType), V_SgType));

        if (SgType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgType* blkptr{reinterpret_cast<SgType*>(SgType::pools[blockIndex-1])};
                blkptr[SgType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgType::pool_size - 1};
    SgType* testPointer{reinterpret_cast<SgType*>(SgType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgType::pool_size * (SgType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeStroageClass
 * from its counterpart of SgType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgType::initializeStorageClassArray(SgTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgType::pools.begin(); block != SgType::pools.end(); ++block) {
        SgType* pointer{reinterpret_cast<SgType*>(*block)};
        for (unsigned i = 0; i < SgType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 16721 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeUnknown that takes its corresponding StorageClass as parameter
 */
SgTypeUnknown::SgTypeUnknown(const SgTypeUnknownStorageClass& storageSource)  : SgType (storageSource) {

/* #line 16731 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_type_name = storageSource.storageOf_type_name.rebuildDataStoredInEasyStorageClass();
    p_has_type_name = storageSource.storageOf_has_type_name;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeUnknown*
SgTypeUnknown::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeUnknown* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnknown)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnknown + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnknown)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnknown)};
        unsigned long positionInPool{localIndex % SgTypeUnknown::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnknown::pool_size};
        returnPointer = &(((SgTypeUnknown*)(SgTypeUnknown::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeUnknown*
SgTypeUnknown::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeUnknown* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnknown)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnknown + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnknown)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeUnknown)};
        unsigned long positionInPool{localIndex % SgTypeUnknown::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnknown::pool_size};
        returnPointer = &(((SgTypeUnknown*)(SgTypeUnknown::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeUnknown::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeUnknown::pools.begin(); block != SgTypeUnknown::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnknown*>(*block)};
        for (unsigned i = 0; i < SgTypeUnknown::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeUnknown*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeUnknown::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeUnknown* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeUnknown::pools.begin(); block != SgTypeUnknown::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnknown*>(*block)};
        for (unsigned i = 0; i < SgTypeUnknown::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeUnknown::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeUnknown::clearMemoryPool() {
    SgTypeUnknown* tempPointer{nullptr};
    if (SgTypeUnknown::pools.empty() == false) {
        auto block{SgTypeUnknown::pools.begin()};
        SgTypeUnknown::next_node = reinterpret_cast<SgTypeUnknown*>(*block);
        for (; block != SgTypeUnknown::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeUnknown*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeUnknown::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeUnknown::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeUnknown::pool_size-1]);
        }
    }
}

void
SgTypeUnknown::deleteMemoryPool() {
    for (auto p: SgTypeUnknown::pools) {
        ROSE_FREE(p);
    }
    SgTypeUnknown::next_node = nullptr;
    SgTypeUnknown::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeUnknown::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnknown) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnknown)};
    for (size_t blockIndex = SgTypeUnknown::pools.size(); (blockIndex * SgTypeUnknown::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnknown) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnknown) = %" PRIuPTR " SgTypeUnknown::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnknown),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnknown),SgTypeUnknown::pool_size);
        }
#endif

        SgTypeUnknown* pointer{static_cast<SgTypeUnknown*>(ROSE_MALLOC(SgTypeUnknown::pool_size * sizeof(SgTypeUnknown)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeUnknown::pool_size * sizeof(SgTypeUnknown));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeUnknown::pool_size * sizeof(SgTypeUnknown));
#endif
        SgTypeUnknown::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeUnknown::pool_size * sizeof(SgTypeUnknown), V_SgTypeUnknown));

        if (SgTypeUnknown::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeUnknown* blkptr{reinterpret_cast<SgTypeUnknown*>(SgTypeUnknown::pools[blockIndex-1])};
                blkptr[SgTypeUnknown::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeUnknown::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeUnknown::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeUnknown::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeUnknown::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeUnknown::pool_size - 1};
    SgTypeUnknown* testPointer{reinterpret_cast<SgTypeUnknown*>(SgTypeUnknown::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeUnknown::pool_size * (SgTypeUnknown::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeUnknownStroageClass
 * from its counterpart of SgTypeUnknown. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeUnknown::initializeStorageClassArray(SgTypeUnknownStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeUnknown::pools.begin(); block != SgTypeUnknown::pools.end(); ++block) {
        SgTypeUnknown* pointer{reinterpret_cast<SgTypeUnknown*>(*block)};
        for (unsigned i = 0; i < SgTypeUnknown::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 16950 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeVoid that takes its corresponding StorageClass as parameter
 */
SgTypeVoid::SgTypeVoid(const SgTypeVoidStorageClass& storageSource)  : SgType (storageSource) {

/* #line 16960 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeVoid*
SgTypeVoid::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeVoid* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeVoid)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeVoid + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeVoid)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeVoid)};
        unsigned long positionInPool{localIndex % SgTypeVoid::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeVoid::pool_size};
        returnPointer = &(((SgTypeVoid*)(SgTypeVoid::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeVoid*
SgTypeVoid::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeVoid* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeVoid)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeVoid + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeVoid)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeVoid)};
        unsigned long positionInPool{localIndex % SgTypeVoid::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeVoid::pool_size};
        returnPointer = &(((SgTypeVoid*)(SgTypeVoid::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeVoid::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeVoid::pools.begin(); block != SgTypeVoid::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeVoid*>(*block)};
        for (unsigned i = 0; i < SgTypeVoid::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeVoid*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeVoid::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeVoid* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeVoid::pools.begin(); block != SgTypeVoid::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeVoid*>(*block)};
        for (unsigned i = 0; i < SgTypeVoid::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeVoid::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeVoid::clearMemoryPool() {
    SgTypeVoid* tempPointer{nullptr};
    if (SgTypeVoid::pools.empty() == false) {
        auto block{SgTypeVoid::pools.begin()};
        SgTypeVoid::next_node = reinterpret_cast<SgTypeVoid*>(*block);
        for (; block != SgTypeVoid::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeVoid*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeVoid::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeVoid::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeVoid::pool_size-1]);
        }
    }
}

void
SgTypeVoid::deleteMemoryPool() {
    for (auto p: SgTypeVoid::pools) {
        ROSE_FREE(p);
    }
    SgTypeVoid::next_node = nullptr;
    SgTypeVoid::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeVoid::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeVoid) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeVoid)};
    for (size_t blockIndex = SgTypeVoid::pools.size(); (blockIndex * SgTypeVoid::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeVoid) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeVoid) = %" PRIuPTR " SgTypeVoid::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeVoid),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeVoid),SgTypeVoid::pool_size);
        }
#endif

        SgTypeVoid* pointer{static_cast<SgTypeVoid*>(ROSE_MALLOC(SgTypeVoid::pool_size * sizeof(SgTypeVoid)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeVoid::pool_size * sizeof(SgTypeVoid));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeVoid::pool_size * sizeof(SgTypeVoid));
#endif
        SgTypeVoid::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeVoid::pool_size * sizeof(SgTypeVoid), V_SgTypeVoid));

        if (SgTypeVoid::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeVoid* blkptr{reinterpret_cast<SgTypeVoid*>(SgTypeVoid::pools[blockIndex-1])};
                blkptr[SgTypeVoid::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeVoid::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeVoid::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeVoid::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeVoid::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeVoid::pool_size - 1};
    SgTypeVoid* testPointer{reinterpret_cast<SgTypeVoid*>(SgTypeVoid::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeVoid::pool_size * (SgTypeVoid::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeVoidStroageClass
 * from its counterpart of SgTypeVoid. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeVoid::initializeStorageClassArray(SgTypeVoidStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeVoid::pools.begin(); block != SgTypeVoid::pools.end(); ++block) {
        SgTypeVoid* pointer{reinterpret_cast<SgTypeVoid*>(*block)};
        for (unsigned i = 0; i < SgTypeVoid::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 17177 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeGlobalVoid that takes its corresponding StorageClass as parameter
 */
SgTypeGlobalVoid::SgTypeGlobalVoid(const SgTypeGlobalVoidStorageClass& storageSource)  : SgType (storageSource) {

/* #line 17187 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeGlobalVoid*
SgTypeGlobalVoid::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeGlobalVoid* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeGlobalVoid)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeGlobalVoid + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeGlobalVoid)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeGlobalVoid)};
        unsigned long positionInPool{localIndex % SgTypeGlobalVoid::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeGlobalVoid::pool_size};
        returnPointer = &(((SgTypeGlobalVoid*)(SgTypeGlobalVoid::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeGlobalVoid*
SgTypeGlobalVoid::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeGlobalVoid* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeGlobalVoid)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeGlobalVoid + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeGlobalVoid)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeGlobalVoid)};
        unsigned long positionInPool{localIndex % SgTypeGlobalVoid::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeGlobalVoid::pool_size};
        returnPointer = &(((SgTypeGlobalVoid*)(SgTypeGlobalVoid::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeGlobalVoid::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeGlobalVoid::pools.begin(); block != SgTypeGlobalVoid::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeGlobalVoid*>(*block)};
        for (unsigned i = 0; i < SgTypeGlobalVoid::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeGlobalVoid*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeGlobalVoid::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeGlobalVoid* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeGlobalVoid::pools.begin(); block != SgTypeGlobalVoid::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeGlobalVoid*>(*block)};
        for (unsigned i = 0; i < SgTypeGlobalVoid::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeGlobalVoid::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeGlobalVoid::clearMemoryPool() {
    SgTypeGlobalVoid* tempPointer{nullptr};
    if (SgTypeGlobalVoid::pools.empty() == false) {
        auto block{SgTypeGlobalVoid::pools.begin()};
        SgTypeGlobalVoid::next_node = reinterpret_cast<SgTypeGlobalVoid*>(*block);
        for (; block != SgTypeGlobalVoid::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeGlobalVoid*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeGlobalVoid::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeGlobalVoid::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeGlobalVoid::pool_size-1]);
        }
    }
}

void
SgTypeGlobalVoid::deleteMemoryPool() {
    for (auto p: SgTypeGlobalVoid::pools) {
        ROSE_FREE(p);
    }
    SgTypeGlobalVoid::next_node = nullptr;
    SgTypeGlobalVoid::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeGlobalVoid::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeGlobalVoid) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeGlobalVoid)};
    for (size_t blockIndex = SgTypeGlobalVoid::pools.size(); (blockIndex * SgTypeGlobalVoid::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeGlobalVoid) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeGlobalVoid) = %" PRIuPTR " SgTypeGlobalVoid::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeGlobalVoid),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeGlobalVoid),SgTypeGlobalVoid::pool_size);
        }
#endif

        SgTypeGlobalVoid* pointer{static_cast<SgTypeGlobalVoid*>(ROSE_MALLOC(SgTypeGlobalVoid::pool_size * sizeof(SgTypeGlobalVoid)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeGlobalVoid::pool_size * sizeof(SgTypeGlobalVoid));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeGlobalVoid::pool_size * sizeof(SgTypeGlobalVoid));
#endif
        SgTypeGlobalVoid::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeGlobalVoid::pool_size * sizeof(SgTypeGlobalVoid), V_SgTypeGlobalVoid));

        if (SgTypeGlobalVoid::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeGlobalVoid* blkptr{reinterpret_cast<SgTypeGlobalVoid*>(SgTypeGlobalVoid::pools[blockIndex-1])};
                blkptr[SgTypeGlobalVoid::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeGlobalVoid::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeGlobalVoid::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeGlobalVoid::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeGlobalVoid::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeGlobalVoid::pool_size - 1};
    SgTypeGlobalVoid* testPointer{reinterpret_cast<SgTypeGlobalVoid*>(SgTypeGlobalVoid::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeGlobalVoid::pool_size * (SgTypeGlobalVoid::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeGlobalVoidStroageClass
 * from its counterpart of SgTypeGlobalVoid. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeGlobalVoid::initializeStorageClassArray(SgTypeGlobalVoidStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeGlobalVoid::pools.begin(); block != SgTypeGlobalVoid::pools.end(); ++block) {
        SgTypeGlobalVoid* pointer{reinterpret_cast<SgTypeGlobalVoid*>(*block)};
        for (unsigned i = 0; i < SgTypeGlobalVoid::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 17404 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeTuple that takes its corresponding StorageClass as parameter
 */
SgTypeTuple::SgTypeTuple(const SgTypeTupleStorageClass& storageSource)  : SgType (storageSource) {

/* #line 17414 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_types = storageSource.storageOf_types.rebuildDataStoredInEasyStorageClass();
    SgTypePtrList::iterator i_types = p_types.begin();
    for ( ; i_types != p_types.end(); ++i_types) {
        (*i_types) = (SgTypePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_types)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeTuple*
SgTypeTuple::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeTuple* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTuple)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTuple + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTuple)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTuple)};
        unsigned long positionInPool{localIndex % SgTypeTuple::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeTuple::pool_size};
        returnPointer = &(((SgTypeTuple*)(SgTypeTuple::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeTuple*
SgTypeTuple::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeTuple* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTuple)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTuple + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTuple)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeTuple)};
        unsigned long positionInPool{localIndex % SgTypeTuple::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeTuple::pool_size};
        returnPointer = &(((SgTypeTuple*)(SgTypeTuple::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeTuple::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeTuple::pools.begin(); block != SgTypeTuple::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeTuple*>(*block)};
        for (unsigned i = 0; i < SgTypeTuple::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeTuple*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeTuple::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeTuple* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeTuple::pools.begin(); block != SgTypeTuple::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeTuple*>(*block)};
        for (unsigned i = 0; i < SgTypeTuple::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeTuple::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeTuple::clearMemoryPool() {
    SgTypeTuple* tempPointer{nullptr};
    if (SgTypeTuple::pools.empty() == false) {
        auto block{SgTypeTuple::pools.begin()};
        SgTypeTuple::next_node = reinterpret_cast<SgTypeTuple*>(*block);
        for (; block != SgTypeTuple::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeTuple*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeTuple::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeTuple::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeTuple::pool_size-1]);
        }
    }
}

void
SgTypeTuple::deleteMemoryPool() {
    for (auto p: SgTypeTuple::pools) {
        ROSE_FREE(p);
    }
    SgTypeTuple::next_node = nullptr;
    SgTypeTuple::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeTuple::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTuple) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTuple)};
    for (size_t blockIndex = SgTypeTuple::pools.size(); (blockIndex * SgTypeTuple::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTuple) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTuple) = %" PRIuPTR " SgTypeTuple::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTuple),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTuple),SgTypeTuple::pool_size);
        }
#endif

        SgTypeTuple* pointer{static_cast<SgTypeTuple*>(ROSE_MALLOC(SgTypeTuple::pool_size * sizeof(SgTypeTuple)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeTuple::pool_size * sizeof(SgTypeTuple));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeTuple::pool_size * sizeof(SgTypeTuple));
#endif
        SgTypeTuple::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeTuple::pool_size * sizeof(SgTypeTuple), V_SgTypeTuple));

        if (SgTypeTuple::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeTuple* blkptr{reinterpret_cast<SgTypeTuple*>(SgTypeTuple::pools[blockIndex-1])};
                blkptr[SgTypeTuple::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeTuple::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeTuple::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeTuple::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeTuple::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeTuple::pool_size - 1};
    SgTypeTuple* testPointer{reinterpret_cast<SgTypeTuple*>(SgTypeTuple::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeTuple::pool_size * (SgTypeTuple::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeTupleStroageClass
 * from its counterpart of SgTypeTuple. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeTuple::initializeStorageClassArray(SgTypeTupleStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeTuple::pools.begin(); block != SgTypeTuple::pools.end(); ++block) {
        SgTypeTuple* pointer{reinterpret_cast<SgTypeTuple*>(*block)};
        for (unsigned i = 0; i < SgTypeTuple::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 17636 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeString that takes its corresponding StorageClass as parameter
 */
SgTypeString::SgTypeString(const SgTypeStringStorageClass& storageSource)  : SgType (storageSource) {

/* #line 17646 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lengthExpression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lengthExpression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeString*
SgTypeString::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeString)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeString)};
        unsigned long positionInPool{localIndex % SgTypeString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeString::pool_size};
        returnPointer = &(((SgTypeString*)(SgTypeString::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeString*
SgTypeString::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeString)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeString)};
        unsigned long positionInPool{localIndex % SgTypeString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeString::pool_size};
        returnPointer = &(((SgTypeString*)(SgTypeString::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeString::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeString::pools.begin(); block != SgTypeString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeString*>(*block)};
        for (unsigned i = 0; i < SgTypeString::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeString*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeString::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeString* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeString::pools.begin(); block != SgTypeString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeString*>(*block)};
        for (unsigned i = 0; i < SgTypeString::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeString::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeString::clearMemoryPool() {
    SgTypeString* tempPointer{nullptr};
    if (SgTypeString::pools.empty() == false) {
        auto block{SgTypeString::pools.begin()};
        SgTypeString::next_node = reinterpret_cast<SgTypeString*>(*block);
        for (; block != SgTypeString::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeString*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeString::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeString::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeString::pool_size-1]);
        }
    }
}

void
SgTypeString::deleteMemoryPool() {
    for (auto p: SgTypeString::pools) {
        ROSE_FREE(p);
    }
    SgTypeString::next_node = nullptr;
    SgTypeString::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeString::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeString) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeString)};
    for (size_t blockIndex = SgTypeString::pools.size(); (blockIndex * SgTypeString::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeString) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeString) = %" PRIuPTR " SgTypeString::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeString),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeString),SgTypeString::pool_size);
        }
#endif

        SgTypeString* pointer{static_cast<SgTypeString*>(ROSE_MALLOC(SgTypeString::pool_size * sizeof(SgTypeString)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeString::pool_size * sizeof(SgTypeString));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeString::pool_size * sizeof(SgTypeString));
#endif
        SgTypeString::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeString::pool_size * sizeof(SgTypeString), V_SgTypeString));

        if (SgTypeString::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeString* blkptr{reinterpret_cast<SgTypeString*>(SgTypeString::pools[blockIndex-1])};
                blkptr[SgTypeString::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeString::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeString::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeString::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeString::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeString::pool_size - 1};
    SgTypeString* testPointer{reinterpret_cast<SgTypeString*>(SgTypeString::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeString::pool_size * (SgTypeString::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeStringStroageClass
 * from its counterpart of SgTypeString. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeString::initializeStorageClassArray(SgTypeStringStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeString::pools.begin(); block != SgTypeString::pools.end(); ++block) {
        SgTypeString* pointer{reinterpret_cast<SgTypeString*>(*block)};
        for (unsigned i = 0; i < SgTypeString::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 17864 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRangeType that takes its corresponding StorageClass as parameter
 */
SgRangeType::SgRangeType(const SgRangeTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 17874 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRangeType*
SgRangeType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRangeType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeType)};
        unsigned long positionInPool{localIndex % SgRangeType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRangeType::pool_size};
        returnPointer = &(((SgRangeType*)(SgRangeType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRangeType*
SgRangeType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRangeType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRangeType)};
        unsigned long positionInPool{localIndex % SgRangeType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRangeType::pool_size};
        returnPointer = &(((SgRangeType*)(SgRangeType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRangeType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRangeType::pools.begin(); block != SgRangeType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRangeType*>(*block)};
        for (unsigned i = 0; i < SgRangeType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRangeType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRangeType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRangeType* pointerOfLinkedList{nullptr};
    for (auto block = SgRangeType::pools.begin(); block != SgRangeType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRangeType*>(*block)};
        for (unsigned i = 0; i < SgRangeType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRangeType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRangeType::clearMemoryPool() {
    SgRangeType* tempPointer{nullptr};
    if (SgRangeType::pools.empty() == false) {
        auto block{SgRangeType::pools.begin()};
        SgRangeType::next_node = reinterpret_cast<SgRangeType*>(*block);
        for (; block != SgRangeType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRangeType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRangeType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRangeType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRangeType::pool_size-1]);
        }
    }
}

void
SgRangeType::deleteMemoryPool() {
    for (auto p: SgRangeType::pools) {
        ROSE_FREE(p);
    }
    SgRangeType::next_node = nullptr;
    SgRangeType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRangeType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeType)};
    for (size_t blockIndex = SgRangeType::pools.size(); (blockIndex * SgRangeType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeType) = %" PRIuPTR " SgRangeType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeType),SgRangeType::pool_size);
        }
#endif

        SgRangeType* pointer{static_cast<SgRangeType*>(ROSE_MALLOC(SgRangeType::pool_size * sizeof(SgRangeType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRangeType::pool_size * sizeof(SgRangeType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRangeType::pool_size * sizeof(SgRangeType));
#endif
        SgRangeType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRangeType::pool_size * sizeof(SgRangeType), V_SgRangeType));

        if (SgRangeType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRangeType* blkptr{reinterpret_cast<SgRangeType*>(SgRangeType::pools[blockIndex-1])};
                blkptr[SgRangeType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRangeType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRangeType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRangeType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRangeType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRangeType::pool_size - 1};
    SgRangeType* testPointer{reinterpret_cast<SgRangeType*>(SgRangeType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRangeType::pool_size * (SgRangeType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRangeTypeStroageClass
 * from its counterpart of SgRangeType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRangeType::initializeStorageClassArray(SgRangeTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRangeType::pools.begin(); block != SgRangeType::pools.end(); ++block) {
        SgRangeType* pointer{reinterpret_cast<SgRangeType*>(*block)};
        for (unsigned i = 0; i < SgRangeType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 18092 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPointerType that takes its corresponding StorageClass as parameter
 */
SgPointerType::SgPointerType(const SgPointerTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 18102 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPointerType*
SgPointerType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPointerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerType)};
        unsigned long positionInPool{localIndex % SgPointerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerType::pool_size};
        returnPointer = &(((SgPointerType*)(SgPointerType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPointerType*
SgPointerType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPointerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPointerType)};
        unsigned long positionInPool{localIndex % SgPointerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerType::pool_size};
        returnPointer = &(((SgPointerType*)(SgPointerType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPointerType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPointerType::pools.begin(); block != SgPointerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerType*>(*block)};
        for (unsigned i = 0; i < SgPointerType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPointerType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPointerType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPointerType* pointerOfLinkedList{nullptr};
    for (auto block = SgPointerType::pools.begin(); block != SgPointerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerType*>(*block)};
        for (unsigned i = 0; i < SgPointerType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPointerType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPointerType::clearMemoryPool() {
    SgPointerType* tempPointer{nullptr};
    if (SgPointerType::pools.empty() == false) {
        auto block{SgPointerType::pools.begin()};
        SgPointerType::next_node = reinterpret_cast<SgPointerType*>(*block);
        for (; block != SgPointerType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPointerType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPointerType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPointerType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPointerType::pool_size-1]);
        }
    }
}

void
SgPointerType::deleteMemoryPool() {
    for (auto p: SgPointerType::pools) {
        ROSE_FREE(p);
    }
    SgPointerType::next_node = nullptr;
    SgPointerType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPointerType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerType)};
    for (size_t blockIndex = SgPointerType::pools.size(); (blockIndex * SgPointerType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerType) = %" PRIuPTR " SgPointerType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerType),SgPointerType::pool_size);
        }
#endif

        SgPointerType* pointer{static_cast<SgPointerType*>(ROSE_MALLOC(SgPointerType::pool_size * sizeof(SgPointerType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPointerType::pool_size * sizeof(SgPointerType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPointerType::pool_size * sizeof(SgPointerType));
#endif
        SgPointerType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPointerType::pool_size * sizeof(SgPointerType), V_SgPointerType));

        if (SgPointerType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPointerType* blkptr{reinterpret_cast<SgPointerType*>(SgPointerType::pools[blockIndex-1])};
                blkptr[SgPointerType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPointerType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPointerType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPointerType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPointerType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPointerType::pool_size - 1};
    SgPointerType* testPointer{reinterpret_cast<SgPointerType*>(SgPointerType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPointerType::pool_size * (SgPointerType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPointerTypeStroageClass
 * from its counterpart of SgPointerType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPointerType::initializeStorageClassArray(SgPointerTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPointerType::pools.begin(); block != SgPointerType::pools.end(); ++block) {
        SgPointerType* pointer{reinterpret_cast<SgPointerType*>(*block)};
        for (unsigned i = 0; i < SgPointerType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 18320 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPointerMemberType that takes its corresponding StorageClass as parameter
 */
SgPointerMemberType::SgPointerMemberType(const SgPointerMemberTypeStorageClass& storageSource)  : SgPointerType (storageSource) {

/* #line 18330 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_class_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_class_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPointerMemberType*
SgPointerMemberType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPointerMemberType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerMemberType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerMemberType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerMemberType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerMemberType)};
        unsigned long positionInPool{localIndex % SgPointerMemberType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerMemberType::pool_size};
        returnPointer = &(((SgPointerMemberType*)(SgPointerMemberType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPointerMemberType*
SgPointerMemberType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPointerMemberType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerMemberType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerMemberType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerMemberType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPointerMemberType)};
        unsigned long positionInPool{localIndex % SgPointerMemberType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerMemberType::pool_size};
        returnPointer = &(((SgPointerMemberType*)(SgPointerMemberType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPointerMemberType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPointerMemberType::pools.begin(); block != SgPointerMemberType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerMemberType*>(*block)};
        for (unsigned i = 0; i < SgPointerMemberType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPointerMemberType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPointerMemberType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPointerMemberType* pointerOfLinkedList{nullptr};
    for (auto block = SgPointerMemberType::pools.begin(); block != SgPointerMemberType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerMemberType*>(*block)};
        for (unsigned i = 0; i < SgPointerMemberType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPointerMemberType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPointerMemberType::clearMemoryPool() {
    SgPointerMemberType* tempPointer{nullptr};
    if (SgPointerMemberType::pools.empty() == false) {
        auto block{SgPointerMemberType::pools.begin()};
        SgPointerMemberType::next_node = reinterpret_cast<SgPointerMemberType*>(*block);
        for (; block != SgPointerMemberType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPointerMemberType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPointerMemberType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPointerMemberType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPointerMemberType::pool_size-1]);
        }
    }
}

void
SgPointerMemberType::deleteMemoryPool() {
    for (auto p: SgPointerMemberType::pools) {
        ROSE_FREE(p);
    }
    SgPointerMemberType::next_node = nullptr;
    SgPointerMemberType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPointerMemberType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerMemberType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerMemberType)};
    for (size_t blockIndex = SgPointerMemberType::pools.size(); (blockIndex * SgPointerMemberType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerMemberType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerMemberType) = %" PRIuPTR " SgPointerMemberType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerMemberType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerMemberType),SgPointerMemberType::pool_size);
        }
#endif

        SgPointerMemberType* pointer{static_cast<SgPointerMemberType*>(ROSE_MALLOC(SgPointerMemberType::pool_size * sizeof(SgPointerMemberType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPointerMemberType::pool_size * sizeof(SgPointerMemberType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPointerMemberType::pool_size * sizeof(SgPointerMemberType));
#endif
        SgPointerMemberType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPointerMemberType::pool_size * sizeof(SgPointerMemberType), V_SgPointerMemberType));

        if (SgPointerMemberType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPointerMemberType* blkptr{reinterpret_cast<SgPointerMemberType*>(SgPointerMemberType::pools[blockIndex-1])};
                blkptr[SgPointerMemberType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPointerMemberType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPointerMemberType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPointerMemberType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPointerMemberType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPointerMemberType::pool_size - 1};
    SgPointerMemberType* testPointer{reinterpret_cast<SgPointerMemberType*>(SgPointerMemberType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPointerMemberType::pool_size * (SgPointerMemberType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPointerMemberTypeStroageClass
 * from its counterpart of SgPointerMemberType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPointerMemberType::initializeStorageClassArray(SgPointerMemberTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPointerMemberType::pools.begin(); block != SgPointerMemberType::pools.end(); ++block) {
        SgPointerMemberType* pointer{reinterpret_cast<SgPointerMemberType*>(*block)};
        for (unsigned i = 0; i < SgPointerMemberType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 18548 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialBitType that takes its corresponding StorageClass as parameter
 */
SgJovialBitType::SgJovialBitType(const SgJovialBitTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 18558 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_size = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_size));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialBitType*
SgJovialBitType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialBitType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialBitType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialBitType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialBitType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitType)};
        unsigned long positionInPool{localIndex % SgJovialBitType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialBitType::pool_size};
        returnPointer = &(((SgJovialBitType*)(SgJovialBitType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialBitType*
SgJovialBitType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialBitType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialBitType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialBitType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialBitType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialBitType)};
        unsigned long positionInPool{localIndex % SgJovialBitType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialBitType::pool_size};
        returnPointer = &(((SgJovialBitType*)(SgJovialBitType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialBitType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialBitType::pools.begin(); block != SgJovialBitType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialBitType*>(*block)};
        for (unsigned i = 0; i < SgJovialBitType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialBitType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialBitType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialBitType* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialBitType::pools.begin(); block != SgJovialBitType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialBitType*>(*block)};
        for (unsigned i = 0; i < SgJovialBitType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialBitType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialBitType::clearMemoryPool() {
    SgJovialBitType* tempPointer{nullptr};
    if (SgJovialBitType::pools.empty() == false) {
        auto block{SgJovialBitType::pools.begin()};
        SgJovialBitType::next_node = reinterpret_cast<SgJovialBitType*>(*block);
        for (; block != SgJovialBitType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialBitType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialBitType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialBitType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialBitType::pool_size-1]);
        }
    }
}

void
SgJovialBitType::deleteMemoryPool() {
    for (auto p: SgJovialBitType::pools) {
        ROSE_FREE(p);
    }
    SgJovialBitType::next_node = nullptr;
    SgJovialBitType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialBitType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialBitType)};
    for (size_t blockIndex = SgJovialBitType::pools.size(); (blockIndex * SgJovialBitType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialBitType) = %" PRIuPTR " SgJovialBitType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialBitType),SgJovialBitType::pool_size);
        }
#endif

        SgJovialBitType* pointer{static_cast<SgJovialBitType*>(ROSE_MALLOC(SgJovialBitType::pool_size * sizeof(SgJovialBitType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialBitType::pool_size * sizeof(SgJovialBitType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialBitType::pool_size * sizeof(SgJovialBitType));
#endif
        SgJovialBitType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialBitType::pool_size * sizeof(SgJovialBitType), V_SgJovialBitType));

        if (SgJovialBitType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialBitType* blkptr{reinterpret_cast<SgJovialBitType*>(SgJovialBitType::pools[blockIndex-1])};
                blkptr[SgJovialBitType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialBitType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialBitType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialBitType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialBitType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialBitType::pool_size - 1};
    SgJovialBitType* testPointer{reinterpret_cast<SgJovialBitType*>(SgJovialBitType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialBitType::pool_size * (SgJovialBitType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialBitTypeStroageClass
 * from its counterpart of SgJovialBitType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialBitType::initializeStorageClassArray(SgJovialBitTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialBitType::pools.begin(); block != SgJovialBitType::pools.end(); ++block) {
        SgJovialBitType* pointer{reinterpret_cast<SgJovialBitType*>(*block)};
        for (unsigned i = 0; i < SgJovialBitType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 18776 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgReferenceType that takes its corresponding StorageClass as parameter
 */
SgReferenceType::SgReferenceType(const SgReferenceTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 18786 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgReferenceType*
SgReferenceType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgReferenceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReferenceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReferenceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReferenceType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceType)};
        unsigned long positionInPool{localIndex % SgReferenceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReferenceType::pool_size};
        returnPointer = &(((SgReferenceType*)(SgReferenceType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgReferenceType*
SgReferenceType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgReferenceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReferenceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReferenceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReferenceType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgReferenceType)};
        unsigned long positionInPool{localIndex % SgReferenceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReferenceType::pool_size};
        returnPointer = &(((SgReferenceType*)(SgReferenceType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgReferenceType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgReferenceType::pools.begin(); block != SgReferenceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReferenceType*>(*block)};
        for (unsigned i = 0; i < SgReferenceType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgReferenceType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgReferenceType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgReferenceType* pointerOfLinkedList{nullptr};
    for (auto block = SgReferenceType::pools.begin(); block != SgReferenceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReferenceType*>(*block)};
        for (unsigned i = 0; i < SgReferenceType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgReferenceType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgReferenceType::clearMemoryPool() {
    SgReferenceType* tempPointer{nullptr};
    if (SgReferenceType::pools.empty() == false) {
        auto block{SgReferenceType::pools.begin()};
        SgReferenceType::next_node = reinterpret_cast<SgReferenceType*>(*block);
        for (; block != SgReferenceType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgReferenceType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgReferenceType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgReferenceType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgReferenceType::pool_size-1]);
        }
    }
}

void
SgReferenceType::deleteMemoryPool() {
    for (auto p: SgReferenceType::pools) {
        ROSE_FREE(p);
    }
    SgReferenceType::next_node = nullptr;
    SgReferenceType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgReferenceType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgReferenceType)};
    for (size_t blockIndex = SgReferenceType::pools.size(); (blockIndex * SgReferenceType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgReferenceType) = %" PRIuPTR " SgReferenceType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgReferenceType),SgReferenceType::pool_size);
        }
#endif

        SgReferenceType* pointer{static_cast<SgReferenceType*>(ROSE_MALLOC(SgReferenceType::pool_size * sizeof(SgReferenceType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgReferenceType::pool_size * sizeof(SgReferenceType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgReferenceType::pool_size * sizeof(SgReferenceType));
#endif
        SgReferenceType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgReferenceType::pool_size * sizeof(SgReferenceType), V_SgReferenceType));

        if (SgReferenceType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgReferenceType* blkptr{reinterpret_cast<SgReferenceType*>(SgReferenceType::pools[blockIndex-1])};
                blkptr[SgReferenceType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgReferenceType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgReferenceType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgReferenceType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgReferenceType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgReferenceType::pool_size - 1};
    SgReferenceType* testPointer{reinterpret_cast<SgReferenceType*>(SgReferenceType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgReferenceType::pool_size * (SgReferenceType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgReferenceTypeStroageClass
 * from its counterpart of SgReferenceType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgReferenceType::initializeStorageClassArray(SgReferenceTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgReferenceType::pools.begin(); block != SgReferenceType::pools.end(); ++block) {
        SgReferenceType* pointer{reinterpret_cast<SgReferenceType*>(*block)};
        for (unsigned i = 0; i < SgReferenceType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 19004 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNamedType that takes its corresponding StorageClass as parameter
 */
SgNamedType::SgNamedType(const SgNamedTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 19014 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
    p_autonomous_declaration = storageSource.storageOf_autonomous_declaration;
    p_is_from_template_parameter = storageSource.storageOf_is_from_template_parameter;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNamedType*
SgNamedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNamedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNamedType)};
        unsigned long positionInPool{localIndex % SgNamedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamedType::pool_size};
        returnPointer = &(((SgNamedType*)(SgNamedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNamedType*
SgNamedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNamedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNamedType)};
        unsigned long positionInPool{localIndex % SgNamedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamedType::pool_size};
        returnPointer = &(((SgNamedType*)(SgNamedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNamedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNamedType::pools.begin(); block != SgNamedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamedType*>(*block)};
        for (unsigned i = 0; i < SgNamedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNamedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNamedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNamedType* pointerOfLinkedList{nullptr};
    for (auto block = SgNamedType::pools.begin(); block != SgNamedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamedType*>(*block)};
        for (unsigned i = 0; i < SgNamedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNamedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNamedType::clearMemoryPool() {
    SgNamedType* tempPointer{nullptr};
    if (SgNamedType::pools.empty() == false) {
        auto block{SgNamedType::pools.begin()};
        SgNamedType::next_node = reinterpret_cast<SgNamedType*>(*block);
        for (; block != SgNamedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNamedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNamedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNamedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNamedType::pool_size-1]);
        }
    }
}

void
SgNamedType::deleteMemoryPool() {
    for (auto p: SgNamedType::pools) {
        ROSE_FREE(p);
    }
    SgNamedType::next_node = nullptr;
    SgNamedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNamedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNamedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamedType)};
    for (size_t blockIndex = SgNamedType::pools.size(); (blockIndex * SgNamedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNamedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamedType) = %" PRIuPTR " SgNamedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNamedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamedType),SgNamedType::pool_size);
        }
#endif

        SgNamedType* pointer{static_cast<SgNamedType*>(ROSE_MALLOC(SgNamedType::pool_size * sizeof(SgNamedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNamedType::pool_size * sizeof(SgNamedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNamedType::pool_size * sizeof(SgNamedType));
#endif
        SgNamedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNamedType::pool_size * sizeof(SgNamedType), V_SgNamedType));

        if (SgNamedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNamedType* blkptr{reinterpret_cast<SgNamedType*>(SgNamedType::pools[blockIndex-1])};
                blkptr[SgNamedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNamedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNamedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNamedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNamedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNamedType::pool_size - 1};
    SgNamedType* testPointer{reinterpret_cast<SgNamedType*>(SgNamedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNamedType::pool_size * (SgNamedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNamedTypeStroageClass
 * from its counterpart of SgNamedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNamedType::initializeStorageClassArray(SgNamedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNamedType::pools.begin(); block != SgNamedType::pools.end(); ++block) {
        SgNamedType* pointer{reinterpret_cast<SgNamedType*>(*block)};
        for (unsigned i = 0; i < SgNamedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 19234 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClassType that takes its corresponding StorageClass as parameter
 */
SgClassType::SgClassType(const SgClassTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 19244 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_packed = storageSource.storageOf_packed;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClassType*
SgClassType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClassType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClassType)};
        unsigned long positionInPool{localIndex % SgClassType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassType::pool_size};
        returnPointer = &(((SgClassType*)(SgClassType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClassType*
SgClassType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClassType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClassType)};
        unsigned long positionInPool{localIndex % SgClassType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassType::pool_size};
        returnPointer = &(((SgClassType*)(SgClassType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClassType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClassType::pools.begin(); block != SgClassType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassType*>(*block)};
        for (unsigned i = 0; i < SgClassType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClassType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClassType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClassType* pointerOfLinkedList{nullptr};
    for (auto block = SgClassType::pools.begin(); block != SgClassType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassType*>(*block)};
        for (unsigned i = 0; i < SgClassType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClassType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClassType::clearMemoryPool() {
    SgClassType* tempPointer{nullptr};
    if (SgClassType::pools.empty() == false) {
        auto block{SgClassType::pools.begin()};
        SgClassType::next_node = reinterpret_cast<SgClassType*>(*block);
        for (; block != SgClassType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClassType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClassType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClassType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClassType::pool_size-1]);
        }
    }
}

void
SgClassType::deleteMemoryPool() {
    for (auto p: SgClassType::pools) {
        ROSE_FREE(p);
    }
    SgClassType::next_node = nullptr;
    SgClassType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClassType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClassType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassType)};
    for (size_t blockIndex = SgClassType::pools.size(); (blockIndex * SgClassType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClassType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassType) = %" PRIuPTR " SgClassType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClassType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassType),SgClassType::pool_size);
        }
#endif

        SgClassType* pointer{static_cast<SgClassType*>(ROSE_MALLOC(SgClassType::pool_size * sizeof(SgClassType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClassType::pool_size * sizeof(SgClassType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClassType::pool_size * sizeof(SgClassType));
#endif
        SgClassType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClassType::pool_size * sizeof(SgClassType), V_SgClassType));

        if (SgClassType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClassType* blkptr{reinterpret_cast<SgClassType*>(SgClassType::pools[blockIndex-1])};
                blkptr[SgClassType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClassType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClassType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClassType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClassType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClassType::pool_size - 1};
    SgClassType* testPointer{reinterpret_cast<SgClassType*>(SgClassType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClassType::pool_size * (SgClassType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClassTypeStroageClass
 * from its counterpart of SgClassType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClassType::initializeStorageClassArray(SgClassTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClassType::pools.begin(); block != SgClassType::pools.end(); ++block) {
        SgClassType* pointer{reinterpret_cast<SgClassType*>(*block)};
        for (unsigned i = 0; i < SgClassType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 19462 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaParameterType that takes its corresponding StorageClass as parameter
 */
SgJavaParameterType::SgJavaParameterType(const SgJavaParameterTypeStorageClass& storageSource)  : SgClassType (storageSource) {

/* #line 19472 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaParameterType*
SgJavaParameterType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaParameterType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaParameterType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaParameterType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaParameterType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterType)};
        unsigned long positionInPool{localIndex % SgJavaParameterType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaParameterType::pool_size};
        returnPointer = &(((SgJavaParameterType*)(SgJavaParameterType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaParameterType*
SgJavaParameterType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaParameterType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaParameterType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaParameterType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaParameterType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaParameterType)};
        unsigned long positionInPool{localIndex % SgJavaParameterType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaParameterType::pool_size};
        returnPointer = &(((SgJavaParameterType*)(SgJavaParameterType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaParameterType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaParameterType::pools.begin(); block != SgJavaParameterType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaParameterType*>(*block)};
        for (unsigned i = 0; i < SgJavaParameterType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaParameterType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaParameterType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaParameterType* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaParameterType::pools.begin(); block != SgJavaParameterType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaParameterType*>(*block)};
        for (unsigned i = 0; i < SgJavaParameterType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaParameterType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaParameterType::clearMemoryPool() {
    SgJavaParameterType* tempPointer{nullptr};
    if (SgJavaParameterType::pools.empty() == false) {
        auto block{SgJavaParameterType::pools.begin()};
        SgJavaParameterType::next_node = reinterpret_cast<SgJavaParameterType*>(*block);
        for (; block != SgJavaParameterType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaParameterType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaParameterType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaParameterType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaParameterType::pool_size-1]);
        }
    }
}

void
SgJavaParameterType::deleteMemoryPool() {
    for (auto p: SgJavaParameterType::pools) {
        ROSE_FREE(p);
    }
    SgJavaParameterType::next_node = nullptr;
    SgJavaParameterType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaParameterType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaParameterType)};
    for (size_t blockIndex = SgJavaParameterType::pools.size(); (blockIndex * SgJavaParameterType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaParameterType) = %" PRIuPTR " SgJavaParameterType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaParameterType),SgJavaParameterType::pool_size);
        }
#endif

        SgJavaParameterType* pointer{static_cast<SgJavaParameterType*>(ROSE_MALLOC(SgJavaParameterType::pool_size * sizeof(SgJavaParameterType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaParameterType::pool_size * sizeof(SgJavaParameterType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaParameterType::pool_size * sizeof(SgJavaParameterType));
#endif
        SgJavaParameterType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaParameterType::pool_size * sizeof(SgJavaParameterType), V_SgJavaParameterType));

        if (SgJavaParameterType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaParameterType* blkptr{reinterpret_cast<SgJavaParameterType*>(SgJavaParameterType::pools[blockIndex-1])};
                blkptr[SgJavaParameterType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaParameterType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaParameterType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaParameterType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaParameterType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaParameterType::pool_size - 1};
    SgJavaParameterType* testPointer{reinterpret_cast<SgJavaParameterType*>(SgJavaParameterType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaParameterType::pool_size * (SgJavaParameterType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaParameterTypeStroageClass
 * from its counterpart of SgJavaParameterType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaParameterType::initializeStorageClassArray(SgJavaParameterTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaParameterType::pools.begin(); block != SgJavaParameterType::pools.end(); ++block) {
        SgJavaParameterType* pointer{reinterpret_cast<SgJavaParameterType*>(*block)};
        for (unsigned i = 0; i < SgJavaParameterType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 19689 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialTableType that takes its corresponding StorageClass as parameter
 */
SgJovialTableType::SgJovialTableType(const SgJovialTableTypeStorageClass& storageSource)  : SgClassType (storageSource) {

/* #line 19699 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));
     p_dim_info = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dim_info));
    p_rank = storageSource.storageOf_rank;
    p_structure_specifier = storageSource.storageOf_structure_specifier;
    p_bits_per_entry = storageSource.storageOf_bits_per_entry;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialTableType*
SgJovialTableType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialTableType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTableType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTableType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTableType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableType)};
        unsigned long positionInPool{localIndex % SgJovialTableType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialTableType::pool_size};
        returnPointer = &(((SgJovialTableType*)(SgJovialTableType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialTableType*
SgJovialTableType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialTableType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTableType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTableType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTableType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialTableType)};
        unsigned long positionInPool{localIndex % SgJovialTableType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialTableType::pool_size};
        returnPointer = &(((SgJovialTableType*)(SgJovialTableType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialTableType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialTableType::pools.begin(); block != SgJovialTableType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialTableType*>(*block)};
        for (unsigned i = 0; i < SgJovialTableType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialTableType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialTableType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialTableType* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialTableType::pools.begin(); block != SgJovialTableType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialTableType*>(*block)};
        for (unsigned i = 0; i < SgJovialTableType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialTableType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialTableType::clearMemoryPool() {
    SgJovialTableType* tempPointer{nullptr};
    if (SgJovialTableType::pools.empty() == false) {
        auto block{SgJovialTableType::pools.begin()};
        SgJovialTableType::next_node = reinterpret_cast<SgJovialTableType*>(*block);
        for (; block != SgJovialTableType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialTableType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialTableType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialTableType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialTableType::pool_size-1]);
        }
    }
}

void
SgJovialTableType::deleteMemoryPool() {
    for (auto p: SgJovialTableType::pools) {
        ROSE_FREE(p);
    }
    SgJovialTableType::next_node = nullptr;
    SgJovialTableType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialTableType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTableType)};
    for (size_t blockIndex = SgJovialTableType::pools.size(); (blockIndex * SgJovialTableType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTableType) = %" PRIuPTR " SgJovialTableType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTableType),SgJovialTableType::pool_size);
        }
#endif

        SgJovialTableType* pointer{static_cast<SgJovialTableType*>(ROSE_MALLOC(SgJovialTableType::pool_size * sizeof(SgJovialTableType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialTableType::pool_size * sizeof(SgJovialTableType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialTableType::pool_size * sizeof(SgJovialTableType));
#endif
        SgJovialTableType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialTableType::pool_size * sizeof(SgJovialTableType), V_SgJovialTableType));

        if (SgJovialTableType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialTableType* blkptr{reinterpret_cast<SgJovialTableType*>(SgJovialTableType::pools[blockIndex-1])};
                blkptr[SgJovialTableType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialTableType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialTableType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialTableType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialTableType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialTableType::pool_size - 1};
    SgJovialTableType* testPointer{reinterpret_cast<SgJovialTableType*>(SgJovialTableType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialTableType::pool_size * (SgJovialTableType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialTableTypeStroageClass
 * from its counterpart of SgJovialTableType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialTableType::initializeStorageClassArray(SgJovialTableTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialTableType::pools.begin(); block != SgJovialTableType::pools.end(); ++block) {
        SgJovialTableType* pointer{reinterpret_cast<SgJovialTableType*>(*block)};
        for (unsigned i = 0; i < SgJovialTableType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 19921 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEnumType that takes its corresponding StorageClass as parameter
 */
SgEnumType::SgEnumType(const SgEnumTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 19931 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEnumType*
SgEnumType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEnumType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumType)};
        unsigned long positionInPool{localIndex % SgEnumType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumType::pool_size};
        returnPointer = &(((SgEnumType*)(SgEnumType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEnumType*
SgEnumType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEnumType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEnumType)};
        unsigned long positionInPool{localIndex % SgEnumType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumType::pool_size};
        returnPointer = &(((SgEnumType*)(SgEnumType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEnumType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEnumType::pools.begin(); block != SgEnumType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumType*>(*block)};
        for (unsigned i = 0; i < SgEnumType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEnumType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEnumType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEnumType* pointerOfLinkedList{nullptr};
    for (auto block = SgEnumType::pools.begin(); block != SgEnumType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumType*>(*block)};
        for (unsigned i = 0; i < SgEnumType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEnumType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEnumType::clearMemoryPool() {
    SgEnumType* tempPointer{nullptr};
    if (SgEnumType::pools.empty() == false) {
        auto block{SgEnumType::pools.begin()};
        SgEnumType::next_node = reinterpret_cast<SgEnumType*>(*block);
        for (; block != SgEnumType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEnumType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEnumType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEnumType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEnumType::pool_size-1]);
        }
    }
}

void
SgEnumType::deleteMemoryPool() {
    for (auto p: SgEnumType::pools) {
        ROSE_FREE(p);
    }
    SgEnumType::next_node = nullptr;
    SgEnumType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEnumType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumType)};
    for (size_t blockIndex = SgEnumType::pools.size(); (blockIndex * SgEnumType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumType) = %" PRIuPTR " SgEnumType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumType),SgEnumType::pool_size);
        }
#endif

        SgEnumType* pointer{static_cast<SgEnumType*>(ROSE_MALLOC(SgEnumType::pool_size * sizeof(SgEnumType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEnumType::pool_size * sizeof(SgEnumType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEnumType::pool_size * sizeof(SgEnumType));
#endif
        SgEnumType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEnumType::pool_size * sizeof(SgEnumType), V_SgEnumType));

        if (SgEnumType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEnumType* blkptr{reinterpret_cast<SgEnumType*>(SgEnumType::pools[blockIndex-1])};
                blkptr[SgEnumType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEnumType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEnumType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEnumType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEnumType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEnumType::pool_size - 1};
    SgEnumType* testPointer{reinterpret_cast<SgEnumType*>(SgEnumType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEnumType::pool_size * (SgEnumType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEnumTypeStroageClass
 * from its counterpart of SgEnumType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEnumType::initializeStorageClassArray(SgEnumTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEnumType::pools.begin(); block != SgEnumType::pools.end(); ++block) {
        SgEnumType* pointer{reinterpret_cast<SgEnumType*>(*block)};
        for (unsigned i = 0; i < SgEnumType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 20148 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypedefType that takes its corresponding StorageClass as parameter
 */
SgTypedefType::SgTypedefType(const SgTypedefTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 20158 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_parent_scope = (SgSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parent_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypedefType*
SgTypedefType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypedefType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefType)};
        unsigned long positionInPool{localIndex % SgTypedefType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefType::pool_size};
        returnPointer = &(((SgTypedefType*)(SgTypedefType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypedefType*
SgTypedefType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypedefType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypedefType)};
        unsigned long positionInPool{localIndex % SgTypedefType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefType::pool_size};
        returnPointer = &(((SgTypedefType*)(SgTypedefType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypedefType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypedefType::pools.begin(); block != SgTypedefType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefType*>(*block)};
        for (unsigned i = 0; i < SgTypedefType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypedefType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypedefType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypedefType* pointerOfLinkedList{nullptr};
    for (auto block = SgTypedefType::pools.begin(); block != SgTypedefType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefType*>(*block)};
        for (unsigned i = 0; i < SgTypedefType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypedefType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypedefType::clearMemoryPool() {
    SgTypedefType* tempPointer{nullptr};
    if (SgTypedefType::pools.empty() == false) {
        auto block{SgTypedefType::pools.begin()};
        SgTypedefType::next_node = reinterpret_cast<SgTypedefType*>(*block);
        for (; block != SgTypedefType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypedefType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypedefType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypedefType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypedefType::pool_size-1]);
        }
    }
}

void
SgTypedefType::deleteMemoryPool() {
    for (auto p: SgTypedefType::pools) {
        ROSE_FREE(p);
    }
    SgTypedefType::next_node = nullptr;
    SgTypedefType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypedefType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefType)};
    for (size_t blockIndex = SgTypedefType::pools.size(); (blockIndex * SgTypedefType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefType) = %" PRIuPTR " SgTypedefType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefType),SgTypedefType::pool_size);
        }
#endif

        SgTypedefType* pointer{static_cast<SgTypedefType*>(ROSE_MALLOC(SgTypedefType::pool_size * sizeof(SgTypedefType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypedefType::pool_size * sizeof(SgTypedefType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypedefType::pool_size * sizeof(SgTypedefType));
#endif
        SgTypedefType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypedefType::pool_size * sizeof(SgTypedefType), V_SgTypedefType));

        if (SgTypedefType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypedefType* blkptr{reinterpret_cast<SgTypedefType*>(SgTypedefType::pools[blockIndex-1])};
                blkptr[SgTypedefType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypedefType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypedefType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypedefType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypedefType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypedefType::pool_size - 1};
    SgTypedefType* testPointer{reinterpret_cast<SgTypedefType*>(SgTypedefType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypedefType::pool_size * (SgTypedefType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypedefTypeStroageClass
 * from its counterpart of SgTypedefType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypedefType::initializeStorageClassArray(SgTypedefTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypedefType::pools.begin(); block != SgTypedefType::pools.end(); ++block) {
        SgTypedefType* pointer{reinterpret_cast<SgTypedefType*>(*block)};
        for (unsigned i = 0; i < SgTypedefType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 20376 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNonrealType that takes its corresponding StorageClass as parameter
 */
SgNonrealType::SgNonrealType(const SgNonrealTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 20386 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNonrealType*
SgNonrealType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNonrealType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealType)};
        unsigned long positionInPool{localIndex % SgNonrealType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealType::pool_size};
        returnPointer = &(((SgNonrealType*)(SgNonrealType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNonrealType*
SgNonrealType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNonrealType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNonrealType)};
        unsigned long positionInPool{localIndex % SgNonrealType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealType::pool_size};
        returnPointer = &(((SgNonrealType*)(SgNonrealType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNonrealType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNonrealType::pools.begin(); block != SgNonrealType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealType*>(*block)};
        for (unsigned i = 0; i < SgNonrealType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNonrealType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNonrealType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNonrealType* pointerOfLinkedList{nullptr};
    for (auto block = SgNonrealType::pools.begin(); block != SgNonrealType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealType*>(*block)};
        for (unsigned i = 0; i < SgNonrealType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNonrealType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNonrealType::clearMemoryPool() {
    SgNonrealType* tempPointer{nullptr};
    if (SgNonrealType::pools.empty() == false) {
        auto block{SgNonrealType::pools.begin()};
        SgNonrealType::next_node = reinterpret_cast<SgNonrealType*>(*block);
        for (; block != SgNonrealType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNonrealType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNonrealType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNonrealType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNonrealType::pool_size-1]);
        }
    }
}

void
SgNonrealType::deleteMemoryPool() {
    for (auto p: SgNonrealType::pools) {
        ROSE_FREE(p);
    }
    SgNonrealType::next_node = nullptr;
    SgNonrealType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNonrealType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealType)};
    for (size_t blockIndex = SgNonrealType::pools.size(); (blockIndex * SgNonrealType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealType) = %" PRIuPTR " SgNonrealType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealType),SgNonrealType::pool_size);
        }
#endif

        SgNonrealType* pointer{static_cast<SgNonrealType*>(ROSE_MALLOC(SgNonrealType::pool_size * sizeof(SgNonrealType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNonrealType::pool_size * sizeof(SgNonrealType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNonrealType::pool_size * sizeof(SgNonrealType));
#endif
        SgNonrealType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNonrealType::pool_size * sizeof(SgNonrealType), V_SgNonrealType));

        if (SgNonrealType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNonrealType* blkptr{reinterpret_cast<SgNonrealType*>(SgNonrealType::pools[blockIndex-1])};
                blkptr[SgNonrealType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNonrealType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNonrealType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNonrealType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNonrealType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNonrealType::pool_size - 1};
    SgNonrealType* testPointer{reinterpret_cast<SgNonrealType*>(SgNonrealType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNonrealType::pool_size * (SgNonrealType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNonrealTypeStroageClass
 * from its counterpart of SgNonrealType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNonrealType::initializeStorageClassArray(SgNonrealTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNonrealType::pools.begin(); block != SgNonrealType::pools.end(); ++block) {
        SgNonrealType* pointer{reinterpret_cast<SgNonrealType*>(*block)};
        for (unsigned i = 0; i < SgNonrealType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 20603 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaParameterizedType that takes its corresponding StorageClass as parameter
 */
SgJavaParameterizedType::SgJavaParameterizedType(const SgJavaParameterizedTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 20613 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_raw_type = (SgNamedType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_raw_type));
     p_type_list = (SgTemplateParameterList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaParameterizedType*
SgJavaParameterizedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaParameterizedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaParameterizedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaParameterizedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaParameterizedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterizedType)};
        unsigned long positionInPool{localIndex % SgJavaParameterizedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaParameterizedType::pool_size};
        returnPointer = &(((SgJavaParameterizedType*)(SgJavaParameterizedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaParameterizedType*
SgJavaParameterizedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaParameterizedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaParameterizedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaParameterizedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaParameterizedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaParameterizedType)};
        unsigned long positionInPool{localIndex % SgJavaParameterizedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaParameterizedType::pool_size};
        returnPointer = &(((SgJavaParameterizedType*)(SgJavaParameterizedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaParameterizedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaParameterizedType::pools.begin(); block != SgJavaParameterizedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaParameterizedType*>(*block)};
        for (unsigned i = 0; i < SgJavaParameterizedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaParameterizedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaParameterizedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaParameterizedType* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaParameterizedType::pools.begin(); block != SgJavaParameterizedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaParameterizedType*>(*block)};
        for (unsigned i = 0; i < SgJavaParameterizedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaParameterizedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaParameterizedType::clearMemoryPool() {
    SgJavaParameterizedType* tempPointer{nullptr};
    if (SgJavaParameterizedType::pools.empty() == false) {
        auto block{SgJavaParameterizedType::pools.begin()};
        SgJavaParameterizedType::next_node = reinterpret_cast<SgJavaParameterizedType*>(*block);
        for (; block != SgJavaParameterizedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaParameterizedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaParameterizedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaParameterizedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaParameterizedType::pool_size-1]);
        }
    }
}

void
SgJavaParameterizedType::deleteMemoryPool() {
    for (auto p: SgJavaParameterizedType::pools) {
        ROSE_FREE(p);
    }
    SgJavaParameterizedType::next_node = nullptr;
    SgJavaParameterizedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaParameterizedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterizedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaParameterizedType)};
    for (size_t blockIndex = SgJavaParameterizedType::pools.size(); (blockIndex * SgJavaParameterizedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterizedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaParameterizedType) = %" PRIuPTR " SgJavaParameterizedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaParameterizedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaParameterizedType),SgJavaParameterizedType::pool_size);
        }
#endif

        SgJavaParameterizedType* pointer{static_cast<SgJavaParameterizedType*>(ROSE_MALLOC(SgJavaParameterizedType::pool_size * sizeof(SgJavaParameterizedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaParameterizedType::pool_size * sizeof(SgJavaParameterizedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaParameterizedType::pool_size * sizeof(SgJavaParameterizedType));
#endif
        SgJavaParameterizedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaParameterizedType::pool_size * sizeof(SgJavaParameterizedType), V_SgJavaParameterizedType));

        if (SgJavaParameterizedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaParameterizedType* blkptr{reinterpret_cast<SgJavaParameterizedType*>(SgJavaParameterizedType::pools[blockIndex-1])};
                blkptr[SgJavaParameterizedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaParameterizedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaParameterizedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaParameterizedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaParameterizedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaParameterizedType::pool_size - 1};
    SgJavaParameterizedType* testPointer{reinterpret_cast<SgJavaParameterizedType*>(SgJavaParameterizedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaParameterizedType::pool_size * (SgJavaParameterizedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaParameterizedTypeStroageClass
 * from its counterpart of SgJavaParameterizedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaParameterizedType::initializeStorageClassArray(SgJavaParameterizedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaParameterizedType::pools.begin(); block != SgJavaParameterizedType::pools.end(); ++block) {
        SgJavaParameterizedType* pointer{reinterpret_cast<SgJavaParameterizedType*>(*block)};
        for (unsigned i = 0; i < SgJavaParameterizedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 20832 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaQualifiedType that takes its corresponding StorageClass as parameter
 */
SgJavaQualifiedType::SgJavaQualifiedType(const SgJavaQualifiedTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 20842 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_parent_type = (SgNamedType *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parent_type));
     p_type = (SgNamedType *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaQualifiedType*
SgJavaQualifiedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaQualifiedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaQualifiedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaQualifiedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaQualifiedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaQualifiedType)};
        unsigned long positionInPool{localIndex % SgJavaQualifiedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaQualifiedType::pool_size};
        returnPointer = &(((SgJavaQualifiedType*)(SgJavaQualifiedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaQualifiedType*
SgJavaQualifiedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaQualifiedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaQualifiedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaQualifiedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaQualifiedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaQualifiedType)};
        unsigned long positionInPool{localIndex % SgJavaQualifiedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaQualifiedType::pool_size};
        returnPointer = &(((SgJavaQualifiedType*)(SgJavaQualifiedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaQualifiedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaQualifiedType::pools.begin(); block != SgJavaQualifiedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaQualifiedType*>(*block)};
        for (unsigned i = 0; i < SgJavaQualifiedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaQualifiedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaQualifiedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaQualifiedType* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaQualifiedType::pools.begin(); block != SgJavaQualifiedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaQualifiedType*>(*block)};
        for (unsigned i = 0; i < SgJavaQualifiedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaQualifiedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaQualifiedType::clearMemoryPool() {
    SgJavaQualifiedType* tempPointer{nullptr};
    if (SgJavaQualifiedType::pools.empty() == false) {
        auto block{SgJavaQualifiedType::pools.begin()};
        SgJavaQualifiedType::next_node = reinterpret_cast<SgJavaQualifiedType*>(*block);
        for (; block != SgJavaQualifiedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaQualifiedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaQualifiedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaQualifiedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaQualifiedType::pool_size-1]);
        }
    }
}

void
SgJavaQualifiedType::deleteMemoryPool() {
    for (auto p: SgJavaQualifiedType::pools) {
        ROSE_FREE(p);
    }
    SgJavaQualifiedType::next_node = nullptr;
    SgJavaQualifiedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaQualifiedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaQualifiedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaQualifiedType)};
    for (size_t blockIndex = SgJavaQualifiedType::pools.size(); (blockIndex * SgJavaQualifiedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaQualifiedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaQualifiedType) = %" PRIuPTR " SgJavaQualifiedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaQualifiedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaQualifiedType),SgJavaQualifiedType::pool_size);
        }
#endif

        SgJavaQualifiedType* pointer{static_cast<SgJavaQualifiedType*>(ROSE_MALLOC(SgJavaQualifiedType::pool_size * sizeof(SgJavaQualifiedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaQualifiedType::pool_size * sizeof(SgJavaQualifiedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaQualifiedType::pool_size * sizeof(SgJavaQualifiedType));
#endif
        SgJavaQualifiedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaQualifiedType::pool_size * sizeof(SgJavaQualifiedType), V_SgJavaQualifiedType));

        if (SgJavaQualifiedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaQualifiedType* blkptr{reinterpret_cast<SgJavaQualifiedType*>(SgJavaQualifiedType::pools[blockIndex-1])};
                blkptr[SgJavaQualifiedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaQualifiedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaQualifiedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaQualifiedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaQualifiedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaQualifiedType::pool_size - 1};
    SgJavaQualifiedType* testPointer{reinterpret_cast<SgJavaQualifiedType*>(SgJavaQualifiedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaQualifiedType::pool_size * (SgJavaQualifiedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaQualifiedTypeStroageClass
 * from its counterpart of SgJavaQualifiedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaQualifiedType::initializeStorageClassArray(SgJavaQualifiedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaQualifiedType::pools.begin(); block != SgJavaQualifiedType::pools.end(); ++block) {
        SgJavaQualifiedType* pointer{reinterpret_cast<SgJavaQualifiedType*>(*block)};
        for (unsigned i = 0; i < SgJavaQualifiedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 21061 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaWildcardType that takes its corresponding StorageClass as parameter
 */
SgJavaWildcardType::SgJavaWildcardType(const SgJavaWildcardTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 21071 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_bound_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_bound_type));
    p_is_unbound = storageSource.storageOf_is_unbound;
    p_has_extends = storageSource.storageOf_has_extends;
    p_has_super = storageSource.storageOf_has_super;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaWildcardType*
SgJavaWildcardType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaWildcardType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaWildcardType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaWildcardType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaWildcardType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaWildcardType)};
        unsigned long positionInPool{localIndex % SgJavaWildcardType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaWildcardType::pool_size};
        returnPointer = &(((SgJavaWildcardType*)(SgJavaWildcardType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaWildcardType*
SgJavaWildcardType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaWildcardType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaWildcardType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaWildcardType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaWildcardType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaWildcardType)};
        unsigned long positionInPool{localIndex % SgJavaWildcardType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaWildcardType::pool_size};
        returnPointer = &(((SgJavaWildcardType*)(SgJavaWildcardType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaWildcardType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaWildcardType::pools.begin(); block != SgJavaWildcardType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaWildcardType*>(*block)};
        for (unsigned i = 0; i < SgJavaWildcardType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaWildcardType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaWildcardType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaWildcardType* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaWildcardType::pools.begin(); block != SgJavaWildcardType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaWildcardType*>(*block)};
        for (unsigned i = 0; i < SgJavaWildcardType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaWildcardType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaWildcardType::clearMemoryPool() {
    SgJavaWildcardType* tempPointer{nullptr};
    if (SgJavaWildcardType::pools.empty() == false) {
        auto block{SgJavaWildcardType::pools.begin()};
        SgJavaWildcardType::next_node = reinterpret_cast<SgJavaWildcardType*>(*block);
        for (; block != SgJavaWildcardType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaWildcardType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaWildcardType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaWildcardType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaWildcardType::pool_size-1]);
        }
    }
}

void
SgJavaWildcardType::deleteMemoryPool() {
    for (auto p: SgJavaWildcardType::pools) {
        ROSE_FREE(p);
    }
    SgJavaWildcardType::next_node = nullptr;
    SgJavaWildcardType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaWildcardType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaWildcardType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaWildcardType)};
    for (size_t blockIndex = SgJavaWildcardType::pools.size(); (blockIndex * SgJavaWildcardType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaWildcardType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaWildcardType) = %" PRIuPTR " SgJavaWildcardType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaWildcardType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaWildcardType),SgJavaWildcardType::pool_size);
        }
#endif

        SgJavaWildcardType* pointer{static_cast<SgJavaWildcardType*>(ROSE_MALLOC(SgJavaWildcardType::pool_size * sizeof(SgJavaWildcardType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaWildcardType::pool_size * sizeof(SgJavaWildcardType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaWildcardType::pool_size * sizeof(SgJavaWildcardType));
#endif
        SgJavaWildcardType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaWildcardType::pool_size * sizeof(SgJavaWildcardType), V_SgJavaWildcardType));

        if (SgJavaWildcardType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaWildcardType* blkptr{reinterpret_cast<SgJavaWildcardType*>(SgJavaWildcardType::pools[blockIndex-1])};
                blkptr[SgJavaWildcardType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaWildcardType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaWildcardType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaWildcardType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaWildcardType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaWildcardType::pool_size - 1};
    SgJavaWildcardType* testPointer{reinterpret_cast<SgJavaWildcardType*>(SgJavaWildcardType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaWildcardType::pool_size * (SgJavaWildcardType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaWildcardTypeStroageClass
 * from its counterpart of SgJavaWildcardType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaWildcardType::initializeStorageClassArray(SgJavaWildcardTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaWildcardType::pools.begin(); block != SgJavaWildcardType::pools.end(); ++block) {
        SgJavaWildcardType* pointer{reinterpret_cast<SgJavaWildcardType*>(*block)};
        for (unsigned i = 0; i < SgJavaWildcardType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 21292 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskType that takes its corresponding StorageClass as parameter
 */
SgAdaTaskType::SgAdaTaskType(const SgAdaTaskTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 21302 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskType*
SgAdaTaskType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskType)};
        unsigned long positionInPool{localIndex % SgAdaTaskType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskType::pool_size};
        returnPointer = &(((SgAdaTaskType*)(SgAdaTaskType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskType*
SgAdaTaskType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskType)};
        unsigned long positionInPool{localIndex % SgAdaTaskType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskType::pool_size};
        returnPointer = &(((SgAdaTaskType*)(SgAdaTaskType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskType::pools.begin(); block != SgAdaTaskType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskType*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskType::pools.begin(); block != SgAdaTaskType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskType*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskType::clearMemoryPool() {
    SgAdaTaskType* tempPointer{nullptr};
    if (SgAdaTaskType::pools.empty() == false) {
        auto block{SgAdaTaskType::pools.begin()};
        SgAdaTaskType::next_node = reinterpret_cast<SgAdaTaskType*>(*block);
        for (; block != SgAdaTaskType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskType::pool_size-1]);
        }
    }
}

void
SgAdaTaskType::deleteMemoryPool() {
    for (auto p: SgAdaTaskType::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskType::next_node = nullptr;
    SgAdaTaskType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskType)};
    for (size_t blockIndex = SgAdaTaskType::pools.size(); (blockIndex * SgAdaTaskType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskType) = %" PRIuPTR " SgAdaTaskType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskType),SgAdaTaskType::pool_size);
        }
#endif

        SgAdaTaskType* pointer{static_cast<SgAdaTaskType*>(ROSE_MALLOC(SgAdaTaskType::pool_size * sizeof(SgAdaTaskType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskType::pool_size * sizeof(SgAdaTaskType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskType::pool_size * sizeof(SgAdaTaskType));
#endif
        SgAdaTaskType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskType::pool_size * sizeof(SgAdaTaskType), V_SgAdaTaskType));

        if (SgAdaTaskType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskType* blkptr{reinterpret_cast<SgAdaTaskType*>(SgAdaTaskType::pools[blockIndex-1])};
                blkptr[SgAdaTaskType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskType::pool_size - 1};
    SgAdaTaskType* testPointer{reinterpret_cast<SgAdaTaskType*>(SgAdaTaskType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskType::pool_size * (SgAdaTaskType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskTypeStroageClass
 * from its counterpart of SgAdaTaskType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskType::initializeStorageClassArray(SgAdaTaskTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskType::pools.begin(); block != SgAdaTaskType::pools.end(); ++block) {
        SgAdaTaskType* pointer{reinterpret_cast<SgAdaTaskType*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 21519 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedType that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedType::SgAdaProtectedType(const SgAdaProtectedTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 21529 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedType*
SgAdaProtectedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedType)};
        unsigned long positionInPool{localIndex % SgAdaProtectedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedType::pool_size};
        returnPointer = &(((SgAdaProtectedType*)(SgAdaProtectedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedType*
SgAdaProtectedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedType)};
        unsigned long positionInPool{localIndex % SgAdaProtectedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedType::pool_size};
        returnPointer = &(((SgAdaProtectedType*)(SgAdaProtectedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedType::pools.begin(); block != SgAdaProtectedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedType*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedType::pools.begin(); block != SgAdaProtectedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedType*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedType::clearMemoryPool() {
    SgAdaProtectedType* tempPointer{nullptr};
    if (SgAdaProtectedType::pools.empty() == false) {
        auto block{SgAdaProtectedType::pools.begin()};
        SgAdaProtectedType::next_node = reinterpret_cast<SgAdaProtectedType*>(*block);
        for (; block != SgAdaProtectedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedType::pool_size-1]);
        }
    }
}

void
SgAdaProtectedType::deleteMemoryPool() {
    for (auto p: SgAdaProtectedType::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedType::next_node = nullptr;
    SgAdaProtectedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedType)};
    for (size_t blockIndex = SgAdaProtectedType::pools.size(); (blockIndex * SgAdaProtectedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedType) = %" PRIuPTR " SgAdaProtectedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedType),SgAdaProtectedType::pool_size);
        }
#endif

        SgAdaProtectedType* pointer{static_cast<SgAdaProtectedType*>(ROSE_MALLOC(SgAdaProtectedType::pool_size * sizeof(SgAdaProtectedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedType::pool_size * sizeof(SgAdaProtectedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedType::pool_size * sizeof(SgAdaProtectedType));
#endif
        SgAdaProtectedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedType::pool_size * sizeof(SgAdaProtectedType), V_SgAdaProtectedType));

        if (SgAdaProtectedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedType* blkptr{reinterpret_cast<SgAdaProtectedType*>(SgAdaProtectedType::pools[blockIndex-1])};
                blkptr[SgAdaProtectedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedType::pool_size - 1};
    SgAdaProtectedType* testPointer{reinterpret_cast<SgAdaProtectedType*>(SgAdaProtectedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedType::pool_size * (SgAdaProtectedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedTypeStroageClass
 * from its counterpart of SgAdaProtectedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedType::initializeStorageClassArray(SgAdaProtectedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedType::pools.begin(); block != SgAdaProtectedType::pools.end(); ++block) {
        SgAdaProtectedType* pointer{reinterpret_cast<SgAdaProtectedType*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 21746 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaFormalType that takes its corresponding StorageClass as parameter
 */
SgAdaFormalType::SgAdaFormalType(const SgAdaFormalTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 21756 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_is_private = storageSource.storageOf_is_private;
     p_formal_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_formal_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaFormalType*
SgAdaFormalType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaFormalType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalType)};
        unsigned long positionInPool{localIndex % SgAdaFormalType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalType::pool_size};
        returnPointer = &(((SgAdaFormalType*)(SgAdaFormalType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaFormalType*
SgAdaFormalType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaFormalType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaFormalType)};
        unsigned long positionInPool{localIndex % SgAdaFormalType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalType::pool_size};
        returnPointer = &(((SgAdaFormalType*)(SgAdaFormalType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaFormalType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaFormalType::pools.begin(); block != SgAdaFormalType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalType*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaFormalType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaFormalType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaFormalType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaFormalType::pools.begin(); block != SgAdaFormalType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalType*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaFormalType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaFormalType::clearMemoryPool() {
    SgAdaFormalType* tempPointer{nullptr};
    if (SgAdaFormalType::pools.empty() == false) {
        auto block{SgAdaFormalType::pools.begin()};
        SgAdaFormalType::next_node = reinterpret_cast<SgAdaFormalType*>(*block);
        for (; block != SgAdaFormalType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaFormalType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaFormalType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaFormalType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaFormalType::pool_size-1]);
        }
    }
}

void
SgAdaFormalType::deleteMemoryPool() {
    for (auto p: SgAdaFormalType::pools) {
        ROSE_FREE(p);
    }
    SgAdaFormalType::next_node = nullptr;
    SgAdaFormalType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaFormalType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalType)};
    for (size_t blockIndex = SgAdaFormalType::pools.size(); (blockIndex * SgAdaFormalType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalType) = %" PRIuPTR " SgAdaFormalType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalType),SgAdaFormalType::pool_size);
        }
#endif

        SgAdaFormalType* pointer{static_cast<SgAdaFormalType*>(ROSE_MALLOC(SgAdaFormalType::pool_size * sizeof(SgAdaFormalType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaFormalType::pool_size * sizeof(SgAdaFormalType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaFormalType::pool_size * sizeof(SgAdaFormalType));
#endif
        SgAdaFormalType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaFormalType::pool_size * sizeof(SgAdaFormalType), V_SgAdaFormalType));

        if (SgAdaFormalType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaFormalType* blkptr{reinterpret_cast<SgAdaFormalType*>(SgAdaFormalType::pools[blockIndex-1])};
                blkptr[SgAdaFormalType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaFormalType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaFormalType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaFormalType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaFormalType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaFormalType::pool_size - 1};
    SgAdaFormalType* testPointer{reinterpret_cast<SgAdaFormalType*>(SgAdaFormalType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaFormalType::pool_size * (SgAdaFormalType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaFormalTypeStroageClass
 * from its counterpart of SgAdaFormalType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaFormalType::initializeStorageClassArray(SgAdaFormalTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaFormalType::pools.begin(); block != SgAdaFormalType::pools.end(); ++block) {
        SgAdaFormalType* pointer{reinterpret_cast<SgAdaFormalType*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 21975 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDiscriminatedType that takes its corresponding StorageClass as parameter
 */
SgAdaDiscriminatedType::SgAdaDiscriminatedType(const SgAdaDiscriminatedTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 21985 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDiscriminatedType*
SgAdaDiscriminatedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDiscriminatedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminatedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminatedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminatedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedType)};
        unsigned long positionInPool{localIndex % SgAdaDiscriminatedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscriminatedType::pool_size};
        returnPointer = &(((SgAdaDiscriminatedType*)(SgAdaDiscriminatedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDiscriminatedType*
SgAdaDiscriminatedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDiscriminatedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminatedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminatedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminatedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDiscriminatedType)};
        unsigned long positionInPool{localIndex % SgAdaDiscriminatedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscriminatedType::pool_size};
        returnPointer = &(((SgAdaDiscriminatedType*)(SgAdaDiscriminatedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDiscriminatedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDiscriminatedType::pools.begin(); block != SgAdaDiscriminatedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscriminatedType*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminatedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDiscriminatedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDiscriminatedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDiscriminatedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDiscriminatedType::pools.begin(); block != SgAdaDiscriminatedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscriminatedType*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminatedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDiscriminatedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDiscriminatedType::clearMemoryPool() {
    SgAdaDiscriminatedType* tempPointer{nullptr};
    if (SgAdaDiscriminatedType::pools.empty() == false) {
        auto block{SgAdaDiscriminatedType::pools.begin()};
        SgAdaDiscriminatedType::next_node = reinterpret_cast<SgAdaDiscriminatedType*>(*block);
        for (; block != SgAdaDiscriminatedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDiscriminatedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDiscriminatedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDiscriminatedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDiscriminatedType::pool_size-1]);
        }
    }
}

void
SgAdaDiscriminatedType::deleteMemoryPool() {
    for (auto p: SgAdaDiscriminatedType::pools) {
        ROSE_FREE(p);
    }
    SgAdaDiscriminatedType::next_node = nullptr;
    SgAdaDiscriminatedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDiscriminatedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminatedType)};
    for (size_t blockIndex = SgAdaDiscriminatedType::pools.size(); (blockIndex * SgAdaDiscriminatedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminatedType) = %" PRIuPTR " SgAdaDiscriminatedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminatedType),SgAdaDiscriminatedType::pool_size);
        }
#endif

        SgAdaDiscriminatedType* pointer{static_cast<SgAdaDiscriminatedType*>(ROSE_MALLOC(SgAdaDiscriminatedType::pool_size * sizeof(SgAdaDiscriminatedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDiscriminatedType::pool_size * sizeof(SgAdaDiscriminatedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDiscriminatedType::pool_size * sizeof(SgAdaDiscriminatedType));
#endif
        SgAdaDiscriminatedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDiscriminatedType::pool_size * sizeof(SgAdaDiscriminatedType), V_SgAdaDiscriminatedType));

        if (SgAdaDiscriminatedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDiscriminatedType* blkptr{reinterpret_cast<SgAdaDiscriminatedType*>(SgAdaDiscriminatedType::pools[blockIndex-1])};
                blkptr[SgAdaDiscriminatedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDiscriminatedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDiscriminatedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDiscriminatedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDiscriminatedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDiscriminatedType::pool_size - 1};
    SgAdaDiscriminatedType* testPointer{reinterpret_cast<SgAdaDiscriminatedType*>(SgAdaDiscriminatedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDiscriminatedType::pool_size * (SgAdaDiscriminatedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDiscriminatedTypeStroageClass
 * from its counterpart of SgAdaDiscriminatedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDiscriminatedType::initializeStorageClassArray(SgAdaDiscriminatedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDiscriminatedType::pools.begin(); block != SgAdaDiscriminatedType::pools.end(); ++block) {
        SgAdaDiscriminatedType* pointer{reinterpret_cast<SgAdaDiscriminatedType*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminatedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 22202 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgScopedType that takes its corresponding StorageClass as parameter
 */
SgScopedType::SgScopedType(const SgScopedTypeStorageClass& storageSource)  : SgNamedType (storageSource) {

/* #line 22212 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgScopedType*
SgScopedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgScopedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedType)};
        unsigned long positionInPool{localIndex % SgScopedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopedType::pool_size};
        returnPointer = &(((SgScopedType*)(SgScopedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgScopedType*
SgScopedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgScopedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgScopedType)};
        unsigned long positionInPool{localIndex % SgScopedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopedType::pool_size};
        returnPointer = &(((SgScopedType*)(SgScopedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgScopedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgScopedType::pools.begin(); block != SgScopedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopedType*>(*block)};
        for (unsigned i = 0; i < SgScopedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgScopedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgScopedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgScopedType* pointerOfLinkedList{nullptr};
    for (auto block = SgScopedType::pools.begin(); block != SgScopedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopedType*>(*block)};
        for (unsigned i = 0; i < SgScopedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgScopedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgScopedType::clearMemoryPool() {
    SgScopedType* tempPointer{nullptr};
    if (SgScopedType::pools.empty() == false) {
        auto block{SgScopedType::pools.begin()};
        SgScopedType::next_node = reinterpret_cast<SgScopedType*>(*block);
        for (; block != SgScopedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgScopedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgScopedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgScopedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgScopedType::pool_size-1]);
        }
    }
}

void
SgScopedType::deleteMemoryPool() {
    for (auto p: SgScopedType::pools) {
        ROSE_FREE(p);
    }
    SgScopedType::next_node = nullptr;
    SgScopedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgScopedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopedType)};
    for (size_t blockIndex = SgScopedType::pools.size(); (blockIndex * SgScopedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopedType) = %" PRIuPTR " SgScopedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopedType),SgScopedType::pool_size);
        }
#endif

        SgScopedType* pointer{static_cast<SgScopedType*>(ROSE_MALLOC(SgScopedType::pool_size * sizeof(SgScopedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgScopedType::pool_size * sizeof(SgScopedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgScopedType::pool_size * sizeof(SgScopedType));
#endif
        SgScopedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgScopedType::pool_size * sizeof(SgScopedType), V_SgScopedType));

        if (SgScopedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgScopedType* blkptr{reinterpret_cast<SgScopedType*>(SgScopedType::pools[blockIndex-1])};
                blkptr[SgScopedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgScopedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgScopedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgScopedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgScopedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgScopedType::pool_size - 1};
    SgScopedType* testPointer{reinterpret_cast<SgScopedType*>(SgScopedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgScopedType::pool_size * (SgScopedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgScopedTypeStroageClass
 * from its counterpart of SgScopedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgScopedType::initializeStorageClassArray(SgScopedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgScopedType::pools.begin(); block != SgScopedType::pools.end(); ++block) {
        SgScopedType* pointer{reinterpret_cast<SgScopedType*>(*block)};
        for (unsigned i = 0; i < SgScopedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 22429 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgModifierType that takes its corresponding StorageClass as parameter
 */
SgModifierType::SgModifierType(const SgModifierTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 22439 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));
    p_typeModifier = SgTypeModifier ( storageSource.storageOf_typeModifier);
     p_frontend_type_reference = const_cast<char* >( storageSource.storageOf_frontend_type_reference.rebuildDataStoredInEasyStorageClass());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgModifierType*
SgModifierType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgModifierType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifierType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifierType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModifierType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierType)};
        unsigned long positionInPool{localIndex % SgModifierType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModifierType::pool_size};
        returnPointer = &(((SgModifierType*)(SgModifierType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgModifierType*
SgModifierType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgModifierType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifierType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifierType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModifierType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgModifierType)};
        unsigned long positionInPool{localIndex % SgModifierType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModifierType::pool_size};
        returnPointer = &(((SgModifierType*)(SgModifierType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgModifierType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgModifierType::pools.begin(); block != SgModifierType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModifierType*>(*block)};
        for (unsigned i = 0; i < SgModifierType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgModifierType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgModifierType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgModifierType* pointerOfLinkedList{nullptr};
    for (auto block = SgModifierType::pools.begin(); block != SgModifierType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModifierType*>(*block)};
        for (unsigned i = 0; i < SgModifierType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgModifierType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgModifierType::clearMemoryPool() {
    SgModifierType* tempPointer{nullptr};
    if (SgModifierType::pools.empty() == false) {
        auto block{SgModifierType::pools.begin()};
        SgModifierType::next_node = reinterpret_cast<SgModifierType*>(*block);
        for (; block != SgModifierType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgModifierType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgModifierType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgModifierType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgModifierType::pool_size-1]);
        }
    }
}

void
SgModifierType::deleteMemoryPool() {
    for (auto p: SgModifierType::pools) {
        ROSE_FREE(p);
    }
    SgModifierType::next_node = nullptr;
    SgModifierType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgModifierType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifierType)};
    for (size_t blockIndex = SgModifierType::pools.size(); (blockIndex * SgModifierType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifierType) = %" PRIuPTR " SgModifierType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgModifierType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgModifierType),SgModifierType::pool_size);
        }
#endif

        SgModifierType* pointer{static_cast<SgModifierType*>(ROSE_MALLOC(SgModifierType::pool_size * sizeof(SgModifierType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgModifierType::pool_size * sizeof(SgModifierType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgModifierType::pool_size * sizeof(SgModifierType));
#endif
        SgModifierType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgModifierType::pool_size * sizeof(SgModifierType), V_SgModifierType));

        if (SgModifierType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgModifierType* blkptr{reinterpret_cast<SgModifierType*>(SgModifierType::pools[blockIndex-1])};
                blkptr[SgModifierType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgModifierType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgModifierType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgModifierType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgModifierType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgModifierType::pool_size - 1};
    SgModifierType* testPointer{reinterpret_cast<SgModifierType*>(SgModifierType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgModifierType::pool_size * (SgModifierType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgModifierTypeStroageClass
 * from its counterpart of SgModifierType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgModifierType::initializeStorageClassArray(SgModifierTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgModifierType::pools.begin(); block != SgModifierType::pools.end(); ++block) {
        SgModifierType* pointer{reinterpret_cast<SgModifierType*>(*block)};
        for (unsigned i = 0; i < SgModifierType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 22659 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionType that takes its corresponding StorageClass as parameter
 */
SgFunctionType::SgFunctionType(const SgFunctionTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 22669 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_return_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_return_type));
    p_has_ellipses = storageSource.storageOf_has_ellipses;
     p_argument_list = (SgFunctionParameterTypeList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_argument_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionType*
SgFunctionType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionType)};
        unsigned long positionInPool{localIndex % SgFunctionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionType::pool_size};
        returnPointer = &(((SgFunctionType*)(SgFunctionType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionType*
SgFunctionType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionType)};
        unsigned long positionInPool{localIndex % SgFunctionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionType::pool_size};
        returnPointer = &(((SgFunctionType*)(SgFunctionType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionType::pools.begin(); block != SgFunctionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionType*>(*block)};
        for (unsigned i = 0; i < SgFunctionType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionType* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionType::pools.begin(); block != SgFunctionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionType*>(*block)};
        for (unsigned i = 0; i < SgFunctionType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionType::clearMemoryPool() {
    SgFunctionType* tempPointer{nullptr};
    if (SgFunctionType::pools.empty() == false) {
        auto block{SgFunctionType::pools.begin()};
        SgFunctionType::next_node = reinterpret_cast<SgFunctionType*>(*block);
        for (; block != SgFunctionType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionType::pool_size-1]);
        }
    }
}

void
SgFunctionType::deleteMemoryPool() {
    for (auto p: SgFunctionType::pools) {
        ROSE_FREE(p);
    }
    SgFunctionType::next_node = nullptr;
    SgFunctionType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionType)};
    for (size_t blockIndex = SgFunctionType::pools.size(); (blockIndex * SgFunctionType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionType) = %" PRIuPTR " SgFunctionType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionType),SgFunctionType::pool_size);
        }
#endif

        SgFunctionType* pointer{static_cast<SgFunctionType*>(ROSE_MALLOC(SgFunctionType::pool_size * sizeof(SgFunctionType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionType::pool_size * sizeof(SgFunctionType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionType::pool_size * sizeof(SgFunctionType));
#endif
        SgFunctionType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionType::pool_size * sizeof(SgFunctionType), V_SgFunctionType));

        if (SgFunctionType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionType* blkptr{reinterpret_cast<SgFunctionType*>(SgFunctionType::pools[blockIndex-1])};
                blkptr[SgFunctionType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionType::pool_size - 1};
    SgFunctionType* testPointer{reinterpret_cast<SgFunctionType*>(SgFunctionType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionType::pool_size * (SgFunctionType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionTypeStroageClass
 * from its counterpart of SgFunctionType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionType::initializeStorageClassArray(SgFunctionTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionType::pools.begin(); block != SgFunctionType::pools.end(); ++block) {
        SgFunctionType* pointer{reinterpret_cast<SgFunctionType*>(*block)};
        for (unsigned i = 0; i < SgFunctionType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 22889 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMemberFunctionType that takes its corresponding StorageClass as parameter
 */
SgMemberFunctionType::SgMemberFunctionType(const SgMemberFunctionTypeStorageClass& storageSource)  : SgFunctionType (storageSource) {

/* #line 22899 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_class_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_class_type));
    p_mfunc_specifier = storageSource.storageOf_mfunc_specifier;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMemberFunctionType*
SgMemberFunctionType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMemberFunctionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionType)};
        unsigned long positionInPool{localIndex % SgMemberFunctionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionType::pool_size};
        returnPointer = &(((SgMemberFunctionType*)(SgMemberFunctionType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMemberFunctionType*
SgMemberFunctionType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMemberFunctionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMemberFunctionType)};
        unsigned long positionInPool{localIndex % SgMemberFunctionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionType::pool_size};
        returnPointer = &(((SgMemberFunctionType*)(SgMemberFunctionType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMemberFunctionType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMemberFunctionType::pools.begin(); block != SgMemberFunctionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionType*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMemberFunctionType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMemberFunctionType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMemberFunctionType* pointerOfLinkedList{nullptr};
    for (auto block = SgMemberFunctionType::pools.begin(); block != SgMemberFunctionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionType*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMemberFunctionType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMemberFunctionType::clearMemoryPool() {
    SgMemberFunctionType* tempPointer{nullptr};
    if (SgMemberFunctionType::pools.empty() == false) {
        auto block{SgMemberFunctionType::pools.begin()};
        SgMemberFunctionType::next_node = reinterpret_cast<SgMemberFunctionType*>(*block);
        for (; block != SgMemberFunctionType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMemberFunctionType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMemberFunctionType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMemberFunctionType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMemberFunctionType::pool_size-1]);
        }
    }
}

void
SgMemberFunctionType::deleteMemoryPool() {
    for (auto p: SgMemberFunctionType::pools) {
        ROSE_FREE(p);
    }
    SgMemberFunctionType::next_node = nullptr;
    SgMemberFunctionType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMemberFunctionType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionType)};
    for (size_t blockIndex = SgMemberFunctionType::pools.size(); (blockIndex * SgMemberFunctionType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionType) = %" PRIuPTR " SgMemberFunctionType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionType),SgMemberFunctionType::pool_size);
        }
#endif

        SgMemberFunctionType* pointer{static_cast<SgMemberFunctionType*>(ROSE_MALLOC(SgMemberFunctionType::pool_size * sizeof(SgMemberFunctionType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMemberFunctionType::pool_size * sizeof(SgMemberFunctionType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMemberFunctionType::pool_size * sizeof(SgMemberFunctionType));
#endif
        SgMemberFunctionType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMemberFunctionType::pool_size * sizeof(SgMemberFunctionType), V_SgMemberFunctionType));

        if (SgMemberFunctionType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMemberFunctionType* blkptr{reinterpret_cast<SgMemberFunctionType*>(SgMemberFunctionType::pools[blockIndex-1])};
                blkptr[SgMemberFunctionType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMemberFunctionType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMemberFunctionType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMemberFunctionType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMemberFunctionType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMemberFunctionType::pool_size - 1};
    SgMemberFunctionType* testPointer{reinterpret_cast<SgMemberFunctionType*>(SgMemberFunctionType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMemberFunctionType::pool_size * (SgMemberFunctionType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMemberFunctionTypeStroageClass
 * from its counterpart of SgMemberFunctionType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMemberFunctionType::initializeStorageClassArray(SgMemberFunctionTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMemberFunctionType::pools.begin(); block != SgMemberFunctionType::pools.end(); ++block) {
        SgMemberFunctionType* pointer{reinterpret_cast<SgMemberFunctionType*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 23118 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPartialFunctionType that takes its corresponding StorageClass as parameter
 */
SgPartialFunctionType::SgPartialFunctionType(const SgPartialFunctionTypeStorageClass& storageSource)  : SgMemberFunctionType (storageSource) {

/* #line 23128 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPartialFunctionType*
SgPartialFunctionType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPartialFunctionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPartialFunctionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPartialFunctionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPartialFunctionType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionType)};
        unsigned long positionInPool{localIndex % SgPartialFunctionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPartialFunctionType::pool_size};
        returnPointer = &(((SgPartialFunctionType*)(SgPartialFunctionType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPartialFunctionType*
SgPartialFunctionType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPartialFunctionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPartialFunctionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPartialFunctionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPartialFunctionType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPartialFunctionType)};
        unsigned long positionInPool{localIndex % SgPartialFunctionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPartialFunctionType::pool_size};
        returnPointer = &(((SgPartialFunctionType*)(SgPartialFunctionType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPartialFunctionType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPartialFunctionType::pools.begin(); block != SgPartialFunctionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPartialFunctionType*>(*block)};
        for (unsigned i = 0; i < SgPartialFunctionType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPartialFunctionType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPartialFunctionType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPartialFunctionType* pointerOfLinkedList{nullptr};
    for (auto block = SgPartialFunctionType::pools.begin(); block != SgPartialFunctionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPartialFunctionType*>(*block)};
        for (unsigned i = 0; i < SgPartialFunctionType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPartialFunctionType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPartialFunctionType::clearMemoryPool() {
    SgPartialFunctionType* tempPointer{nullptr};
    if (SgPartialFunctionType::pools.empty() == false) {
        auto block{SgPartialFunctionType::pools.begin()};
        SgPartialFunctionType::next_node = reinterpret_cast<SgPartialFunctionType*>(*block);
        for (; block != SgPartialFunctionType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPartialFunctionType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPartialFunctionType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPartialFunctionType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPartialFunctionType::pool_size-1]);
        }
    }
}

void
SgPartialFunctionType::deleteMemoryPool() {
    for (auto p: SgPartialFunctionType::pools) {
        ROSE_FREE(p);
    }
    SgPartialFunctionType::next_node = nullptr;
    SgPartialFunctionType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPartialFunctionType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPartialFunctionType)};
    for (size_t blockIndex = SgPartialFunctionType::pools.size(); (blockIndex * SgPartialFunctionType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPartialFunctionType) = %" PRIuPTR " SgPartialFunctionType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPartialFunctionType),SgPartialFunctionType::pool_size);
        }
#endif

        SgPartialFunctionType* pointer{static_cast<SgPartialFunctionType*>(ROSE_MALLOC(SgPartialFunctionType::pool_size * sizeof(SgPartialFunctionType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPartialFunctionType::pool_size * sizeof(SgPartialFunctionType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPartialFunctionType::pool_size * sizeof(SgPartialFunctionType));
#endif
        SgPartialFunctionType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPartialFunctionType::pool_size * sizeof(SgPartialFunctionType), V_SgPartialFunctionType));

        if (SgPartialFunctionType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPartialFunctionType* blkptr{reinterpret_cast<SgPartialFunctionType*>(SgPartialFunctionType::pools[blockIndex-1])};
                blkptr[SgPartialFunctionType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPartialFunctionType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPartialFunctionType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPartialFunctionType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPartialFunctionType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPartialFunctionType::pool_size - 1};
    SgPartialFunctionType* testPointer{reinterpret_cast<SgPartialFunctionType*>(SgPartialFunctionType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPartialFunctionType::pool_size * (SgPartialFunctionType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPartialFunctionTypeStroageClass
 * from its counterpart of SgPartialFunctionType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPartialFunctionType::initializeStorageClassArray(SgPartialFunctionTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPartialFunctionType::pools.begin(); block != SgPartialFunctionType::pools.end(); ++block) {
        SgPartialFunctionType* pointer{reinterpret_cast<SgPartialFunctionType*>(*block)};
        for (unsigned i = 0; i < SgPartialFunctionType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 23345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPartialFunctionModifierType that takes its corresponding StorageClass as parameter
 */
SgPartialFunctionModifierType::SgPartialFunctionModifierType(const SgPartialFunctionModifierTypeStorageClass& storageSource)  : SgPartialFunctionType (storageSource) {

/* #line 23355 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPartialFunctionModifierType*
SgPartialFunctionModifierType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPartialFunctionModifierType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPartialFunctionModifierType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPartialFunctionModifierType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPartialFunctionModifierType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionModifierType)};
        unsigned long positionInPool{localIndex % SgPartialFunctionModifierType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPartialFunctionModifierType::pool_size};
        returnPointer = &(((SgPartialFunctionModifierType*)(SgPartialFunctionModifierType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPartialFunctionModifierType*
SgPartialFunctionModifierType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPartialFunctionModifierType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPartialFunctionModifierType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPartialFunctionModifierType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPartialFunctionModifierType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPartialFunctionModifierType)};
        unsigned long positionInPool{localIndex % SgPartialFunctionModifierType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPartialFunctionModifierType::pool_size};
        returnPointer = &(((SgPartialFunctionModifierType*)(SgPartialFunctionModifierType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPartialFunctionModifierType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPartialFunctionModifierType::pools.begin(); block != SgPartialFunctionModifierType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPartialFunctionModifierType*>(*block)};
        for (unsigned i = 0; i < SgPartialFunctionModifierType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPartialFunctionModifierType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPartialFunctionModifierType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPartialFunctionModifierType* pointerOfLinkedList{nullptr};
    for (auto block = SgPartialFunctionModifierType::pools.begin(); block != SgPartialFunctionModifierType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPartialFunctionModifierType*>(*block)};
        for (unsigned i = 0; i < SgPartialFunctionModifierType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPartialFunctionModifierType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPartialFunctionModifierType::clearMemoryPool() {
    SgPartialFunctionModifierType* tempPointer{nullptr};
    if (SgPartialFunctionModifierType::pools.empty() == false) {
        auto block{SgPartialFunctionModifierType::pools.begin()};
        SgPartialFunctionModifierType::next_node = reinterpret_cast<SgPartialFunctionModifierType*>(*block);
        for (; block != SgPartialFunctionModifierType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPartialFunctionModifierType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPartialFunctionModifierType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPartialFunctionModifierType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPartialFunctionModifierType::pool_size-1]);
        }
    }
}

void
SgPartialFunctionModifierType::deleteMemoryPool() {
    for (auto p: SgPartialFunctionModifierType::pools) {
        ROSE_FREE(p);
    }
    SgPartialFunctionModifierType::next_node = nullptr;
    SgPartialFunctionModifierType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPartialFunctionModifierType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionModifierType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPartialFunctionModifierType)};
    for (size_t blockIndex = SgPartialFunctionModifierType::pools.size(); (blockIndex * SgPartialFunctionModifierType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionModifierType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPartialFunctionModifierType) = %" PRIuPTR " SgPartialFunctionModifierType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPartialFunctionModifierType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPartialFunctionModifierType),SgPartialFunctionModifierType::pool_size);
        }
#endif

        SgPartialFunctionModifierType* pointer{static_cast<SgPartialFunctionModifierType*>(ROSE_MALLOC(SgPartialFunctionModifierType::pool_size * sizeof(SgPartialFunctionModifierType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPartialFunctionModifierType::pool_size * sizeof(SgPartialFunctionModifierType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPartialFunctionModifierType::pool_size * sizeof(SgPartialFunctionModifierType));
#endif
        SgPartialFunctionModifierType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPartialFunctionModifierType::pool_size * sizeof(SgPartialFunctionModifierType), V_SgPartialFunctionModifierType));

        if (SgPartialFunctionModifierType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPartialFunctionModifierType* blkptr{reinterpret_cast<SgPartialFunctionModifierType*>(SgPartialFunctionModifierType::pools[blockIndex-1])};
                blkptr[SgPartialFunctionModifierType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPartialFunctionModifierType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPartialFunctionModifierType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPartialFunctionModifierType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPartialFunctionModifierType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPartialFunctionModifierType::pool_size - 1};
    SgPartialFunctionModifierType* testPointer{reinterpret_cast<SgPartialFunctionModifierType*>(SgPartialFunctionModifierType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPartialFunctionModifierType::pool_size * (SgPartialFunctionModifierType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPartialFunctionModifierTypeStroageClass
 * from its counterpart of SgPartialFunctionModifierType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPartialFunctionModifierType::initializeStorageClassArray(SgPartialFunctionModifierTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPartialFunctionModifierType::pools.begin(); block != SgPartialFunctionModifierType::pools.end(); ++block) {
        SgPartialFunctionModifierType* pointer{reinterpret_cast<SgPartialFunctionModifierType*>(*block)};
        for (unsigned i = 0; i < SgPartialFunctionModifierType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 23572 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgArrayType that takes its corresponding StorageClass as parameter
 */
SgArrayType::SgArrayType(const SgArrayTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 23582 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));
     p_index = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_index));
     p_dim_info = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dim_info));
    p_rank = storageSource.storageOf_rank;
    p_number_of_elements = storageSource.storageOf_number_of_elements;
    p_is_variable_length_array = storageSource.storageOf_is_variable_length_array;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgArrayType*
SgArrayType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgArrayType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrayType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrayType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrayType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgArrayType)};
        unsigned long positionInPool{localIndex % SgArrayType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArrayType::pool_size};
        returnPointer = &(((SgArrayType*)(SgArrayType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgArrayType*
SgArrayType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgArrayType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrayType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrayType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrayType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgArrayType)};
        unsigned long positionInPool{localIndex % SgArrayType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArrayType::pool_size};
        returnPointer = &(((SgArrayType*)(SgArrayType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgArrayType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgArrayType::pools.begin(); block != SgArrayType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArrayType*>(*block)};
        for (unsigned i = 0; i < SgArrayType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgArrayType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgArrayType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgArrayType* pointerOfLinkedList{nullptr};
    for (auto block = SgArrayType::pools.begin(); block != SgArrayType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArrayType*>(*block)};
        for (unsigned i = 0; i < SgArrayType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgArrayType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgArrayType::clearMemoryPool() {
    SgArrayType* tempPointer{nullptr};
    if (SgArrayType::pools.empty() == false) {
        auto block{SgArrayType::pools.begin()};
        SgArrayType::next_node = reinterpret_cast<SgArrayType*>(*block);
        for (; block != SgArrayType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgArrayType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgArrayType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgArrayType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgArrayType::pool_size-1]);
        }
    }
}

void
SgArrayType::deleteMemoryPool() {
    for (auto p: SgArrayType::pools) {
        ROSE_FREE(p);
    }
    SgArrayType::next_node = nullptr;
    SgArrayType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgArrayType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgArrayType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrayType)};
    for (size_t blockIndex = SgArrayType::pools.size(); (blockIndex * SgArrayType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgArrayType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrayType) = %" PRIuPTR " SgArrayType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgArrayType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrayType),SgArrayType::pool_size);
        }
#endif

        SgArrayType* pointer{static_cast<SgArrayType*>(ROSE_MALLOC(SgArrayType::pool_size * sizeof(SgArrayType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgArrayType::pool_size * sizeof(SgArrayType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgArrayType::pool_size * sizeof(SgArrayType));
#endif
        SgArrayType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgArrayType::pool_size * sizeof(SgArrayType), V_SgArrayType));

        if (SgArrayType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgArrayType* blkptr{reinterpret_cast<SgArrayType*>(SgArrayType::pools[blockIndex-1])};
                blkptr[SgArrayType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgArrayType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgArrayType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgArrayType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgArrayType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgArrayType::pool_size - 1};
    SgArrayType* testPointer{reinterpret_cast<SgArrayType*>(SgArrayType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgArrayType::pool_size * (SgArrayType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgArrayTypeStroageClass
 * from its counterpart of SgArrayType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgArrayType::initializeStorageClassArray(SgArrayTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgArrayType::pools.begin(); block != SgArrayType::pools.end(); ++block) {
        SgArrayType* pointer{reinterpret_cast<SgArrayType*>(*block)};
        for (unsigned i = 0; i < SgArrayType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 23805 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeEllipse that takes its corresponding StorageClass as parameter
 */
SgTypeEllipse::SgTypeEllipse(const SgTypeEllipseStorageClass& storageSource)  : SgType (storageSource) {

/* #line 23815 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeEllipse*
SgTypeEllipse::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeEllipse* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeEllipse)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeEllipse + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeEllipse)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeEllipse)};
        unsigned long positionInPool{localIndex % SgTypeEllipse::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeEllipse::pool_size};
        returnPointer = &(((SgTypeEllipse*)(SgTypeEllipse::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeEllipse*
SgTypeEllipse::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeEllipse* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeEllipse)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeEllipse + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeEllipse)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeEllipse)};
        unsigned long positionInPool{localIndex % SgTypeEllipse::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeEllipse::pool_size};
        returnPointer = &(((SgTypeEllipse*)(SgTypeEllipse::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeEllipse::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeEllipse::pools.begin(); block != SgTypeEllipse::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeEllipse*>(*block)};
        for (unsigned i = 0; i < SgTypeEllipse::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeEllipse*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeEllipse::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeEllipse* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeEllipse::pools.begin(); block != SgTypeEllipse::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeEllipse*>(*block)};
        for (unsigned i = 0; i < SgTypeEllipse::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeEllipse::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeEllipse::clearMemoryPool() {
    SgTypeEllipse* tempPointer{nullptr};
    if (SgTypeEllipse::pools.empty() == false) {
        auto block{SgTypeEllipse::pools.begin()};
        SgTypeEllipse::next_node = reinterpret_cast<SgTypeEllipse*>(*block);
        for (; block != SgTypeEllipse::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeEllipse*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeEllipse::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeEllipse::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeEllipse::pool_size-1]);
        }
    }
}

void
SgTypeEllipse::deleteMemoryPool() {
    for (auto p: SgTypeEllipse::pools) {
        ROSE_FREE(p);
    }
    SgTypeEllipse::next_node = nullptr;
    SgTypeEllipse::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeEllipse::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeEllipse) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeEllipse)};
    for (size_t blockIndex = SgTypeEllipse::pools.size(); (blockIndex * SgTypeEllipse::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeEllipse) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeEllipse) = %" PRIuPTR " SgTypeEllipse::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeEllipse),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeEllipse),SgTypeEllipse::pool_size);
        }
#endif

        SgTypeEllipse* pointer{static_cast<SgTypeEllipse*>(ROSE_MALLOC(SgTypeEllipse::pool_size * sizeof(SgTypeEllipse)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeEllipse::pool_size * sizeof(SgTypeEllipse));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeEllipse::pool_size * sizeof(SgTypeEllipse));
#endif
        SgTypeEllipse::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeEllipse::pool_size * sizeof(SgTypeEllipse), V_SgTypeEllipse));

        if (SgTypeEllipse::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeEllipse* blkptr{reinterpret_cast<SgTypeEllipse*>(SgTypeEllipse::pools[blockIndex-1])};
                blkptr[SgTypeEllipse::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeEllipse::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeEllipse::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeEllipse::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeEllipse::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeEllipse::pool_size - 1};
    SgTypeEllipse* testPointer{reinterpret_cast<SgTypeEllipse*>(SgTypeEllipse::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeEllipse::pool_size * (SgTypeEllipse::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeEllipseStroageClass
 * from its counterpart of SgTypeEllipse. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeEllipse::initializeStorageClassArray(SgTypeEllipseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeEllipse::pools.begin(); block != SgTypeEllipse::pools.end(); ++block) {
        SgTypeEllipse* pointer{reinterpret_cast<SgTypeEllipse*>(*block)};
        for (unsigned i = 0; i < SgTypeEllipse::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 24032 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateType that takes its corresponding StorageClass as parameter
 */
SgTemplateType::SgTemplateType(const SgTemplateTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 24042 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_template_parameter_position = storageSource.storageOf_template_parameter_position;
    p_template_parameter_depth = storageSource.storageOf_template_parameter_depth;
     p_class_type = (SgType *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_class_type));
     p_parent_class_type = (SgType *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parent_class_type));
     p_template_parameter = (SgTemplateParameter *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_template_parameter));
    p_tpl_args = storageSource.storageOf_tpl_args.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_tpl_args = p_tpl_args.begin();
    for ( ; i_tpl_args != p_tpl_args.end(); ++i_tpl_args) {
        (*i_tpl_args) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_tpl_args)) );
    }
    p_part_spec_tpl_args = storageSource.storageOf_part_spec_tpl_args.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_part_spec_tpl_args = p_part_spec_tpl_args.begin();
    for ( ; i_part_spec_tpl_args != p_part_spec_tpl_args.end(); ++i_part_spec_tpl_args) {
        (*i_part_spec_tpl_args) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_part_spec_tpl_args)) );
    }
    p_packed = storageSource.storageOf_packed;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateType*
SgTemplateType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateType)};
        unsigned long positionInPool{localIndex % SgTemplateType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateType::pool_size};
        returnPointer = &(((SgTemplateType*)(SgTemplateType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateType*
SgTemplateType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateType)};
        unsigned long positionInPool{localIndex % SgTemplateType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateType::pool_size};
        returnPointer = &(((SgTemplateType*)(SgTemplateType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateType::pools.begin(); block != SgTemplateType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateType*>(*block)};
        for (unsigned i = 0; i < SgTemplateType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateType* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateType::pools.begin(); block != SgTemplateType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateType*>(*block)};
        for (unsigned i = 0; i < SgTemplateType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateType::clearMemoryPool() {
    SgTemplateType* tempPointer{nullptr};
    if (SgTemplateType::pools.empty() == false) {
        auto block{SgTemplateType::pools.begin()};
        SgTemplateType::next_node = reinterpret_cast<SgTemplateType*>(*block);
        for (; block != SgTemplateType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateType::pool_size-1]);
        }
    }
}

void
SgTemplateType::deleteMemoryPool() {
    for (auto p: SgTemplateType::pools) {
        ROSE_FREE(p);
    }
    SgTemplateType::next_node = nullptr;
    SgTemplateType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateType)};
    for (size_t blockIndex = SgTemplateType::pools.size(); (blockIndex * SgTemplateType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateType) = %" PRIuPTR " SgTemplateType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateType),SgTemplateType::pool_size);
        }
#endif

        SgTemplateType* pointer{static_cast<SgTemplateType*>(ROSE_MALLOC(SgTemplateType::pool_size * sizeof(SgTemplateType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateType::pool_size * sizeof(SgTemplateType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateType::pool_size * sizeof(SgTemplateType));
#endif
        SgTemplateType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateType::pool_size * sizeof(SgTemplateType), V_SgTemplateType));

        if (SgTemplateType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateType* blkptr{reinterpret_cast<SgTemplateType*>(SgTemplateType::pools[blockIndex-1])};
                blkptr[SgTemplateType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateType::pool_size - 1};
    SgTemplateType* testPointer{reinterpret_cast<SgTemplateType*>(SgTemplateType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateType::pool_size * (SgTemplateType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateTypeStroageClass
 * from its counterpart of SgTemplateType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateType::initializeStorageClassArray(SgTemplateTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateType::pools.begin(); block != SgTemplateType::pools.end(); ++block) {
        SgTemplateType* pointer{reinterpret_cast<SgTemplateType*>(*block)};
        for (unsigned i = 0; i < SgTemplateType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 24276 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgQualifiedNameType that takes its corresponding StorageClass as parameter
 */
SgQualifiedNameType::SgQualifiedNameType(const SgQualifiedNameTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 24286 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));
    p_qualifiedNameList = storageSource.storageOf_qualifiedNameList.rebuildDataStoredInEasyStorageClass();
    SgQualifiedNamePtrList::iterator i_qualifiedNameList = p_qualifiedNameList.begin();
    for ( ; i_qualifiedNameList != p_qualifiedNameList.end(); ++i_qualifiedNameList) {
        (*i_qualifiedNameList) = (SgQualifiedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_qualifiedNameList)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgQualifiedNameType*
SgQualifiedNameType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgQualifiedNameType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgQualifiedNameType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgQualifiedNameType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgQualifiedNameType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedNameType)};
        unsigned long positionInPool{localIndex % SgQualifiedNameType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgQualifiedNameType::pool_size};
        returnPointer = &(((SgQualifiedNameType*)(SgQualifiedNameType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgQualifiedNameType*
SgQualifiedNameType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgQualifiedNameType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgQualifiedNameType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgQualifiedNameType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgQualifiedNameType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgQualifiedNameType)};
        unsigned long positionInPool{localIndex % SgQualifiedNameType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgQualifiedNameType::pool_size};
        returnPointer = &(((SgQualifiedNameType*)(SgQualifiedNameType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgQualifiedNameType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgQualifiedNameType::pools.begin(); block != SgQualifiedNameType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgQualifiedNameType*>(*block)};
        for (unsigned i = 0; i < SgQualifiedNameType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgQualifiedNameType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgQualifiedNameType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgQualifiedNameType* pointerOfLinkedList{nullptr};
    for (auto block = SgQualifiedNameType::pools.begin(); block != SgQualifiedNameType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgQualifiedNameType*>(*block)};
        for (unsigned i = 0; i < SgQualifiedNameType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgQualifiedNameType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgQualifiedNameType::clearMemoryPool() {
    SgQualifiedNameType* tempPointer{nullptr};
    if (SgQualifiedNameType::pools.empty() == false) {
        auto block{SgQualifiedNameType::pools.begin()};
        SgQualifiedNameType::next_node = reinterpret_cast<SgQualifiedNameType*>(*block);
        for (; block != SgQualifiedNameType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgQualifiedNameType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgQualifiedNameType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgQualifiedNameType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgQualifiedNameType::pool_size-1]);
        }
    }
}

void
SgQualifiedNameType::deleteMemoryPool() {
    for (auto p: SgQualifiedNameType::pools) {
        ROSE_FREE(p);
    }
    SgQualifiedNameType::next_node = nullptr;
    SgQualifiedNameType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgQualifiedNameType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedNameType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgQualifiedNameType)};
    for (size_t blockIndex = SgQualifiedNameType::pools.size(); (blockIndex * SgQualifiedNameType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedNameType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgQualifiedNameType) = %" PRIuPTR " SgQualifiedNameType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgQualifiedNameType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgQualifiedNameType),SgQualifiedNameType::pool_size);
        }
#endif

        SgQualifiedNameType* pointer{static_cast<SgQualifiedNameType*>(ROSE_MALLOC(SgQualifiedNameType::pool_size * sizeof(SgQualifiedNameType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgQualifiedNameType::pool_size * sizeof(SgQualifiedNameType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgQualifiedNameType::pool_size * sizeof(SgQualifiedNameType));
#endif
        SgQualifiedNameType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgQualifiedNameType::pool_size * sizeof(SgQualifiedNameType), V_SgQualifiedNameType));

        if (SgQualifiedNameType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgQualifiedNameType* blkptr{reinterpret_cast<SgQualifiedNameType*>(SgQualifiedNameType::pools[blockIndex-1])};
                blkptr[SgQualifiedNameType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgQualifiedNameType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgQualifiedNameType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgQualifiedNameType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgQualifiedNameType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgQualifiedNameType::pool_size - 1};
    SgQualifiedNameType* testPointer{reinterpret_cast<SgQualifiedNameType*>(SgQualifiedNameType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgQualifiedNameType::pool_size * (SgQualifiedNameType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgQualifiedNameTypeStroageClass
 * from its counterpart of SgQualifiedNameType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgQualifiedNameType::initializeStorageClassArray(SgQualifiedNameTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgQualifiedNameType::pools.begin(); block != SgQualifiedNameType::pools.end(); ++block) {
        SgQualifiedNameType* pointer{reinterpret_cast<SgQualifiedNameType*>(*block)};
        for (unsigned i = 0; i < SgQualifiedNameType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 24509 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeComplex that takes its corresponding StorageClass as parameter
 */
SgTypeComplex::SgTypeComplex(const SgTypeComplexStorageClass& storageSource)  : SgType (storageSource) {

/* #line 24519 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeComplex*
SgTypeComplex::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeComplex* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeComplex)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeComplex + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeComplex)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeComplex)};
        unsigned long positionInPool{localIndex % SgTypeComplex::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeComplex::pool_size};
        returnPointer = &(((SgTypeComplex*)(SgTypeComplex::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeComplex*
SgTypeComplex::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeComplex* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeComplex)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeComplex + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeComplex)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeComplex)};
        unsigned long positionInPool{localIndex % SgTypeComplex::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeComplex::pool_size};
        returnPointer = &(((SgTypeComplex*)(SgTypeComplex::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeComplex::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeComplex::pools.begin(); block != SgTypeComplex::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeComplex*>(*block)};
        for (unsigned i = 0; i < SgTypeComplex::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeComplex*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeComplex::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeComplex* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeComplex::pools.begin(); block != SgTypeComplex::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeComplex*>(*block)};
        for (unsigned i = 0; i < SgTypeComplex::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeComplex::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeComplex::clearMemoryPool() {
    SgTypeComplex* tempPointer{nullptr};
    if (SgTypeComplex::pools.empty() == false) {
        auto block{SgTypeComplex::pools.begin()};
        SgTypeComplex::next_node = reinterpret_cast<SgTypeComplex*>(*block);
        for (; block != SgTypeComplex::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeComplex*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeComplex::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeComplex::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeComplex::pool_size-1]);
        }
    }
}

void
SgTypeComplex::deleteMemoryPool() {
    for (auto p: SgTypeComplex::pools) {
        ROSE_FREE(p);
    }
    SgTypeComplex::next_node = nullptr;
    SgTypeComplex::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeComplex::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeComplex) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeComplex)};
    for (size_t blockIndex = SgTypeComplex::pools.size(); (blockIndex * SgTypeComplex::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeComplex) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeComplex) = %" PRIuPTR " SgTypeComplex::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeComplex),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeComplex),SgTypeComplex::pool_size);
        }
#endif

        SgTypeComplex* pointer{static_cast<SgTypeComplex*>(ROSE_MALLOC(SgTypeComplex::pool_size * sizeof(SgTypeComplex)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeComplex::pool_size * sizeof(SgTypeComplex));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeComplex::pool_size * sizeof(SgTypeComplex));
#endif
        SgTypeComplex::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeComplex::pool_size * sizeof(SgTypeComplex), V_SgTypeComplex));

        if (SgTypeComplex::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeComplex* blkptr{reinterpret_cast<SgTypeComplex*>(SgTypeComplex::pools[blockIndex-1])};
                blkptr[SgTypeComplex::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeComplex::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeComplex::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeComplex::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeComplex::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeComplex::pool_size - 1};
    SgTypeComplex* testPointer{reinterpret_cast<SgTypeComplex*>(SgTypeComplex::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeComplex::pool_size * (SgTypeComplex::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeComplexStroageClass
 * from its counterpart of SgTypeComplex. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeComplex::initializeStorageClassArray(SgTypeComplexStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeComplex::pools.begin(); block != SgTypeComplex::pools.end(); ++block) {
        SgTypeComplex* pointer{reinterpret_cast<SgTypeComplex*>(*block)};
        for (unsigned i = 0; i < SgTypeComplex::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 24737 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeImaginary that takes its corresponding StorageClass as parameter
 */
SgTypeImaginary::SgTypeImaginary(const SgTypeImaginaryStorageClass& storageSource)  : SgType (storageSource) {

/* #line 24747 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeImaginary*
SgTypeImaginary::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeImaginary* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeImaginary)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeImaginary + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeImaginary)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeImaginary)};
        unsigned long positionInPool{localIndex % SgTypeImaginary::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeImaginary::pool_size};
        returnPointer = &(((SgTypeImaginary*)(SgTypeImaginary::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeImaginary*
SgTypeImaginary::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeImaginary* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeImaginary)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeImaginary + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeImaginary)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeImaginary)};
        unsigned long positionInPool{localIndex % SgTypeImaginary::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeImaginary::pool_size};
        returnPointer = &(((SgTypeImaginary*)(SgTypeImaginary::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeImaginary::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeImaginary::pools.begin(); block != SgTypeImaginary::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeImaginary*>(*block)};
        for (unsigned i = 0; i < SgTypeImaginary::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeImaginary*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeImaginary::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeImaginary* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeImaginary::pools.begin(); block != SgTypeImaginary::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeImaginary*>(*block)};
        for (unsigned i = 0; i < SgTypeImaginary::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeImaginary::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeImaginary::clearMemoryPool() {
    SgTypeImaginary* tempPointer{nullptr};
    if (SgTypeImaginary::pools.empty() == false) {
        auto block{SgTypeImaginary::pools.begin()};
        SgTypeImaginary::next_node = reinterpret_cast<SgTypeImaginary*>(*block);
        for (; block != SgTypeImaginary::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeImaginary*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeImaginary::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeImaginary::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeImaginary::pool_size-1]);
        }
    }
}

void
SgTypeImaginary::deleteMemoryPool() {
    for (auto p: SgTypeImaginary::pools) {
        ROSE_FREE(p);
    }
    SgTypeImaginary::next_node = nullptr;
    SgTypeImaginary::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeImaginary::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeImaginary) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeImaginary)};
    for (size_t blockIndex = SgTypeImaginary::pools.size(); (blockIndex * SgTypeImaginary::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeImaginary) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeImaginary) = %" PRIuPTR " SgTypeImaginary::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeImaginary),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeImaginary),SgTypeImaginary::pool_size);
        }
#endif

        SgTypeImaginary* pointer{static_cast<SgTypeImaginary*>(ROSE_MALLOC(SgTypeImaginary::pool_size * sizeof(SgTypeImaginary)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeImaginary::pool_size * sizeof(SgTypeImaginary));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeImaginary::pool_size * sizeof(SgTypeImaginary));
#endif
        SgTypeImaginary::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeImaginary::pool_size * sizeof(SgTypeImaginary), V_SgTypeImaginary));

        if (SgTypeImaginary::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeImaginary* blkptr{reinterpret_cast<SgTypeImaginary*>(SgTypeImaginary::pools[blockIndex-1])};
                blkptr[SgTypeImaginary::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeImaginary::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeImaginary::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeImaginary::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeImaginary::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeImaginary::pool_size - 1};
    SgTypeImaginary* testPointer{reinterpret_cast<SgTypeImaginary*>(SgTypeImaginary::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeImaginary::pool_size * (SgTypeImaginary::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeImaginaryStroageClass
 * from its counterpart of SgTypeImaginary. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeImaginary::initializeStorageClassArray(SgTypeImaginaryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeImaginary::pools.begin(); block != SgTypeImaginary::pools.end(); ++block) {
        SgTypeImaginary* pointer{reinterpret_cast<SgTypeImaginary*>(*block)};
        for (unsigned i = 0; i < SgTypeImaginary::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 24965 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeDefault that takes its corresponding StorageClass as parameter
 */
SgTypeDefault::SgTypeDefault(const SgTypeDefaultStorageClass& storageSource)  : SgType (storageSource) {

/* #line 24975 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeDefault*
SgTypeDefault::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeDefault* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeDefault)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeDefault + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeDefault)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDefault)};
        unsigned long positionInPool{localIndex % SgTypeDefault::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeDefault::pool_size};
        returnPointer = &(((SgTypeDefault*)(SgTypeDefault::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeDefault*
SgTypeDefault::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeDefault* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeDefault)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeDefault + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeDefault)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeDefault)};
        unsigned long positionInPool{localIndex % SgTypeDefault::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeDefault::pool_size};
        returnPointer = &(((SgTypeDefault*)(SgTypeDefault::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeDefault::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeDefault::pools.begin(); block != SgTypeDefault::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeDefault*>(*block)};
        for (unsigned i = 0; i < SgTypeDefault::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeDefault*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeDefault::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeDefault* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeDefault::pools.begin(); block != SgTypeDefault::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeDefault*>(*block)};
        for (unsigned i = 0; i < SgTypeDefault::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeDefault::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeDefault::clearMemoryPool() {
    SgTypeDefault* tempPointer{nullptr};
    if (SgTypeDefault::pools.empty() == false) {
        auto block{SgTypeDefault::pools.begin()};
        SgTypeDefault::next_node = reinterpret_cast<SgTypeDefault*>(*block);
        for (; block != SgTypeDefault::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeDefault*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeDefault::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeDefault::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeDefault::pool_size-1]);
        }
    }
}

void
SgTypeDefault::deleteMemoryPool() {
    for (auto p: SgTypeDefault::pools) {
        ROSE_FREE(p);
    }
    SgTypeDefault::next_node = nullptr;
    SgTypeDefault::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeDefault::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDefault) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeDefault)};
    for (size_t blockIndex = SgTypeDefault::pools.size(); (blockIndex * SgTypeDefault::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDefault) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeDefault) = %" PRIuPTR " SgTypeDefault::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDefault),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeDefault),SgTypeDefault::pool_size);
        }
#endif

        SgTypeDefault* pointer{static_cast<SgTypeDefault*>(ROSE_MALLOC(SgTypeDefault::pool_size * sizeof(SgTypeDefault)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeDefault::pool_size * sizeof(SgTypeDefault));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeDefault::pool_size * sizeof(SgTypeDefault));
#endif
        SgTypeDefault::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeDefault::pool_size * sizeof(SgTypeDefault), V_SgTypeDefault));

        if (SgTypeDefault::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeDefault* blkptr{reinterpret_cast<SgTypeDefault*>(SgTypeDefault::pools[blockIndex-1])};
                blkptr[SgTypeDefault::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeDefault::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeDefault::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeDefault::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeDefault::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeDefault::pool_size - 1};
    SgTypeDefault* testPointer{reinterpret_cast<SgTypeDefault*>(SgTypeDefault::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeDefault::pool_size * (SgTypeDefault::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeDefaultStroageClass
 * from its counterpart of SgTypeDefault. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeDefault::initializeStorageClassArray(SgTypeDefaultStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeDefault::pools.begin(); block != SgTypeDefault::pools.end(); ++block) {
        SgTypeDefault* pointer{reinterpret_cast<SgTypeDefault*>(*block)};
        for (unsigned i = 0; i < SgTypeDefault::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 25193 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeCAFTeam that takes its corresponding StorageClass as parameter
 */
SgTypeCAFTeam::SgTypeCAFTeam(const SgTypeCAFTeamStorageClass& storageSource)  : SgType (storageSource) {

/* #line 25203 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeCAFTeam*
SgTypeCAFTeam::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeCAFTeam* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeCAFTeam)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeCAFTeam + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeCAFTeam)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCAFTeam)};
        unsigned long positionInPool{localIndex % SgTypeCAFTeam::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeCAFTeam::pool_size};
        returnPointer = &(((SgTypeCAFTeam*)(SgTypeCAFTeam::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeCAFTeam*
SgTypeCAFTeam::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeCAFTeam* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeCAFTeam)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeCAFTeam + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeCAFTeam)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeCAFTeam)};
        unsigned long positionInPool{localIndex % SgTypeCAFTeam::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeCAFTeam::pool_size};
        returnPointer = &(((SgTypeCAFTeam*)(SgTypeCAFTeam::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeCAFTeam::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeCAFTeam::pools.begin(); block != SgTypeCAFTeam::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeCAFTeam*>(*block)};
        for (unsigned i = 0; i < SgTypeCAFTeam::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeCAFTeam*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeCAFTeam::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeCAFTeam* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeCAFTeam::pools.begin(); block != SgTypeCAFTeam::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeCAFTeam*>(*block)};
        for (unsigned i = 0; i < SgTypeCAFTeam::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeCAFTeam::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeCAFTeam::clearMemoryPool() {
    SgTypeCAFTeam* tempPointer{nullptr};
    if (SgTypeCAFTeam::pools.empty() == false) {
        auto block{SgTypeCAFTeam::pools.begin()};
        SgTypeCAFTeam::next_node = reinterpret_cast<SgTypeCAFTeam*>(*block);
        for (; block != SgTypeCAFTeam::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeCAFTeam*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeCAFTeam::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeCAFTeam::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeCAFTeam::pool_size-1]);
        }
    }
}

void
SgTypeCAFTeam::deleteMemoryPool() {
    for (auto p: SgTypeCAFTeam::pools) {
        ROSE_FREE(p);
    }
    SgTypeCAFTeam::next_node = nullptr;
    SgTypeCAFTeam::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeCAFTeam::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCAFTeam) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeCAFTeam)};
    for (size_t blockIndex = SgTypeCAFTeam::pools.size(); (blockIndex * SgTypeCAFTeam::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCAFTeam) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeCAFTeam) = %" PRIuPTR " SgTypeCAFTeam::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCAFTeam),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeCAFTeam),SgTypeCAFTeam::pool_size);
        }
#endif

        SgTypeCAFTeam* pointer{static_cast<SgTypeCAFTeam*>(ROSE_MALLOC(SgTypeCAFTeam::pool_size * sizeof(SgTypeCAFTeam)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeCAFTeam::pool_size * sizeof(SgTypeCAFTeam));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeCAFTeam::pool_size * sizeof(SgTypeCAFTeam));
#endif
        SgTypeCAFTeam::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeCAFTeam::pool_size * sizeof(SgTypeCAFTeam), V_SgTypeCAFTeam));

        if (SgTypeCAFTeam::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeCAFTeam* blkptr{reinterpret_cast<SgTypeCAFTeam*>(SgTypeCAFTeam::pools[blockIndex-1])};
                blkptr[SgTypeCAFTeam::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeCAFTeam::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeCAFTeam::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeCAFTeam::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeCAFTeam::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeCAFTeam::pool_size - 1};
    SgTypeCAFTeam* testPointer{reinterpret_cast<SgTypeCAFTeam*>(SgTypeCAFTeam::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeCAFTeam::pool_size * (SgTypeCAFTeam::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeCAFTeamStroageClass
 * from its counterpart of SgTypeCAFTeam. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeCAFTeam::initializeStorageClassArray(SgTypeCAFTeamStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeCAFTeam::pools.begin(); block != SgTypeCAFTeam::pools.end(); ++block) {
        SgTypeCAFTeam* pointer{reinterpret_cast<SgTypeCAFTeam*>(*block)};
        for (unsigned i = 0; i < SgTypeCAFTeam::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 25420 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeCrayPointer that takes its corresponding StorageClass as parameter
 */
SgTypeCrayPointer::SgTypeCrayPointer(const SgTypeCrayPointerStorageClass& storageSource)  : SgType (storageSource) {

/* #line 25430 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeCrayPointer*
SgTypeCrayPointer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeCrayPointer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeCrayPointer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeCrayPointer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeCrayPointer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCrayPointer)};
        unsigned long positionInPool{localIndex % SgTypeCrayPointer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeCrayPointer::pool_size};
        returnPointer = &(((SgTypeCrayPointer*)(SgTypeCrayPointer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeCrayPointer*
SgTypeCrayPointer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeCrayPointer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeCrayPointer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeCrayPointer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeCrayPointer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeCrayPointer)};
        unsigned long positionInPool{localIndex % SgTypeCrayPointer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeCrayPointer::pool_size};
        returnPointer = &(((SgTypeCrayPointer*)(SgTypeCrayPointer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeCrayPointer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeCrayPointer::pools.begin(); block != SgTypeCrayPointer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeCrayPointer*>(*block)};
        for (unsigned i = 0; i < SgTypeCrayPointer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeCrayPointer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeCrayPointer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeCrayPointer* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeCrayPointer::pools.begin(); block != SgTypeCrayPointer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeCrayPointer*>(*block)};
        for (unsigned i = 0; i < SgTypeCrayPointer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeCrayPointer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeCrayPointer::clearMemoryPool() {
    SgTypeCrayPointer* tempPointer{nullptr};
    if (SgTypeCrayPointer::pools.empty() == false) {
        auto block{SgTypeCrayPointer::pools.begin()};
        SgTypeCrayPointer::next_node = reinterpret_cast<SgTypeCrayPointer*>(*block);
        for (; block != SgTypeCrayPointer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeCrayPointer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeCrayPointer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeCrayPointer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeCrayPointer::pool_size-1]);
        }
    }
}

void
SgTypeCrayPointer::deleteMemoryPool() {
    for (auto p: SgTypeCrayPointer::pools) {
        ROSE_FREE(p);
    }
    SgTypeCrayPointer::next_node = nullptr;
    SgTypeCrayPointer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeCrayPointer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCrayPointer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeCrayPointer)};
    for (size_t blockIndex = SgTypeCrayPointer::pools.size(); (blockIndex * SgTypeCrayPointer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCrayPointer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeCrayPointer) = %" PRIuPTR " SgTypeCrayPointer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeCrayPointer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeCrayPointer),SgTypeCrayPointer::pool_size);
        }
#endif

        SgTypeCrayPointer* pointer{static_cast<SgTypeCrayPointer*>(ROSE_MALLOC(SgTypeCrayPointer::pool_size * sizeof(SgTypeCrayPointer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeCrayPointer::pool_size * sizeof(SgTypeCrayPointer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeCrayPointer::pool_size * sizeof(SgTypeCrayPointer));
#endif
        SgTypeCrayPointer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeCrayPointer::pool_size * sizeof(SgTypeCrayPointer), V_SgTypeCrayPointer));

        if (SgTypeCrayPointer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeCrayPointer* blkptr{reinterpret_cast<SgTypeCrayPointer*>(SgTypeCrayPointer::pools[blockIndex-1])};
                blkptr[SgTypeCrayPointer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeCrayPointer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeCrayPointer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeCrayPointer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeCrayPointer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeCrayPointer::pool_size - 1};
    SgTypeCrayPointer* testPointer{reinterpret_cast<SgTypeCrayPointer*>(SgTypeCrayPointer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeCrayPointer::pool_size * (SgTypeCrayPointer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeCrayPointerStroageClass
 * from its counterpart of SgTypeCrayPointer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeCrayPointer::initializeStorageClassArray(SgTypeCrayPointerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeCrayPointer::pools.begin(); block != SgTypeCrayPointer::pools.end(); ++block) {
        SgTypeCrayPointer* pointer{reinterpret_cast<SgTypeCrayPointer*>(*block)};
        for (unsigned i = 0; i < SgTypeCrayPointer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 25647 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeLabel that takes its corresponding StorageClass as parameter
 */
SgTypeLabel::SgTypeLabel(const SgTypeLabelStorageClass& storageSource)  : SgType (storageSource) {

/* #line 25657 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeLabel*
SgTypeLabel::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeLabel* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLabel)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLabel + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLabel)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLabel)};
        unsigned long positionInPool{localIndex % SgTypeLabel::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLabel::pool_size};
        returnPointer = &(((SgTypeLabel*)(SgTypeLabel::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeLabel*
SgTypeLabel::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeLabel* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLabel)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLabel + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLabel)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeLabel)};
        unsigned long positionInPool{localIndex % SgTypeLabel::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLabel::pool_size};
        returnPointer = &(((SgTypeLabel*)(SgTypeLabel::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeLabel::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeLabel::pools.begin(); block != SgTypeLabel::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLabel*>(*block)};
        for (unsigned i = 0; i < SgTypeLabel::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeLabel*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeLabel::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeLabel* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeLabel::pools.begin(); block != SgTypeLabel::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLabel*>(*block)};
        for (unsigned i = 0; i < SgTypeLabel::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeLabel::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeLabel::clearMemoryPool() {
    SgTypeLabel* tempPointer{nullptr};
    if (SgTypeLabel::pools.empty() == false) {
        auto block{SgTypeLabel::pools.begin()};
        SgTypeLabel::next_node = reinterpret_cast<SgTypeLabel*>(*block);
        for (; block != SgTypeLabel::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeLabel*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeLabel::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeLabel::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeLabel::pool_size-1]);
        }
    }
}

void
SgTypeLabel::deleteMemoryPool() {
    for (auto p: SgTypeLabel::pools) {
        ROSE_FREE(p);
    }
    SgTypeLabel::next_node = nullptr;
    SgTypeLabel::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeLabel::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLabel) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLabel)};
    for (size_t blockIndex = SgTypeLabel::pools.size(); (blockIndex * SgTypeLabel::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLabel) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLabel) = %" PRIuPTR " SgTypeLabel::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLabel),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLabel),SgTypeLabel::pool_size);
        }
#endif

        SgTypeLabel* pointer{static_cast<SgTypeLabel*>(ROSE_MALLOC(SgTypeLabel::pool_size * sizeof(SgTypeLabel)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeLabel::pool_size * sizeof(SgTypeLabel));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeLabel::pool_size * sizeof(SgTypeLabel));
#endif
        SgTypeLabel::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeLabel::pool_size * sizeof(SgTypeLabel), V_SgTypeLabel));

        if (SgTypeLabel::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeLabel* blkptr{reinterpret_cast<SgTypeLabel*>(SgTypeLabel::pools[blockIndex-1])};
                blkptr[SgTypeLabel::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeLabel::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeLabel::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeLabel::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeLabel::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeLabel::pool_size - 1};
    SgTypeLabel* testPointer{reinterpret_cast<SgTypeLabel*>(SgTypeLabel::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeLabel::pool_size * (SgTypeLabel::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeLabelStroageClass
 * from its counterpart of SgTypeLabel. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeLabel::initializeStorageClassArray(SgTypeLabelStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeLabel::pools.begin(); block != SgTypeLabel::pools.end(); ++block) {
        SgTypeLabel* pointer{reinterpret_cast<SgTypeLabel*>(*block)};
        for (unsigned i = 0; i < SgTypeLabel::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 25875 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaUnionType that takes its corresponding StorageClass as parameter
 */
SgJavaUnionType::SgJavaUnionType(const SgJavaUnionTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 25885 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_type_list = storageSource.storageOf_type_list.rebuildDataStoredInEasyStorageClass();
    SgTypePtrList::iterator i_type_list = p_type_list.begin();
    for ( ; i_type_list != p_type_list.end(); ++i_type_list) {
        (*i_type_list) = (SgTypePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_type_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaUnionType*
SgJavaUnionType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaUnionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnionType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnionType)};
        unsigned long positionInPool{localIndex % SgJavaUnionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaUnionType::pool_size};
        returnPointer = &(((SgJavaUnionType*)(SgJavaUnionType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaUnionType*
SgJavaUnionType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaUnionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnionType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaUnionType)};
        unsigned long positionInPool{localIndex % SgJavaUnionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaUnionType::pool_size};
        returnPointer = &(((SgJavaUnionType*)(SgJavaUnionType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaUnionType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaUnionType::pools.begin(); block != SgJavaUnionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaUnionType*>(*block)};
        for (unsigned i = 0; i < SgJavaUnionType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaUnionType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaUnionType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaUnionType* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaUnionType::pools.begin(); block != SgJavaUnionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaUnionType*>(*block)};
        for (unsigned i = 0; i < SgJavaUnionType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaUnionType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaUnionType::clearMemoryPool() {
    SgJavaUnionType* tempPointer{nullptr};
    if (SgJavaUnionType::pools.empty() == false) {
        auto block{SgJavaUnionType::pools.begin()};
        SgJavaUnionType::next_node = reinterpret_cast<SgJavaUnionType*>(*block);
        for (; block != SgJavaUnionType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaUnionType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaUnionType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaUnionType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaUnionType::pool_size-1]);
        }
    }
}

void
SgJavaUnionType::deleteMemoryPool() {
    for (auto p: SgJavaUnionType::pools) {
        ROSE_FREE(p);
    }
    SgJavaUnionType::next_node = nullptr;
    SgJavaUnionType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaUnionType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnionType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnionType)};
    for (size_t blockIndex = SgJavaUnionType::pools.size(); (blockIndex * SgJavaUnionType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnionType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnionType) = %" PRIuPTR " SgJavaUnionType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnionType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnionType),SgJavaUnionType::pool_size);
        }
#endif

        SgJavaUnionType* pointer{static_cast<SgJavaUnionType*>(ROSE_MALLOC(SgJavaUnionType::pool_size * sizeof(SgJavaUnionType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaUnionType::pool_size * sizeof(SgJavaUnionType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaUnionType::pool_size * sizeof(SgJavaUnionType));
#endif
        SgJavaUnionType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaUnionType::pool_size * sizeof(SgJavaUnionType), V_SgJavaUnionType));

        if (SgJavaUnionType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaUnionType* blkptr{reinterpret_cast<SgJavaUnionType*>(SgJavaUnionType::pools[blockIndex-1])};
                blkptr[SgJavaUnionType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaUnionType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaUnionType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaUnionType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaUnionType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaUnionType::pool_size - 1};
    SgJavaUnionType* testPointer{reinterpret_cast<SgJavaUnionType*>(SgJavaUnionType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaUnionType::pool_size * (SgJavaUnionType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaUnionTypeStroageClass
 * from its counterpart of SgJavaUnionType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaUnionType::initializeStorageClassArray(SgJavaUnionTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaUnionType::pools.begin(); block != SgJavaUnionType::pools.end(); ++block) {
        SgJavaUnionType* pointer{reinterpret_cast<SgJavaUnionType*>(*block)};
        for (unsigned i = 0; i < SgJavaUnionType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 26107 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRvalueReferenceType that takes its corresponding StorageClass as parameter
 */
SgRvalueReferenceType::SgRvalueReferenceType(const SgRvalueReferenceTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 26117 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRvalueReferenceType*
SgRvalueReferenceType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRvalueReferenceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRvalueReferenceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRvalueReferenceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRvalueReferenceType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRvalueReferenceType)};
        unsigned long positionInPool{localIndex % SgRvalueReferenceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRvalueReferenceType::pool_size};
        returnPointer = &(((SgRvalueReferenceType*)(SgRvalueReferenceType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRvalueReferenceType*
SgRvalueReferenceType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRvalueReferenceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRvalueReferenceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRvalueReferenceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRvalueReferenceType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRvalueReferenceType)};
        unsigned long positionInPool{localIndex % SgRvalueReferenceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRvalueReferenceType::pool_size};
        returnPointer = &(((SgRvalueReferenceType*)(SgRvalueReferenceType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRvalueReferenceType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRvalueReferenceType::pools.begin(); block != SgRvalueReferenceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRvalueReferenceType*>(*block)};
        for (unsigned i = 0; i < SgRvalueReferenceType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRvalueReferenceType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRvalueReferenceType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRvalueReferenceType* pointerOfLinkedList{nullptr};
    for (auto block = SgRvalueReferenceType::pools.begin(); block != SgRvalueReferenceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRvalueReferenceType*>(*block)};
        for (unsigned i = 0; i < SgRvalueReferenceType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRvalueReferenceType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRvalueReferenceType::clearMemoryPool() {
    SgRvalueReferenceType* tempPointer{nullptr};
    if (SgRvalueReferenceType::pools.empty() == false) {
        auto block{SgRvalueReferenceType::pools.begin()};
        SgRvalueReferenceType::next_node = reinterpret_cast<SgRvalueReferenceType*>(*block);
        for (; block != SgRvalueReferenceType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRvalueReferenceType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRvalueReferenceType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRvalueReferenceType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRvalueReferenceType::pool_size-1]);
        }
    }
}

void
SgRvalueReferenceType::deleteMemoryPool() {
    for (auto p: SgRvalueReferenceType::pools) {
        ROSE_FREE(p);
    }
    SgRvalueReferenceType::next_node = nullptr;
    SgRvalueReferenceType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRvalueReferenceType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRvalueReferenceType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRvalueReferenceType)};
    for (size_t blockIndex = SgRvalueReferenceType::pools.size(); (blockIndex * SgRvalueReferenceType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRvalueReferenceType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRvalueReferenceType) = %" PRIuPTR " SgRvalueReferenceType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRvalueReferenceType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRvalueReferenceType),SgRvalueReferenceType::pool_size);
        }
#endif

        SgRvalueReferenceType* pointer{static_cast<SgRvalueReferenceType*>(ROSE_MALLOC(SgRvalueReferenceType::pool_size * sizeof(SgRvalueReferenceType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRvalueReferenceType::pool_size * sizeof(SgRvalueReferenceType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRvalueReferenceType::pool_size * sizeof(SgRvalueReferenceType));
#endif
        SgRvalueReferenceType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRvalueReferenceType::pool_size * sizeof(SgRvalueReferenceType), V_SgRvalueReferenceType));

        if (SgRvalueReferenceType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRvalueReferenceType* blkptr{reinterpret_cast<SgRvalueReferenceType*>(SgRvalueReferenceType::pools[blockIndex-1])};
                blkptr[SgRvalueReferenceType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRvalueReferenceType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRvalueReferenceType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRvalueReferenceType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRvalueReferenceType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRvalueReferenceType::pool_size - 1};
    SgRvalueReferenceType* testPointer{reinterpret_cast<SgRvalueReferenceType*>(SgRvalueReferenceType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRvalueReferenceType::pool_size * (SgRvalueReferenceType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRvalueReferenceTypeStroageClass
 * from its counterpart of SgRvalueReferenceType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRvalueReferenceType::initializeStorageClassArray(SgRvalueReferenceTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRvalueReferenceType::pools.begin(); block != SgRvalueReferenceType::pools.end(); ++block) {
        SgRvalueReferenceType* pointer{reinterpret_cast<SgRvalueReferenceType*>(*block)};
        for (unsigned i = 0; i < SgRvalueReferenceType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 26335 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeNullptr that takes its corresponding StorageClass as parameter
 */
SgTypeNullptr::SgTypeNullptr(const SgTypeNullptrStorageClass& storageSource)  : SgType (storageSource) {

/* #line 26345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeNullptr*
SgTypeNullptr::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeNullptr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeNullptr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeNullptr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeNullptr)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeNullptr)};
        unsigned long positionInPool{localIndex % SgTypeNullptr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeNullptr::pool_size};
        returnPointer = &(((SgTypeNullptr*)(SgTypeNullptr::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeNullptr*
SgTypeNullptr::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeNullptr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeNullptr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeNullptr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeNullptr)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeNullptr)};
        unsigned long positionInPool{localIndex % SgTypeNullptr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeNullptr::pool_size};
        returnPointer = &(((SgTypeNullptr*)(SgTypeNullptr::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeNullptr::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeNullptr::pools.begin(); block != SgTypeNullptr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeNullptr*>(*block)};
        for (unsigned i = 0; i < SgTypeNullptr::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeNullptr*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeNullptr::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeNullptr* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeNullptr::pools.begin(); block != SgTypeNullptr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeNullptr*>(*block)};
        for (unsigned i = 0; i < SgTypeNullptr::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeNullptr::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeNullptr::clearMemoryPool() {
    SgTypeNullptr* tempPointer{nullptr};
    if (SgTypeNullptr::pools.empty() == false) {
        auto block{SgTypeNullptr::pools.begin()};
        SgTypeNullptr::next_node = reinterpret_cast<SgTypeNullptr*>(*block);
        for (; block != SgTypeNullptr::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeNullptr*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeNullptr::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeNullptr::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeNullptr::pool_size-1]);
        }
    }
}

void
SgTypeNullptr::deleteMemoryPool() {
    for (auto p: SgTypeNullptr::pools) {
        ROSE_FREE(p);
    }
    SgTypeNullptr::next_node = nullptr;
    SgTypeNullptr::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeNullptr::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeNullptr) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeNullptr)};
    for (size_t blockIndex = SgTypeNullptr::pools.size(); (blockIndex * SgTypeNullptr::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeNullptr) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeNullptr) = %" PRIuPTR " SgTypeNullptr::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeNullptr),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeNullptr),SgTypeNullptr::pool_size);
        }
#endif

        SgTypeNullptr* pointer{static_cast<SgTypeNullptr*>(ROSE_MALLOC(SgTypeNullptr::pool_size * sizeof(SgTypeNullptr)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeNullptr::pool_size * sizeof(SgTypeNullptr));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeNullptr::pool_size * sizeof(SgTypeNullptr));
#endif
        SgTypeNullptr::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeNullptr::pool_size * sizeof(SgTypeNullptr), V_SgTypeNullptr));

        if (SgTypeNullptr::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeNullptr* blkptr{reinterpret_cast<SgTypeNullptr*>(SgTypeNullptr::pools[blockIndex-1])};
                blkptr[SgTypeNullptr::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeNullptr::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeNullptr::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeNullptr::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeNullptr::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeNullptr::pool_size - 1};
    SgTypeNullptr* testPointer{reinterpret_cast<SgTypeNullptr*>(SgTypeNullptr::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeNullptr::pool_size * (SgTypeNullptr::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeNullptrStroageClass
 * from its counterpart of SgTypeNullptr. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeNullptr::initializeStorageClassArray(SgTypeNullptrStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeNullptr::pools.begin(); block != SgTypeNullptr::pools.end(); ++block) {
        SgTypeNullptr* pointer{reinterpret_cast<SgTypeNullptr*>(*block)};
        for (unsigned i = 0; i < SgTypeNullptr::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 26562 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDeclType that takes its corresponding StorageClass as parameter
 */
SgDeclType::SgDeclType(const SgDeclTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 26572 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_expression));
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDeclType*
SgDeclType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDeclType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclType)};
        unsigned long positionInPool{localIndex % SgDeclType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclType::pool_size};
        returnPointer = &(((SgDeclType*)(SgDeclType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDeclType*
SgDeclType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDeclType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDeclType)};
        unsigned long positionInPool{localIndex % SgDeclType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclType::pool_size};
        returnPointer = &(((SgDeclType*)(SgDeclType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDeclType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDeclType::pools.begin(); block != SgDeclType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclType*>(*block)};
        for (unsigned i = 0; i < SgDeclType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDeclType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDeclType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDeclType* pointerOfLinkedList{nullptr};
    for (auto block = SgDeclType::pools.begin(); block != SgDeclType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclType*>(*block)};
        for (unsigned i = 0; i < SgDeclType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDeclType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDeclType::clearMemoryPool() {
    SgDeclType* tempPointer{nullptr};
    if (SgDeclType::pools.empty() == false) {
        auto block{SgDeclType::pools.begin()};
        SgDeclType::next_node = reinterpret_cast<SgDeclType*>(*block);
        for (; block != SgDeclType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDeclType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDeclType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDeclType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDeclType::pool_size-1]);
        }
    }
}

void
SgDeclType::deleteMemoryPool() {
    for (auto p: SgDeclType::pools) {
        ROSE_FREE(p);
    }
    SgDeclType::next_node = nullptr;
    SgDeclType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDeclType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclType)};
    for (size_t blockIndex = SgDeclType::pools.size(); (blockIndex * SgDeclType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclType) = %" PRIuPTR " SgDeclType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclType),SgDeclType::pool_size);
        }
#endif

        SgDeclType* pointer{static_cast<SgDeclType*>(ROSE_MALLOC(SgDeclType::pool_size * sizeof(SgDeclType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDeclType::pool_size * sizeof(SgDeclType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDeclType::pool_size * sizeof(SgDeclType));
#endif
        SgDeclType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDeclType::pool_size * sizeof(SgDeclType), V_SgDeclType));

        if (SgDeclType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDeclType* blkptr{reinterpret_cast<SgDeclType*>(SgDeclType::pools[blockIndex-1])};
                blkptr[SgDeclType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDeclType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDeclType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDeclType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDeclType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDeclType::pool_size - 1};
    SgDeclType* testPointer{reinterpret_cast<SgDeclType*>(SgDeclType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDeclType::pool_size * (SgDeclType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDeclTypeStroageClass
 * from its counterpart of SgDeclType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDeclType::initializeStorageClassArray(SgDeclTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDeclType::pools.begin(); block != SgDeclType::pools.end(); ++block) {
        SgDeclType* pointer{reinterpret_cast<SgDeclType*>(*block)};
        for (unsigned i = 0; i < SgDeclType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 26791 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeOfType that takes its corresponding StorageClass as parameter
 */
SgTypeOfType::SgTypeOfType(const SgTypeOfTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 26801 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_expression));
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeOfType*
SgTypeOfType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeOfType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeOfType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeOfType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeOfType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeOfType)};
        unsigned long positionInPool{localIndex % SgTypeOfType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeOfType::pool_size};
        returnPointer = &(((SgTypeOfType*)(SgTypeOfType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeOfType*
SgTypeOfType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeOfType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeOfType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeOfType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeOfType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeOfType)};
        unsigned long positionInPool{localIndex % SgTypeOfType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeOfType::pool_size};
        returnPointer = &(((SgTypeOfType*)(SgTypeOfType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeOfType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeOfType::pools.begin(); block != SgTypeOfType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeOfType*>(*block)};
        for (unsigned i = 0; i < SgTypeOfType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeOfType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeOfType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeOfType* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeOfType::pools.begin(); block != SgTypeOfType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeOfType*>(*block)};
        for (unsigned i = 0; i < SgTypeOfType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeOfType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeOfType::clearMemoryPool() {
    SgTypeOfType* tempPointer{nullptr};
    if (SgTypeOfType::pools.empty() == false) {
        auto block{SgTypeOfType::pools.begin()};
        SgTypeOfType::next_node = reinterpret_cast<SgTypeOfType*>(*block);
        for (; block != SgTypeOfType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeOfType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeOfType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeOfType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeOfType::pool_size-1]);
        }
    }
}

void
SgTypeOfType::deleteMemoryPool() {
    for (auto p: SgTypeOfType::pools) {
        ROSE_FREE(p);
    }
    SgTypeOfType::next_node = nullptr;
    SgTypeOfType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeOfType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeOfType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeOfType)};
    for (size_t blockIndex = SgTypeOfType::pools.size(); (blockIndex * SgTypeOfType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeOfType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeOfType) = %" PRIuPTR " SgTypeOfType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeOfType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeOfType),SgTypeOfType::pool_size);
        }
#endif

        SgTypeOfType* pointer{static_cast<SgTypeOfType*>(ROSE_MALLOC(SgTypeOfType::pool_size * sizeof(SgTypeOfType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeOfType::pool_size * sizeof(SgTypeOfType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeOfType::pool_size * sizeof(SgTypeOfType));
#endif
        SgTypeOfType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeOfType::pool_size * sizeof(SgTypeOfType), V_SgTypeOfType));

        if (SgTypeOfType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeOfType* blkptr{reinterpret_cast<SgTypeOfType*>(SgTypeOfType::pools[blockIndex-1])};
                blkptr[SgTypeOfType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeOfType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeOfType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeOfType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeOfType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeOfType::pool_size - 1};
    SgTypeOfType* testPointer{reinterpret_cast<SgTypeOfType*>(SgTypeOfType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeOfType::pool_size * (SgTypeOfType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeOfTypeStroageClass
 * from its counterpart of SgTypeOfType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeOfType::initializeStorageClassArray(SgTypeOfTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeOfType::pools.begin(); block != SgTypeOfType::pools.end(); ++block) {
        SgTypeOfType* pointer{reinterpret_cast<SgTypeOfType*>(*block)};
        for (unsigned i = 0; i < SgTypeOfType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 27020 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeMatrix that takes its corresponding StorageClass as parameter
 */
SgTypeMatrix::SgTypeMatrix(const SgTypeMatrixStorageClass& storageSource)  : SgType (storageSource) {

/* #line 27030 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeMatrix*
SgTypeMatrix::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeMatrix* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeMatrix)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeMatrix + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeMatrix)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeMatrix)};
        unsigned long positionInPool{localIndex % SgTypeMatrix::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeMatrix::pool_size};
        returnPointer = &(((SgTypeMatrix*)(SgTypeMatrix::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeMatrix*
SgTypeMatrix::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeMatrix* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeMatrix)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeMatrix + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeMatrix)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeMatrix)};
        unsigned long positionInPool{localIndex % SgTypeMatrix::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeMatrix::pool_size};
        returnPointer = &(((SgTypeMatrix*)(SgTypeMatrix::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeMatrix::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeMatrix::pools.begin(); block != SgTypeMatrix::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeMatrix*>(*block)};
        for (unsigned i = 0; i < SgTypeMatrix::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeMatrix*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeMatrix::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeMatrix* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeMatrix::pools.begin(); block != SgTypeMatrix::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeMatrix*>(*block)};
        for (unsigned i = 0; i < SgTypeMatrix::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeMatrix::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeMatrix::clearMemoryPool() {
    SgTypeMatrix* tempPointer{nullptr};
    if (SgTypeMatrix::pools.empty() == false) {
        auto block{SgTypeMatrix::pools.begin()};
        SgTypeMatrix::next_node = reinterpret_cast<SgTypeMatrix*>(*block);
        for (; block != SgTypeMatrix::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeMatrix*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeMatrix::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeMatrix::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeMatrix::pool_size-1]);
        }
    }
}

void
SgTypeMatrix::deleteMemoryPool() {
    for (auto p: SgTypeMatrix::pools) {
        ROSE_FREE(p);
    }
    SgTypeMatrix::next_node = nullptr;
    SgTypeMatrix::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeMatrix::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeMatrix) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeMatrix)};
    for (size_t blockIndex = SgTypeMatrix::pools.size(); (blockIndex * SgTypeMatrix::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeMatrix) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeMatrix) = %" PRIuPTR " SgTypeMatrix::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeMatrix),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeMatrix),SgTypeMatrix::pool_size);
        }
#endif

        SgTypeMatrix* pointer{static_cast<SgTypeMatrix*>(ROSE_MALLOC(SgTypeMatrix::pool_size * sizeof(SgTypeMatrix)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeMatrix::pool_size * sizeof(SgTypeMatrix));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeMatrix::pool_size * sizeof(SgTypeMatrix));
#endif
        SgTypeMatrix::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeMatrix::pool_size * sizeof(SgTypeMatrix), V_SgTypeMatrix));

        if (SgTypeMatrix::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeMatrix* blkptr{reinterpret_cast<SgTypeMatrix*>(SgTypeMatrix::pools[blockIndex-1])};
                blkptr[SgTypeMatrix::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeMatrix::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeMatrix::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeMatrix::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeMatrix::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeMatrix::pool_size - 1};
    SgTypeMatrix* testPointer{reinterpret_cast<SgTypeMatrix*>(SgTypeMatrix::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeMatrix::pool_size * (SgTypeMatrix::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeMatrixStroageClass
 * from its counterpart of SgTypeMatrix. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeMatrix::initializeStorageClassArray(SgTypeMatrixStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeMatrix::pools.begin(); block != SgTypeMatrix::pools.end(); ++block) {
        SgTypeMatrix* pointer{reinterpret_cast<SgTypeMatrix*>(*block)};
        for (unsigned i = 0; i < SgTypeMatrix::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 27248 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFixed that takes its corresponding StorageClass as parameter
 */
SgTypeFixed::SgTypeFixed(const SgTypeFixedStorageClass& storageSource)  : SgType (storageSource) {

/* #line 27258 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_scale = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scale));
     p_fraction = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_fraction));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFixed*
SgTypeFixed::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFixed* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFixed)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFixed + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFixed)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFixed)};
        unsigned long positionInPool{localIndex % SgTypeFixed::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFixed::pool_size};
        returnPointer = &(((SgTypeFixed*)(SgTypeFixed::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFixed*
SgTypeFixed::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFixed* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFixed)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFixed + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFixed)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFixed)};
        unsigned long positionInPool{localIndex % SgTypeFixed::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFixed::pool_size};
        returnPointer = &(((SgTypeFixed*)(SgTypeFixed::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFixed::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFixed::pools.begin(); block != SgTypeFixed::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFixed*>(*block)};
        for (unsigned i = 0; i < SgTypeFixed::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFixed*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFixed::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFixed* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFixed::pools.begin(); block != SgTypeFixed::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFixed*>(*block)};
        for (unsigned i = 0; i < SgTypeFixed::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFixed::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFixed::clearMemoryPool() {
    SgTypeFixed* tempPointer{nullptr};
    if (SgTypeFixed::pools.empty() == false) {
        auto block{SgTypeFixed::pools.begin()};
        SgTypeFixed::next_node = reinterpret_cast<SgTypeFixed*>(*block);
        for (; block != SgTypeFixed::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFixed*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFixed::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFixed::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFixed::pool_size-1]);
        }
    }
}

void
SgTypeFixed::deleteMemoryPool() {
    for (auto p: SgTypeFixed::pools) {
        ROSE_FREE(p);
    }
    SgTypeFixed::next_node = nullptr;
    SgTypeFixed::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFixed::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFixed) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFixed)};
    for (size_t blockIndex = SgTypeFixed::pools.size(); (blockIndex * SgTypeFixed::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFixed) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFixed) = %" PRIuPTR " SgTypeFixed::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFixed),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFixed),SgTypeFixed::pool_size);
        }
#endif

        SgTypeFixed* pointer{static_cast<SgTypeFixed*>(ROSE_MALLOC(SgTypeFixed::pool_size * sizeof(SgTypeFixed)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFixed::pool_size * sizeof(SgTypeFixed));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFixed::pool_size * sizeof(SgTypeFixed));
#endif
        SgTypeFixed::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFixed::pool_size * sizeof(SgTypeFixed), V_SgTypeFixed));

        if (SgTypeFixed::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFixed* blkptr{reinterpret_cast<SgTypeFixed*>(SgTypeFixed::pools[blockIndex-1])};
                blkptr[SgTypeFixed::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFixed::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFixed::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFixed::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFixed::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFixed::pool_size - 1};
    SgTypeFixed* testPointer{reinterpret_cast<SgTypeFixed*>(SgTypeFixed::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFixed::pool_size * (SgTypeFixed::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFixedStroageClass
 * from its counterpart of SgTypeFixed. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFixed::initializeStorageClassArray(SgTypeFixedStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFixed::pools.begin(); block != SgTypeFixed::pools.end(); ++block) {
        SgTypeFixed* pointer{reinterpret_cast<SgTypeFixed*>(*block)};
        for (unsigned i = 0; i < SgTypeFixed::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 27477 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAutoType that takes its corresponding StorageClass as parameter
 */
SgAutoType::SgAutoType(const SgAutoTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 27487 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAutoType*
SgAutoType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAutoType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAutoType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAutoType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAutoType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAutoType)};
        unsigned long positionInPool{localIndex % SgAutoType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAutoType::pool_size};
        returnPointer = &(((SgAutoType*)(SgAutoType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAutoType*
SgAutoType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAutoType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAutoType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAutoType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAutoType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAutoType)};
        unsigned long positionInPool{localIndex % SgAutoType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAutoType::pool_size};
        returnPointer = &(((SgAutoType*)(SgAutoType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAutoType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAutoType::pools.begin(); block != SgAutoType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAutoType*>(*block)};
        for (unsigned i = 0; i < SgAutoType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAutoType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAutoType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAutoType* pointerOfLinkedList{nullptr};
    for (auto block = SgAutoType::pools.begin(); block != SgAutoType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAutoType*>(*block)};
        for (unsigned i = 0; i < SgAutoType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAutoType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAutoType::clearMemoryPool() {
    SgAutoType* tempPointer{nullptr};
    if (SgAutoType::pools.empty() == false) {
        auto block{SgAutoType::pools.begin()};
        SgAutoType::next_node = reinterpret_cast<SgAutoType*>(*block);
        for (; block != SgAutoType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAutoType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAutoType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAutoType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAutoType::pool_size-1]);
        }
    }
}

void
SgAutoType::deleteMemoryPool() {
    for (auto p: SgAutoType::pools) {
        ROSE_FREE(p);
    }
    SgAutoType::next_node = nullptr;
    SgAutoType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAutoType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAutoType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAutoType)};
    for (size_t blockIndex = SgAutoType::pools.size(); (blockIndex * SgAutoType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAutoType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAutoType) = %" PRIuPTR " SgAutoType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAutoType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAutoType),SgAutoType::pool_size);
        }
#endif

        SgAutoType* pointer{static_cast<SgAutoType*>(ROSE_MALLOC(SgAutoType::pool_size * sizeof(SgAutoType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAutoType::pool_size * sizeof(SgAutoType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAutoType::pool_size * sizeof(SgAutoType));
#endif
        SgAutoType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAutoType::pool_size * sizeof(SgAutoType), V_SgAutoType));

        if (SgAutoType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAutoType* blkptr{reinterpret_cast<SgAutoType*>(SgAutoType::pools[blockIndex-1])};
                blkptr[SgAutoType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAutoType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAutoType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAutoType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAutoType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAutoType::pool_size - 1};
    SgAutoType* testPointer{reinterpret_cast<SgAutoType*>(SgAutoType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAutoType::pool_size * (SgAutoType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAutoTypeStroageClass
 * from its counterpart of SgAutoType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAutoType::initializeStorageClassArray(SgAutoTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAutoType::pools.begin(); block != SgAutoType::pools.end(); ++block) {
        SgAutoType* pointer{reinterpret_cast<SgAutoType*>(*block)};
        for (unsigned i = 0; i < SgAutoType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 27704 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaAccessType that takes its corresponding StorageClass as parameter
 */
SgAdaAccessType::SgAdaAccessType(const SgAdaAccessTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 27714 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));
    p_is_general_access = storageSource.storageOf_is_general_access;
    p_is_anonymous = storageSource.storageOf_is_anonymous;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaAccessType*
SgAdaAccessType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaAccessType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAccessType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAccessType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAccessType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAccessType)};
        unsigned long positionInPool{localIndex % SgAdaAccessType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAccessType::pool_size};
        returnPointer = &(((SgAdaAccessType*)(SgAdaAccessType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaAccessType*
SgAdaAccessType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaAccessType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAccessType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAccessType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAccessType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaAccessType)};
        unsigned long positionInPool{localIndex % SgAdaAccessType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAccessType::pool_size};
        returnPointer = &(((SgAdaAccessType*)(SgAdaAccessType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaAccessType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaAccessType::pools.begin(); block != SgAdaAccessType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAccessType*>(*block)};
        for (unsigned i = 0; i < SgAdaAccessType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaAccessType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaAccessType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaAccessType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaAccessType::pools.begin(); block != SgAdaAccessType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAccessType*>(*block)};
        for (unsigned i = 0; i < SgAdaAccessType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaAccessType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaAccessType::clearMemoryPool() {
    SgAdaAccessType* tempPointer{nullptr};
    if (SgAdaAccessType::pools.empty() == false) {
        auto block{SgAdaAccessType::pools.begin()};
        SgAdaAccessType::next_node = reinterpret_cast<SgAdaAccessType*>(*block);
        for (; block != SgAdaAccessType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaAccessType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaAccessType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaAccessType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaAccessType::pool_size-1]);
        }
    }
}

void
SgAdaAccessType::deleteMemoryPool() {
    for (auto p: SgAdaAccessType::pools) {
        ROSE_FREE(p);
    }
    SgAdaAccessType::next_node = nullptr;
    SgAdaAccessType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaAccessType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAccessType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAccessType)};
    for (size_t blockIndex = SgAdaAccessType::pools.size(); (blockIndex * SgAdaAccessType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAccessType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAccessType) = %" PRIuPTR " SgAdaAccessType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAccessType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAccessType),SgAdaAccessType::pool_size);
        }
#endif

        SgAdaAccessType* pointer{static_cast<SgAdaAccessType*>(ROSE_MALLOC(SgAdaAccessType::pool_size * sizeof(SgAdaAccessType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaAccessType::pool_size * sizeof(SgAdaAccessType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaAccessType::pool_size * sizeof(SgAdaAccessType));
#endif
        SgAdaAccessType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaAccessType::pool_size * sizeof(SgAdaAccessType), V_SgAdaAccessType));

        if (SgAdaAccessType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaAccessType* blkptr{reinterpret_cast<SgAdaAccessType*>(SgAdaAccessType::pools[blockIndex-1])};
                blkptr[SgAdaAccessType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaAccessType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaAccessType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaAccessType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaAccessType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaAccessType::pool_size - 1};
    SgAdaAccessType* testPointer{reinterpret_cast<SgAdaAccessType*>(SgAdaAccessType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaAccessType::pool_size * (SgAdaAccessType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaAccessTypeStroageClass
 * from its counterpart of SgAdaAccessType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaAccessType::initializeStorageClassArray(SgAdaAccessTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaAccessType::pools.begin(); block != SgAdaAccessType::pools.end(); ++block) {
        SgAdaAccessType* pointer{reinterpret_cast<SgAdaAccessType*>(*block)};
        for (unsigned i = 0; i < SgAdaAccessType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 27934 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaSubtype that takes its corresponding StorageClass as parameter
 */
SgAdaSubtype::SgAdaSubtype(const SgAdaSubtypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 27944 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));
     p_constraint = (SgAdaTypeConstraint*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_constraint));
    p_fromRootType = storageSource.storageOf_fromRootType;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaSubtype*
SgAdaSubtype::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaSubtype* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSubtype)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSubtype + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSubtype)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubtype)};
        unsigned long positionInPool{localIndex % SgAdaSubtype::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSubtype::pool_size};
        returnPointer = &(((SgAdaSubtype*)(SgAdaSubtype::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaSubtype*
SgAdaSubtype::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaSubtype* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSubtype)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSubtype + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSubtype)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaSubtype)};
        unsigned long positionInPool{localIndex % SgAdaSubtype::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSubtype::pool_size};
        returnPointer = &(((SgAdaSubtype*)(SgAdaSubtype::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaSubtype::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaSubtype::pools.begin(); block != SgAdaSubtype::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSubtype*>(*block)};
        for (unsigned i = 0; i < SgAdaSubtype::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaSubtype*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaSubtype::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaSubtype* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaSubtype::pools.begin(); block != SgAdaSubtype::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSubtype*>(*block)};
        for (unsigned i = 0; i < SgAdaSubtype::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaSubtype::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaSubtype::clearMemoryPool() {
    SgAdaSubtype* tempPointer{nullptr};
    if (SgAdaSubtype::pools.empty() == false) {
        auto block{SgAdaSubtype::pools.begin()};
        SgAdaSubtype::next_node = reinterpret_cast<SgAdaSubtype*>(*block);
        for (; block != SgAdaSubtype::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaSubtype*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaSubtype::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaSubtype::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaSubtype::pool_size-1]);
        }
    }
}

void
SgAdaSubtype::deleteMemoryPool() {
    for (auto p: SgAdaSubtype::pools) {
        ROSE_FREE(p);
    }
    SgAdaSubtype::next_node = nullptr;
    SgAdaSubtype::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaSubtype::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubtype) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSubtype)};
    for (size_t blockIndex = SgAdaSubtype::pools.size(); (blockIndex * SgAdaSubtype::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubtype) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSubtype) = %" PRIuPTR " SgAdaSubtype::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubtype),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSubtype),SgAdaSubtype::pool_size);
        }
#endif

        SgAdaSubtype* pointer{static_cast<SgAdaSubtype*>(ROSE_MALLOC(SgAdaSubtype::pool_size * sizeof(SgAdaSubtype)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaSubtype::pool_size * sizeof(SgAdaSubtype));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaSubtype::pool_size * sizeof(SgAdaSubtype));
#endif
        SgAdaSubtype::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaSubtype::pool_size * sizeof(SgAdaSubtype), V_SgAdaSubtype));

        if (SgAdaSubtype::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaSubtype* blkptr{reinterpret_cast<SgAdaSubtype*>(SgAdaSubtype::pools[blockIndex-1])};
                blkptr[SgAdaSubtype::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaSubtype::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaSubtype::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaSubtype::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaSubtype::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaSubtype::pool_size - 1};
    SgAdaSubtype* testPointer{reinterpret_cast<SgAdaSubtype*>(SgAdaSubtype::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaSubtype::pool_size * (SgAdaSubtype::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaSubtypeStroageClass
 * from its counterpart of SgAdaSubtype. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaSubtype::initializeStorageClassArray(SgAdaSubtypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaSubtype::pools.begin(); block != SgAdaSubtype::pools.end(); ++block) {
        SgAdaSubtype* pointer{reinterpret_cast<SgAdaSubtype*>(*block)};
        for (unsigned i = 0; i < SgAdaSubtype::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 28164 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDiscreteType that takes its corresponding StorageClass as parameter
 */
SgAdaDiscreteType::SgAdaDiscreteType(const SgAdaDiscreteTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 28174 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDiscreteType*
SgAdaDiscreteType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDiscreteType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscreteType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscreteType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscreteType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscreteType)};
        unsigned long positionInPool{localIndex % SgAdaDiscreteType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscreteType::pool_size};
        returnPointer = &(((SgAdaDiscreteType*)(SgAdaDiscreteType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDiscreteType*
SgAdaDiscreteType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDiscreteType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscreteType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscreteType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscreteType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDiscreteType)};
        unsigned long positionInPool{localIndex % SgAdaDiscreteType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscreteType::pool_size};
        returnPointer = &(((SgAdaDiscreteType*)(SgAdaDiscreteType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDiscreteType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDiscreteType::pools.begin(); block != SgAdaDiscreteType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscreteType*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscreteType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDiscreteType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDiscreteType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDiscreteType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDiscreteType::pools.begin(); block != SgAdaDiscreteType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscreteType*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscreteType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDiscreteType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDiscreteType::clearMemoryPool() {
    SgAdaDiscreteType* tempPointer{nullptr};
    if (SgAdaDiscreteType::pools.empty() == false) {
        auto block{SgAdaDiscreteType::pools.begin()};
        SgAdaDiscreteType::next_node = reinterpret_cast<SgAdaDiscreteType*>(*block);
        for (; block != SgAdaDiscreteType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDiscreteType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDiscreteType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDiscreteType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDiscreteType::pool_size-1]);
        }
    }
}

void
SgAdaDiscreteType::deleteMemoryPool() {
    for (auto p: SgAdaDiscreteType::pools) {
        ROSE_FREE(p);
    }
    SgAdaDiscreteType::next_node = nullptr;
    SgAdaDiscreteType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDiscreteType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscreteType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscreteType)};
    for (size_t blockIndex = SgAdaDiscreteType::pools.size(); (blockIndex * SgAdaDiscreteType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscreteType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscreteType) = %" PRIuPTR " SgAdaDiscreteType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscreteType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscreteType),SgAdaDiscreteType::pool_size);
        }
#endif

        SgAdaDiscreteType* pointer{static_cast<SgAdaDiscreteType*>(ROSE_MALLOC(SgAdaDiscreteType::pool_size * sizeof(SgAdaDiscreteType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDiscreteType::pool_size * sizeof(SgAdaDiscreteType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDiscreteType::pool_size * sizeof(SgAdaDiscreteType));
#endif
        SgAdaDiscreteType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDiscreteType::pool_size * sizeof(SgAdaDiscreteType), V_SgAdaDiscreteType));

        if (SgAdaDiscreteType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDiscreteType* blkptr{reinterpret_cast<SgAdaDiscreteType*>(SgAdaDiscreteType::pools[blockIndex-1])};
                blkptr[SgAdaDiscreteType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDiscreteType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDiscreteType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDiscreteType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDiscreteType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDiscreteType::pool_size - 1};
    SgAdaDiscreteType* testPointer{reinterpret_cast<SgAdaDiscreteType*>(SgAdaDiscreteType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDiscreteType::pool_size * (SgAdaDiscreteType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDiscreteTypeStroageClass
 * from its counterpart of SgAdaDiscreteType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDiscreteType::initializeStorageClassArray(SgAdaDiscreteTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDiscreteType::pools.begin(); block != SgAdaDiscreteType::pools.end(); ++block) {
        SgAdaDiscreteType* pointer{reinterpret_cast<SgAdaDiscreteType*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscreteType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 28391 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDerivedType that takes its corresponding StorageClass as parameter
 */
SgAdaDerivedType::SgAdaDerivedType(const SgAdaDerivedTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 28401 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDerivedType*
SgAdaDerivedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDerivedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDerivedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDerivedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDerivedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDerivedType)};
        unsigned long positionInPool{localIndex % SgAdaDerivedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDerivedType::pool_size};
        returnPointer = &(((SgAdaDerivedType*)(SgAdaDerivedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDerivedType*
SgAdaDerivedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDerivedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDerivedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDerivedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDerivedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDerivedType)};
        unsigned long positionInPool{localIndex % SgAdaDerivedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDerivedType::pool_size};
        returnPointer = &(((SgAdaDerivedType*)(SgAdaDerivedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDerivedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDerivedType::pools.begin(); block != SgAdaDerivedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDerivedType*>(*block)};
        for (unsigned i = 0; i < SgAdaDerivedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDerivedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDerivedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDerivedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDerivedType::pools.begin(); block != SgAdaDerivedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDerivedType*>(*block)};
        for (unsigned i = 0; i < SgAdaDerivedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDerivedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDerivedType::clearMemoryPool() {
    SgAdaDerivedType* tempPointer{nullptr};
    if (SgAdaDerivedType::pools.empty() == false) {
        auto block{SgAdaDerivedType::pools.begin()};
        SgAdaDerivedType::next_node = reinterpret_cast<SgAdaDerivedType*>(*block);
        for (; block != SgAdaDerivedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDerivedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDerivedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDerivedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDerivedType::pool_size-1]);
        }
    }
}

void
SgAdaDerivedType::deleteMemoryPool() {
    for (auto p: SgAdaDerivedType::pools) {
        ROSE_FREE(p);
    }
    SgAdaDerivedType::next_node = nullptr;
    SgAdaDerivedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDerivedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDerivedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDerivedType)};
    for (size_t blockIndex = SgAdaDerivedType::pools.size(); (blockIndex * SgAdaDerivedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDerivedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDerivedType) = %" PRIuPTR " SgAdaDerivedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDerivedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDerivedType),SgAdaDerivedType::pool_size);
        }
#endif

        SgAdaDerivedType* pointer{static_cast<SgAdaDerivedType*>(ROSE_MALLOC(SgAdaDerivedType::pool_size * sizeof(SgAdaDerivedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDerivedType::pool_size * sizeof(SgAdaDerivedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDerivedType::pool_size * sizeof(SgAdaDerivedType));
#endif
        SgAdaDerivedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDerivedType::pool_size * sizeof(SgAdaDerivedType), V_SgAdaDerivedType));

        if (SgAdaDerivedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDerivedType* blkptr{reinterpret_cast<SgAdaDerivedType*>(SgAdaDerivedType::pools[blockIndex-1])};
                blkptr[SgAdaDerivedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDerivedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDerivedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDerivedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDerivedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDerivedType::pool_size - 1};
    SgAdaDerivedType* testPointer{reinterpret_cast<SgAdaDerivedType*>(SgAdaDerivedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDerivedType::pool_size * (SgAdaDerivedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDerivedTypeStroageClass
 * from its counterpart of SgAdaDerivedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDerivedType::initializeStorageClassArray(SgAdaDerivedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDerivedType::pools.begin(); block != SgAdaDerivedType::pools.end(); ++block) {
        SgAdaDerivedType* pointer{reinterpret_cast<SgAdaDerivedType*>(*block)};
        for (unsigned i = 0; i < SgAdaDerivedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 28619 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaSubroutineType that takes its corresponding StorageClass as parameter
 */
SgAdaSubroutineType::SgAdaSubroutineType(const SgAdaSubroutineTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 28629 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_parameterList = (SgFunctionParameterList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameterList));
     p_functionParameterScope = (SgFunctionParameterScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_functionParameterScope));
     p_return_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_return_type));
    p_is_protected = storageSource.storageOf_is_protected;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaSubroutineType*
SgAdaSubroutineType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaSubroutineType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSubroutineType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSubroutineType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSubroutineType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubroutineType)};
        unsigned long positionInPool{localIndex % SgAdaSubroutineType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSubroutineType::pool_size};
        returnPointer = &(((SgAdaSubroutineType*)(SgAdaSubroutineType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaSubroutineType*
SgAdaSubroutineType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaSubroutineType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSubroutineType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSubroutineType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSubroutineType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaSubroutineType)};
        unsigned long positionInPool{localIndex % SgAdaSubroutineType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSubroutineType::pool_size};
        returnPointer = &(((SgAdaSubroutineType*)(SgAdaSubroutineType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaSubroutineType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaSubroutineType::pools.begin(); block != SgAdaSubroutineType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSubroutineType*>(*block)};
        for (unsigned i = 0; i < SgAdaSubroutineType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaSubroutineType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaSubroutineType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaSubroutineType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaSubroutineType::pools.begin(); block != SgAdaSubroutineType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSubroutineType*>(*block)};
        for (unsigned i = 0; i < SgAdaSubroutineType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaSubroutineType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaSubroutineType::clearMemoryPool() {
    SgAdaSubroutineType* tempPointer{nullptr};
    if (SgAdaSubroutineType::pools.empty() == false) {
        auto block{SgAdaSubroutineType::pools.begin()};
        SgAdaSubroutineType::next_node = reinterpret_cast<SgAdaSubroutineType*>(*block);
        for (; block != SgAdaSubroutineType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaSubroutineType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaSubroutineType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaSubroutineType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaSubroutineType::pool_size-1]);
        }
    }
}

void
SgAdaSubroutineType::deleteMemoryPool() {
    for (auto p: SgAdaSubroutineType::pools) {
        ROSE_FREE(p);
    }
    SgAdaSubroutineType::next_node = nullptr;
    SgAdaSubroutineType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaSubroutineType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubroutineType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSubroutineType)};
    for (size_t blockIndex = SgAdaSubroutineType::pools.size(); (blockIndex * SgAdaSubroutineType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubroutineType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSubroutineType) = %" PRIuPTR " SgAdaSubroutineType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSubroutineType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSubroutineType),SgAdaSubroutineType::pool_size);
        }
#endif

        SgAdaSubroutineType* pointer{static_cast<SgAdaSubroutineType*>(ROSE_MALLOC(SgAdaSubroutineType::pool_size * sizeof(SgAdaSubroutineType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaSubroutineType::pool_size * sizeof(SgAdaSubroutineType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaSubroutineType::pool_size * sizeof(SgAdaSubroutineType));
#endif
        SgAdaSubroutineType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaSubroutineType::pool_size * sizeof(SgAdaSubroutineType), V_SgAdaSubroutineType));

        if (SgAdaSubroutineType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaSubroutineType* blkptr{reinterpret_cast<SgAdaSubroutineType*>(SgAdaSubroutineType::pools[blockIndex-1])};
                blkptr[SgAdaSubroutineType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaSubroutineType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaSubroutineType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaSubroutineType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaSubroutineType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaSubroutineType::pool_size - 1};
    SgAdaSubroutineType* testPointer{reinterpret_cast<SgAdaSubroutineType*>(SgAdaSubroutineType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaSubroutineType::pool_size * (SgAdaSubroutineType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaSubroutineTypeStroageClass
 * from its counterpart of SgAdaSubroutineType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaSubroutineType::initializeStorageClassArray(SgAdaSubroutineTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaSubroutineType::pools.begin(); block != SgAdaSubroutineType::pools.end(); ++block) {
        SgAdaSubroutineType* pointer{reinterpret_cast<SgAdaSubroutineType*>(*block)};
        for (unsigned i = 0; i < SgAdaSubroutineType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 28850 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIntegralType that takes its corresponding StorageClass as parameter
 */
SgIntegralType::SgIntegralType(const SgIntegralTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 28860 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIntegralType*
SgIntegralType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIntegralType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegralType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegralType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegralType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegralType)};
        unsigned long positionInPool{localIndex % SgIntegralType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntegralType::pool_size};
        returnPointer = &(((SgIntegralType*)(SgIntegralType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIntegralType*
SgIntegralType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIntegralType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegralType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegralType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegralType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIntegralType)};
        unsigned long positionInPool{localIndex % SgIntegralType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntegralType::pool_size};
        returnPointer = &(((SgIntegralType*)(SgIntegralType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIntegralType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIntegralType::pools.begin(); block != SgIntegralType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntegralType*>(*block)};
        for (unsigned i = 0; i < SgIntegralType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIntegralType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIntegralType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIntegralType* pointerOfLinkedList{nullptr};
    for (auto block = SgIntegralType::pools.begin(); block != SgIntegralType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntegralType*>(*block)};
        for (unsigned i = 0; i < SgIntegralType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIntegralType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIntegralType::clearMemoryPool() {
    SgIntegralType* tempPointer{nullptr};
    if (SgIntegralType::pools.empty() == false) {
        auto block{SgIntegralType::pools.begin()};
        SgIntegralType::next_node = reinterpret_cast<SgIntegralType*>(*block);
        for (; block != SgIntegralType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIntegralType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIntegralType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIntegralType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIntegralType::pool_size-1]);
        }
    }
}

void
SgIntegralType::deleteMemoryPool() {
    for (auto p: SgIntegralType::pools) {
        ROSE_FREE(p);
    }
    SgIntegralType::next_node = nullptr;
    SgIntegralType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIntegralType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegralType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegralType)};
    for (size_t blockIndex = SgIntegralType::pools.size(); (blockIndex * SgIntegralType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegralType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegralType) = %" PRIuPTR " SgIntegralType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegralType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegralType),SgIntegralType::pool_size);
        }
#endif

        SgIntegralType* pointer{static_cast<SgIntegralType*>(ROSE_MALLOC(SgIntegralType::pool_size * sizeof(SgIntegralType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIntegralType::pool_size * sizeof(SgIntegralType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIntegralType::pool_size * sizeof(SgIntegralType));
#endif
        SgIntegralType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIntegralType::pool_size * sizeof(SgIntegralType), V_SgIntegralType));

        if (SgIntegralType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIntegralType* blkptr{reinterpret_cast<SgIntegralType*>(SgIntegralType::pools[blockIndex-1])};
                blkptr[SgIntegralType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIntegralType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIntegralType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIntegralType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIntegralType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIntegralType::pool_size - 1};
    SgIntegralType* testPointer{reinterpret_cast<SgIntegralType*>(SgIntegralType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIntegralType::pool_size * (SgIntegralType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIntegralTypeStroageClass
 * from its counterpart of SgIntegralType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIntegralType::initializeStorageClassArray(SgIntegralTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIntegralType::pools.begin(); block != SgIntegralType::pools.end(); ++block) {
        SgIntegralType* pointer{reinterpret_cast<SgIntegralType*>(*block)};
        for (unsigned i = 0; i < SgIntegralType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 29077 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSignedIntegralType that takes its corresponding StorageClass as parameter
 */
SgSignedIntegralType::SgSignedIntegralType(const SgSignedIntegralTypeStorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 29087 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSignedIntegralType*
SgSignedIntegralType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSignedIntegralType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSignedIntegralType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSignedIntegralType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSignedIntegralType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedIntegralType)};
        unsigned long positionInPool{localIndex % SgSignedIntegralType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSignedIntegralType::pool_size};
        returnPointer = &(((SgSignedIntegralType*)(SgSignedIntegralType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSignedIntegralType*
SgSignedIntegralType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSignedIntegralType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSignedIntegralType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSignedIntegralType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSignedIntegralType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSignedIntegralType)};
        unsigned long positionInPool{localIndex % SgSignedIntegralType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSignedIntegralType::pool_size};
        returnPointer = &(((SgSignedIntegralType*)(SgSignedIntegralType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSignedIntegralType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSignedIntegralType::pools.begin(); block != SgSignedIntegralType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSignedIntegralType*>(*block)};
        for (unsigned i = 0; i < SgSignedIntegralType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSignedIntegralType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSignedIntegralType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSignedIntegralType* pointerOfLinkedList{nullptr};
    for (auto block = SgSignedIntegralType::pools.begin(); block != SgSignedIntegralType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSignedIntegralType*>(*block)};
        for (unsigned i = 0; i < SgSignedIntegralType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSignedIntegralType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSignedIntegralType::clearMemoryPool() {
    SgSignedIntegralType* tempPointer{nullptr};
    if (SgSignedIntegralType::pools.empty() == false) {
        auto block{SgSignedIntegralType::pools.begin()};
        SgSignedIntegralType::next_node = reinterpret_cast<SgSignedIntegralType*>(*block);
        for (; block != SgSignedIntegralType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSignedIntegralType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSignedIntegralType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSignedIntegralType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSignedIntegralType::pool_size-1]);
        }
    }
}

void
SgSignedIntegralType::deleteMemoryPool() {
    for (auto p: SgSignedIntegralType::pools) {
        ROSE_FREE(p);
    }
    SgSignedIntegralType::next_node = nullptr;
    SgSignedIntegralType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSignedIntegralType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedIntegralType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSignedIntegralType)};
    for (size_t blockIndex = SgSignedIntegralType::pools.size(); (blockIndex * SgSignedIntegralType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedIntegralType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSignedIntegralType) = %" PRIuPTR " SgSignedIntegralType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedIntegralType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSignedIntegralType),SgSignedIntegralType::pool_size);
        }
#endif

        SgSignedIntegralType* pointer{static_cast<SgSignedIntegralType*>(ROSE_MALLOC(SgSignedIntegralType::pool_size * sizeof(SgSignedIntegralType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSignedIntegralType::pool_size * sizeof(SgSignedIntegralType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSignedIntegralType::pool_size * sizeof(SgSignedIntegralType));
#endif
        SgSignedIntegralType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSignedIntegralType::pool_size * sizeof(SgSignedIntegralType), V_SgSignedIntegralType));

        if (SgSignedIntegralType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSignedIntegralType* blkptr{reinterpret_cast<SgSignedIntegralType*>(SgSignedIntegralType::pools[blockIndex-1])};
                blkptr[SgSignedIntegralType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSignedIntegralType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSignedIntegralType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSignedIntegralType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSignedIntegralType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSignedIntegralType::pool_size - 1};
    SgSignedIntegralType* testPointer{reinterpret_cast<SgSignedIntegralType*>(SgSignedIntegralType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSignedIntegralType::pool_size * (SgSignedIntegralType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSignedIntegralTypeStroageClass
 * from its counterpart of SgSignedIntegralType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSignedIntegralType::initializeStorageClassArray(SgSignedIntegralTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSignedIntegralType::pools.begin(); block != SgSignedIntegralType::pools.end(); ++block) {
        SgSignedIntegralType* pointer{reinterpret_cast<SgSignedIntegralType*>(*block)};
        for (unsigned i = 0; i < SgSignedIntegralType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 29304 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeInt that takes its corresponding StorageClass as parameter
 */
SgTypeInt::SgTypeInt(const SgTypeIntStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 29314 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_field_size = storageSource.storageOf_field_size;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeInt*
SgTypeInt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeInt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeInt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeInt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeInt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeInt)};
        unsigned long positionInPool{localIndex % SgTypeInt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeInt::pool_size};
        returnPointer = &(((SgTypeInt*)(SgTypeInt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeInt*
SgTypeInt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeInt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeInt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeInt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeInt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeInt)};
        unsigned long positionInPool{localIndex % SgTypeInt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeInt::pool_size};
        returnPointer = &(((SgTypeInt*)(SgTypeInt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeInt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeInt::pools.begin(); block != SgTypeInt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeInt*>(*block)};
        for (unsigned i = 0; i < SgTypeInt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeInt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeInt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeInt* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeInt::pools.begin(); block != SgTypeInt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeInt*>(*block)};
        for (unsigned i = 0; i < SgTypeInt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeInt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeInt::clearMemoryPool() {
    SgTypeInt* tempPointer{nullptr};
    if (SgTypeInt::pools.empty() == false) {
        auto block{SgTypeInt::pools.begin()};
        SgTypeInt::next_node = reinterpret_cast<SgTypeInt*>(*block);
        for (; block != SgTypeInt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeInt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeInt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeInt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeInt::pool_size-1]);
        }
    }
}

void
SgTypeInt::deleteMemoryPool() {
    for (auto p: SgTypeInt::pools) {
        ROSE_FREE(p);
    }
    SgTypeInt::next_node = nullptr;
    SgTypeInt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeInt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeInt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeInt)};
    for (size_t blockIndex = SgTypeInt::pools.size(); (blockIndex * SgTypeInt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeInt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeInt) = %" PRIuPTR " SgTypeInt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeInt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeInt),SgTypeInt::pool_size);
        }
#endif

        SgTypeInt* pointer{static_cast<SgTypeInt*>(ROSE_MALLOC(SgTypeInt::pool_size * sizeof(SgTypeInt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeInt::pool_size * sizeof(SgTypeInt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeInt::pool_size * sizeof(SgTypeInt));
#endif
        SgTypeInt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeInt::pool_size * sizeof(SgTypeInt), V_SgTypeInt));

        if (SgTypeInt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeInt* blkptr{reinterpret_cast<SgTypeInt*>(SgTypeInt::pools[blockIndex-1])};
                blkptr[SgTypeInt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeInt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeInt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeInt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeInt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeInt::pool_size - 1};
    SgTypeInt* testPointer{reinterpret_cast<SgTypeInt*>(SgTypeInt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeInt::pool_size * (SgTypeInt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeIntStroageClass
 * from its counterpart of SgTypeInt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeInt::initializeStorageClassArray(SgTypeIntStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeInt::pools.begin(); block != SgTypeInt::pools.end(); ++block) {
        SgTypeInt* pointer{reinterpret_cast<SgTypeInt*>(*block)};
        for (unsigned i = 0; i < SgTypeInt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 29532 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeLong that takes its corresponding StorageClass as parameter
 */
SgTypeLong::SgTypeLong(const SgTypeLongStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 29542 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeLong*
SgTypeLong::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLong)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLong)};
        unsigned long positionInPool{localIndex % SgTypeLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLong::pool_size};
        returnPointer = &(((SgTypeLong*)(SgTypeLong::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeLong*
SgTypeLong::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLong)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeLong)};
        unsigned long positionInPool{localIndex % SgTypeLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLong::pool_size};
        returnPointer = &(((SgTypeLong*)(SgTypeLong::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeLong::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeLong::pools.begin(); block != SgTypeLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLong*>(*block)};
        for (unsigned i = 0; i < SgTypeLong::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeLong*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeLong::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeLong* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeLong::pools.begin(); block != SgTypeLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLong*>(*block)};
        for (unsigned i = 0; i < SgTypeLong::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeLong::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeLong::clearMemoryPool() {
    SgTypeLong* tempPointer{nullptr};
    if (SgTypeLong::pools.empty() == false) {
        auto block{SgTypeLong::pools.begin()};
        SgTypeLong::next_node = reinterpret_cast<SgTypeLong*>(*block);
        for (; block != SgTypeLong::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeLong*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeLong::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeLong::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeLong::pool_size-1]);
        }
    }
}

void
SgTypeLong::deleteMemoryPool() {
    for (auto p: SgTypeLong::pools) {
        ROSE_FREE(p);
    }
    SgTypeLong::next_node = nullptr;
    SgTypeLong::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeLong::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLong) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLong)};
    for (size_t blockIndex = SgTypeLong::pools.size(); (blockIndex * SgTypeLong::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLong) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLong) = %" PRIuPTR " SgTypeLong::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLong),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLong),SgTypeLong::pool_size);
        }
#endif

        SgTypeLong* pointer{static_cast<SgTypeLong*>(ROSE_MALLOC(SgTypeLong::pool_size * sizeof(SgTypeLong)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeLong::pool_size * sizeof(SgTypeLong));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeLong::pool_size * sizeof(SgTypeLong));
#endif
        SgTypeLong::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeLong::pool_size * sizeof(SgTypeLong), V_SgTypeLong));

        if (SgTypeLong::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeLong* blkptr{reinterpret_cast<SgTypeLong*>(SgTypeLong::pools[blockIndex-1])};
                blkptr[SgTypeLong::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeLong::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeLong::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeLong::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeLong::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeLong::pool_size - 1};
    SgTypeLong* testPointer{reinterpret_cast<SgTypeLong*>(SgTypeLong::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeLong::pool_size * (SgTypeLong::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeLongStroageClass
 * from its counterpart of SgTypeLong. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeLong::initializeStorageClassArray(SgTypeLongStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeLong::pools.begin(); block != SgTypeLong::pools.end(); ++block) {
        SgTypeLong* pointer{reinterpret_cast<SgTypeLong*>(*block)};
        for (unsigned i = 0; i < SgTypeLong::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 29759 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeLongLong that takes its corresponding StorageClass as parameter
 */
SgTypeLongLong::SgTypeLongLong(const SgTypeLongLongStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 29769 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeLongLong*
SgTypeLongLong::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeLongLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLongLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLongLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLongLong)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongLong)};
        unsigned long positionInPool{localIndex % SgTypeLongLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLongLong::pool_size};
        returnPointer = &(((SgTypeLongLong*)(SgTypeLongLong::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeLongLong*
SgTypeLongLong::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeLongLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLongLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLongLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLongLong)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeLongLong)};
        unsigned long positionInPool{localIndex % SgTypeLongLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLongLong::pool_size};
        returnPointer = &(((SgTypeLongLong*)(SgTypeLongLong::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeLongLong::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeLongLong::pools.begin(); block != SgTypeLongLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeLongLong::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeLongLong*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeLongLong::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeLongLong* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeLongLong::pools.begin(); block != SgTypeLongLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeLongLong::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeLongLong::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeLongLong::clearMemoryPool() {
    SgTypeLongLong* tempPointer{nullptr};
    if (SgTypeLongLong::pools.empty() == false) {
        auto block{SgTypeLongLong::pools.begin()};
        SgTypeLongLong::next_node = reinterpret_cast<SgTypeLongLong*>(*block);
        for (; block != SgTypeLongLong::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeLongLong*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeLongLong::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeLongLong::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeLongLong::pool_size-1]);
        }
    }
}

void
SgTypeLongLong::deleteMemoryPool() {
    for (auto p: SgTypeLongLong::pools) {
        ROSE_FREE(p);
    }
    SgTypeLongLong::next_node = nullptr;
    SgTypeLongLong::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeLongLong::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongLong) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLongLong)};
    for (size_t blockIndex = SgTypeLongLong::pools.size(); (blockIndex * SgTypeLongLong::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongLong) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLongLong) = %" PRIuPTR " SgTypeLongLong::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongLong),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLongLong),SgTypeLongLong::pool_size);
        }
#endif

        SgTypeLongLong* pointer{static_cast<SgTypeLongLong*>(ROSE_MALLOC(SgTypeLongLong::pool_size * sizeof(SgTypeLongLong)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeLongLong::pool_size * sizeof(SgTypeLongLong));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeLongLong::pool_size * sizeof(SgTypeLongLong));
#endif
        SgTypeLongLong::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeLongLong::pool_size * sizeof(SgTypeLongLong), V_SgTypeLongLong));

        if (SgTypeLongLong::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeLongLong* blkptr{reinterpret_cast<SgTypeLongLong*>(SgTypeLongLong::pools[blockIndex-1])};
                blkptr[SgTypeLongLong::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeLongLong::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeLongLong::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeLongLong::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeLongLong::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeLongLong::pool_size - 1};
    SgTypeLongLong* testPointer{reinterpret_cast<SgTypeLongLong*>(SgTypeLongLong::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeLongLong::pool_size * (SgTypeLongLong::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeLongLongStroageClass
 * from its counterpart of SgTypeLongLong. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeLongLong::initializeStorageClassArray(SgTypeLongLongStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeLongLong::pools.begin(); block != SgTypeLongLong::pools.end(); ++block) {
        SgTypeLongLong* pointer{reinterpret_cast<SgTypeLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeLongLong::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 29986 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeShort that takes its corresponding StorageClass as parameter
 */
SgTypeShort::SgTypeShort(const SgTypeShortStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 29996 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeShort*
SgTypeShort::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeShort* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeShort)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeShort + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeShort)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeShort)};
        unsigned long positionInPool{localIndex % SgTypeShort::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeShort::pool_size};
        returnPointer = &(((SgTypeShort*)(SgTypeShort::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeShort*
SgTypeShort::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeShort* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeShort)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeShort + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeShort)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeShort)};
        unsigned long positionInPool{localIndex % SgTypeShort::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeShort::pool_size};
        returnPointer = &(((SgTypeShort*)(SgTypeShort::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeShort::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeShort::pools.begin(); block != SgTypeShort::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeShort*>(*block)};
        for (unsigned i = 0; i < SgTypeShort::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeShort*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeShort::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeShort* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeShort::pools.begin(); block != SgTypeShort::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeShort*>(*block)};
        for (unsigned i = 0; i < SgTypeShort::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeShort::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeShort::clearMemoryPool() {
    SgTypeShort* tempPointer{nullptr};
    if (SgTypeShort::pools.empty() == false) {
        auto block{SgTypeShort::pools.begin()};
        SgTypeShort::next_node = reinterpret_cast<SgTypeShort*>(*block);
        for (; block != SgTypeShort::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeShort*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeShort::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeShort::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeShort::pool_size-1]);
        }
    }
}

void
SgTypeShort::deleteMemoryPool() {
    for (auto p: SgTypeShort::pools) {
        ROSE_FREE(p);
    }
    SgTypeShort::next_node = nullptr;
    SgTypeShort::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeShort::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeShort) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeShort)};
    for (size_t blockIndex = SgTypeShort::pools.size(); (blockIndex * SgTypeShort::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeShort) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeShort) = %" PRIuPTR " SgTypeShort::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeShort),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeShort),SgTypeShort::pool_size);
        }
#endif

        SgTypeShort* pointer{static_cast<SgTypeShort*>(ROSE_MALLOC(SgTypeShort::pool_size * sizeof(SgTypeShort)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeShort::pool_size * sizeof(SgTypeShort));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeShort::pool_size * sizeof(SgTypeShort));
#endif
        SgTypeShort::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeShort::pool_size * sizeof(SgTypeShort), V_SgTypeShort));

        if (SgTypeShort::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeShort* blkptr{reinterpret_cast<SgTypeShort*>(SgTypeShort::pools[blockIndex-1])};
                blkptr[SgTypeShort::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeShort::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeShort::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeShort::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeShort::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeShort::pool_size - 1};
    SgTypeShort* testPointer{reinterpret_cast<SgTypeShort*>(SgTypeShort::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeShort::pool_size * (SgTypeShort::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeShortStroageClass
 * from its counterpart of SgTypeShort. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeShort::initializeStorageClassArray(SgTypeShortStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeShort::pools.begin(); block != SgTypeShort::pools.end(); ++block) {
        SgTypeShort* pointer{reinterpret_cast<SgTypeShort*>(*block)};
        for (unsigned i = 0; i < SgTypeShort::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 30213 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeSigned128bitInteger that takes its corresponding StorageClass as parameter
 */
SgTypeSigned128bitInteger::SgTypeSigned128bitInteger(const SgTypeSigned128bitIntegerStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 30223 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeSigned128bitInteger*
SgTypeSigned128bitInteger::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeSigned128bitInteger* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSigned128bitInteger)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSigned128bitInteger + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSigned128bitInteger)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSigned128bitInteger)};
        unsigned long positionInPool{localIndex % SgTypeSigned128bitInteger::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSigned128bitInteger::pool_size};
        returnPointer = &(((SgTypeSigned128bitInteger*)(SgTypeSigned128bitInteger::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeSigned128bitInteger*
SgTypeSigned128bitInteger::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeSigned128bitInteger* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSigned128bitInteger)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSigned128bitInteger + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSigned128bitInteger)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeSigned128bitInteger)};
        unsigned long positionInPool{localIndex % SgTypeSigned128bitInteger::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSigned128bitInteger::pool_size};
        returnPointer = &(((SgTypeSigned128bitInteger*)(SgTypeSigned128bitInteger::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeSigned128bitInteger::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeSigned128bitInteger::pools.begin(); block != SgTypeSigned128bitInteger::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSigned128bitInteger*>(*block)};
        for (unsigned i = 0; i < SgTypeSigned128bitInteger::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeSigned128bitInteger*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeSigned128bitInteger::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeSigned128bitInteger* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeSigned128bitInteger::pools.begin(); block != SgTypeSigned128bitInteger::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSigned128bitInteger*>(*block)};
        for (unsigned i = 0; i < SgTypeSigned128bitInteger::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeSigned128bitInteger::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeSigned128bitInteger::clearMemoryPool() {
    SgTypeSigned128bitInteger* tempPointer{nullptr};
    if (SgTypeSigned128bitInteger::pools.empty() == false) {
        auto block{SgTypeSigned128bitInteger::pools.begin()};
        SgTypeSigned128bitInteger::next_node = reinterpret_cast<SgTypeSigned128bitInteger*>(*block);
        for (; block != SgTypeSigned128bitInteger::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeSigned128bitInteger*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeSigned128bitInteger::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeSigned128bitInteger::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeSigned128bitInteger::pool_size-1]);
        }
    }
}

void
SgTypeSigned128bitInteger::deleteMemoryPool() {
    for (auto p: SgTypeSigned128bitInteger::pools) {
        ROSE_FREE(p);
    }
    SgTypeSigned128bitInteger::next_node = nullptr;
    SgTypeSigned128bitInteger::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeSigned128bitInteger::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSigned128bitInteger) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSigned128bitInteger)};
    for (size_t blockIndex = SgTypeSigned128bitInteger::pools.size(); (blockIndex * SgTypeSigned128bitInteger::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSigned128bitInteger) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSigned128bitInteger) = %" PRIuPTR " SgTypeSigned128bitInteger::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSigned128bitInteger),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSigned128bitInteger),SgTypeSigned128bitInteger::pool_size);
        }
#endif

        SgTypeSigned128bitInteger* pointer{static_cast<SgTypeSigned128bitInteger*>(ROSE_MALLOC(SgTypeSigned128bitInteger::pool_size * sizeof(SgTypeSigned128bitInteger)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeSigned128bitInteger::pool_size * sizeof(SgTypeSigned128bitInteger));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeSigned128bitInteger::pool_size * sizeof(SgTypeSigned128bitInteger));
#endif
        SgTypeSigned128bitInteger::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeSigned128bitInteger::pool_size * sizeof(SgTypeSigned128bitInteger), V_SgTypeSigned128bitInteger));

        if (SgTypeSigned128bitInteger::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeSigned128bitInteger* blkptr{reinterpret_cast<SgTypeSigned128bitInteger*>(SgTypeSigned128bitInteger::pools[blockIndex-1])};
                blkptr[SgTypeSigned128bitInteger::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeSigned128bitInteger::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeSigned128bitInteger::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeSigned128bitInteger::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeSigned128bitInteger::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeSigned128bitInteger::pool_size - 1};
    SgTypeSigned128bitInteger* testPointer{reinterpret_cast<SgTypeSigned128bitInteger*>(SgTypeSigned128bitInteger::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeSigned128bitInteger::pool_size * (SgTypeSigned128bitInteger::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeSigned128bitIntegerStroageClass
 * from its counterpart of SgTypeSigned128bitInteger. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeSigned128bitInteger::initializeStorageClassArray(SgTypeSigned128bitIntegerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeSigned128bitInteger::pools.begin(); block != SgTypeSigned128bitInteger::pools.end(); ++block) {
        SgTypeSigned128bitInteger* pointer{reinterpret_cast<SgTypeSigned128bitInteger*>(*block)};
        for (unsigned i = 0; i < SgTypeSigned128bitInteger::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 30440 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeSignedChar that takes its corresponding StorageClass as parameter
 */
SgTypeSignedChar::SgTypeSignedChar(const SgTypeSignedCharStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 30450 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeSignedChar*
SgTypeSignedChar::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeSignedChar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedChar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedChar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedChar)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedChar)};
        unsigned long positionInPool{localIndex % SgTypeSignedChar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedChar::pool_size};
        returnPointer = &(((SgTypeSignedChar*)(SgTypeSignedChar::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeSignedChar*
SgTypeSignedChar::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeSignedChar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedChar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedChar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedChar)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeSignedChar)};
        unsigned long positionInPool{localIndex % SgTypeSignedChar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedChar::pool_size};
        returnPointer = &(((SgTypeSignedChar*)(SgTypeSignedChar::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeSignedChar::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeSignedChar::pools.begin(); block != SgTypeSignedChar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedChar*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedChar::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeSignedChar*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeSignedChar::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeSignedChar* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeSignedChar::pools.begin(); block != SgTypeSignedChar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedChar*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedChar::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeSignedChar::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeSignedChar::clearMemoryPool() {
    SgTypeSignedChar* tempPointer{nullptr};
    if (SgTypeSignedChar::pools.empty() == false) {
        auto block{SgTypeSignedChar::pools.begin()};
        SgTypeSignedChar::next_node = reinterpret_cast<SgTypeSignedChar*>(*block);
        for (; block != SgTypeSignedChar::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeSignedChar*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeSignedChar::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeSignedChar::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeSignedChar::pool_size-1]);
        }
    }
}

void
SgTypeSignedChar::deleteMemoryPool() {
    for (auto p: SgTypeSignedChar::pools) {
        ROSE_FREE(p);
    }
    SgTypeSignedChar::next_node = nullptr;
    SgTypeSignedChar::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeSignedChar::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedChar) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedChar)};
    for (size_t blockIndex = SgTypeSignedChar::pools.size(); (blockIndex * SgTypeSignedChar::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedChar) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedChar) = %" PRIuPTR " SgTypeSignedChar::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedChar),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedChar),SgTypeSignedChar::pool_size);
        }
#endif

        SgTypeSignedChar* pointer{static_cast<SgTypeSignedChar*>(ROSE_MALLOC(SgTypeSignedChar::pool_size * sizeof(SgTypeSignedChar)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeSignedChar::pool_size * sizeof(SgTypeSignedChar));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeSignedChar::pool_size * sizeof(SgTypeSignedChar));
#endif
        SgTypeSignedChar::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeSignedChar::pool_size * sizeof(SgTypeSignedChar), V_SgTypeSignedChar));

        if (SgTypeSignedChar::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeSignedChar* blkptr{reinterpret_cast<SgTypeSignedChar*>(SgTypeSignedChar::pools[blockIndex-1])};
                blkptr[SgTypeSignedChar::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeSignedChar::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeSignedChar::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeSignedChar::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeSignedChar::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeSignedChar::pool_size - 1};
    SgTypeSignedChar* testPointer{reinterpret_cast<SgTypeSignedChar*>(SgTypeSignedChar::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeSignedChar::pool_size * (SgTypeSignedChar::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeSignedCharStroageClass
 * from its counterpart of SgTypeSignedChar. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeSignedChar::initializeStorageClassArray(SgTypeSignedCharStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeSignedChar::pools.begin(); block != SgTypeSignedChar::pools.end(); ++block) {
        SgTypeSignedChar* pointer{reinterpret_cast<SgTypeSignedChar*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedChar::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 30667 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeSignedInt that takes its corresponding StorageClass as parameter
 */
SgTypeSignedInt::SgTypeSignedInt(const SgTypeSignedIntStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 30677 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeSignedInt*
SgTypeSignedInt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeSignedInt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedInt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedInt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedInt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedInt)};
        unsigned long positionInPool{localIndex % SgTypeSignedInt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedInt::pool_size};
        returnPointer = &(((SgTypeSignedInt*)(SgTypeSignedInt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeSignedInt*
SgTypeSignedInt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeSignedInt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedInt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedInt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedInt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeSignedInt)};
        unsigned long positionInPool{localIndex % SgTypeSignedInt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedInt::pool_size};
        returnPointer = &(((SgTypeSignedInt*)(SgTypeSignedInt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeSignedInt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeSignedInt::pools.begin(); block != SgTypeSignedInt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedInt*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedInt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeSignedInt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeSignedInt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeSignedInt* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeSignedInt::pools.begin(); block != SgTypeSignedInt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedInt*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedInt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeSignedInt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeSignedInt::clearMemoryPool() {
    SgTypeSignedInt* tempPointer{nullptr};
    if (SgTypeSignedInt::pools.empty() == false) {
        auto block{SgTypeSignedInt::pools.begin()};
        SgTypeSignedInt::next_node = reinterpret_cast<SgTypeSignedInt*>(*block);
        for (; block != SgTypeSignedInt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeSignedInt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeSignedInt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeSignedInt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeSignedInt::pool_size-1]);
        }
    }
}

void
SgTypeSignedInt::deleteMemoryPool() {
    for (auto p: SgTypeSignedInt::pools) {
        ROSE_FREE(p);
    }
    SgTypeSignedInt::next_node = nullptr;
    SgTypeSignedInt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeSignedInt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedInt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedInt)};
    for (size_t blockIndex = SgTypeSignedInt::pools.size(); (blockIndex * SgTypeSignedInt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedInt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedInt) = %" PRIuPTR " SgTypeSignedInt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedInt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedInt),SgTypeSignedInt::pool_size);
        }
#endif

        SgTypeSignedInt* pointer{static_cast<SgTypeSignedInt*>(ROSE_MALLOC(SgTypeSignedInt::pool_size * sizeof(SgTypeSignedInt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeSignedInt::pool_size * sizeof(SgTypeSignedInt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeSignedInt::pool_size * sizeof(SgTypeSignedInt));
#endif
        SgTypeSignedInt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeSignedInt::pool_size * sizeof(SgTypeSignedInt), V_SgTypeSignedInt));

        if (SgTypeSignedInt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeSignedInt* blkptr{reinterpret_cast<SgTypeSignedInt*>(SgTypeSignedInt::pools[blockIndex-1])};
                blkptr[SgTypeSignedInt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeSignedInt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeSignedInt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeSignedInt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeSignedInt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeSignedInt::pool_size - 1};
    SgTypeSignedInt* testPointer{reinterpret_cast<SgTypeSignedInt*>(SgTypeSignedInt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeSignedInt::pool_size * (SgTypeSignedInt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeSignedIntStroageClass
 * from its counterpart of SgTypeSignedInt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeSignedInt::initializeStorageClassArray(SgTypeSignedIntStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeSignedInt::pools.begin(); block != SgTypeSignedInt::pools.end(); ++block) {
        SgTypeSignedInt* pointer{reinterpret_cast<SgTypeSignedInt*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedInt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 30894 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeSignedLong that takes its corresponding StorageClass as parameter
 */
SgTypeSignedLong::SgTypeSignedLong(const SgTypeSignedLongStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 30904 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeSignedLong*
SgTypeSignedLong::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeSignedLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedLong)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLong)};
        unsigned long positionInPool{localIndex % SgTypeSignedLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedLong::pool_size};
        returnPointer = &(((SgTypeSignedLong*)(SgTypeSignedLong::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeSignedLong*
SgTypeSignedLong::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeSignedLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedLong)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeSignedLong)};
        unsigned long positionInPool{localIndex % SgTypeSignedLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedLong::pool_size};
        returnPointer = &(((SgTypeSignedLong*)(SgTypeSignedLong::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeSignedLong::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeSignedLong::pools.begin(); block != SgTypeSignedLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedLong*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedLong::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeSignedLong*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeSignedLong::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeSignedLong* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeSignedLong::pools.begin(); block != SgTypeSignedLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedLong*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedLong::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeSignedLong::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeSignedLong::clearMemoryPool() {
    SgTypeSignedLong* tempPointer{nullptr};
    if (SgTypeSignedLong::pools.empty() == false) {
        auto block{SgTypeSignedLong::pools.begin()};
        SgTypeSignedLong::next_node = reinterpret_cast<SgTypeSignedLong*>(*block);
        for (; block != SgTypeSignedLong::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeSignedLong*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeSignedLong::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeSignedLong::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeSignedLong::pool_size-1]);
        }
    }
}

void
SgTypeSignedLong::deleteMemoryPool() {
    for (auto p: SgTypeSignedLong::pools) {
        ROSE_FREE(p);
    }
    SgTypeSignedLong::next_node = nullptr;
    SgTypeSignedLong::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeSignedLong::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLong) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedLong)};
    for (size_t blockIndex = SgTypeSignedLong::pools.size(); (blockIndex * SgTypeSignedLong::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLong) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedLong) = %" PRIuPTR " SgTypeSignedLong::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLong),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedLong),SgTypeSignedLong::pool_size);
        }
#endif

        SgTypeSignedLong* pointer{static_cast<SgTypeSignedLong*>(ROSE_MALLOC(SgTypeSignedLong::pool_size * sizeof(SgTypeSignedLong)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeSignedLong::pool_size * sizeof(SgTypeSignedLong));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeSignedLong::pool_size * sizeof(SgTypeSignedLong));
#endif
        SgTypeSignedLong::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeSignedLong::pool_size * sizeof(SgTypeSignedLong), V_SgTypeSignedLong));

        if (SgTypeSignedLong::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeSignedLong* blkptr{reinterpret_cast<SgTypeSignedLong*>(SgTypeSignedLong::pools[blockIndex-1])};
                blkptr[SgTypeSignedLong::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeSignedLong::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeSignedLong::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeSignedLong::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeSignedLong::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeSignedLong::pool_size - 1};
    SgTypeSignedLong* testPointer{reinterpret_cast<SgTypeSignedLong*>(SgTypeSignedLong::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeSignedLong::pool_size * (SgTypeSignedLong::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeSignedLongStroageClass
 * from its counterpart of SgTypeSignedLong. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeSignedLong::initializeStorageClassArray(SgTypeSignedLongStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeSignedLong::pools.begin(); block != SgTypeSignedLong::pools.end(); ++block) {
        SgTypeSignedLong* pointer{reinterpret_cast<SgTypeSignedLong*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedLong::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 31121 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeSignedLongLong that takes its corresponding StorageClass as parameter
 */
SgTypeSignedLongLong::SgTypeSignedLongLong(const SgTypeSignedLongLongStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 31131 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeSignedLongLong*
SgTypeSignedLongLong::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeSignedLongLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedLongLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedLongLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedLongLong)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLongLong)};
        unsigned long positionInPool{localIndex % SgTypeSignedLongLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedLongLong::pool_size};
        returnPointer = &(((SgTypeSignedLongLong*)(SgTypeSignedLongLong::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeSignedLongLong*
SgTypeSignedLongLong::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeSignedLongLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedLongLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedLongLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedLongLong)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeSignedLongLong)};
        unsigned long positionInPool{localIndex % SgTypeSignedLongLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedLongLong::pool_size};
        returnPointer = &(((SgTypeSignedLongLong*)(SgTypeSignedLongLong::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeSignedLongLong::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeSignedLongLong::pools.begin(); block != SgTypeSignedLongLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedLongLong::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeSignedLongLong*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeSignedLongLong::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeSignedLongLong* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeSignedLongLong::pools.begin(); block != SgTypeSignedLongLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedLongLong::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeSignedLongLong::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeSignedLongLong::clearMemoryPool() {
    SgTypeSignedLongLong* tempPointer{nullptr};
    if (SgTypeSignedLongLong::pools.empty() == false) {
        auto block{SgTypeSignedLongLong::pools.begin()};
        SgTypeSignedLongLong::next_node = reinterpret_cast<SgTypeSignedLongLong*>(*block);
        for (; block != SgTypeSignedLongLong::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeSignedLongLong*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeSignedLongLong::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeSignedLongLong::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeSignedLongLong::pool_size-1]);
        }
    }
}

void
SgTypeSignedLongLong::deleteMemoryPool() {
    for (auto p: SgTypeSignedLongLong::pools) {
        ROSE_FREE(p);
    }
    SgTypeSignedLongLong::next_node = nullptr;
    SgTypeSignedLongLong::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeSignedLongLong::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLongLong) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedLongLong)};
    for (size_t blockIndex = SgTypeSignedLongLong::pools.size(); (blockIndex * SgTypeSignedLongLong::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLongLong) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedLongLong) = %" PRIuPTR " SgTypeSignedLongLong::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedLongLong),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedLongLong),SgTypeSignedLongLong::pool_size);
        }
#endif

        SgTypeSignedLongLong* pointer{static_cast<SgTypeSignedLongLong*>(ROSE_MALLOC(SgTypeSignedLongLong::pool_size * sizeof(SgTypeSignedLongLong)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeSignedLongLong::pool_size * sizeof(SgTypeSignedLongLong));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeSignedLongLong::pool_size * sizeof(SgTypeSignedLongLong));
#endif
        SgTypeSignedLongLong::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeSignedLongLong::pool_size * sizeof(SgTypeSignedLongLong), V_SgTypeSignedLongLong));

        if (SgTypeSignedLongLong::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeSignedLongLong* blkptr{reinterpret_cast<SgTypeSignedLongLong*>(SgTypeSignedLongLong::pools[blockIndex-1])};
                blkptr[SgTypeSignedLongLong::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeSignedLongLong::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeSignedLongLong::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeSignedLongLong::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeSignedLongLong::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeSignedLongLong::pool_size - 1};
    SgTypeSignedLongLong* testPointer{reinterpret_cast<SgTypeSignedLongLong*>(SgTypeSignedLongLong::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeSignedLongLong::pool_size * (SgTypeSignedLongLong::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeSignedLongLongStroageClass
 * from its counterpart of SgTypeSignedLongLong. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeSignedLongLong::initializeStorageClassArray(SgTypeSignedLongLongStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeSignedLongLong::pools.begin(); block != SgTypeSignedLongLong::pools.end(); ++block) {
        SgTypeSignedLongLong* pointer{reinterpret_cast<SgTypeSignedLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedLongLong::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 31348 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeSignedShort that takes its corresponding StorageClass as parameter
 */
SgTypeSignedShort::SgTypeSignedShort(const SgTypeSignedShortStorageClass& storageSource)  : SgSignedIntegralType (storageSource) {

/* #line 31358 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeSignedShort*
SgTypeSignedShort::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeSignedShort* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedShort)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedShort + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSignedShort)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedShort)};
        unsigned long positionInPool{localIndex % SgTypeSignedShort::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedShort::pool_size};
        returnPointer = &(((SgTypeSignedShort*)(SgTypeSignedShort::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeSignedShort*
SgTypeSignedShort::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeSignedShort* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedShort)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedShort + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSignedShort)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeSignedShort)};
        unsigned long positionInPool{localIndex % SgTypeSignedShort::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSignedShort::pool_size};
        returnPointer = &(((SgTypeSignedShort*)(SgTypeSignedShort::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeSignedShort::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeSignedShort::pools.begin(); block != SgTypeSignedShort::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedShort*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedShort::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeSignedShort*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeSignedShort::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeSignedShort* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeSignedShort::pools.begin(); block != SgTypeSignedShort::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSignedShort*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedShort::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeSignedShort::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeSignedShort::clearMemoryPool() {
    SgTypeSignedShort* tempPointer{nullptr};
    if (SgTypeSignedShort::pools.empty() == false) {
        auto block{SgTypeSignedShort::pools.begin()};
        SgTypeSignedShort::next_node = reinterpret_cast<SgTypeSignedShort*>(*block);
        for (; block != SgTypeSignedShort::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeSignedShort*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeSignedShort::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeSignedShort::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeSignedShort::pool_size-1]);
        }
    }
}

void
SgTypeSignedShort::deleteMemoryPool() {
    for (auto p: SgTypeSignedShort::pools) {
        ROSE_FREE(p);
    }
    SgTypeSignedShort::next_node = nullptr;
    SgTypeSignedShort::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeSignedShort::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedShort) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedShort)};
    for (size_t blockIndex = SgTypeSignedShort::pools.size(); (blockIndex * SgTypeSignedShort::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedShort) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedShort) = %" PRIuPTR " SgTypeSignedShort::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSignedShort),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSignedShort),SgTypeSignedShort::pool_size);
        }
#endif

        SgTypeSignedShort* pointer{static_cast<SgTypeSignedShort*>(ROSE_MALLOC(SgTypeSignedShort::pool_size * sizeof(SgTypeSignedShort)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeSignedShort::pool_size * sizeof(SgTypeSignedShort));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeSignedShort::pool_size * sizeof(SgTypeSignedShort));
#endif
        SgTypeSignedShort::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeSignedShort::pool_size * sizeof(SgTypeSignedShort), V_SgTypeSignedShort));

        if (SgTypeSignedShort::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeSignedShort* blkptr{reinterpret_cast<SgTypeSignedShort*>(SgTypeSignedShort::pools[blockIndex-1])};
                blkptr[SgTypeSignedShort::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeSignedShort::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeSignedShort::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeSignedShort::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeSignedShort::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeSignedShort::pool_size - 1};
    SgTypeSignedShort* testPointer{reinterpret_cast<SgTypeSignedShort*>(SgTypeSignedShort::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeSignedShort::pool_size * (SgTypeSignedShort::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeSignedShortStroageClass
 * from its counterpart of SgTypeSignedShort. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeSignedShort::initializeStorageClassArray(SgTypeSignedShortStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeSignedShort::pools.begin(); block != SgTypeSignedShort::pools.end(); ++block) {
        SgTypeSignedShort* pointer{reinterpret_cast<SgTypeSignedShort*>(*block)};
        for (unsigned i = 0; i < SgTypeSignedShort::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 31575 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnsignedIntegralType that takes its corresponding StorageClass as parameter
 */
SgUnsignedIntegralType::SgUnsignedIntegralType(const SgUnsignedIntegralTypeStorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 31585 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnsignedIntegralType*
SgUnsignedIntegralType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnsignedIntegralType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedIntegralType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedIntegralType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedIntegralType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntegralType)};
        unsigned long positionInPool{localIndex % SgUnsignedIntegralType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedIntegralType::pool_size};
        returnPointer = &(((SgUnsignedIntegralType*)(SgUnsignedIntegralType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnsignedIntegralType*
SgUnsignedIntegralType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnsignedIntegralType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedIntegralType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedIntegralType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedIntegralType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnsignedIntegralType)};
        unsigned long positionInPool{localIndex % SgUnsignedIntegralType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedIntegralType::pool_size};
        returnPointer = &(((SgUnsignedIntegralType*)(SgUnsignedIntegralType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnsignedIntegralType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnsignedIntegralType::pools.begin(); block != SgUnsignedIntegralType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedIntegralType*>(*block)};
        for (unsigned i = 0; i < SgUnsignedIntegralType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnsignedIntegralType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnsignedIntegralType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnsignedIntegralType* pointerOfLinkedList{nullptr};
    for (auto block = SgUnsignedIntegralType::pools.begin(); block != SgUnsignedIntegralType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedIntegralType*>(*block)};
        for (unsigned i = 0; i < SgUnsignedIntegralType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnsignedIntegralType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnsignedIntegralType::clearMemoryPool() {
    SgUnsignedIntegralType* tempPointer{nullptr};
    if (SgUnsignedIntegralType::pools.empty() == false) {
        auto block{SgUnsignedIntegralType::pools.begin()};
        SgUnsignedIntegralType::next_node = reinterpret_cast<SgUnsignedIntegralType*>(*block);
        for (; block != SgUnsignedIntegralType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnsignedIntegralType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnsignedIntegralType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnsignedIntegralType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnsignedIntegralType::pool_size-1]);
        }
    }
}

void
SgUnsignedIntegralType::deleteMemoryPool() {
    for (auto p: SgUnsignedIntegralType::pools) {
        ROSE_FREE(p);
    }
    SgUnsignedIntegralType::next_node = nullptr;
    SgUnsignedIntegralType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnsignedIntegralType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntegralType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedIntegralType)};
    for (size_t blockIndex = SgUnsignedIntegralType::pools.size(); (blockIndex * SgUnsignedIntegralType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntegralType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedIntegralType) = %" PRIuPTR " SgUnsignedIntegralType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntegralType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedIntegralType),SgUnsignedIntegralType::pool_size);
        }
#endif

        SgUnsignedIntegralType* pointer{static_cast<SgUnsignedIntegralType*>(ROSE_MALLOC(SgUnsignedIntegralType::pool_size * sizeof(SgUnsignedIntegralType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnsignedIntegralType::pool_size * sizeof(SgUnsignedIntegralType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnsignedIntegralType::pool_size * sizeof(SgUnsignedIntegralType));
#endif
        SgUnsignedIntegralType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnsignedIntegralType::pool_size * sizeof(SgUnsignedIntegralType), V_SgUnsignedIntegralType));

        if (SgUnsignedIntegralType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnsignedIntegralType* blkptr{reinterpret_cast<SgUnsignedIntegralType*>(SgUnsignedIntegralType::pools[blockIndex-1])};
                blkptr[SgUnsignedIntegralType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnsignedIntegralType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnsignedIntegralType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnsignedIntegralType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnsignedIntegralType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnsignedIntegralType::pool_size - 1};
    SgUnsignedIntegralType* testPointer{reinterpret_cast<SgUnsignedIntegralType*>(SgUnsignedIntegralType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnsignedIntegralType::pool_size * (SgUnsignedIntegralType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnsignedIntegralTypeStroageClass
 * from its counterpart of SgUnsignedIntegralType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnsignedIntegralType::initializeStorageClassArray(SgUnsignedIntegralTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnsignedIntegralType::pools.begin(); block != SgUnsignedIntegralType::pools.end(); ++block) {
        SgUnsignedIntegralType* pointer{reinterpret_cast<SgUnsignedIntegralType*>(*block)};
        for (unsigned i = 0; i < SgUnsignedIntegralType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 31802 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeUnsigned128bitInteger that takes its corresponding StorageClass as parameter
 */
SgTypeUnsigned128bitInteger::SgTypeUnsigned128bitInteger(const SgTypeUnsigned128bitIntegerStorageClass& storageSource)  : SgUnsignedIntegralType (storageSource) {

/* #line 31812 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeUnsigned128bitInteger*
SgTypeUnsigned128bitInteger::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeUnsigned128bitInteger* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsigned128bitInteger)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsigned128bitInteger + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsigned128bitInteger)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsigned128bitInteger)};
        unsigned long positionInPool{localIndex % SgTypeUnsigned128bitInteger::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsigned128bitInteger::pool_size};
        returnPointer = &(((SgTypeUnsigned128bitInteger*)(SgTypeUnsigned128bitInteger::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeUnsigned128bitInteger*
SgTypeUnsigned128bitInteger::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeUnsigned128bitInteger* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsigned128bitInteger)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsigned128bitInteger + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsigned128bitInteger)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeUnsigned128bitInteger)};
        unsigned long positionInPool{localIndex % SgTypeUnsigned128bitInteger::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsigned128bitInteger::pool_size};
        returnPointer = &(((SgTypeUnsigned128bitInteger*)(SgTypeUnsigned128bitInteger::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeUnsigned128bitInteger::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeUnsigned128bitInteger::pools.begin(); block != SgTypeUnsigned128bitInteger::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsigned128bitInteger*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsigned128bitInteger::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeUnsigned128bitInteger*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeUnsigned128bitInteger::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeUnsigned128bitInteger* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeUnsigned128bitInteger::pools.begin(); block != SgTypeUnsigned128bitInteger::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsigned128bitInteger*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsigned128bitInteger::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeUnsigned128bitInteger::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeUnsigned128bitInteger::clearMemoryPool() {
    SgTypeUnsigned128bitInteger* tempPointer{nullptr};
    if (SgTypeUnsigned128bitInteger::pools.empty() == false) {
        auto block{SgTypeUnsigned128bitInteger::pools.begin()};
        SgTypeUnsigned128bitInteger::next_node = reinterpret_cast<SgTypeUnsigned128bitInteger*>(*block);
        for (; block != SgTypeUnsigned128bitInteger::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeUnsigned128bitInteger*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeUnsigned128bitInteger::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeUnsigned128bitInteger::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeUnsigned128bitInteger::pool_size-1]);
        }
    }
}

void
SgTypeUnsigned128bitInteger::deleteMemoryPool() {
    for (auto p: SgTypeUnsigned128bitInteger::pools) {
        ROSE_FREE(p);
    }
    SgTypeUnsigned128bitInteger::next_node = nullptr;
    SgTypeUnsigned128bitInteger::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeUnsigned128bitInteger::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsigned128bitInteger) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsigned128bitInteger)};
    for (size_t blockIndex = SgTypeUnsigned128bitInteger::pools.size(); (blockIndex * SgTypeUnsigned128bitInteger::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsigned128bitInteger) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsigned128bitInteger) = %" PRIuPTR " SgTypeUnsigned128bitInteger::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsigned128bitInteger),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsigned128bitInteger),SgTypeUnsigned128bitInteger::pool_size);
        }
#endif

        SgTypeUnsigned128bitInteger* pointer{static_cast<SgTypeUnsigned128bitInteger*>(ROSE_MALLOC(SgTypeUnsigned128bitInteger::pool_size * sizeof(SgTypeUnsigned128bitInteger)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeUnsigned128bitInteger::pool_size * sizeof(SgTypeUnsigned128bitInteger));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeUnsigned128bitInteger::pool_size * sizeof(SgTypeUnsigned128bitInteger));
#endif
        SgTypeUnsigned128bitInteger::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeUnsigned128bitInteger::pool_size * sizeof(SgTypeUnsigned128bitInteger), V_SgTypeUnsigned128bitInteger));

        if (SgTypeUnsigned128bitInteger::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeUnsigned128bitInteger* blkptr{reinterpret_cast<SgTypeUnsigned128bitInteger*>(SgTypeUnsigned128bitInteger::pools[blockIndex-1])};
                blkptr[SgTypeUnsigned128bitInteger::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeUnsigned128bitInteger::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeUnsigned128bitInteger::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeUnsigned128bitInteger::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeUnsigned128bitInteger::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeUnsigned128bitInteger::pool_size - 1};
    SgTypeUnsigned128bitInteger* testPointer{reinterpret_cast<SgTypeUnsigned128bitInteger*>(SgTypeUnsigned128bitInteger::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeUnsigned128bitInteger::pool_size * (SgTypeUnsigned128bitInteger::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeUnsigned128bitIntegerStroageClass
 * from its counterpart of SgTypeUnsigned128bitInteger. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeUnsigned128bitInteger::initializeStorageClassArray(SgTypeUnsigned128bitIntegerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeUnsigned128bitInteger::pools.begin(); block != SgTypeUnsigned128bitInteger::pools.end(); ++block) {
        SgTypeUnsigned128bitInteger* pointer{reinterpret_cast<SgTypeUnsigned128bitInteger*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsigned128bitInteger::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 32029 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeUnsignedChar that takes its corresponding StorageClass as parameter
 */
SgTypeUnsignedChar::SgTypeUnsignedChar(const SgTypeUnsignedCharStorageClass& storageSource)  : SgUnsignedIntegralType (storageSource) {

/* #line 32039 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeUnsignedChar*
SgTypeUnsignedChar::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeUnsignedChar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedChar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedChar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedChar)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedChar)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedChar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedChar::pool_size};
        returnPointer = &(((SgTypeUnsignedChar*)(SgTypeUnsignedChar::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeUnsignedChar*
SgTypeUnsignedChar::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeUnsignedChar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedChar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedChar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedChar)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeUnsignedChar)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedChar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedChar::pool_size};
        returnPointer = &(((SgTypeUnsignedChar*)(SgTypeUnsignedChar::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeUnsignedChar::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeUnsignedChar::pools.begin(); block != SgTypeUnsignedChar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedChar*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedChar::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeUnsignedChar*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeUnsignedChar::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeUnsignedChar* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeUnsignedChar::pools.begin(); block != SgTypeUnsignedChar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedChar*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedChar::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeUnsignedChar::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeUnsignedChar::clearMemoryPool() {
    SgTypeUnsignedChar* tempPointer{nullptr};
    if (SgTypeUnsignedChar::pools.empty() == false) {
        auto block{SgTypeUnsignedChar::pools.begin()};
        SgTypeUnsignedChar::next_node = reinterpret_cast<SgTypeUnsignedChar*>(*block);
        for (; block != SgTypeUnsignedChar::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeUnsignedChar*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeUnsignedChar::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeUnsignedChar::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeUnsignedChar::pool_size-1]);
        }
    }
}

void
SgTypeUnsignedChar::deleteMemoryPool() {
    for (auto p: SgTypeUnsignedChar::pools) {
        ROSE_FREE(p);
    }
    SgTypeUnsignedChar::next_node = nullptr;
    SgTypeUnsignedChar::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeUnsignedChar::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedChar) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedChar)};
    for (size_t blockIndex = SgTypeUnsignedChar::pools.size(); (blockIndex * SgTypeUnsignedChar::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedChar) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedChar) = %" PRIuPTR " SgTypeUnsignedChar::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedChar),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedChar),SgTypeUnsignedChar::pool_size);
        }
#endif

        SgTypeUnsignedChar* pointer{static_cast<SgTypeUnsignedChar*>(ROSE_MALLOC(SgTypeUnsignedChar::pool_size * sizeof(SgTypeUnsignedChar)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeUnsignedChar::pool_size * sizeof(SgTypeUnsignedChar));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeUnsignedChar::pool_size * sizeof(SgTypeUnsignedChar));
#endif
        SgTypeUnsignedChar::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeUnsignedChar::pool_size * sizeof(SgTypeUnsignedChar), V_SgTypeUnsignedChar));

        if (SgTypeUnsignedChar::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeUnsignedChar* blkptr{reinterpret_cast<SgTypeUnsignedChar*>(SgTypeUnsignedChar::pools[blockIndex-1])};
                blkptr[SgTypeUnsignedChar::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeUnsignedChar::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeUnsignedChar::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeUnsignedChar::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeUnsignedChar::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeUnsignedChar::pool_size - 1};
    SgTypeUnsignedChar* testPointer{reinterpret_cast<SgTypeUnsignedChar*>(SgTypeUnsignedChar::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeUnsignedChar::pool_size * (SgTypeUnsignedChar::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeUnsignedCharStroageClass
 * from its counterpart of SgTypeUnsignedChar. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeUnsignedChar::initializeStorageClassArray(SgTypeUnsignedCharStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeUnsignedChar::pools.begin(); block != SgTypeUnsignedChar::pools.end(); ++block) {
        SgTypeUnsignedChar* pointer{reinterpret_cast<SgTypeUnsignedChar*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedChar::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 32256 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeUnsignedInt that takes its corresponding StorageClass as parameter
 */
SgTypeUnsignedInt::SgTypeUnsignedInt(const SgTypeUnsignedIntStorageClass& storageSource)  : SgUnsignedIntegralType (storageSource) {

/* #line 32266 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeUnsignedInt*
SgTypeUnsignedInt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeUnsignedInt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedInt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedInt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedInt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedInt)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedInt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedInt::pool_size};
        returnPointer = &(((SgTypeUnsignedInt*)(SgTypeUnsignedInt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeUnsignedInt*
SgTypeUnsignedInt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeUnsignedInt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedInt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedInt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedInt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeUnsignedInt)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedInt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedInt::pool_size};
        returnPointer = &(((SgTypeUnsignedInt*)(SgTypeUnsignedInt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeUnsignedInt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeUnsignedInt::pools.begin(); block != SgTypeUnsignedInt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedInt*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedInt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeUnsignedInt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeUnsignedInt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeUnsignedInt* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeUnsignedInt::pools.begin(); block != SgTypeUnsignedInt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedInt*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedInt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeUnsignedInt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeUnsignedInt::clearMemoryPool() {
    SgTypeUnsignedInt* tempPointer{nullptr};
    if (SgTypeUnsignedInt::pools.empty() == false) {
        auto block{SgTypeUnsignedInt::pools.begin()};
        SgTypeUnsignedInt::next_node = reinterpret_cast<SgTypeUnsignedInt*>(*block);
        for (; block != SgTypeUnsignedInt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeUnsignedInt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeUnsignedInt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeUnsignedInt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeUnsignedInt::pool_size-1]);
        }
    }
}

void
SgTypeUnsignedInt::deleteMemoryPool() {
    for (auto p: SgTypeUnsignedInt::pools) {
        ROSE_FREE(p);
    }
    SgTypeUnsignedInt::next_node = nullptr;
    SgTypeUnsignedInt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeUnsignedInt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedInt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedInt)};
    for (size_t blockIndex = SgTypeUnsignedInt::pools.size(); (blockIndex * SgTypeUnsignedInt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedInt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedInt) = %" PRIuPTR " SgTypeUnsignedInt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedInt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedInt),SgTypeUnsignedInt::pool_size);
        }
#endif

        SgTypeUnsignedInt* pointer{static_cast<SgTypeUnsignedInt*>(ROSE_MALLOC(SgTypeUnsignedInt::pool_size * sizeof(SgTypeUnsignedInt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeUnsignedInt::pool_size * sizeof(SgTypeUnsignedInt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeUnsignedInt::pool_size * sizeof(SgTypeUnsignedInt));
#endif
        SgTypeUnsignedInt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeUnsignedInt::pool_size * sizeof(SgTypeUnsignedInt), V_SgTypeUnsignedInt));

        if (SgTypeUnsignedInt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeUnsignedInt* blkptr{reinterpret_cast<SgTypeUnsignedInt*>(SgTypeUnsignedInt::pools[blockIndex-1])};
                blkptr[SgTypeUnsignedInt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeUnsignedInt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeUnsignedInt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeUnsignedInt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeUnsignedInt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeUnsignedInt::pool_size - 1};
    SgTypeUnsignedInt* testPointer{reinterpret_cast<SgTypeUnsignedInt*>(SgTypeUnsignedInt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeUnsignedInt::pool_size * (SgTypeUnsignedInt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeUnsignedIntStroageClass
 * from its counterpart of SgTypeUnsignedInt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeUnsignedInt::initializeStorageClassArray(SgTypeUnsignedIntStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeUnsignedInt::pools.begin(); block != SgTypeUnsignedInt::pools.end(); ++block) {
        SgTypeUnsignedInt* pointer{reinterpret_cast<SgTypeUnsignedInt*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedInt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 32483 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeUnsignedLong that takes its corresponding StorageClass as parameter
 */
SgTypeUnsignedLong::SgTypeUnsignedLong(const SgTypeUnsignedLongStorageClass& storageSource)  : SgUnsignedIntegralType (storageSource) {

/* #line 32493 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeUnsignedLong*
SgTypeUnsignedLong::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeUnsignedLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedLong)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLong)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedLong::pool_size};
        returnPointer = &(((SgTypeUnsignedLong*)(SgTypeUnsignedLong::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeUnsignedLong*
SgTypeUnsignedLong::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeUnsignedLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedLong)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeUnsignedLong)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedLong::pool_size};
        returnPointer = &(((SgTypeUnsignedLong*)(SgTypeUnsignedLong::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeUnsignedLong::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeUnsignedLong::pools.begin(); block != SgTypeUnsignedLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedLong*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedLong::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeUnsignedLong*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeUnsignedLong::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeUnsignedLong* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeUnsignedLong::pools.begin(); block != SgTypeUnsignedLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedLong*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedLong::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeUnsignedLong::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeUnsignedLong::clearMemoryPool() {
    SgTypeUnsignedLong* tempPointer{nullptr};
    if (SgTypeUnsignedLong::pools.empty() == false) {
        auto block{SgTypeUnsignedLong::pools.begin()};
        SgTypeUnsignedLong::next_node = reinterpret_cast<SgTypeUnsignedLong*>(*block);
        for (; block != SgTypeUnsignedLong::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeUnsignedLong*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeUnsignedLong::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeUnsignedLong::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeUnsignedLong::pool_size-1]);
        }
    }
}

void
SgTypeUnsignedLong::deleteMemoryPool() {
    for (auto p: SgTypeUnsignedLong::pools) {
        ROSE_FREE(p);
    }
    SgTypeUnsignedLong::next_node = nullptr;
    SgTypeUnsignedLong::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeUnsignedLong::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLong) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedLong)};
    for (size_t blockIndex = SgTypeUnsignedLong::pools.size(); (blockIndex * SgTypeUnsignedLong::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLong) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedLong) = %" PRIuPTR " SgTypeUnsignedLong::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLong),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedLong),SgTypeUnsignedLong::pool_size);
        }
#endif

        SgTypeUnsignedLong* pointer{static_cast<SgTypeUnsignedLong*>(ROSE_MALLOC(SgTypeUnsignedLong::pool_size * sizeof(SgTypeUnsignedLong)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeUnsignedLong::pool_size * sizeof(SgTypeUnsignedLong));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeUnsignedLong::pool_size * sizeof(SgTypeUnsignedLong));
#endif
        SgTypeUnsignedLong::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeUnsignedLong::pool_size * sizeof(SgTypeUnsignedLong), V_SgTypeUnsignedLong));

        if (SgTypeUnsignedLong::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeUnsignedLong* blkptr{reinterpret_cast<SgTypeUnsignedLong*>(SgTypeUnsignedLong::pools[blockIndex-1])};
                blkptr[SgTypeUnsignedLong::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeUnsignedLong::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeUnsignedLong::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeUnsignedLong::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeUnsignedLong::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeUnsignedLong::pool_size - 1};
    SgTypeUnsignedLong* testPointer{reinterpret_cast<SgTypeUnsignedLong*>(SgTypeUnsignedLong::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeUnsignedLong::pool_size * (SgTypeUnsignedLong::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeUnsignedLongStroageClass
 * from its counterpart of SgTypeUnsignedLong. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeUnsignedLong::initializeStorageClassArray(SgTypeUnsignedLongStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeUnsignedLong::pools.begin(); block != SgTypeUnsignedLong::pools.end(); ++block) {
        SgTypeUnsignedLong* pointer{reinterpret_cast<SgTypeUnsignedLong*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedLong::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 32710 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeUnsignedLongLong that takes its corresponding StorageClass as parameter
 */
SgTypeUnsignedLongLong::SgTypeUnsignedLongLong(const SgTypeUnsignedLongLongStorageClass& storageSource)  : SgUnsignedIntegralType (storageSource) {

/* #line 32720 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeUnsignedLongLong*
SgTypeUnsignedLongLong::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeUnsignedLongLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedLongLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedLongLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedLongLong)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLongLong)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedLongLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedLongLong::pool_size};
        returnPointer = &(((SgTypeUnsignedLongLong*)(SgTypeUnsignedLongLong::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeUnsignedLongLong*
SgTypeUnsignedLongLong::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeUnsignedLongLong* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedLongLong)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedLongLong + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedLongLong)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeUnsignedLongLong)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedLongLong::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedLongLong::pool_size};
        returnPointer = &(((SgTypeUnsignedLongLong*)(SgTypeUnsignedLongLong::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeUnsignedLongLong::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeUnsignedLongLong::pools.begin(); block != SgTypeUnsignedLongLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedLongLong::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeUnsignedLongLong*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeUnsignedLongLong::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeUnsignedLongLong* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeUnsignedLongLong::pools.begin(); block != SgTypeUnsignedLongLong::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedLongLong::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeUnsignedLongLong::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeUnsignedLongLong::clearMemoryPool() {
    SgTypeUnsignedLongLong* tempPointer{nullptr};
    if (SgTypeUnsignedLongLong::pools.empty() == false) {
        auto block{SgTypeUnsignedLongLong::pools.begin()};
        SgTypeUnsignedLongLong::next_node = reinterpret_cast<SgTypeUnsignedLongLong*>(*block);
        for (; block != SgTypeUnsignedLongLong::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeUnsignedLongLong*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeUnsignedLongLong::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeUnsignedLongLong::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeUnsignedLongLong::pool_size-1]);
        }
    }
}

void
SgTypeUnsignedLongLong::deleteMemoryPool() {
    for (auto p: SgTypeUnsignedLongLong::pools) {
        ROSE_FREE(p);
    }
    SgTypeUnsignedLongLong::next_node = nullptr;
    SgTypeUnsignedLongLong::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeUnsignedLongLong::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLongLong) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedLongLong)};
    for (size_t blockIndex = SgTypeUnsignedLongLong::pools.size(); (blockIndex * SgTypeUnsignedLongLong::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLongLong) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedLongLong) = %" PRIuPTR " SgTypeUnsignedLongLong::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedLongLong),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedLongLong),SgTypeUnsignedLongLong::pool_size);
        }
#endif

        SgTypeUnsignedLongLong* pointer{static_cast<SgTypeUnsignedLongLong*>(ROSE_MALLOC(SgTypeUnsignedLongLong::pool_size * sizeof(SgTypeUnsignedLongLong)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeUnsignedLongLong::pool_size * sizeof(SgTypeUnsignedLongLong));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeUnsignedLongLong::pool_size * sizeof(SgTypeUnsignedLongLong));
#endif
        SgTypeUnsignedLongLong::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeUnsignedLongLong::pool_size * sizeof(SgTypeUnsignedLongLong), V_SgTypeUnsignedLongLong));

        if (SgTypeUnsignedLongLong::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeUnsignedLongLong* blkptr{reinterpret_cast<SgTypeUnsignedLongLong*>(SgTypeUnsignedLongLong::pools[blockIndex-1])};
                blkptr[SgTypeUnsignedLongLong::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeUnsignedLongLong::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeUnsignedLongLong::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeUnsignedLongLong::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeUnsignedLongLong::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeUnsignedLongLong::pool_size - 1};
    SgTypeUnsignedLongLong* testPointer{reinterpret_cast<SgTypeUnsignedLongLong*>(SgTypeUnsignedLongLong::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeUnsignedLongLong::pool_size * (SgTypeUnsignedLongLong::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeUnsignedLongLongStroageClass
 * from its counterpart of SgTypeUnsignedLongLong. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeUnsignedLongLong::initializeStorageClassArray(SgTypeUnsignedLongLongStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeUnsignedLongLong::pools.begin(); block != SgTypeUnsignedLongLong::pools.end(); ++block) {
        SgTypeUnsignedLongLong* pointer{reinterpret_cast<SgTypeUnsignedLongLong*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedLongLong::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 32937 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeUnsignedShort that takes its corresponding StorageClass as parameter
 */
SgTypeUnsignedShort::SgTypeUnsignedShort(const SgTypeUnsignedShortStorageClass& storageSource)  : SgUnsignedIntegralType (storageSource) {

/* #line 32947 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeUnsignedShort*
SgTypeUnsignedShort::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeUnsignedShort* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedShort)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedShort + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeUnsignedShort)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedShort)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedShort::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedShort::pool_size};
        returnPointer = &(((SgTypeUnsignedShort*)(SgTypeUnsignedShort::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeUnsignedShort*
SgTypeUnsignedShort::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeUnsignedShort* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedShort)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedShort + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeUnsignedShort)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeUnsignedShort)};
        unsigned long positionInPool{localIndex % SgTypeUnsignedShort::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeUnsignedShort::pool_size};
        returnPointer = &(((SgTypeUnsignedShort*)(SgTypeUnsignedShort::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeUnsignedShort::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeUnsignedShort::pools.begin(); block != SgTypeUnsignedShort::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedShort*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedShort::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeUnsignedShort*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeUnsignedShort::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeUnsignedShort* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeUnsignedShort::pools.begin(); block != SgTypeUnsignedShort::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeUnsignedShort*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedShort::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeUnsignedShort::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeUnsignedShort::clearMemoryPool() {
    SgTypeUnsignedShort* tempPointer{nullptr};
    if (SgTypeUnsignedShort::pools.empty() == false) {
        auto block{SgTypeUnsignedShort::pools.begin()};
        SgTypeUnsignedShort::next_node = reinterpret_cast<SgTypeUnsignedShort*>(*block);
        for (; block != SgTypeUnsignedShort::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeUnsignedShort*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeUnsignedShort::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeUnsignedShort::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeUnsignedShort::pool_size-1]);
        }
    }
}

void
SgTypeUnsignedShort::deleteMemoryPool() {
    for (auto p: SgTypeUnsignedShort::pools) {
        ROSE_FREE(p);
    }
    SgTypeUnsignedShort::next_node = nullptr;
    SgTypeUnsignedShort::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeUnsignedShort::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedShort) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedShort)};
    for (size_t blockIndex = SgTypeUnsignedShort::pools.size(); (blockIndex * SgTypeUnsignedShort::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedShort) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedShort) = %" PRIuPTR " SgTypeUnsignedShort::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeUnsignedShort),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeUnsignedShort),SgTypeUnsignedShort::pool_size);
        }
#endif

        SgTypeUnsignedShort* pointer{static_cast<SgTypeUnsignedShort*>(ROSE_MALLOC(SgTypeUnsignedShort::pool_size * sizeof(SgTypeUnsignedShort)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeUnsignedShort::pool_size * sizeof(SgTypeUnsignedShort));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeUnsignedShort::pool_size * sizeof(SgTypeUnsignedShort));
#endif
        SgTypeUnsignedShort::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeUnsignedShort::pool_size * sizeof(SgTypeUnsignedShort), V_SgTypeUnsignedShort));

        if (SgTypeUnsignedShort::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeUnsignedShort* blkptr{reinterpret_cast<SgTypeUnsignedShort*>(SgTypeUnsignedShort::pools[blockIndex-1])};
                blkptr[SgTypeUnsignedShort::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeUnsignedShort::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeUnsignedShort::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeUnsignedShort::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeUnsignedShort::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeUnsignedShort::pool_size - 1};
    SgTypeUnsignedShort* testPointer{reinterpret_cast<SgTypeUnsignedShort*>(SgTypeUnsignedShort::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeUnsignedShort::pool_size * (SgTypeUnsignedShort::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeUnsignedShortStroageClass
 * from its counterpart of SgTypeUnsignedShort. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeUnsignedShort::initializeStorageClassArray(SgTypeUnsignedShortStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeUnsignedShort::pools.begin(); block != SgTypeUnsignedShort::pools.end(); ++block) {
        SgTypeUnsignedShort* pointer{reinterpret_cast<SgTypeUnsignedShort*>(*block)};
        for (unsigned i = 0; i < SgTypeUnsignedShort::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 33164 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeChar that takes its corresponding StorageClass as parameter
 */
SgTypeChar::SgTypeChar(const SgTypeCharStorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 33174 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeChar*
SgTypeChar::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeChar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar)};
        unsigned long positionInPool{localIndex % SgTypeChar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeChar::pool_size};
        returnPointer = &(((SgTypeChar*)(SgTypeChar::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeChar*
SgTypeChar::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeChar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeChar)};
        unsigned long positionInPool{localIndex % SgTypeChar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeChar::pool_size};
        returnPointer = &(((SgTypeChar*)(SgTypeChar::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeChar::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeChar::pools.begin(); block != SgTypeChar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeChar*>(*block)};
        for (unsigned i = 0; i < SgTypeChar::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeChar*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeChar::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeChar* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeChar::pools.begin(); block != SgTypeChar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeChar*>(*block)};
        for (unsigned i = 0; i < SgTypeChar::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeChar::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeChar::clearMemoryPool() {
    SgTypeChar* tempPointer{nullptr};
    if (SgTypeChar::pools.empty() == false) {
        auto block{SgTypeChar::pools.begin()};
        SgTypeChar::next_node = reinterpret_cast<SgTypeChar*>(*block);
        for (; block != SgTypeChar::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeChar*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeChar::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeChar::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeChar::pool_size-1]);
        }
    }
}

void
SgTypeChar::deleteMemoryPool() {
    for (auto p: SgTypeChar::pools) {
        ROSE_FREE(p);
    }
    SgTypeChar::next_node = nullptr;
    SgTypeChar::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeChar::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar)};
    for (size_t blockIndex = SgTypeChar::pools.size(); (blockIndex * SgTypeChar::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar) = %" PRIuPTR " SgTypeChar::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar),SgTypeChar::pool_size);
        }
#endif

        SgTypeChar* pointer{static_cast<SgTypeChar*>(ROSE_MALLOC(SgTypeChar::pool_size * sizeof(SgTypeChar)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeChar::pool_size * sizeof(SgTypeChar));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeChar::pool_size * sizeof(SgTypeChar));
#endif
        SgTypeChar::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeChar::pool_size * sizeof(SgTypeChar), V_SgTypeChar));

        if (SgTypeChar::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeChar* blkptr{reinterpret_cast<SgTypeChar*>(SgTypeChar::pools[blockIndex-1])};
                blkptr[SgTypeChar::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeChar::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeChar::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeChar::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeChar::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeChar::pool_size - 1};
    SgTypeChar* testPointer{reinterpret_cast<SgTypeChar*>(SgTypeChar::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeChar::pool_size * (SgTypeChar::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeCharStroageClass
 * from its counterpart of SgTypeChar. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeChar::initializeStorageClassArray(SgTypeCharStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeChar::pools.begin(); block != SgTypeChar::pools.end(); ++block) {
        SgTypeChar* pointer{reinterpret_cast<SgTypeChar*>(*block)};
        for (unsigned i = 0; i < SgTypeChar::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 33391 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeWchar that takes its corresponding StorageClass as parameter
 */
SgTypeWchar::SgTypeWchar(const SgTypeWcharStorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 33401 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeWchar*
SgTypeWchar::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeWchar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeWchar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeWchar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeWchar)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeWchar)};
        unsigned long positionInPool{localIndex % SgTypeWchar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeWchar::pool_size};
        returnPointer = &(((SgTypeWchar*)(SgTypeWchar::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeWchar*
SgTypeWchar::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeWchar* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeWchar)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeWchar + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeWchar)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeWchar)};
        unsigned long positionInPool{localIndex % SgTypeWchar::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeWchar::pool_size};
        returnPointer = &(((SgTypeWchar*)(SgTypeWchar::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeWchar::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeWchar::pools.begin(); block != SgTypeWchar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeWchar*>(*block)};
        for (unsigned i = 0; i < SgTypeWchar::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeWchar*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeWchar::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeWchar* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeWchar::pools.begin(); block != SgTypeWchar::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeWchar*>(*block)};
        for (unsigned i = 0; i < SgTypeWchar::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeWchar::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeWchar::clearMemoryPool() {
    SgTypeWchar* tempPointer{nullptr};
    if (SgTypeWchar::pools.empty() == false) {
        auto block{SgTypeWchar::pools.begin()};
        SgTypeWchar::next_node = reinterpret_cast<SgTypeWchar*>(*block);
        for (; block != SgTypeWchar::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeWchar*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeWchar::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeWchar::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeWchar::pool_size-1]);
        }
    }
}

void
SgTypeWchar::deleteMemoryPool() {
    for (auto p: SgTypeWchar::pools) {
        ROSE_FREE(p);
    }
    SgTypeWchar::next_node = nullptr;
    SgTypeWchar::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeWchar::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeWchar) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeWchar)};
    for (size_t blockIndex = SgTypeWchar::pools.size(); (blockIndex * SgTypeWchar::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeWchar) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeWchar) = %" PRIuPTR " SgTypeWchar::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeWchar),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeWchar),SgTypeWchar::pool_size);
        }
#endif

        SgTypeWchar* pointer{static_cast<SgTypeWchar*>(ROSE_MALLOC(SgTypeWchar::pool_size * sizeof(SgTypeWchar)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeWchar::pool_size * sizeof(SgTypeWchar));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeWchar::pool_size * sizeof(SgTypeWchar));
#endif
        SgTypeWchar::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeWchar::pool_size * sizeof(SgTypeWchar), V_SgTypeWchar));

        if (SgTypeWchar::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeWchar* blkptr{reinterpret_cast<SgTypeWchar*>(SgTypeWchar::pools[blockIndex-1])};
                blkptr[SgTypeWchar::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeWchar::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeWchar::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeWchar::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeWchar::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeWchar::pool_size - 1};
    SgTypeWchar* testPointer{reinterpret_cast<SgTypeWchar*>(SgTypeWchar::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeWchar::pool_size * (SgTypeWchar::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeWcharStroageClass
 * from its counterpart of SgTypeWchar. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeWchar::initializeStorageClassArray(SgTypeWcharStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeWchar::pools.begin(); block != SgTypeWchar::pools.end(); ++block) {
        SgTypeWchar* pointer{reinterpret_cast<SgTypeWchar*>(*block)};
        for (unsigned i = 0; i < SgTypeWchar::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 33618 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeChar16 that takes its corresponding StorageClass as parameter
 */
SgTypeChar16::SgTypeChar16(const SgTypeChar16StorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 33628 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeChar16*
SgTypeChar16::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeChar16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar16)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar16)};
        unsigned long positionInPool{localIndex % SgTypeChar16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeChar16::pool_size};
        returnPointer = &(((SgTypeChar16*)(SgTypeChar16::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeChar16*
SgTypeChar16::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeChar16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar16)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeChar16)};
        unsigned long positionInPool{localIndex % SgTypeChar16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeChar16::pool_size};
        returnPointer = &(((SgTypeChar16*)(SgTypeChar16::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeChar16::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeChar16::pools.begin(); block != SgTypeChar16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeChar16*>(*block)};
        for (unsigned i = 0; i < SgTypeChar16::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeChar16*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeChar16::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeChar16* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeChar16::pools.begin(); block != SgTypeChar16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeChar16*>(*block)};
        for (unsigned i = 0; i < SgTypeChar16::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeChar16::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeChar16::clearMemoryPool() {
    SgTypeChar16* tempPointer{nullptr};
    if (SgTypeChar16::pools.empty() == false) {
        auto block{SgTypeChar16::pools.begin()};
        SgTypeChar16::next_node = reinterpret_cast<SgTypeChar16*>(*block);
        for (; block != SgTypeChar16::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeChar16*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeChar16::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeChar16::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeChar16::pool_size-1]);
        }
    }
}

void
SgTypeChar16::deleteMemoryPool() {
    for (auto p: SgTypeChar16::pools) {
        ROSE_FREE(p);
    }
    SgTypeChar16::next_node = nullptr;
    SgTypeChar16::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeChar16::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar16) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar16)};
    for (size_t blockIndex = SgTypeChar16::pools.size(); (blockIndex * SgTypeChar16::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar16) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar16) = %" PRIuPTR " SgTypeChar16::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar16),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar16),SgTypeChar16::pool_size);
        }
#endif

        SgTypeChar16* pointer{static_cast<SgTypeChar16*>(ROSE_MALLOC(SgTypeChar16::pool_size * sizeof(SgTypeChar16)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeChar16::pool_size * sizeof(SgTypeChar16));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeChar16::pool_size * sizeof(SgTypeChar16));
#endif
        SgTypeChar16::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeChar16::pool_size * sizeof(SgTypeChar16), V_SgTypeChar16));

        if (SgTypeChar16::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeChar16* blkptr{reinterpret_cast<SgTypeChar16*>(SgTypeChar16::pools[blockIndex-1])};
                blkptr[SgTypeChar16::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeChar16::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeChar16::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeChar16::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeChar16::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeChar16::pool_size - 1};
    SgTypeChar16* testPointer{reinterpret_cast<SgTypeChar16*>(SgTypeChar16::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeChar16::pool_size * (SgTypeChar16::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeChar16StroageClass
 * from its counterpart of SgTypeChar16. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeChar16::initializeStorageClassArray(SgTypeChar16StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeChar16::pools.begin(); block != SgTypeChar16::pools.end(); ++block) {
        SgTypeChar16* pointer{reinterpret_cast<SgTypeChar16*>(*block)};
        for (unsigned i = 0; i < SgTypeChar16::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 33845 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeChar32 that takes its corresponding StorageClass as parameter
 */
SgTypeChar32::SgTypeChar32(const SgTypeChar32StorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 33855 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeChar32*
SgTypeChar32::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeChar32* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar32)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar32 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeChar32)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar32)};
        unsigned long positionInPool{localIndex % SgTypeChar32::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeChar32::pool_size};
        returnPointer = &(((SgTypeChar32*)(SgTypeChar32::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeChar32*
SgTypeChar32::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeChar32* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar32)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar32 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeChar32)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeChar32)};
        unsigned long positionInPool{localIndex % SgTypeChar32::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeChar32::pool_size};
        returnPointer = &(((SgTypeChar32*)(SgTypeChar32::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeChar32::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeChar32::pools.begin(); block != SgTypeChar32::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeChar32*>(*block)};
        for (unsigned i = 0; i < SgTypeChar32::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeChar32*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeChar32::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeChar32* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeChar32::pools.begin(); block != SgTypeChar32::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeChar32*>(*block)};
        for (unsigned i = 0; i < SgTypeChar32::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeChar32::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeChar32::clearMemoryPool() {
    SgTypeChar32* tempPointer{nullptr};
    if (SgTypeChar32::pools.empty() == false) {
        auto block{SgTypeChar32::pools.begin()};
        SgTypeChar32::next_node = reinterpret_cast<SgTypeChar32*>(*block);
        for (; block != SgTypeChar32::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeChar32*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeChar32::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeChar32::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeChar32::pool_size-1]);
        }
    }
}

void
SgTypeChar32::deleteMemoryPool() {
    for (auto p: SgTypeChar32::pools) {
        ROSE_FREE(p);
    }
    SgTypeChar32::next_node = nullptr;
    SgTypeChar32::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeChar32::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar32) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar32)};
    for (size_t blockIndex = SgTypeChar32::pools.size(); (blockIndex * SgTypeChar32::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar32) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar32) = %" PRIuPTR " SgTypeChar32::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeChar32),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeChar32),SgTypeChar32::pool_size);
        }
#endif

        SgTypeChar32* pointer{static_cast<SgTypeChar32*>(ROSE_MALLOC(SgTypeChar32::pool_size * sizeof(SgTypeChar32)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeChar32::pool_size * sizeof(SgTypeChar32));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeChar32::pool_size * sizeof(SgTypeChar32));
#endif
        SgTypeChar32::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeChar32::pool_size * sizeof(SgTypeChar32), V_SgTypeChar32));

        if (SgTypeChar32::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeChar32* blkptr{reinterpret_cast<SgTypeChar32*>(SgTypeChar32::pools[blockIndex-1])};
                blkptr[SgTypeChar32::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeChar32::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeChar32::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeChar32::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeChar32::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeChar32::pool_size - 1};
    SgTypeChar32* testPointer{reinterpret_cast<SgTypeChar32*>(SgTypeChar32::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeChar32::pool_size * (SgTypeChar32::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeChar32StroageClass
 * from its counterpart of SgTypeChar32. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeChar32::initializeStorageClassArray(SgTypeChar32StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeChar32::pools.begin(); block != SgTypeChar32::pools.end(); ++block) {
        SgTypeChar32* pointer{reinterpret_cast<SgTypeChar32*>(*block)};
        for (unsigned i = 0; i < SgTypeChar32::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 34072 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaModularType that takes its corresponding StorageClass as parameter
 */
SgAdaModularType::SgAdaModularType(const SgAdaModularTypeStorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 34082 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_modexpr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_modexpr));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaModularType*
SgAdaModularType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaModularType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaModularType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaModularType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaModularType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaModularType)};
        unsigned long positionInPool{localIndex % SgAdaModularType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaModularType::pool_size};
        returnPointer = &(((SgAdaModularType*)(SgAdaModularType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaModularType*
SgAdaModularType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaModularType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaModularType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaModularType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaModularType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaModularType)};
        unsigned long positionInPool{localIndex % SgAdaModularType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaModularType::pool_size};
        returnPointer = &(((SgAdaModularType*)(SgAdaModularType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaModularType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaModularType::pools.begin(); block != SgAdaModularType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaModularType*>(*block)};
        for (unsigned i = 0; i < SgAdaModularType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaModularType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaModularType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaModularType* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaModularType::pools.begin(); block != SgAdaModularType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaModularType*>(*block)};
        for (unsigned i = 0; i < SgAdaModularType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaModularType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaModularType::clearMemoryPool() {
    SgAdaModularType* tempPointer{nullptr};
    if (SgAdaModularType::pools.empty() == false) {
        auto block{SgAdaModularType::pools.begin()};
        SgAdaModularType::next_node = reinterpret_cast<SgAdaModularType*>(*block);
        for (; block != SgAdaModularType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaModularType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaModularType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaModularType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaModularType::pool_size-1]);
        }
    }
}

void
SgAdaModularType::deleteMemoryPool() {
    for (auto p: SgAdaModularType::pools) {
        ROSE_FREE(p);
    }
    SgAdaModularType::next_node = nullptr;
    SgAdaModularType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaModularType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaModularType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaModularType)};
    for (size_t blockIndex = SgAdaModularType::pools.size(); (blockIndex * SgAdaModularType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaModularType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaModularType) = %" PRIuPTR " SgAdaModularType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaModularType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaModularType),SgAdaModularType::pool_size);
        }
#endif

        SgAdaModularType* pointer{static_cast<SgAdaModularType*>(ROSE_MALLOC(SgAdaModularType::pool_size * sizeof(SgAdaModularType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaModularType::pool_size * sizeof(SgAdaModularType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaModularType::pool_size * sizeof(SgAdaModularType));
#endif
        SgAdaModularType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaModularType::pool_size * sizeof(SgAdaModularType), V_SgAdaModularType));

        if (SgAdaModularType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaModularType* blkptr{reinterpret_cast<SgAdaModularType*>(SgAdaModularType::pools[blockIndex-1])};
                blkptr[SgAdaModularType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaModularType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaModularType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaModularType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaModularType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaModularType::pool_size - 1};
    SgAdaModularType* testPointer{reinterpret_cast<SgAdaModularType*>(SgAdaModularType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaModularType::pool_size * (SgAdaModularType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaModularTypeStroageClass
 * from its counterpart of SgAdaModularType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaModularType::initializeStorageClassArray(SgAdaModularTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaModularType::pools.begin(); block != SgAdaModularType::pools.end(); ++block) {
        SgAdaModularType* pointer{reinterpret_cast<SgAdaModularType*>(*block)};
        for (unsigned i = 0; i < SgAdaModularType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 34300 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeBool that takes its corresponding StorageClass as parameter
 */
SgTypeBool::SgTypeBool(const SgTypeBoolStorageClass& storageSource)  : SgIntegralType (storageSource) {

/* #line 34310 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeBool*
SgTypeBool::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeBool* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeBool)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeBool + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeBool)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBool)};
        unsigned long positionInPool{localIndex % SgTypeBool::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeBool::pool_size};
        returnPointer = &(((SgTypeBool*)(SgTypeBool::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeBool*
SgTypeBool::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeBool* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeBool)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeBool + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeBool)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeBool)};
        unsigned long positionInPool{localIndex % SgTypeBool::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeBool::pool_size};
        returnPointer = &(((SgTypeBool*)(SgTypeBool::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeBool::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeBool::pools.begin(); block != SgTypeBool::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeBool*>(*block)};
        for (unsigned i = 0; i < SgTypeBool::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeBool*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeBool::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeBool* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeBool::pools.begin(); block != SgTypeBool::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeBool*>(*block)};
        for (unsigned i = 0; i < SgTypeBool::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeBool::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeBool::clearMemoryPool() {
    SgTypeBool* tempPointer{nullptr};
    if (SgTypeBool::pools.empty() == false) {
        auto block{SgTypeBool::pools.begin()};
        SgTypeBool::next_node = reinterpret_cast<SgTypeBool*>(*block);
        for (; block != SgTypeBool::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeBool*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeBool::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeBool::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeBool::pool_size-1]);
        }
    }
}

void
SgTypeBool::deleteMemoryPool() {
    for (auto p: SgTypeBool::pools) {
        ROSE_FREE(p);
    }
    SgTypeBool::next_node = nullptr;
    SgTypeBool::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeBool::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBool) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeBool)};
    for (size_t blockIndex = SgTypeBool::pools.size(); (blockIndex * SgTypeBool::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBool) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeBool) = %" PRIuPTR " SgTypeBool::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBool),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeBool),SgTypeBool::pool_size);
        }
#endif

        SgTypeBool* pointer{static_cast<SgTypeBool*>(ROSE_MALLOC(SgTypeBool::pool_size * sizeof(SgTypeBool)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeBool::pool_size * sizeof(SgTypeBool));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeBool::pool_size * sizeof(SgTypeBool));
#endif
        SgTypeBool::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeBool::pool_size * sizeof(SgTypeBool), V_SgTypeBool));

        if (SgTypeBool::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeBool* blkptr{reinterpret_cast<SgTypeBool*>(SgTypeBool::pools[blockIndex-1])};
                blkptr[SgTypeBool::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeBool::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeBool::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeBool::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeBool::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeBool::pool_size - 1};
    SgTypeBool* testPointer{reinterpret_cast<SgTypeBool*>(SgTypeBool::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeBool::pool_size * (SgTypeBool::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeBoolStroageClass
 * from its counterpart of SgTypeBool. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeBool::initializeStorageClassArray(SgTypeBoolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeBool::pools.begin(); block != SgTypeBool::pools.end(); ++block) {
        SgTypeBool* pointer{reinterpret_cast<SgTypeBool*>(*block)};
        for (unsigned i = 0; i < SgTypeBool::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 34527 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFloatingPointType that takes its corresponding StorageClass as parameter
 */
SgFloatingPointType::SgFloatingPointType(const SgFloatingPointTypeStorageClass& storageSource)  : SgType (storageSource) {

/* #line 34537 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFloatingPointType*
SgFloatingPointType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFloatingPointType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloatingPointType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloatingPointType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloatingPointType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatingPointType)};
        unsigned long positionInPool{localIndex % SgFloatingPointType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloatingPointType::pool_size};
        returnPointer = &(((SgFloatingPointType*)(SgFloatingPointType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFloatingPointType*
SgFloatingPointType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFloatingPointType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloatingPointType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloatingPointType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloatingPointType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFloatingPointType)};
        unsigned long positionInPool{localIndex % SgFloatingPointType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloatingPointType::pool_size};
        returnPointer = &(((SgFloatingPointType*)(SgFloatingPointType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFloatingPointType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFloatingPointType::pools.begin(); block != SgFloatingPointType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloatingPointType*>(*block)};
        for (unsigned i = 0; i < SgFloatingPointType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFloatingPointType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFloatingPointType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFloatingPointType* pointerOfLinkedList{nullptr};
    for (auto block = SgFloatingPointType::pools.begin(); block != SgFloatingPointType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloatingPointType*>(*block)};
        for (unsigned i = 0; i < SgFloatingPointType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFloatingPointType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFloatingPointType::clearMemoryPool() {
    SgFloatingPointType* tempPointer{nullptr};
    if (SgFloatingPointType::pools.empty() == false) {
        auto block{SgFloatingPointType::pools.begin()};
        SgFloatingPointType::next_node = reinterpret_cast<SgFloatingPointType*>(*block);
        for (; block != SgFloatingPointType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFloatingPointType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFloatingPointType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFloatingPointType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFloatingPointType::pool_size-1]);
        }
    }
}

void
SgFloatingPointType::deleteMemoryPool() {
    for (auto p: SgFloatingPointType::pools) {
        ROSE_FREE(p);
    }
    SgFloatingPointType::next_node = nullptr;
    SgFloatingPointType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFloatingPointType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatingPointType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloatingPointType)};
    for (size_t blockIndex = SgFloatingPointType::pools.size(); (blockIndex * SgFloatingPointType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatingPointType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloatingPointType) = %" PRIuPTR " SgFloatingPointType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatingPointType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloatingPointType),SgFloatingPointType::pool_size);
        }
#endif

        SgFloatingPointType* pointer{static_cast<SgFloatingPointType*>(ROSE_MALLOC(SgFloatingPointType::pool_size * sizeof(SgFloatingPointType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFloatingPointType::pool_size * sizeof(SgFloatingPointType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFloatingPointType::pool_size * sizeof(SgFloatingPointType));
#endif
        SgFloatingPointType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFloatingPointType::pool_size * sizeof(SgFloatingPointType), V_SgFloatingPointType));

        if (SgFloatingPointType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFloatingPointType* blkptr{reinterpret_cast<SgFloatingPointType*>(SgFloatingPointType::pools[blockIndex-1])};
                blkptr[SgFloatingPointType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFloatingPointType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFloatingPointType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFloatingPointType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFloatingPointType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFloatingPointType::pool_size - 1};
    SgFloatingPointType* testPointer{reinterpret_cast<SgFloatingPointType*>(SgFloatingPointType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFloatingPointType::pool_size * (SgFloatingPointType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFloatingPointTypeStroageClass
 * from its counterpart of SgFloatingPointType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFloatingPointType::initializeStorageClassArray(SgFloatingPointTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFloatingPointType::pools.begin(); block != SgFloatingPointType::pools.end(); ++block) {
        SgFloatingPointType* pointer{reinterpret_cast<SgFloatingPointType*>(*block)};
        for (unsigned i = 0; i < SgFloatingPointType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 34754 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat that takes its corresponding StorageClass as parameter
 */
SgTypeFloat::SgTypeFloat(const SgTypeFloatStorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 34764 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat*
SgTypeFloat::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat)};
        unsigned long positionInPool{localIndex % SgTypeFloat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat::pool_size};
        returnPointer = &(((SgTypeFloat*)(SgTypeFloat::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat*
SgTypeFloat::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat)};
        unsigned long positionInPool{localIndex % SgTypeFloat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat::pool_size};
        returnPointer = &(((SgTypeFloat*)(SgTypeFloat::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat::pools.begin(); block != SgTypeFloat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat::pools.begin(); block != SgTypeFloat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat::clearMemoryPool() {
    SgTypeFloat* tempPointer{nullptr};
    if (SgTypeFloat::pools.empty() == false) {
        auto block{SgTypeFloat::pools.begin()};
        SgTypeFloat::next_node = reinterpret_cast<SgTypeFloat*>(*block);
        for (; block != SgTypeFloat::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat::pool_size-1]);
        }
    }
}

void
SgTypeFloat::deleteMemoryPool() {
    for (auto p: SgTypeFloat::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat::next_node = nullptr;
    SgTypeFloat::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat)};
    for (size_t blockIndex = SgTypeFloat::pools.size(); (blockIndex * SgTypeFloat::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat) = %" PRIuPTR " SgTypeFloat::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat),SgTypeFloat::pool_size);
        }
#endif

        SgTypeFloat* pointer{static_cast<SgTypeFloat*>(ROSE_MALLOC(SgTypeFloat::pool_size * sizeof(SgTypeFloat)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat::pool_size * sizeof(SgTypeFloat));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat::pool_size * sizeof(SgTypeFloat));
#endif
        SgTypeFloat::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat::pool_size * sizeof(SgTypeFloat), V_SgTypeFloat));

        if (SgTypeFloat::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat* blkptr{reinterpret_cast<SgTypeFloat*>(SgTypeFloat::pools[blockIndex-1])};
                blkptr[SgTypeFloat::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat::pool_size - 1};
    SgTypeFloat* testPointer{reinterpret_cast<SgTypeFloat*>(SgTypeFloat::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat::pool_size * (SgTypeFloat::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloatStroageClass
 * from its counterpart of SgTypeFloat. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat::initializeStorageClassArray(SgTypeFloatStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat::pools.begin(); block != SgTypeFloat::pools.end(); ++block) {
        SgTypeFloat* pointer{reinterpret_cast<SgTypeFloat*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 34981 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat128 that takes its corresponding StorageClass as parameter
 */
SgTypeFloat128::SgTypeFloat128(const SgTypeFloat128StorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 34991 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat128*
SgTypeFloat128::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat128* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat128)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat128 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat128)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat128)};
        unsigned long positionInPool{localIndex % SgTypeFloat128::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat128::pool_size};
        returnPointer = &(((SgTypeFloat128*)(SgTypeFloat128::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat128*
SgTypeFloat128::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat128* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat128)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat128 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat128)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat128)};
        unsigned long positionInPool{localIndex % SgTypeFloat128::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat128::pool_size};
        returnPointer = &(((SgTypeFloat128*)(SgTypeFloat128::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat128::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat128::pools.begin(); block != SgTypeFloat128::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat128*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat128::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat128*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat128::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat128* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat128::pools.begin(); block != SgTypeFloat128::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat128*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat128::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat128::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat128::clearMemoryPool() {
    SgTypeFloat128* tempPointer{nullptr};
    if (SgTypeFloat128::pools.empty() == false) {
        auto block{SgTypeFloat128::pools.begin()};
        SgTypeFloat128::next_node = reinterpret_cast<SgTypeFloat128*>(*block);
        for (; block != SgTypeFloat128::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat128*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat128::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat128::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat128::pool_size-1]);
        }
    }
}

void
SgTypeFloat128::deleteMemoryPool() {
    for (auto p: SgTypeFloat128::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat128::next_node = nullptr;
    SgTypeFloat128::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat128::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat128) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat128)};
    for (size_t blockIndex = SgTypeFloat128::pools.size(); (blockIndex * SgTypeFloat128::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat128) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat128) = %" PRIuPTR " SgTypeFloat128::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat128),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat128),SgTypeFloat128::pool_size);
        }
#endif

        SgTypeFloat128* pointer{static_cast<SgTypeFloat128*>(ROSE_MALLOC(SgTypeFloat128::pool_size * sizeof(SgTypeFloat128)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat128::pool_size * sizeof(SgTypeFloat128));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat128::pool_size * sizeof(SgTypeFloat128));
#endif
        SgTypeFloat128::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat128::pool_size * sizeof(SgTypeFloat128), V_SgTypeFloat128));

        if (SgTypeFloat128::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat128* blkptr{reinterpret_cast<SgTypeFloat128*>(SgTypeFloat128::pools[blockIndex-1])};
                blkptr[SgTypeFloat128::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat128::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat128::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat128::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat128::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat128::pool_size - 1};
    SgTypeFloat128* testPointer{reinterpret_cast<SgTypeFloat128*>(SgTypeFloat128::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat128::pool_size * (SgTypeFloat128::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloat128StroageClass
 * from its counterpart of SgTypeFloat128. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat128::initializeStorageClassArray(SgTypeFloat128StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat128::pools.begin(); block != SgTypeFloat128::pools.end(); ++block) {
        SgTypeFloat128* pointer{reinterpret_cast<SgTypeFloat128*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat128::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 35208 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat80 that takes its corresponding StorageClass as parameter
 */
SgTypeFloat80::SgTypeFloat80(const SgTypeFloat80StorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 35218 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat80*
SgTypeFloat80::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat80* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat80)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat80 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat80)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat80)};
        unsigned long positionInPool{localIndex % SgTypeFloat80::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat80::pool_size};
        returnPointer = &(((SgTypeFloat80*)(SgTypeFloat80::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat80*
SgTypeFloat80::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat80* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat80)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat80 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat80)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat80)};
        unsigned long positionInPool{localIndex % SgTypeFloat80::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat80::pool_size};
        returnPointer = &(((SgTypeFloat80*)(SgTypeFloat80::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat80::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat80::pools.begin(); block != SgTypeFloat80::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat80*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat80::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat80*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat80::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat80* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat80::pools.begin(); block != SgTypeFloat80::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat80*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat80::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat80::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat80::clearMemoryPool() {
    SgTypeFloat80* tempPointer{nullptr};
    if (SgTypeFloat80::pools.empty() == false) {
        auto block{SgTypeFloat80::pools.begin()};
        SgTypeFloat80::next_node = reinterpret_cast<SgTypeFloat80*>(*block);
        for (; block != SgTypeFloat80::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat80*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat80::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat80::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat80::pool_size-1]);
        }
    }
}

void
SgTypeFloat80::deleteMemoryPool() {
    for (auto p: SgTypeFloat80::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat80::next_node = nullptr;
    SgTypeFloat80::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat80::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat80) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat80)};
    for (size_t blockIndex = SgTypeFloat80::pools.size(); (blockIndex * SgTypeFloat80::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat80) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat80) = %" PRIuPTR " SgTypeFloat80::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat80),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat80),SgTypeFloat80::pool_size);
        }
#endif

        SgTypeFloat80* pointer{static_cast<SgTypeFloat80*>(ROSE_MALLOC(SgTypeFloat80::pool_size * sizeof(SgTypeFloat80)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat80::pool_size * sizeof(SgTypeFloat80));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat80::pool_size * sizeof(SgTypeFloat80));
#endif
        SgTypeFloat80::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat80::pool_size * sizeof(SgTypeFloat80), V_SgTypeFloat80));

        if (SgTypeFloat80::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat80* blkptr{reinterpret_cast<SgTypeFloat80*>(SgTypeFloat80::pools[blockIndex-1])};
                blkptr[SgTypeFloat80::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat80::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat80::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat80::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat80::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat80::pool_size - 1};
    SgTypeFloat80* testPointer{reinterpret_cast<SgTypeFloat80*>(SgTypeFloat80::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat80::pool_size * (SgTypeFloat80::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloat80StroageClass
 * from its counterpart of SgTypeFloat80. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat80::initializeStorageClassArray(SgTypeFloat80StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat80::pools.begin(); block != SgTypeFloat80::pools.end(); ++block) {
        SgTypeFloat80* pointer{reinterpret_cast<SgTypeFloat80*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat80::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 35435 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeDouble that takes its corresponding StorageClass as parameter
 */
SgTypeDouble::SgTypeDouble(const SgTypeDoubleStorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 35445 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeDouble*
SgTypeDouble::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeDouble* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeDouble)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeDouble + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeDouble)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDouble)};
        unsigned long positionInPool{localIndex % SgTypeDouble::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeDouble::pool_size};
        returnPointer = &(((SgTypeDouble*)(SgTypeDouble::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeDouble*
SgTypeDouble::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeDouble* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeDouble)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeDouble + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeDouble)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeDouble)};
        unsigned long positionInPool{localIndex % SgTypeDouble::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeDouble::pool_size};
        returnPointer = &(((SgTypeDouble*)(SgTypeDouble::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeDouble::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeDouble::pools.begin(); block != SgTypeDouble::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeDouble*>(*block)};
        for (unsigned i = 0; i < SgTypeDouble::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeDouble*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeDouble::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeDouble* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeDouble::pools.begin(); block != SgTypeDouble::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeDouble*>(*block)};
        for (unsigned i = 0; i < SgTypeDouble::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeDouble::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeDouble::clearMemoryPool() {
    SgTypeDouble* tempPointer{nullptr};
    if (SgTypeDouble::pools.empty() == false) {
        auto block{SgTypeDouble::pools.begin()};
        SgTypeDouble::next_node = reinterpret_cast<SgTypeDouble*>(*block);
        for (; block != SgTypeDouble::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeDouble*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeDouble::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeDouble::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeDouble::pool_size-1]);
        }
    }
}

void
SgTypeDouble::deleteMemoryPool() {
    for (auto p: SgTypeDouble::pools) {
        ROSE_FREE(p);
    }
    SgTypeDouble::next_node = nullptr;
    SgTypeDouble::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeDouble::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDouble) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeDouble)};
    for (size_t blockIndex = SgTypeDouble::pools.size(); (blockIndex * SgTypeDouble::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDouble) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeDouble) = %" PRIuPTR " SgTypeDouble::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeDouble),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeDouble),SgTypeDouble::pool_size);
        }
#endif

        SgTypeDouble* pointer{static_cast<SgTypeDouble*>(ROSE_MALLOC(SgTypeDouble::pool_size * sizeof(SgTypeDouble)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeDouble::pool_size * sizeof(SgTypeDouble));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeDouble::pool_size * sizeof(SgTypeDouble));
#endif
        SgTypeDouble::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeDouble::pool_size * sizeof(SgTypeDouble), V_SgTypeDouble));

        if (SgTypeDouble::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeDouble* blkptr{reinterpret_cast<SgTypeDouble*>(SgTypeDouble::pools[blockIndex-1])};
                blkptr[SgTypeDouble::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeDouble::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeDouble::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeDouble::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeDouble::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeDouble::pool_size - 1};
    SgTypeDouble* testPointer{reinterpret_cast<SgTypeDouble*>(SgTypeDouble::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeDouble::pool_size * (SgTypeDouble::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeDoubleStroageClass
 * from its counterpart of SgTypeDouble. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeDouble::initializeStorageClassArray(SgTypeDoubleStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeDouble::pools.begin(); block != SgTypeDouble::pools.end(); ++block) {
        SgTypeDouble* pointer{reinterpret_cast<SgTypeDouble*>(*block)};
        for (unsigned i = 0; i < SgTypeDouble::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 35662 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeLongDouble that takes its corresponding StorageClass as parameter
 */
SgTypeLongDouble::SgTypeLongDouble(const SgTypeLongDoubleStorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 35672 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeLongDouble*
SgTypeLongDouble::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeLongDouble* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLongDouble)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLongDouble + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeLongDouble)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongDouble)};
        unsigned long positionInPool{localIndex % SgTypeLongDouble::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLongDouble::pool_size};
        returnPointer = &(((SgTypeLongDouble*)(SgTypeLongDouble::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeLongDouble*
SgTypeLongDouble::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeLongDouble* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLongDouble)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLongDouble + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeLongDouble)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeLongDouble)};
        unsigned long positionInPool{localIndex % SgTypeLongDouble::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeLongDouble::pool_size};
        returnPointer = &(((SgTypeLongDouble*)(SgTypeLongDouble::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeLongDouble::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeLongDouble::pools.begin(); block != SgTypeLongDouble::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLongDouble*>(*block)};
        for (unsigned i = 0; i < SgTypeLongDouble::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeLongDouble*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeLongDouble::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeLongDouble* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeLongDouble::pools.begin(); block != SgTypeLongDouble::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeLongDouble*>(*block)};
        for (unsigned i = 0; i < SgTypeLongDouble::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeLongDouble::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeLongDouble::clearMemoryPool() {
    SgTypeLongDouble* tempPointer{nullptr};
    if (SgTypeLongDouble::pools.empty() == false) {
        auto block{SgTypeLongDouble::pools.begin()};
        SgTypeLongDouble::next_node = reinterpret_cast<SgTypeLongDouble*>(*block);
        for (; block != SgTypeLongDouble::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeLongDouble*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeLongDouble::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeLongDouble::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeLongDouble::pool_size-1]);
        }
    }
}

void
SgTypeLongDouble::deleteMemoryPool() {
    for (auto p: SgTypeLongDouble::pools) {
        ROSE_FREE(p);
    }
    SgTypeLongDouble::next_node = nullptr;
    SgTypeLongDouble::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeLongDouble::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongDouble) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLongDouble)};
    for (size_t blockIndex = SgTypeLongDouble::pools.size(); (blockIndex * SgTypeLongDouble::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongDouble) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLongDouble) = %" PRIuPTR " SgTypeLongDouble::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeLongDouble),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeLongDouble),SgTypeLongDouble::pool_size);
        }
#endif

        SgTypeLongDouble* pointer{static_cast<SgTypeLongDouble*>(ROSE_MALLOC(SgTypeLongDouble::pool_size * sizeof(SgTypeLongDouble)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeLongDouble::pool_size * sizeof(SgTypeLongDouble));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeLongDouble::pool_size * sizeof(SgTypeLongDouble));
#endif
        SgTypeLongDouble::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeLongDouble::pool_size * sizeof(SgTypeLongDouble), V_SgTypeLongDouble));

        if (SgTypeLongDouble::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeLongDouble* blkptr{reinterpret_cast<SgTypeLongDouble*>(SgTypeLongDouble::pools[blockIndex-1])};
                blkptr[SgTypeLongDouble::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeLongDouble::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeLongDouble::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeLongDouble::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeLongDouble::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeLongDouble::pool_size - 1};
    SgTypeLongDouble* testPointer{reinterpret_cast<SgTypeLongDouble*>(SgTypeLongDouble::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeLongDouble::pool_size * (SgTypeLongDouble::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeLongDoubleStroageClass
 * from its counterpart of SgTypeLongDouble. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeLongDouble::initializeStorageClassArray(SgTypeLongDoubleStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeLongDouble::pools.begin(); block != SgTypeLongDouble::pools.end(); ++block) {
        SgTypeLongDouble* pointer{reinterpret_cast<SgTypeLongDouble*>(*block)};
        for (unsigned i = 0; i < SgTypeLongDouble::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 35889 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat16 that takes its corresponding StorageClass as parameter
 */
SgTypeFloat16::SgTypeFloat16(const SgTypeFloat16StorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 35899 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat16*
SgTypeFloat16::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat16)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat16)};
        unsigned long positionInPool{localIndex % SgTypeFloat16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat16::pool_size};
        returnPointer = &(((SgTypeFloat16*)(SgTypeFloat16::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat16*
SgTypeFloat16::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat16)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat16)};
        unsigned long positionInPool{localIndex % SgTypeFloat16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat16::pool_size};
        returnPointer = &(((SgTypeFloat16*)(SgTypeFloat16::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat16::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat16::pools.begin(); block != SgTypeFloat16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat16*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat16::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat16*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat16::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat16* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat16::pools.begin(); block != SgTypeFloat16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat16*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat16::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat16::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat16::clearMemoryPool() {
    SgTypeFloat16* tempPointer{nullptr};
    if (SgTypeFloat16::pools.empty() == false) {
        auto block{SgTypeFloat16::pools.begin()};
        SgTypeFloat16::next_node = reinterpret_cast<SgTypeFloat16*>(*block);
        for (; block != SgTypeFloat16::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat16*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat16::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat16::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat16::pool_size-1]);
        }
    }
}

void
SgTypeFloat16::deleteMemoryPool() {
    for (auto p: SgTypeFloat16::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat16::next_node = nullptr;
    SgTypeFloat16::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat16::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat16) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat16)};
    for (size_t blockIndex = SgTypeFloat16::pools.size(); (blockIndex * SgTypeFloat16::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat16) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat16) = %" PRIuPTR " SgTypeFloat16::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat16),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat16),SgTypeFloat16::pool_size);
        }
#endif

        SgTypeFloat16* pointer{static_cast<SgTypeFloat16*>(ROSE_MALLOC(SgTypeFloat16::pool_size * sizeof(SgTypeFloat16)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat16::pool_size * sizeof(SgTypeFloat16));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat16::pool_size * sizeof(SgTypeFloat16));
#endif
        SgTypeFloat16::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat16::pool_size * sizeof(SgTypeFloat16), V_SgTypeFloat16));

        if (SgTypeFloat16::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat16* blkptr{reinterpret_cast<SgTypeFloat16*>(SgTypeFloat16::pools[blockIndex-1])};
                blkptr[SgTypeFloat16::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat16::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat16::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat16::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat16::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat16::pool_size - 1};
    SgTypeFloat16* testPointer{reinterpret_cast<SgTypeFloat16*>(SgTypeFloat16::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat16::pool_size * (SgTypeFloat16::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloat16StroageClass
 * from its counterpart of SgTypeFloat16. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat16::initializeStorageClassArray(SgTypeFloat16StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat16::pools.begin(); block != SgTypeFloat16::pools.end(); ++block) {
        SgTypeFloat16* pointer{reinterpret_cast<SgTypeFloat16*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat16::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 36116 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeBFloat16 that takes its corresponding StorageClass as parameter
 */
SgTypeBFloat16::SgTypeBFloat16(const SgTypeBFloat16StorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 36126 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeBFloat16*
SgTypeBFloat16::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeBFloat16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeBFloat16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeBFloat16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeBFloat16)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBFloat16)};
        unsigned long positionInPool{localIndex % SgTypeBFloat16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeBFloat16::pool_size};
        returnPointer = &(((SgTypeBFloat16*)(SgTypeBFloat16::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeBFloat16*
SgTypeBFloat16::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeBFloat16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeBFloat16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeBFloat16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeBFloat16)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeBFloat16)};
        unsigned long positionInPool{localIndex % SgTypeBFloat16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeBFloat16::pool_size};
        returnPointer = &(((SgTypeBFloat16*)(SgTypeBFloat16::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeBFloat16::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeBFloat16::pools.begin(); block != SgTypeBFloat16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeBFloat16*>(*block)};
        for (unsigned i = 0; i < SgTypeBFloat16::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeBFloat16*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeBFloat16::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeBFloat16* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeBFloat16::pools.begin(); block != SgTypeBFloat16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeBFloat16*>(*block)};
        for (unsigned i = 0; i < SgTypeBFloat16::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeBFloat16::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeBFloat16::clearMemoryPool() {
    SgTypeBFloat16* tempPointer{nullptr};
    if (SgTypeBFloat16::pools.empty() == false) {
        auto block{SgTypeBFloat16::pools.begin()};
        SgTypeBFloat16::next_node = reinterpret_cast<SgTypeBFloat16*>(*block);
        for (; block != SgTypeBFloat16::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeBFloat16*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeBFloat16::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeBFloat16::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeBFloat16::pool_size-1]);
        }
    }
}

void
SgTypeBFloat16::deleteMemoryPool() {
    for (auto p: SgTypeBFloat16::pools) {
        ROSE_FREE(p);
    }
    SgTypeBFloat16::next_node = nullptr;
    SgTypeBFloat16::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeBFloat16::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBFloat16) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeBFloat16)};
    for (size_t blockIndex = SgTypeBFloat16::pools.size(); (blockIndex * SgTypeBFloat16::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBFloat16) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeBFloat16) = %" PRIuPTR " SgTypeBFloat16::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeBFloat16),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeBFloat16),SgTypeBFloat16::pool_size);
        }
#endif

        SgTypeBFloat16* pointer{static_cast<SgTypeBFloat16*>(ROSE_MALLOC(SgTypeBFloat16::pool_size * sizeof(SgTypeBFloat16)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeBFloat16::pool_size * sizeof(SgTypeBFloat16));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeBFloat16::pool_size * sizeof(SgTypeBFloat16));
#endif
        SgTypeBFloat16::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeBFloat16::pool_size * sizeof(SgTypeBFloat16), V_SgTypeBFloat16));

        if (SgTypeBFloat16::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeBFloat16* blkptr{reinterpret_cast<SgTypeBFloat16*>(SgTypeBFloat16::pools[blockIndex-1])};
                blkptr[SgTypeBFloat16::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeBFloat16::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeBFloat16::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeBFloat16::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeBFloat16::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeBFloat16::pool_size - 1};
    SgTypeBFloat16* testPointer{reinterpret_cast<SgTypeBFloat16*>(SgTypeBFloat16::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeBFloat16::pool_size * (SgTypeBFloat16::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeBFloat16StroageClass
 * from its counterpart of SgTypeBFloat16. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeBFloat16::initializeStorageClassArray(SgTypeBFloat16StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeBFloat16::pools.begin(); block != SgTypeBFloat16::pools.end(); ++block) {
        SgTypeBFloat16* pointer{reinterpret_cast<SgTypeBFloat16*>(*block)};
        for (unsigned i = 0; i < SgTypeBFloat16::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 36343 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFp16 that takes its corresponding StorageClass as parameter
 */
SgTypeFp16::SgTypeFp16(const SgTypeFp16StorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 36353 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFp16*
SgTypeFp16::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFp16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFp16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFp16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFp16)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFp16)};
        unsigned long positionInPool{localIndex % SgTypeFp16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFp16::pool_size};
        returnPointer = &(((SgTypeFp16*)(SgTypeFp16::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFp16*
SgTypeFp16::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFp16* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFp16)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFp16 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFp16)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFp16)};
        unsigned long positionInPool{localIndex % SgTypeFp16::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFp16::pool_size};
        returnPointer = &(((SgTypeFp16*)(SgTypeFp16::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFp16::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFp16::pools.begin(); block != SgTypeFp16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFp16*>(*block)};
        for (unsigned i = 0; i < SgTypeFp16::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFp16*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFp16::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFp16* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFp16::pools.begin(); block != SgTypeFp16::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFp16*>(*block)};
        for (unsigned i = 0; i < SgTypeFp16::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFp16::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFp16::clearMemoryPool() {
    SgTypeFp16* tempPointer{nullptr};
    if (SgTypeFp16::pools.empty() == false) {
        auto block{SgTypeFp16::pools.begin()};
        SgTypeFp16::next_node = reinterpret_cast<SgTypeFp16*>(*block);
        for (; block != SgTypeFp16::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFp16*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFp16::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFp16::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFp16::pool_size-1]);
        }
    }
}

void
SgTypeFp16::deleteMemoryPool() {
    for (auto p: SgTypeFp16::pools) {
        ROSE_FREE(p);
    }
    SgTypeFp16::next_node = nullptr;
    SgTypeFp16::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFp16::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFp16) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFp16)};
    for (size_t blockIndex = SgTypeFp16::pools.size(); (blockIndex * SgTypeFp16::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFp16) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFp16) = %" PRIuPTR " SgTypeFp16::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFp16),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFp16),SgTypeFp16::pool_size);
        }
#endif

        SgTypeFp16* pointer{static_cast<SgTypeFp16*>(ROSE_MALLOC(SgTypeFp16::pool_size * sizeof(SgTypeFp16)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFp16::pool_size * sizeof(SgTypeFp16));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFp16::pool_size * sizeof(SgTypeFp16));
#endif
        SgTypeFp16::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFp16::pool_size * sizeof(SgTypeFp16), V_SgTypeFp16));

        if (SgTypeFp16::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFp16* blkptr{reinterpret_cast<SgTypeFp16*>(SgTypeFp16::pools[blockIndex-1])};
                blkptr[SgTypeFp16::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFp16::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFp16::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFp16::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFp16::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFp16::pool_size - 1};
    SgTypeFp16* testPointer{reinterpret_cast<SgTypeFp16*>(SgTypeFp16::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFp16::pool_size * (SgTypeFp16::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFp16StroageClass
 * from its counterpart of SgTypeFp16. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFp16::initializeStorageClassArray(SgTypeFp16StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFp16::pools.begin(); block != SgTypeFp16::pools.end(); ++block) {
        SgTypeFp16* pointer{reinterpret_cast<SgTypeFp16*>(*block)};
        for (unsigned i = 0; i < SgTypeFp16::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 36570 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat32x that takes its corresponding StorageClass as parameter
 */
SgTypeFloat32x::SgTypeFloat32x(const SgTypeFloat32xStorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 36580 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat32x*
SgTypeFloat32x::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat32x* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat32x)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat32x + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat32x)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32x)};
        unsigned long positionInPool{localIndex % SgTypeFloat32x::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat32x::pool_size};
        returnPointer = &(((SgTypeFloat32x*)(SgTypeFloat32x::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat32x*
SgTypeFloat32x::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat32x* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat32x)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat32x + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat32x)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat32x)};
        unsigned long positionInPool{localIndex % SgTypeFloat32x::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat32x::pool_size};
        returnPointer = &(((SgTypeFloat32x*)(SgTypeFloat32x::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat32x::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat32x::pools.begin(); block != SgTypeFloat32x::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat32x*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat32x::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat32x*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat32x::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat32x* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat32x::pools.begin(); block != SgTypeFloat32x::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat32x*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat32x::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat32x::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat32x::clearMemoryPool() {
    SgTypeFloat32x* tempPointer{nullptr};
    if (SgTypeFloat32x::pools.empty() == false) {
        auto block{SgTypeFloat32x::pools.begin()};
        SgTypeFloat32x::next_node = reinterpret_cast<SgTypeFloat32x*>(*block);
        for (; block != SgTypeFloat32x::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat32x*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat32x::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat32x::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat32x::pool_size-1]);
        }
    }
}

void
SgTypeFloat32x::deleteMemoryPool() {
    for (auto p: SgTypeFloat32x::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat32x::next_node = nullptr;
    SgTypeFloat32x::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat32x::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32x) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat32x)};
    for (size_t blockIndex = SgTypeFloat32x::pools.size(); (blockIndex * SgTypeFloat32x::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32x) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat32x) = %" PRIuPTR " SgTypeFloat32x::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32x),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat32x),SgTypeFloat32x::pool_size);
        }
#endif

        SgTypeFloat32x* pointer{static_cast<SgTypeFloat32x*>(ROSE_MALLOC(SgTypeFloat32x::pool_size * sizeof(SgTypeFloat32x)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat32x::pool_size * sizeof(SgTypeFloat32x));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat32x::pool_size * sizeof(SgTypeFloat32x));
#endif
        SgTypeFloat32x::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat32x::pool_size * sizeof(SgTypeFloat32x), V_SgTypeFloat32x));

        if (SgTypeFloat32x::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat32x* blkptr{reinterpret_cast<SgTypeFloat32x*>(SgTypeFloat32x::pools[blockIndex-1])};
                blkptr[SgTypeFloat32x::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat32x::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat32x::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat32x::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat32x::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat32x::pool_size - 1};
    SgTypeFloat32x* testPointer{reinterpret_cast<SgTypeFloat32x*>(SgTypeFloat32x::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat32x::pool_size * (SgTypeFloat32x::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloat32xStroageClass
 * from its counterpart of SgTypeFloat32x. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat32x::initializeStorageClassArray(SgTypeFloat32xStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat32x::pools.begin(); block != SgTypeFloat32x::pools.end(); ++block) {
        SgTypeFloat32x* pointer{reinterpret_cast<SgTypeFloat32x*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat32x::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 36797 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat64x that takes its corresponding StorageClass as parameter
 */
SgTypeFloat64x::SgTypeFloat64x(const SgTypeFloat64xStorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 36807 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat64x*
SgTypeFloat64x::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat64x* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat64x)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat64x + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat64x)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64x)};
        unsigned long positionInPool{localIndex % SgTypeFloat64x::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat64x::pool_size};
        returnPointer = &(((SgTypeFloat64x*)(SgTypeFloat64x::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat64x*
SgTypeFloat64x::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat64x* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat64x)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat64x + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat64x)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat64x)};
        unsigned long positionInPool{localIndex % SgTypeFloat64x::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat64x::pool_size};
        returnPointer = &(((SgTypeFloat64x*)(SgTypeFloat64x::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat64x::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat64x::pools.begin(); block != SgTypeFloat64x::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat64x*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat64x::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat64x*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat64x::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat64x* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat64x::pools.begin(); block != SgTypeFloat64x::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat64x*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat64x::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat64x::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat64x::clearMemoryPool() {
    SgTypeFloat64x* tempPointer{nullptr};
    if (SgTypeFloat64x::pools.empty() == false) {
        auto block{SgTypeFloat64x::pools.begin()};
        SgTypeFloat64x::next_node = reinterpret_cast<SgTypeFloat64x*>(*block);
        for (; block != SgTypeFloat64x::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat64x*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat64x::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat64x::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat64x::pool_size-1]);
        }
    }
}

void
SgTypeFloat64x::deleteMemoryPool() {
    for (auto p: SgTypeFloat64x::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat64x::next_node = nullptr;
    SgTypeFloat64x::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat64x::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64x) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat64x)};
    for (size_t blockIndex = SgTypeFloat64x::pools.size(); (blockIndex * SgTypeFloat64x::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64x) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat64x) = %" PRIuPTR " SgTypeFloat64x::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64x),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat64x),SgTypeFloat64x::pool_size);
        }
#endif

        SgTypeFloat64x* pointer{static_cast<SgTypeFloat64x*>(ROSE_MALLOC(SgTypeFloat64x::pool_size * sizeof(SgTypeFloat64x)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat64x::pool_size * sizeof(SgTypeFloat64x));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat64x::pool_size * sizeof(SgTypeFloat64x));
#endif
        SgTypeFloat64x::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat64x::pool_size * sizeof(SgTypeFloat64x), V_SgTypeFloat64x));

        if (SgTypeFloat64x::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat64x* blkptr{reinterpret_cast<SgTypeFloat64x*>(SgTypeFloat64x::pools[blockIndex-1])};
                blkptr[SgTypeFloat64x::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat64x::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat64x::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat64x::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat64x::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat64x::pool_size - 1};
    SgTypeFloat64x* testPointer{reinterpret_cast<SgTypeFloat64x*>(SgTypeFloat64x::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat64x::pool_size * (SgTypeFloat64x::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloat64xStroageClass
 * from its counterpart of SgTypeFloat64x. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat64x::initializeStorageClassArray(SgTypeFloat64xStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat64x::pools.begin(); block != SgTypeFloat64x::pools.end(); ++block) {
        SgTypeFloat64x* pointer{reinterpret_cast<SgTypeFloat64x*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat64x::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 37024 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat32 that takes its corresponding StorageClass as parameter
 */
SgTypeFloat32::SgTypeFloat32(const SgTypeFloat32StorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 37034 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat32*
SgTypeFloat32::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat32* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat32)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat32 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat32)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32)};
        unsigned long positionInPool{localIndex % SgTypeFloat32::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat32::pool_size};
        returnPointer = &(((SgTypeFloat32*)(SgTypeFloat32::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat32*
SgTypeFloat32::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat32* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat32)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat32 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat32)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat32)};
        unsigned long positionInPool{localIndex % SgTypeFloat32::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat32::pool_size};
        returnPointer = &(((SgTypeFloat32*)(SgTypeFloat32::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat32::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat32::pools.begin(); block != SgTypeFloat32::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat32*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat32::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat32*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat32::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat32* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat32::pools.begin(); block != SgTypeFloat32::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat32*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat32::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat32::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat32::clearMemoryPool() {
    SgTypeFloat32* tempPointer{nullptr};
    if (SgTypeFloat32::pools.empty() == false) {
        auto block{SgTypeFloat32::pools.begin()};
        SgTypeFloat32::next_node = reinterpret_cast<SgTypeFloat32*>(*block);
        for (; block != SgTypeFloat32::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat32*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat32::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat32::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat32::pool_size-1]);
        }
    }
}

void
SgTypeFloat32::deleteMemoryPool() {
    for (auto p: SgTypeFloat32::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat32::next_node = nullptr;
    SgTypeFloat32::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat32::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat32)};
    for (size_t blockIndex = SgTypeFloat32::pools.size(); (blockIndex * SgTypeFloat32::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat32) = %" PRIuPTR " SgTypeFloat32::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat32),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat32),SgTypeFloat32::pool_size);
        }
#endif

        SgTypeFloat32* pointer{static_cast<SgTypeFloat32*>(ROSE_MALLOC(SgTypeFloat32::pool_size * sizeof(SgTypeFloat32)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat32::pool_size * sizeof(SgTypeFloat32));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat32::pool_size * sizeof(SgTypeFloat32));
#endif
        SgTypeFloat32::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat32::pool_size * sizeof(SgTypeFloat32), V_SgTypeFloat32));

        if (SgTypeFloat32::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat32* blkptr{reinterpret_cast<SgTypeFloat32*>(SgTypeFloat32::pools[blockIndex-1])};
                blkptr[SgTypeFloat32::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat32::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat32::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat32::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat32::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat32::pool_size - 1};
    SgTypeFloat32* testPointer{reinterpret_cast<SgTypeFloat32*>(SgTypeFloat32::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat32::pool_size * (SgTypeFloat32::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloat32StroageClass
 * from its counterpart of SgTypeFloat32. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat32::initializeStorageClassArray(SgTypeFloat32StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat32::pools.begin(); block != SgTypeFloat32::pools.end(); ++block) {
        SgTypeFloat32* pointer{reinterpret_cast<SgTypeFloat32*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat32::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 37251 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeFloat64 that takes its corresponding StorageClass as parameter
 */
SgTypeFloat64::SgTypeFloat64(const SgTypeFloat64StorageClass& storageSource)  : SgFloatingPointType (storageSource) {

/* #line 37261 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeFloat64*
SgTypeFloat64::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeFloat64* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat64)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat64 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeFloat64)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64)};
        unsigned long positionInPool{localIndex % SgTypeFloat64::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat64::pool_size};
        returnPointer = &(((SgTypeFloat64*)(SgTypeFloat64::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeFloat64*
SgTypeFloat64::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeFloat64* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat64)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat64 + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeFloat64)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeFloat64)};
        unsigned long positionInPool{localIndex % SgTypeFloat64::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeFloat64::pool_size};
        returnPointer = &(((SgTypeFloat64*)(SgTypeFloat64::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeFloat64::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeFloat64::pools.begin(); block != SgTypeFloat64::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat64*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat64::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeFloat64*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeFloat64::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeFloat64* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeFloat64::pools.begin(); block != SgTypeFloat64::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeFloat64*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat64::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeFloat64::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeFloat64::clearMemoryPool() {
    SgTypeFloat64* tempPointer{nullptr};
    if (SgTypeFloat64::pools.empty() == false) {
        auto block{SgTypeFloat64::pools.begin()};
        SgTypeFloat64::next_node = reinterpret_cast<SgTypeFloat64*>(*block);
        for (; block != SgTypeFloat64::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeFloat64*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeFloat64::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeFloat64::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeFloat64::pool_size-1]);
        }
    }
}

void
SgTypeFloat64::deleteMemoryPool() {
    for (auto p: SgTypeFloat64::pools) {
        ROSE_FREE(p);
    }
    SgTypeFloat64::next_node = nullptr;
    SgTypeFloat64::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeFloat64::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat64)};
    for (size_t blockIndex = SgTypeFloat64::pools.size(); (blockIndex * SgTypeFloat64::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat64) = %" PRIuPTR " SgTypeFloat64::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeFloat64),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeFloat64),SgTypeFloat64::pool_size);
        }
#endif

        SgTypeFloat64* pointer{static_cast<SgTypeFloat64*>(ROSE_MALLOC(SgTypeFloat64::pool_size * sizeof(SgTypeFloat64)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeFloat64::pool_size * sizeof(SgTypeFloat64));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeFloat64::pool_size * sizeof(SgTypeFloat64));
#endif
        SgTypeFloat64::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeFloat64::pool_size * sizeof(SgTypeFloat64), V_SgTypeFloat64));

        if (SgTypeFloat64::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeFloat64* blkptr{reinterpret_cast<SgTypeFloat64*>(SgTypeFloat64::pools[blockIndex-1])};
                blkptr[SgTypeFloat64::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeFloat64::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeFloat64::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeFloat64::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeFloat64::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeFloat64::pool_size - 1};
    SgTypeFloat64* testPointer{reinterpret_cast<SgTypeFloat64*>(SgTypeFloat64::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeFloat64::pool_size * (SgTypeFloat64::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeFloat64StroageClass
 * from its counterpart of SgTypeFloat64. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeFloat64::initializeStorageClassArray(SgTypeFloat64StorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeFloat64::pools.begin(); block != SgTypeFloat64::pools.end(); ++block) {
        SgTypeFloat64* pointer{reinterpret_cast<SgTypeFloat64*>(*block)};
        for (unsigned i = 0; i < SgTypeFloat64::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 37478 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLocatedNode that takes its corresponding StorageClass as parameter
 */
SgLocatedNode::SgLocatedNode(const SgLocatedNodeStorageClass& storageSource)  : SgNode (storageSource) {

/* #line 37488 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_startOfConstruct = (Sg_File_Info*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_startOfConstruct));
     p_endOfConstruct = (Sg_File_Info*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_endOfConstruct));
    p_attachedPreprocessingInfoPtr = storageSource.storageOf_attachedPreprocessingInfoPtr.rebuildDataStoredInEasyStorageClass();
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();
    p_containsTransformationToSurroundingWhitespace = storageSource.storageOf_containsTransformationToSurroundingWhitespace;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLocatedNode*
SgLocatedNode::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLocatedNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLocatedNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLocatedNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLocatedNode)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNode)};
        unsigned long positionInPool{localIndex % SgLocatedNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLocatedNode::pool_size};
        returnPointer = &(((SgLocatedNode*)(SgLocatedNode::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLocatedNode*
SgLocatedNode::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLocatedNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLocatedNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLocatedNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLocatedNode)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLocatedNode)};
        unsigned long positionInPool{localIndex % SgLocatedNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLocatedNode::pool_size};
        returnPointer = &(((SgLocatedNode*)(SgLocatedNode::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLocatedNode::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLocatedNode::pools.begin(); block != SgLocatedNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLocatedNode*>(*block)};
        for (unsigned i = 0; i < SgLocatedNode::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLocatedNode*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLocatedNode::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLocatedNode* pointerOfLinkedList{nullptr};
    for (auto block = SgLocatedNode::pools.begin(); block != SgLocatedNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLocatedNode*>(*block)};
        for (unsigned i = 0; i < SgLocatedNode::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLocatedNode::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLocatedNode::clearMemoryPool() {
    SgLocatedNode* tempPointer{nullptr};
    if (SgLocatedNode::pools.empty() == false) {
        auto block{SgLocatedNode::pools.begin()};
        SgLocatedNode::next_node = reinterpret_cast<SgLocatedNode*>(*block);
        for (; block != SgLocatedNode::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLocatedNode*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLocatedNode::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLocatedNode::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLocatedNode::pool_size-1]);
        }
    }
}

void
SgLocatedNode::deleteMemoryPool() {
    for (auto p: SgLocatedNode::pools) {
        ROSE_FREE(p);
    }
    SgLocatedNode::next_node = nullptr;
    SgLocatedNode::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLocatedNode::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNode) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLocatedNode)};
    for (size_t blockIndex = SgLocatedNode::pools.size(); (blockIndex * SgLocatedNode::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNode) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLocatedNode) = %" PRIuPTR " SgLocatedNode::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNode),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLocatedNode),SgLocatedNode::pool_size);
        }
#endif

        SgLocatedNode* pointer{static_cast<SgLocatedNode*>(ROSE_MALLOC(SgLocatedNode::pool_size * sizeof(SgLocatedNode)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLocatedNode::pool_size * sizeof(SgLocatedNode));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLocatedNode::pool_size * sizeof(SgLocatedNode));
#endif
        SgLocatedNode::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLocatedNode::pool_size * sizeof(SgLocatedNode), V_SgLocatedNode));

        if (SgLocatedNode::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLocatedNode* blkptr{reinterpret_cast<SgLocatedNode*>(SgLocatedNode::pools[blockIndex-1])};
                blkptr[SgLocatedNode::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLocatedNode::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLocatedNode::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLocatedNode::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLocatedNode::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLocatedNode::pool_size - 1};
    SgLocatedNode* testPointer{reinterpret_cast<SgLocatedNode*>(SgLocatedNode::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLocatedNode::pool_size * (SgLocatedNode::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLocatedNodeStroageClass
 * from its counterpart of SgLocatedNode. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLocatedNode::initializeStorageClassArray(SgLocatedNodeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLocatedNode::pools.begin(); block != SgLocatedNode::pools.end(); ++block) {
        SgLocatedNode* pointer{reinterpret_cast<SgLocatedNode*>(*block)};
        for (unsigned i = 0; i < SgLocatedNode::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 37710 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgToken that takes its corresponding StorageClass as parameter
 */
SgToken::SgToken(const SgTokenStorageClass& storageSource)  : SgLocatedNode (storageSource) {

/* #line 37720 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_lexeme_string = storageSource.storageOf_lexeme_string.rebuildDataStoredInEasyStorageClass();
    p_classification_code = storageSource.storageOf_classification_code;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgToken*
SgToken::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgToken* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgToken)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgToken + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgToken)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgToken)};
        unsigned long positionInPool{localIndex % SgToken::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgToken::pool_size};
        returnPointer = &(((SgToken*)(SgToken::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgToken*
SgToken::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgToken* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgToken)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgToken + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgToken)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgToken)};
        unsigned long positionInPool{localIndex % SgToken::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgToken::pool_size};
        returnPointer = &(((SgToken*)(SgToken::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgToken::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgToken::pools.begin(); block != SgToken::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgToken*>(*block)};
        for (unsigned i = 0; i < SgToken::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgToken*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgToken::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgToken* pointerOfLinkedList{nullptr};
    for (auto block = SgToken::pools.begin(); block != SgToken::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgToken*>(*block)};
        for (unsigned i = 0; i < SgToken::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgToken::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgToken::clearMemoryPool() {
    SgToken* tempPointer{nullptr};
    if (SgToken::pools.empty() == false) {
        auto block{SgToken::pools.begin()};
        SgToken::next_node = reinterpret_cast<SgToken*>(*block);
        for (; block != SgToken::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgToken*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgToken::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgToken::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgToken::pool_size-1]);
        }
    }
}

void
SgToken::deleteMemoryPool() {
    for (auto p: SgToken::pools) {
        ROSE_FREE(p);
    }
    SgToken::next_node = nullptr;
    SgToken::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgToken::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgToken) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgToken)};
    for (size_t blockIndex = SgToken::pools.size(); (blockIndex * SgToken::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgToken) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgToken) = %" PRIuPTR " SgToken::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgToken),AST_FILE_IO::getPoolSizeOfNewAst(V_SgToken),SgToken::pool_size);
        }
#endif

        SgToken* pointer{static_cast<SgToken*>(ROSE_MALLOC(SgToken::pool_size * sizeof(SgToken)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgToken::pool_size * sizeof(SgToken));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgToken::pool_size * sizeof(SgToken));
#endif
        SgToken::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgToken::pool_size * sizeof(SgToken), V_SgToken));

        if (SgToken::next_node != nullptr) {
            if (blockIndex > 0) {
                SgToken* blkptr{reinterpret_cast<SgToken*>(SgToken::pools[blockIndex-1])};
                blkptr[SgToken::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgToken::next_node = pointer;
        }

        for (unsigned i = 0; i < SgToken::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgToken::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgToken::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgToken::pool_size - 1};
    SgToken* testPointer{reinterpret_cast<SgToken*>(SgToken::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgToken::pool_size * (SgToken::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTokenStroageClass
 * from its counterpart of SgToken. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgToken::initializeStorageClassArray(SgTokenStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgToken::pools.begin(); block != SgToken::pools.end(); ++block) {
        SgToken* pointer{reinterpret_cast<SgToken*>(*block)};
        for (unsigned i = 0; i < SgToken::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 37939 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLocatedNodeSupport that takes its corresponding StorageClass as parameter
 */
SgLocatedNodeSupport::SgLocatedNodeSupport(const SgLocatedNodeSupportStorageClass& storageSource)  : SgLocatedNode (storageSource) {

/* #line 37949 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLocatedNodeSupport*
SgLocatedNodeSupport::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLocatedNodeSupport* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLocatedNodeSupport)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLocatedNodeSupport + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLocatedNodeSupport)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNodeSupport)};
        unsigned long positionInPool{localIndex % SgLocatedNodeSupport::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLocatedNodeSupport::pool_size};
        returnPointer = &(((SgLocatedNodeSupport*)(SgLocatedNodeSupport::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLocatedNodeSupport*
SgLocatedNodeSupport::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLocatedNodeSupport* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLocatedNodeSupport)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLocatedNodeSupport + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLocatedNodeSupport)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLocatedNodeSupport)};
        unsigned long positionInPool{localIndex % SgLocatedNodeSupport::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLocatedNodeSupport::pool_size};
        returnPointer = &(((SgLocatedNodeSupport*)(SgLocatedNodeSupport::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLocatedNodeSupport::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLocatedNodeSupport::pools.begin(); block != SgLocatedNodeSupport::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLocatedNodeSupport*>(*block)};
        for (unsigned i = 0; i < SgLocatedNodeSupport::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLocatedNodeSupport*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLocatedNodeSupport::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLocatedNodeSupport* pointerOfLinkedList{nullptr};
    for (auto block = SgLocatedNodeSupport::pools.begin(); block != SgLocatedNodeSupport::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLocatedNodeSupport*>(*block)};
        for (unsigned i = 0; i < SgLocatedNodeSupport::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLocatedNodeSupport::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLocatedNodeSupport::clearMemoryPool() {
    SgLocatedNodeSupport* tempPointer{nullptr};
    if (SgLocatedNodeSupport::pools.empty() == false) {
        auto block{SgLocatedNodeSupport::pools.begin()};
        SgLocatedNodeSupport::next_node = reinterpret_cast<SgLocatedNodeSupport*>(*block);
        for (; block != SgLocatedNodeSupport::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLocatedNodeSupport*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLocatedNodeSupport::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLocatedNodeSupport::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLocatedNodeSupport::pool_size-1]);
        }
    }
}

void
SgLocatedNodeSupport::deleteMemoryPool() {
    for (auto p: SgLocatedNodeSupport::pools) {
        ROSE_FREE(p);
    }
    SgLocatedNodeSupport::next_node = nullptr;
    SgLocatedNodeSupport::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLocatedNodeSupport::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNodeSupport) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLocatedNodeSupport)};
    for (size_t blockIndex = SgLocatedNodeSupport::pools.size(); (blockIndex * SgLocatedNodeSupport::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNodeSupport) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLocatedNodeSupport) = %" PRIuPTR " SgLocatedNodeSupport::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLocatedNodeSupport),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLocatedNodeSupport),SgLocatedNodeSupport::pool_size);
        }
#endif

        SgLocatedNodeSupport* pointer{static_cast<SgLocatedNodeSupport*>(ROSE_MALLOC(SgLocatedNodeSupport::pool_size * sizeof(SgLocatedNodeSupport)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLocatedNodeSupport::pool_size * sizeof(SgLocatedNodeSupport));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLocatedNodeSupport::pool_size * sizeof(SgLocatedNodeSupport));
#endif
        SgLocatedNodeSupport::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLocatedNodeSupport::pool_size * sizeof(SgLocatedNodeSupport), V_SgLocatedNodeSupport));

        if (SgLocatedNodeSupport::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLocatedNodeSupport* blkptr{reinterpret_cast<SgLocatedNodeSupport*>(SgLocatedNodeSupport::pools[blockIndex-1])};
                blkptr[SgLocatedNodeSupport::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLocatedNodeSupport::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLocatedNodeSupport::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLocatedNodeSupport::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLocatedNodeSupport::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLocatedNodeSupport::pool_size - 1};
    SgLocatedNodeSupport* testPointer{reinterpret_cast<SgLocatedNodeSupport*>(SgLocatedNodeSupport::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLocatedNodeSupport::pool_size * (SgLocatedNodeSupport::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLocatedNodeSupportStroageClass
 * from its counterpart of SgLocatedNodeSupport. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLocatedNodeSupport::initializeStorageClassArray(SgLocatedNodeSupportStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLocatedNodeSupport::pools.begin(); block != SgLocatedNodeSupport::pools.end(); ++block) {
        SgLocatedNodeSupport* pointer{reinterpret_cast<SgLocatedNodeSupport*>(*block)};
        for (unsigned i = 0; i < SgLocatedNodeSupport::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 38166 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCommonBlockObject that takes its corresponding StorageClass as parameter
 */
SgCommonBlockObject::SgCommonBlockObject(const SgCommonBlockObjectStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 38176 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_block_name = storageSource.storageOf_block_name.rebuildDataStoredInEasyStorageClass();
     p_variable_reference_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_variable_reference_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCommonBlockObject*
SgCommonBlockObject::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCommonBlockObject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonBlockObject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonBlockObject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonBlockObject)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlockObject)};
        unsigned long positionInPool{localIndex % SgCommonBlockObject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommonBlockObject::pool_size};
        returnPointer = &(((SgCommonBlockObject*)(SgCommonBlockObject::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCommonBlockObject*
SgCommonBlockObject::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCommonBlockObject* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonBlockObject)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonBlockObject + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonBlockObject)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCommonBlockObject)};
        unsigned long positionInPool{localIndex % SgCommonBlockObject::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommonBlockObject::pool_size};
        returnPointer = &(((SgCommonBlockObject*)(SgCommonBlockObject::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCommonBlockObject::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCommonBlockObject::pools.begin(); block != SgCommonBlockObject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommonBlockObject*>(*block)};
        for (unsigned i = 0; i < SgCommonBlockObject::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCommonBlockObject*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCommonBlockObject::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCommonBlockObject* pointerOfLinkedList{nullptr};
    for (auto block = SgCommonBlockObject::pools.begin(); block != SgCommonBlockObject::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommonBlockObject*>(*block)};
        for (unsigned i = 0; i < SgCommonBlockObject::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCommonBlockObject::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCommonBlockObject::clearMemoryPool() {
    SgCommonBlockObject* tempPointer{nullptr};
    if (SgCommonBlockObject::pools.empty() == false) {
        auto block{SgCommonBlockObject::pools.begin()};
        SgCommonBlockObject::next_node = reinterpret_cast<SgCommonBlockObject*>(*block);
        for (; block != SgCommonBlockObject::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCommonBlockObject*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCommonBlockObject::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCommonBlockObject::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCommonBlockObject::pool_size-1]);
        }
    }
}

void
SgCommonBlockObject::deleteMemoryPool() {
    for (auto p: SgCommonBlockObject::pools) {
        ROSE_FREE(p);
    }
    SgCommonBlockObject::next_node = nullptr;
    SgCommonBlockObject::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCommonBlockObject::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlockObject) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonBlockObject)};
    for (size_t blockIndex = SgCommonBlockObject::pools.size(); (blockIndex * SgCommonBlockObject::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlockObject) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonBlockObject) = %" PRIuPTR " SgCommonBlockObject::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlockObject),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonBlockObject),SgCommonBlockObject::pool_size);
        }
#endif

        SgCommonBlockObject* pointer{static_cast<SgCommonBlockObject*>(ROSE_MALLOC(SgCommonBlockObject::pool_size * sizeof(SgCommonBlockObject)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCommonBlockObject::pool_size * sizeof(SgCommonBlockObject));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCommonBlockObject::pool_size * sizeof(SgCommonBlockObject));
#endif
        SgCommonBlockObject::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCommonBlockObject::pool_size * sizeof(SgCommonBlockObject), V_SgCommonBlockObject));

        if (SgCommonBlockObject::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCommonBlockObject* blkptr{reinterpret_cast<SgCommonBlockObject*>(SgCommonBlockObject::pools[blockIndex-1])};
                blkptr[SgCommonBlockObject::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCommonBlockObject::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCommonBlockObject::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCommonBlockObject::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCommonBlockObject::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCommonBlockObject::pool_size - 1};
    SgCommonBlockObject* testPointer{reinterpret_cast<SgCommonBlockObject*>(SgCommonBlockObject::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCommonBlockObject::pool_size * (SgCommonBlockObject::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCommonBlockObjectStroageClass
 * from its counterpart of SgCommonBlockObject. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCommonBlockObject::initializeStorageClassArray(SgCommonBlockObjectStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCommonBlockObject::pools.begin(); block != SgCommonBlockObject::pools.end(); ++block) {
        SgCommonBlockObject* pointer{reinterpret_cast<SgCommonBlockObject*>(*block)};
        for (unsigned i = 0; i < SgCommonBlockObject::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 38395 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgInitializedName that takes its corresponding StorageClass as parameter
 */
SgInitializedName::SgInitializedName(const SgInitializedNameStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 38405 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_microsoft_uuid_string = storageSource.storageOf_microsoft_uuid_string.rebuildDataStoredInEasyStorageClass();
     p_typeptr = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_typeptr));
     p_initptr = (SgInitializer*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initptr));
     p_prev_decl_item = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_prev_decl_item));
    p_is_initializer = storageSource.storageOf_is_initializer;
     p_declptr = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declptr));
     p_storageModifier = (SgStorageModifier*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_storageModifier));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
    p_preinitialization = storageSource.storageOf_preinitialization;
    p_register_name_code = storageSource.storageOf_register_name_code;
    p_excess_specifier = storageSource.storageOf_excess_specifier;
    p_register_name_string = storageSource.storageOf_register_name_string.rebuildDataStoredInEasyStorageClass();
    p_flags = storageSource.storageOf_flags;
     p_constant_or_type_argument_for_Alignas_keyword = (SgNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_constant_or_type_argument_for_Alignas_keyword));
    p_gnu_attribute_modifierVector = storageSource.storageOf_gnu_attribute_modifierVector.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_initialization_priority = storageSource.storageOf_gnu_attribute_initialization_priority;
    p_gnu_attribute_named_weak_reference = storageSource.storageOf_gnu_attribute_named_weak_reference.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_named_alias = storageSource.storageOf_gnu_attribute_named_alias.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_cleanup_function = storageSource.storageOf_gnu_attribute_cleanup_function.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_section_name = storageSource.storageOf_gnu_attribute_section_name.rebuildDataStoredInEasyStorageClass();
    p_gnu_attribute_alignment = storageSource.storageOf_gnu_attribute_alignment;
    p_gnu_attribute_visability = storageSource.storageOf_gnu_attribute_visability;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_name_qualification_length_for_type = storageSource.storageOf_name_qualification_length_for_type;
     p_auto_decltype = (SgType *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_auto_decltype));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgInitializedName*
SgInitializedName::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgInitializedName* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInitializedName)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInitializedName + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInitializedName)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializedName)};
        unsigned long positionInPool{localIndex % SgInitializedName::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInitializedName::pool_size};
        returnPointer = &(((SgInitializedName*)(SgInitializedName::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgInitializedName*
SgInitializedName::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgInitializedName* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInitializedName)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInitializedName + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInitializedName)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgInitializedName)};
        unsigned long positionInPool{localIndex % SgInitializedName::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInitializedName::pool_size};
        returnPointer = &(((SgInitializedName*)(SgInitializedName::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgInitializedName::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgInitializedName::pools.begin(); block != SgInitializedName::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInitializedName*>(*block)};
        for (unsigned i = 0; i < SgInitializedName::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgInitializedName*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgInitializedName::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgInitializedName* pointerOfLinkedList{nullptr};
    for (auto block = SgInitializedName::pools.begin(); block != SgInitializedName::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInitializedName*>(*block)};
        for (unsigned i = 0; i < SgInitializedName::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgInitializedName::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgInitializedName::clearMemoryPool() {
    SgInitializedName* tempPointer{nullptr};
    if (SgInitializedName::pools.empty() == false) {
        auto block{SgInitializedName::pools.begin()};
        SgInitializedName::next_node = reinterpret_cast<SgInitializedName*>(*block);
        for (; block != SgInitializedName::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgInitializedName*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgInitializedName::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgInitializedName::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgInitializedName::pool_size-1]);
        }
    }
}

void
SgInitializedName::deleteMemoryPool() {
    for (auto p: SgInitializedName::pools) {
        ROSE_FREE(p);
    }
    SgInitializedName::next_node = nullptr;
    SgInitializedName::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgInitializedName::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializedName) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgInitializedName)};
    for (size_t blockIndex = SgInitializedName::pools.size(); (blockIndex * SgInitializedName::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializedName) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgInitializedName) = %" PRIuPTR " SgInitializedName::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializedName),AST_FILE_IO::getPoolSizeOfNewAst(V_SgInitializedName),SgInitializedName::pool_size);
        }
#endif

        SgInitializedName* pointer{static_cast<SgInitializedName*>(ROSE_MALLOC(SgInitializedName::pool_size * sizeof(SgInitializedName)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgInitializedName::pool_size * sizeof(SgInitializedName));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgInitializedName::pool_size * sizeof(SgInitializedName));
#endif
        SgInitializedName::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgInitializedName::pool_size * sizeof(SgInitializedName), V_SgInitializedName));

        if (SgInitializedName::next_node != nullptr) {
            if (blockIndex > 0) {
                SgInitializedName* blkptr{reinterpret_cast<SgInitializedName*>(SgInitializedName::pools[blockIndex-1])};
                blkptr[SgInitializedName::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgInitializedName::next_node = pointer;
        }

        for (unsigned i = 0; i < SgInitializedName::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgInitializedName::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgInitializedName::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgInitializedName::pool_size - 1};
    SgInitializedName* testPointer{reinterpret_cast<SgInitializedName*>(SgInitializedName::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgInitializedName::pool_size * (SgInitializedName::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgInitializedNameStroageClass
 * from its counterpart of SgInitializedName. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgInitializedName::initializeStorageClassArray(SgInitializedNameStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgInitializedName::pools.begin(); block != SgInitializedName::pools.end(); ++block) {
        SgInitializedName* pointer{reinterpret_cast<SgInitializedName*>(*block)};
        for (unsigned i = 0; i < SgInitializedName::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 38648 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgInterfaceBody that takes its corresponding StorageClass as parameter
 */
SgInterfaceBody::SgInterfaceBody(const SgInterfaceBodyStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 38658 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_function_name = SgName ( storageSource.storageOf_function_name);
     p_functionDeclaration = (SgFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_functionDeclaration));
    p_use_function_name = storageSource.storageOf_use_function_name;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgInterfaceBody*
SgInterfaceBody::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgInterfaceBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceBody)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceBody)};
        unsigned long positionInPool{localIndex % SgInterfaceBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInterfaceBody::pool_size};
        returnPointer = &(((SgInterfaceBody*)(SgInterfaceBody::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgInterfaceBody*
SgInterfaceBody::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgInterfaceBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceBody)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgInterfaceBody)};
        unsigned long positionInPool{localIndex % SgInterfaceBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInterfaceBody::pool_size};
        returnPointer = &(((SgInterfaceBody*)(SgInterfaceBody::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgInterfaceBody::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgInterfaceBody::pools.begin(); block != SgInterfaceBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInterfaceBody*>(*block)};
        for (unsigned i = 0; i < SgInterfaceBody::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgInterfaceBody*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgInterfaceBody::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgInterfaceBody* pointerOfLinkedList{nullptr};
    for (auto block = SgInterfaceBody::pools.begin(); block != SgInterfaceBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInterfaceBody*>(*block)};
        for (unsigned i = 0; i < SgInterfaceBody::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgInterfaceBody::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgInterfaceBody::clearMemoryPool() {
    SgInterfaceBody* tempPointer{nullptr};
    if (SgInterfaceBody::pools.empty() == false) {
        auto block{SgInterfaceBody::pools.begin()};
        SgInterfaceBody::next_node = reinterpret_cast<SgInterfaceBody*>(*block);
        for (; block != SgInterfaceBody::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgInterfaceBody*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgInterfaceBody::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgInterfaceBody::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgInterfaceBody::pool_size-1]);
        }
    }
}

void
SgInterfaceBody::deleteMemoryPool() {
    for (auto p: SgInterfaceBody::pools) {
        ROSE_FREE(p);
    }
    SgInterfaceBody::next_node = nullptr;
    SgInterfaceBody::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgInterfaceBody::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceBody) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceBody)};
    for (size_t blockIndex = SgInterfaceBody::pools.size(); (blockIndex * SgInterfaceBody::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceBody) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceBody) = %" PRIuPTR " SgInterfaceBody::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceBody),AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceBody),SgInterfaceBody::pool_size);
        }
#endif

        SgInterfaceBody* pointer{static_cast<SgInterfaceBody*>(ROSE_MALLOC(SgInterfaceBody::pool_size * sizeof(SgInterfaceBody)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgInterfaceBody::pool_size * sizeof(SgInterfaceBody));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgInterfaceBody::pool_size * sizeof(SgInterfaceBody));
#endif
        SgInterfaceBody::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgInterfaceBody::pool_size * sizeof(SgInterfaceBody), V_SgInterfaceBody));

        if (SgInterfaceBody::next_node != nullptr) {
            if (blockIndex > 0) {
                SgInterfaceBody* blkptr{reinterpret_cast<SgInterfaceBody*>(SgInterfaceBody::pools[blockIndex-1])};
                blkptr[SgInterfaceBody::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgInterfaceBody::next_node = pointer;
        }

        for (unsigned i = 0; i < SgInterfaceBody::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgInterfaceBody::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgInterfaceBody::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgInterfaceBody::pool_size - 1};
    SgInterfaceBody* testPointer{reinterpret_cast<SgInterfaceBody*>(SgInterfaceBody::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgInterfaceBody::pool_size * (SgInterfaceBody::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgInterfaceBodyStroageClass
 * from its counterpart of SgInterfaceBody. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgInterfaceBody::initializeStorageClassArray(SgInterfaceBodyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgInterfaceBody::pools.begin(); block != SgInterfaceBody::pools.end(); ++block) {
        SgInterfaceBody* pointer{reinterpret_cast<SgInterfaceBody*>(*block)};
        for (unsigned i = 0; i < SgInterfaceBody::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 38878 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgHeaderFileBody that takes its corresponding StorageClass as parameter
 */
SgHeaderFileBody::SgHeaderFileBody(const SgHeaderFileBodyStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 38888 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_include_file = (SgSourceFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_include_file));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgHeaderFileBody*
SgHeaderFileBody::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgHeaderFileBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHeaderFileBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHeaderFileBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHeaderFileBody)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileBody)};
        unsigned long positionInPool{localIndex % SgHeaderFileBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgHeaderFileBody::pool_size};
        returnPointer = &(((SgHeaderFileBody*)(SgHeaderFileBody::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgHeaderFileBody*
SgHeaderFileBody::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgHeaderFileBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHeaderFileBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHeaderFileBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHeaderFileBody)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgHeaderFileBody)};
        unsigned long positionInPool{localIndex % SgHeaderFileBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgHeaderFileBody::pool_size};
        returnPointer = &(((SgHeaderFileBody*)(SgHeaderFileBody::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgHeaderFileBody::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgHeaderFileBody::pools.begin(); block != SgHeaderFileBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgHeaderFileBody*>(*block)};
        for (unsigned i = 0; i < SgHeaderFileBody::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgHeaderFileBody*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgHeaderFileBody::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgHeaderFileBody* pointerOfLinkedList{nullptr};
    for (auto block = SgHeaderFileBody::pools.begin(); block != SgHeaderFileBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgHeaderFileBody*>(*block)};
        for (unsigned i = 0; i < SgHeaderFileBody::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgHeaderFileBody::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgHeaderFileBody::clearMemoryPool() {
    SgHeaderFileBody* tempPointer{nullptr};
    if (SgHeaderFileBody::pools.empty() == false) {
        auto block{SgHeaderFileBody::pools.begin()};
        SgHeaderFileBody::next_node = reinterpret_cast<SgHeaderFileBody*>(*block);
        for (; block != SgHeaderFileBody::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgHeaderFileBody*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgHeaderFileBody::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgHeaderFileBody::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgHeaderFileBody::pool_size-1]);
        }
    }
}

void
SgHeaderFileBody::deleteMemoryPool() {
    for (auto p: SgHeaderFileBody::pools) {
        ROSE_FREE(p);
    }
    SgHeaderFileBody::next_node = nullptr;
    SgHeaderFileBody::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgHeaderFileBody::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileBody) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgHeaderFileBody)};
    for (size_t blockIndex = SgHeaderFileBody::pools.size(); (blockIndex * SgHeaderFileBody::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileBody) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgHeaderFileBody) = %" PRIuPTR " SgHeaderFileBody::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgHeaderFileBody),AST_FILE_IO::getPoolSizeOfNewAst(V_SgHeaderFileBody),SgHeaderFileBody::pool_size);
        }
#endif

        SgHeaderFileBody* pointer{static_cast<SgHeaderFileBody*>(ROSE_MALLOC(SgHeaderFileBody::pool_size * sizeof(SgHeaderFileBody)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgHeaderFileBody::pool_size * sizeof(SgHeaderFileBody));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgHeaderFileBody::pool_size * sizeof(SgHeaderFileBody));
#endif
        SgHeaderFileBody::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgHeaderFileBody::pool_size * sizeof(SgHeaderFileBody), V_SgHeaderFileBody));

        if (SgHeaderFileBody::next_node != nullptr) {
            if (blockIndex > 0) {
                SgHeaderFileBody* blkptr{reinterpret_cast<SgHeaderFileBody*>(SgHeaderFileBody::pools[blockIndex-1])};
                blkptr[SgHeaderFileBody::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgHeaderFileBody::next_node = pointer;
        }

        for (unsigned i = 0; i < SgHeaderFileBody::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgHeaderFileBody::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgHeaderFileBody::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgHeaderFileBody::pool_size - 1};
    SgHeaderFileBody* testPointer{reinterpret_cast<SgHeaderFileBody*>(SgHeaderFileBody::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgHeaderFileBody::pool_size * (SgHeaderFileBody::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgHeaderFileBodyStroageClass
 * from its counterpart of SgHeaderFileBody. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgHeaderFileBody::initializeStorageClassArray(SgHeaderFileBodyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgHeaderFileBody::pools.begin(); block != SgHeaderFileBody::pools.end(); ++block) {
        SgHeaderFileBody* pointer{reinterpret_cast<SgHeaderFileBody*>(*block)};
        for (unsigned i = 0; i < SgHeaderFileBody::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 39106 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRenamePair that takes its corresponding StorageClass as parameter
 */
SgRenamePair::SgRenamePair(const SgRenamePairStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 39116 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_local_name = SgName ( storageSource.storageOf_local_name);
    p_use_name = SgName ( storageSource.storageOf_use_name);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRenamePair*
SgRenamePair::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRenamePair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRenamePair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRenamePair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRenamePair)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRenamePair)};
        unsigned long positionInPool{localIndex % SgRenamePair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRenamePair::pool_size};
        returnPointer = &(((SgRenamePair*)(SgRenamePair::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRenamePair*
SgRenamePair::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRenamePair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRenamePair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRenamePair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRenamePair)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRenamePair)};
        unsigned long positionInPool{localIndex % SgRenamePair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRenamePair::pool_size};
        returnPointer = &(((SgRenamePair*)(SgRenamePair::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRenamePair::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRenamePair::pools.begin(); block != SgRenamePair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRenamePair*>(*block)};
        for (unsigned i = 0; i < SgRenamePair::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRenamePair*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRenamePair::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRenamePair* pointerOfLinkedList{nullptr};
    for (auto block = SgRenamePair::pools.begin(); block != SgRenamePair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRenamePair*>(*block)};
        for (unsigned i = 0; i < SgRenamePair::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRenamePair::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRenamePair::clearMemoryPool() {
    SgRenamePair* tempPointer{nullptr};
    if (SgRenamePair::pools.empty() == false) {
        auto block{SgRenamePair::pools.begin()};
        SgRenamePair::next_node = reinterpret_cast<SgRenamePair*>(*block);
        for (; block != SgRenamePair::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRenamePair*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRenamePair::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRenamePair::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRenamePair::pool_size-1]);
        }
    }
}

void
SgRenamePair::deleteMemoryPool() {
    for (auto p: SgRenamePair::pools) {
        ROSE_FREE(p);
    }
    SgRenamePair::next_node = nullptr;
    SgRenamePair::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRenamePair::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRenamePair) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRenamePair)};
    for (size_t blockIndex = SgRenamePair::pools.size(); (blockIndex * SgRenamePair::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRenamePair) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRenamePair) = %" PRIuPTR " SgRenamePair::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRenamePair),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRenamePair),SgRenamePair::pool_size);
        }
#endif

        SgRenamePair* pointer{static_cast<SgRenamePair*>(ROSE_MALLOC(SgRenamePair::pool_size * sizeof(SgRenamePair)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRenamePair::pool_size * sizeof(SgRenamePair));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRenamePair::pool_size * sizeof(SgRenamePair));
#endif
        SgRenamePair::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRenamePair::pool_size * sizeof(SgRenamePair), V_SgRenamePair));

        if (SgRenamePair::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRenamePair* blkptr{reinterpret_cast<SgRenamePair*>(SgRenamePair::pools[blockIndex-1])};
                blkptr[SgRenamePair::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRenamePair::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRenamePair::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRenamePair::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRenamePair::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRenamePair::pool_size - 1};
    SgRenamePair* testPointer{reinterpret_cast<SgRenamePair*>(SgRenamePair::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRenamePair::pool_size * (SgRenamePair::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRenamePairStroageClass
 * from its counterpart of SgRenamePair. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRenamePair::initializeStorageClassArray(SgRenamePairStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRenamePair::pools.begin(); block != SgRenamePair::pools.end(); ++block) {
        SgRenamePair* pointer{reinterpret_cast<SgRenamePair*>(*block)};
        for (unsigned i = 0; i < SgRenamePair::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 39335 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaMemberValuePair that takes its corresponding StorageClass as parameter
 */
SgJavaMemberValuePair::SgJavaMemberValuePair(const SgJavaMemberValuePairStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 39345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaMemberValuePair*
SgJavaMemberValuePair::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaMemberValuePair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaMemberValuePair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaMemberValuePair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaMemberValuePair)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMemberValuePair)};
        unsigned long positionInPool{localIndex % SgJavaMemberValuePair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaMemberValuePair::pool_size};
        returnPointer = &(((SgJavaMemberValuePair*)(SgJavaMemberValuePair::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaMemberValuePair*
SgJavaMemberValuePair::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaMemberValuePair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaMemberValuePair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaMemberValuePair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaMemberValuePair)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaMemberValuePair)};
        unsigned long positionInPool{localIndex % SgJavaMemberValuePair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaMemberValuePair::pool_size};
        returnPointer = &(((SgJavaMemberValuePair*)(SgJavaMemberValuePair::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaMemberValuePair::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaMemberValuePair::pools.begin(); block != SgJavaMemberValuePair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaMemberValuePair*>(*block)};
        for (unsigned i = 0; i < SgJavaMemberValuePair::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaMemberValuePair*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaMemberValuePair::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaMemberValuePair* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaMemberValuePair::pools.begin(); block != SgJavaMemberValuePair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaMemberValuePair*>(*block)};
        for (unsigned i = 0; i < SgJavaMemberValuePair::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaMemberValuePair::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaMemberValuePair::clearMemoryPool() {
    SgJavaMemberValuePair* tempPointer{nullptr};
    if (SgJavaMemberValuePair::pools.empty() == false) {
        auto block{SgJavaMemberValuePair::pools.begin()};
        SgJavaMemberValuePair::next_node = reinterpret_cast<SgJavaMemberValuePair*>(*block);
        for (; block != SgJavaMemberValuePair::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaMemberValuePair*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaMemberValuePair::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaMemberValuePair::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaMemberValuePair::pool_size-1]);
        }
    }
}

void
SgJavaMemberValuePair::deleteMemoryPool() {
    for (auto p: SgJavaMemberValuePair::pools) {
        ROSE_FREE(p);
    }
    SgJavaMemberValuePair::next_node = nullptr;
    SgJavaMemberValuePair::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaMemberValuePair::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMemberValuePair) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaMemberValuePair)};
    for (size_t blockIndex = SgJavaMemberValuePair::pools.size(); (blockIndex * SgJavaMemberValuePair::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMemberValuePair) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaMemberValuePair) = %" PRIuPTR " SgJavaMemberValuePair::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMemberValuePair),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaMemberValuePair),SgJavaMemberValuePair::pool_size);
        }
#endif

        SgJavaMemberValuePair* pointer{static_cast<SgJavaMemberValuePair*>(ROSE_MALLOC(SgJavaMemberValuePair::pool_size * sizeof(SgJavaMemberValuePair)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaMemberValuePair::pool_size * sizeof(SgJavaMemberValuePair));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaMemberValuePair::pool_size * sizeof(SgJavaMemberValuePair));
#endif
        SgJavaMemberValuePair::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaMemberValuePair::pool_size * sizeof(SgJavaMemberValuePair), V_SgJavaMemberValuePair));

        if (SgJavaMemberValuePair::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaMemberValuePair* blkptr{reinterpret_cast<SgJavaMemberValuePair*>(SgJavaMemberValuePair::pools[blockIndex-1])};
                blkptr[SgJavaMemberValuePair::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaMemberValuePair::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaMemberValuePair::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaMemberValuePair::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaMemberValuePair::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaMemberValuePair::pool_size - 1};
    SgJavaMemberValuePair* testPointer{reinterpret_cast<SgJavaMemberValuePair*>(SgJavaMemberValuePair::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaMemberValuePair::pool_size * (SgJavaMemberValuePair::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaMemberValuePairStroageClass
 * from its counterpart of SgJavaMemberValuePair. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaMemberValuePair::initializeStorageClassArray(SgJavaMemberValuePairStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaMemberValuePair::pools.begin(); block != SgJavaMemberValuePair::pools.end(); ++block) {
        SgJavaMemberValuePair* pointer{reinterpret_cast<SgJavaMemberValuePair*>(*block)};
        for (unsigned i = 0; i < SgJavaMemberValuePair::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 39564 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpClause that takes its corresponding StorageClass as parameter
 */
SgOmpClause::SgOmpClause(const SgOmpClauseStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 39574 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpClause*
SgOmpClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClause)};
        unsigned long positionInPool{localIndex % SgOmpClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpClause::pool_size};
        returnPointer = &(((SgOmpClause*)(SgOmpClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpClause*
SgOmpClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpClause)};
        unsigned long positionInPool{localIndex % SgOmpClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpClause::pool_size};
        returnPointer = &(((SgOmpClause*)(SgOmpClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpClause::pools.begin(); block != SgOmpClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpClause*>(*block)};
        for (unsigned i = 0; i < SgOmpClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpClause::pools.begin(); block != SgOmpClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpClause*>(*block)};
        for (unsigned i = 0; i < SgOmpClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpClause::clearMemoryPool() {
    SgOmpClause* tempPointer{nullptr};
    if (SgOmpClause::pools.empty() == false) {
        auto block{SgOmpClause::pools.begin()};
        SgOmpClause::next_node = reinterpret_cast<SgOmpClause*>(*block);
        for (; block != SgOmpClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpClause::pool_size-1]);
        }
    }
}

void
SgOmpClause::deleteMemoryPool() {
    for (auto p: SgOmpClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpClause::next_node = nullptr;
    SgOmpClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpClause)};
    for (size_t blockIndex = SgOmpClause::pools.size(); (blockIndex * SgOmpClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpClause) = %" PRIuPTR " SgOmpClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpClause),SgOmpClause::pool_size);
        }
#endif

        SgOmpClause* pointer{static_cast<SgOmpClause*>(ROSE_MALLOC(SgOmpClause::pool_size * sizeof(SgOmpClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpClause::pool_size * sizeof(SgOmpClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpClause::pool_size * sizeof(SgOmpClause));
#endif
        SgOmpClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpClause::pool_size * sizeof(SgOmpClause), V_SgOmpClause));

        if (SgOmpClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpClause* blkptr{reinterpret_cast<SgOmpClause*>(SgOmpClause::pools[blockIndex-1])};
                blkptr[SgOmpClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpClause::pool_size - 1};
    SgOmpClause* testPointer{reinterpret_cast<SgOmpClause*>(SgOmpClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpClause::pool_size * (SgOmpClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpClauseStroageClass
 * from its counterpart of SgOmpClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpClause::initializeStorageClassArray(SgOmpClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpClause::pools.begin(); block != SgOmpClause::pools.end(); ++block) {
        SgOmpClause* pointer{reinterpret_cast<SgOmpClause*>(*block)};
        for (unsigned i = 0; i < SgOmpClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 39791 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpNowaitClause that takes its corresponding StorageClass as parameter
 */
SgOmpNowaitClause::SgOmpNowaitClause(const SgOmpNowaitClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 39801 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpNowaitClause*
SgOmpNowaitClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpNowaitClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNowaitClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNowaitClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNowaitClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNowaitClause)};
        unsigned long positionInPool{localIndex % SgOmpNowaitClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpNowaitClause::pool_size};
        returnPointer = &(((SgOmpNowaitClause*)(SgOmpNowaitClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpNowaitClause*
SgOmpNowaitClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpNowaitClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNowaitClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNowaitClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNowaitClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpNowaitClause)};
        unsigned long positionInPool{localIndex % SgOmpNowaitClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpNowaitClause::pool_size};
        returnPointer = &(((SgOmpNowaitClause*)(SgOmpNowaitClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpNowaitClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpNowaitClause::pools.begin(); block != SgOmpNowaitClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpNowaitClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNowaitClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpNowaitClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpNowaitClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpNowaitClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpNowaitClause::pools.begin(); block != SgOmpNowaitClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpNowaitClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNowaitClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpNowaitClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpNowaitClause::clearMemoryPool() {
    SgOmpNowaitClause* tempPointer{nullptr};
    if (SgOmpNowaitClause::pools.empty() == false) {
        auto block{SgOmpNowaitClause::pools.begin()};
        SgOmpNowaitClause::next_node = reinterpret_cast<SgOmpNowaitClause*>(*block);
        for (; block != SgOmpNowaitClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpNowaitClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpNowaitClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpNowaitClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpNowaitClause::pool_size-1]);
        }
    }
}

void
SgOmpNowaitClause::deleteMemoryPool() {
    for (auto p: SgOmpNowaitClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpNowaitClause::next_node = nullptr;
    SgOmpNowaitClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpNowaitClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNowaitClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNowaitClause)};
    for (size_t blockIndex = SgOmpNowaitClause::pools.size(); (blockIndex * SgOmpNowaitClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNowaitClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNowaitClause) = %" PRIuPTR " SgOmpNowaitClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNowaitClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNowaitClause),SgOmpNowaitClause::pool_size);
        }
#endif

        SgOmpNowaitClause* pointer{static_cast<SgOmpNowaitClause*>(ROSE_MALLOC(SgOmpNowaitClause::pool_size * sizeof(SgOmpNowaitClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpNowaitClause::pool_size * sizeof(SgOmpNowaitClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpNowaitClause::pool_size * sizeof(SgOmpNowaitClause));
#endif
        SgOmpNowaitClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpNowaitClause::pool_size * sizeof(SgOmpNowaitClause), V_SgOmpNowaitClause));

        if (SgOmpNowaitClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpNowaitClause* blkptr{reinterpret_cast<SgOmpNowaitClause*>(SgOmpNowaitClause::pools[blockIndex-1])};
                blkptr[SgOmpNowaitClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpNowaitClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpNowaitClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpNowaitClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpNowaitClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpNowaitClause::pool_size - 1};
    SgOmpNowaitClause* testPointer{reinterpret_cast<SgOmpNowaitClause*>(SgOmpNowaitClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpNowaitClause::pool_size * (SgOmpNowaitClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpNowaitClauseStroageClass
 * from its counterpart of SgOmpNowaitClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpNowaitClause::initializeStorageClassArray(SgOmpNowaitClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpNowaitClause::pools.begin(); block != SgOmpNowaitClause::pools.end(); ++block) {
        SgOmpNowaitClause* pointer{reinterpret_cast<SgOmpNowaitClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNowaitClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 40018 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpBeginClause that takes its corresponding StorageClass as parameter
 */
SgOmpBeginClause::SgOmpBeginClause(const SgOmpBeginClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 40028 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpBeginClause*
SgOmpBeginClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpBeginClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBeginClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBeginClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBeginClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBeginClause)};
        unsigned long positionInPool{localIndex % SgOmpBeginClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpBeginClause::pool_size};
        returnPointer = &(((SgOmpBeginClause*)(SgOmpBeginClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpBeginClause*
SgOmpBeginClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpBeginClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBeginClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBeginClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBeginClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpBeginClause)};
        unsigned long positionInPool{localIndex % SgOmpBeginClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpBeginClause::pool_size};
        returnPointer = &(((SgOmpBeginClause*)(SgOmpBeginClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpBeginClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpBeginClause::pools.begin(); block != SgOmpBeginClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpBeginClause*>(*block)};
        for (unsigned i = 0; i < SgOmpBeginClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpBeginClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpBeginClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpBeginClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpBeginClause::pools.begin(); block != SgOmpBeginClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpBeginClause*>(*block)};
        for (unsigned i = 0; i < SgOmpBeginClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpBeginClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpBeginClause::clearMemoryPool() {
    SgOmpBeginClause* tempPointer{nullptr};
    if (SgOmpBeginClause::pools.empty() == false) {
        auto block{SgOmpBeginClause::pools.begin()};
        SgOmpBeginClause::next_node = reinterpret_cast<SgOmpBeginClause*>(*block);
        for (; block != SgOmpBeginClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpBeginClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpBeginClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpBeginClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpBeginClause::pool_size-1]);
        }
    }
}

void
SgOmpBeginClause::deleteMemoryPool() {
    for (auto p: SgOmpBeginClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpBeginClause::next_node = nullptr;
    SgOmpBeginClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpBeginClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBeginClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBeginClause)};
    for (size_t blockIndex = SgOmpBeginClause::pools.size(); (blockIndex * SgOmpBeginClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBeginClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBeginClause) = %" PRIuPTR " SgOmpBeginClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBeginClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBeginClause),SgOmpBeginClause::pool_size);
        }
#endif

        SgOmpBeginClause* pointer{static_cast<SgOmpBeginClause*>(ROSE_MALLOC(SgOmpBeginClause::pool_size * sizeof(SgOmpBeginClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpBeginClause::pool_size * sizeof(SgOmpBeginClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpBeginClause::pool_size * sizeof(SgOmpBeginClause));
#endif
        SgOmpBeginClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpBeginClause::pool_size * sizeof(SgOmpBeginClause), V_SgOmpBeginClause));

        if (SgOmpBeginClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpBeginClause* blkptr{reinterpret_cast<SgOmpBeginClause*>(SgOmpBeginClause::pools[blockIndex-1])};
                blkptr[SgOmpBeginClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpBeginClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpBeginClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpBeginClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpBeginClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpBeginClause::pool_size - 1};
    SgOmpBeginClause* testPointer{reinterpret_cast<SgOmpBeginClause*>(SgOmpBeginClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpBeginClause::pool_size * (SgOmpBeginClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpBeginClauseStroageClass
 * from its counterpart of SgOmpBeginClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpBeginClause::initializeStorageClassArray(SgOmpBeginClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpBeginClause::pools.begin(); block != SgOmpBeginClause::pools.end(); ++block) {
        SgOmpBeginClause* pointer{reinterpret_cast<SgOmpBeginClause*>(*block)};
        for (unsigned i = 0; i < SgOmpBeginClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 40245 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpEndClause that takes its corresponding StorageClass as parameter
 */
SgOmpEndClause::SgOmpEndClause(const SgOmpEndClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 40255 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpEndClause*
SgOmpEndClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpEndClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpEndClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpEndClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpEndClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpEndClause)};
        unsigned long positionInPool{localIndex % SgOmpEndClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpEndClause::pool_size};
        returnPointer = &(((SgOmpEndClause*)(SgOmpEndClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpEndClause*
SgOmpEndClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpEndClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpEndClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpEndClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpEndClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpEndClause)};
        unsigned long positionInPool{localIndex % SgOmpEndClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpEndClause::pool_size};
        returnPointer = &(((SgOmpEndClause*)(SgOmpEndClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpEndClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpEndClause::pools.begin(); block != SgOmpEndClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpEndClause*>(*block)};
        for (unsigned i = 0; i < SgOmpEndClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpEndClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpEndClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpEndClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpEndClause::pools.begin(); block != SgOmpEndClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpEndClause*>(*block)};
        for (unsigned i = 0; i < SgOmpEndClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpEndClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpEndClause::clearMemoryPool() {
    SgOmpEndClause* tempPointer{nullptr};
    if (SgOmpEndClause::pools.empty() == false) {
        auto block{SgOmpEndClause::pools.begin()};
        SgOmpEndClause::next_node = reinterpret_cast<SgOmpEndClause*>(*block);
        for (; block != SgOmpEndClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpEndClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpEndClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpEndClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpEndClause::pool_size-1]);
        }
    }
}

void
SgOmpEndClause::deleteMemoryPool() {
    for (auto p: SgOmpEndClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpEndClause::next_node = nullptr;
    SgOmpEndClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpEndClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpEndClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpEndClause)};
    for (size_t blockIndex = SgOmpEndClause::pools.size(); (blockIndex * SgOmpEndClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpEndClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpEndClause) = %" PRIuPTR " SgOmpEndClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpEndClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpEndClause),SgOmpEndClause::pool_size);
        }
#endif

        SgOmpEndClause* pointer{static_cast<SgOmpEndClause*>(ROSE_MALLOC(SgOmpEndClause::pool_size * sizeof(SgOmpEndClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpEndClause::pool_size * sizeof(SgOmpEndClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpEndClause::pool_size * sizeof(SgOmpEndClause));
#endif
        SgOmpEndClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpEndClause::pool_size * sizeof(SgOmpEndClause), V_SgOmpEndClause));

        if (SgOmpEndClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpEndClause* blkptr{reinterpret_cast<SgOmpEndClause*>(SgOmpEndClause::pools[blockIndex-1])};
                blkptr[SgOmpEndClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpEndClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpEndClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpEndClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpEndClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpEndClause::pool_size - 1};
    SgOmpEndClause* testPointer{reinterpret_cast<SgOmpEndClause*>(SgOmpEndClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpEndClause::pool_size * (SgOmpEndClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpEndClauseStroageClass
 * from its counterpart of SgOmpEndClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpEndClause::initializeStorageClassArray(SgOmpEndClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpEndClause::pools.begin(); block != SgOmpEndClause::pools.end(); ++block) {
        SgOmpEndClause* pointer{reinterpret_cast<SgOmpEndClause*>(*block)};
        for (unsigned i = 0; i < SgOmpEndClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 40472 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpUntiedClause that takes its corresponding StorageClass as parameter
 */
SgOmpUntiedClause::SgOmpUntiedClause(const SgOmpUntiedClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 40482 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpUntiedClause*
SgOmpUntiedClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpUntiedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpUntiedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpUntiedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpUntiedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUntiedClause)};
        unsigned long positionInPool{localIndex % SgOmpUntiedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpUntiedClause::pool_size};
        returnPointer = &(((SgOmpUntiedClause*)(SgOmpUntiedClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpUntiedClause*
SgOmpUntiedClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpUntiedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpUntiedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpUntiedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpUntiedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpUntiedClause)};
        unsigned long positionInPool{localIndex % SgOmpUntiedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpUntiedClause::pool_size};
        returnPointer = &(((SgOmpUntiedClause*)(SgOmpUntiedClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpUntiedClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpUntiedClause::pools.begin(); block != SgOmpUntiedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpUntiedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpUntiedClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpUntiedClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpUntiedClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpUntiedClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpUntiedClause::pools.begin(); block != SgOmpUntiedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpUntiedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpUntiedClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpUntiedClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpUntiedClause::clearMemoryPool() {
    SgOmpUntiedClause* tempPointer{nullptr};
    if (SgOmpUntiedClause::pools.empty() == false) {
        auto block{SgOmpUntiedClause::pools.begin()};
        SgOmpUntiedClause::next_node = reinterpret_cast<SgOmpUntiedClause*>(*block);
        for (; block != SgOmpUntiedClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpUntiedClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpUntiedClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpUntiedClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpUntiedClause::pool_size-1]);
        }
    }
}

void
SgOmpUntiedClause::deleteMemoryPool() {
    for (auto p: SgOmpUntiedClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpUntiedClause::next_node = nullptr;
    SgOmpUntiedClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpUntiedClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUntiedClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpUntiedClause)};
    for (size_t blockIndex = SgOmpUntiedClause::pools.size(); (blockIndex * SgOmpUntiedClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUntiedClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpUntiedClause) = %" PRIuPTR " SgOmpUntiedClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUntiedClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpUntiedClause),SgOmpUntiedClause::pool_size);
        }
#endif

        SgOmpUntiedClause* pointer{static_cast<SgOmpUntiedClause*>(ROSE_MALLOC(SgOmpUntiedClause::pool_size * sizeof(SgOmpUntiedClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpUntiedClause::pool_size * sizeof(SgOmpUntiedClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpUntiedClause::pool_size * sizeof(SgOmpUntiedClause));
#endif
        SgOmpUntiedClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpUntiedClause::pool_size * sizeof(SgOmpUntiedClause), V_SgOmpUntiedClause));

        if (SgOmpUntiedClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpUntiedClause* blkptr{reinterpret_cast<SgOmpUntiedClause*>(SgOmpUntiedClause::pools[blockIndex-1])};
                blkptr[SgOmpUntiedClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpUntiedClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpUntiedClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpUntiedClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpUntiedClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpUntiedClause::pool_size - 1};
    SgOmpUntiedClause* testPointer{reinterpret_cast<SgOmpUntiedClause*>(SgOmpUntiedClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpUntiedClause::pool_size * (SgOmpUntiedClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpUntiedClauseStroageClass
 * from its counterpart of SgOmpUntiedClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpUntiedClause::initializeStorageClassArray(SgOmpUntiedClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpUntiedClause::pools.begin(); block != SgOmpUntiedClause::pools.end(); ++block) {
        SgOmpUntiedClause* pointer{reinterpret_cast<SgOmpUntiedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpUntiedClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 40699 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpDefaultClause that takes its corresponding StorageClass as parameter
 */
SgOmpDefaultClause::SgOmpDefaultClause(const SgOmpDefaultClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 40709 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_data_sharing = storageSource.storageOf_data_sharing;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpDefaultClause*
SgOmpDefaultClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpDefaultClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDefaultClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDefaultClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDefaultClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDefaultClause)};
        unsigned long positionInPool{localIndex % SgOmpDefaultClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDefaultClause::pool_size};
        returnPointer = &(((SgOmpDefaultClause*)(SgOmpDefaultClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpDefaultClause*
SgOmpDefaultClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpDefaultClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDefaultClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDefaultClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDefaultClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpDefaultClause)};
        unsigned long positionInPool{localIndex % SgOmpDefaultClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDefaultClause::pool_size};
        returnPointer = &(((SgOmpDefaultClause*)(SgOmpDefaultClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpDefaultClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpDefaultClause::pools.begin(); block != SgOmpDefaultClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDefaultClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDefaultClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpDefaultClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpDefaultClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpDefaultClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpDefaultClause::pools.begin(); block != SgOmpDefaultClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDefaultClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDefaultClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpDefaultClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpDefaultClause::clearMemoryPool() {
    SgOmpDefaultClause* tempPointer{nullptr};
    if (SgOmpDefaultClause::pools.empty() == false) {
        auto block{SgOmpDefaultClause::pools.begin()};
        SgOmpDefaultClause::next_node = reinterpret_cast<SgOmpDefaultClause*>(*block);
        for (; block != SgOmpDefaultClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpDefaultClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpDefaultClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpDefaultClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpDefaultClause::pool_size-1]);
        }
    }
}

void
SgOmpDefaultClause::deleteMemoryPool() {
    for (auto p: SgOmpDefaultClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpDefaultClause::next_node = nullptr;
    SgOmpDefaultClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpDefaultClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDefaultClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDefaultClause)};
    for (size_t blockIndex = SgOmpDefaultClause::pools.size(); (blockIndex * SgOmpDefaultClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDefaultClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDefaultClause) = %" PRIuPTR " SgOmpDefaultClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDefaultClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDefaultClause),SgOmpDefaultClause::pool_size);
        }
#endif

        SgOmpDefaultClause* pointer{static_cast<SgOmpDefaultClause*>(ROSE_MALLOC(SgOmpDefaultClause::pool_size * sizeof(SgOmpDefaultClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpDefaultClause::pool_size * sizeof(SgOmpDefaultClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpDefaultClause::pool_size * sizeof(SgOmpDefaultClause));
#endif
        SgOmpDefaultClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpDefaultClause::pool_size * sizeof(SgOmpDefaultClause), V_SgOmpDefaultClause));

        if (SgOmpDefaultClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpDefaultClause* blkptr{reinterpret_cast<SgOmpDefaultClause*>(SgOmpDefaultClause::pools[blockIndex-1])};
                blkptr[SgOmpDefaultClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpDefaultClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpDefaultClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpDefaultClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpDefaultClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpDefaultClause::pool_size - 1};
    SgOmpDefaultClause* testPointer{reinterpret_cast<SgOmpDefaultClause*>(SgOmpDefaultClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpDefaultClause::pool_size * (SgOmpDefaultClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpDefaultClauseStroageClass
 * from its counterpart of SgOmpDefaultClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpDefaultClause::initializeStorageClassArray(SgOmpDefaultClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpDefaultClause::pools.begin(); block != SgOmpDefaultClause::pools.end(); ++block) {
        SgOmpDefaultClause* pointer{reinterpret_cast<SgOmpDefaultClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDefaultClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 40927 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpAtomicClause that takes its corresponding StorageClass as parameter
 */
SgOmpAtomicClause::SgOmpAtomicClause(const SgOmpAtomicClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 40937 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_atomicity = storageSource.storageOf_atomicity;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpAtomicClause*
SgOmpAtomicClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpAtomicClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAtomicClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAtomicClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAtomicClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicClause)};
        unsigned long positionInPool{localIndex % SgOmpAtomicClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpAtomicClause::pool_size};
        returnPointer = &(((SgOmpAtomicClause*)(SgOmpAtomicClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpAtomicClause*
SgOmpAtomicClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpAtomicClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAtomicClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAtomicClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAtomicClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpAtomicClause)};
        unsigned long positionInPool{localIndex % SgOmpAtomicClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpAtomicClause::pool_size};
        returnPointer = &(((SgOmpAtomicClause*)(SgOmpAtomicClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpAtomicClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpAtomicClause::pools.begin(); block != SgOmpAtomicClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpAtomicClause*>(*block)};
        for (unsigned i = 0; i < SgOmpAtomicClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpAtomicClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpAtomicClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpAtomicClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpAtomicClause::pools.begin(); block != SgOmpAtomicClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpAtomicClause*>(*block)};
        for (unsigned i = 0; i < SgOmpAtomicClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpAtomicClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpAtomicClause::clearMemoryPool() {
    SgOmpAtomicClause* tempPointer{nullptr};
    if (SgOmpAtomicClause::pools.empty() == false) {
        auto block{SgOmpAtomicClause::pools.begin()};
        SgOmpAtomicClause::next_node = reinterpret_cast<SgOmpAtomicClause*>(*block);
        for (; block != SgOmpAtomicClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpAtomicClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpAtomicClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpAtomicClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpAtomicClause::pool_size-1]);
        }
    }
}

void
SgOmpAtomicClause::deleteMemoryPool() {
    for (auto p: SgOmpAtomicClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpAtomicClause::next_node = nullptr;
    SgOmpAtomicClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpAtomicClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAtomicClause)};
    for (size_t blockIndex = SgOmpAtomicClause::pools.size(); (blockIndex * SgOmpAtomicClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAtomicClause) = %" PRIuPTR " SgOmpAtomicClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAtomicClause),SgOmpAtomicClause::pool_size);
        }
#endif

        SgOmpAtomicClause* pointer{static_cast<SgOmpAtomicClause*>(ROSE_MALLOC(SgOmpAtomicClause::pool_size * sizeof(SgOmpAtomicClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpAtomicClause::pool_size * sizeof(SgOmpAtomicClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpAtomicClause::pool_size * sizeof(SgOmpAtomicClause));
#endif
        SgOmpAtomicClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpAtomicClause::pool_size * sizeof(SgOmpAtomicClause), V_SgOmpAtomicClause));

        if (SgOmpAtomicClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpAtomicClause* blkptr{reinterpret_cast<SgOmpAtomicClause*>(SgOmpAtomicClause::pools[blockIndex-1])};
                blkptr[SgOmpAtomicClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpAtomicClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpAtomicClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpAtomicClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpAtomicClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpAtomicClause::pool_size - 1};
    SgOmpAtomicClause* testPointer{reinterpret_cast<SgOmpAtomicClause*>(SgOmpAtomicClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpAtomicClause::pool_size * (SgOmpAtomicClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpAtomicClauseStroageClass
 * from its counterpart of SgOmpAtomicClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpAtomicClause::initializeStorageClassArray(SgOmpAtomicClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpAtomicClause::pools.begin(); block != SgOmpAtomicClause::pools.end(); ++block) {
        SgOmpAtomicClause* pointer{reinterpret_cast<SgOmpAtomicClause*>(*block)};
        for (unsigned i = 0; i < SgOmpAtomicClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 41155 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpProcBindClause that takes its corresponding StorageClass as parameter
 */
SgOmpProcBindClause::SgOmpProcBindClause(const SgOmpProcBindClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 41165 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_policy = storageSource.storageOf_policy;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpProcBindClause*
SgOmpProcBindClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpProcBindClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpProcBindClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpProcBindClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpProcBindClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpProcBindClause)};
        unsigned long positionInPool{localIndex % SgOmpProcBindClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpProcBindClause::pool_size};
        returnPointer = &(((SgOmpProcBindClause*)(SgOmpProcBindClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpProcBindClause*
SgOmpProcBindClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpProcBindClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpProcBindClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpProcBindClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpProcBindClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpProcBindClause)};
        unsigned long positionInPool{localIndex % SgOmpProcBindClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpProcBindClause::pool_size};
        returnPointer = &(((SgOmpProcBindClause*)(SgOmpProcBindClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpProcBindClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpProcBindClause::pools.begin(); block != SgOmpProcBindClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpProcBindClause*>(*block)};
        for (unsigned i = 0; i < SgOmpProcBindClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpProcBindClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpProcBindClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpProcBindClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpProcBindClause::pools.begin(); block != SgOmpProcBindClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpProcBindClause*>(*block)};
        for (unsigned i = 0; i < SgOmpProcBindClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpProcBindClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpProcBindClause::clearMemoryPool() {
    SgOmpProcBindClause* tempPointer{nullptr};
    if (SgOmpProcBindClause::pools.empty() == false) {
        auto block{SgOmpProcBindClause::pools.begin()};
        SgOmpProcBindClause::next_node = reinterpret_cast<SgOmpProcBindClause*>(*block);
        for (; block != SgOmpProcBindClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpProcBindClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpProcBindClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpProcBindClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpProcBindClause::pool_size-1]);
        }
    }
}

void
SgOmpProcBindClause::deleteMemoryPool() {
    for (auto p: SgOmpProcBindClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpProcBindClause::next_node = nullptr;
    SgOmpProcBindClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpProcBindClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpProcBindClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpProcBindClause)};
    for (size_t blockIndex = SgOmpProcBindClause::pools.size(); (blockIndex * SgOmpProcBindClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpProcBindClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpProcBindClause) = %" PRIuPTR " SgOmpProcBindClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpProcBindClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpProcBindClause),SgOmpProcBindClause::pool_size);
        }
#endif

        SgOmpProcBindClause* pointer{static_cast<SgOmpProcBindClause*>(ROSE_MALLOC(SgOmpProcBindClause::pool_size * sizeof(SgOmpProcBindClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpProcBindClause::pool_size * sizeof(SgOmpProcBindClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpProcBindClause::pool_size * sizeof(SgOmpProcBindClause));
#endif
        SgOmpProcBindClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpProcBindClause::pool_size * sizeof(SgOmpProcBindClause), V_SgOmpProcBindClause));

        if (SgOmpProcBindClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpProcBindClause* blkptr{reinterpret_cast<SgOmpProcBindClause*>(SgOmpProcBindClause::pools[blockIndex-1])};
                blkptr[SgOmpProcBindClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpProcBindClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpProcBindClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpProcBindClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpProcBindClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpProcBindClause::pool_size - 1};
    SgOmpProcBindClause* testPointer{reinterpret_cast<SgOmpProcBindClause*>(SgOmpProcBindClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpProcBindClause::pool_size * (SgOmpProcBindClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpProcBindClauseStroageClass
 * from its counterpart of SgOmpProcBindClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpProcBindClause::initializeStorageClassArray(SgOmpProcBindClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpProcBindClause::pools.begin(); block != SgOmpProcBindClause::pools.end(); ++block) {
        SgOmpProcBindClause* pointer{reinterpret_cast<SgOmpProcBindClause*>(*block)};
        for (unsigned i = 0; i < SgOmpProcBindClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 41383 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpExpressionClause that takes its corresponding StorageClass as parameter
 */
SgOmpExpressionClause::SgOmpExpressionClause(const SgOmpExpressionClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 41393 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpExpressionClause*
SgOmpExpressionClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpExpressionClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpExpressionClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpExpressionClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpExpressionClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpExpressionClause)};
        unsigned long positionInPool{localIndex % SgOmpExpressionClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpExpressionClause::pool_size};
        returnPointer = &(((SgOmpExpressionClause*)(SgOmpExpressionClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpExpressionClause*
SgOmpExpressionClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpExpressionClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpExpressionClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpExpressionClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpExpressionClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpExpressionClause)};
        unsigned long positionInPool{localIndex % SgOmpExpressionClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpExpressionClause::pool_size};
        returnPointer = &(((SgOmpExpressionClause*)(SgOmpExpressionClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpExpressionClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpExpressionClause::pools.begin(); block != SgOmpExpressionClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpExpressionClause*>(*block)};
        for (unsigned i = 0; i < SgOmpExpressionClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpExpressionClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpExpressionClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpExpressionClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpExpressionClause::pools.begin(); block != SgOmpExpressionClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpExpressionClause*>(*block)};
        for (unsigned i = 0; i < SgOmpExpressionClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpExpressionClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpExpressionClause::clearMemoryPool() {
    SgOmpExpressionClause* tempPointer{nullptr};
    if (SgOmpExpressionClause::pools.empty() == false) {
        auto block{SgOmpExpressionClause::pools.begin()};
        SgOmpExpressionClause::next_node = reinterpret_cast<SgOmpExpressionClause*>(*block);
        for (; block != SgOmpExpressionClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpExpressionClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpExpressionClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpExpressionClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpExpressionClause::pool_size-1]);
        }
    }
}

void
SgOmpExpressionClause::deleteMemoryPool() {
    for (auto p: SgOmpExpressionClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpExpressionClause::next_node = nullptr;
    SgOmpExpressionClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpExpressionClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpExpressionClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpExpressionClause)};
    for (size_t blockIndex = SgOmpExpressionClause::pools.size(); (blockIndex * SgOmpExpressionClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpExpressionClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpExpressionClause) = %" PRIuPTR " SgOmpExpressionClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpExpressionClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpExpressionClause),SgOmpExpressionClause::pool_size);
        }
#endif

        SgOmpExpressionClause* pointer{static_cast<SgOmpExpressionClause*>(ROSE_MALLOC(SgOmpExpressionClause::pool_size * sizeof(SgOmpExpressionClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpExpressionClause::pool_size * sizeof(SgOmpExpressionClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpExpressionClause::pool_size * sizeof(SgOmpExpressionClause));
#endif
        SgOmpExpressionClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpExpressionClause::pool_size * sizeof(SgOmpExpressionClause), V_SgOmpExpressionClause));

        if (SgOmpExpressionClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpExpressionClause* blkptr{reinterpret_cast<SgOmpExpressionClause*>(SgOmpExpressionClause::pools[blockIndex-1])};
                blkptr[SgOmpExpressionClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpExpressionClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpExpressionClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpExpressionClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpExpressionClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpExpressionClause::pool_size - 1};
    SgOmpExpressionClause* testPointer{reinterpret_cast<SgOmpExpressionClause*>(SgOmpExpressionClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpExpressionClause::pool_size * (SgOmpExpressionClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpExpressionClauseStroageClass
 * from its counterpart of SgOmpExpressionClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpExpressionClause::initializeStorageClassArray(SgOmpExpressionClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpExpressionClause::pools.begin(); block != SgOmpExpressionClause::pools.end(); ++block) {
        SgOmpExpressionClause* pointer{reinterpret_cast<SgOmpExpressionClause*>(*block)};
        for (unsigned i = 0; i < SgOmpExpressionClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 41611 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpOrderedClause that takes its corresponding StorageClass as parameter
 */
SgOmpOrderedClause::SgOmpOrderedClause(const SgOmpOrderedClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 41621 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpOrderedClause*
SgOmpOrderedClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpOrderedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpOrderedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpOrderedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpOrderedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedClause)};
        unsigned long positionInPool{localIndex % SgOmpOrderedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpOrderedClause::pool_size};
        returnPointer = &(((SgOmpOrderedClause*)(SgOmpOrderedClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpOrderedClause*
SgOmpOrderedClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpOrderedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpOrderedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpOrderedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpOrderedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpOrderedClause)};
        unsigned long positionInPool{localIndex % SgOmpOrderedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpOrderedClause::pool_size};
        returnPointer = &(((SgOmpOrderedClause*)(SgOmpOrderedClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpOrderedClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpOrderedClause::pools.begin(); block != SgOmpOrderedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpOrderedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpOrderedClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpOrderedClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpOrderedClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpOrderedClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpOrderedClause::pools.begin(); block != SgOmpOrderedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpOrderedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpOrderedClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpOrderedClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpOrderedClause::clearMemoryPool() {
    SgOmpOrderedClause* tempPointer{nullptr};
    if (SgOmpOrderedClause::pools.empty() == false) {
        auto block{SgOmpOrderedClause::pools.begin()};
        SgOmpOrderedClause::next_node = reinterpret_cast<SgOmpOrderedClause*>(*block);
        for (; block != SgOmpOrderedClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpOrderedClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpOrderedClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpOrderedClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpOrderedClause::pool_size-1]);
        }
    }
}

void
SgOmpOrderedClause::deleteMemoryPool() {
    for (auto p: SgOmpOrderedClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpOrderedClause::next_node = nullptr;
    SgOmpOrderedClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpOrderedClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpOrderedClause)};
    for (size_t blockIndex = SgOmpOrderedClause::pools.size(); (blockIndex * SgOmpOrderedClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpOrderedClause) = %" PRIuPTR " SgOmpOrderedClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpOrderedClause),SgOmpOrderedClause::pool_size);
        }
#endif

        SgOmpOrderedClause* pointer{static_cast<SgOmpOrderedClause*>(ROSE_MALLOC(SgOmpOrderedClause::pool_size * sizeof(SgOmpOrderedClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpOrderedClause::pool_size * sizeof(SgOmpOrderedClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpOrderedClause::pool_size * sizeof(SgOmpOrderedClause));
#endif
        SgOmpOrderedClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpOrderedClause::pool_size * sizeof(SgOmpOrderedClause), V_SgOmpOrderedClause));

        if (SgOmpOrderedClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpOrderedClause* blkptr{reinterpret_cast<SgOmpOrderedClause*>(SgOmpOrderedClause::pools[blockIndex-1])};
                blkptr[SgOmpOrderedClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpOrderedClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpOrderedClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpOrderedClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpOrderedClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpOrderedClause::pool_size - 1};
    SgOmpOrderedClause* testPointer{reinterpret_cast<SgOmpOrderedClause*>(SgOmpOrderedClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpOrderedClause::pool_size * (SgOmpOrderedClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpOrderedClauseStroageClass
 * from its counterpart of SgOmpOrderedClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpOrderedClause::initializeStorageClassArray(SgOmpOrderedClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpOrderedClause::pools.begin(); block != SgOmpOrderedClause::pools.end(); ++block) {
        SgOmpOrderedClause* pointer{reinterpret_cast<SgOmpOrderedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpOrderedClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 41838 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpCollapseClause that takes its corresponding StorageClass as parameter
 */
SgOmpCollapseClause::SgOmpCollapseClause(const SgOmpCollapseClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 41848 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpCollapseClause*
SgOmpCollapseClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpCollapseClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCollapseClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCollapseClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCollapseClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCollapseClause)};
        unsigned long positionInPool{localIndex % SgOmpCollapseClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCollapseClause::pool_size};
        returnPointer = &(((SgOmpCollapseClause*)(SgOmpCollapseClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpCollapseClause*
SgOmpCollapseClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpCollapseClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCollapseClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCollapseClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCollapseClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpCollapseClause)};
        unsigned long positionInPool{localIndex % SgOmpCollapseClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCollapseClause::pool_size};
        returnPointer = &(((SgOmpCollapseClause*)(SgOmpCollapseClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpCollapseClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpCollapseClause::pools.begin(); block != SgOmpCollapseClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCollapseClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCollapseClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpCollapseClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpCollapseClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpCollapseClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpCollapseClause::pools.begin(); block != SgOmpCollapseClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCollapseClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCollapseClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpCollapseClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpCollapseClause::clearMemoryPool() {
    SgOmpCollapseClause* tempPointer{nullptr};
    if (SgOmpCollapseClause::pools.empty() == false) {
        auto block{SgOmpCollapseClause::pools.begin()};
        SgOmpCollapseClause::next_node = reinterpret_cast<SgOmpCollapseClause*>(*block);
        for (; block != SgOmpCollapseClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpCollapseClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpCollapseClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpCollapseClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpCollapseClause::pool_size-1]);
        }
    }
}

void
SgOmpCollapseClause::deleteMemoryPool() {
    for (auto p: SgOmpCollapseClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpCollapseClause::next_node = nullptr;
    SgOmpCollapseClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpCollapseClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCollapseClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCollapseClause)};
    for (size_t blockIndex = SgOmpCollapseClause::pools.size(); (blockIndex * SgOmpCollapseClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCollapseClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCollapseClause) = %" PRIuPTR " SgOmpCollapseClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCollapseClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCollapseClause),SgOmpCollapseClause::pool_size);
        }
#endif

        SgOmpCollapseClause* pointer{static_cast<SgOmpCollapseClause*>(ROSE_MALLOC(SgOmpCollapseClause::pool_size * sizeof(SgOmpCollapseClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpCollapseClause::pool_size * sizeof(SgOmpCollapseClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpCollapseClause::pool_size * sizeof(SgOmpCollapseClause));
#endif
        SgOmpCollapseClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpCollapseClause::pool_size * sizeof(SgOmpCollapseClause), V_SgOmpCollapseClause));

        if (SgOmpCollapseClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpCollapseClause* blkptr{reinterpret_cast<SgOmpCollapseClause*>(SgOmpCollapseClause::pools[blockIndex-1])};
                blkptr[SgOmpCollapseClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpCollapseClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpCollapseClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpCollapseClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpCollapseClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpCollapseClause::pool_size - 1};
    SgOmpCollapseClause* testPointer{reinterpret_cast<SgOmpCollapseClause*>(SgOmpCollapseClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpCollapseClause::pool_size * (SgOmpCollapseClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpCollapseClauseStroageClass
 * from its counterpart of SgOmpCollapseClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpCollapseClause::initializeStorageClassArray(SgOmpCollapseClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpCollapseClause::pools.begin(); block != SgOmpCollapseClause::pools.end(); ++block) {
        SgOmpCollapseClause* pointer{reinterpret_cast<SgOmpCollapseClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCollapseClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 42065 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpIfClause that takes its corresponding StorageClass as parameter
 */
SgOmpIfClause::SgOmpIfClause(const SgOmpIfClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 42075 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpIfClause*
SgOmpIfClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpIfClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpIfClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpIfClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpIfClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpIfClause)};
        unsigned long positionInPool{localIndex % SgOmpIfClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpIfClause::pool_size};
        returnPointer = &(((SgOmpIfClause*)(SgOmpIfClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpIfClause*
SgOmpIfClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpIfClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpIfClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpIfClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpIfClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpIfClause)};
        unsigned long positionInPool{localIndex % SgOmpIfClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpIfClause::pool_size};
        returnPointer = &(((SgOmpIfClause*)(SgOmpIfClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpIfClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpIfClause::pools.begin(); block != SgOmpIfClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpIfClause*>(*block)};
        for (unsigned i = 0; i < SgOmpIfClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpIfClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpIfClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpIfClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpIfClause::pools.begin(); block != SgOmpIfClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpIfClause*>(*block)};
        for (unsigned i = 0; i < SgOmpIfClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpIfClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpIfClause::clearMemoryPool() {
    SgOmpIfClause* tempPointer{nullptr};
    if (SgOmpIfClause::pools.empty() == false) {
        auto block{SgOmpIfClause::pools.begin()};
        SgOmpIfClause::next_node = reinterpret_cast<SgOmpIfClause*>(*block);
        for (; block != SgOmpIfClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpIfClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpIfClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpIfClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpIfClause::pool_size-1]);
        }
    }
}

void
SgOmpIfClause::deleteMemoryPool() {
    for (auto p: SgOmpIfClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpIfClause::next_node = nullptr;
    SgOmpIfClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpIfClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpIfClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpIfClause)};
    for (size_t blockIndex = SgOmpIfClause::pools.size(); (blockIndex * SgOmpIfClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpIfClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpIfClause) = %" PRIuPTR " SgOmpIfClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpIfClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpIfClause),SgOmpIfClause::pool_size);
        }
#endif

        SgOmpIfClause* pointer{static_cast<SgOmpIfClause*>(ROSE_MALLOC(SgOmpIfClause::pool_size * sizeof(SgOmpIfClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpIfClause::pool_size * sizeof(SgOmpIfClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpIfClause::pool_size * sizeof(SgOmpIfClause));
#endif
        SgOmpIfClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpIfClause::pool_size * sizeof(SgOmpIfClause), V_SgOmpIfClause));

        if (SgOmpIfClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpIfClause* blkptr{reinterpret_cast<SgOmpIfClause*>(SgOmpIfClause::pools[blockIndex-1])};
                blkptr[SgOmpIfClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpIfClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpIfClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpIfClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpIfClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpIfClause::pool_size - 1};
    SgOmpIfClause* testPointer{reinterpret_cast<SgOmpIfClause*>(SgOmpIfClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpIfClause::pool_size * (SgOmpIfClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpIfClauseStroageClass
 * from its counterpart of SgOmpIfClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpIfClause::initializeStorageClassArray(SgOmpIfClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpIfClause::pools.begin(); block != SgOmpIfClause::pools.end(); ++block) {
        SgOmpIfClause* pointer{reinterpret_cast<SgOmpIfClause*>(*block)};
        for (unsigned i = 0; i < SgOmpIfClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 42292 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpNumThreadsClause that takes its corresponding StorageClass as parameter
 */
SgOmpNumThreadsClause::SgOmpNumThreadsClause(const SgOmpNumThreadsClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 42302 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpNumThreadsClause*
SgOmpNumThreadsClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpNumThreadsClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNumThreadsClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNumThreadsClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNumThreadsClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNumThreadsClause)};
        unsigned long positionInPool{localIndex % SgOmpNumThreadsClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpNumThreadsClause::pool_size};
        returnPointer = &(((SgOmpNumThreadsClause*)(SgOmpNumThreadsClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpNumThreadsClause*
SgOmpNumThreadsClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpNumThreadsClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNumThreadsClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNumThreadsClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNumThreadsClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpNumThreadsClause)};
        unsigned long positionInPool{localIndex % SgOmpNumThreadsClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpNumThreadsClause::pool_size};
        returnPointer = &(((SgOmpNumThreadsClause*)(SgOmpNumThreadsClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpNumThreadsClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpNumThreadsClause::pools.begin(); block != SgOmpNumThreadsClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpNumThreadsClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNumThreadsClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpNumThreadsClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpNumThreadsClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpNumThreadsClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpNumThreadsClause::pools.begin(); block != SgOmpNumThreadsClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpNumThreadsClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNumThreadsClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpNumThreadsClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpNumThreadsClause::clearMemoryPool() {
    SgOmpNumThreadsClause* tempPointer{nullptr};
    if (SgOmpNumThreadsClause::pools.empty() == false) {
        auto block{SgOmpNumThreadsClause::pools.begin()};
        SgOmpNumThreadsClause::next_node = reinterpret_cast<SgOmpNumThreadsClause*>(*block);
        for (; block != SgOmpNumThreadsClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpNumThreadsClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpNumThreadsClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpNumThreadsClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpNumThreadsClause::pool_size-1]);
        }
    }
}

void
SgOmpNumThreadsClause::deleteMemoryPool() {
    for (auto p: SgOmpNumThreadsClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpNumThreadsClause::next_node = nullptr;
    SgOmpNumThreadsClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpNumThreadsClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNumThreadsClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNumThreadsClause)};
    for (size_t blockIndex = SgOmpNumThreadsClause::pools.size(); (blockIndex * SgOmpNumThreadsClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNumThreadsClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNumThreadsClause) = %" PRIuPTR " SgOmpNumThreadsClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNumThreadsClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNumThreadsClause),SgOmpNumThreadsClause::pool_size);
        }
#endif

        SgOmpNumThreadsClause* pointer{static_cast<SgOmpNumThreadsClause*>(ROSE_MALLOC(SgOmpNumThreadsClause::pool_size * sizeof(SgOmpNumThreadsClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpNumThreadsClause::pool_size * sizeof(SgOmpNumThreadsClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpNumThreadsClause::pool_size * sizeof(SgOmpNumThreadsClause));
#endif
        SgOmpNumThreadsClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpNumThreadsClause::pool_size * sizeof(SgOmpNumThreadsClause), V_SgOmpNumThreadsClause));

        if (SgOmpNumThreadsClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpNumThreadsClause* blkptr{reinterpret_cast<SgOmpNumThreadsClause*>(SgOmpNumThreadsClause::pools[blockIndex-1])};
                blkptr[SgOmpNumThreadsClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpNumThreadsClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpNumThreadsClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpNumThreadsClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpNumThreadsClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpNumThreadsClause::pool_size - 1};
    SgOmpNumThreadsClause* testPointer{reinterpret_cast<SgOmpNumThreadsClause*>(SgOmpNumThreadsClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpNumThreadsClause::pool_size * (SgOmpNumThreadsClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpNumThreadsClauseStroageClass
 * from its counterpart of SgOmpNumThreadsClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpNumThreadsClause::initializeStorageClassArray(SgOmpNumThreadsClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpNumThreadsClause::pools.begin(); block != SgOmpNumThreadsClause::pools.end(); ++block) {
        SgOmpNumThreadsClause* pointer{reinterpret_cast<SgOmpNumThreadsClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNumThreadsClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 42519 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpDeviceClause that takes its corresponding StorageClass as parameter
 */
SgOmpDeviceClause::SgOmpDeviceClause(const SgOmpDeviceClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 42529 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpDeviceClause*
SgOmpDeviceClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpDeviceClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDeviceClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDeviceClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDeviceClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeviceClause)};
        unsigned long positionInPool{localIndex % SgOmpDeviceClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDeviceClause::pool_size};
        returnPointer = &(((SgOmpDeviceClause*)(SgOmpDeviceClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpDeviceClause*
SgOmpDeviceClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpDeviceClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDeviceClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDeviceClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDeviceClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpDeviceClause)};
        unsigned long positionInPool{localIndex % SgOmpDeviceClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDeviceClause::pool_size};
        returnPointer = &(((SgOmpDeviceClause*)(SgOmpDeviceClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpDeviceClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpDeviceClause::pools.begin(); block != SgOmpDeviceClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDeviceClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDeviceClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpDeviceClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpDeviceClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpDeviceClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpDeviceClause::pools.begin(); block != SgOmpDeviceClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDeviceClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDeviceClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpDeviceClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpDeviceClause::clearMemoryPool() {
    SgOmpDeviceClause* tempPointer{nullptr};
    if (SgOmpDeviceClause::pools.empty() == false) {
        auto block{SgOmpDeviceClause::pools.begin()};
        SgOmpDeviceClause::next_node = reinterpret_cast<SgOmpDeviceClause*>(*block);
        for (; block != SgOmpDeviceClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpDeviceClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpDeviceClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpDeviceClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpDeviceClause::pool_size-1]);
        }
    }
}

void
SgOmpDeviceClause::deleteMemoryPool() {
    for (auto p: SgOmpDeviceClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpDeviceClause::next_node = nullptr;
    SgOmpDeviceClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpDeviceClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeviceClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDeviceClause)};
    for (size_t blockIndex = SgOmpDeviceClause::pools.size(); (blockIndex * SgOmpDeviceClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeviceClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDeviceClause) = %" PRIuPTR " SgOmpDeviceClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeviceClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDeviceClause),SgOmpDeviceClause::pool_size);
        }
#endif

        SgOmpDeviceClause* pointer{static_cast<SgOmpDeviceClause*>(ROSE_MALLOC(SgOmpDeviceClause::pool_size * sizeof(SgOmpDeviceClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpDeviceClause::pool_size * sizeof(SgOmpDeviceClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpDeviceClause::pool_size * sizeof(SgOmpDeviceClause));
#endif
        SgOmpDeviceClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpDeviceClause::pool_size * sizeof(SgOmpDeviceClause), V_SgOmpDeviceClause));

        if (SgOmpDeviceClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpDeviceClause* blkptr{reinterpret_cast<SgOmpDeviceClause*>(SgOmpDeviceClause::pools[blockIndex-1])};
                blkptr[SgOmpDeviceClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpDeviceClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpDeviceClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpDeviceClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpDeviceClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpDeviceClause::pool_size - 1};
    SgOmpDeviceClause* testPointer{reinterpret_cast<SgOmpDeviceClause*>(SgOmpDeviceClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpDeviceClause::pool_size * (SgOmpDeviceClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpDeviceClauseStroageClass
 * from its counterpart of SgOmpDeviceClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpDeviceClause::initializeStorageClassArray(SgOmpDeviceClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpDeviceClause::pools.begin(); block != SgOmpDeviceClause::pools.end(); ++block) {
        SgOmpDeviceClause* pointer{reinterpret_cast<SgOmpDeviceClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDeviceClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 42746 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpSafelenClause that takes its corresponding StorageClass as parameter
 */
SgOmpSafelenClause::SgOmpSafelenClause(const SgOmpSafelenClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 42756 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpSafelenClause*
SgOmpSafelenClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpSafelenClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSafelenClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSafelenClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSafelenClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSafelenClause)};
        unsigned long positionInPool{localIndex % SgOmpSafelenClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSafelenClause::pool_size};
        returnPointer = &(((SgOmpSafelenClause*)(SgOmpSafelenClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpSafelenClause*
SgOmpSafelenClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpSafelenClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSafelenClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSafelenClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSafelenClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpSafelenClause)};
        unsigned long positionInPool{localIndex % SgOmpSafelenClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSafelenClause::pool_size};
        returnPointer = &(((SgOmpSafelenClause*)(SgOmpSafelenClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpSafelenClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpSafelenClause::pools.begin(); block != SgOmpSafelenClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSafelenClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSafelenClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpSafelenClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpSafelenClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpSafelenClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpSafelenClause::pools.begin(); block != SgOmpSafelenClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSafelenClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSafelenClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpSafelenClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpSafelenClause::clearMemoryPool() {
    SgOmpSafelenClause* tempPointer{nullptr};
    if (SgOmpSafelenClause::pools.empty() == false) {
        auto block{SgOmpSafelenClause::pools.begin()};
        SgOmpSafelenClause::next_node = reinterpret_cast<SgOmpSafelenClause*>(*block);
        for (; block != SgOmpSafelenClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpSafelenClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpSafelenClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpSafelenClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpSafelenClause::pool_size-1]);
        }
    }
}

void
SgOmpSafelenClause::deleteMemoryPool() {
    for (auto p: SgOmpSafelenClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpSafelenClause::next_node = nullptr;
    SgOmpSafelenClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpSafelenClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSafelenClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSafelenClause)};
    for (size_t blockIndex = SgOmpSafelenClause::pools.size(); (blockIndex * SgOmpSafelenClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSafelenClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSafelenClause) = %" PRIuPTR " SgOmpSafelenClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSafelenClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSafelenClause),SgOmpSafelenClause::pool_size);
        }
#endif

        SgOmpSafelenClause* pointer{static_cast<SgOmpSafelenClause*>(ROSE_MALLOC(SgOmpSafelenClause::pool_size * sizeof(SgOmpSafelenClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpSafelenClause::pool_size * sizeof(SgOmpSafelenClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpSafelenClause::pool_size * sizeof(SgOmpSafelenClause));
#endif
        SgOmpSafelenClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpSafelenClause::pool_size * sizeof(SgOmpSafelenClause), V_SgOmpSafelenClause));

        if (SgOmpSafelenClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpSafelenClause* blkptr{reinterpret_cast<SgOmpSafelenClause*>(SgOmpSafelenClause::pools[blockIndex-1])};
                blkptr[SgOmpSafelenClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpSafelenClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpSafelenClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpSafelenClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpSafelenClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpSafelenClause::pool_size - 1};
    SgOmpSafelenClause* testPointer{reinterpret_cast<SgOmpSafelenClause*>(SgOmpSafelenClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpSafelenClause::pool_size * (SgOmpSafelenClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpSafelenClauseStroageClass
 * from its counterpart of SgOmpSafelenClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpSafelenClause::initializeStorageClassArray(SgOmpSafelenClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpSafelenClause::pools.begin(); block != SgOmpSafelenClause::pools.end(); ++block) {
        SgOmpSafelenClause* pointer{reinterpret_cast<SgOmpSafelenClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSafelenClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 42973 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpSimdlenClause that takes its corresponding StorageClass as parameter
 */
SgOmpSimdlenClause::SgOmpSimdlenClause(const SgOmpSimdlenClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 42983 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpSimdlenClause*
SgOmpSimdlenClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpSimdlenClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSimdlenClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSimdlenClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSimdlenClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdlenClause)};
        unsigned long positionInPool{localIndex % SgOmpSimdlenClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSimdlenClause::pool_size};
        returnPointer = &(((SgOmpSimdlenClause*)(SgOmpSimdlenClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpSimdlenClause*
SgOmpSimdlenClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpSimdlenClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSimdlenClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSimdlenClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSimdlenClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpSimdlenClause)};
        unsigned long positionInPool{localIndex % SgOmpSimdlenClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSimdlenClause::pool_size};
        returnPointer = &(((SgOmpSimdlenClause*)(SgOmpSimdlenClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpSimdlenClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpSimdlenClause::pools.begin(); block != SgOmpSimdlenClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSimdlenClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSimdlenClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpSimdlenClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpSimdlenClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpSimdlenClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpSimdlenClause::pools.begin(); block != SgOmpSimdlenClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSimdlenClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSimdlenClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpSimdlenClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpSimdlenClause::clearMemoryPool() {
    SgOmpSimdlenClause* tempPointer{nullptr};
    if (SgOmpSimdlenClause::pools.empty() == false) {
        auto block{SgOmpSimdlenClause::pools.begin()};
        SgOmpSimdlenClause::next_node = reinterpret_cast<SgOmpSimdlenClause*>(*block);
        for (; block != SgOmpSimdlenClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpSimdlenClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpSimdlenClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpSimdlenClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpSimdlenClause::pool_size-1]);
        }
    }
}

void
SgOmpSimdlenClause::deleteMemoryPool() {
    for (auto p: SgOmpSimdlenClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpSimdlenClause::next_node = nullptr;
    SgOmpSimdlenClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpSimdlenClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdlenClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSimdlenClause)};
    for (size_t blockIndex = SgOmpSimdlenClause::pools.size(); (blockIndex * SgOmpSimdlenClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdlenClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSimdlenClause) = %" PRIuPTR " SgOmpSimdlenClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdlenClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSimdlenClause),SgOmpSimdlenClause::pool_size);
        }
#endif

        SgOmpSimdlenClause* pointer{static_cast<SgOmpSimdlenClause*>(ROSE_MALLOC(SgOmpSimdlenClause::pool_size * sizeof(SgOmpSimdlenClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpSimdlenClause::pool_size * sizeof(SgOmpSimdlenClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpSimdlenClause::pool_size * sizeof(SgOmpSimdlenClause));
#endif
        SgOmpSimdlenClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpSimdlenClause::pool_size * sizeof(SgOmpSimdlenClause), V_SgOmpSimdlenClause));

        if (SgOmpSimdlenClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpSimdlenClause* blkptr{reinterpret_cast<SgOmpSimdlenClause*>(SgOmpSimdlenClause::pools[blockIndex-1])};
                blkptr[SgOmpSimdlenClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpSimdlenClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpSimdlenClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpSimdlenClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpSimdlenClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpSimdlenClause::pool_size - 1};
    SgOmpSimdlenClause* testPointer{reinterpret_cast<SgOmpSimdlenClause*>(SgOmpSimdlenClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpSimdlenClause::pool_size * (SgOmpSimdlenClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpSimdlenClauseStroageClass
 * from its counterpart of SgOmpSimdlenClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpSimdlenClause::initializeStorageClassArray(SgOmpSimdlenClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpSimdlenClause::pools.begin(); block != SgOmpSimdlenClause::pools.end(); ++block) {
        SgOmpSimdlenClause* pointer{reinterpret_cast<SgOmpSimdlenClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSimdlenClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 43200 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpFinalClause that takes its corresponding StorageClass as parameter
 */
SgOmpFinalClause::SgOmpFinalClause(const SgOmpFinalClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 43210 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpFinalClause*
SgOmpFinalClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpFinalClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFinalClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFinalClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFinalClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFinalClause)};
        unsigned long positionInPool{localIndex % SgOmpFinalClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpFinalClause::pool_size};
        returnPointer = &(((SgOmpFinalClause*)(SgOmpFinalClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpFinalClause*
SgOmpFinalClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpFinalClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFinalClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFinalClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFinalClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpFinalClause)};
        unsigned long positionInPool{localIndex % SgOmpFinalClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpFinalClause::pool_size};
        returnPointer = &(((SgOmpFinalClause*)(SgOmpFinalClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpFinalClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpFinalClause::pools.begin(); block != SgOmpFinalClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpFinalClause*>(*block)};
        for (unsigned i = 0; i < SgOmpFinalClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpFinalClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpFinalClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpFinalClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpFinalClause::pools.begin(); block != SgOmpFinalClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpFinalClause*>(*block)};
        for (unsigned i = 0; i < SgOmpFinalClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpFinalClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpFinalClause::clearMemoryPool() {
    SgOmpFinalClause* tempPointer{nullptr};
    if (SgOmpFinalClause::pools.empty() == false) {
        auto block{SgOmpFinalClause::pools.begin()};
        SgOmpFinalClause::next_node = reinterpret_cast<SgOmpFinalClause*>(*block);
        for (; block != SgOmpFinalClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpFinalClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpFinalClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpFinalClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpFinalClause::pool_size-1]);
        }
    }
}

void
SgOmpFinalClause::deleteMemoryPool() {
    for (auto p: SgOmpFinalClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpFinalClause::next_node = nullptr;
    SgOmpFinalClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpFinalClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFinalClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFinalClause)};
    for (size_t blockIndex = SgOmpFinalClause::pools.size(); (blockIndex * SgOmpFinalClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFinalClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFinalClause) = %" PRIuPTR " SgOmpFinalClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFinalClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFinalClause),SgOmpFinalClause::pool_size);
        }
#endif

        SgOmpFinalClause* pointer{static_cast<SgOmpFinalClause*>(ROSE_MALLOC(SgOmpFinalClause::pool_size * sizeof(SgOmpFinalClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpFinalClause::pool_size * sizeof(SgOmpFinalClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpFinalClause::pool_size * sizeof(SgOmpFinalClause));
#endif
        SgOmpFinalClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpFinalClause::pool_size * sizeof(SgOmpFinalClause), V_SgOmpFinalClause));

        if (SgOmpFinalClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpFinalClause* blkptr{reinterpret_cast<SgOmpFinalClause*>(SgOmpFinalClause::pools[blockIndex-1])};
                blkptr[SgOmpFinalClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpFinalClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpFinalClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpFinalClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpFinalClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpFinalClause::pool_size - 1};
    SgOmpFinalClause* testPointer{reinterpret_cast<SgOmpFinalClause*>(SgOmpFinalClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpFinalClause::pool_size * (SgOmpFinalClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpFinalClauseStroageClass
 * from its counterpart of SgOmpFinalClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpFinalClause::initializeStorageClassArray(SgOmpFinalClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpFinalClause::pools.begin(); block != SgOmpFinalClause::pools.end(); ++block) {
        SgOmpFinalClause* pointer{reinterpret_cast<SgOmpFinalClause*>(*block)};
        for (unsigned i = 0; i < SgOmpFinalClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 43427 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpPriorityClause that takes its corresponding StorageClass as parameter
 */
SgOmpPriorityClause::SgOmpPriorityClause(const SgOmpPriorityClauseStorageClass& storageSource)  : SgOmpExpressionClause (storageSource) {

/* #line 43437 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpPriorityClause*
SgOmpPriorityClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpPriorityClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpPriorityClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpPriorityClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpPriorityClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPriorityClause)};
        unsigned long positionInPool{localIndex % SgOmpPriorityClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpPriorityClause::pool_size};
        returnPointer = &(((SgOmpPriorityClause*)(SgOmpPriorityClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpPriorityClause*
SgOmpPriorityClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpPriorityClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpPriorityClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpPriorityClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpPriorityClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpPriorityClause)};
        unsigned long positionInPool{localIndex % SgOmpPriorityClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpPriorityClause::pool_size};
        returnPointer = &(((SgOmpPriorityClause*)(SgOmpPriorityClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpPriorityClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpPriorityClause::pools.begin(); block != SgOmpPriorityClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpPriorityClause*>(*block)};
        for (unsigned i = 0; i < SgOmpPriorityClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpPriorityClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpPriorityClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpPriorityClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpPriorityClause::pools.begin(); block != SgOmpPriorityClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpPriorityClause*>(*block)};
        for (unsigned i = 0; i < SgOmpPriorityClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpPriorityClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpPriorityClause::clearMemoryPool() {
    SgOmpPriorityClause* tempPointer{nullptr};
    if (SgOmpPriorityClause::pools.empty() == false) {
        auto block{SgOmpPriorityClause::pools.begin()};
        SgOmpPriorityClause::next_node = reinterpret_cast<SgOmpPriorityClause*>(*block);
        for (; block != SgOmpPriorityClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpPriorityClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpPriorityClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpPriorityClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpPriorityClause::pool_size-1]);
        }
    }
}

void
SgOmpPriorityClause::deleteMemoryPool() {
    for (auto p: SgOmpPriorityClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpPriorityClause::next_node = nullptr;
    SgOmpPriorityClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpPriorityClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPriorityClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpPriorityClause)};
    for (size_t blockIndex = SgOmpPriorityClause::pools.size(); (blockIndex * SgOmpPriorityClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPriorityClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpPriorityClause) = %" PRIuPTR " SgOmpPriorityClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPriorityClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpPriorityClause),SgOmpPriorityClause::pool_size);
        }
#endif

        SgOmpPriorityClause* pointer{static_cast<SgOmpPriorityClause*>(ROSE_MALLOC(SgOmpPriorityClause::pool_size * sizeof(SgOmpPriorityClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpPriorityClause::pool_size * sizeof(SgOmpPriorityClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpPriorityClause::pool_size * sizeof(SgOmpPriorityClause));
#endif
        SgOmpPriorityClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpPriorityClause::pool_size * sizeof(SgOmpPriorityClause), V_SgOmpPriorityClause));

        if (SgOmpPriorityClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpPriorityClause* blkptr{reinterpret_cast<SgOmpPriorityClause*>(SgOmpPriorityClause::pools[blockIndex-1])};
                blkptr[SgOmpPriorityClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpPriorityClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpPriorityClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpPriorityClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpPriorityClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpPriorityClause::pool_size - 1};
    SgOmpPriorityClause* testPointer{reinterpret_cast<SgOmpPriorityClause*>(SgOmpPriorityClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpPriorityClause::pool_size * (SgOmpPriorityClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpPriorityClauseStroageClass
 * from its counterpart of SgOmpPriorityClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpPriorityClause::initializeStorageClassArray(SgOmpPriorityClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpPriorityClause::pools.begin(); block != SgOmpPriorityClause::pools.end(); ++block) {
        SgOmpPriorityClause* pointer{reinterpret_cast<SgOmpPriorityClause*>(*block)};
        for (unsigned i = 0; i < SgOmpPriorityClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 43654 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpInbranchClause that takes its corresponding StorageClass as parameter
 */
SgOmpInbranchClause::SgOmpInbranchClause(const SgOmpInbranchClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 43664 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpInbranchClause*
SgOmpInbranchClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpInbranchClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpInbranchClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpInbranchClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpInbranchClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpInbranchClause)};
        unsigned long positionInPool{localIndex % SgOmpInbranchClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpInbranchClause::pool_size};
        returnPointer = &(((SgOmpInbranchClause*)(SgOmpInbranchClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpInbranchClause*
SgOmpInbranchClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpInbranchClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpInbranchClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpInbranchClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpInbranchClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpInbranchClause)};
        unsigned long positionInPool{localIndex % SgOmpInbranchClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpInbranchClause::pool_size};
        returnPointer = &(((SgOmpInbranchClause*)(SgOmpInbranchClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpInbranchClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpInbranchClause::pools.begin(); block != SgOmpInbranchClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpInbranchClause*>(*block)};
        for (unsigned i = 0; i < SgOmpInbranchClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpInbranchClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpInbranchClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpInbranchClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpInbranchClause::pools.begin(); block != SgOmpInbranchClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpInbranchClause*>(*block)};
        for (unsigned i = 0; i < SgOmpInbranchClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpInbranchClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpInbranchClause::clearMemoryPool() {
    SgOmpInbranchClause* tempPointer{nullptr};
    if (SgOmpInbranchClause::pools.empty() == false) {
        auto block{SgOmpInbranchClause::pools.begin()};
        SgOmpInbranchClause::next_node = reinterpret_cast<SgOmpInbranchClause*>(*block);
        for (; block != SgOmpInbranchClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpInbranchClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpInbranchClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpInbranchClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpInbranchClause::pool_size-1]);
        }
    }
}

void
SgOmpInbranchClause::deleteMemoryPool() {
    for (auto p: SgOmpInbranchClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpInbranchClause::next_node = nullptr;
    SgOmpInbranchClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpInbranchClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpInbranchClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpInbranchClause)};
    for (size_t blockIndex = SgOmpInbranchClause::pools.size(); (blockIndex * SgOmpInbranchClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpInbranchClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpInbranchClause) = %" PRIuPTR " SgOmpInbranchClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpInbranchClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpInbranchClause),SgOmpInbranchClause::pool_size);
        }
#endif

        SgOmpInbranchClause* pointer{static_cast<SgOmpInbranchClause*>(ROSE_MALLOC(SgOmpInbranchClause::pool_size * sizeof(SgOmpInbranchClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpInbranchClause::pool_size * sizeof(SgOmpInbranchClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpInbranchClause::pool_size * sizeof(SgOmpInbranchClause));
#endif
        SgOmpInbranchClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpInbranchClause::pool_size * sizeof(SgOmpInbranchClause), V_SgOmpInbranchClause));

        if (SgOmpInbranchClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpInbranchClause* blkptr{reinterpret_cast<SgOmpInbranchClause*>(SgOmpInbranchClause::pools[blockIndex-1])};
                blkptr[SgOmpInbranchClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpInbranchClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpInbranchClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpInbranchClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpInbranchClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpInbranchClause::pool_size - 1};
    SgOmpInbranchClause* testPointer{reinterpret_cast<SgOmpInbranchClause*>(SgOmpInbranchClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpInbranchClause::pool_size * (SgOmpInbranchClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpInbranchClauseStroageClass
 * from its counterpart of SgOmpInbranchClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpInbranchClause::initializeStorageClassArray(SgOmpInbranchClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpInbranchClause::pools.begin(); block != SgOmpInbranchClause::pools.end(); ++block) {
        SgOmpInbranchClause* pointer{reinterpret_cast<SgOmpInbranchClause*>(*block)};
        for (unsigned i = 0; i < SgOmpInbranchClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 43881 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpNotinbranchClause that takes its corresponding StorageClass as parameter
 */
SgOmpNotinbranchClause::SgOmpNotinbranchClause(const SgOmpNotinbranchClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 43891 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpNotinbranchClause*
SgOmpNotinbranchClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpNotinbranchClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNotinbranchClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNotinbranchClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpNotinbranchClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNotinbranchClause)};
        unsigned long positionInPool{localIndex % SgOmpNotinbranchClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpNotinbranchClause::pool_size};
        returnPointer = &(((SgOmpNotinbranchClause*)(SgOmpNotinbranchClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpNotinbranchClause*
SgOmpNotinbranchClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpNotinbranchClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNotinbranchClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNotinbranchClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpNotinbranchClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpNotinbranchClause)};
        unsigned long positionInPool{localIndex % SgOmpNotinbranchClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpNotinbranchClause::pool_size};
        returnPointer = &(((SgOmpNotinbranchClause*)(SgOmpNotinbranchClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpNotinbranchClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpNotinbranchClause::pools.begin(); block != SgOmpNotinbranchClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpNotinbranchClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNotinbranchClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpNotinbranchClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpNotinbranchClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpNotinbranchClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpNotinbranchClause::pools.begin(); block != SgOmpNotinbranchClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpNotinbranchClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNotinbranchClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpNotinbranchClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpNotinbranchClause::clearMemoryPool() {
    SgOmpNotinbranchClause* tempPointer{nullptr};
    if (SgOmpNotinbranchClause::pools.empty() == false) {
        auto block{SgOmpNotinbranchClause::pools.begin()};
        SgOmpNotinbranchClause::next_node = reinterpret_cast<SgOmpNotinbranchClause*>(*block);
        for (; block != SgOmpNotinbranchClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpNotinbranchClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpNotinbranchClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpNotinbranchClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpNotinbranchClause::pool_size-1]);
        }
    }
}

void
SgOmpNotinbranchClause::deleteMemoryPool() {
    for (auto p: SgOmpNotinbranchClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpNotinbranchClause::next_node = nullptr;
    SgOmpNotinbranchClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpNotinbranchClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNotinbranchClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNotinbranchClause)};
    for (size_t blockIndex = SgOmpNotinbranchClause::pools.size(); (blockIndex * SgOmpNotinbranchClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNotinbranchClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNotinbranchClause) = %" PRIuPTR " SgOmpNotinbranchClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpNotinbranchClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpNotinbranchClause),SgOmpNotinbranchClause::pool_size);
        }
#endif

        SgOmpNotinbranchClause* pointer{static_cast<SgOmpNotinbranchClause*>(ROSE_MALLOC(SgOmpNotinbranchClause::pool_size * sizeof(SgOmpNotinbranchClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpNotinbranchClause::pool_size * sizeof(SgOmpNotinbranchClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpNotinbranchClause::pool_size * sizeof(SgOmpNotinbranchClause));
#endif
        SgOmpNotinbranchClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpNotinbranchClause::pool_size * sizeof(SgOmpNotinbranchClause), V_SgOmpNotinbranchClause));

        if (SgOmpNotinbranchClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpNotinbranchClause* blkptr{reinterpret_cast<SgOmpNotinbranchClause*>(SgOmpNotinbranchClause::pools[blockIndex-1])};
                blkptr[SgOmpNotinbranchClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpNotinbranchClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpNotinbranchClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpNotinbranchClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpNotinbranchClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpNotinbranchClause::pool_size - 1};
    SgOmpNotinbranchClause* testPointer{reinterpret_cast<SgOmpNotinbranchClause*>(SgOmpNotinbranchClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpNotinbranchClause::pool_size * (SgOmpNotinbranchClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpNotinbranchClauseStroageClass
 * from its counterpart of SgOmpNotinbranchClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpNotinbranchClause::initializeStorageClassArray(SgOmpNotinbranchClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpNotinbranchClause::pools.begin(); block != SgOmpNotinbranchClause::pools.end(); ++block) {
        SgOmpNotinbranchClause* pointer{reinterpret_cast<SgOmpNotinbranchClause*>(*block)};
        for (unsigned i = 0; i < SgOmpNotinbranchClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 44108 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpVariablesClause that takes its corresponding StorageClass as parameter
 */
SgOmpVariablesClause::SgOmpVariablesClause(const SgOmpVariablesClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 44118 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_variables = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_variables));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpVariablesClause*
SgOmpVariablesClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpVariablesClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpVariablesClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpVariablesClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpVariablesClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpVariablesClause)};
        unsigned long positionInPool{localIndex % SgOmpVariablesClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpVariablesClause::pool_size};
        returnPointer = &(((SgOmpVariablesClause*)(SgOmpVariablesClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpVariablesClause*
SgOmpVariablesClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpVariablesClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpVariablesClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpVariablesClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpVariablesClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpVariablesClause)};
        unsigned long positionInPool{localIndex % SgOmpVariablesClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpVariablesClause::pool_size};
        returnPointer = &(((SgOmpVariablesClause*)(SgOmpVariablesClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpVariablesClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpVariablesClause::pools.begin(); block != SgOmpVariablesClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpVariablesClause*>(*block)};
        for (unsigned i = 0; i < SgOmpVariablesClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpVariablesClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpVariablesClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpVariablesClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpVariablesClause::pools.begin(); block != SgOmpVariablesClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpVariablesClause*>(*block)};
        for (unsigned i = 0; i < SgOmpVariablesClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpVariablesClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpVariablesClause::clearMemoryPool() {
    SgOmpVariablesClause* tempPointer{nullptr};
    if (SgOmpVariablesClause::pools.empty() == false) {
        auto block{SgOmpVariablesClause::pools.begin()};
        SgOmpVariablesClause::next_node = reinterpret_cast<SgOmpVariablesClause*>(*block);
        for (; block != SgOmpVariablesClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpVariablesClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpVariablesClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpVariablesClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpVariablesClause::pool_size-1]);
        }
    }
}

void
SgOmpVariablesClause::deleteMemoryPool() {
    for (auto p: SgOmpVariablesClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpVariablesClause::next_node = nullptr;
    SgOmpVariablesClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpVariablesClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpVariablesClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpVariablesClause)};
    for (size_t blockIndex = SgOmpVariablesClause::pools.size(); (blockIndex * SgOmpVariablesClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpVariablesClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpVariablesClause) = %" PRIuPTR " SgOmpVariablesClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpVariablesClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpVariablesClause),SgOmpVariablesClause::pool_size);
        }
#endif

        SgOmpVariablesClause* pointer{static_cast<SgOmpVariablesClause*>(ROSE_MALLOC(SgOmpVariablesClause::pool_size * sizeof(SgOmpVariablesClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpVariablesClause::pool_size * sizeof(SgOmpVariablesClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpVariablesClause::pool_size * sizeof(SgOmpVariablesClause));
#endif
        SgOmpVariablesClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpVariablesClause::pool_size * sizeof(SgOmpVariablesClause), V_SgOmpVariablesClause));

        if (SgOmpVariablesClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpVariablesClause* blkptr{reinterpret_cast<SgOmpVariablesClause*>(SgOmpVariablesClause::pools[blockIndex-1])};
                blkptr[SgOmpVariablesClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpVariablesClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpVariablesClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpVariablesClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpVariablesClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpVariablesClause::pool_size - 1};
    SgOmpVariablesClause* testPointer{reinterpret_cast<SgOmpVariablesClause*>(SgOmpVariablesClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpVariablesClause::pool_size * (SgOmpVariablesClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpVariablesClauseStroageClass
 * from its counterpart of SgOmpVariablesClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpVariablesClause::initializeStorageClassArray(SgOmpVariablesClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpVariablesClause::pools.begin(); block != SgOmpVariablesClause::pools.end(); ++block) {
        SgOmpVariablesClause* pointer{reinterpret_cast<SgOmpVariablesClause*>(*block)};
        for (unsigned i = 0; i < SgOmpVariablesClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 44336 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpCopyprivateClause that takes its corresponding StorageClass as parameter
 */
SgOmpCopyprivateClause::SgOmpCopyprivateClause(const SgOmpCopyprivateClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 44346 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpCopyprivateClause*
SgOmpCopyprivateClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpCopyprivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCopyprivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCopyprivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCopyprivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyprivateClause)};
        unsigned long positionInPool{localIndex % SgOmpCopyprivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCopyprivateClause::pool_size};
        returnPointer = &(((SgOmpCopyprivateClause*)(SgOmpCopyprivateClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpCopyprivateClause*
SgOmpCopyprivateClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpCopyprivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCopyprivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCopyprivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCopyprivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpCopyprivateClause)};
        unsigned long positionInPool{localIndex % SgOmpCopyprivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCopyprivateClause::pool_size};
        returnPointer = &(((SgOmpCopyprivateClause*)(SgOmpCopyprivateClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpCopyprivateClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpCopyprivateClause::pools.begin(); block != SgOmpCopyprivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCopyprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCopyprivateClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpCopyprivateClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpCopyprivateClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpCopyprivateClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpCopyprivateClause::pools.begin(); block != SgOmpCopyprivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCopyprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCopyprivateClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpCopyprivateClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpCopyprivateClause::clearMemoryPool() {
    SgOmpCopyprivateClause* tempPointer{nullptr};
    if (SgOmpCopyprivateClause::pools.empty() == false) {
        auto block{SgOmpCopyprivateClause::pools.begin()};
        SgOmpCopyprivateClause::next_node = reinterpret_cast<SgOmpCopyprivateClause*>(*block);
        for (; block != SgOmpCopyprivateClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpCopyprivateClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpCopyprivateClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpCopyprivateClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpCopyprivateClause::pool_size-1]);
        }
    }
}

void
SgOmpCopyprivateClause::deleteMemoryPool() {
    for (auto p: SgOmpCopyprivateClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpCopyprivateClause::next_node = nullptr;
    SgOmpCopyprivateClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpCopyprivateClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyprivateClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCopyprivateClause)};
    for (size_t blockIndex = SgOmpCopyprivateClause::pools.size(); (blockIndex * SgOmpCopyprivateClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyprivateClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCopyprivateClause) = %" PRIuPTR " SgOmpCopyprivateClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyprivateClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCopyprivateClause),SgOmpCopyprivateClause::pool_size);
        }
#endif

        SgOmpCopyprivateClause* pointer{static_cast<SgOmpCopyprivateClause*>(ROSE_MALLOC(SgOmpCopyprivateClause::pool_size * sizeof(SgOmpCopyprivateClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpCopyprivateClause::pool_size * sizeof(SgOmpCopyprivateClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpCopyprivateClause::pool_size * sizeof(SgOmpCopyprivateClause));
#endif
        SgOmpCopyprivateClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpCopyprivateClause::pool_size * sizeof(SgOmpCopyprivateClause), V_SgOmpCopyprivateClause));

        if (SgOmpCopyprivateClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpCopyprivateClause* blkptr{reinterpret_cast<SgOmpCopyprivateClause*>(SgOmpCopyprivateClause::pools[blockIndex-1])};
                blkptr[SgOmpCopyprivateClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpCopyprivateClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpCopyprivateClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpCopyprivateClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpCopyprivateClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpCopyprivateClause::pool_size - 1};
    SgOmpCopyprivateClause* testPointer{reinterpret_cast<SgOmpCopyprivateClause*>(SgOmpCopyprivateClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpCopyprivateClause::pool_size * (SgOmpCopyprivateClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpCopyprivateClauseStroageClass
 * from its counterpart of SgOmpCopyprivateClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpCopyprivateClause::initializeStorageClassArray(SgOmpCopyprivateClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpCopyprivateClause::pools.begin(); block != SgOmpCopyprivateClause::pools.end(); ++block) {
        SgOmpCopyprivateClause* pointer{reinterpret_cast<SgOmpCopyprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCopyprivateClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 44563 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpPrivateClause that takes its corresponding StorageClass as parameter
 */
SgOmpPrivateClause::SgOmpPrivateClause(const SgOmpPrivateClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 44573 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpPrivateClause*
SgOmpPrivateClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpPrivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpPrivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpPrivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpPrivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPrivateClause)};
        unsigned long positionInPool{localIndex % SgOmpPrivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpPrivateClause::pool_size};
        returnPointer = &(((SgOmpPrivateClause*)(SgOmpPrivateClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpPrivateClause*
SgOmpPrivateClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpPrivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpPrivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpPrivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpPrivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpPrivateClause)};
        unsigned long positionInPool{localIndex % SgOmpPrivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpPrivateClause::pool_size};
        returnPointer = &(((SgOmpPrivateClause*)(SgOmpPrivateClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpPrivateClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpPrivateClause::pools.begin(); block != SgOmpPrivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpPrivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpPrivateClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpPrivateClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpPrivateClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpPrivateClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpPrivateClause::pools.begin(); block != SgOmpPrivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpPrivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpPrivateClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpPrivateClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpPrivateClause::clearMemoryPool() {
    SgOmpPrivateClause* tempPointer{nullptr};
    if (SgOmpPrivateClause::pools.empty() == false) {
        auto block{SgOmpPrivateClause::pools.begin()};
        SgOmpPrivateClause::next_node = reinterpret_cast<SgOmpPrivateClause*>(*block);
        for (; block != SgOmpPrivateClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpPrivateClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpPrivateClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpPrivateClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpPrivateClause::pool_size-1]);
        }
    }
}

void
SgOmpPrivateClause::deleteMemoryPool() {
    for (auto p: SgOmpPrivateClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpPrivateClause::next_node = nullptr;
    SgOmpPrivateClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpPrivateClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPrivateClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpPrivateClause)};
    for (size_t blockIndex = SgOmpPrivateClause::pools.size(); (blockIndex * SgOmpPrivateClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPrivateClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpPrivateClause) = %" PRIuPTR " SgOmpPrivateClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpPrivateClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpPrivateClause),SgOmpPrivateClause::pool_size);
        }
#endif

        SgOmpPrivateClause* pointer{static_cast<SgOmpPrivateClause*>(ROSE_MALLOC(SgOmpPrivateClause::pool_size * sizeof(SgOmpPrivateClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpPrivateClause::pool_size * sizeof(SgOmpPrivateClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpPrivateClause::pool_size * sizeof(SgOmpPrivateClause));
#endif
        SgOmpPrivateClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpPrivateClause::pool_size * sizeof(SgOmpPrivateClause), V_SgOmpPrivateClause));

        if (SgOmpPrivateClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpPrivateClause* blkptr{reinterpret_cast<SgOmpPrivateClause*>(SgOmpPrivateClause::pools[blockIndex-1])};
                blkptr[SgOmpPrivateClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpPrivateClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpPrivateClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpPrivateClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpPrivateClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpPrivateClause::pool_size - 1};
    SgOmpPrivateClause* testPointer{reinterpret_cast<SgOmpPrivateClause*>(SgOmpPrivateClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpPrivateClause::pool_size * (SgOmpPrivateClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpPrivateClauseStroageClass
 * from its counterpart of SgOmpPrivateClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpPrivateClause::initializeStorageClassArray(SgOmpPrivateClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpPrivateClause::pools.begin(); block != SgOmpPrivateClause::pools.end(); ++block) {
        SgOmpPrivateClause* pointer{reinterpret_cast<SgOmpPrivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpPrivateClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 44790 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpFirstprivateClause that takes its corresponding StorageClass as parameter
 */
SgOmpFirstprivateClause::SgOmpFirstprivateClause(const SgOmpFirstprivateClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 44800 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpFirstprivateClause*
SgOmpFirstprivateClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpFirstprivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFirstprivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFirstprivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFirstprivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFirstprivateClause)};
        unsigned long positionInPool{localIndex % SgOmpFirstprivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpFirstprivateClause::pool_size};
        returnPointer = &(((SgOmpFirstprivateClause*)(SgOmpFirstprivateClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpFirstprivateClause*
SgOmpFirstprivateClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpFirstprivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFirstprivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFirstprivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFirstprivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpFirstprivateClause)};
        unsigned long positionInPool{localIndex % SgOmpFirstprivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpFirstprivateClause::pool_size};
        returnPointer = &(((SgOmpFirstprivateClause*)(SgOmpFirstprivateClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpFirstprivateClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpFirstprivateClause::pools.begin(); block != SgOmpFirstprivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpFirstprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpFirstprivateClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpFirstprivateClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpFirstprivateClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpFirstprivateClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpFirstprivateClause::pools.begin(); block != SgOmpFirstprivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpFirstprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpFirstprivateClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpFirstprivateClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpFirstprivateClause::clearMemoryPool() {
    SgOmpFirstprivateClause* tempPointer{nullptr};
    if (SgOmpFirstprivateClause::pools.empty() == false) {
        auto block{SgOmpFirstprivateClause::pools.begin()};
        SgOmpFirstprivateClause::next_node = reinterpret_cast<SgOmpFirstprivateClause*>(*block);
        for (; block != SgOmpFirstprivateClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpFirstprivateClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpFirstprivateClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpFirstprivateClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpFirstprivateClause::pool_size-1]);
        }
    }
}

void
SgOmpFirstprivateClause::deleteMemoryPool() {
    for (auto p: SgOmpFirstprivateClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpFirstprivateClause::next_node = nullptr;
    SgOmpFirstprivateClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpFirstprivateClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFirstprivateClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFirstprivateClause)};
    for (size_t blockIndex = SgOmpFirstprivateClause::pools.size(); (blockIndex * SgOmpFirstprivateClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFirstprivateClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFirstprivateClause) = %" PRIuPTR " SgOmpFirstprivateClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFirstprivateClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFirstprivateClause),SgOmpFirstprivateClause::pool_size);
        }
#endif

        SgOmpFirstprivateClause* pointer{static_cast<SgOmpFirstprivateClause*>(ROSE_MALLOC(SgOmpFirstprivateClause::pool_size * sizeof(SgOmpFirstprivateClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpFirstprivateClause::pool_size * sizeof(SgOmpFirstprivateClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpFirstprivateClause::pool_size * sizeof(SgOmpFirstprivateClause));
#endif
        SgOmpFirstprivateClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpFirstprivateClause::pool_size * sizeof(SgOmpFirstprivateClause), V_SgOmpFirstprivateClause));

        if (SgOmpFirstprivateClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpFirstprivateClause* blkptr{reinterpret_cast<SgOmpFirstprivateClause*>(SgOmpFirstprivateClause::pools[blockIndex-1])};
                blkptr[SgOmpFirstprivateClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpFirstprivateClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpFirstprivateClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpFirstprivateClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpFirstprivateClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpFirstprivateClause::pool_size - 1};
    SgOmpFirstprivateClause* testPointer{reinterpret_cast<SgOmpFirstprivateClause*>(SgOmpFirstprivateClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpFirstprivateClause::pool_size * (SgOmpFirstprivateClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpFirstprivateClauseStroageClass
 * from its counterpart of SgOmpFirstprivateClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpFirstprivateClause::initializeStorageClassArray(SgOmpFirstprivateClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpFirstprivateClause::pools.begin(); block != SgOmpFirstprivateClause::pools.end(); ++block) {
        SgOmpFirstprivateClause* pointer{reinterpret_cast<SgOmpFirstprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpFirstprivateClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 45017 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpSharedClause that takes its corresponding StorageClass as parameter
 */
SgOmpSharedClause::SgOmpSharedClause(const SgOmpSharedClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 45027 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpSharedClause*
SgOmpSharedClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpSharedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSharedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSharedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSharedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSharedClause)};
        unsigned long positionInPool{localIndex % SgOmpSharedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSharedClause::pool_size};
        returnPointer = &(((SgOmpSharedClause*)(SgOmpSharedClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpSharedClause*
SgOmpSharedClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpSharedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSharedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSharedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSharedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpSharedClause)};
        unsigned long positionInPool{localIndex % SgOmpSharedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSharedClause::pool_size};
        returnPointer = &(((SgOmpSharedClause*)(SgOmpSharedClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpSharedClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpSharedClause::pools.begin(); block != SgOmpSharedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSharedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSharedClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpSharedClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpSharedClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpSharedClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpSharedClause::pools.begin(); block != SgOmpSharedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSharedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSharedClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpSharedClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpSharedClause::clearMemoryPool() {
    SgOmpSharedClause* tempPointer{nullptr};
    if (SgOmpSharedClause::pools.empty() == false) {
        auto block{SgOmpSharedClause::pools.begin()};
        SgOmpSharedClause::next_node = reinterpret_cast<SgOmpSharedClause*>(*block);
        for (; block != SgOmpSharedClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpSharedClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpSharedClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpSharedClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpSharedClause::pool_size-1]);
        }
    }
}

void
SgOmpSharedClause::deleteMemoryPool() {
    for (auto p: SgOmpSharedClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpSharedClause::next_node = nullptr;
    SgOmpSharedClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpSharedClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSharedClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSharedClause)};
    for (size_t blockIndex = SgOmpSharedClause::pools.size(); (blockIndex * SgOmpSharedClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSharedClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSharedClause) = %" PRIuPTR " SgOmpSharedClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSharedClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSharedClause),SgOmpSharedClause::pool_size);
        }
#endif

        SgOmpSharedClause* pointer{static_cast<SgOmpSharedClause*>(ROSE_MALLOC(SgOmpSharedClause::pool_size * sizeof(SgOmpSharedClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpSharedClause::pool_size * sizeof(SgOmpSharedClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpSharedClause::pool_size * sizeof(SgOmpSharedClause));
#endif
        SgOmpSharedClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpSharedClause::pool_size * sizeof(SgOmpSharedClause), V_SgOmpSharedClause));

        if (SgOmpSharedClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpSharedClause* blkptr{reinterpret_cast<SgOmpSharedClause*>(SgOmpSharedClause::pools[blockIndex-1])};
                blkptr[SgOmpSharedClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpSharedClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpSharedClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpSharedClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpSharedClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpSharedClause::pool_size - 1};
    SgOmpSharedClause* testPointer{reinterpret_cast<SgOmpSharedClause*>(SgOmpSharedClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpSharedClause::pool_size * (SgOmpSharedClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpSharedClauseStroageClass
 * from its counterpart of SgOmpSharedClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpSharedClause::initializeStorageClassArray(SgOmpSharedClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpSharedClause::pools.begin(); block != SgOmpSharedClause::pools.end(); ++block) {
        SgOmpSharedClause* pointer{reinterpret_cast<SgOmpSharedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpSharedClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 45244 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpCopyinClause that takes its corresponding StorageClass as parameter
 */
SgOmpCopyinClause::SgOmpCopyinClause(const SgOmpCopyinClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 45254 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpCopyinClause*
SgOmpCopyinClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpCopyinClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCopyinClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCopyinClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCopyinClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyinClause)};
        unsigned long positionInPool{localIndex % SgOmpCopyinClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCopyinClause::pool_size};
        returnPointer = &(((SgOmpCopyinClause*)(SgOmpCopyinClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpCopyinClause*
SgOmpCopyinClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpCopyinClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCopyinClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCopyinClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCopyinClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpCopyinClause)};
        unsigned long positionInPool{localIndex % SgOmpCopyinClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCopyinClause::pool_size};
        returnPointer = &(((SgOmpCopyinClause*)(SgOmpCopyinClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpCopyinClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpCopyinClause::pools.begin(); block != SgOmpCopyinClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCopyinClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCopyinClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpCopyinClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpCopyinClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpCopyinClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpCopyinClause::pools.begin(); block != SgOmpCopyinClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCopyinClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCopyinClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpCopyinClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpCopyinClause::clearMemoryPool() {
    SgOmpCopyinClause* tempPointer{nullptr};
    if (SgOmpCopyinClause::pools.empty() == false) {
        auto block{SgOmpCopyinClause::pools.begin()};
        SgOmpCopyinClause::next_node = reinterpret_cast<SgOmpCopyinClause*>(*block);
        for (; block != SgOmpCopyinClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpCopyinClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpCopyinClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpCopyinClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpCopyinClause::pool_size-1]);
        }
    }
}

void
SgOmpCopyinClause::deleteMemoryPool() {
    for (auto p: SgOmpCopyinClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpCopyinClause::next_node = nullptr;
    SgOmpCopyinClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpCopyinClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyinClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCopyinClause)};
    for (size_t blockIndex = SgOmpCopyinClause::pools.size(); (blockIndex * SgOmpCopyinClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyinClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCopyinClause) = %" PRIuPTR " SgOmpCopyinClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCopyinClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCopyinClause),SgOmpCopyinClause::pool_size);
        }
#endif

        SgOmpCopyinClause* pointer{static_cast<SgOmpCopyinClause*>(ROSE_MALLOC(SgOmpCopyinClause::pool_size * sizeof(SgOmpCopyinClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpCopyinClause::pool_size * sizeof(SgOmpCopyinClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpCopyinClause::pool_size * sizeof(SgOmpCopyinClause));
#endif
        SgOmpCopyinClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpCopyinClause::pool_size * sizeof(SgOmpCopyinClause), V_SgOmpCopyinClause));

        if (SgOmpCopyinClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpCopyinClause* blkptr{reinterpret_cast<SgOmpCopyinClause*>(SgOmpCopyinClause::pools[blockIndex-1])};
                blkptr[SgOmpCopyinClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpCopyinClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpCopyinClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpCopyinClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpCopyinClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpCopyinClause::pool_size - 1};
    SgOmpCopyinClause* testPointer{reinterpret_cast<SgOmpCopyinClause*>(SgOmpCopyinClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpCopyinClause::pool_size * (SgOmpCopyinClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpCopyinClauseStroageClass
 * from its counterpart of SgOmpCopyinClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpCopyinClause::initializeStorageClassArray(SgOmpCopyinClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpCopyinClause::pools.begin(); block != SgOmpCopyinClause::pools.end(); ++block) {
        SgOmpCopyinClause* pointer{reinterpret_cast<SgOmpCopyinClause*>(*block)};
        for (unsigned i = 0; i < SgOmpCopyinClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 45471 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpLastprivateClause that takes its corresponding StorageClass as parameter
 */
SgOmpLastprivateClause::SgOmpLastprivateClause(const SgOmpLastprivateClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 45481 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpLastprivateClause*
SgOmpLastprivateClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpLastprivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpLastprivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpLastprivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpLastprivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLastprivateClause)};
        unsigned long positionInPool{localIndex % SgOmpLastprivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpLastprivateClause::pool_size};
        returnPointer = &(((SgOmpLastprivateClause*)(SgOmpLastprivateClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpLastprivateClause*
SgOmpLastprivateClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpLastprivateClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpLastprivateClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpLastprivateClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpLastprivateClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpLastprivateClause)};
        unsigned long positionInPool{localIndex % SgOmpLastprivateClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpLastprivateClause::pool_size};
        returnPointer = &(((SgOmpLastprivateClause*)(SgOmpLastprivateClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpLastprivateClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpLastprivateClause::pools.begin(); block != SgOmpLastprivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpLastprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpLastprivateClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpLastprivateClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpLastprivateClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpLastprivateClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpLastprivateClause::pools.begin(); block != SgOmpLastprivateClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpLastprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpLastprivateClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpLastprivateClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpLastprivateClause::clearMemoryPool() {
    SgOmpLastprivateClause* tempPointer{nullptr};
    if (SgOmpLastprivateClause::pools.empty() == false) {
        auto block{SgOmpLastprivateClause::pools.begin()};
        SgOmpLastprivateClause::next_node = reinterpret_cast<SgOmpLastprivateClause*>(*block);
        for (; block != SgOmpLastprivateClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpLastprivateClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpLastprivateClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpLastprivateClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpLastprivateClause::pool_size-1]);
        }
    }
}

void
SgOmpLastprivateClause::deleteMemoryPool() {
    for (auto p: SgOmpLastprivateClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpLastprivateClause::next_node = nullptr;
    SgOmpLastprivateClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpLastprivateClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLastprivateClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpLastprivateClause)};
    for (size_t blockIndex = SgOmpLastprivateClause::pools.size(); (blockIndex * SgOmpLastprivateClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLastprivateClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpLastprivateClause) = %" PRIuPTR " SgOmpLastprivateClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLastprivateClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpLastprivateClause),SgOmpLastprivateClause::pool_size);
        }
#endif

        SgOmpLastprivateClause* pointer{static_cast<SgOmpLastprivateClause*>(ROSE_MALLOC(SgOmpLastprivateClause::pool_size * sizeof(SgOmpLastprivateClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpLastprivateClause::pool_size * sizeof(SgOmpLastprivateClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpLastprivateClause::pool_size * sizeof(SgOmpLastprivateClause));
#endif
        SgOmpLastprivateClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpLastprivateClause::pool_size * sizeof(SgOmpLastprivateClause), V_SgOmpLastprivateClause));

        if (SgOmpLastprivateClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpLastprivateClause* blkptr{reinterpret_cast<SgOmpLastprivateClause*>(SgOmpLastprivateClause::pools[blockIndex-1])};
                blkptr[SgOmpLastprivateClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpLastprivateClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpLastprivateClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpLastprivateClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpLastprivateClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpLastprivateClause::pool_size - 1};
    SgOmpLastprivateClause* testPointer{reinterpret_cast<SgOmpLastprivateClause*>(SgOmpLastprivateClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpLastprivateClause::pool_size * (SgOmpLastprivateClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpLastprivateClauseStroageClass
 * from its counterpart of SgOmpLastprivateClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpLastprivateClause::initializeStorageClassArray(SgOmpLastprivateClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpLastprivateClause::pools.begin(); block != SgOmpLastprivateClause::pools.end(); ++block) {
        SgOmpLastprivateClause* pointer{reinterpret_cast<SgOmpLastprivateClause*>(*block)};
        for (unsigned i = 0; i < SgOmpLastprivateClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 45698 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpReductionClause that takes its corresponding StorageClass as parameter
 */
SgOmpReductionClause::SgOmpReductionClause(const SgOmpReductionClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 45708 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_operation = storageSource.storageOf_operation;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpReductionClause*
SgOmpReductionClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpReductionClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpReductionClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpReductionClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpReductionClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpReductionClause)};
        unsigned long positionInPool{localIndex % SgOmpReductionClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpReductionClause::pool_size};
        returnPointer = &(((SgOmpReductionClause*)(SgOmpReductionClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpReductionClause*
SgOmpReductionClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpReductionClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpReductionClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpReductionClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpReductionClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpReductionClause)};
        unsigned long positionInPool{localIndex % SgOmpReductionClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpReductionClause::pool_size};
        returnPointer = &(((SgOmpReductionClause*)(SgOmpReductionClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpReductionClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpReductionClause::pools.begin(); block != SgOmpReductionClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpReductionClause*>(*block)};
        for (unsigned i = 0; i < SgOmpReductionClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpReductionClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpReductionClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpReductionClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpReductionClause::pools.begin(); block != SgOmpReductionClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpReductionClause*>(*block)};
        for (unsigned i = 0; i < SgOmpReductionClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpReductionClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpReductionClause::clearMemoryPool() {
    SgOmpReductionClause* tempPointer{nullptr};
    if (SgOmpReductionClause::pools.empty() == false) {
        auto block{SgOmpReductionClause::pools.begin()};
        SgOmpReductionClause::next_node = reinterpret_cast<SgOmpReductionClause*>(*block);
        for (; block != SgOmpReductionClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpReductionClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpReductionClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpReductionClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpReductionClause::pool_size-1]);
        }
    }
}

void
SgOmpReductionClause::deleteMemoryPool() {
    for (auto p: SgOmpReductionClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpReductionClause::next_node = nullptr;
    SgOmpReductionClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpReductionClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpReductionClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpReductionClause)};
    for (size_t blockIndex = SgOmpReductionClause::pools.size(); (blockIndex * SgOmpReductionClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpReductionClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpReductionClause) = %" PRIuPTR " SgOmpReductionClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpReductionClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpReductionClause),SgOmpReductionClause::pool_size);
        }
#endif

        SgOmpReductionClause* pointer{static_cast<SgOmpReductionClause*>(ROSE_MALLOC(SgOmpReductionClause::pool_size * sizeof(SgOmpReductionClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpReductionClause::pool_size * sizeof(SgOmpReductionClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpReductionClause::pool_size * sizeof(SgOmpReductionClause));
#endif
        SgOmpReductionClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpReductionClause::pool_size * sizeof(SgOmpReductionClause), V_SgOmpReductionClause));

        if (SgOmpReductionClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpReductionClause* blkptr{reinterpret_cast<SgOmpReductionClause*>(SgOmpReductionClause::pools[blockIndex-1])};
                blkptr[SgOmpReductionClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpReductionClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpReductionClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpReductionClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpReductionClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpReductionClause::pool_size - 1};
    SgOmpReductionClause* testPointer{reinterpret_cast<SgOmpReductionClause*>(SgOmpReductionClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpReductionClause::pool_size * (SgOmpReductionClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpReductionClauseStroageClass
 * from its counterpart of SgOmpReductionClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpReductionClause::initializeStorageClassArray(SgOmpReductionClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpReductionClause::pools.begin(); block != SgOmpReductionClause::pools.end(); ++block) {
        SgOmpReductionClause* pointer{reinterpret_cast<SgOmpReductionClause*>(*block)};
        for (unsigned i = 0; i < SgOmpReductionClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 45926 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpMapClause that takes its corresponding StorageClass as parameter
 */
SgOmpMapClause::SgOmpMapClause(const SgOmpMapClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 45936 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_operation = storageSource.storageOf_operation;
    p_array_dimensions = storageSource.storageOf_array_dimensions.rebuildDataStoredInEasyStorageClass();
    p_dist_data_policies = storageSource.storageOf_dist_data_policies.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpMapClause*
SgOmpMapClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpMapClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMapClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMapClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMapClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMapClause)};
        unsigned long positionInPool{localIndex % SgOmpMapClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpMapClause::pool_size};
        returnPointer = &(((SgOmpMapClause*)(SgOmpMapClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpMapClause*
SgOmpMapClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpMapClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMapClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMapClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMapClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpMapClause)};
        unsigned long positionInPool{localIndex % SgOmpMapClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpMapClause::pool_size};
        returnPointer = &(((SgOmpMapClause*)(SgOmpMapClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpMapClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpMapClause::pools.begin(); block != SgOmpMapClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpMapClause*>(*block)};
        for (unsigned i = 0; i < SgOmpMapClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpMapClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpMapClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpMapClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpMapClause::pools.begin(); block != SgOmpMapClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpMapClause*>(*block)};
        for (unsigned i = 0; i < SgOmpMapClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpMapClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpMapClause::clearMemoryPool() {
    SgOmpMapClause* tempPointer{nullptr};
    if (SgOmpMapClause::pools.empty() == false) {
        auto block{SgOmpMapClause::pools.begin()};
        SgOmpMapClause::next_node = reinterpret_cast<SgOmpMapClause*>(*block);
        for (; block != SgOmpMapClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpMapClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpMapClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpMapClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpMapClause::pool_size-1]);
        }
    }
}

void
SgOmpMapClause::deleteMemoryPool() {
    for (auto p: SgOmpMapClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpMapClause::next_node = nullptr;
    SgOmpMapClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpMapClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMapClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMapClause)};
    for (size_t blockIndex = SgOmpMapClause::pools.size(); (blockIndex * SgOmpMapClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMapClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMapClause) = %" PRIuPTR " SgOmpMapClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMapClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMapClause),SgOmpMapClause::pool_size);
        }
#endif

        SgOmpMapClause* pointer{static_cast<SgOmpMapClause*>(ROSE_MALLOC(SgOmpMapClause::pool_size * sizeof(SgOmpMapClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpMapClause::pool_size * sizeof(SgOmpMapClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpMapClause::pool_size * sizeof(SgOmpMapClause));
#endif
        SgOmpMapClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpMapClause::pool_size * sizeof(SgOmpMapClause), V_SgOmpMapClause));

        if (SgOmpMapClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpMapClause* blkptr{reinterpret_cast<SgOmpMapClause*>(SgOmpMapClause::pools[blockIndex-1])};
                blkptr[SgOmpMapClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpMapClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpMapClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpMapClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpMapClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpMapClause::pool_size - 1};
    SgOmpMapClause* testPointer{reinterpret_cast<SgOmpMapClause*>(SgOmpMapClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpMapClause::pool_size * (SgOmpMapClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpMapClauseStroageClass
 * from its counterpart of SgOmpMapClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpMapClause::initializeStorageClassArray(SgOmpMapClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpMapClause::pools.begin(); block != SgOmpMapClause::pools.end(); ++block) {
        SgOmpMapClause* pointer{reinterpret_cast<SgOmpMapClause*>(*block)};
        for (unsigned i = 0; i < SgOmpMapClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 46156 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpUniformClause that takes its corresponding StorageClass as parameter
 */
SgOmpUniformClause::SgOmpUniformClause(const SgOmpUniformClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 46166 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpUniformClause*
SgOmpUniformClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpUniformClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpUniformClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpUniformClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpUniformClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUniformClause)};
        unsigned long positionInPool{localIndex % SgOmpUniformClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpUniformClause::pool_size};
        returnPointer = &(((SgOmpUniformClause*)(SgOmpUniformClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpUniformClause*
SgOmpUniformClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpUniformClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpUniformClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpUniformClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpUniformClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpUniformClause)};
        unsigned long positionInPool{localIndex % SgOmpUniformClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpUniformClause::pool_size};
        returnPointer = &(((SgOmpUniformClause*)(SgOmpUniformClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpUniformClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpUniformClause::pools.begin(); block != SgOmpUniformClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpUniformClause*>(*block)};
        for (unsigned i = 0; i < SgOmpUniformClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpUniformClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpUniformClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpUniformClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpUniformClause::pools.begin(); block != SgOmpUniformClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpUniformClause*>(*block)};
        for (unsigned i = 0; i < SgOmpUniformClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpUniformClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpUniformClause::clearMemoryPool() {
    SgOmpUniformClause* tempPointer{nullptr};
    if (SgOmpUniformClause::pools.empty() == false) {
        auto block{SgOmpUniformClause::pools.begin()};
        SgOmpUniformClause::next_node = reinterpret_cast<SgOmpUniformClause*>(*block);
        for (; block != SgOmpUniformClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpUniformClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpUniformClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpUniformClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpUniformClause::pool_size-1]);
        }
    }
}

void
SgOmpUniformClause::deleteMemoryPool() {
    for (auto p: SgOmpUniformClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpUniformClause::next_node = nullptr;
    SgOmpUniformClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpUniformClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUniformClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpUniformClause)};
    for (size_t blockIndex = SgOmpUniformClause::pools.size(); (blockIndex * SgOmpUniformClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUniformClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpUniformClause) = %" PRIuPTR " SgOmpUniformClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpUniformClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpUniformClause),SgOmpUniformClause::pool_size);
        }
#endif

        SgOmpUniformClause* pointer{static_cast<SgOmpUniformClause*>(ROSE_MALLOC(SgOmpUniformClause::pool_size * sizeof(SgOmpUniformClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpUniformClause::pool_size * sizeof(SgOmpUniformClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpUniformClause::pool_size * sizeof(SgOmpUniformClause));
#endif
        SgOmpUniformClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpUniformClause::pool_size * sizeof(SgOmpUniformClause), V_SgOmpUniformClause));

        if (SgOmpUniformClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpUniformClause* blkptr{reinterpret_cast<SgOmpUniformClause*>(SgOmpUniformClause::pools[blockIndex-1])};
                blkptr[SgOmpUniformClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpUniformClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpUniformClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpUniformClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpUniformClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpUniformClause::pool_size - 1};
    SgOmpUniformClause* testPointer{reinterpret_cast<SgOmpUniformClause*>(SgOmpUniformClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpUniformClause::pool_size * (SgOmpUniformClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpUniformClauseStroageClass
 * from its counterpart of SgOmpUniformClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpUniformClause::initializeStorageClassArray(SgOmpUniformClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpUniformClause::pools.begin(); block != SgOmpUniformClause::pools.end(); ++block) {
        SgOmpUniformClause* pointer{reinterpret_cast<SgOmpUniformClause*>(*block)};
        for (unsigned i = 0; i < SgOmpUniformClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 46383 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpAlignedClause that takes its corresponding StorageClass as parameter
 */
SgOmpAlignedClause::SgOmpAlignedClause(const SgOmpAlignedClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 46393 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_alignment = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_alignment));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpAlignedClause*
SgOmpAlignedClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpAlignedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAlignedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAlignedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAlignedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAlignedClause)};
        unsigned long positionInPool{localIndex % SgOmpAlignedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpAlignedClause::pool_size};
        returnPointer = &(((SgOmpAlignedClause*)(SgOmpAlignedClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpAlignedClause*
SgOmpAlignedClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpAlignedClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAlignedClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAlignedClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAlignedClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpAlignedClause)};
        unsigned long positionInPool{localIndex % SgOmpAlignedClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpAlignedClause::pool_size};
        returnPointer = &(((SgOmpAlignedClause*)(SgOmpAlignedClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpAlignedClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpAlignedClause::pools.begin(); block != SgOmpAlignedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpAlignedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpAlignedClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpAlignedClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpAlignedClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpAlignedClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpAlignedClause::pools.begin(); block != SgOmpAlignedClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpAlignedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpAlignedClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpAlignedClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpAlignedClause::clearMemoryPool() {
    SgOmpAlignedClause* tempPointer{nullptr};
    if (SgOmpAlignedClause::pools.empty() == false) {
        auto block{SgOmpAlignedClause::pools.begin()};
        SgOmpAlignedClause::next_node = reinterpret_cast<SgOmpAlignedClause*>(*block);
        for (; block != SgOmpAlignedClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpAlignedClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpAlignedClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpAlignedClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpAlignedClause::pool_size-1]);
        }
    }
}

void
SgOmpAlignedClause::deleteMemoryPool() {
    for (auto p: SgOmpAlignedClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpAlignedClause::next_node = nullptr;
    SgOmpAlignedClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpAlignedClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAlignedClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAlignedClause)};
    for (size_t blockIndex = SgOmpAlignedClause::pools.size(); (blockIndex * SgOmpAlignedClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAlignedClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAlignedClause) = %" PRIuPTR " SgOmpAlignedClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAlignedClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAlignedClause),SgOmpAlignedClause::pool_size);
        }
#endif

        SgOmpAlignedClause* pointer{static_cast<SgOmpAlignedClause*>(ROSE_MALLOC(SgOmpAlignedClause::pool_size * sizeof(SgOmpAlignedClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpAlignedClause::pool_size * sizeof(SgOmpAlignedClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpAlignedClause::pool_size * sizeof(SgOmpAlignedClause));
#endif
        SgOmpAlignedClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpAlignedClause::pool_size * sizeof(SgOmpAlignedClause), V_SgOmpAlignedClause));

        if (SgOmpAlignedClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpAlignedClause* blkptr{reinterpret_cast<SgOmpAlignedClause*>(SgOmpAlignedClause::pools[blockIndex-1])};
                blkptr[SgOmpAlignedClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpAlignedClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpAlignedClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpAlignedClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpAlignedClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpAlignedClause::pool_size - 1};
    SgOmpAlignedClause* testPointer{reinterpret_cast<SgOmpAlignedClause*>(SgOmpAlignedClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpAlignedClause::pool_size * (SgOmpAlignedClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpAlignedClauseStroageClass
 * from its counterpart of SgOmpAlignedClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpAlignedClause::initializeStorageClassArray(SgOmpAlignedClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpAlignedClause::pools.begin(); block != SgOmpAlignedClause::pools.end(); ++block) {
        SgOmpAlignedClause* pointer{reinterpret_cast<SgOmpAlignedClause*>(*block)};
        for (unsigned i = 0; i < SgOmpAlignedClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 46611 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpLinearClause that takes its corresponding StorageClass as parameter
 */
SgOmpLinearClause::SgOmpLinearClause(const SgOmpLinearClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 46621 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_step = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_step));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpLinearClause*
SgOmpLinearClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpLinearClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpLinearClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpLinearClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpLinearClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLinearClause)};
        unsigned long positionInPool{localIndex % SgOmpLinearClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpLinearClause::pool_size};
        returnPointer = &(((SgOmpLinearClause*)(SgOmpLinearClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpLinearClause*
SgOmpLinearClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpLinearClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpLinearClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpLinearClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpLinearClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpLinearClause)};
        unsigned long positionInPool{localIndex % SgOmpLinearClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpLinearClause::pool_size};
        returnPointer = &(((SgOmpLinearClause*)(SgOmpLinearClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpLinearClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpLinearClause::pools.begin(); block != SgOmpLinearClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpLinearClause*>(*block)};
        for (unsigned i = 0; i < SgOmpLinearClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpLinearClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpLinearClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpLinearClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpLinearClause::pools.begin(); block != SgOmpLinearClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpLinearClause*>(*block)};
        for (unsigned i = 0; i < SgOmpLinearClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpLinearClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpLinearClause::clearMemoryPool() {
    SgOmpLinearClause* tempPointer{nullptr};
    if (SgOmpLinearClause::pools.empty() == false) {
        auto block{SgOmpLinearClause::pools.begin()};
        SgOmpLinearClause::next_node = reinterpret_cast<SgOmpLinearClause*>(*block);
        for (; block != SgOmpLinearClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpLinearClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpLinearClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpLinearClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpLinearClause::pool_size-1]);
        }
    }
}

void
SgOmpLinearClause::deleteMemoryPool() {
    for (auto p: SgOmpLinearClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpLinearClause::next_node = nullptr;
    SgOmpLinearClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpLinearClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLinearClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpLinearClause)};
    for (size_t blockIndex = SgOmpLinearClause::pools.size(); (blockIndex * SgOmpLinearClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLinearClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpLinearClause) = %" PRIuPTR " SgOmpLinearClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpLinearClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpLinearClause),SgOmpLinearClause::pool_size);
        }
#endif

        SgOmpLinearClause* pointer{static_cast<SgOmpLinearClause*>(ROSE_MALLOC(SgOmpLinearClause::pool_size * sizeof(SgOmpLinearClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpLinearClause::pool_size * sizeof(SgOmpLinearClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpLinearClause::pool_size * sizeof(SgOmpLinearClause));
#endif
        SgOmpLinearClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpLinearClause::pool_size * sizeof(SgOmpLinearClause), V_SgOmpLinearClause));

        if (SgOmpLinearClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpLinearClause* blkptr{reinterpret_cast<SgOmpLinearClause*>(SgOmpLinearClause::pools[blockIndex-1])};
                blkptr[SgOmpLinearClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpLinearClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpLinearClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpLinearClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpLinearClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpLinearClause::pool_size - 1};
    SgOmpLinearClause* testPointer{reinterpret_cast<SgOmpLinearClause*>(SgOmpLinearClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpLinearClause::pool_size * (SgOmpLinearClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpLinearClauseStroageClass
 * from its counterpart of SgOmpLinearClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpLinearClause::initializeStorageClassArray(SgOmpLinearClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpLinearClause::pools.begin(); block != SgOmpLinearClause::pools.end(); ++block) {
        SgOmpLinearClause* pointer{reinterpret_cast<SgOmpLinearClause*>(*block)};
        for (unsigned i = 0; i < SgOmpLinearClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 46839 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpDependClause that takes its corresponding StorageClass as parameter
 */
SgOmpDependClause::SgOmpDependClause(const SgOmpDependClauseStorageClass& storageSource)  : SgOmpVariablesClause (storageSource) {

/* #line 46849 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_dependence_type = storageSource.storageOf_dependence_type;
    p_array_dimensions = storageSource.storageOf_array_dimensions.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpDependClause*
SgOmpDependClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpDependClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDependClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDependClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDependClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDependClause)};
        unsigned long positionInPool{localIndex % SgOmpDependClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDependClause::pool_size};
        returnPointer = &(((SgOmpDependClause*)(SgOmpDependClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpDependClause*
SgOmpDependClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpDependClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDependClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDependClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDependClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpDependClause)};
        unsigned long positionInPool{localIndex % SgOmpDependClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDependClause::pool_size};
        returnPointer = &(((SgOmpDependClause*)(SgOmpDependClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpDependClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpDependClause::pools.begin(); block != SgOmpDependClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDependClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDependClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpDependClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpDependClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpDependClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpDependClause::pools.begin(); block != SgOmpDependClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDependClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDependClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpDependClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpDependClause::clearMemoryPool() {
    SgOmpDependClause* tempPointer{nullptr};
    if (SgOmpDependClause::pools.empty() == false) {
        auto block{SgOmpDependClause::pools.begin()};
        SgOmpDependClause::next_node = reinterpret_cast<SgOmpDependClause*>(*block);
        for (; block != SgOmpDependClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpDependClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpDependClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpDependClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpDependClause::pool_size-1]);
        }
    }
}

void
SgOmpDependClause::deleteMemoryPool() {
    for (auto p: SgOmpDependClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpDependClause::next_node = nullptr;
    SgOmpDependClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpDependClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDependClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDependClause)};
    for (size_t blockIndex = SgOmpDependClause::pools.size(); (blockIndex * SgOmpDependClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDependClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDependClause) = %" PRIuPTR " SgOmpDependClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDependClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDependClause),SgOmpDependClause::pool_size);
        }
#endif

        SgOmpDependClause* pointer{static_cast<SgOmpDependClause*>(ROSE_MALLOC(SgOmpDependClause::pool_size * sizeof(SgOmpDependClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpDependClause::pool_size * sizeof(SgOmpDependClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpDependClause::pool_size * sizeof(SgOmpDependClause));
#endif
        SgOmpDependClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpDependClause::pool_size * sizeof(SgOmpDependClause), V_SgOmpDependClause));

        if (SgOmpDependClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpDependClause* blkptr{reinterpret_cast<SgOmpDependClause*>(SgOmpDependClause::pools[blockIndex-1])};
                blkptr[SgOmpDependClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpDependClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpDependClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpDependClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpDependClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpDependClause::pool_size - 1};
    SgOmpDependClause* testPointer{reinterpret_cast<SgOmpDependClause*>(SgOmpDependClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpDependClause::pool_size * (SgOmpDependClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpDependClauseStroageClass
 * from its counterpart of SgOmpDependClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpDependClause::initializeStorageClassArray(SgOmpDependClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpDependClause::pools.begin(); block != SgOmpDependClause::pools.end(); ++block) {
        SgOmpDependClause* pointer{reinterpret_cast<SgOmpDependClause*>(*block)};
        for (unsigned i = 0; i < SgOmpDependClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 47068 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpScheduleClause that takes its corresponding StorageClass as parameter
 */
SgOmpScheduleClause::SgOmpScheduleClause(const SgOmpScheduleClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 47078 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;
     p_chunk_size = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_chunk_size));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpScheduleClause*
SgOmpScheduleClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpScheduleClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpScheduleClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpScheduleClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpScheduleClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpScheduleClause)};
        unsigned long positionInPool{localIndex % SgOmpScheduleClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpScheduleClause::pool_size};
        returnPointer = &(((SgOmpScheduleClause*)(SgOmpScheduleClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpScheduleClause*
SgOmpScheduleClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpScheduleClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpScheduleClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpScheduleClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpScheduleClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpScheduleClause)};
        unsigned long positionInPool{localIndex % SgOmpScheduleClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpScheduleClause::pool_size};
        returnPointer = &(((SgOmpScheduleClause*)(SgOmpScheduleClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpScheduleClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpScheduleClause::pools.begin(); block != SgOmpScheduleClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpScheduleClause*>(*block)};
        for (unsigned i = 0; i < SgOmpScheduleClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpScheduleClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpScheduleClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpScheduleClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpScheduleClause::pools.begin(); block != SgOmpScheduleClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpScheduleClause*>(*block)};
        for (unsigned i = 0; i < SgOmpScheduleClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpScheduleClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpScheduleClause::clearMemoryPool() {
    SgOmpScheduleClause* tempPointer{nullptr};
    if (SgOmpScheduleClause::pools.empty() == false) {
        auto block{SgOmpScheduleClause::pools.begin()};
        SgOmpScheduleClause::next_node = reinterpret_cast<SgOmpScheduleClause*>(*block);
        for (; block != SgOmpScheduleClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpScheduleClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpScheduleClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpScheduleClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpScheduleClause::pool_size-1]);
        }
    }
}

void
SgOmpScheduleClause::deleteMemoryPool() {
    for (auto p: SgOmpScheduleClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpScheduleClause::next_node = nullptr;
    SgOmpScheduleClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpScheduleClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpScheduleClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpScheduleClause)};
    for (size_t blockIndex = SgOmpScheduleClause::pools.size(); (blockIndex * SgOmpScheduleClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpScheduleClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpScheduleClause) = %" PRIuPTR " SgOmpScheduleClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpScheduleClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpScheduleClause),SgOmpScheduleClause::pool_size);
        }
#endif

        SgOmpScheduleClause* pointer{static_cast<SgOmpScheduleClause*>(ROSE_MALLOC(SgOmpScheduleClause::pool_size * sizeof(SgOmpScheduleClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpScheduleClause::pool_size * sizeof(SgOmpScheduleClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpScheduleClause::pool_size * sizeof(SgOmpScheduleClause));
#endif
        SgOmpScheduleClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpScheduleClause::pool_size * sizeof(SgOmpScheduleClause), V_SgOmpScheduleClause));

        if (SgOmpScheduleClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpScheduleClause* blkptr{reinterpret_cast<SgOmpScheduleClause*>(SgOmpScheduleClause::pools[blockIndex-1])};
                blkptr[SgOmpScheduleClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpScheduleClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpScheduleClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpScheduleClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpScheduleClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpScheduleClause::pool_size - 1};
    SgOmpScheduleClause* testPointer{reinterpret_cast<SgOmpScheduleClause*>(SgOmpScheduleClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpScheduleClause::pool_size * (SgOmpScheduleClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpScheduleClauseStroageClass
 * from its counterpart of SgOmpScheduleClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpScheduleClause::initializeStorageClassArray(SgOmpScheduleClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpScheduleClause::pools.begin(); block != SgOmpScheduleClause::pools.end(); ++block) {
        SgOmpScheduleClause* pointer{reinterpret_cast<SgOmpScheduleClause*>(*block)};
        for (unsigned i = 0; i < SgOmpScheduleClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 47297 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpMergeableClause that takes its corresponding StorageClass as parameter
 */
SgOmpMergeableClause::SgOmpMergeableClause(const SgOmpMergeableClauseStorageClass& storageSource)  : SgOmpClause (storageSource) {

/* #line 47307 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpMergeableClause*
SgOmpMergeableClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpMergeableClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMergeableClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMergeableClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMergeableClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMergeableClause)};
        unsigned long positionInPool{localIndex % SgOmpMergeableClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpMergeableClause::pool_size};
        returnPointer = &(((SgOmpMergeableClause*)(SgOmpMergeableClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpMergeableClause*
SgOmpMergeableClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpMergeableClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMergeableClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMergeableClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMergeableClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpMergeableClause)};
        unsigned long positionInPool{localIndex % SgOmpMergeableClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpMergeableClause::pool_size};
        returnPointer = &(((SgOmpMergeableClause*)(SgOmpMergeableClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpMergeableClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpMergeableClause::pools.begin(); block != SgOmpMergeableClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpMergeableClause*>(*block)};
        for (unsigned i = 0; i < SgOmpMergeableClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpMergeableClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpMergeableClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpMergeableClause* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpMergeableClause::pools.begin(); block != SgOmpMergeableClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpMergeableClause*>(*block)};
        for (unsigned i = 0; i < SgOmpMergeableClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpMergeableClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpMergeableClause::clearMemoryPool() {
    SgOmpMergeableClause* tempPointer{nullptr};
    if (SgOmpMergeableClause::pools.empty() == false) {
        auto block{SgOmpMergeableClause::pools.begin()};
        SgOmpMergeableClause::next_node = reinterpret_cast<SgOmpMergeableClause*>(*block);
        for (; block != SgOmpMergeableClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpMergeableClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpMergeableClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpMergeableClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpMergeableClause::pool_size-1]);
        }
    }
}

void
SgOmpMergeableClause::deleteMemoryPool() {
    for (auto p: SgOmpMergeableClause::pools) {
        ROSE_FREE(p);
    }
    SgOmpMergeableClause::next_node = nullptr;
    SgOmpMergeableClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpMergeableClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMergeableClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMergeableClause)};
    for (size_t blockIndex = SgOmpMergeableClause::pools.size(); (blockIndex * SgOmpMergeableClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMergeableClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMergeableClause) = %" PRIuPTR " SgOmpMergeableClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMergeableClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMergeableClause),SgOmpMergeableClause::pool_size);
        }
#endif

        SgOmpMergeableClause* pointer{static_cast<SgOmpMergeableClause*>(ROSE_MALLOC(SgOmpMergeableClause::pool_size * sizeof(SgOmpMergeableClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpMergeableClause::pool_size * sizeof(SgOmpMergeableClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpMergeableClause::pool_size * sizeof(SgOmpMergeableClause));
#endif
        SgOmpMergeableClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpMergeableClause::pool_size * sizeof(SgOmpMergeableClause), V_SgOmpMergeableClause));

        if (SgOmpMergeableClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpMergeableClause* blkptr{reinterpret_cast<SgOmpMergeableClause*>(SgOmpMergeableClause::pools[blockIndex-1])};
                blkptr[SgOmpMergeableClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpMergeableClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpMergeableClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpMergeableClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpMergeableClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpMergeableClause::pool_size - 1};
    SgOmpMergeableClause* testPointer{reinterpret_cast<SgOmpMergeableClause*>(SgOmpMergeableClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpMergeableClause::pool_size * (SgOmpMergeableClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpMergeableClauseStroageClass
 * from its counterpart of SgOmpMergeableClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpMergeableClause::initializeStorageClassArray(SgOmpMergeableClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpMergeableClause::pools.begin(); block != SgOmpMergeableClause::pools.end(); ++block) {
        SgOmpMergeableClause* pointer{reinterpret_cast<SgOmpMergeableClause*>(*block)};
        for (unsigned i = 0; i < SgOmpMergeableClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 47524 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLambdaCapture that takes its corresponding StorageClass as parameter
 */
SgLambdaCapture::SgLambdaCapture(const SgLambdaCaptureStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 47534 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_capture_variable = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_capture_variable));
     p_source_closure_variable = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_source_closure_variable));
     p_closure_variable = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_closure_variable));
    p_capture_by_reference = storageSource.storageOf_capture_by_reference;
    p_implicit = storageSource.storageOf_implicit;
    p_pack_expansion = storageSource.storageOf_pack_expansion;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLambdaCapture*
SgLambdaCapture::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLambdaCapture* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaCapture)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaCapture + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaCapture)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCapture)};
        unsigned long positionInPool{localIndex % SgLambdaCapture::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaCapture::pool_size};
        returnPointer = &(((SgLambdaCapture*)(SgLambdaCapture::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLambdaCapture*
SgLambdaCapture::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLambdaCapture* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaCapture)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaCapture + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaCapture)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLambdaCapture)};
        unsigned long positionInPool{localIndex % SgLambdaCapture::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaCapture::pool_size};
        returnPointer = &(((SgLambdaCapture*)(SgLambdaCapture::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLambdaCapture::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLambdaCapture::pools.begin(); block != SgLambdaCapture::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaCapture*>(*block)};
        for (unsigned i = 0; i < SgLambdaCapture::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLambdaCapture*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLambdaCapture::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLambdaCapture* pointerOfLinkedList{nullptr};
    for (auto block = SgLambdaCapture::pools.begin(); block != SgLambdaCapture::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaCapture*>(*block)};
        for (unsigned i = 0; i < SgLambdaCapture::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLambdaCapture::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLambdaCapture::clearMemoryPool() {
    SgLambdaCapture* tempPointer{nullptr};
    if (SgLambdaCapture::pools.empty() == false) {
        auto block{SgLambdaCapture::pools.begin()};
        SgLambdaCapture::next_node = reinterpret_cast<SgLambdaCapture*>(*block);
        for (; block != SgLambdaCapture::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLambdaCapture*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLambdaCapture::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLambdaCapture::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLambdaCapture::pool_size-1]);
        }
    }
}

void
SgLambdaCapture::deleteMemoryPool() {
    for (auto p: SgLambdaCapture::pools) {
        ROSE_FREE(p);
    }
    SgLambdaCapture::next_node = nullptr;
    SgLambdaCapture::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLambdaCapture::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCapture) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaCapture)};
    for (size_t blockIndex = SgLambdaCapture::pools.size(); (blockIndex * SgLambdaCapture::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCapture) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaCapture) = %" PRIuPTR " SgLambdaCapture::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCapture),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaCapture),SgLambdaCapture::pool_size);
        }
#endif

        SgLambdaCapture* pointer{static_cast<SgLambdaCapture*>(ROSE_MALLOC(SgLambdaCapture::pool_size * sizeof(SgLambdaCapture)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLambdaCapture::pool_size * sizeof(SgLambdaCapture));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLambdaCapture::pool_size * sizeof(SgLambdaCapture));
#endif
        SgLambdaCapture::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLambdaCapture::pool_size * sizeof(SgLambdaCapture), V_SgLambdaCapture));

        if (SgLambdaCapture::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLambdaCapture* blkptr{reinterpret_cast<SgLambdaCapture*>(SgLambdaCapture::pools[blockIndex-1])};
                blkptr[SgLambdaCapture::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLambdaCapture::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLambdaCapture::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLambdaCapture::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLambdaCapture::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLambdaCapture::pool_size - 1};
    SgLambdaCapture* testPointer{reinterpret_cast<SgLambdaCapture*>(SgLambdaCapture::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLambdaCapture::pool_size * (SgLambdaCapture::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLambdaCaptureStroageClass
 * from its counterpart of SgLambdaCapture. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLambdaCapture::initializeStorageClassArray(SgLambdaCaptureStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLambdaCapture::pools.begin(); block != SgLambdaCapture::pools.end(); ++block) {
        SgLambdaCapture* pointer{reinterpret_cast<SgLambdaCapture*>(*block)};
        for (unsigned i = 0; i < SgLambdaCapture::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 47757 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLambdaCaptureList that takes its corresponding StorageClass as parameter
 */
SgLambdaCaptureList::SgLambdaCaptureList(const SgLambdaCaptureListStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 47767 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_capture_list = storageSource.storageOf_capture_list.rebuildDataStoredInEasyStorageClass();
    SgLambdaCapturePtrList::iterator i_capture_list = p_capture_list.begin();
    for ( ; i_capture_list != p_capture_list.end(); ++i_capture_list) {
        (*i_capture_list) = (SgLambdaCapturePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_capture_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLambdaCaptureList*
SgLambdaCaptureList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLambdaCaptureList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaCaptureList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaCaptureList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaCaptureList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCaptureList)};
        unsigned long positionInPool{localIndex % SgLambdaCaptureList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaCaptureList::pool_size};
        returnPointer = &(((SgLambdaCaptureList*)(SgLambdaCaptureList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLambdaCaptureList*
SgLambdaCaptureList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLambdaCaptureList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaCaptureList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaCaptureList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaCaptureList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLambdaCaptureList)};
        unsigned long positionInPool{localIndex % SgLambdaCaptureList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaCaptureList::pool_size};
        returnPointer = &(((SgLambdaCaptureList*)(SgLambdaCaptureList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLambdaCaptureList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLambdaCaptureList::pools.begin(); block != SgLambdaCaptureList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaCaptureList*>(*block)};
        for (unsigned i = 0; i < SgLambdaCaptureList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLambdaCaptureList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLambdaCaptureList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLambdaCaptureList* pointerOfLinkedList{nullptr};
    for (auto block = SgLambdaCaptureList::pools.begin(); block != SgLambdaCaptureList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaCaptureList*>(*block)};
        for (unsigned i = 0; i < SgLambdaCaptureList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLambdaCaptureList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLambdaCaptureList::clearMemoryPool() {
    SgLambdaCaptureList* tempPointer{nullptr};
    if (SgLambdaCaptureList::pools.empty() == false) {
        auto block{SgLambdaCaptureList::pools.begin()};
        SgLambdaCaptureList::next_node = reinterpret_cast<SgLambdaCaptureList*>(*block);
        for (; block != SgLambdaCaptureList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLambdaCaptureList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLambdaCaptureList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLambdaCaptureList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLambdaCaptureList::pool_size-1]);
        }
    }
}

void
SgLambdaCaptureList::deleteMemoryPool() {
    for (auto p: SgLambdaCaptureList::pools) {
        ROSE_FREE(p);
    }
    SgLambdaCaptureList::next_node = nullptr;
    SgLambdaCaptureList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLambdaCaptureList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCaptureList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaCaptureList)};
    for (size_t blockIndex = SgLambdaCaptureList::pools.size(); (blockIndex * SgLambdaCaptureList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCaptureList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaCaptureList) = %" PRIuPTR " SgLambdaCaptureList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaCaptureList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaCaptureList),SgLambdaCaptureList::pool_size);
        }
#endif

        SgLambdaCaptureList* pointer{static_cast<SgLambdaCaptureList*>(ROSE_MALLOC(SgLambdaCaptureList::pool_size * sizeof(SgLambdaCaptureList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLambdaCaptureList::pool_size * sizeof(SgLambdaCaptureList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLambdaCaptureList::pool_size * sizeof(SgLambdaCaptureList));
#endif
        SgLambdaCaptureList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLambdaCaptureList::pool_size * sizeof(SgLambdaCaptureList), V_SgLambdaCaptureList));

        if (SgLambdaCaptureList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLambdaCaptureList* blkptr{reinterpret_cast<SgLambdaCaptureList*>(SgLambdaCaptureList::pools[blockIndex-1])};
                blkptr[SgLambdaCaptureList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLambdaCaptureList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLambdaCaptureList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLambdaCaptureList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLambdaCaptureList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLambdaCaptureList::pool_size - 1};
    SgLambdaCaptureList* testPointer{reinterpret_cast<SgLambdaCaptureList*>(SgLambdaCaptureList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLambdaCaptureList::pool_size * (SgLambdaCaptureList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLambdaCaptureListStroageClass
 * from its counterpart of SgLambdaCaptureList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLambdaCaptureList::initializeStorageClassArray(SgLambdaCaptureListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLambdaCaptureList::pools.begin(); block != SgLambdaCaptureList::pools.end(); ++block) {
        SgLambdaCaptureList* pointer{reinterpret_cast<SgLambdaCaptureList*>(*block)};
        for (unsigned i = 0; i < SgLambdaCaptureList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 47989 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTypeConstraint that takes its corresponding StorageClass as parameter
 */
SgAdaTypeConstraint::SgAdaTypeConstraint(const SgAdaTypeConstraintStorageClass& storageSource)  : SgLocatedNodeSupport (storageSource) {

/* #line 47999 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTypeConstraint*
SgAdaTypeConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTypeConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTypeConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTypeConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTypeConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTypeConstraint)};
        unsigned long positionInPool{localIndex % SgAdaTypeConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTypeConstraint::pool_size};
        returnPointer = &(((SgAdaTypeConstraint*)(SgAdaTypeConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTypeConstraint*
SgAdaTypeConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTypeConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTypeConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTypeConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTypeConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTypeConstraint)};
        unsigned long positionInPool{localIndex % SgAdaTypeConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTypeConstraint::pool_size};
        returnPointer = &(((SgAdaTypeConstraint*)(SgAdaTypeConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTypeConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTypeConstraint::pools.begin(); block != SgAdaTypeConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTypeConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaTypeConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTypeConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTypeConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTypeConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTypeConstraint::pools.begin(); block != SgAdaTypeConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTypeConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaTypeConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTypeConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTypeConstraint::clearMemoryPool() {
    SgAdaTypeConstraint* tempPointer{nullptr};
    if (SgAdaTypeConstraint::pools.empty() == false) {
        auto block{SgAdaTypeConstraint::pools.begin()};
        SgAdaTypeConstraint::next_node = reinterpret_cast<SgAdaTypeConstraint*>(*block);
        for (; block != SgAdaTypeConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTypeConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTypeConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTypeConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTypeConstraint::pool_size-1]);
        }
    }
}

void
SgAdaTypeConstraint::deleteMemoryPool() {
    for (auto p: SgAdaTypeConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAdaTypeConstraint::next_node = nullptr;
    SgAdaTypeConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTypeConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTypeConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTypeConstraint)};
    for (size_t blockIndex = SgAdaTypeConstraint::pools.size(); (blockIndex * SgAdaTypeConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTypeConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTypeConstraint) = %" PRIuPTR " SgAdaTypeConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTypeConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTypeConstraint),SgAdaTypeConstraint::pool_size);
        }
#endif

        SgAdaTypeConstraint* pointer{static_cast<SgAdaTypeConstraint*>(ROSE_MALLOC(SgAdaTypeConstraint::pool_size * sizeof(SgAdaTypeConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTypeConstraint::pool_size * sizeof(SgAdaTypeConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTypeConstraint::pool_size * sizeof(SgAdaTypeConstraint));
#endif
        SgAdaTypeConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTypeConstraint::pool_size * sizeof(SgAdaTypeConstraint), V_SgAdaTypeConstraint));

        if (SgAdaTypeConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTypeConstraint* blkptr{reinterpret_cast<SgAdaTypeConstraint*>(SgAdaTypeConstraint::pools[blockIndex-1])};
                blkptr[SgAdaTypeConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTypeConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTypeConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTypeConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTypeConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTypeConstraint::pool_size - 1};
    SgAdaTypeConstraint* testPointer{reinterpret_cast<SgAdaTypeConstraint*>(SgAdaTypeConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTypeConstraint::pool_size * (SgAdaTypeConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTypeConstraintStroageClass
 * from its counterpart of SgAdaTypeConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTypeConstraint::initializeStorageClassArray(SgAdaTypeConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTypeConstraint::pools.begin(); block != SgAdaTypeConstraint::pools.end(); ++block) {
        SgAdaTypeConstraint* pointer{reinterpret_cast<SgAdaTypeConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaTypeConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 48216 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaRangeConstraint that takes its corresponding StorageClass as parameter
 */
SgAdaRangeConstraint::SgAdaRangeConstraint(const SgAdaRangeConstraintStorageClass& storageSource)  : SgAdaTypeConstraint (storageSource) {

/* #line 48226 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_range = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_range));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaRangeConstraint*
SgAdaRangeConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaRangeConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRangeConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRangeConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRangeConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRangeConstraint)};
        unsigned long positionInPool{localIndex % SgAdaRangeConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRangeConstraint::pool_size};
        returnPointer = &(((SgAdaRangeConstraint*)(SgAdaRangeConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaRangeConstraint*
SgAdaRangeConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaRangeConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRangeConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRangeConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRangeConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaRangeConstraint)};
        unsigned long positionInPool{localIndex % SgAdaRangeConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRangeConstraint::pool_size};
        returnPointer = &(((SgAdaRangeConstraint*)(SgAdaRangeConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaRangeConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaRangeConstraint::pools.begin(); block != SgAdaRangeConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRangeConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaRangeConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaRangeConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaRangeConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaRangeConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaRangeConstraint::pools.begin(); block != SgAdaRangeConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRangeConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaRangeConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaRangeConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaRangeConstraint::clearMemoryPool() {
    SgAdaRangeConstraint* tempPointer{nullptr};
    if (SgAdaRangeConstraint::pools.empty() == false) {
        auto block{SgAdaRangeConstraint::pools.begin()};
        SgAdaRangeConstraint::next_node = reinterpret_cast<SgAdaRangeConstraint*>(*block);
        for (; block != SgAdaRangeConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaRangeConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaRangeConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaRangeConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaRangeConstraint::pool_size-1]);
        }
    }
}

void
SgAdaRangeConstraint::deleteMemoryPool() {
    for (auto p: SgAdaRangeConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAdaRangeConstraint::next_node = nullptr;
    SgAdaRangeConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaRangeConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRangeConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRangeConstraint)};
    for (size_t blockIndex = SgAdaRangeConstraint::pools.size(); (blockIndex * SgAdaRangeConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRangeConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRangeConstraint) = %" PRIuPTR " SgAdaRangeConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRangeConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRangeConstraint),SgAdaRangeConstraint::pool_size);
        }
#endif

        SgAdaRangeConstraint* pointer{static_cast<SgAdaRangeConstraint*>(ROSE_MALLOC(SgAdaRangeConstraint::pool_size * sizeof(SgAdaRangeConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaRangeConstraint::pool_size * sizeof(SgAdaRangeConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaRangeConstraint::pool_size * sizeof(SgAdaRangeConstraint));
#endif
        SgAdaRangeConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaRangeConstraint::pool_size * sizeof(SgAdaRangeConstraint), V_SgAdaRangeConstraint));

        if (SgAdaRangeConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaRangeConstraint* blkptr{reinterpret_cast<SgAdaRangeConstraint*>(SgAdaRangeConstraint::pools[blockIndex-1])};
                blkptr[SgAdaRangeConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaRangeConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaRangeConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaRangeConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaRangeConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaRangeConstraint::pool_size - 1};
    SgAdaRangeConstraint* testPointer{reinterpret_cast<SgAdaRangeConstraint*>(SgAdaRangeConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaRangeConstraint::pool_size * (SgAdaRangeConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaRangeConstraintStroageClass
 * from its counterpart of SgAdaRangeConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaRangeConstraint::initializeStorageClassArray(SgAdaRangeConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaRangeConstraint::pools.begin(); block != SgAdaRangeConstraint::pools.end(); ++block) {
        SgAdaRangeConstraint* pointer{reinterpret_cast<SgAdaRangeConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaRangeConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 48444 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaIndexConstraint that takes its corresponding StorageClass as parameter
 */
SgAdaIndexConstraint::SgAdaIndexConstraint(const SgAdaIndexConstraintStorageClass& storageSource)  : SgAdaTypeConstraint (storageSource) {

/* #line 48454 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_indexRanges = storageSource.storageOf_indexRanges.rebuildDataStoredInEasyStorageClass();
    SgExpressionPtrList::iterator i_indexRanges = p_indexRanges.begin();
    for ( ; i_indexRanges != p_indexRanges.end(); ++i_indexRanges) {
        (*i_indexRanges) = (SgExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_indexRanges)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaIndexConstraint*
SgAdaIndexConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaIndexConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaIndexConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaIndexConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaIndexConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaIndexConstraint)};
        unsigned long positionInPool{localIndex % SgAdaIndexConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaIndexConstraint::pool_size};
        returnPointer = &(((SgAdaIndexConstraint*)(SgAdaIndexConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaIndexConstraint*
SgAdaIndexConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaIndexConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaIndexConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaIndexConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaIndexConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaIndexConstraint)};
        unsigned long positionInPool{localIndex % SgAdaIndexConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaIndexConstraint::pool_size};
        returnPointer = &(((SgAdaIndexConstraint*)(SgAdaIndexConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaIndexConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaIndexConstraint::pools.begin(); block != SgAdaIndexConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaIndexConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaIndexConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaIndexConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaIndexConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaIndexConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaIndexConstraint::pools.begin(); block != SgAdaIndexConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaIndexConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaIndexConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaIndexConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaIndexConstraint::clearMemoryPool() {
    SgAdaIndexConstraint* tempPointer{nullptr};
    if (SgAdaIndexConstraint::pools.empty() == false) {
        auto block{SgAdaIndexConstraint::pools.begin()};
        SgAdaIndexConstraint::next_node = reinterpret_cast<SgAdaIndexConstraint*>(*block);
        for (; block != SgAdaIndexConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaIndexConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaIndexConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaIndexConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaIndexConstraint::pool_size-1]);
        }
    }
}

void
SgAdaIndexConstraint::deleteMemoryPool() {
    for (auto p: SgAdaIndexConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAdaIndexConstraint::next_node = nullptr;
    SgAdaIndexConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaIndexConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaIndexConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaIndexConstraint)};
    for (size_t blockIndex = SgAdaIndexConstraint::pools.size(); (blockIndex * SgAdaIndexConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaIndexConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaIndexConstraint) = %" PRIuPTR " SgAdaIndexConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaIndexConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaIndexConstraint),SgAdaIndexConstraint::pool_size);
        }
#endif

        SgAdaIndexConstraint* pointer{static_cast<SgAdaIndexConstraint*>(ROSE_MALLOC(SgAdaIndexConstraint::pool_size * sizeof(SgAdaIndexConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaIndexConstraint::pool_size * sizeof(SgAdaIndexConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaIndexConstraint::pool_size * sizeof(SgAdaIndexConstraint));
#endif
        SgAdaIndexConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaIndexConstraint::pool_size * sizeof(SgAdaIndexConstraint), V_SgAdaIndexConstraint));

        if (SgAdaIndexConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaIndexConstraint* blkptr{reinterpret_cast<SgAdaIndexConstraint*>(SgAdaIndexConstraint::pools[blockIndex-1])};
                blkptr[SgAdaIndexConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaIndexConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaIndexConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaIndexConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaIndexConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaIndexConstraint::pool_size - 1};
    SgAdaIndexConstraint* testPointer{reinterpret_cast<SgAdaIndexConstraint*>(SgAdaIndexConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaIndexConstraint::pool_size * (SgAdaIndexConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaIndexConstraintStroageClass
 * from its counterpart of SgAdaIndexConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaIndexConstraint::initializeStorageClassArray(SgAdaIndexConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaIndexConstraint::pools.begin(); block != SgAdaIndexConstraint::pools.end(); ++block) {
        SgAdaIndexConstraint* pointer{reinterpret_cast<SgAdaIndexConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaIndexConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 48676 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDigitsConstraint that takes its corresponding StorageClass as parameter
 */
SgAdaDigitsConstraint::SgAdaDigitsConstraint(const SgAdaDigitsConstraintStorageClass& storageSource)  : SgAdaTypeConstraint (storageSource) {

/* #line 48686 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_digits = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_digits));
     p_subConstraint = (SgAdaTypeConstraint*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_subConstraint));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDigitsConstraint*
SgAdaDigitsConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDigitsConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDigitsConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDigitsConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDigitsConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDigitsConstraint)};
        unsigned long positionInPool{localIndex % SgAdaDigitsConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDigitsConstraint::pool_size};
        returnPointer = &(((SgAdaDigitsConstraint*)(SgAdaDigitsConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDigitsConstraint*
SgAdaDigitsConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDigitsConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDigitsConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDigitsConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDigitsConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDigitsConstraint)};
        unsigned long positionInPool{localIndex % SgAdaDigitsConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDigitsConstraint::pool_size};
        returnPointer = &(((SgAdaDigitsConstraint*)(SgAdaDigitsConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDigitsConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDigitsConstraint::pools.begin(); block != SgAdaDigitsConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDigitsConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDigitsConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDigitsConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDigitsConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDigitsConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDigitsConstraint::pools.begin(); block != SgAdaDigitsConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDigitsConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDigitsConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDigitsConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDigitsConstraint::clearMemoryPool() {
    SgAdaDigitsConstraint* tempPointer{nullptr};
    if (SgAdaDigitsConstraint::pools.empty() == false) {
        auto block{SgAdaDigitsConstraint::pools.begin()};
        SgAdaDigitsConstraint::next_node = reinterpret_cast<SgAdaDigitsConstraint*>(*block);
        for (; block != SgAdaDigitsConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDigitsConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDigitsConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDigitsConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDigitsConstraint::pool_size-1]);
        }
    }
}

void
SgAdaDigitsConstraint::deleteMemoryPool() {
    for (auto p: SgAdaDigitsConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAdaDigitsConstraint::next_node = nullptr;
    SgAdaDigitsConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDigitsConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDigitsConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDigitsConstraint)};
    for (size_t blockIndex = SgAdaDigitsConstraint::pools.size(); (blockIndex * SgAdaDigitsConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDigitsConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDigitsConstraint) = %" PRIuPTR " SgAdaDigitsConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDigitsConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDigitsConstraint),SgAdaDigitsConstraint::pool_size);
        }
#endif

        SgAdaDigitsConstraint* pointer{static_cast<SgAdaDigitsConstraint*>(ROSE_MALLOC(SgAdaDigitsConstraint::pool_size * sizeof(SgAdaDigitsConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDigitsConstraint::pool_size * sizeof(SgAdaDigitsConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDigitsConstraint::pool_size * sizeof(SgAdaDigitsConstraint));
#endif
        SgAdaDigitsConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDigitsConstraint::pool_size * sizeof(SgAdaDigitsConstraint), V_SgAdaDigitsConstraint));

        if (SgAdaDigitsConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDigitsConstraint* blkptr{reinterpret_cast<SgAdaDigitsConstraint*>(SgAdaDigitsConstraint::pools[blockIndex-1])};
                blkptr[SgAdaDigitsConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDigitsConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDigitsConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDigitsConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDigitsConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDigitsConstraint::pool_size - 1};
    SgAdaDigitsConstraint* testPointer{reinterpret_cast<SgAdaDigitsConstraint*>(SgAdaDigitsConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDigitsConstraint::pool_size * (SgAdaDigitsConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDigitsConstraintStroageClass
 * from its counterpart of SgAdaDigitsConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDigitsConstraint::initializeStorageClassArray(SgAdaDigitsConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDigitsConstraint::pools.begin(); block != SgAdaDigitsConstraint::pools.end(); ++block) {
        SgAdaDigitsConstraint* pointer{reinterpret_cast<SgAdaDigitsConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDigitsConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 48905 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDeltaConstraint that takes its corresponding StorageClass as parameter
 */
SgAdaDeltaConstraint::SgAdaDeltaConstraint(const SgAdaDeltaConstraintStorageClass& storageSource)  : SgAdaTypeConstraint (storageSource) {

/* #line 48915 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_delta = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_delta));
     p_subConstraint = (SgAdaTypeConstraint*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_subConstraint));
    p_isDecimal = storageSource.storageOf_isDecimal;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDeltaConstraint*
SgAdaDeltaConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDeltaConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDeltaConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDeltaConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDeltaConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDeltaConstraint)};
        unsigned long positionInPool{localIndex % SgAdaDeltaConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDeltaConstraint::pool_size};
        returnPointer = &(((SgAdaDeltaConstraint*)(SgAdaDeltaConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDeltaConstraint*
SgAdaDeltaConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDeltaConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDeltaConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDeltaConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDeltaConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDeltaConstraint)};
        unsigned long positionInPool{localIndex % SgAdaDeltaConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDeltaConstraint::pool_size};
        returnPointer = &(((SgAdaDeltaConstraint*)(SgAdaDeltaConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDeltaConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDeltaConstraint::pools.begin(); block != SgAdaDeltaConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDeltaConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDeltaConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDeltaConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDeltaConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDeltaConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDeltaConstraint::pools.begin(); block != SgAdaDeltaConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDeltaConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDeltaConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDeltaConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDeltaConstraint::clearMemoryPool() {
    SgAdaDeltaConstraint* tempPointer{nullptr};
    if (SgAdaDeltaConstraint::pools.empty() == false) {
        auto block{SgAdaDeltaConstraint::pools.begin()};
        SgAdaDeltaConstraint::next_node = reinterpret_cast<SgAdaDeltaConstraint*>(*block);
        for (; block != SgAdaDeltaConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDeltaConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDeltaConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDeltaConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDeltaConstraint::pool_size-1]);
        }
    }
}

void
SgAdaDeltaConstraint::deleteMemoryPool() {
    for (auto p: SgAdaDeltaConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAdaDeltaConstraint::next_node = nullptr;
    SgAdaDeltaConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDeltaConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDeltaConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDeltaConstraint)};
    for (size_t blockIndex = SgAdaDeltaConstraint::pools.size(); (blockIndex * SgAdaDeltaConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDeltaConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDeltaConstraint) = %" PRIuPTR " SgAdaDeltaConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDeltaConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDeltaConstraint),SgAdaDeltaConstraint::pool_size);
        }
#endif

        SgAdaDeltaConstraint* pointer{static_cast<SgAdaDeltaConstraint*>(ROSE_MALLOC(SgAdaDeltaConstraint::pool_size * sizeof(SgAdaDeltaConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDeltaConstraint::pool_size * sizeof(SgAdaDeltaConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDeltaConstraint::pool_size * sizeof(SgAdaDeltaConstraint));
#endif
        SgAdaDeltaConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDeltaConstraint::pool_size * sizeof(SgAdaDeltaConstraint), V_SgAdaDeltaConstraint));

        if (SgAdaDeltaConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDeltaConstraint* blkptr{reinterpret_cast<SgAdaDeltaConstraint*>(SgAdaDeltaConstraint::pools[blockIndex-1])};
                blkptr[SgAdaDeltaConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDeltaConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDeltaConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDeltaConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDeltaConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDeltaConstraint::pool_size - 1};
    SgAdaDeltaConstraint* testPointer{reinterpret_cast<SgAdaDeltaConstraint*>(SgAdaDeltaConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDeltaConstraint::pool_size * (SgAdaDeltaConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDeltaConstraintStroageClass
 * from its counterpart of SgAdaDeltaConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDeltaConstraint::initializeStorageClassArray(SgAdaDeltaConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDeltaConstraint::pools.begin(); block != SgAdaDeltaConstraint::pools.end(); ++block) {
        SgAdaDeltaConstraint* pointer{reinterpret_cast<SgAdaDeltaConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDeltaConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 49135 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDiscriminantConstraint that takes its corresponding StorageClass as parameter
 */
SgAdaDiscriminantConstraint::SgAdaDiscriminantConstraint(const SgAdaDiscriminantConstraintStorageClass& storageSource)  : SgAdaTypeConstraint (storageSource) {

/* #line 49145 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_discriminants = storageSource.storageOf_discriminants.rebuildDataStoredInEasyStorageClass();
    SgExpressionPtrList::iterator i_discriminants = p_discriminants.begin();
    for ( ; i_discriminants != p_discriminants.end(); ++i_discriminants) {
        (*i_discriminants) = (SgExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_discriminants)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDiscriminantConstraint*
SgAdaDiscriminantConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDiscriminantConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminantConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminantConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminantConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminantConstraint)};
        unsigned long positionInPool{localIndex % SgAdaDiscriminantConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscriminantConstraint::pool_size};
        returnPointer = &(((SgAdaDiscriminantConstraint*)(SgAdaDiscriminantConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDiscriminantConstraint*
SgAdaDiscriminantConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDiscriminantConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminantConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminantConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminantConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDiscriminantConstraint)};
        unsigned long positionInPool{localIndex % SgAdaDiscriminantConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscriminantConstraint::pool_size};
        returnPointer = &(((SgAdaDiscriminantConstraint*)(SgAdaDiscriminantConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDiscriminantConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDiscriminantConstraint::pools.begin(); block != SgAdaDiscriminantConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscriminantConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminantConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDiscriminantConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDiscriminantConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDiscriminantConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDiscriminantConstraint::pools.begin(); block != SgAdaDiscriminantConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscriminantConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminantConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDiscriminantConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDiscriminantConstraint::clearMemoryPool() {
    SgAdaDiscriminantConstraint* tempPointer{nullptr};
    if (SgAdaDiscriminantConstraint::pools.empty() == false) {
        auto block{SgAdaDiscriminantConstraint::pools.begin()};
        SgAdaDiscriminantConstraint::next_node = reinterpret_cast<SgAdaDiscriminantConstraint*>(*block);
        for (; block != SgAdaDiscriminantConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDiscriminantConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDiscriminantConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDiscriminantConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDiscriminantConstraint::pool_size-1]);
        }
    }
}

void
SgAdaDiscriminantConstraint::deleteMemoryPool() {
    for (auto p: SgAdaDiscriminantConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAdaDiscriminantConstraint::next_node = nullptr;
    SgAdaDiscriminantConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDiscriminantConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminantConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminantConstraint)};
    for (size_t blockIndex = SgAdaDiscriminantConstraint::pools.size(); (blockIndex * SgAdaDiscriminantConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminantConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminantConstraint) = %" PRIuPTR " SgAdaDiscriminantConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminantConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminantConstraint),SgAdaDiscriminantConstraint::pool_size);
        }
#endif

        SgAdaDiscriminantConstraint* pointer{static_cast<SgAdaDiscriminantConstraint*>(ROSE_MALLOC(SgAdaDiscriminantConstraint::pool_size * sizeof(SgAdaDiscriminantConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDiscriminantConstraint::pool_size * sizeof(SgAdaDiscriminantConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDiscriminantConstraint::pool_size * sizeof(SgAdaDiscriminantConstraint));
#endif
        SgAdaDiscriminantConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDiscriminantConstraint::pool_size * sizeof(SgAdaDiscriminantConstraint), V_SgAdaDiscriminantConstraint));

        if (SgAdaDiscriminantConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDiscriminantConstraint* blkptr{reinterpret_cast<SgAdaDiscriminantConstraint*>(SgAdaDiscriminantConstraint::pools[blockIndex-1])};
                blkptr[SgAdaDiscriminantConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDiscriminantConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDiscriminantConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDiscriminantConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDiscriminantConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDiscriminantConstraint::pool_size - 1};
    SgAdaDiscriminantConstraint* testPointer{reinterpret_cast<SgAdaDiscriminantConstraint*>(SgAdaDiscriminantConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDiscriminantConstraint::pool_size * (SgAdaDiscriminantConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDiscriminantConstraintStroageClass
 * from its counterpart of SgAdaDiscriminantConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDiscriminantConstraint::initializeStorageClassArray(SgAdaDiscriminantConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDiscriminantConstraint::pools.begin(); block != SgAdaDiscriminantConstraint::pools.end(); ++block) {
        SgAdaDiscriminantConstraint* pointer{reinterpret_cast<SgAdaDiscriminantConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminantConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 49367 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaNullConstraint that takes its corresponding StorageClass as parameter
 */
SgAdaNullConstraint::SgAdaNullConstraint(const SgAdaNullConstraintStorageClass& storageSource)  : SgAdaTypeConstraint (storageSource) {

/* #line 49377 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaNullConstraint*
SgAdaNullConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaNullConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaNullConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaNullConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaNullConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaNullConstraint)};
        unsigned long positionInPool{localIndex % SgAdaNullConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaNullConstraint::pool_size};
        returnPointer = &(((SgAdaNullConstraint*)(SgAdaNullConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaNullConstraint*
SgAdaNullConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaNullConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaNullConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaNullConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaNullConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaNullConstraint)};
        unsigned long positionInPool{localIndex % SgAdaNullConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaNullConstraint::pool_size};
        returnPointer = &(((SgAdaNullConstraint*)(SgAdaNullConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaNullConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaNullConstraint::pools.begin(); block != SgAdaNullConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaNullConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaNullConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaNullConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaNullConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaNullConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaNullConstraint::pools.begin(); block != SgAdaNullConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaNullConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaNullConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaNullConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaNullConstraint::clearMemoryPool() {
    SgAdaNullConstraint* tempPointer{nullptr};
    if (SgAdaNullConstraint::pools.empty() == false) {
        auto block{SgAdaNullConstraint::pools.begin()};
        SgAdaNullConstraint::next_node = reinterpret_cast<SgAdaNullConstraint*>(*block);
        for (; block != SgAdaNullConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaNullConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaNullConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaNullConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaNullConstraint::pool_size-1]);
        }
    }
}

void
SgAdaNullConstraint::deleteMemoryPool() {
    for (auto p: SgAdaNullConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAdaNullConstraint::next_node = nullptr;
    SgAdaNullConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaNullConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaNullConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaNullConstraint)};
    for (size_t blockIndex = SgAdaNullConstraint::pools.size(); (blockIndex * SgAdaNullConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaNullConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaNullConstraint) = %" PRIuPTR " SgAdaNullConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaNullConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaNullConstraint),SgAdaNullConstraint::pool_size);
        }
#endif

        SgAdaNullConstraint* pointer{static_cast<SgAdaNullConstraint*>(ROSE_MALLOC(SgAdaNullConstraint::pool_size * sizeof(SgAdaNullConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaNullConstraint::pool_size * sizeof(SgAdaNullConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaNullConstraint::pool_size * sizeof(SgAdaNullConstraint));
#endif
        SgAdaNullConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaNullConstraint::pool_size * sizeof(SgAdaNullConstraint), V_SgAdaNullConstraint));

        if (SgAdaNullConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaNullConstraint* blkptr{reinterpret_cast<SgAdaNullConstraint*>(SgAdaNullConstraint::pools[blockIndex-1])};
                blkptr[SgAdaNullConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaNullConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaNullConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaNullConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaNullConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaNullConstraint::pool_size - 1};
    SgAdaNullConstraint* testPointer{reinterpret_cast<SgAdaNullConstraint*>(SgAdaNullConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaNullConstraint::pool_size * (SgAdaNullConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaNullConstraintStroageClass
 * from its counterpart of SgAdaNullConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaNullConstraint::initializeStorageClassArray(SgAdaNullConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaNullConstraint::pools.begin(); block != SgAdaNullConstraint::pools.end(); ++block) {
        SgAdaNullConstraint* pointer{reinterpret_cast<SgAdaNullConstraint*>(*block)};
        for (unsigned i = 0; i < SgAdaNullConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 49594 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStatement that takes its corresponding StorageClass as parameter
 */
SgStatement::SgStatement(const SgStatementStorageClass& storageSource)  : SgLocatedNode (storageSource) {

/* #line 49604 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_numeric_label));
    p_source_sequence_value = storageSource.storageOf_source_sequence_value;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStatement*
SgStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStatement)};
        unsigned long positionInPool{localIndex % SgStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStatement::pool_size};
        returnPointer = &(((SgStatement*)(SgStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStatement*
SgStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStatement)};
        unsigned long positionInPool{localIndex % SgStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStatement::pool_size};
        returnPointer = &(((SgStatement*)(SgStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStatement::pools.begin(); block != SgStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStatement*>(*block)};
        for (unsigned i = 0; i < SgStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgStatement::pools.begin(); block != SgStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStatement*>(*block)};
        for (unsigned i = 0; i < SgStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStatement::clearMemoryPool() {
    SgStatement* tempPointer{nullptr};
    if (SgStatement::pools.empty() == false) {
        auto block{SgStatement::pools.begin()};
        SgStatement::next_node = reinterpret_cast<SgStatement*>(*block);
        for (; block != SgStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStatement::pool_size-1]);
        }
    }
}

void
SgStatement::deleteMemoryPool() {
    for (auto p: SgStatement::pools) {
        ROSE_FREE(p);
    }
    SgStatement::next_node = nullptr;
    SgStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatement)};
    for (size_t blockIndex = SgStatement::pools.size(); (blockIndex * SgStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatement) = %" PRIuPTR " SgStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatement),SgStatement::pool_size);
        }
#endif

        SgStatement* pointer{static_cast<SgStatement*>(ROSE_MALLOC(SgStatement::pool_size * sizeof(SgStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStatement::pool_size * sizeof(SgStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStatement::pool_size * sizeof(SgStatement));
#endif
        SgStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStatement::pool_size * sizeof(SgStatement), V_SgStatement));

        if (SgStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStatement* blkptr{reinterpret_cast<SgStatement*>(SgStatement::pools[blockIndex-1])};
                blkptr[SgStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStatement::pool_size - 1};
    SgStatement* testPointer{reinterpret_cast<SgStatement*>(SgStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStatement::pool_size * (SgStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStatementStroageClass
 * from its counterpart of SgStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStatement::initializeStorageClassArray(SgStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStatement::pools.begin(); block != SgStatement::pools.end(); ++block) {
        SgStatement* pointer{reinterpret_cast<SgStatement*>(*block)};
        for (unsigned i = 0; i < SgStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 49823 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgScopeStatement that takes its corresponding StorageClass as parameter
 */
SgScopeStatement::SgScopeStatement(const SgScopeStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 49833 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol_table = (SgSymbolTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol_table));
     p_type_table = (SgTypeTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type_table));
    p_type_elaboration_list = storageSource.storageOf_type_elaboration_list.rebuildDataStoredInEasyStorageClass();
    p_hidden_type_list = storageSource.storageOf_hidden_type_list.rebuildDataStoredInEasyStorageClass();
    p_hidden_declaration_list = storageSource.storageOf_hidden_declaration_list.rebuildDataStoredInEasyStorageClass();
     p_pragma = (SgPragma*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pragma));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgScopeStatement*
SgScopeStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgScopeStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopeStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopeStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopeStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeStatement)};
        unsigned long positionInPool{localIndex % SgScopeStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopeStatement::pool_size};
        returnPointer = &(((SgScopeStatement*)(SgScopeStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgScopeStatement*
SgScopeStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgScopeStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopeStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopeStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopeStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgScopeStatement)};
        unsigned long positionInPool{localIndex % SgScopeStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopeStatement::pool_size};
        returnPointer = &(((SgScopeStatement*)(SgScopeStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgScopeStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgScopeStatement::pools.begin(); block != SgScopeStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopeStatement*>(*block)};
        for (unsigned i = 0; i < SgScopeStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgScopeStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgScopeStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgScopeStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgScopeStatement::pools.begin(); block != SgScopeStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopeStatement*>(*block)};
        for (unsigned i = 0; i < SgScopeStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgScopeStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgScopeStatement::clearMemoryPool() {
    SgScopeStatement* tempPointer{nullptr};
    if (SgScopeStatement::pools.empty() == false) {
        auto block{SgScopeStatement::pools.begin()};
        SgScopeStatement::next_node = reinterpret_cast<SgScopeStatement*>(*block);
        for (; block != SgScopeStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgScopeStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgScopeStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgScopeStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgScopeStatement::pool_size-1]);
        }
    }
}

void
SgScopeStatement::deleteMemoryPool() {
    for (auto p: SgScopeStatement::pools) {
        ROSE_FREE(p);
    }
    SgScopeStatement::next_node = nullptr;
    SgScopeStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgScopeStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopeStatement)};
    for (size_t blockIndex = SgScopeStatement::pools.size(); (blockIndex * SgScopeStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopeStatement) = %" PRIuPTR " SgScopeStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopeStatement),SgScopeStatement::pool_size);
        }
#endif

        SgScopeStatement* pointer{static_cast<SgScopeStatement*>(ROSE_MALLOC(SgScopeStatement::pool_size * sizeof(SgScopeStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgScopeStatement::pool_size * sizeof(SgScopeStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgScopeStatement::pool_size * sizeof(SgScopeStatement));
#endif
        SgScopeStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgScopeStatement::pool_size * sizeof(SgScopeStatement), V_SgScopeStatement));

        if (SgScopeStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgScopeStatement* blkptr{reinterpret_cast<SgScopeStatement*>(SgScopeStatement::pools[blockIndex-1])};
                blkptr[SgScopeStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgScopeStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgScopeStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgScopeStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgScopeStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgScopeStatement::pool_size - 1};
    SgScopeStatement* testPointer{reinterpret_cast<SgScopeStatement*>(SgScopeStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgScopeStatement::pool_size * (SgScopeStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgScopeStatementStroageClass
 * from its counterpart of SgScopeStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgScopeStatement::initializeStorageClassArray(SgScopeStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgScopeStatement::pools.begin(); block != SgScopeStatement::pools.end(); ++block) {
        SgScopeStatement* pointer{reinterpret_cast<SgScopeStatement*>(*block)};
        for (unsigned i = 0; i < SgScopeStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 50056 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGlobal that takes its corresponding StorageClass as parameter
 */
SgGlobal::SgGlobal(const SgGlobalStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 50066 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_declarations = storageSource.storageOf_declarations.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_declarations = p_declarations.begin();
    for ( ; i_declarations != p_declarations.end(); ++i_declarations) {
        (*i_declarations) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_declarations)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGlobal*
SgGlobal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGlobal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGlobal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGlobal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGlobal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGlobal)};
        unsigned long positionInPool{localIndex % SgGlobal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGlobal::pool_size};
        returnPointer = &(((SgGlobal*)(SgGlobal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGlobal*
SgGlobal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGlobal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGlobal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGlobal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGlobal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGlobal)};
        unsigned long positionInPool{localIndex % SgGlobal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGlobal::pool_size};
        returnPointer = &(((SgGlobal*)(SgGlobal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGlobal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGlobal::pools.begin(); block != SgGlobal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGlobal*>(*block)};
        for (unsigned i = 0; i < SgGlobal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGlobal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGlobal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGlobal* pointerOfLinkedList{nullptr};
    for (auto block = SgGlobal::pools.begin(); block != SgGlobal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGlobal*>(*block)};
        for (unsigned i = 0; i < SgGlobal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGlobal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGlobal::clearMemoryPool() {
    SgGlobal* tempPointer{nullptr};
    if (SgGlobal::pools.empty() == false) {
        auto block{SgGlobal::pools.begin()};
        SgGlobal::next_node = reinterpret_cast<SgGlobal*>(*block);
        for (; block != SgGlobal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGlobal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGlobal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGlobal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGlobal::pool_size-1]);
        }
    }
}

void
SgGlobal::deleteMemoryPool() {
    for (auto p: SgGlobal::pools) {
        ROSE_FREE(p);
    }
    SgGlobal::next_node = nullptr;
    SgGlobal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGlobal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGlobal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGlobal)};
    for (size_t blockIndex = SgGlobal::pools.size(); (blockIndex * SgGlobal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGlobal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGlobal) = %" PRIuPTR " SgGlobal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGlobal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGlobal),SgGlobal::pool_size);
        }
#endif

        SgGlobal* pointer{static_cast<SgGlobal*>(ROSE_MALLOC(SgGlobal::pool_size * sizeof(SgGlobal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGlobal::pool_size * sizeof(SgGlobal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGlobal::pool_size * sizeof(SgGlobal));
#endif
        SgGlobal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGlobal::pool_size * sizeof(SgGlobal), V_SgGlobal));

        if (SgGlobal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGlobal* blkptr{reinterpret_cast<SgGlobal*>(SgGlobal::pools[blockIndex-1])};
                blkptr[SgGlobal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGlobal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGlobal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGlobal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGlobal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGlobal::pool_size - 1};
    SgGlobal* testPointer{reinterpret_cast<SgGlobal*>(SgGlobal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGlobal::pool_size * (SgGlobal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGlobalStroageClass
 * from its counterpart of SgGlobal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGlobal::initializeStorageClassArray(SgGlobalStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGlobal::pools.begin(); block != SgGlobal::pools.end(); ++block) {
        SgGlobal* pointer{reinterpret_cast<SgGlobal*>(*block)};
        for (unsigned i = 0; i < SgGlobal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 50288 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBasicBlock that takes its corresponding StorageClass as parameter
 */
SgBasicBlock::SgBasicBlock(const SgBasicBlockStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 50298 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_statements = storageSource.storageOf_statements.rebuildDataStoredInEasyStorageClass();
    SgStatementPtrList::iterator i_statements = p_statements.begin();
    for ( ; i_statements != p_statements.end(); ++i_statements) {
        (*i_statements) = (SgStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_statements)) );
    }
    p_asm_function_body = storageSource.storageOf_asm_function_body.rebuildDataStoredInEasyStorageClass();
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBasicBlock*
SgBasicBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBasicBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBasicBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBasicBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBasicBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBasicBlock)};
        unsigned long positionInPool{localIndex % SgBasicBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBasicBlock::pool_size};
        returnPointer = &(((SgBasicBlock*)(SgBasicBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBasicBlock*
SgBasicBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBasicBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBasicBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBasicBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBasicBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBasicBlock)};
        unsigned long positionInPool{localIndex % SgBasicBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBasicBlock::pool_size};
        returnPointer = &(((SgBasicBlock*)(SgBasicBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBasicBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBasicBlock::pools.begin(); block != SgBasicBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBasicBlock*>(*block)};
        for (unsigned i = 0; i < SgBasicBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBasicBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBasicBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBasicBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgBasicBlock::pools.begin(); block != SgBasicBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBasicBlock*>(*block)};
        for (unsigned i = 0; i < SgBasicBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBasicBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBasicBlock::clearMemoryPool() {
    SgBasicBlock* tempPointer{nullptr};
    if (SgBasicBlock::pools.empty() == false) {
        auto block{SgBasicBlock::pools.begin()};
        SgBasicBlock::next_node = reinterpret_cast<SgBasicBlock*>(*block);
        for (; block != SgBasicBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBasicBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBasicBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBasicBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBasicBlock::pool_size-1]);
        }
    }
}

void
SgBasicBlock::deleteMemoryPool() {
    for (auto p: SgBasicBlock::pools) {
        ROSE_FREE(p);
    }
    SgBasicBlock::next_node = nullptr;
    SgBasicBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBasicBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBasicBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBasicBlock)};
    for (size_t blockIndex = SgBasicBlock::pools.size(); (blockIndex * SgBasicBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBasicBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBasicBlock) = %" PRIuPTR " SgBasicBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBasicBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBasicBlock),SgBasicBlock::pool_size);
        }
#endif

        SgBasicBlock* pointer{static_cast<SgBasicBlock*>(ROSE_MALLOC(SgBasicBlock::pool_size * sizeof(SgBasicBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBasicBlock::pool_size * sizeof(SgBasicBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBasicBlock::pool_size * sizeof(SgBasicBlock));
#endif
        SgBasicBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBasicBlock::pool_size * sizeof(SgBasicBlock), V_SgBasicBlock));

        if (SgBasicBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBasicBlock* blkptr{reinterpret_cast<SgBasicBlock*>(SgBasicBlock::pools[blockIndex-1])};
                blkptr[SgBasicBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBasicBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBasicBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBasicBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBasicBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBasicBlock::pool_size - 1};
    SgBasicBlock* testPointer{reinterpret_cast<SgBasicBlock*>(SgBasicBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBasicBlock::pool_size * (SgBasicBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBasicBlockStroageClass
 * from its counterpart of SgBasicBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBasicBlock::initializeStorageClassArray(SgBasicBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBasicBlock::pools.begin(); block != SgBasicBlock::pools.end(); ++block) {
        SgBasicBlock* pointer{reinterpret_cast<SgBasicBlock*>(*block)};
        for (unsigned i = 0; i < SgBasicBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 50522 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIfStmt that takes its corresponding StorageClass as parameter
 */
SgIfStmt::SgIfStmt(const SgIfStmtStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 50532 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_conditional = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_conditional));
     p_true_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_true_body));
     p_false_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_false_body));
    p_is_if_constexpr_statement = storageSource.storageOf_is_if_constexpr_statement;
    p_if_constexpr_value_known = storageSource.storageOf_if_constexpr_value_known;
    p_if_constexpr_value = storageSource.storageOf_if_constexpr_value;
     p_else_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_else_numeric_label));
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));
    p_has_end_statement = storageSource.storageOf_has_end_statement;
    p_use_then_keyword = storageSource.storageOf_use_then_keyword;
    p_is_else_if_statement = storageSource.storageOf_is_else_if_statement;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIfStmt*
SgIfStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIfStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIfStmt)};
        unsigned long positionInPool{localIndex % SgIfStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfStmt::pool_size};
        returnPointer = &(((SgIfStmt*)(SgIfStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIfStmt*
SgIfStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIfStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIfStmt)};
        unsigned long positionInPool{localIndex % SgIfStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfStmt::pool_size};
        returnPointer = &(((SgIfStmt*)(SgIfStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIfStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIfStmt::pools.begin(); block != SgIfStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfStmt*>(*block)};
        for (unsigned i = 0; i < SgIfStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIfStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIfStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIfStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgIfStmt::pools.begin(); block != SgIfStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfStmt*>(*block)};
        for (unsigned i = 0; i < SgIfStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIfStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIfStmt::clearMemoryPool() {
    SgIfStmt* tempPointer{nullptr};
    if (SgIfStmt::pools.empty() == false) {
        auto block{SgIfStmt::pools.begin()};
        SgIfStmt::next_node = reinterpret_cast<SgIfStmt*>(*block);
        for (; block != SgIfStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIfStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIfStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIfStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIfStmt::pool_size-1]);
        }
    }
}

void
SgIfStmt::deleteMemoryPool() {
    for (auto p: SgIfStmt::pools) {
        ROSE_FREE(p);
    }
    SgIfStmt::next_node = nullptr;
    SgIfStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIfStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIfStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfStmt)};
    for (size_t blockIndex = SgIfStmt::pools.size(); (blockIndex * SgIfStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIfStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfStmt) = %" PRIuPTR " SgIfStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIfStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfStmt),SgIfStmt::pool_size);
        }
#endif

        SgIfStmt* pointer{static_cast<SgIfStmt*>(ROSE_MALLOC(SgIfStmt::pool_size * sizeof(SgIfStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIfStmt::pool_size * sizeof(SgIfStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIfStmt::pool_size * sizeof(SgIfStmt));
#endif
        SgIfStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIfStmt::pool_size * sizeof(SgIfStmt), V_SgIfStmt));

        if (SgIfStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIfStmt* blkptr{reinterpret_cast<SgIfStmt*>(SgIfStmt::pools[blockIndex-1])};
                blkptr[SgIfStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIfStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIfStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIfStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIfStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIfStmt::pool_size - 1};
    SgIfStmt* testPointer{reinterpret_cast<SgIfStmt*>(SgIfStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIfStmt::pool_size * (SgIfStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIfStmtStroageClass
 * from its counterpart of SgIfStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIfStmt::initializeStorageClassArray(SgIfStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIfStmt::pools.begin(); block != SgIfStmt::pools.end(); ++block) {
        SgIfStmt* pointer{reinterpret_cast<SgIfStmt*>(*block)};
        for (unsigned i = 0; i < SgIfStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 50761 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgForStatement that takes its corresponding StorageClass as parameter
 */
SgForStatement::SgForStatement(const SgForStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 50771 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_for_init_stmt = (SgForInitStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_for_init_stmt));
     p_test = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_test));
     p_increment = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_increment));
     p_loop_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_loop_body));
     p_else_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_else_body));
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgForStatement*
SgForStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgForStatement)};
        unsigned long positionInPool{localIndex % SgForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgForStatement::pool_size};
        returnPointer = &(((SgForStatement*)(SgForStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgForStatement*
SgForStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgForStatement)};
        unsigned long positionInPool{localIndex % SgForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgForStatement::pool_size};
        returnPointer = &(((SgForStatement*)(SgForStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgForStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgForStatement::pools.begin(); block != SgForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgForStatement*>(*block)};
        for (unsigned i = 0; i < SgForStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgForStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgForStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgForStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgForStatement::pools.begin(); block != SgForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgForStatement*>(*block)};
        for (unsigned i = 0; i < SgForStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgForStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgForStatement::clearMemoryPool() {
    SgForStatement* tempPointer{nullptr};
    if (SgForStatement::pools.empty() == false) {
        auto block{SgForStatement::pools.begin()};
        SgForStatement::next_node = reinterpret_cast<SgForStatement*>(*block);
        for (; block != SgForStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgForStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgForStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgForStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgForStatement::pool_size-1]);
        }
    }
}

void
SgForStatement::deleteMemoryPool() {
    for (auto p: SgForStatement::pools) {
        ROSE_FREE(p);
    }
    SgForStatement::next_node = nullptr;
    SgForStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgForStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgForStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgForStatement)};
    for (size_t blockIndex = SgForStatement::pools.size(); (blockIndex * SgForStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgForStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgForStatement) = %" PRIuPTR " SgForStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgForStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgForStatement),SgForStatement::pool_size);
        }
#endif

        SgForStatement* pointer{static_cast<SgForStatement*>(ROSE_MALLOC(SgForStatement::pool_size * sizeof(SgForStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgForStatement::pool_size * sizeof(SgForStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgForStatement::pool_size * sizeof(SgForStatement));
#endif
        SgForStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgForStatement::pool_size * sizeof(SgForStatement), V_SgForStatement));

        if (SgForStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgForStatement* blkptr{reinterpret_cast<SgForStatement*>(SgForStatement::pools[blockIndex-1])};
                blkptr[SgForStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgForStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgForStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgForStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgForStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgForStatement::pool_size - 1};
    SgForStatement* testPointer{reinterpret_cast<SgForStatement*>(SgForStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgForStatement::pool_size * (SgForStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgForStatementStroageClass
 * from its counterpart of SgForStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgForStatement::initializeStorageClassArray(SgForStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgForStatement::pools.begin(); block != SgForStatement::pools.end(); ++block) {
        SgForStatement* pointer{reinterpret_cast<SgForStatement*>(*block)};
        for (unsigned i = 0; i < SgForStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 50994 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionDefinition that takes its corresponding StorageClass as parameter
 */
SgFunctionDefinition::SgFunctionDefinition(const SgFunctionDefinitionStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 51004 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_scope_number_list = storageSource.storageOf_scope_number_list.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionDefinition*
SgFunctionDefinition::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDefinition)};
        unsigned long positionInPool{localIndex % SgFunctionDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionDefinition::pool_size};
        returnPointer = &(((SgFunctionDefinition*)(SgFunctionDefinition::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionDefinition*
SgFunctionDefinition::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionDefinition)};
        unsigned long positionInPool{localIndex % SgFunctionDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionDefinition::pool_size};
        returnPointer = &(((SgFunctionDefinition*)(SgFunctionDefinition::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionDefinition::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionDefinition::pools.begin(); block != SgFunctionDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionDefinition*>(*block)};
        for (unsigned i = 0; i < SgFunctionDefinition::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionDefinition*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionDefinition::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionDefinition* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionDefinition::pools.begin(); block != SgFunctionDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionDefinition*>(*block)};
        for (unsigned i = 0; i < SgFunctionDefinition::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionDefinition::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionDefinition::clearMemoryPool() {
    SgFunctionDefinition* tempPointer{nullptr};
    if (SgFunctionDefinition::pools.empty() == false) {
        auto block{SgFunctionDefinition::pools.begin()};
        SgFunctionDefinition::next_node = reinterpret_cast<SgFunctionDefinition*>(*block);
        for (; block != SgFunctionDefinition::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionDefinition*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionDefinition::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionDefinition::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionDefinition::pool_size-1]);
        }
    }
}

void
SgFunctionDefinition::deleteMemoryPool() {
    for (auto p: SgFunctionDefinition::pools) {
        ROSE_FREE(p);
    }
    SgFunctionDefinition::next_node = nullptr;
    SgFunctionDefinition::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionDefinition::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDefinition) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionDefinition)};
    for (size_t blockIndex = SgFunctionDefinition::pools.size(); (blockIndex * SgFunctionDefinition::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDefinition) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionDefinition) = %" PRIuPTR " SgFunctionDefinition::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDefinition),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionDefinition),SgFunctionDefinition::pool_size);
        }
#endif

        SgFunctionDefinition* pointer{static_cast<SgFunctionDefinition*>(ROSE_MALLOC(SgFunctionDefinition::pool_size * sizeof(SgFunctionDefinition)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionDefinition::pool_size * sizeof(SgFunctionDefinition));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionDefinition::pool_size * sizeof(SgFunctionDefinition));
#endif
        SgFunctionDefinition::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionDefinition::pool_size * sizeof(SgFunctionDefinition), V_SgFunctionDefinition));

        if (SgFunctionDefinition::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionDefinition* blkptr{reinterpret_cast<SgFunctionDefinition*>(SgFunctionDefinition::pools[blockIndex-1])};
                blkptr[SgFunctionDefinition::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionDefinition::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionDefinition::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionDefinition::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionDefinition::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionDefinition::pool_size - 1};
    SgFunctionDefinition* testPointer{reinterpret_cast<SgFunctionDefinition*>(SgFunctionDefinition::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionDefinition::pool_size * (SgFunctionDefinition::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionDefinitionStroageClass
 * from its counterpart of SgFunctionDefinition. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionDefinition::initializeStorageClassArray(SgFunctionDefinitionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionDefinition::pools.begin(); block != SgFunctionDefinition::pools.end(); ++block) {
        SgFunctionDefinition* pointer{reinterpret_cast<SgFunctionDefinition*>(*block)};
        for (unsigned i = 0; i < SgFunctionDefinition::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 51223 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateFunctionDefinition that takes its corresponding StorageClass as parameter
 */
SgTemplateFunctionDefinition::SgTemplateFunctionDefinition(const SgTemplateFunctionDefinitionStorageClass& storageSource)  : SgFunctionDefinition (storageSource) {

/* #line 51233 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateFunctionDefinition*
SgTemplateFunctionDefinition::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateFunctionDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDefinition)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionDefinition::pool_size};
        returnPointer = &(((SgTemplateFunctionDefinition*)(SgTemplateFunctionDefinition::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateFunctionDefinition*
SgTemplateFunctionDefinition::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateFunctionDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateFunctionDefinition)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionDefinition::pool_size};
        returnPointer = &(((SgTemplateFunctionDefinition*)(SgTemplateFunctionDefinition::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateFunctionDefinition::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateFunctionDefinition::pools.begin(); block != SgTemplateFunctionDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionDefinition*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionDefinition::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateFunctionDefinition*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateFunctionDefinition::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateFunctionDefinition* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateFunctionDefinition::pools.begin(); block != SgTemplateFunctionDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionDefinition*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionDefinition::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateFunctionDefinition::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateFunctionDefinition::clearMemoryPool() {
    SgTemplateFunctionDefinition* tempPointer{nullptr};
    if (SgTemplateFunctionDefinition::pools.empty() == false) {
        auto block{SgTemplateFunctionDefinition::pools.begin()};
        SgTemplateFunctionDefinition::next_node = reinterpret_cast<SgTemplateFunctionDefinition*>(*block);
        for (; block != SgTemplateFunctionDefinition::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateFunctionDefinition*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateFunctionDefinition::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateFunctionDefinition::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateFunctionDefinition::pool_size-1]);
        }
    }
}

void
SgTemplateFunctionDefinition::deleteMemoryPool() {
    for (auto p: SgTemplateFunctionDefinition::pools) {
        ROSE_FREE(p);
    }
    SgTemplateFunctionDefinition::next_node = nullptr;
    SgTemplateFunctionDefinition::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateFunctionDefinition::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDefinition) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionDefinition)};
    for (size_t blockIndex = SgTemplateFunctionDefinition::pools.size(); (blockIndex * SgTemplateFunctionDefinition::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDefinition) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionDefinition) = %" PRIuPTR " SgTemplateFunctionDefinition::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDefinition),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionDefinition),SgTemplateFunctionDefinition::pool_size);
        }
#endif

        SgTemplateFunctionDefinition* pointer{static_cast<SgTemplateFunctionDefinition*>(ROSE_MALLOC(SgTemplateFunctionDefinition::pool_size * sizeof(SgTemplateFunctionDefinition)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateFunctionDefinition::pool_size * sizeof(SgTemplateFunctionDefinition));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateFunctionDefinition::pool_size * sizeof(SgTemplateFunctionDefinition));
#endif
        SgTemplateFunctionDefinition::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateFunctionDefinition::pool_size * sizeof(SgTemplateFunctionDefinition), V_SgTemplateFunctionDefinition));

        if (SgTemplateFunctionDefinition::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateFunctionDefinition* blkptr{reinterpret_cast<SgTemplateFunctionDefinition*>(SgTemplateFunctionDefinition::pools[blockIndex-1])};
                blkptr[SgTemplateFunctionDefinition::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateFunctionDefinition::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateFunctionDefinition::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateFunctionDefinition::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateFunctionDefinition::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateFunctionDefinition::pool_size - 1};
    SgTemplateFunctionDefinition* testPointer{reinterpret_cast<SgTemplateFunctionDefinition*>(SgTemplateFunctionDefinition::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateFunctionDefinition::pool_size * (SgTemplateFunctionDefinition::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateFunctionDefinitionStroageClass
 * from its counterpart of SgTemplateFunctionDefinition. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateFunctionDefinition::initializeStorageClassArray(SgTemplateFunctionDefinitionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateFunctionDefinition::pools.begin(); block != SgTemplateFunctionDefinition::pools.end(); ++block) {
        SgTemplateFunctionDefinition* pointer{reinterpret_cast<SgTemplateFunctionDefinition*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionDefinition::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 51450 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClassDefinition that takes its corresponding StorageClass as parameter
 */
SgClassDefinition::SgClassDefinition(const SgClassDefinitionStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 51460 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_members = storageSource.storageOf_members.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_members = p_members.begin();
    for ( ; i_members != p_members.end(); ++i_members) {
        (*i_members) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_members)) );
    }
    p_inheritances = storageSource.storageOf_inheritances.rebuildDataStoredInEasyStorageClass();
    SgBaseClassPtrList::iterator i_inheritances = p_inheritances.begin();
    for ( ; i_inheritances != p_inheritances.end(); ++i_inheritances) {
        (*i_inheritances) = (SgBaseClassPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_inheritances)) );
    }
    p_packingAlignment = storageSource.storageOf_packingAlignment;
    p_isSequence = storageSource.storageOf_isSequence;
    p_isPrivate = storageSource.storageOf_isPrivate;
    p_isAbstract = storageSource.storageOf_isAbstract;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClassDefinition*
SgClassDefinition::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClassDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDefinition)};
        unsigned long positionInPool{localIndex % SgClassDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassDefinition::pool_size};
        returnPointer = &(((SgClassDefinition*)(SgClassDefinition::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClassDefinition*
SgClassDefinition::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClassDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClassDefinition)};
        unsigned long positionInPool{localIndex % SgClassDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassDefinition::pool_size};
        returnPointer = &(((SgClassDefinition*)(SgClassDefinition::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClassDefinition::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClassDefinition::pools.begin(); block != SgClassDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassDefinition*>(*block)};
        for (unsigned i = 0; i < SgClassDefinition::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClassDefinition*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClassDefinition::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClassDefinition* pointerOfLinkedList{nullptr};
    for (auto block = SgClassDefinition::pools.begin(); block != SgClassDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassDefinition*>(*block)};
        for (unsigned i = 0; i < SgClassDefinition::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClassDefinition::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClassDefinition::clearMemoryPool() {
    SgClassDefinition* tempPointer{nullptr};
    if (SgClassDefinition::pools.empty() == false) {
        auto block{SgClassDefinition::pools.begin()};
        SgClassDefinition::next_node = reinterpret_cast<SgClassDefinition*>(*block);
        for (; block != SgClassDefinition::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClassDefinition*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClassDefinition::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClassDefinition::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClassDefinition::pool_size-1]);
        }
    }
}

void
SgClassDefinition::deleteMemoryPool() {
    for (auto p: SgClassDefinition::pools) {
        ROSE_FREE(p);
    }
    SgClassDefinition::next_node = nullptr;
    SgClassDefinition::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClassDefinition::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDefinition) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDefinition)};
    for (size_t blockIndex = SgClassDefinition::pools.size(); (blockIndex * SgClassDefinition::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDefinition) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDefinition) = %" PRIuPTR " SgClassDefinition::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDefinition),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDefinition),SgClassDefinition::pool_size);
        }
#endif

        SgClassDefinition* pointer{static_cast<SgClassDefinition*>(ROSE_MALLOC(SgClassDefinition::pool_size * sizeof(SgClassDefinition)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClassDefinition::pool_size * sizeof(SgClassDefinition));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClassDefinition::pool_size * sizeof(SgClassDefinition));
#endif
        SgClassDefinition::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClassDefinition::pool_size * sizeof(SgClassDefinition), V_SgClassDefinition));

        if (SgClassDefinition::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClassDefinition* blkptr{reinterpret_cast<SgClassDefinition*>(SgClassDefinition::pools[blockIndex-1])};
                blkptr[SgClassDefinition::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClassDefinition::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClassDefinition::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClassDefinition::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClassDefinition::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClassDefinition::pool_size - 1};
    SgClassDefinition* testPointer{reinterpret_cast<SgClassDefinition*>(SgClassDefinition::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClassDefinition::pool_size * (SgClassDefinition::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClassDefinitionStroageClass
 * from its counterpart of SgClassDefinition. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClassDefinition::initializeStorageClassArray(SgClassDefinitionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClassDefinition::pools.begin(); block != SgClassDefinition::pools.end(); ++block) {
        SgClassDefinition* pointer{reinterpret_cast<SgClassDefinition*>(*block)};
        for (unsigned i = 0; i < SgClassDefinition::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 51691 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateInstantiationDefn that takes its corresponding StorageClass as parameter
 */
SgTemplateInstantiationDefn::SgTemplateInstantiationDefn(const SgTemplateInstantiationDefnStorageClass& storageSource)  : SgClassDefinition (storageSource) {

/* #line 51701 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateInstantiationDefn*
SgTemplateInstantiationDefn::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateInstantiationDefn* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDefn)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDefn + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDefn)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDefn)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationDefn::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationDefn::pool_size};
        returnPointer = &(((SgTemplateInstantiationDefn*)(SgTemplateInstantiationDefn::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateInstantiationDefn*
SgTemplateInstantiationDefn::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateInstantiationDefn* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDefn)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDefn + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDefn)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateInstantiationDefn)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationDefn::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationDefn::pool_size};
        returnPointer = &(((SgTemplateInstantiationDefn*)(SgTemplateInstantiationDefn::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateInstantiationDefn::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateInstantiationDefn::pools.begin(); block != SgTemplateInstantiationDefn::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationDefn*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDefn::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateInstantiationDefn*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateInstantiationDefn::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateInstantiationDefn* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateInstantiationDefn::pools.begin(); block != SgTemplateInstantiationDefn::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationDefn*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDefn::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateInstantiationDefn::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateInstantiationDefn::clearMemoryPool() {
    SgTemplateInstantiationDefn* tempPointer{nullptr};
    if (SgTemplateInstantiationDefn::pools.empty() == false) {
        auto block{SgTemplateInstantiationDefn::pools.begin()};
        SgTemplateInstantiationDefn::next_node = reinterpret_cast<SgTemplateInstantiationDefn*>(*block);
        for (; block != SgTemplateInstantiationDefn::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateInstantiationDefn*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateInstantiationDefn::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateInstantiationDefn::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateInstantiationDefn::pool_size-1]);
        }
    }
}

void
SgTemplateInstantiationDefn::deleteMemoryPool() {
    for (auto p: SgTemplateInstantiationDefn::pools) {
        ROSE_FREE(p);
    }
    SgTemplateInstantiationDefn::next_node = nullptr;
    SgTemplateInstantiationDefn::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateInstantiationDefn::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDefn) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDefn)};
    for (size_t blockIndex = SgTemplateInstantiationDefn::pools.size(); (blockIndex * SgTemplateInstantiationDefn::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDefn) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDefn) = %" PRIuPTR " SgTemplateInstantiationDefn::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDefn),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDefn),SgTemplateInstantiationDefn::pool_size);
        }
#endif

        SgTemplateInstantiationDefn* pointer{static_cast<SgTemplateInstantiationDefn*>(ROSE_MALLOC(SgTemplateInstantiationDefn::pool_size * sizeof(SgTemplateInstantiationDefn)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateInstantiationDefn::pool_size * sizeof(SgTemplateInstantiationDefn));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateInstantiationDefn::pool_size * sizeof(SgTemplateInstantiationDefn));
#endif
        SgTemplateInstantiationDefn::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateInstantiationDefn::pool_size * sizeof(SgTemplateInstantiationDefn), V_SgTemplateInstantiationDefn));

        if (SgTemplateInstantiationDefn::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateInstantiationDefn* blkptr{reinterpret_cast<SgTemplateInstantiationDefn*>(SgTemplateInstantiationDefn::pools[blockIndex-1])};
                blkptr[SgTemplateInstantiationDefn::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateInstantiationDefn::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateInstantiationDefn::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateInstantiationDefn::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateInstantiationDefn::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateInstantiationDefn::pool_size - 1};
    SgTemplateInstantiationDefn* testPointer{reinterpret_cast<SgTemplateInstantiationDefn*>(SgTemplateInstantiationDefn::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateInstantiationDefn::pool_size * (SgTemplateInstantiationDefn::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateInstantiationDefnStroageClass
 * from its counterpart of SgTemplateInstantiationDefn. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateInstantiationDefn::initializeStorageClassArray(SgTemplateInstantiationDefnStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateInstantiationDefn::pools.begin(); block != SgTemplateInstantiationDefn::pools.end(); ++block) {
        SgTemplateInstantiationDefn* pointer{reinterpret_cast<SgTemplateInstantiationDefn*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDefn::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 51918 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateClassDefinition that takes its corresponding StorageClass as parameter
 */
SgTemplateClassDefinition::SgTemplateClassDefinition(const SgTemplateClassDefinitionStorageClass& storageSource)  : SgClassDefinition (storageSource) {

/* #line 51928 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateClassDefinition*
SgTemplateClassDefinition::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateClassDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDefinition)};
        unsigned long positionInPool{localIndex % SgTemplateClassDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateClassDefinition::pool_size};
        returnPointer = &(((SgTemplateClassDefinition*)(SgTemplateClassDefinition::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateClassDefinition*
SgTemplateClassDefinition::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateClassDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateClassDefinition)};
        unsigned long positionInPool{localIndex % SgTemplateClassDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateClassDefinition::pool_size};
        returnPointer = &(((SgTemplateClassDefinition*)(SgTemplateClassDefinition::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateClassDefinition::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateClassDefinition::pools.begin(); block != SgTemplateClassDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateClassDefinition*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassDefinition::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateClassDefinition*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateClassDefinition::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateClassDefinition* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateClassDefinition::pools.begin(); block != SgTemplateClassDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateClassDefinition*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassDefinition::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateClassDefinition::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateClassDefinition::clearMemoryPool() {
    SgTemplateClassDefinition* tempPointer{nullptr};
    if (SgTemplateClassDefinition::pools.empty() == false) {
        auto block{SgTemplateClassDefinition::pools.begin()};
        SgTemplateClassDefinition::next_node = reinterpret_cast<SgTemplateClassDefinition*>(*block);
        for (; block != SgTemplateClassDefinition::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateClassDefinition*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateClassDefinition::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateClassDefinition::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateClassDefinition::pool_size-1]);
        }
    }
}

void
SgTemplateClassDefinition::deleteMemoryPool() {
    for (auto p: SgTemplateClassDefinition::pools) {
        ROSE_FREE(p);
    }
    SgTemplateClassDefinition::next_node = nullptr;
    SgTemplateClassDefinition::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateClassDefinition::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDefinition) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassDefinition)};
    for (size_t blockIndex = SgTemplateClassDefinition::pools.size(); (blockIndex * SgTemplateClassDefinition::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDefinition) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassDefinition) = %" PRIuPTR " SgTemplateClassDefinition::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDefinition),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassDefinition),SgTemplateClassDefinition::pool_size);
        }
#endif

        SgTemplateClassDefinition* pointer{static_cast<SgTemplateClassDefinition*>(ROSE_MALLOC(SgTemplateClassDefinition::pool_size * sizeof(SgTemplateClassDefinition)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateClassDefinition::pool_size * sizeof(SgTemplateClassDefinition));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateClassDefinition::pool_size * sizeof(SgTemplateClassDefinition));
#endif
        SgTemplateClassDefinition::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateClassDefinition::pool_size * sizeof(SgTemplateClassDefinition), V_SgTemplateClassDefinition));

        if (SgTemplateClassDefinition::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateClassDefinition* blkptr{reinterpret_cast<SgTemplateClassDefinition*>(SgTemplateClassDefinition::pools[blockIndex-1])};
                blkptr[SgTemplateClassDefinition::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateClassDefinition::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateClassDefinition::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateClassDefinition::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateClassDefinition::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateClassDefinition::pool_size - 1};
    SgTemplateClassDefinition* testPointer{reinterpret_cast<SgTemplateClassDefinition*>(SgTemplateClassDefinition::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateClassDefinition::pool_size * (SgTemplateClassDefinition::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateClassDefinitionStroageClass
 * from its counterpart of SgTemplateClassDefinition. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateClassDefinition::initializeStorageClassArray(SgTemplateClassDefinitionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateClassDefinition::pools.begin(); block != SgTemplateClassDefinition::pools.end(); ++block) {
        SgTemplateClassDefinition* pointer{reinterpret_cast<SgTemplateClassDefinition*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassDefinition::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 52145 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWhileStmt that takes its corresponding StorageClass as parameter
 */
SgWhileStmt::SgWhileStmt(const SgWhileStmtStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 52155 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_condition = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_condition));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_else_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_else_body));
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();
    p_has_end_statement = storageSource.storageOf_has_end_statement;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWhileStmt*
SgWhileStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWhileStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhileStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhileStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhileStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWhileStmt)};
        unsigned long positionInPool{localIndex % SgWhileStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWhileStmt::pool_size};
        returnPointer = &(((SgWhileStmt*)(SgWhileStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWhileStmt*
SgWhileStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWhileStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhileStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhileStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhileStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWhileStmt)};
        unsigned long positionInPool{localIndex % SgWhileStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWhileStmt::pool_size};
        returnPointer = &(((SgWhileStmt*)(SgWhileStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWhileStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWhileStmt::pools.begin(); block != SgWhileStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWhileStmt*>(*block)};
        for (unsigned i = 0; i < SgWhileStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWhileStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWhileStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWhileStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgWhileStmt::pools.begin(); block != SgWhileStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWhileStmt*>(*block)};
        for (unsigned i = 0; i < SgWhileStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWhileStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWhileStmt::clearMemoryPool() {
    SgWhileStmt* tempPointer{nullptr};
    if (SgWhileStmt::pools.empty() == false) {
        auto block{SgWhileStmt::pools.begin()};
        SgWhileStmt::next_node = reinterpret_cast<SgWhileStmt*>(*block);
        for (; block != SgWhileStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWhileStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWhileStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWhileStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWhileStmt::pool_size-1]);
        }
    }
}

void
SgWhileStmt::deleteMemoryPool() {
    for (auto p: SgWhileStmt::pools) {
        ROSE_FREE(p);
    }
    SgWhileStmt::next_node = nullptr;
    SgWhileStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWhileStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWhileStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhileStmt)};
    for (size_t blockIndex = SgWhileStmt::pools.size(); (blockIndex * SgWhileStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWhileStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhileStmt) = %" PRIuPTR " SgWhileStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWhileStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhileStmt),SgWhileStmt::pool_size);
        }
#endif

        SgWhileStmt* pointer{static_cast<SgWhileStmt*>(ROSE_MALLOC(SgWhileStmt::pool_size * sizeof(SgWhileStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWhileStmt::pool_size * sizeof(SgWhileStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWhileStmt::pool_size * sizeof(SgWhileStmt));
#endif
        SgWhileStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWhileStmt::pool_size * sizeof(SgWhileStmt), V_SgWhileStmt));

        if (SgWhileStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWhileStmt* blkptr{reinterpret_cast<SgWhileStmt*>(SgWhileStmt::pools[blockIndex-1])};
                blkptr[SgWhileStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWhileStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWhileStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWhileStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWhileStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWhileStmt::pool_size - 1};
    SgWhileStmt* testPointer{reinterpret_cast<SgWhileStmt*>(SgWhileStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWhileStmt::pool_size * (SgWhileStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWhileStmtStroageClass
 * from its counterpart of SgWhileStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWhileStmt::initializeStorageClassArray(SgWhileStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWhileStmt::pools.begin(); block != SgWhileStmt::pools.end(); ++block) {
        SgWhileStmt* pointer{reinterpret_cast<SgWhileStmt*>(*block)};
        for (unsigned i = 0; i < SgWhileStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 52378 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDoWhileStmt that takes its corresponding StorageClass as parameter
 */
SgDoWhileStmt::SgDoWhileStmt(const SgDoWhileStmtStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 52388 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_condition = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_condition));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDoWhileStmt*
SgDoWhileStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDoWhileStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDoWhileStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDoWhileStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDoWhileStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDoWhileStmt)};
        unsigned long positionInPool{localIndex % SgDoWhileStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDoWhileStmt::pool_size};
        returnPointer = &(((SgDoWhileStmt*)(SgDoWhileStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDoWhileStmt*
SgDoWhileStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDoWhileStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDoWhileStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDoWhileStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDoWhileStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDoWhileStmt)};
        unsigned long positionInPool{localIndex % SgDoWhileStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDoWhileStmt::pool_size};
        returnPointer = &(((SgDoWhileStmt*)(SgDoWhileStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDoWhileStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDoWhileStmt::pools.begin(); block != SgDoWhileStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDoWhileStmt*>(*block)};
        for (unsigned i = 0; i < SgDoWhileStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDoWhileStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDoWhileStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDoWhileStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgDoWhileStmt::pools.begin(); block != SgDoWhileStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDoWhileStmt*>(*block)};
        for (unsigned i = 0; i < SgDoWhileStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDoWhileStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDoWhileStmt::clearMemoryPool() {
    SgDoWhileStmt* tempPointer{nullptr};
    if (SgDoWhileStmt::pools.empty() == false) {
        auto block{SgDoWhileStmt::pools.begin()};
        SgDoWhileStmt::next_node = reinterpret_cast<SgDoWhileStmt*>(*block);
        for (; block != SgDoWhileStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDoWhileStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDoWhileStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDoWhileStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDoWhileStmt::pool_size-1]);
        }
    }
}

void
SgDoWhileStmt::deleteMemoryPool() {
    for (auto p: SgDoWhileStmt::pools) {
        ROSE_FREE(p);
    }
    SgDoWhileStmt::next_node = nullptr;
    SgDoWhileStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDoWhileStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDoWhileStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDoWhileStmt)};
    for (size_t blockIndex = SgDoWhileStmt::pools.size(); (blockIndex * SgDoWhileStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDoWhileStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDoWhileStmt) = %" PRIuPTR " SgDoWhileStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDoWhileStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDoWhileStmt),SgDoWhileStmt::pool_size);
        }
#endif

        SgDoWhileStmt* pointer{static_cast<SgDoWhileStmt*>(ROSE_MALLOC(SgDoWhileStmt::pool_size * sizeof(SgDoWhileStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDoWhileStmt::pool_size * sizeof(SgDoWhileStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDoWhileStmt::pool_size * sizeof(SgDoWhileStmt));
#endif
        SgDoWhileStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDoWhileStmt::pool_size * sizeof(SgDoWhileStmt), V_SgDoWhileStmt));

        if (SgDoWhileStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDoWhileStmt* blkptr{reinterpret_cast<SgDoWhileStmt*>(SgDoWhileStmt::pools[blockIndex-1])};
                blkptr[SgDoWhileStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDoWhileStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDoWhileStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDoWhileStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDoWhileStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDoWhileStmt::pool_size - 1};
    SgDoWhileStmt* testPointer{reinterpret_cast<SgDoWhileStmt*>(SgDoWhileStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDoWhileStmt::pool_size * (SgDoWhileStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDoWhileStmtStroageClass
 * from its counterpart of SgDoWhileStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDoWhileStmt::initializeStorageClassArray(SgDoWhileStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDoWhileStmt::pools.begin(); block != SgDoWhileStmt::pools.end(); ++block) {
        SgDoWhileStmt* pointer{reinterpret_cast<SgDoWhileStmt*>(*block)};
        for (unsigned i = 0; i < SgDoWhileStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 52607 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSwitchStatement that takes its corresponding StorageClass as parameter
 */
SgSwitchStatement::SgSwitchStatement(const SgSwitchStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 52617 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_item_selector = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_item_selector));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSwitchStatement*
SgSwitchStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSwitchStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSwitchStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSwitchStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSwitchStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSwitchStatement)};
        unsigned long positionInPool{localIndex % SgSwitchStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSwitchStatement::pool_size};
        returnPointer = &(((SgSwitchStatement*)(SgSwitchStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSwitchStatement*
SgSwitchStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSwitchStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSwitchStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSwitchStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSwitchStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSwitchStatement)};
        unsigned long positionInPool{localIndex % SgSwitchStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSwitchStatement::pool_size};
        returnPointer = &(((SgSwitchStatement*)(SgSwitchStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSwitchStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSwitchStatement::pools.begin(); block != SgSwitchStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSwitchStatement*>(*block)};
        for (unsigned i = 0; i < SgSwitchStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSwitchStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSwitchStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSwitchStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgSwitchStatement::pools.begin(); block != SgSwitchStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSwitchStatement*>(*block)};
        for (unsigned i = 0; i < SgSwitchStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSwitchStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSwitchStatement::clearMemoryPool() {
    SgSwitchStatement* tempPointer{nullptr};
    if (SgSwitchStatement::pools.empty() == false) {
        auto block{SgSwitchStatement::pools.begin()};
        SgSwitchStatement::next_node = reinterpret_cast<SgSwitchStatement*>(*block);
        for (; block != SgSwitchStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSwitchStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSwitchStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSwitchStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSwitchStatement::pool_size-1]);
        }
    }
}

void
SgSwitchStatement::deleteMemoryPool() {
    for (auto p: SgSwitchStatement::pools) {
        ROSE_FREE(p);
    }
    SgSwitchStatement::next_node = nullptr;
    SgSwitchStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSwitchStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSwitchStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSwitchStatement)};
    for (size_t blockIndex = SgSwitchStatement::pools.size(); (blockIndex * SgSwitchStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSwitchStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSwitchStatement) = %" PRIuPTR " SgSwitchStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSwitchStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSwitchStatement),SgSwitchStatement::pool_size);
        }
#endif

        SgSwitchStatement* pointer{static_cast<SgSwitchStatement*>(ROSE_MALLOC(SgSwitchStatement::pool_size * sizeof(SgSwitchStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSwitchStatement::pool_size * sizeof(SgSwitchStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSwitchStatement::pool_size * sizeof(SgSwitchStatement));
#endif
        SgSwitchStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSwitchStatement::pool_size * sizeof(SgSwitchStatement), V_SgSwitchStatement));

        if (SgSwitchStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSwitchStatement* blkptr{reinterpret_cast<SgSwitchStatement*>(SgSwitchStatement::pools[blockIndex-1])};
                blkptr[SgSwitchStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSwitchStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSwitchStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSwitchStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSwitchStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSwitchStatement::pool_size - 1};
    SgSwitchStatement* testPointer{reinterpret_cast<SgSwitchStatement*>(SgSwitchStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSwitchStatement::pool_size * (SgSwitchStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSwitchStatementStroageClass
 * from its counterpart of SgSwitchStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSwitchStatement::initializeStorageClassArray(SgSwitchStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSwitchStatement::pools.begin(); block != SgSwitchStatement::pools.end(); ++block) {
        SgSwitchStatement* pointer{reinterpret_cast<SgSwitchStatement*>(*block)};
        for (unsigned i = 0; i < SgSwitchStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 52838 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCatchOptionStmt that takes its corresponding StorageClass as parameter
 */
SgCatchOptionStmt::SgCatchOptionStmt(const SgCatchOptionStmtStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 52848 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_condition = (SgVariableDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_condition));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_trystmt = (SgTryStmt*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_trystmt));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCatchOptionStmt*
SgCatchOptionStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCatchOptionStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCatchOptionStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCatchOptionStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCatchOptionStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchOptionStmt)};
        unsigned long positionInPool{localIndex % SgCatchOptionStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCatchOptionStmt::pool_size};
        returnPointer = &(((SgCatchOptionStmt*)(SgCatchOptionStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCatchOptionStmt*
SgCatchOptionStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCatchOptionStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCatchOptionStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCatchOptionStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCatchOptionStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCatchOptionStmt)};
        unsigned long positionInPool{localIndex % SgCatchOptionStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCatchOptionStmt::pool_size};
        returnPointer = &(((SgCatchOptionStmt*)(SgCatchOptionStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCatchOptionStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCatchOptionStmt::pools.begin(); block != SgCatchOptionStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCatchOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgCatchOptionStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCatchOptionStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCatchOptionStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCatchOptionStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgCatchOptionStmt::pools.begin(); block != SgCatchOptionStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCatchOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgCatchOptionStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCatchOptionStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCatchOptionStmt::clearMemoryPool() {
    SgCatchOptionStmt* tempPointer{nullptr};
    if (SgCatchOptionStmt::pools.empty() == false) {
        auto block{SgCatchOptionStmt::pools.begin()};
        SgCatchOptionStmt::next_node = reinterpret_cast<SgCatchOptionStmt*>(*block);
        for (; block != SgCatchOptionStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCatchOptionStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCatchOptionStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCatchOptionStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCatchOptionStmt::pool_size-1]);
        }
    }
}

void
SgCatchOptionStmt::deleteMemoryPool() {
    for (auto p: SgCatchOptionStmt::pools) {
        ROSE_FREE(p);
    }
    SgCatchOptionStmt::next_node = nullptr;
    SgCatchOptionStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCatchOptionStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchOptionStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCatchOptionStmt)};
    for (size_t blockIndex = SgCatchOptionStmt::pools.size(); (blockIndex * SgCatchOptionStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchOptionStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCatchOptionStmt) = %" PRIuPTR " SgCatchOptionStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchOptionStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCatchOptionStmt),SgCatchOptionStmt::pool_size);
        }
#endif

        SgCatchOptionStmt* pointer{static_cast<SgCatchOptionStmt*>(ROSE_MALLOC(SgCatchOptionStmt::pool_size * sizeof(SgCatchOptionStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCatchOptionStmt::pool_size * sizeof(SgCatchOptionStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCatchOptionStmt::pool_size * sizeof(SgCatchOptionStmt));
#endif
        SgCatchOptionStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCatchOptionStmt::pool_size * sizeof(SgCatchOptionStmt), V_SgCatchOptionStmt));

        if (SgCatchOptionStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCatchOptionStmt* blkptr{reinterpret_cast<SgCatchOptionStmt*>(SgCatchOptionStmt::pools[blockIndex-1])};
                blkptr[SgCatchOptionStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCatchOptionStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCatchOptionStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCatchOptionStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCatchOptionStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCatchOptionStmt::pool_size - 1};
    SgCatchOptionStmt* testPointer{reinterpret_cast<SgCatchOptionStmt*>(SgCatchOptionStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCatchOptionStmt::pool_size * (SgCatchOptionStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCatchOptionStmtStroageClass
 * from its counterpart of SgCatchOptionStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCatchOptionStmt::initializeStorageClassArray(SgCatchOptionStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCatchOptionStmt::pools.begin(); block != SgCatchOptionStmt::pools.end(); ++block) {
        SgCatchOptionStmt* pointer{reinterpret_cast<SgCatchOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgCatchOptionStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 53068 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNamespaceDefinitionStatement that takes its corresponding StorageClass as parameter
 */
SgNamespaceDefinitionStatement::SgNamespaceDefinitionStatement(const SgNamespaceDefinitionStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 53078 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_declarations = storageSource.storageOf_declarations.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_declarations = p_declarations.begin();
    for ( ; i_declarations != p_declarations.end(); ++i_declarations) {
        (*i_declarations) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_declarations)) );
    }
     p_namespaceDeclaration = (SgNamespaceDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_namespaceDeclaration));
     p_previousNamespaceDefinition = (SgNamespaceDefinitionStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_previousNamespaceDefinition));
     p_nextNamespaceDefinition = (SgNamespaceDefinitionStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nextNamespaceDefinition));
     p_global_definition = (SgNamespaceDefinitionStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_global_definition));
    p_isUnionOfReentrantNamespaceDefinitions = storageSource.storageOf_isUnionOfReentrantNamespaceDefinitions;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNamespaceDefinitionStatement*
SgNamespaceDefinitionStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNamespaceDefinitionStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceDefinitionStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceDefinitionStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceDefinitionStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDefinitionStatement)};
        unsigned long positionInPool{localIndex % SgNamespaceDefinitionStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceDefinitionStatement::pool_size};
        returnPointer = &(((SgNamespaceDefinitionStatement*)(SgNamespaceDefinitionStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNamespaceDefinitionStatement*
SgNamespaceDefinitionStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNamespaceDefinitionStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceDefinitionStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceDefinitionStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceDefinitionStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNamespaceDefinitionStatement)};
        unsigned long positionInPool{localIndex % SgNamespaceDefinitionStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceDefinitionStatement::pool_size};
        returnPointer = &(((SgNamespaceDefinitionStatement*)(SgNamespaceDefinitionStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNamespaceDefinitionStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNamespaceDefinitionStatement::pools.begin(); block != SgNamespaceDefinitionStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceDefinitionStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceDefinitionStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNamespaceDefinitionStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNamespaceDefinitionStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNamespaceDefinitionStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgNamespaceDefinitionStatement::pools.begin(); block != SgNamespaceDefinitionStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceDefinitionStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceDefinitionStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNamespaceDefinitionStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNamespaceDefinitionStatement::clearMemoryPool() {
    SgNamespaceDefinitionStatement* tempPointer{nullptr};
    if (SgNamespaceDefinitionStatement::pools.empty() == false) {
        auto block{SgNamespaceDefinitionStatement::pools.begin()};
        SgNamespaceDefinitionStatement::next_node = reinterpret_cast<SgNamespaceDefinitionStatement*>(*block);
        for (; block != SgNamespaceDefinitionStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNamespaceDefinitionStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNamespaceDefinitionStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNamespaceDefinitionStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNamespaceDefinitionStatement::pool_size-1]);
        }
    }
}

void
SgNamespaceDefinitionStatement::deleteMemoryPool() {
    for (auto p: SgNamespaceDefinitionStatement::pools) {
        ROSE_FREE(p);
    }
    SgNamespaceDefinitionStatement::next_node = nullptr;
    SgNamespaceDefinitionStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNamespaceDefinitionStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDefinitionStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceDefinitionStatement)};
    for (size_t blockIndex = SgNamespaceDefinitionStatement::pools.size(); (blockIndex * SgNamespaceDefinitionStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDefinitionStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceDefinitionStatement) = %" PRIuPTR " SgNamespaceDefinitionStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDefinitionStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceDefinitionStatement),SgNamespaceDefinitionStatement::pool_size);
        }
#endif

        SgNamespaceDefinitionStatement* pointer{static_cast<SgNamespaceDefinitionStatement*>(ROSE_MALLOC(SgNamespaceDefinitionStatement::pool_size * sizeof(SgNamespaceDefinitionStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNamespaceDefinitionStatement::pool_size * sizeof(SgNamespaceDefinitionStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNamespaceDefinitionStatement::pool_size * sizeof(SgNamespaceDefinitionStatement));
#endif
        SgNamespaceDefinitionStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNamespaceDefinitionStatement::pool_size * sizeof(SgNamespaceDefinitionStatement), V_SgNamespaceDefinitionStatement));

        if (SgNamespaceDefinitionStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNamespaceDefinitionStatement* blkptr{reinterpret_cast<SgNamespaceDefinitionStatement*>(SgNamespaceDefinitionStatement::pools[blockIndex-1])};
                blkptr[SgNamespaceDefinitionStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNamespaceDefinitionStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNamespaceDefinitionStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNamespaceDefinitionStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNamespaceDefinitionStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNamespaceDefinitionStatement::pool_size - 1};
    SgNamespaceDefinitionStatement* testPointer{reinterpret_cast<SgNamespaceDefinitionStatement*>(SgNamespaceDefinitionStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNamespaceDefinitionStatement::pool_size * (SgNamespaceDefinitionStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNamespaceDefinitionStatementStroageClass
 * from its counterpart of SgNamespaceDefinitionStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNamespaceDefinitionStatement::initializeStorageClassArray(SgNamespaceDefinitionStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNamespaceDefinitionStatement::pools.begin(); block != SgNamespaceDefinitionStatement::pools.end(); ++block) {
        SgNamespaceDefinitionStatement* pointer{reinterpret_cast<SgNamespaceDefinitionStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceDefinitionStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 53305 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBlockDataStatement that takes its corresponding StorageClass as parameter
 */
SgBlockDataStatement::SgBlockDataStatement(const SgBlockDataStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 53315 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBlockDataStatement*
SgBlockDataStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBlockDataStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBlockDataStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBlockDataStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBlockDataStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBlockDataStatement)};
        unsigned long positionInPool{localIndex % SgBlockDataStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBlockDataStatement::pool_size};
        returnPointer = &(((SgBlockDataStatement*)(SgBlockDataStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBlockDataStatement*
SgBlockDataStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBlockDataStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBlockDataStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBlockDataStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBlockDataStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBlockDataStatement)};
        unsigned long positionInPool{localIndex % SgBlockDataStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBlockDataStatement::pool_size};
        returnPointer = &(((SgBlockDataStatement*)(SgBlockDataStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBlockDataStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBlockDataStatement::pools.begin(); block != SgBlockDataStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBlockDataStatement*>(*block)};
        for (unsigned i = 0; i < SgBlockDataStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBlockDataStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBlockDataStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBlockDataStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgBlockDataStatement::pools.begin(); block != SgBlockDataStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBlockDataStatement*>(*block)};
        for (unsigned i = 0; i < SgBlockDataStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBlockDataStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBlockDataStatement::clearMemoryPool() {
    SgBlockDataStatement* tempPointer{nullptr};
    if (SgBlockDataStatement::pools.empty() == false) {
        auto block{SgBlockDataStatement::pools.begin()};
        SgBlockDataStatement::next_node = reinterpret_cast<SgBlockDataStatement*>(*block);
        for (; block != SgBlockDataStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBlockDataStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBlockDataStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBlockDataStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBlockDataStatement::pool_size-1]);
        }
    }
}

void
SgBlockDataStatement::deleteMemoryPool() {
    for (auto p: SgBlockDataStatement::pools) {
        ROSE_FREE(p);
    }
    SgBlockDataStatement::next_node = nullptr;
    SgBlockDataStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBlockDataStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBlockDataStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBlockDataStatement)};
    for (size_t blockIndex = SgBlockDataStatement::pools.size(); (blockIndex * SgBlockDataStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBlockDataStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBlockDataStatement) = %" PRIuPTR " SgBlockDataStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBlockDataStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBlockDataStatement),SgBlockDataStatement::pool_size);
        }
#endif

        SgBlockDataStatement* pointer{static_cast<SgBlockDataStatement*>(ROSE_MALLOC(SgBlockDataStatement::pool_size * sizeof(SgBlockDataStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBlockDataStatement::pool_size * sizeof(SgBlockDataStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBlockDataStatement::pool_size * sizeof(SgBlockDataStatement));
#endif
        SgBlockDataStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBlockDataStatement::pool_size * sizeof(SgBlockDataStatement), V_SgBlockDataStatement));

        if (SgBlockDataStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBlockDataStatement* blkptr{reinterpret_cast<SgBlockDataStatement*>(SgBlockDataStatement::pools[blockIndex-1])};
                blkptr[SgBlockDataStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBlockDataStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBlockDataStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBlockDataStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBlockDataStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBlockDataStatement::pool_size - 1};
    SgBlockDataStatement* testPointer{reinterpret_cast<SgBlockDataStatement*>(SgBlockDataStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBlockDataStatement::pool_size * (SgBlockDataStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBlockDataStatementStroageClass
 * from its counterpart of SgBlockDataStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBlockDataStatement::initializeStorageClassArray(SgBlockDataStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBlockDataStatement::pools.begin(); block != SgBlockDataStatement::pools.end(); ++block) {
        SgBlockDataStatement* pointer{reinterpret_cast<SgBlockDataStatement*>(*block)};
        for (unsigned i = 0; i < SgBlockDataStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 53533 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAssociateStatement that takes its corresponding StorageClass as parameter
 */
SgAssociateStatement::SgAssociateStatement(const SgAssociateStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 53543 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_associates = storageSource.storageOf_associates.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_associates = p_associates.begin();
    for ( ; i_associates != p_associates.end(); ++i_associates) {
        (*i_associates) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_associates)) );
    }
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAssociateStatement*
SgAssociateStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAssociateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssociateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssociateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssociateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAssociateStatement)};
        unsigned long positionInPool{localIndex % SgAssociateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssociateStatement::pool_size};
        returnPointer = &(((SgAssociateStatement*)(SgAssociateStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAssociateStatement*
SgAssociateStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAssociateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssociateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssociateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssociateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAssociateStatement)};
        unsigned long positionInPool{localIndex % SgAssociateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssociateStatement::pool_size};
        returnPointer = &(((SgAssociateStatement*)(SgAssociateStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAssociateStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAssociateStatement::pools.begin(); block != SgAssociateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssociateStatement*>(*block)};
        for (unsigned i = 0; i < SgAssociateStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAssociateStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAssociateStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAssociateStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgAssociateStatement::pools.begin(); block != SgAssociateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssociateStatement*>(*block)};
        for (unsigned i = 0; i < SgAssociateStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAssociateStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAssociateStatement::clearMemoryPool() {
    SgAssociateStatement* tempPointer{nullptr};
    if (SgAssociateStatement::pools.empty() == false) {
        auto block{SgAssociateStatement::pools.begin()};
        SgAssociateStatement::next_node = reinterpret_cast<SgAssociateStatement*>(*block);
        for (; block != SgAssociateStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAssociateStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAssociateStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAssociateStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAssociateStatement::pool_size-1]);
        }
    }
}

void
SgAssociateStatement::deleteMemoryPool() {
    for (auto p: SgAssociateStatement::pools) {
        ROSE_FREE(p);
    }
    SgAssociateStatement::next_node = nullptr;
    SgAssociateStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAssociateStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAssociateStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssociateStatement)};
    for (size_t blockIndex = SgAssociateStatement::pools.size(); (blockIndex * SgAssociateStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAssociateStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssociateStatement) = %" PRIuPTR " SgAssociateStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAssociateStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssociateStatement),SgAssociateStatement::pool_size);
        }
#endif

        SgAssociateStatement* pointer{static_cast<SgAssociateStatement*>(ROSE_MALLOC(SgAssociateStatement::pool_size * sizeof(SgAssociateStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAssociateStatement::pool_size * sizeof(SgAssociateStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAssociateStatement::pool_size * sizeof(SgAssociateStatement));
#endif
        SgAssociateStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAssociateStatement::pool_size * sizeof(SgAssociateStatement), V_SgAssociateStatement));

        if (SgAssociateStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAssociateStatement* blkptr{reinterpret_cast<SgAssociateStatement*>(SgAssociateStatement::pools[blockIndex-1])};
                blkptr[SgAssociateStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAssociateStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAssociateStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAssociateStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAssociateStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAssociateStatement::pool_size - 1};
    SgAssociateStatement* testPointer{reinterpret_cast<SgAssociateStatement*>(SgAssociateStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAssociateStatement::pool_size * (SgAssociateStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAssociateStatementStroageClass
 * from its counterpart of SgAssociateStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAssociateStatement::initializeStorageClassArray(SgAssociateStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAssociateStatement::pools.begin(); block != SgAssociateStatement::pools.end(); ++block) {
        SgAssociateStatement* pointer{reinterpret_cast<SgAssociateStatement*>(*block)};
        for (unsigned i = 0; i < SgAssociateStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 53766 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFortranDo that takes its corresponding StorageClass as parameter
 */
SgFortranDo::SgFortranDo(const SgFortranDoStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 53776 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_initialization = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initialization));
     p_bound = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_bound));
     p_increment = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_increment));
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();
    p_old_style = storageSource.storageOf_old_style;
    p_has_end_statement = storageSource.storageOf_has_end_statement;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFortranDo*
SgFortranDo::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFortranDo* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranDo)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranDo + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranDo)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranDo)};
        unsigned long positionInPool{localIndex % SgFortranDo::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranDo::pool_size};
        returnPointer = &(((SgFortranDo*)(SgFortranDo::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFortranDo*
SgFortranDo::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFortranDo* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranDo)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranDo + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranDo)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFortranDo)};
        unsigned long positionInPool{localIndex % SgFortranDo::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranDo::pool_size};
        returnPointer = &(((SgFortranDo*)(SgFortranDo::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFortranDo::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFortranDo::pools.begin(); block != SgFortranDo::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranDo*>(*block)};
        for (unsigned i = 0; i < SgFortranDo::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFortranDo*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFortranDo::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFortranDo* pointerOfLinkedList{nullptr};
    for (auto block = SgFortranDo::pools.begin(); block != SgFortranDo::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranDo*>(*block)};
        for (unsigned i = 0; i < SgFortranDo::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFortranDo::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFortranDo::clearMemoryPool() {
    SgFortranDo* tempPointer{nullptr};
    if (SgFortranDo::pools.empty() == false) {
        auto block{SgFortranDo::pools.begin()};
        SgFortranDo::next_node = reinterpret_cast<SgFortranDo*>(*block);
        for (; block != SgFortranDo::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFortranDo*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFortranDo::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFortranDo::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFortranDo::pool_size-1]);
        }
    }
}

void
SgFortranDo::deleteMemoryPool() {
    for (auto p: SgFortranDo::pools) {
        ROSE_FREE(p);
    }
    SgFortranDo::next_node = nullptr;
    SgFortranDo::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFortranDo::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranDo) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranDo)};
    for (size_t blockIndex = SgFortranDo::pools.size(); (blockIndex * SgFortranDo::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranDo) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranDo) = %" PRIuPTR " SgFortranDo::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranDo),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranDo),SgFortranDo::pool_size);
        }
#endif

        SgFortranDo* pointer{static_cast<SgFortranDo*>(ROSE_MALLOC(SgFortranDo::pool_size * sizeof(SgFortranDo)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFortranDo::pool_size * sizeof(SgFortranDo));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFortranDo::pool_size * sizeof(SgFortranDo));
#endif
        SgFortranDo::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFortranDo::pool_size * sizeof(SgFortranDo), V_SgFortranDo));

        if (SgFortranDo::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFortranDo* blkptr{reinterpret_cast<SgFortranDo*>(SgFortranDo::pools[blockIndex-1])};
                blkptr[SgFortranDo::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFortranDo::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFortranDo::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFortranDo::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFortranDo::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFortranDo::pool_size - 1};
    SgFortranDo* testPointer{reinterpret_cast<SgFortranDo*>(SgFortranDo::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFortranDo::pool_size * (SgFortranDo::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFortranDoStroageClass
 * from its counterpart of SgFortranDo. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFortranDo::initializeStorageClassArray(SgFortranDoStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFortranDo::pools.begin(); block != SgFortranDo::pools.end(); ++block) {
        SgFortranDo* pointer{reinterpret_cast<SgFortranDo*>(*block)};
        for (unsigned i = 0; i < SgFortranDo::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 54001 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFortranNonblockedDo that takes its corresponding StorageClass as parameter
 */
SgFortranNonblockedDo::SgFortranNonblockedDo(const SgFortranNonblockedDoStorageClass& storageSource)  : SgFortranDo (storageSource) {

/* #line 54011 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_end_statement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_statement));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFortranNonblockedDo*
SgFortranNonblockedDo::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFortranNonblockedDo* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranNonblockedDo)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranNonblockedDo + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranNonblockedDo)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranNonblockedDo)};
        unsigned long positionInPool{localIndex % SgFortranNonblockedDo::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranNonblockedDo::pool_size};
        returnPointer = &(((SgFortranNonblockedDo*)(SgFortranNonblockedDo::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFortranNonblockedDo*
SgFortranNonblockedDo::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFortranNonblockedDo* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranNonblockedDo)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranNonblockedDo + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranNonblockedDo)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFortranNonblockedDo)};
        unsigned long positionInPool{localIndex % SgFortranNonblockedDo::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranNonblockedDo::pool_size};
        returnPointer = &(((SgFortranNonblockedDo*)(SgFortranNonblockedDo::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFortranNonblockedDo::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFortranNonblockedDo::pools.begin(); block != SgFortranNonblockedDo::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranNonblockedDo*>(*block)};
        for (unsigned i = 0; i < SgFortranNonblockedDo::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFortranNonblockedDo*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFortranNonblockedDo::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFortranNonblockedDo* pointerOfLinkedList{nullptr};
    for (auto block = SgFortranNonblockedDo::pools.begin(); block != SgFortranNonblockedDo::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranNonblockedDo*>(*block)};
        for (unsigned i = 0; i < SgFortranNonblockedDo::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFortranNonblockedDo::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFortranNonblockedDo::clearMemoryPool() {
    SgFortranNonblockedDo* tempPointer{nullptr};
    if (SgFortranNonblockedDo::pools.empty() == false) {
        auto block{SgFortranNonblockedDo::pools.begin()};
        SgFortranNonblockedDo::next_node = reinterpret_cast<SgFortranNonblockedDo*>(*block);
        for (; block != SgFortranNonblockedDo::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFortranNonblockedDo*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFortranNonblockedDo::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFortranNonblockedDo::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFortranNonblockedDo::pool_size-1]);
        }
    }
}

void
SgFortranNonblockedDo::deleteMemoryPool() {
    for (auto p: SgFortranNonblockedDo::pools) {
        ROSE_FREE(p);
    }
    SgFortranNonblockedDo::next_node = nullptr;
    SgFortranNonblockedDo::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFortranNonblockedDo::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranNonblockedDo) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranNonblockedDo)};
    for (size_t blockIndex = SgFortranNonblockedDo::pools.size(); (blockIndex * SgFortranNonblockedDo::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranNonblockedDo) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranNonblockedDo) = %" PRIuPTR " SgFortranNonblockedDo::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranNonblockedDo),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranNonblockedDo),SgFortranNonblockedDo::pool_size);
        }
#endif

        SgFortranNonblockedDo* pointer{static_cast<SgFortranNonblockedDo*>(ROSE_MALLOC(SgFortranNonblockedDo::pool_size * sizeof(SgFortranNonblockedDo)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFortranNonblockedDo::pool_size * sizeof(SgFortranNonblockedDo));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFortranNonblockedDo::pool_size * sizeof(SgFortranNonblockedDo));
#endif
        SgFortranNonblockedDo::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFortranNonblockedDo::pool_size * sizeof(SgFortranNonblockedDo), V_SgFortranNonblockedDo));

        if (SgFortranNonblockedDo::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFortranNonblockedDo* blkptr{reinterpret_cast<SgFortranNonblockedDo*>(SgFortranNonblockedDo::pools[blockIndex-1])};
                blkptr[SgFortranNonblockedDo::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFortranNonblockedDo::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFortranNonblockedDo::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFortranNonblockedDo::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFortranNonblockedDo::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFortranNonblockedDo::pool_size - 1};
    SgFortranNonblockedDo* testPointer{reinterpret_cast<SgFortranNonblockedDo*>(SgFortranNonblockedDo::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFortranNonblockedDo::pool_size * (SgFortranNonblockedDo::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFortranNonblockedDoStroageClass
 * from its counterpart of SgFortranNonblockedDo. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFortranNonblockedDo::initializeStorageClassArray(SgFortranNonblockedDoStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFortranNonblockedDo::pools.begin(); block != SgFortranNonblockedDo::pools.end(); ++block) {
        SgFortranNonblockedDo* pointer{reinterpret_cast<SgFortranNonblockedDo*>(*block)};
        for (unsigned i = 0; i < SgFortranNonblockedDo::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 54229 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgForAllStatement that takes its corresponding StorageClass as parameter
 */
SgForAllStatement::SgForAllStatement(const SgForAllStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 54239 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_forall_header = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_forall_header));
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_has_end_statement = storageSource.storageOf_has_end_statement;
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));
    p_forall_statement_kind = storageSource.storageOf_forall_statement_kind;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgForAllStatement*
SgForAllStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgForAllStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForAllStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForAllStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForAllStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgForAllStatement)};
        unsigned long positionInPool{localIndex % SgForAllStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgForAllStatement::pool_size};
        returnPointer = &(((SgForAllStatement*)(SgForAllStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgForAllStatement*
SgForAllStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgForAllStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForAllStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForAllStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForAllStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgForAllStatement)};
        unsigned long positionInPool{localIndex % SgForAllStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgForAllStatement::pool_size};
        returnPointer = &(((SgForAllStatement*)(SgForAllStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgForAllStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgForAllStatement::pools.begin(); block != SgForAllStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgForAllStatement*>(*block)};
        for (unsigned i = 0; i < SgForAllStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgForAllStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgForAllStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgForAllStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgForAllStatement::pools.begin(); block != SgForAllStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgForAllStatement*>(*block)};
        for (unsigned i = 0; i < SgForAllStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgForAllStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgForAllStatement::clearMemoryPool() {
    SgForAllStatement* tempPointer{nullptr};
    if (SgForAllStatement::pools.empty() == false) {
        auto block{SgForAllStatement::pools.begin()};
        SgForAllStatement::next_node = reinterpret_cast<SgForAllStatement*>(*block);
        for (; block != SgForAllStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgForAllStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgForAllStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgForAllStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgForAllStatement::pool_size-1]);
        }
    }
}

void
SgForAllStatement::deleteMemoryPool() {
    for (auto p: SgForAllStatement::pools) {
        ROSE_FREE(p);
    }
    SgForAllStatement::next_node = nullptr;
    SgForAllStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgForAllStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgForAllStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgForAllStatement)};
    for (size_t blockIndex = SgForAllStatement::pools.size(); (blockIndex * SgForAllStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgForAllStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgForAllStatement) = %" PRIuPTR " SgForAllStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgForAllStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgForAllStatement),SgForAllStatement::pool_size);
        }
#endif

        SgForAllStatement* pointer{static_cast<SgForAllStatement*>(ROSE_MALLOC(SgForAllStatement::pool_size * sizeof(SgForAllStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgForAllStatement::pool_size * sizeof(SgForAllStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgForAllStatement::pool_size * sizeof(SgForAllStatement));
#endif
        SgForAllStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgForAllStatement::pool_size * sizeof(SgForAllStatement), V_SgForAllStatement));

        if (SgForAllStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgForAllStatement* blkptr{reinterpret_cast<SgForAllStatement*>(SgForAllStatement::pools[blockIndex-1])};
                blkptr[SgForAllStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgForAllStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgForAllStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgForAllStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgForAllStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgForAllStatement::pool_size - 1};
    SgForAllStatement* testPointer{reinterpret_cast<SgForAllStatement*>(SgForAllStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgForAllStatement::pool_size * (SgForAllStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgForAllStatementStroageClass
 * from its counterpart of SgForAllStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgForAllStatement::initializeStorageClassArray(SgForAllStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgForAllStatement::pools.begin(); block != SgForAllStatement::pools.end(); ++block) {
        SgForAllStatement* pointer{reinterpret_cast<SgForAllStatement*>(*block)};
        for (unsigned i = 0; i < SgForAllStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 54462 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcForAllStatement that takes its corresponding StorageClass as parameter
 */
SgUpcForAllStatement::SgUpcForAllStatement(const SgUpcForAllStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 54472 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_for_init_stmt = (SgForInitStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_for_init_stmt));
     p_test = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_test));
     p_increment = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_increment));
     p_affinity = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_affinity));
     p_loop_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_loop_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcForAllStatement*
SgUpcForAllStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcForAllStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcForAllStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcForAllStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcForAllStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcForAllStatement)};
        unsigned long positionInPool{localIndex % SgUpcForAllStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcForAllStatement::pool_size};
        returnPointer = &(((SgUpcForAllStatement*)(SgUpcForAllStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcForAllStatement*
SgUpcForAllStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcForAllStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcForAllStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcForAllStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcForAllStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcForAllStatement)};
        unsigned long positionInPool{localIndex % SgUpcForAllStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcForAllStatement::pool_size};
        returnPointer = &(((SgUpcForAllStatement*)(SgUpcForAllStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcForAllStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcForAllStatement::pools.begin(); block != SgUpcForAllStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcForAllStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcForAllStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcForAllStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcForAllStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcForAllStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcForAllStatement::pools.begin(); block != SgUpcForAllStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcForAllStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcForAllStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcForAllStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcForAllStatement::clearMemoryPool() {
    SgUpcForAllStatement* tempPointer{nullptr};
    if (SgUpcForAllStatement::pools.empty() == false) {
        auto block{SgUpcForAllStatement::pools.begin()};
        SgUpcForAllStatement::next_node = reinterpret_cast<SgUpcForAllStatement*>(*block);
        for (; block != SgUpcForAllStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcForAllStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcForAllStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcForAllStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcForAllStatement::pool_size-1]);
        }
    }
}

void
SgUpcForAllStatement::deleteMemoryPool() {
    for (auto p: SgUpcForAllStatement::pools) {
        ROSE_FREE(p);
    }
    SgUpcForAllStatement::next_node = nullptr;
    SgUpcForAllStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcForAllStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcForAllStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcForAllStatement)};
    for (size_t blockIndex = SgUpcForAllStatement::pools.size(); (blockIndex * SgUpcForAllStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcForAllStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcForAllStatement) = %" PRIuPTR " SgUpcForAllStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcForAllStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcForAllStatement),SgUpcForAllStatement::pool_size);
        }
#endif

        SgUpcForAllStatement* pointer{static_cast<SgUpcForAllStatement*>(ROSE_MALLOC(SgUpcForAllStatement::pool_size * sizeof(SgUpcForAllStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcForAllStatement::pool_size * sizeof(SgUpcForAllStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcForAllStatement::pool_size * sizeof(SgUpcForAllStatement));
#endif
        SgUpcForAllStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcForAllStatement::pool_size * sizeof(SgUpcForAllStatement), V_SgUpcForAllStatement));

        if (SgUpcForAllStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcForAllStatement* blkptr{reinterpret_cast<SgUpcForAllStatement*>(SgUpcForAllStatement::pools[blockIndex-1])};
                blkptr[SgUpcForAllStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcForAllStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcForAllStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcForAllStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcForAllStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcForAllStatement::pool_size - 1};
    SgUpcForAllStatement* testPointer{reinterpret_cast<SgUpcForAllStatement*>(SgUpcForAllStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcForAllStatement::pool_size * (SgUpcForAllStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcForAllStatementStroageClass
 * from its counterpart of SgUpcForAllStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcForAllStatement::initializeStorageClassArray(SgUpcForAllStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcForAllStatement::pools.begin(); block != SgUpcForAllStatement::pools.end(); ++block) {
        SgUpcForAllStatement* pointer{reinterpret_cast<SgUpcForAllStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcForAllStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 54694 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCAFWithTeamStatement that takes its corresponding StorageClass as parameter
 */
SgCAFWithTeamStatement::SgCAFWithTeamStatement(const SgCAFWithTeamStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 54704 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_teamId = (SgVarRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_teamId));
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_endHasTeamId = storageSource.storageOf_endHasTeamId;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCAFWithTeamStatement*
SgCAFWithTeamStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCAFWithTeamStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCAFWithTeamStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCAFWithTeamStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCAFWithTeamStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFWithTeamStatement)};
        unsigned long positionInPool{localIndex % SgCAFWithTeamStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCAFWithTeamStatement::pool_size};
        returnPointer = &(((SgCAFWithTeamStatement*)(SgCAFWithTeamStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCAFWithTeamStatement*
SgCAFWithTeamStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCAFWithTeamStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCAFWithTeamStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCAFWithTeamStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCAFWithTeamStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCAFWithTeamStatement)};
        unsigned long positionInPool{localIndex % SgCAFWithTeamStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCAFWithTeamStatement::pool_size};
        returnPointer = &(((SgCAFWithTeamStatement*)(SgCAFWithTeamStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCAFWithTeamStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCAFWithTeamStatement::pools.begin(); block != SgCAFWithTeamStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCAFWithTeamStatement*>(*block)};
        for (unsigned i = 0; i < SgCAFWithTeamStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCAFWithTeamStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCAFWithTeamStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCAFWithTeamStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgCAFWithTeamStatement::pools.begin(); block != SgCAFWithTeamStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCAFWithTeamStatement*>(*block)};
        for (unsigned i = 0; i < SgCAFWithTeamStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCAFWithTeamStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCAFWithTeamStatement::clearMemoryPool() {
    SgCAFWithTeamStatement* tempPointer{nullptr};
    if (SgCAFWithTeamStatement::pools.empty() == false) {
        auto block{SgCAFWithTeamStatement::pools.begin()};
        SgCAFWithTeamStatement::next_node = reinterpret_cast<SgCAFWithTeamStatement*>(*block);
        for (; block != SgCAFWithTeamStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCAFWithTeamStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCAFWithTeamStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCAFWithTeamStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCAFWithTeamStatement::pool_size-1]);
        }
    }
}

void
SgCAFWithTeamStatement::deleteMemoryPool() {
    for (auto p: SgCAFWithTeamStatement::pools) {
        ROSE_FREE(p);
    }
    SgCAFWithTeamStatement::next_node = nullptr;
    SgCAFWithTeamStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCAFWithTeamStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFWithTeamStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCAFWithTeamStatement)};
    for (size_t blockIndex = SgCAFWithTeamStatement::pools.size(); (blockIndex * SgCAFWithTeamStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFWithTeamStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCAFWithTeamStatement) = %" PRIuPTR " SgCAFWithTeamStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFWithTeamStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCAFWithTeamStatement),SgCAFWithTeamStatement::pool_size);
        }
#endif

        SgCAFWithTeamStatement* pointer{static_cast<SgCAFWithTeamStatement*>(ROSE_MALLOC(SgCAFWithTeamStatement::pool_size * sizeof(SgCAFWithTeamStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCAFWithTeamStatement::pool_size * sizeof(SgCAFWithTeamStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCAFWithTeamStatement::pool_size * sizeof(SgCAFWithTeamStatement));
#endif
        SgCAFWithTeamStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCAFWithTeamStatement::pool_size * sizeof(SgCAFWithTeamStatement), V_SgCAFWithTeamStatement));

        if (SgCAFWithTeamStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCAFWithTeamStatement* blkptr{reinterpret_cast<SgCAFWithTeamStatement*>(SgCAFWithTeamStatement::pools[blockIndex-1])};
                blkptr[SgCAFWithTeamStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCAFWithTeamStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCAFWithTeamStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCAFWithTeamStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCAFWithTeamStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCAFWithTeamStatement::pool_size - 1};
    SgCAFWithTeamStatement* testPointer{reinterpret_cast<SgCAFWithTeamStatement*>(SgCAFWithTeamStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCAFWithTeamStatement::pool_size * (SgCAFWithTeamStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCAFWithTeamStatementStroageClass
 * from its counterpart of SgCAFWithTeamStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCAFWithTeamStatement::initializeStorageClassArray(SgCAFWithTeamStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCAFWithTeamStatement::pools.begin(); block != SgCAFWithTeamStatement::pools.end(); ++block) {
        SgCAFWithTeamStatement* pointer{reinterpret_cast<SgCAFWithTeamStatement*>(*block)};
        for (unsigned i = 0; i < SgCAFWithTeamStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 54924 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaForEachStatement that takes its corresponding StorageClass as parameter
 */
SgJavaForEachStatement::SgJavaForEachStatement(const SgJavaForEachStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 54934 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_element = (SgVariableDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_element));
     p_collection = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_collection));
     p_loop_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_loop_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaForEachStatement*
SgJavaForEachStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaForEachStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaForEachStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaForEachStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaForEachStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaForEachStatement)};
        unsigned long positionInPool{localIndex % SgJavaForEachStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaForEachStatement::pool_size};
        returnPointer = &(((SgJavaForEachStatement*)(SgJavaForEachStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaForEachStatement*
SgJavaForEachStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaForEachStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaForEachStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaForEachStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaForEachStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaForEachStatement)};
        unsigned long positionInPool{localIndex % SgJavaForEachStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaForEachStatement::pool_size};
        returnPointer = &(((SgJavaForEachStatement*)(SgJavaForEachStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaForEachStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaForEachStatement::pools.begin(); block != SgJavaForEachStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaForEachStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaForEachStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaForEachStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaForEachStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaForEachStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaForEachStatement::pools.begin(); block != SgJavaForEachStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaForEachStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaForEachStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaForEachStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaForEachStatement::clearMemoryPool() {
    SgJavaForEachStatement* tempPointer{nullptr};
    if (SgJavaForEachStatement::pools.empty() == false) {
        auto block{SgJavaForEachStatement::pools.begin()};
        SgJavaForEachStatement::next_node = reinterpret_cast<SgJavaForEachStatement*>(*block);
        for (; block != SgJavaForEachStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaForEachStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaForEachStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaForEachStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaForEachStatement::pool_size-1]);
        }
    }
}

void
SgJavaForEachStatement::deleteMemoryPool() {
    for (auto p: SgJavaForEachStatement::pools) {
        ROSE_FREE(p);
    }
    SgJavaForEachStatement::next_node = nullptr;
    SgJavaForEachStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaForEachStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaForEachStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaForEachStatement)};
    for (size_t blockIndex = SgJavaForEachStatement::pools.size(); (blockIndex * SgJavaForEachStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaForEachStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaForEachStatement) = %" PRIuPTR " SgJavaForEachStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaForEachStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaForEachStatement),SgJavaForEachStatement::pool_size);
        }
#endif

        SgJavaForEachStatement* pointer{static_cast<SgJavaForEachStatement*>(ROSE_MALLOC(SgJavaForEachStatement::pool_size * sizeof(SgJavaForEachStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaForEachStatement::pool_size * sizeof(SgJavaForEachStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaForEachStatement::pool_size * sizeof(SgJavaForEachStatement));
#endif
        SgJavaForEachStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaForEachStatement::pool_size * sizeof(SgJavaForEachStatement), V_SgJavaForEachStatement));

        if (SgJavaForEachStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaForEachStatement* blkptr{reinterpret_cast<SgJavaForEachStatement*>(SgJavaForEachStatement::pools[blockIndex-1])};
                blkptr[SgJavaForEachStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaForEachStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaForEachStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaForEachStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaForEachStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaForEachStatement::pool_size - 1};
    SgJavaForEachStatement* testPointer{reinterpret_cast<SgJavaForEachStatement*>(SgJavaForEachStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaForEachStatement::pool_size * (SgJavaForEachStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaForEachStatementStroageClass
 * from its counterpart of SgJavaForEachStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaForEachStatement::initializeStorageClassArray(SgJavaForEachStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaForEachStatement::pools.begin(); block != SgJavaForEachStatement::pools.end(); ++block) {
        SgJavaForEachStatement* pointer{reinterpret_cast<SgJavaForEachStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaForEachStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 55154 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaLabelStatement that takes its corresponding StorageClass as parameter
 */
SgJavaLabelStatement::SgJavaLabelStatement(const SgJavaLabelStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 55164 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_label = SgName ( storageSource.storageOf_label);
     p_statement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_statement));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaLabelStatement*
SgJavaLabelStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaLabelStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaLabelStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaLabelStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaLabelStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelStatement)};
        unsigned long positionInPool{localIndex % SgJavaLabelStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaLabelStatement::pool_size};
        returnPointer = &(((SgJavaLabelStatement*)(SgJavaLabelStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaLabelStatement*
SgJavaLabelStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaLabelStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaLabelStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaLabelStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaLabelStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaLabelStatement)};
        unsigned long positionInPool{localIndex % SgJavaLabelStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaLabelStatement::pool_size};
        returnPointer = &(((SgJavaLabelStatement*)(SgJavaLabelStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaLabelStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaLabelStatement::pools.begin(); block != SgJavaLabelStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaLabelStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaLabelStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaLabelStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaLabelStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaLabelStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaLabelStatement::pools.begin(); block != SgJavaLabelStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaLabelStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaLabelStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaLabelStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaLabelStatement::clearMemoryPool() {
    SgJavaLabelStatement* tempPointer{nullptr};
    if (SgJavaLabelStatement::pools.empty() == false) {
        auto block{SgJavaLabelStatement::pools.begin()};
        SgJavaLabelStatement::next_node = reinterpret_cast<SgJavaLabelStatement*>(*block);
        for (; block != SgJavaLabelStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaLabelStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaLabelStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaLabelStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaLabelStatement::pool_size-1]);
        }
    }
}

void
SgJavaLabelStatement::deleteMemoryPool() {
    for (auto p: SgJavaLabelStatement::pools) {
        ROSE_FREE(p);
    }
    SgJavaLabelStatement::next_node = nullptr;
    SgJavaLabelStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaLabelStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaLabelStatement)};
    for (size_t blockIndex = SgJavaLabelStatement::pools.size(); (blockIndex * SgJavaLabelStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaLabelStatement) = %" PRIuPTR " SgJavaLabelStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaLabelStatement),SgJavaLabelStatement::pool_size);
        }
#endif

        SgJavaLabelStatement* pointer{static_cast<SgJavaLabelStatement*>(ROSE_MALLOC(SgJavaLabelStatement::pool_size * sizeof(SgJavaLabelStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaLabelStatement::pool_size * sizeof(SgJavaLabelStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaLabelStatement::pool_size * sizeof(SgJavaLabelStatement));
#endif
        SgJavaLabelStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaLabelStatement::pool_size * sizeof(SgJavaLabelStatement), V_SgJavaLabelStatement));

        if (SgJavaLabelStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaLabelStatement* blkptr{reinterpret_cast<SgJavaLabelStatement*>(SgJavaLabelStatement::pools[blockIndex-1])};
                blkptr[SgJavaLabelStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaLabelStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaLabelStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaLabelStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaLabelStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaLabelStatement::pool_size - 1};
    SgJavaLabelStatement* testPointer{reinterpret_cast<SgJavaLabelStatement*>(SgJavaLabelStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaLabelStatement::pool_size * (SgJavaLabelStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaLabelStatementStroageClass
 * from its counterpart of SgJavaLabelStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaLabelStatement::initializeStorageClassArray(SgJavaLabelStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaLabelStatement::pools.begin(); block != SgJavaLabelStatement::pools.end(); ++block) {
        SgJavaLabelStatement* pointer{reinterpret_cast<SgJavaLabelStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaLabelStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 55383 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMatlabForStatement that takes its corresponding StorageClass as parameter
 */
SgMatlabForStatement::SgMatlabForStatement(const SgMatlabForStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 55393 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_index = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_index));
     p_range = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_range));
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMatlabForStatement*
SgMatlabForStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMatlabForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatlabForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatlabForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatlabForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMatlabForStatement)};
        unsigned long positionInPool{localIndex % SgMatlabForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMatlabForStatement::pool_size};
        returnPointer = &(((SgMatlabForStatement*)(SgMatlabForStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMatlabForStatement*
SgMatlabForStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMatlabForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatlabForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatlabForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatlabForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMatlabForStatement)};
        unsigned long positionInPool{localIndex % SgMatlabForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMatlabForStatement::pool_size};
        returnPointer = &(((SgMatlabForStatement*)(SgMatlabForStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMatlabForStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMatlabForStatement::pools.begin(); block != SgMatlabForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMatlabForStatement*>(*block)};
        for (unsigned i = 0; i < SgMatlabForStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMatlabForStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMatlabForStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMatlabForStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgMatlabForStatement::pools.begin(); block != SgMatlabForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMatlabForStatement*>(*block)};
        for (unsigned i = 0; i < SgMatlabForStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMatlabForStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMatlabForStatement::clearMemoryPool() {
    SgMatlabForStatement* tempPointer{nullptr};
    if (SgMatlabForStatement::pools.empty() == false) {
        auto block{SgMatlabForStatement::pools.begin()};
        SgMatlabForStatement::next_node = reinterpret_cast<SgMatlabForStatement*>(*block);
        for (; block != SgMatlabForStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMatlabForStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMatlabForStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMatlabForStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMatlabForStatement::pool_size-1]);
        }
    }
}

void
SgMatlabForStatement::deleteMemoryPool() {
    for (auto p: SgMatlabForStatement::pools) {
        ROSE_FREE(p);
    }
    SgMatlabForStatement::next_node = nullptr;
    SgMatlabForStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMatlabForStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMatlabForStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatlabForStatement)};
    for (size_t blockIndex = SgMatlabForStatement::pools.size(); (blockIndex * SgMatlabForStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMatlabForStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatlabForStatement) = %" PRIuPTR " SgMatlabForStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMatlabForStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatlabForStatement),SgMatlabForStatement::pool_size);
        }
#endif

        SgMatlabForStatement* pointer{static_cast<SgMatlabForStatement*>(ROSE_MALLOC(SgMatlabForStatement::pool_size * sizeof(SgMatlabForStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMatlabForStatement::pool_size * sizeof(SgMatlabForStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMatlabForStatement::pool_size * sizeof(SgMatlabForStatement));
#endif
        SgMatlabForStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMatlabForStatement::pool_size * sizeof(SgMatlabForStatement), V_SgMatlabForStatement));

        if (SgMatlabForStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMatlabForStatement* blkptr{reinterpret_cast<SgMatlabForStatement*>(SgMatlabForStatement::pools[blockIndex-1])};
                blkptr[SgMatlabForStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMatlabForStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMatlabForStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMatlabForStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMatlabForStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMatlabForStatement::pool_size - 1};
    SgMatlabForStatement* testPointer{reinterpret_cast<SgMatlabForStatement*>(SgMatlabForStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMatlabForStatement::pool_size * (SgMatlabForStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMatlabForStatementStroageClass
 * from its counterpart of SgMatlabForStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMatlabForStatement::initializeStorageClassArray(SgMatlabForStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMatlabForStatement::pools.begin(); block != SgMatlabForStatement::pools.end(); ++block) {
        SgMatlabForStatement* pointer{reinterpret_cast<SgMatlabForStatement*>(*block)};
        for (unsigned i = 0; i < SgMatlabForStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 55613 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionParameterScope that takes its corresponding StorageClass as parameter
 */
SgFunctionParameterScope::SgFunctionParameterScope(const SgFunctionParameterScopeStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 55623 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_declarations = storageSource.storageOf_declarations.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_declarations = p_declarations.begin();
    for ( ; i_declarations != p_declarations.end(); ++i_declarations) {
        (*i_declarations) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_declarations)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionParameterScope*
SgFunctionParameterScope::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionParameterScope* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterScope)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterScope + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterScope)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterScope)};
        unsigned long positionInPool{localIndex % SgFunctionParameterScope::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterScope::pool_size};
        returnPointer = &(((SgFunctionParameterScope*)(SgFunctionParameterScope::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionParameterScope*
SgFunctionParameterScope::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionParameterScope* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterScope)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterScope + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterScope)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionParameterScope)};
        unsigned long positionInPool{localIndex % SgFunctionParameterScope::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterScope::pool_size};
        returnPointer = &(((SgFunctionParameterScope*)(SgFunctionParameterScope::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionParameterScope::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionParameterScope::pools.begin(); block != SgFunctionParameterScope::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterScope*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterScope::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionParameterScope*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionParameterScope::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionParameterScope* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionParameterScope::pools.begin(); block != SgFunctionParameterScope::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterScope*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterScope::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionParameterScope::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionParameterScope::clearMemoryPool() {
    SgFunctionParameterScope* tempPointer{nullptr};
    if (SgFunctionParameterScope::pools.empty() == false) {
        auto block{SgFunctionParameterScope::pools.begin()};
        SgFunctionParameterScope::next_node = reinterpret_cast<SgFunctionParameterScope*>(*block);
        for (; block != SgFunctionParameterScope::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionParameterScope*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionParameterScope::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionParameterScope::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionParameterScope::pool_size-1]);
        }
    }
}

void
SgFunctionParameterScope::deleteMemoryPool() {
    for (auto p: SgFunctionParameterScope::pools) {
        ROSE_FREE(p);
    }
    SgFunctionParameterScope::next_node = nullptr;
    SgFunctionParameterScope::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionParameterScope::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterScope) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterScope)};
    for (size_t blockIndex = SgFunctionParameterScope::pools.size(); (blockIndex * SgFunctionParameterScope::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterScope) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterScope) = %" PRIuPTR " SgFunctionParameterScope::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterScope),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterScope),SgFunctionParameterScope::pool_size);
        }
#endif

        SgFunctionParameterScope* pointer{static_cast<SgFunctionParameterScope*>(ROSE_MALLOC(SgFunctionParameterScope::pool_size * sizeof(SgFunctionParameterScope)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionParameterScope::pool_size * sizeof(SgFunctionParameterScope));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionParameterScope::pool_size * sizeof(SgFunctionParameterScope));
#endif
        SgFunctionParameterScope::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionParameterScope::pool_size * sizeof(SgFunctionParameterScope), V_SgFunctionParameterScope));

        if (SgFunctionParameterScope::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionParameterScope* blkptr{reinterpret_cast<SgFunctionParameterScope*>(SgFunctionParameterScope::pools[blockIndex-1])};
                blkptr[SgFunctionParameterScope::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionParameterScope::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionParameterScope::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionParameterScope::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionParameterScope::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionParameterScope::pool_size - 1};
    SgFunctionParameterScope* testPointer{reinterpret_cast<SgFunctionParameterScope*>(SgFunctionParameterScope::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionParameterScope::pool_size * (SgFunctionParameterScope::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionParameterScopeStroageClass
 * from its counterpart of SgFunctionParameterScope. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionParameterScope::initializeStorageClassArray(SgFunctionParameterScopeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionParameterScope::pools.begin(); block != SgFunctionParameterScope::pools.end(); ++block) {
        SgFunctionParameterScope* pointer{reinterpret_cast<SgFunctionParameterScope*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterScope::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 55845 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDeclarationScope that takes its corresponding StorageClass as parameter
 */
SgDeclarationScope::SgDeclarationScope(const SgDeclarationScopeStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 55855 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDeclarationScope*
SgDeclarationScope::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDeclarationScope* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationScope)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationScope + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationScope)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationScope)};
        unsigned long positionInPool{localIndex % SgDeclarationScope::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclarationScope::pool_size};
        returnPointer = &(((SgDeclarationScope*)(SgDeclarationScope::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDeclarationScope*
SgDeclarationScope::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDeclarationScope* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationScope)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationScope + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationScope)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDeclarationScope)};
        unsigned long positionInPool{localIndex % SgDeclarationScope::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclarationScope::pool_size};
        returnPointer = &(((SgDeclarationScope*)(SgDeclarationScope::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDeclarationScope::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDeclarationScope::pools.begin(); block != SgDeclarationScope::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclarationScope*>(*block)};
        for (unsigned i = 0; i < SgDeclarationScope::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDeclarationScope*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDeclarationScope::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDeclarationScope* pointerOfLinkedList{nullptr};
    for (auto block = SgDeclarationScope::pools.begin(); block != SgDeclarationScope::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclarationScope*>(*block)};
        for (unsigned i = 0; i < SgDeclarationScope::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDeclarationScope::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDeclarationScope::clearMemoryPool() {
    SgDeclarationScope* tempPointer{nullptr};
    if (SgDeclarationScope::pools.empty() == false) {
        auto block{SgDeclarationScope::pools.begin()};
        SgDeclarationScope::next_node = reinterpret_cast<SgDeclarationScope*>(*block);
        for (; block != SgDeclarationScope::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDeclarationScope*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDeclarationScope::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDeclarationScope::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDeclarationScope::pool_size-1]);
        }
    }
}

void
SgDeclarationScope::deleteMemoryPool() {
    for (auto p: SgDeclarationScope::pools) {
        ROSE_FREE(p);
    }
    SgDeclarationScope::next_node = nullptr;
    SgDeclarationScope::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDeclarationScope::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationScope) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationScope)};
    for (size_t blockIndex = SgDeclarationScope::pools.size(); (blockIndex * SgDeclarationScope::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationScope) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationScope) = %" PRIuPTR " SgDeclarationScope::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationScope),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationScope),SgDeclarationScope::pool_size);
        }
#endif

        SgDeclarationScope* pointer{static_cast<SgDeclarationScope*>(ROSE_MALLOC(SgDeclarationScope::pool_size * sizeof(SgDeclarationScope)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDeclarationScope::pool_size * sizeof(SgDeclarationScope));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDeclarationScope::pool_size * sizeof(SgDeclarationScope));
#endif
        SgDeclarationScope::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDeclarationScope::pool_size * sizeof(SgDeclarationScope), V_SgDeclarationScope));

        if (SgDeclarationScope::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDeclarationScope* blkptr{reinterpret_cast<SgDeclarationScope*>(SgDeclarationScope::pools[blockIndex-1])};
                blkptr[SgDeclarationScope::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDeclarationScope::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDeclarationScope::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDeclarationScope::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDeclarationScope::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDeclarationScope::pool_size - 1};
    SgDeclarationScope* testPointer{reinterpret_cast<SgDeclarationScope*>(SgDeclarationScope::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDeclarationScope::pool_size * (SgDeclarationScope::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDeclarationScopeStroageClass
 * from its counterpart of SgDeclarationScope. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDeclarationScope::initializeStorageClassArray(SgDeclarationScopeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDeclarationScope::pools.begin(); block != SgDeclarationScope::pools.end(); ++block) {
        SgDeclarationScope* pointer{reinterpret_cast<SgDeclarationScope*>(*block)};
        for (unsigned i = 0; i < SgDeclarationScope::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 56072 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRangeBasedForStatement that takes its corresponding StorageClass as parameter
 */
SgRangeBasedForStatement::SgRangeBasedForStatement(const SgRangeBasedForStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 56082 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_iterator_declaration = (SgVariableDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_iterator_declaration));
     p_range_declaration = (SgVariableDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_range_declaration));
     p_begin_declaration = (SgVariableDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_begin_declaration));
     p_end_declaration = (SgVariableDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_declaration));
     p_not_equal_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_not_equal_expression));
     p_increment_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_increment_expression));
     p_loop_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_loop_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRangeBasedForStatement*
SgRangeBasedForStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRangeBasedForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeBasedForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeBasedForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeBasedForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeBasedForStatement)};
        unsigned long positionInPool{localIndex % SgRangeBasedForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRangeBasedForStatement::pool_size};
        returnPointer = &(((SgRangeBasedForStatement*)(SgRangeBasedForStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRangeBasedForStatement*
SgRangeBasedForStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRangeBasedForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeBasedForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeBasedForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeBasedForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRangeBasedForStatement)};
        unsigned long positionInPool{localIndex % SgRangeBasedForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRangeBasedForStatement::pool_size};
        returnPointer = &(((SgRangeBasedForStatement*)(SgRangeBasedForStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRangeBasedForStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRangeBasedForStatement::pools.begin(); block != SgRangeBasedForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRangeBasedForStatement*>(*block)};
        for (unsigned i = 0; i < SgRangeBasedForStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRangeBasedForStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRangeBasedForStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRangeBasedForStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgRangeBasedForStatement::pools.begin(); block != SgRangeBasedForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRangeBasedForStatement*>(*block)};
        for (unsigned i = 0; i < SgRangeBasedForStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRangeBasedForStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRangeBasedForStatement::clearMemoryPool() {
    SgRangeBasedForStatement* tempPointer{nullptr};
    if (SgRangeBasedForStatement::pools.empty() == false) {
        auto block{SgRangeBasedForStatement::pools.begin()};
        SgRangeBasedForStatement::next_node = reinterpret_cast<SgRangeBasedForStatement*>(*block);
        for (; block != SgRangeBasedForStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRangeBasedForStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRangeBasedForStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRangeBasedForStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRangeBasedForStatement::pool_size-1]);
        }
    }
}

void
SgRangeBasedForStatement::deleteMemoryPool() {
    for (auto p: SgRangeBasedForStatement::pools) {
        ROSE_FREE(p);
    }
    SgRangeBasedForStatement::next_node = nullptr;
    SgRangeBasedForStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRangeBasedForStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeBasedForStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeBasedForStatement)};
    for (size_t blockIndex = SgRangeBasedForStatement::pools.size(); (blockIndex * SgRangeBasedForStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeBasedForStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeBasedForStatement) = %" PRIuPTR " SgRangeBasedForStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeBasedForStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeBasedForStatement),SgRangeBasedForStatement::pool_size);
        }
#endif

        SgRangeBasedForStatement* pointer{static_cast<SgRangeBasedForStatement*>(ROSE_MALLOC(SgRangeBasedForStatement::pool_size * sizeof(SgRangeBasedForStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRangeBasedForStatement::pool_size * sizeof(SgRangeBasedForStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRangeBasedForStatement::pool_size * sizeof(SgRangeBasedForStatement));
#endif
        SgRangeBasedForStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRangeBasedForStatement::pool_size * sizeof(SgRangeBasedForStatement), V_SgRangeBasedForStatement));

        if (SgRangeBasedForStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRangeBasedForStatement* blkptr{reinterpret_cast<SgRangeBasedForStatement*>(SgRangeBasedForStatement::pools[blockIndex-1])};
                blkptr[SgRangeBasedForStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRangeBasedForStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRangeBasedForStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRangeBasedForStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRangeBasedForStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRangeBasedForStatement::pool_size - 1};
    SgRangeBasedForStatement* testPointer{reinterpret_cast<SgRangeBasedForStatement*>(SgRangeBasedForStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRangeBasedForStatement::pool_size * (SgRangeBasedForStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRangeBasedForStatementStroageClass
 * from its counterpart of SgRangeBasedForStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRangeBasedForStatement::initializeStorageClassArray(SgRangeBasedForStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRangeBasedForStatement::pools.begin(); block != SgRangeBasedForStatement::pools.end(); ++block) {
        SgRangeBasedForStatement* pointer{reinterpret_cast<SgRangeBasedForStatement*>(*block)};
        for (unsigned i = 0; i < SgRangeBasedForStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 56306 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialForThenStatement that takes its corresponding StorageClass as parameter
 */
SgJovialForThenStatement::SgJovialForThenStatement(const SgJovialForThenStatementStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 56316 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_initialization = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initialization));
     p_while_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_while_expression));
     p_by_or_then_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_by_or_then_expression));
     p_loop_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_loop_body));
    p_loop_statement_type = storageSource.storageOf_loop_statement_type;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialForThenStatement*
SgJovialForThenStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialForThenStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialForThenStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialForThenStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialForThenStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialForThenStatement)};
        unsigned long positionInPool{localIndex % SgJovialForThenStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialForThenStatement::pool_size};
        returnPointer = &(((SgJovialForThenStatement*)(SgJovialForThenStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialForThenStatement*
SgJovialForThenStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialForThenStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialForThenStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialForThenStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialForThenStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialForThenStatement)};
        unsigned long positionInPool{localIndex % SgJovialForThenStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialForThenStatement::pool_size};
        returnPointer = &(((SgJovialForThenStatement*)(SgJovialForThenStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialForThenStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialForThenStatement::pools.begin(); block != SgJovialForThenStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialForThenStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialForThenStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialForThenStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialForThenStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialForThenStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialForThenStatement::pools.begin(); block != SgJovialForThenStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialForThenStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialForThenStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialForThenStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialForThenStatement::clearMemoryPool() {
    SgJovialForThenStatement* tempPointer{nullptr};
    if (SgJovialForThenStatement::pools.empty() == false) {
        auto block{SgJovialForThenStatement::pools.begin()};
        SgJovialForThenStatement::next_node = reinterpret_cast<SgJovialForThenStatement*>(*block);
        for (; block != SgJovialForThenStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialForThenStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialForThenStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialForThenStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialForThenStatement::pool_size-1]);
        }
    }
}

void
SgJovialForThenStatement::deleteMemoryPool() {
    for (auto p: SgJovialForThenStatement::pools) {
        ROSE_FREE(p);
    }
    SgJovialForThenStatement::next_node = nullptr;
    SgJovialForThenStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialForThenStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialForThenStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialForThenStatement)};
    for (size_t blockIndex = SgJovialForThenStatement::pools.size(); (blockIndex * SgJovialForThenStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialForThenStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialForThenStatement) = %" PRIuPTR " SgJovialForThenStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialForThenStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialForThenStatement),SgJovialForThenStatement::pool_size);
        }
#endif

        SgJovialForThenStatement* pointer{static_cast<SgJovialForThenStatement*>(ROSE_MALLOC(SgJovialForThenStatement::pool_size * sizeof(SgJovialForThenStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialForThenStatement::pool_size * sizeof(SgJovialForThenStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialForThenStatement::pool_size * sizeof(SgJovialForThenStatement));
#endif
        SgJovialForThenStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialForThenStatement::pool_size * sizeof(SgJovialForThenStatement), V_SgJovialForThenStatement));

        if (SgJovialForThenStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialForThenStatement* blkptr{reinterpret_cast<SgJovialForThenStatement*>(SgJovialForThenStatement::pools[blockIndex-1])};
                blkptr[SgJovialForThenStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialForThenStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialForThenStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialForThenStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialForThenStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialForThenStatement::pool_size - 1};
    SgJovialForThenStatement* testPointer{reinterpret_cast<SgJovialForThenStatement*>(SgJovialForThenStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialForThenStatement::pool_size * (SgJovialForThenStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialForThenStatementStroageClass
 * from its counterpart of SgJovialForThenStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialForThenStatement::initializeStorageClassArray(SgJovialForThenStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialForThenStatement::pools.begin(); block != SgJovialForThenStatement::pools.end(); ++block) {
        SgJovialForThenStatement* pointer{reinterpret_cast<SgJovialForThenStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialForThenStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 56538 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaAcceptStmt that takes its corresponding StorageClass as parameter
 */
SgAdaAcceptStmt::SgAdaAcceptStmt(const SgAdaAcceptStmtStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 56548 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_parameterScope = (SgFunctionParameterScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameterScope));
     p_parameterList = (SgFunctionParameterList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameterList));
     p_entry = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entry));
     p_index = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_index));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaAcceptStmt*
SgAdaAcceptStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaAcceptStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAcceptStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAcceptStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAcceptStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAcceptStmt)};
        unsigned long positionInPool{localIndex % SgAdaAcceptStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAcceptStmt::pool_size};
        returnPointer = &(((SgAdaAcceptStmt*)(SgAdaAcceptStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaAcceptStmt*
SgAdaAcceptStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaAcceptStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAcceptStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAcceptStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAcceptStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaAcceptStmt)};
        unsigned long positionInPool{localIndex % SgAdaAcceptStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAcceptStmt::pool_size};
        returnPointer = &(((SgAdaAcceptStmt*)(SgAdaAcceptStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaAcceptStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaAcceptStmt::pools.begin(); block != SgAdaAcceptStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAcceptStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaAcceptStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaAcceptStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaAcceptStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaAcceptStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaAcceptStmt::pools.begin(); block != SgAdaAcceptStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAcceptStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaAcceptStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaAcceptStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaAcceptStmt::clearMemoryPool() {
    SgAdaAcceptStmt* tempPointer{nullptr};
    if (SgAdaAcceptStmt::pools.empty() == false) {
        auto block{SgAdaAcceptStmt::pools.begin()};
        SgAdaAcceptStmt::next_node = reinterpret_cast<SgAdaAcceptStmt*>(*block);
        for (; block != SgAdaAcceptStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaAcceptStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaAcceptStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaAcceptStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaAcceptStmt::pool_size-1]);
        }
    }
}

void
SgAdaAcceptStmt::deleteMemoryPool() {
    for (auto p: SgAdaAcceptStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaAcceptStmt::next_node = nullptr;
    SgAdaAcceptStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaAcceptStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAcceptStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAcceptStmt)};
    for (size_t blockIndex = SgAdaAcceptStmt::pools.size(); (blockIndex * SgAdaAcceptStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAcceptStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAcceptStmt) = %" PRIuPTR " SgAdaAcceptStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAcceptStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAcceptStmt),SgAdaAcceptStmt::pool_size);
        }
#endif

        SgAdaAcceptStmt* pointer{static_cast<SgAdaAcceptStmt*>(ROSE_MALLOC(SgAdaAcceptStmt::pool_size * sizeof(SgAdaAcceptStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaAcceptStmt::pool_size * sizeof(SgAdaAcceptStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaAcceptStmt::pool_size * sizeof(SgAdaAcceptStmt));
#endif
        SgAdaAcceptStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaAcceptStmt::pool_size * sizeof(SgAdaAcceptStmt), V_SgAdaAcceptStmt));

        if (SgAdaAcceptStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaAcceptStmt* blkptr{reinterpret_cast<SgAdaAcceptStmt*>(SgAdaAcceptStmt::pools[blockIndex-1])};
                blkptr[SgAdaAcceptStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaAcceptStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaAcceptStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaAcceptStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaAcceptStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaAcceptStmt::pool_size - 1};
    SgAdaAcceptStmt* testPointer{reinterpret_cast<SgAdaAcceptStmt*>(SgAdaAcceptStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaAcceptStmt::pool_size * (SgAdaAcceptStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaAcceptStmtStroageClass
 * from its counterpart of SgAdaAcceptStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaAcceptStmt::initializeStorageClassArray(SgAdaAcceptStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaAcceptStmt::pools.begin(); block != SgAdaAcceptStmt::pools.end(); ++block) {
        SgAdaAcceptStmt* pointer{reinterpret_cast<SgAdaAcceptStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaAcceptStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 56770 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaPackageSpec that takes its corresponding StorageClass as parameter
 */
SgAdaPackageSpec::SgAdaPackageSpec(const SgAdaPackageSpecStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 56780 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_hasPrivate = storageSource.storageOf_hasPrivate;
    p_declarations = storageSource.storageOf_declarations.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_declarations = p_declarations.begin();
    for ( ; i_declarations != p_declarations.end(); ++i_declarations) {
        (*i_declarations) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_declarations)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaPackageSpec*
SgAdaPackageSpec::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaPackageSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpec)};
        unsigned long positionInPool{localIndex % SgAdaPackageSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageSpec::pool_size};
        returnPointer = &(((SgAdaPackageSpec*)(SgAdaPackageSpec::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaPackageSpec*
SgAdaPackageSpec::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaPackageSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaPackageSpec)};
        unsigned long positionInPool{localIndex % SgAdaPackageSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageSpec::pool_size};
        returnPointer = &(((SgAdaPackageSpec*)(SgAdaPackageSpec::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaPackageSpec::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaPackageSpec::pools.begin(); block != SgAdaPackageSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSpec::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaPackageSpec*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaPackageSpec::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaPackageSpec* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaPackageSpec::pools.begin(); block != SgAdaPackageSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSpec::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaPackageSpec::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaPackageSpec::clearMemoryPool() {
    SgAdaPackageSpec* tempPointer{nullptr};
    if (SgAdaPackageSpec::pools.empty() == false) {
        auto block{SgAdaPackageSpec::pools.begin()};
        SgAdaPackageSpec::next_node = reinterpret_cast<SgAdaPackageSpec*>(*block);
        for (; block != SgAdaPackageSpec::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaPackageSpec*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaPackageSpec::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaPackageSpec::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaPackageSpec::pool_size-1]);
        }
    }
}

void
SgAdaPackageSpec::deleteMemoryPool() {
    for (auto p: SgAdaPackageSpec::pools) {
        ROSE_FREE(p);
    }
    SgAdaPackageSpec::next_node = nullptr;
    SgAdaPackageSpec::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaPackageSpec::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpec) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSpec)};
    for (size_t blockIndex = SgAdaPackageSpec::pools.size(); (blockIndex * SgAdaPackageSpec::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpec) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSpec) = %" PRIuPTR " SgAdaPackageSpec::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpec),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSpec),SgAdaPackageSpec::pool_size);
        }
#endif

        SgAdaPackageSpec* pointer{static_cast<SgAdaPackageSpec*>(ROSE_MALLOC(SgAdaPackageSpec::pool_size * sizeof(SgAdaPackageSpec)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaPackageSpec::pool_size * sizeof(SgAdaPackageSpec));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaPackageSpec::pool_size * sizeof(SgAdaPackageSpec));
#endif
        SgAdaPackageSpec::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaPackageSpec::pool_size * sizeof(SgAdaPackageSpec), V_SgAdaPackageSpec));

        if (SgAdaPackageSpec::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaPackageSpec* blkptr{reinterpret_cast<SgAdaPackageSpec*>(SgAdaPackageSpec::pools[blockIndex-1])};
                blkptr[SgAdaPackageSpec::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaPackageSpec::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaPackageSpec::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaPackageSpec::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaPackageSpec::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaPackageSpec::pool_size - 1};
    SgAdaPackageSpec* testPointer{reinterpret_cast<SgAdaPackageSpec*>(SgAdaPackageSpec::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaPackageSpec::pool_size * (SgAdaPackageSpec::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaPackageSpecStroageClass
 * from its counterpart of SgAdaPackageSpec. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaPackageSpec::initializeStorageClassArray(SgAdaPackageSpecStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaPackageSpec::pools.begin(); block != SgAdaPackageSpec::pools.end(); ++block) {
        SgAdaPackageSpec* pointer{reinterpret_cast<SgAdaPackageSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSpec::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 57003 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaPackageBody that takes its corresponding StorageClass as parameter
 */
SgAdaPackageBody::SgAdaPackageBody(const SgAdaPackageBodyStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 57013 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_statements = storageSource.storageOf_statements.rebuildDataStoredInEasyStorageClass();
    SgStatementPtrList::iterator i_statements = p_statements.begin();
    for ( ; i_statements != p_statements.end(); ++i_statements) {
        (*i_statements) = (SgStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_statements)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaPackageBody*
SgAdaPackageBody::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaPackageBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageBody)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBody)};
        unsigned long positionInPool{localIndex % SgAdaPackageBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageBody::pool_size};
        returnPointer = &(((SgAdaPackageBody*)(SgAdaPackageBody::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaPackageBody*
SgAdaPackageBody::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaPackageBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageBody)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaPackageBody)};
        unsigned long positionInPool{localIndex % SgAdaPackageBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageBody::pool_size};
        returnPointer = &(((SgAdaPackageBody*)(SgAdaPackageBody::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaPackageBody::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaPackageBody::pools.begin(); block != SgAdaPackageBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageBody*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageBody::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaPackageBody*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaPackageBody::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaPackageBody* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaPackageBody::pools.begin(); block != SgAdaPackageBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageBody*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageBody::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaPackageBody::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaPackageBody::clearMemoryPool() {
    SgAdaPackageBody* tempPointer{nullptr};
    if (SgAdaPackageBody::pools.empty() == false) {
        auto block{SgAdaPackageBody::pools.begin()};
        SgAdaPackageBody::next_node = reinterpret_cast<SgAdaPackageBody*>(*block);
        for (; block != SgAdaPackageBody::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaPackageBody*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaPackageBody::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaPackageBody::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaPackageBody::pool_size-1]);
        }
    }
}

void
SgAdaPackageBody::deleteMemoryPool() {
    for (auto p: SgAdaPackageBody::pools) {
        ROSE_FREE(p);
    }
    SgAdaPackageBody::next_node = nullptr;
    SgAdaPackageBody::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaPackageBody::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBody) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageBody)};
    for (size_t blockIndex = SgAdaPackageBody::pools.size(); (blockIndex * SgAdaPackageBody::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBody) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageBody) = %" PRIuPTR " SgAdaPackageBody::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBody),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageBody),SgAdaPackageBody::pool_size);
        }
#endif

        SgAdaPackageBody* pointer{static_cast<SgAdaPackageBody*>(ROSE_MALLOC(SgAdaPackageBody::pool_size * sizeof(SgAdaPackageBody)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaPackageBody::pool_size * sizeof(SgAdaPackageBody));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaPackageBody::pool_size * sizeof(SgAdaPackageBody));
#endif
        SgAdaPackageBody::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaPackageBody::pool_size * sizeof(SgAdaPackageBody), V_SgAdaPackageBody));

        if (SgAdaPackageBody::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaPackageBody* blkptr{reinterpret_cast<SgAdaPackageBody*>(SgAdaPackageBody::pools[blockIndex-1])};
                blkptr[SgAdaPackageBody::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaPackageBody::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaPackageBody::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaPackageBody::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaPackageBody::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaPackageBody::pool_size - 1};
    SgAdaPackageBody* testPointer{reinterpret_cast<SgAdaPackageBody*>(SgAdaPackageBody::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaPackageBody::pool_size * (SgAdaPackageBody::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaPackageBodyStroageClass
 * from its counterpart of SgAdaPackageBody. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaPackageBody::initializeStorageClassArray(SgAdaPackageBodyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaPackageBody::pools.begin(); block != SgAdaPackageBody::pools.end(); ++block) {
        SgAdaPackageBody* pointer{reinterpret_cast<SgAdaPackageBody*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageBody::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 57235 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskSpec that takes its corresponding StorageClass as parameter
 */
SgAdaTaskSpec::SgAdaTaskSpec(const SgAdaTaskSpecStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 57245 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAdaTaskBody*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_declarations = storageSource.storageOf_declarations.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_declarations = p_declarations.begin();
    for ( ; i_declarations != p_declarations.end(); ++i_declarations) {
        (*i_declarations) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_declarations)) );
    }
    p_hasPrivate = storageSource.storageOf_hasPrivate;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskSpec*
SgAdaTaskSpec::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpec)};
        unsigned long positionInPool{localIndex % SgAdaTaskSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskSpec::pool_size};
        returnPointer = &(((SgAdaTaskSpec*)(SgAdaTaskSpec::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskSpec*
SgAdaTaskSpec::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskSpec)};
        unsigned long positionInPool{localIndex % SgAdaTaskSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskSpec::pool_size};
        returnPointer = &(((SgAdaTaskSpec*)(SgAdaTaskSpec::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskSpec::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskSpec::pools.begin(); block != SgAdaTaskSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSpec::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskSpec*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskSpec::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskSpec* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskSpec::pools.begin(); block != SgAdaTaskSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSpec::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskSpec::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskSpec::clearMemoryPool() {
    SgAdaTaskSpec* tempPointer{nullptr};
    if (SgAdaTaskSpec::pools.empty() == false) {
        auto block{SgAdaTaskSpec::pools.begin()};
        SgAdaTaskSpec::next_node = reinterpret_cast<SgAdaTaskSpec*>(*block);
        for (; block != SgAdaTaskSpec::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskSpec*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskSpec::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskSpec::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskSpec::pool_size-1]);
        }
    }
}

void
SgAdaTaskSpec::deleteMemoryPool() {
    for (auto p: SgAdaTaskSpec::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskSpec::next_node = nullptr;
    SgAdaTaskSpec::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskSpec::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpec) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSpec)};
    for (size_t blockIndex = SgAdaTaskSpec::pools.size(); (blockIndex * SgAdaTaskSpec::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpec) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSpec) = %" PRIuPTR " SgAdaTaskSpec::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpec),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSpec),SgAdaTaskSpec::pool_size);
        }
#endif

        SgAdaTaskSpec* pointer{static_cast<SgAdaTaskSpec*>(ROSE_MALLOC(SgAdaTaskSpec::pool_size * sizeof(SgAdaTaskSpec)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskSpec::pool_size * sizeof(SgAdaTaskSpec));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskSpec::pool_size * sizeof(SgAdaTaskSpec));
#endif
        SgAdaTaskSpec::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskSpec::pool_size * sizeof(SgAdaTaskSpec), V_SgAdaTaskSpec));

        if (SgAdaTaskSpec::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskSpec* blkptr{reinterpret_cast<SgAdaTaskSpec*>(SgAdaTaskSpec::pools[blockIndex-1])};
                blkptr[SgAdaTaskSpec::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskSpec::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskSpec::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskSpec::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskSpec::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskSpec::pool_size - 1};
    SgAdaTaskSpec* testPointer{reinterpret_cast<SgAdaTaskSpec*>(SgAdaTaskSpec::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskSpec::pool_size * (SgAdaTaskSpec::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskSpecStroageClass
 * from its counterpart of SgAdaTaskSpec. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskSpec::initializeStorageClassArray(SgAdaTaskSpecStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskSpec::pools.begin(); block != SgAdaTaskSpec::pools.end(); ++block) {
        SgAdaTaskSpec* pointer{reinterpret_cast<SgAdaTaskSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSpec::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 57469 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskBody that takes its corresponding StorageClass as parameter
 */
SgAdaTaskBody::SgAdaTaskBody(const SgAdaTaskBodyStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 57479 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_spec = (SgAdaTaskSpec*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_spec));
    p_statements = storageSource.storageOf_statements.rebuildDataStoredInEasyStorageClass();
    SgStatementPtrList::iterator i_statements = p_statements.begin();
    for ( ; i_statements != p_statements.end(); ++i_statements) {
        (*i_statements) = (SgStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_statements)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskBody*
SgAdaTaskBody::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskBody)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBody)};
        unsigned long positionInPool{localIndex % SgAdaTaskBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskBody::pool_size};
        returnPointer = &(((SgAdaTaskBody*)(SgAdaTaskBody::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskBody*
SgAdaTaskBody::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskBody)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskBody)};
        unsigned long positionInPool{localIndex % SgAdaTaskBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskBody::pool_size};
        returnPointer = &(((SgAdaTaskBody*)(SgAdaTaskBody::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskBody::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskBody::pools.begin(); block != SgAdaTaskBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskBody*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskBody::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskBody*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskBody::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskBody* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskBody::pools.begin(); block != SgAdaTaskBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskBody*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskBody::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskBody::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskBody::clearMemoryPool() {
    SgAdaTaskBody* tempPointer{nullptr};
    if (SgAdaTaskBody::pools.empty() == false) {
        auto block{SgAdaTaskBody::pools.begin()};
        SgAdaTaskBody::next_node = reinterpret_cast<SgAdaTaskBody*>(*block);
        for (; block != SgAdaTaskBody::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskBody*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskBody::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskBody::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskBody::pool_size-1]);
        }
    }
}

void
SgAdaTaskBody::deleteMemoryPool() {
    for (auto p: SgAdaTaskBody::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskBody::next_node = nullptr;
    SgAdaTaskBody::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskBody::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBody) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskBody)};
    for (size_t blockIndex = SgAdaTaskBody::pools.size(); (blockIndex * SgAdaTaskBody::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBody) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskBody) = %" PRIuPTR " SgAdaTaskBody::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBody),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskBody),SgAdaTaskBody::pool_size);
        }
#endif

        SgAdaTaskBody* pointer{static_cast<SgAdaTaskBody*>(ROSE_MALLOC(SgAdaTaskBody::pool_size * sizeof(SgAdaTaskBody)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskBody::pool_size * sizeof(SgAdaTaskBody));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskBody::pool_size * sizeof(SgAdaTaskBody));
#endif
        SgAdaTaskBody::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskBody::pool_size * sizeof(SgAdaTaskBody), V_SgAdaTaskBody));

        if (SgAdaTaskBody::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskBody* blkptr{reinterpret_cast<SgAdaTaskBody*>(SgAdaTaskBody::pools[blockIndex-1])};
                blkptr[SgAdaTaskBody::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskBody::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskBody::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskBody::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskBody::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskBody::pool_size - 1};
    SgAdaTaskBody* testPointer{reinterpret_cast<SgAdaTaskBody*>(SgAdaTaskBody::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskBody::pool_size * (SgAdaTaskBody::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskBodyStroageClass
 * from its counterpart of SgAdaTaskBody. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskBody::initializeStorageClassArray(SgAdaTaskBodyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskBody::pools.begin(); block != SgAdaTaskBody::pools.end(); ++block) {
        SgAdaTaskBody* pointer{reinterpret_cast<SgAdaTaskBody*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskBody::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 57702 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedBody that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedBody::SgAdaProtectedBody(const SgAdaProtectedBodyStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 57712 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_spec = (SgAdaProtectedSpec*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_spec));
    p_statements = storageSource.storageOf_statements.rebuildDataStoredInEasyStorageClass();
    SgStatementPtrList::iterator i_statements = p_statements.begin();
    for ( ; i_statements != p_statements.end(); ++i_statements) {
        (*i_statements) = (SgStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_statements)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedBody*
SgAdaProtectedBody::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedBody)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBody)};
        unsigned long positionInPool{localIndex % SgAdaProtectedBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedBody::pool_size};
        returnPointer = &(((SgAdaProtectedBody*)(SgAdaProtectedBody::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedBody*
SgAdaProtectedBody::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedBody* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedBody)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedBody + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedBody)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedBody)};
        unsigned long positionInPool{localIndex % SgAdaProtectedBody::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedBody::pool_size};
        returnPointer = &(((SgAdaProtectedBody*)(SgAdaProtectedBody::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedBody::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedBody::pools.begin(); block != SgAdaProtectedBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedBody*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedBody::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedBody*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedBody::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedBody* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedBody::pools.begin(); block != SgAdaProtectedBody::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedBody*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedBody::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedBody::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedBody::clearMemoryPool() {
    SgAdaProtectedBody* tempPointer{nullptr};
    if (SgAdaProtectedBody::pools.empty() == false) {
        auto block{SgAdaProtectedBody::pools.begin()};
        SgAdaProtectedBody::next_node = reinterpret_cast<SgAdaProtectedBody*>(*block);
        for (; block != SgAdaProtectedBody::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedBody*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedBody::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedBody::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedBody::pool_size-1]);
        }
    }
}

void
SgAdaProtectedBody::deleteMemoryPool() {
    for (auto p: SgAdaProtectedBody::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedBody::next_node = nullptr;
    SgAdaProtectedBody::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedBody::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBody) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedBody)};
    for (size_t blockIndex = SgAdaProtectedBody::pools.size(); (blockIndex * SgAdaProtectedBody::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBody) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedBody) = %" PRIuPTR " SgAdaProtectedBody::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBody),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedBody),SgAdaProtectedBody::pool_size);
        }
#endif

        SgAdaProtectedBody* pointer{static_cast<SgAdaProtectedBody*>(ROSE_MALLOC(SgAdaProtectedBody::pool_size * sizeof(SgAdaProtectedBody)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedBody::pool_size * sizeof(SgAdaProtectedBody));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedBody::pool_size * sizeof(SgAdaProtectedBody));
#endif
        SgAdaProtectedBody::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedBody::pool_size * sizeof(SgAdaProtectedBody), V_SgAdaProtectedBody));

        if (SgAdaProtectedBody::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedBody* blkptr{reinterpret_cast<SgAdaProtectedBody*>(SgAdaProtectedBody::pools[blockIndex-1])};
                blkptr[SgAdaProtectedBody::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedBody::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedBody::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedBody::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedBody::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedBody::pool_size - 1};
    SgAdaProtectedBody* testPointer{reinterpret_cast<SgAdaProtectedBody*>(SgAdaProtectedBody::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedBody::pool_size * (SgAdaProtectedBody::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedBodyStroageClass
 * from its counterpart of SgAdaProtectedBody. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedBody::initializeStorageClassArray(SgAdaProtectedBodyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedBody::pools.begin(); block != SgAdaProtectedBody::pools.end(); ++block) {
        SgAdaProtectedBody* pointer{reinterpret_cast<SgAdaProtectedBody*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedBody::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 57935 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedSpec that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedSpec::SgAdaProtectedSpec(const SgAdaProtectedSpecStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 57945 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAdaProtectedBody*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_declarations = storageSource.storageOf_declarations.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_declarations = p_declarations.begin();
    for ( ; i_declarations != p_declarations.end(); ++i_declarations) {
        (*i_declarations) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_declarations)) );
    }
    p_hasPrivate = storageSource.storageOf_hasPrivate;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedSpec*
SgAdaProtectedSpec::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpec)};
        unsigned long positionInPool{localIndex % SgAdaProtectedSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedSpec::pool_size};
        returnPointer = &(((SgAdaProtectedSpec*)(SgAdaProtectedSpec::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedSpec*
SgAdaProtectedSpec::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedSpec)};
        unsigned long positionInPool{localIndex % SgAdaProtectedSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedSpec::pool_size};
        returnPointer = &(((SgAdaProtectedSpec*)(SgAdaProtectedSpec::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedSpec::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedSpec::pools.begin(); block != SgAdaProtectedSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSpec::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedSpec*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedSpec::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedSpec* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedSpec::pools.begin(); block != SgAdaProtectedSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSpec::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedSpec::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedSpec::clearMemoryPool() {
    SgAdaProtectedSpec* tempPointer{nullptr};
    if (SgAdaProtectedSpec::pools.empty() == false) {
        auto block{SgAdaProtectedSpec::pools.begin()};
        SgAdaProtectedSpec::next_node = reinterpret_cast<SgAdaProtectedSpec*>(*block);
        for (; block != SgAdaProtectedSpec::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedSpec*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedSpec::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedSpec::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedSpec::pool_size-1]);
        }
    }
}

void
SgAdaProtectedSpec::deleteMemoryPool() {
    for (auto p: SgAdaProtectedSpec::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedSpec::next_node = nullptr;
    SgAdaProtectedSpec::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedSpec::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpec) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSpec)};
    for (size_t blockIndex = SgAdaProtectedSpec::pools.size(); (blockIndex * SgAdaProtectedSpec::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpec) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSpec) = %" PRIuPTR " SgAdaProtectedSpec::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpec),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSpec),SgAdaProtectedSpec::pool_size);
        }
#endif

        SgAdaProtectedSpec* pointer{static_cast<SgAdaProtectedSpec*>(ROSE_MALLOC(SgAdaProtectedSpec::pool_size * sizeof(SgAdaProtectedSpec)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedSpec::pool_size * sizeof(SgAdaProtectedSpec));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedSpec::pool_size * sizeof(SgAdaProtectedSpec));
#endif
        SgAdaProtectedSpec::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedSpec::pool_size * sizeof(SgAdaProtectedSpec), V_SgAdaProtectedSpec));

        if (SgAdaProtectedSpec::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedSpec* blkptr{reinterpret_cast<SgAdaProtectedSpec*>(SgAdaProtectedSpec::pools[blockIndex-1])};
                blkptr[SgAdaProtectedSpec::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedSpec::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedSpec::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedSpec::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedSpec::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedSpec::pool_size - 1};
    SgAdaProtectedSpec* testPointer{reinterpret_cast<SgAdaProtectedSpec*>(SgAdaProtectedSpec::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedSpec::pool_size * (SgAdaProtectedSpec::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedSpecStroageClass
 * from its counterpart of SgAdaProtectedSpec. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedSpec::initializeStorageClassArray(SgAdaProtectedSpecStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedSpec::pools.begin(); block != SgAdaProtectedSpec::pools.end(); ++block) {
        SgAdaProtectedSpec* pointer{reinterpret_cast<SgAdaProtectedSpec*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSpec::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 58169 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaGenericDefn that takes its corresponding StorageClass as parameter
 */
SgAdaGenericDefn::SgAdaGenericDefn(const SgAdaGenericDefnStorageClass& storageSource)  : SgScopeStatement (storageSource) {

/* #line 58179 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_declarations = storageSource.storageOf_declarations.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_declarations = p_declarations.begin();
    for ( ; i_declarations != p_declarations.end(); ++i_declarations) {
        (*i_declarations) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_declarations)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaGenericDefn*
SgAdaGenericDefn::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaGenericDefn* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericDefn)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericDefn + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericDefn)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDefn)};
        unsigned long positionInPool{localIndex % SgAdaGenericDefn::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericDefn::pool_size};
        returnPointer = &(((SgAdaGenericDefn*)(SgAdaGenericDefn::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaGenericDefn*
SgAdaGenericDefn::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaGenericDefn* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericDefn)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericDefn + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericDefn)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaGenericDefn)};
        unsigned long positionInPool{localIndex % SgAdaGenericDefn::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericDefn::pool_size};
        returnPointer = &(((SgAdaGenericDefn*)(SgAdaGenericDefn::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaGenericDefn::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaGenericDefn::pools.begin(); block != SgAdaGenericDefn::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericDefn*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericDefn::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaGenericDefn*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaGenericDefn::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaGenericDefn* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaGenericDefn::pools.begin(); block != SgAdaGenericDefn::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericDefn*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericDefn::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaGenericDefn::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaGenericDefn::clearMemoryPool() {
    SgAdaGenericDefn* tempPointer{nullptr};
    if (SgAdaGenericDefn::pools.empty() == false) {
        auto block{SgAdaGenericDefn::pools.begin()};
        SgAdaGenericDefn::next_node = reinterpret_cast<SgAdaGenericDefn*>(*block);
        for (; block != SgAdaGenericDefn::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaGenericDefn*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaGenericDefn::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaGenericDefn::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaGenericDefn::pool_size-1]);
        }
    }
}

void
SgAdaGenericDefn::deleteMemoryPool() {
    for (auto p: SgAdaGenericDefn::pools) {
        ROSE_FREE(p);
    }
    SgAdaGenericDefn::next_node = nullptr;
    SgAdaGenericDefn::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaGenericDefn::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDefn) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericDefn)};
    for (size_t blockIndex = SgAdaGenericDefn::pools.size(); (blockIndex * SgAdaGenericDefn::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDefn) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericDefn) = %" PRIuPTR " SgAdaGenericDefn::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDefn),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericDefn),SgAdaGenericDefn::pool_size);
        }
#endif

        SgAdaGenericDefn* pointer{static_cast<SgAdaGenericDefn*>(ROSE_MALLOC(SgAdaGenericDefn::pool_size * sizeof(SgAdaGenericDefn)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaGenericDefn::pool_size * sizeof(SgAdaGenericDefn));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaGenericDefn::pool_size * sizeof(SgAdaGenericDefn));
#endif
        SgAdaGenericDefn::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaGenericDefn::pool_size * sizeof(SgAdaGenericDefn), V_SgAdaGenericDefn));

        if (SgAdaGenericDefn::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaGenericDefn* blkptr{reinterpret_cast<SgAdaGenericDefn*>(SgAdaGenericDefn::pools[blockIndex-1])};
                blkptr[SgAdaGenericDefn::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaGenericDefn::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaGenericDefn::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaGenericDefn::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaGenericDefn::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaGenericDefn::pool_size - 1};
    SgAdaGenericDefn* testPointer{reinterpret_cast<SgAdaGenericDefn*>(SgAdaGenericDefn::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaGenericDefn::pool_size * (SgAdaGenericDefn::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaGenericDefnStroageClass
 * from its counterpart of SgAdaGenericDefn. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaGenericDefn::initializeStorageClassArray(SgAdaGenericDefnStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaGenericDefn::pools.begin(); block != SgAdaGenericDefn::pools.end(); ++block) {
        SgAdaGenericDefn* pointer{reinterpret_cast<SgAdaGenericDefn*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericDefn::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 58401 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionTypeTable that takes its corresponding StorageClass as parameter
 */
SgFunctionTypeTable::SgFunctionTypeTable(const SgFunctionTypeTableStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 58411 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_function_type_table = (SgSymbolTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_function_type_table));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionTypeTable*
SgFunctionTypeTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeTable)};
        unsigned long positionInPool{localIndex % SgFunctionTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionTypeTable::pool_size};
        returnPointer = &(((SgFunctionTypeTable*)(SgFunctionTypeTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionTypeTable*
SgFunctionTypeTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionTypeTable)};
        unsigned long positionInPool{localIndex % SgFunctionTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionTypeTable::pool_size};
        returnPointer = &(((SgFunctionTypeTable*)(SgFunctionTypeTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionTypeTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionTypeTable::pools.begin(); block != SgFunctionTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionTypeTable*>(*block)};
        for (unsigned i = 0; i < SgFunctionTypeTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionTypeTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionTypeTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionTypeTable* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionTypeTable::pools.begin(); block != SgFunctionTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionTypeTable*>(*block)};
        for (unsigned i = 0; i < SgFunctionTypeTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionTypeTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionTypeTable::clearMemoryPool() {
    SgFunctionTypeTable* tempPointer{nullptr};
    if (SgFunctionTypeTable::pools.empty() == false) {
        auto block{SgFunctionTypeTable::pools.begin()};
        SgFunctionTypeTable::next_node = reinterpret_cast<SgFunctionTypeTable*>(*block);
        for (; block != SgFunctionTypeTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionTypeTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionTypeTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionTypeTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionTypeTable::pool_size-1]);
        }
    }
}

void
SgFunctionTypeTable::deleteMemoryPool() {
    for (auto p: SgFunctionTypeTable::pools) {
        ROSE_FREE(p);
    }
    SgFunctionTypeTable::next_node = nullptr;
    SgFunctionTypeTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionTypeTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionTypeTable)};
    for (size_t blockIndex = SgFunctionTypeTable::pools.size(); (blockIndex * SgFunctionTypeTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionTypeTable) = %" PRIuPTR " SgFunctionTypeTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionTypeTable),SgFunctionTypeTable::pool_size);
        }
#endif

        SgFunctionTypeTable* pointer{static_cast<SgFunctionTypeTable*>(ROSE_MALLOC(SgFunctionTypeTable::pool_size * sizeof(SgFunctionTypeTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionTypeTable::pool_size * sizeof(SgFunctionTypeTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionTypeTable::pool_size * sizeof(SgFunctionTypeTable));
#endif
        SgFunctionTypeTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionTypeTable::pool_size * sizeof(SgFunctionTypeTable), V_SgFunctionTypeTable));

        if (SgFunctionTypeTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionTypeTable* blkptr{reinterpret_cast<SgFunctionTypeTable*>(SgFunctionTypeTable::pools[blockIndex-1])};
                blkptr[SgFunctionTypeTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionTypeTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionTypeTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionTypeTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionTypeTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionTypeTable::pool_size - 1};
    SgFunctionTypeTable* testPointer{reinterpret_cast<SgFunctionTypeTable*>(SgFunctionTypeTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionTypeTable::pool_size * (SgFunctionTypeTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionTypeTableStroageClass
 * from its counterpart of SgFunctionTypeTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionTypeTable::initializeStorageClassArray(SgFunctionTypeTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionTypeTable::pools.begin(); block != SgFunctionTypeTable::pools.end(); ++block) {
        SgFunctionTypeTable* pointer{reinterpret_cast<SgFunctionTypeTable*>(*block)};
        for (unsigned i = 0; i < SgFunctionTypeTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 58629 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDeclarationStatement that takes its corresponding StorageClass as parameter
 */
SgDeclarationStatement::SgDeclarationStatement(const SgDeclarationStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 58639 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_decl_attributes = storageSource.storageOf_decl_attributes;
    p_linkage = storageSource.storageOf_linkage.rebuildDataStoredInEasyStorageClass();
    p_declarationModifier = SgDeclarationModifier ( storageSource.storageOf_declarationModifier);
    p_nameOnly = storageSource.storageOf_nameOnly;
    p_forward = storageSource.storageOf_forward;
    p_externBrace = storageSource.storageOf_externBrace;
    p_skipElaborateType = storageSource.storageOf_skipElaborateType;
     p_definingDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definingDeclaration));
     p_firstNondefiningDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_firstNondefiningDeclaration));
    p_qualifiedNameList = storageSource.storageOf_qualifiedNameList.rebuildDataStoredInEasyStorageClass();
    SgQualifiedNamePtrList::iterator i_qualifiedNameList = p_qualifiedNameList.begin();
    for ( ; i_qualifiedNameList != p_qualifiedNameList.end(); ++i_qualifiedNameList) {
        (*i_qualifiedNameList) = (SgQualifiedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_qualifiedNameList)) );
    }
    p_binding_label = storageSource.storageOf_binding_label.rebuildDataStoredInEasyStorageClass();
     p_declarationScope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declarationScope));
    p_unparse_template_ast = storageSource.storageOf_unparse_template_ast;
     p_adaAspects = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_adaAspects));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDeclarationStatement*
SgDeclarationStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclarationStatement::pool_size};
        returnPointer = &(((SgDeclarationStatement*)(SgDeclarationStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDeclarationStatement*
SgDeclarationStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeclarationStatement::pool_size};
        returnPointer = &(((SgDeclarationStatement*)(SgDeclarationStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDeclarationStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDeclarationStatement::pools.begin(); block != SgDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgDeclarationStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDeclarationStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDeclarationStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDeclarationStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgDeclarationStatement::pools.begin(); block != SgDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgDeclarationStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDeclarationStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDeclarationStatement::clearMemoryPool() {
    SgDeclarationStatement* tempPointer{nullptr};
    if (SgDeclarationStatement::pools.empty() == false) {
        auto block{SgDeclarationStatement::pools.begin()};
        SgDeclarationStatement::next_node = reinterpret_cast<SgDeclarationStatement*>(*block);
        for (; block != SgDeclarationStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDeclarationStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDeclarationStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDeclarationStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDeclarationStatement::pool_size-1]);
        }
    }
}

void
SgDeclarationStatement::deleteMemoryPool() {
    for (auto p: SgDeclarationStatement::pools) {
        ROSE_FREE(p);
    }
    SgDeclarationStatement::next_node = nullptr;
    SgDeclarationStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDeclarationStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationStatement)};
    for (size_t blockIndex = SgDeclarationStatement::pools.size(); (blockIndex * SgDeclarationStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationStatement) = %" PRIuPTR " SgDeclarationStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDeclarationStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeclarationStatement),SgDeclarationStatement::pool_size);
        }
#endif

        SgDeclarationStatement* pointer{static_cast<SgDeclarationStatement*>(ROSE_MALLOC(SgDeclarationStatement::pool_size * sizeof(SgDeclarationStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDeclarationStatement::pool_size * sizeof(SgDeclarationStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDeclarationStatement::pool_size * sizeof(SgDeclarationStatement));
#endif
        SgDeclarationStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDeclarationStatement::pool_size * sizeof(SgDeclarationStatement), V_SgDeclarationStatement));

        if (SgDeclarationStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDeclarationStatement* blkptr{reinterpret_cast<SgDeclarationStatement*>(SgDeclarationStatement::pools[blockIndex-1])};
                blkptr[SgDeclarationStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDeclarationStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDeclarationStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDeclarationStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDeclarationStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDeclarationStatement::pool_size - 1};
    SgDeclarationStatement* testPointer{reinterpret_cast<SgDeclarationStatement*>(SgDeclarationStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDeclarationStatement::pool_size * (SgDeclarationStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDeclarationStatementStroageClass
 * from its counterpart of SgDeclarationStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDeclarationStatement::initializeStorageClassArray(SgDeclarationStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDeclarationStatement::pools.begin(); block != SgDeclarationStatement::pools.end(); ++block) {
        SgDeclarationStatement* pointer{reinterpret_cast<SgDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgDeclarationStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 58874 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionParameterList that takes its corresponding StorageClass as parameter
 */
SgFunctionParameterList::SgFunctionParameterList(const SgFunctionParameterListStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 58884 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_args = storageSource.storageOf_args.rebuildDataStoredInEasyStorageClass();
    SgInitializedNamePtrList::iterator i_args = p_args.begin();
    for ( ; i_args != p_args.end(); ++i_args) {
        (*i_args) = (SgInitializedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_args)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionParameterList*
SgFunctionParameterList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionParameterList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterList)};
        unsigned long positionInPool{localIndex % SgFunctionParameterList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterList::pool_size};
        returnPointer = &(((SgFunctionParameterList*)(SgFunctionParameterList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionParameterList*
SgFunctionParameterList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionParameterList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionParameterList)};
        unsigned long positionInPool{localIndex % SgFunctionParameterList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterList::pool_size};
        returnPointer = &(((SgFunctionParameterList*)(SgFunctionParameterList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionParameterList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionParameterList::pools.begin(); block != SgFunctionParameterList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterList*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionParameterList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionParameterList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionParameterList* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionParameterList::pools.begin(); block != SgFunctionParameterList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterList*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionParameterList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionParameterList::clearMemoryPool() {
    SgFunctionParameterList* tempPointer{nullptr};
    if (SgFunctionParameterList::pools.empty() == false) {
        auto block{SgFunctionParameterList::pools.begin()};
        SgFunctionParameterList::next_node = reinterpret_cast<SgFunctionParameterList*>(*block);
        for (; block != SgFunctionParameterList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionParameterList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionParameterList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionParameterList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionParameterList::pool_size-1]);
        }
    }
}

void
SgFunctionParameterList::deleteMemoryPool() {
    for (auto p: SgFunctionParameterList::pools) {
        ROSE_FREE(p);
    }
    SgFunctionParameterList::next_node = nullptr;
    SgFunctionParameterList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionParameterList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterList)};
    for (size_t blockIndex = SgFunctionParameterList::pools.size(); (blockIndex * SgFunctionParameterList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterList) = %" PRIuPTR " SgFunctionParameterList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterList),SgFunctionParameterList::pool_size);
        }
#endif

        SgFunctionParameterList* pointer{static_cast<SgFunctionParameterList*>(ROSE_MALLOC(SgFunctionParameterList::pool_size * sizeof(SgFunctionParameterList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionParameterList::pool_size * sizeof(SgFunctionParameterList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionParameterList::pool_size * sizeof(SgFunctionParameterList));
#endif
        SgFunctionParameterList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionParameterList::pool_size * sizeof(SgFunctionParameterList), V_SgFunctionParameterList));

        if (SgFunctionParameterList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionParameterList* blkptr{reinterpret_cast<SgFunctionParameterList*>(SgFunctionParameterList::pools[blockIndex-1])};
                blkptr[SgFunctionParameterList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionParameterList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionParameterList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionParameterList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionParameterList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionParameterList::pool_size - 1};
    SgFunctionParameterList* testPointer{reinterpret_cast<SgFunctionParameterList*>(SgFunctionParameterList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionParameterList::pool_size * (SgFunctionParameterList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionParameterListStroageClass
 * from its counterpart of SgFunctionParameterList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionParameterList::initializeStorageClassArray(SgFunctionParameterListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionParameterList::pools.begin(); block != SgFunctionParameterList::pools.end(); ++block) {
        SgFunctionParameterList* pointer{reinterpret_cast<SgFunctionParameterList*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 59106 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVariableDeclaration that takes its corresponding StorageClass as parameter
 */
SgVariableDeclaration::SgVariableDeclaration(const SgVariableDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 59116 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_baseTypeDefiningDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_baseTypeDefiningDeclaration));
    p_variables = storageSource.storageOf_variables.rebuildDataStoredInEasyStorageClass();
    SgInitializedNamePtrList::iterator i_variables = p_variables.begin();
    for ( ; i_variables != p_variables.end(); ++i_variables) {
        (*i_variables) = (SgInitializedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_variables)) );
    }
    p_variableDeclarationContainsBaseTypeDefiningDeclaration = storageSource.storageOf_variableDeclarationContainsBaseTypeDefiningDeclaration;
    p_specialization = storageSource.storageOf_specialization;
    p_requiresGlobalNameQualificationOnType = storageSource.storageOf_requiresGlobalNameQualificationOnType;
    p_gnu_extension_section = storageSource.storageOf_gnu_extension_section.rebuildDataStoredInEasyStorageClass();
    p_gnu_extension_alias = storageSource.storageOf_gnu_extension_alias.rebuildDataStoredInEasyStorageClass();
    p_gnu_extension_initialization_priority = storageSource.storageOf_gnu_extension_initialization_priority;
    p_gnu_extension_visability = storageSource.storageOf_gnu_extension_visability;
    p_gnu_extension_declaration_attribute = storageSource.storageOf_gnu_extension_declaration_attribute;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_isAssociatedWithDeclarationList = storageSource.storageOf_isAssociatedWithDeclarationList;
    p_isFirstDeclarationOfDeclarationList = storageSource.storageOf_isFirstDeclarationOfDeclarationList;
    p_is_thread_local = storageSource.storageOf_is_thread_local;
    p_is_constexpr = storageSource.storageOf_is_constexpr;
    p_builtFromUseOnly = storageSource.storageOf_builtFromUseOnly;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVariableDeclaration*
SgVariableDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVariableDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDeclaration)};
        unsigned long positionInPool{localIndex % SgVariableDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariableDeclaration::pool_size};
        returnPointer = &(((SgVariableDeclaration*)(SgVariableDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVariableDeclaration*
SgVariableDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVariableDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVariableDeclaration)};
        unsigned long positionInPool{localIndex % SgVariableDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariableDeclaration::pool_size};
        returnPointer = &(((SgVariableDeclaration*)(SgVariableDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVariableDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVariableDeclaration::pools.begin(); block != SgVariableDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariableDeclaration*>(*block)};
        for (unsigned i = 0; i < SgVariableDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVariableDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVariableDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVariableDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgVariableDeclaration::pools.begin(); block != SgVariableDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariableDeclaration*>(*block)};
        for (unsigned i = 0; i < SgVariableDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVariableDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVariableDeclaration::clearMemoryPool() {
    SgVariableDeclaration* tempPointer{nullptr};
    if (SgVariableDeclaration::pools.empty() == false) {
        auto block{SgVariableDeclaration::pools.begin()};
        SgVariableDeclaration::next_node = reinterpret_cast<SgVariableDeclaration*>(*block);
        for (; block != SgVariableDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVariableDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVariableDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVariableDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVariableDeclaration::pool_size-1]);
        }
    }
}

void
SgVariableDeclaration::deleteMemoryPool() {
    for (auto p: SgVariableDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgVariableDeclaration::next_node = nullptr;
    SgVariableDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVariableDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableDeclaration)};
    for (size_t blockIndex = SgVariableDeclaration::pools.size(); (blockIndex * SgVariableDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableDeclaration) = %" PRIuPTR " SgVariableDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableDeclaration),SgVariableDeclaration::pool_size);
        }
#endif

        SgVariableDeclaration* pointer{static_cast<SgVariableDeclaration*>(ROSE_MALLOC(SgVariableDeclaration::pool_size * sizeof(SgVariableDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVariableDeclaration::pool_size * sizeof(SgVariableDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVariableDeclaration::pool_size * sizeof(SgVariableDeclaration));
#endif
        SgVariableDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVariableDeclaration::pool_size * sizeof(SgVariableDeclaration), V_SgVariableDeclaration));

        if (SgVariableDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVariableDeclaration* blkptr{reinterpret_cast<SgVariableDeclaration*>(SgVariableDeclaration::pools[blockIndex-1])};
                blkptr[SgVariableDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVariableDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVariableDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVariableDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVariableDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVariableDeclaration::pool_size - 1};
    SgVariableDeclaration* testPointer{reinterpret_cast<SgVariableDeclaration*>(SgVariableDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVariableDeclaration::pool_size * (SgVariableDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVariableDeclarationStroageClass
 * from its counterpart of SgVariableDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVariableDeclaration::initializeStorageClassArray(SgVariableDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVariableDeclaration::pools.begin(); block != SgVariableDeclaration::pools.end(); ++block) {
        SgVariableDeclaration* pointer{reinterpret_cast<SgVariableDeclaration*>(*block)};
        for (unsigned i = 0; i < SgVariableDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 59355 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateVariableDeclaration that takes its corresponding StorageClass as parameter
 */
SgTemplateVariableDeclaration::SgTemplateVariableDeclaration(const SgTemplateVariableDeclarationStorageClass& storageSource)  : SgVariableDeclaration (storageSource) {

/* #line 59365 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateParameters = storageSource.storageOf_templateParameters.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_templateParameters = p_templateParameters.begin();
    for ( ; i_templateParameters != p_templateParameters.end(); ++i_templateParameters) {
        (*i_templateParameters) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateParameters)) );
    }
    p_templateSpecializationArguments = storageSource.storageOf_templateSpecializationArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateSpecializationArguments = p_templateSpecializationArguments.begin();
    for ( ; i_templateSpecializationArguments != p_templateSpecializationArguments.end(); ++i_templateSpecializationArguments) {
        (*i_templateSpecializationArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateSpecializationArguments)) );
    }
    p_string = SgName ( storageSource.storageOf_string);
     p_nonreal_decl_scope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_decl_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateVariableDeclaration*
SgTemplateVariableDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateVariableDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateVariableDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateVariableDeclaration::pool_size};
        returnPointer = &(((SgTemplateVariableDeclaration*)(SgTemplateVariableDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateVariableDeclaration*
SgTemplateVariableDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateVariableDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateVariableDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateVariableDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateVariableDeclaration::pool_size};
        returnPointer = &(((SgTemplateVariableDeclaration*)(SgTemplateVariableDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateVariableDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateVariableDeclaration::pools.begin(); block != SgTemplateVariableDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateVariableDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateVariableDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateVariableDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateVariableDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateVariableDeclaration::pools.begin(); block != SgTemplateVariableDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateVariableDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateVariableDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateVariableDeclaration::clearMemoryPool() {
    SgTemplateVariableDeclaration* tempPointer{nullptr};
    if (SgTemplateVariableDeclaration::pools.empty() == false) {
        auto block{SgTemplateVariableDeclaration::pools.begin()};
        SgTemplateVariableDeclaration::next_node = reinterpret_cast<SgTemplateVariableDeclaration*>(*block);
        for (; block != SgTemplateVariableDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateVariableDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateVariableDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateVariableDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateVariableDeclaration::pool_size-1]);
        }
    }
}

void
SgTemplateVariableDeclaration::deleteMemoryPool() {
    for (auto p: SgTemplateVariableDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTemplateVariableDeclaration::next_node = nullptr;
    SgTemplateVariableDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateVariableDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableDeclaration)};
    for (size_t blockIndex = SgTemplateVariableDeclaration::pools.size(); (blockIndex * SgTemplateVariableDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableDeclaration) = %" PRIuPTR " SgTemplateVariableDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableDeclaration),SgTemplateVariableDeclaration::pool_size);
        }
#endif

        SgTemplateVariableDeclaration* pointer{static_cast<SgTemplateVariableDeclaration*>(ROSE_MALLOC(SgTemplateVariableDeclaration::pool_size * sizeof(SgTemplateVariableDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateVariableDeclaration::pool_size * sizeof(SgTemplateVariableDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateVariableDeclaration::pool_size * sizeof(SgTemplateVariableDeclaration));
#endif
        SgTemplateVariableDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateVariableDeclaration::pool_size * sizeof(SgTemplateVariableDeclaration), V_SgTemplateVariableDeclaration));

        if (SgTemplateVariableDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateVariableDeclaration* blkptr{reinterpret_cast<SgTemplateVariableDeclaration*>(SgTemplateVariableDeclaration::pools[blockIndex-1])};
                blkptr[SgTemplateVariableDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateVariableDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateVariableDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateVariableDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateVariableDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateVariableDeclaration::pool_size - 1};
    SgTemplateVariableDeclaration* testPointer{reinterpret_cast<SgTemplateVariableDeclaration*>(SgTemplateVariableDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateVariableDeclaration::pool_size * (SgTemplateVariableDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateVariableDeclarationStroageClass
 * from its counterpart of SgTemplateVariableDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateVariableDeclaration::initializeStorageClassArray(SgTemplateVariableDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateVariableDeclaration::pools.begin(); block != SgTemplateVariableDeclaration::pools.end(); ++block) {
        SgTemplateVariableDeclaration* pointer{reinterpret_cast<SgTemplateVariableDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 59594 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateVariableInstantiation that takes its corresponding StorageClass as parameter
 */
SgTemplateVariableInstantiation::SgTemplateVariableInstantiation(const SgTemplateVariableInstantiationStorageClass& storageSource)  : SgVariableDeclaration (storageSource) {

/* #line 59604 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateName = SgName ( storageSource.storageOf_templateName);
    p_templateHeader = SgName ( storageSource.storageOf_templateHeader);
     p_templateDeclaration = (SgTemplateVariableDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
    p_templateArguments = storageSource.storageOf_templateArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateArguments = p_templateArguments.begin();
    for ( ; i_templateArguments != p_templateArguments.end(); ++i_templateArguments) {
        (*i_templateArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateArguments)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateVariableInstantiation*
SgTemplateVariableInstantiation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateVariableInstantiation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableInstantiation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableInstantiation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableInstantiation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableInstantiation)};
        unsigned long positionInPool{localIndex % SgTemplateVariableInstantiation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateVariableInstantiation::pool_size};
        returnPointer = &(((SgTemplateVariableInstantiation*)(SgTemplateVariableInstantiation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateVariableInstantiation*
SgTemplateVariableInstantiation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateVariableInstantiation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableInstantiation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableInstantiation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableInstantiation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateVariableInstantiation)};
        unsigned long positionInPool{localIndex % SgTemplateVariableInstantiation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateVariableInstantiation::pool_size};
        returnPointer = &(((SgTemplateVariableInstantiation*)(SgTemplateVariableInstantiation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateVariableInstantiation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateVariableInstantiation::pools.begin(); block != SgTemplateVariableInstantiation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateVariableInstantiation*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableInstantiation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateVariableInstantiation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateVariableInstantiation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateVariableInstantiation* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateVariableInstantiation::pools.begin(); block != SgTemplateVariableInstantiation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateVariableInstantiation*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableInstantiation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateVariableInstantiation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateVariableInstantiation::clearMemoryPool() {
    SgTemplateVariableInstantiation* tempPointer{nullptr};
    if (SgTemplateVariableInstantiation::pools.empty() == false) {
        auto block{SgTemplateVariableInstantiation::pools.begin()};
        SgTemplateVariableInstantiation::next_node = reinterpret_cast<SgTemplateVariableInstantiation*>(*block);
        for (; block != SgTemplateVariableInstantiation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateVariableInstantiation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateVariableInstantiation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateVariableInstantiation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateVariableInstantiation::pool_size-1]);
        }
    }
}

void
SgTemplateVariableInstantiation::deleteMemoryPool() {
    for (auto p: SgTemplateVariableInstantiation::pools) {
        ROSE_FREE(p);
    }
    SgTemplateVariableInstantiation::next_node = nullptr;
    SgTemplateVariableInstantiation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateVariableInstantiation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableInstantiation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableInstantiation)};
    for (size_t blockIndex = SgTemplateVariableInstantiation::pools.size(); (blockIndex * SgTemplateVariableInstantiation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableInstantiation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableInstantiation) = %" PRIuPTR " SgTemplateVariableInstantiation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableInstantiation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableInstantiation),SgTemplateVariableInstantiation::pool_size);
        }
#endif

        SgTemplateVariableInstantiation* pointer{static_cast<SgTemplateVariableInstantiation*>(ROSE_MALLOC(SgTemplateVariableInstantiation::pool_size * sizeof(SgTemplateVariableInstantiation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateVariableInstantiation::pool_size * sizeof(SgTemplateVariableInstantiation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateVariableInstantiation::pool_size * sizeof(SgTemplateVariableInstantiation));
#endif
        SgTemplateVariableInstantiation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateVariableInstantiation::pool_size * sizeof(SgTemplateVariableInstantiation), V_SgTemplateVariableInstantiation));

        if (SgTemplateVariableInstantiation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateVariableInstantiation* blkptr{reinterpret_cast<SgTemplateVariableInstantiation*>(SgTemplateVariableInstantiation::pools[blockIndex-1])};
                blkptr[SgTemplateVariableInstantiation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateVariableInstantiation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateVariableInstantiation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateVariableInstantiation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateVariableInstantiation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateVariableInstantiation::pool_size - 1};
    SgTemplateVariableInstantiation* testPointer{reinterpret_cast<SgTemplateVariableInstantiation*>(SgTemplateVariableInstantiation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateVariableInstantiation::pool_size * (SgTemplateVariableInstantiation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateVariableInstantiationStroageClass
 * from its counterpart of SgTemplateVariableInstantiation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateVariableInstantiation::initializeStorageClassArray(SgTemplateVariableInstantiationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateVariableInstantiation::pools.begin(); block != SgTemplateVariableInstantiation::pools.end(); ++block) {
        SgTemplateVariableInstantiation* pointer{reinterpret_cast<SgTemplateVariableInstantiation*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableInstantiation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 59829 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVariableDefinition that takes its corresponding StorageClass as parameter
 */
SgVariableDefinition::SgVariableDefinition(const SgVariableDefinitionStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 59839 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_vardefn = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_vardefn));
     p_bitfield = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_bitfield));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVariableDefinition*
SgVariableDefinition::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVariableDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDefinition)};
        unsigned long positionInPool{localIndex % SgVariableDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariableDefinition::pool_size};
        returnPointer = &(((SgVariableDefinition*)(SgVariableDefinition::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVariableDefinition*
SgVariableDefinition::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVariableDefinition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableDefinition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableDefinition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableDefinition)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVariableDefinition)};
        unsigned long positionInPool{localIndex % SgVariableDefinition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariableDefinition::pool_size};
        returnPointer = &(((SgVariableDefinition*)(SgVariableDefinition::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVariableDefinition::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVariableDefinition::pools.begin(); block != SgVariableDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariableDefinition*>(*block)};
        for (unsigned i = 0; i < SgVariableDefinition::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVariableDefinition*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVariableDefinition::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVariableDefinition* pointerOfLinkedList{nullptr};
    for (auto block = SgVariableDefinition::pools.begin(); block != SgVariableDefinition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariableDefinition*>(*block)};
        for (unsigned i = 0; i < SgVariableDefinition::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVariableDefinition::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVariableDefinition::clearMemoryPool() {
    SgVariableDefinition* tempPointer{nullptr};
    if (SgVariableDefinition::pools.empty() == false) {
        auto block{SgVariableDefinition::pools.begin()};
        SgVariableDefinition::next_node = reinterpret_cast<SgVariableDefinition*>(*block);
        for (; block != SgVariableDefinition::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVariableDefinition*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVariableDefinition::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVariableDefinition::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVariableDefinition::pool_size-1]);
        }
    }
}

void
SgVariableDefinition::deleteMemoryPool() {
    for (auto p: SgVariableDefinition::pools) {
        ROSE_FREE(p);
    }
    SgVariableDefinition::next_node = nullptr;
    SgVariableDefinition::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVariableDefinition::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDefinition) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableDefinition)};
    for (size_t blockIndex = SgVariableDefinition::pools.size(); (blockIndex * SgVariableDefinition::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDefinition) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableDefinition) = %" PRIuPTR " SgVariableDefinition::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableDefinition),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableDefinition),SgVariableDefinition::pool_size);
        }
#endif

        SgVariableDefinition* pointer{static_cast<SgVariableDefinition*>(ROSE_MALLOC(SgVariableDefinition::pool_size * sizeof(SgVariableDefinition)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVariableDefinition::pool_size * sizeof(SgVariableDefinition));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVariableDefinition::pool_size * sizeof(SgVariableDefinition));
#endif
        SgVariableDefinition::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVariableDefinition::pool_size * sizeof(SgVariableDefinition), V_SgVariableDefinition));

        if (SgVariableDefinition::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVariableDefinition* blkptr{reinterpret_cast<SgVariableDefinition*>(SgVariableDefinition::pools[blockIndex-1])};
                blkptr[SgVariableDefinition::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVariableDefinition::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVariableDefinition::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVariableDefinition::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVariableDefinition::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVariableDefinition::pool_size - 1};
    SgVariableDefinition* testPointer{reinterpret_cast<SgVariableDefinition*>(SgVariableDefinition::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVariableDefinition::pool_size * (SgVariableDefinition::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVariableDefinitionStroageClass
 * from its counterpart of SgVariableDefinition. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVariableDefinition::initializeStorageClassArray(SgVariableDefinitionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVariableDefinition::pools.begin(); block != SgVariableDefinition::pools.end(); ++block) {
        SgVariableDefinition* pointer{reinterpret_cast<SgVariableDefinition*>(*block)};
        for (unsigned i = 0; i < SgVariableDefinition::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 60058 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClinkageDeclarationStatement that takes its corresponding StorageClass as parameter
 */
SgClinkageDeclarationStatement::SgClinkageDeclarationStatement(const SgClinkageDeclarationStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 60068 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_languageSpecifier = storageSource.storageOf_languageSpecifier.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClinkageDeclarationStatement*
SgClinkageDeclarationStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClinkageDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgClinkageDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClinkageDeclarationStatement::pool_size};
        returnPointer = &(((SgClinkageDeclarationStatement*)(SgClinkageDeclarationStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClinkageDeclarationStatement*
SgClinkageDeclarationStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClinkageDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClinkageDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgClinkageDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClinkageDeclarationStatement::pool_size};
        returnPointer = &(((SgClinkageDeclarationStatement*)(SgClinkageDeclarationStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClinkageDeclarationStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClinkageDeclarationStatement::pools.begin(); block != SgClinkageDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClinkageDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageDeclarationStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClinkageDeclarationStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClinkageDeclarationStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClinkageDeclarationStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgClinkageDeclarationStatement::pools.begin(); block != SgClinkageDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClinkageDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageDeclarationStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClinkageDeclarationStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClinkageDeclarationStatement::clearMemoryPool() {
    SgClinkageDeclarationStatement* tempPointer{nullptr};
    if (SgClinkageDeclarationStatement::pools.empty() == false) {
        auto block{SgClinkageDeclarationStatement::pools.begin()};
        SgClinkageDeclarationStatement::next_node = reinterpret_cast<SgClinkageDeclarationStatement*>(*block);
        for (; block != SgClinkageDeclarationStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClinkageDeclarationStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClinkageDeclarationStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClinkageDeclarationStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClinkageDeclarationStatement::pool_size-1]);
        }
    }
}

void
SgClinkageDeclarationStatement::deleteMemoryPool() {
    for (auto p: SgClinkageDeclarationStatement::pools) {
        ROSE_FREE(p);
    }
    SgClinkageDeclarationStatement::next_node = nullptr;
    SgClinkageDeclarationStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClinkageDeclarationStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageDeclarationStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageDeclarationStatement)};
    for (size_t blockIndex = SgClinkageDeclarationStatement::pools.size(); (blockIndex * SgClinkageDeclarationStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageDeclarationStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageDeclarationStatement) = %" PRIuPTR " SgClinkageDeclarationStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageDeclarationStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageDeclarationStatement),SgClinkageDeclarationStatement::pool_size);
        }
#endif

        SgClinkageDeclarationStatement* pointer{static_cast<SgClinkageDeclarationStatement*>(ROSE_MALLOC(SgClinkageDeclarationStatement::pool_size * sizeof(SgClinkageDeclarationStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClinkageDeclarationStatement::pool_size * sizeof(SgClinkageDeclarationStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClinkageDeclarationStatement::pool_size * sizeof(SgClinkageDeclarationStatement));
#endif
        SgClinkageDeclarationStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClinkageDeclarationStatement::pool_size * sizeof(SgClinkageDeclarationStatement), V_SgClinkageDeclarationStatement));

        if (SgClinkageDeclarationStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClinkageDeclarationStatement* blkptr{reinterpret_cast<SgClinkageDeclarationStatement*>(SgClinkageDeclarationStatement::pools[blockIndex-1])};
                blkptr[SgClinkageDeclarationStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClinkageDeclarationStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClinkageDeclarationStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClinkageDeclarationStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClinkageDeclarationStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClinkageDeclarationStatement::pool_size - 1};
    SgClinkageDeclarationStatement* testPointer{reinterpret_cast<SgClinkageDeclarationStatement*>(SgClinkageDeclarationStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClinkageDeclarationStatement::pool_size * (SgClinkageDeclarationStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClinkageDeclarationStatementStroageClass
 * from its counterpart of SgClinkageDeclarationStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClinkageDeclarationStatement::initializeStorageClassArray(SgClinkageDeclarationStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClinkageDeclarationStatement::pools.begin(); block != SgClinkageDeclarationStatement::pools.end(); ++block) {
        SgClinkageDeclarationStatement* pointer{reinterpret_cast<SgClinkageDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageDeclarationStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 60286 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClinkageStartStatement that takes its corresponding StorageClass as parameter
 */
SgClinkageStartStatement::SgClinkageStartStatement(const SgClinkageStartStatementStorageClass& storageSource)  : SgClinkageDeclarationStatement (storageSource) {

/* #line 60296 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClinkageStartStatement*
SgClinkageStartStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClinkageStartStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageStartStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageStartStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageStartStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageStartStatement)};
        unsigned long positionInPool{localIndex % SgClinkageStartStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClinkageStartStatement::pool_size};
        returnPointer = &(((SgClinkageStartStatement*)(SgClinkageStartStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClinkageStartStatement*
SgClinkageStartStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClinkageStartStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageStartStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageStartStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageStartStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClinkageStartStatement)};
        unsigned long positionInPool{localIndex % SgClinkageStartStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClinkageStartStatement::pool_size};
        returnPointer = &(((SgClinkageStartStatement*)(SgClinkageStartStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClinkageStartStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClinkageStartStatement::pools.begin(); block != SgClinkageStartStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClinkageStartStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageStartStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClinkageStartStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClinkageStartStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClinkageStartStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgClinkageStartStatement::pools.begin(); block != SgClinkageStartStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClinkageStartStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageStartStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClinkageStartStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClinkageStartStatement::clearMemoryPool() {
    SgClinkageStartStatement* tempPointer{nullptr};
    if (SgClinkageStartStatement::pools.empty() == false) {
        auto block{SgClinkageStartStatement::pools.begin()};
        SgClinkageStartStatement::next_node = reinterpret_cast<SgClinkageStartStatement*>(*block);
        for (; block != SgClinkageStartStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClinkageStartStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClinkageStartStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClinkageStartStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClinkageStartStatement::pool_size-1]);
        }
    }
}

void
SgClinkageStartStatement::deleteMemoryPool() {
    for (auto p: SgClinkageStartStatement::pools) {
        ROSE_FREE(p);
    }
    SgClinkageStartStatement::next_node = nullptr;
    SgClinkageStartStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClinkageStartStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageStartStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageStartStatement)};
    for (size_t blockIndex = SgClinkageStartStatement::pools.size(); (blockIndex * SgClinkageStartStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageStartStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageStartStatement) = %" PRIuPTR " SgClinkageStartStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageStartStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageStartStatement),SgClinkageStartStatement::pool_size);
        }
#endif

        SgClinkageStartStatement* pointer{static_cast<SgClinkageStartStatement*>(ROSE_MALLOC(SgClinkageStartStatement::pool_size * sizeof(SgClinkageStartStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClinkageStartStatement::pool_size * sizeof(SgClinkageStartStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClinkageStartStatement::pool_size * sizeof(SgClinkageStartStatement));
#endif
        SgClinkageStartStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClinkageStartStatement::pool_size * sizeof(SgClinkageStartStatement), V_SgClinkageStartStatement));

        if (SgClinkageStartStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClinkageStartStatement* blkptr{reinterpret_cast<SgClinkageStartStatement*>(SgClinkageStartStatement::pools[blockIndex-1])};
                blkptr[SgClinkageStartStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClinkageStartStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClinkageStartStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClinkageStartStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClinkageStartStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClinkageStartStatement::pool_size - 1};
    SgClinkageStartStatement* testPointer{reinterpret_cast<SgClinkageStartStatement*>(SgClinkageStartStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClinkageStartStatement::pool_size * (SgClinkageStartStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClinkageStartStatementStroageClass
 * from its counterpart of SgClinkageStartStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClinkageStartStatement::initializeStorageClassArray(SgClinkageStartStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClinkageStartStatement::pools.begin(); block != SgClinkageStartStatement::pools.end(); ++block) {
        SgClinkageStartStatement* pointer{reinterpret_cast<SgClinkageStartStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageStartStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 60513 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClinkageEndStatement that takes its corresponding StorageClass as parameter
 */
SgClinkageEndStatement::SgClinkageEndStatement(const SgClinkageEndStatementStorageClass& storageSource)  : SgClinkageDeclarationStatement (storageSource) {

/* #line 60523 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClinkageEndStatement*
SgClinkageEndStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClinkageEndStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageEndStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageEndStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClinkageEndStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageEndStatement)};
        unsigned long positionInPool{localIndex % SgClinkageEndStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClinkageEndStatement::pool_size};
        returnPointer = &(((SgClinkageEndStatement*)(SgClinkageEndStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClinkageEndStatement*
SgClinkageEndStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClinkageEndStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageEndStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageEndStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClinkageEndStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClinkageEndStatement)};
        unsigned long positionInPool{localIndex % SgClinkageEndStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClinkageEndStatement::pool_size};
        returnPointer = &(((SgClinkageEndStatement*)(SgClinkageEndStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClinkageEndStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClinkageEndStatement::pools.begin(); block != SgClinkageEndStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClinkageEndStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageEndStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClinkageEndStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClinkageEndStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClinkageEndStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgClinkageEndStatement::pools.begin(); block != SgClinkageEndStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClinkageEndStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageEndStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClinkageEndStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClinkageEndStatement::clearMemoryPool() {
    SgClinkageEndStatement* tempPointer{nullptr};
    if (SgClinkageEndStatement::pools.empty() == false) {
        auto block{SgClinkageEndStatement::pools.begin()};
        SgClinkageEndStatement::next_node = reinterpret_cast<SgClinkageEndStatement*>(*block);
        for (; block != SgClinkageEndStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClinkageEndStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClinkageEndStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClinkageEndStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClinkageEndStatement::pool_size-1]);
        }
    }
}

void
SgClinkageEndStatement::deleteMemoryPool() {
    for (auto p: SgClinkageEndStatement::pools) {
        ROSE_FREE(p);
    }
    SgClinkageEndStatement::next_node = nullptr;
    SgClinkageEndStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClinkageEndStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageEndStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageEndStatement)};
    for (size_t blockIndex = SgClinkageEndStatement::pools.size(); (blockIndex * SgClinkageEndStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageEndStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageEndStatement) = %" PRIuPTR " SgClinkageEndStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClinkageEndStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClinkageEndStatement),SgClinkageEndStatement::pool_size);
        }
#endif

        SgClinkageEndStatement* pointer{static_cast<SgClinkageEndStatement*>(ROSE_MALLOC(SgClinkageEndStatement::pool_size * sizeof(SgClinkageEndStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClinkageEndStatement::pool_size * sizeof(SgClinkageEndStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClinkageEndStatement::pool_size * sizeof(SgClinkageEndStatement));
#endif
        SgClinkageEndStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClinkageEndStatement::pool_size * sizeof(SgClinkageEndStatement), V_SgClinkageEndStatement));

        if (SgClinkageEndStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClinkageEndStatement* blkptr{reinterpret_cast<SgClinkageEndStatement*>(SgClinkageEndStatement::pools[blockIndex-1])};
                blkptr[SgClinkageEndStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClinkageEndStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClinkageEndStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClinkageEndStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClinkageEndStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClinkageEndStatement::pool_size - 1};
    SgClinkageEndStatement* testPointer{reinterpret_cast<SgClinkageEndStatement*>(SgClinkageEndStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClinkageEndStatement::pool_size * (SgClinkageEndStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClinkageEndStatementStroageClass
 * from its counterpart of SgClinkageEndStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClinkageEndStatement::initializeStorageClassArray(SgClinkageEndStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClinkageEndStatement::pools.begin(); block != SgClinkageEndStatement::pools.end(); ++block) {
        SgClinkageEndStatement* pointer{reinterpret_cast<SgClinkageEndStatement*>(*block)};
        for (unsigned i = 0; i < SgClinkageEndStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 60740 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEnumDeclaration that takes its corresponding StorageClass as parameter
 */
SgEnumDeclaration::SgEnumDeclaration(const SgEnumDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 60750 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_embedded = storageSource.storageOf_embedded;
     p_type = (SgEnumType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_adaParentType = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_adaParentType));
    p_enumerators = storageSource.storageOf_enumerators.rebuildDataStoredInEasyStorageClass();
    SgInitializedNamePtrList::iterator i_enumerators = p_enumerators.begin();
    for ( ; i_enumerators != p_enumerators.end(); ++i_enumerators) {
        (*i_enumerators) = (SgInitializedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_enumerators)) );
    }
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
    p_isUnNamed = storageSource.storageOf_isUnNamed;
    p_isAutonomousDeclaration = storageSource.storageOf_isAutonomousDeclaration;
     p_field_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_field_type));
    p_isScopedEnum = storageSource.storageOf_isScopedEnum;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEnumDeclaration*
SgEnumDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEnumDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumDeclaration)};
        unsigned long positionInPool{localIndex % SgEnumDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumDeclaration::pool_size};
        returnPointer = &(((SgEnumDeclaration*)(SgEnumDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEnumDeclaration*
SgEnumDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEnumDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEnumDeclaration)};
        unsigned long positionInPool{localIndex % SgEnumDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumDeclaration::pool_size};
        returnPointer = &(((SgEnumDeclaration*)(SgEnumDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEnumDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEnumDeclaration::pools.begin(); block != SgEnumDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumDeclaration*>(*block)};
        for (unsigned i = 0; i < SgEnumDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEnumDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEnumDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEnumDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgEnumDeclaration::pools.begin(); block != SgEnumDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumDeclaration*>(*block)};
        for (unsigned i = 0; i < SgEnumDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEnumDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEnumDeclaration::clearMemoryPool() {
    SgEnumDeclaration* tempPointer{nullptr};
    if (SgEnumDeclaration::pools.empty() == false) {
        auto block{SgEnumDeclaration::pools.begin()};
        SgEnumDeclaration::next_node = reinterpret_cast<SgEnumDeclaration*>(*block);
        for (; block != SgEnumDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEnumDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEnumDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEnumDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEnumDeclaration::pool_size-1]);
        }
    }
}

void
SgEnumDeclaration::deleteMemoryPool() {
    for (auto p: SgEnumDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgEnumDeclaration::next_node = nullptr;
    SgEnumDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEnumDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumDeclaration)};
    for (size_t blockIndex = SgEnumDeclaration::pools.size(); (blockIndex * SgEnumDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumDeclaration) = %" PRIuPTR " SgEnumDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumDeclaration),SgEnumDeclaration::pool_size);
        }
#endif

        SgEnumDeclaration* pointer{static_cast<SgEnumDeclaration*>(ROSE_MALLOC(SgEnumDeclaration::pool_size * sizeof(SgEnumDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEnumDeclaration::pool_size * sizeof(SgEnumDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEnumDeclaration::pool_size * sizeof(SgEnumDeclaration));
#endif
        SgEnumDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEnumDeclaration::pool_size * sizeof(SgEnumDeclaration), V_SgEnumDeclaration));

        if (SgEnumDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEnumDeclaration* blkptr{reinterpret_cast<SgEnumDeclaration*>(SgEnumDeclaration::pools[blockIndex-1])};
                blkptr[SgEnumDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEnumDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEnumDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEnumDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEnumDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEnumDeclaration::pool_size - 1};
    SgEnumDeclaration* testPointer{reinterpret_cast<SgEnumDeclaration*>(SgEnumDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEnumDeclaration::pool_size * (SgEnumDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEnumDeclarationStroageClass
 * from its counterpart of SgEnumDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEnumDeclaration::initializeStorageClassArray(SgEnumDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEnumDeclaration::pools.begin(); block != SgEnumDeclaration::pools.end(); ++block) {
        SgEnumDeclaration* pointer{reinterpret_cast<SgEnumDeclaration*>(*block)};
        for (unsigned i = 0; i < SgEnumDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 60984 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmStmt that takes its corresponding StorageClass as parameter
 */
SgAsmStmt::SgAsmStmt(const SgAsmStmtStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 60994 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_assemblyCode = storageSource.storageOf_assemblyCode.rebuildDataStoredInEasyStorageClass();
    p_useGnuExtendedFormat = storageSource.storageOf_useGnuExtendedFormat;
    p_operands = storageSource.storageOf_operands.rebuildDataStoredInEasyStorageClass();
    SgExpressionPtrList::iterator i_operands = p_operands.begin();
    for ( ; i_operands != p_operands.end(); ++i_operands) {
        (*i_operands) = (SgExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_operands)) );
    }
    p_clobberRegisterList = storageSource.storageOf_clobberRegisterList.rebuildDataStoredInEasyStorageClass();
    p_isVolatile = storageSource.storageOf_isVolatile;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmStmt*
SgAsmStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStmt)};
        unsigned long positionInPool{localIndex % SgAsmStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStmt::pool_size};
        returnPointer = &(((SgAsmStmt*)(SgAsmStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmStmt*
SgAsmStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmStmt)};
        unsigned long positionInPool{localIndex % SgAsmStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStmt::pool_size};
        returnPointer = &(((SgAsmStmt*)(SgAsmStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmStmt::pools.begin(); block != SgAsmStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStmt*>(*block)};
        for (unsigned i = 0; i < SgAsmStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmStmt::pools.begin(); block != SgAsmStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStmt*>(*block)};
        for (unsigned i = 0; i < SgAsmStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmStmt::clearMemoryPool() {
    SgAsmStmt* tempPointer{nullptr};
    if (SgAsmStmt::pools.empty() == false) {
        auto block{SgAsmStmt::pools.begin()};
        SgAsmStmt::next_node = reinterpret_cast<SgAsmStmt*>(*block);
        for (; block != SgAsmStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmStmt::pool_size-1]);
        }
    }
}

void
SgAsmStmt::deleteMemoryPool() {
    for (auto p: SgAsmStmt::pools) {
        ROSE_FREE(p);
    }
    SgAsmStmt::next_node = nullptr;
    SgAsmStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStmt)};
    for (size_t blockIndex = SgAsmStmt::pools.size(); (blockIndex * SgAsmStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStmt) = %" PRIuPTR " SgAsmStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStmt),SgAsmStmt::pool_size);
        }
#endif

        SgAsmStmt* pointer{static_cast<SgAsmStmt*>(ROSE_MALLOC(SgAsmStmt::pool_size * sizeof(SgAsmStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmStmt::pool_size * sizeof(SgAsmStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmStmt::pool_size * sizeof(SgAsmStmt));
#endif
        SgAsmStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmStmt::pool_size * sizeof(SgAsmStmt), V_SgAsmStmt));

        if (SgAsmStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmStmt* blkptr{reinterpret_cast<SgAsmStmt*>(SgAsmStmt::pools[blockIndex-1])};
                blkptr[SgAsmStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmStmt::pool_size - 1};
    SgAsmStmt* testPointer{reinterpret_cast<SgAsmStmt*>(SgAsmStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmStmt::pool_size * (SgAsmStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmStmtStroageClass
 * from its counterpart of SgAsmStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmStmt::initializeStorageClassArray(SgAsmStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmStmt::pools.begin(); block != SgAsmStmt::pools.end(); ++block) {
        SgAsmStmt* pointer{reinterpret_cast<SgAsmStmt*>(*block)};
        for (unsigned i = 0; i < SgAsmStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 61220 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAttributeSpecificationStatement that takes its corresponding StorageClass as parameter
 */
SgAttributeSpecificationStatement::SgAttributeSpecificationStatement(const SgAttributeSpecificationStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 61230 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name_list = storageSource.storageOf_name_list.rebuildDataStoredInEasyStorageClass();
    p_attribute_kind = storageSource.storageOf_attribute_kind;
    p_intent = storageSource.storageOf_intent;
     p_parameter_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameter_list));
    p_data_statement_group_list = storageSource.storageOf_data_statement_group_list.rebuildDataStoredInEasyStorageClass();
    SgDataStatementGroupPtrList::iterator i_data_statement_group_list = p_data_statement_group_list.begin();
    for ( ; i_data_statement_group_list != p_data_statement_group_list.end(); ++i_data_statement_group_list) {
        (*i_data_statement_group_list) = (SgDataStatementGroupPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_data_statement_group_list)) );
    }
     p_bind_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_bind_list));
    p_dimension_object_list = storageSource.storageOf_dimension_object_list.rebuildDataStoredInEasyStorageClass();
    SgDimensionObjectPtrList::iterator i_dimension_object_list = p_dimension_object_list.begin();
    for ( ; i_dimension_object_list != p_dimension_object_list.end(); ++i_dimension_object_list) {
        (*i_dimension_object_list) = (SgDimensionObjectPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_dimension_object_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAttributeSpecificationStatement*
SgAttributeSpecificationStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAttributeSpecificationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAttributeSpecificationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAttributeSpecificationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAttributeSpecificationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAttributeSpecificationStatement)};
        unsigned long positionInPool{localIndex % SgAttributeSpecificationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAttributeSpecificationStatement::pool_size};
        returnPointer = &(((SgAttributeSpecificationStatement*)(SgAttributeSpecificationStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAttributeSpecificationStatement*
SgAttributeSpecificationStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAttributeSpecificationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAttributeSpecificationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAttributeSpecificationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAttributeSpecificationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAttributeSpecificationStatement)};
        unsigned long positionInPool{localIndex % SgAttributeSpecificationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAttributeSpecificationStatement::pool_size};
        returnPointer = &(((SgAttributeSpecificationStatement*)(SgAttributeSpecificationStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAttributeSpecificationStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAttributeSpecificationStatement::pools.begin(); block != SgAttributeSpecificationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAttributeSpecificationStatement*>(*block)};
        for (unsigned i = 0; i < SgAttributeSpecificationStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAttributeSpecificationStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAttributeSpecificationStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAttributeSpecificationStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgAttributeSpecificationStatement::pools.begin(); block != SgAttributeSpecificationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAttributeSpecificationStatement*>(*block)};
        for (unsigned i = 0; i < SgAttributeSpecificationStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAttributeSpecificationStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAttributeSpecificationStatement::clearMemoryPool() {
    SgAttributeSpecificationStatement* tempPointer{nullptr};
    if (SgAttributeSpecificationStatement::pools.empty() == false) {
        auto block{SgAttributeSpecificationStatement::pools.begin()};
        SgAttributeSpecificationStatement::next_node = reinterpret_cast<SgAttributeSpecificationStatement*>(*block);
        for (; block != SgAttributeSpecificationStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAttributeSpecificationStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAttributeSpecificationStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAttributeSpecificationStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAttributeSpecificationStatement::pool_size-1]);
        }
    }
}

void
SgAttributeSpecificationStatement::deleteMemoryPool() {
    for (auto p: SgAttributeSpecificationStatement::pools) {
        ROSE_FREE(p);
    }
    SgAttributeSpecificationStatement::next_node = nullptr;
    SgAttributeSpecificationStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAttributeSpecificationStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAttributeSpecificationStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAttributeSpecificationStatement)};
    for (size_t blockIndex = SgAttributeSpecificationStatement::pools.size(); (blockIndex * SgAttributeSpecificationStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAttributeSpecificationStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAttributeSpecificationStatement) = %" PRIuPTR " SgAttributeSpecificationStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAttributeSpecificationStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAttributeSpecificationStatement),SgAttributeSpecificationStatement::pool_size);
        }
#endif

        SgAttributeSpecificationStatement* pointer{static_cast<SgAttributeSpecificationStatement*>(ROSE_MALLOC(SgAttributeSpecificationStatement::pool_size * sizeof(SgAttributeSpecificationStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAttributeSpecificationStatement::pool_size * sizeof(SgAttributeSpecificationStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAttributeSpecificationStatement::pool_size * sizeof(SgAttributeSpecificationStatement));
#endif
        SgAttributeSpecificationStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAttributeSpecificationStatement::pool_size * sizeof(SgAttributeSpecificationStatement), V_SgAttributeSpecificationStatement));

        if (SgAttributeSpecificationStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAttributeSpecificationStatement* blkptr{reinterpret_cast<SgAttributeSpecificationStatement*>(SgAttributeSpecificationStatement::pools[blockIndex-1])};
                blkptr[SgAttributeSpecificationStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAttributeSpecificationStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAttributeSpecificationStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAttributeSpecificationStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAttributeSpecificationStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAttributeSpecificationStatement::pool_size - 1};
    SgAttributeSpecificationStatement* testPointer{reinterpret_cast<SgAttributeSpecificationStatement*>(SgAttributeSpecificationStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAttributeSpecificationStatement::pool_size * (SgAttributeSpecificationStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAttributeSpecificationStatementStroageClass
 * from its counterpart of SgAttributeSpecificationStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAttributeSpecificationStatement::initializeStorageClassArray(SgAttributeSpecificationStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAttributeSpecificationStatement::pools.begin(); block != SgAttributeSpecificationStatement::pools.end(); ++block) {
        SgAttributeSpecificationStatement* pointer{reinterpret_cast<SgAttributeSpecificationStatement*>(*block)};
        for (unsigned i = 0; i < SgAttributeSpecificationStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 61462 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFormatStatement that takes its corresponding StorageClass as parameter
 */
SgFormatStatement::SgFormatStatement(const SgFormatStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 61472 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_format_item_list = (SgFormatItemList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_format_item_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFormatStatement*
SgFormatStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFormatStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFormatStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatStatement)};
        unsigned long positionInPool{localIndex % SgFormatStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFormatStatement::pool_size};
        returnPointer = &(((SgFormatStatement*)(SgFormatStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFormatStatement*
SgFormatStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFormatStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFormatStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFormatStatement)};
        unsigned long positionInPool{localIndex % SgFormatStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFormatStatement::pool_size};
        returnPointer = &(((SgFormatStatement*)(SgFormatStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFormatStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFormatStatement::pools.begin(); block != SgFormatStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFormatStatement*>(*block)};
        for (unsigned i = 0; i < SgFormatStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFormatStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFormatStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFormatStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgFormatStatement::pools.begin(); block != SgFormatStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFormatStatement*>(*block)};
        for (unsigned i = 0; i < SgFormatStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFormatStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFormatStatement::clearMemoryPool() {
    SgFormatStatement* tempPointer{nullptr};
    if (SgFormatStatement::pools.empty() == false) {
        auto block{SgFormatStatement::pools.begin()};
        SgFormatStatement::next_node = reinterpret_cast<SgFormatStatement*>(*block);
        for (; block != SgFormatStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFormatStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFormatStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFormatStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFormatStatement::pool_size-1]);
        }
    }
}

void
SgFormatStatement::deleteMemoryPool() {
    for (auto p: SgFormatStatement::pools) {
        ROSE_FREE(p);
    }
    SgFormatStatement::next_node = nullptr;
    SgFormatStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFormatStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatStatement)};
    for (size_t blockIndex = SgFormatStatement::pools.size(); (blockIndex * SgFormatStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatStatement) = %" PRIuPTR " SgFormatStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFormatStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFormatStatement),SgFormatStatement::pool_size);
        }
#endif

        SgFormatStatement* pointer{static_cast<SgFormatStatement*>(ROSE_MALLOC(SgFormatStatement::pool_size * sizeof(SgFormatStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFormatStatement::pool_size * sizeof(SgFormatStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFormatStatement::pool_size * sizeof(SgFormatStatement));
#endif
        SgFormatStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFormatStatement::pool_size * sizeof(SgFormatStatement), V_SgFormatStatement));

        if (SgFormatStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFormatStatement* blkptr{reinterpret_cast<SgFormatStatement*>(SgFormatStatement::pools[blockIndex-1])};
                blkptr[SgFormatStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFormatStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFormatStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFormatStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFormatStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFormatStatement::pool_size - 1};
    SgFormatStatement* testPointer{reinterpret_cast<SgFormatStatement*>(SgFormatStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFormatStatement::pool_size * (SgFormatStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFormatStatementStroageClass
 * from its counterpart of SgFormatStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFormatStatement::initializeStorageClassArray(SgFormatStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFormatStatement::pools.begin(); block != SgFormatStatement::pools.end(); ++block) {
        SgFormatStatement* pointer{reinterpret_cast<SgFormatStatement*>(*block)};
        for (unsigned i = 0; i < SgFormatStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 61690 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateDeclaration that takes its corresponding StorageClass as parameter
 */
SgTemplateDeclaration::SgTemplateDeclaration(const SgTemplateDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 61700 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_string = SgName ( storageSource.storageOf_string);
    p_template_kind = storageSource.storageOf_template_kind;
    p_templateParameters = storageSource.storageOf_templateParameters.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_templateParameters = p_templateParameters.begin();
    for ( ; i_templateParameters != p_templateParameters.end(); ++i_templateParameters) {
        (*i_templateParameters) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateParameters)) );
    }
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
     p_nonreal_decl_scope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_decl_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateDeclaration*
SgTemplateDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateDeclaration::pool_size};
        returnPointer = &(((SgTemplateDeclaration*)(SgTemplateDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateDeclaration*
SgTemplateDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateDeclaration::pool_size};
        returnPointer = &(((SgTemplateDeclaration*)(SgTemplateDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateDeclaration::pools.begin(); block != SgTemplateDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateDeclaration::pools.begin(); block != SgTemplateDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateDeclaration::clearMemoryPool() {
    SgTemplateDeclaration* tempPointer{nullptr};
    if (SgTemplateDeclaration::pools.empty() == false) {
        auto block{SgTemplateDeclaration::pools.begin()};
        SgTemplateDeclaration::next_node = reinterpret_cast<SgTemplateDeclaration*>(*block);
        for (; block != SgTemplateDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateDeclaration::pool_size-1]);
        }
    }
}

void
SgTemplateDeclaration::deleteMemoryPool() {
    for (auto p: SgTemplateDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTemplateDeclaration::next_node = nullptr;
    SgTemplateDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateDeclaration)};
    for (size_t blockIndex = SgTemplateDeclaration::pools.size(); (blockIndex * SgTemplateDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateDeclaration) = %" PRIuPTR " SgTemplateDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateDeclaration),SgTemplateDeclaration::pool_size);
        }
#endif

        SgTemplateDeclaration* pointer{static_cast<SgTemplateDeclaration*>(ROSE_MALLOC(SgTemplateDeclaration::pool_size * sizeof(SgTemplateDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateDeclaration::pool_size * sizeof(SgTemplateDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateDeclaration::pool_size * sizeof(SgTemplateDeclaration));
#endif
        SgTemplateDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateDeclaration::pool_size * sizeof(SgTemplateDeclaration), V_SgTemplateDeclaration));

        if (SgTemplateDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateDeclaration* blkptr{reinterpret_cast<SgTemplateDeclaration*>(SgTemplateDeclaration::pools[blockIndex-1])};
                blkptr[SgTemplateDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateDeclaration::pool_size - 1};
    SgTemplateDeclaration* testPointer{reinterpret_cast<SgTemplateDeclaration*>(SgTemplateDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateDeclaration::pool_size * (SgTemplateDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateDeclarationStroageClass
 * from its counterpart of SgTemplateDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateDeclaration::initializeStorageClassArray(SgTemplateDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateDeclaration::pools.begin(); block != SgTemplateDeclaration::pools.end(); ++block) {
        SgTemplateDeclaration* pointer{reinterpret_cast<SgTemplateDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 61927 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateInstantiationDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgTemplateInstantiationDirectiveStatement::SgTemplateInstantiationDirectiveStatement(const SgTemplateInstantiationDirectiveStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 61937 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
    p_do_not_instantiate = storageSource.storageOf_do_not_instantiate;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateInstantiationDirectiveStatement*
SgTemplateInstantiationDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateInstantiationDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationDirectiveStatement::pool_size};
        returnPointer = &(((SgTemplateInstantiationDirectiveStatement*)(SgTemplateInstantiationDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateInstantiationDirectiveStatement*
SgTemplateInstantiationDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateInstantiationDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateInstantiationDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationDirectiveStatement::pool_size};
        returnPointer = &(((SgTemplateInstantiationDirectiveStatement*)(SgTemplateInstantiationDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateInstantiationDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateInstantiationDirectiveStatement::pools.begin(); block != SgTemplateInstantiationDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateInstantiationDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateInstantiationDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateInstantiationDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateInstantiationDirectiveStatement::pools.begin(); block != SgTemplateInstantiationDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateInstantiationDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateInstantiationDirectiveStatement::clearMemoryPool() {
    SgTemplateInstantiationDirectiveStatement* tempPointer{nullptr};
    if (SgTemplateInstantiationDirectiveStatement::pools.empty() == false) {
        auto block{SgTemplateInstantiationDirectiveStatement::pools.begin()};
        SgTemplateInstantiationDirectiveStatement::next_node = reinterpret_cast<SgTemplateInstantiationDirectiveStatement*>(*block);
        for (; block != SgTemplateInstantiationDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateInstantiationDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateInstantiationDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateInstantiationDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateInstantiationDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgTemplateInstantiationDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgTemplateInstantiationDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgTemplateInstantiationDirectiveStatement::next_node = nullptr;
    SgTemplateInstantiationDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateInstantiationDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDirectiveStatement)};
    for (size_t blockIndex = SgTemplateInstantiationDirectiveStatement::pools.size(); (blockIndex * SgTemplateInstantiationDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDirectiveStatement) = %" PRIuPTR " SgTemplateInstantiationDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDirectiveStatement),SgTemplateInstantiationDirectiveStatement::pool_size);
        }
#endif

        SgTemplateInstantiationDirectiveStatement* pointer{static_cast<SgTemplateInstantiationDirectiveStatement*>(ROSE_MALLOC(SgTemplateInstantiationDirectiveStatement::pool_size * sizeof(SgTemplateInstantiationDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateInstantiationDirectiveStatement::pool_size * sizeof(SgTemplateInstantiationDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateInstantiationDirectiveStatement::pool_size * sizeof(SgTemplateInstantiationDirectiveStatement));
#endif
        SgTemplateInstantiationDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateInstantiationDirectiveStatement::pool_size * sizeof(SgTemplateInstantiationDirectiveStatement), V_SgTemplateInstantiationDirectiveStatement));

        if (SgTemplateInstantiationDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateInstantiationDirectiveStatement* blkptr{reinterpret_cast<SgTemplateInstantiationDirectiveStatement*>(SgTemplateInstantiationDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgTemplateInstantiationDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateInstantiationDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateInstantiationDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateInstantiationDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateInstantiationDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateInstantiationDirectiveStatement::pool_size - 1};
    SgTemplateInstantiationDirectiveStatement* testPointer{reinterpret_cast<SgTemplateInstantiationDirectiveStatement*>(SgTemplateInstantiationDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateInstantiationDirectiveStatement::pool_size * (SgTemplateInstantiationDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateInstantiationDirectiveStatementStroageClass
 * from its counterpart of SgTemplateInstantiationDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateInstantiationDirectiveStatement::initializeStorageClassArray(SgTemplateInstantiationDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateInstantiationDirectiveStatement::pools.begin(); block != SgTemplateInstantiationDirectiveStatement::pools.end(); ++block) {
        SgTemplateInstantiationDirectiveStatement* pointer{reinterpret_cast<SgTemplateInstantiationDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 62156 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUseStatement that takes its corresponding StorageClass as parameter
 */
SgUseStatement::SgUseStatement(const SgUseStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 62166 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_only_option = storageSource.storageOf_only_option;
    p_module_nature = storageSource.storageOf_module_nature.rebuildDataStoredInEasyStorageClass();
    p_rename_list = storageSource.storageOf_rename_list.rebuildDataStoredInEasyStorageClass();
    SgRenamePairPtrList::iterator i_rename_list = p_rename_list.begin();
    for ( ; i_rename_list != p_rename_list.end(); ++i_rename_list) {
        (*i_rename_list) = (SgRenamePairPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_rename_list)) );
    }
     p_module = (SgModuleStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_module));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUseStatement*
SgUseStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUseStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUseStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUseStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUseStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUseStatement)};
        unsigned long positionInPool{localIndex % SgUseStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUseStatement::pool_size};
        returnPointer = &(((SgUseStatement*)(SgUseStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUseStatement*
SgUseStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUseStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUseStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUseStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUseStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUseStatement)};
        unsigned long positionInPool{localIndex % SgUseStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUseStatement::pool_size};
        returnPointer = &(((SgUseStatement*)(SgUseStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUseStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUseStatement::pools.begin(); block != SgUseStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUseStatement*>(*block)};
        for (unsigned i = 0; i < SgUseStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUseStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUseStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUseStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUseStatement::pools.begin(); block != SgUseStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUseStatement*>(*block)};
        for (unsigned i = 0; i < SgUseStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUseStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUseStatement::clearMemoryPool() {
    SgUseStatement* tempPointer{nullptr};
    if (SgUseStatement::pools.empty() == false) {
        auto block{SgUseStatement::pools.begin()};
        SgUseStatement::next_node = reinterpret_cast<SgUseStatement*>(*block);
        for (; block != SgUseStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUseStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUseStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUseStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUseStatement::pool_size-1]);
        }
    }
}

void
SgUseStatement::deleteMemoryPool() {
    for (auto p: SgUseStatement::pools) {
        ROSE_FREE(p);
    }
    SgUseStatement::next_node = nullptr;
    SgUseStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUseStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUseStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUseStatement)};
    for (size_t blockIndex = SgUseStatement::pools.size(); (blockIndex * SgUseStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUseStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUseStatement) = %" PRIuPTR " SgUseStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUseStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUseStatement),SgUseStatement::pool_size);
        }
#endif

        SgUseStatement* pointer{static_cast<SgUseStatement*>(ROSE_MALLOC(SgUseStatement::pool_size * sizeof(SgUseStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUseStatement::pool_size * sizeof(SgUseStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUseStatement::pool_size * sizeof(SgUseStatement));
#endif
        SgUseStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUseStatement::pool_size * sizeof(SgUseStatement), V_SgUseStatement));

        if (SgUseStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUseStatement* blkptr{reinterpret_cast<SgUseStatement*>(SgUseStatement::pools[blockIndex-1])};
                blkptr[SgUseStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUseStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUseStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUseStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUseStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUseStatement::pool_size - 1};
    SgUseStatement* testPointer{reinterpret_cast<SgUseStatement*>(SgUseStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUseStatement::pool_size * (SgUseStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUseStatementStroageClass
 * from its counterpart of SgUseStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUseStatement::initializeStorageClassArray(SgUseStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUseStatement::pools.begin(); block != SgUseStatement::pools.end(); ++block) {
        SgUseStatement* pointer{reinterpret_cast<SgUseStatement*>(*block)};
        for (unsigned i = 0; i < SgUseStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 62392 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgParameterStatement that takes its corresponding StorageClass as parameter
 */
SgParameterStatement::SgParameterStatement(const SgParameterStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 62402 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgParameterStatement*
SgParameterStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgParameterStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgParameterStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgParameterStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgParameterStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgParameterStatement)};
        unsigned long positionInPool{localIndex % SgParameterStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgParameterStatement::pool_size};
        returnPointer = &(((SgParameterStatement*)(SgParameterStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgParameterStatement*
SgParameterStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgParameterStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgParameterStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgParameterStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgParameterStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgParameterStatement)};
        unsigned long positionInPool{localIndex % SgParameterStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgParameterStatement::pool_size};
        returnPointer = &(((SgParameterStatement*)(SgParameterStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgParameterStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgParameterStatement::pools.begin(); block != SgParameterStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgParameterStatement*>(*block)};
        for (unsigned i = 0; i < SgParameterStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgParameterStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgParameterStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgParameterStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgParameterStatement::pools.begin(); block != SgParameterStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgParameterStatement*>(*block)};
        for (unsigned i = 0; i < SgParameterStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgParameterStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgParameterStatement::clearMemoryPool() {
    SgParameterStatement* tempPointer{nullptr};
    if (SgParameterStatement::pools.empty() == false) {
        auto block{SgParameterStatement::pools.begin()};
        SgParameterStatement::next_node = reinterpret_cast<SgParameterStatement*>(*block);
        for (; block != SgParameterStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgParameterStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgParameterStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgParameterStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgParameterStatement::pool_size-1]);
        }
    }
}

void
SgParameterStatement::deleteMemoryPool() {
    for (auto p: SgParameterStatement::pools) {
        ROSE_FREE(p);
    }
    SgParameterStatement::next_node = nullptr;
    SgParameterStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgParameterStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgParameterStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgParameterStatement)};
    for (size_t blockIndex = SgParameterStatement::pools.size(); (blockIndex * SgParameterStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgParameterStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgParameterStatement) = %" PRIuPTR " SgParameterStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgParameterStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgParameterStatement),SgParameterStatement::pool_size);
        }
#endif

        SgParameterStatement* pointer{static_cast<SgParameterStatement*>(ROSE_MALLOC(SgParameterStatement::pool_size * sizeof(SgParameterStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgParameterStatement::pool_size * sizeof(SgParameterStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgParameterStatement::pool_size * sizeof(SgParameterStatement));
#endif
        SgParameterStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgParameterStatement::pool_size * sizeof(SgParameterStatement), V_SgParameterStatement));

        if (SgParameterStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgParameterStatement* blkptr{reinterpret_cast<SgParameterStatement*>(SgParameterStatement::pools[blockIndex-1])};
                blkptr[SgParameterStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgParameterStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgParameterStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgParameterStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgParameterStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgParameterStatement::pool_size - 1};
    SgParameterStatement* testPointer{reinterpret_cast<SgParameterStatement*>(SgParameterStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgParameterStatement::pool_size * (SgParameterStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgParameterStatementStroageClass
 * from its counterpart of SgParameterStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgParameterStatement::initializeStorageClassArray(SgParameterStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgParameterStatement::pools.begin(); block != SgParameterStatement::pools.end(); ++block) {
        SgParameterStatement* pointer{reinterpret_cast<SgParameterStatement*>(*block)};
        for (unsigned i = 0; i < SgParameterStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 62619 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNamespaceDeclarationStatement that takes its corresponding StorageClass as parameter
 */
SgNamespaceDeclarationStatement::SgNamespaceDeclarationStatement(const SgNamespaceDeclarationStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 62629 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgNamespaceDefinitionStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
    p_isUnnamedNamespace = storageSource.storageOf_isUnnamedNamespace;
    p_isInlinedNamespace = storageSource.storageOf_isInlinedNamespace;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNamespaceDeclarationStatement*
SgNamespaceDeclarationStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNamespaceDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgNamespaceDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceDeclarationStatement::pool_size};
        returnPointer = &(((SgNamespaceDeclarationStatement*)(SgNamespaceDeclarationStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNamespaceDeclarationStatement*
SgNamespaceDeclarationStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNamespaceDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNamespaceDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgNamespaceDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceDeclarationStatement::pool_size};
        returnPointer = &(((SgNamespaceDeclarationStatement*)(SgNamespaceDeclarationStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNamespaceDeclarationStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNamespaceDeclarationStatement::pools.begin(); block != SgNamespaceDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceDeclarationStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNamespaceDeclarationStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNamespaceDeclarationStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNamespaceDeclarationStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgNamespaceDeclarationStatement::pools.begin(); block != SgNamespaceDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceDeclarationStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNamespaceDeclarationStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNamespaceDeclarationStatement::clearMemoryPool() {
    SgNamespaceDeclarationStatement* tempPointer{nullptr};
    if (SgNamespaceDeclarationStatement::pools.empty() == false) {
        auto block{SgNamespaceDeclarationStatement::pools.begin()};
        SgNamespaceDeclarationStatement::next_node = reinterpret_cast<SgNamespaceDeclarationStatement*>(*block);
        for (; block != SgNamespaceDeclarationStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNamespaceDeclarationStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNamespaceDeclarationStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNamespaceDeclarationStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNamespaceDeclarationStatement::pool_size-1]);
        }
    }
}

void
SgNamespaceDeclarationStatement::deleteMemoryPool() {
    for (auto p: SgNamespaceDeclarationStatement::pools) {
        ROSE_FREE(p);
    }
    SgNamespaceDeclarationStatement::next_node = nullptr;
    SgNamespaceDeclarationStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNamespaceDeclarationStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDeclarationStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceDeclarationStatement)};
    for (size_t blockIndex = SgNamespaceDeclarationStatement::pools.size(); (blockIndex * SgNamespaceDeclarationStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDeclarationStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceDeclarationStatement) = %" PRIuPTR " SgNamespaceDeclarationStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceDeclarationStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceDeclarationStatement),SgNamespaceDeclarationStatement::pool_size);
        }
#endif

        SgNamespaceDeclarationStatement* pointer{static_cast<SgNamespaceDeclarationStatement*>(ROSE_MALLOC(SgNamespaceDeclarationStatement::pool_size * sizeof(SgNamespaceDeclarationStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNamespaceDeclarationStatement::pool_size * sizeof(SgNamespaceDeclarationStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNamespaceDeclarationStatement::pool_size * sizeof(SgNamespaceDeclarationStatement));
#endif
        SgNamespaceDeclarationStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNamespaceDeclarationStatement::pool_size * sizeof(SgNamespaceDeclarationStatement), V_SgNamespaceDeclarationStatement));

        if (SgNamespaceDeclarationStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNamespaceDeclarationStatement* blkptr{reinterpret_cast<SgNamespaceDeclarationStatement*>(SgNamespaceDeclarationStatement::pools[blockIndex-1])};
                blkptr[SgNamespaceDeclarationStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNamespaceDeclarationStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNamespaceDeclarationStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNamespaceDeclarationStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNamespaceDeclarationStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNamespaceDeclarationStatement::pool_size - 1};
    SgNamespaceDeclarationStatement* testPointer{reinterpret_cast<SgNamespaceDeclarationStatement*>(SgNamespaceDeclarationStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNamespaceDeclarationStatement::pool_size * (SgNamespaceDeclarationStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNamespaceDeclarationStatementStroageClass
 * from its counterpart of SgNamespaceDeclarationStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNamespaceDeclarationStatement::initializeStorageClassArray(SgNamespaceDeclarationStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNamespaceDeclarationStatement::pools.begin(); block != SgNamespaceDeclarationStatement::pools.end(); ++block) {
        SgNamespaceDeclarationStatement* pointer{reinterpret_cast<SgNamespaceDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceDeclarationStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 62850 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEquivalenceStatement that takes its corresponding StorageClass as parameter
 */
SgEquivalenceStatement::SgEquivalenceStatement(const SgEquivalenceStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 62860 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_equivalence_set_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_equivalence_set_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEquivalenceStatement*
SgEquivalenceStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEquivalenceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEquivalenceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEquivalenceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEquivalenceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEquivalenceStatement)};
        unsigned long positionInPool{localIndex % SgEquivalenceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEquivalenceStatement::pool_size};
        returnPointer = &(((SgEquivalenceStatement*)(SgEquivalenceStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEquivalenceStatement*
SgEquivalenceStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEquivalenceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEquivalenceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEquivalenceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEquivalenceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEquivalenceStatement)};
        unsigned long positionInPool{localIndex % SgEquivalenceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEquivalenceStatement::pool_size};
        returnPointer = &(((SgEquivalenceStatement*)(SgEquivalenceStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEquivalenceStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEquivalenceStatement::pools.begin(); block != SgEquivalenceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEquivalenceStatement*>(*block)};
        for (unsigned i = 0; i < SgEquivalenceStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEquivalenceStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEquivalenceStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEquivalenceStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgEquivalenceStatement::pools.begin(); block != SgEquivalenceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEquivalenceStatement*>(*block)};
        for (unsigned i = 0; i < SgEquivalenceStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEquivalenceStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEquivalenceStatement::clearMemoryPool() {
    SgEquivalenceStatement* tempPointer{nullptr};
    if (SgEquivalenceStatement::pools.empty() == false) {
        auto block{SgEquivalenceStatement::pools.begin()};
        SgEquivalenceStatement::next_node = reinterpret_cast<SgEquivalenceStatement*>(*block);
        for (; block != SgEquivalenceStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEquivalenceStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEquivalenceStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEquivalenceStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEquivalenceStatement::pool_size-1]);
        }
    }
}

void
SgEquivalenceStatement::deleteMemoryPool() {
    for (auto p: SgEquivalenceStatement::pools) {
        ROSE_FREE(p);
    }
    SgEquivalenceStatement::next_node = nullptr;
    SgEquivalenceStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEquivalenceStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEquivalenceStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEquivalenceStatement)};
    for (size_t blockIndex = SgEquivalenceStatement::pools.size(); (blockIndex * SgEquivalenceStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEquivalenceStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEquivalenceStatement) = %" PRIuPTR " SgEquivalenceStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEquivalenceStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEquivalenceStatement),SgEquivalenceStatement::pool_size);
        }
#endif

        SgEquivalenceStatement* pointer{static_cast<SgEquivalenceStatement*>(ROSE_MALLOC(SgEquivalenceStatement::pool_size * sizeof(SgEquivalenceStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEquivalenceStatement::pool_size * sizeof(SgEquivalenceStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEquivalenceStatement::pool_size * sizeof(SgEquivalenceStatement));
#endif
        SgEquivalenceStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEquivalenceStatement::pool_size * sizeof(SgEquivalenceStatement), V_SgEquivalenceStatement));

        if (SgEquivalenceStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEquivalenceStatement* blkptr{reinterpret_cast<SgEquivalenceStatement*>(SgEquivalenceStatement::pools[blockIndex-1])};
                blkptr[SgEquivalenceStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEquivalenceStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEquivalenceStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEquivalenceStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEquivalenceStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEquivalenceStatement::pool_size - 1};
    SgEquivalenceStatement* testPointer{reinterpret_cast<SgEquivalenceStatement*>(SgEquivalenceStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEquivalenceStatement::pool_size * (SgEquivalenceStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEquivalenceStatementStroageClass
 * from its counterpart of SgEquivalenceStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEquivalenceStatement::initializeStorageClassArray(SgEquivalenceStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEquivalenceStatement::pools.begin(); block != SgEquivalenceStatement::pools.end(); ++block) {
        SgEquivalenceStatement* pointer{reinterpret_cast<SgEquivalenceStatement*>(*block)};
        for (unsigned i = 0; i < SgEquivalenceStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 63078 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgInterfaceStatement that takes its corresponding StorageClass as parameter
 */
SgInterfaceStatement::SgInterfaceStatement(const SgInterfaceStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 63088 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_generic_spec = storageSource.storageOf_generic_spec;
    p_interface_body_list = storageSource.storageOf_interface_body_list.rebuildDataStoredInEasyStorageClass();
    SgInterfaceBodyPtrList::iterator i_interface_body_list = p_interface_body_list.begin();
    for ( ; i_interface_body_list != p_interface_body_list.end(); ++i_interface_body_list) {
        (*i_interface_body_list) = (SgInterfaceBodyPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_interface_body_list)) );
    }
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgInterfaceStatement*
SgInterfaceStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgInterfaceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceStatement)};
        unsigned long positionInPool{localIndex % SgInterfaceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInterfaceStatement::pool_size};
        returnPointer = &(((SgInterfaceStatement*)(SgInterfaceStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgInterfaceStatement*
SgInterfaceStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgInterfaceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgInterfaceStatement)};
        unsigned long positionInPool{localIndex % SgInterfaceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInterfaceStatement::pool_size};
        returnPointer = &(((SgInterfaceStatement*)(SgInterfaceStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgInterfaceStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgInterfaceStatement::pools.begin(); block != SgInterfaceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInterfaceStatement*>(*block)};
        for (unsigned i = 0; i < SgInterfaceStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgInterfaceStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgInterfaceStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgInterfaceStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgInterfaceStatement::pools.begin(); block != SgInterfaceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInterfaceStatement*>(*block)};
        for (unsigned i = 0; i < SgInterfaceStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgInterfaceStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgInterfaceStatement::clearMemoryPool() {
    SgInterfaceStatement* tempPointer{nullptr};
    if (SgInterfaceStatement::pools.empty() == false) {
        auto block{SgInterfaceStatement::pools.begin()};
        SgInterfaceStatement::next_node = reinterpret_cast<SgInterfaceStatement*>(*block);
        for (; block != SgInterfaceStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgInterfaceStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgInterfaceStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgInterfaceStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgInterfaceStatement::pool_size-1]);
        }
    }
}

void
SgInterfaceStatement::deleteMemoryPool() {
    for (auto p: SgInterfaceStatement::pools) {
        ROSE_FREE(p);
    }
    SgInterfaceStatement::next_node = nullptr;
    SgInterfaceStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgInterfaceStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceStatement)};
    for (size_t blockIndex = SgInterfaceStatement::pools.size(); (blockIndex * SgInterfaceStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceStatement) = %" PRIuPTR " SgInterfaceStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceStatement),SgInterfaceStatement::pool_size);
        }
#endif

        SgInterfaceStatement* pointer{static_cast<SgInterfaceStatement*>(ROSE_MALLOC(SgInterfaceStatement::pool_size * sizeof(SgInterfaceStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgInterfaceStatement::pool_size * sizeof(SgInterfaceStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgInterfaceStatement::pool_size * sizeof(SgInterfaceStatement));
#endif
        SgInterfaceStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgInterfaceStatement::pool_size * sizeof(SgInterfaceStatement), V_SgInterfaceStatement));

        if (SgInterfaceStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgInterfaceStatement* blkptr{reinterpret_cast<SgInterfaceStatement*>(SgInterfaceStatement::pools[blockIndex-1])};
                blkptr[SgInterfaceStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgInterfaceStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgInterfaceStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgInterfaceStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgInterfaceStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgInterfaceStatement::pool_size - 1};
    SgInterfaceStatement* testPointer{reinterpret_cast<SgInterfaceStatement*>(SgInterfaceStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgInterfaceStatement::pool_size * (SgInterfaceStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgInterfaceStatementStroageClass
 * from its counterpart of SgInterfaceStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgInterfaceStatement::initializeStorageClassArray(SgInterfaceStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgInterfaceStatement::pools.begin(); block != SgInterfaceStatement::pools.end(); ++block) {
        SgInterfaceStatement* pointer{reinterpret_cast<SgInterfaceStatement*>(*block)};
        for (unsigned i = 0; i < SgInterfaceStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 63313 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNamespaceAliasDeclarationStatement that takes its corresponding StorageClass as parameter
 */
SgNamespaceAliasDeclarationStatement::SgNamespaceAliasDeclarationStatement(const SgNamespaceAliasDeclarationStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 63323 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_namespaceDeclaration = (SgNamespaceDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_namespaceDeclaration));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_is_alias_for_another_namespace_alias = storageSource.storageOf_is_alias_for_another_namespace_alias;
     p_namespaceAliasDeclaration = (SgNamespaceAliasDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_namespaceAliasDeclaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNamespaceAliasDeclarationStatement*
SgNamespaceAliasDeclarationStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNamespaceAliasDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceAliasDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceAliasDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceAliasDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceAliasDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgNamespaceAliasDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceAliasDeclarationStatement::pool_size};
        returnPointer = &(((SgNamespaceAliasDeclarationStatement*)(SgNamespaceAliasDeclarationStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNamespaceAliasDeclarationStatement*
SgNamespaceAliasDeclarationStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNamespaceAliasDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceAliasDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceAliasDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceAliasDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNamespaceAliasDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgNamespaceAliasDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceAliasDeclarationStatement::pool_size};
        returnPointer = &(((SgNamespaceAliasDeclarationStatement*)(SgNamespaceAliasDeclarationStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNamespaceAliasDeclarationStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNamespaceAliasDeclarationStatement::pools.begin(); block != SgNamespaceAliasDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceAliasDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceAliasDeclarationStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNamespaceAliasDeclarationStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNamespaceAliasDeclarationStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNamespaceAliasDeclarationStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgNamespaceAliasDeclarationStatement::pools.begin(); block != SgNamespaceAliasDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceAliasDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceAliasDeclarationStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNamespaceAliasDeclarationStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNamespaceAliasDeclarationStatement::clearMemoryPool() {
    SgNamespaceAliasDeclarationStatement* tempPointer{nullptr};
    if (SgNamespaceAliasDeclarationStatement::pools.empty() == false) {
        auto block{SgNamespaceAliasDeclarationStatement::pools.begin()};
        SgNamespaceAliasDeclarationStatement::next_node = reinterpret_cast<SgNamespaceAliasDeclarationStatement*>(*block);
        for (; block != SgNamespaceAliasDeclarationStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNamespaceAliasDeclarationStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNamespaceAliasDeclarationStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNamespaceAliasDeclarationStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNamespaceAliasDeclarationStatement::pool_size-1]);
        }
    }
}

void
SgNamespaceAliasDeclarationStatement::deleteMemoryPool() {
    for (auto p: SgNamespaceAliasDeclarationStatement::pools) {
        ROSE_FREE(p);
    }
    SgNamespaceAliasDeclarationStatement::next_node = nullptr;
    SgNamespaceAliasDeclarationStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNamespaceAliasDeclarationStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceAliasDeclarationStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceAliasDeclarationStatement)};
    for (size_t blockIndex = SgNamespaceAliasDeclarationStatement::pools.size(); (blockIndex * SgNamespaceAliasDeclarationStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceAliasDeclarationStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceAliasDeclarationStatement) = %" PRIuPTR " SgNamespaceAliasDeclarationStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceAliasDeclarationStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceAliasDeclarationStatement),SgNamespaceAliasDeclarationStatement::pool_size);
        }
#endif

        SgNamespaceAliasDeclarationStatement* pointer{static_cast<SgNamespaceAliasDeclarationStatement*>(ROSE_MALLOC(SgNamespaceAliasDeclarationStatement::pool_size * sizeof(SgNamespaceAliasDeclarationStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNamespaceAliasDeclarationStatement::pool_size * sizeof(SgNamespaceAliasDeclarationStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNamespaceAliasDeclarationStatement::pool_size * sizeof(SgNamespaceAliasDeclarationStatement));
#endif
        SgNamespaceAliasDeclarationStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNamespaceAliasDeclarationStatement::pool_size * sizeof(SgNamespaceAliasDeclarationStatement), V_SgNamespaceAliasDeclarationStatement));

        if (SgNamespaceAliasDeclarationStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNamespaceAliasDeclarationStatement* blkptr{reinterpret_cast<SgNamespaceAliasDeclarationStatement*>(SgNamespaceAliasDeclarationStatement::pools[blockIndex-1])};
                blkptr[SgNamespaceAliasDeclarationStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNamespaceAliasDeclarationStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNamespaceAliasDeclarationStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNamespaceAliasDeclarationStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNamespaceAliasDeclarationStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNamespaceAliasDeclarationStatement::pool_size - 1};
    SgNamespaceAliasDeclarationStatement* testPointer{reinterpret_cast<SgNamespaceAliasDeclarationStatement*>(SgNamespaceAliasDeclarationStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNamespaceAliasDeclarationStatement::pool_size * (SgNamespaceAliasDeclarationStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNamespaceAliasDeclarationStatementStroageClass
 * from its counterpart of SgNamespaceAliasDeclarationStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNamespaceAliasDeclarationStatement::initializeStorageClassArray(SgNamespaceAliasDeclarationStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNamespaceAliasDeclarationStatement::pools.begin(); block != SgNamespaceAliasDeclarationStatement::pools.end(); ++block) {
        SgNamespaceAliasDeclarationStatement* pointer{reinterpret_cast<SgNamespaceAliasDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgNamespaceAliasDeclarationStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 63547 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCommonBlock that takes its corresponding StorageClass as parameter
 */
SgCommonBlock::SgCommonBlock(const SgCommonBlockStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 63557 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_block_list = storageSource.storageOf_block_list.rebuildDataStoredInEasyStorageClass();
    SgCommonBlockObjectPtrList::iterator i_block_list = p_block_list.begin();
    for ( ; i_block_list != p_block_list.end(); ++i_block_list) {
        (*i_block_list) = (SgCommonBlockObjectPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_block_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCommonBlock*
SgCommonBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCommonBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlock)};
        unsigned long positionInPool{localIndex % SgCommonBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommonBlock::pool_size};
        returnPointer = &(((SgCommonBlock*)(SgCommonBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCommonBlock*
SgCommonBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCommonBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCommonBlock)};
        unsigned long positionInPool{localIndex % SgCommonBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommonBlock::pool_size};
        returnPointer = &(((SgCommonBlock*)(SgCommonBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCommonBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCommonBlock::pools.begin(); block != SgCommonBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommonBlock*>(*block)};
        for (unsigned i = 0; i < SgCommonBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCommonBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCommonBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCommonBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgCommonBlock::pools.begin(); block != SgCommonBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommonBlock*>(*block)};
        for (unsigned i = 0; i < SgCommonBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCommonBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCommonBlock::clearMemoryPool() {
    SgCommonBlock* tempPointer{nullptr};
    if (SgCommonBlock::pools.empty() == false) {
        auto block{SgCommonBlock::pools.begin()};
        SgCommonBlock::next_node = reinterpret_cast<SgCommonBlock*>(*block);
        for (; block != SgCommonBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCommonBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCommonBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCommonBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCommonBlock::pool_size-1]);
        }
    }
}

void
SgCommonBlock::deleteMemoryPool() {
    for (auto p: SgCommonBlock::pools) {
        ROSE_FREE(p);
    }
    SgCommonBlock::next_node = nullptr;
    SgCommonBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCommonBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonBlock)};
    for (size_t blockIndex = SgCommonBlock::pools.size(); (blockIndex * SgCommonBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonBlock) = %" PRIuPTR " SgCommonBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonBlock),SgCommonBlock::pool_size);
        }
#endif

        SgCommonBlock* pointer{static_cast<SgCommonBlock*>(ROSE_MALLOC(SgCommonBlock::pool_size * sizeof(SgCommonBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCommonBlock::pool_size * sizeof(SgCommonBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCommonBlock::pool_size * sizeof(SgCommonBlock));
#endif
        SgCommonBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCommonBlock::pool_size * sizeof(SgCommonBlock), V_SgCommonBlock));

        if (SgCommonBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCommonBlock* blkptr{reinterpret_cast<SgCommonBlock*>(SgCommonBlock::pools[blockIndex-1])};
                blkptr[SgCommonBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCommonBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCommonBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCommonBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCommonBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCommonBlock::pool_size - 1};
    SgCommonBlock* testPointer{reinterpret_cast<SgCommonBlock*>(SgCommonBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCommonBlock::pool_size * (SgCommonBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCommonBlockStroageClass
 * from its counterpart of SgCommonBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCommonBlock::initializeStorageClassArray(SgCommonBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCommonBlock::pools.begin(); block != SgCommonBlock::pools.end(); ++block) {
        SgCommonBlock* pointer{reinterpret_cast<SgCommonBlock*>(*block)};
        for (unsigned i = 0; i < SgCommonBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 63779 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypedefDeclaration that takes its corresponding StorageClass as parameter
 */
SgTypedefDeclaration::SgTypedefDeclaration(const SgTypedefDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 63789 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_base_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_base_type));
     p_type = (SgTypedefType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
     p_parent_scope = (SgSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parent_scope));
    p_typedefBaseTypeContainsDefiningDeclaration = storageSource.storageOf_typedefBaseTypeContainsDefiningDeclaration;
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
    p_requiresGlobalNameQualificationOnType = storageSource.storageOf_requiresGlobalNameQualificationOnType;
    p_name_qualification_length_for_base_type = storageSource.storageOf_name_qualification_length_for_base_type;
    p_type_elaboration_required_for_base_type = storageSource.storageOf_type_elaboration_required_for_base_type;
    p_global_qualification_required_for_base_type = storageSource.storageOf_global_qualification_required_for_base_type;
    p_isAutonomousDeclaration = storageSource.storageOf_isAutonomousDeclaration;
    p_isAssociatedWithDeclarationList = storageSource.storageOf_isAssociatedWithDeclarationList;
    p_isFirstDeclarationOfDeclarationList = storageSource.storageOf_isFirstDeclarationOfDeclarationList;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypedefDeclaration*
SgTypedefDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypedefDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefDeclaration)};
        unsigned long positionInPool{localIndex % SgTypedefDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefDeclaration::pool_size};
        returnPointer = &(((SgTypedefDeclaration*)(SgTypedefDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypedefDeclaration*
SgTypedefDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypedefDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypedefDeclaration)};
        unsigned long positionInPool{localIndex % SgTypedefDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefDeclaration::pool_size};
        returnPointer = &(((SgTypedefDeclaration*)(SgTypedefDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypedefDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypedefDeclaration::pools.begin(); block != SgTypedefDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTypedefDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypedefDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypedefDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypedefDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTypedefDeclaration::pools.begin(); block != SgTypedefDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTypedefDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypedefDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypedefDeclaration::clearMemoryPool() {
    SgTypedefDeclaration* tempPointer{nullptr};
    if (SgTypedefDeclaration::pools.empty() == false) {
        auto block{SgTypedefDeclaration::pools.begin()};
        SgTypedefDeclaration::next_node = reinterpret_cast<SgTypedefDeclaration*>(*block);
        for (; block != SgTypedefDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypedefDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypedefDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypedefDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypedefDeclaration::pool_size-1]);
        }
    }
}

void
SgTypedefDeclaration::deleteMemoryPool() {
    for (auto p: SgTypedefDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTypedefDeclaration::next_node = nullptr;
    SgTypedefDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypedefDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefDeclaration)};
    for (size_t blockIndex = SgTypedefDeclaration::pools.size(); (blockIndex * SgTypedefDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefDeclaration) = %" PRIuPTR " SgTypedefDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefDeclaration),SgTypedefDeclaration::pool_size);
        }
#endif

        SgTypedefDeclaration* pointer{static_cast<SgTypedefDeclaration*>(ROSE_MALLOC(SgTypedefDeclaration::pool_size * sizeof(SgTypedefDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypedefDeclaration::pool_size * sizeof(SgTypedefDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypedefDeclaration::pool_size * sizeof(SgTypedefDeclaration));
#endif
        SgTypedefDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypedefDeclaration::pool_size * sizeof(SgTypedefDeclaration), V_SgTypedefDeclaration));

        if (SgTypedefDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypedefDeclaration* blkptr{reinterpret_cast<SgTypedefDeclaration*>(SgTypedefDeclaration::pools[blockIndex-1])};
                blkptr[SgTypedefDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypedefDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypedefDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypedefDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypedefDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypedefDeclaration::pool_size - 1};
    SgTypedefDeclaration* testPointer{reinterpret_cast<SgTypedefDeclaration*>(SgTypedefDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypedefDeclaration::pool_size * (SgTypedefDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypedefDeclarationStroageClass
 * from its counterpart of SgTypedefDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypedefDeclaration::initializeStorageClassArray(SgTypedefDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypedefDeclaration::pools.begin(); block != SgTypedefDeclaration::pools.end(); ++block) {
        SgTypedefDeclaration* pointer{reinterpret_cast<SgTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTypedefDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 64023 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateTypedefDeclaration that takes its corresponding StorageClass as parameter
 */
SgTemplateTypedefDeclaration::SgTemplateTypedefDeclaration(const SgTemplateTypedefDeclarationStorageClass& storageSource)  : SgTypedefDeclaration (storageSource) {

/* #line 64033 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateParameters = storageSource.storageOf_templateParameters.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_templateParameters = p_templateParameters.begin();
    for ( ; i_templateParameters != p_templateParameters.end(); ++i_templateParameters) {
        (*i_templateParameters) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateParameters)) );
    }
    p_templateSpecializationArguments = storageSource.storageOf_templateSpecializationArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateSpecializationArguments = p_templateSpecializationArguments.begin();
    for ( ; i_templateSpecializationArguments != p_templateSpecializationArguments.end(); ++i_templateSpecializationArguments) {
        (*i_templateSpecializationArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateSpecializationArguments)) );
    }
    p_string = SgName ( storageSource.storageOf_string);
     p_nonreal_decl_scope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_decl_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateTypedefDeclaration*
SgTemplateTypedefDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateTypedefDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateTypedefDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateTypedefDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateTypedefDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateTypedefDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateTypedefDeclaration::pool_size};
        returnPointer = &(((SgTemplateTypedefDeclaration*)(SgTemplateTypedefDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateTypedefDeclaration*
SgTemplateTypedefDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateTypedefDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateTypedefDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateTypedefDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateTypedefDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateTypedefDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateTypedefDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateTypedefDeclaration::pool_size};
        returnPointer = &(((SgTemplateTypedefDeclaration*)(SgTemplateTypedefDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateTypedefDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateTypedefDeclaration::pools.begin(); block != SgTemplateTypedefDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateTypedefDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateTypedefDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateTypedefDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateTypedefDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateTypedefDeclaration::pools.begin(); block != SgTemplateTypedefDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateTypedefDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateTypedefDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateTypedefDeclaration::clearMemoryPool() {
    SgTemplateTypedefDeclaration* tempPointer{nullptr};
    if (SgTemplateTypedefDeclaration::pools.empty() == false) {
        auto block{SgTemplateTypedefDeclaration::pools.begin()};
        SgTemplateTypedefDeclaration::next_node = reinterpret_cast<SgTemplateTypedefDeclaration*>(*block);
        for (; block != SgTemplateTypedefDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateTypedefDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateTypedefDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateTypedefDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateTypedefDeclaration::pool_size-1]);
        }
    }
}

void
SgTemplateTypedefDeclaration::deleteMemoryPool() {
    for (auto p: SgTemplateTypedefDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTemplateTypedefDeclaration::next_node = nullptr;
    SgTemplateTypedefDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateTypedefDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateTypedefDeclaration)};
    for (size_t blockIndex = SgTemplateTypedefDeclaration::pools.size(); (blockIndex * SgTemplateTypedefDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateTypedefDeclaration) = %" PRIuPTR " SgTemplateTypedefDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateTypedefDeclaration),SgTemplateTypedefDeclaration::pool_size);
        }
#endif

        SgTemplateTypedefDeclaration* pointer{static_cast<SgTemplateTypedefDeclaration*>(ROSE_MALLOC(SgTemplateTypedefDeclaration::pool_size * sizeof(SgTemplateTypedefDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateTypedefDeclaration::pool_size * sizeof(SgTemplateTypedefDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateTypedefDeclaration::pool_size * sizeof(SgTemplateTypedefDeclaration));
#endif
        SgTemplateTypedefDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateTypedefDeclaration::pool_size * sizeof(SgTemplateTypedefDeclaration), V_SgTemplateTypedefDeclaration));

        if (SgTemplateTypedefDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateTypedefDeclaration* blkptr{reinterpret_cast<SgTemplateTypedefDeclaration*>(SgTemplateTypedefDeclaration::pools[blockIndex-1])};
                blkptr[SgTemplateTypedefDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateTypedefDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateTypedefDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateTypedefDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateTypedefDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateTypedefDeclaration::pool_size - 1};
    SgTemplateTypedefDeclaration* testPointer{reinterpret_cast<SgTemplateTypedefDeclaration*>(SgTemplateTypedefDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateTypedefDeclaration::pool_size * (SgTemplateTypedefDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateTypedefDeclarationStroageClass
 * from its counterpart of SgTemplateTypedefDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateTypedefDeclaration::initializeStorageClassArray(SgTemplateTypedefDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateTypedefDeclaration::pools.begin(); block != SgTemplateTypedefDeclaration::pools.end(); ++block) {
        SgTemplateTypedefDeclaration* pointer{reinterpret_cast<SgTemplateTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateTypedefDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 64262 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateInstantiationTypedefDeclaration that takes its corresponding StorageClass as parameter
 */
SgTemplateInstantiationTypedefDeclaration::SgTemplateInstantiationTypedefDeclaration(const SgTemplateInstantiationTypedefDeclarationStorageClass& storageSource)  : SgTypedefDeclaration (storageSource) {

/* #line 64272 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateName = SgName ( storageSource.storageOf_templateName);
    p_templateHeader = SgName ( storageSource.storageOf_templateHeader);
     p_templateDeclaration = (SgTemplateTypedefDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
    p_templateArguments = storageSource.storageOf_templateArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateArguments = p_templateArguments.begin();
    for ( ; i_templateArguments != p_templateArguments.end(); ++i_templateArguments) {
        (*i_templateArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateArguments)) );
    }
    p_nameResetFromMangledForm = storageSource.storageOf_nameResetFromMangledForm;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateInstantiationTypedefDeclaration*
SgTemplateInstantiationTypedefDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateInstantiationTypedefDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationTypedefDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationTypedefDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationTypedefDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationTypedefDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationTypedefDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationTypedefDeclaration::pool_size};
        returnPointer = &(((SgTemplateInstantiationTypedefDeclaration*)(SgTemplateInstantiationTypedefDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateInstantiationTypedefDeclaration*
SgTemplateInstantiationTypedefDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateInstantiationTypedefDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationTypedefDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationTypedefDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationTypedefDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateInstantiationTypedefDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationTypedefDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationTypedefDeclaration::pool_size};
        returnPointer = &(((SgTemplateInstantiationTypedefDeclaration*)(SgTemplateInstantiationTypedefDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateInstantiationTypedefDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateInstantiationTypedefDeclaration::pools.begin(); block != SgTemplateInstantiationTypedefDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationTypedefDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateInstantiationTypedefDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateInstantiationTypedefDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateInstantiationTypedefDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateInstantiationTypedefDeclaration::pools.begin(); block != SgTemplateInstantiationTypedefDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationTypedefDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateInstantiationTypedefDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateInstantiationTypedefDeclaration::clearMemoryPool() {
    SgTemplateInstantiationTypedefDeclaration* tempPointer{nullptr};
    if (SgTemplateInstantiationTypedefDeclaration::pools.empty() == false) {
        auto block{SgTemplateInstantiationTypedefDeclaration::pools.begin()};
        SgTemplateInstantiationTypedefDeclaration::next_node = reinterpret_cast<SgTemplateInstantiationTypedefDeclaration*>(*block);
        for (; block != SgTemplateInstantiationTypedefDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateInstantiationTypedefDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateInstantiationTypedefDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateInstantiationTypedefDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateInstantiationTypedefDeclaration::pool_size-1]);
        }
    }
}

void
SgTemplateInstantiationTypedefDeclaration::deleteMemoryPool() {
    for (auto p: SgTemplateInstantiationTypedefDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTemplateInstantiationTypedefDeclaration::next_node = nullptr;
    SgTemplateInstantiationTypedefDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateInstantiationTypedefDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationTypedefDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationTypedefDeclaration)};
    for (size_t blockIndex = SgTemplateInstantiationTypedefDeclaration::pools.size(); (blockIndex * SgTemplateInstantiationTypedefDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationTypedefDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationTypedefDeclaration) = %" PRIuPTR " SgTemplateInstantiationTypedefDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationTypedefDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationTypedefDeclaration),SgTemplateInstantiationTypedefDeclaration::pool_size);
        }
#endif

        SgTemplateInstantiationTypedefDeclaration* pointer{static_cast<SgTemplateInstantiationTypedefDeclaration*>(ROSE_MALLOC(SgTemplateInstantiationTypedefDeclaration::pool_size * sizeof(SgTemplateInstantiationTypedefDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateInstantiationTypedefDeclaration::pool_size * sizeof(SgTemplateInstantiationTypedefDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateInstantiationTypedefDeclaration::pool_size * sizeof(SgTemplateInstantiationTypedefDeclaration));
#endif
        SgTemplateInstantiationTypedefDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateInstantiationTypedefDeclaration::pool_size * sizeof(SgTemplateInstantiationTypedefDeclaration), V_SgTemplateInstantiationTypedefDeclaration));

        if (SgTemplateInstantiationTypedefDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateInstantiationTypedefDeclaration* blkptr{reinterpret_cast<SgTemplateInstantiationTypedefDeclaration*>(SgTemplateInstantiationTypedefDeclaration::pools[blockIndex-1])};
                blkptr[SgTemplateInstantiationTypedefDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateInstantiationTypedefDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateInstantiationTypedefDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateInstantiationTypedefDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateInstantiationTypedefDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateInstantiationTypedefDeclaration::pool_size - 1};
    SgTemplateInstantiationTypedefDeclaration* testPointer{reinterpret_cast<SgTemplateInstantiationTypedefDeclaration*>(SgTemplateInstantiationTypedefDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateInstantiationTypedefDeclaration::pool_size * (SgTemplateInstantiationTypedefDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateInstantiationTypedefDeclarationStroageClass
 * from its counterpart of SgTemplateInstantiationTypedefDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateInstantiationTypedefDeclaration::initializeStorageClassArray(SgTemplateInstantiationTypedefDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateInstantiationTypedefDeclaration::pools.begin(); block != SgTemplateInstantiationTypedefDeclaration::pools.end(); ++block) {
        SgTemplateInstantiationTypedefDeclaration* pointer{reinterpret_cast<SgTemplateInstantiationTypedefDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationTypedefDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 64498 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStatementFunctionStatement that takes its corresponding StorageClass as parameter
 */
SgStatementFunctionStatement::SgStatementFunctionStatement(const SgStatementFunctionStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 64508 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_function = (SgFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_function));
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStatementFunctionStatement*
SgStatementFunctionStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStatementFunctionStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatementFunctionStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatementFunctionStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatementFunctionStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementFunctionStatement)};
        unsigned long positionInPool{localIndex % SgStatementFunctionStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStatementFunctionStatement::pool_size};
        returnPointer = &(((SgStatementFunctionStatement*)(SgStatementFunctionStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStatementFunctionStatement*
SgStatementFunctionStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStatementFunctionStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatementFunctionStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatementFunctionStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatementFunctionStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStatementFunctionStatement)};
        unsigned long positionInPool{localIndex % SgStatementFunctionStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStatementFunctionStatement::pool_size};
        returnPointer = &(((SgStatementFunctionStatement*)(SgStatementFunctionStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStatementFunctionStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStatementFunctionStatement::pools.begin(); block != SgStatementFunctionStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStatementFunctionStatement*>(*block)};
        for (unsigned i = 0; i < SgStatementFunctionStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStatementFunctionStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStatementFunctionStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStatementFunctionStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgStatementFunctionStatement::pools.begin(); block != SgStatementFunctionStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStatementFunctionStatement*>(*block)};
        for (unsigned i = 0; i < SgStatementFunctionStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStatementFunctionStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStatementFunctionStatement::clearMemoryPool() {
    SgStatementFunctionStatement* tempPointer{nullptr};
    if (SgStatementFunctionStatement::pools.empty() == false) {
        auto block{SgStatementFunctionStatement::pools.begin()};
        SgStatementFunctionStatement::next_node = reinterpret_cast<SgStatementFunctionStatement*>(*block);
        for (; block != SgStatementFunctionStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStatementFunctionStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStatementFunctionStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStatementFunctionStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStatementFunctionStatement::pool_size-1]);
        }
    }
}

void
SgStatementFunctionStatement::deleteMemoryPool() {
    for (auto p: SgStatementFunctionStatement::pools) {
        ROSE_FREE(p);
    }
    SgStatementFunctionStatement::next_node = nullptr;
    SgStatementFunctionStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStatementFunctionStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementFunctionStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatementFunctionStatement)};
    for (size_t blockIndex = SgStatementFunctionStatement::pools.size(); (blockIndex * SgStatementFunctionStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementFunctionStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatementFunctionStatement) = %" PRIuPTR " SgStatementFunctionStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementFunctionStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatementFunctionStatement),SgStatementFunctionStatement::pool_size);
        }
#endif

        SgStatementFunctionStatement* pointer{static_cast<SgStatementFunctionStatement*>(ROSE_MALLOC(SgStatementFunctionStatement::pool_size * sizeof(SgStatementFunctionStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStatementFunctionStatement::pool_size * sizeof(SgStatementFunctionStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStatementFunctionStatement::pool_size * sizeof(SgStatementFunctionStatement));
#endif
        SgStatementFunctionStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStatementFunctionStatement::pool_size * sizeof(SgStatementFunctionStatement), V_SgStatementFunctionStatement));

        if (SgStatementFunctionStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStatementFunctionStatement* blkptr{reinterpret_cast<SgStatementFunctionStatement*>(SgStatementFunctionStatement::pools[blockIndex-1])};
                blkptr[SgStatementFunctionStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStatementFunctionStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStatementFunctionStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStatementFunctionStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStatementFunctionStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStatementFunctionStatement::pool_size - 1};
    SgStatementFunctionStatement* testPointer{reinterpret_cast<SgStatementFunctionStatement*>(SgStatementFunctionStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStatementFunctionStatement::pool_size * (SgStatementFunctionStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStatementFunctionStatementStroageClass
 * from its counterpart of SgStatementFunctionStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStatementFunctionStatement::initializeStorageClassArray(SgStatementFunctionStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStatementFunctionStatement::pools.begin(); block != SgStatementFunctionStatement::pools.end(); ++block) {
        SgStatementFunctionStatement* pointer{reinterpret_cast<SgStatementFunctionStatement*>(*block)};
        for (unsigned i = 0; i < SgStatementFunctionStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 64727 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCtorInitializerList that takes its corresponding StorageClass as parameter
 */
SgCtorInitializerList::SgCtorInitializerList(const SgCtorInitializerListStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 64737 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_ctors = storageSource.storageOf_ctors.rebuildDataStoredInEasyStorageClass();
    SgInitializedNamePtrList::iterator i_ctors = p_ctors.begin();
    for ( ; i_ctors != p_ctors.end(); ++i_ctors) {
        (*i_ctors) = (SgInitializedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_ctors)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCtorInitializerList*
SgCtorInitializerList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCtorInitializerList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCtorInitializerList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCtorInitializerList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCtorInitializerList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCtorInitializerList)};
        unsigned long positionInPool{localIndex % SgCtorInitializerList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCtorInitializerList::pool_size};
        returnPointer = &(((SgCtorInitializerList*)(SgCtorInitializerList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCtorInitializerList*
SgCtorInitializerList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCtorInitializerList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCtorInitializerList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCtorInitializerList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCtorInitializerList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCtorInitializerList)};
        unsigned long positionInPool{localIndex % SgCtorInitializerList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCtorInitializerList::pool_size};
        returnPointer = &(((SgCtorInitializerList*)(SgCtorInitializerList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCtorInitializerList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCtorInitializerList::pools.begin(); block != SgCtorInitializerList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCtorInitializerList*>(*block)};
        for (unsigned i = 0; i < SgCtorInitializerList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCtorInitializerList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCtorInitializerList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCtorInitializerList* pointerOfLinkedList{nullptr};
    for (auto block = SgCtorInitializerList::pools.begin(); block != SgCtorInitializerList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCtorInitializerList*>(*block)};
        for (unsigned i = 0; i < SgCtorInitializerList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCtorInitializerList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCtorInitializerList::clearMemoryPool() {
    SgCtorInitializerList* tempPointer{nullptr};
    if (SgCtorInitializerList::pools.empty() == false) {
        auto block{SgCtorInitializerList::pools.begin()};
        SgCtorInitializerList::next_node = reinterpret_cast<SgCtorInitializerList*>(*block);
        for (; block != SgCtorInitializerList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCtorInitializerList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCtorInitializerList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCtorInitializerList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCtorInitializerList::pool_size-1]);
        }
    }
}

void
SgCtorInitializerList::deleteMemoryPool() {
    for (auto p: SgCtorInitializerList::pools) {
        ROSE_FREE(p);
    }
    SgCtorInitializerList::next_node = nullptr;
    SgCtorInitializerList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCtorInitializerList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCtorInitializerList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCtorInitializerList)};
    for (size_t blockIndex = SgCtorInitializerList::pools.size(); (blockIndex * SgCtorInitializerList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCtorInitializerList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCtorInitializerList) = %" PRIuPTR " SgCtorInitializerList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCtorInitializerList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCtorInitializerList),SgCtorInitializerList::pool_size);
        }
#endif

        SgCtorInitializerList* pointer{static_cast<SgCtorInitializerList*>(ROSE_MALLOC(SgCtorInitializerList::pool_size * sizeof(SgCtorInitializerList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCtorInitializerList::pool_size * sizeof(SgCtorInitializerList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCtorInitializerList::pool_size * sizeof(SgCtorInitializerList));
#endif
        SgCtorInitializerList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCtorInitializerList::pool_size * sizeof(SgCtorInitializerList), V_SgCtorInitializerList));

        if (SgCtorInitializerList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCtorInitializerList* blkptr{reinterpret_cast<SgCtorInitializerList*>(SgCtorInitializerList::pools[blockIndex-1])};
                blkptr[SgCtorInitializerList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCtorInitializerList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCtorInitializerList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCtorInitializerList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCtorInitializerList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCtorInitializerList::pool_size - 1};
    SgCtorInitializerList* testPointer{reinterpret_cast<SgCtorInitializerList*>(SgCtorInitializerList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCtorInitializerList::pool_size * (SgCtorInitializerList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCtorInitializerListStroageClass
 * from its counterpart of SgCtorInitializerList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCtorInitializerList::initializeStorageClassArray(SgCtorInitializerListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCtorInitializerList::pools.begin(); block != SgCtorInitializerList::pools.end(); ++block) {
        SgCtorInitializerList* pointer{reinterpret_cast<SgCtorInitializerList*>(*block)};
        for (unsigned i = 0; i < SgCtorInitializerList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 64959 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPragmaDeclaration that takes its corresponding StorageClass as parameter
 */
SgPragmaDeclaration::SgPragmaDeclaration(const SgPragmaDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 64969 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_pragma = (SgPragma*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pragma));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPragmaDeclaration*
SgPragmaDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPragmaDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPragmaDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPragmaDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPragmaDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPragmaDeclaration)};
        unsigned long positionInPool{localIndex % SgPragmaDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPragmaDeclaration::pool_size};
        returnPointer = &(((SgPragmaDeclaration*)(SgPragmaDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPragmaDeclaration*
SgPragmaDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPragmaDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPragmaDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPragmaDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPragmaDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPragmaDeclaration)};
        unsigned long positionInPool{localIndex % SgPragmaDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPragmaDeclaration::pool_size};
        returnPointer = &(((SgPragmaDeclaration*)(SgPragmaDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPragmaDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPragmaDeclaration::pools.begin(); block != SgPragmaDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPragmaDeclaration*>(*block)};
        for (unsigned i = 0; i < SgPragmaDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPragmaDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPragmaDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPragmaDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgPragmaDeclaration::pools.begin(); block != SgPragmaDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPragmaDeclaration*>(*block)};
        for (unsigned i = 0; i < SgPragmaDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPragmaDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPragmaDeclaration::clearMemoryPool() {
    SgPragmaDeclaration* tempPointer{nullptr};
    if (SgPragmaDeclaration::pools.empty() == false) {
        auto block{SgPragmaDeclaration::pools.begin()};
        SgPragmaDeclaration::next_node = reinterpret_cast<SgPragmaDeclaration*>(*block);
        for (; block != SgPragmaDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPragmaDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPragmaDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPragmaDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPragmaDeclaration::pool_size-1]);
        }
    }
}

void
SgPragmaDeclaration::deleteMemoryPool() {
    for (auto p: SgPragmaDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgPragmaDeclaration::next_node = nullptr;
    SgPragmaDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPragmaDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPragmaDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPragmaDeclaration)};
    for (size_t blockIndex = SgPragmaDeclaration::pools.size(); (blockIndex * SgPragmaDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPragmaDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPragmaDeclaration) = %" PRIuPTR " SgPragmaDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPragmaDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPragmaDeclaration),SgPragmaDeclaration::pool_size);
        }
#endif

        SgPragmaDeclaration* pointer{static_cast<SgPragmaDeclaration*>(ROSE_MALLOC(SgPragmaDeclaration::pool_size * sizeof(SgPragmaDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPragmaDeclaration::pool_size * sizeof(SgPragmaDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPragmaDeclaration::pool_size * sizeof(SgPragmaDeclaration));
#endif
        SgPragmaDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPragmaDeclaration::pool_size * sizeof(SgPragmaDeclaration), V_SgPragmaDeclaration));

        if (SgPragmaDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPragmaDeclaration* blkptr{reinterpret_cast<SgPragmaDeclaration*>(SgPragmaDeclaration::pools[blockIndex-1])};
                blkptr[SgPragmaDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPragmaDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPragmaDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPragmaDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPragmaDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPragmaDeclaration::pool_size - 1};
    SgPragmaDeclaration* testPointer{reinterpret_cast<SgPragmaDeclaration*>(SgPragmaDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPragmaDeclaration::pool_size * (SgPragmaDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPragmaDeclarationStroageClass
 * from its counterpart of SgPragmaDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPragmaDeclaration::initializeStorageClassArray(SgPragmaDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPragmaDeclaration::pools.begin(); block != SgPragmaDeclaration::pools.end(); ++block) {
        SgPragmaDeclaration* pointer{reinterpret_cast<SgPragmaDeclaration*>(*block)};
        for (unsigned i = 0; i < SgPragmaDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 65187 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUsingDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgUsingDirectiveStatement::SgUsingDirectiveStatement(const SgUsingDirectiveStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 65197 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_namespaceDeclaration = (SgNamespaceDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_namespaceDeclaration));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUsingDirectiveStatement*
SgUsingDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUsingDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUsingDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUsingDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUsingDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgUsingDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUsingDirectiveStatement::pool_size};
        returnPointer = &(((SgUsingDirectiveStatement*)(SgUsingDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUsingDirectiveStatement*
SgUsingDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUsingDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUsingDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUsingDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUsingDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUsingDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgUsingDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUsingDirectiveStatement::pool_size};
        returnPointer = &(((SgUsingDirectiveStatement*)(SgUsingDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUsingDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUsingDirectiveStatement::pools.begin(); block != SgUsingDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUsingDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgUsingDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUsingDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUsingDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUsingDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUsingDirectiveStatement::pools.begin(); block != SgUsingDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUsingDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgUsingDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUsingDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUsingDirectiveStatement::clearMemoryPool() {
    SgUsingDirectiveStatement* tempPointer{nullptr};
    if (SgUsingDirectiveStatement::pools.empty() == false) {
        auto block{SgUsingDirectiveStatement::pools.begin()};
        SgUsingDirectiveStatement::next_node = reinterpret_cast<SgUsingDirectiveStatement*>(*block);
        for (; block != SgUsingDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUsingDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUsingDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUsingDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUsingDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgUsingDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgUsingDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgUsingDirectiveStatement::next_node = nullptr;
    SgUsingDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUsingDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUsingDirectiveStatement)};
    for (size_t blockIndex = SgUsingDirectiveStatement::pools.size(); (blockIndex * SgUsingDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUsingDirectiveStatement) = %" PRIuPTR " SgUsingDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUsingDirectiveStatement),SgUsingDirectiveStatement::pool_size);
        }
#endif

        SgUsingDirectiveStatement* pointer{static_cast<SgUsingDirectiveStatement*>(ROSE_MALLOC(SgUsingDirectiveStatement::pool_size * sizeof(SgUsingDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUsingDirectiveStatement::pool_size * sizeof(SgUsingDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUsingDirectiveStatement::pool_size * sizeof(SgUsingDirectiveStatement));
#endif
        SgUsingDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUsingDirectiveStatement::pool_size * sizeof(SgUsingDirectiveStatement), V_SgUsingDirectiveStatement));

        if (SgUsingDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUsingDirectiveStatement* blkptr{reinterpret_cast<SgUsingDirectiveStatement*>(SgUsingDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgUsingDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUsingDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUsingDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUsingDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUsingDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUsingDirectiveStatement::pool_size - 1};
    SgUsingDirectiveStatement* testPointer{reinterpret_cast<SgUsingDirectiveStatement*>(SgUsingDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUsingDirectiveStatement::pool_size * (SgUsingDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUsingDirectiveStatementStroageClass
 * from its counterpart of SgUsingDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUsingDirectiveStatement::initializeStorageClassArray(SgUsingDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUsingDirectiveStatement::pools.begin(); block != SgUsingDirectiveStatement::pools.end(); ++block) {
        SgUsingDirectiveStatement* pointer{reinterpret_cast<SgUsingDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgUsingDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 65418 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClassDeclaration that takes its corresponding StorageClass as parameter
 */
SgClassDeclaration::SgClassDeclaration(const SgClassDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 65428 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_class_type = storageSource.storageOf_class_type;
     p_type = (SgClassType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_definition = (SgClassDefinition*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
    p_specialization = storageSource.storageOf_specialization;
    p_from_template = storageSource.storageOf_from_template;
    p_fixupScope = storageSource.storageOf_fixupScope;
    p_isUnNamed = storageSource.storageOf_isUnNamed;
    p_explicit_annotation_interface = storageSource.storageOf_explicit_annotation_interface;
    p_explicit_interface = storageSource.storageOf_explicit_interface;
    p_explicit_enum = storageSource.storageOf_explicit_enum;
    p_explicit_anonymous = storageSource.storageOf_explicit_anonymous;
     p_decoratorList = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_decoratorList));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_isAutonomousDeclaration = storageSource.storageOf_isAutonomousDeclaration;
    p_isRepresentingTemplateParameterInTemplateDeclaration = storageSource.storageOf_isRepresentingTemplateParameterInTemplateDeclaration;
     p_adaParentType = (SgBaseClass*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_adaParentType));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClassDeclaration*
SgClassDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClassDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDeclaration)};
        unsigned long positionInPool{localIndex % SgClassDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassDeclaration::pool_size};
        returnPointer = &(((SgClassDeclaration*)(SgClassDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClassDeclaration*
SgClassDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClassDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClassDeclaration)};
        unsigned long positionInPool{localIndex % SgClassDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassDeclaration::pool_size};
        returnPointer = &(((SgClassDeclaration*)(SgClassDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClassDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClassDeclaration::pools.begin(); block != SgClassDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassDeclaration*>(*block)};
        for (unsigned i = 0; i < SgClassDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClassDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClassDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClassDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgClassDeclaration::pools.begin(); block != SgClassDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassDeclaration*>(*block)};
        for (unsigned i = 0; i < SgClassDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClassDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClassDeclaration::clearMemoryPool() {
    SgClassDeclaration* tempPointer{nullptr};
    if (SgClassDeclaration::pools.empty() == false) {
        auto block{SgClassDeclaration::pools.begin()};
        SgClassDeclaration::next_node = reinterpret_cast<SgClassDeclaration*>(*block);
        for (; block != SgClassDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClassDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClassDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClassDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClassDeclaration::pool_size-1]);
        }
    }
}

void
SgClassDeclaration::deleteMemoryPool() {
    for (auto p: SgClassDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgClassDeclaration::next_node = nullptr;
    SgClassDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClassDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDeclaration)};
    for (size_t blockIndex = SgClassDeclaration::pools.size(); (blockIndex * SgClassDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDeclaration) = %" PRIuPTR " SgClassDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClassDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassDeclaration),SgClassDeclaration::pool_size);
        }
#endif

        SgClassDeclaration* pointer{static_cast<SgClassDeclaration*>(ROSE_MALLOC(SgClassDeclaration::pool_size * sizeof(SgClassDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClassDeclaration::pool_size * sizeof(SgClassDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClassDeclaration::pool_size * sizeof(SgClassDeclaration));
#endif
        SgClassDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClassDeclaration::pool_size * sizeof(SgClassDeclaration), V_SgClassDeclaration));

        if (SgClassDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClassDeclaration* blkptr{reinterpret_cast<SgClassDeclaration*>(SgClassDeclaration::pools[blockIndex-1])};
                blkptr[SgClassDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClassDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClassDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClassDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClassDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClassDeclaration::pool_size - 1};
    SgClassDeclaration* testPointer{reinterpret_cast<SgClassDeclaration*>(SgClassDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClassDeclaration::pool_size * (SgClassDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClassDeclarationStroageClass
 * from its counterpart of SgClassDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClassDeclaration::initializeStorageClassArray(SgClassDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClassDeclaration::pools.begin(); block != SgClassDeclaration::pools.end(); ++block) {
        SgClassDeclaration* pointer{reinterpret_cast<SgClassDeclaration*>(*block)};
        for (unsigned i = 0; i < SgClassDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 65665 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateClassDeclaration that takes its corresponding StorageClass as parameter
 */
SgTemplateClassDeclaration::SgTemplateClassDeclaration(const SgTemplateClassDeclarationStorageClass& storageSource)  : SgClassDeclaration (storageSource) {

/* #line 65675 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateParameters = storageSource.storageOf_templateParameters.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_templateParameters = p_templateParameters.begin();
    for ( ; i_templateParameters != p_templateParameters.end(); ++i_templateParameters) {
        (*i_templateParameters) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateParameters)) );
    }
    p_templateSpecializationArguments = storageSource.storageOf_templateSpecializationArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateSpecializationArguments = p_templateSpecializationArguments.begin();
    for ( ; i_templateSpecializationArguments != p_templateSpecializationArguments.end(); ++i_templateSpecializationArguments) {
        (*i_templateSpecializationArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateSpecializationArguments)) );
    }
    p_string = SgName ( storageSource.storageOf_string);
    p_templateName = SgName ( storageSource.storageOf_templateName);
     p_nonreal_decl_scope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_decl_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateClassDeclaration*
SgTemplateClassDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateClassDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateClassDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateClassDeclaration::pool_size};
        returnPointer = &(((SgTemplateClassDeclaration*)(SgTemplateClassDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateClassDeclaration*
SgTemplateClassDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateClassDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateClassDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateClassDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateClassDeclaration::pool_size};
        returnPointer = &(((SgTemplateClassDeclaration*)(SgTemplateClassDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateClassDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateClassDeclaration::pools.begin(); block != SgTemplateClassDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateClassDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateClassDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateClassDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateClassDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateClassDeclaration::pools.begin(); block != SgTemplateClassDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateClassDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateClassDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateClassDeclaration::clearMemoryPool() {
    SgTemplateClassDeclaration* tempPointer{nullptr};
    if (SgTemplateClassDeclaration::pools.empty() == false) {
        auto block{SgTemplateClassDeclaration::pools.begin()};
        SgTemplateClassDeclaration::next_node = reinterpret_cast<SgTemplateClassDeclaration*>(*block);
        for (; block != SgTemplateClassDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateClassDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateClassDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateClassDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateClassDeclaration::pool_size-1]);
        }
    }
}

void
SgTemplateClassDeclaration::deleteMemoryPool() {
    for (auto p: SgTemplateClassDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTemplateClassDeclaration::next_node = nullptr;
    SgTemplateClassDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateClassDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassDeclaration)};
    for (size_t blockIndex = SgTemplateClassDeclaration::pools.size(); (blockIndex * SgTemplateClassDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassDeclaration) = %" PRIuPTR " SgTemplateClassDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassDeclaration),SgTemplateClassDeclaration::pool_size);
        }
#endif

        SgTemplateClassDeclaration* pointer{static_cast<SgTemplateClassDeclaration*>(ROSE_MALLOC(SgTemplateClassDeclaration::pool_size * sizeof(SgTemplateClassDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateClassDeclaration::pool_size * sizeof(SgTemplateClassDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateClassDeclaration::pool_size * sizeof(SgTemplateClassDeclaration));
#endif
        SgTemplateClassDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateClassDeclaration::pool_size * sizeof(SgTemplateClassDeclaration), V_SgTemplateClassDeclaration));

        if (SgTemplateClassDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateClassDeclaration* blkptr{reinterpret_cast<SgTemplateClassDeclaration*>(SgTemplateClassDeclaration::pools[blockIndex-1])};
                blkptr[SgTemplateClassDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateClassDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateClassDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateClassDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateClassDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateClassDeclaration::pool_size - 1};
    SgTemplateClassDeclaration* testPointer{reinterpret_cast<SgTemplateClassDeclaration*>(SgTemplateClassDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateClassDeclaration::pool_size * (SgTemplateClassDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateClassDeclarationStroageClass
 * from its counterpart of SgTemplateClassDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateClassDeclaration::initializeStorageClassArray(SgTemplateClassDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateClassDeclaration::pools.begin(); block != SgTemplateClassDeclaration::pools.end(); ++block) {
        SgTemplateClassDeclaration* pointer{reinterpret_cast<SgTemplateClassDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 65905 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateInstantiationDecl that takes its corresponding StorageClass as parameter
 */
SgTemplateInstantiationDecl::SgTemplateInstantiationDecl(const SgTemplateInstantiationDeclStorageClass& storageSource)  : SgClassDeclaration (storageSource) {

/* #line 65915 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateName = SgName ( storageSource.storageOf_templateName);
    p_templateHeader = SgName ( storageSource.storageOf_templateHeader);
     p_templateDeclaration = (SgTemplateClassDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
    p_templateArguments = storageSource.storageOf_templateArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateArguments = p_templateArguments.begin();
    for ( ; i_templateArguments != p_templateArguments.end(); ++i_templateArguments) {
        (*i_templateArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateArguments)) );
    }
    p_nameResetFromMangledForm = storageSource.storageOf_nameResetFromMangledForm;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateInstantiationDecl*
SgTemplateInstantiationDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateInstantiationDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDecl)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationDecl::pool_size};
        returnPointer = &(((SgTemplateInstantiationDecl*)(SgTemplateInstantiationDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateInstantiationDecl*
SgTemplateInstantiationDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateInstantiationDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateInstantiationDecl)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationDecl::pool_size};
        returnPointer = &(((SgTemplateInstantiationDecl*)(SgTemplateInstantiationDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateInstantiationDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateInstantiationDecl::pools.begin(); block != SgTemplateInstantiationDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateInstantiationDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateInstantiationDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateInstantiationDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateInstantiationDecl::pools.begin(); block != SgTemplateInstantiationDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateInstantiationDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateInstantiationDecl::clearMemoryPool() {
    SgTemplateInstantiationDecl* tempPointer{nullptr};
    if (SgTemplateInstantiationDecl::pools.empty() == false) {
        auto block{SgTemplateInstantiationDecl::pools.begin()};
        SgTemplateInstantiationDecl::next_node = reinterpret_cast<SgTemplateInstantiationDecl*>(*block);
        for (; block != SgTemplateInstantiationDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateInstantiationDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateInstantiationDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateInstantiationDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateInstantiationDecl::pool_size-1]);
        }
    }
}

void
SgTemplateInstantiationDecl::deleteMemoryPool() {
    for (auto p: SgTemplateInstantiationDecl::pools) {
        ROSE_FREE(p);
    }
    SgTemplateInstantiationDecl::next_node = nullptr;
    SgTemplateInstantiationDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateInstantiationDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDecl)};
    for (size_t blockIndex = SgTemplateInstantiationDecl::pools.size(); (blockIndex * SgTemplateInstantiationDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDecl) = %" PRIuPTR " SgTemplateInstantiationDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationDecl),SgTemplateInstantiationDecl::pool_size);
        }
#endif

        SgTemplateInstantiationDecl* pointer{static_cast<SgTemplateInstantiationDecl*>(ROSE_MALLOC(SgTemplateInstantiationDecl::pool_size * sizeof(SgTemplateInstantiationDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateInstantiationDecl::pool_size * sizeof(SgTemplateInstantiationDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateInstantiationDecl::pool_size * sizeof(SgTemplateInstantiationDecl));
#endif
        SgTemplateInstantiationDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateInstantiationDecl::pool_size * sizeof(SgTemplateInstantiationDecl), V_SgTemplateInstantiationDecl));

        if (SgTemplateInstantiationDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateInstantiationDecl* blkptr{reinterpret_cast<SgTemplateInstantiationDecl*>(SgTemplateInstantiationDecl::pools[blockIndex-1])};
                blkptr[SgTemplateInstantiationDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateInstantiationDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateInstantiationDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateInstantiationDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateInstantiationDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateInstantiationDecl::pool_size - 1};
    SgTemplateInstantiationDecl* testPointer{reinterpret_cast<SgTemplateInstantiationDecl*>(SgTemplateInstantiationDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateInstantiationDecl::pool_size * (SgTemplateInstantiationDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateInstantiationDeclStroageClass
 * from its counterpart of SgTemplateInstantiationDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateInstantiationDecl::initializeStorageClassArray(SgTemplateInstantiationDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateInstantiationDecl::pools.begin(); block != SgTemplateInstantiationDecl::pools.end(); ++block) {
        SgTemplateInstantiationDecl* pointer{reinterpret_cast<SgTemplateInstantiationDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 66141 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDerivedTypeStatement that takes its corresponding StorageClass as parameter
 */
SgDerivedTypeStatement::SgDerivedTypeStatement(const SgDerivedTypeStatementStorageClass& storageSource)  : SgClassDeclaration (storageSource) {

/* #line 66151 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDerivedTypeStatement*
SgDerivedTypeStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDerivedTypeStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDerivedTypeStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDerivedTypeStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDerivedTypeStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDerivedTypeStatement)};
        unsigned long positionInPool{localIndex % SgDerivedTypeStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDerivedTypeStatement::pool_size};
        returnPointer = &(((SgDerivedTypeStatement*)(SgDerivedTypeStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDerivedTypeStatement*
SgDerivedTypeStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDerivedTypeStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDerivedTypeStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDerivedTypeStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDerivedTypeStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDerivedTypeStatement)};
        unsigned long positionInPool{localIndex % SgDerivedTypeStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDerivedTypeStatement::pool_size};
        returnPointer = &(((SgDerivedTypeStatement*)(SgDerivedTypeStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDerivedTypeStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDerivedTypeStatement::pools.begin(); block != SgDerivedTypeStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDerivedTypeStatement*>(*block)};
        for (unsigned i = 0; i < SgDerivedTypeStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDerivedTypeStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDerivedTypeStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDerivedTypeStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgDerivedTypeStatement::pools.begin(); block != SgDerivedTypeStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDerivedTypeStatement*>(*block)};
        for (unsigned i = 0; i < SgDerivedTypeStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDerivedTypeStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDerivedTypeStatement::clearMemoryPool() {
    SgDerivedTypeStatement* tempPointer{nullptr};
    if (SgDerivedTypeStatement::pools.empty() == false) {
        auto block{SgDerivedTypeStatement::pools.begin()};
        SgDerivedTypeStatement::next_node = reinterpret_cast<SgDerivedTypeStatement*>(*block);
        for (; block != SgDerivedTypeStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDerivedTypeStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDerivedTypeStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDerivedTypeStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDerivedTypeStatement::pool_size-1]);
        }
    }
}

void
SgDerivedTypeStatement::deleteMemoryPool() {
    for (auto p: SgDerivedTypeStatement::pools) {
        ROSE_FREE(p);
    }
    SgDerivedTypeStatement::next_node = nullptr;
    SgDerivedTypeStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDerivedTypeStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDerivedTypeStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDerivedTypeStatement)};
    for (size_t blockIndex = SgDerivedTypeStatement::pools.size(); (blockIndex * SgDerivedTypeStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDerivedTypeStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDerivedTypeStatement) = %" PRIuPTR " SgDerivedTypeStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDerivedTypeStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDerivedTypeStatement),SgDerivedTypeStatement::pool_size);
        }
#endif

        SgDerivedTypeStatement* pointer{static_cast<SgDerivedTypeStatement*>(ROSE_MALLOC(SgDerivedTypeStatement::pool_size * sizeof(SgDerivedTypeStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDerivedTypeStatement::pool_size * sizeof(SgDerivedTypeStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDerivedTypeStatement::pool_size * sizeof(SgDerivedTypeStatement));
#endif
        SgDerivedTypeStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDerivedTypeStatement::pool_size * sizeof(SgDerivedTypeStatement), V_SgDerivedTypeStatement));

        if (SgDerivedTypeStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDerivedTypeStatement* blkptr{reinterpret_cast<SgDerivedTypeStatement*>(SgDerivedTypeStatement::pools[blockIndex-1])};
                blkptr[SgDerivedTypeStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDerivedTypeStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDerivedTypeStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDerivedTypeStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDerivedTypeStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDerivedTypeStatement::pool_size - 1};
    SgDerivedTypeStatement* testPointer{reinterpret_cast<SgDerivedTypeStatement*>(SgDerivedTypeStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDerivedTypeStatement::pool_size * (SgDerivedTypeStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDerivedTypeStatementStroageClass
 * from its counterpart of SgDerivedTypeStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDerivedTypeStatement::initializeStorageClassArray(SgDerivedTypeStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDerivedTypeStatement::pools.begin(); block != SgDerivedTypeStatement::pools.end(); ++block) {
        SgDerivedTypeStatement* pointer{reinterpret_cast<SgDerivedTypeStatement*>(*block)};
        for (unsigned i = 0; i < SgDerivedTypeStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 66369 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgModuleStatement that takes its corresponding StorageClass as parameter
 */
SgModuleStatement::SgModuleStatement(const SgModuleStatementStorageClass& storageSource)  : SgClassDeclaration (storageSource) {

/* #line 66379 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgModuleStatement*
SgModuleStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgModuleStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModuleStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModuleStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModuleStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleStatement)};
        unsigned long positionInPool{localIndex % SgModuleStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModuleStatement::pool_size};
        returnPointer = &(((SgModuleStatement*)(SgModuleStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgModuleStatement*
SgModuleStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgModuleStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModuleStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModuleStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModuleStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgModuleStatement)};
        unsigned long positionInPool{localIndex % SgModuleStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModuleStatement::pool_size};
        returnPointer = &(((SgModuleStatement*)(SgModuleStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgModuleStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgModuleStatement::pools.begin(); block != SgModuleStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModuleStatement*>(*block)};
        for (unsigned i = 0; i < SgModuleStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgModuleStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgModuleStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgModuleStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgModuleStatement::pools.begin(); block != SgModuleStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModuleStatement*>(*block)};
        for (unsigned i = 0; i < SgModuleStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgModuleStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgModuleStatement::clearMemoryPool() {
    SgModuleStatement* tempPointer{nullptr};
    if (SgModuleStatement::pools.empty() == false) {
        auto block{SgModuleStatement::pools.begin()};
        SgModuleStatement::next_node = reinterpret_cast<SgModuleStatement*>(*block);
        for (; block != SgModuleStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgModuleStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgModuleStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgModuleStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgModuleStatement::pool_size-1]);
        }
    }
}

void
SgModuleStatement::deleteMemoryPool() {
    for (auto p: SgModuleStatement::pools) {
        ROSE_FREE(p);
    }
    SgModuleStatement::next_node = nullptr;
    SgModuleStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgModuleStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgModuleStatement)};
    for (size_t blockIndex = SgModuleStatement::pools.size(); (blockIndex * SgModuleStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgModuleStatement) = %" PRIuPTR " SgModuleStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgModuleStatement),SgModuleStatement::pool_size);
        }
#endif

        SgModuleStatement* pointer{static_cast<SgModuleStatement*>(ROSE_MALLOC(SgModuleStatement::pool_size * sizeof(SgModuleStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgModuleStatement::pool_size * sizeof(SgModuleStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgModuleStatement::pool_size * sizeof(SgModuleStatement));
#endif
        SgModuleStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgModuleStatement::pool_size * sizeof(SgModuleStatement), V_SgModuleStatement));

        if (SgModuleStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgModuleStatement* blkptr{reinterpret_cast<SgModuleStatement*>(SgModuleStatement::pools[blockIndex-1])};
                blkptr[SgModuleStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgModuleStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgModuleStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgModuleStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgModuleStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgModuleStatement::pool_size - 1};
    SgModuleStatement* testPointer{reinterpret_cast<SgModuleStatement*>(SgModuleStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgModuleStatement::pool_size * (SgModuleStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgModuleStatementStroageClass
 * from its counterpart of SgModuleStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgModuleStatement::initializeStorageClassArray(SgModuleStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgModuleStatement::pools.begin(); block != SgModuleStatement::pools.end(); ++block) {
        SgModuleStatement* pointer{reinterpret_cast<SgModuleStatement*>(*block)};
        for (unsigned i = 0; i < SgModuleStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 66597 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaPackageDeclaration that takes its corresponding StorageClass as parameter
 */
SgJavaPackageDeclaration::SgJavaPackageDeclaration(const SgJavaPackageDeclarationStorageClass& storageSource)  : SgClassDeclaration (storageSource) {

/* #line 66607 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaPackageDeclaration*
SgJavaPackageDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaPackageDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaPackageDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaPackageDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaPackageDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageDeclaration)};
        unsigned long positionInPool{localIndex % SgJavaPackageDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaPackageDeclaration::pool_size};
        returnPointer = &(((SgJavaPackageDeclaration*)(SgJavaPackageDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaPackageDeclaration*
SgJavaPackageDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaPackageDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaPackageDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaPackageDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaPackageDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaPackageDeclaration)};
        unsigned long positionInPool{localIndex % SgJavaPackageDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaPackageDeclaration::pool_size};
        returnPointer = &(((SgJavaPackageDeclaration*)(SgJavaPackageDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaPackageDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaPackageDeclaration::pools.begin(); block != SgJavaPackageDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaPackageDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJavaPackageDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaPackageDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaPackageDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaPackageDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaPackageDeclaration::pools.begin(); block != SgJavaPackageDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaPackageDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJavaPackageDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaPackageDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaPackageDeclaration::clearMemoryPool() {
    SgJavaPackageDeclaration* tempPointer{nullptr};
    if (SgJavaPackageDeclaration::pools.empty() == false) {
        auto block{SgJavaPackageDeclaration::pools.begin()};
        SgJavaPackageDeclaration::next_node = reinterpret_cast<SgJavaPackageDeclaration*>(*block);
        for (; block != SgJavaPackageDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaPackageDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaPackageDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaPackageDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaPackageDeclaration::pool_size-1]);
        }
    }
}

void
SgJavaPackageDeclaration::deleteMemoryPool() {
    for (auto p: SgJavaPackageDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgJavaPackageDeclaration::next_node = nullptr;
    SgJavaPackageDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaPackageDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaPackageDeclaration)};
    for (size_t blockIndex = SgJavaPackageDeclaration::pools.size(); (blockIndex * SgJavaPackageDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaPackageDeclaration) = %" PRIuPTR " SgJavaPackageDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaPackageDeclaration),SgJavaPackageDeclaration::pool_size);
        }
#endif

        SgJavaPackageDeclaration* pointer{static_cast<SgJavaPackageDeclaration*>(ROSE_MALLOC(SgJavaPackageDeclaration::pool_size * sizeof(SgJavaPackageDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaPackageDeclaration::pool_size * sizeof(SgJavaPackageDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaPackageDeclaration::pool_size * sizeof(SgJavaPackageDeclaration));
#endif
        SgJavaPackageDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaPackageDeclaration::pool_size * sizeof(SgJavaPackageDeclaration), V_SgJavaPackageDeclaration));

        if (SgJavaPackageDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaPackageDeclaration* blkptr{reinterpret_cast<SgJavaPackageDeclaration*>(SgJavaPackageDeclaration::pools[blockIndex-1])};
                blkptr[SgJavaPackageDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaPackageDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaPackageDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaPackageDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaPackageDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaPackageDeclaration::pool_size - 1};
    SgJavaPackageDeclaration* testPointer{reinterpret_cast<SgJavaPackageDeclaration*>(SgJavaPackageDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaPackageDeclaration::pool_size * (SgJavaPackageDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaPackageDeclarationStroageClass
 * from its counterpart of SgJavaPackageDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaPackageDeclaration::initializeStorageClassArray(SgJavaPackageDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaPackageDeclaration::pools.begin(); block != SgJavaPackageDeclaration::pools.end(); ++block) {
        SgJavaPackageDeclaration* pointer{reinterpret_cast<SgJavaPackageDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJavaPackageDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 66824 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialTableStatement that takes its corresponding StorageClass as parameter
 */
SgJovialTableStatement::SgJovialTableStatement(const SgJovialTableStatementStorageClass& storageSource)  : SgClassDeclaration (storageSource) {

/* #line 66834 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_table_entry_size = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_table_entry_size));
    p_has_table_entry_size = storageSource.storageOf_has_table_entry_size;
    p_words_per_entry = storageSource.storageOf_words_per_entry;
    p_like_table_name = storageSource.storageOf_like_table_name.rebuildDataStoredInEasyStorageClass();
    p_has_like_option = storageSource.storageOf_has_like_option;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialTableStatement*
SgJovialTableStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialTableStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTableStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTableStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTableStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableStatement)};
        unsigned long positionInPool{localIndex % SgJovialTableStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialTableStatement::pool_size};
        returnPointer = &(((SgJovialTableStatement*)(SgJovialTableStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialTableStatement*
SgJovialTableStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialTableStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTableStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTableStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTableStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialTableStatement)};
        unsigned long positionInPool{localIndex % SgJovialTableStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialTableStatement::pool_size};
        returnPointer = &(((SgJovialTableStatement*)(SgJovialTableStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialTableStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialTableStatement::pools.begin(); block != SgJovialTableStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialTableStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialTableStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialTableStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialTableStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialTableStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialTableStatement::pools.begin(); block != SgJovialTableStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialTableStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialTableStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialTableStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialTableStatement::clearMemoryPool() {
    SgJovialTableStatement* tempPointer{nullptr};
    if (SgJovialTableStatement::pools.empty() == false) {
        auto block{SgJovialTableStatement::pools.begin()};
        SgJovialTableStatement::next_node = reinterpret_cast<SgJovialTableStatement*>(*block);
        for (; block != SgJovialTableStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialTableStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialTableStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialTableStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialTableStatement::pool_size-1]);
        }
    }
}

void
SgJovialTableStatement::deleteMemoryPool() {
    for (auto p: SgJovialTableStatement::pools) {
        ROSE_FREE(p);
    }
    SgJovialTableStatement::next_node = nullptr;
    SgJovialTableStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialTableStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTableStatement)};
    for (size_t blockIndex = SgJovialTableStatement::pools.size(); (blockIndex * SgJovialTableStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTableStatement) = %" PRIuPTR " SgJovialTableStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTableStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTableStatement),SgJovialTableStatement::pool_size);
        }
#endif

        SgJovialTableStatement* pointer{static_cast<SgJovialTableStatement*>(ROSE_MALLOC(SgJovialTableStatement::pool_size * sizeof(SgJovialTableStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialTableStatement::pool_size * sizeof(SgJovialTableStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialTableStatement::pool_size * sizeof(SgJovialTableStatement));
#endif
        SgJovialTableStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialTableStatement::pool_size * sizeof(SgJovialTableStatement), V_SgJovialTableStatement));

        if (SgJovialTableStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialTableStatement* blkptr{reinterpret_cast<SgJovialTableStatement*>(SgJovialTableStatement::pools[blockIndex-1])};
                blkptr[SgJovialTableStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialTableStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialTableStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialTableStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialTableStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialTableStatement::pool_size - 1};
    SgJovialTableStatement* testPointer{reinterpret_cast<SgJovialTableStatement*>(SgJovialTableStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialTableStatement::pool_size * (SgJovialTableStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialTableStatementStroageClass
 * from its counterpart of SgJovialTableStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialTableStatement::initializeStorageClassArray(SgJovialTableStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialTableStatement::pools.begin(); block != SgJovialTableStatement::pools.end(); ++block) {
        SgJovialTableStatement* pointer{reinterpret_cast<SgJovialTableStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialTableStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 67056 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgImplicitStatement that takes its corresponding StorageClass as parameter
 */
SgImplicitStatement::SgImplicitStatement(const SgImplicitStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 67066 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_implicit_none = storageSource.storageOf_implicit_none;
    p_implicit_spec = storageSource.storageOf_implicit_spec;
    p_variables = storageSource.storageOf_variables.rebuildDataStoredInEasyStorageClass();
    SgInitializedNamePtrList::iterator i_variables = p_variables.begin();
    for ( ; i_variables != p_variables.end(); ++i_variables) {
        (*i_variables) = (SgInitializedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_variables)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgImplicitStatement*
SgImplicitStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgImplicitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImplicitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImplicitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImplicitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgImplicitStatement)};
        unsigned long positionInPool{localIndex % SgImplicitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImplicitStatement::pool_size};
        returnPointer = &(((SgImplicitStatement*)(SgImplicitStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgImplicitStatement*
SgImplicitStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgImplicitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImplicitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImplicitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImplicitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgImplicitStatement)};
        unsigned long positionInPool{localIndex % SgImplicitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImplicitStatement::pool_size};
        returnPointer = &(((SgImplicitStatement*)(SgImplicitStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgImplicitStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgImplicitStatement::pools.begin(); block != SgImplicitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImplicitStatement*>(*block)};
        for (unsigned i = 0; i < SgImplicitStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgImplicitStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgImplicitStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgImplicitStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgImplicitStatement::pools.begin(); block != SgImplicitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImplicitStatement*>(*block)};
        for (unsigned i = 0; i < SgImplicitStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgImplicitStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgImplicitStatement::clearMemoryPool() {
    SgImplicitStatement* tempPointer{nullptr};
    if (SgImplicitStatement::pools.empty() == false) {
        auto block{SgImplicitStatement::pools.begin()};
        SgImplicitStatement::next_node = reinterpret_cast<SgImplicitStatement*>(*block);
        for (; block != SgImplicitStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgImplicitStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgImplicitStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgImplicitStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgImplicitStatement::pool_size-1]);
        }
    }
}

void
SgImplicitStatement::deleteMemoryPool() {
    for (auto p: SgImplicitStatement::pools) {
        ROSE_FREE(p);
    }
    SgImplicitStatement::next_node = nullptr;
    SgImplicitStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgImplicitStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgImplicitStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgImplicitStatement)};
    for (size_t blockIndex = SgImplicitStatement::pools.size(); (blockIndex * SgImplicitStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgImplicitStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgImplicitStatement) = %" PRIuPTR " SgImplicitStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgImplicitStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgImplicitStatement),SgImplicitStatement::pool_size);
        }
#endif

        SgImplicitStatement* pointer{static_cast<SgImplicitStatement*>(ROSE_MALLOC(SgImplicitStatement::pool_size * sizeof(SgImplicitStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgImplicitStatement::pool_size * sizeof(SgImplicitStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgImplicitStatement::pool_size * sizeof(SgImplicitStatement));
#endif
        SgImplicitStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgImplicitStatement::pool_size * sizeof(SgImplicitStatement), V_SgImplicitStatement));

        if (SgImplicitStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgImplicitStatement* blkptr{reinterpret_cast<SgImplicitStatement*>(SgImplicitStatement::pools[blockIndex-1])};
                blkptr[SgImplicitStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgImplicitStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgImplicitStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgImplicitStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgImplicitStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgImplicitStatement::pool_size - 1};
    SgImplicitStatement* testPointer{reinterpret_cast<SgImplicitStatement*>(SgImplicitStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgImplicitStatement::pool_size * (SgImplicitStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgImplicitStatementStroageClass
 * from its counterpart of SgImplicitStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgImplicitStatement::initializeStorageClassArray(SgImplicitStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgImplicitStatement::pools.begin(); block != SgImplicitStatement::pools.end(); ++block) {
        SgImplicitStatement* pointer{reinterpret_cast<SgImplicitStatement*>(*block)};
        for (unsigned i = 0; i < SgImplicitStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 67290 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUsingDeclarationStatement that takes its corresponding StorageClass as parameter
 */
SgUsingDeclarationStatement::SgUsingDeclarationStatement(const SgUsingDeclarationStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 67300 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
     p_initializedName = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initializedName));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_is_inheriting_constructor = storageSource.storageOf_is_inheriting_constructor;
    p_adaTypeAttribute = SgName ( storageSource.storageOf_adaTypeAttribute);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUsingDeclarationStatement*
SgUsingDeclarationStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUsingDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUsingDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUsingDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUsingDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgUsingDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUsingDeclarationStatement::pool_size};
        returnPointer = &(((SgUsingDeclarationStatement*)(SgUsingDeclarationStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUsingDeclarationStatement*
SgUsingDeclarationStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUsingDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUsingDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUsingDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUsingDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUsingDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgUsingDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUsingDeclarationStatement::pool_size};
        returnPointer = &(((SgUsingDeclarationStatement*)(SgUsingDeclarationStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUsingDeclarationStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUsingDeclarationStatement::pools.begin(); block != SgUsingDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUsingDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgUsingDeclarationStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUsingDeclarationStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUsingDeclarationStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUsingDeclarationStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUsingDeclarationStatement::pools.begin(); block != SgUsingDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUsingDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgUsingDeclarationStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUsingDeclarationStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUsingDeclarationStatement::clearMemoryPool() {
    SgUsingDeclarationStatement* tempPointer{nullptr};
    if (SgUsingDeclarationStatement::pools.empty() == false) {
        auto block{SgUsingDeclarationStatement::pools.begin()};
        SgUsingDeclarationStatement::next_node = reinterpret_cast<SgUsingDeclarationStatement*>(*block);
        for (; block != SgUsingDeclarationStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUsingDeclarationStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUsingDeclarationStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUsingDeclarationStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUsingDeclarationStatement::pool_size-1]);
        }
    }
}

void
SgUsingDeclarationStatement::deleteMemoryPool() {
    for (auto p: SgUsingDeclarationStatement::pools) {
        ROSE_FREE(p);
    }
    SgUsingDeclarationStatement::next_node = nullptr;
    SgUsingDeclarationStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUsingDeclarationStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDeclarationStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUsingDeclarationStatement)};
    for (size_t blockIndex = SgUsingDeclarationStatement::pools.size(); (blockIndex * SgUsingDeclarationStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDeclarationStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUsingDeclarationStatement) = %" PRIuPTR " SgUsingDeclarationStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUsingDeclarationStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUsingDeclarationStatement),SgUsingDeclarationStatement::pool_size);
        }
#endif

        SgUsingDeclarationStatement* pointer{static_cast<SgUsingDeclarationStatement*>(ROSE_MALLOC(SgUsingDeclarationStatement::pool_size * sizeof(SgUsingDeclarationStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUsingDeclarationStatement::pool_size * sizeof(SgUsingDeclarationStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUsingDeclarationStatement::pool_size * sizeof(SgUsingDeclarationStatement));
#endif
        SgUsingDeclarationStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUsingDeclarationStatement::pool_size * sizeof(SgUsingDeclarationStatement), V_SgUsingDeclarationStatement));

        if (SgUsingDeclarationStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUsingDeclarationStatement* blkptr{reinterpret_cast<SgUsingDeclarationStatement*>(SgUsingDeclarationStatement::pools[blockIndex-1])};
                blkptr[SgUsingDeclarationStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUsingDeclarationStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUsingDeclarationStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUsingDeclarationStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUsingDeclarationStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUsingDeclarationStatement::pool_size - 1};
    SgUsingDeclarationStatement* testPointer{reinterpret_cast<SgUsingDeclarationStatement*>(SgUsingDeclarationStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUsingDeclarationStatement::pool_size * (SgUsingDeclarationStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUsingDeclarationStatementStroageClass
 * from its counterpart of SgUsingDeclarationStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUsingDeclarationStatement::initializeStorageClassArray(SgUsingDeclarationStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUsingDeclarationStatement::pools.begin(); block != SgUsingDeclarationStatement::pools.end(); ++block) {
        SgUsingDeclarationStatement* pointer{reinterpret_cast<SgUsingDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgUsingDeclarationStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 67524 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNamelistStatement that takes its corresponding StorageClass as parameter
 */
SgNamelistStatement::SgNamelistStatement(const SgNamelistStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 67534 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_group_list = storageSource.storageOf_group_list.rebuildDataStoredInEasyStorageClass();
    SgNameGroupPtrList::iterator i_group_list = p_group_list.begin();
    for ( ; i_group_list != p_group_list.end(); ++i_group_list) {
        (*i_group_list) = (SgNameGroupPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_group_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNamelistStatement*
SgNamelistStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNamelistStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamelistStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamelistStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamelistStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNamelistStatement)};
        unsigned long positionInPool{localIndex % SgNamelistStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamelistStatement::pool_size};
        returnPointer = &(((SgNamelistStatement*)(SgNamelistStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNamelistStatement*
SgNamelistStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNamelistStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamelistStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamelistStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamelistStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNamelistStatement)};
        unsigned long positionInPool{localIndex % SgNamelistStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamelistStatement::pool_size};
        returnPointer = &(((SgNamelistStatement*)(SgNamelistStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNamelistStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNamelistStatement::pools.begin(); block != SgNamelistStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamelistStatement*>(*block)};
        for (unsigned i = 0; i < SgNamelistStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNamelistStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNamelistStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNamelistStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgNamelistStatement::pools.begin(); block != SgNamelistStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamelistStatement*>(*block)};
        for (unsigned i = 0; i < SgNamelistStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNamelistStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNamelistStatement::clearMemoryPool() {
    SgNamelistStatement* tempPointer{nullptr};
    if (SgNamelistStatement::pools.empty() == false) {
        auto block{SgNamelistStatement::pools.begin()};
        SgNamelistStatement::next_node = reinterpret_cast<SgNamelistStatement*>(*block);
        for (; block != SgNamelistStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNamelistStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNamelistStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNamelistStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNamelistStatement::pool_size-1]);
        }
    }
}

void
SgNamelistStatement::deleteMemoryPool() {
    for (auto p: SgNamelistStatement::pools) {
        ROSE_FREE(p);
    }
    SgNamelistStatement::next_node = nullptr;
    SgNamelistStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNamelistStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNamelistStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamelistStatement)};
    for (size_t blockIndex = SgNamelistStatement::pools.size(); (blockIndex * SgNamelistStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNamelistStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamelistStatement) = %" PRIuPTR " SgNamelistStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNamelistStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamelistStatement),SgNamelistStatement::pool_size);
        }
#endif

        SgNamelistStatement* pointer{static_cast<SgNamelistStatement*>(ROSE_MALLOC(SgNamelistStatement::pool_size * sizeof(SgNamelistStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNamelistStatement::pool_size * sizeof(SgNamelistStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNamelistStatement::pool_size * sizeof(SgNamelistStatement));
#endif
        SgNamelistStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNamelistStatement::pool_size * sizeof(SgNamelistStatement), V_SgNamelistStatement));

        if (SgNamelistStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNamelistStatement* blkptr{reinterpret_cast<SgNamelistStatement*>(SgNamelistStatement::pools[blockIndex-1])};
                blkptr[SgNamelistStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNamelistStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNamelistStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNamelistStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNamelistStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNamelistStatement::pool_size - 1};
    SgNamelistStatement* testPointer{reinterpret_cast<SgNamelistStatement*>(SgNamelistStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNamelistStatement::pool_size * (SgNamelistStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNamelistStatementStroageClass
 * from its counterpart of SgNamelistStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNamelistStatement::initializeStorageClassArray(SgNamelistStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNamelistStatement::pools.begin(); block != SgNamelistStatement::pools.end(); ++block) {
        SgNamelistStatement* pointer{reinterpret_cast<SgNamelistStatement*>(*block)};
        for (unsigned i = 0; i < SgNamelistStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 67756 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgImportStatement that takes its corresponding StorageClass as parameter
 */
SgImportStatement::SgImportStatement(const SgImportStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 67766 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_import_list = storageSource.storageOf_import_list.rebuildDataStoredInEasyStorageClass();
    SgExpressionPtrList::iterator i_import_list = p_import_list.begin();
    for ( ; i_import_list != p_import_list.end(); ++i_import_list) {
        (*i_import_list) = (SgExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_import_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgImportStatement*
SgImportStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgImportStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImportStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImportStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImportStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgImportStatement)};
        unsigned long positionInPool{localIndex % SgImportStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImportStatement::pool_size};
        returnPointer = &(((SgImportStatement*)(SgImportStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgImportStatement*
SgImportStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgImportStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImportStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImportStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImportStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgImportStatement)};
        unsigned long positionInPool{localIndex % SgImportStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImportStatement::pool_size};
        returnPointer = &(((SgImportStatement*)(SgImportStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgImportStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgImportStatement::pools.begin(); block != SgImportStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImportStatement*>(*block)};
        for (unsigned i = 0; i < SgImportStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgImportStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgImportStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgImportStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgImportStatement::pools.begin(); block != SgImportStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImportStatement*>(*block)};
        for (unsigned i = 0; i < SgImportStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgImportStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgImportStatement::clearMemoryPool() {
    SgImportStatement* tempPointer{nullptr};
    if (SgImportStatement::pools.empty() == false) {
        auto block{SgImportStatement::pools.begin()};
        SgImportStatement::next_node = reinterpret_cast<SgImportStatement*>(*block);
        for (; block != SgImportStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgImportStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgImportStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgImportStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgImportStatement::pool_size-1]);
        }
    }
}

void
SgImportStatement::deleteMemoryPool() {
    for (auto p: SgImportStatement::pools) {
        ROSE_FREE(p);
    }
    SgImportStatement::next_node = nullptr;
    SgImportStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgImportStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgImportStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgImportStatement)};
    for (size_t blockIndex = SgImportStatement::pools.size(); (blockIndex * SgImportStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgImportStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgImportStatement) = %" PRIuPTR " SgImportStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgImportStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgImportStatement),SgImportStatement::pool_size);
        }
#endif

        SgImportStatement* pointer{static_cast<SgImportStatement*>(ROSE_MALLOC(SgImportStatement::pool_size * sizeof(SgImportStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgImportStatement::pool_size * sizeof(SgImportStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgImportStatement::pool_size * sizeof(SgImportStatement));
#endif
        SgImportStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgImportStatement::pool_size * sizeof(SgImportStatement), V_SgImportStatement));

        if (SgImportStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgImportStatement* blkptr{reinterpret_cast<SgImportStatement*>(SgImportStatement::pools[blockIndex-1])};
                blkptr[SgImportStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgImportStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgImportStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgImportStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgImportStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgImportStatement::pool_size - 1};
    SgImportStatement* testPointer{reinterpret_cast<SgImportStatement*>(SgImportStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgImportStatement::pool_size * (SgImportStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgImportStatementStroageClass
 * from its counterpart of SgImportStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgImportStatement::initializeStorageClassArray(SgImportStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgImportStatement::pools.begin(); block != SgImportStatement::pools.end(); ++block) {
        SgImportStatement* pointer{reinterpret_cast<SgImportStatement*>(*block)};
        for (unsigned i = 0; i < SgImportStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 67988 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionDeclaration that takes its corresponding StorageClass as parameter
 */
SgFunctionDeclaration::SgFunctionDeclaration(const SgFunctionDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 67998 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_parameterList = (SgFunctionParameterList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameterList));
    p_functionModifier = SgFunctionModifier ( storageSource.storageOf_functionModifier);
    p_specialFunctionModifier = SgSpecialFunctionModifier ( storageSource.storageOf_specialFunctionModifier);
    p_exceptionSpecification = storageSource.storageOf_exceptionSpecification.rebuildDataStoredInEasyStorageClass();
    SgTypePtrList::iterator i_exceptionSpecification = p_exceptionSpecification.begin();
    for ( ; i_exceptionSpecification != p_exceptionSpecification.end(); ++i_exceptionSpecification) {
        (*i_exceptionSpecification) = (SgTypePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_exceptionSpecification)) );
    }
    p_named_in_end_statement = storageSource.storageOf_named_in_end_statement;
    p_asm_name = storageSource.storageOf_asm_name.rebuildDataStoredInEasyStorageClass();
     p_decoratorList = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_decoratorList));
    p_ada_formal_subprogram_decl = storageSource.storageOf_ada_formal_subprogram_decl;
    p_ada_formal_decl_with_box = storageSource.storageOf_ada_formal_decl_with_box;
     p_type = (SgFunctionType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_definition = (SgFunctionDefinition*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
    p_oldStyleDefinition = storageSource.storageOf_oldStyleDefinition;
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
    p_specialization = storageSource.storageOf_specialization;
    p_requiresNameQualificationOnReturnType = storageSource.storageOf_requiresNameQualificationOnReturnType;
    p_gnu_extension_section = storageSource.storageOf_gnu_extension_section.rebuildDataStoredInEasyStorageClass();
    p_gnu_extension_alias = storageSource.storageOf_gnu_extension_alias.rebuildDataStoredInEasyStorageClass();
    p_gnu_extension_visability = storageSource.storageOf_gnu_extension_visability;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_name_qualification_length_for_return_type = storageSource.storageOf_name_qualification_length_for_return_type;
    p_type_elaboration_required_for_return_type = storageSource.storageOf_type_elaboration_required_for_return_type;
    p_global_qualification_required_for_return_type = storageSource.storageOf_global_qualification_required_for_return_type;
    p_prototypeIsWithoutParameters = storageSource.storageOf_prototypeIsWithoutParameters;
    p_gnu_regparm_attribute = storageSource.storageOf_gnu_regparm_attribute;
     p_type_syntax = (SgFunctionType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type_syntax));
    p_type_syntax_is_available = storageSource.storageOf_type_syntax_is_available;
     p_parameterList_syntax = (SgFunctionParameterList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameterList_syntax));
    p_using_C11_Noreturn_keyword = storageSource.storageOf_using_C11_Noreturn_keyword;
    p_is_constexpr = storageSource.storageOf_is_constexpr;
    p_using_new_function_return_type_syntax = storageSource.storageOf_using_new_function_return_type_syntax;
     p_functionParameterScope = (SgFunctionParameterScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_functionParameterScope));
    p_marked_as_edg_normalization = storageSource.storageOf_marked_as_edg_normalization;
    p_is_implicit_function = storageSource.storageOf_is_implicit_function;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionDeclaration*
SgFunctionDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionDeclaration::pool_size};
        returnPointer = &(((SgFunctionDeclaration*)(SgFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionDeclaration*
SgFunctionDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionDeclaration::pool_size};
        returnPointer = &(((SgFunctionDeclaration*)(SgFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionDeclaration::pools.begin(); block != SgFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgFunctionDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionDeclaration::pools.begin(); block != SgFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgFunctionDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionDeclaration::clearMemoryPool() {
    SgFunctionDeclaration* tempPointer{nullptr};
    if (SgFunctionDeclaration::pools.empty() == false) {
        auto block{SgFunctionDeclaration::pools.begin()};
        SgFunctionDeclaration::next_node = reinterpret_cast<SgFunctionDeclaration*>(*block);
        for (; block != SgFunctionDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionDeclaration::pool_size-1]);
        }
    }
}

void
SgFunctionDeclaration::deleteMemoryPool() {
    for (auto p: SgFunctionDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgFunctionDeclaration::next_node = nullptr;
    SgFunctionDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionDeclaration)};
    for (size_t blockIndex = SgFunctionDeclaration::pools.size(); (blockIndex * SgFunctionDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionDeclaration) = %" PRIuPTR " SgFunctionDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionDeclaration),SgFunctionDeclaration::pool_size);
        }
#endif

        SgFunctionDeclaration* pointer{static_cast<SgFunctionDeclaration*>(ROSE_MALLOC(SgFunctionDeclaration::pool_size * sizeof(SgFunctionDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionDeclaration::pool_size * sizeof(SgFunctionDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionDeclaration::pool_size * sizeof(SgFunctionDeclaration));
#endif
        SgFunctionDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionDeclaration::pool_size * sizeof(SgFunctionDeclaration), V_SgFunctionDeclaration));

        if (SgFunctionDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionDeclaration* blkptr{reinterpret_cast<SgFunctionDeclaration*>(SgFunctionDeclaration::pools[blockIndex-1])};
                blkptr[SgFunctionDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionDeclaration::pool_size - 1};
    SgFunctionDeclaration* testPointer{reinterpret_cast<SgFunctionDeclaration*>(SgFunctionDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionDeclaration::pool_size * (SgFunctionDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionDeclarationStroageClass
 * from its counterpart of SgFunctionDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionDeclaration::initializeStorageClassArray(SgFunctionDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionDeclaration::pools.begin(); block != SgFunctionDeclaration::pools.end(); ++block) {
        SgFunctionDeclaration* pointer{reinterpret_cast<SgFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgFunctionDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 68255 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateFunctionDeclaration that takes its corresponding StorageClass as parameter
 */
SgTemplateFunctionDeclaration::SgTemplateFunctionDeclaration(const SgTemplateFunctionDeclarationStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 68265 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateParameters = storageSource.storageOf_templateParameters.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_templateParameters = p_templateParameters.begin();
    for ( ; i_templateParameters != p_templateParameters.end(); ++i_templateParameters) {
        (*i_templateParameters) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateParameters)) );
    }
    p_templateSpecializationArguments = storageSource.storageOf_templateSpecializationArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateSpecializationArguments = p_templateSpecializationArguments.begin();
    for ( ; i_templateSpecializationArguments != p_templateSpecializationArguments.end(); ++i_templateSpecializationArguments) {
        (*i_templateSpecializationArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateSpecializationArguments)) );
    }
    p_string = SgName ( storageSource.storageOf_string);
    p_string_represents_function_body = storageSource.storageOf_string_represents_function_body;
     p_nonreal_decl_scope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_decl_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateFunctionDeclaration*
SgTemplateFunctionDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionDeclaration::pool_size};
        returnPointer = &(((SgTemplateFunctionDeclaration*)(SgTemplateFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateFunctionDeclaration*
SgTemplateFunctionDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionDeclaration::pool_size};
        returnPointer = &(((SgTemplateFunctionDeclaration*)(SgTemplateFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateFunctionDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateFunctionDeclaration::pools.begin(); block != SgTemplateFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateFunctionDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateFunctionDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateFunctionDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateFunctionDeclaration::pools.begin(); block != SgTemplateFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateFunctionDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateFunctionDeclaration::clearMemoryPool() {
    SgTemplateFunctionDeclaration* tempPointer{nullptr};
    if (SgTemplateFunctionDeclaration::pools.empty() == false) {
        auto block{SgTemplateFunctionDeclaration::pools.begin()};
        SgTemplateFunctionDeclaration::next_node = reinterpret_cast<SgTemplateFunctionDeclaration*>(*block);
        for (; block != SgTemplateFunctionDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateFunctionDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateFunctionDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateFunctionDeclaration::pool_size-1]);
        }
    }
}

void
SgTemplateFunctionDeclaration::deleteMemoryPool() {
    for (auto p: SgTemplateFunctionDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTemplateFunctionDeclaration::next_node = nullptr;
    SgTemplateFunctionDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateFunctionDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionDeclaration)};
    for (size_t blockIndex = SgTemplateFunctionDeclaration::pools.size(); (blockIndex * SgTemplateFunctionDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionDeclaration) = %" PRIuPTR " SgTemplateFunctionDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionDeclaration),SgTemplateFunctionDeclaration::pool_size);
        }
#endif

        SgTemplateFunctionDeclaration* pointer{static_cast<SgTemplateFunctionDeclaration*>(ROSE_MALLOC(SgTemplateFunctionDeclaration::pool_size * sizeof(SgTemplateFunctionDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateFunctionDeclaration::pool_size * sizeof(SgTemplateFunctionDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateFunctionDeclaration::pool_size * sizeof(SgTemplateFunctionDeclaration));
#endif
        SgTemplateFunctionDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateFunctionDeclaration::pool_size * sizeof(SgTemplateFunctionDeclaration), V_SgTemplateFunctionDeclaration));

        if (SgTemplateFunctionDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateFunctionDeclaration* blkptr{reinterpret_cast<SgTemplateFunctionDeclaration*>(SgTemplateFunctionDeclaration::pools[blockIndex-1])};
                blkptr[SgTemplateFunctionDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateFunctionDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateFunctionDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateFunctionDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateFunctionDeclaration::pool_size - 1};
    SgTemplateFunctionDeclaration* testPointer{reinterpret_cast<SgTemplateFunctionDeclaration*>(SgTemplateFunctionDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateFunctionDeclaration::pool_size * (SgTemplateFunctionDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateFunctionDeclarationStroageClass
 * from its counterpart of SgTemplateFunctionDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateFunctionDeclaration::initializeStorageClassArray(SgTemplateFunctionDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateFunctionDeclaration::pools.begin(); block != SgTemplateFunctionDeclaration::pools.end(); ++block) {
        SgTemplateFunctionDeclaration* pointer{reinterpret_cast<SgTemplateFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 68495 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMemberFunctionDeclaration that takes its corresponding StorageClass as parameter
 */
SgMemberFunctionDeclaration::SgMemberFunctionDeclaration(const SgMemberFunctionDeclarationStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 68505 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_CtorInitializerList = (SgCtorInitializerList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_CtorInitializerList));
     p_associatedClassDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_associatedClassDeclaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMemberFunctionDeclaration*
SgMemberFunctionDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMemberFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgMemberFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionDeclaration::pool_size};
        returnPointer = &(((SgMemberFunctionDeclaration*)(SgMemberFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMemberFunctionDeclaration*
SgMemberFunctionDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMemberFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMemberFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgMemberFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionDeclaration::pool_size};
        returnPointer = &(((SgMemberFunctionDeclaration*)(SgMemberFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMemberFunctionDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMemberFunctionDeclaration::pools.begin(); block != SgMemberFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMemberFunctionDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMemberFunctionDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMemberFunctionDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgMemberFunctionDeclaration::pools.begin(); block != SgMemberFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMemberFunctionDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMemberFunctionDeclaration::clearMemoryPool() {
    SgMemberFunctionDeclaration* tempPointer{nullptr};
    if (SgMemberFunctionDeclaration::pools.empty() == false) {
        auto block{SgMemberFunctionDeclaration::pools.begin()};
        SgMemberFunctionDeclaration::next_node = reinterpret_cast<SgMemberFunctionDeclaration*>(*block);
        for (; block != SgMemberFunctionDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMemberFunctionDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMemberFunctionDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMemberFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMemberFunctionDeclaration::pool_size-1]);
        }
    }
}

void
SgMemberFunctionDeclaration::deleteMemoryPool() {
    for (auto p: SgMemberFunctionDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgMemberFunctionDeclaration::next_node = nullptr;
    SgMemberFunctionDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMemberFunctionDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionDeclaration)};
    for (size_t blockIndex = SgMemberFunctionDeclaration::pools.size(); (blockIndex * SgMemberFunctionDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionDeclaration) = %" PRIuPTR " SgMemberFunctionDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionDeclaration),SgMemberFunctionDeclaration::pool_size);
        }
#endif

        SgMemberFunctionDeclaration* pointer{static_cast<SgMemberFunctionDeclaration*>(ROSE_MALLOC(SgMemberFunctionDeclaration::pool_size * sizeof(SgMemberFunctionDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMemberFunctionDeclaration::pool_size * sizeof(SgMemberFunctionDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMemberFunctionDeclaration::pool_size * sizeof(SgMemberFunctionDeclaration));
#endif
        SgMemberFunctionDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMemberFunctionDeclaration::pool_size * sizeof(SgMemberFunctionDeclaration), V_SgMemberFunctionDeclaration));

        if (SgMemberFunctionDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMemberFunctionDeclaration* blkptr{reinterpret_cast<SgMemberFunctionDeclaration*>(SgMemberFunctionDeclaration::pools[blockIndex-1])};
                blkptr[SgMemberFunctionDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMemberFunctionDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMemberFunctionDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMemberFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMemberFunctionDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMemberFunctionDeclaration::pool_size - 1};
    SgMemberFunctionDeclaration* testPointer{reinterpret_cast<SgMemberFunctionDeclaration*>(SgMemberFunctionDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMemberFunctionDeclaration::pool_size * (SgMemberFunctionDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMemberFunctionDeclarationStroageClass
 * from its counterpart of SgMemberFunctionDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMemberFunctionDeclaration::initializeStorageClassArray(SgMemberFunctionDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMemberFunctionDeclaration::pools.begin(); block != SgMemberFunctionDeclaration::pools.end(); ++block) {
        SgMemberFunctionDeclaration* pointer{reinterpret_cast<SgMemberFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 68724 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateMemberFunctionDeclaration that takes its corresponding StorageClass as parameter
 */
SgTemplateMemberFunctionDeclaration::SgTemplateMemberFunctionDeclaration(const SgTemplateMemberFunctionDeclarationStorageClass& storageSource)  : SgMemberFunctionDeclaration (storageSource) {

/* #line 68734 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateParameters = storageSource.storageOf_templateParameters.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_templateParameters = p_templateParameters.begin();
    for ( ; i_templateParameters != p_templateParameters.end(); ++i_templateParameters) {
        (*i_templateParameters) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateParameters)) );
    }
    p_templateSpecializationArguments = storageSource.storageOf_templateSpecializationArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateSpecializationArguments = p_templateSpecializationArguments.begin();
    for ( ; i_templateSpecializationArguments != p_templateSpecializationArguments.end(); ++i_templateSpecializationArguments) {
        (*i_templateSpecializationArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateSpecializationArguments)) );
    }
    p_string = SgName ( storageSource.storageOf_string);
    p_string_represents_function_body = storageSource.storageOf_string_represents_function_body;
     p_nonreal_decl_scope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_decl_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateMemberFunctionDeclaration*
SgTemplateMemberFunctionDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateMemberFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateMemberFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateMemberFunctionDeclaration::pool_size};
        returnPointer = &(((SgTemplateMemberFunctionDeclaration*)(SgTemplateMemberFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateMemberFunctionDeclaration*
SgTemplateMemberFunctionDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateMemberFunctionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateMemberFunctionDeclaration)};
        unsigned long positionInPool{localIndex % SgTemplateMemberFunctionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateMemberFunctionDeclaration::pool_size};
        returnPointer = &(((SgTemplateMemberFunctionDeclaration*)(SgTemplateMemberFunctionDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateMemberFunctionDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateMemberFunctionDeclaration::pools.begin(); block != SgTemplateMemberFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateMemberFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateMemberFunctionDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateMemberFunctionDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateMemberFunctionDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateMemberFunctionDeclaration::pools.begin(); block != SgTemplateMemberFunctionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateMemberFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateMemberFunctionDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateMemberFunctionDeclaration::clearMemoryPool() {
    SgTemplateMemberFunctionDeclaration* tempPointer{nullptr};
    if (SgTemplateMemberFunctionDeclaration::pools.empty() == false) {
        auto block{SgTemplateMemberFunctionDeclaration::pools.begin()};
        SgTemplateMemberFunctionDeclaration::next_node = reinterpret_cast<SgTemplateMemberFunctionDeclaration*>(*block);
        for (; block != SgTemplateMemberFunctionDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateMemberFunctionDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateMemberFunctionDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateMemberFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateMemberFunctionDeclaration::pool_size-1]);
        }
    }
}

void
SgTemplateMemberFunctionDeclaration::deleteMemoryPool() {
    for (auto p: SgTemplateMemberFunctionDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgTemplateMemberFunctionDeclaration::next_node = nullptr;
    SgTemplateMemberFunctionDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateMemberFunctionDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionDeclaration)};
    for (size_t blockIndex = SgTemplateMemberFunctionDeclaration::pools.size(); (blockIndex * SgTemplateMemberFunctionDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionDeclaration) = %" PRIuPTR " SgTemplateMemberFunctionDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionDeclaration),SgTemplateMemberFunctionDeclaration::pool_size);
        }
#endif

        SgTemplateMemberFunctionDeclaration* pointer{static_cast<SgTemplateMemberFunctionDeclaration*>(ROSE_MALLOC(SgTemplateMemberFunctionDeclaration::pool_size * sizeof(SgTemplateMemberFunctionDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateMemberFunctionDeclaration::pool_size * sizeof(SgTemplateMemberFunctionDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateMemberFunctionDeclaration::pool_size * sizeof(SgTemplateMemberFunctionDeclaration));
#endif
        SgTemplateMemberFunctionDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateMemberFunctionDeclaration::pool_size * sizeof(SgTemplateMemberFunctionDeclaration), V_SgTemplateMemberFunctionDeclaration));

        if (SgTemplateMemberFunctionDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateMemberFunctionDeclaration* blkptr{reinterpret_cast<SgTemplateMemberFunctionDeclaration*>(SgTemplateMemberFunctionDeclaration::pools[blockIndex-1])};
                blkptr[SgTemplateMemberFunctionDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateMemberFunctionDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateMemberFunctionDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateMemberFunctionDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateMemberFunctionDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateMemberFunctionDeclaration::pool_size - 1};
    SgTemplateMemberFunctionDeclaration* testPointer{reinterpret_cast<SgTemplateMemberFunctionDeclaration*>(SgTemplateMemberFunctionDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateMemberFunctionDeclaration::pool_size * (SgTemplateMemberFunctionDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateMemberFunctionDeclarationStroageClass
 * from its counterpart of SgTemplateMemberFunctionDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateMemberFunctionDeclaration::initializeStorageClassArray(SgTemplateMemberFunctionDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateMemberFunctionDeclaration::pools.begin(); block != SgTemplateMemberFunctionDeclaration::pools.end(); ++block) {
        SgTemplateMemberFunctionDeclaration* pointer{reinterpret_cast<SgTemplateMemberFunctionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 68964 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateInstantiationMemberFunctionDecl that takes its corresponding StorageClass as parameter
 */
SgTemplateInstantiationMemberFunctionDecl::SgTemplateInstantiationMemberFunctionDecl(const SgTemplateInstantiationMemberFunctionDeclStorageClass& storageSource)  : SgMemberFunctionDeclaration (storageSource) {

/* #line 68974 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateName = SgName ( storageSource.storageOf_templateName);
     p_templateDeclaration = (SgTemplateMemberFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
    p_templateArguments = storageSource.storageOf_templateArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateArguments = p_templateArguments.begin();
    for ( ; i_templateArguments != p_templateArguments.end(); ++i_templateArguments) {
        (*i_templateArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateArguments)) );
    }
    p_nameResetFromMangledForm = storageSource.storageOf_nameResetFromMangledForm;
    p_template_argument_list_is_explicit = storageSource.storageOf_template_argument_list_is_explicit;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateInstantiationMemberFunctionDecl*
SgTemplateInstantiationMemberFunctionDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateInstantiationMemberFunctionDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationMemberFunctionDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationMemberFunctionDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationMemberFunctionDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationMemberFunctionDecl)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationMemberFunctionDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationMemberFunctionDecl::pool_size};
        returnPointer = &(((SgTemplateInstantiationMemberFunctionDecl*)(SgTemplateInstantiationMemberFunctionDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateInstantiationMemberFunctionDecl*
SgTemplateInstantiationMemberFunctionDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateInstantiationMemberFunctionDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationMemberFunctionDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationMemberFunctionDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationMemberFunctionDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateInstantiationMemberFunctionDecl)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationMemberFunctionDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationMemberFunctionDecl::pool_size};
        returnPointer = &(((SgTemplateInstantiationMemberFunctionDecl*)(SgTemplateInstantiationMemberFunctionDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateInstantiationMemberFunctionDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateInstantiationMemberFunctionDecl::pools.begin(); block != SgTemplateInstantiationMemberFunctionDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationMemberFunctionDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationMemberFunctionDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateInstantiationMemberFunctionDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateInstantiationMemberFunctionDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateInstantiationMemberFunctionDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateInstantiationMemberFunctionDecl::pools.begin(); block != SgTemplateInstantiationMemberFunctionDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationMemberFunctionDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationMemberFunctionDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateInstantiationMemberFunctionDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateInstantiationMemberFunctionDecl::clearMemoryPool() {
    SgTemplateInstantiationMemberFunctionDecl* tempPointer{nullptr};
    if (SgTemplateInstantiationMemberFunctionDecl::pools.empty() == false) {
        auto block{SgTemplateInstantiationMemberFunctionDecl::pools.begin()};
        SgTemplateInstantiationMemberFunctionDecl::next_node = reinterpret_cast<SgTemplateInstantiationMemberFunctionDecl*>(*block);
        for (; block != SgTemplateInstantiationMemberFunctionDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateInstantiationMemberFunctionDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateInstantiationMemberFunctionDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateInstantiationMemberFunctionDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateInstantiationMemberFunctionDecl::pool_size-1]);
        }
    }
}

void
SgTemplateInstantiationMemberFunctionDecl::deleteMemoryPool() {
    for (auto p: SgTemplateInstantiationMemberFunctionDecl::pools) {
        ROSE_FREE(p);
    }
    SgTemplateInstantiationMemberFunctionDecl::next_node = nullptr;
    SgTemplateInstantiationMemberFunctionDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateInstantiationMemberFunctionDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationMemberFunctionDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationMemberFunctionDecl)};
    for (size_t blockIndex = SgTemplateInstantiationMemberFunctionDecl::pools.size(); (blockIndex * SgTemplateInstantiationMemberFunctionDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationMemberFunctionDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationMemberFunctionDecl) = %" PRIuPTR " SgTemplateInstantiationMemberFunctionDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationMemberFunctionDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationMemberFunctionDecl),SgTemplateInstantiationMemberFunctionDecl::pool_size);
        }
#endif

        SgTemplateInstantiationMemberFunctionDecl* pointer{static_cast<SgTemplateInstantiationMemberFunctionDecl*>(ROSE_MALLOC(SgTemplateInstantiationMemberFunctionDecl::pool_size * sizeof(SgTemplateInstantiationMemberFunctionDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateInstantiationMemberFunctionDecl::pool_size * sizeof(SgTemplateInstantiationMemberFunctionDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateInstantiationMemberFunctionDecl::pool_size * sizeof(SgTemplateInstantiationMemberFunctionDecl));
#endif
        SgTemplateInstantiationMemberFunctionDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateInstantiationMemberFunctionDecl::pool_size * sizeof(SgTemplateInstantiationMemberFunctionDecl), V_SgTemplateInstantiationMemberFunctionDecl));

        if (SgTemplateInstantiationMemberFunctionDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateInstantiationMemberFunctionDecl* blkptr{reinterpret_cast<SgTemplateInstantiationMemberFunctionDecl*>(SgTemplateInstantiationMemberFunctionDecl::pools[blockIndex-1])};
                blkptr[SgTemplateInstantiationMemberFunctionDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateInstantiationMemberFunctionDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateInstantiationMemberFunctionDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateInstantiationMemberFunctionDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateInstantiationMemberFunctionDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateInstantiationMemberFunctionDecl::pool_size - 1};
    SgTemplateInstantiationMemberFunctionDecl* testPointer{reinterpret_cast<SgTemplateInstantiationMemberFunctionDecl*>(SgTemplateInstantiationMemberFunctionDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateInstantiationMemberFunctionDecl::pool_size * (SgTemplateInstantiationMemberFunctionDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateInstantiationMemberFunctionDeclStroageClass
 * from its counterpart of SgTemplateInstantiationMemberFunctionDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateInstantiationMemberFunctionDecl::initializeStorageClassArray(SgTemplateInstantiationMemberFunctionDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateInstantiationMemberFunctionDecl::pools.begin(); block != SgTemplateInstantiationMemberFunctionDecl::pools.end(); ++block) {
        SgTemplateInstantiationMemberFunctionDecl* pointer{reinterpret_cast<SgTemplateInstantiationMemberFunctionDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationMemberFunctionDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 69200 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateInstantiationFunctionDecl that takes its corresponding StorageClass as parameter
 */
SgTemplateInstantiationFunctionDecl::SgTemplateInstantiationFunctionDecl(const SgTemplateInstantiationFunctionDeclStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 69210 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_templateName = SgName ( storageSource.storageOf_templateName);
     p_templateDeclaration = (SgTemplateFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
    p_templateArguments = storageSource.storageOf_templateArguments.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_templateArguments = p_templateArguments.begin();
    for ( ; i_templateArguments != p_templateArguments.end(); ++i_templateArguments) {
        (*i_templateArguments) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_templateArguments)) );
    }
    p_nameResetFromMangledForm = storageSource.storageOf_nameResetFromMangledForm;
    p_template_argument_list_is_explicit = storageSource.storageOf_template_argument_list_is_explicit;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateInstantiationFunctionDecl*
SgTemplateInstantiationFunctionDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateInstantiationFunctionDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationFunctionDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationFunctionDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateInstantiationFunctionDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationFunctionDecl)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationFunctionDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationFunctionDecl::pool_size};
        returnPointer = &(((SgTemplateInstantiationFunctionDecl*)(SgTemplateInstantiationFunctionDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateInstantiationFunctionDecl*
SgTemplateInstantiationFunctionDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateInstantiationFunctionDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationFunctionDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationFunctionDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateInstantiationFunctionDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateInstantiationFunctionDecl)};
        unsigned long positionInPool{localIndex % SgTemplateInstantiationFunctionDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateInstantiationFunctionDecl::pool_size};
        returnPointer = &(((SgTemplateInstantiationFunctionDecl*)(SgTemplateInstantiationFunctionDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateInstantiationFunctionDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateInstantiationFunctionDecl::pools.begin(); block != SgTemplateInstantiationFunctionDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationFunctionDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationFunctionDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateInstantiationFunctionDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateInstantiationFunctionDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateInstantiationFunctionDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateInstantiationFunctionDecl::pools.begin(); block != SgTemplateInstantiationFunctionDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateInstantiationFunctionDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationFunctionDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateInstantiationFunctionDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateInstantiationFunctionDecl::clearMemoryPool() {
    SgTemplateInstantiationFunctionDecl* tempPointer{nullptr};
    if (SgTemplateInstantiationFunctionDecl::pools.empty() == false) {
        auto block{SgTemplateInstantiationFunctionDecl::pools.begin()};
        SgTemplateInstantiationFunctionDecl::next_node = reinterpret_cast<SgTemplateInstantiationFunctionDecl*>(*block);
        for (; block != SgTemplateInstantiationFunctionDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateInstantiationFunctionDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateInstantiationFunctionDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateInstantiationFunctionDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateInstantiationFunctionDecl::pool_size-1]);
        }
    }
}

void
SgTemplateInstantiationFunctionDecl::deleteMemoryPool() {
    for (auto p: SgTemplateInstantiationFunctionDecl::pools) {
        ROSE_FREE(p);
    }
    SgTemplateInstantiationFunctionDecl::next_node = nullptr;
    SgTemplateInstantiationFunctionDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateInstantiationFunctionDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationFunctionDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationFunctionDecl)};
    for (size_t blockIndex = SgTemplateInstantiationFunctionDecl::pools.size(); (blockIndex * SgTemplateInstantiationFunctionDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationFunctionDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationFunctionDecl) = %" PRIuPTR " SgTemplateInstantiationFunctionDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateInstantiationFunctionDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateInstantiationFunctionDecl),SgTemplateInstantiationFunctionDecl::pool_size);
        }
#endif

        SgTemplateInstantiationFunctionDecl* pointer{static_cast<SgTemplateInstantiationFunctionDecl*>(ROSE_MALLOC(SgTemplateInstantiationFunctionDecl::pool_size * sizeof(SgTemplateInstantiationFunctionDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateInstantiationFunctionDecl::pool_size * sizeof(SgTemplateInstantiationFunctionDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateInstantiationFunctionDecl::pool_size * sizeof(SgTemplateInstantiationFunctionDecl));
#endif
        SgTemplateInstantiationFunctionDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateInstantiationFunctionDecl::pool_size * sizeof(SgTemplateInstantiationFunctionDecl), V_SgTemplateInstantiationFunctionDecl));

        if (SgTemplateInstantiationFunctionDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateInstantiationFunctionDecl* blkptr{reinterpret_cast<SgTemplateInstantiationFunctionDecl*>(SgTemplateInstantiationFunctionDecl::pools[blockIndex-1])};
                blkptr[SgTemplateInstantiationFunctionDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateInstantiationFunctionDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateInstantiationFunctionDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateInstantiationFunctionDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateInstantiationFunctionDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateInstantiationFunctionDecl::pool_size - 1};
    SgTemplateInstantiationFunctionDecl* testPointer{reinterpret_cast<SgTemplateInstantiationFunctionDecl*>(SgTemplateInstantiationFunctionDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateInstantiationFunctionDecl::pool_size * (SgTemplateInstantiationFunctionDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateInstantiationFunctionDeclStroageClass
 * from its counterpart of SgTemplateInstantiationFunctionDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateInstantiationFunctionDecl::initializeStorageClassArray(SgTemplateInstantiationFunctionDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateInstantiationFunctionDecl::pools.begin(); block != SgTemplateInstantiationFunctionDecl::pools.end(); ++block) {
        SgTemplateInstantiationFunctionDecl* pointer{reinterpret_cast<SgTemplateInstantiationFunctionDecl*>(*block)};
        for (unsigned i = 0; i < SgTemplateInstantiationFunctionDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 69436 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgProgramHeaderStatement that takes its corresponding StorageClass as parameter
 */
SgProgramHeaderStatement::SgProgramHeaderStatement(const SgProgramHeaderStatementStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 69446 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_program_statement_explicit = storageSource.storageOf_program_statement_explicit;
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgProgramHeaderStatement*
SgProgramHeaderStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgProgramHeaderStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProgramHeaderStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProgramHeaderStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProgramHeaderStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgProgramHeaderStatement)};
        unsigned long positionInPool{localIndex % SgProgramHeaderStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProgramHeaderStatement::pool_size};
        returnPointer = &(((SgProgramHeaderStatement*)(SgProgramHeaderStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgProgramHeaderStatement*
SgProgramHeaderStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgProgramHeaderStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProgramHeaderStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProgramHeaderStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProgramHeaderStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgProgramHeaderStatement)};
        unsigned long positionInPool{localIndex % SgProgramHeaderStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProgramHeaderStatement::pool_size};
        returnPointer = &(((SgProgramHeaderStatement*)(SgProgramHeaderStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgProgramHeaderStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgProgramHeaderStatement::pools.begin(); block != SgProgramHeaderStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProgramHeaderStatement*>(*block)};
        for (unsigned i = 0; i < SgProgramHeaderStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgProgramHeaderStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgProgramHeaderStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgProgramHeaderStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgProgramHeaderStatement::pools.begin(); block != SgProgramHeaderStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProgramHeaderStatement*>(*block)};
        for (unsigned i = 0; i < SgProgramHeaderStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgProgramHeaderStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgProgramHeaderStatement::clearMemoryPool() {
    SgProgramHeaderStatement* tempPointer{nullptr};
    if (SgProgramHeaderStatement::pools.empty() == false) {
        auto block{SgProgramHeaderStatement::pools.begin()};
        SgProgramHeaderStatement::next_node = reinterpret_cast<SgProgramHeaderStatement*>(*block);
        for (; block != SgProgramHeaderStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgProgramHeaderStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgProgramHeaderStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgProgramHeaderStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgProgramHeaderStatement::pool_size-1]);
        }
    }
}

void
SgProgramHeaderStatement::deleteMemoryPool() {
    for (auto p: SgProgramHeaderStatement::pools) {
        ROSE_FREE(p);
    }
    SgProgramHeaderStatement::next_node = nullptr;
    SgProgramHeaderStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgProgramHeaderStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgProgramHeaderStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgProgramHeaderStatement)};
    for (size_t blockIndex = SgProgramHeaderStatement::pools.size(); (blockIndex * SgProgramHeaderStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgProgramHeaderStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgProgramHeaderStatement) = %" PRIuPTR " SgProgramHeaderStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgProgramHeaderStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgProgramHeaderStatement),SgProgramHeaderStatement::pool_size);
        }
#endif

        SgProgramHeaderStatement* pointer{static_cast<SgProgramHeaderStatement*>(ROSE_MALLOC(SgProgramHeaderStatement::pool_size * sizeof(SgProgramHeaderStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgProgramHeaderStatement::pool_size * sizeof(SgProgramHeaderStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgProgramHeaderStatement::pool_size * sizeof(SgProgramHeaderStatement));
#endif
        SgProgramHeaderStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgProgramHeaderStatement::pool_size * sizeof(SgProgramHeaderStatement), V_SgProgramHeaderStatement));

        if (SgProgramHeaderStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgProgramHeaderStatement* blkptr{reinterpret_cast<SgProgramHeaderStatement*>(SgProgramHeaderStatement::pools[blockIndex-1])};
                blkptr[SgProgramHeaderStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgProgramHeaderStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgProgramHeaderStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgProgramHeaderStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgProgramHeaderStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgProgramHeaderStatement::pool_size - 1};
    SgProgramHeaderStatement* testPointer{reinterpret_cast<SgProgramHeaderStatement*>(SgProgramHeaderStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgProgramHeaderStatement::pool_size * (SgProgramHeaderStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgProgramHeaderStatementStroageClass
 * from its counterpart of SgProgramHeaderStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgProgramHeaderStatement::initializeStorageClassArray(SgProgramHeaderStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgProgramHeaderStatement::pools.begin(); block != SgProgramHeaderStatement::pools.end(); ++block) {
        SgProgramHeaderStatement* pointer{reinterpret_cast<SgProgramHeaderStatement*>(*block)};
        for (unsigned i = 0; i < SgProgramHeaderStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 69665 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgProcedureHeaderStatement that takes its corresponding StorageClass as parameter
 */
SgProcedureHeaderStatement::SgProcedureHeaderStatement(const SgProcedureHeaderStatementStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 69675 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_subprogram_kind = storageSource.storageOf_subprogram_kind;
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));
     p_result_name = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_result_name));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgProcedureHeaderStatement*
SgProcedureHeaderStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgProcedureHeaderStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProcedureHeaderStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProcedureHeaderStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProcedureHeaderStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgProcedureHeaderStatement)};
        unsigned long positionInPool{localIndex % SgProcedureHeaderStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProcedureHeaderStatement::pool_size};
        returnPointer = &(((SgProcedureHeaderStatement*)(SgProcedureHeaderStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgProcedureHeaderStatement*
SgProcedureHeaderStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgProcedureHeaderStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProcedureHeaderStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProcedureHeaderStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProcedureHeaderStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgProcedureHeaderStatement)};
        unsigned long positionInPool{localIndex % SgProcedureHeaderStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProcedureHeaderStatement::pool_size};
        returnPointer = &(((SgProcedureHeaderStatement*)(SgProcedureHeaderStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgProcedureHeaderStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgProcedureHeaderStatement::pools.begin(); block != SgProcedureHeaderStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProcedureHeaderStatement*>(*block)};
        for (unsigned i = 0; i < SgProcedureHeaderStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgProcedureHeaderStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgProcedureHeaderStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgProcedureHeaderStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgProcedureHeaderStatement::pools.begin(); block != SgProcedureHeaderStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProcedureHeaderStatement*>(*block)};
        for (unsigned i = 0; i < SgProcedureHeaderStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgProcedureHeaderStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgProcedureHeaderStatement::clearMemoryPool() {
    SgProcedureHeaderStatement* tempPointer{nullptr};
    if (SgProcedureHeaderStatement::pools.empty() == false) {
        auto block{SgProcedureHeaderStatement::pools.begin()};
        SgProcedureHeaderStatement::next_node = reinterpret_cast<SgProcedureHeaderStatement*>(*block);
        for (; block != SgProcedureHeaderStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgProcedureHeaderStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgProcedureHeaderStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgProcedureHeaderStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgProcedureHeaderStatement::pool_size-1]);
        }
    }
}

void
SgProcedureHeaderStatement::deleteMemoryPool() {
    for (auto p: SgProcedureHeaderStatement::pools) {
        ROSE_FREE(p);
    }
    SgProcedureHeaderStatement::next_node = nullptr;
    SgProcedureHeaderStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgProcedureHeaderStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgProcedureHeaderStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgProcedureHeaderStatement)};
    for (size_t blockIndex = SgProcedureHeaderStatement::pools.size(); (blockIndex * SgProcedureHeaderStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgProcedureHeaderStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgProcedureHeaderStatement) = %" PRIuPTR " SgProcedureHeaderStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgProcedureHeaderStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgProcedureHeaderStatement),SgProcedureHeaderStatement::pool_size);
        }
#endif

        SgProcedureHeaderStatement* pointer{static_cast<SgProcedureHeaderStatement*>(ROSE_MALLOC(SgProcedureHeaderStatement::pool_size * sizeof(SgProcedureHeaderStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgProcedureHeaderStatement::pool_size * sizeof(SgProcedureHeaderStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgProcedureHeaderStatement::pool_size * sizeof(SgProcedureHeaderStatement));
#endif
        SgProcedureHeaderStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgProcedureHeaderStatement::pool_size * sizeof(SgProcedureHeaderStatement), V_SgProcedureHeaderStatement));

        if (SgProcedureHeaderStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgProcedureHeaderStatement* blkptr{reinterpret_cast<SgProcedureHeaderStatement*>(SgProcedureHeaderStatement::pools[blockIndex-1])};
                blkptr[SgProcedureHeaderStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgProcedureHeaderStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgProcedureHeaderStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgProcedureHeaderStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgProcedureHeaderStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgProcedureHeaderStatement::pool_size - 1};
    SgProcedureHeaderStatement* testPointer{reinterpret_cast<SgProcedureHeaderStatement*>(SgProcedureHeaderStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgProcedureHeaderStatement::pool_size * (SgProcedureHeaderStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgProcedureHeaderStatementStroageClass
 * from its counterpart of SgProcedureHeaderStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgProcedureHeaderStatement::initializeStorageClassArray(SgProcedureHeaderStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgProcedureHeaderStatement::pools.begin(); block != SgProcedureHeaderStatement::pools.end(); ++block) {
        SgProcedureHeaderStatement* pointer{reinterpret_cast<SgProcedureHeaderStatement*>(*block)};
        for (unsigned i = 0; i < SgProcedureHeaderStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 69895 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEntryStatement that takes its corresponding StorageClass as parameter
 */
SgEntryStatement::SgEntryStatement(const SgEntryStatementStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 69905 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_result_name = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_result_name));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEntryStatement*
SgEntryStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEntryStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEntryStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEntryStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEntryStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEntryStatement)};
        unsigned long positionInPool{localIndex % SgEntryStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEntryStatement::pool_size};
        returnPointer = &(((SgEntryStatement*)(SgEntryStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEntryStatement*
SgEntryStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEntryStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEntryStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEntryStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEntryStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEntryStatement)};
        unsigned long positionInPool{localIndex % SgEntryStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEntryStatement::pool_size};
        returnPointer = &(((SgEntryStatement*)(SgEntryStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEntryStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEntryStatement::pools.begin(); block != SgEntryStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEntryStatement*>(*block)};
        for (unsigned i = 0; i < SgEntryStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEntryStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEntryStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEntryStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgEntryStatement::pools.begin(); block != SgEntryStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEntryStatement*>(*block)};
        for (unsigned i = 0; i < SgEntryStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEntryStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEntryStatement::clearMemoryPool() {
    SgEntryStatement* tempPointer{nullptr};
    if (SgEntryStatement::pools.empty() == false) {
        auto block{SgEntryStatement::pools.begin()};
        SgEntryStatement::next_node = reinterpret_cast<SgEntryStatement*>(*block);
        for (; block != SgEntryStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEntryStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEntryStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEntryStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEntryStatement::pool_size-1]);
        }
    }
}

void
SgEntryStatement::deleteMemoryPool() {
    for (auto p: SgEntryStatement::pools) {
        ROSE_FREE(p);
    }
    SgEntryStatement::next_node = nullptr;
    SgEntryStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEntryStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEntryStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEntryStatement)};
    for (size_t blockIndex = SgEntryStatement::pools.size(); (blockIndex * SgEntryStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEntryStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEntryStatement) = %" PRIuPTR " SgEntryStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEntryStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEntryStatement),SgEntryStatement::pool_size);
        }
#endif

        SgEntryStatement* pointer{static_cast<SgEntryStatement*>(ROSE_MALLOC(SgEntryStatement::pool_size * sizeof(SgEntryStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEntryStatement::pool_size * sizeof(SgEntryStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEntryStatement::pool_size * sizeof(SgEntryStatement));
#endif
        SgEntryStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEntryStatement::pool_size * sizeof(SgEntryStatement), V_SgEntryStatement));

        if (SgEntryStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEntryStatement* blkptr{reinterpret_cast<SgEntryStatement*>(SgEntryStatement::pools[blockIndex-1])};
                blkptr[SgEntryStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEntryStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEntryStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEntryStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEntryStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEntryStatement::pool_size - 1};
    SgEntryStatement* testPointer{reinterpret_cast<SgEntryStatement*>(SgEntryStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEntryStatement::pool_size * (SgEntryStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEntryStatementStroageClass
 * from its counterpart of SgEntryStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEntryStatement::initializeStorageClassArray(SgEntryStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEntryStatement::pools.begin(); block != SgEntryStatement::pools.end(); ++block) {
        SgEntryStatement* pointer{reinterpret_cast<SgEntryStatement*>(*block)};
        for (unsigned i = 0; i < SgEntryStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 70123 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaEntryDecl that takes its corresponding StorageClass as parameter
 */
SgAdaEntryDecl::SgAdaEntryDecl(const SgAdaEntryDeclStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 70133 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_entryIndex = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entryIndex));
     p_entryBarrier = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entryBarrier));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaEntryDecl*
SgAdaEntryDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaEntryDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaEntryDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaEntryDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaEntryDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEntryDecl)};
        unsigned long positionInPool{localIndex % SgAdaEntryDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaEntryDecl::pool_size};
        returnPointer = &(((SgAdaEntryDecl*)(SgAdaEntryDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaEntryDecl*
SgAdaEntryDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaEntryDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaEntryDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaEntryDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaEntryDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaEntryDecl)};
        unsigned long positionInPool{localIndex % SgAdaEntryDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaEntryDecl::pool_size};
        returnPointer = &(((SgAdaEntryDecl*)(SgAdaEntryDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaEntryDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaEntryDecl::pools.begin(); block != SgAdaEntryDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaEntryDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaEntryDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaEntryDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaEntryDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaEntryDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaEntryDecl::pools.begin(); block != SgAdaEntryDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaEntryDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaEntryDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaEntryDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaEntryDecl::clearMemoryPool() {
    SgAdaEntryDecl* tempPointer{nullptr};
    if (SgAdaEntryDecl::pools.empty() == false) {
        auto block{SgAdaEntryDecl::pools.begin()};
        SgAdaEntryDecl::next_node = reinterpret_cast<SgAdaEntryDecl*>(*block);
        for (; block != SgAdaEntryDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaEntryDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaEntryDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaEntryDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaEntryDecl::pool_size-1]);
        }
    }
}

void
SgAdaEntryDecl::deleteMemoryPool() {
    for (auto p: SgAdaEntryDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaEntryDecl::next_node = nullptr;
    SgAdaEntryDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaEntryDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEntryDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaEntryDecl)};
    for (size_t blockIndex = SgAdaEntryDecl::pools.size(); (blockIndex * SgAdaEntryDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEntryDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaEntryDecl) = %" PRIuPTR " SgAdaEntryDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEntryDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaEntryDecl),SgAdaEntryDecl::pool_size);
        }
#endif

        SgAdaEntryDecl* pointer{static_cast<SgAdaEntryDecl*>(ROSE_MALLOC(SgAdaEntryDecl::pool_size * sizeof(SgAdaEntryDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaEntryDecl::pool_size * sizeof(SgAdaEntryDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaEntryDecl::pool_size * sizeof(SgAdaEntryDecl));
#endif
        SgAdaEntryDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaEntryDecl::pool_size * sizeof(SgAdaEntryDecl), V_SgAdaEntryDecl));

        if (SgAdaEntryDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaEntryDecl* blkptr{reinterpret_cast<SgAdaEntryDecl*>(SgAdaEntryDecl::pools[blockIndex-1])};
                blkptr[SgAdaEntryDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaEntryDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaEntryDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaEntryDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaEntryDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaEntryDecl::pool_size - 1};
    SgAdaEntryDecl* testPointer{reinterpret_cast<SgAdaEntryDecl*>(SgAdaEntryDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaEntryDecl::pool_size * (SgAdaEntryDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaEntryDeclStroageClass
 * from its counterpart of SgAdaEntryDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaEntryDecl::initializeStorageClassArray(SgAdaEntryDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaEntryDecl::pools.begin(); block != SgAdaEntryDecl::pools.end(); ++block) {
        SgAdaEntryDecl* pointer{reinterpret_cast<SgAdaEntryDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaEntryDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 70352 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaFunctionRenamingDecl that takes its corresponding StorageClass as parameter
 */
SgAdaFunctionRenamingDecl::SgAdaFunctionRenamingDecl(const SgAdaFunctionRenamingDeclStorageClass& storageSource)  : SgFunctionDeclaration (storageSource) {

/* #line 70362 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_renamed_function = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_renamed_function));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaFunctionRenamingDecl*
SgAdaFunctionRenamingDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaFunctionRenamingDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFunctionRenamingDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFunctionRenamingDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFunctionRenamingDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFunctionRenamingDecl)};
        unsigned long positionInPool{localIndex % SgAdaFunctionRenamingDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFunctionRenamingDecl::pool_size};
        returnPointer = &(((SgAdaFunctionRenamingDecl*)(SgAdaFunctionRenamingDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaFunctionRenamingDecl*
SgAdaFunctionRenamingDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaFunctionRenamingDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFunctionRenamingDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFunctionRenamingDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFunctionRenamingDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaFunctionRenamingDecl)};
        unsigned long positionInPool{localIndex % SgAdaFunctionRenamingDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFunctionRenamingDecl::pool_size};
        returnPointer = &(((SgAdaFunctionRenamingDecl*)(SgAdaFunctionRenamingDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaFunctionRenamingDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaFunctionRenamingDecl::pools.begin(); block != SgAdaFunctionRenamingDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFunctionRenamingDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFunctionRenamingDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaFunctionRenamingDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaFunctionRenamingDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaFunctionRenamingDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaFunctionRenamingDecl::pools.begin(); block != SgAdaFunctionRenamingDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFunctionRenamingDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFunctionRenamingDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaFunctionRenamingDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaFunctionRenamingDecl::clearMemoryPool() {
    SgAdaFunctionRenamingDecl* tempPointer{nullptr};
    if (SgAdaFunctionRenamingDecl::pools.empty() == false) {
        auto block{SgAdaFunctionRenamingDecl::pools.begin()};
        SgAdaFunctionRenamingDecl::next_node = reinterpret_cast<SgAdaFunctionRenamingDecl*>(*block);
        for (; block != SgAdaFunctionRenamingDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaFunctionRenamingDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaFunctionRenamingDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaFunctionRenamingDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaFunctionRenamingDecl::pool_size-1]);
        }
    }
}

void
SgAdaFunctionRenamingDecl::deleteMemoryPool() {
    for (auto p: SgAdaFunctionRenamingDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaFunctionRenamingDecl::next_node = nullptr;
    SgAdaFunctionRenamingDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaFunctionRenamingDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFunctionRenamingDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFunctionRenamingDecl)};
    for (size_t blockIndex = SgAdaFunctionRenamingDecl::pools.size(); (blockIndex * SgAdaFunctionRenamingDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFunctionRenamingDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFunctionRenamingDecl) = %" PRIuPTR " SgAdaFunctionRenamingDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFunctionRenamingDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFunctionRenamingDecl),SgAdaFunctionRenamingDecl::pool_size);
        }
#endif

        SgAdaFunctionRenamingDecl* pointer{static_cast<SgAdaFunctionRenamingDecl*>(ROSE_MALLOC(SgAdaFunctionRenamingDecl::pool_size * sizeof(SgAdaFunctionRenamingDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaFunctionRenamingDecl::pool_size * sizeof(SgAdaFunctionRenamingDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaFunctionRenamingDecl::pool_size * sizeof(SgAdaFunctionRenamingDecl));
#endif
        SgAdaFunctionRenamingDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaFunctionRenamingDecl::pool_size * sizeof(SgAdaFunctionRenamingDecl), V_SgAdaFunctionRenamingDecl));

        if (SgAdaFunctionRenamingDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaFunctionRenamingDecl* blkptr{reinterpret_cast<SgAdaFunctionRenamingDecl*>(SgAdaFunctionRenamingDecl::pools[blockIndex-1])};
                blkptr[SgAdaFunctionRenamingDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaFunctionRenamingDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaFunctionRenamingDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaFunctionRenamingDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaFunctionRenamingDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaFunctionRenamingDecl::pool_size - 1};
    SgAdaFunctionRenamingDecl* testPointer{reinterpret_cast<SgAdaFunctionRenamingDecl*>(SgAdaFunctionRenamingDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaFunctionRenamingDecl::pool_size * (SgAdaFunctionRenamingDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaFunctionRenamingDeclStroageClass
 * from its counterpart of SgAdaFunctionRenamingDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaFunctionRenamingDecl::initializeStorageClassArray(SgAdaFunctionRenamingDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaFunctionRenamingDecl::pools.begin(); block != SgAdaFunctionRenamingDecl::pools.end(); ++block) {
        SgAdaFunctionRenamingDecl* pointer{reinterpret_cast<SgAdaFunctionRenamingDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFunctionRenamingDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 70580 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgContainsStatement that takes its corresponding StorageClass as parameter
 */
SgContainsStatement::SgContainsStatement(const SgContainsStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 70590 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgContainsStatement*
SgContainsStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgContainsStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgContainsStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgContainsStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgContainsStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgContainsStatement)};
        unsigned long positionInPool{localIndex % SgContainsStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgContainsStatement::pool_size};
        returnPointer = &(((SgContainsStatement*)(SgContainsStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgContainsStatement*
SgContainsStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgContainsStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgContainsStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgContainsStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgContainsStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgContainsStatement)};
        unsigned long positionInPool{localIndex % SgContainsStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgContainsStatement::pool_size};
        returnPointer = &(((SgContainsStatement*)(SgContainsStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgContainsStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgContainsStatement::pools.begin(); block != SgContainsStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgContainsStatement*>(*block)};
        for (unsigned i = 0; i < SgContainsStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgContainsStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgContainsStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgContainsStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgContainsStatement::pools.begin(); block != SgContainsStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgContainsStatement*>(*block)};
        for (unsigned i = 0; i < SgContainsStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgContainsStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgContainsStatement::clearMemoryPool() {
    SgContainsStatement* tempPointer{nullptr};
    if (SgContainsStatement::pools.empty() == false) {
        auto block{SgContainsStatement::pools.begin()};
        SgContainsStatement::next_node = reinterpret_cast<SgContainsStatement*>(*block);
        for (; block != SgContainsStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgContainsStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgContainsStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgContainsStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgContainsStatement::pool_size-1]);
        }
    }
}

void
SgContainsStatement::deleteMemoryPool() {
    for (auto p: SgContainsStatement::pools) {
        ROSE_FREE(p);
    }
    SgContainsStatement::next_node = nullptr;
    SgContainsStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgContainsStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgContainsStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgContainsStatement)};
    for (size_t blockIndex = SgContainsStatement::pools.size(); (blockIndex * SgContainsStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgContainsStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgContainsStatement) = %" PRIuPTR " SgContainsStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgContainsStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgContainsStatement),SgContainsStatement::pool_size);
        }
#endif

        SgContainsStatement* pointer{static_cast<SgContainsStatement*>(ROSE_MALLOC(SgContainsStatement::pool_size * sizeof(SgContainsStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgContainsStatement::pool_size * sizeof(SgContainsStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgContainsStatement::pool_size * sizeof(SgContainsStatement));
#endif
        SgContainsStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgContainsStatement::pool_size * sizeof(SgContainsStatement), V_SgContainsStatement));

        if (SgContainsStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgContainsStatement* blkptr{reinterpret_cast<SgContainsStatement*>(SgContainsStatement::pools[blockIndex-1])};
                blkptr[SgContainsStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgContainsStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgContainsStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgContainsStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgContainsStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgContainsStatement::pool_size - 1};
    SgContainsStatement* testPointer{reinterpret_cast<SgContainsStatement*>(SgContainsStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgContainsStatement::pool_size * (SgContainsStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgContainsStatementStroageClass
 * from its counterpart of SgContainsStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgContainsStatement::initializeStorageClassArray(SgContainsStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgContainsStatement::pools.begin(); block != SgContainsStatement::pools.end(); ++block) {
        SgContainsStatement* pointer{reinterpret_cast<SgContainsStatement*>(*block)};
        for (unsigned i = 0; i < SgContainsStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 70807 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgC_PreprocessorDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgC_PreprocessorDirectiveStatement::SgC_PreprocessorDirectiveStatement(const SgC_PreprocessorDirectiveStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 70817 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_directiveString = storageSource.storageOf_directiveString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgC_PreprocessorDirectiveStatement*
SgC_PreprocessorDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgC_PreprocessorDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgC_PreprocessorDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgC_PreprocessorDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgC_PreprocessorDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgC_PreprocessorDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgC_PreprocessorDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgC_PreprocessorDirectiveStatement::pool_size};
        returnPointer = &(((SgC_PreprocessorDirectiveStatement*)(SgC_PreprocessorDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgC_PreprocessorDirectiveStatement*
SgC_PreprocessorDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgC_PreprocessorDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgC_PreprocessorDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgC_PreprocessorDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgC_PreprocessorDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgC_PreprocessorDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgC_PreprocessorDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgC_PreprocessorDirectiveStatement::pool_size};
        returnPointer = &(((SgC_PreprocessorDirectiveStatement*)(SgC_PreprocessorDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgC_PreprocessorDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgC_PreprocessorDirectiveStatement::pools.begin(); block != SgC_PreprocessorDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgC_PreprocessorDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgC_PreprocessorDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgC_PreprocessorDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgC_PreprocessorDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgC_PreprocessorDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgC_PreprocessorDirectiveStatement::pools.begin(); block != SgC_PreprocessorDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgC_PreprocessorDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgC_PreprocessorDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgC_PreprocessorDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgC_PreprocessorDirectiveStatement::clearMemoryPool() {
    SgC_PreprocessorDirectiveStatement* tempPointer{nullptr};
    if (SgC_PreprocessorDirectiveStatement::pools.empty() == false) {
        auto block{SgC_PreprocessorDirectiveStatement::pools.begin()};
        SgC_PreprocessorDirectiveStatement::next_node = reinterpret_cast<SgC_PreprocessorDirectiveStatement*>(*block);
        for (; block != SgC_PreprocessorDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgC_PreprocessorDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgC_PreprocessorDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgC_PreprocessorDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgC_PreprocessorDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgC_PreprocessorDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgC_PreprocessorDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgC_PreprocessorDirectiveStatement::next_node = nullptr;
    SgC_PreprocessorDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgC_PreprocessorDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgC_PreprocessorDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgC_PreprocessorDirectiveStatement)};
    for (size_t blockIndex = SgC_PreprocessorDirectiveStatement::pools.size(); (blockIndex * SgC_PreprocessorDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgC_PreprocessorDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgC_PreprocessorDirectiveStatement) = %" PRIuPTR " SgC_PreprocessorDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgC_PreprocessorDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgC_PreprocessorDirectiveStatement),SgC_PreprocessorDirectiveStatement::pool_size);
        }
#endif

        SgC_PreprocessorDirectiveStatement* pointer{static_cast<SgC_PreprocessorDirectiveStatement*>(ROSE_MALLOC(SgC_PreprocessorDirectiveStatement::pool_size * sizeof(SgC_PreprocessorDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgC_PreprocessorDirectiveStatement::pool_size * sizeof(SgC_PreprocessorDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgC_PreprocessorDirectiveStatement::pool_size * sizeof(SgC_PreprocessorDirectiveStatement));
#endif
        SgC_PreprocessorDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgC_PreprocessorDirectiveStatement::pool_size * sizeof(SgC_PreprocessorDirectiveStatement), V_SgC_PreprocessorDirectiveStatement));

        if (SgC_PreprocessorDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgC_PreprocessorDirectiveStatement* blkptr{reinterpret_cast<SgC_PreprocessorDirectiveStatement*>(SgC_PreprocessorDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgC_PreprocessorDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgC_PreprocessorDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgC_PreprocessorDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgC_PreprocessorDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgC_PreprocessorDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgC_PreprocessorDirectiveStatement::pool_size - 1};
    SgC_PreprocessorDirectiveStatement* testPointer{reinterpret_cast<SgC_PreprocessorDirectiveStatement*>(SgC_PreprocessorDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgC_PreprocessorDirectiveStatement::pool_size * (SgC_PreprocessorDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgC_PreprocessorDirectiveStatementStroageClass
 * from its counterpart of SgC_PreprocessorDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgC_PreprocessorDirectiveStatement::initializeStorageClassArray(SgC_PreprocessorDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgC_PreprocessorDirectiveStatement::pools.begin(); block != SgC_PreprocessorDirectiveStatement::pools.end(); ++block) {
        SgC_PreprocessorDirectiveStatement* pointer{reinterpret_cast<SgC_PreprocessorDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgC_PreprocessorDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 71035 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIncludeDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgIncludeDirectiveStatement::SgIncludeDirectiveStatement(const SgIncludeDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 71045 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_headerFileBody = (SgHeaderFileBody*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_headerFileBody));
     p_include_file_heirarchy = (SgIncludeFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_include_file_heirarchy));
    p_name_used_in_include_directive = storageSource.storageOf_name_used_in_include_directive.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIncludeDirectiveStatement*
SgIncludeDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIncludeDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIncludeDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncludeDirectiveStatement::pool_size};
        returnPointer = &(((SgIncludeDirectiveStatement*)(SgIncludeDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIncludeDirectiveStatement*
SgIncludeDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIncludeDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIncludeDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIncludeDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncludeDirectiveStatement::pool_size};
        returnPointer = &(((SgIncludeDirectiveStatement*)(SgIncludeDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIncludeDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIncludeDirectiveStatement::pools.begin(); block != SgIncludeDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncludeDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIncludeDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIncludeDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIncludeDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIncludeDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgIncludeDirectiveStatement::pools.begin(); block != SgIncludeDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncludeDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIncludeDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIncludeDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIncludeDirectiveStatement::clearMemoryPool() {
    SgIncludeDirectiveStatement* tempPointer{nullptr};
    if (SgIncludeDirectiveStatement::pools.empty() == false) {
        auto block{SgIncludeDirectiveStatement::pools.begin()};
        SgIncludeDirectiveStatement::next_node = reinterpret_cast<SgIncludeDirectiveStatement*>(*block);
        for (; block != SgIncludeDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIncludeDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIncludeDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIncludeDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIncludeDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgIncludeDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgIncludeDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgIncludeDirectiveStatement::next_node = nullptr;
    SgIncludeDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIncludeDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeDirectiveStatement)};
    for (size_t blockIndex = SgIncludeDirectiveStatement::pools.size(); (blockIndex * SgIncludeDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeDirectiveStatement) = %" PRIuPTR " SgIncludeDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeDirectiveStatement),SgIncludeDirectiveStatement::pool_size);
        }
#endif

        SgIncludeDirectiveStatement* pointer{static_cast<SgIncludeDirectiveStatement*>(ROSE_MALLOC(SgIncludeDirectiveStatement::pool_size * sizeof(SgIncludeDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIncludeDirectiveStatement::pool_size * sizeof(SgIncludeDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIncludeDirectiveStatement::pool_size * sizeof(SgIncludeDirectiveStatement));
#endif
        SgIncludeDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIncludeDirectiveStatement::pool_size * sizeof(SgIncludeDirectiveStatement), V_SgIncludeDirectiveStatement));

        if (SgIncludeDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIncludeDirectiveStatement* blkptr{reinterpret_cast<SgIncludeDirectiveStatement*>(SgIncludeDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgIncludeDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIncludeDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIncludeDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIncludeDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIncludeDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIncludeDirectiveStatement::pool_size - 1};
    SgIncludeDirectiveStatement* testPointer{reinterpret_cast<SgIncludeDirectiveStatement*>(SgIncludeDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIncludeDirectiveStatement::pool_size * (SgIncludeDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIncludeDirectiveStatementStroageClass
 * from its counterpart of SgIncludeDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIncludeDirectiveStatement::initializeStorageClassArray(SgIncludeDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIncludeDirectiveStatement::pools.begin(); block != SgIncludeDirectiveStatement::pools.end(); ++block) {
        SgIncludeDirectiveStatement* pointer{reinterpret_cast<SgIncludeDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIncludeDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 71265 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDefineDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgDefineDirectiveStatement::SgDefineDirectiveStatement(const SgDefineDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 71275 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDefineDirectiveStatement*
SgDefineDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDefineDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefineDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefineDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefineDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDefineDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgDefineDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDefineDirectiveStatement::pool_size};
        returnPointer = &(((SgDefineDirectiveStatement*)(SgDefineDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDefineDirectiveStatement*
SgDefineDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDefineDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefineDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefineDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefineDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDefineDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgDefineDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDefineDirectiveStatement::pool_size};
        returnPointer = &(((SgDefineDirectiveStatement*)(SgDefineDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDefineDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDefineDirectiveStatement::pools.begin(); block != SgDefineDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDefineDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgDefineDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDefineDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDefineDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDefineDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgDefineDirectiveStatement::pools.begin(); block != SgDefineDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDefineDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgDefineDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDefineDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDefineDirectiveStatement::clearMemoryPool() {
    SgDefineDirectiveStatement* tempPointer{nullptr};
    if (SgDefineDirectiveStatement::pools.empty() == false) {
        auto block{SgDefineDirectiveStatement::pools.begin()};
        SgDefineDirectiveStatement::next_node = reinterpret_cast<SgDefineDirectiveStatement*>(*block);
        for (; block != SgDefineDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDefineDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDefineDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDefineDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDefineDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgDefineDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgDefineDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgDefineDirectiveStatement::next_node = nullptr;
    SgDefineDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDefineDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDefineDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefineDirectiveStatement)};
    for (size_t blockIndex = SgDefineDirectiveStatement::pools.size(); (blockIndex * SgDefineDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDefineDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefineDirectiveStatement) = %" PRIuPTR " SgDefineDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDefineDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefineDirectiveStatement),SgDefineDirectiveStatement::pool_size);
        }
#endif

        SgDefineDirectiveStatement* pointer{static_cast<SgDefineDirectiveStatement*>(ROSE_MALLOC(SgDefineDirectiveStatement::pool_size * sizeof(SgDefineDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDefineDirectiveStatement::pool_size * sizeof(SgDefineDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDefineDirectiveStatement::pool_size * sizeof(SgDefineDirectiveStatement));
#endif
        SgDefineDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDefineDirectiveStatement::pool_size * sizeof(SgDefineDirectiveStatement), V_SgDefineDirectiveStatement));

        if (SgDefineDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDefineDirectiveStatement* blkptr{reinterpret_cast<SgDefineDirectiveStatement*>(SgDefineDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgDefineDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDefineDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDefineDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDefineDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDefineDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDefineDirectiveStatement::pool_size - 1};
    SgDefineDirectiveStatement* testPointer{reinterpret_cast<SgDefineDirectiveStatement*>(SgDefineDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDefineDirectiveStatement::pool_size * (SgDefineDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDefineDirectiveStatementStroageClass
 * from its counterpart of SgDefineDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDefineDirectiveStatement::initializeStorageClassArray(SgDefineDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDefineDirectiveStatement::pools.begin(); block != SgDefineDirectiveStatement::pools.end(); ++block) {
        SgDefineDirectiveStatement* pointer{reinterpret_cast<SgDefineDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgDefineDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 71492 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUndefDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgUndefDirectiveStatement::SgUndefDirectiveStatement(const SgUndefDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 71502 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUndefDirectiveStatement*
SgUndefDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUndefDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUndefDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUndefDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUndefDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUndefDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgUndefDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUndefDirectiveStatement::pool_size};
        returnPointer = &(((SgUndefDirectiveStatement*)(SgUndefDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUndefDirectiveStatement*
SgUndefDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUndefDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUndefDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUndefDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUndefDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUndefDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgUndefDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUndefDirectiveStatement::pool_size};
        returnPointer = &(((SgUndefDirectiveStatement*)(SgUndefDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUndefDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUndefDirectiveStatement::pools.begin(); block != SgUndefDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUndefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgUndefDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUndefDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUndefDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUndefDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUndefDirectiveStatement::pools.begin(); block != SgUndefDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUndefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgUndefDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUndefDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUndefDirectiveStatement::clearMemoryPool() {
    SgUndefDirectiveStatement* tempPointer{nullptr};
    if (SgUndefDirectiveStatement::pools.empty() == false) {
        auto block{SgUndefDirectiveStatement::pools.begin()};
        SgUndefDirectiveStatement::next_node = reinterpret_cast<SgUndefDirectiveStatement*>(*block);
        for (; block != SgUndefDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUndefDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUndefDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUndefDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUndefDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgUndefDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgUndefDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgUndefDirectiveStatement::next_node = nullptr;
    SgUndefDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUndefDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUndefDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUndefDirectiveStatement)};
    for (size_t blockIndex = SgUndefDirectiveStatement::pools.size(); (blockIndex * SgUndefDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUndefDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUndefDirectiveStatement) = %" PRIuPTR " SgUndefDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUndefDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUndefDirectiveStatement),SgUndefDirectiveStatement::pool_size);
        }
#endif

        SgUndefDirectiveStatement* pointer{static_cast<SgUndefDirectiveStatement*>(ROSE_MALLOC(SgUndefDirectiveStatement::pool_size * sizeof(SgUndefDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUndefDirectiveStatement::pool_size * sizeof(SgUndefDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUndefDirectiveStatement::pool_size * sizeof(SgUndefDirectiveStatement));
#endif
        SgUndefDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUndefDirectiveStatement::pool_size * sizeof(SgUndefDirectiveStatement), V_SgUndefDirectiveStatement));

        if (SgUndefDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUndefDirectiveStatement* blkptr{reinterpret_cast<SgUndefDirectiveStatement*>(SgUndefDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgUndefDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUndefDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUndefDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUndefDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUndefDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUndefDirectiveStatement::pool_size - 1};
    SgUndefDirectiveStatement* testPointer{reinterpret_cast<SgUndefDirectiveStatement*>(SgUndefDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUndefDirectiveStatement::pool_size * (SgUndefDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUndefDirectiveStatementStroageClass
 * from its counterpart of SgUndefDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUndefDirectiveStatement::initializeStorageClassArray(SgUndefDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUndefDirectiveStatement::pools.begin(); block != SgUndefDirectiveStatement::pools.end(); ++block) {
        SgUndefDirectiveStatement* pointer{reinterpret_cast<SgUndefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgUndefDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 71719 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIfdefDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgIfdefDirectiveStatement::SgIfdefDirectiveStatement(const SgIfdefDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 71729 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIfdefDirectiveStatement*
SgIfdefDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIfdefDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfdefDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfdefDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfdefDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIfdefDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIfdefDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfdefDirectiveStatement::pool_size};
        returnPointer = &(((SgIfdefDirectiveStatement*)(SgIfdefDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIfdefDirectiveStatement*
SgIfdefDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIfdefDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfdefDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfdefDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfdefDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIfdefDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIfdefDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfdefDirectiveStatement::pool_size};
        returnPointer = &(((SgIfdefDirectiveStatement*)(SgIfdefDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIfdefDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIfdefDirectiveStatement::pools.begin(); block != SgIfdefDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfdefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfdefDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIfdefDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIfdefDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIfdefDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgIfdefDirectiveStatement::pools.begin(); block != SgIfdefDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfdefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfdefDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIfdefDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIfdefDirectiveStatement::clearMemoryPool() {
    SgIfdefDirectiveStatement* tempPointer{nullptr};
    if (SgIfdefDirectiveStatement::pools.empty() == false) {
        auto block{SgIfdefDirectiveStatement::pools.begin()};
        SgIfdefDirectiveStatement::next_node = reinterpret_cast<SgIfdefDirectiveStatement*>(*block);
        for (; block != SgIfdefDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIfdefDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIfdefDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIfdefDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIfdefDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgIfdefDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgIfdefDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgIfdefDirectiveStatement::next_node = nullptr;
    SgIfdefDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIfdefDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIfdefDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfdefDirectiveStatement)};
    for (size_t blockIndex = SgIfdefDirectiveStatement::pools.size(); (blockIndex * SgIfdefDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIfdefDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfdefDirectiveStatement) = %" PRIuPTR " SgIfdefDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIfdefDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfdefDirectiveStatement),SgIfdefDirectiveStatement::pool_size);
        }
#endif

        SgIfdefDirectiveStatement* pointer{static_cast<SgIfdefDirectiveStatement*>(ROSE_MALLOC(SgIfdefDirectiveStatement::pool_size * sizeof(SgIfdefDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIfdefDirectiveStatement::pool_size * sizeof(SgIfdefDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIfdefDirectiveStatement::pool_size * sizeof(SgIfdefDirectiveStatement));
#endif
        SgIfdefDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIfdefDirectiveStatement::pool_size * sizeof(SgIfdefDirectiveStatement), V_SgIfdefDirectiveStatement));

        if (SgIfdefDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIfdefDirectiveStatement* blkptr{reinterpret_cast<SgIfdefDirectiveStatement*>(SgIfdefDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgIfdefDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIfdefDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIfdefDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIfdefDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIfdefDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIfdefDirectiveStatement::pool_size - 1};
    SgIfdefDirectiveStatement* testPointer{reinterpret_cast<SgIfdefDirectiveStatement*>(SgIfdefDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIfdefDirectiveStatement::pool_size * (SgIfdefDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIfdefDirectiveStatementStroageClass
 * from its counterpart of SgIfdefDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIfdefDirectiveStatement::initializeStorageClassArray(SgIfdefDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIfdefDirectiveStatement::pools.begin(); block != SgIfdefDirectiveStatement::pools.end(); ++block) {
        SgIfdefDirectiveStatement* pointer{reinterpret_cast<SgIfdefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfdefDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 71946 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIfndefDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgIfndefDirectiveStatement::SgIfndefDirectiveStatement(const SgIfndefDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 71956 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIfndefDirectiveStatement*
SgIfndefDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIfndefDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfndefDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfndefDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfndefDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIfndefDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIfndefDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfndefDirectiveStatement::pool_size};
        returnPointer = &(((SgIfndefDirectiveStatement*)(SgIfndefDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIfndefDirectiveStatement*
SgIfndefDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIfndefDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfndefDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfndefDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfndefDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIfndefDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIfndefDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfndefDirectiveStatement::pool_size};
        returnPointer = &(((SgIfndefDirectiveStatement*)(SgIfndefDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIfndefDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIfndefDirectiveStatement::pools.begin(); block != SgIfndefDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfndefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfndefDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIfndefDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIfndefDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIfndefDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgIfndefDirectiveStatement::pools.begin(); block != SgIfndefDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfndefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfndefDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIfndefDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIfndefDirectiveStatement::clearMemoryPool() {
    SgIfndefDirectiveStatement* tempPointer{nullptr};
    if (SgIfndefDirectiveStatement::pools.empty() == false) {
        auto block{SgIfndefDirectiveStatement::pools.begin()};
        SgIfndefDirectiveStatement::next_node = reinterpret_cast<SgIfndefDirectiveStatement*>(*block);
        for (; block != SgIfndefDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIfndefDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIfndefDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIfndefDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIfndefDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgIfndefDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgIfndefDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgIfndefDirectiveStatement::next_node = nullptr;
    SgIfndefDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIfndefDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIfndefDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfndefDirectiveStatement)};
    for (size_t blockIndex = SgIfndefDirectiveStatement::pools.size(); (blockIndex * SgIfndefDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIfndefDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfndefDirectiveStatement) = %" PRIuPTR " SgIfndefDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIfndefDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfndefDirectiveStatement),SgIfndefDirectiveStatement::pool_size);
        }
#endif

        SgIfndefDirectiveStatement* pointer{static_cast<SgIfndefDirectiveStatement*>(ROSE_MALLOC(SgIfndefDirectiveStatement::pool_size * sizeof(SgIfndefDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIfndefDirectiveStatement::pool_size * sizeof(SgIfndefDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIfndefDirectiveStatement::pool_size * sizeof(SgIfndefDirectiveStatement));
#endif
        SgIfndefDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIfndefDirectiveStatement::pool_size * sizeof(SgIfndefDirectiveStatement), V_SgIfndefDirectiveStatement));

        if (SgIfndefDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIfndefDirectiveStatement* blkptr{reinterpret_cast<SgIfndefDirectiveStatement*>(SgIfndefDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgIfndefDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIfndefDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIfndefDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIfndefDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIfndefDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIfndefDirectiveStatement::pool_size - 1};
    SgIfndefDirectiveStatement* testPointer{reinterpret_cast<SgIfndefDirectiveStatement*>(SgIfndefDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIfndefDirectiveStatement::pool_size * (SgIfndefDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIfndefDirectiveStatementStroageClass
 * from its counterpart of SgIfndefDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIfndefDirectiveStatement::initializeStorageClassArray(SgIfndefDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIfndefDirectiveStatement::pools.begin(); block != SgIfndefDirectiveStatement::pools.end(); ++block) {
        SgIfndefDirectiveStatement* pointer{reinterpret_cast<SgIfndefDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfndefDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 72173 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIfDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgIfDirectiveStatement::SgIfDirectiveStatement(const SgIfDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 72183 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIfDirectiveStatement*
SgIfDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIfDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIfDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIfDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIfDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfDirectiveStatement::pool_size};
        returnPointer = &(((SgIfDirectiveStatement*)(SgIfDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIfDirectiveStatement*
SgIfDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIfDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIfDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIfDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIfDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIfDirectiveStatement::pool_size};
        returnPointer = &(((SgIfDirectiveStatement*)(SgIfDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIfDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIfDirectiveStatement::pools.begin(); block != SgIfDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIfDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIfDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIfDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgIfDirectiveStatement::pools.begin(); block != SgIfDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIfDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIfDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIfDirectiveStatement::clearMemoryPool() {
    SgIfDirectiveStatement* tempPointer{nullptr};
    if (SgIfDirectiveStatement::pools.empty() == false) {
        auto block{SgIfDirectiveStatement::pools.begin()};
        SgIfDirectiveStatement::next_node = reinterpret_cast<SgIfDirectiveStatement*>(*block);
        for (; block != SgIfDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIfDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIfDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIfDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIfDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgIfDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgIfDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgIfDirectiveStatement::next_node = nullptr;
    SgIfDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIfDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIfDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfDirectiveStatement)};
    for (size_t blockIndex = SgIfDirectiveStatement::pools.size(); (blockIndex * SgIfDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIfDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfDirectiveStatement) = %" PRIuPTR " SgIfDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIfDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIfDirectiveStatement),SgIfDirectiveStatement::pool_size);
        }
#endif

        SgIfDirectiveStatement* pointer{static_cast<SgIfDirectiveStatement*>(ROSE_MALLOC(SgIfDirectiveStatement::pool_size * sizeof(SgIfDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIfDirectiveStatement::pool_size * sizeof(SgIfDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIfDirectiveStatement::pool_size * sizeof(SgIfDirectiveStatement));
#endif
        SgIfDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIfDirectiveStatement::pool_size * sizeof(SgIfDirectiveStatement), V_SgIfDirectiveStatement));

        if (SgIfDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIfDirectiveStatement* blkptr{reinterpret_cast<SgIfDirectiveStatement*>(SgIfDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgIfDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIfDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIfDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIfDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIfDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIfDirectiveStatement::pool_size - 1};
    SgIfDirectiveStatement* testPointer{reinterpret_cast<SgIfDirectiveStatement*>(SgIfDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIfDirectiveStatement::pool_size * (SgIfDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIfDirectiveStatementStroageClass
 * from its counterpart of SgIfDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIfDirectiveStatement::initializeStorageClassArray(SgIfDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIfDirectiveStatement::pools.begin(); block != SgIfDirectiveStatement::pools.end(); ++block) {
        SgIfDirectiveStatement* pointer{reinterpret_cast<SgIfDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIfDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 72400 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDeadIfDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgDeadIfDirectiveStatement::SgDeadIfDirectiveStatement(const SgDeadIfDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 72410 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDeadIfDirectiveStatement*
SgDeadIfDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDeadIfDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeadIfDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeadIfDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeadIfDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDeadIfDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgDeadIfDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeadIfDirectiveStatement::pool_size};
        returnPointer = &(((SgDeadIfDirectiveStatement*)(SgDeadIfDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDeadIfDirectiveStatement*
SgDeadIfDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDeadIfDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeadIfDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeadIfDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeadIfDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDeadIfDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgDeadIfDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeadIfDirectiveStatement::pool_size};
        returnPointer = &(((SgDeadIfDirectiveStatement*)(SgDeadIfDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDeadIfDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDeadIfDirectiveStatement::pools.begin(); block != SgDeadIfDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeadIfDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgDeadIfDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDeadIfDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDeadIfDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDeadIfDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgDeadIfDirectiveStatement::pools.begin(); block != SgDeadIfDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeadIfDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgDeadIfDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDeadIfDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDeadIfDirectiveStatement::clearMemoryPool() {
    SgDeadIfDirectiveStatement* tempPointer{nullptr};
    if (SgDeadIfDirectiveStatement::pools.empty() == false) {
        auto block{SgDeadIfDirectiveStatement::pools.begin()};
        SgDeadIfDirectiveStatement::next_node = reinterpret_cast<SgDeadIfDirectiveStatement*>(*block);
        for (; block != SgDeadIfDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDeadIfDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDeadIfDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDeadIfDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDeadIfDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgDeadIfDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgDeadIfDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgDeadIfDirectiveStatement::next_node = nullptr;
    SgDeadIfDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDeadIfDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDeadIfDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeadIfDirectiveStatement)};
    for (size_t blockIndex = SgDeadIfDirectiveStatement::pools.size(); (blockIndex * SgDeadIfDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDeadIfDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeadIfDirectiveStatement) = %" PRIuPTR " SgDeadIfDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDeadIfDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeadIfDirectiveStatement),SgDeadIfDirectiveStatement::pool_size);
        }
#endif

        SgDeadIfDirectiveStatement* pointer{static_cast<SgDeadIfDirectiveStatement*>(ROSE_MALLOC(SgDeadIfDirectiveStatement::pool_size * sizeof(SgDeadIfDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDeadIfDirectiveStatement::pool_size * sizeof(SgDeadIfDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDeadIfDirectiveStatement::pool_size * sizeof(SgDeadIfDirectiveStatement));
#endif
        SgDeadIfDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDeadIfDirectiveStatement::pool_size * sizeof(SgDeadIfDirectiveStatement), V_SgDeadIfDirectiveStatement));

        if (SgDeadIfDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDeadIfDirectiveStatement* blkptr{reinterpret_cast<SgDeadIfDirectiveStatement*>(SgDeadIfDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgDeadIfDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDeadIfDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDeadIfDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDeadIfDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDeadIfDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDeadIfDirectiveStatement::pool_size - 1};
    SgDeadIfDirectiveStatement* testPointer{reinterpret_cast<SgDeadIfDirectiveStatement*>(SgDeadIfDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDeadIfDirectiveStatement::pool_size * (SgDeadIfDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDeadIfDirectiveStatementStroageClass
 * from its counterpart of SgDeadIfDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDeadIfDirectiveStatement::initializeStorageClassArray(SgDeadIfDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDeadIfDirectiveStatement::pools.begin(); block != SgDeadIfDirectiveStatement::pools.end(); ++block) {
        SgDeadIfDirectiveStatement* pointer{reinterpret_cast<SgDeadIfDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgDeadIfDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 72627 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElseDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgElseDirectiveStatement::SgElseDirectiveStatement(const SgElseDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 72637 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElseDirectiveStatement*
SgElseDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElseDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElseDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgElseDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElseDirectiveStatement::pool_size};
        returnPointer = &(((SgElseDirectiveStatement*)(SgElseDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElseDirectiveStatement*
SgElseDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElseDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElseDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgElseDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElseDirectiveStatement::pool_size};
        returnPointer = &(((SgElseDirectiveStatement*)(SgElseDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElseDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElseDirectiveStatement::pools.begin(); block != SgElseDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElseDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgElseDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElseDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElseDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElseDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgElseDirectiveStatement::pools.begin(); block != SgElseDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElseDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgElseDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElseDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElseDirectiveStatement::clearMemoryPool() {
    SgElseDirectiveStatement* tempPointer{nullptr};
    if (SgElseDirectiveStatement::pools.empty() == false) {
        auto block{SgElseDirectiveStatement::pools.begin()};
        SgElseDirectiveStatement::next_node = reinterpret_cast<SgElseDirectiveStatement*>(*block);
        for (; block != SgElseDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElseDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElseDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElseDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElseDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgElseDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgElseDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgElseDirectiveStatement::next_node = nullptr;
    SgElseDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElseDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElseDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseDirectiveStatement)};
    for (size_t blockIndex = SgElseDirectiveStatement::pools.size(); (blockIndex * SgElseDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElseDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseDirectiveStatement) = %" PRIuPTR " SgElseDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElseDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseDirectiveStatement),SgElseDirectiveStatement::pool_size);
        }
#endif

        SgElseDirectiveStatement* pointer{static_cast<SgElseDirectiveStatement*>(ROSE_MALLOC(SgElseDirectiveStatement::pool_size * sizeof(SgElseDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElseDirectiveStatement::pool_size * sizeof(SgElseDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElseDirectiveStatement::pool_size * sizeof(SgElseDirectiveStatement));
#endif
        SgElseDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElseDirectiveStatement::pool_size * sizeof(SgElseDirectiveStatement), V_SgElseDirectiveStatement));

        if (SgElseDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElseDirectiveStatement* blkptr{reinterpret_cast<SgElseDirectiveStatement*>(SgElseDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgElseDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElseDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElseDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElseDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElseDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElseDirectiveStatement::pool_size - 1};
    SgElseDirectiveStatement* testPointer{reinterpret_cast<SgElseDirectiveStatement*>(SgElseDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElseDirectiveStatement::pool_size * (SgElseDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElseDirectiveStatementStroageClass
 * from its counterpart of SgElseDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElseDirectiveStatement::initializeStorageClassArray(SgElseDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElseDirectiveStatement::pools.begin(); block != SgElseDirectiveStatement::pools.end(); ++block) {
        SgElseDirectiveStatement* pointer{reinterpret_cast<SgElseDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgElseDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 72854 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElseifDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgElseifDirectiveStatement::SgElseifDirectiveStatement(const SgElseifDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 72864 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElseifDirectiveStatement*
SgElseifDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElseifDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseifDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseifDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseifDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElseifDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgElseifDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElseifDirectiveStatement::pool_size};
        returnPointer = &(((SgElseifDirectiveStatement*)(SgElseifDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElseifDirectiveStatement*
SgElseifDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElseifDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseifDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseifDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseifDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElseifDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgElseifDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElseifDirectiveStatement::pool_size};
        returnPointer = &(((SgElseifDirectiveStatement*)(SgElseifDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElseifDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElseifDirectiveStatement::pools.begin(); block != SgElseifDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElseifDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgElseifDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElseifDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElseifDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElseifDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgElseifDirectiveStatement::pools.begin(); block != SgElseifDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElseifDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgElseifDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElseifDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElseifDirectiveStatement::clearMemoryPool() {
    SgElseifDirectiveStatement* tempPointer{nullptr};
    if (SgElseifDirectiveStatement::pools.empty() == false) {
        auto block{SgElseifDirectiveStatement::pools.begin()};
        SgElseifDirectiveStatement::next_node = reinterpret_cast<SgElseifDirectiveStatement*>(*block);
        for (; block != SgElseifDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElseifDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElseifDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElseifDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElseifDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgElseifDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgElseifDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgElseifDirectiveStatement::next_node = nullptr;
    SgElseifDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElseifDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElseifDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseifDirectiveStatement)};
    for (size_t blockIndex = SgElseifDirectiveStatement::pools.size(); (blockIndex * SgElseifDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElseifDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseifDirectiveStatement) = %" PRIuPTR " SgElseifDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElseifDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseifDirectiveStatement),SgElseifDirectiveStatement::pool_size);
        }
#endif

        SgElseifDirectiveStatement* pointer{static_cast<SgElseifDirectiveStatement*>(ROSE_MALLOC(SgElseifDirectiveStatement::pool_size * sizeof(SgElseifDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElseifDirectiveStatement::pool_size * sizeof(SgElseifDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElseifDirectiveStatement::pool_size * sizeof(SgElseifDirectiveStatement));
#endif
        SgElseifDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElseifDirectiveStatement::pool_size * sizeof(SgElseifDirectiveStatement), V_SgElseifDirectiveStatement));

        if (SgElseifDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElseifDirectiveStatement* blkptr{reinterpret_cast<SgElseifDirectiveStatement*>(SgElseifDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgElseifDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElseifDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElseifDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElseifDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElseifDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElseifDirectiveStatement::pool_size - 1};
    SgElseifDirectiveStatement* testPointer{reinterpret_cast<SgElseifDirectiveStatement*>(SgElseifDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElseifDirectiveStatement::pool_size * (SgElseifDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElseifDirectiveStatementStroageClass
 * from its counterpart of SgElseifDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElseifDirectiveStatement::initializeStorageClassArray(SgElseifDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElseifDirectiveStatement::pools.begin(); block != SgElseifDirectiveStatement::pools.end(); ++block) {
        SgElseifDirectiveStatement* pointer{reinterpret_cast<SgElseifDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgElseifDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 73081 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEndifDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgEndifDirectiveStatement::SgEndifDirectiveStatement(const SgEndifDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 73091 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEndifDirectiveStatement*
SgEndifDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEndifDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEndifDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEndifDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEndifDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEndifDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgEndifDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEndifDirectiveStatement::pool_size};
        returnPointer = &(((SgEndifDirectiveStatement*)(SgEndifDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEndifDirectiveStatement*
SgEndifDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEndifDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEndifDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEndifDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEndifDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEndifDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgEndifDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEndifDirectiveStatement::pool_size};
        returnPointer = &(((SgEndifDirectiveStatement*)(SgEndifDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEndifDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEndifDirectiveStatement::pools.begin(); block != SgEndifDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEndifDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgEndifDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEndifDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEndifDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEndifDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgEndifDirectiveStatement::pools.begin(); block != SgEndifDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEndifDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgEndifDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEndifDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEndifDirectiveStatement::clearMemoryPool() {
    SgEndifDirectiveStatement* tempPointer{nullptr};
    if (SgEndifDirectiveStatement::pools.empty() == false) {
        auto block{SgEndifDirectiveStatement::pools.begin()};
        SgEndifDirectiveStatement::next_node = reinterpret_cast<SgEndifDirectiveStatement*>(*block);
        for (; block != SgEndifDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEndifDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEndifDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEndifDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEndifDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgEndifDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgEndifDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgEndifDirectiveStatement::next_node = nullptr;
    SgEndifDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEndifDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEndifDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEndifDirectiveStatement)};
    for (size_t blockIndex = SgEndifDirectiveStatement::pools.size(); (blockIndex * SgEndifDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEndifDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEndifDirectiveStatement) = %" PRIuPTR " SgEndifDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEndifDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEndifDirectiveStatement),SgEndifDirectiveStatement::pool_size);
        }
#endif

        SgEndifDirectiveStatement* pointer{static_cast<SgEndifDirectiveStatement*>(ROSE_MALLOC(SgEndifDirectiveStatement::pool_size * sizeof(SgEndifDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEndifDirectiveStatement::pool_size * sizeof(SgEndifDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEndifDirectiveStatement::pool_size * sizeof(SgEndifDirectiveStatement));
#endif
        SgEndifDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEndifDirectiveStatement::pool_size * sizeof(SgEndifDirectiveStatement), V_SgEndifDirectiveStatement));

        if (SgEndifDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEndifDirectiveStatement* blkptr{reinterpret_cast<SgEndifDirectiveStatement*>(SgEndifDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgEndifDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEndifDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEndifDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEndifDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEndifDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEndifDirectiveStatement::pool_size - 1};
    SgEndifDirectiveStatement* testPointer{reinterpret_cast<SgEndifDirectiveStatement*>(SgEndifDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEndifDirectiveStatement::pool_size * (SgEndifDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEndifDirectiveStatementStroageClass
 * from its counterpart of SgEndifDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEndifDirectiveStatement::initializeStorageClassArray(SgEndifDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEndifDirectiveStatement::pools.begin(); block != SgEndifDirectiveStatement::pools.end(); ++block) {
        SgEndifDirectiveStatement* pointer{reinterpret_cast<SgEndifDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgEndifDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 73308 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLineDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgLineDirectiveStatement::SgLineDirectiveStatement(const SgLineDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 73318 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLineDirectiveStatement*
SgLineDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLineDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLineDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLineDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLineDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLineDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgLineDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLineDirectiveStatement::pool_size};
        returnPointer = &(((SgLineDirectiveStatement*)(SgLineDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLineDirectiveStatement*
SgLineDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLineDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLineDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLineDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLineDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLineDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgLineDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLineDirectiveStatement::pool_size};
        returnPointer = &(((SgLineDirectiveStatement*)(SgLineDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLineDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLineDirectiveStatement::pools.begin(); block != SgLineDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLineDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgLineDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLineDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLineDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLineDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgLineDirectiveStatement::pools.begin(); block != SgLineDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLineDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgLineDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLineDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLineDirectiveStatement::clearMemoryPool() {
    SgLineDirectiveStatement* tempPointer{nullptr};
    if (SgLineDirectiveStatement::pools.empty() == false) {
        auto block{SgLineDirectiveStatement::pools.begin()};
        SgLineDirectiveStatement::next_node = reinterpret_cast<SgLineDirectiveStatement*>(*block);
        for (; block != SgLineDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLineDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLineDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLineDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLineDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgLineDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgLineDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgLineDirectiveStatement::next_node = nullptr;
    SgLineDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLineDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLineDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLineDirectiveStatement)};
    for (size_t blockIndex = SgLineDirectiveStatement::pools.size(); (blockIndex * SgLineDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLineDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLineDirectiveStatement) = %" PRIuPTR " SgLineDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLineDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLineDirectiveStatement),SgLineDirectiveStatement::pool_size);
        }
#endif

        SgLineDirectiveStatement* pointer{static_cast<SgLineDirectiveStatement*>(ROSE_MALLOC(SgLineDirectiveStatement::pool_size * sizeof(SgLineDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLineDirectiveStatement::pool_size * sizeof(SgLineDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLineDirectiveStatement::pool_size * sizeof(SgLineDirectiveStatement));
#endif
        SgLineDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLineDirectiveStatement::pool_size * sizeof(SgLineDirectiveStatement), V_SgLineDirectiveStatement));

        if (SgLineDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLineDirectiveStatement* blkptr{reinterpret_cast<SgLineDirectiveStatement*>(SgLineDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgLineDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLineDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLineDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLineDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLineDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLineDirectiveStatement::pool_size - 1};
    SgLineDirectiveStatement* testPointer{reinterpret_cast<SgLineDirectiveStatement*>(SgLineDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLineDirectiveStatement::pool_size * (SgLineDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLineDirectiveStatementStroageClass
 * from its counterpart of SgLineDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLineDirectiveStatement::initializeStorageClassArray(SgLineDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLineDirectiveStatement::pools.begin(); block != SgLineDirectiveStatement::pools.end(); ++block) {
        SgLineDirectiveStatement* pointer{reinterpret_cast<SgLineDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgLineDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 73535 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWarningDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgWarningDirectiveStatement::SgWarningDirectiveStatement(const SgWarningDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 73545 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWarningDirectiveStatement*
SgWarningDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWarningDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWarningDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWarningDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWarningDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWarningDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgWarningDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWarningDirectiveStatement::pool_size};
        returnPointer = &(((SgWarningDirectiveStatement*)(SgWarningDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWarningDirectiveStatement*
SgWarningDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWarningDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWarningDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWarningDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWarningDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWarningDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgWarningDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWarningDirectiveStatement::pool_size};
        returnPointer = &(((SgWarningDirectiveStatement*)(SgWarningDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWarningDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWarningDirectiveStatement::pools.begin(); block != SgWarningDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWarningDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgWarningDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWarningDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWarningDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWarningDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgWarningDirectiveStatement::pools.begin(); block != SgWarningDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWarningDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgWarningDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWarningDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWarningDirectiveStatement::clearMemoryPool() {
    SgWarningDirectiveStatement* tempPointer{nullptr};
    if (SgWarningDirectiveStatement::pools.empty() == false) {
        auto block{SgWarningDirectiveStatement::pools.begin()};
        SgWarningDirectiveStatement::next_node = reinterpret_cast<SgWarningDirectiveStatement*>(*block);
        for (; block != SgWarningDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWarningDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWarningDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWarningDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWarningDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgWarningDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgWarningDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgWarningDirectiveStatement::next_node = nullptr;
    SgWarningDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWarningDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWarningDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWarningDirectiveStatement)};
    for (size_t blockIndex = SgWarningDirectiveStatement::pools.size(); (blockIndex * SgWarningDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWarningDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWarningDirectiveStatement) = %" PRIuPTR " SgWarningDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWarningDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWarningDirectiveStatement),SgWarningDirectiveStatement::pool_size);
        }
#endif

        SgWarningDirectiveStatement* pointer{static_cast<SgWarningDirectiveStatement*>(ROSE_MALLOC(SgWarningDirectiveStatement::pool_size * sizeof(SgWarningDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWarningDirectiveStatement::pool_size * sizeof(SgWarningDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWarningDirectiveStatement::pool_size * sizeof(SgWarningDirectiveStatement));
#endif
        SgWarningDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWarningDirectiveStatement::pool_size * sizeof(SgWarningDirectiveStatement), V_SgWarningDirectiveStatement));

        if (SgWarningDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWarningDirectiveStatement* blkptr{reinterpret_cast<SgWarningDirectiveStatement*>(SgWarningDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgWarningDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWarningDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWarningDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWarningDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWarningDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWarningDirectiveStatement::pool_size - 1};
    SgWarningDirectiveStatement* testPointer{reinterpret_cast<SgWarningDirectiveStatement*>(SgWarningDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWarningDirectiveStatement::pool_size * (SgWarningDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWarningDirectiveStatementStroageClass
 * from its counterpart of SgWarningDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWarningDirectiveStatement::initializeStorageClassArray(SgWarningDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWarningDirectiveStatement::pools.begin(); block != SgWarningDirectiveStatement::pools.end(); ++block) {
        SgWarningDirectiveStatement* pointer{reinterpret_cast<SgWarningDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgWarningDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 73762 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgErrorDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgErrorDirectiveStatement::SgErrorDirectiveStatement(const SgErrorDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 73772 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgErrorDirectiveStatement*
SgErrorDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgErrorDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgErrorDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgErrorDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgErrorDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgErrorDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgErrorDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgErrorDirectiveStatement::pool_size};
        returnPointer = &(((SgErrorDirectiveStatement*)(SgErrorDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgErrorDirectiveStatement*
SgErrorDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgErrorDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgErrorDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgErrorDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgErrorDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgErrorDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgErrorDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgErrorDirectiveStatement::pool_size};
        returnPointer = &(((SgErrorDirectiveStatement*)(SgErrorDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgErrorDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgErrorDirectiveStatement::pools.begin(); block != SgErrorDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgErrorDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgErrorDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgErrorDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgErrorDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgErrorDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgErrorDirectiveStatement::pools.begin(); block != SgErrorDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgErrorDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgErrorDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgErrorDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgErrorDirectiveStatement::clearMemoryPool() {
    SgErrorDirectiveStatement* tempPointer{nullptr};
    if (SgErrorDirectiveStatement::pools.empty() == false) {
        auto block{SgErrorDirectiveStatement::pools.begin()};
        SgErrorDirectiveStatement::next_node = reinterpret_cast<SgErrorDirectiveStatement*>(*block);
        for (; block != SgErrorDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgErrorDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgErrorDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgErrorDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgErrorDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgErrorDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgErrorDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgErrorDirectiveStatement::next_node = nullptr;
    SgErrorDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgErrorDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgErrorDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgErrorDirectiveStatement)};
    for (size_t blockIndex = SgErrorDirectiveStatement::pools.size(); (blockIndex * SgErrorDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgErrorDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgErrorDirectiveStatement) = %" PRIuPTR " SgErrorDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgErrorDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgErrorDirectiveStatement),SgErrorDirectiveStatement::pool_size);
        }
#endif

        SgErrorDirectiveStatement* pointer{static_cast<SgErrorDirectiveStatement*>(ROSE_MALLOC(SgErrorDirectiveStatement::pool_size * sizeof(SgErrorDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgErrorDirectiveStatement::pool_size * sizeof(SgErrorDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgErrorDirectiveStatement::pool_size * sizeof(SgErrorDirectiveStatement));
#endif
        SgErrorDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgErrorDirectiveStatement::pool_size * sizeof(SgErrorDirectiveStatement), V_SgErrorDirectiveStatement));

        if (SgErrorDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgErrorDirectiveStatement* blkptr{reinterpret_cast<SgErrorDirectiveStatement*>(SgErrorDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgErrorDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgErrorDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgErrorDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgErrorDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgErrorDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgErrorDirectiveStatement::pool_size - 1};
    SgErrorDirectiveStatement* testPointer{reinterpret_cast<SgErrorDirectiveStatement*>(SgErrorDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgErrorDirectiveStatement::pool_size * (SgErrorDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgErrorDirectiveStatementStroageClass
 * from its counterpart of SgErrorDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgErrorDirectiveStatement::initializeStorageClassArray(SgErrorDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgErrorDirectiveStatement::pools.begin(); block != SgErrorDirectiveStatement::pools.end(); ++block) {
        SgErrorDirectiveStatement* pointer{reinterpret_cast<SgErrorDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgErrorDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 73989 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEmptyDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgEmptyDirectiveStatement::SgEmptyDirectiveStatement(const SgEmptyDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 73999 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEmptyDirectiveStatement*
SgEmptyDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEmptyDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEmptyDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEmptyDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEmptyDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgEmptyDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEmptyDirectiveStatement::pool_size};
        returnPointer = &(((SgEmptyDirectiveStatement*)(SgEmptyDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEmptyDirectiveStatement*
SgEmptyDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEmptyDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEmptyDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEmptyDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEmptyDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEmptyDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgEmptyDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEmptyDirectiveStatement::pool_size};
        returnPointer = &(((SgEmptyDirectiveStatement*)(SgEmptyDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEmptyDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEmptyDirectiveStatement::pools.begin(); block != SgEmptyDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEmptyDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgEmptyDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEmptyDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEmptyDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEmptyDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgEmptyDirectiveStatement::pools.begin(); block != SgEmptyDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEmptyDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgEmptyDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEmptyDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEmptyDirectiveStatement::clearMemoryPool() {
    SgEmptyDirectiveStatement* tempPointer{nullptr};
    if (SgEmptyDirectiveStatement::pools.empty() == false) {
        auto block{SgEmptyDirectiveStatement::pools.begin()};
        SgEmptyDirectiveStatement::next_node = reinterpret_cast<SgEmptyDirectiveStatement*>(*block);
        for (; block != SgEmptyDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEmptyDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEmptyDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEmptyDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEmptyDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgEmptyDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgEmptyDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgEmptyDirectiveStatement::next_node = nullptr;
    SgEmptyDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEmptyDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEmptyDirectiveStatement)};
    for (size_t blockIndex = SgEmptyDirectiveStatement::pools.size(); (blockIndex * SgEmptyDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEmptyDirectiveStatement) = %" PRIuPTR " SgEmptyDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEmptyDirectiveStatement),SgEmptyDirectiveStatement::pool_size);
        }
#endif

        SgEmptyDirectiveStatement* pointer{static_cast<SgEmptyDirectiveStatement*>(ROSE_MALLOC(SgEmptyDirectiveStatement::pool_size * sizeof(SgEmptyDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEmptyDirectiveStatement::pool_size * sizeof(SgEmptyDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEmptyDirectiveStatement::pool_size * sizeof(SgEmptyDirectiveStatement));
#endif
        SgEmptyDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEmptyDirectiveStatement::pool_size * sizeof(SgEmptyDirectiveStatement), V_SgEmptyDirectiveStatement));

        if (SgEmptyDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEmptyDirectiveStatement* blkptr{reinterpret_cast<SgEmptyDirectiveStatement*>(SgEmptyDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgEmptyDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEmptyDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEmptyDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEmptyDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEmptyDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEmptyDirectiveStatement::pool_size - 1};
    SgEmptyDirectiveStatement* testPointer{reinterpret_cast<SgEmptyDirectiveStatement*>(SgEmptyDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEmptyDirectiveStatement::pool_size * (SgEmptyDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEmptyDirectiveStatementStroageClass
 * from its counterpart of SgEmptyDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEmptyDirectiveStatement::initializeStorageClassArray(SgEmptyDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEmptyDirectiveStatement::pools.begin(); block != SgEmptyDirectiveStatement::pools.end(); ++block) {
        SgEmptyDirectiveStatement* pointer{reinterpret_cast<SgEmptyDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgEmptyDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 74216 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIncludeNextDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgIncludeNextDirectiveStatement::SgIncludeNextDirectiveStatement(const SgIncludeNextDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 74226 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIncludeNextDirectiveStatement*
SgIncludeNextDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIncludeNextDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeNextDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeNextDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIncludeNextDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeNextDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIncludeNextDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncludeNextDirectiveStatement::pool_size};
        returnPointer = &(((SgIncludeNextDirectiveStatement*)(SgIncludeNextDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIncludeNextDirectiveStatement*
SgIncludeNextDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIncludeNextDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeNextDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeNextDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIncludeNextDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIncludeNextDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIncludeNextDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIncludeNextDirectiveStatement::pool_size};
        returnPointer = &(((SgIncludeNextDirectiveStatement*)(SgIncludeNextDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIncludeNextDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIncludeNextDirectiveStatement::pools.begin(); block != SgIncludeNextDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncludeNextDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIncludeNextDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIncludeNextDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIncludeNextDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIncludeNextDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgIncludeNextDirectiveStatement::pools.begin(); block != SgIncludeNextDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIncludeNextDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIncludeNextDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIncludeNextDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIncludeNextDirectiveStatement::clearMemoryPool() {
    SgIncludeNextDirectiveStatement* tempPointer{nullptr};
    if (SgIncludeNextDirectiveStatement::pools.empty() == false) {
        auto block{SgIncludeNextDirectiveStatement::pools.begin()};
        SgIncludeNextDirectiveStatement::next_node = reinterpret_cast<SgIncludeNextDirectiveStatement*>(*block);
        for (; block != SgIncludeNextDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIncludeNextDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIncludeNextDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIncludeNextDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIncludeNextDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgIncludeNextDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgIncludeNextDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgIncludeNextDirectiveStatement::next_node = nullptr;
    SgIncludeNextDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIncludeNextDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeNextDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeNextDirectiveStatement)};
    for (size_t blockIndex = SgIncludeNextDirectiveStatement::pools.size(); (blockIndex * SgIncludeNextDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeNextDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeNextDirectiveStatement) = %" PRIuPTR " SgIncludeNextDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIncludeNextDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIncludeNextDirectiveStatement),SgIncludeNextDirectiveStatement::pool_size);
        }
#endif

        SgIncludeNextDirectiveStatement* pointer{static_cast<SgIncludeNextDirectiveStatement*>(ROSE_MALLOC(SgIncludeNextDirectiveStatement::pool_size * sizeof(SgIncludeNextDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIncludeNextDirectiveStatement::pool_size * sizeof(SgIncludeNextDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIncludeNextDirectiveStatement::pool_size * sizeof(SgIncludeNextDirectiveStatement));
#endif
        SgIncludeNextDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIncludeNextDirectiveStatement::pool_size * sizeof(SgIncludeNextDirectiveStatement), V_SgIncludeNextDirectiveStatement));

        if (SgIncludeNextDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIncludeNextDirectiveStatement* blkptr{reinterpret_cast<SgIncludeNextDirectiveStatement*>(SgIncludeNextDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgIncludeNextDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIncludeNextDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIncludeNextDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIncludeNextDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIncludeNextDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIncludeNextDirectiveStatement::pool_size - 1};
    SgIncludeNextDirectiveStatement* testPointer{reinterpret_cast<SgIncludeNextDirectiveStatement*>(SgIncludeNextDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIncludeNextDirectiveStatement::pool_size * (SgIncludeNextDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIncludeNextDirectiveStatementStroageClass
 * from its counterpart of SgIncludeNextDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIncludeNextDirectiveStatement::initializeStorageClassArray(SgIncludeNextDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIncludeNextDirectiveStatement::pools.begin(); block != SgIncludeNextDirectiveStatement::pools.end(); ++block) {
        SgIncludeNextDirectiveStatement* pointer{reinterpret_cast<SgIncludeNextDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIncludeNextDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 74443 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIdentDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgIdentDirectiveStatement::SgIdentDirectiveStatement(const SgIdentDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 74453 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIdentDirectiveStatement*
SgIdentDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIdentDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIdentDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIdentDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIdentDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIdentDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIdentDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIdentDirectiveStatement::pool_size};
        returnPointer = &(((SgIdentDirectiveStatement*)(SgIdentDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIdentDirectiveStatement*
SgIdentDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIdentDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIdentDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIdentDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIdentDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIdentDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgIdentDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIdentDirectiveStatement::pool_size};
        returnPointer = &(((SgIdentDirectiveStatement*)(SgIdentDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIdentDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIdentDirectiveStatement::pools.begin(); block != SgIdentDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIdentDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIdentDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIdentDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIdentDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIdentDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgIdentDirectiveStatement::pools.begin(); block != SgIdentDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIdentDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIdentDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIdentDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIdentDirectiveStatement::clearMemoryPool() {
    SgIdentDirectiveStatement* tempPointer{nullptr};
    if (SgIdentDirectiveStatement::pools.empty() == false) {
        auto block{SgIdentDirectiveStatement::pools.begin()};
        SgIdentDirectiveStatement::next_node = reinterpret_cast<SgIdentDirectiveStatement*>(*block);
        for (; block != SgIdentDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIdentDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIdentDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIdentDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIdentDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgIdentDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgIdentDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgIdentDirectiveStatement::next_node = nullptr;
    SgIdentDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIdentDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIdentDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIdentDirectiveStatement)};
    for (size_t blockIndex = SgIdentDirectiveStatement::pools.size(); (blockIndex * SgIdentDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIdentDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIdentDirectiveStatement) = %" PRIuPTR " SgIdentDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIdentDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIdentDirectiveStatement),SgIdentDirectiveStatement::pool_size);
        }
#endif

        SgIdentDirectiveStatement* pointer{static_cast<SgIdentDirectiveStatement*>(ROSE_MALLOC(SgIdentDirectiveStatement::pool_size * sizeof(SgIdentDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIdentDirectiveStatement::pool_size * sizeof(SgIdentDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIdentDirectiveStatement::pool_size * sizeof(SgIdentDirectiveStatement));
#endif
        SgIdentDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIdentDirectiveStatement::pool_size * sizeof(SgIdentDirectiveStatement), V_SgIdentDirectiveStatement));

        if (SgIdentDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIdentDirectiveStatement* blkptr{reinterpret_cast<SgIdentDirectiveStatement*>(SgIdentDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgIdentDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIdentDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIdentDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIdentDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIdentDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIdentDirectiveStatement::pool_size - 1};
    SgIdentDirectiveStatement* testPointer{reinterpret_cast<SgIdentDirectiveStatement*>(SgIdentDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIdentDirectiveStatement::pool_size * (SgIdentDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIdentDirectiveStatementStroageClass
 * from its counterpart of SgIdentDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIdentDirectiveStatement::initializeStorageClassArray(SgIdentDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIdentDirectiveStatement::pools.begin(); block != SgIdentDirectiveStatement::pools.end(); ++block) {
        SgIdentDirectiveStatement* pointer{reinterpret_cast<SgIdentDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgIdentDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 74670 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLinemarkerDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgLinemarkerDirectiveStatement::SgLinemarkerDirectiveStatement(const SgLinemarkerDirectiveStatementStorageClass& storageSource)  : SgC_PreprocessorDirectiveStatement (storageSource) {

/* #line 74680 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_linenumber = storageSource.storageOf_linenumber;
    p_filename = storageSource.storageOf_filename.rebuildDataStoredInEasyStorageClass();
    p_flaglist = storageSource.storageOf_flaglist.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLinemarkerDirectiveStatement*
SgLinemarkerDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLinemarkerDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLinemarkerDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLinemarkerDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLinemarkerDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLinemarkerDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgLinemarkerDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLinemarkerDirectiveStatement::pool_size};
        returnPointer = &(((SgLinemarkerDirectiveStatement*)(SgLinemarkerDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLinemarkerDirectiveStatement*
SgLinemarkerDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLinemarkerDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLinemarkerDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLinemarkerDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLinemarkerDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLinemarkerDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgLinemarkerDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLinemarkerDirectiveStatement::pool_size};
        returnPointer = &(((SgLinemarkerDirectiveStatement*)(SgLinemarkerDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLinemarkerDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLinemarkerDirectiveStatement::pools.begin(); block != SgLinemarkerDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLinemarkerDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgLinemarkerDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLinemarkerDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLinemarkerDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLinemarkerDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgLinemarkerDirectiveStatement::pools.begin(); block != SgLinemarkerDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLinemarkerDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgLinemarkerDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLinemarkerDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLinemarkerDirectiveStatement::clearMemoryPool() {
    SgLinemarkerDirectiveStatement* tempPointer{nullptr};
    if (SgLinemarkerDirectiveStatement::pools.empty() == false) {
        auto block{SgLinemarkerDirectiveStatement::pools.begin()};
        SgLinemarkerDirectiveStatement::next_node = reinterpret_cast<SgLinemarkerDirectiveStatement*>(*block);
        for (; block != SgLinemarkerDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLinemarkerDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLinemarkerDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLinemarkerDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLinemarkerDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgLinemarkerDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgLinemarkerDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgLinemarkerDirectiveStatement::next_node = nullptr;
    SgLinemarkerDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLinemarkerDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLinemarkerDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLinemarkerDirectiveStatement)};
    for (size_t blockIndex = SgLinemarkerDirectiveStatement::pools.size(); (blockIndex * SgLinemarkerDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLinemarkerDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLinemarkerDirectiveStatement) = %" PRIuPTR " SgLinemarkerDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLinemarkerDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLinemarkerDirectiveStatement),SgLinemarkerDirectiveStatement::pool_size);
        }
#endif

        SgLinemarkerDirectiveStatement* pointer{static_cast<SgLinemarkerDirectiveStatement*>(ROSE_MALLOC(SgLinemarkerDirectiveStatement::pool_size * sizeof(SgLinemarkerDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLinemarkerDirectiveStatement::pool_size * sizeof(SgLinemarkerDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLinemarkerDirectiveStatement::pool_size * sizeof(SgLinemarkerDirectiveStatement));
#endif
        SgLinemarkerDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLinemarkerDirectiveStatement::pool_size * sizeof(SgLinemarkerDirectiveStatement), V_SgLinemarkerDirectiveStatement));

        if (SgLinemarkerDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLinemarkerDirectiveStatement* blkptr{reinterpret_cast<SgLinemarkerDirectiveStatement*>(SgLinemarkerDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgLinemarkerDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLinemarkerDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLinemarkerDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLinemarkerDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLinemarkerDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLinemarkerDirectiveStatement::pool_size - 1};
    SgLinemarkerDirectiveStatement* testPointer{reinterpret_cast<SgLinemarkerDirectiveStatement*>(SgLinemarkerDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLinemarkerDirectiveStatement::pool_size * (SgLinemarkerDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLinemarkerDirectiveStatementStroageClass
 * from its counterpart of SgLinemarkerDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLinemarkerDirectiveStatement::initializeStorageClassArray(SgLinemarkerDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLinemarkerDirectiveStatement::pools.begin(); block != SgLinemarkerDirectiveStatement::pools.end(); ++block) {
        SgLinemarkerDirectiveStatement* pointer{reinterpret_cast<SgLinemarkerDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgLinemarkerDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 74900 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpThreadprivateStatement that takes its corresponding StorageClass as parameter
 */
SgOmpThreadprivateStatement::SgOmpThreadprivateStatement(const SgOmpThreadprivateStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 74910 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_variables = storageSource.storageOf_variables.rebuildDataStoredInEasyStorageClass();
    SgVarRefExpPtrList::iterator i_variables = p_variables.begin();
    for ( ; i_variables != p_variables.end(); ++i_variables) {
        (*i_variables) = (SgVarRefExpPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_variables)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpThreadprivateStatement*
SgOmpThreadprivateStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpThreadprivateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpThreadprivateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpThreadprivateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpThreadprivateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpThreadprivateStatement)};
        unsigned long positionInPool{localIndex % SgOmpThreadprivateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpThreadprivateStatement::pool_size};
        returnPointer = &(((SgOmpThreadprivateStatement*)(SgOmpThreadprivateStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpThreadprivateStatement*
SgOmpThreadprivateStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpThreadprivateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpThreadprivateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpThreadprivateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpThreadprivateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpThreadprivateStatement)};
        unsigned long positionInPool{localIndex % SgOmpThreadprivateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpThreadprivateStatement::pool_size};
        returnPointer = &(((SgOmpThreadprivateStatement*)(SgOmpThreadprivateStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpThreadprivateStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpThreadprivateStatement::pools.begin(); block != SgOmpThreadprivateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpThreadprivateStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpThreadprivateStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpThreadprivateStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpThreadprivateStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpThreadprivateStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpThreadprivateStatement::pools.begin(); block != SgOmpThreadprivateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpThreadprivateStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpThreadprivateStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpThreadprivateStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpThreadprivateStatement::clearMemoryPool() {
    SgOmpThreadprivateStatement* tempPointer{nullptr};
    if (SgOmpThreadprivateStatement::pools.empty() == false) {
        auto block{SgOmpThreadprivateStatement::pools.begin()};
        SgOmpThreadprivateStatement::next_node = reinterpret_cast<SgOmpThreadprivateStatement*>(*block);
        for (; block != SgOmpThreadprivateStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpThreadprivateStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpThreadprivateStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpThreadprivateStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpThreadprivateStatement::pool_size-1]);
        }
    }
}

void
SgOmpThreadprivateStatement::deleteMemoryPool() {
    for (auto p: SgOmpThreadprivateStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpThreadprivateStatement::next_node = nullptr;
    SgOmpThreadprivateStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpThreadprivateStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpThreadprivateStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpThreadprivateStatement)};
    for (size_t blockIndex = SgOmpThreadprivateStatement::pools.size(); (blockIndex * SgOmpThreadprivateStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpThreadprivateStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpThreadprivateStatement) = %" PRIuPTR " SgOmpThreadprivateStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpThreadprivateStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpThreadprivateStatement),SgOmpThreadprivateStatement::pool_size);
        }
#endif

        SgOmpThreadprivateStatement* pointer{static_cast<SgOmpThreadprivateStatement*>(ROSE_MALLOC(SgOmpThreadprivateStatement::pool_size * sizeof(SgOmpThreadprivateStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpThreadprivateStatement::pool_size * sizeof(SgOmpThreadprivateStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpThreadprivateStatement::pool_size * sizeof(SgOmpThreadprivateStatement));
#endif
        SgOmpThreadprivateStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpThreadprivateStatement::pool_size * sizeof(SgOmpThreadprivateStatement), V_SgOmpThreadprivateStatement));

        if (SgOmpThreadprivateStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpThreadprivateStatement* blkptr{reinterpret_cast<SgOmpThreadprivateStatement*>(SgOmpThreadprivateStatement::pools[blockIndex-1])};
                blkptr[SgOmpThreadprivateStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpThreadprivateStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpThreadprivateStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpThreadprivateStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpThreadprivateStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpThreadprivateStatement::pool_size - 1};
    SgOmpThreadprivateStatement* testPointer{reinterpret_cast<SgOmpThreadprivateStatement*>(SgOmpThreadprivateStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpThreadprivateStatement::pool_size * (SgOmpThreadprivateStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpThreadprivateStatementStroageClass
 * from its counterpart of SgOmpThreadprivateStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpThreadprivateStatement::initializeStorageClassArray(SgOmpThreadprivateStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpThreadprivateStatement::pools.begin(); block != SgOmpThreadprivateStatement::pools.end(); ++block) {
        SgOmpThreadprivateStatement* pointer{reinterpret_cast<SgOmpThreadprivateStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpThreadprivateStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 75132 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFortranIncludeLine that takes its corresponding StorageClass as parameter
 */
SgFortranIncludeLine::SgFortranIncludeLine(const SgFortranIncludeLineStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 75142 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_filename = storageSource.storageOf_filename.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFortranIncludeLine*
SgFortranIncludeLine::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFortranIncludeLine* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranIncludeLine)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranIncludeLine + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranIncludeLine)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranIncludeLine)};
        unsigned long positionInPool{localIndex % SgFortranIncludeLine::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranIncludeLine::pool_size};
        returnPointer = &(((SgFortranIncludeLine*)(SgFortranIncludeLine::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFortranIncludeLine*
SgFortranIncludeLine::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFortranIncludeLine* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranIncludeLine)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranIncludeLine + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranIncludeLine)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFortranIncludeLine)};
        unsigned long positionInPool{localIndex % SgFortranIncludeLine::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranIncludeLine::pool_size};
        returnPointer = &(((SgFortranIncludeLine*)(SgFortranIncludeLine::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFortranIncludeLine::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFortranIncludeLine::pools.begin(); block != SgFortranIncludeLine::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranIncludeLine*>(*block)};
        for (unsigned i = 0; i < SgFortranIncludeLine::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFortranIncludeLine*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFortranIncludeLine::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFortranIncludeLine* pointerOfLinkedList{nullptr};
    for (auto block = SgFortranIncludeLine::pools.begin(); block != SgFortranIncludeLine::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranIncludeLine*>(*block)};
        for (unsigned i = 0; i < SgFortranIncludeLine::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFortranIncludeLine::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFortranIncludeLine::clearMemoryPool() {
    SgFortranIncludeLine* tempPointer{nullptr};
    if (SgFortranIncludeLine::pools.empty() == false) {
        auto block{SgFortranIncludeLine::pools.begin()};
        SgFortranIncludeLine::next_node = reinterpret_cast<SgFortranIncludeLine*>(*block);
        for (; block != SgFortranIncludeLine::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFortranIncludeLine*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFortranIncludeLine::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFortranIncludeLine::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFortranIncludeLine::pool_size-1]);
        }
    }
}

void
SgFortranIncludeLine::deleteMemoryPool() {
    for (auto p: SgFortranIncludeLine::pools) {
        ROSE_FREE(p);
    }
    SgFortranIncludeLine::next_node = nullptr;
    SgFortranIncludeLine::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFortranIncludeLine::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranIncludeLine) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranIncludeLine)};
    for (size_t blockIndex = SgFortranIncludeLine::pools.size(); (blockIndex * SgFortranIncludeLine::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranIncludeLine) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranIncludeLine) = %" PRIuPTR " SgFortranIncludeLine::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranIncludeLine),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranIncludeLine),SgFortranIncludeLine::pool_size);
        }
#endif

        SgFortranIncludeLine* pointer{static_cast<SgFortranIncludeLine*>(ROSE_MALLOC(SgFortranIncludeLine::pool_size * sizeof(SgFortranIncludeLine)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFortranIncludeLine::pool_size * sizeof(SgFortranIncludeLine));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFortranIncludeLine::pool_size * sizeof(SgFortranIncludeLine));
#endif
        SgFortranIncludeLine::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFortranIncludeLine::pool_size * sizeof(SgFortranIncludeLine), V_SgFortranIncludeLine));

        if (SgFortranIncludeLine::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFortranIncludeLine* blkptr{reinterpret_cast<SgFortranIncludeLine*>(SgFortranIncludeLine::pools[blockIndex-1])};
                blkptr[SgFortranIncludeLine::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFortranIncludeLine::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFortranIncludeLine::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFortranIncludeLine::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFortranIncludeLine::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFortranIncludeLine::pool_size - 1};
    SgFortranIncludeLine* testPointer{reinterpret_cast<SgFortranIncludeLine*>(SgFortranIncludeLine::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFortranIncludeLine::pool_size * (SgFortranIncludeLine::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFortranIncludeLineStroageClass
 * from its counterpart of SgFortranIncludeLine. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFortranIncludeLine::initializeStorageClassArray(SgFortranIncludeLineStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFortranIncludeLine::pools.begin(); block != SgFortranIncludeLine::pools.end(); ++block) {
        SgFortranIncludeLine* pointer{reinterpret_cast<SgFortranIncludeLine*>(*block)};
        for (unsigned i = 0; i < SgFortranIncludeLine::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 75360 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaImportStatement that takes its corresponding StorageClass as parameter
 */
SgJavaImportStatement::SgJavaImportStatement(const SgJavaImportStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 75370 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_path = SgName ( storageSource.storageOf_path);
    p_containsWildCard = storageSource.storageOf_containsWildCard;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaImportStatement*
SgJavaImportStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaImportStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaImportStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaImportStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaImportStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatement)};
        unsigned long positionInPool{localIndex % SgJavaImportStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaImportStatement::pool_size};
        returnPointer = &(((SgJavaImportStatement*)(SgJavaImportStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaImportStatement*
SgJavaImportStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaImportStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaImportStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaImportStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaImportStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaImportStatement)};
        unsigned long positionInPool{localIndex % SgJavaImportStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaImportStatement::pool_size};
        returnPointer = &(((SgJavaImportStatement*)(SgJavaImportStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaImportStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaImportStatement::pools.begin(); block != SgJavaImportStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaImportStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaImportStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaImportStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaImportStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaImportStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaImportStatement::pools.begin(); block != SgJavaImportStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaImportStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaImportStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaImportStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaImportStatement::clearMemoryPool() {
    SgJavaImportStatement* tempPointer{nullptr};
    if (SgJavaImportStatement::pools.empty() == false) {
        auto block{SgJavaImportStatement::pools.begin()};
        SgJavaImportStatement::next_node = reinterpret_cast<SgJavaImportStatement*>(*block);
        for (; block != SgJavaImportStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaImportStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaImportStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaImportStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaImportStatement::pool_size-1]);
        }
    }
}

void
SgJavaImportStatement::deleteMemoryPool() {
    for (auto p: SgJavaImportStatement::pools) {
        ROSE_FREE(p);
    }
    SgJavaImportStatement::next_node = nullptr;
    SgJavaImportStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaImportStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaImportStatement)};
    for (size_t blockIndex = SgJavaImportStatement::pools.size(); (blockIndex * SgJavaImportStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaImportStatement) = %" PRIuPTR " SgJavaImportStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaImportStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaImportStatement),SgJavaImportStatement::pool_size);
        }
#endif

        SgJavaImportStatement* pointer{static_cast<SgJavaImportStatement*>(ROSE_MALLOC(SgJavaImportStatement::pool_size * sizeof(SgJavaImportStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaImportStatement::pool_size * sizeof(SgJavaImportStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaImportStatement::pool_size * sizeof(SgJavaImportStatement));
#endif
        SgJavaImportStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaImportStatement::pool_size * sizeof(SgJavaImportStatement), V_SgJavaImportStatement));

        if (SgJavaImportStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaImportStatement* blkptr{reinterpret_cast<SgJavaImportStatement*>(SgJavaImportStatement::pools[blockIndex-1])};
                blkptr[SgJavaImportStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaImportStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaImportStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaImportStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaImportStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaImportStatement::pool_size - 1};
    SgJavaImportStatement* testPointer{reinterpret_cast<SgJavaImportStatement*>(SgJavaImportStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaImportStatement::pool_size * (SgJavaImportStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaImportStatementStroageClass
 * from its counterpart of SgJavaImportStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaImportStatement::initializeStorageClassArray(SgJavaImportStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaImportStatement::pools.begin(); block != SgJavaImportStatement::pools.end(); ++block) {
        SgJavaImportStatement* pointer{reinterpret_cast<SgJavaImportStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaImportStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 75589 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaPackageStatement that takes its corresponding StorageClass as parameter
 */
SgJavaPackageStatement::SgJavaPackageStatement(const SgJavaPackageStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 75599 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaPackageStatement*
SgJavaPackageStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaPackageStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaPackageStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaPackageStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaPackageStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageStatement)};
        unsigned long positionInPool{localIndex % SgJavaPackageStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaPackageStatement::pool_size};
        returnPointer = &(((SgJavaPackageStatement*)(SgJavaPackageStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaPackageStatement*
SgJavaPackageStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaPackageStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaPackageStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaPackageStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaPackageStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaPackageStatement)};
        unsigned long positionInPool{localIndex % SgJavaPackageStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaPackageStatement::pool_size};
        returnPointer = &(((SgJavaPackageStatement*)(SgJavaPackageStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaPackageStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaPackageStatement::pools.begin(); block != SgJavaPackageStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaPackageStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaPackageStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaPackageStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaPackageStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaPackageStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaPackageStatement::pools.begin(); block != SgJavaPackageStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaPackageStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaPackageStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaPackageStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaPackageStatement::clearMemoryPool() {
    SgJavaPackageStatement* tempPointer{nullptr};
    if (SgJavaPackageStatement::pools.empty() == false) {
        auto block{SgJavaPackageStatement::pools.begin()};
        SgJavaPackageStatement::next_node = reinterpret_cast<SgJavaPackageStatement*>(*block);
        for (; block != SgJavaPackageStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaPackageStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaPackageStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaPackageStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaPackageStatement::pool_size-1]);
        }
    }
}

void
SgJavaPackageStatement::deleteMemoryPool() {
    for (auto p: SgJavaPackageStatement::pools) {
        ROSE_FREE(p);
    }
    SgJavaPackageStatement::next_node = nullptr;
    SgJavaPackageStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaPackageStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaPackageStatement)};
    for (size_t blockIndex = SgJavaPackageStatement::pools.size(); (blockIndex * SgJavaPackageStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaPackageStatement) = %" PRIuPTR " SgJavaPackageStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaPackageStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaPackageStatement),SgJavaPackageStatement::pool_size);
        }
#endif

        SgJavaPackageStatement* pointer{static_cast<SgJavaPackageStatement*>(ROSE_MALLOC(SgJavaPackageStatement::pool_size * sizeof(SgJavaPackageStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaPackageStatement::pool_size * sizeof(SgJavaPackageStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaPackageStatement::pool_size * sizeof(SgJavaPackageStatement));
#endif
        SgJavaPackageStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaPackageStatement::pool_size * sizeof(SgJavaPackageStatement), V_SgJavaPackageStatement));

        if (SgJavaPackageStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaPackageStatement* blkptr{reinterpret_cast<SgJavaPackageStatement*>(SgJavaPackageStatement::pools[blockIndex-1])};
                blkptr[SgJavaPackageStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaPackageStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaPackageStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaPackageStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaPackageStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaPackageStatement::pool_size - 1};
    SgJavaPackageStatement* testPointer{reinterpret_cast<SgJavaPackageStatement*>(SgJavaPackageStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaPackageStatement::pool_size * (SgJavaPackageStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaPackageStatementStroageClass
 * from its counterpart of SgJavaPackageStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaPackageStatement::initializeStorageClassArray(SgJavaPackageStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaPackageStatement::pools.begin(); block != SgJavaPackageStatement::pools.end(); ++block) {
        SgJavaPackageStatement* pointer{reinterpret_cast<SgJavaPackageStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaPackageStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 75817 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStmtDeclarationStatement that takes its corresponding StorageClass as parameter
 */
SgStmtDeclarationStatement::SgStmtDeclarationStatement(const SgStmtDeclarationStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 75827 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_statement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_statement));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStmtDeclarationStatement*
SgStmtDeclarationStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStmtDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStmtDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStmtDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStmtDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStmtDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgStmtDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStmtDeclarationStatement::pool_size};
        returnPointer = &(((SgStmtDeclarationStatement*)(SgStmtDeclarationStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStmtDeclarationStatement*
SgStmtDeclarationStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStmtDeclarationStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStmtDeclarationStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStmtDeclarationStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStmtDeclarationStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStmtDeclarationStatement)};
        unsigned long positionInPool{localIndex % SgStmtDeclarationStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStmtDeclarationStatement::pool_size};
        returnPointer = &(((SgStmtDeclarationStatement*)(SgStmtDeclarationStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStmtDeclarationStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStmtDeclarationStatement::pools.begin(); block != SgStmtDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStmtDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgStmtDeclarationStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStmtDeclarationStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStmtDeclarationStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStmtDeclarationStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgStmtDeclarationStatement::pools.begin(); block != SgStmtDeclarationStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStmtDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgStmtDeclarationStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStmtDeclarationStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStmtDeclarationStatement::clearMemoryPool() {
    SgStmtDeclarationStatement* tempPointer{nullptr};
    if (SgStmtDeclarationStatement::pools.empty() == false) {
        auto block{SgStmtDeclarationStatement::pools.begin()};
        SgStmtDeclarationStatement::next_node = reinterpret_cast<SgStmtDeclarationStatement*>(*block);
        for (; block != SgStmtDeclarationStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStmtDeclarationStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStmtDeclarationStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStmtDeclarationStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStmtDeclarationStatement::pool_size-1]);
        }
    }
}

void
SgStmtDeclarationStatement::deleteMemoryPool() {
    for (auto p: SgStmtDeclarationStatement::pools) {
        ROSE_FREE(p);
    }
    SgStmtDeclarationStatement::next_node = nullptr;
    SgStmtDeclarationStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStmtDeclarationStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStmtDeclarationStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStmtDeclarationStatement)};
    for (size_t blockIndex = SgStmtDeclarationStatement::pools.size(); (blockIndex * SgStmtDeclarationStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStmtDeclarationStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStmtDeclarationStatement) = %" PRIuPTR " SgStmtDeclarationStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStmtDeclarationStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStmtDeclarationStatement),SgStmtDeclarationStatement::pool_size);
        }
#endif

        SgStmtDeclarationStatement* pointer{static_cast<SgStmtDeclarationStatement*>(ROSE_MALLOC(SgStmtDeclarationStatement::pool_size * sizeof(SgStmtDeclarationStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStmtDeclarationStatement::pool_size * sizeof(SgStmtDeclarationStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStmtDeclarationStatement::pool_size * sizeof(SgStmtDeclarationStatement));
#endif
        SgStmtDeclarationStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStmtDeclarationStatement::pool_size * sizeof(SgStmtDeclarationStatement), V_SgStmtDeclarationStatement));

        if (SgStmtDeclarationStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStmtDeclarationStatement* blkptr{reinterpret_cast<SgStmtDeclarationStatement*>(SgStmtDeclarationStatement::pools[blockIndex-1])};
                blkptr[SgStmtDeclarationStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStmtDeclarationStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStmtDeclarationStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStmtDeclarationStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStmtDeclarationStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStmtDeclarationStatement::pool_size - 1};
    SgStmtDeclarationStatement* testPointer{reinterpret_cast<SgStmtDeclarationStatement*>(SgStmtDeclarationStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStmtDeclarationStatement::pool_size * (SgStmtDeclarationStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStmtDeclarationStatementStroageClass
 * from its counterpart of SgStmtDeclarationStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStmtDeclarationStatement::initializeStorageClassArray(SgStmtDeclarationStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStmtDeclarationStatement::pools.begin(); block != SgStmtDeclarationStatement::pools.end(); ++block) {
        SgStmtDeclarationStatement* pointer{reinterpret_cast<SgStmtDeclarationStatement*>(*block)};
        for (unsigned i = 0; i < SgStmtDeclarationStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 76045 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStaticAssertionDeclaration that takes its corresponding StorageClass as parameter
 */
SgStaticAssertionDeclaration::SgStaticAssertionDeclaration(const SgStaticAssertionDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 76055 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_condition = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_condition));
    p_string_literal = SgName ( storageSource.storageOf_string_literal);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStaticAssertionDeclaration*
SgStaticAssertionDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStaticAssertionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStaticAssertionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStaticAssertionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStaticAssertionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStaticAssertionDeclaration)};
        unsigned long positionInPool{localIndex % SgStaticAssertionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStaticAssertionDeclaration::pool_size};
        returnPointer = &(((SgStaticAssertionDeclaration*)(SgStaticAssertionDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStaticAssertionDeclaration*
SgStaticAssertionDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStaticAssertionDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStaticAssertionDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStaticAssertionDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStaticAssertionDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStaticAssertionDeclaration)};
        unsigned long positionInPool{localIndex % SgStaticAssertionDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStaticAssertionDeclaration::pool_size};
        returnPointer = &(((SgStaticAssertionDeclaration*)(SgStaticAssertionDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStaticAssertionDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStaticAssertionDeclaration::pools.begin(); block != SgStaticAssertionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStaticAssertionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgStaticAssertionDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStaticAssertionDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStaticAssertionDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStaticAssertionDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgStaticAssertionDeclaration::pools.begin(); block != SgStaticAssertionDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStaticAssertionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgStaticAssertionDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStaticAssertionDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStaticAssertionDeclaration::clearMemoryPool() {
    SgStaticAssertionDeclaration* tempPointer{nullptr};
    if (SgStaticAssertionDeclaration::pools.empty() == false) {
        auto block{SgStaticAssertionDeclaration::pools.begin()};
        SgStaticAssertionDeclaration::next_node = reinterpret_cast<SgStaticAssertionDeclaration*>(*block);
        for (; block != SgStaticAssertionDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStaticAssertionDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStaticAssertionDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStaticAssertionDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStaticAssertionDeclaration::pool_size-1]);
        }
    }
}

void
SgStaticAssertionDeclaration::deleteMemoryPool() {
    for (auto p: SgStaticAssertionDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgStaticAssertionDeclaration::next_node = nullptr;
    SgStaticAssertionDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStaticAssertionDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStaticAssertionDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStaticAssertionDeclaration)};
    for (size_t blockIndex = SgStaticAssertionDeclaration::pools.size(); (blockIndex * SgStaticAssertionDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStaticAssertionDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStaticAssertionDeclaration) = %" PRIuPTR " SgStaticAssertionDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStaticAssertionDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStaticAssertionDeclaration),SgStaticAssertionDeclaration::pool_size);
        }
#endif

        SgStaticAssertionDeclaration* pointer{static_cast<SgStaticAssertionDeclaration*>(ROSE_MALLOC(SgStaticAssertionDeclaration::pool_size * sizeof(SgStaticAssertionDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStaticAssertionDeclaration::pool_size * sizeof(SgStaticAssertionDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStaticAssertionDeclaration::pool_size * sizeof(SgStaticAssertionDeclaration));
#endif
        SgStaticAssertionDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStaticAssertionDeclaration::pool_size * sizeof(SgStaticAssertionDeclaration), V_SgStaticAssertionDeclaration));

        if (SgStaticAssertionDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStaticAssertionDeclaration* blkptr{reinterpret_cast<SgStaticAssertionDeclaration*>(SgStaticAssertionDeclaration::pools[blockIndex-1])};
                blkptr[SgStaticAssertionDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStaticAssertionDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStaticAssertionDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStaticAssertionDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStaticAssertionDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStaticAssertionDeclaration::pool_size - 1};
    SgStaticAssertionDeclaration* testPointer{reinterpret_cast<SgStaticAssertionDeclaration*>(SgStaticAssertionDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStaticAssertionDeclaration::pool_size * (SgStaticAssertionDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStaticAssertionDeclarationStroageClass
 * from its counterpart of SgStaticAssertionDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStaticAssertionDeclaration::initializeStorageClassArray(SgStaticAssertionDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStaticAssertionDeclaration::pools.begin(); block != SgStaticAssertionDeclaration::pools.end(); ++block) {
        SgStaticAssertionDeclaration* pointer{reinterpret_cast<SgStaticAssertionDeclaration*>(*block)};
        for (unsigned i = 0; i < SgStaticAssertionDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 76274 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpDeclareSimdStatement that takes its corresponding StorageClass as parameter
 */
SgOmpDeclareSimdStatement::SgOmpDeclareSimdStatement(const SgOmpDeclareSimdStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 76284 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_clauses = storageSource.storageOf_clauses.rebuildDataStoredInEasyStorageClass();
    SgOmpClausePtrList::iterator i_clauses = p_clauses.begin();
    for ( ; i_clauses != p_clauses.end(); ++i_clauses) {
        (*i_clauses) = (SgOmpClausePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_clauses)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpDeclareSimdStatement*
SgOmpDeclareSimdStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpDeclareSimdStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDeclareSimdStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDeclareSimdStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDeclareSimdStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeclareSimdStatement)};
        unsigned long positionInPool{localIndex % SgOmpDeclareSimdStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDeclareSimdStatement::pool_size};
        returnPointer = &(((SgOmpDeclareSimdStatement*)(SgOmpDeclareSimdStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpDeclareSimdStatement*
SgOmpDeclareSimdStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpDeclareSimdStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDeclareSimdStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDeclareSimdStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDeclareSimdStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpDeclareSimdStatement)};
        unsigned long positionInPool{localIndex % SgOmpDeclareSimdStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDeclareSimdStatement::pool_size};
        returnPointer = &(((SgOmpDeclareSimdStatement*)(SgOmpDeclareSimdStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpDeclareSimdStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpDeclareSimdStatement::pools.begin(); block != SgOmpDeclareSimdStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDeclareSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpDeclareSimdStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpDeclareSimdStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpDeclareSimdStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpDeclareSimdStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpDeclareSimdStatement::pools.begin(); block != SgOmpDeclareSimdStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDeclareSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpDeclareSimdStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpDeclareSimdStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpDeclareSimdStatement::clearMemoryPool() {
    SgOmpDeclareSimdStatement* tempPointer{nullptr};
    if (SgOmpDeclareSimdStatement::pools.empty() == false) {
        auto block{SgOmpDeclareSimdStatement::pools.begin()};
        SgOmpDeclareSimdStatement::next_node = reinterpret_cast<SgOmpDeclareSimdStatement*>(*block);
        for (; block != SgOmpDeclareSimdStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpDeclareSimdStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpDeclareSimdStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpDeclareSimdStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpDeclareSimdStatement::pool_size-1]);
        }
    }
}

void
SgOmpDeclareSimdStatement::deleteMemoryPool() {
    for (auto p: SgOmpDeclareSimdStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpDeclareSimdStatement::next_node = nullptr;
    SgOmpDeclareSimdStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpDeclareSimdStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeclareSimdStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDeclareSimdStatement)};
    for (size_t blockIndex = SgOmpDeclareSimdStatement::pools.size(); (blockIndex * SgOmpDeclareSimdStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeclareSimdStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDeclareSimdStatement) = %" PRIuPTR " SgOmpDeclareSimdStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDeclareSimdStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDeclareSimdStatement),SgOmpDeclareSimdStatement::pool_size);
        }
#endif

        SgOmpDeclareSimdStatement* pointer{static_cast<SgOmpDeclareSimdStatement*>(ROSE_MALLOC(SgOmpDeclareSimdStatement::pool_size * sizeof(SgOmpDeclareSimdStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpDeclareSimdStatement::pool_size * sizeof(SgOmpDeclareSimdStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpDeclareSimdStatement::pool_size * sizeof(SgOmpDeclareSimdStatement));
#endif
        SgOmpDeclareSimdStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpDeclareSimdStatement::pool_size * sizeof(SgOmpDeclareSimdStatement), V_SgOmpDeclareSimdStatement));

        if (SgOmpDeclareSimdStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpDeclareSimdStatement* blkptr{reinterpret_cast<SgOmpDeclareSimdStatement*>(SgOmpDeclareSimdStatement::pools[blockIndex-1])};
                blkptr[SgOmpDeclareSimdStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpDeclareSimdStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpDeclareSimdStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpDeclareSimdStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpDeclareSimdStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpDeclareSimdStatement::pool_size - 1};
    SgOmpDeclareSimdStatement* testPointer{reinterpret_cast<SgOmpDeclareSimdStatement*>(SgOmpDeclareSimdStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpDeclareSimdStatement::pool_size * (SgOmpDeclareSimdStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpDeclareSimdStatementStroageClass
 * from its counterpart of SgOmpDeclareSimdStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpDeclareSimdStatement::initializeStorageClassArray(SgOmpDeclareSimdStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpDeclareSimdStatement::pools.begin(); block != SgOmpDeclareSimdStatement::pools.end(); ++block) {
        SgOmpDeclareSimdStatement* pointer{reinterpret_cast<SgOmpDeclareSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpDeclareSimdStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 76506 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMicrosoftAttributeDeclaration that takes its corresponding StorageClass as parameter
 */
SgMicrosoftAttributeDeclaration::SgMicrosoftAttributeDeclaration(const SgMicrosoftAttributeDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 76516 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_attribute_string = SgName ( storageSource.storageOf_attribute_string);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMicrosoftAttributeDeclaration*
SgMicrosoftAttributeDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMicrosoftAttributeDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMicrosoftAttributeDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMicrosoftAttributeDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMicrosoftAttributeDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMicrosoftAttributeDeclaration)};
        unsigned long positionInPool{localIndex % SgMicrosoftAttributeDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMicrosoftAttributeDeclaration::pool_size};
        returnPointer = &(((SgMicrosoftAttributeDeclaration*)(SgMicrosoftAttributeDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMicrosoftAttributeDeclaration*
SgMicrosoftAttributeDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMicrosoftAttributeDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMicrosoftAttributeDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMicrosoftAttributeDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMicrosoftAttributeDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMicrosoftAttributeDeclaration)};
        unsigned long positionInPool{localIndex % SgMicrosoftAttributeDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMicrosoftAttributeDeclaration::pool_size};
        returnPointer = &(((SgMicrosoftAttributeDeclaration*)(SgMicrosoftAttributeDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMicrosoftAttributeDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMicrosoftAttributeDeclaration::pools.begin(); block != SgMicrosoftAttributeDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMicrosoftAttributeDeclaration*>(*block)};
        for (unsigned i = 0; i < SgMicrosoftAttributeDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMicrosoftAttributeDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMicrosoftAttributeDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMicrosoftAttributeDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgMicrosoftAttributeDeclaration::pools.begin(); block != SgMicrosoftAttributeDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMicrosoftAttributeDeclaration*>(*block)};
        for (unsigned i = 0; i < SgMicrosoftAttributeDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMicrosoftAttributeDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMicrosoftAttributeDeclaration::clearMemoryPool() {
    SgMicrosoftAttributeDeclaration* tempPointer{nullptr};
    if (SgMicrosoftAttributeDeclaration::pools.empty() == false) {
        auto block{SgMicrosoftAttributeDeclaration::pools.begin()};
        SgMicrosoftAttributeDeclaration::next_node = reinterpret_cast<SgMicrosoftAttributeDeclaration*>(*block);
        for (; block != SgMicrosoftAttributeDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMicrosoftAttributeDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMicrosoftAttributeDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMicrosoftAttributeDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMicrosoftAttributeDeclaration::pool_size-1]);
        }
    }
}

void
SgMicrosoftAttributeDeclaration::deleteMemoryPool() {
    for (auto p: SgMicrosoftAttributeDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgMicrosoftAttributeDeclaration::next_node = nullptr;
    SgMicrosoftAttributeDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMicrosoftAttributeDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMicrosoftAttributeDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMicrosoftAttributeDeclaration)};
    for (size_t blockIndex = SgMicrosoftAttributeDeclaration::pools.size(); (blockIndex * SgMicrosoftAttributeDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMicrosoftAttributeDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMicrosoftAttributeDeclaration) = %" PRIuPTR " SgMicrosoftAttributeDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMicrosoftAttributeDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMicrosoftAttributeDeclaration),SgMicrosoftAttributeDeclaration::pool_size);
        }
#endif

        SgMicrosoftAttributeDeclaration* pointer{static_cast<SgMicrosoftAttributeDeclaration*>(ROSE_MALLOC(SgMicrosoftAttributeDeclaration::pool_size * sizeof(SgMicrosoftAttributeDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMicrosoftAttributeDeclaration::pool_size * sizeof(SgMicrosoftAttributeDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMicrosoftAttributeDeclaration::pool_size * sizeof(SgMicrosoftAttributeDeclaration));
#endif
        SgMicrosoftAttributeDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMicrosoftAttributeDeclaration::pool_size * sizeof(SgMicrosoftAttributeDeclaration), V_SgMicrosoftAttributeDeclaration));

        if (SgMicrosoftAttributeDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMicrosoftAttributeDeclaration* blkptr{reinterpret_cast<SgMicrosoftAttributeDeclaration*>(SgMicrosoftAttributeDeclaration::pools[blockIndex-1])};
                blkptr[SgMicrosoftAttributeDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMicrosoftAttributeDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMicrosoftAttributeDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMicrosoftAttributeDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMicrosoftAttributeDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMicrosoftAttributeDeclaration::pool_size - 1};
    SgMicrosoftAttributeDeclaration* testPointer{reinterpret_cast<SgMicrosoftAttributeDeclaration*>(SgMicrosoftAttributeDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMicrosoftAttributeDeclaration::pool_size * (SgMicrosoftAttributeDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMicrosoftAttributeDeclarationStroageClass
 * from its counterpart of SgMicrosoftAttributeDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMicrosoftAttributeDeclaration::initializeStorageClassArray(SgMicrosoftAttributeDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMicrosoftAttributeDeclaration::pools.begin(); block != SgMicrosoftAttributeDeclaration::pools.end(); ++block) {
        SgMicrosoftAttributeDeclaration* pointer{reinterpret_cast<SgMicrosoftAttributeDeclaration*>(*block)};
        for (unsigned i = 0; i < SgMicrosoftAttributeDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 76734 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialCompoolStatement that takes its corresponding StorageClass as parameter
 */
SgJovialCompoolStatement::SgJovialCompoolStatement(const SgJovialCompoolStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 76744 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialCompoolStatement*
SgJovialCompoolStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialCompoolStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialCompoolStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialCompoolStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialCompoolStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialCompoolStatement)};
        unsigned long positionInPool{localIndex % SgJovialCompoolStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialCompoolStatement::pool_size};
        returnPointer = &(((SgJovialCompoolStatement*)(SgJovialCompoolStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialCompoolStatement*
SgJovialCompoolStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialCompoolStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialCompoolStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialCompoolStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialCompoolStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialCompoolStatement)};
        unsigned long positionInPool{localIndex % SgJovialCompoolStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialCompoolStatement::pool_size};
        returnPointer = &(((SgJovialCompoolStatement*)(SgJovialCompoolStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialCompoolStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialCompoolStatement::pools.begin(); block != SgJovialCompoolStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialCompoolStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialCompoolStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialCompoolStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialCompoolStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialCompoolStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialCompoolStatement::pools.begin(); block != SgJovialCompoolStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialCompoolStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialCompoolStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialCompoolStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialCompoolStatement::clearMemoryPool() {
    SgJovialCompoolStatement* tempPointer{nullptr};
    if (SgJovialCompoolStatement::pools.empty() == false) {
        auto block{SgJovialCompoolStatement::pools.begin()};
        SgJovialCompoolStatement::next_node = reinterpret_cast<SgJovialCompoolStatement*>(*block);
        for (; block != SgJovialCompoolStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialCompoolStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialCompoolStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialCompoolStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialCompoolStatement::pool_size-1]);
        }
    }
}

void
SgJovialCompoolStatement::deleteMemoryPool() {
    for (auto p: SgJovialCompoolStatement::pools) {
        ROSE_FREE(p);
    }
    SgJovialCompoolStatement::next_node = nullptr;
    SgJovialCompoolStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialCompoolStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialCompoolStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialCompoolStatement)};
    for (size_t blockIndex = SgJovialCompoolStatement::pools.size(); (blockIndex * SgJovialCompoolStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialCompoolStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialCompoolStatement) = %" PRIuPTR " SgJovialCompoolStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialCompoolStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialCompoolStatement),SgJovialCompoolStatement::pool_size);
        }
#endif

        SgJovialCompoolStatement* pointer{static_cast<SgJovialCompoolStatement*>(ROSE_MALLOC(SgJovialCompoolStatement::pool_size * sizeof(SgJovialCompoolStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialCompoolStatement::pool_size * sizeof(SgJovialCompoolStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialCompoolStatement::pool_size * sizeof(SgJovialCompoolStatement));
#endif
        SgJovialCompoolStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialCompoolStatement::pool_size * sizeof(SgJovialCompoolStatement), V_SgJovialCompoolStatement));

        if (SgJovialCompoolStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialCompoolStatement* blkptr{reinterpret_cast<SgJovialCompoolStatement*>(SgJovialCompoolStatement::pools[blockIndex-1])};
                blkptr[SgJovialCompoolStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialCompoolStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialCompoolStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialCompoolStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialCompoolStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialCompoolStatement::pool_size - 1};
    SgJovialCompoolStatement* testPointer{reinterpret_cast<SgJovialCompoolStatement*>(SgJovialCompoolStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialCompoolStatement::pool_size * (SgJovialCompoolStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialCompoolStatementStroageClass
 * from its counterpart of SgJovialCompoolStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialCompoolStatement::initializeStorageClassArray(SgJovialCompoolStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialCompoolStatement::pools.begin(); block != SgJovialCompoolStatement::pools.end(); ++block) {
        SgJovialCompoolStatement* pointer{reinterpret_cast<SgJovialCompoolStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialCompoolStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 76962 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialDirectiveStatement that takes its corresponding StorageClass as parameter
 */
SgJovialDirectiveStatement::SgJovialDirectiveStatement(const SgJovialDirectiveStatementStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 76972 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_content_string = storageSource.storageOf_content_string.rebuildDataStoredInEasyStorageClass();
    p_directive_type = storageSource.storageOf_directive_type;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialDirectiveStatement*
SgJovialDirectiveStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgJovialDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialDirectiveStatement::pool_size};
        returnPointer = &(((SgJovialDirectiveStatement*)(SgJovialDirectiveStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialDirectiveStatement*
SgJovialDirectiveStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialDirectiveStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialDirectiveStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialDirectiveStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialDirectiveStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialDirectiveStatement)};
        unsigned long positionInPool{localIndex % SgJovialDirectiveStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialDirectiveStatement::pool_size};
        returnPointer = &(((SgJovialDirectiveStatement*)(SgJovialDirectiveStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialDirectiveStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialDirectiveStatement::pools.begin(); block != SgJovialDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialDirectiveStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialDirectiveStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialDirectiveStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialDirectiveStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialDirectiveStatement::pools.begin(); block != SgJovialDirectiveStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialDirectiveStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialDirectiveStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialDirectiveStatement::clearMemoryPool() {
    SgJovialDirectiveStatement* tempPointer{nullptr};
    if (SgJovialDirectiveStatement::pools.empty() == false) {
        auto block{SgJovialDirectiveStatement::pools.begin()};
        SgJovialDirectiveStatement::next_node = reinterpret_cast<SgJovialDirectiveStatement*>(*block);
        for (; block != SgJovialDirectiveStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialDirectiveStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialDirectiveStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialDirectiveStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialDirectiveStatement::pool_size-1]);
        }
    }
}

void
SgJovialDirectiveStatement::deleteMemoryPool() {
    for (auto p: SgJovialDirectiveStatement::pools) {
        ROSE_FREE(p);
    }
    SgJovialDirectiveStatement::next_node = nullptr;
    SgJovialDirectiveStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialDirectiveStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDirectiveStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialDirectiveStatement)};
    for (size_t blockIndex = SgJovialDirectiveStatement::pools.size(); (blockIndex * SgJovialDirectiveStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDirectiveStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialDirectiveStatement) = %" PRIuPTR " SgJovialDirectiveStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDirectiveStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialDirectiveStatement),SgJovialDirectiveStatement::pool_size);
        }
#endif

        SgJovialDirectiveStatement* pointer{static_cast<SgJovialDirectiveStatement*>(ROSE_MALLOC(SgJovialDirectiveStatement::pool_size * sizeof(SgJovialDirectiveStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialDirectiveStatement::pool_size * sizeof(SgJovialDirectiveStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialDirectiveStatement::pool_size * sizeof(SgJovialDirectiveStatement));
#endif
        SgJovialDirectiveStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialDirectiveStatement::pool_size * sizeof(SgJovialDirectiveStatement), V_SgJovialDirectiveStatement));

        if (SgJovialDirectiveStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialDirectiveStatement* blkptr{reinterpret_cast<SgJovialDirectiveStatement*>(SgJovialDirectiveStatement::pools[blockIndex-1])};
                blkptr[SgJovialDirectiveStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialDirectiveStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialDirectiveStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialDirectiveStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialDirectiveStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialDirectiveStatement::pool_size - 1};
    SgJovialDirectiveStatement* testPointer{reinterpret_cast<SgJovialDirectiveStatement*>(SgJovialDirectiveStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialDirectiveStatement::pool_size * (SgJovialDirectiveStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialDirectiveStatementStroageClass
 * from its counterpart of SgJovialDirectiveStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialDirectiveStatement::initializeStorageClassArray(SgJovialDirectiveStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialDirectiveStatement::pools.begin(); block != SgJovialDirectiveStatement::pools.end(); ++block) {
        SgJovialDirectiveStatement* pointer{reinterpret_cast<SgJovialDirectiveStatement*>(*block)};
        for (unsigned i = 0; i < SgJovialDirectiveStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 77191 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialDefineDeclaration that takes its corresponding StorageClass as parameter
 */
SgJovialDefineDeclaration::SgJovialDefineDeclaration(const SgJovialDefineDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 77201 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_define_string = storageSource.storageOf_define_string.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialDefineDeclaration*
SgJovialDefineDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialDefineDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialDefineDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialDefineDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialDefineDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDefineDeclaration)};
        unsigned long positionInPool{localIndex % SgJovialDefineDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialDefineDeclaration::pool_size};
        returnPointer = &(((SgJovialDefineDeclaration*)(SgJovialDefineDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialDefineDeclaration*
SgJovialDefineDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialDefineDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialDefineDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialDefineDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialDefineDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialDefineDeclaration)};
        unsigned long positionInPool{localIndex % SgJovialDefineDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialDefineDeclaration::pool_size};
        returnPointer = &(((SgJovialDefineDeclaration*)(SgJovialDefineDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialDefineDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialDefineDeclaration::pools.begin(); block != SgJovialDefineDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialDefineDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialDefineDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialDefineDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialDefineDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialDefineDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialDefineDeclaration::pools.begin(); block != SgJovialDefineDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialDefineDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialDefineDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialDefineDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialDefineDeclaration::clearMemoryPool() {
    SgJovialDefineDeclaration* tempPointer{nullptr};
    if (SgJovialDefineDeclaration::pools.empty() == false) {
        auto block{SgJovialDefineDeclaration::pools.begin()};
        SgJovialDefineDeclaration::next_node = reinterpret_cast<SgJovialDefineDeclaration*>(*block);
        for (; block != SgJovialDefineDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialDefineDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialDefineDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialDefineDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialDefineDeclaration::pool_size-1]);
        }
    }
}

void
SgJovialDefineDeclaration::deleteMemoryPool() {
    for (auto p: SgJovialDefineDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgJovialDefineDeclaration::next_node = nullptr;
    SgJovialDefineDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialDefineDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDefineDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialDefineDeclaration)};
    for (size_t blockIndex = SgJovialDefineDeclaration::pools.size(); (blockIndex * SgJovialDefineDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDefineDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialDefineDeclaration) = %" PRIuPTR " SgJovialDefineDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialDefineDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialDefineDeclaration),SgJovialDefineDeclaration::pool_size);
        }
#endif

        SgJovialDefineDeclaration* pointer{static_cast<SgJovialDefineDeclaration*>(ROSE_MALLOC(SgJovialDefineDeclaration::pool_size * sizeof(SgJovialDefineDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialDefineDeclaration::pool_size * sizeof(SgJovialDefineDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialDefineDeclaration::pool_size * sizeof(SgJovialDefineDeclaration));
#endif
        SgJovialDefineDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialDefineDeclaration::pool_size * sizeof(SgJovialDefineDeclaration), V_SgJovialDefineDeclaration));

        if (SgJovialDefineDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialDefineDeclaration* blkptr{reinterpret_cast<SgJovialDefineDeclaration*>(SgJovialDefineDeclaration::pools[blockIndex-1])};
                blkptr[SgJovialDefineDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialDefineDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialDefineDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialDefineDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialDefineDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialDefineDeclaration::pool_size - 1};
    SgJovialDefineDeclaration* testPointer{reinterpret_cast<SgJovialDefineDeclaration*>(SgJovialDefineDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialDefineDeclaration::pool_size * (SgJovialDefineDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialDefineDeclarationStroageClass
 * from its counterpart of SgJovialDefineDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialDefineDeclaration::initializeStorageClassArray(SgJovialDefineDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialDefineDeclaration::pools.begin(); block != SgJovialDefineDeclaration::pools.end(); ++block) {
        SgJovialDefineDeclaration* pointer{reinterpret_cast<SgJovialDefineDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialDefineDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 77419 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialLabelDeclaration that takes its corresponding StorageClass as parameter
 */
SgJovialLabelDeclaration::SgJovialLabelDeclaration(const SgJovialLabelDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 77429 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_label = storageSource.storageOf_label.rebuildDataStoredInEasyStorageClass();
    p_label_type = storageSource.storageOf_label_type;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialLabelDeclaration*
SgJovialLabelDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialLabelDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialLabelDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialLabelDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialLabelDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialLabelDeclaration)};
        unsigned long positionInPool{localIndex % SgJovialLabelDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialLabelDeclaration::pool_size};
        returnPointer = &(((SgJovialLabelDeclaration*)(SgJovialLabelDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialLabelDeclaration*
SgJovialLabelDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialLabelDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialLabelDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialLabelDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialLabelDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialLabelDeclaration)};
        unsigned long positionInPool{localIndex % SgJovialLabelDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialLabelDeclaration::pool_size};
        returnPointer = &(((SgJovialLabelDeclaration*)(SgJovialLabelDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialLabelDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialLabelDeclaration::pools.begin(); block != SgJovialLabelDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialLabelDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialLabelDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialLabelDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialLabelDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialLabelDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialLabelDeclaration::pools.begin(); block != SgJovialLabelDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialLabelDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialLabelDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialLabelDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialLabelDeclaration::clearMemoryPool() {
    SgJovialLabelDeclaration* tempPointer{nullptr};
    if (SgJovialLabelDeclaration::pools.empty() == false) {
        auto block{SgJovialLabelDeclaration::pools.begin()};
        SgJovialLabelDeclaration::next_node = reinterpret_cast<SgJovialLabelDeclaration*>(*block);
        for (; block != SgJovialLabelDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialLabelDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialLabelDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialLabelDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialLabelDeclaration::pool_size-1]);
        }
    }
}

void
SgJovialLabelDeclaration::deleteMemoryPool() {
    for (auto p: SgJovialLabelDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgJovialLabelDeclaration::next_node = nullptr;
    SgJovialLabelDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialLabelDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialLabelDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialLabelDeclaration)};
    for (size_t blockIndex = SgJovialLabelDeclaration::pools.size(); (blockIndex * SgJovialLabelDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialLabelDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialLabelDeclaration) = %" PRIuPTR " SgJovialLabelDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialLabelDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialLabelDeclaration),SgJovialLabelDeclaration::pool_size);
        }
#endif

        SgJovialLabelDeclaration* pointer{static_cast<SgJovialLabelDeclaration*>(ROSE_MALLOC(SgJovialLabelDeclaration::pool_size * sizeof(SgJovialLabelDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialLabelDeclaration::pool_size * sizeof(SgJovialLabelDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialLabelDeclaration::pool_size * sizeof(SgJovialLabelDeclaration));
#endif
        SgJovialLabelDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialLabelDeclaration::pool_size * sizeof(SgJovialLabelDeclaration), V_SgJovialLabelDeclaration));

        if (SgJovialLabelDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialLabelDeclaration* blkptr{reinterpret_cast<SgJovialLabelDeclaration*>(SgJovialLabelDeclaration::pools[blockIndex-1])};
                blkptr[SgJovialLabelDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialLabelDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialLabelDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialLabelDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialLabelDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialLabelDeclaration::pool_size - 1};
    SgJovialLabelDeclaration* testPointer{reinterpret_cast<SgJovialLabelDeclaration*>(SgJovialLabelDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialLabelDeclaration::pool_size * (SgJovialLabelDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialLabelDeclarationStroageClass
 * from its counterpart of SgJovialLabelDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialLabelDeclaration::initializeStorageClassArray(SgJovialLabelDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialLabelDeclaration::pools.begin(); block != SgJovialLabelDeclaration::pools.end(); ++block) {
        SgJovialLabelDeclaration* pointer{reinterpret_cast<SgJovialLabelDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialLabelDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 77648 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialOverlayDeclaration that takes its corresponding StorageClass as parameter
 */
SgJovialOverlayDeclaration::SgJovialOverlayDeclaration(const SgJovialOverlayDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 77658 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_address = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_address));
     p_overlay = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_overlay));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialOverlayDeclaration*
SgJovialOverlayDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialOverlayDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialOverlayDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialOverlayDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialOverlayDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialOverlayDeclaration)};
        unsigned long positionInPool{localIndex % SgJovialOverlayDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialOverlayDeclaration::pool_size};
        returnPointer = &(((SgJovialOverlayDeclaration*)(SgJovialOverlayDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialOverlayDeclaration*
SgJovialOverlayDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialOverlayDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialOverlayDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialOverlayDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialOverlayDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialOverlayDeclaration)};
        unsigned long positionInPool{localIndex % SgJovialOverlayDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialOverlayDeclaration::pool_size};
        returnPointer = &(((SgJovialOverlayDeclaration*)(SgJovialOverlayDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialOverlayDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialOverlayDeclaration::pools.begin(); block != SgJovialOverlayDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialOverlayDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialOverlayDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialOverlayDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialOverlayDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialOverlayDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialOverlayDeclaration::pools.begin(); block != SgJovialOverlayDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialOverlayDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialOverlayDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialOverlayDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialOverlayDeclaration::clearMemoryPool() {
    SgJovialOverlayDeclaration* tempPointer{nullptr};
    if (SgJovialOverlayDeclaration::pools.empty() == false) {
        auto block{SgJovialOverlayDeclaration::pools.begin()};
        SgJovialOverlayDeclaration::next_node = reinterpret_cast<SgJovialOverlayDeclaration*>(*block);
        for (; block != SgJovialOverlayDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialOverlayDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialOverlayDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialOverlayDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialOverlayDeclaration::pool_size-1]);
        }
    }
}

void
SgJovialOverlayDeclaration::deleteMemoryPool() {
    for (auto p: SgJovialOverlayDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgJovialOverlayDeclaration::next_node = nullptr;
    SgJovialOverlayDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialOverlayDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialOverlayDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialOverlayDeclaration)};
    for (size_t blockIndex = SgJovialOverlayDeclaration::pools.size(); (blockIndex * SgJovialOverlayDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialOverlayDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialOverlayDeclaration) = %" PRIuPTR " SgJovialOverlayDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialOverlayDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialOverlayDeclaration),SgJovialOverlayDeclaration::pool_size);
        }
#endif

        SgJovialOverlayDeclaration* pointer{static_cast<SgJovialOverlayDeclaration*>(ROSE_MALLOC(SgJovialOverlayDeclaration::pool_size * sizeof(SgJovialOverlayDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialOverlayDeclaration::pool_size * sizeof(SgJovialOverlayDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialOverlayDeclaration::pool_size * sizeof(SgJovialOverlayDeclaration));
#endif
        SgJovialOverlayDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialOverlayDeclaration::pool_size * sizeof(SgJovialOverlayDeclaration), V_SgJovialOverlayDeclaration));

        if (SgJovialOverlayDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialOverlayDeclaration* blkptr{reinterpret_cast<SgJovialOverlayDeclaration*>(SgJovialOverlayDeclaration::pools[blockIndex-1])};
                blkptr[SgJovialOverlayDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialOverlayDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialOverlayDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialOverlayDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialOverlayDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialOverlayDeclaration::pool_size - 1};
    SgJovialOverlayDeclaration* testPointer{reinterpret_cast<SgJovialOverlayDeclaration*>(SgJovialOverlayDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialOverlayDeclaration::pool_size * (SgJovialOverlayDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialOverlayDeclarationStroageClass
 * from its counterpart of SgJovialOverlayDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialOverlayDeclaration::initializeStorageClassArray(SgJovialOverlayDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialOverlayDeclaration::pools.begin(); block != SgJovialOverlayDeclaration::pools.end(); ++block) {
        SgJovialOverlayDeclaration* pointer{reinterpret_cast<SgJovialOverlayDeclaration*>(*block)};
        for (unsigned i = 0; i < SgJovialOverlayDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 77877 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNonrealDecl that takes its corresponding StorageClass as parameter
 */
SgNonrealDecl::SgNonrealDecl(const SgNonrealDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 77887 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_nonreal_decl_scope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_decl_scope));
     p_type = (SgNonrealType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
    p_template_parameter_position = storageSource.storageOf_template_parameter_position;
    p_template_parameter_depth = storageSource.storageOf_template_parameter_depth;
     p_templateDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_templateDeclaration));
    p_tpl_args = storageSource.storageOf_tpl_args.rebuildDataStoredInEasyStorageClass();
    SgTemplateArgumentPtrList::iterator i_tpl_args = p_tpl_args.begin();
    for ( ; i_tpl_args != p_tpl_args.end(); ++i_tpl_args) {
        (*i_tpl_args) = (SgTemplateArgumentPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_tpl_args)) );
    }
    p_tpl_params = storageSource.storageOf_tpl_params.rebuildDataStoredInEasyStorageClass();
    SgTemplateParameterPtrList::iterator i_tpl_params = p_tpl_params.begin();
    for ( ; i_tpl_params != p_tpl_params.end(); ++i_tpl_params) {
        (*i_tpl_params) = (SgTemplateParameterPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_tpl_params)) );
    }
    p_is_class_member = storageSource.storageOf_is_class_member;
    p_is_template_param = storageSource.storageOf_is_template_param;
    p_is_template_template_param = storageSource.storageOf_is_template_template_param;
    p_is_nonreal_template = storageSource.storageOf_is_nonreal_template;
    p_is_nonreal_function = storageSource.storageOf_is_nonreal_function;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNonrealDecl*
SgNonrealDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNonrealDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealDecl)};
        unsigned long positionInPool{localIndex % SgNonrealDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealDecl::pool_size};
        returnPointer = &(((SgNonrealDecl*)(SgNonrealDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNonrealDecl*
SgNonrealDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNonrealDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNonrealDecl)};
        unsigned long positionInPool{localIndex % SgNonrealDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealDecl::pool_size};
        returnPointer = &(((SgNonrealDecl*)(SgNonrealDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNonrealDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNonrealDecl::pools.begin(); block != SgNonrealDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealDecl*>(*block)};
        for (unsigned i = 0; i < SgNonrealDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNonrealDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNonrealDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNonrealDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgNonrealDecl::pools.begin(); block != SgNonrealDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealDecl*>(*block)};
        for (unsigned i = 0; i < SgNonrealDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNonrealDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNonrealDecl::clearMemoryPool() {
    SgNonrealDecl* tempPointer{nullptr};
    if (SgNonrealDecl::pools.empty() == false) {
        auto block{SgNonrealDecl::pools.begin()};
        SgNonrealDecl::next_node = reinterpret_cast<SgNonrealDecl*>(*block);
        for (; block != SgNonrealDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNonrealDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNonrealDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNonrealDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNonrealDecl::pool_size-1]);
        }
    }
}

void
SgNonrealDecl::deleteMemoryPool() {
    for (auto p: SgNonrealDecl::pools) {
        ROSE_FREE(p);
    }
    SgNonrealDecl::next_node = nullptr;
    SgNonrealDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNonrealDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealDecl)};
    for (size_t blockIndex = SgNonrealDecl::pools.size(); (blockIndex * SgNonrealDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealDecl) = %" PRIuPTR " SgNonrealDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealDecl),SgNonrealDecl::pool_size);
        }
#endif

        SgNonrealDecl* pointer{static_cast<SgNonrealDecl*>(ROSE_MALLOC(SgNonrealDecl::pool_size * sizeof(SgNonrealDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNonrealDecl::pool_size * sizeof(SgNonrealDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNonrealDecl::pool_size * sizeof(SgNonrealDecl));
#endif
        SgNonrealDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNonrealDecl::pool_size * sizeof(SgNonrealDecl), V_SgNonrealDecl));

        if (SgNonrealDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNonrealDecl* blkptr{reinterpret_cast<SgNonrealDecl*>(SgNonrealDecl::pools[blockIndex-1])};
                blkptr[SgNonrealDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNonrealDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNonrealDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNonrealDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNonrealDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNonrealDecl::pool_size - 1};
    SgNonrealDecl* testPointer{reinterpret_cast<SgNonrealDecl*>(SgNonrealDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNonrealDecl::pool_size * (SgNonrealDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNonrealDeclStroageClass
 * from its counterpart of SgNonrealDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNonrealDecl::initializeStorageClassArray(SgNonrealDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNonrealDecl::pools.begin(); block != SgNonrealDecl::pools.end(); ++block) {
        SgNonrealDecl* pointer{reinterpret_cast<SgNonrealDecl*>(*block)};
        for (unsigned i = 0; i < SgNonrealDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 78125 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEmptyDeclaration that takes its corresponding StorageClass as parameter
 */
SgEmptyDeclaration::SgEmptyDeclaration(const SgEmptyDeclarationStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 78135 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEmptyDeclaration*
SgEmptyDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEmptyDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEmptyDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEmptyDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEmptyDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDeclaration)};
        unsigned long positionInPool{localIndex % SgEmptyDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEmptyDeclaration::pool_size};
        returnPointer = &(((SgEmptyDeclaration*)(SgEmptyDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEmptyDeclaration*
SgEmptyDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEmptyDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEmptyDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEmptyDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEmptyDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEmptyDeclaration)};
        unsigned long positionInPool{localIndex % SgEmptyDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEmptyDeclaration::pool_size};
        returnPointer = &(((SgEmptyDeclaration*)(SgEmptyDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEmptyDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEmptyDeclaration::pools.begin(); block != SgEmptyDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEmptyDeclaration*>(*block)};
        for (unsigned i = 0; i < SgEmptyDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEmptyDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEmptyDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEmptyDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgEmptyDeclaration::pools.begin(); block != SgEmptyDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEmptyDeclaration*>(*block)};
        for (unsigned i = 0; i < SgEmptyDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEmptyDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEmptyDeclaration::clearMemoryPool() {
    SgEmptyDeclaration* tempPointer{nullptr};
    if (SgEmptyDeclaration::pools.empty() == false) {
        auto block{SgEmptyDeclaration::pools.begin()};
        SgEmptyDeclaration::next_node = reinterpret_cast<SgEmptyDeclaration*>(*block);
        for (; block != SgEmptyDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEmptyDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEmptyDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEmptyDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEmptyDeclaration::pool_size-1]);
        }
    }
}

void
SgEmptyDeclaration::deleteMemoryPool() {
    for (auto p: SgEmptyDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgEmptyDeclaration::next_node = nullptr;
    SgEmptyDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEmptyDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEmptyDeclaration)};
    for (size_t blockIndex = SgEmptyDeclaration::pools.size(); (blockIndex * SgEmptyDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEmptyDeclaration) = %" PRIuPTR " SgEmptyDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEmptyDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEmptyDeclaration),SgEmptyDeclaration::pool_size);
        }
#endif

        SgEmptyDeclaration* pointer{static_cast<SgEmptyDeclaration*>(ROSE_MALLOC(SgEmptyDeclaration::pool_size * sizeof(SgEmptyDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEmptyDeclaration::pool_size * sizeof(SgEmptyDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEmptyDeclaration::pool_size * sizeof(SgEmptyDeclaration));
#endif
        SgEmptyDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEmptyDeclaration::pool_size * sizeof(SgEmptyDeclaration), V_SgEmptyDeclaration));

        if (SgEmptyDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEmptyDeclaration* blkptr{reinterpret_cast<SgEmptyDeclaration*>(SgEmptyDeclaration::pools[blockIndex-1])};
                blkptr[SgEmptyDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEmptyDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEmptyDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEmptyDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEmptyDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEmptyDeclaration::pool_size - 1};
    SgEmptyDeclaration* testPointer{reinterpret_cast<SgEmptyDeclaration*>(SgEmptyDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEmptyDeclaration::pool_size * (SgEmptyDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEmptyDeclarationStroageClass
 * from its counterpart of SgEmptyDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEmptyDeclaration::initializeStorageClassArray(SgEmptyDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEmptyDeclaration::pools.begin(); block != SgEmptyDeclaration::pools.end(); ++block) {
        SgEmptyDeclaration* pointer{reinterpret_cast<SgEmptyDeclaration*>(*block)};
        for (unsigned i = 0; i < SgEmptyDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 78352 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaPackageBodyDecl that takes its corresponding StorageClass as parameter
 */
SgAdaPackageBodyDecl::SgAdaPackageBodyDecl(const SgAdaPackageBodyDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 78362 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgAdaPackageBody*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
     p_spec = (SgAdaPackageSpecDecl*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_spec));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaPackageBodyDecl*
SgAdaPackageBodyDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaPackageBodyDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageBodyDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageBodyDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageBodyDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBodyDecl)};
        unsigned long positionInPool{localIndex % SgAdaPackageBodyDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageBodyDecl::pool_size};
        returnPointer = &(((SgAdaPackageBodyDecl*)(SgAdaPackageBodyDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaPackageBodyDecl*
SgAdaPackageBodyDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaPackageBodyDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageBodyDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageBodyDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageBodyDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaPackageBodyDecl)};
        unsigned long positionInPool{localIndex % SgAdaPackageBodyDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageBodyDecl::pool_size};
        returnPointer = &(((SgAdaPackageBodyDecl*)(SgAdaPackageBodyDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaPackageBodyDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaPackageBodyDecl::pools.begin(); block != SgAdaPackageBodyDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageBodyDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaPackageBodyDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaPackageBodyDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaPackageBodyDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaPackageBodyDecl::pools.begin(); block != SgAdaPackageBodyDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageBodyDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaPackageBodyDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaPackageBodyDecl::clearMemoryPool() {
    SgAdaPackageBodyDecl* tempPointer{nullptr};
    if (SgAdaPackageBodyDecl::pools.empty() == false) {
        auto block{SgAdaPackageBodyDecl::pools.begin()};
        SgAdaPackageBodyDecl::next_node = reinterpret_cast<SgAdaPackageBodyDecl*>(*block);
        for (; block != SgAdaPackageBodyDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaPackageBodyDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaPackageBodyDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaPackageBodyDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaPackageBodyDecl::pool_size-1]);
        }
    }
}

void
SgAdaPackageBodyDecl::deleteMemoryPool() {
    for (auto p: SgAdaPackageBodyDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaPackageBodyDecl::next_node = nullptr;
    SgAdaPackageBodyDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaPackageBodyDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBodyDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageBodyDecl)};
    for (size_t blockIndex = SgAdaPackageBodyDecl::pools.size(); (blockIndex * SgAdaPackageBodyDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBodyDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageBodyDecl) = %" PRIuPTR " SgAdaPackageBodyDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageBodyDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageBodyDecl),SgAdaPackageBodyDecl::pool_size);
        }
#endif

        SgAdaPackageBodyDecl* pointer{static_cast<SgAdaPackageBodyDecl*>(ROSE_MALLOC(SgAdaPackageBodyDecl::pool_size * sizeof(SgAdaPackageBodyDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaPackageBodyDecl::pool_size * sizeof(SgAdaPackageBodyDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaPackageBodyDecl::pool_size * sizeof(SgAdaPackageBodyDecl));
#endif
        SgAdaPackageBodyDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaPackageBodyDecl::pool_size * sizeof(SgAdaPackageBodyDecl), V_SgAdaPackageBodyDecl));

        if (SgAdaPackageBodyDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaPackageBodyDecl* blkptr{reinterpret_cast<SgAdaPackageBodyDecl*>(SgAdaPackageBodyDecl::pools[blockIndex-1])};
                blkptr[SgAdaPackageBodyDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaPackageBodyDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaPackageBodyDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaPackageBodyDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaPackageBodyDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaPackageBodyDecl::pool_size - 1};
    SgAdaPackageBodyDecl* testPointer{reinterpret_cast<SgAdaPackageBodyDecl*>(SgAdaPackageBodyDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaPackageBodyDecl::pool_size * (SgAdaPackageBodyDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaPackageBodyDeclStroageClass
 * from its counterpart of SgAdaPackageBodyDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaPackageBodyDecl::initializeStorageClassArray(SgAdaPackageBodyDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaPackageBodyDecl::pools.begin(); block != SgAdaPackageBodyDecl::pools.end(); ++block) {
        SgAdaPackageBodyDecl* pointer{reinterpret_cast<SgAdaPackageBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageBodyDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 78583 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaPackageSpecDecl that takes its corresponding StorageClass as parameter
 */
SgAdaPackageSpecDecl::SgAdaPackageSpecDecl(const SgAdaPackageSpecDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 78593 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgAdaPackageSpec*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
     p_body = (SgAdaPackageBodyDecl*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaPackageSpecDecl*
SgAdaPackageSpecDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaPackageSpecDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSpecDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSpecDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSpecDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpecDecl)};
        unsigned long positionInPool{localIndex % SgAdaPackageSpecDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageSpecDecl::pool_size};
        returnPointer = &(((SgAdaPackageSpecDecl*)(SgAdaPackageSpecDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaPackageSpecDecl*
SgAdaPackageSpecDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaPackageSpecDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSpecDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSpecDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSpecDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaPackageSpecDecl)};
        unsigned long positionInPool{localIndex % SgAdaPackageSpecDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageSpecDecl::pool_size};
        returnPointer = &(((SgAdaPackageSpecDecl*)(SgAdaPackageSpecDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaPackageSpecDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaPackageSpecDecl::pools.begin(); block != SgAdaPackageSpecDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSpecDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaPackageSpecDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaPackageSpecDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaPackageSpecDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaPackageSpecDecl::pools.begin(); block != SgAdaPackageSpecDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSpecDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaPackageSpecDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaPackageSpecDecl::clearMemoryPool() {
    SgAdaPackageSpecDecl* tempPointer{nullptr};
    if (SgAdaPackageSpecDecl::pools.empty() == false) {
        auto block{SgAdaPackageSpecDecl::pools.begin()};
        SgAdaPackageSpecDecl::next_node = reinterpret_cast<SgAdaPackageSpecDecl*>(*block);
        for (; block != SgAdaPackageSpecDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaPackageSpecDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaPackageSpecDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaPackageSpecDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaPackageSpecDecl::pool_size-1]);
        }
    }
}

void
SgAdaPackageSpecDecl::deleteMemoryPool() {
    for (auto p: SgAdaPackageSpecDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaPackageSpecDecl::next_node = nullptr;
    SgAdaPackageSpecDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaPackageSpecDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpecDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSpecDecl)};
    for (size_t blockIndex = SgAdaPackageSpecDecl::pools.size(); (blockIndex * SgAdaPackageSpecDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpecDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSpecDecl) = %" PRIuPTR " SgAdaPackageSpecDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSpecDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSpecDecl),SgAdaPackageSpecDecl::pool_size);
        }
#endif

        SgAdaPackageSpecDecl* pointer{static_cast<SgAdaPackageSpecDecl*>(ROSE_MALLOC(SgAdaPackageSpecDecl::pool_size * sizeof(SgAdaPackageSpecDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaPackageSpecDecl::pool_size * sizeof(SgAdaPackageSpecDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaPackageSpecDecl::pool_size * sizeof(SgAdaPackageSpecDecl));
#endif
        SgAdaPackageSpecDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaPackageSpecDecl::pool_size * sizeof(SgAdaPackageSpecDecl), V_SgAdaPackageSpecDecl));

        if (SgAdaPackageSpecDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaPackageSpecDecl* blkptr{reinterpret_cast<SgAdaPackageSpecDecl*>(SgAdaPackageSpecDecl::pools[blockIndex-1])};
                blkptr[SgAdaPackageSpecDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaPackageSpecDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaPackageSpecDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaPackageSpecDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaPackageSpecDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaPackageSpecDecl::pool_size - 1};
    SgAdaPackageSpecDecl* testPointer{reinterpret_cast<SgAdaPackageSpecDecl*>(SgAdaPackageSpecDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaPackageSpecDecl::pool_size * (SgAdaPackageSpecDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaPackageSpecDeclStroageClass
 * from its counterpart of SgAdaPackageSpecDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaPackageSpecDecl::initializeStorageClassArray(SgAdaPackageSpecDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaPackageSpecDecl::pools.begin(); block != SgAdaPackageSpecDecl::pools.end(); ++block) {
        SgAdaPackageSpecDecl* pointer{reinterpret_cast<SgAdaPackageSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSpecDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 78814 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaRenamingDecl that takes its corresponding StorageClass as parameter
 */
SgAdaRenamingDecl::SgAdaRenamingDecl(const SgAdaRenamingDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 78824 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_renamed = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_renamed));
     p_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaRenamingDecl*
SgAdaRenamingDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaRenamingDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingDecl)};
        unsigned long positionInPool{localIndex % SgAdaRenamingDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRenamingDecl::pool_size};
        returnPointer = &(((SgAdaRenamingDecl*)(SgAdaRenamingDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaRenamingDecl*
SgAdaRenamingDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaRenamingDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaRenamingDecl)};
        unsigned long positionInPool{localIndex % SgAdaRenamingDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRenamingDecl::pool_size};
        returnPointer = &(((SgAdaRenamingDecl*)(SgAdaRenamingDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaRenamingDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaRenamingDecl::pools.begin(); block != SgAdaRenamingDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRenamingDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaRenamingDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaRenamingDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaRenamingDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaRenamingDecl::pools.begin(); block != SgAdaRenamingDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRenamingDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaRenamingDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaRenamingDecl::clearMemoryPool() {
    SgAdaRenamingDecl* tempPointer{nullptr};
    if (SgAdaRenamingDecl::pools.empty() == false) {
        auto block{SgAdaRenamingDecl::pools.begin()};
        SgAdaRenamingDecl::next_node = reinterpret_cast<SgAdaRenamingDecl*>(*block);
        for (; block != SgAdaRenamingDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaRenamingDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaRenamingDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaRenamingDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaRenamingDecl::pool_size-1]);
        }
    }
}

void
SgAdaRenamingDecl::deleteMemoryPool() {
    for (auto p: SgAdaRenamingDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaRenamingDecl::next_node = nullptr;
    SgAdaRenamingDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaRenamingDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingDecl)};
    for (size_t blockIndex = SgAdaRenamingDecl::pools.size(); (blockIndex * SgAdaRenamingDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingDecl) = %" PRIuPTR " SgAdaRenamingDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingDecl),SgAdaRenamingDecl::pool_size);
        }
#endif

        SgAdaRenamingDecl* pointer{static_cast<SgAdaRenamingDecl*>(ROSE_MALLOC(SgAdaRenamingDecl::pool_size * sizeof(SgAdaRenamingDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaRenamingDecl::pool_size * sizeof(SgAdaRenamingDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaRenamingDecl::pool_size * sizeof(SgAdaRenamingDecl));
#endif
        SgAdaRenamingDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaRenamingDecl::pool_size * sizeof(SgAdaRenamingDecl), V_SgAdaRenamingDecl));

        if (SgAdaRenamingDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaRenamingDecl* blkptr{reinterpret_cast<SgAdaRenamingDecl*>(SgAdaRenamingDecl::pools[blockIndex-1])};
                blkptr[SgAdaRenamingDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaRenamingDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaRenamingDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaRenamingDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaRenamingDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaRenamingDecl::pool_size - 1};
    SgAdaRenamingDecl* testPointer{reinterpret_cast<SgAdaRenamingDecl*>(SgAdaRenamingDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaRenamingDecl::pool_size * (SgAdaRenamingDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaRenamingDeclStroageClass
 * from its counterpart of SgAdaRenamingDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaRenamingDecl::initializeStorageClassArray(SgAdaRenamingDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaRenamingDecl::pools.begin(); block != SgAdaRenamingDecl::pools.end(); ++block) {
        SgAdaRenamingDecl* pointer{reinterpret_cast<SgAdaRenamingDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 79044 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskSpecDecl that takes its corresponding StorageClass as parameter
 */
SgAdaTaskSpecDecl::SgAdaTaskSpecDecl(const SgAdaTaskSpecDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 79054 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgAdaTaskSpec*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_type = (SgAdaTaskType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskSpecDecl*
SgAdaTaskSpecDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskSpecDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSpecDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSpecDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSpecDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpecDecl)};
        unsigned long positionInPool{localIndex % SgAdaTaskSpecDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskSpecDecl::pool_size};
        returnPointer = &(((SgAdaTaskSpecDecl*)(SgAdaTaskSpecDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskSpecDecl*
SgAdaTaskSpecDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskSpecDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSpecDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSpecDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSpecDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskSpecDecl)};
        unsigned long positionInPool{localIndex % SgAdaTaskSpecDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskSpecDecl::pool_size};
        returnPointer = &(((SgAdaTaskSpecDecl*)(SgAdaTaskSpecDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskSpecDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskSpecDecl::pools.begin(); block != SgAdaTaskSpecDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSpecDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskSpecDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskSpecDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskSpecDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskSpecDecl::pools.begin(); block != SgAdaTaskSpecDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSpecDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskSpecDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskSpecDecl::clearMemoryPool() {
    SgAdaTaskSpecDecl* tempPointer{nullptr};
    if (SgAdaTaskSpecDecl::pools.empty() == false) {
        auto block{SgAdaTaskSpecDecl::pools.begin()};
        SgAdaTaskSpecDecl::next_node = reinterpret_cast<SgAdaTaskSpecDecl*>(*block);
        for (; block != SgAdaTaskSpecDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskSpecDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskSpecDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskSpecDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskSpecDecl::pool_size-1]);
        }
    }
}

void
SgAdaTaskSpecDecl::deleteMemoryPool() {
    for (auto p: SgAdaTaskSpecDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskSpecDecl::next_node = nullptr;
    SgAdaTaskSpecDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskSpecDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpecDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSpecDecl)};
    for (size_t blockIndex = SgAdaTaskSpecDecl::pools.size(); (blockIndex * SgAdaTaskSpecDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpecDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSpecDecl) = %" PRIuPTR " SgAdaTaskSpecDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSpecDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSpecDecl),SgAdaTaskSpecDecl::pool_size);
        }
#endif

        SgAdaTaskSpecDecl* pointer{static_cast<SgAdaTaskSpecDecl*>(ROSE_MALLOC(SgAdaTaskSpecDecl::pool_size * sizeof(SgAdaTaskSpecDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskSpecDecl::pool_size * sizeof(SgAdaTaskSpecDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskSpecDecl::pool_size * sizeof(SgAdaTaskSpecDecl));
#endif
        SgAdaTaskSpecDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskSpecDecl::pool_size * sizeof(SgAdaTaskSpecDecl), V_SgAdaTaskSpecDecl));

        if (SgAdaTaskSpecDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskSpecDecl* blkptr{reinterpret_cast<SgAdaTaskSpecDecl*>(SgAdaTaskSpecDecl::pools[blockIndex-1])};
                blkptr[SgAdaTaskSpecDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskSpecDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskSpecDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskSpecDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskSpecDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskSpecDecl::pool_size - 1};
    SgAdaTaskSpecDecl* testPointer{reinterpret_cast<SgAdaTaskSpecDecl*>(SgAdaTaskSpecDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskSpecDecl::pool_size * (SgAdaTaskSpecDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskSpecDeclStroageClass
 * from its counterpart of SgAdaTaskSpecDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskSpecDecl::initializeStorageClassArray(SgAdaTaskSpecDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskSpecDecl::pools.begin(); block != SgAdaTaskSpecDecl::pools.end(); ++block) {
        SgAdaTaskSpecDecl* pointer{reinterpret_cast<SgAdaTaskSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSpecDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 79274 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskBodyDecl that takes its corresponding StorageClass as parameter
 */
SgAdaTaskBodyDecl::SgAdaTaskBodyDecl(const SgAdaTaskBodyDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 79284 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_specificationDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_specificationDeclaration));
     p_definition = (SgAdaTaskBody*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskBodyDecl*
SgAdaTaskBodyDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskBodyDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskBodyDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskBodyDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskBodyDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBodyDecl)};
        unsigned long positionInPool{localIndex % SgAdaTaskBodyDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskBodyDecl::pool_size};
        returnPointer = &(((SgAdaTaskBodyDecl*)(SgAdaTaskBodyDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskBodyDecl*
SgAdaTaskBodyDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskBodyDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskBodyDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskBodyDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskBodyDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskBodyDecl)};
        unsigned long positionInPool{localIndex % SgAdaTaskBodyDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskBodyDecl::pool_size};
        returnPointer = &(((SgAdaTaskBodyDecl*)(SgAdaTaskBodyDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskBodyDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskBodyDecl::pools.begin(); block != SgAdaTaskBodyDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskBodyDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskBodyDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskBodyDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskBodyDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskBodyDecl::pools.begin(); block != SgAdaTaskBodyDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskBodyDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskBodyDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskBodyDecl::clearMemoryPool() {
    SgAdaTaskBodyDecl* tempPointer{nullptr};
    if (SgAdaTaskBodyDecl::pools.empty() == false) {
        auto block{SgAdaTaskBodyDecl::pools.begin()};
        SgAdaTaskBodyDecl::next_node = reinterpret_cast<SgAdaTaskBodyDecl*>(*block);
        for (; block != SgAdaTaskBodyDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskBodyDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskBodyDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskBodyDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskBodyDecl::pool_size-1]);
        }
    }
}

void
SgAdaTaskBodyDecl::deleteMemoryPool() {
    for (auto p: SgAdaTaskBodyDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskBodyDecl::next_node = nullptr;
    SgAdaTaskBodyDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskBodyDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBodyDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskBodyDecl)};
    for (size_t blockIndex = SgAdaTaskBodyDecl::pools.size(); (blockIndex * SgAdaTaskBodyDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBodyDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskBodyDecl) = %" PRIuPTR " SgAdaTaskBodyDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskBodyDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskBodyDecl),SgAdaTaskBodyDecl::pool_size);
        }
#endif

        SgAdaTaskBodyDecl* pointer{static_cast<SgAdaTaskBodyDecl*>(ROSE_MALLOC(SgAdaTaskBodyDecl::pool_size * sizeof(SgAdaTaskBodyDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskBodyDecl::pool_size * sizeof(SgAdaTaskBodyDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskBodyDecl::pool_size * sizeof(SgAdaTaskBodyDecl));
#endif
        SgAdaTaskBodyDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskBodyDecl::pool_size * sizeof(SgAdaTaskBodyDecl), V_SgAdaTaskBodyDecl));

        if (SgAdaTaskBodyDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskBodyDecl* blkptr{reinterpret_cast<SgAdaTaskBodyDecl*>(SgAdaTaskBodyDecl::pools[blockIndex-1])};
                blkptr[SgAdaTaskBodyDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskBodyDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskBodyDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskBodyDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskBodyDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskBodyDecl::pool_size - 1};
    SgAdaTaskBodyDecl* testPointer{reinterpret_cast<SgAdaTaskBodyDecl*>(SgAdaTaskBodyDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskBodyDecl::pool_size * (SgAdaTaskBodyDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskBodyDeclStroageClass
 * from its counterpart of SgAdaTaskBodyDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskBodyDecl::initializeStorageClassArray(SgAdaTaskBodyDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskBodyDecl::pools.begin(); block != SgAdaTaskBodyDecl::pools.end(); ++block) {
        SgAdaTaskBodyDecl* pointer{reinterpret_cast<SgAdaTaskBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskBodyDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 79504 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskTypeDecl that takes its corresponding StorageClass as parameter
 */
SgAdaTaskTypeDecl::SgAdaTaskTypeDecl(const SgAdaTaskTypeDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 79514 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgAdaTaskSpec*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_type = (SgAdaTaskType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskTypeDecl*
SgAdaTaskTypeDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaTaskTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskTypeDecl::pool_size};
        returnPointer = &(((SgAdaTaskTypeDecl*)(SgAdaTaskTypeDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskTypeDecl*
SgAdaTaskTypeDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaTaskTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskTypeDecl::pool_size};
        returnPointer = &(((SgAdaTaskTypeDecl*)(SgAdaTaskTypeDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskTypeDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskTypeDecl::pools.begin(); block != SgAdaTaskTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskTypeDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskTypeDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskTypeDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskTypeDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskTypeDecl::pools.begin(); block != SgAdaTaskTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskTypeDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskTypeDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskTypeDecl::clearMemoryPool() {
    SgAdaTaskTypeDecl* tempPointer{nullptr};
    if (SgAdaTaskTypeDecl::pools.empty() == false) {
        auto block{SgAdaTaskTypeDecl::pools.begin()};
        SgAdaTaskTypeDecl::next_node = reinterpret_cast<SgAdaTaskTypeDecl*>(*block);
        for (; block != SgAdaTaskTypeDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskTypeDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskTypeDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskTypeDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskTypeDecl::pool_size-1]);
        }
    }
}

void
SgAdaTaskTypeDecl::deleteMemoryPool() {
    for (auto p: SgAdaTaskTypeDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskTypeDecl::next_node = nullptr;
    SgAdaTaskTypeDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskTypeDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskTypeDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskTypeDecl)};
    for (size_t blockIndex = SgAdaTaskTypeDecl::pools.size(); (blockIndex * SgAdaTaskTypeDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskTypeDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskTypeDecl) = %" PRIuPTR " SgAdaTaskTypeDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskTypeDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskTypeDecl),SgAdaTaskTypeDecl::pool_size);
        }
#endif

        SgAdaTaskTypeDecl* pointer{static_cast<SgAdaTaskTypeDecl*>(ROSE_MALLOC(SgAdaTaskTypeDecl::pool_size * sizeof(SgAdaTaskTypeDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskTypeDecl::pool_size * sizeof(SgAdaTaskTypeDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskTypeDecl::pool_size * sizeof(SgAdaTaskTypeDecl));
#endif
        SgAdaTaskTypeDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskTypeDecl::pool_size * sizeof(SgAdaTaskTypeDecl), V_SgAdaTaskTypeDecl));

        if (SgAdaTaskTypeDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskTypeDecl* blkptr{reinterpret_cast<SgAdaTaskTypeDecl*>(SgAdaTaskTypeDecl::pools[blockIndex-1])};
                blkptr[SgAdaTaskTypeDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskTypeDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskTypeDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskTypeDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskTypeDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskTypeDecl::pool_size - 1};
    SgAdaTaskTypeDecl* testPointer{reinterpret_cast<SgAdaTaskTypeDecl*>(SgAdaTaskTypeDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskTypeDecl::pool_size * (SgAdaTaskTypeDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskTypeDeclStroageClass
 * from its counterpart of SgAdaTaskTypeDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskTypeDecl::initializeStorageClassArray(SgAdaTaskTypeDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskTypeDecl::pools.begin(); block != SgAdaTaskTypeDecl::pools.end(); ++block) {
        SgAdaTaskTypeDecl* pointer{reinterpret_cast<SgAdaTaskTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskTypeDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 79735 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedSpecDecl that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedSpecDecl::SgAdaProtectedSpecDecl(const SgAdaProtectedSpecDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 79745 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgAdaProtectedSpec*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_type = (SgAdaProtectedType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedSpecDecl*
SgAdaProtectedSpecDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedSpecDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSpecDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSpecDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSpecDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpecDecl)};
        unsigned long positionInPool{localIndex % SgAdaProtectedSpecDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedSpecDecl::pool_size};
        returnPointer = &(((SgAdaProtectedSpecDecl*)(SgAdaProtectedSpecDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedSpecDecl*
SgAdaProtectedSpecDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedSpecDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSpecDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSpecDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSpecDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedSpecDecl)};
        unsigned long positionInPool{localIndex % SgAdaProtectedSpecDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedSpecDecl::pool_size};
        returnPointer = &(((SgAdaProtectedSpecDecl*)(SgAdaProtectedSpecDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedSpecDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedSpecDecl::pools.begin(); block != SgAdaProtectedSpecDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSpecDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedSpecDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedSpecDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedSpecDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedSpecDecl::pools.begin(); block != SgAdaProtectedSpecDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSpecDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedSpecDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedSpecDecl::clearMemoryPool() {
    SgAdaProtectedSpecDecl* tempPointer{nullptr};
    if (SgAdaProtectedSpecDecl::pools.empty() == false) {
        auto block{SgAdaProtectedSpecDecl::pools.begin()};
        SgAdaProtectedSpecDecl::next_node = reinterpret_cast<SgAdaProtectedSpecDecl*>(*block);
        for (; block != SgAdaProtectedSpecDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedSpecDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedSpecDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedSpecDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedSpecDecl::pool_size-1]);
        }
    }
}

void
SgAdaProtectedSpecDecl::deleteMemoryPool() {
    for (auto p: SgAdaProtectedSpecDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedSpecDecl::next_node = nullptr;
    SgAdaProtectedSpecDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedSpecDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpecDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSpecDecl)};
    for (size_t blockIndex = SgAdaProtectedSpecDecl::pools.size(); (blockIndex * SgAdaProtectedSpecDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpecDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSpecDecl) = %" PRIuPTR " SgAdaProtectedSpecDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSpecDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSpecDecl),SgAdaProtectedSpecDecl::pool_size);
        }
#endif

        SgAdaProtectedSpecDecl* pointer{static_cast<SgAdaProtectedSpecDecl*>(ROSE_MALLOC(SgAdaProtectedSpecDecl::pool_size * sizeof(SgAdaProtectedSpecDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedSpecDecl::pool_size * sizeof(SgAdaProtectedSpecDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedSpecDecl::pool_size * sizeof(SgAdaProtectedSpecDecl));
#endif
        SgAdaProtectedSpecDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedSpecDecl::pool_size * sizeof(SgAdaProtectedSpecDecl), V_SgAdaProtectedSpecDecl));

        if (SgAdaProtectedSpecDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedSpecDecl* blkptr{reinterpret_cast<SgAdaProtectedSpecDecl*>(SgAdaProtectedSpecDecl::pools[blockIndex-1])};
                blkptr[SgAdaProtectedSpecDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedSpecDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedSpecDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedSpecDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedSpecDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedSpecDecl::pool_size - 1};
    SgAdaProtectedSpecDecl* testPointer{reinterpret_cast<SgAdaProtectedSpecDecl*>(SgAdaProtectedSpecDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedSpecDecl::pool_size * (SgAdaProtectedSpecDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedSpecDeclStroageClass
 * from its counterpart of SgAdaProtectedSpecDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedSpecDecl::initializeStorageClassArray(SgAdaProtectedSpecDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedSpecDecl::pools.begin(); block != SgAdaProtectedSpecDecl::pools.end(); ++block) {
        SgAdaProtectedSpecDecl* pointer{reinterpret_cast<SgAdaProtectedSpecDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSpecDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 79965 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedBodyDecl that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedBodyDecl::SgAdaProtectedBodyDecl(const SgAdaProtectedBodyDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 79975 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_specificationDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_specificationDeclaration));
     p_definition = (SgAdaProtectedBody*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedBodyDecl*
SgAdaProtectedBodyDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedBodyDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedBodyDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedBodyDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedBodyDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBodyDecl)};
        unsigned long positionInPool{localIndex % SgAdaProtectedBodyDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedBodyDecl::pool_size};
        returnPointer = &(((SgAdaProtectedBodyDecl*)(SgAdaProtectedBodyDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedBodyDecl*
SgAdaProtectedBodyDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedBodyDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedBodyDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedBodyDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedBodyDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedBodyDecl)};
        unsigned long positionInPool{localIndex % SgAdaProtectedBodyDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedBodyDecl::pool_size};
        returnPointer = &(((SgAdaProtectedBodyDecl*)(SgAdaProtectedBodyDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedBodyDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedBodyDecl::pools.begin(); block != SgAdaProtectedBodyDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedBodyDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedBodyDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedBodyDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedBodyDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedBodyDecl::pools.begin(); block != SgAdaProtectedBodyDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedBodyDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedBodyDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedBodyDecl::clearMemoryPool() {
    SgAdaProtectedBodyDecl* tempPointer{nullptr};
    if (SgAdaProtectedBodyDecl::pools.empty() == false) {
        auto block{SgAdaProtectedBodyDecl::pools.begin()};
        SgAdaProtectedBodyDecl::next_node = reinterpret_cast<SgAdaProtectedBodyDecl*>(*block);
        for (; block != SgAdaProtectedBodyDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedBodyDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedBodyDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedBodyDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedBodyDecl::pool_size-1]);
        }
    }
}

void
SgAdaProtectedBodyDecl::deleteMemoryPool() {
    for (auto p: SgAdaProtectedBodyDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedBodyDecl::next_node = nullptr;
    SgAdaProtectedBodyDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedBodyDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBodyDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedBodyDecl)};
    for (size_t blockIndex = SgAdaProtectedBodyDecl::pools.size(); (blockIndex * SgAdaProtectedBodyDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBodyDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedBodyDecl) = %" PRIuPTR " SgAdaProtectedBodyDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedBodyDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedBodyDecl),SgAdaProtectedBodyDecl::pool_size);
        }
#endif

        SgAdaProtectedBodyDecl* pointer{static_cast<SgAdaProtectedBodyDecl*>(ROSE_MALLOC(SgAdaProtectedBodyDecl::pool_size * sizeof(SgAdaProtectedBodyDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedBodyDecl::pool_size * sizeof(SgAdaProtectedBodyDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedBodyDecl::pool_size * sizeof(SgAdaProtectedBodyDecl));
#endif
        SgAdaProtectedBodyDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedBodyDecl::pool_size * sizeof(SgAdaProtectedBodyDecl), V_SgAdaProtectedBodyDecl));

        if (SgAdaProtectedBodyDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedBodyDecl* blkptr{reinterpret_cast<SgAdaProtectedBodyDecl*>(SgAdaProtectedBodyDecl::pools[blockIndex-1])};
                blkptr[SgAdaProtectedBodyDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedBodyDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedBodyDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedBodyDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedBodyDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedBodyDecl::pool_size - 1};
    SgAdaProtectedBodyDecl* testPointer{reinterpret_cast<SgAdaProtectedBodyDecl*>(SgAdaProtectedBodyDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedBodyDecl::pool_size * (SgAdaProtectedBodyDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedBodyDeclStroageClass
 * from its counterpart of SgAdaProtectedBodyDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedBodyDecl::initializeStorageClassArray(SgAdaProtectedBodyDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedBodyDecl::pools.begin(); block != SgAdaProtectedBodyDecl::pools.end(); ++block) {
        SgAdaProtectedBodyDecl* pointer{reinterpret_cast<SgAdaProtectedBodyDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedBodyDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 80195 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedTypeDecl that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedTypeDecl::SgAdaProtectedTypeDecl(const SgAdaProtectedTypeDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 80205 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgAdaProtectedSpec*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_type = (SgAdaProtectedType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedTypeDecl*
SgAdaProtectedTypeDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaProtectedTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedTypeDecl::pool_size};
        returnPointer = &(((SgAdaProtectedTypeDecl*)(SgAdaProtectedTypeDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedTypeDecl*
SgAdaProtectedTypeDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaProtectedTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedTypeDecl::pool_size};
        returnPointer = &(((SgAdaProtectedTypeDecl*)(SgAdaProtectedTypeDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedTypeDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedTypeDecl::pools.begin(); block != SgAdaProtectedTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedTypeDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedTypeDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedTypeDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedTypeDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedTypeDecl::pools.begin(); block != SgAdaProtectedTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedTypeDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedTypeDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedTypeDecl::clearMemoryPool() {
    SgAdaProtectedTypeDecl* tempPointer{nullptr};
    if (SgAdaProtectedTypeDecl::pools.empty() == false) {
        auto block{SgAdaProtectedTypeDecl::pools.begin()};
        SgAdaProtectedTypeDecl::next_node = reinterpret_cast<SgAdaProtectedTypeDecl*>(*block);
        for (; block != SgAdaProtectedTypeDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedTypeDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedTypeDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedTypeDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedTypeDecl::pool_size-1]);
        }
    }
}

void
SgAdaProtectedTypeDecl::deleteMemoryPool() {
    for (auto p: SgAdaProtectedTypeDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedTypeDecl::next_node = nullptr;
    SgAdaProtectedTypeDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedTypeDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedTypeDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedTypeDecl)};
    for (size_t blockIndex = SgAdaProtectedTypeDecl::pools.size(); (blockIndex * SgAdaProtectedTypeDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedTypeDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedTypeDecl) = %" PRIuPTR " SgAdaProtectedTypeDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedTypeDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedTypeDecl),SgAdaProtectedTypeDecl::pool_size);
        }
#endif

        SgAdaProtectedTypeDecl* pointer{static_cast<SgAdaProtectedTypeDecl*>(ROSE_MALLOC(SgAdaProtectedTypeDecl::pool_size * sizeof(SgAdaProtectedTypeDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedTypeDecl::pool_size * sizeof(SgAdaProtectedTypeDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedTypeDecl::pool_size * sizeof(SgAdaProtectedTypeDecl));
#endif
        SgAdaProtectedTypeDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedTypeDecl::pool_size * sizeof(SgAdaProtectedTypeDecl), V_SgAdaProtectedTypeDecl));

        if (SgAdaProtectedTypeDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedTypeDecl* blkptr{reinterpret_cast<SgAdaProtectedTypeDecl*>(SgAdaProtectedTypeDecl::pools[blockIndex-1])};
                blkptr[SgAdaProtectedTypeDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedTypeDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedTypeDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedTypeDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedTypeDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedTypeDecl::pool_size - 1};
    SgAdaProtectedTypeDecl* testPointer{reinterpret_cast<SgAdaProtectedTypeDecl*>(SgAdaProtectedTypeDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedTypeDecl::pool_size * (SgAdaProtectedTypeDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedTypeDeclStroageClass
 * from its counterpart of SgAdaProtectedTypeDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedTypeDecl::initializeStorageClassArray(SgAdaProtectedTypeDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedTypeDecl::pools.begin(); block != SgAdaProtectedTypeDecl::pools.end(); ++block) {
        SgAdaProtectedTypeDecl* pointer{reinterpret_cast<SgAdaProtectedTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedTypeDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 80426 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaRepresentationClause that takes its corresponding StorageClass as parameter
 */
SgAdaRepresentationClause::SgAdaRepresentationClause(const SgAdaRepresentationClauseStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 80436 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_recordType = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_recordType));
     p_alignment = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_alignment));
     p_components = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_components));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaRepresentationClause*
SgAdaRepresentationClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaRepresentationClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRepresentationClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRepresentationClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRepresentationClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRepresentationClause)};
        unsigned long positionInPool{localIndex % SgAdaRepresentationClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRepresentationClause::pool_size};
        returnPointer = &(((SgAdaRepresentationClause*)(SgAdaRepresentationClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaRepresentationClause*
SgAdaRepresentationClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaRepresentationClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRepresentationClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRepresentationClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRepresentationClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaRepresentationClause)};
        unsigned long positionInPool{localIndex % SgAdaRepresentationClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRepresentationClause::pool_size};
        returnPointer = &(((SgAdaRepresentationClause*)(SgAdaRepresentationClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaRepresentationClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaRepresentationClause::pools.begin(); block != SgAdaRepresentationClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRepresentationClause*>(*block)};
        for (unsigned i = 0; i < SgAdaRepresentationClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaRepresentationClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaRepresentationClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaRepresentationClause* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaRepresentationClause::pools.begin(); block != SgAdaRepresentationClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRepresentationClause*>(*block)};
        for (unsigned i = 0; i < SgAdaRepresentationClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaRepresentationClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaRepresentationClause::clearMemoryPool() {
    SgAdaRepresentationClause* tempPointer{nullptr};
    if (SgAdaRepresentationClause::pools.empty() == false) {
        auto block{SgAdaRepresentationClause::pools.begin()};
        SgAdaRepresentationClause::next_node = reinterpret_cast<SgAdaRepresentationClause*>(*block);
        for (; block != SgAdaRepresentationClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaRepresentationClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaRepresentationClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaRepresentationClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaRepresentationClause::pool_size-1]);
        }
    }
}

void
SgAdaRepresentationClause::deleteMemoryPool() {
    for (auto p: SgAdaRepresentationClause::pools) {
        ROSE_FREE(p);
    }
    SgAdaRepresentationClause::next_node = nullptr;
    SgAdaRepresentationClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaRepresentationClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRepresentationClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRepresentationClause)};
    for (size_t blockIndex = SgAdaRepresentationClause::pools.size(); (blockIndex * SgAdaRepresentationClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRepresentationClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRepresentationClause) = %" PRIuPTR " SgAdaRepresentationClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRepresentationClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRepresentationClause),SgAdaRepresentationClause::pool_size);
        }
#endif

        SgAdaRepresentationClause* pointer{static_cast<SgAdaRepresentationClause*>(ROSE_MALLOC(SgAdaRepresentationClause::pool_size * sizeof(SgAdaRepresentationClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaRepresentationClause::pool_size * sizeof(SgAdaRepresentationClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaRepresentationClause::pool_size * sizeof(SgAdaRepresentationClause));
#endif
        SgAdaRepresentationClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaRepresentationClause::pool_size * sizeof(SgAdaRepresentationClause), V_SgAdaRepresentationClause));

        if (SgAdaRepresentationClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaRepresentationClause* blkptr{reinterpret_cast<SgAdaRepresentationClause*>(SgAdaRepresentationClause::pools[blockIndex-1])};
                blkptr[SgAdaRepresentationClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaRepresentationClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaRepresentationClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaRepresentationClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaRepresentationClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaRepresentationClause::pool_size - 1};
    SgAdaRepresentationClause* testPointer{reinterpret_cast<SgAdaRepresentationClause*>(SgAdaRepresentationClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaRepresentationClause::pool_size * (SgAdaRepresentationClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaRepresentationClauseStroageClass
 * from its counterpart of SgAdaRepresentationClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaRepresentationClause::initializeStorageClassArray(SgAdaRepresentationClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaRepresentationClause::pools.begin(); block != SgAdaRepresentationClause::pools.end(); ++block) {
        SgAdaRepresentationClause* pointer{reinterpret_cast<SgAdaRepresentationClause*>(*block)};
        for (unsigned i = 0; i < SgAdaRepresentationClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 80656 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaComponentClause that takes its corresponding StorageClass as parameter
 */
SgAdaComponentClause::SgAdaComponentClause(const SgAdaComponentClauseStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 80666 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_component = (SgVarRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_component));
     p_offset = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_offset));
     p_range = (SgRangeExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_range));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaComponentClause*
SgAdaComponentClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaComponentClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaComponentClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaComponentClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaComponentClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaComponentClause)};
        unsigned long positionInPool{localIndex % SgAdaComponentClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaComponentClause::pool_size};
        returnPointer = &(((SgAdaComponentClause*)(SgAdaComponentClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaComponentClause*
SgAdaComponentClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaComponentClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaComponentClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaComponentClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaComponentClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaComponentClause)};
        unsigned long positionInPool{localIndex % SgAdaComponentClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaComponentClause::pool_size};
        returnPointer = &(((SgAdaComponentClause*)(SgAdaComponentClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaComponentClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaComponentClause::pools.begin(); block != SgAdaComponentClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaComponentClause*>(*block)};
        for (unsigned i = 0; i < SgAdaComponentClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaComponentClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaComponentClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaComponentClause* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaComponentClause::pools.begin(); block != SgAdaComponentClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaComponentClause*>(*block)};
        for (unsigned i = 0; i < SgAdaComponentClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaComponentClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaComponentClause::clearMemoryPool() {
    SgAdaComponentClause* tempPointer{nullptr};
    if (SgAdaComponentClause::pools.empty() == false) {
        auto block{SgAdaComponentClause::pools.begin()};
        SgAdaComponentClause::next_node = reinterpret_cast<SgAdaComponentClause*>(*block);
        for (; block != SgAdaComponentClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaComponentClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaComponentClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaComponentClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaComponentClause::pool_size-1]);
        }
    }
}

void
SgAdaComponentClause::deleteMemoryPool() {
    for (auto p: SgAdaComponentClause::pools) {
        ROSE_FREE(p);
    }
    SgAdaComponentClause::next_node = nullptr;
    SgAdaComponentClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaComponentClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaComponentClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaComponentClause)};
    for (size_t blockIndex = SgAdaComponentClause::pools.size(); (blockIndex * SgAdaComponentClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaComponentClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaComponentClause) = %" PRIuPTR " SgAdaComponentClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaComponentClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaComponentClause),SgAdaComponentClause::pool_size);
        }
#endif

        SgAdaComponentClause* pointer{static_cast<SgAdaComponentClause*>(ROSE_MALLOC(SgAdaComponentClause::pool_size * sizeof(SgAdaComponentClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaComponentClause::pool_size * sizeof(SgAdaComponentClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaComponentClause::pool_size * sizeof(SgAdaComponentClause));
#endif
        SgAdaComponentClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaComponentClause::pool_size * sizeof(SgAdaComponentClause), V_SgAdaComponentClause));

        if (SgAdaComponentClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaComponentClause* blkptr{reinterpret_cast<SgAdaComponentClause*>(SgAdaComponentClause::pools[blockIndex-1])};
                blkptr[SgAdaComponentClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaComponentClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaComponentClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaComponentClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaComponentClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaComponentClause::pool_size - 1};
    SgAdaComponentClause* testPointer{reinterpret_cast<SgAdaComponentClause*>(SgAdaComponentClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaComponentClause::pool_size * (SgAdaComponentClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaComponentClauseStroageClass
 * from its counterpart of SgAdaComponentClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaComponentClause::initializeStorageClassArray(SgAdaComponentClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaComponentClause::pools.begin(); block != SgAdaComponentClause::pools.end(); ++block) {
        SgAdaComponentClause* pointer{reinterpret_cast<SgAdaComponentClause*>(*block)};
        for (unsigned i = 0; i < SgAdaComponentClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 80886 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaAttributeClause that takes its corresponding StorageClass as parameter
 */
SgAdaAttributeClause::SgAdaAttributeClause(const SgAdaAttributeClauseStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 80896 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_attribute = (SgAdaAttributeExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_attribute));
     p_size = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_size));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaAttributeClause*
SgAdaAttributeClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaAttributeClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAttributeClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAttributeClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAttributeClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeClause)};
        unsigned long positionInPool{localIndex % SgAdaAttributeClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAttributeClause::pool_size};
        returnPointer = &(((SgAdaAttributeClause*)(SgAdaAttributeClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaAttributeClause*
SgAdaAttributeClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaAttributeClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAttributeClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAttributeClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAttributeClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaAttributeClause)};
        unsigned long positionInPool{localIndex % SgAdaAttributeClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAttributeClause::pool_size};
        returnPointer = &(((SgAdaAttributeClause*)(SgAdaAttributeClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaAttributeClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaAttributeClause::pools.begin(); block != SgAdaAttributeClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAttributeClause*>(*block)};
        for (unsigned i = 0; i < SgAdaAttributeClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaAttributeClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaAttributeClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaAttributeClause* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaAttributeClause::pools.begin(); block != SgAdaAttributeClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAttributeClause*>(*block)};
        for (unsigned i = 0; i < SgAdaAttributeClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaAttributeClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaAttributeClause::clearMemoryPool() {
    SgAdaAttributeClause* tempPointer{nullptr};
    if (SgAdaAttributeClause::pools.empty() == false) {
        auto block{SgAdaAttributeClause::pools.begin()};
        SgAdaAttributeClause::next_node = reinterpret_cast<SgAdaAttributeClause*>(*block);
        for (; block != SgAdaAttributeClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaAttributeClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaAttributeClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaAttributeClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaAttributeClause::pool_size-1]);
        }
    }
}

void
SgAdaAttributeClause::deleteMemoryPool() {
    for (auto p: SgAdaAttributeClause::pools) {
        ROSE_FREE(p);
    }
    SgAdaAttributeClause::next_node = nullptr;
    SgAdaAttributeClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaAttributeClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAttributeClause)};
    for (size_t blockIndex = SgAdaAttributeClause::pools.size(); (blockIndex * SgAdaAttributeClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAttributeClause) = %" PRIuPTR " SgAdaAttributeClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAttributeClause),SgAdaAttributeClause::pool_size);
        }
#endif

        SgAdaAttributeClause* pointer{static_cast<SgAdaAttributeClause*>(ROSE_MALLOC(SgAdaAttributeClause::pool_size * sizeof(SgAdaAttributeClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaAttributeClause::pool_size * sizeof(SgAdaAttributeClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaAttributeClause::pool_size * sizeof(SgAdaAttributeClause));
#endif
        SgAdaAttributeClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaAttributeClause::pool_size * sizeof(SgAdaAttributeClause), V_SgAdaAttributeClause));

        if (SgAdaAttributeClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaAttributeClause* blkptr{reinterpret_cast<SgAdaAttributeClause*>(SgAdaAttributeClause::pools[blockIndex-1])};
                blkptr[SgAdaAttributeClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaAttributeClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaAttributeClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaAttributeClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaAttributeClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaAttributeClause::pool_size - 1};
    SgAdaAttributeClause* testPointer{reinterpret_cast<SgAdaAttributeClause*>(SgAdaAttributeClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaAttributeClause::pool_size * (SgAdaAttributeClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaAttributeClauseStroageClass
 * from its counterpart of SgAdaAttributeClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaAttributeClause::initializeStorageClassArray(SgAdaAttributeClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaAttributeClause::pools.begin(); block != SgAdaAttributeClause::pools.end(); ++block) {
        SgAdaAttributeClause* pointer{reinterpret_cast<SgAdaAttributeClause*>(*block)};
        for (unsigned i = 0; i < SgAdaAttributeClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 81115 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaEnumRepresentationClause that takes its corresponding StorageClass as parameter
 */
SgAdaEnumRepresentationClause::SgAdaEnumRepresentationClause(const SgAdaEnumRepresentationClauseStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 81125 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_enumType = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_enumType));
     p_components = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_components));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaEnumRepresentationClause*
SgAdaEnumRepresentationClause::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaEnumRepresentationClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaEnumRepresentationClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaEnumRepresentationClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaEnumRepresentationClause)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEnumRepresentationClause)};
        unsigned long positionInPool{localIndex % SgAdaEnumRepresentationClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaEnumRepresentationClause::pool_size};
        returnPointer = &(((SgAdaEnumRepresentationClause*)(SgAdaEnumRepresentationClause::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaEnumRepresentationClause*
SgAdaEnumRepresentationClause::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaEnumRepresentationClause* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaEnumRepresentationClause)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaEnumRepresentationClause + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaEnumRepresentationClause)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaEnumRepresentationClause)};
        unsigned long positionInPool{localIndex % SgAdaEnumRepresentationClause::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaEnumRepresentationClause::pool_size};
        returnPointer = &(((SgAdaEnumRepresentationClause*)(SgAdaEnumRepresentationClause::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaEnumRepresentationClause::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaEnumRepresentationClause::pools.begin(); block != SgAdaEnumRepresentationClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaEnumRepresentationClause*>(*block)};
        for (unsigned i = 0; i < SgAdaEnumRepresentationClause::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaEnumRepresentationClause*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaEnumRepresentationClause::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaEnumRepresentationClause* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaEnumRepresentationClause::pools.begin(); block != SgAdaEnumRepresentationClause::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaEnumRepresentationClause*>(*block)};
        for (unsigned i = 0; i < SgAdaEnumRepresentationClause::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaEnumRepresentationClause::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaEnumRepresentationClause::clearMemoryPool() {
    SgAdaEnumRepresentationClause* tempPointer{nullptr};
    if (SgAdaEnumRepresentationClause::pools.empty() == false) {
        auto block{SgAdaEnumRepresentationClause::pools.begin()};
        SgAdaEnumRepresentationClause::next_node = reinterpret_cast<SgAdaEnumRepresentationClause*>(*block);
        for (; block != SgAdaEnumRepresentationClause::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaEnumRepresentationClause*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaEnumRepresentationClause::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaEnumRepresentationClause::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaEnumRepresentationClause::pool_size-1]);
        }
    }
}

void
SgAdaEnumRepresentationClause::deleteMemoryPool() {
    for (auto p: SgAdaEnumRepresentationClause::pools) {
        ROSE_FREE(p);
    }
    SgAdaEnumRepresentationClause::next_node = nullptr;
    SgAdaEnumRepresentationClause::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaEnumRepresentationClause::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEnumRepresentationClause) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaEnumRepresentationClause)};
    for (size_t blockIndex = SgAdaEnumRepresentationClause::pools.size(); (blockIndex * SgAdaEnumRepresentationClause::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEnumRepresentationClause) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaEnumRepresentationClause) = %" PRIuPTR " SgAdaEnumRepresentationClause::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaEnumRepresentationClause),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaEnumRepresentationClause),SgAdaEnumRepresentationClause::pool_size);
        }
#endif

        SgAdaEnumRepresentationClause* pointer{static_cast<SgAdaEnumRepresentationClause*>(ROSE_MALLOC(SgAdaEnumRepresentationClause::pool_size * sizeof(SgAdaEnumRepresentationClause)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaEnumRepresentationClause::pool_size * sizeof(SgAdaEnumRepresentationClause));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaEnumRepresentationClause::pool_size * sizeof(SgAdaEnumRepresentationClause));
#endif
        SgAdaEnumRepresentationClause::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaEnumRepresentationClause::pool_size * sizeof(SgAdaEnumRepresentationClause), V_SgAdaEnumRepresentationClause));

        if (SgAdaEnumRepresentationClause::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaEnumRepresentationClause* blkptr{reinterpret_cast<SgAdaEnumRepresentationClause*>(SgAdaEnumRepresentationClause::pools[blockIndex-1])};
                blkptr[SgAdaEnumRepresentationClause::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaEnumRepresentationClause::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaEnumRepresentationClause::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaEnumRepresentationClause::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaEnumRepresentationClause::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaEnumRepresentationClause::pool_size - 1};
    SgAdaEnumRepresentationClause* testPointer{reinterpret_cast<SgAdaEnumRepresentationClause*>(SgAdaEnumRepresentationClause::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaEnumRepresentationClause::pool_size * (SgAdaEnumRepresentationClause::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaEnumRepresentationClauseStroageClass
 * from its counterpart of SgAdaEnumRepresentationClause. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaEnumRepresentationClause::initializeStorageClassArray(SgAdaEnumRepresentationClauseStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaEnumRepresentationClause::pools.begin(); block != SgAdaEnumRepresentationClause::pools.end(); ++block) {
        SgAdaEnumRepresentationClause* pointer{reinterpret_cast<SgAdaEnumRepresentationClause*>(*block)};
        for (unsigned i = 0; i < SgAdaEnumRepresentationClause::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 81344 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaGenericDecl that takes its corresponding StorageClass as parameter
 */
SgAdaGenericDecl::SgAdaGenericDecl(const SgAdaGenericDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 81354 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_definition = (SgAdaGenericDefn*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_definition));
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaGenericDecl*
SgAdaGenericDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaGenericDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDecl)};
        unsigned long positionInPool{localIndex % SgAdaGenericDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericDecl::pool_size};
        returnPointer = &(((SgAdaGenericDecl*)(SgAdaGenericDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaGenericDecl*
SgAdaGenericDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaGenericDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaGenericDecl)};
        unsigned long positionInPool{localIndex % SgAdaGenericDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericDecl::pool_size};
        returnPointer = &(((SgAdaGenericDecl*)(SgAdaGenericDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaGenericDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaGenericDecl::pools.begin(); block != SgAdaGenericDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaGenericDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaGenericDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaGenericDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaGenericDecl::pools.begin(); block != SgAdaGenericDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaGenericDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaGenericDecl::clearMemoryPool() {
    SgAdaGenericDecl* tempPointer{nullptr};
    if (SgAdaGenericDecl::pools.empty() == false) {
        auto block{SgAdaGenericDecl::pools.begin()};
        SgAdaGenericDecl::next_node = reinterpret_cast<SgAdaGenericDecl*>(*block);
        for (; block != SgAdaGenericDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaGenericDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaGenericDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaGenericDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaGenericDecl::pool_size-1]);
        }
    }
}

void
SgAdaGenericDecl::deleteMemoryPool() {
    for (auto p: SgAdaGenericDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaGenericDecl::next_node = nullptr;
    SgAdaGenericDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaGenericDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericDecl)};
    for (size_t blockIndex = SgAdaGenericDecl::pools.size(); (blockIndex * SgAdaGenericDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericDecl) = %" PRIuPTR " SgAdaGenericDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericDecl),SgAdaGenericDecl::pool_size);
        }
#endif

        SgAdaGenericDecl* pointer{static_cast<SgAdaGenericDecl*>(ROSE_MALLOC(SgAdaGenericDecl::pool_size * sizeof(SgAdaGenericDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaGenericDecl::pool_size * sizeof(SgAdaGenericDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaGenericDecl::pool_size * sizeof(SgAdaGenericDecl));
#endif
        SgAdaGenericDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaGenericDecl::pool_size * sizeof(SgAdaGenericDecl), V_SgAdaGenericDecl));

        if (SgAdaGenericDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaGenericDecl* blkptr{reinterpret_cast<SgAdaGenericDecl*>(SgAdaGenericDecl::pools[blockIndex-1])};
                blkptr[SgAdaGenericDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaGenericDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaGenericDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaGenericDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaGenericDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaGenericDecl::pool_size - 1};
    SgAdaGenericDecl* testPointer{reinterpret_cast<SgAdaGenericDecl*>(SgAdaGenericDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaGenericDecl::pool_size * (SgAdaGenericDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaGenericDeclStroageClass
 * from its counterpart of SgAdaGenericDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaGenericDecl::initializeStorageClassArray(SgAdaGenericDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaGenericDecl::pools.begin(); block != SgAdaGenericDecl::pools.end(); ++block) {
        SgAdaGenericDecl* pointer{reinterpret_cast<SgAdaGenericDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 81575 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaFormalTypeDecl that takes its corresponding StorageClass as parameter
 */
SgAdaFormalTypeDecl::SgAdaFormalTypeDecl(const SgAdaFormalTypeDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 81585 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_type = (SgAdaFormalType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_discriminants = (SgAdaParameterList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_discriminants));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaFormalTypeDecl*
SgAdaFormalTypeDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaFormalTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaFormalTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalTypeDecl::pool_size};
        returnPointer = &(((SgAdaFormalTypeDecl*)(SgAdaFormalTypeDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaFormalTypeDecl*
SgAdaFormalTypeDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaFormalTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaFormalTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaFormalTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalTypeDecl::pool_size};
        returnPointer = &(((SgAdaFormalTypeDecl*)(SgAdaFormalTypeDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaFormalTypeDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaFormalTypeDecl::pools.begin(); block != SgAdaFormalTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalTypeDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaFormalTypeDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaFormalTypeDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaFormalTypeDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaFormalTypeDecl::pools.begin(); block != SgAdaFormalTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalTypeDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaFormalTypeDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaFormalTypeDecl::clearMemoryPool() {
    SgAdaFormalTypeDecl* tempPointer{nullptr};
    if (SgAdaFormalTypeDecl::pools.empty() == false) {
        auto block{SgAdaFormalTypeDecl::pools.begin()};
        SgAdaFormalTypeDecl::next_node = reinterpret_cast<SgAdaFormalTypeDecl*>(*block);
        for (; block != SgAdaFormalTypeDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaFormalTypeDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaFormalTypeDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaFormalTypeDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaFormalTypeDecl::pool_size-1]);
        }
    }
}

void
SgAdaFormalTypeDecl::deleteMemoryPool() {
    for (auto p: SgAdaFormalTypeDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaFormalTypeDecl::next_node = nullptr;
    SgAdaFormalTypeDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaFormalTypeDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalTypeDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalTypeDecl)};
    for (size_t blockIndex = SgAdaFormalTypeDecl::pools.size(); (blockIndex * SgAdaFormalTypeDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalTypeDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalTypeDecl) = %" PRIuPTR " SgAdaFormalTypeDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalTypeDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalTypeDecl),SgAdaFormalTypeDecl::pool_size);
        }
#endif

        SgAdaFormalTypeDecl* pointer{static_cast<SgAdaFormalTypeDecl*>(ROSE_MALLOC(SgAdaFormalTypeDecl::pool_size * sizeof(SgAdaFormalTypeDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaFormalTypeDecl::pool_size * sizeof(SgAdaFormalTypeDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaFormalTypeDecl::pool_size * sizeof(SgAdaFormalTypeDecl));
#endif
        SgAdaFormalTypeDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaFormalTypeDecl::pool_size * sizeof(SgAdaFormalTypeDecl), V_SgAdaFormalTypeDecl));

        if (SgAdaFormalTypeDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaFormalTypeDecl* blkptr{reinterpret_cast<SgAdaFormalTypeDecl*>(SgAdaFormalTypeDecl::pools[blockIndex-1])};
                blkptr[SgAdaFormalTypeDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaFormalTypeDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaFormalTypeDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaFormalTypeDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaFormalTypeDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaFormalTypeDecl::pool_size - 1};
    SgAdaFormalTypeDecl* testPointer{reinterpret_cast<SgAdaFormalTypeDecl*>(SgAdaFormalTypeDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaFormalTypeDecl::pool_size * (SgAdaFormalTypeDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaFormalTypeDeclStroageClass
 * from its counterpart of SgAdaFormalTypeDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaFormalTypeDecl::initializeStorageClassArray(SgAdaFormalTypeDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaFormalTypeDecl::pools.begin(); block != SgAdaFormalTypeDecl::pools.end(); ++block) {
        SgAdaFormalTypeDecl* pointer{reinterpret_cast<SgAdaFormalTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalTypeDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 81805 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDiscriminatedTypeDecl that takes its corresponding StorageClass as parameter
 */
SgAdaDiscriminatedTypeDecl::SgAdaDiscriminatedTypeDecl(const SgAdaDiscriminatedTypeDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 81815 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_discriminantScope = (SgDeclarationScope*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_discriminantScope));
     p_discriminants = (SgAdaParameterList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_discriminants));
     p_discriminatedDecl = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_discriminatedDecl));
     p_type = (SgAdaDiscriminatedType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDiscriminatedTypeDecl*
SgAdaDiscriminatedTypeDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDiscriminatedTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminatedTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminatedTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDiscriminatedTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaDiscriminatedTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscriminatedTypeDecl::pool_size};
        returnPointer = &(((SgAdaDiscriminatedTypeDecl*)(SgAdaDiscriminatedTypeDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDiscriminatedTypeDecl*
SgAdaDiscriminatedTypeDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDiscriminatedTypeDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminatedTypeDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminatedTypeDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDiscriminatedTypeDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDiscriminatedTypeDecl)};
        unsigned long positionInPool{localIndex % SgAdaDiscriminatedTypeDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDiscriminatedTypeDecl::pool_size};
        returnPointer = &(((SgAdaDiscriminatedTypeDecl*)(SgAdaDiscriminatedTypeDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDiscriminatedTypeDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDiscriminatedTypeDecl::pools.begin(); block != SgAdaDiscriminatedTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscriminatedTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminatedTypeDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDiscriminatedTypeDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDiscriminatedTypeDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDiscriminatedTypeDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDiscriminatedTypeDecl::pools.begin(); block != SgAdaDiscriminatedTypeDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDiscriminatedTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminatedTypeDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDiscriminatedTypeDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDiscriminatedTypeDecl::clearMemoryPool() {
    SgAdaDiscriminatedTypeDecl* tempPointer{nullptr};
    if (SgAdaDiscriminatedTypeDecl::pools.empty() == false) {
        auto block{SgAdaDiscriminatedTypeDecl::pools.begin()};
        SgAdaDiscriminatedTypeDecl::next_node = reinterpret_cast<SgAdaDiscriminatedTypeDecl*>(*block);
        for (; block != SgAdaDiscriminatedTypeDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDiscriminatedTypeDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDiscriminatedTypeDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDiscriminatedTypeDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDiscriminatedTypeDecl::pool_size-1]);
        }
    }
}

void
SgAdaDiscriminatedTypeDecl::deleteMemoryPool() {
    for (auto p: SgAdaDiscriminatedTypeDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaDiscriminatedTypeDecl::next_node = nullptr;
    SgAdaDiscriminatedTypeDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDiscriminatedTypeDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedTypeDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminatedTypeDecl)};
    for (size_t blockIndex = SgAdaDiscriminatedTypeDecl::pools.size(); (blockIndex * SgAdaDiscriminatedTypeDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedTypeDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminatedTypeDecl) = %" PRIuPTR " SgAdaDiscriminatedTypeDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDiscriminatedTypeDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDiscriminatedTypeDecl),SgAdaDiscriminatedTypeDecl::pool_size);
        }
#endif

        SgAdaDiscriminatedTypeDecl* pointer{static_cast<SgAdaDiscriminatedTypeDecl*>(ROSE_MALLOC(SgAdaDiscriminatedTypeDecl::pool_size * sizeof(SgAdaDiscriminatedTypeDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDiscriminatedTypeDecl::pool_size * sizeof(SgAdaDiscriminatedTypeDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDiscriminatedTypeDecl::pool_size * sizeof(SgAdaDiscriminatedTypeDecl));
#endif
        SgAdaDiscriminatedTypeDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDiscriminatedTypeDecl::pool_size * sizeof(SgAdaDiscriminatedTypeDecl), V_SgAdaDiscriminatedTypeDecl));

        if (SgAdaDiscriminatedTypeDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDiscriminatedTypeDecl* blkptr{reinterpret_cast<SgAdaDiscriminatedTypeDecl*>(SgAdaDiscriminatedTypeDecl::pools[blockIndex-1])};
                blkptr[SgAdaDiscriminatedTypeDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDiscriminatedTypeDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDiscriminatedTypeDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDiscriminatedTypeDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDiscriminatedTypeDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDiscriminatedTypeDecl::pool_size - 1};
    SgAdaDiscriminatedTypeDecl* testPointer{reinterpret_cast<SgAdaDiscriminatedTypeDecl*>(SgAdaDiscriminatedTypeDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDiscriminatedTypeDecl::pool_size * (SgAdaDiscriminatedTypeDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDiscriminatedTypeDeclStroageClass
 * from its counterpart of SgAdaDiscriminatedTypeDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDiscriminatedTypeDecl::initializeStorageClassArray(SgAdaDiscriminatedTypeDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDiscriminatedTypeDecl::pools.begin(); block != SgAdaDiscriminatedTypeDecl::pools.end(); ++block) {
        SgAdaDiscriminatedTypeDecl* pointer{reinterpret_cast<SgAdaDiscriminatedTypeDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaDiscriminatedTypeDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 82037 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaGenericInstanceDecl that takes its corresponding StorageClass as parameter
 */
SgAdaGenericInstanceDecl::SgAdaGenericInstanceDecl(const SgAdaGenericInstanceDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 82047 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_genericDeclaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_genericDeclaration));
     p_instantiatedScope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_instantiatedScope));
     p_actual_parameters = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_actual_parameters));
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaGenericInstanceDecl*
SgAdaGenericInstanceDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaGenericInstanceDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericInstanceDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericInstanceDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericInstanceDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceDecl)};
        unsigned long positionInPool{localIndex % SgAdaGenericInstanceDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericInstanceDecl::pool_size};
        returnPointer = &(((SgAdaGenericInstanceDecl*)(SgAdaGenericInstanceDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaGenericInstanceDecl*
SgAdaGenericInstanceDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaGenericInstanceDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericInstanceDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericInstanceDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericInstanceDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaGenericInstanceDecl)};
        unsigned long positionInPool{localIndex % SgAdaGenericInstanceDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericInstanceDecl::pool_size};
        returnPointer = &(((SgAdaGenericInstanceDecl*)(SgAdaGenericInstanceDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaGenericInstanceDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaGenericInstanceDecl::pools.begin(); block != SgAdaGenericInstanceDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericInstanceDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericInstanceDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaGenericInstanceDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaGenericInstanceDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaGenericInstanceDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaGenericInstanceDecl::pools.begin(); block != SgAdaGenericInstanceDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericInstanceDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericInstanceDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaGenericInstanceDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaGenericInstanceDecl::clearMemoryPool() {
    SgAdaGenericInstanceDecl* tempPointer{nullptr};
    if (SgAdaGenericInstanceDecl::pools.empty() == false) {
        auto block{SgAdaGenericInstanceDecl::pools.begin()};
        SgAdaGenericInstanceDecl::next_node = reinterpret_cast<SgAdaGenericInstanceDecl*>(*block);
        for (; block != SgAdaGenericInstanceDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaGenericInstanceDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaGenericInstanceDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaGenericInstanceDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaGenericInstanceDecl::pool_size-1]);
        }
    }
}

void
SgAdaGenericInstanceDecl::deleteMemoryPool() {
    for (auto p: SgAdaGenericInstanceDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaGenericInstanceDecl::next_node = nullptr;
    SgAdaGenericInstanceDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaGenericInstanceDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericInstanceDecl)};
    for (size_t blockIndex = SgAdaGenericInstanceDecl::pools.size(); (blockIndex * SgAdaGenericInstanceDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericInstanceDecl) = %" PRIuPTR " SgAdaGenericInstanceDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericInstanceDecl),SgAdaGenericInstanceDecl::pool_size);
        }
#endif

        SgAdaGenericInstanceDecl* pointer{static_cast<SgAdaGenericInstanceDecl*>(ROSE_MALLOC(SgAdaGenericInstanceDecl::pool_size * sizeof(SgAdaGenericInstanceDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaGenericInstanceDecl::pool_size * sizeof(SgAdaGenericInstanceDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaGenericInstanceDecl::pool_size * sizeof(SgAdaGenericInstanceDecl));
#endif
        SgAdaGenericInstanceDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaGenericInstanceDecl::pool_size * sizeof(SgAdaGenericInstanceDecl), V_SgAdaGenericInstanceDecl));

        if (SgAdaGenericInstanceDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaGenericInstanceDecl* blkptr{reinterpret_cast<SgAdaGenericInstanceDecl*>(SgAdaGenericInstanceDecl::pools[blockIndex-1])};
                blkptr[SgAdaGenericInstanceDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaGenericInstanceDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaGenericInstanceDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaGenericInstanceDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaGenericInstanceDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaGenericInstanceDecl::pool_size - 1};
    SgAdaGenericInstanceDecl* testPointer{reinterpret_cast<SgAdaGenericInstanceDecl*>(SgAdaGenericInstanceDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaGenericInstanceDecl::pool_size * (SgAdaGenericInstanceDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaGenericInstanceDeclStroageClass
 * from its counterpart of SgAdaGenericInstanceDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaGenericInstanceDecl::initializeStorageClassArray(SgAdaGenericInstanceDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaGenericInstanceDecl::pools.begin(); block != SgAdaGenericInstanceDecl::pools.end(); ++block) {
        SgAdaGenericInstanceDecl* pointer{reinterpret_cast<SgAdaGenericInstanceDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericInstanceDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 82269 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaFormalPackageDecl that takes its corresponding StorageClass as parameter
 */
SgAdaFormalPackageDecl::SgAdaFormalPackageDecl(const SgAdaFormalPackageDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 82279 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
     p_actual_parameters = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_actual_parameters));
     p_prototype = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_prototype));
     p_prototypeScope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_prototypeScope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaFormalPackageDecl*
SgAdaFormalPackageDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaFormalPackageDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalPackageDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalPackageDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalPackageDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageDecl)};
        unsigned long positionInPool{localIndex % SgAdaFormalPackageDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalPackageDecl::pool_size};
        returnPointer = &(((SgAdaFormalPackageDecl*)(SgAdaFormalPackageDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaFormalPackageDecl*
SgAdaFormalPackageDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaFormalPackageDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalPackageDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalPackageDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalPackageDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaFormalPackageDecl)};
        unsigned long positionInPool{localIndex % SgAdaFormalPackageDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalPackageDecl::pool_size};
        returnPointer = &(((SgAdaFormalPackageDecl*)(SgAdaFormalPackageDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaFormalPackageDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaFormalPackageDecl::pools.begin(); block != SgAdaFormalPackageDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalPackageDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalPackageDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaFormalPackageDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaFormalPackageDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaFormalPackageDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaFormalPackageDecl::pools.begin(); block != SgAdaFormalPackageDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalPackageDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalPackageDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaFormalPackageDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaFormalPackageDecl::clearMemoryPool() {
    SgAdaFormalPackageDecl* tempPointer{nullptr};
    if (SgAdaFormalPackageDecl::pools.empty() == false) {
        auto block{SgAdaFormalPackageDecl::pools.begin()};
        SgAdaFormalPackageDecl::next_node = reinterpret_cast<SgAdaFormalPackageDecl*>(*block);
        for (; block != SgAdaFormalPackageDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaFormalPackageDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaFormalPackageDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaFormalPackageDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaFormalPackageDecl::pool_size-1]);
        }
    }
}

void
SgAdaFormalPackageDecl::deleteMemoryPool() {
    for (auto p: SgAdaFormalPackageDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaFormalPackageDecl::next_node = nullptr;
    SgAdaFormalPackageDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaFormalPackageDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalPackageDecl)};
    for (size_t blockIndex = SgAdaFormalPackageDecl::pools.size(); (blockIndex * SgAdaFormalPackageDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalPackageDecl) = %" PRIuPTR " SgAdaFormalPackageDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalPackageDecl),SgAdaFormalPackageDecl::pool_size);
        }
#endif

        SgAdaFormalPackageDecl* pointer{static_cast<SgAdaFormalPackageDecl*>(ROSE_MALLOC(SgAdaFormalPackageDecl::pool_size * sizeof(SgAdaFormalPackageDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaFormalPackageDecl::pool_size * sizeof(SgAdaFormalPackageDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaFormalPackageDecl::pool_size * sizeof(SgAdaFormalPackageDecl));
#endif
        SgAdaFormalPackageDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaFormalPackageDecl::pool_size * sizeof(SgAdaFormalPackageDecl), V_SgAdaFormalPackageDecl));

        if (SgAdaFormalPackageDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaFormalPackageDecl* blkptr{reinterpret_cast<SgAdaFormalPackageDecl*>(SgAdaFormalPackageDecl::pools[blockIndex-1])};
                blkptr[SgAdaFormalPackageDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaFormalPackageDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaFormalPackageDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaFormalPackageDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaFormalPackageDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaFormalPackageDecl::pool_size - 1};
    SgAdaFormalPackageDecl* testPointer{reinterpret_cast<SgAdaFormalPackageDecl*>(SgAdaFormalPackageDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaFormalPackageDecl::pool_size * (SgAdaFormalPackageDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaFormalPackageDeclStroageClass
 * from its counterpart of SgAdaFormalPackageDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaFormalPackageDecl::initializeStorageClassArray(SgAdaFormalPackageDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaFormalPackageDecl::pools.begin(); block != SgAdaFormalPackageDecl::pools.end(); ++block) {
        SgAdaFormalPackageDecl* pointer{reinterpret_cast<SgAdaFormalPackageDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalPackageDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 82501 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaParameterList that takes its corresponding StorageClass as parameter
 */
SgAdaParameterList::SgAdaParameterList(const SgAdaParameterListStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 82511 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_parameters = storageSource.storageOf_parameters.rebuildDataStoredInEasyStorageClass();
    SgDeclarationStatementPtrList::iterator i_parameters = p_parameters.begin();
    for ( ; i_parameters != p_parameters.end(); ++i_parameters) {
        (*i_parameters) = (SgDeclarationStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_parameters)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaParameterList*
SgAdaParameterList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaParameterList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaParameterList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaParameterList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaParameterList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaParameterList)};
        unsigned long positionInPool{localIndex % SgAdaParameterList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaParameterList::pool_size};
        returnPointer = &(((SgAdaParameterList*)(SgAdaParameterList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaParameterList*
SgAdaParameterList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaParameterList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaParameterList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaParameterList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaParameterList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaParameterList)};
        unsigned long positionInPool{localIndex % SgAdaParameterList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaParameterList::pool_size};
        returnPointer = &(((SgAdaParameterList*)(SgAdaParameterList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaParameterList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaParameterList::pools.begin(); block != SgAdaParameterList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaParameterList*>(*block)};
        for (unsigned i = 0; i < SgAdaParameterList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaParameterList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaParameterList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaParameterList* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaParameterList::pools.begin(); block != SgAdaParameterList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaParameterList*>(*block)};
        for (unsigned i = 0; i < SgAdaParameterList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaParameterList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaParameterList::clearMemoryPool() {
    SgAdaParameterList* tempPointer{nullptr};
    if (SgAdaParameterList::pools.empty() == false) {
        auto block{SgAdaParameterList::pools.begin()};
        SgAdaParameterList::next_node = reinterpret_cast<SgAdaParameterList*>(*block);
        for (; block != SgAdaParameterList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaParameterList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaParameterList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaParameterList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaParameterList::pool_size-1]);
        }
    }
}

void
SgAdaParameterList::deleteMemoryPool() {
    for (auto p: SgAdaParameterList::pools) {
        ROSE_FREE(p);
    }
    SgAdaParameterList::next_node = nullptr;
    SgAdaParameterList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaParameterList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaParameterList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaParameterList)};
    for (size_t blockIndex = SgAdaParameterList::pools.size(); (blockIndex * SgAdaParameterList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaParameterList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaParameterList) = %" PRIuPTR " SgAdaParameterList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaParameterList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaParameterList),SgAdaParameterList::pool_size);
        }
#endif

        SgAdaParameterList* pointer{static_cast<SgAdaParameterList*>(ROSE_MALLOC(SgAdaParameterList::pool_size * sizeof(SgAdaParameterList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaParameterList::pool_size * sizeof(SgAdaParameterList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaParameterList::pool_size * sizeof(SgAdaParameterList));
#endif
        SgAdaParameterList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaParameterList::pool_size * sizeof(SgAdaParameterList), V_SgAdaParameterList));

        if (SgAdaParameterList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaParameterList* blkptr{reinterpret_cast<SgAdaParameterList*>(SgAdaParameterList::pools[blockIndex-1])};
                blkptr[SgAdaParameterList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaParameterList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaParameterList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaParameterList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaParameterList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaParameterList::pool_size - 1};
    SgAdaParameterList* testPointer{reinterpret_cast<SgAdaParameterList*>(SgAdaParameterList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaParameterList::pool_size * (SgAdaParameterList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaParameterListStroageClass
 * from its counterpart of SgAdaParameterList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaParameterList::initializeStorageClassArray(SgAdaParameterListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaParameterList::pools.begin(); block != SgAdaParameterList::pools.end(); ++block) {
        SgAdaParameterList* pointer{reinterpret_cast<SgAdaParameterList*>(*block)};
        for (unsigned i = 0; i < SgAdaParameterList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 82733 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaVariantDecl that takes its corresponding StorageClass as parameter
 */
SgAdaVariantDecl::SgAdaVariantDecl(const SgAdaVariantDeclStorageClass& storageSource)  : SgDeclarationStatement (storageSource) {

/* #line 82743 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_discriminant = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_discriminant));
     p_variants = (SgAdaUnscopedBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_variants));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaVariantDecl*
SgAdaVariantDecl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaVariantDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaVariantDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaVariantDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaVariantDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantDecl)};
        unsigned long positionInPool{localIndex % SgAdaVariantDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaVariantDecl::pool_size};
        returnPointer = &(((SgAdaVariantDecl*)(SgAdaVariantDecl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaVariantDecl*
SgAdaVariantDecl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaVariantDecl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaVariantDecl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaVariantDecl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaVariantDecl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaVariantDecl)};
        unsigned long positionInPool{localIndex % SgAdaVariantDecl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaVariantDecl::pool_size};
        returnPointer = &(((SgAdaVariantDecl*)(SgAdaVariantDecl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaVariantDecl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaVariantDecl::pools.begin(); block != SgAdaVariantDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaVariantDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaVariantDecl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaVariantDecl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaVariantDecl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaVariantDecl* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaVariantDecl::pools.begin(); block != SgAdaVariantDecl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaVariantDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaVariantDecl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaVariantDecl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaVariantDecl::clearMemoryPool() {
    SgAdaVariantDecl* tempPointer{nullptr};
    if (SgAdaVariantDecl::pools.empty() == false) {
        auto block{SgAdaVariantDecl::pools.begin()};
        SgAdaVariantDecl::next_node = reinterpret_cast<SgAdaVariantDecl*>(*block);
        for (; block != SgAdaVariantDecl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaVariantDecl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaVariantDecl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaVariantDecl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaVariantDecl::pool_size-1]);
        }
    }
}

void
SgAdaVariantDecl::deleteMemoryPool() {
    for (auto p: SgAdaVariantDecl::pools) {
        ROSE_FREE(p);
    }
    SgAdaVariantDecl::next_node = nullptr;
    SgAdaVariantDecl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaVariantDecl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantDecl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaVariantDecl)};
    for (size_t blockIndex = SgAdaVariantDecl::pools.size(); (blockIndex * SgAdaVariantDecl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantDecl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaVariantDecl) = %" PRIuPTR " SgAdaVariantDecl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantDecl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaVariantDecl),SgAdaVariantDecl::pool_size);
        }
#endif

        SgAdaVariantDecl* pointer{static_cast<SgAdaVariantDecl*>(ROSE_MALLOC(SgAdaVariantDecl::pool_size * sizeof(SgAdaVariantDecl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaVariantDecl::pool_size * sizeof(SgAdaVariantDecl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaVariantDecl::pool_size * sizeof(SgAdaVariantDecl));
#endif
        SgAdaVariantDecl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaVariantDecl::pool_size * sizeof(SgAdaVariantDecl), V_SgAdaVariantDecl));

        if (SgAdaVariantDecl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaVariantDecl* blkptr{reinterpret_cast<SgAdaVariantDecl*>(SgAdaVariantDecl::pools[blockIndex-1])};
                blkptr[SgAdaVariantDecl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaVariantDecl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaVariantDecl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaVariantDecl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaVariantDecl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaVariantDecl::pool_size - 1};
    SgAdaVariantDecl* testPointer{reinterpret_cast<SgAdaVariantDecl*>(SgAdaVariantDecl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaVariantDecl::pool_size * (SgAdaVariantDecl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaVariantDeclStroageClass
 * from its counterpart of SgAdaVariantDecl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaVariantDecl::initializeStorageClassArray(SgAdaVariantDeclStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaVariantDecl::pools.begin(); block != SgAdaVariantDecl::pools.end(); ++block) {
        SgAdaVariantDecl* pointer{reinterpret_cast<SgAdaVariantDecl*>(*block)};
        for (unsigned i = 0; i < SgAdaVariantDecl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 82962 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExprStatement that takes its corresponding StorageClass as parameter
 */
SgExprStatement::SgExprStatement(const SgExprStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 82972 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExprStatement*
SgExprStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExprStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExprStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExprStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExprStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExprStatement)};
        unsigned long positionInPool{localIndex % SgExprStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExprStatement::pool_size};
        returnPointer = &(((SgExprStatement*)(SgExprStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExprStatement*
SgExprStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExprStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExprStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExprStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExprStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExprStatement)};
        unsigned long positionInPool{localIndex % SgExprStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExprStatement::pool_size};
        returnPointer = &(((SgExprStatement*)(SgExprStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExprStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExprStatement::pools.begin(); block != SgExprStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExprStatement*>(*block)};
        for (unsigned i = 0; i < SgExprStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExprStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExprStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExprStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgExprStatement::pools.begin(); block != SgExprStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExprStatement*>(*block)};
        for (unsigned i = 0; i < SgExprStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExprStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExprStatement::clearMemoryPool() {
    SgExprStatement* tempPointer{nullptr};
    if (SgExprStatement::pools.empty() == false) {
        auto block{SgExprStatement::pools.begin()};
        SgExprStatement::next_node = reinterpret_cast<SgExprStatement*>(*block);
        for (; block != SgExprStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExprStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExprStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExprStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExprStatement::pool_size-1]);
        }
    }
}

void
SgExprStatement::deleteMemoryPool() {
    for (auto p: SgExprStatement::pools) {
        ROSE_FREE(p);
    }
    SgExprStatement::next_node = nullptr;
    SgExprStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExprStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExprStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExprStatement)};
    for (size_t blockIndex = SgExprStatement::pools.size(); (blockIndex * SgExprStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExprStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExprStatement) = %" PRIuPTR " SgExprStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExprStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExprStatement),SgExprStatement::pool_size);
        }
#endif

        SgExprStatement* pointer{static_cast<SgExprStatement*>(ROSE_MALLOC(SgExprStatement::pool_size * sizeof(SgExprStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExprStatement::pool_size * sizeof(SgExprStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExprStatement::pool_size * sizeof(SgExprStatement));
#endif
        SgExprStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExprStatement::pool_size * sizeof(SgExprStatement), V_SgExprStatement));

        if (SgExprStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExprStatement* blkptr{reinterpret_cast<SgExprStatement*>(SgExprStatement::pools[blockIndex-1])};
                blkptr[SgExprStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExprStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExprStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExprStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExprStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExprStatement::pool_size - 1};
    SgExprStatement* testPointer{reinterpret_cast<SgExprStatement*>(SgExprStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExprStatement::pool_size * (SgExprStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExprStatementStroageClass
 * from its counterpart of SgExprStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExprStatement::initializeStorageClassArray(SgExprStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExprStatement::pools.begin(); block != SgExprStatement::pools.end(); ++block) {
        SgExprStatement* pointer{reinterpret_cast<SgExprStatement*>(*block)};
        for (unsigned i = 0; i < SgExprStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 83190 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLabelStatement that takes its corresponding StorageClass as parameter
 */
SgLabelStatement::SgLabelStatement(const SgLabelStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 83200 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_label = SgName ( storageSource.storageOf_label);
     p_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_scope));
     p_statement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_statement));
    p_label_type = storageSource.storageOf_label_type;
    p_gnu_extension_unused = storageSource.storageOf_gnu_extension_unused;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLabelStatement*
SgLabelStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLabelStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelStatement)};
        unsigned long positionInPool{localIndex % SgLabelStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLabelStatement::pool_size};
        returnPointer = &(((SgLabelStatement*)(SgLabelStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLabelStatement*
SgLabelStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLabelStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLabelStatement)};
        unsigned long positionInPool{localIndex % SgLabelStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLabelStatement::pool_size};
        returnPointer = &(((SgLabelStatement*)(SgLabelStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLabelStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLabelStatement::pools.begin(); block != SgLabelStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLabelStatement*>(*block)};
        for (unsigned i = 0; i < SgLabelStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLabelStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLabelStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLabelStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgLabelStatement::pools.begin(); block != SgLabelStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLabelStatement*>(*block)};
        for (unsigned i = 0; i < SgLabelStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLabelStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLabelStatement::clearMemoryPool() {
    SgLabelStatement* tempPointer{nullptr};
    if (SgLabelStatement::pools.empty() == false) {
        auto block{SgLabelStatement::pools.begin()};
        SgLabelStatement::next_node = reinterpret_cast<SgLabelStatement*>(*block);
        for (; block != SgLabelStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLabelStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLabelStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLabelStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLabelStatement::pool_size-1]);
        }
    }
}

void
SgLabelStatement::deleteMemoryPool() {
    for (auto p: SgLabelStatement::pools) {
        ROSE_FREE(p);
    }
    SgLabelStatement::next_node = nullptr;
    SgLabelStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLabelStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelStatement)};
    for (size_t blockIndex = SgLabelStatement::pools.size(); (blockIndex * SgLabelStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelStatement) = %" PRIuPTR " SgLabelStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelStatement),SgLabelStatement::pool_size);
        }
#endif

        SgLabelStatement* pointer{static_cast<SgLabelStatement*>(ROSE_MALLOC(SgLabelStatement::pool_size * sizeof(SgLabelStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLabelStatement::pool_size * sizeof(SgLabelStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLabelStatement::pool_size * sizeof(SgLabelStatement));
#endif
        SgLabelStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLabelStatement::pool_size * sizeof(SgLabelStatement), V_SgLabelStatement));

        if (SgLabelStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLabelStatement* blkptr{reinterpret_cast<SgLabelStatement*>(SgLabelStatement::pools[blockIndex-1])};
                blkptr[SgLabelStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLabelStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLabelStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLabelStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLabelStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLabelStatement::pool_size - 1};
    SgLabelStatement* testPointer{reinterpret_cast<SgLabelStatement*>(SgLabelStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLabelStatement::pool_size * (SgLabelStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLabelStatementStroageClass
 * from its counterpart of SgLabelStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLabelStatement::initializeStorageClassArray(SgLabelStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLabelStatement::pools.begin(); block != SgLabelStatement::pools.end(); ++block) {
        SgLabelStatement* pointer{reinterpret_cast<SgLabelStatement*>(*block)};
        for (unsigned i = 0; i < SgLabelStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 83422 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCaseOptionStmt that takes its corresponding StorageClass as parameter
 */
SgCaseOptionStmt::SgCaseOptionStmt(const SgCaseOptionStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 83432 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_key = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_key));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_key_range_end = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_key_range_end));
    p_case_construct_name = storageSource.storageOf_case_construct_name.rebuildDataStoredInEasyStorageClass();
    p_has_fall_through = storageSource.storageOf_has_fall_through;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCaseOptionStmt*
SgCaseOptionStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCaseOptionStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCaseOptionStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCaseOptionStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCaseOptionStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCaseOptionStmt)};
        unsigned long positionInPool{localIndex % SgCaseOptionStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCaseOptionStmt::pool_size};
        returnPointer = &(((SgCaseOptionStmt*)(SgCaseOptionStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCaseOptionStmt*
SgCaseOptionStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCaseOptionStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCaseOptionStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCaseOptionStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCaseOptionStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCaseOptionStmt)};
        unsigned long positionInPool{localIndex % SgCaseOptionStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCaseOptionStmt::pool_size};
        returnPointer = &(((SgCaseOptionStmt*)(SgCaseOptionStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCaseOptionStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCaseOptionStmt::pools.begin(); block != SgCaseOptionStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCaseOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgCaseOptionStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCaseOptionStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCaseOptionStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCaseOptionStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgCaseOptionStmt::pools.begin(); block != SgCaseOptionStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCaseOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgCaseOptionStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCaseOptionStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCaseOptionStmt::clearMemoryPool() {
    SgCaseOptionStmt* tempPointer{nullptr};
    if (SgCaseOptionStmt::pools.empty() == false) {
        auto block{SgCaseOptionStmt::pools.begin()};
        SgCaseOptionStmt::next_node = reinterpret_cast<SgCaseOptionStmt*>(*block);
        for (; block != SgCaseOptionStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCaseOptionStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCaseOptionStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCaseOptionStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCaseOptionStmt::pool_size-1]);
        }
    }
}

void
SgCaseOptionStmt::deleteMemoryPool() {
    for (auto p: SgCaseOptionStmt::pools) {
        ROSE_FREE(p);
    }
    SgCaseOptionStmt::next_node = nullptr;
    SgCaseOptionStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCaseOptionStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCaseOptionStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCaseOptionStmt)};
    for (size_t blockIndex = SgCaseOptionStmt::pools.size(); (blockIndex * SgCaseOptionStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCaseOptionStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCaseOptionStmt) = %" PRIuPTR " SgCaseOptionStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCaseOptionStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCaseOptionStmt),SgCaseOptionStmt::pool_size);
        }
#endif

        SgCaseOptionStmt* pointer{static_cast<SgCaseOptionStmt*>(ROSE_MALLOC(SgCaseOptionStmt::pool_size * sizeof(SgCaseOptionStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCaseOptionStmt::pool_size * sizeof(SgCaseOptionStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCaseOptionStmt::pool_size * sizeof(SgCaseOptionStmt));
#endif
        SgCaseOptionStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCaseOptionStmt::pool_size * sizeof(SgCaseOptionStmt), V_SgCaseOptionStmt));

        if (SgCaseOptionStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCaseOptionStmt* blkptr{reinterpret_cast<SgCaseOptionStmt*>(SgCaseOptionStmt::pools[blockIndex-1])};
                blkptr[SgCaseOptionStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCaseOptionStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCaseOptionStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCaseOptionStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCaseOptionStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCaseOptionStmt::pool_size - 1};
    SgCaseOptionStmt* testPointer{reinterpret_cast<SgCaseOptionStmt*>(SgCaseOptionStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCaseOptionStmt::pool_size * (SgCaseOptionStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCaseOptionStmtStroageClass
 * from its counterpart of SgCaseOptionStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCaseOptionStmt::initializeStorageClassArray(SgCaseOptionStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCaseOptionStmt::pools.begin(); block != SgCaseOptionStmt::pools.end(); ++block) {
        SgCaseOptionStmt* pointer{reinterpret_cast<SgCaseOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgCaseOptionStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 83654 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTryStmt that takes its corresponding StorageClass as parameter
 */
SgTryStmt::SgTryStmt(const SgTryStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 83664 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_catch_statement_seq_root = (SgCatchStatementSeq*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_catch_statement_seq_root));
     p_else_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_else_body));
     p_finally_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_finally_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTryStmt*
SgTryStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTryStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTryStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTryStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTryStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTryStmt)};
        unsigned long positionInPool{localIndex % SgTryStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTryStmt::pool_size};
        returnPointer = &(((SgTryStmt*)(SgTryStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTryStmt*
SgTryStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTryStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTryStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTryStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTryStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTryStmt)};
        unsigned long positionInPool{localIndex % SgTryStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTryStmt::pool_size};
        returnPointer = &(((SgTryStmt*)(SgTryStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTryStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTryStmt::pools.begin(); block != SgTryStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTryStmt*>(*block)};
        for (unsigned i = 0; i < SgTryStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTryStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTryStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTryStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgTryStmt::pools.begin(); block != SgTryStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTryStmt*>(*block)};
        for (unsigned i = 0; i < SgTryStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTryStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTryStmt::clearMemoryPool() {
    SgTryStmt* tempPointer{nullptr};
    if (SgTryStmt::pools.empty() == false) {
        auto block{SgTryStmt::pools.begin()};
        SgTryStmt::next_node = reinterpret_cast<SgTryStmt*>(*block);
        for (; block != SgTryStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTryStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTryStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTryStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTryStmt::pool_size-1]);
        }
    }
}

void
SgTryStmt::deleteMemoryPool() {
    for (auto p: SgTryStmt::pools) {
        ROSE_FREE(p);
    }
    SgTryStmt::next_node = nullptr;
    SgTryStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTryStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTryStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTryStmt)};
    for (size_t blockIndex = SgTryStmt::pools.size(); (blockIndex * SgTryStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTryStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTryStmt) = %" PRIuPTR " SgTryStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTryStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTryStmt),SgTryStmt::pool_size);
        }
#endif

        SgTryStmt* pointer{static_cast<SgTryStmt*>(ROSE_MALLOC(SgTryStmt::pool_size * sizeof(SgTryStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTryStmt::pool_size * sizeof(SgTryStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTryStmt::pool_size * sizeof(SgTryStmt));
#endif
        SgTryStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTryStmt::pool_size * sizeof(SgTryStmt), V_SgTryStmt));

        if (SgTryStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTryStmt* blkptr{reinterpret_cast<SgTryStmt*>(SgTryStmt::pools[blockIndex-1])};
                blkptr[SgTryStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTryStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTryStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTryStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTryStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTryStmt::pool_size - 1};
    SgTryStmt* testPointer{reinterpret_cast<SgTryStmt*>(SgTryStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTryStmt::pool_size * (SgTryStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTryStmtStroageClass
 * from its counterpart of SgTryStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTryStmt::initializeStorageClassArray(SgTryStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTryStmt::pools.begin(); block != SgTryStmt::pools.end(); ++block) {
        SgTryStmt* pointer{reinterpret_cast<SgTryStmt*>(*block)};
        for (unsigned i = 0; i < SgTryStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 83885 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDefaultOptionStmt that takes its corresponding StorageClass as parameter
 */
SgDefaultOptionStmt::SgDefaultOptionStmt(const SgDefaultOptionStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 83895 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_default_construct_name = storageSource.storageOf_default_construct_name.rebuildDataStoredInEasyStorageClass();
    p_has_fall_through = storageSource.storageOf_has_fall_through;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDefaultOptionStmt*
SgDefaultOptionStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDefaultOptionStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefaultOptionStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefaultOptionStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefaultOptionStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultOptionStmt)};
        unsigned long positionInPool{localIndex % SgDefaultOptionStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDefaultOptionStmt::pool_size};
        returnPointer = &(((SgDefaultOptionStmt*)(SgDefaultOptionStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDefaultOptionStmt*
SgDefaultOptionStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDefaultOptionStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefaultOptionStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefaultOptionStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefaultOptionStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDefaultOptionStmt)};
        unsigned long positionInPool{localIndex % SgDefaultOptionStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDefaultOptionStmt::pool_size};
        returnPointer = &(((SgDefaultOptionStmt*)(SgDefaultOptionStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDefaultOptionStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDefaultOptionStmt::pools.begin(); block != SgDefaultOptionStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDefaultOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgDefaultOptionStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDefaultOptionStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDefaultOptionStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDefaultOptionStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgDefaultOptionStmt::pools.begin(); block != SgDefaultOptionStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDefaultOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgDefaultOptionStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDefaultOptionStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDefaultOptionStmt::clearMemoryPool() {
    SgDefaultOptionStmt* tempPointer{nullptr};
    if (SgDefaultOptionStmt::pools.empty() == false) {
        auto block{SgDefaultOptionStmt::pools.begin()};
        SgDefaultOptionStmt::next_node = reinterpret_cast<SgDefaultOptionStmt*>(*block);
        for (; block != SgDefaultOptionStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDefaultOptionStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDefaultOptionStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDefaultOptionStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDefaultOptionStmt::pool_size-1]);
        }
    }
}

void
SgDefaultOptionStmt::deleteMemoryPool() {
    for (auto p: SgDefaultOptionStmt::pools) {
        ROSE_FREE(p);
    }
    SgDefaultOptionStmt::next_node = nullptr;
    SgDefaultOptionStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDefaultOptionStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultOptionStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefaultOptionStmt)};
    for (size_t blockIndex = SgDefaultOptionStmt::pools.size(); (blockIndex * SgDefaultOptionStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultOptionStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefaultOptionStmt) = %" PRIuPTR " SgDefaultOptionStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultOptionStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefaultOptionStmt),SgDefaultOptionStmt::pool_size);
        }
#endif

        SgDefaultOptionStmt* pointer{static_cast<SgDefaultOptionStmt*>(ROSE_MALLOC(SgDefaultOptionStmt::pool_size * sizeof(SgDefaultOptionStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDefaultOptionStmt::pool_size * sizeof(SgDefaultOptionStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDefaultOptionStmt::pool_size * sizeof(SgDefaultOptionStmt));
#endif
        SgDefaultOptionStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDefaultOptionStmt::pool_size * sizeof(SgDefaultOptionStmt), V_SgDefaultOptionStmt));

        if (SgDefaultOptionStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDefaultOptionStmt* blkptr{reinterpret_cast<SgDefaultOptionStmt*>(SgDefaultOptionStmt::pools[blockIndex-1])};
                blkptr[SgDefaultOptionStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDefaultOptionStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDefaultOptionStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDefaultOptionStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDefaultOptionStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDefaultOptionStmt::pool_size - 1};
    SgDefaultOptionStmt* testPointer{reinterpret_cast<SgDefaultOptionStmt*>(SgDefaultOptionStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDefaultOptionStmt::pool_size * (SgDefaultOptionStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDefaultOptionStmtStroageClass
 * from its counterpart of SgDefaultOptionStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDefaultOptionStmt::initializeStorageClassArray(SgDefaultOptionStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDefaultOptionStmt::pools.begin(); block != SgDefaultOptionStmt::pools.end(); ++block) {
        SgDefaultOptionStmt* pointer{reinterpret_cast<SgDefaultOptionStmt*>(*block)};
        for (unsigned i = 0; i < SgDefaultOptionStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 84115 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBreakStmt that takes its corresponding StorageClass as parameter
 */
SgBreakStmt::SgBreakStmt(const SgBreakStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 84125 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_do_string_label = storageSource.storageOf_do_string_label.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBreakStmt*
SgBreakStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBreakStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBreakStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBreakStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBreakStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBreakStmt)};
        unsigned long positionInPool{localIndex % SgBreakStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBreakStmt::pool_size};
        returnPointer = &(((SgBreakStmt*)(SgBreakStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBreakStmt*
SgBreakStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBreakStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBreakStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBreakStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBreakStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBreakStmt)};
        unsigned long positionInPool{localIndex % SgBreakStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBreakStmt::pool_size};
        returnPointer = &(((SgBreakStmt*)(SgBreakStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBreakStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBreakStmt::pools.begin(); block != SgBreakStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBreakStmt*>(*block)};
        for (unsigned i = 0; i < SgBreakStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBreakStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBreakStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBreakStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgBreakStmt::pools.begin(); block != SgBreakStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBreakStmt*>(*block)};
        for (unsigned i = 0; i < SgBreakStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBreakStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBreakStmt::clearMemoryPool() {
    SgBreakStmt* tempPointer{nullptr};
    if (SgBreakStmt::pools.empty() == false) {
        auto block{SgBreakStmt::pools.begin()};
        SgBreakStmt::next_node = reinterpret_cast<SgBreakStmt*>(*block);
        for (; block != SgBreakStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBreakStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBreakStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBreakStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBreakStmt::pool_size-1]);
        }
    }
}

void
SgBreakStmt::deleteMemoryPool() {
    for (auto p: SgBreakStmt::pools) {
        ROSE_FREE(p);
    }
    SgBreakStmt::next_node = nullptr;
    SgBreakStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBreakStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBreakStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBreakStmt)};
    for (size_t blockIndex = SgBreakStmt::pools.size(); (blockIndex * SgBreakStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBreakStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBreakStmt) = %" PRIuPTR " SgBreakStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBreakStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBreakStmt),SgBreakStmt::pool_size);
        }
#endif

        SgBreakStmt* pointer{static_cast<SgBreakStmt*>(ROSE_MALLOC(SgBreakStmt::pool_size * sizeof(SgBreakStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBreakStmt::pool_size * sizeof(SgBreakStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBreakStmt::pool_size * sizeof(SgBreakStmt));
#endif
        SgBreakStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBreakStmt::pool_size * sizeof(SgBreakStmt), V_SgBreakStmt));

        if (SgBreakStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBreakStmt* blkptr{reinterpret_cast<SgBreakStmt*>(SgBreakStmt::pools[blockIndex-1])};
                blkptr[SgBreakStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBreakStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBreakStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBreakStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBreakStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBreakStmt::pool_size - 1};
    SgBreakStmt* testPointer{reinterpret_cast<SgBreakStmt*>(SgBreakStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBreakStmt::pool_size * (SgBreakStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBreakStmtStroageClass
 * from its counterpart of SgBreakStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBreakStmt::initializeStorageClassArray(SgBreakStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBreakStmt::pools.begin(); block != SgBreakStmt::pools.end(); ++block) {
        SgBreakStmt* pointer{reinterpret_cast<SgBreakStmt*>(*block)};
        for (unsigned i = 0; i < SgBreakStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 84343 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgContinueStmt that takes its corresponding StorageClass as parameter
 */
SgContinueStmt::SgContinueStmt(const SgContinueStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 84353 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_do_string_label = storageSource.storageOf_do_string_label.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgContinueStmt*
SgContinueStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgContinueStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgContinueStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgContinueStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgContinueStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgContinueStmt)};
        unsigned long positionInPool{localIndex % SgContinueStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgContinueStmt::pool_size};
        returnPointer = &(((SgContinueStmt*)(SgContinueStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgContinueStmt*
SgContinueStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgContinueStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgContinueStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgContinueStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgContinueStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgContinueStmt)};
        unsigned long positionInPool{localIndex % SgContinueStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgContinueStmt::pool_size};
        returnPointer = &(((SgContinueStmt*)(SgContinueStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgContinueStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgContinueStmt::pools.begin(); block != SgContinueStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgContinueStmt*>(*block)};
        for (unsigned i = 0; i < SgContinueStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgContinueStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgContinueStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgContinueStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgContinueStmt::pools.begin(); block != SgContinueStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgContinueStmt*>(*block)};
        for (unsigned i = 0; i < SgContinueStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgContinueStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgContinueStmt::clearMemoryPool() {
    SgContinueStmt* tempPointer{nullptr};
    if (SgContinueStmt::pools.empty() == false) {
        auto block{SgContinueStmt::pools.begin()};
        SgContinueStmt::next_node = reinterpret_cast<SgContinueStmt*>(*block);
        for (; block != SgContinueStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgContinueStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgContinueStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgContinueStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgContinueStmt::pool_size-1]);
        }
    }
}

void
SgContinueStmt::deleteMemoryPool() {
    for (auto p: SgContinueStmt::pools) {
        ROSE_FREE(p);
    }
    SgContinueStmt::next_node = nullptr;
    SgContinueStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgContinueStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgContinueStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgContinueStmt)};
    for (size_t blockIndex = SgContinueStmt::pools.size(); (blockIndex * SgContinueStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgContinueStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgContinueStmt) = %" PRIuPTR " SgContinueStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgContinueStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgContinueStmt),SgContinueStmt::pool_size);
        }
#endif

        SgContinueStmt* pointer{static_cast<SgContinueStmt*>(ROSE_MALLOC(SgContinueStmt::pool_size * sizeof(SgContinueStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgContinueStmt::pool_size * sizeof(SgContinueStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgContinueStmt::pool_size * sizeof(SgContinueStmt));
#endif
        SgContinueStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgContinueStmt::pool_size * sizeof(SgContinueStmt), V_SgContinueStmt));

        if (SgContinueStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgContinueStmt* blkptr{reinterpret_cast<SgContinueStmt*>(SgContinueStmt::pools[blockIndex-1])};
                blkptr[SgContinueStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgContinueStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgContinueStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgContinueStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgContinueStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgContinueStmt::pool_size - 1};
    SgContinueStmt* testPointer{reinterpret_cast<SgContinueStmt*>(SgContinueStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgContinueStmt::pool_size * (SgContinueStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgContinueStmtStroageClass
 * from its counterpart of SgContinueStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgContinueStmt::initializeStorageClassArray(SgContinueStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgContinueStmt::pools.begin(); block != SgContinueStmt::pools.end(); ++block) {
        SgContinueStmt* pointer{reinterpret_cast<SgContinueStmt*>(*block)};
        for (unsigned i = 0; i < SgContinueStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 84571 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgReturnStmt that takes its corresponding StorageClass as parameter
 */
SgReturnStmt::SgReturnStmt(const SgReturnStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 84581 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgReturnStmt*
SgReturnStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgReturnStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReturnStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReturnStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReturnStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgReturnStmt)};
        unsigned long positionInPool{localIndex % SgReturnStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReturnStmt::pool_size};
        returnPointer = &(((SgReturnStmt*)(SgReturnStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgReturnStmt*
SgReturnStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgReturnStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReturnStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReturnStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReturnStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgReturnStmt)};
        unsigned long positionInPool{localIndex % SgReturnStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReturnStmt::pool_size};
        returnPointer = &(((SgReturnStmt*)(SgReturnStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgReturnStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgReturnStmt::pools.begin(); block != SgReturnStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReturnStmt*>(*block)};
        for (unsigned i = 0; i < SgReturnStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgReturnStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgReturnStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgReturnStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgReturnStmt::pools.begin(); block != SgReturnStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReturnStmt*>(*block)};
        for (unsigned i = 0; i < SgReturnStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgReturnStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgReturnStmt::clearMemoryPool() {
    SgReturnStmt* tempPointer{nullptr};
    if (SgReturnStmt::pools.empty() == false) {
        auto block{SgReturnStmt::pools.begin()};
        SgReturnStmt::next_node = reinterpret_cast<SgReturnStmt*>(*block);
        for (; block != SgReturnStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgReturnStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgReturnStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgReturnStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgReturnStmt::pool_size-1]);
        }
    }
}

void
SgReturnStmt::deleteMemoryPool() {
    for (auto p: SgReturnStmt::pools) {
        ROSE_FREE(p);
    }
    SgReturnStmt::next_node = nullptr;
    SgReturnStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgReturnStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgReturnStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgReturnStmt)};
    for (size_t blockIndex = SgReturnStmt::pools.size(); (blockIndex * SgReturnStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgReturnStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgReturnStmt) = %" PRIuPTR " SgReturnStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgReturnStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgReturnStmt),SgReturnStmt::pool_size);
        }
#endif

        SgReturnStmt* pointer{static_cast<SgReturnStmt*>(ROSE_MALLOC(SgReturnStmt::pool_size * sizeof(SgReturnStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgReturnStmt::pool_size * sizeof(SgReturnStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgReturnStmt::pool_size * sizeof(SgReturnStmt));
#endif
        SgReturnStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgReturnStmt::pool_size * sizeof(SgReturnStmt), V_SgReturnStmt));

        if (SgReturnStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgReturnStmt* blkptr{reinterpret_cast<SgReturnStmt*>(SgReturnStmt::pools[blockIndex-1])};
                blkptr[SgReturnStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgReturnStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgReturnStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgReturnStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgReturnStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgReturnStmt::pool_size - 1};
    SgReturnStmt* testPointer{reinterpret_cast<SgReturnStmt*>(SgReturnStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgReturnStmt::pool_size * (SgReturnStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgReturnStmtStroageClass
 * from its counterpart of SgReturnStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgReturnStmt::initializeStorageClassArray(SgReturnStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgReturnStmt::pools.begin(); block != SgReturnStmt::pools.end(); ++block) {
        SgReturnStmt* pointer{reinterpret_cast<SgReturnStmt*>(*block)};
        for (unsigned i = 0; i < SgReturnStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 84799 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGotoStatement that takes its corresponding StorageClass as parameter
 */
SgGotoStatement::SgGotoStatement(const SgGotoStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 84809 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_label = (SgLabelStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_label));
     p_label_expression = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_label_expression));
     p_selector_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_selector_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGotoStatement*
SgGotoStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGotoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGotoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGotoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGotoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGotoStatement)};
        unsigned long positionInPool{localIndex % SgGotoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGotoStatement::pool_size};
        returnPointer = &(((SgGotoStatement*)(SgGotoStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGotoStatement*
SgGotoStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGotoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGotoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGotoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGotoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGotoStatement)};
        unsigned long positionInPool{localIndex % SgGotoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGotoStatement::pool_size};
        returnPointer = &(((SgGotoStatement*)(SgGotoStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGotoStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGotoStatement::pools.begin(); block != SgGotoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgGotoStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGotoStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGotoStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGotoStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgGotoStatement::pools.begin(); block != SgGotoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgGotoStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGotoStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGotoStatement::clearMemoryPool() {
    SgGotoStatement* tempPointer{nullptr};
    if (SgGotoStatement::pools.empty() == false) {
        auto block{SgGotoStatement::pools.begin()};
        SgGotoStatement::next_node = reinterpret_cast<SgGotoStatement*>(*block);
        for (; block != SgGotoStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGotoStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGotoStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGotoStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGotoStatement::pool_size-1]);
        }
    }
}

void
SgGotoStatement::deleteMemoryPool() {
    for (auto p: SgGotoStatement::pools) {
        ROSE_FREE(p);
    }
    SgGotoStatement::next_node = nullptr;
    SgGotoStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGotoStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGotoStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGotoStatement)};
    for (size_t blockIndex = SgGotoStatement::pools.size(); (blockIndex * SgGotoStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGotoStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGotoStatement) = %" PRIuPTR " SgGotoStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGotoStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGotoStatement),SgGotoStatement::pool_size);
        }
#endif

        SgGotoStatement* pointer{static_cast<SgGotoStatement*>(ROSE_MALLOC(SgGotoStatement::pool_size * sizeof(SgGotoStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGotoStatement::pool_size * sizeof(SgGotoStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGotoStatement::pool_size * sizeof(SgGotoStatement));
#endif
        SgGotoStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGotoStatement::pool_size * sizeof(SgGotoStatement), V_SgGotoStatement));

        if (SgGotoStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGotoStatement* blkptr{reinterpret_cast<SgGotoStatement*>(SgGotoStatement::pools[blockIndex-1])};
                blkptr[SgGotoStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGotoStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGotoStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGotoStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGotoStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGotoStatement::pool_size - 1};
    SgGotoStatement* testPointer{reinterpret_cast<SgGotoStatement*>(SgGotoStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGotoStatement::pool_size * (SgGotoStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGotoStatementStroageClass
 * from its counterpart of SgGotoStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGotoStatement::initializeStorageClassArray(SgGotoStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGotoStatement::pools.begin(); block != SgGotoStatement::pools.end(); ++block) {
        SgGotoStatement* pointer{reinterpret_cast<SgGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgGotoStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 85029 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSpawnStmt that takes its corresponding StorageClass as parameter
 */
SgSpawnStmt::SgSpawnStmt(const SgSpawnStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 85039 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_the_func = (SgFunctionCallExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_the_func));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSpawnStmt*
SgSpawnStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSpawnStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpawnStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpawnStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpawnStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSpawnStmt)};
        unsigned long positionInPool{localIndex % SgSpawnStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSpawnStmt::pool_size};
        returnPointer = &(((SgSpawnStmt*)(SgSpawnStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSpawnStmt*
SgSpawnStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSpawnStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpawnStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpawnStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpawnStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSpawnStmt)};
        unsigned long positionInPool{localIndex % SgSpawnStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSpawnStmt::pool_size};
        returnPointer = &(((SgSpawnStmt*)(SgSpawnStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSpawnStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSpawnStmt::pools.begin(); block != SgSpawnStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSpawnStmt*>(*block)};
        for (unsigned i = 0; i < SgSpawnStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSpawnStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSpawnStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSpawnStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgSpawnStmt::pools.begin(); block != SgSpawnStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSpawnStmt*>(*block)};
        for (unsigned i = 0; i < SgSpawnStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSpawnStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSpawnStmt::clearMemoryPool() {
    SgSpawnStmt* tempPointer{nullptr};
    if (SgSpawnStmt::pools.empty() == false) {
        auto block{SgSpawnStmt::pools.begin()};
        SgSpawnStmt::next_node = reinterpret_cast<SgSpawnStmt*>(*block);
        for (; block != SgSpawnStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSpawnStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSpawnStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSpawnStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSpawnStmt::pool_size-1]);
        }
    }
}

void
SgSpawnStmt::deleteMemoryPool() {
    for (auto p: SgSpawnStmt::pools) {
        ROSE_FREE(p);
    }
    SgSpawnStmt::next_node = nullptr;
    SgSpawnStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSpawnStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSpawnStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpawnStmt)};
    for (size_t blockIndex = SgSpawnStmt::pools.size(); (blockIndex * SgSpawnStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSpawnStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpawnStmt) = %" PRIuPTR " SgSpawnStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSpawnStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpawnStmt),SgSpawnStmt::pool_size);
        }
#endif

        SgSpawnStmt* pointer{static_cast<SgSpawnStmt*>(ROSE_MALLOC(SgSpawnStmt::pool_size * sizeof(SgSpawnStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSpawnStmt::pool_size * sizeof(SgSpawnStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSpawnStmt::pool_size * sizeof(SgSpawnStmt));
#endif
        SgSpawnStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSpawnStmt::pool_size * sizeof(SgSpawnStmt), V_SgSpawnStmt));

        if (SgSpawnStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSpawnStmt* blkptr{reinterpret_cast<SgSpawnStmt*>(SgSpawnStmt::pools[blockIndex-1])};
                blkptr[SgSpawnStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSpawnStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSpawnStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSpawnStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSpawnStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSpawnStmt::pool_size - 1};
    SgSpawnStmt* testPointer{reinterpret_cast<SgSpawnStmt*>(SgSpawnStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSpawnStmt::pool_size * (SgSpawnStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSpawnStmtStroageClass
 * from its counterpart of SgSpawnStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSpawnStmt::initializeStorageClassArray(SgSpawnStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSpawnStmt::pools.begin(); block != SgSpawnStmt::pools.end(); ++block) {
        SgSpawnStmt* pointer{reinterpret_cast<SgSpawnStmt*>(*block)};
        for (unsigned i = 0; i < SgSpawnStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 85257 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNullStatement that takes its corresponding StorageClass as parameter
 */
SgNullStatement::SgNullStatement(const SgNullStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 85267 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNullStatement*
SgNullStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNullStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNullStatement)};
        unsigned long positionInPool{localIndex % SgNullStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullStatement::pool_size};
        returnPointer = &(((SgNullStatement*)(SgNullStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNullStatement*
SgNullStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNullStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNullStatement)};
        unsigned long positionInPool{localIndex % SgNullStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullStatement::pool_size};
        returnPointer = &(((SgNullStatement*)(SgNullStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNullStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNullStatement::pools.begin(); block != SgNullStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullStatement*>(*block)};
        for (unsigned i = 0; i < SgNullStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNullStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNullStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNullStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgNullStatement::pools.begin(); block != SgNullStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullStatement*>(*block)};
        for (unsigned i = 0; i < SgNullStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNullStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNullStatement::clearMemoryPool() {
    SgNullStatement* tempPointer{nullptr};
    if (SgNullStatement::pools.empty() == false) {
        auto block{SgNullStatement::pools.begin()};
        SgNullStatement::next_node = reinterpret_cast<SgNullStatement*>(*block);
        for (; block != SgNullStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNullStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNullStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNullStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNullStatement::pool_size-1]);
        }
    }
}

void
SgNullStatement::deleteMemoryPool() {
    for (auto p: SgNullStatement::pools) {
        ROSE_FREE(p);
    }
    SgNullStatement::next_node = nullptr;
    SgNullStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNullStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNullStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullStatement)};
    for (size_t blockIndex = SgNullStatement::pools.size(); (blockIndex * SgNullStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNullStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullStatement) = %" PRIuPTR " SgNullStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNullStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullStatement),SgNullStatement::pool_size);
        }
#endif

        SgNullStatement* pointer{static_cast<SgNullStatement*>(ROSE_MALLOC(SgNullStatement::pool_size * sizeof(SgNullStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNullStatement::pool_size * sizeof(SgNullStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNullStatement::pool_size * sizeof(SgNullStatement));
#endif
        SgNullStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNullStatement::pool_size * sizeof(SgNullStatement), V_SgNullStatement));

        if (SgNullStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNullStatement* blkptr{reinterpret_cast<SgNullStatement*>(SgNullStatement::pools[blockIndex-1])};
                blkptr[SgNullStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNullStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNullStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNullStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNullStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNullStatement::pool_size - 1};
    SgNullStatement* testPointer{reinterpret_cast<SgNullStatement*>(SgNullStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNullStatement::pool_size * (SgNullStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNullStatementStroageClass
 * from its counterpart of SgNullStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNullStatement::initializeStorageClassArray(SgNullStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNullStatement::pools.begin(); block != SgNullStatement::pools.end(); ++block) {
        SgNullStatement* pointer{reinterpret_cast<SgNullStatement*>(*block)};
        for (unsigned i = 0; i < SgNullStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 85484 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVariantStatement that takes its corresponding StorageClass as parameter
 */
SgVariantStatement::SgVariantStatement(const SgVariantStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 85494 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVariantStatement*
SgVariantStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVariantStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariantStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariantStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariantStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantStatement)};
        unsigned long positionInPool{localIndex % SgVariantStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariantStatement::pool_size};
        returnPointer = &(((SgVariantStatement*)(SgVariantStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVariantStatement*
SgVariantStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVariantStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariantStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariantStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariantStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVariantStatement)};
        unsigned long positionInPool{localIndex % SgVariantStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariantStatement::pool_size};
        returnPointer = &(((SgVariantStatement*)(SgVariantStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVariantStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVariantStatement::pools.begin(); block != SgVariantStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariantStatement*>(*block)};
        for (unsigned i = 0; i < SgVariantStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVariantStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVariantStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVariantStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgVariantStatement::pools.begin(); block != SgVariantStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariantStatement*>(*block)};
        for (unsigned i = 0; i < SgVariantStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVariantStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVariantStatement::clearMemoryPool() {
    SgVariantStatement* tempPointer{nullptr};
    if (SgVariantStatement::pools.empty() == false) {
        auto block{SgVariantStatement::pools.begin()};
        SgVariantStatement::next_node = reinterpret_cast<SgVariantStatement*>(*block);
        for (; block != SgVariantStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVariantStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVariantStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVariantStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVariantStatement::pool_size-1]);
        }
    }
}

void
SgVariantStatement::deleteMemoryPool() {
    for (auto p: SgVariantStatement::pools) {
        ROSE_FREE(p);
    }
    SgVariantStatement::next_node = nullptr;
    SgVariantStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVariantStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariantStatement)};
    for (size_t blockIndex = SgVariantStatement::pools.size(); (blockIndex * SgVariantStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariantStatement) = %" PRIuPTR " SgVariantStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariantStatement),SgVariantStatement::pool_size);
        }
#endif

        SgVariantStatement* pointer{static_cast<SgVariantStatement*>(ROSE_MALLOC(SgVariantStatement::pool_size * sizeof(SgVariantStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVariantStatement::pool_size * sizeof(SgVariantStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVariantStatement::pool_size * sizeof(SgVariantStatement));
#endif
        SgVariantStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVariantStatement::pool_size * sizeof(SgVariantStatement), V_SgVariantStatement));

        if (SgVariantStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVariantStatement* blkptr{reinterpret_cast<SgVariantStatement*>(SgVariantStatement::pools[blockIndex-1])};
                blkptr[SgVariantStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVariantStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVariantStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVariantStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVariantStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVariantStatement::pool_size - 1};
    SgVariantStatement* testPointer{reinterpret_cast<SgVariantStatement*>(SgVariantStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVariantStatement::pool_size * (SgVariantStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVariantStatementStroageClass
 * from its counterpart of SgVariantStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVariantStatement::initializeStorageClassArray(SgVariantStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVariantStatement::pools.begin(); block != SgVariantStatement::pools.end(); ++block) {
        SgVariantStatement* pointer{reinterpret_cast<SgVariantStatement*>(*block)};
        for (unsigned i = 0; i < SgVariantStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 85711 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgForInitStatement that takes its corresponding StorageClass as parameter
 */
SgForInitStatement::SgForInitStatement(const SgForInitStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 85721 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_init_stmt = storageSource.storageOf_init_stmt.rebuildDataStoredInEasyStorageClass();
    SgStatementPtrList::iterator i_init_stmt = p_init_stmt.begin();
    for ( ; i_init_stmt != p_init_stmt.end(); ++i_init_stmt) {
        (*i_init_stmt) = (SgStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_init_stmt)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgForInitStatement*
SgForInitStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgForInitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForInitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForInitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgForInitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgForInitStatement)};
        unsigned long positionInPool{localIndex % SgForInitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgForInitStatement::pool_size};
        returnPointer = &(((SgForInitStatement*)(SgForInitStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgForInitStatement*
SgForInitStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgForInitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForInitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForInitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgForInitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgForInitStatement)};
        unsigned long positionInPool{localIndex % SgForInitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgForInitStatement::pool_size};
        returnPointer = &(((SgForInitStatement*)(SgForInitStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgForInitStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgForInitStatement::pools.begin(); block != SgForInitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgForInitStatement*>(*block)};
        for (unsigned i = 0; i < SgForInitStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgForInitStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgForInitStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgForInitStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgForInitStatement::pools.begin(); block != SgForInitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgForInitStatement*>(*block)};
        for (unsigned i = 0; i < SgForInitStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgForInitStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgForInitStatement::clearMemoryPool() {
    SgForInitStatement* tempPointer{nullptr};
    if (SgForInitStatement::pools.empty() == false) {
        auto block{SgForInitStatement::pools.begin()};
        SgForInitStatement::next_node = reinterpret_cast<SgForInitStatement*>(*block);
        for (; block != SgForInitStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgForInitStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgForInitStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgForInitStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgForInitStatement::pool_size-1]);
        }
    }
}

void
SgForInitStatement::deleteMemoryPool() {
    for (auto p: SgForInitStatement::pools) {
        ROSE_FREE(p);
    }
    SgForInitStatement::next_node = nullptr;
    SgForInitStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgForInitStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgForInitStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgForInitStatement)};
    for (size_t blockIndex = SgForInitStatement::pools.size(); (blockIndex * SgForInitStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgForInitStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgForInitStatement) = %" PRIuPTR " SgForInitStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgForInitStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgForInitStatement),SgForInitStatement::pool_size);
        }
#endif

        SgForInitStatement* pointer{static_cast<SgForInitStatement*>(ROSE_MALLOC(SgForInitStatement::pool_size * sizeof(SgForInitStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgForInitStatement::pool_size * sizeof(SgForInitStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgForInitStatement::pool_size * sizeof(SgForInitStatement));
#endif
        SgForInitStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgForInitStatement::pool_size * sizeof(SgForInitStatement), V_SgForInitStatement));

        if (SgForInitStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgForInitStatement* blkptr{reinterpret_cast<SgForInitStatement*>(SgForInitStatement::pools[blockIndex-1])};
                blkptr[SgForInitStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgForInitStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgForInitStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgForInitStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgForInitStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgForInitStatement::pool_size - 1};
    SgForInitStatement* testPointer{reinterpret_cast<SgForInitStatement*>(SgForInitStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgForInitStatement::pool_size * (SgForInitStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgForInitStatementStroageClass
 * from its counterpart of SgForInitStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgForInitStatement::initializeStorageClassArray(SgForInitStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgForInitStatement::pools.begin(); block != SgForInitStatement::pools.end(); ++block) {
        SgForInitStatement* pointer{reinterpret_cast<SgForInitStatement*>(*block)};
        for (unsigned i = 0; i < SgForInitStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 85943 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCatchStatementSeq that takes its corresponding StorageClass as parameter
 */
SgCatchStatementSeq::SgCatchStatementSeq(const SgCatchStatementSeqStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 85953 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_catch_statement_seq = storageSource.storageOf_catch_statement_seq.rebuildDataStoredInEasyStorageClass();
    SgStatementPtrList::iterator i_catch_statement_seq = p_catch_statement_seq.begin();
    for ( ; i_catch_statement_seq != p_catch_statement_seq.end(); ++i_catch_statement_seq) {
        (*i_catch_statement_seq) = (SgStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_catch_statement_seq)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCatchStatementSeq*
SgCatchStatementSeq::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCatchStatementSeq* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCatchStatementSeq)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCatchStatementSeq + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCatchStatementSeq)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchStatementSeq)};
        unsigned long positionInPool{localIndex % SgCatchStatementSeq::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCatchStatementSeq::pool_size};
        returnPointer = &(((SgCatchStatementSeq*)(SgCatchStatementSeq::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCatchStatementSeq*
SgCatchStatementSeq::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCatchStatementSeq* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCatchStatementSeq)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCatchStatementSeq + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCatchStatementSeq)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCatchStatementSeq)};
        unsigned long positionInPool{localIndex % SgCatchStatementSeq::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCatchStatementSeq::pool_size};
        returnPointer = &(((SgCatchStatementSeq*)(SgCatchStatementSeq::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCatchStatementSeq::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCatchStatementSeq::pools.begin(); block != SgCatchStatementSeq::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCatchStatementSeq*>(*block)};
        for (unsigned i = 0; i < SgCatchStatementSeq::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCatchStatementSeq*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCatchStatementSeq::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCatchStatementSeq* pointerOfLinkedList{nullptr};
    for (auto block = SgCatchStatementSeq::pools.begin(); block != SgCatchStatementSeq::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCatchStatementSeq*>(*block)};
        for (unsigned i = 0; i < SgCatchStatementSeq::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCatchStatementSeq::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCatchStatementSeq::clearMemoryPool() {
    SgCatchStatementSeq* tempPointer{nullptr};
    if (SgCatchStatementSeq::pools.empty() == false) {
        auto block{SgCatchStatementSeq::pools.begin()};
        SgCatchStatementSeq::next_node = reinterpret_cast<SgCatchStatementSeq*>(*block);
        for (; block != SgCatchStatementSeq::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCatchStatementSeq*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCatchStatementSeq::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCatchStatementSeq::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCatchStatementSeq::pool_size-1]);
        }
    }
}

void
SgCatchStatementSeq::deleteMemoryPool() {
    for (auto p: SgCatchStatementSeq::pools) {
        ROSE_FREE(p);
    }
    SgCatchStatementSeq::next_node = nullptr;
    SgCatchStatementSeq::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCatchStatementSeq::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchStatementSeq) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCatchStatementSeq)};
    for (size_t blockIndex = SgCatchStatementSeq::pools.size(); (blockIndex * SgCatchStatementSeq::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchStatementSeq) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCatchStatementSeq) = %" PRIuPTR " SgCatchStatementSeq::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCatchStatementSeq),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCatchStatementSeq),SgCatchStatementSeq::pool_size);
        }
#endif

        SgCatchStatementSeq* pointer{static_cast<SgCatchStatementSeq*>(ROSE_MALLOC(SgCatchStatementSeq::pool_size * sizeof(SgCatchStatementSeq)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCatchStatementSeq::pool_size * sizeof(SgCatchStatementSeq));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCatchStatementSeq::pool_size * sizeof(SgCatchStatementSeq));
#endif
        SgCatchStatementSeq::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCatchStatementSeq::pool_size * sizeof(SgCatchStatementSeq), V_SgCatchStatementSeq));

        if (SgCatchStatementSeq::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCatchStatementSeq* blkptr{reinterpret_cast<SgCatchStatementSeq*>(SgCatchStatementSeq::pools[blockIndex-1])};
                blkptr[SgCatchStatementSeq::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCatchStatementSeq::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCatchStatementSeq::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCatchStatementSeq::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCatchStatementSeq::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCatchStatementSeq::pool_size - 1};
    SgCatchStatementSeq* testPointer{reinterpret_cast<SgCatchStatementSeq*>(SgCatchStatementSeq::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCatchStatementSeq::pool_size * (SgCatchStatementSeq::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCatchStatementSeqStroageClass
 * from its counterpart of SgCatchStatementSeq. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCatchStatementSeq::initializeStorageClassArray(SgCatchStatementSeqStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCatchStatementSeq::pools.begin(); block != SgCatchStatementSeq::pools.end(); ++block) {
        SgCatchStatementSeq* pointer{reinterpret_cast<SgCatchStatementSeq*>(*block)};
        for (unsigned i = 0; i < SgCatchStatementSeq::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 86175 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgProcessControlStatement that takes its corresponding StorageClass as parameter
 */
SgProcessControlStatement::SgProcessControlStatement(const SgProcessControlStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 86185 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_control_kind = storageSource.storageOf_control_kind;
     p_code = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_code));
     p_quiet = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_quiet));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgProcessControlStatement*
SgProcessControlStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgProcessControlStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProcessControlStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProcessControlStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgProcessControlStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgProcessControlStatement)};
        unsigned long positionInPool{localIndex % SgProcessControlStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProcessControlStatement::pool_size};
        returnPointer = &(((SgProcessControlStatement*)(SgProcessControlStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgProcessControlStatement*
SgProcessControlStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgProcessControlStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProcessControlStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProcessControlStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgProcessControlStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgProcessControlStatement)};
        unsigned long positionInPool{localIndex % SgProcessControlStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgProcessControlStatement::pool_size};
        returnPointer = &(((SgProcessControlStatement*)(SgProcessControlStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgProcessControlStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgProcessControlStatement::pools.begin(); block != SgProcessControlStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProcessControlStatement*>(*block)};
        for (unsigned i = 0; i < SgProcessControlStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgProcessControlStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgProcessControlStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgProcessControlStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgProcessControlStatement::pools.begin(); block != SgProcessControlStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgProcessControlStatement*>(*block)};
        for (unsigned i = 0; i < SgProcessControlStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgProcessControlStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgProcessControlStatement::clearMemoryPool() {
    SgProcessControlStatement* tempPointer{nullptr};
    if (SgProcessControlStatement::pools.empty() == false) {
        auto block{SgProcessControlStatement::pools.begin()};
        SgProcessControlStatement::next_node = reinterpret_cast<SgProcessControlStatement*>(*block);
        for (; block != SgProcessControlStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgProcessControlStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgProcessControlStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgProcessControlStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgProcessControlStatement::pool_size-1]);
        }
    }
}

void
SgProcessControlStatement::deleteMemoryPool() {
    for (auto p: SgProcessControlStatement::pools) {
        ROSE_FREE(p);
    }
    SgProcessControlStatement::next_node = nullptr;
    SgProcessControlStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgProcessControlStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgProcessControlStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgProcessControlStatement)};
    for (size_t blockIndex = SgProcessControlStatement::pools.size(); (blockIndex * SgProcessControlStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgProcessControlStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgProcessControlStatement) = %" PRIuPTR " SgProcessControlStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgProcessControlStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgProcessControlStatement),SgProcessControlStatement::pool_size);
        }
#endif

        SgProcessControlStatement* pointer{static_cast<SgProcessControlStatement*>(ROSE_MALLOC(SgProcessControlStatement::pool_size * sizeof(SgProcessControlStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgProcessControlStatement::pool_size * sizeof(SgProcessControlStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgProcessControlStatement::pool_size * sizeof(SgProcessControlStatement));
#endif
        SgProcessControlStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgProcessControlStatement::pool_size * sizeof(SgProcessControlStatement), V_SgProcessControlStatement));

        if (SgProcessControlStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgProcessControlStatement* blkptr{reinterpret_cast<SgProcessControlStatement*>(SgProcessControlStatement::pools[blockIndex-1])};
                blkptr[SgProcessControlStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgProcessControlStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgProcessControlStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgProcessControlStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgProcessControlStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgProcessControlStatement::pool_size - 1};
    SgProcessControlStatement* testPointer{reinterpret_cast<SgProcessControlStatement*>(SgProcessControlStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgProcessControlStatement::pool_size * (SgProcessControlStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgProcessControlStatementStroageClass
 * from its counterpart of SgProcessControlStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgProcessControlStatement::initializeStorageClassArray(SgProcessControlStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgProcessControlStatement::pools.begin(); block != SgProcessControlStatement::pools.end(); ++block) {
        SgProcessControlStatement* pointer{reinterpret_cast<SgProcessControlStatement*>(*block)};
        for (unsigned i = 0; i < SgProcessControlStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 86405 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIOStatement that takes its corresponding StorageClass as parameter
 */
SgIOStatement::SgIOStatement(const SgIOStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 86415 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_io_statement = storageSource.storageOf_io_statement;
     p_io_stmt_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_io_stmt_list));
     p_unit = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_unit));
     p_iostat = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_iostat));
     p_err = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_err));
     p_iomsg = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_iomsg));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIOStatement*
SgIOStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIOStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIOStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIOStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIOStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIOStatement)};
        unsigned long positionInPool{localIndex % SgIOStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIOStatement::pool_size};
        returnPointer = &(((SgIOStatement*)(SgIOStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIOStatement*
SgIOStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIOStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIOStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIOStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIOStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIOStatement)};
        unsigned long positionInPool{localIndex % SgIOStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIOStatement::pool_size};
        returnPointer = &(((SgIOStatement*)(SgIOStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIOStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIOStatement::pools.begin(); block != SgIOStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIOStatement*>(*block)};
        for (unsigned i = 0; i < SgIOStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIOStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIOStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIOStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgIOStatement::pools.begin(); block != SgIOStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIOStatement*>(*block)};
        for (unsigned i = 0; i < SgIOStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIOStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIOStatement::clearMemoryPool() {
    SgIOStatement* tempPointer{nullptr};
    if (SgIOStatement::pools.empty() == false) {
        auto block{SgIOStatement::pools.begin()};
        SgIOStatement::next_node = reinterpret_cast<SgIOStatement*>(*block);
        for (; block != SgIOStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIOStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIOStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIOStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIOStatement::pool_size-1]);
        }
    }
}

void
SgIOStatement::deleteMemoryPool() {
    for (auto p: SgIOStatement::pools) {
        ROSE_FREE(p);
    }
    SgIOStatement::next_node = nullptr;
    SgIOStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIOStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIOStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIOStatement)};
    for (size_t blockIndex = SgIOStatement::pools.size(); (blockIndex * SgIOStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIOStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIOStatement) = %" PRIuPTR " SgIOStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIOStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIOStatement),SgIOStatement::pool_size);
        }
#endif

        SgIOStatement* pointer{static_cast<SgIOStatement*>(ROSE_MALLOC(SgIOStatement::pool_size * sizeof(SgIOStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIOStatement::pool_size * sizeof(SgIOStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIOStatement::pool_size * sizeof(SgIOStatement));
#endif
        SgIOStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIOStatement::pool_size * sizeof(SgIOStatement), V_SgIOStatement));

        if (SgIOStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIOStatement* blkptr{reinterpret_cast<SgIOStatement*>(SgIOStatement::pools[blockIndex-1])};
                blkptr[SgIOStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIOStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIOStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIOStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIOStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIOStatement::pool_size - 1};
    SgIOStatement* testPointer{reinterpret_cast<SgIOStatement*>(SgIOStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIOStatement::pool_size * (SgIOStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIOStatementStroageClass
 * from its counterpart of SgIOStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIOStatement::initializeStorageClassArray(SgIOStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIOStatement::pools.begin(); block != SgIOStatement::pools.end(); ++block) {
        SgIOStatement* pointer{reinterpret_cast<SgIOStatement*>(*block)};
        for (unsigned i = 0; i < SgIOStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 86638 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPrintStatement that takes its corresponding StorageClass as parameter
 */
SgPrintStatement::SgPrintStatement(const SgPrintStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 86648 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_format = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_format));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPrintStatement*
SgPrintStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPrintStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPrintStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPrintStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPrintStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPrintStatement)};
        unsigned long positionInPool{localIndex % SgPrintStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPrintStatement::pool_size};
        returnPointer = &(((SgPrintStatement*)(SgPrintStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPrintStatement*
SgPrintStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPrintStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPrintStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPrintStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPrintStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPrintStatement)};
        unsigned long positionInPool{localIndex % SgPrintStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPrintStatement::pool_size};
        returnPointer = &(((SgPrintStatement*)(SgPrintStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPrintStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPrintStatement::pools.begin(); block != SgPrintStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPrintStatement*>(*block)};
        for (unsigned i = 0; i < SgPrintStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPrintStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPrintStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPrintStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgPrintStatement::pools.begin(); block != SgPrintStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPrintStatement*>(*block)};
        for (unsigned i = 0; i < SgPrintStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPrintStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPrintStatement::clearMemoryPool() {
    SgPrintStatement* tempPointer{nullptr};
    if (SgPrintStatement::pools.empty() == false) {
        auto block{SgPrintStatement::pools.begin()};
        SgPrintStatement::next_node = reinterpret_cast<SgPrintStatement*>(*block);
        for (; block != SgPrintStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPrintStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPrintStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPrintStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPrintStatement::pool_size-1]);
        }
    }
}

void
SgPrintStatement::deleteMemoryPool() {
    for (auto p: SgPrintStatement::pools) {
        ROSE_FREE(p);
    }
    SgPrintStatement::next_node = nullptr;
    SgPrintStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPrintStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPrintStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPrintStatement)};
    for (size_t blockIndex = SgPrintStatement::pools.size(); (blockIndex * SgPrintStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPrintStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPrintStatement) = %" PRIuPTR " SgPrintStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPrintStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPrintStatement),SgPrintStatement::pool_size);
        }
#endif

        SgPrintStatement* pointer{static_cast<SgPrintStatement*>(ROSE_MALLOC(SgPrintStatement::pool_size * sizeof(SgPrintStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPrintStatement::pool_size * sizeof(SgPrintStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPrintStatement::pool_size * sizeof(SgPrintStatement));
#endif
        SgPrintStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPrintStatement::pool_size * sizeof(SgPrintStatement), V_SgPrintStatement));

        if (SgPrintStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPrintStatement* blkptr{reinterpret_cast<SgPrintStatement*>(SgPrintStatement::pools[blockIndex-1])};
                blkptr[SgPrintStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPrintStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPrintStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPrintStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPrintStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPrintStatement::pool_size - 1};
    SgPrintStatement* testPointer{reinterpret_cast<SgPrintStatement*>(SgPrintStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPrintStatement::pool_size * (SgPrintStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPrintStatementStroageClass
 * from its counterpart of SgPrintStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPrintStatement::initializeStorageClassArray(SgPrintStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPrintStatement::pools.begin(); block != SgPrintStatement::pools.end(); ++block) {
        SgPrintStatement* pointer{reinterpret_cast<SgPrintStatement*>(*block)};
        for (unsigned i = 0; i < SgPrintStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 86866 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgReadStatement that takes its corresponding StorageClass as parameter
 */
SgReadStatement::SgReadStatement(const SgReadStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 86876 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_format = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_format));
     p_namelist = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_namelist));
     p_advance = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_advance));
     p_asynchronous = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_asynchronous));
     p_blank = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_blank));
     p_decimal = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_decimal));
     p_delim = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_delim));
     p_end = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end));
     p_eor = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_eor));
     p_id = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_id));
     p_pad = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pad));
     p_pos = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pos));
     p_rec = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rec));
     p_round = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_round));
     p_sign = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sign));
     p_size = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_size));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgReadStatement*
SgReadStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgReadStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReadStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReadStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReadStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgReadStatement)};
        unsigned long positionInPool{localIndex % SgReadStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReadStatement::pool_size};
        returnPointer = &(((SgReadStatement*)(SgReadStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgReadStatement*
SgReadStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgReadStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReadStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReadStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReadStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgReadStatement)};
        unsigned long positionInPool{localIndex % SgReadStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReadStatement::pool_size};
        returnPointer = &(((SgReadStatement*)(SgReadStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgReadStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgReadStatement::pools.begin(); block != SgReadStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReadStatement*>(*block)};
        for (unsigned i = 0; i < SgReadStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgReadStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgReadStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgReadStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgReadStatement::pools.begin(); block != SgReadStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReadStatement*>(*block)};
        for (unsigned i = 0; i < SgReadStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgReadStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgReadStatement::clearMemoryPool() {
    SgReadStatement* tempPointer{nullptr};
    if (SgReadStatement::pools.empty() == false) {
        auto block{SgReadStatement::pools.begin()};
        SgReadStatement::next_node = reinterpret_cast<SgReadStatement*>(*block);
        for (; block != SgReadStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgReadStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgReadStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgReadStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgReadStatement::pool_size-1]);
        }
    }
}

void
SgReadStatement::deleteMemoryPool() {
    for (auto p: SgReadStatement::pools) {
        ROSE_FREE(p);
    }
    SgReadStatement::next_node = nullptr;
    SgReadStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgReadStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgReadStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgReadStatement)};
    for (size_t blockIndex = SgReadStatement::pools.size(); (blockIndex * SgReadStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgReadStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgReadStatement) = %" PRIuPTR " SgReadStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgReadStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgReadStatement),SgReadStatement::pool_size);
        }
#endif

        SgReadStatement* pointer{static_cast<SgReadStatement*>(ROSE_MALLOC(SgReadStatement::pool_size * sizeof(SgReadStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgReadStatement::pool_size * sizeof(SgReadStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgReadStatement::pool_size * sizeof(SgReadStatement));
#endif
        SgReadStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgReadStatement::pool_size * sizeof(SgReadStatement), V_SgReadStatement));

        if (SgReadStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgReadStatement* blkptr{reinterpret_cast<SgReadStatement*>(SgReadStatement::pools[blockIndex-1])};
                blkptr[SgReadStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgReadStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgReadStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgReadStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgReadStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgReadStatement::pool_size - 1};
    SgReadStatement* testPointer{reinterpret_cast<SgReadStatement*>(SgReadStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgReadStatement::pool_size * (SgReadStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgReadStatementStroageClass
 * from its counterpart of SgReadStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgReadStatement::initializeStorageClassArray(SgReadStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgReadStatement::pools.begin(); block != SgReadStatement::pools.end(); ++block) {
        SgReadStatement* pointer{reinterpret_cast<SgReadStatement*>(*block)};
        for (unsigned i = 0; i < SgReadStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 87109 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWriteStatement that takes its corresponding StorageClass as parameter
 */
SgWriteStatement::SgWriteStatement(const SgWriteStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 87119 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_format = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_format));
     p_namelist = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_namelist));
     p_advance = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_advance));
     p_asynchronous = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_asynchronous));
     p_blank = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_blank));
     p_decimal = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_decimal));
     p_delim = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_delim));
     p_end = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end));
     p_eor = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_eor));
     p_id = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_id));
     p_pad = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pad));
     p_pos = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pos));
     p_rec = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rec));
     p_round = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_round));
     p_sign = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sign));
     p_size = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_size));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWriteStatement*
SgWriteStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWriteStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWriteStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWriteStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWriteStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWriteStatement)};
        unsigned long positionInPool{localIndex % SgWriteStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWriteStatement::pool_size};
        returnPointer = &(((SgWriteStatement*)(SgWriteStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWriteStatement*
SgWriteStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWriteStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWriteStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWriteStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWriteStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWriteStatement)};
        unsigned long positionInPool{localIndex % SgWriteStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWriteStatement::pool_size};
        returnPointer = &(((SgWriteStatement*)(SgWriteStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWriteStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWriteStatement::pools.begin(); block != SgWriteStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWriteStatement*>(*block)};
        for (unsigned i = 0; i < SgWriteStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWriteStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWriteStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWriteStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgWriteStatement::pools.begin(); block != SgWriteStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWriteStatement*>(*block)};
        for (unsigned i = 0; i < SgWriteStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWriteStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWriteStatement::clearMemoryPool() {
    SgWriteStatement* tempPointer{nullptr};
    if (SgWriteStatement::pools.empty() == false) {
        auto block{SgWriteStatement::pools.begin()};
        SgWriteStatement::next_node = reinterpret_cast<SgWriteStatement*>(*block);
        for (; block != SgWriteStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWriteStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWriteStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWriteStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWriteStatement::pool_size-1]);
        }
    }
}

void
SgWriteStatement::deleteMemoryPool() {
    for (auto p: SgWriteStatement::pools) {
        ROSE_FREE(p);
    }
    SgWriteStatement::next_node = nullptr;
    SgWriteStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWriteStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWriteStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWriteStatement)};
    for (size_t blockIndex = SgWriteStatement::pools.size(); (blockIndex * SgWriteStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWriteStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWriteStatement) = %" PRIuPTR " SgWriteStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWriteStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWriteStatement),SgWriteStatement::pool_size);
        }
#endif

        SgWriteStatement* pointer{static_cast<SgWriteStatement*>(ROSE_MALLOC(SgWriteStatement::pool_size * sizeof(SgWriteStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWriteStatement::pool_size * sizeof(SgWriteStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWriteStatement::pool_size * sizeof(SgWriteStatement));
#endif
        SgWriteStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWriteStatement::pool_size * sizeof(SgWriteStatement), V_SgWriteStatement));

        if (SgWriteStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWriteStatement* blkptr{reinterpret_cast<SgWriteStatement*>(SgWriteStatement::pools[blockIndex-1])};
                blkptr[SgWriteStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWriteStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWriteStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWriteStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWriteStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWriteStatement::pool_size - 1};
    SgWriteStatement* testPointer{reinterpret_cast<SgWriteStatement*>(SgWriteStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWriteStatement::pool_size * (SgWriteStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWriteStatementStroageClass
 * from its counterpart of SgWriteStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWriteStatement::initializeStorageClassArray(SgWriteStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWriteStatement::pools.begin(); block != SgWriteStatement::pools.end(); ++block) {
        SgWriteStatement* pointer{reinterpret_cast<SgWriteStatement*>(*block)};
        for (unsigned i = 0; i < SgWriteStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 87352 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOpenStatement that takes its corresponding StorageClass as parameter
 */
SgOpenStatement::SgOpenStatement(const SgOpenStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 87362 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_file = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_file));
     p_status = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_status));
     p_access = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_access));
     p_form = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_form));
     p_recl = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_recl));
     p_blank = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_blank));
     p_position = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_position));
     p_action = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_action));
     p_delim = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_delim));
     p_pad = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pad));
     p_round = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_round));
     p_sign = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sign));
     p_asynchronous = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_asynchronous));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOpenStatement*
SgOpenStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOpenStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOpenStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOpenStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOpenStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenStatement)};
        unsigned long positionInPool{localIndex % SgOpenStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOpenStatement::pool_size};
        returnPointer = &(((SgOpenStatement*)(SgOpenStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOpenStatement*
SgOpenStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOpenStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOpenStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOpenStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOpenStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOpenStatement)};
        unsigned long positionInPool{localIndex % SgOpenStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOpenStatement::pool_size};
        returnPointer = &(((SgOpenStatement*)(SgOpenStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOpenStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOpenStatement::pools.begin(); block != SgOpenStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOpenStatement*>(*block)};
        for (unsigned i = 0; i < SgOpenStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOpenStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOpenStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOpenStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOpenStatement::pools.begin(); block != SgOpenStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOpenStatement*>(*block)};
        for (unsigned i = 0; i < SgOpenStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOpenStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOpenStatement::clearMemoryPool() {
    SgOpenStatement* tempPointer{nullptr};
    if (SgOpenStatement::pools.empty() == false) {
        auto block{SgOpenStatement::pools.begin()};
        SgOpenStatement::next_node = reinterpret_cast<SgOpenStatement*>(*block);
        for (; block != SgOpenStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOpenStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOpenStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOpenStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOpenStatement::pool_size-1]);
        }
    }
}

void
SgOpenStatement::deleteMemoryPool() {
    for (auto p: SgOpenStatement::pools) {
        ROSE_FREE(p);
    }
    SgOpenStatement::next_node = nullptr;
    SgOpenStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOpenStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOpenStatement)};
    for (size_t blockIndex = SgOpenStatement::pools.size(); (blockIndex * SgOpenStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOpenStatement) = %" PRIuPTR " SgOpenStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOpenStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOpenStatement),SgOpenStatement::pool_size);
        }
#endif

        SgOpenStatement* pointer{static_cast<SgOpenStatement*>(ROSE_MALLOC(SgOpenStatement::pool_size * sizeof(SgOpenStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOpenStatement::pool_size * sizeof(SgOpenStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOpenStatement::pool_size * sizeof(SgOpenStatement));
#endif
        SgOpenStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOpenStatement::pool_size * sizeof(SgOpenStatement), V_SgOpenStatement));

        if (SgOpenStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOpenStatement* blkptr{reinterpret_cast<SgOpenStatement*>(SgOpenStatement::pools[blockIndex-1])};
                blkptr[SgOpenStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOpenStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOpenStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOpenStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOpenStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOpenStatement::pool_size - 1};
    SgOpenStatement* testPointer{reinterpret_cast<SgOpenStatement*>(SgOpenStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOpenStatement::pool_size * (SgOpenStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOpenStatementStroageClass
 * from its counterpart of SgOpenStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOpenStatement::initializeStorageClassArray(SgOpenStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOpenStatement::pools.begin(); block != SgOpenStatement::pools.end(); ++block) {
        SgOpenStatement* pointer{reinterpret_cast<SgOpenStatement*>(*block)};
        for (unsigned i = 0; i < SgOpenStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 87592 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCloseStatement that takes its corresponding StorageClass as parameter
 */
SgCloseStatement::SgCloseStatement(const SgCloseStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 87602 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_status = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_status));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCloseStatement*
SgCloseStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCloseStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCloseStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCloseStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCloseStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCloseStatement)};
        unsigned long positionInPool{localIndex % SgCloseStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCloseStatement::pool_size};
        returnPointer = &(((SgCloseStatement*)(SgCloseStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCloseStatement*
SgCloseStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCloseStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCloseStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCloseStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCloseStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCloseStatement)};
        unsigned long positionInPool{localIndex % SgCloseStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCloseStatement::pool_size};
        returnPointer = &(((SgCloseStatement*)(SgCloseStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCloseStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCloseStatement::pools.begin(); block != SgCloseStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCloseStatement*>(*block)};
        for (unsigned i = 0; i < SgCloseStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCloseStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCloseStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCloseStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgCloseStatement::pools.begin(); block != SgCloseStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCloseStatement*>(*block)};
        for (unsigned i = 0; i < SgCloseStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCloseStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCloseStatement::clearMemoryPool() {
    SgCloseStatement* tempPointer{nullptr};
    if (SgCloseStatement::pools.empty() == false) {
        auto block{SgCloseStatement::pools.begin()};
        SgCloseStatement::next_node = reinterpret_cast<SgCloseStatement*>(*block);
        for (; block != SgCloseStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCloseStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCloseStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCloseStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCloseStatement::pool_size-1]);
        }
    }
}

void
SgCloseStatement::deleteMemoryPool() {
    for (auto p: SgCloseStatement::pools) {
        ROSE_FREE(p);
    }
    SgCloseStatement::next_node = nullptr;
    SgCloseStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCloseStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCloseStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCloseStatement)};
    for (size_t blockIndex = SgCloseStatement::pools.size(); (blockIndex * SgCloseStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCloseStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCloseStatement) = %" PRIuPTR " SgCloseStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCloseStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCloseStatement),SgCloseStatement::pool_size);
        }
#endif

        SgCloseStatement* pointer{static_cast<SgCloseStatement*>(ROSE_MALLOC(SgCloseStatement::pool_size * sizeof(SgCloseStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCloseStatement::pool_size * sizeof(SgCloseStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCloseStatement::pool_size * sizeof(SgCloseStatement));
#endif
        SgCloseStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCloseStatement::pool_size * sizeof(SgCloseStatement), V_SgCloseStatement));

        if (SgCloseStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCloseStatement* blkptr{reinterpret_cast<SgCloseStatement*>(SgCloseStatement::pools[blockIndex-1])};
                blkptr[SgCloseStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCloseStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCloseStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCloseStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCloseStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCloseStatement::pool_size - 1};
    SgCloseStatement* testPointer{reinterpret_cast<SgCloseStatement*>(SgCloseStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCloseStatement::pool_size * (SgCloseStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCloseStatementStroageClass
 * from its counterpart of SgCloseStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCloseStatement::initializeStorageClassArray(SgCloseStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCloseStatement::pools.begin(); block != SgCloseStatement::pools.end(); ++block) {
        SgCloseStatement* pointer{reinterpret_cast<SgCloseStatement*>(*block)};
        for (unsigned i = 0; i < SgCloseStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 87820 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgInquireStatement that takes its corresponding StorageClass as parameter
 */
SgInquireStatement::SgInquireStatement(const SgInquireStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 87830 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_file = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_file));
     p_access = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_access));
     p_form = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_form));
     p_recl = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_recl));
     p_blank = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_blank));
     p_exist = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_exist));
     p_opened = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_opened));
     p_number = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_number));
     p_named = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_named));
     p_name = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));
     p_sequential = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sequential));
     p_direct = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_direct));
     p_formatted = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_formatted));
     p_unformatted = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_unformatted));
     p_nextrec = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nextrec));
     p_position = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_position));
     p_action = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_action));
     p_read = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_read));
     p_write = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_write));
     p_readwrite = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_readwrite));
     p_delim = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_delim));
     p_pad = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pad));
     p_asynchronous = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_asynchronous));
     p_decimal = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_decimal));
     p_stream = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_stream));
     p_size = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_size));
     p_pending = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pending));
     p_iolengthExp = (SgVarRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_iolengthExp));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgInquireStatement*
SgInquireStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgInquireStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInquireStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInquireStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInquireStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgInquireStatement)};
        unsigned long positionInPool{localIndex % SgInquireStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInquireStatement::pool_size};
        returnPointer = &(((SgInquireStatement*)(SgInquireStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgInquireStatement*
SgInquireStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgInquireStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInquireStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInquireStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInquireStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgInquireStatement)};
        unsigned long positionInPool{localIndex % SgInquireStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInquireStatement::pool_size};
        returnPointer = &(((SgInquireStatement*)(SgInquireStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgInquireStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgInquireStatement::pools.begin(); block != SgInquireStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInquireStatement*>(*block)};
        for (unsigned i = 0; i < SgInquireStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgInquireStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgInquireStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgInquireStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgInquireStatement::pools.begin(); block != SgInquireStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInquireStatement*>(*block)};
        for (unsigned i = 0; i < SgInquireStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgInquireStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgInquireStatement::clearMemoryPool() {
    SgInquireStatement* tempPointer{nullptr};
    if (SgInquireStatement::pools.empty() == false) {
        auto block{SgInquireStatement::pools.begin()};
        SgInquireStatement::next_node = reinterpret_cast<SgInquireStatement*>(*block);
        for (; block != SgInquireStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgInquireStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgInquireStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgInquireStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgInquireStatement::pool_size-1]);
        }
    }
}

void
SgInquireStatement::deleteMemoryPool() {
    for (auto p: SgInquireStatement::pools) {
        ROSE_FREE(p);
    }
    SgInquireStatement::next_node = nullptr;
    SgInquireStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgInquireStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgInquireStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgInquireStatement)};
    for (size_t blockIndex = SgInquireStatement::pools.size(); (blockIndex * SgInquireStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgInquireStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgInquireStatement) = %" PRIuPTR " SgInquireStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgInquireStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgInquireStatement),SgInquireStatement::pool_size);
        }
#endif

        SgInquireStatement* pointer{static_cast<SgInquireStatement*>(ROSE_MALLOC(SgInquireStatement::pool_size * sizeof(SgInquireStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgInquireStatement::pool_size * sizeof(SgInquireStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgInquireStatement::pool_size * sizeof(SgInquireStatement));
#endif
        SgInquireStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgInquireStatement::pool_size * sizeof(SgInquireStatement), V_SgInquireStatement));

        if (SgInquireStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgInquireStatement* blkptr{reinterpret_cast<SgInquireStatement*>(SgInquireStatement::pools[blockIndex-1])};
                blkptr[SgInquireStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgInquireStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgInquireStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgInquireStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgInquireStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgInquireStatement::pool_size - 1};
    SgInquireStatement* testPointer{reinterpret_cast<SgInquireStatement*>(SgInquireStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgInquireStatement::pool_size * (SgInquireStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgInquireStatementStroageClass
 * from its counterpart of SgInquireStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgInquireStatement::initializeStorageClassArray(SgInquireStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgInquireStatement::pools.begin(); block != SgInquireStatement::pools.end(); ++block) {
        SgInquireStatement* pointer{reinterpret_cast<SgInquireStatement*>(*block)};
        for (unsigned i = 0; i < SgInquireStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 88075 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFlushStatement that takes its corresponding StorageClass as parameter
 */
SgFlushStatement::SgFlushStatement(const SgFlushStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 88085 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFlushStatement*
SgFlushStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFlushStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFlushStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFlushStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFlushStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFlushStatement)};
        unsigned long positionInPool{localIndex % SgFlushStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFlushStatement::pool_size};
        returnPointer = &(((SgFlushStatement*)(SgFlushStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFlushStatement*
SgFlushStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFlushStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFlushStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFlushStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFlushStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFlushStatement)};
        unsigned long positionInPool{localIndex % SgFlushStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFlushStatement::pool_size};
        returnPointer = &(((SgFlushStatement*)(SgFlushStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFlushStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFlushStatement::pools.begin(); block != SgFlushStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFlushStatement*>(*block)};
        for (unsigned i = 0; i < SgFlushStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFlushStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFlushStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFlushStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgFlushStatement::pools.begin(); block != SgFlushStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFlushStatement*>(*block)};
        for (unsigned i = 0; i < SgFlushStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFlushStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFlushStatement::clearMemoryPool() {
    SgFlushStatement* tempPointer{nullptr};
    if (SgFlushStatement::pools.empty() == false) {
        auto block{SgFlushStatement::pools.begin()};
        SgFlushStatement::next_node = reinterpret_cast<SgFlushStatement*>(*block);
        for (; block != SgFlushStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFlushStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFlushStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFlushStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFlushStatement::pool_size-1]);
        }
    }
}

void
SgFlushStatement::deleteMemoryPool() {
    for (auto p: SgFlushStatement::pools) {
        ROSE_FREE(p);
    }
    SgFlushStatement::next_node = nullptr;
    SgFlushStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFlushStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFlushStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFlushStatement)};
    for (size_t blockIndex = SgFlushStatement::pools.size(); (blockIndex * SgFlushStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFlushStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFlushStatement) = %" PRIuPTR " SgFlushStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFlushStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFlushStatement),SgFlushStatement::pool_size);
        }
#endif

        SgFlushStatement* pointer{static_cast<SgFlushStatement*>(ROSE_MALLOC(SgFlushStatement::pool_size * sizeof(SgFlushStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFlushStatement::pool_size * sizeof(SgFlushStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFlushStatement::pool_size * sizeof(SgFlushStatement));
#endif
        SgFlushStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFlushStatement::pool_size * sizeof(SgFlushStatement), V_SgFlushStatement));

        if (SgFlushStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFlushStatement* blkptr{reinterpret_cast<SgFlushStatement*>(SgFlushStatement::pools[blockIndex-1])};
                blkptr[SgFlushStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFlushStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFlushStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFlushStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFlushStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFlushStatement::pool_size - 1};
    SgFlushStatement* testPointer{reinterpret_cast<SgFlushStatement*>(SgFlushStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFlushStatement::pool_size * (SgFlushStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFlushStatementStroageClass
 * from its counterpart of SgFlushStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFlushStatement::initializeStorageClassArray(SgFlushStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFlushStatement::pools.begin(); block != SgFlushStatement::pools.end(); ++block) {
        SgFlushStatement* pointer{reinterpret_cast<SgFlushStatement*>(*block)};
        for (unsigned i = 0; i < SgFlushStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 88302 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBackspaceStatement that takes its corresponding StorageClass as parameter
 */
SgBackspaceStatement::SgBackspaceStatement(const SgBackspaceStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 88312 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBackspaceStatement*
SgBackspaceStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBackspaceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBackspaceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBackspaceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBackspaceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBackspaceStatement)};
        unsigned long positionInPool{localIndex % SgBackspaceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBackspaceStatement::pool_size};
        returnPointer = &(((SgBackspaceStatement*)(SgBackspaceStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBackspaceStatement*
SgBackspaceStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBackspaceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBackspaceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBackspaceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBackspaceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBackspaceStatement)};
        unsigned long positionInPool{localIndex % SgBackspaceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBackspaceStatement::pool_size};
        returnPointer = &(((SgBackspaceStatement*)(SgBackspaceStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBackspaceStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBackspaceStatement::pools.begin(); block != SgBackspaceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBackspaceStatement*>(*block)};
        for (unsigned i = 0; i < SgBackspaceStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBackspaceStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBackspaceStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBackspaceStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgBackspaceStatement::pools.begin(); block != SgBackspaceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBackspaceStatement*>(*block)};
        for (unsigned i = 0; i < SgBackspaceStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBackspaceStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBackspaceStatement::clearMemoryPool() {
    SgBackspaceStatement* tempPointer{nullptr};
    if (SgBackspaceStatement::pools.empty() == false) {
        auto block{SgBackspaceStatement::pools.begin()};
        SgBackspaceStatement::next_node = reinterpret_cast<SgBackspaceStatement*>(*block);
        for (; block != SgBackspaceStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBackspaceStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBackspaceStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBackspaceStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBackspaceStatement::pool_size-1]);
        }
    }
}

void
SgBackspaceStatement::deleteMemoryPool() {
    for (auto p: SgBackspaceStatement::pools) {
        ROSE_FREE(p);
    }
    SgBackspaceStatement::next_node = nullptr;
    SgBackspaceStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBackspaceStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBackspaceStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBackspaceStatement)};
    for (size_t blockIndex = SgBackspaceStatement::pools.size(); (blockIndex * SgBackspaceStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBackspaceStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBackspaceStatement) = %" PRIuPTR " SgBackspaceStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBackspaceStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBackspaceStatement),SgBackspaceStatement::pool_size);
        }
#endif

        SgBackspaceStatement* pointer{static_cast<SgBackspaceStatement*>(ROSE_MALLOC(SgBackspaceStatement::pool_size * sizeof(SgBackspaceStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBackspaceStatement::pool_size * sizeof(SgBackspaceStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBackspaceStatement::pool_size * sizeof(SgBackspaceStatement));
#endif
        SgBackspaceStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBackspaceStatement::pool_size * sizeof(SgBackspaceStatement), V_SgBackspaceStatement));

        if (SgBackspaceStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBackspaceStatement* blkptr{reinterpret_cast<SgBackspaceStatement*>(SgBackspaceStatement::pools[blockIndex-1])};
                blkptr[SgBackspaceStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBackspaceStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBackspaceStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBackspaceStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBackspaceStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBackspaceStatement::pool_size - 1};
    SgBackspaceStatement* testPointer{reinterpret_cast<SgBackspaceStatement*>(SgBackspaceStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBackspaceStatement::pool_size * (SgBackspaceStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBackspaceStatementStroageClass
 * from its counterpart of SgBackspaceStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBackspaceStatement::initializeStorageClassArray(SgBackspaceStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBackspaceStatement::pools.begin(); block != SgBackspaceStatement::pools.end(); ++block) {
        SgBackspaceStatement* pointer{reinterpret_cast<SgBackspaceStatement*>(*block)};
        for (unsigned i = 0; i < SgBackspaceStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 88529 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRewindStatement that takes its corresponding StorageClass as parameter
 */
SgRewindStatement::SgRewindStatement(const SgRewindStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 88539 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRewindStatement*
SgRewindStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRewindStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRewindStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRewindStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRewindStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRewindStatement)};
        unsigned long positionInPool{localIndex % SgRewindStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRewindStatement::pool_size};
        returnPointer = &(((SgRewindStatement*)(SgRewindStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRewindStatement*
SgRewindStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRewindStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRewindStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRewindStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRewindStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRewindStatement)};
        unsigned long positionInPool{localIndex % SgRewindStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRewindStatement::pool_size};
        returnPointer = &(((SgRewindStatement*)(SgRewindStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRewindStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRewindStatement::pools.begin(); block != SgRewindStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRewindStatement*>(*block)};
        for (unsigned i = 0; i < SgRewindStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRewindStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRewindStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRewindStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgRewindStatement::pools.begin(); block != SgRewindStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRewindStatement*>(*block)};
        for (unsigned i = 0; i < SgRewindStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRewindStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRewindStatement::clearMemoryPool() {
    SgRewindStatement* tempPointer{nullptr};
    if (SgRewindStatement::pools.empty() == false) {
        auto block{SgRewindStatement::pools.begin()};
        SgRewindStatement::next_node = reinterpret_cast<SgRewindStatement*>(*block);
        for (; block != SgRewindStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRewindStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRewindStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRewindStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRewindStatement::pool_size-1]);
        }
    }
}

void
SgRewindStatement::deleteMemoryPool() {
    for (auto p: SgRewindStatement::pools) {
        ROSE_FREE(p);
    }
    SgRewindStatement::next_node = nullptr;
    SgRewindStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRewindStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRewindStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRewindStatement)};
    for (size_t blockIndex = SgRewindStatement::pools.size(); (blockIndex * SgRewindStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRewindStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRewindStatement) = %" PRIuPTR " SgRewindStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRewindStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRewindStatement),SgRewindStatement::pool_size);
        }
#endif

        SgRewindStatement* pointer{static_cast<SgRewindStatement*>(ROSE_MALLOC(SgRewindStatement::pool_size * sizeof(SgRewindStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRewindStatement::pool_size * sizeof(SgRewindStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRewindStatement::pool_size * sizeof(SgRewindStatement));
#endif
        SgRewindStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRewindStatement::pool_size * sizeof(SgRewindStatement), V_SgRewindStatement));

        if (SgRewindStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRewindStatement* blkptr{reinterpret_cast<SgRewindStatement*>(SgRewindStatement::pools[blockIndex-1])};
                blkptr[SgRewindStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRewindStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRewindStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRewindStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRewindStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRewindStatement::pool_size - 1};
    SgRewindStatement* testPointer{reinterpret_cast<SgRewindStatement*>(SgRewindStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRewindStatement::pool_size * (SgRewindStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRewindStatementStroageClass
 * from its counterpart of SgRewindStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRewindStatement::initializeStorageClassArray(SgRewindStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRewindStatement::pools.begin(); block != SgRewindStatement::pools.end(); ++block) {
        SgRewindStatement* pointer{reinterpret_cast<SgRewindStatement*>(*block)};
        for (unsigned i = 0; i < SgRewindStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 88756 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEndfileStatement that takes its corresponding StorageClass as parameter
 */
SgEndfileStatement::SgEndfileStatement(const SgEndfileStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 88766 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEndfileStatement*
SgEndfileStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEndfileStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEndfileStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEndfileStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEndfileStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEndfileStatement)};
        unsigned long positionInPool{localIndex % SgEndfileStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEndfileStatement::pool_size};
        returnPointer = &(((SgEndfileStatement*)(SgEndfileStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEndfileStatement*
SgEndfileStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEndfileStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEndfileStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEndfileStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEndfileStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEndfileStatement)};
        unsigned long positionInPool{localIndex % SgEndfileStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEndfileStatement::pool_size};
        returnPointer = &(((SgEndfileStatement*)(SgEndfileStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEndfileStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEndfileStatement::pools.begin(); block != SgEndfileStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEndfileStatement*>(*block)};
        for (unsigned i = 0; i < SgEndfileStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEndfileStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEndfileStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEndfileStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgEndfileStatement::pools.begin(); block != SgEndfileStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEndfileStatement*>(*block)};
        for (unsigned i = 0; i < SgEndfileStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEndfileStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEndfileStatement::clearMemoryPool() {
    SgEndfileStatement* tempPointer{nullptr};
    if (SgEndfileStatement::pools.empty() == false) {
        auto block{SgEndfileStatement::pools.begin()};
        SgEndfileStatement::next_node = reinterpret_cast<SgEndfileStatement*>(*block);
        for (; block != SgEndfileStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEndfileStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEndfileStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEndfileStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEndfileStatement::pool_size-1]);
        }
    }
}

void
SgEndfileStatement::deleteMemoryPool() {
    for (auto p: SgEndfileStatement::pools) {
        ROSE_FREE(p);
    }
    SgEndfileStatement::next_node = nullptr;
    SgEndfileStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEndfileStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEndfileStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEndfileStatement)};
    for (size_t blockIndex = SgEndfileStatement::pools.size(); (blockIndex * SgEndfileStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEndfileStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEndfileStatement) = %" PRIuPTR " SgEndfileStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEndfileStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEndfileStatement),SgEndfileStatement::pool_size);
        }
#endif

        SgEndfileStatement* pointer{static_cast<SgEndfileStatement*>(ROSE_MALLOC(SgEndfileStatement::pool_size * sizeof(SgEndfileStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEndfileStatement::pool_size * sizeof(SgEndfileStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEndfileStatement::pool_size * sizeof(SgEndfileStatement));
#endif
        SgEndfileStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEndfileStatement::pool_size * sizeof(SgEndfileStatement), V_SgEndfileStatement));

        if (SgEndfileStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEndfileStatement* blkptr{reinterpret_cast<SgEndfileStatement*>(SgEndfileStatement::pools[blockIndex-1])};
                blkptr[SgEndfileStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEndfileStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEndfileStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEndfileStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEndfileStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEndfileStatement::pool_size - 1};
    SgEndfileStatement* testPointer{reinterpret_cast<SgEndfileStatement*>(SgEndfileStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEndfileStatement::pool_size * (SgEndfileStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEndfileStatementStroageClass
 * from its counterpart of SgEndfileStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEndfileStatement::initializeStorageClassArray(SgEndfileStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEndfileStatement::pools.begin(); block != SgEndfileStatement::pools.end(); ++block) {
        SgEndfileStatement* pointer{reinterpret_cast<SgEndfileStatement*>(*block)};
        for (unsigned i = 0; i < SgEndfileStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 88983 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWaitStatement that takes its corresponding StorageClass as parameter
 */
SgWaitStatement::SgWaitStatement(const SgWaitStatementStorageClass& storageSource)  : SgIOStatement (storageSource) {

/* #line 88993 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWaitStatement*
SgWaitStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWaitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWaitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWaitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWaitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWaitStatement)};
        unsigned long positionInPool{localIndex % SgWaitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWaitStatement::pool_size};
        returnPointer = &(((SgWaitStatement*)(SgWaitStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWaitStatement*
SgWaitStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWaitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWaitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWaitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWaitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWaitStatement)};
        unsigned long positionInPool{localIndex % SgWaitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWaitStatement::pool_size};
        returnPointer = &(((SgWaitStatement*)(SgWaitStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWaitStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWaitStatement::pools.begin(); block != SgWaitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWaitStatement*>(*block)};
        for (unsigned i = 0; i < SgWaitStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWaitStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWaitStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWaitStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgWaitStatement::pools.begin(); block != SgWaitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWaitStatement*>(*block)};
        for (unsigned i = 0; i < SgWaitStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWaitStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWaitStatement::clearMemoryPool() {
    SgWaitStatement* tempPointer{nullptr};
    if (SgWaitStatement::pools.empty() == false) {
        auto block{SgWaitStatement::pools.begin()};
        SgWaitStatement::next_node = reinterpret_cast<SgWaitStatement*>(*block);
        for (; block != SgWaitStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWaitStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWaitStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWaitStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWaitStatement::pool_size-1]);
        }
    }
}

void
SgWaitStatement::deleteMemoryPool() {
    for (auto p: SgWaitStatement::pools) {
        ROSE_FREE(p);
    }
    SgWaitStatement::next_node = nullptr;
    SgWaitStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWaitStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWaitStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWaitStatement)};
    for (size_t blockIndex = SgWaitStatement::pools.size(); (blockIndex * SgWaitStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWaitStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWaitStatement) = %" PRIuPTR " SgWaitStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWaitStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWaitStatement),SgWaitStatement::pool_size);
        }
#endif

        SgWaitStatement* pointer{static_cast<SgWaitStatement*>(ROSE_MALLOC(SgWaitStatement::pool_size * sizeof(SgWaitStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWaitStatement::pool_size * sizeof(SgWaitStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWaitStatement::pool_size * sizeof(SgWaitStatement));
#endif
        SgWaitStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWaitStatement::pool_size * sizeof(SgWaitStatement), V_SgWaitStatement));

        if (SgWaitStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWaitStatement* blkptr{reinterpret_cast<SgWaitStatement*>(SgWaitStatement::pools[blockIndex-1])};
                blkptr[SgWaitStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWaitStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWaitStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWaitStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWaitStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWaitStatement::pool_size - 1};
    SgWaitStatement* testPointer{reinterpret_cast<SgWaitStatement*>(SgWaitStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWaitStatement::pool_size * (SgWaitStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWaitStatementStroageClass
 * from its counterpart of SgWaitStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWaitStatement::initializeStorageClassArray(SgWaitStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWaitStatement::pools.begin(); block != SgWaitStatement::pools.end(); ++block) {
        SgWaitStatement* pointer{reinterpret_cast<SgWaitStatement*>(*block)};
        for (unsigned i = 0; i < SgWaitStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 89210 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFortranContinueStmt that takes its corresponding StorageClass as parameter
 */
SgFortranContinueStmt::SgFortranContinueStmt(const SgFortranContinueStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 89220 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFortranContinueStmt*
SgFortranContinueStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFortranContinueStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranContinueStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranContinueStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFortranContinueStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranContinueStmt)};
        unsigned long positionInPool{localIndex % SgFortranContinueStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranContinueStmt::pool_size};
        returnPointer = &(((SgFortranContinueStmt*)(SgFortranContinueStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFortranContinueStmt*
SgFortranContinueStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFortranContinueStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranContinueStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranContinueStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFortranContinueStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFortranContinueStmt)};
        unsigned long positionInPool{localIndex % SgFortranContinueStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFortranContinueStmt::pool_size};
        returnPointer = &(((SgFortranContinueStmt*)(SgFortranContinueStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFortranContinueStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFortranContinueStmt::pools.begin(); block != SgFortranContinueStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranContinueStmt*>(*block)};
        for (unsigned i = 0; i < SgFortranContinueStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFortranContinueStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFortranContinueStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFortranContinueStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgFortranContinueStmt::pools.begin(); block != SgFortranContinueStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFortranContinueStmt*>(*block)};
        for (unsigned i = 0; i < SgFortranContinueStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFortranContinueStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFortranContinueStmt::clearMemoryPool() {
    SgFortranContinueStmt* tempPointer{nullptr};
    if (SgFortranContinueStmt::pools.empty() == false) {
        auto block{SgFortranContinueStmt::pools.begin()};
        SgFortranContinueStmt::next_node = reinterpret_cast<SgFortranContinueStmt*>(*block);
        for (; block != SgFortranContinueStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFortranContinueStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFortranContinueStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFortranContinueStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFortranContinueStmt::pool_size-1]);
        }
    }
}

void
SgFortranContinueStmt::deleteMemoryPool() {
    for (auto p: SgFortranContinueStmt::pools) {
        ROSE_FREE(p);
    }
    SgFortranContinueStmt::next_node = nullptr;
    SgFortranContinueStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFortranContinueStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranContinueStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranContinueStmt)};
    for (size_t blockIndex = SgFortranContinueStmt::pools.size(); (blockIndex * SgFortranContinueStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranContinueStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranContinueStmt) = %" PRIuPTR " SgFortranContinueStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFortranContinueStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFortranContinueStmt),SgFortranContinueStmt::pool_size);
        }
#endif

        SgFortranContinueStmt* pointer{static_cast<SgFortranContinueStmt*>(ROSE_MALLOC(SgFortranContinueStmt::pool_size * sizeof(SgFortranContinueStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFortranContinueStmt::pool_size * sizeof(SgFortranContinueStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFortranContinueStmt::pool_size * sizeof(SgFortranContinueStmt));
#endif
        SgFortranContinueStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFortranContinueStmt::pool_size * sizeof(SgFortranContinueStmt), V_SgFortranContinueStmt));

        if (SgFortranContinueStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFortranContinueStmt* blkptr{reinterpret_cast<SgFortranContinueStmt*>(SgFortranContinueStmt::pools[blockIndex-1])};
                blkptr[SgFortranContinueStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFortranContinueStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFortranContinueStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFortranContinueStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFortranContinueStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFortranContinueStmt::pool_size - 1};
    SgFortranContinueStmt* testPointer{reinterpret_cast<SgFortranContinueStmt*>(SgFortranContinueStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFortranContinueStmt::pool_size * (SgFortranContinueStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFortranContinueStmtStroageClass
 * from its counterpart of SgFortranContinueStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFortranContinueStmt::initializeStorageClassArray(SgFortranContinueStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFortranContinueStmt::pools.begin(); block != SgFortranContinueStmt::pools.end(); ++block) {
        SgFortranContinueStmt* pointer{reinterpret_cast<SgFortranContinueStmt*>(*block)};
        for (unsigned i = 0; i < SgFortranContinueStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 89437 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWhereStatement that takes its corresponding StorageClass as parameter
 */
SgWhereStatement::SgWhereStatement(const SgWhereStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 89447 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_condition = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_condition));
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_elsewhere = (SgElseWhereStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_elsewhere));
     p_end_numeric_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end_numeric_label));
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();
    p_has_end_statement = storageSource.storageOf_has_end_statement;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWhereStatement*
SgWhereStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWhereStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhereStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhereStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhereStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWhereStatement)};
        unsigned long positionInPool{localIndex % SgWhereStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWhereStatement::pool_size};
        returnPointer = &(((SgWhereStatement*)(SgWhereStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWhereStatement*
SgWhereStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWhereStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhereStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhereStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhereStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWhereStatement)};
        unsigned long positionInPool{localIndex % SgWhereStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWhereStatement::pool_size};
        returnPointer = &(((SgWhereStatement*)(SgWhereStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWhereStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWhereStatement::pools.begin(); block != SgWhereStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWhereStatement*>(*block)};
        for (unsigned i = 0; i < SgWhereStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWhereStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWhereStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWhereStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgWhereStatement::pools.begin(); block != SgWhereStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWhereStatement*>(*block)};
        for (unsigned i = 0; i < SgWhereStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWhereStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWhereStatement::clearMemoryPool() {
    SgWhereStatement* tempPointer{nullptr};
    if (SgWhereStatement::pools.empty() == false) {
        auto block{SgWhereStatement::pools.begin()};
        SgWhereStatement::next_node = reinterpret_cast<SgWhereStatement*>(*block);
        for (; block != SgWhereStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWhereStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWhereStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWhereStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWhereStatement::pool_size-1]);
        }
    }
}

void
SgWhereStatement::deleteMemoryPool() {
    for (auto p: SgWhereStatement::pools) {
        ROSE_FREE(p);
    }
    SgWhereStatement::next_node = nullptr;
    SgWhereStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWhereStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWhereStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhereStatement)};
    for (size_t blockIndex = SgWhereStatement::pools.size(); (blockIndex * SgWhereStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWhereStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhereStatement) = %" PRIuPTR " SgWhereStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWhereStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhereStatement),SgWhereStatement::pool_size);
        }
#endif

        SgWhereStatement* pointer{static_cast<SgWhereStatement*>(ROSE_MALLOC(SgWhereStatement::pool_size * sizeof(SgWhereStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWhereStatement::pool_size * sizeof(SgWhereStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWhereStatement::pool_size * sizeof(SgWhereStatement));
#endif
        SgWhereStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWhereStatement::pool_size * sizeof(SgWhereStatement), V_SgWhereStatement));

        if (SgWhereStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWhereStatement* blkptr{reinterpret_cast<SgWhereStatement*>(SgWhereStatement::pools[blockIndex-1])};
                blkptr[SgWhereStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWhereStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWhereStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWhereStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWhereStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWhereStatement::pool_size - 1};
    SgWhereStatement* testPointer{reinterpret_cast<SgWhereStatement*>(SgWhereStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWhereStatement::pool_size * (SgWhereStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWhereStatementStroageClass
 * from its counterpart of SgWhereStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWhereStatement::initializeStorageClassArray(SgWhereStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWhereStatement::pools.begin(); block != SgWhereStatement::pools.end(); ++block) {
        SgWhereStatement* pointer{reinterpret_cast<SgWhereStatement*>(*block)};
        for (unsigned i = 0; i < SgWhereStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 89670 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElseWhereStatement that takes its corresponding StorageClass as parameter
 */
SgElseWhereStatement::SgElseWhereStatement(const SgElseWhereStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 89680 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_condition = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_condition));
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_elsewhere = (SgElseWhereStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_elsewhere));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElseWhereStatement*
SgElseWhereStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElseWhereStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseWhereStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseWhereStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElseWhereStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElseWhereStatement)};
        unsigned long positionInPool{localIndex % SgElseWhereStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElseWhereStatement::pool_size};
        returnPointer = &(((SgElseWhereStatement*)(SgElseWhereStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElseWhereStatement*
SgElseWhereStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElseWhereStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseWhereStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseWhereStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElseWhereStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElseWhereStatement)};
        unsigned long positionInPool{localIndex % SgElseWhereStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElseWhereStatement::pool_size};
        returnPointer = &(((SgElseWhereStatement*)(SgElseWhereStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElseWhereStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElseWhereStatement::pools.begin(); block != SgElseWhereStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElseWhereStatement*>(*block)};
        for (unsigned i = 0; i < SgElseWhereStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElseWhereStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElseWhereStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElseWhereStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgElseWhereStatement::pools.begin(); block != SgElseWhereStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElseWhereStatement*>(*block)};
        for (unsigned i = 0; i < SgElseWhereStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElseWhereStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElseWhereStatement::clearMemoryPool() {
    SgElseWhereStatement* tempPointer{nullptr};
    if (SgElseWhereStatement::pools.empty() == false) {
        auto block{SgElseWhereStatement::pools.begin()};
        SgElseWhereStatement::next_node = reinterpret_cast<SgElseWhereStatement*>(*block);
        for (; block != SgElseWhereStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElseWhereStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElseWhereStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElseWhereStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElseWhereStatement::pool_size-1]);
        }
    }
}

void
SgElseWhereStatement::deleteMemoryPool() {
    for (auto p: SgElseWhereStatement::pools) {
        ROSE_FREE(p);
    }
    SgElseWhereStatement::next_node = nullptr;
    SgElseWhereStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElseWhereStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElseWhereStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseWhereStatement)};
    for (size_t blockIndex = SgElseWhereStatement::pools.size(); (blockIndex * SgElseWhereStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElseWhereStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseWhereStatement) = %" PRIuPTR " SgElseWhereStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElseWhereStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElseWhereStatement),SgElseWhereStatement::pool_size);
        }
#endif

        SgElseWhereStatement* pointer{static_cast<SgElseWhereStatement*>(ROSE_MALLOC(SgElseWhereStatement::pool_size * sizeof(SgElseWhereStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElseWhereStatement::pool_size * sizeof(SgElseWhereStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElseWhereStatement::pool_size * sizeof(SgElseWhereStatement));
#endif
        SgElseWhereStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElseWhereStatement::pool_size * sizeof(SgElseWhereStatement), V_SgElseWhereStatement));

        if (SgElseWhereStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElseWhereStatement* blkptr{reinterpret_cast<SgElseWhereStatement*>(SgElseWhereStatement::pools[blockIndex-1])};
                blkptr[SgElseWhereStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElseWhereStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElseWhereStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElseWhereStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElseWhereStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElseWhereStatement::pool_size - 1};
    SgElseWhereStatement* testPointer{reinterpret_cast<SgElseWhereStatement*>(SgElseWhereStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElseWhereStatement::pool_size * (SgElseWhereStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElseWhereStatementStroageClass
 * from its counterpart of SgElseWhereStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElseWhereStatement::initializeStorageClassArray(SgElseWhereStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElseWhereStatement::pools.begin(); block != SgElseWhereStatement::pools.end(); ++block) {
        SgElseWhereStatement* pointer{reinterpret_cast<SgElseWhereStatement*>(*block)};
        for (unsigned i = 0; i < SgElseWhereStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 89900 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNullifyStatement that takes its corresponding StorageClass as parameter
 */
SgNullifyStatement::SgNullifyStatement(const SgNullifyStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 89910 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_pointer_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pointer_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNullifyStatement*
SgNullifyStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNullifyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullifyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullifyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullifyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNullifyStatement)};
        unsigned long positionInPool{localIndex % SgNullifyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullifyStatement::pool_size};
        returnPointer = &(((SgNullifyStatement*)(SgNullifyStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNullifyStatement*
SgNullifyStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNullifyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullifyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullifyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullifyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNullifyStatement)};
        unsigned long positionInPool{localIndex % SgNullifyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullifyStatement::pool_size};
        returnPointer = &(((SgNullifyStatement*)(SgNullifyStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNullifyStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNullifyStatement::pools.begin(); block != SgNullifyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullifyStatement*>(*block)};
        for (unsigned i = 0; i < SgNullifyStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNullifyStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNullifyStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNullifyStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgNullifyStatement::pools.begin(); block != SgNullifyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullifyStatement*>(*block)};
        for (unsigned i = 0; i < SgNullifyStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNullifyStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNullifyStatement::clearMemoryPool() {
    SgNullifyStatement* tempPointer{nullptr};
    if (SgNullifyStatement::pools.empty() == false) {
        auto block{SgNullifyStatement::pools.begin()};
        SgNullifyStatement::next_node = reinterpret_cast<SgNullifyStatement*>(*block);
        for (; block != SgNullifyStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNullifyStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNullifyStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNullifyStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNullifyStatement::pool_size-1]);
        }
    }
}

void
SgNullifyStatement::deleteMemoryPool() {
    for (auto p: SgNullifyStatement::pools) {
        ROSE_FREE(p);
    }
    SgNullifyStatement::next_node = nullptr;
    SgNullifyStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNullifyStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNullifyStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullifyStatement)};
    for (size_t blockIndex = SgNullifyStatement::pools.size(); (blockIndex * SgNullifyStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNullifyStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullifyStatement) = %" PRIuPTR " SgNullifyStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNullifyStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullifyStatement),SgNullifyStatement::pool_size);
        }
#endif

        SgNullifyStatement* pointer{static_cast<SgNullifyStatement*>(ROSE_MALLOC(SgNullifyStatement::pool_size * sizeof(SgNullifyStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNullifyStatement::pool_size * sizeof(SgNullifyStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNullifyStatement::pool_size * sizeof(SgNullifyStatement));
#endif
        SgNullifyStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNullifyStatement::pool_size * sizeof(SgNullifyStatement), V_SgNullifyStatement));

        if (SgNullifyStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNullifyStatement* blkptr{reinterpret_cast<SgNullifyStatement*>(SgNullifyStatement::pools[blockIndex-1])};
                blkptr[SgNullifyStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNullifyStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNullifyStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNullifyStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNullifyStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNullifyStatement::pool_size - 1};
    SgNullifyStatement* testPointer{reinterpret_cast<SgNullifyStatement*>(SgNullifyStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNullifyStatement::pool_size * (SgNullifyStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNullifyStatementStroageClass
 * from its counterpart of SgNullifyStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNullifyStatement::initializeStorageClassArray(SgNullifyStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNullifyStatement::pools.begin(); block != SgNullifyStatement::pools.end(); ++block) {
        SgNullifyStatement* pointer{reinterpret_cast<SgNullifyStatement*>(*block)};
        for (unsigned i = 0; i < SgNullifyStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 90128 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgArithmeticIfStatement that takes its corresponding StorageClass as parameter
 */
SgArithmeticIfStatement::SgArithmeticIfStatement(const SgArithmeticIfStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 90138 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_conditional = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_conditional));
     p_less_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_less_label));
     p_equal_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_equal_label));
     p_greater_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_greater_label));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgArithmeticIfStatement*
SgArithmeticIfStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgArithmeticIfStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArithmeticIfStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArithmeticIfStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArithmeticIfStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgArithmeticIfStatement)};
        unsigned long positionInPool{localIndex % SgArithmeticIfStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArithmeticIfStatement::pool_size};
        returnPointer = &(((SgArithmeticIfStatement*)(SgArithmeticIfStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgArithmeticIfStatement*
SgArithmeticIfStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgArithmeticIfStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArithmeticIfStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArithmeticIfStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArithmeticIfStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgArithmeticIfStatement)};
        unsigned long positionInPool{localIndex % SgArithmeticIfStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArithmeticIfStatement::pool_size};
        returnPointer = &(((SgArithmeticIfStatement*)(SgArithmeticIfStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgArithmeticIfStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgArithmeticIfStatement::pools.begin(); block != SgArithmeticIfStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArithmeticIfStatement*>(*block)};
        for (unsigned i = 0; i < SgArithmeticIfStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgArithmeticIfStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgArithmeticIfStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgArithmeticIfStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgArithmeticIfStatement::pools.begin(); block != SgArithmeticIfStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArithmeticIfStatement*>(*block)};
        for (unsigned i = 0; i < SgArithmeticIfStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgArithmeticIfStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgArithmeticIfStatement::clearMemoryPool() {
    SgArithmeticIfStatement* tempPointer{nullptr};
    if (SgArithmeticIfStatement::pools.empty() == false) {
        auto block{SgArithmeticIfStatement::pools.begin()};
        SgArithmeticIfStatement::next_node = reinterpret_cast<SgArithmeticIfStatement*>(*block);
        for (; block != SgArithmeticIfStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgArithmeticIfStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgArithmeticIfStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgArithmeticIfStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgArithmeticIfStatement::pool_size-1]);
        }
    }
}

void
SgArithmeticIfStatement::deleteMemoryPool() {
    for (auto p: SgArithmeticIfStatement::pools) {
        ROSE_FREE(p);
    }
    SgArithmeticIfStatement::next_node = nullptr;
    SgArithmeticIfStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgArithmeticIfStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgArithmeticIfStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgArithmeticIfStatement)};
    for (size_t blockIndex = SgArithmeticIfStatement::pools.size(); (blockIndex * SgArithmeticIfStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgArithmeticIfStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgArithmeticIfStatement) = %" PRIuPTR " SgArithmeticIfStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgArithmeticIfStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgArithmeticIfStatement),SgArithmeticIfStatement::pool_size);
        }
#endif

        SgArithmeticIfStatement* pointer{static_cast<SgArithmeticIfStatement*>(ROSE_MALLOC(SgArithmeticIfStatement::pool_size * sizeof(SgArithmeticIfStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgArithmeticIfStatement::pool_size * sizeof(SgArithmeticIfStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgArithmeticIfStatement::pool_size * sizeof(SgArithmeticIfStatement));
#endif
        SgArithmeticIfStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgArithmeticIfStatement::pool_size * sizeof(SgArithmeticIfStatement), V_SgArithmeticIfStatement));

        if (SgArithmeticIfStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgArithmeticIfStatement* blkptr{reinterpret_cast<SgArithmeticIfStatement*>(SgArithmeticIfStatement::pools[blockIndex-1])};
                blkptr[SgArithmeticIfStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgArithmeticIfStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgArithmeticIfStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgArithmeticIfStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgArithmeticIfStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgArithmeticIfStatement::pool_size - 1};
    SgArithmeticIfStatement* testPointer{reinterpret_cast<SgArithmeticIfStatement*>(SgArithmeticIfStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgArithmeticIfStatement::pool_size * (SgArithmeticIfStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgArithmeticIfStatementStroageClass
 * from its counterpart of SgArithmeticIfStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgArithmeticIfStatement::initializeStorageClassArray(SgArithmeticIfStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgArithmeticIfStatement::pools.begin(); block != SgArithmeticIfStatement::pools.end(); ++block) {
        SgArithmeticIfStatement* pointer{reinterpret_cast<SgArithmeticIfStatement*>(*block)};
        for (unsigned i = 0; i < SgArithmeticIfStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 90359 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAssignStatement that takes its corresponding StorageClass as parameter
 */
SgAssignStatement::SgAssignStatement(const SgAssignStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 90369 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_label = (SgLabelRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_label));
     p_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAssignStatement*
SgAssignStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAssignStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignStatement)};
        unsigned long positionInPool{localIndex % SgAssignStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignStatement::pool_size};
        returnPointer = &(((SgAssignStatement*)(SgAssignStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAssignStatement*
SgAssignStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAssignStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAssignStatement)};
        unsigned long positionInPool{localIndex % SgAssignStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignStatement::pool_size};
        returnPointer = &(((SgAssignStatement*)(SgAssignStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAssignStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAssignStatement::pools.begin(); block != SgAssignStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignStatement*>(*block)};
        for (unsigned i = 0; i < SgAssignStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAssignStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAssignStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAssignStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgAssignStatement::pools.begin(); block != SgAssignStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignStatement*>(*block)};
        for (unsigned i = 0; i < SgAssignStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAssignStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAssignStatement::clearMemoryPool() {
    SgAssignStatement* tempPointer{nullptr};
    if (SgAssignStatement::pools.empty() == false) {
        auto block{SgAssignStatement::pools.begin()};
        SgAssignStatement::next_node = reinterpret_cast<SgAssignStatement*>(*block);
        for (; block != SgAssignStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAssignStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAssignStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAssignStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAssignStatement::pool_size-1]);
        }
    }
}

void
SgAssignStatement::deleteMemoryPool() {
    for (auto p: SgAssignStatement::pools) {
        ROSE_FREE(p);
    }
    SgAssignStatement::next_node = nullptr;
    SgAssignStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAssignStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignStatement)};
    for (size_t blockIndex = SgAssignStatement::pools.size(); (blockIndex * SgAssignStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignStatement) = %" PRIuPTR " SgAssignStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignStatement),SgAssignStatement::pool_size);
        }
#endif

        SgAssignStatement* pointer{static_cast<SgAssignStatement*>(ROSE_MALLOC(SgAssignStatement::pool_size * sizeof(SgAssignStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAssignStatement::pool_size * sizeof(SgAssignStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAssignStatement::pool_size * sizeof(SgAssignStatement));
#endif
        SgAssignStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAssignStatement::pool_size * sizeof(SgAssignStatement), V_SgAssignStatement));

        if (SgAssignStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAssignStatement* blkptr{reinterpret_cast<SgAssignStatement*>(SgAssignStatement::pools[blockIndex-1])};
                blkptr[SgAssignStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAssignStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAssignStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAssignStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAssignStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAssignStatement::pool_size - 1};
    SgAssignStatement* testPointer{reinterpret_cast<SgAssignStatement*>(SgAssignStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAssignStatement::pool_size * (SgAssignStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAssignStatementStroageClass
 * from its counterpart of SgAssignStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAssignStatement::initializeStorageClassArray(SgAssignStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAssignStatement::pools.begin(); block != SgAssignStatement::pools.end(); ++block) {
        SgAssignStatement* pointer{reinterpret_cast<SgAssignStatement*>(*block)};
        for (unsigned i = 0; i < SgAssignStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 90588 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgComputedGotoStatement that takes its corresponding StorageClass as parameter
 */
SgComputedGotoStatement::SgComputedGotoStatement(const SgComputedGotoStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 90598 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_labelList = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_labelList));
     p_label_index = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_label_index));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgComputedGotoStatement*
SgComputedGotoStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgComputedGotoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComputedGotoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComputedGotoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComputedGotoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgComputedGotoStatement)};
        unsigned long positionInPool{localIndex % SgComputedGotoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgComputedGotoStatement::pool_size};
        returnPointer = &(((SgComputedGotoStatement*)(SgComputedGotoStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgComputedGotoStatement*
SgComputedGotoStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgComputedGotoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComputedGotoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComputedGotoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComputedGotoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgComputedGotoStatement)};
        unsigned long positionInPool{localIndex % SgComputedGotoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgComputedGotoStatement::pool_size};
        returnPointer = &(((SgComputedGotoStatement*)(SgComputedGotoStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgComputedGotoStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgComputedGotoStatement::pools.begin(); block != SgComputedGotoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgComputedGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgComputedGotoStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgComputedGotoStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgComputedGotoStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgComputedGotoStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgComputedGotoStatement::pools.begin(); block != SgComputedGotoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgComputedGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgComputedGotoStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgComputedGotoStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgComputedGotoStatement::clearMemoryPool() {
    SgComputedGotoStatement* tempPointer{nullptr};
    if (SgComputedGotoStatement::pools.empty() == false) {
        auto block{SgComputedGotoStatement::pools.begin()};
        SgComputedGotoStatement::next_node = reinterpret_cast<SgComputedGotoStatement*>(*block);
        for (; block != SgComputedGotoStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgComputedGotoStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgComputedGotoStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgComputedGotoStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgComputedGotoStatement::pool_size-1]);
        }
    }
}

void
SgComputedGotoStatement::deleteMemoryPool() {
    for (auto p: SgComputedGotoStatement::pools) {
        ROSE_FREE(p);
    }
    SgComputedGotoStatement::next_node = nullptr;
    SgComputedGotoStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgComputedGotoStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgComputedGotoStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgComputedGotoStatement)};
    for (size_t blockIndex = SgComputedGotoStatement::pools.size(); (blockIndex * SgComputedGotoStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgComputedGotoStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgComputedGotoStatement) = %" PRIuPTR " SgComputedGotoStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgComputedGotoStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgComputedGotoStatement),SgComputedGotoStatement::pool_size);
        }
#endif

        SgComputedGotoStatement* pointer{static_cast<SgComputedGotoStatement*>(ROSE_MALLOC(SgComputedGotoStatement::pool_size * sizeof(SgComputedGotoStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgComputedGotoStatement::pool_size * sizeof(SgComputedGotoStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgComputedGotoStatement::pool_size * sizeof(SgComputedGotoStatement));
#endif
        SgComputedGotoStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgComputedGotoStatement::pool_size * sizeof(SgComputedGotoStatement), V_SgComputedGotoStatement));

        if (SgComputedGotoStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgComputedGotoStatement* blkptr{reinterpret_cast<SgComputedGotoStatement*>(SgComputedGotoStatement::pools[blockIndex-1])};
                blkptr[SgComputedGotoStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgComputedGotoStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgComputedGotoStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgComputedGotoStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgComputedGotoStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgComputedGotoStatement::pool_size - 1};
    SgComputedGotoStatement* testPointer{reinterpret_cast<SgComputedGotoStatement*>(SgComputedGotoStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgComputedGotoStatement::pool_size * (SgComputedGotoStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgComputedGotoStatementStroageClass
 * from its counterpart of SgComputedGotoStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgComputedGotoStatement::initializeStorageClassArray(SgComputedGotoStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgComputedGotoStatement::pools.begin(); block != SgComputedGotoStatement::pools.end(); ++block) {
        SgComputedGotoStatement* pointer{reinterpret_cast<SgComputedGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgComputedGotoStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 90817 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAssignedGotoStatement that takes its corresponding StorageClass as parameter
 */
SgAssignedGotoStatement::SgAssignedGotoStatement(const SgAssignedGotoStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 90827 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_targets = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_targets));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAssignedGotoStatement*
SgAssignedGotoStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAssignedGotoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignedGotoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignedGotoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignedGotoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignedGotoStatement)};
        unsigned long positionInPool{localIndex % SgAssignedGotoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignedGotoStatement::pool_size};
        returnPointer = &(((SgAssignedGotoStatement*)(SgAssignedGotoStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAssignedGotoStatement*
SgAssignedGotoStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAssignedGotoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignedGotoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignedGotoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignedGotoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAssignedGotoStatement)};
        unsigned long positionInPool{localIndex % SgAssignedGotoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignedGotoStatement::pool_size};
        returnPointer = &(((SgAssignedGotoStatement*)(SgAssignedGotoStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAssignedGotoStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAssignedGotoStatement::pools.begin(); block != SgAssignedGotoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignedGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgAssignedGotoStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAssignedGotoStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAssignedGotoStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAssignedGotoStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgAssignedGotoStatement::pools.begin(); block != SgAssignedGotoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignedGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgAssignedGotoStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAssignedGotoStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAssignedGotoStatement::clearMemoryPool() {
    SgAssignedGotoStatement* tempPointer{nullptr};
    if (SgAssignedGotoStatement::pools.empty() == false) {
        auto block{SgAssignedGotoStatement::pools.begin()};
        SgAssignedGotoStatement::next_node = reinterpret_cast<SgAssignedGotoStatement*>(*block);
        for (; block != SgAssignedGotoStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAssignedGotoStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAssignedGotoStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAssignedGotoStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAssignedGotoStatement::pool_size-1]);
        }
    }
}

void
SgAssignedGotoStatement::deleteMemoryPool() {
    for (auto p: SgAssignedGotoStatement::pools) {
        ROSE_FREE(p);
    }
    SgAssignedGotoStatement::next_node = nullptr;
    SgAssignedGotoStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAssignedGotoStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignedGotoStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignedGotoStatement)};
    for (size_t blockIndex = SgAssignedGotoStatement::pools.size(); (blockIndex * SgAssignedGotoStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignedGotoStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignedGotoStatement) = %" PRIuPTR " SgAssignedGotoStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignedGotoStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignedGotoStatement),SgAssignedGotoStatement::pool_size);
        }
#endif

        SgAssignedGotoStatement* pointer{static_cast<SgAssignedGotoStatement*>(ROSE_MALLOC(SgAssignedGotoStatement::pool_size * sizeof(SgAssignedGotoStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAssignedGotoStatement::pool_size * sizeof(SgAssignedGotoStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAssignedGotoStatement::pool_size * sizeof(SgAssignedGotoStatement));
#endif
        SgAssignedGotoStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAssignedGotoStatement::pool_size * sizeof(SgAssignedGotoStatement), V_SgAssignedGotoStatement));

        if (SgAssignedGotoStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAssignedGotoStatement* blkptr{reinterpret_cast<SgAssignedGotoStatement*>(SgAssignedGotoStatement::pools[blockIndex-1])};
                blkptr[SgAssignedGotoStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAssignedGotoStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAssignedGotoStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAssignedGotoStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAssignedGotoStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAssignedGotoStatement::pool_size - 1};
    SgAssignedGotoStatement* testPointer{reinterpret_cast<SgAssignedGotoStatement*>(SgAssignedGotoStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAssignedGotoStatement::pool_size * (SgAssignedGotoStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAssignedGotoStatementStroageClass
 * from its counterpart of SgAssignedGotoStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAssignedGotoStatement::initializeStorageClassArray(SgAssignedGotoStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAssignedGotoStatement::pools.begin(); block != SgAssignedGotoStatement::pools.end(); ++block) {
        SgAssignedGotoStatement* pointer{reinterpret_cast<SgAssignedGotoStatement*>(*block)};
        for (unsigned i = 0; i < SgAssignedGotoStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 91045 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAllocateStatement that takes its corresponding StorageClass as parameter
 */
SgAllocateStatement::SgAllocateStatement(const SgAllocateStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 91055 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expr_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expr_list));
     p_stat_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_stat_expression));
     p_errmsg_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_errmsg_expression));
     p_source_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_source_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAllocateStatement*
SgAllocateStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAllocateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAllocateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAllocateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAllocateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAllocateStatement)};
        unsigned long positionInPool{localIndex % SgAllocateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAllocateStatement::pool_size};
        returnPointer = &(((SgAllocateStatement*)(SgAllocateStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAllocateStatement*
SgAllocateStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAllocateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAllocateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAllocateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAllocateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAllocateStatement)};
        unsigned long positionInPool{localIndex % SgAllocateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAllocateStatement::pool_size};
        returnPointer = &(((SgAllocateStatement*)(SgAllocateStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAllocateStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAllocateStatement::pools.begin(); block != SgAllocateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAllocateStatement*>(*block)};
        for (unsigned i = 0; i < SgAllocateStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAllocateStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAllocateStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAllocateStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgAllocateStatement::pools.begin(); block != SgAllocateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAllocateStatement*>(*block)};
        for (unsigned i = 0; i < SgAllocateStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAllocateStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAllocateStatement::clearMemoryPool() {
    SgAllocateStatement* tempPointer{nullptr};
    if (SgAllocateStatement::pools.empty() == false) {
        auto block{SgAllocateStatement::pools.begin()};
        SgAllocateStatement::next_node = reinterpret_cast<SgAllocateStatement*>(*block);
        for (; block != SgAllocateStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAllocateStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAllocateStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAllocateStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAllocateStatement::pool_size-1]);
        }
    }
}

void
SgAllocateStatement::deleteMemoryPool() {
    for (auto p: SgAllocateStatement::pools) {
        ROSE_FREE(p);
    }
    SgAllocateStatement::next_node = nullptr;
    SgAllocateStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAllocateStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAllocateStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAllocateStatement)};
    for (size_t blockIndex = SgAllocateStatement::pools.size(); (blockIndex * SgAllocateStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAllocateStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAllocateStatement) = %" PRIuPTR " SgAllocateStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAllocateStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAllocateStatement),SgAllocateStatement::pool_size);
        }
#endif

        SgAllocateStatement* pointer{static_cast<SgAllocateStatement*>(ROSE_MALLOC(SgAllocateStatement::pool_size * sizeof(SgAllocateStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAllocateStatement::pool_size * sizeof(SgAllocateStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAllocateStatement::pool_size * sizeof(SgAllocateStatement));
#endif
        SgAllocateStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAllocateStatement::pool_size * sizeof(SgAllocateStatement), V_SgAllocateStatement));

        if (SgAllocateStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAllocateStatement* blkptr{reinterpret_cast<SgAllocateStatement*>(SgAllocateStatement::pools[blockIndex-1])};
                blkptr[SgAllocateStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAllocateStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAllocateStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAllocateStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAllocateStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAllocateStatement::pool_size - 1};
    SgAllocateStatement* testPointer{reinterpret_cast<SgAllocateStatement*>(SgAllocateStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAllocateStatement::pool_size * (SgAllocateStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAllocateStatementStroageClass
 * from its counterpart of SgAllocateStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAllocateStatement::initializeStorageClassArray(SgAllocateStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAllocateStatement::pools.begin(); block != SgAllocateStatement::pools.end(); ++block) {
        SgAllocateStatement* pointer{reinterpret_cast<SgAllocateStatement*>(*block)};
        for (unsigned i = 0; i < SgAllocateStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 91276 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDeallocateStatement that takes its corresponding StorageClass as parameter
 */
SgDeallocateStatement::SgDeallocateStatement(const SgDeallocateStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 91286 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expr_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expr_list));
     p_stat_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_stat_expression));
     p_errmsg_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_errmsg_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDeallocateStatement*
SgDeallocateStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDeallocateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeallocateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeallocateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeallocateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDeallocateStatement)};
        unsigned long positionInPool{localIndex % SgDeallocateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeallocateStatement::pool_size};
        returnPointer = &(((SgDeallocateStatement*)(SgDeallocateStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDeallocateStatement*
SgDeallocateStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDeallocateStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeallocateStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeallocateStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeallocateStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDeallocateStatement)};
        unsigned long positionInPool{localIndex % SgDeallocateStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeallocateStatement::pool_size};
        returnPointer = &(((SgDeallocateStatement*)(SgDeallocateStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDeallocateStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDeallocateStatement::pools.begin(); block != SgDeallocateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeallocateStatement*>(*block)};
        for (unsigned i = 0; i < SgDeallocateStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDeallocateStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDeallocateStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDeallocateStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgDeallocateStatement::pools.begin(); block != SgDeallocateStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeallocateStatement*>(*block)};
        for (unsigned i = 0; i < SgDeallocateStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDeallocateStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDeallocateStatement::clearMemoryPool() {
    SgDeallocateStatement* tempPointer{nullptr};
    if (SgDeallocateStatement::pools.empty() == false) {
        auto block{SgDeallocateStatement::pools.begin()};
        SgDeallocateStatement::next_node = reinterpret_cast<SgDeallocateStatement*>(*block);
        for (; block != SgDeallocateStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDeallocateStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDeallocateStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDeallocateStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDeallocateStatement::pool_size-1]);
        }
    }
}

void
SgDeallocateStatement::deleteMemoryPool() {
    for (auto p: SgDeallocateStatement::pools) {
        ROSE_FREE(p);
    }
    SgDeallocateStatement::next_node = nullptr;
    SgDeallocateStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDeallocateStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDeallocateStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeallocateStatement)};
    for (size_t blockIndex = SgDeallocateStatement::pools.size(); (blockIndex * SgDeallocateStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDeallocateStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeallocateStatement) = %" PRIuPTR " SgDeallocateStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDeallocateStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeallocateStatement),SgDeallocateStatement::pool_size);
        }
#endif

        SgDeallocateStatement* pointer{static_cast<SgDeallocateStatement*>(ROSE_MALLOC(SgDeallocateStatement::pool_size * sizeof(SgDeallocateStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDeallocateStatement::pool_size * sizeof(SgDeallocateStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDeallocateStatement::pool_size * sizeof(SgDeallocateStatement));
#endif
        SgDeallocateStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDeallocateStatement::pool_size * sizeof(SgDeallocateStatement), V_SgDeallocateStatement));

        if (SgDeallocateStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDeallocateStatement* blkptr{reinterpret_cast<SgDeallocateStatement*>(SgDeallocateStatement::pools[blockIndex-1])};
                blkptr[SgDeallocateStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDeallocateStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDeallocateStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDeallocateStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDeallocateStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDeallocateStatement::pool_size - 1};
    SgDeallocateStatement* testPointer{reinterpret_cast<SgDeallocateStatement*>(SgDeallocateStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDeallocateStatement::pool_size * (SgDeallocateStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDeallocateStatementStroageClass
 * from its counterpart of SgDeallocateStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDeallocateStatement::initializeStorageClassArray(SgDeallocateStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDeallocateStatement::pools.begin(); block != SgDeallocateStatement::pools.end(); ++block) {
        SgDeallocateStatement* pointer{reinterpret_cast<SgDeallocateStatement*>(*block)};
        for (unsigned i = 0; i < SgDeallocateStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 91506 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcNotifyStatement that takes its corresponding StorageClass as parameter
 */
SgUpcNotifyStatement::SgUpcNotifyStatement(const SgUpcNotifyStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 91516 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_notify_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_notify_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcNotifyStatement*
SgUpcNotifyStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcNotifyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcNotifyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcNotifyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcNotifyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcNotifyStatement)};
        unsigned long positionInPool{localIndex % SgUpcNotifyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcNotifyStatement::pool_size};
        returnPointer = &(((SgUpcNotifyStatement*)(SgUpcNotifyStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcNotifyStatement*
SgUpcNotifyStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcNotifyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcNotifyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcNotifyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcNotifyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcNotifyStatement)};
        unsigned long positionInPool{localIndex % SgUpcNotifyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcNotifyStatement::pool_size};
        returnPointer = &(((SgUpcNotifyStatement*)(SgUpcNotifyStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcNotifyStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcNotifyStatement::pools.begin(); block != SgUpcNotifyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcNotifyStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcNotifyStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcNotifyStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcNotifyStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcNotifyStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcNotifyStatement::pools.begin(); block != SgUpcNotifyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcNotifyStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcNotifyStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcNotifyStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcNotifyStatement::clearMemoryPool() {
    SgUpcNotifyStatement* tempPointer{nullptr};
    if (SgUpcNotifyStatement::pools.empty() == false) {
        auto block{SgUpcNotifyStatement::pools.begin()};
        SgUpcNotifyStatement::next_node = reinterpret_cast<SgUpcNotifyStatement*>(*block);
        for (; block != SgUpcNotifyStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcNotifyStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcNotifyStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcNotifyStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcNotifyStatement::pool_size-1]);
        }
    }
}

void
SgUpcNotifyStatement::deleteMemoryPool() {
    for (auto p: SgUpcNotifyStatement::pools) {
        ROSE_FREE(p);
    }
    SgUpcNotifyStatement::next_node = nullptr;
    SgUpcNotifyStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcNotifyStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcNotifyStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcNotifyStatement)};
    for (size_t blockIndex = SgUpcNotifyStatement::pools.size(); (blockIndex * SgUpcNotifyStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcNotifyStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcNotifyStatement) = %" PRIuPTR " SgUpcNotifyStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcNotifyStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcNotifyStatement),SgUpcNotifyStatement::pool_size);
        }
#endif

        SgUpcNotifyStatement* pointer{static_cast<SgUpcNotifyStatement*>(ROSE_MALLOC(SgUpcNotifyStatement::pool_size * sizeof(SgUpcNotifyStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcNotifyStatement::pool_size * sizeof(SgUpcNotifyStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcNotifyStatement::pool_size * sizeof(SgUpcNotifyStatement));
#endif
        SgUpcNotifyStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcNotifyStatement::pool_size * sizeof(SgUpcNotifyStatement), V_SgUpcNotifyStatement));

        if (SgUpcNotifyStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcNotifyStatement* blkptr{reinterpret_cast<SgUpcNotifyStatement*>(SgUpcNotifyStatement::pools[blockIndex-1])};
                blkptr[SgUpcNotifyStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcNotifyStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcNotifyStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcNotifyStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcNotifyStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcNotifyStatement::pool_size - 1};
    SgUpcNotifyStatement* testPointer{reinterpret_cast<SgUpcNotifyStatement*>(SgUpcNotifyStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcNotifyStatement::pool_size * (SgUpcNotifyStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcNotifyStatementStroageClass
 * from its counterpart of SgUpcNotifyStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcNotifyStatement::initializeStorageClassArray(SgUpcNotifyStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcNotifyStatement::pools.begin(); block != SgUpcNotifyStatement::pools.end(); ++block) {
        SgUpcNotifyStatement* pointer{reinterpret_cast<SgUpcNotifyStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcNotifyStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 91734 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcWaitStatement that takes its corresponding StorageClass as parameter
 */
SgUpcWaitStatement::SgUpcWaitStatement(const SgUpcWaitStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 91744 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_wait_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_wait_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcWaitStatement*
SgUpcWaitStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcWaitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcWaitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcWaitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcWaitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcWaitStatement)};
        unsigned long positionInPool{localIndex % SgUpcWaitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcWaitStatement::pool_size};
        returnPointer = &(((SgUpcWaitStatement*)(SgUpcWaitStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcWaitStatement*
SgUpcWaitStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcWaitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcWaitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcWaitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcWaitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcWaitStatement)};
        unsigned long positionInPool{localIndex % SgUpcWaitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcWaitStatement::pool_size};
        returnPointer = &(((SgUpcWaitStatement*)(SgUpcWaitStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcWaitStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcWaitStatement::pools.begin(); block != SgUpcWaitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcWaitStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcWaitStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcWaitStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcWaitStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcWaitStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcWaitStatement::pools.begin(); block != SgUpcWaitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcWaitStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcWaitStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcWaitStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcWaitStatement::clearMemoryPool() {
    SgUpcWaitStatement* tempPointer{nullptr};
    if (SgUpcWaitStatement::pools.empty() == false) {
        auto block{SgUpcWaitStatement::pools.begin()};
        SgUpcWaitStatement::next_node = reinterpret_cast<SgUpcWaitStatement*>(*block);
        for (; block != SgUpcWaitStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcWaitStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcWaitStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcWaitStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcWaitStatement::pool_size-1]);
        }
    }
}

void
SgUpcWaitStatement::deleteMemoryPool() {
    for (auto p: SgUpcWaitStatement::pools) {
        ROSE_FREE(p);
    }
    SgUpcWaitStatement::next_node = nullptr;
    SgUpcWaitStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcWaitStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcWaitStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcWaitStatement)};
    for (size_t blockIndex = SgUpcWaitStatement::pools.size(); (blockIndex * SgUpcWaitStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcWaitStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcWaitStatement) = %" PRIuPTR " SgUpcWaitStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcWaitStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcWaitStatement),SgUpcWaitStatement::pool_size);
        }
#endif

        SgUpcWaitStatement* pointer{static_cast<SgUpcWaitStatement*>(ROSE_MALLOC(SgUpcWaitStatement::pool_size * sizeof(SgUpcWaitStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcWaitStatement::pool_size * sizeof(SgUpcWaitStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcWaitStatement::pool_size * sizeof(SgUpcWaitStatement));
#endif
        SgUpcWaitStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcWaitStatement::pool_size * sizeof(SgUpcWaitStatement), V_SgUpcWaitStatement));

        if (SgUpcWaitStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcWaitStatement* blkptr{reinterpret_cast<SgUpcWaitStatement*>(SgUpcWaitStatement::pools[blockIndex-1])};
                blkptr[SgUpcWaitStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcWaitStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcWaitStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcWaitStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcWaitStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcWaitStatement::pool_size - 1};
    SgUpcWaitStatement* testPointer{reinterpret_cast<SgUpcWaitStatement*>(SgUpcWaitStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcWaitStatement::pool_size * (SgUpcWaitStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcWaitStatementStroageClass
 * from its counterpart of SgUpcWaitStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcWaitStatement::initializeStorageClassArray(SgUpcWaitStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcWaitStatement::pools.begin(); block != SgUpcWaitStatement::pools.end(); ++block) {
        SgUpcWaitStatement* pointer{reinterpret_cast<SgUpcWaitStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcWaitStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 91962 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcBarrierStatement that takes its corresponding StorageClass as parameter
 */
SgUpcBarrierStatement::SgUpcBarrierStatement(const SgUpcBarrierStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 91972 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_barrier_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_barrier_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcBarrierStatement*
SgUpcBarrierStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcBarrierStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcBarrierStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcBarrierStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcBarrierStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBarrierStatement)};
        unsigned long positionInPool{localIndex % SgUpcBarrierStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcBarrierStatement::pool_size};
        returnPointer = &(((SgUpcBarrierStatement*)(SgUpcBarrierStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcBarrierStatement*
SgUpcBarrierStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcBarrierStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcBarrierStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcBarrierStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcBarrierStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcBarrierStatement)};
        unsigned long positionInPool{localIndex % SgUpcBarrierStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcBarrierStatement::pool_size};
        returnPointer = &(((SgUpcBarrierStatement*)(SgUpcBarrierStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcBarrierStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcBarrierStatement::pools.begin(); block != SgUpcBarrierStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcBarrierStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcBarrierStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcBarrierStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcBarrierStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcBarrierStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcBarrierStatement::pools.begin(); block != SgUpcBarrierStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcBarrierStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcBarrierStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcBarrierStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcBarrierStatement::clearMemoryPool() {
    SgUpcBarrierStatement* tempPointer{nullptr};
    if (SgUpcBarrierStatement::pools.empty() == false) {
        auto block{SgUpcBarrierStatement::pools.begin()};
        SgUpcBarrierStatement::next_node = reinterpret_cast<SgUpcBarrierStatement*>(*block);
        for (; block != SgUpcBarrierStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcBarrierStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcBarrierStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcBarrierStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcBarrierStatement::pool_size-1]);
        }
    }
}

void
SgUpcBarrierStatement::deleteMemoryPool() {
    for (auto p: SgUpcBarrierStatement::pools) {
        ROSE_FREE(p);
    }
    SgUpcBarrierStatement::next_node = nullptr;
    SgUpcBarrierStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcBarrierStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBarrierStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcBarrierStatement)};
    for (size_t blockIndex = SgUpcBarrierStatement::pools.size(); (blockIndex * SgUpcBarrierStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBarrierStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcBarrierStatement) = %" PRIuPTR " SgUpcBarrierStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBarrierStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcBarrierStatement),SgUpcBarrierStatement::pool_size);
        }
#endif

        SgUpcBarrierStatement* pointer{static_cast<SgUpcBarrierStatement*>(ROSE_MALLOC(SgUpcBarrierStatement::pool_size * sizeof(SgUpcBarrierStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcBarrierStatement::pool_size * sizeof(SgUpcBarrierStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcBarrierStatement::pool_size * sizeof(SgUpcBarrierStatement));
#endif
        SgUpcBarrierStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcBarrierStatement::pool_size * sizeof(SgUpcBarrierStatement), V_SgUpcBarrierStatement));

        if (SgUpcBarrierStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcBarrierStatement* blkptr{reinterpret_cast<SgUpcBarrierStatement*>(SgUpcBarrierStatement::pools[blockIndex-1])};
                blkptr[SgUpcBarrierStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcBarrierStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcBarrierStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcBarrierStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcBarrierStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcBarrierStatement::pool_size - 1};
    SgUpcBarrierStatement* testPointer{reinterpret_cast<SgUpcBarrierStatement*>(SgUpcBarrierStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcBarrierStatement::pool_size * (SgUpcBarrierStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcBarrierStatementStroageClass
 * from its counterpart of SgUpcBarrierStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcBarrierStatement::initializeStorageClassArray(SgUpcBarrierStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcBarrierStatement::pools.begin(); block != SgUpcBarrierStatement::pools.end(); ++block) {
        SgUpcBarrierStatement* pointer{reinterpret_cast<SgUpcBarrierStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcBarrierStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 92190 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcFenceStatement that takes its corresponding StorageClass as parameter
 */
SgUpcFenceStatement::SgUpcFenceStatement(const SgUpcFenceStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 92200 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcFenceStatement*
SgUpcFenceStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcFenceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcFenceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcFenceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcFenceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcFenceStatement)};
        unsigned long positionInPool{localIndex % SgUpcFenceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcFenceStatement::pool_size};
        returnPointer = &(((SgUpcFenceStatement*)(SgUpcFenceStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcFenceStatement*
SgUpcFenceStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcFenceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcFenceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcFenceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcFenceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcFenceStatement)};
        unsigned long positionInPool{localIndex % SgUpcFenceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcFenceStatement::pool_size};
        returnPointer = &(((SgUpcFenceStatement*)(SgUpcFenceStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcFenceStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcFenceStatement::pools.begin(); block != SgUpcFenceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcFenceStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcFenceStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcFenceStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcFenceStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcFenceStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcFenceStatement::pools.begin(); block != SgUpcFenceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcFenceStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcFenceStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcFenceStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcFenceStatement::clearMemoryPool() {
    SgUpcFenceStatement* tempPointer{nullptr};
    if (SgUpcFenceStatement::pools.empty() == false) {
        auto block{SgUpcFenceStatement::pools.begin()};
        SgUpcFenceStatement::next_node = reinterpret_cast<SgUpcFenceStatement*>(*block);
        for (; block != SgUpcFenceStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcFenceStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcFenceStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcFenceStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcFenceStatement::pool_size-1]);
        }
    }
}

void
SgUpcFenceStatement::deleteMemoryPool() {
    for (auto p: SgUpcFenceStatement::pools) {
        ROSE_FREE(p);
    }
    SgUpcFenceStatement::next_node = nullptr;
    SgUpcFenceStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcFenceStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcFenceStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcFenceStatement)};
    for (size_t blockIndex = SgUpcFenceStatement::pools.size(); (blockIndex * SgUpcFenceStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcFenceStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcFenceStatement) = %" PRIuPTR " SgUpcFenceStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcFenceStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcFenceStatement),SgUpcFenceStatement::pool_size);
        }
#endif

        SgUpcFenceStatement* pointer{static_cast<SgUpcFenceStatement*>(ROSE_MALLOC(SgUpcFenceStatement::pool_size * sizeof(SgUpcFenceStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcFenceStatement::pool_size * sizeof(SgUpcFenceStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcFenceStatement::pool_size * sizeof(SgUpcFenceStatement));
#endif
        SgUpcFenceStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcFenceStatement::pool_size * sizeof(SgUpcFenceStatement), V_SgUpcFenceStatement));

        if (SgUpcFenceStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcFenceStatement* blkptr{reinterpret_cast<SgUpcFenceStatement*>(SgUpcFenceStatement::pools[blockIndex-1])};
                blkptr[SgUpcFenceStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcFenceStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcFenceStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcFenceStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcFenceStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcFenceStatement::pool_size - 1};
    SgUpcFenceStatement* testPointer{reinterpret_cast<SgUpcFenceStatement*>(SgUpcFenceStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcFenceStatement::pool_size * (SgUpcFenceStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcFenceStatementStroageClass
 * from its counterpart of SgUpcFenceStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcFenceStatement::initializeStorageClassArray(SgUpcFenceStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcFenceStatement::pools.begin(); block != SgUpcFenceStatement::pools.end(); ++block) {
        SgUpcFenceStatement* pointer{reinterpret_cast<SgUpcFenceStatement*>(*block)};
        for (unsigned i = 0; i < SgUpcFenceStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 92417 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpBarrierStatement that takes its corresponding StorageClass as parameter
 */
SgOmpBarrierStatement::SgOmpBarrierStatement(const SgOmpBarrierStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 92427 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpBarrierStatement*
SgOmpBarrierStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpBarrierStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBarrierStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBarrierStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBarrierStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBarrierStatement)};
        unsigned long positionInPool{localIndex % SgOmpBarrierStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpBarrierStatement::pool_size};
        returnPointer = &(((SgOmpBarrierStatement*)(SgOmpBarrierStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpBarrierStatement*
SgOmpBarrierStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpBarrierStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBarrierStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBarrierStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBarrierStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpBarrierStatement)};
        unsigned long positionInPool{localIndex % SgOmpBarrierStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpBarrierStatement::pool_size};
        returnPointer = &(((SgOmpBarrierStatement*)(SgOmpBarrierStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpBarrierStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpBarrierStatement::pools.begin(); block != SgOmpBarrierStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpBarrierStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpBarrierStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpBarrierStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpBarrierStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpBarrierStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpBarrierStatement::pools.begin(); block != SgOmpBarrierStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpBarrierStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpBarrierStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpBarrierStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpBarrierStatement::clearMemoryPool() {
    SgOmpBarrierStatement* tempPointer{nullptr};
    if (SgOmpBarrierStatement::pools.empty() == false) {
        auto block{SgOmpBarrierStatement::pools.begin()};
        SgOmpBarrierStatement::next_node = reinterpret_cast<SgOmpBarrierStatement*>(*block);
        for (; block != SgOmpBarrierStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpBarrierStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpBarrierStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpBarrierStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpBarrierStatement::pool_size-1]);
        }
    }
}

void
SgOmpBarrierStatement::deleteMemoryPool() {
    for (auto p: SgOmpBarrierStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpBarrierStatement::next_node = nullptr;
    SgOmpBarrierStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpBarrierStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBarrierStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBarrierStatement)};
    for (size_t blockIndex = SgOmpBarrierStatement::pools.size(); (blockIndex * SgOmpBarrierStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBarrierStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBarrierStatement) = %" PRIuPTR " SgOmpBarrierStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBarrierStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBarrierStatement),SgOmpBarrierStatement::pool_size);
        }
#endif

        SgOmpBarrierStatement* pointer{static_cast<SgOmpBarrierStatement*>(ROSE_MALLOC(SgOmpBarrierStatement::pool_size * sizeof(SgOmpBarrierStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpBarrierStatement::pool_size * sizeof(SgOmpBarrierStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpBarrierStatement::pool_size * sizeof(SgOmpBarrierStatement));
#endif
        SgOmpBarrierStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpBarrierStatement::pool_size * sizeof(SgOmpBarrierStatement), V_SgOmpBarrierStatement));

        if (SgOmpBarrierStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpBarrierStatement* blkptr{reinterpret_cast<SgOmpBarrierStatement*>(SgOmpBarrierStatement::pools[blockIndex-1])};
                blkptr[SgOmpBarrierStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpBarrierStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpBarrierStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpBarrierStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpBarrierStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpBarrierStatement::pool_size - 1};
    SgOmpBarrierStatement* testPointer{reinterpret_cast<SgOmpBarrierStatement*>(SgOmpBarrierStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpBarrierStatement::pool_size * (SgOmpBarrierStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpBarrierStatementStroageClass
 * from its counterpart of SgOmpBarrierStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpBarrierStatement::initializeStorageClassArray(SgOmpBarrierStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpBarrierStatement::pools.begin(); block != SgOmpBarrierStatement::pools.end(); ++block) {
        SgOmpBarrierStatement* pointer{reinterpret_cast<SgOmpBarrierStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpBarrierStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 92644 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpTaskwaitStatement that takes its corresponding StorageClass as parameter
 */
SgOmpTaskwaitStatement::SgOmpTaskwaitStatement(const SgOmpTaskwaitStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 92654 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpTaskwaitStatement*
SgOmpTaskwaitStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpTaskwaitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTaskwaitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTaskwaitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTaskwaitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskwaitStatement)};
        unsigned long positionInPool{localIndex % SgOmpTaskwaitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTaskwaitStatement::pool_size};
        returnPointer = &(((SgOmpTaskwaitStatement*)(SgOmpTaskwaitStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpTaskwaitStatement*
SgOmpTaskwaitStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpTaskwaitStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTaskwaitStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTaskwaitStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTaskwaitStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpTaskwaitStatement)};
        unsigned long positionInPool{localIndex % SgOmpTaskwaitStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTaskwaitStatement::pool_size};
        returnPointer = &(((SgOmpTaskwaitStatement*)(SgOmpTaskwaitStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpTaskwaitStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpTaskwaitStatement::pools.begin(); block != SgOmpTaskwaitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTaskwaitStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTaskwaitStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpTaskwaitStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpTaskwaitStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpTaskwaitStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpTaskwaitStatement::pools.begin(); block != SgOmpTaskwaitStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTaskwaitStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTaskwaitStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpTaskwaitStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpTaskwaitStatement::clearMemoryPool() {
    SgOmpTaskwaitStatement* tempPointer{nullptr};
    if (SgOmpTaskwaitStatement::pools.empty() == false) {
        auto block{SgOmpTaskwaitStatement::pools.begin()};
        SgOmpTaskwaitStatement::next_node = reinterpret_cast<SgOmpTaskwaitStatement*>(*block);
        for (; block != SgOmpTaskwaitStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpTaskwaitStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpTaskwaitStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpTaskwaitStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpTaskwaitStatement::pool_size-1]);
        }
    }
}

void
SgOmpTaskwaitStatement::deleteMemoryPool() {
    for (auto p: SgOmpTaskwaitStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpTaskwaitStatement::next_node = nullptr;
    SgOmpTaskwaitStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpTaskwaitStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskwaitStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTaskwaitStatement)};
    for (size_t blockIndex = SgOmpTaskwaitStatement::pools.size(); (blockIndex * SgOmpTaskwaitStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskwaitStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTaskwaitStatement) = %" PRIuPTR " SgOmpTaskwaitStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskwaitStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTaskwaitStatement),SgOmpTaskwaitStatement::pool_size);
        }
#endif

        SgOmpTaskwaitStatement* pointer{static_cast<SgOmpTaskwaitStatement*>(ROSE_MALLOC(SgOmpTaskwaitStatement::pool_size * sizeof(SgOmpTaskwaitStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpTaskwaitStatement::pool_size * sizeof(SgOmpTaskwaitStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpTaskwaitStatement::pool_size * sizeof(SgOmpTaskwaitStatement));
#endif
        SgOmpTaskwaitStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpTaskwaitStatement::pool_size * sizeof(SgOmpTaskwaitStatement), V_SgOmpTaskwaitStatement));

        if (SgOmpTaskwaitStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpTaskwaitStatement* blkptr{reinterpret_cast<SgOmpTaskwaitStatement*>(SgOmpTaskwaitStatement::pools[blockIndex-1])};
                blkptr[SgOmpTaskwaitStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpTaskwaitStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpTaskwaitStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpTaskwaitStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpTaskwaitStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpTaskwaitStatement::pool_size - 1};
    SgOmpTaskwaitStatement* testPointer{reinterpret_cast<SgOmpTaskwaitStatement*>(SgOmpTaskwaitStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpTaskwaitStatement::pool_size * (SgOmpTaskwaitStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpTaskwaitStatementStroageClass
 * from its counterpart of SgOmpTaskwaitStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpTaskwaitStatement::initializeStorageClassArray(SgOmpTaskwaitStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpTaskwaitStatement::pools.begin(); block != SgOmpTaskwaitStatement::pools.end(); ++block) {
        SgOmpTaskwaitStatement* pointer{reinterpret_cast<SgOmpTaskwaitStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTaskwaitStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 92871 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpFlushStatement that takes its corresponding StorageClass as parameter
 */
SgOmpFlushStatement::SgOmpFlushStatement(const SgOmpFlushStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 92881 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_variables = storageSource.storageOf_variables.rebuildDataStoredInEasyStorageClass();
    SgVarRefExpPtrList::iterator i_variables = p_variables.begin();
    for ( ; i_variables != p_variables.end(); ++i_variables) {
        (*i_variables) = (SgVarRefExpPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_variables)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpFlushStatement*
SgOmpFlushStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpFlushStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFlushStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFlushStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpFlushStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFlushStatement)};
        unsigned long positionInPool{localIndex % SgOmpFlushStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpFlushStatement::pool_size};
        returnPointer = &(((SgOmpFlushStatement*)(SgOmpFlushStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpFlushStatement*
SgOmpFlushStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpFlushStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFlushStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFlushStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpFlushStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpFlushStatement)};
        unsigned long positionInPool{localIndex % SgOmpFlushStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpFlushStatement::pool_size};
        returnPointer = &(((SgOmpFlushStatement*)(SgOmpFlushStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpFlushStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpFlushStatement::pools.begin(); block != SgOmpFlushStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpFlushStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpFlushStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpFlushStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpFlushStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpFlushStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpFlushStatement::pools.begin(); block != SgOmpFlushStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpFlushStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpFlushStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpFlushStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpFlushStatement::clearMemoryPool() {
    SgOmpFlushStatement* tempPointer{nullptr};
    if (SgOmpFlushStatement::pools.empty() == false) {
        auto block{SgOmpFlushStatement::pools.begin()};
        SgOmpFlushStatement::next_node = reinterpret_cast<SgOmpFlushStatement*>(*block);
        for (; block != SgOmpFlushStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpFlushStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpFlushStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpFlushStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpFlushStatement::pool_size-1]);
        }
    }
}

void
SgOmpFlushStatement::deleteMemoryPool() {
    for (auto p: SgOmpFlushStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpFlushStatement::next_node = nullptr;
    SgOmpFlushStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpFlushStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFlushStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFlushStatement)};
    for (size_t blockIndex = SgOmpFlushStatement::pools.size(); (blockIndex * SgOmpFlushStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFlushStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFlushStatement) = %" PRIuPTR " SgOmpFlushStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpFlushStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpFlushStatement),SgOmpFlushStatement::pool_size);
        }
#endif

        SgOmpFlushStatement* pointer{static_cast<SgOmpFlushStatement*>(ROSE_MALLOC(SgOmpFlushStatement::pool_size * sizeof(SgOmpFlushStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpFlushStatement::pool_size * sizeof(SgOmpFlushStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpFlushStatement::pool_size * sizeof(SgOmpFlushStatement));
#endif
        SgOmpFlushStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpFlushStatement::pool_size * sizeof(SgOmpFlushStatement), V_SgOmpFlushStatement));

        if (SgOmpFlushStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpFlushStatement* blkptr{reinterpret_cast<SgOmpFlushStatement*>(SgOmpFlushStatement::pools[blockIndex-1])};
                blkptr[SgOmpFlushStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpFlushStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpFlushStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpFlushStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpFlushStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpFlushStatement::pool_size - 1};
    SgOmpFlushStatement* testPointer{reinterpret_cast<SgOmpFlushStatement*>(SgOmpFlushStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpFlushStatement::pool_size * (SgOmpFlushStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpFlushStatementStroageClass
 * from its counterpart of SgOmpFlushStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpFlushStatement::initializeStorageClassArray(SgOmpFlushStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpFlushStatement::pools.begin(); block != SgOmpFlushStatement::pools.end(); ++block) {
        SgOmpFlushStatement* pointer{reinterpret_cast<SgOmpFlushStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpFlushStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 93103 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpBodyStatement that takes its corresponding StorageClass as parameter
 */
SgOmpBodyStatement::SgOmpBodyStatement(const SgOmpBodyStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 93113 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpBodyStatement*
SgOmpBodyStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpBodyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBodyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBodyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpBodyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBodyStatement)};
        unsigned long positionInPool{localIndex % SgOmpBodyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpBodyStatement::pool_size};
        returnPointer = &(((SgOmpBodyStatement*)(SgOmpBodyStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpBodyStatement*
SgOmpBodyStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpBodyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBodyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBodyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpBodyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpBodyStatement)};
        unsigned long positionInPool{localIndex % SgOmpBodyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpBodyStatement::pool_size};
        returnPointer = &(((SgOmpBodyStatement*)(SgOmpBodyStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpBodyStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpBodyStatement::pools.begin(); block != SgOmpBodyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpBodyStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpBodyStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpBodyStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpBodyStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpBodyStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpBodyStatement::pools.begin(); block != SgOmpBodyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpBodyStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpBodyStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpBodyStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpBodyStatement::clearMemoryPool() {
    SgOmpBodyStatement* tempPointer{nullptr};
    if (SgOmpBodyStatement::pools.empty() == false) {
        auto block{SgOmpBodyStatement::pools.begin()};
        SgOmpBodyStatement::next_node = reinterpret_cast<SgOmpBodyStatement*>(*block);
        for (; block != SgOmpBodyStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpBodyStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpBodyStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpBodyStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpBodyStatement::pool_size-1]);
        }
    }
}

void
SgOmpBodyStatement::deleteMemoryPool() {
    for (auto p: SgOmpBodyStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpBodyStatement::next_node = nullptr;
    SgOmpBodyStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpBodyStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBodyStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBodyStatement)};
    for (size_t blockIndex = SgOmpBodyStatement::pools.size(); (blockIndex * SgOmpBodyStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBodyStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBodyStatement) = %" PRIuPTR " SgOmpBodyStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpBodyStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpBodyStatement),SgOmpBodyStatement::pool_size);
        }
#endif

        SgOmpBodyStatement* pointer{static_cast<SgOmpBodyStatement*>(ROSE_MALLOC(SgOmpBodyStatement::pool_size * sizeof(SgOmpBodyStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpBodyStatement::pool_size * sizeof(SgOmpBodyStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpBodyStatement::pool_size * sizeof(SgOmpBodyStatement));
#endif
        SgOmpBodyStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpBodyStatement::pool_size * sizeof(SgOmpBodyStatement), V_SgOmpBodyStatement));

        if (SgOmpBodyStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpBodyStatement* blkptr{reinterpret_cast<SgOmpBodyStatement*>(SgOmpBodyStatement::pools[blockIndex-1])};
                blkptr[SgOmpBodyStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpBodyStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpBodyStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpBodyStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpBodyStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpBodyStatement::pool_size - 1};
    SgOmpBodyStatement* testPointer{reinterpret_cast<SgOmpBodyStatement*>(SgOmpBodyStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpBodyStatement::pool_size * (SgOmpBodyStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpBodyStatementStroageClass
 * from its counterpart of SgOmpBodyStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpBodyStatement::initializeStorageClassArray(SgOmpBodyStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpBodyStatement::pools.begin(); block != SgOmpBodyStatement::pools.end(); ++block) {
        SgOmpBodyStatement* pointer{reinterpret_cast<SgOmpBodyStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpBodyStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 93331 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpMasterStatement that takes its corresponding StorageClass as parameter
 */
SgOmpMasterStatement::SgOmpMasterStatement(const SgOmpMasterStatementStorageClass& storageSource)  : SgOmpBodyStatement (storageSource) {

/* #line 93341 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpMasterStatement*
SgOmpMasterStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpMasterStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMasterStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMasterStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpMasterStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMasterStatement)};
        unsigned long positionInPool{localIndex % SgOmpMasterStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpMasterStatement::pool_size};
        returnPointer = &(((SgOmpMasterStatement*)(SgOmpMasterStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpMasterStatement*
SgOmpMasterStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpMasterStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMasterStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMasterStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpMasterStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpMasterStatement)};
        unsigned long positionInPool{localIndex % SgOmpMasterStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpMasterStatement::pool_size};
        returnPointer = &(((SgOmpMasterStatement*)(SgOmpMasterStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpMasterStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpMasterStatement::pools.begin(); block != SgOmpMasterStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpMasterStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpMasterStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpMasterStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpMasterStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpMasterStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpMasterStatement::pools.begin(); block != SgOmpMasterStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpMasterStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpMasterStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpMasterStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpMasterStatement::clearMemoryPool() {
    SgOmpMasterStatement* tempPointer{nullptr};
    if (SgOmpMasterStatement::pools.empty() == false) {
        auto block{SgOmpMasterStatement::pools.begin()};
        SgOmpMasterStatement::next_node = reinterpret_cast<SgOmpMasterStatement*>(*block);
        for (; block != SgOmpMasterStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpMasterStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpMasterStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpMasterStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpMasterStatement::pool_size-1]);
        }
    }
}

void
SgOmpMasterStatement::deleteMemoryPool() {
    for (auto p: SgOmpMasterStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpMasterStatement::next_node = nullptr;
    SgOmpMasterStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpMasterStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMasterStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMasterStatement)};
    for (size_t blockIndex = SgOmpMasterStatement::pools.size(); (blockIndex * SgOmpMasterStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMasterStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMasterStatement) = %" PRIuPTR " SgOmpMasterStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpMasterStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpMasterStatement),SgOmpMasterStatement::pool_size);
        }
#endif

        SgOmpMasterStatement* pointer{static_cast<SgOmpMasterStatement*>(ROSE_MALLOC(SgOmpMasterStatement::pool_size * sizeof(SgOmpMasterStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpMasterStatement::pool_size * sizeof(SgOmpMasterStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpMasterStatement::pool_size * sizeof(SgOmpMasterStatement));
#endif
        SgOmpMasterStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpMasterStatement::pool_size * sizeof(SgOmpMasterStatement), V_SgOmpMasterStatement));

        if (SgOmpMasterStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpMasterStatement* blkptr{reinterpret_cast<SgOmpMasterStatement*>(SgOmpMasterStatement::pools[blockIndex-1])};
                blkptr[SgOmpMasterStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpMasterStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpMasterStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpMasterStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpMasterStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpMasterStatement::pool_size - 1};
    SgOmpMasterStatement* testPointer{reinterpret_cast<SgOmpMasterStatement*>(SgOmpMasterStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpMasterStatement::pool_size * (SgOmpMasterStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpMasterStatementStroageClass
 * from its counterpart of SgOmpMasterStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpMasterStatement::initializeStorageClassArray(SgOmpMasterStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpMasterStatement::pools.begin(); block != SgOmpMasterStatement::pools.end(); ++block) {
        SgOmpMasterStatement* pointer{reinterpret_cast<SgOmpMasterStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpMasterStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 93558 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpOrderedStatement that takes its corresponding StorageClass as parameter
 */
SgOmpOrderedStatement::SgOmpOrderedStatement(const SgOmpOrderedStatementStorageClass& storageSource)  : SgOmpBodyStatement (storageSource) {

/* #line 93568 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpOrderedStatement*
SgOmpOrderedStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpOrderedStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpOrderedStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpOrderedStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpOrderedStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedStatement)};
        unsigned long positionInPool{localIndex % SgOmpOrderedStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpOrderedStatement::pool_size};
        returnPointer = &(((SgOmpOrderedStatement*)(SgOmpOrderedStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpOrderedStatement*
SgOmpOrderedStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpOrderedStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpOrderedStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpOrderedStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpOrderedStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpOrderedStatement)};
        unsigned long positionInPool{localIndex % SgOmpOrderedStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpOrderedStatement::pool_size};
        returnPointer = &(((SgOmpOrderedStatement*)(SgOmpOrderedStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpOrderedStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpOrderedStatement::pools.begin(); block != SgOmpOrderedStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpOrderedStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpOrderedStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpOrderedStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpOrderedStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpOrderedStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpOrderedStatement::pools.begin(); block != SgOmpOrderedStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpOrderedStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpOrderedStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpOrderedStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpOrderedStatement::clearMemoryPool() {
    SgOmpOrderedStatement* tempPointer{nullptr};
    if (SgOmpOrderedStatement::pools.empty() == false) {
        auto block{SgOmpOrderedStatement::pools.begin()};
        SgOmpOrderedStatement::next_node = reinterpret_cast<SgOmpOrderedStatement*>(*block);
        for (; block != SgOmpOrderedStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpOrderedStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpOrderedStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpOrderedStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpOrderedStatement::pool_size-1]);
        }
    }
}

void
SgOmpOrderedStatement::deleteMemoryPool() {
    for (auto p: SgOmpOrderedStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpOrderedStatement::next_node = nullptr;
    SgOmpOrderedStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpOrderedStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpOrderedStatement)};
    for (size_t blockIndex = SgOmpOrderedStatement::pools.size(); (blockIndex * SgOmpOrderedStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpOrderedStatement) = %" PRIuPTR " SgOmpOrderedStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpOrderedStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpOrderedStatement),SgOmpOrderedStatement::pool_size);
        }
#endif

        SgOmpOrderedStatement* pointer{static_cast<SgOmpOrderedStatement*>(ROSE_MALLOC(SgOmpOrderedStatement::pool_size * sizeof(SgOmpOrderedStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpOrderedStatement::pool_size * sizeof(SgOmpOrderedStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpOrderedStatement::pool_size * sizeof(SgOmpOrderedStatement));
#endif
        SgOmpOrderedStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpOrderedStatement::pool_size * sizeof(SgOmpOrderedStatement), V_SgOmpOrderedStatement));

        if (SgOmpOrderedStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpOrderedStatement* blkptr{reinterpret_cast<SgOmpOrderedStatement*>(SgOmpOrderedStatement::pools[blockIndex-1])};
                blkptr[SgOmpOrderedStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpOrderedStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpOrderedStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpOrderedStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpOrderedStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpOrderedStatement::pool_size - 1};
    SgOmpOrderedStatement* testPointer{reinterpret_cast<SgOmpOrderedStatement*>(SgOmpOrderedStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpOrderedStatement::pool_size * (SgOmpOrderedStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpOrderedStatementStroageClass
 * from its counterpart of SgOmpOrderedStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpOrderedStatement::initializeStorageClassArray(SgOmpOrderedStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpOrderedStatement::pools.begin(); block != SgOmpOrderedStatement::pools.end(); ++block) {
        SgOmpOrderedStatement* pointer{reinterpret_cast<SgOmpOrderedStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpOrderedStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 93785 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpCriticalStatement that takes its corresponding StorageClass as parameter
 */
SgOmpCriticalStatement::SgOmpCriticalStatement(const SgOmpCriticalStatementStorageClass& storageSource)  : SgOmpBodyStatement (storageSource) {

/* #line 93795 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpCriticalStatement*
SgOmpCriticalStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpCriticalStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCriticalStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCriticalStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpCriticalStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCriticalStatement)};
        unsigned long positionInPool{localIndex % SgOmpCriticalStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCriticalStatement::pool_size};
        returnPointer = &(((SgOmpCriticalStatement*)(SgOmpCriticalStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpCriticalStatement*
SgOmpCriticalStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpCriticalStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCriticalStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCriticalStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpCriticalStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpCriticalStatement)};
        unsigned long positionInPool{localIndex % SgOmpCriticalStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpCriticalStatement::pool_size};
        returnPointer = &(((SgOmpCriticalStatement*)(SgOmpCriticalStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpCriticalStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpCriticalStatement::pools.begin(); block != SgOmpCriticalStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCriticalStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpCriticalStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpCriticalStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpCriticalStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpCriticalStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpCriticalStatement::pools.begin(); block != SgOmpCriticalStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpCriticalStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpCriticalStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpCriticalStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpCriticalStatement::clearMemoryPool() {
    SgOmpCriticalStatement* tempPointer{nullptr};
    if (SgOmpCriticalStatement::pools.empty() == false) {
        auto block{SgOmpCriticalStatement::pools.begin()};
        SgOmpCriticalStatement::next_node = reinterpret_cast<SgOmpCriticalStatement*>(*block);
        for (; block != SgOmpCriticalStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpCriticalStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpCriticalStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpCriticalStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpCriticalStatement::pool_size-1]);
        }
    }
}

void
SgOmpCriticalStatement::deleteMemoryPool() {
    for (auto p: SgOmpCriticalStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpCriticalStatement::next_node = nullptr;
    SgOmpCriticalStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpCriticalStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCriticalStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCriticalStatement)};
    for (size_t blockIndex = SgOmpCriticalStatement::pools.size(); (blockIndex * SgOmpCriticalStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCriticalStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCriticalStatement) = %" PRIuPTR " SgOmpCriticalStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpCriticalStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpCriticalStatement),SgOmpCriticalStatement::pool_size);
        }
#endif

        SgOmpCriticalStatement* pointer{static_cast<SgOmpCriticalStatement*>(ROSE_MALLOC(SgOmpCriticalStatement::pool_size * sizeof(SgOmpCriticalStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpCriticalStatement::pool_size * sizeof(SgOmpCriticalStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpCriticalStatement::pool_size * sizeof(SgOmpCriticalStatement));
#endif
        SgOmpCriticalStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpCriticalStatement::pool_size * sizeof(SgOmpCriticalStatement), V_SgOmpCriticalStatement));

        if (SgOmpCriticalStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpCriticalStatement* blkptr{reinterpret_cast<SgOmpCriticalStatement*>(SgOmpCriticalStatement::pools[blockIndex-1])};
                blkptr[SgOmpCriticalStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpCriticalStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpCriticalStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpCriticalStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpCriticalStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpCriticalStatement::pool_size - 1};
    SgOmpCriticalStatement* testPointer{reinterpret_cast<SgOmpCriticalStatement*>(SgOmpCriticalStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpCriticalStatement::pool_size * (SgOmpCriticalStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpCriticalStatementStroageClass
 * from its counterpart of SgOmpCriticalStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpCriticalStatement::initializeStorageClassArray(SgOmpCriticalStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpCriticalStatement::pools.begin(); block != SgOmpCriticalStatement::pools.end(); ++block) {
        SgOmpCriticalStatement* pointer{reinterpret_cast<SgOmpCriticalStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpCriticalStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 94013 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpSectionStatement that takes its corresponding StorageClass as parameter
 */
SgOmpSectionStatement::SgOmpSectionStatement(const SgOmpSectionStatementStorageClass& storageSource)  : SgOmpBodyStatement (storageSource) {

/* #line 94023 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpSectionStatement*
SgOmpSectionStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpSectionStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSectionStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSectionStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSectionStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionStatement)};
        unsigned long positionInPool{localIndex % SgOmpSectionStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSectionStatement::pool_size};
        returnPointer = &(((SgOmpSectionStatement*)(SgOmpSectionStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpSectionStatement*
SgOmpSectionStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpSectionStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSectionStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSectionStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSectionStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpSectionStatement)};
        unsigned long positionInPool{localIndex % SgOmpSectionStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSectionStatement::pool_size};
        returnPointer = &(((SgOmpSectionStatement*)(SgOmpSectionStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpSectionStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpSectionStatement::pools.begin(); block != SgOmpSectionStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSectionStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSectionStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpSectionStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpSectionStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpSectionStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpSectionStatement::pools.begin(); block != SgOmpSectionStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSectionStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSectionStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpSectionStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpSectionStatement::clearMemoryPool() {
    SgOmpSectionStatement* tempPointer{nullptr};
    if (SgOmpSectionStatement::pools.empty() == false) {
        auto block{SgOmpSectionStatement::pools.begin()};
        SgOmpSectionStatement::next_node = reinterpret_cast<SgOmpSectionStatement*>(*block);
        for (; block != SgOmpSectionStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpSectionStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpSectionStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpSectionStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpSectionStatement::pool_size-1]);
        }
    }
}

void
SgOmpSectionStatement::deleteMemoryPool() {
    for (auto p: SgOmpSectionStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpSectionStatement::next_node = nullptr;
    SgOmpSectionStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpSectionStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSectionStatement)};
    for (size_t blockIndex = SgOmpSectionStatement::pools.size(); (blockIndex * SgOmpSectionStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSectionStatement) = %" PRIuPTR " SgOmpSectionStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSectionStatement),SgOmpSectionStatement::pool_size);
        }
#endif

        SgOmpSectionStatement* pointer{static_cast<SgOmpSectionStatement*>(ROSE_MALLOC(SgOmpSectionStatement::pool_size * sizeof(SgOmpSectionStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpSectionStatement::pool_size * sizeof(SgOmpSectionStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpSectionStatement::pool_size * sizeof(SgOmpSectionStatement));
#endif
        SgOmpSectionStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpSectionStatement::pool_size * sizeof(SgOmpSectionStatement), V_SgOmpSectionStatement));

        if (SgOmpSectionStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpSectionStatement* blkptr{reinterpret_cast<SgOmpSectionStatement*>(SgOmpSectionStatement::pools[blockIndex-1])};
                blkptr[SgOmpSectionStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpSectionStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpSectionStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpSectionStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpSectionStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpSectionStatement::pool_size - 1};
    SgOmpSectionStatement* testPointer{reinterpret_cast<SgOmpSectionStatement*>(SgOmpSectionStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpSectionStatement::pool_size * (SgOmpSectionStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpSectionStatementStroageClass
 * from its counterpart of SgOmpSectionStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpSectionStatement::initializeStorageClassArray(SgOmpSectionStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpSectionStatement::pools.begin(); block != SgOmpSectionStatement::pools.end(); ++block) {
        SgOmpSectionStatement* pointer{reinterpret_cast<SgOmpSectionStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSectionStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 94240 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpWorkshareStatement that takes its corresponding StorageClass as parameter
 */
SgOmpWorkshareStatement::SgOmpWorkshareStatement(const SgOmpWorkshareStatementStorageClass& storageSource)  : SgOmpBodyStatement (storageSource) {

/* #line 94250 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpWorkshareStatement*
SgOmpWorkshareStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpWorkshareStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpWorkshareStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpWorkshareStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpWorkshareStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpWorkshareStatement)};
        unsigned long positionInPool{localIndex % SgOmpWorkshareStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpWorkshareStatement::pool_size};
        returnPointer = &(((SgOmpWorkshareStatement*)(SgOmpWorkshareStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpWorkshareStatement*
SgOmpWorkshareStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpWorkshareStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpWorkshareStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpWorkshareStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpWorkshareStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpWorkshareStatement)};
        unsigned long positionInPool{localIndex % SgOmpWorkshareStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpWorkshareStatement::pool_size};
        returnPointer = &(((SgOmpWorkshareStatement*)(SgOmpWorkshareStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpWorkshareStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpWorkshareStatement::pools.begin(); block != SgOmpWorkshareStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpWorkshareStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpWorkshareStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpWorkshareStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpWorkshareStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpWorkshareStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpWorkshareStatement::pools.begin(); block != SgOmpWorkshareStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpWorkshareStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpWorkshareStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpWorkshareStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpWorkshareStatement::clearMemoryPool() {
    SgOmpWorkshareStatement* tempPointer{nullptr};
    if (SgOmpWorkshareStatement::pools.empty() == false) {
        auto block{SgOmpWorkshareStatement::pools.begin()};
        SgOmpWorkshareStatement::next_node = reinterpret_cast<SgOmpWorkshareStatement*>(*block);
        for (; block != SgOmpWorkshareStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpWorkshareStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpWorkshareStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpWorkshareStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpWorkshareStatement::pool_size-1]);
        }
    }
}

void
SgOmpWorkshareStatement::deleteMemoryPool() {
    for (auto p: SgOmpWorkshareStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpWorkshareStatement::next_node = nullptr;
    SgOmpWorkshareStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpWorkshareStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpWorkshareStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpWorkshareStatement)};
    for (size_t blockIndex = SgOmpWorkshareStatement::pools.size(); (blockIndex * SgOmpWorkshareStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpWorkshareStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpWorkshareStatement) = %" PRIuPTR " SgOmpWorkshareStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpWorkshareStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpWorkshareStatement),SgOmpWorkshareStatement::pool_size);
        }
#endif

        SgOmpWorkshareStatement* pointer{static_cast<SgOmpWorkshareStatement*>(ROSE_MALLOC(SgOmpWorkshareStatement::pool_size * sizeof(SgOmpWorkshareStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpWorkshareStatement::pool_size * sizeof(SgOmpWorkshareStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpWorkshareStatement::pool_size * sizeof(SgOmpWorkshareStatement));
#endif
        SgOmpWorkshareStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpWorkshareStatement::pool_size * sizeof(SgOmpWorkshareStatement), V_SgOmpWorkshareStatement));

        if (SgOmpWorkshareStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpWorkshareStatement* blkptr{reinterpret_cast<SgOmpWorkshareStatement*>(SgOmpWorkshareStatement::pools[blockIndex-1])};
                blkptr[SgOmpWorkshareStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpWorkshareStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpWorkshareStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpWorkshareStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpWorkshareStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpWorkshareStatement::pool_size - 1};
    SgOmpWorkshareStatement* testPointer{reinterpret_cast<SgOmpWorkshareStatement*>(SgOmpWorkshareStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpWorkshareStatement::pool_size * (SgOmpWorkshareStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpWorkshareStatementStroageClass
 * from its counterpart of SgOmpWorkshareStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpWorkshareStatement::initializeStorageClassArray(SgOmpWorkshareStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpWorkshareStatement::pools.begin(); block != SgOmpWorkshareStatement::pools.end(); ++block) {
        SgOmpWorkshareStatement* pointer{reinterpret_cast<SgOmpWorkshareStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpWorkshareStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 94467 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpClauseBodyStatement that takes its corresponding StorageClass as parameter
 */
SgOmpClauseBodyStatement::SgOmpClauseBodyStatement(const SgOmpClauseBodyStatementStorageClass& storageSource)  : SgOmpBodyStatement (storageSource) {

/* #line 94477 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_clauses = storageSource.storageOf_clauses.rebuildDataStoredInEasyStorageClass();
    SgOmpClausePtrList::iterator i_clauses = p_clauses.begin();
    for ( ; i_clauses != p_clauses.end(); ++i_clauses) {
        (*i_clauses) = (SgOmpClausePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_clauses)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpClauseBodyStatement*
SgOmpClauseBodyStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpClauseBodyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpClauseBodyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpClauseBodyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpClauseBodyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClauseBodyStatement)};
        unsigned long positionInPool{localIndex % SgOmpClauseBodyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpClauseBodyStatement::pool_size};
        returnPointer = &(((SgOmpClauseBodyStatement*)(SgOmpClauseBodyStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpClauseBodyStatement*
SgOmpClauseBodyStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpClauseBodyStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpClauseBodyStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpClauseBodyStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpClauseBodyStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpClauseBodyStatement)};
        unsigned long positionInPool{localIndex % SgOmpClauseBodyStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpClauseBodyStatement::pool_size};
        returnPointer = &(((SgOmpClauseBodyStatement*)(SgOmpClauseBodyStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpClauseBodyStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpClauseBodyStatement::pools.begin(); block != SgOmpClauseBodyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpClauseBodyStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpClauseBodyStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpClauseBodyStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpClauseBodyStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpClauseBodyStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpClauseBodyStatement::pools.begin(); block != SgOmpClauseBodyStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpClauseBodyStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpClauseBodyStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpClauseBodyStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpClauseBodyStatement::clearMemoryPool() {
    SgOmpClauseBodyStatement* tempPointer{nullptr};
    if (SgOmpClauseBodyStatement::pools.empty() == false) {
        auto block{SgOmpClauseBodyStatement::pools.begin()};
        SgOmpClauseBodyStatement::next_node = reinterpret_cast<SgOmpClauseBodyStatement*>(*block);
        for (; block != SgOmpClauseBodyStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpClauseBodyStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpClauseBodyStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpClauseBodyStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpClauseBodyStatement::pool_size-1]);
        }
    }
}

void
SgOmpClauseBodyStatement::deleteMemoryPool() {
    for (auto p: SgOmpClauseBodyStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpClauseBodyStatement::next_node = nullptr;
    SgOmpClauseBodyStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpClauseBodyStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClauseBodyStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpClauseBodyStatement)};
    for (size_t blockIndex = SgOmpClauseBodyStatement::pools.size(); (blockIndex * SgOmpClauseBodyStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClauseBodyStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpClauseBodyStatement) = %" PRIuPTR " SgOmpClauseBodyStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpClauseBodyStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpClauseBodyStatement),SgOmpClauseBodyStatement::pool_size);
        }
#endif

        SgOmpClauseBodyStatement* pointer{static_cast<SgOmpClauseBodyStatement*>(ROSE_MALLOC(SgOmpClauseBodyStatement::pool_size * sizeof(SgOmpClauseBodyStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpClauseBodyStatement::pool_size * sizeof(SgOmpClauseBodyStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpClauseBodyStatement::pool_size * sizeof(SgOmpClauseBodyStatement));
#endif
        SgOmpClauseBodyStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpClauseBodyStatement::pool_size * sizeof(SgOmpClauseBodyStatement), V_SgOmpClauseBodyStatement));

        if (SgOmpClauseBodyStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpClauseBodyStatement* blkptr{reinterpret_cast<SgOmpClauseBodyStatement*>(SgOmpClauseBodyStatement::pools[blockIndex-1])};
                blkptr[SgOmpClauseBodyStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpClauseBodyStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpClauseBodyStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpClauseBodyStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpClauseBodyStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpClauseBodyStatement::pool_size - 1};
    SgOmpClauseBodyStatement* testPointer{reinterpret_cast<SgOmpClauseBodyStatement*>(SgOmpClauseBodyStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpClauseBodyStatement::pool_size * (SgOmpClauseBodyStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpClauseBodyStatementStroageClass
 * from its counterpart of SgOmpClauseBodyStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpClauseBodyStatement::initializeStorageClassArray(SgOmpClauseBodyStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpClauseBodyStatement::pools.begin(); block != SgOmpClauseBodyStatement::pools.end(); ++block) {
        SgOmpClauseBodyStatement* pointer{reinterpret_cast<SgOmpClauseBodyStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpClauseBodyStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 94699 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpParallelStatement that takes its corresponding StorageClass as parameter
 */
SgOmpParallelStatement::SgOmpParallelStatement(const SgOmpParallelStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 94709 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpParallelStatement*
SgOmpParallelStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpParallelStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpParallelStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpParallelStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpParallelStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpParallelStatement)};
        unsigned long positionInPool{localIndex % SgOmpParallelStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpParallelStatement::pool_size};
        returnPointer = &(((SgOmpParallelStatement*)(SgOmpParallelStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpParallelStatement*
SgOmpParallelStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpParallelStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpParallelStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpParallelStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpParallelStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpParallelStatement)};
        unsigned long positionInPool{localIndex % SgOmpParallelStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpParallelStatement::pool_size};
        returnPointer = &(((SgOmpParallelStatement*)(SgOmpParallelStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpParallelStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpParallelStatement::pools.begin(); block != SgOmpParallelStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpParallelStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpParallelStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpParallelStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpParallelStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpParallelStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpParallelStatement::pools.begin(); block != SgOmpParallelStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpParallelStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpParallelStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpParallelStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpParallelStatement::clearMemoryPool() {
    SgOmpParallelStatement* tempPointer{nullptr};
    if (SgOmpParallelStatement::pools.empty() == false) {
        auto block{SgOmpParallelStatement::pools.begin()};
        SgOmpParallelStatement::next_node = reinterpret_cast<SgOmpParallelStatement*>(*block);
        for (; block != SgOmpParallelStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpParallelStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpParallelStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpParallelStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpParallelStatement::pool_size-1]);
        }
    }
}

void
SgOmpParallelStatement::deleteMemoryPool() {
    for (auto p: SgOmpParallelStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpParallelStatement::next_node = nullptr;
    SgOmpParallelStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpParallelStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpParallelStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpParallelStatement)};
    for (size_t blockIndex = SgOmpParallelStatement::pools.size(); (blockIndex * SgOmpParallelStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpParallelStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpParallelStatement) = %" PRIuPTR " SgOmpParallelStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpParallelStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpParallelStatement),SgOmpParallelStatement::pool_size);
        }
#endif

        SgOmpParallelStatement* pointer{static_cast<SgOmpParallelStatement*>(ROSE_MALLOC(SgOmpParallelStatement::pool_size * sizeof(SgOmpParallelStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpParallelStatement::pool_size * sizeof(SgOmpParallelStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpParallelStatement::pool_size * sizeof(SgOmpParallelStatement));
#endif
        SgOmpParallelStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpParallelStatement::pool_size * sizeof(SgOmpParallelStatement), V_SgOmpParallelStatement));

        if (SgOmpParallelStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpParallelStatement* blkptr{reinterpret_cast<SgOmpParallelStatement*>(SgOmpParallelStatement::pools[blockIndex-1])};
                blkptr[SgOmpParallelStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpParallelStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpParallelStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpParallelStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpParallelStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpParallelStatement::pool_size - 1};
    SgOmpParallelStatement* testPointer{reinterpret_cast<SgOmpParallelStatement*>(SgOmpParallelStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpParallelStatement::pool_size * (SgOmpParallelStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpParallelStatementStroageClass
 * from its counterpart of SgOmpParallelStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpParallelStatement::initializeStorageClassArray(SgOmpParallelStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpParallelStatement::pools.begin(); block != SgOmpParallelStatement::pools.end(); ++block) {
        SgOmpParallelStatement* pointer{reinterpret_cast<SgOmpParallelStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpParallelStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 94926 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpSingleStatement that takes its corresponding StorageClass as parameter
 */
SgOmpSingleStatement::SgOmpSingleStatement(const SgOmpSingleStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 94936 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpSingleStatement*
SgOmpSingleStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpSingleStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSingleStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSingleStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSingleStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSingleStatement)};
        unsigned long positionInPool{localIndex % SgOmpSingleStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSingleStatement::pool_size};
        returnPointer = &(((SgOmpSingleStatement*)(SgOmpSingleStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpSingleStatement*
SgOmpSingleStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpSingleStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSingleStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSingleStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSingleStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpSingleStatement)};
        unsigned long positionInPool{localIndex % SgOmpSingleStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSingleStatement::pool_size};
        returnPointer = &(((SgOmpSingleStatement*)(SgOmpSingleStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpSingleStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpSingleStatement::pools.begin(); block != SgOmpSingleStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSingleStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSingleStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpSingleStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpSingleStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpSingleStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpSingleStatement::pools.begin(); block != SgOmpSingleStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSingleStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSingleStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpSingleStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpSingleStatement::clearMemoryPool() {
    SgOmpSingleStatement* tempPointer{nullptr};
    if (SgOmpSingleStatement::pools.empty() == false) {
        auto block{SgOmpSingleStatement::pools.begin()};
        SgOmpSingleStatement::next_node = reinterpret_cast<SgOmpSingleStatement*>(*block);
        for (; block != SgOmpSingleStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpSingleStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpSingleStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpSingleStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpSingleStatement::pool_size-1]);
        }
    }
}

void
SgOmpSingleStatement::deleteMemoryPool() {
    for (auto p: SgOmpSingleStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpSingleStatement::next_node = nullptr;
    SgOmpSingleStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpSingleStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSingleStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSingleStatement)};
    for (size_t blockIndex = SgOmpSingleStatement::pools.size(); (blockIndex * SgOmpSingleStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSingleStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSingleStatement) = %" PRIuPTR " SgOmpSingleStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSingleStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSingleStatement),SgOmpSingleStatement::pool_size);
        }
#endif

        SgOmpSingleStatement* pointer{static_cast<SgOmpSingleStatement*>(ROSE_MALLOC(SgOmpSingleStatement::pool_size * sizeof(SgOmpSingleStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpSingleStatement::pool_size * sizeof(SgOmpSingleStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpSingleStatement::pool_size * sizeof(SgOmpSingleStatement));
#endif
        SgOmpSingleStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpSingleStatement::pool_size * sizeof(SgOmpSingleStatement), V_SgOmpSingleStatement));

        if (SgOmpSingleStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpSingleStatement* blkptr{reinterpret_cast<SgOmpSingleStatement*>(SgOmpSingleStatement::pools[blockIndex-1])};
                blkptr[SgOmpSingleStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpSingleStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpSingleStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpSingleStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpSingleStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpSingleStatement::pool_size - 1};
    SgOmpSingleStatement* testPointer{reinterpret_cast<SgOmpSingleStatement*>(SgOmpSingleStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpSingleStatement::pool_size * (SgOmpSingleStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpSingleStatementStroageClass
 * from its counterpart of SgOmpSingleStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpSingleStatement::initializeStorageClassArray(SgOmpSingleStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpSingleStatement::pools.begin(); block != SgOmpSingleStatement::pools.end(); ++block) {
        SgOmpSingleStatement* pointer{reinterpret_cast<SgOmpSingleStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSingleStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 95153 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpAtomicStatement that takes its corresponding StorageClass as parameter
 */
SgOmpAtomicStatement::SgOmpAtomicStatement(const SgOmpAtomicStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 95163 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpAtomicStatement*
SgOmpAtomicStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpAtomicStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAtomicStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAtomicStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpAtomicStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicStatement)};
        unsigned long positionInPool{localIndex % SgOmpAtomicStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpAtomicStatement::pool_size};
        returnPointer = &(((SgOmpAtomicStatement*)(SgOmpAtomicStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpAtomicStatement*
SgOmpAtomicStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpAtomicStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAtomicStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAtomicStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpAtomicStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpAtomicStatement)};
        unsigned long positionInPool{localIndex % SgOmpAtomicStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpAtomicStatement::pool_size};
        returnPointer = &(((SgOmpAtomicStatement*)(SgOmpAtomicStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpAtomicStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpAtomicStatement::pools.begin(); block != SgOmpAtomicStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpAtomicStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpAtomicStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpAtomicStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpAtomicStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpAtomicStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpAtomicStatement::pools.begin(); block != SgOmpAtomicStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpAtomicStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpAtomicStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpAtomicStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpAtomicStatement::clearMemoryPool() {
    SgOmpAtomicStatement* tempPointer{nullptr};
    if (SgOmpAtomicStatement::pools.empty() == false) {
        auto block{SgOmpAtomicStatement::pools.begin()};
        SgOmpAtomicStatement::next_node = reinterpret_cast<SgOmpAtomicStatement*>(*block);
        for (; block != SgOmpAtomicStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpAtomicStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpAtomicStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpAtomicStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpAtomicStatement::pool_size-1]);
        }
    }
}

void
SgOmpAtomicStatement::deleteMemoryPool() {
    for (auto p: SgOmpAtomicStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpAtomicStatement::next_node = nullptr;
    SgOmpAtomicStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpAtomicStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAtomicStatement)};
    for (size_t blockIndex = SgOmpAtomicStatement::pools.size(); (blockIndex * SgOmpAtomicStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAtomicStatement) = %" PRIuPTR " SgOmpAtomicStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpAtomicStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpAtomicStatement),SgOmpAtomicStatement::pool_size);
        }
#endif

        SgOmpAtomicStatement* pointer{static_cast<SgOmpAtomicStatement*>(ROSE_MALLOC(SgOmpAtomicStatement::pool_size * sizeof(SgOmpAtomicStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpAtomicStatement::pool_size * sizeof(SgOmpAtomicStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpAtomicStatement::pool_size * sizeof(SgOmpAtomicStatement));
#endif
        SgOmpAtomicStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpAtomicStatement::pool_size * sizeof(SgOmpAtomicStatement), V_SgOmpAtomicStatement));

        if (SgOmpAtomicStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpAtomicStatement* blkptr{reinterpret_cast<SgOmpAtomicStatement*>(SgOmpAtomicStatement::pools[blockIndex-1])};
                blkptr[SgOmpAtomicStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpAtomicStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpAtomicStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpAtomicStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpAtomicStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpAtomicStatement::pool_size - 1};
    SgOmpAtomicStatement* testPointer{reinterpret_cast<SgOmpAtomicStatement*>(SgOmpAtomicStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpAtomicStatement::pool_size * (SgOmpAtomicStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpAtomicStatementStroageClass
 * from its counterpart of SgOmpAtomicStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpAtomicStatement::initializeStorageClassArray(SgOmpAtomicStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpAtomicStatement::pools.begin(); block != SgOmpAtomicStatement::pools.end(); ++block) {
        SgOmpAtomicStatement* pointer{reinterpret_cast<SgOmpAtomicStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpAtomicStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 95380 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpTaskStatement that takes its corresponding StorageClass as parameter
 */
SgOmpTaskStatement::SgOmpTaskStatement(const SgOmpTaskStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 95390 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpTaskStatement*
SgOmpTaskStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpTaskStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTaskStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTaskStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTaskStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskStatement)};
        unsigned long positionInPool{localIndex % SgOmpTaskStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTaskStatement::pool_size};
        returnPointer = &(((SgOmpTaskStatement*)(SgOmpTaskStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpTaskStatement*
SgOmpTaskStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpTaskStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTaskStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTaskStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTaskStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpTaskStatement)};
        unsigned long positionInPool{localIndex % SgOmpTaskStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTaskStatement::pool_size};
        returnPointer = &(((SgOmpTaskStatement*)(SgOmpTaskStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpTaskStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpTaskStatement::pools.begin(); block != SgOmpTaskStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTaskStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTaskStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpTaskStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpTaskStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpTaskStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpTaskStatement::pools.begin(); block != SgOmpTaskStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTaskStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTaskStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpTaskStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpTaskStatement::clearMemoryPool() {
    SgOmpTaskStatement* tempPointer{nullptr};
    if (SgOmpTaskStatement::pools.empty() == false) {
        auto block{SgOmpTaskStatement::pools.begin()};
        SgOmpTaskStatement::next_node = reinterpret_cast<SgOmpTaskStatement*>(*block);
        for (; block != SgOmpTaskStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpTaskStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpTaskStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpTaskStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpTaskStatement::pool_size-1]);
        }
    }
}

void
SgOmpTaskStatement::deleteMemoryPool() {
    for (auto p: SgOmpTaskStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpTaskStatement::next_node = nullptr;
    SgOmpTaskStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpTaskStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTaskStatement)};
    for (size_t blockIndex = SgOmpTaskStatement::pools.size(); (blockIndex * SgOmpTaskStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTaskStatement) = %" PRIuPTR " SgOmpTaskStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTaskStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTaskStatement),SgOmpTaskStatement::pool_size);
        }
#endif

        SgOmpTaskStatement* pointer{static_cast<SgOmpTaskStatement*>(ROSE_MALLOC(SgOmpTaskStatement::pool_size * sizeof(SgOmpTaskStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpTaskStatement::pool_size * sizeof(SgOmpTaskStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpTaskStatement::pool_size * sizeof(SgOmpTaskStatement));
#endif
        SgOmpTaskStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpTaskStatement::pool_size * sizeof(SgOmpTaskStatement), V_SgOmpTaskStatement));

        if (SgOmpTaskStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpTaskStatement* blkptr{reinterpret_cast<SgOmpTaskStatement*>(SgOmpTaskStatement::pools[blockIndex-1])};
                blkptr[SgOmpTaskStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpTaskStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpTaskStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpTaskStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpTaskStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpTaskStatement::pool_size - 1};
    SgOmpTaskStatement* testPointer{reinterpret_cast<SgOmpTaskStatement*>(SgOmpTaskStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpTaskStatement::pool_size * (SgOmpTaskStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpTaskStatementStroageClass
 * from its counterpart of SgOmpTaskStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpTaskStatement::initializeStorageClassArray(SgOmpTaskStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpTaskStatement::pools.begin(); block != SgOmpTaskStatement::pools.end(); ++block) {
        SgOmpTaskStatement* pointer{reinterpret_cast<SgOmpTaskStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTaskStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 95607 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpForStatement that takes its corresponding StorageClass as parameter
 */
SgOmpForStatement::SgOmpForStatement(const SgOmpForStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 95617 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpForStatement*
SgOmpForStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForStatement)};
        unsigned long positionInPool{localIndex % SgOmpForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpForStatement::pool_size};
        returnPointer = &(((SgOmpForStatement*)(SgOmpForStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpForStatement*
SgOmpForStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpForStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpForStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpForStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpForStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpForStatement)};
        unsigned long positionInPool{localIndex % SgOmpForStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpForStatement::pool_size};
        returnPointer = &(((SgOmpForStatement*)(SgOmpForStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpForStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpForStatement::pools.begin(); block != SgOmpForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpForStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpForStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpForStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpForStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpForStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpForStatement::pools.begin(); block != SgOmpForStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpForStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpForStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpForStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpForStatement::clearMemoryPool() {
    SgOmpForStatement* tempPointer{nullptr};
    if (SgOmpForStatement::pools.empty() == false) {
        auto block{SgOmpForStatement::pools.begin()};
        SgOmpForStatement::next_node = reinterpret_cast<SgOmpForStatement*>(*block);
        for (; block != SgOmpForStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpForStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpForStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpForStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpForStatement::pool_size-1]);
        }
    }
}

void
SgOmpForStatement::deleteMemoryPool() {
    for (auto p: SgOmpForStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpForStatement::next_node = nullptr;
    SgOmpForStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpForStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpForStatement)};
    for (size_t blockIndex = SgOmpForStatement::pools.size(); (blockIndex * SgOmpForStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpForStatement) = %" PRIuPTR " SgOmpForStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpForStatement),SgOmpForStatement::pool_size);
        }
#endif

        SgOmpForStatement* pointer{static_cast<SgOmpForStatement*>(ROSE_MALLOC(SgOmpForStatement::pool_size * sizeof(SgOmpForStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpForStatement::pool_size * sizeof(SgOmpForStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpForStatement::pool_size * sizeof(SgOmpForStatement));
#endif
        SgOmpForStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpForStatement::pool_size * sizeof(SgOmpForStatement), V_SgOmpForStatement));

        if (SgOmpForStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpForStatement* blkptr{reinterpret_cast<SgOmpForStatement*>(SgOmpForStatement::pools[blockIndex-1])};
                blkptr[SgOmpForStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpForStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpForStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpForStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpForStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpForStatement::pool_size - 1};
    SgOmpForStatement* testPointer{reinterpret_cast<SgOmpForStatement*>(SgOmpForStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpForStatement::pool_size * (SgOmpForStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpForStatementStroageClass
 * from its counterpart of SgOmpForStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpForStatement::initializeStorageClassArray(SgOmpForStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpForStatement::pools.begin(); block != SgOmpForStatement::pools.end(); ++block) {
        SgOmpForStatement* pointer{reinterpret_cast<SgOmpForStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpForStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 95834 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpDoStatement that takes its corresponding StorageClass as parameter
 */
SgOmpDoStatement::SgOmpDoStatement(const SgOmpDoStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 95844 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpDoStatement*
SgOmpDoStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpDoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpDoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDoStatement)};
        unsigned long positionInPool{localIndex % SgOmpDoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDoStatement::pool_size};
        returnPointer = &(((SgOmpDoStatement*)(SgOmpDoStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpDoStatement*
SgOmpDoStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpDoStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDoStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDoStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpDoStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpDoStatement)};
        unsigned long positionInPool{localIndex % SgOmpDoStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpDoStatement::pool_size};
        returnPointer = &(((SgOmpDoStatement*)(SgOmpDoStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpDoStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpDoStatement::pools.begin(); block != SgOmpDoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDoStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpDoStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpDoStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpDoStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpDoStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpDoStatement::pools.begin(); block != SgOmpDoStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpDoStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpDoStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpDoStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpDoStatement::clearMemoryPool() {
    SgOmpDoStatement* tempPointer{nullptr};
    if (SgOmpDoStatement::pools.empty() == false) {
        auto block{SgOmpDoStatement::pools.begin()};
        SgOmpDoStatement::next_node = reinterpret_cast<SgOmpDoStatement*>(*block);
        for (; block != SgOmpDoStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpDoStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpDoStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpDoStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpDoStatement::pool_size-1]);
        }
    }
}

void
SgOmpDoStatement::deleteMemoryPool() {
    for (auto p: SgOmpDoStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpDoStatement::next_node = nullptr;
    SgOmpDoStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpDoStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDoStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDoStatement)};
    for (size_t blockIndex = SgOmpDoStatement::pools.size(); (blockIndex * SgOmpDoStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDoStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDoStatement) = %" PRIuPTR " SgOmpDoStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpDoStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpDoStatement),SgOmpDoStatement::pool_size);
        }
#endif

        SgOmpDoStatement* pointer{static_cast<SgOmpDoStatement*>(ROSE_MALLOC(SgOmpDoStatement::pool_size * sizeof(SgOmpDoStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpDoStatement::pool_size * sizeof(SgOmpDoStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpDoStatement::pool_size * sizeof(SgOmpDoStatement));
#endif
        SgOmpDoStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpDoStatement::pool_size * sizeof(SgOmpDoStatement), V_SgOmpDoStatement));

        if (SgOmpDoStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpDoStatement* blkptr{reinterpret_cast<SgOmpDoStatement*>(SgOmpDoStatement::pools[blockIndex-1])};
                blkptr[SgOmpDoStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpDoStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpDoStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpDoStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpDoStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpDoStatement::pool_size - 1};
    SgOmpDoStatement* testPointer{reinterpret_cast<SgOmpDoStatement*>(SgOmpDoStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpDoStatement::pool_size * (SgOmpDoStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpDoStatementStroageClass
 * from its counterpart of SgOmpDoStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpDoStatement::initializeStorageClassArray(SgOmpDoStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpDoStatement::pools.begin(); block != SgOmpDoStatement::pools.end(); ++block) {
        SgOmpDoStatement* pointer{reinterpret_cast<SgOmpDoStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpDoStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 96061 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpSectionsStatement that takes its corresponding StorageClass as parameter
 */
SgOmpSectionsStatement::SgOmpSectionsStatement(const SgOmpSectionsStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 96071 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpSectionsStatement*
SgOmpSectionsStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpSectionsStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSectionsStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSectionsStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSectionsStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionsStatement)};
        unsigned long positionInPool{localIndex % SgOmpSectionsStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSectionsStatement::pool_size};
        returnPointer = &(((SgOmpSectionsStatement*)(SgOmpSectionsStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpSectionsStatement*
SgOmpSectionsStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpSectionsStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSectionsStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSectionsStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSectionsStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpSectionsStatement)};
        unsigned long positionInPool{localIndex % SgOmpSectionsStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSectionsStatement::pool_size};
        returnPointer = &(((SgOmpSectionsStatement*)(SgOmpSectionsStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpSectionsStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpSectionsStatement::pools.begin(); block != SgOmpSectionsStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSectionsStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSectionsStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpSectionsStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpSectionsStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpSectionsStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpSectionsStatement::pools.begin(); block != SgOmpSectionsStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSectionsStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSectionsStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpSectionsStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpSectionsStatement::clearMemoryPool() {
    SgOmpSectionsStatement* tempPointer{nullptr};
    if (SgOmpSectionsStatement::pools.empty() == false) {
        auto block{SgOmpSectionsStatement::pools.begin()};
        SgOmpSectionsStatement::next_node = reinterpret_cast<SgOmpSectionsStatement*>(*block);
        for (; block != SgOmpSectionsStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpSectionsStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpSectionsStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpSectionsStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpSectionsStatement::pool_size-1]);
        }
    }
}

void
SgOmpSectionsStatement::deleteMemoryPool() {
    for (auto p: SgOmpSectionsStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpSectionsStatement::next_node = nullptr;
    SgOmpSectionsStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpSectionsStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionsStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSectionsStatement)};
    for (size_t blockIndex = SgOmpSectionsStatement::pools.size(); (blockIndex * SgOmpSectionsStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionsStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSectionsStatement) = %" PRIuPTR " SgOmpSectionsStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSectionsStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSectionsStatement),SgOmpSectionsStatement::pool_size);
        }
#endif

        SgOmpSectionsStatement* pointer{static_cast<SgOmpSectionsStatement*>(ROSE_MALLOC(SgOmpSectionsStatement::pool_size * sizeof(SgOmpSectionsStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpSectionsStatement::pool_size * sizeof(SgOmpSectionsStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpSectionsStatement::pool_size * sizeof(SgOmpSectionsStatement));
#endif
        SgOmpSectionsStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpSectionsStatement::pool_size * sizeof(SgOmpSectionsStatement), V_SgOmpSectionsStatement));

        if (SgOmpSectionsStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpSectionsStatement* blkptr{reinterpret_cast<SgOmpSectionsStatement*>(SgOmpSectionsStatement::pools[blockIndex-1])};
                blkptr[SgOmpSectionsStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpSectionsStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpSectionsStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpSectionsStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpSectionsStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpSectionsStatement::pool_size - 1};
    SgOmpSectionsStatement* testPointer{reinterpret_cast<SgOmpSectionsStatement*>(SgOmpSectionsStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpSectionsStatement::pool_size * (SgOmpSectionsStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpSectionsStatementStroageClass
 * from its counterpart of SgOmpSectionsStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpSectionsStatement::initializeStorageClassArray(SgOmpSectionsStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpSectionsStatement::pools.begin(); block != SgOmpSectionsStatement::pools.end(); ++block) {
        SgOmpSectionsStatement* pointer{reinterpret_cast<SgOmpSectionsStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSectionsStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 96288 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpTargetStatement that takes its corresponding StorageClass as parameter
 */
SgOmpTargetStatement::SgOmpTargetStatement(const SgOmpTargetStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 96298 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpTargetStatement*
SgOmpTargetStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpTargetStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTargetStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTargetStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTargetStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetStatement)};
        unsigned long positionInPool{localIndex % SgOmpTargetStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTargetStatement::pool_size};
        returnPointer = &(((SgOmpTargetStatement*)(SgOmpTargetStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpTargetStatement*
SgOmpTargetStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpTargetStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTargetStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTargetStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTargetStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpTargetStatement)};
        unsigned long positionInPool{localIndex % SgOmpTargetStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTargetStatement::pool_size};
        returnPointer = &(((SgOmpTargetStatement*)(SgOmpTargetStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpTargetStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpTargetStatement::pools.begin(); block != SgOmpTargetStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTargetStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTargetStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpTargetStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpTargetStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpTargetStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpTargetStatement::pools.begin(); block != SgOmpTargetStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTargetStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTargetStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpTargetStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpTargetStatement::clearMemoryPool() {
    SgOmpTargetStatement* tempPointer{nullptr};
    if (SgOmpTargetStatement::pools.empty() == false) {
        auto block{SgOmpTargetStatement::pools.begin()};
        SgOmpTargetStatement::next_node = reinterpret_cast<SgOmpTargetStatement*>(*block);
        for (; block != SgOmpTargetStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpTargetStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpTargetStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpTargetStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpTargetStatement::pool_size-1]);
        }
    }
}

void
SgOmpTargetStatement::deleteMemoryPool() {
    for (auto p: SgOmpTargetStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpTargetStatement::next_node = nullptr;
    SgOmpTargetStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpTargetStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTargetStatement)};
    for (size_t blockIndex = SgOmpTargetStatement::pools.size(); (blockIndex * SgOmpTargetStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTargetStatement) = %" PRIuPTR " SgOmpTargetStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTargetStatement),SgOmpTargetStatement::pool_size);
        }
#endif

        SgOmpTargetStatement* pointer{static_cast<SgOmpTargetStatement*>(ROSE_MALLOC(SgOmpTargetStatement::pool_size * sizeof(SgOmpTargetStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpTargetStatement::pool_size * sizeof(SgOmpTargetStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpTargetStatement::pool_size * sizeof(SgOmpTargetStatement));
#endif
        SgOmpTargetStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpTargetStatement::pool_size * sizeof(SgOmpTargetStatement), V_SgOmpTargetStatement));

        if (SgOmpTargetStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpTargetStatement* blkptr{reinterpret_cast<SgOmpTargetStatement*>(SgOmpTargetStatement::pools[blockIndex-1])};
                blkptr[SgOmpTargetStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpTargetStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpTargetStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpTargetStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpTargetStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpTargetStatement::pool_size - 1};
    SgOmpTargetStatement* testPointer{reinterpret_cast<SgOmpTargetStatement*>(SgOmpTargetStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpTargetStatement::pool_size * (SgOmpTargetStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpTargetStatementStroageClass
 * from its counterpart of SgOmpTargetStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpTargetStatement::initializeStorageClassArray(SgOmpTargetStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpTargetStatement::pools.begin(); block != SgOmpTargetStatement::pools.end(); ++block) {
        SgOmpTargetStatement* pointer{reinterpret_cast<SgOmpTargetStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTargetStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 96515 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpTargetDataStatement that takes its corresponding StorageClass as parameter
 */
SgOmpTargetDataStatement::SgOmpTargetDataStatement(const SgOmpTargetDataStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 96525 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpTargetDataStatement*
SgOmpTargetDataStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpTargetDataStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTargetDataStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTargetDataStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpTargetDataStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetDataStatement)};
        unsigned long positionInPool{localIndex % SgOmpTargetDataStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTargetDataStatement::pool_size};
        returnPointer = &(((SgOmpTargetDataStatement*)(SgOmpTargetDataStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpTargetDataStatement*
SgOmpTargetDataStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpTargetDataStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTargetDataStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTargetDataStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpTargetDataStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpTargetDataStatement)};
        unsigned long positionInPool{localIndex % SgOmpTargetDataStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpTargetDataStatement::pool_size};
        returnPointer = &(((SgOmpTargetDataStatement*)(SgOmpTargetDataStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpTargetDataStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpTargetDataStatement::pools.begin(); block != SgOmpTargetDataStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTargetDataStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTargetDataStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpTargetDataStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpTargetDataStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpTargetDataStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpTargetDataStatement::pools.begin(); block != SgOmpTargetDataStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpTargetDataStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTargetDataStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpTargetDataStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpTargetDataStatement::clearMemoryPool() {
    SgOmpTargetDataStatement* tempPointer{nullptr};
    if (SgOmpTargetDataStatement::pools.empty() == false) {
        auto block{SgOmpTargetDataStatement::pools.begin()};
        SgOmpTargetDataStatement::next_node = reinterpret_cast<SgOmpTargetDataStatement*>(*block);
        for (; block != SgOmpTargetDataStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpTargetDataStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpTargetDataStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpTargetDataStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpTargetDataStatement::pool_size-1]);
        }
    }
}

void
SgOmpTargetDataStatement::deleteMemoryPool() {
    for (auto p: SgOmpTargetDataStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpTargetDataStatement::next_node = nullptr;
    SgOmpTargetDataStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpTargetDataStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetDataStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTargetDataStatement)};
    for (size_t blockIndex = SgOmpTargetDataStatement::pools.size(); (blockIndex * SgOmpTargetDataStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetDataStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTargetDataStatement) = %" PRIuPTR " SgOmpTargetDataStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpTargetDataStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpTargetDataStatement),SgOmpTargetDataStatement::pool_size);
        }
#endif

        SgOmpTargetDataStatement* pointer{static_cast<SgOmpTargetDataStatement*>(ROSE_MALLOC(SgOmpTargetDataStatement::pool_size * sizeof(SgOmpTargetDataStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpTargetDataStatement::pool_size * sizeof(SgOmpTargetDataStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpTargetDataStatement::pool_size * sizeof(SgOmpTargetDataStatement));
#endif
        SgOmpTargetDataStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpTargetDataStatement::pool_size * sizeof(SgOmpTargetDataStatement), V_SgOmpTargetDataStatement));

        if (SgOmpTargetDataStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpTargetDataStatement* blkptr{reinterpret_cast<SgOmpTargetDataStatement*>(SgOmpTargetDataStatement::pools[blockIndex-1])};
                blkptr[SgOmpTargetDataStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpTargetDataStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpTargetDataStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpTargetDataStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpTargetDataStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpTargetDataStatement::pool_size - 1};
    SgOmpTargetDataStatement* testPointer{reinterpret_cast<SgOmpTargetDataStatement*>(SgOmpTargetDataStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpTargetDataStatement::pool_size * (SgOmpTargetDataStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpTargetDataStatementStroageClass
 * from its counterpart of SgOmpTargetDataStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpTargetDataStatement::initializeStorageClassArray(SgOmpTargetDataStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpTargetDataStatement::pools.begin(); block != SgOmpTargetDataStatement::pools.end(); ++block) {
        SgOmpTargetDataStatement* pointer{reinterpret_cast<SgOmpTargetDataStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpTargetDataStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 96742 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpSimdStatement that takes its corresponding StorageClass as parameter
 */
SgOmpSimdStatement::SgOmpSimdStatement(const SgOmpSimdStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 96752 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpSimdStatement*
SgOmpSimdStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpSimdStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSimdStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSimdStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpSimdStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdStatement)};
        unsigned long positionInPool{localIndex % SgOmpSimdStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSimdStatement::pool_size};
        returnPointer = &(((SgOmpSimdStatement*)(SgOmpSimdStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpSimdStatement*
SgOmpSimdStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpSimdStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSimdStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSimdStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpSimdStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpSimdStatement)};
        unsigned long positionInPool{localIndex % SgOmpSimdStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpSimdStatement::pool_size};
        returnPointer = &(((SgOmpSimdStatement*)(SgOmpSimdStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpSimdStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpSimdStatement::pools.begin(); block != SgOmpSimdStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSimdStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpSimdStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpSimdStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpSimdStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpSimdStatement::pools.begin(); block != SgOmpSimdStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSimdStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpSimdStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpSimdStatement::clearMemoryPool() {
    SgOmpSimdStatement* tempPointer{nullptr};
    if (SgOmpSimdStatement::pools.empty() == false) {
        auto block{SgOmpSimdStatement::pools.begin()};
        SgOmpSimdStatement::next_node = reinterpret_cast<SgOmpSimdStatement*>(*block);
        for (; block != SgOmpSimdStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpSimdStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpSimdStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpSimdStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpSimdStatement::pool_size-1]);
        }
    }
}

void
SgOmpSimdStatement::deleteMemoryPool() {
    for (auto p: SgOmpSimdStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpSimdStatement::next_node = nullptr;
    SgOmpSimdStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpSimdStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSimdStatement)};
    for (size_t blockIndex = SgOmpSimdStatement::pools.size(); (blockIndex * SgOmpSimdStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSimdStatement) = %" PRIuPTR " SgOmpSimdStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpSimdStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpSimdStatement),SgOmpSimdStatement::pool_size);
        }
#endif

        SgOmpSimdStatement* pointer{static_cast<SgOmpSimdStatement*>(ROSE_MALLOC(SgOmpSimdStatement::pool_size * sizeof(SgOmpSimdStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpSimdStatement::pool_size * sizeof(SgOmpSimdStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpSimdStatement::pool_size * sizeof(SgOmpSimdStatement));
#endif
        SgOmpSimdStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpSimdStatement::pool_size * sizeof(SgOmpSimdStatement), V_SgOmpSimdStatement));

        if (SgOmpSimdStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpSimdStatement* blkptr{reinterpret_cast<SgOmpSimdStatement*>(SgOmpSimdStatement::pools[blockIndex-1])};
                blkptr[SgOmpSimdStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpSimdStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpSimdStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpSimdStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpSimdStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpSimdStatement::pool_size - 1};
    SgOmpSimdStatement* testPointer{reinterpret_cast<SgOmpSimdStatement*>(SgOmpSimdStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpSimdStatement::pool_size * (SgOmpSimdStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpSimdStatementStroageClass
 * from its counterpart of SgOmpSimdStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpSimdStatement::initializeStorageClassArray(SgOmpSimdStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpSimdStatement::pools.begin(); block != SgOmpSimdStatement::pools.end(); ++block) {
        SgOmpSimdStatement* pointer{reinterpret_cast<SgOmpSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpSimdStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 96969 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOmpForSimdStatement that takes its corresponding StorageClass as parameter
 */
SgOmpForSimdStatement::SgOmpForSimdStatement(const SgOmpForSimdStatementStorageClass& storageSource)  : SgOmpClauseBodyStatement (storageSource) {

/* #line 96979 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOmpForSimdStatement*
SgOmpForSimdStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOmpForSimdStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpForSimdStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpForSimdStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOmpForSimdStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForSimdStatement)};
        unsigned long positionInPool{localIndex % SgOmpForSimdStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpForSimdStatement::pool_size};
        returnPointer = &(((SgOmpForSimdStatement*)(SgOmpForSimdStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOmpForSimdStatement*
SgOmpForSimdStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOmpForSimdStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpForSimdStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpForSimdStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOmpForSimdStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOmpForSimdStatement)};
        unsigned long positionInPool{localIndex % SgOmpForSimdStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOmpForSimdStatement::pool_size};
        returnPointer = &(((SgOmpForSimdStatement*)(SgOmpForSimdStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOmpForSimdStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOmpForSimdStatement::pools.begin(); block != SgOmpForSimdStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpForSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpForSimdStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOmpForSimdStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOmpForSimdStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOmpForSimdStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgOmpForSimdStatement::pools.begin(); block != SgOmpForSimdStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOmpForSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpForSimdStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOmpForSimdStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOmpForSimdStatement::clearMemoryPool() {
    SgOmpForSimdStatement* tempPointer{nullptr};
    if (SgOmpForSimdStatement::pools.empty() == false) {
        auto block{SgOmpForSimdStatement::pools.begin()};
        SgOmpForSimdStatement::next_node = reinterpret_cast<SgOmpForSimdStatement*>(*block);
        for (; block != SgOmpForSimdStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOmpForSimdStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOmpForSimdStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOmpForSimdStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOmpForSimdStatement::pool_size-1]);
        }
    }
}

void
SgOmpForSimdStatement::deleteMemoryPool() {
    for (auto p: SgOmpForSimdStatement::pools) {
        ROSE_FREE(p);
    }
    SgOmpForSimdStatement::next_node = nullptr;
    SgOmpForSimdStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOmpForSimdStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForSimdStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpForSimdStatement)};
    for (size_t blockIndex = SgOmpForSimdStatement::pools.size(); (blockIndex * SgOmpForSimdStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForSimdStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpForSimdStatement) = %" PRIuPTR " SgOmpForSimdStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOmpForSimdStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOmpForSimdStatement),SgOmpForSimdStatement::pool_size);
        }
#endif

        SgOmpForSimdStatement* pointer{static_cast<SgOmpForSimdStatement*>(ROSE_MALLOC(SgOmpForSimdStatement::pool_size * sizeof(SgOmpForSimdStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOmpForSimdStatement::pool_size * sizeof(SgOmpForSimdStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOmpForSimdStatement::pool_size * sizeof(SgOmpForSimdStatement));
#endif
        SgOmpForSimdStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOmpForSimdStatement::pool_size * sizeof(SgOmpForSimdStatement), V_SgOmpForSimdStatement));

        if (SgOmpForSimdStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOmpForSimdStatement* blkptr{reinterpret_cast<SgOmpForSimdStatement*>(SgOmpForSimdStatement::pools[blockIndex-1])};
                blkptr[SgOmpForSimdStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOmpForSimdStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOmpForSimdStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOmpForSimdStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOmpForSimdStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOmpForSimdStatement::pool_size - 1};
    SgOmpForSimdStatement* testPointer{reinterpret_cast<SgOmpForSimdStatement*>(SgOmpForSimdStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOmpForSimdStatement::pool_size * (SgOmpForSimdStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOmpForSimdStatementStroageClass
 * from its counterpart of SgOmpForSimdStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOmpForSimdStatement::initializeStorageClassArray(SgOmpForSimdStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOmpForSimdStatement::pools.begin(); block != SgOmpForSimdStatement::pools.end(); ++block) {
        SgOmpForSimdStatement* pointer{reinterpret_cast<SgOmpForSimdStatement*>(*block)};
        for (unsigned i = 0; i < SgOmpForSimdStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 97196 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSequenceStatement that takes its corresponding StorageClass as parameter
 */
SgSequenceStatement::SgSequenceStatement(const SgSequenceStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 97206 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSequenceStatement*
SgSequenceStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSequenceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSequenceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSequenceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSequenceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSequenceStatement)};
        unsigned long positionInPool{localIndex % SgSequenceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSequenceStatement::pool_size};
        returnPointer = &(((SgSequenceStatement*)(SgSequenceStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSequenceStatement*
SgSequenceStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSequenceStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSequenceStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSequenceStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSequenceStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSequenceStatement)};
        unsigned long positionInPool{localIndex % SgSequenceStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSequenceStatement::pool_size};
        returnPointer = &(((SgSequenceStatement*)(SgSequenceStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSequenceStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSequenceStatement::pools.begin(); block != SgSequenceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSequenceStatement*>(*block)};
        for (unsigned i = 0; i < SgSequenceStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSequenceStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSequenceStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSequenceStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgSequenceStatement::pools.begin(); block != SgSequenceStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSequenceStatement*>(*block)};
        for (unsigned i = 0; i < SgSequenceStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSequenceStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSequenceStatement::clearMemoryPool() {
    SgSequenceStatement* tempPointer{nullptr};
    if (SgSequenceStatement::pools.empty() == false) {
        auto block{SgSequenceStatement::pools.begin()};
        SgSequenceStatement::next_node = reinterpret_cast<SgSequenceStatement*>(*block);
        for (; block != SgSequenceStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSequenceStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSequenceStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSequenceStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSequenceStatement::pool_size-1]);
        }
    }
}

void
SgSequenceStatement::deleteMemoryPool() {
    for (auto p: SgSequenceStatement::pools) {
        ROSE_FREE(p);
    }
    SgSequenceStatement::next_node = nullptr;
    SgSequenceStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSequenceStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSequenceStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSequenceStatement)};
    for (size_t blockIndex = SgSequenceStatement::pools.size(); (blockIndex * SgSequenceStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSequenceStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSequenceStatement) = %" PRIuPTR " SgSequenceStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSequenceStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSequenceStatement),SgSequenceStatement::pool_size);
        }
#endif

        SgSequenceStatement* pointer{static_cast<SgSequenceStatement*>(ROSE_MALLOC(SgSequenceStatement::pool_size * sizeof(SgSequenceStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSequenceStatement::pool_size * sizeof(SgSequenceStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSequenceStatement::pool_size * sizeof(SgSequenceStatement));
#endif
        SgSequenceStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSequenceStatement::pool_size * sizeof(SgSequenceStatement), V_SgSequenceStatement));

        if (SgSequenceStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSequenceStatement* blkptr{reinterpret_cast<SgSequenceStatement*>(SgSequenceStatement::pools[blockIndex-1])};
                blkptr[SgSequenceStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSequenceStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSequenceStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSequenceStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSequenceStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSequenceStatement::pool_size - 1};
    SgSequenceStatement* testPointer{reinterpret_cast<SgSequenceStatement*>(SgSequenceStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSequenceStatement::pool_size * (SgSequenceStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSequenceStatementStroageClass
 * from its counterpart of SgSequenceStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSequenceStatement::initializeStorageClassArray(SgSequenceStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSequenceStatement::pools.begin(); block != SgSequenceStatement::pools.end(); ++block) {
        SgSequenceStatement* pointer{reinterpret_cast<SgSequenceStatement*>(*block)};
        for (unsigned i = 0; i < SgSequenceStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 97423 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWithStatement that takes its corresponding StorageClass as parameter
 */
SgWithStatement::SgWithStatement(const SgWithStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 97433 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWithStatement*
SgWithStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWithStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWithStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWithStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWithStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWithStatement)};
        unsigned long positionInPool{localIndex % SgWithStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWithStatement::pool_size};
        returnPointer = &(((SgWithStatement*)(SgWithStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWithStatement*
SgWithStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWithStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWithStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWithStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWithStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWithStatement)};
        unsigned long positionInPool{localIndex % SgWithStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWithStatement::pool_size};
        returnPointer = &(((SgWithStatement*)(SgWithStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWithStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWithStatement::pools.begin(); block != SgWithStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWithStatement*>(*block)};
        for (unsigned i = 0; i < SgWithStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWithStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWithStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWithStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgWithStatement::pools.begin(); block != SgWithStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWithStatement*>(*block)};
        for (unsigned i = 0; i < SgWithStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWithStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWithStatement::clearMemoryPool() {
    SgWithStatement* tempPointer{nullptr};
    if (SgWithStatement::pools.empty() == false) {
        auto block{SgWithStatement::pools.begin()};
        SgWithStatement::next_node = reinterpret_cast<SgWithStatement*>(*block);
        for (; block != SgWithStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWithStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWithStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWithStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWithStatement::pool_size-1]);
        }
    }
}

void
SgWithStatement::deleteMemoryPool() {
    for (auto p: SgWithStatement::pools) {
        ROSE_FREE(p);
    }
    SgWithStatement::next_node = nullptr;
    SgWithStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWithStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWithStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWithStatement)};
    for (size_t blockIndex = SgWithStatement::pools.size(); (blockIndex * SgWithStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWithStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWithStatement) = %" PRIuPTR " SgWithStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWithStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWithStatement),SgWithStatement::pool_size);
        }
#endif

        SgWithStatement* pointer{static_cast<SgWithStatement*>(ROSE_MALLOC(SgWithStatement::pool_size * sizeof(SgWithStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWithStatement::pool_size * sizeof(SgWithStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWithStatement::pool_size * sizeof(SgWithStatement));
#endif
        SgWithStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWithStatement::pool_size * sizeof(SgWithStatement), V_SgWithStatement));

        if (SgWithStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWithStatement* blkptr{reinterpret_cast<SgWithStatement*>(SgWithStatement::pools[blockIndex-1])};
                blkptr[SgWithStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWithStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWithStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWithStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWithStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWithStatement::pool_size - 1};
    SgWithStatement* testPointer{reinterpret_cast<SgWithStatement*>(SgWithStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWithStatement::pool_size * (SgWithStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWithStatementStroageClass
 * from its counterpart of SgWithStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWithStatement::initializeStorageClassArray(SgWithStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWithStatement::pools.begin(); block != SgWithStatement::pools.end(); ++block) {
        SgWithStatement* pointer{reinterpret_cast<SgWithStatement*>(*block)};
        for (unsigned i = 0; i < SgWithStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 97652 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPythonPrintStmt that takes its corresponding StorageClass as parameter
 */
SgPythonPrintStmt::SgPythonPrintStmt(const SgPythonPrintStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 97662 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_destination = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_destination));
     p_values = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_values));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPythonPrintStmt*
SgPythonPrintStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPythonPrintStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPythonPrintStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPythonPrintStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPythonPrintStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonPrintStmt)};
        unsigned long positionInPool{localIndex % SgPythonPrintStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPythonPrintStmt::pool_size};
        returnPointer = &(((SgPythonPrintStmt*)(SgPythonPrintStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPythonPrintStmt*
SgPythonPrintStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPythonPrintStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPythonPrintStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPythonPrintStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPythonPrintStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPythonPrintStmt)};
        unsigned long positionInPool{localIndex % SgPythonPrintStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPythonPrintStmt::pool_size};
        returnPointer = &(((SgPythonPrintStmt*)(SgPythonPrintStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPythonPrintStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPythonPrintStmt::pools.begin(); block != SgPythonPrintStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPythonPrintStmt*>(*block)};
        for (unsigned i = 0; i < SgPythonPrintStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPythonPrintStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPythonPrintStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPythonPrintStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgPythonPrintStmt::pools.begin(); block != SgPythonPrintStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPythonPrintStmt*>(*block)};
        for (unsigned i = 0; i < SgPythonPrintStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPythonPrintStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPythonPrintStmt::clearMemoryPool() {
    SgPythonPrintStmt* tempPointer{nullptr};
    if (SgPythonPrintStmt::pools.empty() == false) {
        auto block{SgPythonPrintStmt::pools.begin()};
        SgPythonPrintStmt::next_node = reinterpret_cast<SgPythonPrintStmt*>(*block);
        for (; block != SgPythonPrintStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPythonPrintStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPythonPrintStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPythonPrintStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPythonPrintStmt::pool_size-1]);
        }
    }
}

void
SgPythonPrintStmt::deleteMemoryPool() {
    for (auto p: SgPythonPrintStmt::pools) {
        ROSE_FREE(p);
    }
    SgPythonPrintStmt::next_node = nullptr;
    SgPythonPrintStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPythonPrintStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonPrintStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPythonPrintStmt)};
    for (size_t blockIndex = SgPythonPrintStmt::pools.size(); (blockIndex * SgPythonPrintStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonPrintStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPythonPrintStmt) = %" PRIuPTR " SgPythonPrintStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonPrintStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPythonPrintStmt),SgPythonPrintStmt::pool_size);
        }
#endif

        SgPythonPrintStmt* pointer{static_cast<SgPythonPrintStmt*>(ROSE_MALLOC(SgPythonPrintStmt::pool_size * sizeof(SgPythonPrintStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPythonPrintStmt::pool_size * sizeof(SgPythonPrintStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPythonPrintStmt::pool_size * sizeof(SgPythonPrintStmt));
#endif
        SgPythonPrintStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPythonPrintStmt::pool_size * sizeof(SgPythonPrintStmt), V_SgPythonPrintStmt));

        if (SgPythonPrintStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPythonPrintStmt* blkptr{reinterpret_cast<SgPythonPrintStmt*>(SgPythonPrintStmt::pools[blockIndex-1])};
                blkptr[SgPythonPrintStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPythonPrintStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPythonPrintStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPythonPrintStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPythonPrintStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPythonPrintStmt::pool_size - 1};
    SgPythonPrintStmt* testPointer{reinterpret_cast<SgPythonPrintStmt*>(SgPythonPrintStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPythonPrintStmt::pool_size * (SgPythonPrintStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPythonPrintStmtStroageClass
 * from its counterpart of SgPythonPrintStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPythonPrintStmt::initializeStorageClassArray(SgPythonPrintStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPythonPrintStmt::pools.begin(); block != SgPythonPrintStmt::pools.end(); ++block) {
        SgPythonPrintStmt* pointer{reinterpret_cast<SgPythonPrintStmt*>(*block)};
        for (unsigned i = 0; i < SgPythonPrintStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 97881 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPassStatement that takes its corresponding StorageClass as parameter
 */
SgPassStatement::SgPassStatement(const SgPassStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 97891 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPassStatement*
SgPassStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPassStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPassStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPassStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPassStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPassStatement)};
        unsigned long positionInPool{localIndex % SgPassStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPassStatement::pool_size};
        returnPointer = &(((SgPassStatement*)(SgPassStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPassStatement*
SgPassStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPassStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPassStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPassStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPassStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPassStatement)};
        unsigned long positionInPool{localIndex % SgPassStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPassStatement::pool_size};
        returnPointer = &(((SgPassStatement*)(SgPassStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPassStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPassStatement::pools.begin(); block != SgPassStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPassStatement*>(*block)};
        for (unsigned i = 0; i < SgPassStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPassStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPassStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPassStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgPassStatement::pools.begin(); block != SgPassStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPassStatement*>(*block)};
        for (unsigned i = 0; i < SgPassStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPassStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPassStatement::clearMemoryPool() {
    SgPassStatement* tempPointer{nullptr};
    if (SgPassStatement::pools.empty() == false) {
        auto block{SgPassStatement::pools.begin()};
        SgPassStatement::next_node = reinterpret_cast<SgPassStatement*>(*block);
        for (; block != SgPassStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPassStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPassStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPassStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPassStatement::pool_size-1]);
        }
    }
}

void
SgPassStatement::deleteMemoryPool() {
    for (auto p: SgPassStatement::pools) {
        ROSE_FREE(p);
    }
    SgPassStatement::next_node = nullptr;
    SgPassStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPassStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPassStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPassStatement)};
    for (size_t blockIndex = SgPassStatement::pools.size(); (blockIndex * SgPassStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPassStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPassStatement) = %" PRIuPTR " SgPassStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPassStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPassStatement),SgPassStatement::pool_size);
        }
#endif

        SgPassStatement* pointer{static_cast<SgPassStatement*>(ROSE_MALLOC(SgPassStatement::pool_size * sizeof(SgPassStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPassStatement::pool_size * sizeof(SgPassStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPassStatement::pool_size * sizeof(SgPassStatement));
#endif
        SgPassStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPassStatement::pool_size * sizeof(SgPassStatement), V_SgPassStatement));

        if (SgPassStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPassStatement* blkptr{reinterpret_cast<SgPassStatement*>(SgPassStatement::pools[blockIndex-1])};
                blkptr[SgPassStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPassStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPassStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPassStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPassStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPassStatement::pool_size - 1};
    SgPassStatement* testPointer{reinterpret_cast<SgPassStatement*>(SgPassStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPassStatement::pool_size * (SgPassStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPassStatementStroageClass
 * from its counterpart of SgPassStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPassStatement::initializeStorageClassArray(SgPassStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPassStatement::pools.begin(); block != SgPassStatement::pools.end(); ++block) {
        SgPassStatement* pointer{reinterpret_cast<SgPassStatement*>(*block)};
        for (unsigned i = 0; i < SgPassStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 98108 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAssertStmt that takes its corresponding StorageClass as parameter
 */
SgAssertStmt::SgAssertStmt(const SgAssertStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 98118 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_test = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_test));
     p_exception_argument = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_exception_argument));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAssertStmt*
SgAssertStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAssertStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssertStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssertStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssertStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAssertStmt)};
        unsigned long positionInPool{localIndex % SgAssertStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssertStmt::pool_size};
        returnPointer = &(((SgAssertStmt*)(SgAssertStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAssertStmt*
SgAssertStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAssertStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssertStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssertStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssertStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAssertStmt)};
        unsigned long positionInPool{localIndex % SgAssertStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssertStmt::pool_size};
        returnPointer = &(((SgAssertStmt*)(SgAssertStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAssertStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAssertStmt::pools.begin(); block != SgAssertStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssertStmt*>(*block)};
        for (unsigned i = 0; i < SgAssertStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAssertStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAssertStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAssertStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAssertStmt::pools.begin(); block != SgAssertStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssertStmt*>(*block)};
        for (unsigned i = 0; i < SgAssertStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAssertStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAssertStmt::clearMemoryPool() {
    SgAssertStmt* tempPointer{nullptr};
    if (SgAssertStmt::pools.empty() == false) {
        auto block{SgAssertStmt::pools.begin()};
        SgAssertStmt::next_node = reinterpret_cast<SgAssertStmt*>(*block);
        for (; block != SgAssertStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAssertStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAssertStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAssertStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAssertStmt::pool_size-1]);
        }
    }
}

void
SgAssertStmt::deleteMemoryPool() {
    for (auto p: SgAssertStmt::pools) {
        ROSE_FREE(p);
    }
    SgAssertStmt::next_node = nullptr;
    SgAssertStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAssertStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAssertStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssertStmt)};
    for (size_t blockIndex = SgAssertStmt::pools.size(); (blockIndex * SgAssertStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAssertStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssertStmt) = %" PRIuPTR " SgAssertStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAssertStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssertStmt),SgAssertStmt::pool_size);
        }
#endif

        SgAssertStmt* pointer{static_cast<SgAssertStmt*>(ROSE_MALLOC(SgAssertStmt::pool_size * sizeof(SgAssertStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAssertStmt::pool_size * sizeof(SgAssertStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAssertStmt::pool_size * sizeof(SgAssertStmt));
#endif
        SgAssertStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAssertStmt::pool_size * sizeof(SgAssertStmt), V_SgAssertStmt));

        if (SgAssertStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAssertStmt* blkptr{reinterpret_cast<SgAssertStmt*>(SgAssertStmt::pools[blockIndex-1])};
                blkptr[SgAssertStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAssertStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAssertStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAssertStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAssertStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAssertStmt::pool_size - 1};
    SgAssertStmt* testPointer{reinterpret_cast<SgAssertStmt*>(SgAssertStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAssertStmt::pool_size * (SgAssertStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAssertStmtStroageClass
 * from its counterpart of SgAssertStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAssertStmt::initializeStorageClassArray(SgAssertStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAssertStmt::pools.begin(); block != SgAssertStmt::pools.end(); ++block) {
        SgAssertStmt* pointer{reinterpret_cast<SgAssertStmt*>(*block)};
        for (unsigned i = 0; i < SgAssertStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 98337 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExecStatement that takes its corresponding StorageClass as parameter
 */
SgExecStatement::SgExecStatement(const SgExecStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 98347 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_executable = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_executable));
     p_globals = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_globals));
     p_locals = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_locals));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExecStatement*
SgExecStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExecStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExecStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExecStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExecStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExecStatement)};
        unsigned long positionInPool{localIndex % SgExecStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExecStatement::pool_size};
        returnPointer = &(((SgExecStatement*)(SgExecStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExecStatement*
SgExecStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExecStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExecStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExecStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExecStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExecStatement)};
        unsigned long positionInPool{localIndex % SgExecStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExecStatement::pool_size};
        returnPointer = &(((SgExecStatement*)(SgExecStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExecStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExecStatement::pools.begin(); block != SgExecStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExecStatement*>(*block)};
        for (unsigned i = 0; i < SgExecStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExecStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExecStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExecStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgExecStatement::pools.begin(); block != SgExecStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExecStatement*>(*block)};
        for (unsigned i = 0; i < SgExecStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExecStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExecStatement::clearMemoryPool() {
    SgExecStatement* tempPointer{nullptr};
    if (SgExecStatement::pools.empty() == false) {
        auto block{SgExecStatement::pools.begin()};
        SgExecStatement::next_node = reinterpret_cast<SgExecStatement*>(*block);
        for (; block != SgExecStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExecStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExecStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExecStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExecStatement::pool_size-1]);
        }
    }
}

void
SgExecStatement::deleteMemoryPool() {
    for (auto p: SgExecStatement::pools) {
        ROSE_FREE(p);
    }
    SgExecStatement::next_node = nullptr;
    SgExecStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExecStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExecStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExecStatement)};
    for (size_t blockIndex = SgExecStatement::pools.size(); (blockIndex * SgExecStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExecStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExecStatement) = %" PRIuPTR " SgExecStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExecStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExecStatement),SgExecStatement::pool_size);
        }
#endif

        SgExecStatement* pointer{static_cast<SgExecStatement*>(ROSE_MALLOC(SgExecStatement::pool_size * sizeof(SgExecStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExecStatement::pool_size * sizeof(SgExecStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExecStatement::pool_size * sizeof(SgExecStatement));
#endif
        SgExecStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExecStatement::pool_size * sizeof(SgExecStatement), V_SgExecStatement));

        if (SgExecStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExecStatement* blkptr{reinterpret_cast<SgExecStatement*>(SgExecStatement::pools[blockIndex-1])};
                blkptr[SgExecStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExecStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExecStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExecStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExecStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExecStatement::pool_size - 1};
    SgExecStatement* testPointer{reinterpret_cast<SgExecStatement*>(SgExecStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExecStatement::pool_size * (SgExecStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExecStatementStroageClass
 * from its counterpart of SgExecStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExecStatement::initializeStorageClassArray(SgExecStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExecStatement::pools.begin(); block != SgExecStatement::pools.end(); ++block) {
        SgExecStatement* pointer{reinterpret_cast<SgExecStatement*>(*block)};
        for (unsigned i = 0; i < SgExecStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 98567 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPythonGlobalStmt that takes its corresponding StorageClass as parameter
 */
SgPythonGlobalStmt::SgPythonGlobalStmt(const SgPythonGlobalStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 98577 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_names = storageSource.storageOf_names.rebuildDataStoredInEasyStorageClass();
    SgInitializedNamePtrList::iterator i_names = p_names.begin();
    for ( ; i_names != p_names.end(); ++i_names) {
        (*i_names) = (SgInitializedNamePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_names)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPythonGlobalStmt*
SgPythonGlobalStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPythonGlobalStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPythonGlobalStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPythonGlobalStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPythonGlobalStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonGlobalStmt)};
        unsigned long positionInPool{localIndex % SgPythonGlobalStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPythonGlobalStmt::pool_size};
        returnPointer = &(((SgPythonGlobalStmt*)(SgPythonGlobalStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPythonGlobalStmt*
SgPythonGlobalStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPythonGlobalStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPythonGlobalStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPythonGlobalStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPythonGlobalStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPythonGlobalStmt)};
        unsigned long positionInPool{localIndex % SgPythonGlobalStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPythonGlobalStmt::pool_size};
        returnPointer = &(((SgPythonGlobalStmt*)(SgPythonGlobalStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPythonGlobalStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPythonGlobalStmt::pools.begin(); block != SgPythonGlobalStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPythonGlobalStmt*>(*block)};
        for (unsigned i = 0; i < SgPythonGlobalStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPythonGlobalStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPythonGlobalStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPythonGlobalStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgPythonGlobalStmt::pools.begin(); block != SgPythonGlobalStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPythonGlobalStmt*>(*block)};
        for (unsigned i = 0; i < SgPythonGlobalStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPythonGlobalStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPythonGlobalStmt::clearMemoryPool() {
    SgPythonGlobalStmt* tempPointer{nullptr};
    if (SgPythonGlobalStmt::pools.empty() == false) {
        auto block{SgPythonGlobalStmt::pools.begin()};
        SgPythonGlobalStmt::next_node = reinterpret_cast<SgPythonGlobalStmt*>(*block);
        for (; block != SgPythonGlobalStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPythonGlobalStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPythonGlobalStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPythonGlobalStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPythonGlobalStmt::pool_size-1]);
        }
    }
}

void
SgPythonGlobalStmt::deleteMemoryPool() {
    for (auto p: SgPythonGlobalStmt::pools) {
        ROSE_FREE(p);
    }
    SgPythonGlobalStmt::next_node = nullptr;
    SgPythonGlobalStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPythonGlobalStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonGlobalStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPythonGlobalStmt)};
    for (size_t blockIndex = SgPythonGlobalStmt::pools.size(); (blockIndex * SgPythonGlobalStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonGlobalStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPythonGlobalStmt) = %" PRIuPTR " SgPythonGlobalStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPythonGlobalStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPythonGlobalStmt),SgPythonGlobalStmt::pool_size);
        }
#endif

        SgPythonGlobalStmt* pointer{static_cast<SgPythonGlobalStmt*>(ROSE_MALLOC(SgPythonGlobalStmt::pool_size * sizeof(SgPythonGlobalStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPythonGlobalStmt::pool_size * sizeof(SgPythonGlobalStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPythonGlobalStmt::pool_size * sizeof(SgPythonGlobalStmt));
#endif
        SgPythonGlobalStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPythonGlobalStmt::pool_size * sizeof(SgPythonGlobalStmt), V_SgPythonGlobalStmt));

        if (SgPythonGlobalStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPythonGlobalStmt* blkptr{reinterpret_cast<SgPythonGlobalStmt*>(SgPythonGlobalStmt::pools[blockIndex-1])};
                blkptr[SgPythonGlobalStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPythonGlobalStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPythonGlobalStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPythonGlobalStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPythonGlobalStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPythonGlobalStmt::pool_size - 1};
    SgPythonGlobalStmt* testPointer{reinterpret_cast<SgPythonGlobalStmt*>(SgPythonGlobalStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPythonGlobalStmt::pool_size * (SgPythonGlobalStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPythonGlobalStmtStroageClass
 * from its counterpart of SgPythonGlobalStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPythonGlobalStmt::initializeStorageClassArray(SgPythonGlobalStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPythonGlobalStmt::pools.begin(); block != SgPythonGlobalStmt::pools.end(); ++block) {
        SgPythonGlobalStmt* pointer{reinterpret_cast<SgPythonGlobalStmt*>(*block)};
        for (unsigned i = 0; i < SgPythonGlobalStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 98799 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaThrowStatement that takes its corresponding StorageClass as parameter
 */
SgJavaThrowStatement::SgJavaThrowStatement(const SgJavaThrowStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 98809 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_throwOp = (SgThrowOp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_throwOp));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaThrowStatement*
SgJavaThrowStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaThrowStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaThrowStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaThrowStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaThrowStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaThrowStatement)};
        unsigned long positionInPool{localIndex % SgJavaThrowStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaThrowStatement::pool_size};
        returnPointer = &(((SgJavaThrowStatement*)(SgJavaThrowStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaThrowStatement*
SgJavaThrowStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaThrowStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaThrowStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaThrowStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaThrowStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaThrowStatement)};
        unsigned long positionInPool{localIndex % SgJavaThrowStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaThrowStatement::pool_size};
        returnPointer = &(((SgJavaThrowStatement*)(SgJavaThrowStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaThrowStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaThrowStatement::pools.begin(); block != SgJavaThrowStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaThrowStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaThrowStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaThrowStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaThrowStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaThrowStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaThrowStatement::pools.begin(); block != SgJavaThrowStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaThrowStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaThrowStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaThrowStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaThrowStatement::clearMemoryPool() {
    SgJavaThrowStatement* tempPointer{nullptr};
    if (SgJavaThrowStatement::pools.empty() == false) {
        auto block{SgJavaThrowStatement::pools.begin()};
        SgJavaThrowStatement::next_node = reinterpret_cast<SgJavaThrowStatement*>(*block);
        for (; block != SgJavaThrowStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaThrowStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaThrowStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaThrowStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaThrowStatement::pool_size-1]);
        }
    }
}

void
SgJavaThrowStatement::deleteMemoryPool() {
    for (auto p: SgJavaThrowStatement::pools) {
        ROSE_FREE(p);
    }
    SgJavaThrowStatement::next_node = nullptr;
    SgJavaThrowStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaThrowStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaThrowStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaThrowStatement)};
    for (size_t blockIndex = SgJavaThrowStatement::pools.size(); (blockIndex * SgJavaThrowStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaThrowStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaThrowStatement) = %" PRIuPTR " SgJavaThrowStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaThrowStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaThrowStatement),SgJavaThrowStatement::pool_size);
        }
#endif

        SgJavaThrowStatement* pointer{static_cast<SgJavaThrowStatement*>(ROSE_MALLOC(SgJavaThrowStatement::pool_size * sizeof(SgJavaThrowStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaThrowStatement::pool_size * sizeof(SgJavaThrowStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaThrowStatement::pool_size * sizeof(SgJavaThrowStatement));
#endif
        SgJavaThrowStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaThrowStatement::pool_size * sizeof(SgJavaThrowStatement), V_SgJavaThrowStatement));

        if (SgJavaThrowStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaThrowStatement* blkptr{reinterpret_cast<SgJavaThrowStatement*>(SgJavaThrowStatement::pools[blockIndex-1])};
                blkptr[SgJavaThrowStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaThrowStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaThrowStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaThrowStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaThrowStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaThrowStatement::pool_size - 1};
    SgJavaThrowStatement* testPointer{reinterpret_cast<SgJavaThrowStatement*>(SgJavaThrowStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaThrowStatement::pool_size * (SgJavaThrowStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaThrowStatementStroageClass
 * from its counterpart of SgJavaThrowStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaThrowStatement::initializeStorageClassArray(SgJavaThrowStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaThrowStatement::pools.begin(); block != SgJavaThrowStatement::pools.end(); ++block) {
        SgJavaThrowStatement* pointer{reinterpret_cast<SgJavaThrowStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaThrowStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 99027 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaSynchronizedStatement that takes its corresponding StorageClass as parameter
 */
SgJavaSynchronizedStatement::SgJavaSynchronizedStatement(const SgJavaSynchronizedStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 99037 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaSynchronizedStatement*
SgJavaSynchronizedStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaSynchronizedStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaSynchronizedStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaSynchronizedStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaSynchronizedStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSynchronizedStatement)};
        unsigned long positionInPool{localIndex % SgJavaSynchronizedStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaSynchronizedStatement::pool_size};
        returnPointer = &(((SgJavaSynchronizedStatement*)(SgJavaSynchronizedStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaSynchronizedStatement*
SgJavaSynchronizedStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaSynchronizedStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaSynchronizedStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaSynchronizedStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaSynchronizedStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaSynchronizedStatement)};
        unsigned long positionInPool{localIndex % SgJavaSynchronizedStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaSynchronizedStatement::pool_size};
        returnPointer = &(((SgJavaSynchronizedStatement*)(SgJavaSynchronizedStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaSynchronizedStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaSynchronizedStatement::pools.begin(); block != SgJavaSynchronizedStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaSynchronizedStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaSynchronizedStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaSynchronizedStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaSynchronizedStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaSynchronizedStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaSynchronizedStatement::pools.begin(); block != SgJavaSynchronizedStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaSynchronizedStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaSynchronizedStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaSynchronizedStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaSynchronizedStatement::clearMemoryPool() {
    SgJavaSynchronizedStatement* tempPointer{nullptr};
    if (SgJavaSynchronizedStatement::pools.empty() == false) {
        auto block{SgJavaSynchronizedStatement::pools.begin()};
        SgJavaSynchronizedStatement::next_node = reinterpret_cast<SgJavaSynchronizedStatement*>(*block);
        for (; block != SgJavaSynchronizedStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaSynchronizedStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaSynchronizedStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaSynchronizedStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaSynchronizedStatement::pool_size-1]);
        }
    }
}

void
SgJavaSynchronizedStatement::deleteMemoryPool() {
    for (auto p: SgJavaSynchronizedStatement::pools) {
        ROSE_FREE(p);
    }
    SgJavaSynchronizedStatement::next_node = nullptr;
    SgJavaSynchronizedStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaSynchronizedStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSynchronizedStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaSynchronizedStatement)};
    for (size_t blockIndex = SgJavaSynchronizedStatement::pools.size(); (blockIndex * SgJavaSynchronizedStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSynchronizedStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaSynchronizedStatement) = %" PRIuPTR " SgJavaSynchronizedStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSynchronizedStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaSynchronizedStatement),SgJavaSynchronizedStatement::pool_size);
        }
#endif

        SgJavaSynchronizedStatement* pointer{static_cast<SgJavaSynchronizedStatement*>(ROSE_MALLOC(SgJavaSynchronizedStatement::pool_size * sizeof(SgJavaSynchronizedStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaSynchronizedStatement::pool_size * sizeof(SgJavaSynchronizedStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaSynchronizedStatement::pool_size * sizeof(SgJavaSynchronizedStatement));
#endif
        SgJavaSynchronizedStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaSynchronizedStatement::pool_size * sizeof(SgJavaSynchronizedStatement), V_SgJavaSynchronizedStatement));

        if (SgJavaSynchronizedStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaSynchronizedStatement* blkptr{reinterpret_cast<SgJavaSynchronizedStatement*>(SgJavaSynchronizedStatement::pools[blockIndex-1])};
                blkptr[SgJavaSynchronizedStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaSynchronizedStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaSynchronizedStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaSynchronizedStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaSynchronizedStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaSynchronizedStatement::pool_size - 1};
    SgJavaSynchronizedStatement* testPointer{reinterpret_cast<SgJavaSynchronizedStatement*>(SgJavaSynchronizedStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaSynchronizedStatement::pool_size * (SgJavaSynchronizedStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaSynchronizedStatementStroageClass
 * from its counterpart of SgJavaSynchronizedStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaSynchronizedStatement::initializeStorageClassArray(SgJavaSynchronizedStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaSynchronizedStatement::pools.begin(); block != SgJavaSynchronizedStatement::pools.end(); ++block) {
        SgJavaSynchronizedStatement* pointer{reinterpret_cast<SgJavaSynchronizedStatement*>(*block)};
        for (unsigned i = 0; i < SgJavaSynchronizedStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 99256 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsyncStmt that takes its corresponding StorageClass as parameter
 */
SgAsyncStmt::SgAsyncStmt(const SgAsyncStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 99266 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsyncStmt*
SgAsyncStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsyncStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsyncStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsyncStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsyncStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsyncStmt)};
        unsigned long positionInPool{localIndex % SgAsyncStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsyncStmt::pool_size};
        returnPointer = &(((SgAsyncStmt*)(SgAsyncStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsyncStmt*
SgAsyncStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsyncStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsyncStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsyncStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsyncStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsyncStmt)};
        unsigned long positionInPool{localIndex % SgAsyncStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsyncStmt::pool_size};
        returnPointer = &(((SgAsyncStmt*)(SgAsyncStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsyncStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsyncStmt::pools.begin(); block != SgAsyncStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsyncStmt*>(*block)};
        for (unsigned i = 0; i < SgAsyncStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsyncStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsyncStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsyncStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAsyncStmt::pools.begin(); block != SgAsyncStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsyncStmt*>(*block)};
        for (unsigned i = 0; i < SgAsyncStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsyncStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsyncStmt::clearMemoryPool() {
    SgAsyncStmt* tempPointer{nullptr};
    if (SgAsyncStmt::pools.empty() == false) {
        auto block{SgAsyncStmt::pools.begin()};
        SgAsyncStmt::next_node = reinterpret_cast<SgAsyncStmt*>(*block);
        for (; block != SgAsyncStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsyncStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsyncStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsyncStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsyncStmt::pool_size-1]);
        }
    }
}

void
SgAsyncStmt::deleteMemoryPool() {
    for (auto p: SgAsyncStmt::pools) {
        ROSE_FREE(p);
    }
    SgAsyncStmt::next_node = nullptr;
    SgAsyncStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsyncStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsyncStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsyncStmt)};
    for (size_t blockIndex = SgAsyncStmt::pools.size(); (blockIndex * SgAsyncStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsyncStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsyncStmt) = %" PRIuPTR " SgAsyncStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsyncStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsyncStmt),SgAsyncStmt::pool_size);
        }
#endif

        SgAsyncStmt* pointer{static_cast<SgAsyncStmt*>(ROSE_MALLOC(SgAsyncStmt::pool_size * sizeof(SgAsyncStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsyncStmt::pool_size * sizeof(SgAsyncStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsyncStmt::pool_size * sizeof(SgAsyncStmt));
#endif
        SgAsyncStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsyncStmt::pool_size * sizeof(SgAsyncStmt), V_SgAsyncStmt));

        if (SgAsyncStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsyncStmt* blkptr{reinterpret_cast<SgAsyncStmt*>(SgAsyncStmt::pools[blockIndex-1])};
                blkptr[SgAsyncStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsyncStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsyncStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsyncStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsyncStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsyncStmt::pool_size - 1};
    SgAsyncStmt* testPointer{reinterpret_cast<SgAsyncStmt*>(SgAsyncStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsyncStmt::pool_size * (SgAsyncStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsyncStmtStroageClass
 * from its counterpart of SgAsyncStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsyncStmt::initializeStorageClassArray(SgAsyncStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsyncStmt::pools.begin(); block != SgAsyncStmt::pools.end(); ++block) {
        SgAsyncStmt* pointer{reinterpret_cast<SgAsyncStmt*>(*block)};
        for (unsigned i = 0; i < SgAsyncStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 99484 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFinishStmt that takes its corresponding StorageClass as parameter
 */
SgFinishStmt::SgFinishStmt(const SgFinishStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 99494 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFinishStmt*
SgFinishStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFinishStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFinishStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFinishStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFinishStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishStmt)};
        unsigned long positionInPool{localIndex % SgFinishStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFinishStmt::pool_size};
        returnPointer = &(((SgFinishStmt*)(SgFinishStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFinishStmt*
SgFinishStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFinishStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFinishStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFinishStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFinishStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFinishStmt)};
        unsigned long positionInPool{localIndex % SgFinishStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFinishStmt::pool_size};
        returnPointer = &(((SgFinishStmt*)(SgFinishStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFinishStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFinishStmt::pools.begin(); block != SgFinishStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFinishStmt*>(*block)};
        for (unsigned i = 0; i < SgFinishStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFinishStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFinishStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFinishStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgFinishStmt::pools.begin(); block != SgFinishStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFinishStmt*>(*block)};
        for (unsigned i = 0; i < SgFinishStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFinishStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFinishStmt::clearMemoryPool() {
    SgFinishStmt* tempPointer{nullptr};
    if (SgFinishStmt::pools.empty() == false) {
        auto block{SgFinishStmt::pools.begin()};
        SgFinishStmt::next_node = reinterpret_cast<SgFinishStmt*>(*block);
        for (; block != SgFinishStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFinishStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFinishStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFinishStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFinishStmt::pool_size-1]);
        }
    }
}

void
SgFinishStmt::deleteMemoryPool() {
    for (auto p: SgFinishStmt::pools) {
        ROSE_FREE(p);
    }
    SgFinishStmt::next_node = nullptr;
    SgFinishStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFinishStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFinishStmt)};
    for (size_t blockIndex = SgFinishStmt::pools.size(); (blockIndex * SgFinishStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFinishStmt) = %" PRIuPTR " SgFinishStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFinishStmt),SgFinishStmt::pool_size);
        }
#endif

        SgFinishStmt* pointer{static_cast<SgFinishStmt*>(ROSE_MALLOC(SgFinishStmt::pool_size * sizeof(SgFinishStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFinishStmt::pool_size * sizeof(SgFinishStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFinishStmt::pool_size * sizeof(SgFinishStmt));
#endif
        SgFinishStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFinishStmt::pool_size * sizeof(SgFinishStmt), V_SgFinishStmt));

        if (SgFinishStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFinishStmt* blkptr{reinterpret_cast<SgFinishStmt*>(SgFinishStmt::pools[blockIndex-1])};
                blkptr[SgFinishStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFinishStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFinishStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFinishStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFinishStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFinishStmt::pool_size - 1};
    SgFinishStmt* testPointer{reinterpret_cast<SgFinishStmt*>(SgFinishStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFinishStmt::pool_size * (SgFinishStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFinishStmtStroageClass
 * from its counterpart of SgFinishStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFinishStmt::initializeStorageClassArray(SgFinishStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFinishStmt::pools.begin(); block != SgFinishStmt::pools.end(); ++block) {
        SgFinishStmt* pointer{reinterpret_cast<SgFinishStmt*>(*block)};
        for (unsigned i = 0; i < SgFinishStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 99712 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAtStmt that takes its corresponding StorageClass as parameter
 */
SgAtStmt::SgAtStmt(const SgAtStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 99722 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAtStmt*
SgAtStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAtStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAtStmt)};
        unsigned long positionInPool{localIndex % SgAtStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtStmt::pool_size};
        returnPointer = &(((SgAtStmt*)(SgAtStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAtStmt*
SgAtStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAtStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAtStmt)};
        unsigned long positionInPool{localIndex % SgAtStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtStmt::pool_size};
        returnPointer = &(((SgAtStmt*)(SgAtStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAtStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAtStmt::pools.begin(); block != SgAtStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtStmt*>(*block)};
        for (unsigned i = 0; i < SgAtStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAtStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAtStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAtStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAtStmt::pools.begin(); block != SgAtStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtStmt*>(*block)};
        for (unsigned i = 0; i < SgAtStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAtStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAtStmt::clearMemoryPool() {
    SgAtStmt* tempPointer{nullptr};
    if (SgAtStmt::pools.empty() == false) {
        auto block{SgAtStmt::pools.begin()};
        SgAtStmt::next_node = reinterpret_cast<SgAtStmt*>(*block);
        for (; block != SgAtStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAtStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAtStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAtStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAtStmt::pool_size-1]);
        }
    }
}

void
SgAtStmt::deleteMemoryPool() {
    for (auto p: SgAtStmt::pools) {
        ROSE_FREE(p);
    }
    SgAtStmt::next_node = nullptr;
    SgAtStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAtStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAtStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtStmt)};
    for (size_t blockIndex = SgAtStmt::pools.size(); (blockIndex * SgAtStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAtStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtStmt) = %" PRIuPTR " SgAtStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAtStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtStmt),SgAtStmt::pool_size);
        }
#endif

        SgAtStmt* pointer{static_cast<SgAtStmt*>(ROSE_MALLOC(SgAtStmt::pool_size * sizeof(SgAtStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAtStmt::pool_size * sizeof(SgAtStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAtStmt::pool_size * sizeof(SgAtStmt));
#endif
        SgAtStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAtStmt::pool_size * sizeof(SgAtStmt), V_SgAtStmt));

        if (SgAtStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAtStmt* blkptr{reinterpret_cast<SgAtStmt*>(SgAtStmt::pools[blockIndex-1])};
                blkptr[SgAtStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAtStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAtStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAtStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAtStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAtStmt::pool_size - 1};
    SgAtStmt* testPointer{reinterpret_cast<SgAtStmt*>(SgAtStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAtStmt::pool_size * (SgAtStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAtStmtStroageClass
 * from its counterpart of SgAtStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAtStmt::initializeStorageClassArray(SgAtStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAtStmt::pools.begin(); block != SgAtStmt::pools.end(); ++block) {
        SgAtStmt* pointer{reinterpret_cast<SgAtStmt*>(*block)};
        for (unsigned i = 0; i < SgAtStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 99941 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAtomicStmt that takes its corresponding StorageClass as parameter
 */
SgAtomicStmt::SgAtomicStmt(const SgAtomicStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 99951 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAtomicStmt*
SgAtomicStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAtomicStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtomicStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtomicStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtomicStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAtomicStmt)};
        unsigned long positionInPool{localIndex % SgAtomicStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtomicStmt::pool_size};
        returnPointer = &(((SgAtomicStmt*)(SgAtomicStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAtomicStmt*
SgAtomicStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAtomicStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtomicStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtomicStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtomicStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAtomicStmt)};
        unsigned long positionInPool{localIndex % SgAtomicStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtomicStmt::pool_size};
        returnPointer = &(((SgAtomicStmt*)(SgAtomicStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAtomicStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAtomicStmt::pools.begin(); block != SgAtomicStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtomicStmt*>(*block)};
        for (unsigned i = 0; i < SgAtomicStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAtomicStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAtomicStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAtomicStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAtomicStmt::pools.begin(); block != SgAtomicStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtomicStmt*>(*block)};
        for (unsigned i = 0; i < SgAtomicStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAtomicStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAtomicStmt::clearMemoryPool() {
    SgAtomicStmt* tempPointer{nullptr};
    if (SgAtomicStmt::pools.empty() == false) {
        auto block{SgAtomicStmt::pools.begin()};
        SgAtomicStmt::next_node = reinterpret_cast<SgAtomicStmt*>(*block);
        for (; block != SgAtomicStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAtomicStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAtomicStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAtomicStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAtomicStmt::pool_size-1]);
        }
    }
}

void
SgAtomicStmt::deleteMemoryPool() {
    for (auto p: SgAtomicStmt::pools) {
        ROSE_FREE(p);
    }
    SgAtomicStmt::next_node = nullptr;
    SgAtomicStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAtomicStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAtomicStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtomicStmt)};
    for (size_t blockIndex = SgAtomicStmt::pools.size(); (blockIndex * SgAtomicStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAtomicStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtomicStmt) = %" PRIuPTR " SgAtomicStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAtomicStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtomicStmt),SgAtomicStmt::pool_size);
        }
#endif

        SgAtomicStmt* pointer{static_cast<SgAtomicStmt*>(ROSE_MALLOC(SgAtomicStmt::pool_size * sizeof(SgAtomicStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAtomicStmt::pool_size * sizeof(SgAtomicStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAtomicStmt::pool_size * sizeof(SgAtomicStmt));
#endif
        SgAtomicStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAtomicStmt::pool_size * sizeof(SgAtomicStmt), V_SgAtomicStmt));

        if (SgAtomicStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAtomicStmt* blkptr{reinterpret_cast<SgAtomicStmt*>(SgAtomicStmt::pools[blockIndex-1])};
                blkptr[SgAtomicStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAtomicStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAtomicStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAtomicStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAtomicStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAtomicStmt::pool_size - 1};
    SgAtomicStmt* testPointer{reinterpret_cast<SgAtomicStmt*>(SgAtomicStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAtomicStmt::pool_size * (SgAtomicStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAtomicStmtStroageClass
 * from its counterpart of SgAtomicStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAtomicStmt::initializeStorageClassArray(SgAtomicStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAtomicStmt::pools.begin(); block != SgAtomicStmt::pools.end(); ++block) {
        SgAtomicStmt* pointer{reinterpret_cast<SgAtomicStmt*>(*block)};
        for (unsigned i = 0; i < SgAtomicStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 100169 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWhenStmt that takes its corresponding StorageClass as parameter
 */
SgWhenStmt::SgWhenStmt(const SgWhenStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 100179 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWhenStmt*
SgWhenStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWhenStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhenStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhenStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWhenStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWhenStmt)};
        unsigned long positionInPool{localIndex % SgWhenStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWhenStmt::pool_size};
        returnPointer = &(((SgWhenStmt*)(SgWhenStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWhenStmt*
SgWhenStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWhenStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhenStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhenStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWhenStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWhenStmt)};
        unsigned long positionInPool{localIndex % SgWhenStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWhenStmt::pool_size};
        returnPointer = &(((SgWhenStmt*)(SgWhenStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWhenStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWhenStmt::pools.begin(); block != SgWhenStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWhenStmt*>(*block)};
        for (unsigned i = 0; i < SgWhenStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWhenStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWhenStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWhenStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgWhenStmt::pools.begin(); block != SgWhenStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWhenStmt*>(*block)};
        for (unsigned i = 0; i < SgWhenStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWhenStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWhenStmt::clearMemoryPool() {
    SgWhenStmt* tempPointer{nullptr};
    if (SgWhenStmt::pools.empty() == false) {
        auto block{SgWhenStmt::pools.begin()};
        SgWhenStmt::next_node = reinterpret_cast<SgWhenStmt*>(*block);
        for (; block != SgWhenStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWhenStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWhenStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWhenStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWhenStmt::pool_size-1]);
        }
    }
}

void
SgWhenStmt::deleteMemoryPool() {
    for (auto p: SgWhenStmt::pools) {
        ROSE_FREE(p);
    }
    SgWhenStmt::next_node = nullptr;
    SgWhenStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWhenStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWhenStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhenStmt)};
    for (size_t blockIndex = SgWhenStmt::pools.size(); (blockIndex * SgWhenStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWhenStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhenStmt) = %" PRIuPTR " SgWhenStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWhenStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWhenStmt),SgWhenStmt::pool_size);
        }
#endif

        SgWhenStmt* pointer{static_cast<SgWhenStmt*>(ROSE_MALLOC(SgWhenStmt::pool_size * sizeof(SgWhenStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWhenStmt::pool_size * sizeof(SgWhenStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWhenStmt::pool_size * sizeof(SgWhenStmt));
#endif
        SgWhenStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWhenStmt::pool_size * sizeof(SgWhenStmt), V_SgWhenStmt));

        if (SgWhenStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWhenStmt* blkptr{reinterpret_cast<SgWhenStmt*>(SgWhenStmt::pools[blockIndex-1])};
                blkptr[SgWhenStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWhenStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWhenStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWhenStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWhenStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWhenStmt::pool_size - 1};
    SgWhenStmt* testPointer{reinterpret_cast<SgWhenStmt*>(SgWhenStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWhenStmt::pool_size * (SgWhenStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWhenStmtStroageClass
 * from its counterpart of SgWhenStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWhenStmt::initializeStorageClassArray(SgWhenStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWhenStmt::pools.begin(); block != SgWhenStmt::pools.end(); ++block) {
        SgWhenStmt* pointer{reinterpret_cast<SgWhenStmt*>(*block)};
        for (unsigned i = 0; i < SgWhenStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 100398 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgImageControlStatement that takes its corresponding StorageClass as parameter
 */
SgImageControlStatement::SgImageControlStatement(const SgImageControlStatementStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 100408 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_image_control_statement = storageSource.storageOf_image_control_statement;
     p_stat = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_stat));
     p_err_msg = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_err_msg));
     p_acquired_lock = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_acquired_lock));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgImageControlStatement*
SgImageControlStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgImageControlStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImageControlStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImageControlStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImageControlStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgImageControlStatement)};
        unsigned long positionInPool{localIndex % SgImageControlStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImageControlStatement::pool_size};
        returnPointer = &(((SgImageControlStatement*)(SgImageControlStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgImageControlStatement*
SgImageControlStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgImageControlStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImageControlStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImageControlStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImageControlStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgImageControlStatement)};
        unsigned long positionInPool{localIndex % SgImageControlStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImageControlStatement::pool_size};
        returnPointer = &(((SgImageControlStatement*)(SgImageControlStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgImageControlStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgImageControlStatement::pools.begin(); block != SgImageControlStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImageControlStatement*>(*block)};
        for (unsigned i = 0; i < SgImageControlStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgImageControlStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgImageControlStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgImageControlStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgImageControlStatement::pools.begin(); block != SgImageControlStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImageControlStatement*>(*block)};
        for (unsigned i = 0; i < SgImageControlStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgImageControlStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgImageControlStatement::clearMemoryPool() {
    SgImageControlStatement* tempPointer{nullptr};
    if (SgImageControlStatement::pools.empty() == false) {
        auto block{SgImageControlStatement::pools.begin()};
        SgImageControlStatement::next_node = reinterpret_cast<SgImageControlStatement*>(*block);
        for (; block != SgImageControlStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgImageControlStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgImageControlStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgImageControlStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgImageControlStatement::pool_size-1]);
        }
    }
}

void
SgImageControlStatement::deleteMemoryPool() {
    for (auto p: SgImageControlStatement::pools) {
        ROSE_FREE(p);
    }
    SgImageControlStatement::next_node = nullptr;
    SgImageControlStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgImageControlStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgImageControlStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgImageControlStatement)};
    for (size_t blockIndex = SgImageControlStatement::pools.size(); (blockIndex * SgImageControlStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgImageControlStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgImageControlStatement) = %" PRIuPTR " SgImageControlStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgImageControlStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgImageControlStatement),SgImageControlStatement::pool_size);
        }
#endif

        SgImageControlStatement* pointer{static_cast<SgImageControlStatement*>(ROSE_MALLOC(SgImageControlStatement::pool_size * sizeof(SgImageControlStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgImageControlStatement::pool_size * sizeof(SgImageControlStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgImageControlStatement::pool_size * sizeof(SgImageControlStatement));
#endif
        SgImageControlStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgImageControlStatement::pool_size * sizeof(SgImageControlStatement), V_SgImageControlStatement));

        if (SgImageControlStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgImageControlStatement* blkptr{reinterpret_cast<SgImageControlStatement*>(SgImageControlStatement::pools[blockIndex-1])};
                blkptr[SgImageControlStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgImageControlStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgImageControlStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgImageControlStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgImageControlStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgImageControlStatement::pool_size - 1};
    SgImageControlStatement* testPointer{reinterpret_cast<SgImageControlStatement*>(SgImageControlStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgImageControlStatement::pool_size * (SgImageControlStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgImageControlStatementStroageClass
 * from its counterpart of SgImageControlStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgImageControlStatement::initializeStorageClassArray(SgImageControlStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgImageControlStatement::pools.begin(); block != SgImageControlStatement::pools.end(); ++block) {
        SgImageControlStatement* pointer{reinterpret_cast<SgImageControlStatement*>(*block)};
        for (unsigned i = 0; i < SgImageControlStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 100629 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSyncAllStatement that takes its corresponding StorageClass as parameter
 */
SgSyncAllStatement::SgSyncAllStatement(const SgSyncAllStatementStorageClass& storageSource)  : SgImageControlStatement (storageSource) {

/* #line 100639 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSyncAllStatement*
SgSyncAllStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSyncAllStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncAllStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncAllStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncAllStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncAllStatement)};
        unsigned long positionInPool{localIndex % SgSyncAllStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncAllStatement::pool_size};
        returnPointer = &(((SgSyncAllStatement*)(SgSyncAllStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSyncAllStatement*
SgSyncAllStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSyncAllStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncAllStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncAllStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncAllStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSyncAllStatement)};
        unsigned long positionInPool{localIndex % SgSyncAllStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncAllStatement::pool_size};
        returnPointer = &(((SgSyncAllStatement*)(SgSyncAllStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSyncAllStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSyncAllStatement::pools.begin(); block != SgSyncAllStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncAllStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncAllStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSyncAllStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSyncAllStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSyncAllStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgSyncAllStatement::pools.begin(); block != SgSyncAllStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncAllStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncAllStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSyncAllStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSyncAllStatement::clearMemoryPool() {
    SgSyncAllStatement* tempPointer{nullptr};
    if (SgSyncAllStatement::pools.empty() == false) {
        auto block{SgSyncAllStatement::pools.begin()};
        SgSyncAllStatement::next_node = reinterpret_cast<SgSyncAllStatement*>(*block);
        for (; block != SgSyncAllStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSyncAllStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSyncAllStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSyncAllStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSyncAllStatement::pool_size-1]);
        }
    }
}

void
SgSyncAllStatement::deleteMemoryPool() {
    for (auto p: SgSyncAllStatement::pools) {
        ROSE_FREE(p);
    }
    SgSyncAllStatement::next_node = nullptr;
    SgSyncAllStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSyncAllStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncAllStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncAllStatement)};
    for (size_t blockIndex = SgSyncAllStatement::pools.size(); (blockIndex * SgSyncAllStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncAllStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncAllStatement) = %" PRIuPTR " SgSyncAllStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncAllStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncAllStatement),SgSyncAllStatement::pool_size);
        }
#endif

        SgSyncAllStatement* pointer{static_cast<SgSyncAllStatement*>(ROSE_MALLOC(SgSyncAllStatement::pool_size * sizeof(SgSyncAllStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSyncAllStatement::pool_size * sizeof(SgSyncAllStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSyncAllStatement::pool_size * sizeof(SgSyncAllStatement));
#endif
        SgSyncAllStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSyncAllStatement::pool_size * sizeof(SgSyncAllStatement), V_SgSyncAllStatement));

        if (SgSyncAllStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSyncAllStatement* blkptr{reinterpret_cast<SgSyncAllStatement*>(SgSyncAllStatement::pools[blockIndex-1])};
                blkptr[SgSyncAllStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSyncAllStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSyncAllStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSyncAllStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSyncAllStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSyncAllStatement::pool_size - 1};
    SgSyncAllStatement* testPointer{reinterpret_cast<SgSyncAllStatement*>(SgSyncAllStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSyncAllStatement::pool_size * (SgSyncAllStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSyncAllStatementStroageClass
 * from its counterpart of SgSyncAllStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSyncAllStatement::initializeStorageClassArray(SgSyncAllStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSyncAllStatement::pools.begin(); block != SgSyncAllStatement::pools.end(); ++block) {
        SgSyncAllStatement* pointer{reinterpret_cast<SgSyncAllStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncAllStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 100856 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSyncImagesStatement that takes its corresponding StorageClass as parameter
 */
SgSyncImagesStatement::SgSyncImagesStatement(const SgSyncImagesStatementStorageClass& storageSource)  : SgImageControlStatement (storageSource) {

/* #line 100866 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_image_set = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_image_set));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSyncImagesStatement*
SgSyncImagesStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSyncImagesStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncImagesStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncImagesStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncImagesStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncImagesStatement)};
        unsigned long positionInPool{localIndex % SgSyncImagesStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncImagesStatement::pool_size};
        returnPointer = &(((SgSyncImagesStatement*)(SgSyncImagesStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSyncImagesStatement*
SgSyncImagesStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSyncImagesStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncImagesStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncImagesStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncImagesStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSyncImagesStatement)};
        unsigned long positionInPool{localIndex % SgSyncImagesStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncImagesStatement::pool_size};
        returnPointer = &(((SgSyncImagesStatement*)(SgSyncImagesStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSyncImagesStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSyncImagesStatement::pools.begin(); block != SgSyncImagesStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncImagesStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncImagesStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSyncImagesStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSyncImagesStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSyncImagesStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgSyncImagesStatement::pools.begin(); block != SgSyncImagesStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncImagesStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncImagesStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSyncImagesStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSyncImagesStatement::clearMemoryPool() {
    SgSyncImagesStatement* tempPointer{nullptr};
    if (SgSyncImagesStatement::pools.empty() == false) {
        auto block{SgSyncImagesStatement::pools.begin()};
        SgSyncImagesStatement::next_node = reinterpret_cast<SgSyncImagesStatement*>(*block);
        for (; block != SgSyncImagesStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSyncImagesStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSyncImagesStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSyncImagesStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSyncImagesStatement::pool_size-1]);
        }
    }
}

void
SgSyncImagesStatement::deleteMemoryPool() {
    for (auto p: SgSyncImagesStatement::pools) {
        ROSE_FREE(p);
    }
    SgSyncImagesStatement::next_node = nullptr;
    SgSyncImagesStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSyncImagesStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncImagesStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncImagesStatement)};
    for (size_t blockIndex = SgSyncImagesStatement::pools.size(); (blockIndex * SgSyncImagesStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncImagesStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncImagesStatement) = %" PRIuPTR " SgSyncImagesStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncImagesStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncImagesStatement),SgSyncImagesStatement::pool_size);
        }
#endif

        SgSyncImagesStatement* pointer{static_cast<SgSyncImagesStatement*>(ROSE_MALLOC(SgSyncImagesStatement::pool_size * sizeof(SgSyncImagesStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSyncImagesStatement::pool_size * sizeof(SgSyncImagesStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSyncImagesStatement::pool_size * sizeof(SgSyncImagesStatement));
#endif
        SgSyncImagesStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSyncImagesStatement::pool_size * sizeof(SgSyncImagesStatement), V_SgSyncImagesStatement));

        if (SgSyncImagesStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSyncImagesStatement* blkptr{reinterpret_cast<SgSyncImagesStatement*>(SgSyncImagesStatement::pools[blockIndex-1])};
                blkptr[SgSyncImagesStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSyncImagesStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSyncImagesStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSyncImagesStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSyncImagesStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSyncImagesStatement::pool_size - 1};
    SgSyncImagesStatement* testPointer{reinterpret_cast<SgSyncImagesStatement*>(SgSyncImagesStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSyncImagesStatement::pool_size * (SgSyncImagesStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSyncImagesStatementStroageClass
 * from its counterpart of SgSyncImagesStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSyncImagesStatement::initializeStorageClassArray(SgSyncImagesStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSyncImagesStatement::pools.begin(); block != SgSyncImagesStatement::pools.end(); ++block) {
        SgSyncImagesStatement* pointer{reinterpret_cast<SgSyncImagesStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncImagesStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 101084 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSyncMemoryStatement that takes its corresponding StorageClass as parameter
 */
SgSyncMemoryStatement::SgSyncMemoryStatement(const SgSyncMemoryStatementStorageClass& storageSource)  : SgImageControlStatement (storageSource) {

/* #line 101094 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSyncMemoryStatement*
SgSyncMemoryStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSyncMemoryStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncMemoryStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncMemoryStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncMemoryStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncMemoryStatement)};
        unsigned long positionInPool{localIndex % SgSyncMemoryStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncMemoryStatement::pool_size};
        returnPointer = &(((SgSyncMemoryStatement*)(SgSyncMemoryStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSyncMemoryStatement*
SgSyncMemoryStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSyncMemoryStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncMemoryStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncMemoryStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncMemoryStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSyncMemoryStatement)};
        unsigned long positionInPool{localIndex % SgSyncMemoryStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncMemoryStatement::pool_size};
        returnPointer = &(((SgSyncMemoryStatement*)(SgSyncMemoryStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSyncMemoryStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSyncMemoryStatement::pools.begin(); block != SgSyncMemoryStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncMemoryStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncMemoryStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSyncMemoryStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSyncMemoryStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSyncMemoryStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgSyncMemoryStatement::pools.begin(); block != SgSyncMemoryStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncMemoryStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncMemoryStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSyncMemoryStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSyncMemoryStatement::clearMemoryPool() {
    SgSyncMemoryStatement* tempPointer{nullptr};
    if (SgSyncMemoryStatement::pools.empty() == false) {
        auto block{SgSyncMemoryStatement::pools.begin()};
        SgSyncMemoryStatement::next_node = reinterpret_cast<SgSyncMemoryStatement*>(*block);
        for (; block != SgSyncMemoryStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSyncMemoryStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSyncMemoryStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSyncMemoryStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSyncMemoryStatement::pool_size-1]);
        }
    }
}

void
SgSyncMemoryStatement::deleteMemoryPool() {
    for (auto p: SgSyncMemoryStatement::pools) {
        ROSE_FREE(p);
    }
    SgSyncMemoryStatement::next_node = nullptr;
    SgSyncMemoryStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSyncMemoryStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncMemoryStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncMemoryStatement)};
    for (size_t blockIndex = SgSyncMemoryStatement::pools.size(); (blockIndex * SgSyncMemoryStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncMemoryStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncMemoryStatement) = %" PRIuPTR " SgSyncMemoryStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncMemoryStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncMemoryStatement),SgSyncMemoryStatement::pool_size);
        }
#endif

        SgSyncMemoryStatement* pointer{static_cast<SgSyncMemoryStatement*>(ROSE_MALLOC(SgSyncMemoryStatement::pool_size * sizeof(SgSyncMemoryStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSyncMemoryStatement::pool_size * sizeof(SgSyncMemoryStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSyncMemoryStatement::pool_size * sizeof(SgSyncMemoryStatement));
#endif
        SgSyncMemoryStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSyncMemoryStatement::pool_size * sizeof(SgSyncMemoryStatement), V_SgSyncMemoryStatement));

        if (SgSyncMemoryStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSyncMemoryStatement* blkptr{reinterpret_cast<SgSyncMemoryStatement*>(SgSyncMemoryStatement::pools[blockIndex-1])};
                blkptr[SgSyncMemoryStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSyncMemoryStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSyncMemoryStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSyncMemoryStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSyncMemoryStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSyncMemoryStatement::pool_size - 1};
    SgSyncMemoryStatement* testPointer{reinterpret_cast<SgSyncMemoryStatement*>(SgSyncMemoryStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSyncMemoryStatement::pool_size * (SgSyncMemoryStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSyncMemoryStatementStroageClass
 * from its counterpart of SgSyncMemoryStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSyncMemoryStatement::initializeStorageClassArray(SgSyncMemoryStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSyncMemoryStatement::pools.begin(); block != SgSyncMemoryStatement::pools.end(); ++block) {
        SgSyncMemoryStatement* pointer{reinterpret_cast<SgSyncMemoryStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncMemoryStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 101311 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSyncTeamStatement that takes its corresponding StorageClass as parameter
 */
SgSyncTeamStatement::SgSyncTeamStatement(const SgSyncTeamStatementStorageClass& storageSource)  : SgImageControlStatement (storageSource) {

/* #line 101321 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_team_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_team_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSyncTeamStatement*
SgSyncTeamStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSyncTeamStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncTeamStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncTeamStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSyncTeamStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncTeamStatement)};
        unsigned long positionInPool{localIndex % SgSyncTeamStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncTeamStatement::pool_size};
        returnPointer = &(((SgSyncTeamStatement*)(SgSyncTeamStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSyncTeamStatement*
SgSyncTeamStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSyncTeamStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncTeamStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncTeamStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSyncTeamStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSyncTeamStatement)};
        unsigned long positionInPool{localIndex % SgSyncTeamStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSyncTeamStatement::pool_size};
        returnPointer = &(((SgSyncTeamStatement*)(SgSyncTeamStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSyncTeamStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSyncTeamStatement::pools.begin(); block != SgSyncTeamStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncTeamStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncTeamStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSyncTeamStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSyncTeamStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSyncTeamStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgSyncTeamStatement::pools.begin(); block != SgSyncTeamStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSyncTeamStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncTeamStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSyncTeamStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSyncTeamStatement::clearMemoryPool() {
    SgSyncTeamStatement* tempPointer{nullptr};
    if (SgSyncTeamStatement::pools.empty() == false) {
        auto block{SgSyncTeamStatement::pools.begin()};
        SgSyncTeamStatement::next_node = reinterpret_cast<SgSyncTeamStatement*>(*block);
        for (; block != SgSyncTeamStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSyncTeamStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSyncTeamStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSyncTeamStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSyncTeamStatement::pool_size-1]);
        }
    }
}

void
SgSyncTeamStatement::deleteMemoryPool() {
    for (auto p: SgSyncTeamStatement::pools) {
        ROSE_FREE(p);
    }
    SgSyncTeamStatement::next_node = nullptr;
    SgSyncTeamStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSyncTeamStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncTeamStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncTeamStatement)};
    for (size_t blockIndex = SgSyncTeamStatement::pools.size(); (blockIndex * SgSyncTeamStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncTeamStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncTeamStatement) = %" PRIuPTR " SgSyncTeamStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSyncTeamStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSyncTeamStatement),SgSyncTeamStatement::pool_size);
        }
#endif

        SgSyncTeamStatement* pointer{static_cast<SgSyncTeamStatement*>(ROSE_MALLOC(SgSyncTeamStatement::pool_size * sizeof(SgSyncTeamStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSyncTeamStatement::pool_size * sizeof(SgSyncTeamStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSyncTeamStatement::pool_size * sizeof(SgSyncTeamStatement));
#endif
        SgSyncTeamStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSyncTeamStatement::pool_size * sizeof(SgSyncTeamStatement), V_SgSyncTeamStatement));

        if (SgSyncTeamStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSyncTeamStatement* blkptr{reinterpret_cast<SgSyncTeamStatement*>(SgSyncTeamStatement::pools[blockIndex-1])};
                blkptr[SgSyncTeamStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSyncTeamStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSyncTeamStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSyncTeamStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSyncTeamStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSyncTeamStatement::pool_size - 1};
    SgSyncTeamStatement* testPointer{reinterpret_cast<SgSyncTeamStatement*>(SgSyncTeamStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSyncTeamStatement::pool_size * (SgSyncTeamStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSyncTeamStatementStroageClass
 * from its counterpart of SgSyncTeamStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSyncTeamStatement::initializeStorageClassArray(SgSyncTeamStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSyncTeamStatement::pools.begin(); block != SgSyncTeamStatement::pools.end(); ++block) {
        SgSyncTeamStatement* pointer{reinterpret_cast<SgSyncTeamStatement*>(*block)};
        for (unsigned i = 0; i < SgSyncTeamStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 101539 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLockStatement that takes its corresponding StorageClass as parameter
 */
SgLockStatement::SgLockStatement(const SgLockStatementStorageClass& storageSource)  : SgImageControlStatement (storageSource) {

/* #line 101549 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lock_variable = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lock_variable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLockStatement*
SgLockStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLockStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLockStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLockStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLockStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLockStatement)};
        unsigned long positionInPool{localIndex % SgLockStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLockStatement::pool_size};
        returnPointer = &(((SgLockStatement*)(SgLockStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLockStatement*
SgLockStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLockStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLockStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLockStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLockStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLockStatement)};
        unsigned long positionInPool{localIndex % SgLockStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLockStatement::pool_size};
        returnPointer = &(((SgLockStatement*)(SgLockStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLockStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLockStatement::pools.begin(); block != SgLockStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLockStatement*>(*block)};
        for (unsigned i = 0; i < SgLockStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLockStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLockStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLockStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgLockStatement::pools.begin(); block != SgLockStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLockStatement*>(*block)};
        for (unsigned i = 0; i < SgLockStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLockStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLockStatement::clearMemoryPool() {
    SgLockStatement* tempPointer{nullptr};
    if (SgLockStatement::pools.empty() == false) {
        auto block{SgLockStatement::pools.begin()};
        SgLockStatement::next_node = reinterpret_cast<SgLockStatement*>(*block);
        for (; block != SgLockStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLockStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLockStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLockStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLockStatement::pool_size-1]);
        }
    }
}

void
SgLockStatement::deleteMemoryPool() {
    for (auto p: SgLockStatement::pools) {
        ROSE_FREE(p);
    }
    SgLockStatement::next_node = nullptr;
    SgLockStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLockStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLockStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLockStatement)};
    for (size_t blockIndex = SgLockStatement::pools.size(); (blockIndex * SgLockStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLockStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLockStatement) = %" PRIuPTR " SgLockStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLockStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLockStatement),SgLockStatement::pool_size);
        }
#endif

        SgLockStatement* pointer{static_cast<SgLockStatement*>(ROSE_MALLOC(SgLockStatement::pool_size * sizeof(SgLockStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLockStatement::pool_size * sizeof(SgLockStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLockStatement::pool_size * sizeof(SgLockStatement));
#endif
        SgLockStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLockStatement::pool_size * sizeof(SgLockStatement), V_SgLockStatement));

        if (SgLockStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLockStatement* blkptr{reinterpret_cast<SgLockStatement*>(SgLockStatement::pools[blockIndex-1])};
                blkptr[SgLockStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLockStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLockStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLockStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLockStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLockStatement::pool_size - 1};
    SgLockStatement* testPointer{reinterpret_cast<SgLockStatement*>(SgLockStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLockStatement::pool_size * (SgLockStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLockStatementStroageClass
 * from its counterpart of SgLockStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLockStatement::initializeStorageClassArray(SgLockStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLockStatement::pools.begin(); block != SgLockStatement::pools.end(); ++block) {
        SgLockStatement* pointer{reinterpret_cast<SgLockStatement*>(*block)};
        for (unsigned i = 0; i < SgLockStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 101767 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnlockStatement that takes its corresponding StorageClass as parameter
 */
SgUnlockStatement::SgUnlockStatement(const SgUnlockStatementStorageClass& storageSource)  : SgImageControlStatement (storageSource) {

/* #line 101777 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lock_variable = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lock_variable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnlockStatement*
SgUnlockStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnlockStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnlockStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnlockStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnlockStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnlockStatement)};
        unsigned long positionInPool{localIndex % SgUnlockStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnlockStatement::pool_size};
        returnPointer = &(((SgUnlockStatement*)(SgUnlockStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnlockStatement*
SgUnlockStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnlockStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnlockStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnlockStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnlockStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnlockStatement)};
        unsigned long positionInPool{localIndex % SgUnlockStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnlockStatement::pool_size};
        returnPointer = &(((SgUnlockStatement*)(SgUnlockStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnlockStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnlockStatement::pools.begin(); block != SgUnlockStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnlockStatement*>(*block)};
        for (unsigned i = 0; i < SgUnlockStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnlockStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnlockStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnlockStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgUnlockStatement::pools.begin(); block != SgUnlockStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnlockStatement*>(*block)};
        for (unsigned i = 0; i < SgUnlockStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnlockStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnlockStatement::clearMemoryPool() {
    SgUnlockStatement* tempPointer{nullptr};
    if (SgUnlockStatement::pools.empty() == false) {
        auto block{SgUnlockStatement::pools.begin()};
        SgUnlockStatement::next_node = reinterpret_cast<SgUnlockStatement*>(*block);
        for (; block != SgUnlockStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnlockStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnlockStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnlockStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnlockStatement::pool_size-1]);
        }
    }
}

void
SgUnlockStatement::deleteMemoryPool() {
    for (auto p: SgUnlockStatement::pools) {
        ROSE_FREE(p);
    }
    SgUnlockStatement::next_node = nullptr;
    SgUnlockStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnlockStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnlockStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnlockStatement)};
    for (size_t blockIndex = SgUnlockStatement::pools.size(); (blockIndex * SgUnlockStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnlockStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnlockStatement) = %" PRIuPTR " SgUnlockStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnlockStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnlockStatement),SgUnlockStatement::pool_size);
        }
#endif

        SgUnlockStatement* pointer{static_cast<SgUnlockStatement*>(ROSE_MALLOC(SgUnlockStatement::pool_size * sizeof(SgUnlockStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnlockStatement::pool_size * sizeof(SgUnlockStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnlockStatement::pool_size * sizeof(SgUnlockStatement));
#endif
        SgUnlockStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnlockStatement::pool_size * sizeof(SgUnlockStatement), V_SgUnlockStatement));

        if (SgUnlockStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnlockStatement* blkptr{reinterpret_cast<SgUnlockStatement*>(SgUnlockStatement::pools[blockIndex-1])};
                blkptr[SgUnlockStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnlockStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnlockStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnlockStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnlockStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnlockStatement::pool_size - 1};
    SgUnlockStatement* testPointer{reinterpret_cast<SgUnlockStatement*>(SgUnlockStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnlockStatement::pool_size * (SgUnlockStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnlockStatementStroageClass
 * from its counterpart of SgUnlockStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnlockStatement::initializeStorageClassArray(SgUnlockStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnlockStatement::pools.begin(); block != SgUnlockStatement::pools.end(); ++block) {
        SgUnlockStatement* pointer{reinterpret_cast<SgUnlockStatement*>(*block)};
        for (unsigned i = 0; i < SgUnlockStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 101995 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaExitStmt that takes its corresponding StorageClass as parameter
 */
SgAdaExitStmt::SgAdaExitStmt(const SgAdaExitStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 102005 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_loop = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_loop));
     p_condition = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_condition));
    p_explicitLoopName = storageSource.storageOf_explicitLoopName;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaExitStmt*
SgAdaExitStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaExitStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaExitStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaExitStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaExitStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaExitStmt)};
        unsigned long positionInPool{localIndex % SgAdaExitStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaExitStmt::pool_size};
        returnPointer = &(((SgAdaExitStmt*)(SgAdaExitStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaExitStmt*
SgAdaExitStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaExitStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaExitStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaExitStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaExitStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaExitStmt)};
        unsigned long positionInPool{localIndex % SgAdaExitStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaExitStmt::pool_size};
        returnPointer = &(((SgAdaExitStmt*)(SgAdaExitStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaExitStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaExitStmt::pools.begin(); block != SgAdaExitStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaExitStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaExitStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaExitStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaExitStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaExitStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaExitStmt::pools.begin(); block != SgAdaExitStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaExitStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaExitStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaExitStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaExitStmt::clearMemoryPool() {
    SgAdaExitStmt* tempPointer{nullptr};
    if (SgAdaExitStmt::pools.empty() == false) {
        auto block{SgAdaExitStmt::pools.begin()};
        SgAdaExitStmt::next_node = reinterpret_cast<SgAdaExitStmt*>(*block);
        for (; block != SgAdaExitStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaExitStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaExitStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaExitStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaExitStmt::pool_size-1]);
        }
    }
}

void
SgAdaExitStmt::deleteMemoryPool() {
    for (auto p: SgAdaExitStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaExitStmt::next_node = nullptr;
    SgAdaExitStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaExitStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaExitStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaExitStmt)};
    for (size_t blockIndex = SgAdaExitStmt::pools.size(); (blockIndex * SgAdaExitStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaExitStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaExitStmt) = %" PRIuPTR " SgAdaExitStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaExitStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaExitStmt),SgAdaExitStmt::pool_size);
        }
#endif

        SgAdaExitStmt* pointer{static_cast<SgAdaExitStmt*>(ROSE_MALLOC(SgAdaExitStmt::pool_size * sizeof(SgAdaExitStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaExitStmt::pool_size * sizeof(SgAdaExitStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaExitStmt::pool_size * sizeof(SgAdaExitStmt));
#endif
        SgAdaExitStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaExitStmt::pool_size * sizeof(SgAdaExitStmt), V_SgAdaExitStmt));

        if (SgAdaExitStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaExitStmt* blkptr{reinterpret_cast<SgAdaExitStmt*>(SgAdaExitStmt::pools[blockIndex-1])};
                blkptr[SgAdaExitStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaExitStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaExitStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaExitStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaExitStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaExitStmt::pool_size - 1};
    SgAdaExitStmt* testPointer{reinterpret_cast<SgAdaExitStmt*>(SgAdaExitStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaExitStmt::pool_size * (SgAdaExitStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaExitStmtStroageClass
 * from its counterpart of SgAdaExitStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaExitStmt::initializeStorageClassArray(SgAdaExitStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaExitStmt::pools.begin(); block != SgAdaExitStmt::pools.end(); ++block) {
        SgAdaExitStmt* pointer{reinterpret_cast<SgAdaExitStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaExitStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 102225 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaDelayStmt that takes its corresponding StorageClass as parameter
 */
SgAdaDelayStmt::SgAdaDelayStmt(const SgAdaDelayStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 102235 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_time = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_time));
    p_isRelative = storageSource.storageOf_isRelative;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaDelayStmt*
SgAdaDelayStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaDelayStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDelayStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDelayStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaDelayStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDelayStmt)};
        unsigned long positionInPool{localIndex % SgAdaDelayStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDelayStmt::pool_size};
        returnPointer = &(((SgAdaDelayStmt*)(SgAdaDelayStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaDelayStmt*
SgAdaDelayStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaDelayStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDelayStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDelayStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaDelayStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaDelayStmt)};
        unsigned long positionInPool{localIndex % SgAdaDelayStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaDelayStmt::pool_size};
        returnPointer = &(((SgAdaDelayStmt*)(SgAdaDelayStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaDelayStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaDelayStmt::pools.begin(); block != SgAdaDelayStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDelayStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaDelayStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaDelayStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaDelayStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaDelayStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaDelayStmt::pools.begin(); block != SgAdaDelayStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaDelayStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaDelayStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaDelayStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaDelayStmt::clearMemoryPool() {
    SgAdaDelayStmt* tempPointer{nullptr};
    if (SgAdaDelayStmt::pools.empty() == false) {
        auto block{SgAdaDelayStmt::pools.begin()};
        SgAdaDelayStmt::next_node = reinterpret_cast<SgAdaDelayStmt*>(*block);
        for (; block != SgAdaDelayStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaDelayStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaDelayStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaDelayStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaDelayStmt::pool_size-1]);
        }
    }
}

void
SgAdaDelayStmt::deleteMemoryPool() {
    for (auto p: SgAdaDelayStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaDelayStmt::next_node = nullptr;
    SgAdaDelayStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaDelayStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDelayStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDelayStmt)};
    for (size_t blockIndex = SgAdaDelayStmt::pools.size(); (blockIndex * SgAdaDelayStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDelayStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDelayStmt) = %" PRIuPTR " SgAdaDelayStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaDelayStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaDelayStmt),SgAdaDelayStmt::pool_size);
        }
#endif

        SgAdaDelayStmt* pointer{static_cast<SgAdaDelayStmt*>(ROSE_MALLOC(SgAdaDelayStmt::pool_size * sizeof(SgAdaDelayStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaDelayStmt::pool_size * sizeof(SgAdaDelayStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaDelayStmt::pool_size * sizeof(SgAdaDelayStmt));
#endif
        SgAdaDelayStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaDelayStmt::pool_size * sizeof(SgAdaDelayStmt), V_SgAdaDelayStmt));

        if (SgAdaDelayStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaDelayStmt* blkptr{reinterpret_cast<SgAdaDelayStmt*>(SgAdaDelayStmt::pools[blockIndex-1])};
                blkptr[SgAdaDelayStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaDelayStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaDelayStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaDelayStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaDelayStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaDelayStmt::pool_size - 1};
    SgAdaDelayStmt* testPointer{reinterpret_cast<SgAdaDelayStmt*>(SgAdaDelayStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaDelayStmt::pool_size * (SgAdaDelayStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaDelayStmtStroageClass
 * from its counterpart of SgAdaDelayStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaDelayStmt::initializeStorageClassArray(SgAdaDelayStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaDelayStmt::pools.begin(); block != SgAdaDelayStmt::pools.end(); ++block) {
        SgAdaDelayStmt* pointer{reinterpret_cast<SgAdaDelayStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaDelayStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 102454 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaLoopStmt that takes its corresponding StorageClass as parameter
 */
SgAdaLoopStmt::SgAdaLoopStmt(const SgAdaLoopStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 102464 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_string_label = storageSource.storageOf_string_label.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaLoopStmt*
SgAdaLoopStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaLoopStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaLoopStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaLoopStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaLoopStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaLoopStmt)};
        unsigned long positionInPool{localIndex % SgAdaLoopStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaLoopStmt::pool_size};
        returnPointer = &(((SgAdaLoopStmt*)(SgAdaLoopStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaLoopStmt*
SgAdaLoopStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaLoopStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaLoopStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaLoopStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaLoopStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaLoopStmt)};
        unsigned long positionInPool{localIndex % SgAdaLoopStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaLoopStmt::pool_size};
        returnPointer = &(((SgAdaLoopStmt*)(SgAdaLoopStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaLoopStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaLoopStmt::pools.begin(); block != SgAdaLoopStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaLoopStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaLoopStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaLoopStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaLoopStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaLoopStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaLoopStmt::pools.begin(); block != SgAdaLoopStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaLoopStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaLoopStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaLoopStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaLoopStmt::clearMemoryPool() {
    SgAdaLoopStmt* tempPointer{nullptr};
    if (SgAdaLoopStmt::pools.empty() == false) {
        auto block{SgAdaLoopStmt::pools.begin()};
        SgAdaLoopStmt::next_node = reinterpret_cast<SgAdaLoopStmt*>(*block);
        for (; block != SgAdaLoopStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaLoopStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaLoopStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaLoopStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaLoopStmt::pool_size-1]);
        }
    }
}

void
SgAdaLoopStmt::deleteMemoryPool() {
    for (auto p: SgAdaLoopStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaLoopStmt::next_node = nullptr;
    SgAdaLoopStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaLoopStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaLoopStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaLoopStmt)};
    for (size_t blockIndex = SgAdaLoopStmt::pools.size(); (blockIndex * SgAdaLoopStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaLoopStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaLoopStmt) = %" PRIuPTR " SgAdaLoopStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaLoopStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaLoopStmt),SgAdaLoopStmt::pool_size);
        }
#endif

        SgAdaLoopStmt* pointer{static_cast<SgAdaLoopStmt*>(ROSE_MALLOC(SgAdaLoopStmt::pool_size * sizeof(SgAdaLoopStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaLoopStmt::pool_size * sizeof(SgAdaLoopStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaLoopStmt::pool_size * sizeof(SgAdaLoopStmt));
#endif
        SgAdaLoopStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaLoopStmt::pool_size * sizeof(SgAdaLoopStmt), V_SgAdaLoopStmt));

        if (SgAdaLoopStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaLoopStmt* blkptr{reinterpret_cast<SgAdaLoopStmt*>(SgAdaLoopStmt::pools[blockIndex-1])};
                blkptr[SgAdaLoopStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaLoopStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaLoopStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaLoopStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaLoopStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaLoopStmt::pool_size - 1};
    SgAdaLoopStmt* testPointer{reinterpret_cast<SgAdaLoopStmt*>(SgAdaLoopStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaLoopStmt::pool_size * (SgAdaLoopStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaLoopStmtStroageClass
 * from its counterpart of SgAdaLoopStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaLoopStmt::initializeStorageClassArray(SgAdaLoopStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaLoopStmt::pools.begin(); block != SgAdaLoopStmt::pools.end(); ++block) {
        SgAdaLoopStmt* pointer{reinterpret_cast<SgAdaLoopStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaLoopStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 102683 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaSelectStmt that takes its corresponding StorageClass as parameter
 */
SgAdaSelectStmt::SgAdaSelectStmt(const SgAdaSelectStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 102693 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_select_path = (SgAdaSelectAlternativeStmt*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_select_path));
     p_or_path = (SgAdaSelectAlternativeStmt*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_or_path));
     p_else_path = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_else_path));
     p_abort_path = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_abort_path));
    p_select_type = storageSource.storageOf_select_type;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaSelectStmt*
SgAdaSelectStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaSelectStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSelectStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSelectStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSelectStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectStmt)};
        unsigned long positionInPool{localIndex % SgAdaSelectStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSelectStmt::pool_size};
        returnPointer = &(((SgAdaSelectStmt*)(SgAdaSelectStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaSelectStmt*
SgAdaSelectStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaSelectStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSelectStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSelectStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSelectStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaSelectStmt)};
        unsigned long positionInPool{localIndex % SgAdaSelectStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSelectStmt::pool_size};
        returnPointer = &(((SgAdaSelectStmt*)(SgAdaSelectStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaSelectStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaSelectStmt::pools.begin(); block != SgAdaSelectStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSelectStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaSelectStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaSelectStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaSelectStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaSelectStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaSelectStmt::pools.begin(); block != SgAdaSelectStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSelectStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaSelectStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaSelectStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaSelectStmt::clearMemoryPool() {
    SgAdaSelectStmt* tempPointer{nullptr};
    if (SgAdaSelectStmt::pools.empty() == false) {
        auto block{SgAdaSelectStmt::pools.begin()};
        SgAdaSelectStmt::next_node = reinterpret_cast<SgAdaSelectStmt*>(*block);
        for (; block != SgAdaSelectStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaSelectStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaSelectStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaSelectStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaSelectStmt::pool_size-1]);
        }
    }
}

void
SgAdaSelectStmt::deleteMemoryPool() {
    for (auto p: SgAdaSelectStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaSelectStmt::next_node = nullptr;
    SgAdaSelectStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaSelectStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSelectStmt)};
    for (size_t blockIndex = SgAdaSelectStmt::pools.size(); (blockIndex * SgAdaSelectStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSelectStmt) = %" PRIuPTR " SgAdaSelectStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSelectStmt),SgAdaSelectStmt::pool_size);
        }
#endif

        SgAdaSelectStmt* pointer{static_cast<SgAdaSelectStmt*>(ROSE_MALLOC(SgAdaSelectStmt::pool_size * sizeof(SgAdaSelectStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaSelectStmt::pool_size * sizeof(SgAdaSelectStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaSelectStmt::pool_size * sizeof(SgAdaSelectStmt));
#endif
        SgAdaSelectStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaSelectStmt::pool_size * sizeof(SgAdaSelectStmt), V_SgAdaSelectStmt));

        if (SgAdaSelectStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaSelectStmt* blkptr{reinterpret_cast<SgAdaSelectStmt*>(SgAdaSelectStmt::pools[blockIndex-1])};
                blkptr[SgAdaSelectStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaSelectStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaSelectStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaSelectStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaSelectStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaSelectStmt::pool_size - 1};
    SgAdaSelectStmt* testPointer{reinterpret_cast<SgAdaSelectStmt*>(SgAdaSelectStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaSelectStmt::pool_size * (SgAdaSelectStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaSelectStmtStroageClass
 * from its counterpart of SgAdaSelectStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaSelectStmt::initializeStorageClassArray(SgAdaSelectStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaSelectStmt::pools.begin(); block != SgAdaSelectStmt::pools.end(); ++block) {
        SgAdaSelectStmt* pointer{reinterpret_cast<SgAdaSelectStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaSelectStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 102915 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaSelectAlternativeStmt that takes its corresponding StorageClass as parameter
 */
SgAdaSelectAlternativeStmt::SgAdaSelectAlternativeStmt(const SgAdaSelectAlternativeStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 102925 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgBasicBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
     p_guard = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_guard));
     p_next = (SgAdaSelectAlternativeStmt*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_next));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaSelectAlternativeStmt*
SgAdaSelectAlternativeStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaSelectAlternativeStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSelectAlternativeStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSelectAlternativeStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaSelectAlternativeStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectAlternativeStmt)};
        unsigned long positionInPool{localIndex % SgAdaSelectAlternativeStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSelectAlternativeStmt::pool_size};
        returnPointer = &(((SgAdaSelectAlternativeStmt*)(SgAdaSelectAlternativeStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaSelectAlternativeStmt*
SgAdaSelectAlternativeStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaSelectAlternativeStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSelectAlternativeStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSelectAlternativeStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaSelectAlternativeStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaSelectAlternativeStmt)};
        unsigned long positionInPool{localIndex % SgAdaSelectAlternativeStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaSelectAlternativeStmt::pool_size};
        returnPointer = &(((SgAdaSelectAlternativeStmt*)(SgAdaSelectAlternativeStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaSelectAlternativeStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaSelectAlternativeStmt::pools.begin(); block != SgAdaSelectAlternativeStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSelectAlternativeStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaSelectAlternativeStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaSelectAlternativeStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaSelectAlternativeStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaSelectAlternativeStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaSelectAlternativeStmt::pools.begin(); block != SgAdaSelectAlternativeStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaSelectAlternativeStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaSelectAlternativeStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaSelectAlternativeStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaSelectAlternativeStmt::clearMemoryPool() {
    SgAdaSelectAlternativeStmt* tempPointer{nullptr};
    if (SgAdaSelectAlternativeStmt::pools.empty() == false) {
        auto block{SgAdaSelectAlternativeStmt::pools.begin()};
        SgAdaSelectAlternativeStmt::next_node = reinterpret_cast<SgAdaSelectAlternativeStmt*>(*block);
        for (; block != SgAdaSelectAlternativeStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaSelectAlternativeStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaSelectAlternativeStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaSelectAlternativeStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaSelectAlternativeStmt::pool_size-1]);
        }
    }
}

void
SgAdaSelectAlternativeStmt::deleteMemoryPool() {
    for (auto p: SgAdaSelectAlternativeStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaSelectAlternativeStmt::next_node = nullptr;
    SgAdaSelectAlternativeStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaSelectAlternativeStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectAlternativeStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSelectAlternativeStmt)};
    for (size_t blockIndex = SgAdaSelectAlternativeStmt::pools.size(); (blockIndex * SgAdaSelectAlternativeStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectAlternativeStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSelectAlternativeStmt) = %" PRIuPTR " SgAdaSelectAlternativeStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaSelectAlternativeStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaSelectAlternativeStmt),SgAdaSelectAlternativeStmt::pool_size);
        }
#endif

        SgAdaSelectAlternativeStmt* pointer{static_cast<SgAdaSelectAlternativeStmt*>(ROSE_MALLOC(SgAdaSelectAlternativeStmt::pool_size * sizeof(SgAdaSelectAlternativeStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaSelectAlternativeStmt::pool_size * sizeof(SgAdaSelectAlternativeStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaSelectAlternativeStmt::pool_size * sizeof(SgAdaSelectAlternativeStmt));
#endif
        SgAdaSelectAlternativeStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaSelectAlternativeStmt::pool_size * sizeof(SgAdaSelectAlternativeStmt), V_SgAdaSelectAlternativeStmt));

        if (SgAdaSelectAlternativeStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaSelectAlternativeStmt* blkptr{reinterpret_cast<SgAdaSelectAlternativeStmt*>(SgAdaSelectAlternativeStmt::pools[blockIndex-1])};
                blkptr[SgAdaSelectAlternativeStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaSelectAlternativeStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaSelectAlternativeStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaSelectAlternativeStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaSelectAlternativeStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaSelectAlternativeStmt::pool_size - 1};
    SgAdaSelectAlternativeStmt* testPointer{reinterpret_cast<SgAdaSelectAlternativeStmt*>(SgAdaSelectAlternativeStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaSelectAlternativeStmt::pool_size * (SgAdaSelectAlternativeStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaSelectAlternativeStmtStroageClass
 * from its counterpart of SgAdaSelectAlternativeStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaSelectAlternativeStmt::initializeStorageClassArray(SgAdaSelectAlternativeStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaSelectAlternativeStmt::pools.begin(); block != SgAdaSelectAlternativeStmt::pools.end(); ++block) {
        SgAdaSelectAlternativeStmt* pointer{reinterpret_cast<SgAdaSelectAlternativeStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaSelectAlternativeStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 103145 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTerminateStmt that takes its corresponding StorageClass as parameter
 */
SgAdaTerminateStmt::SgAdaTerminateStmt(const SgAdaTerminateStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 103155 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTerminateStmt*
SgAdaTerminateStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTerminateStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTerminateStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTerminateStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTerminateStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTerminateStmt)};
        unsigned long positionInPool{localIndex % SgAdaTerminateStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTerminateStmt::pool_size};
        returnPointer = &(((SgAdaTerminateStmt*)(SgAdaTerminateStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTerminateStmt*
SgAdaTerminateStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTerminateStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTerminateStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTerminateStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTerminateStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTerminateStmt)};
        unsigned long positionInPool{localIndex % SgAdaTerminateStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTerminateStmt::pool_size};
        returnPointer = &(((SgAdaTerminateStmt*)(SgAdaTerminateStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTerminateStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTerminateStmt::pools.begin(); block != SgAdaTerminateStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTerminateStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaTerminateStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTerminateStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTerminateStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTerminateStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTerminateStmt::pools.begin(); block != SgAdaTerminateStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTerminateStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaTerminateStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTerminateStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTerminateStmt::clearMemoryPool() {
    SgAdaTerminateStmt* tempPointer{nullptr};
    if (SgAdaTerminateStmt::pools.empty() == false) {
        auto block{SgAdaTerminateStmt::pools.begin()};
        SgAdaTerminateStmt::next_node = reinterpret_cast<SgAdaTerminateStmt*>(*block);
        for (; block != SgAdaTerminateStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTerminateStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTerminateStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTerminateStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTerminateStmt::pool_size-1]);
        }
    }
}

void
SgAdaTerminateStmt::deleteMemoryPool() {
    for (auto p: SgAdaTerminateStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaTerminateStmt::next_node = nullptr;
    SgAdaTerminateStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTerminateStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTerminateStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTerminateStmt)};
    for (size_t blockIndex = SgAdaTerminateStmt::pools.size(); (blockIndex * SgAdaTerminateStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTerminateStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTerminateStmt) = %" PRIuPTR " SgAdaTerminateStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTerminateStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTerminateStmt),SgAdaTerminateStmt::pool_size);
        }
#endif

        SgAdaTerminateStmt* pointer{static_cast<SgAdaTerminateStmt*>(ROSE_MALLOC(SgAdaTerminateStmt::pool_size * sizeof(SgAdaTerminateStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTerminateStmt::pool_size * sizeof(SgAdaTerminateStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTerminateStmt::pool_size * sizeof(SgAdaTerminateStmt));
#endif
        SgAdaTerminateStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTerminateStmt::pool_size * sizeof(SgAdaTerminateStmt), V_SgAdaTerminateStmt));

        if (SgAdaTerminateStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTerminateStmt* blkptr{reinterpret_cast<SgAdaTerminateStmt*>(SgAdaTerminateStmt::pools[blockIndex-1])};
                blkptr[SgAdaTerminateStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTerminateStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTerminateStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTerminateStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTerminateStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTerminateStmt::pool_size - 1};
    SgAdaTerminateStmt* testPointer{reinterpret_cast<SgAdaTerminateStmt*>(SgAdaTerminateStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTerminateStmt::pool_size * (SgAdaTerminateStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTerminateStmtStroageClass
 * from its counterpart of SgAdaTerminateStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTerminateStmt::initializeStorageClassArray(SgAdaTerminateStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTerminateStmt::pools.begin(); block != SgAdaTerminateStmt::pools.end(); ++block) {
        SgAdaTerminateStmt* pointer{reinterpret_cast<SgAdaTerminateStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaTerminateStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 103372 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaUnscopedBlock that takes its corresponding StorageClass as parameter
 */
SgAdaUnscopedBlock::SgAdaUnscopedBlock(const SgAdaUnscopedBlockStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 103382 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_statements = storageSource.storageOf_statements.rebuildDataStoredInEasyStorageClass();
    SgStatementPtrList::iterator i_statements = p_statements.begin();
    for ( ; i_statements != p_statements.end(); ++i_statements) {
        (*i_statements) = (SgStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_statements)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaUnscopedBlock*
SgAdaUnscopedBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaUnscopedBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaUnscopedBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaUnscopedBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaUnscopedBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnscopedBlock)};
        unsigned long positionInPool{localIndex % SgAdaUnscopedBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaUnscopedBlock::pool_size};
        returnPointer = &(((SgAdaUnscopedBlock*)(SgAdaUnscopedBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaUnscopedBlock*
SgAdaUnscopedBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaUnscopedBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaUnscopedBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaUnscopedBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaUnscopedBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaUnscopedBlock)};
        unsigned long positionInPool{localIndex % SgAdaUnscopedBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaUnscopedBlock::pool_size};
        returnPointer = &(((SgAdaUnscopedBlock*)(SgAdaUnscopedBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaUnscopedBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaUnscopedBlock::pools.begin(); block != SgAdaUnscopedBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaUnscopedBlock*>(*block)};
        for (unsigned i = 0; i < SgAdaUnscopedBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaUnscopedBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaUnscopedBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaUnscopedBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaUnscopedBlock::pools.begin(); block != SgAdaUnscopedBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaUnscopedBlock*>(*block)};
        for (unsigned i = 0; i < SgAdaUnscopedBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaUnscopedBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaUnscopedBlock::clearMemoryPool() {
    SgAdaUnscopedBlock* tempPointer{nullptr};
    if (SgAdaUnscopedBlock::pools.empty() == false) {
        auto block{SgAdaUnscopedBlock::pools.begin()};
        SgAdaUnscopedBlock::next_node = reinterpret_cast<SgAdaUnscopedBlock*>(*block);
        for (; block != SgAdaUnscopedBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaUnscopedBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaUnscopedBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaUnscopedBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaUnscopedBlock::pool_size-1]);
        }
    }
}

void
SgAdaUnscopedBlock::deleteMemoryPool() {
    for (auto p: SgAdaUnscopedBlock::pools) {
        ROSE_FREE(p);
    }
    SgAdaUnscopedBlock::next_node = nullptr;
    SgAdaUnscopedBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaUnscopedBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnscopedBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaUnscopedBlock)};
    for (size_t blockIndex = SgAdaUnscopedBlock::pools.size(); (blockIndex * SgAdaUnscopedBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnscopedBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaUnscopedBlock) = %" PRIuPTR " SgAdaUnscopedBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnscopedBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaUnscopedBlock),SgAdaUnscopedBlock::pool_size);
        }
#endif

        SgAdaUnscopedBlock* pointer{static_cast<SgAdaUnscopedBlock*>(ROSE_MALLOC(SgAdaUnscopedBlock::pool_size * sizeof(SgAdaUnscopedBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaUnscopedBlock::pool_size * sizeof(SgAdaUnscopedBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaUnscopedBlock::pool_size * sizeof(SgAdaUnscopedBlock));
#endif
        SgAdaUnscopedBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaUnscopedBlock::pool_size * sizeof(SgAdaUnscopedBlock), V_SgAdaUnscopedBlock));

        if (SgAdaUnscopedBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaUnscopedBlock* blkptr{reinterpret_cast<SgAdaUnscopedBlock*>(SgAdaUnscopedBlock::pools[blockIndex-1])};
                blkptr[SgAdaUnscopedBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaUnscopedBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaUnscopedBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaUnscopedBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaUnscopedBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaUnscopedBlock::pool_size - 1};
    SgAdaUnscopedBlock* testPointer{reinterpret_cast<SgAdaUnscopedBlock*>(SgAdaUnscopedBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaUnscopedBlock::pool_size * (SgAdaUnscopedBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaUnscopedBlockStroageClass
 * from its counterpart of SgAdaUnscopedBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaUnscopedBlock::initializeStorageClassArray(SgAdaUnscopedBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaUnscopedBlock::pools.begin(); block != SgAdaUnscopedBlock::pools.end(); ++block) {
        SgAdaUnscopedBlock* pointer{reinterpret_cast<SgAdaUnscopedBlock*>(*block)};
        for (unsigned i = 0; i < SgAdaUnscopedBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 103604 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaVariantWhenStmt that takes its corresponding StorageClass as parameter
 */
SgAdaVariantWhenStmt::SgAdaVariantWhenStmt(const SgAdaVariantWhenStmtStorageClass& storageSource)  : SgStatement (storageSource) {

/* #line 103614 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_choices = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_choices));
     p_components = (SgAdaUnscopedBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_components));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaVariantWhenStmt*
SgAdaVariantWhenStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaVariantWhenStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaVariantWhenStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaVariantWhenStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaVariantWhenStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantWhenStmt)};
        unsigned long positionInPool{localIndex % SgAdaVariantWhenStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaVariantWhenStmt::pool_size};
        returnPointer = &(((SgAdaVariantWhenStmt*)(SgAdaVariantWhenStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaVariantWhenStmt*
SgAdaVariantWhenStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaVariantWhenStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaVariantWhenStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaVariantWhenStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaVariantWhenStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaVariantWhenStmt)};
        unsigned long positionInPool{localIndex % SgAdaVariantWhenStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaVariantWhenStmt::pool_size};
        returnPointer = &(((SgAdaVariantWhenStmt*)(SgAdaVariantWhenStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaVariantWhenStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaVariantWhenStmt::pools.begin(); block != SgAdaVariantWhenStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaVariantWhenStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaVariantWhenStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaVariantWhenStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaVariantWhenStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaVariantWhenStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaVariantWhenStmt::pools.begin(); block != SgAdaVariantWhenStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaVariantWhenStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaVariantWhenStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaVariantWhenStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaVariantWhenStmt::clearMemoryPool() {
    SgAdaVariantWhenStmt* tempPointer{nullptr};
    if (SgAdaVariantWhenStmt::pools.empty() == false) {
        auto block{SgAdaVariantWhenStmt::pools.begin()};
        SgAdaVariantWhenStmt::next_node = reinterpret_cast<SgAdaVariantWhenStmt*>(*block);
        for (; block != SgAdaVariantWhenStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaVariantWhenStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaVariantWhenStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaVariantWhenStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaVariantWhenStmt::pool_size-1]);
        }
    }
}

void
SgAdaVariantWhenStmt::deleteMemoryPool() {
    for (auto p: SgAdaVariantWhenStmt::pools) {
        ROSE_FREE(p);
    }
    SgAdaVariantWhenStmt::next_node = nullptr;
    SgAdaVariantWhenStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaVariantWhenStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantWhenStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaVariantWhenStmt)};
    for (size_t blockIndex = SgAdaVariantWhenStmt::pools.size(); (blockIndex * SgAdaVariantWhenStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantWhenStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaVariantWhenStmt) = %" PRIuPTR " SgAdaVariantWhenStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaVariantWhenStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaVariantWhenStmt),SgAdaVariantWhenStmt::pool_size);
        }
#endif

        SgAdaVariantWhenStmt* pointer{static_cast<SgAdaVariantWhenStmt*>(ROSE_MALLOC(SgAdaVariantWhenStmt::pool_size * sizeof(SgAdaVariantWhenStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaVariantWhenStmt::pool_size * sizeof(SgAdaVariantWhenStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaVariantWhenStmt::pool_size * sizeof(SgAdaVariantWhenStmt));
#endif
        SgAdaVariantWhenStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaVariantWhenStmt::pool_size * sizeof(SgAdaVariantWhenStmt), V_SgAdaVariantWhenStmt));

        if (SgAdaVariantWhenStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaVariantWhenStmt* blkptr{reinterpret_cast<SgAdaVariantWhenStmt*>(SgAdaVariantWhenStmt::pools[blockIndex-1])};
                blkptr[SgAdaVariantWhenStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaVariantWhenStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaVariantWhenStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaVariantWhenStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaVariantWhenStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaVariantWhenStmt::pool_size - 1};
    SgAdaVariantWhenStmt* testPointer{reinterpret_cast<SgAdaVariantWhenStmt*>(SgAdaVariantWhenStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaVariantWhenStmt::pool_size * (SgAdaVariantWhenStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaVariantWhenStmtStroageClass
 * from its counterpart of SgAdaVariantWhenStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaVariantWhenStmt::initializeStorageClassArray(SgAdaVariantWhenStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaVariantWhenStmt::pools.begin(); block != SgAdaVariantWhenStmt::pools.end(); ++block) {
        SgAdaVariantWhenStmt* pointer{reinterpret_cast<SgAdaVariantWhenStmt*>(*block)};
        for (unsigned i = 0; i < SgAdaVariantWhenStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 103833 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExpression that takes its corresponding StorageClass as parameter
 */
SgExpression::SgExpression(const SgExpressionStorageClass& storageSource)  : SgLocatedNode (storageSource) {

/* #line 103843 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_need_paren = storageSource.storageOf_need_paren;
    p_lvalue = storageSource.storageOf_lvalue;
    p_global_qualified_name = storageSource.storageOf_global_qualified_name;
     p_operatorPosition = (Sg_File_Info*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operatorPosition));
     p_alternativeExpr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_alternativeExpr));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExpression*
SgExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExpression)};
        unsigned long positionInPool{localIndex % SgExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExpression::pool_size};
        returnPointer = &(((SgExpression*)(SgExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExpression*
SgExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExpression)};
        unsigned long positionInPool{localIndex % SgExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExpression::pool_size};
        returnPointer = &(((SgExpression*)(SgExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExpression::pools.begin(); block != SgExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExpression*>(*block)};
        for (unsigned i = 0; i < SgExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgExpression::pools.begin(); block != SgExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExpression*>(*block)};
        for (unsigned i = 0; i < SgExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExpression::clearMemoryPool() {
    SgExpression* tempPointer{nullptr};
    if (SgExpression::pools.empty() == false) {
        auto block{SgExpression::pools.begin()};
        SgExpression::next_node = reinterpret_cast<SgExpression*>(*block);
        for (; block != SgExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExpression::pool_size-1]);
        }
    }
}

void
SgExpression::deleteMemoryPool() {
    for (auto p: SgExpression::pools) {
        ROSE_FREE(p);
    }
    SgExpression::next_node = nullptr;
    SgExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpression)};
    for (size_t blockIndex = SgExpression::pools.size(); (blockIndex * SgExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpression) = %" PRIuPTR " SgExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpression),SgExpression::pool_size);
        }
#endif

        SgExpression* pointer{static_cast<SgExpression*>(ROSE_MALLOC(SgExpression::pool_size * sizeof(SgExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExpression::pool_size * sizeof(SgExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExpression::pool_size * sizeof(SgExpression));
#endif
        SgExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExpression::pool_size * sizeof(SgExpression), V_SgExpression));

        if (SgExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExpression* blkptr{reinterpret_cast<SgExpression*>(SgExpression::pools[blockIndex-1])};
                blkptr[SgExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExpression::pool_size - 1};
    SgExpression* testPointer{reinterpret_cast<SgExpression*>(SgExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExpression::pool_size * (SgExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExpressionStroageClass
 * from its counterpart of SgExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExpression::initializeStorageClassArray(SgExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExpression::pools.begin(); block != SgExpression::pools.end(); ++block) {
        SgExpression* pointer{reinterpret_cast<SgExpression*>(*block)};
        for (unsigned i = 0; i < SgExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 104065 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnaryOp that takes its corresponding StorageClass as parameter
 */
SgUnaryOp::SgUnaryOp(const SgUnaryOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 104075 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_i = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_i));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
    p_mode = storageSource.storageOf_mode;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnaryOp*
SgUnaryOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryOp)};
        unsigned long positionInPool{localIndex % SgUnaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnaryOp::pool_size};
        returnPointer = &(((SgUnaryOp*)(SgUnaryOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnaryOp*
SgUnaryOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnaryOp)};
        unsigned long positionInPool{localIndex % SgUnaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnaryOp::pool_size};
        returnPointer = &(((SgUnaryOp*)(SgUnaryOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnaryOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnaryOp::pools.begin(); block != SgUnaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnaryOp*>(*block)};
        for (unsigned i = 0; i < SgUnaryOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnaryOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnaryOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnaryOp* pointerOfLinkedList{nullptr};
    for (auto block = SgUnaryOp::pools.begin(); block != SgUnaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnaryOp*>(*block)};
        for (unsigned i = 0; i < SgUnaryOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnaryOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnaryOp::clearMemoryPool() {
    SgUnaryOp* tempPointer{nullptr};
    if (SgUnaryOp::pools.empty() == false) {
        auto block{SgUnaryOp::pools.begin()};
        SgUnaryOp::next_node = reinterpret_cast<SgUnaryOp*>(*block);
        for (; block != SgUnaryOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnaryOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnaryOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnaryOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnaryOp::pool_size-1]);
        }
    }
}

void
SgUnaryOp::deleteMemoryPool() {
    for (auto p: SgUnaryOp::pools) {
        ROSE_FREE(p);
    }
    SgUnaryOp::next_node = nullptr;
    SgUnaryOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnaryOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnaryOp)};
    for (size_t blockIndex = SgUnaryOp::pools.size(); (blockIndex * SgUnaryOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnaryOp) = %" PRIuPTR " SgUnaryOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnaryOp),SgUnaryOp::pool_size);
        }
#endif

        SgUnaryOp* pointer{static_cast<SgUnaryOp*>(ROSE_MALLOC(SgUnaryOp::pool_size * sizeof(SgUnaryOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnaryOp::pool_size * sizeof(SgUnaryOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnaryOp::pool_size * sizeof(SgUnaryOp));
#endif
        SgUnaryOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnaryOp::pool_size * sizeof(SgUnaryOp), V_SgUnaryOp));

        if (SgUnaryOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnaryOp* blkptr{reinterpret_cast<SgUnaryOp*>(SgUnaryOp::pools[blockIndex-1])};
                blkptr[SgUnaryOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnaryOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnaryOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnaryOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnaryOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnaryOp::pool_size - 1};
    SgUnaryOp* testPointer{reinterpret_cast<SgUnaryOp*>(SgUnaryOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnaryOp::pool_size * (SgUnaryOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnaryOpStroageClass
 * from its counterpart of SgUnaryOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnaryOp::initializeStorageClassArray(SgUnaryOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnaryOp::pools.begin(); block != SgUnaryOp::pools.end(); ++block) {
        SgUnaryOp* pointer{reinterpret_cast<SgUnaryOp*>(*block)};
        for (unsigned i = 0; i < SgUnaryOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 104295 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExpressionRoot that takes its corresponding StorageClass as parameter
 */
SgExpressionRoot::SgExpressionRoot(const SgExpressionRootStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 104305 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExpressionRoot*
SgExpressionRoot::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExpressionRoot* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpressionRoot)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpressionRoot + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExpressionRoot)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExpressionRoot)};
        unsigned long positionInPool{localIndex % SgExpressionRoot::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExpressionRoot::pool_size};
        returnPointer = &(((SgExpressionRoot*)(SgExpressionRoot::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExpressionRoot*
SgExpressionRoot::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExpressionRoot* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpressionRoot)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpressionRoot + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExpressionRoot)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExpressionRoot)};
        unsigned long positionInPool{localIndex % SgExpressionRoot::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExpressionRoot::pool_size};
        returnPointer = &(((SgExpressionRoot*)(SgExpressionRoot::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExpressionRoot::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExpressionRoot::pools.begin(); block != SgExpressionRoot::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExpressionRoot*>(*block)};
        for (unsigned i = 0; i < SgExpressionRoot::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExpressionRoot*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExpressionRoot::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExpressionRoot* pointerOfLinkedList{nullptr};
    for (auto block = SgExpressionRoot::pools.begin(); block != SgExpressionRoot::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExpressionRoot*>(*block)};
        for (unsigned i = 0; i < SgExpressionRoot::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExpressionRoot::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExpressionRoot::clearMemoryPool() {
    SgExpressionRoot* tempPointer{nullptr};
    if (SgExpressionRoot::pools.empty() == false) {
        auto block{SgExpressionRoot::pools.begin()};
        SgExpressionRoot::next_node = reinterpret_cast<SgExpressionRoot*>(*block);
        for (; block != SgExpressionRoot::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExpressionRoot*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExpressionRoot::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExpressionRoot::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExpressionRoot::pool_size-1]);
        }
    }
}

void
SgExpressionRoot::deleteMemoryPool() {
    for (auto p: SgExpressionRoot::pools) {
        ROSE_FREE(p);
    }
    SgExpressionRoot::next_node = nullptr;
    SgExpressionRoot::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExpressionRoot::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExpressionRoot) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpressionRoot)};
    for (size_t blockIndex = SgExpressionRoot::pools.size(); (blockIndex * SgExpressionRoot::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExpressionRoot) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpressionRoot) = %" PRIuPTR " SgExpressionRoot::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExpressionRoot),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExpressionRoot),SgExpressionRoot::pool_size);
        }
#endif

        SgExpressionRoot* pointer{static_cast<SgExpressionRoot*>(ROSE_MALLOC(SgExpressionRoot::pool_size * sizeof(SgExpressionRoot)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExpressionRoot::pool_size * sizeof(SgExpressionRoot));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExpressionRoot::pool_size * sizeof(SgExpressionRoot));
#endif
        SgExpressionRoot::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExpressionRoot::pool_size * sizeof(SgExpressionRoot), V_SgExpressionRoot));

        if (SgExpressionRoot::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExpressionRoot* blkptr{reinterpret_cast<SgExpressionRoot*>(SgExpressionRoot::pools[blockIndex-1])};
                blkptr[SgExpressionRoot::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExpressionRoot::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExpressionRoot::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExpressionRoot::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExpressionRoot::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExpressionRoot::pool_size - 1};
    SgExpressionRoot* testPointer{reinterpret_cast<SgExpressionRoot*>(SgExpressionRoot::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExpressionRoot::pool_size * (SgExpressionRoot::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExpressionRootStroageClass
 * from its counterpart of SgExpressionRoot. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExpressionRoot::initializeStorageClassArray(SgExpressionRootStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExpressionRoot::pools.begin(); block != SgExpressionRoot::pools.end(); ++block) {
        SgExpressionRoot* pointer{reinterpret_cast<SgExpressionRoot*>(*block)};
        for (unsigned i = 0; i < SgExpressionRoot::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 104522 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMinusOp that takes its corresponding StorageClass as parameter
 */
SgMinusOp::SgMinusOp(const SgMinusOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 104532 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMinusOp*
SgMinusOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMinusOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusOp)};
        unsigned long positionInPool{localIndex % SgMinusOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMinusOp::pool_size};
        returnPointer = &(((SgMinusOp*)(SgMinusOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMinusOp*
SgMinusOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMinusOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMinusOp)};
        unsigned long positionInPool{localIndex % SgMinusOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMinusOp::pool_size};
        returnPointer = &(((SgMinusOp*)(SgMinusOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMinusOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMinusOp::pools.begin(); block != SgMinusOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMinusOp*>(*block)};
        for (unsigned i = 0; i < SgMinusOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMinusOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMinusOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMinusOp* pointerOfLinkedList{nullptr};
    for (auto block = SgMinusOp::pools.begin(); block != SgMinusOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMinusOp*>(*block)};
        for (unsigned i = 0; i < SgMinusOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMinusOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMinusOp::clearMemoryPool() {
    SgMinusOp* tempPointer{nullptr};
    if (SgMinusOp::pools.empty() == false) {
        auto block{SgMinusOp::pools.begin()};
        SgMinusOp::next_node = reinterpret_cast<SgMinusOp*>(*block);
        for (; block != SgMinusOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMinusOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMinusOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMinusOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMinusOp::pool_size-1]);
        }
    }
}

void
SgMinusOp::deleteMemoryPool() {
    for (auto p: SgMinusOp::pools) {
        ROSE_FREE(p);
    }
    SgMinusOp::next_node = nullptr;
    SgMinusOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMinusOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusOp)};
    for (size_t blockIndex = SgMinusOp::pools.size(); (blockIndex * SgMinusOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusOp) = %" PRIuPTR " SgMinusOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusOp),SgMinusOp::pool_size);
        }
#endif

        SgMinusOp* pointer{static_cast<SgMinusOp*>(ROSE_MALLOC(SgMinusOp::pool_size * sizeof(SgMinusOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMinusOp::pool_size * sizeof(SgMinusOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMinusOp::pool_size * sizeof(SgMinusOp));
#endif
        SgMinusOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMinusOp::pool_size * sizeof(SgMinusOp), V_SgMinusOp));

        if (SgMinusOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMinusOp* blkptr{reinterpret_cast<SgMinusOp*>(SgMinusOp::pools[blockIndex-1])};
                blkptr[SgMinusOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMinusOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMinusOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMinusOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMinusOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMinusOp::pool_size - 1};
    SgMinusOp* testPointer{reinterpret_cast<SgMinusOp*>(SgMinusOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMinusOp::pool_size * (SgMinusOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMinusOpStroageClass
 * from its counterpart of SgMinusOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMinusOp::initializeStorageClassArray(SgMinusOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMinusOp::pools.begin(); block != SgMinusOp::pools.end(); ++block) {
        SgMinusOp* pointer{reinterpret_cast<SgMinusOp*>(*block)};
        for (unsigned i = 0; i < SgMinusOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 104749 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnaryAddOp that takes its corresponding StorageClass as parameter
 */
SgUnaryAddOp::SgUnaryAddOp(const SgUnaryAddOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 104759 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnaryAddOp*
SgUnaryAddOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnaryAddOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnaryAddOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnaryAddOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnaryAddOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryAddOp)};
        unsigned long positionInPool{localIndex % SgUnaryAddOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnaryAddOp::pool_size};
        returnPointer = &(((SgUnaryAddOp*)(SgUnaryAddOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnaryAddOp*
SgUnaryAddOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnaryAddOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnaryAddOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnaryAddOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnaryAddOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnaryAddOp)};
        unsigned long positionInPool{localIndex % SgUnaryAddOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnaryAddOp::pool_size};
        returnPointer = &(((SgUnaryAddOp*)(SgUnaryAddOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnaryAddOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnaryAddOp::pools.begin(); block != SgUnaryAddOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnaryAddOp*>(*block)};
        for (unsigned i = 0; i < SgUnaryAddOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnaryAddOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnaryAddOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnaryAddOp* pointerOfLinkedList{nullptr};
    for (auto block = SgUnaryAddOp::pools.begin(); block != SgUnaryAddOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnaryAddOp*>(*block)};
        for (unsigned i = 0; i < SgUnaryAddOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnaryAddOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnaryAddOp::clearMemoryPool() {
    SgUnaryAddOp* tempPointer{nullptr};
    if (SgUnaryAddOp::pools.empty() == false) {
        auto block{SgUnaryAddOp::pools.begin()};
        SgUnaryAddOp::next_node = reinterpret_cast<SgUnaryAddOp*>(*block);
        for (; block != SgUnaryAddOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnaryAddOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnaryAddOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnaryAddOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnaryAddOp::pool_size-1]);
        }
    }
}

void
SgUnaryAddOp::deleteMemoryPool() {
    for (auto p: SgUnaryAddOp::pools) {
        ROSE_FREE(p);
    }
    SgUnaryAddOp::next_node = nullptr;
    SgUnaryAddOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnaryAddOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryAddOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnaryAddOp)};
    for (size_t blockIndex = SgUnaryAddOp::pools.size(); (blockIndex * SgUnaryAddOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryAddOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnaryAddOp) = %" PRIuPTR " SgUnaryAddOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnaryAddOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnaryAddOp),SgUnaryAddOp::pool_size);
        }
#endif

        SgUnaryAddOp* pointer{static_cast<SgUnaryAddOp*>(ROSE_MALLOC(SgUnaryAddOp::pool_size * sizeof(SgUnaryAddOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnaryAddOp::pool_size * sizeof(SgUnaryAddOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnaryAddOp::pool_size * sizeof(SgUnaryAddOp));
#endif
        SgUnaryAddOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnaryAddOp::pool_size * sizeof(SgUnaryAddOp), V_SgUnaryAddOp));

        if (SgUnaryAddOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnaryAddOp* blkptr{reinterpret_cast<SgUnaryAddOp*>(SgUnaryAddOp::pools[blockIndex-1])};
                blkptr[SgUnaryAddOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnaryAddOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnaryAddOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnaryAddOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnaryAddOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnaryAddOp::pool_size - 1};
    SgUnaryAddOp* testPointer{reinterpret_cast<SgUnaryAddOp*>(SgUnaryAddOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnaryAddOp::pool_size * (SgUnaryAddOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnaryAddOpStroageClass
 * from its counterpart of SgUnaryAddOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnaryAddOp::initializeStorageClassArray(SgUnaryAddOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnaryAddOp::pools.begin(); block != SgUnaryAddOp::pools.end(); ++block) {
        SgUnaryAddOp* pointer{reinterpret_cast<SgUnaryAddOp*>(*block)};
        for (unsigned i = 0; i < SgUnaryAddOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 104976 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNotOp that takes its corresponding StorageClass as parameter
 */
SgNotOp::SgNotOp(const SgNotOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 104986 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNotOp*
SgNotOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNotOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNotOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNotOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNotOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNotOp)};
        unsigned long positionInPool{localIndex % SgNotOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNotOp::pool_size};
        returnPointer = &(((SgNotOp*)(SgNotOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNotOp*
SgNotOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNotOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNotOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNotOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNotOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNotOp)};
        unsigned long positionInPool{localIndex % SgNotOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNotOp::pool_size};
        returnPointer = &(((SgNotOp*)(SgNotOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNotOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNotOp::pools.begin(); block != SgNotOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNotOp*>(*block)};
        for (unsigned i = 0; i < SgNotOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNotOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNotOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNotOp* pointerOfLinkedList{nullptr};
    for (auto block = SgNotOp::pools.begin(); block != SgNotOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNotOp*>(*block)};
        for (unsigned i = 0; i < SgNotOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNotOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNotOp::clearMemoryPool() {
    SgNotOp* tempPointer{nullptr};
    if (SgNotOp::pools.empty() == false) {
        auto block{SgNotOp::pools.begin()};
        SgNotOp::next_node = reinterpret_cast<SgNotOp*>(*block);
        for (; block != SgNotOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNotOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNotOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNotOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNotOp::pool_size-1]);
        }
    }
}

void
SgNotOp::deleteMemoryPool() {
    for (auto p: SgNotOp::pools) {
        ROSE_FREE(p);
    }
    SgNotOp::next_node = nullptr;
    SgNotOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNotOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNotOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNotOp)};
    for (size_t blockIndex = SgNotOp::pools.size(); (blockIndex * SgNotOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNotOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNotOp) = %" PRIuPTR " SgNotOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNotOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNotOp),SgNotOp::pool_size);
        }
#endif

        SgNotOp* pointer{static_cast<SgNotOp*>(ROSE_MALLOC(SgNotOp::pool_size * sizeof(SgNotOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNotOp::pool_size * sizeof(SgNotOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNotOp::pool_size * sizeof(SgNotOp));
#endif
        SgNotOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNotOp::pool_size * sizeof(SgNotOp), V_SgNotOp));

        if (SgNotOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNotOp* blkptr{reinterpret_cast<SgNotOp*>(SgNotOp::pools[blockIndex-1])};
                blkptr[SgNotOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNotOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNotOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNotOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNotOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNotOp::pool_size - 1};
    SgNotOp* testPointer{reinterpret_cast<SgNotOp*>(SgNotOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNotOp::pool_size * (SgNotOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNotOpStroageClass
 * from its counterpart of SgNotOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNotOp::initializeStorageClassArray(SgNotOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNotOp::pools.begin(); block != SgNotOp::pools.end(); ++block) {
        SgNotOp* pointer{reinterpret_cast<SgNotOp*>(*block)};
        for (unsigned i = 0; i < SgNotOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 105203 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPointerDerefExp that takes its corresponding StorageClass as parameter
 */
SgPointerDerefExp::SgPointerDerefExp(const SgPointerDerefExpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 105213 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPointerDerefExp*
SgPointerDerefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPointerDerefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerDerefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerDerefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerDerefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerDerefExp)};
        unsigned long positionInPool{localIndex % SgPointerDerefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerDerefExp::pool_size};
        returnPointer = &(((SgPointerDerefExp*)(SgPointerDerefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPointerDerefExp*
SgPointerDerefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPointerDerefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerDerefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerDerefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerDerefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPointerDerefExp)};
        unsigned long positionInPool{localIndex % SgPointerDerefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerDerefExp::pool_size};
        returnPointer = &(((SgPointerDerefExp*)(SgPointerDerefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPointerDerefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPointerDerefExp::pools.begin(); block != SgPointerDerefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerDerefExp*>(*block)};
        for (unsigned i = 0; i < SgPointerDerefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPointerDerefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPointerDerefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPointerDerefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgPointerDerefExp::pools.begin(); block != SgPointerDerefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerDerefExp*>(*block)};
        for (unsigned i = 0; i < SgPointerDerefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPointerDerefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPointerDerefExp::clearMemoryPool() {
    SgPointerDerefExp* tempPointer{nullptr};
    if (SgPointerDerefExp::pools.empty() == false) {
        auto block{SgPointerDerefExp::pools.begin()};
        SgPointerDerefExp::next_node = reinterpret_cast<SgPointerDerefExp*>(*block);
        for (; block != SgPointerDerefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPointerDerefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPointerDerefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPointerDerefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPointerDerefExp::pool_size-1]);
        }
    }
}

void
SgPointerDerefExp::deleteMemoryPool() {
    for (auto p: SgPointerDerefExp::pools) {
        ROSE_FREE(p);
    }
    SgPointerDerefExp::next_node = nullptr;
    SgPointerDerefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPointerDerefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerDerefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerDerefExp)};
    for (size_t blockIndex = SgPointerDerefExp::pools.size(); (blockIndex * SgPointerDerefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerDerefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerDerefExp) = %" PRIuPTR " SgPointerDerefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerDerefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerDerefExp),SgPointerDerefExp::pool_size);
        }
#endif

        SgPointerDerefExp* pointer{static_cast<SgPointerDerefExp*>(ROSE_MALLOC(SgPointerDerefExp::pool_size * sizeof(SgPointerDerefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPointerDerefExp::pool_size * sizeof(SgPointerDerefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPointerDerefExp::pool_size * sizeof(SgPointerDerefExp));
#endif
        SgPointerDerefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPointerDerefExp::pool_size * sizeof(SgPointerDerefExp), V_SgPointerDerefExp));

        if (SgPointerDerefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPointerDerefExp* blkptr{reinterpret_cast<SgPointerDerefExp*>(SgPointerDerefExp::pools[blockIndex-1])};
                blkptr[SgPointerDerefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPointerDerefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPointerDerefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPointerDerefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPointerDerefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPointerDerefExp::pool_size - 1};
    SgPointerDerefExp* testPointer{reinterpret_cast<SgPointerDerefExp*>(SgPointerDerefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPointerDerefExp::pool_size * (SgPointerDerefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPointerDerefExpStroageClass
 * from its counterpart of SgPointerDerefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPointerDerefExp::initializeStorageClassArray(SgPointerDerefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPointerDerefExp::pools.begin(); block != SgPointerDerefExp::pools.end(); ++block) {
        SgPointerDerefExp* pointer{reinterpret_cast<SgPointerDerefExp*>(*block)};
        for (unsigned i = 0; i < SgPointerDerefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 105430 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAddressOfOp that takes its corresponding StorageClass as parameter
 */
SgAddressOfOp::SgAddressOfOp(const SgAddressOfOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 105440 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_originalExpressionTree = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_originalExpressionTree));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAddressOfOp*
SgAddressOfOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAddressOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAddressOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAddressOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAddressOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAddressOfOp)};
        unsigned long positionInPool{localIndex % SgAddressOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAddressOfOp::pool_size};
        returnPointer = &(((SgAddressOfOp*)(SgAddressOfOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAddressOfOp*
SgAddressOfOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAddressOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAddressOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAddressOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAddressOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAddressOfOp)};
        unsigned long positionInPool{localIndex % SgAddressOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAddressOfOp::pool_size};
        returnPointer = &(((SgAddressOfOp*)(SgAddressOfOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAddressOfOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAddressOfOp::pools.begin(); block != SgAddressOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAddressOfOp*>(*block)};
        for (unsigned i = 0; i < SgAddressOfOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAddressOfOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAddressOfOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAddressOfOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAddressOfOp::pools.begin(); block != SgAddressOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAddressOfOp*>(*block)};
        for (unsigned i = 0; i < SgAddressOfOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAddressOfOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAddressOfOp::clearMemoryPool() {
    SgAddressOfOp* tempPointer{nullptr};
    if (SgAddressOfOp::pools.empty() == false) {
        auto block{SgAddressOfOp::pools.begin()};
        SgAddressOfOp::next_node = reinterpret_cast<SgAddressOfOp*>(*block);
        for (; block != SgAddressOfOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAddressOfOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAddressOfOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAddressOfOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAddressOfOp::pool_size-1]);
        }
    }
}

void
SgAddressOfOp::deleteMemoryPool() {
    for (auto p: SgAddressOfOp::pools) {
        ROSE_FREE(p);
    }
    SgAddressOfOp::next_node = nullptr;
    SgAddressOfOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAddressOfOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAddressOfOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAddressOfOp)};
    for (size_t blockIndex = SgAddressOfOp::pools.size(); (blockIndex * SgAddressOfOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAddressOfOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAddressOfOp) = %" PRIuPTR " SgAddressOfOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAddressOfOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAddressOfOp),SgAddressOfOp::pool_size);
        }
#endif

        SgAddressOfOp* pointer{static_cast<SgAddressOfOp*>(ROSE_MALLOC(SgAddressOfOp::pool_size * sizeof(SgAddressOfOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAddressOfOp::pool_size * sizeof(SgAddressOfOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAddressOfOp::pool_size * sizeof(SgAddressOfOp));
#endif
        SgAddressOfOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAddressOfOp::pool_size * sizeof(SgAddressOfOp), V_SgAddressOfOp));

        if (SgAddressOfOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAddressOfOp* blkptr{reinterpret_cast<SgAddressOfOp*>(SgAddressOfOp::pools[blockIndex-1])};
                blkptr[SgAddressOfOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAddressOfOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAddressOfOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAddressOfOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAddressOfOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAddressOfOp::pool_size - 1};
    SgAddressOfOp* testPointer{reinterpret_cast<SgAddressOfOp*>(SgAddressOfOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAddressOfOp::pool_size * (SgAddressOfOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAddressOfOpStroageClass
 * from its counterpart of SgAddressOfOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAddressOfOp::initializeStorageClassArray(SgAddressOfOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAddressOfOp::pools.begin(); block != SgAddressOfOp::pools.end(); ++block) {
        SgAddressOfOp* pointer{reinterpret_cast<SgAddressOfOp*>(*block)};
        for (unsigned i = 0; i < SgAddressOfOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 105658 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMinusMinusOp that takes its corresponding StorageClass as parameter
 */
SgMinusMinusOp::SgMinusMinusOp(const SgMinusMinusOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 105668 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMinusMinusOp*
SgMinusMinusOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMinusMinusOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusMinusOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusMinusOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusMinusOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusMinusOp)};
        unsigned long positionInPool{localIndex % SgMinusMinusOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMinusMinusOp::pool_size};
        returnPointer = &(((SgMinusMinusOp*)(SgMinusMinusOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMinusMinusOp*
SgMinusMinusOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMinusMinusOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusMinusOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusMinusOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusMinusOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMinusMinusOp)};
        unsigned long positionInPool{localIndex % SgMinusMinusOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMinusMinusOp::pool_size};
        returnPointer = &(((SgMinusMinusOp*)(SgMinusMinusOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMinusMinusOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMinusMinusOp::pools.begin(); block != SgMinusMinusOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMinusMinusOp*>(*block)};
        for (unsigned i = 0; i < SgMinusMinusOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMinusMinusOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMinusMinusOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMinusMinusOp* pointerOfLinkedList{nullptr};
    for (auto block = SgMinusMinusOp::pools.begin(); block != SgMinusMinusOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMinusMinusOp*>(*block)};
        for (unsigned i = 0; i < SgMinusMinusOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMinusMinusOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMinusMinusOp::clearMemoryPool() {
    SgMinusMinusOp* tempPointer{nullptr};
    if (SgMinusMinusOp::pools.empty() == false) {
        auto block{SgMinusMinusOp::pools.begin()};
        SgMinusMinusOp::next_node = reinterpret_cast<SgMinusMinusOp*>(*block);
        for (; block != SgMinusMinusOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMinusMinusOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMinusMinusOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMinusMinusOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMinusMinusOp::pool_size-1]);
        }
    }
}

void
SgMinusMinusOp::deleteMemoryPool() {
    for (auto p: SgMinusMinusOp::pools) {
        ROSE_FREE(p);
    }
    SgMinusMinusOp::next_node = nullptr;
    SgMinusMinusOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMinusMinusOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusMinusOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusMinusOp)};
    for (size_t blockIndex = SgMinusMinusOp::pools.size(); (blockIndex * SgMinusMinusOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusMinusOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusMinusOp) = %" PRIuPTR " SgMinusMinusOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusMinusOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusMinusOp),SgMinusMinusOp::pool_size);
        }
#endif

        SgMinusMinusOp* pointer{static_cast<SgMinusMinusOp*>(ROSE_MALLOC(SgMinusMinusOp::pool_size * sizeof(SgMinusMinusOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMinusMinusOp::pool_size * sizeof(SgMinusMinusOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMinusMinusOp::pool_size * sizeof(SgMinusMinusOp));
#endif
        SgMinusMinusOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMinusMinusOp::pool_size * sizeof(SgMinusMinusOp), V_SgMinusMinusOp));

        if (SgMinusMinusOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMinusMinusOp* blkptr{reinterpret_cast<SgMinusMinusOp*>(SgMinusMinusOp::pools[blockIndex-1])};
                blkptr[SgMinusMinusOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMinusMinusOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMinusMinusOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMinusMinusOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMinusMinusOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMinusMinusOp::pool_size - 1};
    SgMinusMinusOp* testPointer{reinterpret_cast<SgMinusMinusOp*>(SgMinusMinusOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMinusMinusOp::pool_size * (SgMinusMinusOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMinusMinusOpStroageClass
 * from its counterpart of SgMinusMinusOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMinusMinusOp::initializeStorageClassArray(SgMinusMinusOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMinusMinusOp::pools.begin(); block != SgMinusMinusOp::pools.end(); ++block) {
        SgMinusMinusOp* pointer{reinterpret_cast<SgMinusMinusOp*>(*block)};
        for (unsigned i = 0; i < SgMinusMinusOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 105885 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPlusPlusOp that takes its corresponding StorageClass as parameter
 */
SgPlusPlusOp::SgPlusPlusOp(const SgPlusPlusOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 105895 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPlusPlusOp*
SgPlusPlusOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPlusPlusOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPlusPlusOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPlusPlusOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPlusPlusOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusPlusOp)};
        unsigned long positionInPool{localIndex % SgPlusPlusOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPlusPlusOp::pool_size};
        returnPointer = &(((SgPlusPlusOp*)(SgPlusPlusOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPlusPlusOp*
SgPlusPlusOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPlusPlusOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPlusPlusOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPlusPlusOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPlusPlusOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPlusPlusOp)};
        unsigned long positionInPool{localIndex % SgPlusPlusOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPlusPlusOp::pool_size};
        returnPointer = &(((SgPlusPlusOp*)(SgPlusPlusOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPlusPlusOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPlusPlusOp::pools.begin(); block != SgPlusPlusOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPlusPlusOp*>(*block)};
        for (unsigned i = 0; i < SgPlusPlusOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPlusPlusOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPlusPlusOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPlusPlusOp* pointerOfLinkedList{nullptr};
    for (auto block = SgPlusPlusOp::pools.begin(); block != SgPlusPlusOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPlusPlusOp*>(*block)};
        for (unsigned i = 0; i < SgPlusPlusOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPlusPlusOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPlusPlusOp::clearMemoryPool() {
    SgPlusPlusOp* tempPointer{nullptr};
    if (SgPlusPlusOp::pools.empty() == false) {
        auto block{SgPlusPlusOp::pools.begin()};
        SgPlusPlusOp::next_node = reinterpret_cast<SgPlusPlusOp*>(*block);
        for (; block != SgPlusPlusOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPlusPlusOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPlusPlusOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPlusPlusOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPlusPlusOp::pool_size-1]);
        }
    }
}

void
SgPlusPlusOp::deleteMemoryPool() {
    for (auto p: SgPlusPlusOp::pools) {
        ROSE_FREE(p);
    }
    SgPlusPlusOp::next_node = nullptr;
    SgPlusPlusOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPlusPlusOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusPlusOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPlusPlusOp)};
    for (size_t blockIndex = SgPlusPlusOp::pools.size(); (blockIndex * SgPlusPlusOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusPlusOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPlusPlusOp) = %" PRIuPTR " SgPlusPlusOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusPlusOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPlusPlusOp),SgPlusPlusOp::pool_size);
        }
#endif

        SgPlusPlusOp* pointer{static_cast<SgPlusPlusOp*>(ROSE_MALLOC(SgPlusPlusOp::pool_size * sizeof(SgPlusPlusOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPlusPlusOp::pool_size * sizeof(SgPlusPlusOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPlusPlusOp::pool_size * sizeof(SgPlusPlusOp));
#endif
        SgPlusPlusOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPlusPlusOp::pool_size * sizeof(SgPlusPlusOp), V_SgPlusPlusOp));

        if (SgPlusPlusOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPlusPlusOp* blkptr{reinterpret_cast<SgPlusPlusOp*>(SgPlusPlusOp::pools[blockIndex-1])};
                blkptr[SgPlusPlusOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPlusPlusOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPlusPlusOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPlusPlusOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPlusPlusOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPlusPlusOp::pool_size - 1};
    SgPlusPlusOp* testPointer{reinterpret_cast<SgPlusPlusOp*>(SgPlusPlusOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPlusPlusOp::pool_size * (SgPlusPlusOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPlusPlusOpStroageClass
 * from its counterpart of SgPlusPlusOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPlusPlusOp::initializeStorageClassArray(SgPlusPlusOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPlusPlusOp::pools.begin(); block != SgPlusPlusOp::pools.end(); ++block) {
        SgPlusPlusOp* pointer{reinterpret_cast<SgPlusPlusOp*>(*block)};
        for (unsigned i = 0; i < SgPlusPlusOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 106112 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBitComplementOp that takes its corresponding StorageClass as parameter
 */
SgBitComplementOp::SgBitComplementOp(const SgBitComplementOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 106122 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBitComplementOp*
SgBitComplementOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBitComplementOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitComplementOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitComplementOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitComplementOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBitComplementOp)};
        unsigned long positionInPool{localIndex % SgBitComplementOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitComplementOp::pool_size};
        returnPointer = &(((SgBitComplementOp*)(SgBitComplementOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBitComplementOp*
SgBitComplementOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBitComplementOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitComplementOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitComplementOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitComplementOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBitComplementOp)};
        unsigned long positionInPool{localIndex % SgBitComplementOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitComplementOp::pool_size};
        returnPointer = &(((SgBitComplementOp*)(SgBitComplementOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBitComplementOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBitComplementOp::pools.begin(); block != SgBitComplementOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitComplementOp*>(*block)};
        for (unsigned i = 0; i < SgBitComplementOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBitComplementOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBitComplementOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBitComplementOp* pointerOfLinkedList{nullptr};
    for (auto block = SgBitComplementOp::pools.begin(); block != SgBitComplementOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitComplementOp*>(*block)};
        for (unsigned i = 0; i < SgBitComplementOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBitComplementOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBitComplementOp::clearMemoryPool() {
    SgBitComplementOp* tempPointer{nullptr};
    if (SgBitComplementOp::pools.empty() == false) {
        auto block{SgBitComplementOp::pools.begin()};
        SgBitComplementOp::next_node = reinterpret_cast<SgBitComplementOp*>(*block);
        for (; block != SgBitComplementOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBitComplementOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBitComplementOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBitComplementOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBitComplementOp::pool_size-1]);
        }
    }
}

void
SgBitComplementOp::deleteMemoryPool() {
    for (auto p: SgBitComplementOp::pools) {
        ROSE_FREE(p);
    }
    SgBitComplementOp::next_node = nullptr;
    SgBitComplementOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBitComplementOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBitComplementOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitComplementOp)};
    for (size_t blockIndex = SgBitComplementOp::pools.size(); (blockIndex * SgBitComplementOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBitComplementOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitComplementOp) = %" PRIuPTR " SgBitComplementOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBitComplementOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitComplementOp),SgBitComplementOp::pool_size);
        }
#endif

        SgBitComplementOp* pointer{static_cast<SgBitComplementOp*>(ROSE_MALLOC(SgBitComplementOp::pool_size * sizeof(SgBitComplementOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBitComplementOp::pool_size * sizeof(SgBitComplementOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBitComplementOp::pool_size * sizeof(SgBitComplementOp));
#endif
        SgBitComplementOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBitComplementOp::pool_size * sizeof(SgBitComplementOp), V_SgBitComplementOp));

        if (SgBitComplementOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBitComplementOp* blkptr{reinterpret_cast<SgBitComplementOp*>(SgBitComplementOp::pools[blockIndex-1])};
                blkptr[SgBitComplementOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBitComplementOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBitComplementOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBitComplementOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBitComplementOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBitComplementOp::pool_size - 1};
    SgBitComplementOp* testPointer{reinterpret_cast<SgBitComplementOp*>(SgBitComplementOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBitComplementOp::pool_size * (SgBitComplementOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBitComplementOpStroageClass
 * from its counterpart of SgBitComplementOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBitComplementOp::initializeStorageClassArray(SgBitComplementOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBitComplementOp::pools.begin(); block != SgBitComplementOp::pools.end(); ++block) {
        SgBitComplementOp* pointer{reinterpret_cast<SgBitComplementOp*>(*block)};
        for (unsigned i = 0; i < SgBitComplementOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 106339 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCastExp that takes its corresponding StorageClass as parameter
 */
SgCastExp::SgCastExp(const SgCastExpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 106349 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_cast_type = storageSource.storageOf_cast_type;
     p_originalExpressionTree = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_originalExpressionTree));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_castContainsBaseTypeDefiningDeclaration = storageSource.storageOf_castContainsBaseTypeDefiningDeclaration;
    p_name_qualification_for_pointer_to_member_class_length = storageSource.storageOf_name_qualification_for_pointer_to_member_class_length;
    p_type_elaboration_for_pointer_to_member_class_required = storageSource.storageOf_type_elaboration_for_pointer_to_member_class_required;
    p_global_qualification_for_pointer_to_member_class_required = storageSource.storageOf_global_qualification_for_pointer_to_member_class_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCastExp*
SgCastExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCastExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCastExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCastExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCastExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCastExp)};
        unsigned long positionInPool{localIndex % SgCastExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCastExp::pool_size};
        returnPointer = &(((SgCastExp*)(SgCastExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCastExp*
SgCastExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCastExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCastExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCastExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCastExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCastExp)};
        unsigned long positionInPool{localIndex % SgCastExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCastExp::pool_size};
        returnPointer = &(((SgCastExp*)(SgCastExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCastExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCastExp::pools.begin(); block != SgCastExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCastExp*>(*block)};
        for (unsigned i = 0; i < SgCastExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCastExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCastExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCastExp* pointerOfLinkedList{nullptr};
    for (auto block = SgCastExp::pools.begin(); block != SgCastExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCastExp*>(*block)};
        for (unsigned i = 0; i < SgCastExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCastExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCastExp::clearMemoryPool() {
    SgCastExp* tempPointer{nullptr};
    if (SgCastExp::pools.empty() == false) {
        auto block{SgCastExp::pools.begin()};
        SgCastExp::next_node = reinterpret_cast<SgCastExp*>(*block);
        for (; block != SgCastExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCastExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCastExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCastExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCastExp::pool_size-1]);
        }
    }
}

void
SgCastExp::deleteMemoryPool() {
    for (auto p: SgCastExp::pools) {
        ROSE_FREE(p);
    }
    SgCastExp::next_node = nullptr;
    SgCastExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCastExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCastExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCastExp)};
    for (size_t blockIndex = SgCastExp::pools.size(); (blockIndex * SgCastExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCastExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCastExp) = %" PRIuPTR " SgCastExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCastExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCastExp),SgCastExp::pool_size);
        }
#endif

        SgCastExp* pointer{static_cast<SgCastExp*>(ROSE_MALLOC(SgCastExp::pool_size * sizeof(SgCastExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCastExp::pool_size * sizeof(SgCastExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCastExp::pool_size * sizeof(SgCastExp));
#endif
        SgCastExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCastExp::pool_size * sizeof(SgCastExp), V_SgCastExp));

        if (SgCastExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCastExp* blkptr{reinterpret_cast<SgCastExp*>(SgCastExp::pools[blockIndex-1])};
                blkptr[SgCastExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCastExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCastExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCastExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCastExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCastExp::pool_size - 1};
    SgCastExp* testPointer{reinterpret_cast<SgCastExp*>(SgCastExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCastExp::pool_size * (SgCastExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCastExpStroageClass
 * from its counterpart of SgCastExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCastExp::initializeStorageClassArray(SgCastExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCastExp::pools.begin(); block != SgCastExp::pools.end(); ++block) {
        SgCastExp* pointer{reinterpret_cast<SgCastExp*>(*block)};
        for (unsigned i = 0; i < SgCastExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 106575 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgThrowOp that takes its corresponding StorageClass as parameter
 */
SgThrowOp::SgThrowOp(const SgThrowOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 106585 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_throwKind = storageSource.storageOf_throwKind;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgThrowOp*
SgThrowOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgThrowOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgThrowOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgThrowOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgThrowOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgThrowOp)};
        unsigned long positionInPool{localIndex % SgThrowOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgThrowOp::pool_size};
        returnPointer = &(((SgThrowOp*)(SgThrowOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgThrowOp*
SgThrowOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgThrowOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgThrowOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgThrowOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgThrowOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgThrowOp)};
        unsigned long positionInPool{localIndex % SgThrowOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgThrowOp::pool_size};
        returnPointer = &(((SgThrowOp*)(SgThrowOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgThrowOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgThrowOp::pools.begin(); block != SgThrowOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgThrowOp*>(*block)};
        for (unsigned i = 0; i < SgThrowOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgThrowOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgThrowOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgThrowOp* pointerOfLinkedList{nullptr};
    for (auto block = SgThrowOp::pools.begin(); block != SgThrowOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgThrowOp*>(*block)};
        for (unsigned i = 0; i < SgThrowOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgThrowOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgThrowOp::clearMemoryPool() {
    SgThrowOp* tempPointer{nullptr};
    if (SgThrowOp::pools.empty() == false) {
        auto block{SgThrowOp::pools.begin()};
        SgThrowOp::next_node = reinterpret_cast<SgThrowOp*>(*block);
        for (; block != SgThrowOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgThrowOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgThrowOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgThrowOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgThrowOp::pool_size-1]);
        }
    }
}

void
SgThrowOp::deleteMemoryPool() {
    for (auto p: SgThrowOp::pools) {
        ROSE_FREE(p);
    }
    SgThrowOp::next_node = nullptr;
    SgThrowOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgThrowOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgThrowOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgThrowOp)};
    for (size_t blockIndex = SgThrowOp::pools.size(); (blockIndex * SgThrowOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgThrowOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgThrowOp) = %" PRIuPTR " SgThrowOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgThrowOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgThrowOp),SgThrowOp::pool_size);
        }
#endif

        SgThrowOp* pointer{static_cast<SgThrowOp*>(ROSE_MALLOC(SgThrowOp::pool_size * sizeof(SgThrowOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgThrowOp::pool_size * sizeof(SgThrowOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgThrowOp::pool_size * sizeof(SgThrowOp));
#endif
        SgThrowOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgThrowOp::pool_size * sizeof(SgThrowOp), V_SgThrowOp));

        if (SgThrowOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgThrowOp* blkptr{reinterpret_cast<SgThrowOp*>(SgThrowOp::pools[blockIndex-1])};
                blkptr[SgThrowOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgThrowOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgThrowOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgThrowOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgThrowOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgThrowOp::pool_size - 1};
    SgThrowOp* testPointer{reinterpret_cast<SgThrowOp*>(SgThrowOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgThrowOp::pool_size * (SgThrowOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgThrowOpStroageClass
 * from its counterpart of SgThrowOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgThrowOp::initializeStorageClassArray(SgThrowOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgThrowOp::pools.begin(); block != SgThrowOp::pools.end(); ++block) {
        SgThrowOp* pointer{reinterpret_cast<SgThrowOp*>(*block)};
        for (unsigned i = 0; i < SgThrowOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 106803 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRealPartOp that takes its corresponding StorageClass as parameter
 */
SgRealPartOp::SgRealPartOp(const SgRealPartOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 106813 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRealPartOp*
SgRealPartOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRealPartOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRealPartOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRealPartOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRealPartOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRealPartOp)};
        unsigned long positionInPool{localIndex % SgRealPartOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRealPartOp::pool_size};
        returnPointer = &(((SgRealPartOp*)(SgRealPartOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRealPartOp*
SgRealPartOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRealPartOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRealPartOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRealPartOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRealPartOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRealPartOp)};
        unsigned long positionInPool{localIndex % SgRealPartOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRealPartOp::pool_size};
        returnPointer = &(((SgRealPartOp*)(SgRealPartOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRealPartOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRealPartOp::pools.begin(); block != SgRealPartOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRealPartOp*>(*block)};
        for (unsigned i = 0; i < SgRealPartOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRealPartOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRealPartOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRealPartOp* pointerOfLinkedList{nullptr};
    for (auto block = SgRealPartOp::pools.begin(); block != SgRealPartOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRealPartOp*>(*block)};
        for (unsigned i = 0; i < SgRealPartOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRealPartOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRealPartOp::clearMemoryPool() {
    SgRealPartOp* tempPointer{nullptr};
    if (SgRealPartOp::pools.empty() == false) {
        auto block{SgRealPartOp::pools.begin()};
        SgRealPartOp::next_node = reinterpret_cast<SgRealPartOp*>(*block);
        for (; block != SgRealPartOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRealPartOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRealPartOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRealPartOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRealPartOp::pool_size-1]);
        }
    }
}

void
SgRealPartOp::deleteMemoryPool() {
    for (auto p: SgRealPartOp::pools) {
        ROSE_FREE(p);
    }
    SgRealPartOp::next_node = nullptr;
    SgRealPartOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRealPartOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRealPartOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRealPartOp)};
    for (size_t blockIndex = SgRealPartOp::pools.size(); (blockIndex * SgRealPartOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRealPartOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRealPartOp) = %" PRIuPTR " SgRealPartOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRealPartOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRealPartOp),SgRealPartOp::pool_size);
        }
#endif

        SgRealPartOp* pointer{static_cast<SgRealPartOp*>(ROSE_MALLOC(SgRealPartOp::pool_size * sizeof(SgRealPartOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRealPartOp::pool_size * sizeof(SgRealPartOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRealPartOp::pool_size * sizeof(SgRealPartOp));
#endif
        SgRealPartOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRealPartOp::pool_size * sizeof(SgRealPartOp), V_SgRealPartOp));

        if (SgRealPartOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRealPartOp* blkptr{reinterpret_cast<SgRealPartOp*>(SgRealPartOp::pools[blockIndex-1])};
                blkptr[SgRealPartOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRealPartOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRealPartOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRealPartOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRealPartOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRealPartOp::pool_size - 1};
    SgRealPartOp* testPointer{reinterpret_cast<SgRealPartOp*>(SgRealPartOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRealPartOp::pool_size * (SgRealPartOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRealPartOpStroageClass
 * from its counterpart of SgRealPartOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRealPartOp::initializeStorageClassArray(SgRealPartOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRealPartOp::pools.begin(); block != SgRealPartOp::pools.end(); ++block) {
        SgRealPartOp* pointer{reinterpret_cast<SgRealPartOp*>(*block)};
        for (unsigned i = 0; i < SgRealPartOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 107030 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgImagPartOp that takes its corresponding StorageClass as parameter
 */
SgImagPartOp::SgImagPartOp(const SgImagPartOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 107040 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgImagPartOp*
SgImagPartOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgImagPartOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImagPartOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImagPartOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImagPartOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgImagPartOp)};
        unsigned long positionInPool{localIndex % SgImagPartOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImagPartOp::pool_size};
        returnPointer = &(((SgImagPartOp*)(SgImagPartOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgImagPartOp*
SgImagPartOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgImagPartOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImagPartOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImagPartOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImagPartOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgImagPartOp)};
        unsigned long positionInPool{localIndex % SgImagPartOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImagPartOp::pool_size};
        returnPointer = &(((SgImagPartOp*)(SgImagPartOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgImagPartOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgImagPartOp::pools.begin(); block != SgImagPartOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImagPartOp*>(*block)};
        for (unsigned i = 0; i < SgImagPartOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgImagPartOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgImagPartOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgImagPartOp* pointerOfLinkedList{nullptr};
    for (auto block = SgImagPartOp::pools.begin(); block != SgImagPartOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImagPartOp*>(*block)};
        for (unsigned i = 0; i < SgImagPartOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgImagPartOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgImagPartOp::clearMemoryPool() {
    SgImagPartOp* tempPointer{nullptr};
    if (SgImagPartOp::pools.empty() == false) {
        auto block{SgImagPartOp::pools.begin()};
        SgImagPartOp::next_node = reinterpret_cast<SgImagPartOp*>(*block);
        for (; block != SgImagPartOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgImagPartOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgImagPartOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgImagPartOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgImagPartOp::pool_size-1]);
        }
    }
}

void
SgImagPartOp::deleteMemoryPool() {
    for (auto p: SgImagPartOp::pools) {
        ROSE_FREE(p);
    }
    SgImagPartOp::next_node = nullptr;
    SgImagPartOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgImagPartOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgImagPartOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgImagPartOp)};
    for (size_t blockIndex = SgImagPartOp::pools.size(); (blockIndex * SgImagPartOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgImagPartOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgImagPartOp) = %" PRIuPTR " SgImagPartOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgImagPartOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgImagPartOp),SgImagPartOp::pool_size);
        }
#endif

        SgImagPartOp* pointer{static_cast<SgImagPartOp*>(ROSE_MALLOC(SgImagPartOp::pool_size * sizeof(SgImagPartOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgImagPartOp::pool_size * sizeof(SgImagPartOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgImagPartOp::pool_size * sizeof(SgImagPartOp));
#endif
        SgImagPartOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgImagPartOp::pool_size * sizeof(SgImagPartOp), V_SgImagPartOp));

        if (SgImagPartOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgImagPartOp* blkptr{reinterpret_cast<SgImagPartOp*>(SgImagPartOp::pools[blockIndex-1])};
                blkptr[SgImagPartOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgImagPartOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgImagPartOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgImagPartOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgImagPartOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgImagPartOp::pool_size - 1};
    SgImagPartOp* testPointer{reinterpret_cast<SgImagPartOp*>(SgImagPartOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgImagPartOp::pool_size * (SgImagPartOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgImagPartOpStroageClass
 * from its counterpart of SgImagPartOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgImagPartOp::initializeStorageClassArray(SgImagPartOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgImagPartOp::pools.begin(); block != SgImagPartOp::pools.end(); ++block) {
        SgImagPartOp* pointer{reinterpret_cast<SgImagPartOp*>(*block)};
        for (unsigned i = 0; i < SgImagPartOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 107257 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgConjugateOp that takes its corresponding StorageClass as parameter
 */
SgConjugateOp::SgConjugateOp(const SgConjugateOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 107267 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgConjugateOp*
SgConjugateOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgConjugateOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConjugateOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConjugateOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConjugateOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgConjugateOp)};
        unsigned long positionInPool{localIndex % SgConjugateOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConjugateOp::pool_size};
        returnPointer = &(((SgConjugateOp*)(SgConjugateOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgConjugateOp*
SgConjugateOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgConjugateOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConjugateOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConjugateOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConjugateOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgConjugateOp)};
        unsigned long positionInPool{localIndex % SgConjugateOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConjugateOp::pool_size};
        returnPointer = &(((SgConjugateOp*)(SgConjugateOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgConjugateOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgConjugateOp::pools.begin(); block != SgConjugateOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConjugateOp*>(*block)};
        for (unsigned i = 0; i < SgConjugateOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgConjugateOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgConjugateOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgConjugateOp* pointerOfLinkedList{nullptr};
    for (auto block = SgConjugateOp::pools.begin(); block != SgConjugateOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConjugateOp*>(*block)};
        for (unsigned i = 0; i < SgConjugateOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgConjugateOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgConjugateOp::clearMemoryPool() {
    SgConjugateOp* tempPointer{nullptr};
    if (SgConjugateOp::pools.empty() == false) {
        auto block{SgConjugateOp::pools.begin()};
        SgConjugateOp::next_node = reinterpret_cast<SgConjugateOp*>(*block);
        for (; block != SgConjugateOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgConjugateOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgConjugateOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgConjugateOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgConjugateOp::pool_size-1]);
        }
    }
}

void
SgConjugateOp::deleteMemoryPool() {
    for (auto p: SgConjugateOp::pools) {
        ROSE_FREE(p);
    }
    SgConjugateOp::next_node = nullptr;
    SgConjugateOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgConjugateOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgConjugateOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgConjugateOp)};
    for (size_t blockIndex = SgConjugateOp::pools.size(); (blockIndex * SgConjugateOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgConjugateOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgConjugateOp) = %" PRIuPTR " SgConjugateOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgConjugateOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgConjugateOp),SgConjugateOp::pool_size);
        }
#endif

        SgConjugateOp* pointer{static_cast<SgConjugateOp*>(ROSE_MALLOC(SgConjugateOp::pool_size * sizeof(SgConjugateOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgConjugateOp::pool_size * sizeof(SgConjugateOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgConjugateOp::pool_size * sizeof(SgConjugateOp));
#endif
        SgConjugateOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgConjugateOp::pool_size * sizeof(SgConjugateOp), V_SgConjugateOp));

        if (SgConjugateOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgConjugateOp* blkptr{reinterpret_cast<SgConjugateOp*>(SgConjugateOp::pools[blockIndex-1])};
                blkptr[SgConjugateOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgConjugateOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgConjugateOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgConjugateOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgConjugateOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgConjugateOp::pool_size - 1};
    SgConjugateOp* testPointer{reinterpret_cast<SgConjugateOp*>(SgConjugateOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgConjugateOp::pool_size * (SgConjugateOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgConjugateOpStroageClass
 * from its counterpart of SgConjugateOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgConjugateOp::initializeStorageClassArray(SgConjugateOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgConjugateOp::pools.begin(); block != SgConjugateOp::pools.end(); ++block) {
        SgConjugateOp* pointer{reinterpret_cast<SgConjugateOp*>(*block)};
        for (unsigned i = 0; i < SgConjugateOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 107484 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUserDefinedUnaryOp that takes its corresponding StorageClass as parameter
 */
SgUserDefinedUnaryOp::SgUserDefinedUnaryOp(const SgUserDefinedUnaryOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 107494 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_operator_name = SgName ( storageSource.storageOf_operator_name);
     p_symbol = (SgFunctionSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUserDefinedUnaryOp*
SgUserDefinedUnaryOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUserDefinedUnaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUserDefinedUnaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUserDefinedUnaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUserDefinedUnaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedUnaryOp)};
        unsigned long positionInPool{localIndex % SgUserDefinedUnaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUserDefinedUnaryOp::pool_size};
        returnPointer = &(((SgUserDefinedUnaryOp*)(SgUserDefinedUnaryOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUserDefinedUnaryOp*
SgUserDefinedUnaryOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUserDefinedUnaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUserDefinedUnaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUserDefinedUnaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUserDefinedUnaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUserDefinedUnaryOp)};
        unsigned long positionInPool{localIndex % SgUserDefinedUnaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUserDefinedUnaryOp::pool_size};
        returnPointer = &(((SgUserDefinedUnaryOp*)(SgUserDefinedUnaryOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUserDefinedUnaryOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUserDefinedUnaryOp::pools.begin(); block != SgUserDefinedUnaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUserDefinedUnaryOp*>(*block)};
        for (unsigned i = 0; i < SgUserDefinedUnaryOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUserDefinedUnaryOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUserDefinedUnaryOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUserDefinedUnaryOp* pointerOfLinkedList{nullptr};
    for (auto block = SgUserDefinedUnaryOp::pools.begin(); block != SgUserDefinedUnaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUserDefinedUnaryOp*>(*block)};
        for (unsigned i = 0; i < SgUserDefinedUnaryOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUserDefinedUnaryOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUserDefinedUnaryOp::clearMemoryPool() {
    SgUserDefinedUnaryOp* tempPointer{nullptr};
    if (SgUserDefinedUnaryOp::pools.empty() == false) {
        auto block{SgUserDefinedUnaryOp::pools.begin()};
        SgUserDefinedUnaryOp::next_node = reinterpret_cast<SgUserDefinedUnaryOp*>(*block);
        for (; block != SgUserDefinedUnaryOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUserDefinedUnaryOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUserDefinedUnaryOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUserDefinedUnaryOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUserDefinedUnaryOp::pool_size-1]);
        }
    }
}

void
SgUserDefinedUnaryOp::deleteMemoryPool() {
    for (auto p: SgUserDefinedUnaryOp::pools) {
        ROSE_FREE(p);
    }
    SgUserDefinedUnaryOp::next_node = nullptr;
    SgUserDefinedUnaryOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUserDefinedUnaryOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedUnaryOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUserDefinedUnaryOp)};
    for (size_t blockIndex = SgUserDefinedUnaryOp::pools.size(); (blockIndex * SgUserDefinedUnaryOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedUnaryOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUserDefinedUnaryOp) = %" PRIuPTR " SgUserDefinedUnaryOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedUnaryOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUserDefinedUnaryOp),SgUserDefinedUnaryOp::pool_size);
        }
#endif

        SgUserDefinedUnaryOp* pointer{static_cast<SgUserDefinedUnaryOp*>(ROSE_MALLOC(SgUserDefinedUnaryOp::pool_size * sizeof(SgUserDefinedUnaryOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUserDefinedUnaryOp::pool_size * sizeof(SgUserDefinedUnaryOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUserDefinedUnaryOp::pool_size * sizeof(SgUserDefinedUnaryOp));
#endif
        SgUserDefinedUnaryOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUserDefinedUnaryOp::pool_size * sizeof(SgUserDefinedUnaryOp), V_SgUserDefinedUnaryOp));

        if (SgUserDefinedUnaryOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUserDefinedUnaryOp* blkptr{reinterpret_cast<SgUserDefinedUnaryOp*>(SgUserDefinedUnaryOp::pools[blockIndex-1])};
                blkptr[SgUserDefinedUnaryOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUserDefinedUnaryOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUserDefinedUnaryOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUserDefinedUnaryOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUserDefinedUnaryOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUserDefinedUnaryOp::pool_size - 1};
    SgUserDefinedUnaryOp* testPointer{reinterpret_cast<SgUserDefinedUnaryOp*>(SgUserDefinedUnaryOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUserDefinedUnaryOp::pool_size * (SgUserDefinedUnaryOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUserDefinedUnaryOpStroageClass
 * from its counterpart of SgUserDefinedUnaryOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUserDefinedUnaryOp::initializeStorageClassArray(SgUserDefinedUnaryOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUserDefinedUnaryOp::pools.begin(); block != SgUserDefinedUnaryOp::pools.end(); ++block) {
        SgUserDefinedUnaryOp* pointer{reinterpret_cast<SgUserDefinedUnaryOp*>(*block)};
        for (unsigned i = 0; i < SgUserDefinedUnaryOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 107713 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMatrixTransposeOp that takes its corresponding StorageClass as parameter
 */
SgMatrixTransposeOp::SgMatrixTransposeOp(const SgMatrixTransposeOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 107723 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_is_conjugate = storageSource.storageOf_is_conjugate;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMatrixTransposeOp*
SgMatrixTransposeOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMatrixTransposeOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatrixTransposeOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatrixTransposeOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatrixTransposeOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixTransposeOp)};
        unsigned long positionInPool{localIndex % SgMatrixTransposeOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMatrixTransposeOp::pool_size};
        returnPointer = &(((SgMatrixTransposeOp*)(SgMatrixTransposeOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMatrixTransposeOp*
SgMatrixTransposeOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMatrixTransposeOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatrixTransposeOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatrixTransposeOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatrixTransposeOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMatrixTransposeOp)};
        unsigned long positionInPool{localIndex % SgMatrixTransposeOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMatrixTransposeOp::pool_size};
        returnPointer = &(((SgMatrixTransposeOp*)(SgMatrixTransposeOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMatrixTransposeOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMatrixTransposeOp::pools.begin(); block != SgMatrixTransposeOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMatrixTransposeOp*>(*block)};
        for (unsigned i = 0; i < SgMatrixTransposeOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMatrixTransposeOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMatrixTransposeOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMatrixTransposeOp* pointerOfLinkedList{nullptr};
    for (auto block = SgMatrixTransposeOp::pools.begin(); block != SgMatrixTransposeOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMatrixTransposeOp*>(*block)};
        for (unsigned i = 0; i < SgMatrixTransposeOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMatrixTransposeOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMatrixTransposeOp::clearMemoryPool() {
    SgMatrixTransposeOp* tempPointer{nullptr};
    if (SgMatrixTransposeOp::pools.empty() == false) {
        auto block{SgMatrixTransposeOp::pools.begin()};
        SgMatrixTransposeOp::next_node = reinterpret_cast<SgMatrixTransposeOp*>(*block);
        for (; block != SgMatrixTransposeOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMatrixTransposeOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMatrixTransposeOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMatrixTransposeOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMatrixTransposeOp::pool_size-1]);
        }
    }
}

void
SgMatrixTransposeOp::deleteMemoryPool() {
    for (auto p: SgMatrixTransposeOp::pools) {
        ROSE_FREE(p);
    }
    SgMatrixTransposeOp::next_node = nullptr;
    SgMatrixTransposeOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMatrixTransposeOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixTransposeOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatrixTransposeOp)};
    for (size_t blockIndex = SgMatrixTransposeOp::pools.size(); (blockIndex * SgMatrixTransposeOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixTransposeOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatrixTransposeOp) = %" PRIuPTR " SgMatrixTransposeOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixTransposeOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatrixTransposeOp),SgMatrixTransposeOp::pool_size);
        }
#endif

        SgMatrixTransposeOp* pointer{static_cast<SgMatrixTransposeOp*>(ROSE_MALLOC(SgMatrixTransposeOp::pool_size * sizeof(SgMatrixTransposeOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMatrixTransposeOp::pool_size * sizeof(SgMatrixTransposeOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMatrixTransposeOp::pool_size * sizeof(SgMatrixTransposeOp));
#endif
        SgMatrixTransposeOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMatrixTransposeOp::pool_size * sizeof(SgMatrixTransposeOp), V_SgMatrixTransposeOp));

        if (SgMatrixTransposeOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMatrixTransposeOp* blkptr{reinterpret_cast<SgMatrixTransposeOp*>(SgMatrixTransposeOp::pools[blockIndex-1])};
                blkptr[SgMatrixTransposeOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMatrixTransposeOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMatrixTransposeOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMatrixTransposeOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMatrixTransposeOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMatrixTransposeOp::pool_size - 1};
    SgMatrixTransposeOp* testPointer{reinterpret_cast<SgMatrixTransposeOp*>(SgMatrixTransposeOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMatrixTransposeOp::pool_size * (SgMatrixTransposeOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMatrixTransposeOpStroageClass
 * from its counterpart of SgMatrixTransposeOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMatrixTransposeOp::initializeStorageClassArray(SgMatrixTransposeOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMatrixTransposeOp::pools.begin(); block != SgMatrixTransposeOp::pools.end(); ++block) {
        SgMatrixTransposeOp* pointer{reinterpret_cast<SgMatrixTransposeOp*>(*block)};
        for (unsigned i = 0; i < SgMatrixTransposeOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 107941 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAbsOp that takes its corresponding StorageClass as parameter
 */
SgAbsOp::SgAbsOp(const SgAbsOpStorageClass& storageSource)  : SgUnaryOp (storageSource) {

/* #line 107951 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAbsOp*
SgAbsOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAbsOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAbsOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAbsOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAbsOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAbsOp)};
        unsigned long positionInPool{localIndex % SgAbsOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAbsOp::pool_size};
        returnPointer = &(((SgAbsOp*)(SgAbsOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAbsOp*
SgAbsOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAbsOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAbsOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAbsOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAbsOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAbsOp)};
        unsigned long positionInPool{localIndex % SgAbsOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAbsOp::pool_size};
        returnPointer = &(((SgAbsOp*)(SgAbsOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAbsOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAbsOp::pools.begin(); block != SgAbsOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAbsOp*>(*block)};
        for (unsigned i = 0; i < SgAbsOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAbsOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAbsOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAbsOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAbsOp::pools.begin(); block != SgAbsOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAbsOp*>(*block)};
        for (unsigned i = 0; i < SgAbsOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAbsOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAbsOp::clearMemoryPool() {
    SgAbsOp* tempPointer{nullptr};
    if (SgAbsOp::pools.empty() == false) {
        auto block{SgAbsOp::pools.begin()};
        SgAbsOp::next_node = reinterpret_cast<SgAbsOp*>(*block);
        for (; block != SgAbsOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAbsOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAbsOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAbsOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAbsOp::pool_size-1]);
        }
    }
}

void
SgAbsOp::deleteMemoryPool() {
    for (auto p: SgAbsOp::pools) {
        ROSE_FREE(p);
    }
    SgAbsOp::next_node = nullptr;
    SgAbsOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAbsOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAbsOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAbsOp)};
    for (size_t blockIndex = SgAbsOp::pools.size(); (blockIndex * SgAbsOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAbsOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAbsOp) = %" PRIuPTR " SgAbsOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAbsOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAbsOp),SgAbsOp::pool_size);
        }
#endif

        SgAbsOp* pointer{static_cast<SgAbsOp*>(ROSE_MALLOC(SgAbsOp::pool_size * sizeof(SgAbsOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAbsOp::pool_size * sizeof(SgAbsOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAbsOp::pool_size * sizeof(SgAbsOp));
#endif
        SgAbsOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAbsOp::pool_size * sizeof(SgAbsOp), V_SgAbsOp));

        if (SgAbsOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAbsOp* blkptr{reinterpret_cast<SgAbsOp*>(SgAbsOp::pools[blockIndex-1])};
                blkptr[SgAbsOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAbsOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAbsOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAbsOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAbsOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAbsOp::pool_size - 1};
    SgAbsOp* testPointer{reinterpret_cast<SgAbsOp*>(SgAbsOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAbsOp::pool_size * (SgAbsOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAbsOpStroageClass
 * from its counterpart of SgAbsOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAbsOp::initializeStorageClassArray(SgAbsOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAbsOp::pools.begin(); block != SgAbsOp::pools.end(); ++block) {
        SgAbsOp* pointer{reinterpret_cast<SgAbsOp*>(*block)};
        for (unsigned i = 0; i < SgAbsOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 108168 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBinaryOp that takes its corresponding StorageClass as parameter
 */
SgBinaryOp::SgBinaryOp(const SgBinaryOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 108178 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lhs_operand_i = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lhs_operand_i));
     p_rhs_operand_i = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rhs_operand_i));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
     p_originalExpressionTree = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_originalExpressionTree));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBinaryOp*
SgBinaryOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBinaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBinaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBinaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBinaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryOp)};
        unsigned long positionInPool{localIndex % SgBinaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBinaryOp::pool_size};
        returnPointer = &(((SgBinaryOp*)(SgBinaryOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBinaryOp*
SgBinaryOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBinaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBinaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBinaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBinaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBinaryOp)};
        unsigned long positionInPool{localIndex % SgBinaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBinaryOp::pool_size};
        returnPointer = &(((SgBinaryOp*)(SgBinaryOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBinaryOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBinaryOp::pools.begin(); block != SgBinaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBinaryOp*>(*block)};
        for (unsigned i = 0; i < SgBinaryOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBinaryOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBinaryOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBinaryOp* pointerOfLinkedList{nullptr};
    for (auto block = SgBinaryOp::pools.begin(); block != SgBinaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBinaryOp*>(*block)};
        for (unsigned i = 0; i < SgBinaryOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBinaryOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBinaryOp::clearMemoryPool() {
    SgBinaryOp* tempPointer{nullptr};
    if (SgBinaryOp::pools.empty() == false) {
        auto block{SgBinaryOp::pools.begin()};
        SgBinaryOp::next_node = reinterpret_cast<SgBinaryOp*>(*block);
        for (; block != SgBinaryOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBinaryOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBinaryOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBinaryOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBinaryOp::pool_size-1]);
        }
    }
}

void
SgBinaryOp::deleteMemoryPool() {
    for (auto p: SgBinaryOp::pools) {
        ROSE_FREE(p);
    }
    SgBinaryOp::next_node = nullptr;
    SgBinaryOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBinaryOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBinaryOp)};
    for (size_t blockIndex = SgBinaryOp::pools.size(); (blockIndex * SgBinaryOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBinaryOp) = %" PRIuPTR " SgBinaryOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBinaryOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBinaryOp),SgBinaryOp::pool_size);
        }
#endif

        SgBinaryOp* pointer{static_cast<SgBinaryOp*>(ROSE_MALLOC(SgBinaryOp::pool_size * sizeof(SgBinaryOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBinaryOp::pool_size * sizeof(SgBinaryOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBinaryOp::pool_size * sizeof(SgBinaryOp));
#endif
        SgBinaryOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBinaryOp::pool_size * sizeof(SgBinaryOp), V_SgBinaryOp));

        if (SgBinaryOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBinaryOp* blkptr{reinterpret_cast<SgBinaryOp*>(SgBinaryOp::pools[blockIndex-1])};
                blkptr[SgBinaryOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBinaryOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBinaryOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBinaryOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBinaryOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBinaryOp::pool_size - 1};
    SgBinaryOp* testPointer{reinterpret_cast<SgBinaryOp*>(SgBinaryOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBinaryOp::pool_size * (SgBinaryOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBinaryOpStroageClass
 * from its counterpart of SgBinaryOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBinaryOp::initializeStorageClassArray(SgBinaryOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBinaryOp::pools.begin(); block != SgBinaryOp::pools.end(); ++block) {
        SgBinaryOp* pointer{reinterpret_cast<SgBinaryOp*>(*block)};
        for (unsigned i = 0; i < SgBinaryOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 108399 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgArrowExp that takes its corresponding StorageClass as parameter
 */
SgArrowExp::SgArrowExp(const SgArrowExpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 108409 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgArrowExp*
SgArrowExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgArrowExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrowExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrowExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrowExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowExp)};
        unsigned long positionInPool{localIndex % SgArrowExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArrowExp::pool_size};
        returnPointer = &(((SgArrowExp*)(SgArrowExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgArrowExp*
SgArrowExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgArrowExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrowExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrowExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrowExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgArrowExp)};
        unsigned long positionInPool{localIndex % SgArrowExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArrowExp::pool_size};
        returnPointer = &(((SgArrowExp*)(SgArrowExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgArrowExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgArrowExp::pools.begin(); block != SgArrowExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArrowExp*>(*block)};
        for (unsigned i = 0; i < SgArrowExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgArrowExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgArrowExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgArrowExp* pointerOfLinkedList{nullptr};
    for (auto block = SgArrowExp::pools.begin(); block != SgArrowExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArrowExp*>(*block)};
        for (unsigned i = 0; i < SgArrowExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgArrowExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgArrowExp::clearMemoryPool() {
    SgArrowExp* tempPointer{nullptr};
    if (SgArrowExp::pools.empty() == false) {
        auto block{SgArrowExp::pools.begin()};
        SgArrowExp::next_node = reinterpret_cast<SgArrowExp*>(*block);
        for (; block != SgArrowExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgArrowExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgArrowExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgArrowExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgArrowExp::pool_size-1]);
        }
    }
}

void
SgArrowExp::deleteMemoryPool() {
    for (auto p: SgArrowExp::pools) {
        ROSE_FREE(p);
    }
    SgArrowExp::next_node = nullptr;
    SgArrowExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgArrowExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrowExp)};
    for (size_t blockIndex = SgArrowExp::pools.size(); (blockIndex * SgArrowExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrowExp) = %" PRIuPTR " SgArrowExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrowExp),SgArrowExp::pool_size);
        }
#endif

        SgArrowExp* pointer{static_cast<SgArrowExp*>(ROSE_MALLOC(SgArrowExp::pool_size * sizeof(SgArrowExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgArrowExp::pool_size * sizeof(SgArrowExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgArrowExp::pool_size * sizeof(SgArrowExp));
#endif
        SgArrowExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgArrowExp::pool_size * sizeof(SgArrowExp), V_SgArrowExp));

        if (SgArrowExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgArrowExp* blkptr{reinterpret_cast<SgArrowExp*>(SgArrowExp::pools[blockIndex-1])};
                blkptr[SgArrowExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgArrowExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgArrowExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgArrowExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgArrowExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgArrowExp::pool_size - 1};
    SgArrowExp* testPointer{reinterpret_cast<SgArrowExp*>(SgArrowExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgArrowExp::pool_size * (SgArrowExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgArrowExpStroageClass
 * from its counterpart of SgArrowExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgArrowExp::initializeStorageClassArray(SgArrowExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgArrowExp::pools.begin(); block != SgArrowExp::pools.end(); ++block) {
        SgArrowExp* pointer{reinterpret_cast<SgArrowExp*>(*block)};
        for (unsigned i = 0; i < SgArrowExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 108626 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDotExp that takes its corresponding StorageClass as parameter
 */
SgDotExp::SgDotExp(const SgDotExpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 108636 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDotExp*
SgDotExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDotExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDotExp)};
        unsigned long positionInPool{localIndex % SgDotExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDotExp::pool_size};
        returnPointer = &(((SgDotExp*)(SgDotExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDotExp*
SgDotExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDotExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDotExp)};
        unsigned long positionInPool{localIndex % SgDotExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDotExp::pool_size};
        returnPointer = &(((SgDotExp*)(SgDotExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDotExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDotExp::pools.begin(); block != SgDotExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDotExp*>(*block)};
        for (unsigned i = 0; i < SgDotExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDotExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDotExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDotExp* pointerOfLinkedList{nullptr};
    for (auto block = SgDotExp::pools.begin(); block != SgDotExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDotExp*>(*block)};
        for (unsigned i = 0; i < SgDotExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDotExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDotExp::clearMemoryPool() {
    SgDotExp* tempPointer{nullptr};
    if (SgDotExp::pools.empty() == false) {
        auto block{SgDotExp::pools.begin()};
        SgDotExp::next_node = reinterpret_cast<SgDotExp*>(*block);
        for (; block != SgDotExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDotExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDotExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDotExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDotExp::pool_size-1]);
        }
    }
}

void
SgDotExp::deleteMemoryPool() {
    for (auto p: SgDotExp::pools) {
        ROSE_FREE(p);
    }
    SgDotExp::next_node = nullptr;
    SgDotExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDotExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDotExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotExp)};
    for (size_t blockIndex = SgDotExp::pools.size(); (blockIndex * SgDotExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDotExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotExp) = %" PRIuPTR " SgDotExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDotExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotExp),SgDotExp::pool_size);
        }
#endif

        SgDotExp* pointer{static_cast<SgDotExp*>(ROSE_MALLOC(SgDotExp::pool_size * sizeof(SgDotExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDotExp::pool_size * sizeof(SgDotExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDotExp::pool_size * sizeof(SgDotExp));
#endif
        SgDotExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDotExp::pool_size * sizeof(SgDotExp), V_SgDotExp));

        if (SgDotExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDotExp* blkptr{reinterpret_cast<SgDotExp*>(SgDotExp::pools[blockIndex-1])};
                blkptr[SgDotExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDotExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDotExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDotExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDotExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDotExp::pool_size - 1};
    SgDotExp* testPointer{reinterpret_cast<SgDotExp*>(SgDotExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDotExp::pool_size * (SgDotExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDotExpStroageClass
 * from its counterpart of SgDotExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDotExp::initializeStorageClassArray(SgDotExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDotExp::pools.begin(); block != SgDotExp::pools.end(); ++block) {
        SgDotExp* pointer{reinterpret_cast<SgDotExp*>(*block)};
        for (unsigned i = 0; i < SgDotExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 108853 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDotStarOp that takes its corresponding StorageClass as parameter
 */
SgDotStarOp::SgDotStarOp(const SgDotStarOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 108863 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDotStarOp*
SgDotStarOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDotStarOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotStarOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotStarOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotStarOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDotStarOp)};
        unsigned long positionInPool{localIndex % SgDotStarOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDotStarOp::pool_size};
        returnPointer = &(((SgDotStarOp*)(SgDotStarOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDotStarOp*
SgDotStarOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDotStarOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotStarOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotStarOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotStarOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDotStarOp)};
        unsigned long positionInPool{localIndex % SgDotStarOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDotStarOp::pool_size};
        returnPointer = &(((SgDotStarOp*)(SgDotStarOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDotStarOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDotStarOp::pools.begin(); block != SgDotStarOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDotStarOp*>(*block)};
        for (unsigned i = 0; i < SgDotStarOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDotStarOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDotStarOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDotStarOp* pointerOfLinkedList{nullptr};
    for (auto block = SgDotStarOp::pools.begin(); block != SgDotStarOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDotStarOp*>(*block)};
        for (unsigned i = 0; i < SgDotStarOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDotStarOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDotStarOp::clearMemoryPool() {
    SgDotStarOp* tempPointer{nullptr};
    if (SgDotStarOp::pools.empty() == false) {
        auto block{SgDotStarOp::pools.begin()};
        SgDotStarOp::next_node = reinterpret_cast<SgDotStarOp*>(*block);
        for (; block != SgDotStarOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDotStarOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDotStarOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDotStarOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDotStarOp::pool_size-1]);
        }
    }
}

void
SgDotStarOp::deleteMemoryPool() {
    for (auto p: SgDotStarOp::pools) {
        ROSE_FREE(p);
    }
    SgDotStarOp::next_node = nullptr;
    SgDotStarOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDotStarOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDotStarOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotStarOp)};
    for (size_t blockIndex = SgDotStarOp::pools.size(); (blockIndex * SgDotStarOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDotStarOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotStarOp) = %" PRIuPTR " SgDotStarOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDotStarOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotStarOp),SgDotStarOp::pool_size);
        }
#endif

        SgDotStarOp* pointer{static_cast<SgDotStarOp*>(ROSE_MALLOC(SgDotStarOp::pool_size * sizeof(SgDotStarOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDotStarOp::pool_size * sizeof(SgDotStarOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDotStarOp::pool_size * sizeof(SgDotStarOp));
#endif
        SgDotStarOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDotStarOp::pool_size * sizeof(SgDotStarOp), V_SgDotStarOp));

        if (SgDotStarOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDotStarOp* blkptr{reinterpret_cast<SgDotStarOp*>(SgDotStarOp::pools[blockIndex-1])};
                blkptr[SgDotStarOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDotStarOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDotStarOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDotStarOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDotStarOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDotStarOp::pool_size - 1};
    SgDotStarOp* testPointer{reinterpret_cast<SgDotStarOp*>(SgDotStarOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDotStarOp::pool_size * (SgDotStarOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDotStarOpStroageClass
 * from its counterpart of SgDotStarOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDotStarOp::initializeStorageClassArray(SgDotStarOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDotStarOp::pools.begin(); block != SgDotStarOp::pools.end(); ++block) {
        SgDotStarOp* pointer{reinterpret_cast<SgDotStarOp*>(*block)};
        for (unsigned i = 0; i < SgDotStarOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 109080 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgArrowStarOp that takes its corresponding StorageClass as parameter
 */
SgArrowStarOp::SgArrowStarOp(const SgArrowStarOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 109090 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgArrowStarOp*
SgArrowStarOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgArrowStarOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrowStarOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrowStarOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgArrowStarOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowStarOp)};
        unsigned long positionInPool{localIndex % SgArrowStarOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArrowStarOp::pool_size};
        returnPointer = &(((SgArrowStarOp*)(SgArrowStarOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgArrowStarOp*
SgArrowStarOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgArrowStarOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrowStarOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrowStarOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgArrowStarOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgArrowStarOp)};
        unsigned long positionInPool{localIndex % SgArrowStarOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgArrowStarOp::pool_size};
        returnPointer = &(((SgArrowStarOp*)(SgArrowStarOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgArrowStarOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgArrowStarOp::pools.begin(); block != SgArrowStarOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArrowStarOp*>(*block)};
        for (unsigned i = 0; i < SgArrowStarOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgArrowStarOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgArrowStarOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgArrowStarOp* pointerOfLinkedList{nullptr};
    for (auto block = SgArrowStarOp::pools.begin(); block != SgArrowStarOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgArrowStarOp*>(*block)};
        for (unsigned i = 0; i < SgArrowStarOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgArrowStarOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgArrowStarOp::clearMemoryPool() {
    SgArrowStarOp* tempPointer{nullptr};
    if (SgArrowStarOp::pools.empty() == false) {
        auto block{SgArrowStarOp::pools.begin()};
        SgArrowStarOp::next_node = reinterpret_cast<SgArrowStarOp*>(*block);
        for (; block != SgArrowStarOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgArrowStarOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgArrowStarOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgArrowStarOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgArrowStarOp::pool_size-1]);
        }
    }
}

void
SgArrowStarOp::deleteMemoryPool() {
    for (auto p: SgArrowStarOp::pools) {
        ROSE_FREE(p);
    }
    SgArrowStarOp::next_node = nullptr;
    SgArrowStarOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgArrowStarOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowStarOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrowStarOp)};
    for (size_t blockIndex = SgArrowStarOp::pools.size(); (blockIndex * SgArrowStarOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowStarOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrowStarOp) = %" PRIuPTR " SgArrowStarOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgArrowStarOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgArrowStarOp),SgArrowStarOp::pool_size);
        }
#endif

        SgArrowStarOp* pointer{static_cast<SgArrowStarOp*>(ROSE_MALLOC(SgArrowStarOp::pool_size * sizeof(SgArrowStarOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgArrowStarOp::pool_size * sizeof(SgArrowStarOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgArrowStarOp::pool_size * sizeof(SgArrowStarOp));
#endif
        SgArrowStarOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgArrowStarOp::pool_size * sizeof(SgArrowStarOp), V_SgArrowStarOp));

        if (SgArrowStarOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgArrowStarOp* blkptr{reinterpret_cast<SgArrowStarOp*>(SgArrowStarOp::pools[blockIndex-1])};
                blkptr[SgArrowStarOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgArrowStarOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgArrowStarOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgArrowStarOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgArrowStarOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgArrowStarOp::pool_size - 1};
    SgArrowStarOp* testPointer{reinterpret_cast<SgArrowStarOp*>(SgArrowStarOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgArrowStarOp::pool_size * (SgArrowStarOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgArrowStarOpStroageClass
 * from its counterpart of SgArrowStarOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgArrowStarOp::initializeStorageClassArray(SgArrowStarOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgArrowStarOp::pools.begin(); block != SgArrowStarOp::pools.end(); ++block) {
        SgArrowStarOp* pointer{reinterpret_cast<SgArrowStarOp*>(*block)};
        for (unsigned i = 0; i < SgArrowStarOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 109307 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEqualityOp that takes its corresponding StorageClass as parameter
 */
SgEqualityOp::SgEqualityOp(const SgEqualityOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 109317 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEqualityOp*
SgEqualityOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEqualityOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEqualityOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEqualityOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEqualityOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEqualityOp)};
        unsigned long positionInPool{localIndex % SgEqualityOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEqualityOp::pool_size};
        returnPointer = &(((SgEqualityOp*)(SgEqualityOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEqualityOp*
SgEqualityOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEqualityOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEqualityOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEqualityOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEqualityOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEqualityOp)};
        unsigned long positionInPool{localIndex % SgEqualityOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEqualityOp::pool_size};
        returnPointer = &(((SgEqualityOp*)(SgEqualityOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEqualityOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEqualityOp::pools.begin(); block != SgEqualityOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEqualityOp*>(*block)};
        for (unsigned i = 0; i < SgEqualityOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEqualityOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEqualityOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEqualityOp* pointerOfLinkedList{nullptr};
    for (auto block = SgEqualityOp::pools.begin(); block != SgEqualityOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEqualityOp*>(*block)};
        for (unsigned i = 0; i < SgEqualityOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEqualityOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEqualityOp::clearMemoryPool() {
    SgEqualityOp* tempPointer{nullptr};
    if (SgEqualityOp::pools.empty() == false) {
        auto block{SgEqualityOp::pools.begin()};
        SgEqualityOp::next_node = reinterpret_cast<SgEqualityOp*>(*block);
        for (; block != SgEqualityOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEqualityOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEqualityOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEqualityOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEqualityOp::pool_size-1]);
        }
    }
}

void
SgEqualityOp::deleteMemoryPool() {
    for (auto p: SgEqualityOp::pools) {
        ROSE_FREE(p);
    }
    SgEqualityOp::next_node = nullptr;
    SgEqualityOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEqualityOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEqualityOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEqualityOp)};
    for (size_t blockIndex = SgEqualityOp::pools.size(); (blockIndex * SgEqualityOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEqualityOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEqualityOp) = %" PRIuPTR " SgEqualityOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEqualityOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEqualityOp),SgEqualityOp::pool_size);
        }
#endif

        SgEqualityOp* pointer{static_cast<SgEqualityOp*>(ROSE_MALLOC(SgEqualityOp::pool_size * sizeof(SgEqualityOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEqualityOp::pool_size * sizeof(SgEqualityOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEqualityOp::pool_size * sizeof(SgEqualityOp));
#endif
        SgEqualityOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEqualityOp::pool_size * sizeof(SgEqualityOp), V_SgEqualityOp));

        if (SgEqualityOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEqualityOp* blkptr{reinterpret_cast<SgEqualityOp*>(SgEqualityOp::pools[blockIndex-1])};
                blkptr[SgEqualityOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEqualityOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEqualityOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEqualityOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEqualityOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEqualityOp::pool_size - 1};
    SgEqualityOp* testPointer{reinterpret_cast<SgEqualityOp*>(SgEqualityOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEqualityOp::pool_size * (SgEqualityOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEqualityOpStroageClass
 * from its counterpart of SgEqualityOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEqualityOp::initializeStorageClassArray(SgEqualityOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEqualityOp::pools.begin(); block != SgEqualityOp::pools.end(); ++block) {
        SgEqualityOp* pointer{reinterpret_cast<SgEqualityOp*>(*block)};
        for (unsigned i = 0; i < SgEqualityOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 109534 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLessThanOp that takes its corresponding StorageClass as parameter
 */
SgLessThanOp::SgLessThanOp(const SgLessThanOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 109544 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLessThanOp*
SgLessThanOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLessThanOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLessThanOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLessThanOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLessThanOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLessThanOp)};
        unsigned long positionInPool{localIndex % SgLessThanOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLessThanOp::pool_size};
        returnPointer = &(((SgLessThanOp*)(SgLessThanOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLessThanOp*
SgLessThanOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLessThanOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLessThanOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLessThanOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLessThanOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLessThanOp)};
        unsigned long positionInPool{localIndex % SgLessThanOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLessThanOp::pool_size};
        returnPointer = &(((SgLessThanOp*)(SgLessThanOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLessThanOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLessThanOp::pools.begin(); block != SgLessThanOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLessThanOp*>(*block)};
        for (unsigned i = 0; i < SgLessThanOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLessThanOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLessThanOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLessThanOp* pointerOfLinkedList{nullptr};
    for (auto block = SgLessThanOp::pools.begin(); block != SgLessThanOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLessThanOp*>(*block)};
        for (unsigned i = 0; i < SgLessThanOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLessThanOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLessThanOp::clearMemoryPool() {
    SgLessThanOp* tempPointer{nullptr};
    if (SgLessThanOp::pools.empty() == false) {
        auto block{SgLessThanOp::pools.begin()};
        SgLessThanOp::next_node = reinterpret_cast<SgLessThanOp*>(*block);
        for (; block != SgLessThanOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLessThanOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLessThanOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLessThanOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLessThanOp::pool_size-1]);
        }
    }
}

void
SgLessThanOp::deleteMemoryPool() {
    for (auto p: SgLessThanOp::pools) {
        ROSE_FREE(p);
    }
    SgLessThanOp::next_node = nullptr;
    SgLessThanOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLessThanOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLessThanOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLessThanOp)};
    for (size_t blockIndex = SgLessThanOp::pools.size(); (blockIndex * SgLessThanOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLessThanOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLessThanOp) = %" PRIuPTR " SgLessThanOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLessThanOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLessThanOp),SgLessThanOp::pool_size);
        }
#endif

        SgLessThanOp* pointer{static_cast<SgLessThanOp*>(ROSE_MALLOC(SgLessThanOp::pool_size * sizeof(SgLessThanOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLessThanOp::pool_size * sizeof(SgLessThanOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLessThanOp::pool_size * sizeof(SgLessThanOp));
#endif
        SgLessThanOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLessThanOp::pool_size * sizeof(SgLessThanOp), V_SgLessThanOp));

        if (SgLessThanOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLessThanOp* blkptr{reinterpret_cast<SgLessThanOp*>(SgLessThanOp::pools[blockIndex-1])};
                blkptr[SgLessThanOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLessThanOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLessThanOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLessThanOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLessThanOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLessThanOp::pool_size - 1};
    SgLessThanOp* testPointer{reinterpret_cast<SgLessThanOp*>(SgLessThanOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLessThanOp::pool_size * (SgLessThanOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLessThanOpStroageClass
 * from its counterpart of SgLessThanOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLessThanOp::initializeStorageClassArray(SgLessThanOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLessThanOp::pools.begin(); block != SgLessThanOp::pools.end(); ++block) {
        SgLessThanOp* pointer{reinterpret_cast<SgLessThanOp*>(*block)};
        for (unsigned i = 0; i < SgLessThanOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 109761 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGreaterThanOp that takes its corresponding StorageClass as parameter
 */
SgGreaterThanOp::SgGreaterThanOp(const SgGreaterThanOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 109771 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGreaterThanOp*
SgGreaterThanOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGreaterThanOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGreaterThanOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGreaterThanOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGreaterThanOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterThanOp)};
        unsigned long positionInPool{localIndex % SgGreaterThanOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGreaterThanOp::pool_size};
        returnPointer = &(((SgGreaterThanOp*)(SgGreaterThanOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGreaterThanOp*
SgGreaterThanOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGreaterThanOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGreaterThanOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGreaterThanOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGreaterThanOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGreaterThanOp)};
        unsigned long positionInPool{localIndex % SgGreaterThanOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGreaterThanOp::pool_size};
        returnPointer = &(((SgGreaterThanOp*)(SgGreaterThanOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGreaterThanOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGreaterThanOp::pools.begin(); block != SgGreaterThanOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGreaterThanOp*>(*block)};
        for (unsigned i = 0; i < SgGreaterThanOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGreaterThanOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGreaterThanOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGreaterThanOp* pointerOfLinkedList{nullptr};
    for (auto block = SgGreaterThanOp::pools.begin(); block != SgGreaterThanOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGreaterThanOp*>(*block)};
        for (unsigned i = 0; i < SgGreaterThanOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGreaterThanOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGreaterThanOp::clearMemoryPool() {
    SgGreaterThanOp* tempPointer{nullptr};
    if (SgGreaterThanOp::pools.empty() == false) {
        auto block{SgGreaterThanOp::pools.begin()};
        SgGreaterThanOp::next_node = reinterpret_cast<SgGreaterThanOp*>(*block);
        for (; block != SgGreaterThanOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGreaterThanOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGreaterThanOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGreaterThanOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGreaterThanOp::pool_size-1]);
        }
    }
}

void
SgGreaterThanOp::deleteMemoryPool() {
    for (auto p: SgGreaterThanOp::pools) {
        ROSE_FREE(p);
    }
    SgGreaterThanOp::next_node = nullptr;
    SgGreaterThanOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGreaterThanOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterThanOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGreaterThanOp)};
    for (size_t blockIndex = SgGreaterThanOp::pools.size(); (blockIndex * SgGreaterThanOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterThanOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGreaterThanOp) = %" PRIuPTR " SgGreaterThanOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterThanOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGreaterThanOp),SgGreaterThanOp::pool_size);
        }
#endif

        SgGreaterThanOp* pointer{static_cast<SgGreaterThanOp*>(ROSE_MALLOC(SgGreaterThanOp::pool_size * sizeof(SgGreaterThanOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGreaterThanOp::pool_size * sizeof(SgGreaterThanOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGreaterThanOp::pool_size * sizeof(SgGreaterThanOp));
#endif
        SgGreaterThanOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGreaterThanOp::pool_size * sizeof(SgGreaterThanOp), V_SgGreaterThanOp));

        if (SgGreaterThanOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGreaterThanOp* blkptr{reinterpret_cast<SgGreaterThanOp*>(SgGreaterThanOp::pools[blockIndex-1])};
                blkptr[SgGreaterThanOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGreaterThanOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGreaterThanOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGreaterThanOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGreaterThanOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGreaterThanOp::pool_size - 1};
    SgGreaterThanOp* testPointer{reinterpret_cast<SgGreaterThanOp*>(SgGreaterThanOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGreaterThanOp::pool_size * (SgGreaterThanOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGreaterThanOpStroageClass
 * from its counterpart of SgGreaterThanOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGreaterThanOp::initializeStorageClassArray(SgGreaterThanOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGreaterThanOp::pools.begin(); block != SgGreaterThanOp::pools.end(); ++block) {
        SgGreaterThanOp* pointer{reinterpret_cast<SgGreaterThanOp*>(*block)};
        for (unsigned i = 0; i < SgGreaterThanOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 109988 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNotEqualOp that takes its corresponding StorageClass as parameter
 */
SgNotEqualOp::SgNotEqualOp(const SgNotEqualOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 109998 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNotEqualOp*
SgNotEqualOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNotEqualOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNotEqualOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNotEqualOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNotEqualOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNotEqualOp)};
        unsigned long positionInPool{localIndex % SgNotEqualOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNotEqualOp::pool_size};
        returnPointer = &(((SgNotEqualOp*)(SgNotEqualOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNotEqualOp*
SgNotEqualOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNotEqualOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNotEqualOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNotEqualOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNotEqualOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNotEqualOp)};
        unsigned long positionInPool{localIndex % SgNotEqualOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNotEqualOp::pool_size};
        returnPointer = &(((SgNotEqualOp*)(SgNotEqualOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNotEqualOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNotEqualOp::pools.begin(); block != SgNotEqualOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNotEqualOp*>(*block)};
        for (unsigned i = 0; i < SgNotEqualOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNotEqualOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNotEqualOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNotEqualOp* pointerOfLinkedList{nullptr};
    for (auto block = SgNotEqualOp::pools.begin(); block != SgNotEqualOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNotEqualOp*>(*block)};
        for (unsigned i = 0; i < SgNotEqualOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNotEqualOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNotEqualOp::clearMemoryPool() {
    SgNotEqualOp* tempPointer{nullptr};
    if (SgNotEqualOp::pools.empty() == false) {
        auto block{SgNotEqualOp::pools.begin()};
        SgNotEqualOp::next_node = reinterpret_cast<SgNotEqualOp*>(*block);
        for (; block != SgNotEqualOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNotEqualOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNotEqualOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNotEqualOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNotEqualOp::pool_size-1]);
        }
    }
}

void
SgNotEqualOp::deleteMemoryPool() {
    for (auto p: SgNotEqualOp::pools) {
        ROSE_FREE(p);
    }
    SgNotEqualOp::next_node = nullptr;
    SgNotEqualOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNotEqualOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNotEqualOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNotEqualOp)};
    for (size_t blockIndex = SgNotEqualOp::pools.size(); (blockIndex * SgNotEqualOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNotEqualOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNotEqualOp) = %" PRIuPTR " SgNotEqualOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNotEqualOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNotEqualOp),SgNotEqualOp::pool_size);
        }
#endif

        SgNotEqualOp* pointer{static_cast<SgNotEqualOp*>(ROSE_MALLOC(SgNotEqualOp::pool_size * sizeof(SgNotEqualOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNotEqualOp::pool_size * sizeof(SgNotEqualOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNotEqualOp::pool_size * sizeof(SgNotEqualOp));
#endif
        SgNotEqualOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNotEqualOp::pool_size * sizeof(SgNotEqualOp), V_SgNotEqualOp));

        if (SgNotEqualOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNotEqualOp* blkptr{reinterpret_cast<SgNotEqualOp*>(SgNotEqualOp::pools[blockIndex-1])};
                blkptr[SgNotEqualOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNotEqualOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNotEqualOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNotEqualOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNotEqualOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNotEqualOp::pool_size - 1};
    SgNotEqualOp* testPointer{reinterpret_cast<SgNotEqualOp*>(SgNotEqualOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNotEqualOp::pool_size * (SgNotEqualOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNotEqualOpStroageClass
 * from its counterpart of SgNotEqualOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNotEqualOp::initializeStorageClassArray(SgNotEqualOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNotEqualOp::pools.begin(); block != SgNotEqualOp::pools.end(); ++block) {
        SgNotEqualOp* pointer{reinterpret_cast<SgNotEqualOp*>(*block)};
        for (unsigned i = 0; i < SgNotEqualOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 110215 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLessOrEqualOp that takes its corresponding StorageClass as parameter
 */
SgLessOrEqualOp::SgLessOrEqualOp(const SgLessOrEqualOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 110225 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLessOrEqualOp*
SgLessOrEqualOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLessOrEqualOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLessOrEqualOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLessOrEqualOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLessOrEqualOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLessOrEqualOp)};
        unsigned long positionInPool{localIndex % SgLessOrEqualOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLessOrEqualOp::pool_size};
        returnPointer = &(((SgLessOrEqualOp*)(SgLessOrEqualOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLessOrEqualOp*
SgLessOrEqualOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLessOrEqualOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLessOrEqualOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLessOrEqualOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLessOrEqualOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLessOrEqualOp)};
        unsigned long positionInPool{localIndex % SgLessOrEqualOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLessOrEqualOp::pool_size};
        returnPointer = &(((SgLessOrEqualOp*)(SgLessOrEqualOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLessOrEqualOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLessOrEqualOp::pools.begin(); block != SgLessOrEqualOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLessOrEqualOp*>(*block)};
        for (unsigned i = 0; i < SgLessOrEqualOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLessOrEqualOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLessOrEqualOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLessOrEqualOp* pointerOfLinkedList{nullptr};
    for (auto block = SgLessOrEqualOp::pools.begin(); block != SgLessOrEqualOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLessOrEqualOp*>(*block)};
        for (unsigned i = 0; i < SgLessOrEqualOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLessOrEqualOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLessOrEqualOp::clearMemoryPool() {
    SgLessOrEqualOp* tempPointer{nullptr};
    if (SgLessOrEqualOp::pools.empty() == false) {
        auto block{SgLessOrEqualOp::pools.begin()};
        SgLessOrEqualOp::next_node = reinterpret_cast<SgLessOrEqualOp*>(*block);
        for (; block != SgLessOrEqualOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLessOrEqualOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLessOrEqualOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLessOrEqualOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLessOrEqualOp::pool_size-1]);
        }
    }
}

void
SgLessOrEqualOp::deleteMemoryPool() {
    for (auto p: SgLessOrEqualOp::pools) {
        ROSE_FREE(p);
    }
    SgLessOrEqualOp::next_node = nullptr;
    SgLessOrEqualOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLessOrEqualOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLessOrEqualOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLessOrEqualOp)};
    for (size_t blockIndex = SgLessOrEqualOp::pools.size(); (blockIndex * SgLessOrEqualOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLessOrEqualOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLessOrEqualOp) = %" PRIuPTR " SgLessOrEqualOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLessOrEqualOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLessOrEqualOp),SgLessOrEqualOp::pool_size);
        }
#endif

        SgLessOrEqualOp* pointer{static_cast<SgLessOrEqualOp*>(ROSE_MALLOC(SgLessOrEqualOp::pool_size * sizeof(SgLessOrEqualOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLessOrEqualOp::pool_size * sizeof(SgLessOrEqualOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLessOrEqualOp::pool_size * sizeof(SgLessOrEqualOp));
#endif
        SgLessOrEqualOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLessOrEqualOp::pool_size * sizeof(SgLessOrEqualOp), V_SgLessOrEqualOp));

        if (SgLessOrEqualOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLessOrEqualOp* blkptr{reinterpret_cast<SgLessOrEqualOp*>(SgLessOrEqualOp::pools[blockIndex-1])};
                blkptr[SgLessOrEqualOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLessOrEqualOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLessOrEqualOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLessOrEqualOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLessOrEqualOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLessOrEqualOp::pool_size - 1};
    SgLessOrEqualOp* testPointer{reinterpret_cast<SgLessOrEqualOp*>(SgLessOrEqualOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLessOrEqualOp::pool_size * (SgLessOrEqualOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLessOrEqualOpStroageClass
 * from its counterpart of SgLessOrEqualOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLessOrEqualOp::initializeStorageClassArray(SgLessOrEqualOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLessOrEqualOp::pools.begin(); block != SgLessOrEqualOp::pools.end(); ++block) {
        SgLessOrEqualOp* pointer{reinterpret_cast<SgLessOrEqualOp*>(*block)};
        for (unsigned i = 0; i < SgLessOrEqualOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 110442 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgGreaterOrEqualOp that takes its corresponding StorageClass as parameter
 */
SgGreaterOrEqualOp::SgGreaterOrEqualOp(const SgGreaterOrEqualOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 110452 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgGreaterOrEqualOp*
SgGreaterOrEqualOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgGreaterOrEqualOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGreaterOrEqualOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGreaterOrEqualOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgGreaterOrEqualOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterOrEqualOp)};
        unsigned long positionInPool{localIndex % SgGreaterOrEqualOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGreaterOrEqualOp::pool_size};
        returnPointer = &(((SgGreaterOrEqualOp*)(SgGreaterOrEqualOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgGreaterOrEqualOp*
SgGreaterOrEqualOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgGreaterOrEqualOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGreaterOrEqualOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGreaterOrEqualOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgGreaterOrEqualOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgGreaterOrEqualOp)};
        unsigned long positionInPool{localIndex % SgGreaterOrEqualOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgGreaterOrEqualOp::pool_size};
        returnPointer = &(((SgGreaterOrEqualOp*)(SgGreaterOrEqualOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgGreaterOrEqualOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgGreaterOrEqualOp::pools.begin(); block != SgGreaterOrEqualOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGreaterOrEqualOp*>(*block)};
        for (unsigned i = 0; i < SgGreaterOrEqualOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgGreaterOrEqualOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgGreaterOrEqualOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgGreaterOrEqualOp* pointerOfLinkedList{nullptr};
    for (auto block = SgGreaterOrEqualOp::pools.begin(); block != SgGreaterOrEqualOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgGreaterOrEqualOp*>(*block)};
        for (unsigned i = 0; i < SgGreaterOrEqualOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgGreaterOrEqualOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgGreaterOrEqualOp::clearMemoryPool() {
    SgGreaterOrEqualOp* tempPointer{nullptr};
    if (SgGreaterOrEqualOp::pools.empty() == false) {
        auto block{SgGreaterOrEqualOp::pools.begin()};
        SgGreaterOrEqualOp::next_node = reinterpret_cast<SgGreaterOrEqualOp*>(*block);
        for (; block != SgGreaterOrEqualOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgGreaterOrEqualOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgGreaterOrEqualOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgGreaterOrEqualOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgGreaterOrEqualOp::pool_size-1]);
        }
    }
}

void
SgGreaterOrEqualOp::deleteMemoryPool() {
    for (auto p: SgGreaterOrEqualOp::pools) {
        ROSE_FREE(p);
    }
    SgGreaterOrEqualOp::next_node = nullptr;
    SgGreaterOrEqualOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgGreaterOrEqualOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterOrEqualOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgGreaterOrEqualOp)};
    for (size_t blockIndex = SgGreaterOrEqualOp::pools.size(); (blockIndex * SgGreaterOrEqualOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterOrEqualOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgGreaterOrEqualOp) = %" PRIuPTR " SgGreaterOrEqualOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgGreaterOrEqualOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgGreaterOrEqualOp),SgGreaterOrEqualOp::pool_size);
        }
#endif

        SgGreaterOrEqualOp* pointer{static_cast<SgGreaterOrEqualOp*>(ROSE_MALLOC(SgGreaterOrEqualOp::pool_size * sizeof(SgGreaterOrEqualOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgGreaterOrEqualOp::pool_size * sizeof(SgGreaterOrEqualOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgGreaterOrEqualOp::pool_size * sizeof(SgGreaterOrEqualOp));
#endif
        SgGreaterOrEqualOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgGreaterOrEqualOp::pool_size * sizeof(SgGreaterOrEqualOp), V_SgGreaterOrEqualOp));

        if (SgGreaterOrEqualOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgGreaterOrEqualOp* blkptr{reinterpret_cast<SgGreaterOrEqualOp*>(SgGreaterOrEqualOp::pools[blockIndex-1])};
                blkptr[SgGreaterOrEqualOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgGreaterOrEqualOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgGreaterOrEqualOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgGreaterOrEqualOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgGreaterOrEqualOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgGreaterOrEqualOp::pool_size - 1};
    SgGreaterOrEqualOp* testPointer{reinterpret_cast<SgGreaterOrEqualOp*>(SgGreaterOrEqualOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgGreaterOrEqualOp::pool_size * (SgGreaterOrEqualOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgGreaterOrEqualOpStroageClass
 * from its counterpart of SgGreaterOrEqualOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgGreaterOrEqualOp::initializeStorageClassArray(SgGreaterOrEqualOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgGreaterOrEqualOp::pools.begin(); block != SgGreaterOrEqualOp::pools.end(); ++block) {
        SgGreaterOrEqualOp* pointer{reinterpret_cast<SgGreaterOrEqualOp*>(*block)};
        for (unsigned i = 0; i < SgGreaterOrEqualOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 110669 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAddOp that takes its corresponding StorageClass as parameter
 */
SgAddOp::SgAddOp(const SgAddOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 110679 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAddOp*
SgAddOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAddOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAddOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAddOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAddOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAddOp)};
        unsigned long positionInPool{localIndex % SgAddOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAddOp::pool_size};
        returnPointer = &(((SgAddOp*)(SgAddOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAddOp*
SgAddOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAddOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAddOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAddOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAddOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAddOp)};
        unsigned long positionInPool{localIndex % SgAddOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAddOp::pool_size};
        returnPointer = &(((SgAddOp*)(SgAddOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAddOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAddOp::pools.begin(); block != SgAddOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAddOp*>(*block)};
        for (unsigned i = 0; i < SgAddOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAddOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAddOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAddOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAddOp::pools.begin(); block != SgAddOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAddOp*>(*block)};
        for (unsigned i = 0; i < SgAddOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAddOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAddOp::clearMemoryPool() {
    SgAddOp* tempPointer{nullptr};
    if (SgAddOp::pools.empty() == false) {
        auto block{SgAddOp::pools.begin()};
        SgAddOp::next_node = reinterpret_cast<SgAddOp*>(*block);
        for (; block != SgAddOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAddOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAddOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAddOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAddOp::pool_size-1]);
        }
    }
}

void
SgAddOp::deleteMemoryPool() {
    for (auto p: SgAddOp::pools) {
        ROSE_FREE(p);
    }
    SgAddOp::next_node = nullptr;
    SgAddOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAddOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAddOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAddOp)};
    for (size_t blockIndex = SgAddOp::pools.size(); (blockIndex * SgAddOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAddOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAddOp) = %" PRIuPTR " SgAddOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAddOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAddOp),SgAddOp::pool_size);
        }
#endif

        SgAddOp* pointer{static_cast<SgAddOp*>(ROSE_MALLOC(SgAddOp::pool_size * sizeof(SgAddOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAddOp::pool_size * sizeof(SgAddOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAddOp::pool_size * sizeof(SgAddOp));
#endif
        SgAddOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAddOp::pool_size * sizeof(SgAddOp), V_SgAddOp));

        if (SgAddOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAddOp* blkptr{reinterpret_cast<SgAddOp*>(SgAddOp::pools[blockIndex-1])};
                blkptr[SgAddOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAddOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAddOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAddOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAddOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAddOp::pool_size - 1};
    SgAddOp* testPointer{reinterpret_cast<SgAddOp*>(SgAddOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAddOp::pool_size * (SgAddOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAddOpStroageClass
 * from its counterpart of SgAddOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAddOp::initializeStorageClassArray(SgAddOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAddOp::pools.begin(); block != SgAddOp::pools.end(); ++block) {
        SgAddOp* pointer{reinterpret_cast<SgAddOp*>(*block)};
        for (unsigned i = 0; i < SgAddOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 110896 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSubtractOp that takes its corresponding StorageClass as parameter
 */
SgSubtractOp::SgSubtractOp(const SgSubtractOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 110906 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSubtractOp*
SgSubtractOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSubtractOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSubtractOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSubtractOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSubtractOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSubtractOp)};
        unsigned long positionInPool{localIndex % SgSubtractOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSubtractOp::pool_size};
        returnPointer = &(((SgSubtractOp*)(SgSubtractOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSubtractOp*
SgSubtractOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSubtractOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSubtractOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSubtractOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSubtractOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSubtractOp)};
        unsigned long positionInPool{localIndex % SgSubtractOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSubtractOp::pool_size};
        returnPointer = &(((SgSubtractOp*)(SgSubtractOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSubtractOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSubtractOp::pools.begin(); block != SgSubtractOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSubtractOp*>(*block)};
        for (unsigned i = 0; i < SgSubtractOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSubtractOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSubtractOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSubtractOp* pointerOfLinkedList{nullptr};
    for (auto block = SgSubtractOp::pools.begin(); block != SgSubtractOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSubtractOp*>(*block)};
        for (unsigned i = 0; i < SgSubtractOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSubtractOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSubtractOp::clearMemoryPool() {
    SgSubtractOp* tempPointer{nullptr};
    if (SgSubtractOp::pools.empty() == false) {
        auto block{SgSubtractOp::pools.begin()};
        SgSubtractOp::next_node = reinterpret_cast<SgSubtractOp*>(*block);
        for (; block != SgSubtractOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSubtractOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSubtractOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSubtractOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSubtractOp::pool_size-1]);
        }
    }
}

void
SgSubtractOp::deleteMemoryPool() {
    for (auto p: SgSubtractOp::pools) {
        ROSE_FREE(p);
    }
    SgSubtractOp::next_node = nullptr;
    SgSubtractOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSubtractOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSubtractOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSubtractOp)};
    for (size_t blockIndex = SgSubtractOp::pools.size(); (blockIndex * SgSubtractOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSubtractOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSubtractOp) = %" PRIuPTR " SgSubtractOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSubtractOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSubtractOp),SgSubtractOp::pool_size);
        }
#endif

        SgSubtractOp* pointer{static_cast<SgSubtractOp*>(ROSE_MALLOC(SgSubtractOp::pool_size * sizeof(SgSubtractOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSubtractOp::pool_size * sizeof(SgSubtractOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSubtractOp::pool_size * sizeof(SgSubtractOp));
#endif
        SgSubtractOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSubtractOp::pool_size * sizeof(SgSubtractOp), V_SgSubtractOp));

        if (SgSubtractOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSubtractOp* blkptr{reinterpret_cast<SgSubtractOp*>(SgSubtractOp::pools[blockIndex-1])};
                blkptr[SgSubtractOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSubtractOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSubtractOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSubtractOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSubtractOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSubtractOp::pool_size - 1};
    SgSubtractOp* testPointer{reinterpret_cast<SgSubtractOp*>(SgSubtractOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSubtractOp::pool_size * (SgSubtractOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSubtractOpStroageClass
 * from its counterpart of SgSubtractOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSubtractOp::initializeStorageClassArray(SgSubtractOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSubtractOp::pools.begin(); block != SgSubtractOp::pools.end(); ++block) {
        SgSubtractOp* pointer{reinterpret_cast<SgSubtractOp*>(*block)};
        for (unsigned i = 0; i < SgSubtractOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 111123 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMultiplyOp that takes its corresponding StorageClass as parameter
 */
SgMultiplyOp::SgMultiplyOp(const SgMultiplyOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 111133 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMultiplyOp*
SgMultiplyOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMultiplyOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMultiplyOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMultiplyOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMultiplyOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMultiplyOp)};
        unsigned long positionInPool{localIndex % SgMultiplyOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMultiplyOp::pool_size};
        returnPointer = &(((SgMultiplyOp*)(SgMultiplyOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMultiplyOp*
SgMultiplyOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMultiplyOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMultiplyOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMultiplyOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMultiplyOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMultiplyOp)};
        unsigned long positionInPool{localIndex % SgMultiplyOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMultiplyOp::pool_size};
        returnPointer = &(((SgMultiplyOp*)(SgMultiplyOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMultiplyOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMultiplyOp::pools.begin(); block != SgMultiplyOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMultiplyOp*>(*block)};
        for (unsigned i = 0; i < SgMultiplyOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMultiplyOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMultiplyOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMultiplyOp* pointerOfLinkedList{nullptr};
    for (auto block = SgMultiplyOp::pools.begin(); block != SgMultiplyOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMultiplyOp*>(*block)};
        for (unsigned i = 0; i < SgMultiplyOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMultiplyOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMultiplyOp::clearMemoryPool() {
    SgMultiplyOp* tempPointer{nullptr};
    if (SgMultiplyOp::pools.empty() == false) {
        auto block{SgMultiplyOp::pools.begin()};
        SgMultiplyOp::next_node = reinterpret_cast<SgMultiplyOp*>(*block);
        for (; block != SgMultiplyOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMultiplyOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMultiplyOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMultiplyOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMultiplyOp::pool_size-1]);
        }
    }
}

void
SgMultiplyOp::deleteMemoryPool() {
    for (auto p: SgMultiplyOp::pools) {
        ROSE_FREE(p);
    }
    SgMultiplyOp::next_node = nullptr;
    SgMultiplyOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMultiplyOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMultiplyOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMultiplyOp)};
    for (size_t blockIndex = SgMultiplyOp::pools.size(); (blockIndex * SgMultiplyOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMultiplyOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMultiplyOp) = %" PRIuPTR " SgMultiplyOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMultiplyOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMultiplyOp),SgMultiplyOp::pool_size);
        }
#endif

        SgMultiplyOp* pointer{static_cast<SgMultiplyOp*>(ROSE_MALLOC(SgMultiplyOp::pool_size * sizeof(SgMultiplyOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMultiplyOp::pool_size * sizeof(SgMultiplyOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMultiplyOp::pool_size * sizeof(SgMultiplyOp));
#endif
        SgMultiplyOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMultiplyOp::pool_size * sizeof(SgMultiplyOp), V_SgMultiplyOp));

        if (SgMultiplyOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMultiplyOp* blkptr{reinterpret_cast<SgMultiplyOp*>(SgMultiplyOp::pools[blockIndex-1])};
                blkptr[SgMultiplyOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMultiplyOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMultiplyOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMultiplyOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMultiplyOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMultiplyOp::pool_size - 1};
    SgMultiplyOp* testPointer{reinterpret_cast<SgMultiplyOp*>(SgMultiplyOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMultiplyOp::pool_size * (SgMultiplyOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMultiplyOpStroageClass
 * from its counterpart of SgMultiplyOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMultiplyOp::initializeStorageClassArray(SgMultiplyOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMultiplyOp::pools.begin(); block != SgMultiplyOp::pools.end(); ++block) {
        SgMultiplyOp* pointer{reinterpret_cast<SgMultiplyOp*>(*block)};
        for (unsigned i = 0; i < SgMultiplyOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 111350 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDivideOp that takes its corresponding StorageClass as parameter
 */
SgDivideOp::SgDivideOp(const SgDivideOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 111360 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDivideOp*
SgDivideOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDivideOp)};
        unsigned long positionInPool{localIndex % SgDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDivideOp::pool_size};
        returnPointer = &(((SgDivideOp*)(SgDivideOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDivideOp*
SgDivideOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDivideOp)};
        unsigned long positionInPool{localIndex % SgDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDivideOp::pool_size};
        returnPointer = &(((SgDivideOp*)(SgDivideOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDivideOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDivideOp::pools.begin(); block != SgDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDivideOp*>(*block)};
        for (unsigned i = 0; i < SgDivideOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDivideOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDivideOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDivideOp* pointerOfLinkedList{nullptr};
    for (auto block = SgDivideOp::pools.begin(); block != SgDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDivideOp*>(*block)};
        for (unsigned i = 0; i < SgDivideOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDivideOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDivideOp::clearMemoryPool() {
    SgDivideOp* tempPointer{nullptr};
    if (SgDivideOp::pools.empty() == false) {
        auto block{SgDivideOp::pools.begin()};
        SgDivideOp::next_node = reinterpret_cast<SgDivideOp*>(*block);
        for (; block != SgDivideOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDivideOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDivideOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDivideOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDivideOp::pool_size-1]);
        }
    }
}

void
SgDivideOp::deleteMemoryPool() {
    for (auto p: SgDivideOp::pools) {
        ROSE_FREE(p);
    }
    SgDivideOp::next_node = nullptr;
    SgDivideOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDivideOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDivideOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDivideOp)};
    for (size_t blockIndex = SgDivideOp::pools.size(); (blockIndex * SgDivideOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDivideOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDivideOp) = %" PRIuPTR " SgDivideOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDivideOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDivideOp),SgDivideOp::pool_size);
        }
#endif

        SgDivideOp* pointer{static_cast<SgDivideOp*>(ROSE_MALLOC(SgDivideOp::pool_size * sizeof(SgDivideOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDivideOp::pool_size * sizeof(SgDivideOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDivideOp::pool_size * sizeof(SgDivideOp));
#endif
        SgDivideOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDivideOp::pool_size * sizeof(SgDivideOp), V_SgDivideOp));

        if (SgDivideOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDivideOp* blkptr{reinterpret_cast<SgDivideOp*>(SgDivideOp::pools[blockIndex-1])};
                blkptr[SgDivideOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDivideOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDivideOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDivideOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDivideOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDivideOp::pool_size - 1};
    SgDivideOp* testPointer{reinterpret_cast<SgDivideOp*>(SgDivideOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDivideOp::pool_size * (SgDivideOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDivideOpStroageClass
 * from its counterpart of SgDivideOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDivideOp::initializeStorageClassArray(SgDivideOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDivideOp::pools.begin(); block != SgDivideOp::pools.end(); ++block) {
        SgDivideOp* pointer{reinterpret_cast<SgDivideOp*>(*block)};
        for (unsigned i = 0; i < SgDivideOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 111577 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIntegerDivideOp that takes its corresponding StorageClass as parameter
 */
SgIntegerDivideOp::SgIntegerDivideOp(const SgIntegerDivideOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 111587 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIntegerDivideOp*
SgIntegerDivideOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIntegerDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegerDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegerDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegerDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideOp)};
        unsigned long positionInPool{localIndex % SgIntegerDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntegerDivideOp::pool_size};
        returnPointer = &(((SgIntegerDivideOp*)(SgIntegerDivideOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIntegerDivideOp*
SgIntegerDivideOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIntegerDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegerDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegerDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegerDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIntegerDivideOp)};
        unsigned long positionInPool{localIndex % SgIntegerDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntegerDivideOp::pool_size};
        returnPointer = &(((SgIntegerDivideOp*)(SgIntegerDivideOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIntegerDivideOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIntegerDivideOp::pools.begin(); block != SgIntegerDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntegerDivideOp*>(*block)};
        for (unsigned i = 0; i < SgIntegerDivideOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIntegerDivideOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIntegerDivideOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIntegerDivideOp* pointerOfLinkedList{nullptr};
    for (auto block = SgIntegerDivideOp::pools.begin(); block != SgIntegerDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntegerDivideOp*>(*block)};
        for (unsigned i = 0; i < SgIntegerDivideOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIntegerDivideOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIntegerDivideOp::clearMemoryPool() {
    SgIntegerDivideOp* tempPointer{nullptr};
    if (SgIntegerDivideOp::pools.empty() == false) {
        auto block{SgIntegerDivideOp::pools.begin()};
        SgIntegerDivideOp::next_node = reinterpret_cast<SgIntegerDivideOp*>(*block);
        for (; block != SgIntegerDivideOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIntegerDivideOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIntegerDivideOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIntegerDivideOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIntegerDivideOp::pool_size-1]);
        }
    }
}

void
SgIntegerDivideOp::deleteMemoryPool() {
    for (auto p: SgIntegerDivideOp::pools) {
        ROSE_FREE(p);
    }
    SgIntegerDivideOp::next_node = nullptr;
    SgIntegerDivideOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIntegerDivideOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegerDivideOp)};
    for (size_t blockIndex = SgIntegerDivideOp::pools.size(); (blockIndex * SgIntegerDivideOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegerDivideOp) = %" PRIuPTR " SgIntegerDivideOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegerDivideOp),SgIntegerDivideOp::pool_size);
        }
#endif

        SgIntegerDivideOp* pointer{static_cast<SgIntegerDivideOp*>(ROSE_MALLOC(SgIntegerDivideOp::pool_size * sizeof(SgIntegerDivideOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIntegerDivideOp::pool_size * sizeof(SgIntegerDivideOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIntegerDivideOp::pool_size * sizeof(SgIntegerDivideOp));
#endif
        SgIntegerDivideOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIntegerDivideOp::pool_size * sizeof(SgIntegerDivideOp), V_SgIntegerDivideOp));

        if (SgIntegerDivideOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIntegerDivideOp* blkptr{reinterpret_cast<SgIntegerDivideOp*>(SgIntegerDivideOp::pools[blockIndex-1])};
                blkptr[SgIntegerDivideOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIntegerDivideOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIntegerDivideOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIntegerDivideOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIntegerDivideOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIntegerDivideOp::pool_size - 1};
    SgIntegerDivideOp* testPointer{reinterpret_cast<SgIntegerDivideOp*>(SgIntegerDivideOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIntegerDivideOp::pool_size * (SgIntegerDivideOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIntegerDivideOpStroageClass
 * from its counterpart of SgIntegerDivideOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIntegerDivideOp::initializeStorageClassArray(SgIntegerDivideOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIntegerDivideOp::pools.begin(); block != SgIntegerDivideOp::pools.end(); ++block) {
        SgIntegerDivideOp* pointer{reinterpret_cast<SgIntegerDivideOp*>(*block)};
        for (unsigned i = 0; i < SgIntegerDivideOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 111804 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgModOp that takes its corresponding StorageClass as parameter
 */
SgModOp::SgModOp(const SgModOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 111814 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgModOp*
SgModOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgModOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgModOp)};
        unsigned long positionInPool{localIndex % SgModOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModOp::pool_size};
        returnPointer = &(((SgModOp*)(SgModOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgModOp*
SgModOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgModOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgModOp)};
        unsigned long positionInPool{localIndex % SgModOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModOp::pool_size};
        returnPointer = &(((SgModOp*)(SgModOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgModOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgModOp::pools.begin(); block != SgModOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModOp*>(*block)};
        for (unsigned i = 0; i < SgModOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgModOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgModOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgModOp* pointerOfLinkedList{nullptr};
    for (auto block = SgModOp::pools.begin(); block != SgModOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModOp*>(*block)};
        for (unsigned i = 0; i < SgModOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgModOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgModOp::clearMemoryPool() {
    SgModOp* tempPointer{nullptr};
    if (SgModOp::pools.empty() == false) {
        auto block{SgModOp::pools.begin()};
        SgModOp::next_node = reinterpret_cast<SgModOp*>(*block);
        for (; block != SgModOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgModOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgModOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgModOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgModOp::pool_size-1]);
        }
    }
}

void
SgModOp::deleteMemoryPool() {
    for (auto p: SgModOp::pools) {
        ROSE_FREE(p);
    }
    SgModOp::next_node = nullptr;
    SgModOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgModOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgModOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgModOp)};
    for (size_t blockIndex = SgModOp::pools.size(); (blockIndex * SgModOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgModOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgModOp) = %" PRIuPTR " SgModOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgModOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgModOp),SgModOp::pool_size);
        }
#endif

        SgModOp* pointer{static_cast<SgModOp*>(ROSE_MALLOC(SgModOp::pool_size * sizeof(SgModOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgModOp::pool_size * sizeof(SgModOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgModOp::pool_size * sizeof(SgModOp));
#endif
        SgModOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgModOp::pool_size * sizeof(SgModOp), V_SgModOp));

        if (SgModOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgModOp* blkptr{reinterpret_cast<SgModOp*>(SgModOp::pools[blockIndex-1])};
                blkptr[SgModOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgModOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgModOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgModOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgModOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgModOp::pool_size - 1};
    SgModOp* testPointer{reinterpret_cast<SgModOp*>(SgModOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgModOp::pool_size * (SgModOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgModOpStroageClass
 * from its counterpart of SgModOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgModOp::initializeStorageClassArray(SgModOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgModOp::pools.begin(); block != SgModOp::pools.end(); ++block) {
        SgModOp* pointer{reinterpret_cast<SgModOp*>(*block)};
        for (unsigned i = 0; i < SgModOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 112031 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAndOp that takes its corresponding StorageClass as parameter
 */
SgAndOp::SgAndOp(const SgAndOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 112041 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAndOp*
SgAndOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAndOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAndOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAndOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAndOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAndOp)};
        unsigned long positionInPool{localIndex % SgAndOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAndOp::pool_size};
        returnPointer = &(((SgAndOp*)(SgAndOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAndOp*
SgAndOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAndOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAndOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAndOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAndOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAndOp)};
        unsigned long positionInPool{localIndex % SgAndOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAndOp::pool_size};
        returnPointer = &(((SgAndOp*)(SgAndOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAndOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAndOp::pools.begin(); block != SgAndOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAndOp*>(*block)};
        for (unsigned i = 0; i < SgAndOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAndOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAndOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAndOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAndOp::pools.begin(); block != SgAndOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAndOp*>(*block)};
        for (unsigned i = 0; i < SgAndOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAndOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAndOp::clearMemoryPool() {
    SgAndOp* tempPointer{nullptr};
    if (SgAndOp::pools.empty() == false) {
        auto block{SgAndOp::pools.begin()};
        SgAndOp::next_node = reinterpret_cast<SgAndOp*>(*block);
        for (; block != SgAndOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAndOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAndOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAndOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAndOp::pool_size-1]);
        }
    }
}

void
SgAndOp::deleteMemoryPool() {
    for (auto p: SgAndOp::pools) {
        ROSE_FREE(p);
    }
    SgAndOp::next_node = nullptr;
    SgAndOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAndOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAndOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAndOp)};
    for (size_t blockIndex = SgAndOp::pools.size(); (blockIndex * SgAndOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAndOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAndOp) = %" PRIuPTR " SgAndOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAndOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAndOp),SgAndOp::pool_size);
        }
#endif

        SgAndOp* pointer{static_cast<SgAndOp*>(ROSE_MALLOC(SgAndOp::pool_size * sizeof(SgAndOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAndOp::pool_size * sizeof(SgAndOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAndOp::pool_size * sizeof(SgAndOp));
#endif
        SgAndOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAndOp::pool_size * sizeof(SgAndOp), V_SgAndOp));

        if (SgAndOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAndOp* blkptr{reinterpret_cast<SgAndOp*>(SgAndOp::pools[blockIndex-1])};
                blkptr[SgAndOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAndOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAndOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAndOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAndOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAndOp::pool_size - 1};
    SgAndOp* testPointer{reinterpret_cast<SgAndOp*>(SgAndOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAndOp::pool_size * (SgAndOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAndOpStroageClass
 * from its counterpart of SgAndOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAndOp::initializeStorageClassArray(SgAndOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAndOp::pools.begin(); block != SgAndOp::pools.end(); ++block) {
        SgAndOp* pointer{reinterpret_cast<SgAndOp*>(*block)};
        for (unsigned i = 0; i < SgAndOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 112258 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgOrOp that takes its corresponding StorageClass as parameter
 */
SgOrOp::SgOrOp(const SgOrOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 112268 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgOrOp*
SgOrOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgOrOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOrOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOrOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgOrOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgOrOp)};
        unsigned long positionInPool{localIndex % SgOrOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOrOp::pool_size};
        returnPointer = &(((SgOrOp*)(SgOrOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgOrOp*
SgOrOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgOrOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOrOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOrOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgOrOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgOrOp)};
        unsigned long positionInPool{localIndex % SgOrOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgOrOp::pool_size};
        returnPointer = &(((SgOrOp*)(SgOrOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgOrOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgOrOp::pools.begin(); block != SgOrOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOrOp*>(*block)};
        for (unsigned i = 0; i < SgOrOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgOrOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgOrOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgOrOp* pointerOfLinkedList{nullptr};
    for (auto block = SgOrOp::pools.begin(); block != SgOrOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgOrOp*>(*block)};
        for (unsigned i = 0; i < SgOrOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgOrOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgOrOp::clearMemoryPool() {
    SgOrOp* tempPointer{nullptr};
    if (SgOrOp::pools.empty() == false) {
        auto block{SgOrOp::pools.begin()};
        SgOrOp::next_node = reinterpret_cast<SgOrOp*>(*block);
        for (; block != SgOrOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgOrOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgOrOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgOrOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgOrOp::pool_size-1]);
        }
    }
}

void
SgOrOp::deleteMemoryPool() {
    for (auto p: SgOrOp::pools) {
        ROSE_FREE(p);
    }
    SgOrOp::next_node = nullptr;
    SgOrOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgOrOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgOrOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgOrOp)};
    for (size_t blockIndex = SgOrOp::pools.size(); (blockIndex * SgOrOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgOrOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgOrOp) = %" PRIuPTR " SgOrOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgOrOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgOrOp),SgOrOp::pool_size);
        }
#endif

        SgOrOp* pointer{static_cast<SgOrOp*>(ROSE_MALLOC(SgOrOp::pool_size * sizeof(SgOrOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgOrOp::pool_size * sizeof(SgOrOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgOrOp::pool_size * sizeof(SgOrOp));
#endif
        SgOrOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgOrOp::pool_size * sizeof(SgOrOp), V_SgOrOp));

        if (SgOrOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgOrOp* blkptr{reinterpret_cast<SgOrOp*>(SgOrOp::pools[blockIndex-1])};
                blkptr[SgOrOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgOrOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgOrOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgOrOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgOrOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgOrOp::pool_size - 1};
    SgOrOp* testPointer{reinterpret_cast<SgOrOp*>(SgOrOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgOrOp::pool_size * (SgOrOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgOrOpStroageClass
 * from its counterpart of SgOrOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgOrOp::initializeStorageClassArray(SgOrOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgOrOp::pools.begin(); block != SgOrOp::pools.end(); ++block) {
        SgOrOp* pointer{reinterpret_cast<SgOrOp*>(*block)};
        for (unsigned i = 0; i < SgOrOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 112485 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBitXorOp that takes its corresponding StorageClass as parameter
 */
SgBitXorOp::SgBitXorOp(const SgBitXorOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 112495 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBitXorOp*
SgBitXorOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBitXorOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitXorOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitXorOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitXorOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBitXorOp)};
        unsigned long positionInPool{localIndex % SgBitXorOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitXorOp::pool_size};
        returnPointer = &(((SgBitXorOp*)(SgBitXorOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBitXorOp*
SgBitXorOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBitXorOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitXorOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitXorOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitXorOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBitXorOp)};
        unsigned long positionInPool{localIndex % SgBitXorOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitXorOp::pool_size};
        returnPointer = &(((SgBitXorOp*)(SgBitXorOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBitXorOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBitXorOp::pools.begin(); block != SgBitXorOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitXorOp*>(*block)};
        for (unsigned i = 0; i < SgBitXorOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBitXorOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBitXorOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBitXorOp* pointerOfLinkedList{nullptr};
    for (auto block = SgBitXorOp::pools.begin(); block != SgBitXorOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitXorOp*>(*block)};
        for (unsigned i = 0; i < SgBitXorOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBitXorOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBitXorOp::clearMemoryPool() {
    SgBitXorOp* tempPointer{nullptr};
    if (SgBitXorOp::pools.empty() == false) {
        auto block{SgBitXorOp::pools.begin()};
        SgBitXorOp::next_node = reinterpret_cast<SgBitXorOp*>(*block);
        for (; block != SgBitXorOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBitXorOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBitXorOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBitXorOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBitXorOp::pool_size-1]);
        }
    }
}

void
SgBitXorOp::deleteMemoryPool() {
    for (auto p: SgBitXorOp::pools) {
        ROSE_FREE(p);
    }
    SgBitXorOp::next_node = nullptr;
    SgBitXorOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBitXorOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBitXorOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitXorOp)};
    for (size_t blockIndex = SgBitXorOp::pools.size(); (blockIndex * SgBitXorOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBitXorOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitXorOp) = %" PRIuPTR " SgBitXorOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBitXorOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitXorOp),SgBitXorOp::pool_size);
        }
#endif

        SgBitXorOp* pointer{static_cast<SgBitXorOp*>(ROSE_MALLOC(SgBitXorOp::pool_size * sizeof(SgBitXorOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBitXorOp::pool_size * sizeof(SgBitXorOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBitXorOp::pool_size * sizeof(SgBitXorOp));
#endif
        SgBitXorOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBitXorOp::pool_size * sizeof(SgBitXorOp), V_SgBitXorOp));

        if (SgBitXorOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBitXorOp* blkptr{reinterpret_cast<SgBitXorOp*>(SgBitXorOp::pools[blockIndex-1])};
                blkptr[SgBitXorOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBitXorOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBitXorOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBitXorOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBitXorOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBitXorOp::pool_size - 1};
    SgBitXorOp* testPointer{reinterpret_cast<SgBitXorOp*>(SgBitXorOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBitXorOp::pool_size * (SgBitXorOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBitXorOpStroageClass
 * from its counterpart of SgBitXorOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBitXorOp::initializeStorageClassArray(SgBitXorOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBitXorOp::pools.begin(); block != SgBitXorOp::pools.end(); ++block) {
        SgBitXorOp* pointer{reinterpret_cast<SgBitXorOp*>(*block)};
        for (unsigned i = 0; i < SgBitXorOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 112712 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBitAndOp that takes its corresponding StorageClass as parameter
 */
SgBitAndOp::SgBitAndOp(const SgBitAndOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 112722 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBitAndOp*
SgBitAndOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBitAndOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitAndOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitAndOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitAndOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAndOp)};
        unsigned long positionInPool{localIndex % SgBitAndOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitAndOp::pool_size};
        returnPointer = &(((SgBitAndOp*)(SgBitAndOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBitAndOp*
SgBitAndOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBitAndOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitAndOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitAndOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitAndOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBitAndOp)};
        unsigned long positionInPool{localIndex % SgBitAndOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitAndOp::pool_size};
        returnPointer = &(((SgBitAndOp*)(SgBitAndOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBitAndOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBitAndOp::pools.begin(); block != SgBitAndOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitAndOp*>(*block)};
        for (unsigned i = 0; i < SgBitAndOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBitAndOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBitAndOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBitAndOp* pointerOfLinkedList{nullptr};
    for (auto block = SgBitAndOp::pools.begin(); block != SgBitAndOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitAndOp*>(*block)};
        for (unsigned i = 0; i < SgBitAndOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBitAndOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBitAndOp::clearMemoryPool() {
    SgBitAndOp* tempPointer{nullptr};
    if (SgBitAndOp::pools.empty() == false) {
        auto block{SgBitAndOp::pools.begin()};
        SgBitAndOp::next_node = reinterpret_cast<SgBitAndOp*>(*block);
        for (; block != SgBitAndOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBitAndOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBitAndOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBitAndOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBitAndOp::pool_size-1]);
        }
    }
}

void
SgBitAndOp::deleteMemoryPool() {
    for (auto p: SgBitAndOp::pools) {
        ROSE_FREE(p);
    }
    SgBitAndOp::next_node = nullptr;
    SgBitAndOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBitAndOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAndOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitAndOp)};
    for (size_t blockIndex = SgBitAndOp::pools.size(); (blockIndex * SgBitAndOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAndOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitAndOp) = %" PRIuPTR " SgBitAndOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBitAndOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitAndOp),SgBitAndOp::pool_size);
        }
#endif

        SgBitAndOp* pointer{static_cast<SgBitAndOp*>(ROSE_MALLOC(SgBitAndOp::pool_size * sizeof(SgBitAndOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBitAndOp::pool_size * sizeof(SgBitAndOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBitAndOp::pool_size * sizeof(SgBitAndOp));
#endif
        SgBitAndOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBitAndOp::pool_size * sizeof(SgBitAndOp), V_SgBitAndOp));

        if (SgBitAndOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBitAndOp* blkptr{reinterpret_cast<SgBitAndOp*>(SgBitAndOp::pools[blockIndex-1])};
                blkptr[SgBitAndOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBitAndOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBitAndOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBitAndOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBitAndOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBitAndOp::pool_size - 1};
    SgBitAndOp* testPointer{reinterpret_cast<SgBitAndOp*>(SgBitAndOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBitAndOp::pool_size * (SgBitAndOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBitAndOpStroageClass
 * from its counterpart of SgBitAndOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBitAndOp::initializeStorageClassArray(SgBitAndOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBitAndOp::pools.begin(); block != SgBitAndOp::pools.end(); ++block) {
        SgBitAndOp* pointer{reinterpret_cast<SgBitAndOp*>(*block)};
        for (unsigned i = 0; i < SgBitAndOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 112939 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBitOrOp that takes its corresponding StorageClass as parameter
 */
SgBitOrOp::SgBitOrOp(const SgBitOrOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 112949 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBitOrOp*
SgBitOrOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBitOrOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitOrOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitOrOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitOrOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBitOrOp)};
        unsigned long positionInPool{localIndex % SgBitOrOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitOrOp::pool_size};
        returnPointer = &(((SgBitOrOp*)(SgBitOrOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBitOrOp*
SgBitOrOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBitOrOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitOrOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitOrOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitOrOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBitOrOp)};
        unsigned long positionInPool{localIndex % SgBitOrOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitOrOp::pool_size};
        returnPointer = &(((SgBitOrOp*)(SgBitOrOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBitOrOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBitOrOp::pools.begin(); block != SgBitOrOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitOrOp*>(*block)};
        for (unsigned i = 0; i < SgBitOrOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBitOrOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBitOrOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBitOrOp* pointerOfLinkedList{nullptr};
    for (auto block = SgBitOrOp::pools.begin(); block != SgBitOrOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitOrOp*>(*block)};
        for (unsigned i = 0; i < SgBitOrOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBitOrOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBitOrOp::clearMemoryPool() {
    SgBitOrOp* tempPointer{nullptr};
    if (SgBitOrOp::pools.empty() == false) {
        auto block{SgBitOrOp::pools.begin()};
        SgBitOrOp::next_node = reinterpret_cast<SgBitOrOp*>(*block);
        for (; block != SgBitOrOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBitOrOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBitOrOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBitOrOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBitOrOp::pool_size-1]);
        }
    }
}

void
SgBitOrOp::deleteMemoryPool() {
    for (auto p: SgBitOrOp::pools) {
        ROSE_FREE(p);
    }
    SgBitOrOp::next_node = nullptr;
    SgBitOrOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBitOrOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBitOrOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitOrOp)};
    for (size_t blockIndex = SgBitOrOp::pools.size(); (blockIndex * SgBitOrOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBitOrOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitOrOp) = %" PRIuPTR " SgBitOrOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBitOrOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitOrOp),SgBitOrOp::pool_size);
        }
#endif

        SgBitOrOp* pointer{static_cast<SgBitOrOp*>(ROSE_MALLOC(SgBitOrOp::pool_size * sizeof(SgBitOrOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBitOrOp::pool_size * sizeof(SgBitOrOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBitOrOp::pool_size * sizeof(SgBitOrOp));
#endif
        SgBitOrOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBitOrOp::pool_size * sizeof(SgBitOrOp), V_SgBitOrOp));

        if (SgBitOrOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBitOrOp* blkptr{reinterpret_cast<SgBitOrOp*>(SgBitOrOp::pools[blockIndex-1])};
                blkptr[SgBitOrOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBitOrOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBitOrOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBitOrOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBitOrOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBitOrOp::pool_size - 1};
    SgBitOrOp* testPointer{reinterpret_cast<SgBitOrOp*>(SgBitOrOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBitOrOp::pool_size * (SgBitOrOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBitOrOpStroageClass
 * from its counterpart of SgBitOrOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBitOrOp::initializeStorageClassArray(SgBitOrOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBitOrOp::pools.begin(); block != SgBitOrOp::pools.end(); ++block) {
        SgBitOrOp* pointer{reinterpret_cast<SgBitOrOp*>(*block)};
        for (unsigned i = 0; i < SgBitOrOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 113166 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBitEqvOp that takes its corresponding StorageClass as parameter
 */
SgBitEqvOp::SgBitEqvOp(const SgBitEqvOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 113176 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBitEqvOp*
SgBitEqvOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBitEqvOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitEqvOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitEqvOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBitEqvOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBitEqvOp)};
        unsigned long positionInPool{localIndex % SgBitEqvOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitEqvOp::pool_size};
        returnPointer = &(((SgBitEqvOp*)(SgBitEqvOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBitEqvOp*
SgBitEqvOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBitEqvOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitEqvOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitEqvOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBitEqvOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBitEqvOp)};
        unsigned long positionInPool{localIndex % SgBitEqvOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBitEqvOp::pool_size};
        returnPointer = &(((SgBitEqvOp*)(SgBitEqvOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBitEqvOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBitEqvOp::pools.begin(); block != SgBitEqvOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitEqvOp*>(*block)};
        for (unsigned i = 0; i < SgBitEqvOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBitEqvOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBitEqvOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBitEqvOp* pointerOfLinkedList{nullptr};
    for (auto block = SgBitEqvOp::pools.begin(); block != SgBitEqvOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBitEqvOp*>(*block)};
        for (unsigned i = 0; i < SgBitEqvOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBitEqvOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBitEqvOp::clearMemoryPool() {
    SgBitEqvOp* tempPointer{nullptr};
    if (SgBitEqvOp::pools.empty() == false) {
        auto block{SgBitEqvOp::pools.begin()};
        SgBitEqvOp::next_node = reinterpret_cast<SgBitEqvOp*>(*block);
        for (; block != SgBitEqvOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBitEqvOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBitEqvOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBitEqvOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBitEqvOp::pool_size-1]);
        }
    }
}

void
SgBitEqvOp::deleteMemoryPool() {
    for (auto p: SgBitEqvOp::pools) {
        ROSE_FREE(p);
    }
    SgBitEqvOp::next_node = nullptr;
    SgBitEqvOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBitEqvOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBitEqvOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitEqvOp)};
    for (size_t blockIndex = SgBitEqvOp::pools.size(); (blockIndex * SgBitEqvOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBitEqvOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitEqvOp) = %" PRIuPTR " SgBitEqvOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBitEqvOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBitEqvOp),SgBitEqvOp::pool_size);
        }
#endif

        SgBitEqvOp* pointer{static_cast<SgBitEqvOp*>(ROSE_MALLOC(SgBitEqvOp::pool_size * sizeof(SgBitEqvOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBitEqvOp::pool_size * sizeof(SgBitEqvOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBitEqvOp::pool_size * sizeof(SgBitEqvOp));
#endif
        SgBitEqvOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBitEqvOp::pool_size * sizeof(SgBitEqvOp), V_SgBitEqvOp));

        if (SgBitEqvOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBitEqvOp* blkptr{reinterpret_cast<SgBitEqvOp*>(SgBitEqvOp::pools[blockIndex-1])};
                blkptr[SgBitEqvOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBitEqvOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBitEqvOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBitEqvOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBitEqvOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBitEqvOp::pool_size - 1};
    SgBitEqvOp* testPointer{reinterpret_cast<SgBitEqvOp*>(SgBitEqvOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBitEqvOp::pool_size * (SgBitEqvOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBitEqvOpStroageClass
 * from its counterpart of SgBitEqvOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBitEqvOp::initializeStorageClassArray(SgBitEqvOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBitEqvOp::pools.begin(); block != SgBitEqvOp::pools.end(); ++block) {
        SgBitEqvOp* pointer{reinterpret_cast<SgBitEqvOp*>(*block)};
        for (unsigned i = 0; i < SgBitEqvOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 113393 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCommaOpExp that takes its corresponding StorageClass as parameter
 */
SgCommaOpExp::SgCommaOpExp(const SgCommaOpExpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 113403 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCommaOpExp*
SgCommaOpExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCommaOpExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommaOpExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommaOpExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommaOpExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCommaOpExp)};
        unsigned long positionInPool{localIndex % SgCommaOpExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommaOpExp::pool_size};
        returnPointer = &(((SgCommaOpExp*)(SgCommaOpExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCommaOpExp*
SgCommaOpExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCommaOpExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommaOpExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommaOpExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommaOpExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCommaOpExp)};
        unsigned long positionInPool{localIndex % SgCommaOpExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommaOpExp::pool_size};
        returnPointer = &(((SgCommaOpExp*)(SgCommaOpExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCommaOpExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCommaOpExp::pools.begin(); block != SgCommaOpExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommaOpExp*>(*block)};
        for (unsigned i = 0; i < SgCommaOpExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCommaOpExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCommaOpExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCommaOpExp* pointerOfLinkedList{nullptr};
    for (auto block = SgCommaOpExp::pools.begin(); block != SgCommaOpExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommaOpExp*>(*block)};
        for (unsigned i = 0; i < SgCommaOpExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCommaOpExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCommaOpExp::clearMemoryPool() {
    SgCommaOpExp* tempPointer{nullptr};
    if (SgCommaOpExp::pools.empty() == false) {
        auto block{SgCommaOpExp::pools.begin()};
        SgCommaOpExp::next_node = reinterpret_cast<SgCommaOpExp*>(*block);
        for (; block != SgCommaOpExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCommaOpExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCommaOpExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCommaOpExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCommaOpExp::pool_size-1]);
        }
    }
}

void
SgCommaOpExp::deleteMemoryPool() {
    for (auto p: SgCommaOpExp::pools) {
        ROSE_FREE(p);
    }
    SgCommaOpExp::next_node = nullptr;
    SgCommaOpExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCommaOpExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCommaOpExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommaOpExp)};
    for (size_t blockIndex = SgCommaOpExp::pools.size(); (blockIndex * SgCommaOpExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCommaOpExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommaOpExp) = %" PRIuPTR " SgCommaOpExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCommaOpExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommaOpExp),SgCommaOpExp::pool_size);
        }
#endif

        SgCommaOpExp* pointer{static_cast<SgCommaOpExp*>(ROSE_MALLOC(SgCommaOpExp::pool_size * sizeof(SgCommaOpExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCommaOpExp::pool_size * sizeof(SgCommaOpExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCommaOpExp::pool_size * sizeof(SgCommaOpExp));
#endif
        SgCommaOpExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCommaOpExp::pool_size * sizeof(SgCommaOpExp), V_SgCommaOpExp));

        if (SgCommaOpExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCommaOpExp* blkptr{reinterpret_cast<SgCommaOpExp*>(SgCommaOpExp::pools[blockIndex-1])};
                blkptr[SgCommaOpExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCommaOpExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCommaOpExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCommaOpExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCommaOpExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCommaOpExp::pool_size - 1};
    SgCommaOpExp* testPointer{reinterpret_cast<SgCommaOpExp*>(SgCommaOpExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCommaOpExp::pool_size * (SgCommaOpExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCommaOpExpStroageClass
 * from its counterpart of SgCommaOpExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCommaOpExp::initializeStorageClassArray(SgCommaOpExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCommaOpExp::pools.begin(); block != SgCommaOpExp::pools.end(); ++block) {
        SgCommaOpExp* pointer{reinterpret_cast<SgCommaOpExp*>(*block)};
        for (unsigned i = 0; i < SgCommaOpExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 113620 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLshiftOp that takes its corresponding StorageClass as parameter
 */
SgLshiftOp::SgLshiftOp(const SgLshiftOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 113630 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLshiftOp*
SgLshiftOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLshiftOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLshiftOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLshiftOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLshiftOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftOp)};
        unsigned long positionInPool{localIndex % SgLshiftOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLshiftOp::pool_size};
        returnPointer = &(((SgLshiftOp*)(SgLshiftOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLshiftOp*
SgLshiftOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLshiftOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLshiftOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLshiftOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLshiftOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLshiftOp)};
        unsigned long positionInPool{localIndex % SgLshiftOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLshiftOp::pool_size};
        returnPointer = &(((SgLshiftOp*)(SgLshiftOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLshiftOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLshiftOp::pools.begin(); block != SgLshiftOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLshiftOp*>(*block)};
        for (unsigned i = 0; i < SgLshiftOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLshiftOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLshiftOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLshiftOp* pointerOfLinkedList{nullptr};
    for (auto block = SgLshiftOp::pools.begin(); block != SgLshiftOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLshiftOp*>(*block)};
        for (unsigned i = 0; i < SgLshiftOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLshiftOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLshiftOp::clearMemoryPool() {
    SgLshiftOp* tempPointer{nullptr};
    if (SgLshiftOp::pools.empty() == false) {
        auto block{SgLshiftOp::pools.begin()};
        SgLshiftOp::next_node = reinterpret_cast<SgLshiftOp*>(*block);
        for (; block != SgLshiftOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLshiftOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLshiftOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLshiftOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLshiftOp::pool_size-1]);
        }
    }
}

void
SgLshiftOp::deleteMemoryPool() {
    for (auto p: SgLshiftOp::pools) {
        ROSE_FREE(p);
    }
    SgLshiftOp::next_node = nullptr;
    SgLshiftOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLshiftOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLshiftOp)};
    for (size_t blockIndex = SgLshiftOp::pools.size(); (blockIndex * SgLshiftOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLshiftOp) = %" PRIuPTR " SgLshiftOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLshiftOp),SgLshiftOp::pool_size);
        }
#endif

        SgLshiftOp* pointer{static_cast<SgLshiftOp*>(ROSE_MALLOC(SgLshiftOp::pool_size * sizeof(SgLshiftOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLshiftOp::pool_size * sizeof(SgLshiftOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLshiftOp::pool_size * sizeof(SgLshiftOp));
#endif
        SgLshiftOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLshiftOp::pool_size * sizeof(SgLshiftOp), V_SgLshiftOp));

        if (SgLshiftOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLshiftOp* blkptr{reinterpret_cast<SgLshiftOp*>(SgLshiftOp::pools[blockIndex-1])};
                blkptr[SgLshiftOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLshiftOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLshiftOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLshiftOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLshiftOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLshiftOp::pool_size - 1};
    SgLshiftOp* testPointer{reinterpret_cast<SgLshiftOp*>(SgLshiftOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLshiftOp::pool_size * (SgLshiftOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLshiftOpStroageClass
 * from its counterpart of SgLshiftOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLshiftOp::initializeStorageClassArray(SgLshiftOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLshiftOp::pools.begin(); block != SgLshiftOp::pools.end(); ++block) {
        SgLshiftOp* pointer{reinterpret_cast<SgLshiftOp*>(*block)};
        for (unsigned i = 0; i < SgLshiftOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 113847 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRshiftOp that takes its corresponding StorageClass as parameter
 */
SgRshiftOp::SgRshiftOp(const SgRshiftOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 113857 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRshiftOp*
SgRshiftOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRshiftOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRshiftOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRshiftOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRshiftOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftOp)};
        unsigned long positionInPool{localIndex % SgRshiftOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRshiftOp::pool_size};
        returnPointer = &(((SgRshiftOp*)(SgRshiftOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRshiftOp*
SgRshiftOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRshiftOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRshiftOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRshiftOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRshiftOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRshiftOp)};
        unsigned long positionInPool{localIndex % SgRshiftOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRshiftOp::pool_size};
        returnPointer = &(((SgRshiftOp*)(SgRshiftOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRshiftOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRshiftOp::pools.begin(); block != SgRshiftOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRshiftOp*>(*block)};
        for (unsigned i = 0; i < SgRshiftOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRshiftOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRshiftOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRshiftOp* pointerOfLinkedList{nullptr};
    for (auto block = SgRshiftOp::pools.begin(); block != SgRshiftOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRshiftOp*>(*block)};
        for (unsigned i = 0; i < SgRshiftOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRshiftOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRshiftOp::clearMemoryPool() {
    SgRshiftOp* tempPointer{nullptr};
    if (SgRshiftOp::pools.empty() == false) {
        auto block{SgRshiftOp::pools.begin()};
        SgRshiftOp::next_node = reinterpret_cast<SgRshiftOp*>(*block);
        for (; block != SgRshiftOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRshiftOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRshiftOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRshiftOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRshiftOp::pool_size-1]);
        }
    }
}

void
SgRshiftOp::deleteMemoryPool() {
    for (auto p: SgRshiftOp::pools) {
        ROSE_FREE(p);
    }
    SgRshiftOp::next_node = nullptr;
    SgRshiftOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRshiftOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRshiftOp)};
    for (size_t blockIndex = SgRshiftOp::pools.size(); (blockIndex * SgRshiftOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRshiftOp) = %" PRIuPTR " SgRshiftOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRshiftOp),SgRshiftOp::pool_size);
        }
#endif

        SgRshiftOp* pointer{static_cast<SgRshiftOp*>(ROSE_MALLOC(SgRshiftOp::pool_size * sizeof(SgRshiftOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRshiftOp::pool_size * sizeof(SgRshiftOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRshiftOp::pool_size * sizeof(SgRshiftOp));
#endif
        SgRshiftOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRshiftOp::pool_size * sizeof(SgRshiftOp), V_SgRshiftOp));

        if (SgRshiftOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRshiftOp* blkptr{reinterpret_cast<SgRshiftOp*>(SgRshiftOp::pools[blockIndex-1])};
                blkptr[SgRshiftOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRshiftOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRshiftOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRshiftOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRshiftOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRshiftOp::pool_size - 1};
    SgRshiftOp* testPointer{reinterpret_cast<SgRshiftOp*>(SgRshiftOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRshiftOp::pool_size * (SgRshiftOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRshiftOpStroageClass
 * from its counterpart of SgRshiftOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRshiftOp::initializeStorageClassArray(SgRshiftOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRshiftOp::pools.begin(); block != SgRshiftOp::pools.end(); ++block) {
        SgRshiftOp* pointer{reinterpret_cast<SgRshiftOp*>(*block)};
        for (unsigned i = 0; i < SgRshiftOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 114074 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPntrArrRefExp that takes its corresponding StorageClass as parameter
 */
SgPntrArrRefExp::SgPntrArrRefExp(const SgPntrArrRefExpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 114084 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPntrArrRefExp*
SgPntrArrRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPntrArrRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPntrArrRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPntrArrRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPntrArrRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPntrArrRefExp)};
        unsigned long positionInPool{localIndex % SgPntrArrRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPntrArrRefExp::pool_size};
        returnPointer = &(((SgPntrArrRefExp*)(SgPntrArrRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPntrArrRefExp*
SgPntrArrRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPntrArrRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPntrArrRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPntrArrRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPntrArrRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPntrArrRefExp)};
        unsigned long positionInPool{localIndex % SgPntrArrRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPntrArrRefExp::pool_size};
        returnPointer = &(((SgPntrArrRefExp*)(SgPntrArrRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPntrArrRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPntrArrRefExp::pools.begin(); block != SgPntrArrRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPntrArrRefExp*>(*block)};
        for (unsigned i = 0; i < SgPntrArrRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPntrArrRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPntrArrRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPntrArrRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgPntrArrRefExp::pools.begin(); block != SgPntrArrRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPntrArrRefExp*>(*block)};
        for (unsigned i = 0; i < SgPntrArrRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPntrArrRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPntrArrRefExp::clearMemoryPool() {
    SgPntrArrRefExp* tempPointer{nullptr};
    if (SgPntrArrRefExp::pools.empty() == false) {
        auto block{SgPntrArrRefExp::pools.begin()};
        SgPntrArrRefExp::next_node = reinterpret_cast<SgPntrArrRefExp*>(*block);
        for (; block != SgPntrArrRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPntrArrRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPntrArrRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPntrArrRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPntrArrRefExp::pool_size-1]);
        }
    }
}

void
SgPntrArrRefExp::deleteMemoryPool() {
    for (auto p: SgPntrArrRefExp::pools) {
        ROSE_FREE(p);
    }
    SgPntrArrRefExp::next_node = nullptr;
    SgPntrArrRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPntrArrRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPntrArrRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPntrArrRefExp)};
    for (size_t blockIndex = SgPntrArrRefExp::pools.size(); (blockIndex * SgPntrArrRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPntrArrRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPntrArrRefExp) = %" PRIuPTR " SgPntrArrRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPntrArrRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPntrArrRefExp),SgPntrArrRefExp::pool_size);
        }
#endif

        SgPntrArrRefExp* pointer{static_cast<SgPntrArrRefExp*>(ROSE_MALLOC(SgPntrArrRefExp::pool_size * sizeof(SgPntrArrRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPntrArrRefExp::pool_size * sizeof(SgPntrArrRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPntrArrRefExp::pool_size * sizeof(SgPntrArrRefExp));
#endif
        SgPntrArrRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPntrArrRefExp::pool_size * sizeof(SgPntrArrRefExp), V_SgPntrArrRefExp));

        if (SgPntrArrRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPntrArrRefExp* blkptr{reinterpret_cast<SgPntrArrRefExp*>(SgPntrArrRefExp::pools[blockIndex-1])};
                blkptr[SgPntrArrRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPntrArrRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPntrArrRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPntrArrRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPntrArrRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPntrArrRefExp::pool_size - 1};
    SgPntrArrRefExp* testPointer{reinterpret_cast<SgPntrArrRefExp*>(SgPntrArrRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPntrArrRefExp::pool_size * (SgPntrArrRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPntrArrRefExpStroageClass
 * from its counterpart of SgPntrArrRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPntrArrRefExp::initializeStorageClassArray(SgPntrArrRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPntrArrRefExp::pools.begin(); block != SgPntrArrRefExp::pools.end(); ++block) {
        SgPntrArrRefExp* pointer{reinterpret_cast<SgPntrArrRefExp*>(*block)};
        for (unsigned i = 0; i < SgPntrArrRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 114301 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgScopeOp that takes its corresponding StorageClass as parameter
 */
SgScopeOp::SgScopeOp(const SgScopeOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 114311 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgScopeOp*
SgScopeOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgScopeOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopeOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopeOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopeOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeOp)};
        unsigned long positionInPool{localIndex % SgScopeOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopeOp::pool_size};
        returnPointer = &(((SgScopeOp*)(SgScopeOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgScopeOp*
SgScopeOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgScopeOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopeOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopeOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopeOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgScopeOp)};
        unsigned long positionInPool{localIndex % SgScopeOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopeOp::pool_size};
        returnPointer = &(((SgScopeOp*)(SgScopeOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgScopeOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgScopeOp::pools.begin(); block != SgScopeOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopeOp*>(*block)};
        for (unsigned i = 0; i < SgScopeOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgScopeOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgScopeOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgScopeOp* pointerOfLinkedList{nullptr};
    for (auto block = SgScopeOp::pools.begin(); block != SgScopeOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopeOp*>(*block)};
        for (unsigned i = 0; i < SgScopeOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgScopeOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgScopeOp::clearMemoryPool() {
    SgScopeOp* tempPointer{nullptr};
    if (SgScopeOp::pools.empty() == false) {
        auto block{SgScopeOp::pools.begin()};
        SgScopeOp::next_node = reinterpret_cast<SgScopeOp*>(*block);
        for (; block != SgScopeOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgScopeOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgScopeOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgScopeOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgScopeOp::pool_size-1]);
        }
    }
}

void
SgScopeOp::deleteMemoryPool() {
    for (auto p: SgScopeOp::pools) {
        ROSE_FREE(p);
    }
    SgScopeOp::next_node = nullptr;
    SgScopeOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgScopeOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopeOp)};
    for (size_t blockIndex = SgScopeOp::pools.size(); (blockIndex * SgScopeOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopeOp) = %" PRIuPTR " SgScopeOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgScopeOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopeOp),SgScopeOp::pool_size);
        }
#endif

        SgScopeOp* pointer{static_cast<SgScopeOp*>(ROSE_MALLOC(SgScopeOp::pool_size * sizeof(SgScopeOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgScopeOp::pool_size * sizeof(SgScopeOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgScopeOp::pool_size * sizeof(SgScopeOp));
#endif
        SgScopeOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgScopeOp::pool_size * sizeof(SgScopeOp), V_SgScopeOp));

        if (SgScopeOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgScopeOp* blkptr{reinterpret_cast<SgScopeOp*>(SgScopeOp::pools[blockIndex-1])};
                blkptr[SgScopeOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgScopeOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgScopeOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgScopeOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgScopeOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgScopeOp::pool_size - 1};
    SgScopeOp* testPointer{reinterpret_cast<SgScopeOp*>(SgScopeOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgScopeOp::pool_size * (SgScopeOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgScopeOpStroageClass
 * from its counterpart of SgScopeOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgScopeOp::initializeStorageClassArray(SgScopeOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgScopeOp::pools.begin(); block != SgScopeOp::pools.end(); ++block) {
        SgScopeOp* pointer{reinterpret_cast<SgScopeOp*>(*block)};
        for (unsigned i = 0; i < SgScopeOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 114528 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAssignOp that takes its corresponding StorageClass as parameter
 */
SgAssignOp::SgAssignOp(const SgAssignOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 114538 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAssignOp*
SgAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignOp)};
        unsigned long positionInPool{localIndex % SgAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignOp::pool_size};
        returnPointer = &(((SgAssignOp*)(SgAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAssignOp*
SgAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAssignOp)};
        unsigned long positionInPool{localIndex % SgAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignOp::pool_size};
        returnPointer = &(((SgAssignOp*)(SgAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAssignOp::pools.begin(); block != SgAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignOp*>(*block)};
        for (unsigned i = 0; i < SgAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAssignOp::pools.begin(); block != SgAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignOp*>(*block)};
        for (unsigned i = 0; i < SgAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAssignOp::clearMemoryPool() {
    SgAssignOp* tempPointer{nullptr};
    if (SgAssignOp::pools.empty() == false) {
        auto block{SgAssignOp::pools.begin()};
        SgAssignOp::next_node = reinterpret_cast<SgAssignOp*>(*block);
        for (; block != SgAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAssignOp::pool_size-1]);
        }
    }
}

void
SgAssignOp::deleteMemoryPool() {
    for (auto p: SgAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgAssignOp::next_node = nullptr;
    SgAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignOp)};
    for (size_t blockIndex = SgAssignOp::pools.size(); (blockIndex * SgAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignOp) = %" PRIuPTR " SgAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignOp),SgAssignOp::pool_size);
        }
#endif

        SgAssignOp* pointer{static_cast<SgAssignOp*>(ROSE_MALLOC(SgAssignOp::pool_size * sizeof(SgAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAssignOp::pool_size * sizeof(SgAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAssignOp::pool_size * sizeof(SgAssignOp));
#endif
        SgAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAssignOp::pool_size * sizeof(SgAssignOp), V_SgAssignOp));

        if (SgAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAssignOp* blkptr{reinterpret_cast<SgAssignOp*>(SgAssignOp::pools[blockIndex-1])};
                blkptr[SgAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAssignOp::pool_size - 1};
    SgAssignOp* testPointer{reinterpret_cast<SgAssignOp*>(SgAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAssignOp::pool_size * (SgAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAssignOpStroageClass
 * from its counterpart of SgAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAssignOp::initializeStorageClassArray(SgAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAssignOp::pools.begin(); block != SgAssignOp::pools.end(); ++block) {
        SgAssignOp* pointer{reinterpret_cast<SgAssignOp*>(*block)};
        for (unsigned i = 0; i < SgAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 114755 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExponentiationOp that takes its corresponding StorageClass as parameter
 */
SgExponentiationOp::SgExponentiationOp(const SgExponentiationOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 114765 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExponentiationOp*
SgExponentiationOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExponentiationOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExponentiationOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExponentiationOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExponentiationOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationOp)};
        unsigned long positionInPool{localIndex % SgExponentiationOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExponentiationOp::pool_size};
        returnPointer = &(((SgExponentiationOp*)(SgExponentiationOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExponentiationOp*
SgExponentiationOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExponentiationOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExponentiationOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExponentiationOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExponentiationOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExponentiationOp)};
        unsigned long positionInPool{localIndex % SgExponentiationOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExponentiationOp::pool_size};
        returnPointer = &(((SgExponentiationOp*)(SgExponentiationOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExponentiationOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExponentiationOp::pools.begin(); block != SgExponentiationOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExponentiationOp*>(*block)};
        for (unsigned i = 0; i < SgExponentiationOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExponentiationOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExponentiationOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExponentiationOp* pointerOfLinkedList{nullptr};
    for (auto block = SgExponentiationOp::pools.begin(); block != SgExponentiationOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExponentiationOp*>(*block)};
        for (unsigned i = 0; i < SgExponentiationOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExponentiationOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExponentiationOp::clearMemoryPool() {
    SgExponentiationOp* tempPointer{nullptr};
    if (SgExponentiationOp::pools.empty() == false) {
        auto block{SgExponentiationOp::pools.begin()};
        SgExponentiationOp::next_node = reinterpret_cast<SgExponentiationOp*>(*block);
        for (; block != SgExponentiationOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExponentiationOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExponentiationOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExponentiationOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExponentiationOp::pool_size-1]);
        }
    }
}

void
SgExponentiationOp::deleteMemoryPool() {
    for (auto p: SgExponentiationOp::pools) {
        ROSE_FREE(p);
    }
    SgExponentiationOp::next_node = nullptr;
    SgExponentiationOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExponentiationOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExponentiationOp)};
    for (size_t blockIndex = SgExponentiationOp::pools.size(); (blockIndex * SgExponentiationOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExponentiationOp) = %" PRIuPTR " SgExponentiationOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExponentiationOp),SgExponentiationOp::pool_size);
        }
#endif

        SgExponentiationOp* pointer{static_cast<SgExponentiationOp*>(ROSE_MALLOC(SgExponentiationOp::pool_size * sizeof(SgExponentiationOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExponentiationOp::pool_size * sizeof(SgExponentiationOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExponentiationOp::pool_size * sizeof(SgExponentiationOp));
#endif
        SgExponentiationOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExponentiationOp::pool_size * sizeof(SgExponentiationOp), V_SgExponentiationOp));

        if (SgExponentiationOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExponentiationOp* blkptr{reinterpret_cast<SgExponentiationOp*>(SgExponentiationOp::pools[blockIndex-1])};
                blkptr[SgExponentiationOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExponentiationOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExponentiationOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExponentiationOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExponentiationOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExponentiationOp::pool_size - 1};
    SgExponentiationOp* testPointer{reinterpret_cast<SgExponentiationOp*>(SgExponentiationOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExponentiationOp::pool_size * (SgExponentiationOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExponentiationOpStroageClass
 * from its counterpart of SgExponentiationOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExponentiationOp::initializeStorageClassArray(SgExponentiationOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExponentiationOp::pools.begin(); block != SgExponentiationOp::pools.end(); ++block) {
        SgExponentiationOp* pointer{reinterpret_cast<SgExponentiationOp*>(*block)};
        for (unsigned i = 0; i < SgExponentiationOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 114982 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaUnsignedRshiftOp that takes its corresponding StorageClass as parameter
 */
SgJavaUnsignedRshiftOp::SgJavaUnsignedRshiftOp(const SgJavaUnsignedRshiftOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 114992 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaUnsignedRshiftOp*
SgJavaUnsignedRshiftOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaUnsignedRshiftOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnsignedRshiftOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnsignedRshiftOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnsignedRshiftOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftOp)};
        unsigned long positionInPool{localIndex % SgJavaUnsignedRshiftOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaUnsignedRshiftOp::pool_size};
        returnPointer = &(((SgJavaUnsignedRshiftOp*)(SgJavaUnsignedRshiftOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaUnsignedRshiftOp*
SgJavaUnsignedRshiftOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaUnsignedRshiftOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnsignedRshiftOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnsignedRshiftOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnsignedRshiftOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaUnsignedRshiftOp)};
        unsigned long positionInPool{localIndex % SgJavaUnsignedRshiftOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaUnsignedRshiftOp::pool_size};
        returnPointer = &(((SgJavaUnsignedRshiftOp*)(SgJavaUnsignedRshiftOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaUnsignedRshiftOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaUnsignedRshiftOp::pools.begin(); block != SgJavaUnsignedRshiftOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaUnsignedRshiftOp*>(*block)};
        for (unsigned i = 0; i < SgJavaUnsignedRshiftOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaUnsignedRshiftOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaUnsignedRshiftOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaUnsignedRshiftOp* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaUnsignedRshiftOp::pools.begin(); block != SgJavaUnsignedRshiftOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaUnsignedRshiftOp*>(*block)};
        for (unsigned i = 0; i < SgJavaUnsignedRshiftOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaUnsignedRshiftOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaUnsignedRshiftOp::clearMemoryPool() {
    SgJavaUnsignedRshiftOp* tempPointer{nullptr};
    if (SgJavaUnsignedRshiftOp::pools.empty() == false) {
        auto block{SgJavaUnsignedRshiftOp::pools.begin()};
        SgJavaUnsignedRshiftOp::next_node = reinterpret_cast<SgJavaUnsignedRshiftOp*>(*block);
        for (; block != SgJavaUnsignedRshiftOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaUnsignedRshiftOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaUnsignedRshiftOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaUnsignedRshiftOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaUnsignedRshiftOp::pool_size-1]);
        }
    }
}

void
SgJavaUnsignedRshiftOp::deleteMemoryPool() {
    for (auto p: SgJavaUnsignedRshiftOp::pools) {
        ROSE_FREE(p);
    }
    SgJavaUnsignedRshiftOp::next_node = nullptr;
    SgJavaUnsignedRshiftOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaUnsignedRshiftOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnsignedRshiftOp)};
    for (size_t blockIndex = SgJavaUnsignedRshiftOp::pools.size(); (blockIndex * SgJavaUnsignedRshiftOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnsignedRshiftOp) = %" PRIuPTR " SgJavaUnsignedRshiftOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnsignedRshiftOp),SgJavaUnsignedRshiftOp::pool_size);
        }
#endif

        SgJavaUnsignedRshiftOp* pointer{static_cast<SgJavaUnsignedRshiftOp*>(ROSE_MALLOC(SgJavaUnsignedRshiftOp::pool_size * sizeof(SgJavaUnsignedRshiftOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaUnsignedRshiftOp::pool_size * sizeof(SgJavaUnsignedRshiftOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaUnsignedRshiftOp::pool_size * sizeof(SgJavaUnsignedRshiftOp));
#endif
        SgJavaUnsignedRshiftOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaUnsignedRshiftOp::pool_size * sizeof(SgJavaUnsignedRshiftOp), V_SgJavaUnsignedRshiftOp));

        if (SgJavaUnsignedRshiftOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaUnsignedRshiftOp* blkptr{reinterpret_cast<SgJavaUnsignedRshiftOp*>(SgJavaUnsignedRshiftOp::pools[blockIndex-1])};
                blkptr[SgJavaUnsignedRshiftOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaUnsignedRshiftOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaUnsignedRshiftOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaUnsignedRshiftOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaUnsignedRshiftOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaUnsignedRshiftOp::pool_size - 1};
    SgJavaUnsignedRshiftOp* testPointer{reinterpret_cast<SgJavaUnsignedRshiftOp*>(SgJavaUnsignedRshiftOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaUnsignedRshiftOp::pool_size * (SgJavaUnsignedRshiftOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaUnsignedRshiftOpStroageClass
 * from its counterpart of SgJavaUnsignedRshiftOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaUnsignedRshiftOp::initializeStorageClassArray(SgJavaUnsignedRshiftOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaUnsignedRshiftOp::pools.begin(); block != SgJavaUnsignedRshiftOp::pools.end(); ++block) {
        SgJavaUnsignedRshiftOp* pointer{reinterpret_cast<SgJavaUnsignedRshiftOp*>(*block)};
        for (unsigned i = 0; i < SgJavaUnsignedRshiftOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 115209 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgConcatenationOp that takes its corresponding StorageClass as parameter
 */
SgConcatenationOp::SgConcatenationOp(const SgConcatenationOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 115219 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgConcatenationOp*
SgConcatenationOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgConcatenationOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConcatenationOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConcatenationOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConcatenationOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgConcatenationOp)};
        unsigned long positionInPool{localIndex % SgConcatenationOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConcatenationOp::pool_size};
        returnPointer = &(((SgConcatenationOp*)(SgConcatenationOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgConcatenationOp*
SgConcatenationOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgConcatenationOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConcatenationOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConcatenationOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConcatenationOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgConcatenationOp)};
        unsigned long positionInPool{localIndex % SgConcatenationOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConcatenationOp::pool_size};
        returnPointer = &(((SgConcatenationOp*)(SgConcatenationOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgConcatenationOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgConcatenationOp::pools.begin(); block != SgConcatenationOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConcatenationOp*>(*block)};
        for (unsigned i = 0; i < SgConcatenationOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgConcatenationOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgConcatenationOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgConcatenationOp* pointerOfLinkedList{nullptr};
    for (auto block = SgConcatenationOp::pools.begin(); block != SgConcatenationOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConcatenationOp*>(*block)};
        for (unsigned i = 0; i < SgConcatenationOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgConcatenationOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgConcatenationOp::clearMemoryPool() {
    SgConcatenationOp* tempPointer{nullptr};
    if (SgConcatenationOp::pools.empty() == false) {
        auto block{SgConcatenationOp::pools.begin()};
        SgConcatenationOp::next_node = reinterpret_cast<SgConcatenationOp*>(*block);
        for (; block != SgConcatenationOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgConcatenationOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgConcatenationOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgConcatenationOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgConcatenationOp::pool_size-1]);
        }
    }
}

void
SgConcatenationOp::deleteMemoryPool() {
    for (auto p: SgConcatenationOp::pools) {
        ROSE_FREE(p);
    }
    SgConcatenationOp::next_node = nullptr;
    SgConcatenationOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgConcatenationOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgConcatenationOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgConcatenationOp)};
    for (size_t blockIndex = SgConcatenationOp::pools.size(); (blockIndex * SgConcatenationOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgConcatenationOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgConcatenationOp) = %" PRIuPTR " SgConcatenationOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgConcatenationOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgConcatenationOp),SgConcatenationOp::pool_size);
        }
#endif

        SgConcatenationOp* pointer{static_cast<SgConcatenationOp*>(ROSE_MALLOC(SgConcatenationOp::pool_size * sizeof(SgConcatenationOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgConcatenationOp::pool_size * sizeof(SgConcatenationOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgConcatenationOp::pool_size * sizeof(SgConcatenationOp));
#endif
        SgConcatenationOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgConcatenationOp::pool_size * sizeof(SgConcatenationOp), V_SgConcatenationOp));

        if (SgConcatenationOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgConcatenationOp* blkptr{reinterpret_cast<SgConcatenationOp*>(SgConcatenationOp::pools[blockIndex-1])};
                blkptr[SgConcatenationOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgConcatenationOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgConcatenationOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgConcatenationOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgConcatenationOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgConcatenationOp::pool_size - 1};
    SgConcatenationOp* testPointer{reinterpret_cast<SgConcatenationOp*>(SgConcatenationOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgConcatenationOp::pool_size * (SgConcatenationOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgConcatenationOpStroageClass
 * from its counterpart of SgConcatenationOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgConcatenationOp::initializeStorageClassArray(SgConcatenationOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgConcatenationOp::pools.begin(); block != SgConcatenationOp::pools.end(); ++block) {
        SgConcatenationOp* pointer{reinterpret_cast<SgConcatenationOp*>(*block)};
        for (unsigned i = 0; i < SgConcatenationOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 115436 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPointerAssignOp that takes its corresponding StorageClass as parameter
 */
SgPointerAssignOp::SgPointerAssignOp(const SgPointerAssignOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 115446 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPointerAssignOp*
SgPointerAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPointerAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPointerAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerAssignOp)};
        unsigned long positionInPool{localIndex % SgPointerAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerAssignOp::pool_size};
        returnPointer = &(((SgPointerAssignOp*)(SgPointerAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPointerAssignOp*
SgPointerAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPointerAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPointerAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPointerAssignOp)};
        unsigned long positionInPool{localIndex % SgPointerAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPointerAssignOp::pool_size};
        returnPointer = &(((SgPointerAssignOp*)(SgPointerAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPointerAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPointerAssignOp::pools.begin(); block != SgPointerAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerAssignOp*>(*block)};
        for (unsigned i = 0; i < SgPointerAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPointerAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPointerAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPointerAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgPointerAssignOp::pools.begin(); block != SgPointerAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPointerAssignOp*>(*block)};
        for (unsigned i = 0; i < SgPointerAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPointerAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPointerAssignOp::clearMemoryPool() {
    SgPointerAssignOp* tempPointer{nullptr};
    if (SgPointerAssignOp::pools.empty() == false) {
        auto block{SgPointerAssignOp::pools.begin()};
        SgPointerAssignOp::next_node = reinterpret_cast<SgPointerAssignOp*>(*block);
        for (; block != SgPointerAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPointerAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPointerAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPointerAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPointerAssignOp::pool_size-1]);
        }
    }
}

void
SgPointerAssignOp::deleteMemoryPool() {
    for (auto p: SgPointerAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgPointerAssignOp::next_node = nullptr;
    SgPointerAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPointerAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerAssignOp)};
    for (size_t blockIndex = SgPointerAssignOp::pools.size(); (blockIndex * SgPointerAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerAssignOp) = %" PRIuPTR " SgPointerAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPointerAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPointerAssignOp),SgPointerAssignOp::pool_size);
        }
#endif

        SgPointerAssignOp* pointer{static_cast<SgPointerAssignOp*>(ROSE_MALLOC(SgPointerAssignOp::pool_size * sizeof(SgPointerAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPointerAssignOp::pool_size * sizeof(SgPointerAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPointerAssignOp::pool_size * sizeof(SgPointerAssignOp));
#endif
        SgPointerAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPointerAssignOp::pool_size * sizeof(SgPointerAssignOp), V_SgPointerAssignOp));

        if (SgPointerAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPointerAssignOp* blkptr{reinterpret_cast<SgPointerAssignOp*>(SgPointerAssignOp::pools[blockIndex-1])};
                blkptr[SgPointerAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPointerAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPointerAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPointerAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPointerAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPointerAssignOp::pool_size - 1};
    SgPointerAssignOp* testPointer{reinterpret_cast<SgPointerAssignOp*>(SgPointerAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPointerAssignOp::pool_size * (SgPointerAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPointerAssignOpStroageClass
 * from its counterpart of SgPointerAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPointerAssignOp::initializeStorageClassArray(SgPointerAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPointerAssignOp::pools.begin(); block != SgPointerAssignOp::pools.end(); ++block) {
        SgPointerAssignOp* pointer{reinterpret_cast<SgPointerAssignOp*>(*block)};
        for (unsigned i = 0; i < SgPointerAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 115663 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUserDefinedBinaryOp that takes its corresponding StorageClass as parameter
 */
SgUserDefinedBinaryOp::SgUserDefinedBinaryOp(const SgUserDefinedBinaryOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 115673 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_operator_name = SgName ( storageSource.storageOf_operator_name);
     p_symbol = (SgFunctionSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUserDefinedBinaryOp*
SgUserDefinedBinaryOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUserDefinedBinaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUserDefinedBinaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUserDefinedBinaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUserDefinedBinaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedBinaryOp)};
        unsigned long positionInPool{localIndex % SgUserDefinedBinaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUserDefinedBinaryOp::pool_size};
        returnPointer = &(((SgUserDefinedBinaryOp*)(SgUserDefinedBinaryOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUserDefinedBinaryOp*
SgUserDefinedBinaryOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUserDefinedBinaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUserDefinedBinaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUserDefinedBinaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUserDefinedBinaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUserDefinedBinaryOp)};
        unsigned long positionInPool{localIndex % SgUserDefinedBinaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUserDefinedBinaryOp::pool_size};
        returnPointer = &(((SgUserDefinedBinaryOp*)(SgUserDefinedBinaryOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUserDefinedBinaryOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUserDefinedBinaryOp::pools.begin(); block != SgUserDefinedBinaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUserDefinedBinaryOp*>(*block)};
        for (unsigned i = 0; i < SgUserDefinedBinaryOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUserDefinedBinaryOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUserDefinedBinaryOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUserDefinedBinaryOp* pointerOfLinkedList{nullptr};
    for (auto block = SgUserDefinedBinaryOp::pools.begin(); block != SgUserDefinedBinaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUserDefinedBinaryOp*>(*block)};
        for (unsigned i = 0; i < SgUserDefinedBinaryOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUserDefinedBinaryOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUserDefinedBinaryOp::clearMemoryPool() {
    SgUserDefinedBinaryOp* tempPointer{nullptr};
    if (SgUserDefinedBinaryOp::pools.empty() == false) {
        auto block{SgUserDefinedBinaryOp::pools.begin()};
        SgUserDefinedBinaryOp::next_node = reinterpret_cast<SgUserDefinedBinaryOp*>(*block);
        for (; block != SgUserDefinedBinaryOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUserDefinedBinaryOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUserDefinedBinaryOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUserDefinedBinaryOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUserDefinedBinaryOp::pool_size-1]);
        }
    }
}

void
SgUserDefinedBinaryOp::deleteMemoryPool() {
    for (auto p: SgUserDefinedBinaryOp::pools) {
        ROSE_FREE(p);
    }
    SgUserDefinedBinaryOp::next_node = nullptr;
    SgUserDefinedBinaryOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUserDefinedBinaryOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedBinaryOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUserDefinedBinaryOp)};
    for (size_t blockIndex = SgUserDefinedBinaryOp::pools.size(); (blockIndex * SgUserDefinedBinaryOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedBinaryOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUserDefinedBinaryOp) = %" PRIuPTR " SgUserDefinedBinaryOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUserDefinedBinaryOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUserDefinedBinaryOp),SgUserDefinedBinaryOp::pool_size);
        }
#endif

        SgUserDefinedBinaryOp* pointer{static_cast<SgUserDefinedBinaryOp*>(ROSE_MALLOC(SgUserDefinedBinaryOp::pool_size * sizeof(SgUserDefinedBinaryOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUserDefinedBinaryOp::pool_size * sizeof(SgUserDefinedBinaryOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUserDefinedBinaryOp::pool_size * sizeof(SgUserDefinedBinaryOp));
#endif
        SgUserDefinedBinaryOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUserDefinedBinaryOp::pool_size * sizeof(SgUserDefinedBinaryOp), V_SgUserDefinedBinaryOp));

        if (SgUserDefinedBinaryOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUserDefinedBinaryOp* blkptr{reinterpret_cast<SgUserDefinedBinaryOp*>(SgUserDefinedBinaryOp::pools[blockIndex-1])};
                blkptr[SgUserDefinedBinaryOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUserDefinedBinaryOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUserDefinedBinaryOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUserDefinedBinaryOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUserDefinedBinaryOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUserDefinedBinaryOp::pool_size - 1};
    SgUserDefinedBinaryOp* testPointer{reinterpret_cast<SgUserDefinedBinaryOp*>(SgUserDefinedBinaryOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUserDefinedBinaryOp::pool_size * (SgUserDefinedBinaryOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUserDefinedBinaryOpStroageClass
 * from its counterpart of SgUserDefinedBinaryOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUserDefinedBinaryOp::initializeStorageClassArray(SgUserDefinedBinaryOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUserDefinedBinaryOp::pools.begin(); block != SgUserDefinedBinaryOp::pools.end(); ++block) {
        SgUserDefinedBinaryOp* pointer{reinterpret_cast<SgUserDefinedBinaryOp*>(*block)};
        for (unsigned i = 0; i < SgUserDefinedBinaryOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 115892 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCompoundAssignOp that takes its corresponding StorageClass as parameter
 */
SgCompoundAssignOp::SgCompoundAssignOp(const SgCompoundAssignOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 115902 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCompoundAssignOp*
SgCompoundAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCompoundAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundAssignOp)};
        unsigned long positionInPool{localIndex % SgCompoundAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCompoundAssignOp::pool_size};
        returnPointer = &(((SgCompoundAssignOp*)(SgCompoundAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCompoundAssignOp*
SgCompoundAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCompoundAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCompoundAssignOp)};
        unsigned long positionInPool{localIndex % SgCompoundAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCompoundAssignOp::pool_size};
        returnPointer = &(((SgCompoundAssignOp*)(SgCompoundAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCompoundAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCompoundAssignOp::pools.begin(); block != SgCompoundAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCompoundAssignOp*>(*block)};
        for (unsigned i = 0; i < SgCompoundAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCompoundAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCompoundAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCompoundAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgCompoundAssignOp::pools.begin(); block != SgCompoundAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCompoundAssignOp*>(*block)};
        for (unsigned i = 0; i < SgCompoundAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCompoundAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCompoundAssignOp::clearMemoryPool() {
    SgCompoundAssignOp* tempPointer{nullptr};
    if (SgCompoundAssignOp::pools.empty() == false) {
        auto block{SgCompoundAssignOp::pools.begin()};
        SgCompoundAssignOp::next_node = reinterpret_cast<SgCompoundAssignOp*>(*block);
        for (; block != SgCompoundAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCompoundAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCompoundAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCompoundAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCompoundAssignOp::pool_size-1]);
        }
    }
}

void
SgCompoundAssignOp::deleteMemoryPool() {
    for (auto p: SgCompoundAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgCompoundAssignOp::next_node = nullptr;
    SgCompoundAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCompoundAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundAssignOp)};
    for (size_t blockIndex = SgCompoundAssignOp::pools.size(); (blockIndex * SgCompoundAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundAssignOp) = %" PRIuPTR " SgCompoundAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundAssignOp),SgCompoundAssignOp::pool_size);
        }
#endif

        SgCompoundAssignOp* pointer{static_cast<SgCompoundAssignOp*>(ROSE_MALLOC(SgCompoundAssignOp::pool_size * sizeof(SgCompoundAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCompoundAssignOp::pool_size * sizeof(SgCompoundAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCompoundAssignOp::pool_size * sizeof(SgCompoundAssignOp));
#endif
        SgCompoundAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCompoundAssignOp::pool_size * sizeof(SgCompoundAssignOp), V_SgCompoundAssignOp));

        if (SgCompoundAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCompoundAssignOp* blkptr{reinterpret_cast<SgCompoundAssignOp*>(SgCompoundAssignOp::pools[blockIndex-1])};
                blkptr[SgCompoundAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCompoundAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCompoundAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCompoundAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCompoundAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCompoundAssignOp::pool_size - 1};
    SgCompoundAssignOp* testPointer{reinterpret_cast<SgCompoundAssignOp*>(SgCompoundAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCompoundAssignOp::pool_size * (SgCompoundAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCompoundAssignOpStroageClass
 * from its counterpart of SgCompoundAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCompoundAssignOp::initializeStorageClassArray(SgCompoundAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCompoundAssignOp::pools.begin(); block != SgCompoundAssignOp::pools.end(); ++block) {
        SgCompoundAssignOp* pointer{reinterpret_cast<SgCompoundAssignOp*>(*block)};
        for (unsigned i = 0; i < SgCompoundAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 116119 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPlusAssignOp that takes its corresponding StorageClass as parameter
 */
SgPlusAssignOp::SgPlusAssignOp(const SgPlusAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 116129 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPlusAssignOp*
SgPlusAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPlusAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPlusAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPlusAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPlusAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusAssignOp)};
        unsigned long positionInPool{localIndex % SgPlusAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPlusAssignOp::pool_size};
        returnPointer = &(((SgPlusAssignOp*)(SgPlusAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPlusAssignOp*
SgPlusAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPlusAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPlusAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPlusAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPlusAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPlusAssignOp)};
        unsigned long positionInPool{localIndex % SgPlusAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPlusAssignOp::pool_size};
        returnPointer = &(((SgPlusAssignOp*)(SgPlusAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPlusAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPlusAssignOp::pools.begin(); block != SgPlusAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPlusAssignOp*>(*block)};
        for (unsigned i = 0; i < SgPlusAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPlusAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPlusAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPlusAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgPlusAssignOp::pools.begin(); block != SgPlusAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPlusAssignOp*>(*block)};
        for (unsigned i = 0; i < SgPlusAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPlusAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPlusAssignOp::clearMemoryPool() {
    SgPlusAssignOp* tempPointer{nullptr};
    if (SgPlusAssignOp::pools.empty() == false) {
        auto block{SgPlusAssignOp::pools.begin()};
        SgPlusAssignOp::next_node = reinterpret_cast<SgPlusAssignOp*>(*block);
        for (; block != SgPlusAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPlusAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPlusAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPlusAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPlusAssignOp::pool_size-1]);
        }
    }
}

void
SgPlusAssignOp::deleteMemoryPool() {
    for (auto p: SgPlusAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgPlusAssignOp::next_node = nullptr;
    SgPlusAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPlusAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPlusAssignOp)};
    for (size_t blockIndex = SgPlusAssignOp::pools.size(); (blockIndex * SgPlusAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPlusAssignOp) = %" PRIuPTR " SgPlusAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPlusAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPlusAssignOp),SgPlusAssignOp::pool_size);
        }
#endif

        SgPlusAssignOp* pointer{static_cast<SgPlusAssignOp*>(ROSE_MALLOC(SgPlusAssignOp::pool_size * sizeof(SgPlusAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPlusAssignOp::pool_size * sizeof(SgPlusAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPlusAssignOp::pool_size * sizeof(SgPlusAssignOp));
#endif
        SgPlusAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPlusAssignOp::pool_size * sizeof(SgPlusAssignOp), V_SgPlusAssignOp));

        if (SgPlusAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPlusAssignOp* blkptr{reinterpret_cast<SgPlusAssignOp*>(SgPlusAssignOp::pools[blockIndex-1])};
                blkptr[SgPlusAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPlusAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPlusAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPlusAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPlusAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPlusAssignOp::pool_size - 1};
    SgPlusAssignOp* testPointer{reinterpret_cast<SgPlusAssignOp*>(SgPlusAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPlusAssignOp::pool_size * (SgPlusAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPlusAssignOpStroageClass
 * from its counterpart of SgPlusAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPlusAssignOp::initializeStorageClassArray(SgPlusAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPlusAssignOp::pools.begin(); block != SgPlusAssignOp::pools.end(); ++block) {
        SgPlusAssignOp* pointer{reinterpret_cast<SgPlusAssignOp*>(*block)};
        for (unsigned i = 0; i < SgPlusAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 116346 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMinusAssignOp that takes its corresponding StorageClass as parameter
 */
SgMinusAssignOp::SgMinusAssignOp(const SgMinusAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 116356 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMinusAssignOp*
SgMinusAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMinusAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMinusAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusAssignOp)};
        unsigned long positionInPool{localIndex % SgMinusAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMinusAssignOp::pool_size};
        returnPointer = &(((SgMinusAssignOp*)(SgMinusAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMinusAssignOp*
SgMinusAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMinusAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMinusAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMinusAssignOp)};
        unsigned long positionInPool{localIndex % SgMinusAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMinusAssignOp::pool_size};
        returnPointer = &(((SgMinusAssignOp*)(SgMinusAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMinusAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMinusAssignOp::pools.begin(); block != SgMinusAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMinusAssignOp*>(*block)};
        for (unsigned i = 0; i < SgMinusAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMinusAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMinusAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMinusAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgMinusAssignOp::pools.begin(); block != SgMinusAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMinusAssignOp*>(*block)};
        for (unsigned i = 0; i < SgMinusAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMinusAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMinusAssignOp::clearMemoryPool() {
    SgMinusAssignOp* tempPointer{nullptr};
    if (SgMinusAssignOp::pools.empty() == false) {
        auto block{SgMinusAssignOp::pools.begin()};
        SgMinusAssignOp::next_node = reinterpret_cast<SgMinusAssignOp*>(*block);
        for (; block != SgMinusAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMinusAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMinusAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMinusAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMinusAssignOp::pool_size-1]);
        }
    }
}

void
SgMinusAssignOp::deleteMemoryPool() {
    for (auto p: SgMinusAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgMinusAssignOp::next_node = nullptr;
    SgMinusAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMinusAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusAssignOp)};
    for (size_t blockIndex = SgMinusAssignOp::pools.size(); (blockIndex * SgMinusAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusAssignOp) = %" PRIuPTR " SgMinusAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMinusAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMinusAssignOp),SgMinusAssignOp::pool_size);
        }
#endif

        SgMinusAssignOp* pointer{static_cast<SgMinusAssignOp*>(ROSE_MALLOC(SgMinusAssignOp::pool_size * sizeof(SgMinusAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMinusAssignOp::pool_size * sizeof(SgMinusAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMinusAssignOp::pool_size * sizeof(SgMinusAssignOp));
#endif
        SgMinusAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMinusAssignOp::pool_size * sizeof(SgMinusAssignOp), V_SgMinusAssignOp));

        if (SgMinusAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMinusAssignOp* blkptr{reinterpret_cast<SgMinusAssignOp*>(SgMinusAssignOp::pools[blockIndex-1])};
                blkptr[SgMinusAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMinusAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMinusAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMinusAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMinusAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMinusAssignOp::pool_size - 1};
    SgMinusAssignOp* testPointer{reinterpret_cast<SgMinusAssignOp*>(SgMinusAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMinusAssignOp::pool_size * (SgMinusAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMinusAssignOpStroageClass
 * from its counterpart of SgMinusAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMinusAssignOp::initializeStorageClassArray(SgMinusAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMinusAssignOp::pools.begin(); block != SgMinusAssignOp::pools.end(); ++block) {
        SgMinusAssignOp* pointer{reinterpret_cast<SgMinusAssignOp*>(*block)};
        for (unsigned i = 0; i < SgMinusAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 116573 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAndAssignOp that takes its corresponding StorageClass as parameter
 */
SgAndAssignOp::SgAndAssignOp(const SgAndAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 116583 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAndAssignOp*
SgAndAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAndAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAndAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAndAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAndAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAndAssignOp)};
        unsigned long positionInPool{localIndex % SgAndAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAndAssignOp::pool_size};
        returnPointer = &(((SgAndAssignOp*)(SgAndAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAndAssignOp*
SgAndAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAndAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAndAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAndAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAndAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAndAssignOp)};
        unsigned long positionInPool{localIndex % SgAndAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAndAssignOp::pool_size};
        returnPointer = &(((SgAndAssignOp*)(SgAndAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAndAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAndAssignOp::pools.begin(); block != SgAndAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAndAssignOp*>(*block)};
        for (unsigned i = 0; i < SgAndAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAndAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAndAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAndAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAndAssignOp::pools.begin(); block != SgAndAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAndAssignOp*>(*block)};
        for (unsigned i = 0; i < SgAndAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAndAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAndAssignOp::clearMemoryPool() {
    SgAndAssignOp* tempPointer{nullptr};
    if (SgAndAssignOp::pools.empty() == false) {
        auto block{SgAndAssignOp::pools.begin()};
        SgAndAssignOp::next_node = reinterpret_cast<SgAndAssignOp*>(*block);
        for (; block != SgAndAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAndAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAndAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAndAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAndAssignOp::pool_size-1]);
        }
    }
}

void
SgAndAssignOp::deleteMemoryPool() {
    for (auto p: SgAndAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgAndAssignOp::next_node = nullptr;
    SgAndAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAndAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAndAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAndAssignOp)};
    for (size_t blockIndex = SgAndAssignOp::pools.size(); (blockIndex * SgAndAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAndAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAndAssignOp) = %" PRIuPTR " SgAndAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAndAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAndAssignOp),SgAndAssignOp::pool_size);
        }
#endif

        SgAndAssignOp* pointer{static_cast<SgAndAssignOp*>(ROSE_MALLOC(SgAndAssignOp::pool_size * sizeof(SgAndAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAndAssignOp::pool_size * sizeof(SgAndAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAndAssignOp::pool_size * sizeof(SgAndAssignOp));
#endif
        SgAndAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAndAssignOp::pool_size * sizeof(SgAndAssignOp), V_SgAndAssignOp));

        if (SgAndAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAndAssignOp* blkptr{reinterpret_cast<SgAndAssignOp*>(SgAndAssignOp::pools[blockIndex-1])};
                blkptr[SgAndAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAndAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAndAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAndAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAndAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAndAssignOp::pool_size - 1};
    SgAndAssignOp* testPointer{reinterpret_cast<SgAndAssignOp*>(SgAndAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAndAssignOp::pool_size * (SgAndAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAndAssignOpStroageClass
 * from its counterpart of SgAndAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAndAssignOp::initializeStorageClassArray(SgAndAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAndAssignOp::pools.begin(); block != SgAndAssignOp::pools.end(); ++block) {
        SgAndAssignOp* pointer{reinterpret_cast<SgAndAssignOp*>(*block)};
        for (unsigned i = 0; i < SgAndAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 116800 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIorAssignOp that takes its corresponding StorageClass as parameter
 */
SgIorAssignOp::SgIorAssignOp(const SgIorAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 116810 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIorAssignOp*
SgIorAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIorAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIorAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIorAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIorAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIorAssignOp)};
        unsigned long positionInPool{localIndex % SgIorAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIorAssignOp::pool_size};
        returnPointer = &(((SgIorAssignOp*)(SgIorAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIorAssignOp*
SgIorAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIorAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIorAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIorAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIorAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIorAssignOp)};
        unsigned long positionInPool{localIndex % SgIorAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIorAssignOp::pool_size};
        returnPointer = &(((SgIorAssignOp*)(SgIorAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIorAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIorAssignOp::pools.begin(); block != SgIorAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIorAssignOp*>(*block)};
        for (unsigned i = 0; i < SgIorAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIorAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIorAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIorAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgIorAssignOp::pools.begin(); block != SgIorAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIorAssignOp*>(*block)};
        for (unsigned i = 0; i < SgIorAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIorAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIorAssignOp::clearMemoryPool() {
    SgIorAssignOp* tempPointer{nullptr};
    if (SgIorAssignOp::pools.empty() == false) {
        auto block{SgIorAssignOp::pools.begin()};
        SgIorAssignOp::next_node = reinterpret_cast<SgIorAssignOp*>(*block);
        for (; block != SgIorAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIorAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIorAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIorAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIorAssignOp::pool_size-1]);
        }
    }
}

void
SgIorAssignOp::deleteMemoryPool() {
    for (auto p: SgIorAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgIorAssignOp::next_node = nullptr;
    SgIorAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIorAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIorAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIorAssignOp)};
    for (size_t blockIndex = SgIorAssignOp::pools.size(); (blockIndex * SgIorAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIorAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIorAssignOp) = %" PRIuPTR " SgIorAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIorAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIorAssignOp),SgIorAssignOp::pool_size);
        }
#endif

        SgIorAssignOp* pointer{static_cast<SgIorAssignOp*>(ROSE_MALLOC(SgIorAssignOp::pool_size * sizeof(SgIorAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIorAssignOp::pool_size * sizeof(SgIorAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIorAssignOp::pool_size * sizeof(SgIorAssignOp));
#endif
        SgIorAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIorAssignOp::pool_size * sizeof(SgIorAssignOp), V_SgIorAssignOp));

        if (SgIorAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIorAssignOp* blkptr{reinterpret_cast<SgIorAssignOp*>(SgIorAssignOp::pools[blockIndex-1])};
                blkptr[SgIorAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIorAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIorAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIorAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIorAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIorAssignOp::pool_size - 1};
    SgIorAssignOp* testPointer{reinterpret_cast<SgIorAssignOp*>(SgIorAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIorAssignOp::pool_size * (SgIorAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIorAssignOpStroageClass
 * from its counterpart of SgIorAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIorAssignOp::initializeStorageClassArray(SgIorAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIorAssignOp::pools.begin(); block != SgIorAssignOp::pools.end(); ++block) {
        SgIorAssignOp* pointer{reinterpret_cast<SgIorAssignOp*>(*block)};
        for (unsigned i = 0; i < SgIorAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 117027 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMultAssignOp that takes its corresponding StorageClass as parameter
 */
SgMultAssignOp::SgMultAssignOp(const SgMultAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 117037 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMultAssignOp*
SgMultAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMultAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMultAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMultAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMultAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMultAssignOp)};
        unsigned long positionInPool{localIndex % SgMultAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMultAssignOp::pool_size};
        returnPointer = &(((SgMultAssignOp*)(SgMultAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMultAssignOp*
SgMultAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMultAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMultAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMultAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMultAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMultAssignOp)};
        unsigned long positionInPool{localIndex % SgMultAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMultAssignOp::pool_size};
        returnPointer = &(((SgMultAssignOp*)(SgMultAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMultAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMultAssignOp::pools.begin(); block != SgMultAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMultAssignOp*>(*block)};
        for (unsigned i = 0; i < SgMultAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMultAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMultAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMultAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgMultAssignOp::pools.begin(); block != SgMultAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMultAssignOp*>(*block)};
        for (unsigned i = 0; i < SgMultAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMultAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMultAssignOp::clearMemoryPool() {
    SgMultAssignOp* tempPointer{nullptr};
    if (SgMultAssignOp::pools.empty() == false) {
        auto block{SgMultAssignOp::pools.begin()};
        SgMultAssignOp::next_node = reinterpret_cast<SgMultAssignOp*>(*block);
        for (; block != SgMultAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMultAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMultAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMultAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMultAssignOp::pool_size-1]);
        }
    }
}

void
SgMultAssignOp::deleteMemoryPool() {
    for (auto p: SgMultAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgMultAssignOp::next_node = nullptr;
    SgMultAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMultAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMultAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMultAssignOp)};
    for (size_t blockIndex = SgMultAssignOp::pools.size(); (blockIndex * SgMultAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMultAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMultAssignOp) = %" PRIuPTR " SgMultAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMultAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMultAssignOp),SgMultAssignOp::pool_size);
        }
#endif

        SgMultAssignOp* pointer{static_cast<SgMultAssignOp*>(ROSE_MALLOC(SgMultAssignOp::pool_size * sizeof(SgMultAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMultAssignOp::pool_size * sizeof(SgMultAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMultAssignOp::pool_size * sizeof(SgMultAssignOp));
#endif
        SgMultAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMultAssignOp::pool_size * sizeof(SgMultAssignOp), V_SgMultAssignOp));

        if (SgMultAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMultAssignOp* blkptr{reinterpret_cast<SgMultAssignOp*>(SgMultAssignOp::pools[blockIndex-1])};
                blkptr[SgMultAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMultAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMultAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMultAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMultAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMultAssignOp::pool_size - 1};
    SgMultAssignOp* testPointer{reinterpret_cast<SgMultAssignOp*>(SgMultAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMultAssignOp::pool_size * (SgMultAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMultAssignOpStroageClass
 * from its counterpart of SgMultAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMultAssignOp::initializeStorageClassArray(SgMultAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMultAssignOp::pools.begin(); block != SgMultAssignOp::pools.end(); ++block) {
        SgMultAssignOp* pointer{reinterpret_cast<SgMultAssignOp*>(*block)};
        for (unsigned i = 0; i < SgMultAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 117254 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDivAssignOp that takes its corresponding StorageClass as parameter
 */
SgDivAssignOp::SgDivAssignOp(const SgDivAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 117264 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDivAssignOp*
SgDivAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDivAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDivAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDivAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDivAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDivAssignOp)};
        unsigned long positionInPool{localIndex % SgDivAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDivAssignOp::pool_size};
        returnPointer = &(((SgDivAssignOp*)(SgDivAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDivAssignOp*
SgDivAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDivAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDivAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDivAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDivAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDivAssignOp)};
        unsigned long positionInPool{localIndex % SgDivAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDivAssignOp::pool_size};
        returnPointer = &(((SgDivAssignOp*)(SgDivAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDivAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDivAssignOp::pools.begin(); block != SgDivAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDivAssignOp*>(*block)};
        for (unsigned i = 0; i < SgDivAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDivAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDivAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDivAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgDivAssignOp::pools.begin(); block != SgDivAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDivAssignOp*>(*block)};
        for (unsigned i = 0; i < SgDivAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDivAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDivAssignOp::clearMemoryPool() {
    SgDivAssignOp* tempPointer{nullptr};
    if (SgDivAssignOp::pools.empty() == false) {
        auto block{SgDivAssignOp::pools.begin()};
        SgDivAssignOp::next_node = reinterpret_cast<SgDivAssignOp*>(*block);
        for (; block != SgDivAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDivAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDivAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDivAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDivAssignOp::pool_size-1]);
        }
    }
}

void
SgDivAssignOp::deleteMemoryPool() {
    for (auto p: SgDivAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgDivAssignOp::next_node = nullptr;
    SgDivAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDivAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDivAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDivAssignOp)};
    for (size_t blockIndex = SgDivAssignOp::pools.size(); (blockIndex * SgDivAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDivAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDivAssignOp) = %" PRIuPTR " SgDivAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDivAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDivAssignOp),SgDivAssignOp::pool_size);
        }
#endif

        SgDivAssignOp* pointer{static_cast<SgDivAssignOp*>(ROSE_MALLOC(SgDivAssignOp::pool_size * sizeof(SgDivAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDivAssignOp::pool_size * sizeof(SgDivAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDivAssignOp::pool_size * sizeof(SgDivAssignOp));
#endif
        SgDivAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDivAssignOp::pool_size * sizeof(SgDivAssignOp), V_SgDivAssignOp));

        if (SgDivAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDivAssignOp* blkptr{reinterpret_cast<SgDivAssignOp*>(SgDivAssignOp::pools[blockIndex-1])};
                blkptr[SgDivAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDivAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDivAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDivAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDivAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDivAssignOp::pool_size - 1};
    SgDivAssignOp* testPointer{reinterpret_cast<SgDivAssignOp*>(SgDivAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDivAssignOp::pool_size * (SgDivAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDivAssignOpStroageClass
 * from its counterpart of SgDivAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDivAssignOp::initializeStorageClassArray(SgDivAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDivAssignOp::pools.begin(); block != SgDivAssignOp::pools.end(); ++block) {
        SgDivAssignOp* pointer{reinterpret_cast<SgDivAssignOp*>(*block)};
        for (unsigned i = 0; i < SgDivAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 117481 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgModAssignOp that takes its corresponding StorageClass as parameter
 */
SgModAssignOp::SgModAssignOp(const SgModAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 117491 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgModAssignOp*
SgModAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgModAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgModAssignOp)};
        unsigned long positionInPool{localIndex % SgModAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModAssignOp::pool_size};
        returnPointer = &(((SgModAssignOp*)(SgModAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgModAssignOp*
SgModAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgModAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgModAssignOp)};
        unsigned long positionInPool{localIndex % SgModAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModAssignOp::pool_size};
        returnPointer = &(((SgModAssignOp*)(SgModAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgModAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgModAssignOp::pools.begin(); block != SgModAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModAssignOp*>(*block)};
        for (unsigned i = 0; i < SgModAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgModAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgModAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgModAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgModAssignOp::pools.begin(); block != SgModAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModAssignOp*>(*block)};
        for (unsigned i = 0; i < SgModAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgModAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgModAssignOp::clearMemoryPool() {
    SgModAssignOp* tempPointer{nullptr};
    if (SgModAssignOp::pools.empty() == false) {
        auto block{SgModAssignOp::pools.begin()};
        SgModAssignOp::next_node = reinterpret_cast<SgModAssignOp*>(*block);
        for (; block != SgModAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgModAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgModAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgModAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgModAssignOp::pool_size-1]);
        }
    }
}

void
SgModAssignOp::deleteMemoryPool() {
    for (auto p: SgModAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgModAssignOp::next_node = nullptr;
    SgModAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgModAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgModAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgModAssignOp)};
    for (size_t blockIndex = SgModAssignOp::pools.size(); (blockIndex * SgModAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgModAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgModAssignOp) = %" PRIuPTR " SgModAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgModAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgModAssignOp),SgModAssignOp::pool_size);
        }
#endif

        SgModAssignOp* pointer{static_cast<SgModAssignOp*>(ROSE_MALLOC(SgModAssignOp::pool_size * sizeof(SgModAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgModAssignOp::pool_size * sizeof(SgModAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgModAssignOp::pool_size * sizeof(SgModAssignOp));
#endif
        SgModAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgModAssignOp::pool_size * sizeof(SgModAssignOp), V_SgModAssignOp));

        if (SgModAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgModAssignOp* blkptr{reinterpret_cast<SgModAssignOp*>(SgModAssignOp::pools[blockIndex-1])};
                blkptr[SgModAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgModAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgModAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgModAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgModAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgModAssignOp::pool_size - 1};
    SgModAssignOp* testPointer{reinterpret_cast<SgModAssignOp*>(SgModAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgModAssignOp::pool_size * (SgModAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgModAssignOpStroageClass
 * from its counterpart of SgModAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgModAssignOp::initializeStorageClassArray(SgModAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgModAssignOp::pools.begin(); block != SgModAssignOp::pools.end(); ++block) {
        SgModAssignOp* pointer{reinterpret_cast<SgModAssignOp*>(*block)};
        for (unsigned i = 0; i < SgModAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 117708 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgXorAssignOp that takes its corresponding StorageClass as parameter
 */
SgXorAssignOp::SgXorAssignOp(const SgXorAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 117718 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgXorAssignOp*
SgXorAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgXorAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgXorAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgXorAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgXorAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgXorAssignOp)};
        unsigned long positionInPool{localIndex % SgXorAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgXorAssignOp::pool_size};
        returnPointer = &(((SgXorAssignOp*)(SgXorAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgXorAssignOp*
SgXorAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgXorAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgXorAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgXorAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgXorAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgXorAssignOp)};
        unsigned long positionInPool{localIndex % SgXorAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgXorAssignOp::pool_size};
        returnPointer = &(((SgXorAssignOp*)(SgXorAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgXorAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgXorAssignOp::pools.begin(); block != SgXorAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgXorAssignOp*>(*block)};
        for (unsigned i = 0; i < SgXorAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgXorAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgXorAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgXorAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgXorAssignOp::pools.begin(); block != SgXorAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgXorAssignOp*>(*block)};
        for (unsigned i = 0; i < SgXorAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgXorAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgXorAssignOp::clearMemoryPool() {
    SgXorAssignOp* tempPointer{nullptr};
    if (SgXorAssignOp::pools.empty() == false) {
        auto block{SgXorAssignOp::pools.begin()};
        SgXorAssignOp::next_node = reinterpret_cast<SgXorAssignOp*>(*block);
        for (; block != SgXorAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgXorAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgXorAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgXorAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgXorAssignOp::pool_size-1]);
        }
    }
}

void
SgXorAssignOp::deleteMemoryPool() {
    for (auto p: SgXorAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgXorAssignOp::next_node = nullptr;
    SgXorAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgXorAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgXorAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgXorAssignOp)};
    for (size_t blockIndex = SgXorAssignOp::pools.size(); (blockIndex * SgXorAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgXorAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgXorAssignOp) = %" PRIuPTR " SgXorAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgXorAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgXorAssignOp),SgXorAssignOp::pool_size);
        }
#endif

        SgXorAssignOp* pointer{static_cast<SgXorAssignOp*>(ROSE_MALLOC(SgXorAssignOp::pool_size * sizeof(SgXorAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgXorAssignOp::pool_size * sizeof(SgXorAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgXorAssignOp::pool_size * sizeof(SgXorAssignOp));
#endif
        SgXorAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgXorAssignOp::pool_size * sizeof(SgXorAssignOp), V_SgXorAssignOp));

        if (SgXorAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgXorAssignOp* blkptr{reinterpret_cast<SgXorAssignOp*>(SgXorAssignOp::pools[blockIndex-1])};
                blkptr[SgXorAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgXorAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgXorAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgXorAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgXorAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgXorAssignOp::pool_size - 1};
    SgXorAssignOp* testPointer{reinterpret_cast<SgXorAssignOp*>(SgXorAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgXorAssignOp::pool_size * (SgXorAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgXorAssignOpStroageClass
 * from its counterpart of SgXorAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgXorAssignOp::initializeStorageClassArray(SgXorAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgXorAssignOp::pools.begin(); block != SgXorAssignOp::pools.end(); ++block) {
        SgXorAssignOp* pointer{reinterpret_cast<SgXorAssignOp*>(*block)};
        for (unsigned i = 0; i < SgXorAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 117935 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLshiftAssignOp that takes its corresponding StorageClass as parameter
 */
SgLshiftAssignOp::SgLshiftAssignOp(const SgLshiftAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 117945 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLshiftAssignOp*
SgLshiftAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLshiftAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLshiftAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLshiftAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLshiftAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftAssignOp)};
        unsigned long positionInPool{localIndex % SgLshiftAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLshiftAssignOp::pool_size};
        returnPointer = &(((SgLshiftAssignOp*)(SgLshiftAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLshiftAssignOp*
SgLshiftAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLshiftAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLshiftAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLshiftAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLshiftAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLshiftAssignOp)};
        unsigned long positionInPool{localIndex % SgLshiftAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLshiftAssignOp::pool_size};
        returnPointer = &(((SgLshiftAssignOp*)(SgLshiftAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLshiftAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLshiftAssignOp::pools.begin(); block != SgLshiftAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgLshiftAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLshiftAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLshiftAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLshiftAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgLshiftAssignOp::pools.begin(); block != SgLshiftAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgLshiftAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLshiftAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLshiftAssignOp::clearMemoryPool() {
    SgLshiftAssignOp* tempPointer{nullptr};
    if (SgLshiftAssignOp::pools.empty() == false) {
        auto block{SgLshiftAssignOp::pools.begin()};
        SgLshiftAssignOp::next_node = reinterpret_cast<SgLshiftAssignOp*>(*block);
        for (; block != SgLshiftAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLshiftAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLshiftAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLshiftAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLshiftAssignOp::pool_size-1]);
        }
    }
}

void
SgLshiftAssignOp::deleteMemoryPool() {
    for (auto p: SgLshiftAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgLshiftAssignOp::next_node = nullptr;
    SgLshiftAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLshiftAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLshiftAssignOp)};
    for (size_t blockIndex = SgLshiftAssignOp::pools.size(); (blockIndex * SgLshiftAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLshiftAssignOp) = %" PRIuPTR " SgLshiftAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLshiftAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLshiftAssignOp),SgLshiftAssignOp::pool_size);
        }
#endif

        SgLshiftAssignOp* pointer{static_cast<SgLshiftAssignOp*>(ROSE_MALLOC(SgLshiftAssignOp::pool_size * sizeof(SgLshiftAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLshiftAssignOp::pool_size * sizeof(SgLshiftAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLshiftAssignOp::pool_size * sizeof(SgLshiftAssignOp));
#endif
        SgLshiftAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLshiftAssignOp::pool_size * sizeof(SgLshiftAssignOp), V_SgLshiftAssignOp));

        if (SgLshiftAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLshiftAssignOp* blkptr{reinterpret_cast<SgLshiftAssignOp*>(SgLshiftAssignOp::pools[blockIndex-1])};
                blkptr[SgLshiftAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLshiftAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLshiftAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLshiftAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLshiftAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLshiftAssignOp::pool_size - 1};
    SgLshiftAssignOp* testPointer{reinterpret_cast<SgLshiftAssignOp*>(SgLshiftAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLshiftAssignOp::pool_size * (SgLshiftAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLshiftAssignOpStroageClass
 * from its counterpart of SgLshiftAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLshiftAssignOp::initializeStorageClassArray(SgLshiftAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLshiftAssignOp::pools.begin(); block != SgLshiftAssignOp::pools.end(); ++block) {
        SgLshiftAssignOp* pointer{reinterpret_cast<SgLshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgLshiftAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 118162 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRshiftAssignOp that takes its corresponding StorageClass as parameter
 */
SgRshiftAssignOp::SgRshiftAssignOp(const SgRshiftAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 118172 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRshiftAssignOp*
SgRshiftAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRshiftAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRshiftAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRshiftAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRshiftAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftAssignOp)};
        unsigned long positionInPool{localIndex % SgRshiftAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRshiftAssignOp::pool_size};
        returnPointer = &(((SgRshiftAssignOp*)(SgRshiftAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRshiftAssignOp*
SgRshiftAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRshiftAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRshiftAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRshiftAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRshiftAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRshiftAssignOp)};
        unsigned long positionInPool{localIndex % SgRshiftAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRshiftAssignOp::pool_size};
        returnPointer = &(((SgRshiftAssignOp*)(SgRshiftAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRshiftAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRshiftAssignOp::pools.begin(); block != SgRshiftAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgRshiftAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRshiftAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRshiftAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRshiftAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgRshiftAssignOp::pools.begin(); block != SgRshiftAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgRshiftAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRshiftAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRshiftAssignOp::clearMemoryPool() {
    SgRshiftAssignOp* tempPointer{nullptr};
    if (SgRshiftAssignOp::pools.empty() == false) {
        auto block{SgRshiftAssignOp::pools.begin()};
        SgRshiftAssignOp::next_node = reinterpret_cast<SgRshiftAssignOp*>(*block);
        for (; block != SgRshiftAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRshiftAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRshiftAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRshiftAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRshiftAssignOp::pool_size-1]);
        }
    }
}

void
SgRshiftAssignOp::deleteMemoryPool() {
    for (auto p: SgRshiftAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgRshiftAssignOp::next_node = nullptr;
    SgRshiftAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRshiftAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRshiftAssignOp)};
    for (size_t blockIndex = SgRshiftAssignOp::pools.size(); (blockIndex * SgRshiftAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRshiftAssignOp) = %" PRIuPTR " SgRshiftAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRshiftAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRshiftAssignOp),SgRshiftAssignOp::pool_size);
        }
#endif

        SgRshiftAssignOp* pointer{static_cast<SgRshiftAssignOp*>(ROSE_MALLOC(SgRshiftAssignOp::pool_size * sizeof(SgRshiftAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRshiftAssignOp::pool_size * sizeof(SgRshiftAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRshiftAssignOp::pool_size * sizeof(SgRshiftAssignOp));
#endif
        SgRshiftAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRshiftAssignOp::pool_size * sizeof(SgRshiftAssignOp), V_SgRshiftAssignOp));

        if (SgRshiftAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRshiftAssignOp* blkptr{reinterpret_cast<SgRshiftAssignOp*>(SgRshiftAssignOp::pools[blockIndex-1])};
                blkptr[SgRshiftAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRshiftAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRshiftAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRshiftAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRshiftAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRshiftAssignOp::pool_size - 1};
    SgRshiftAssignOp* testPointer{reinterpret_cast<SgRshiftAssignOp*>(SgRshiftAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRshiftAssignOp::pool_size * (SgRshiftAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRshiftAssignOpStroageClass
 * from its counterpart of SgRshiftAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRshiftAssignOp::initializeStorageClassArray(SgRshiftAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRshiftAssignOp::pools.begin(); block != SgRshiftAssignOp::pools.end(); ++block) {
        SgRshiftAssignOp* pointer{reinterpret_cast<SgRshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgRshiftAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 118389 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaUnsignedRshiftAssignOp that takes its corresponding StorageClass as parameter
 */
SgJavaUnsignedRshiftAssignOp::SgJavaUnsignedRshiftAssignOp(const SgJavaUnsignedRshiftAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 118399 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaUnsignedRshiftAssignOp*
SgJavaUnsignedRshiftAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaUnsignedRshiftAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnsignedRshiftAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnsignedRshiftAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaUnsignedRshiftAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftAssignOp)};
        unsigned long positionInPool{localIndex % SgJavaUnsignedRshiftAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaUnsignedRshiftAssignOp::pool_size};
        returnPointer = &(((SgJavaUnsignedRshiftAssignOp*)(SgJavaUnsignedRshiftAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaUnsignedRshiftAssignOp*
SgJavaUnsignedRshiftAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaUnsignedRshiftAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnsignedRshiftAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnsignedRshiftAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaUnsignedRshiftAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaUnsignedRshiftAssignOp)};
        unsigned long positionInPool{localIndex % SgJavaUnsignedRshiftAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaUnsignedRshiftAssignOp::pool_size};
        returnPointer = &(((SgJavaUnsignedRshiftAssignOp*)(SgJavaUnsignedRshiftAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaUnsignedRshiftAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaUnsignedRshiftAssignOp::pools.begin(); block != SgJavaUnsignedRshiftAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaUnsignedRshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgJavaUnsignedRshiftAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaUnsignedRshiftAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaUnsignedRshiftAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaUnsignedRshiftAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaUnsignedRshiftAssignOp::pools.begin(); block != SgJavaUnsignedRshiftAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaUnsignedRshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgJavaUnsignedRshiftAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaUnsignedRshiftAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaUnsignedRshiftAssignOp::clearMemoryPool() {
    SgJavaUnsignedRshiftAssignOp* tempPointer{nullptr};
    if (SgJavaUnsignedRshiftAssignOp::pools.empty() == false) {
        auto block{SgJavaUnsignedRshiftAssignOp::pools.begin()};
        SgJavaUnsignedRshiftAssignOp::next_node = reinterpret_cast<SgJavaUnsignedRshiftAssignOp*>(*block);
        for (; block != SgJavaUnsignedRshiftAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaUnsignedRshiftAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaUnsignedRshiftAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaUnsignedRshiftAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaUnsignedRshiftAssignOp::pool_size-1]);
        }
    }
}

void
SgJavaUnsignedRshiftAssignOp::deleteMemoryPool() {
    for (auto p: SgJavaUnsignedRshiftAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgJavaUnsignedRshiftAssignOp::next_node = nullptr;
    SgJavaUnsignedRshiftAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaUnsignedRshiftAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnsignedRshiftAssignOp)};
    for (size_t blockIndex = SgJavaUnsignedRshiftAssignOp::pools.size(); (blockIndex * SgJavaUnsignedRshiftAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnsignedRshiftAssignOp) = %" PRIuPTR " SgJavaUnsignedRshiftAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaUnsignedRshiftAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaUnsignedRshiftAssignOp),SgJavaUnsignedRshiftAssignOp::pool_size);
        }
#endif

        SgJavaUnsignedRshiftAssignOp* pointer{static_cast<SgJavaUnsignedRshiftAssignOp*>(ROSE_MALLOC(SgJavaUnsignedRshiftAssignOp::pool_size * sizeof(SgJavaUnsignedRshiftAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaUnsignedRshiftAssignOp::pool_size * sizeof(SgJavaUnsignedRshiftAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaUnsignedRshiftAssignOp::pool_size * sizeof(SgJavaUnsignedRshiftAssignOp));
#endif
        SgJavaUnsignedRshiftAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaUnsignedRshiftAssignOp::pool_size * sizeof(SgJavaUnsignedRshiftAssignOp), V_SgJavaUnsignedRshiftAssignOp));

        if (SgJavaUnsignedRshiftAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaUnsignedRshiftAssignOp* blkptr{reinterpret_cast<SgJavaUnsignedRshiftAssignOp*>(SgJavaUnsignedRshiftAssignOp::pools[blockIndex-1])};
                blkptr[SgJavaUnsignedRshiftAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaUnsignedRshiftAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaUnsignedRshiftAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaUnsignedRshiftAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaUnsignedRshiftAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaUnsignedRshiftAssignOp::pool_size - 1};
    SgJavaUnsignedRshiftAssignOp* testPointer{reinterpret_cast<SgJavaUnsignedRshiftAssignOp*>(SgJavaUnsignedRshiftAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaUnsignedRshiftAssignOp::pool_size * (SgJavaUnsignedRshiftAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaUnsignedRshiftAssignOpStroageClass
 * from its counterpart of SgJavaUnsignedRshiftAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaUnsignedRshiftAssignOp::initializeStorageClassArray(SgJavaUnsignedRshiftAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaUnsignedRshiftAssignOp::pools.begin(); block != SgJavaUnsignedRshiftAssignOp::pools.end(); ++block) {
        SgJavaUnsignedRshiftAssignOp* pointer{reinterpret_cast<SgJavaUnsignedRshiftAssignOp*>(*block)};
        for (unsigned i = 0; i < SgJavaUnsignedRshiftAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 118616 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIntegerDivideAssignOp that takes its corresponding StorageClass as parameter
 */
SgIntegerDivideAssignOp::SgIntegerDivideAssignOp(const SgIntegerDivideAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 118626 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIntegerDivideAssignOp*
SgIntegerDivideAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIntegerDivideAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegerDivideAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegerDivideAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntegerDivideAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideAssignOp)};
        unsigned long positionInPool{localIndex % SgIntegerDivideAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntegerDivideAssignOp::pool_size};
        returnPointer = &(((SgIntegerDivideAssignOp*)(SgIntegerDivideAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIntegerDivideAssignOp*
SgIntegerDivideAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIntegerDivideAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegerDivideAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegerDivideAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntegerDivideAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIntegerDivideAssignOp)};
        unsigned long positionInPool{localIndex % SgIntegerDivideAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntegerDivideAssignOp::pool_size};
        returnPointer = &(((SgIntegerDivideAssignOp*)(SgIntegerDivideAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIntegerDivideAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIntegerDivideAssignOp::pools.begin(); block != SgIntegerDivideAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntegerDivideAssignOp*>(*block)};
        for (unsigned i = 0; i < SgIntegerDivideAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIntegerDivideAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIntegerDivideAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIntegerDivideAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgIntegerDivideAssignOp::pools.begin(); block != SgIntegerDivideAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntegerDivideAssignOp*>(*block)};
        for (unsigned i = 0; i < SgIntegerDivideAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIntegerDivideAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIntegerDivideAssignOp::clearMemoryPool() {
    SgIntegerDivideAssignOp* tempPointer{nullptr};
    if (SgIntegerDivideAssignOp::pools.empty() == false) {
        auto block{SgIntegerDivideAssignOp::pools.begin()};
        SgIntegerDivideAssignOp::next_node = reinterpret_cast<SgIntegerDivideAssignOp*>(*block);
        for (; block != SgIntegerDivideAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIntegerDivideAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIntegerDivideAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIntegerDivideAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIntegerDivideAssignOp::pool_size-1]);
        }
    }
}

void
SgIntegerDivideAssignOp::deleteMemoryPool() {
    for (auto p: SgIntegerDivideAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgIntegerDivideAssignOp::next_node = nullptr;
    SgIntegerDivideAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIntegerDivideAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegerDivideAssignOp)};
    for (size_t blockIndex = SgIntegerDivideAssignOp::pools.size(); (blockIndex * SgIntegerDivideAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegerDivideAssignOp) = %" PRIuPTR " SgIntegerDivideAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIntegerDivideAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntegerDivideAssignOp),SgIntegerDivideAssignOp::pool_size);
        }
#endif

        SgIntegerDivideAssignOp* pointer{static_cast<SgIntegerDivideAssignOp*>(ROSE_MALLOC(SgIntegerDivideAssignOp::pool_size * sizeof(SgIntegerDivideAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIntegerDivideAssignOp::pool_size * sizeof(SgIntegerDivideAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIntegerDivideAssignOp::pool_size * sizeof(SgIntegerDivideAssignOp));
#endif
        SgIntegerDivideAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIntegerDivideAssignOp::pool_size * sizeof(SgIntegerDivideAssignOp), V_SgIntegerDivideAssignOp));

        if (SgIntegerDivideAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIntegerDivideAssignOp* blkptr{reinterpret_cast<SgIntegerDivideAssignOp*>(SgIntegerDivideAssignOp::pools[blockIndex-1])};
                blkptr[SgIntegerDivideAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIntegerDivideAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIntegerDivideAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIntegerDivideAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIntegerDivideAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIntegerDivideAssignOp::pool_size - 1};
    SgIntegerDivideAssignOp* testPointer{reinterpret_cast<SgIntegerDivideAssignOp*>(SgIntegerDivideAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIntegerDivideAssignOp::pool_size * (SgIntegerDivideAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIntegerDivideAssignOpStroageClass
 * from its counterpart of SgIntegerDivideAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIntegerDivideAssignOp::initializeStorageClassArray(SgIntegerDivideAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIntegerDivideAssignOp::pools.begin(); block != SgIntegerDivideAssignOp::pools.end(); ++block) {
        SgIntegerDivideAssignOp* pointer{reinterpret_cast<SgIntegerDivideAssignOp*>(*block)};
        for (unsigned i = 0; i < SgIntegerDivideAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 118843 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExponentiationAssignOp that takes its corresponding StorageClass as parameter
 */
SgExponentiationAssignOp::SgExponentiationAssignOp(const SgExponentiationAssignOpStorageClass& storageSource)  : SgCompoundAssignOp (storageSource) {

/* #line 118853 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExponentiationAssignOp*
SgExponentiationAssignOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExponentiationAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExponentiationAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExponentiationAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExponentiationAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationAssignOp)};
        unsigned long positionInPool{localIndex % SgExponentiationAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExponentiationAssignOp::pool_size};
        returnPointer = &(((SgExponentiationAssignOp*)(SgExponentiationAssignOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExponentiationAssignOp*
SgExponentiationAssignOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExponentiationAssignOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExponentiationAssignOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExponentiationAssignOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExponentiationAssignOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExponentiationAssignOp)};
        unsigned long positionInPool{localIndex % SgExponentiationAssignOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExponentiationAssignOp::pool_size};
        returnPointer = &(((SgExponentiationAssignOp*)(SgExponentiationAssignOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExponentiationAssignOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExponentiationAssignOp::pools.begin(); block != SgExponentiationAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExponentiationAssignOp*>(*block)};
        for (unsigned i = 0; i < SgExponentiationAssignOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExponentiationAssignOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExponentiationAssignOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExponentiationAssignOp* pointerOfLinkedList{nullptr};
    for (auto block = SgExponentiationAssignOp::pools.begin(); block != SgExponentiationAssignOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExponentiationAssignOp*>(*block)};
        for (unsigned i = 0; i < SgExponentiationAssignOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExponentiationAssignOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExponentiationAssignOp::clearMemoryPool() {
    SgExponentiationAssignOp* tempPointer{nullptr};
    if (SgExponentiationAssignOp::pools.empty() == false) {
        auto block{SgExponentiationAssignOp::pools.begin()};
        SgExponentiationAssignOp::next_node = reinterpret_cast<SgExponentiationAssignOp*>(*block);
        for (; block != SgExponentiationAssignOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExponentiationAssignOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExponentiationAssignOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExponentiationAssignOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExponentiationAssignOp::pool_size-1]);
        }
    }
}

void
SgExponentiationAssignOp::deleteMemoryPool() {
    for (auto p: SgExponentiationAssignOp::pools) {
        ROSE_FREE(p);
    }
    SgExponentiationAssignOp::next_node = nullptr;
    SgExponentiationAssignOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExponentiationAssignOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationAssignOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExponentiationAssignOp)};
    for (size_t blockIndex = SgExponentiationAssignOp::pools.size(); (blockIndex * SgExponentiationAssignOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationAssignOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExponentiationAssignOp) = %" PRIuPTR " SgExponentiationAssignOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExponentiationAssignOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExponentiationAssignOp),SgExponentiationAssignOp::pool_size);
        }
#endif

        SgExponentiationAssignOp* pointer{static_cast<SgExponentiationAssignOp*>(ROSE_MALLOC(SgExponentiationAssignOp::pool_size * sizeof(SgExponentiationAssignOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExponentiationAssignOp::pool_size * sizeof(SgExponentiationAssignOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExponentiationAssignOp::pool_size * sizeof(SgExponentiationAssignOp));
#endif
        SgExponentiationAssignOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExponentiationAssignOp::pool_size * sizeof(SgExponentiationAssignOp), V_SgExponentiationAssignOp));

        if (SgExponentiationAssignOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExponentiationAssignOp* blkptr{reinterpret_cast<SgExponentiationAssignOp*>(SgExponentiationAssignOp::pools[blockIndex-1])};
                blkptr[SgExponentiationAssignOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExponentiationAssignOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExponentiationAssignOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExponentiationAssignOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExponentiationAssignOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExponentiationAssignOp::pool_size - 1};
    SgExponentiationAssignOp* testPointer{reinterpret_cast<SgExponentiationAssignOp*>(SgExponentiationAssignOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExponentiationAssignOp::pool_size * (SgExponentiationAssignOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExponentiationAssignOpStroageClass
 * from its counterpart of SgExponentiationAssignOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExponentiationAssignOp::initializeStorageClassArray(SgExponentiationAssignOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExponentiationAssignOp::pools.begin(); block != SgExponentiationAssignOp::pools.end(); ++block) {
        SgExponentiationAssignOp* pointer{reinterpret_cast<SgExponentiationAssignOp*>(*block)};
        for (unsigned i = 0; i < SgExponentiationAssignOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 119070 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMembershipOp that takes its corresponding StorageClass as parameter
 */
SgMembershipOp::SgMembershipOp(const SgMembershipOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 119080 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMembershipOp*
SgMembershipOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMembershipOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMembershipOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMembershipOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMembershipOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMembershipOp)};
        unsigned long positionInPool{localIndex % SgMembershipOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMembershipOp::pool_size};
        returnPointer = &(((SgMembershipOp*)(SgMembershipOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMembershipOp*
SgMembershipOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMembershipOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMembershipOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMembershipOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMembershipOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMembershipOp)};
        unsigned long positionInPool{localIndex % SgMembershipOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMembershipOp::pool_size};
        returnPointer = &(((SgMembershipOp*)(SgMembershipOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMembershipOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMembershipOp::pools.begin(); block != SgMembershipOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMembershipOp*>(*block)};
        for (unsigned i = 0; i < SgMembershipOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMembershipOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMembershipOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMembershipOp* pointerOfLinkedList{nullptr};
    for (auto block = SgMembershipOp::pools.begin(); block != SgMembershipOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMembershipOp*>(*block)};
        for (unsigned i = 0; i < SgMembershipOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMembershipOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMembershipOp::clearMemoryPool() {
    SgMembershipOp* tempPointer{nullptr};
    if (SgMembershipOp::pools.empty() == false) {
        auto block{SgMembershipOp::pools.begin()};
        SgMembershipOp::next_node = reinterpret_cast<SgMembershipOp*>(*block);
        for (; block != SgMembershipOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMembershipOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMembershipOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMembershipOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMembershipOp::pool_size-1]);
        }
    }
}

void
SgMembershipOp::deleteMemoryPool() {
    for (auto p: SgMembershipOp::pools) {
        ROSE_FREE(p);
    }
    SgMembershipOp::next_node = nullptr;
    SgMembershipOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMembershipOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMembershipOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMembershipOp)};
    for (size_t blockIndex = SgMembershipOp::pools.size(); (blockIndex * SgMembershipOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMembershipOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMembershipOp) = %" PRIuPTR " SgMembershipOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMembershipOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMembershipOp),SgMembershipOp::pool_size);
        }
#endif

        SgMembershipOp* pointer{static_cast<SgMembershipOp*>(ROSE_MALLOC(SgMembershipOp::pool_size * sizeof(SgMembershipOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMembershipOp::pool_size * sizeof(SgMembershipOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMembershipOp::pool_size * sizeof(SgMembershipOp));
#endif
        SgMembershipOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMembershipOp::pool_size * sizeof(SgMembershipOp), V_SgMembershipOp));

        if (SgMembershipOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMembershipOp* blkptr{reinterpret_cast<SgMembershipOp*>(SgMembershipOp::pools[blockIndex-1])};
                blkptr[SgMembershipOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMembershipOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMembershipOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMembershipOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMembershipOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMembershipOp::pool_size - 1};
    SgMembershipOp* testPointer{reinterpret_cast<SgMembershipOp*>(SgMembershipOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMembershipOp::pool_size * (SgMembershipOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMembershipOpStroageClass
 * from its counterpart of SgMembershipOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMembershipOp::initializeStorageClassArray(SgMembershipOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMembershipOp::pools.begin(); block != SgMembershipOp::pools.end(); ++block) {
        SgMembershipOp* pointer{reinterpret_cast<SgMembershipOp*>(*block)};
        for (unsigned i = 0; i < SgMembershipOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 119297 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSpaceshipOp that takes its corresponding StorageClass as parameter
 */
SgSpaceshipOp::SgSpaceshipOp(const SgSpaceshipOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 119307 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSpaceshipOp*
SgSpaceshipOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSpaceshipOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpaceshipOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpaceshipOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSpaceshipOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSpaceshipOp)};
        unsigned long positionInPool{localIndex % SgSpaceshipOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSpaceshipOp::pool_size};
        returnPointer = &(((SgSpaceshipOp*)(SgSpaceshipOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSpaceshipOp*
SgSpaceshipOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSpaceshipOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpaceshipOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpaceshipOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSpaceshipOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSpaceshipOp)};
        unsigned long positionInPool{localIndex % SgSpaceshipOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSpaceshipOp::pool_size};
        returnPointer = &(((SgSpaceshipOp*)(SgSpaceshipOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSpaceshipOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSpaceshipOp::pools.begin(); block != SgSpaceshipOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSpaceshipOp*>(*block)};
        for (unsigned i = 0; i < SgSpaceshipOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSpaceshipOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSpaceshipOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSpaceshipOp* pointerOfLinkedList{nullptr};
    for (auto block = SgSpaceshipOp::pools.begin(); block != SgSpaceshipOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSpaceshipOp*>(*block)};
        for (unsigned i = 0; i < SgSpaceshipOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSpaceshipOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSpaceshipOp::clearMemoryPool() {
    SgSpaceshipOp* tempPointer{nullptr};
    if (SgSpaceshipOp::pools.empty() == false) {
        auto block{SgSpaceshipOp::pools.begin()};
        SgSpaceshipOp::next_node = reinterpret_cast<SgSpaceshipOp*>(*block);
        for (; block != SgSpaceshipOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSpaceshipOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSpaceshipOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSpaceshipOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSpaceshipOp::pool_size-1]);
        }
    }
}

void
SgSpaceshipOp::deleteMemoryPool() {
    for (auto p: SgSpaceshipOp::pools) {
        ROSE_FREE(p);
    }
    SgSpaceshipOp::next_node = nullptr;
    SgSpaceshipOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSpaceshipOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSpaceshipOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpaceshipOp)};
    for (size_t blockIndex = SgSpaceshipOp::pools.size(); (blockIndex * SgSpaceshipOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSpaceshipOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpaceshipOp) = %" PRIuPTR " SgSpaceshipOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSpaceshipOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSpaceshipOp),SgSpaceshipOp::pool_size);
        }
#endif

        SgSpaceshipOp* pointer{static_cast<SgSpaceshipOp*>(ROSE_MALLOC(SgSpaceshipOp::pool_size * sizeof(SgSpaceshipOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSpaceshipOp::pool_size * sizeof(SgSpaceshipOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSpaceshipOp::pool_size * sizeof(SgSpaceshipOp));
#endif
        SgSpaceshipOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSpaceshipOp::pool_size * sizeof(SgSpaceshipOp), V_SgSpaceshipOp));

        if (SgSpaceshipOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSpaceshipOp* blkptr{reinterpret_cast<SgSpaceshipOp*>(SgSpaceshipOp::pools[blockIndex-1])};
                blkptr[SgSpaceshipOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSpaceshipOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSpaceshipOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSpaceshipOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSpaceshipOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSpaceshipOp::pool_size - 1};
    SgSpaceshipOp* testPointer{reinterpret_cast<SgSpaceshipOp*>(SgSpaceshipOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSpaceshipOp::pool_size * (SgSpaceshipOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSpaceshipOpStroageClass
 * from its counterpart of SgSpaceshipOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSpaceshipOp::initializeStorageClassArray(SgSpaceshipOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSpaceshipOp::pools.begin(); block != SgSpaceshipOp::pools.end(); ++block) {
        SgSpaceshipOp* pointer{reinterpret_cast<SgSpaceshipOp*>(*block)};
        for (unsigned i = 0; i < SgSpaceshipOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 119524 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNonMembershipOp that takes its corresponding StorageClass as parameter
 */
SgNonMembershipOp::SgNonMembershipOp(const SgNonMembershipOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 119534 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNonMembershipOp*
SgNonMembershipOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNonMembershipOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonMembershipOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonMembershipOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonMembershipOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNonMembershipOp)};
        unsigned long positionInPool{localIndex % SgNonMembershipOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonMembershipOp::pool_size};
        returnPointer = &(((SgNonMembershipOp*)(SgNonMembershipOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNonMembershipOp*
SgNonMembershipOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNonMembershipOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonMembershipOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonMembershipOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonMembershipOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNonMembershipOp)};
        unsigned long positionInPool{localIndex % SgNonMembershipOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonMembershipOp::pool_size};
        returnPointer = &(((SgNonMembershipOp*)(SgNonMembershipOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNonMembershipOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNonMembershipOp::pools.begin(); block != SgNonMembershipOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonMembershipOp*>(*block)};
        for (unsigned i = 0; i < SgNonMembershipOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNonMembershipOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNonMembershipOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNonMembershipOp* pointerOfLinkedList{nullptr};
    for (auto block = SgNonMembershipOp::pools.begin(); block != SgNonMembershipOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonMembershipOp*>(*block)};
        for (unsigned i = 0; i < SgNonMembershipOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNonMembershipOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNonMembershipOp::clearMemoryPool() {
    SgNonMembershipOp* tempPointer{nullptr};
    if (SgNonMembershipOp::pools.empty() == false) {
        auto block{SgNonMembershipOp::pools.begin()};
        SgNonMembershipOp::next_node = reinterpret_cast<SgNonMembershipOp*>(*block);
        for (; block != SgNonMembershipOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNonMembershipOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNonMembershipOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNonMembershipOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNonMembershipOp::pool_size-1]);
        }
    }
}

void
SgNonMembershipOp::deleteMemoryPool() {
    for (auto p: SgNonMembershipOp::pools) {
        ROSE_FREE(p);
    }
    SgNonMembershipOp::next_node = nullptr;
    SgNonMembershipOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNonMembershipOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNonMembershipOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonMembershipOp)};
    for (size_t blockIndex = SgNonMembershipOp::pools.size(); (blockIndex * SgNonMembershipOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNonMembershipOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonMembershipOp) = %" PRIuPTR " SgNonMembershipOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNonMembershipOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonMembershipOp),SgNonMembershipOp::pool_size);
        }
#endif

        SgNonMembershipOp* pointer{static_cast<SgNonMembershipOp*>(ROSE_MALLOC(SgNonMembershipOp::pool_size * sizeof(SgNonMembershipOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNonMembershipOp::pool_size * sizeof(SgNonMembershipOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNonMembershipOp::pool_size * sizeof(SgNonMembershipOp));
#endif
        SgNonMembershipOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNonMembershipOp::pool_size * sizeof(SgNonMembershipOp), V_SgNonMembershipOp));

        if (SgNonMembershipOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNonMembershipOp* blkptr{reinterpret_cast<SgNonMembershipOp*>(SgNonMembershipOp::pools[blockIndex-1])};
                blkptr[SgNonMembershipOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNonMembershipOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNonMembershipOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNonMembershipOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNonMembershipOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNonMembershipOp::pool_size - 1};
    SgNonMembershipOp* testPointer{reinterpret_cast<SgNonMembershipOp*>(SgNonMembershipOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNonMembershipOp::pool_size * (SgNonMembershipOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNonMembershipOpStroageClass
 * from its counterpart of SgNonMembershipOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNonMembershipOp::initializeStorageClassArray(SgNonMembershipOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNonMembershipOp::pools.begin(); block != SgNonMembershipOp::pools.end(); ++block) {
        SgNonMembershipOp* pointer{reinterpret_cast<SgNonMembershipOp*>(*block)};
        for (unsigned i = 0; i < SgNonMembershipOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 119751 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIsOp that takes its corresponding StorageClass as parameter
 */
SgIsOp::SgIsOp(const SgIsOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 119761 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIsOp*
SgIsOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIsOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIsOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIsOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIsOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIsOp)};
        unsigned long positionInPool{localIndex % SgIsOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIsOp::pool_size};
        returnPointer = &(((SgIsOp*)(SgIsOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIsOp*
SgIsOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIsOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIsOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIsOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIsOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIsOp)};
        unsigned long positionInPool{localIndex % SgIsOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIsOp::pool_size};
        returnPointer = &(((SgIsOp*)(SgIsOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIsOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIsOp::pools.begin(); block != SgIsOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIsOp*>(*block)};
        for (unsigned i = 0; i < SgIsOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIsOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIsOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIsOp* pointerOfLinkedList{nullptr};
    for (auto block = SgIsOp::pools.begin(); block != SgIsOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIsOp*>(*block)};
        for (unsigned i = 0; i < SgIsOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIsOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIsOp::clearMemoryPool() {
    SgIsOp* tempPointer{nullptr};
    if (SgIsOp::pools.empty() == false) {
        auto block{SgIsOp::pools.begin()};
        SgIsOp::next_node = reinterpret_cast<SgIsOp*>(*block);
        for (; block != SgIsOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIsOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIsOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIsOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIsOp::pool_size-1]);
        }
    }
}

void
SgIsOp::deleteMemoryPool() {
    for (auto p: SgIsOp::pools) {
        ROSE_FREE(p);
    }
    SgIsOp::next_node = nullptr;
    SgIsOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIsOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIsOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIsOp)};
    for (size_t blockIndex = SgIsOp::pools.size(); (blockIndex * SgIsOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIsOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIsOp) = %" PRIuPTR " SgIsOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIsOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIsOp),SgIsOp::pool_size);
        }
#endif

        SgIsOp* pointer{static_cast<SgIsOp*>(ROSE_MALLOC(SgIsOp::pool_size * sizeof(SgIsOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIsOp::pool_size * sizeof(SgIsOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIsOp::pool_size * sizeof(SgIsOp));
#endif
        SgIsOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIsOp::pool_size * sizeof(SgIsOp), V_SgIsOp));

        if (SgIsOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIsOp* blkptr{reinterpret_cast<SgIsOp*>(SgIsOp::pools[blockIndex-1])};
                blkptr[SgIsOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIsOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIsOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIsOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIsOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIsOp::pool_size - 1};
    SgIsOp* testPointer{reinterpret_cast<SgIsOp*>(SgIsOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIsOp::pool_size * (SgIsOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIsOpStroageClass
 * from its counterpart of SgIsOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIsOp::initializeStorageClassArray(SgIsOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIsOp::pools.begin(); block != SgIsOp::pools.end(); ++block) {
        SgIsOp* pointer{reinterpret_cast<SgIsOp*>(*block)};
        for (unsigned i = 0; i < SgIsOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 119978 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIsNotOp that takes its corresponding StorageClass as parameter
 */
SgIsNotOp::SgIsNotOp(const SgIsNotOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 119988 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIsNotOp*
SgIsNotOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIsNotOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIsNotOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIsNotOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIsNotOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIsNotOp)};
        unsigned long positionInPool{localIndex % SgIsNotOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIsNotOp::pool_size};
        returnPointer = &(((SgIsNotOp*)(SgIsNotOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIsNotOp*
SgIsNotOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIsNotOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIsNotOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIsNotOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIsNotOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIsNotOp)};
        unsigned long positionInPool{localIndex % SgIsNotOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIsNotOp::pool_size};
        returnPointer = &(((SgIsNotOp*)(SgIsNotOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIsNotOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIsNotOp::pools.begin(); block != SgIsNotOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIsNotOp*>(*block)};
        for (unsigned i = 0; i < SgIsNotOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIsNotOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIsNotOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIsNotOp* pointerOfLinkedList{nullptr};
    for (auto block = SgIsNotOp::pools.begin(); block != SgIsNotOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIsNotOp*>(*block)};
        for (unsigned i = 0; i < SgIsNotOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIsNotOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIsNotOp::clearMemoryPool() {
    SgIsNotOp* tempPointer{nullptr};
    if (SgIsNotOp::pools.empty() == false) {
        auto block{SgIsNotOp::pools.begin()};
        SgIsNotOp::next_node = reinterpret_cast<SgIsNotOp*>(*block);
        for (; block != SgIsNotOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIsNotOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIsNotOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIsNotOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIsNotOp::pool_size-1]);
        }
    }
}

void
SgIsNotOp::deleteMemoryPool() {
    for (auto p: SgIsNotOp::pools) {
        ROSE_FREE(p);
    }
    SgIsNotOp::next_node = nullptr;
    SgIsNotOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIsNotOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIsNotOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIsNotOp)};
    for (size_t blockIndex = SgIsNotOp::pools.size(); (blockIndex * SgIsNotOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIsNotOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIsNotOp) = %" PRIuPTR " SgIsNotOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIsNotOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIsNotOp),SgIsNotOp::pool_size);
        }
#endif

        SgIsNotOp* pointer{static_cast<SgIsNotOp*>(ROSE_MALLOC(SgIsNotOp::pool_size * sizeof(SgIsNotOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIsNotOp::pool_size * sizeof(SgIsNotOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIsNotOp::pool_size * sizeof(SgIsNotOp));
#endif
        SgIsNotOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIsNotOp::pool_size * sizeof(SgIsNotOp), V_SgIsNotOp));

        if (SgIsNotOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIsNotOp* blkptr{reinterpret_cast<SgIsNotOp*>(SgIsNotOp::pools[blockIndex-1])};
                blkptr[SgIsNotOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIsNotOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIsNotOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIsNotOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIsNotOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIsNotOp::pool_size - 1};
    SgIsNotOp* testPointer{reinterpret_cast<SgIsNotOp*>(SgIsNotOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIsNotOp::pool_size * (SgIsNotOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIsNotOpStroageClass
 * from its counterpart of SgIsNotOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIsNotOp::initializeStorageClassArray(SgIsNotOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIsNotOp::pools.begin(); block != SgIsNotOp::pools.end(); ++block) {
        SgIsNotOp* pointer{reinterpret_cast<SgIsNotOp*>(*block)};
        for (unsigned i = 0; i < SgIsNotOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 120205 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDotDotExp that takes its corresponding StorageClass as parameter
 */
SgDotDotExp::SgDotDotExp(const SgDotDotExpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 120215 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDotDotExp*
SgDotDotExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDotDotExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotDotExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotDotExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDotDotExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDotDotExp)};
        unsigned long positionInPool{localIndex % SgDotDotExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDotDotExp::pool_size};
        returnPointer = &(((SgDotDotExp*)(SgDotDotExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDotDotExp*
SgDotDotExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDotDotExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotDotExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotDotExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDotDotExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDotDotExp)};
        unsigned long positionInPool{localIndex % SgDotDotExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDotDotExp::pool_size};
        returnPointer = &(((SgDotDotExp*)(SgDotDotExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDotDotExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDotDotExp::pools.begin(); block != SgDotDotExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDotDotExp*>(*block)};
        for (unsigned i = 0; i < SgDotDotExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDotDotExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDotDotExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDotDotExp* pointerOfLinkedList{nullptr};
    for (auto block = SgDotDotExp::pools.begin(); block != SgDotDotExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDotDotExp*>(*block)};
        for (unsigned i = 0; i < SgDotDotExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDotDotExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDotDotExp::clearMemoryPool() {
    SgDotDotExp* tempPointer{nullptr};
    if (SgDotDotExp::pools.empty() == false) {
        auto block{SgDotDotExp::pools.begin()};
        SgDotDotExp::next_node = reinterpret_cast<SgDotDotExp*>(*block);
        for (; block != SgDotDotExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDotDotExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDotDotExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDotDotExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDotDotExp::pool_size-1]);
        }
    }
}

void
SgDotDotExp::deleteMemoryPool() {
    for (auto p: SgDotDotExp::pools) {
        ROSE_FREE(p);
    }
    SgDotDotExp::next_node = nullptr;
    SgDotDotExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDotDotExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDotDotExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotDotExp)};
    for (size_t blockIndex = SgDotDotExp::pools.size(); (blockIndex * SgDotDotExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDotDotExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotDotExp) = %" PRIuPTR " SgDotDotExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDotDotExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDotDotExp),SgDotDotExp::pool_size);
        }
#endif

        SgDotDotExp* pointer{static_cast<SgDotDotExp*>(ROSE_MALLOC(SgDotDotExp::pool_size * sizeof(SgDotDotExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDotDotExp::pool_size * sizeof(SgDotDotExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDotDotExp::pool_size * sizeof(SgDotDotExp));
#endif
        SgDotDotExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDotDotExp::pool_size * sizeof(SgDotDotExp), V_SgDotDotExp));

        if (SgDotDotExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDotDotExp* blkptr{reinterpret_cast<SgDotDotExp*>(SgDotDotExp::pools[blockIndex-1])};
                blkptr[SgDotDotExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDotDotExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDotDotExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDotDotExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDotDotExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDotDotExp::pool_size - 1};
    SgDotDotExp* testPointer{reinterpret_cast<SgDotDotExp*>(SgDotDotExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDotDotExp::pool_size * (SgDotDotExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDotDotExpStroageClass
 * from its counterpart of SgDotDotExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDotDotExp::initializeStorageClassArray(SgDotDotExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDotDotExp::pools.begin(); block != SgDotDotExp::pools.end(); ++block) {
        SgDotDotExp* pointer{reinterpret_cast<SgDotDotExp*>(*block)};
        for (unsigned i = 0; i < SgDotDotExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 120432 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElementwiseOp that takes its corresponding StorageClass as parameter
 */
SgElementwiseOp::SgElementwiseOp(const SgElementwiseOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 120442 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElementwiseOp*
SgElementwiseOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElementwiseOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseOp)};
        unsigned long positionInPool{localIndex % SgElementwiseOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseOp::pool_size};
        returnPointer = &(((SgElementwiseOp*)(SgElementwiseOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElementwiseOp*
SgElementwiseOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElementwiseOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElementwiseOp)};
        unsigned long positionInPool{localIndex % SgElementwiseOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseOp::pool_size};
        returnPointer = &(((SgElementwiseOp*)(SgElementwiseOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElementwiseOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElementwiseOp::pools.begin(); block != SgElementwiseOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElementwiseOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElementwiseOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElementwiseOp* pointerOfLinkedList{nullptr};
    for (auto block = SgElementwiseOp::pools.begin(); block != SgElementwiseOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElementwiseOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElementwiseOp::clearMemoryPool() {
    SgElementwiseOp* tempPointer{nullptr};
    if (SgElementwiseOp::pools.empty() == false) {
        auto block{SgElementwiseOp::pools.begin()};
        SgElementwiseOp::next_node = reinterpret_cast<SgElementwiseOp*>(*block);
        for (; block != SgElementwiseOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElementwiseOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElementwiseOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElementwiseOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElementwiseOp::pool_size-1]);
        }
    }
}

void
SgElementwiseOp::deleteMemoryPool() {
    for (auto p: SgElementwiseOp::pools) {
        ROSE_FREE(p);
    }
    SgElementwiseOp::next_node = nullptr;
    SgElementwiseOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElementwiseOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseOp)};
    for (size_t blockIndex = SgElementwiseOp::pools.size(); (blockIndex * SgElementwiseOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseOp) = %" PRIuPTR " SgElementwiseOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseOp),SgElementwiseOp::pool_size);
        }
#endif

        SgElementwiseOp* pointer{static_cast<SgElementwiseOp*>(ROSE_MALLOC(SgElementwiseOp::pool_size * sizeof(SgElementwiseOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElementwiseOp::pool_size * sizeof(SgElementwiseOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElementwiseOp::pool_size * sizeof(SgElementwiseOp));
#endif
        SgElementwiseOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElementwiseOp::pool_size * sizeof(SgElementwiseOp), V_SgElementwiseOp));

        if (SgElementwiseOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElementwiseOp* blkptr{reinterpret_cast<SgElementwiseOp*>(SgElementwiseOp::pools[blockIndex-1])};
                blkptr[SgElementwiseOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElementwiseOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElementwiseOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElementwiseOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElementwiseOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElementwiseOp::pool_size - 1};
    SgElementwiseOp* testPointer{reinterpret_cast<SgElementwiseOp*>(SgElementwiseOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElementwiseOp::pool_size * (SgElementwiseOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElementwiseOpStroageClass
 * from its counterpart of SgElementwiseOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElementwiseOp::initializeStorageClassArray(SgElementwiseOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElementwiseOp::pools.begin(); block != SgElementwiseOp::pools.end(); ++block) {
        SgElementwiseOp* pointer{reinterpret_cast<SgElementwiseOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 120659 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElementwiseMultiplyOp that takes its corresponding StorageClass as parameter
 */
SgElementwiseMultiplyOp::SgElementwiseMultiplyOp(const SgElementwiseMultiplyOpStorageClass& storageSource)  : SgElementwiseOp (storageSource) {

/* #line 120669 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElementwiseMultiplyOp*
SgElementwiseMultiplyOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElementwiseMultiplyOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseMultiplyOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseMultiplyOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseMultiplyOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseMultiplyOp)};
        unsigned long positionInPool{localIndex % SgElementwiseMultiplyOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseMultiplyOp::pool_size};
        returnPointer = &(((SgElementwiseMultiplyOp*)(SgElementwiseMultiplyOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElementwiseMultiplyOp*
SgElementwiseMultiplyOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElementwiseMultiplyOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseMultiplyOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseMultiplyOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseMultiplyOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElementwiseMultiplyOp)};
        unsigned long positionInPool{localIndex % SgElementwiseMultiplyOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseMultiplyOp::pool_size};
        returnPointer = &(((SgElementwiseMultiplyOp*)(SgElementwiseMultiplyOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElementwiseMultiplyOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElementwiseMultiplyOp::pools.begin(); block != SgElementwiseMultiplyOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseMultiplyOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseMultiplyOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElementwiseMultiplyOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElementwiseMultiplyOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElementwiseMultiplyOp* pointerOfLinkedList{nullptr};
    for (auto block = SgElementwiseMultiplyOp::pools.begin(); block != SgElementwiseMultiplyOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseMultiplyOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseMultiplyOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElementwiseMultiplyOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElementwiseMultiplyOp::clearMemoryPool() {
    SgElementwiseMultiplyOp* tempPointer{nullptr};
    if (SgElementwiseMultiplyOp::pools.empty() == false) {
        auto block{SgElementwiseMultiplyOp::pools.begin()};
        SgElementwiseMultiplyOp::next_node = reinterpret_cast<SgElementwiseMultiplyOp*>(*block);
        for (; block != SgElementwiseMultiplyOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElementwiseMultiplyOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElementwiseMultiplyOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElementwiseMultiplyOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElementwiseMultiplyOp::pool_size-1]);
        }
    }
}

void
SgElementwiseMultiplyOp::deleteMemoryPool() {
    for (auto p: SgElementwiseMultiplyOp::pools) {
        ROSE_FREE(p);
    }
    SgElementwiseMultiplyOp::next_node = nullptr;
    SgElementwiseMultiplyOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElementwiseMultiplyOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseMultiplyOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseMultiplyOp)};
    for (size_t blockIndex = SgElementwiseMultiplyOp::pools.size(); (blockIndex * SgElementwiseMultiplyOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseMultiplyOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseMultiplyOp) = %" PRIuPTR " SgElementwiseMultiplyOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseMultiplyOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseMultiplyOp),SgElementwiseMultiplyOp::pool_size);
        }
#endif

        SgElementwiseMultiplyOp* pointer{static_cast<SgElementwiseMultiplyOp*>(ROSE_MALLOC(SgElementwiseMultiplyOp::pool_size * sizeof(SgElementwiseMultiplyOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElementwiseMultiplyOp::pool_size * sizeof(SgElementwiseMultiplyOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElementwiseMultiplyOp::pool_size * sizeof(SgElementwiseMultiplyOp));
#endif
        SgElementwiseMultiplyOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElementwiseMultiplyOp::pool_size * sizeof(SgElementwiseMultiplyOp), V_SgElementwiseMultiplyOp));

        if (SgElementwiseMultiplyOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElementwiseMultiplyOp* blkptr{reinterpret_cast<SgElementwiseMultiplyOp*>(SgElementwiseMultiplyOp::pools[blockIndex-1])};
                blkptr[SgElementwiseMultiplyOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElementwiseMultiplyOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElementwiseMultiplyOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElementwiseMultiplyOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElementwiseMultiplyOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElementwiseMultiplyOp::pool_size - 1};
    SgElementwiseMultiplyOp* testPointer{reinterpret_cast<SgElementwiseMultiplyOp*>(SgElementwiseMultiplyOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElementwiseMultiplyOp::pool_size * (SgElementwiseMultiplyOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElementwiseMultiplyOpStroageClass
 * from its counterpart of SgElementwiseMultiplyOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElementwiseMultiplyOp::initializeStorageClassArray(SgElementwiseMultiplyOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElementwiseMultiplyOp::pools.begin(); block != SgElementwiseMultiplyOp::pools.end(); ++block) {
        SgElementwiseMultiplyOp* pointer{reinterpret_cast<SgElementwiseMultiplyOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseMultiplyOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 120886 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElementwisePowerOp that takes its corresponding StorageClass as parameter
 */
SgElementwisePowerOp::SgElementwisePowerOp(const SgElementwisePowerOpStorageClass& storageSource)  : SgElementwiseOp (storageSource) {

/* #line 120896 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElementwisePowerOp*
SgElementwisePowerOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElementwisePowerOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwisePowerOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwisePowerOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwisePowerOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwisePowerOp)};
        unsigned long positionInPool{localIndex % SgElementwisePowerOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwisePowerOp::pool_size};
        returnPointer = &(((SgElementwisePowerOp*)(SgElementwisePowerOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElementwisePowerOp*
SgElementwisePowerOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElementwisePowerOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwisePowerOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwisePowerOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwisePowerOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElementwisePowerOp)};
        unsigned long positionInPool{localIndex % SgElementwisePowerOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwisePowerOp::pool_size};
        returnPointer = &(((SgElementwisePowerOp*)(SgElementwisePowerOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElementwisePowerOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElementwisePowerOp::pools.begin(); block != SgElementwisePowerOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwisePowerOp*>(*block)};
        for (unsigned i = 0; i < SgElementwisePowerOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElementwisePowerOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElementwisePowerOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElementwisePowerOp* pointerOfLinkedList{nullptr};
    for (auto block = SgElementwisePowerOp::pools.begin(); block != SgElementwisePowerOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwisePowerOp*>(*block)};
        for (unsigned i = 0; i < SgElementwisePowerOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElementwisePowerOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElementwisePowerOp::clearMemoryPool() {
    SgElementwisePowerOp* tempPointer{nullptr};
    if (SgElementwisePowerOp::pools.empty() == false) {
        auto block{SgElementwisePowerOp::pools.begin()};
        SgElementwisePowerOp::next_node = reinterpret_cast<SgElementwisePowerOp*>(*block);
        for (; block != SgElementwisePowerOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElementwisePowerOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElementwisePowerOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElementwisePowerOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElementwisePowerOp::pool_size-1]);
        }
    }
}

void
SgElementwisePowerOp::deleteMemoryPool() {
    for (auto p: SgElementwisePowerOp::pools) {
        ROSE_FREE(p);
    }
    SgElementwisePowerOp::next_node = nullptr;
    SgElementwisePowerOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElementwisePowerOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwisePowerOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwisePowerOp)};
    for (size_t blockIndex = SgElementwisePowerOp::pools.size(); (blockIndex * SgElementwisePowerOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwisePowerOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwisePowerOp) = %" PRIuPTR " SgElementwisePowerOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwisePowerOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwisePowerOp),SgElementwisePowerOp::pool_size);
        }
#endif

        SgElementwisePowerOp* pointer{static_cast<SgElementwisePowerOp*>(ROSE_MALLOC(SgElementwisePowerOp::pool_size * sizeof(SgElementwisePowerOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElementwisePowerOp::pool_size * sizeof(SgElementwisePowerOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElementwisePowerOp::pool_size * sizeof(SgElementwisePowerOp));
#endif
        SgElementwisePowerOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElementwisePowerOp::pool_size * sizeof(SgElementwisePowerOp), V_SgElementwisePowerOp));

        if (SgElementwisePowerOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElementwisePowerOp* blkptr{reinterpret_cast<SgElementwisePowerOp*>(SgElementwisePowerOp::pools[blockIndex-1])};
                blkptr[SgElementwisePowerOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElementwisePowerOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElementwisePowerOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElementwisePowerOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElementwisePowerOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElementwisePowerOp::pool_size - 1};
    SgElementwisePowerOp* testPointer{reinterpret_cast<SgElementwisePowerOp*>(SgElementwisePowerOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElementwisePowerOp::pool_size * (SgElementwisePowerOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElementwisePowerOpStroageClass
 * from its counterpart of SgElementwisePowerOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElementwisePowerOp::initializeStorageClassArray(SgElementwisePowerOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElementwisePowerOp::pools.begin(); block != SgElementwisePowerOp::pools.end(); ++block) {
        SgElementwisePowerOp* pointer{reinterpret_cast<SgElementwisePowerOp*>(*block)};
        for (unsigned i = 0; i < SgElementwisePowerOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 121113 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElementwiseLeftDivideOp that takes its corresponding StorageClass as parameter
 */
SgElementwiseLeftDivideOp::SgElementwiseLeftDivideOp(const SgElementwiseLeftDivideOpStorageClass& storageSource)  : SgElementwiseOp (storageSource) {

/* #line 121123 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElementwiseLeftDivideOp*
SgElementwiseLeftDivideOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElementwiseLeftDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseLeftDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseLeftDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseLeftDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseLeftDivideOp)};
        unsigned long positionInPool{localIndex % SgElementwiseLeftDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseLeftDivideOp::pool_size};
        returnPointer = &(((SgElementwiseLeftDivideOp*)(SgElementwiseLeftDivideOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElementwiseLeftDivideOp*
SgElementwiseLeftDivideOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElementwiseLeftDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseLeftDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseLeftDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseLeftDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElementwiseLeftDivideOp)};
        unsigned long positionInPool{localIndex % SgElementwiseLeftDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseLeftDivideOp::pool_size};
        returnPointer = &(((SgElementwiseLeftDivideOp*)(SgElementwiseLeftDivideOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElementwiseLeftDivideOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElementwiseLeftDivideOp::pools.begin(); block != SgElementwiseLeftDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseLeftDivideOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseLeftDivideOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElementwiseLeftDivideOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElementwiseLeftDivideOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElementwiseLeftDivideOp* pointerOfLinkedList{nullptr};
    for (auto block = SgElementwiseLeftDivideOp::pools.begin(); block != SgElementwiseLeftDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseLeftDivideOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseLeftDivideOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElementwiseLeftDivideOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElementwiseLeftDivideOp::clearMemoryPool() {
    SgElementwiseLeftDivideOp* tempPointer{nullptr};
    if (SgElementwiseLeftDivideOp::pools.empty() == false) {
        auto block{SgElementwiseLeftDivideOp::pools.begin()};
        SgElementwiseLeftDivideOp::next_node = reinterpret_cast<SgElementwiseLeftDivideOp*>(*block);
        for (; block != SgElementwiseLeftDivideOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElementwiseLeftDivideOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElementwiseLeftDivideOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElementwiseLeftDivideOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElementwiseLeftDivideOp::pool_size-1]);
        }
    }
}

void
SgElementwiseLeftDivideOp::deleteMemoryPool() {
    for (auto p: SgElementwiseLeftDivideOp::pools) {
        ROSE_FREE(p);
    }
    SgElementwiseLeftDivideOp::next_node = nullptr;
    SgElementwiseLeftDivideOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElementwiseLeftDivideOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseLeftDivideOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseLeftDivideOp)};
    for (size_t blockIndex = SgElementwiseLeftDivideOp::pools.size(); (blockIndex * SgElementwiseLeftDivideOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseLeftDivideOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseLeftDivideOp) = %" PRIuPTR " SgElementwiseLeftDivideOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseLeftDivideOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseLeftDivideOp),SgElementwiseLeftDivideOp::pool_size);
        }
#endif

        SgElementwiseLeftDivideOp* pointer{static_cast<SgElementwiseLeftDivideOp*>(ROSE_MALLOC(SgElementwiseLeftDivideOp::pool_size * sizeof(SgElementwiseLeftDivideOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElementwiseLeftDivideOp::pool_size * sizeof(SgElementwiseLeftDivideOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElementwiseLeftDivideOp::pool_size * sizeof(SgElementwiseLeftDivideOp));
#endif
        SgElementwiseLeftDivideOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElementwiseLeftDivideOp::pool_size * sizeof(SgElementwiseLeftDivideOp), V_SgElementwiseLeftDivideOp));

        if (SgElementwiseLeftDivideOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElementwiseLeftDivideOp* blkptr{reinterpret_cast<SgElementwiseLeftDivideOp*>(SgElementwiseLeftDivideOp::pools[blockIndex-1])};
                blkptr[SgElementwiseLeftDivideOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElementwiseLeftDivideOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElementwiseLeftDivideOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElementwiseLeftDivideOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElementwiseLeftDivideOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElementwiseLeftDivideOp::pool_size - 1};
    SgElementwiseLeftDivideOp* testPointer{reinterpret_cast<SgElementwiseLeftDivideOp*>(SgElementwiseLeftDivideOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElementwiseLeftDivideOp::pool_size * (SgElementwiseLeftDivideOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElementwiseLeftDivideOpStroageClass
 * from its counterpart of SgElementwiseLeftDivideOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElementwiseLeftDivideOp::initializeStorageClassArray(SgElementwiseLeftDivideOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElementwiseLeftDivideOp::pools.begin(); block != SgElementwiseLeftDivideOp::pools.end(); ++block) {
        SgElementwiseLeftDivideOp* pointer{reinterpret_cast<SgElementwiseLeftDivideOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseLeftDivideOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 121340 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElementwiseDivideOp that takes its corresponding StorageClass as parameter
 */
SgElementwiseDivideOp::SgElementwiseDivideOp(const SgElementwiseDivideOpStorageClass& storageSource)  : SgElementwiseOp (storageSource) {

/* #line 121350 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElementwiseDivideOp*
SgElementwiseDivideOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElementwiseDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseDivideOp)};
        unsigned long positionInPool{localIndex % SgElementwiseDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseDivideOp::pool_size};
        returnPointer = &(((SgElementwiseDivideOp*)(SgElementwiseDivideOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElementwiseDivideOp*
SgElementwiseDivideOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElementwiseDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElementwiseDivideOp)};
        unsigned long positionInPool{localIndex % SgElementwiseDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseDivideOp::pool_size};
        returnPointer = &(((SgElementwiseDivideOp*)(SgElementwiseDivideOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElementwiseDivideOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElementwiseDivideOp::pools.begin(); block != SgElementwiseDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseDivideOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseDivideOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElementwiseDivideOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElementwiseDivideOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElementwiseDivideOp* pointerOfLinkedList{nullptr};
    for (auto block = SgElementwiseDivideOp::pools.begin(); block != SgElementwiseDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseDivideOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseDivideOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElementwiseDivideOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElementwiseDivideOp::clearMemoryPool() {
    SgElementwiseDivideOp* tempPointer{nullptr};
    if (SgElementwiseDivideOp::pools.empty() == false) {
        auto block{SgElementwiseDivideOp::pools.begin()};
        SgElementwiseDivideOp::next_node = reinterpret_cast<SgElementwiseDivideOp*>(*block);
        for (; block != SgElementwiseDivideOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElementwiseDivideOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElementwiseDivideOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElementwiseDivideOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElementwiseDivideOp::pool_size-1]);
        }
    }
}

void
SgElementwiseDivideOp::deleteMemoryPool() {
    for (auto p: SgElementwiseDivideOp::pools) {
        ROSE_FREE(p);
    }
    SgElementwiseDivideOp::next_node = nullptr;
    SgElementwiseDivideOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElementwiseDivideOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseDivideOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseDivideOp)};
    for (size_t blockIndex = SgElementwiseDivideOp::pools.size(); (blockIndex * SgElementwiseDivideOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseDivideOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseDivideOp) = %" PRIuPTR " SgElementwiseDivideOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseDivideOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseDivideOp),SgElementwiseDivideOp::pool_size);
        }
#endif

        SgElementwiseDivideOp* pointer{static_cast<SgElementwiseDivideOp*>(ROSE_MALLOC(SgElementwiseDivideOp::pool_size * sizeof(SgElementwiseDivideOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElementwiseDivideOp::pool_size * sizeof(SgElementwiseDivideOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElementwiseDivideOp::pool_size * sizeof(SgElementwiseDivideOp));
#endif
        SgElementwiseDivideOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElementwiseDivideOp::pool_size * sizeof(SgElementwiseDivideOp), V_SgElementwiseDivideOp));

        if (SgElementwiseDivideOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElementwiseDivideOp* blkptr{reinterpret_cast<SgElementwiseDivideOp*>(SgElementwiseDivideOp::pools[blockIndex-1])};
                blkptr[SgElementwiseDivideOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElementwiseDivideOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElementwiseDivideOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElementwiseDivideOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElementwiseDivideOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElementwiseDivideOp::pool_size - 1};
    SgElementwiseDivideOp* testPointer{reinterpret_cast<SgElementwiseDivideOp*>(SgElementwiseDivideOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElementwiseDivideOp::pool_size * (SgElementwiseDivideOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElementwiseDivideOpStroageClass
 * from its counterpart of SgElementwiseDivideOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElementwiseDivideOp::initializeStorageClassArray(SgElementwiseDivideOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElementwiseDivideOp::pools.begin(); block != SgElementwiseDivideOp::pools.end(); ++block) {
        SgElementwiseDivideOp* pointer{reinterpret_cast<SgElementwiseDivideOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseDivideOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 121567 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElementwiseAddOp that takes its corresponding StorageClass as parameter
 */
SgElementwiseAddOp::SgElementwiseAddOp(const SgElementwiseAddOpStorageClass& storageSource)  : SgElementwiseOp (storageSource) {

/* #line 121577 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElementwiseAddOp*
SgElementwiseAddOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElementwiseAddOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseAddOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseAddOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseAddOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseAddOp)};
        unsigned long positionInPool{localIndex % SgElementwiseAddOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseAddOp::pool_size};
        returnPointer = &(((SgElementwiseAddOp*)(SgElementwiseAddOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElementwiseAddOp*
SgElementwiseAddOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElementwiseAddOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseAddOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseAddOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseAddOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElementwiseAddOp)};
        unsigned long positionInPool{localIndex % SgElementwiseAddOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseAddOp::pool_size};
        returnPointer = &(((SgElementwiseAddOp*)(SgElementwiseAddOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElementwiseAddOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElementwiseAddOp::pools.begin(); block != SgElementwiseAddOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseAddOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseAddOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElementwiseAddOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElementwiseAddOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElementwiseAddOp* pointerOfLinkedList{nullptr};
    for (auto block = SgElementwiseAddOp::pools.begin(); block != SgElementwiseAddOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseAddOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseAddOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElementwiseAddOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElementwiseAddOp::clearMemoryPool() {
    SgElementwiseAddOp* tempPointer{nullptr};
    if (SgElementwiseAddOp::pools.empty() == false) {
        auto block{SgElementwiseAddOp::pools.begin()};
        SgElementwiseAddOp::next_node = reinterpret_cast<SgElementwiseAddOp*>(*block);
        for (; block != SgElementwiseAddOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElementwiseAddOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElementwiseAddOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElementwiseAddOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElementwiseAddOp::pool_size-1]);
        }
    }
}

void
SgElementwiseAddOp::deleteMemoryPool() {
    for (auto p: SgElementwiseAddOp::pools) {
        ROSE_FREE(p);
    }
    SgElementwiseAddOp::next_node = nullptr;
    SgElementwiseAddOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElementwiseAddOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseAddOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseAddOp)};
    for (size_t blockIndex = SgElementwiseAddOp::pools.size(); (blockIndex * SgElementwiseAddOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseAddOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseAddOp) = %" PRIuPTR " SgElementwiseAddOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseAddOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseAddOp),SgElementwiseAddOp::pool_size);
        }
#endif

        SgElementwiseAddOp* pointer{static_cast<SgElementwiseAddOp*>(ROSE_MALLOC(SgElementwiseAddOp::pool_size * sizeof(SgElementwiseAddOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElementwiseAddOp::pool_size * sizeof(SgElementwiseAddOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElementwiseAddOp::pool_size * sizeof(SgElementwiseAddOp));
#endif
        SgElementwiseAddOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElementwiseAddOp::pool_size * sizeof(SgElementwiseAddOp), V_SgElementwiseAddOp));

        if (SgElementwiseAddOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElementwiseAddOp* blkptr{reinterpret_cast<SgElementwiseAddOp*>(SgElementwiseAddOp::pools[blockIndex-1])};
                blkptr[SgElementwiseAddOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElementwiseAddOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElementwiseAddOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElementwiseAddOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElementwiseAddOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElementwiseAddOp::pool_size - 1};
    SgElementwiseAddOp* testPointer{reinterpret_cast<SgElementwiseAddOp*>(SgElementwiseAddOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElementwiseAddOp::pool_size * (SgElementwiseAddOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElementwiseAddOpStroageClass
 * from its counterpart of SgElementwiseAddOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElementwiseAddOp::initializeStorageClassArray(SgElementwiseAddOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElementwiseAddOp::pools.begin(); block != SgElementwiseAddOp::pools.end(); ++block) {
        SgElementwiseAddOp* pointer{reinterpret_cast<SgElementwiseAddOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseAddOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 121794 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgElementwiseSubtractOp that takes its corresponding StorageClass as parameter
 */
SgElementwiseSubtractOp::SgElementwiseSubtractOp(const SgElementwiseSubtractOpStorageClass& storageSource)  : SgElementwiseOp (storageSource) {

/* #line 121804 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgElementwiseSubtractOp*
SgElementwiseSubtractOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgElementwiseSubtractOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseSubtractOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseSubtractOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgElementwiseSubtractOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseSubtractOp)};
        unsigned long positionInPool{localIndex % SgElementwiseSubtractOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseSubtractOp::pool_size};
        returnPointer = &(((SgElementwiseSubtractOp*)(SgElementwiseSubtractOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgElementwiseSubtractOp*
SgElementwiseSubtractOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgElementwiseSubtractOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseSubtractOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseSubtractOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgElementwiseSubtractOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgElementwiseSubtractOp)};
        unsigned long positionInPool{localIndex % SgElementwiseSubtractOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgElementwiseSubtractOp::pool_size};
        returnPointer = &(((SgElementwiseSubtractOp*)(SgElementwiseSubtractOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgElementwiseSubtractOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgElementwiseSubtractOp::pools.begin(); block != SgElementwiseSubtractOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseSubtractOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseSubtractOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgElementwiseSubtractOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgElementwiseSubtractOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgElementwiseSubtractOp* pointerOfLinkedList{nullptr};
    for (auto block = SgElementwiseSubtractOp::pools.begin(); block != SgElementwiseSubtractOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgElementwiseSubtractOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseSubtractOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgElementwiseSubtractOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgElementwiseSubtractOp::clearMemoryPool() {
    SgElementwiseSubtractOp* tempPointer{nullptr};
    if (SgElementwiseSubtractOp::pools.empty() == false) {
        auto block{SgElementwiseSubtractOp::pools.begin()};
        SgElementwiseSubtractOp::next_node = reinterpret_cast<SgElementwiseSubtractOp*>(*block);
        for (; block != SgElementwiseSubtractOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgElementwiseSubtractOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgElementwiseSubtractOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgElementwiseSubtractOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgElementwiseSubtractOp::pool_size-1]);
        }
    }
}

void
SgElementwiseSubtractOp::deleteMemoryPool() {
    for (auto p: SgElementwiseSubtractOp::pools) {
        ROSE_FREE(p);
    }
    SgElementwiseSubtractOp::next_node = nullptr;
    SgElementwiseSubtractOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgElementwiseSubtractOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseSubtractOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseSubtractOp)};
    for (size_t blockIndex = SgElementwiseSubtractOp::pools.size(); (blockIndex * SgElementwiseSubtractOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseSubtractOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseSubtractOp) = %" PRIuPTR " SgElementwiseSubtractOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgElementwiseSubtractOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgElementwiseSubtractOp),SgElementwiseSubtractOp::pool_size);
        }
#endif

        SgElementwiseSubtractOp* pointer{static_cast<SgElementwiseSubtractOp*>(ROSE_MALLOC(SgElementwiseSubtractOp::pool_size * sizeof(SgElementwiseSubtractOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgElementwiseSubtractOp::pool_size * sizeof(SgElementwiseSubtractOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgElementwiseSubtractOp::pool_size * sizeof(SgElementwiseSubtractOp));
#endif
        SgElementwiseSubtractOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgElementwiseSubtractOp::pool_size * sizeof(SgElementwiseSubtractOp), V_SgElementwiseSubtractOp));

        if (SgElementwiseSubtractOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgElementwiseSubtractOp* blkptr{reinterpret_cast<SgElementwiseSubtractOp*>(SgElementwiseSubtractOp::pools[blockIndex-1])};
                blkptr[SgElementwiseSubtractOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgElementwiseSubtractOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgElementwiseSubtractOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgElementwiseSubtractOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgElementwiseSubtractOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgElementwiseSubtractOp::pool_size - 1};
    SgElementwiseSubtractOp* testPointer{reinterpret_cast<SgElementwiseSubtractOp*>(SgElementwiseSubtractOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgElementwiseSubtractOp::pool_size * (SgElementwiseSubtractOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgElementwiseSubtractOpStroageClass
 * from its counterpart of SgElementwiseSubtractOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgElementwiseSubtractOp::initializeStorageClassArray(SgElementwiseSubtractOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgElementwiseSubtractOp::pools.begin(); block != SgElementwiseSubtractOp::pools.end(); ++block) {
        SgElementwiseSubtractOp* pointer{reinterpret_cast<SgElementwiseSubtractOp*>(*block)};
        for (unsigned i = 0; i < SgElementwiseSubtractOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 122021 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPowerOp that takes its corresponding StorageClass as parameter
 */
SgPowerOp::SgPowerOp(const SgPowerOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 122031 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPowerOp*
SgPowerOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPowerOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPowerOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPowerOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPowerOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPowerOp)};
        unsigned long positionInPool{localIndex % SgPowerOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPowerOp::pool_size};
        returnPointer = &(((SgPowerOp*)(SgPowerOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPowerOp*
SgPowerOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPowerOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPowerOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPowerOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPowerOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPowerOp)};
        unsigned long positionInPool{localIndex % SgPowerOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPowerOp::pool_size};
        returnPointer = &(((SgPowerOp*)(SgPowerOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPowerOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPowerOp::pools.begin(); block != SgPowerOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPowerOp*>(*block)};
        for (unsigned i = 0; i < SgPowerOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPowerOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPowerOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPowerOp* pointerOfLinkedList{nullptr};
    for (auto block = SgPowerOp::pools.begin(); block != SgPowerOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPowerOp*>(*block)};
        for (unsigned i = 0; i < SgPowerOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPowerOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPowerOp::clearMemoryPool() {
    SgPowerOp* tempPointer{nullptr};
    if (SgPowerOp::pools.empty() == false) {
        auto block{SgPowerOp::pools.begin()};
        SgPowerOp::next_node = reinterpret_cast<SgPowerOp*>(*block);
        for (; block != SgPowerOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPowerOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPowerOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPowerOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPowerOp::pool_size-1]);
        }
    }
}

void
SgPowerOp::deleteMemoryPool() {
    for (auto p: SgPowerOp::pools) {
        ROSE_FREE(p);
    }
    SgPowerOp::next_node = nullptr;
    SgPowerOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPowerOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPowerOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPowerOp)};
    for (size_t blockIndex = SgPowerOp::pools.size(); (blockIndex * SgPowerOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPowerOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPowerOp) = %" PRIuPTR " SgPowerOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPowerOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPowerOp),SgPowerOp::pool_size);
        }
#endif

        SgPowerOp* pointer{static_cast<SgPowerOp*>(ROSE_MALLOC(SgPowerOp::pool_size * sizeof(SgPowerOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPowerOp::pool_size * sizeof(SgPowerOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPowerOp::pool_size * sizeof(SgPowerOp));
#endif
        SgPowerOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPowerOp::pool_size * sizeof(SgPowerOp), V_SgPowerOp));

        if (SgPowerOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPowerOp* blkptr{reinterpret_cast<SgPowerOp*>(SgPowerOp::pools[blockIndex-1])};
                blkptr[SgPowerOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPowerOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPowerOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPowerOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPowerOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPowerOp::pool_size - 1};
    SgPowerOp* testPointer{reinterpret_cast<SgPowerOp*>(SgPowerOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPowerOp::pool_size * (SgPowerOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPowerOpStroageClass
 * from its counterpart of SgPowerOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPowerOp::initializeStorageClassArray(SgPowerOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPowerOp::pools.begin(); block != SgPowerOp::pools.end(); ++block) {
        SgPowerOp* pointer{reinterpret_cast<SgPowerOp*>(*block)};
        for (unsigned i = 0; i < SgPowerOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 122248 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLeftDivideOp that takes its corresponding StorageClass as parameter
 */
SgLeftDivideOp::SgLeftDivideOp(const SgLeftDivideOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 122258 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLeftDivideOp*
SgLeftDivideOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLeftDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLeftDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLeftDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLeftDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLeftDivideOp)};
        unsigned long positionInPool{localIndex % SgLeftDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLeftDivideOp::pool_size};
        returnPointer = &(((SgLeftDivideOp*)(SgLeftDivideOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLeftDivideOp*
SgLeftDivideOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLeftDivideOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLeftDivideOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLeftDivideOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLeftDivideOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLeftDivideOp)};
        unsigned long positionInPool{localIndex % SgLeftDivideOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLeftDivideOp::pool_size};
        returnPointer = &(((SgLeftDivideOp*)(SgLeftDivideOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLeftDivideOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLeftDivideOp::pools.begin(); block != SgLeftDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLeftDivideOp*>(*block)};
        for (unsigned i = 0; i < SgLeftDivideOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLeftDivideOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLeftDivideOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLeftDivideOp* pointerOfLinkedList{nullptr};
    for (auto block = SgLeftDivideOp::pools.begin(); block != SgLeftDivideOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLeftDivideOp*>(*block)};
        for (unsigned i = 0; i < SgLeftDivideOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLeftDivideOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLeftDivideOp::clearMemoryPool() {
    SgLeftDivideOp* tempPointer{nullptr};
    if (SgLeftDivideOp::pools.empty() == false) {
        auto block{SgLeftDivideOp::pools.begin()};
        SgLeftDivideOp::next_node = reinterpret_cast<SgLeftDivideOp*>(*block);
        for (; block != SgLeftDivideOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLeftDivideOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLeftDivideOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLeftDivideOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLeftDivideOp::pool_size-1]);
        }
    }
}

void
SgLeftDivideOp::deleteMemoryPool() {
    for (auto p: SgLeftDivideOp::pools) {
        ROSE_FREE(p);
    }
    SgLeftDivideOp::next_node = nullptr;
    SgLeftDivideOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLeftDivideOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLeftDivideOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLeftDivideOp)};
    for (size_t blockIndex = SgLeftDivideOp::pools.size(); (blockIndex * SgLeftDivideOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLeftDivideOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLeftDivideOp) = %" PRIuPTR " SgLeftDivideOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLeftDivideOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLeftDivideOp),SgLeftDivideOp::pool_size);
        }
#endif

        SgLeftDivideOp* pointer{static_cast<SgLeftDivideOp*>(ROSE_MALLOC(SgLeftDivideOp::pool_size * sizeof(SgLeftDivideOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLeftDivideOp::pool_size * sizeof(SgLeftDivideOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLeftDivideOp::pool_size * sizeof(SgLeftDivideOp));
#endif
        SgLeftDivideOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLeftDivideOp::pool_size * sizeof(SgLeftDivideOp), V_SgLeftDivideOp));

        if (SgLeftDivideOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLeftDivideOp* blkptr{reinterpret_cast<SgLeftDivideOp*>(SgLeftDivideOp::pools[blockIndex-1])};
                blkptr[SgLeftDivideOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLeftDivideOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLeftDivideOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLeftDivideOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLeftDivideOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLeftDivideOp::pool_size - 1};
    SgLeftDivideOp* testPointer{reinterpret_cast<SgLeftDivideOp*>(SgLeftDivideOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLeftDivideOp::pool_size * (SgLeftDivideOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLeftDivideOpStroageClass
 * from its counterpart of SgLeftDivideOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLeftDivideOp::initializeStorageClassArray(SgLeftDivideOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLeftDivideOp::pools.begin(); block != SgLeftDivideOp::pools.end(); ++block) {
        SgLeftDivideOp* pointer{reinterpret_cast<SgLeftDivideOp*>(*block)};
        for (unsigned i = 0; i < SgLeftDivideOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 122475 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRemOp that takes its corresponding StorageClass as parameter
 */
SgRemOp::SgRemOp(const SgRemOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 122485 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRemOp*
SgRemOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRemOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRemOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRemOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRemOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRemOp)};
        unsigned long positionInPool{localIndex % SgRemOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRemOp::pool_size};
        returnPointer = &(((SgRemOp*)(SgRemOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRemOp*
SgRemOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRemOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRemOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRemOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRemOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRemOp)};
        unsigned long positionInPool{localIndex % SgRemOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRemOp::pool_size};
        returnPointer = &(((SgRemOp*)(SgRemOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRemOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRemOp::pools.begin(); block != SgRemOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRemOp*>(*block)};
        for (unsigned i = 0; i < SgRemOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRemOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRemOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRemOp* pointerOfLinkedList{nullptr};
    for (auto block = SgRemOp::pools.begin(); block != SgRemOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRemOp*>(*block)};
        for (unsigned i = 0; i < SgRemOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRemOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRemOp::clearMemoryPool() {
    SgRemOp* tempPointer{nullptr};
    if (SgRemOp::pools.empty() == false) {
        auto block{SgRemOp::pools.begin()};
        SgRemOp::next_node = reinterpret_cast<SgRemOp*>(*block);
        for (; block != SgRemOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRemOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRemOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRemOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRemOp::pool_size-1]);
        }
    }
}

void
SgRemOp::deleteMemoryPool() {
    for (auto p: SgRemOp::pools) {
        ROSE_FREE(p);
    }
    SgRemOp::next_node = nullptr;
    SgRemOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRemOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRemOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRemOp)};
    for (size_t blockIndex = SgRemOp::pools.size(); (blockIndex * SgRemOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRemOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRemOp) = %" PRIuPTR " SgRemOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRemOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRemOp),SgRemOp::pool_size);
        }
#endif

        SgRemOp* pointer{static_cast<SgRemOp*>(ROSE_MALLOC(SgRemOp::pool_size * sizeof(SgRemOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRemOp::pool_size * sizeof(SgRemOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRemOp::pool_size * sizeof(SgRemOp));
#endif
        SgRemOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRemOp::pool_size * sizeof(SgRemOp), V_SgRemOp));

        if (SgRemOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRemOp* blkptr{reinterpret_cast<SgRemOp*>(SgRemOp::pools[blockIndex-1])};
                blkptr[SgRemOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRemOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRemOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRemOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRemOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRemOp::pool_size - 1};
    SgRemOp* testPointer{reinterpret_cast<SgRemOp*>(SgRemOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRemOp::pool_size * (SgRemOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRemOpStroageClass
 * from its counterpart of SgRemOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRemOp::initializeStorageClassArray(SgRemOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRemOp::pools.begin(); block != SgRemOp::pools.end(); ++block) {
        SgRemOp* pointer{reinterpret_cast<SgRemOp*>(*block)};
        for (unsigned i = 0; i < SgRemOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 122702 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgReplicationOp that takes its corresponding StorageClass as parameter
 */
SgReplicationOp::SgReplicationOp(const SgReplicationOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 122712 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgReplicationOp*
SgReplicationOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgReplicationOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReplicationOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReplicationOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReplicationOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgReplicationOp)};
        unsigned long positionInPool{localIndex % SgReplicationOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReplicationOp::pool_size};
        returnPointer = &(((SgReplicationOp*)(SgReplicationOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgReplicationOp*
SgReplicationOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgReplicationOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReplicationOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReplicationOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReplicationOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgReplicationOp)};
        unsigned long positionInPool{localIndex % SgReplicationOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReplicationOp::pool_size};
        returnPointer = &(((SgReplicationOp*)(SgReplicationOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgReplicationOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgReplicationOp::pools.begin(); block != SgReplicationOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReplicationOp*>(*block)};
        for (unsigned i = 0; i < SgReplicationOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgReplicationOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgReplicationOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgReplicationOp* pointerOfLinkedList{nullptr};
    for (auto block = SgReplicationOp::pools.begin(); block != SgReplicationOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReplicationOp*>(*block)};
        for (unsigned i = 0; i < SgReplicationOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgReplicationOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgReplicationOp::clearMemoryPool() {
    SgReplicationOp* tempPointer{nullptr};
    if (SgReplicationOp::pools.empty() == false) {
        auto block{SgReplicationOp::pools.begin()};
        SgReplicationOp::next_node = reinterpret_cast<SgReplicationOp*>(*block);
        for (; block != SgReplicationOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgReplicationOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgReplicationOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgReplicationOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgReplicationOp::pool_size-1]);
        }
    }
}

void
SgReplicationOp::deleteMemoryPool() {
    for (auto p: SgReplicationOp::pools) {
        ROSE_FREE(p);
    }
    SgReplicationOp::next_node = nullptr;
    SgReplicationOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgReplicationOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgReplicationOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgReplicationOp)};
    for (size_t blockIndex = SgReplicationOp::pools.size(); (blockIndex * SgReplicationOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgReplicationOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgReplicationOp) = %" PRIuPTR " SgReplicationOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgReplicationOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgReplicationOp),SgReplicationOp::pool_size);
        }
#endif

        SgReplicationOp* pointer{static_cast<SgReplicationOp*>(ROSE_MALLOC(SgReplicationOp::pool_size * sizeof(SgReplicationOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgReplicationOp::pool_size * sizeof(SgReplicationOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgReplicationOp::pool_size * sizeof(SgReplicationOp));
#endif
        SgReplicationOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgReplicationOp::pool_size * sizeof(SgReplicationOp), V_SgReplicationOp));

        if (SgReplicationOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgReplicationOp* blkptr{reinterpret_cast<SgReplicationOp*>(SgReplicationOp::pools[blockIndex-1])};
                blkptr[SgReplicationOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgReplicationOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgReplicationOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgReplicationOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgReplicationOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgReplicationOp::pool_size - 1};
    SgReplicationOp* testPointer{reinterpret_cast<SgReplicationOp*>(SgReplicationOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgReplicationOp::pool_size * (SgReplicationOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgReplicationOpStroageClass
 * from its counterpart of SgReplicationOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgReplicationOp::initializeStorageClassArray(SgReplicationOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgReplicationOp::pools.begin(); block != SgReplicationOp::pools.end(); ++block) {
        SgReplicationOp* pointer{reinterpret_cast<SgReplicationOp*>(*block)};
        for (unsigned i = 0; i < SgReplicationOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 122929 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAtOp that takes its corresponding StorageClass as parameter
 */
SgAtOp::SgAtOp(const SgAtOpStorageClass& storageSource)  : SgBinaryOp (storageSource) {

/* #line 122939 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAtOp*
SgAtOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAtOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAtOp)};
        unsigned long positionInPool{localIndex % SgAtOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtOp::pool_size};
        returnPointer = &(((SgAtOp*)(SgAtOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAtOp*
SgAtOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAtOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAtOp)};
        unsigned long positionInPool{localIndex % SgAtOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtOp::pool_size};
        returnPointer = &(((SgAtOp*)(SgAtOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAtOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAtOp::pools.begin(); block != SgAtOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtOp*>(*block)};
        for (unsigned i = 0; i < SgAtOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAtOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAtOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAtOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAtOp::pools.begin(); block != SgAtOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtOp*>(*block)};
        for (unsigned i = 0; i < SgAtOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAtOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAtOp::clearMemoryPool() {
    SgAtOp* tempPointer{nullptr};
    if (SgAtOp::pools.empty() == false) {
        auto block{SgAtOp::pools.begin()};
        SgAtOp::next_node = reinterpret_cast<SgAtOp*>(*block);
        for (; block != SgAtOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAtOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAtOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAtOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAtOp::pool_size-1]);
        }
    }
}

void
SgAtOp::deleteMemoryPool() {
    for (auto p: SgAtOp::pools) {
        ROSE_FREE(p);
    }
    SgAtOp::next_node = nullptr;
    SgAtOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAtOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAtOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtOp)};
    for (size_t blockIndex = SgAtOp::pools.size(); (blockIndex * SgAtOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAtOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtOp) = %" PRIuPTR " SgAtOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAtOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtOp),SgAtOp::pool_size);
        }
#endif

        SgAtOp* pointer{static_cast<SgAtOp*>(ROSE_MALLOC(SgAtOp::pool_size * sizeof(SgAtOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAtOp::pool_size * sizeof(SgAtOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAtOp::pool_size * sizeof(SgAtOp));
#endif
        SgAtOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAtOp::pool_size * sizeof(SgAtOp), V_SgAtOp));

        if (SgAtOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAtOp* blkptr{reinterpret_cast<SgAtOp*>(SgAtOp::pools[blockIndex-1])};
                blkptr[SgAtOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAtOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAtOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAtOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAtOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAtOp::pool_size - 1};
    SgAtOp* testPointer{reinterpret_cast<SgAtOp*>(SgAtOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAtOp::pool_size * (SgAtOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAtOpStroageClass
 * from its counterpart of SgAtOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAtOp::initializeStorageClassArray(SgAtOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAtOp::pools.begin(); block != SgAtOp::pools.end(); ++block) {
        SgAtOp* pointer{reinterpret_cast<SgAtOp*>(*block)};
        for (unsigned i = 0; i < SgAtOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 123156 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgExprListExp that takes its corresponding StorageClass as parameter
 */
SgExprListExp::SgExprListExp(const SgExprListExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 123166 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_expressions = storageSource.storageOf_expressions.rebuildDataStoredInEasyStorageClass();
    SgExpressionPtrList::iterator i_expressions = p_expressions.begin();
    for ( ; i_expressions != p_expressions.end(); ++i_expressions) {
        (*i_expressions) = (SgExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_expressions)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgExprListExp*
SgExprListExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgExprListExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExprListExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExprListExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgExprListExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgExprListExp)};
        unsigned long positionInPool{localIndex % SgExprListExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExprListExp::pool_size};
        returnPointer = &(((SgExprListExp*)(SgExprListExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgExprListExp*
SgExprListExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgExprListExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExprListExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExprListExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgExprListExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgExprListExp)};
        unsigned long positionInPool{localIndex % SgExprListExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgExprListExp::pool_size};
        returnPointer = &(((SgExprListExp*)(SgExprListExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgExprListExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgExprListExp::pools.begin(); block != SgExprListExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExprListExp*>(*block)};
        for (unsigned i = 0; i < SgExprListExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgExprListExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgExprListExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgExprListExp* pointerOfLinkedList{nullptr};
    for (auto block = SgExprListExp::pools.begin(); block != SgExprListExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgExprListExp*>(*block)};
        for (unsigned i = 0; i < SgExprListExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgExprListExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgExprListExp::clearMemoryPool() {
    SgExprListExp* tempPointer{nullptr};
    if (SgExprListExp::pools.empty() == false) {
        auto block{SgExprListExp::pools.begin()};
        SgExprListExp::next_node = reinterpret_cast<SgExprListExp*>(*block);
        for (; block != SgExprListExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgExprListExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgExprListExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgExprListExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgExprListExp::pool_size-1]);
        }
    }
}

void
SgExprListExp::deleteMemoryPool() {
    for (auto p: SgExprListExp::pools) {
        ROSE_FREE(p);
    }
    SgExprListExp::next_node = nullptr;
    SgExprListExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgExprListExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgExprListExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgExprListExp)};
    for (size_t blockIndex = SgExprListExp::pools.size(); (blockIndex * SgExprListExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgExprListExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgExprListExp) = %" PRIuPTR " SgExprListExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgExprListExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgExprListExp),SgExprListExp::pool_size);
        }
#endif

        SgExprListExp* pointer{static_cast<SgExprListExp*>(ROSE_MALLOC(SgExprListExp::pool_size * sizeof(SgExprListExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgExprListExp::pool_size * sizeof(SgExprListExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgExprListExp::pool_size * sizeof(SgExprListExp));
#endif
        SgExprListExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgExprListExp::pool_size * sizeof(SgExprListExp), V_SgExprListExp));

        if (SgExprListExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgExprListExp* blkptr{reinterpret_cast<SgExprListExp*>(SgExprListExp::pools[blockIndex-1])};
                blkptr[SgExprListExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgExprListExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgExprListExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgExprListExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgExprListExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgExprListExp::pool_size - 1};
    SgExprListExp* testPointer{reinterpret_cast<SgExprListExp*>(SgExprListExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgExprListExp::pool_size * (SgExprListExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgExprListExpStroageClass
 * from its counterpart of SgExprListExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgExprListExp::initializeStorageClassArray(SgExprListExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgExprListExp::pools.begin(); block != SgExprListExp::pools.end(); ++block) {
        SgExprListExp* pointer{reinterpret_cast<SgExprListExp*>(*block)};
        for (unsigned i = 0; i < SgExprListExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 123388 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgListExp that takes its corresponding StorageClass as parameter
 */
SgListExp::SgListExp(const SgListExpStorageClass& storageSource)  : SgExprListExp (storageSource) {

/* #line 123398 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgListExp*
SgListExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgListExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgListExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgListExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgListExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgListExp)};
        unsigned long positionInPool{localIndex % SgListExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgListExp::pool_size};
        returnPointer = &(((SgListExp*)(SgListExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgListExp*
SgListExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgListExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgListExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgListExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgListExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgListExp)};
        unsigned long positionInPool{localIndex % SgListExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgListExp::pool_size};
        returnPointer = &(((SgListExp*)(SgListExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgListExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgListExp::pools.begin(); block != SgListExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgListExp*>(*block)};
        for (unsigned i = 0; i < SgListExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgListExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgListExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgListExp* pointerOfLinkedList{nullptr};
    for (auto block = SgListExp::pools.begin(); block != SgListExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgListExp*>(*block)};
        for (unsigned i = 0; i < SgListExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgListExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgListExp::clearMemoryPool() {
    SgListExp* tempPointer{nullptr};
    if (SgListExp::pools.empty() == false) {
        auto block{SgListExp::pools.begin()};
        SgListExp::next_node = reinterpret_cast<SgListExp*>(*block);
        for (; block != SgListExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgListExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgListExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgListExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgListExp::pool_size-1]);
        }
    }
}

void
SgListExp::deleteMemoryPool() {
    for (auto p: SgListExp::pools) {
        ROSE_FREE(p);
    }
    SgListExp::next_node = nullptr;
    SgListExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgListExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgListExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgListExp)};
    for (size_t blockIndex = SgListExp::pools.size(); (blockIndex * SgListExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgListExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgListExp) = %" PRIuPTR " SgListExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgListExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgListExp),SgListExp::pool_size);
        }
#endif

        SgListExp* pointer{static_cast<SgListExp*>(ROSE_MALLOC(SgListExp::pool_size * sizeof(SgListExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgListExp::pool_size * sizeof(SgListExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgListExp::pool_size * sizeof(SgListExp));
#endif
        SgListExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgListExp::pool_size * sizeof(SgListExp), V_SgListExp));

        if (SgListExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgListExp* blkptr{reinterpret_cast<SgListExp*>(SgListExp::pools[blockIndex-1])};
                blkptr[SgListExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgListExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgListExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgListExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgListExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgListExp::pool_size - 1};
    SgListExp* testPointer{reinterpret_cast<SgListExp*>(SgListExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgListExp::pool_size * (SgListExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgListExpStroageClass
 * from its counterpart of SgListExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgListExp::initializeStorageClassArray(SgListExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgListExp::pools.begin(); block != SgListExp::pools.end(); ++block) {
        SgListExp* pointer{reinterpret_cast<SgListExp*>(*block)};
        for (unsigned i = 0; i < SgListExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 123615 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTupleExp that takes its corresponding StorageClass as parameter
 */
SgTupleExp::SgTupleExp(const SgTupleExpStorageClass& storageSource)  : SgExprListExp (storageSource) {

/* #line 123625 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTupleExp*
SgTupleExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTupleExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTupleExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTupleExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTupleExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTupleExp)};
        unsigned long positionInPool{localIndex % SgTupleExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTupleExp::pool_size};
        returnPointer = &(((SgTupleExp*)(SgTupleExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTupleExp*
SgTupleExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTupleExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTupleExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTupleExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTupleExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTupleExp)};
        unsigned long positionInPool{localIndex % SgTupleExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTupleExp::pool_size};
        returnPointer = &(((SgTupleExp*)(SgTupleExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTupleExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTupleExp::pools.begin(); block != SgTupleExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTupleExp*>(*block)};
        for (unsigned i = 0; i < SgTupleExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTupleExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTupleExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTupleExp* pointerOfLinkedList{nullptr};
    for (auto block = SgTupleExp::pools.begin(); block != SgTupleExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTupleExp*>(*block)};
        for (unsigned i = 0; i < SgTupleExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTupleExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTupleExp::clearMemoryPool() {
    SgTupleExp* tempPointer{nullptr};
    if (SgTupleExp::pools.empty() == false) {
        auto block{SgTupleExp::pools.begin()};
        SgTupleExp::next_node = reinterpret_cast<SgTupleExp*>(*block);
        for (; block != SgTupleExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTupleExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTupleExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTupleExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTupleExp::pool_size-1]);
        }
    }
}

void
SgTupleExp::deleteMemoryPool() {
    for (auto p: SgTupleExp::pools) {
        ROSE_FREE(p);
    }
    SgTupleExp::next_node = nullptr;
    SgTupleExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTupleExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTupleExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTupleExp)};
    for (size_t blockIndex = SgTupleExp::pools.size(); (blockIndex * SgTupleExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTupleExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTupleExp) = %" PRIuPTR " SgTupleExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTupleExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTupleExp),SgTupleExp::pool_size);
        }
#endif

        SgTupleExp* pointer{static_cast<SgTupleExp*>(ROSE_MALLOC(SgTupleExp::pool_size * sizeof(SgTupleExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTupleExp::pool_size * sizeof(SgTupleExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTupleExp::pool_size * sizeof(SgTupleExp));
#endif
        SgTupleExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTupleExp::pool_size * sizeof(SgTupleExp), V_SgTupleExp));

        if (SgTupleExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTupleExp* blkptr{reinterpret_cast<SgTupleExp*>(SgTupleExp::pools[blockIndex-1])};
                blkptr[SgTupleExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTupleExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTupleExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTupleExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTupleExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTupleExp::pool_size - 1};
    SgTupleExp* testPointer{reinterpret_cast<SgTupleExp*>(SgTupleExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTupleExp::pool_size * (SgTupleExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTupleExpStroageClass
 * from its counterpart of SgTupleExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTupleExp::initializeStorageClassArray(SgTupleExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTupleExp::pools.begin(); block != SgTupleExp::pools.end(); ++block) {
        SgTupleExp* pointer{reinterpret_cast<SgTupleExp*>(*block)};
        for (unsigned i = 0; i < SgTupleExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 123842 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMatrixExp that takes its corresponding StorageClass as parameter
 */
SgMatrixExp::SgMatrixExp(const SgMatrixExpStorageClass& storageSource)  : SgExprListExp (storageSource) {

/* #line 123852 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMatrixExp*
SgMatrixExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMatrixExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatrixExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatrixExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMatrixExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixExp)};
        unsigned long positionInPool{localIndex % SgMatrixExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMatrixExp::pool_size};
        returnPointer = &(((SgMatrixExp*)(SgMatrixExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMatrixExp*
SgMatrixExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMatrixExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatrixExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatrixExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMatrixExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMatrixExp)};
        unsigned long positionInPool{localIndex % SgMatrixExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMatrixExp::pool_size};
        returnPointer = &(((SgMatrixExp*)(SgMatrixExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMatrixExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMatrixExp::pools.begin(); block != SgMatrixExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMatrixExp*>(*block)};
        for (unsigned i = 0; i < SgMatrixExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMatrixExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMatrixExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMatrixExp* pointerOfLinkedList{nullptr};
    for (auto block = SgMatrixExp::pools.begin(); block != SgMatrixExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMatrixExp*>(*block)};
        for (unsigned i = 0; i < SgMatrixExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMatrixExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMatrixExp::clearMemoryPool() {
    SgMatrixExp* tempPointer{nullptr};
    if (SgMatrixExp::pools.empty() == false) {
        auto block{SgMatrixExp::pools.begin()};
        SgMatrixExp::next_node = reinterpret_cast<SgMatrixExp*>(*block);
        for (; block != SgMatrixExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMatrixExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMatrixExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMatrixExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMatrixExp::pool_size-1]);
        }
    }
}

void
SgMatrixExp::deleteMemoryPool() {
    for (auto p: SgMatrixExp::pools) {
        ROSE_FREE(p);
    }
    SgMatrixExp::next_node = nullptr;
    SgMatrixExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMatrixExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatrixExp)};
    for (size_t blockIndex = SgMatrixExp::pools.size(); (blockIndex * SgMatrixExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatrixExp) = %" PRIuPTR " SgMatrixExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMatrixExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMatrixExp),SgMatrixExp::pool_size);
        }
#endif

        SgMatrixExp* pointer{static_cast<SgMatrixExp*>(ROSE_MALLOC(SgMatrixExp::pool_size * sizeof(SgMatrixExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMatrixExp::pool_size * sizeof(SgMatrixExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMatrixExp::pool_size * sizeof(SgMatrixExp));
#endif
        SgMatrixExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMatrixExp::pool_size * sizeof(SgMatrixExp), V_SgMatrixExp));

        if (SgMatrixExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMatrixExp* blkptr{reinterpret_cast<SgMatrixExp*>(SgMatrixExp::pools[blockIndex-1])};
                blkptr[SgMatrixExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMatrixExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMatrixExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMatrixExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMatrixExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMatrixExp::pool_size - 1};
    SgMatrixExp* testPointer{reinterpret_cast<SgMatrixExp*>(SgMatrixExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMatrixExp::pool_size * (SgMatrixExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMatrixExpStroageClass
 * from its counterpart of SgMatrixExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMatrixExp::initializeStorageClassArray(SgMatrixExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMatrixExp::pools.begin(); block != SgMatrixExp::pools.end(); ++block) {
        SgMatrixExp* pointer{reinterpret_cast<SgMatrixExp*>(*block)};
        for (unsigned i = 0; i < SgMatrixExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 124069 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgValueExp that takes its corresponding StorageClass as parameter
 */
SgValueExp::SgValueExp(const SgValueExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 124079 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_originalExpressionTree = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_originalExpressionTree));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgValueExp*
SgValueExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgValueExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgValueExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgValueExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgValueExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgValueExp)};
        unsigned long positionInPool{localIndex % SgValueExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgValueExp::pool_size};
        returnPointer = &(((SgValueExp*)(SgValueExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgValueExp*
SgValueExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgValueExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgValueExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgValueExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgValueExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgValueExp)};
        unsigned long positionInPool{localIndex % SgValueExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgValueExp::pool_size};
        returnPointer = &(((SgValueExp*)(SgValueExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgValueExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgValueExp::pools.begin(); block != SgValueExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgValueExp*>(*block)};
        for (unsigned i = 0; i < SgValueExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgValueExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgValueExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgValueExp* pointerOfLinkedList{nullptr};
    for (auto block = SgValueExp::pools.begin(); block != SgValueExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgValueExp*>(*block)};
        for (unsigned i = 0; i < SgValueExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgValueExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgValueExp::clearMemoryPool() {
    SgValueExp* tempPointer{nullptr};
    if (SgValueExp::pools.empty() == false) {
        auto block{SgValueExp::pools.begin()};
        SgValueExp::next_node = reinterpret_cast<SgValueExp*>(*block);
        for (; block != SgValueExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgValueExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgValueExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgValueExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgValueExp::pool_size-1]);
        }
    }
}

void
SgValueExp::deleteMemoryPool() {
    for (auto p: SgValueExp::pools) {
        ROSE_FREE(p);
    }
    SgValueExp::next_node = nullptr;
    SgValueExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgValueExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgValueExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgValueExp)};
    for (size_t blockIndex = SgValueExp::pools.size(); (blockIndex * SgValueExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgValueExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgValueExp) = %" PRIuPTR " SgValueExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgValueExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgValueExp),SgValueExp::pool_size);
        }
#endif

        SgValueExp* pointer{static_cast<SgValueExp*>(ROSE_MALLOC(SgValueExp::pool_size * sizeof(SgValueExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgValueExp::pool_size * sizeof(SgValueExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgValueExp::pool_size * sizeof(SgValueExp));
#endif
        SgValueExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgValueExp::pool_size * sizeof(SgValueExp), V_SgValueExp));

        if (SgValueExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgValueExp* blkptr{reinterpret_cast<SgValueExp*>(SgValueExp::pools[blockIndex-1])};
                blkptr[SgValueExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgValueExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgValueExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgValueExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgValueExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgValueExp::pool_size - 1};
    SgValueExp* testPointer{reinterpret_cast<SgValueExp*>(SgValueExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgValueExp::pool_size * (SgValueExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgValueExpStroageClass
 * from its counterpart of SgValueExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgValueExp::initializeStorageClassArray(SgValueExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgValueExp::pools.begin(); block != SgValueExp::pools.end(); ++block) {
        SgValueExp* pointer{reinterpret_cast<SgValueExp*>(*block)};
        for (unsigned i = 0; i < SgValueExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 124297 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBoolValExp that takes its corresponding StorageClass as parameter
 */
SgBoolValExp::SgBoolValExp(const SgBoolValExpStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 124307 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBoolValExp*
SgBoolValExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBoolValExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBoolValExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBoolValExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBoolValExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBoolValExp)};
        unsigned long positionInPool{localIndex % SgBoolValExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBoolValExp::pool_size};
        returnPointer = &(((SgBoolValExp*)(SgBoolValExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBoolValExp*
SgBoolValExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBoolValExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBoolValExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBoolValExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBoolValExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBoolValExp)};
        unsigned long positionInPool{localIndex % SgBoolValExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBoolValExp::pool_size};
        returnPointer = &(((SgBoolValExp*)(SgBoolValExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBoolValExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBoolValExp::pools.begin(); block != SgBoolValExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBoolValExp*>(*block)};
        for (unsigned i = 0; i < SgBoolValExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBoolValExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBoolValExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBoolValExp* pointerOfLinkedList{nullptr};
    for (auto block = SgBoolValExp::pools.begin(); block != SgBoolValExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBoolValExp*>(*block)};
        for (unsigned i = 0; i < SgBoolValExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBoolValExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBoolValExp::clearMemoryPool() {
    SgBoolValExp* tempPointer{nullptr};
    if (SgBoolValExp::pools.empty() == false) {
        auto block{SgBoolValExp::pools.begin()};
        SgBoolValExp::next_node = reinterpret_cast<SgBoolValExp*>(*block);
        for (; block != SgBoolValExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBoolValExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBoolValExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBoolValExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBoolValExp::pool_size-1]);
        }
    }
}

void
SgBoolValExp::deleteMemoryPool() {
    for (auto p: SgBoolValExp::pools) {
        ROSE_FREE(p);
    }
    SgBoolValExp::next_node = nullptr;
    SgBoolValExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBoolValExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBoolValExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBoolValExp)};
    for (size_t blockIndex = SgBoolValExp::pools.size(); (blockIndex * SgBoolValExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBoolValExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBoolValExp) = %" PRIuPTR " SgBoolValExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBoolValExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBoolValExp),SgBoolValExp::pool_size);
        }
#endif

        SgBoolValExp* pointer{static_cast<SgBoolValExp*>(ROSE_MALLOC(SgBoolValExp::pool_size * sizeof(SgBoolValExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBoolValExp::pool_size * sizeof(SgBoolValExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBoolValExp::pool_size * sizeof(SgBoolValExp));
#endif
        SgBoolValExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBoolValExp::pool_size * sizeof(SgBoolValExp), V_SgBoolValExp));

        if (SgBoolValExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBoolValExp* blkptr{reinterpret_cast<SgBoolValExp*>(SgBoolValExp::pools[blockIndex-1])};
                blkptr[SgBoolValExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBoolValExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBoolValExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBoolValExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBoolValExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBoolValExp::pool_size - 1};
    SgBoolValExp* testPointer{reinterpret_cast<SgBoolValExp*>(SgBoolValExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBoolValExp::pool_size * (SgBoolValExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBoolValExpStroageClass
 * from its counterpart of SgBoolValExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBoolValExp::initializeStorageClassArray(SgBoolValExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBoolValExp::pools.begin(); block != SgBoolValExp::pools.end(); ++block) {
        SgBoolValExp* pointer{reinterpret_cast<SgBoolValExp*>(*block)};
        for (unsigned i = 0; i < SgBoolValExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 124525 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStringVal that takes its corresponding StorageClass as parameter
 */
SgStringVal::SgStringVal(const SgStringValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 124535 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value.rebuildDataStoredInEasyStorageClass();
    p_wcharString = storageSource.storageOf_wcharString;
    p_stringDelimiter = storageSource.storageOf_stringDelimiter;
    p_is16bitString = storageSource.storageOf_is16bitString;
    p_is32bitString = storageSource.storageOf_is32bitString;
    p_isRawString = storageSource.storageOf_isRawString;
    p_raw_string_value = storageSource.storageOf_raw_string_value.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStringVal*
SgStringVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStringVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStringVal)};
        unsigned long positionInPool{localIndex % SgStringVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStringVal::pool_size};
        returnPointer = &(((SgStringVal*)(SgStringVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStringVal*
SgStringVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStringVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStringVal)};
        unsigned long positionInPool{localIndex % SgStringVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStringVal::pool_size};
        returnPointer = &(((SgStringVal*)(SgStringVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStringVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStringVal::pools.begin(); block != SgStringVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStringVal*>(*block)};
        for (unsigned i = 0; i < SgStringVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStringVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStringVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStringVal* pointerOfLinkedList{nullptr};
    for (auto block = SgStringVal::pools.begin(); block != SgStringVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStringVal*>(*block)};
        for (unsigned i = 0; i < SgStringVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStringVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStringVal::clearMemoryPool() {
    SgStringVal* tempPointer{nullptr};
    if (SgStringVal::pools.empty() == false) {
        auto block{SgStringVal::pools.begin()};
        SgStringVal::next_node = reinterpret_cast<SgStringVal*>(*block);
        for (; block != SgStringVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStringVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStringVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStringVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStringVal::pool_size-1]);
        }
    }
}

void
SgStringVal::deleteMemoryPool() {
    for (auto p: SgStringVal::pools) {
        ROSE_FREE(p);
    }
    SgStringVal::next_node = nullptr;
    SgStringVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStringVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStringVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringVal)};
    for (size_t blockIndex = SgStringVal::pools.size(); (blockIndex * SgStringVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStringVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringVal) = %" PRIuPTR " SgStringVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStringVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringVal),SgStringVal::pool_size);
        }
#endif

        SgStringVal* pointer{static_cast<SgStringVal*>(ROSE_MALLOC(SgStringVal::pool_size * sizeof(SgStringVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStringVal::pool_size * sizeof(SgStringVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStringVal::pool_size * sizeof(SgStringVal));
#endif
        SgStringVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStringVal::pool_size * sizeof(SgStringVal), V_SgStringVal));

        if (SgStringVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStringVal* blkptr{reinterpret_cast<SgStringVal*>(SgStringVal::pools[blockIndex-1])};
                blkptr[SgStringVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStringVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStringVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStringVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStringVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStringVal::pool_size - 1};
    SgStringVal* testPointer{reinterpret_cast<SgStringVal*>(SgStringVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStringVal::pool_size * (SgStringVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStringValStroageClass
 * from its counterpart of SgStringVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStringVal::initializeStorageClassArray(SgStringValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStringVal::pools.begin(); block != SgStringVal::pools.end(); ++block) {
        SgStringVal* pointer{reinterpret_cast<SgStringVal*>(*block)};
        for (unsigned i = 0; i < SgStringVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 124759 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgShortVal that takes its corresponding StorageClass as parameter
 */
SgShortVal::SgShortVal(const SgShortValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 124769 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgShortVal*
SgShortVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgShortVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgShortVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgShortVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgShortVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgShortVal)};
        unsigned long positionInPool{localIndex % SgShortVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgShortVal::pool_size};
        returnPointer = &(((SgShortVal*)(SgShortVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgShortVal*
SgShortVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgShortVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgShortVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgShortVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgShortVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgShortVal)};
        unsigned long positionInPool{localIndex % SgShortVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgShortVal::pool_size};
        returnPointer = &(((SgShortVal*)(SgShortVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgShortVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgShortVal::pools.begin(); block != SgShortVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgShortVal*>(*block)};
        for (unsigned i = 0; i < SgShortVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgShortVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgShortVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgShortVal* pointerOfLinkedList{nullptr};
    for (auto block = SgShortVal::pools.begin(); block != SgShortVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgShortVal*>(*block)};
        for (unsigned i = 0; i < SgShortVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgShortVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgShortVal::clearMemoryPool() {
    SgShortVal* tempPointer{nullptr};
    if (SgShortVal::pools.empty() == false) {
        auto block{SgShortVal::pools.begin()};
        SgShortVal::next_node = reinterpret_cast<SgShortVal*>(*block);
        for (; block != SgShortVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgShortVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgShortVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgShortVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgShortVal::pool_size-1]);
        }
    }
}

void
SgShortVal::deleteMemoryPool() {
    for (auto p: SgShortVal::pools) {
        ROSE_FREE(p);
    }
    SgShortVal::next_node = nullptr;
    SgShortVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgShortVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgShortVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgShortVal)};
    for (size_t blockIndex = SgShortVal::pools.size(); (blockIndex * SgShortVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgShortVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgShortVal) = %" PRIuPTR " SgShortVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgShortVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgShortVal),SgShortVal::pool_size);
        }
#endif

        SgShortVal* pointer{static_cast<SgShortVal*>(ROSE_MALLOC(SgShortVal::pool_size * sizeof(SgShortVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgShortVal::pool_size * sizeof(SgShortVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgShortVal::pool_size * sizeof(SgShortVal));
#endif
        SgShortVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgShortVal::pool_size * sizeof(SgShortVal), V_SgShortVal));

        if (SgShortVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgShortVal* blkptr{reinterpret_cast<SgShortVal*>(SgShortVal::pools[blockIndex-1])};
                blkptr[SgShortVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgShortVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgShortVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgShortVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgShortVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgShortVal::pool_size - 1};
    SgShortVal* testPointer{reinterpret_cast<SgShortVal*>(SgShortVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgShortVal::pool_size * (SgShortVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgShortValStroageClass
 * from its counterpart of SgShortVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgShortVal::initializeStorageClassArray(SgShortValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgShortVal::pools.begin(); block != SgShortVal::pools.end(); ++block) {
        SgShortVal* pointer{reinterpret_cast<SgShortVal*>(*block)};
        for (unsigned i = 0; i < SgShortVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 124988 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCharVal that takes its corresponding StorageClass as parameter
 */
SgCharVal::SgCharVal(const SgCharValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 124998 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCharVal*
SgCharVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCharVal)};
        unsigned long positionInPool{localIndex % SgCharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCharVal::pool_size};
        returnPointer = &(((SgCharVal*)(SgCharVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCharVal*
SgCharVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCharVal)};
        unsigned long positionInPool{localIndex % SgCharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCharVal::pool_size};
        returnPointer = &(((SgCharVal*)(SgCharVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCharVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCharVal::pools.begin(); block != SgCharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCharVal*>(*block)};
        for (unsigned i = 0; i < SgCharVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCharVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCharVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCharVal* pointerOfLinkedList{nullptr};
    for (auto block = SgCharVal::pools.begin(); block != SgCharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCharVal*>(*block)};
        for (unsigned i = 0; i < SgCharVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCharVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCharVal::clearMemoryPool() {
    SgCharVal* tempPointer{nullptr};
    if (SgCharVal::pools.empty() == false) {
        auto block{SgCharVal::pools.begin()};
        SgCharVal::next_node = reinterpret_cast<SgCharVal*>(*block);
        for (; block != SgCharVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCharVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCharVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCharVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCharVal::pool_size-1]);
        }
    }
}

void
SgCharVal::deleteMemoryPool() {
    for (auto p: SgCharVal::pools) {
        ROSE_FREE(p);
    }
    SgCharVal::next_node = nullptr;
    SgCharVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCharVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCharVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCharVal)};
    for (size_t blockIndex = SgCharVal::pools.size(); (blockIndex * SgCharVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCharVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCharVal) = %" PRIuPTR " SgCharVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCharVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCharVal),SgCharVal::pool_size);
        }
#endif

        SgCharVal* pointer{static_cast<SgCharVal*>(ROSE_MALLOC(SgCharVal::pool_size * sizeof(SgCharVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCharVal::pool_size * sizeof(SgCharVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCharVal::pool_size * sizeof(SgCharVal));
#endif
        SgCharVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCharVal::pool_size * sizeof(SgCharVal), V_SgCharVal));

        if (SgCharVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCharVal* blkptr{reinterpret_cast<SgCharVal*>(SgCharVal::pools[blockIndex-1])};
                blkptr[SgCharVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCharVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCharVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCharVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCharVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCharVal::pool_size - 1};
    SgCharVal* testPointer{reinterpret_cast<SgCharVal*>(SgCharVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCharVal::pool_size * (SgCharVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCharValStroageClass
 * from its counterpart of SgCharVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCharVal::initializeStorageClassArray(SgCharValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCharVal::pools.begin(); block != SgCharVal::pools.end(); ++block) {
        SgCharVal* pointer{reinterpret_cast<SgCharVal*>(*block)};
        for (unsigned i = 0; i < SgCharVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 125217 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnsignedCharVal that takes its corresponding StorageClass as parameter
 */
SgUnsignedCharVal::SgUnsignedCharVal(const SgUnsignedCharValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 125227 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnsignedCharVal*
SgUnsignedCharVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnsignedCharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedCharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedCharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedCharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedCharVal)};
        unsigned long positionInPool{localIndex % SgUnsignedCharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedCharVal::pool_size};
        returnPointer = &(((SgUnsignedCharVal*)(SgUnsignedCharVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnsignedCharVal*
SgUnsignedCharVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnsignedCharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedCharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedCharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedCharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnsignedCharVal)};
        unsigned long positionInPool{localIndex % SgUnsignedCharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedCharVal::pool_size};
        returnPointer = &(((SgUnsignedCharVal*)(SgUnsignedCharVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnsignedCharVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnsignedCharVal::pools.begin(); block != SgUnsignedCharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedCharVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedCharVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnsignedCharVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnsignedCharVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnsignedCharVal* pointerOfLinkedList{nullptr};
    for (auto block = SgUnsignedCharVal::pools.begin(); block != SgUnsignedCharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedCharVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedCharVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnsignedCharVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnsignedCharVal::clearMemoryPool() {
    SgUnsignedCharVal* tempPointer{nullptr};
    if (SgUnsignedCharVal::pools.empty() == false) {
        auto block{SgUnsignedCharVal::pools.begin()};
        SgUnsignedCharVal::next_node = reinterpret_cast<SgUnsignedCharVal*>(*block);
        for (; block != SgUnsignedCharVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnsignedCharVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnsignedCharVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnsignedCharVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnsignedCharVal::pool_size-1]);
        }
    }
}

void
SgUnsignedCharVal::deleteMemoryPool() {
    for (auto p: SgUnsignedCharVal::pools) {
        ROSE_FREE(p);
    }
    SgUnsignedCharVal::next_node = nullptr;
    SgUnsignedCharVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnsignedCharVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedCharVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedCharVal)};
    for (size_t blockIndex = SgUnsignedCharVal::pools.size(); (blockIndex * SgUnsignedCharVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedCharVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedCharVal) = %" PRIuPTR " SgUnsignedCharVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedCharVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedCharVal),SgUnsignedCharVal::pool_size);
        }
#endif

        SgUnsignedCharVal* pointer{static_cast<SgUnsignedCharVal*>(ROSE_MALLOC(SgUnsignedCharVal::pool_size * sizeof(SgUnsignedCharVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnsignedCharVal::pool_size * sizeof(SgUnsignedCharVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnsignedCharVal::pool_size * sizeof(SgUnsignedCharVal));
#endif
        SgUnsignedCharVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnsignedCharVal::pool_size * sizeof(SgUnsignedCharVal), V_SgUnsignedCharVal));

        if (SgUnsignedCharVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnsignedCharVal* blkptr{reinterpret_cast<SgUnsignedCharVal*>(SgUnsignedCharVal::pools[blockIndex-1])};
                blkptr[SgUnsignedCharVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnsignedCharVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnsignedCharVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnsignedCharVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnsignedCharVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnsignedCharVal::pool_size - 1};
    SgUnsignedCharVal* testPointer{reinterpret_cast<SgUnsignedCharVal*>(SgUnsignedCharVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnsignedCharVal::pool_size * (SgUnsignedCharVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnsignedCharValStroageClass
 * from its counterpart of SgUnsignedCharVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnsignedCharVal::initializeStorageClassArray(SgUnsignedCharValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnsignedCharVal::pools.begin(); block != SgUnsignedCharVal::pools.end(); ++block) {
        SgUnsignedCharVal* pointer{reinterpret_cast<SgUnsignedCharVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedCharVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 125446 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgWcharVal that takes its corresponding StorageClass as parameter
 */
SgWcharVal::SgWcharVal(const SgWcharValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 125456 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_valueUL = storageSource.storageOf_valueUL;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgWcharVal*
SgWcharVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgWcharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWcharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWcharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgWcharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgWcharVal)};
        unsigned long positionInPool{localIndex % SgWcharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWcharVal::pool_size};
        returnPointer = &(((SgWcharVal*)(SgWcharVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgWcharVal*
SgWcharVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgWcharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWcharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWcharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgWcharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgWcharVal)};
        unsigned long positionInPool{localIndex % SgWcharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgWcharVal::pool_size};
        returnPointer = &(((SgWcharVal*)(SgWcharVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgWcharVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgWcharVal::pools.begin(); block != SgWcharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWcharVal*>(*block)};
        for (unsigned i = 0; i < SgWcharVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgWcharVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgWcharVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgWcharVal* pointerOfLinkedList{nullptr};
    for (auto block = SgWcharVal::pools.begin(); block != SgWcharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgWcharVal*>(*block)};
        for (unsigned i = 0; i < SgWcharVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgWcharVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgWcharVal::clearMemoryPool() {
    SgWcharVal* tempPointer{nullptr};
    if (SgWcharVal::pools.empty() == false) {
        auto block{SgWcharVal::pools.begin()};
        SgWcharVal::next_node = reinterpret_cast<SgWcharVal*>(*block);
        for (; block != SgWcharVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgWcharVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgWcharVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgWcharVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgWcharVal::pool_size-1]);
        }
    }
}

void
SgWcharVal::deleteMemoryPool() {
    for (auto p: SgWcharVal::pools) {
        ROSE_FREE(p);
    }
    SgWcharVal::next_node = nullptr;
    SgWcharVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgWcharVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgWcharVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgWcharVal)};
    for (size_t blockIndex = SgWcharVal::pools.size(); (blockIndex * SgWcharVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgWcharVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgWcharVal) = %" PRIuPTR " SgWcharVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgWcharVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgWcharVal),SgWcharVal::pool_size);
        }
#endif

        SgWcharVal* pointer{static_cast<SgWcharVal*>(ROSE_MALLOC(SgWcharVal::pool_size * sizeof(SgWcharVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgWcharVal::pool_size * sizeof(SgWcharVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgWcharVal::pool_size * sizeof(SgWcharVal));
#endif
        SgWcharVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgWcharVal::pool_size * sizeof(SgWcharVal), V_SgWcharVal));

        if (SgWcharVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgWcharVal* blkptr{reinterpret_cast<SgWcharVal*>(SgWcharVal::pools[blockIndex-1])};
                blkptr[SgWcharVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgWcharVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgWcharVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgWcharVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgWcharVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgWcharVal::pool_size - 1};
    SgWcharVal* testPointer{reinterpret_cast<SgWcharVal*>(SgWcharVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgWcharVal::pool_size * (SgWcharVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgWcharValStroageClass
 * from its counterpart of SgWcharVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgWcharVal::initializeStorageClassArray(SgWcharValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgWcharVal::pools.begin(); block != SgWcharVal::pools.end(); ++block) {
        SgWcharVal* pointer{reinterpret_cast<SgWcharVal*>(*block)};
        for (unsigned i = 0; i < SgWcharVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 125675 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnsignedShortVal that takes its corresponding StorageClass as parameter
 */
SgUnsignedShortVal::SgUnsignedShortVal(const SgUnsignedShortValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 125685 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnsignedShortVal*
SgUnsignedShortVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnsignedShortVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedShortVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedShortVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedShortVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedShortVal)};
        unsigned long positionInPool{localIndex % SgUnsignedShortVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedShortVal::pool_size};
        returnPointer = &(((SgUnsignedShortVal*)(SgUnsignedShortVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnsignedShortVal*
SgUnsignedShortVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnsignedShortVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedShortVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedShortVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedShortVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnsignedShortVal)};
        unsigned long positionInPool{localIndex % SgUnsignedShortVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedShortVal::pool_size};
        returnPointer = &(((SgUnsignedShortVal*)(SgUnsignedShortVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnsignedShortVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnsignedShortVal::pools.begin(); block != SgUnsignedShortVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedShortVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedShortVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnsignedShortVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnsignedShortVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnsignedShortVal* pointerOfLinkedList{nullptr};
    for (auto block = SgUnsignedShortVal::pools.begin(); block != SgUnsignedShortVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedShortVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedShortVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnsignedShortVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnsignedShortVal::clearMemoryPool() {
    SgUnsignedShortVal* tempPointer{nullptr};
    if (SgUnsignedShortVal::pools.empty() == false) {
        auto block{SgUnsignedShortVal::pools.begin()};
        SgUnsignedShortVal::next_node = reinterpret_cast<SgUnsignedShortVal*>(*block);
        for (; block != SgUnsignedShortVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnsignedShortVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnsignedShortVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnsignedShortVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnsignedShortVal::pool_size-1]);
        }
    }
}

void
SgUnsignedShortVal::deleteMemoryPool() {
    for (auto p: SgUnsignedShortVal::pools) {
        ROSE_FREE(p);
    }
    SgUnsignedShortVal::next_node = nullptr;
    SgUnsignedShortVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnsignedShortVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedShortVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedShortVal)};
    for (size_t blockIndex = SgUnsignedShortVal::pools.size(); (blockIndex * SgUnsignedShortVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedShortVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedShortVal) = %" PRIuPTR " SgUnsignedShortVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedShortVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedShortVal),SgUnsignedShortVal::pool_size);
        }
#endif

        SgUnsignedShortVal* pointer{static_cast<SgUnsignedShortVal*>(ROSE_MALLOC(SgUnsignedShortVal::pool_size * sizeof(SgUnsignedShortVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnsignedShortVal::pool_size * sizeof(SgUnsignedShortVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnsignedShortVal::pool_size * sizeof(SgUnsignedShortVal));
#endif
        SgUnsignedShortVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnsignedShortVal::pool_size * sizeof(SgUnsignedShortVal), V_SgUnsignedShortVal));

        if (SgUnsignedShortVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnsignedShortVal* blkptr{reinterpret_cast<SgUnsignedShortVal*>(SgUnsignedShortVal::pools[blockIndex-1])};
                blkptr[SgUnsignedShortVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnsignedShortVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnsignedShortVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnsignedShortVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnsignedShortVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnsignedShortVal::pool_size - 1};
    SgUnsignedShortVal* testPointer{reinterpret_cast<SgUnsignedShortVal*>(SgUnsignedShortVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnsignedShortVal::pool_size * (SgUnsignedShortVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnsignedShortValStroageClass
 * from its counterpart of SgUnsignedShortVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnsignedShortVal::initializeStorageClassArray(SgUnsignedShortValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnsignedShortVal::pools.begin(); block != SgUnsignedShortVal::pools.end(); ++block) {
        SgUnsignedShortVal* pointer{reinterpret_cast<SgUnsignedShortVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedShortVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 125904 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIntVal that takes its corresponding StorageClass as parameter
 */
SgIntVal::SgIntVal(const SgIntValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 125914 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIntVal*
SgIntVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIntVal)};
        unsigned long positionInPool{localIndex % SgIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntVal::pool_size};
        returnPointer = &(((SgIntVal*)(SgIntVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIntVal*
SgIntVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIntVal)};
        unsigned long positionInPool{localIndex % SgIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntVal::pool_size};
        returnPointer = &(((SgIntVal*)(SgIntVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIntVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIntVal::pools.begin(); block != SgIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntVal*>(*block)};
        for (unsigned i = 0; i < SgIntVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIntVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIntVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIntVal* pointerOfLinkedList{nullptr};
    for (auto block = SgIntVal::pools.begin(); block != SgIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntVal*>(*block)};
        for (unsigned i = 0; i < SgIntVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIntVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIntVal::clearMemoryPool() {
    SgIntVal* tempPointer{nullptr};
    if (SgIntVal::pools.empty() == false) {
        auto block{SgIntVal::pools.begin()};
        SgIntVal::next_node = reinterpret_cast<SgIntVal*>(*block);
        for (; block != SgIntVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIntVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIntVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIntVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIntVal::pool_size-1]);
        }
    }
}

void
SgIntVal::deleteMemoryPool() {
    for (auto p: SgIntVal::pools) {
        ROSE_FREE(p);
    }
    SgIntVal::next_node = nullptr;
    SgIntVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIntVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIntVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntVal)};
    for (size_t blockIndex = SgIntVal::pools.size(); (blockIndex * SgIntVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIntVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntVal) = %" PRIuPTR " SgIntVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIntVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntVal),SgIntVal::pool_size);
        }
#endif

        SgIntVal* pointer{static_cast<SgIntVal*>(ROSE_MALLOC(SgIntVal::pool_size * sizeof(SgIntVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIntVal::pool_size * sizeof(SgIntVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIntVal::pool_size * sizeof(SgIntVal));
#endif
        SgIntVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIntVal::pool_size * sizeof(SgIntVal), V_SgIntVal));

        if (SgIntVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIntVal* blkptr{reinterpret_cast<SgIntVal*>(SgIntVal::pools[blockIndex-1])};
                blkptr[SgIntVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIntVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIntVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIntVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIntVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIntVal::pool_size - 1};
    SgIntVal* testPointer{reinterpret_cast<SgIntVal*>(SgIntVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIntVal::pool_size * (SgIntVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIntValStroageClass
 * from its counterpart of SgIntVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIntVal::initializeStorageClassArray(SgIntValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIntVal::pools.begin(); block != SgIntVal::pools.end(); ++block) {
        SgIntVal* pointer{reinterpret_cast<SgIntVal*>(*block)};
        for (unsigned i = 0; i < SgIntVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 126133 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEnumVal that takes its corresponding StorageClass as parameter
 */
SgEnumVal::SgEnumVal(const SgEnumValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 126143 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
     p_declaration = (SgEnumDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
    p_name = SgName ( storageSource.storageOf_name);
    p_requiresNameQualification = storageSource.storageOf_requiresNameQualification;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEnumVal*
SgEnumVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEnumVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumVal)};
        unsigned long positionInPool{localIndex % SgEnumVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumVal::pool_size};
        returnPointer = &(((SgEnumVal*)(SgEnumVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEnumVal*
SgEnumVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEnumVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEnumVal)};
        unsigned long positionInPool{localIndex % SgEnumVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumVal::pool_size};
        returnPointer = &(((SgEnumVal*)(SgEnumVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEnumVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEnumVal::pools.begin(); block != SgEnumVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumVal*>(*block)};
        for (unsigned i = 0; i < SgEnumVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEnumVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEnumVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEnumVal* pointerOfLinkedList{nullptr};
    for (auto block = SgEnumVal::pools.begin(); block != SgEnumVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumVal*>(*block)};
        for (unsigned i = 0; i < SgEnumVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEnumVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEnumVal::clearMemoryPool() {
    SgEnumVal* tempPointer{nullptr};
    if (SgEnumVal::pools.empty() == false) {
        auto block{SgEnumVal::pools.begin()};
        SgEnumVal::next_node = reinterpret_cast<SgEnumVal*>(*block);
        for (; block != SgEnumVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEnumVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEnumVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEnumVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEnumVal::pool_size-1]);
        }
    }
}

void
SgEnumVal::deleteMemoryPool() {
    for (auto p: SgEnumVal::pools) {
        ROSE_FREE(p);
    }
    SgEnumVal::next_node = nullptr;
    SgEnumVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEnumVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumVal)};
    for (size_t blockIndex = SgEnumVal::pools.size(); (blockIndex * SgEnumVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumVal) = %" PRIuPTR " SgEnumVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumVal),SgEnumVal::pool_size);
        }
#endif

        SgEnumVal* pointer{static_cast<SgEnumVal*>(ROSE_MALLOC(SgEnumVal::pool_size * sizeof(SgEnumVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEnumVal::pool_size * sizeof(SgEnumVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEnumVal::pool_size * sizeof(SgEnumVal));
#endif
        SgEnumVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEnumVal::pool_size * sizeof(SgEnumVal), V_SgEnumVal));

        if (SgEnumVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEnumVal* blkptr{reinterpret_cast<SgEnumVal*>(SgEnumVal::pools[blockIndex-1])};
                blkptr[SgEnumVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEnumVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEnumVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEnumVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEnumVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEnumVal::pool_size - 1};
    SgEnumVal* testPointer{reinterpret_cast<SgEnumVal*>(SgEnumVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEnumVal::pool_size * (SgEnumVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEnumValStroageClass
 * from its counterpart of SgEnumVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEnumVal::initializeStorageClassArray(SgEnumValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEnumVal::pools.begin(); block != SgEnumVal::pools.end(); ++block) {
        SgEnumVal* pointer{reinterpret_cast<SgEnumVal*>(*block)};
        for (unsigned i = 0; i < SgEnumVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 126367 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnsignedIntVal that takes its corresponding StorageClass as parameter
 */
SgUnsignedIntVal::SgUnsignedIntVal(const SgUnsignedIntValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 126377 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnsignedIntVal*
SgUnsignedIntVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnsignedIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntVal)};
        unsigned long positionInPool{localIndex % SgUnsignedIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedIntVal::pool_size};
        returnPointer = &(((SgUnsignedIntVal*)(SgUnsignedIntVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnsignedIntVal*
SgUnsignedIntVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnsignedIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnsignedIntVal)};
        unsigned long positionInPool{localIndex % SgUnsignedIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedIntVal::pool_size};
        returnPointer = &(((SgUnsignedIntVal*)(SgUnsignedIntVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnsignedIntVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnsignedIntVal::pools.begin(); block != SgUnsignedIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedIntVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedIntVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnsignedIntVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnsignedIntVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnsignedIntVal* pointerOfLinkedList{nullptr};
    for (auto block = SgUnsignedIntVal::pools.begin(); block != SgUnsignedIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedIntVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedIntVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnsignedIntVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnsignedIntVal::clearMemoryPool() {
    SgUnsignedIntVal* tempPointer{nullptr};
    if (SgUnsignedIntVal::pools.empty() == false) {
        auto block{SgUnsignedIntVal::pools.begin()};
        SgUnsignedIntVal::next_node = reinterpret_cast<SgUnsignedIntVal*>(*block);
        for (; block != SgUnsignedIntVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnsignedIntVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnsignedIntVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnsignedIntVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnsignedIntVal::pool_size-1]);
        }
    }
}

void
SgUnsignedIntVal::deleteMemoryPool() {
    for (auto p: SgUnsignedIntVal::pools) {
        ROSE_FREE(p);
    }
    SgUnsignedIntVal::next_node = nullptr;
    SgUnsignedIntVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnsignedIntVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedIntVal)};
    for (size_t blockIndex = SgUnsignedIntVal::pools.size(); (blockIndex * SgUnsignedIntVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedIntVal) = %" PRIuPTR " SgUnsignedIntVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedIntVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedIntVal),SgUnsignedIntVal::pool_size);
        }
#endif

        SgUnsignedIntVal* pointer{static_cast<SgUnsignedIntVal*>(ROSE_MALLOC(SgUnsignedIntVal::pool_size * sizeof(SgUnsignedIntVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnsignedIntVal::pool_size * sizeof(SgUnsignedIntVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnsignedIntVal::pool_size * sizeof(SgUnsignedIntVal));
#endif
        SgUnsignedIntVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnsignedIntVal::pool_size * sizeof(SgUnsignedIntVal), V_SgUnsignedIntVal));

        if (SgUnsignedIntVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnsignedIntVal* blkptr{reinterpret_cast<SgUnsignedIntVal*>(SgUnsignedIntVal::pools[blockIndex-1])};
                blkptr[SgUnsignedIntVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnsignedIntVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnsignedIntVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnsignedIntVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnsignedIntVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnsignedIntVal::pool_size - 1};
    SgUnsignedIntVal* testPointer{reinterpret_cast<SgUnsignedIntVal*>(SgUnsignedIntVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnsignedIntVal::pool_size * (SgUnsignedIntVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnsignedIntValStroageClass
 * from its counterpart of SgUnsignedIntVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnsignedIntVal::initializeStorageClassArray(SgUnsignedIntValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnsignedIntVal::pools.begin(); block != SgUnsignedIntVal::pools.end(); ++block) {
        SgUnsignedIntVal* pointer{reinterpret_cast<SgUnsignedIntVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedIntVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 126596 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLongIntVal that takes its corresponding StorageClass as parameter
 */
SgLongIntVal::SgLongIntVal(const SgLongIntValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 126606 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLongIntVal*
SgLongIntVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLongIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLongIntVal)};
        unsigned long positionInPool{localIndex % SgLongIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLongIntVal::pool_size};
        returnPointer = &(((SgLongIntVal*)(SgLongIntVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLongIntVal*
SgLongIntVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLongIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLongIntVal)};
        unsigned long positionInPool{localIndex % SgLongIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLongIntVal::pool_size};
        returnPointer = &(((SgLongIntVal*)(SgLongIntVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLongIntVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLongIntVal::pools.begin(); block != SgLongIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgLongIntVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLongIntVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLongIntVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLongIntVal* pointerOfLinkedList{nullptr};
    for (auto block = SgLongIntVal::pools.begin(); block != SgLongIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgLongIntVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLongIntVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLongIntVal::clearMemoryPool() {
    SgLongIntVal* tempPointer{nullptr};
    if (SgLongIntVal::pools.empty() == false) {
        auto block{SgLongIntVal::pools.begin()};
        SgLongIntVal::next_node = reinterpret_cast<SgLongIntVal*>(*block);
        for (; block != SgLongIntVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLongIntVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLongIntVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLongIntVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLongIntVal::pool_size-1]);
        }
    }
}

void
SgLongIntVal::deleteMemoryPool() {
    for (auto p: SgLongIntVal::pools) {
        ROSE_FREE(p);
    }
    SgLongIntVal::next_node = nullptr;
    SgLongIntVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLongIntVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLongIntVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongIntVal)};
    for (size_t blockIndex = SgLongIntVal::pools.size(); (blockIndex * SgLongIntVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLongIntVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongIntVal) = %" PRIuPTR " SgLongIntVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLongIntVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongIntVal),SgLongIntVal::pool_size);
        }
#endif

        SgLongIntVal* pointer{static_cast<SgLongIntVal*>(ROSE_MALLOC(SgLongIntVal::pool_size * sizeof(SgLongIntVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLongIntVal::pool_size * sizeof(SgLongIntVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLongIntVal::pool_size * sizeof(SgLongIntVal));
#endif
        SgLongIntVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLongIntVal::pool_size * sizeof(SgLongIntVal), V_SgLongIntVal));

        if (SgLongIntVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLongIntVal* blkptr{reinterpret_cast<SgLongIntVal*>(SgLongIntVal::pools[blockIndex-1])};
                blkptr[SgLongIntVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLongIntVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLongIntVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLongIntVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLongIntVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLongIntVal::pool_size - 1};
    SgLongIntVal* testPointer{reinterpret_cast<SgLongIntVal*>(SgLongIntVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLongIntVal::pool_size * (SgLongIntVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLongIntValStroageClass
 * from its counterpart of SgLongIntVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLongIntVal::initializeStorageClassArray(SgLongIntValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLongIntVal::pools.begin(); block != SgLongIntVal::pools.end(); ++block) {
        SgLongIntVal* pointer{reinterpret_cast<SgLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgLongIntVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 126825 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLongLongIntVal that takes its corresponding StorageClass as parameter
 */
SgLongLongIntVal::SgLongLongIntVal(const SgLongLongIntValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 126835 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLongLongIntVal*
SgLongLongIntVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLongLongIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongLongIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongLongIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongLongIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLongLongIntVal)};
        unsigned long positionInPool{localIndex % SgLongLongIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLongLongIntVal::pool_size};
        returnPointer = &(((SgLongLongIntVal*)(SgLongLongIntVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLongLongIntVal*
SgLongLongIntVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLongLongIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongLongIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongLongIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongLongIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLongLongIntVal)};
        unsigned long positionInPool{localIndex % SgLongLongIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLongLongIntVal::pool_size};
        returnPointer = &(((SgLongLongIntVal*)(SgLongLongIntVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLongLongIntVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLongLongIntVal::pools.begin(); block != SgLongLongIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLongLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgLongLongIntVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLongLongIntVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLongLongIntVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLongLongIntVal* pointerOfLinkedList{nullptr};
    for (auto block = SgLongLongIntVal::pools.begin(); block != SgLongLongIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLongLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgLongLongIntVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLongLongIntVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLongLongIntVal::clearMemoryPool() {
    SgLongLongIntVal* tempPointer{nullptr};
    if (SgLongLongIntVal::pools.empty() == false) {
        auto block{SgLongLongIntVal::pools.begin()};
        SgLongLongIntVal::next_node = reinterpret_cast<SgLongLongIntVal*>(*block);
        for (; block != SgLongLongIntVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLongLongIntVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLongLongIntVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLongLongIntVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLongLongIntVal::pool_size-1]);
        }
    }
}

void
SgLongLongIntVal::deleteMemoryPool() {
    for (auto p: SgLongLongIntVal::pools) {
        ROSE_FREE(p);
    }
    SgLongLongIntVal::next_node = nullptr;
    SgLongLongIntVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLongLongIntVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLongLongIntVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongLongIntVal)};
    for (size_t blockIndex = SgLongLongIntVal::pools.size(); (blockIndex * SgLongLongIntVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLongLongIntVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongLongIntVal) = %" PRIuPTR " SgLongLongIntVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLongLongIntVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongLongIntVal),SgLongLongIntVal::pool_size);
        }
#endif

        SgLongLongIntVal* pointer{static_cast<SgLongLongIntVal*>(ROSE_MALLOC(SgLongLongIntVal::pool_size * sizeof(SgLongLongIntVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLongLongIntVal::pool_size * sizeof(SgLongLongIntVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLongLongIntVal::pool_size * sizeof(SgLongLongIntVal));
#endif
        SgLongLongIntVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLongLongIntVal::pool_size * sizeof(SgLongLongIntVal), V_SgLongLongIntVal));

        if (SgLongLongIntVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLongLongIntVal* blkptr{reinterpret_cast<SgLongLongIntVal*>(SgLongLongIntVal::pools[blockIndex-1])};
                blkptr[SgLongLongIntVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLongLongIntVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLongLongIntVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLongLongIntVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLongLongIntVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLongLongIntVal::pool_size - 1};
    SgLongLongIntVal* testPointer{reinterpret_cast<SgLongLongIntVal*>(SgLongLongIntVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLongLongIntVal::pool_size * (SgLongLongIntVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLongLongIntValStroageClass
 * from its counterpart of SgLongLongIntVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLongLongIntVal::initializeStorageClassArray(SgLongLongIntValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLongLongIntVal::pools.begin(); block != SgLongLongIntVal::pools.end(); ++block) {
        SgLongLongIntVal* pointer{reinterpret_cast<SgLongLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgLongLongIntVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 127054 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnsignedLongLongIntVal that takes its corresponding StorageClass as parameter
 */
SgUnsignedLongLongIntVal::SgUnsignedLongLongIntVal(const SgUnsignedLongLongIntValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 127064 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnsignedLongLongIntVal*
SgUnsignedLongLongIntVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnsignedLongLongIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedLongLongIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedLongLongIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedLongLongIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongLongIntVal)};
        unsigned long positionInPool{localIndex % SgUnsignedLongLongIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedLongLongIntVal::pool_size};
        returnPointer = &(((SgUnsignedLongLongIntVal*)(SgUnsignedLongLongIntVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnsignedLongLongIntVal*
SgUnsignedLongLongIntVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnsignedLongLongIntVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedLongLongIntVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedLongLongIntVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedLongLongIntVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnsignedLongLongIntVal)};
        unsigned long positionInPool{localIndex % SgUnsignedLongLongIntVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedLongLongIntVal::pool_size};
        returnPointer = &(((SgUnsignedLongLongIntVal*)(SgUnsignedLongLongIntVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnsignedLongLongIntVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnsignedLongLongIntVal::pools.begin(); block != SgUnsignedLongLongIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedLongLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedLongLongIntVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnsignedLongLongIntVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnsignedLongLongIntVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnsignedLongLongIntVal* pointerOfLinkedList{nullptr};
    for (auto block = SgUnsignedLongLongIntVal::pools.begin(); block != SgUnsignedLongLongIntVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedLongLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedLongLongIntVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnsignedLongLongIntVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnsignedLongLongIntVal::clearMemoryPool() {
    SgUnsignedLongLongIntVal* tempPointer{nullptr};
    if (SgUnsignedLongLongIntVal::pools.empty() == false) {
        auto block{SgUnsignedLongLongIntVal::pools.begin()};
        SgUnsignedLongLongIntVal::next_node = reinterpret_cast<SgUnsignedLongLongIntVal*>(*block);
        for (; block != SgUnsignedLongLongIntVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnsignedLongLongIntVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnsignedLongLongIntVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnsignedLongLongIntVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnsignedLongLongIntVal::pool_size-1]);
        }
    }
}

void
SgUnsignedLongLongIntVal::deleteMemoryPool() {
    for (auto p: SgUnsignedLongLongIntVal::pools) {
        ROSE_FREE(p);
    }
    SgUnsignedLongLongIntVal::next_node = nullptr;
    SgUnsignedLongLongIntVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnsignedLongLongIntVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongLongIntVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedLongLongIntVal)};
    for (size_t blockIndex = SgUnsignedLongLongIntVal::pools.size(); (blockIndex * SgUnsignedLongLongIntVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongLongIntVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedLongLongIntVal) = %" PRIuPTR " SgUnsignedLongLongIntVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongLongIntVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedLongLongIntVal),SgUnsignedLongLongIntVal::pool_size);
        }
#endif

        SgUnsignedLongLongIntVal* pointer{static_cast<SgUnsignedLongLongIntVal*>(ROSE_MALLOC(SgUnsignedLongLongIntVal::pool_size * sizeof(SgUnsignedLongLongIntVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnsignedLongLongIntVal::pool_size * sizeof(SgUnsignedLongLongIntVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnsignedLongLongIntVal::pool_size * sizeof(SgUnsignedLongLongIntVal));
#endif
        SgUnsignedLongLongIntVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnsignedLongLongIntVal::pool_size * sizeof(SgUnsignedLongLongIntVal), V_SgUnsignedLongLongIntVal));

        if (SgUnsignedLongLongIntVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnsignedLongLongIntVal* blkptr{reinterpret_cast<SgUnsignedLongLongIntVal*>(SgUnsignedLongLongIntVal::pools[blockIndex-1])};
                blkptr[SgUnsignedLongLongIntVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnsignedLongLongIntVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnsignedLongLongIntVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnsignedLongLongIntVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnsignedLongLongIntVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnsignedLongLongIntVal::pool_size - 1};
    SgUnsignedLongLongIntVal* testPointer{reinterpret_cast<SgUnsignedLongLongIntVal*>(SgUnsignedLongLongIntVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnsignedLongLongIntVal::pool_size * (SgUnsignedLongLongIntVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnsignedLongLongIntValStroageClass
 * from its counterpart of SgUnsignedLongLongIntVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnsignedLongLongIntVal::initializeStorageClassArray(SgUnsignedLongLongIntValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnsignedLongLongIntVal::pools.begin(); block != SgUnsignedLongLongIntVal::pools.end(); ++block) {
        SgUnsignedLongLongIntVal* pointer{reinterpret_cast<SgUnsignedLongLongIntVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedLongLongIntVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 127283 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnsignedLongVal that takes its corresponding StorageClass as parameter
 */
SgUnsignedLongVal::SgUnsignedLongVal(const SgUnsignedLongValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 127293 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnsignedLongVal*
SgUnsignedLongVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnsignedLongVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedLongVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedLongVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnsignedLongVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongVal)};
        unsigned long positionInPool{localIndex % SgUnsignedLongVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedLongVal::pool_size};
        returnPointer = &(((SgUnsignedLongVal*)(SgUnsignedLongVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnsignedLongVal*
SgUnsignedLongVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnsignedLongVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedLongVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedLongVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnsignedLongVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnsignedLongVal)};
        unsigned long positionInPool{localIndex % SgUnsignedLongVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnsignedLongVal::pool_size};
        returnPointer = &(((SgUnsignedLongVal*)(SgUnsignedLongVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnsignedLongVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnsignedLongVal::pools.begin(); block != SgUnsignedLongVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedLongVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedLongVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnsignedLongVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnsignedLongVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnsignedLongVal* pointerOfLinkedList{nullptr};
    for (auto block = SgUnsignedLongVal::pools.begin(); block != SgUnsignedLongVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnsignedLongVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedLongVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnsignedLongVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnsignedLongVal::clearMemoryPool() {
    SgUnsignedLongVal* tempPointer{nullptr};
    if (SgUnsignedLongVal::pools.empty() == false) {
        auto block{SgUnsignedLongVal::pools.begin()};
        SgUnsignedLongVal::next_node = reinterpret_cast<SgUnsignedLongVal*>(*block);
        for (; block != SgUnsignedLongVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnsignedLongVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnsignedLongVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnsignedLongVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnsignedLongVal::pool_size-1]);
        }
    }
}

void
SgUnsignedLongVal::deleteMemoryPool() {
    for (auto p: SgUnsignedLongVal::pools) {
        ROSE_FREE(p);
    }
    SgUnsignedLongVal::next_node = nullptr;
    SgUnsignedLongVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnsignedLongVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedLongVal)};
    for (size_t blockIndex = SgUnsignedLongVal::pools.size(); (blockIndex * SgUnsignedLongVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedLongVal) = %" PRIuPTR " SgUnsignedLongVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnsignedLongVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnsignedLongVal),SgUnsignedLongVal::pool_size);
        }
#endif

        SgUnsignedLongVal* pointer{static_cast<SgUnsignedLongVal*>(ROSE_MALLOC(SgUnsignedLongVal::pool_size * sizeof(SgUnsignedLongVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnsignedLongVal::pool_size * sizeof(SgUnsignedLongVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnsignedLongVal::pool_size * sizeof(SgUnsignedLongVal));
#endif
        SgUnsignedLongVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnsignedLongVal::pool_size * sizeof(SgUnsignedLongVal), V_SgUnsignedLongVal));

        if (SgUnsignedLongVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnsignedLongVal* blkptr{reinterpret_cast<SgUnsignedLongVal*>(SgUnsignedLongVal::pools[blockIndex-1])};
                blkptr[SgUnsignedLongVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnsignedLongVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnsignedLongVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnsignedLongVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnsignedLongVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnsignedLongVal::pool_size - 1};
    SgUnsignedLongVal* testPointer{reinterpret_cast<SgUnsignedLongVal*>(SgUnsignedLongVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnsignedLongVal::pool_size * (SgUnsignedLongVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnsignedLongValStroageClass
 * from its counterpart of SgUnsignedLongVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnsignedLongVal::initializeStorageClassArray(SgUnsignedLongValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnsignedLongVal::pools.begin(); block != SgUnsignedLongVal::pools.end(); ++block) {
        SgUnsignedLongVal* pointer{reinterpret_cast<SgUnsignedLongVal*>(*block)};
        for (unsigned i = 0; i < SgUnsignedLongVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 127512 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFloatVal that takes its corresponding StorageClass as parameter
 */
SgFloatVal::SgFloatVal(const SgFloatValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 127522 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFloatVal*
SgFloatVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFloatVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloatVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloatVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloatVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatVal)};
        unsigned long positionInPool{localIndex % SgFloatVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloatVal::pool_size};
        returnPointer = &(((SgFloatVal*)(SgFloatVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFloatVal*
SgFloatVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFloatVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloatVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloatVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloatVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFloatVal)};
        unsigned long positionInPool{localIndex % SgFloatVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloatVal::pool_size};
        returnPointer = &(((SgFloatVal*)(SgFloatVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFloatVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFloatVal::pools.begin(); block != SgFloatVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloatVal*>(*block)};
        for (unsigned i = 0; i < SgFloatVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFloatVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFloatVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFloatVal* pointerOfLinkedList{nullptr};
    for (auto block = SgFloatVal::pools.begin(); block != SgFloatVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloatVal*>(*block)};
        for (unsigned i = 0; i < SgFloatVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFloatVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFloatVal::clearMemoryPool() {
    SgFloatVal* tempPointer{nullptr};
    if (SgFloatVal::pools.empty() == false) {
        auto block{SgFloatVal::pools.begin()};
        SgFloatVal::next_node = reinterpret_cast<SgFloatVal*>(*block);
        for (; block != SgFloatVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFloatVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFloatVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFloatVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFloatVal::pool_size-1]);
        }
    }
}

void
SgFloatVal::deleteMemoryPool() {
    for (auto p: SgFloatVal::pools) {
        ROSE_FREE(p);
    }
    SgFloatVal::next_node = nullptr;
    SgFloatVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFloatVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloatVal)};
    for (size_t blockIndex = SgFloatVal::pools.size(); (blockIndex * SgFloatVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloatVal) = %" PRIuPTR " SgFloatVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFloatVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloatVal),SgFloatVal::pool_size);
        }
#endif

        SgFloatVal* pointer{static_cast<SgFloatVal*>(ROSE_MALLOC(SgFloatVal::pool_size * sizeof(SgFloatVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFloatVal::pool_size * sizeof(SgFloatVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFloatVal::pool_size * sizeof(SgFloatVal));
#endif
        SgFloatVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFloatVal::pool_size * sizeof(SgFloatVal), V_SgFloatVal));

        if (SgFloatVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFloatVal* blkptr{reinterpret_cast<SgFloatVal*>(SgFloatVal::pools[blockIndex-1])};
                blkptr[SgFloatVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFloatVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFloatVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFloatVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFloatVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFloatVal::pool_size - 1};
    SgFloatVal* testPointer{reinterpret_cast<SgFloatVal*>(SgFloatVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFloatVal::pool_size * (SgFloatVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFloatValStroageClass
 * from its counterpart of SgFloatVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFloatVal::initializeStorageClassArray(SgFloatValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFloatVal::pools.begin(); block != SgFloatVal::pools.end(); ++block) {
        SgFloatVal* pointer{reinterpret_cast<SgFloatVal*>(*block)};
        for (unsigned i = 0; i < SgFloatVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 127741 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDoubleVal that takes its corresponding StorageClass as parameter
 */
SgDoubleVal::SgDoubleVal(const SgDoubleValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 127751 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDoubleVal*
SgDoubleVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDoubleVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDoubleVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDoubleVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDoubleVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDoubleVal)};
        unsigned long positionInPool{localIndex % SgDoubleVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDoubleVal::pool_size};
        returnPointer = &(((SgDoubleVal*)(SgDoubleVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDoubleVal*
SgDoubleVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDoubleVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDoubleVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDoubleVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDoubleVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDoubleVal)};
        unsigned long positionInPool{localIndex % SgDoubleVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDoubleVal::pool_size};
        returnPointer = &(((SgDoubleVal*)(SgDoubleVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDoubleVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDoubleVal::pools.begin(); block != SgDoubleVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDoubleVal*>(*block)};
        for (unsigned i = 0; i < SgDoubleVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDoubleVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDoubleVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDoubleVal* pointerOfLinkedList{nullptr};
    for (auto block = SgDoubleVal::pools.begin(); block != SgDoubleVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDoubleVal*>(*block)};
        for (unsigned i = 0; i < SgDoubleVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDoubleVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDoubleVal::clearMemoryPool() {
    SgDoubleVal* tempPointer{nullptr};
    if (SgDoubleVal::pools.empty() == false) {
        auto block{SgDoubleVal::pools.begin()};
        SgDoubleVal::next_node = reinterpret_cast<SgDoubleVal*>(*block);
        for (; block != SgDoubleVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDoubleVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDoubleVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDoubleVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDoubleVal::pool_size-1]);
        }
    }
}

void
SgDoubleVal::deleteMemoryPool() {
    for (auto p: SgDoubleVal::pools) {
        ROSE_FREE(p);
    }
    SgDoubleVal::next_node = nullptr;
    SgDoubleVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDoubleVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDoubleVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDoubleVal)};
    for (size_t blockIndex = SgDoubleVal::pools.size(); (blockIndex * SgDoubleVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDoubleVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDoubleVal) = %" PRIuPTR " SgDoubleVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDoubleVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDoubleVal),SgDoubleVal::pool_size);
        }
#endif

        SgDoubleVal* pointer{static_cast<SgDoubleVal*>(ROSE_MALLOC(SgDoubleVal::pool_size * sizeof(SgDoubleVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDoubleVal::pool_size * sizeof(SgDoubleVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDoubleVal::pool_size * sizeof(SgDoubleVal));
#endif
        SgDoubleVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDoubleVal::pool_size * sizeof(SgDoubleVal), V_SgDoubleVal));

        if (SgDoubleVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDoubleVal* blkptr{reinterpret_cast<SgDoubleVal*>(SgDoubleVal::pools[blockIndex-1])};
                blkptr[SgDoubleVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDoubleVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDoubleVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDoubleVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDoubleVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDoubleVal::pool_size - 1};
    SgDoubleVal* testPointer{reinterpret_cast<SgDoubleVal*>(SgDoubleVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDoubleVal::pool_size * (SgDoubleVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDoubleValStroageClass
 * from its counterpart of SgDoubleVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDoubleVal::initializeStorageClassArray(SgDoubleValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDoubleVal::pools.begin(); block != SgDoubleVal::pools.end(); ++block) {
        SgDoubleVal* pointer{reinterpret_cast<SgDoubleVal*>(*block)};
        for (unsigned i = 0; i < SgDoubleVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 127970 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLongDoubleVal that takes its corresponding StorageClass as parameter
 */
SgLongDoubleVal::SgLongDoubleVal(const SgLongDoubleValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 127980 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLongDoubleVal*
SgLongDoubleVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLongDoubleVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongDoubleVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongDoubleVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLongDoubleVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLongDoubleVal)};
        unsigned long positionInPool{localIndex % SgLongDoubleVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLongDoubleVal::pool_size};
        returnPointer = &(((SgLongDoubleVal*)(SgLongDoubleVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLongDoubleVal*
SgLongDoubleVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLongDoubleVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongDoubleVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongDoubleVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLongDoubleVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLongDoubleVal)};
        unsigned long positionInPool{localIndex % SgLongDoubleVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLongDoubleVal::pool_size};
        returnPointer = &(((SgLongDoubleVal*)(SgLongDoubleVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLongDoubleVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLongDoubleVal::pools.begin(); block != SgLongDoubleVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLongDoubleVal*>(*block)};
        for (unsigned i = 0; i < SgLongDoubleVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLongDoubleVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLongDoubleVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLongDoubleVal* pointerOfLinkedList{nullptr};
    for (auto block = SgLongDoubleVal::pools.begin(); block != SgLongDoubleVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLongDoubleVal*>(*block)};
        for (unsigned i = 0; i < SgLongDoubleVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLongDoubleVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLongDoubleVal::clearMemoryPool() {
    SgLongDoubleVal* tempPointer{nullptr};
    if (SgLongDoubleVal::pools.empty() == false) {
        auto block{SgLongDoubleVal::pools.begin()};
        SgLongDoubleVal::next_node = reinterpret_cast<SgLongDoubleVal*>(*block);
        for (; block != SgLongDoubleVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLongDoubleVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLongDoubleVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLongDoubleVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLongDoubleVal::pool_size-1]);
        }
    }
}

void
SgLongDoubleVal::deleteMemoryPool() {
    for (auto p: SgLongDoubleVal::pools) {
        ROSE_FREE(p);
    }
    SgLongDoubleVal::next_node = nullptr;
    SgLongDoubleVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLongDoubleVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLongDoubleVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongDoubleVal)};
    for (size_t blockIndex = SgLongDoubleVal::pools.size(); (blockIndex * SgLongDoubleVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLongDoubleVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongDoubleVal) = %" PRIuPTR " SgLongDoubleVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLongDoubleVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLongDoubleVal),SgLongDoubleVal::pool_size);
        }
#endif

        SgLongDoubleVal* pointer{static_cast<SgLongDoubleVal*>(ROSE_MALLOC(SgLongDoubleVal::pool_size * sizeof(SgLongDoubleVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLongDoubleVal::pool_size * sizeof(SgLongDoubleVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLongDoubleVal::pool_size * sizeof(SgLongDoubleVal));
#endif
        SgLongDoubleVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLongDoubleVal::pool_size * sizeof(SgLongDoubleVal), V_SgLongDoubleVal));

        if (SgLongDoubleVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLongDoubleVal* blkptr{reinterpret_cast<SgLongDoubleVal*>(SgLongDoubleVal::pools[blockIndex-1])};
                blkptr[SgLongDoubleVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLongDoubleVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLongDoubleVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLongDoubleVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLongDoubleVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLongDoubleVal::pool_size - 1};
    SgLongDoubleVal* testPointer{reinterpret_cast<SgLongDoubleVal*>(SgLongDoubleVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLongDoubleVal::pool_size * (SgLongDoubleVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLongDoubleValStroageClass
 * from its counterpart of SgLongDoubleVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLongDoubleVal::initializeStorageClassArray(SgLongDoubleValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLongDoubleVal::pools.begin(); block != SgLongDoubleVal::pools.end(); ++block) {
        SgLongDoubleVal* pointer{reinterpret_cast<SgLongDoubleVal*>(*block)};
        for (unsigned i = 0; i < SgLongDoubleVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 128199 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgComplexVal that takes its corresponding StorageClass as parameter
 */
SgComplexVal::SgComplexVal(const SgComplexValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 128209 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_real_value = (SgValueExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_real_value));
     p_imaginary_value = (SgValueExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_imaginary_value));
     p_precisionType = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_precisionType));
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgComplexVal*
SgComplexVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgComplexVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComplexVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComplexVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComplexVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgComplexVal)};
        unsigned long positionInPool{localIndex % SgComplexVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgComplexVal::pool_size};
        returnPointer = &(((SgComplexVal*)(SgComplexVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgComplexVal*
SgComplexVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgComplexVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComplexVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComplexVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComplexVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgComplexVal)};
        unsigned long positionInPool{localIndex % SgComplexVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgComplexVal::pool_size};
        returnPointer = &(((SgComplexVal*)(SgComplexVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgComplexVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgComplexVal::pools.begin(); block != SgComplexVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgComplexVal*>(*block)};
        for (unsigned i = 0; i < SgComplexVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgComplexVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgComplexVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgComplexVal* pointerOfLinkedList{nullptr};
    for (auto block = SgComplexVal::pools.begin(); block != SgComplexVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgComplexVal*>(*block)};
        for (unsigned i = 0; i < SgComplexVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgComplexVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgComplexVal::clearMemoryPool() {
    SgComplexVal* tempPointer{nullptr};
    if (SgComplexVal::pools.empty() == false) {
        auto block{SgComplexVal::pools.begin()};
        SgComplexVal::next_node = reinterpret_cast<SgComplexVal*>(*block);
        for (; block != SgComplexVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgComplexVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgComplexVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgComplexVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgComplexVal::pool_size-1]);
        }
    }
}

void
SgComplexVal::deleteMemoryPool() {
    for (auto p: SgComplexVal::pools) {
        ROSE_FREE(p);
    }
    SgComplexVal::next_node = nullptr;
    SgComplexVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgComplexVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgComplexVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgComplexVal)};
    for (size_t blockIndex = SgComplexVal::pools.size(); (blockIndex * SgComplexVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgComplexVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgComplexVal) = %" PRIuPTR " SgComplexVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgComplexVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgComplexVal),SgComplexVal::pool_size);
        }
#endif

        SgComplexVal* pointer{static_cast<SgComplexVal*>(ROSE_MALLOC(SgComplexVal::pool_size * sizeof(SgComplexVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgComplexVal::pool_size * sizeof(SgComplexVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgComplexVal::pool_size * sizeof(SgComplexVal));
#endif
        SgComplexVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgComplexVal::pool_size * sizeof(SgComplexVal), V_SgComplexVal));

        if (SgComplexVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgComplexVal* blkptr{reinterpret_cast<SgComplexVal*>(SgComplexVal::pools[blockIndex-1])};
                blkptr[SgComplexVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgComplexVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgComplexVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgComplexVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgComplexVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgComplexVal::pool_size - 1};
    SgComplexVal* testPointer{reinterpret_cast<SgComplexVal*>(SgComplexVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgComplexVal::pool_size * (SgComplexVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgComplexValStroageClass
 * from its counterpart of SgComplexVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgComplexVal::initializeStorageClassArray(SgComplexValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgComplexVal::pools.begin(); block != SgComplexVal::pools.end(); ++block) {
        SgComplexVal* pointer{reinterpret_cast<SgComplexVal*>(*block)};
        for (unsigned i = 0; i < SgComplexVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 128430 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcThreads that takes its corresponding StorageClass as parameter
 */
SgUpcThreads::SgUpcThreads(const SgUpcThreadsStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 128440 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcThreads*
SgUpcThreads::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcThreads* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcThreads)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcThreads + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcThreads)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcThreads)};
        unsigned long positionInPool{localIndex % SgUpcThreads::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcThreads::pool_size};
        returnPointer = &(((SgUpcThreads*)(SgUpcThreads::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcThreads*
SgUpcThreads::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcThreads* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcThreads)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcThreads + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcThreads)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcThreads)};
        unsigned long positionInPool{localIndex % SgUpcThreads::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcThreads::pool_size};
        returnPointer = &(((SgUpcThreads*)(SgUpcThreads::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcThreads::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcThreads::pools.begin(); block != SgUpcThreads::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcThreads*>(*block)};
        for (unsigned i = 0; i < SgUpcThreads::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcThreads*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcThreads::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcThreads* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcThreads::pools.begin(); block != SgUpcThreads::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcThreads*>(*block)};
        for (unsigned i = 0; i < SgUpcThreads::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcThreads::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcThreads::clearMemoryPool() {
    SgUpcThreads* tempPointer{nullptr};
    if (SgUpcThreads::pools.empty() == false) {
        auto block{SgUpcThreads::pools.begin()};
        SgUpcThreads::next_node = reinterpret_cast<SgUpcThreads*>(*block);
        for (; block != SgUpcThreads::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcThreads*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcThreads::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcThreads::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcThreads::pool_size-1]);
        }
    }
}

void
SgUpcThreads::deleteMemoryPool() {
    for (auto p: SgUpcThreads::pools) {
        ROSE_FREE(p);
    }
    SgUpcThreads::next_node = nullptr;
    SgUpcThreads::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcThreads::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcThreads) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcThreads)};
    for (size_t blockIndex = SgUpcThreads::pools.size(); (blockIndex * SgUpcThreads::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcThreads) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcThreads) = %" PRIuPTR " SgUpcThreads::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcThreads),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcThreads),SgUpcThreads::pool_size);
        }
#endif

        SgUpcThreads* pointer{static_cast<SgUpcThreads*>(ROSE_MALLOC(SgUpcThreads::pool_size * sizeof(SgUpcThreads)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcThreads::pool_size * sizeof(SgUpcThreads));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcThreads::pool_size * sizeof(SgUpcThreads));
#endif
        SgUpcThreads::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcThreads::pool_size * sizeof(SgUpcThreads), V_SgUpcThreads));

        if (SgUpcThreads::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcThreads* blkptr{reinterpret_cast<SgUpcThreads*>(SgUpcThreads::pools[blockIndex-1])};
                blkptr[SgUpcThreads::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcThreads::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcThreads::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcThreads::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcThreads::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcThreads::pool_size - 1};
    SgUpcThreads* testPointer{reinterpret_cast<SgUpcThreads*>(SgUpcThreads::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcThreads::pool_size * (SgUpcThreads::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcThreadsStroageClass
 * from its counterpart of SgUpcThreads. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcThreads::initializeStorageClassArray(SgUpcThreadsStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcThreads::pools.begin(); block != SgUpcThreads::pools.end(); ++block) {
        SgUpcThreads* pointer{reinterpret_cast<SgUpcThreads*>(*block)};
        for (unsigned i = 0; i < SgUpcThreads::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 128659 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcMythread that takes its corresponding StorageClass as parameter
 */
SgUpcMythread::SgUpcMythread(const SgUpcMythreadStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 128669 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcMythread*
SgUpcMythread::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcMythread* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcMythread)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcMythread + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcMythread)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcMythread)};
        unsigned long positionInPool{localIndex % SgUpcMythread::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcMythread::pool_size};
        returnPointer = &(((SgUpcMythread*)(SgUpcMythread::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcMythread*
SgUpcMythread::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcMythread* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcMythread)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcMythread + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcMythread)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcMythread)};
        unsigned long positionInPool{localIndex % SgUpcMythread::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcMythread::pool_size};
        returnPointer = &(((SgUpcMythread*)(SgUpcMythread::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcMythread::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcMythread::pools.begin(); block != SgUpcMythread::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcMythread*>(*block)};
        for (unsigned i = 0; i < SgUpcMythread::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcMythread*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcMythread::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcMythread* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcMythread::pools.begin(); block != SgUpcMythread::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcMythread*>(*block)};
        for (unsigned i = 0; i < SgUpcMythread::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcMythread::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcMythread::clearMemoryPool() {
    SgUpcMythread* tempPointer{nullptr};
    if (SgUpcMythread::pools.empty() == false) {
        auto block{SgUpcMythread::pools.begin()};
        SgUpcMythread::next_node = reinterpret_cast<SgUpcMythread*>(*block);
        for (; block != SgUpcMythread::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcMythread*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcMythread::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcMythread::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcMythread::pool_size-1]);
        }
    }
}

void
SgUpcMythread::deleteMemoryPool() {
    for (auto p: SgUpcMythread::pools) {
        ROSE_FREE(p);
    }
    SgUpcMythread::next_node = nullptr;
    SgUpcMythread::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcMythread::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcMythread) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcMythread)};
    for (size_t blockIndex = SgUpcMythread::pools.size(); (blockIndex * SgUpcMythread::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcMythread) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcMythread) = %" PRIuPTR " SgUpcMythread::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcMythread),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcMythread),SgUpcMythread::pool_size);
        }
#endif

        SgUpcMythread* pointer{static_cast<SgUpcMythread*>(ROSE_MALLOC(SgUpcMythread::pool_size * sizeof(SgUpcMythread)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcMythread::pool_size * sizeof(SgUpcMythread));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcMythread::pool_size * sizeof(SgUpcMythread));
#endif
        SgUpcMythread::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcMythread::pool_size * sizeof(SgUpcMythread), V_SgUpcMythread));

        if (SgUpcMythread::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcMythread* blkptr{reinterpret_cast<SgUpcMythread*>(SgUpcMythread::pools[blockIndex-1])};
                blkptr[SgUpcMythread::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcMythread::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcMythread::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcMythread::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcMythread::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcMythread::pool_size - 1};
    SgUpcMythread* testPointer{reinterpret_cast<SgUpcMythread*>(SgUpcMythread::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcMythread::pool_size * (SgUpcMythread::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcMythreadStroageClass
 * from its counterpart of SgUpcMythread. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcMythread::initializeStorageClassArray(SgUpcMythreadStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcMythread::pools.begin(); block != SgUpcMythread::pools.end(); ++block) {
        SgUpcMythread* pointer{reinterpret_cast<SgUpcMythread*>(*block)};
        for (unsigned i = 0; i < SgUpcMythread::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 128888 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateParameterVal that takes its corresponding StorageClass as parameter
 */
SgTemplateParameterVal::SgTemplateParameterVal(const SgTemplateParameterValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 128898 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_template_parameter_position = storageSource.storageOf_template_parameter_position;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();
     p_valueType = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_valueType));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateParameterVal*
SgTemplateParameterVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateParameterVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameterVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameterVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateParameterVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterVal)};
        unsigned long positionInPool{localIndex % SgTemplateParameterVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateParameterVal::pool_size};
        returnPointer = &(((SgTemplateParameterVal*)(SgTemplateParameterVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateParameterVal*
SgTemplateParameterVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateParameterVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameterVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameterVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateParameterVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateParameterVal)};
        unsigned long positionInPool{localIndex % SgTemplateParameterVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateParameterVal::pool_size};
        returnPointer = &(((SgTemplateParameterVal*)(SgTemplateParameterVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateParameterVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateParameterVal::pools.begin(); block != SgTemplateParameterVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateParameterVal*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameterVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateParameterVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateParameterVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateParameterVal* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateParameterVal::pools.begin(); block != SgTemplateParameterVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateParameterVal*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameterVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateParameterVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateParameterVal::clearMemoryPool() {
    SgTemplateParameterVal* tempPointer{nullptr};
    if (SgTemplateParameterVal::pools.empty() == false) {
        auto block{SgTemplateParameterVal::pools.begin()};
        SgTemplateParameterVal::next_node = reinterpret_cast<SgTemplateParameterVal*>(*block);
        for (; block != SgTemplateParameterVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateParameterVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateParameterVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateParameterVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateParameterVal::pool_size-1]);
        }
    }
}

void
SgTemplateParameterVal::deleteMemoryPool() {
    for (auto p: SgTemplateParameterVal::pools) {
        ROSE_FREE(p);
    }
    SgTemplateParameterVal::next_node = nullptr;
    SgTemplateParameterVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateParameterVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameterVal)};
    for (size_t blockIndex = SgTemplateParameterVal::pools.size(); (blockIndex * SgTemplateParameterVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameterVal) = %" PRIuPTR " SgTemplateParameterVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateParameterVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateParameterVal),SgTemplateParameterVal::pool_size);
        }
#endif

        SgTemplateParameterVal* pointer{static_cast<SgTemplateParameterVal*>(ROSE_MALLOC(SgTemplateParameterVal::pool_size * sizeof(SgTemplateParameterVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateParameterVal::pool_size * sizeof(SgTemplateParameterVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateParameterVal::pool_size * sizeof(SgTemplateParameterVal));
#endif
        SgTemplateParameterVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateParameterVal::pool_size * sizeof(SgTemplateParameterVal), V_SgTemplateParameterVal));

        if (SgTemplateParameterVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateParameterVal* blkptr{reinterpret_cast<SgTemplateParameterVal*>(SgTemplateParameterVal::pools[blockIndex-1])};
                blkptr[SgTemplateParameterVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateParameterVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateParameterVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateParameterVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateParameterVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateParameterVal::pool_size - 1};
    SgTemplateParameterVal* testPointer{reinterpret_cast<SgTemplateParameterVal*>(SgTemplateParameterVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateParameterVal::pool_size * (SgTemplateParameterVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateParameterValStroageClass
 * from its counterpart of SgTemplateParameterVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateParameterVal::initializeStorageClassArray(SgTemplateParameterValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateParameterVal::pools.begin(); block != SgTemplateParameterVal::pools.end(); ++block) {
        SgTemplateParameterVal* pointer{reinterpret_cast<SgTemplateParameterVal*>(*block)};
        for (unsigned i = 0; i < SgTemplateParameterVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 129118 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNullptrValExp that takes its corresponding StorageClass as parameter
 */
SgNullptrValExp::SgNullptrValExp(const SgNullptrValExpStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 129128 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNullptrValExp*
SgNullptrValExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNullptrValExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullptrValExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullptrValExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullptrValExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNullptrValExp)};
        unsigned long positionInPool{localIndex % SgNullptrValExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullptrValExp::pool_size};
        returnPointer = &(((SgNullptrValExp*)(SgNullptrValExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNullptrValExp*
SgNullptrValExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNullptrValExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullptrValExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullptrValExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullptrValExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNullptrValExp)};
        unsigned long positionInPool{localIndex % SgNullptrValExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullptrValExp::pool_size};
        returnPointer = &(((SgNullptrValExp*)(SgNullptrValExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNullptrValExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNullptrValExp::pools.begin(); block != SgNullptrValExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullptrValExp*>(*block)};
        for (unsigned i = 0; i < SgNullptrValExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNullptrValExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNullptrValExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNullptrValExp* pointerOfLinkedList{nullptr};
    for (auto block = SgNullptrValExp::pools.begin(); block != SgNullptrValExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullptrValExp*>(*block)};
        for (unsigned i = 0; i < SgNullptrValExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNullptrValExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNullptrValExp::clearMemoryPool() {
    SgNullptrValExp* tempPointer{nullptr};
    if (SgNullptrValExp::pools.empty() == false) {
        auto block{SgNullptrValExp::pools.begin()};
        SgNullptrValExp::next_node = reinterpret_cast<SgNullptrValExp*>(*block);
        for (; block != SgNullptrValExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNullptrValExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNullptrValExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNullptrValExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNullptrValExp::pool_size-1]);
        }
    }
}

void
SgNullptrValExp::deleteMemoryPool() {
    for (auto p: SgNullptrValExp::pools) {
        ROSE_FREE(p);
    }
    SgNullptrValExp::next_node = nullptr;
    SgNullptrValExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNullptrValExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNullptrValExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullptrValExp)};
    for (size_t blockIndex = SgNullptrValExp::pools.size(); (blockIndex * SgNullptrValExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNullptrValExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullptrValExp) = %" PRIuPTR " SgNullptrValExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNullptrValExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullptrValExp),SgNullptrValExp::pool_size);
        }
#endif

        SgNullptrValExp* pointer{static_cast<SgNullptrValExp*>(ROSE_MALLOC(SgNullptrValExp::pool_size * sizeof(SgNullptrValExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNullptrValExp::pool_size * sizeof(SgNullptrValExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNullptrValExp::pool_size * sizeof(SgNullptrValExp));
#endif
        SgNullptrValExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNullptrValExp::pool_size * sizeof(SgNullptrValExp), V_SgNullptrValExp));

        if (SgNullptrValExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNullptrValExp* blkptr{reinterpret_cast<SgNullptrValExp*>(SgNullptrValExp::pools[blockIndex-1])};
                blkptr[SgNullptrValExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNullptrValExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNullptrValExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNullptrValExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNullptrValExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNullptrValExp::pool_size - 1};
    SgNullptrValExp* testPointer{reinterpret_cast<SgNullptrValExp*>(SgNullptrValExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNullptrValExp::pool_size * (SgNullptrValExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNullptrValExpStroageClass
 * from its counterpart of SgNullptrValExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNullptrValExp::initializeStorageClassArray(SgNullptrValExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNullptrValExp::pools.begin(); block != SgNullptrValExp::pools.end(); ++block) {
        SgNullptrValExp* pointer{reinterpret_cast<SgNullptrValExp*>(*block)};
        for (unsigned i = 0; i < SgNullptrValExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 129345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgChar16Val that takes its corresponding StorageClass as parameter
 */
SgChar16Val::SgChar16Val(const SgChar16ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 129355 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_valueUL = storageSource.storageOf_valueUL;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgChar16Val*
SgChar16Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgChar16Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChar16Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChar16Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChar16Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgChar16Val)};
        unsigned long positionInPool{localIndex % SgChar16Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgChar16Val::pool_size};
        returnPointer = &(((SgChar16Val*)(SgChar16Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgChar16Val*
SgChar16Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgChar16Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChar16Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChar16Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChar16Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgChar16Val)};
        unsigned long positionInPool{localIndex % SgChar16Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgChar16Val::pool_size};
        returnPointer = &(((SgChar16Val*)(SgChar16Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgChar16Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgChar16Val::pools.begin(); block != SgChar16Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgChar16Val*>(*block)};
        for (unsigned i = 0; i < SgChar16Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgChar16Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgChar16Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgChar16Val* pointerOfLinkedList{nullptr};
    for (auto block = SgChar16Val::pools.begin(); block != SgChar16Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgChar16Val*>(*block)};
        for (unsigned i = 0; i < SgChar16Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgChar16Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgChar16Val::clearMemoryPool() {
    SgChar16Val* tempPointer{nullptr};
    if (SgChar16Val::pools.empty() == false) {
        auto block{SgChar16Val::pools.begin()};
        SgChar16Val::next_node = reinterpret_cast<SgChar16Val*>(*block);
        for (; block != SgChar16Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgChar16Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgChar16Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgChar16Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgChar16Val::pool_size-1]);
        }
    }
}

void
SgChar16Val::deleteMemoryPool() {
    for (auto p: SgChar16Val::pools) {
        ROSE_FREE(p);
    }
    SgChar16Val::next_node = nullptr;
    SgChar16Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgChar16Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgChar16Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgChar16Val)};
    for (size_t blockIndex = SgChar16Val::pools.size(); (blockIndex * SgChar16Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgChar16Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgChar16Val) = %" PRIuPTR " SgChar16Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgChar16Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgChar16Val),SgChar16Val::pool_size);
        }
#endif

        SgChar16Val* pointer{static_cast<SgChar16Val*>(ROSE_MALLOC(SgChar16Val::pool_size * sizeof(SgChar16Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgChar16Val::pool_size * sizeof(SgChar16Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgChar16Val::pool_size * sizeof(SgChar16Val));
#endif
        SgChar16Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgChar16Val::pool_size * sizeof(SgChar16Val), V_SgChar16Val));

        if (SgChar16Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgChar16Val* blkptr{reinterpret_cast<SgChar16Val*>(SgChar16Val::pools[blockIndex-1])};
                blkptr[SgChar16Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgChar16Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgChar16Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgChar16Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgChar16Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgChar16Val::pool_size - 1};
    SgChar16Val* testPointer{reinterpret_cast<SgChar16Val*>(SgChar16Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgChar16Val::pool_size * (SgChar16Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgChar16ValStroageClass
 * from its counterpart of SgChar16Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgChar16Val::initializeStorageClassArray(SgChar16ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgChar16Val::pools.begin(); block != SgChar16Val::pools.end(); ++block) {
        SgChar16Val* pointer{reinterpret_cast<SgChar16Val*>(*block)};
        for (unsigned i = 0; i < SgChar16Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 129574 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgChar32Val that takes its corresponding StorageClass as parameter
 */
SgChar32Val::SgChar32Val(const SgChar32ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 129584 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_valueUL = storageSource.storageOf_valueUL;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgChar32Val*
SgChar32Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgChar32Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChar32Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChar32Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChar32Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgChar32Val)};
        unsigned long positionInPool{localIndex % SgChar32Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgChar32Val::pool_size};
        returnPointer = &(((SgChar32Val*)(SgChar32Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgChar32Val*
SgChar32Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgChar32Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChar32Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChar32Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChar32Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgChar32Val)};
        unsigned long positionInPool{localIndex % SgChar32Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgChar32Val::pool_size};
        returnPointer = &(((SgChar32Val*)(SgChar32Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgChar32Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgChar32Val::pools.begin(); block != SgChar32Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgChar32Val*>(*block)};
        for (unsigned i = 0; i < SgChar32Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgChar32Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgChar32Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgChar32Val* pointerOfLinkedList{nullptr};
    for (auto block = SgChar32Val::pools.begin(); block != SgChar32Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgChar32Val*>(*block)};
        for (unsigned i = 0; i < SgChar32Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgChar32Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgChar32Val::clearMemoryPool() {
    SgChar32Val* tempPointer{nullptr};
    if (SgChar32Val::pools.empty() == false) {
        auto block{SgChar32Val::pools.begin()};
        SgChar32Val::next_node = reinterpret_cast<SgChar32Val*>(*block);
        for (; block != SgChar32Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgChar32Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgChar32Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgChar32Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgChar32Val::pool_size-1]);
        }
    }
}

void
SgChar32Val::deleteMemoryPool() {
    for (auto p: SgChar32Val::pools) {
        ROSE_FREE(p);
    }
    SgChar32Val::next_node = nullptr;
    SgChar32Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgChar32Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgChar32Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgChar32Val)};
    for (size_t blockIndex = SgChar32Val::pools.size(); (blockIndex * SgChar32Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgChar32Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgChar32Val) = %" PRIuPTR " SgChar32Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgChar32Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgChar32Val),SgChar32Val::pool_size);
        }
#endif

        SgChar32Val* pointer{static_cast<SgChar32Val*>(ROSE_MALLOC(SgChar32Val::pool_size * sizeof(SgChar32Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgChar32Val::pool_size * sizeof(SgChar32Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgChar32Val::pool_size * sizeof(SgChar32Val));
#endif
        SgChar32Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgChar32Val::pool_size * sizeof(SgChar32Val), V_SgChar32Val));

        if (SgChar32Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgChar32Val* blkptr{reinterpret_cast<SgChar32Val*>(SgChar32Val::pools[blockIndex-1])};
                blkptr[SgChar32Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgChar32Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgChar32Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgChar32Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgChar32Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgChar32Val::pool_size - 1};
    SgChar32Val* testPointer{reinterpret_cast<SgChar32Val*>(SgChar32Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgChar32Val::pool_size * (SgChar32Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgChar32ValStroageClass
 * from its counterpart of SgChar32Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgChar32Val::initializeStorageClassArray(SgChar32ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgChar32Val::pools.begin(); block != SgChar32Val::pools.end(); ++block) {
        SgChar32Val* pointer{reinterpret_cast<SgChar32Val*>(*block)};
        for (unsigned i = 0; i < SgChar32Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 129803 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFloat80Val that takes its corresponding StorageClass as parameter
 */
SgFloat80Val::SgFloat80Val(const SgFloat80ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 129813 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFloat80Val*
SgFloat80Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFloat80Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat80Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat80Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat80Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat80Val)};
        unsigned long positionInPool{localIndex % SgFloat80Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat80Val::pool_size};
        returnPointer = &(((SgFloat80Val*)(SgFloat80Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFloat80Val*
SgFloat80Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFloat80Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat80Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat80Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat80Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFloat80Val)};
        unsigned long positionInPool{localIndex % SgFloat80Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat80Val::pool_size};
        returnPointer = &(((SgFloat80Val*)(SgFloat80Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFloat80Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFloat80Val::pools.begin(); block != SgFloat80Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat80Val*>(*block)};
        for (unsigned i = 0; i < SgFloat80Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFloat80Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFloat80Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFloat80Val* pointerOfLinkedList{nullptr};
    for (auto block = SgFloat80Val::pools.begin(); block != SgFloat80Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat80Val*>(*block)};
        for (unsigned i = 0; i < SgFloat80Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFloat80Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFloat80Val::clearMemoryPool() {
    SgFloat80Val* tempPointer{nullptr};
    if (SgFloat80Val::pools.empty() == false) {
        auto block{SgFloat80Val::pools.begin()};
        SgFloat80Val::next_node = reinterpret_cast<SgFloat80Val*>(*block);
        for (; block != SgFloat80Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFloat80Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFloat80Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFloat80Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFloat80Val::pool_size-1]);
        }
    }
}

void
SgFloat80Val::deleteMemoryPool() {
    for (auto p: SgFloat80Val::pools) {
        ROSE_FREE(p);
    }
    SgFloat80Val::next_node = nullptr;
    SgFloat80Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFloat80Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat80Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat80Val)};
    for (size_t blockIndex = SgFloat80Val::pools.size(); (blockIndex * SgFloat80Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat80Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat80Val) = %" PRIuPTR " SgFloat80Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat80Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat80Val),SgFloat80Val::pool_size);
        }
#endif

        SgFloat80Val* pointer{static_cast<SgFloat80Val*>(ROSE_MALLOC(SgFloat80Val::pool_size * sizeof(SgFloat80Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFloat80Val::pool_size * sizeof(SgFloat80Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFloat80Val::pool_size * sizeof(SgFloat80Val));
#endif
        SgFloat80Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFloat80Val::pool_size * sizeof(SgFloat80Val), V_SgFloat80Val));

        if (SgFloat80Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFloat80Val* blkptr{reinterpret_cast<SgFloat80Val*>(SgFloat80Val::pools[blockIndex-1])};
                blkptr[SgFloat80Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFloat80Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFloat80Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFloat80Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFloat80Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFloat80Val::pool_size - 1};
    SgFloat80Val* testPointer{reinterpret_cast<SgFloat80Val*>(SgFloat80Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFloat80Val::pool_size * (SgFloat80Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFloat80ValStroageClass
 * from its counterpart of SgFloat80Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFloat80Val::initializeStorageClassArray(SgFloat80ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFloat80Val::pools.begin(); block != SgFloat80Val::pools.end(); ++block) {
        SgFloat80Val* pointer{reinterpret_cast<SgFloat80Val*>(*block)};
        for (unsigned i = 0; i < SgFloat80Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 130032 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFloat128Val that takes its corresponding StorageClass as parameter
 */
SgFloat128Val::SgFloat128Val(const SgFloat128ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 130042 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFloat128Val*
SgFloat128Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFloat128Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat128Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat128Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat128Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat128Val)};
        unsigned long positionInPool{localIndex % SgFloat128Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat128Val::pool_size};
        returnPointer = &(((SgFloat128Val*)(SgFloat128Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFloat128Val*
SgFloat128Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFloat128Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat128Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat128Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat128Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFloat128Val)};
        unsigned long positionInPool{localIndex % SgFloat128Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat128Val::pool_size};
        returnPointer = &(((SgFloat128Val*)(SgFloat128Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFloat128Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFloat128Val::pools.begin(); block != SgFloat128Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat128Val*>(*block)};
        for (unsigned i = 0; i < SgFloat128Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFloat128Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFloat128Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFloat128Val* pointerOfLinkedList{nullptr};
    for (auto block = SgFloat128Val::pools.begin(); block != SgFloat128Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat128Val*>(*block)};
        for (unsigned i = 0; i < SgFloat128Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFloat128Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFloat128Val::clearMemoryPool() {
    SgFloat128Val* tempPointer{nullptr};
    if (SgFloat128Val::pools.empty() == false) {
        auto block{SgFloat128Val::pools.begin()};
        SgFloat128Val::next_node = reinterpret_cast<SgFloat128Val*>(*block);
        for (; block != SgFloat128Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFloat128Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFloat128Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFloat128Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFloat128Val::pool_size-1]);
        }
    }
}

void
SgFloat128Val::deleteMemoryPool() {
    for (auto p: SgFloat128Val::pools) {
        ROSE_FREE(p);
    }
    SgFloat128Val::next_node = nullptr;
    SgFloat128Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFloat128Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat128Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat128Val)};
    for (size_t blockIndex = SgFloat128Val::pools.size(); (blockIndex * SgFloat128Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat128Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat128Val) = %" PRIuPTR " SgFloat128Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat128Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat128Val),SgFloat128Val::pool_size);
        }
#endif

        SgFloat128Val* pointer{static_cast<SgFloat128Val*>(ROSE_MALLOC(SgFloat128Val::pool_size * sizeof(SgFloat128Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFloat128Val::pool_size * sizeof(SgFloat128Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFloat128Val::pool_size * sizeof(SgFloat128Val));
#endif
        SgFloat128Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFloat128Val::pool_size * sizeof(SgFloat128Val), V_SgFloat128Val));

        if (SgFloat128Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFloat128Val* blkptr{reinterpret_cast<SgFloat128Val*>(SgFloat128Val::pools[blockIndex-1])};
                blkptr[SgFloat128Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFloat128Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFloat128Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFloat128Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFloat128Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFloat128Val::pool_size - 1};
    SgFloat128Val* testPointer{reinterpret_cast<SgFloat128Val*>(SgFloat128Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFloat128Val::pool_size * (SgFloat128Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFloat128ValStroageClass
 * from its counterpart of SgFloat128Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFloat128Val::initializeStorageClassArray(SgFloat128ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFloat128Val::pools.begin(); block != SgFloat128Val::pools.end(); ++block) {
        SgFloat128Val* pointer{reinterpret_cast<SgFloat128Val*>(*block)};
        for (unsigned i = 0; i < SgFloat128Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 130261 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVoidVal that takes its corresponding StorageClass as parameter
 */
SgVoidVal::SgVoidVal(const SgVoidValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 130271 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVoidVal*
SgVoidVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVoidVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVoidVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVoidVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVoidVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVoidVal)};
        unsigned long positionInPool{localIndex % SgVoidVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVoidVal::pool_size};
        returnPointer = &(((SgVoidVal*)(SgVoidVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVoidVal*
SgVoidVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVoidVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVoidVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVoidVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVoidVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVoidVal)};
        unsigned long positionInPool{localIndex % SgVoidVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVoidVal::pool_size};
        returnPointer = &(((SgVoidVal*)(SgVoidVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVoidVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVoidVal::pools.begin(); block != SgVoidVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVoidVal*>(*block)};
        for (unsigned i = 0; i < SgVoidVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVoidVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVoidVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVoidVal* pointerOfLinkedList{nullptr};
    for (auto block = SgVoidVal::pools.begin(); block != SgVoidVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVoidVal*>(*block)};
        for (unsigned i = 0; i < SgVoidVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVoidVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVoidVal::clearMemoryPool() {
    SgVoidVal* tempPointer{nullptr};
    if (SgVoidVal::pools.empty() == false) {
        auto block{SgVoidVal::pools.begin()};
        SgVoidVal::next_node = reinterpret_cast<SgVoidVal*>(*block);
        for (; block != SgVoidVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVoidVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVoidVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVoidVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVoidVal::pool_size-1]);
        }
    }
}

void
SgVoidVal::deleteMemoryPool() {
    for (auto p: SgVoidVal::pools) {
        ROSE_FREE(p);
    }
    SgVoidVal::next_node = nullptr;
    SgVoidVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVoidVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVoidVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVoidVal)};
    for (size_t blockIndex = SgVoidVal::pools.size(); (blockIndex * SgVoidVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVoidVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVoidVal) = %" PRIuPTR " SgVoidVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVoidVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVoidVal),SgVoidVal::pool_size);
        }
#endif

        SgVoidVal* pointer{static_cast<SgVoidVal*>(ROSE_MALLOC(SgVoidVal::pool_size * sizeof(SgVoidVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVoidVal::pool_size * sizeof(SgVoidVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVoidVal::pool_size * sizeof(SgVoidVal));
#endif
        SgVoidVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVoidVal::pool_size * sizeof(SgVoidVal), V_SgVoidVal));

        if (SgVoidVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVoidVal* blkptr{reinterpret_cast<SgVoidVal*>(SgVoidVal::pools[blockIndex-1])};
                blkptr[SgVoidVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVoidVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVoidVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVoidVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVoidVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVoidVal::pool_size - 1};
    SgVoidVal* testPointer{reinterpret_cast<SgVoidVal*>(SgVoidVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVoidVal::pool_size * (SgVoidVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVoidValStroageClass
 * from its counterpart of SgVoidVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVoidVal::initializeStorageClassArray(SgVoidValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVoidVal::pools.begin(); block != SgVoidVal::pools.end(); ++block) {
        SgVoidVal* pointer{reinterpret_cast<SgVoidVal*>(*block)};
        for (unsigned i = 0; i < SgVoidVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 130488 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaFloatVal that takes its corresponding StorageClass as parameter
 */
SgAdaFloatVal::SgAdaFloatVal(const SgAdaFloatValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 130498 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaFloatVal*
SgAdaFloatVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaFloatVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFloatVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFloatVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFloatVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFloatVal)};
        unsigned long positionInPool{localIndex % SgAdaFloatVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFloatVal::pool_size};
        returnPointer = &(((SgAdaFloatVal*)(SgAdaFloatVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaFloatVal*
SgAdaFloatVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaFloatVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFloatVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFloatVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFloatVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaFloatVal)};
        unsigned long positionInPool{localIndex % SgAdaFloatVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFloatVal::pool_size};
        returnPointer = &(((SgAdaFloatVal*)(SgAdaFloatVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaFloatVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaFloatVal::pools.begin(); block != SgAdaFloatVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFloatVal*>(*block)};
        for (unsigned i = 0; i < SgAdaFloatVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaFloatVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaFloatVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaFloatVal* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaFloatVal::pools.begin(); block != SgAdaFloatVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFloatVal*>(*block)};
        for (unsigned i = 0; i < SgAdaFloatVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaFloatVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaFloatVal::clearMemoryPool() {
    SgAdaFloatVal* tempPointer{nullptr};
    if (SgAdaFloatVal::pools.empty() == false) {
        auto block{SgAdaFloatVal::pools.begin()};
        SgAdaFloatVal::next_node = reinterpret_cast<SgAdaFloatVal*>(*block);
        for (; block != SgAdaFloatVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaFloatVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaFloatVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaFloatVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaFloatVal::pool_size-1]);
        }
    }
}

void
SgAdaFloatVal::deleteMemoryPool() {
    for (auto p: SgAdaFloatVal::pools) {
        ROSE_FREE(p);
    }
    SgAdaFloatVal::next_node = nullptr;
    SgAdaFloatVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaFloatVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFloatVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFloatVal)};
    for (size_t blockIndex = SgAdaFloatVal::pools.size(); (blockIndex * SgAdaFloatVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFloatVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFloatVal) = %" PRIuPTR " SgAdaFloatVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFloatVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFloatVal),SgAdaFloatVal::pool_size);
        }
#endif

        SgAdaFloatVal* pointer{static_cast<SgAdaFloatVal*>(ROSE_MALLOC(SgAdaFloatVal::pool_size * sizeof(SgAdaFloatVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaFloatVal::pool_size * sizeof(SgAdaFloatVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaFloatVal::pool_size * sizeof(SgAdaFloatVal));
#endif
        SgAdaFloatVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaFloatVal::pool_size * sizeof(SgAdaFloatVal), V_SgAdaFloatVal));

        if (SgAdaFloatVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaFloatVal* blkptr{reinterpret_cast<SgAdaFloatVal*>(SgAdaFloatVal::pools[blockIndex-1])};
                blkptr[SgAdaFloatVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaFloatVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaFloatVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaFloatVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaFloatVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaFloatVal::pool_size - 1};
    SgAdaFloatVal* testPointer{reinterpret_cast<SgAdaFloatVal*>(SgAdaFloatVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaFloatVal::pool_size * (SgAdaFloatVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaFloatValStroageClass
 * from its counterpart of SgAdaFloatVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaFloatVal::initializeStorageClassArray(SgAdaFloatValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaFloatVal::pools.begin(); block != SgAdaFloatVal::pools.end(); ++block) {
        SgAdaFloatVal* pointer{reinterpret_cast<SgAdaFloatVal*>(*block)};
        for (unsigned i = 0; i < SgAdaFloatVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 130716 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSignedCharVal that takes its corresponding StorageClass as parameter
 */
SgSignedCharVal::SgSignedCharVal(const SgSignedCharValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 130726 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSignedCharVal*
SgSignedCharVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSignedCharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSignedCharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSignedCharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSignedCharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedCharVal)};
        unsigned long positionInPool{localIndex % SgSignedCharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSignedCharVal::pool_size};
        returnPointer = &(((SgSignedCharVal*)(SgSignedCharVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSignedCharVal*
SgSignedCharVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSignedCharVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSignedCharVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSignedCharVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSignedCharVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSignedCharVal)};
        unsigned long positionInPool{localIndex % SgSignedCharVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSignedCharVal::pool_size};
        returnPointer = &(((SgSignedCharVal*)(SgSignedCharVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSignedCharVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSignedCharVal::pools.begin(); block != SgSignedCharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSignedCharVal*>(*block)};
        for (unsigned i = 0; i < SgSignedCharVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSignedCharVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSignedCharVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSignedCharVal* pointerOfLinkedList{nullptr};
    for (auto block = SgSignedCharVal::pools.begin(); block != SgSignedCharVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSignedCharVal*>(*block)};
        for (unsigned i = 0; i < SgSignedCharVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSignedCharVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSignedCharVal::clearMemoryPool() {
    SgSignedCharVal* tempPointer{nullptr};
    if (SgSignedCharVal::pools.empty() == false) {
        auto block{SgSignedCharVal::pools.begin()};
        SgSignedCharVal::next_node = reinterpret_cast<SgSignedCharVal*>(*block);
        for (; block != SgSignedCharVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSignedCharVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSignedCharVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSignedCharVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSignedCharVal::pool_size-1]);
        }
    }
}

void
SgSignedCharVal::deleteMemoryPool() {
    for (auto p: SgSignedCharVal::pools) {
        ROSE_FREE(p);
    }
    SgSignedCharVal::next_node = nullptr;
    SgSignedCharVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSignedCharVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedCharVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSignedCharVal)};
    for (size_t blockIndex = SgSignedCharVal::pools.size(); (blockIndex * SgSignedCharVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedCharVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSignedCharVal) = %" PRIuPTR " SgSignedCharVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSignedCharVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSignedCharVal),SgSignedCharVal::pool_size);
        }
#endif

        SgSignedCharVal* pointer{static_cast<SgSignedCharVal*>(ROSE_MALLOC(SgSignedCharVal::pool_size * sizeof(SgSignedCharVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSignedCharVal::pool_size * sizeof(SgSignedCharVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSignedCharVal::pool_size * sizeof(SgSignedCharVal));
#endif
        SgSignedCharVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSignedCharVal::pool_size * sizeof(SgSignedCharVal), V_SgSignedCharVal));

        if (SgSignedCharVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSignedCharVal* blkptr{reinterpret_cast<SgSignedCharVal*>(SgSignedCharVal::pools[blockIndex-1])};
                blkptr[SgSignedCharVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSignedCharVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSignedCharVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSignedCharVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSignedCharVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSignedCharVal::pool_size - 1};
    SgSignedCharVal* testPointer{reinterpret_cast<SgSignedCharVal*>(SgSignedCharVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSignedCharVal::pool_size * (SgSignedCharVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSignedCharValStroageClass
 * from its counterpart of SgSignedCharVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSignedCharVal::initializeStorageClassArray(SgSignedCharValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSignedCharVal::pools.begin(); block != SgSignedCharVal::pools.end(); ++block) {
        SgSignedCharVal* pointer{reinterpret_cast<SgSignedCharVal*>(*block)};
        for (unsigned i = 0; i < SgSignedCharVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 130945 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBFloat16Val that takes its corresponding StorageClass as parameter
 */
SgBFloat16Val::SgBFloat16Val(const SgBFloat16ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 130955 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBFloat16Val*
SgBFloat16Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBFloat16Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBFloat16Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBFloat16Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBFloat16Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBFloat16Val)};
        unsigned long positionInPool{localIndex % SgBFloat16Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBFloat16Val::pool_size};
        returnPointer = &(((SgBFloat16Val*)(SgBFloat16Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBFloat16Val*
SgBFloat16Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBFloat16Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBFloat16Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBFloat16Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBFloat16Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBFloat16Val)};
        unsigned long positionInPool{localIndex % SgBFloat16Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBFloat16Val::pool_size};
        returnPointer = &(((SgBFloat16Val*)(SgBFloat16Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBFloat16Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBFloat16Val::pools.begin(); block != SgBFloat16Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBFloat16Val*>(*block)};
        for (unsigned i = 0; i < SgBFloat16Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBFloat16Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBFloat16Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBFloat16Val* pointerOfLinkedList{nullptr};
    for (auto block = SgBFloat16Val::pools.begin(); block != SgBFloat16Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBFloat16Val*>(*block)};
        for (unsigned i = 0; i < SgBFloat16Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBFloat16Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBFloat16Val::clearMemoryPool() {
    SgBFloat16Val* tempPointer{nullptr};
    if (SgBFloat16Val::pools.empty() == false) {
        auto block{SgBFloat16Val::pools.begin()};
        SgBFloat16Val::next_node = reinterpret_cast<SgBFloat16Val*>(*block);
        for (; block != SgBFloat16Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBFloat16Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBFloat16Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBFloat16Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBFloat16Val::pool_size-1]);
        }
    }
}

void
SgBFloat16Val::deleteMemoryPool() {
    for (auto p: SgBFloat16Val::pools) {
        ROSE_FREE(p);
    }
    SgBFloat16Val::next_node = nullptr;
    SgBFloat16Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBFloat16Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBFloat16Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBFloat16Val)};
    for (size_t blockIndex = SgBFloat16Val::pools.size(); (blockIndex * SgBFloat16Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBFloat16Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBFloat16Val) = %" PRIuPTR " SgBFloat16Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBFloat16Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBFloat16Val),SgBFloat16Val::pool_size);
        }
#endif

        SgBFloat16Val* pointer{static_cast<SgBFloat16Val*>(ROSE_MALLOC(SgBFloat16Val::pool_size * sizeof(SgBFloat16Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBFloat16Val::pool_size * sizeof(SgBFloat16Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBFloat16Val::pool_size * sizeof(SgBFloat16Val));
#endif
        SgBFloat16Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBFloat16Val::pool_size * sizeof(SgBFloat16Val), V_SgBFloat16Val));

        if (SgBFloat16Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBFloat16Val* blkptr{reinterpret_cast<SgBFloat16Val*>(SgBFloat16Val::pools[blockIndex-1])};
                blkptr[SgBFloat16Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBFloat16Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBFloat16Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBFloat16Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBFloat16Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBFloat16Val::pool_size - 1};
    SgBFloat16Val* testPointer{reinterpret_cast<SgBFloat16Val*>(SgBFloat16Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBFloat16Val::pool_size * (SgBFloat16Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBFloat16ValStroageClass
 * from its counterpart of SgBFloat16Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBFloat16Val::initializeStorageClassArray(SgBFloat16ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBFloat16Val::pools.begin(); block != SgBFloat16Val::pools.end(); ++block) {
        SgBFloat16Val* pointer{reinterpret_cast<SgBFloat16Val*>(*block)};
        for (unsigned i = 0; i < SgBFloat16Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 131174 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFloat16Val that takes its corresponding StorageClass as parameter
 */
SgFloat16Val::SgFloat16Val(const SgFloat16ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 131184 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFloat16Val*
SgFloat16Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFloat16Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat16Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat16Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat16Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat16Val)};
        unsigned long positionInPool{localIndex % SgFloat16Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat16Val::pool_size};
        returnPointer = &(((SgFloat16Val*)(SgFloat16Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFloat16Val*
SgFloat16Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFloat16Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat16Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat16Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat16Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFloat16Val)};
        unsigned long positionInPool{localIndex % SgFloat16Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat16Val::pool_size};
        returnPointer = &(((SgFloat16Val*)(SgFloat16Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFloat16Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFloat16Val::pools.begin(); block != SgFloat16Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat16Val*>(*block)};
        for (unsigned i = 0; i < SgFloat16Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFloat16Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFloat16Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFloat16Val* pointerOfLinkedList{nullptr};
    for (auto block = SgFloat16Val::pools.begin(); block != SgFloat16Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat16Val*>(*block)};
        for (unsigned i = 0; i < SgFloat16Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFloat16Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFloat16Val::clearMemoryPool() {
    SgFloat16Val* tempPointer{nullptr};
    if (SgFloat16Val::pools.empty() == false) {
        auto block{SgFloat16Val::pools.begin()};
        SgFloat16Val::next_node = reinterpret_cast<SgFloat16Val*>(*block);
        for (; block != SgFloat16Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFloat16Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFloat16Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFloat16Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFloat16Val::pool_size-1]);
        }
    }
}

void
SgFloat16Val::deleteMemoryPool() {
    for (auto p: SgFloat16Val::pools) {
        ROSE_FREE(p);
    }
    SgFloat16Val::next_node = nullptr;
    SgFloat16Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFloat16Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat16Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat16Val)};
    for (size_t blockIndex = SgFloat16Val::pools.size(); (blockIndex * SgFloat16Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat16Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat16Val) = %" PRIuPTR " SgFloat16Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat16Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat16Val),SgFloat16Val::pool_size);
        }
#endif

        SgFloat16Val* pointer{static_cast<SgFloat16Val*>(ROSE_MALLOC(SgFloat16Val::pool_size * sizeof(SgFloat16Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFloat16Val::pool_size * sizeof(SgFloat16Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFloat16Val::pool_size * sizeof(SgFloat16Val));
#endif
        SgFloat16Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFloat16Val::pool_size * sizeof(SgFloat16Val), V_SgFloat16Val));

        if (SgFloat16Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFloat16Val* blkptr{reinterpret_cast<SgFloat16Val*>(SgFloat16Val::pools[blockIndex-1])};
                blkptr[SgFloat16Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFloat16Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFloat16Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFloat16Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFloat16Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFloat16Val::pool_size - 1};
    SgFloat16Val* testPointer{reinterpret_cast<SgFloat16Val*>(SgFloat16Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFloat16Val::pool_size * (SgFloat16Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFloat16ValStroageClass
 * from its counterpart of SgFloat16Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFloat16Val::initializeStorageClassArray(SgFloat16ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFloat16Val::pools.begin(); block != SgFloat16Val::pools.end(); ++block) {
        SgFloat16Val* pointer{reinterpret_cast<SgFloat16Val*>(*block)};
        for (unsigned i = 0; i < SgFloat16Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 131403 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFloat32Val that takes its corresponding StorageClass as parameter
 */
SgFloat32Val::SgFloat32Val(const SgFloat32ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 131413 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFloat32Val*
SgFloat32Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFloat32Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat32Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat32Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat32Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat32Val)};
        unsigned long positionInPool{localIndex % SgFloat32Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat32Val::pool_size};
        returnPointer = &(((SgFloat32Val*)(SgFloat32Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFloat32Val*
SgFloat32Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFloat32Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat32Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat32Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat32Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFloat32Val)};
        unsigned long positionInPool{localIndex % SgFloat32Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat32Val::pool_size};
        returnPointer = &(((SgFloat32Val*)(SgFloat32Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFloat32Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFloat32Val::pools.begin(); block != SgFloat32Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat32Val*>(*block)};
        for (unsigned i = 0; i < SgFloat32Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFloat32Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFloat32Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFloat32Val* pointerOfLinkedList{nullptr};
    for (auto block = SgFloat32Val::pools.begin(); block != SgFloat32Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat32Val*>(*block)};
        for (unsigned i = 0; i < SgFloat32Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFloat32Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFloat32Val::clearMemoryPool() {
    SgFloat32Val* tempPointer{nullptr};
    if (SgFloat32Val::pools.empty() == false) {
        auto block{SgFloat32Val::pools.begin()};
        SgFloat32Val::next_node = reinterpret_cast<SgFloat32Val*>(*block);
        for (; block != SgFloat32Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFloat32Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFloat32Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFloat32Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFloat32Val::pool_size-1]);
        }
    }
}

void
SgFloat32Val::deleteMemoryPool() {
    for (auto p: SgFloat32Val::pools) {
        ROSE_FREE(p);
    }
    SgFloat32Val::next_node = nullptr;
    SgFloat32Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFloat32Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat32Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat32Val)};
    for (size_t blockIndex = SgFloat32Val::pools.size(); (blockIndex * SgFloat32Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat32Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat32Val) = %" PRIuPTR " SgFloat32Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat32Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat32Val),SgFloat32Val::pool_size);
        }
#endif

        SgFloat32Val* pointer{static_cast<SgFloat32Val*>(ROSE_MALLOC(SgFloat32Val::pool_size * sizeof(SgFloat32Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFloat32Val::pool_size * sizeof(SgFloat32Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFloat32Val::pool_size * sizeof(SgFloat32Val));
#endif
        SgFloat32Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFloat32Val::pool_size * sizeof(SgFloat32Val), V_SgFloat32Val));

        if (SgFloat32Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFloat32Val* blkptr{reinterpret_cast<SgFloat32Val*>(SgFloat32Val::pools[blockIndex-1])};
                blkptr[SgFloat32Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFloat32Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFloat32Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFloat32Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFloat32Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFloat32Val::pool_size - 1};
    SgFloat32Val* testPointer{reinterpret_cast<SgFloat32Val*>(SgFloat32Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFloat32Val::pool_size * (SgFloat32Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFloat32ValStroageClass
 * from its counterpart of SgFloat32Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFloat32Val::initializeStorageClassArray(SgFloat32ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFloat32Val::pools.begin(); block != SgFloat32Val::pools.end(); ++block) {
        SgFloat32Val* pointer{reinterpret_cast<SgFloat32Val*>(*block)};
        for (unsigned i = 0; i < SgFloat32Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 131632 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFloat64Val that takes its corresponding StorageClass as parameter
 */
SgFloat64Val::SgFloat64Val(const SgFloat64ValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 131642 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFloat64Val*
SgFloat64Val::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFloat64Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat64Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat64Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFloat64Val)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat64Val)};
        unsigned long positionInPool{localIndex % SgFloat64Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat64Val::pool_size};
        returnPointer = &(((SgFloat64Val*)(SgFloat64Val::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFloat64Val*
SgFloat64Val::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFloat64Val* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat64Val)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat64Val + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFloat64Val)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFloat64Val)};
        unsigned long positionInPool{localIndex % SgFloat64Val::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFloat64Val::pool_size};
        returnPointer = &(((SgFloat64Val*)(SgFloat64Val::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFloat64Val::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFloat64Val::pools.begin(); block != SgFloat64Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat64Val*>(*block)};
        for (unsigned i = 0; i < SgFloat64Val::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFloat64Val*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFloat64Val::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFloat64Val* pointerOfLinkedList{nullptr};
    for (auto block = SgFloat64Val::pools.begin(); block != SgFloat64Val::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFloat64Val*>(*block)};
        for (unsigned i = 0; i < SgFloat64Val::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFloat64Val::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFloat64Val::clearMemoryPool() {
    SgFloat64Val* tempPointer{nullptr};
    if (SgFloat64Val::pools.empty() == false) {
        auto block{SgFloat64Val::pools.begin()};
        SgFloat64Val::next_node = reinterpret_cast<SgFloat64Val*>(*block);
        for (; block != SgFloat64Val::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFloat64Val*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFloat64Val::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFloat64Val::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFloat64Val::pool_size-1]);
        }
    }
}

void
SgFloat64Val::deleteMemoryPool() {
    for (auto p: SgFloat64Val::pools) {
        ROSE_FREE(p);
    }
    SgFloat64Val::next_node = nullptr;
    SgFloat64Val::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFloat64Val::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat64Val) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat64Val)};
    for (size_t blockIndex = SgFloat64Val::pools.size(); (blockIndex * SgFloat64Val::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat64Val) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat64Val) = %" PRIuPTR " SgFloat64Val::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFloat64Val),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFloat64Val),SgFloat64Val::pool_size);
        }
#endif

        SgFloat64Val* pointer{static_cast<SgFloat64Val*>(ROSE_MALLOC(SgFloat64Val::pool_size * sizeof(SgFloat64Val)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFloat64Val::pool_size * sizeof(SgFloat64Val));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFloat64Val::pool_size * sizeof(SgFloat64Val));
#endif
        SgFloat64Val::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFloat64Val::pool_size * sizeof(SgFloat64Val), V_SgFloat64Val));

        if (SgFloat64Val::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFloat64Val* blkptr{reinterpret_cast<SgFloat64Val*>(SgFloat64Val::pools[blockIndex-1])};
                blkptr[SgFloat64Val::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFloat64Val::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFloat64Val::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFloat64Val::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFloat64Val::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFloat64Val::pool_size - 1};
    SgFloat64Val* testPointer{reinterpret_cast<SgFloat64Val*>(SgFloat64Val::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFloat64Val::pool_size * (SgFloat64Val::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFloat64ValStroageClass
 * from its counterpart of SgFloat64Val. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFloat64Val::initializeStorageClassArray(SgFloat64ValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFloat64Val::pools.begin(); block != SgFloat64Val::pools.end(); ++block) {
        SgFloat64Val* pointer{reinterpret_cast<SgFloat64Val*>(*block)};
        for (unsigned i = 0; i < SgFloat64Val::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 131861 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialBitVal that takes its corresponding StorageClass as parameter
 */
SgJovialBitVal::SgJovialBitVal(const SgJovialBitValStorageClass& storageSource)  : SgValueExp (storageSource) {

/* #line 131871 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_valueString = storageSource.storageOf_valueString.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialBitVal*
SgJovialBitVal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialBitVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialBitVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialBitVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialBitVal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitVal)};
        unsigned long positionInPool{localIndex % SgJovialBitVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialBitVal::pool_size};
        returnPointer = &(((SgJovialBitVal*)(SgJovialBitVal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialBitVal*
SgJovialBitVal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialBitVal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialBitVal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialBitVal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialBitVal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialBitVal)};
        unsigned long positionInPool{localIndex % SgJovialBitVal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialBitVal::pool_size};
        returnPointer = &(((SgJovialBitVal*)(SgJovialBitVal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialBitVal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialBitVal::pools.begin(); block != SgJovialBitVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialBitVal*>(*block)};
        for (unsigned i = 0; i < SgJovialBitVal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialBitVal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialBitVal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialBitVal* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialBitVal::pools.begin(); block != SgJovialBitVal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialBitVal*>(*block)};
        for (unsigned i = 0; i < SgJovialBitVal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialBitVal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialBitVal::clearMemoryPool() {
    SgJovialBitVal* tempPointer{nullptr};
    if (SgJovialBitVal::pools.empty() == false) {
        auto block{SgJovialBitVal::pools.begin()};
        SgJovialBitVal::next_node = reinterpret_cast<SgJovialBitVal*>(*block);
        for (; block != SgJovialBitVal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialBitVal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialBitVal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialBitVal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialBitVal::pool_size-1]);
        }
    }
}

void
SgJovialBitVal::deleteMemoryPool() {
    for (auto p: SgJovialBitVal::pools) {
        ROSE_FREE(p);
    }
    SgJovialBitVal::next_node = nullptr;
    SgJovialBitVal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialBitVal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitVal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialBitVal)};
    for (size_t blockIndex = SgJovialBitVal::pools.size(); (blockIndex * SgJovialBitVal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitVal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialBitVal) = %" PRIuPTR " SgJovialBitVal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialBitVal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialBitVal),SgJovialBitVal::pool_size);
        }
#endif

        SgJovialBitVal* pointer{static_cast<SgJovialBitVal*>(ROSE_MALLOC(SgJovialBitVal::pool_size * sizeof(SgJovialBitVal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialBitVal::pool_size * sizeof(SgJovialBitVal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialBitVal::pool_size * sizeof(SgJovialBitVal));
#endif
        SgJovialBitVal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialBitVal::pool_size * sizeof(SgJovialBitVal), V_SgJovialBitVal));

        if (SgJovialBitVal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialBitVal* blkptr{reinterpret_cast<SgJovialBitVal*>(SgJovialBitVal::pools[blockIndex-1])};
                blkptr[SgJovialBitVal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialBitVal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialBitVal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialBitVal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialBitVal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialBitVal::pool_size - 1};
    SgJovialBitVal* testPointer{reinterpret_cast<SgJovialBitVal*>(SgJovialBitVal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialBitVal::pool_size * (SgJovialBitVal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialBitValStroageClass
 * from its counterpart of SgJovialBitVal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialBitVal::initializeStorageClassArray(SgJovialBitValStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialBitVal::pools.begin(); block != SgJovialBitVal::pools.end(); ++block) {
        SgJovialBitVal* pointer{reinterpret_cast<SgJovialBitVal*>(*block)};
        for (unsigned i = 0; i < SgJovialBitVal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 132089 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCallExpression that takes its corresponding StorageClass as parameter
 */
SgCallExpression::SgCallExpression(const SgCallExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 132099 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_function = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_function));
     p_args = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_args));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCallExpression*
SgCallExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCallExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCallExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCallExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCallExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCallExpression)};
        unsigned long positionInPool{localIndex % SgCallExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCallExpression::pool_size};
        returnPointer = &(((SgCallExpression*)(SgCallExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCallExpression*
SgCallExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCallExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCallExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCallExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCallExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCallExpression)};
        unsigned long positionInPool{localIndex % SgCallExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCallExpression::pool_size};
        returnPointer = &(((SgCallExpression*)(SgCallExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCallExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCallExpression::pools.begin(); block != SgCallExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCallExpression*>(*block)};
        for (unsigned i = 0; i < SgCallExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCallExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCallExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCallExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgCallExpression::pools.begin(); block != SgCallExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCallExpression*>(*block)};
        for (unsigned i = 0; i < SgCallExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCallExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCallExpression::clearMemoryPool() {
    SgCallExpression* tempPointer{nullptr};
    if (SgCallExpression::pools.empty() == false) {
        auto block{SgCallExpression::pools.begin()};
        SgCallExpression::next_node = reinterpret_cast<SgCallExpression*>(*block);
        for (; block != SgCallExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCallExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCallExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCallExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCallExpression::pool_size-1]);
        }
    }
}

void
SgCallExpression::deleteMemoryPool() {
    for (auto p: SgCallExpression::pools) {
        ROSE_FREE(p);
    }
    SgCallExpression::next_node = nullptr;
    SgCallExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCallExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCallExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCallExpression)};
    for (size_t blockIndex = SgCallExpression::pools.size(); (blockIndex * SgCallExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCallExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCallExpression) = %" PRIuPTR " SgCallExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCallExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCallExpression),SgCallExpression::pool_size);
        }
#endif

        SgCallExpression* pointer{static_cast<SgCallExpression*>(ROSE_MALLOC(SgCallExpression::pool_size * sizeof(SgCallExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCallExpression::pool_size * sizeof(SgCallExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCallExpression::pool_size * sizeof(SgCallExpression));
#endif
        SgCallExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCallExpression::pool_size * sizeof(SgCallExpression), V_SgCallExpression));

        if (SgCallExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCallExpression* blkptr{reinterpret_cast<SgCallExpression*>(SgCallExpression::pools[blockIndex-1])};
                blkptr[SgCallExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCallExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCallExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCallExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCallExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCallExpression::pool_size - 1};
    SgCallExpression* testPointer{reinterpret_cast<SgCallExpression*>(SgCallExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCallExpression::pool_size * (SgCallExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCallExpressionStroageClass
 * from its counterpart of SgCallExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCallExpression::initializeStorageClassArray(SgCallExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCallExpression::pools.begin(); block != SgCallExpression::pools.end(); ++block) {
        SgCallExpression* pointer{reinterpret_cast<SgCallExpression*>(*block)};
        for (unsigned i = 0; i < SgCallExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 132319 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionCallExp that takes its corresponding StorageClass as parameter
 */
SgFunctionCallExp::SgFunctionCallExp(const SgFunctionCallExpStorageClass& storageSource)  : SgCallExpression (storageSource) {

/* #line 132329 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_uses_operator_syntax = storageSource.storageOf_uses_operator_syntax;
    p_usesObjectCallSyntax = storageSource.storageOf_usesObjectCallSyntax;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionCallExp*
SgFunctionCallExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionCallExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionCallExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionCallExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionCallExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionCallExp)};
        unsigned long positionInPool{localIndex % SgFunctionCallExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionCallExp::pool_size};
        returnPointer = &(((SgFunctionCallExp*)(SgFunctionCallExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionCallExp*
SgFunctionCallExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionCallExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionCallExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionCallExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionCallExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionCallExp)};
        unsigned long positionInPool{localIndex % SgFunctionCallExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionCallExp::pool_size};
        returnPointer = &(((SgFunctionCallExp*)(SgFunctionCallExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionCallExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionCallExp::pools.begin(); block != SgFunctionCallExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionCallExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionCallExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionCallExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionCallExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionCallExp* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionCallExp::pools.begin(); block != SgFunctionCallExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionCallExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionCallExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionCallExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionCallExp::clearMemoryPool() {
    SgFunctionCallExp* tempPointer{nullptr};
    if (SgFunctionCallExp::pools.empty() == false) {
        auto block{SgFunctionCallExp::pools.begin()};
        SgFunctionCallExp::next_node = reinterpret_cast<SgFunctionCallExp*>(*block);
        for (; block != SgFunctionCallExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionCallExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionCallExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionCallExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionCallExp::pool_size-1]);
        }
    }
}

void
SgFunctionCallExp::deleteMemoryPool() {
    for (auto p: SgFunctionCallExp::pools) {
        ROSE_FREE(p);
    }
    SgFunctionCallExp::next_node = nullptr;
    SgFunctionCallExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionCallExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionCallExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionCallExp)};
    for (size_t blockIndex = SgFunctionCallExp::pools.size(); (blockIndex * SgFunctionCallExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionCallExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionCallExp) = %" PRIuPTR " SgFunctionCallExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionCallExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionCallExp),SgFunctionCallExp::pool_size);
        }
#endif

        SgFunctionCallExp* pointer{static_cast<SgFunctionCallExp*>(ROSE_MALLOC(SgFunctionCallExp::pool_size * sizeof(SgFunctionCallExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionCallExp::pool_size * sizeof(SgFunctionCallExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionCallExp::pool_size * sizeof(SgFunctionCallExp));
#endif
        SgFunctionCallExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionCallExp::pool_size * sizeof(SgFunctionCallExp), V_SgFunctionCallExp));

        if (SgFunctionCallExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionCallExp* blkptr{reinterpret_cast<SgFunctionCallExp*>(SgFunctionCallExp::pools[blockIndex-1])};
                blkptr[SgFunctionCallExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionCallExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionCallExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionCallExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionCallExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionCallExp::pool_size - 1};
    SgFunctionCallExp* testPointer{reinterpret_cast<SgFunctionCallExp*>(SgFunctionCallExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionCallExp::pool_size * (SgFunctionCallExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionCallExpStroageClass
 * from its counterpart of SgFunctionCallExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionCallExp::initializeStorageClassArray(SgFunctionCallExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionCallExp::pools.begin(); block != SgFunctionCallExp::pools.end(); ++block) {
        SgFunctionCallExp* pointer{reinterpret_cast<SgFunctionCallExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionCallExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 132548 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCudaKernelCallExp that takes its corresponding StorageClass as parameter
 */
SgCudaKernelCallExp::SgCudaKernelCallExp(const SgCudaKernelCallExpStorageClass& storageSource)  : SgFunctionCallExp (storageSource) {

/* #line 132558 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_exec_config = (SgCudaKernelExecConfig*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_exec_config));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCudaKernelCallExp*
SgCudaKernelCallExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCudaKernelCallExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCudaKernelCallExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCudaKernelCallExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCudaKernelCallExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelCallExp)};
        unsigned long positionInPool{localIndex % SgCudaKernelCallExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCudaKernelCallExp::pool_size};
        returnPointer = &(((SgCudaKernelCallExp*)(SgCudaKernelCallExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCudaKernelCallExp*
SgCudaKernelCallExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCudaKernelCallExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCudaKernelCallExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCudaKernelCallExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCudaKernelCallExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCudaKernelCallExp)};
        unsigned long positionInPool{localIndex % SgCudaKernelCallExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCudaKernelCallExp::pool_size};
        returnPointer = &(((SgCudaKernelCallExp*)(SgCudaKernelCallExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCudaKernelCallExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCudaKernelCallExp::pools.begin(); block != SgCudaKernelCallExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCudaKernelCallExp*>(*block)};
        for (unsigned i = 0; i < SgCudaKernelCallExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCudaKernelCallExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCudaKernelCallExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCudaKernelCallExp* pointerOfLinkedList{nullptr};
    for (auto block = SgCudaKernelCallExp::pools.begin(); block != SgCudaKernelCallExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCudaKernelCallExp*>(*block)};
        for (unsigned i = 0; i < SgCudaKernelCallExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCudaKernelCallExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCudaKernelCallExp::clearMemoryPool() {
    SgCudaKernelCallExp* tempPointer{nullptr};
    if (SgCudaKernelCallExp::pools.empty() == false) {
        auto block{SgCudaKernelCallExp::pools.begin()};
        SgCudaKernelCallExp::next_node = reinterpret_cast<SgCudaKernelCallExp*>(*block);
        for (; block != SgCudaKernelCallExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCudaKernelCallExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCudaKernelCallExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCudaKernelCallExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCudaKernelCallExp::pool_size-1]);
        }
    }
}

void
SgCudaKernelCallExp::deleteMemoryPool() {
    for (auto p: SgCudaKernelCallExp::pools) {
        ROSE_FREE(p);
    }
    SgCudaKernelCallExp::next_node = nullptr;
    SgCudaKernelCallExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCudaKernelCallExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelCallExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCudaKernelCallExp)};
    for (size_t blockIndex = SgCudaKernelCallExp::pools.size(); (blockIndex * SgCudaKernelCallExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelCallExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCudaKernelCallExp) = %" PRIuPTR " SgCudaKernelCallExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelCallExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCudaKernelCallExp),SgCudaKernelCallExp::pool_size);
        }
#endif

        SgCudaKernelCallExp* pointer{static_cast<SgCudaKernelCallExp*>(ROSE_MALLOC(SgCudaKernelCallExp::pool_size * sizeof(SgCudaKernelCallExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCudaKernelCallExp::pool_size * sizeof(SgCudaKernelCallExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCudaKernelCallExp::pool_size * sizeof(SgCudaKernelCallExp));
#endif
        SgCudaKernelCallExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCudaKernelCallExp::pool_size * sizeof(SgCudaKernelCallExp), V_SgCudaKernelCallExp));

        if (SgCudaKernelCallExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCudaKernelCallExp* blkptr{reinterpret_cast<SgCudaKernelCallExp*>(SgCudaKernelCallExp::pools[blockIndex-1])};
                blkptr[SgCudaKernelCallExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCudaKernelCallExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCudaKernelCallExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCudaKernelCallExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCudaKernelCallExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCudaKernelCallExp::pool_size - 1};
    SgCudaKernelCallExp* testPointer{reinterpret_cast<SgCudaKernelCallExp*>(SgCudaKernelCallExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCudaKernelCallExp::pool_size * (SgCudaKernelCallExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCudaKernelCallExpStroageClass
 * from its counterpart of SgCudaKernelCallExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCudaKernelCallExp::initializeStorageClassArray(SgCudaKernelCallExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCudaKernelCallExp::pools.begin(); block != SgCudaKernelCallExp::pools.end(); ++block) {
        SgCudaKernelCallExp* pointer{reinterpret_cast<SgCudaKernelCallExp*>(*block)};
        for (unsigned i = 0; i < SgCudaKernelCallExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 132776 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcLocalsizeofExpression that takes its corresponding StorageClass as parameter
 */
SgUpcLocalsizeofExpression::SgUpcLocalsizeofExpression(const SgUpcLocalsizeofExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 132786 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_operand_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_type));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcLocalsizeofExpression*
SgUpcLocalsizeofExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcLocalsizeofExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcLocalsizeofExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcLocalsizeofExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcLocalsizeofExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcLocalsizeofExpression)};
        unsigned long positionInPool{localIndex % SgUpcLocalsizeofExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcLocalsizeofExpression::pool_size};
        returnPointer = &(((SgUpcLocalsizeofExpression*)(SgUpcLocalsizeofExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcLocalsizeofExpression*
SgUpcLocalsizeofExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcLocalsizeofExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcLocalsizeofExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcLocalsizeofExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcLocalsizeofExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcLocalsizeofExpression)};
        unsigned long positionInPool{localIndex % SgUpcLocalsizeofExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcLocalsizeofExpression::pool_size};
        returnPointer = &(((SgUpcLocalsizeofExpression*)(SgUpcLocalsizeofExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcLocalsizeofExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcLocalsizeofExpression::pools.begin(); block != SgUpcLocalsizeofExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcLocalsizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcLocalsizeofExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcLocalsizeofExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcLocalsizeofExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcLocalsizeofExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcLocalsizeofExpression::pools.begin(); block != SgUpcLocalsizeofExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcLocalsizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcLocalsizeofExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcLocalsizeofExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcLocalsizeofExpression::clearMemoryPool() {
    SgUpcLocalsizeofExpression* tempPointer{nullptr};
    if (SgUpcLocalsizeofExpression::pools.empty() == false) {
        auto block{SgUpcLocalsizeofExpression::pools.begin()};
        SgUpcLocalsizeofExpression::next_node = reinterpret_cast<SgUpcLocalsizeofExpression*>(*block);
        for (; block != SgUpcLocalsizeofExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcLocalsizeofExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcLocalsizeofExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcLocalsizeofExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcLocalsizeofExpression::pool_size-1]);
        }
    }
}

void
SgUpcLocalsizeofExpression::deleteMemoryPool() {
    for (auto p: SgUpcLocalsizeofExpression::pools) {
        ROSE_FREE(p);
    }
    SgUpcLocalsizeofExpression::next_node = nullptr;
    SgUpcLocalsizeofExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcLocalsizeofExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcLocalsizeofExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcLocalsizeofExpression)};
    for (size_t blockIndex = SgUpcLocalsizeofExpression::pools.size(); (blockIndex * SgUpcLocalsizeofExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcLocalsizeofExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcLocalsizeofExpression) = %" PRIuPTR " SgUpcLocalsizeofExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcLocalsizeofExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcLocalsizeofExpression),SgUpcLocalsizeofExpression::pool_size);
        }
#endif

        SgUpcLocalsizeofExpression* pointer{static_cast<SgUpcLocalsizeofExpression*>(ROSE_MALLOC(SgUpcLocalsizeofExpression::pool_size * sizeof(SgUpcLocalsizeofExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcLocalsizeofExpression::pool_size * sizeof(SgUpcLocalsizeofExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcLocalsizeofExpression::pool_size * sizeof(SgUpcLocalsizeofExpression));
#endif
        SgUpcLocalsizeofExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcLocalsizeofExpression::pool_size * sizeof(SgUpcLocalsizeofExpression), V_SgUpcLocalsizeofExpression));

        if (SgUpcLocalsizeofExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcLocalsizeofExpression* blkptr{reinterpret_cast<SgUpcLocalsizeofExpression*>(SgUpcLocalsizeofExpression::pools[blockIndex-1])};
                blkptr[SgUpcLocalsizeofExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcLocalsizeofExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcLocalsizeofExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcLocalsizeofExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcLocalsizeofExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcLocalsizeofExpression::pool_size - 1};
    SgUpcLocalsizeofExpression* testPointer{reinterpret_cast<SgUpcLocalsizeofExpression*>(SgUpcLocalsizeofExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcLocalsizeofExpression::pool_size * (SgUpcLocalsizeofExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcLocalsizeofExpressionStroageClass
 * from its counterpart of SgUpcLocalsizeofExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcLocalsizeofExpression::initializeStorageClassArray(SgUpcLocalsizeofExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcLocalsizeofExpression::pools.begin(); block != SgUpcLocalsizeofExpression::pools.end(); ++block) {
        SgUpcLocalsizeofExpression* pointer{reinterpret_cast<SgUpcLocalsizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcLocalsizeofExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 133006 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcBlocksizeofExpression that takes its corresponding StorageClass as parameter
 */
SgUpcBlocksizeofExpression::SgUpcBlocksizeofExpression(const SgUpcBlocksizeofExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 133016 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_operand_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_type));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcBlocksizeofExpression*
SgUpcBlocksizeofExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcBlocksizeofExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcBlocksizeofExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcBlocksizeofExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcBlocksizeofExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBlocksizeofExpression)};
        unsigned long positionInPool{localIndex % SgUpcBlocksizeofExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcBlocksizeofExpression::pool_size};
        returnPointer = &(((SgUpcBlocksizeofExpression*)(SgUpcBlocksizeofExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcBlocksizeofExpression*
SgUpcBlocksizeofExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcBlocksizeofExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcBlocksizeofExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcBlocksizeofExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcBlocksizeofExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcBlocksizeofExpression)};
        unsigned long positionInPool{localIndex % SgUpcBlocksizeofExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcBlocksizeofExpression::pool_size};
        returnPointer = &(((SgUpcBlocksizeofExpression*)(SgUpcBlocksizeofExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcBlocksizeofExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcBlocksizeofExpression::pools.begin(); block != SgUpcBlocksizeofExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcBlocksizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcBlocksizeofExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcBlocksizeofExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcBlocksizeofExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcBlocksizeofExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcBlocksizeofExpression::pools.begin(); block != SgUpcBlocksizeofExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcBlocksizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcBlocksizeofExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcBlocksizeofExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcBlocksizeofExpression::clearMemoryPool() {
    SgUpcBlocksizeofExpression* tempPointer{nullptr};
    if (SgUpcBlocksizeofExpression::pools.empty() == false) {
        auto block{SgUpcBlocksizeofExpression::pools.begin()};
        SgUpcBlocksizeofExpression::next_node = reinterpret_cast<SgUpcBlocksizeofExpression*>(*block);
        for (; block != SgUpcBlocksizeofExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcBlocksizeofExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcBlocksizeofExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcBlocksizeofExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcBlocksizeofExpression::pool_size-1]);
        }
    }
}

void
SgUpcBlocksizeofExpression::deleteMemoryPool() {
    for (auto p: SgUpcBlocksizeofExpression::pools) {
        ROSE_FREE(p);
    }
    SgUpcBlocksizeofExpression::next_node = nullptr;
    SgUpcBlocksizeofExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcBlocksizeofExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBlocksizeofExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcBlocksizeofExpression)};
    for (size_t blockIndex = SgUpcBlocksizeofExpression::pools.size(); (blockIndex * SgUpcBlocksizeofExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBlocksizeofExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcBlocksizeofExpression) = %" PRIuPTR " SgUpcBlocksizeofExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcBlocksizeofExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcBlocksizeofExpression),SgUpcBlocksizeofExpression::pool_size);
        }
#endif

        SgUpcBlocksizeofExpression* pointer{static_cast<SgUpcBlocksizeofExpression*>(ROSE_MALLOC(SgUpcBlocksizeofExpression::pool_size * sizeof(SgUpcBlocksizeofExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcBlocksizeofExpression::pool_size * sizeof(SgUpcBlocksizeofExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcBlocksizeofExpression::pool_size * sizeof(SgUpcBlocksizeofExpression));
#endif
        SgUpcBlocksizeofExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcBlocksizeofExpression::pool_size * sizeof(SgUpcBlocksizeofExpression), V_SgUpcBlocksizeofExpression));

        if (SgUpcBlocksizeofExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcBlocksizeofExpression* blkptr{reinterpret_cast<SgUpcBlocksizeofExpression*>(SgUpcBlocksizeofExpression::pools[blockIndex-1])};
                blkptr[SgUpcBlocksizeofExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcBlocksizeofExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcBlocksizeofExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcBlocksizeofExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcBlocksizeofExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcBlocksizeofExpression::pool_size - 1};
    SgUpcBlocksizeofExpression* testPointer{reinterpret_cast<SgUpcBlocksizeofExpression*>(SgUpcBlocksizeofExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcBlocksizeofExpression::pool_size * (SgUpcBlocksizeofExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcBlocksizeofExpressionStroageClass
 * from its counterpart of SgUpcBlocksizeofExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcBlocksizeofExpression::initializeStorageClassArray(SgUpcBlocksizeofExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcBlocksizeofExpression::pools.begin(); block != SgUpcBlocksizeofExpression::pools.end(); ++block) {
        SgUpcBlocksizeofExpression* pointer{reinterpret_cast<SgUpcBlocksizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcBlocksizeofExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 133236 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUpcElemsizeofExpression that takes its corresponding StorageClass as parameter
 */
SgUpcElemsizeofExpression::SgUpcElemsizeofExpression(const SgUpcElemsizeofExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 133246 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_operand_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_type));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUpcElemsizeofExpression*
SgUpcElemsizeofExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUpcElemsizeofExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcElemsizeofExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcElemsizeofExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUpcElemsizeofExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcElemsizeofExpression)};
        unsigned long positionInPool{localIndex % SgUpcElemsizeofExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcElemsizeofExpression::pool_size};
        returnPointer = &(((SgUpcElemsizeofExpression*)(SgUpcElemsizeofExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUpcElemsizeofExpression*
SgUpcElemsizeofExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUpcElemsizeofExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcElemsizeofExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcElemsizeofExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUpcElemsizeofExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUpcElemsizeofExpression)};
        unsigned long positionInPool{localIndex % SgUpcElemsizeofExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUpcElemsizeofExpression::pool_size};
        returnPointer = &(((SgUpcElemsizeofExpression*)(SgUpcElemsizeofExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUpcElemsizeofExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUpcElemsizeofExpression::pools.begin(); block != SgUpcElemsizeofExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcElemsizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcElemsizeofExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUpcElemsizeofExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUpcElemsizeofExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUpcElemsizeofExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgUpcElemsizeofExpression::pools.begin(); block != SgUpcElemsizeofExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUpcElemsizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcElemsizeofExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUpcElemsizeofExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUpcElemsizeofExpression::clearMemoryPool() {
    SgUpcElemsizeofExpression* tempPointer{nullptr};
    if (SgUpcElemsizeofExpression::pools.empty() == false) {
        auto block{SgUpcElemsizeofExpression::pools.begin()};
        SgUpcElemsizeofExpression::next_node = reinterpret_cast<SgUpcElemsizeofExpression*>(*block);
        for (; block != SgUpcElemsizeofExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUpcElemsizeofExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUpcElemsizeofExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUpcElemsizeofExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUpcElemsizeofExpression::pool_size-1]);
        }
    }
}

void
SgUpcElemsizeofExpression::deleteMemoryPool() {
    for (auto p: SgUpcElemsizeofExpression::pools) {
        ROSE_FREE(p);
    }
    SgUpcElemsizeofExpression::next_node = nullptr;
    SgUpcElemsizeofExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUpcElemsizeofExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcElemsizeofExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcElemsizeofExpression)};
    for (size_t blockIndex = SgUpcElemsizeofExpression::pools.size(); (blockIndex * SgUpcElemsizeofExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcElemsizeofExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcElemsizeofExpression) = %" PRIuPTR " SgUpcElemsizeofExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUpcElemsizeofExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUpcElemsizeofExpression),SgUpcElemsizeofExpression::pool_size);
        }
#endif

        SgUpcElemsizeofExpression* pointer{static_cast<SgUpcElemsizeofExpression*>(ROSE_MALLOC(SgUpcElemsizeofExpression::pool_size * sizeof(SgUpcElemsizeofExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUpcElemsizeofExpression::pool_size * sizeof(SgUpcElemsizeofExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUpcElemsizeofExpression::pool_size * sizeof(SgUpcElemsizeofExpression));
#endif
        SgUpcElemsizeofExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUpcElemsizeofExpression::pool_size * sizeof(SgUpcElemsizeofExpression), V_SgUpcElemsizeofExpression));

        if (SgUpcElemsizeofExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUpcElemsizeofExpression* blkptr{reinterpret_cast<SgUpcElemsizeofExpression*>(SgUpcElemsizeofExpression::pools[blockIndex-1])};
                blkptr[SgUpcElemsizeofExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUpcElemsizeofExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUpcElemsizeofExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUpcElemsizeofExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUpcElemsizeofExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUpcElemsizeofExpression::pool_size - 1};
    SgUpcElemsizeofExpression* testPointer{reinterpret_cast<SgUpcElemsizeofExpression*>(SgUpcElemsizeofExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUpcElemsizeofExpression::pool_size * (SgUpcElemsizeofExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUpcElemsizeofExpressionStroageClass
 * from its counterpart of SgUpcElemsizeofExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUpcElemsizeofExpression::initializeStorageClassArray(SgUpcElemsizeofExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUpcElemsizeofExpression::pools.begin(); block != SgUpcElemsizeofExpression::pools.end(); ++block) {
        SgUpcElemsizeofExpression* pointer{reinterpret_cast<SgUpcElemsizeofExpression*>(*block)};
        for (unsigned i = 0; i < SgUpcElemsizeofExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 133466 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaInstanceOfOp that takes its corresponding StorageClass as parameter
 */
SgJavaInstanceOfOp::SgJavaInstanceOfOp(const SgJavaInstanceOfOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 133476 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));
     p_operand_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_type));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaInstanceOfOp*
SgJavaInstanceOfOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaInstanceOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaInstanceOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaInstanceOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaInstanceOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaInstanceOfOp)};
        unsigned long positionInPool{localIndex % SgJavaInstanceOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaInstanceOfOp::pool_size};
        returnPointer = &(((SgJavaInstanceOfOp*)(SgJavaInstanceOfOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaInstanceOfOp*
SgJavaInstanceOfOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaInstanceOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaInstanceOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaInstanceOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaInstanceOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaInstanceOfOp)};
        unsigned long positionInPool{localIndex % SgJavaInstanceOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaInstanceOfOp::pool_size};
        returnPointer = &(((SgJavaInstanceOfOp*)(SgJavaInstanceOfOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaInstanceOfOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaInstanceOfOp::pools.begin(); block != SgJavaInstanceOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaInstanceOfOp*>(*block)};
        for (unsigned i = 0; i < SgJavaInstanceOfOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaInstanceOfOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaInstanceOfOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaInstanceOfOp* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaInstanceOfOp::pools.begin(); block != SgJavaInstanceOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaInstanceOfOp*>(*block)};
        for (unsigned i = 0; i < SgJavaInstanceOfOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaInstanceOfOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaInstanceOfOp::clearMemoryPool() {
    SgJavaInstanceOfOp* tempPointer{nullptr};
    if (SgJavaInstanceOfOp::pools.empty() == false) {
        auto block{SgJavaInstanceOfOp::pools.begin()};
        SgJavaInstanceOfOp::next_node = reinterpret_cast<SgJavaInstanceOfOp*>(*block);
        for (; block != SgJavaInstanceOfOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaInstanceOfOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaInstanceOfOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaInstanceOfOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaInstanceOfOp::pool_size-1]);
        }
    }
}

void
SgJavaInstanceOfOp::deleteMemoryPool() {
    for (auto p: SgJavaInstanceOfOp::pools) {
        ROSE_FREE(p);
    }
    SgJavaInstanceOfOp::next_node = nullptr;
    SgJavaInstanceOfOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaInstanceOfOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaInstanceOfOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaInstanceOfOp)};
    for (size_t blockIndex = SgJavaInstanceOfOp::pools.size(); (blockIndex * SgJavaInstanceOfOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaInstanceOfOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaInstanceOfOp) = %" PRIuPTR " SgJavaInstanceOfOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaInstanceOfOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaInstanceOfOp),SgJavaInstanceOfOp::pool_size);
        }
#endif

        SgJavaInstanceOfOp* pointer{static_cast<SgJavaInstanceOfOp*>(ROSE_MALLOC(SgJavaInstanceOfOp::pool_size * sizeof(SgJavaInstanceOfOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaInstanceOfOp::pool_size * sizeof(SgJavaInstanceOfOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaInstanceOfOp::pool_size * sizeof(SgJavaInstanceOfOp));
#endif
        SgJavaInstanceOfOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaInstanceOfOp::pool_size * sizeof(SgJavaInstanceOfOp), V_SgJavaInstanceOfOp));

        if (SgJavaInstanceOfOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaInstanceOfOp* blkptr{reinterpret_cast<SgJavaInstanceOfOp*>(SgJavaInstanceOfOp::pools[blockIndex-1])};
                blkptr[SgJavaInstanceOfOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaInstanceOfOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaInstanceOfOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaInstanceOfOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaInstanceOfOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaInstanceOfOp::pool_size - 1};
    SgJavaInstanceOfOp* testPointer{reinterpret_cast<SgJavaInstanceOfOp*>(SgJavaInstanceOfOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaInstanceOfOp::pool_size * (SgJavaInstanceOfOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaInstanceOfOpStroageClass
 * from its counterpart of SgJavaInstanceOfOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaInstanceOfOp::initializeStorageClassArray(SgJavaInstanceOfOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaInstanceOfOp::pools.begin(); block != SgJavaInstanceOfOp::pools.end(); ++block) {
        SgJavaInstanceOfOp* pointer{reinterpret_cast<SgJavaInstanceOfOp*>(*block)};
        for (unsigned i = 0; i < SgJavaInstanceOfOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 133696 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSuperExp that takes its corresponding StorageClass as parameter
 */
SgSuperExp::SgSuperExp(const SgSuperExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 133706 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_class_symbol = (SgClassSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_class_symbol));
    p_pobj_super = storageSource.storageOf_pobj_super;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSuperExp*
SgSuperExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSuperExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSuperExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSuperExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSuperExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSuperExp)};
        unsigned long positionInPool{localIndex % SgSuperExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSuperExp::pool_size};
        returnPointer = &(((SgSuperExp*)(SgSuperExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSuperExp*
SgSuperExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSuperExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSuperExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSuperExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSuperExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSuperExp)};
        unsigned long positionInPool{localIndex % SgSuperExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSuperExp::pool_size};
        returnPointer = &(((SgSuperExp*)(SgSuperExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSuperExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSuperExp::pools.begin(); block != SgSuperExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSuperExp*>(*block)};
        for (unsigned i = 0; i < SgSuperExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSuperExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSuperExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSuperExp* pointerOfLinkedList{nullptr};
    for (auto block = SgSuperExp::pools.begin(); block != SgSuperExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSuperExp*>(*block)};
        for (unsigned i = 0; i < SgSuperExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSuperExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSuperExp::clearMemoryPool() {
    SgSuperExp* tempPointer{nullptr};
    if (SgSuperExp::pools.empty() == false) {
        auto block{SgSuperExp::pools.begin()};
        SgSuperExp::next_node = reinterpret_cast<SgSuperExp*>(*block);
        for (; block != SgSuperExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSuperExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSuperExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSuperExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSuperExp::pool_size-1]);
        }
    }
}

void
SgSuperExp::deleteMemoryPool() {
    for (auto p: SgSuperExp::pools) {
        ROSE_FREE(p);
    }
    SgSuperExp::next_node = nullptr;
    SgSuperExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSuperExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSuperExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSuperExp)};
    for (size_t blockIndex = SgSuperExp::pools.size(); (blockIndex * SgSuperExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSuperExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSuperExp) = %" PRIuPTR " SgSuperExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSuperExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSuperExp),SgSuperExp::pool_size);
        }
#endif

        SgSuperExp* pointer{static_cast<SgSuperExp*>(ROSE_MALLOC(SgSuperExp::pool_size * sizeof(SgSuperExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSuperExp::pool_size * sizeof(SgSuperExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSuperExp::pool_size * sizeof(SgSuperExp));
#endif
        SgSuperExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSuperExp::pool_size * sizeof(SgSuperExp), V_SgSuperExp));

        if (SgSuperExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSuperExp* blkptr{reinterpret_cast<SgSuperExp*>(SgSuperExp::pools[blockIndex-1])};
                blkptr[SgSuperExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSuperExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSuperExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSuperExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSuperExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSuperExp::pool_size - 1};
    SgSuperExp* testPointer{reinterpret_cast<SgSuperExp*>(SgSuperExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSuperExp::pool_size * (SgSuperExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSuperExpStroageClass
 * from its counterpart of SgSuperExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSuperExp::initializeStorageClassArray(SgSuperExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSuperExp::pools.begin(); block != SgSuperExp::pools.end(); ++block) {
        SgSuperExp* pointer{reinterpret_cast<SgSuperExp*>(*block)};
        for (unsigned i = 0; i < SgSuperExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 133925 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeIdOp that takes its corresponding StorageClass as parameter
 */
SgTypeIdOp::SgTypeIdOp(const SgTypeIdOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 133935 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));
     p_operand_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_type));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_name_qualification_for_pointer_to_member_class_length = storageSource.storageOf_name_qualification_for_pointer_to_member_class_length;
    p_type_elaboration_for_pointer_to_member_class_required = storageSource.storageOf_type_elaboration_for_pointer_to_member_class_required;
    p_global_qualification_for_pointer_to_member_class_required = storageSource.storageOf_global_qualification_for_pointer_to_member_class_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeIdOp*
SgTypeIdOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeIdOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeIdOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeIdOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeIdOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeIdOp)};
        unsigned long positionInPool{localIndex % SgTypeIdOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeIdOp::pool_size};
        returnPointer = &(((SgTypeIdOp*)(SgTypeIdOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeIdOp*
SgTypeIdOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeIdOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeIdOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeIdOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeIdOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeIdOp)};
        unsigned long positionInPool{localIndex % SgTypeIdOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeIdOp::pool_size};
        returnPointer = &(((SgTypeIdOp*)(SgTypeIdOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeIdOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeIdOp::pools.begin(); block != SgTypeIdOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeIdOp*>(*block)};
        for (unsigned i = 0; i < SgTypeIdOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeIdOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeIdOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeIdOp* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeIdOp::pools.begin(); block != SgTypeIdOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeIdOp*>(*block)};
        for (unsigned i = 0; i < SgTypeIdOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeIdOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeIdOp::clearMemoryPool() {
    SgTypeIdOp* tempPointer{nullptr};
    if (SgTypeIdOp::pools.empty() == false) {
        auto block{SgTypeIdOp::pools.begin()};
        SgTypeIdOp::next_node = reinterpret_cast<SgTypeIdOp*>(*block);
        for (; block != SgTypeIdOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeIdOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeIdOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeIdOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeIdOp::pool_size-1]);
        }
    }
}

void
SgTypeIdOp::deleteMemoryPool() {
    for (auto p: SgTypeIdOp::pools) {
        ROSE_FREE(p);
    }
    SgTypeIdOp::next_node = nullptr;
    SgTypeIdOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeIdOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeIdOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeIdOp)};
    for (size_t blockIndex = SgTypeIdOp::pools.size(); (blockIndex * SgTypeIdOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeIdOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeIdOp) = %" PRIuPTR " SgTypeIdOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeIdOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeIdOp),SgTypeIdOp::pool_size);
        }
#endif

        SgTypeIdOp* pointer{static_cast<SgTypeIdOp*>(ROSE_MALLOC(SgTypeIdOp::pool_size * sizeof(SgTypeIdOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeIdOp::pool_size * sizeof(SgTypeIdOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeIdOp::pool_size * sizeof(SgTypeIdOp));
#endif
        SgTypeIdOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeIdOp::pool_size * sizeof(SgTypeIdOp), V_SgTypeIdOp));

        if (SgTypeIdOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeIdOp* blkptr{reinterpret_cast<SgTypeIdOp*>(SgTypeIdOp::pools[blockIndex-1])};
                blkptr[SgTypeIdOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeIdOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeIdOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeIdOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeIdOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeIdOp::pool_size - 1};
    SgTypeIdOp* testPointer{reinterpret_cast<SgTypeIdOp*>(SgTypeIdOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeIdOp::pool_size * (SgTypeIdOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeIdOpStroageClass
 * from its counterpart of SgTypeIdOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeIdOp::initializeStorageClassArray(SgTypeIdOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeIdOp::pools.begin(); block != SgTypeIdOp::pools.end(); ++block) {
        SgTypeIdOp* pointer{reinterpret_cast<SgTypeIdOp*>(*block)};
        for (unsigned i = 0; i < SgTypeIdOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 134160 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgConditionalExp that takes its corresponding StorageClass as parameter
 */
SgConditionalExp::SgConditionalExp(const SgConditionalExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 134170 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_conditional_exp = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_conditional_exp));
     p_true_exp = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_true_exp));
     p_false_exp = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_false_exp));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgConditionalExp*
SgConditionalExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgConditionalExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConditionalExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConditionalExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConditionalExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgConditionalExp)};
        unsigned long positionInPool{localIndex % SgConditionalExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConditionalExp::pool_size};
        returnPointer = &(((SgConditionalExp*)(SgConditionalExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgConditionalExp*
SgConditionalExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgConditionalExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConditionalExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConditionalExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConditionalExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgConditionalExp)};
        unsigned long positionInPool{localIndex % SgConditionalExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConditionalExp::pool_size};
        returnPointer = &(((SgConditionalExp*)(SgConditionalExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgConditionalExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgConditionalExp::pools.begin(); block != SgConditionalExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConditionalExp*>(*block)};
        for (unsigned i = 0; i < SgConditionalExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgConditionalExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgConditionalExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgConditionalExp* pointerOfLinkedList{nullptr};
    for (auto block = SgConditionalExp::pools.begin(); block != SgConditionalExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConditionalExp*>(*block)};
        for (unsigned i = 0; i < SgConditionalExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgConditionalExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgConditionalExp::clearMemoryPool() {
    SgConditionalExp* tempPointer{nullptr};
    if (SgConditionalExp::pools.empty() == false) {
        auto block{SgConditionalExp::pools.begin()};
        SgConditionalExp::next_node = reinterpret_cast<SgConditionalExp*>(*block);
        for (; block != SgConditionalExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgConditionalExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgConditionalExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgConditionalExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgConditionalExp::pool_size-1]);
        }
    }
}

void
SgConditionalExp::deleteMemoryPool() {
    for (auto p: SgConditionalExp::pools) {
        ROSE_FREE(p);
    }
    SgConditionalExp::next_node = nullptr;
    SgConditionalExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgConditionalExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgConditionalExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgConditionalExp)};
    for (size_t blockIndex = SgConditionalExp::pools.size(); (blockIndex * SgConditionalExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgConditionalExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgConditionalExp) = %" PRIuPTR " SgConditionalExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgConditionalExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgConditionalExp),SgConditionalExp::pool_size);
        }
#endif

        SgConditionalExp* pointer{static_cast<SgConditionalExp*>(ROSE_MALLOC(SgConditionalExp::pool_size * sizeof(SgConditionalExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgConditionalExp::pool_size * sizeof(SgConditionalExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgConditionalExp::pool_size * sizeof(SgConditionalExp));
#endif
        SgConditionalExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgConditionalExp::pool_size * sizeof(SgConditionalExp), V_SgConditionalExp));

        if (SgConditionalExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgConditionalExp* blkptr{reinterpret_cast<SgConditionalExp*>(SgConditionalExp::pools[blockIndex-1])};
                blkptr[SgConditionalExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgConditionalExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgConditionalExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgConditionalExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgConditionalExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgConditionalExp::pool_size - 1};
    SgConditionalExp* testPointer{reinterpret_cast<SgConditionalExp*>(SgConditionalExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgConditionalExp::pool_size * (SgConditionalExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgConditionalExpStroageClass
 * from its counterpart of SgConditionalExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgConditionalExp::initializeStorageClassArray(SgConditionalExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgConditionalExp::pools.begin(); block != SgConditionalExp::pools.end(); ++block) {
        SgConditionalExp* pointer{reinterpret_cast<SgConditionalExp*>(*block)};
        for (unsigned i = 0; i < SgConditionalExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 134391 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNewExp that takes its corresponding StorageClass as parameter
 */
SgNewExp::SgNewExp(const SgNewExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 134401 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_specified_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_specified_type));
     p_placement_args = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_placement_args));
     p_constructor_args = (SgConstructorInitializer*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_constructor_args));
     p_builtin_args = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_builtin_args));
    p_need_global_specifier = storageSource.storageOf_need_global_specifier;
     p_newOperatorDeclaration = (SgFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_newOperatorDeclaration));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_name_qualification_for_pointer_to_member_class_length = storageSource.storageOf_name_qualification_for_pointer_to_member_class_length;
    p_type_elaboration_for_pointer_to_member_class_required = storageSource.storageOf_type_elaboration_for_pointer_to_member_class_required;
    p_global_qualification_for_pointer_to_member_class_required = storageSource.storageOf_global_qualification_for_pointer_to_member_class_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNewExp*
SgNewExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNewExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNewExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNewExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNewExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNewExp)};
        unsigned long positionInPool{localIndex % SgNewExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNewExp::pool_size};
        returnPointer = &(((SgNewExp*)(SgNewExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNewExp*
SgNewExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNewExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNewExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNewExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNewExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNewExp)};
        unsigned long positionInPool{localIndex % SgNewExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNewExp::pool_size};
        returnPointer = &(((SgNewExp*)(SgNewExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNewExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNewExp::pools.begin(); block != SgNewExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNewExp*>(*block)};
        for (unsigned i = 0; i < SgNewExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNewExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNewExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNewExp* pointerOfLinkedList{nullptr};
    for (auto block = SgNewExp::pools.begin(); block != SgNewExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNewExp*>(*block)};
        for (unsigned i = 0; i < SgNewExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNewExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNewExp::clearMemoryPool() {
    SgNewExp* tempPointer{nullptr};
    if (SgNewExp::pools.empty() == false) {
        auto block{SgNewExp::pools.begin()};
        SgNewExp::next_node = reinterpret_cast<SgNewExp*>(*block);
        for (; block != SgNewExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNewExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNewExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNewExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNewExp::pool_size-1]);
        }
    }
}

void
SgNewExp::deleteMemoryPool() {
    for (auto p: SgNewExp::pools) {
        ROSE_FREE(p);
    }
    SgNewExp::next_node = nullptr;
    SgNewExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNewExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNewExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNewExp)};
    for (size_t blockIndex = SgNewExp::pools.size(); (blockIndex * SgNewExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNewExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNewExp) = %" PRIuPTR " SgNewExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNewExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNewExp),SgNewExp::pool_size);
        }
#endif

        SgNewExp* pointer{static_cast<SgNewExp*>(ROSE_MALLOC(SgNewExp::pool_size * sizeof(SgNewExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNewExp::pool_size * sizeof(SgNewExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNewExp::pool_size * sizeof(SgNewExp));
#endif
        SgNewExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNewExp::pool_size * sizeof(SgNewExp), V_SgNewExp));

        if (SgNewExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNewExp* blkptr{reinterpret_cast<SgNewExp*>(SgNewExp::pools[blockIndex-1])};
                blkptr[SgNewExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNewExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNewExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNewExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNewExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNewExp::pool_size - 1};
    SgNewExp* testPointer{reinterpret_cast<SgNewExp*>(SgNewExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNewExp::pool_size * (SgNewExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNewExpStroageClass
 * from its counterpart of SgNewExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNewExp::initializeStorageClassArray(SgNewExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNewExp::pools.begin(); block != SgNewExp::pools.end(); ++block) {
        SgNewExp* pointer{reinterpret_cast<SgNewExp*>(*block)};
        for (unsigned i = 0; i < SgNewExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 134630 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDeleteExp that takes its corresponding StorageClass as parameter
 */
SgDeleteExp::SgDeleteExp(const SgDeleteExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 134640 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_variable = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_variable));
    p_is_array = storageSource.storageOf_is_array;
    p_need_global_specifier = storageSource.storageOf_need_global_specifier;
     p_deleteOperatorDeclaration = (SgFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_deleteOperatorDeclaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDeleteExp*
SgDeleteExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDeleteExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeleteExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeleteExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDeleteExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDeleteExp)};
        unsigned long positionInPool{localIndex % SgDeleteExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeleteExp::pool_size};
        returnPointer = &(((SgDeleteExp*)(SgDeleteExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDeleteExp*
SgDeleteExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDeleteExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeleteExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeleteExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDeleteExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDeleteExp)};
        unsigned long positionInPool{localIndex % SgDeleteExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDeleteExp::pool_size};
        returnPointer = &(((SgDeleteExp*)(SgDeleteExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDeleteExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDeleteExp::pools.begin(); block != SgDeleteExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeleteExp*>(*block)};
        for (unsigned i = 0; i < SgDeleteExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDeleteExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDeleteExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDeleteExp* pointerOfLinkedList{nullptr};
    for (auto block = SgDeleteExp::pools.begin(); block != SgDeleteExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDeleteExp*>(*block)};
        for (unsigned i = 0; i < SgDeleteExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDeleteExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDeleteExp::clearMemoryPool() {
    SgDeleteExp* tempPointer{nullptr};
    if (SgDeleteExp::pools.empty() == false) {
        auto block{SgDeleteExp::pools.begin()};
        SgDeleteExp::next_node = reinterpret_cast<SgDeleteExp*>(*block);
        for (; block != SgDeleteExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDeleteExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDeleteExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDeleteExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDeleteExp::pool_size-1]);
        }
    }
}

void
SgDeleteExp::deleteMemoryPool() {
    for (auto p: SgDeleteExp::pools) {
        ROSE_FREE(p);
    }
    SgDeleteExp::next_node = nullptr;
    SgDeleteExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDeleteExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDeleteExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeleteExp)};
    for (size_t blockIndex = SgDeleteExp::pools.size(); (blockIndex * SgDeleteExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDeleteExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeleteExp) = %" PRIuPTR " SgDeleteExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDeleteExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDeleteExp),SgDeleteExp::pool_size);
        }
#endif

        SgDeleteExp* pointer{static_cast<SgDeleteExp*>(ROSE_MALLOC(SgDeleteExp::pool_size * sizeof(SgDeleteExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDeleteExp::pool_size * sizeof(SgDeleteExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDeleteExp::pool_size * sizeof(SgDeleteExp));
#endif
        SgDeleteExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDeleteExp::pool_size * sizeof(SgDeleteExp), V_SgDeleteExp));

        if (SgDeleteExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDeleteExp* blkptr{reinterpret_cast<SgDeleteExp*>(SgDeleteExp::pools[blockIndex-1])};
                blkptr[SgDeleteExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDeleteExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDeleteExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDeleteExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDeleteExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDeleteExp::pool_size - 1};
    SgDeleteExp* testPointer{reinterpret_cast<SgDeleteExp*>(SgDeleteExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDeleteExp::pool_size * (SgDeleteExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDeleteExpStroageClass
 * from its counterpart of SgDeleteExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDeleteExp::initializeStorageClassArray(SgDeleteExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDeleteExp::pools.begin(); block != SgDeleteExp::pools.end(); ++block) {
        SgDeleteExp* pointer{reinterpret_cast<SgDeleteExp*>(*block)};
        for (unsigned i = 0; i < SgDeleteExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 134861 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgThisExp that takes its corresponding StorageClass as parameter
 */
SgThisExp::SgThisExp(const SgThisExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 134871 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_class_symbol = (SgClassSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_class_symbol));
     p_nonreal_symbol = (SgNonrealSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonreal_symbol));
    p_pobj_this = storageSource.storageOf_pobj_this;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgThisExp*
SgThisExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgThisExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgThisExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgThisExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgThisExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgThisExp)};
        unsigned long positionInPool{localIndex % SgThisExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgThisExp::pool_size};
        returnPointer = &(((SgThisExp*)(SgThisExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgThisExp*
SgThisExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgThisExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgThisExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgThisExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgThisExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgThisExp)};
        unsigned long positionInPool{localIndex % SgThisExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgThisExp::pool_size};
        returnPointer = &(((SgThisExp*)(SgThisExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgThisExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgThisExp::pools.begin(); block != SgThisExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgThisExp*>(*block)};
        for (unsigned i = 0; i < SgThisExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgThisExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgThisExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgThisExp* pointerOfLinkedList{nullptr};
    for (auto block = SgThisExp::pools.begin(); block != SgThisExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgThisExp*>(*block)};
        for (unsigned i = 0; i < SgThisExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgThisExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgThisExp::clearMemoryPool() {
    SgThisExp* tempPointer{nullptr};
    if (SgThisExp::pools.empty() == false) {
        auto block{SgThisExp::pools.begin()};
        SgThisExp::next_node = reinterpret_cast<SgThisExp*>(*block);
        for (; block != SgThisExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgThisExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgThisExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgThisExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgThisExp::pool_size-1]);
        }
    }
}

void
SgThisExp::deleteMemoryPool() {
    for (auto p: SgThisExp::pools) {
        ROSE_FREE(p);
    }
    SgThisExp::next_node = nullptr;
    SgThisExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgThisExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgThisExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgThisExp)};
    for (size_t blockIndex = SgThisExp::pools.size(); (blockIndex * SgThisExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgThisExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgThisExp) = %" PRIuPTR " SgThisExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgThisExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgThisExp),SgThisExp::pool_size);
        }
#endif

        SgThisExp* pointer{static_cast<SgThisExp*>(ROSE_MALLOC(SgThisExp::pool_size * sizeof(SgThisExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgThisExp::pool_size * sizeof(SgThisExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgThisExp::pool_size * sizeof(SgThisExp));
#endif
        SgThisExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgThisExp::pool_size * sizeof(SgThisExp), V_SgThisExp));

        if (SgThisExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgThisExp* blkptr{reinterpret_cast<SgThisExp*>(SgThisExp::pools[blockIndex-1])};
                blkptr[SgThisExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgThisExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgThisExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgThisExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgThisExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgThisExp::pool_size - 1};
    SgThisExp* testPointer{reinterpret_cast<SgThisExp*>(SgThisExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgThisExp::pool_size * (SgThisExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgThisExpStroageClass
 * from its counterpart of SgThisExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgThisExp::initializeStorageClassArray(SgThisExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgThisExp::pools.begin(); block != SgThisExp::pools.end(); ++block) {
        SgThisExp* pointer{reinterpret_cast<SgThisExp*>(*block)};
        for (unsigned i = 0; i < SgThisExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 135091 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRefExp that takes its corresponding StorageClass as parameter
 */
SgRefExp::SgRefExp(const SgRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 135101 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_type_name = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type_name));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRefExp*
SgRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRefExp)};
        unsigned long positionInPool{localIndex % SgRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRefExp::pool_size};
        returnPointer = &(((SgRefExp*)(SgRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRefExp*
SgRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRefExp)};
        unsigned long positionInPool{localIndex % SgRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRefExp::pool_size};
        returnPointer = &(((SgRefExp*)(SgRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRefExp::pools.begin(); block != SgRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRefExp*>(*block)};
        for (unsigned i = 0; i < SgRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgRefExp::pools.begin(); block != SgRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRefExp*>(*block)};
        for (unsigned i = 0; i < SgRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRefExp::clearMemoryPool() {
    SgRefExp* tempPointer{nullptr};
    if (SgRefExp::pools.empty() == false) {
        auto block{SgRefExp::pools.begin()};
        SgRefExp::next_node = reinterpret_cast<SgRefExp*>(*block);
        for (; block != SgRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRefExp::pool_size-1]);
        }
    }
}

void
SgRefExp::deleteMemoryPool() {
    for (auto p: SgRefExp::pools) {
        ROSE_FREE(p);
    }
    SgRefExp::next_node = nullptr;
    SgRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRefExp)};
    for (size_t blockIndex = SgRefExp::pools.size(); (blockIndex * SgRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRefExp) = %" PRIuPTR " SgRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRefExp),SgRefExp::pool_size);
        }
#endif

        SgRefExp* pointer{static_cast<SgRefExp*>(ROSE_MALLOC(SgRefExp::pool_size * sizeof(SgRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRefExp::pool_size * sizeof(SgRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRefExp::pool_size * sizeof(SgRefExp));
#endif
        SgRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRefExp::pool_size * sizeof(SgRefExp), V_SgRefExp));

        if (SgRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRefExp* blkptr{reinterpret_cast<SgRefExp*>(SgRefExp::pools[blockIndex-1])};
                blkptr[SgRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRefExp::pool_size - 1};
    SgRefExp* testPointer{reinterpret_cast<SgRefExp*>(SgRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRefExp::pool_size * (SgRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRefExpStroageClass
 * from its counterpart of SgRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRefExp::initializeStorageClassArray(SgRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRefExp::pools.begin(); block != SgRefExp::pools.end(); ++block) {
        SgRefExp* pointer{reinterpret_cast<SgRefExp*>(*block)};
        for (unsigned i = 0; i < SgRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 135319 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgInitializer that takes its corresponding StorageClass as parameter
 */
SgInitializer::SgInitializer(const SgInitializerStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 135329 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_is_explicit_cast = storageSource.storageOf_is_explicit_cast;
    p_is_braced_initialized = storageSource.storageOf_is_braced_initialized;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgInitializer*
SgInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializer)};
        unsigned long positionInPool{localIndex % SgInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInitializer::pool_size};
        returnPointer = &(((SgInitializer*)(SgInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgInitializer*
SgInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgInitializer)};
        unsigned long positionInPool{localIndex % SgInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInitializer::pool_size};
        returnPointer = &(((SgInitializer*)(SgInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgInitializer::pools.begin(); block != SgInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInitializer*>(*block)};
        for (unsigned i = 0; i < SgInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgInitializer::pools.begin(); block != SgInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInitializer*>(*block)};
        for (unsigned i = 0; i < SgInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgInitializer::clearMemoryPool() {
    SgInitializer* tempPointer{nullptr};
    if (SgInitializer::pools.empty() == false) {
        auto block{SgInitializer::pools.begin()};
        SgInitializer::next_node = reinterpret_cast<SgInitializer*>(*block);
        for (; block != SgInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgInitializer::pool_size-1]);
        }
    }
}

void
SgInitializer::deleteMemoryPool() {
    for (auto p: SgInitializer::pools) {
        ROSE_FREE(p);
    }
    SgInitializer::next_node = nullptr;
    SgInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgInitializer)};
    for (size_t blockIndex = SgInitializer::pools.size(); (blockIndex * SgInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgInitializer) = %" PRIuPTR " SgInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgInitializer),SgInitializer::pool_size);
        }
#endif

        SgInitializer* pointer{static_cast<SgInitializer*>(ROSE_MALLOC(SgInitializer::pool_size * sizeof(SgInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgInitializer::pool_size * sizeof(SgInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgInitializer::pool_size * sizeof(SgInitializer));
#endif
        SgInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgInitializer::pool_size * sizeof(SgInitializer), V_SgInitializer));

        if (SgInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgInitializer* blkptr{reinterpret_cast<SgInitializer*>(SgInitializer::pools[blockIndex-1])};
                blkptr[SgInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgInitializer::pool_size - 1};
    SgInitializer* testPointer{reinterpret_cast<SgInitializer*>(SgInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgInitializer::pool_size * (SgInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgInitializerStroageClass
 * from its counterpart of SgInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgInitializer::initializeStorageClassArray(SgInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgInitializer::pools.begin(); block != SgInitializer::pools.end(); ++block) {
        SgInitializer* pointer{reinterpret_cast<SgInitializer*>(*block)};
        for (unsigned i = 0; i < SgInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 135548 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAggregateInitializer that takes its corresponding StorageClass as parameter
 */
SgAggregateInitializer::SgAggregateInitializer(const SgAggregateInitializerStorageClass& storageSource)  : SgInitializer (storageSource) {

/* #line 135558 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_initializers = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initializers));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
    p_need_explicit_braces = storageSource.storageOf_need_explicit_braces;
    p_uses_compound_literal = storageSource.storageOf_uses_compound_literal;
    p_requiresGlobalNameQualificationOnType = storageSource.storageOf_requiresGlobalNameQualificationOnType;
    p_name_qualification_length_for_type = storageSource.storageOf_name_qualification_length_for_type;
    p_type_elaboration_required_for_type = storageSource.storageOf_type_elaboration_required_for_type;
    p_global_qualification_required_for_type = storageSource.storageOf_global_qualification_required_for_type;
     p_originalExpressionTree = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_originalExpressionTree));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAggregateInitializer*
SgAggregateInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAggregateInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAggregateInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAggregateInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAggregateInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAggregateInitializer)};
        unsigned long positionInPool{localIndex % SgAggregateInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAggregateInitializer::pool_size};
        returnPointer = &(((SgAggregateInitializer*)(SgAggregateInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAggregateInitializer*
SgAggregateInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAggregateInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAggregateInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAggregateInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAggregateInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAggregateInitializer)};
        unsigned long positionInPool{localIndex % SgAggregateInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAggregateInitializer::pool_size};
        returnPointer = &(((SgAggregateInitializer*)(SgAggregateInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAggregateInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAggregateInitializer::pools.begin(); block != SgAggregateInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAggregateInitializer*>(*block)};
        for (unsigned i = 0; i < SgAggregateInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAggregateInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAggregateInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAggregateInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgAggregateInitializer::pools.begin(); block != SgAggregateInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAggregateInitializer*>(*block)};
        for (unsigned i = 0; i < SgAggregateInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAggregateInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAggregateInitializer::clearMemoryPool() {
    SgAggregateInitializer* tempPointer{nullptr};
    if (SgAggregateInitializer::pools.empty() == false) {
        auto block{SgAggregateInitializer::pools.begin()};
        SgAggregateInitializer::next_node = reinterpret_cast<SgAggregateInitializer*>(*block);
        for (; block != SgAggregateInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAggregateInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAggregateInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAggregateInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAggregateInitializer::pool_size-1]);
        }
    }
}

void
SgAggregateInitializer::deleteMemoryPool() {
    for (auto p: SgAggregateInitializer::pools) {
        ROSE_FREE(p);
    }
    SgAggregateInitializer::next_node = nullptr;
    SgAggregateInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAggregateInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAggregateInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAggregateInitializer)};
    for (size_t blockIndex = SgAggregateInitializer::pools.size(); (blockIndex * SgAggregateInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAggregateInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAggregateInitializer) = %" PRIuPTR " SgAggregateInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAggregateInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAggregateInitializer),SgAggregateInitializer::pool_size);
        }
#endif

        SgAggregateInitializer* pointer{static_cast<SgAggregateInitializer*>(ROSE_MALLOC(SgAggregateInitializer::pool_size * sizeof(SgAggregateInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAggregateInitializer::pool_size * sizeof(SgAggregateInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAggregateInitializer::pool_size * sizeof(SgAggregateInitializer));
#endif
        SgAggregateInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAggregateInitializer::pool_size * sizeof(SgAggregateInitializer), V_SgAggregateInitializer));

        if (SgAggregateInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAggregateInitializer* blkptr{reinterpret_cast<SgAggregateInitializer*>(SgAggregateInitializer::pools[blockIndex-1])};
                blkptr[SgAggregateInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAggregateInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAggregateInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAggregateInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAggregateInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAggregateInitializer::pool_size - 1};
    SgAggregateInitializer* testPointer{reinterpret_cast<SgAggregateInitializer*>(SgAggregateInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAggregateInitializer::pool_size * (SgAggregateInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAggregateInitializerStroageClass
 * from its counterpart of SgAggregateInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAggregateInitializer::initializeStorageClassArray(SgAggregateInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAggregateInitializer::pools.begin(); block != SgAggregateInitializer::pools.end(); ++block) {
        SgAggregateInitializer* pointer{reinterpret_cast<SgAggregateInitializer*>(*block)};
        for (unsigned i = 0; i < SgAggregateInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 135784 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCompoundInitializer that takes its corresponding StorageClass as parameter
 */
SgCompoundInitializer::SgCompoundInitializer(const SgCompoundInitializerStorageClass& storageSource)  : SgInitializer (storageSource) {

/* #line 135794 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_initializers = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initializers));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCompoundInitializer*
SgCompoundInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCompoundInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundInitializer)};
        unsigned long positionInPool{localIndex % SgCompoundInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCompoundInitializer::pool_size};
        returnPointer = &(((SgCompoundInitializer*)(SgCompoundInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCompoundInitializer*
SgCompoundInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCompoundInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCompoundInitializer)};
        unsigned long positionInPool{localIndex % SgCompoundInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCompoundInitializer::pool_size};
        returnPointer = &(((SgCompoundInitializer*)(SgCompoundInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCompoundInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCompoundInitializer::pools.begin(); block != SgCompoundInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCompoundInitializer*>(*block)};
        for (unsigned i = 0; i < SgCompoundInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCompoundInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCompoundInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCompoundInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgCompoundInitializer::pools.begin(); block != SgCompoundInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCompoundInitializer*>(*block)};
        for (unsigned i = 0; i < SgCompoundInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCompoundInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCompoundInitializer::clearMemoryPool() {
    SgCompoundInitializer* tempPointer{nullptr};
    if (SgCompoundInitializer::pools.empty() == false) {
        auto block{SgCompoundInitializer::pools.begin()};
        SgCompoundInitializer::next_node = reinterpret_cast<SgCompoundInitializer*>(*block);
        for (; block != SgCompoundInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCompoundInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCompoundInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCompoundInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCompoundInitializer::pool_size-1]);
        }
    }
}

void
SgCompoundInitializer::deleteMemoryPool() {
    for (auto p: SgCompoundInitializer::pools) {
        ROSE_FREE(p);
    }
    SgCompoundInitializer::next_node = nullptr;
    SgCompoundInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCompoundInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundInitializer)};
    for (size_t blockIndex = SgCompoundInitializer::pools.size(); (blockIndex * SgCompoundInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundInitializer) = %" PRIuPTR " SgCompoundInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundInitializer),SgCompoundInitializer::pool_size);
        }
#endif

        SgCompoundInitializer* pointer{static_cast<SgCompoundInitializer*>(ROSE_MALLOC(SgCompoundInitializer::pool_size * sizeof(SgCompoundInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCompoundInitializer::pool_size * sizeof(SgCompoundInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCompoundInitializer::pool_size * sizeof(SgCompoundInitializer));
#endif
        SgCompoundInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCompoundInitializer::pool_size * sizeof(SgCompoundInitializer), V_SgCompoundInitializer));

        if (SgCompoundInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCompoundInitializer* blkptr{reinterpret_cast<SgCompoundInitializer*>(SgCompoundInitializer::pools[blockIndex-1])};
                blkptr[SgCompoundInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCompoundInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCompoundInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCompoundInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCompoundInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCompoundInitializer::pool_size - 1};
    SgCompoundInitializer* testPointer{reinterpret_cast<SgCompoundInitializer*>(SgCompoundInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCompoundInitializer::pool_size * (SgCompoundInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCompoundInitializerStroageClass
 * from its counterpart of SgCompoundInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCompoundInitializer::initializeStorageClassArray(SgCompoundInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCompoundInitializer::pools.begin(); block != SgCompoundInitializer::pools.end(); ++block) {
        SgCompoundInitializer* pointer{reinterpret_cast<SgCompoundInitializer*>(*block)};
        for (unsigned i = 0; i < SgCompoundInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 136013 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgConstructorInitializer that takes its corresponding StorageClass as parameter
 */
SgConstructorInitializer::SgConstructorInitializer(const SgConstructorInitializerStorageClass& storageSource)  : SgInitializer (storageSource) {

/* #line 136023 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgMemberFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
     p_args = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_args));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
    p_need_name = storageSource.storageOf_need_name;
    p_need_qualifier = storageSource.storageOf_need_qualifier;
    p_need_parenthesis_after_name = storageSource.storageOf_need_parenthesis_after_name;
    p_associated_class_unknown = storageSource.storageOf_associated_class_unknown;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_is_used_in_conditional = storageSource.storageOf_is_used_in_conditional;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgConstructorInitializer*
SgConstructorInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgConstructorInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConstructorInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConstructorInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgConstructorInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgConstructorInitializer)};
        unsigned long positionInPool{localIndex % SgConstructorInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConstructorInitializer::pool_size};
        returnPointer = &(((SgConstructorInitializer*)(SgConstructorInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgConstructorInitializer*
SgConstructorInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgConstructorInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConstructorInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConstructorInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgConstructorInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgConstructorInitializer)};
        unsigned long positionInPool{localIndex % SgConstructorInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgConstructorInitializer::pool_size};
        returnPointer = &(((SgConstructorInitializer*)(SgConstructorInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgConstructorInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgConstructorInitializer::pools.begin(); block != SgConstructorInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConstructorInitializer*>(*block)};
        for (unsigned i = 0; i < SgConstructorInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgConstructorInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgConstructorInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgConstructorInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgConstructorInitializer::pools.begin(); block != SgConstructorInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgConstructorInitializer*>(*block)};
        for (unsigned i = 0; i < SgConstructorInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgConstructorInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgConstructorInitializer::clearMemoryPool() {
    SgConstructorInitializer* tempPointer{nullptr};
    if (SgConstructorInitializer::pools.empty() == false) {
        auto block{SgConstructorInitializer::pools.begin()};
        SgConstructorInitializer::next_node = reinterpret_cast<SgConstructorInitializer*>(*block);
        for (; block != SgConstructorInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgConstructorInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgConstructorInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgConstructorInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgConstructorInitializer::pool_size-1]);
        }
    }
}

void
SgConstructorInitializer::deleteMemoryPool() {
    for (auto p: SgConstructorInitializer::pools) {
        ROSE_FREE(p);
    }
    SgConstructorInitializer::next_node = nullptr;
    SgConstructorInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgConstructorInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgConstructorInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgConstructorInitializer)};
    for (size_t blockIndex = SgConstructorInitializer::pools.size(); (blockIndex * SgConstructorInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgConstructorInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgConstructorInitializer) = %" PRIuPTR " SgConstructorInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgConstructorInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgConstructorInitializer),SgConstructorInitializer::pool_size);
        }
#endif

        SgConstructorInitializer* pointer{static_cast<SgConstructorInitializer*>(ROSE_MALLOC(SgConstructorInitializer::pool_size * sizeof(SgConstructorInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgConstructorInitializer::pool_size * sizeof(SgConstructorInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgConstructorInitializer::pool_size * sizeof(SgConstructorInitializer));
#endif
        SgConstructorInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgConstructorInitializer::pool_size * sizeof(SgConstructorInitializer), V_SgConstructorInitializer));

        if (SgConstructorInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgConstructorInitializer* blkptr{reinterpret_cast<SgConstructorInitializer*>(SgConstructorInitializer::pools[blockIndex-1])};
                blkptr[SgConstructorInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgConstructorInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgConstructorInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgConstructorInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgConstructorInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgConstructorInitializer::pool_size - 1};
    SgConstructorInitializer* testPointer{reinterpret_cast<SgConstructorInitializer*>(SgConstructorInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgConstructorInitializer::pool_size * (SgConstructorInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgConstructorInitializerStroageClass
 * from its counterpart of SgConstructorInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgConstructorInitializer::initializeStorageClassArray(SgConstructorInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgConstructorInitializer::pools.begin(); block != SgConstructorInitializer::pools.end(); ++block) {
        SgConstructorInitializer* pointer{reinterpret_cast<SgConstructorInitializer*>(*block)};
        for (unsigned i = 0; i < SgConstructorInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 136251 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAssignInitializer that takes its corresponding StorageClass as parameter
 */
SgAssignInitializer::SgAssignInitializer(const SgAssignInitializerStorageClass& storageSource)  : SgInitializer (storageSource) {

/* #line 136261 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_i = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_i));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAssignInitializer*
SgAssignInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAssignInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAssignInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignInitializer)};
        unsigned long positionInPool{localIndex % SgAssignInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignInitializer::pool_size};
        returnPointer = &(((SgAssignInitializer*)(SgAssignInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAssignInitializer*
SgAssignInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAssignInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAssignInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAssignInitializer)};
        unsigned long positionInPool{localIndex % SgAssignInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAssignInitializer::pool_size};
        returnPointer = &(((SgAssignInitializer*)(SgAssignInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAssignInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAssignInitializer::pools.begin(); block != SgAssignInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignInitializer*>(*block)};
        for (unsigned i = 0; i < SgAssignInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAssignInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAssignInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAssignInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgAssignInitializer::pools.begin(); block != SgAssignInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAssignInitializer*>(*block)};
        for (unsigned i = 0; i < SgAssignInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAssignInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAssignInitializer::clearMemoryPool() {
    SgAssignInitializer* tempPointer{nullptr};
    if (SgAssignInitializer::pools.empty() == false) {
        auto block{SgAssignInitializer::pools.begin()};
        SgAssignInitializer::next_node = reinterpret_cast<SgAssignInitializer*>(*block);
        for (; block != SgAssignInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAssignInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAssignInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAssignInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAssignInitializer::pool_size-1]);
        }
    }
}

void
SgAssignInitializer::deleteMemoryPool() {
    for (auto p: SgAssignInitializer::pools) {
        ROSE_FREE(p);
    }
    SgAssignInitializer::next_node = nullptr;
    SgAssignInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAssignInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignInitializer)};
    for (size_t blockIndex = SgAssignInitializer::pools.size(); (blockIndex * SgAssignInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignInitializer) = %" PRIuPTR " SgAssignInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAssignInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAssignInitializer),SgAssignInitializer::pool_size);
        }
#endif

        SgAssignInitializer* pointer{static_cast<SgAssignInitializer*>(ROSE_MALLOC(SgAssignInitializer::pool_size * sizeof(SgAssignInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAssignInitializer::pool_size * sizeof(SgAssignInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAssignInitializer::pool_size * sizeof(SgAssignInitializer));
#endif
        SgAssignInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAssignInitializer::pool_size * sizeof(SgAssignInitializer), V_SgAssignInitializer));

        if (SgAssignInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAssignInitializer* blkptr{reinterpret_cast<SgAssignInitializer*>(SgAssignInitializer::pools[blockIndex-1])};
                blkptr[SgAssignInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAssignInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAssignInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAssignInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAssignInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAssignInitializer::pool_size - 1};
    SgAssignInitializer* testPointer{reinterpret_cast<SgAssignInitializer*>(SgAssignInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAssignInitializer::pool_size * (SgAssignInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAssignInitializerStroageClass
 * from its counterpart of SgAssignInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAssignInitializer::initializeStorageClassArray(SgAssignInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAssignInitializer::pools.begin(); block != SgAssignInitializer::pools.end(); ++block) {
        SgAssignInitializer* pointer{reinterpret_cast<SgAssignInitializer*>(*block)};
        for (unsigned i = 0; i < SgAssignInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 136480 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDesignatedInitializer that takes its corresponding StorageClass as parameter
 */
SgDesignatedInitializer::SgDesignatedInitializer(const SgDesignatedInitializerStorageClass& storageSource)  : SgInitializer (storageSource) {

/* #line 136490 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_designatorList = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_designatorList));
     p_memberInit = (SgInitializer*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_memberInit));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDesignatedInitializer*
SgDesignatedInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDesignatedInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDesignatedInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDesignatedInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDesignatedInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDesignatedInitializer)};
        unsigned long positionInPool{localIndex % SgDesignatedInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDesignatedInitializer::pool_size};
        returnPointer = &(((SgDesignatedInitializer*)(SgDesignatedInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDesignatedInitializer*
SgDesignatedInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDesignatedInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDesignatedInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDesignatedInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDesignatedInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDesignatedInitializer)};
        unsigned long positionInPool{localIndex % SgDesignatedInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDesignatedInitializer::pool_size};
        returnPointer = &(((SgDesignatedInitializer*)(SgDesignatedInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDesignatedInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDesignatedInitializer::pools.begin(); block != SgDesignatedInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDesignatedInitializer*>(*block)};
        for (unsigned i = 0; i < SgDesignatedInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDesignatedInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDesignatedInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDesignatedInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgDesignatedInitializer::pools.begin(); block != SgDesignatedInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDesignatedInitializer*>(*block)};
        for (unsigned i = 0; i < SgDesignatedInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDesignatedInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDesignatedInitializer::clearMemoryPool() {
    SgDesignatedInitializer* tempPointer{nullptr};
    if (SgDesignatedInitializer::pools.empty() == false) {
        auto block{SgDesignatedInitializer::pools.begin()};
        SgDesignatedInitializer::next_node = reinterpret_cast<SgDesignatedInitializer*>(*block);
        for (; block != SgDesignatedInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDesignatedInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDesignatedInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDesignatedInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDesignatedInitializer::pool_size-1]);
        }
    }
}

void
SgDesignatedInitializer::deleteMemoryPool() {
    for (auto p: SgDesignatedInitializer::pools) {
        ROSE_FREE(p);
    }
    SgDesignatedInitializer::next_node = nullptr;
    SgDesignatedInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDesignatedInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDesignatedInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDesignatedInitializer)};
    for (size_t blockIndex = SgDesignatedInitializer::pools.size(); (blockIndex * SgDesignatedInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDesignatedInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDesignatedInitializer) = %" PRIuPTR " SgDesignatedInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDesignatedInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDesignatedInitializer),SgDesignatedInitializer::pool_size);
        }
#endif

        SgDesignatedInitializer* pointer{static_cast<SgDesignatedInitializer*>(ROSE_MALLOC(SgDesignatedInitializer::pool_size * sizeof(SgDesignatedInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDesignatedInitializer::pool_size * sizeof(SgDesignatedInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDesignatedInitializer::pool_size * sizeof(SgDesignatedInitializer));
#endif
        SgDesignatedInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDesignatedInitializer::pool_size * sizeof(SgDesignatedInitializer), V_SgDesignatedInitializer));

        if (SgDesignatedInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDesignatedInitializer* blkptr{reinterpret_cast<SgDesignatedInitializer*>(SgDesignatedInitializer::pools[blockIndex-1])};
                blkptr[SgDesignatedInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDesignatedInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDesignatedInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDesignatedInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDesignatedInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDesignatedInitializer::pool_size - 1};
    SgDesignatedInitializer* testPointer{reinterpret_cast<SgDesignatedInitializer*>(SgDesignatedInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDesignatedInitializer::pool_size * (SgDesignatedInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDesignatedInitializerStroageClass
 * from its counterpart of SgDesignatedInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDesignatedInitializer::initializeStorageClassArray(SgDesignatedInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDesignatedInitializer::pools.begin(); block != SgDesignatedInitializer::pools.end(); ++block) {
        SgDesignatedInitializer* pointer{reinterpret_cast<SgDesignatedInitializer*>(*block)};
        for (unsigned i = 0; i < SgDesignatedInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 136709 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgBracedInitializer that takes its corresponding StorageClass as parameter
 */
SgBracedInitializer::SgBracedInitializer(const SgBracedInitializerStorageClass& storageSource)  : SgInitializer (storageSource) {

/* #line 136719 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_initializers = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_initializers));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgBracedInitializer*
SgBracedInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgBracedInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBracedInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBracedInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgBracedInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgBracedInitializer)};
        unsigned long positionInPool{localIndex % SgBracedInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBracedInitializer::pool_size};
        returnPointer = &(((SgBracedInitializer*)(SgBracedInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgBracedInitializer*
SgBracedInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgBracedInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBracedInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBracedInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgBracedInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgBracedInitializer)};
        unsigned long positionInPool{localIndex % SgBracedInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgBracedInitializer::pool_size};
        returnPointer = &(((SgBracedInitializer*)(SgBracedInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgBracedInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgBracedInitializer::pools.begin(); block != SgBracedInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBracedInitializer*>(*block)};
        for (unsigned i = 0; i < SgBracedInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgBracedInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgBracedInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgBracedInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgBracedInitializer::pools.begin(); block != SgBracedInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgBracedInitializer*>(*block)};
        for (unsigned i = 0; i < SgBracedInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgBracedInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgBracedInitializer::clearMemoryPool() {
    SgBracedInitializer* tempPointer{nullptr};
    if (SgBracedInitializer::pools.empty() == false) {
        auto block{SgBracedInitializer::pools.begin()};
        SgBracedInitializer::next_node = reinterpret_cast<SgBracedInitializer*>(*block);
        for (; block != SgBracedInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgBracedInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgBracedInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgBracedInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgBracedInitializer::pool_size-1]);
        }
    }
}

void
SgBracedInitializer::deleteMemoryPool() {
    for (auto p: SgBracedInitializer::pools) {
        ROSE_FREE(p);
    }
    SgBracedInitializer::next_node = nullptr;
    SgBracedInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgBracedInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgBracedInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgBracedInitializer)};
    for (size_t blockIndex = SgBracedInitializer::pools.size(); (blockIndex * SgBracedInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgBracedInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgBracedInitializer) = %" PRIuPTR " SgBracedInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgBracedInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgBracedInitializer),SgBracedInitializer::pool_size);
        }
#endif

        SgBracedInitializer* pointer{static_cast<SgBracedInitializer*>(ROSE_MALLOC(SgBracedInitializer::pool_size * sizeof(SgBracedInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgBracedInitializer::pool_size * sizeof(SgBracedInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgBracedInitializer::pool_size * sizeof(SgBracedInitializer));
#endif
        SgBracedInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgBracedInitializer::pool_size * sizeof(SgBracedInitializer), V_SgBracedInitializer));

        if (SgBracedInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgBracedInitializer* blkptr{reinterpret_cast<SgBracedInitializer*>(SgBracedInitializer::pools[blockIndex-1])};
                blkptr[SgBracedInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgBracedInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgBracedInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgBracedInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgBracedInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgBracedInitializer::pool_size - 1};
    SgBracedInitializer* testPointer{reinterpret_cast<SgBracedInitializer*>(SgBracedInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgBracedInitializer::pool_size * (SgBracedInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgBracedInitializerStroageClass
 * from its counterpart of SgBracedInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgBracedInitializer::initializeStorageClassArray(SgBracedInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgBracedInitializer::pools.begin(); block != SgBracedInitializer::pools.end(); ++block) {
        SgBracedInitializer* pointer{reinterpret_cast<SgBracedInitializer*>(*block)};
        for (unsigned i = 0; i < SgBracedInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 136938 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaAncestorInitializer that takes its corresponding StorageClass as parameter
 */
SgAdaAncestorInitializer::SgAdaAncestorInitializer(const SgAdaAncestorInitializerStorageClass& storageSource)  : SgInitializer (storageSource) {

/* #line 136948 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_ancestor = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ancestor));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaAncestorInitializer*
SgAdaAncestorInitializer::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaAncestorInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAncestorInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAncestorInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAncestorInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAncestorInitializer)};
        unsigned long positionInPool{localIndex % SgAdaAncestorInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAncestorInitializer::pool_size};
        returnPointer = &(((SgAdaAncestorInitializer*)(SgAdaAncestorInitializer::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaAncestorInitializer*
SgAdaAncestorInitializer::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaAncestorInitializer* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAncestorInitializer)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAncestorInitializer + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAncestorInitializer)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaAncestorInitializer)};
        unsigned long positionInPool{localIndex % SgAdaAncestorInitializer::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAncestorInitializer::pool_size};
        returnPointer = &(((SgAdaAncestorInitializer*)(SgAdaAncestorInitializer::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaAncestorInitializer::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaAncestorInitializer::pools.begin(); block != SgAdaAncestorInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAncestorInitializer*>(*block)};
        for (unsigned i = 0; i < SgAdaAncestorInitializer::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaAncestorInitializer*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaAncestorInitializer::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaAncestorInitializer* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaAncestorInitializer::pools.begin(); block != SgAdaAncestorInitializer::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAncestorInitializer*>(*block)};
        for (unsigned i = 0; i < SgAdaAncestorInitializer::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaAncestorInitializer::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaAncestorInitializer::clearMemoryPool() {
    SgAdaAncestorInitializer* tempPointer{nullptr};
    if (SgAdaAncestorInitializer::pools.empty() == false) {
        auto block{SgAdaAncestorInitializer::pools.begin()};
        SgAdaAncestorInitializer::next_node = reinterpret_cast<SgAdaAncestorInitializer*>(*block);
        for (; block != SgAdaAncestorInitializer::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaAncestorInitializer*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaAncestorInitializer::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaAncestorInitializer::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaAncestorInitializer::pool_size-1]);
        }
    }
}

void
SgAdaAncestorInitializer::deleteMemoryPool() {
    for (auto p: SgAdaAncestorInitializer::pools) {
        ROSE_FREE(p);
    }
    SgAdaAncestorInitializer::next_node = nullptr;
    SgAdaAncestorInitializer::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaAncestorInitializer::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAncestorInitializer) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAncestorInitializer)};
    for (size_t blockIndex = SgAdaAncestorInitializer::pools.size(); (blockIndex * SgAdaAncestorInitializer::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAncestorInitializer) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAncestorInitializer) = %" PRIuPTR " SgAdaAncestorInitializer::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAncestorInitializer),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAncestorInitializer),SgAdaAncestorInitializer::pool_size);
        }
#endif

        SgAdaAncestorInitializer* pointer{static_cast<SgAdaAncestorInitializer*>(ROSE_MALLOC(SgAdaAncestorInitializer::pool_size * sizeof(SgAdaAncestorInitializer)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaAncestorInitializer::pool_size * sizeof(SgAdaAncestorInitializer));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaAncestorInitializer::pool_size * sizeof(SgAdaAncestorInitializer));
#endif
        SgAdaAncestorInitializer::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaAncestorInitializer::pool_size * sizeof(SgAdaAncestorInitializer), V_SgAdaAncestorInitializer));

        if (SgAdaAncestorInitializer::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaAncestorInitializer* blkptr{reinterpret_cast<SgAdaAncestorInitializer*>(SgAdaAncestorInitializer::pools[blockIndex-1])};
                blkptr[SgAdaAncestorInitializer::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaAncestorInitializer::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaAncestorInitializer::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaAncestorInitializer::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaAncestorInitializer::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaAncestorInitializer::pool_size - 1};
    SgAdaAncestorInitializer* testPointer{reinterpret_cast<SgAdaAncestorInitializer*>(SgAdaAncestorInitializer::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaAncestorInitializer::pool_size * (SgAdaAncestorInitializer::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaAncestorInitializerStroageClass
 * from its counterpart of SgAdaAncestorInitializer. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaAncestorInitializer::initializeStorageClassArray(SgAdaAncestorInitializerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaAncestorInitializer::pools.begin(); block != SgAdaAncestorInitializer::pools.end(); ++block) {
        SgAdaAncestorInitializer* pointer{reinterpret_cast<SgAdaAncestorInitializer*>(*block)};
        for (unsigned i = 0; i < SgAdaAncestorInitializer::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 137166 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVarArgStartOp that takes its corresponding StorageClass as parameter
 */
SgVarArgStartOp::SgVarArgStartOp(const SgVarArgStartOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 137176 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lhs_operand = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lhs_operand));
     p_rhs_operand = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rhs_operand));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVarArgStartOp*
SgVarArgStartOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVarArgStartOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgStartOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgStartOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgStartOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOp)};
        unsigned long positionInPool{localIndex % SgVarArgStartOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgStartOp::pool_size};
        returnPointer = &(((SgVarArgStartOp*)(SgVarArgStartOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVarArgStartOp*
SgVarArgStartOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVarArgStartOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgStartOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgStartOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgStartOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVarArgStartOp)};
        unsigned long positionInPool{localIndex % SgVarArgStartOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgStartOp::pool_size};
        returnPointer = &(((SgVarArgStartOp*)(SgVarArgStartOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVarArgStartOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVarArgStartOp::pools.begin(); block != SgVarArgStartOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgStartOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgStartOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVarArgStartOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVarArgStartOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVarArgStartOp* pointerOfLinkedList{nullptr};
    for (auto block = SgVarArgStartOp::pools.begin(); block != SgVarArgStartOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgStartOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgStartOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVarArgStartOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVarArgStartOp::clearMemoryPool() {
    SgVarArgStartOp* tempPointer{nullptr};
    if (SgVarArgStartOp::pools.empty() == false) {
        auto block{SgVarArgStartOp::pools.begin()};
        SgVarArgStartOp::next_node = reinterpret_cast<SgVarArgStartOp*>(*block);
        for (; block != SgVarArgStartOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVarArgStartOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVarArgStartOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVarArgStartOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVarArgStartOp::pool_size-1]);
        }
    }
}

void
SgVarArgStartOp::deleteMemoryPool() {
    for (auto p: SgVarArgStartOp::pools) {
        ROSE_FREE(p);
    }
    SgVarArgStartOp::next_node = nullptr;
    SgVarArgStartOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVarArgStartOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgStartOp)};
    for (size_t blockIndex = SgVarArgStartOp::pools.size(); (blockIndex * SgVarArgStartOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgStartOp) = %" PRIuPTR " SgVarArgStartOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgStartOp),SgVarArgStartOp::pool_size);
        }
#endif

        SgVarArgStartOp* pointer{static_cast<SgVarArgStartOp*>(ROSE_MALLOC(SgVarArgStartOp::pool_size * sizeof(SgVarArgStartOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVarArgStartOp::pool_size * sizeof(SgVarArgStartOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVarArgStartOp::pool_size * sizeof(SgVarArgStartOp));
#endif
        SgVarArgStartOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVarArgStartOp::pool_size * sizeof(SgVarArgStartOp), V_SgVarArgStartOp));

        if (SgVarArgStartOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVarArgStartOp* blkptr{reinterpret_cast<SgVarArgStartOp*>(SgVarArgStartOp::pools[blockIndex-1])};
                blkptr[SgVarArgStartOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVarArgStartOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVarArgStartOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVarArgStartOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVarArgStartOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVarArgStartOp::pool_size - 1};
    SgVarArgStartOp* testPointer{reinterpret_cast<SgVarArgStartOp*>(SgVarArgStartOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVarArgStartOp::pool_size * (SgVarArgStartOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVarArgStartOpStroageClass
 * from its counterpart of SgVarArgStartOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVarArgStartOp::initializeStorageClassArray(SgVarArgStartOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVarArgStartOp::pools.begin(); block != SgVarArgStartOp::pools.end(); ++block) {
        SgVarArgStartOp* pointer{reinterpret_cast<SgVarArgStartOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgStartOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 137396 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVarArgOp that takes its corresponding StorageClass as parameter
 */
SgVarArgOp::SgVarArgOp(const SgVarArgOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 137406 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVarArgOp*
SgVarArgOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVarArgOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgOp)};
        unsigned long positionInPool{localIndex % SgVarArgOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgOp::pool_size};
        returnPointer = &(((SgVarArgOp*)(SgVarArgOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVarArgOp*
SgVarArgOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVarArgOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVarArgOp)};
        unsigned long positionInPool{localIndex % SgVarArgOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgOp::pool_size};
        returnPointer = &(((SgVarArgOp*)(SgVarArgOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVarArgOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVarArgOp::pools.begin(); block != SgVarArgOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVarArgOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVarArgOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVarArgOp* pointerOfLinkedList{nullptr};
    for (auto block = SgVarArgOp::pools.begin(); block != SgVarArgOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVarArgOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVarArgOp::clearMemoryPool() {
    SgVarArgOp* tempPointer{nullptr};
    if (SgVarArgOp::pools.empty() == false) {
        auto block{SgVarArgOp::pools.begin()};
        SgVarArgOp::next_node = reinterpret_cast<SgVarArgOp*>(*block);
        for (; block != SgVarArgOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVarArgOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVarArgOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVarArgOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVarArgOp::pool_size-1]);
        }
    }
}

void
SgVarArgOp::deleteMemoryPool() {
    for (auto p: SgVarArgOp::pools) {
        ROSE_FREE(p);
    }
    SgVarArgOp::next_node = nullptr;
    SgVarArgOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVarArgOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgOp)};
    for (size_t blockIndex = SgVarArgOp::pools.size(); (blockIndex * SgVarArgOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgOp) = %" PRIuPTR " SgVarArgOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgOp),SgVarArgOp::pool_size);
        }
#endif

        SgVarArgOp* pointer{static_cast<SgVarArgOp*>(ROSE_MALLOC(SgVarArgOp::pool_size * sizeof(SgVarArgOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVarArgOp::pool_size * sizeof(SgVarArgOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVarArgOp::pool_size * sizeof(SgVarArgOp));
#endif
        SgVarArgOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVarArgOp::pool_size * sizeof(SgVarArgOp), V_SgVarArgOp));

        if (SgVarArgOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVarArgOp* blkptr{reinterpret_cast<SgVarArgOp*>(SgVarArgOp::pools[blockIndex-1])};
                blkptr[SgVarArgOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVarArgOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVarArgOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVarArgOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVarArgOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVarArgOp::pool_size - 1};
    SgVarArgOp* testPointer{reinterpret_cast<SgVarArgOp*>(SgVarArgOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVarArgOp::pool_size * (SgVarArgOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVarArgOpStroageClass
 * from its counterpart of SgVarArgOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVarArgOp::initializeStorageClassArray(SgVarArgOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVarArgOp::pools.begin(); block != SgVarArgOp::pools.end(); ++block) {
        SgVarArgOp* pointer{reinterpret_cast<SgVarArgOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 137625 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVarArgEndOp that takes its corresponding StorageClass as parameter
 */
SgVarArgEndOp::SgVarArgEndOp(const SgVarArgEndOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 137635 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVarArgEndOp*
SgVarArgEndOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVarArgEndOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgEndOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgEndOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgEndOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgEndOp)};
        unsigned long positionInPool{localIndex % SgVarArgEndOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgEndOp::pool_size};
        returnPointer = &(((SgVarArgEndOp*)(SgVarArgEndOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVarArgEndOp*
SgVarArgEndOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVarArgEndOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgEndOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgEndOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgEndOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVarArgEndOp)};
        unsigned long positionInPool{localIndex % SgVarArgEndOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgEndOp::pool_size};
        returnPointer = &(((SgVarArgEndOp*)(SgVarArgEndOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVarArgEndOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVarArgEndOp::pools.begin(); block != SgVarArgEndOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgEndOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgEndOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVarArgEndOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVarArgEndOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVarArgEndOp* pointerOfLinkedList{nullptr};
    for (auto block = SgVarArgEndOp::pools.begin(); block != SgVarArgEndOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgEndOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgEndOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVarArgEndOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVarArgEndOp::clearMemoryPool() {
    SgVarArgEndOp* tempPointer{nullptr};
    if (SgVarArgEndOp::pools.empty() == false) {
        auto block{SgVarArgEndOp::pools.begin()};
        SgVarArgEndOp::next_node = reinterpret_cast<SgVarArgEndOp*>(*block);
        for (; block != SgVarArgEndOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVarArgEndOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVarArgEndOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVarArgEndOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVarArgEndOp::pool_size-1]);
        }
    }
}

void
SgVarArgEndOp::deleteMemoryPool() {
    for (auto p: SgVarArgEndOp::pools) {
        ROSE_FREE(p);
    }
    SgVarArgEndOp::next_node = nullptr;
    SgVarArgEndOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVarArgEndOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgEndOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgEndOp)};
    for (size_t blockIndex = SgVarArgEndOp::pools.size(); (blockIndex * SgVarArgEndOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgEndOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgEndOp) = %" PRIuPTR " SgVarArgEndOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgEndOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgEndOp),SgVarArgEndOp::pool_size);
        }
#endif

        SgVarArgEndOp* pointer{static_cast<SgVarArgEndOp*>(ROSE_MALLOC(SgVarArgEndOp::pool_size * sizeof(SgVarArgEndOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVarArgEndOp::pool_size * sizeof(SgVarArgEndOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVarArgEndOp::pool_size * sizeof(SgVarArgEndOp));
#endif
        SgVarArgEndOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVarArgEndOp::pool_size * sizeof(SgVarArgEndOp), V_SgVarArgEndOp));

        if (SgVarArgEndOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVarArgEndOp* blkptr{reinterpret_cast<SgVarArgEndOp*>(SgVarArgEndOp::pools[blockIndex-1])};
                blkptr[SgVarArgEndOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVarArgEndOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVarArgEndOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVarArgEndOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVarArgEndOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVarArgEndOp::pool_size - 1};
    SgVarArgEndOp* testPointer{reinterpret_cast<SgVarArgEndOp*>(SgVarArgEndOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVarArgEndOp::pool_size * (SgVarArgEndOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVarArgEndOpStroageClass
 * from its counterpart of SgVarArgEndOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVarArgEndOp::initializeStorageClassArray(SgVarArgEndOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVarArgEndOp::pools.begin(); block != SgVarArgEndOp::pools.end(); ++block) {
        SgVarArgEndOp* pointer{reinterpret_cast<SgVarArgEndOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgEndOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 137854 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVarArgCopyOp that takes its corresponding StorageClass as parameter
 */
SgVarArgCopyOp::SgVarArgCopyOp(const SgVarArgCopyOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 137864 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lhs_operand = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lhs_operand));
     p_rhs_operand = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rhs_operand));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVarArgCopyOp*
SgVarArgCopyOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVarArgCopyOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgCopyOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgCopyOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgCopyOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgCopyOp)};
        unsigned long positionInPool{localIndex % SgVarArgCopyOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgCopyOp::pool_size};
        returnPointer = &(((SgVarArgCopyOp*)(SgVarArgCopyOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVarArgCopyOp*
SgVarArgCopyOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVarArgCopyOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgCopyOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgCopyOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgCopyOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVarArgCopyOp)};
        unsigned long positionInPool{localIndex % SgVarArgCopyOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgCopyOp::pool_size};
        returnPointer = &(((SgVarArgCopyOp*)(SgVarArgCopyOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVarArgCopyOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVarArgCopyOp::pools.begin(); block != SgVarArgCopyOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgCopyOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgCopyOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVarArgCopyOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVarArgCopyOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVarArgCopyOp* pointerOfLinkedList{nullptr};
    for (auto block = SgVarArgCopyOp::pools.begin(); block != SgVarArgCopyOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgCopyOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgCopyOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVarArgCopyOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVarArgCopyOp::clearMemoryPool() {
    SgVarArgCopyOp* tempPointer{nullptr};
    if (SgVarArgCopyOp::pools.empty() == false) {
        auto block{SgVarArgCopyOp::pools.begin()};
        SgVarArgCopyOp::next_node = reinterpret_cast<SgVarArgCopyOp*>(*block);
        for (; block != SgVarArgCopyOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVarArgCopyOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVarArgCopyOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVarArgCopyOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVarArgCopyOp::pool_size-1]);
        }
    }
}

void
SgVarArgCopyOp::deleteMemoryPool() {
    for (auto p: SgVarArgCopyOp::pools) {
        ROSE_FREE(p);
    }
    SgVarArgCopyOp::next_node = nullptr;
    SgVarArgCopyOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVarArgCopyOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgCopyOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgCopyOp)};
    for (size_t blockIndex = SgVarArgCopyOp::pools.size(); (blockIndex * SgVarArgCopyOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgCopyOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgCopyOp) = %" PRIuPTR " SgVarArgCopyOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgCopyOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgCopyOp),SgVarArgCopyOp::pool_size);
        }
#endif

        SgVarArgCopyOp* pointer{static_cast<SgVarArgCopyOp*>(ROSE_MALLOC(SgVarArgCopyOp::pool_size * sizeof(SgVarArgCopyOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVarArgCopyOp::pool_size * sizeof(SgVarArgCopyOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVarArgCopyOp::pool_size * sizeof(SgVarArgCopyOp));
#endif
        SgVarArgCopyOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVarArgCopyOp::pool_size * sizeof(SgVarArgCopyOp), V_SgVarArgCopyOp));

        if (SgVarArgCopyOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVarArgCopyOp* blkptr{reinterpret_cast<SgVarArgCopyOp*>(SgVarArgCopyOp::pools[blockIndex-1])};
                blkptr[SgVarArgCopyOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVarArgCopyOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVarArgCopyOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVarArgCopyOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVarArgCopyOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVarArgCopyOp::pool_size - 1};
    SgVarArgCopyOp* testPointer{reinterpret_cast<SgVarArgCopyOp*>(SgVarArgCopyOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVarArgCopyOp::pool_size * (SgVarArgCopyOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVarArgCopyOpStroageClass
 * from its counterpart of SgVarArgCopyOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVarArgCopyOp::initializeStorageClassArray(SgVarArgCopyOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVarArgCopyOp::pools.begin(); block != SgVarArgCopyOp::pools.end(); ++block) {
        SgVarArgCopyOp* pointer{reinterpret_cast<SgVarArgCopyOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgCopyOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 138084 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVarArgStartOneOperandOp that takes its corresponding StorageClass as parameter
 */
SgVarArgStartOneOperandOp::SgVarArgStartOneOperandOp(const SgVarArgStartOneOperandOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 138094 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVarArgStartOneOperandOp*
SgVarArgStartOneOperandOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVarArgStartOneOperandOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgStartOneOperandOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgStartOneOperandOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarArgStartOneOperandOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOneOperandOp)};
        unsigned long positionInPool{localIndex % SgVarArgStartOneOperandOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgStartOneOperandOp::pool_size};
        returnPointer = &(((SgVarArgStartOneOperandOp*)(SgVarArgStartOneOperandOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVarArgStartOneOperandOp*
SgVarArgStartOneOperandOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVarArgStartOneOperandOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgStartOneOperandOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgStartOneOperandOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarArgStartOneOperandOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVarArgStartOneOperandOp)};
        unsigned long positionInPool{localIndex % SgVarArgStartOneOperandOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarArgStartOneOperandOp::pool_size};
        returnPointer = &(((SgVarArgStartOneOperandOp*)(SgVarArgStartOneOperandOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVarArgStartOneOperandOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVarArgStartOneOperandOp::pools.begin(); block != SgVarArgStartOneOperandOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgStartOneOperandOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgStartOneOperandOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVarArgStartOneOperandOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVarArgStartOneOperandOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVarArgStartOneOperandOp* pointerOfLinkedList{nullptr};
    for (auto block = SgVarArgStartOneOperandOp::pools.begin(); block != SgVarArgStartOneOperandOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarArgStartOneOperandOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgStartOneOperandOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVarArgStartOneOperandOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVarArgStartOneOperandOp::clearMemoryPool() {
    SgVarArgStartOneOperandOp* tempPointer{nullptr};
    if (SgVarArgStartOneOperandOp::pools.empty() == false) {
        auto block{SgVarArgStartOneOperandOp::pools.begin()};
        SgVarArgStartOneOperandOp::next_node = reinterpret_cast<SgVarArgStartOneOperandOp*>(*block);
        for (; block != SgVarArgStartOneOperandOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVarArgStartOneOperandOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVarArgStartOneOperandOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVarArgStartOneOperandOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVarArgStartOneOperandOp::pool_size-1]);
        }
    }
}

void
SgVarArgStartOneOperandOp::deleteMemoryPool() {
    for (auto p: SgVarArgStartOneOperandOp::pools) {
        ROSE_FREE(p);
    }
    SgVarArgStartOneOperandOp::next_node = nullptr;
    SgVarArgStartOneOperandOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVarArgStartOneOperandOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOneOperandOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgStartOneOperandOp)};
    for (size_t blockIndex = SgVarArgStartOneOperandOp::pools.size(); (blockIndex * SgVarArgStartOneOperandOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOneOperandOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgStartOneOperandOp) = %" PRIuPTR " SgVarArgStartOneOperandOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVarArgStartOneOperandOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarArgStartOneOperandOp),SgVarArgStartOneOperandOp::pool_size);
        }
#endif

        SgVarArgStartOneOperandOp* pointer{static_cast<SgVarArgStartOneOperandOp*>(ROSE_MALLOC(SgVarArgStartOneOperandOp::pool_size * sizeof(SgVarArgStartOneOperandOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVarArgStartOneOperandOp::pool_size * sizeof(SgVarArgStartOneOperandOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVarArgStartOneOperandOp::pool_size * sizeof(SgVarArgStartOneOperandOp));
#endif
        SgVarArgStartOneOperandOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVarArgStartOneOperandOp::pool_size * sizeof(SgVarArgStartOneOperandOp), V_SgVarArgStartOneOperandOp));

        if (SgVarArgStartOneOperandOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVarArgStartOneOperandOp* blkptr{reinterpret_cast<SgVarArgStartOneOperandOp*>(SgVarArgStartOneOperandOp::pools[blockIndex-1])};
                blkptr[SgVarArgStartOneOperandOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVarArgStartOneOperandOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVarArgStartOneOperandOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVarArgStartOneOperandOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVarArgStartOneOperandOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVarArgStartOneOperandOp::pool_size - 1};
    SgVarArgStartOneOperandOp* testPointer{reinterpret_cast<SgVarArgStartOneOperandOp*>(SgVarArgStartOneOperandOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVarArgStartOneOperandOp::pool_size * (SgVarArgStartOneOperandOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVarArgStartOneOperandOpStroageClass
 * from its counterpart of SgVarArgStartOneOperandOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVarArgStartOneOperandOp::initializeStorageClassArray(SgVarArgStartOneOperandOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVarArgStartOneOperandOp::pools.begin(); block != SgVarArgStartOneOperandOp::pools.end(); ++block) {
        SgVarArgStartOneOperandOp* pointer{reinterpret_cast<SgVarArgStartOneOperandOp*>(*block)};
        for (unsigned i = 0; i < SgVarArgStartOneOperandOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 138313 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNullExpression that takes its corresponding StorageClass as parameter
 */
SgNullExpression::SgNullExpression(const SgNullExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 138323 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNullExpression*
SgNullExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNullExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNullExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNullExpression)};
        unsigned long positionInPool{localIndex % SgNullExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullExpression::pool_size};
        returnPointer = &(((SgNullExpression*)(SgNullExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNullExpression*
SgNullExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNullExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNullExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNullExpression)};
        unsigned long positionInPool{localIndex % SgNullExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNullExpression::pool_size};
        returnPointer = &(((SgNullExpression*)(SgNullExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNullExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNullExpression::pools.begin(); block != SgNullExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullExpression*>(*block)};
        for (unsigned i = 0; i < SgNullExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNullExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNullExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNullExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgNullExpression::pools.begin(); block != SgNullExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNullExpression*>(*block)};
        for (unsigned i = 0; i < SgNullExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNullExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNullExpression::clearMemoryPool() {
    SgNullExpression* tempPointer{nullptr};
    if (SgNullExpression::pools.empty() == false) {
        auto block{SgNullExpression::pools.begin()};
        SgNullExpression::next_node = reinterpret_cast<SgNullExpression*>(*block);
        for (; block != SgNullExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNullExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNullExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNullExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNullExpression::pool_size-1]);
        }
    }
}

void
SgNullExpression::deleteMemoryPool() {
    for (auto p: SgNullExpression::pools) {
        ROSE_FREE(p);
    }
    SgNullExpression::next_node = nullptr;
    SgNullExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNullExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNullExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullExpression)};
    for (size_t blockIndex = SgNullExpression::pools.size(); (blockIndex * SgNullExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNullExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullExpression) = %" PRIuPTR " SgNullExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNullExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNullExpression),SgNullExpression::pool_size);
        }
#endif

        SgNullExpression* pointer{static_cast<SgNullExpression*>(ROSE_MALLOC(SgNullExpression::pool_size * sizeof(SgNullExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNullExpression::pool_size * sizeof(SgNullExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNullExpression::pool_size * sizeof(SgNullExpression));
#endif
        SgNullExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNullExpression::pool_size * sizeof(SgNullExpression), V_SgNullExpression));

        if (SgNullExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNullExpression* blkptr{reinterpret_cast<SgNullExpression*>(SgNullExpression::pools[blockIndex-1])};
                blkptr[SgNullExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNullExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNullExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNullExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNullExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNullExpression::pool_size - 1};
    SgNullExpression* testPointer{reinterpret_cast<SgNullExpression*>(SgNullExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNullExpression::pool_size * (SgNullExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNullExpressionStroageClass
 * from its counterpart of SgNullExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNullExpression::initializeStorageClassArray(SgNullExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNullExpression::pools.begin(); block != SgNullExpression::pools.end(); ++block) {
        SgNullExpression* pointer{reinterpret_cast<SgNullExpression*>(*block)};
        for (unsigned i = 0; i < SgNullExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 138540 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVariantExpression that takes its corresponding StorageClass as parameter
 */
SgVariantExpression::SgVariantExpression(const SgVariantExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 138550 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVariantExpression*
SgVariantExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVariantExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariantExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariantExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariantExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantExpression)};
        unsigned long positionInPool{localIndex % SgVariantExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariantExpression::pool_size};
        returnPointer = &(((SgVariantExpression*)(SgVariantExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVariantExpression*
SgVariantExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVariantExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariantExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariantExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariantExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVariantExpression)};
        unsigned long positionInPool{localIndex % SgVariantExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariantExpression::pool_size};
        returnPointer = &(((SgVariantExpression*)(SgVariantExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVariantExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVariantExpression::pools.begin(); block != SgVariantExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariantExpression*>(*block)};
        for (unsigned i = 0; i < SgVariantExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVariantExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVariantExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVariantExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgVariantExpression::pools.begin(); block != SgVariantExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariantExpression*>(*block)};
        for (unsigned i = 0; i < SgVariantExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVariantExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVariantExpression::clearMemoryPool() {
    SgVariantExpression* tempPointer{nullptr};
    if (SgVariantExpression::pools.empty() == false) {
        auto block{SgVariantExpression::pools.begin()};
        SgVariantExpression::next_node = reinterpret_cast<SgVariantExpression*>(*block);
        for (; block != SgVariantExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVariantExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVariantExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVariantExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVariantExpression::pool_size-1]);
        }
    }
}

void
SgVariantExpression::deleteMemoryPool() {
    for (auto p: SgVariantExpression::pools) {
        ROSE_FREE(p);
    }
    SgVariantExpression::next_node = nullptr;
    SgVariantExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVariantExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariantExpression)};
    for (size_t blockIndex = SgVariantExpression::pools.size(); (blockIndex * SgVariantExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariantExpression) = %" PRIuPTR " SgVariantExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVariantExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariantExpression),SgVariantExpression::pool_size);
        }
#endif

        SgVariantExpression* pointer{static_cast<SgVariantExpression*>(ROSE_MALLOC(SgVariantExpression::pool_size * sizeof(SgVariantExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVariantExpression::pool_size * sizeof(SgVariantExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVariantExpression::pool_size * sizeof(SgVariantExpression));
#endif
        SgVariantExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVariantExpression::pool_size * sizeof(SgVariantExpression), V_SgVariantExpression));

        if (SgVariantExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVariantExpression* blkptr{reinterpret_cast<SgVariantExpression*>(SgVariantExpression::pools[blockIndex-1])};
                blkptr[SgVariantExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVariantExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVariantExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVariantExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVariantExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVariantExpression::pool_size - 1};
    SgVariantExpression* testPointer{reinterpret_cast<SgVariantExpression*>(SgVariantExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVariantExpression::pool_size * (SgVariantExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVariantExpressionStroageClass
 * from its counterpart of SgVariantExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVariantExpression::initializeStorageClassArray(SgVariantExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVariantExpression::pools.begin(); block != SgVariantExpression::pools.end(); ++block) {
        SgVariantExpression* pointer{reinterpret_cast<SgVariantExpression*>(*block)};
        for (unsigned i = 0; i < SgVariantExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 138767 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSubscriptExpression that takes its corresponding StorageClass as parameter
 */
SgSubscriptExpression::SgSubscriptExpression(const SgSubscriptExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 138777 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lowerBound = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lowerBound));
     p_upperBound = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_upperBound));
     p_stride = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_stride));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSubscriptExpression*
SgSubscriptExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSubscriptExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSubscriptExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSubscriptExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSubscriptExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSubscriptExpression)};
        unsigned long positionInPool{localIndex % SgSubscriptExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSubscriptExpression::pool_size};
        returnPointer = &(((SgSubscriptExpression*)(SgSubscriptExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSubscriptExpression*
SgSubscriptExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSubscriptExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSubscriptExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSubscriptExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSubscriptExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSubscriptExpression)};
        unsigned long positionInPool{localIndex % SgSubscriptExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSubscriptExpression::pool_size};
        returnPointer = &(((SgSubscriptExpression*)(SgSubscriptExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSubscriptExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSubscriptExpression::pools.begin(); block != SgSubscriptExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSubscriptExpression*>(*block)};
        for (unsigned i = 0; i < SgSubscriptExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSubscriptExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSubscriptExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSubscriptExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgSubscriptExpression::pools.begin(); block != SgSubscriptExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSubscriptExpression*>(*block)};
        for (unsigned i = 0; i < SgSubscriptExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSubscriptExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSubscriptExpression::clearMemoryPool() {
    SgSubscriptExpression* tempPointer{nullptr};
    if (SgSubscriptExpression::pools.empty() == false) {
        auto block{SgSubscriptExpression::pools.begin()};
        SgSubscriptExpression::next_node = reinterpret_cast<SgSubscriptExpression*>(*block);
        for (; block != SgSubscriptExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSubscriptExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSubscriptExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSubscriptExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSubscriptExpression::pool_size-1]);
        }
    }
}

void
SgSubscriptExpression::deleteMemoryPool() {
    for (auto p: SgSubscriptExpression::pools) {
        ROSE_FREE(p);
    }
    SgSubscriptExpression::next_node = nullptr;
    SgSubscriptExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSubscriptExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSubscriptExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSubscriptExpression)};
    for (size_t blockIndex = SgSubscriptExpression::pools.size(); (blockIndex * SgSubscriptExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSubscriptExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSubscriptExpression) = %" PRIuPTR " SgSubscriptExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSubscriptExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSubscriptExpression),SgSubscriptExpression::pool_size);
        }
#endif

        SgSubscriptExpression* pointer{static_cast<SgSubscriptExpression*>(ROSE_MALLOC(SgSubscriptExpression::pool_size * sizeof(SgSubscriptExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSubscriptExpression::pool_size * sizeof(SgSubscriptExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSubscriptExpression::pool_size * sizeof(SgSubscriptExpression));
#endif
        SgSubscriptExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSubscriptExpression::pool_size * sizeof(SgSubscriptExpression), V_SgSubscriptExpression));

        if (SgSubscriptExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSubscriptExpression* blkptr{reinterpret_cast<SgSubscriptExpression*>(SgSubscriptExpression::pools[blockIndex-1])};
                blkptr[SgSubscriptExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSubscriptExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSubscriptExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSubscriptExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSubscriptExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSubscriptExpression::pool_size - 1};
    SgSubscriptExpression* testPointer{reinterpret_cast<SgSubscriptExpression*>(SgSubscriptExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSubscriptExpression::pool_size * (SgSubscriptExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSubscriptExpressionStroageClass
 * from its counterpart of SgSubscriptExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSubscriptExpression::initializeStorageClassArray(SgSubscriptExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSubscriptExpression::pools.begin(); block != SgSubscriptExpression::pools.end(); ++block) {
        SgSubscriptExpression* pointer{reinterpret_cast<SgSubscriptExpression*>(*block)};
        for (unsigned i = 0; i < SgSubscriptExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 138997 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgColonShapeExp that takes its corresponding StorageClass as parameter
 */
SgColonShapeExp::SgColonShapeExp(const SgColonShapeExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 139007 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgColonShapeExp*
SgColonShapeExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgColonShapeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgColonShapeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgColonShapeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgColonShapeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgColonShapeExp)};
        unsigned long positionInPool{localIndex % SgColonShapeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgColonShapeExp::pool_size};
        returnPointer = &(((SgColonShapeExp*)(SgColonShapeExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgColonShapeExp*
SgColonShapeExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgColonShapeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgColonShapeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgColonShapeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgColonShapeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgColonShapeExp)};
        unsigned long positionInPool{localIndex % SgColonShapeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgColonShapeExp::pool_size};
        returnPointer = &(((SgColonShapeExp*)(SgColonShapeExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgColonShapeExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgColonShapeExp::pools.begin(); block != SgColonShapeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgColonShapeExp*>(*block)};
        for (unsigned i = 0; i < SgColonShapeExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgColonShapeExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgColonShapeExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgColonShapeExp* pointerOfLinkedList{nullptr};
    for (auto block = SgColonShapeExp::pools.begin(); block != SgColonShapeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgColonShapeExp*>(*block)};
        for (unsigned i = 0; i < SgColonShapeExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgColonShapeExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgColonShapeExp::clearMemoryPool() {
    SgColonShapeExp* tempPointer{nullptr};
    if (SgColonShapeExp::pools.empty() == false) {
        auto block{SgColonShapeExp::pools.begin()};
        SgColonShapeExp::next_node = reinterpret_cast<SgColonShapeExp*>(*block);
        for (; block != SgColonShapeExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgColonShapeExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgColonShapeExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgColonShapeExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgColonShapeExp::pool_size-1]);
        }
    }
}

void
SgColonShapeExp::deleteMemoryPool() {
    for (auto p: SgColonShapeExp::pools) {
        ROSE_FREE(p);
    }
    SgColonShapeExp::next_node = nullptr;
    SgColonShapeExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgColonShapeExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgColonShapeExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgColonShapeExp)};
    for (size_t blockIndex = SgColonShapeExp::pools.size(); (blockIndex * SgColonShapeExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgColonShapeExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgColonShapeExp) = %" PRIuPTR " SgColonShapeExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgColonShapeExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgColonShapeExp),SgColonShapeExp::pool_size);
        }
#endif

        SgColonShapeExp* pointer{static_cast<SgColonShapeExp*>(ROSE_MALLOC(SgColonShapeExp::pool_size * sizeof(SgColonShapeExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgColonShapeExp::pool_size * sizeof(SgColonShapeExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgColonShapeExp::pool_size * sizeof(SgColonShapeExp));
#endif
        SgColonShapeExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgColonShapeExp::pool_size * sizeof(SgColonShapeExp), V_SgColonShapeExp));

        if (SgColonShapeExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgColonShapeExp* blkptr{reinterpret_cast<SgColonShapeExp*>(SgColonShapeExp::pools[blockIndex-1])};
                blkptr[SgColonShapeExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgColonShapeExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgColonShapeExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgColonShapeExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgColonShapeExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgColonShapeExp::pool_size - 1};
    SgColonShapeExp* testPointer{reinterpret_cast<SgColonShapeExp*>(SgColonShapeExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgColonShapeExp::pool_size * (SgColonShapeExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgColonShapeExpStroageClass
 * from its counterpart of SgColonShapeExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgColonShapeExp::initializeStorageClassArray(SgColonShapeExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgColonShapeExp::pools.begin(); block != SgColonShapeExp::pools.end(); ++block) {
        SgColonShapeExp* pointer{reinterpret_cast<SgColonShapeExp*>(*block)};
        for (unsigned i = 0; i < SgColonShapeExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 139224 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsteriskShapeExp that takes its corresponding StorageClass as parameter
 */
SgAsteriskShapeExp::SgAsteriskShapeExp(const SgAsteriskShapeExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 139234 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsteriskShapeExp*
SgAsteriskShapeExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsteriskShapeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsteriskShapeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsteriskShapeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsteriskShapeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsteriskShapeExp)};
        unsigned long positionInPool{localIndex % SgAsteriskShapeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsteriskShapeExp::pool_size};
        returnPointer = &(((SgAsteriskShapeExp*)(SgAsteriskShapeExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsteriskShapeExp*
SgAsteriskShapeExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsteriskShapeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsteriskShapeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsteriskShapeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsteriskShapeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsteriskShapeExp)};
        unsigned long positionInPool{localIndex % SgAsteriskShapeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsteriskShapeExp::pool_size};
        returnPointer = &(((SgAsteriskShapeExp*)(SgAsteriskShapeExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsteriskShapeExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsteriskShapeExp::pools.begin(); block != SgAsteriskShapeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsteriskShapeExp*>(*block)};
        for (unsigned i = 0; i < SgAsteriskShapeExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsteriskShapeExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsteriskShapeExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsteriskShapeExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAsteriskShapeExp::pools.begin(); block != SgAsteriskShapeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsteriskShapeExp*>(*block)};
        for (unsigned i = 0; i < SgAsteriskShapeExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsteriskShapeExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsteriskShapeExp::clearMemoryPool() {
    SgAsteriskShapeExp* tempPointer{nullptr};
    if (SgAsteriskShapeExp::pools.empty() == false) {
        auto block{SgAsteriskShapeExp::pools.begin()};
        SgAsteriskShapeExp::next_node = reinterpret_cast<SgAsteriskShapeExp*>(*block);
        for (; block != SgAsteriskShapeExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsteriskShapeExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsteriskShapeExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsteriskShapeExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsteriskShapeExp::pool_size-1]);
        }
    }
}

void
SgAsteriskShapeExp::deleteMemoryPool() {
    for (auto p: SgAsteriskShapeExp::pools) {
        ROSE_FREE(p);
    }
    SgAsteriskShapeExp::next_node = nullptr;
    SgAsteriskShapeExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsteriskShapeExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsteriskShapeExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsteriskShapeExp)};
    for (size_t blockIndex = SgAsteriskShapeExp::pools.size(); (blockIndex * SgAsteriskShapeExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsteriskShapeExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsteriskShapeExp) = %" PRIuPTR " SgAsteriskShapeExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsteriskShapeExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsteriskShapeExp),SgAsteriskShapeExp::pool_size);
        }
#endif

        SgAsteriskShapeExp* pointer{static_cast<SgAsteriskShapeExp*>(ROSE_MALLOC(SgAsteriskShapeExp::pool_size * sizeof(SgAsteriskShapeExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsteriskShapeExp::pool_size * sizeof(SgAsteriskShapeExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsteriskShapeExp::pool_size * sizeof(SgAsteriskShapeExp));
#endif
        SgAsteriskShapeExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsteriskShapeExp::pool_size * sizeof(SgAsteriskShapeExp), V_SgAsteriskShapeExp));

        if (SgAsteriskShapeExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsteriskShapeExp* blkptr{reinterpret_cast<SgAsteriskShapeExp*>(SgAsteriskShapeExp::pools[blockIndex-1])};
                blkptr[SgAsteriskShapeExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsteriskShapeExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsteriskShapeExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsteriskShapeExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsteriskShapeExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsteriskShapeExp::pool_size - 1};
    SgAsteriskShapeExp* testPointer{reinterpret_cast<SgAsteriskShapeExp*>(SgAsteriskShapeExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsteriskShapeExp::pool_size * (SgAsteriskShapeExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsteriskShapeExpStroageClass
 * from its counterpart of SgAsteriskShapeExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsteriskShapeExp::initializeStorageClassArray(SgAsteriskShapeExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsteriskShapeExp::pools.begin(); block != SgAsteriskShapeExp::pools.end(); ++block) {
        SgAsteriskShapeExp* pointer{reinterpret_cast<SgAsteriskShapeExp*>(*block)};
        for (unsigned i = 0; i < SgAsteriskShapeExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 139451 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgImpliedDo that takes its corresponding StorageClass as parameter
 */
SgImpliedDo::SgImpliedDo(const SgImpliedDoStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 139461 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_do_var_initialization = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_do_var_initialization));
     p_last_val = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_last_val));
     p_increment = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_increment));
     p_object_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_object_list));
     p_implied_do_scope = (SgScopeStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_implied_do_scope));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgImpliedDo*
SgImpliedDo::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgImpliedDo* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImpliedDo)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImpliedDo + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgImpliedDo)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgImpliedDo)};
        unsigned long positionInPool{localIndex % SgImpliedDo::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImpliedDo::pool_size};
        returnPointer = &(((SgImpliedDo*)(SgImpliedDo::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgImpliedDo*
SgImpliedDo::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgImpliedDo* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImpliedDo)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImpliedDo + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgImpliedDo)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgImpliedDo)};
        unsigned long positionInPool{localIndex % SgImpliedDo::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgImpliedDo::pool_size};
        returnPointer = &(((SgImpliedDo*)(SgImpliedDo::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgImpliedDo::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgImpliedDo::pools.begin(); block != SgImpliedDo::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImpliedDo*>(*block)};
        for (unsigned i = 0; i < SgImpliedDo::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgImpliedDo*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgImpliedDo::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgImpliedDo* pointerOfLinkedList{nullptr};
    for (auto block = SgImpliedDo::pools.begin(); block != SgImpliedDo::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgImpliedDo*>(*block)};
        for (unsigned i = 0; i < SgImpliedDo::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgImpliedDo::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgImpliedDo::clearMemoryPool() {
    SgImpliedDo* tempPointer{nullptr};
    if (SgImpliedDo::pools.empty() == false) {
        auto block{SgImpliedDo::pools.begin()};
        SgImpliedDo::next_node = reinterpret_cast<SgImpliedDo*>(*block);
        for (; block != SgImpliedDo::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgImpliedDo*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgImpliedDo::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgImpliedDo::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgImpliedDo::pool_size-1]);
        }
    }
}

void
SgImpliedDo::deleteMemoryPool() {
    for (auto p: SgImpliedDo::pools) {
        ROSE_FREE(p);
    }
    SgImpliedDo::next_node = nullptr;
    SgImpliedDo::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgImpliedDo::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgImpliedDo) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgImpliedDo)};
    for (size_t blockIndex = SgImpliedDo::pools.size(); (blockIndex * SgImpliedDo::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgImpliedDo) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgImpliedDo) = %" PRIuPTR " SgImpliedDo::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgImpliedDo),AST_FILE_IO::getPoolSizeOfNewAst(V_SgImpliedDo),SgImpliedDo::pool_size);
        }
#endif

        SgImpliedDo* pointer{static_cast<SgImpliedDo*>(ROSE_MALLOC(SgImpliedDo::pool_size * sizeof(SgImpliedDo)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgImpliedDo::pool_size * sizeof(SgImpliedDo));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgImpliedDo::pool_size * sizeof(SgImpliedDo));
#endif
        SgImpliedDo::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgImpliedDo::pool_size * sizeof(SgImpliedDo), V_SgImpliedDo));

        if (SgImpliedDo::next_node != nullptr) {
            if (blockIndex > 0) {
                SgImpliedDo* blkptr{reinterpret_cast<SgImpliedDo*>(SgImpliedDo::pools[blockIndex-1])};
                blkptr[SgImpliedDo::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgImpliedDo::next_node = pointer;
        }

        for (unsigned i = 0; i < SgImpliedDo::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgImpliedDo::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgImpliedDo::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgImpliedDo::pool_size - 1};
    SgImpliedDo* testPointer{reinterpret_cast<SgImpliedDo*>(SgImpliedDo::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgImpliedDo::pool_size * (SgImpliedDo::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgImpliedDoStroageClass
 * from its counterpart of SgImpliedDo. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgImpliedDo::initializeStorageClassArray(SgImpliedDoStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgImpliedDo::pools.begin(); block != SgImpliedDo::pools.end(); ++block) {
        SgImpliedDo* pointer{reinterpret_cast<SgImpliedDo*>(*block)};
        for (unsigned i = 0; i < SgImpliedDo::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 139683 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIOItemExpression that takes its corresponding StorageClass as parameter
 */
SgIOItemExpression::SgIOItemExpression(const SgIOItemExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 139693 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_io_item = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_io_item));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIOItemExpression*
SgIOItemExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIOItemExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIOItemExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIOItemExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIOItemExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIOItemExpression)};
        unsigned long positionInPool{localIndex % SgIOItemExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIOItemExpression::pool_size};
        returnPointer = &(((SgIOItemExpression*)(SgIOItemExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIOItemExpression*
SgIOItemExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIOItemExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIOItemExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIOItemExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIOItemExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIOItemExpression)};
        unsigned long positionInPool{localIndex % SgIOItemExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIOItemExpression::pool_size};
        returnPointer = &(((SgIOItemExpression*)(SgIOItemExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIOItemExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIOItemExpression::pools.begin(); block != SgIOItemExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIOItemExpression*>(*block)};
        for (unsigned i = 0; i < SgIOItemExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIOItemExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIOItemExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIOItemExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgIOItemExpression::pools.begin(); block != SgIOItemExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIOItemExpression*>(*block)};
        for (unsigned i = 0; i < SgIOItemExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIOItemExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIOItemExpression::clearMemoryPool() {
    SgIOItemExpression* tempPointer{nullptr};
    if (SgIOItemExpression::pools.empty() == false) {
        auto block{SgIOItemExpression::pools.begin()};
        SgIOItemExpression::next_node = reinterpret_cast<SgIOItemExpression*>(*block);
        for (; block != SgIOItemExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIOItemExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIOItemExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIOItemExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIOItemExpression::pool_size-1]);
        }
    }
}

void
SgIOItemExpression::deleteMemoryPool() {
    for (auto p: SgIOItemExpression::pools) {
        ROSE_FREE(p);
    }
    SgIOItemExpression::next_node = nullptr;
    SgIOItemExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIOItemExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIOItemExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIOItemExpression)};
    for (size_t blockIndex = SgIOItemExpression::pools.size(); (blockIndex * SgIOItemExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIOItemExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIOItemExpression) = %" PRIuPTR " SgIOItemExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIOItemExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIOItemExpression),SgIOItemExpression::pool_size);
        }
#endif

        SgIOItemExpression* pointer{static_cast<SgIOItemExpression*>(ROSE_MALLOC(SgIOItemExpression::pool_size * sizeof(SgIOItemExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIOItemExpression::pool_size * sizeof(SgIOItemExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIOItemExpression::pool_size * sizeof(SgIOItemExpression));
#endif
        SgIOItemExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIOItemExpression::pool_size * sizeof(SgIOItemExpression), V_SgIOItemExpression));

        if (SgIOItemExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIOItemExpression* blkptr{reinterpret_cast<SgIOItemExpression*>(SgIOItemExpression::pools[blockIndex-1])};
                blkptr[SgIOItemExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIOItemExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIOItemExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIOItemExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIOItemExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIOItemExpression::pool_size - 1};
    SgIOItemExpression* testPointer{reinterpret_cast<SgIOItemExpression*>(SgIOItemExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIOItemExpression::pool_size * (SgIOItemExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIOItemExpressionStroageClass
 * from its counterpart of SgIOItemExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIOItemExpression::initializeStorageClassArray(SgIOItemExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIOItemExpression::pools.begin(); block != SgIOItemExpression::pools.end(); ++block) {
        SgIOItemExpression* pointer{reinterpret_cast<SgIOItemExpression*>(*block)};
        for (unsigned i = 0; i < SgIOItemExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 139912 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgActualArgumentExpression that takes its corresponding StorageClass as parameter
 */
SgActualArgumentExpression::SgActualArgumentExpression(const SgActualArgumentExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 139922 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_argument_name = SgName ( storageSource.storageOf_argument_name);
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgActualArgumentExpression*
SgActualArgumentExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgActualArgumentExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgActualArgumentExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgActualArgumentExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgActualArgumentExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgActualArgumentExpression)};
        unsigned long positionInPool{localIndex % SgActualArgumentExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgActualArgumentExpression::pool_size};
        returnPointer = &(((SgActualArgumentExpression*)(SgActualArgumentExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgActualArgumentExpression*
SgActualArgumentExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgActualArgumentExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgActualArgumentExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgActualArgumentExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgActualArgumentExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgActualArgumentExpression)};
        unsigned long positionInPool{localIndex % SgActualArgumentExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgActualArgumentExpression::pool_size};
        returnPointer = &(((SgActualArgumentExpression*)(SgActualArgumentExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgActualArgumentExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgActualArgumentExpression::pools.begin(); block != SgActualArgumentExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgActualArgumentExpression*>(*block)};
        for (unsigned i = 0; i < SgActualArgumentExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgActualArgumentExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgActualArgumentExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgActualArgumentExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgActualArgumentExpression::pools.begin(); block != SgActualArgumentExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgActualArgumentExpression*>(*block)};
        for (unsigned i = 0; i < SgActualArgumentExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgActualArgumentExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgActualArgumentExpression::clearMemoryPool() {
    SgActualArgumentExpression* tempPointer{nullptr};
    if (SgActualArgumentExpression::pools.empty() == false) {
        auto block{SgActualArgumentExpression::pools.begin()};
        SgActualArgumentExpression::next_node = reinterpret_cast<SgActualArgumentExpression*>(*block);
        for (; block != SgActualArgumentExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgActualArgumentExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgActualArgumentExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgActualArgumentExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgActualArgumentExpression::pool_size-1]);
        }
    }
}

void
SgActualArgumentExpression::deleteMemoryPool() {
    for (auto p: SgActualArgumentExpression::pools) {
        ROSE_FREE(p);
    }
    SgActualArgumentExpression::next_node = nullptr;
    SgActualArgumentExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgActualArgumentExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgActualArgumentExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgActualArgumentExpression)};
    for (size_t blockIndex = SgActualArgumentExpression::pools.size(); (blockIndex * SgActualArgumentExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgActualArgumentExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgActualArgumentExpression) = %" PRIuPTR " SgActualArgumentExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgActualArgumentExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgActualArgumentExpression),SgActualArgumentExpression::pool_size);
        }
#endif

        SgActualArgumentExpression* pointer{static_cast<SgActualArgumentExpression*>(ROSE_MALLOC(SgActualArgumentExpression::pool_size * sizeof(SgActualArgumentExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgActualArgumentExpression::pool_size * sizeof(SgActualArgumentExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgActualArgumentExpression::pool_size * sizeof(SgActualArgumentExpression));
#endif
        SgActualArgumentExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgActualArgumentExpression::pool_size * sizeof(SgActualArgumentExpression), V_SgActualArgumentExpression));

        if (SgActualArgumentExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgActualArgumentExpression* blkptr{reinterpret_cast<SgActualArgumentExpression*>(SgActualArgumentExpression::pools[blockIndex-1])};
                blkptr[SgActualArgumentExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgActualArgumentExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgActualArgumentExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgActualArgumentExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgActualArgumentExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgActualArgumentExpression::pool_size - 1};
    SgActualArgumentExpression* testPointer{reinterpret_cast<SgActualArgumentExpression*>(SgActualArgumentExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgActualArgumentExpression::pool_size * (SgActualArgumentExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgActualArgumentExpressionStroageClass
 * from its counterpart of SgActualArgumentExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgActualArgumentExpression::initializeStorageClassArray(SgActualArgumentExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgActualArgumentExpression::pools.begin(); block != SgActualArgumentExpression::pools.end(); ++block) {
        SgActualArgumentExpression* pointer{reinterpret_cast<SgActualArgumentExpression*>(*block)};
        for (unsigned i = 0; i < SgActualArgumentExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 140141 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStatementExpression that takes its corresponding StorageClass as parameter
 */
SgStatementExpression::SgStatementExpression(const SgStatementExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 140151 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_statement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_statement));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStatementExpression*
SgStatementExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStatementExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatementExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatementExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStatementExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementExpression)};
        unsigned long positionInPool{localIndex % SgStatementExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStatementExpression::pool_size};
        returnPointer = &(((SgStatementExpression*)(SgStatementExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStatementExpression*
SgStatementExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStatementExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatementExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatementExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStatementExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStatementExpression)};
        unsigned long positionInPool{localIndex % SgStatementExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStatementExpression::pool_size};
        returnPointer = &(((SgStatementExpression*)(SgStatementExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStatementExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStatementExpression::pools.begin(); block != SgStatementExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStatementExpression*>(*block)};
        for (unsigned i = 0; i < SgStatementExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStatementExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStatementExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStatementExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgStatementExpression::pools.begin(); block != SgStatementExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStatementExpression*>(*block)};
        for (unsigned i = 0; i < SgStatementExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStatementExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStatementExpression::clearMemoryPool() {
    SgStatementExpression* tempPointer{nullptr};
    if (SgStatementExpression::pools.empty() == false) {
        auto block{SgStatementExpression::pools.begin()};
        SgStatementExpression::next_node = reinterpret_cast<SgStatementExpression*>(*block);
        for (; block != SgStatementExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStatementExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStatementExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStatementExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStatementExpression::pool_size-1]);
        }
    }
}

void
SgStatementExpression::deleteMemoryPool() {
    for (auto p: SgStatementExpression::pools) {
        ROSE_FREE(p);
    }
    SgStatementExpression::next_node = nullptr;
    SgStatementExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStatementExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatementExpression)};
    for (size_t blockIndex = SgStatementExpression::pools.size(); (blockIndex * SgStatementExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatementExpression) = %" PRIuPTR " SgStatementExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStatementExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStatementExpression),SgStatementExpression::pool_size);
        }
#endif

        SgStatementExpression* pointer{static_cast<SgStatementExpression*>(ROSE_MALLOC(SgStatementExpression::pool_size * sizeof(SgStatementExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStatementExpression::pool_size * sizeof(SgStatementExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStatementExpression::pool_size * sizeof(SgStatementExpression));
#endif
        SgStatementExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStatementExpression::pool_size * sizeof(SgStatementExpression), V_SgStatementExpression));

        if (SgStatementExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStatementExpression* blkptr{reinterpret_cast<SgStatementExpression*>(SgStatementExpression::pools[blockIndex-1])};
                blkptr[SgStatementExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStatementExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStatementExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStatementExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStatementExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStatementExpression::pool_size - 1};
    SgStatementExpression* testPointer{reinterpret_cast<SgStatementExpression*>(SgStatementExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStatementExpression::pool_size * (SgStatementExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStatementExpressionStroageClass
 * from its counterpart of SgStatementExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStatementExpression::initializeStorageClassArray(SgStatementExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStatementExpression::pools.begin(); block != SgStatementExpression::pools.end(); ++block) {
        SgStatementExpression* pointer{reinterpret_cast<SgStatementExpression*>(*block)};
        for (unsigned i = 0; i < SgStatementExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 140369 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmOp that takes its corresponding StorageClass as parameter
 */
SgAsmOp::SgAsmOp(const SgAsmOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 140379 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_constraint = storageSource.storageOf_constraint;
    p_modifiers = storageSource.storageOf_modifiers;
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
    p_recordRawAsmOperandDescriptions = storageSource.storageOf_recordRawAsmOperandDescriptions;
    p_isOutputOperand = storageSource.storageOf_isOutputOperand;
    p_constraintString = storageSource.storageOf_constraintString.rebuildDataStoredInEasyStorageClass();
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmOp*
SgAsmOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOp)};
        unsigned long positionInPool{localIndex % SgAsmOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmOp::pool_size};
        returnPointer = &(((SgAsmOp*)(SgAsmOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmOp*
SgAsmOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmOp)};
        unsigned long positionInPool{localIndex % SgAsmOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmOp::pool_size};
        returnPointer = &(((SgAsmOp*)(SgAsmOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmOp::pools.begin(); block != SgAsmOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmOp*>(*block)};
        for (unsigned i = 0; i < SgAsmOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmOp::pools.begin(); block != SgAsmOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmOp*>(*block)};
        for (unsigned i = 0; i < SgAsmOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmOp::clearMemoryPool() {
    SgAsmOp* tempPointer{nullptr};
    if (SgAsmOp::pools.empty() == false) {
        auto block{SgAsmOp::pools.begin()};
        SgAsmOp::next_node = reinterpret_cast<SgAsmOp*>(*block);
        for (; block != SgAsmOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmOp::pool_size-1]);
        }
    }
}

void
SgAsmOp::deleteMemoryPool() {
    for (auto p: SgAsmOp::pools) {
        ROSE_FREE(p);
    }
    SgAsmOp::next_node = nullptr;
    SgAsmOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmOp)};
    for (size_t blockIndex = SgAsmOp::pools.size(); (blockIndex * SgAsmOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmOp) = %" PRIuPTR " SgAsmOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmOp),SgAsmOp::pool_size);
        }
#endif

        SgAsmOp* pointer{static_cast<SgAsmOp*>(ROSE_MALLOC(SgAsmOp::pool_size * sizeof(SgAsmOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmOp::pool_size * sizeof(SgAsmOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmOp::pool_size * sizeof(SgAsmOp));
#endif
        SgAsmOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmOp::pool_size * sizeof(SgAsmOp), V_SgAsmOp));

        if (SgAsmOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmOp* blkptr{reinterpret_cast<SgAsmOp*>(SgAsmOp::pools[blockIndex-1])};
                blkptr[SgAsmOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmOp::pool_size - 1};
    SgAsmOp* testPointer{reinterpret_cast<SgAsmOp*>(SgAsmOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmOp::pool_size * (SgAsmOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmOpStroageClass
 * from its counterpart of SgAsmOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmOp::initializeStorageClassArray(SgAsmOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmOp::pools.begin(); block != SgAsmOp::pools.end(); ++block) {
        SgAsmOp* pointer{reinterpret_cast<SgAsmOp*>(*block)};
        for (unsigned i = 0; i < SgAsmOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 140603 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCudaKernelExecConfig that takes its corresponding StorageClass as parameter
 */
SgCudaKernelExecConfig::SgCudaKernelExecConfig(const SgCudaKernelExecConfigStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 140613 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_grid = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_grid));
     p_blocks = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_blocks));
     p_shared = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_shared));
     p_stream = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_stream));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCudaKernelExecConfig*
SgCudaKernelExecConfig::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCudaKernelExecConfig* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCudaKernelExecConfig)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCudaKernelExecConfig + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCudaKernelExecConfig)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelExecConfig)};
        unsigned long positionInPool{localIndex % SgCudaKernelExecConfig::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCudaKernelExecConfig::pool_size};
        returnPointer = &(((SgCudaKernelExecConfig*)(SgCudaKernelExecConfig::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCudaKernelExecConfig*
SgCudaKernelExecConfig::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCudaKernelExecConfig* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCudaKernelExecConfig)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCudaKernelExecConfig + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCudaKernelExecConfig)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCudaKernelExecConfig)};
        unsigned long positionInPool{localIndex % SgCudaKernelExecConfig::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCudaKernelExecConfig::pool_size};
        returnPointer = &(((SgCudaKernelExecConfig*)(SgCudaKernelExecConfig::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCudaKernelExecConfig::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCudaKernelExecConfig::pools.begin(); block != SgCudaKernelExecConfig::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCudaKernelExecConfig*>(*block)};
        for (unsigned i = 0; i < SgCudaKernelExecConfig::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCudaKernelExecConfig*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCudaKernelExecConfig::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCudaKernelExecConfig* pointerOfLinkedList{nullptr};
    for (auto block = SgCudaKernelExecConfig::pools.begin(); block != SgCudaKernelExecConfig::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCudaKernelExecConfig*>(*block)};
        for (unsigned i = 0; i < SgCudaKernelExecConfig::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCudaKernelExecConfig::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCudaKernelExecConfig::clearMemoryPool() {
    SgCudaKernelExecConfig* tempPointer{nullptr};
    if (SgCudaKernelExecConfig::pools.empty() == false) {
        auto block{SgCudaKernelExecConfig::pools.begin()};
        SgCudaKernelExecConfig::next_node = reinterpret_cast<SgCudaKernelExecConfig*>(*block);
        for (; block != SgCudaKernelExecConfig::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCudaKernelExecConfig*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCudaKernelExecConfig::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCudaKernelExecConfig::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCudaKernelExecConfig::pool_size-1]);
        }
    }
}

void
SgCudaKernelExecConfig::deleteMemoryPool() {
    for (auto p: SgCudaKernelExecConfig::pools) {
        ROSE_FREE(p);
    }
    SgCudaKernelExecConfig::next_node = nullptr;
    SgCudaKernelExecConfig::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCudaKernelExecConfig::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelExecConfig) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCudaKernelExecConfig)};
    for (size_t blockIndex = SgCudaKernelExecConfig::pools.size(); (blockIndex * SgCudaKernelExecConfig::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelExecConfig) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCudaKernelExecConfig) = %" PRIuPTR " SgCudaKernelExecConfig::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCudaKernelExecConfig),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCudaKernelExecConfig),SgCudaKernelExecConfig::pool_size);
        }
#endif

        SgCudaKernelExecConfig* pointer{static_cast<SgCudaKernelExecConfig*>(ROSE_MALLOC(SgCudaKernelExecConfig::pool_size * sizeof(SgCudaKernelExecConfig)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCudaKernelExecConfig::pool_size * sizeof(SgCudaKernelExecConfig));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCudaKernelExecConfig::pool_size * sizeof(SgCudaKernelExecConfig));
#endif
        SgCudaKernelExecConfig::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCudaKernelExecConfig::pool_size * sizeof(SgCudaKernelExecConfig), V_SgCudaKernelExecConfig));

        if (SgCudaKernelExecConfig::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCudaKernelExecConfig* blkptr{reinterpret_cast<SgCudaKernelExecConfig*>(SgCudaKernelExecConfig::pools[blockIndex-1])};
                blkptr[SgCudaKernelExecConfig::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCudaKernelExecConfig::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCudaKernelExecConfig::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCudaKernelExecConfig::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCudaKernelExecConfig::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCudaKernelExecConfig::pool_size - 1};
    SgCudaKernelExecConfig* testPointer{reinterpret_cast<SgCudaKernelExecConfig*>(SgCudaKernelExecConfig::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCudaKernelExecConfig::pool_size * (SgCudaKernelExecConfig::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCudaKernelExecConfigStroageClass
 * from its counterpart of SgCudaKernelExecConfig. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCudaKernelExecConfig::initializeStorageClassArray(SgCudaKernelExecConfigStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCudaKernelExecConfig::pools.begin(); block != SgCudaKernelExecConfig::pools.end(); ++block) {
        SgCudaKernelExecConfig* pointer{reinterpret_cast<SgCudaKernelExecConfig*>(*block)};
        for (unsigned i = 0; i < SgCudaKernelExecConfig::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 140834 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgUnknownArrayOrFunctionReference that takes its corresponding StorageClass as parameter
 */
SgUnknownArrayOrFunctionReference::SgUnknownArrayOrFunctionReference(const SgUnknownArrayOrFunctionReferenceStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 140844 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
     p_named_reference = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_named_reference));
     p_expression_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgUnknownArrayOrFunctionReference*
SgUnknownArrayOrFunctionReference::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgUnknownArrayOrFunctionReference* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnknownArrayOrFunctionReference)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnknownArrayOrFunctionReference + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgUnknownArrayOrFunctionReference)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownArrayOrFunctionReference)};
        unsigned long positionInPool{localIndex % SgUnknownArrayOrFunctionReference::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnknownArrayOrFunctionReference::pool_size};
        returnPointer = &(((SgUnknownArrayOrFunctionReference*)(SgUnknownArrayOrFunctionReference::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgUnknownArrayOrFunctionReference*
SgUnknownArrayOrFunctionReference::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgUnknownArrayOrFunctionReference* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnknownArrayOrFunctionReference)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnknownArrayOrFunctionReference + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgUnknownArrayOrFunctionReference)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgUnknownArrayOrFunctionReference)};
        unsigned long positionInPool{localIndex % SgUnknownArrayOrFunctionReference::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgUnknownArrayOrFunctionReference::pool_size};
        returnPointer = &(((SgUnknownArrayOrFunctionReference*)(SgUnknownArrayOrFunctionReference::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgUnknownArrayOrFunctionReference::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgUnknownArrayOrFunctionReference::pools.begin(); block != SgUnknownArrayOrFunctionReference::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnknownArrayOrFunctionReference*>(*block)};
        for (unsigned i = 0; i < SgUnknownArrayOrFunctionReference::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgUnknownArrayOrFunctionReference*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgUnknownArrayOrFunctionReference::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgUnknownArrayOrFunctionReference* pointerOfLinkedList{nullptr};
    for (auto block = SgUnknownArrayOrFunctionReference::pools.begin(); block != SgUnknownArrayOrFunctionReference::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgUnknownArrayOrFunctionReference*>(*block)};
        for (unsigned i = 0; i < SgUnknownArrayOrFunctionReference::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgUnknownArrayOrFunctionReference::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgUnknownArrayOrFunctionReference::clearMemoryPool() {
    SgUnknownArrayOrFunctionReference* tempPointer{nullptr};
    if (SgUnknownArrayOrFunctionReference::pools.empty() == false) {
        auto block{SgUnknownArrayOrFunctionReference::pools.begin()};
        SgUnknownArrayOrFunctionReference::next_node = reinterpret_cast<SgUnknownArrayOrFunctionReference*>(*block);
        for (; block != SgUnknownArrayOrFunctionReference::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgUnknownArrayOrFunctionReference*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgUnknownArrayOrFunctionReference::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgUnknownArrayOrFunctionReference::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgUnknownArrayOrFunctionReference::pool_size-1]);
        }
    }
}

void
SgUnknownArrayOrFunctionReference::deleteMemoryPool() {
    for (auto p: SgUnknownArrayOrFunctionReference::pools) {
        ROSE_FREE(p);
    }
    SgUnknownArrayOrFunctionReference::next_node = nullptr;
    SgUnknownArrayOrFunctionReference::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgUnknownArrayOrFunctionReference::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownArrayOrFunctionReference) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnknownArrayOrFunctionReference)};
    for (size_t blockIndex = SgUnknownArrayOrFunctionReference::pools.size(); (blockIndex * SgUnknownArrayOrFunctionReference::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownArrayOrFunctionReference) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnknownArrayOrFunctionReference) = %" PRIuPTR " SgUnknownArrayOrFunctionReference::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgUnknownArrayOrFunctionReference),AST_FILE_IO::getPoolSizeOfNewAst(V_SgUnknownArrayOrFunctionReference),SgUnknownArrayOrFunctionReference::pool_size);
        }
#endif

        SgUnknownArrayOrFunctionReference* pointer{static_cast<SgUnknownArrayOrFunctionReference*>(ROSE_MALLOC(SgUnknownArrayOrFunctionReference::pool_size * sizeof(SgUnknownArrayOrFunctionReference)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgUnknownArrayOrFunctionReference::pool_size * sizeof(SgUnknownArrayOrFunctionReference));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgUnknownArrayOrFunctionReference::pool_size * sizeof(SgUnknownArrayOrFunctionReference));
#endif
        SgUnknownArrayOrFunctionReference::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgUnknownArrayOrFunctionReference::pool_size * sizeof(SgUnknownArrayOrFunctionReference), V_SgUnknownArrayOrFunctionReference));

        if (SgUnknownArrayOrFunctionReference::next_node != nullptr) {
            if (blockIndex > 0) {
                SgUnknownArrayOrFunctionReference* blkptr{reinterpret_cast<SgUnknownArrayOrFunctionReference*>(SgUnknownArrayOrFunctionReference::pools[blockIndex-1])};
                blkptr[SgUnknownArrayOrFunctionReference::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgUnknownArrayOrFunctionReference::next_node = pointer;
        }

        for (unsigned i = 0; i < SgUnknownArrayOrFunctionReference::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgUnknownArrayOrFunctionReference::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgUnknownArrayOrFunctionReference::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgUnknownArrayOrFunctionReference::pool_size - 1};
    SgUnknownArrayOrFunctionReference* testPointer{reinterpret_cast<SgUnknownArrayOrFunctionReference*>(SgUnknownArrayOrFunctionReference::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgUnknownArrayOrFunctionReference::pool_size * (SgUnknownArrayOrFunctionReference::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgUnknownArrayOrFunctionReferenceStroageClass
 * from its counterpart of SgUnknownArrayOrFunctionReference. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgUnknownArrayOrFunctionReference::initializeStorageClassArray(SgUnknownArrayOrFunctionReferenceStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgUnknownArrayOrFunctionReference::pools.begin(); block != SgUnknownArrayOrFunctionReference::pools.end(); ++block) {
        SgUnknownArrayOrFunctionReference* pointer{reinterpret_cast<SgUnknownArrayOrFunctionReference*>(*block)};
        for (unsigned i = 0; i < SgUnknownArrayOrFunctionReference::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 141064 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgPseudoDestructorRefExp that takes its corresponding StorageClass as parameter
 */
SgPseudoDestructorRefExp::SgPseudoDestructorRefExp(const SgPseudoDestructorRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 141074 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_object_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_object_type));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgPseudoDestructorRefExp*
SgPseudoDestructorRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgPseudoDestructorRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPseudoDestructorRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPseudoDestructorRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgPseudoDestructorRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgPseudoDestructorRefExp)};
        unsigned long positionInPool{localIndex % SgPseudoDestructorRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPseudoDestructorRefExp::pool_size};
        returnPointer = &(((SgPseudoDestructorRefExp*)(SgPseudoDestructorRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgPseudoDestructorRefExp*
SgPseudoDestructorRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgPseudoDestructorRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPseudoDestructorRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPseudoDestructorRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgPseudoDestructorRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgPseudoDestructorRefExp)};
        unsigned long positionInPool{localIndex % SgPseudoDestructorRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgPseudoDestructorRefExp::pool_size};
        returnPointer = &(((SgPseudoDestructorRefExp*)(SgPseudoDestructorRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgPseudoDestructorRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgPseudoDestructorRefExp::pools.begin(); block != SgPseudoDestructorRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPseudoDestructorRefExp*>(*block)};
        for (unsigned i = 0; i < SgPseudoDestructorRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgPseudoDestructorRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgPseudoDestructorRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgPseudoDestructorRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgPseudoDestructorRefExp::pools.begin(); block != SgPseudoDestructorRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgPseudoDestructorRefExp*>(*block)};
        for (unsigned i = 0; i < SgPseudoDestructorRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgPseudoDestructorRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgPseudoDestructorRefExp::clearMemoryPool() {
    SgPseudoDestructorRefExp* tempPointer{nullptr};
    if (SgPseudoDestructorRefExp::pools.empty() == false) {
        auto block{SgPseudoDestructorRefExp::pools.begin()};
        SgPseudoDestructorRefExp::next_node = reinterpret_cast<SgPseudoDestructorRefExp*>(*block);
        for (; block != SgPseudoDestructorRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgPseudoDestructorRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgPseudoDestructorRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgPseudoDestructorRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgPseudoDestructorRefExp::pool_size-1]);
        }
    }
}

void
SgPseudoDestructorRefExp::deleteMemoryPool() {
    for (auto p: SgPseudoDestructorRefExp::pools) {
        ROSE_FREE(p);
    }
    SgPseudoDestructorRefExp::next_node = nullptr;
    SgPseudoDestructorRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgPseudoDestructorRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgPseudoDestructorRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgPseudoDestructorRefExp)};
    for (size_t blockIndex = SgPseudoDestructorRefExp::pools.size(); (blockIndex * SgPseudoDestructorRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgPseudoDestructorRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgPseudoDestructorRefExp) = %" PRIuPTR " SgPseudoDestructorRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgPseudoDestructorRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgPseudoDestructorRefExp),SgPseudoDestructorRefExp::pool_size);
        }
#endif

        SgPseudoDestructorRefExp* pointer{static_cast<SgPseudoDestructorRefExp*>(ROSE_MALLOC(SgPseudoDestructorRefExp::pool_size * sizeof(SgPseudoDestructorRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgPseudoDestructorRefExp::pool_size * sizeof(SgPseudoDestructorRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgPseudoDestructorRefExp::pool_size * sizeof(SgPseudoDestructorRefExp));
#endif
        SgPseudoDestructorRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgPseudoDestructorRefExp::pool_size * sizeof(SgPseudoDestructorRefExp), V_SgPseudoDestructorRefExp));

        if (SgPseudoDestructorRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgPseudoDestructorRefExp* blkptr{reinterpret_cast<SgPseudoDestructorRefExp*>(SgPseudoDestructorRefExp::pools[blockIndex-1])};
                blkptr[SgPseudoDestructorRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgPseudoDestructorRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgPseudoDestructorRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgPseudoDestructorRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgPseudoDestructorRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgPseudoDestructorRefExp::pool_size - 1};
    SgPseudoDestructorRefExp* testPointer{reinterpret_cast<SgPseudoDestructorRefExp*>(SgPseudoDestructorRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgPseudoDestructorRefExp::pool_size * (SgPseudoDestructorRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgPseudoDestructorRefExpStroageClass
 * from its counterpart of SgPseudoDestructorRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgPseudoDestructorRefExp::initializeStorageClassArray(SgPseudoDestructorRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgPseudoDestructorRefExp::pools.begin(); block != SgPseudoDestructorRefExp::pools.end(); ++block) {
        SgPseudoDestructorRefExp* pointer{reinterpret_cast<SgPseudoDestructorRefExp*>(*block)};
        for (unsigned i = 0; i < SgPseudoDestructorRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 141296 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCAFCoExpression that takes its corresponding StorageClass as parameter
 */
SgCAFCoExpression::SgCAFCoExpression(const SgCAFCoExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 141306 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_teamId = (SgVarRefExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_teamId));
     p_teamRank = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_teamRank));
     p_referData = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_referData));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCAFCoExpression*
SgCAFCoExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCAFCoExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCAFCoExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCAFCoExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCAFCoExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFCoExpression)};
        unsigned long positionInPool{localIndex % SgCAFCoExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCAFCoExpression::pool_size};
        returnPointer = &(((SgCAFCoExpression*)(SgCAFCoExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCAFCoExpression*
SgCAFCoExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCAFCoExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCAFCoExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCAFCoExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCAFCoExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCAFCoExpression)};
        unsigned long positionInPool{localIndex % SgCAFCoExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCAFCoExpression::pool_size};
        returnPointer = &(((SgCAFCoExpression*)(SgCAFCoExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCAFCoExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCAFCoExpression::pools.begin(); block != SgCAFCoExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCAFCoExpression*>(*block)};
        for (unsigned i = 0; i < SgCAFCoExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCAFCoExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCAFCoExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCAFCoExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgCAFCoExpression::pools.begin(); block != SgCAFCoExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCAFCoExpression*>(*block)};
        for (unsigned i = 0; i < SgCAFCoExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCAFCoExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCAFCoExpression::clearMemoryPool() {
    SgCAFCoExpression* tempPointer{nullptr};
    if (SgCAFCoExpression::pools.empty() == false) {
        auto block{SgCAFCoExpression::pools.begin()};
        SgCAFCoExpression::next_node = reinterpret_cast<SgCAFCoExpression*>(*block);
        for (; block != SgCAFCoExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCAFCoExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCAFCoExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCAFCoExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCAFCoExpression::pool_size-1]);
        }
    }
}

void
SgCAFCoExpression::deleteMemoryPool() {
    for (auto p: SgCAFCoExpression::pools) {
        ROSE_FREE(p);
    }
    SgCAFCoExpression::next_node = nullptr;
    SgCAFCoExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCAFCoExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFCoExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCAFCoExpression)};
    for (size_t blockIndex = SgCAFCoExpression::pools.size(); (blockIndex * SgCAFCoExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFCoExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCAFCoExpression) = %" PRIuPTR " SgCAFCoExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCAFCoExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCAFCoExpression),SgCAFCoExpression::pool_size);
        }
#endif

        SgCAFCoExpression* pointer{static_cast<SgCAFCoExpression*>(ROSE_MALLOC(SgCAFCoExpression::pool_size * sizeof(SgCAFCoExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCAFCoExpression::pool_size * sizeof(SgCAFCoExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCAFCoExpression::pool_size * sizeof(SgCAFCoExpression));
#endif
        SgCAFCoExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCAFCoExpression::pool_size * sizeof(SgCAFCoExpression), V_SgCAFCoExpression));

        if (SgCAFCoExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCAFCoExpression* blkptr{reinterpret_cast<SgCAFCoExpression*>(SgCAFCoExpression::pools[blockIndex-1])};
                blkptr[SgCAFCoExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCAFCoExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCAFCoExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCAFCoExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCAFCoExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCAFCoExpression::pool_size - 1};
    SgCAFCoExpression* testPointer{reinterpret_cast<SgCAFCoExpression*>(SgCAFCoExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCAFCoExpression::pool_size * (SgCAFCoExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCAFCoExpressionStroageClass
 * from its counterpart of SgCAFCoExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCAFCoExpression::initializeStorageClassArray(SgCAFCoExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCAFCoExpression::pools.begin(); block != SgCAFCoExpression::pools.end(); ++block) {
        SgCAFCoExpression* pointer{reinterpret_cast<SgCAFCoExpression*>(*block)};
        for (unsigned i = 0; i < SgCAFCoExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 141526 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSizeOfOp that takes its corresponding StorageClass as parameter
 */
SgSizeOfOp::SgSizeOfOp(const SgSizeOfOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 141536 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));
     p_operand_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_type));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_sizeOfContainsBaseTypeDefiningDeclaration = storageSource.storageOf_sizeOfContainsBaseTypeDefiningDeclaration;
    p_is_objectless_nonstatic_data_member_reference = storageSource.storageOf_is_objectless_nonstatic_data_member_reference;
    p_name_qualification_for_pointer_to_member_class_length = storageSource.storageOf_name_qualification_for_pointer_to_member_class_length;
    p_type_elaboration_for_pointer_to_member_class_required = storageSource.storageOf_type_elaboration_for_pointer_to_member_class_required;
    p_global_qualification_for_pointer_to_member_class_required = storageSource.storageOf_global_qualification_for_pointer_to_member_class_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSizeOfOp*
SgSizeOfOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSizeOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSizeOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSizeOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSizeOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSizeOfOp)};
        unsigned long positionInPool{localIndex % SgSizeOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSizeOfOp::pool_size};
        returnPointer = &(((SgSizeOfOp*)(SgSizeOfOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSizeOfOp*
SgSizeOfOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSizeOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSizeOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSizeOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSizeOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSizeOfOp)};
        unsigned long positionInPool{localIndex % SgSizeOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSizeOfOp::pool_size};
        returnPointer = &(((SgSizeOfOp*)(SgSizeOfOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSizeOfOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSizeOfOp::pools.begin(); block != SgSizeOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSizeOfOp*>(*block)};
        for (unsigned i = 0; i < SgSizeOfOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSizeOfOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSizeOfOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSizeOfOp* pointerOfLinkedList{nullptr};
    for (auto block = SgSizeOfOp::pools.begin(); block != SgSizeOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSizeOfOp*>(*block)};
        for (unsigned i = 0; i < SgSizeOfOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSizeOfOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSizeOfOp::clearMemoryPool() {
    SgSizeOfOp* tempPointer{nullptr};
    if (SgSizeOfOp::pools.empty() == false) {
        auto block{SgSizeOfOp::pools.begin()};
        SgSizeOfOp::next_node = reinterpret_cast<SgSizeOfOp*>(*block);
        for (; block != SgSizeOfOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSizeOfOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSizeOfOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSizeOfOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSizeOfOp::pool_size-1]);
        }
    }
}

void
SgSizeOfOp::deleteMemoryPool() {
    for (auto p: SgSizeOfOp::pools) {
        ROSE_FREE(p);
    }
    SgSizeOfOp::next_node = nullptr;
    SgSizeOfOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSizeOfOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSizeOfOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSizeOfOp)};
    for (size_t blockIndex = SgSizeOfOp::pools.size(); (blockIndex * SgSizeOfOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSizeOfOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSizeOfOp) = %" PRIuPTR " SgSizeOfOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSizeOfOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSizeOfOp),SgSizeOfOp::pool_size);
        }
#endif

        SgSizeOfOp* pointer{static_cast<SgSizeOfOp*>(ROSE_MALLOC(SgSizeOfOp::pool_size * sizeof(SgSizeOfOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSizeOfOp::pool_size * sizeof(SgSizeOfOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSizeOfOp::pool_size * sizeof(SgSizeOfOp));
#endif
        SgSizeOfOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSizeOfOp::pool_size * sizeof(SgSizeOfOp), V_SgSizeOfOp));

        if (SgSizeOfOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSizeOfOp* blkptr{reinterpret_cast<SgSizeOfOp*>(SgSizeOfOp::pools[blockIndex-1])};
                blkptr[SgSizeOfOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSizeOfOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSizeOfOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSizeOfOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSizeOfOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSizeOfOp::pool_size - 1};
    SgSizeOfOp* testPointer{reinterpret_cast<SgSizeOfOp*>(SgSizeOfOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSizeOfOp::pool_size * (SgSizeOfOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSizeOfOpStroageClass
 * from its counterpart of SgSizeOfOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSizeOfOp::initializeStorageClassArray(SgSizeOfOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSizeOfOp::pools.begin(); block != SgSizeOfOp::pools.end(); ++block) {
        SgSizeOfOp* pointer{reinterpret_cast<SgSizeOfOp*>(*block)};
        for (unsigned i = 0; i < SgSizeOfOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 141764 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLambdaRefExp that takes its corresponding StorageClass as parameter
 */
SgLambdaRefExp::SgLambdaRefExp(const SgLambdaRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 141774 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_functionDeclaration = (SgFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_functionDeclaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLambdaRefExp*
SgLambdaRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLambdaRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaRefExp)};
        unsigned long positionInPool{localIndex % SgLambdaRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaRefExp::pool_size};
        returnPointer = &(((SgLambdaRefExp*)(SgLambdaRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLambdaRefExp*
SgLambdaRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLambdaRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLambdaRefExp)};
        unsigned long positionInPool{localIndex % SgLambdaRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaRefExp::pool_size};
        returnPointer = &(((SgLambdaRefExp*)(SgLambdaRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLambdaRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLambdaRefExp::pools.begin(); block != SgLambdaRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaRefExp*>(*block)};
        for (unsigned i = 0; i < SgLambdaRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLambdaRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLambdaRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLambdaRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgLambdaRefExp::pools.begin(); block != SgLambdaRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaRefExp*>(*block)};
        for (unsigned i = 0; i < SgLambdaRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLambdaRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLambdaRefExp::clearMemoryPool() {
    SgLambdaRefExp* tempPointer{nullptr};
    if (SgLambdaRefExp::pools.empty() == false) {
        auto block{SgLambdaRefExp::pools.begin()};
        SgLambdaRefExp::next_node = reinterpret_cast<SgLambdaRefExp*>(*block);
        for (; block != SgLambdaRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLambdaRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLambdaRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLambdaRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLambdaRefExp::pool_size-1]);
        }
    }
}

void
SgLambdaRefExp::deleteMemoryPool() {
    for (auto p: SgLambdaRefExp::pools) {
        ROSE_FREE(p);
    }
    SgLambdaRefExp::next_node = nullptr;
    SgLambdaRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLambdaRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaRefExp)};
    for (size_t blockIndex = SgLambdaRefExp::pools.size(); (blockIndex * SgLambdaRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaRefExp) = %" PRIuPTR " SgLambdaRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaRefExp),SgLambdaRefExp::pool_size);
        }
#endif

        SgLambdaRefExp* pointer{static_cast<SgLambdaRefExp*>(ROSE_MALLOC(SgLambdaRefExp::pool_size * sizeof(SgLambdaRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLambdaRefExp::pool_size * sizeof(SgLambdaRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLambdaRefExp::pool_size * sizeof(SgLambdaRefExp));
#endif
        SgLambdaRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLambdaRefExp::pool_size * sizeof(SgLambdaRefExp), V_SgLambdaRefExp));

        if (SgLambdaRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLambdaRefExp* blkptr{reinterpret_cast<SgLambdaRefExp*>(SgLambdaRefExp::pools[blockIndex-1])};
                blkptr[SgLambdaRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLambdaRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLambdaRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLambdaRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLambdaRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLambdaRefExp::pool_size - 1};
    SgLambdaRefExp* testPointer{reinterpret_cast<SgLambdaRefExp*>(SgLambdaRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLambdaRefExp::pool_size * (SgLambdaRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLambdaRefExpStroageClass
 * from its counterpart of SgLambdaRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLambdaRefExp::initializeStorageClassArray(SgLambdaRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLambdaRefExp::pools.begin(); block != SgLambdaRefExp::pools.end(); ++block) {
        SgLambdaRefExp* pointer{reinterpret_cast<SgLambdaRefExp*>(*block)};
        for (unsigned i = 0; i < SgLambdaRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 141992 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDictionaryExp that takes its corresponding StorageClass as parameter
 */
SgDictionaryExp::SgDictionaryExp(const SgDictionaryExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 142002 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_key_datum_pairs = storageSource.storageOf_key_datum_pairs.rebuildDataStoredInEasyStorageClass();
    SgKeyDatumPairPtrList::iterator i_key_datum_pairs = p_key_datum_pairs.begin();
    for ( ; i_key_datum_pairs != p_key_datum_pairs.end(); ++i_key_datum_pairs) {
        (*i_key_datum_pairs) = (SgKeyDatumPairPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_key_datum_pairs)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDictionaryExp*
SgDictionaryExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDictionaryExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDictionaryExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDictionaryExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDictionaryExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryExp)};
        unsigned long positionInPool{localIndex % SgDictionaryExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDictionaryExp::pool_size};
        returnPointer = &(((SgDictionaryExp*)(SgDictionaryExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDictionaryExp*
SgDictionaryExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDictionaryExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDictionaryExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDictionaryExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDictionaryExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDictionaryExp)};
        unsigned long positionInPool{localIndex % SgDictionaryExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDictionaryExp::pool_size};
        returnPointer = &(((SgDictionaryExp*)(SgDictionaryExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDictionaryExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDictionaryExp::pools.begin(); block != SgDictionaryExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDictionaryExp*>(*block)};
        for (unsigned i = 0; i < SgDictionaryExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDictionaryExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDictionaryExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDictionaryExp* pointerOfLinkedList{nullptr};
    for (auto block = SgDictionaryExp::pools.begin(); block != SgDictionaryExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDictionaryExp*>(*block)};
        for (unsigned i = 0; i < SgDictionaryExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDictionaryExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDictionaryExp::clearMemoryPool() {
    SgDictionaryExp* tempPointer{nullptr};
    if (SgDictionaryExp::pools.empty() == false) {
        auto block{SgDictionaryExp::pools.begin()};
        SgDictionaryExp::next_node = reinterpret_cast<SgDictionaryExp*>(*block);
        for (; block != SgDictionaryExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDictionaryExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDictionaryExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDictionaryExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDictionaryExp::pool_size-1]);
        }
    }
}

void
SgDictionaryExp::deleteMemoryPool() {
    for (auto p: SgDictionaryExp::pools) {
        ROSE_FREE(p);
    }
    SgDictionaryExp::next_node = nullptr;
    SgDictionaryExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDictionaryExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDictionaryExp)};
    for (size_t blockIndex = SgDictionaryExp::pools.size(); (blockIndex * SgDictionaryExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDictionaryExp) = %" PRIuPTR " SgDictionaryExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDictionaryExp),SgDictionaryExp::pool_size);
        }
#endif

        SgDictionaryExp* pointer{static_cast<SgDictionaryExp*>(ROSE_MALLOC(SgDictionaryExp::pool_size * sizeof(SgDictionaryExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDictionaryExp::pool_size * sizeof(SgDictionaryExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDictionaryExp::pool_size * sizeof(SgDictionaryExp));
#endif
        SgDictionaryExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDictionaryExp::pool_size * sizeof(SgDictionaryExp), V_SgDictionaryExp));

        if (SgDictionaryExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDictionaryExp* blkptr{reinterpret_cast<SgDictionaryExp*>(SgDictionaryExp::pools[blockIndex-1])};
                blkptr[SgDictionaryExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDictionaryExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDictionaryExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDictionaryExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDictionaryExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDictionaryExp::pool_size - 1};
    SgDictionaryExp* testPointer{reinterpret_cast<SgDictionaryExp*>(SgDictionaryExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDictionaryExp::pool_size * (SgDictionaryExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDictionaryExpStroageClass
 * from its counterpart of SgDictionaryExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDictionaryExp::initializeStorageClassArray(SgDictionaryExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDictionaryExp::pools.begin(); block != SgDictionaryExp::pools.end(); ++block) {
        SgDictionaryExp* pointer{reinterpret_cast<SgDictionaryExp*>(*block)};
        for (unsigned i = 0; i < SgDictionaryExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 142224 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgKeyDatumPair that takes its corresponding StorageClass as parameter
 */
SgKeyDatumPair::SgKeyDatumPair(const SgKeyDatumPairStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 142234 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_key = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_key));
     p_datum = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_datum));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgKeyDatumPair*
SgKeyDatumPair::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgKeyDatumPair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgKeyDatumPair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgKeyDatumPair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgKeyDatumPair)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgKeyDatumPair)};
        unsigned long positionInPool{localIndex % SgKeyDatumPair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgKeyDatumPair::pool_size};
        returnPointer = &(((SgKeyDatumPair*)(SgKeyDatumPair::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgKeyDatumPair*
SgKeyDatumPair::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgKeyDatumPair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgKeyDatumPair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgKeyDatumPair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgKeyDatumPair)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgKeyDatumPair)};
        unsigned long positionInPool{localIndex % SgKeyDatumPair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgKeyDatumPair::pool_size};
        returnPointer = &(((SgKeyDatumPair*)(SgKeyDatumPair::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgKeyDatumPair::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgKeyDatumPair::pools.begin(); block != SgKeyDatumPair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgKeyDatumPair*>(*block)};
        for (unsigned i = 0; i < SgKeyDatumPair::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgKeyDatumPair*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgKeyDatumPair::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgKeyDatumPair* pointerOfLinkedList{nullptr};
    for (auto block = SgKeyDatumPair::pools.begin(); block != SgKeyDatumPair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgKeyDatumPair*>(*block)};
        for (unsigned i = 0; i < SgKeyDatumPair::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgKeyDatumPair::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgKeyDatumPair::clearMemoryPool() {
    SgKeyDatumPair* tempPointer{nullptr};
    if (SgKeyDatumPair::pools.empty() == false) {
        auto block{SgKeyDatumPair::pools.begin()};
        SgKeyDatumPair::next_node = reinterpret_cast<SgKeyDatumPair*>(*block);
        for (; block != SgKeyDatumPair::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgKeyDatumPair*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgKeyDatumPair::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgKeyDatumPair::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgKeyDatumPair::pool_size-1]);
        }
    }
}

void
SgKeyDatumPair::deleteMemoryPool() {
    for (auto p: SgKeyDatumPair::pools) {
        ROSE_FREE(p);
    }
    SgKeyDatumPair::next_node = nullptr;
    SgKeyDatumPair::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgKeyDatumPair::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgKeyDatumPair) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgKeyDatumPair)};
    for (size_t blockIndex = SgKeyDatumPair::pools.size(); (blockIndex * SgKeyDatumPair::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgKeyDatumPair) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgKeyDatumPair) = %" PRIuPTR " SgKeyDatumPair::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgKeyDatumPair),AST_FILE_IO::getPoolSizeOfNewAst(V_SgKeyDatumPair),SgKeyDatumPair::pool_size);
        }
#endif

        SgKeyDatumPair* pointer{static_cast<SgKeyDatumPair*>(ROSE_MALLOC(SgKeyDatumPair::pool_size * sizeof(SgKeyDatumPair)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgKeyDatumPair::pool_size * sizeof(SgKeyDatumPair));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgKeyDatumPair::pool_size * sizeof(SgKeyDatumPair));
#endif
        SgKeyDatumPair::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgKeyDatumPair::pool_size * sizeof(SgKeyDatumPair), V_SgKeyDatumPair));

        if (SgKeyDatumPair::next_node != nullptr) {
            if (blockIndex > 0) {
                SgKeyDatumPair* blkptr{reinterpret_cast<SgKeyDatumPair*>(SgKeyDatumPair::pools[blockIndex-1])};
                blkptr[SgKeyDatumPair::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgKeyDatumPair::next_node = pointer;
        }

        for (unsigned i = 0; i < SgKeyDatumPair::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgKeyDatumPair::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgKeyDatumPair::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgKeyDatumPair::pool_size - 1};
    SgKeyDatumPair* testPointer{reinterpret_cast<SgKeyDatumPair*>(SgKeyDatumPair::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgKeyDatumPair::pool_size * (SgKeyDatumPair::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgKeyDatumPairStroageClass
 * from its counterpart of SgKeyDatumPair. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgKeyDatumPair::initializeStorageClassArray(SgKeyDatumPairStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgKeyDatumPair::pools.begin(); block != SgKeyDatumPair::pools.end(); ++block) {
        SgKeyDatumPair* pointer{reinterpret_cast<SgKeyDatumPair*>(*block)};
        for (unsigned i = 0; i < SgKeyDatumPair::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 142453 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgReferenceExp that takes its corresponding StorageClass as parameter
 */
SgReferenceExp::SgReferenceExp(const SgReferenceExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 142463 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgReferenceExp*
SgReferenceExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgReferenceExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReferenceExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReferenceExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgReferenceExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceExp)};
        unsigned long positionInPool{localIndex % SgReferenceExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReferenceExp::pool_size};
        returnPointer = &(((SgReferenceExp*)(SgReferenceExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgReferenceExp*
SgReferenceExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgReferenceExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReferenceExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReferenceExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgReferenceExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgReferenceExp)};
        unsigned long positionInPool{localIndex % SgReferenceExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgReferenceExp::pool_size};
        returnPointer = &(((SgReferenceExp*)(SgReferenceExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgReferenceExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgReferenceExp::pools.begin(); block != SgReferenceExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReferenceExp*>(*block)};
        for (unsigned i = 0; i < SgReferenceExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgReferenceExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgReferenceExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgReferenceExp* pointerOfLinkedList{nullptr};
    for (auto block = SgReferenceExp::pools.begin(); block != SgReferenceExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgReferenceExp*>(*block)};
        for (unsigned i = 0; i < SgReferenceExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgReferenceExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgReferenceExp::clearMemoryPool() {
    SgReferenceExp* tempPointer{nullptr};
    if (SgReferenceExp::pools.empty() == false) {
        auto block{SgReferenceExp::pools.begin()};
        SgReferenceExp::next_node = reinterpret_cast<SgReferenceExp*>(*block);
        for (; block != SgReferenceExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgReferenceExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgReferenceExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgReferenceExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgReferenceExp::pool_size-1]);
        }
    }
}

void
SgReferenceExp::deleteMemoryPool() {
    for (auto p: SgReferenceExp::pools) {
        ROSE_FREE(p);
    }
    SgReferenceExp::next_node = nullptr;
    SgReferenceExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgReferenceExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgReferenceExp)};
    for (size_t blockIndex = SgReferenceExp::pools.size(); (blockIndex * SgReferenceExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgReferenceExp) = %" PRIuPTR " SgReferenceExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgReferenceExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgReferenceExp),SgReferenceExp::pool_size);
        }
#endif

        SgReferenceExp* pointer{static_cast<SgReferenceExp*>(ROSE_MALLOC(SgReferenceExp::pool_size * sizeof(SgReferenceExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgReferenceExp::pool_size * sizeof(SgReferenceExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgReferenceExp::pool_size * sizeof(SgReferenceExp));
#endif
        SgReferenceExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgReferenceExp::pool_size * sizeof(SgReferenceExp), V_SgReferenceExp));

        if (SgReferenceExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgReferenceExp* blkptr{reinterpret_cast<SgReferenceExp*>(SgReferenceExp::pools[blockIndex-1])};
                blkptr[SgReferenceExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgReferenceExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgReferenceExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgReferenceExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgReferenceExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgReferenceExp::pool_size - 1};
    SgReferenceExp* testPointer{reinterpret_cast<SgReferenceExp*>(SgReferenceExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgReferenceExp::pool_size * (SgReferenceExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgReferenceExpStroageClass
 * from its counterpart of SgReferenceExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgReferenceExp::initializeStorageClassArray(SgReferenceExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgReferenceExp::pools.begin(); block != SgReferenceExp::pools.end(); ++block) {
        SgReferenceExp* pointer{reinterpret_cast<SgReferenceExp*>(*block)};
        for (unsigned i = 0; i < SgReferenceExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 142680 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVarRefExp that takes its corresponding StorageClass as parameter
 */
SgVarRefExp::SgVarRefExp(const SgVarRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 142690 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgVariableSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));
     p_originalExpressionTree = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_originalExpressionTree));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVarRefExp*
SgVarRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVarRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVarRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVarRefExp)};
        unsigned long positionInPool{localIndex % SgVarRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarRefExp::pool_size};
        returnPointer = &(((SgVarRefExp*)(SgVarRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVarRefExp*
SgVarRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVarRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVarRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVarRefExp)};
        unsigned long positionInPool{localIndex % SgVarRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVarRefExp::pool_size};
        returnPointer = &(((SgVarRefExp*)(SgVarRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVarRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVarRefExp::pools.begin(); block != SgVarRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarRefExp*>(*block)};
        for (unsigned i = 0; i < SgVarRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVarRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVarRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVarRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgVarRefExp::pools.begin(); block != SgVarRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVarRefExp*>(*block)};
        for (unsigned i = 0; i < SgVarRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVarRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVarRefExp::clearMemoryPool() {
    SgVarRefExp* tempPointer{nullptr};
    if (SgVarRefExp::pools.empty() == false) {
        auto block{SgVarRefExp::pools.begin()};
        SgVarRefExp::next_node = reinterpret_cast<SgVarRefExp*>(*block);
        for (; block != SgVarRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVarRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVarRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVarRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVarRefExp::pool_size-1]);
        }
    }
}

void
SgVarRefExp::deleteMemoryPool() {
    for (auto p: SgVarRefExp::pools) {
        ROSE_FREE(p);
    }
    SgVarRefExp::next_node = nullptr;
    SgVarRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVarRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVarRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarRefExp)};
    for (size_t blockIndex = SgVarRefExp::pools.size(); (blockIndex * SgVarRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVarRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarRefExp) = %" PRIuPTR " SgVarRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVarRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVarRefExp),SgVarRefExp::pool_size);
        }
#endif

        SgVarRefExp* pointer{static_cast<SgVarRefExp*>(ROSE_MALLOC(SgVarRefExp::pool_size * sizeof(SgVarRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVarRefExp::pool_size * sizeof(SgVarRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVarRefExp::pool_size * sizeof(SgVarRefExp));
#endif
        SgVarRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVarRefExp::pool_size * sizeof(SgVarRefExp), V_SgVarRefExp));

        if (SgVarRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVarRefExp* blkptr{reinterpret_cast<SgVarRefExp*>(SgVarRefExp::pools[blockIndex-1])};
                blkptr[SgVarRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVarRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVarRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVarRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVarRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVarRefExp::pool_size - 1};
    SgVarRefExp* testPointer{reinterpret_cast<SgVarRefExp*>(SgVarRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVarRefExp::pool_size * (SgVarRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVarRefExpStroageClass
 * from its counterpart of SgVarRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVarRefExp::initializeStorageClassArray(SgVarRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVarRefExp::pools.begin(); block != SgVarRefExp::pools.end(); ++block) {
        SgVarRefExp* pointer{reinterpret_cast<SgVarRefExp*>(*block)};
        for (unsigned i = 0; i < SgVarRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 142912 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLabelRefExp that takes its corresponding StorageClass as parameter
 */
SgLabelRefExp::SgLabelRefExp(const SgLabelRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 142922 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgLabelSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLabelRefExp*
SgLabelRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLabelRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelRefExp)};
        unsigned long positionInPool{localIndex % SgLabelRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLabelRefExp::pool_size};
        returnPointer = &(((SgLabelRefExp*)(SgLabelRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLabelRefExp*
SgLabelRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLabelRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLabelRefExp)};
        unsigned long positionInPool{localIndex % SgLabelRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLabelRefExp::pool_size};
        returnPointer = &(((SgLabelRefExp*)(SgLabelRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLabelRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLabelRefExp::pools.begin(); block != SgLabelRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLabelRefExp*>(*block)};
        for (unsigned i = 0; i < SgLabelRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLabelRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLabelRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLabelRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgLabelRefExp::pools.begin(); block != SgLabelRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLabelRefExp*>(*block)};
        for (unsigned i = 0; i < SgLabelRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLabelRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLabelRefExp::clearMemoryPool() {
    SgLabelRefExp* tempPointer{nullptr};
    if (SgLabelRefExp::pools.empty() == false) {
        auto block{SgLabelRefExp::pools.begin()};
        SgLabelRefExp::next_node = reinterpret_cast<SgLabelRefExp*>(*block);
        for (; block != SgLabelRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLabelRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLabelRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLabelRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLabelRefExp::pool_size-1]);
        }
    }
}

void
SgLabelRefExp::deleteMemoryPool() {
    for (auto p: SgLabelRefExp::pools) {
        ROSE_FREE(p);
    }
    SgLabelRefExp::next_node = nullptr;
    SgLabelRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLabelRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelRefExp)};
    for (size_t blockIndex = SgLabelRefExp::pools.size(); (blockIndex * SgLabelRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelRefExp) = %" PRIuPTR " SgLabelRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelRefExp),SgLabelRefExp::pool_size);
        }
#endif

        SgLabelRefExp* pointer{static_cast<SgLabelRefExp*>(ROSE_MALLOC(SgLabelRefExp::pool_size * sizeof(SgLabelRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLabelRefExp::pool_size * sizeof(SgLabelRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLabelRefExp::pool_size * sizeof(SgLabelRefExp));
#endif
        SgLabelRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLabelRefExp::pool_size * sizeof(SgLabelRefExp), V_SgLabelRefExp));

        if (SgLabelRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLabelRefExp* blkptr{reinterpret_cast<SgLabelRefExp*>(SgLabelRefExp::pools[blockIndex-1])};
                blkptr[SgLabelRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLabelRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLabelRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLabelRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLabelRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLabelRefExp::pool_size - 1};
    SgLabelRefExp* testPointer{reinterpret_cast<SgLabelRefExp*>(SgLabelRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLabelRefExp::pool_size * (SgLabelRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLabelRefExpStroageClass
 * from its counterpart of SgLabelRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLabelRefExp::initializeStorageClassArray(SgLabelRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLabelRefExp::pools.begin(); block != SgLabelRefExp::pools.end(); ++block) {
        SgLabelRefExp* pointer{reinterpret_cast<SgLabelRefExp*>(*block)};
        for (unsigned i = 0; i < SgLabelRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 143140 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClassNameRefExp that takes its corresponding StorageClass as parameter
 */
SgClassNameRefExp::SgClassNameRefExp(const SgClassNameRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 143150 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgClassSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClassNameRefExp*
SgClassNameRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClassNameRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassNameRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassNameRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassNameRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClassNameRefExp)};
        unsigned long positionInPool{localIndex % SgClassNameRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassNameRefExp::pool_size};
        returnPointer = &(((SgClassNameRefExp*)(SgClassNameRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClassNameRefExp*
SgClassNameRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClassNameRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassNameRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassNameRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassNameRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClassNameRefExp)};
        unsigned long positionInPool{localIndex % SgClassNameRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassNameRefExp::pool_size};
        returnPointer = &(((SgClassNameRefExp*)(SgClassNameRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClassNameRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClassNameRefExp::pools.begin(); block != SgClassNameRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassNameRefExp*>(*block)};
        for (unsigned i = 0; i < SgClassNameRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClassNameRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClassNameRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClassNameRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgClassNameRefExp::pools.begin(); block != SgClassNameRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassNameRefExp*>(*block)};
        for (unsigned i = 0; i < SgClassNameRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClassNameRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClassNameRefExp::clearMemoryPool() {
    SgClassNameRefExp* tempPointer{nullptr};
    if (SgClassNameRefExp::pools.empty() == false) {
        auto block{SgClassNameRefExp::pools.begin()};
        SgClassNameRefExp::next_node = reinterpret_cast<SgClassNameRefExp*>(*block);
        for (; block != SgClassNameRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClassNameRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClassNameRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClassNameRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClassNameRefExp::pool_size-1]);
        }
    }
}

void
SgClassNameRefExp::deleteMemoryPool() {
    for (auto p: SgClassNameRefExp::pools) {
        ROSE_FREE(p);
    }
    SgClassNameRefExp::next_node = nullptr;
    SgClassNameRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClassNameRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClassNameRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassNameRefExp)};
    for (size_t blockIndex = SgClassNameRefExp::pools.size(); (blockIndex * SgClassNameRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClassNameRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassNameRefExp) = %" PRIuPTR " SgClassNameRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClassNameRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassNameRefExp),SgClassNameRefExp::pool_size);
        }
#endif

        SgClassNameRefExp* pointer{static_cast<SgClassNameRefExp*>(ROSE_MALLOC(SgClassNameRefExp::pool_size * sizeof(SgClassNameRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClassNameRefExp::pool_size * sizeof(SgClassNameRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClassNameRefExp::pool_size * sizeof(SgClassNameRefExp));
#endif
        SgClassNameRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClassNameRefExp::pool_size * sizeof(SgClassNameRefExp), V_SgClassNameRefExp));

        if (SgClassNameRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClassNameRefExp* blkptr{reinterpret_cast<SgClassNameRefExp*>(SgClassNameRefExp::pools[blockIndex-1])};
                blkptr[SgClassNameRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClassNameRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClassNameRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClassNameRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClassNameRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClassNameRefExp::pool_size - 1};
    SgClassNameRefExp* testPointer{reinterpret_cast<SgClassNameRefExp*>(SgClassNameRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClassNameRefExp::pool_size * (SgClassNameRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClassNameRefExpStroageClass
 * from its counterpart of SgClassNameRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClassNameRefExp::initializeStorageClassArray(SgClassNameRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClassNameRefExp::pools.begin(); block != SgClassNameRefExp::pools.end(); ++block) {
        SgClassNameRefExp* pointer{reinterpret_cast<SgClassNameRefExp*>(*block)};
        for (unsigned i = 0; i < SgClassNameRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 143368 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNonrealRefExp that takes its corresponding StorageClass as parameter
 */
SgNonrealRefExp::SgNonrealRefExp(const SgNonrealRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 143378 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgNonrealSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNonrealRefExp*
SgNonrealRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNonrealRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealRefExp)};
        unsigned long positionInPool{localIndex % SgNonrealRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealRefExp::pool_size};
        returnPointer = &(((SgNonrealRefExp*)(SgNonrealRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNonrealRefExp*
SgNonrealRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNonrealRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNonrealRefExp)};
        unsigned long positionInPool{localIndex % SgNonrealRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealRefExp::pool_size};
        returnPointer = &(((SgNonrealRefExp*)(SgNonrealRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNonrealRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNonrealRefExp::pools.begin(); block != SgNonrealRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealRefExp*>(*block)};
        for (unsigned i = 0; i < SgNonrealRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNonrealRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNonrealRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNonrealRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgNonrealRefExp::pools.begin(); block != SgNonrealRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealRefExp*>(*block)};
        for (unsigned i = 0; i < SgNonrealRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNonrealRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNonrealRefExp::clearMemoryPool() {
    SgNonrealRefExp* tempPointer{nullptr};
    if (SgNonrealRefExp::pools.empty() == false) {
        auto block{SgNonrealRefExp::pools.begin()};
        SgNonrealRefExp::next_node = reinterpret_cast<SgNonrealRefExp*>(*block);
        for (; block != SgNonrealRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNonrealRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNonrealRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNonrealRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNonrealRefExp::pool_size-1]);
        }
    }
}

void
SgNonrealRefExp::deleteMemoryPool() {
    for (auto p: SgNonrealRefExp::pools) {
        ROSE_FREE(p);
    }
    SgNonrealRefExp::next_node = nullptr;
    SgNonrealRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNonrealRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealRefExp)};
    for (size_t blockIndex = SgNonrealRefExp::pools.size(); (blockIndex * SgNonrealRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealRefExp) = %" PRIuPTR " SgNonrealRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealRefExp),SgNonrealRefExp::pool_size);
        }
#endif

        SgNonrealRefExp* pointer{static_cast<SgNonrealRefExp*>(ROSE_MALLOC(SgNonrealRefExp::pool_size * sizeof(SgNonrealRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNonrealRefExp::pool_size * sizeof(SgNonrealRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNonrealRefExp::pool_size * sizeof(SgNonrealRefExp));
#endif
        SgNonrealRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNonrealRefExp::pool_size * sizeof(SgNonrealRefExp), V_SgNonrealRefExp));

        if (SgNonrealRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNonrealRefExp* blkptr{reinterpret_cast<SgNonrealRefExp*>(SgNonrealRefExp::pools[blockIndex-1])};
                blkptr[SgNonrealRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNonrealRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNonrealRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNonrealRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNonrealRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNonrealRefExp::pool_size - 1};
    SgNonrealRefExp* testPointer{reinterpret_cast<SgNonrealRefExp*>(SgNonrealRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNonrealRefExp::pool_size * (SgNonrealRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNonrealRefExpStroageClass
 * from its counterpart of SgNonrealRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNonrealRefExp::initializeStorageClassArray(SgNonrealRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNonrealRefExp::pools.begin(); block != SgNonrealRefExp::pools.end(); ++block) {
        SgNonrealRefExp* pointer{reinterpret_cast<SgNonrealRefExp*>(*block)};
        for (unsigned i = 0; i < SgNonrealRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 143599 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgScopedRefExp that takes its corresponding StorageClass as parameter
 */
SgScopedRefExp::SgScopedRefExp(const SgScopedRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 143609 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lhs = (SgReferenceExp * )(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lhs));
     p_rhs = (SgReferenceExp * )(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rhs));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgScopedRefExp*
SgScopedRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgScopedRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopedRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopedRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgScopedRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedRefExp)};
        unsigned long positionInPool{localIndex % SgScopedRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopedRefExp::pool_size};
        returnPointer = &(((SgScopedRefExp*)(SgScopedRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgScopedRefExp*
SgScopedRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgScopedRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopedRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopedRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgScopedRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgScopedRefExp)};
        unsigned long positionInPool{localIndex % SgScopedRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgScopedRefExp::pool_size};
        returnPointer = &(((SgScopedRefExp*)(SgScopedRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgScopedRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgScopedRefExp::pools.begin(); block != SgScopedRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopedRefExp*>(*block)};
        for (unsigned i = 0; i < SgScopedRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgScopedRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgScopedRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgScopedRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgScopedRefExp::pools.begin(); block != SgScopedRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgScopedRefExp*>(*block)};
        for (unsigned i = 0; i < SgScopedRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgScopedRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgScopedRefExp::clearMemoryPool() {
    SgScopedRefExp* tempPointer{nullptr};
    if (SgScopedRefExp::pools.empty() == false) {
        auto block{SgScopedRefExp::pools.begin()};
        SgScopedRefExp::next_node = reinterpret_cast<SgScopedRefExp*>(*block);
        for (; block != SgScopedRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgScopedRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgScopedRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgScopedRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgScopedRefExp::pool_size-1]);
        }
    }
}

void
SgScopedRefExp::deleteMemoryPool() {
    for (auto p: SgScopedRefExp::pools) {
        ROSE_FREE(p);
    }
    SgScopedRefExp::next_node = nullptr;
    SgScopedRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgScopedRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopedRefExp)};
    for (size_t blockIndex = SgScopedRefExp::pools.size(); (blockIndex * SgScopedRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopedRefExp) = %" PRIuPTR " SgScopedRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgScopedRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgScopedRefExp),SgScopedRefExp::pool_size);
        }
#endif

        SgScopedRefExp* pointer{static_cast<SgScopedRefExp*>(ROSE_MALLOC(SgScopedRefExp::pool_size * sizeof(SgScopedRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgScopedRefExp::pool_size * sizeof(SgScopedRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgScopedRefExp::pool_size * sizeof(SgScopedRefExp));
#endif
        SgScopedRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgScopedRefExp::pool_size * sizeof(SgScopedRefExp), V_SgScopedRefExp));

        if (SgScopedRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgScopedRefExp* blkptr{reinterpret_cast<SgScopedRefExp*>(SgScopedRefExp::pools[blockIndex-1])};
                blkptr[SgScopedRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgScopedRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgScopedRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgScopedRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgScopedRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgScopedRefExp::pool_size - 1};
    SgScopedRefExp* testPointer{reinterpret_cast<SgScopedRefExp*>(SgScopedRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgScopedRefExp::pool_size * (SgScopedRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgScopedRefExpStroageClass
 * from its counterpart of SgScopedRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgScopedRefExp::initializeStorageClassArray(SgScopedRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgScopedRefExp::pools.begin(); block != SgScopedRefExp::pools.end(); ++block) {
        SgScopedRefExp* pointer{reinterpret_cast<SgScopedRefExp*>(*block)};
        for (unsigned i = 0; i < SgScopedRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 143828 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeRefExp that takes its corresponding StorageClass as parameter
 */
SgTypeRefExp::SgTypeRefExp(const SgTypeRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 143838 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_named_type = (SgNamedType * )(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_named_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeRefExp*
SgTypeRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeRefExp)};
        unsigned long positionInPool{localIndex % SgTypeRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeRefExp::pool_size};
        returnPointer = &(((SgTypeRefExp*)(SgTypeRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeRefExp*
SgTypeRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeRefExp)};
        unsigned long positionInPool{localIndex % SgTypeRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeRefExp::pool_size};
        returnPointer = &(((SgTypeRefExp*)(SgTypeRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeRefExp::pools.begin(); block != SgTypeRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeRefExp*>(*block)};
        for (unsigned i = 0; i < SgTypeRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeRefExp::pools.begin(); block != SgTypeRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeRefExp*>(*block)};
        for (unsigned i = 0; i < SgTypeRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeRefExp::clearMemoryPool() {
    SgTypeRefExp* tempPointer{nullptr};
    if (SgTypeRefExp::pools.empty() == false) {
        auto block{SgTypeRefExp::pools.begin()};
        SgTypeRefExp::next_node = reinterpret_cast<SgTypeRefExp*>(*block);
        for (; block != SgTypeRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeRefExp::pool_size-1]);
        }
    }
}

void
SgTypeRefExp::deleteMemoryPool() {
    for (auto p: SgTypeRefExp::pools) {
        ROSE_FREE(p);
    }
    SgTypeRefExp::next_node = nullptr;
    SgTypeRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeRefExp)};
    for (size_t blockIndex = SgTypeRefExp::pools.size(); (blockIndex * SgTypeRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeRefExp) = %" PRIuPTR " SgTypeRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeRefExp),SgTypeRefExp::pool_size);
        }
#endif

        SgTypeRefExp* pointer{static_cast<SgTypeRefExp*>(ROSE_MALLOC(SgTypeRefExp::pool_size * sizeof(SgTypeRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeRefExp::pool_size * sizeof(SgTypeRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeRefExp::pool_size * sizeof(SgTypeRefExp));
#endif
        SgTypeRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeRefExp::pool_size * sizeof(SgTypeRefExp), V_SgTypeRefExp));

        if (SgTypeRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeRefExp* blkptr{reinterpret_cast<SgTypeRefExp*>(SgTypeRefExp::pools[blockIndex-1])};
                blkptr[SgTypeRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeRefExp::pool_size - 1};
    SgTypeRefExp* testPointer{reinterpret_cast<SgTypeRefExp*>(SgTypeRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeRefExp::pool_size * (SgTypeRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeRefExpStroageClass
 * from its counterpart of SgTypeRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeRefExp::initializeStorageClassArray(SgTypeRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeRefExp::pools.begin(); block != SgTypeRefExp::pools.end(); ++block) {
        SgTypeRefExp* pointer{reinterpret_cast<SgTypeRefExp*>(*block)};
        for (unsigned i = 0; i < SgTypeRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 144056 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionRefExp that takes its corresponding StorageClass as parameter
 */
SgFunctionRefExp::SgFunctionRefExp(const SgFunctionRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 144066 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol_i = (SgFunctionSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol_i));
     p_function_type = (SgFunctionType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_function_type));
     p_originalExpressionTree = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_originalExpressionTree));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionRefExp*
SgFunctionRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionRefExp::pool_size};
        returnPointer = &(((SgFunctionRefExp*)(SgFunctionRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionRefExp*
SgFunctionRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionRefExp::pool_size};
        returnPointer = &(((SgFunctionRefExp*)(SgFunctionRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionRefExp::pools.begin(); block != SgFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionRefExp::pools.begin(); block != SgFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionRefExp::clearMemoryPool() {
    SgFunctionRefExp* tempPointer{nullptr};
    if (SgFunctionRefExp::pools.empty() == false) {
        auto block{SgFunctionRefExp::pools.begin()};
        SgFunctionRefExp::next_node = reinterpret_cast<SgFunctionRefExp*>(*block);
        for (; block != SgFunctionRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionRefExp::pool_size-1]);
        }
    }
}

void
SgFunctionRefExp::deleteMemoryPool() {
    for (auto p: SgFunctionRefExp::pools) {
        ROSE_FREE(p);
    }
    SgFunctionRefExp::next_node = nullptr;
    SgFunctionRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionRefExp)};
    for (size_t blockIndex = SgFunctionRefExp::pools.size(); (blockIndex * SgFunctionRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionRefExp) = %" PRIuPTR " SgFunctionRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionRefExp),SgFunctionRefExp::pool_size);
        }
#endif

        SgFunctionRefExp* pointer{static_cast<SgFunctionRefExp*>(ROSE_MALLOC(SgFunctionRefExp::pool_size * sizeof(SgFunctionRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionRefExp::pool_size * sizeof(SgFunctionRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionRefExp::pool_size * sizeof(SgFunctionRefExp));
#endif
        SgFunctionRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionRefExp::pool_size * sizeof(SgFunctionRefExp), V_SgFunctionRefExp));

        if (SgFunctionRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionRefExp* blkptr{reinterpret_cast<SgFunctionRefExp*>(SgFunctionRefExp::pools[blockIndex-1])};
                blkptr[SgFunctionRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionRefExp::pool_size - 1};
    SgFunctionRefExp* testPointer{reinterpret_cast<SgFunctionRefExp*>(SgFunctionRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionRefExp::pool_size * (SgFunctionRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionRefExpStroageClass
 * from its counterpart of SgFunctionRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionRefExp::initializeStorageClassArray(SgFunctionRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionRefExp::pools.begin(); block != SgFunctionRefExp::pools.end(); ++block) {
        SgFunctionRefExp* pointer{reinterpret_cast<SgFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 144289 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMemberFunctionRefExp that takes its corresponding StorageClass as parameter
 */
SgMemberFunctionRefExp::SgMemberFunctionRefExp(const SgMemberFunctionRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 144299 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol_i = (SgMemberFunctionSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol_i));
    p_virtual_call = storageSource.storageOf_virtual_call;
     p_function_type = (SgFunctionType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_function_type));
    p_need_qualifier = storageSource.storageOf_need_qualifier;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMemberFunctionRefExp*
SgMemberFunctionRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMemberFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgMemberFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionRefExp::pool_size};
        returnPointer = &(((SgMemberFunctionRefExp*)(SgMemberFunctionRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMemberFunctionRefExp*
SgMemberFunctionRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMemberFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMemberFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgMemberFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionRefExp::pool_size};
        returnPointer = &(((SgMemberFunctionRefExp*)(SgMemberFunctionRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMemberFunctionRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMemberFunctionRefExp::pools.begin(); block != SgMemberFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMemberFunctionRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMemberFunctionRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMemberFunctionRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgMemberFunctionRefExp::pools.begin(); block != SgMemberFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMemberFunctionRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMemberFunctionRefExp::clearMemoryPool() {
    SgMemberFunctionRefExp* tempPointer{nullptr};
    if (SgMemberFunctionRefExp::pools.empty() == false) {
        auto block{SgMemberFunctionRefExp::pools.begin()};
        SgMemberFunctionRefExp::next_node = reinterpret_cast<SgMemberFunctionRefExp*>(*block);
        for (; block != SgMemberFunctionRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMemberFunctionRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMemberFunctionRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMemberFunctionRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMemberFunctionRefExp::pool_size-1]);
        }
    }
}

void
SgMemberFunctionRefExp::deleteMemoryPool() {
    for (auto p: SgMemberFunctionRefExp::pools) {
        ROSE_FREE(p);
    }
    SgMemberFunctionRefExp::next_node = nullptr;
    SgMemberFunctionRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMemberFunctionRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionRefExp)};
    for (size_t blockIndex = SgMemberFunctionRefExp::pools.size(); (blockIndex * SgMemberFunctionRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionRefExp) = %" PRIuPTR " SgMemberFunctionRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionRefExp),SgMemberFunctionRefExp::pool_size);
        }
#endif

        SgMemberFunctionRefExp* pointer{static_cast<SgMemberFunctionRefExp*>(ROSE_MALLOC(SgMemberFunctionRefExp::pool_size * sizeof(SgMemberFunctionRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMemberFunctionRefExp::pool_size * sizeof(SgMemberFunctionRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMemberFunctionRefExp::pool_size * sizeof(SgMemberFunctionRefExp));
#endif
        SgMemberFunctionRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMemberFunctionRefExp::pool_size * sizeof(SgMemberFunctionRefExp), V_SgMemberFunctionRefExp));

        if (SgMemberFunctionRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMemberFunctionRefExp* blkptr{reinterpret_cast<SgMemberFunctionRefExp*>(SgMemberFunctionRefExp::pools[blockIndex-1])};
                blkptr[SgMemberFunctionRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMemberFunctionRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMemberFunctionRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMemberFunctionRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMemberFunctionRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMemberFunctionRefExp::pool_size - 1};
    SgMemberFunctionRefExp* testPointer{reinterpret_cast<SgMemberFunctionRefExp*>(SgMemberFunctionRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMemberFunctionRefExp::pool_size * (SgMemberFunctionRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMemberFunctionRefExpStroageClass
 * from its counterpart of SgMemberFunctionRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMemberFunctionRefExp::initializeStorageClassArray(SgMemberFunctionRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMemberFunctionRefExp::pools.begin(); block != SgMemberFunctionRefExp::pools.end(); ++block) {
        SgMemberFunctionRefExp* pointer{reinterpret_cast<SgMemberFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 144523 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateFunctionRefExp that takes its corresponding StorageClass as parameter
 */
SgTemplateFunctionRefExp::SgTemplateFunctionRefExp(const SgTemplateFunctionRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 144533 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol_i = (SgTemplateFunctionSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol_i));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateFunctionRefExp*
SgTemplateFunctionRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionRefExp::pool_size};
        returnPointer = &(((SgTemplateFunctionRefExp*)(SgTemplateFunctionRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateFunctionRefExp*
SgTemplateFunctionRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionRefExp::pool_size};
        returnPointer = &(((SgTemplateFunctionRefExp*)(SgTemplateFunctionRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateFunctionRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateFunctionRefExp::pools.begin(); block != SgTemplateFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateFunctionRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateFunctionRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateFunctionRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateFunctionRefExp::pools.begin(); block != SgTemplateFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateFunctionRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateFunctionRefExp::clearMemoryPool() {
    SgTemplateFunctionRefExp* tempPointer{nullptr};
    if (SgTemplateFunctionRefExp::pools.empty() == false) {
        auto block{SgTemplateFunctionRefExp::pools.begin()};
        SgTemplateFunctionRefExp::next_node = reinterpret_cast<SgTemplateFunctionRefExp*>(*block);
        for (; block != SgTemplateFunctionRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateFunctionRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateFunctionRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateFunctionRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateFunctionRefExp::pool_size-1]);
        }
    }
}

void
SgTemplateFunctionRefExp::deleteMemoryPool() {
    for (auto p: SgTemplateFunctionRefExp::pools) {
        ROSE_FREE(p);
    }
    SgTemplateFunctionRefExp::next_node = nullptr;
    SgTemplateFunctionRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateFunctionRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionRefExp)};
    for (size_t blockIndex = SgTemplateFunctionRefExp::pools.size(); (blockIndex * SgTemplateFunctionRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionRefExp) = %" PRIuPTR " SgTemplateFunctionRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionRefExp),SgTemplateFunctionRefExp::pool_size);
        }
#endif

        SgTemplateFunctionRefExp* pointer{static_cast<SgTemplateFunctionRefExp*>(ROSE_MALLOC(SgTemplateFunctionRefExp::pool_size * sizeof(SgTemplateFunctionRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateFunctionRefExp::pool_size * sizeof(SgTemplateFunctionRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateFunctionRefExp::pool_size * sizeof(SgTemplateFunctionRefExp));
#endif
        SgTemplateFunctionRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateFunctionRefExp::pool_size * sizeof(SgTemplateFunctionRefExp), V_SgTemplateFunctionRefExp));

        if (SgTemplateFunctionRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateFunctionRefExp* blkptr{reinterpret_cast<SgTemplateFunctionRefExp*>(SgTemplateFunctionRefExp::pools[blockIndex-1])};
                blkptr[SgTemplateFunctionRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateFunctionRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateFunctionRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateFunctionRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateFunctionRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateFunctionRefExp::pool_size - 1};
    SgTemplateFunctionRefExp* testPointer{reinterpret_cast<SgTemplateFunctionRefExp*>(SgTemplateFunctionRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateFunctionRefExp::pool_size * (SgTemplateFunctionRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateFunctionRefExpStroageClass
 * from its counterpart of SgTemplateFunctionRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateFunctionRefExp::initializeStorageClassArray(SgTemplateFunctionRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateFunctionRefExp::pools.begin(); block != SgTemplateFunctionRefExp::pools.end(); ++block) {
        SgTemplateFunctionRefExp* pointer{reinterpret_cast<SgTemplateFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 144754 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateMemberFunctionRefExp that takes its corresponding StorageClass as parameter
 */
SgTemplateMemberFunctionRefExp::SgTemplateMemberFunctionRefExp(const SgTemplateMemberFunctionRefExpStorageClass& storageSource)  : SgReferenceExp (storageSource) {

/* #line 144764 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol_i = (SgTemplateMemberFunctionSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol_i));
    p_virtual_call = storageSource.storageOf_virtual_call;
    p_need_qualifier = storageSource.storageOf_need_qualifier;
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateMemberFunctionRefExp*
SgTemplateMemberFunctionRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateMemberFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgTemplateMemberFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateMemberFunctionRefExp::pool_size};
        returnPointer = &(((SgTemplateMemberFunctionRefExp*)(SgTemplateMemberFunctionRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateMemberFunctionRefExp*
SgTemplateMemberFunctionRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateMemberFunctionRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateMemberFunctionRefExp)};
        unsigned long positionInPool{localIndex % SgTemplateMemberFunctionRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateMemberFunctionRefExp::pool_size};
        returnPointer = &(((SgTemplateMemberFunctionRefExp*)(SgTemplateMemberFunctionRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateMemberFunctionRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateMemberFunctionRefExp::pools.begin(); block != SgTemplateMemberFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateMemberFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateMemberFunctionRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateMemberFunctionRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateMemberFunctionRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateMemberFunctionRefExp::pools.begin(); block != SgTemplateMemberFunctionRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateMemberFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateMemberFunctionRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateMemberFunctionRefExp::clearMemoryPool() {
    SgTemplateMemberFunctionRefExp* tempPointer{nullptr};
    if (SgTemplateMemberFunctionRefExp::pools.empty() == false) {
        auto block{SgTemplateMemberFunctionRefExp::pools.begin()};
        SgTemplateMemberFunctionRefExp::next_node = reinterpret_cast<SgTemplateMemberFunctionRefExp*>(*block);
        for (; block != SgTemplateMemberFunctionRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateMemberFunctionRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateMemberFunctionRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateMemberFunctionRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateMemberFunctionRefExp::pool_size-1]);
        }
    }
}

void
SgTemplateMemberFunctionRefExp::deleteMemoryPool() {
    for (auto p: SgTemplateMemberFunctionRefExp::pools) {
        ROSE_FREE(p);
    }
    SgTemplateMemberFunctionRefExp::next_node = nullptr;
    SgTemplateMemberFunctionRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateMemberFunctionRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionRefExp)};
    for (size_t blockIndex = SgTemplateMemberFunctionRefExp::pools.size(); (blockIndex * SgTemplateMemberFunctionRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionRefExp) = %" PRIuPTR " SgTemplateMemberFunctionRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionRefExp),SgTemplateMemberFunctionRefExp::pool_size);
        }
#endif

        SgTemplateMemberFunctionRefExp* pointer{static_cast<SgTemplateMemberFunctionRefExp*>(ROSE_MALLOC(SgTemplateMemberFunctionRefExp::pool_size * sizeof(SgTemplateMemberFunctionRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateMemberFunctionRefExp::pool_size * sizeof(SgTemplateMemberFunctionRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateMemberFunctionRefExp::pool_size * sizeof(SgTemplateMemberFunctionRefExp));
#endif
        SgTemplateMemberFunctionRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateMemberFunctionRefExp::pool_size * sizeof(SgTemplateMemberFunctionRefExp), V_SgTemplateMemberFunctionRefExp));

        if (SgTemplateMemberFunctionRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateMemberFunctionRefExp* blkptr{reinterpret_cast<SgTemplateMemberFunctionRefExp*>(SgTemplateMemberFunctionRefExp::pools[blockIndex-1])};
                blkptr[SgTemplateMemberFunctionRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateMemberFunctionRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateMemberFunctionRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateMemberFunctionRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateMemberFunctionRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateMemberFunctionRefExp::pool_size - 1};
    SgTemplateMemberFunctionRefExp* testPointer{reinterpret_cast<SgTemplateMemberFunctionRefExp*>(SgTemplateMemberFunctionRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateMemberFunctionRefExp::pool_size * (SgTemplateMemberFunctionRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateMemberFunctionRefExpStroageClass
 * from its counterpart of SgTemplateMemberFunctionRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateMemberFunctionRefExp::initializeStorageClassArray(SgTemplateMemberFunctionRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateMemberFunctionRefExp::pools.begin(); block != SgTemplateMemberFunctionRefExp::pools.end(); ++block) {
        SgTemplateMemberFunctionRefExp* pointer{reinterpret_cast<SgTemplateMemberFunctionRefExp*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 144987 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgComprehension that takes its corresponding StorageClass as parameter
 */
SgComprehension::SgComprehension(const SgComprehensionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 144997 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_target = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_target));
     p_iter = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_iter));
     p_filters = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_filters));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgComprehension*
SgComprehension::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgComprehension)};
        unsigned long positionInPool{localIndex % SgComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgComprehension::pool_size};
        returnPointer = &(((SgComprehension*)(SgComprehension::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgComprehension*
SgComprehension::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgComprehension)};
        unsigned long positionInPool{localIndex % SgComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgComprehension::pool_size};
        returnPointer = &(((SgComprehension*)(SgComprehension::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgComprehension::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgComprehension::pools.begin(); block != SgComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgComprehension*>(*block)};
        for (unsigned i = 0; i < SgComprehension::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgComprehension*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgComprehension::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgComprehension* pointerOfLinkedList{nullptr};
    for (auto block = SgComprehension::pools.begin(); block != SgComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgComprehension*>(*block)};
        for (unsigned i = 0; i < SgComprehension::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgComprehension::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgComprehension::clearMemoryPool() {
    SgComprehension* tempPointer{nullptr};
    if (SgComprehension::pools.empty() == false) {
        auto block{SgComprehension::pools.begin()};
        SgComprehension::next_node = reinterpret_cast<SgComprehension*>(*block);
        for (; block != SgComprehension::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgComprehension*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgComprehension::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgComprehension::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgComprehension::pool_size-1]);
        }
    }
}

void
SgComprehension::deleteMemoryPool() {
    for (auto p: SgComprehension::pools) {
        ROSE_FREE(p);
    }
    SgComprehension::next_node = nullptr;
    SgComprehension::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgComprehension::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgComprehension) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgComprehension)};
    for (size_t blockIndex = SgComprehension::pools.size(); (blockIndex * SgComprehension::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgComprehension) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgComprehension) = %" PRIuPTR " SgComprehension::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgComprehension),AST_FILE_IO::getPoolSizeOfNewAst(V_SgComprehension),SgComprehension::pool_size);
        }
#endif

        SgComprehension* pointer{static_cast<SgComprehension*>(ROSE_MALLOC(SgComprehension::pool_size * sizeof(SgComprehension)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgComprehension::pool_size * sizeof(SgComprehension));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgComprehension::pool_size * sizeof(SgComprehension));
#endif
        SgComprehension::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgComprehension::pool_size * sizeof(SgComprehension), V_SgComprehension));

        if (SgComprehension::next_node != nullptr) {
            if (blockIndex > 0) {
                SgComprehension* blkptr{reinterpret_cast<SgComprehension*>(SgComprehension::pools[blockIndex-1])};
                blkptr[SgComprehension::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgComprehension::next_node = pointer;
        }

        for (unsigned i = 0; i < SgComprehension::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgComprehension::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgComprehension::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgComprehension::pool_size - 1};
    SgComprehension* testPointer{reinterpret_cast<SgComprehension*>(SgComprehension::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgComprehension::pool_size * (SgComprehension::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgComprehensionStroageClass
 * from its counterpart of SgComprehension. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgComprehension::initializeStorageClassArray(SgComprehensionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgComprehension::pools.begin(); block != SgComprehension::pools.end(); ++block) {
        SgComprehension* pointer{reinterpret_cast<SgComprehension*>(*block)};
        for (unsigned i = 0; i < SgComprehension::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 145217 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgListComprehension that takes its corresponding StorageClass as parameter
 */
SgListComprehension::SgListComprehension(const SgListComprehensionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 145227 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_element = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_element));
     p_generators = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_generators));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgListComprehension*
SgListComprehension::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgListComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgListComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgListComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgListComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgListComprehension)};
        unsigned long positionInPool{localIndex % SgListComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgListComprehension::pool_size};
        returnPointer = &(((SgListComprehension*)(SgListComprehension::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgListComprehension*
SgListComprehension::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgListComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgListComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgListComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgListComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgListComprehension)};
        unsigned long positionInPool{localIndex % SgListComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgListComprehension::pool_size};
        returnPointer = &(((SgListComprehension*)(SgListComprehension::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgListComprehension::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgListComprehension::pools.begin(); block != SgListComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgListComprehension*>(*block)};
        for (unsigned i = 0; i < SgListComprehension::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgListComprehension*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgListComprehension::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgListComprehension* pointerOfLinkedList{nullptr};
    for (auto block = SgListComprehension::pools.begin(); block != SgListComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgListComprehension*>(*block)};
        for (unsigned i = 0; i < SgListComprehension::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgListComprehension::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgListComprehension::clearMemoryPool() {
    SgListComprehension* tempPointer{nullptr};
    if (SgListComprehension::pools.empty() == false) {
        auto block{SgListComprehension::pools.begin()};
        SgListComprehension::next_node = reinterpret_cast<SgListComprehension*>(*block);
        for (; block != SgListComprehension::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgListComprehension*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgListComprehension::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgListComprehension::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgListComprehension::pool_size-1]);
        }
    }
}

void
SgListComprehension::deleteMemoryPool() {
    for (auto p: SgListComprehension::pools) {
        ROSE_FREE(p);
    }
    SgListComprehension::next_node = nullptr;
    SgListComprehension::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgListComprehension::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgListComprehension) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgListComprehension)};
    for (size_t blockIndex = SgListComprehension::pools.size(); (blockIndex * SgListComprehension::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgListComprehension) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgListComprehension) = %" PRIuPTR " SgListComprehension::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgListComprehension),AST_FILE_IO::getPoolSizeOfNewAst(V_SgListComprehension),SgListComprehension::pool_size);
        }
#endif

        SgListComprehension* pointer{static_cast<SgListComprehension*>(ROSE_MALLOC(SgListComprehension::pool_size * sizeof(SgListComprehension)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgListComprehension::pool_size * sizeof(SgListComprehension));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgListComprehension::pool_size * sizeof(SgListComprehension));
#endif
        SgListComprehension::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgListComprehension::pool_size * sizeof(SgListComprehension), V_SgListComprehension));

        if (SgListComprehension::next_node != nullptr) {
            if (blockIndex > 0) {
                SgListComprehension* blkptr{reinterpret_cast<SgListComprehension*>(SgListComprehension::pools[blockIndex-1])};
                blkptr[SgListComprehension::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgListComprehension::next_node = pointer;
        }

        for (unsigned i = 0; i < SgListComprehension::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgListComprehension::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgListComprehension::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgListComprehension::pool_size - 1};
    SgListComprehension* testPointer{reinterpret_cast<SgListComprehension*>(SgListComprehension::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgListComprehension::pool_size * (SgListComprehension::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgListComprehensionStroageClass
 * from its counterpart of SgListComprehension. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgListComprehension::initializeStorageClassArray(SgListComprehensionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgListComprehension::pools.begin(); block != SgListComprehension::pools.end(); ++block) {
        SgListComprehension* pointer{reinterpret_cast<SgListComprehension*>(*block)};
        for (unsigned i = 0; i < SgListComprehension::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 145446 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSetComprehension that takes its corresponding StorageClass as parameter
 */
SgSetComprehension::SgSetComprehension(const SgSetComprehensionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 145456 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_element = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_element));
     p_generators = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_generators));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSetComprehension*
SgSetComprehension::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSetComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSetComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSetComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSetComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSetComprehension)};
        unsigned long positionInPool{localIndex % SgSetComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSetComprehension::pool_size};
        returnPointer = &(((SgSetComprehension*)(SgSetComprehension::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSetComprehension*
SgSetComprehension::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSetComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSetComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSetComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSetComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSetComprehension)};
        unsigned long positionInPool{localIndex % SgSetComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSetComprehension::pool_size};
        returnPointer = &(((SgSetComprehension*)(SgSetComprehension::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSetComprehension::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSetComprehension::pools.begin(); block != SgSetComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSetComprehension*>(*block)};
        for (unsigned i = 0; i < SgSetComprehension::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSetComprehension*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSetComprehension::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSetComprehension* pointerOfLinkedList{nullptr};
    for (auto block = SgSetComprehension::pools.begin(); block != SgSetComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSetComprehension*>(*block)};
        for (unsigned i = 0; i < SgSetComprehension::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSetComprehension::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSetComprehension::clearMemoryPool() {
    SgSetComprehension* tempPointer{nullptr};
    if (SgSetComprehension::pools.empty() == false) {
        auto block{SgSetComprehension::pools.begin()};
        SgSetComprehension::next_node = reinterpret_cast<SgSetComprehension*>(*block);
        for (; block != SgSetComprehension::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSetComprehension*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSetComprehension::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSetComprehension::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSetComprehension::pool_size-1]);
        }
    }
}

void
SgSetComprehension::deleteMemoryPool() {
    for (auto p: SgSetComprehension::pools) {
        ROSE_FREE(p);
    }
    SgSetComprehension::next_node = nullptr;
    SgSetComprehension::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSetComprehension::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSetComprehension) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSetComprehension)};
    for (size_t blockIndex = SgSetComprehension::pools.size(); (blockIndex * SgSetComprehension::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSetComprehension) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSetComprehension) = %" PRIuPTR " SgSetComprehension::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSetComprehension),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSetComprehension),SgSetComprehension::pool_size);
        }
#endif

        SgSetComprehension* pointer{static_cast<SgSetComprehension*>(ROSE_MALLOC(SgSetComprehension::pool_size * sizeof(SgSetComprehension)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSetComprehension::pool_size * sizeof(SgSetComprehension));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSetComprehension::pool_size * sizeof(SgSetComprehension));
#endif
        SgSetComprehension::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSetComprehension::pool_size * sizeof(SgSetComprehension), V_SgSetComprehension));

        if (SgSetComprehension::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSetComprehension* blkptr{reinterpret_cast<SgSetComprehension*>(SgSetComprehension::pools[blockIndex-1])};
                blkptr[SgSetComprehension::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSetComprehension::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSetComprehension::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSetComprehension::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSetComprehension::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSetComprehension::pool_size - 1};
    SgSetComprehension* testPointer{reinterpret_cast<SgSetComprehension*>(SgSetComprehension::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSetComprehension::pool_size * (SgSetComprehension::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSetComprehensionStroageClass
 * from its counterpart of SgSetComprehension. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSetComprehension::initializeStorageClassArray(SgSetComprehensionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSetComprehension::pools.begin(); block != SgSetComprehension::pools.end(); ++block) {
        SgSetComprehension* pointer{reinterpret_cast<SgSetComprehension*>(*block)};
        for (unsigned i = 0; i < SgSetComprehension::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 145675 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDictionaryComprehension that takes its corresponding StorageClass as parameter
 */
SgDictionaryComprehension::SgDictionaryComprehension(const SgDictionaryComprehensionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 145685 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_element = (SgKeyDatumPair*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_element));
     p_generators = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_generators));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDictionaryComprehension*
SgDictionaryComprehension::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDictionaryComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDictionaryComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDictionaryComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDictionaryComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryComprehension)};
        unsigned long positionInPool{localIndex % SgDictionaryComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDictionaryComprehension::pool_size};
        returnPointer = &(((SgDictionaryComprehension*)(SgDictionaryComprehension::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDictionaryComprehension*
SgDictionaryComprehension::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDictionaryComprehension* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDictionaryComprehension)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDictionaryComprehension + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDictionaryComprehension)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDictionaryComprehension)};
        unsigned long positionInPool{localIndex % SgDictionaryComprehension::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDictionaryComprehension::pool_size};
        returnPointer = &(((SgDictionaryComprehension*)(SgDictionaryComprehension::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDictionaryComprehension::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDictionaryComprehension::pools.begin(); block != SgDictionaryComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDictionaryComprehension*>(*block)};
        for (unsigned i = 0; i < SgDictionaryComprehension::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDictionaryComprehension*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDictionaryComprehension::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDictionaryComprehension* pointerOfLinkedList{nullptr};
    for (auto block = SgDictionaryComprehension::pools.begin(); block != SgDictionaryComprehension::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDictionaryComprehension*>(*block)};
        for (unsigned i = 0; i < SgDictionaryComprehension::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDictionaryComprehension::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDictionaryComprehension::clearMemoryPool() {
    SgDictionaryComprehension* tempPointer{nullptr};
    if (SgDictionaryComprehension::pools.empty() == false) {
        auto block{SgDictionaryComprehension::pools.begin()};
        SgDictionaryComprehension::next_node = reinterpret_cast<SgDictionaryComprehension*>(*block);
        for (; block != SgDictionaryComprehension::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDictionaryComprehension*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDictionaryComprehension::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDictionaryComprehension::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDictionaryComprehension::pool_size-1]);
        }
    }
}

void
SgDictionaryComprehension::deleteMemoryPool() {
    for (auto p: SgDictionaryComprehension::pools) {
        ROSE_FREE(p);
    }
    SgDictionaryComprehension::next_node = nullptr;
    SgDictionaryComprehension::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDictionaryComprehension::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryComprehension) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDictionaryComprehension)};
    for (size_t blockIndex = SgDictionaryComprehension::pools.size(); (blockIndex * SgDictionaryComprehension::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryComprehension) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDictionaryComprehension) = %" PRIuPTR " SgDictionaryComprehension::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDictionaryComprehension),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDictionaryComprehension),SgDictionaryComprehension::pool_size);
        }
#endif

        SgDictionaryComprehension* pointer{static_cast<SgDictionaryComprehension*>(ROSE_MALLOC(SgDictionaryComprehension::pool_size * sizeof(SgDictionaryComprehension)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDictionaryComprehension::pool_size * sizeof(SgDictionaryComprehension));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDictionaryComprehension::pool_size * sizeof(SgDictionaryComprehension));
#endif
        SgDictionaryComprehension::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDictionaryComprehension::pool_size * sizeof(SgDictionaryComprehension), V_SgDictionaryComprehension));

        if (SgDictionaryComprehension::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDictionaryComprehension* blkptr{reinterpret_cast<SgDictionaryComprehension*>(SgDictionaryComprehension::pools[blockIndex-1])};
                blkptr[SgDictionaryComprehension::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDictionaryComprehension::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDictionaryComprehension::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDictionaryComprehension::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDictionaryComprehension::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDictionaryComprehension::pool_size - 1};
    SgDictionaryComprehension* testPointer{reinterpret_cast<SgDictionaryComprehension*>(SgDictionaryComprehension::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDictionaryComprehension::pool_size * (SgDictionaryComprehension::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDictionaryComprehensionStroageClass
 * from its counterpart of SgDictionaryComprehension. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDictionaryComprehension::initializeStorageClassArray(SgDictionaryComprehensionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDictionaryComprehension::pools.begin(); block != SgDictionaryComprehension::pools.end(); ++block) {
        SgDictionaryComprehension* pointer{reinterpret_cast<SgDictionaryComprehension*>(*block)};
        for (unsigned i = 0; i < SgDictionaryComprehension::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 145904 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNaryOp that takes its corresponding StorageClass as parameter
 */
SgNaryOp::SgNaryOp(const SgNaryOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 145914 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_operands = storageSource.storageOf_operands.rebuildDataStoredInEasyStorageClass();
    SgExpressionPtrList::iterator i_operands = p_operands.begin();
    for ( ; i_operands != p_operands.end(); ++i_operands) {
        (*i_operands) = (SgExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_operands)) );
    }
    p_operators = storageSource.storageOf_operators.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNaryOp*
SgNaryOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryOp)};
        unsigned long positionInPool{localIndex % SgNaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNaryOp::pool_size};
        returnPointer = &(((SgNaryOp*)(SgNaryOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNaryOp*
SgNaryOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNaryOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNaryOp)};
        unsigned long positionInPool{localIndex % SgNaryOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNaryOp::pool_size};
        returnPointer = &(((SgNaryOp*)(SgNaryOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNaryOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNaryOp::pools.begin(); block != SgNaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNaryOp*>(*block)};
        for (unsigned i = 0; i < SgNaryOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNaryOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNaryOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNaryOp* pointerOfLinkedList{nullptr};
    for (auto block = SgNaryOp::pools.begin(); block != SgNaryOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNaryOp*>(*block)};
        for (unsigned i = 0; i < SgNaryOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNaryOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNaryOp::clearMemoryPool() {
    SgNaryOp* tempPointer{nullptr};
    if (SgNaryOp::pools.empty() == false) {
        auto block{SgNaryOp::pools.begin()};
        SgNaryOp::next_node = reinterpret_cast<SgNaryOp*>(*block);
        for (; block != SgNaryOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNaryOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNaryOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNaryOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNaryOp::pool_size-1]);
        }
    }
}

void
SgNaryOp::deleteMemoryPool() {
    for (auto p: SgNaryOp::pools) {
        ROSE_FREE(p);
    }
    SgNaryOp::next_node = nullptr;
    SgNaryOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNaryOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryOp)};
    for (size_t blockIndex = SgNaryOp::pools.size(); (blockIndex * SgNaryOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryOp) = %" PRIuPTR " SgNaryOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryOp),SgNaryOp::pool_size);
        }
#endif

        SgNaryOp* pointer{static_cast<SgNaryOp*>(ROSE_MALLOC(SgNaryOp::pool_size * sizeof(SgNaryOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNaryOp::pool_size * sizeof(SgNaryOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNaryOp::pool_size * sizeof(SgNaryOp));
#endif
        SgNaryOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNaryOp::pool_size * sizeof(SgNaryOp), V_SgNaryOp));

        if (SgNaryOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNaryOp* blkptr{reinterpret_cast<SgNaryOp*>(SgNaryOp::pools[blockIndex-1])};
                blkptr[SgNaryOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNaryOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNaryOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNaryOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNaryOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNaryOp::pool_size - 1};
    SgNaryOp* testPointer{reinterpret_cast<SgNaryOp*>(SgNaryOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNaryOp::pool_size * (SgNaryOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNaryOpStroageClass
 * from its counterpart of SgNaryOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNaryOp::initializeStorageClassArray(SgNaryOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNaryOp::pools.begin(); block != SgNaryOp::pools.end(); ++block) {
        SgNaryOp* pointer{reinterpret_cast<SgNaryOp*>(*block)};
        for (unsigned i = 0; i < SgNaryOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 146137 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNaryBooleanOp that takes its corresponding StorageClass as parameter
 */
SgNaryBooleanOp::SgNaryBooleanOp(const SgNaryBooleanOpStorageClass& storageSource)  : SgNaryOp (storageSource) {

/* #line 146147 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNaryBooleanOp*
SgNaryBooleanOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNaryBooleanOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryBooleanOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryBooleanOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryBooleanOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryBooleanOp)};
        unsigned long positionInPool{localIndex % SgNaryBooleanOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNaryBooleanOp::pool_size};
        returnPointer = &(((SgNaryBooleanOp*)(SgNaryBooleanOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNaryBooleanOp*
SgNaryBooleanOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNaryBooleanOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryBooleanOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryBooleanOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryBooleanOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNaryBooleanOp)};
        unsigned long positionInPool{localIndex % SgNaryBooleanOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNaryBooleanOp::pool_size};
        returnPointer = &(((SgNaryBooleanOp*)(SgNaryBooleanOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNaryBooleanOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNaryBooleanOp::pools.begin(); block != SgNaryBooleanOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNaryBooleanOp*>(*block)};
        for (unsigned i = 0; i < SgNaryBooleanOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNaryBooleanOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNaryBooleanOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNaryBooleanOp* pointerOfLinkedList{nullptr};
    for (auto block = SgNaryBooleanOp::pools.begin(); block != SgNaryBooleanOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNaryBooleanOp*>(*block)};
        for (unsigned i = 0; i < SgNaryBooleanOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNaryBooleanOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNaryBooleanOp::clearMemoryPool() {
    SgNaryBooleanOp* tempPointer{nullptr};
    if (SgNaryBooleanOp::pools.empty() == false) {
        auto block{SgNaryBooleanOp::pools.begin()};
        SgNaryBooleanOp::next_node = reinterpret_cast<SgNaryBooleanOp*>(*block);
        for (; block != SgNaryBooleanOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNaryBooleanOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNaryBooleanOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNaryBooleanOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNaryBooleanOp::pool_size-1]);
        }
    }
}

void
SgNaryBooleanOp::deleteMemoryPool() {
    for (auto p: SgNaryBooleanOp::pools) {
        ROSE_FREE(p);
    }
    SgNaryBooleanOp::next_node = nullptr;
    SgNaryBooleanOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNaryBooleanOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryBooleanOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryBooleanOp)};
    for (size_t blockIndex = SgNaryBooleanOp::pools.size(); (blockIndex * SgNaryBooleanOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryBooleanOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryBooleanOp) = %" PRIuPTR " SgNaryBooleanOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryBooleanOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryBooleanOp),SgNaryBooleanOp::pool_size);
        }
#endif

        SgNaryBooleanOp* pointer{static_cast<SgNaryBooleanOp*>(ROSE_MALLOC(SgNaryBooleanOp::pool_size * sizeof(SgNaryBooleanOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNaryBooleanOp::pool_size * sizeof(SgNaryBooleanOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNaryBooleanOp::pool_size * sizeof(SgNaryBooleanOp));
#endif
        SgNaryBooleanOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNaryBooleanOp::pool_size * sizeof(SgNaryBooleanOp), V_SgNaryBooleanOp));

        if (SgNaryBooleanOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNaryBooleanOp* blkptr{reinterpret_cast<SgNaryBooleanOp*>(SgNaryBooleanOp::pools[blockIndex-1])};
                blkptr[SgNaryBooleanOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNaryBooleanOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNaryBooleanOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNaryBooleanOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNaryBooleanOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNaryBooleanOp::pool_size - 1};
    SgNaryBooleanOp* testPointer{reinterpret_cast<SgNaryBooleanOp*>(SgNaryBooleanOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNaryBooleanOp::pool_size * (SgNaryBooleanOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNaryBooleanOpStroageClass
 * from its counterpart of SgNaryBooleanOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNaryBooleanOp::initializeStorageClassArray(SgNaryBooleanOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNaryBooleanOp::pools.begin(); block != SgNaryBooleanOp::pools.end(); ++block) {
        SgNaryBooleanOp* pointer{reinterpret_cast<SgNaryBooleanOp*>(*block)};
        for (unsigned i = 0; i < SgNaryBooleanOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 146364 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNaryComparisonOp that takes its corresponding StorageClass as parameter
 */
SgNaryComparisonOp::SgNaryComparisonOp(const SgNaryComparisonOpStorageClass& storageSource)  : SgNaryOp (storageSource) {

/* #line 146374 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNaryComparisonOp*
SgNaryComparisonOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNaryComparisonOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryComparisonOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryComparisonOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNaryComparisonOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryComparisonOp)};
        unsigned long positionInPool{localIndex % SgNaryComparisonOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNaryComparisonOp::pool_size};
        returnPointer = &(((SgNaryComparisonOp*)(SgNaryComparisonOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNaryComparisonOp*
SgNaryComparisonOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNaryComparisonOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryComparisonOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryComparisonOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNaryComparisonOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNaryComparisonOp)};
        unsigned long positionInPool{localIndex % SgNaryComparisonOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNaryComparisonOp::pool_size};
        returnPointer = &(((SgNaryComparisonOp*)(SgNaryComparisonOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNaryComparisonOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNaryComparisonOp::pools.begin(); block != SgNaryComparisonOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNaryComparisonOp*>(*block)};
        for (unsigned i = 0; i < SgNaryComparisonOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNaryComparisonOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNaryComparisonOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNaryComparisonOp* pointerOfLinkedList{nullptr};
    for (auto block = SgNaryComparisonOp::pools.begin(); block != SgNaryComparisonOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNaryComparisonOp*>(*block)};
        for (unsigned i = 0; i < SgNaryComparisonOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNaryComparisonOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNaryComparisonOp::clearMemoryPool() {
    SgNaryComparisonOp* tempPointer{nullptr};
    if (SgNaryComparisonOp::pools.empty() == false) {
        auto block{SgNaryComparisonOp::pools.begin()};
        SgNaryComparisonOp::next_node = reinterpret_cast<SgNaryComparisonOp*>(*block);
        for (; block != SgNaryComparisonOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNaryComparisonOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNaryComparisonOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNaryComparisonOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNaryComparisonOp::pool_size-1]);
        }
    }
}

void
SgNaryComparisonOp::deleteMemoryPool() {
    for (auto p: SgNaryComparisonOp::pools) {
        ROSE_FREE(p);
    }
    SgNaryComparisonOp::next_node = nullptr;
    SgNaryComparisonOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNaryComparisonOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryComparisonOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryComparisonOp)};
    for (size_t blockIndex = SgNaryComparisonOp::pools.size(); (blockIndex * SgNaryComparisonOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryComparisonOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryComparisonOp) = %" PRIuPTR " SgNaryComparisonOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNaryComparisonOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNaryComparisonOp),SgNaryComparisonOp::pool_size);
        }
#endif

        SgNaryComparisonOp* pointer{static_cast<SgNaryComparisonOp*>(ROSE_MALLOC(SgNaryComparisonOp::pool_size * sizeof(SgNaryComparisonOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNaryComparisonOp::pool_size * sizeof(SgNaryComparisonOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNaryComparisonOp::pool_size * sizeof(SgNaryComparisonOp));
#endif
        SgNaryComparisonOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNaryComparisonOp::pool_size * sizeof(SgNaryComparisonOp), V_SgNaryComparisonOp));

        if (SgNaryComparisonOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNaryComparisonOp* blkptr{reinterpret_cast<SgNaryComparisonOp*>(SgNaryComparisonOp::pools[blockIndex-1])};
                blkptr[SgNaryComparisonOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNaryComparisonOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNaryComparisonOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNaryComparisonOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNaryComparisonOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNaryComparisonOp::pool_size - 1};
    SgNaryComparisonOp* testPointer{reinterpret_cast<SgNaryComparisonOp*>(SgNaryComparisonOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNaryComparisonOp::pool_size * (SgNaryComparisonOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNaryComparisonOpStroageClass
 * from its counterpart of SgNaryComparisonOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNaryComparisonOp::initializeStorageClassArray(SgNaryComparisonOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNaryComparisonOp::pools.begin(); block != SgNaryComparisonOp::pools.end(); ++block) {
        SgNaryComparisonOp* pointer{reinterpret_cast<SgNaryComparisonOp*>(*block)};
        for (unsigned i = 0; i < SgNaryComparisonOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 146591 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgStringConversion that takes its corresponding StorageClass as parameter
 */
SgStringConversion::SgStringConversion(const SgStringConversionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 146601 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgStringConversion*
SgStringConversion::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgStringConversion* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringConversion)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringConversion + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgStringConversion)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgStringConversion)};
        unsigned long positionInPool{localIndex % SgStringConversion::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStringConversion::pool_size};
        returnPointer = &(((SgStringConversion*)(SgStringConversion::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgStringConversion*
SgStringConversion::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgStringConversion* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringConversion)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringConversion + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgStringConversion)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgStringConversion)};
        unsigned long positionInPool{localIndex % SgStringConversion::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgStringConversion::pool_size};
        returnPointer = &(((SgStringConversion*)(SgStringConversion::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgStringConversion::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgStringConversion::pools.begin(); block != SgStringConversion::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStringConversion*>(*block)};
        for (unsigned i = 0; i < SgStringConversion::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgStringConversion*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgStringConversion::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgStringConversion* pointerOfLinkedList{nullptr};
    for (auto block = SgStringConversion::pools.begin(); block != SgStringConversion::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgStringConversion*>(*block)};
        for (unsigned i = 0; i < SgStringConversion::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgStringConversion::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgStringConversion::clearMemoryPool() {
    SgStringConversion* tempPointer{nullptr};
    if (SgStringConversion::pools.empty() == false) {
        auto block{SgStringConversion::pools.begin()};
        SgStringConversion::next_node = reinterpret_cast<SgStringConversion*>(*block);
        for (; block != SgStringConversion::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgStringConversion*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgStringConversion::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgStringConversion::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgStringConversion::pool_size-1]);
        }
    }
}

void
SgStringConversion::deleteMemoryPool() {
    for (auto p: SgStringConversion::pools) {
        ROSE_FREE(p);
    }
    SgStringConversion::next_node = nullptr;
    SgStringConversion::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgStringConversion::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgStringConversion) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringConversion)};
    for (size_t blockIndex = SgStringConversion::pools.size(); (blockIndex * SgStringConversion::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgStringConversion) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringConversion) = %" PRIuPTR " SgStringConversion::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgStringConversion),AST_FILE_IO::getPoolSizeOfNewAst(V_SgStringConversion),SgStringConversion::pool_size);
        }
#endif

        SgStringConversion* pointer{static_cast<SgStringConversion*>(ROSE_MALLOC(SgStringConversion::pool_size * sizeof(SgStringConversion)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgStringConversion::pool_size * sizeof(SgStringConversion));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgStringConversion::pool_size * sizeof(SgStringConversion));
#endif
        SgStringConversion::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgStringConversion::pool_size * sizeof(SgStringConversion), V_SgStringConversion));

        if (SgStringConversion::next_node != nullptr) {
            if (blockIndex > 0) {
                SgStringConversion* blkptr{reinterpret_cast<SgStringConversion*>(SgStringConversion::pools[blockIndex-1])};
                blkptr[SgStringConversion::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgStringConversion::next_node = pointer;
        }

        for (unsigned i = 0; i < SgStringConversion::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgStringConversion::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgStringConversion::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgStringConversion::pool_size - 1};
    SgStringConversion* testPointer{reinterpret_cast<SgStringConversion*>(SgStringConversion::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgStringConversion::pool_size * (SgStringConversion::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgStringConversionStroageClass
 * from its counterpart of SgStringConversion. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgStringConversion::initializeStorageClassArray(SgStringConversionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgStringConversion::pools.begin(); block != SgStringConversion::pools.end(); ++block) {
        SgStringConversion* pointer{reinterpret_cast<SgStringConversion*>(*block)};
        for (unsigned i = 0; i < SgStringConversion::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 146819 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgYieldExpression that takes its corresponding StorageClass as parameter
 */
SgYieldExpression::SgYieldExpression(const SgYieldExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 146829 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgYieldExpression*
SgYieldExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgYieldExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgYieldExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgYieldExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgYieldExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgYieldExpression)};
        unsigned long positionInPool{localIndex % SgYieldExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgYieldExpression::pool_size};
        returnPointer = &(((SgYieldExpression*)(SgYieldExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgYieldExpression*
SgYieldExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgYieldExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgYieldExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgYieldExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgYieldExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgYieldExpression)};
        unsigned long positionInPool{localIndex % SgYieldExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgYieldExpression::pool_size};
        returnPointer = &(((SgYieldExpression*)(SgYieldExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgYieldExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgYieldExpression::pools.begin(); block != SgYieldExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgYieldExpression*>(*block)};
        for (unsigned i = 0; i < SgYieldExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgYieldExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgYieldExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgYieldExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgYieldExpression::pools.begin(); block != SgYieldExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgYieldExpression*>(*block)};
        for (unsigned i = 0; i < SgYieldExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgYieldExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgYieldExpression::clearMemoryPool() {
    SgYieldExpression* tempPointer{nullptr};
    if (SgYieldExpression::pools.empty() == false) {
        auto block{SgYieldExpression::pools.begin()};
        SgYieldExpression::next_node = reinterpret_cast<SgYieldExpression*>(*block);
        for (; block != SgYieldExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgYieldExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgYieldExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgYieldExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgYieldExpression::pool_size-1]);
        }
    }
}

void
SgYieldExpression::deleteMemoryPool() {
    for (auto p: SgYieldExpression::pools) {
        ROSE_FREE(p);
    }
    SgYieldExpression::next_node = nullptr;
    SgYieldExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgYieldExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgYieldExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgYieldExpression)};
    for (size_t blockIndex = SgYieldExpression::pools.size(); (blockIndex * SgYieldExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgYieldExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgYieldExpression) = %" PRIuPTR " SgYieldExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgYieldExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgYieldExpression),SgYieldExpression::pool_size);
        }
#endif

        SgYieldExpression* pointer{static_cast<SgYieldExpression*>(ROSE_MALLOC(SgYieldExpression::pool_size * sizeof(SgYieldExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgYieldExpression::pool_size * sizeof(SgYieldExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgYieldExpression::pool_size * sizeof(SgYieldExpression));
#endif
        SgYieldExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgYieldExpression::pool_size * sizeof(SgYieldExpression), V_SgYieldExpression));

        if (SgYieldExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgYieldExpression* blkptr{reinterpret_cast<SgYieldExpression*>(SgYieldExpression::pools[blockIndex-1])};
                blkptr[SgYieldExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgYieldExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgYieldExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgYieldExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgYieldExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgYieldExpression::pool_size - 1};
    SgYieldExpression* testPointer{reinterpret_cast<SgYieldExpression*>(SgYieldExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgYieldExpression::pool_size * (SgYieldExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgYieldExpressionStroageClass
 * from its counterpart of SgYieldExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgYieldExpression::initializeStorageClassArray(SgYieldExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgYieldExpression::pools.begin(); block != SgYieldExpression::pools.end(); ++block) {
        SgYieldExpression* pointer{reinterpret_cast<SgYieldExpression*>(*block)};
        for (unsigned i = 0; i < SgYieldExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 147047 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAlignOfOp that takes its corresponding StorageClass as parameter
 */
SgAlignOfOp::SgAlignOfOp(const SgAlignOfOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 147057 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));
     p_operand_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_type));
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
    p_name_qualification_length = storageSource.storageOf_name_qualification_length;
    p_type_elaboration_required = storageSource.storageOf_type_elaboration_required;
    p_global_qualification_required = storageSource.storageOf_global_qualification_required;
    p_alignOfContainsBaseTypeDefiningDeclaration = storageSource.storageOf_alignOfContainsBaseTypeDefiningDeclaration;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAlignOfOp*
SgAlignOfOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAlignOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAlignOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAlignOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAlignOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAlignOfOp)};
        unsigned long positionInPool{localIndex % SgAlignOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAlignOfOp::pool_size};
        returnPointer = &(((SgAlignOfOp*)(SgAlignOfOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAlignOfOp*
SgAlignOfOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAlignOfOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAlignOfOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAlignOfOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAlignOfOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAlignOfOp)};
        unsigned long positionInPool{localIndex % SgAlignOfOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAlignOfOp::pool_size};
        returnPointer = &(((SgAlignOfOp*)(SgAlignOfOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAlignOfOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAlignOfOp::pools.begin(); block != SgAlignOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAlignOfOp*>(*block)};
        for (unsigned i = 0; i < SgAlignOfOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAlignOfOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAlignOfOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAlignOfOp* pointerOfLinkedList{nullptr};
    for (auto block = SgAlignOfOp::pools.begin(); block != SgAlignOfOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAlignOfOp*>(*block)};
        for (unsigned i = 0; i < SgAlignOfOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAlignOfOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAlignOfOp::clearMemoryPool() {
    SgAlignOfOp* tempPointer{nullptr};
    if (SgAlignOfOp::pools.empty() == false) {
        auto block{SgAlignOfOp::pools.begin()};
        SgAlignOfOp::next_node = reinterpret_cast<SgAlignOfOp*>(*block);
        for (; block != SgAlignOfOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAlignOfOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAlignOfOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAlignOfOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAlignOfOp::pool_size-1]);
        }
    }
}

void
SgAlignOfOp::deleteMemoryPool() {
    for (auto p: SgAlignOfOp::pools) {
        ROSE_FREE(p);
    }
    SgAlignOfOp::next_node = nullptr;
    SgAlignOfOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAlignOfOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAlignOfOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAlignOfOp)};
    for (size_t blockIndex = SgAlignOfOp::pools.size(); (blockIndex * SgAlignOfOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAlignOfOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAlignOfOp) = %" PRIuPTR " SgAlignOfOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAlignOfOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAlignOfOp),SgAlignOfOp::pool_size);
        }
#endif

        SgAlignOfOp* pointer{static_cast<SgAlignOfOp*>(ROSE_MALLOC(SgAlignOfOp::pool_size * sizeof(SgAlignOfOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAlignOfOp::pool_size * sizeof(SgAlignOfOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAlignOfOp::pool_size * sizeof(SgAlignOfOp));
#endif
        SgAlignOfOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAlignOfOp::pool_size * sizeof(SgAlignOfOp), V_SgAlignOfOp));

        if (SgAlignOfOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAlignOfOp* blkptr{reinterpret_cast<SgAlignOfOp*>(SgAlignOfOp::pools[blockIndex-1])};
                blkptr[SgAlignOfOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAlignOfOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAlignOfOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAlignOfOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAlignOfOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAlignOfOp::pool_size - 1};
    SgAlignOfOp* testPointer{reinterpret_cast<SgAlignOfOp*>(SgAlignOfOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAlignOfOp::pool_size * (SgAlignOfOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAlignOfOpStroageClass
 * from its counterpart of SgAlignOfOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAlignOfOp::initializeStorageClassArray(SgAlignOfOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAlignOfOp::pools.begin(); block != SgAlignOfOp::pools.end(); ++block) {
        SgAlignOfOp* pointer{reinterpret_cast<SgAlignOfOp*>(*block)};
        for (unsigned i = 0; i < SgAlignOfOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 147281 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRangeExp that takes its corresponding StorageClass as parameter
 */
SgRangeExp::SgRangeExp(const SgRangeExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 147291 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_start = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_start));
     p_end = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_end));
     p_stride = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_stride));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRangeExp*
SgRangeExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRangeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRangeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeExp)};
        unsigned long positionInPool{localIndex % SgRangeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRangeExp::pool_size};
        returnPointer = &(((SgRangeExp*)(SgRangeExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRangeExp*
SgRangeExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRangeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRangeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRangeExp)};
        unsigned long positionInPool{localIndex % SgRangeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRangeExp::pool_size};
        returnPointer = &(((SgRangeExp*)(SgRangeExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRangeExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRangeExp::pools.begin(); block != SgRangeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRangeExp*>(*block)};
        for (unsigned i = 0; i < SgRangeExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRangeExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRangeExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRangeExp* pointerOfLinkedList{nullptr};
    for (auto block = SgRangeExp::pools.begin(); block != SgRangeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRangeExp*>(*block)};
        for (unsigned i = 0; i < SgRangeExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRangeExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRangeExp::clearMemoryPool() {
    SgRangeExp* tempPointer{nullptr};
    if (SgRangeExp::pools.empty() == false) {
        auto block{SgRangeExp::pools.begin()};
        SgRangeExp::next_node = reinterpret_cast<SgRangeExp*>(*block);
        for (; block != SgRangeExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRangeExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRangeExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRangeExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRangeExp::pool_size-1]);
        }
    }
}

void
SgRangeExp::deleteMemoryPool() {
    for (auto p: SgRangeExp::pools) {
        ROSE_FREE(p);
    }
    SgRangeExp::next_node = nullptr;
    SgRangeExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRangeExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeExp)};
    for (size_t blockIndex = SgRangeExp::pools.size(); (blockIndex * SgRangeExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeExp) = %" PRIuPTR " SgRangeExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRangeExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRangeExp),SgRangeExp::pool_size);
        }
#endif

        SgRangeExp* pointer{static_cast<SgRangeExp*>(ROSE_MALLOC(SgRangeExp::pool_size * sizeof(SgRangeExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRangeExp::pool_size * sizeof(SgRangeExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRangeExp::pool_size * sizeof(SgRangeExp));
#endif
        SgRangeExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRangeExp::pool_size * sizeof(SgRangeExp), V_SgRangeExp));

        if (SgRangeExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRangeExp* blkptr{reinterpret_cast<SgRangeExp*>(SgRangeExp::pools[blockIndex-1])};
                blkptr[SgRangeExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRangeExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRangeExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRangeExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRangeExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRangeExp::pool_size - 1};
    SgRangeExp* testPointer{reinterpret_cast<SgRangeExp*>(SgRangeExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRangeExp::pool_size * (SgRangeExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRangeExpStroageClass
 * from its counterpart of SgRangeExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRangeExp::initializeStorageClassArray(SgRangeExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRangeExp::pools.begin(); block != SgRangeExp::pools.end(); ++block) {
        SgRangeExp* pointer{reinterpret_cast<SgRangeExp*>(*block)};
        for (unsigned i = 0; i < SgRangeExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 147511 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMagicColonExp that takes its corresponding StorageClass as parameter
 */
SgMagicColonExp::SgMagicColonExp(const SgMagicColonExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 147521 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMagicColonExp*
SgMagicColonExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMagicColonExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMagicColonExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMagicColonExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMagicColonExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMagicColonExp)};
        unsigned long positionInPool{localIndex % SgMagicColonExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMagicColonExp::pool_size};
        returnPointer = &(((SgMagicColonExp*)(SgMagicColonExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMagicColonExp*
SgMagicColonExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMagicColonExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMagicColonExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMagicColonExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMagicColonExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMagicColonExp)};
        unsigned long positionInPool{localIndex % SgMagicColonExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMagicColonExp::pool_size};
        returnPointer = &(((SgMagicColonExp*)(SgMagicColonExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMagicColonExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMagicColonExp::pools.begin(); block != SgMagicColonExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMagicColonExp*>(*block)};
        for (unsigned i = 0; i < SgMagicColonExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMagicColonExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMagicColonExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMagicColonExp* pointerOfLinkedList{nullptr};
    for (auto block = SgMagicColonExp::pools.begin(); block != SgMagicColonExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMagicColonExp*>(*block)};
        for (unsigned i = 0; i < SgMagicColonExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMagicColonExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMagicColonExp::clearMemoryPool() {
    SgMagicColonExp* tempPointer{nullptr};
    if (SgMagicColonExp::pools.empty() == false) {
        auto block{SgMagicColonExp::pools.begin()};
        SgMagicColonExp::next_node = reinterpret_cast<SgMagicColonExp*>(*block);
        for (; block != SgMagicColonExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMagicColonExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMagicColonExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMagicColonExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMagicColonExp::pool_size-1]);
        }
    }
}

void
SgMagicColonExp::deleteMemoryPool() {
    for (auto p: SgMagicColonExp::pools) {
        ROSE_FREE(p);
    }
    SgMagicColonExp::next_node = nullptr;
    SgMagicColonExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMagicColonExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMagicColonExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMagicColonExp)};
    for (size_t blockIndex = SgMagicColonExp::pools.size(); (blockIndex * SgMagicColonExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMagicColonExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMagicColonExp) = %" PRIuPTR " SgMagicColonExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMagicColonExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMagicColonExp),SgMagicColonExp::pool_size);
        }
#endif

        SgMagicColonExp* pointer{static_cast<SgMagicColonExp*>(ROSE_MALLOC(SgMagicColonExp::pool_size * sizeof(SgMagicColonExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMagicColonExp::pool_size * sizeof(SgMagicColonExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMagicColonExp::pool_size * sizeof(SgMagicColonExp));
#endif
        SgMagicColonExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMagicColonExp::pool_size * sizeof(SgMagicColonExp), V_SgMagicColonExp));

        if (SgMagicColonExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMagicColonExp* blkptr{reinterpret_cast<SgMagicColonExp*>(SgMagicColonExp::pools[blockIndex-1])};
                blkptr[SgMagicColonExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMagicColonExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMagicColonExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMagicColonExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMagicColonExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMagicColonExp::pool_size - 1};
    SgMagicColonExp* testPointer{reinterpret_cast<SgMagicColonExp*>(SgMagicColonExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMagicColonExp::pool_size * (SgMagicColonExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMagicColonExpStroageClass
 * from its counterpart of SgMagicColonExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMagicColonExp::initializeStorageClassArray(SgMagicColonExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMagicColonExp::pools.begin(); block != SgMagicColonExp::pools.end(); ++block) {
        SgMagicColonExp* pointer{reinterpret_cast<SgMagicColonExp*>(*block)};
        for (unsigned i = 0; i < SgMagicColonExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 147738 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeTraitBuiltinOperator that takes its corresponding StorageClass as parameter
 */
SgTypeTraitBuiltinOperator::SgTypeTraitBuiltinOperator(const SgTypeTraitBuiltinOperatorStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 147748 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
    p_args = storageSource.storageOf_args.rebuildDataStoredInEasyStorageClass();
    SgNodePtrList::iterator i_args = p_args.begin();
    for ( ; i_args != p_args.end(); ++i_args) {
        (*i_args) = (SgNodePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_args)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeTraitBuiltinOperator*
SgTypeTraitBuiltinOperator::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeTraitBuiltinOperator* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTraitBuiltinOperator)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTraitBuiltinOperator + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeTraitBuiltinOperator)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTraitBuiltinOperator)};
        unsigned long positionInPool{localIndex % SgTypeTraitBuiltinOperator::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeTraitBuiltinOperator::pool_size};
        returnPointer = &(((SgTypeTraitBuiltinOperator*)(SgTypeTraitBuiltinOperator::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeTraitBuiltinOperator*
SgTypeTraitBuiltinOperator::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeTraitBuiltinOperator* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTraitBuiltinOperator)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTraitBuiltinOperator + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeTraitBuiltinOperator)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeTraitBuiltinOperator)};
        unsigned long positionInPool{localIndex % SgTypeTraitBuiltinOperator::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeTraitBuiltinOperator::pool_size};
        returnPointer = &(((SgTypeTraitBuiltinOperator*)(SgTypeTraitBuiltinOperator::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeTraitBuiltinOperator::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeTraitBuiltinOperator::pools.begin(); block != SgTypeTraitBuiltinOperator::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeTraitBuiltinOperator*>(*block)};
        for (unsigned i = 0; i < SgTypeTraitBuiltinOperator::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeTraitBuiltinOperator*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeTraitBuiltinOperator::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeTraitBuiltinOperator* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeTraitBuiltinOperator::pools.begin(); block != SgTypeTraitBuiltinOperator::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeTraitBuiltinOperator*>(*block)};
        for (unsigned i = 0; i < SgTypeTraitBuiltinOperator::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeTraitBuiltinOperator::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeTraitBuiltinOperator::clearMemoryPool() {
    SgTypeTraitBuiltinOperator* tempPointer{nullptr};
    if (SgTypeTraitBuiltinOperator::pools.empty() == false) {
        auto block{SgTypeTraitBuiltinOperator::pools.begin()};
        SgTypeTraitBuiltinOperator::next_node = reinterpret_cast<SgTypeTraitBuiltinOperator*>(*block);
        for (; block != SgTypeTraitBuiltinOperator::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeTraitBuiltinOperator*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeTraitBuiltinOperator::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeTraitBuiltinOperator::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeTraitBuiltinOperator::pool_size-1]);
        }
    }
}

void
SgTypeTraitBuiltinOperator::deleteMemoryPool() {
    for (auto p: SgTypeTraitBuiltinOperator::pools) {
        ROSE_FREE(p);
    }
    SgTypeTraitBuiltinOperator::next_node = nullptr;
    SgTypeTraitBuiltinOperator::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeTraitBuiltinOperator::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTraitBuiltinOperator) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTraitBuiltinOperator)};
    for (size_t blockIndex = SgTypeTraitBuiltinOperator::pools.size(); (blockIndex * SgTypeTraitBuiltinOperator::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTraitBuiltinOperator) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTraitBuiltinOperator) = %" PRIuPTR " SgTypeTraitBuiltinOperator::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeTraitBuiltinOperator),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeTraitBuiltinOperator),SgTypeTraitBuiltinOperator::pool_size);
        }
#endif

        SgTypeTraitBuiltinOperator* pointer{static_cast<SgTypeTraitBuiltinOperator*>(ROSE_MALLOC(SgTypeTraitBuiltinOperator::pool_size * sizeof(SgTypeTraitBuiltinOperator)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeTraitBuiltinOperator::pool_size * sizeof(SgTypeTraitBuiltinOperator));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeTraitBuiltinOperator::pool_size * sizeof(SgTypeTraitBuiltinOperator));
#endif
        SgTypeTraitBuiltinOperator::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeTraitBuiltinOperator::pool_size * sizeof(SgTypeTraitBuiltinOperator), V_SgTypeTraitBuiltinOperator));

        if (SgTypeTraitBuiltinOperator::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeTraitBuiltinOperator* blkptr{reinterpret_cast<SgTypeTraitBuiltinOperator*>(SgTypeTraitBuiltinOperator::pools[blockIndex-1])};
                blkptr[SgTypeTraitBuiltinOperator::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeTraitBuiltinOperator::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeTraitBuiltinOperator::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeTraitBuiltinOperator::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeTraitBuiltinOperator::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeTraitBuiltinOperator::pool_size - 1};
    SgTypeTraitBuiltinOperator* testPointer{reinterpret_cast<SgTypeTraitBuiltinOperator*>(SgTypeTraitBuiltinOperator::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeTraitBuiltinOperator::pool_size * (SgTypeTraitBuiltinOperator::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeTraitBuiltinOperatorStroageClass
 * from its counterpart of SgTypeTraitBuiltinOperator. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeTraitBuiltinOperator::initializeStorageClassArray(SgTypeTraitBuiltinOperatorStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeTraitBuiltinOperator::pools.begin(); block != SgTypeTraitBuiltinOperator::pools.end(); ++block) {
        SgTypeTraitBuiltinOperator* pointer{reinterpret_cast<SgTypeTraitBuiltinOperator*>(*block)};
        for (unsigned i = 0; i < SgTypeTraitBuiltinOperator::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 147971 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCompoundLiteralExp that takes its corresponding StorageClass as parameter
 */
SgCompoundLiteralExp::SgCompoundLiteralExp(const SgCompoundLiteralExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 147981 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgVariableSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCompoundLiteralExp*
SgCompoundLiteralExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCompoundLiteralExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundLiteralExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundLiteralExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCompoundLiteralExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundLiteralExp)};
        unsigned long positionInPool{localIndex % SgCompoundLiteralExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCompoundLiteralExp::pool_size};
        returnPointer = &(((SgCompoundLiteralExp*)(SgCompoundLiteralExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCompoundLiteralExp*
SgCompoundLiteralExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCompoundLiteralExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundLiteralExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundLiteralExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCompoundLiteralExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCompoundLiteralExp)};
        unsigned long positionInPool{localIndex % SgCompoundLiteralExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCompoundLiteralExp::pool_size};
        returnPointer = &(((SgCompoundLiteralExp*)(SgCompoundLiteralExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCompoundLiteralExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCompoundLiteralExp::pools.begin(); block != SgCompoundLiteralExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCompoundLiteralExp*>(*block)};
        for (unsigned i = 0; i < SgCompoundLiteralExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCompoundLiteralExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCompoundLiteralExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCompoundLiteralExp* pointerOfLinkedList{nullptr};
    for (auto block = SgCompoundLiteralExp::pools.begin(); block != SgCompoundLiteralExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCompoundLiteralExp*>(*block)};
        for (unsigned i = 0; i < SgCompoundLiteralExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCompoundLiteralExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCompoundLiteralExp::clearMemoryPool() {
    SgCompoundLiteralExp* tempPointer{nullptr};
    if (SgCompoundLiteralExp::pools.empty() == false) {
        auto block{SgCompoundLiteralExp::pools.begin()};
        SgCompoundLiteralExp::next_node = reinterpret_cast<SgCompoundLiteralExp*>(*block);
        for (; block != SgCompoundLiteralExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCompoundLiteralExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCompoundLiteralExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCompoundLiteralExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCompoundLiteralExp::pool_size-1]);
        }
    }
}

void
SgCompoundLiteralExp::deleteMemoryPool() {
    for (auto p: SgCompoundLiteralExp::pools) {
        ROSE_FREE(p);
    }
    SgCompoundLiteralExp::next_node = nullptr;
    SgCompoundLiteralExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCompoundLiteralExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundLiteralExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundLiteralExp)};
    for (size_t blockIndex = SgCompoundLiteralExp::pools.size(); (blockIndex * SgCompoundLiteralExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundLiteralExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundLiteralExp) = %" PRIuPTR " SgCompoundLiteralExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCompoundLiteralExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCompoundLiteralExp),SgCompoundLiteralExp::pool_size);
        }
#endif

        SgCompoundLiteralExp* pointer{static_cast<SgCompoundLiteralExp*>(ROSE_MALLOC(SgCompoundLiteralExp::pool_size * sizeof(SgCompoundLiteralExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCompoundLiteralExp::pool_size * sizeof(SgCompoundLiteralExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCompoundLiteralExp::pool_size * sizeof(SgCompoundLiteralExp));
#endif
        SgCompoundLiteralExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCompoundLiteralExp::pool_size * sizeof(SgCompoundLiteralExp), V_SgCompoundLiteralExp));

        if (SgCompoundLiteralExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCompoundLiteralExp* blkptr{reinterpret_cast<SgCompoundLiteralExp*>(SgCompoundLiteralExp::pools[blockIndex-1])};
                blkptr[SgCompoundLiteralExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCompoundLiteralExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCompoundLiteralExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCompoundLiteralExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCompoundLiteralExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCompoundLiteralExp::pool_size - 1};
    SgCompoundLiteralExp* testPointer{reinterpret_cast<SgCompoundLiteralExp*>(SgCompoundLiteralExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCompoundLiteralExp::pool_size * (SgCompoundLiteralExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCompoundLiteralExpStroageClass
 * from its counterpart of SgCompoundLiteralExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCompoundLiteralExp::initializeStorageClassArray(SgCompoundLiteralExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCompoundLiteralExp::pools.begin(); block != SgCompoundLiteralExp::pools.end(); ++block) {
        SgCompoundLiteralExp* pointer{reinterpret_cast<SgCompoundLiteralExp*>(*block)};
        for (unsigned i = 0; i < SgCompoundLiteralExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 148199 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaAnnotation that takes its corresponding StorageClass as parameter
 */
SgJavaAnnotation::SgJavaAnnotation(const SgJavaAnnotationStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 148209 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaAnnotation*
SgJavaAnnotation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaAnnotation::pool_size};
        returnPointer = &(((SgJavaAnnotation*)(SgJavaAnnotation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaAnnotation*
SgJavaAnnotation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaAnnotation::pool_size};
        returnPointer = &(((SgJavaAnnotation*)(SgJavaAnnotation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaAnnotation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaAnnotation::pools.begin(); block != SgJavaAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaAnnotation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaAnnotation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaAnnotation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaAnnotation* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaAnnotation::pools.begin(); block != SgJavaAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaAnnotation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaAnnotation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaAnnotation::clearMemoryPool() {
    SgJavaAnnotation* tempPointer{nullptr};
    if (SgJavaAnnotation::pools.empty() == false) {
        auto block{SgJavaAnnotation::pools.begin()};
        SgJavaAnnotation::next_node = reinterpret_cast<SgJavaAnnotation*>(*block);
        for (; block != SgJavaAnnotation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaAnnotation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaAnnotation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaAnnotation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaAnnotation::pool_size-1]);
        }
    }
}

void
SgJavaAnnotation::deleteMemoryPool() {
    for (auto p: SgJavaAnnotation::pools) {
        ROSE_FREE(p);
    }
    SgJavaAnnotation::next_node = nullptr;
    SgJavaAnnotation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaAnnotation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaAnnotation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaAnnotation)};
    for (size_t blockIndex = SgJavaAnnotation::pools.size(); (blockIndex * SgJavaAnnotation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaAnnotation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaAnnotation) = %" PRIuPTR " SgJavaAnnotation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaAnnotation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaAnnotation),SgJavaAnnotation::pool_size);
        }
#endif

        SgJavaAnnotation* pointer{static_cast<SgJavaAnnotation*>(ROSE_MALLOC(SgJavaAnnotation::pool_size * sizeof(SgJavaAnnotation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaAnnotation::pool_size * sizeof(SgJavaAnnotation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaAnnotation::pool_size * sizeof(SgJavaAnnotation));
#endif
        SgJavaAnnotation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaAnnotation::pool_size * sizeof(SgJavaAnnotation), V_SgJavaAnnotation));

        if (SgJavaAnnotation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaAnnotation* blkptr{reinterpret_cast<SgJavaAnnotation*>(SgJavaAnnotation::pools[blockIndex-1])};
                blkptr[SgJavaAnnotation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaAnnotation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaAnnotation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaAnnotation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaAnnotation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaAnnotation::pool_size - 1};
    SgJavaAnnotation* testPointer{reinterpret_cast<SgJavaAnnotation*>(SgJavaAnnotation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaAnnotation::pool_size * (SgJavaAnnotation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaAnnotationStroageClass
 * from its counterpart of SgJavaAnnotation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaAnnotation::initializeStorageClassArray(SgJavaAnnotationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaAnnotation::pools.begin(); block != SgJavaAnnotation::pools.end(); ++block) {
        SgJavaAnnotation* pointer{reinterpret_cast<SgJavaAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaAnnotation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 148427 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaMarkerAnnotation that takes its corresponding StorageClass as parameter
 */
SgJavaMarkerAnnotation::SgJavaMarkerAnnotation(const SgJavaMarkerAnnotationStorageClass& storageSource)  : SgJavaAnnotation (storageSource) {

/* #line 148437 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaMarkerAnnotation*
SgJavaMarkerAnnotation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaMarkerAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaMarkerAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaMarkerAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaMarkerAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMarkerAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaMarkerAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaMarkerAnnotation::pool_size};
        returnPointer = &(((SgJavaMarkerAnnotation*)(SgJavaMarkerAnnotation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaMarkerAnnotation*
SgJavaMarkerAnnotation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaMarkerAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaMarkerAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaMarkerAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaMarkerAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaMarkerAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaMarkerAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaMarkerAnnotation::pool_size};
        returnPointer = &(((SgJavaMarkerAnnotation*)(SgJavaMarkerAnnotation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaMarkerAnnotation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaMarkerAnnotation::pools.begin(); block != SgJavaMarkerAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaMarkerAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaMarkerAnnotation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaMarkerAnnotation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaMarkerAnnotation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaMarkerAnnotation* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaMarkerAnnotation::pools.begin(); block != SgJavaMarkerAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaMarkerAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaMarkerAnnotation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaMarkerAnnotation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaMarkerAnnotation::clearMemoryPool() {
    SgJavaMarkerAnnotation* tempPointer{nullptr};
    if (SgJavaMarkerAnnotation::pools.empty() == false) {
        auto block{SgJavaMarkerAnnotation::pools.begin()};
        SgJavaMarkerAnnotation::next_node = reinterpret_cast<SgJavaMarkerAnnotation*>(*block);
        for (; block != SgJavaMarkerAnnotation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaMarkerAnnotation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaMarkerAnnotation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaMarkerAnnotation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaMarkerAnnotation::pool_size-1]);
        }
    }
}

void
SgJavaMarkerAnnotation::deleteMemoryPool() {
    for (auto p: SgJavaMarkerAnnotation::pools) {
        ROSE_FREE(p);
    }
    SgJavaMarkerAnnotation::next_node = nullptr;
    SgJavaMarkerAnnotation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaMarkerAnnotation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMarkerAnnotation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaMarkerAnnotation)};
    for (size_t blockIndex = SgJavaMarkerAnnotation::pools.size(); (blockIndex * SgJavaMarkerAnnotation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMarkerAnnotation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaMarkerAnnotation) = %" PRIuPTR " SgJavaMarkerAnnotation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaMarkerAnnotation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaMarkerAnnotation),SgJavaMarkerAnnotation::pool_size);
        }
#endif

        SgJavaMarkerAnnotation* pointer{static_cast<SgJavaMarkerAnnotation*>(ROSE_MALLOC(SgJavaMarkerAnnotation::pool_size * sizeof(SgJavaMarkerAnnotation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaMarkerAnnotation::pool_size * sizeof(SgJavaMarkerAnnotation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaMarkerAnnotation::pool_size * sizeof(SgJavaMarkerAnnotation));
#endif
        SgJavaMarkerAnnotation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaMarkerAnnotation::pool_size * sizeof(SgJavaMarkerAnnotation), V_SgJavaMarkerAnnotation));

        if (SgJavaMarkerAnnotation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaMarkerAnnotation* blkptr{reinterpret_cast<SgJavaMarkerAnnotation*>(SgJavaMarkerAnnotation::pools[blockIndex-1])};
                blkptr[SgJavaMarkerAnnotation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaMarkerAnnotation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaMarkerAnnotation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaMarkerAnnotation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaMarkerAnnotation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaMarkerAnnotation::pool_size - 1};
    SgJavaMarkerAnnotation* testPointer{reinterpret_cast<SgJavaMarkerAnnotation*>(SgJavaMarkerAnnotation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaMarkerAnnotation::pool_size * (SgJavaMarkerAnnotation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaMarkerAnnotationStroageClass
 * from its counterpart of SgJavaMarkerAnnotation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaMarkerAnnotation::initializeStorageClassArray(SgJavaMarkerAnnotationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaMarkerAnnotation::pools.begin(); block != SgJavaMarkerAnnotation::pools.end(); ++block) {
        SgJavaMarkerAnnotation* pointer{reinterpret_cast<SgJavaMarkerAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaMarkerAnnotation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 148654 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaSingleMemberAnnotation that takes its corresponding StorageClass as parameter
 */
SgJavaSingleMemberAnnotation::SgJavaSingleMemberAnnotation(const SgJavaSingleMemberAnnotationStorageClass& storageSource)  : SgJavaAnnotation (storageSource) {

/* #line 148664 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaSingleMemberAnnotation*
SgJavaSingleMemberAnnotation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaSingleMemberAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaSingleMemberAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaSingleMemberAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaSingleMemberAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSingleMemberAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaSingleMemberAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaSingleMemberAnnotation::pool_size};
        returnPointer = &(((SgJavaSingleMemberAnnotation*)(SgJavaSingleMemberAnnotation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaSingleMemberAnnotation*
SgJavaSingleMemberAnnotation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaSingleMemberAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaSingleMemberAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaSingleMemberAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaSingleMemberAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaSingleMemberAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaSingleMemberAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaSingleMemberAnnotation::pool_size};
        returnPointer = &(((SgJavaSingleMemberAnnotation*)(SgJavaSingleMemberAnnotation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaSingleMemberAnnotation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaSingleMemberAnnotation::pools.begin(); block != SgJavaSingleMemberAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaSingleMemberAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaSingleMemberAnnotation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaSingleMemberAnnotation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaSingleMemberAnnotation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaSingleMemberAnnotation* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaSingleMemberAnnotation::pools.begin(); block != SgJavaSingleMemberAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaSingleMemberAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaSingleMemberAnnotation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaSingleMemberAnnotation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaSingleMemberAnnotation::clearMemoryPool() {
    SgJavaSingleMemberAnnotation* tempPointer{nullptr};
    if (SgJavaSingleMemberAnnotation::pools.empty() == false) {
        auto block{SgJavaSingleMemberAnnotation::pools.begin()};
        SgJavaSingleMemberAnnotation::next_node = reinterpret_cast<SgJavaSingleMemberAnnotation*>(*block);
        for (; block != SgJavaSingleMemberAnnotation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaSingleMemberAnnotation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaSingleMemberAnnotation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaSingleMemberAnnotation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaSingleMemberAnnotation::pool_size-1]);
        }
    }
}

void
SgJavaSingleMemberAnnotation::deleteMemoryPool() {
    for (auto p: SgJavaSingleMemberAnnotation::pools) {
        ROSE_FREE(p);
    }
    SgJavaSingleMemberAnnotation::next_node = nullptr;
    SgJavaSingleMemberAnnotation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaSingleMemberAnnotation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSingleMemberAnnotation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaSingleMemberAnnotation)};
    for (size_t blockIndex = SgJavaSingleMemberAnnotation::pools.size(); (blockIndex * SgJavaSingleMemberAnnotation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSingleMemberAnnotation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaSingleMemberAnnotation) = %" PRIuPTR " SgJavaSingleMemberAnnotation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaSingleMemberAnnotation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaSingleMemberAnnotation),SgJavaSingleMemberAnnotation::pool_size);
        }
#endif

        SgJavaSingleMemberAnnotation* pointer{static_cast<SgJavaSingleMemberAnnotation*>(ROSE_MALLOC(SgJavaSingleMemberAnnotation::pool_size * sizeof(SgJavaSingleMemberAnnotation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaSingleMemberAnnotation::pool_size * sizeof(SgJavaSingleMemberAnnotation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaSingleMemberAnnotation::pool_size * sizeof(SgJavaSingleMemberAnnotation));
#endif
        SgJavaSingleMemberAnnotation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaSingleMemberAnnotation::pool_size * sizeof(SgJavaSingleMemberAnnotation), V_SgJavaSingleMemberAnnotation));

        if (SgJavaSingleMemberAnnotation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaSingleMemberAnnotation* blkptr{reinterpret_cast<SgJavaSingleMemberAnnotation*>(SgJavaSingleMemberAnnotation::pools[blockIndex-1])};
                blkptr[SgJavaSingleMemberAnnotation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaSingleMemberAnnotation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaSingleMemberAnnotation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaSingleMemberAnnotation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaSingleMemberAnnotation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaSingleMemberAnnotation::pool_size - 1};
    SgJavaSingleMemberAnnotation* testPointer{reinterpret_cast<SgJavaSingleMemberAnnotation*>(SgJavaSingleMemberAnnotation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaSingleMemberAnnotation::pool_size * (SgJavaSingleMemberAnnotation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaSingleMemberAnnotationStroageClass
 * from its counterpart of SgJavaSingleMemberAnnotation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaSingleMemberAnnotation::initializeStorageClassArray(SgJavaSingleMemberAnnotationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaSingleMemberAnnotation::pools.begin(); block != SgJavaSingleMemberAnnotation::pools.end(); ++block) {
        SgJavaSingleMemberAnnotation* pointer{reinterpret_cast<SgJavaSingleMemberAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaSingleMemberAnnotation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 148882 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaNormalAnnotation that takes its corresponding StorageClass as parameter
 */
SgJavaNormalAnnotation::SgJavaNormalAnnotation(const SgJavaNormalAnnotationStorageClass& storageSource)  : SgJavaAnnotation (storageSource) {

/* #line 148892 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value_pair_list = storageSource.storageOf_value_pair_list.rebuildDataStoredInEasyStorageClass();
    SgJavaMemberValuePairPtrList::iterator i_value_pair_list = p_value_pair_list.begin();
    for ( ; i_value_pair_list != p_value_pair_list.end(); ++i_value_pair_list) {
        (*i_value_pair_list) = (SgJavaMemberValuePairPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_value_pair_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaNormalAnnotation*
SgJavaNormalAnnotation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaNormalAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaNormalAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaNormalAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaNormalAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaNormalAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaNormalAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaNormalAnnotation::pool_size};
        returnPointer = &(((SgJavaNormalAnnotation*)(SgJavaNormalAnnotation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaNormalAnnotation*
SgJavaNormalAnnotation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaNormalAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaNormalAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaNormalAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaNormalAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaNormalAnnotation)};
        unsigned long positionInPool{localIndex % SgJavaNormalAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaNormalAnnotation::pool_size};
        returnPointer = &(((SgJavaNormalAnnotation*)(SgJavaNormalAnnotation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaNormalAnnotation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaNormalAnnotation::pools.begin(); block != SgJavaNormalAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaNormalAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaNormalAnnotation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaNormalAnnotation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaNormalAnnotation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaNormalAnnotation* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaNormalAnnotation::pools.begin(); block != SgJavaNormalAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaNormalAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaNormalAnnotation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaNormalAnnotation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaNormalAnnotation::clearMemoryPool() {
    SgJavaNormalAnnotation* tempPointer{nullptr};
    if (SgJavaNormalAnnotation::pools.empty() == false) {
        auto block{SgJavaNormalAnnotation::pools.begin()};
        SgJavaNormalAnnotation::next_node = reinterpret_cast<SgJavaNormalAnnotation*>(*block);
        for (; block != SgJavaNormalAnnotation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaNormalAnnotation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaNormalAnnotation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaNormalAnnotation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaNormalAnnotation::pool_size-1]);
        }
    }
}

void
SgJavaNormalAnnotation::deleteMemoryPool() {
    for (auto p: SgJavaNormalAnnotation::pools) {
        ROSE_FREE(p);
    }
    SgJavaNormalAnnotation::next_node = nullptr;
    SgJavaNormalAnnotation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaNormalAnnotation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaNormalAnnotation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaNormalAnnotation)};
    for (size_t blockIndex = SgJavaNormalAnnotation::pools.size(); (blockIndex * SgJavaNormalAnnotation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaNormalAnnotation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaNormalAnnotation) = %" PRIuPTR " SgJavaNormalAnnotation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaNormalAnnotation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaNormalAnnotation),SgJavaNormalAnnotation::pool_size);
        }
#endif

        SgJavaNormalAnnotation* pointer{static_cast<SgJavaNormalAnnotation*>(ROSE_MALLOC(SgJavaNormalAnnotation::pool_size * sizeof(SgJavaNormalAnnotation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaNormalAnnotation::pool_size * sizeof(SgJavaNormalAnnotation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaNormalAnnotation::pool_size * sizeof(SgJavaNormalAnnotation));
#endif
        SgJavaNormalAnnotation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaNormalAnnotation::pool_size * sizeof(SgJavaNormalAnnotation), V_SgJavaNormalAnnotation));

        if (SgJavaNormalAnnotation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaNormalAnnotation* blkptr{reinterpret_cast<SgJavaNormalAnnotation*>(SgJavaNormalAnnotation::pools[blockIndex-1])};
                blkptr[SgJavaNormalAnnotation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaNormalAnnotation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaNormalAnnotation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaNormalAnnotation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaNormalAnnotation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaNormalAnnotation::pool_size - 1};
    SgJavaNormalAnnotation* testPointer{reinterpret_cast<SgJavaNormalAnnotation*>(SgJavaNormalAnnotation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaNormalAnnotation::pool_size * (SgJavaNormalAnnotation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaNormalAnnotationStroageClass
 * from its counterpart of SgJavaNormalAnnotation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaNormalAnnotation::initializeStorageClassArray(SgJavaNormalAnnotationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaNormalAnnotation::pools.begin(); block != SgJavaNormalAnnotation::pools.end(); ++block) {
        SgJavaNormalAnnotation* pointer{reinterpret_cast<SgJavaNormalAnnotation*>(*block)};
        for (unsigned i = 0; i < SgJavaNormalAnnotation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 149114 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaTypeExpression that takes its corresponding StorageClass as parameter
 */
SgJavaTypeExpression::SgJavaTypeExpression(const SgJavaTypeExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 149124 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaTypeExpression*
SgJavaTypeExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaTypeExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaTypeExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaTypeExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaTypeExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaTypeExpression)};
        unsigned long positionInPool{localIndex % SgJavaTypeExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaTypeExpression::pool_size};
        returnPointer = &(((SgJavaTypeExpression*)(SgJavaTypeExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaTypeExpression*
SgJavaTypeExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaTypeExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaTypeExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaTypeExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaTypeExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaTypeExpression)};
        unsigned long positionInPool{localIndex % SgJavaTypeExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaTypeExpression::pool_size};
        returnPointer = &(((SgJavaTypeExpression*)(SgJavaTypeExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaTypeExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaTypeExpression::pools.begin(); block != SgJavaTypeExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaTypeExpression*>(*block)};
        for (unsigned i = 0; i < SgJavaTypeExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaTypeExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaTypeExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaTypeExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaTypeExpression::pools.begin(); block != SgJavaTypeExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaTypeExpression*>(*block)};
        for (unsigned i = 0; i < SgJavaTypeExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaTypeExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaTypeExpression::clearMemoryPool() {
    SgJavaTypeExpression* tempPointer{nullptr};
    if (SgJavaTypeExpression::pools.empty() == false) {
        auto block{SgJavaTypeExpression::pools.begin()};
        SgJavaTypeExpression::next_node = reinterpret_cast<SgJavaTypeExpression*>(*block);
        for (; block != SgJavaTypeExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaTypeExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaTypeExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaTypeExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaTypeExpression::pool_size-1]);
        }
    }
}

void
SgJavaTypeExpression::deleteMemoryPool() {
    for (auto p: SgJavaTypeExpression::pools) {
        ROSE_FREE(p);
    }
    SgJavaTypeExpression::next_node = nullptr;
    SgJavaTypeExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaTypeExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaTypeExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaTypeExpression)};
    for (size_t blockIndex = SgJavaTypeExpression::pools.size(); (blockIndex * SgJavaTypeExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaTypeExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaTypeExpression) = %" PRIuPTR " SgJavaTypeExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaTypeExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaTypeExpression),SgJavaTypeExpression::pool_size);
        }
#endif

        SgJavaTypeExpression* pointer{static_cast<SgJavaTypeExpression*>(ROSE_MALLOC(SgJavaTypeExpression::pool_size * sizeof(SgJavaTypeExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaTypeExpression::pool_size * sizeof(SgJavaTypeExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaTypeExpression::pool_size * sizeof(SgJavaTypeExpression));
#endif
        SgJavaTypeExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaTypeExpression::pool_size * sizeof(SgJavaTypeExpression), V_SgJavaTypeExpression));

        if (SgJavaTypeExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaTypeExpression* blkptr{reinterpret_cast<SgJavaTypeExpression*>(SgJavaTypeExpression::pools[blockIndex-1])};
                blkptr[SgJavaTypeExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaTypeExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaTypeExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaTypeExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaTypeExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaTypeExpression::pool_size - 1};
    SgJavaTypeExpression* testPointer{reinterpret_cast<SgJavaTypeExpression*>(SgJavaTypeExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaTypeExpression::pool_size * (SgJavaTypeExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaTypeExpressionStroageClass
 * from its counterpart of SgJavaTypeExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaTypeExpression::initializeStorageClassArray(SgJavaTypeExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaTypeExpression::pools.begin(); block != SgJavaTypeExpression::pools.end(); ++block) {
        SgJavaTypeExpression* pointer{reinterpret_cast<SgJavaTypeExpression*>(*block)};
        for (unsigned i = 0; i < SgJavaTypeExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 149342 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeExpression that takes its corresponding StorageClass as parameter
 */
SgTypeExpression::SgTypeExpression(const SgTypeExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 149352 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeExpression*
SgTypeExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeExpression)};
        unsigned long positionInPool{localIndex % SgTypeExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeExpression::pool_size};
        returnPointer = &(((SgTypeExpression*)(SgTypeExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeExpression*
SgTypeExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeExpression)};
        unsigned long positionInPool{localIndex % SgTypeExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeExpression::pool_size};
        returnPointer = &(((SgTypeExpression*)(SgTypeExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeExpression::pools.begin(); block != SgTypeExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeExpression*>(*block)};
        for (unsigned i = 0; i < SgTypeExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeExpression::pools.begin(); block != SgTypeExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeExpression*>(*block)};
        for (unsigned i = 0; i < SgTypeExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeExpression::clearMemoryPool() {
    SgTypeExpression* tempPointer{nullptr};
    if (SgTypeExpression::pools.empty() == false) {
        auto block{SgTypeExpression::pools.begin()};
        SgTypeExpression::next_node = reinterpret_cast<SgTypeExpression*>(*block);
        for (; block != SgTypeExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeExpression::pool_size-1]);
        }
    }
}

void
SgTypeExpression::deleteMemoryPool() {
    for (auto p: SgTypeExpression::pools) {
        ROSE_FREE(p);
    }
    SgTypeExpression::next_node = nullptr;
    SgTypeExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeExpression)};
    for (size_t blockIndex = SgTypeExpression::pools.size(); (blockIndex * SgTypeExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeExpression) = %" PRIuPTR " SgTypeExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeExpression),SgTypeExpression::pool_size);
        }
#endif

        SgTypeExpression* pointer{static_cast<SgTypeExpression*>(ROSE_MALLOC(SgTypeExpression::pool_size * sizeof(SgTypeExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeExpression::pool_size * sizeof(SgTypeExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeExpression::pool_size * sizeof(SgTypeExpression));
#endif
        SgTypeExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeExpression::pool_size * sizeof(SgTypeExpression), V_SgTypeExpression));

        if (SgTypeExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeExpression* blkptr{reinterpret_cast<SgTypeExpression*>(SgTypeExpression::pools[blockIndex-1])};
                blkptr[SgTypeExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeExpression::pool_size - 1};
    SgTypeExpression* testPointer{reinterpret_cast<SgTypeExpression*>(SgTypeExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeExpression::pool_size * (SgTypeExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeExpressionStroageClass
 * from its counterpart of SgTypeExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeExpression::initializeStorageClassArray(SgTypeExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeExpression::pools.begin(); block != SgTypeExpression::pools.end(); ++block) {
        SgTypeExpression* pointer{reinterpret_cast<SgTypeExpression*>(*block)};
        for (unsigned i = 0; i < SgTypeExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 149570 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClassExp that takes its corresponding StorageClass as parameter
 */
SgClassExp::SgClassExp(const SgClassExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 149580 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_class_symbol = (SgClassSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_class_symbol));
    p_pobj_class = storageSource.storageOf_pobj_class;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClassExp*
SgClassExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClassExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClassExp)};
        unsigned long positionInPool{localIndex % SgClassExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassExp::pool_size};
        returnPointer = &(((SgClassExp*)(SgClassExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClassExp*
SgClassExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClassExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClassExp)};
        unsigned long positionInPool{localIndex % SgClassExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassExp::pool_size};
        returnPointer = &(((SgClassExp*)(SgClassExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClassExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClassExp::pools.begin(); block != SgClassExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassExp*>(*block)};
        for (unsigned i = 0; i < SgClassExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClassExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClassExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClassExp* pointerOfLinkedList{nullptr};
    for (auto block = SgClassExp::pools.begin(); block != SgClassExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassExp*>(*block)};
        for (unsigned i = 0; i < SgClassExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClassExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClassExp::clearMemoryPool() {
    SgClassExp* tempPointer{nullptr};
    if (SgClassExp::pools.empty() == false) {
        auto block{SgClassExp::pools.begin()};
        SgClassExp::next_node = reinterpret_cast<SgClassExp*>(*block);
        for (; block != SgClassExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClassExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClassExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClassExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClassExp::pool_size-1]);
        }
    }
}

void
SgClassExp::deleteMemoryPool() {
    for (auto p: SgClassExp::pools) {
        ROSE_FREE(p);
    }
    SgClassExp::next_node = nullptr;
    SgClassExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClassExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClassExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassExp)};
    for (size_t blockIndex = SgClassExp::pools.size(); (blockIndex * SgClassExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClassExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassExp) = %" PRIuPTR " SgClassExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClassExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassExp),SgClassExp::pool_size);
        }
#endif

        SgClassExp* pointer{static_cast<SgClassExp*>(ROSE_MALLOC(SgClassExp::pool_size * sizeof(SgClassExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClassExp::pool_size * sizeof(SgClassExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClassExp::pool_size * sizeof(SgClassExp));
#endif
        SgClassExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClassExp::pool_size * sizeof(SgClassExp), V_SgClassExp));

        if (SgClassExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClassExp* blkptr{reinterpret_cast<SgClassExp*>(SgClassExp::pools[blockIndex-1])};
                blkptr[SgClassExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClassExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClassExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClassExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClassExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClassExp::pool_size - 1};
    SgClassExp* testPointer{reinterpret_cast<SgClassExp*>(SgClassExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClassExp::pool_size * (SgClassExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClassExpStroageClass
 * from its counterpart of SgClassExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClassExp::initializeStorageClassArray(SgClassExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClassExp::pools.begin(); block != SgClassExp::pools.end(); ++block) {
        SgClassExp* pointer{reinterpret_cast<SgClassExp*>(*block)};
        for (unsigned i = 0; i < SgClassExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 149799 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionParameterRefExp that takes its corresponding StorageClass as parameter
 */
SgFunctionParameterRefExp::SgFunctionParameterRefExp(const SgFunctionParameterRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 149809 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_parameter_number = storageSource.storageOf_parameter_number;
    p_parameter_levels_up = storageSource.storageOf_parameter_levels_up;
     p_parameter_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameter_expression));
     p_parameter_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_parameter_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionParameterRefExp*
SgFunctionParameterRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionParameterRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionParameterRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterRefExp)};
        unsigned long positionInPool{localIndex % SgFunctionParameterRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterRefExp::pool_size};
        returnPointer = &(((SgFunctionParameterRefExp*)(SgFunctionParameterRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionParameterRefExp*
SgFunctionParameterRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionParameterRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionParameterRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionParameterRefExp)};
        unsigned long positionInPool{localIndex % SgFunctionParameterRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionParameterRefExp::pool_size};
        returnPointer = &(((SgFunctionParameterRefExp*)(SgFunctionParameterRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionParameterRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionParameterRefExp::pools.begin(); block != SgFunctionParameterRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterRefExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionParameterRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionParameterRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionParameterRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionParameterRefExp::pools.begin(); block != SgFunctionParameterRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionParameterRefExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionParameterRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionParameterRefExp::clearMemoryPool() {
    SgFunctionParameterRefExp* tempPointer{nullptr};
    if (SgFunctionParameterRefExp::pools.empty() == false) {
        auto block{SgFunctionParameterRefExp::pools.begin()};
        SgFunctionParameterRefExp::next_node = reinterpret_cast<SgFunctionParameterRefExp*>(*block);
        for (; block != SgFunctionParameterRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionParameterRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionParameterRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionParameterRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionParameterRefExp::pool_size-1]);
        }
    }
}

void
SgFunctionParameterRefExp::deleteMemoryPool() {
    for (auto p: SgFunctionParameterRefExp::pools) {
        ROSE_FREE(p);
    }
    SgFunctionParameterRefExp::next_node = nullptr;
    SgFunctionParameterRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionParameterRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterRefExp)};
    for (size_t blockIndex = SgFunctionParameterRefExp::pools.size(); (blockIndex * SgFunctionParameterRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterRefExp) = %" PRIuPTR " SgFunctionParameterRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionParameterRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionParameterRefExp),SgFunctionParameterRefExp::pool_size);
        }
#endif

        SgFunctionParameterRefExp* pointer{static_cast<SgFunctionParameterRefExp*>(ROSE_MALLOC(SgFunctionParameterRefExp::pool_size * sizeof(SgFunctionParameterRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionParameterRefExp::pool_size * sizeof(SgFunctionParameterRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionParameterRefExp::pool_size * sizeof(SgFunctionParameterRefExp));
#endif
        SgFunctionParameterRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionParameterRefExp::pool_size * sizeof(SgFunctionParameterRefExp), V_SgFunctionParameterRefExp));

        if (SgFunctionParameterRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionParameterRefExp* blkptr{reinterpret_cast<SgFunctionParameterRefExp*>(SgFunctionParameterRefExp::pools[blockIndex-1])};
                blkptr[SgFunctionParameterRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionParameterRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionParameterRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionParameterRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionParameterRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionParameterRefExp::pool_size - 1};
    SgFunctionParameterRefExp* testPointer{reinterpret_cast<SgFunctionParameterRefExp*>(SgFunctionParameterRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionParameterRefExp::pool_size * (SgFunctionParameterRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionParameterRefExpStroageClass
 * from its counterpart of SgFunctionParameterRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionParameterRefExp::initializeStorageClassArray(SgFunctionParameterRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionParameterRefExp::pools.begin(); block != SgFunctionParameterRefExp::pools.end(); ++block) {
        SgFunctionParameterRefExp* pointer{reinterpret_cast<SgFunctionParameterRefExp*>(*block)};
        for (unsigned i = 0; i < SgFunctionParameterRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 150030 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLambdaExp that takes its corresponding StorageClass as parameter
 */
SgLambdaExp::SgLambdaExp(const SgLambdaExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 150040 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lambda_capture_list = (SgLambdaCaptureList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lambda_capture_list));
     p_lambda_closure_class = (SgClassDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lambda_closure_class));
     p_lambda_function = (SgFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lambda_function));
    p_is_mutable = storageSource.storageOf_is_mutable;
    p_capture_default = storageSource.storageOf_capture_default;
    p_default_is_by_reference = storageSource.storageOf_default_is_by_reference;
    p_explicit_return_type = storageSource.storageOf_explicit_return_type;
    p_has_parameter_decl = storageSource.storageOf_has_parameter_decl;
    p_is_device = storageSource.storageOf_is_device;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLambdaExp*
SgLambdaExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLambdaExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLambdaExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaExp)};
        unsigned long positionInPool{localIndex % SgLambdaExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaExp::pool_size};
        returnPointer = &(((SgLambdaExp*)(SgLambdaExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLambdaExp*
SgLambdaExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLambdaExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLambdaExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLambdaExp)};
        unsigned long positionInPool{localIndex % SgLambdaExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLambdaExp::pool_size};
        returnPointer = &(((SgLambdaExp*)(SgLambdaExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLambdaExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLambdaExp::pools.begin(); block != SgLambdaExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaExp*>(*block)};
        for (unsigned i = 0; i < SgLambdaExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLambdaExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLambdaExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLambdaExp* pointerOfLinkedList{nullptr};
    for (auto block = SgLambdaExp::pools.begin(); block != SgLambdaExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLambdaExp*>(*block)};
        for (unsigned i = 0; i < SgLambdaExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLambdaExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLambdaExp::clearMemoryPool() {
    SgLambdaExp* tempPointer{nullptr};
    if (SgLambdaExp::pools.empty() == false) {
        auto block{SgLambdaExp::pools.begin()};
        SgLambdaExp::next_node = reinterpret_cast<SgLambdaExp*>(*block);
        for (; block != SgLambdaExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLambdaExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLambdaExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLambdaExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLambdaExp::pool_size-1]);
        }
    }
}

void
SgLambdaExp::deleteMemoryPool() {
    for (auto p: SgLambdaExp::pools) {
        ROSE_FREE(p);
    }
    SgLambdaExp::next_node = nullptr;
    SgLambdaExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLambdaExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaExp)};
    for (size_t blockIndex = SgLambdaExp::pools.size(); (blockIndex * SgLambdaExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaExp) = %" PRIuPTR " SgLambdaExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLambdaExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLambdaExp),SgLambdaExp::pool_size);
        }
#endif

        SgLambdaExp* pointer{static_cast<SgLambdaExp*>(ROSE_MALLOC(SgLambdaExp::pool_size * sizeof(SgLambdaExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLambdaExp::pool_size * sizeof(SgLambdaExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLambdaExp::pool_size * sizeof(SgLambdaExp));
#endif
        SgLambdaExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLambdaExp::pool_size * sizeof(SgLambdaExp), V_SgLambdaExp));

        if (SgLambdaExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLambdaExp* blkptr{reinterpret_cast<SgLambdaExp*>(SgLambdaExp::pools[blockIndex-1])};
                blkptr[SgLambdaExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLambdaExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLambdaExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLambdaExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLambdaExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLambdaExp::pool_size - 1};
    SgLambdaExp* testPointer{reinterpret_cast<SgLambdaExp*>(SgLambdaExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLambdaExp::pool_size * (SgLambdaExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLambdaExpStroageClass
 * from its counterpart of SgLambdaExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLambdaExp::initializeStorageClassArray(SgLambdaExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLambdaExp::pools.begin(); block != SgLambdaExp::pools.end(); ++block) {
        SgLambdaExp* pointer{reinterpret_cast<SgLambdaExp*>(*block)};
        for (unsigned i = 0; i < SgLambdaExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 150266 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgHereExp that takes its corresponding StorageClass as parameter
 */
SgHereExp::SgHereExp(const SgHereExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 150276 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression_type));
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgHereExp*
SgHereExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgHereExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHereExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHereExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgHereExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgHereExp)};
        unsigned long positionInPool{localIndex % SgHereExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgHereExp::pool_size};
        returnPointer = &(((SgHereExp*)(SgHereExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgHereExp*
SgHereExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgHereExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHereExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHereExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgHereExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgHereExp)};
        unsigned long positionInPool{localIndex % SgHereExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgHereExp::pool_size};
        returnPointer = &(((SgHereExp*)(SgHereExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgHereExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgHereExp::pools.begin(); block != SgHereExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgHereExp*>(*block)};
        for (unsigned i = 0; i < SgHereExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgHereExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgHereExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgHereExp* pointerOfLinkedList{nullptr};
    for (auto block = SgHereExp::pools.begin(); block != SgHereExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgHereExp*>(*block)};
        for (unsigned i = 0; i < SgHereExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgHereExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgHereExp::clearMemoryPool() {
    SgHereExp* tempPointer{nullptr};
    if (SgHereExp::pools.empty() == false) {
        auto block{SgHereExp::pools.begin()};
        SgHereExp::next_node = reinterpret_cast<SgHereExp*>(*block);
        for (; block != SgHereExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgHereExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgHereExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgHereExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgHereExp::pool_size-1]);
        }
    }
}

void
SgHereExp::deleteMemoryPool() {
    for (auto p: SgHereExp::pools) {
        ROSE_FREE(p);
    }
    SgHereExp::next_node = nullptr;
    SgHereExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgHereExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgHereExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgHereExp)};
    for (size_t blockIndex = SgHereExp::pools.size(); (blockIndex * SgHereExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgHereExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgHereExp) = %" PRIuPTR " SgHereExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgHereExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgHereExp),SgHereExp::pool_size);
        }
#endif

        SgHereExp* pointer{static_cast<SgHereExp*>(ROSE_MALLOC(SgHereExp::pool_size * sizeof(SgHereExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgHereExp::pool_size * sizeof(SgHereExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgHereExp::pool_size * sizeof(SgHereExp));
#endif
        SgHereExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgHereExp::pool_size * sizeof(SgHereExp), V_SgHereExp));

        if (SgHereExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgHereExp* blkptr{reinterpret_cast<SgHereExp*>(SgHereExp::pools[blockIndex-1])};
                blkptr[SgHereExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgHereExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgHereExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgHereExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgHereExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgHereExp::pool_size - 1};
    SgHereExp* testPointer{reinterpret_cast<SgHereExp*>(SgHereExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgHereExp::pool_size * (SgHereExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgHereExpStroageClass
 * from its counterpart of SgHereExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgHereExp::initializeStorageClassArray(SgHereExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgHereExp::pools.begin(); block != SgHereExp::pools.end(); ++block) {
        SgHereExp* pointer{reinterpret_cast<SgHereExp*>(*block)};
        for (unsigned i = 0; i < SgHereExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 150495 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAtExp that takes its corresponding StorageClass as parameter
 */
SgAtExp::SgAtExp(const SgAtExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 150505 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAtExp*
SgAtExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAtExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAtExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAtExp)};
        unsigned long positionInPool{localIndex % SgAtExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtExp::pool_size};
        returnPointer = &(((SgAtExp*)(SgAtExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAtExp*
SgAtExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAtExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAtExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAtExp)};
        unsigned long positionInPool{localIndex % SgAtExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAtExp::pool_size};
        returnPointer = &(((SgAtExp*)(SgAtExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAtExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAtExp::pools.begin(); block != SgAtExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtExp*>(*block)};
        for (unsigned i = 0; i < SgAtExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAtExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAtExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAtExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAtExp::pools.begin(); block != SgAtExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAtExp*>(*block)};
        for (unsigned i = 0; i < SgAtExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAtExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAtExp::clearMemoryPool() {
    SgAtExp* tempPointer{nullptr};
    if (SgAtExp::pools.empty() == false) {
        auto block{SgAtExp::pools.begin()};
        SgAtExp::next_node = reinterpret_cast<SgAtExp*>(*block);
        for (; block != SgAtExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAtExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAtExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAtExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAtExp::pool_size-1]);
        }
    }
}

void
SgAtExp::deleteMemoryPool() {
    for (auto p: SgAtExp::pools) {
        ROSE_FREE(p);
    }
    SgAtExp::next_node = nullptr;
    SgAtExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAtExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAtExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtExp)};
    for (size_t blockIndex = SgAtExp::pools.size(); (blockIndex * SgAtExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAtExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtExp) = %" PRIuPTR " SgAtExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAtExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAtExp),SgAtExp::pool_size);
        }
#endif

        SgAtExp* pointer{static_cast<SgAtExp*>(ROSE_MALLOC(SgAtExp::pool_size * sizeof(SgAtExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAtExp::pool_size * sizeof(SgAtExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAtExp::pool_size * sizeof(SgAtExp));
#endif
        SgAtExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAtExp::pool_size * sizeof(SgAtExp), V_SgAtExp));

        if (SgAtExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAtExp* blkptr{reinterpret_cast<SgAtExp*>(SgAtExp::pools[blockIndex-1])};
                blkptr[SgAtExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAtExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAtExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAtExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAtExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAtExp::pool_size - 1};
    SgAtExp* testPointer{reinterpret_cast<SgAtExp*>(SgAtExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAtExp::pool_size * (SgAtExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAtExpStroageClass
 * from its counterpart of SgAtExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAtExp::initializeStorageClassArray(SgAtExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAtExp::pools.begin(); block != SgAtExp::pools.end(); ++block) {
        SgAtExp* pointer{reinterpret_cast<SgAtExp*>(*block)};
        for (unsigned i = 0; i < SgAtExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 150724 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFinishExp that takes its corresponding StorageClass as parameter
 */
SgFinishExp::SgFinishExp(const SgFinishExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 150734 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_expression = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_expression));
     p_body = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFinishExp*
SgFinishExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFinishExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFinishExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFinishExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFinishExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishExp)};
        unsigned long positionInPool{localIndex % SgFinishExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFinishExp::pool_size};
        returnPointer = &(((SgFinishExp*)(SgFinishExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFinishExp*
SgFinishExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFinishExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFinishExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFinishExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFinishExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFinishExp)};
        unsigned long positionInPool{localIndex % SgFinishExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFinishExp::pool_size};
        returnPointer = &(((SgFinishExp*)(SgFinishExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFinishExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFinishExp::pools.begin(); block != SgFinishExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFinishExp*>(*block)};
        for (unsigned i = 0; i < SgFinishExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFinishExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFinishExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFinishExp* pointerOfLinkedList{nullptr};
    for (auto block = SgFinishExp::pools.begin(); block != SgFinishExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFinishExp*>(*block)};
        for (unsigned i = 0; i < SgFinishExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFinishExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFinishExp::clearMemoryPool() {
    SgFinishExp* tempPointer{nullptr};
    if (SgFinishExp::pools.empty() == false) {
        auto block{SgFinishExp::pools.begin()};
        SgFinishExp::next_node = reinterpret_cast<SgFinishExp*>(*block);
        for (; block != SgFinishExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFinishExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFinishExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFinishExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFinishExp::pool_size-1]);
        }
    }
}

void
SgFinishExp::deleteMemoryPool() {
    for (auto p: SgFinishExp::pools) {
        ROSE_FREE(p);
    }
    SgFinishExp::next_node = nullptr;
    SgFinishExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFinishExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFinishExp)};
    for (size_t blockIndex = SgFinishExp::pools.size(); (blockIndex * SgFinishExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFinishExp) = %" PRIuPTR " SgFinishExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFinishExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFinishExp),SgFinishExp::pool_size);
        }
#endif

        SgFinishExp* pointer{static_cast<SgFinishExp*>(ROSE_MALLOC(SgFinishExp::pool_size * sizeof(SgFinishExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFinishExp::pool_size * sizeof(SgFinishExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFinishExp::pool_size * sizeof(SgFinishExp));
#endif
        SgFinishExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFinishExp::pool_size * sizeof(SgFinishExp), V_SgFinishExp));

        if (SgFinishExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFinishExp* blkptr{reinterpret_cast<SgFinishExp*>(SgFinishExp::pools[blockIndex-1])};
                blkptr[SgFinishExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFinishExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFinishExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFinishExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFinishExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFinishExp::pool_size - 1};
    SgFinishExp* testPointer{reinterpret_cast<SgFinishExp*>(SgFinishExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFinishExp::pool_size * (SgFinishExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFinishExpStroageClass
 * from its counterpart of SgFinishExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFinishExp::initializeStorageClassArray(SgFinishExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFinishExp::pools.begin(); block != SgFinishExp::pools.end(); ++block) {
        SgFinishExp* pointer{reinterpret_cast<SgFinishExp*>(*block)};
        for (unsigned i = 0; i < SgFinishExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 150953 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNoexceptOp that takes its corresponding StorageClass as parameter
 */
SgNoexceptOp::SgNoexceptOp(const SgNoexceptOpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 150963 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand_expr = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand_expr));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNoexceptOp*
SgNoexceptOp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNoexceptOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNoexceptOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNoexceptOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNoexceptOp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNoexceptOp)};
        unsigned long positionInPool{localIndex % SgNoexceptOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNoexceptOp::pool_size};
        returnPointer = &(((SgNoexceptOp*)(SgNoexceptOp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNoexceptOp*
SgNoexceptOp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNoexceptOp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNoexceptOp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNoexceptOp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNoexceptOp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNoexceptOp)};
        unsigned long positionInPool{localIndex % SgNoexceptOp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNoexceptOp::pool_size};
        returnPointer = &(((SgNoexceptOp*)(SgNoexceptOp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNoexceptOp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNoexceptOp::pools.begin(); block != SgNoexceptOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNoexceptOp*>(*block)};
        for (unsigned i = 0; i < SgNoexceptOp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNoexceptOp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNoexceptOp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNoexceptOp* pointerOfLinkedList{nullptr};
    for (auto block = SgNoexceptOp::pools.begin(); block != SgNoexceptOp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNoexceptOp*>(*block)};
        for (unsigned i = 0; i < SgNoexceptOp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNoexceptOp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNoexceptOp::clearMemoryPool() {
    SgNoexceptOp* tempPointer{nullptr};
    if (SgNoexceptOp::pools.empty() == false) {
        auto block{SgNoexceptOp::pools.begin()};
        SgNoexceptOp::next_node = reinterpret_cast<SgNoexceptOp*>(*block);
        for (; block != SgNoexceptOp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNoexceptOp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNoexceptOp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNoexceptOp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNoexceptOp::pool_size-1]);
        }
    }
}

void
SgNoexceptOp::deleteMemoryPool() {
    for (auto p: SgNoexceptOp::pools) {
        ROSE_FREE(p);
    }
    SgNoexceptOp::next_node = nullptr;
    SgNoexceptOp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNoexceptOp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNoexceptOp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNoexceptOp)};
    for (size_t blockIndex = SgNoexceptOp::pools.size(); (blockIndex * SgNoexceptOp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNoexceptOp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNoexceptOp) = %" PRIuPTR " SgNoexceptOp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNoexceptOp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNoexceptOp),SgNoexceptOp::pool_size);
        }
#endif

        SgNoexceptOp* pointer{static_cast<SgNoexceptOp*>(ROSE_MALLOC(SgNoexceptOp::pool_size * sizeof(SgNoexceptOp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNoexceptOp::pool_size * sizeof(SgNoexceptOp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNoexceptOp::pool_size * sizeof(SgNoexceptOp));
#endif
        SgNoexceptOp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNoexceptOp::pool_size * sizeof(SgNoexceptOp), V_SgNoexceptOp));

        if (SgNoexceptOp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNoexceptOp* blkptr{reinterpret_cast<SgNoexceptOp*>(SgNoexceptOp::pools[blockIndex-1])};
                blkptr[SgNoexceptOp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNoexceptOp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNoexceptOp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNoexceptOp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNoexceptOp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNoexceptOp::pool_size - 1};
    SgNoexceptOp* testPointer{reinterpret_cast<SgNoexceptOp*>(SgNoexceptOp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNoexceptOp::pool_size * (SgNoexceptOp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNoexceptOpStroageClass
 * from its counterpart of SgNoexceptOp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNoexceptOp::initializeStorageClassArray(SgNoexceptOpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNoexceptOp::pools.begin(); block != SgNoexceptOp::pools.end(); ++block) {
        SgNoexceptOp* pointer{reinterpret_cast<SgNoexceptOp*>(*block)};
        for (unsigned i = 0; i < SgNoexceptOp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 151181 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskRefExp that takes its corresponding StorageClass as parameter
 */
SgAdaTaskRefExp::SgAdaTaskRefExp(const SgAdaTaskRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 151191 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgAdaTaskSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskRefExp*
SgAdaTaskRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskRefExp)};
        unsigned long positionInPool{localIndex % SgAdaTaskRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskRefExp::pool_size};
        returnPointer = &(((SgAdaTaskRefExp*)(SgAdaTaskRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskRefExp*
SgAdaTaskRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskRefExp)};
        unsigned long positionInPool{localIndex % SgAdaTaskRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskRefExp::pool_size};
        returnPointer = &(((SgAdaTaskRefExp*)(SgAdaTaskRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskRefExp::pools.begin(); block != SgAdaTaskRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskRefExp::pools.begin(); block != SgAdaTaskRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskRefExp::clearMemoryPool() {
    SgAdaTaskRefExp* tempPointer{nullptr};
    if (SgAdaTaskRefExp::pools.empty() == false) {
        auto block{SgAdaTaskRefExp::pools.begin()};
        SgAdaTaskRefExp::next_node = reinterpret_cast<SgAdaTaskRefExp*>(*block);
        for (; block != SgAdaTaskRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskRefExp::pool_size-1]);
        }
    }
}

void
SgAdaTaskRefExp::deleteMemoryPool() {
    for (auto p: SgAdaTaskRefExp::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskRefExp::next_node = nullptr;
    SgAdaTaskRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskRefExp)};
    for (size_t blockIndex = SgAdaTaskRefExp::pools.size(); (blockIndex * SgAdaTaskRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskRefExp) = %" PRIuPTR " SgAdaTaskRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskRefExp),SgAdaTaskRefExp::pool_size);
        }
#endif

        SgAdaTaskRefExp* pointer{static_cast<SgAdaTaskRefExp*>(ROSE_MALLOC(SgAdaTaskRefExp::pool_size * sizeof(SgAdaTaskRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskRefExp::pool_size * sizeof(SgAdaTaskRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskRefExp::pool_size * sizeof(SgAdaTaskRefExp));
#endif
        SgAdaTaskRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskRefExp::pool_size * sizeof(SgAdaTaskRefExp), V_SgAdaTaskRefExp));

        if (SgAdaTaskRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskRefExp* blkptr{reinterpret_cast<SgAdaTaskRefExp*>(SgAdaTaskRefExp::pools[blockIndex-1])};
                blkptr[SgAdaTaskRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskRefExp::pool_size - 1};
    SgAdaTaskRefExp* testPointer{reinterpret_cast<SgAdaTaskRefExp*>(SgAdaTaskRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskRefExp::pool_size * (SgAdaTaskRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskRefExpStroageClass
 * from its counterpart of SgAdaTaskRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskRefExp::initializeStorageClassArray(SgAdaTaskRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskRefExp::pools.begin(); block != SgAdaTaskRefExp::pools.end(); ++block) {
        SgAdaTaskRefExp* pointer{reinterpret_cast<SgAdaTaskRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 151409 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedRefExp that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedRefExp::SgAdaProtectedRefExp(const SgAdaProtectedRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 151419 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgAdaProtectedSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedRefExp*
SgAdaProtectedRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedRefExp)};
        unsigned long positionInPool{localIndex % SgAdaProtectedRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedRefExp::pool_size};
        returnPointer = &(((SgAdaProtectedRefExp*)(SgAdaProtectedRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedRefExp*
SgAdaProtectedRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedRefExp)};
        unsigned long positionInPool{localIndex % SgAdaProtectedRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedRefExp::pool_size};
        returnPointer = &(((SgAdaProtectedRefExp*)(SgAdaProtectedRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedRefExp::pools.begin(); block != SgAdaProtectedRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedRefExp::pools.begin(); block != SgAdaProtectedRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedRefExp::clearMemoryPool() {
    SgAdaProtectedRefExp* tempPointer{nullptr};
    if (SgAdaProtectedRefExp::pools.empty() == false) {
        auto block{SgAdaProtectedRefExp::pools.begin()};
        SgAdaProtectedRefExp::next_node = reinterpret_cast<SgAdaProtectedRefExp*>(*block);
        for (; block != SgAdaProtectedRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedRefExp::pool_size-1]);
        }
    }
}

void
SgAdaProtectedRefExp::deleteMemoryPool() {
    for (auto p: SgAdaProtectedRefExp::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedRefExp::next_node = nullptr;
    SgAdaProtectedRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedRefExp)};
    for (size_t blockIndex = SgAdaProtectedRefExp::pools.size(); (blockIndex * SgAdaProtectedRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedRefExp) = %" PRIuPTR " SgAdaProtectedRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedRefExp),SgAdaProtectedRefExp::pool_size);
        }
#endif

        SgAdaProtectedRefExp* pointer{static_cast<SgAdaProtectedRefExp*>(ROSE_MALLOC(SgAdaProtectedRefExp::pool_size * sizeof(SgAdaProtectedRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedRefExp::pool_size * sizeof(SgAdaProtectedRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedRefExp::pool_size * sizeof(SgAdaProtectedRefExp));
#endif
        SgAdaProtectedRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedRefExp::pool_size * sizeof(SgAdaProtectedRefExp), V_SgAdaProtectedRefExp));

        if (SgAdaProtectedRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedRefExp* blkptr{reinterpret_cast<SgAdaProtectedRefExp*>(SgAdaProtectedRefExp::pools[blockIndex-1])};
                blkptr[SgAdaProtectedRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedRefExp::pool_size - 1};
    SgAdaProtectedRefExp* testPointer{reinterpret_cast<SgAdaProtectedRefExp*>(SgAdaProtectedRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedRefExp::pool_size * (SgAdaProtectedRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedRefExpStroageClass
 * from its counterpart of SgAdaProtectedRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedRefExp::initializeStorageClassArray(SgAdaProtectedRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedRefExp::pools.begin(); block != SgAdaProtectedRefExp::pools.end(); ++block) {
        SgAdaProtectedRefExp* pointer{reinterpret_cast<SgAdaProtectedRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 151637 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFoldExpression that takes its corresponding StorageClass as parameter
 */
SgFoldExpression::SgFoldExpression(const SgFoldExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 151647 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operands = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operands));
    p_operator_token = storageSource.storageOf_operator_token.rebuildDataStoredInEasyStorageClass();
    p_is_left_associative = storageSource.storageOf_is_left_associative;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFoldExpression*
SgFoldExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFoldExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFoldExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFoldExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFoldExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFoldExpression)};
        unsigned long positionInPool{localIndex % SgFoldExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFoldExpression::pool_size};
        returnPointer = &(((SgFoldExpression*)(SgFoldExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFoldExpression*
SgFoldExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFoldExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFoldExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFoldExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFoldExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFoldExpression)};
        unsigned long positionInPool{localIndex % SgFoldExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFoldExpression::pool_size};
        returnPointer = &(((SgFoldExpression*)(SgFoldExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFoldExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFoldExpression::pools.begin(); block != SgFoldExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFoldExpression*>(*block)};
        for (unsigned i = 0; i < SgFoldExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFoldExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFoldExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFoldExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgFoldExpression::pools.begin(); block != SgFoldExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFoldExpression*>(*block)};
        for (unsigned i = 0; i < SgFoldExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFoldExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFoldExpression::clearMemoryPool() {
    SgFoldExpression* tempPointer{nullptr};
    if (SgFoldExpression::pools.empty() == false) {
        auto block{SgFoldExpression::pools.begin()};
        SgFoldExpression::next_node = reinterpret_cast<SgFoldExpression*>(*block);
        for (; block != SgFoldExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFoldExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFoldExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFoldExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFoldExpression::pool_size-1]);
        }
    }
}

void
SgFoldExpression::deleteMemoryPool() {
    for (auto p: SgFoldExpression::pools) {
        ROSE_FREE(p);
    }
    SgFoldExpression::next_node = nullptr;
    SgFoldExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFoldExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFoldExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFoldExpression)};
    for (size_t blockIndex = SgFoldExpression::pools.size(); (blockIndex * SgFoldExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFoldExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFoldExpression) = %" PRIuPTR " SgFoldExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFoldExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFoldExpression),SgFoldExpression::pool_size);
        }
#endif

        SgFoldExpression* pointer{static_cast<SgFoldExpression*>(ROSE_MALLOC(SgFoldExpression::pool_size * sizeof(SgFoldExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFoldExpression::pool_size * sizeof(SgFoldExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFoldExpression::pool_size * sizeof(SgFoldExpression));
#endif
        SgFoldExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFoldExpression::pool_size * sizeof(SgFoldExpression), V_SgFoldExpression));

        if (SgFoldExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFoldExpression* blkptr{reinterpret_cast<SgFoldExpression*>(SgFoldExpression::pools[blockIndex-1])};
                blkptr[SgFoldExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFoldExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFoldExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFoldExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFoldExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFoldExpression::pool_size - 1};
    SgFoldExpression* testPointer{reinterpret_cast<SgFoldExpression*>(SgFoldExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFoldExpression::pool_size * (SgFoldExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFoldExpressionStroageClass
 * from its counterpart of SgFoldExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFoldExpression::initializeStorageClassArray(SgFoldExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFoldExpression::pools.begin(); block != SgFoldExpression::pools.end(); ++block) {
        SgFoldExpression* pointer{reinterpret_cast<SgFoldExpression*>(*block)};
        for (unsigned i = 0; i < SgFoldExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 151867 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAwaitExpression that takes its corresponding StorageClass as parameter
 */
SgAwaitExpression::SgAwaitExpression(const SgAwaitExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 151877 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAwaitExpression*
SgAwaitExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAwaitExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAwaitExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAwaitExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAwaitExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAwaitExpression)};
        unsigned long positionInPool{localIndex % SgAwaitExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAwaitExpression::pool_size};
        returnPointer = &(((SgAwaitExpression*)(SgAwaitExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAwaitExpression*
SgAwaitExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAwaitExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAwaitExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAwaitExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAwaitExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAwaitExpression)};
        unsigned long positionInPool{localIndex % SgAwaitExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAwaitExpression::pool_size};
        returnPointer = &(((SgAwaitExpression*)(SgAwaitExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAwaitExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAwaitExpression::pools.begin(); block != SgAwaitExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAwaitExpression*>(*block)};
        for (unsigned i = 0; i < SgAwaitExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAwaitExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAwaitExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAwaitExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAwaitExpression::pools.begin(); block != SgAwaitExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAwaitExpression*>(*block)};
        for (unsigned i = 0; i < SgAwaitExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAwaitExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAwaitExpression::clearMemoryPool() {
    SgAwaitExpression* tempPointer{nullptr};
    if (SgAwaitExpression::pools.empty() == false) {
        auto block{SgAwaitExpression::pools.begin()};
        SgAwaitExpression::next_node = reinterpret_cast<SgAwaitExpression*>(*block);
        for (; block != SgAwaitExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAwaitExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAwaitExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAwaitExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAwaitExpression::pool_size-1]);
        }
    }
}

void
SgAwaitExpression::deleteMemoryPool() {
    for (auto p: SgAwaitExpression::pools) {
        ROSE_FREE(p);
    }
    SgAwaitExpression::next_node = nullptr;
    SgAwaitExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAwaitExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAwaitExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAwaitExpression)};
    for (size_t blockIndex = SgAwaitExpression::pools.size(); (blockIndex * SgAwaitExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAwaitExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAwaitExpression) = %" PRIuPTR " SgAwaitExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAwaitExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAwaitExpression),SgAwaitExpression::pool_size);
        }
#endif

        SgAwaitExpression* pointer{static_cast<SgAwaitExpression*>(ROSE_MALLOC(SgAwaitExpression::pool_size * sizeof(SgAwaitExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAwaitExpression::pool_size * sizeof(SgAwaitExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAwaitExpression::pool_size * sizeof(SgAwaitExpression));
#endif
        SgAwaitExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAwaitExpression::pool_size * sizeof(SgAwaitExpression), V_SgAwaitExpression));

        if (SgAwaitExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAwaitExpression* blkptr{reinterpret_cast<SgAwaitExpression*>(SgAwaitExpression::pools[blockIndex-1])};
                blkptr[SgAwaitExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAwaitExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAwaitExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAwaitExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAwaitExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAwaitExpression::pool_size - 1};
    SgAwaitExpression* testPointer{reinterpret_cast<SgAwaitExpression*>(SgAwaitExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAwaitExpression::pool_size * (SgAwaitExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAwaitExpressionStroageClass
 * from its counterpart of SgAwaitExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAwaitExpression::initializeStorageClassArray(SgAwaitExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAwaitExpression::pools.begin(); block != SgAwaitExpression::pools.end(); ++block) {
        SgAwaitExpression* pointer{reinterpret_cast<SgAwaitExpression*>(*block)};
        for (unsigned i = 0; i < SgAwaitExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 152095 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgChooseExpression that takes its corresponding StorageClass as parameter
 */
SgChooseExpression::SgChooseExpression(const SgChooseExpressionStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 152105 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgChooseExpression*
SgChooseExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgChooseExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChooseExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChooseExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgChooseExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgChooseExpression)};
        unsigned long positionInPool{localIndex % SgChooseExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgChooseExpression::pool_size};
        returnPointer = &(((SgChooseExpression*)(SgChooseExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgChooseExpression*
SgChooseExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgChooseExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChooseExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChooseExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgChooseExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgChooseExpression)};
        unsigned long positionInPool{localIndex % SgChooseExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgChooseExpression::pool_size};
        returnPointer = &(((SgChooseExpression*)(SgChooseExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgChooseExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgChooseExpression::pools.begin(); block != SgChooseExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgChooseExpression*>(*block)};
        for (unsigned i = 0; i < SgChooseExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgChooseExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgChooseExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgChooseExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgChooseExpression::pools.begin(); block != SgChooseExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgChooseExpression*>(*block)};
        for (unsigned i = 0; i < SgChooseExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgChooseExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgChooseExpression::clearMemoryPool() {
    SgChooseExpression* tempPointer{nullptr};
    if (SgChooseExpression::pools.empty() == false) {
        auto block{SgChooseExpression::pools.begin()};
        SgChooseExpression::next_node = reinterpret_cast<SgChooseExpression*>(*block);
        for (; block != SgChooseExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgChooseExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgChooseExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgChooseExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgChooseExpression::pool_size-1]);
        }
    }
}

void
SgChooseExpression::deleteMemoryPool() {
    for (auto p: SgChooseExpression::pools) {
        ROSE_FREE(p);
    }
    SgChooseExpression::next_node = nullptr;
    SgChooseExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgChooseExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgChooseExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgChooseExpression)};
    for (size_t blockIndex = SgChooseExpression::pools.size(); (blockIndex * SgChooseExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgChooseExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgChooseExpression) = %" PRIuPTR " SgChooseExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgChooseExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgChooseExpression),SgChooseExpression::pool_size);
        }
#endif

        SgChooseExpression* pointer{static_cast<SgChooseExpression*>(ROSE_MALLOC(SgChooseExpression::pool_size * sizeof(SgChooseExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgChooseExpression::pool_size * sizeof(SgChooseExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgChooseExpression::pool_size * sizeof(SgChooseExpression));
#endif
        SgChooseExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgChooseExpression::pool_size * sizeof(SgChooseExpression), V_SgChooseExpression));

        if (SgChooseExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgChooseExpression* blkptr{reinterpret_cast<SgChooseExpression*>(SgChooseExpression::pools[blockIndex-1])};
                blkptr[SgChooseExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgChooseExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgChooseExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgChooseExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgChooseExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgChooseExpression::pool_size - 1};
    SgChooseExpression* testPointer{reinterpret_cast<SgChooseExpression*>(SgChooseExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgChooseExpression::pool_size * (SgChooseExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgChooseExpressionStroageClass
 * from its counterpart of SgChooseExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgChooseExpression::initializeStorageClassArray(SgChooseExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgChooseExpression::pools.begin(); block != SgChooseExpression::pools.end(); ++block) {
        SgChooseExpression* pointer{reinterpret_cast<SgChooseExpression*>(*block)};
        for (unsigned i = 0; i < SgChooseExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 152323 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaAttributeExp that takes its corresponding StorageClass as parameter
 */
SgAdaAttributeExp::SgAdaAttributeExp(const SgAdaAttributeExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 152333 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_attribute = SgName ( storageSource.storageOf_attribute);
     p_object = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_object));
     p_args = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_args));
     p_attrType = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_attrType));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaAttributeExp*
SgAdaAttributeExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaAttributeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAttributeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAttributeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaAttributeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeExp)};
        unsigned long positionInPool{localIndex % SgAdaAttributeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAttributeExp::pool_size};
        returnPointer = &(((SgAdaAttributeExp*)(SgAdaAttributeExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaAttributeExp*
SgAdaAttributeExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaAttributeExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAttributeExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAttributeExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaAttributeExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaAttributeExp)};
        unsigned long positionInPool{localIndex % SgAdaAttributeExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaAttributeExp::pool_size};
        returnPointer = &(((SgAdaAttributeExp*)(SgAdaAttributeExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaAttributeExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaAttributeExp::pools.begin(); block != SgAdaAttributeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAttributeExp*>(*block)};
        for (unsigned i = 0; i < SgAdaAttributeExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaAttributeExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaAttributeExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaAttributeExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaAttributeExp::pools.begin(); block != SgAdaAttributeExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaAttributeExp*>(*block)};
        for (unsigned i = 0; i < SgAdaAttributeExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaAttributeExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaAttributeExp::clearMemoryPool() {
    SgAdaAttributeExp* tempPointer{nullptr};
    if (SgAdaAttributeExp::pools.empty() == false) {
        auto block{SgAdaAttributeExp::pools.begin()};
        SgAdaAttributeExp::next_node = reinterpret_cast<SgAdaAttributeExp*>(*block);
        for (; block != SgAdaAttributeExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaAttributeExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaAttributeExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaAttributeExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaAttributeExp::pool_size-1]);
        }
    }
}

void
SgAdaAttributeExp::deleteMemoryPool() {
    for (auto p: SgAdaAttributeExp::pools) {
        ROSE_FREE(p);
    }
    SgAdaAttributeExp::next_node = nullptr;
    SgAdaAttributeExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaAttributeExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAttributeExp)};
    for (size_t blockIndex = SgAdaAttributeExp::pools.size(); (blockIndex * SgAdaAttributeExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAttributeExp) = %" PRIuPTR " SgAdaAttributeExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaAttributeExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaAttributeExp),SgAdaAttributeExp::pool_size);
        }
#endif

        SgAdaAttributeExp* pointer{static_cast<SgAdaAttributeExp*>(ROSE_MALLOC(SgAdaAttributeExp::pool_size * sizeof(SgAdaAttributeExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaAttributeExp::pool_size * sizeof(SgAdaAttributeExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaAttributeExp::pool_size * sizeof(SgAdaAttributeExp));
#endif
        SgAdaAttributeExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaAttributeExp::pool_size * sizeof(SgAdaAttributeExp), V_SgAdaAttributeExp));

        if (SgAdaAttributeExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaAttributeExp* blkptr{reinterpret_cast<SgAdaAttributeExp*>(SgAdaAttributeExp::pools[blockIndex-1])};
                blkptr[SgAdaAttributeExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaAttributeExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaAttributeExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaAttributeExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaAttributeExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaAttributeExp::pool_size - 1};
    SgAdaAttributeExp* testPointer{reinterpret_cast<SgAdaAttributeExp*>(SgAdaAttributeExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaAttributeExp::pool_size * (SgAdaAttributeExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaAttributeExpStroageClass
 * from its counterpart of SgAdaAttributeExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaAttributeExp::initializeStorageClassArray(SgAdaAttributeExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaAttributeExp::pools.begin(); block != SgAdaAttributeExp::pools.end(); ++block) {
        SgAdaAttributeExp* pointer{reinterpret_cast<SgAdaAttributeExp*>(*block)};
        for (unsigned i = 0; i < SgAdaAttributeExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 152554 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialTablePresetExp that takes its corresponding StorageClass as parameter
 */
SgJovialTablePresetExp::SgJovialTablePresetExp(const SgJovialTablePresetExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 152564 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_preset_list = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_preset_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialTablePresetExp*
SgJovialTablePresetExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialTablePresetExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTablePresetExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTablePresetExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialTablePresetExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTablePresetExp)};
        unsigned long positionInPool{localIndex % SgJovialTablePresetExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialTablePresetExp::pool_size};
        returnPointer = &(((SgJovialTablePresetExp*)(SgJovialTablePresetExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialTablePresetExp*
SgJovialTablePresetExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialTablePresetExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTablePresetExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTablePresetExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialTablePresetExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialTablePresetExp)};
        unsigned long positionInPool{localIndex % SgJovialTablePresetExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialTablePresetExp::pool_size};
        returnPointer = &(((SgJovialTablePresetExp*)(SgJovialTablePresetExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialTablePresetExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialTablePresetExp::pools.begin(); block != SgJovialTablePresetExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialTablePresetExp*>(*block)};
        for (unsigned i = 0; i < SgJovialTablePresetExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialTablePresetExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialTablePresetExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialTablePresetExp* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialTablePresetExp::pools.begin(); block != SgJovialTablePresetExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialTablePresetExp*>(*block)};
        for (unsigned i = 0; i < SgJovialTablePresetExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialTablePresetExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialTablePresetExp::clearMemoryPool() {
    SgJovialTablePresetExp* tempPointer{nullptr};
    if (SgJovialTablePresetExp::pools.empty() == false) {
        auto block{SgJovialTablePresetExp::pools.begin()};
        SgJovialTablePresetExp::next_node = reinterpret_cast<SgJovialTablePresetExp*>(*block);
        for (; block != SgJovialTablePresetExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialTablePresetExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialTablePresetExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialTablePresetExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialTablePresetExp::pool_size-1]);
        }
    }
}

void
SgJovialTablePresetExp::deleteMemoryPool() {
    for (auto p: SgJovialTablePresetExp::pools) {
        ROSE_FREE(p);
    }
    SgJovialTablePresetExp::next_node = nullptr;
    SgJovialTablePresetExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialTablePresetExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTablePresetExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTablePresetExp)};
    for (size_t blockIndex = SgJovialTablePresetExp::pools.size(); (blockIndex * SgJovialTablePresetExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTablePresetExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTablePresetExp) = %" PRIuPTR " SgJovialTablePresetExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialTablePresetExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialTablePresetExp),SgJovialTablePresetExp::pool_size);
        }
#endif

        SgJovialTablePresetExp* pointer{static_cast<SgJovialTablePresetExp*>(ROSE_MALLOC(SgJovialTablePresetExp::pool_size * sizeof(SgJovialTablePresetExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialTablePresetExp::pool_size * sizeof(SgJovialTablePresetExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialTablePresetExp::pool_size * sizeof(SgJovialTablePresetExp));
#endif
        SgJovialTablePresetExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialTablePresetExp::pool_size * sizeof(SgJovialTablePresetExp), V_SgJovialTablePresetExp));

        if (SgJovialTablePresetExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialTablePresetExp* blkptr{reinterpret_cast<SgJovialTablePresetExp*>(SgJovialTablePresetExp::pools[blockIndex-1])};
                blkptr[SgJovialTablePresetExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialTablePresetExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialTablePresetExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialTablePresetExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialTablePresetExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialTablePresetExp::pool_size - 1};
    SgJovialTablePresetExp* testPointer{reinterpret_cast<SgJovialTablePresetExp*>(SgJovialTablePresetExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialTablePresetExp::pool_size * (SgJovialTablePresetExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialTablePresetExpStroageClass
 * from its counterpart of SgJovialTablePresetExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialTablePresetExp::initializeStorageClassArray(SgJovialTablePresetExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialTablePresetExp::pools.begin(); block != SgJovialTablePresetExp::pools.end(); ++block) {
        SgJovialTablePresetExp* pointer{reinterpret_cast<SgJovialTablePresetExp*>(*block)};
        for (unsigned i = 0; i < SgJovialTablePresetExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 152782 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJovialPresetPositionExp that takes its corresponding StorageClass as parameter
 */
SgJovialPresetPositionExp::SgJovialPresetPositionExp(const SgJovialPresetPositionExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 152792 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_indices = (SgExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_indices));
     p_value = (SgExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJovialPresetPositionExp*
SgJovialPresetPositionExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJovialPresetPositionExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialPresetPositionExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialPresetPositionExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJovialPresetPositionExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialPresetPositionExp)};
        unsigned long positionInPool{localIndex % SgJovialPresetPositionExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialPresetPositionExp::pool_size};
        returnPointer = &(((SgJovialPresetPositionExp*)(SgJovialPresetPositionExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJovialPresetPositionExp*
SgJovialPresetPositionExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJovialPresetPositionExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialPresetPositionExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialPresetPositionExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJovialPresetPositionExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJovialPresetPositionExp)};
        unsigned long positionInPool{localIndex % SgJovialPresetPositionExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJovialPresetPositionExp::pool_size};
        returnPointer = &(((SgJovialPresetPositionExp*)(SgJovialPresetPositionExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJovialPresetPositionExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJovialPresetPositionExp::pools.begin(); block != SgJovialPresetPositionExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialPresetPositionExp*>(*block)};
        for (unsigned i = 0; i < SgJovialPresetPositionExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJovialPresetPositionExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJovialPresetPositionExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJovialPresetPositionExp* pointerOfLinkedList{nullptr};
    for (auto block = SgJovialPresetPositionExp::pools.begin(); block != SgJovialPresetPositionExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJovialPresetPositionExp*>(*block)};
        for (unsigned i = 0; i < SgJovialPresetPositionExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJovialPresetPositionExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJovialPresetPositionExp::clearMemoryPool() {
    SgJovialPresetPositionExp* tempPointer{nullptr};
    if (SgJovialPresetPositionExp::pools.empty() == false) {
        auto block{SgJovialPresetPositionExp::pools.begin()};
        SgJovialPresetPositionExp::next_node = reinterpret_cast<SgJovialPresetPositionExp*>(*block);
        for (; block != SgJovialPresetPositionExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJovialPresetPositionExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJovialPresetPositionExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJovialPresetPositionExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJovialPresetPositionExp::pool_size-1]);
        }
    }
}

void
SgJovialPresetPositionExp::deleteMemoryPool() {
    for (auto p: SgJovialPresetPositionExp::pools) {
        ROSE_FREE(p);
    }
    SgJovialPresetPositionExp::next_node = nullptr;
    SgJovialPresetPositionExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJovialPresetPositionExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialPresetPositionExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialPresetPositionExp)};
    for (size_t blockIndex = SgJovialPresetPositionExp::pools.size(); (blockIndex * SgJovialPresetPositionExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialPresetPositionExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialPresetPositionExp) = %" PRIuPTR " SgJovialPresetPositionExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJovialPresetPositionExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJovialPresetPositionExp),SgJovialPresetPositionExp::pool_size);
        }
#endif

        SgJovialPresetPositionExp* pointer{static_cast<SgJovialPresetPositionExp*>(ROSE_MALLOC(SgJovialPresetPositionExp::pool_size * sizeof(SgJovialPresetPositionExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJovialPresetPositionExp::pool_size * sizeof(SgJovialPresetPositionExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJovialPresetPositionExp::pool_size * sizeof(SgJovialPresetPositionExp));
#endif
        SgJovialPresetPositionExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJovialPresetPositionExp::pool_size * sizeof(SgJovialPresetPositionExp), V_SgJovialPresetPositionExp));

        if (SgJovialPresetPositionExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJovialPresetPositionExp* blkptr{reinterpret_cast<SgJovialPresetPositionExp*>(SgJovialPresetPositionExp::pools[blockIndex-1])};
                blkptr[SgJovialPresetPositionExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJovialPresetPositionExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJovialPresetPositionExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJovialPresetPositionExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJovialPresetPositionExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJovialPresetPositionExp::pool_size - 1};
    SgJovialPresetPositionExp* testPointer{reinterpret_cast<SgJovialPresetPositionExp*>(SgJovialPresetPositionExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJovialPresetPositionExp::pool_size * (SgJovialPresetPositionExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJovialPresetPositionExpStroageClass
 * from its counterpart of SgJovialPresetPositionExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJovialPresetPositionExp::initializeStorageClassArray(SgJovialPresetPositionExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJovialPresetPositionExp::pools.begin(); block != SgJovialPresetPositionExp::pools.end(); ++block) {
        SgJovialPresetPositionExp* pointer{reinterpret_cast<SgJovialPresetPositionExp*>(*block)};
        for (unsigned i = 0; i < SgJovialPresetPositionExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 153011 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaOthersExp that takes its corresponding StorageClass as parameter
 */
SgAdaOthersExp::SgAdaOthersExp(const SgAdaOthersExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 153021 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaOthersExp*
SgAdaOthersExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaOthersExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaOthersExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaOthersExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaOthersExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaOthersExp)};
        unsigned long positionInPool{localIndex % SgAdaOthersExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaOthersExp::pool_size};
        returnPointer = &(((SgAdaOthersExp*)(SgAdaOthersExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaOthersExp*
SgAdaOthersExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaOthersExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaOthersExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaOthersExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaOthersExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaOthersExp)};
        unsigned long positionInPool{localIndex % SgAdaOthersExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaOthersExp::pool_size};
        returnPointer = &(((SgAdaOthersExp*)(SgAdaOthersExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaOthersExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaOthersExp::pools.begin(); block != SgAdaOthersExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaOthersExp*>(*block)};
        for (unsigned i = 0; i < SgAdaOthersExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaOthersExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaOthersExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaOthersExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaOthersExp::pools.begin(); block != SgAdaOthersExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaOthersExp*>(*block)};
        for (unsigned i = 0; i < SgAdaOthersExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaOthersExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaOthersExp::clearMemoryPool() {
    SgAdaOthersExp* tempPointer{nullptr};
    if (SgAdaOthersExp::pools.empty() == false) {
        auto block{SgAdaOthersExp::pools.begin()};
        SgAdaOthersExp::next_node = reinterpret_cast<SgAdaOthersExp*>(*block);
        for (; block != SgAdaOthersExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaOthersExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaOthersExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaOthersExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaOthersExp::pool_size-1]);
        }
    }
}

void
SgAdaOthersExp::deleteMemoryPool() {
    for (auto p: SgAdaOthersExp::pools) {
        ROSE_FREE(p);
    }
    SgAdaOthersExp::next_node = nullptr;
    SgAdaOthersExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaOthersExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaOthersExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaOthersExp)};
    for (size_t blockIndex = SgAdaOthersExp::pools.size(); (blockIndex * SgAdaOthersExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaOthersExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaOthersExp) = %" PRIuPTR " SgAdaOthersExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaOthersExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaOthersExp),SgAdaOthersExp::pool_size);
        }
#endif

        SgAdaOthersExp* pointer{static_cast<SgAdaOthersExp*>(ROSE_MALLOC(SgAdaOthersExp::pool_size * sizeof(SgAdaOthersExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaOthersExp::pool_size * sizeof(SgAdaOthersExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaOthersExp::pool_size * sizeof(SgAdaOthersExp));
#endif
        SgAdaOthersExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaOthersExp::pool_size * sizeof(SgAdaOthersExp), V_SgAdaOthersExp));

        if (SgAdaOthersExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaOthersExp* blkptr{reinterpret_cast<SgAdaOthersExp*>(SgAdaOthersExp::pools[blockIndex-1])};
                blkptr[SgAdaOthersExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaOthersExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaOthersExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaOthersExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaOthersExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaOthersExp::pool_size - 1};
    SgAdaOthersExp* testPointer{reinterpret_cast<SgAdaOthersExp*>(SgAdaOthersExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaOthersExp::pool_size * (SgAdaOthersExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaOthersExpStroageClass
 * from its counterpart of SgAdaOthersExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaOthersExp::initializeStorageClassArray(SgAdaOthersExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaOthersExp::pools.begin(); block != SgAdaOthersExp::pools.end(); ++block) {
        SgAdaOthersExp* pointer{reinterpret_cast<SgAdaOthersExp*>(*block)};
        for (unsigned i = 0; i < SgAdaOthersExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 153238 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaRenamingRefExp that takes its corresponding StorageClass as parameter
 */
SgAdaRenamingRefExp::SgAdaRenamingRefExp(const SgAdaRenamingRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 153248 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgAdaRenamingSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaRenamingRefExp*
SgAdaRenamingRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaRenamingRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingRefExp)};
        unsigned long positionInPool{localIndex % SgAdaRenamingRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRenamingRefExp::pool_size};
        returnPointer = &(((SgAdaRenamingRefExp*)(SgAdaRenamingRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaRenamingRefExp*
SgAdaRenamingRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaRenamingRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaRenamingRefExp)};
        unsigned long positionInPool{localIndex % SgAdaRenamingRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRenamingRefExp::pool_size};
        returnPointer = &(((SgAdaRenamingRefExp*)(SgAdaRenamingRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaRenamingRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaRenamingRefExp::pools.begin(); block != SgAdaRenamingRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRenamingRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaRenamingRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaRenamingRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaRenamingRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaRenamingRefExp::pools.begin(); block != SgAdaRenamingRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRenamingRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaRenamingRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaRenamingRefExp::clearMemoryPool() {
    SgAdaRenamingRefExp* tempPointer{nullptr};
    if (SgAdaRenamingRefExp::pools.empty() == false) {
        auto block{SgAdaRenamingRefExp::pools.begin()};
        SgAdaRenamingRefExp::next_node = reinterpret_cast<SgAdaRenamingRefExp*>(*block);
        for (; block != SgAdaRenamingRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaRenamingRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaRenamingRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaRenamingRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaRenamingRefExp::pool_size-1]);
        }
    }
}

void
SgAdaRenamingRefExp::deleteMemoryPool() {
    for (auto p: SgAdaRenamingRefExp::pools) {
        ROSE_FREE(p);
    }
    SgAdaRenamingRefExp::next_node = nullptr;
    SgAdaRenamingRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaRenamingRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingRefExp)};
    for (size_t blockIndex = SgAdaRenamingRefExp::pools.size(); (blockIndex * SgAdaRenamingRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingRefExp) = %" PRIuPTR " SgAdaRenamingRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingRefExp),SgAdaRenamingRefExp::pool_size);
        }
#endif

        SgAdaRenamingRefExp* pointer{static_cast<SgAdaRenamingRefExp*>(ROSE_MALLOC(SgAdaRenamingRefExp::pool_size * sizeof(SgAdaRenamingRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaRenamingRefExp::pool_size * sizeof(SgAdaRenamingRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaRenamingRefExp::pool_size * sizeof(SgAdaRenamingRefExp));
#endif
        SgAdaRenamingRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaRenamingRefExp::pool_size * sizeof(SgAdaRenamingRefExp), V_SgAdaRenamingRefExp));

        if (SgAdaRenamingRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaRenamingRefExp* blkptr{reinterpret_cast<SgAdaRenamingRefExp*>(SgAdaRenamingRefExp::pools[blockIndex-1])};
                blkptr[SgAdaRenamingRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaRenamingRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaRenamingRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaRenamingRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaRenamingRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaRenamingRefExp::pool_size - 1};
    SgAdaRenamingRefExp* testPointer{reinterpret_cast<SgAdaRenamingRefExp*>(SgAdaRenamingRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaRenamingRefExp::pool_size * (SgAdaRenamingRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaRenamingRefExpStroageClass
 * from its counterpart of SgAdaRenamingRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaRenamingRefExp::initializeStorageClassArray(SgAdaRenamingRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaRenamingRefExp::pools.begin(); block != SgAdaRenamingRefExp::pools.end(); ++block) {
        SgAdaRenamingRefExp* pointer{reinterpret_cast<SgAdaRenamingRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 153466 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaUnitRefExp that takes its corresponding StorageClass as parameter
 */
SgAdaUnitRefExp::SgAdaUnitRefExp(const SgAdaUnitRefExpStorageClass& storageSource)  : SgExpression (storageSource) {

/* #line 153476 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_symbol = (SgSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaUnitRefExp*
SgAdaUnitRefExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaUnitRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaUnitRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaUnitRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaUnitRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnitRefExp)};
        unsigned long positionInPool{localIndex % SgAdaUnitRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaUnitRefExp::pool_size};
        returnPointer = &(((SgAdaUnitRefExp*)(SgAdaUnitRefExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaUnitRefExp*
SgAdaUnitRefExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaUnitRefExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaUnitRefExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaUnitRefExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaUnitRefExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaUnitRefExp)};
        unsigned long positionInPool{localIndex % SgAdaUnitRefExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaUnitRefExp::pool_size};
        returnPointer = &(((SgAdaUnitRefExp*)(SgAdaUnitRefExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaUnitRefExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaUnitRefExp::pools.begin(); block != SgAdaUnitRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaUnitRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaUnitRefExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaUnitRefExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaUnitRefExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaUnitRefExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaUnitRefExp::pools.begin(); block != SgAdaUnitRefExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaUnitRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaUnitRefExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaUnitRefExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaUnitRefExp::clearMemoryPool() {
    SgAdaUnitRefExp* tempPointer{nullptr};
    if (SgAdaUnitRefExp::pools.empty() == false) {
        auto block{SgAdaUnitRefExp::pools.begin()};
        SgAdaUnitRefExp::next_node = reinterpret_cast<SgAdaUnitRefExp*>(*block);
        for (; block != SgAdaUnitRefExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaUnitRefExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaUnitRefExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaUnitRefExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaUnitRefExp::pool_size-1]);
        }
    }
}

void
SgAdaUnitRefExp::deleteMemoryPool() {
    for (auto p: SgAdaUnitRefExp::pools) {
        ROSE_FREE(p);
    }
    SgAdaUnitRefExp::next_node = nullptr;
    SgAdaUnitRefExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaUnitRefExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnitRefExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaUnitRefExp)};
    for (size_t blockIndex = SgAdaUnitRefExp::pools.size(); (blockIndex * SgAdaUnitRefExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnitRefExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaUnitRefExp) = %" PRIuPTR " SgAdaUnitRefExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaUnitRefExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaUnitRefExp),SgAdaUnitRefExp::pool_size);
        }
#endif

        SgAdaUnitRefExp* pointer{static_cast<SgAdaUnitRefExp*>(ROSE_MALLOC(SgAdaUnitRefExp::pool_size * sizeof(SgAdaUnitRefExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaUnitRefExp::pool_size * sizeof(SgAdaUnitRefExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaUnitRefExp::pool_size * sizeof(SgAdaUnitRefExp));
#endif
        SgAdaUnitRefExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaUnitRefExp::pool_size * sizeof(SgAdaUnitRefExp), V_SgAdaUnitRefExp));

        if (SgAdaUnitRefExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaUnitRefExp* blkptr{reinterpret_cast<SgAdaUnitRefExp*>(SgAdaUnitRefExp::pools[blockIndex-1])};
                blkptr[SgAdaUnitRefExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaUnitRefExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaUnitRefExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaUnitRefExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaUnitRefExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaUnitRefExp::pool_size - 1};
    SgAdaUnitRefExp* testPointer{reinterpret_cast<SgAdaUnitRefExp*>(SgAdaUnitRefExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaUnitRefExp::pool_size * (SgAdaUnitRefExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaUnitRefExpStroageClass
 * from its counterpart of SgAdaUnitRefExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaUnitRefExp::initializeStorageClassArray(SgAdaUnitRefExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaUnitRefExp::pools.begin(); block != SgAdaUnitRefExp::pools.end(); ++block) {
        SgAdaUnitRefExp* pointer{reinterpret_cast<SgAdaUnitRefExp*>(*block)};
        for (unsigned i = 0; i < SgAdaUnitRefExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 153694 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgSymbol that takes its corresponding StorageClass as parameter
 */
SgSymbol::SgSymbol(const SgSymbolStorageClass& storageSource)  : SgNode (storageSource) {

/* #line 153704 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_attributeMechanism = storageSource.storageOf_attributeMechanism.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgSymbol*
SgSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbol)};
        unsigned long positionInPool{localIndex % SgSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSymbol::pool_size};
        returnPointer = &(((SgSymbol*)(SgSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgSymbol*
SgSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgSymbol)};
        unsigned long positionInPool{localIndex % SgSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgSymbol::pool_size};
        returnPointer = &(((SgSymbol*)(SgSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgSymbol::pools.begin(); block != SgSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSymbol*>(*block)};
        for (unsigned i = 0; i < SgSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgSymbol::pools.begin(); block != SgSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgSymbol*>(*block)};
        for (unsigned i = 0; i < SgSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgSymbol::clearMemoryPool() {
    SgSymbol* tempPointer{nullptr};
    if (SgSymbol::pools.empty() == false) {
        auto block{SgSymbol::pools.begin()};
        SgSymbol::next_node = reinterpret_cast<SgSymbol*>(*block);
        for (; block != SgSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgSymbol::pool_size-1]);
        }
    }
}

void
SgSymbol::deleteMemoryPool() {
    for (auto p: SgSymbol::pools) {
        ROSE_FREE(p);
    }
    SgSymbol::next_node = nullptr;
    SgSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgSymbol)};
    for (size_t blockIndex = SgSymbol::pools.size(); (blockIndex * SgSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgSymbol) = %" PRIuPTR " SgSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgSymbol),SgSymbol::pool_size);
        }
#endif

        SgSymbol* pointer{static_cast<SgSymbol*>(ROSE_MALLOC(SgSymbol::pool_size * sizeof(SgSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgSymbol::pool_size * sizeof(SgSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgSymbol::pool_size * sizeof(SgSymbol));
#endif
        SgSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgSymbol::pool_size * sizeof(SgSymbol), V_SgSymbol));

        if (SgSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgSymbol* blkptr{reinterpret_cast<SgSymbol*>(SgSymbol::pools[blockIndex-1])};
                blkptr[SgSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgSymbol::pool_size - 1};
    SgSymbol* testPointer{reinterpret_cast<SgSymbol*>(SgSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgSymbol::pool_size * (SgSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgSymbolStroageClass
 * from its counterpart of SgSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgSymbol::initializeStorageClassArray(SgSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgSymbol::pools.begin(); block != SgSymbol::pools.end(); ++block) {
        SgSymbol* pointer{reinterpret_cast<SgSymbol*>(*block)};
        for (unsigned i = 0; i < SgSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 153922 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgVariableSymbol that takes its corresponding StorageClass as parameter
 */
SgVariableSymbol::SgVariableSymbol(const SgVariableSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 153932 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgVariableSymbol*
SgVariableSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgVariableSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgVariableSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableSymbol)};
        unsigned long positionInPool{localIndex % SgVariableSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariableSymbol::pool_size};
        returnPointer = &(((SgVariableSymbol*)(SgVariableSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgVariableSymbol*
SgVariableSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgVariableSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgVariableSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgVariableSymbol)};
        unsigned long positionInPool{localIndex % SgVariableSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgVariableSymbol::pool_size};
        returnPointer = &(((SgVariableSymbol*)(SgVariableSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgVariableSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgVariableSymbol::pools.begin(); block != SgVariableSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariableSymbol*>(*block)};
        for (unsigned i = 0; i < SgVariableSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgVariableSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgVariableSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgVariableSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgVariableSymbol::pools.begin(); block != SgVariableSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgVariableSymbol*>(*block)};
        for (unsigned i = 0; i < SgVariableSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgVariableSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgVariableSymbol::clearMemoryPool() {
    SgVariableSymbol* tempPointer{nullptr};
    if (SgVariableSymbol::pools.empty() == false) {
        auto block{SgVariableSymbol::pools.begin()};
        SgVariableSymbol::next_node = reinterpret_cast<SgVariableSymbol*>(*block);
        for (; block != SgVariableSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgVariableSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgVariableSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgVariableSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgVariableSymbol::pool_size-1]);
        }
    }
}

void
SgVariableSymbol::deleteMemoryPool() {
    for (auto p: SgVariableSymbol::pools) {
        ROSE_FREE(p);
    }
    SgVariableSymbol::next_node = nullptr;
    SgVariableSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgVariableSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableSymbol)};
    for (size_t blockIndex = SgVariableSymbol::pools.size(); (blockIndex * SgVariableSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableSymbol) = %" PRIuPTR " SgVariableSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgVariableSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgVariableSymbol),SgVariableSymbol::pool_size);
        }
#endif

        SgVariableSymbol* pointer{static_cast<SgVariableSymbol*>(ROSE_MALLOC(SgVariableSymbol::pool_size * sizeof(SgVariableSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgVariableSymbol::pool_size * sizeof(SgVariableSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgVariableSymbol::pool_size * sizeof(SgVariableSymbol));
#endif
        SgVariableSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgVariableSymbol::pool_size * sizeof(SgVariableSymbol), V_SgVariableSymbol));

        if (SgVariableSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgVariableSymbol* blkptr{reinterpret_cast<SgVariableSymbol*>(SgVariableSymbol::pools[blockIndex-1])};
                blkptr[SgVariableSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgVariableSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgVariableSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgVariableSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgVariableSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgVariableSymbol::pool_size - 1};
    SgVariableSymbol* testPointer{reinterpret_cast<SgVariableSymbol*>(SgVariableSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgVariableSymbol::pool_size * (SgVariableSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgVariableSymbolStroageClass
 * from its counterpart of SgVariableSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgVariableSymbol::initializeStorageClassArray(SgVariableSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgVariableSymbol::pools.begin(); block != SgVariableSymbol::pools.end(); ++block) {
        SgVariableSymbol* pointer{reinterpret_cast<SgVariableSymbol*>(*block)};
        for (unsigned i = 0; i < SgVariableSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 154150 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateVariableSymbol that takes its corresponding StorageClass as parameter
 */
SgTemplateVariableSymbol::SgTemplateVariableSymbol(const SgTemplateVariableSymbolStorageClass& storageSource)  : SgVariableSymbol (storageSource) {

/* #line 154160 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateVariableSymbol*
SgTemplateVariableSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateVariableSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateVariableSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateVariableSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateVariableSymbol::pool_size};
        returnPointer = &(((SgTemplateVariableSymbol*)(SgTemplateVariableSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateVariableSymbol*
SgTemplateVariableSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateVariableSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateVariableSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateVariableSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateVariableSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateVariableSymbol::pool_size};
        returnPointer = &(((SgTemplateVariableSymbol*)(SgTemplateVariableSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateVariableSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateVariableSymbol::pools.begin(); block != SgTemplateVariableSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateVariableSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateVariableSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateVariableSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateVariableSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateVariableSymbol::pools.begin(); block != SgTemplateVariableSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateVariableSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateVariableSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateVariableSymbol::clearMemoryPool() {
    SgTemplateVariableSymbol* tempPointer{nullptr};
    if (SgTemplateVariableSymbol::pools.empty() == false) {
        auto block{SgTemplateVariableSymbol::pools.begin()};
        SgTemplateVariableSymbol::next_node = reinterpret_cast<SgTemplateVariableSymbol*>(*block);
        for (; block != SgTemplateVariableSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateVariableSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateVariableSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateVariableSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateVariableSymbol::pool_size-1]);
        }
    }
}

void
SgTemplateVariableSymbol::deleteMemoryPool() {
    for (auto p: SgTemplateVariableSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTemplateVariableSymbol::next_node = nullptr;
    SgTemplateVariableSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateVariableSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableSymbol)};
    for (size_t blockIndex = SgTemplateVariableSymbol::pools.size(); (blockIndex * SgTemplateVariableSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableSymbol) = %" PRIuPTR " SgTemplateVariableSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateVariableSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateVariableSymbol),SgTemplateVariableSymbol::pool_size);
        }
#endif

        SgTemplateVariableSymbol* pointer{static_cast<SgTemplateVariableSymbol*>(ROSE_MALLOC(SgTemplateVariableSymbol::pool_size * sizeof(SgTemplateVariableSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateVariableSymbol::pool_size * sizeof(SgTemplateVariableSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateVariableSymbol::pool_size * sizeof(SgTemplateVariableSymbol));
#endif
        SgTemplateVariableSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateVariableSymbol::pool_size * sizeof(SgTemplateVariableSymbol), V_SgTemplateVariableSymbol));

        if (SgTemplateVariableSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateVariableSymbol* blkptr{reinterpret_cast<SgTemplateVariableSymbol*>(SgTemplateVariableSymbol::pools[blockIndex-1])};
                blkptr[SgTemplateVariableSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateVariableSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateVariableSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateVariableSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateVariableSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateVariableSymbol::pool_size - 1};
    SgTemplateVariableSymbol* testPointer{reinterpret_cast<SgTemplateVariableSymbol*>(SgTemplateVariableSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateVariableSymbol::pool_size * (SgTemplateVariableSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateVariableSymbolStroageClass
 * from its counterpart of SgTemplateVariableSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateVariableSymbol::initializeStorageClassArray(SgTemplateVariableSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateVariableSymbol::pools.begin(); block != SgTemplateVariableSymbol::pools.end(); ++block) {
        SgTemplateVariableSymbol* pointer{reinterpret_cast<SgTemplateVariableSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateVariableSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 154377 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNonrealSymbol that takes its corresponding StorageClass as parameter
 */
SgNonrealSymbol::SgNonrealSymbol(const SgNonrealSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 154387 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgNonrealDecl*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNonrealSymbol*
SgNonrealSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNonrealSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNonrealSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealSymbol)};
        unsigned long positionInPool{localIndex % SgNonrealSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealSymbol::pool_size};
        returnPointer = &(((SgNonrealSymbol*)(SgNonrealSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNonrealSymbol*
SgNonrealSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNonrealSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNonrealSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNonrealSymbol)};
        unsigned long positionInPool{localIndex % SgNonrealSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNonrealSymbol::pool_size};
        returnPointer = &(((SgNonrealSymbol*)(SgNonrealSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNonrealSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNonrealSymbol::pools.begin(); block != SgNonrealSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealSymbol*>(*block)};
        for (unsigned i = 0; i < SgNonrealSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNonrealSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNonrealSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNonrealSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgNonrealSymbol::pools.begin(); block != SgNonrealSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNonrealSymbol*>(*block)};
        for (unsigned i = 0; i < SgNonrealSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNonrealSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNonrealSymbol::clearMemoryPool() {
    SgNonrealSymbol* tempPointer{nullptr};
    if (SgNonrealSymbol::pools.empty() == false) {
        auto block{SgNonrealSymbol::pools.begin()};
        SgNonrealSymbol::next_node = reinterpret_cast<SgNonrealSymbol*>(*block);
        for (; block != SgNonrealSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNonrealSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNonrealSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNonrealSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNonrealSymbol::pool_size-1]);
        }
    }
}

void
SgNonrealSymbol::deleteMemoryPool() {
    for (auto p: SgNonrealSymbol::pools) {
        ROSE_FREE(p);
    }
    SgNonrealSymbol::next_node = nullptr;
    SgNonrealSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNonrealSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealSymbol)};
    for (size_t blockIndex = SgNonrealSymbol::pools.size(); (blockIndex * SgNonrealSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealSymbol) = %" PRIuPTR " SgNonrealSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNonrealSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNonrealSymbol),SgNonrealSymbol::pool_size);
        }
#endif

        SgNonrealSymbol* pointer{static_cast<SgNonrealSymbol*>(ROSE_MALLOC(SgNonrealSymbol::pool_size * sizeof(SgNonrealSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNonrealSymbol::pool_size * sizeof(SgNonrealSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNonrealSymbol::pool_size * sizeof(SgNonrealSymbol));
#endif
        SgNonrealSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNonrealSymbol::pool_size * sizeof(SgNonrealSymbol), V_SgNonrealSymbol));

        if (SgNonrealSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNonrealSymbol* blkptr{reinterpret_cast<SgNonrealSymbol*>(SgNonrealSymbol::pools[blockIndex-1])};
                blkptr[SgNonrealSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNonrealSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNonrealSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNonrealSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNonrealSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNonrealSymbol::pool_size - 1};
    SgNonrealSymbol* testPointer{reinterpret_cast<SgNonrealSymbol*>(SgNonrealSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNonrealSymbol::pool_size * (SgNonrealSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNonrealSymbolStroageClass
 * from its counterpart of SgNonrealSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNonrealSymbol::initializeStorageClassArray(SgNonrealSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNonrealSymbol::pools.begin(); block != SgNonrealSymbol::pools.end(); ++block) {
        SgNonrealSymbol* pointer{reinterpret_cast<SgNonrealSymbol*>(*block)};
        for (unsigned i = 0; i < SgNonrealSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 154605 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionSymbol that takes its corresponding StorageClass as parameter
 */
SgFunctionSymbol::SgFunctionSymbol(const SgFunctionSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 154615 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgFunctionDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionSymbol*
SgFunctionSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionSymbol::pool_size};
        returnPointer = &(((SgFunctionSymbol*)(SgFunctionSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionSymbol*
SgFunctionSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionSymbol::pool_size};
        returnPointer = &(((SgFunctionSymbol*)(SgFunctionSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionSymbol::pools.begin(); block != SgFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgFunctionSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionSymbol::pools.begin(); block != SgFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgFunctionSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionSymbol::clearMemoryPool() {
    SgFunctionSymbol* tempPointer{nullptr};
    if (SgFunctionSymbol::pools.empty() == false) {
        auto block{SgFunctionSymbol::pools.begin()};
        SgFunctionSymbol::next_node = reinterpret_cast<SgFunctionSymbol*>(*block);
        for (; block != SgFunctionSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionSymbol::pool_size-1]);
        }
    }
}

void
SgFunctionSymbol::deleteMemoryPool() {
    for (auto p: SgFunctionSymbol::pools) {
        ROSE_FREE(p);
    }
    SgFunctionSymbol::next_node = nullptr;
    SgFunctionSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionSymbol)};
    for (size_t blockIndex = SgFunctionSymbol::pools.size(); (blockIndex * SgFunctionSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionSymbol) = %" PRIuPTR " SgFunctionSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionSymbol),SgFunctionSymbol::pool_size);
        }
#endif

        SgFunctionSymbol* pointer{static_cast<SgFunctionSymbol*>(ROSE_MALLOC(SgFunctionSymbol::pool_size * sizeof(SgFunctionSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionSymbol::pool_size * sizeof(SgFunctionSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionSymbol::pool_size * sizeof(SgFunctionSymbol));
#endif
        SgFunctionSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionSymbol::pool_size * sizeof(SgFunctionSymbol), V_SgFunctionSymbol));

        if (SgFunctionSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionSymbol* blkptr{reinterpret_cast<SgFunctionSymbol*>(SgFunctionSymbol::pools[blockIndex-1])};
                blkptr[SgFunctionSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionSymbol::pool_size - 1};
    SgFunctionSymbol* testPointer{reinterpret_cast<SgFunctionSymbol*>(SgFunctionSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionSymbol::pool_size * (SgFunctionSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionSymbolStroageClass
 * from its counterpart of SgFunctionSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionSymbol::initializeStorageClassArray(SgFunctionSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionSymbol::pools.begin(); block != SgFunctionSymbol::pools.end(); ++block) {
        SgFunctionSymbol* pointer{reinterpret_cast<SgFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgFunctionSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 154833 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgMemberFunctionSymbol that takes its corresponding StorageClass as parameter
 */
SgMemberFunctionSymbol::SgMemberFunctionSymbol(const SgMemberFunctionSymbolStorageClass& storageSource)  : SgFunctionSymbol (storageSource) {

/* #line 154843 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgMemberFunctionSymbol*
SgMemberFunctionSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgMemberFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgMemberFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgMemberFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionSymbol::pool_size};
        returnPointer = &(((SgMemberFunctionSymbol*)(SgMemberFunctionSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgMemberFunctionSymbol*
SgMemberFunctionSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgMemberFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgMemberFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgMemberFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgMemberFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgMemberFunctionSymbol::pool_size};
        returnPointer = &(((SgMemberFunctionSymbol*)(SgMemberFunctionSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgMemberFunctionSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgMemberFunctionSymbol::pools.begin(); block != SgMemberFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgMemberFunctionSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgMemberFunctionSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgMemberFunctionSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgMemberFunctionSymbol::pools.begin(); block != SgMemberFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgMemberFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgMemberFunctionSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgMemberFunctionSymbol::clearMemoryPool() {
    SgMemberFunctionSymbol* tempPointer{nullptr};
    if (SgMemberFunctionSymbol::pools.empty() == false) {
        auto block{SgMemberFunctionSymbol::pools.begin()};
        SgMemberFunctionSymbol::next_node = reinterpret_cast<SgMemberFunctionSymbol*>(*block);
        for (; block != SgMemberFunctionSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgMemberFunctionSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgMemberFunctionSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgMemberFunctionSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgMemberFunctionSymbol::pool_size-1]);
        }
    }
}

void
SgMemberFunctionSymbol::deleteMemoryPool() {
    for (auto p: SgMemberFunctionSymbol::pools) {
        ROSE_FREE(p);
    }
    SgMemberFunctionSymbol::next_node = nullptr;
    SgMemberFunctionSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgMemberFunctionSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionSymbol)};
    for (size_t blockIndex = SgMemberFunctionSymbol::pools.size(); (blockIndex * SgMemberFunctionSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionSymbol) = %" PRIuPTR " SgMemberFunctionSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgMemberFunctionSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgMemberFunctionSymbol),SgMemberFunctionSymbol::pool_size);
        }
#endif

        SgMemberFunctionSymbol* pointer{static_cast<SgMemberFunctionSymbol*>(ROSE_MALLOC(SgMemberFunctionSymbol::pool_size * sizeof(SgMemberFunctionSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgMemberFunctionSymbol::pool_size * sizeof(SgMemberFunctionSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgMemberFunctionSymbol::pool_size * sizeof(SgMemberFunctionSymbol));
#endif
        SgMemberFunctionSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgMemberFunctionSymbol::pool_size * sizeof(SgMemberFunctionSymbol), V_SgMemberFunctionSymbol));

        if (SgMemberFunctionSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgMemberFunctionSymbol* blkptr{reinterpret_cast<SgMemberFunctionSymbol*>(SgMemberFunctionSymbol::pools[blockIndex-1])};
                blkptr[SgMemberFunctionSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgMemberFunctionSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgMemberFunctionSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgMemberFunctionSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgMemberFunctionSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgMemberFunctionSymbol::pool_size - 1};
    SgMemberFunctionSymbol* testPointer{reinterpret_cast<SgMemberFunctionSymbol*>(SgMemberFunctionSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgMemberFunctionSymbol::pool_size * (SgMemberFunctionSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgMemberFunctionSymbolStroageClass
 * from its counterpart of SgMemberFunctionSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgMemberFunctionSymbol::initializeStorageClassArray(SgMemberFunctionSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgMemberFunctionSymbol::pools.begin(); block != SgMemberFunctionSymbol::pools.end(); ++block) {
        SgMemberFunctionSymbol* pointer{reinterpret_cast<SgMemberFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgMemberFunctionSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 155060 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateMemberFunctionSymbol that takes its corresponding StorageClass as parameter
 */
SgTemplateMemberFunctionSymbol::SgTemplateMemberFunctionSymbol(const SgTemplateMemberFunctionSymbolStorageClass& storageSource)  : SgMemberFunctionSymbol (storageSource) {

/* #line 155070 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateMemberFunctionSymbol*
SgTemplateMemberFunctionSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateMemberFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateMemberFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateMemberFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateMemberFunctionSymbol::pool_size};
        returnPointer = &(((SgTemplateMemberFunctionSymbol*)(SgTemplateMemberFunctionSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateMemberFunctionSymbol*
SgTemplateMemberFunctionSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateMemberFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateMemberFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateMemberFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateMemberFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateMemberFunctionSymbol::pool_size};
        returnPointer = &(((SgTemplateMemberFunctionSymbol*)(SgTemplateMemberFunctionSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateMemberFunctionSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateMemberFunctionSymbol::pools.begin(); block != SgTemplateMemberFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateMemberFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateMemberFunctionSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateMemberFunctionSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateMemberFunctionSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateMemberFunctionSymbol::pools.begin(); block != SgTemplateMemberFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateMemberFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateMemberFunctionSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateMemberFunctionSymbol::clearMemoryPool() {
    SgTemplateMemberFunctionSymbol* tempPointer{nullptr};
    if (SgTemplateMemberFunctionSymbol::pools.empty() == false) {
        auto block{SgTemplateMemberFunctionSymbol::pools.begin()};
        SgTemplateMemberFunctionSymbol::next_node = reinterpret_cast<SgTemplateMemberFunctionSymbol*>(*block);
        for (; block != SgTemplateMemberFunctionSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateMemberFunctionSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateMemberFunctionSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateMemberFunctionSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateMemberFunctionSymbol::pool_size-1]);
        }
    }
}

void
SgTemplateMemberFunctionSymbol::deleteMemoryPool() {
    for (auto p: SgTemplateMemberFunctionSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTemplateMemberFunctionSymbol::next_node = nullptr;
    SgTemplateMemberFunctionSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateMemberFunctionSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionSymbol)};
    for (size_t blockIndex = SgTemplateMemberFunctionSymbol::pools.size(); (blockIndex * SgTemplateMemberFunctionSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionSymbol) = %" PRIuPTR " SgTemplateMemberFunctionSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateMemberFunctionSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateMemberFunctionSymbol),SgTemplateMemberFunctionSymbol::pool_size);
        }
#endif

        SgTemplateMemberFunctionSymbol* pointer{static_cast<SgTemplateMemberFunctionSymbol*>(ROSE_MALLOC(SgTemplateMemberFunctionSymbol::pool_size * sizeof(SgTemplateMemberFunctionSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateMemberFunctionSymbol::pool_size * sizeof(SgTemplateMemberFunctionSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateMemberFunctionSymbol::pool_size * sizeof(SgTemplateMemberFunctionSymbol));
#endif
        SgTemplateMemberFunctionSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateMemberFunctionSymbol::pool_size * sizeof(SgTemplateMemberFunctionSymbol), V_SgTemplateMemberFunctionSymbol));

        if (SgTemplateMemberFunctionSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateMemberFunctionSymbol* blkptr{reinterpret_cast<SgTemplateMemberFunctionSymbol*>(SgTemplateMemberFunctionSymbol::pools[blockIndex-1])};
                blkptr[SgTemplateMemberFunctionSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateMemberFunctionSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateMemberFunctionSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateMemberFunctionSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateMemberFunctionSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateMemberFunctionSymbol::pool_size - 1};
    SgTemplateMemberFunctionSymbol* testPointer{reinterpret_cast<SgTemplateMemberFunctionSymbol*>(SgTemplateMemberFunctionSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateMemberFunctionSymbol::pool_size * (SgTemplateMemberFunctionSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateMemberFunctionSymbolStroageClass
 * from its counterpart of SgTemplateMemberFunctionSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateMemberFunctionSymbol::initializeStorageClassArray(SgTemplateMemberFunctionSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateMemberFunctionSymbol::pools.begin(); block != SgTemplateMemberFunctionSymbol::pools.end(); ++block) {
        SgTemplateMemberFunctionSymbol* pointer{reinterpret_cast<SgTemplateMemberFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateMemberFunctionSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 155287 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateFunctionSymbol that takes its corresponding StorageClass as parameter
 */
SgTemplateFunctionSymbol::SgTemplateFunctionSymbol(const SgTemplateFunctionSymbolStorageClass& storageSource)  : SgFunctionSymbol (storageSource) {

/* #line 155297 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateFunctionSymbol*
SgTemplateFunctionSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionSymbol::pool_size};
        returnPointer = &(((SgTemplateFunctionSymbol*)(SgTemplateFunctionSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateFunctionSymbol*
SgTemplateFunctionSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateFunctionSymbol::pool_size};
        returnPointer = &(((SgTemplateFunctionSymbol*)(SgTemplateFunctionSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateFunctionSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateFunctionSymbol::pools.begin(); block != SgTemplateFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateFunctionSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateFunctionSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateFunctionSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateFunctionSymbol::pools.begin(); block != SgTemplateFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateFunctionSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateFunctionSymbol::clearMemoryPool() {
    SgTemplateFunctionSymbol* tempPointer{nullptr};
    if (SgTemplateFunctionSymbol::pools.empty() == false) {
        auto block{SgTemplateFunctionSymbol::pools.begin()};
        SgTemplateFunctionSymbol::next_node = reinterpret_cast<SgTemplateFunctionSymbol*>(*block);
        for (; block != SgTemplateFunctionSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateFunctionSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateFunctionSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateFunctionSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateFunctionSymbol::pool_size-1]);
        }
    }
}

void
SgTemplateFunctionSymbol::deleteMemoryPool() {
    for (auto p: SgTemplateFunctionSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTemplateFunctionSymbol::next_node = nullptr;
    SgTemplateFunctionSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateFunctionSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionSymbol)};
    for (size_t blockIndex = SgTemplateFunctionSymbol::pools.size(); (blockIndex * SgTemplateFunctionSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionSymbol) = %" PRIuPTR " SgTemplateFunctionSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateFunctionSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateFunctionSymbol),SgTemplateFunctionSymbol::pool_size);
        }
#endif

        SgTemplateFunctionSymbol* pointer{static_cast<SgTemplateFunctionSymbol*>(ROSE_MALLOC(SgTemplateFunctionSymbol::pool_size * sizeof(SgTemplateFunctionSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateFunctionSymbol::pool_size * sizeof(SgTemplateFunctionSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateFunctionSymbol::pool_size * sizeof(SgTemplateFunctionSymbol));
#endif
        SgTemplateFunctionSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateFunctionSymbol::pool_size * sizeof(SgTemplateFunctionSymbol), V_SgTemplateFunctionSymbol));

        if (SgTemplateFunctionSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateFunctionSymbol* blkptr{reinterpret_cast<SgTemplateFunctionSymbol*>(SgTemplateFunctionSymbol::pools[blockIndex-1])};
                blkptr[SgTemplateFunctionSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateFunctionSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateFunctionSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateFunctionSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateFunctionSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateFunctionSymbol::pool_size - 1};
    SgTemplateFunctionSymbol* testPointer{reinterpret_cast<SgTemplateFunctionSymbol*>(SgTemplateFunctionSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateFunctionSymbol::pool_size * (SgTemplateFunctionSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateFunctionSymbolStroageClass
 * from its counterpart of SgTemplateFunctionSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateFunctionSymbol::initializeStorageClassArray(SgTemplateFunctionSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateFunctionSymbol::pools.begin(); block != SgTemplateFunctionSymbol::pools.end(); ++block) {
        SgTemplateFunctionSymbol* pointer{reinterpret_cast<SgTemplateFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateFunctionSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 155514 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgRenameSymbol that takes its corresponding StorageClass as parameter
 */
SgRenameSymbol::SgRenameSymbol(const SgRenameSymbolStorageClass& storageSource)  : SgFunctionSymbol (storageSource) {

/* #line 155524 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_original_symbol = (SgSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_original_symbol));
    p_new_name = SgName ( storageSource.storageOf_new_name);


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgRenameSymbol*
SgRenameSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgRenameSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRenameSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRenameSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgRenameSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgRenameSymbol)};
        unsigned long positionInPool{localIndex % SgRenameSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRenameSymbol::pool_size};
        returnPointer = &(((SgRenameSymbol*)(SgRenameSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgRenameSymbol*
SgRenameSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgRenameSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRenameSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRenameSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgRenameSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgRenameSymbol)};
        unsigned long positionInPool{localIndex % SgRenameSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgRenameSymbol::pool_size};
        returnPointer = &(((SgRenameSymbol*)(SgRenameSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgRenameSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgRenameSymbol::pools.begin(); block != SgRenameSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRenameSymbol*>(*block)};
        for (unsigned i = 0; i < SgRenameSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgRenameSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgRenameSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgRenameSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgRenameSymbol::pools.begin(); block != SgRenameSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgRenameSymbol*>(*block)};
        for (unsigned i = 0; i < SgRenameSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgRenameSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgRenameSymbol::clearMemoryPool() {
    SgRenameSymbol* tempPointer{nullptr};
    if (SgRenameSymbol::pools.empty() == false) {
        auto block{SgRenameSymbol::pools.begin()};
        SgRenameSymbol::next_node = reinterpret_cast<SgRenameSymbol*>(*block);
        for (; block != SgRenameSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgRenameSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgRenameSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgRenameSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgRenameSymbol::pool_size-1]);
        }
    }
}

void
SgRenameSymbol::deleteMemoryPool() {
    for (auto p: SgRenameSymbol::pools) {
        ROSE_FREE(p);
    }
    SgRenameSymbol::next_node = nullptr;
    SgRenameSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgRenameSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgRenameSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgRenameSymbol)};
    for (size_t blockIndex = SgRenameSymbol::pools.size(); (blockIndex * SgRenameSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgRenameSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgRenameSymbol) = %" PRIuPTR " SgRenameSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgRenameSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgRenameSymbol),SgRenameSymbol::pool_size);
        }
#endif

        SgRenameSymbol* pointer{static_cast<SgRenameSymbol*>(ROSE_MALLOC(SgRenameSymbol::pool_size * sizeof(SgRenameSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgRenameSymbol::pool_size * sizeof(SgRenameSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgRenameSymbol::pool_size * sizeof(SgRenameSymbol));
#endif
        SgRenameSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgRenameSymbol::pool_size * sizeof(SgRenameSymbol), V_SgRenameSymbol));

        if (SgRenameSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgRenameSymbol* blkptr{reinterpret_cast<SgRenameSymbol*>(SgRenameSymbol::pools[blockIndex-1])};
                blkptr[SgRenameSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgRenameSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgRenameSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgRenameSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgRenameSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgRenameSymbol::pool_size - 1};
    SgRenameSymbol* testPointer{reinterpret_cast<SgRenameSymbol*>(SgRenameSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgRenameSymbol::pool_size * (SgRenameSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgRenameSymbolStroageClass
 * from its counterpart of SgRenameSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgRenameSymbol::initializeStorageClassArray(SgRenameSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgRenameSymbol::pools.begin(); block != SgRenameSymbol::pools.end(); ++block) {
        SgRenameSymbol* pointer{reinterpret_cast<SgRenameSymbol*>(*block)};
        for (unsigned i = 0; i < SgRenameSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 155743 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaInheritedFunctionSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaInheritedFunctionSymbol::SgAdaInheritedFunctionSymbol(const SgAdaInheritedFunctionSymbolStorageClass& storageSource)  : SgFunctionSymbol (storageSource) {

/* #line 155753 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_derivedFunctionType = (SgFunctionType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_derivedFunctionType));
     p_associatedDerivedType = (SgNamedType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_associatedDerivedType));
     p_publiclyVisibleFunctionSymbol = (SgFunctionSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_publiclyVisibleFunctionSymbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaInheritedFunctionSymbol*
SgAdaInheritedFunctionSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaInheritedFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaInheritedFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaInheritedFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaInheritedFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaInheritedFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgAdaInheritedFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaInheritedFunctionSymbol::pool_size};
        returnPointer = &(((SgAdaInheritedFunctionSymbol*)(SgAdaInheritedFunctionSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaInheritedFunctionSymbol*
SgAdaInheritedFunctionSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaInheritedFunctionSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaInheritedFunctionSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaInheritedFunctionSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaInheritedFunctionSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaInheritedFunctionSymbol)};
        unsigned long positionInPool{localIndex % SgAdaInheritedFunctionSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaInheritedFunctionSymbol::pool_size};
        returnPointer = &(((SgAdaInheritedFunctionSymbol*)(SgAdaInheritedFunctionSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaInheritedFunctionSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaInheritedFunctionSymbol::pools.begin(); block != SgAdaInheritedFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaInheritedFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaInheritedFunctionSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaInheritedFunctionSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaInheritedFunctionSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaInheritedFunctionSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaInheritedFunctionSymbol::pools.begin(); block != SgAdaInheritedFunctionSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaInheritedFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaInheritedFunctionSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaInheritedFunctionSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaInheritedFunctionSymbol::clearMemoryPool() {
    SgAdaInheritedFunctionSymbol* tempPointer{nullptr};
    if (SgAdaInheritedFunctionSymbol::pools.empty() == false) {
        auto block{SgAdaInheritedFunctionSymbol::pools.begin()};
        SgAdaInheritedFunctionSymbol::next_node = reinterpret_cast<SgAdaInheritedFunctionSymbol*>(*block);
        for (; block != SgAdaInheritedFunctionSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaInheritedFunctionSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaInheritedFunctionSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaInheritedFunctionSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaInheritedFunctionSymbol::pool_size-1]);
        }
    }
}

void
SgAdaInheritedFunctionSymbol::deleteMemoryPool() {
    for (auto p: SgAdaInheritedFunctionSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaInheritedFunctionSymbol::next_node = nullptr;
    SgAdaInheritedFunctionSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaInheritedFunctionSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaInheritedFunctionSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaInheritedFunctionSymbol)};
    for (size_t blockIndex = SgAdaInheritedFunctionSymbol::pools.size(); (blockIndex * SgAdaInheritedFunctionSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaInheritedFunctionSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaInheritedFunctionSymbol) = %" PRIuPTR " SgAdaInheritedFunctionSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaInheritedFunctionSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaInheritedFunctionSymbol),SgAdaInheritedFunctionSymbol::pool_size);
        }
#endif

        SgAdaInheritedFunctionSymbol* pointer{static_cast<SgAdaInheritedFunctionSymbol*>(ROSE_MALLOC(SgAdaInheritedFunctionSymbol::pool_size * sizeof(SgAdaInheritedFunctionSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaInheritedFunctionSymbol::pool_size * sizeof(SgAdaInheritedFunctionSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaInheritedFunctionSymbol::pool_size * sizeof(SgAdaInheritedFunctionSymbol));
#endif
        SgAdaInheritedFunctionSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaInheritedFunctionSymbol::pool_size * sizeof(SgAdaInheritedFunctionSymbol), V_SgAdaInheritedFunctionSymbol));

        if (SgAdaInheritedFunctionSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaInheritedFunctionSymbol* blkptr{reinterpret_cast<SgAdaInheritedFunctionSymbol*>(SgAdaInheritedFunctionSymbol::pools[blockIndex-1])};
                blkptr[SgAdaInheritedFunctionSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaInheritedFunctionSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaInheritedFunctionSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaInheritedFunctionSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaInheritedFunctionSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaInheritedFunctionSymbol::pool_size - 1};
    SgAdaInheritedFunctionSymbol* testPointer{reinterpret_cast<SgAdaInheritedFunctionSymbol*>(SgAdaInheritedFunctionSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaInheritedFunctionSymbol::pool_size * (SgAdaInheritedFunctionSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaInheritedFunctionSymbolStroageClass
 * from its counterpart of SgAdaInheritedFunctionSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaInheritedFunctionSymbol::initializeStorageClassArray(SgAdaInheritedFunctionSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaInheritedFunctionSymbol::pools.begin(); block != SgAdaInheritedFunctionSymbol::pools.end(); ++block) {
        SgAdaInheritedFunctionSymbol* pointer{reinterpret_cast<SgAdaInheritedFunctionSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaInheritedFunctionSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 155973 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgFunctionTypeSymbol that takes its corresponding StorageClass as parameter
 */
SgFunctionTypeSymbol::SgFunctionTypeSymbol(const SgFunctionTypeSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 155983 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgFunctionTypeSymbol*
SgFunctionTypeSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgFunctionTypeSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionTypeSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionTypeSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgFunctionTypeSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeSymbol)};
        unsigned long positionInPool{localIndex % SgFunctionTypeSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionTypeSymbol::pool_size};
        returnPointer = &(((SgFunctionTypeSymbol*)(SgFunctionTypeSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgFunctionTypeSymbol*
SgFunctionTypeSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgFunctionTypeSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionTypeSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionTypeSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgFunctionTypeSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgFunctionTypeSymbol)};
        unsigned long positionInPool{localIndex % SgFunctionTypeSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgFunctionTypeSymbol::pool_size};
        returnPointer = &(((SgFunctionTypeSymbol*)(SgFunctionTypeSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgFunctionTypeSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgFunctionTypeSymbol::pools.begin(); block != SgFunctionTypeSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionTypeSymbol*>(*block)};
        for (unsigned i = 0; i < SgFunctionTypeSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgFunctionTypeSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgFunctionTypeSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgFunctionTypeSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgFunctionTypeSymbol::pools.begin(); block != SgFunctionTypeSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgFunctionTypeSymbol*>(*block)};
        for (unsigned i = 0; i < SgFunctionTypeSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgFunctionTypeSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgFunctionTypeSymbol::clearMemoryPool() {
    SgFunctionTypeSymbol* tempPointer{nullptr};
    if (SgFunctionTypeSymbol::pools.empty() == false) {
        auto block{SgFunctionTypeSymbol::pools.begin()};
        SgFunctionTypeSymbol::next_node = reinterpret_cast<SgFunctionTypeSymbol*>(*block);
        for (; block != SgFunctionTypeSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgFunctionTypeSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgFunctionTypeSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgFunctionTypeSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgFunctionTypeSymbol::pool_size-1]);
        }
    }
}

void
SgFunctionTypeSymbol::deleteMemoryPool() {
    for (auto p: SgFunctionTypeSymbol::pools) {
        ROSE_FREE(p);
    }
    SgFunctionTypeSymbol::next_node = nullptr;
    SgFunctionTypeSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgFunctionTypeSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionTypeSymbol)};
    for (size_t blockIndex = SgFunctionTypeSymbol::pools.size(); (blockIndex * SgFunctionTypeSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionTypeSymbol) = %" PRIuPTR " SgFunctionTypeSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgFunctionTypeSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgFunctionTypeSymbol),SgFunctionTypeSymbol::pool_size);
        }
#endif

        SgFunctionTypeSymbol* pointer{static_cast<SgFunctionTypeSymbol*>(ROSE_MALLOC(SgFunctionTypeSymbol::pool_size * sizeof(SgFunctionTypeSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgFunctionTypeSymbol::pool_size * sizeof(SgFunctionTypeSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgFunctionTypeSymbol::pool_size * sizeof(SgFunctionTypeSymbol));
#endif
        SgFunctionTypeSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgFunctionTypeSymbol::pool_size * sizeof(SgFunctionTypeSymbol), V_SgFunctionTypeSymbol));

        if (SgFunctionTypeSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgFunctionTypeSymbol* blkptr{reinterpret_cast<SgFunctionTypeSymbol*>(SgFunctionTypeSymbol::pools[blockIndex-1])};
                blkptr[SgFunctionTypeSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgFunctionTypeSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgFunctionTypeSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgFunctionTypeSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgFunctionTypeSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgFunctionTypeSymbol::pool_size - 1};
    SgFunctionTypeSymbol* testPointer{reinterpret_cast<SgFunctionTypeSymbol*>(SgFunctionTypeSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgFunctionTypeSymbol::pool_size * (SgFunctionTypeSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgFunctionTypeSymbolStroageClass
 * from its counterpart of SgFunctionTypeSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgFunctionTypeSymbol::initializeStorageClassArray(SgFunctionTypeSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgFunctionTypeSymbol::pools.begin(); block != SgFunctionTypeSymbol::pools.end(); ++block) {
        SgFunctionTypeSymbol* pointer{reinterpret_cast<SgFunctionTypeSymbol*>(*block)};
        for (unsigned i = 0; i < SgFunctionTypeSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 156202 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateSymbol that takes its corresponding StorageClass as parameter
 */
SgTemplateSymbol::SgTemplateSymbol(const SgTemplateSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 156212 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgTemplateDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateSymbol*
SgTemplateSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateSymbol::pool_size};
        returnPointer = &(((SgTemplateSymbol*)(SgTemplateSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateSymbol*
SgTemplateSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateSymbol::pool_size};
        returnPointer = &(((SgTemplateSymbol*)(SgTemplateSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateSymbol::pools.begin(); block != SgTemplateSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateSymbol::pools.begin(); block != SgTemplateSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateSymbol::clearMemoryPool() {
    SgTemplateSymbol* tempPointer{nullptr};
    if (SgTemplateSymbol::pools.empty() == false) {
        auto block{SgTemplateSymbol::pools.begin()};
        SgTemplateSymbol::next_node = reinterpret_cast<SgTemplateSymbol*>(*block);
        for (; block != SgTemplateSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateSymbol::pool_size-1]);
        }
    }
}

void
SgTemplateSymbol::deleteMemoryPool() {
    for (auto p: SgTemplateSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTemplateSymbol::next_node = nullptr;
    SgTemplateSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateSymbol)};
    for (size_t blockIndex = SgTemplateSymbol::pools.size(); (blockIndex * SgTemplateSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateSymbol) = %" PRIuPTR " SgTemplateSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateSymbol),SgTemplateSymbol::pool_size);
        }
#endif

        SgTemplateSymbol* pointer{static_cast<SgTemplateSymbol*>(ROSE_MALLOC(SgTemplateSymbol::pool_size * sizeof(SgTemplateSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateSymbol::pool_size * sizeof(SgTemplateSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateSymbol::pool_size * sizeof(SgTemplateSymbol));
#endif
        SgTemplateSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateSymbol::pool_size * sizeof(SgTemplateSymbol), V_SgTemplateSymbol));

        if (SgTemplateSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateSymbol* blkptr{reinterpret_cast<SgTemplateSymbol*>(SgTemplateSymbol::pools[blockIndex-1])};
                blkptr[SgTemplateSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateSymbol::pool_size - 1};
    SgTemplateSymbol* testPointer{reinterpret_cast<SgTemplateSymbol*>(SgTemplateSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateSymbol::pool_size * (SgTemplateSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateSymbolStroageClass
 * from its counterpart of SgTemplateSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateSymbol::initializeStorageClassArray(SgTemplateSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateSymbol::pools.begin(); block != SgTemplateSymbol::pools.end(); ++block) {
        SgTemplateSymbol* pointer{reinterpret_cast<SgTemplateSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 156430 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEnumFieldSymbol that takes its corresponding StorageClass as parameter
 */
SgEnumFieldSymbol::SgEnumFieldSymbol(const SgEnumFieldSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 156440 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEnumFieldSymbol*
SgEnumFieldSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEnumFieldSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumFieldSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumFieldSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumFieldSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumFieldSymbol)};
        unsigned long positionInPool{localIndex % SgEnumFieldSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumFieldSymbol::pool_size};
        returnPointer = &(((SgEnumFieldSymbol*)(SgEnumFieldSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEnumFieldSymbol*
SgEnumFieldSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEnumFieldSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumFieldSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumFieldSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumFieldSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEnumFieldSymbol)};
        unsigned long positionInPool{localIndex % SgEnumFieldSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumFieldSymbol::pool_size};
        returnPointer = &(((SgEnumFieldSymbol*)(SgEnumFieldSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEnumFieldSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEnumFieldSymbol::pools.begin(); block != SgEnumFieldSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumFieldSymbol*>(*block)};
        for (unsigned i = 0; i < SgEnumFieldSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEnumFieldSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEnumFieldSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEnumFieldSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgEnumFieldSymbol::pools.begin(); block != SgEnumFieldSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumFieldSymbol*>(*block)};
        for (unsigned i = 0; i < SgEnumFieldSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEnumFieldSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEnumFieldSymbol::clearMemoryPool() {
    SgEnumFieldSymbol* tempPointer{nullptr};
    if (SgEnumFieldSymbol::pools.empty() == false) {
        auto block{SgEnumFieldSymbol::pools.begin()};
        SgEnumFieldSymbol::next_node = reinterpret_cast<SgEnumFieldSymbol*>(*block);
        for (; block != SgEnumFieldSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEnumFieldSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEnumFieldSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEnumFieldSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEnumFieldSymbol::pool_size-1]);
        }
    }
}

void
SgEnumFieldSymbol::deleteMemoryPool() {
    for (auto p: SgEnumFieldSymbol::pools) {
        ROSE_FREE(p);
    }
    SgEnumFieldSymbol::next_node = nullptr;
    SgEnumFieldSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEnumFieldSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumFieldSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumFieldSymbol)};
    for (size_t blockIndex = SgEnumFieldSymbol::pools.size(); (blockIndex * SgEnumFieldSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumFieldSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumFieldSymbol) = %" PRIuPTR " SgEnumFieldSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumFieldSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumFieldSymbol),SgEnumFieldSymbol::pool_size);
        }
#endif

        SgEnumFieldSymbol* pointer{static_cast<SgEnumFieldSymbol*>(ROSE_MALLOC(SgEnumFieldSymbol::pool_size * sizeof(SgEnumFieldSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEnumFieldSymbol::pool_size * sizeof(SgEnumFieldSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEnumFieldSymbol::pool_size * sizeof(SgEnumFieldSymbol));
#endif
        SgEnumFieldSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEnumFieldSymbol::pool_size * sizeof(SgEnumFieldSymbol), V_SgEnumFieldSymbol));

        if (SgEnumFieldSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEnumFieldSymbol* blkptr{reinterpret_cast<SgEnumFieldSymbol*>(SgEnumFieldSymbol::pools[blockIndex-1])};
                blkptr[SgEnumFieldSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEnumFieldSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEnumFieldSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEnumFieldSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEnumFieldSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEnumFieldSymbol::pool_size - 1};
    SgEnumFieldSymbol* testPointer{reinterpret_cast<SgEnumFieldSymbol*>(SgEnumFieldSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEnumFieldSymbol::pool_size * (SgEnumFieldSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEnumFieldSymbolStroageClass
 * from its counterpart of SgEnumFieldSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEnumFieldSymbol::initializeStorageClassArray(SgEnumFieldSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEnumFieldSymbol::pools.begin(); block != SgEnumFieldSymbol::pools.end(); ++block) {
        SgEnumFieldSymbol* pointer{reinterpret_cast<SgEnumFieldSymbol*>(*block)};
        for (unsigned i = 0; i < SgEnumFieldSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 156658 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypeSymbol that takes its corresponding StorageClass as parameter
 */
SgTypeSymbol::SgTypeSymbol(const SgTypeSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 156668 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypeSymbol*
SgTypeSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypeSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypeSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSymbol)};
        unsigned long positionInPool{localIndex % SgTypeSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSymbol::pool_size};
        returnPointer = &(((SgTypeSymbol*)(SgTypeSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypeSymbol*
SgTypeSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypeSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypeSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypeSymbol)};
        unsigned long positionInPool{localIndex % SgTypeSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypeSymbol::pool_size};
        returnPointer = &(((SgTypeSymbol*)(SgTypeSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypeSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypeSymbol::pools.begin(); block != SgTypeSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSymbol*>(*block)};
        for (unsigned i = 0; i < SgTypeSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypeSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypeSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypeSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTypeSymbol::pools.begin(); block != SgTypeSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypeSymbol*>(*block)};
        for (unsigned i = 0; i < SgTypeSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypeSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypeSymbol::clearMemoryPool() {
    SgTypeSymbol* tempPointer{nullptr};
    if (SgTypeSymbol::pools.empty() == false) {
        auto block{SgTypeSymbol::pools.begin()};
        SgTypeSymbol::next_node = reinterpret_cast<SgTypeSymbol*>(*block);
        for (; block != SgTypeSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypeSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypeSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypeSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypeSymbol::pool_size-1]);
        }
    }
}

void
SgTypeSymbol::deleteMemoryPool() {
    for (auto p: SgTypeSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTypeSymbol::next_node = nullptr;
    SgTypeSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypeSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSymbol)};
    for (size_t blockIndex = SgTypeSymbol::pools.size(); (blockIndex * SgTypeSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSymbol) = %" PRIuPTR " SgTypeSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypeSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypeSymbol),SgTypeSymbol::pool_size);
        }
#endif

        SgTypeSymbol* pointer{static_cast<SgTypeSymbol*>(ROSE_MALLOC(SgTypeSymbol::pool_size * sizeof(SgTypeSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypeSymbol::pool_size * sizeof(SgTypeSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypeSymbol::pool_size * sizeof(SgTypeSymbol));
#endif
        SgTypeSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypeSymbol::pool_size * sizeof(SgTypeSymbol), V_SgTypeSymbol));

        if (SgTypeSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypeSymbol* blkptr{reinterpret_cast<SgTypeSymbol*>(SgTypeSymbol::pools[blockIndex-1])};
                blkptr[SgTypeSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypeSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypeSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypeSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypeSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypeSymbol::pool_size - 1};
    SgTypeSymbol* testPointer{reinterpret_cast<SgTypeSymbol*>(SgTypeSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypeSymbol::pool_size * (SgTypeSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypeSymbolStroageClass
 * from its counterpart of SgTypeSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypeSymbol::initializeStorageClassArray(SgTypeSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypeSymbol::pools.begin(); block != SgTypeSymbol::pools.end(); ++block) {
        SgTypeSymbol* pointer{reinterpret_cast<SgTypeSymbol*>(*block)};
        for (unsigned i = 0; i < SgTypeSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 156885 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgClassSymbol that takes its corresponding StorageClass as parameter
 */
SgClassSymbol::SgClassSymbol(const SgClassSymbolStorageClass& storageSource)  : SgTypeSymbol (storageSource) {

/* #line 156895 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgClassDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgClassSymbol*
SgClassSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgClassSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgClassSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgClassSymbol)};
        unsigned long positionInPool{localIndex % SgClassSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassSymbol::pool_size};
        returnPointer = &(((SgClassSymbol*)(SgClassSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgClassSymbol*
SgClassSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgClassSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgClassSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgClassSymbol)};
        unsigned long positionInPool{localIndex % SgClassSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgClassSymbol::pool_size};
        returnPointer = &(((SgClassSymbol*)(SgClassSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgClassSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgClassSymbol::pools.begin(); block != SgClassSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassSymbol*>(*block)};
        for (unsigned i = 0; i < SgClassSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgClassSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgClassSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgClassSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgClassSymbol::pools.begin(); block != SgClassSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgClassSymbol*>(*block)};
        for (unsigned i = 0; i < SgClassSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgClassSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgClassSymbol::clearMemoryPool() {
    SgClassSymbol* tempPointer{nullptr};
    if (SgClassSymbol::pools.empty() == false) {
        auto block{SgClassSymbol::pools.begin()};
        SgClassSymbol::next_node = reinterpret_cast<SgClassSymbol*>(*block);
        for (; block != SgClassSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgClassSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgClassSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgClassSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgClassSymbol::pool_size-1]);
        }
    }
}

void
SgClassSymbol::deleteMemoryPool() {
    for (auto p: SgClassSymbol::pools) {
        ROSE_FREE(p);
    }
    SgClassSymbol::next_node = nullptr;
    SgClassSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgClassSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgClassSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassSymbol)};
    for (size_t blockIndex = SgClassSymbol::pools.size(); (blockIndex * SgClassSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgClassSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassSymbol) = %" PRIuPTR " SgClassSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgClassSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgClassSymbol),SgClassSymbol::pool_size);
        }
#endif

        SgClassSymbol* pointer{static_cast<SgClassSymbol*>(ROSE_MALLOC(SgClassSymbol::pool_size * sizeof(SgClassSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgClassSymbol::pool_size * sizeof(SgClassSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgClassSymbol::pool_size * sizeof(SgClassSymbol));
#endif
        SgClassSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgClassSymbol::pool_size * sizeof(SgClassSymbol), V_SgClassSymbol));

        if (SgClassSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgClassSymbol* blkptr{reinterpret_cast<SgClassSymbol*>(SgClassSymbol::pools[blockIndex-1])};
                blkptr[SgClassSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgClassSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgClassSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgClassSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgClassSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgClassSymbol::pool_size - 1};
    SgClassSymbol* testPointer{reinterpret_cast<SgClassSymbol*>(SgClassSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgClassSymbol::pool_size * (SgClassSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgClassSymbolStroageClass
 * from its counterpart of SgClassSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgClassSymbol::initializeStorageClassArray(SgClassSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgClassSymbol::pools.begin(); block != SgClassSymbol::pools.end(); ++block) {
        SgClassSymbol* pointer{reinterpret_cast<SgClassSymbol*>(*block)};
        for (unsigned i = 0; i < SgClassSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 157113 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateClassSymbol that takes its corresponding StorageClass as parameter
 */
SgTemplateClassSymbol::SgTemplateClassSymbol(const SgTemplateClassSymbolStorageClass& storageSource)  : SgClassSymbol (storageSource) {

/* #line 157123 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateClassSymbol*
SgTemplateClassSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateClassSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateClassSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateClassSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateClassSymbol::pool_size};
        returnPointer = &(((SgTemplateClassSymbol*)(SgTemplateClassSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateClassSymbol*
SgTemplateClassSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateClassSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateClassSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateClassSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateClassSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateClassSymbol::pool_size};
        returnPointer = &(((SgTemplateClassSymbol*)(SgTemplateClassSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateClassSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateClassSymbol::pools.begin(); block != SgTemplateClassSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateClassSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateClassSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateClassSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateClassSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateClassSymbol::pools.begin(); block != SgTemplateClassSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateClassSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateClassSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateClassSymbol::clearMemoryPool() {
    SgTemplateClassSymbol* tempPointer{nullptr};
    if (SgTemplateClassSymbol::pools.empty() == false) {
        auto block{SgTemplateClassSymbol::pools.begin()};
        SgTemplateClassSymbol::next_node = reinterpret_cast<SgTemplateClassSymbol*>(*block);
        for (; block != SgTemplateClassSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateClassSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateClassSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateClassSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateClassSymbol::pool_size-1]);
        }
    }
}

void
SgTemplateClassSymbol::deleteMemoryPool() {
    for (auto p: SgTemplateClassSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTemplateClassSymbol::next_node = nullptr;
    SgTemplateClassSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateClassSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassSymbol)};
    for (size_t blockIndex = SgTemplateClassSymbol::pools.size(); (blockIndex * SgTemplateClassSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassSymbol) = %" PRIuPTR " SgTemplateClassSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateClassSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateClassSymbol),SgTemplateClassSymbol::pool_size);
        }
#endif

        SgTemplateClassSymbol* pointer{static_cast<SgTemplateClassSymbol*>(ROSE_MALLOC(SgTemplateClassSymbol::pool_size * sizeof(SgTemplateClassSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateClassSymbol::pool_size * sizeof(SgTemplateClassSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateClassSymbol::pool_size * sizeof(SgTemplateClassSymbol));
#endif
        SgTemplateClassSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateClassSymbol::pool_size * sizeof(SgTemplateClassSymbol), V_SgTemplateClassSymbol));

        if (SgTemplateClassSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateClassSymbol* blkptr{reinterpret_cast<SgTemplateClassSymbol*>(SgTemplateClassSymbol::pools[blockIndex-1])};
                blkptr[SgTemplateClassSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateClassSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateClassSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateClassSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateClassSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateClassSymbol::pool_size - 1};
    SgTemplateClassSymbol* testPointer{reinterpret_cast<SgTemplateClassSymbol*>(SgTemplateClassSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateClassSymbol::pool_size * (SgTemplateClassSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateClassSymbolStroageClass
 * from its counterpart of SgTemplateClassSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateClassSymbol::initializeStorageClassArray(SgTemplateClassSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateClassSymbol::pools.begin(); block != SgTemplateClassSymbol::pools.end(); ++block) {
        SgTemplateClassSymbol* pointer{reinterpret_cast<SgTemplateClassSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateClassSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 157340 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTypedefSymbol that takes its corresponding StorageClass as parameter
 */
SgTypedefSymbol::SgTypedefSymbol(const SgTypedefSymbolStorageClass& storageSource)  : SgTypeSymbol (storageSource) {

/* #line 157350 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgTypedefDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTypedefSymbol*
SgTypedefSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTypedefSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTypedefSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSymbol)};
        unsigned long positionInPool{localIndex % SgTypedefSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefSymbol::pool_size};
        returnPointer = &(((SgTypedefSymbol*)(SgTypedefSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTypedefSymbol*
SgTypedefSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTypedefSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTypedefSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTypedefSymbol)};
        unsigned long positionInPool{localIndex % SgTypedefSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTypedefSymbol::pool_size};
        returnPointer = &(((SgTypedefSymbol*)(SgTypedefSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTypedefSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTypedefSymbol::pools.begin(); block != SgTypedefSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefSymbol*>(*block)};
        for (unsigned i = 0; i < SgTypedefSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTypedefSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTypedefSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTypedefSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTypedefSymbol::pools.begin(); block != SgTypedefSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTypedefSymbol*>(*block)};
        for (unsigned i = 0; i < SgTypedefSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTypedefSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTypedefSymbol::clearMemoryPool() {
    SgTypedefSymbol* tempPointer{nullptr};
    if (SgTypedefSymbol::pools.empty() == false) {
        auto block{SgTypedefSymbol::pools.begin()};
        SgTypedefSymbol::next_node = reinterpret_cast<SgTypedefSymbol*>(*block);
        for (; block != SgTypedefSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTypedefSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTypedefSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTypedefSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTypedefSymbol::pool_size-1]);
        }
    }
}

void
SgTypedefSymbol::deleteMemoryPool() {
    for (auto p: SgTypedefSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTypedefSymbol::next_node = nullptr;
    SgTypedefSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTypedefSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefSymbol)};
    for (size_t blockIndex = SgTypedefSymbol::pools.size(); (blockIndex * SgTypedefSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefSymbol) = %" PRIuPTR " SgTypedefSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTypedefSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTypedefSymbol),SgTypedefSymbol::pool_size);
        }
#endif

        SgTypedefSymbol* pointer{static_cast<SgTypedefSymbol*>(ROSE_MALLOC(SgTypedefSymbol::pool_size * sizeof(SgTypedefSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTypedefSymbol::pool_size * sizeof(SgTypedefSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTypedefSymbol::pool_size * sizeof(SgTypedefSymbol));
#endif
        SgTypedefSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTypedefSymbol::pool_size * sizeof(SgTypedefSymbol), V_SgTypedefSymbol));

        if (SgTypedefSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTypedefSymbol* blkptr{reinterpret_cast<SgTypedefSymbol*>(SgTypedefSymbol::pools[blockIndex-1])};
                blkptr[SgTypedefSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTypedefSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTypedefSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTypedefSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTypedefSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTypedefSymbol::pool_size - 1};
    SgTypedefSymbol* testPointer{reinterpret_cast<SgTypedefSymbol*>(SgTypedefSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTypedefSymbol::pool_size * (SgTypedefSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTypedefSymbolStroageClass
 * from its counterpart of SgTypedefSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTypedefSymbol::initializeStorageClassArray(SgTypedefSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTypedefSymbol::pools.begin(); block != SgTypedefSymbol::pools.end(); ++block) {
        SgTypedefSymbol* pointer{reinterpret_cast<SgTypedefSymbol*>(*block)};
        for (unsigned i = 0; i < SgTypedefSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 157568 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgTemplateTypedefSymbol that takes its corresponding StorageClass as parameter
 */
SgTemplateTypedefSymbol::SgTemplateTypedefSymbol(const SgTemplateTypedefSymbolStorageClass& storageSource)  : SgTypedefSymbol (storageSource) {

/* #line 157578 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgTemplateTypedefSymbol*
SgTemplateTypedefSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgTemplateTypedefSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateTypedefSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateTypedefSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgTemplateTypedefSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateTypedefSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateTypedefSymbol::pool_size};
        returnPointer = &(((SgTemplateTypedefSymbol*)(SgTemplateTypedefSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgTemplateTypedefSymbol*
SgTemplateTypedefSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgTemplateTypedefSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateTypedefSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateTypedefSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgTemplateTypedefSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgTemplateTypedefSymbol)};
        unsigned long positionInPool{localIndex % SgTemplateTypedefSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgTemplateTypedefSymbol::pool_size};
        returnPointer = &(((SgTemplateTypedefSymbol*)(SgTemplateTypedefSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgTemplateTypedefSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgTemplateTypedefSymbol::pools.begin(); block != SgTemplateTypedefSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateTypedefSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateTypedefSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgTemplateTypedefSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgTemplateTypedefSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgTemplateTypedefSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgTemplateTypedefSymbol::pools.begin(); block != SgTemplateTypedefSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgTemplateTypedefSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateTypedefSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgTemplateTypedefSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgTemplateTypedefSymbol::clearMemoryPool() {
    SgTemplateTypedefSymbol* tempPointer{nullptr};
    if (SgTemplateTypedefSymbol::pools.empty() == false) {
        auto block{SgTemplateTypedefSymbol::pools.begin()};
        SgTemplateTypedefSymbol::next_node = reinterpret_cast<SgTemplateTypedefSymbol*>(*block);
        for (; block != SgTemplateTypedefSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgTemplateTypedefSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgTemplateTypedefSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgTemplateTypedefSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgTemplateTypedefSymbol::pool_size-1]);
        }
    }
}

void
SgTemplateTypedefSymbol::deleteMemoryPool() {
    for (auto p: SgTemplateTypedefSymbol::pools) {
        ROSE_FREE(p);
    }
    SgTemplateTypedefSymbol::next_node = nullptr;
    SgTemplateTypedefSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgTemplateTypedefSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateTypedefSymbol)};
    for (size_t blockIndex = SgTemplateTypedefSymbol::pools.size(); (blockIndex * SgTemplateTypedefSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateTypedefSymbol) = %" PRIuPTR " SgTemplateTypedefSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgTemplateTypedefSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgTemplateTypedefSymbol),SgTemplateTypedefSymbol::pool_size);
        }
#endif

        SgTemplateTypedefSymbol* pointer{static_cast<SgTemplateTypedefSymbol*>(ROSE_MALLOC(SgTemplateTypedefSymbol::pool_size * sizeof(SgTemplateTypedefSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgTemplateTypedefSymbol::pool_size * sizeof(SgTemplateTypedefSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgTemplateTypedefSymbol::pool_size * sizeof(SgTemplateTypedefSymbol));
#endif
        SgTemplateTypedefSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgTemplateTypedefSymbol::pool_size * sizeof(SgTemplateTypedefSymbol), V_SgTemplateTypedefSymbol));

        if (SgTemplateTypedefSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgTemplateTypedefSymbol* blkptr{reinterpret_cast<SgTemplateTypedefSymbol*>(SgTemplateTypedefSymbol::pools[blockIndex-1])};
                blkptr[SgTemplateTypedefSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgTemplateTypedefSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgTemplateTypedefSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgTemplateTypedefSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgTemplateTypedefSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgTemplateTypedefSymbol::pool_size - 1};
    SgTemplateTypedefSymbol* testPointer{reinterpret_cast<SgTemplateTypedefSymbol*>(SgTemplateTypedefSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgTemplateTypedefSymbol::pool_size * (SgTemplateTypedefSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgTemplateTypedefSymbolStroageClass
 * from its counterpart of SgTemplateTypedefSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgTemplateTypedefSymbol::initializeStorageClassArray(SgTemplateTypedefSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgTemplateTypedefSymbol::pools.begin(); block != SgTemplateTypedefSymbol::pools.end(); ++block) {
        SgTemplateTypedefSymbol* pointer{reinterpret_cast<SgTemplateTypedefSymbol*>(*block)};
        for (unsigned i = 0; i < SgTemplateTypedefSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 157795 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgEnumSymbol that takes its corresponding StorageClass as parameter
 */
SgEnumSymbol::SgEnumSymbol(const SgEnumSymbolStorageClass& storageSource)  : SgTypeSymbol (storageSource) {

/* #line 157805 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgEnumDeclaration*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgEnumSymbol*
SgEnumSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgEnumSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgEnumSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumSymbol)};
        unsigned long positionInPool{localIndex % SgEnumSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumSymbol::pool_size};
        returnPointer = &(((SgEnumSymbol*)(SgEnumSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgEnumSymbol*
SgEnumSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgEnumSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgEnumSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgEnumSymbol)};
        unsigned long positionInPool{localIndex % SgEnumSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgEnumSymbol::pool_size};
        returnPointer = &(((SgEnumSymbol*)(SgEnumSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgEnumSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgEnumSymbol::pools.begin(); block != SgEnumSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumSymbol*>(*block)};
        for (unsigned i = 0; i < SgEnumSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgEnumSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgEnumSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgEnumSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgEnumSymbol::pools.begin(); block != SgEnumSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgEnumSymbol*>(*block)};
        for (unsigned i = 0; i < SgEnumSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgEnumSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgEnumSymbol::clearMemoryPool() {
    SgEnumSymbol* tempPointer{nullptr};
    if (SgEnumSymbol::pools.empty() == false) {
        auto block{SgEnumSymbol::pools.begin()};
        SgEnumSymbol::next_node = reinterpret_cast<SgEnumSymbol*>(*block);
        for (; block != SgEnumSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgEnumSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgEnumSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgEnumSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgEnumSymbol::pool_size-1]);
        }
    }
}

void
SgEnumSymbol::deleteMemoryPool() {
    for (auto p: SgEnumSymbol::pools) {
        ROSE_FREE(p);
    }
    SgEnumSymbol::next_node = nullptr;
    SgEnumSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgEnumSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumSymbol)};
    for (size_t blockIndex = SgEnumSymbol::pools.size(); (blockIndex * SgEnumSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumSymbol) = %" PRIuPTR " SgEnumSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgEnumSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgEnumSymbol),SgEnumSymbol::pool_size);
        }
#endif

        SgEnumSymbol* pointer{static_cast<SgEnumSymbol*>(ROSE_MALLOC(SgEnumSymbol::pool_size * sizeof(SgEnumSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgEnumSymbol::pool_size * sizeof(SgEnumSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgEnumSymbol::pool_size * sizeof(SgEnumSymbol));
#endif
        SgEnumSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgEnumSymbol::pool_size * sizeof(SgEnumSymbol), V_SgEnumSymbol));

        if (SgEnumSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgEnumSymbol* blkptr{reinterpret_cast<SgEnumSymbol*>(SgEnumSymbol::pools[blockIndex-1])};
                blkptr[SgEnumSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgEnumSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgEnumSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgEnumSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgEnumSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgEnumSymbol::pool_size - 1};
    SgEnumSymbol* testPointer{reinterpret_cast<SgEnumSymbol*>(SgEnumSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgEnumSymbol::pool_size * (SgEnumSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgEnumSymbolStroageClass
 * from its counterpart of SgEnumSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgEnumSymbol::initializeStorageClassArray(SgEnumSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgEnumSymbol::pools.begin(); block != SgEnumSymbol::pools.end(); ++block) {
        SgEnumSymbol* pointer{reinterpret_cast<SgEnumSymbol*>(*block)};
        for (unsigned i = 0; i < SgEnumSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 158023 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgLabelSymbol that takes its corresponding StorageClass as parameter
 */
SgLabelSymbol::SgLabelSymbol(const SgLabelSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 158033 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgLabelStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
     p_fortran_statement = (SgStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_fortran_statement));
     p_fortran_alternate_return_parameter = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_fortran_alternate_return_parameter));
    p_numeric_label_value = storageSource.storageOf_numeric_label_value;
    p_label_type = storageSource.storageOf_label_type;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgLabelSymbol*
SgLabelSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgLabelSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgLabelSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelSymbol)};
        unsigned long positionInPool{localIndex % SgLabelSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLabelSymbol::pool_size};
        returnPointer = &(((SgLabelSymbol*)(SgLabelSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgLabelSymbol*
SgLabelSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgLabelSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgLabelSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgLabelSymbol)};
        unsigned long positionInPool{localIndex % SgLabelSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgLabelSymbol::pool_size};
        returnPointer = &(((SgLabelSymbol*)(SgLabelSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgLabelSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgLabelSymbol::pools.begin(); block != SgLabelSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLabelSymbol*>(*block)};
        for (unsigned i = 0; i < SgLabelSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgLabelSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgLabelSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgLabelSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgLabelSymbol::pools.begin(); block != SgLabelSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgLabelSymbol*>(*block)};
        for (unsigned i = 0; i < SgLabelSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgLabelSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgLabelSymbol::clearMemoryPool() {
    SgLabelSymbol* tempPointer{nullptr};
    if (SgLabelSymbol::pools.empty() == false) {
        auto block{SgLabelSymbol::pools.begin()};
        SgLabelSymbol::next_node = reinterpret_cast<SgLabelSymbol*>(*block);
        for (; block != SgLabelSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgLabelSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgLabelSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgLabelSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgLabelSymbol::pool_size-1]);
        }
    }
}

void
SgLabelSymbol::deleteMemoryPool() {
    for (auto p: SgLabelSymbol::pools) {
        ROSE_FREE(p);
    }
    SgLabelSymbol::next_node = nullptr;
    SgLabelSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgLabelSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelSymbol)};
    for (size_t blockIndex = SgLabelSymbol::pools.size(); (blockIndex * SgLabelSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelSymbol) = %" PRIuPTR " SgLabelSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgLabelSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgLabelSymbol),SgLabelSymbol::pool_size);
        }
#endif

        SgLabelSymbol* pointer{static_cast<SgLabelSymbol*>(ROSE_MALLOC(SgLabelSymbol::pool_size * sizeof(SgLabelSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgLabelSymbol::pool_size * sizeof(SgLabelSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgLabelSymbol::pool_size * sizeof(SgLabelSymbol));
#endif
        SgLabelSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgLabelSymbol::pool_size * sizeof(SgLabelSymbol), V_SgLabelSymbol));

        if (SgLabelSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgLabelSymbol* blkptr{reinterpret_cast<SgLabelSymbol*>(SgLabelSymbol::pools[blockIndex-1])};
                blkptr[SgLabelSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgLabelSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgLabelSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgLabelSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgLabelSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgLabelSymbol::pool_size - 1};
    SgLabelSymbol* testPointer{reinterpret_cast<SgLabelSymbol*>(SgLabelSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgLabelSymbol::pool_size * (SgLabelSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgLabelSymbolStroageClass
 * from its counterpart of SgLabelSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgLabelSymbol::initializeStorageClassArray(SgLabelSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgLabelSymbol::pools.begin(); block != SgLabelSymbol::pools.end(); ++block) {
        SgLabelSymbol* pointer{reinterpret_cast<SgLabelSymbol*>(*block)};
        for (unsigned i = 0; i < SgLabelSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 158255 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgDefaultSymbol that takes its corresponding StorageClass as parameter
 */
SgDefaultSymbol::SgDefaultSymbol(const SgDefaultSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 158265 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_type = (SgType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgDefaultSymbol*
SgDefaultSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgDefaultSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefaultSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefaultSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgDefaultSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultSymbol)};
        unsigned long positionInPool{localIndex % SgDefaultSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDefaultSymbol::pool_size};
        returnPointer = &(((SgDefaultSymbol*)(SgDefaultSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgDefaultSymbol*
SgDefaultSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgDefaultSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefaultSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefaultSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgDefaultSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgDefaultSymbol)};
        unsigned long positionInPool{localIndex % SgDefaultSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgDefaultSymbol::pool_size};
        returnPointer = &(((SgDefaultSymbol*)(SgDefaultSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgDefaultSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgDefaultSymbol::pools.begin(); block != SgDefaultSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDefaultSymbol*>(*block)};
        for (unsigned i = 0; i < SgDefaultSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgDefaultSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgDefaultSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgDefaultSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgDefaultSymbol::pools.begin(); block != SgDefaultSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgDefaultSymbol*>(*block)};
        for (unsigned i = 0; i < SgDefaultSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgDefaultSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgDefaultSymbol::clearMemoryPool() {
    SgDefaultSymbol* tempPointer{nullptr};
    if (SgDefaultSymbol::pools.empty() == false) {
        auto block{SgDefaultSymbol::pools.begin()};
        SgDefaultSymbol::next_node = reinterpret_cast<SgDefaultSymbol*>(*block);
        for (; block != SgDefaultSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgDefaultSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgDefaultSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgDefaultSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgDefaultSymbol::pool_size-1]);
        }
    }
}

void
SgDefaultSymbol::deleteMemoryPool() {
    for (auto p: SgDefaultSymbol::pools) {
        ROSE_FREE(p);
    }
    SgDefaultSymbol::next_node = nullptr;
    SgDefaultSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgDefaultSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefaultSymbol)};
    for (size_t blockIndex = SgDefaultSymbol::pools.size(); (blockIndex * SgDefaultSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefaultSymbol) = %" PRIuPTR " SgDefaultSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgDefaultSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgDefaultSymbol),SgDefaultSymbol::pool_size);
        }
#endif

        SgDefaultSymbol* pointer{static_cast<SgDefaultSymbol*>(ROSE_MALLOC(SgDefaultSymbol::pool_size * sizeof(SgDefaultSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgDefaultSymbol::pool_size * sizeof(SgDefaultSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgDefaultSymbol::pool_size * sizeof(SgDefaultSymbol));
#endif
        SgDefaultSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgDefaultSymbol::pool_size * sizeof(SgDefaultSymbol), V_SgDefaultSymbol));

        if (SgDefaultSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgDefaultSymbol* blkptr{reinterpret_cast<SgDefaultSymbol*>(SgDefaultSymbol::pools[blockIndex-1])};
                blkptr[SgDefaultSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgDefaultSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgDefaultSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgDefaultSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgDefaultSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgDefaultSymbol::pool_size - 1};
    SgDefaultSymbol* testPointer{reinterpret_cast<SgDefaultSymbol*>(SgDefaultSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgDefaultSymbol::pool_size * (SgDefaultSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgDefaultSymbolStroageClass
 * from its counterpart of SgDefaultSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgDefaultSymbol::initializeStorageClassArray(SgDefaultSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgDefaultSymbol::pools.begin(); block != SgDefaultSymbol::pools.end(); ++block) {
        SgDefaultSymbol* pointer{reinterpret_cast<SgDefaultSymbol*>(*block)};
        for (unsigned i = 0; i < SgDefaultSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 158483 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgNamespaceSymbol that takes its corresponding StorageClass as parameter
 */
SgNamespaceSymbol::SgNamespaceSymbol(const SgNamespaceSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 158493 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = SgName ( storageSource.storageOf_name);
     p_declaration = (SgNamespaceDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));
     p_aliasDeclaration = (SgNamespaceAliasDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_aliasDeclaration));
    p_isAlias = storageSource.storageOf_isAlias;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgNamespaceSymbol*
SgNamespaceSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgNamespaceSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgNamespaceSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceSymbol)};
        unsigned long positionInPool{localIndex % SgNamespaceSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceSymbol::pool_size};
        returnPointer = &(((SgNamespaceSymbol*)(SgNamespaceSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgNamespaceSymbol*
SgNamespaceSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgNamespaceSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgNamespaceSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgNamespaceSymbol)};
        unsigned long positionInPool{localIndex % SgNamespaceSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgNamespaceSymbol::pool_size};
        returnPointer = &(((SgNamespaceSymbol*)(SgNamespaceSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgNamespaceSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgNamespaceSymbol::pools.begin(); block != SgNamespaceSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceSymbol*>(*block)};
        for (unsigned i = 0; i < SgNamespaceSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgNamespaceSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgNamespaceSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgNamespaceSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgNamespaceSymbol::pools.begin(); block != SgNamespaceSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgNamespaceSymbol*>(*block)};
        for (unsigned i = 0; i < SgNamespaceSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgNamespaceSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgNamespaceSymbol::clearMemoryPool() {
    SgNamespaceSymbol* tempPointer{nullptr};
    if (SgNamespaceSymbol::pools.empty() == false) {
        auto block{SgNamespaceSymbol::pools.begin()};
        SgNamespaceSymbol::next_node = reinterpret_cast<SgNamespaceSymbol*>(*block);
        for (; block != SgNamespaceSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgNamespaceSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgNamespaceSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgNamespaceSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgNamespaceSymbol::pool_size-1]);
        }
    }
}

void
SgNamespaceSymbol::deleteMemoryPool() {
    for (auto p: SgNamespaceSymbol::pools) {
        ROSE_FREE(p);
    }
    SgNamespaceSymbol::next_node = nullptr;
    SgNamespaceSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgNamespaceSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceSymbol)};
    for (size_t blockIndex = SgNamespaceSymbol::pools.size(); (blockIndex * SgNamespaceSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceSymbol) = %" PRIuPTR " SgNamespaceSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgNamespaceSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgNamespaceSymbol),SgNamespaceSymbol::pool_size);
        }
#endif

        SgNamespaceSymbol* pointer{static_cast<SgNamespaceSymbol*>(ROSE_MALLOC(SgNamespaceSymbol::pool_size * sizeof(SgNamespaceSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgNamespaceSymbol::pool_size * sizeof(SgNamespaceSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgNamespaceSymbol::pool_size * sizeof(SgNamespaceSymbol));
#endif
        SgNamespaceSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgNamespaceSymbol::pool_size * sizeof(SgNamespaceSymbol), V_SgNamespaceSymbol));

        if (SgNamespaceSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgNamespaceSymbol* blkptr{reinterpret_cast<SgNamespaceSymbol*>(SgNamespaceSymbol::pools[blockIndex-1])};
                blkptr[SgNamespaceSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgNamespaceSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgNamespaceSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgNamespaceSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgNamespaceSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgNamespaceSymbol::pool_size - 1};
    SgNamespaceSymbol* testPointer{reinterpret_cast<SgNamespaceSymbol*>(SgNamespaceSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgNamespaceSymbol::pool_size * (SgNamespaceSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgNamespaceSymbolStroageClass
 * from its counterpart of SgNamespaceSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgNamespaceSymbol::initializeStorageClassArray(SgNamespaceSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgNamespaceSymbol::pools.begin(); block != SgNamespaceSymbol::pools.end(); ++block) {
        SgNamespaceSymbol* pointer{reinterpret_cast<SgNamespaceSymbol*>(*block)};
        for (unsigned i = 0; i < SgNamespaceSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 158714 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgIntrinsicSymbol that takes its corresponding StorageClass as parameter
 */
SgIntrinsicSymbol::SgIntrinsicSymbol(const SgIntrinsicSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 158724 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgIntrinsicSymbol*
SgIntrinsicSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgIntrinsicSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntrinsicSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntrinsicSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgIntrinsicSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgIntrinsicSymbol)};
        unsigned long positionInPool{localIndex % SgIntrinsicSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntrinsicSymbol::pool_size};
        returnPointer = &(((SgIntrinsicSymbol*)(SgIntrinsicSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgIntrinsicSymbol*
SgIntrinsicSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgIntrinsicSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntrinsicSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntrinsicSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgIntrinsicSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgIntrinsicSymbol)};
        unsigned long positionInPool{localIndex % SgIntrinsicSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgIntrinsicSymbol::pool_size};
        returnPointer = &(((SgIntrinsicSymbol*)(SgIntrinsicSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgIntrinsicSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgIntrinsicSymbol::pools.begin(); block != SgIntrinsicSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntrinsicSymbol*>(*block)};
        for (unsigned i = 0; i < SgIntrinsicSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgIntrinsicSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgIntrinsicSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgIntrinsicSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgIntrinsicSymbol::pools.begin(); block != SgIntrinsicSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgIntrinsicSymbol*>(*block)};
        for (unsigned i = 0; i < SgIntrinsicSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgIntrinsicSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgIntrinsicSymbol::clearMemoryPool() {
    SgIntrinsicSymbol* tempPointer{nullptr};
    if (SgIntrinsicSymbol::pools.empty() == false) {
        auto block{SgIntrinsicSymbol::pools.begin()};
        SgIntrinsicSymbol::next_node = reinterpret_cast<SgIntrinsicSymbol*>(*block);
        for (; block != SgIntrinsicSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgIntrinsicSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgIntrinsicSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgIntrinsicSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgIntrinsicSymbol::pool_size-1]);
        }
    }
}

void
SgIntrinsicSymbol::deleteMemoryPool() {
    for (auto p: SgIntrinsicSymbol::pools) {
        ROSE_FREE(p);
    }
    SgIntrinsicSymbol::next_node = nullptr;
    SgIntrinsicSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgIntrinsicSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgIntrinsicSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntrinsicSymbol)};
    for (size_t blockIndex = SgIntrinsicSymbol::pools.size(); (blockIndex * SgIntrinsicSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgIntrinsicSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntrinsicSymbol) = %" PRIuPTR " SgIntrinsicSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgIntrinsicSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgIntrinsicSymbol),SgIntrinsicSymbol::pool_size);
        }
#endif

        SgIntrinsicSymbol* pointer{static_cast<SgIntrinsicSymbol*>(ROSE_MALLOC(SgIntrinsicSymbol::pool_size * sizeof(SgIntrinsicSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgIntrinsicSymbol::pool_size * sizeof(SgIntrinsicSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgIntrinsicSymbol::pool_size * sizeof(SgIntrinsicSymbol));
#endif
        SgIntrinsicSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgIntrinsicSymbol::pool_size * sizeof(SgIntrinsicSymbol), V_SgIntrinsicSymbol));

        if (SgIntrinsicSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgIntrinsicSymbol* blkptr{reinterpret_cast<SgIntrinsicSymbol*>(SgIntrinsicSymbol::pools[blockIndex-1])};
                blkptr[SgIntrinsicSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgIntrinsicSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgIntrinsicSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgIntrinsicSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgIntrinsicSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgIntrinsicSymbol::pool_size - 1};
    SgIntrinsicSymbol* testPointer{reinterpret_cast<SgIntrinsicSymbol*>(SgIntrinsicSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgIntrinsicSymbol::pool_size * (SgIntrinsicSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgIntrinsicSymbolStroageClass
 * from its counterpart of SgIntrinsicSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgIntrinsicSymbol::initializeStorageClassArray(SgIntrinsicSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgIntrinsicSymbol::pools.begin(); block != SgIntrinsicSymbol::pools.end(); ++block) {
        SgIntrinsicSymbol* pointer{reinterpret_cast<SgIntrinsicSymbol*>(*block)};
        for (unsigned i = 0; i < SgIntrinsicSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 158942 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgModuleSymbol that takes its corresponding StorageClass as parameter
 */
SgModuleSymbol::SgModuleSymbol(const SgModuleSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 158952 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgModuleStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgModuleSymbol*
SgModuleSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgModuleSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModuleSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModuleSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgModuleSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleSymbol)};
        unsigned long positionInPool{localIndex % SgModuleSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModuleSymbol::pool_size};
        returnPointer = &(((SgModuleSymbol*)(SgModuleSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgModuleSymbol*
SgModuleSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgModuleSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModuleSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModuleSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgModuleSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgModuleSymbol)};
        unsigned long positionInPool{localIndex % SgModuleSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgModuleSymbol::pool_size};
        returnPointer = &(((SgModuleSymbol*)(SgModuleSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgModuleSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgModuleSymbol::pools.begin(); block != SgModuleSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModuleSymbol*>(*block)};
        for (unsigned i = 0; i < SgModuleSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgModuleSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgModuleSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgModuleSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgModuleSymbol::pools.begin(); block != SgModuleSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgModuleSymbol*>(*block)};
        for (unsigned i = 0; i < SgModuleSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgModuleSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgModuleSymbol::clearMemoryPool() {
    SgModuleSymbol* tempPointer{nullptr};
    if (SgModuleSymbol::pools.empty() == false) {
        auto block{SgModuleSymbol::pools.begin()};
        SgModuleSymbol::next_node = reinterpret_cast<SgModuleSymbol*>(*block);
        for (; block != SgModuleSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgModuleSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgModuleSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgModuleSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgModuleSymbol::pool_size-1]);
        }
    }
}

void
SgModuleSymbol::deleteMemoryPool() {
    for (auto p: SgModuleSymbol::pools) {
        ROSE_FREE(p);
    }
    SgModuleSymbol::next_node = nullptr;
    SgModuleSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgModuleSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgModuleSymbol)};
    for (size_t blockIndex = SgModuleSymbol::pools.size(); (blockIndex * SgModuleSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgModuleSymbol) = %" PRIuPTR " SgModuleSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgModuleSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgModuleSymbol),SgModuleSymbol::pool_size);
        }
#endif

        SgModuleSymbol* pointer{static_cast<SgModuleSymbol*>(ROSE_MALLOC(SgModuleSymbol::pool_size * sizeof(SgModuleSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgModuleSymbol::pool_size * sizeof(SgModuleSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgModuleSymbol::pool_size * sizeof(SgModuleSymbol));
#endif
        SgModuleSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgModuleSymbol::pool_size * sizeof(SgModuleSymbol), V_SgModuleSymbol));

        if (SgModuleSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgModuleSymbol* blkptr{reinterpret_cast<SgModuleSymbol*>(SgModuleSymbol::pools[blockIndex-1])};
                blkptr[SgModuleSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgModuleSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgModuleSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgModuleSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgModuleSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgModuleSymbol::pool_size - 1};
    SgModuleSymbol* testPointer{reinterpret_cast<SgModuleSymbol*>(SgModuleSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgModuleSymbol::pool_size * (SgModuleSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgModuleSymbolStroageClass
 * from its counterpart of SgModuleSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgModuleSymbol::initializeStorageClassArray(SgModuleSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgModuleSymbol::pools.begin(); block != SgModuleSymbol::pools.end(); ++block) {
        SgModuleSymbol* pointer{reinterpret_cast<SgModuleSymbol*>(*block)};
        for (unsigned i = 0; i < SgModuleSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 159170 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgInterfaceSymbol that takes its corresponding StorageClass as parameter
 */
SgInterfaceSymbol::SgInterfaceSymbol(const SgInterfaceSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 159180 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgInterfaceStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgInterfaceSymbol*
SgInterfaceSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgInterfaceSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgInterfaceSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceSymbol)};
        unsigned long positionInPool{localIndex % SgInterfaceSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInterfaceSymbol::pool_size};
        returnPointer = &(((SgInterfaceSymbol*)(SgInterfaceSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgInterfaceSymbol*
SgInterfaceSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgInterfaceSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgInterfaceSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgInterfaceSymbol)};
        unsigned long positionInPool{localIndex % SgInterfaceSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgInterfaceSymbol::pool_size};
        returnPointer = &(((SgInterfaceSymbol*)(SgInterfaceSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgInterfaceSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgInterfaceSymbol::pools.begin(); block != SgInterfaceSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInterfaceSymbol*>(*block)};
        for (unsigned i = 0; i < SgInterfaceSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgInterfaceSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgInterfaceSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgInterfaceSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgInterfaceSymbol::pools.begin(); block != SgInterfaceSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgInterfaceSymbol*>(*block)};
        for (unsigned i = 0; i < SgInterfaceSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgInterfaceSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgInterfaceSymbol::clearMemoryPool() {
    SgInterfaceSymbol* tempPointer{nullptr};
    if (SgInterfaceSymbol::pools.empty() == false) {
        auto block{SgInterfaceSymbol::pools.begin()};
        SgInterfaceSymbol::next_node = reinterpret_cast<SgInterfaceSymbol*>(*block);
        for (; block != SgInterfaceSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgInterfaceSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgInterfaceSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgInterfaceSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgInterfaceSymbol::pool_size-1]);
        }
    }
}

void
SgInterfaceSymbol::deleteMemoryPool() {
    for (auto p: SgInterfaceSymbol::pools) {
        ROSE_FREE(p);
    }
    SgInterfaceSymbol::next_node = nullptr;
    SgInterfaceSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgInterfaceSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceSymbol)};
    for (size_t blockIndex = SgInterfaceSymbol::pools.size(); (blockIndex * SgInterfaceSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceSymbol) = %" PRIuPTR " SgInterfaceSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgInterfaceSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgInterfaceSymbol),SgInterfaceSymbol::pool_size);
        }
#endif

        SgInterfaceSymbol* pointer{static_cast<SgInterfaceSymbol*>(ROSE_MALLOC(SgInterfaceSymbol::pool_size * sizeof(SgInterfaceSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgInterfaceSymbol::pool_size * sizeof(SgInterfaceSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgInterfaceSymbol::pool_size * sizeof(SgInterfaceSymbol));
#endif
        SgInterfaceSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgInterfaceSymbol::pool_size * sizeof(SgInterfaceSymbol), V_SgInterfaceSymbol));

        if (SgInterfaceSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgInterfaceSymbol* blkptr{reinterpret_cast<SgInterfaceSymbol*>(SgInterfaceSymbol::pools[blockIndex-1])};
                blkptr[SgInterfaceSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgInterfaceSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgInterfaceSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgInterfaceSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgInterfaceSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgInterfaceSymbol::pool_size - 1};
    SgInterfaceSymbol* testPointer{reinterpret_cast<SgInterfaceSymbol*>(SgInterfaceSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgInterfaceSymbol::pool_size * (SgInterfaceSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgInterfaceSymbolStroageClass
 * from its counterpart of SgInterfaceSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgInterfaceSymbol::initializeStorageClassArray(SgInterfaceSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgInterfaceSymbol::pools.begin(); block != SgInterfaceSymbol::pools.end(); ++block) {
        SgInterfaceSymbol* pointer{reinterpret_cast<SgInterfaceSymbol*>(*block)};
        for (unsigned i = 0; i < SgInterfaceSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 159398 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgCommonSymbol that takes its corresponding StorageClass as parameter
 */
SgCommonSymbol::SgCommonSymbol(const SgCommonSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 159408 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgInitializedName*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgCommonSymbol*
SgCommonSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgCommonSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgCommonSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonSymbol)};
        unsigned long positionInPool{localIndex % SgCommonSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommonSymbol::pool_size};
        returnPointer = &(((SgCommonSymbol*)(SgCommonSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgCommonSymbol*
SgCommonSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgCommonSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgCommonSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgCommonSymbol)};
        unsigned long positionInPool{localIndex % SgCommonSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgCommonSymbol::pool_size};
        returnPointer = &(((SgCommonSymbol*)(SgCommonSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgCommonSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgCommonSymbol::pools.begin(); block != SgCommonSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommonSymbol*>(*block)};
        for (unsigned i = 0; i < SgCommonSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgCommonSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgCommonSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgCommonSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgCommonSymbol::pools.begin(); block != SgCommonSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgCommonSymbol*>(*block)};
        for (unsigned i = 0; i < SgCommonSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgCommonSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgCommonSymbol::clearMemoryPool() {
    SgCommonSymbol* tempPointer{nullptr};
    if (SgCommonSymbol::pools.empty() == false) {
        auto block{SgCommonSymbol::pools.begin()};
        SgCommonSymbol::next_node = reinterpret_cast<SgCommonSymbol*>(*block);
        for (; block != SgCommonSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgCommonSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgCommonSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgCommonSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgCommonSymbol::pool_size-1]);
        }
    }
}

void
SgCommonSymbol::deleteMemoryPool() {
    for (auto p: SgCommonSymbol::pools) {
        ROSE_FREE(p);
    }
    SgCommonSymbol::next_node = nullptr;
    SgCommonSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgCommonSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonSymbol)};
    for (size_t blockIndex = SgCommonSymbol::pools.size(); (blockIndex * SgCommonSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonSymbol) = %" PRIuPTR " SgCommonSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgCommonSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgCommonSymbol),SgCommonSymbol::pool_size);
        }
#endif

        SgCommonSymbol* pointer{static_cast<SgCommonSymbol*>(ROSE_MALLOC(SgCommonSymbol::pool_size * sizeof(SgCommonSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgCommonSymbol::pool_size * sizeof(SgCommonSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgCommonSymbol::pool_size * sizeof(SgCommonSymbol));
#endif
        SgCommonSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgCommonSymbol::pool_size * sizeof(SgCommonSymbol), V_SgCommonSymbol));

        if (SgCommonSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgCommonSymbol* blkptr{reinterpret_cast<SgCommonSymbol*>(SgCommonSymbol::pools[blockIndex-1])};
                blkptr[SgCommonSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgCommonSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgCommonSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgCommonSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgCommonSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgCommonSymbol::pool_size - 1};
    SgCommonSymbol* testPointer{reinterpret_cast<SgCommonSymbol*>(SgCommonSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgCommonSymbol::pool_size * (SgCommonSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgCommonSymbolStroageClass
 * from its counterpart of SgCommonSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgCommonSymbol::initializeStorageClassArray(SgCommonSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgCommonSymbol::pools.begin(); block != SgCommonSymbol::pools.end(); ++block) {
        SgCommonSymbol* pointer{reinterpret_cast<SgCommonSymbol*>(*block)};
        for (unsigned i = 0; i < SgCommonSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 159626 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAliasSymbol that takes its corresponding StorageClass as parameter
 */
SgAliasSymbol::SgAliasSymbol(const SgAliasSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 159636 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_alias = (SgSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_alias));
    p_isRenamed = storageSource.storageOf_isRenamed;
    p_new_name = SgName ( storageSource.storageOf_new_name);
    p_causal_nodes = storageSource.storageOf_causal_nodes.rebuildDataStoredInEasyStorageClass();
    SgNodePtrList::iterator i_causal_nodes = p_causal_nodes.begin();
    for ( ; i_causal_nodes != p_causal_nodes.end(); ++i_causal_nodes) {
        (*i_causal_nodes) = (SgNodePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_causal_nodes)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAliasSymbol*
SgAliasSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAliasSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAliasSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAliasSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAliasSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAliasSymbol)};
        unsigned long positionInPool{localIndex % SgAliasSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAliasSymbol::pool_size};
        returnPointer = &(((SgAliasSymbol*)(SgAliasSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAliasSymbol*
SgAliasSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAliasSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAliasSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAliasSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAliasSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAliasSymbol)};
        unsigned long positionInPool{localIndex % SgAliasSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAliasSymbol::pool_size};
        returnPointer = &(((SgAliasSymbol*)(SgAliasSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAliasSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAliasSymbol::pools.begin(); block != SgAliasSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAliasSymbol*>(*block)};
        for (unsigned i = 0; i < SgAliasSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAliasSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAliasSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAliasSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAliasSymbol::pools.begin(); block != SgAliasSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAliasSymbol*>(*block)};
        for (unsigned i = 0; i < SgAliasSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAliasSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAliasSymbol::clearMemoryPool() {
    SgAliasSymbol* tempPointer{nullptr};
    if (SgAliasSymbol::pools.empty() == false) {
        auto block{SgAliasSymbol::pools.begin()};
        SgAliasSymbol::next_node = reinterpret_cast<SgAliasSymbol*>(*block);
        for (; block != SgAliasSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAliasSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAliasSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAliasSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAliasSymbol::pool_size-1]);
        }
    }
}

void
SgAliasSymbol::deleteMemoryPool() {
    for (auto p: SgAliasSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAliasSymbol::next_node = nullptr;
    SgAliasSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAliasSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAliasSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAliasSymbol)};
    for (size_t blockIndex = SgAliasSymbol::pools.size(); (blockIndex * SgAliasSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAliasSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAliasSymbol) = %" PRIuPTR " SgAliasSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAliasSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAliasSymbol),SgAliasSymbol::pool_size);
        }
#endif

        SgAliasSymbol* pointer{static_cast<SgAliasSymbol*>(ROSE_MALLOC(SgAliasSymbol::pool_size * sizeof(SgAliasSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAliasSymbol::pool_size * sizeof(SgAliasSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAliasSymbol::pool_size * sizeof(SgAliasSymbol));
#endif
        SgAliasSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAliasSymbol::pool_size * sizeof(SgAliasSymbol), V_SgAliasSymbol));

        if (SgAliasSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAliasSymbol* blkptr{reinterpret_cast<SgAliasSymbol*>(SgAliasSymbol::pools[blockIndex-1])};
                blkptr[SgAliasSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAliasSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAliasSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAliasSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAliasSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAliasSymbol::pool_size - 1};
    SgAliasSymbol* testPointer{reinterpret_cast<SgAliasSymbol*>(SgAliasSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAliasSymbol::pool_size * (SgAliasSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAliasSymbolStroageClass
 * from its counterpart of SgAliasSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAliasSymbol::initializeStorageClassArray(SgAliasSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAliasSymbol::pools.begin(); block != SgAliasSymbol::pools.end(); ++block) {
        SgAliasSymbol* pointer{reinterpret_cast<SgAliasSymbol*>(*block)};
        for (unsigned i = 0; i < SgAliasSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 159861 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryAddressSymbol that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryAddressSymbol::SgAsmBinaryAddressSymbol(const SgAsmBinaryAddressSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 159871 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_address_name = SgName ( storageSource.storageOf_address_name);
     p_address = (SgAsmInstruction*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_address));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryAddressSymbol*
SgAsmBinaryAddressSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryAddressSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAddressSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAddressSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAddressSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAddressSymbol)};
        unsigned long positionInPool{localIndex % SgAsmBinaryAddressSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryAddressSymbol::pool_size};
        returnPointer = &(((SgAsmBinaryAddressSymbol*)(SgAsmBinaryAddressSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryAddressSymbol*
SgAsmBinaryAddressSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryAddressSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAddressSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAddressSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAddressSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryAddressSymbol)};
        unsigned long positionInPool{localIndex % SgAsmBinaryAddressSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryAddressSymbol::pool_size};
        returnPointer = &(((SgAsmBinaryAddressSymbol*)(SgAsmBinaryAddressSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryAddressSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryAddressSymbol::pools.begin(); block != SgAsmBinaryAddressSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryAddressSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAddressSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryAddressSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryAddressSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryAddressSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryAddressSymbol::pools.begin(); block != SgAsmBinaryAddressSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryAddressSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAddressSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryAddressSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryAddressSymbol::clearMemoryPool() {
    SgAsmBinaryAddressSymbol* tempPointer{nullptr};
    if (SgAsmBinaryAddressSymbol::pools.empty() == false) {
        auto block{SgAsmBinaryAddressSymbol::pools.begin()};
        SgAsmBinaryAddressSymbol::next_node = reinterpret_cast<SgAsmBinaryAddressSymbol*>(*block);
        for (; block != SgAsmBinaryAddressSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryAddressSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryAddressSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryAddressSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryAddressSymbol::pool_size-1]);
        }
    }
}

void
SgAsmBinaryAddressSymbol::deleteMemoryPool() {
    for (auto p: SgAsmBinaryAddressSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryAddressSymbol::next_node = nullptr;
    SgAsmBinaryAddressSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryAddressSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAddressSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAddressSymbol)};
    for (size_t blockIndex = SgAsmBinaryAddressSymbol::pools.size(); (blockIndex * SgAsmBinaryAddressSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAddressSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAddressSymbol) = %" PRIuPTR " SgAsmBinaryAddressSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAddressSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAddressSymbol),SgAsmBinaryAddressSymbol::pool_size);
        }
#endif

        SgAsmBinaryAddressSymbol* pointer{static_cast<SgAsmBinaryAddressSymbol*>(ROSE_MALLOC(SgAsmBinaryAddressSymbol::pool_size * sizeof(SgAsmBinaryAddressSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryAddressSymbol::pool_size * sizeof(SgAsmBinaryAddressSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryAddressSymbol::pool_size * sizeof(SgAsmBinaryAddressSymbol));
#endif
        SgAsmBinaryAddressSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryAddressSymbol::pool_size * sizeof(SgAsmBinaryAddressSymbol), V_SgAsmBinaryAddressSymbol));

        if (SgAsmBinaryAddressSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryAddressSymbol* blkptr{reinterpret_cast<SgAsmBinaryAddressSymbol*>(SgAsmBinaryAddressSymbol::pools[blockIndex-1])};
                blkptr[SgAsmBinaryAddressSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryAddressSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryAddressSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryAddressSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryAddressSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryAddressSymbol::pool_size - 1};
    SgAsmBinaryAddressSymbol* testPointer{reinterpret_cast<SgAsmBinaryAddressSymbol*>(SgAsmBinaryAddressSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryAddressSymbol::pool_size * (SgAsmBinaryAddressSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryAddressSymbolStroageClass
 * from its counterpart of SgAsmBinaryAddressSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryAddressSymbol::initializeStorageClassArray(SgAsmBinaryAddressSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryAddressSymbol::pools.begin(); block != SgAsmBinaryAddressSymbol::pools.end(); ++block) {
        SgAsmBinaryAddressSymbol* pointer{reinterpret_cast<SgAsmBinaryAddressSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAddressSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 160090 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryDataSymbol that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryDataSymbol::SgAsmBinaryDataSymbol(const SgAsmBinaryDataSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 160100 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_variable_name = SgName ( storageSource.storageOf_variable_name);
     p_address = (SgAsmInstruction*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_address));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryDataSymbol*
SgAsmBinaryDataSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryDataSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryDataSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryDataSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryDataSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDataSymbol)};
        unsigned long positionInPool{localIndex % SgAsmBinaryDataSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryDataSymbol::pool_size};
        returnPointer = &(((SgAsmBinaryDataSymbol*)(SgAsmBinaryDataSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryDataSymbol*
SgAsmBinaryDataSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryDataSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryDataSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryDataSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryDataSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryDataSymbol)};
        unsigned long positionInPool{localIndex % SgAsmBinaryDataSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryDataSymbol::pool_size};
        returnPointer = &(((SgAsmBinaryDataSymbol*)(SgAsmBinaryDataSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryDataSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryDataSymbol::pools.begin(); block != SgAsmBinaryDataSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryDataSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryDataSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryDataSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryDataSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryDataSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryDataSymbol::pools.begin(); block != SgAsmBinaryDataSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryDataSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryDataSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryDataSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryDataSymbol::clearMemoryPool() {
    SgAsmBinaryDataSymbol* tempPointer{nullptr};
    if (SgAsmBinaryDataSymbol::pools.empty() == false) {
        auto block{SgAsmBinaryDataSymbol::pools.begin()};
        SgAsmBinaryDataSymbol::next_node = reinterpret_cast<SgAsmBinaryDataSymbol*>(*block);
        for (; block != SgAsmBinaryDataSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryDataSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryDataSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryDataSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryDataSymbol::pool_size-1]);
        }
    }
}

void
SgAsmBinaryDataSymbol::deleteMemoryPool() {
    for (auto p: SgAsmBinaryDataSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryDataSymbol::next_node = nullptr;
    SgAsmBinaryDataSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryDataSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDataSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryDataSymbol)};
    for (size_t blockIndex = SgAsmBinaryDataSymbol::pools.size(); (blockIndex * SgAsmBinaryDataSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDataSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryDataSymbol) = %" PRIuPTR " SgAsmBinaryDataSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDataSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryDataSymbol),SgAsmBinaryDataSymbol::pool_size);
        }
#endif

        SgAsmBinaryDataSymbol* pointer{static_cast<SgAsmBinaryDataSymbol*>(ROSE_MALLOC(SgAsmBinaryDataSymbol::pool_size * sizeof(SgAsmBinaryDataSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryDataSymbol::pool_size * sizeof(SgAsmBinaryDataSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryDataSymbol::pool_size * sizeof(SgAsmBinaryDataSymbol));
#endif
        SgAsmBinaryDataSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryDataSymbol::pool_size * sizeof(SgAsmBinaryDataSymbol), V_SgAsmBinaryDataSymbol));

        if (SgAsmBinaryDataSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryDataSymbol* blkptr{reinterpret_cast<SgAsmBinaryDataSymbol*>(SgAsmBinaryDataSymbol::pools[blockIndex-1])};
                blkptr[SgAsmBinaryDataSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryDataSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryDataSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryDataSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryDataSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryDataSymbol::pool_size - 1};
    SgAsmBinaryDataSymbol* testPointer{reinterpret_cast<SgAsmBinaryDataSymbol*>(SgAsmBinaryDataSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryDataSymbol::pool_size * (SgAsmBinaryDataSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryDataSymbolStroageClass
 * from its counterpart of SgAsmBinaryDataSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryDataSymbol::initializeStorageClassArray(SgAsmBinaryDataSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryDataSymbol::pools.begin(); block != SgAsmBinaryDataSymbol::pools.end(); ++block) {
        SgAsmBinaryDataSymbol* pointer{reinterpret_cast<SgAsmBinaryDataSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryDataSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 160319 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgJavaLabelSymbol that takes its corresponding StorageClass as parameter
 */
SgJavaLabelSymbol::SgJavaLabelSymbol(const SgJavaLabelSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 160329 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgJavaLabelStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgJavaLabelSymbol*
SgJavaLabelSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgJavaLabelSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaLabelSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaLabelSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgJavaLabelSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelSymbol)};
        unsigned long positionInPool{localIndex % SgJavaLabelSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaLabelSymbol::pool_size};
        returnPointer = &(((SgJavaLabelSymbol*)(SgJavaLabelSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgJavaLabelSymbol*
SgJavaLabelSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgJavaLabelSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaLabelSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaLabelSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgJavaLabelSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgJavaLabelSymbol)};
        unsigned long positionInPool{localIndex % SgJavaLabelSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgJavaLabelSymbol::pool_size};
        returnPointer = &(((SgJavaLabelSymbol*)(SgJavaLabelSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgJavaLabelSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgJavaLabelSymbol::pools.begin(); block != SgJavaLabelSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaLabelSymbol*>(*block)};
        for (unsigned i = 0; i < SgJavaLabelSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgJavaLabelSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgJavaLabelSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgJavaLabelSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgJavaLabelSymbol::pools.begin(); block != SgJavaLabelSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgJavaLabelSymbol*>(*block)};
        for (unsigned i = 0; i < SgJavaLabelSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgJavaLabelSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgJavaLabelSymbol::clearMemoryPool() {
    SgJavaLabelSymbol* tempPointer{nullptr};
    if (SgJavaLabelSymbol::pools.empty() == false) {
        auto block{SgJavaLabelSymbol::pools.begin()};
        SgJavaLabelSymbol::next_node = reinterpret_cast<SgJavaLabelSymbol*>(*block);
        for (; block != SgJavaLabelSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgJavaLabelSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgJavaLabelSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgJavaLabelSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgJavaLabelSymbol::pool_size-1]);
        }
    }
}

void
SgJavaLabelSymbol::deleteMemoryPool() {
    for (auto p: SgJavaLabelSymbol::pools) {
        ROSE_FREE(p);
    }
    SgJavaLabelSymbol::next_node = nullptr;
    SgJavaLabelSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgJavaLabelSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaLabelSymbol)};
    for (size_t blockIndex = SgJavaLabelSymbol::pools.size(); (blockIndex * SgJavaLabelSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaLabelSymbol) = %" PRIuPTR " SgJavaLabelSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgJavaLabelSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgJavaLabelSymbol),SgJavaLabelSymbol::pool_size);
        }
#endif

        SgJavaLabelSymbol* pointer{static_cast<SgJavaLabelSymbol*>(ROSE_MALLOC(SgJavaLabelSymbol::pool_size * sizeof(SgJavaLabelSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgJavaLabelSymbol::pool_size * sizeof(SgJavaLabelSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgJavaLabelSymbol::pool_size * sizeof(SgJavaLabelSymbol));
#endif
        SgJavaLabelSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgJavaLabelSymbol::pool_size * sizeof(SgJavaLabelSymbol), V_SgJavaLabelSymbol));

        if (SgJavaLabelSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgJavaLabelSymbol* blkptr{reinterpret_cast<SgJavaLabelSymbol*>(SgJavaLabelSymbol::pools[blockIndex-1])};
                blkptr[SgJavaLabelSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgJavaLabelSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgJavaLabelSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgJavaLabelSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgJavaLabelSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgJavaLabelSymbol::pool_size - 1};
    SgJavaLabelSymbol* testPointer{reinterpret_cast<SgJavaLabelSymbol*>(SgJavaLabelSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgJavaLabelSymbol::pool_size * (SgJavaLabelSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgJavaLabelSymbolStroageClass
 * from its counterpart of SgJavaLabelSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgJavaLabelSymbol::initializeStorageClassArray(SgJavaLabelSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgJavaLabelSymbol::pools.begin(); block != SgJavaLabelSymbol::pools.end(); ++block) {
        SgJavaLabelSymbol* pointer{reinterpret_cast<SgJavaLabelSymbol*>(*block)};
        for (unsigned i = 0; i < SgJavaLabelSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 160547 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaPackageSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaPackageSymbol::SgAdaPackageSymbol(const SgAdaPackageSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 160557 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaPackageSymbol*
SgAdaPackageSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaPackageSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaPackageSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSymbol)};
        unsigned long positionInPool{localIndex % SgAdaPackageSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageSymbol::pool_size};
        returnPointer = &(((SgAdaPackageSymbol*)(SgAdaPackageSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaPackageSymbol*
SgAdaPackageSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaPackageSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaPackageSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaPackageSymbol)};
        unsigned long positionInPool{localIndex % SgAdaPackageSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaPackageSymbol::pool_size};
        returnPointer = &(((SgAdaPackageSymbol*)(SgAdaPackageSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaPackageSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaPackageSymbol::pools.begin(); block != SgAdaPackageSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaPackageSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaPackageSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaPackageSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaPackageSymbol::pools.begin(); block != SgAdaPackageSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaPackageSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaPackageSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaPackageSymbol::clearMemoryPool() {
    SgAdaPackageSymbol* tempPointer{nullptr};
    if (SgAdaPackageSymbol::pools.empty() == false) {
        auto block{SgAdaPackageSymbol::pools.begin()};
        SgAdaPackageSymbol::next_node = reinterpret_cast<SgAdaPackageSymbol*>(*block);
        for (; block != SgAdaPackageSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaPackageSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaPackageSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaPackageSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaPackageSymbol::pool_size-1]);
        }
    }
}

void
SgAdaPackageSymbol::deleteMemoryPool() {
    for (auto p: SgAdaPackageSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaPackageSymbol::next_node = nullptr;
    SgAdaPackageSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaPackageSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSymbol)};
    for (size_t blockIndex = SgAdaPackageSymbol::pools.size(); (blockIndex * SgAdaPackageSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSymbol) = %" PRIuPTR " SgAdaPackageSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaPackageSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaPackageSymbol),SgAdaPackageSymbol::pool_size);
        }
#endif

        SgAdaPackageSymbol* pointer{static_cast<SgAdaPackageSymbol*>(ROSE_MALLOC(SgAdaPackageSymbol::pool_size * sizeof(SgAdaPackageSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaPackageSymbol::pool_size * sizeof(SgAdaPackageSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaPackageSymbol::pool_size * sizeof(SgAdaPackageSymbol));
#endif
        SgAdaPackageSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaPackageSymbol::pool_size * sizeof(SgAdaPackageSymbol), V_SgAdaPackageSymbol));

        if (SgAdaPackageSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaPackageSymbol* blkptr{reinterpret_cast<SgAdaPackageSymbol*>(SgAdaPackageSymbol::pools[blockIndex-1])};
                blkptr[SgAdaPackageSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaPackageSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaPackageSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaPackageSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaPackageSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaPackageSymbol::pool_size - 1};
    SgAdaPackageSymbol* testPointer{reinterpret_cast<SgAdaPackageSymbol*>(SgAdaPackageSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaPackageSymbol::pool_size * (SgAdaPackageSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaPackageSymbolStroageClass
 * from its counterpart of SgAdaPackageSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaPackageSymbol::initializeStorageClassArray(SgAdaPackageSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaPackageSymbol::pools.begin(); block != SgAdaPackageSymbol::pools.end(); ++block) {
        SgAdaPackageSymbol* pointer{reinterpret_cast<SgAdaPackageSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaPackageSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 160775 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaTaskSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaTaskSymbol::SgAdaTaskSymbol(const SgAdaTaskSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 160785 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaTaskSymbol*
SgAdaTaskSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaTaskSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaTaskSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSymbol)};
        unsigned long positionInPool{localIndex % SgAdaTaskSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskSymbol::pool_size};
        returnPointer = &(((SgAdaTaskSymbol*)(SgAdaTaskSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaTaskSymbol*
SgAdaTaskSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaTaskSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaTaskSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaTaskSymbol)};
        unsigned long positionInPool{localIndex % SgAdaTaskSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaTaskSymbol::pool_size};
        returnPointer = &(((SgAdaTaskSymbol*)(SgAdaTaskSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaTaskSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaTaskSymbol::pools.begin(); block != SgAdaTaskSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaTaskSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaTaskSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaTaskSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaTaskSymbol::pools.begin(); block != SgAdaTaskSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaTaskSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaTaskSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaTaskSymbol::clearMemoryPool() {
    SgAdaTaskSymbol* tempPointer{nullptr};
    if (SgAdaTaskSymbol::pools.empty() == false) {
        auto block{SgAdaTaskSymbol::pools.begin()};
        SgAdaTaskSymbol::next_node = reinterpret_cast<SgAdaTaskSymbol*>(*block);
        for (; block != SgAdaTaskSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaTaskSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaTaskSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaTaskSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaTaskSymbol::pool_size-1]);
        }
    }
}

void
SgAdaTaskSymbol::deleteMemoryPool() {
    for (auto p: SgAdaTaskSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaTaskSymbol::next_node = nullptr;
    SgAdaTaskSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaTaskSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSymbol)};
    for (size_t blockIndex = SgAdaTaskSymbol::pools.size(); (blockIndex * SgAdaTaskSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSymbol) = %" PRIuPTR " SgAdaTaskSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaTaskSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaTaskSymbol),SgAdaTaskSymbol::pool_size);
        }
#endif

        SgAdaTaskSymbol* pointer{static_cast<SgAdaTaskSymbol*>(ROSE_MALLOC(SgAdaTaskSymbol::pool_size * sizeof(SgAdaTaskSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaTaskSymbol::pool_size * sizeof(SgAdaTaskSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaTaskSymbol::pool_size * sizeof(SgAdaTaskSymbol));
#endif
        SgAdaTaskSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaTaskSymbol::pool_size * sizeof(SgAdaTaskSymbol), V_SgAdaTaskSymbol));

        if (SgAdaTaskSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaTaskSymbol* blkptr{reinterpret_cast<SgAdaTaskSymbol*>(SgAdaTaskSymbol::pools[blockIndex-1])};
                blkptr[SgAdaTaskSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaTaskSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaTaskSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaTaskSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaTaskSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaTaskSymbol::pool_size - 1};
    SgAdaTaskSymbol* testPointer{reinterpret_cast<SgAdaTaskSymbol*>(SgAdaTaskSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaTaskSymbol::pool_size * (SgAdaTaskSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaTaskSymbolStroageClass
 * from its counterpart of SgAdaTaskSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaTaskSymbol::initializeStorageClassArray(SgAdaTaskSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaTaskSymbol::pools.begin(); block != SgAdaTaskSymbol::pools.end(); ++block) {
        SgAdaTaskSymbol* pointer{reinterpret_cast<SgAdaTaskSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaTaskSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 161003 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaProtectedSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaProtectedSymbol::SgAdaProtectedSymbol(const SgAdaProtectedSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 161013 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaProtectedSymbol*
SgAdaProtectedSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaProtectedSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaProtectedSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSymbol)};
        unsigned long positionInPool{localIndex % SgAdaProtectedSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedSymbol::pool_size};
        returnPointer = &(((SgAdaProtectedSymbol*)(SgAdaProtectedSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaProtectedSymbol*
SgAdaProtectedSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaProtectedSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaProtectedSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaProtectedSymbol)};
        unsigned long positionInPool{localIndex % SgAdaProtectedSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaProtectedSymbol::pool_size};
        returnPointer = &(((SgAdaProtectedSymbol*)(SgAdaProtectedSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaProtectedSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaProtectedSymbol::pools.begin(); block != SgAdaProtectedSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaProtectedSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaProtectedSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaProtectedSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaProtectedSymbol::pools.begin(); block != SgAdaProtectedSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaProtectedSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaProtectedSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaProtectedSymbol::clearMemoryPool() {
    SgAdaProtectedSymbol* tempPointer{nullptr};
    if (SgAdaProtectedSymbol::pools.empty() == false) {
        auto block{SgAdaProtectedSymbol::pools.begin()};
        SgAdaProtectedSymbol::next_node = reinterpret_cast<SgAdaProtectedSymbol*>(*block);
        for (; block != SgAdaProtectedSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaProtectedSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaProtectedSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaProtectedSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaProtectedSymbol::pool_size-1]);
        }
    }
}

void
SgAdaProtectedSymbol::deleteMemoryPool() {
    for (auto p: SgAdaProtectedSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaProtectedSymbol::next_node = nullptr;
    SgAdaProtectedSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaProtectedSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSymbol)};
    for (size_t blockIndex = SgAdaProtectedSymbol::pools.size(); (blockIndex * SgAdaProtectedSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSymbol) = %" PRIuPTR " SgAdaProtectedSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaProtectedSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaProtectedSymbol),SgAdaProtectedSymbol::pool_size);
        }
#endif

        SgAdaProtectedSymbol* pointer{static_cast<SgAdaProtectedSymbol*>(ROSE_MALLOC(SgAdaProtectedSymbol::pool_size * sizeof(SgAdaProtectedSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaProtectedSymbol::pool_size * sizeof(SgAdaProtectedSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaProtectedSymbol::pool_size * sizeof(SgAdaProtectedSymbol));
#endif
        SgAdaProtectedSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaProtectedSymbol::pool_size * sizeof(SgAdaProtectedSymbol), V_SgAdaProtectedSymbol));

        if (SgAdaProtectedSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaProtectedSymbol* blkptr{reinterpret_cast<SgAdaProtectedSymbol*>(SgAdaProtectedSymbol::pools[blockIndex-1])};
                blkptr[SgAdaProtectedSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaProtectedSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaProtectedSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaProtectedSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaProtectedSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaProtectedSymbol::pool_size - 1};
    SgAdaProtectedSymbol* testPointer{reinterpret_cast<SgAdaProtectedSymbol*>(SgAdaProtectedSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaProtectedSymbol::pool_size * (SgAdaProtectedSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaProtectedSymbolStroageClass
 * from its counterpart of SgAdaProtectedSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaProtectedSymbol::initializeStorageClassArray(SgAdaProtectedSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaProtectedSymbol::pools.begin(); block != SgAdaProtectedSymbol::pools.end(); ++block) {
        SgAdaProtectedSymbol* pointer{reinterpret_cast<SgAdaProtectedSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaProtectedSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 161231 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaRenamingSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaRenamingSymbol::SgAdaRenamingSymbol(const SgAdaRenamingSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 161241 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgAdaRenamingDecl*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaRenamingSymbol*
SgAdaRenamingSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaRenamingSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaRenamingSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingSymbol)};
        unsigned long positionInPool{localIndex % SgAdaRenamingSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRenamingSymbol::pool_size};
        returnPointer = &(((SgAdaRenamingSymbol*)(SgAdaRenamingSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaRenamingSymbol*
SgAdaRenamingSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaRenamingSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaRenamingSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaRenamingSymbol)};
        unsigned long positionInPool{localIndex % SgAdaRenamingSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaRenamingSymbol::pool_size};
        returnPointer = &(((SgAdaRenamingSymbol*)(SgAdaRenamingSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaRenamingSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaRenamingSymbol::pools.begin(); block != SgAdaRenamingSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRenamingSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaRenamingSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaRenamingSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaRenamingSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaRenamingSymbol::pools.begin(); block != SgAdaRenamingSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaRenamingSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaRenamingSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaRenamingSymbol::clearMemoryPool() {
    SgAdaRenamingSymbol* tempPointer{nullptr};
    if (SgAdaRenamingSymbol::pools.empty() == false) {
        auto block{SgAdaRenamingSymbol::pools.begin()};
        SgAdaRenamingSymbol::next_node = reinterpret_cast<SgAdaRenamingSymbol*>(*block);
        for (; block != SgAdaRenamingSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaRenamingSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaRenamingSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaRenamingSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaRenamingSymbol::pool_size-1]);
        }
    }
}

void
SgAdaRenamingSymbol::deleteMemoryPool() {
    for (auto p: SgAdaRenamingSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaRenamingSymbol::next_node = nullptr;
    SgAdaRenamingSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaRenamingSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingSymbol)};
    for (size_t blockIndex = SgAdaRenamingSymbol::pools.size(); (blockIndex * SgAdaRenamingSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingSymbol) = %" PRIuPTR " SgAdaRenamingSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaRenamingSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaRenamingSymbol),SgAdaRenamingSymbol::pool_size);
        }
#endif

        SgAdaRenamingSymbol* pointer{static_cast<SgAdaRenamingSymbol*>(ROSE_MALLOC(SgAdaRenamingSymbol::pool_size * sizeof(SgAdaRenamingSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaRenamingSymbol::pool_size * sizeof(SgAdaRenamingSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaRenamingSymbol::pool_size * sizeof(SgAdaRenamingSymbol));
#endif
        SgAdaRenamingSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaRenamingSymbol::pool_size * sizeof(SgAdaRenamingSymbol), V_SgAdaRenamingSymbol));

        if (SgAdaRenamingSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaRenamingSymbol* blkptr{reinterpret_cast<SgAdaRenamingSymbol*>(SgAdaRenamingSymbol::pools[blockIndex-1])};
                blkptr[SgAdaRenamingSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaRenamingSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaRenamingSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaRenamingSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaRenamingSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaRenamingSymbol::pool_size - 1};
    SgAdaRenamingSymbol* testPointer{reinterpret_cast<SgAdaRenamingSymbol*>(SgAdaRenamingSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaRenamingSymbol::pool_size * (SgAdaRenamingSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaRenamingSymbolStroageClass
 * from its counterpart of SgAdaRenamingSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaRenamingSymbol::initializeStorageClassArray(SgAdaRenamingSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaRenamingSymbol::pools.begin(); block != SgAdaRenamingSymbol::pools.end(); ++block) {
        SgAdaRenamingSymbol* pointer{reinterpret_cast<SgAdaRenamingSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaRenamingSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 161459 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaGenericSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaGenericSymbol::SgAdaGenericSymbol(const SgAdaGenericSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 161469 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgDeclarationStatement*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaGenericSymbol*
SgAdaGenericSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaGenericSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericSymbol)};
        unsigned long positionInPool{localIndex % SgAdaGenericSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericSymbol::pool_size};
        returnPointer = &(((SgAdaGenericSymbol*)(SgAdaGenericSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaGenericSymbol*
SgAdaGenericSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaGenericSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaGenericSymbol)};
        unsigned long positionInPool{localIndex % SgAdaGenericSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericSymbol::pool_size};
        returnPointer = &(((SgAdaGenericSymbol*)(SgAdaGenericSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaGenericSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaGenericSymbol::pools.begin(); block != SgAdaGenericSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaGenericSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaGenericSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaGenericSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaGenericSymbol::pools.begin(); block != SgAdaGenericSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaGenericSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaGenericSymbol::clearMemoryPool() {
    SgAdaGenericSymbol* tempPointer{nullptr};
    if (SgAdaGenericSymbol::pools.empty() == false) {
        auto block{SgAdaGenericSymbol::pools.begin()};
        SgAdaGenericSymbol::next_node = reinterpret_cast<SgAdaGenericSymbol*>(*block);
        for (; block != SgAdaGenericSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaGenericSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaGenericSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaGenericSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaGenericSymbol::pool_size-1]);
        }
    }
}

void
SgAdaGenericSymbol::deleteMemoryPool() {
    for (auto p: SgAdaGenericSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaGenericSymbol::next_node = nullptr;
    SgAdaGenericSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaGenericSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericSymbol)};
    for (size_t blockIndex = SgAdaGenericSymbol::pools.size(); (blockIndex * SgAdaGenericSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericSymbol) = %" PRIuPTR " SgAdaGenericSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericSymbol),SgAdaGenericSymbol::pool_size);
        }
#endif

        SgAdaGenericSymbol* pointer{static_cast<SgAdaGenericSymbol*>(ROSE_MALLOC(SgAdaGenericSymbol::pool_size * sizeof(SgAdaGenericSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaGenericSymbol::pool_size * sizeof(SgAdaGenericSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaGenericSymbol::pool_size * sizeof(SgAdaGenericSymbol));
#endif
        SgAdaGenericSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaGenericSymbol::pool_size * sizeof(SgAdaGenericSymbol), V_SgAdaGenericSymbol));

        if (SgAdaGenericSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaGenericSymbol* blkptr{reinterpret_cast<SgAdaGenericSymbol*>(SgAdaGenericSymbol::pools[blockIndex-1])};
                blkptr[SgAdaGenericSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaGenericSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaGenericSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaGenericSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaGenericSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaGenericSymbol::pool_size - 1};
    SgAdaGenericSymbol* testPointer{reinterpret_cast<SgAdaGenericSymbol*>(SgAdaGenericSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaGenericSymbol::pool_size * (SgAdaGenericSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaGenericSymbolStroageClass
 * from its counterpart of SgAdaGenericSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaGenericSymbol::initializeStorageClassArray(SgAdaGenericSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaGenericSymbol::pools.begin(); block != SgAdaGenericSymbol::pools.end(); ++block) {
        SgAdaGenericSymbol* pointer{reinterpret_cast<SgAdaGenericSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 161687 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaGenericInstanceSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaGenericInstanceSymbol::SgAdaGenericInstanceSymbol(const SgAdaGenericInstanceSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 161697 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgAdaGenericInstanceDecl*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaGenericInstanceSymbol*
SgAdaGenericInstanceSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaGenericInstanceSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericInstanceSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericInstanceSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaGenericInstanceSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceSymbol)};
        unsigned long positionInPool{localIndex % SgAdaGenericInstanceSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericInstanceSymbol::pool_size};
        returnPointer = &(((SgAdaGenericInstanceSymbol*)(SgAdaGenericInstanceSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaGenericInstanceSymbol*
SgAdaGenericInstanceSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaGenericInstanceSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericInstanceSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericInstanceSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaGenericInstanceSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaGenericInstanceSymbol)};
        unsigned long positionInPool{localIndex % SgAdaGenericInstanceSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaGenericInstanceSymbol::pool_size};
        returnPointer = &(((SgAdaGenericInstanceSymbol*)(SgAdaGenericInstanceSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaGenericInstanceSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaGenericInstanceSymbol::pools.begin(); block != SgAdaGenericInstanceSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericInstanceSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericInstanceSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaGenericInstanceSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaGenericInstanceSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaGenericInstanceSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaGenericInstanceSymbol::pools.begin(); block != SgAdaGenericInstanceSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaGenericInstanceSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericInstanceSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaGenericInstanceSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaGenericInstanceSymbol::clearMemoryPool() {
    SgAdaGenericInstanceSymbol* tempPointer{nullptr};
    if (SgAdaGenericInstanceSymbol::pools.empty() == false) {
        auto block{SgAdaGenericInstanceSymbol::pools.begin()};
        SgAdaGenericInstanceSymbol::next_node = reinterpret_cast<SgAdaGenericInstanceSymbol*>(*block);
        for (; block != SgAdaGenericInstanceSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaGenericInstanceSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaGenericInstanceSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaGenericInstanceSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaGenericInstanceSymbol::pool_size-1]);
        }
    }
}

void
SgAdaGenericInstanceSymbol::deleteMemoryPool() {
    for (auto p: SgAdaGenericInstanceSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaGenericInstanceSymbol::next_node = nullptr;
    SgAdaGenericInstanceSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaGenericInstanceSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericInstanceSymbol)};
    for (size_t blockIndex = SgAdaGenericInstanceSymbol::pools.size(); (blockIndex * SgAdaGenericInstanceSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericInstanceSymbol) = %" PRIuPTR " SgAdaGenericInstanceSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaGenericInstanceSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaGenericInstanceSymbol),SgAdaGenericInstanceSymbol::pool_size);
        }
#endif

        SgAdaGenericInstanceSymbol* pointer{static_cast<SgAdaGenericInstanceSymbol*>(ROSE_MALLOC(SgAdaGenericInstanceSymbol::pool_size * sizeof(SgAdaGenericInstanceSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaGenericInstanceSymbol::pool_size * sizeof(SgAdaGenericInstanceSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaGenericInstanceSymbol::pool_size * sizeof(SgAdaGenericInstanceSymbol));
#endif
        SgAdaGenericInstanceSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaGenericInstanceSymbol::pool_size * sizeof(SgAdaGenericInstanceSymbol), V_SgAdaGenericInstanceSymbol));

        if (SgAdaGenericInstanceSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaGenericInstanceSymbol* blkptr{reinterpret_cast<SgAdaGenericInstanceSymbol*>(SgAdaGenericInstanceSymbol::pools[blockIndex-1])};
                blkptr[SgAdaGenericInstanceSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaGenericInstanceSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaGenericInstanceSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaGenericInstanceSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaGenericInstanceSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaGenericInstanceSymbol::pool_size - 1};
    SgAdaGenericInstanceSymbol* testPointer{reinterpret_cast<SgAdaGenericInstanceSymbol*>(SgAdaGenericInstanceSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaGenericInstanceSymbol::pool_size * (SgAdaGenericInstanceSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaGenericInstanceSymbolStroageClass
 * from its counterpart of SgAdaGenericInstanceSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaGenericInstanceSymbol::initializeStorageClassArray(SgAdaGenericInstanceSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaGenericInstanceSymbol::pools.begin(); block != SgAdaGenericInstanceSymbol::pools.end(); ++block) {
        SgAdaGenericInstanceSymbol* pointer{reinterpret_cast<SgAdaGenericInstanceSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaGenericInstanceSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 161915 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAdaFormalPackageSymbol that takes its corresponding StorageClass as parameter
 */
SgAdaFormalPackageSymbol::SgAdaFormalPackageSymbol(const SgAdaFormalPackageSymbolStorageClass& storageSource)  : SgSymbol (storageSource) {

/* #line 161925 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_declaration = (SgAdaFormalPackageDecl*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_declaration));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAdaFormalPackageSymbol*
SgAdaFormalPackageSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAdaFormalPackageSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalPackageSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalPackageSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAdaFormalPackageSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageSymbol)};
        unsigned long positionInPool{localIndex % SgAdaFormalPackageSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalPackageSymbol::pool_size};
        returnPointer = &(((SgAdaFormalPackageSymbol*)(SgAdaFormalPackageSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAdaFormalPackageSymbol*
SgAdaFormalPackageSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAdaFormalPackageSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalPackageSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalPackageSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAdaFormalPackageSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAdaFormalPackageSymbol)};
        unsigned long positionInPool{localIndex % SgAdaFormalPackageSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAdaFormalPackageSymbol::pool_size};
        returnPointer = &(((SgAdaFormalPackageSymbol*)(SgAdaFormalPackageSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAdaFormalPackageSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAdaFormalPackageSymbol::pools.begin(); block != SgAdaFormalPackageSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalPackageSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalPackageSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAdaFormalPackageSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAdaFormalPackageSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAdaFormalPackageSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAdaFormalPackageSymbol::pools.begin(); block != SgAdaFormalPackageSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAdaFormalPackageSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalPackageSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAdaFormalPackageSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAdaFormalPackageSymbol::clearMemoryPool() {
    SgAdaFormalPackageSymbol* tempPointer{nullptr};
    if (SgAdaFormalPackageSymbol::pools.empty() == false) {
        auto block{SgAdaFormalPackageSymbol::pools.begin()};
        SgAdaFormalPackageSymbol::next_node = reinterpret_cast<SgAdaFormalPackageSymbol*>(*block);
        for (; block != SgAdaFormalPackageSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAdaFormalPackageSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAdaFormalPackageSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAdaFormalPackageSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAdaFormalPackageSymbol::pool_size-1]);
        }
    }
}

void
SgAdaFormalPackageSymbol::deleteMemoryPool() {
    for (auto p: SgAdaFormalPackageSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAdaFormalPackageSymbol::next_node = nullptr;
    SgAdaFormalPackageSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAdaFormalPackageSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalPackageSymbol)};
    for (size_t blockIndex = SgAdaFormalPackageSymbol::pools.size(); (blockIndex * SgAdaFormalPackageSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalPackageSymbol) = %" PRIuPTR " SgAdaFormalPackageSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAdaFormalPackageSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAdaFormalPackageSymbol),SgAdaFormalPackageSymbol::pool_size);
        }
#endif

        SgAdaFormalPackageSymbol* pointer{static_cast<SgAdaFormalPackageSymbol*>(ROSE_MALLOC(SgAdaFormalPackageSymbol::pool_size * sizeof(SgAdaFormalPackageSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAdaFormalPackageSymbol::pool_size * sizeof(SgAdaFormalPackageSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAdaFormalPackageSymbol::pool_size * sizeof(SgAdaFormalPackageSymbol));
#endif
        SgAdaFormalPackageSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAdaFormalPackageSymbol::pool_size * sizeof(SgAdaFormalPackageSymbol), V_SgAdaFormalPackageSymbol));

        if (SgAdaFormalPackageSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAdaFormalPackageSymbol* blkptr{reinterpret_cast<SgAdaFormalPackageSymbol*>(SgAdaFormalPackageSymbol::pools[blockIndex-1])};
                blkptr[SgAdaFormalPackageSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAdaFormalPackageSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAdaFormalPackageSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAdaFormalPackageSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAdaFormalPackageSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAdaFormalPackageSymbol::pool_size - 1};
    SgAdaFormalPackageSymbol* testPointer{reinterpret_cast<SgAdaFormalPackageSymbol*>(SgAdaFormalPackageSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAdaFormalPackageSymbol::pool_size * (SgAdaFormalPackageSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAdaFormalPackageSymbolStroageClass
 * from its counterpart of SgAdaFormalPackageSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAdaFormalPackageSymbol::initializeStorageClassArray(SgAdaFormalPackageSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAdaFormalPackageSymbol::pools.begin(); block != SgAdaFormalPackageSymbol::pools.end(); ++block) {
        SgAdaFormalPackageSymbol* pointer{reinterpret_cast<SgAdaFormalPackageSymbol*>(*block)};
        for (unsigned i = 0; i < SgAdaFormalPackageSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 162143 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNode that takes its corresponding StorageClass as parameter
 */
SgAsmNode::SgAsmNode(const SgAsmNodeStorageClass& storageSource)  : SgNode (storageSource) {

/* #line 162153 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNode*
SgAsmNode::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNode)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNode)};
        unsigned long positionInPool{localIndex % SgAsmNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNode::pool_size};
        returnPointer = &(((SgAsmNode*)(SgAsmNode::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNode*
SgAsmNode::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNode)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNode)};
        unsigned long positionInPool{localIndex % SgAsmNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNode::pool_size};
        returnPointer = &(((SgAsmNode*)(SgAsmNode::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNode::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNode::pools.begin(); block != SgAsmNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNode*>(*block)};
        for (unsigned i = 0; i < SgAsmNode::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNode*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNode::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNode* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNode::pools.begin(); block != SgAsmNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNode*>(*block)};
        for (unsigned i = 0; i < SgAsmNode::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNode::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNode::clearMemoryPool() {
    SgAsmNode* tempPointer{nullptr};
    if (SgAsmNode::pools.empty() == false) {
        auto block{SgAsmNode::pools.begin()};
        SgAsmNode::next_node = reinterpret_cast<SgAsmNode*>(*block);
        for (; block != SgAsmNode::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNode*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNode::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNode::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNode::pool_size-1]);
        }
    }
}

void
SgAsmNode::deleteMemoryPool() {
    for (auto p: SgAsmNode::pools) {
        ROSE_FREE(p);
    }
    SgAsmNode::next_node = nullptr;
    SgAsmNode::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNode::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNode) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNode)};
    for (size_t blockIndex = SgAsmNode::pools.size(); (blockIndex * SgAsmNode::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNode) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNode) = %" PRIuPTR " SgAsmNode::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNode),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNode),SgAsmNode::pool_size);
        }
#endif

        SgAsmNode* pointer{static_cast<SgAsmNode*>(ROSE_MALLOC(SgAsmNode::pool_size * sizeof(SgAsmNode)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNode::pool_size * sizeof(SgAsmNode));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNode::pool_size * sizeof(SgAsmNode));
#endif
        SgAsmNode::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNode::pool_size * sizeof(SgAsmNode), V_SgAsmNode));

        if (SgAsmNode::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNode* blkptr{reinterpret_cast<SgAsmNode*>(SgAsmNode::pools[blockIndex-1])};
                blkptr[SgAsmNode::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNode::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNode::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNode::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNode::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNode::pool_size - 1};
    SgAsmNode* testPointer{reinterpret_cast<SgAsmNode*>(SgAsmNode::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNode::pool_size * (SgAsmNode::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNodeStroageClass
 * from its counterpart of SgAsmNode. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNode::initializeStorageClassArray(SgAsmNodeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNode::pools.begin(); block != SgAsmNode::pools.end(); ++block) {
        SgAsmNode* pointer{reinterpret_cast<SgAsmNode*>(*block)};
        for (unsigned i = 0; i < SgAsmNode::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 162370 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilNode that takes its corresponding StorageClass as parameter
 */
SgAsmCilNode::SgAsmCilNode(const SgAsmCilNodeStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 162380 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilNode*
SgAsmCilNode::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNode)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNode)};
        unsigned long positionInPool{localIndex % SgAsmCilNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilNode::pool_size};
        returnPointer = &(((SgAsmCilNode*)(SgAsmCilNode::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilNode*
SgAsmCilNode::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNode)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilNode)};
        unsigned long positionInPool{localIndex % SgAsmCilNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilNode::pool_size};
        returnPointer = &(((SgAsmCilNode*)(SgAsmCilNode::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilNode::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilNode::pools.begin(); block != SgAsmCilNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilNode*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNode::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilNode*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilNode::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilNode* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilNode::pools.begin(); block != SgAsmCilNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilNode*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNode::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilNode::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilNode::clearMemoryPool() {
    SgAsmCilNode* tempPointer{nullptr};
    if (SgAsmCilNode::pools.empty() == false) {
        auto block{SgAsmCilNode::pools.begin()};
        SgAsmCilNode::next_node = reinterpret_cast<SgAsmCilNode*>(*block);
        for (; block != SgAsmCilNode::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilNode*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilNode::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilNode::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilNode::pool_size-1]);
        }
    }
}

void
SgAsmCilNode::deleteMemoryPool() {
    for (auto p: SgAsmCilNode::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilNode::next_node = nullptr;
    SgAsmCilNode::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilNode::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNode) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNode)};
    for (size_t blockIndex = SgAsmCilNode::pools.size(); (blockIndex * SgAsmCilNode::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNode) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNode) = %" PRIuPTR " SgAsmCilNode::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNode),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNode),SgAsmCilNode::pool_size);
        }
#endif

        SgAsmCilNode* pointer{static_cast<SgAsmCilNode*>(ROSE_MALLOC(SgAsmCilNode::pool_size * sizeof(SgAsmCilNode)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilNode::pool_size * sizeof(SgAsmCilNode));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilNode::pool_size * sizeof(SgAsmCilNode));
#endif
        SgAsmCilNode::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilNode::pool_size * sizeof(SgAsmCilNode), V_SgAsmCilNode));

        if (SgAsmCilNode::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilNode* blkptr{reinterpret_cast<SgAsmCilNode*>(SgAsmCilNode::pools[blockIndex-1])};
                blkptr[SgAsmCilNode::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilNode::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilNode::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilNode::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilNode::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilNode::pool_size - 1};
    SgAsmCilNode* testPointer{reinterpret_cast<SgAsmCilNode*>(SgAsmCilNode::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilNode::pool_size * (SgAsmCilNode::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilNodeStroageClass
 * from its counterpart of SgAsmCilNode. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilNode::initializeStorageClassArray(SgAsmCilNodeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilNode::pools.begin(); block != SgAsmCilNode::pools.end(); ++block) {
        SgAsmCilNode* pointer{reinterpret_cast<SgAsmCilNode*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNode::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 162597 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilDataStream that takes its corresponding StorageClass as parameter
 */
SgAsmCilDataStream::SgAsmCilDataStream(const SgAsmCilDataStreamStorageClass& storageSource)  : SgAsmCilNode (storageSource) {

/* #line 162607 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Offset = storageSource.storageOf_Offset;
    p_Size = storageSource.storageOf_Size;
    p_Name = storageSource.storageOf_Name.rebuildDataStoredInEasyStorageClass();
    p_NamePadding = storageSource.storageOf_NamePadding;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilDataStream*
SgAsmCilDataStream::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilDataStream* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDataStream)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDataStream + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDataStream)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDataStream)};
        unsigned long positionInPool{localIndex % SgAsmCilDataStream::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilDataStream::pool_size};
        returnPointer = &(((SgAsmCilDataStream*)(SgAsmCilDataStream::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilDataStream*
SgAsmCilDataStream::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilDataStream* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDataStream)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDataStream + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDataStream)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilDataStream)};
        unsigned long positionInPool{localIndex % SgAsmCilDataStream::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilDataStream::pool_size};
        returnPointer = &(((SgAsmCilDataStream*)(SgAsmCilDataStream::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilDataStream::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilDataStream::pools.begin(); block != SgAsmCilDataStream::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilDataStream*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDataStream::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilDataStream*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilDataStream::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilDataStream* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilDataStream::pools.begin(); block != SgAsmCilDataStream::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilDataStream*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDataStream::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilDataStream::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilDataStream::clearMemoryPool() {
    SgAsmCilDataStream* tempPointer{nullptr};
    if (SgAsmCilDataStream::pools.empty() == false) {
        auto block{SgAsmCilDataStream::pools.begin()};
        SgAsmCilDataStream::next_node = reinterpret_cast<SgAsmCilDataStream*>(*block);
        for (; block != SgAsmCilDataStream::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilDataStream*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilDataStream::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilDataStream::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilDataStream::pool_size-1]);
        }
    }
}

void
SgAsmCilDataStream::deleteMemoryPool() {
    for (auto p: SgAsmCilDataStream::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilDataStream::next_node = nullptr;
    SgAsmCilDataStream::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilDataStream::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDataStream) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDataStream)};
    for (size_t blockIndex = SgAsmCilDataStream::pools.size(); (blockIndex * SgAsmCilDataStream::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDataStream) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDataStream) = %" PRIuPTR " SgAsmCilDataStream::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDataStream),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDataStream),SgAsmCilDataStream::pool_size);
        }
#endif

        SgAsmCilDataStream* pointer{static_cast<SgAsmCilDataStream*>(ROSE_MALLOC(SgAsmCilDataStream::pool_size * sizeof(SgAsmCilDataStream)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilDataStream::pool_size * sizeof(SgAsmCilDataStream));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilDataStream::pool_size * sizeof(SgAsmCilDataStream));
#endif
        SgAsmCilDataStream::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilDataStream::pool_size * sizeof(SgAsmCilDataStream), V_SgAsmCilDataStream));

        if (SgAsmCilDataStream::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilDataStream* blkptr{reinterpret_cast<SgAsmCilDataStream*>(SgAsmCilDataStream::pools[blockIndex-1])};
                blkptr[SgAsmCilDataStream::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilDataStream::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilDataStream::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilDataStream::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilDataStream::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilDataStream::pool_size - 1};
    SgAsmCilDataStream* testPointer{reinterpret_cast<SgAsmCilDataStream*>(SgAsmCilDataStream::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilDataStream::pool_size * (SgAsmCilDataStream::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilDataStreamStroageClass
 * from its counterpart of SgAsmCilDataStream. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilDataStream::initializeStorageClassArray(SgAsmCilDataStreamStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilDataStream::pools.begin(); block != SgAsmCilDataStream::pools.end(); ++block) {
        SgAsmCilDataStream* pointer{reinterpret_cast<SgAsmCilDataStream*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDataStream::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 162828 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilErrorHeap that takes its corresponding StorageClass as parameter
 */
SgAsmCilErrorHeap::SgAsmCilErrorHeap(const SgAsmCilErrorHeapStorageClass& storageSource)  : SgAsmCilDataStream (storageSource) {

/* #line 162838 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilErrorHeap*
SgAsmCilErrorHeap::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilErrorHeap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilErrorHeap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilErrorHeap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilErrorHeap)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilErrorHeap)};
        unsigned long positionInPool{localIndex % SgAsmCilErrorHeap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilErrorHeap::pool_size};
        returnPointer = &(((SgAsmCilErrorHeap*)(SgAsmCilErrorHeap::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilErrorHeap*
SgAsmCilErrorHeap::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilErrorHeap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilErrorHeap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilErrorHeap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilErrorHeap)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilErrorHeap)};
        unsigned long positionInPool{localIndex % SgAsmCilErrorHeap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilErrorHeap::pool_size};
        returnPointer = &(((SgAsmCilErrorHeap*)(SgAsmCilErrorHeap::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilErrorHeap::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilErrorHeap::pools.begin(); block != SgAsmCilErrorHeap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilErrorHeap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilErrorHeap::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilErrorHeap*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilErrorHeap::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilErrorHeap* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilErrorHeap::pools.begin(); block != SgAsmCilErrorHeap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilErrorHeap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilErrorHeap::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilErrorHeap::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilErrorHeap::clearMemoryPool() {
    SgAsmCilErrorHeap* tempPointer{nullptr};
    if (SgAsmCilErrorHeap::pools.empty() == false) {
        auto block{SgAsmCilErrorHeap::pools.begin()};
        SgAsmCilErrorHeap::next_node = reinterpret_cast<SgAsmCilErrorHeap*>(*block);
        for (; block != SgAsmCilErrorHeap::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilErrorHeap*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilErrorHeap::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilErrorHeap::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilErrorHeap::pool_size-1]);
        }
    }
}

void
SgAsmCilErrorHeap::deleteMemoryPool() {
    for (auto p: SgAsmCilErrorHeap::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilErrorHeap::next_node = nullptr;
    SgAsmCilErrorHeap::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilErrorHeap::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilErrorHeap) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilErrorHeap)};
    for (size_t blockIndex = SgAsmCilErrorHeap::pools.size(); (blockIndex * SgAsmCilErrorHeap::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilErrorHeap) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilErrorHeap) = %" PRIuPTR " SgAsmCilErrorHeap::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilErrorHeap),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilErrorHeap),SgAsmCilErrorHeap::pool_size);
        }
#endif

        SgAsmCilErrorHeap* pointer{static_cast<SgAsmCilErrorHeap*>(ROSE_MALLOC(SgAsmCilErrorHeap::pool_size * sizeof(SgAsmCilErrorHeap)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilErrorHeap::pool_size * sizeof(SgAsmCilErrorHeap));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilErrorHeap::pool_size * sizeof(SgAsmCilErrorHeap));
#endif
        SgAsmCilErrorHeap::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilErrorHeap::pool_size * sizeof(SgAsmCilErrorHeap), V_SgAsmCilErrorHeap));

        if (SgAsmCilErrorHeap::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilErrorHeap* blkptr{reinterpret_cast<SgAsmCilErrorHeap*>(SgAsmCilErrorHeap::pools[blockIndex-1])};
                blkptr[SgAsmCilErrorHeap::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilErrorHeap::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilErrorHeap::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilErrorHeap::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilErrorHeap::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilErrorHeap::pool_size - 1};
    SgAsmCilErrorHeap* testPointer{reinterpret_cast<SgAsmCilErrorHeap*>(SgAsmCilErrorHeap::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilErrorHeap::pool_size * (SgAsmCilErrorHeap::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilErrorHeapStroageClass
 * from its counterpart of SgAsmCilErrorHeap. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilErrorHeap::initializeStorageClassArray(SgAsmCilErrorHeapStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilErrorHeap::pools.begin(); block != SgAsmCilErrorHeap::pools.end(); ++block) {
        SgAsmCilErrorHeap* pointer{reinterpret_cast<SgAsmCilErrorHeap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilErrorHeap::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 163055 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMetadataHeap that takes its corresponding StorageClass as parameter
 */
SgAsmCilMetadataHeap::SgAsmCilMetadataHeap(const SgAsmCilMetadataHeapStorageClass& storageSource)  : SgAsmCilDataStream (storageSource) {

/* #line 163065 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_ReservedAlwaysZero = storageSource.storageOf_ReservedAlwaysZero;
    p_MajorVersion = storageSource.storageOf_MajorVersion;
    p_MinorVersion = storageSource.storageOf_MinorVersion;
    p_HeapSizes = storageSource.storageOf_HeapSizes;
    p_ReservedAlwaysOne = storageSource.storageOf_ReservedAlwaysOne;
    p_Valid = storageSource.storageOf_Valid;
    p_Sorted = storageSource.storageOf_Sorted;
    p_NumberOfRows = storageSource.storageOf_NumberOfRows.rebuildDataStoredInEasyStorageClass();
    p_DataSizeFlags = storageSource.storageOf_DataSizeFlags;
     p_AssemblyTable = (SgAsmCilAssemblyTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_AssemblyTable));
     p_AssemblyOSTable = (SgAsmCilAssemblyOSTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_AssemblyOSTable));
     p_AssemblyProcessorTable = (SgAsmCilAssemblyProcessorTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_AssemblyProcessorTable));
     p_AssemblyRefTable = (SgAsmCilAssemblyRefTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_AssemblyRefTable));
     p_AssemblyRefOSTable = (SgAsmCilAssemblyRefOSTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_AssemblyRefOSTable));
     p_AssemblyRefProcessorTable = (SgAsmCilAssemblyRefProcessorTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_AssemblyRefProcessorTable));
     p_ClassLayoutTable = (SgAsmCilClassLayoutTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ClassLayoutTable));
     p_ConstantTable = (SgAsmCilConstantTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ConstantTable));
     p_CustomAttributeTable = (SgAsmCilCustomAttributeTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_CustomAttributeTable));
     p_DeclSecurityTable = (SgAsmCilDeclSecurityTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_DeclSecurityTable));
     p_EventTable = (SgAsmCilEventTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_EventTable));
     p_EventMapTable = (SgAsmCilEventMapTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_EventMapTable));
     p_ExportedTypeTable = (SgAsmCilExportedTypeTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ExportedTypeTable));
     p_FieldTable = (SgAsmCilFieldTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_FieldTable));
     p_FieldLayoutTable = (SgAsmCilFieldLayoutTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_FieldLayoutTable));
     p_FieldMarshalTable = (SgAsmCilFieldMarshalTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_FieldMarshalTable));
     p_FieldRVATable = (SgAsmCilFieldRVATable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_FieldRVATable));
     p_FileTable = (SgAsmCilFileTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_FileTable));
     p_GenericParamTable = (SgAsmCilGenericParamTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_GenericParamTable));
     p_GenericParamConstraintTable = (SgAsmCilGenericParamConstraintTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_GenericParamConstraintTable));
     p_ImplMapTable = (SgAsmCilImplMapTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ImplMapTable));
     p_InterfaceImplTable = (SgAsmCilInterfaceImplTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_InterfaceImplTable));
     p_ManifestResourceTable = (SgAsmCilManifestResourceTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ManifestResourceTable));
     p_MemberRefTable = (SgAsmCilMemberRefTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_MemberRefTable));
     p_MethodDefTable = (SgAsmCilMethodDefTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_MethodDefTable));
     p_MethodImplTable = (SgAsmCilMethodImplTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_MethodImplTable));
     p_MethodSemanticsTable = (SgAsmCilMethodSemanticsTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_MethodSemanticsTable));
     p_MethodSpecTable = (SgAsmCilMethodSpecTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_MethodSpecTable));
     p_ModuleTable = (SgAsmCilModuleTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ModuleTable));
     p_ModuleRefTable = (SgAsmCilModuleRefTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ModuleRefTable));
     p_NestedClassTable = (SgAsmCilNestedClassTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_NestedClassTable));
     p_ParamTable = (SgAsmCilParamTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ParamTable));
     p_PropertyTable = (SgAsmCilPropertyTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_PropertyTable));
     p_PropertyMapTable = (SgAsmCilPropertyMapTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_PropertyMapTable));
     p_StandAloneSigTable = (SgAsmCilStandAloneSigTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_StandAloneSigTable));
     p_TypeDefTable = (SgAsmCilTypeDefTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_TypeDefTable));
     p_TypeRefTable = (SgAsmCilTypeRefTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_TypeRefTable));
     p_TypeSpecTable = (SgAsmCilTypeSpecTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_TypeSpecTable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMetadataHeap*
SgAsmCilMetadataHeap::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMetadataHeap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadataHeap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadataHeap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadataHeap)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataHeap)};
        unsigned long positionInPool{localIndex % SgAsmCilMetadataHeap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMetadataHeap::pool_size};
        returnPointer = &(((SgAsmCilMetadataHeap*)(SgAsmCilMetadataHeap::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMetadataHeap*
SgAsmCilMetadataHeap::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMetadataHeap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadataHeap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadataHeap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadataHeap)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMetadataHeap)};
        unsigned long positionInPool{localIndex % SgAsmCilMetadataHeap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMetadataHeap::pool_size};
        returnPointer = &(((SgAsmCilMetadataHeap*)(SgAsmCilMetadataHeap::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMetadataHeap::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMetadataHeap::pools.begin(); block != SgAsmCilMetadataHeap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMetadataHeap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadataHeap::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMetadataHeap*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMetadataHeap::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMetadataHeap* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMetadataHeap::pools.begin(); block != SgAsmCilMetadataHeap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMetadataHeap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadataHeap::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMetadataHeap::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMetadataHeap::clearMemoryPool() {
    SgAsmCilMetadataHeap* tempPointer{nullptr};
    if (SgAsmCilMetadataHeap::pools.empty() == false) {
        auto block{SgAsmCilMetadataHeap::pools.begin()};
        SgAsmCilMetadataHeap::next_node = reinterpret_cast<SgAsmCilMetadataHeap*>(*block);
        for (; block != SgAsmCilMetadataHeap::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMetadataHeap*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMetadataHeap::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMetadataHeap::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMetadataHeap::pool_size-1]);
        }
    }
}

void
SgAsmCilMetadataHeap::deleteMemoryPool() {
    for (auto p: SgAsmCilMetadataHeap::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMetadataHeap::next_node = nullptr;
    SgAsmCilMetadataHeap::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMetadataHeap::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataHeap) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadataHeap)};
    for (size_t blockIndex = SgAsmCilMetadataHeap::pools.size(); (blockIndex * SgAsmCilMetadataHeap::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataHeap) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadataHeap) = %" PRIuPTR " SgAsmCilMetadataHeap::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataHeap),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadataHeap),SgAsmCilMetadataHeap::pool_size);
        }
#endif

        SgAsmCilMetadataHeap* pointer{static_cast<SgAsmCilMetadataHeap*>(ROSE_MALLOC(SgAsmCilMetadataHeap::pool_size * sizeof(SgAsmCilMetadataHeap)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMetadataHeap::pool_size * sizeof(SgAsmCilMetadataHeap));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMetadataHeap::pool_size * sizeof(SgAsmCilMetadataHeap));
#endif
        SgAsmCilMetadataHeap::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMetadataHeap::pool_size * sizeof(SgAsmCilMetadataHeap), V_SgAsmCilMetadataHeap));

        if (SgAsmCilMetadataHeap::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMetadataHeap* blkptr{reinterpret_cast<SgAsmCilMetadataHeap*>(SgAsmCilMetadataHeap::pools[blockIndex-1])};
                blkptr[SgAsmCilMetadataHeap::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMetadataHeap::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMetadataHeap::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMetadataHeap::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMetadataHeap::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMetadataHeap::pool_size - 1};
    SgAsmCilMetadataHeap* testPointer{reinterpret_cast<SgAsmCilMetadataHeap*>(SgAsmCilMetadataHeap::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMetadataHeap::pool_size * (SgAsmCilMetadataHeap::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMetadataHeapStroageClass
 * from its counterpart of SgAsmCilMetadataHeap. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMetadataHeap::initializeStorageClassArray(SgAsmCilMetadataHeapStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMetadataHeap::pools.begin(); block != SgAsmCilMetadataHeap::pools.end(); ++block) {
        SgAsmCilMetadataHeap* pointer{reinterpret_cast<SgAsmCilMetadataHeap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadataHeap::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 163329 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilUint32Heap that takes its corresponding StorageClass as parameter
 */
SgAsmCilUint32Heap::SgAsmCilUint32Heap(const SgAsmCilUint32HeapStorageClass& storageSource)  : SgAsmCilDataStream (storageSource) {

/* #line 163339 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Stream = storageSource.storageOf_Stream.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilUint32Heap*
SgAsmCilUint32Heap::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilUint32Heap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilUint32Heap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilUint32Heap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilUint32Heap)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint32Heap)};
        unsigned long positionInPool{localIndex % SgAsmCilUint32Heap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilUint32Heap::pool_size};
        returnPointer = &(((SgAsmCilUint32Heap*)(SgAsmCilUint32Heap::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilUint32Heap*
SgAsmCilUint32Heap::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilUint32Heap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilUint32Heap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilUint32Heap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilUint32Heap)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilUint32Heap)};
        unsigned long positionInPool{localIndex % SgAsmCilUint32Heap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilUint32Heap::pool_size};
        returnPointer = &(((SgAsmCilUint32Heap*)(SgAsmCilUint32Heap::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilUint32Heap::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilUint32Heap::pools.begin(); block != SgAsmCilUint32Heap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilUint32Heap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilUint32Heap::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilUint32Heap*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilUint32Heap::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilUint32Heap* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilUint32Heap::pools.begin(); block != SgAsmCilUint32Heap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilUint32Heap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilUint32Heap::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilUint32Heap::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilUint32Heap::clearMemoryPool() {
    SgAsmCilUint32Heap* tempPointer{nullptr};
    if (SgAsmCilUint32Heap::pools.empty() == false) {
        auto block{SgAsmCilUint32Heap::pools.begin()};
        SgAsmCilUint32Heap::next_node = reinterpret_cast<SgAsmCilUint32Heap*>(*block);
        for (; block != SgAsmCilUint32Heap::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilUint32Heap*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilUint32Heap::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilUint32Heap::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilUint32Heap::pool_size-1]);
        }
    }
}

void
SgAsmCilUint32Heap::deleteMemoryPool() {
    for (auto p: SgAsmCilUint32Heap::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilUint32Heap::next_node = nullptr;
    SgAsmCilUint32Heap::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilUint32Heap::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint32Heap) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilUint32Heap)};
    for (size_t blockIndex = SgAsmCilUint32Heap::pools.size(); (blockIndex * SgAsmCilUint32Heap::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint32Heap) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilUint32Heap) = %" PRIuPTR " SgAsmCilUint32Heap::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint32Heap),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilUint32Heap),SgAsmCilUint32Heap::pool_size);
        }
#endif

        SgAsmCilUint32Heap* pointer{static_cast<SgAsmCilUint32Heap*>(ROSE_MALLOC(SgAsmCilUint32Heap::pool_size * sizeof(SgAsmCilUint32Heap)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilUint32Heap::pool_size * sizeof(SgAsmCilUint32Heap));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilUint32Heap::pool_size * sizeof(SgAsmCilUint32Heap));
#endif
        SgAsmCilUint32Heap::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilUint32Heap::pool_size * sizeof(SgAsmCilUint32Heap), V_SgAsmCilUint32Heap));

        if (SgAsmCilUint32Heap::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilUint32Heap* blkptr{reinterpret_cast<SgAsmCilUint32Heap*>(SgAsmCilUint32Heap::pools[blockIndex-1])};
                blkptr[SgAsmCilUint32Heap::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilUint32Heap::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilUint32Heap::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilUint32Heap::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilUint32Heap::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilUint32Heap::pool_size - 1};
    SgAsmCilUint32Heap* testPointer{reinterpret_cast<SgAsmCilUint32Heap*>(SgAsmCilUint32Heap::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilUint32Heap::pool_size * (SgAsmCilUint32Heap::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilUint32HeapStroageClass
 * from its counterpart of SgAsmCilUint32Heap. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilUint32Heap::initializeStorageClassArray(SgAsmCilUint32HeapStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilUint32Heap::pools.begin(); block != SgAsmCilUint32Heap::pools.end(); ++block) {
        SgAsmCilUint32Heap* pointer{reinterpret_cast<SgAsmCilUint32Heap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilUint32Heap::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 163557 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilUint8Heap that takes its corresponding StorageClass as parameter
 */
SgAsmCilUint8Heap::SgAsmCilUint8Heap(const SgAsmCilUint8HeapStorageClass& storageSource)  : SgAsmCilDataStream (storageSource) {

/* #line 163567 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Stream = storageSource.storageOf_Stream.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilUint8Heap*
SgAsmCilUint8Heap::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilUint8Heap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilUint8Heap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilUint8Heap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilUint8Heap)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint8Heap)};
        unsigned long positionInPool{localIndex % SgAsmCilUint8Heap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilUint8Heap::pool_size};
        returnPointer = &(((SgAsmCilUint8Heap*)(SgAsmCilUint8Heap::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilUint8Heap*
SgAsmCilUint8Heap::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilUint8Heap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilUint8Heap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilUint8Heap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilUint8Heap)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilUint8Heap)};
        unsigned long positionInPool{localIndex % SgAsmCilUint8Heap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilUint8Heap::pool_size};
        returnPointer = &(((SgAsmCilUint8Heap*)(SgAsmCilUint8Heap::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilUint8Heap::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilUint8Heap::pools.begin(); block != SgAsmCilUint8Heap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilUint8Heap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilUint8Heap::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilUint8Heap*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilUint8Heap::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilUint8Heap* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilUint8Heap::pools.begin(); block != SgAsmCilUint8Heap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilUint8Heap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilUint8Heap::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilUint8Heap::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilUint8Heap::clearMemoryPool() {
    SgAsmCilUint8Heap* tempPointer{nullptr};
    if (SgAsmCilUint8Heap::pools.empty() == false) {
        auto block{SgAsmCilUint8Heap::pools.begin()};
        SgAsmCilUint8Heap::next_node = reinterpret_cast<SgAsmCilUint8Heap*>(*block);
        for (; block != SgAsmCilUint8Heap::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilUint8Heap*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilUint8Heap::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilUint8Heap::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilUint8Heap::pool_size-1]);
        }
    }
}

void
SgAsmCilUint8Heap::deleteMemoryPool() {
    for (auto p: SgAsmCilUint8Heap::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilUint8Heap::next_node = nullptr;
    SgAsmCilUint8Heap::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilUint8Heap::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint8Heap) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilUint8Heap)};
    for (size_t blockIndex = SgAsmCilUint8Heap::pools.size(); (blockIndex * SgAsmCilUint8Heap::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint8Heap) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilUint8Heap) = %" PRIuPTR " SgAsmCilUint8Heap::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilUint8Heap),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilUint8Heap),SgAsmCilUint8Heap::pool_size);
        }
#endif

        SgAsmCilUint8Heap* pointer{static_cast<SgAsmCilUint8Heap*>(ROSE_MALLOC(SgAsmCilUint8Heap::pool_size * sizeof(SgAsmCilUint8Heap)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilUint8Heap::pool_size * sizeof(SgAsmCilUint8Heap));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilUint8Heap::pool_size * sizeof(SgAsmCilUint8Heap));
#endif
        SgAsmCilUint8Heap::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilUint8Heap::pool_size * sizeof(SgAsmCilUint8Heap), V_SgAsmCilUint8Heap));

        if (SgAsmCilUint8Heap::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilUint8Heap* blkptr{reinterpret_cast<SgAsmCilUint8Heap*>(SgAsmCilUint8Heap::pools[blockIndex-1])};
                blkptr[SgAsmCilUint8Heap::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilUint8Heap::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilUint8Heap::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilUint8Heap::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilUint8Heap::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilUint8Heap::pool_size - 1};
    SgAsmCilUint8Heap* testPointer{reinterpret_cast<SgAsmCilUint8Heap*>(SgAsmCilUint8Heap::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilUint8Heap::pool_size * (SgAsmCilUint8Heap::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilUint8HeapStroageClass
 * from its counterpart of SgAsmCilUint8Heap. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilUint8Heap::initializeStorageClassArray(SgAsmCilUint8HeapStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilUint8Heap::pools.begin(); block != SgAsmCilUint8Heap::pools.end(); ++block) {
        SgAsmCilUint8Heap* pointer{reinterpret_cast<SgAsmCilUint8Heap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilUint8Heap::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 163785 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilExceptionData that takes its corresponding StorageClass as parameter
 */
SgAsmCilExceptionData::SgAsmCilExceptionData(const SgAsmCilExceptionDataStorageClass& storageSource)  : SgAsmCilNode (storageSource) {

/* #line 163795 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_flags = storageSource.storageOf_flags;
    p_tryOffset = storageSource.storageOf_tryOffset;
    p_tryLength = storageSource.storageOf_tryLength;
    p_handlerOffset = storageSource.storageOf_handlerOffset;
    p_handlerLength = storageSource.storageOf_handlerLength;
    p_classTokenOrFilterOffset = storageSource.storageOf_classTokenOrFilterOffset;
    p_standardConforming = storageSource.storageOf_standardConforming;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilExceptionData*
SgAsmCilExceptionData::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilExceptionData* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExceptionData)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExceptionData + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExceptionData)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExceptionData)};
        unsigned long positionInPool{localIndex % SgAsmCilExceptionData::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilExceptionData::pool_size};
        returnPointer = &(((SgAsmCilExceptionData*)(SgAsmCilExceptionData::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilExceptionData*
SgAsmCilExceptionData::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilExceptionData* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExceptionData)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExceptionData + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExceptionData)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilExceptionData)};
        unsigned long positionInPool{localIndex % SgAsmCilExceptionData::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilExceptionData::pool_size};
        returnPointer = &(((SgAsmCilExceptionData*)(SgAsmCilExceptionData::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilExceptionData::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilExceptionData::pools.begin(); block != SgAsmCilExceptionData::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilExceptionData*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExceptionData::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilExceptionData*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilExceptionData::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilExceptionData* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilExceptionData::pools.begin(); block != SgAsmCilExceptionData::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilExceptionData*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExceptionData::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilExceptionData::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilExceptionData::clearMemoryPool() {
    SgAsmCilExceptionData* tempPointer{nullptr};
    if (SgAsmCilExceptionData::pools.empty() == false) {
        auto block{SgAsmCilExceptionData::pools.begin()};
        SgAsmCilExceptionData::next_node = reinterpret_cast<SgAsmCilExceptionData*>(*block);
        for (; block != SgAsmCilExceptionData::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilExceptionData*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilExceptionData::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilExceptionData::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilExceptionData::pool_size-1]);
        }
    }
}

void
SgAsmCilExceptionData::deleteMemoryPool() {
    for (auto p: SgAsmCilExceptionData::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilExceptionData::next_node = nullptr;
    SgAsmCilExceptionData::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilExceptionData::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExceptionData) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExceptionData)};
    for (size_t blockIndex = SgAsmCilExceptionData::pools.size(); (blockIndex * SgAsmCilExceptionData::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExceptionData) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExceptionData) = %" PRIuPTR " SgAsmCilExceptionData::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExceptionData),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExceptionData),SgAsmCilExceptionData::pool_size);
        }
#endif

        SgAsmCilExceptionData* pointer{static_cast<SgAsmCilExceptionData*>(ROSE_MALLOC(SgAsmCilExceptionData::pool_size * sizeof(SgAsmCilExceptionData)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilExceptionData::pool_size * sizeof(SgAsmCilExceptionData));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilExceptionData::pool_size * sizeof(SgAsmCilExceptionData));
#endif
        SgAsmCilExceptionData::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilExceptionData::pool_size * sizeof(SgAsmCilExceptionData), V_SgAsmCilExceptionData));

        if (SgAsmCilExceptionData::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilExceptionData* blkptr{reinterpret_cast<SgAsmCilExceptionData*>(SgAsmCilExceptionData::pools[blockIndex-1])};
                blkptr[SgAsmCilExceptionData::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilExceptionData::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilExceptionData::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilExceptionData::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilExceptionData::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilExceptionData::pool_size - 1};
    SgAsmCilExceptionData* testPointer{reinterpret_cast<SgAsmCilExceptionData*>(SgAsmCilExceptionData::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilExceptionData::pool_size * (SgAsmCilExceptionData::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilExceptionDataStroageClass
 * from its counterpart of SgAsmCilExceptionData. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilExceptionData::initializeStorageClassArray(SgAsmCilExceptionDataStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilExceptionData::pools.begin(); block != SgAsmCilExceptionData::pools.end(); ++block) {
        SgAsmCilExceptionData* pointer{reinterpret_cast<SgAsmCilExceptionData*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExceptionData::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 164019 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMetadata that takes its corresponding StorageClass as parameter
 */
SgAsmCilMetadata::SgAsmCilMetadata(const SgAsmCilMetadataStorageClass& storageSource)  : SgAsmCilNode (storageSource) {

/* #line 164029 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMetadata*
SgAsmCilMetadata::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMetadata* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadata)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadata + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadata)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadata)};
        unsigned long positionInPool{localIndex % SgAsmCilMetadata::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMetadata::pool_size};
        returnPointer = &(((SgAsmCilMetadata*)(SgAsmCilMetadata::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMetadata*
SgAsmCilMetadata::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMetadata* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadata)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadata + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadata)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMetadata)};
        unsigned long positionInPool{localIndex % SgAsmCilMetadata::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMetadata::pool_size};
        returnPointer = &(((SgAsmCilMetadata*)(SgAsmCilMetadata::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMetadata::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMetadata::pools.begin(); block != SgAsmCilMetadata::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMetadata*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadata::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMetadata*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMetadata::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMetadata* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMetadata::pools.begin(); block != SgAsmCilMetadata::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMetadata*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadata::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMetadata::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMetadata::clearMemoryPool() {
    SgAsmCilMetadata* tempPointer{nullptr};
    if (SgAsmCilMetadata::pools.empty() == false) {
        auto block{SgAsmCilMetadata::pools.begin()};
        SgAsmCilMetadata::next_node = reinterpret_cast<SgAsmCilMetadata*>(*block);
        for (; block != SgAsmCilMetadata::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMetadata*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMetadata::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMetadata::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMetadata::pool_size-1]);
        }
    }
}

void
SgAsmCilMetadata::deleteMemoryPool() {
    for (auto p: SgAsmCilMetadata::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMetadata::next_node = nullptr;
    SgAsmCilMetadata::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMetadata::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadata) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadata)};
    for (size_t blockIndex = SgAsmCilMetadata::pools.size(); (blockIndex * SgAsmCilMetadata::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadata) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadata) = %" PRIuPTR " SgAsmCilMetadata::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadata),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadata),SgAsmCilMetadata::pool_size);
        }
#endif

        SgAsmCilMetadata* pointer{static_cast<SgAsmCilMetadata*>(ROSE_MALLOC(SgAsmCilMetadata::pool_size * sizeof(SgAsmCilMetadata)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMetadata::pool_size * sizeof(SgAsmCilMetadata));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMetadata::pool_size * sizeof(SgAsmCilMetadata));
#endif
        SgAsmCilMetadata::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMetadata::pool_size * sizeof(SgAsmCilMetadata), V_SgAsmCilMetadata));

        if (SgAsmCilMetadata::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMetadata* blkptr{reinterpret_cast<SgAsmCilMetadata*>(SgAsmCilMetadata::pools[blockIndex-1])};
                blkptr[SgAsmCilMetadata::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMetadata::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMetadata::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMetadata::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMetadata::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMetadata::pool_size - 1};
    SgAsmCilMetadata* testPointer{reinterpret_cast<SgAsmCilMetadata*>(SgAsmCilMetadata::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMetadata::pool_size * (SgAsmCilMetadata::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMetadataStroageClass
 * from its counterpart of SgAsmCilMetadata. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMetadata::initializeStorageClassArray(SgAsmCilMetadataStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMetadata::pools.begin(); block != SgAsmCilMetadata::pools.end(); ++block) {
        SgAsmCilMetadata* pointer{reinterpret_cast<SgAsmCilMetadata*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadata::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 164246 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssembly that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssembly::SgAsmCilAssembly(const SgAsmCilAssemblyStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 164256 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_HashAlgId = storageSource.storageOf_HashAlgId;
    p_MajorVersion = storageSource.storageOf_MajorVersion;
    p_MinorVersion = storageSource.storageOf_MinorVersion;
    p_BuildNumber = storageSource.storageOf_BuildNumber;
    p_RevisionNumber = storageSource.storageOf_RevisionNumber;
    p_Flags = storageSource.storageOf_Flags;
    p_PublicKey = storageSource.storageOf_PublicKey;
    p_Name = storageSource.storageOf_Name;
    p_Culture = storageSource.storageOf_Culture;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssembly*
SgAsmCilAssembly::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssembly* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssembly)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssembly + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssembly)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssembly)};
        unsigned long positionInPool{localIndex % SgAsmCilAssembly::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssembly::pool_size};
        returnPointer = &(((SgAsmCilAssembly*)(SgAsmCilAssembly::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssembly*
SgAsmCilAssembly::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssembly* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssembly)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssembly + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssembly)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssembly)};
        unsigned long positionInPool{localIndex % SgAsmCilAssembly::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssembly::pool_size};
        returnPointer = &(((SgAsmCilAssembly*)(SgAsmCilAssembly::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssembly::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssembly::pools.begin(); block != SgAsmCilAssembly::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssembly*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssembly::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssembly*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssembly::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssembly* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssembly::pools.begin(); block != SgAsmCilAssembly::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssembly*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssembly::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssembly::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssembly::clearMemoryPool() {
    SgAsmCilAssembly* tempPointer{nullptr};
    if (SgAsmCilAssembly::pools.empty() == false) {
        auto block{SgAsmCilAssembly::pools.begin()};
        SgAsmCilAssembly::next_node = reinterpret_cast<SgAsmCilAssembly*>(*block);
        for (; block != SgAsmCilAssembly::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssembly*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssembly::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssembly::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssembly::pool_size-1]);
        }
    }
}

void
SgAsmCilAssembly::deleteMemoryPool() {
    for (auto p: SgAsmCilAssembly::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssembly::next_node = nullptr;
    SgAsmCilAssembly::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssembly::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssembly) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssembly)};
    for (size_t blockIndex = SgAsmCilAssembly::pools.size(); (blockIndex * SgAsmCilAssembly::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssembly) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssembly) = %" PRIuPTR " SgAsmCilAssembly::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssembly),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssembly),SgAsmCilAssembly::pool_size);
        }
#endif

        SgAsmCilAssembly* pointer{static_cast<SgAsmCilAssembly*>(ROSE_MALLOC(SgAsmCilAssembly::pool_size * sizeof(SgAsmCilAssembly)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssembly::pool_size * sizeof(SgAsmCilAssembly));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssembly::pool_size * sizeof(SgAsmCilAssembly));
#endif
        SgAsmCilAssembly::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssembly::pool_size * sizeof(SgAsmCilAssembly), V_SgAsmCilAssembly));

        if (SgAsmCilAssembly::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssembly* blkptr{reinterpret_cast<SgAsmCilAssembly*>(SgAsmCilAssembly::pools[blockIndex-1])};
                blkptr[SgAsmCilAssembly::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssembly::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssembly::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssembly::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssembly::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssembly::pool_size - 1};
    SgAsmCilAssembly* testPointer{reinterpret_cast<SgAsmCilAssembly*>(SgAsmCilAssembly::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssembly::pool_size * (SgAsmCilAssembly::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyStroageClass
 * from its counterpart of SgAsmCilAssembly. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssembly::initializeStorageClassArray(SgAsmCilAssemblyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssembly::pools.begin(); block != SgAsmCilAssembly::pools.end(); ++block) {
        SgAsmCilAssembly* pointer{reinterpret_cast<SgAsmCilAssembly*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssembly::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 164482 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyOS that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyOS::SgAsmCilAssemblyOS(const SgAsmCilAssemblyOSStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 164492 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_OSPlatformID = storageSource.storageOf_OSPlatformID;
    p_OSMajorVersion = storageSource.storageOf_OSMajorVersion;
    p_OSMinorVersion = storageSource.storageOf_OSMinorVersion;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyOS*
SgAsmCilAssemblyOS::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyOS* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyOS)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyOS + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyOS)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOS)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyOS::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyOS::pool_size};
        returnPointer = &(((SgAsmCilAssemblyOS*)(SgAsmCilAssemblyOS::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyOS*
SgAsmCilAssemblyOS::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyOS* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyOS)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyOS + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyOS)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyOS)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyOS::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyOS::pool_size};
        returnPointer = &(((SgAsmCilAssemblyOS*)(SgAsmCilAssemblyOS::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyOS::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyOS::pools.begin(); block != SgAsmCilAssemblyOS::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyOS*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyOS::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyOS*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyOS::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyOS* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyOS::pools.begin(); block != SgAsmCilAssemblyOS::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyOS*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyOS::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyOS::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyOS::clearMemoryPool() {
    SgAsmCilAssemblyOS* tempPointer{nullptr};
    if (SgAsmCilAssemblyOS::pools.empty() == false) {
        auto block{SgAsmCilAssemblyOS::pools.begin()};
        SgAsmCilAssemblyOS::next_node = reinterpret_cast<SgAsmCilAssemblyOS*>(*block);
        for (; block != SgAsmCilAssemblyOS::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyOS*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyOS::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyOS::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyOS::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyOS::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyOS::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyOS::next_node = nullptr;
    SgAsmCilAssemblyOS::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyOS::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOS) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyOS)};
    for (size_t blockIndex = SgAsmCilAssemblyOS::pools.size(); (blockIndex * SgAsmCilAssemblyOS::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOS) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyOS) = %" PRIuPTR " SgAsmCilAssemblyOS::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOS),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyOS),SgAsmCilAssemblyOS::pool_size);
        }
#endif

        SgAsmCilAssemblyOS* pointer{static_cast<SgAsmCilAssemblyOS*>(ROSE_MALLOC(SgAsmCilAssemblyOS::pool_size * sizeof(SgAsmCilAssemblyOS)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyOS::pool_size * sizeof(SgAsmCilAssemblyOS));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyOS::pool_size * sizeof(SgAsmCilAssemblyOS));
#endif
        SgAsmCilAssemblyOS::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyOS::pool_size * sizeof(SgAsmCilAssemblyOS), V_SgAsmCilAssemblyOS));

        if (SgAsmCilAssemblyOS::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyOS* blkptr{reinterpret_cast<SgAsmCilAssemblyOS*>(SgAsmCilAssemblyOS::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyOS::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyOS::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyOS::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyOS::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyOS::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyOS::pool_size - 1};
    SgAsmCilAssemblyOS* testPointer{reinterpret_cast<SgAsmCilAssemblyOS*>(SgAsmCilAssemblyOS::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyOS::pool_size * (SgAsmCilAssemblyOS::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyOSStroageClass
 * from its counterpart of SgAsmCilAssemblyOS. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyOS::initializeStorageClassArray(SgAsmCilAssemblyOSStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyOS::pools.begin(); block != SgAsmCilAssemblyOS::pools.end(); ++block) {
        SgAsmCilAssemblyOS* pointer{reinterpret_cast<SgAsmCilAssemblyOS*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyOS::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 164712 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyOSTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyOSTable::SgAsmCilAssemblyOSTable(const SgAsmCilAssemblyOSTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 164722 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyOSTable*
SgAsmCilAssemblyOSTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyOSTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyOSTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyOSTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyOSTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOSTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyOSTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyOSTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyOSTable*)(SgAsmCilAssemblyOSTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyOSTable*
SgAsmCilAssemblyOSTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyOSTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyOSTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyOSTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyOSTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyOSTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyOSTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyOSTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyOSTable*)(SgAsmCilAssemblyOSTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyOSTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyOSTable::pools.begin(); block != SgAsmCilAssemblyOSTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyOSTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyOSTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyOSTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyOSTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyOSTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyOSTable::pools.begin(); block != SgAsmCilAssemblyOSTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyOSTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyOSTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyOSTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyOSTable::clearMemoryPool() {
    SgAsmCilAssemblyOSTable* tempPointer{nullptr};
    if (SgAsmCilAssemblyOSTable::pools.empty() == false) {
        auto block{SgAsmCilAssemblyOSTable::pools.begin()};
        SgAsmCilAssemblyOSTable::next_node = reinterpret_cast<SgAsmCilAssemblyOSTable*>(*block);
        for (; block != SgAsmCilAssemblyOSTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyOSTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyOSTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyOSTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyOSTable::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyOSTable::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyOSTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyOSTable::next_node = nullptr;
    SgAsmCilAssemblyOSTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyOSTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOSTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyOSTable)};
    for (size_t blockIndex = SgAsmCilAssemblyOSTable::pools.size(); (blockIndex * SgAsmCilAssemblyOSTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOSTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyOSTable) = %" PRIuPTR " SgAsmCilAssemblyOSTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyOSTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyOSTable),SgAsmCilAssemblyOSTable::pool_size);
        }
#endif

        SgAsmCilAssemblyOSTable* pointer{static_cast<SgAsmCilAssemblyOSTable*>(ROSE_MALLOC(SgAsmCilAssemblyOSTable::pool_size * sizeof(SgAsmCilAssemblyOSTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyOSTable::pool_size * sizeof(SgAsmCilAssemblyOSTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyOSTable::pool_size * sizeof(SgAsmCilAssemblyOSTable));
#endif
        SgAsmCilAssemblyOSTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyOSTable::pool_size * sizeof(SgAsmCilAssemblyOSTable), V_SgAsmCilAssemblyOSTable));

        if (SgAsmCilAssemblyOSTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyOSTable* blkptr{reinterpret_cast<SgAsmCilAssemblyOSTable*>(SgAsmCilAssemblyOSTable::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyOSTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyOSTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyOSTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyOSTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyOSTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyOSTable::pool_size - 1};
    SgAsmCilAssemblyOSTable* testPointer{reinterpret_cast<SgAsmCilAssemblyOSTable*>(SgAsmCilAssemblyOSTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyOSTable::pool_size * (SgAsmCilAssemblyOSTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyOSTableStroageClass
 * from its counterpart of SgAsmCilAssemblyOSTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyOSTable::initializeStorageClassArray(SgAsmCilAssemblyOSTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyOSTable::pools.begin(); block != SgAsmCilAssemblyOSTable::pools.end(); ++block) {
        SgAsmCilAssemblyOSTable* pointer{reinterpret_cast<SgAsmCilAssemblyOSTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyOSTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 164940 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyProcessor that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyProcessor::SgAsmCilAssemblyProcessor(const SgAsmCilAssemblyProcessorStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 164950 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Processor = storageSource.storageOf_Processor;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyProcessor*
SgAsmCilAssemblyProcessor::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyProcessor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessor)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessor)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyProcessor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyProcessor::pool_size};
        returnPointer = &(((SgAsmCilAssemblyProcessor*)(SgAsmCilAssemblyProcessor::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyProcessor*
SgAsmCilAssemblyProcessor::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyProcessor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyProcessor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyProcessor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyProcessor)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyProcessor)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyProcessor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyProcessor::pool_size};
        returnPointer = &(((SgAsmCilAssemblyProcessor*)(SgAsmCilAssemblyProcessor::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyProcessor::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyProcessor::pools.begin(); block != SgAsmCilAssemblyProcessor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyProcessor*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyProcessor::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyProcessor*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyProcessor::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyProcessor* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyProcessor::pools.begin(); block != SgAsmCilAssemblyProcessor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyProcessor*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyProcessor::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyProcessor::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyProcessor::clearMemoryPool() {
    SgAsmCilAssemblyProcessor* tempPointer{nullptr};
    if (SgAsmCilAssemblyProcessor::pools.empty() == false) {
        auto block{SgAsmCilAssemblyProcessor::pools.begin()};
        SgAsmCilAssemblyProcessor::next_node = reinterpret_cast<SgAsmCilAssemblyProcessor*>(*block);
        for (; block != SgAsmCilAssemblyProcessor::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyProcessor*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyProcessor::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyProcessor::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyProcessor::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyProcessor::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyProcessor::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyProcessor::next_node = nullptr;
    SgAsmCilAssemblyProcessor::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyProcessor::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessor) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessor)};
    for (size_t blockIndex = SgAsmCilAssemblyProcessor::pools.size(); (blockIndex * SgAsmCilAssemblyProcessor::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessor) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessor) = %" PRIuPTR " SgAsmCilAssemblyProcessor::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessor),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessor),SgAsmCilAssemblyProcessor::pool_size);
        }
#endif

        SgAsmCilAssemblyProcessor* pointer{static_cast<SgAsmCilAssemblyProcessor*>(ROSE_MALLOC(SgAsmCilAssemblyProcessor::pool_size * sizeof(SgAsmCilAssemblyProcessor)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyProcessor::pool_size * sizeof(SgAsmCilAssemblyProcessor));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyProcessor::pool_size * sizeof(SgAsmCilAssemblyProcessor));
#endif
        SgAsmCilAssemblyProcessor::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyProcessor::pool_size * sizeof(SgAsmCilAssemblyProcessor), V_SgAsmCilAssemblyProcessor));

        if (SgAsmCilAssemblyProcessor::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyProcessor* blkptr{reinterpret_cast<SgAsmCilAssemblyProcessor*>(SgAsmCilAssemblyProcessor::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyProcessor::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyProcessor::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyProcessor::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyProcessor::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyProcessor::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyProcessor::pool_size - 1};
    SgAsmCilAssemblyProcessor* testPointer{reinterpret_cast<SgAsmCilAssemblyProcessor*>(SgAsmCilAssemblyProcessor::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyProcessor::pool_size * (SgAsmCilAssemblyProcessor::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyProcessorStroageClass
 * from its counterpart of SgAsmCilAssemblyProcessor. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyProcessor::initializeStorageClassArray(SgAsmCilAssemblyProcessorStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyProcessor::pools.begin(); block != SgAsmCilAssemblyProcessor::pools.end(); ++block) {
        SgAsmCilAssemblyProcessor* pointer{reinterpret_cast<SgAsmCilAssemblyProcessor*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyProcessor::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 165168 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyProcessorTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyProcessorTable::SgAsmCilAssemblyProcessorTable(const SgAsmCilAssemblyProcessorTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 165178 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyProcessorTable*
SgAsmCilAssemblyProcessorTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyProcessorTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessorTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessorTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessorTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessorTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyProcessorTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyProcessorTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyProcessorTable*)(SgAsmCilAssemblyProcessorTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyProcessorTable*
SgAsmCilAssemblyProcessorTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyProcessorTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyProcessorTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyProcessorTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyProcessorTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyProcessorTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyProcessorTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyProcessorTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyProcessorTable*)(SgAsmCilAssemblyProcessorTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyProcessorTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyProcessorTable::pools.begin(); block != SgAsmCilAssemblyProcessorTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyProcessorTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyProcessorTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyProcessorTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyProcessorTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyProcessorTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyProcessorTable::pools.begin(); block != SgAsmCilAssemblyProcessorTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyProcessorTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyProcessorTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyProcessorTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyProcessorTable::clearMemoryPool() {
    SgAsmCilAssemblyProcessorTable* tempPointer{nullptr};
    if (SgAsmCilAssemblyProcessorTable::pools.empty() == false) {
        auto block{SgAsmCilAssemblyProcessorTable::pools.begin()};
        SgAsmCilAssemblyProcessorTable::next_node = reinterpret_cast<SgAsmCilAssemblyProcessorTable*>(*block);
        for (; block != SgAsmCilAssemblyProcessorTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyProcessorTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyProcessorTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyProcessorTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyProcessorTable::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyProcessorTable::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyProcessorTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyProcessorTable::next_node = nullptr;
    SgAsmCilAssemblyProcessorTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyProcessorTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessorTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessorTable)};
    for (size_t blockIndex = SgAsmCilAssemblyProcessorTable::pools.size(); (blockIndex * SgAsmCilAssemblyProcessorTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessorTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessorTable) = %" PRIuPTR " SgAsmCilAssemblyProcessorTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyProcessorTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyProcessorTable),SgAsmCilAssemblyProcessorTable::pool_size);
        }
#endif

        SgAsmCilAssemblyProcessorTable* pointer{static_cast<SgAsmCilAssemblyProcessorTable*>(ROSE_MALLOC(SgAsmCilAssemblyProcessorTable::pool_size * sizeof(SgAsmCilAssemblyProcessorTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyProcessorTable::pool_size * sizeof(SgAsmCilAssemblyProcessorTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyProcessorTable::pool_size * sizeof(SgAsmCilAssemblyProcessorTable));
#endif
        SgAsmCilAssemblyProcessorTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyProcessorTable::pool_size * sizeof(SgAsmCilAssemblyProcessorTable), V_SgAsmCilAssemblyProcessorTable));

        if (SgAsmCilAssemblyProcessorTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyProcessorTable* blkptr{reinterpret_cast<SgAsmCilAssemblyProcessorTable*>(SgAsmCilAssemblyProcessorTable::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyProcessorTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyProcessorTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyProcessorTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyProcessorTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyProcessorTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyProcessorTable::pool_size - 1};
    SgAsmCilAssemblyProcessorTable* testPointer{reinterpret_cast<SgAsmCilAssemblyProcessorTable*>(SgAsmCilAssemblyProcessorTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyProcessorTable::pool_size * (SgAsmCilAssemblyProcessorTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyProcessorTableStroageClass
 * from its counterpart of SgAsmCilAssemblyProcessorTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyProcessorTable::initializeStorageClassArray(SgAsmCilAssemblyProcessorTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyProcessorTable::pools.begin(); block != SgAsmCilAssemblyProcessorTable::pools.end(); ++block) {
        SgAsmCilAssemblyProcessorTable* pointer{reinterpret_cast<SgAsmCilAssemblyProcessorTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyProcessorTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 165396 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyRef that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyRef::SgAsmCilAssemblyRef(const SgAsmCilAssemblyRefStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 165406 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_MajorVersion = storageSource.storageOf_MajorVersion;
    p_MinorVersion = storageSource.storageOf_MinorVersion;
    p_BuildNumber = storageSource.storageOf_BuildNumber;
    p_RevisionNumber = storageSource.storageOf_RevisionNumber;
    p_Flags = storageSource.storageOf_Flags;
    p_PublicKeyOrToken = storageSource.storageOf_PublicKeyOrToken;
    p_Name = storageSource.storageOf_Name;
    p_Culture = storageSource.storageOf_Culture;
    p_HashValue = storageSource.storageOf_HashValue;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyRef*
SgAsmCilAssemblyRef::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRef)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRef)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRef::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRef*)(SgAsmCilAssemblyRef::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyRef*
SgAsmCilAssemblyRef::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRef)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyRef)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRef::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRef*)(SgAsmCilAssemblyRef::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyRef::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyRef::pools.begin(); block != SgAsmCilAssemblyRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRef::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyRef*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyRef::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyRef* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyRef::pools.begin(); block != SgAsmCilAssemblyRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRef::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyRef::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyRef::clearMemoryPool() {
    SgAsmCilAssemblyRef* tempPointer{nullptr};
    if (SgAsmCilAssemblyRef::pools.empty() == false) {
        auto block{SgAsmCilAssemblyRef::pools.begin()};
        SgAsmCilAssemblyRef::next_node = reinterpret_cast<SgAsmCilAssemblyRef*>(*block);
        for (; block != SgAsmCilAssemblyRef::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyRef*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyRef::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyRef::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyRef::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyRef::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyRef::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyRef::next_node = nullptr;
    SgAsmCilAssemblyRef::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyRef::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRef) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRef)};
    for (size_t blockIndex = SgAsmCilAssemblyRef::pools.size(); (blockIndex * SgAsmCilAssemblyRef::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRef) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRef) = %" PRIuPTR " SgAsmCilAssemblyRef::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRef),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRef),SgAsmCilAssemblyRef::pool_size);
        }
#endif

        SgAsmCilAssemblyRef* pointer{static_cast<SgAsmCilAssemblyRef*>(ROSE_MALLOC(SgAsmCilAssemblyRef::pool_size * sizeof(SgAsmCilAssemblyRef)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyRef::pool_size * sizeof(SgAsmCilAssemblyRef));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyRef::pool_size * sizeof(SgAsmCilAssemblyRef));
#endif
        SgAsmCilAssemblyRef::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyRef::pool_size * sizeof(SgAsmCilAssemblyRef), V_SgAsmCilAssemblyRef));

        if (SgAsmCilAssemblyRef::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyRef* blkptr{reinterpret_cast<SgAsmCilAssemblyRef*>(SgAsmCilAssemblyRef::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyRef::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyRef::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyRef::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyRef::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyRef::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyRef::pool_size - 1};
    SgAsmCilAssemblyRef* testPointer{reinterpret_cast<SgAsmCilAssemblyRef*>(SgAsmCilAssemblyRef::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyRef::pool_size * (SgAsmCilAssemblyRef::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyRefStroageClass
 * from its counterpart of SgAsmCilAssemblyRef. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyRef::initializeStorageClassArray(SgAsmCilAssemblyRefStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyRef::pools.begin(); block != SgAsmCilAssemblyRef::pools.end(); ++block) {
        SgAsmCilAssemblyRef* pointer{reinterpret_cast<SgAsmCilAssemblyRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRef::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 165632 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyRefOS that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyRefOS::SgAsmCilAssemblyRefOS(const SgAsmCilAssemblyRefOSStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 165642 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_OSPlatformID = storageSource.storageOf_OSPlatformID;
    p_OSMajorVersion = storageSource.storageOf_OSMajorVersion;
    p_OSMinorVersion = storageSource.storageOf_OSMinorVersion;
    p_AssemblyRefOS = storageSource.storageOf_AssemblyRefOS;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyRefOS*
SgAsmCilAssemblyRefOS::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyRefOS* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOS)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOS + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOS)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOS)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefOS::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefOS::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefOS*)(SgAsmCilAssemblyRefOS::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyRefOS*
SgAsmCilAssemblyRefOS::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyRefOS* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefOS)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefOS + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefOS)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyRefOS)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefOS::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefOS::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefOS*)(SgAsmCilAssemblyRefOS::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyRefOS::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyRefOS::pools.begin(); block != SgAsmCilAssemblyRefOS::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefOS*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefOS::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyRefOS*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyRefOS::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyRefOS* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyRefOS::pools.begin(); block != SgAsmCilAssemblyRefOS::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefOS*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefOS::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyRefOS::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyRefOS::clearMemoryPool() {
    SgAsmCilAssemblyRefOS* tempPointer{nullptr};
    if (SgAsmCilAssemblyRefOS::pools.empty() == false) {
        auto block{SgAsmCilAssemblyRefOS::pools.begin()};
        SgAsmCilAssemblyRefOS::next_node = reinterpret_cast<SgAsmCilAssemblyRefOS*>(*block);
        for (; block != SgAsmCilAssemblyRefOS::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefOS*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyRefOS::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyRefOS::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyRefOS::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyRefOS::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyRefOS::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyRefOS::next_node = nullptr;
    SgAsmCilAssemblyRefOS::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyRefOS::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOS) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOS)};
    for (size_t blockIndex = SgAsmCilAssemblyRefOS::pools.size(); (blockIndex * SgAsmCilAssemblyRefOS::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOS) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOS) = %" PRIuPTR " SgAsmCilAssemblyRefOS::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOS),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOS),SgAsmCilAssemblyRefOS::pool_size);
        }
#endif

        SgAsmCilAssemblyRefOS* pointer{static_cast<SgAsmCilAssemblyRefOS*>(ROSE_MALLOC(SgAsmCilAssemblyRefOS::pool_size * sizeof(SgAsmCilAssemblyRefOS)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyRefOS::pool_size * sizeof(SgAsmCilAssemblyRefOS));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyRefOS::pool_size * sizeof(SgAsmCilAssemblyRefOS));
#endif
        SgAsmCilAssemblyRefOS::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyRefOS::pool_size * sizeof(SgAsmCilAssemblyRefOS), V_SgAsmCilAssemblyRefOS));

        if (SgAsmCilAssemblyRefOS::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyRefOS* blkptr{reinterpret_cast<SgAsmCilAssemblyRefOS*>(SgAsmCilAssemblyRefOS::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyRefOS::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyRefOS::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyRefOS::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyRefOS::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyRefOS::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyRefOS::pool_size - 1};
    SgAsmCilAssemblyRefOS* testPointer{reinterpret_cast<SgAsmCilAssemblyRefOS*>(SgAsmCilAssemblyRefOS::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyRefOS::pool_size * (SgAsmCilAssemblyRefOS::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyRefOSStroageClass
 * from its counterpart of SgAsmCilAssemblyRefOS. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyRefOS::initializeStorageClassArray(SgAsmCilAssemblyRefOSStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyRefOS::pools.begin(); block != SgAsmCilAssemblyRefOS::pools.end(); ++block) {
        SgAsmCilAssemblyRefOS* pointer{reinterpret_cast<SgAsmCilAssemblyRefOS*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefOS::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 165863 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyRefOSTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyRefOSTable::SgAsmCilAssemblyRefOSTable(const SgAsmCilAssemblyRefOSTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 165873 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyRefOSTable*
SgAsmCilAssemblyRefOSTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyRefOSTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOSTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOSTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOSTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOSTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefOSTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefOSTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefOSTable*)(SgAsmCilAssemblyRefOSTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyRefOSTable*
SgAsmCilAssemblyRefOSTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyRefOSTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefOSTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefOSTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefOSTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyRefOSTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefOSTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefOSTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefOSTable*)(SgAsmCilAssemblyRefOSTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyRefOSTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyRefOSTable::pools.begin(); block != SgAsmCilAssemblyRefOSTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefOSTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefOSTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyRefOSTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyRefOSTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyRefOSTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyRefOSTable::pools.begin(); block != SgAsmCilAssemblyRefOSTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefOSTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefOSTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyRefOSTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyRefOSTable::clearMemoryPool() {
    SgAsmCilAssemblyRefOSTable* tempPointer{nullptr};
    if (SgAsmCilAssemblyRefOSTable::pools.empty() == false) {
        auto block{SgAsmCilAssemblyRefOSTable::pools.begin()};
        SgAsmCilAssemblyRefOSTable::next_node = reinterpret_cast<SgAsmCilAssemblyRefOSTable*>(*block);
        for (; block != SgAsmCilAssemblyRefOSTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefOSTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyRefOSTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyRefOSTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyRefOSTable::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyRefOSTable::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyRefOSTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyRefOSTable::next_node = nullptr;
    SgAsmCilAssemblyRefOSTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyRefOSTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOSTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOSTable)};
    for (size_t blockIndex = SgAsmCilAssemblyRefOSTable::pools.size(); (blockIndex * SgAsmCilAssemblyRefOSTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOSTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOSTable) = %" PRIuPTR " SgAsmCilAssemblyRefOSTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefOSTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefOSTable),SgAsmCilAssemblyRefOSTable::pool_size);
        }
#endif

        SgAsmCilAssemblyRefOSTable* pointer{static_cast<SgAsmCilAssemblyRefOSTable*>(ROSE_MALLOC(SgAsmCilAssemblyRefOSTable::pool_size * sizeof(SgAsmCilAssemblyRefOSTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyRefOSTable::pool_size * sizeof(SgAsmCilAssemblyRefOSTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyRefOSTable::pool_size * sizeof(SgAsmCilAssemblyRefOSTable));
#endif
        SgAsmCilAssemblyRefOSTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyRefOSTable::pool_size * sizeof(SgAsmCilAssemblyRefOSTable), V_SgAsmCilAssemblyRefOSTable));

        if (SgAsmCilAssemblyRefOSTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyRefOSTable* blkptr{reinterpret_cast<SgAsmCilAssemblyRefOSTable*>(SgAsmCilAssemblyRefOSTable::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyRefOSTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyRefOSTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyRefOSTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyRefOSTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyRefOSTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyRefOSTable::pool_size - 1};
    SgAsmCilAssemblyRefOSTable* testPointer{reinterpret_cast<SgAsmCilAssemblyRefOSTable*>(SgAsmCilAssemblyRefOSTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyRefOSTable::pool_size * (SgAsmCilAssemblyRefOSTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyRefOSTableStroageClass
 * from its counterpart of SgAsmCilAssemblyRefOSTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyRefOSTable::initializeStorageClassArray(SgAsmCilAssemblyRefOSTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyRefOSTable::pools.begin(); block != SgAsmCilAssemblyRefOSTable::pools.end(); ++block) {
        SgAsmCilAssemblyRefOSTable* pointer{reinterpret_cast<SgAsmCilAssemblyRefOSTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefOSTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 166091 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyRefProcessor that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyRefProcessor::SgAsmCilAssemblyRefProcessor(const SgAsmCilAssemblyRefProcessorStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 166101 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Processor = storageSource.storageOf_Processor;
    p_AssemblyRef = storageSource.storageOf_AssemblyRef;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyRefProcessor*
SgAsmCilAssemblyRefProcessor::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyRefProcessor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessor)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessor)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefProcessor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefProcessor::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefProcessor*)(SgAsmCilAssemblyRefProcessor::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyRefProcessor*
SgAsmCilAssemblyRefProcessor::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyRefProcessor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefProcessor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefProcessor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefProcessor)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyRefProcessor)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefProcessor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefProcessor::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefProcessor*)(SgAsmCilAssemblyRefProcessor::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyRefProcessor::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyRefProcessor::pools.begin(); block != SgAsmCilAssemblyRefProcessor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessor*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessor::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyRefProcessor*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyRefProcessor::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyRefProcessor* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyRefProcessor::pools.begin(); block != SgAsmCilAssemblyRefProcessor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessor*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessor::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyRefProcessor::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyRefProcessor::clearMemoryPool() {
    SgAsmCilAssemblyRefProcessor* tempPointer{nullptr};
    if (SgAsmCilAssemblyRefProcessor::pools.empty() == false) {
        auto block{SgAsmCilAssemblyRefProcessor::pools.begin()};
        SgAsmCilAssemblyRefProcessor::next_node = reinterpret_cast<SgAsmCilAssemblyRefProcessor*>(*block);
        for (; block != SgAsmCilAssemblyRefProcessor::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessor*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessor::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyRefProcessor::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyRefProcessor::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyRefProcessor::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyRefProcessor::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyRefProcessor::next_node = nullptr;
    SgAsmCilAssemblyRefProcessor::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyRefProcessor::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessor) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessor)};
    for (size_t blockIndex = SgAsmCilAssemblyRefProcessor::pools.size(); (blockIndex * SgAsmCilAssemblyRefProcessor::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessor) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessor) = %" PRIuPTR " SgAsmCilAssemblyRefProcessor::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessor),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessor),SgAsmCilAssemblyRefProcessor::pool_size);
        }
#endif

        SgAsmCilAssemblyRefProcessor* pointer{static_cast<SgAsmCilAssemblyRefProcessor*>(ROSE_MALLOC(SgAsmCilAssemblyRefProcessor::pool_size * sizeof(SgAsmCilAssemblyRefProcessor)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyRefProcessor::pool_size * sizeof(SgAsmCilAssemblyRefProcessor));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyRefProcessor::pool_size * sizeof(SgAsmCilAssemblyRefProcessor));
#endif
        SgAsmCilAssemblyRefProcessor::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyRefProcessor::pool_size * sizeof(SgAsmCilAssemblyRefProcessor), V_SgAsmCilAssemblyRefProcessor));

        if (SgAsmCilAssemblyRefProcessor::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyRefProcessor* blkptr{reinterpret_cast<SgAsmCilAssemblyRefProcessor*>(SgAsmCilAssemblyRefProcessor::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyRefProcessor::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyRefProcessor::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessor::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyRefProcessor::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyRefProcessor::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyRefProcessor::pool_size - 1};
    SgAsmCilAssemblyRefProcessor* testPointer{reinterpret_cast<SgAsmCilAssemblyRefProcessor*>(SgAsmCilAssemblyRefProcessor::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyRefProcessor::pool_size * (SgAsmCilAssemblyRefProcessor::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyRefProcessorStroageClass
 * from its counterpart of SgAsmCilAssemblyRefProcessor. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyRefProcessor::initializeStorageClassArray(SgAsmCilAssemblyRefProcessorStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyRefProcessor::pools.begin(); block != SgAsmCilAssemblyRefProcessor::pools.end(); ++block) {
        SgAsmCilAssemblyRefProcessor* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessor*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessor::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 166320 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyRefProcessorTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyRefProcessorTable::SgAsmCilAssemblyRefProcessorTable(const SgAsmCilAssemblyRefProcessorTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 166330 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyRefProcessorTable*
SgAsmCilAssemblyRefProcessorTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyRefProcessorTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessorTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessorTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessorTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessorTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefProcessorTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefProcessorTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefProcessorTable*)(SgAsmCilAssemblyRefProcessorTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyRefProcessorTable*
SgAsmCilAssemblyRefProcessorTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyRefProcessorTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefProcessorTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefProcessorTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefProcessorTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyRefProcessorTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefProcessorTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefProcessorTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefProcessorTable*)(SgAsmCilAssemblyRefProcessorTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyRefProcessorTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyRefProcessorTable::pools.begin(); block != SgAsmCilAssemblyRefProcessorTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessorTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessorTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyRefProcessorTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyRefProcessorTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyRefProcessorTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyRefProcessorTable::pools.begin(); block != SgAsmCilAssemblyRefProcessorTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessorTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessorTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyRefProcessorTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyRefProcessorTable::clearMemoryPool() {
    SgAsmCilAssemblyRefProcessorTable* tempPointer{nullptr};
    if (SgAsmCilAssemblyRefProcessorTable::pools.empty() == false) {
        auto block{SgAsmCilAssemblyRefProcessorTable::pools.begin()};
        SgAsmCilAssemblyRefProcessorTable::next_node = reinterpret_cast<SgAsmCilAssemblyRefProcessorTable*>(*block);
        for (; block != SgAsmCilAssemblyRefProcessorTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessorTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessorTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyRefProcessorTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyRefProcessorTable::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyRefProcessorTable::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyRefProcessorTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyRefProcessorTable::next_node = nullptr;
    SgAsmCilAssemblyRefProcessorTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyRefProcessorTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessorTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessorTable)};
    for (size_t blockIndex = SgAsmCilAssemblyRefProcessorTable::pools.size(); (blockIndex * SgAsmCilAssemblyRefProcessorTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessorTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessorTable) = %" PRIuPTR " SgAsmCilAssemblyRefProcessorTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefProcessorTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefProcessorTable),SgAsmCilAssemblyRefProcessorTable::pool_size);
        }
#endif

        SgAsmCilAssemblyRefProcessorTable* pointer{static_cast<SgAsmCilAssemblyRefProcessorTable*>(ROSE_MALLOC(SgAsmCilAssemblyRefProcessorTable::pool_size * sizeof(SgAsmCilAssemblyRefProcessorTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyRefProcessorTable::pool_size * sizeof(SgAsmCilAssemblyRefProcessorTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyRefProcessorTable::pool_size * sizeof(SgAsmCilAssemblyRefProcessorTable));
#endif
        SgAsmCilAssemblyRefProcessorTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyRefProcessorTable::pool_size * sizeof(SgAsmCilAssemblyRefProcessorTable), V_SgAsmCilAssemblyRefProcessorTable));

        if (SgAsmCilAssemblyRefProcessorTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyRefProcessorTable* blkptr{reinterpret_cast<SgAsmCilAssemblyRefProcessorTable*>(SgAsmCilAssemblyRefProcessorTable::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyRefProcessorTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyRefProcessorTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessorTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyRefProcessorTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyRefProcessorTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyRefProcessorTable::pool_size - 1};
    SgAsmCilAssemblyRefProcessorTable* testPointer{reinterpret_cast<SgAsmCilAssemblyRefProcessorTable*>(SgAsmCilAssemblyRefProcessorTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyRefProcessorTable::pool_size * (SgAsmCilAssemblyRefProcessorTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyRefProcessorTableStroageClass
 * from its counterpart of SgAsmCilAssemblyRefProcessorTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyRefProcessorTable::initializeStorageClassArray(SgAsmCilAssemblyRefProcessorTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyRefProcessorTable::pools.begin(); block != SgAsmCilAssemblyRefProcessorTable::pools.end(); ++block) {
        SgAsmCilAssemblyRefProcessorTable* pointer{reinterpret_cast<SgAsmCilAssemblyRefProcessorTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefProcessorTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 166548 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyRefTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyRefTable::SgAsmCilAssemblyRefTable(const SgAsmCilAssemblyRefTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 166558 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyRefTable*
SgAsmCilAssemblyRefTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefTable*)(SgAsmCilAssemblyRefTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyRefTable*
SgAsmCilAssemblyRefTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyRefTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyRefTable*)(SgAsmCilAssemblyRefTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyRefTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyRefTable::pools.begin(); block != SgAsmCilAssemblyRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyRefTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyRefTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyRefTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyRefTable::pools.begin(); block != SgAsmCilAssemblyRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyRefTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyRefTable::clearMemoryPool() {
    SgAsmCilAssemblyRefTable* tempPointer{nullptr};
    if (SgAsmCilAssemblyRefTable::pools.empty() == false) {
        auto block{SgAsmCilAssemblyRefTable::pools.begin()};
        SgAsmCilAssemblyRefTable::next_node = reinterpret_cast<SgAsmCilAssemblyRefTable*>(*block);
        for (; block != SgAsmCilAssemblyRefTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyRefTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyRefTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyRefTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyRefTable::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyRefTable::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyRefTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyRefTable::next_node = nullptr;
    SgAsmCilAssemblyRefTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyRefTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefTable)};
    for (size_t blockIndex = SgAsmCilAssemblyRefTable::pools.size(); (blockIndex * SgAsmCilAssemblyRefTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefTable) = %" PRIuPTR " SgAsmCilAssemblyRefTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyRefTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyRefTable),SgAsmCilAssemblyRefTable::pool_size);
        }
#endif

        SgAsmCilAssemblyRefTable* pointer{static_cast<SgAsmCilAssemblyRefTable*>(ROSE_MALLOC(SgAsmCilAssemblyRefTable::pool_size * sizeof(SgAsmCilAssemblyRefTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyRefTable::pool_size * sizeof(SgAsmCilAssemblyRefTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyRefTable::pool_size * sizeof(SgAsmCilAssemblyRefTable));
#endif
        SgAsmCilAssemblyRefTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyRefTable::pool_size * sizeof(SgAsmCilAssemblyRefTable), V_SgAsmCilAssemblyRefTable));

        if (SgAsmCilAssemblyRefTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyRefTable* blkptr{reinterpret_cast<SgAsmCilAssemblyRefTable*>(SgAsmCilAssemblyRefTable::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyRefTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyRefTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyRefTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyRefTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyRefTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyRefTable::pool_size - 1};
    SgAsmCilAssemblyRefTable* testPointer{reinterpret_cast<SgAsmCilAssemblyRefTable*>(SgAsmCilAssemblyRefTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyRefTable::pool_size * (SgAsmCilAssemblyRefTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyRefTableStroageClass
 * from its counterpart of SgAsmCilAssemblyRefTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyRefTable::initializeStorageClassArray(SgAsmCilAssemblyRefTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyRefTable::pools.begin(); block != SgAsmCilAssemblyRefTable::pools.end(); ++block) {
        SgAsmCilAssemblyRefTable* pointer{reinterpret_cast<SgAsmCilAssemblyRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyRefTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 166776 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilAssemblyTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilAssemblyTable::SgAsmCilAssemblyTable(const SgAsmCilAssemblyTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 166786 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilAssemblyTable*
SgAsmCilAssemblyTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilAssemblyTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilAssemblyTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyTable*)(SgAsmCilAssemblyTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilAssemblyTable*
SgAsmCilAssemblyTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilAssemblyTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilAssemblyTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilAssemblyTable)};
        unsigned long positionInPool{localIndex % SgAsmCilAssemblyTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilAssemblyTable::pool_size};
        returnPointer = &(((SgAsmCilAssemblyTable*)(SgAsmCilAssemblyTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilAssemblyTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilAssemblyTable::pools.begin(); block != SgAsmCilAssemblyTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilAssemblyTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilAssemblyTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilAssemblyTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilAssemblyTable::pools.begin(); block != SgAsmCilAssemblyTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilAssemblyTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilAssemblyTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilAssemblyTable::clearMemoryPool() {
    SgAsmCilAssemblyTable* tempPointer{nullptr};
    if (SgAsmCilAssemblyTable::pools.empty() == false) {
        auto block{SgAsmCilAssemblyTable::pools.begin()};
        SgAsmCilAssemblyTable::next_node = reinterpret_cast<SgAsmCilAssemblyTable*>(*block);
        for (; block != SgAsmCilAssemblyTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilAssemblyTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilAssemblyTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilAssemblyTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilAssemblyTable::pool_size-1]);
        }
    }
}

void
SgAsmCilAssemblyTable::deleteMemoryPool() {
    for (auto p: SgAsmCilAssemblyTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilAssemblyTable::next_node = nullptr;
    SgAsmCilAssemblyTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilAssemblyTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyTable)};
    for (size_t blockIndex = SgAsmCilAssemblyTable::pools.size(); (blockIndex * SgAsmCilAssemblyTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyTable) = %" PRIuPTR " SgAsmCilAssemblyTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilAssemblyTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilAssemblyTable),SgAsmCilAssemblyTable::pool_size);
        }
#endif

        SgAsmCilAssemblyTable* pointer{static_cast<SgAsmCilAssemblyTable*>(ROSE_MALLOC(SgAsmCilAssemblyTable::pool_size * sizeof(SgAsmCilAssemblyTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilAssemblyTable::pool_size * sizeof(SgAsmCilAssemblyTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilAssemblyTable::pool_size * sizeof(SgAsmCilAssemblyTable));
#endif
        SgAsmCilAssemblyTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilAssemblyTable::pool_size * sizeof(SgAsmCilAssemblyTable), V_SgAsmCilAssemblyTable));

        if (SgAsmCilAssemblyTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilAssemblyTable* blkptr{reinterpret_cast<SgAsmCilAssemblyTable*>(SgAsmCilAssemblyTable::pools[blockIndex-1])};
                blkptr[SgAsmCilAssemblyTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilAssemblyTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilAssemblyTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilAssemblyTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilAssemblyTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilAssemblyTable::pool_size - 1};
    SgAsmCilAssemblyTable* testPointer{reinterpret_cast<SgAsmCilAssemblyTable*>(SgAsmCilAssemblyTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilAssemblyTable::pool_size * (SgAsmCilAssemblyTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilAssemblyTableStroageClass
 * from its counterpart of SgAsmCilAssemblyTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilAssemblyTable::initializeStorageClassArray(SgAsmCilAssemblyTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilAssemblyTable::pools.begin(); block != SgAsmCilAssemblyTable::pools.end(); ++block) {
        SgAsmCilAssemblyTable* pointer{reinterpret_cast<SgAsmCilAssemblyTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilAssemblyTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 167004 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilClassLayout that takes its corresponding StorageClass as parameter
 */
SgAsmCilClassLayout::SgAsmCilClassLayout(const SgAsmCilClassLayoutStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 167014 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_PackingSize = storageSource.storageOf_PackingSize;
    p_ClassSize = storageSource.storageOf_ClassSize;
    p_Parent = storageSource.storageOf_Parent;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilClassLayout*
SgAsmCilClassLayout::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilClassLayout* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilClassLayout)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilClassLayout + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilClassLayout)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayout)};
        unsigned long positionInPool{localIndex % SgAsmCilClassLayout::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilClassLayout::pool_size};
        returnPointer = &(((SgAsmCilClassLayout*)(SgAsmCilClassLayout::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilClassLayout*
SgAsmCilClassLayout::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilClassLayout* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilClassLayout)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilClassLayout + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilClassLayout)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilClassLayout)};
        unsigned long positionInPool{localIndex % SgAsmCilClassLayout::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilClassLayout::pool_size};
        returnPointer = &(((SgAsmCilClassLayout*)(SgAsmCilClassLayout::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilClassLayout::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilClassLayout::pools.begin(); block != SgAsmCilClassLayout::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilClassLayout*>(*block)};
        for (unsigned i = 0; i < SgAsmCilClassLayout::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilClassLayout*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilClassLayout::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilClassLayout* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilClassLayout::pools.begin(); block != SgAsmCilClassLayout::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilClassLayout*>(*block)};
        for (unsigned i = 0; i < SgAsmCilClassLayout::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilClassLayout::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilClassLayout::clearMemoryPool() {
    SgAsmCilClassLayout* tempPointer{nullptr};
    if (SgAsmCilClassLayout::pools.empty() == false) {
        auto block{SgAsmCilClassLayout::pools.begin()};
        SgAsmCilClassLayout::next_node = reinterpret_cast<SgAsmCilClassLayout*>(*block);
        for (; block != SgAsmCilClassLayout::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilClassLayout*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilClassLayout::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilClassLayout::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilClassLayout::pool_size-1]);
        }
    }
}

void
SgAsmCilClassLayout::deleteMemoryPool() {
    for (auto p: SgAsmCilClassLayout::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilClassLayout::next_node = nullptr;
    SgAsmCilClassLayout::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilClassLayout::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayout) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilClassLayout)};
    for (size_t blockIndex = SgAsmCilClassLayout::pools.size(); (blockIndex * SgAsmCilClassLayout::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayout) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilClassLayout) = %" PRIuPTR " SgAsmCilClassLayout::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayout),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilClassLayout),SgAsmCilClassLayout::pool_size);
        }
#endif

        SgAsmCilClassLayout* pointer{static_cast<SgAsmCilClassLayout*>(ROSE_MALLOC(SgAsmCilClassLayout::pool_size * sizeof(SgAsmCilClassLayout)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilClassLayout::pool_size * sizeof(SgAsmCilClassLayout));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilClassLayout::pool_size * sizeof(SgAsmCilClassLayout));
#endif
        SgAsmCilClassLayout::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilClassLayout::pool_size * sizeof(SgAsmCilClassLayout), V_SgAsmCilClassLayout));

        if (SgAsmCilClassLayout::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilClassLayout* blkptr{reinterpret_cast<SgAsmCilClassLayout*>(SgAsmCilClassLayout::pools[blockIndex-1])};
                blkptr[SgAsmCilClassLayout::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilClassLayout::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilClassLayout::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilClassLayout::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilClassLayout::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilClassLayout::pool_size - 1};
    SgAsmCilClassLayout* testPointer{reinterpret_cast<SgAsmCilClassLayout*>(SgAsmCilClassLayout::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilClassLayout::pool_size * (SgAsmCilClassLayout::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilClassLayoutStroageClass
 * from its counterpart of SgAsmCilClassLayout. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilClassLayout::initializeStorageClassArray(SgAsmCilClassLayoutStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilClassLayout::pools.begin(); block != SgAsmCilClassLayout::pools.end(); ++block) {
        SgAsmCilClassLayout* pointer{reinterpret_cast<SgAsmCilClassLayout*>(*block)};
        for (unsigned i = 0; i < SgAsmCilClassLayout::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 167234 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilClassLayoutTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilClassLayoutTable::SgAsmCilClassLayoutTable(const SgAsmCilClassLayoutTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 167244 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilClassLayoutTable*
SgAsmCilClassLayoutTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilClassLayoutTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilClassLayoutTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilClassLayoutTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilClassLayoutTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayoutTable)};
        unsigned long positionInPool{localIndex % SgAsmCilClassLayoutTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilClassLayoutTable::pool_size};
        returnPointer = &(((SgAsmCilClassLayoutTable*)(SgAsmCilClassLayoutTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilClassLayoutTable*
SgAsmCilClassLayoutTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilClassLayoutTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilClassLayoutTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilClassLayoutTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilClassLayoutTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilClassLayoutTable)};
        unsigned long positionInPool{localIndex % SgAsmCilClassLayoutTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilClassLayoutTable::pool_size};
        returnPointer = &(((SgAsmCilClassLayoutTable*)(SgAsmCilClassLayoutTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilClassLayoutTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilClassLayoutTable::pools.begin(); block != SgAsmCilClassLayoutTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilClassLayoutTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilClassLayoutTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilClassLayoutTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilClassLayoutTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilClassLayoutTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilClassLayoutTable::pools.begin(); block != SgAsmCilClassLayoutTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilClassLayoutTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilClassLayoutTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilClassLayoutTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilClassLayoutTable::clearMemoryPool() {
    SgAsmCilClassLayoutTable* tempPointer{nullptr};
    if (SgAsmCilClassLayoutTable::pools.empty() == false) {
        auto block{SgAsmCilClassLayoutTable::pools.begin()};
        SgAsmCilClassLayoutTable::next_node = reinterpret_cast<SgAsmCilClassLayoutTable*>(*block);
        for (; block != SgAsmCilClassLayoutTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilClassLayoutTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilClassLayoutTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilClassLayoutTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilClassLayoutTable::pool_size-1]);
        }
    }
}

void
SgAsmCilClassLayoutTable::deleteMemoryPool() {
    for (auto p: SgAsmCilClassLayoutTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilClassLayoutTable::next_node = nullptr;
    SgAsmCilClassLayoutTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilClassLayoutTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayoutTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilClassLayoutTable)};
    for (size_t blockIndex = SgAsmCilClassLayoutTable::pools.size(); (blockIndex * SgAsmCilClassLayoutTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayoutTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilClassLayoutTable) = %" PRIuPTR " SgAsmCilClassLayoutTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilClassLayoutTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilClassLayoutTable),SgAsmCilClassLayoutTable::pool_size);
        }
#endif

        SgAsmCilClassLayoutTable* pointer{static_cast<SgAsmCilClassLayoutTable*>(ROSE_MALLOC(SgAsmCilClassLayoutTable::pool_size * sizeof(SgAsmCilClassLayoutTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilClassLayoutTable::pool_size * sizeof(SgAsmCilClassLayoutTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilClassLayoutTable::pool_size * sizeof(SgAsmCilClassLayoutTable));
#endif
        SgAsmCilClassLayoutTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilClassLayoutTable::pool_size * sizeof(SgAsmCilClassLayoutTable), V_SgAsmCilClassLayoutTable));

        if (SgAsmCilClassLayoutTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilClassLayoutTable* blkptr{reinterpret_cast<SgAsmCilClassLayoutTable*>(SgAsmCilClassLayoutTable::pools[blockIndex-1])};
                blkptr[SgAsmCilClassLayoutTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilClassLayoutTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilClassLayoutTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilClassLayoutTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilClassLayoutTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilClassLayoutTable::pool_size - 1};
    SgAsmCilClassLayoutTable* testPointer{reinterpret_cast<SgAsmCilClassLayoutTable*>(SgAsmCilClassLayoutTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilClassLayoutTable::pool_size * (SgAsmCilClassLayoutTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilClassLayoutTableStroageClass
 * from its counterpart of SgAsmCilClassLayoutTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilClassLayoutTable::initializeStorageClassArray(SgAsmCilClassLayoutTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilClassLayoutTable::pools.begin(); block != SgAsmCilClassLayoutTable::pools.end(); ++block) {
        SgAsmCilClassLayoutTable* pointer{reinterpret_cast<SgAsmCilClassLayoutTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilClassLayoutTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 167462 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilConstant that takes its corresponding StorageClass as parameter
 */
SgAsmCilConstant::SgAsmCilConstant(const SgAsmCilConstantStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 167472 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Type = storageSource.storageOf_Type;
    p_Padding = storageSource.storageOf_Padding;
    p_Parent = storageSource.storageOf_Parent;
    p_Value = storageSource.storageOf_Value;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilConstant*
SgAsmCilConstant::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilConstant* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilConstant)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilConstant + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilConstant)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstant)};
        unsigned long positionInPool{localIndex % SgAsmCilConstant::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilConstant::pool_size};
        returnPointer = &(((SgAsmCilConstant*)(SgAsmCilConstant::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilConstant*
SgAsmCilConstant::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilConstant* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilConstant)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilConstant + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilConstant)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilConstant)};
        unsigned long positionInPool{localIndex % SgAsmCilConstant::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilConstant::pool_size};
        returnPointer = &(((SgAsmCilConstant*)(SgAsmCilConstant::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilConstant::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilConstant::pools.begin(); block != SgAsmCilConstant::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilConstant*>(*block)};
        for (unsigned i = 0; i < SgAsmCilConstant::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilConstant*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilConstant::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilConstant* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilConstant::pools.begin(); block != SgAsmCilConstant::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilConstant*>(*block)};
        for (unsigned i = 0; i < SgAsmCilConstant::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilConstant::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilConstant::clearMemoryPool() {
    SgAsmCilConstant* tempPointer{nullptr};
    if (SgAsmCilConstant::pools.empty() == false) {
        auto block{SgAsmCilConstant::pools.begin()};
        SgAsmCilConstant::next_node = reinterpret_cast<SgAsmCilConstant*>(*block);
        for (; block != SgAsmCilConstant::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilConstant*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilConstant::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilConstant::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilConstant::pool_size-1]);
        }
    }
}

void
SgAsmCilConstant::deleteMemoryPool() {
    for (auto p: SgAsmCilConstant::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilConstant::next_node = nullptr;
    SgAsmCilConstant::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilConstant::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstant) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilConstant)};
    for (size_t blockIndex = SgAsmCilConstant::pools.size(); (blockIndex * SgAsmCilConstant::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstant) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilConstant) = %" PRIuPTR " SgAsmCilConstant::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstant),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilConstant),SgAsmCilConstant::pool_size);
        }
#endif

        SgAsmCilConstant* pointer{static_cast<SgAsmCilConstant*>(ROSE_MALLOC(SgAsmCilConstant::pool_size * sizeof(SgAsmCilConstant)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilConstant::pool_size * sizeof(SgAsmCilConstant));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilConstant::pool_size * sizeof(SgAsmCilConstant));
#endif
        SgAsmCilConstant::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilConstant::pool_size * sizeof(SgAsmCilConstant), V_SgAsmCilConstant));

        if (SgAsmCilConstant::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilConstant* blkptr{reinterpret_cast<SgAsmCilConstant*>(SgAsmCilConstant::pools[blockIndex-1])};
                blkptr[SgAsmCilConstant::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilConstant::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilConstant::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilConstant::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilConstant::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilConstant::pool_size - 1};
    SgAsmCilConstant* testPointer{reinterpret_cast<SgAsmCilConstant*>(SgAsmCilConstant::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilConstant::pool_size * (SgAsmCilConstant::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilConstantStroageClass
 * from its counterpart of SgAsmCilConstant. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilConstant::initializeStorageClassArray(SgAsmCilConstantStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilConstant::pools.begin(); block != SgAsmCilConstant::pools.end(); ++block) {
        SgAsmCilConstant* pointer{reinterpret_cast<SgAsmCilConstant*>(*block)};
        for (unsigned i = 0; i < SgAsmCilConstant::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 167693 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilConstantTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilConstantTable::SgAsmCilConstantTable(const SgAsmCilConstantTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 167703 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilConstantTable*
SgAsmCilConstantTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilConstantTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilConstantTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilConstantTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilConstantTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstantTable)};
        unsigned long positionInPool{localIndex % SgAsmCilConstantTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilConstantTable::pool_size};
        returnPointer = &(((SgAsmCilConstantTable*)(SgAsmCilConstantTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilConstantTable*
SgAsmCilConstantTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilConstantTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilConstantTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilConstantTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilConstantTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilConstantTable)};
        unsigned long positionInPool{localIndex % SgAsmCilConstantTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilConstantTable::pool_size};
        returnPointer = &(((SgAsmCilConstantTable*)(SgAsmCilConstantTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilConstantTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilConstantTable::pools.begin(); block != SgAsmCilConstantTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilConstantTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilConstantTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilConstantTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilConstantTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilConstantTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilConstantTable::pools.begin(); block != SgAsmCilConstantTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilConstantTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilConstantTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilConstantTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilConstantTable::clearMemoryPool() {
    SgAsmCilConstantTable* tempPointer{nullptr};
    if (SgAsmCilConstantTable::pools.empty() == false) {
        auto block{SgAsmCilConstantTable::pools.begin()};
        SgAsmCilConstantTable::next_node = reinterpret_cast<SgAsmCilConstantTable*>(*block);
        for (; block != SgAsmCilConstantTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilConstantTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilConstantTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilConstantTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilConstantTable::pool_size-1]);
        }
    }
}

void
SgAsmCilConstantTable::deleteMemoryPool() {
    for (auto p: SgAsmCilConstantTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilConstantTable::next_node = nullptr;
    SgAsmCilConstantTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilConstantTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstantTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilConstantTable)};
    for (size_t blockIndex = SgAsmCilConstantTable::pools.size(); (blockIndex * SgAsmCilConstantTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstantTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilConstantTable) = %" PRIuPTR " SgAsmCilConstantTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilConstantTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilConstantTable),SgAsmCilConstantTable::pool_size);
        }
#endif

        SgAsmCilConstantTable* pointer{static_cast<SgAsmCilConstantTable*>(ROSE_MALLOC(SgAsmCilConstantTable::pool_size * sizeof(SgAsmCilConstantTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilConstantTable::pool_size * sizeof(SgAsmCilConstantTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilConstantTable::pool_size * sizeof(SgAsmCilConstantTable));
#endif
        SgAsmCilConstantTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilConstantTable::pool_size * sizeof(SgAsmCilConstantTable), V_SgAsmCilConstantTable));

        if (SgAsmCilConstantTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilConstantTable* blkptr{reinterpret_cast<SgAsmCilConstantTable*>(SgAsmCilConstantTable::pools[blockIndex-1])};
                blkptr[SgAsmCilConstantTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilConstantTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilConstantTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilConstantTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilConstantTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilConstantTable::pool_size - 1};
    SgAsmCilConstantTable* testPointer{reinterpret_cast<SgAsmCilConstantTable*>(SgAsmCilConstantTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilConstantTable::pool_size * (SgAsmCilConstantTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilConstantTableStroageClass
 * from its counterpart of SgAsmCilConstantTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilConstantTable::initializeStorageClassArray(SgAsmCilConstantTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilConstantTable::pools.begin(); block != SgAsmCilConstantTable::pools.end(); ++block) {
        SgAsmCilConstantTable* pointer{reinterpret_cast<SgAsmCilConstantTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilConstantTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 167921 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilCustomAttribute that takes its corresponding StorageClass as parameter
 */
SgAsmCilCustomAttribute::SgAsmCilCustomAttribute(const SgAsmCilCustomAttributeStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 167931 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Parent = storageSource.storageOf_Parent;
    p_Type = storageSource.storageOf_Type;
    p_Value = storageSource.storageOf_Value;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilCustomAttribute*
SgAsmCilCustomAttribute::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilCustomAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilCustomAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilCustomAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilCustomAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttribute)};
        unsigned long positionInPool{localIndex % SgAsmCilCustomAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilCustomAttribute::pool_size};
        returnPointer = &(((SgAsmCilCustomAttribute*)(SgAsmCilCustomAttribute::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilCustomAttribute*
SgAsmCilCustomAttribute::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilCustomAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilCustomAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilCustomAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilCustomAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilCustomAttribute)};
        unsigned long positionInPool{localIndex % SgAsmCilCustomAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilCustomAttribute::pool_size};
        returnPointer = &(((SgAsmCilCustomAttribute*)(SgAsmCilCustomAttribute::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilCustomAttribute::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilCustomAttribute::pools.begin(); block != SgAsmCilCustomAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilCustomAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmCilCustomAttribute::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilCustomAttribute*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilCustomAttribute::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilCustomAttribute* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilCustomAttribute::pools.begin(); block != SgAsmCilCustomAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilCustomAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmCilCustomAttribute::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilCustomAttribute::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilCustomAttribute::clearMemoryPool() {
    SgAsmCilCustomAttribute* tempPointer{nullptr};
    if (SgAsmCilCustomAttribute::pools.empty() == false) {
        auto block{SgAsmCilCustomAttribute::pools.begin()};
        SgAsmCilCustomAttribute::next_node = reinterpret_cast<SgAsmCilCustomAttribute*>(*block);
        for (; block != SgAsmCilCustomAttribute::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilCustomAttribute*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilCustomAttribute::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilCustomAttribute::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilCustomAttribute::pool_size-1]);
        }
    }
}

void
SgAsmCilCustomAttribute::deleteMemoryPool() {
    for (auto p: SgAsmCilCustomAttribute::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilCustomAttribute::next_node = nullptr;
    SgAsmCilCustomAttribute::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilCustomAttribute::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttribute) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilCustomAttribute)};
    for (size_t blockIndex = SgAsmCilCustomAttribute::pools.size(); (blockIndex * SgAsmCilCustomAttribute::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttribute) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilCustomAttribute) = %" PRIuPTR " SgAsmCilCustomAttribute::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttribute),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilCustomAttribute),SgAsmCilCustomAttribute::pool_size);
        }
#endif

        SgAsmCilCustomAttribute* pointer{static_cast<SgAsmCilCustomAttribute*>(ROSE_MALLOC(SgAsmCilCustomAttribute::pool_size * sizeof(SgAsmCilCustomAttribute)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilCustomAttribute::pool_size * sizeof(SgAsmCilCustomAttribute));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilCustomAttribute::pool_size * sizeof(SgAsmCilCustomAttribute));
#endif
        SgAsmCilCustomAttribute::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilCustomAttribute::pool_size * sizeof(SgAsmCilCustomAttribute), V_SgAsmCilCustomAttribute));

        if (SgAsmCilCustomAttribute::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilCustomAttribute* blkptr{reinterpret_cast<SgAsmCilCustomAttribute*>(SgAsmCilCustomAttribute::pools[blockIndex-1])};
                blkptr[SgAsmCilCustomAttribute::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilCustomAttribute::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilCustomAttribute::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilCustomAttribute::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilCustomAttribute::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilCustomAttribute::pool_size - 1};
    SgAsmCilCustomAttribute* testPointer{reinterpret_cast<SgAsmCilCustomAttribute*>(SgAsmCilCustomAttribute::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilCustomAttribute::pool_size * (SgAsmCilCustomAttribute::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilCustomAttributeStroageClass
 * from its counterpart of SgAsmCilCustomAttribute. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilCustomAttribute::initializeStorageClassArray(SgAsmCilCustomAttributeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilCustomAttribute::pools.begin(); block != SgAsmCilCustomAttribute::pools.end(); ++block) {
        SgAsmCilCustomAttribute* pointer{reinterpret_cast<SgAsmCilCustomAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmCilCustomAttribute::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 168151 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilCustomAttributeTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilCustomAttributeTable::SgAsmCilCustomAttributeTable(const SgAsmCilCustomAttributeTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 168161 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilCustomAttributeTable*
SgAsmCilCustomAttributeTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilCustomAttributeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilCustomAttributeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilCustomAttributeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilCustomAttributeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttributeTable)};
        unsigned long positionInPool{localIndex % SgAsmCilCustomAttributeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilCustomAttributeTable::pool_size};
        returnPointer = &(((SgAsmCilCustomAttributeTable*)(SgAsmCilCustomAttributeTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilCustomAttributeTable*
SgAsmCilCustomAttributeTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilCustomAttributeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilCustomAttributeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilCustomAttributeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilCustomAttributeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilCustomAttributeTable)};
        unsigned long positionInPool{localIndex % SgAsmCilCustomAttributeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilCustomAttributeTable::pool_size};
        returnPointer = &(((SgAsmCilCustomAttributeTable*)(SgAsmCilCustomAttributeTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilCustomAttributeTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilCustomAttributeTable::pools.begin(); block != SgAsmCilCustomAttributeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilCustomAttributeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilCustomAttributeTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilCustomAttributeTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilCustomAttributeTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilCustomAttributeTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilCustomAttributeTable::pools.begin(); block != SgAsmCilCustomAttributeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilCustomAttributeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilCustomAttributeTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilCustomAttributeTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilCustomAttributeTable::clearMemoryPool() {
    SgAsmCilCustomAttributeTable* tempPointer{nullptr};
    if (SgAsmCilCustomAttributeTable::pools.empty() == false) {
        auto block{SgAsmCilCustomAttributeTable::pools.begin()};
        SgAsmCilCustomAttributeTable::next_node = reinterpret_cast<SgAsmCilCustomAttributeTable*>(*block);
        for (; block != SgAsmCilCustomAttributeTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilCustomAttributeTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilCustomAttributeTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilCustomAttributeTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilCustomAttributeTable::pool_size-1]);
        }
    }
}

void
SgAsmCilCustomAttributeTable::deleteMemoryPool() {
    for (auto p: SgAsmCilCustomAttributeTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilCustomAttributeTable::next_node = nullptr;
    SgAsmCilCustomAttributeTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilCustomAttributeTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttributeTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilCustomAttributeTable)};
    for (size_t blockIndex = SgAsmCilCustomAttributeTable::pools.size(); (blockIndex * SgAsmCilCustomAttributeTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttributeTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilCustomAttributeTable) = %" PRIuPTR " SgAsmCilCustomAttributeTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilCustomAttributeTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilCustomAttributeTable),SgAsmCilCustomAttributeTable::pool_size);
        }
#endif

        SgAsmCilCustomAttributeTable* pointer{static_cast<SgAsmCilCustomAttributeTable*>(ROSE_MALLOC(SgAsmCilCustomAttributeTable::pool_size * sizeof(SgAsmCilCustomAttributeTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilCustomAttributeTable::pool_size * sizeof(SgAsmCilCustomAttributeTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilCustomAttributeTable::pool_size * sizeof(SgAsmCilCustomAttributeTable));
#endif
        SgAsmCilCustomAttributeTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilCustomAttributeTable::pool_size * sizeof(SgAsmCilCustomAttributeTable), V_SgAsmCilCustomAttributeTable));

        if (SgAsmCilCustomAttributeTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilCustomAttributeTable* blkptr{reinterpret_cast<SgAsmCilCustomAttributeTable*>(SgAsmCilCustomAttributeTable::pools[blockIndex-1])};
                blkptr[SgAsmCilCustomAttributeTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilCustomAttributeTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilCustomAttributeTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilCustomAttributeTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilCustomAttributeTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilCustomAttributeTable::pool_size - 1};
    SgAsmCilCustomAttributeTable* testPointer{reinterpret_cast<SgAsmCilCustomAttributeTable*>(SgAsmCilCustomAttributeTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilCustomAttributeTable::pool_size * (SgAsmCilCustomAttributeTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilCustomAttributeTableStroageClass
 * from its counterpart of SgAsmCilCustomAttributeTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilCustomAttributeTable::initializeStorageClassArray(SgAsmCilCustomAttributeTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilCustomAttributeTable::pools.begin(); block != SgAsmCilCustomAttributeTable::pools.end(); ++block) {
        SgAsmCilCustomAttributeTable* pointer{reinterpret_cast<SgAsmCilCustomAttributeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilCustomAttributeTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 168379 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilDeclSecurity that takes its corresponding StorageClass as parameter
 */
SgAsmCilDeclSecurity::SgAsmCilDeclSecurity(const SgAsmCilDeclSecurityStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 168389 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Action = storageSource.storageOf_Action;
    p_Parent = storageSource.storageOf_Parent;
    p_PermissionSet = storageSource.storageOf_PermissionSet;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilDeclSecurity*
SgAsmCilDeclSecurity::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilDeclSecurity* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDeclSecurity)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDeclSecurity + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDeclSecurity)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurity)};
        unsigned long positionInPool{localIndex % SgAsmCilDeclSecurity::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilDeclSecurity::pool_size};
        returnPointer = &(((SgAsmCilDeclSecurity*)(SgAsmCilDeclSecurity::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilDeclSecurity*
SgAsmCilDeclSecurity::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilDeclSecurity* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDeclSecurity)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDeclSecurity + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDeclSecurity)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilDeclSecurity)};
        unsigned long positionInPool{localIndex % SgAsmCilDeclSecurity::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilDeclSecurity::pool_size};
        returnPointer = &(((SgAsmCilDeclSecurity*)(SgAsmCilDeclSecurity::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilDeclSecurity::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilDeclSecurity::pools.begin(); block != SgAsmCilDeclSecurity::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilDeclSecurity*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDeclSecurity::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilDeclSecurity*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilDeclSecurity::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilDeclSecurity* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilDeclSecurity::pools.begin(); block != SgAsmCilDeclSecurity::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilDeclSecurity*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDeclSecurity::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilDeclSecurity::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilDeclSecurity::clearMemoryPool() {
    SgAsmCilDeclSecurity* tempPointer{nullptr};
    if (SgAsmCilDeclSecurity::pools.empty() == false) {
        auto block{SgAsmCilDeclSecurity::pools.begin()};
        SgAsmCilDeclSecurity::next_node = reinterpret_cast<SgAsmCilDeclSecurity*>(*block);
        for (; block != SgAsmCilDeclSecurity::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilDeclSecurity*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilDeclSecurity::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilDeclSecurity::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilDeclSecurity::pool_size-1]);
        }
    }
}

void
SgAsmCilDeclSecurity::deleteMemoryPool() {
    for (auto p: SgAsmCilDeclSecurity::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilDeclSecurity::next_node = nullptr;
    SgAsmCilDeclSecurity::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilDeclSecurity::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurity) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDeclSecurity)};
    for (size_t blockIndex = SgAsmCilDeclSecurity::pools.size(); (blockIndex * SgAsmCilDeclSecurity::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurity) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDeclSecurity) = %" PRIuPTR " SgAsmCilDeclSecurity::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurity),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDeclSecurity),SgAsmCilDeclSecurity::pool_size);
        }
#endif

        SgAsmCilDeclSecurity* pointer{static_cast<SgAsmCilDeclSecurity*>(ROSE_MALLOC(SgAsmCilDeclSecurity::pool_size * sizeof(SgAsmCilDeclSecurity)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilDeclSecurity::pool_size * sizeof(SgAsmCilDeclSecurity));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilDeclSecurity::pool_size * sizeof(SgAsmCilDeclSecurity));
#endif
        SgAsmCilDeclSecurity::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilDeclSecurity::pool_size * sizeof(SgAsmCilDeclSecurity), V_SgAsmCilDeclSecurity));

        if (SgAsmCilDeclSecurity::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilDeclSecurity* blkptr{reinterpret_cast<SgAsmCilDeclSecurity*>(SgAsmCilDeclSecurity::pools[blockIndex-1])};
                blkptr[SgAsmCilDeclSecurity::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilDeclSecurity::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilDeclSecurity::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilDeclSecurity::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilDeclSecurity::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilDeclSecurity::pool_size - 1};
    SgAsmCilDeclSecurity* testPointer{reinterpret_cast<SgAsmCilDeclSecurity*>(SgAsmCilDeclSecurity::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilDeclSecurity::pool_size * (SgAsmCilDeclSecurity::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilDeclSecurityStroageClass
 * from its counterpart of SgAsmCilDeclSecurity. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilDeclSecurity::initializeStorageClassArray(SgAsmCilDeclSecurityStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilDeclSecurity::pools.begin(); block != SgAsmCilDeclSecurity::pools.end(); ++block) {
        SgAsmCilDeclSecurity* pointer{reinterpret_cast<SgAsmCilDeclSecurity*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDeclSecurity::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 168609 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilDeclSecurityTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilDeclSecurityTable::SgAsmCilDeclSecurityTable(const SgAsmCilDeclSecurityTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 168619 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilDeclSecurityTable*
SgAsmCilDeclSecurityTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilDeclSecurityTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDeclSecurityTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDeclSecurityTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilDeclSecurityTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurityTable)};
        unsigned long positionInPool{localIndex % SgAsmCilDeclSecurityTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilDeclSecurityTable::pool_size};
        returnPointer = &(((SgAsmCilDeclSecurityTable*)(SgAsmCilDeclSecurityTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilDeclSecurityTable*
SgAsmCilDeclSecurityTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilDeclSecurityTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDeclSecurityTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDeclSecurityTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilDeclSecurityTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilDeclSecurityTable)};
        unsigned long positionInPool{localIndex % SgAsmCilDeclSecurityTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilDeclSecurityTable::pool_size};
        returnPointer = &(((SgAsmCilDeclSecurityTable*)(SgAsmCilDeclSecurityTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilDeclSecurityTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilDeclSecurityTable::pools.begin(); block != SgAsmCilDeclSecurityTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilDeclSecurityTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDeclSecurityTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilDeclSecurityTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilDeclSecurityTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilDeclSecurityTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilDeclSecurityTable::pools.begin(); block != SgAsmCilDeclSecurityTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilDeclSecurityTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDeclSecurityTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilDeclSecurityTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilDeclSecurityTable::clearMemoryPool() {
    SgAsmCilDeclSecurityTable* tempPointer{nullptr};
    if (SgAsmCilDeclSecurityTable::pools.empty() == false) {
        auto block{SgAsmCilDeclSecurityTable::pools.begin()};
        SgAsmCilDeclSecurityTable::next_node = reinterpret_cast<SgAsmCilDeclSecurityTable*>(*block);
        for (; block != SgAsmCilDeclSecurityTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilDeclSecurityTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilDeclSecurityTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilDeclSecurityTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilDeclSecurityTable::pool_size-1]);
        }
    }
}

void
SgAsmCilDeclSecurityTable::deleteMemoryPool() {
    for (auto p: SgAsmCilDeclSecurityTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilDeclSecurityTable::next_node = nullptr;
    SgAsmCilDeclSecurityTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilDeclSecurityTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurityTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDeclSecurityTable)};
    for (size_t blockIndex = SgAsmCilDeclSecurityTable::pools.size(); (blockIndex * SgAsmCilDeclSecurityTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurityTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDeclSecurityTable) = %" PRIuPTR " SgAsmCilDeclSecurityTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilDeclSecurityTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilDeclSecurityTable),SgAsmCilDeclSecurityTable::pool_size);
        }
#endif

        SgAsmCilDeclSecurityTable* pointer{static_cast<SgAsmCilDeclSecurityTable*>(ROSE_MALLOC(SgAsmCilDeclSecurityTable::pool_size * sizeof(SgAsmCilDeclSecurityTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilDeclSecurityTable::pool_size * sizeof(SgAsmCilDeclSecurityTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilDeclSecurityTable::pool_size * sizeof(SgAsmCilDeclSecurityTable));
#endif
        SgAsmCilDeclSecurityTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilDeclSecurityTable::pool_size * sizeof(SgAsmCilDeclSecurityTable), V_SgAsmCilDeclSecurityTable));

        if (SgAsmCilDeclSecurityTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilDeclSecurityTable* blkptr{reinterpret_cast<SgAsmCilDeclSecurityTable*>(SgAsmCilDeclSecurityTable::pools[blockIndex-1])};
                blkptr[SgAsmCilDeclSecurityTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilDeclSecurityTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilDeclSecurityTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilDeclSecurityTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilDeclSecurityTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilDeclSecurityTable::pool_size - 1};
    SgAsmCilDeclSecurityTable* testPointer{reinterpret_cast<SgAsmCilDeclSecurityTable*>(SgAsmCilDeclSecurityTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilDeclSecurityTable::pool_size * (SgAsmCilDeclSecurityTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilDeclSecurityTableStroageClass
 * from its counterpart of SgAsmCilDeclSecurityTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilDeclSecurityTable::initializeStorageClassArray(SgAsmCilDeclSecurityTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilDeclSecurityTable::pools.begin(); block != SgAsmCilDeclSecurityTable::pools.end(); ++block) {
        SgAsmCilDeclSecurityTable* pointer{reinterpret_cast<SgAsmCilDeclSecurityTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilDeclSecurityTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 168837 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilEvent that takes its corresponding StorageClass as parameter
 */
SgAsmCilEvent::SgAsmCilEvent(const SgAsmCilEventStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 168847 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_EventFlags = storageSource.storageOf_EventFlags;
    p_Name = storageSource.storageOf_Name;
    p_EventType = storageSource.storageOf_EventType;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilEvent*
SgAsmCilEvent::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilEvent* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEvent)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEvent + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEvent)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEvent)};
        unsigned long positionInPool{localIndex % SgAsmCilEvent::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEvent::pool_size};
        returnPointer = &(((SgAsmCilEvent*)(SgAsmCilEvent::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilEvent*
SgAsmCilEvent::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilEvent* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEvent)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEvent + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEvent)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilEvent)};
        unsigned long positionInPool{localIndex % SgAsmCilEvent::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEvent::pool_size};
        returnPointer = &(((SgAsmCilEvent*)(SgAsmCilEvent::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilEvent::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilEvent::pools.begin(); block != SgAsmCilEvent::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEvent*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEvent::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilEvent*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilEvent::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilEvent* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilEvent::pools.begin(); block != SgAsmCilEvent::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEvent*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEvent::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilEvent::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilEvent::clearMemoryPool() {
    SgAsmCilEvent* tempPointer{nullptr};
    if (SgAsmCilEvent::pools.empty() == false) {
        auto block{SgAsmCilEvent::pools.begin()};
        SgAsmCilEvent::next_node = reinterpret_cast<SgAsmCilEvent*>(*block);
        for (; block != SgAsmCilEvent::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilEvent*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilEvent::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilEvent::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilEvent::pool_size-1]);
        }
    }
}

void
SgAsmCilEvent::deleteMemoryPool() {
    for (auto p: SgAsmCilEvent::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilEvent::next_node = nullptr;
    SgAsmCilEvent::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilEvent::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEvent) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEvent)};
    for (size_t blockIndex = SgAsmCilEvent::pools.size(); (blockIndex * SgAsmCilEvent::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEvent) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEvent) = %" PRIuPTR " SgAsmCilEvent::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEvent),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEvent),SgAsmCilEvent::pool_size);
        }
#endif

        SgAsmCilEvent* pointer{static_cast<SgAsmCilEvent*>(ROSE_MALLOC(SgAsmCilEvent::pool_size * sizeof(SgAsmCilEvent)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilEvent::pool_size * sizeof(SgAsmCilEvent));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilEvent::pool_size * sizeof(SgAsmCilEvent));
#endif
        SgAsmCilEvent::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilEvent::pool_size * sizeof(SgAsmCilEvent), V_SgAsmCilEvent));

        if (SgAsmCilEvent::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilEvent* blkptr{reinterpret_cast<SgAsmCilEvent*>(SgAsmCilEvent::pools[blockIndex-1])};
                blkptr[SgAsmCilEvent::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilEvent::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilEvent::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilEvent::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilEvent::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilEvent::pool_size - 1};
    SgAsmCilEvent* testPointer{reinterpret_cast<SgAsmCilEvent*>(SgAsmCilEvent::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilEvent::pool_size * (SgAsmCilEvent::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilEventStroageClass
 * from its counterpart of SgAsmCilEvent. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilEvent::initializeStorageClassArray(SgAsmCilEventStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilEvent::pools.begin(); block != SgAsmCilEvent::pools.end(); ++block) {
        SgAsmCilEvent* pointer{reinterpret_cast<SgAsmCilEvent*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEvent::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 169067 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilEventMap that takes its corresponding StorageClass as parameter
 */
SgAsmCilEventMap::SgAsmCilEventMap(const SgAsmCilEventMapStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 169077 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Parent = storageSource.storageOf_Parent;
    p_EventList = storageSource.storageOf_EventList;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilEventMap*
SgAsmCilEventMap::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilEventMap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventMap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventMap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventMap)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMap)};
        unsigned long positionInPool{localIndex % SgAsmCilEventMap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEventMap::pool_size};
        returnPointer = &(((SgAsmCilEventMap*)(SgAsmCilEventMap::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilEventMap*
SgAsmCilEventMap::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilEventMap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventMap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventMap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventMap)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilEventMap)};
        unsigned long positionInPool{localIndex % SgAsmCilEventMap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEventMap::pool_size};
        returnPointer = &(((SgAsmCilEventMap*)(SgAsmCilEventMap::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilEventMap::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilEventMap::pools.begin(); block != SgAsmCilEventMap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEventMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventMap::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilEventMap*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilEventMap::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilEventMap* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilEventMap::pools.begin(); block != SgAsmCilEventMap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEventMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventMap::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilEventMap::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilEventMap::clearMemoryPool() {
    SgAsmCilEventMap* tempPointer{nullptr};
    if (SgAsmCilEventMap::pools.empty() == false) {
        auto block{SgAsmCilEventMap::pools.begin()};
        SgAsmCilEventMap::next_node = reinterpret_cast<SgAsmCilEventMap*>(*block);
        for (; block != SgAsmCilEventMap::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilEventMap*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilEventMap::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilEventMap::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilEventMap::pool_size-1]);
        }
    }
}

void
SgAsmCilEventMap::deleteMemoryPool() {
    for (auto p: SgAsmCilEventMap::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilEventMap::next_node = nullptr;
    SgAsmCilEventMap::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilEventMap::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMap) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventMap)};
    for (size_t blockIndex = SgAsmCilEventMap::pools.size(); (blockIndex * SgAsmCilEventMap::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMap) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventMap) = %" PRIuPTR " SgAsmCilEventMap::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMap),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventMap),SgAsmCilEventMap::pool_size);
        }
#endif

        SgAsmCilEventMap* pointer{static_cast<SgAsmCilEventMap*>(ROSE_MALLOC(SgAsmCilEventMap::pool_size * sizeof(SgAsmCilEventMap)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilEventMap::pool_size * sizeof(SgAsmCilEventMap));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilEventMap::pool_size * sizeof(SgAsmCilEventMap));
#endif
        SgAsmCilEventMap::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilEventMap::pool_size * sizeof(SgAsmCilEventMap), V_SgAsmCilEventMap));

        if (SgAsmCilEventMap::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilEventMap* blkptr{reinterpret_cast<SgAsmCilEventMap*>(SgAsmCilEventMap::pools[blockIndex-1])};
                blkptr[SgAsmCilEventMap::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilEventMap::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilEventMap::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilEventMap::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilEventMap::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilEventMap::pool_size - 1};
    SgAsmCilEventMap* testPointer{reinterpret_cast<SgAsmCilEventMap*>(SgAsmCilEventMap::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilEventMap::pool_size * (SgAsmCilEventMap::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilEventMapStroageClass
 * from its counterpart of SgAsmCilEventMap. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilEventMap::initializeStorageClassArray(SgAsmCilEventMapStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilEventMap::pools.begin(); block != SgAsmCilEventMap::pools.end(); ++block) {
        SgAsmCilEventMap* pointer{reinterpret_cast<SgAsmCilEventMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventMap::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 169296 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilEventMapTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilEventMapTable::SgAsmCilEventMapTable(const SgAsmCilEventMapTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 169306 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilEventMapTable*
SgAsmCilEventMapTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilEventMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMapTable)};
        unsigned long positionInPool{localIndex % SgAsmCilEventMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEventMapTable::pool_size};
        returnPointer = &(((SgAsmCilEventMapTable*)(SgAsmCilEventMapTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilEventMapTable*
SgAsmCilEventMapTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilEventMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilEventMapTable)};
        unsigned long positionInPool{localIndex % SgAsmCilEventMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEventMapTable::pool_size};
        returnPointer = &(((SgAsmCilEventMapTable*)(SgAsmCilEventMapTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilEventMapTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilEventMapTable::pools.begin(); block != SgAsmCilEventMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEventMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventMapTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilEventMapTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilEventMapTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilEventMapTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilEventMapTable::pools.begin(); block != SgAsmCilEventMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEventMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventMapTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilEventMapTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilEventMapTable::clearMemoryPool() {
    SgAsmCilEventMapTable* tempPointer{nullptr};
    if (SgAsmCilEventMapTable::pools.empty() == false) {
        auto block{SgAsmCilEventMapTable::pools.begin()};
        SgAsmCilEventMapTable::next_node = reinterpret_cast<SgAsmCilEventMapTable*>(*block);
        for (; block != SgAsmCilEventMapTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilEventMapTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilEventMapTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilEventMapTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilEventMapTable::pool_size-1]);
        }
    }
}

void
SgAsmCilEventMapTable::deleteMemoryPool() {
    for (auto p: SgAsmCilEventMapTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilEventMapTable::next_node = nullptr;
    SgAsmCilEventMapTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilEventMapTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMapTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventMapTable)};
    for (size_t blockIndex = SgAsmCilEventMapTable::pools.size(); (blockIndex * SgAsmCilEventMapTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMapTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventMapTable) = %" PRIuPTR " SgAsmCilEventMapTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventMapTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventMapTable),SgAsmCilEventMapTable::pool_size);
        }
#endif

        SgAsmCilEventMapTable* pointer{static_cast<SgAsmCilEventMapTable*>(ROSE_MALLOC(SgAsmCilEventMapTable::pool_size * sizeof(SgAsmCilEventMapTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilEventMapTable::pool_size * sizeof(SgAsmCilEventMapTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilEventMapTable::pool_size * sizeof(SgAsmCilEventMapTable));
#endif
        SgAsmCilEventMapTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilEventMapTable::pool_size * sizeof(SgAsmCilEventMapTable), V_SgAsmCilEventMapTable));

        if (SgAsmCilEventMapTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilEventMapTable* blkptr{reinterpret_cast<SgAsmCilEventMapTable*>(SgAsmCilEventMapTable::pools[blockIndex-1])};
                blkptr[SgAsmCilEventMapTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilEventMapTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilEventMapTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilEventMapTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilEventMapTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilEventMapTable::pool_size - 1};
    SgAsmCilEventMapTable* testPointer{reinterpret_cast<SgAsmCilEventMapTable*>(SgAsmCilEventMapTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilEventMapTable::pool_size * (SgAsmCilEventMapTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilEventMapTableStroageClass
 * from its counterpart of SgAsmCilEventMapTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilEventMapTable::initializeStorageClassArray(SgAsmCilEventMapTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilEventMapTable::pools.begin(); block != SgAsmCilEventMapTable::pools.end(); ++block) {
        SgAsmCilEventMapTable* pointer{reinterpret_cast<SgAsmCilEventMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventMapTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 169524 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilEventTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilEventTable::SgAsmCilEventTable(const SgAsmCilEventTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 169534 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilEventTable*
SgAsmCilEventTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilEventTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilEventTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventTable)};
        unsigned long positionInPool{localIndex % SgAsmCilEventTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEventTable::pool_size};
        returnPointer = &(((SgAsmCilEventTable*)(SgAsmCilEventTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilEventTable*
SgAsmCilEventTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilEventTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilEventTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilEventTable)};
        unsigned long positionInPool{localIndex % SgAsmCilEventTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilEventTable::pool_size};
        returnPointer = &(((SgAsmCilEventTable*)(SgAsmCilEventTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilEventTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilEventTable::pools.begin(); block != SgAsmCilEventTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEventTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilEventTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilEventTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilEventTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilEventTable::pools.begin(); block != SgAsmCilEventTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilEventTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilEventTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilEventTable::clearMemoryPool() {
    SgAsmCilEventTable* tempPointer{nullptr};
    if (SgAsmCilEventTable::pools.empty() == false) {
        auto block{SgAsmCilEventTable::pools.begin()};
        SgAsmCilEventTable::next_node = reinterpret_cast<SgAsmCilEventTable*>(*block);
        for (; block != SgAsmCilEventTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilEventTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilEventTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilEventTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilEventTable::pool_size-1]);
        }
    }
}

void
SgAsmCilEventTable::deleteMemoryPool() {
    for (auto p: SgAsmCilEventTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilEventTable::next_node = nullptr;
    SgAsmCilEventTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilEventTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventTable)};
    for (size_t blockIndex = SgAsmCilEventTable::pools.size(); (blockIndex * SgAsmCilEventTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventTable) = %" PRIuPTR " SgAsmCilEventTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilEventTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilEventTable),SgAsmCilEventTable::pool_size);
        }
#endif

        SgAsmCilEventTable* pointer{static_cast<SgAsmCilEventTable*>(ROSE_MALLOC(SgAsmCilEventTable::pool_size * sizeof(SgAsmCilEventTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilEventTable::pool_size * sizeof(SgAsmCilEventTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilEventTable::pool_size * sizeof(SgAsmCilEventTable));
#endif
        SgAsmCilEventTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilEventTable::pool_size * sizeof(SgAsmCilEventTable), V_SgAsmCilEventTable));

        if (SgAsmCilEventTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilEventTable* blkptr{reinterpret_cast<SgAsmCilEventTable*>(SgAsmCilEventTable::pools[blockIndex-1])};
                blkptr[SgAsmCilEventTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilEventTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilEventTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilEventTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilEventTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilEventTable::pool_size - 1};
    SgAsmCilEventTable* testPointer{reinterpret_cast<SgAsmCilEventTable*>(SgAsmCilEventTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilEventTable::pool_size * (SgAsmCilEventTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilEventTableStroageClass
 * from its counterpart of SgAsmCilEventTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilEventTable::initializeStorageClassArray(SgAsmCilEventTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilEventTable::pools.begin(); block != SgAsmCilEventTable::pools.end(); ++block) {
        SgAsmCilEventTable* pointer{reinterpret_cast<SgAsmCilEventTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilEventTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 169752 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilExportedType that takes its corresponding StorageClass as parameter
 */
SgAsmCilExportedType::SgAsmCilExportedType(const SgAsmCilExportedTypeStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 169762 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_EventFlags = storageSource.storageOf_EventFlags;
    p_TypeDefIdName = storageSource.storageOf_TypeDefIdName;
    p_TypeName = storageSource.storageOf_TypeName;
    p_TypeNamespace = storageSource.storageOf_TypeNamespace;
    p_Implementation = storageSource.storageOf_Implementation;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilExportedType*
SgAsmCilExportedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilExportedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExportedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExportedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExportedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedType)};
        unsigned long positionInPool{localIndex % SgAsmCilExportedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilExportedType::pool_size};
        returnPointer = &(((SgAsmCilExportedType*)(SgAsmCilExportedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilExportedType*
SgAsmCilExportedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilExportedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExportedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExportedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExportedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilExportedType)};
        unsigned long positionInPool{localIndex % SgAsmCilExportedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilExportedType::pool_size};
        returnPointer = &(((SgAsmCilExportedType*)(SgAsmCilExportedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilExportedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilExportedType::pools.begin(); block != SgAsmCilExportedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilExportedType*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExportedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilExportedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilExportedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilExportedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilExportedType::pools.begin(); block != SgAsmCilExportedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilExportedType*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExportedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilExportedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilExportedType::clearMemoryPool() {
    SgAsmCilExportedType* tempPointer{nullptr};
    if (SgAsmCilExportedType::pools.empty() == false) {
        auto block{SgAsmCilExportedType::pools.begin()};
        SgAsmCilExportedType::next_node = reinterpret_cast<SgAsmCilExportedType*>(*block);
        for (; block != SgAsmCilExportedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilExportedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilExportedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilExportedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilExportedType::pool_size-1]);
        }
    }
}

void
SgAsmCilExportedType::deleteMemoryPool() {
    for (auto p: SgAsmCilExportedType::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilExportedType::next_node = nullptr;
    SgAsmCilExportedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilExportedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExportedType)};
    for (size_t blockIndex = SgAsmCilExportedType::pools.size(); (blockIndex * SgAsmCilExportedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExportedType) = %" PRIuPTR " SgAsmCilExportedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExportedType),SgAsmCilExportedType::pool_size);
        }
#endif

        SgAsmCilExportedType* pointer{static_cast<SgAsmCilExportedType*>(ROSE_MALLOC(SgAsmCilExportedType::pool_size * sizeof(SgAsmCilExportedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilExportedType::pool_size * sizeof(SgAsmCilExportedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilExportedType::pool_size * sizeof(SgAsmCilExportedType));
#endif
        SgAsmCilExportedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilExportedType::pool_size * sizeof(SgAsmCilExportedType), V_SgAsmCilExportedType));

        if (SgAsmCilExportedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilExportedType* blkptr{reinterpret_cast<SgAsmCilExportedType*>(SgAsmCilExportedType::pools[blockIndex-1])};
                blkptr[SgAsmCilExportedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilExportedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilExportedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilExportedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilExportedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilExportedType::pool_size - 1};
    SgAsmCilExportedType* testPointer{reinterpret_cast<SgAsmCilExportedType*>(SgAsmCilExportedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilExportedType::pool_size * (SgAsmCilExportedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilExportedTypeStroageClass
 * from its counterpart of SgAsmCilExportedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilExportedType::initializeStorageClassArray(SgAsmCilExportedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilExportedType::pools.begin(); block != SgAsmCilExportedType::pools.end(); ++block) {
        SgAsmCilExportedType* pointer{reinterpret_cast<SgAsmCilExportedType*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExportedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 169984 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilExportedTypeTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilExportedTypeTable::SgAsmCilExportedTypeTable(const SgAsmCilExportedTypeTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 169994 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilExportedTypeTable*
SgAsmCilExportedTypeTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilExportedTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExportedTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExportedTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilExportedTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedTypeTable)};
        unsigned long positionInPool{localIndex % SgAsmCilExportedTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilExportedTypeTable::pool_size};
        returnPointer = &(((SgAsmCilExportedTypeTable*)(SgAsmCilExportedTypeTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilExportedTypeTable*
SgAsmCilExportedTypeTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilExportedTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExportedTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExportedTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilExportedTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilExportedTypeTable)};
        unsigned long positionInPool{localIndex % SgAsmCilExportedTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilExportedTypeTable::pool_size};
        returnPointer = &(((SgAsmCilExportedTypeTable*)(SgAsmCilExportedTypeTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilExportedTypeTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilExportedTypeTable::pools.begin(); block != SgAsmCilExportedTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilExportedTypeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExportedTypeTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilExportedTypeTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilExportedTypeTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilExportedTypeTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilExportedTypeTable::pools.begin(); block != SgAsmCilExportedTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilExportedTypeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExportedTypeTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilExportedTypeTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilExportedTypeTable::clearMemoryPool() {
    SgAsmCilExportedTypeTable* tempPointer{nullptr};
    if (SgAsmCilExportedTypeTable::pools.empty() == false) {
        auto block{SgAsmCilExportedTypeTable::pools.begin()};
        SgAsmCilExportedTypeTable::next_node = reinterpret_cast<SgAsmCilExportedTypeTable*>(*block);
        for (; block != SgAsmCilExportedTypeTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilExportedTypeTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilExportedTypeTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilExportedTypeTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilExportedTypeTable::pool_size-1]);
        }
    }
}

void
SgAsmCilExportedTypeTable::deleteMemoryPool() {
    for (auto p: SgAsmCilExportedTypeTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilExportedTypeTable::next_node = nullptr;
    SgAsmCilExportedTypeTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilExportedTypeTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedTypeTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExportedTypeTable)};
    for (size_t blockIndex = SgAsmCilExportedTypeTable::pools.size(); (blockIndex * SgAsmCilExportedTypeTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedTypeTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExportedTypeTable) = %" PRIuPTR " SgAsmCilExportedTypeTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilExportedTypeTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilExportedTypeTable),SgAsmCilExportedTypeTable::pool_size);
        }
#endif

        SgAsmCilExportedTypeTable* pointer{static_cast<SgAsmCilExportedTypeTable*>(ROSE_MALLOC(SgAsmCilExportedTypeTable::pool_size * sizeof(SgAsmCilExportedTypeTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilExportedTypeTable::pool_size * sizeof(SgAsmCilExportedTypeTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilExportedTypeTable::pool_size * sizeof(SgAsmCilExportedTypeTable));
#endif
        SgAsmCilExportedTypeTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilExportedTypeTable::pool_size * sizeof(SgAsmCilExportedTypeTable), V_SgAsmCilExportedTypeTable));

        if (SgAsmCilExportedTypeTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilExportedTypeTable* blkptr{reinterpret_cast<SgAsmCilExportedTypeTable*>(SgAsmCilExportedTypeTable::pools[blockIndex-1])};
                blkptr[SgAsmCilExportedTypeTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilExportedTypeTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilExportedTypeTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilExportedTypeTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilExportedTypeTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilExportedTypeTable::pool_size - 1};
    SgAsmCilExportedTypeTable* testPointer{reinterpret_cast<SgAsmCilExportedTypeTable*>(SgAsmCilExportedTypeTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilExportedTypeTable::pool_size * (SgAsmCilExportedTypeTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilExportedTypeTableStroageClass
 * from its counterpart of SgAsmCilExportedTypeTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilExportedTypeTable::initializeStorageClassArray(SgAsmCilExportedTypeTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilExportedTypeTable::pools.begin(); block != SgAsmCilExportedTypeTable::pools.end(); ++block) {
        SgAsmCilExportedTypeTable* pointer{reinterpret_cast<SgAsmCilExportedTypeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilExportedTypeTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 170212 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilField that takes its corresponding StorageClass as parameter
 */
SgAsmCilField::SgAsmCilField(const SgAsmCilFieldStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 170222 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Flags = storageSource.storageOf_Flags;
    p_Name = storageSource.storageOf_Name;
    p_Signature = storageSource.storageOf_Signature;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilField*
SgAsmCilField::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilField* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilField)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilField + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilField)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilField)};
        unsigned long positionInPool{localIndex % SgAsmCilField::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilField::pool_size};
        returnPointer = &(((SgAsmCilField*)(SgAsmCilField::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilField*
SgAsmCilField::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilField* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilField)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilField + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilField)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilField)};
        unsigned long positionInPool{localIndex % SgAsmCilField::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilField::pool_size};
        returnPointer = &(((SgAsmCilField*)(SgAsmCilField::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilField::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilField::pools.begin(); block != SgAsmCilField::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilField*>(*block)};
        for (unsigned i = 0; i < SgAsmCilField::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilField*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilField::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilField* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilField::pools.begin(); block != SgAsmCilField::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilField*>(*block)};
        for (unsigned i = 0; i < SgAsmCilField::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilField::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilField::clearMemoryPool() {
    SgAsmCilField* tempPointer{nullptr};
    if (SgAsmCilField::pools.empty() == false) {
        auto block{SgAsmCilField::pools.begin()};
        SgAsmCilField::next_node = reinterpret_cast<SgAsmCilField*>(*block);
        for (; block != SgAsmCilField::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilField*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilField::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilField::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilField::pool_size-1]);
        }
    }
}

void
SgAsmCilField::deleteMemoryPool() {
    for (auto p: SgAsmCilField::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilField::next_node = nullptr;
    SgAsmCilField::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilField::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilField) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilField)};
    for (size_t blockIndex = SgAsmCilField::pools.size(); (blockIndex * SgAsmCilField::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilField) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilField) = %" PRIuPTR " SgAsmCilField::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilField),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilField),SgAsmCilField::pool_size);
        }
#endif

        SgAsmCilField* pointer{static_cast<SgAsmCilField*>(ROSE_MALLOC(SgAsmCilField::pool_size * sizeof(SgAsmCilField)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilField::pool_size * sizeof(SgAsmCilField));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilField::pool_size * sizeof(SgAsmCilField));
#endif
        SgAsmCilField::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilField::pool_size * sizeof(SgAsmCilField), V_SgAsmCilField));

        if (SgAsmCilField::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilField* blkptr{reinterpret_cast<SgAsmCilField*>(SgAsmCilField::pools[blockIndex-1])};
                blkptr[SgAsmCilField::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilField::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilField::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilField::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilField::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilField::pool_size - 1};
    SgAsmCilField* testPointer{reinterpret_cast<SgAsmCilField*>(SgAsmCilField::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilField::pool_size * (SgAsmCilField::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldStroageClass
 * from its counterpart of SgAsmCilField. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilField::initializeStorageClassArray(SgAsmCilFieldStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilField::pools.begin(); block != SgAsmCilField::pools.end(); ++block) {
        SgAsmCilField* pointer{reinterpret_cast<SgAsmCilField*>(*block)};
        for (unsigned i = 0; i < SgAsmCilField::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 170442 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFieldLayout that takes its corresponding StorageClass as parameter
 */
SgAsmCilFieldLayout::SgAsmCilFieldLayout(const SgAsmCilFieldLayoutStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 170452 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Offset = storageSource.storageOf_Offset;
    p_Field = storageSource.storageOf_Field;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFieldLayout*
SgAsmCilFieldLayout::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFieldLayout* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldLayout)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldLayout + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldLayout)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayout)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldLayout::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldLayout::pool_size};
        returnPointer = &(((SgAsmCilFieldLayout*)(SgAsmCilFieldLayout::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFieldLayout*
SgAsmCilFieldLayout::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFieldLayout* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldLayout)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldLayout + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldLayout)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFieldLayout)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldLayout::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldLayout::pool_size};
        returnPointer = &(((SgAsmCilFieldLayout*)(SgAsmCilFieldLayout::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFieldLayout::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFieldLayout::pools.begin(); block != SgAsmCilFieldLayout::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldLayout*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldLayout::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFieldLayout*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFieldLayout::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFieldLayout* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFieldLayout::pools.begin(); block != SgAsmCilFieldLayout::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldLayout*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldLayout::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFieldLayout::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFieldLayout::clearMemoryPool() {
    SgAsmCilFieldLayout* tempPointer{nullptr};
    if (SgAsmCilFieldLayout::pools.empty() == false) {
        auto block{SgAsmCilFieldLayout::pools.begin()};
        SgAsmCilFieldLayout::next_node = reinterpret_cast<SgAsmCilFieldLayout*>(*block);
        for (; block != SgAsmCilFieldLayout::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFieldLayout*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFieldLayout::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFieldLayout::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFieldLayout::pool_size-1]);
        }
    }
}

void
SgAsmCilFieldLayout::deleteMemoryPool() {
    for (auto p: SgAsmCilFieldLayout::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFieldLayout::next_node = nullptr;
    SgAsmCilFieldLayout::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFieldLayout::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayout) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldLayout)};
    for (size_t blockIndex = SgAsmCilFieldLayout::pools.size(); (blockIndex * SgAsmCilFieldLayout::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayout) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldLayout) = %" PRIuPTR " SgAsmCilFieldLayout::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayout),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldLayout),SgAsmCilFieldLayout::pool_size);
        }
#endif

        SgAsmCilFieldLayout* pointer{static_cast<SgAsmCilFieldLayout*>(ROSE_MALLOC(SgAsmCilFieldLayout::pool_size * sizeof(SgAsmCilFieldLayout)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFieldLayout::pool_size * sizeof(SgAsmCilFieldLayout));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFieldLayout::pool_size * sizeof(SgAsmCilFieldLayout));
#endif
        SgAsmCilFieldLayout::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFieldLayout::pool_size * sizeof(SgAsmCilFieldLayout), V_SgAsmCilFieldLayout));

        if (SgAsmCilFieldLayout::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFieldLayout* blkptr{reinterpret_cast<SgAsmCilFieldLayout*>(SgAsmCilFieldLayout::pools[blockIndex-1])};
                blkptr[SgAsmCilFieldLayout::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFieldLayout::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFieldLayout::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFieldLayout::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFieldLayout::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFieldLayout::pool_size - 1};
    SgAsmCilFieldLayout* testPointer{reinterpret_cast<SgAsmCilFieldLayout*>(SgAsmCilFieldLayout::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFieldLayout::pool_size * (SgAsmCilFieldLayout::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldLayoutStroageClass
 * from its counterpart of SgAsmCilFieldLayout. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFieldLayout::initializeStorageClassArray(SgAsmCilFieldLayoutStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFieldLayout::pools.begin(); block != SgAsmCilFieldLayout::pools.end(); ++block) {
        SgAsmCilFieldLayout* pointer{reinterpret_cast<SgAsmCilFieldLayout*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldLayout::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 170671 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFieldLayoutTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilFieldLayoutTable::SgAsmCilFieldLayoutTable(const SgAsmCilFieldLayoutTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 170681 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFieldLayoutTable*
SgAsmCilFieldLayoutTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFieldLayoutTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldLayoutTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldLayoutTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldLayoutTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayoutTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldLayoutTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldLayoutTable::pool_size};
        returnPointer = &(((SgAsmCilFieldLayoutTable*)(SgAsmCilFieldLayoutTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFieldLayoutTable*
SgAsmCilFieldLayoutTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFieldLayoutTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldLayoutTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldLayoutTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldLayoutTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFieldLayoutTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldLayoutTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldLayoutTable::pool_size};
        returnPointer = &(((SgAsmCilFieldLayoutTable*)(SgAsmCilFieldLayoutTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFieldLayoutTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFieldLayoutTable::pools.begin(); block != SgAsmCilFieldLayoutTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldLayoutTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldLayoutTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFieldLayoutTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFieldLayoutTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFieldLayoutTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFieldLayoutTable::pools.begin(); block != SgAsmCilFieldLayoutTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldLayoutTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldLayoutTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFieldLayoutTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFieldLayoutTable::clearMemoryPool() {
    SgAsmCilFieldLayoutTable* tempPointer{nullptr};
    if (SgAsmCilFieldLayoutTable::pools.empty() == false) {
        auto block{SgAsmCilFieldLayoutTable::pools.begin()};
        SgAsmCilFieldLayoutTable::next_node = reinterpret_cast<SgAsmCilFieldLayoutTable*>(*block);
        for (; block != SgAsmCilFieldLayoutTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFieldLayoutTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFieldLayoutTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFieldLayoutTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFieldLayoutTable::pool_size-1]);
        }
    }
}

void
SgAsmCilFieldLayoutTable::deleteMemoryPool() {
    for (auto p: SgAsmCilFieldLayoutTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFieldLayoutTable::next_node = nullptr;
    SgAsmCilFieldLayoutTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFieldLayoutTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayoutTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldLayoutTable)};
    for (size_t blockIndex = SgAsmCilFieldLayoutTable::pools.size(); (blockIndex * SgAsmCilFieldLayoutTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayoutTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldLayoutTable) = %" PRIuPTR " SgAsmCilFieldLayoutTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldLayoutTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldLayoutTable),SgAsmCilFieldLayoutTable::pool_size);
        }
#endif

        SgAsmCilFieldLayoutTable* pointer{static_cast<SgAsmCilFieldLayoutTable*>(ROSE_MALLOC(SgAsmCilFieldLayoutTable::pool_size * sizeof(SgAsmCilFieldLayoutTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFieldLayoutTable::pool_size * sizeof(SgAsmCilFieldLayoutTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFieldLayoutTable::pool_size * sizeof(SgAsmCilFieldLayoutTable));
#endif
        SgAsmCilFieldLayoutTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFieldLayoutTable::pool_size * sizeof(SgAsmCilFieldLayoutTable), V_SgAsmCilFieldLayoutTable));

        if (SgAsmCilFieldLayoutTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFieldLayoutTable* blkptr{reinterpret_cast<SgAsmCilFieldLayoutTable*>(SgAsmCilFieldLayoutTable::pools[blockIndex-1])};
                blkptr[SgAsmCilFieldLayoutTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFieldLayoutTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFieldLayoutTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFieldLayoutTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFieldLayoutTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFieldLayoutTable::pool_size - 1};
    SgAsmCilFieldLayoutTable* testPointer{reinterpret_cast<SgAsmCilFieldLayoutTable*>(SgAsmCilFieldLayoutTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFieldLayoutTable::pool_size * (SgAsmCilFieldLayoutTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldLayoutTableStroageClass
 * from its counterpart of SgAsmCilFieldLayoutTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFieldLayoutTable::initializeStorageClassArray(SgAsmCilFieldLayoutTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFieldLayoutTable::pools.begin(); block != SgAsmCilFieldLayoutTable::pools.end(); ++block) {
        SgAsmCilFieldLayoutTable* pointer{reinterpret_cast<SgAsmCilFieldLayoutTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldLayoutTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 170899 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFieldMarshal that takes its corresponding StorageClass as parameter
 */
SgAsmCilFieldMarshal::SgAsmCilFieldMarshal(const SgAsmCilFieldMarshalStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 170909 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Parent = storageSource.storageOf_Parent;
    p_NativeType = storageSource.storageOf_NativeType;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFieldMarshal*
SgAsmCilFieldMarshal::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFieldMarshal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldMarshal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldMarshal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldMarshal)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshal)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldMarshal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldMarshal::pool_size};
        returnPointer = &(((SgAsmCilFieldMarshal*)(SgAsmCilFieldMarshal::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFieldMarshal*
SgAsmCilFieldMarshal::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFieldMarshal* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldMarshal)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldMarshal + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldMarshal)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFieldMarshal)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldMarshal::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldMarshal::pool_size};
        returnPointer = &(((SgAsmCilFieldMarshal*)(SgAsmCilFieldMarshal::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFieldMarshal::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFieldMarshal::pools.begin(); block != SgAsmCilFieldMarshal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldMarshal*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldMarshal::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFieldMarshal*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFieldMarshal::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFieldMarshal* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFieldMarshal::pools.begin(); block != SgAsmCilFieldMarshal::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldMarshal*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldMarshal::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFieldMarshal::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFieldMarshal::clearMemoryPool() {
    SgAsmCilFieldMarshal* tempPointer{nullptr};
    if (SgAsmCilFieldMarshal::pools.empty() == false) {
        auto block{SgAsmCilFieldMarshal::pools.begin()};
        SgAsmCilFieldMarshal::next_node = reinterpret_cast<SgAsmCilFieldMarshal*>(*block);
        for (; block != SgAsmCilFieldMarshal::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFieldMarshal*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFieldMarshal::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFieldMarshal::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFieldMarshal::pool_size-1]);
        }
    }
}

void
SgAsmCilFieldMarshal::deleteMemoryPool() {
    for (auto p: SgAsmCilFieldMarshal::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFieldMarshal::next_node = nullptr;
    SgAsmCilFieldMarshal::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFieldMarshal::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshal) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldMarshal)};
    for (size_t blockIndex = SgAsmCilFieldMarshal::pools.size(); (blockIndex * SgAsmCilFieldMarshal::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshal) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldMarshal) = %" PRIuPTR " SgAsmCilFieldMarshal::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshal),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldMarshal),SgAsmCilFieldMarshal::pool_size);
        }
#endif

        SgAsmCilFieldMarshal* pointer{static_cast<SgAsmCilFieldMarshal*>(ROSE_MALLOC(SgAsmCilFieldMarshal::pool_size * sizeof(SgAsmCilFieldMarshal)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFieldMarshal::pool_size * sizeof(SgAsmCilFieldMarshal));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFieldMarshal::pool_size * sizeof(SgAsmCilFieldMarshal));
#endif
        SgAsmCilFieldMarshal::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFieldMarshal::pool_size * sizeof(SgAsmCilFieldMarshal), V_SgAsmCilFieldMarshal));

        if (SgAsmCilFieldMarshal::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFieldMarshal* blkptr{reinterpret_cast<SgAsmCilFieldMarshal*>(SgAsmCilFieldMarshal::pools[blockIndex-1])};
                blkptr[SgAsmCilFieldMarshal::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFieldMarshal::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFieldMarshal::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFieldMarshal::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFieldMarshal::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFieldMarshal::pool_size - 1};
    SgAsmCilFieldMarshal* testPointer{reinterpret_cast<SgAsmCilFieldMarshal*>(SgAsmCilFieldMarshal::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFieldMarshal::pool_size * (SgAsmCilFieldMarshal::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldMarshalStroageClass
 * from its counterpart of SgAsmCilFieldMarshal. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFieldMarshal::initializeStorageClassArray(SgAsmCilFieldMarshalStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFieldMarshal::pools.begin(); block != SgAsmCilFieldMarshal::pools.end(); ++block) {
        SgAsmCilFieldMarshal* pointer{reinterpret_cast<SgAsmCilFieldMarshal*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldMarshal::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 171128 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFieldMarshalTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilFieldMarshalTable::SgAsmCilFieldMarshalTable(const SgAsmCilFieldMarshalTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 171138 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFieldMarshalTable*
SgAsmCilFieldMarshalTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFieldMarshalTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldMarshalTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldMarshalTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldMarshalTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshalTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldMarshalTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldMarshalTable::pool_size};
        returnPointer = &(((SgAsmCilFieldMarshalTable*)(SgAsmCilFieldMarshalTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFieldMarshalTable*
SgAsmCilFieldMarshalTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFieldMarshalTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldMarshalTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldMarshalTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldMarshalTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFieldMarshalTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldMarshalTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldMarshalTable::pool_size};
        returnPointer = &(((SgAsmCilFieldMarshalTable*)(SgAsmCilFieldMarshalTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFieldMarshalTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFieldMarshalTable::pools.begin(); block != SgAsmCilFieldMarshalTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldMarshalTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldMarshalTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFieldMarshalTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFieldMarshalTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFieldMarshalTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFieldMarshalTable::pools.begin(); block != SgAsmCilFieldMarshalTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldMarshalTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldMarshalTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFieldMarshalTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFieldMarshalTable::clearMemoryPool() {
    SgAsmCilFieldMarshalTable* tempPointer{nullptr};
    if (SgAsmCilFieldMarshalTable::pools.empty() == false) {
        auto block{SgAsmCilFieldMarshalTable::pools.begin()};
        SgAsmCilFieldMarshalTable::next_node = reinterpret_cast<SgAsmCilFieldMarshalTable*>(*block);
        for (; block != SgAsmCilFieldMarshalTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFieldMarshalTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFieldMarshalTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFieldMarshalTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFieldMarshalTable::pool_size-1]);
        }
    }
}

void
SgAsmCilFieldMarshalTable::deleteMemoryPool() {
    for (auto p: SgAsmCilFieldMarshalTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFieldMarshalTable::next_node = nullptr;
    SgAsmCilFieldMarshalTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFieldMarshalTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshalTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldMarshalTable)};
    for (size_t blockIndex = SgAsmCilFieldMarshalTable::pools.size(); (blockIndex * SgAsmCilFieldMarshalTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshalTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldMarshalTable) = %" PRIuPTR " SgAsmCilFieldMarshalTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldMarshalTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldMarshalTable),SgAsmCilFieldMarshalTable::pool_size);
        }
#endif

        SgAsmCilFieldMarshalTable* pointer{static_cast<SgAsmCilFieldMarshalTable*>(ROSE_MALLOC(SgAsmCilFieldMarshalTable::pool_size * sizeof(SgAsmCilFieldMarshalTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFieldMarshalTable::pool_size * sizeof(SgAsmCilFieldMarshalTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFieldMarshalTable::pool_size * sizeof(SgAsmCilFieldMarshalTable));
#endif
        SgAsmCilFieldMarshalTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFieldMarshalTable::pool_size * sizeof(SgAsmCilFieldMarshalTable), V_SgAsmCilFieldMarshalTable));

        if (SgAsmCilFieldMarshalTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFieldMarshalTable* blkptr{reinterpret_cast<SgAsmCilFieldMarshalTable*>(SgAsmCilFieldMarshalTable::pools[blockIndex-1])};
                blkptr[SgAsmCilFieldMarshalTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFieldMarshalTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFieldMarshalTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFieldMarshalTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFieldMarshalTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFieldMarshalTable::pool_size - 1};
    SgAsmCilFieldMarshalTable* testPointer{reinterpret_cast<SgAsmCilFieldMarshalTable*>(SgAsmCilFieldMarshalTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFieldMarshalTable::pool_size * (SgAsmCilFieldMarshalTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldMarshalTableStroageClass
 * from its counterpart of SgAsmCilFieldMarshalTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFieldMarshalTable::initializeStorageClassArray(SgAsmCilFieldMarshalTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFieldMarshalTable::pools.begin(); block != SgAsmCilFieldMarshalTable::pools.end(); ++block) {
        SgAsmCilFieldMarshalTable* pointer{reinterpret_cast<SgAsmCilFieldMarshalTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldMarshalTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 171356 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFieldRVA that takes its corresponding StorageClass as parameter
 */
SgAsmCilFieldRVA::SgAsmCilFieldRVA(const SgAsmCilFieldRVAStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 171366 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_RVA = storageSource.storageOf_RVA;
    p_Field = storageSource.storageOf_Field;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFieldRVA*
SgAsmCilFieldRVA::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFieldRVA* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldRVA)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldRVA + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldRVA)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVA)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldRVA::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldRVA::pool_size};
        returnPointer = &(((SgAsmCilFieldRVA*)(SgAsmCilFieldRVA::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFieldRVA*
SgAsmCilFieldRVA::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFieldRVA* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldRVA)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldRVA + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldRVA)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFieldRVA)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldRVA::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldRVA::pool_size};
        returnPointer = &(((SgAsmCilFieldRVA*)(SgAsmCilFieldRVA::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFieldRVA::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFieldRVA::pools.begin(); block != SgAsmCilFieldRVA::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldRVA*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldRVA::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFieldRVA*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFieldRVA::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFieldRVA* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFieldRVA::pools.begin(); block != SgAsmCilFieldRVA::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldRVA*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldRVA::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFieldRVA::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFieldRVA::clearMemoryPool() {
    SgAsmCilFieldRVA* tempPointer{nullptr};
    if (SgAsmCilFieldRVA::pools.empty() == false) {
        auto block{SgAsmCilFieldRVA::pools.begin()};
        SgAsmCilFieldRVA::next_node = reinterpret_cast<SgAsmCilFieldRVA*>(*block);
        for (; block != SgAsmCilFieldRVA::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFieldRVA*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFieldRVA::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFieldRVA::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFieldRVA::pool_size-1]);
        }
    }
}

void
SgAsmCilFieldRVA::deleteMemoryPool() {
    for (auto p: SgAsmCilFieldRVA::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFieldRVA::next_node = nullptr;
    SgAsmCilFieldRVA::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFieldRVA::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVA) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldRVA)};
    for (size_t blockIndex = SgAsmCilFieldRVA::pools.size(); (blockIndex * SgAsmCilFieldRVA::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVA) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldRVA) = %" PRIuPTR " SgAsmCilFieldRVA::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVA),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldRVA),SgAsmCilFieldRVA::pool_size);
        }
#endif

        SgAsmCilFieldRVA* pointer{static_cast<SgAsmCilFieldRVA*>(ROSE_MALLOC(SgAsmCilFieldRVA::pool_size * sizeof(SgAsmCilFieldRVA)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFieldRVA::pool_size * sizeof(SgAsmCilFieldRVA));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFieldRVA::pool_size * sizeof(SgAsmCilFieldRVA));
#endif
        SgAsmCilFieldRVA::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFieldRVA::pool_size * sizeof(SgAsmCilFieldRVA), V_SgAsmCilFieldRVA));

        if (SgAsmCilFieldRVA::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFieldRVA* blkptr{reinterpret_cast<SgAsmCilFieldRVA*>(SgAsmCilFieldRVA::pools[blockIndex-1])};
                blkptr[SgAsmCilFieldRVA::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFieldRVA::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFieldRVA::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFieldRVA::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFieldRVA::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFieldRVA::pool_size - 1};
    SgAsmCilFieldRVA* testPointer{reinterpret_cast<SgAsmCilFieldRVA*>(SgAsmCilFieldRVA::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFieldRVA::pool_size * (SgAsmCilFieldRVA::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldRVAStroageClass
 * from its counterpart of SgAsmCilFieldRVA. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFieldRVA::initializeStorageClassArray(SgAsmCilFieldRVAStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFieldRVA::pools.begin(); block != SgAsmCilFieldRVA::pools.end(); ++block) {
        SgAsmCilFieldRVA* pointer{reinterpret_cast<SgAsmCilFieldRVA*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldRVA::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 171585 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFieldRVATable that takes its corresponding StorageClass as parameter
 */
SgAsmCilFieldRVATable::SgAsmCilFieldRVATable(const SgAsmCilFieldRVATableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 171595 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFieldRVATable*
SgAsmCilFieldRVATable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFieldRVATable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldRVATable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldRVATable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldRVATable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVATable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldRVATable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldRVATable::pool_size};
        returnPointer = &(((SgAsmCilFieldRVATable*)(SgAsmCilFieldRVATable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFieldRVATable*
SgAsmCilFieldRVATable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFieldRVATable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldRVATable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldRVATable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldRVATable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFieldRVATable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldRVATable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldRVATable::pool_size};
        returnPointer = &(((SgAsmCilFieldRVATable*)(SgAsmCilFieldRVATable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFieldRVATable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFieldRVATable::pools.begin(); block != SgAsmCilFieldRVATable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldRVATable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldRVATable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFieldRVATable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFieldRVATable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFieldRVATable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFieldRVATable::pools.begin(); block != SgAsmCilFieldRVATable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldRVATable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldRVATable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFieldRVATable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFieldRVATable::clearMemoryPool() {
    SgAsmCilFieldRVATable* tempPointer{nullptr};
    if (SgAsmCilFieldRVATable::pools.empty() == false) {
        auto block{SgAsmCilFieldRVATable::pools.begin()};
        SgAsmCilFieldRVATable::next_node = reinterpret_cast<SgAsmCilFieldRVATable*>(*block);
        for (; block != SgAsmCilFieldRVATable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFieldRVATable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFieldRVATable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFieldRVATable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFieldRVATable::pool_size-1]);
        }
    }
}

void
SgAsmCilFieldRVATable::deleteMemoryPool() {
    for (auto p: SgAsmCilFieldRVATable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFieldRVATable::next_node = nullptr;
    SgAsmCilFieldRVATable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFieldRVATable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVATable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldRVATable)};
    for (size_t blockIndex = SgAsmCilFieldRVATable::pools.size(); (blockIndex * SgAsmCilFieldRVATable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVATable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldRVATable) = %" PRIuPTR " SgAsmCilFieldRVATable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldRVATable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldRVATable),SgAsmCilFieldRVATable::pool_size);
        }
#endif

        SgAsmCilFieldRVATable* pointer{static_cast<SgAsmCilFieldRVATable*>(ROSE_MALLOC(SgAsmCilFieldRVATable::pool_size * sizeof(SgAsmCilFieldRVATable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFieldRVATable::pool_size * sizeof(SgAsmCilFieldRVATable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFieldRVATable::pool_size * sizeof(SgAsmCilFieldRVATable));
#endif
        SgAsmCilFieldRVATable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFieldRVATable::pool_size * sizeof(SgAsmCilFieldRVATable), V_SgAsmCilFieldRVATable));

        if (SgAsmCilFieldRVATable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFieldRVATable* blkptr{reinterpret_cast<SgAsmCilFieldRVATable*>(SgAsmCilFieldRVATable::pools[blockIndex-1])};
                blkptr[SgAsmCilFieldRVATable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFieldRVATable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFieldRVATable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFieldRVATable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFieldRVATable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFieldRVATable::pool_size - 1};
    SgAsmCilFieldRVATable* testPointer{reinterpret_cast<SgAsmCilFieldRVATable*>(SgAsmCilFieldRVATable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFieldRVATable::pool_size * (SgAsmCilFieldRVATable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldRVATableStroageClass
 * from its counterpart of SgAsmCilFieldRVATable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFieldRVATable::initializeStorageClassArray(SgAsmCilFieldRVATableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFieldRVATable::pools.begin(); block != SgAsmCilFieldRVATable::pools.end(); ++block) {
        SgAsmCilFieldRVATable* pointer{reinterpret_cast<SgAsmCilFieldRVATable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldRVATable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 171813 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFieldTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilFieldTable::SgAsmCilFieldTable(const SgAsmCilFieldTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 171823 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFieldTable*
SgAsmCilFieldTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFieldTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFieldTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldTable::pool_size};
        returnPointer = &(((SgAsmCilFieldTable*)(SgAsmCilFieldTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFieldTable*
SgAsmCilFieldTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFieldTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFieldTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFieldTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFieldTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFieldTable::pool_size};
        returnPointer = &(((SgAsmCilFieldTable*)(SgAsmCilFieldTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFieldTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFieldTable::pools.begin(); block != SgAsmCilFieldTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFieldTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFieldTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFieldTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFieldTable::pools.begin(); block != SgAsmCilFieldTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFieldTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFieldTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFieldTable::clearMemoryPool() {
    SgAsmCilFieldTable* tempPointer{nullptr};
    if (SgAsmCilFieldTable::pools.empty() == false) {
        auto block{SgAsmCilFieldTable::pools.begin()};
        SgAsmCilFieldTable::next_node = reinterpret_cast<SgAsmCilFieldTable*>(*block);
        for (; block != SgAsmCilFieldTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFieldTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFieldTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFieldTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFieldTable::pool_size-1]);
        }
    }
}

void
SgAsmCilFieldTable::deleteMemoryPool() {
    for (auto p: SgAsmCilFieldTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFieldTable::next_node = nullptr;
    SgAsmCilFieldTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFieldTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldTable)};
    for (size_t blockIndex = SgAsmCilFieldTable::pools.size(); (blockIndex * SgAsmCilFieldTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldTable) = %" PRIuPTR " SgAsmCilFieldTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFieldTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFieldTable),SgAsmCilFieldTable::pool_size);
        }
#endif

        SgAsmCilFieldTable* pointer{static_cast<SgAsmCilFieldTable*>(ROSE_MALLOC(SgAsmCilFieldTable::pool_size * sizeof(SgAsmCilFieldTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFieldTable::pool_size * sizeof(SgAsmCilFieldTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFieldTable::pool_size * sizeof(SgAsmCilFieldTable));
#endif
        SgAsmCilFieldTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFieldTable::pool_size * sizeof(SgAsmCilFieldTable), V_SgAsmCilFieldTable));

        if (SgAsmCilFieldTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFieldTable* blkptr{reinterpret_cast<SgAsmCilFieldTable*>(SgAsmCilFieldTable::pools[blockIndex-1])};
                blkptr[SgAsmCilFieldTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFieldTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFieldTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFieldTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFieldTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFieldTable::pool_size - 1};
    SgAsmCilFieldTable* testPointer{reinterpret_cast<SgAsmCilFieldTable*>(SgAsmCilFieldTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFieldTable::pool_size * (SgAsmCilFieldTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFieldTableStroageClass
 * from its counterpart of SgAsmCilFieldTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFieldTable::initializeStorageClassArray(SgAsmCilFieldTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFieldTable::pools.begin(); block != SgAsmCilFieldTable::pools.end(); ++block) {
        SgAsmCilFieldTable* pointer{reinterpret_cast<SgAsmCilFieldTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFieldTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 172041 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFile that takes its corresponding StorageClass as parameter
 */
SgAsmCilFile::SgAsmCilFile(const SgAsmCilFileStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 172051 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Flags = storageSource.storageOf_Flags;
    p_Name = storageSource.storageOf_Name;
    p_HashValue = storageSource.storageOf_HashValue;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFile*
SgAsmCilFile::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFile)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFile)};
        unsigned long positionInPool{localIndex % SgAsmCilFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFile::pool_size};
        returnPointer = &(((SgAsmCilFile*)(SgAsmCilFile::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFile*
SgAsmCilFile::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFile)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFile)};
        unsigned long positionInPool{localIndex % SgAsmCilFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFile::pool_size};
        returnPointer = &(((SgAsmCilFile*)(SgAsmCilFile::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFile::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFile::pools.begin(); block != SgAsmCilFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFile*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFile::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFile*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFile::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFile* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFile::pools.begin(); block != SgAsmCilFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFile*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFile::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFile::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFile::clearMemoryPool() {
    SgAsmCilFile* tempPointer{nullptr};
    if (SgAsmCilFile::pools.empty() == false) {
        auto block{SgAsmCilFile::pools.begin()};
        SgAsmCilFile::next_node = reinterpret_cast<SgAsmCilFile*>(*block);
        for (; block != SgAsmCilFile::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFile*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFile::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFile::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFile::pool_size-1]);
        }
    }
}

void
SgAsmCilFile::deleteMemoryPool() {
    for (auto p: SgAsmCilFile::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFile::next_node = nullptr;
    SgAsmCilFile::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFile::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFile) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFile)};
    for (size_t blockIndex = SgAsmCilFile::pools.size(); (blockIndex * SgAsmCilFile::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFile) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFile) = %" PRIuPTR " SgAsmCilFile::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFile),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFile),SgAsmCilFile::pool_size);
        }
#endif

        SgAsmCilFile* pointer{static_cast<SgAsmCilFile*>(ROSE_MALLOC(SgAsmCilFile::pool_size * sizeof(SgAsmCilFile)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFile::pool_size * sizeof(SgAsmCilFile));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFile::pool_size * sizeof(SgAsmCilFile));
#endif
        SgAsmCilFile::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFile::pool_size * sizeof(SgAsmCilFile), V_SgAsmCilFile));

        if (SgAsmCilFile::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFile* blkptr{reinterpret_cast<SgAsmCilFile*>(SgAsmCilFile::pools[blockIndex-1])};
                blkptr[SgAsmCilFile::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFile::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFile::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFile::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFile::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFile::pool_size - 1};
    SgAsmCilFile* testPointer{reinterpret_cast<SgAsmCilFile*>(SgAsmCilFile::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFile::pool_size * (SgAsmCilFile::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFileStroageClass
 * from its counterpart of SgAsmCilFile. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFile::initializeStorageClassArray(SgAsmCilFileStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFile::pools.begin(); block != SgAsmCilFile::pools.end(); ++block) {
        SgAsmCilFile* pointer{reinterpret_cast<SgAsmCilFile*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFile::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 172271 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilFileTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilFileTable::SgAsmCilFileTable(const SgAsmCilFileTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 172281 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilFileTable*
SgAsmCilFileTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilFileTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFileTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFileTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilFileTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFileTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFileTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFileTable::pool_size};
        returnPointer = &(((SgAsmCilFileTable*)(SgAsmCilFileTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilFileTable*
SgAsmCilFileTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilFileTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFileTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFileTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilFileTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilFileTable)};
        unsigned long positionInPool{localIndex % SgAsmCilFileTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilFileTable::pool_size};
        returnPointer = &(((SgAsmCilFileTable*)(SgAsmCilFileTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilFileTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilFileTable::pools.begin(); block != SgAsmCilFileTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFileTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFileTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilFileTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilFileTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilFileTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilFileTable::pools.begin(); block != SgAsmCilFileTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilFileTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFileTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilFileTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilFileTable::clearMemoryPool() {
    SgAsmCilFileTable* tempPointer{nullptr};
    if (SgAsmCilFileTable::pools.empty() == false) {
        auto block{SgAsmCilFileTable::pools.begin()};
        SgAsmCilFileTable::next_node = reinterpret_cast<SgAsmCilFileTable*>(*block);
        for (; block != SgAsmCilFileTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilFileTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilFileTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilFileTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilFileTable::pool_size-1]);
        }
    }
}

void
SgAsmCilFileTable::deleteMemoryPool() {
    for (auto p: SgAsmCilFileTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilFileTable::next_node = nullptr;
    SgAsmCilFileTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilFileTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFileTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFileTable)};
    for (size_t blockIndex = SgAsmCilFileTable::pools.size(); (blockIndex * SgAsmCilFileTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFileTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFileTable) = %" PRIuPTR " SgAsmCilFileTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilFileTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilFileTable),SgAsmCilFileTable::pool_size);
        }
#endif

        SgAsmCilFileTable* pointer{static_cast<SgAsmCilFileTable*>(ROSE_MALLOC(SgAsmCilFileTable::pool_size * sizeof(SgAsmCilFileTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilFileTable::pool_size * sizeof(SgAsmCilFileTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilFileTable::pool_size * sizeof(SgAsmCilFileTable));
#endif
        SgAsmCilFileTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilFileTable::pool_size * sizeof(SgAsmCilFileTable), V_SgAsmCilFileTable));

        if (SgAsmCilFileTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilFileTable* blkptr{reinterpret_cast<SgAsmCilFileTable*>(SgAsmCilFileTable::pools[blockIndex-1])};
                blkptr[SgAsmCilFileTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilFileTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilFileTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilFileTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilFileTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilFileTable::pool_size - 1};
    SgAsmCilFileTable* testPointer{reinterpret_cast<SgAsmCilFileTable*>(SgAsmCilFileTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilFileTable::pool_size * (SgAsmCilFileTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilFileTableStroageClass
 * from its counterpart of SgAsmCilFileTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilFileTable::initializeStorageClassArray(SgAsmCilFileTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilFileTable::pools.begin(); block != SgAsmCilFileTable::pools.end(); ++block) {
        SgAsmCilFileTable* pointer{reinterpret_cast<SgAsmCilFileTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilFileTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 172499 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilGenericParamConstraint that takes its corresponding StorageClass as parameter
 */
SgAsmCilGenericParamConstraint::SgAsmCilGenericParamConstraint(const SgAsmCilGenericParamConstraintStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 172509 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Owner = storageSource.storageOf_Owner;
    p_Constraint = storageSource.storageOf_Constraint;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilGenericParamConstraint*
SgAsmCilGenericParamConstraint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilGenericParamConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraint)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParamConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParamConstraint::pool_size};
        returnPointer = &(((SgAsmCilGenericParamConstraint*)(SgAsmCilGenericParamConstraint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilGenericParamConstraint*
SgAsmCilGenericParamConstraint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilGenericParamConstraint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamConstraint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamConstraint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamConstraint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilGenericParamConstraint)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParamConstraint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParamConstraint::pool_size};
        returnPointer = &(((SgAsmCilGenericParamConstraint*)(SgAsmCilGenericParamConstraint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilGenericParamConstraint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilGenericParamConstraint::pools.begin(); block != SgAsmCilGenericParamConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParamConstraint*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamConstraint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilGenericParamConstraint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilGenericParamConstraint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilGenericParamConstraint* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilGenericParamConstraint::pools.begin(); block != SgAsmCilGenericParamConstraint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParamConstraint*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamConstraint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilGenericParamConstraint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilGenericParamConstraint::clearMemoryPool() {
    SgAsmCilGenericParamConstraint* tempPointer{nullptr};
    if (SgAsmCilGenericParamConstraint::pools.empty() == false) {
        auto block{SgAsmCilGenericParamConstraint::pools.begin()};
        SgAsmCilGenericParamConstraint::next_node = reinterpret_cast<SgAsmCilGenericParamConstraint*>(*block);
        for (; block != SgAsmCilGenericParamConstraint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilGenericParamConstraint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilGenericParamConstraint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilGenericParamConstraint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilGenericParamConstraint::pool_size-1]);
        }
    }
}

void
SgAsmCilGenericParamConstraint::deleteMemoryPool() {
    for (auto p: SgAsmCilGenericParamConstraint::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilGenericParamConstraint::next_node = nullptr;
    SgAsmCilGenericParamConstraint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilGenericParamConstraint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraint)};
    for (size_t blockIndex = SgAsmCilGenericParamConstraint::pools.size(); (blockIndex * SgAsmCilGenericParamConstraint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraint) = %" PRIuPTR " SgAsmCilGenericParamConstraint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraint),SgAsmCilGenericParamConstraint::pool_size);
        }
#endif

        SgAsmCilGenericParamConstraint* pointer{static_cast<SgAsmCilGenericParamConstraint*>(ROSE_MALLOC(SgAsmCilGenericParamConstraint::pool_size * sizeof(SgAsmCilGenericParamConstraint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilGenericParamConstraint::pool_size * sizeof(SgAsmCilGenericParamConstraint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilGenericParamConstraint::pool_size * sizeof(SgAsmCilGenericParamConstraint));
#endif
        SgAsmCilGenericParamConstraint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilGenericParamConstraint::pool_size * sizeof(SgAsmCilGenericParamConstraint), V_SgAsmCilGenericParamConstraint));

        if (SgAsmCilGenericParamConstraint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilGenericParamConstraint* blkptr{reinterpret_cast<SgAsmCilGenericParamConstraint*>(SgAsmCilGenericParamConstraint::pools[blockIndex-1])};
                blkptr[SgAsmCilGenericParamConstraint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilGenericParamConstraint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilGenericParamConstraint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilGenericParamConstraint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilGenericParamConstraint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilGenericParamConstraint::pool_size - 1};
    SgAsmCilGenericParamConstraint* testPointer{reinterpret_cast<SgAsmCilGenericParamConstraint*>(SgAsmCilGenericParamConstraint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilGenericParamConstraint::pool_size * (SgAsmCilGenericParamConstraint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilGenericParamConstraintStroageClass
 * from its counterpart of SgAsmCilGenericParamConstraint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilGenericParamConstraint::initializeStorageClassArray(SgAsmCilGenericParamConstraintStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilGenericParamConstraint::pools.begin(); block != SgAsmCilGenericParamConstraint::pools.end(); ++block) {
        SgAsmCilGenericParamConstraint* pointer{reinterpret_cast<SgAsmCilGenericParamConstraint*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamConstraint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 172728 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilGenericParamConstraintTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilGenericParamConstraintTable::SgAsmCilGenericParamConstraintTable(const SgAsmCilGenericParamConstraintTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 172738 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilGenericParamConstraintTable*
SgAsmCilGenericParamConstraintTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilGenericParamConstraintTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraintTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraintTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraintTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraintTable)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParamConstraintTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParamConstraintTable::pool_size};
        returnPointer = &(((SgAsmCilGenericParamConstraintTable*)(SgAsmCilGenericParamConstraintTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilGenericParamConstraintTable*
SgAsmCilGenericParamConstraintTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilGenericParamConstraintTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamConstraintTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamConstraintTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamConstraintTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilGenericParamConstraintTable)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParamConstraintTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParamConstraintTable::pool_size};
        returnPointer = &(((SgAsmCilGenericParamConstraintTable*)(SgAsmCilGenericParamConstraintTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilGenericParamConstraintTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilGenericParamConstraintTable::pools.begin(); block != SgAsmCilGenericParamConstraintTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParamConstraintTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamConstraintTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilGenericParamConstraintTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilGenericParamConstraintTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilGenericParamConstraintTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilGenericParamConstraintTable::pools.begin(); block != SgAsmCilGenericParamConstraintTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParamConstraintTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamConstraintTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilGenericParamConstraintTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilGenericParamConstraintTable::clearMemoryPool() {
    SgAsmCilGenericParamConstraintTable* tempPointer{nullptr};
    if (SgAsmCilGenericParamConstraintTable::pools.empty() == false) {
        auto block{SgAsmCilGenericParamConstraintTable::pools.begin()};
        SgAsmCilGenericParamConstraintTable::next_node = reinterpret_cast<SgAsmCilGenericParamConstraintTable*>(*block);
        for (; block != SgAsmCilGenericParamConstraintTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilGenericParamConstraintTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilGenericParamConstraintTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilGenericParamConstraintTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilGenericParamConstraintTable::pool_size-1]);
        }
    }
}

void
SgAsmCilGenericParamConstraintTable::deleteMemoryPool() {
    for (auto p: SgAsmCilGenericParamConstraintTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilGenericParamConstraintTable::next_node = nullptr;
    SgAsmCilGenericParamConstraintTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilGenericParamConstraintTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraintTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraintTable)};
    for (size_t blockIndex = SgAsmCilGenericParamConstraintTable::pools.size(); (blockIndex * SgAsmCilGenericParamConstraintTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraintTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraintTable) = %" PRIuPTR " SgAsmCilGenericParamConstraintTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamConstraintTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamConstraintTable),SgAsmCilGenericParamConstraintTable::pool_size);
        }
#endif

        SgAsmCilGenericParamConstraintTable* pointer{static_cast<SgAsmCilGenericParamConstraintTable*>(ROSE_MALLOC(SgAsmCilGenericParamConstraintTable::pool_size * sizeof(SgAsmCilGenericParamConstraintTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilGenericParamConstraintTable::pool_size * sizeof(SgAsmCilGenericParamConstraintTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilGenericParamConstraintTable::pool_size * sizeof(SgAsmCilGenericParamConstraintTable));
#endif
        SgAsmCilGenericParamConstraintTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilGenericParamConstraintTable::pool_size * sizeof(SgAsmCilGenericParamConstraintTable), V_SgAsmCilGenericParamConstraintTable));

        if (SgAsmCilGenericParamConstraintTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilGenericParamConstraintTable* blkptr{reinterpret_cast<SgAsmCilGenericParamConstraintTable*>(SgAsmCilGenericParamConstraintTable::pools[blockIndex-1])};
                blkptr[SgAsmCilGenericParamConstraintTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilGenericParamConstraintTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilGenericParamConstraintTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilGenericParamConstraintTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilGenericParamConstraintTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilGenericParamConstraintTable::pool_size - 1};
    SgAsmCilGenericParamConstraintTable* testPointer{reinterpret_cast<SgAsmCilGenericParamConstraintTable*>(SgAsmCilGenericParamConstraintTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilGenericParamConstraintTable::pool_size * (SgAsmCilGenericParamConstraintTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilGenericParamConstraintTableStroageClass
 * from its counterpart of SgAsmCilGenericParamConstraintTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilGenericParamConstraintTable::initializeStorageClassArray(SgAsmCilGenericParamConstraintTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilGenericParamConstraintTable::pools.begin(); block != SgAsmCilGenericParamConstraintTable::pools.end(); ++block) {
        SgAsmCilGenericParamConstraintTable* pointer{reinterpret_cast<SgAsmCilGenericParamConstraintTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamConstraintTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 172956 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilGenericParam that takes its corresponding StorageClass as parameter
 */
SgAsmCilGenericParam::SgAsmCilGenericParam(const SgAsmCilGenericParamStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 172966 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Number = storageSource.storageOf_Number;
    p_Flags = storageSource.storageOf_Flags;
    p_Owner = storageSource.storageOf_Owner;
    p_Name = storageSource.storageOf_Name;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilGenericParam*
SgAsmCilGenericParam::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilGenericParam* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParam)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParam + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParam)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParam)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParam::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParam::pool_size};
        returnPointer = &(((SgAsmCilGenericParam*)(SgAsmCilGenericParam::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilGenericParam*
SgAsmCilGenericParam::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilGenericParam* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParam)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParam + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParam)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilGenericParam)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParam::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParam::pool_size};
        returnPointer = &(((SgAsmCilGenericParam*)(SgAsmCilGenericParam::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilGenericParam::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilGenericParam::pools.begin(); block != SgAsmCilGenericParam::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParam*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParam::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilGenericParam*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilGenericParam::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilGenericParam* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilGenericParam::pools.begin(); block != SgAsmCilGenericParam::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParam*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParam::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilGenericParam::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilGenericParam::clearMemoryPool() {
    SgAsmCilGenericParam* tempPointer{nullptr};
    if (SgAsmCilGenericParam::pools.empty() == false) {
        auto block{SgAsmCilGenericParam::pools.begin()};
        SgAsmCilGenericParam::next_node = reinterpret_cast<SgAsmCilGenericParam*>(*block);
        for (; block != SgAsmCilGenericParam::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilGenericParam*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilGenericParam::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilGenericParam::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilGenericParam::pool_size-1]);
        }
    }
}

void
SgAsmCilGenericParam::deleteMemoryPool() {
    for (auto p: SgAsmCilGenericParam::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilGenericParam::next_node = nullptr;
    SgAsmCilGenericParam::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilGenericParam::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParam) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParam)};
    for (size_t blockIndex = SgAsmCilGenericParam::pools.size(); (blockIndex * SgAsmCilGenericParam::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParam) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParam) = %" PRIuPTR " SgAsmCilGenericParam::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParam),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParam),SgAsmCilGenericParam::pool_size);
        }
#endif

        SgAsmCilGenericParam* pointer{static_cast<SgAsmCilGenericParam*>(ROSE_MALLOC(SgAsmCilGenericParam::pool_size * sizeof(SgAsmCilGenericParam)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilGenericParam::pool_size * sizeof(SgAsmCilGenericParam));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilGenericParam::pool_size * sizeof(SgAsmCilGenericParam));
#endif
        SgAsmCilGenericParam::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilGenericParam::pool_size * sizeof(SgAsmCilGenericParam), V_SgAsmCilGenericParam));

        if (SgAsmCilGenericParam::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilGenericParam* blkptr{reinterpret_cast<SgAsmCilGenericParam*>(SgAsmCilGenericParam::pools[blockIndex-1])};
                blkptr[SgAsmCilGenericParam::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilGenericParam::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilGenericParam::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilGenericParam::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilGenericParam::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilGenericParam::pool_size - 1};
    SgAsmCilGenericParam* testPointer{reinterpret_cast<SgAsmCilGenericParam*>(SgAsmCilGenericParam::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilGenericParam::pool_size * (SgAsmCilGenericParam::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilGenericParamStroageClass
 * from its counterpart of SgAsmCilGenericParam. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilGenericParam::initializeStorageClassArray(SgAsmCilGenericParamStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilGenericParam::pools.begin(); block != SgAsmCilGenericParam::pools.end(); ++block) {
        SgAsmCilGenericParam* pointer{reinterpret_cast<SgAsmCilGenericParam*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParam::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 173187 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilGenericParamTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilGenericParamTable::SgAsmCilGenericParamTable(const SgAsmCilGenericParamTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 173197 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilGenericParamTable*
SgAsmCilGenericParamTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilGenericParamTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilGenericParamTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamTable)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParamTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParamTable::pool_size};
        returnPointer = &(((SgAsmCilGenericParamTable*)(SgAsmCilGenericParamTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilGenericParamTable*
SgAsmCilGenericParamTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilGenericParamTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilGenericParamTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilGenericParamTable)};
        unsigned long positionInPool{localIndex % SgAsmCilGenericParamTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilGenericParamTable::pool_size};
        returnPointer = &(((SgAsmCilGenericParamTable*)(SgAsmCilGenericParamTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilGenericParamTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilGenericParamTable::pools.begin(); block != SgAsmCilGenericParamTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParamTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilGenericParamTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilGenericParamTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilGenericParamTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilGenericParamTable::pools.begin(); block != SgAsmCilGenericParamTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilGenericParamTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilGenericParamTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilGenericParamTable::clearMemoryPool() {
    SgAsmCilGenericParamTable* tempPointer{nullptr};
    if (SgAsmCilGenericParamTable::pools.empty() == false) {
        auto block{SgAsmCilGenericParamTable::pools.begin()};
        SgAsmCilGenericParamTable::next_node = reinterpret_cast<SgAsmCilGenericParamTable*>(*block);
        for (; block != SgAsmCilGenericParamTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilGenericParamTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilGenericParamTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilGenericParamTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilGenericParamTable::pool_size-1]);
        }
    }
}

void
SgAsmCilGenericParamTable::deleteMemoryPool() {
    for (auto p: SgAsmCilGenericParamTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilGenericParamTable::next_node = nullptr;
    SgAsmCilGenericParamTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilGenericParamTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamTable)};
    for (size_t blockIndex = SgAsmCilGenericParamTable::pools.size(); (blockIndex * SgAsmCilGenericParamTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamTable) = %" PRIuPTR " SgAsmCilGenericParamTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilGenericParamTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilGenericParamTable),SgAsmCilGenericParamTable::pool_size);
        }
#endif

        SgAsmCilGenericParamTable* pointer{static_cast<SgAsmCilGenericParamTable*>(ROSE_MALLOC(SgAsmCilGenericParamTable::pool_size * sizeof(SgAsmCilGenericParamTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilGenericParamTable::pool_size * sizeof(SgAsmCilGenericParamTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilGenericParamTable::pool_size * sizeof(SgAsmCilGenericParamTable));
#endif
        SgAsmCilGenericParamTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilGenericParamTable::pool_size * sizeof(SgAsmCilGenericParamTable), V_SgAsmCilGenericParamTable));

        if (SgAsmCilGenericParamTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilGenericParamTable* blkptr{reinterpret_cast<SgAsmCilGenericParamTable*>(SgAsmCilGenericParamTable::pools[blockIndex-1])};
                blkptr[SgAsmCilGenericParamTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilGenericParamTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilGenericParamTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilGenericParamTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilGenericParamTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilGenericParamTable::pool_size - 1};
    SgAsmCilGenericParamTable* testPointer{reinterpret_cast<SgAsmCilGenericParamTable*>(SgAsmCilGenericParamTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilGenericParamTable::pool_size * (SgAsmCilGenericParamTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilGenericParamTableStroageClass
 * from its counterpart of SgAsmCilGenericParamTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilGenericParamTable::initializeStorageClassArray(SgAsmCilGenericParamTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilGenericParamTable::pools.begin(); block != SgAsmCilGenericParamTable::pools.end(); ++block) {
        SgAsmCilGenericParamTable* pointer{reinterpret_cast<SgAsmCilGenericParamTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilGenericParamTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 173415 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilImplMap that takes its corresponding StorageClass as parameter
 */
SgAsmCilImplMap::SgAsmCilImplMap(const SgAsmCilImplMapStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 173425 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_MappingFlags = storageSource.storageOf_MappingFlags;
    p_MemberForwarded = storageSource.storageOf_MemberForwarded;
    p_ImportName = storageSource.storageOf_ImportName;
    p_ImportScope = storageSource.storageOf_ImportScope;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilImplMap*
SgAsmCilImplMap::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilImplMap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilImplMap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilImplMap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilImplMap)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMap)};
        unsigned long positionInPool{localIndex % SgAsmCilImplMap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilImplMap::pool_size};
        returnPointer = &(((SgAsmCilImplMap*)(SgAsmCilImplMap::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilImplMap*
SgAsmCilImplMap::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilImplMap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilImplMap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilImplMap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilImplMap)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilImplMap)};
        unsigned long positionInPool{localIndex % SgAsmCilImplMap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilImplMap::pool_size};
        returnPointer = &(((SgAsmCilImplMap*)(SgAsmCilImplMap::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilImplMap::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilImplMap::pools.begin(); block != SgAsmCilImplMap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilImplMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilImplMap::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilImplMap*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilImplMap::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilImplMap* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilImplMap::pools.begin(); block != SgAsmCilImplMap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilImplMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilImplMap::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilImplMap::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilImplMap::clearMemoryPool() {
    SgAsmCilImplMap* tempPointer{nullptr};
    if (SgAsmCilImplMap::pools.empty() == false) {
        auto block{SgAsmCilImplMap::pools.begin()};
        SgAsmCilImplMap::next_node = reinterpret_cast<SgAsmCilImplMap*>(*block);
        for (; block != SgAsmCilImplMap::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilImplMap*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilImplMap::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilImplMap::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilImplMap::pool_size-1]);
        }
    }
}

void
SgAsmCilImplMap::deleteMemoryPool() {
    for (auto p: SgAsmCilImplMap::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilImplMap::next_node = nullptr;
    SgAsmCilImplMap::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilImplMap::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMap) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilImplMap)};
    for (size_t blockIndex = SgAsmCilImplMap::pools.size(); (blockIndex * SgAsmCilImplMap::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMap) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilImplMap) = %" PRIuPTR " SgAsmCilImplMap::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMap),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilImplMap),SgAsmCilImplMap::pool_size);
        }
#endif

        SgAsmCilImplMap* pointer{static_cast<SgAsmCilImplMap*>(ROSE_MALLOC(SgAsmCilImplMap::pool_size * sizeof(SgAsmCilImplMap)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilImplMap::pool_size * sizeof(SgAsmCilImplMap));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilImplMap::pool_size * sizeof(SgAsmCilImplMap));
#endif
        SgAsmCilImplMap::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilImplMap::pool_size * sizeof(SgAsmCilImplMap), V_SgAsmCilImplMap));

        if (SgAsmCilImplMap::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilImplMap* blkptr{reinterpret_cast<SgAsmCilImplMap*>(SgAsmCilImplMap::pools[blockIndex-1])};
                blkptr[SgAsmCilImplMap::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilImplMap::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilImplMap::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilImplMap::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilImplMap::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilImplMap::pool_size - 1};
    SgAsmCilImplMap* testPointer{reinterpret_cast<SgAsmCilImplMap*>(SgAsmCilImplMap::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilImplMap::pool_size * (SgAsmCilImplMap::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilImplMapStroageClass
 * from its counterpart of SgAsmCilImplMap. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilImplMap::initializeStorageClassArray(SgAsmCilImplMapStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilImplMap::pools.begin(); block != SgAsmCilImplMap::pools.end(); ++block) {
        SgAsmCilImplMap* pointer{reinterpret_cast<SgAsmCilImplMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilImplMap::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 173646 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilImplMapTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilImplMapTable::SgAsmCilImplMapTable(const SgAsmCilImplMapTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 173656 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilImplMapTable*
SgAsmCilImplMapTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilImplMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilImplMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilImplMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilImplMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMapTable)};
        unsigned long positionInPool{localIndex % SgAsmCilImplMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilImplMapTable::pool_size};
        returnPointer = &(((SgAsmCilImplMapTable*)(SgAsmCilImplMapTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilImplMapTable*
SgAsmCilImplMapTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilImplMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilImplMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilImplMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilImplMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilImplMapTable)};
        unsigned long positionInPool{localIndex % SgAsmCilImplMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilImplMapTable::pool_size};
        returnPointer = &(((SgAsmCilImplMapTable*)(SgAsmCilImplMapTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilImplMapTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilImplMapTable::pools.begin(); block != SgAsmCilImplMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilImplMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilImplMapTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilImplMapTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilImplMapTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilImplMapTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilImplMapTable::pools.begin(); block != SgAsmCilImplMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilImplMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilImplMapTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilImplMapTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilImplMapTable::clearMemoryPool() {
    SgAsmCilImplMapTable* tempPointer{nullptr};
    if (SgAsmCilImplMapTable::pools.empty() == false) {
        auto block{SgAsmCilImplMapTable::pools.begin()};
        SgAsmCilImplMapTable::next_node = reinterpret_cast<SgAsmCilImplMapTable*>(*block);
        for (; block != SgAsmCilImplMapTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilImplMapTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilImplMapTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilImplMapTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilImplMapTable::pool_size-1]);
        }
    }
}

void
SgAsmCilImplMapTable::deleteMemoryPool() {
    for (auto p: SgAsmCilImplMapTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilImplMapTable::next_node = nullptr;
    SgAsmCilImplMapTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilImplMapTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMapTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilImplMapTable)};
    for (size_t blockIndex = SgAsmCilImplMapTable::pools.size(); (blockIndex * SgAsmCilImplMapTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMapTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilImplMapTable) = %" PRIuPTR " SgAsmCilImplMapTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilImplMapTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilImplMapTable),SgAsmCilImplMapTable::pool_size);
        }
#endif

        SgAsmCilImplMapTable* pointer{static_cast<SgAsmCilImplMapTable*>(ROSE_MALLOC(SgAsmCilImplMapTable::pool_size * sizeof(SgAsmCilImplMapTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilImplMapTable::pool_size * sizeof(SgAsmCilImplMapTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilImplMapTable::pool_size * sizeof(SgAsmCilImplMapTable));
#endif
        SgAsmCilImplMapTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilImplMapTable::pool_size * sizeof(SgAsmCilImplMapTable), V_SgAsmCilImplMapTable));

        if (SgAsmCilImplMapTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilImplMapTable* blkptr{reinterpret_cast<SgAsmCilImplMapTable*>(SgAsmCilImplMapTable::pools[blockIndex-1])};
                blkptr[SgAsmCilImplMapTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilImplMapTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilImplMapTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilImplMapTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilImplMapTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilImplMapTable::pool_size - 1};
    SgAsmCilImplMapTable* testPointer{reinterpret_cast<SgAsmCilImplMapTable*>(SgAsmCilImplMapTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilImplMapTable::pool_size * (SgAsmCilImplMapTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilImplMapTableStroageClass
 * from its counterpart of SgAsmCilImplMapTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilImplMapTable::initializeStorageClassArray(SgAsmCilImplMapTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilImplMapTable::pools.begin(); block != SgAsmCilImplMapTable::pools.end(); ++block) {
        SgAsmCilImplMapTable* pointer{reinterpret_cast<SgAsmCilImplMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilImplMapTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 173874 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilInterfaceImpl that takes its corresponding StorageClass as parameter
 */
SgAsmCilInterfaceImpl::SgAsmCilInterfaceImpl(const SgAsmCilInterfaceImplStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 173884 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Class = storageSource.storageOf_Class;
    p_Interface = storageSource.storageOf_Interface;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilInterfaceImpl*
SgAsmCilInterfaceImpl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilInterfaceImpl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInterfaceImpl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInterfaceImpl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInterfaceImpl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImpl)};
        unsigned long positionInPool{localIndex % SgAsmCilInterfaceImpl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilInterfaceImpl::pool_size};
        returnPointer = &(((SgAsmCilInterfaceImpl*)(SgAsmCilInterfaceImpl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilInterfaceImpl*
SgAsmCilInterfaceImpl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilInterfaceImpl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInterfaceImpl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInterfaceImpl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInterfaceImpl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilInterfaceImpl)};
        unsigned long positionInPool{localIndex % SgAsmCilInterfaceImpl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilInterfaceImpl::pool_size};
        returnPointer = &(((SgAsmCilInterfaceImpl*)(SgAsmCilInterfaceImpl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilInterfaceImpl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilInterfaceImpl::pools.begin(); block != SgAsmCilInterfaceImpl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilInterfaceImpl*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInterfaceImpl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilInterfaceImpl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilInterfaceImpl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilInterfaceImpl* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilInterfaceImpl::pools.begin(); block != SgAsmCilInterfaceImpl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilInterfaceImpl*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInterfaceImpl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilInterfaceImpl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilInterfaceImpl::clearMemoryPool() {
    SgAsmCilInterfaceImpl* tempPointer{nullptr};
    if (SgAsmCilInterfaceImpl::pools.empty() == false) {
        auto block{SgAsmCilInterfaceImpl::pools.begin()};
        SgAsmCilInterfaceImpl::next_node = reinterpret_cast<SgAsmCilInterfaceImpl*>(*block);
        for (; block != SgAsmCilInterfaceImpl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilInterfaceImpl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilInterfaceImpl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilInterfaceImpl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilInterfaceImpl::pool_size-1]);
        }
    }
}

void
SgAsmCilInterfaceImpl::deleteMemoryPool() {
    for (auto p: SgAsmCilInterfaceImpl::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilInterfaceImpl::next_node = nullptr;
    SgAsmCilInterfaceImpl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilInterfaceImpl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImpl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInterfaceImpl)};
    for (size_t blockIndex = SgAsmCilInterfaceImpl::pools.size(); (blockIndex * SgAsmCilInterfaceImpl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImpl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInterfaceImpl) = %" PRIuPTR " SgAsmCilInterfaceImpl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImpl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInterfaceImpl),SgAsmCilInterfaceImpl::pool_size);
        }
#endif

        SgAsmCilInterfaceImpl* pointer{static_cast<SgAsmCilInterfaceImpl*>(ROSE_MALLOC(SgAsmCilInterfaceImpl::pool_size * sizeof(SgAsmCilInterfaceImpl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilInterfaceImpl::pool_size * sizeof(SgAsmCilInterfaceImpl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilInterfaceImpl::pool_size * sizeof(SgAsmCilInterfaceImpl));
#endif
        SgAsmCilInterfaceImpl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilInterfaceImpl::pool_size * sizeof(SgAsmCilInterfaceImpl), V_SgAsmCilInterfaceImpl));

        if (SgAsmCilInterfaceImpl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilInterfaceImpl* blkptr{reinterpret_cast<SgAsmCilInterfaceImpl*>(SgAsmCilInterfaceImpl::pools[blockIndex-1])};
                blkptr[SgAsmCilInterfaceImpl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilInterfaceImpl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilInterfaceImpl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilInterfaceImpl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilInterfaceImpl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilInterfaceImpl::pool_size - 1};
    SgAsmCilInterfaceImpl* testPointer{reinterpret_cast<SgAsmCilInterfaceImpl*>(SgAsmCilInterfaceImpl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilInterfaceImpl::pool_size * (SgAsmCilInterfaceImpl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilInterfaceImplStroageClass
 * from its counterpart of SgAsmCilInterfaceImpl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilInterfaceImpl::initializeStorageClassArray(SgAsmCilInterfaceImplStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilInterfaceImpl::pools.begin(); block != SgAsmCilInterfaceImpl::pools.end(); ++block) {
        SgAsmCilInterfaceImpl* pointer{reinterpret_cast<SgAsmCilInterfaceImpl*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInterfaceImpl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 174103 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilInterfaceImplTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilInterfaceImplTable::SgAsmCilInterfaceImplTable(const SgAsmCilInterfaceImplTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 174113 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilInterfaceImplTable*
SgAsmCilInterfaceImplTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilInterfaceImplTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInterfaceImplTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInterfaceImplTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInterfaceImplTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImplTable)};
        unsigned long positionInPool{localIndex % SgAsmCilInterfaceImplTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilInterfaceImplTable::pool_size};
        returnPointer = &(((SgAsmCilInterfaceImplTable*)(SgAsmCilInterfaceImplTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilInterfaceImplTable*
SgAsmCilInterfaceImplTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilInterfaceImplTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInterfaceImplTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInterfaceImplTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInterfaceImplTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilInterfaceImplTable)};
        unsigned long positionInPool{localIndex % SgAsmCilInterfaceImplTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilInterfaceImplTable::pool_size};
        returnPointer = &(((SgAsmCilInterfaceImplTable*)(SgAsmCilInterfaceImplTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilInterfaceImplTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilInterfaceImplTable::pools.begin(); block != SgAsmCilInterfaceImplTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilInterfaceImplTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInterfaceImplTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilInterfaceImplTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilInterfaceImplTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilInterfaceImplTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilInterfaceImplTable::pools.begin(); block != SgAsmCilInterfaceImplTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilInterfaceImplTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInterfaceImplTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilInterfaceImplTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilInterfaceImplTable::clearMemoryPool() {
    SgAsmCilInterfaceImplTable* tempPointer{nullptr};
    if (SgAsmCilInterfaceImplTable::pools.empty() == false) {
        auto block{SgAsmCilInterfaceImplTable::pools.begin()};
        SgAsmCilInterfaceImplTable::next_node = reinterpret_cast<SgAsmCilInterfaceImplTable*>(*block);
        for (; block != SgAsmCilInterfaceImplTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilInterfaceImplTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilInterfaceImplTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilInterfaceImplTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilInterfaceImplTable::pool_size-1]);
        }
    }
}

void
SgAsmCilInterfaceImplTable::deleteMemoryPool() {
    for (auto p: SgAsmCilInterfaceImplTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilInterfaceImplTable::next_node = nullptr;
    SgAsmCilInterfaceImplTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilInterfaceImplTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImplTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInterfaceImplTable)};
    for (size_t blockIndex = SgAsmCilInterfaceImplTable::pools.size(); (blockIndex * SgAsmCilInterfaceImplTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImplTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInterfaceImplTable) = %" PRIuPTR " SgAsmCilInterfaceImplTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInterfaceImplTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInterfaceImplTable),SgAsmCilInterfaceImplTable::pool_size);
        }
#endif

        SgAsmCilInterfaceImplTable* pointer{static_cast<SgAsmCilInterfaceImplTable*>(ROSE_MALLOC(SgAsmCilInterfaceImplTable::pool_size * sizeof(SgAsmCilInterfaceImplTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilInterfaceImplTable::pool_size * sizeof(SgAsmCilInterfaceImplTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilInterfaceImplTable::pool_size * sizeof(SgAsmCilInterfaceImplTable));
#endif
        SgAsmCilInterfaceImplTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilInterfaceImplTable::pool_size * sizeof(SgAsmCilInterfaceImplTable), V_SgAsmCilInterfaceImplTable));

        if (SgAsmCilInterfaceImplTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilInterfaceImplTable* blkptr{reinterpret_cast<SgAsmCilInterfaceImplTable*>(SgAsmCilInterfaceImplTable::pools[blockIndex-1])};
                blkptr[SgAsmCilInterfaceImplTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilInterfaceImplTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilInterfaceImplTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilInterfaceImplTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilInterfaceImplTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilInterfaceImplTable::pool_size - 1};
    SgAsmCilInterfaceImplTable* testPointer{reinterpret_cast<SgAsmCilInterfaceImplTable*>(SgAsmCilInterfaceImplTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilInterfaceImplTable::pool_size * (SgAsmCilInterfaceImplTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilInterfaceImplTableStroageClass
 * from its counterpart of SgAsmCilInterfaceImplTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilInterfaceImplTable::initializeStorageClassArray(SgAsmCilInterfaceImplTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilInterfaceImplTable::pools.begin(); block != SgAsmCilInterfaceImplTable::pools.end(); ++block) {
        SgAsmCilInterfaceImplTable* pointer{reinterpret_cast<SgAsmCilInterfaceImplTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInterfaceImplTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 174331 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilManifestResource that takes its corresponding StorageClass as parameter
 */
SgAsmCilManifestResource::SgAsmCilManifestResource(const SgAsmCilManifestResourceStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 174341 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Offset = storageSource.storageOf_Offset;
    p_Flags = storageSource.storageOf_Flags;
    p_Name = storageSource.storageOf_Name;
    p_Implementation = storageSource.storageOf_Implementation;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilManifestResource*
SgAsmCilManifestResource::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilManifestResource* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilManifestResource)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilManifestResource + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilManifestResource)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResource)};
        unsigned long positionInPool{localIndex % SgAsmCilManifestResource::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilManifestResource::pool_size};
        returnPointer = &(((SgAsmCilManifestResource*)(SgAsmCilManifestResource::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilManifestResource*
SgAsmCilManifestResource::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilManifestResource* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilManifestResource)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilManifestResource + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilManifestResource)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilManifestResource)};
        unsigned long positionInPool{localIndex % SgAsmCilManifestResource::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilManifestResource::pool_size};
        returnPointer = &(((SgAsmCilManifestResource*)(SgAsmCilManifestResource::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilManifestResource::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilManifestResource::pools.begin(); block != SgAsmCilManifestResource::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilManifestResource*>(*block)};
        for (unsigned i = 0; i < SgAsmCilManifestResource::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilManifestResource*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilManifestResource::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilManifestResource* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilManifestResource::pools.begin(); block != SgAsmCilManifestResource::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilManifestResource*>(*block)};
        for (unsigned i = 0; i < SgAsmCilManifestResource::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilManifestResource::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilManifestResource::clearMemoryPool() {
    SgAsmCilManifestResource* tempPointer{nullptr};
    if (SgAsmCilManifestResource::pools.empty() == false) {
        auto block{SgAsmCilManifestResource::pools.begin()};
        SgAsmCilManifestResource::next_node = reinterpret_cast<SgAsmCilManifestResource*>(*block);
        for (; block != SgAsmCilManifestResource::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilManifestResource*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilManifestResource::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilManifestResource::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilManifestResource::pool_size-1]);
        }
    }
}

void
SgAsmCilManifestResource::deleteMemoryPool() {
    for (auto p: SgAsmCilManifestResource::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilManifestResource::next_node = nullptr;
    SgAsmCilManifestResource::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilManifestResource::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResource) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilManifestResource)};
    for (size_t blockIndex = SgAsmCilManifestResource::pools.size(); (blockIndex * SgAsmCilManifestResource::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResource) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilManifestResource) = %" PRIuPTR " SgAsmCilManifestResource::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResource),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilManifestResource),SgAsmCilManifestResource::pool_size);
        }
#endif

        SgAsmCilManifestResource* pointer{static_cast<SgAsmCilManifestResource*>(ROSE_MALLOC(SgAsmCilManifestResource::pool_size * sizeof(SgAsmCilManifestResource)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilManifestResource::pool_size * sizeof(SgAsmCilManifestResource));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilManifestResource::pool_size * sizeof(SgAsmCilManifestResource));
#endif
        SgAsmCilManifestResource::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilManifestResource::pool_size * sizeof(SgAsmCilManifestResource), V_SgAsmCilManifestResource));

        if (SgAsmCilManifestResource::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilManifestResource* blkptr{reinterpret_cast<SgAsmCilManifestResource*>(SgAsmCilManifestResource::pools[blockIndex-1])};
                blkptr[SgAsmCilManifestResource::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilManifestResource::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilManifestResource::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilManifestResource::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilManifestResource::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilManifestResource::pool_size - 1};
    SgAsmCilManifestResource* testPointer{reinterpret_cast<SgAsmCilManifestResource*>(SgAsmCilManifestResource::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilManifestResource::pool_size * (SgAsmCilManifestResource::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilManifestResourceStroageClass
 * from its counterpart of SgAsmCilManifestResource. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilManifestResource::initializeStorageClassArray(SgAsmCilManifestResourceStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilManifestResource::pools.begin(); block != SgAsmCilManifestResource::pools.end(); ++block) {
        SgAsmCilManifestResource* pointer{reinterpret_cast<SgAsmCilManifestResource*>(*block)};
        for (unsigned i = 0; i < SgAsmCilManifestResource::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 174562 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilManifestResourceTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilManifestResourceTable::SgAsmCilManifestResourceTable(const SgAsmCilManifestResourceTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 174572 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilManifestResourceTable*
SgAsmCilManifestResourceTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilManifestResourceTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilManifestResourceTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilManifestResourceTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilManifestResourceTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResourceTable)};
        unsigned long positionInPool{localIndex % SgAsmCilManifestResourceTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilManifestResourceTable::pool_size};
        returnPointer = &(((SgAsmCilManifestResourceTable*)(SgAsmCilManifestResourceTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilManifestResourceTable*
SgAsmCilManifestResourceTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilManifestResourceTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilManifestResourceTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilManifestResourceTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilManifestResourceTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilManifestResourceTable)};
        unsigned long positionInPool{localIndex % SgAsmCilManifestResourceTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilManifestResourceTable::pool_size};
        returnPointer = &(((SgAsmCilManifestResourceTable*)(SgAsmCilManifestResourceTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilManifestResourceTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilManifestResourceTable::pools.begin(); block != SgAsmCilManifestResourceTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilManifestResourceTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilManifestResourceTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilManifestResourceTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilManifestResourceTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilManifestResourceTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilManifestResourceTable::pools.begin(); block != SgAsmCilManifestResourceTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilManifestResourceTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilManifestResourceTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilManifestResourceTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilManifestResourceTable::clearMemoryPool() {
    SgAsmCilManifestResourceTable* tempPointer{nullptr};
    if (SgAsmCilManifestResourceTable::pools.empty() == false) {
        auto block{SgAsmCilManifestResourceTable::pools.begin()};
        SgAsmCilManifestResourceTable::next_node = reinterpret_cast<SgAsmCilManifestResourceTable*>(*block);
        for (; block != SgAsmCilManifestResourceTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilManifestResourceTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilManifestResourceTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilManifestResourceTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilManifestResourceTable::pool_size-1]);
        }
    }
}

void
SgAsmCilManifestResourceTable::deleteMemoryPool() {
    for (auto p: SgAsmCilManifestResourceTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilManifestResourceTable::next_node = nullptr;
    SgAsmCilManifestResourceTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilManifestResourceTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResourceTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilManifestResourceTable)};
    for (size_t blockIndex = SgAsmCilManifestResourceTable::pools.size(); (blockIndex * SgAsmCilManifestResourceTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResourceTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilManifestResourceTable) = %" PRIuPTR " SgAsmCilManifestResourceTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilManifestResourceTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilManifestResourceTable),SgAsmCilManifestResourceTable::pool_size);
        }
#endif

        SgAsmCilManifestResourceTable* pointer{static_cast<SgAsmCilManifestResourceTable*>(ROSE_MALLOC(SgAsmCilManifestResourceTable::pool_size * sizeof(SgAsmCilManifestResourceTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilManifestResourceTable::pool_size * sizeof(SgAsmCilManifestResourceTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilManifestResourceTable::pool_size * sizeof(SgAsmCilManifestResourceTable));
#endif
        SgAsmCilManifestResourceTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilManifestResourceTable::pool_size * sizeof(SgAsmCilManifestResourceTable), V_SgAsmCilManifestResourceTable));

        if (SgAsmCilManifestResourceTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilManifestResourceTable* blkptr{reinterpret_cast<SgAsmCilManifestResourceTable*>(SgAsmCilManifestResourceTable::pools[blockIndex-1])};
                blkptr[SgAsmCilManifestResourceTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilManifestResourceTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilManifestResourceTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilManifestResourceTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilManifestResourceTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilManifestResourceTable::pool_size - 1};
    SgAsmCilManifestResourceTable* testPointer{reinterpret_cast<SgAsmCilManifestResourceTable*>(SgAsmCilManifestResourceTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilManifestResourceTable::pool_size * (SgAsmCilManifestResourceTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilManifestResourceTableStroageClass
 * from its counterpart of SgAsmCilManifestResourceTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilManifestResourceTable::initializeStorageClassArray(SgAsmCilManifestResourceTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilManifestResourceTable::pools.begin(); block != SgAsmCilManifestResourceTable::pools.end(); ++block) {
        SgAsmCilManifestResourceTable* pointer{reinterpret_cast<SgAsmCilManifestResourceTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilManifestResourceTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 174790 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMemberRef that takes its corresponding StorageClass as parameter
 */
SgAsmCilMemberRef::SgAsmCilMemberRef(const SgAsmCilMemberRefStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 174800 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Class = storageSource.storageOf_Class;
    p_Name = storageSource.storageOf_Name;
    p_Signature = storageSource.storageOf_Signature;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMemberRef*
SgAsmCilMemberRef::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMemberRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMemberRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMemberRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMemberRef)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRef)};
        unsigned long positionInPool{localIndex % SgAsmCilMemberRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMemberRef::pool_size};
        returnPointer = &(((SgAsmCilMemberRef*)(SgAsmCilMemberRef::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMemberRef*
SgAsmCilMemberRef::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMemberRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMemberRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMemberRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMemberRef)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMemberRef)};
        unsigned long positionInPool{localIndex % SgAsmCilMemberRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMemberRef::pool_size};
        returnPointer = &(((SgAsmCilMemberRef*)(SgAsmCilMemberRef::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMemberRef::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMemberRef::pools.begin(); block != SgAsmCilMemberRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMemberRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMemberRef::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMemberRef*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMemberRef::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMemberRef* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMemberRef::pools.begin(); block != SgAsmCilMemberRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMemberRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMemberRef::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMemberRef::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMemberRef::clearMemoryPool() {
    SgAsmCilMemberRef* tempPointer{nullptr};
    if (SgAsmCilMemberRef::pools.empty() == false) {
        auto block{SgAsmCilMemberRef::pools.begin()};
        SgAsmCilMemberRef::next_node = reinterpret_cast<SgAsmCilMemberRef*>(*block);
        for (; block != SgAsmCilMemberRef::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMemberRef*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMemberRef::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMemberRef::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMemberRef::pool_size-1]);
        }
    }
}

void
SgAsmCilMemberRef::deleteMemoryPool() {
    for (auto p: SgAsmCilMemberRef::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMemberRef::next_node = nullptr;
    SgAsmCilMemberRef::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMemberRef::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRef) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMemberRef)};
    for (size_t blockIndex = SgAsmCilMemberRef::pools.size(); (blockIndex * SgAsmCilMemberRef::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRef) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMemberRef) = %" PRIuPTR " SgAsmCilMemberRef::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRef),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMemberRef),SgAsmCilMemberRef::pool_size);
        }
#endif

        SgAsmCilMemberRef* pointer{static_cast<SgAsmCilMemberRef*>(ROSE_MALLOC(SgAsmCilMemberRef::pool_size * sizeof(SgAsmCilMemberRef)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMemberRef::pool_size * sizeof(SgAsmCilMemberRef));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMemberRef::pool_size * sizeof(SgAsmCilMemberRef));
#endif
        SgAsmCilMemberRef::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMemberRef::pool_size * sizeof(SgAsmCilMemberRef), V_SgAsmCilMemberRef));

        if (SgAsmCilMemberRef::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMemberRef* blkptr{reinterpret_cast<SgAsmCilMemberRef*>(SgAsmCilMemberRef::pools[blockIndex-1])};
                blkptr[SgAsmCilMemberRef::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMemberRef::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMemberRef::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMemberRef::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMemberRef::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMemberRef::pool_size - 1};
    SgAsmCilMemberRef* testPointer{reinterpret_cast<SgAsmCilMemberRef*>(SgAsmCilMemberRef::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMemberRef::pool_size * (SgAsmCilMemberRef::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMemberRefStroageClass
 * from its counterpart of SgAsmCilMemberRef. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMemberRef::initializeStorageClassArray(SgAsmCilMemberRefStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMemberRef::pools.begin(); block != SgAsmCilMemberRef::pools.end(); ++block) {
        SgAsmCilMemberRef* pointer{reinterpret_cast<SgAsmCilMemberRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMemberRef::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 175020 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMemberRefTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilMemberRefTable::SgAsmCilMemberRefTable(const SgAsmCilMemberRefTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 175030 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMemberRefTable*
SgAsmCilMemberRefTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMemberRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMemberRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMemberRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMemberRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMemberRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMemberRefTable::pool_size};
        returnPointer = &(((SgAsmCilMemberRefTable*)(SgAsmCilMemberRefTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMemberRefTable*
SgAsmCilMemberRefTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMemberRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMemberRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMemberRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMemberRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMemberRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMemberRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMemberRefTable::pool_size};
        returnPointer = &(((SgAsmCilMemberRefTable*)(SgAsmCilMemberRefTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMemberRefTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMemberRefTable::pools.begin(); block != SgAsmCilMemberRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMemberRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMemberRefTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMemberRefTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMemberRefTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMemberRefTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMemberRefTable::pools.begin(); block != SgAsmCilMemberRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMemberRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMemberRefTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMemberRefTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMemberRefTable::clearMemoryPool() {
    SgAsmCilMemberRefTable* tempPointer{nullptr};
    if (SgAsmCilMemberRefTable::pools.empty() == false) {
        auto block{SgAsmCilMemberRefTable::pools.begin()};
        SgAsmCilMemberRefTable::next_node = reinterpret_cast<SgAsmCilMemberRefTable*>(*block);
        for (; block != SgAsmCilMemberRefTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMemberRefTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMemberRefTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMemberRefTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMemberRefTable::pool_size-1]);
        }
    }
}

void
SgAsmCilMemberRefTable::deleteMemoryPool() {
    for (auto p: SgAsmCilMemberRefTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMemberRefTable::next_node = nullptr;
    SgAsmCilMemberRefTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMemberRefTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRefTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMemberRefTable)};
    for (size_t blockIndex = SgAsmCilMemberRefTable::pools.size(); (blockIndex * SgAsmCilMemberRefTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRefTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMemberRefTable) = %" PRIuPTR " SgAsmCilMemberRefTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMemberRefTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMemberRefTable),SgAsmCilMemberRefTable::pool_size);
        }
#endif

        SgAsmCilMemberRefTable* pointer{static_cast<SgAsmCilMemberRefTable*>(ROSE_MALLOC(SgAsmCilMemberRefTable::pool_size * sizeof(SgAsmCilMemberRefTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMemberRefTable::pool_size * sizeof(SgAsmCilMemberRefTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMemberRefTable::pool_size * sizeof(SgAsmCilMemberRefTable));
#endif
        SgAsmCilMemberRefTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMemberRefTable::pool_size * sizeof(SgAsmCilMemberRefTable), V_SgAsmCilMemberRefTable));

        if (SgAsmCilMemberRefTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMemberRefTable* blkptr{reinterpret_cast<SgAsmCilMemberRefTable*>(SgAsmCilMemberRefTable::pools[blockIndex-1])};
                blkptr[SgAsmCilMemberRefTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMemberRefTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMemberRefTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMemberRefTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMemberRefTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMemberRefTable::pool_size - 1};
    SgAsmCilMemberRefTable* testPointer{reinterpret_cast<SgAsmCilMemberRefTable*>(SgAsmCilMemberRefTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMemberRefTable::pool_size * (SgAsmCilMemberRefTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMemberRefTableStroageClass
 * from its counterpart of SgAsmCilMemberRefTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMemberRefTable::initializeStorageClassArray(SgAsmCilMemberRefTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMemberRefTable::pools.begin(); block != SgAsmCilMemberRefTable::pools.end(); ++block) {
        SgAsmCilMemberRefTable* pointer{reinterpret_cast<SgAsmCilMemberRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMemberRefTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 175248 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodDef that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodDef::SgAsmCilMethodDef(const SgAsmCilMethodDefStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 175258 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_RVA = storageSource.storageOf_RVA;
    p_ImplFlags = storageSource.storageOf_ImplFlags;
    p_Flags = storageSource.storageOf_Flags;
    p_Name = storageSource.storageOf_Name;
    p_Signature = storageSource.storageOf_Signature;
    p_ParamList = storageSource.storageOf_ParamList;
     p_body = (SgAsmBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_initLocals = storageSource.storageOf_initLocals;
    p_stackSize = storageSource.storageOf_stackSize;
    p_hasMoreSections = storageSource.storageOf_hasMoreSections;
    p_localVarSigTok = storageSource.storageOf_localVarSigTok;
    p_methodData = storageSource.storageOf_methodData.rebuildDataStoredInEasyStorageClass();
    p_bodyState = storageSource.storageOf_bodyState;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodDef*
SgAsmCilMethodDef::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodDef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodDef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodDef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodDef)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDef)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodDef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodDef::pool_size};
        returnPointer = &(((SgAsmCilMethodDef*)(SgAsmCilMethodDef::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodDef*
SgAsmCilMethodDef::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodDef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodDef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodDef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodDef)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodDef)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodDef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodDef::pool_size};
        returnPointer = &(((SgAsmCilMethodDef*)(SgAsmCilMethodDef::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodDef::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodDef::pools.begin(); block != SgAsmCilMethodDef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodDef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodDef::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodDef*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodDef::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodDef* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodDef::pools.begin(); block != SgAsmCilMethodDef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodDef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodDef::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodDef::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodDef::clearMemoryPool() {
    SgAsmCilMethodDef* tempPointer{nullptr};
    if (SgAsmCilMethodDef::pools.empty() == false) {
        auto block{SgAsmCilMethodDef::pools.begin()};
        SgAsmCilMethodDef::next_node = reinterpret_cast<SgAsmCilMethodDef*>(*block);
        for (; block != SgAsmCilMethodDef::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodDef*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodDef::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodDef::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodDef::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodDef::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodDef::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodDef::next_node = nullptr;
    SgAsmCilMethodDef::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodDef::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDef) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodDef)};
    for (size_t blockIndex = SgAsmCilMethodDef::pools.size(); (blockIndex * SgAsmCilMethodDef::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDef) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodDef) = %" PRIuPTR " SgAsmCilMethodDef::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDef),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodDef),SgAsmCilMethodDef::pool_size);
        }
#endif

        SgAsmCilMethodDef* pointer{static_cast<SgAsmCilMethodDef*>(ROSE_MALLOC(SgAsmCilMethodDef::pool_size * sizeof(SgAsmCilMethodDef)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodDef::pool_size * sizeof(SgAsmCilMethodDef));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodDef::pool_size * sizeof(SgAsmCilMethodDef));
#endif
        SgAsmCilMethodDef::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodDef::pool_size * sizeof(SgAsmCilMethodDef), V_SgAsmCilMethodDef));

        if (SgAsmCilMethodDef::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodDef* blkptr{reinterpret_cast<SgAsmCilMethodDef*>(SgAsmCilMethodDef::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodDef::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodDef::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodDef::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodDef::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodDef::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodDef::pool_size - 1};
    SgAsmCilMethodDef* testPointer{reinterpret_cast<SgAsmCilMethodDef*>(SgAsmCilMethodDef::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodDef::pool_size * (SgAsmCilMethodDef::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodDefStroageClass
 * from its counterpart of SgAsmCilMethodDef. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodDef::initializeStorageClassArray(SgAsmCilMethodDefStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodDef::pools.begin(); block != SgAsmCilMethodDef::pools.end(); ++block) {
        SgAsmCilMethodDef* pointer{reinterpret_cast<SgAsmCilMethodDef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodDef::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 175488 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodDefTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodDefTable::SgAsmCilMethodDefTable(const SgAsmCilMethodDefTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 175498 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodDefTable*
SgAsmCilMethodDefTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodDefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodDefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodDefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodDefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodDefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodDefTable::pool_size};
        returnPointer = &(((SgAsmCilMethodDefTable*)(SgAsmCilMethodDefTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodDefTable*
SgAsmCilMethodDefTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodDefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodDefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodDefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodDefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodDefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodDefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodDefTable::pool_size};
        returnPointer = &(((SgAsmCilMethodDefTable*)(SgAsmCilMethodDefTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodDefTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodDefTable::pools.begin(); block != SgAsmCilMethodDefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodDefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodDefTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodDefTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodDefTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodDefTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodDefTable::pools.begin(); block != SgAsmCilMethodDefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodDefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodDefTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodDefTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodDefTable::clearMemoryPool() {
    SgAsmCilMethodDefTable* tempPointer{nullptr};
    if (SgAsmCilMethodDefTable::pools.empty() == false) {
        auto block{SgAsmCilMethodDefTable::pools.begin()};
        SgAsmCilMethodDefTable::next_node = reinterpret_cast<SgAsmCilMethodDefTable*>(*block);
        for (; block != SgAsmCilMethodDefTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodDefTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodDefTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodDefTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodDefTable::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodDefTable::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodDefTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodDefTable::next_node = nullptr;
    SgAsmCilMethodDefTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodDefTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDefTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodDefTable)};
    for (size_t blockIndex = SgAsmCilMethodDefTable::pools.size(); (blockIndex * SgAsmCilMethodDefTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDefTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodDefTable) = %" PRIuPTR " SgAsmCilMethodDefTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodDefTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodDefTable),SgAsmCilMethodDefTable::pool_size);
        }
#endif

        SgAsmCilMethodDefTable* pointer{static_cast<SgAsmCilMethodDefTable*>(ROSE_MALLOC(SgAsmCilMethodDefTable::pool_size * sizeof(SgAsmCilMethodDefTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodDefTable::pool_size * sizeof(SgAsmCilMethodDefTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodDefTable::pool_size * sizeof(SgAsmCilMethodDefTable));
#endif
        SgAsmCilMethodDefTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodDefTable::pool_size * sizeof(SgAsmCilMethodDefTable), V_SgAsmCilMethodDefTable));

        if (SgAsmCilMethodDefTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodDefTable* blkptr{reinterpret_cast<SgAsmCilMethodDefTable*>(SgAsmCilMethodDefTable::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodDefTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodDefTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodDefTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodDefTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodDefTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodDefTable::pool_size - 1};
    SgAsmCilMethodDefTable* testPointer{reinterpret_cast<SgAsmCilMethodDefTable*>(SgAsmCilMethodDefTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodDefTable::pool_size * (SgAsmCilMethodDefTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodDefTableStroageClass
 * from its counterpart of SgAsmCilMethodDefTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodDefTable::initializeStorageClassArray(SgAsmCilMethodDefTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodDefTable::pools.begin(); block != SgAsmCilMethodDefTable::pools.end(); ++block) {
        SgAsmCilMethodDefTable* pointer{reinterpret_cast<SgAsmCilMethodDefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodDefTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 175716 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodImpl that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodImpl::SgAsmCilMethodImpl(const SgAsmCilMethodImplStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 175726 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Class = storageSource.storageOf_Class;
    p_MethodBody = storageSource.storageOf_MethodBody;
    p_MethodDeclaration = storageSource.storageOf_MethodDeclaration;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodImpl*
SgAsmCilMethodImpl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodImpl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodImpl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodImpl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodImpl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImpl)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodImpl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodImpl::pool_size};
        returnPointer = &(((SgAsmCilMethodImpl*)(SgAsmCilMethodImpl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodImpl*
SgAsmCilMethodImpl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodImpl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodImpl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodImpl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodImpl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodImpl)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodImpl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodImpl::pool_size};
        returnPointer = &(((SgAsmCilMethodImpl*)(SgAsmCilMethodImpl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodImpl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodImpl::pools.begin(); block != SgAsmCilMethodImpl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodImpl*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodImpl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodImpl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodImpl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodImpl* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodImpl::pools.begin(); block != SgAsmCilMethodImpl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodImpl*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodImpl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodImpl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodImpl::clearMemoryPool() {
    SgAsmCilMethodImpl* tempPointer{nullptr};
    if (SgAsmCilMethodImpl::pools.empty() == false) {
        auto block{SgAsmCilMethodImpl::pools.begin()};
        SgAsmCilMethodImpl::next_node = reinterpret_cast<SgAsmCilMethodImpl*>(*block);
        for (; block != SgAsmCilMethodImpl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodImpl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodImpl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodImpl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodImpl::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodImpl::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodImpl::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodImpl::next_node = nullptr;
    SgAsmCilMethodImpl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodImpl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImpl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodImpl)};
    for (size_t blockIndex = SgAsmCilMethodImpl::pools.size(); (blockIndex * SgAsmCilMethodImpl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImpl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodImpl) = %" PRIuPTR " SgAsmCilMethodImpl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImpl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodImpl),SgAsmCilMethodImpl::pool_size);
        }
#endif

        SgAsmCilMethodImpl* pointer{static_cast<SgAsmCilMethodImpl*>(ROSE_MALLOC(SgAsmCilMethodImpl::pool_size * sizeof(SgAsmCilMethodImpl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodImpl::pool_size * sizeof(SgAsmCilMethodImpl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodImpl::pool_size * sizeof(SgAsmCilMethodImpl));
#endif
        SgAsmCilMethodImpl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodImpl::pool_size * sizeof(SgAsmCilMethodImpl), V_SgAsmCilMethodImpl));

        if (SgAsmCilMethodImpl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodImpl* blkptr{reinterpret_cast<SgAsmCilMethodImpl*>(SgAsmCilMethodImpl::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodImpl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodImpl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodImpl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodImpl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodImpl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodImpl::pool_size - 1};
    SgAsmCilMethodImpl* testPointer{reinterpret_cast<SgAsmCilMethodImpl*>(SgAsmCilMethodImpl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodImpl::pool_size * (SgAsmCilMethodImpl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodImplStroageClass
 * from its counterpart of SgAsmCilMethodImpl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodImpl::initializeStorageClassArray(SgAsmCilMethodImplStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodImpl::pools.begin(); block != SgAsmCilMethodImpl::pools.end(); ++block) {
        SgAsmCilMethodImpl* pointer{reinterpret_cast<SgAsmCilMethodImpl*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodImpl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 175946 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodImplTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodImplTable::SgAsmCilMethodImplTable(const SgAsmCilMethodImplTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 175956 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodImplTable*
SgAsmCilMethodImplTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodImplTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodImplTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodImplTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodImplTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImplTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodImplTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodImplTable::pool_size};
        returnPointer = &(((SgAsmCilMethodImplTable*)(SgAsmCilMethodImplTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodImplTable*
SgAsmCilMethodImplTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodImplTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodImplTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodImplTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodImplTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodImplTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodImplTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodImplTable::pool_size};
        returnPointer = &(((SgAsmCilMethodImplTable*)(SgAsmCilMethodImplTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodImplTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodImplTable::pools.begin(); block != SgAsmCilMethodImplTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodImplTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodImplTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodImplTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodImplTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodImplTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodImplTable::pools.begin(); block != SgAsmCilMethodImplTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodImplTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodImplTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodImplTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodImplTable::clearMemoryPool() {
    SgAsmCilMethodImplTable* tempPointer{nullptr};
    if (SgAsmCilMethodImplTable::pools.empty() == false) {
        auto block{SgAsmCilMethodImplTable::pools.begin()};
        SgAsmCilMethodImplTable::next_node = reinterpret_cast<SgAsmCilMethodImplTable*>(*block);
        for (; block != SgAsmCilMethodImplTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodImplTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodImplTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodImplTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodImplTable::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodImplTable::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodImplTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodImplTable::next_node = nullptr;
    SgAsmCilMethodImplTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodImplTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImplTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodImplTable)};
    for (size_t blockIndex = SgAsmCilMethodImplTable::pools.size(); (blockIndex * SgAsmCilMethodImplTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImplTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodImplTable) = %" PRIuPTR " SgAsmCilMethodImplTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodImplTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodImplTable),SgAsmCilMethodImplTable::pool_size);
        }
#endif

        SgAsmCilMethodImplTable* pointer{static_cast<SgAsmCilMethodImplTable*>(ROSE_MALLOC(SgAsmCilMethodImplTable::pool_size * sizeof(SgAsmCilMethodImplTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodImplTable::pool_size * sizeof(SgAsmCilMethodImplTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodImplTable::pool_size * sizeof(SgAsmCilMethodImplTable));
#endif
        SgAsmCilMethodImplTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodImplTable::pool_size * sizeof(SgAsmCilMethodImplTable), V_SgAsmCilMethodImplTable));

        if (SgAsmCilMethodImplTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodImplTable* blkptr{reinterpret_cast<SgAsmCilMethodImplTable*>(SgAsmCilMethodImplTable::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodImplTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodImplTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodImplTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodImplTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodImplTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodImplTable::pool_size - 1};
    SgAsmCilMethodImplTable* testPointer{reinterpret_cast<SgAsmCilMethodImplTable*>(SgAsmCilMethodImplTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodImplTable::pool_size * (SgAsmCilMethodImplTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodImplTableStroageClass
 * from its counterpart of SgAsmCilMethodImplTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodImplTable::initializeStorageClassArray(SgAsmCilMethodImplTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodImplTable::pools.begin(); block != SgAsmCilMethodImplTable::pools.end(); ++block) {
        SgAsmCilMethodImplTable* pointer{reinterpret_cast<SgAsmCilMethodImplTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodImplTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 176174 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodSemantics that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodSemantics::SgAsmCilMethodSemantics(const SgAsmCilMethodSemanticsStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 176184 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Semantics = storageSource.storageOf_Semantics;
    p_Method = storageSource.storageOf_Method;
    p_Association = storageSource.storageOf_Association;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodSemantics*
SgAsmCilMethodSemantics::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodSemantics* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSemantics)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSemantics + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSemantics)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemantics)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSemantics::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSemantics::pool_size};
        returnPointer = &(((SgAsmCilMethodSemantics*)(SgAsmCilMethodSemantics::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodSemantics*
SgAsmCilMethodSemantics::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodSemantics* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSemantics)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSemantics + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSemantics)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodSemantics)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSemantics::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSemantics::pool_size};
        returnPointer = &(((SgAsmCilMethodSemantics*)(SgAsmCilMethodSemantics::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodSemantics::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodSemantics::pools.begin(); block != SgAsmCilMethodSemantics::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSemantics*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSemantics::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodSemantics*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodSemantics::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodSemantics* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodSemantics::pools.begin(); block != SgAsmCilMethodSemantics::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSemantics*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSemantics::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodSemantics::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodSemantics::clearMemoryPool() {
    SgAsmCilMethodSemantics* tempPointer{nullptr};
    if (SgAsmCilMethodSemantics::pools.empty() == false) {
        auto block{SgAsmCilMethodSemantics::pools.begin()};
        SgAsmCilMethodSemantics::next_node = reinterpret_cast<SgAsmCilMethodSemantics*>(*block);
        for (; block != SgAsmCilMethodSemantics::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodSemantics*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodSemantics::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodSemantics::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodSemantics::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodSemantics::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodSemantics::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodSemantics::next_node = nullptr;
    SgAsmCilMethodSemantics::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodSemantics::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemantics) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSemantics)};
    for (size_t blockIndex = SgAsmCilMethodSemantics::pools.size(); (blockIndex * SgAsmCilMethodSemantics::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemantics) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSemantics) = %" PRIuPTR " SgAsmCilMethodSemantics::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemantics),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSemantics),SgAsmCilMethodSemantics::pool_size);
        }
#endif

        SgAsmCilMethodSemantics* pointer{static_cast<SgAsmCilMethodSemantics*>(ROSE_MALLOC(SgAsmCilMethodSemantics::pool_size * sizeof(SgAsmCilMethodSemantics)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodSemantics::pool_size * sizeof(SgAsmCilMethodSemantics));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodSemantics::pool_size * sizeof(SgAsmCilMethodSemantics));
#endif
        SgAsmCilMethodSemantics::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodSemantics::pool_size * sizeof(SgAsmCilMethodSemantics), V_SgAsmCilMethodSemantics));

        if (SgAsmCilMethodSemantics::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodSemantics* blkptr{reinterpret_cast<SgAsmCilMethodSemantics*>(SgAsmCilMethodSemantics::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodSemantics::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodSemantics::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodSemantics::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodSemantics::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodSemantics::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodSemantics::pool_size - 1};
    SgAsmCilMethodSemantics* testPointer{reinterpret_cast<SgAsmCilMethodSemantics*>(SgAsmCilMethodSemantics::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodSemantics::pool_size * (SgAsmCilMethodSemantics::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodSemanticsStroageClass
 * from its counterpart of SgAsmCilMethodSemantics. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodSemantics::initializeStorageClassArray(SgAsmCilMethodSemanticsStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodSemantics::pools.begin(); block != SgAsmCilMethodSemantics::pools.end(); ++block) {
        SgAsmCilMethodSemantics* pointer{reinterpret_cast<SgAsmCilMethodSemantics*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSemantics::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 176404 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodSemanticsTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodSemanticsTable::SgAsmCilMethodSemanticsTable(const SgAsmCilMethodSemanticsTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 176414 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodSemanticsTable*
SgAsmCilMethodSemanticsTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodSemanticsTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSemanticsTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSemanticsTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSemanticsTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemanticsTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSemanticsTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSemanticsTable::pool_size};
        returnPointer = &(((SgAsmCilMethodSemanticsTable*)(SgAsmCilMethodSemanticsTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodSemanticsTable*
SgAsmCilMethodSemanticsTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodSemanticsTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSemanticsTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSemanticsTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSemanticsTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodSemanticsTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSemanticsTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSemanticsTable::pool_size};
        returnPointer = &(((SgAsmCilMethodSemanticsTable*)(SgAsmCilMethodSemanticsTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodSemanticsTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodSemanticsTable::pools.begin(); block != SgAsmCilMethodSemanticsTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSemanticsTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSemanticsTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodSemanticsTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodSemanticsTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodSemanticsTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodSemanticsTable::pools.begin(); block != SgAsmCilMethodSemanticsTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSemanticsTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSemanticsTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodSemanticsTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodSemanticsTable::clearMemoryPool() {
    SgAsmCilMethodSemanticsTable* tempPointer{nullptr};
    if (SgAsmCilMethodSemanticsTable::pools.empty() == false) {
        auto block{SgAsmCilMethodSemanticsTable::pools.begin()};
        SgAsmCilMethodSemanticsTable::next_node = reinterpret_cast<SgAsmCilMethodSemanticsTable*>(*block);
        for (; block != SgAsmCilMethodSemanticsTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodSemanticsTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodSemanticsTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodSemanticsTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodSemanticsTable::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodSemanticsTable::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodSemanticsTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodSemanticsTable::next_node = nullptr;
    SgAsmCilMethodSemanticsTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodSemanticsTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemanticsTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSemanticsTable)};
    for (size_t blockIndex = SgAsmCilMethodSemanticsTable::pools.size(); (blockIndex * SgAsmCilMethodSemanticsTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemanticsTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSemanticsTable) = %" PRIuPTR " SgAsmCilMethodSemanticsTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSemanticsTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSemanticsTable),SgAsmCilMethodSemanticsTable::pool_size);
        }
#endif

        SgAsmCilMethodSemanticsTable* pointer{static_cast<SgAsmCilMethodSemanticsTable*>(ROSE_MALLOC(SgAsmCilMethodSemanticsTable::pool_size * sizeof(SgAsmCilMethodSemanticsTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodSemanticsTable::pool_size * sizeof(SgAsmCilMethodSemanticsTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodSemanticsTable::pool_size * sizeof(SgAsmCilMethodSemanticsTable));
#endif
        SgAsmCilMethodSemanticsTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodSemanticsTable::pool_size * sizeof(SgAsmCilMethodSemanticsTable), V_SgAsmCilMethodSemanticsTable));

        if (SgAsmCilMethodSemanticsTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodSemanticsTable* blkptr{reinterpret_cast<SgAsmCilMethodSemanticsTable*>(SgAsmCilMethodSemanticsTable::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodSemanticsTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodSemanticsTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodSemanticsTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodSemanticsTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodSemanticsTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodSemanticsTable::pool_size - 1};
    SgAsmCilMethodSemanticsTable* testPointer{reinterpret_cast<SgAsmCilMethodSemanticsTable*>(SgAsmCilMethodSemanticsTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodSemanticsTable::pool_size * (SgAsmCilMethodSemanticsTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodSemanticsTableStroageClass
 * from its counterpart of SgAsmCilMethodSemanticsTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodSemanticsTable::initializeStorageClassArray(SgAsmCilMethodSemanticsTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodSemanticsTable::pools.begin(); block != SgAsmCilMethodSemanticsTable::pools.end(); ++block) {
        SgAsmCilMethodSemanticsTable* pointer{reinterpret_cast<SgAsmCilMethodSemanticsTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSemanticsTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 176632 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodSpec that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodSpec::SgAsmCilMethodSpec(const SgAsmCilMethodSpecStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 176642 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Method = storageSource.storageOf_Method;
    p_Instantiation = storageSource.storageOf_Instantiation;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodSpec*
SgAsmCilMethodSpec::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpec)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSpec::pool_size};
        returnPointer = &(((SgAsmCilMethodSpec*)(SgAsmCilMethodSpec::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodSpec*
SgAsmCilMethodSpec::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodSpec)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSpec::pool_size};
        returnPointer = &(((SgAsmCilMethodSpec*)(SgAsmCilMethodSpec::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodSpec::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodSpec::pools.begin(); block != SgAsmCilMethodSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSpec*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSpec::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodSpec*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodSpec::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodSpec* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodSpec::pools.begin(); block != SgAsmCilMethodSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSpec*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSpec::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodSpec::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodSpec::clearMemoryPool() {
    SgAsmCilMethodSpec* tempPointer{nullptr};
    if (SgAsmCilMethodSpec::pools.empty() == false) {
        auto block{SgAsmCilMethodSpec::pools.begin()};
        SgAsmCilMethodSpec::next_node = reinterpret_cast<SgAsmCilMethodSpec*>(*block);
        for (; block != SgAsmCilMethodSpec::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodSpec*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodSpec::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodSpec::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodSpec::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodSpec::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodSpec::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodSpec::next_node = nullptr;
    SgAsmCilMethodSpec::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodSpec::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpec) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSpec)};
    for (size_t blockIndex = SgAsmCilMethodSpec::pools.size(); (blockIndex * SgAsmCilMethodSpec::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpec) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSpec) = %" PRIuPTR " SgAsmCilMethodSpec::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpec),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSpec),SgAsmCilMethodSpec::pool_size);
        }
#endif

        SgAsmCilMethodSpec* pointer{static_cast<SgAsmCilMethodSpec*>(ROSE_MALLOC(SgAsmCilMethodSpec::pool_size * sizeof(SgAsmCilMethodSpec)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodSpec::pool_size * sizeof(SgAsmCilMethodSpec));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodSpec::pool_size * sizeof(SgAsmCilMethodSpec));
#endif
        SgAsmCilMethodSpec::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodSpec::pool_size * sizeof(SgAsmCilMethodSpec), V_SgAsmCilMethodSpec));

        if (SgAsmCilMethodSpec::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodSpec* blkptr{reinterpret_cast<SgAsmCilMethodSpec*>(SgAsmCilMethodSpec::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodSpec::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodSpec::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodSpec::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodSpec::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodSpec::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodSpec::pool_size - 1};
    SgAsmCilMethodSpec* testPointer{reinterpret_cast<SgAsmCilMethodSpec*>(SgAsmCilMethodSpec::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodSpec::pool_size * (SgAsmCilMethodSpec::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodSpecStroageClass
 * from its counterpart of SgAsmCilMethodSpec. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodSpec::initializeStorageClassArray(SgAsmCilMethodSpecStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodSpec::pools.begin(); block != SgAsmCilMethodSpec::pools.end(); ++block) {
        SgAsmCilMethodSpec* pointer{reinterpret_cast<SgAsmCilMethodSpec*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSpec::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 176861 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodSpecTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodSpecTable::SgAsmCilMethodSpecTable(const SgAsmCilMethodSpecTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 176871 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodSpecTable*
SgAsmCilMethodSpecTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodSpecTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSpecTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSpecTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodSpecTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpecTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSpecTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSpecTable::pool_size};
        returnPointer = &(((SgAsmCilMethodSpecTable*)(SgAsmCilMethodSpecTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodSpecTable*
SgAsmCilMethodSpecTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodSpecTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSpecTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSpecTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodSpecTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodSpecTable)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodSpecTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodSpecTable::pool_size};
        returnPointer = &(((SgAsmCilMethodSpecTable*)(SgAsmCilMethodSpecTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodSpecTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodSpecTable::pools.begin(); block != SgAsmCilMethodSpecTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSpecTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSpecTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodSpecTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodSpecTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodSpecTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodSpecTable::pools.begin(); block != SgAsmCilMethodSpecTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodSpecTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSpecTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodSpecTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodSpecTable::clearMemoryPool() {
    SgAsmCilMethodSpecTable* tempPointer{nullptr};
    if (SgAsmCilMethodSpecTable::pools.empty() == false) {
        auto block{SgAsmCilMethodSpecTable::pools.begin()};
        SgAsmCilMethodSpecTable::next_node = reinterpret_cast<SgAsmCilMethodSpecTable*>(*block);
        for (; block != SgAsmCilMethodSpecTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodSpecTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodSpecTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodSpecTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodSpecTable::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodSpecTable::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodSpecTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodSpecTable::next_node = nullptr;
    SgAsmCilMethodSpecTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodSpecTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpecTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSpecTable)};
    for (size_t blockIndex = SgAsmCilMethodSpecTable::pools.size(); (blockIndex * SgAsmCilMethodSpecTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpecTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSpecTable) = %" PRIuPTR " SgAsmCilMethodSpecTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodSpecTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodSpecTable),SgAsmCilMethodSpecTable::pool_size);
        }
#endif

        SgAsmCilMethodSpecTable* pointer{static_cast<SgAsmCilMethodSpecTable*>(ROSE_MALLOC(SgAsmCilMethodSpecTable::pool_size * sizeof(SgAsmCilMethodSpecTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodSpecTable::pool_size * sizeof(SgAsmCilMethodSpecTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodSpecTable::pool_size * sizeof(SgAsmCilMethodSpecTable));
#endif
        SgAsmCilMethodSpecTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodSpecTable::pool_size * sizeof(SgAsmCilMethodSpecTable), V_SgAsmCilMethodSpecTable));

        if (SgAsmCilMethodSpecTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodSpecTable* blkptr{reinterpret_cast<SgAsmCilMethodSpecTable*>(SgAsmCilMethodSpecTable::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodSpecTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodSpecTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodSpecTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodSpecTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodSpecTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodSpecTable::pool_size - 1};
    SgAsmCilMethodSpecTable* testPointer{reinterpret_cast<SgAsmCilMethodSpecTable*>(SgAsmCilMethodSpecTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodSpecTable::pool_size * (SgAsmCilMethodSpecTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodSpecTableStroageClass
 * from its counterpart of SgAsmCilMethodSpecTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodSpecTable::initializeStorageClassArray(SgAsmCilMethodSpecTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodSpecTable::pools.begin(); block != SgAsmCilMethodSpecTable::pools.end(); ++block) {
        SgAsmCilMethodSpecTable* pointer{reinterpret_cast<SgAsmCilMethodSpecTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodSpecTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 177089 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilModule that takes its corresponding StorageClass as parameter
 */
SgAsmCilModule::SgAsmCilModule(const SgAsmCilModuleStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 177099 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Generation = storageSource.storageOf_Generation;
    p_Name = storageSource.storageOf_Name;
    p_Mvid = storageSource.storageOf_Mvid;
    p_Encld = storageSource.storageOf_Encld;
    p_EncBaseId = storageSource.storageOf_EncBaseId;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilModule*
SgAsmCilModule::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModule)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModule)};
        unsigned long positionInPool{localIndex % SgAsmCilModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModule::pool_size};
        returnPointer = &(((SgAsmCilModule*)(SgAsmCilModule::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilModule*
SgAsmCilModule::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModule)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilModule)};
        unsigned long positionInPool{localIndex % SgAsmCilModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModule::pool_size};
        returnPointer = &(((SgAsmCilModule*)(SgAsmCilModule::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilModule::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilModule::pools.begin(); block != SgAsmCilModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModule*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModule::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilModule*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilModule::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilModule* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilModule::pools.begin(); block != SgAsmCilModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModule*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModule::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilModule::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilModule::clearMemoryPool() {
    SgAsmCilModule* tempPointer{nullptr};
    if (SgAsmCilModule::pools.empty() == false) {
        auto block{SgAsmCilModule::pools.begin()};
        SgAsmCilModule::next_node = reinterpret_cast<SgAsmCilModule*>(*block);
        for (; block != SgAsmCilModule::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilModule*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilModule::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilModule::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilModule::pool_size-1]);
        }
    }
}

void
SgAsmCilModule::deleteMemoryPool() {
    for (auto p: SgAsmCilModule::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilModule::next_node = nullptr;
    SgAsmCilModule::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilModule::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModule) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModule)};
    for (size_t blockIndex = SgAsmCilModule::pools.size(); (blockIndex * SgAsmCilModule::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModule) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModule) = %" PRIuPTR " SgAsmCilModule::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModule),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModule),SgAsmCilModule::pool_size);
        }
#endif

        SgAsmCilModule* pointer{static_cast<SgAsmCilModule*>(ROSE_MALLOC(SgAsmCilModule::pool_size * sizeof(SgAsmCilModule)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilModule::pool_size * sizeof(SgAsmCilModule));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilModule::pool_size * sizeof(SgAsmCilModule));
#endif
        SgAsmCilModule::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilModule::pool_size * sizeof(SgAsmCilModule), V_SgAsmCilModule));

        if (SgAsmCilModule::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilModule* blkptr{reinterpret_cast<SgAsmCilModule*>(SgAsmCilModule::pools[blockIndex-1])};
                blkptr[SgAsmCilModule::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilModule::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilModule::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilModule::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilModule::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilModule::pool_size - 1};
    SgAsmCilModule* testPointer{reinterpret_cast<SgAsmCilModule*>(SgAsmCilModule::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilModule::pool_size * (SgAsmCilModule::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilModuleStroageClass
 * from its counterpart of SgAsmCilModule. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilModule::initializeStorageClassArray(SgAsmCilModuleStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilModule::pools.begin(); block != SgAsmCilModule::pools.end(); ++block) {
        SgAsmCilModule* pointer{reinterpret_cast<SgAsmCilModule*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModule::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 177321 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilModuleRef that takes its corresponding StorageClass as parameter
 */
SgAsmCilModuleRef::SgAsmCilModuleRef(const SgAsmCilModuleRefStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 177331 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Name = storageSource.storageOf_Name;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilModuleRef*
SgAsmCilModuleRef::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilModuleRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleRef)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRef)};
        unsigned long positionInPool{localIndex % SgAsmCilModuleRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModuleRef::pool_size};
        returnPointer = &(((SgAsmCilModuleRef*)(SgAsmCilModuleRef::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilModuleRef*
SgAsmCilModuleRef::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilModuleRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleRef)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilModuleRef)};
        unsigned long positionInPool{localIndex % SgAsmCilModuleRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModuleRef::pool_size};
        returnPointer = &(((SgAsmCilModuleRef*)(SgAsmCilModuleRef::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilModuleRef::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilModuleRef::pools.begin(); block != SgAsmCilModuleRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModuleRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleRef::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilModuleRef*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilModuleRef::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilModuleRef* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilModuleRef::pools.begin(); block != SgAsmCilModuleRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModuleRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleRef::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilModuleRef::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilModuleRef::clearMemoryPool() {
    SgAsmCilModuleRef* tempPointer{nullptr};
    if (SgAsmCilModuleRef::pools.empty() == false) {
        auto block{SgAsmCilModuleRef::pools.begin()};
        SgAsmCilModuleRef::next_node = reinterpret_cast<SgAsmCilModuleRef*>(*block);
        for (; block != SgAsmCilModuleRef::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilModuleRef*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilModuleRef::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilModuleRef::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilModuleRef::pool_size-1]);
        }
    }
}

void
SgAsmCilModuleRef::deleteMemoryPool() {
    for (auto p: SgAsmCilModuleRef::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilModuleRef::next_node = nullptr;
    SgAsmCilModuleRef::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilModuleRef::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRef) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleRef)};
    for (size_t blockIndex = SgAsmCilModuleRef::pools.size(); (blockIndex * SgAsmCilModuleRef::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRef) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleRef) = %" PRIuPTR " SgAsmCilModuleRef::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRef),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleRef),SgAsmCilModuleRef::pool_size);
        }
#endif

        SgAsmCilModuleRef* pointer{static_cast<SgAsmCilModuleRef*>(ROSE_MALLOC(SgAsmCilModuleRef::pool_size * sizeof(SgAsmCilModuleRef)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilModuleRef::pool_size * sizeof(SgAsmCilModuleRef));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilModuleRef::pool_size * sizeof(SgAsmCilModuleRef));
#endif
        SgAsmCilModuleRef::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilModuleRef::pool_size * sizeof(SgAsmCilModuleRef), V_SgAsmCilModuleRef));

        if (SgAsmCilModuleRef::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilModuleRef* blkptr{reinterpret_cast<SgAsmCilModuleRef*>(SgAsmCilModuleRef::pools[blockIndex-1])};
                blkptr[SgAsmCilModuleRef::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilModuleRef::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilModuleRef::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilModuleRef::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilModuleRef::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilModuleRef::pool_size - 1};
    SgAsmCilModuleRef* testPointer{reinterpret_cast<SgAsmCilModuleRef*>(SgAsmCilModuleRef::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilModuleRef::pool_size * (SgAsmCilModuleRef::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilModuleRefStroageClass
 * from its counterpart of SgAsmCilModuleRef. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilModuleRef::initializeStorageClassArray(SgAsmCilModuleRefStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilModuleRef::pools.begin(); block != SgAsmCilModuleRef::pools.end(); ++block) {
        SgAsmCilModuleRef* pointer{reinterpret_cast<SgAsmCilModuleRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleRef::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 177549 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilModuleRefTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilModuleRefTable::SgAsmCilModuleRefTable(const SgAsmCilModuleRefTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 177559 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilModuleRefTable*
SgAsmCilModuleRefTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilModuleRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilModuleRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModuleRefTable::pool_size};
        returnPointer = &(((SgAsmCilModuleRefTable*)(SgAsmCilModuleRefTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilModuleRefTable*
SgAsmCilModuleRefTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilModuleRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilModuleRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilModuleRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModuleRefTable::pool_size};
        returnPointer = &(((SgAsmCilModuleRefTable*)(SgAsmCilModuleRefTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilModuleRefTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilModuleRefTable::pools.begin(); block != SgAsmCilModuleRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModuleRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleRefTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilModuleRefTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilModuleRefTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilModuleRefTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilModuleRefTable::pools.begin(); block != SgAsmCilModuleRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModuleRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleRefTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilModuleRefTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilModuleRefTable::clearMemoryPool() {
    SgAsmCilModuleRefTable* tempPointer{nullptr};
    if (SgAsmCilModuleRefTable::pools.empty() == false) {
        auto block{SgAsmCilModuleRefTable::pools.begin()};
        SgAsmCilModuleRefTable::next_node = reinterpret_cast<SgAsmCilModuleRefTable*>(*block);
        for (; block != SgAsmCilModuleRefTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilModuleRefTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilModuleRefTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilModuleRefTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilModuleRefTable::pool_size-1]);
        }
    }
}

void
SgAsmCilModuleRefTable::deleteMemoryPool() {
    for (auto p: SgAsmCilModuleRefTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilModuleRefTable::next_node = nullptr;
    SgAsmCilModuleRefTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilModuleRefTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRefTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleRefTable)};
    for (size_t blockIndex = SgAsmCilModuleRefTable::pools.size(); (blockIndex * SgAsmCilModuleRefTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRefTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleRefTable) = %" PRIuPTR " SgAsmCilModuleRefTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleRefTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleRefTable),SgAsmCilModuleRefTable::pool_size);
        }
#endif

        SgAsmCilModuleRefTable* pointer{static_cast<SgAsmCilModuleRefTable*>(ROSE_MALLOC(SgAsmCilModuleRefTable::pool_size * sizeof(SgAsmCilModuleRefTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilModuleRefTable::pool_size * sizeof(SgAsmCilModuleRefTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilModuleRefTable::pool_size * sizeof(SgAsmCilModuleRefTable));
#endif
        SgAsmCilModuleRefTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilModuleRefTable::pool_size * sizeof(SgAsmCilModuleRefTable), V_SgAsmCilModuleRefTable));

        if (SgAsmCilModuleRefTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilModuleRefTable* blkptr{reinterpret_cast<SgAsmCilModuleRefTable*>(SgAsmCilModuleRefTable::pools[blockIndex-1])};
                blkptr[SgAsmCilModuleRefTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilModuleRefTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilModuleRefTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilModuleRefTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilModuleRefTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilModuleRefTable::pool_size - 1};
    SgAsmCilModuleRefTable* testPointer{reinterpret_cast<SgAsmCilModuleRefTable*>(SgAsmCilModuleRefTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilModuleRefTable::pool_size * (SgAsmCilModuleRefTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilModuleRefTableStroageClass
 * from its counterpart of SgAsmCilModuleRefTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilModuleRefTable::initializeStorageClassArray(SgAsmCilModuleRefTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilModuleRefTable::pools.begin(); block != SgAsmCilModuleRefTable::pools.end(); ++block) {
        SgAsmCilModuleRefTable* pointer{reinterpret_cast<SgAsmCilModuleRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleRefTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 177777 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilModuleTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilModuleTable::SgAsmCilModuleTable(const SgAsmCilModuleTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 177787 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilModuleTable*
SgAsmCilModuleTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilModuleTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilModuleTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleTable)};
        unsigned long positionInPool{localIndex % SgAsmCilModuleTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModuleTable::pool_size};
        returnPointer = &(((SgAsmCilModuleTable*)(SgAsmCilModuleTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilModuleTable*
SgAsmCilModuleTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilModuleTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilModuleTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilModuleTable)};
        unsigned long positionInPool{localIndex % SgAsmCilModuleTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilModuleTable::pool_size};
        returnPointer = &(((SgAsmCilModuleTable*)(SgAsmCilModuleTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilModuleTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilModuleTable::pools.begin(); block != SgAsmCilModuleTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModuleTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilModuleTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilModuleTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilModuleTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilModuleTable::pools.begin(); block != SgAsmCilModuleTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilModuleTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilModuleTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilModuleTable::clearMemoryPool() {
    SgAsmCilModuleTable* tempPointer{nullptr};
    if (SgAsmCilModuleTable::pools.empty() == false) {
        auto block{SgAsmCilModuleTable::pools.begin()};
        SgAsmCilModuleTable::next_node = reinterpret_cast<SgAsmCilModuleTable*>(*block);
        for (; block != SgAsmCilModuleTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilModuleTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilModuleTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilModuleTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilModuleTable::pool_size-1]);
        }
    }
}

void
SgAsmCilModuleTable::deleteMemoryPool() {
    for (auto p: SgAsmCilModuleTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilModuleTable::next_node = nullptr;
    SgAsmCilModuleTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilModuleTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleTable)};
    for (size_t blockIndex = SgAsmCilModuleTable::pools.size(); (blockIndex * SgAsmCilModuleTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleTable) = %" PRIuPTR " SgAsmCilModuleTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilModuleTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilModuleTable),SgAsmCilModuleTable::pool_size);
        }
#endif

        SgAsmCilModuleTable* pointer{static_cast<SgAsmCilModuleTable*>(ROSE_MALLOC(SgAsmCilModuleTable::pool_size * sizeof(SgAsmCilModuleTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilModuleTable::pool_size * sizeof(SgAsmCilModuleTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilModuleTable::pool_size * sizeof(SgAsmCilModuleTable));
#endif
        SgAsmCilModuleTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilModuleTable::pool_size * sizeof(SgAsmCilModuleTable), V_SgAsmCilModuleTable));

        if (SgAsmCilModuleTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilModuleTable* blkptr{reinterpret_cast<SgAsmCilModuleTable*>(SgAsmCilModuleTable::pools[blockIndex-1])};
                blkptr[SgAsmCilModuleTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilModuleTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilModuleTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilModuleTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilModuleTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilModuleTable::pool_size - 1};
    SgAsmCilModuleTable* testPointer{reinterpret_cast<SgAsmCilModuleTable*>(SgAsmCilModuleTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilModuleTable::pool_size * (SgAsmCilModuleTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilModuleTableStroageClass
 * from its counterpart of SgAsmCilModuleTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilModuleTable::initializeStorageClassArray(SgAsmCilModuleTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilModuleTable::pools.begin(); block != SgAsmCilModuleTable::pools.end(); ++block) {
        SgAsmCilModuleTable* pointer{reinterpret_cast<SgAsmCilModuleTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilModuleTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 178005 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilNestedClass that takes its corresponding StorageClass as parameter
 */
SgAsmCilNestedClass::SgAsmCilNestedClass(const SgAsmCilNestedClassStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 178015 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_NestedClass = storageSource.storageOf_NestedClass;
    p_EnclosingClass = storageSource.storageOf_EnclosingClass;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilNestedClass*
SgAsmCilNestedClass::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilNestedClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNestedClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNestedClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNestedClass)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClass)};
        unsigned long positionInPool{localIndex % SgAsmCilNestedClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilNestedClass::pool_size};
        returnPointer = &(((SgAsmCilNestedClass*)(SgAsmCilNestedClass::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilNestedClass*
SgAsmCilNestedClass::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilNestedClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNestedClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNestedClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNestedClass)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilNestedClass)};
        unsigned long positionInPool{localIndex % SgAsmCilNestedClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilNestedClass::pool_size};
        returnPointer = &(((SgAsmCilNestedClass*)(SgAsmCilNestedClass::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilNestedClass::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilNestedClass::pools.begin(); block != SgAsmCilNestedClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilNestedClass*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNestedClass::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilNestedClass*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilNestedClass::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilNestedClass* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilNestedClass::pools.begin(); block != SgAsmCilNestedClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilNestedClass*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNestedClass::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilNestedClass::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilNestedClass::clearMemoryPool() {
    SgAsmCilNestedClass* tempPointer{nullptr};
    if (SgAsmCilNestedClass::pools.empty() == false) {
        auto block{SgAsmCilNestedClass::pools.begin()};
        SgAsmCilNestedClass::next_node = reinterpret_cast<SgAsmCilNestedClass*>(*block);
        for (; block != SgAsmCilNestedClass::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilNestedClass*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilNestedClass::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilNestedClass::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilNestedClass::pool_size-1]);
        }
    }
}

void
SgAsmCilNestedClass::deleteMemoryPool() {
    for (auto p: SgAsmCilNestedClass::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilNestedClass::next_node = nullptr;
    SgAsmCilNestedClass::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilNestedClass::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClass) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNestedClass)};
    for (size_t blockIndex = SgAsmCilNestedClass::pools.size(); (blockIndex * SgAsmCilNestedClass::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClass) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNestedClass) = %" PRIuPTR " SgAsmCilNestedClass::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClass),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNestedClass),SgAsmCilNestedClass::pool_size);
        }
#endif

        SgAsmCilNestedClass* pointer{static_cast<SgAsmCilNestedClass*>(ROSE_MALLOC(SgAsmCilNestedClass::pool_size * sizeof(SgAsmCilNestedClass)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilNestedClass::pool_size * sizeof(SgAsmCilNestedClass));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilNestedClass::pool_size * sizeof(SgAsmCilNestedClass));
#endif
        SgAsmCilNestedClass::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilNestedClass::pool_size * sizeof(SgAsmCilNestedClass), V_SgAsmCilNestedClass));

        if (SgAsmCilNestedClass::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilNestedClass* blkptr{reinterpret_cast<SgAsmCilNestedClass*>(SgAsmCilNestedClass::pools[blockIndex-1])};
                blkptr[SgAsmCilNestedClass::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilNestedClass::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilNestedClass::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilNestedClass::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilNestedClass::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilNestedClass::pool_size - 1};
    SgAsmCilNestedClass* testPointer{reinterpret_cast<SgAsmCilNestedClass*>(SgAsmCilNestedClass::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilNestedClass::pool_size * (SgAsmCilNestedClass::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilNestedClassStroageClass
 * from its counterpart of SgAsmCilNestedClass. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilNestedClass::initializeStorageClassArray(SgAsmCilNestedClassStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilNestedClass::pools.begin(); block != SgAsmCilNestedClass::pools.end(); ++block) {
        SgAsmCilNestedClass* pointer{reinterpret_cast<SgAsmCilNestedClass*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNestedClass::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 178234 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilNestedClassTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilNestedClassTable::SgAsmCilNestedClassTable(const SgAsmCilNestedClassTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 178244 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilNestedClassTable*
SgAsmCilNestedClassTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilNestedClassTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNestedClassTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNestedClassTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilNestedClassTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClassTable)};
        unsigned long positionInPool{localIndex % SgAsmCilNestedClassTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilNestedClassTable::pool_size};
        returnPointer = &(((SgAsmCilNestedClassTable*)(SgAsmCilNestedClassTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilNestedClassTable*
SgAsmCilNestedClassTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilNestedClassTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNestedClassTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNestedClassTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilNestedClassTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilNestedClassTable)};
        unsigned long positionInPool{localIndex % SgAsmCilNestedClassTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilNestedClassTable::pool_size};
        returnPointer = &(((SgAsmCilNestedClassTable*)(SgAsmCilNestedClassTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilNestedClassTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilNestedClassTable::pools.begin(); block != SgAsmCilNestedClassTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilNestedClassTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNestedClassTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilNestedClassTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilNestedClassTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilNestedClassTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilNestedClassTable::pools.begin(); block != SgAsmCilNestedClassTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilNestedClassTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNestedClassTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilNestedClassTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilNestedClassTable::clearMemoryPool() {
    SgAsmCilNestedClassTable* tempPointer{nullptr};
    if (SgAsmCilNestedClassTable::pools.empty() == false) {
        auto block{SgAsmCilNestedClassTable::pools.begin()};
        SgAsmCilNestedClassTable::next_node = reinterpret_cast<SgAsmCilNestedClassTable*>(*block);
        for (; block != SgAsmCilNestedClassTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilNestedClassTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilNestedClassTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilNestedClassTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilNestedClassTable::pool_size-1]);
        }
    }
}

void
SgAsmCilNestedClassTable::deleteMemoryPool() {
    for (auto p: SgAsmCilNestedClassTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilNestedClassTable::next_node = nullptr;
    SgAsmCilNestedClassTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilNestedClassTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClassTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNestedClassTable)};
    for (size_t blockIndex = SgAsmCilNestedClassTable::pools.size(); (blockIndex * SgAsmCilNestedClassTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClassTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNestedClassTable) = %" PRIuPTR " SgAsmCilNestedClassTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilNestedClassTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilNestedClassTable),SgAsmCilNestedClassTable::pool_size);
        }
#endif

        SgAsmCilNestedClassTable* pointer{static_cast<SgAsmCilNestedClassTable*>(ROSE_MALLOC(SgAsmCilNestedClassTable::pool_size * sizeof(SgAsmCilNestedClassTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilNestedClassTable::pool_size * sizeof(SgAsmCilNestedClassTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilNestedClassTable::pool_size * sizeof(SgAsmCilNestedClassTable));
#endif
        SgAsmCilNestedClassTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilNestedClassTable::pool_size * sizeof(SgAsmCilNestedClassTable), V_SgAsmCilNestedClassTable));

        if (SgAsmCilNestedClassTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilNestedClassTable* blkptr{reinterpret_cast<SgAsmCilNestedClassTable*>(SgAsmCilNestedClassTable::pools[blockIndex-1])};
                blkptr[SgAsmCilNestedClassTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilNestedClassTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilNestedClassTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilNestedClassTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilNestedClassTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilNestedClassTable::pool_size - 1};
    SgAsmCilNestedClassTable* testPointer{reinterpret_cast<SgAsmCilNestedClassTable*>(SgAsmCilNestedClassTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilNestedClassTable::pool_size * (SgAsmCilNestedClassTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilNestedClassTableStroageClass
 * from its counterpart of SgAsmCilNestedClassTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilNestedClassTable::initializeStorageClassArray(SgAsmCilNestedClassTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilNestedClassTable::pools.begin(); block != SgAsmCilNestedClassTable::pools.end(); ++block) {
        SgAsmCilNestedClassTable* pointer{reinterpret_cast<SgAsmCilNestedClassTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilNestedClassTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 178462 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilParam that takes its corresponding StorageClass as parameter
 */
SgAsmCilParam::SgAsmCilParam(const SgAsmCilParamStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 178472 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Flags = storageSource.storageOf_Flags;
    p_Sequence = storageSource.storageOf_Sequence;
    p_Name = storageSource.storageOf_Name;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilParam*
SgAsmCilParam::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilParam* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilParam)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilParam + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilParam)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParam)};
        unsigned long positionInPool{localIndex % SgAsmCilParam::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilParam::pool_size};
        returnPointer = &(((SgAsmCilParam*)(SgAsmCilParam::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilParam*
SgAsmCilParam::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilParam* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilParam)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilParam + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilParam)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilParam)};
        unsigned long positionInPool{localIndex % SgAsmCilParam::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilParam::pool_size};
        returnPointer = &(((SgAsmCilParam*)(SgAsmCilParam::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilParam::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilParam::pools.begin(); block != SgAsmCilParam::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilParam*>(*block)};
        for (unsigned i = 0; i < SgAsmCilParam::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilParam*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilParam::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilParam* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilParam::pools.begin(); block != SgAsmCilParam::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilParam*>(*block)};
        for (unsigned i = 0; i < SgAsmCilParam::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilParam::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilParam::clearMemoryPool() {
    SgAsmCilParam* tempPointer{nullptr};
    if (SgAsmCilParam::pools.empty() == false) {
        auto block{SgAsmCilParam::pools.begin()};
        SgAsmCilParam::next_node = reinterpret_cast<SgAsmCilParam*>(*block);
        for (; block != SgAsmCilParam::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilParam*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilParam::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilParam::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilParam::pool_size-1]);
        }
    }
}

void
SgAsmCilParam::deleteMemoryPool() {
    for (auto p: SgAsmCilParam::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilParam::next_node = nullptr;
    SgAsmCilParam::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilParam::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParam) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilParam)};
    for (size_t blockIndex = SgAsmCilParam::pools.size(); (blockIndex * SgAsmCilParam::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParam) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilParam) = %" PRIuPTR " SgAsmCilParam::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParam),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilParam),SgAsmCilParam::pool_size);
        }
#endif

        SgAsmCilParam* pointer{static_cast<SgAsmCilParam*>(ROSE_MALLOC(SgAsmCilParam::pool_size * sizeof(SgAsmCilParam)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilParam::pool_size * sizeof(SgAsmCilParam));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilParam::pool_size * sizeof(SgAsmCilParam));
#endif
        SgAsmCilParam::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilParam::pool_size * sizeof(SgAsmCilParam), V_SgAsmCilParam));

        if (SgAsmCilParam::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilParam* blkptr{reinterpret_cast<SgAsmCilParam*>(SgAsmCilParam::pools[blockIndex-1])};
                blkptr[SgAsmCilParam::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilParam::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilParam::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilParam::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilParam::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilParam::pool_size - 1};
    SgAsmCilParam* testPointer{reinterpret_cast<SgAsmCilParam*>(SgAsmCilParam::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilParam::pool_size * (SgAsmCilParam::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilParamStroageClass
 * from its counterpart of SgAsmCilParam. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilParam::initializeStorageClassArray(SgAsmCilParamStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilParam::pools.begin(); block != SgAsmCilParam::pools.end(); ++block) {
        SgAsmCilParam* pointer{reinterpret_cast<SgAsmCilParam*>(*block)};
        for (unsigned i = 0; i < SgAsmCilParam::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 178692 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilParamTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilParamTable::SgAsmCilParamTable(const SgAsmCilParamTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 178702 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilParamTable*
SgAsmCilParamTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilParamTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilParamTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilParamTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilParamTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParamTable)};
        unsigned long positionInPool{localIndex % SgAsmCilParamTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilParamTable::pool_size};
        returnPointer = &(((SgAsmCilParamTable*)(SgAsmCilParamTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilParamTable*
SgAsmCilParamTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilParamTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilParamTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilParamTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilParamTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilParamTable)};
        unsigned long positionInPool{localIndex % SgAsmCilParamTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilParamTable::pool_size};
        returnPointer = &(((SgAsmCilParamTable*)(SgAsmCilParamTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilParamTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilParamTable::pools.begin(); block != SgAsmCilParamTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilParamTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilParamTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilParamTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilParamTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilParamTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilParamTable::pools.begin(); block != SgAsmCilParamTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilParamTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilParamTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilParamTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilParamTable::clearMemoryPool() {
    SgAsmCilParamTable* tempPointer{nullptr};
    if (SgAsmCilParamTable::pools.empty() == false) {
        auto block{SgAsmCilParamTable::pools.begin()};
        SgAsmCilParamTable::next_node = reinterpret_cast<SgAsmCilParamTable*>(*block);
        for (; block != SgAsmCilParamTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilParamTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilParamTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilParamTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilParamTable::pool_size-1]);
        }
    }
}

void
SgAsmCilParamTable::deleteMemoryPool() {
    for (auto p: SgAsmCilParamTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilParamTable::next_node = nullptr;
    SgAsmCilParamTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilParamTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParamTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilParamTable)};
    for (size_t blockIndex = SgAsmCilParamTable::pools.size(); (blockIndex * SgAsmCilParamTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParamTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilParamTable) = %" PRIuPTR " SgAsmCilParamTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilParamTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilParamTable),SgAsmCilParamTable::pool_size);
        }
#endif

        SgAsmCilParamTable* pointer{static_cast<SgAsmCilParamTable*>(ROSE_MALLOC(SgAsmCilParamTable::pool_size * sizeof(SgAsmCilParamTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilParamTable::pool_size * sizeof(SgAsmCilParamTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilParamTable::pool_size * sizeof(SgAsmCilParamTable));
#endif
        SgAsmCilParamTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilParamTable::pool_size * sizeof(SgAsmCilParamTable), V_SgAsmCilParamTable));

        if (SgAsmCilParamTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilParamTable* blkptr{reinterpret_cast<SgAsmCilParamTable*>(SgAsmCilParamTable::pools[blockIndex-1])};
                blkptr[SgAsmCilParamTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilParamTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilParamTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilParamTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilParamTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilParamTable::pool_size - 1};
    SgAsmCilParamTable* testPointer{reinterpret_cast<SgAsmCilParamTable*>(SgAsmCilParamTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilParamTable::pool_size * (SgAsmCilParamTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilParamTableStroageClass
 * from its counterpart of SgAsmCilParamTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilParamTable::initializeStorageClassArray(SgAsmCilParamTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilParamTable::pools.begin(); block != SgAsmCilParamTable::pools.end(); ++block) {
        SgAsmCilParamTable* pointer{reinterpret_cast<SgAsmCilParamTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilParamTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 178920 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilProperty that takes its corresponding StorageClass as parameter
 */
SgAsmCilProperty::SgAsmCilProperty(const SgAsmCilPropertyStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 178930 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Flags = storageSource.storageOf_Flags;
    p_Name = storageSource.storageOf_Name;
    p_Type = storageSource.storageOf_Type;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilProperty*
SgAsmCilProperty::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilProperty* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilProperty)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilProperty + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilProperty)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilProperty)};
        unsigned long positionInPool{localIndex % SgAsmCilProperty::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilProperty::pool_size};
        returnPointer = &(((SgAsmCilProperty*)(SgAsmCilProperty::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilProperty*
SgAsmCilProperty::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilProperty* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilProperty)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilProperty + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilProperty)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilProperty)};
        unsigned long positionInPool{localIndex % SgAsmCilProperty::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilProperty::pool_size};
        returnPointer = &(((SgAsmCilProperty*)(SgAsmCilProperty::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilProperty::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilProperty::pools.begin(); block != SgAsmCilProperty::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilProperty*>(*block)};
        for (unsigned i = 0; i < SgAsmCilProperty::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilProperty*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilProperty::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilProperty* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilProperty::pools.begin(); block != SgAsmCilProperty::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilProperty*>(*block)};
        for (unsigned i = 0; i < SgAsmCilProperty::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilProperty::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilProperty::clearMemoryPool() {
    SgAsmCilProperty* tempPointer{nullptr};
    if (SgAsmCilProperty::pools.empty() == false) {
        auto block{SgAsmCilProperty::pools.begin()};
        SgAsmCilProperty::next_node = reinterpret_cast<SgAsmCilProperty*>(*block);
        for (; block != SgAsmCilProperty::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilProperty*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilProperty::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilProperty::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilProperty::pool_size-1]);
        }
    }
}

void
SgAsmCilProperty::deleteMemoryPool() {
    for (auto p: SgAsmCilProperty::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilProperty::next_node = nullptr;
    SgAsmCilProperty::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilProperty::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilProperty) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilProperty)};
    for (size_t blockIndex = SgAsmCilProperty::pools.size(); (blockIndex * SgAsmCilProperty::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilProperty) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilProperty) = %" PRIuPTR " SgAsmCilProperty::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilProperty),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilProperty),SgAsmCilProperty::pool_size);
        }
#endif

        SgAsmCilProperty* pointer{static_cast<SgAsmCilProperty*>(ROSE_MALLOC(SgAsmCilProperty::pool_size * sizeof(SgAsmCilProperty)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilProperty::pool_size * sizeof(SgAsmCilProperty));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilProperty::pool_size * sizeof(SgAsmCilProperty));
#endif
        SgAsmCilProperty::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilProperty::pool_size * sizeof(SgAsmCilProperty), V_SgAsmCilProperty));

        if (SgAsmCilProperty::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilProperty* blkptr{reinterpret_cast<SgAsmCilProperty*>(SgAsmCilProperty::pools[blockIndex-1])};
                blkptr[SgAsmCilProperty::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilProperty::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilProperty::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilProperty::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilProperty::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilProperty::pool_size - 1};
    SgAsmCilProperty* testPointer{reinterpret_cast<SgAsmCilProperty*>(SgAsmCilProperty::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilProperty::pool_size * (SgAsmCilProperty::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilPropertyStroageClass
 * from its counterpart of SgAsmCilProperty. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilProperty::initializeStorageClassArray(SgAsmCilPropertyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilProperty::pools.begin(); block != SgAsmCilProperty::pools.end(); ++block) {
        SgAsmCilProperty* pointer{reinterpret_cast<SgAsmCilProperty*>(*block)};
        for (unsigned i = 0; i < SgAsmCilProperty::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 179150 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilPropertyMap that takes its corresponding StorageClass as parameter
 */
SgAsmCilPropertyMap::SgAsmCilPropertyMap(const SgAsmCilPropertyMapStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 179160 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Parent = storageSource.storageOf_Parent;
    p_PropertyList = storageSource.storageOf_PropertyList;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilPropertyMap*
SgAsmCilPropertyMap::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilPropertyMap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyMap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyMap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyMap)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMap)};
        unsigned long positionInPool{localIndex % SgAsmCilPropertyMap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilPropertyMap::pool_size};
        returnPointer = &(((SgAsmCilPropertyMap*)(SgAsmCilPropertyMap::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilPropertyMap*
SgAsmCilPropertyMap::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilPropertyMap* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyMap)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyMap + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyMap)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilPropertyMap)};
        unsigned long positionInPool{localIndex % SgAsmCilPropertyMap::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilPropertyMap::pool_size};
        returnPointer = &(((SgAsmCilPropertyMap*)(SgAsmCilPropertyMap::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilPropertyMap::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilPropertyMap::pools.begin(); block != SgAsmCilPropertyMap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilPropertyMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyMap::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilPropertyMap*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilPropertyMap::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilPropertyMap* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilPropertyMap::pools.begin(); block != SgAsmCilPropertyMap::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilPropertyMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyMap::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilPropertyMap::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilPropertyMap::clearMemoryPool() {
    SgAsmCilPropertyMap* tempPointer{nullptr};
    if (SgAsmCilPropertyMap::pools.empty() == false) {
        auto block{SgAsmCilPropertyMap::pools.begin()};
        SgAsmCilPropertyMap::next_node = reinterpret_cast<SgAsmCilPropertyMap*>(*block);
        for (; block != SgAsmCilPropertyMap::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilPropertyMap*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilPropertyMap::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilPropertyMap::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilPropertyMap::pool_size-1]);
        }
    }
}

void
SgAsmCilPropertyMap::deleteMemoryPool() {
    for (auto p: SgAsmCilPropertyMap::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilPropertyMap::next_node = nullptr;
    SgAsmCilPropertyMap::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilPropertyMap::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMap) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyMap)};
    for (size_t blockIndex = SgAsmCilPropertyMap::pools.size(); (blockIndex * SgAsmCilPropertyMap::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMap) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyMap) = %" PRIuPTR " SgAsmCilPropertyMap::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMap),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyMap),SgAsmCilPropertyMap::pool_size);
        }
#endif

        SgAsmCilPropertyMap* pointer{static_cast<SgAsmCilPropertyMap*>(ROSE_MALLOC(SgAsmCilPropertyMap::pool_size * sizeof(SgAsmCilPropertyMap)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilPropertyMap::pool_size * sizeof(SgAsmCilPropertyMap));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilPropertyMap::pool_size * sizeof(SgAsmCilPropertyMap));
#endif
        SgAsmCilPropertyMap::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilPropertyMap::pool_size * sizeof(SgAsmCilPropertyMap), V_SgAsmCilPropertyMap));

        if (SgAsmCilPropertyMap::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilPropertyMap* blkptr{reinterpret_cast<SgAsmCilPropertyMap*>(SgAsmCilPropertyMap::pools[blockIndex-1])};
                blkptr[SgAsmCilPropertyMap::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilPropertyMap::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilPropertyMap::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilPropertyMap::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilPropertyMap::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilPropertyMap::pool_size - 1};
    SgAsmCilPropertyMap* testPointer{reinterpret_cast<SgAsmCilPropertyMap*>(SgAsmCilPropertyMap::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilPropertyMap::pool_size * (SgAsmCilPropertyMap::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilPropertyMapStroageClass
 * from its counterpart of SgAsmCilPropertyMap. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilPropertyMap::initializeStorageClassArray(SgAsmCilPropertyMapStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilPropertyMap::pools.begin(); block != SgAsmCilPropertyMap::pools.end(); ++block) {
        SgAsmCilPropertyMap* pointer{reinterpret_cast<SgAsmCilPropertyMap*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyMap::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 179379 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilPropertyMapTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilPropertyMapTable::SgAsmCilPropertyMapTable(const SgAsmCilPropertyMapTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 179389 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilPropertyMapTable*
SgAsmCilPropertyMapTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilPropertyMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMapTable)};
        unsigned long positionInPool{localIndex % SgAsmCilPropertyMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilPropertyMapTable::pool_size};
        returnPointer = &(((SgAsmCilPropertyMapTable*)(SgAsmCilPropertyMapTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilPropertyMapTable*
SgAsmCilPropertyMapTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilPropertyMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilPropertyMapTable)};
        unsigned long positionInPool{localIndex % SgAsmCilPropertyMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilPropertyMapTable::pool_size};
        returnPointer = &(((SgAsmCilPropertyMapTable*)(SgAsmCilPropertyMapTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilPropertyMapTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilPropertyMapTable::pools.begin(); block != SgAsmCilPropertyMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilPropertyMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyMapTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilPropertyMapTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilPropertyMapTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilPropertyMapTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilPropertyMapTable::pools.begin(); block != SgAsmCilPropertyMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilPropertyMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyMapTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilPropertyMapTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilPropertyMapTable::clearMemoryPool() {
    SgAsmCilPropertyMapTable* tempPointer{nullptr};
    if (SgAsmCilPropertyMapTable::pools.empty() == false) {
        auto block{SgAsmCilPropertyMapTable::pools.begin()};
        SgAsmCilPropertyMapTable::next_node = reinterpret_cast<SgAsmCilPropertyMapTable*>(*block);
        for (; block != SgAsmCilPropertyMapTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilPropertyMapTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilPropertyMapTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilPropertyMapTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilPropertyMapTable::pool_size-1]);
        }
    }
}

void
SgAsmCilPropertyMapTable::deleteMemoryPool() {
    for (auto p: SgAsmCilPropertyMapTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilPropertyMapTable::next_node = nullptr;
    SgAsmCilPropertyMapTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilPropertyMapTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMapTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyMapTable)};
    for (size_t blockIndex = SgAsmCilPropertyMapTable::pools.size(); (blockIndex * SgAsmCilPropertyMapTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMapTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyMapTable) = %" PRIuPTR " SgAsmCilPropertyMapTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyMapTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyMapTable),SgAsmCilPropertyMapTable::pool_size);
        }
#endif

        SgAsmCilPropertyMapTable* pointer{static_cast<SgAsmCilPropertyMapTable*>(ROSE_MALLOC(SgAsmCilPropertyMapTable::pool_size * sizeof(SgAsmCilPropertyMapTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilPropertyMapTable::pool_size * sizeof(SgAsmCilPropertyMapTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilPropertyMapTable::pool_size * sizeof(SgAsmCilPropertyMapTable));
#endif
        SgAsmCilPropertyMapTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilPropertyMapTable::pool_size * sizeof(SgAsmCilPropertyMapTable), V_SgAsmCilPropertyMapTable));

        if (SgAsmCilPropertyMapTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilPropertyMapTable* blkptr{reinterpret_cast<SgAsmCilPropertyMapTable*>(SgAsmCilPropertyMapTable::pools[blockIndex-1])};
                blkptr[SgAsmCilPropertyMapTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilPropertyMapTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilPropertyMapTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilPropertyMapTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilPropertyMapTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilPropertyMapTable::pool_size - 1};
    SgAsmCilPropertyMapTable* testPointer{reinterpret_cast<SgAsmCilPropertyMapTable*>(SgAsmCilPropertyMapTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilPropertyMapTable::pool_size * (SgAsmCilPropertyMapTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilPropertyMapTableStroageClass
 * from its counterpart of SgAsmCilPropertyMapTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilPropertyMapTable::initializeStorageClassArray(SgAsmCilPropertyMapTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilPropertyMapTable::pools.begin(); block != SgAsmCilPropertyMapTable::pools.end(); ++block) {
        SgAsmCilPropertyMapTable* pointer{reinterpret_cast<SgAsmCilPropertyMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyMapTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 179607 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilPropertyTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilPropertyTable::SgAsmCilPropertyTable(const SgAsmCilPropertyTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 179617 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilPropertyTable*
SgAsmCilPropertyTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilPropertyTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilPropertyTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyTable)};
        unsigned long positionInPool{localIndex % SgAsmCilPropertyTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilPropertyTable::pool_size};
        returnPointer = &(((SgAsmCilPropertyTable*)(SgAsmCilPropertyTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilPropertyTable*
SgAsmCilPropertyTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilPropertyTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilPropertyTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilPropertyTable)};
        unsigned long positionInPool{localIndex % SgAsmCilPropertyTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilPropertyTable::pool_size};
        returnPointer = &(((SgAsmCilPropertyTable*)(SgAsmCilPropertyTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilPropertyTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilPropertyTable::pools.begin(); block != SgAsmCilPropertyTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilPropertyTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilPropertyTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilPropertyTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilPropertyTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilPropertyTable::pools.begin(); block != SgAsmCilPropertyTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilPropertyTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilPropertyTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilPropertyTable::clearMemoryPool() {
    SgAsmCilPropertyTable* tempPointer{nullptr};
    if (SgAsmCilPropertyTable::pools.empty() == false) {
        auto block{SgAsmCilPropertyTable::pools.begin()};
        SgAsmCilPropertyTable::next_node = reinterpret_cast<SgAsmCilPropertyTable*>(*block);
        for (; block != SgAsmCilPropertyTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilPropertyTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilPropertyTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilPropertyTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilPropertyTable::pool_size-1]);
        }
    }
}

void
SgAsmCilPropertyTable::deleteMemoryPool() {
    for (auto p: SgAsmCilPropertyTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilPropertyTable::next_node = nullptr;
    SgAsmCilPropertyTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilPropertyTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyTable)};
    for (size_t blockIndex = SgAsmCilPropertyTable::pools.size(); (blockIndex * SgAsmCilPropertyTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyTable) = %" PRIuPTR " SgAsmCilPropertyTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilPropertyTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilPropertyTable),SgAsmCilPropertyTable::pool_size);
        }
#endif

        SgAsmCilPropertyTable* pointer{static_cast<SgAsmCilPropertyTable*>(ROSE_MALLOC(SgAsmCilPropertyTable::pool_size * sizeof(SgAsmCilPropertyTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilPropertyTable::pool_size * sizeof(SgAsmCilPropertyTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilPropertyTable::pool_size * sizeof(SgAsmCilPropertyTable));
#endif
        SgAsmCilPropertyTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilPropertyTable::pool_size * sizeof(SgAsmCilPropertyTable), V_SgAsmCilPropertyTable));

        if (SgAsmCilPropertyTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilPropertyTable* blkptr{reinterpret_cast<SgAsmCilPropertyTable*>(SgAsmCilPropertyTable::pools[blockIndex-1])};
                blkptr[SgAsmCilPropertyTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilPropertyTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilPropertyTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilPropertyTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilPropertyTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilPropertyTable::pool_size - 1};
    SgAsmCilPropertyTable* testPointer{reinterpret_cast<SgAsmCilPropertyTable*>(SgAsmCilPropertyTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilPropertyTable::pool_size * (SgAsmCilPropertyTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilPropertyTableStroageClass
 * from its counterpart of SgAsmCilPropertyTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilPropertyTable::initializeStorageClassArray(SgAsmCilPropertyTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilPropertyTable::pools.begin(); block != SgAsmCilPropertyTable::pools.end(); ++block) {
        SgAsmCilPropertyTable* pointer{reinterpret_cast<SgAsmCilPropertyTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilPropertyTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 179835 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilStandAloneSig that takes its corresponding StorageClass as parameter
 */
SgAsmCilStandAloneSig::SgAsmCilStandAloneSig(const SgAsmCilStandAloneSigStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 179845 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Signature = storageSource.storageOf_Signature;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilStandAloneSig*
SgAsmCilStandAloneSig::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilStandAloneSig* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilStandAloneSig)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilStandAloneSig + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilStandAloneSig)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSig)};
        unsigned long positionInPool{localIndex % SgAsmCilStandAloneSig::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilStandAloneSig::pool_size};
        returnPointer = &(((SgAsmCilStandAloneSig*)(SgAsmCilStandAloneSig::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilStandAloneSig*
SgAsmCilStandAloneSig::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilStandAloneSig* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilStandAloneSig)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilStandAloneSig + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilStandAloneSig)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilStandAloneSig)};
        unsigned long positionInPool{localIndex % SgAsmCilStandAloneSig::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilStandAloneSig::pool_size};
        returnPointer = &(((SgAsmCilStandAloneSig*)(SgAsmCilStandAloneSig::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilStandAloneSig::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilStandAloneSig::pools.begin(); block != SgAsmCilStandAloneSig::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilStandAloneSig*>(*block)};
        for (unsigned i = 0; i < SgAsmCilStandAloneSig::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilStandAloneSig*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilStandAloneSig::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilStandAloneSig* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilStandAloneSig::pools.begin(); block != SgAsmCilStandAloneSig::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilStandAloneSig*>(*block)};
        for (unsigned i = 0; i < SgAsmCilStandAloneSig::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilStandAloneSig::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilStandAloneSig::clearMemoryPool() {
    SgAsmCilStandAloneSig* tempPointer{nullptr};
    if (SgAsmCilStandAloneSig::pools.empty() == false) {
        auto block{SgAsmCilStandAloneSig::pools.begin()};
        SgAsmCilStandAloneSig::next_node = reinterpret_cast<SgAsmCilStandAloneSig*>(*block);
        for (; block != SgAsmCilStandAloneSig::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilStandAloneSig*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilStandAloneSig::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilStandAloneSig::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilStandAloneSig::pool_size-1]);
        }
    }
}

void
SgAsmCilStandAloneSig::deleteMemoryPool() {
    for (auto p: SgAsmCilStandAloneSig::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilStandAloneSig::next_node = nullptr;
    SgAsmCilStandAloneSig::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilStandAloneSig::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSig) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilStandAloneSig)};
    for (size_t blockIndex = SgAsmCilStandAloneSig::pools.size(); (blockIndex * SgAsmCilStandAloneSig::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSig) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilStandAloneSig) = %" PRIuPTR " SgAsmCilStandAloneSig::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSig),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilStandAloneSig),SgAsmCilStandAloneSig::pool_size);
        }
#endif

        SgAsmCilStandAloneSig* pointer{static_cast<SgAsmCilStandAloneSig*>(ROSE_MALLOC(SgAsmCilStandAloneSig::pool_size * sizeof(SgAsmCilStandAloneSig)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilStandAloneSig::pool_size * sizeof(SgAsmCilStandAloneSig));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilStandAloneSig::pool_size * sizeof(SgAsmCilStandAloneSig));
#endif
        SgAsmCilStandAloneSig::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilStandAloneSig::pool_size * sizeof(SgAsmCilStandAloneSig), V_SgAsmCilStandAloneSig));

        if (SgAsmCilStandAloneSig::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilStandAloneSig* blkptr{reinterpret_cast<SgAsmCilStandAloneSig*>(SgAsmCilStandAloneSig::pools[blockIndex-1])};
                blkptr[SgAsmCilStandAloneSig::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilStandAloneSig::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilStandAloneSig::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilStandAloneSig::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilStandAloneSig::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilStandAloneSig::pool_size - 1};
    SgAsmCilStandAloneSig* testPointer{reinterpret_cast<SgAsmCilStandAloneSig*>(SgAsmCilStandAloneSig::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilStandAloneSig::pool_size * (SgAsmCilStandAloneSig::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilStandAloneSigStroageClass
 * from its counterpart of SgAsmCilStandAloneSig. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilStandAloneSig::initializeStorageClassArray(SgAsmCilStandAloneSigStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilStandAloneSig::pools.begin(); block != SgAsmCilStandAloneSig::pools.end(); ++block) {
        SgAsmCilStandAloneSig* pointer{reinterpret_cast<SgAsmCilStandAloneSig*>(*block)};
        for (unsigned i = 0; i < SgAsmCilStandAloneSig::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 180063 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilStandAloneSigTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilStandAloneSigTable::SgAsmCilStandAloneSigTable(const SgAsmCilStandAloneSigTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 180073 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilStandAloneSigTable*
SgAsmCilStandAloneSigTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilStandAloneSigTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilStandAloneSigTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilStandAloneSigTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilStandAloneSigTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSigTable)};
        unsigned long positionInPool{localIndex % SgAsmCilStandAloneSigTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilStandAloneSigTable::pool_size};
        returnPointer = &(((SgAsmCilStandAloneSigTable*)(SgAsmCilStandAloneSigTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilStandAloneSigTable*
SgAsmCilStandAloneSigTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilStandAloneSigTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilStandAloneSigTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilStandAloneSigTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilStandAloneSigTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilStandAloneSigTable)};
        unsigned long positionInPool{localIndex % SgAsmCilStandAloneSigTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilStandAloneSigTable::pool_size};
        returnPointer = &(((SgAsmCilStandAloneSigTable*)(SgAsmCilStandAloneSigTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilStandAloneSigTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilStandAloneSigTable::pools.begin(); block != SgAsmCilStandAloneSigTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilStandAloneSigTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilStandAloneSigTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilStandAloneSigTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilStandAloneSigTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilStandAloneSigTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilStandAloneSigTable::pools.begin(); block != SgAsmCilStandAloneSigTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilStandAloneSigTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilStandAloneSigTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilStandAloneSigTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilStandAloneSigTable::clearMemoryPool() {
    SgAsmCilStandAloneSigTable* tempPointer{nullptr};
    if (SgAsmCilStandAloneSigTable::pools.empty() == false) {
        auto block{SgAsmCilStandAloneSigTable::pools.begin()};
        SgAsmCilStandAloneSigTable::next_node = reinterpret_cast<SgAsmCilStandAloneSigTable*>(*block);
        for (; block != SgAsmCilStandAloneSigTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilStandAloneSigTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilStandAloneSigTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilStandAloneSigTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilStandAloneSigTable::pool_size-1]);
        }
    }
}

void
SgAsmCilStandAloneSigTable::deleteMemoryPool() {
    for (auto p: SgAsmCilStandAloneSigTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilStandAloneSigTable::next_node = nullptr;
    SgAsmCilStandAloneSigTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilStandAloneSigTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSigTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilStandAloneSigTable)};
    for (size_t blockIndex = SgAsmCilStandAloneSigTable::pools.size(); (blockIndex * SgAsmCilStandAloneSigTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSigTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilStandAloneSigTable) = %" PRIuPTR " SgAsmCilStandAloneSigTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilStandAloneSigTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilStandAloneSigTable),SgAsmCilStandAloneSigTable::pool_size);
        }
#endif

        SgAsmCilStandAloneSigTable* pointer{static_cast<SgAsmCilStandAloneSigTable*>(ROSE_MALLOC(SgAsmCilStandAloneSigTable::pool_size * sizeof(SgAsmCilStandAloneSigTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilStandAloneSigTable::pool_size * sizeof(SgAsmCilStandAloneSigTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilStandAloneSigTable::pool_size * sizeof(SgAsmCilStandAloneSigTable));
#endif
        SgAsmCilStandAloneSigTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilStandAloneSigTable::pool_size * sizeof(SgAsmCilStandAloneSigTable), V_SgAsmCilStandAloneSigTable));

        if (SgAsmCilStandAloneSigTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilStandAloneSigTable* blkptr{reinterpret_cast<SgAsmCilStandAloneSigTable*>(SgAsmCilStandAloneSigTable::pools[blockIndex-1])};
                blkptr[SgAsmCilStandAloneSigTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilStandAloneSigTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilStandAloneSigTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilStandAloneSigTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilStandAloneSigTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilStandAloneSigTable::pool_size - 1};
    SgAsmCilStandAloneSigTable* testPointer{reinterpret_cast<SgAsmCilStandAloneSigTable*>(SgAsmCilStandAloneSigTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilStandAloneSigTable::pool_size * (SgAsmCilStandAloneSigTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilStandAloneSigTableStroageClass
 * from its counterpart of SgAsmCilStandAloneSigTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilStandAloneSigTable::initializeStorageClassArray(SgAsmCilStandAloneSigTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilStandAloneSigTable::pools.begin(); block != SgAsmCilStandAloneSigTable::pools.end(); ++block) {
        SgAsmCilStandAloneSigTable* pointer{reinterpret_cast<SgAsmCilStandAloneSigTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilStandAloneSigTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 180291 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilTypeDef that takes its corresponding StorageClass as parameter
 */
SgAsmCilTypeDef::SgAsmCilTypeDef(const SgAsmCilTypeDefStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 180301 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Flags = storageSource.storageOf_Flags;
    p_TypeName = storageSource.storageOf_TypeName;
    p_TypeNamespace = storageSource.storageOf_TypeNamespace;
    p_Extends = storageSource.storageOf_Extends;
    p_FieldList = storageSource.storageOf_FieldList;
    p_MethodList = storageSource.storageOf_MethodList;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilTypeDef*
SgAsmCilTypeDef::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilTypeDef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeDef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeDef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeDef)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDef)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeDef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeDef::pool_size};
        returnPointer = &(((SgAsmCilTypeDef*)(SgAsmCilTypeDef::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilTypeDef*
SgAsmCilTypeDef::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilTypeDef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeDef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeDef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeDef)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilTypeDef)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeDef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeDef::pool_size};
        returnPointer = &(((SgAsmCilTypeDef*)(SgAsmCilTypeDef::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilTypeDef::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilTypeDef::pools.begin(); block != SgAsmCilTypeDef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeDef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeDef::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilTypeDef*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilTypeDef::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilTypeDef* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilTypeDef::pools.begin(); block != SgAsmCilTypeDef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeDef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeDef::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilTypeDef::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilTypeDef::clearMemoryPool() {
    SgAsmCilTypeDef* tempPointer{nullptr};
    if (SgAsmCilTypeDef::pools.empty() == false) {
        auto block{SgAsmCilTypeDef::pools.begin()};
        SgAsmCilTypeDef::next_node = reinterpret_cast<SgAsmCilTypeDef*>(*block);
        for (; block != SgAsmCilTypeDef::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilTypeDef*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilTypeDef::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilTypeDef::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilTypeDef::pool_size-1]);
        }
    }
}

void
SgAsmCilTypeDef::deleteMemoryPool() {
    for (auto p: SgAsmCilTypeDef::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilTypeDef::next_node = nullptr;
    SgAsmCilTypeDef::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilTypeDef::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDef) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeDef)};
    for (size_t blockIndex = SgAsmCilTypeDef::pools.size(); (blockIndex * SgAsmCilTypeDef::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDef) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeDef) = %" PRIuPTR " SgAsmCilTypeDef::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDef),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeDef),SgAsmCilTypeDef::pool_size);
        }
#endif

        SgAsmCilTypeDef* pointer{static_cast<SgAsmCilTypeDef*>(ROSE_MALLOC(SgAsmCilTypeDef::pool_size * sizeof(SgAsmCilTypeDef)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilTypeDef::pool_size * sizeof(SgAsmCilTypeDef));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilTypeDef::pool_size * sizeof(SgAsmCilTypeDef));
#endif
        SgAsmCilTypeDef::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilTypeDef::pool_size * sizeof(SgAsmCilTypeDef), V_SgAsmCilTypeDef));

        if (SgAsmCilTypeDef::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilTypeDef* blkptr{reinterpret_cast<SgAsmCilTypeDef*>(SgAsmCilTypeDef::pools[blockIndex-1])};
                blkptr[SgAsmCilTypeDef::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilTypeDef::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilTypeDef::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilTypeDef::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilTypeDef::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilTypeDef::pool_size - 1};
    SgAsmCilTypeDef* testPointer{reinterpret_cast<SgAsmCilTypeDef*>(SgAsmCilTypeDef::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilTypeDef::pool_size * (SgAsmCilTypeDef::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilTypeDefStroageClass
 * from its counterpart of SgAsmCilTypeDef. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilTypeDef::initializeStorageClassArray(SgAsmCilTypeDefStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilTypeDef::pools.begin(); block != SgAsmCilTypeDef::pools.end(); ++block) {
        SgAsmCilTypeDef* pointer{reinterpret_cast<SgAsmCilTypeDef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeDef::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 180524 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilTypeDefTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilTypeDefTable::SgAsmCilTypeDefTable(const SgAsmCilTypeDefTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 180534 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilTypeDefTable*
SgAsmCilTypeDefTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilTypeDefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeDefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeDefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeDefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeDefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeDefTable::pool_size};
        returnPointer = &(((SgAsmCilTypeDefTable*)(SgAsmCilTypeDefTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilTypeDefTable*
SgAsmCilTypeDefTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilTypeDefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeDefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeDefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeDefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilTypeDefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeDefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeDefTable::pool_size};
        returnPointer = &(((SgAsmCilTypeDefTable*)(SgAsmCilTypeDefTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilTypeDefTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilTypeDefTable::pools.begin(); block != SgAsmCilTypeDefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeDefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeDefTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilTypeDefTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilTypeDefTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilTypeDefTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilTypeDefTable::pools.begin(); block != SgAsmCilTypeDefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeDefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeDefTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilTypeDefTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilTypeDefTable::clearMemoryPool() {
    SgAsmCilTypeDefTable* tempPointer{nullptr};
    if (SgAsmCilTypeDefTable::pools.empty() == false) {
        auto block{SgAsmCilTypeDefTable::pools.begin()};
        SgAsmCilTypeDefTable::next_node = reinterpret_cast<SgAsmCilTypeDefTable*>(*block);
        for (; block != SgAsmCilTypeDefTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilTypeDefTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilTypeDefTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilTypeDefTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilTypeDefTable::pool_size-1]);
        }
    }
}

void
SgAsmCilTypeDefTable::deleteMemoryPool() {
    for (auto p: SgAsmCilTypeDefTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilTypeDefTable::next_node = nullptr;
    SgAsmCilTypeDefTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilTypeDefTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDefTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeDefTable)};
    for (size_t blockIndex = SgAsmCilTypeDefTable::pools.size(); (blockIndex * SgAsmCilTypeDefTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDefTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeDefTable) = %" PRIuPTR " SgAsmCilTypeDefTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeDefTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeDefTable),SgAsmCilTypeDefTable::pool_size);
        }
#endif

        SgAsmCilTypeDefTable* pointer{static_cast<SgAsmCilTypeDefTable*>(ROSE_MALLOC(SgAsmCilTypeDefTable::pool_size * sizeof(SgAsmCilTypeDefTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilTypeDefTable::pool_size * sizeof(SgAsmCilTypeDefTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilTypeDefTable::pool_size * sizeof(SgAsmCilTypeDefTable));
#endif
        SgAsmCilTypeDefTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilTypeDefTable::pool_size * sizeof(SgAsmCilTypeDefTable), V_SgAsmCilTypeDefTable));

        if (SgAsmCilTypeDefTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilTypeDefTable* blkptr{reinterpret_cast<SgAsmCilTypeDefTable*>(SgAsmCilTypeDefTable::pools[blockIndex-1])};
                blkptr[SgAsmCilTypeDefTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilTypeDefTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilTypeDefTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilTypeDefTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilTypeDefTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilTypeDefTable::pool_size - 1};
    SgAsmCilTypeDefTable* testPointer{reinterpret_cast<SgAsmCilTypeDefTable*>(SgAsmCilTypeDefTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilTypeDefTable::pool_size * (SgAsmCilTypeDefTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilTypeDefTableStroageClass
 * from its counterpart of SgAsmCilTypeDefTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilTypeDefTable::initializeStorageClassArray(SgAsmCilTypeDefTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilTypeDefTable::pools.begin(); block != SgAsmCilTypeDefTable::pools.end(); ++block) {
        SgAsmCilTypeDefTable* pointer{reinterpret_cast<SgAsmCilTypeDefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeDefTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 180752 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilTypeRef that takes its corresponding StorageClass as parameter
 */
SgAsmCilTypeRef::SgAsmCilTypeRef(const SgAsmCilTypeRefStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 180762 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_ResolutionScope = storageSource.storageOf_ResolutionScope;
    p_TypeName = storageSource.storageOf_TypeName;
    p_TypeNamespace = storageSource.storageOf_TypeNamespace;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilTypeRef*
SgAsmCilTypeRef::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilTypeRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeRef)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRef)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeRef::pool_size};
        returnPointer = &(((SgAsmCilTypeRef*)(SgAsmCilTypeRef::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilTypeRef*
SgAsmCilTypeRef::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilTypeRef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeRef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeRef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeRef)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilTypeRef)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeRef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeRef::pool_size};
        returnPointer = &(((SgAsmCilTypeRef*)(SgAsmCilTypeRef::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilTypeRef::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilTypeRef::pools.begin(); block != SgAsmCilTypeRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeRef::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilTypeRef*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilTypeRef::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilTypeRef* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilTypeRef::pools.begin(); block != SgAsmCilTypeRef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeRef::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilTypeRef::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilTypeRef::clearMemoryPool() {
    SgAsmCilTypeRef* tempPointer{nullptr};
    if (SgAsmCilTypeRef::pools.empty() == false) {
        auto block{SgAsmCilTypeRef::pools.begin()};
        SgAsmCilTypeRef::next_node = reinterpret_cast<SgAsmCilTypeRef*>(*block);
        for (; block != SgAsmCilTypeRef::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilTypeRef*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilTypeRef::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilTypeRef::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilTypeRef::pool_size-1]);
        }
    }
}

void
SgAsmCilTypeRef::deleteMemoryPool() {
    for (auto p: SgAsmCilTypeRef::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilTypeRef::next_node = nullptr;
    SgAsmCilTypeRef::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilTypeRef::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRef) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeRef)};
    for (size_t blockIndex = SgAsmCilTypeRef::pools.size(); (blockIndex * SgAsmCilTypeRef::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRef) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeRef) = %" PRIuPTR " SgAsmCilTypeRef::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRef),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeRef),SgAsmCilTypeRef::pool_size);
        }
#endif

        SgAsmCilTypeRef* pointer{static_cast<SgAsmCilTypeRef*>(ROSE_MALLOC(SgAsmCilTypeRef::pool_size * sizeof(SgAsmCilTypeRef)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilTypeRef::pool_size * sizeof(SgAsmCilTypeRef));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilTypeRef::pool_size * sizeof(SgAsmCilTypeRef));
#endif
        SgAsmCilTypeRef::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilTypeRef::pool_size * sizeof(SgAsmCilTypeRef), V_SgAsmCilTypeRef));

        if (SgAsmCilTypeRef::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilTypeRef* blkptr{reinterpret_cast<SgAsmCilTypeRef*>(SgAsmCilTypeRef::pools[blockIndex-1])};
                blkptr[SgAsmCilTypeRef::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilTypeRef::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilTypeRef::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilTypeRef::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilTypeRef::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilTypeRef::pool_size - 1};
    SgAsmCilTypeRef* testPointer{reinterpret_cast<SgAsmCilTypeRef*>(SgAsmCilTypeRef::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilTypeRef::pool_size * (SgAsmCilTypeRef::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilTypeRefStroageClass
 * from its counterpart of SgAsmCilTypeRef. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilTypeRef::initializeStorageClassArray(SgAsmCilTypeRefStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilTypeRef::pools.begin(); block != SgAsmCilTypeRef::pools.end(); ++block) {
        SgAsmCilTypeRef* pointer{reinterpret_cast<SgAsmCilTypeRef*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeRef::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 180982 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilTypeRefTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilTypeRefTable::SgAsmCilTypeRefTable(const SgAsmCilTypeRefTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 180992 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilTypeRefTable*
SgAsmCilTypeRefTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilTypeRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeRefTable::pool_size};
        returnPointer = &(((SgAsmCilTypeRefTable*)(SgAsmCilTypeRefTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilTypeRefTable*
SgAsmCilTypeRefTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilTypeRefTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeRefTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeRefTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeRefTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilTypeRefTable)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeRefTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeRefTable::pool_size};
        returnPointer = &(((SgAsmCilTypeRefTable*)(SgAsmCilTypeRefTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilTypeRefTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilTypeRefTable::pools.begin(); block != SgAsmCilTypeRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeRefTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilTypeRefTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilTypeRefTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilTypeRefTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilTypeRefTable::pools.begin(); block != SgAsmCilTypeRefTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeRefTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilTypeRefTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilTypeRefTable::clearMemoryPool() {
    SgAsmCilTypeRefTable* tempPointer{nullptr};
    if (SgAsmCilTypeRefTable::pools.empty() == false) {
        auto block{SgAsmCilTypeRefTable::pools.begin()};
        SgAsmCilTypeRefTable::next_node = reinterpret_cast<SgAsmCilTypeRefTable*>(*block);
        for (; block != SgAsmCilTypeRefTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilTypeRefTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilTypeRefTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilTypeRefTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilTypeRefTable::pool_size-1]);
        }
    }
}

void
SgAsmCilTypeRefTable::deleteMemoryPool() {
    for (auto p: SgAsmCilTypeRefTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilTypeRefTable::next_node = nullptr;
    SgAsmCilTypeRefTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilTypeRefTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRefTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeRefTable)};
    for (size_t blockIndex = SgAsmCilTypeRefTable::pools.size(); (blockIndex * SgAsmCilTypeRefTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRefTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeRefTable) = %" PRIuPTR " SgAsmCilTypeRefTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeRefTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeRefTable),SgAsmCilTypeRefTable::pool_size);
        }
#endif

        SgAsmCilTypeRefTable* pointer{static_cast<SgAsmCilTypeRefTable*>(ROSE_MALLOC(SgAsmCilTypeRefTable::pool_size * sizeof(SgAsmCilTypeRefTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilTypeRefTable::pool_size * sizeof(SgAsmCilTypeRefTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilTypeRefTable::pool_size * sizeof(SgAsmCilTypeRefTable));
#endif
        SgAsmCilTypeRefTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilTypeRefTable::pool_size * sizeof(SgAsmCilTypeRefTable), V_SgAsmCilTypeRefTable));

        if (SgAsmCilTypeRefTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilTypeRefTable* blkptr{reinterpret_cast<SgAsmCilTypeRefTable*>(SgAsmCilTypeRefTable::pools[blockIndex-1])};
                blkptr[SgAsmCilTypeRefTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilTypeRefTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilTypeRefTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilTypeRefTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilTypeRefTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilTypeRefTable::pool_size - 1};
    SgAsmCilTypeRefTable* testPointer{reinterpret_cast<SgAsmCilTypeRefTable*>(SgAsmCilTypeRefTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilTypeRefTable::pool_size * (SgAsmCilTypeRefTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilTypeRefTableStroageClass
 * from its counterpart of SgAsmCilTypeRefTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilTypeRefTable::initializeStorageClassArray(SgAsmCilTypeRefTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilTypeRefTable::pools.begin(); block != SgAsmCilTypeRefTable::pools.end(); ++block) {
        SgAsmCilTypeRefTable* pointer{reinterpret_cast<SgAsmCilTypeRefTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeRefTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 181210 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilTypeSpec that takes its corresponding StorageClass as parameter
 */
SgAsmCilTypeSpec::SgAsmCilTypeSpec(const SgAsmCilTypeSpecStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 181220 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Signature = storageSource.storageOf_Signature;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilTypeSpec*
SgAsmCilTypeSpec::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilTypeSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpec)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeSpec::pool_size};
        returnPointer = &(((SgAsmCilTypeSpec*)(SgAsmCilTypeSpec::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilTypeSpec*
SgAsmCilTypeSpec::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilTypeSpec* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeSpec)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeSpec + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeSpec)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilTypeSpec)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeSpec::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeSpec::pool_size};
        returnPointer = &(((SgAsmCilTypeSpec*)(SgAsmCilTypeSpec::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilTypeSpec::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilTypeSpec::pools.begin(); block != SgAsmCilTypeSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeSpec*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeSpec::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilTypeSpec*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilTypeSpec::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilTypeSpec* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilTypeSpec::pools.begin(); block != SgAsmCilTypeSpec::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeSpec*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeSpec::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilTypeSpec::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilTypeSpec::clearMemoryPool() {
    SgAsmCilTypeSpec* tempPointer{nullptr};
    if (SgAsmCilTypeSpec::pools.empty() == false) {
        auto block{SgAsmCilTypeSpec::pools.begin()};
        SgAsmCilTypeSpec::next_node = reinterpret_cast<SgAsmCilTypeSpec*>(*block);
        for (; block != SgAsmCilTypeSpec::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilTypeSpec*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilTypeSpec::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilTypeSpec::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilTypeSpec::pool_size-1]);
        }
    }
}

void
SgAsmCilTypeSpec::deleteMemoryPool() {
    for (auto p: SgAsmCilTypeSpec::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilTypeSpec::next_node = nullptr;
    SgAsmCilTypeSpec::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilTypeSpec::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpec) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeSpec)};
    for (size_t blockIndex = SgAsmCilTypeSpec::pools.size(); (blockIndex * SgAsmCilTypeSpec::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpec) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeSpec) = %" PRIuPTR " SgAsmCilTypeSpec::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpec),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeSpec),SgAsmCilTypeSpec::pool_size);
        }
#endif

        SgAsmCilTypeSpec* pointer{static_cast<SgAsmCilTypeSpec*>(ROSE_MALLOC(SgAsmCilTypeSpec::pool_size * sizeof(SgAsmCilTypeSpec)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilTypeSpec::pool_size * sizeof(SgAsmCilTypeSpec));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilTypeSpec::pool_size * sizeof(SgAsmCilTypeSpec));
#endif
        SgAsmCilTypeSpec::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilTypeSpec::pool_size * sizeof(SgAsmCilTypeSpec), V_SgAsmCilTypeSpec));

        if (SgAsmCilTypeSpec::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilTypeSpec* blkptr{reinterpret_cast<SgAsmCilTypeSpec*>(SgAsmCilTypeSpec::pools[blockIndex-1])};
                blkptr[SgAsmCilTypeSpec::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilTypeSpec::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilTypeSpec::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilTypeSpec::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilTypeSpec::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilTypeSpec::pool_size - 1};
    SgAsmCilTypeSpec* testPointer{reinterpret_cast<SgAsmCilTypeSpec*>(SgAsmCilTypeSpec::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilTypeSpec::pool_size * (SgAsmCilTypeSpec::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilTypeSpecStroageClass
 * from its counterpart of SgAsmCilTypeSpec. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilTypeSpec::initializeStorageClassArray(SgAsmCilTypeSpecStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilTypeSpec::pools.begin(); block != SgAsmCilTypeSpec::pools.end(); ++block) {
        SgAsmCilTypeSpec* pointer{reinterpret_cast<SgAsmCilTypeSpec*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeSpec::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 181438 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilTypeSpecTable that takes its corresponding StorageClass as parameter
 */
SgAsmCilTypeSpecTable::SgAsmCilTypeSpecTable(const SgAsmCilTypeSpecTableStorageClass& storageSource)  : SgAsmCilMetadata (storageSource) {

/* #line 181448 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_elements = storageSource.storageOf_elements.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilTypeSpecTable*
SgAsmCilTypeSpecTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilTypeSpecTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeSpecTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeSpecTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilTypeSpecTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpecTable)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeSpecTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeSpecTable::pool_size};
        returnPointer = &(((SgAsmCilTypeSpecTable*)(SgAsmCilTypeSpecTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilTypeSpecTable*
SgAsmCilTypeSpecTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilTypeSpecTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeSpecTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeSpecTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilTypeSpecTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilTypeSpecTable)};
        unsigned long positionInPool{localIndex % SgAsmCilTypeSpecTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilTypeSpecTable::pool_size};
        returnPointer = &(((SgAsmCilTypeSpecTable*)(SgAsmCilTypeSpecTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilTypeSpecTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilTypeSpecTable::pools.begin(); block != SgAsmCilTypeSpecTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeSpecTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeSpecTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilTypeSpecTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilTypeSpecTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilTypeSpecTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilTypeSpecTable::pools.begin(); block != SgAsmCilTypeSpecTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilTypeSpecTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeSpecTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilTypeSpecTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilTypeSpecTable::clearMemoryPool() {
    SgAsmCilTypeSpecTable* tempPointer{nullptr};
    if (SgAsmCilTypeSpecTable::pools.empty() == false) {
        auto block{SgAsmCilTypeSpecTable::pools.begin()};
        SgAsmCilTypeSpecTable::next_node = reinterpret_cast<SgAsmCilTypeSpecTable*>(*block);
        for (; block != SgAsmCilTypeSpecTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilTypeSpecTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilTypeSpecTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilTypeSpecTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilTypeSpecTable::pool_size-1]);
        }
    }
}

void
SgAsmCilTypeSpecTable::deleteMemoryPool() {
    for (auto p: SgAsmCilTypeSpecTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilTypeSpecTable::next_node = nullptr;
    SgAsmCilTypeSpecTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilTypeSpecTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpecTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeSpecTable)};
    for (size_t blockIndex = SgAsmCilTypeSpecTable::pools.size(); (blockIndex * SgAsmCilTypeSpecTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpecTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeSpecTable) = %" PRIuPTR " SgAsmCilTypeSpecTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilTypeSpecTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilTypeSpecTable),SgAsmCilTypeSpecTable::pool_size);
        }
#endif

        SgAsmCilTypeSpecTable* pointer{static_cast<SgAsmCilTypeSpecTable*>(ROSE_MALLOC(SgAsmCilTypeSpecTable::pool_size * sizeof(SgAsmCilTypeSpecTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilTypeSpecTable::pool_size * sizeof(SgAsmCilTypeSpecTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilTypeSpecTable::pool_size * sizeof(SgAsmCilTypeSpecTable));
#endif
        SgAsmCilTypeSpecTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilTypeSpecTable::pool_size * sizeof(SgAsmCilTypeSpecTable), V_SgAsmCilTypeSpecTable));

        if (SgAsmCilTypeSpecTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilTypeSpecTable* blkptr{reinterpret_cast<SgAsmCilTypeSpecTable*>(SgAsmCilTypeSpecTable::pools[blockIndex-1])};
                blkptr[SgAsmCilTypeSpecTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilTypeSpecTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilTypeSpecTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilTypeSpecTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilTypeSpecTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilTypeSpecTable::pool_size - 1};
    SgAsmCilTypeSpecTable* testPointer{reinterpret_cast<SgAsmCilTypeSpecTable*>(SgAsmCilTypeSpecTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilTypeSpecTable::pool_size * (SgAsmCilTypeSpecTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilTypeSpecTableStroageClass
 * from its counterpart of SgAsmCilTypeSpecTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilTypeSpecTable::initializeStorageClassArray(SgAsmCilTypeSpecTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilTypeSpecTable::pools.begin(); block != SgAsmCilTypeSpecTable::pools.end(); ++block) {
        SgAsmCilTypeSpecTable* pointer{reinterpret_cast<SgAsmCilTypeSpecTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCilTypeSpecTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 181666 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMetadataRoot that takes its corresponding StorageClass as parameter
 */
SgAsmCilMetadataRoot::SgAsmCilMetadataRoot(const SgAsmCilMetadataRootStorageClass& storageSource)  : SgAsmCilNode (storageSource) {

/* #line 181676 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_Signature = storageSource.storageOf_Signature;
    p_MajorVersion = storageSource.storageOf_MajorVersion;
    p_MinorVersion = storageSource.storageOf_MinorVersion;
    p_Reserved0 = storageSource.storageOf_Reserved0;
    p_Length = storageSource.storageOf_Length;
    p_Version = storageSource.storageOf_Version.rebuildDataStoredInEasyStorageClass();
    p_VersionPadding = storageSource.storageOf_VersionPadding;
    p_Flags = storageSource.storageOf_Flags;
    p_NumberOfStreams = storageSource.storageOf_NumberOfStreams;
    p_Streams = storageSource.storageOf_Streams.rebuildDataStoredInEasyStorageClass();
    p_ErrorFree = storageSource.storageOf_ErrorFree;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMetadataRoot*
SgAsmCilMetadataRoot::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMetadataRoot* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadataRoot)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadataRoot + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMetadataRoot)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataRoot)};
        unsigned long positionInPool{localIndex % SgAsmCilMetadataRoot::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMetadataRoot::pool_size};
        returnPointer = &(((SgAsmCilMetadataRoot*)(SgAsmCilMetadataRoot::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMetadataRoot*
SgAsmCilMetadataRoot::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMetadataRoot* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadataRoot)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadataRoot + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMetadataRoot)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMetadataRoot)};
        unsigned long positionInPool{localIndex % SgAsmCilMetadataRoot::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMetadataRoot::pool_size};
        returnPointer = &(((SgAsmCilMetadataRoot*)(SgAsmCilMetadataRoot::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMetadataRoot::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMetadataRoot::pools.begin(); block != SgAsmCilMetadataRoot::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMetadataRoot*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadataRoot::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMetadataRoot*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMetadataRoot::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMetadataRoot* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMetadataRoot::pools.begin(); block != SgAsmCilMetadataRoot::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMetadataRoot*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadataRoot::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMetadataRoot::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMetadataRoot::clearMemoryPool() {
    SgAsmCilMetadataRoot* tempPointer{nullptr};
    if (SgAsmCilMetadataRoot::pools.empty() == false) {
        auto block{SgAsmCilMetadataRoot::pools.begin()};
        SgAsmCilMetadataRoot::next_node = reinterpret_cast<SgAsmCilMetadataRoot*>(*block);
        for (; block != SgAsmCilMetadataRoot::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMetadataRoot*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMetadataRoot::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMetadataRoot::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMetadataRoot::pool_size-1]);
        }
    }
}

void
SgAsmCilMetadataRoot::deleteMemoryPool() {
    for (auto p: SgAsmCilMetadataRoot::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMetadataRoot::next_node = nullptr;
    SgAsmCilMetadataRoot::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMetadataRoot::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataRoot) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadataRoot)};
    for (size_t blockIndex = SgAsmCilMetadataRoot::pools.size(); (blockIndex * SgAsmCilMetadataRoot::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataRoot) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadataRoot) = %" PRIuPTR " SgAsmCilMetadataRoot::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMetadataRoot),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMetadataRoot),SgAsmCilMetadataRoot::pool_size);
        }
#endif

        SgAsmCilMetadataRoot* pointer{static_cast<SgAsmCilMetadataRoot*>(ROSE_MALLOC(SgAsmCilMetadataRoot::pool_size * sizeof(SgAsmCilMetadataRoot)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMetadataRoot::pool_size * sizeof(SgAsmCilMetadataRoot));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMetadataRoot::pool_size * sizeof(SgAsmCilMetadataRoot));
#endif
        SgAsmCilMetadataRoot::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMetadataRoot::pool_size * sizeof(SgAsmCilMetadataRoot), V_SgAsmCilMetadataRoot));

        if (SgAsmCilMetadataRoot::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMetadataRoot* blkptr{reinterpret_cast<SgAsmCilMetadataRoot*>(SgAsmCilMetadataRoot::pools[blockIndex-1])};
                blkptr[SgAsmCilMetadataRoot::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMetadataRoot::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMetadataRoot::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMetadataRoot::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMetadataRoot::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMetadataRoot::pool_size - 1};
    SgAsmCilMetadataRoot* testPointer{reinterpret_cast<SgAsmCilMetadataRoot*>(SgAsmCilMetadataRoot::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMetadataRoot::pool_size * (SgAsmCilMetadataRoot::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMetadataRootStroageClass
 * from its counterpart of SgAsmCilMetadataRoot. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMetadataRoot::initializeStorageClassArray(SgAsmCilMetadataRootStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMetadataRoot::pools.begin(); block != SgAsmCilMetadataRoot::pools.end(); ++block) {
        SgAsmCilMetadataRoot* pointer{reinterpret_cast<SgAsmCilMetadataRoot*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMetadataRoot::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 181904 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilMethodData that takes its corresponding StorageClass as parameter
 */
SgAsmCilMethodData::SgAsmCilMethodData(const SgAsmCilMethodDataStorageClass& storageSource)  : SgAsmCilNode (storageSource) {

/* #line 181914 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_clauses = storageSource.storageOf_clauses.rebuildDataStoredInEasyStorageClass();
    p_rawHeader = storageSource.storageOf_rawHeader;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilMethodData*
SgAsmCilMethodData::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilMethodData* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodData)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodData + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilMethodData)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodData)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodData::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodData::pool_size};
        returnPointer = &(((SgAsmCilMethodData*)(SgAsmCilMethodData::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilMethodData*
SgAsmCilMethodData::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilMethodData* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodData)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodData + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilMethodData)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilMethodData)};
        unsigned long positionInPool{localIndex % SgAsmCilMethodData::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilMethodData::pool_size};
        returnPointer = &(((SgAsmCilMethodData*)(SgAsmCilMethodData::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilMethodData::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilMethodData::pools.begin(); block != SgAsmCilMethodData::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodData*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodData::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilMethodData*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilMethodData::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilMethodData* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilMethodData::pools.begin(); block != SgAsmCilMethodData::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilMethodData*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodData::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilMethodData::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilMethodData::clearMemoryPool() {
    SgAsmCilMethodData* tempPointer{nullptr};
    if (SgAsmCilMethodData::pools.empty() == false) {
        auto block{SgAsmCilMethodData::pools.begin()};
        SgAsmCilMethodData::next_node = reinterpret_cast<SgAsmCilMethodData*>(*block);
        for (; block != SgAsmCilMethodData::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilMethodData*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilMethodData::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilMethodData::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilMethodData::pool_size-1]);
        }
    }
}

void
SgAsmCilMethodData::deleteMemoryPool() {
    for (auto p: SgAsmCilMethodData::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilMethodData::next_node = nullptr;
    SgAsmCilMethodData::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilMethodData::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodData) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodData)};
    for (size_t blockIndex = SgAsmCilMethodData::pools.size(); (blockIndex * SgAsmCilMethodData::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodData) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodData) = %" PRIuPTR " SgAsmCilMethodData::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilMethodData),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilMethodData),SgAsmCilMethodData::pool_size);
        }
#endif

        SgAsmCilMethodData* pointer{static_cast<SgAsmCilMethodData*>(ROSE_MALLOC(SgAsmCilMethodData::pool_size * sizeof(SgAsmCilMethodData)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilMethodData::pool_size * sizeof(SgAsmCilMethodData));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilMethodData::pool_size * sizeof(SgAsmCilMethodData));
#endif
        SgAsmCilMethodData::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilMethodData::pool_size * sizeof(SgAsmCilMethodData), V_SgAsmCilMethodData));

        if (SgAsmCilMethodData::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilMethodData* blkptr{reinterpret_cast<SgAsmCilMethodData*>(SgAsmCilMethodData::pools[blockIndex-1])};
                blkptr[SgAsmCilMethodData::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilMethodData::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilMethodData::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilMethodData::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilMethodData::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilMethodData::pool_size - 1};
    SgAsmCilMethodData* testPointer{reinterpret_cast<SgAsmCilMethodData*>(SgAsmCilMethodData::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilMethodData::pool_size * (SgAsmCilMethodData::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilMethodDataStroageClass
 * from its counterpart of SgAsmCilMethodData. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilMethodData::initializeStorageClassArray(SgAsmCilMethodDataStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilMethodData::pools.begin(); block != SgAsmCilMethodData::pools.end(); ++block) {
        SgAsmCilMethodData* pointer{reinterpret_cast<SgAsmCilMethodData*>(*block)};
        for (unsigned i = 0; i < SgAsmCilMethodData::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 182133 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmExecutableFileFormat that takes its corresponding StorageClass as parameter
 */
SgAsmExecutableFileFormat::SgAsmExecutableFileFormat(const SgAsmExecutableFileFormatStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 182143 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmExecutableFileFormat*
SgAsmExecutableFileFormat::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmExecutableFileFormat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExecutableFileFormat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExecutableFileFormat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExecutableFileFormat)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExecutableFileFormat)};
        unsigned long positionInPool{localIndex % SgAsmExecutableFileFormat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmExecutableFileFormat::pool_size};
        returnPointer = &(((SgAsmExecutableFileFormat*)(SgAsmExecutableFileFormat::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmExecutableFileFormat*
SgAsmExecutableFileFormat::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmExecutableFileFormat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExecutableFileFormat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExecutableFileFormat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExecutableFileFormat)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmExecutableFileFormat)};
        unsigned long positionInPool{localIndex % SgAsmExecutableFileFormat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmExecutableFileFormat::pool_size};
        returnPointer = &(((SgAsmExecutableFileFormat*)(SgAsmExecutableFileFormat::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmExecutableFileFormat::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmExecutableFileFormat::pools.begin(); block != SgAsmExecutableFileFormat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmExecutableFileFormat*>(*block)};
        for (unsigned i = 0; i < SgAsmExecutableFileFormat::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmExecutableFileFormat*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmExecutableFileFormat::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmExecutableFileFormat* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmExecutableFileFormat::pools.begin(); block != SgAsmExecutableFileFormat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmExecutableFileFormat*>(*block)};
        for (unsigned i = 0; i < SgAsmExecutableFileFormat::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmExecutableFileFormat::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmExecutableFileFormat::clearMemoryPool() {
    SgAsmExecutableFileFormat* tempPointer{nullptr};
    if (SgAsmExecutableFileFormat::pools.empty() == false) {
        auto block{SgAsmExecutableFileFormat::pools.begin()};
        SgAsmExecutableFileFormat::next_node = reinterpret_cast<SgAsmExecutableFileFormat*>(*block);
        for (; block != SgAsmExecutableFileFormat::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmExecutableFileFormat*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmExecutableFileFormat::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmExecutableFileFormat::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmExecutableFileFormat::pool_size-1]);
        }
    }
}

void
SgAsmExecutableFileFormat::deleteMemoryPool() {
    for (auto p: SgAsmExecutableFileFormat::pools) {
        ROSE_FREE(p);
    }
    SgAsmExecutableFileFormat::next_node = nullptr;
    SgAsmExecutableFileFormat::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmExecutableFileFormat::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExecutableFileFormat) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExecutableFileFormat)};
    for (size_t blockIndex = SgAsmExecutableFileFormat::pools.size(); (blockIndex * SgAsmExecutableFileFormat::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExecutableFileFormat) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExecutableFileFormat) = %" PRIuPTR " SgAsmExecutableFileFormat::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExecutableFileFormat),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExecutableFileFormat),SgAsmExecutableFileFormat::pool_size);
        }
#endif

        SgAsmExecutableFileFormat* pointer{static_cast<SgAsmExecutableFileFormat*>(ROSE_MALLOC(SgAsmExecutableFileFormat::pool_size * sizeof(SgAsmExecutableFileFormat)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmExecutableFileFormat::pool_size * sizeof(SgAsmExecutableFileFormat));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmExecutableFileFormat::pool_size * sizeof(SgAsmExecutableFileFormat));
#endif
        SgAsmExecutableFileFormat::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmExecutableFileFormat::pool_size * sizeof(SgAsmExecutableFileFormat), V_SgAsmExecutableFileFormat));

        if (SgAsmExecutableFileFormat::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmExecutableFileFormat* blkptr{reinterpret_cast<SgAsmExecutableFileFormat*>(SgAsmExecutableFileFormat::pools[blockIndex-1])};
                blkptr[SgAsmExecutableFileFormat::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmExecutableFileFormat::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmExecutableFileFormat::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmExecutableFileFormat::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmExecutableFileFormat::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmExecutableFileFormat::pool_size - 1};
    SgAsmExecutableFileFormat* testPointer{reinterpret_cast<SgAsmExecutableFileFormat*>(SgAsmExecutableFileFormat::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmExecutableFileFormat::pool_size * (SgAsmExecutableFileFormat::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmExecutableFileFormatStroageClass
 * from its counterpart of SgAsmExecutableFileFormat. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmExecutableFileFormat::initializeStorageClassArray(SgAsmExecutableFileFormatStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmExecutableFileFormat::pools.begin(); block != SgAsmExecutableFileFormat::pools.end(); ++block) {
        SgAsmExecutableFileFormat* pointer{reinterpret_cast<SgAsmExecutableFileFormat*>(*block)};
        for (unsigned i = 0; i < SgAsmExecutableFileFormat::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 182360 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCoffSymbolList that takes its corresponding StorageClass as parameter
 */
SgAsmCoffSymbolList::SgAsmCoffSymbolList(const SgAsmCoffSymbolListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 182370 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_symbols = storageSource.storageOf_symbols.rebuildDataStoredInEasyStorageClass();
    SgAsmCoffSymbolPtrList::iterator i_symbols = p_symbols.begin();
    for ( ; i_symbols != p_symbols.end(); ++i_symbols) {
        (*i_symbols) = (SgAsmCoffSymbolPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_symbols)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCoffSymbolList*
SgAsmCoffSymbolList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCoffSymbolList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbolList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbolList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbolList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolList)};
        unsigned long positionInPool{localIndex % SgAsmCoffSymbolList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffSymbolList::pool_size};
        returnPointer = &(((SgAsmCoffSymbolList*)(SgAsmCoffSymbolList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCoffSymbolList*
SgAsmCoffSymbolList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCoffSymbolList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbolList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbolList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbolList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCoffSymbolList)};
        unsigned long positionInPool{localIndex % SgAsmCoffSymbolList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffSymbolList::pool_size};
        returnPointer = &(((SgAsmCoffSymbolList*)(SgAsmCoffSymbolList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCoffSymbolList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCoffSymbolList::pools.begin(); block != SgAsmCoffSymbolList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbolList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCoffSymbolList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCoffSymbolList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCoffSymbolList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCoffSymbolList::pools.begin(); block != SgAsmCoffSymbolList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbolList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCoffSymbolList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCoffSymbolList::clearMemoryPool() {
    SgAsmCoffSymbolList* tempPointer{nullptr};
    if (SgAsmCoffSymbolList::pools.empty() == false) {
        auto block{SgAsmCoffSymbolList::pools.begin()};
        SgAsmCoffSymbolList::next_node = reinterpret_cast<SgAsmCoffSymbolList*>(*block);
        for (; block != SgAsmCoffSymbolList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCoffSymbolList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCoffSymbolList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCoffSymbolList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCoffSymbolList::pool_size-1]);
        }
    }
}

void
SgAsmCoffSymbolList::deleteMemoryPool() {
    for (auto p: SgAsmCoffSymbolList::pools) {
        ROSE_FREE(p);
    }
    SgAsmCoffSymbolList::next_node = nullptr;
    SgAsmCoffSymbolList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCoffSymbolList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbolList)};
    for (size_t blockIndex = SgAsmCoffSymbolList::pools.size(); (blockIndex * SgAsmCoffSymbolList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbolList) = %" PRIuPTR " SgAsmCoffSymbolList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbolList),SgAsmCoffSymbolList::pool_size);
        }
#endif

        SgAsmCoffSymbolList* pointer{static_cast<SgAsmCoffSymbolList*>(ROSE_MALLOC(SgAsmCoffSymbolList::pool_size * sizeof(SgAsmCoffSymbolList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCoffSymbolList::pool_size * sizeof(SgAsmCoffSymbolList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCoffSymbolList::pool_size * sizeof(SgAsmCoffSymbolList));
#endif
        SgAsmCoffSymbolList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCoffSymbolList::pool_size * sizeof(SgAsmCoffSymbolList), V_SgAsmCoffSymbolList));

        if (SgAsmCoffSymbolList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCoffSymbolList* blkptr{reinterpret_cast<SgAsmCoffSymbolList*>(SgAsmCoffSymbolList::pools[blockIndex-1])};
                blkptr[SgAsmCoffSymbolList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCoffSymbolList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCoffSymbolList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCoffSymbolList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCoffSymbolList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCoffSymbolList::pool_size - 1};
    SgAsmCoffSymbolList* testPointer{reinterpret_cast<SgAsmCoffSymbolList*>(SgAsmCoffSymbolList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCoffSymbolList::pool_size * (SgAsmCoffSymbolList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCoffSymbolListStroageClass
 * from its counterpart of SgAsmCoffSymbolList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCoffSymbolList::initializeStorageClassArray(SgAsmCoffSymbolListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCoffSymbolList::pools.begin(); block != SgAsmCoffSymbolList::pools.end(); ++block) {
        SgAsmCoffSymbolList* pointer{reinterpret_cast<SgAsmCoffSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbolList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 182592 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfInformation that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfInformation::SgAsmDwarfInformation(const SgAsmDwarfInformationStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 182602 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfInformation*
SgAsmDwarfInformation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfInformation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInformation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInformation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInformation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInformation)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInformation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInformation::pool_size};
        returnPointer = &(((SgAsmDwarfInformation*)(SgAsmDwarfInformation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfInformation*
SgAsmDwarfInformation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfInformation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInformation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInformation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInformation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfInformation)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInformation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInformation::pool_size};
        returnPointer = &(((SgAsmDwarfInformation*)(SgAsmDwarfInformation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfInformation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfInformation::pools.begin(); block != SgAsmDwarfInformation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInformation*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInformation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfInformation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfInformation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfInformation* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfInformation::pools.begin(); block != SgAsmDwarfInformation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInformation*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInformation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfInformation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfInformation::clearMemoryPool() {
    SgAsmDwarfInformation* tempPointer{nullptr};
    if (SgAsmDwarfInformation::pools.empty() == false) {
        auto block{SgAsmDwarfInformation::pools.begin()};
        SgAsmDwarfInformation::next_node = reinterpret_cast<SgAsmDwarfInformation*>(*block);
        for (; block != SgAsmDwarfInformation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfInformation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfInformation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfInformation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfInformation::pool_size-1]);
        }
    }
}

void
SgAsmDwarfInformation::deleteMemoryPool() {
    for (auto p: SgAsmDwarfInformation::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfInformation::next_node = nullptr;
    SgAsmDwarfInformation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfInformation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInformation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInformation)};
    for (size_t blockIndex = SgAsmDwarfInformation::pools.size(); (blockIndex * SgAsmDwarfInformation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInformation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInformation) = %" PRIuPTR " SgAsmDwarfInformation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInformation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInformation),SgAsmDwarfInformation::pool_size);
        }
#endif

        SgAsmDwarfInformation* pointer{static_cast<SgAsmDwarfInformation*>(ROSE_MALLOC(SgAsmDwarfInformation::pool_size * sizeof(SgAsmDwarfInformation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfInformation::pool_size * sizeof(SgAsmDwarfInformation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfInformation::pool_size * sizeof(SgAsmDwarfInformation));
#endif
        SgAsmDwarfInformation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfInformation::pool_size * sizeof(SgAsmDwarfInformation), V_SgAsmDwarfInformation));

        if (SgAsmDwarfInformation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfInformation* blkptr{reinterpret_cast<SgAsmDwarfInformation*>(SgAsmDwarfInformation::pools[blockIndex-1])};
                blkptr[SgAsmDwarfInformation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfInformation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfInformation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfInformation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfInformation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfInformation::pool_size - 1};
    SgAsmDwarfInformation* testPointer{reinterpret_cast<SgAsmDwarfInformation*>(SgAsmDwarfInformation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfInformation::pool_size * (SgAsmDwarfInformation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfInformationStroageClass
 * from its counterpart of SgAsmDwarfInformation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfInformation::initializeStorageClassArray(SgAsmDwarfInformationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfInformation::pools.begin(); block != SgAsmDwarfInformation::pools.end(); ++block) {
        SgAsmDwarfInformation* pointer{reinterpret_cast<SgAsmDwarfInformation*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInformation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 182819 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfCompilationUnitList that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfCompilationUnitList::SgAsmDwarfCompilationUnitList(const SgAsmDwarfCompilationUnitListStorageClass& storageSource)  : SgAsmDwarfInformation (storageSource) {

/* #line 182829 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_cu_list = storageSource.storageOf_cu_list.rebuildDataStoredInEasyStorageClass();
    SgAsmDwarfCompilationUnitPtrList::iterator i_cu_list = p_cu_list.begin();
    for ( ; i_cu_list != p_cu_list.end(); ++i_cu_list) {
        (*i_cu_list) = (SgAsmDwarfCompilationUnitPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_cu_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfCompilationUnitList*
SgAsmDwarfCompilationUnitList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfCompilationUnitList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnitList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnitList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnitList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnitList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCompilationUnitList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCompilationUnitList::pool_size};
        returnPointer = &(((SgAsmDwarfCompilationUnitList*)(SgAsmDwarfCompilationUnitList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfCompilationUnitList*
SgAsmDwarfCompilationUnitList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfCompilationUnitList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCompilationUnitList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCompilationUnitList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCompilationUnitList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfCompilationUnitList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCompilationUnitList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCompilationUnitList::pool_size};
        returnPointer = &(((SgAsmDwarfCompilationUnitList*)(SgAsmDwarfCompilationUnitList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfCompilationUnitList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfCompilationUnitList::pools.begin(); block != SgAsmDwarfCompilationUnitList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCompilationUnitList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCompilationUnitList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfCompilationUnitList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfCompilationUnitList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfCompilationUnitList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfCompilationUnitList::pools.begin(); block != SgAsmDwarfCompilationUnitList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCompilationUnitList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCompilationUnitList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfCompilationUnitList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfCompilationUnitList::clearMemoryPool() {
    SgAsmDwarfCompilationUnitList* tempPointer{nullptr};
    if (SgAsmDwarfCompilationUnitList::pools.empty() == false) {
        auto block{SgAsmDwarfCompilationUnitList::pools.begin()};
        SgAsmDwarfCompilationUnitList::next_node = reinterpret_cast<SgAsmDwarfCompilationUnitList*>(*block);
        for (; block != SgAsmDwarfCompilationUnitList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfCompilationUnitList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfCompilationUnitList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfCompilationUnitList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfCompilationUnitList::pool_size-1]);
        }
    }
}

void
SgAsmDwarfCompilationUnitList::deleteMemoryPool() {
    for (auto p: SgAsmDwarfCompilationUnitList::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfCompilationUnitList::next_node = nullptr;
    SgAsmDwarfCompilationUnitList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfCompilationUnitList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnitList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnitList)};
    for (size_t blockIndex = SgAsmDwarfCompilationUnitList::pools.size(); (blockIndex * SgAsmDwarfCompilationUnitList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnitList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnitList) = %" PRIuPTR " SgAsmDwarfCompilationUnitList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnitList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnitList),SgAsmDwarfCompilationUnitList::pool_size);
        }
#endif

        SgAsmDwarfCompilationUnitList* pointer{static_cast<SgAsmDwarfCompilationUnitList*>(ROSE_MALLOC(SgAsmDwarfCompilationUnitList::pool_size * sizeof(SgAsmDwarfCompilationUnitList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfCompilationUnitList::pool_size * sizeof(SgAsmDwarfCompilationUnitList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfCompilationUnitList::pool_size * sizeof(SgAsmDwarfCompilationUnitList));
#endif
        SgAsmDwarfCompilationUnitList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfCompilationUnitList::pool_size * sizeof(SgAsmDwarfCompilationUnitList), V_SgAsmDwarfCompilationUnitList));

        if (SgAsmDwarfCompilationUnitList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfCompilationUnitList* blkptr{reinterpret_cast<SgAsmDwarfCompilationUnitList*>(SgAsmDwarfCompilationUnitList::pools[blockIndex-1])};
                blkptr[SgAsmDwarfCompilationUnitList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfCompilationUnitList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfCompilationUnitList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfCompilationUnitList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfCompilationUnitList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfCompilationUnitList::pool_size - 1};
    SgAsmDwarfCompilationUnitList* testPointer{reinterpret_cast<SgAsmDwarfCompilationUnitList*>(SgAsmDwarfCompilationUnitList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfCompilationUnitList::pool_size * (SgAsmDwarfCompilationUnitList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfCompilationUnitListStroageClass
 * from its counterpart of SgAsmDwarfCompilationUnitList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfCompilationUnitList::initializeStorageClassArray(SgAsmDwarfCompilationUnitListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfCompilationUnitList::pools.begin(); block != SgAsmDwarfCompilationUnitList::pools.end(); ++block) {
        SgAsmDwarfCompilationUnitList* pointer{reinterpret_cast<SgAsmDwarfCompilationUnitList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCompilationUnitList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 183051 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfConstruct that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfConstruct::SgAsmDwarfConstruct(const SgAsmDwarfConstructStorageClass& storageSource)  : SgAsmDwarfInformation (storageSource) {

/* #line 183061 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_nesting_level = storageSource.storageOf_nesting_level;
    p_offset = storageSource.storageOf_offset;
    p_overall_offset = storageSource.storageOf_overall_offset;
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
     p_source_position = (SgAsmDwarfLine*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_source_position));
    p_decl_file_id = storageSource.storageOf_decl_file_id;
    p_decl_line = storageSource.storageOf_decl_line;
    p_decl_column = storageSource.storageOf_decl_column;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfConstruct*
SgAsmDwarfConstruct::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfConstruct* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstruct)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstruct + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstruct)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstruct)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstruct::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstruct::pool_size};
        returnPointer = &(((SgAsmDwarfConstruct*)(SgAsmDwarfConstruct::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfConstruct*
SgAsmDwarfConstruct::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfConstruct* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstruct)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstruct + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstruct)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfConstruct)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstruct::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstruct::pool_size};
        returnPointer = &(((SgAsmDwarfConstruct*)(SgAsmDwarfConstruct::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfConstruct::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfConstruct::pools.begin(); block != SgAsmDwarfConstruct::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstruct*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstruct::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfConstruct*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfConstruct::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfConstruct* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfConstruct::pools.begin(); block != SgAsmDwarfConstruct::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstruct*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstruct::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfConstruct::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfConstruct::clearMemoryPool() {
    SgAsmDwarfConstruct* tempPointer{nullptr};
    if (SgAsmDwarfConstruct::pools.empty() == false) {
        auto block{SgAsmDwarfConstruct::pools.begin()};
        SgAsmDwarfConstruct::next_node = reinterpret_cast<SgAsmDwarfConstruct*>(*block);
        for (; block != SgAsmDwarfConstruct::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfConstruct*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfConstruct::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfConstruct::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfConstruct::pool_size-1]);
        }
    }
}

void
SgAsmDwarfConstruct::deleteMemoryPool() {
    for (auto p: SgAsmDwarfConstruct::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfConstruct::next_node = nullptr;
    SgAsmDwarfConstruct::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfConstruct::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstruct) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstruct)};
    for (size_t blockIndex = SgAsmDwarfConstruct::pools.size(); (blockIndex * SgAsmDwarfConstruct::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstruct) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstruct) = %" PRIuPTR " SgAsmDwarfConstruct::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstruct),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstruct),SgAsmDwarfConstruct::pool_size);
        }
#endif

        SgAsmDwarfConstruct* pointer{static_cast<SgAsmDwarfConstruct*>(ROSE_MALLOC(SgAsmDwarfConstruct::pool_size * sizeof(SgAsmDwarfConstruct)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfConstruct::pool_size * sizeof(SgAsmDwarfConstruct));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfConstruct::pool_size * sizeof(SgAsmDwarfConstruct));
#endif
        SgAsmDwarfConstruct::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfConstruct::pool_size * sizeof(SgAsmDwarfConstruct), V_SgAsmDwarfConstruct));

        if (SgAsmDwarfConstruct::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfConstruct* blkptr{reinterpret_cast<SgAsmDwarfConstruct*>(SgAsmDwarfConstruct::pools[blockIndex-1])};
                blkptr[SgAsmDwarfConstruct::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfConstruct::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfConstruct::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfConstruct::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfConstruct::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfConstruct::pool_size - 1};
    SgAsmDwarfConstruct* testPointer{reinterpret_cast<SgAsmDwarfConstruct*>(SgAsmDwarfConstruct::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfConstruct::pool_size * (SgAsmDwarfConstruct::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfConstructStroageClass
 * from its counterpart of SgAsmDwarfConstruct. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfConstruct::initializeStorageClassArray(SgAsmDwarfConstructStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfConstruct::pools.begin(); block != SgAsmDwarfConstruct::pools.end(); ++block) {
        SgAsmDwarfConstruct* pointer{reinterpret_cast<SgAsmDwarfConstruct*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstruct::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 183286 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfAccessDeclaration that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfAccessDeclaration::SgAsmDwarfAccessDeclaration(const SgAsmDwarfAccessDeclarationStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 183296 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfAccessDeclaration*
SgAsmDwarfAccessDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfAccessDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfAccessDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfAccessDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfAccessDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfAccessDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmDwarfAccessDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfAccessDeclaration::pool_size};
        returnPointer = &(((SgAsmDwarfAccessDeclaration*)(SgAsmDwarfAccessDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfAccessDeclaration*
SgAsmDwarfAccessDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfAccessDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfAccessDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfAccessDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfAccessDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfAccessDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmDwarfAccessDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfAccessDeclaration::pool_size};
        returnPointer = &(((SgAsmDwarfAccessDeclaration*)(SgAsmDwarfAccessDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfAccessDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfAccessDeclaration::pools.begin(); block != SgAsmDwarfAccessDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfAccessDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfAccessDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfAccessDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfAccessDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfAccessDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfAccessDeclaration::pools.begin(); block != SgAsmDwarfAccessDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfAccessDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfAccessDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfAccessDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfAccessDeclaration::clearMemoryPool() {
    SgAsmDwarfAccessDeclaration* tempPointer{nullptr};
    if (SgAsmDwarfAccessDeclaration::pools.empty() == false) {
        auto block{SgAsmDwarfAccessDeclaration::pools.begin()};
        SgAsmDwarfAccessDeclaration::next_node = reinterpret_cast<SgAsmDwarfAccessDeclaration*>(*block);
        for (; block != SgAsmDwarfAccessDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfAccessDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfAccessDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfAccessDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfAccessDeclaration::pool_size-1]);
        }
    }
}

void
SgAsmDwarfAccessDeclaration::deleteMemoryPool() {
    for (auto p: SgAsmDwarfAccessDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfAccessDeclaration::next_node = nullptr;
    SgAsmDwarfAccessDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfAccessDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfAccessDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfAccessDeclaration)};
    for (size_t blockIndex = SgAsmDwarfAccessDeclaration::pools.size(); (blockIndex * SgAsmDwarfAccessDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfAccessDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfAccessDeclaration) = %" PRIuPTR " SgAsmDwarfAccessDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfAccessDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfAccessDeclaration),SgAsmDwarfAccessDeclaration::pool_size);
        }
#endif

        SgAsmDwarfAccessDeclaration* pointer{static_cast<SgAsmDwarfAccessDeclaration*>(ROSE_MALLOC(SgAsmDwarfAccessDeclaration::pool_size * sizeof(SgAsmDwarfAccessDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfAccessDeclaration::pool_size * sizeof(SgAsmDwarfAccessDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfAccessDeclaration::pool_size * sizeof(SgAsmDwarfAccessDeclaration));
#endif
        SgAsmDwarfAccessDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfAccessDeclaration::pool_size * sizeof(SgAsmDwarfAccessDeclaration), V_SgAsmDwarfAccessDeclaration));

        if (SgAsmDwarfAccessDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfAccessDeclaration* blkptr{reinterpret_cast<SgAsmDwarfAccessDeclaration*>(SgAsmDwarfAccessDeclaration::pools[blockIndex-1])};
                blkptr[SgAsmDwarfAccessDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfAccessDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfAccessDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfAccessDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfAccessDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfAccessDeclaration::pool_size - 1};
    SgAsmDwarfAccessDeclaration* testPointer{reinterpret_cast<SgAsmDwarfAccessDeclaration*>(SgAsmDwarfAccessDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfAccessDeclaration::pool_size * (SgAsmDwarfAccessDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfAccessDeclarationStroageClass
 * from its counterpart of SgAsmDwarfAccessDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfAccessDeclaration::initializeStorageClassArray(SgAsmDwarfAccessDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfAccessDeclaration::pools.begin(); block != SgAsmDwarfAccessDeclaration::pools.end(); ++block) {
        SgAsmDwarfAccessDeclaration* pointer{reinterpret_cast<SgAsmDwarfAccessDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfAccessDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 183513 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfArrayType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfArrayType::SgAsmDwarfArrayType(const SgAsmDwarfArrayTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 183523 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfArrayType*
SgAsmDwarfArrayType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfArrayType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfArrayType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfArrayType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfArrayType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfArrayType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfArrayType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfArrayType::pool_size};
        returnPointer = &(((SgAsmDwarfArrayType*)(SgAsmDwarfArrayType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfArrayType*
SgAsmDwarfArrayType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfArrayType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfArrayType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfArrayType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfArrayType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfArrayType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfArrayType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfArrayType::pool_size};
        returnPointer = &(((SgAsmDwarfArrayType*)(SgAsmDwarfArrayType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfArrayType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfArrayType::pools.begin(); block != SgAsmDwarfArrayType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfArrayType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfArrayType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfArrayType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfArrayType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfArrayType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfArrayType::pools.begin(); block != SgAsmDwarfArrayType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfArrayType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfArrayType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfArrayType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfArrayType::clearMemoryPool() {
    SgAsmDwarfArrayType* tempPointer{nullptr};
    if (SgAsmDwarfArrayType::pools.empty() == false) {
        auto block{SgAsmDwarfArrayType::pools.begin()};
        SgAsmDwarfArrayType::next_node = reinterpret_cast<SgAsmDwarfArrayType*>(*block);
        for (; block != SgAsmDwarfArrayType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfArrayType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfArrayType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfArrayType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfArrayType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfArrayType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfArrayType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfArrayType::next_node = nullptr;
    SgAsmDwarfArrayType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfArrayType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfArrayType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfArrayType)};
    for (size_t blockIndex = SgAsmDwarfArrayType::pools.size(); (blockIndex * SgAsmDwarfArrayType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfArrayType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfArrayType) = %" PRIuPTR " SgAsmDwarfArrayType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfArrayType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfArrayType),SgAsmDwarfArrayType::pool_size);
        }
#endif

        SgAsmDwarfArrayType* pointer{static_cast<SgAsmDwarfArrayType*>(ROSE_MALLOC(SgAsmDwarfArrayType::pool_size * sizeof(SgAsmDwarfArrayType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfArrayType::pool_size * sizeof(SgAsmDwarfArrayType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfArrayType::pool_size * sizeof(SgAsmDwarfArrayType));
#endif
        SgAsmDwarfArrayType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfArrayType::pool_size * sizeof(SgAsmDwarfArrayType), V_SgAsmDwarfArrayType));

        if (SgAsmDwarfArrayType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfArrayType* blkptr{reinterpret_cast<SgAsmDwarfArrayType*>(SgAsmDwarfArrayType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfArrayType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfArrayType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfArrayType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfArrayType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfArrayType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfArrayType::pool_size - 1};
    SgAsmDwarfArrayType* testPointer{reinterpret_cast<SgAsmDwarfArrayType*>(SgAsmDwarfArrayType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfArrayType::pool_size * (SgAsmDwarfArrayType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfArrayTypeStroageClass
 * from its counterpart of SgAsmDwarfArrayType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfArrayType::initializeStorageClassArray(SgAsmDwarfArrayTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfArrayType::pools.begin(); block != SgAsmDwarfArrayType::pools.end(); ++block) {
        SgAsmDwarfArrayType* pointer{reinterpret_cast<SgAsmDwarfArrayType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfArrayType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 183741 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfBaseType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfBaseType::SgAsmDwarfBaseType(const SgAsmDwarfBaseTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 183751 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfBaseType*
SgAsmDwarfBaseType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfBaseType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfBaseType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfBaseType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfBaseType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfBaseType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfBaseType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfBaseType::pool_size};
        returnPointer = &(((SgAsmDwarfBaseType*)(SgAsmDwarfBaseType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfBaseType*
SgAsmDwarfBaseType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfBaseType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfBaseType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfBaseType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfBaseType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfBaseType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfBaseType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfBaseType::pool_size};
        returnPointer = &(((SgAsmDwarfBaseType*)(SgAsmDwarfBaseType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfBaseType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfBaseType::pools.begin(); block != SgAsmDwarfBaseType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfBaseType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfBaseType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfBaseType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfBaseType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfBaseType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfBaseType::pools.begin(); block != SgAsmDwarfBaseType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfBaseType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfBaseType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfBaseType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfBaseType::clearMemoryPool() {
    SgAsmDwarfBaseType* tempPointer{nullptr};
    if (SgAsmDwarfBaseType::pools.empty() == false) {
        auto block{SgAsmDwarfBaseType::pools.begin()};
        SgAsmDwarfBaseType::next_node = reinterpret_cast<SgAsmDwarfBaseType*>(*block);
        for (; block != SgAsmDwarfBaseType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfBaseType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfBaseType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfBaseType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfBaseType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfBaseType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfBaseType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfBaseType::next_node = nullptr;
    SgAsmDwarfBaseType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfBaseType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfBaseType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfBaseType)};
    for (size_t blockIndex = SgAsmDwarfBaseType::pools.size(); (blockIndex * SgAsmDwarfBaseType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfBaseType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfBaseType) = %" PRIuPTR " SgAsmDwarfBaseType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfBaseType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfBaseType),SgAsmDwarfBaseType::pool_size);
        }
#endif

        SgAsmDwarfBaseType* pointer{static_cast<SgAsmDwarfBaseType*>(ROSE_MALLOC(SgAsmDwarfBaseType::pool_size * sizeof(SgAsmDwarfBaseType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfBaseType::pool_size * sizeof(SgAsmDwarfBaseType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfBaseType::pool_size * sizeof(SgAsmDwarfBaseType));
#endif
        SgAsmDwarfBaseType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfBaseType::pool_size * sizeof(SgAsmDwarfBaseType), V_SgAsmDwarfBaseType));

        if (SgAsmDwarfBaseType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfBaseType* blkptr{reinterpret_cast<SgAsmDwarfBaseType*>(SgAsmDwarfBaseType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfBaseType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfBaseType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfBaseType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfBaseType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfBaseType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfBaseType::pool_size - 1};
    SgAsmDwarfBaseType* testPointer{reinterpret_cast<SgAsmDwarfBaseType*>(SgAsmDwarfBaseType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfBaseType::pool_size * (SgAsmDwarfBaseType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfBaseTypeStroageClass
 * from its counterpart of SgAsmDwarfBaseType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfBaseType::initializeStorageClassArray(SgAsmDwarfBaseTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfBaseType::pools.begin(); block != SgAsmDwarfBaseType::pools.end(); ++block) {
        SgAsmDwarfBaseType* pointer{reinterpret_cast<SgAsmDwarfBaseType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfBaseType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 183968 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfCatchBlock that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfCatchBlock::SgAsmDwarfCatchBlock(const SgAsmDwarfCatchBlockStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 183978 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfCatchBlock*
SgAsmDwarfCatchBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfCatchBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCatchBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCatchBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCatchBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCatchBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCatchBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCatchBlock::pool_size};
        returnPointer = &(((SgAsmDwarfCatchBlock*)(SgAsmDwarfCatchBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfCatchBlock*
SgAsmDwarfCatchBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfCatchBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCatchBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCatchBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCatchBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfCatchBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCatchBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCatchBlock::pool_size};
        returnPointer = &(((SgAsmDwarfCatchBlock*)(SgAsmDwarfCatchBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfCatchBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfCatchBlock::pools.begin(); block != SgAsmDwarfCatchBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCatchBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCatchBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfCatchBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfCatchBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfCatchBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfCatchBlock::pools.begin(); block != SgAsmDwarfCatchBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCatchBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCatchBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfCatchBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfCatchBlock::clearMemoryPool() {
    SgAsmDwarfCatchBlock* tempPointer{nullptr};
    if (SgAsmDwarfCatchBlock::pools.empty() == false) {
        auto block{SgAsmDwarfCatchBlock::pools.begin()};
        SgAsmDwarfCatchBlock::next_node = reinterpret_cast<SgAsmDwarfCatchBlock*>(*block);
        for (; block != SgAsmDwarfCatchBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfCatchBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfCatchBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfCatchBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfCatchBlock::pool_size-1]);
        }
    }
}

void
SgAsmDwarfCatchBlock::deleteMemoryPool() {
    for (auto p: SgAsmDwarfCatchBlock::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfCatchBlock::next_node = nullptr;
    SgAsmDwarfCatchBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfCatchBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCatchBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCatchBlock)};
    for (size_t blockIndex = SgAsmDwarfCatchBlock::pools.size(); (blockIndex * SgAsmDwarfCatchBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCatchBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCatchBlock) = %" PRIuPTR " SgAsmDwarfCatchBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCatchBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCatchBlock),SgAsmDwarfCatchBlock::pool_size);
        }
#endif

        SgAsmDwarfCatchBlock* pointer{static_cast<SgAsmDwarfCatchBlock*>(ROSE_MALLOC(SgAsmDwarfCatchBlock::pool_size * sizeof(SgAsmDwarfCatchBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfCatchBlock::pool_size * sizeof(SgAsmDwarfCatchBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfCatchBlock::pool_size * sizeof(SgAsmDwarfCatchBlock));
#endif
        SgAsmDwarfCatchBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfCatchBlock::pool_size * sizeof(SgAsmDwarfCatchBlock), V_SgAsmDwarfCatchBlock));

        if (SgAsmDwarfCatchBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfCatchBlock* blkptr{reinterpret_cast<SgAsmDwarfCatchBlock*>(SgAsmDwarfCatchBlock::pools[blockIndex-1])};
                blkptr[SgAsmDwarfCatchBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfCatchBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfCatchBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfCatchBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfCatchBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfCatchBlock::pool_size - 1};
    SgAsmDwarfCatchBlock* testPointer{reinterpret_cast<SgAsmDwarfCatchBlock*>(SgAsmDwarfCatchBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfCatchBlock::pool_size * (SgAsmDwarfCatchBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfCatchBlockStroageClass
 * from its counterpart of SgAsmDwarfCatchBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfCatchBlock::initializeStorageClassArray(SgAsmDwarfCatchBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfCatchBlock::pools.begin(); block != SgAsmDwarfCatchBlock::pools.end(); ++block) {
        SgAsmDwarfCatchBlock* pointer{reinterpret_cast<SgAsmDwarfCatchBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCatchBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 184195 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfClassTemplate that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfClassTemplate::SgAsmDwarfClassTemplate(const SgAsmDwarfClassTemplateStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 184205 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfClassTemplate*
SgAsmDwarfClassTemplate::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfClassTemplate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfClassTemplate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfClassTemplate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfClassTemplate)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassTemplate)};
        unsigned long positionInPool{localIndex % SgAsmDwarfClassTemplate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfClassTemplate::pool_size};
        returnPointer = &(((SgAsmDwarfClassTemplate*)(SgAsmDwarfClassTemplate::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfClassTemplate*
SgAsmDwarfClassTemplate::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfClassTemplate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfClassTemplate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfClassTemplate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfClassTemplate)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfClassTemplate)};
        unsigned long positionInPool{localIndex % SgAsmDwarfClassTemplate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfClassTemplate::pool_size};
        returnPointer = &(((SgAsmDwarfClassTemplate*)(SgAsmDwarfClassTemplate::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfClassTemplate::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfClassTemplate::pools.begin(); block != SgAsmDwarfClassTemplate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfClassTemplate*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfClassTemplate::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfClassTemplate*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfClassTemplate::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfClassTemplate* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfClassTemplate::pools.begin(); block != SgAsmDwarfClassTemplate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfClassTemplate*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfClassTemplate::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfClassTemplate::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfClassTemplate::clearMemoryPool() {
    SgAsmDwarfClassTemplate* tempPointer{nullptr};
    if (SgAsmDwarfClassTemplate::pools.empty() == false) {
        auto block{SgAsmDwarfClassTemplate::pools.begin()};
        SgAsmDwarfClassTemplate::next_node = reinterpret_cast<SgAsmDwarfClassTemplate*>(*block);
        for (; block != SgAsmDwarfClassTemplate::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfClassTemplate*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfClassTemplate::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfClassTemplate::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfClassTemplate::pool_size-1]);
        }
    }
}

void
SgAsmDwarfClassTemplate::deleteMemoryPool() {
    for (auto p: SgAsmDwarfClassTemplate::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfClassTemplate::next_node = nullptr;
    SgAsmDwarfClassTemplate::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfClassTemplate::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassTemplate) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfClassTemplate)};
    for (size_t blockIndex = SgAsmDwarfClassTemplate::pools.size(); (blockIndex * SgAsmDwarfClassTemplate::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassTemplate) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfClassTemplate) = %" PRIuPTR " SgAsmDwarfClassTemplate::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassTemplate),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfClassTemplate),SgAsmDwarfClassTemplate::pool_size);
        }
#endif

        SgAsmDwarfClassTemplate* pointer{static_cast<SgAsmDwarfClassTemplate*>(ROSE_MALLOC(SgAsmDwarfClassTemplate::pool_size * sizeof(SgAsmDwarfClassTemplate)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfClassTemplate::pool_size * sizeof(SgAsmDwarfClassTemplate));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfClassTemplate::pool_size * sizeof(SgAsmDwarfClassTemplate));
#endif
        SgAsmDwarfClassTemplate::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfClassTemplate::pool_size * sizeof(SgAsmDwarfClassTemplate), V_SgAsmDwarfClassTemplate));

        if (SgAsmDwarfClassTemplate::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfClassTemplate* blkptr{reinterpret_cast<SgAsmDwarfClassTemplate*>(SgAsmDwarfClassTemplate::pools[blockIndex-1])};
                blkptr[SgAsmDwarfClassTemplate::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfClassTemplate::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfClassTemplate::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfClassTemplate::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfClassTemplate::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfClassTemplate::pool_size - 1};
    SgAsmDwarfClassTemplate* testPointer{reinterpret_cast<SgAsmDwarfClassTemplate*>(SgAsmDwarfClassTemplate::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfClassTemplate::pool_size * (SgAsmDwarfClassTemplate::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfClassTemplateStroageClass
 * from its counterpart of SgAsmDwarfClassTemplate. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfClassTemplate::initializeStorageClassArray(SgAsmDwarfClassTemplateStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfClassTemplate::pools.begin(); block != SgAsmDwarfClassTemplate::pools.end(); ++block) {
        SgAsmDwarfClassTemplate* pointer{reinterpret_cast<SgAsmDwarfClassTemplate*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfClassTemplate::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 184422 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfClassType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfClassType::SgAsmDwarfClassType(const SgAsmDwarfClassTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 184432 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfClassType*
SgAsmDwarfClassType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfClassType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfClassType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfClassType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfClassType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfClassType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfClassType::pool_size};
        returnPointer = &(((SgAsmDwarfClassType*)(SgAsmDwarfClassType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfClassType*
SgAsmDwarfClassType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfClassType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfClassType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfClassType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfClassType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfClassType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfClassType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfClassType::pool_size};
        returnPointer = &(((SgAsmDwarfClassType*)(SgAsmDwarfClassType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfClassType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfClassType::pools.begin(); block != SgAsmDwarfClassType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfClassType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfClassType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfClassType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfClassType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfClassType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfClassType::pools.begin(); block != SgAsmDwarfClassType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfClassType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfClassType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfClassType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfClassType::clearMemoryPool() {
    SgAsmDwarfClassType* tempPointer{nullptr};
    if (SgAsmDwarfClassType::pools.empty() == false) {
        auto block{SgAsmDwarfClassType::pools.begin()};
        SgAsmDwarfClassType::next_node = reinterpret_cast<SgAsmDwarfClassType*>(*block);
        for (; block != SgAsmDwarfClassType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfClassType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfClassType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfClassType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfClassType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfClassType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfClassType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfClassType::next_node = nullptr;
    SgAsmDwarfClassType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfClassType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfClassType)};
    for (size_t blockIndex = SgAsmDwarfClassType::pools.size(); (blockIndex * SgAsmDwarfClassType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfClassType) = %" PRIuPTR " SgAsmDwarfClassType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfClassType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfClassType),SgAsmDwarfClassType::pool_size);
        }
#endif

        SgAsmDwarfClassType* pointer{static_cast<SgAsmDwarfClassType*>(ROSE_MALLOC(SgAsmDwarfClassType::pool_size * sizeof(SgAsmDwarfClassType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfClassType::pool_size * sizeof(SgAsmDwarfClassType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfClassType::pool_size * sizeof(SgAsmDwarfClassType));
#endif
        SgAsmDwarfClassType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfClassType::pool_size * sizeof(SgAsmDwarfClassType), V_SgAsmDwarfClassType));

        if (SgAsmDwarfClassType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfClassType* blkptr{reinterpret_cast<SgAsmDwarfClassType*>(SgAsmDwarfClassType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfClassType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfClassType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfClassType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfClassType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfClassType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfClassType::pool_size - 1};
    SgAsmDwarfClassType* testPointer{reinterpret_cast<SgAsmDwarfClassType*>(SgAsmDwarfClassType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfClassType::pool_size * (SgAsmDwarfClassType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfClassTypeStroageClass
 * from its counterpart of SgAsmDwarfClassType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfClassType::initializeStorageClassArray(SgAsmDwarfClassTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfClassType::pools.begin(); block != SgAsmDwarfClassType::pools.end(); ++block) {
        SgAsmDwarfClassType* pointer{reinterpret_cast<SgAsmDwarfClassType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfClassType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 184650 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfCommonBlock that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfCommonBlock::SgAsmDwarfCommonBlock(const SgAsmDwarfCommonBlockStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 184660 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfCommonBlock*
SgAsmDwarfCommonBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfCommonBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCommonBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCommonBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCommonBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCommonBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCommonBlock::pool_size};
        returnPointer = &(((SgAsmDwarfCommonBlock*)(SgAsmDwarfCommonBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfCommonBlock*
SgAsmDwarfCommonBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfCommonBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCommonBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCommonBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCommonBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfCommonBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCommonBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCommonBlock::pool_size};
        returnPointer = &(((SgAsmDwarfCommonBlock*)(SgAsmDwarfCommonBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfCommonBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfCommonBlock::pools.begin(); block != SgAsmDwarfCommonBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCommonBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCommonBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfCommonBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfCommonBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfCommonBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfCommonBlock::pools.begin(); block != SgAsmDwarfCommonBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCommonBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCommonBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfCommonBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfCommonBlock::clearMemoryPool() {
    SgAsmDwarfCommonBlock* tempPointer{nullptr};
    if (SgAsmDwarfCommonBlock::pools.empty() == false) {
        auto block{SgAsmDwarfCommonBlock::pools.begin()};
        SgAsmDwarfCommonBlock::next_node = reinterpret_cast<SgAsmDwarfCommonBlock*>(*block);
        for (; block != SgAsmDwarfCommonBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfCommonBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfCommonBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfCommonBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfCommonBlock::pool_size-1]);
        }
    }
}

void
SgAsmDwarfCommonBlock::deleteMemoryPool() {
    for (auto p: SgAsmDwarfCommonBlock::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfCommonBlock::next_node = nullptr;
    SgAsmDwarfCommonBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfCommonBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCommonBlock)};
    for (size_t blockIndex = SgAsmDwarfCommonBlock::pools.size(); (blockIndex * SgAsmDwarfCommonBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCommonBlock) = %" PRIuPTR " SgAsmDwarfCommonBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCommonBlock),SgAsmDwarfCommonBlock::pool_size);
        }
#endif

        SgAsmDwarfCommonBlock* pointer{static_cast<SgAsmDwarfCommonBlock*>(ROSE_MALLOC(SgAsmDwarfCommonBlock::pool_size * sizeof(SgAsmDwarfCommonBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfCommonBlock::pool_size * sizeof(SgAsmDwarfCommonBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfCommonBlock::pool_size * sizeof(SgAsmDwarfCommonBlock));
#endif
        SgAsmDwarfCommonBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfCommonBlock::pool_size * sizeof(SgAsmDwarfCommonBlock), V_SgAsmDwarfCommonBlock));

        if (SgAsmDwarfCommonBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfCommonBlock* blkptr{reinterpret_cast<SgAsmDwarfCommonBlock*>(SgAsmDwarfCommonBlock::pools[blockIndex-1])};
                blkptr[SgAsmDwarfCommonBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfCommonBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfCommonBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfCommonBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfCommonBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfCommonBlock::pool_size - 1};
    SgAsmDwarfCommonBlock* testPointer{reinterpret_cast<SgAsmDwarfCommonBlock*>(SgAsmDwarfCommonBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfCommonBlock::pool_size * (SgAsmDwarfCommonBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfCommonBlockStroageClass
 * from its counterpart of SgAsmDwarfCommonBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfCommonBlock::initializeStorageClassArray(SgAsmDwarfCommonBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfCommonBlock::pools.begin(); block != SgAsmDwarfCommonBlock::pools.end(); ++block) {
        SgAsmDwarfCommonBlock* pointer{reinterpret_cast<SgAsmDwarfCommonBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCommonBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 184878 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfCommonInclusion that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfCommonInclusion::SgAsmDwarfCommonInclusion(const SgAsmDwarfCommonInclusionStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 184888 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfCommonInclusion*
SgAsmDwarfCommonInclusion::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfCommonInclusion* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCommonInclusion)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCommonInclusion + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCommonInclusion)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonInclusion)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCommonInclusion::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCommonInclusion::pool_size};
        returnPointer = &(((SgAsmDwarfCommonInclusion*)(SgAsmDwarfCommonInclusion::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfCommonInclusion*
SgAsmDwarfCommonInclusion::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfCommonInclusion* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCommonInclusion)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCommonInclusion + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCommonInclusion)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfCommonInclusion)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCommonInclusion::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCommonInclusion::pool_size};
        returnPointer = &(((SgAsmDwarfCommonInclusion*)(SgAsmDwarfCommonInclusion::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfCommonInclusion::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfCommonInclusion::pools.begin(); block != SgAsmDwarfCommonInclusion::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCommonInclusion*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCommonInclusion::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfCommonInclusion*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfCommonInclusion::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfCommonInclusion* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfCommonInclusion::pools.begin(); block != SgAsmDwarfCommonInclusion::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCommonInclusion*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCommonInclusion::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfCommonInclusion::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfCommonInclusion::clearMemoryPool() {
    SgAsmDwarfCommonInclusion* tempPointer{nullptr};
    if (SgAsmDwarfCommonInclusion::pools.empty() == false) {
        auto block{SgAsmDwarfCommonInclusion::pools.begin()};
        SgAsmDwarfCommonInclusion::next_node = reinterpret_cast<SgAsmDwarfCommonInclusion*>(*block);
        for (; block != SgAsmDwarfCommonInclusion::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfCommonInclusion*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfCommonInclusion::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfCommonInclusion::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfCommonInclusion::pool_size-1]);
        }
    }
}

void
SgAsmDwarfCommonInclusion::deleteMemoryPool() {
    for (auto p: SgAsmDwarfCommonInclusion::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfCommonInclusion::next_node = nullptr;
    SgAsmDwarfCommonInclusion::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfCommonInclusion::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonInclusion) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCommonInclusion)};
    for (size_t blockIndex = SgAsmDwarfCommonInclusion::pools.size(); (blockIndex * SgAsmDwarfCommonInclusion::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonInclusion) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCommonInclusion) = %" PRIuPTR " SgAsmDwarfCommonInclusion::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCommonInclusion),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCommonInclusion),SgAsmDwarfCommonInclusion::pool_size);
        }
#endif

        SgAsmDwarfCommonInclusion* pointer{static_cast<SgAsmDwarfCommonInclusion*>(ROSE_MALLOC(SgAsmDwarfCommonInclusion::pool_size * sizeof(SgAsmDwarfCommonInclusion)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfCommonInclusion::pool_size * sizeof(SgAsmDwarfCommonInclusion));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfCommonInclusion::pool_size * sizeof(SgAsmDwarfCommonInclusion));
#endif
        SgAsmDwarfCommonInclusion::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfCommonInclusion::pool_size * sizeof(SgAsmDwarfCommonInclusion), V_SgAsmDwarfCommonInclusion));

        if (SgAsmDwarfCommonInclusion::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfCommonInclusion* blkptr{reinterpret_cast<SgAsmDwarfCommonInclusion*>(SgAsmDwarfCommonInclusion::pools[blockIndex-1])};
                blkptr[SgAsmDwarfCommonInclusion::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfCommonInclusion::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfCommonInclusion::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfCommonInclusion::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfCommonInclusion::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfCommonInclusion::pool_size - 1};
    SgAsmDwarfCommonInclusion* testPointer{reinterpret_cast<SgAsmDwarfCommonInclusion*>(SgAsmDwarfCommonInclusion::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfCommonInclusion::pool_size * (SgAsmDwarfCommonInclusion::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfCommonInclusionStroageClass
 * from its counterpart of SgAsmDwarfCommonInclusion. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfCommonInclusion::initializeStorageClassArray(SgAsmDwarfCommonInclusionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfCommonInclusion::pools.begin(); block != SgAsmDwarfCommonInclusion::pools.end(); ++block) {
        SgAsmDwarfCommonInclusion* pointer{reinterpret_cast<SgAsmDwarfCommonInclusion*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCommonInclusion::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 185105 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfCompilationUnit that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfCompilationUnit::SgAsmDwarfCompilationUnit(const SgAsmDwarfCompilationUnitStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 185115 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_producer = storageSource.storageOf_producer.rebuildDataStoredInEasyStorageClass();
    p_language = storageSource.storageOf_language.rebuildDataStoredInEasyStorageClass();
    p_low_pc = storageSource.storageOf_low_pc;
    p_hi_pc = storageSource.storageOf_hi_pc;
    p_version_stamp = storageSource.storageOf_version_stamp;
    p_abbrev_offset = storageSource.storageOf_abbrev_offset;
    p_address_size = storageSource.storageOf_address_size;
    p_offset_length = storageSource.storageOf_offset_length;
    p_type_signature = storageSource.storageOf_type_signature.rebuildDataStoredInEasyStorageClass();
    p_comp_dir = storageSource.storageOf_comp_dir.rebuildDataStoredInEasyStorageClass();
     p_line_info = (SgAsmDwarfLineList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_line_info));
     p_language_constructs = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_language_constructs));
     p_macro_info = (SgAsmDwarfMacroList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_macro_info));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfCompilationUnit*
SgAsmDwarfCompilationUnit::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfCompilationUnit* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnit)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnit + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnit)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnit)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCompilationUnit::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCompilationUnit::pool_size};
        returnPointer = &(((SgAsmDwarfCompilationUnit*)(SgAsmDwarfCompilationUnit::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfCompilationUnit*
SgAsmDwarfCompilationUnit::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfCompilationUnit* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCompilationUnit)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCompilationUnit + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCompilationUnit)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfCompilationUnit)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCompilationUnit::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCompilationUnit::pool_size};
        returnPointer = &(((SgAsmDwarfCompilationUnit*)(SgAsmDwarfCompilationUnit::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfCompilationUnit::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfCompilationUnit::pools.begin(); block != SgAsmDwarfCompilationUnit::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCompilationUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCompilationUnit::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfCompilationUnit*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfCompilationUnit::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfCompilationUnit* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfCompilationUnit::pools.begin(); block != SgAsmDwarfCompilationUnit::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCompilationUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCompilationUnit::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfCompilationUnit::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfCompilationUnit::clearMemoryPool() {
    SgAsmDwarfCompilationUnit* tempPointer{nullptr};
    if (SgAsmDwarfCompilationUnit::pools.empty() == false) {
        auto block{SgAsmDwarfCompilationUnit::pools.begin()};
        SgAsmDwarfCompilationUnit::next_node = reinterpret_cast<SgAsmDwarfCompilationUnit*>(*block);
        for (; block != SgAsmDwarfCompilationUnit::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfCompilationUnit*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfCompilationUnit::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfCompilationUnit::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfCompilationUnit::pool_size-1]);
        }
    }
}

void
SgAsmDwarfCompilationUnit::deleteMemoryPool() {
    for (auto p: SgAsmDwarfCompilationUnit::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfCompilationUnit::next_node = nullptr;
    SgAsmDwarfCompilationUnit::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfCompilationUnit::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnit) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnit)};
    for (size_t blockIndex = SgAsmDwarfCompilationUnit::pools.size(); (blockIndex * SgAsmDwarfCompilationUnit::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnit) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnit) = %" PRIuPTR " SgAsmDwarfCompilationUnit::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCompilationUnit),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCompilationUnit),SgAsmDwarfCompilationUnit::pool_size);
        }
#endif

        SgAsmDwarfCompilationUnit* pointer{static_cast<SgAsmDwarfCompilationUnit*>(ROSE_MALLOC(SgAsmDwarfCompilationUnit::pool_size * sizeof(SgAsmDwarfCompilationUnit)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfCompilationUnit::pool_size * sizeof(SgAsmDwarfCompilationUnit));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfCompilationUnit::pool_size * sizeof(SgAsmDwarfCompilationUnit));
#endif
        SgAsmDwarfCompilationUnit::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfCompilationUnit::pool_size * sizeof(SgAsmDwarfCompilationUnit), V_SgAsmDwarfCompilationUnit));

        if (SgAsmDwarfCompilationUnit::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfCompilationUnit* blkptr{reinterpret_cast<SgAsmDwarfCompilationUnit*>(SgAsmDwarfCompilationUnit::pools[blockIndex-1])};
                blkptr[SgAsmDwarfCompilationUnit::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfCompilationUnit::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfCompilationUnit::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfCompilationUnit::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfCompilationUnit::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfCompilationUnit::pool_size - 1};
    SgAsmDwarfCompilationUnit* testPointer{reinterpret_cast<SgAsmDwarfCompilationUnit*>(SgAsmDwarfCompilationUnit::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfCompilationUnit::pool_size * (SgAsmDwarfCompilationUnit::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfCompilationUnitStroageClass
 * from its counterpart of SgAsmDwarfCompilationUnit. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfCompilationUnit::initializeStorageClassArray(SgAsmDwarfCompilationUnitStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfCompilationUnit::pools.begin(); block != SgAsmDwarfCompilationUnit::pools.end(); ++block) {
        SgAsmDwarfCompilationUnit* pointer{reinterpret_cast<SgAsmDwarfCompilationUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCompilationUnit::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 185345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfCondition that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfCondition::SgAsmDwarfCondition(const SgAsmDwarfConditionStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 185355 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfCondition*
SgAsmDwarfCondition::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfCondition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCondition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCondition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfCondition)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCondition)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCondition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCondition::pool_size};
        returnPointer = &(((SgAsmDwarfCondition*)(SgAsmDwarfCondition::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfCondition*
SgAsmDwarfCondition::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfCondition* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCondition)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCondition + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfCondition)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfCondition)};
        unsigned long positionInPool{localIndex % SgAsmDwarfCondition::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfCondition::pool_size};
        returnPointer = &(((SgAsmDwarfCondition*)(SgAsmDwarfCondition::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfCondition::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfCondition::pools.begin(); block != SgAsmDwarfCondition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCondition*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCondition::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfCondition*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfCondition::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfCondition* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfCondition::pools.begin(); block != SgAsmDwarfCondition::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfCondition*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCondition::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfCondition::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfCondition::clearMemoryPool() {
    SgAsmDwarfCondition* tempPointer{nullptr};
    if (SgAsmDwarfCondition::pools.empty() == false) {
        auto block{SgAsmDwarfCondition::pools.begin()};
        SgAsmDwarfCondition::next_node = reinterpret_cast<SgAsmDwarfCondition*>(*block);
        for (; block != SgAsmDwarfCondition::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfCondition*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfCondition::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfCondition::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfCondition::pool_size-1]);
        }
    }
}

void
SgAsmDwarfCondition::deleteMemoryPool() {
    for (auto p: SgAsmDwarfCondition::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfCondition::next_node = nullptr;
    SgAsmDwarfCondition::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfCondition::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCondition) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCondition)};
    for (size_t blockIndex = SgAsmDwarfCondition::pools.size(); (blockIndex * SgAsmDwarfCondition::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCondition) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCondition) = %" PRIuPTR " SgAsmDwarfCondition::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfCondition),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfCondition),SgAsmDwarfCondition::pool_size);
        }
#endif

        SgAsmDwarfCondition* pointer{static_cast<SgAsmDwarfCondition*>(ROSE_MALLOC(SgAsmDwarfCondition::pool_size * sizeof(SgAsmDwarfCondition)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfCondition::pool_size * sizeof(SgAsmDwarfCondition));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfCondition::pool_size * sizeof(SgAsmDwarfCondition));
#endif
        SgAsmDwarfCondition::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfCondition::pool_size * sizeof(SgAsmDwarfCondition), V_SgAsmDwarfCondition));

        if (SgAsmDwarfCondition::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfCondition* blkptr{reinterpret_cast<SgAsmDwarfCondition*>(SgAsmDwarfCondition::pools[blockIndex-1])};
                blkptr[SgAsmDwarfCondition::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfCondition::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfCondition::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfCondition::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfCondition::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfCondition::pool_size - 1};
    SgAsmDwarfCondition* testPointer{reinterpret_cast<SgAsmDwarfCondition*>(SgAsmDwarfCondition::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfCondition::pool_size * (SgAsmDwarfCondition::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfConditionStroageClass
 * from its counterpart of SgAsmDwarfCondition. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfCondition::initializeStorageClassArray(SgAsmDwarfConditionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfCondition::pools.begin(); block != SgAsmDwarfCondition::pools.end(); ++block) {
        SgAsmDwarfCondition* pointer{reinterpret_cast<SgAsmDwarfCondition*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfCondition::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 185572 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfConstant that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfConstant::SgAsmDwarfConstant(const SgAsmDwarfConstantStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 185582 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfConstant*
SgAsmDwarfConstant::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfConstant* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstant)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstant + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstant)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstant)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstant::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstant::pool_size};
        returnPointer = &(((SgAsmDwarfConstant*)(SgAsmDwarfConstant::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfConstant*
SgAsmDwarfConstant::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfConstant* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstant)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstant + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstant)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfConstant)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstant::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstant::pool_size};
        returnPointer = &(((SgAsmDwarfConstant*)(SgAsmDwarfConstant::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfConstant::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfConstant::pools.begin(); block != SgAsmDwarfConstant::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstant*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstant::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfConstant*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfConstant::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfConstant* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfConstant::pools.begin(); block != SgAsmDwarfConstant::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstant*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstant::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfConstant::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfConstant::clearMemoryPool() {
    SgAsmDwarfConstant* tempPointer{nullptr};
    if (SgAsmDwarfConstant::pools.empty() == false) {
        auto block{SgAsmDwarfConstant::pools.begin()};
        SgAsmDwarfConstant::next_node = reinterpret_cast<SgAsmDwarfConstant*>(*block);
        for (; block != SgAsmDwarfConstant::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfConstant*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfConstant::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfConstant::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfConstant::pool_size-1]);
        }
    }
}

void
SgAsmDwarfConstant::deleteMemoryPool() {
    for (auto p: SgAsmDwarfConstant::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfConstant::next_node = nullptr;
    SgAsmDwarfConstant::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfConstant::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstant) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstant)};
    for (size_t blockIndex = SgAsmDwarfConstant::pools.size(); (blockIndex * SgAsmDwarfConstant::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstant) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstant) = %" PRIuPTR " SgAsmDwarfConstant::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstant),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstant),SgAsmDwarfConstant::pool_size);
        }
#endif

        SgAsmDwarfConstant* pointer{static_cast<SgAsmDwarfConstant*>(ROSE_MALLOC(SgAsmDwarfConstant::pool_size * sizeof(SgAsmDwarfConstant)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfConstant::pool_size * sizeof(SgAsmDwarfConstant));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfConstant::pool_size * sizeof(SgAsmDwarfConstant));
#endif
        SgAsmDwarfConstant::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfConstant::pool_size * sizeof(SgAsmDwarfConstant), V_SgAsmDwarfConstant));

        if (SgAsmDwarfConstant::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfConstant* blkptr{reinterpret_cast<SgAsmDwarfConstant*>(SgAsmDwarfConstant::pools[blockIndex-1])};
                blkptr[SgAsmDwarfConstant::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfConstant::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfConstant::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfConstant::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfConstant::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfConstant::pool_size - 1};
    SgAsmDwarfConstant* testPointer{reinterpret_cast<SgAsmDwarfConstant*>(SgAsmDwarfConstant::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfConstant::pool_size * (SgAsmDwarfConstant::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfConstantStroageClass
 * from its counterpart of SgAsmDwarfConstant. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfConstant::initializeStorageClassArray(SgAsmDwarfConstantStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfConstant::pools.begin(); block != SgAsmDwarfConstant::pools.end(); ++block) {
        SgAsmDwarfConstant* pointer{reinterpret_cast<SgAsmDwarfConstant*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstant::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 185799 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfConstType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfConstType::SgAsmDwarfConstType(const SgAsmDwarfConstTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 185809 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfConstType*
SgAsmDwarfConstType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfConstType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstType::pool_size};
        returnPointer = &(((SgAsmDwarfConstType*)(SgAsmDwarfConstType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfConstType*
SgAsmDwarfConstType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfConstType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfConstType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstType::pool_size};
        returnPointer = &(((SgAsmDwarfConstType*)(SgAsmDwarfConstType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfConstType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfConstType::pools.begin(); block != SgAsmDwarfConstType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfConstType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfConstType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfConstType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfConstType::pools.begin(); block != SgAsmDwarfConstType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfConstType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfConstType::clearMemoryPool() {
    SgAsmDwarfConstType* tempPointer{nullptr};
    if (SgAsmDwarfConstType::pools.empty() == false) {
        auto block{SgAsmDwarfConstType::pools.begin()};
        SgAsmDwarfConstType::next_node = reinterpret_cast<SgAsmDwarfConstType*>(*block);
        for (; block != SgAsmDwarfConstType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfConstType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfConstType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfConstType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfConstType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfConstType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfConstType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfConstType::next_node = nullptr;
    SgAsmDwarfConstType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfConstType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstType)};
    for (size_t blockIndex = SgAsmDwarfConstType::pools.size(); (blockIndex * SgAsmDwarfConstType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstType) = %" PRIuPTR " SgAsmDwarfConstType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstType),SgAsmDwarfConstType::pool_size);
        }
#endif

        SgAsmDwarfConstType* pointer{static_cast<SgAsmDwarfConstType*>(ROSE_MALLOC(SgAsmDwarfConstType::pool_size * sizeof(SgAsmDwarfConstType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfConstType::pool_size * sizeof(SgAsmDwarfConstType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfConstType::pool_size * sizeof(SgAsmDwarfConstType));
#endif
        SgAsmDwarfConstType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfConstType::pool_size * sizeof(SgAsmDwarfConstType), V_SgAsmDwarfConstType));

        if (SgAsmDwarfConstType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfConstType* blkptr{reinterpret_cast<SgAsmDwarfConstType*>(SgAsmDwarfConstType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfConstType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfConstType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfConstType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfConstType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfConstType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfConstType::pool_size - 1};
    SgAsmDwarfConstType* testPointer{reinterpret_cast<SgAsmDwarfConstType*>(SgAsmDwarfConstType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfConstType::pool_size * (SgAsmDwarfConstType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfConstTypeStroageClass
 * from its counterpart of SgAsmDwarfConstType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfConstType::initializeStorageClassArray(SgAsmDwarfConstTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfConstType::pools.begin(); block != SgAsmDwarfConstType::pools.end(); ++block) {
        SgAsmDwarfConstType* pointer{reinterpret_cast<SgAsmDwarfConstType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 186026 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfDwarfProcedure that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfDwarfProcedure::SgAsmDwarfDwarfProcedure(const SgAsmDwarfDwarfProcedureStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 186036 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfDwarfProcedure*
SgAsmDwarfDwarfProcedure::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfDwarfProcedure* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfDwarfProcedure)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfDwarfProcedure + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfDwarfProcedure)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfDwarfProcedure)};
        unsigned long positionInPool{localIndex % SgAsmDwarfDwarfProcedure::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfDwarfProcedure::pool_size};
        returnPointer = &(((SgAsmDwarfDwarfProcedure*)(SgAsmDwarfDwarfProcedure::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfDwarfProcedure*
SgAsmDwarfDwarfProcedure::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfDwarfProcedure* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfDwarfProcedure)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfDwarfProcedure + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfDwarfProcedure)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfDwarfProcedure)};
        unsigned long positionInPool{localIndex % SgAsmDwarfDwarfProcedure::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfDwarfProcedure::pool_size};
        returnPointer = &(((SgAsmDwarfDwarfProcedure*)(SgAsmDwarfDwarfProcedure::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfDwarfProcedure::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfDwarfProcedure::pools.begin(); block != SgAsmDwarfDwarfProcedure::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfDwarfProcedure*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfDwarfProcedure::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfDwarfProcedure*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfDwarfProcedure::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfDwarfProcedure* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfDwarfProcedure::pools.begin(); block != SgAsmDwarfDwarfProcedure::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfDwarfProcedure*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfDwarfProcedure::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfDwarfProcedure::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfDwarfProcedure::clearMemoryPool() {
    SgAsmDwarfDwarfProcedure* tempPointer{nullptr};
    if (SgAsmDwarfDwarfProcedure::pools.empty() == false) {
        auto block{SgAsmDwarfDwarfProcedure::pools.begin()};
        SgAsmDwarfDwarfProcedure::next_node = reinterpret_cast<SgAsmDwarfDwarfProcedure*>(*block);
        for (; block != SgAsmDwarfDwarfProcedure::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfDwarfProcedure*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfDwarfProcedure::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfDwarfProcedure::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfDwarfProcedure::pool_size-1]);
        }
    }
}

void
SgAsmDwarfDwarfProcedure::deleteMemoryPool() {
    for (auto p: SgAsmDwarfDwarfProcedure::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfDwarfProcedure::next_node = nullptr;
    SgAsmDwarfDwarfProcedure::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfDwarfProcedure::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfDwarfProcedure) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfDwarfProcedure)};
    for (size_t blockIndex = SgAsmDwarfDwarfProcedure::pools.size(); (blockIndex * SgAsmDwarfDwarfProcedure::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfDwarfProcedure) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfDwarfProcedure) = %" PRIuPTR " SgAsmDwarfDwarfProcedure::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfDwarfProcedure),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfDwarfProcedure),SgAsmDwarfDwarfProcedure::pool_size);
        }
#endif

        SgAsmDwarfDwarfProcedure* pointer{static_cast<SgAsmDwarfDwarfProcedure*>(ROSE_MALLOC(SgAsmDwarfDwarfProcedure::pool_size * sizeof(SgAsmDwarfDwarfProcedure)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfDwarfProcedure::pool_size * sizeof(SgAsmDwarfDwarfProcedure));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfDwarfProcedure::pool_size * sizeof(SgAsmDwarfDwarfProcedure));
#endif
        SgAsmDwarfDwarfProcedure::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfDwarfProcedure::pool_size * sizeof(SgAsmDwarfDwarfProcedure), V_SgAsmDwarfDwarfProcedure));

        if (SgAsmDwarfDwarfProcedure::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfDwarfProcedure* blkptr{reinterpret_cast<SgAsmDwarfDwarfProcedure*>(SgAsmDwarfDwarfProcedure::pools[blockIndex-1])};
                blkptr[SgAsmDwarfDwarfProcedure::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfDwarfProcedure::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfDwarfProcedure::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfDwarfProcedure::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfDwarfProcedure::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfDwarfProcedure::pool_size - 1};
    SgAsmDwarfDwarfProcedure* testPointer{reinterpret_cast<SgAsmDwarfDwarfProcedure*>(SgAsmDwarfDwarfProcedure::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfDwarfProcedure::pool_size * (SgAsmDwarfDwarfProcedure::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfDwarfProcedureStroageClass
 * from its counterpart of SgAsmDwarfDwarfProcedure. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfDwarfProcedure::initializeStorageClassArray(SgAsmDwarfDwarfProcedureStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfDwarfProcedure::pools.begin(); block != SgAsmDwarfDwarfProcedure::pools.end(); ++block) {
        SgAsmDwarfDwarfProcedure* pointer{reinterpret_cast<SgAsmDwarfDwarfProcedure*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfDwarfProcedure::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 186253 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfEntryPoint that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfEntryPoint::SgAsmDwarfEntryPoint(const SgAsmDwarfEntryPointStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 186263 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfEntryPoint*
SgAsmDwarfEntryPoint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfEntryPoint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEntryPoint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEntryPoint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEntryPoint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEntryPoint)};
        unsigned long positionInPool{localIndex % SgAsmDwarfEntryPoint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfEntryPoint::pool_size};
        returnPointer = &(((SgAsmDwarfEntryPoint*)(SgAsmDwarfEntryPoint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfEntryPoint*
SgAsmDwarfEntryPoint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfEntryPoint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEntryPoint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEntryPoint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEntryPoint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfEntryPoint)};
        unsigned long positionInPool{localIndex % SgAsmDwarfEntryPoint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfEntryPoint::pool_size};
        returnPointer = &(((SgAsmDwarfEntryPoint*)(SgAsmDwarfEntryPoint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfEntryPoint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfEntryPoint::pools.begin(); block != SgAsmDwarfEntryPoint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEntryPoint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfEntryPoint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfEntryPoint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfEntryPoint* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfEntryPoint::pools.begin(); block != SgAsmDwarfEntryPoint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEntryPoint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfEntryPoint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfEntryPoint::clearMemoryPool() {
    SgAsmDwarfEntryPoint* tempPointer{nullptr};
    if (SgAsmDwarfEntryPoint::pools.empty() == false) {
        auto block{SgAsmDwarfEntryPoint::pools.begin()};
        SgAsmDwarfEntryPoint::next_node = reinterpret_cast<SgAsmDwarfEntryPoint*>(*block);
        for (; block != SgAsmDwarfEntryPoint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfEntryPoint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfEntryPoint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfEntryPoint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfEntryPoint::pool_size-1]);
        }
    }
}

void
SgAsmDwarfEntryPoint::deleteMemoryPool() {
    for (auto p: SgAsmDwarfEntryPoint::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfEntryPoint::next_node = nullptr;
    SgAsmDwarfEntryPoint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfEntryPoint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEntryPoint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEntryPoint)};
    for (size_t blockIndex = SgAsmDwarfEntryPoint::pools.size(); (blockIndex * SgAsmDwarfEntryPoint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEntryPoint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEntryPoint) = %" PRIuPTR " SgAsmDwarfEntryPoint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEntryPoint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEntryPoint),SgAsmDwarfEntryPoint::pool_size);
        }
#endif

        SgAsmDwarfEntryPoint* pointer{static_cast<SgAsmDwarfEntryPoint*>(ROSE_MALLOC(SgAsmDwarfEntryPoint::pool_size * sizeof(SgAsmDwarfEntryPoint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfEntryPoint::pool_size * sizeof(SgAsmDwarfEntryPoint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfEntryPoint::pool_size * sizeof(SgAsmDwarfEntryPoint));
#endif
        SgAsmDwarfEntryPoint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfEntryPoint::pool_size * sizeof(SgAsmDwarfEntryPoint), V_SgAsmDwarfEntryPoint));

        if (SgAsmDwarfEntryPoint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfEntryPoint* blkptr{reinterpret_cast<SgAsmDwarfEntryPoint*>(SgAsmDwarfEntryPoint::pools[blockIndex-1])};
                blkptr[SgAsmDwarfEntryPoint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfEntryPoint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfEntryPoint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfEntryPoint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfEntryPoint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfEntryPoint::pool_size - 1};
    SgAsmDwarfEntryPoint* testPointer{reinterpret_cast<SgAsmDwarfEntryPoint*>(SgAsmDwarfEntryPoint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfEntryPoint::pool_size * (SgAsmDwarfEntryPoint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfEntryPointStroageClass
 * from its counterpart of SgAsmDwarfEntryPoint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfEntryPoint::initializeStorageClassArray(SgAsmDwarfEntryPointStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfEntryPoint::pools.begin(); block != SgAsmDwarfEntryPoint::pools.end(); ++block) {
        SgAsmDwarfEntryPoint* pointer{reinterpret_cast<SgAsmDwarfEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEntryPoint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 186480 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfEnumerationType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfEnumerationType::SgAsmDwarfEnumerationType(const SgAsmDwarfEnumerationTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 186490 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_is_enum_class = storageSource.storageOf_is_enum_class;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfEnumerationType*
SgAsmDwarfEnumerationType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfEnumerationType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEnumerationType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEnumerationType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEnumerationType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerationType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfEnumerationType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfEnumerationType::pool_size};
        returnPointer = &(((SgAsmDwarfEnumerationType*)(SgAsmDwarfEnumerationType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfEnumerationType*
SgAsmDwarfEnumerationType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfEnumerationType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEnumerationType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEnumerationType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEnumerationType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfEnumerationType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfEnumerationType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfEnumerationType::pool_size};
        returnPointer = &(((SgAsmDwarfEnumerationType*)(SgAsmDwarfEnumerationType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfEnumerationType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfEnumerationType::pools.begin(); block != SgAsmDwarfEnumerationType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfEnumerationType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEnumerationType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfEnumerationType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfEnumerationType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfEnumerationType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfEnumerationType::pools.begin(); block != SgAsmDwarfEnumerationType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfEnumerationType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEnumerationType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfEnumerationType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfEnumerationType::clearMemoryPool() {
    SgAsmDwarfEnumerationType* tempPointer{nullptr};
    if (SgAsmDwarfEnumerationType::pools.empty() == false) {
        auto block{SgAsmDwarfEnumerationType::pools.begin()};
        SgAsmDwarfEnumerationType::next_node = reinterpret_cast<SgAsmDwarfEnumerationType*>(*block);
        for (; block != SgAsmDwarfEnumerationType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfEnumerationType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfEnumerationType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfEnumerationType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfEnumerationType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfEnumerationType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfEnumerationType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfEnumerationType::next_node = nullptr;
    SgAsmDwarfEnumerationType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfEnumerationType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerationType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEnumerationType)};
    for (size_t blockIndex = SgAsmDwarfEnumerationType::pools.size(); (blockIndex * SgAsmDwarfEnumerationType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerationType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEnumerationType) = %" PRIuPTR " SgAsmDwarfEnumerationType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerationType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEnumerationType),SgAsmDwarfEnumerationType::pool_size);
        }
#endif

        SgAsmDwarfEnumerationType* pointer{static_cast<SgAsmDwarfEnumerationType*>(ROSE_MALLOC(SgAsmDwarfEnumerationType::pool_size * sizeof(SgAsmDwarfEnumerationType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfEnumerationType::pool_size * sizeof(SgAsmDwarfEnumerationType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfEnumerationType::pool_size * sizeof(SgAsmDwarfEnumerationType));
#endif
        SgAsmDwarfEnumerationType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfEnumerationType::pool_size * sizeof(SgAsmDwarfEnumerationType), V_SgAsmDwarfEnumerationType));

        if (SgAsmDwarfEnumerationType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfEnumerationType* blkptr{reinterpret_cast<SgAsmDwarfEnumerationType*>(SgAsmDwarfEnumerationType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfEnumerationType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfEnumerationType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfEnumerationType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfEnumerationType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfEnumerationType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfEnumerationType::pool_size - 1};
    SgAsmDwarfEnumerationType* testPointer{reinterpret_cast<SgAsmDwarfEnumerationType*>(SgAsmDwarfEnumerationType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfEnumerationType::pool_size * (SgAsmDwarfEnumerationType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfEnumerationTypeStroageClass
 * from its counterpart of SgAsmDwarfEnumerationType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfEnumerationType::initializeStorageClassArray(SgAsmDwarfEnumerationTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfEnumerationType::pools.begin(); block != SgAsmDwarfEnumerationType::pools.end(); ++block) {
        SgAsmDwarfEnumerationType* pointer{reinterpret_cast<SgAsmDwarfEnumerationType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEnumerationType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 186709 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfEnumerator that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfEnumerator::SgAsmDwarfEnumerator(const SgAsmDwarfEnumeratorStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 186719 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfEnumerator*
SgAsmDwarfEnumerator::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfEnumerator* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEnumerator)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEnumerator + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfEnumerator)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerator)};
        unsigned long positionInPool{localIndex % SgAsmDwarfEnumerator::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfEnumerator::pool_size};
        returnPointer = &(((SgAsmDwarfEnumerator*)(SgAsmDwarfEnumerator::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfEnumerator*
SgAsmDwarfEnumerator::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfEnumerator* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEnumerator)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEnumerator + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfEnumerator)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfEnumerator)};
        unsigned long positionInPool{localIndex % SgAsmDwarfEnumerator::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfEnumerator::pool_size};
        returnPointer = &(((SgAsmDwarfEnumerator*)(SgAsmDwarfEnumerator::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfEnumerator::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfEnumerator::pools.begin(); block != SgAsmDwarfEnumerator::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfEnumerator*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEnumerator::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfEnumerator*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfEnumerator::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfEnumerator* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfEnumerator::pools.begin(); block != SgAsmDwarfEnumerator::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfEnumerator*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEnumerator::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfEnumerator::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfEnumerator::clearMemoryPool() {
    SgAsmDwarfEnumerator* tempPointer{nullptr};
    if (SgAsmDwarfEnumerator::pools.empty() == false) {
        auto block{SgAsmDwarfEnumerator::pools.begin()};
        SgAsmDwarfEnumerator::next_node = reinterpret_cast<SgAsmDwarfEnumerator*>(*block);
        for (; block != SgAsmDwarfEnumerator::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfEnumerator*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfEnumerator::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfEnumerator::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfEnumerator::pool_size-1]);
        }
    }
}

void
SgAsmDwarfEnumerator::deleteMemoryPool() {
    for (auto p: SgAsmDwarfEnumerator::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfEnumerator::next_node = nullptr;
    SgAsmDwarfEnumerator::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfEnumerator::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerator) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEnumerator)};
    for (size_t blockIndex = SgAsmDwarfEnumerator::pools.size(); (blockIndex * SgAsmDwarfEnumerator::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerator) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEnumerator) = %" PRIuPTR " SgAsmDwarfEnumerator::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfEnumerator),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfEnumerator),SgAsmDwarfEnumerator::pool_size);
        }
#endif

        SgAsmDwarfEnumerator* pointer{static_cast<SgAsmDwarfEnumerator*>(ROSE_MALLOC(SgAsmDwarfEnumerator::pool_size * sizeof(SgAsmDwarfEnumerator)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfEnumerator::pool_size * sizeof(SgAsmDwarfEnumerator));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfEnumerator::pool_size * sizeof(SgAsmDwarfEnumerator));
#endif
        SgAsmDwarfEnumerator::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfEnumerator::pool_size * sizeof(SgAsmDwarfEnumerator), V_SgAsmDwarfEnumerator));

        if (SgAsmDwarfEnumerator::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfEnumerator* blkptr{reinterpret_cast<SgAsmDwarfEnumerator*>(SgAsmDwarfEnumerator::pools[blockIndex-1])};
                blkptr[SgAsmDwarfEnumerator::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfEnumerator::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfEnumerator::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfEnumerator::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfEnumerator::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfEnumerator::pool_size - 1};
    SgAsmDwarfEnumerator* testPointer{reinterpret_cast<SgAsmDwarfEnumerator*>(SgAsmDwarfEnumerator::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfEnumerator::pool_size * (SgAsmDwarfEnumerator::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfEnumeratorStroageClass
 * from its counterpart of SgAsmDwarfEnumerator. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfEnumerator::initializeStorageClassArray(SgAsmDwarfEnumeratorStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfEnumerator::pools.begin(); block != SgAsmDwarfEnumerator::pools.end(); ++block) {
        SgAsmDwarfEnumerator* pointer{reinterpret_cast<SgAsmDwarfEnumerator*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfEnumerator::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 186936 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfFileType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfFileType::SgAsmDwarfFileType(const SgAsmDwarfFileTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 186946 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfFileType*
SgAsmDwarfFileType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfFileType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFileType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFileType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFileType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFileType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFileType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFileType::pool_size};
        returnPointer = &(((SgAsmDwarfFileType*)(SgAsmDwarfFileType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfFileType*
SgAsmDwarfFileType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfFileType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFileType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFileType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFileType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfFileType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFileType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFileType::pool_size};
        returnPointer = &(((SgAsmDwarfFileType*)(SgAsmDwarfFileType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfFileType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfFileType::pools.begin(); block != SgAsmDwarfFileType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFileType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFileType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfFileType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfFileType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfFileType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfFileType::pools.begin(); block != SgAsmDwarfFileType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFileType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFileType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfFileType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfFileType::clearMemoryPool() {
    SgAsmDwarfFileType* tempPointer{nullptr};
    if (SgAsmDwarfFileType::pools.empty() == false) {
        auto block{SgAsmDwarfFileType::pools.begin()};
        SgAsmDwarfFileType::next_node = reinterpret_cast<SgAsmDwarfFileType*>(*block);
        for (; block != SgAsmDwarfFileType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfFileType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfFileType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfFileType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfFileType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfFileType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfFileType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfFileType::next_node = nullptr;
    SgAsmDwarfFileType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfFileType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFileType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFileType)};
    for (size_t blockIndex = SgAsmDwarfFileType::pools.size(); (blockIndex * SgAsmDwarfFileType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFileType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFileType) = %" PRIuPTR " SgAsmDwarfFileType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFileType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFileType),SgAsmDwarfFileType::pool_size);
        }
#endif

        SgAsmDwarfFileType* pointer{static_cast<SgAsmDwarfFileType*>(ROSE_MALLOC(SgAsmDwarfFileType::pool_size * sizeof(SgAsmDwarfFileType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfFileType::pool_size * sizeof(SgAsmDwarfFileType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfFileType::pool_size * sizeof(SgAsmDwarfFileType));
#endif
        SgAsmDwarfFileType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfFileType::pool_size * sizeof(SgAsmDwarfFileType), V_SgAsmDwarfFileType));

        if (SgAsmDwarfFileType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfFileType* blkptr{reinterpret_cast<SgAsmDwarfFileType*>(SgAsmDwarfFileType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfFileType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfFileType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfFileType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfFileType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfFileType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfFileType::pool_size - 1};
    SgAsmDwarfFileType* testPointer{reinterpret_cast<SgAsmDwarfFileType*>(SgAsmDwarfFileType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfFileType::pool_size * (SgAsmDwarfFileType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfFileTypeStroageClass
 * from its counterpart of SgAsmDwarfFileType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfFileType::initializeStorageClassArray(SgAsmDwarfFileTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfFileType::pools.begin(); block != SgAsmDwarfFileType::pools.end(); ++block) {
        SgAsmDwarfFileType* pointer{reinterpret_cast<SgAsmDwarfFileType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFileType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 187163 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfFormalParameter that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfFormalParameter::SgAsmDwarfFormalParameter(const SgAsmDwarfFormalParameterStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 187173 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfFormalParameter*
SgAsmDwarfFormalParameter::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfFormalParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFormalParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFormalParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFormalParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormalParameter)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFormalParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFormalParameter::pool_size};
        returnPointer = &(((SgAsmDwarfFormalParameter*)(SgAsmDwarfFormalParameter::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfFormalParameter*
SgAsmDwarfFormalParameter::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfFormalParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFormalParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFormalParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFormalParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfFormalParameter)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFormalParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFormalParameter::pool_size};
        returnPointer = &(((SgAsmDwarfFormalParameter*)(SgAsmDwarfFormalParameter::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfFormalParameter::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfFormalParameter::pools.begin(); block != SgAsmDwarfFormalParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFormalParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFormalParameter::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfFormalParameter*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfFormalParameter::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfFormalParameter* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfFormalParameter::pools.begin(); block != SgAsmDwarfFormalParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFormalParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFormalParameter::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfFormalParameter::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfFormalParameter::clearMemoryPool() {
    SgAsmDwarfFormalParameter* tempPointer{nullptr};
    if (SgAsmDwarfFormalParameter::pools.empty() == false) {
        auto block{SgAsmDwarfFormalParameter::pools.begin()};
        SgAsmDwarfFormalParameter::next_node = reinterpret_cast<SgAsmDwarfFormalParameter*>(*block);
        for (; block != SgAsmDwarfFormalParameter::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfFormalParameter*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfFormalParameter::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfFormalParameter::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfFormalParameter::pool_size-1]);
        }
    }
}

void
SgAsmDwarfFormalParameter::deleteMemoryPool() {
    for (auto p: SgAsmDwarfFormalParameter::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfFormalParameter::next_node = nullptr;
    SgAsmDwarfFormalParameter::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfFormalParameter::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormalParameter) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFormalParameter)};
    for (size_t blockIndex = SgAsmDwarfFormalParameter::pools.size(); (blockIndex * SgAsmDwarfFormalParameter::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormalParameter) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFormalParameter) = %" PRIuPTR " SgAsmDwarfFormalParameter::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormalParameter),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFormalParameter),SgAsmDwarfFormalParameter::pool_size);
        }
#endif

        SgAsmDwarfFormalParameter* pointer{static_cast<SgAsmDwarfFormalParameter*>(ROSE_MALLOC(SgAsmDwarfFormalParameter::pool_size * sizeof(SgAsmDwarfFormalParameter)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfFormalParameter::pool_size * sizeof(SgAsmDwarfFormalParameter));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfFormalParameter::pool_size * sizeof(SgAsmDwarfFormalParameter));
#endif
        SgAsmDwarfFormalParameter::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfFormalParameter::pool_size * sizeof(SgAsmDwarfFormalParameter), V_SgAsmDwarfFormalParameter));

        if (SgAsmDwarfFormalParameter::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfFormalParameter* blkptr{reinterpret_cast<SgAsmDwarfFormalParameter*>(SgAsmDwarfFormalParameter::pools[blockIndex-1])};
                blkptr[SgAsmDwarfFormalParameter::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfFormalParameter::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfFormalParameter::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfFormalParameter::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfFormalParameter::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfFormalParameter::pool_size - 1};
    SgAsmDwarfFormalParameter* testPointer{reinterpret_cast<SgAsmDwarfFormalParameter*>(SgAsmDwarfFormalParameter::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfFormalParameter::pool_size * (SgAsmDwarfFormalParameter::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfFormalParameterStroageClass
 * from its counterpart of SgAsmDwarfFormalParameter. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfFormalParameter::initializeStorageClassArray(SgAsmDwarfFormalParameterStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfFormalParameter::pools.begin(); block != SgAsmDwarfFormalParameter::pools.end(); ++block) {
        SgAsmDwarfFormalParameter* pointer{reinterpret_cast<SgAsmDwarfFormalParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFormalParameter::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 187390 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfFormatLabel that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfFormatLabel::SgAsmDwarfFormatLabel(const SgAsmDwarfFormatLabelStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 187400 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfFormatLabel*
SgAsmDwarfFormatLabel::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfFormatLabel* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFormatLabel)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFormatLabel + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFormatLabel)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormatLabel)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFormatLabel::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFormatLabel::pool_size};
        returnPointer = &(((SgAsmDwarfFormatLabel*)(SgAsmDwarfFormatLabel::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfFormatLabel*
SgAsmDwarfFormatLabel::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfFormatLabel* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFormatLabel)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFormatLabel + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFormatLabel)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfFormatLabel)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFormatLabel::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFormatLabel::pool_size};
        returnPointer = &(((SgAsmDwarfFormatLabel*)(SgAsmDwarfFormatLabel::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfFormatLabel::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfFormatLabel::pools.begin(); block != SgAsmDwarfFormatLabel::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFormatLabel*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFormatLabel::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfFormatLabel*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfFormatLabel::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfFormatLabel* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfFormatLabel::pools.begin(); block != SgAsmDwarfFormatLabel::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFormatLabel*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFormatLabel::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfFormatLabel::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfFormatLabel::clearMemoryPool() {
    SgAsmDwarfFormatLabel* tempPointer{nullptr};
    if (SgAsmDwarfFormatLabel::pools.empty() == false) {
        auto block{SgAsmDwarfFormatLabel::pools.begin()};
        SgAsmDwarfFormatLabel::next_node = reinterpret_cast<SgAsmDwarfFormatLabel*>(*block);
        for (; block != SgAsmDwarfFormatLabel::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfFormatLabel*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfFormatLabel::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfFormatLabel::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfFormatLabel::pool_size-1]);
        }
    }
}

void
SgAsmDwarfFormatLabel::deleteMemoryPool() {
    for (auto p: SgAsmDwarfFormatLabel::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfFormatLabel::next_node = nullptr;
    SgAsmDwarfFormatLabel::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfFormatLabel::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormatLabel) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFormatLabel)};
    for (size_t blockIndex = SgAsmDwarfFormatLabel::pools.size(); (blockIndex * SgAsmDwarfFormatLabel::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormatLabel) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFormatLabel) = %" PRIuPTR " SgAsmDwarfFormatLabel::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFormatLabel),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFormatLabel),SgAsmDwarfFormatLabel::pool_size);
        }
#endif

        SgAsmDwarfFormatLabel* pointer{static_cast<SgAsmDwarfFormatLabel*>(ROSE_MALLOC(SgAsmDwarfFormatLabel::pool_size * sizeof(SgAsmDwarfFormatLabel)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfFormatLabel::pool_size * sizeof(SgAsmDwarfFormatLabel));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfFormatLabel::pool_size * sizeof(SgAsmDwarfFormatLabel));
#endif
        SgAsmDwarfFormatLabel::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfFormatLabel::pool_size * sizeof(SgAsmDwarfFormatLabel), V_SgAsmDwarfFormatLabel));

        if (SgAsmDwarfFormatLabel::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfFormatLabel* blkptr{reinterpret_cast<SgAsmDwarfFormatLabel*>(SgAsmDwarfFormatLabel::pools[blockIndex-1])};
                blkptr[SgAsmDwarfFormatLabel::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfFormatLabel::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfFormatLabel::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfFormatLabel::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfFormatLabel::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfFormatLabel::pool_size - 1};
    SgAsmDwarfFormatLabel* testPointer{reinterpret_cast<SgAsmDwarfFormatLabel*>(SgAsmDwarfFormatLabel::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfFormatLabel::pool_size * (SgAsmDwarfFormatLabel::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfFormatLabelStroageClass
 * from its counterpart of SgAsmDwarfFormatLabel. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfFormatLabel::initializeStorageClassArray(SgAsmDwarfFormatLabelStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfFormatLabel::pools.begin(); block != SgAsmDwarfFormatLabel::pools.end(); ++block) {
        SgAsmDwarfFormatLabel* pointer{reinterpret_cast<SgAsmDwarfFormatLabel*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFormatLabel::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 187617 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfFriend that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfFriend::SgAsmDwarfFriend(const SgAsmDwarfFriendStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 187627 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfFriend*
SgAsmDwarfFriend::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfFriend* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFriend)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFriend + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFriend)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFriend)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFriend::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFriend::pool_size};
        returnPointer = &(((SgAsmDwarfFriend*)(SgAsmDwarfFriend::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfFriend*
SgAsmDwarfFriend::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfFriend* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFriend)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFriend + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFriend)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfFriend)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFriend::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFriend::pool_size};
        returnPointer = &(((SgAsmDwarfFriend*)(SgAsmDwarfFriend::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfFriend::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfFriend::pools.begin(); block != SgAsmDwarfFriend::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFriend*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFriend::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfFriend*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfFriend::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfFriend* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfFriend::pools.begin(); block != SgAsmDwarfFriend::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFriend*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFriend::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfFriend::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfFriend::clearMemoryPool() {
    SgAsmDwarfFriend* tempPointer{nullptr};
    if (SgAsmDwarfFriend::pools.empty() == false) {
        auto block{SgAsmDwarfFriend::pools.begin()};
        SgAsmDwarfFriend::next_node = reinterpret_cast<SgAsmDwarfFriend*>(*block);
        for (; block != SgAsmDwarfFriend::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfFriend*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfFriend::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfFriend::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfFriend::pool_size-1]);
        }
    }
}

void
SgAsmDwarfFriend::deleteMemoryPool() {
    for (auto p: SgAsmDwarfFriend::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfFriend::next_node = nullptr;
    SgAsmDwarfFriend::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfFriend::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFriend) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFriend)};
    for (size_t blockIndex = SgAsmDwarfFriend::pools.size(); (blockIndex * SgAsmDwarfFriend::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFriend) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFriend) = %" PRIuPTR " SgAsmDwarfFriend::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFriend),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFriend),SgAsmDwarfFriend::pool_size);
        }
#endif

        SgAsmDwarfFriend* pointer{static_cast<SgAsmDwarfFriend*>(ROSE_MALLOC(SgAsmDwarfFriend::pool_size * sizeof(SgAsmDwarfFriend)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfFriend::pool_size * sizeof(SgAsmDwarfFriend));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfFriend::pool_size * sizeof(SgAsmDwarfFriend));
#endif
        SgAsmDwarfFriend::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfFriend::pool_size * sizeof(SgAsmDwarfFriend), V_SgAsmDwarfFriend));

        if (SgAsmDwarfFriend::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfFriend* blkptr{reinterpret_cast<SgAsmDwarfFriend*>(SgAsmDwarfFriend::pools[blockIndex-1])};
                blkptr[SgAsmDwarfFriend::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfFriend::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfFriend::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfFriend::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfFriend::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfFriend::pool_size - 1};
    SgAsmDwarfFriend* testPointer{reinterpret_cast<SgAsmDwarfFriend*>(SgAsmDwarfFriend::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfFriend::pool_size * (SgAsmDwarfFriend::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfFriendStroageClass
 * from its counterpart of SgAsmDwarfFriend. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfFriend::initializeStorageClassArray(SgAsmDwarfFriendStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfFriend::pools.begin(); block != SgAsmDwarfFriend::pools.end(); ++block) {
        SgAsmDwarfFriend* pointer{reinterpret_cast<SgAsmDwarfFriend*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFriend::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 187844 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfFunctionTemplate that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfFunctionTemplate::SgAsmDwarfFunctionTemplate(const SgAsmDwarfFunctionTemplateStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 187854 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfFunctionTemplate*
SgAsmDwarfFunctionTemplate::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfFunctionTemplate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFunctionTemplate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFunctionTemplate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfFunctionTemplate)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFunctionTemplate)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFunctionTemplate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFunctionTemplate::pool_size};
        returnPointer = &(((SgAsmDwarfFunctionTemplate*)(SgAsmDwarfFunctionTemplate::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfFunctionTemplate*
SgAsmDwarfFunctionTemplate::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfFunctionTemplate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFunctionTemplate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFunctionTemplate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfFunctionTemplate)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfFunctionTemplate)};
        unsigned long positionInPool{localIndex % SgAsmDwarfFunctionTemplate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfFunctionTemplate::pool_size};
        returnPointer = &(((SgAsmDwarfFunctionTemplate*)(SgAsmDwarfFunctionTemplate::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfFunctionTemplate::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfFunctionTemplate::pools.begin(); block != SgAsmDwarfFunctionTemplate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFunctionTemplate*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFunctionTemplate::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfFunctionTemplate*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfFunctionTemplate::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfFunctionTemplate* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfFunctionTemplate::pools.begin(); block != SgAsmDwarfFunctionTemplate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfFunctionTemplate*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFunctionTemplate::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfFunctionTemplate::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfFunctionTemplate::clearMemoryPool() {
    SgAsmDwarfFunctionTemplate* tempPointer{nullptr};
    if (SgAsmDwarfFunctionTemplate::pools.empty() == false) {
        auto block{SgAsmDwarfFunctionTemplate::pools.begin()};
        SgAsmDwarfFunctionTemplate::next_node = reinterpret_cast<SgAsmDwarfFunctionTemplate*>(*block);
        for (; block != SgAsmDwarfFunctionTemplate::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfFunctionTemplate*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfFunctionTemplate::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfFunctionTemplate::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfFunctionTemplate::pool_size-1]);
        }
    }
}

void
SgAsmDwarfFunctionTemplate::deleteMemoryPool() {
    for (auto p: SgAsmDwarfFunctionTemplate::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfFunctionTemplate::next_node = nullptr;
    SgAsmDwarfFunctionTemplate::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfFunctionTemplate::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFunctionTemplate) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFunctionTemplate)};
    for (size_t blockIndex = SgAsmDwarfFunctionTemplate::pools.size(); (blockIndex * SgAsmDwarfFunctionTemplate::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFunctionTemplate) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFunctionTemplate) = %" PRIuPTR " SgAsmDwarfFunctionTemplate::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfFunctionTemplate),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfFunctionTemplate),SgAsmDwarfFunctionTemplate::pool_size);
        }
#endif

        SgAsmDwarfFunctionTemplate* pointer{static_cast<SgAsmDwarfFunctionTemplate*>(ROSE_MALLOC(SgAsmDwarfFunctionTemplate::pool_size * sizeof(SgAsmDwarfFunctionTemplate)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfFunctionTemplate::pool_size * sizeof(SgAsmDwarfFunctionTemplate));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfFunctionTemplate::pool_size * sizeof(SgAsmDwarfFunctionTemplate));
#endif
        SgAsmDwarfFunctionTemplate::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfFunctionTemplate::pool_size * sizeof(SgAsmDwarfFunctionTemplate), V_SgAsmDwarfFunctionTemplate));

        if (SgAsmDwarfFunctionTemplate::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfFunctionTemplate* blkptr{reinterpret_cast<SgAsmDwarfFunctionTemplate*>(SgAsmDwarfFunctionTemplate::pools[blockIndex-1])};
                blkptr[SgAsmDwarfFunctionTemplate::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfFunctionTemplate::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfFunctionTemplate::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfFunctionTemplate::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfFunctionTemplate::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfFunctionTemplate::pool_size - 1};
    SgAsmDwarfFunctionTemplate* testPointer{reinterpret_cast<SgAsmDwarfFunctionTemplate*>(SgAsmDwarfFunctionTemplate::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfFunctionTemplate::pool_size * (SgAsmDwarfFunctionTemplate::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfFunctionTemplateStroageClass
 * from its counterpart of SgAsmDwarfFunctionTemplate. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfFunctionTemplate::initializeStorageClassArray(SgAsmDwarfFunctionTemplateStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfFunctionTemplate::pools.begin(); block != SgAsmDwarfFunctionTemplate::pools.end(); ++block) {
        SgAsmDwarfFunctionTemplate* pointer{reinterpret_cast<SgAsmDwarfFunctionTemplate*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfFunctionTemplate::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 188071 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfImportedDeclaration that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfImportedDeclaration::SgAsmDwarfImportedDeclaration(const SgAsmDwarfImportedDeclarationStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 188081 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfImportedDeclaration*
SgAsmDwarfImportedDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfImportedDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmDwarfImportedDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfImportedDeclaration::pool_size};
        returnPointer = &(((SgAsmDwarfImportedDeclaration*)(SgAsmDwarfImportedDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfImportedDeclaration*
SgAsmDwarfImportedDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfImportedDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfImportedDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmDwarfImportedDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfImportedDeclaration::pool_size};
        returnPointer = &(((SgAsmDwarfImportedDeclaration*)(SgAsmDwarfImportedDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfImportedDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfImportedDeclaration::pools.begin(); block != SgAsmDwarfImportedDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfImportedDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfImportedDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfImportedDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfImportedDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfImportedDeclaration::pools.begin(); block != SgAsmDwarfImportedDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfImportedDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfImportedDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfImportedDeclaration::clearMemoryPool() {
    SgAsmDwarfImportedDeclaration* tempPointer{nullptr};
    if (SgAsmDwarfImportedDeclaration::pools.empty() == false) {
        auto block{SgAsmDwarfImportedDeclaration::pools.begin()};
        SgAsmDwarfImportedDeclaration::next_node = reinterpret_cast<SgAsmDwarfImportedDeclaration*>(*block);
        for (; block != SgAsmDwarfImportedDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfImportedDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfImportedDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfImportedDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfImportedDeclaration::pool_size-1]);
        }
    }
}

void
SgAsmDwarfImportedDeclaration::deleteMemoryPool() {
    for (auto p: SgAsmDwarfImportedDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfImportedDeclaration::next_node = nullptr;
    SgAsmDwarfImportedDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfImportedDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedDeclaration)};
    for (size_t blockIndex = SgAsmDwarfImportedDeclaration::pools.size(); (blockIndex * SgAsmDwarfImportedDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedDeclaration) = %" PRIuPTR " SgAsmDwarfImportedDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedDeclaration),SgAsmDwarfImportedDeclaration::pool_size);
        }
#endif

        SgAsmDwarfImportedDeclaration* pointer{static_cast<SgAsmDwarfImportedDeclaration*>(ROSE_MALLOC(SgAsmDwarfImportedDeclaration::pool_size * sizeof(SgAsmDwarfImportedDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfImportedDeclaration::pool_size * sizeof(SgAsmDwarfImportedDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfImportedDeclaration::pool_size * sizeof(SgAsmDwarfImportedDeclaration));
#endif
        SgAsmDwarfImportedDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfImportedDeclaration::pool_size * sizeof(SgAsmDwarfImportedDeclaration), V_SgAsmDwarfImportedDeclaration));

        if (SgAsmDwarfImportedDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfImportedDeclaration* blkptr{reinterpret_cast<SgAsmDwarfImportedDeclaration*>(SgAsmDwarfImportedDeclaration::pools[blockIndex-1])};
                blkptr[SgAsmDwarfImportedDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfImportedDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfImportedDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfImportedDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfImportedDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfImportedDeclaration::pool_size - 1};
    SgAsmDwarfImportedDeclaration* testPointer{reinterpret_cast<SgAsmDwarfImportedDeclaration*>(SgAsmDwarfImportedDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfImportedDeclaration::pool_size * (SgAsmDwarfImportedDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfImportedDeclarationStroageClass
 * from its counterpart of SgAsmDwarfImportedDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfImportedDeclaration::initializeStorageClassArray(SgAsmDwarfImportedDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfImportedDeclaration::pools.begin(); block != SgAsmDwarfImportedDeclaration::pools.end(); ++block) {
        SgAsmDwarfImportedDeclaration* pointer{reinterpret_cast<SgAsmDwarfImportedDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 188298 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfImportedModule that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfImportedModule::SgAsmDwarfImportedModule(const SgAsmDwarfImportedModuleStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 188308 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfImportedModule*
SgAsmDwarfImportedModule::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfImportedModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedModule)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedModule)};
        unsigned long positionInPool{localIndex % SgAsmDwarfImportedModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfImportedModule::pool_size};
        returnPointer = &(((SgAsmDwarfImportedModule*)(SgAsmDwarfImportedModule::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfImportedModule*
SgAsmDwarfImportedModule::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfImportedModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedModule)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfImportedModule)};
        unsigned long positionInPool{localIndex % SgAsmDwarfImportedModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfImportedModule::pool_size};
        returnPointer = &(((SgAsmDwarfImportedModule*)(SgAsmDwarfImportedModule::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfImportedModule::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfImportedModule::pools.begin(); block != SgAsmDwarfImportedModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfImportedModule*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedModule::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfImportedModule*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfImportedModule::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfImportedModule* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfImportedModule::pools.begin(); block != SgAsmDwarfImportedModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfImportedModule*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedModule::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfImportedModule::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfImportedModule::clearMemoryPool() {
    SgAsmDwarfImportedModule* tempPointer{nullptr};
    if (SgAsmDwarfImportedModule::pools.empty() == false) {
        auto block{SgAsmDwarfImportedModule::pools.begin()};
        SgAsmDwarfImportedModule::next_node = reinterpret_cast<SgAsmDwarfImportedModule*>(*block);
        for (; block != SgAsmDwarfImportedModule::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfImportedModule*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfImportedModule::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfImportedModule::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfImportedModule::pool_size-1]);
        }
    }
}

void
SgAsmDwarfImportedModule::deleteMemoryPool() {
    for (auto p: SgAsmDwarfImportedModule::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfImportedModule::next_node = nullptr;
    SgAsmDwarfImportedModule::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfImportedModule::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedModule) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedModule)};
    for (size_t blockIndex = SgAsmDwarfImportedModule::pools.size(); (blockIndex * SgAsmDwarfImportedModule::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedModule) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedModule) = %" PRIuPTR " SgAsmDwarfImportedModule::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedModule),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedModule),SgAsmDwarfImportedModule::pool_size);
        }
#endif

        SgAsmDwarfImportedModule* pointer{static_cast<SgAsmDwarfImportedModule*>(ROSE_MALLOC(SgAsmDwarfImportedModule::pool_size * sizeof(SgAsmDwarfImportedModule)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfImportedModule::pool_size * sizeof(SgAsmDwarfImportedModule));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfImportedModule::pool_size * sizeof(SgAsmDwarfImportedModule));
#endif
        SgAsmDwarfImportedModule::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfImportedModule::pool_size * sizeof(SgAsmDwarfImportedModule), V_SgAsmDwarfImportedModule));

        if (SgAsmDwarfImportedModule::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfImportedModule* blkptr{reinterpret_cast<SgAsmDwarfImportedModule*>(SgAsmDwarfImportedModule::pools[blockIndex-1])};
                blkptr[SgAsmDwarfImportedModule::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfImportedModule::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfImportedModule::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfImportedModule::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfImportedModule::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfImportedModule::pool_size - 1};
    SgAsmDwarfImportedModule* testPointer{reinterpret_cast<SgAsmDwarfImportedModule*>(SgAsmDwarfImportedModule::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfImportedModule::pool_size * (SgAsmDwarfImportedModule::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfImportedModuleStroageClass
 * from its counterpart of SgAsmDwarfImportedModule. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfImportedModule::initializeStorageClassArray(SgAsmDwarfImportedModuleStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfImportedModule::pools.begin(); block != SgAsmDwarfImportedModule::pools.end(); ++block) {
        SgAsmDwarfImportedModule* pointer{reinterpret_cast<SgAsmDwarfImportedModule*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedModule::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 188525 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfImportedUnit that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfImportedUnit::SgAsmDwarfImportedUnit(const SgAsmDwarfImportedUnitStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 188535 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfImportedUnit*
SgAsmDwarfImportedUnit::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfImportedUnit* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedUnit)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedUnit + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfImportedUnit)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedUnit)};
        unsigned long positionInPool{localIndex % SgAsmDwarfImportedUnit::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfImportedUnit::pool_size};
        returnPointer = &(((SgAsmDwarfImportedUnit*)(SgAsmDwarfImportedUnit::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfImportedUnit*
SgAsmDwarfImportedUnit::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfImportedUnit* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedUnit)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedUnit + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfImportedUnit)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfImportedUnit)};
        unsigned long positionInPool{localIndex % SgAsmDwarfImportedUnit::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfImportedUnit::pool_size};
        returnPointer = &(((SgAsmDwarfImportedUnit*)(SgAsmDwarfImportedUnit::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfImportedUnit::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfImportedUnit::pools.begin(); block != SgAsmDwarfImportedUnit::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfImportedUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedUnit::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfImportedUnit*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfImportedUnit::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfImportedUnit* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfImportedUnit::pools.begin(); block != SgAsmDwarfImportedUnit::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfImportedUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedUnit::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfImportedUnit::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfImportedUnit::clearMemoryPool() {
    SgAsmDwarfImportedUnit* tempPointer{nullptr};
    if (SgAsmDwarfImportedUnit::pools.empty() == false) {
        auto block{SgAsmDwarfImportedUnit::pools.begin()};
        SgAsmDwarfImportedUnit::next_node = reinterpret_cast<SgAsmDwarfImportedUnit*>(*block);
        for (; block != SgAsmDwarfImportedUnit::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfImportedUnit*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfImportedUnit::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfImportedUnit::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfImportedUnit::pool_size-1]);
        }
    }
}

void
SgAsmDwarfImportedUnit::deleteMemoryPool() {
    for (auto p: SgAsmDwarfImportedUnit::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfImportedUnit::next_node = nullptr;
    SgAsmDwarfImportedUnit::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfImportedUnit::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedUnit) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedUnit)};
    for (size_t blockIndex = SgAsmDwarfImportedUnit::pools.size(); (blockIndex * SgAsmDwarfImportedUnit::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedUnit) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedUnit) = %" PRIuPTR " SgAsmDwarfImportedUnit::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfImportedUnit),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfImportedUnit),SgAsmDwarfImportedUnit::pool_size);
        }
#endif

        SgAsmDwarfImportedUnit* pointer{static_cast<SgAsmDwarfImportedUnit*>(ROSE_MALLOC(SgAsmDwarfImportedUnit::pool_size * sizeof(SgAsmDwarfImportedUnit)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfImportedUnit::pool_size * sizeof(SgAsmDwarfImportedUnit));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfImportedUnit::pool_size * sizeof(SgAsmDwarfImportedUnit));
#endif
        SgAsmDwarfImportedUnit::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfImportedUnit::pool_size * sizeof(SgAsmDwarfImportedUnit), V_SgAsmDwarfImportedUnit));

        if (SgAsmDwarfImportedUnit::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfImportedUnit* blkptr{reinterpret_cast<SgAsmDwarfImportedUnit*>(SgAsmDwarfImportedUnit::pools[blockIndex-1])};
                blkptr[SgAsmDwarfImportedUnit::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfImportedUnit::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfImportedUnit::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfImportedUnit::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfImportedUnit::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfImportedUnit::pool_size - 1};
    SgAsmDwarfImportedUnit* testPointer{reinterpret_cast<SgAsmDwarfImportedUnit*>(SgAsmDwarfImportedUnit::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfImportedUnit::pool_size * (SgAsmDwarfImportedUnit::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfImportedUnitStroageClass
 * from its counterpart of SgAsmDwarfImportedUnit. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfImportedUnit::initializeStorageClassArray(SgAsmDwarfImportedUnitStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfImportedUnit::pools.begin(); block != SgAsmDwarfImportedUnit::pools.end(); ++block) {
        SgAsmDwarfImportedUnit* pointer{reinterpret_cast<SgAsmDwarfImportedUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfImportedUnit::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 188752 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfInheritance that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfInheritance::SgAsmDwarfInheritance(const SgAsmDwarfInheritanceStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 188762 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfInheritance*
SgAsmDwarfInheritance::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfInheritance* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInheritance)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInheritance + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInheritance)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInheritance)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInheritance::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInheritance::pool_size};
        returnPointer = &(((SgAsmDwarfInheritance*)(SgAsmDwarfInheritance::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfInheritance*
SgAsmDwarfInheritance::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfInheritance* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInheritance)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInheritance + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInheritance)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfInheritance)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInheritance::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInheritance::pool_size};
        returnPointer = &(((SgAsmDwarfInheritance*)(SgAsmDwarfInheritance::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfInheritance::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfInheritance::pools.begin(); block != SgAsmDwarfInheritance::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInheritance*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInheritance::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfInheritance*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfInheritance::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfInheritance* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfInheritance::pools.begin(); block != SgAsmDwarfInheritance::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInheritance*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInheritance::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfInheritance::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfInheritance::clearMemoryPool() {
    SgAsmDwarfInheritance* tempPointer{nullptr};
    if (SgAsmDwarfInheritance::pools.empty() == false) {
        auto block{SgAsmDwarfInheritance::pools.begin()};
        SgAsmDwarfInheritance::next_node = reinterpret_cast<SgAsmDwarfInheritance*>(*block);
        for (; block != SgAsmDwarfInheritance::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfInheritance*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfInheritance::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfInheritance::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfInheritance::pool_size-1]);
        }
    }
}

void
SgAsmDwarfInheritance::deleteMemoryPool() {
    for (auto p: SgAsmDwarfInheritance::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfInheritance::next_node = nullptr;
    SgAsmDwarfInheritance::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfInheritance::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInheritance) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInheritance)};
    for (size_t blockIndex = SgAsmDwarfInheritance::pools.size(); (blockIndex * SgAsmDwarfInheritance::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInheritance) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInheritance) = %" PRIuPTR " SgAsmDwarfInheritance::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInheritance),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInheritance),SgAsmDwarfInheritance::pool_size);
        }
#endif

        SgAsmDwarfInheritance* pointer{static_cast<SgAsmDwarfInheritance*>(ROSE_MALLOC(SgAsmDwarfInheritance::pool_size * sizeof(SgAsmDwarfInheritance)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfInheritance::pool_size * sizeof(SgAsmDwarfInheritance));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfInheritance::pool_size * sizeof(SgAsmDwarfInheritance));
#endif
        SgAsmDwarfInheritance::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfInheritance::pool_size * sizeof(SgAsmDwarfInheritance), V_SgAsmDwarfInheritance));

        if (SgAsmDwarfInheritance::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfInheritance* blkptr{reinterpret_cast<SgAsmDwarfInheritance*>(SgAsmDwarfInheritance::pools[blockIndex-1])};
                blkptr[SgAsmDwarfInheritance::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfInheritance::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfInheritance::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfInheritance::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfInheritance::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfInheritance::pool_size - 1};
    SgAsmDwarfInheritance* testPointer{reinterpret_cast<SgAsmDwarfInheritance*>(SgAsmDwarfInheritance::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfInheritance::pool_size * (SgAsmDwarfInheritance::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfInheritanceStroageClass
 * from its counterpart of SgAsmDwarfInheritance. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfInheritance::initializeStorageClassArray(SgAsmDwarfInheritanceStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfInheritance::pools.begin(); block != SgAsmDwarfInheritance::pools.end(); ++block) {
        SgAsmDwarfInheritance* pointer{reinterpret_cast<SgAsmDwarfInheritance*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInheritance::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 188979 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfInlinedSubroutine that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfInlinedSubroutine::SgAsmDwarfInlinedSubroutine(const SgAsmDwarfInlinedSubroutineStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 188989 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfInlinedSubroutine*
SgAsmDwarfInlinedSubroutine::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfInlinedSubroutine* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInlinedSubroutine)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInlinedSubroutine + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInlinedSubroutine)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInlinedSubroutine)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInlinedSubroutine::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInlinedSubroutine::pool_size};
        returnPointer = &(((SgAsmDwarfInlinedSubroutine*)(SgAsmDwarfInlinedSubroutine::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfInlinedSubroutine*
SgAsmDwarfInlinedSubroutine::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfInlinedSubroutine* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInlinedSubroutine)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInlinedSubroutine + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInlinedSubroutine)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfInlinedSubroutine)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInlinedSubroutine::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInlinedSubroutine::pool_size};
        returnPointer = &(((SgAsmDwarfInlinedSubroutine*)(SgAsmDwarfInlinedSubroutine::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfInlinedSubroutine::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfInlinedSubroutine::pools.begin(); block != SgAsmDwarfInlinedSubroutine::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInlinedSubroutine*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInlinedSubroutine::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfInlinedSubroutine*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfInlinedSubroutine::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfInlinedSubroutine* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfInlinedSubroutine::pools.begin(); block != SgAsmDwarfInlinedSubroutine::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInlinedSubroutine*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInlinedSubroutine::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfInlinedSubroutine::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfInlinedSubroutine::clearMemoryPool() {
    SgAsmDwarfInlinedSubroutine* tempPointer{nullptr};
    if (SgAsmDwarfInlinedSubroutine::pools.empty() == false) {
        auto block{SgAsmDwarfInlinedSubroutine::pools.begin()};
        SgAsmDwarfInlinedSubroutine::next_node = reinterpret_cast<SgAsmDwarfInlinedSubroutine*>(*block);
        for (; block != SgAsmDwarfInlinedSubroutine::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfInlinedSubroutine*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfInlinedSubroutine::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfInlinedSubroutine::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfInlinedSubroutine::pool_size-1]);
        }
    }
}

void
SgAsmDwarfInlinedSubroutine::deleteMemoryPool() {
    for (auto p: SgAsmDwarfInlinedSubroutine::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfInlinedSubroutine::next_node = nullptr;
    SgAsmDwarfInlinedSubroutine::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfInlinedSubroutine::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInlinedSubroutine) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInlinedSubroutine)};
    for (size_t blockIndex = SgAsmDwarfInlinedSubroutine::pools.size(); (blockIndex * SgAsmDwarfInlinedSubroutine::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInlinedSubroutine) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInlinedSubroutine) = %" PRIuPTR " SgAsmDwarfInlinedSubroutine::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInlinedSubroutine),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInlinedSubroutine),SgAsmDwarfInlinedSubroutine::pool_size);
        }
#endif

        SgAsmDwarfInlinedSubroutine* pointer{static_cast<SgAsmDwarfInlinedSubroutine*>(ROSE_MALLOC(SgAsmDwarfInlinedSubroutine::pool_size * sizeof(SgAsmDwarfInlinedSubroutine)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfInlinedSubroutine::pool_size * sizeof(SgAsmDwarfInlinedSubroutine));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfInlinedSubroutine::pool_size * sizeof(SgAsmDwarfInlinedSubroutine));
#endif
        SgAsmDwarfInlinedSubroutine::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfInlinedSubroutine::pool_size * sizeof(SgAsmDwarfInlinedSubroutine), V_SgAsmDwarfInlinedSubroutine));

        if (SgAsmDwarfInlinedSubroutine::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfInlinedSubroutine* blkptr{reinterpret_cast<SgAsmDwarfInlinedSubroutine*>(SgAsmDwarfInlinedSubroutine::pools[blockIndex-1])};
                blkptr[SgAsmDwarfInlinedSubroutine::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfInlinedSubroutine::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfInlinedSubroutine::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfInlinedSubroutine::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfInlinedSubroutine::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfInlinedSubroutine::pool_size - 1};
    SgAsmDwarfInlinedSubroutine* testPointer{reinterpret_cast<SgAsmDwarfInlinedSubroutine*>(SgAsmDwarfInlinedSubroutine::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfInlinedSubroutine::pool_size * (SgAsmDwarfInlinedSubroutine::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfInlinedSubroutineStroageClass
 * from its counterpart of SgAsmDwarfInlinedSubroutine. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfInlinedSubroutine::initializeStorageClassArray(SgAsmDwarfInlinedSubroutineStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfInlinedSubroutine::pools.begin(); block != SgAsmDwarfInlinedSubroutine::pools.end(); ++block) {
        SgAsmDwarfInlinedSubroutine* pointer{reinterpret_cast<SgAsmDwarfInlinedSubroutine*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInlinedSubroutine::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 189207 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfInterfaceType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfInterfaceType::SgAsmDwarfInterfaceType(const SgAsmDwarfInterfaceTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 189217 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfInterfaceType*
SgAsmDwarfInterfaceType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfInterfaceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInterfaceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInterfaceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfInterfaceType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInterfaceType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInterfaceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInterfaceType::pool_size};
        returnPointer = &(((SgAsmDwarfInterfaceType*)(SgAsmDwarfInterfaceType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfInterfaceType*
SgAsmDwarfInterfaceType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfInterfaceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInterfaceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInterfaceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfInterfaceType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfInterfaceType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfInterfaceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfInterfaceType::pool_size};
        returnPointer = &(((SgAsmDwarfInterfaceType*)(SgAsmDwarfInterfaceType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfInterfaceType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfInterfaceType::pools.begin(); block != SgAsmDwarfInterfaceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInterfaceType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInterfaceType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfInterfaceType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfInterfaceType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfInterfaceType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfInterfaceType::pools.begin(); block != SgAsmDwarfInterfaceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfInterfaceType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInterfaceType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfInterfaceType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfInterfaceType::clearMemoryPool() {
    SgAsmDwarfInterfaceType* tempPointer{nullptr};
    if (SgAsmDwarfInterfaceType::pools.empty() == false) {
        auto block{SgAsmDwarfInterfaceType::pools.begin()};
        SgAsmDwarfInterfaceType::next_node = reinterpret_cast<SgAsmDwarfInterfaceType*>(*block);
        for (; block != SgAsmDwarfInterfaceType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfInterfaceType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfInterfaceType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfInterfaceType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfInterfaceType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfInterfaceType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfInterfaceType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfInterfaceType::next_node = nullptr;
    SgAsmDwarfInterfaceType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfInterfaceType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInterfaceType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInterfaceType)};
    for (size_t blockIndex = SgAsmDwarfInterfaceType::pools.size(); (blockIndex * SgAsmDwarfInterfaceType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInterfaceType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInterfaceType) = %" PRIuPTR " SgAsmDwarfInterfaceType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfInterfaceType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfInterfaceType),SgAsmDwarfInterfaceType::pool_size);
        }
#endif

        SgAsmDwarfInterfaceType* pointer{static_cast<SgAsmDwarfInterfaceType*>(ROSE_MALLOC(SgAsmDwarfInterfaceType::pool_size * sizeof(SgAsmDwarfInterfaceType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfInterfaceType::pool_size * sizeof(SgAsmDwarfInterfaceType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfInterfaceType::pool_size * sizeof(SgAsmDwarfInterfaceType));
#endif
        SgAsmDwarfInterfaceType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfInterfaceType::pool_size * sizeof(SgAsmDwarfInterfaceType), V_SgAsmDwarfInterfaceType));

        if (SgAsmDwarfInterfaceType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfInterfaceType* blkptr{reinterpret_cast<SgAsmDwarfInterfaceType*>(SgAsmDwarfInterfaceType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfInterfaceType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfInterfaceType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfInterfaceType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfInterfaceType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfInterfaceType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfInterfaceType::pool_size - 1};
    SgAsmDwarfInterfaceType* testPointer{reinterpret_cast<SgAsmDwarfInterfaceType*>(SgAsmDwarfInterfaceType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfInterfaceType::pool_size * (SgAsmDwarfInterfaceType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfInterfaceTypeStroageClass
 * from its counterpart of SgAsmDwarfInterfaceType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfInterfaceType::initializeStorageClassArray(SgAsmDwarfInterfaceTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfInterfaceType::pools.begin(); block != SgAsmDwarfInterfaceType::pools.end(); ++block) {
        SgAsmDwarfInterfaceType* pointer{reinterpret_cast<SgAsmDwarfInterfaceType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfInterfaceType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 189434 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfLabel that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfLabel::SgAsmDwarfLabel(const SgAsmDwarfLabelStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 189444 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfLabel*
SgAsmDwarfLabel::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfLabel* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLabel)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLabel + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLabel)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLabel)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLabel::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLabel::pool_size};
        returnPointer = &(((SgAsmDwarfLabel*)(SgAsmDwarfLabel::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfLabel*
SgAsmDwarfLabel::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfLabel* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLabel)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLabel + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLabel)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfLabel)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLabel::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLabel::pool_size};
        returnPointer = &(((SgAsmDwarfLabel*)(SgAsmDwarfLabel::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfLabel::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfLabel::pools.begin(); block != SgAsmDwarfLabel::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLabel*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLabel::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfLabel*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfLabel::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfLabel* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfLabel::pools.begin(); block != SgAsmDwarfLabel::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLabel*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLabel::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfLabel::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfLabel::clearMemoryPool() {
    SgAsmDwarfLabel* tempPointer{nullptr};
    if (SgAsmDwarfLabel::pools.empty() == false) {
        auto block{SgAsmDwarfLabel::pools.begin()};
        SgAsmDwarfLabel::next_node = reinterpret_cast<SgAsmDwarfLabel*>(*block);
        for (; block != SgAsmDwarfLabel::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfLabel*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfLabel::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfLabel::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfLabel::pool_size-1]);
        }
    }
}

void
SgAsmDwarfLabel::deleteMemoryPool() {
    for (auto p: SgAsmDwarfLabel::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfLabel::next_node = nullptr;
    SgAsmDwarfLabel::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfLabel::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLabel) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLabel)};
    for (size_t blockIndex = SgAsmDwarfLabel::pools.size(); (blockIndex * SgAsmDwarfLabel::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLabel) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLabel) = %" PRIuPTR " SgAsmDwarfLabel::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLabel),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLabel),SgAsmDwarfLabel::pool_size);
        }
#endif

        SgAsmDwarfLabel* pointer{static_cast<SgAsmDwarfLabel*>(ROSE_MALLOC(SgAsmDwarfLabel::pool_size * sizeof(SgAsmDwarfLabel)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfLabel::pool_size * sizeof(SgAsmDwarfLabel));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfLabel::pool_size * sizeof(SgAsmDwarfLabel));
#endif
        SgAsmDwarfLabel::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfLabel::pool_size * sizeof(SgAsmDwarfLabel), V_SgAsmDwarfLabel));

        if (SgAsmDwarfLabel::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfLabel* blkptr{reinterpret_cast<SgAsmDwarfLabel*>(SgAsmDwarfLabel::pools[blockIndex-1])};
                blkptr[SgAsmDwarfLabel::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfLabel::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfLabel::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfLabel::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfLabel::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfLabel::pool_size - 1};
    SgAsmDwarfLabel* testPointer{reinterpret_cast<SgAsmDwarfLabel*>(SgAsmDwarfLabel::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfLabel::pool_size * (SgAsmDwarfLabel::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfLabelStroageClass
 * from its counterpart of SgAsmDwarfLabel. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfLabel::initializeStorageClassArray(SgAsmDwarfLabelStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfLabel::pools.begin(); block != SgAsmDwarfLabel::pools.end(); ++block) {
        SgAsmDwarfLabel* pointer{reinterpret_cast<SgAsmDwarfLabel*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLabel::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 189661 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfLexicalBlock that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfLexicalBlock::SgAsmDwarfLexicalBlock(const SgAsmDwarfLexicalBlockStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 189671 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfLexicalBlock*
SgAsmDwarfLexicalBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfLexicalBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLexicalBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLexicalBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLexicalBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLexicalBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLexicalBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLexicalBlock::pool_size};
        returnPointer = &(((SgAsmDwarfLexicalBlock*)(SgAsmDwarfLexicalBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfLexicalBlock*
SgAsmDwarfLexicalBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfLexicalBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLexicalBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLexicalBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLexicalBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfLexicalBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLexicalBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLexicalBlock::pool_size};
        returnPointer = &(((SgAsmDwarfLexicalBlock*)(SgAsmDwarfLexicalBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfLexicalBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfLexicalBlock::pools.begin(); block != SgAsmDwarfLexicalBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLexicalBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLexicalBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfLexicalBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfLexicalBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfLexicalBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfLexicalBlock::pools.begin(); block != SgAsmDwarfLexicalBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLexicalBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLexicalBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfLexicalBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfLexicalBlock::clearMemoryPool() {
    SgAsmDwarfLexicalBlock* tempPointer{nullptr};
    if (SgAsmDwarfLexicalBlock::pools.empty() == false) {
        auto block{SgAsmDwarfLexicalBlock::pools.begin()};
        SgAsmDwarfLexicalBlock::next_node = reinterpret_cast<SgAsmDwarfLexicalBlock*>(*block);
        for (; block != SgAsmDwarfLexicalBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfLexicalBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfLexicalBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfLexicalBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfLexicalBlock::pool_size-1]);
        }
    }
}

void
SgAsmDwarfLexicalBlock::deleteMemoryPool() {
    for (auto p: SgAsmDwarfLexicalBlock::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfLexicalBlock::next_node = nullptr;
    SgAsmDwarfLexicalBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfLexicalBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLexicalBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLexicalBlock)};
    for (size_t blockIndex = SgAsmDwarfLexicalBlock::pools.size(); (blockIndex * SgAsmDwarfLexicalBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLexicalBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLexicalBlock) = %" PRIuPTR " SgAsmDwarfLexicalBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLexicalBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLexicalBlock),SgAsmDwarfLexicalBlock::pool_size);
        }
#endif

        SgAsmDwarfLexicalBlock* pointer{static_cast<SgAsmDwarfLexicalBlock*>(ROSE_MALLOC(SgAsmDwarfLexicalBlock::pool_size * sizeof(SgAsmDwarfLexicalBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfLexicalBlock::pool_size * sizeof(SgAsmDwarfLexicalBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfLexicalBlock::pool_size * sizeof(SgAsmDwarfLexicalBlock));
#endif
        SgAsmDwarfLexicalBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfLexicalBlock::pool_size * sizeof(SgAsmDwarfLexicalBlock), V_SgAsmDwarfLexicalBlock));

        if (SgAsmDwarfLexicalBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfLexicalBlock* blkptr{reinterpret_cast<SgAsmDwarfLexicalBlock*>(SgAsmDwarfLexicalBlock::pools[blockIndex-1])};
                blkptr[SgAsmDwarfLexicalBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfLexicalBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfLexicalBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfLexicalBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfLexicalBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfLexicalBlock::pool_size - 1};
    SgAsmDwarfLexicalBlock* testPointer{reinterpret_cast<SgAsmDwarfLexicalBlock*>(SgAsmDwarfLexicalBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfLexicalBlock::pool_size * (SgAsmDwarfLexicalBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfLexicalBlockStroageClass
 * from its counterpart of SgAsmDwarfLexicalBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfLexicalBlock::initializeStorageClassArray(SgAsmDwarfLexicalBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfLexicalBlock::pools.begin(); block != SgAsmDwarfLexicalBlock::pools.end(); ++block) {
        SgAsmDwarfLexicalBlock* pointer{reinterpret_cast<SgAsmDwarfLexicalBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLexicalBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 189889 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfMember that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfMember::SgAsmDwarfMember(const SgAsmDwarfMemberStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 189899 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_data_bit_offset = storageSource.storageOf_data_bit_offset;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfMember*
SgAsmDwarfMember::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfMember* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMember)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMember + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMember)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMember)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMember::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMember::pool_size};
        returnPointer = &(((SgAsmDwarfMember*)(SgAsmDwarfMember::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfMember*
SgAsmDwarfMember::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfMember* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMember)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMember + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMember)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfMember)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMember::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMember::pool_size};
        returnPointer = &(((SgAsmDwarfMember*)(SgAsmDwarfMember::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfMember::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfMember::pools.begin(); block != SgAsmDwarfMember::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMember*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMember::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfMember*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfMember::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfMember* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfMember::pools.begin(); block != SgAsmDwarfMember::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMember*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMember::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfMember::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfMember::clearMemoryPool() {
    SgAsmDwarfMember* tempPointer{nullptr};
    if (SgAsmDwarfMember::pools.empty() == false) {
        auto block{SgAsmDwarfMember::pools.begin()};
        SgAsmDwarfMember::next_node = reinterpret_cast<SgAsmDwarfMember*>(*block);
        for (; block != SgAsmDwarfMember::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfMember*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfMember::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfMember::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfMember::pool_size-1]);
        }
    }
}

void
SgAsmDwarfMember::deleteMemoryPool() {
    for (auto p: SgAsmDwarfMember::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfMember::next_node = nullptr;
    SgAsmDwarfMember::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfMember::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMember) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMember)};
    for (size_t blockIndex = SgAsmDwarfMember::pools.size(); (blockIndex * SgAsmDwarfMember::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMember) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMember) = %" PRIuPTR " SgAsmDwarfMember::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMember),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMember),SgAsmDwarfMember::pool_size);
        }
#endif

        SgAsmDwarfMember* pointer{static_cast<SgAsmDwarfMember*>(ROSE_MALLOC(SgAsmDwarfMember::pool_size * sizeof(SgAsmDwarfMember)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfMember::pool_size * sizeof(SgAsmDwarfMember));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfMember::pool_size * sizeof(SgAsmDwarfMember));
#endif
        SgAsmDwarfMember::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfMember::pool_size * sizeof(SgAsmDwarfMember), V_SgAsmDwarfMember));

        if (SgAsmDwarfMember::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfMember* blkptr{reinterpret_cast<SgAsmDwarfMember*>(SgAsmDwarfMember::pools[blockIndex-1])};
                blkptr[SgAsmDwarfMember::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfMember::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfMember::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfMember::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfMember::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfMember::pool_size - 1};
    SgAsmDwarfMember* testPointer{reinterpret_cast<SgAsmDwarfMember*>(SgAsmDwarfMember::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfMember::pool_size * (SgAsmDwarfMember::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfMemberStroageClass
 * from its counterpart of SgAsmDwarfMember. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfMember::initializeStorageClassArray(SgAsmDwarfMemberStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfMember::pools.begin(); block != SgAsmDwarfMember::pools.end(); ++block) {
        SgAsmDwarfMember* pointer{reinterpret_cast<SgAsmDwarfMember*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMember::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 190117 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfModule that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfModule::SgAsmDwarfModule(const SgAsmDwarfModuleStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 190127 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfModule*
SgAsmDwarfModule::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfModule)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfModule)};
        unsigned long positionInPool{localIndex % SgAsmDwarfModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfModule::pool_size};
        returnPointer = &(((SgAsmDwarfModule*)(SgAsmDwarfModule::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfModule*
SgAsmDwarfModule::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfModule)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfModule)};
        unsigned long positionInPool{localIndex % SgAsmDwarfModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfModule::pool_size};
        returnPointer = &(((SgAsmDwarfModule*)(SgAsmDwarfModule::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfModule::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfModule::pools.begin(); block != SgAsmDwarfModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfModule*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfModule::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfModule*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfModule::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfModule* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfModule::pools.begin(); block != SgAsmDwarfModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfModule*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfModule::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfModule::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfModule::clearMemoryPool() {
    SgAsmDwarfModule* tempPointer{nullptr};
    if (SgAsmDwarfModule::pools.empty() == false) {
        auto block{SgAsmDwarfModule::pools.begin()};
        SgAsmDwarfModule::next_node = reinterpret_cast<SgAsmDwarfModule*>(*block);
        for (; block != SgAsmDwarfModule::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfModule*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfModule::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfModule::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfModule::pool_size-1]);
        }
    }
}

void
SgAsmDwarfModule::deleteMemoryPool() {
    for (auto p: SgAsmDwarfModule::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfModule::next_node = nullptr;
    SgAsmDwarfModule::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfModule::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfModule) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfModule)};
    for (size_t blockIndex = SgAsmDwarfModule::pools.size(); (blockIndex * SgAsmDwarfModule::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfModule) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfModule) = %" PRIuPTR " SgAsmDwarfModule::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfModule),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfModule),SgAsmDwarfModule::pool_size);
        }
#endif

        SgAsmDwarfModule* pointer{static_cast<SgAsmDwarfModule*>(ROSE_MALLOC(SgAsmDwarfModule::pool_size * sizeof(SgAsmDwarfModule)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfModule::pool_size * sizeof(SgAsmDwarfModule));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfModule::pool_size * sizeof(SgAsmDwarfModule));
#endif
        SgAsmDwarfModule::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfModule::pool_size * sizeof(SgAsmDwarfModule), V_SgAsmDwarfModule));

        if (SgAsmDwarfModule::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfModule* blkptr{reinterpret_cast<SgAsmDwarfModule*>(SgAsmDwarfModule::pools[blockIndex-1])};
                blkptr[SgAsmDwarfModule::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfModule::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfModule::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfModule::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfModule::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfModule::pool_size - 1};
    SgAsmDwarfModule* testPointer{reinterpret_cast<SgAsmDwarfModule*>(SgAsmDwarfModule::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfModule::pool_size * (SgAsmDwarfModule::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfModuleStroageClass
 * from its counterpart of SgAsmDwarfModule. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfModule::initializeStorageClassArray(SgAsmDwarfModuleStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfModule::pools.begin(); block != SgAsmDwarfModule::pools.end(); ++block) {
        SgAsmDwarfModule* pointer{reinterpret_cast<SgAsmDwarfModule*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfModule::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 190344 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfMutableType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfMutableType::SgAsmDwarfMutableType(const SgAsmDwarfMutableTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 190354 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfMutableType*
SgAsmDwarfMutableType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfMutableType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMutableType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMutableType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMutableType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMutableType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMutableType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMutableType::pool_size};
        returnPointer = &(((SgAsmDwarfMutableType*)(SgAsmDwarfMutableType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfMutableType*
SgAsmDwarfMutableType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfMutableType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMutableType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMutableType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMutableType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfMutableType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMutableType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMutableType::pool_size};
        returnPointer = &(((SgAsmDwarfMutableType*)(SgAsmDwarfMutableType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfMutableType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfMutableType::pools.begin(); block != SgAsmDwarfMutableType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMutableType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMutableType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfMutableType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfMutableType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfMutableType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfMutableType::pools.begin(); block != SgAsmDwarfMutableType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMutableType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMutableType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfMutableType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfMutableType::clearMemoryPool() {
    SgAsmDwarfMutableType* tempPointer{nullptr};
    if (SgAsmDwarfMutableType::pools.empty() == false) {
        auto block{SgAsmDwarfMutableType::pools.begin()};
        SgAsmDwarfMutableType::next_node = reinterpret_cast<SgAsmDwarfMutableType*>(*block);
        for (; block != SgAsmDwarfMutableType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfMutableType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfMutableType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfMutableType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfMutableType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfMutableType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfMutableType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfMutableType::next_node = nullptr;
    SgAsmDwarfMutableType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfMutableType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMutableType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMutableType)};
    for (size_t blockIndex = SgAsmDwarfMutableType::pools.size(); (blockIndex * SgAsmDwarfMutableType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMutableType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMutableType) = %" PRIuPTR " SgAsmDwarfMutableType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMutableType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMutableType),SgAsmDwarfMutableType::pool_size);
        }
#endif

        SgAsmDwarfMutableType* pointer{static_cast<SgAsmDwarfMutableType*>(ROSE_MALLOC(SgAsmDwarfMutableType::pool_size * sizeof(SgAsmDwarfMutableType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfMutableType::pool_size * sizeof(SgAsmDwarfMutableType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfMutableType::pool_size * sizeof(SgAsmDwarfMutableType));
#endif
        SgAsmDwarfMutableType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfMutableType::pool_size * sizeof(SgAsmDwarfMutableType), V_SgAsmDwarfMutableType));

        if (SgAsmDwarfMutableType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfMutableType* blkptr{reinterpret_cast<SgAsmDwarfMutableType*>(SgAsmDwarfMutableType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfMutableType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfMutableType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfMutableType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfMutableType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfMutableType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfMutableType::pool_size - 1};
    SgAsmDwarfMutableType* testPointer{reinterpret_cast<SgAsmDwarfMutableType*>(SgAsmDwarfMutableType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfMutableType::pool_size * (SgAsmDwarfMutableType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfMutableTypeStroageClass
 * from its counterpart of SgAsmDwarfMutableType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfMutableType::initializeStorageClassArray(SgAsmDwarfMutableTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfMutableType::pools.begin(); block != SgAsmDwarfMutableType::pools.end(); ++block) {
        SgAsmDwarfMutableType* pointer{reinterpret_cast<SgAsmDwarfMutableType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMutableType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 190571 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfNamelist that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfNamelist::SgAsmDwarfNamelist(const SgAsmDwarfNamelistStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 190581 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfNamelist*
SgAsmDwarfNamelist::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfNamelist* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamelist)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamelist + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamelist)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelist)};
        unsigned long positionInPool{localIndex % SgAsmDwarfNamelist::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfNamelist::pool_size};
        returnPointer = &(((SgAsmDwarfNamelist*)(SgAsmDwarfNamelist::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfNamelist*
SgAsmDwarfNamelist::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfNamelist* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamelist)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamelist + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamelist)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfNamelist)};
        unsigned long positionInPool{localIndex % SgAsmDwarfNamelist::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfNamelist::pool_size};
        returnPointer = &(((SgAsmDwarfNamelist*)(SgAsmDwarfNamelist::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfNamelist::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfNamelist::pools.begin(); block != SgAsmDwarfNamelist::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfNamelist*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamelist::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfNamelist*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfNamelist::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfNamelist* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfNamelist::pools.begin(); block != SgAsmDwarfNamelist::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfNamelist*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamelist::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfNamelist::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfNamelist::clearMemoryPool() {
    SgAsmDwarfNamelist* tempPointer{nullptr};
    if (SgAsmDwarfNamelist::pools.empty() == false) {
        auto block{SgAsmDwarfNamelist::pools.begin()};
        SgAsmDwarfNamelist::next_node = reinterpret_cast<SgAsmDwarfNamelist*>(*block);
        for (; block != SgAsmDwarfNamelist::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfNamelist*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfNamelist::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfNamelist::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfNamelist::pool_size-1]);
        }
    }
}

void
SgAsmDwarfNamelist::deleteMemoryPool() {
    for (auto p: SgAsmDwarfNamelist::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfNamelist::next_node = nullptr;
    SgAsmDwarfNamelist::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfNamelist::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelist) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamelist)};
    for (size_t blockIndex = SgAsmDwarfNamelist::pools.size(); (blockIndex * SgAsmDwarfNamelist::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelist) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamelist) = %" PRIuPTR " SgAsmDwarfNamelist::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelist),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamelist),SgAsmDwarfNamelist::pool_size);
        }
#endif

        SgAsmDwarfNamelist* pointer{static_cast<SgAsmDwarfNamelist*>(ROSE_MALLOC(SgAsmDwarfNamelist::pool_size * sizeof(SgAsmDwarfNamelist)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfNamelist::pool_size * sizeof(SgAsmDwarfNamelist));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfNamelist::pool_size * sizeof(SgAsmDwarfNamelist));
#endif
        SgAsmDwarfNamelist::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfNamelist::pool_size * sizeof(SgAsmDwarfNamelist), V_SgAsmDwarfNamelist));

        if (SgAsmDwarfNamelist::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfNamelist* blkptr{reinterpret_cast<SgAsmDwarfNamelist*>(SgAsmDwarfNamelist::pools[blockIndex-1])};
                blkptr[SgAsmDwarfNamelist::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfNamelist::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfNamelist::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfNamelist::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfNamelist::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfNamelist::pool_size - 1};
    SgAsmDwarfNamelist* testPointer{reinterpret_cast<SgAsmDwarfNamelist*>(SgAsmDwarfNamelist::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfNamelist::pool_size * (SgAsmDwarfNamelist::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfNamelistStroageClass
 * from its counterpart of SgAsmDwarfNamelist. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfNamelist::initializeStorageClassArray(SgAsmDwarfNamelistStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfNamelist::pools.begin(); block != SgAsmDwarfNamelist::pools.end(); ++block) {
        SgAsmDwarfNamelist* pointer{reinterpret_cast<SgAsmDwarfNamelist*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamelist::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 190798 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfNamelistItem that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfNamelistItem::SgAsmDwarfNamelistItem(const SgAsmDwarfNamelistItemStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 190808 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfNamelistItem*
SgAsmDwarfNamelistItem::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfNamelistItem* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamelistItem)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamelistItem + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamelistItem)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelistItem)};
        unsigned long positionInPool{localIndex % SgAsmDwarfNamelistItem::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfNamelistItem::pool_size};
        returnPointer = &(((SgAsmDwarfNamelistItem*)(SgAsmDwarfNamelistItem::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfNamelistItem*
SgAsmDwarfNamelistItem::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfNamelistItem* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamelistItem)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamelistItem + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamelistItem)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfNamelistItem)};
        unsigned long positionInPool{localIndex % SgAsmDwarfNamelistItem::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfNamelistItem::pool_size};
        returnPointer = &(((SgAsmDwarfNamelistItem*)(SgAsmDwarfNamelistItem::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfNamelistItem::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfNamelistItem::pools.begin(); block != SgAsmDwarfNamelistItem::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfNamelistItem*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamelistItem::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfNamelistItem*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfNamelistItem::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfNamelistItem* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfNamelistItem::pools.begin(); block != SgAsmDwarfNamelistItem::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfNamelistItem*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamelistItem::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfNamelistItem::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfNamelistItem::clearMemoryPool() {
    SgAsmDwarfNamelistItem* tempPointer{nullptr};
    if (SgAsmDwarfNamelistItem::pools.empty() == false) {
        auto block{SgAsmDwarfNamelistItem::pools.begin()};
        SgAsmDwarfNamelistItem::next_node = reinterpret_cast<SgAsmDwarfNamelistItem*>(*block);
        for (; block != SgAsmDwarfNamelistItem::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfNamelistItem*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfNamelistItem::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfNamelistItem::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfNamelistItem::pool_size-1]);
        }
    }
}

void
SgAsmDwarfNamelistItem::deleteMemoryPool() {
    for (auto p: SgAsmDwarfNamelistItem::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfNamelistItem::next_node = nullptr;
    SgAsmDwarfNamelistItem::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfNamelistItem::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelistItem) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamelistItem)};
    for (size_t blockIndex = SgAsmDwarfNamelistItem::pools.size(); (blockIndex * SgAsmDwarfNamelistItem::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelistItem) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamelistItem) = %" PRIuPTR " SgAsmDwarfNamelistItem::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamelistItem),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamelistItem),SgAsmDwarfNamelistItem::pool_size);
        }
#endif

        SgAsmDwarfNamelistItem* pointer{static_cast<SgAsmDwarfNamelistItem*>(ROSE_MALLOC(SgAsmDwarfNamelistItem::pool_size * sizeof(SgAsmDwarfNamelistItem)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfNamelistItem::pool_size * sizeof(SgAsmDwarfNamelistItem));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfNamelistItem::pool_size * sizeof(SgAsmDwarfNamelistItem));
#endif
        SgAsmDwarfNamelistItem::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfNamelistItem::pool_size * sizeof(SgAsmDwarfNamelistItem), V_SgAsmDwarfNamelistItem));

        if (SgAsmDwarfNamelistItem::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfNamelistItem* blkptr{reinterpret_cast<SgAsmDwarfNamelistItem*>(SgAsmDwarfNamelistItem::pools[blockIndex-1])};
                blkptr[SgAsmDwarfNamelistItem::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfNamelistItem::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfNamelistItem::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfNamelistItem::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfNamelistItem::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfNamelistItem::pool_size - 1};
    SgAsmDwarfNamelistItem* testPointer{reinterpret_cast<SgAsmDwarfNamelistItem*>(SgAsmDwarfNamelistItem::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfNamelistItem::pool_size * (SgAsmDwarfNamelistItem::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfNamelistItemStroageClass
 * from its counterpart of SgAsmDwarfNamelistItem. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfNamelistItem::initializeStorageClassArray(SgAsmDwarfNamelistItemStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfNamelistItem::pools.begin(); block != SgAsmDwarfNamelistItem::pools.end(); ++block) {
        SgAsmDwarfNamelistItem* pointer{reinterpret_cast<SgAsmDwarfNamelistItem*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamelistItem::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 191025 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfNamespace that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfNamespace::SgAsmDwarfNamespace(const SgAsmDwarfNamespaceStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 191035 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfNamespace*
SgAsmDwarfNamespace::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfNamespace* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamespace)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamespace + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfNamespace)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamespace)};
        unsigned long positionInPool{localIndex % SgAsmDwarfNamespace::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfNamespace::pool_size};
        returnPointer = &(((SgAsmDwarfNamespace*)(SgAsmDwarfNamespace::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfNamespace*
SgAsmDwarfNamespace::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfNamespace* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamespace)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamespace + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfNamespace)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfNamespace)};
        unsigned long positionInPool{localIndex % SgAsmDwarfNamespace::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfNamespace::pool_size};
        returnPointer = &(((SgAsmDwarfNamespace*)(SgAsmDwarfNamespace::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfNamespace::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfNamespace::pools.begin(); block != SgAsmDwarfNamespace::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfNamespace*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamespace::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfNamespace*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfNamespace::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfNamespace* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfNamespace::pools.begin(); block != SgAsmDwarfNamespace::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfNamespace*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamespace::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfNamespace::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfNamespace::clearMemoryPool() {
    SgAsmDwarfNamespace* tempPointer{nullptr};
    if (SgAsmDwarfNamespace::pools.empty() == false) {
        auto block{SgAsmDwarfNamespace::pools.begin()};
        SgAsmDwarfNamespace::next_node = reinterpret_cast<SgAsmDwarfNamespace*>(*block);
        for (; block != SgAsmDwarfNamespace::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfNamespace*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfNamespace::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfNamespace::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfNamespace::pool_size-1]);
        }
    }
}

void
SgAsmDwarfNamespace::deleteMemoryPool() {
    for (auto p: SgAsmDwarfNamespace::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfNamespace::next_node = nullptr;
    SgAsmDwarfNamespace::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfNamespace::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamespace) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamespace)};
    for (size_t blockIndex = SgAsmDwarfNamespace::pools.size(); (blockIndex * SgAsmDwarfNamespace::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamespace) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamespace) = %" PRIuPTR " SgAsmDwarfNamespace::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfNamespace),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfNamespace),SgAsmDwarfNamespace::pool_size);
        }
#endif

        SgAsmDwarfNamespace* pointer{static_cast<SgAsmDwarfNamespace*>(ROSE_MALLOC(SgAsmDwarfNamespace::pool_size * sizeof(SgAsmDwarfNamespace)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfNamespace::pool_size * sizeof(SgAsmDwarfNamespace));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfNamespace::pool_size * sizeof(SgAsmDwarfNamespace));
#endif
        SgAsmDwarfNamespace::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfNamespace::pool_size * sizeof(SgAsmDwarfNamespace), V_SgAsmDwarfNamespace));

        if (SgAsmDwarfNamespace::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfNamespace* blkptr{reinterpret_cast<SgAsmDwarfNamespace*>(SgAsmDwarfNamespace::pools[blockIndex-1])};
                blkptr[SgAsmDwarfNamespace::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfNamespace::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfNamespace::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfNamespace::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfNamespace::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfNamespace::pool_size - 1};
    SgAsmDwarfNamespace* testPointer{reinterpret_cast<SgAsmDwarfNamespace*>(SgAsmDwarfNamespace::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfNamespace::pool_size * (SgAsmDwarfNamespace::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfNamespaceStroageClass
 * from its counterpart of SgAsmDwarfNamespace. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfNamespace::initializeStorageClassArray(SgAsmDwarfNamespaceStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfNamespace::pools.begin(); block != SgAsmDwarfNamespace::pools.end(); ++block) {
        SgAsmDwarfNamespace* pointer{reinterpret_cast<SgAsmDwarfNamespace*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfNamespace::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 191253 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfPackedType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfPackedType::SgAsmDwarfPackedType(const SgAsmDwarfPackedTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 191263 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfPackedType*
SgAsmDwarfPackedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfPackedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPackedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPackedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPackedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPackedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPackedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPackedType::pool_size};
        returnPointer = &(((SgAsmDwarfPackedType*)(SgAsmDwarfPackedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfPackedType*
SgAsmDwarfPackedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfPackedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPackedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPackedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPackedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfPackedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPackedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPackedType::pool_size};
        returnPointer = &(((SgAsmDwarfPackedType*)(SgAsmDwarfPackedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfPackedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfPackedType::pools.begin(); block != SgAsmDwarfPackedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPackedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPackedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfPackedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfPackedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfPackedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfPackedType::pools.begin(); block != SgAsmDwarfPackedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPackedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPackedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfPackedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfPackedType::clearMemoryPool() {
    SgAsmDwarfPackedType* tempPointer{nullptr};
    if (SgAsmDwarfPackedType::pools.empty() == false) {
        auto block{SgAsmDwarfPackedType::pools.begin()};
        SgAsmDwarfPackedType::next_node = reinterpret_cast<SgAsmDwarfPackedType*>(*block);
        for (; block != SgAsmDwarfPackedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfPackedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfPackedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfPackedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfPackedType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfPackedType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfPackedType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfPackedType::next_node = nullptr;
    SgAsmDwarfPackedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfPackedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPackedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPackedType)};
    for (size_t blockIndex = SgAsmDwarfPackedType::pools.size(); (blockIndex * SgAsmDwarfPackedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPackedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPackedType) = %" PRIuPTR " SgAsmDwarfPackedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPackedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPackedType),SgAsmDwarfPackedType::pool_size);
        }
#endif

        SgAsmDwarfPackedType* pointer{static_cast<SgAsmDwarfPackedType*>(ROSE_MALLOC(SgAsmDwarfPackedType::pool_size * sizeof(SgAsmDwarfPackedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfPackedType::pool_size * sizeof(SgAsmDwarfPackedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfPackedType::pool_size * sizeof(SgAsmDwarfPackedType));
#endif
        SgAsmDwarfPackedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfPackedType::pool_size * sizeof(SgAsmDwarfPackedType), V_SgAsmDwarfPackedType));

        if (SgAsmDwarfPackedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfPackedType* blkptr{reinterpret_cast<SgAsmDwarfPackedType*>(SgAsmDwarfPackedType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfPackedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfPackedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfPackedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfPackedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfPackedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfPackedType::pool_size - 1};
    SgAsmDwarfPackedType* testPointer{reinterpret_cast<SgAsmDwarfPackedType*>(SgAsmDwarfPackedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfPackedType::pool_size * (SgAsmDwarfPackedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfPackedTypeStroageClass
 * from its counterpart of SgAsmDwarfPackedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfPackedType::initializeStorageClassArray(SgAsmDwarfPackedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfPackedType::pools.begin(); block != SgAsmDwarfPackedType::pools.end(); ++block) {
        SgAsmDwarfPackedType* pointer{reinterpret_cast<SgAsmDwarfPackedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPackedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 191480 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfPartialUnit that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfPartialUnit::SgAsmDwarfPartialUnit(const SgAsmDwarfPartialUnitStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 191490 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfPartialUnit*
SgAsmDwarfPartialUnit::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfPartialUnit* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPartialUnit)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPartialUnit + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPartialUnit)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPartialUnit)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPartialUnit::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPartialUnit::pool_size};
        returnPointer = &(((SgAsmDwarfPartialUnit*)(SgAsmDwarfPartialUnit::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfPartialUnit*
SgAsmDwarfPartialUnit::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfPartialUnit* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPartialUnit)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPartialUnit + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPartialUnit)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfPartialUnit)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPartialUnit::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPartialUnit::pool_size};
        returnPointer = &(((SgAsmDwarfPartialUnit*)(SgAsmDwarfPartialUnit::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfPartialUnit::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfPartialUnit::pools.begin(); block != SgAsmDwarfPartialUnit::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPartialUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPartialUnit::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfPartialUnit*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfPartialUnit::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfPartialUnit* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfPartialUnit::pools.begin(); block != SgAsmDwarfPartialUnit::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPartialUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPartialUnit::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfPartialUnit::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfPartialUnit::clearMemoryPool() {
    SgAsmDwarfPartialUnit* tempPointer{nullptr};
    if (SgAsmDwarfPartialUnit::pools.empty() == false) {
        auto block{SgAsmDwarfPartialUnit::pools.begin()};
        SgAsmDwarfPartialUnit::next_node = reinterpret_cast<SgAsmDwarfPartialUnit*>(*block);
        for (; block != SgAsmDwarfPartialUnit::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfPartialUnit*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfPartialUnit::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfPartialUnit::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfPartialUnit::pool_size-1]);
        }
    }
}

void
SgAsmDwarfPartialUnit::deleteMemoryPool() {
    for (auto p: SgAsmDwarfPartialUnit::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfPartialUnit::next_node = nullptr;
    SgAsmDwarfPartialUnit::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfPartialUnit::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPartialUnit) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPartialUnit)};
    for (size_t blockIndex = SgAsmDwarfPartialUnit::pools.size(); (blockIndex * SgAsmDwarfPartialUnit::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPartialUnit) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPartialUnit) = %" PRIuPTR " SgAsmDwarfPartialUnit::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPartialUnit),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPartialUnit),SgAsmDwarfPartialUnit::pool_size);
        }
#endif

        SgAsmDwarfPartialUnit* pointer{static_cast<SgAsmDwarfPartialUnit*>(ROSE_MALLOC(SgAsmDwarfPartialUnit::pool_size * sizeof(SgAsmDwarfPartialUnit)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfPartialUnit::pool_size * sizeof(SgAsmDwarfPartialUnit));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfPartialUnit::pool_size * sizeof(SgAsmDwarfPartialUnit));
#endif
        SgAsmDwarfPartialUnit::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfPartialUnit::pool_size * sizeof(SgAsmDwarfPartialUnit), V_SgAsmDwarfPartialUnit));

        if (SgAsmDwarfPartialUnit::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfPartialUnit* blkptr{reinterpret_cast<SgAsmDwarfPartialUnit*>(SgAsmDwarfPartialUnit::pools[blockIndex-1])};
                blkptr[SgAsmDwarfPartialUnit::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfPartialUnit::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfPartialUnit::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfPartialUnit::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfPartialUnit::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfPartialUnit::pool_size - 1};
    SgAsmDwarfPartialUnit* testPointer{reinterpret_cast<SgAsmDwarfPartialUnit*>(SgAsmDwarfPartialUnit::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfPartialUnit::pool_size * (SgAsmDwarfPartialUnit::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfPartialUnitStroageClass
 * from its counterpart of SgAsmDwarfPartialUnit. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfPartialUnit::initializeStorageClassArray(SgAsmDwarfPartialUnitStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfPartialUnit::pools.begin(); block != SgAsmDwarfPartialUnit::pools.end(); ++block) {
        SgAsmDwarfPartialUnit* pointer{reinterpret_cast<SgAsmDwarfPartialUnit*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPartialUnit::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 191707 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfPointerType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfPointerType::SgAsmDwarfPointerType(const SgAsmDwarfPointerTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 191717 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfPointerType*
SgAsmDwarfPointerType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfPointerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPointerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPointerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPointerType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPointerType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPointerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPointerType::pool_size};
        returnPointer = &(((SgAsmDwarfPointerType*)(SgAsmDwarfPointerType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfPointerType*
SgAsmDwarfPointerType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfPointerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPointerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPointerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPointerType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfPointerType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPointerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPointerType::pool_size};
        returnPointer = &(((SgAsmDwarfPointerType*)(SgAsmDwarfPointerType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfPointerType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfPointerType::pools.begin(); block != SgAsmDwarfPointerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPointerType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPointerType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfPointerType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfPointerType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfPointerType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfPointerType::pools.begin(); block != SgAsmDwarfPointerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPointerType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPointerType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfPointerType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfPointerType::clearMemoryPool() {
    SgAsmDwarfPointerType* tempPointer{nullptr};
    if (SgAsmDwarfPointerType::pools.empty() == false) {
        auto block{SgAsmDwarfPointerType::pools.begin()};
        SgAsmDwarfPointerType::next_node = reinterpret_cast<SgAsmDwarfPointerType*>(*block);
        for (; block != SgAsmDwarfPointerType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfPointerType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfPointerType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfPointerType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfPointerType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfPointerType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfPointerType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfPointerType::next_node = nullptr;
    SgAsmDwarfPointerType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfPointerType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPointerType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPointerType)};
    for (size_t blockIndex = SgAsmDwarfPointerType::pools.size(); (blockIndex * SgAsmDwarfPointerType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPointerType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPointerType) = %" PRIuPTR " SgAsmDwarfPointerType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPointerType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPointerType),SgAsmDwarfPointerType::pool_size);
        }
#endif

        SgAsmDwarfPointerType* pointer{static_cast<SgAsmDwarfPointerType*>(ROSE_MALLOC(SgAsmDwarfPointerType::pool_size * sizeof(SgAsmDwarfPointerType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfPointerType::pool_size * sizeof(SgAsmDwarfPointerType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfPointerType::pool_size * sizeof(SgAsmDwarfPointerType));
#endif
        SgAsmDwarfPointerType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfPointerType::pool_size * sizeof(SgAsmDwarfPointerType), V_SgAsmDwarfPointerType));

        if (SgAsmDwarfPointerType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfPointerType* blkptr{reinterpret_cast<SgAsmDwarfPointerType*>(SgAsmDwarfPointerType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfPointerType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfPointerType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfPointerType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfPointerType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfPointerType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfPointerType::pool_size - 1};
    SgAsmDwarfPointerType* testPointer{reinterpret_cast<SgAsmDwarfPointerType*>(SgAsmDwarfPointerType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfPointerType::pool_size * (SgAsmDwarfPointerType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfPointerTypeStroageClass
 * from its counterpart of SgAsmDwarfPointerType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfPointerType::initializeStorageClassArray(SgAsmDwarfPointerTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfPointerType::pools.begin(); block != SgAsmDwarfPointerType::pools.end(); ++block) {
        SgAsmDwarfPointerType* pointer{reinterpret_cast<SgAsmDwarfPointerType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPointerType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 191934 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfPtrToMemberType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfPtrToMemberType::SgAsmDwarfPtrToMemberType(const SgAsmDwarfPtrToMemberTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 191944 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfPtrToMemberType*
SgAsmDwarfPtrToMemberType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfPtrToMemberType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPtrToMemberType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPtrToMemberType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfPtrToMemberType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPtrToMemberType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPtrToMemberType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPtrToMemberType::pool_size};
        returnPointer = &(((SgAsmDwarfPtrToMemberType*)(SgAsmDwarfPtrToMemberType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfPtrToMemberType*
SgAsmDwarfPtrToMemberType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfPtrToMemberType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPtrToMemberType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPtrToMemberType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfPtrToMemberType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfPtrToMemberType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfPtrToMemberType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfPtrToMemberType::pool_size};
        returnPointer = &(((SgAsmDwarfPtrToMemberType*)(SgAsmDwarfPtrToMemberType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfPtrToMemberType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfPtrToMemberType::pools.begin(); block != SgAsmDwarfPtrToMemberType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPtrToMemberType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPtrToMemberType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfPtrToMemberType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfPtrToMemberType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfPtrToMemberType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfPtrToMemberType::pools.begin(); block != SgAsmDwarfPtrToMemberType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfPtrToMemberType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPtrToMemberType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfPtrToMemberType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfPtrToMemberType::clearMemoryPool() {
    SgAsmDwarfPtrToMemberType* tempPointer{nullptr};
    if (SgAsmDwarfPtrToMemberType::pools.empty() == false) {
        auto block{SgAsmDwarfPtrToMemberType::pools.begin()};
        SgAsmDwarfPtrToMemberType::next_node = reinterpret_cast<SgAsmDwarfPtrToMemberType*>(*block);
        for (; block != SgAsmDwarfPtrToMemberType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfPtrToMemberType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfPtrToMemberType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfPtrToMemberType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfPtrToMemberType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfPtrToMemberType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfPtrToMemberType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfPtrToMemberType::next_node = nullptr;
    SgAsmDwarfPtrToMemberType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfPtrToMemberType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPtrToMemberType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPtrToMemberType)};
    for (size_t blockIndex = SgAsmDwarfPtrToMemberType::pools.size(); (blockIndex * SgAsmDwarfPtrToMemberType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPtrToMemberType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPtrToMemberType) = %" PRIuPTR " SgAsmDwarfPtrToMemberType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfPtrToMemberType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfPtrToMemberType),SgAsmDwarfPtrToMemberType::pool_size);
        }
#endif

        SgAsmDwarfPtrToMemberType* pointer{static_cast<SgAsmDwarfPtrToMemberType*>(ROSE_MALLOC(SgAsmDwarfPtrToMemberType::pool_size * sizeof(SgAsmDwarfPtrToMemberType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfPtrToMemberType::pool_size * sizeof(SgAsmDwarfPtrToMemberType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfPtrToMemberType::pool_size * sizeof(SgAsmDwarfPtrToMemberType));
#endif
        SgAsmDwarfPtrToMemberType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfPtrToMemberType::pool_size * sizeof(SgAsmDwarfPtrToMemberType), V_SgAsmDwarfPtrToMemberType));

        if (SgAsmDwarfPtrToMemberType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfPtrToMemberType* blkptr{reinterpret_cast<SgAsmDwarfPtrToMemberType*>(SgAsmDwarfPtrToMemberType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfPtrToMemberType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfPtrToMemberType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfPtrToMemberType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfPtrToMemberType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfPtrToMemberType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfPtrToMemberType::pool_size - 1};
    SgAsmDwarfPtrToMemberType* testPointer{reinterpret_cast<SgAsmDwarfPtrToMemberType*>(SgAsmDwarfPtrToMemberType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfPtrToMemberType::pool_size * (SgAsmDwarfPtrToMemberType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfPtrToMemberTypeStroageClass
 * from its counterpart of SgAsmDwarfPtrToMemberType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfPtrToMemberType::initializeStorageClassArray(SgAsmDwarfPtrToMemberTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfPtrToMemberType::pools.begin(); block != SgAsmDwarfPtrToMemberType::pools.end(); ++block) {
        SgAsmDwarfPtrToMemberType* pointer{reinterpret_cast<SgAsmDwarfPtrToMemberType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfPtrToMemberType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 192161 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfReferenceType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfReferenceType::SgAsmDwarfReferenceType(const SgAsmDwarfReferenceTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 192171 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfReferenceType*
SgAsmDwarfReferenceType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfReferenceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfReferenceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfReferenceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfReferenceType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfReferenceType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfReferenceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfReferenceType::pool_size};
        returnPointer = &(((SgAsmDwarfReferenceType*)(SgAsmDwarfReferenceType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfReferenceType*
SgAsmDwarfReferenceType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfReferenceType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfReferenceType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfReferenceType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfReferenceType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfReferenceType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfReferenceType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfReferenceType::pool_size};
        returnPointer = &(((SgAsmDwarfReferenceType*)(SgAsmDwarfReferenceType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfReferenceType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfReferenceType::pools.begin(); block != SgAsmDwarfReferenceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfReferenceType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfReferenceType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfReferenceType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfReferenceType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfReferenceType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfReferenceType::pools.begin(); block != SgAsmDwarfReferenceType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfReferenceType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfReferenceType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfReferenceType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfReferenceType::clearMemoryPool() {
    SgAsmDwarfReferenceType* tempPointer{nullptr};
    if (SgAsmDwarfReferenceType::pools.empty() == false) {
        auto block{SgAsmDwarfReferenceType::pools.begin()};
        SgAsmDwarfReferenceType::next_node = reinterpret_cast<SgAsmDwarfReferenceType*>(*block);
        for (; block != SgAsmDwarfReferenceType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfReferenceType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfReferenceType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfReferenceType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfReferenceType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfReferenceType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfReferenceType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfReferenceType::next_node = nullptr;
    SgAsmDwarfReferenceType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfReferenceType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfReferenceType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfReferenceType)};
    for (size_t blockIndex = SgAsmDwarfReferenceType::pools.size(); (blockIndex * SgAsmDwarfReferenceType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfReferenceType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfReferenceType) = %" PRIuPTR " SgAsmDwarfReferenceType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfReferenceType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfReferenceType),SgAsmDwarfReferenceType::pool_size);
        }
#endif

        SgAsmDwarfReferenceType* pointer{static_cast<SgAsmDwarfReferenceType*>(ROSE_MALLOC(SgAsmDwarfReferenceType::pool_size * sizeof(SgAsmDwarfReferenceType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfReferenceType::pool_size * sizeof(SgAsmDwarfReferenceType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfReferenceType::pool_size * sizeof(SgAsmDwarfReferenceType));
#endif
        SgAsmDwarfReferenceType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfReferenceType::pool_size * sizeof(SgAsmDwarfReferenceType), V_SgAsmDwarfReferenceType));

        if (SgAsmDwarfReferenceType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfReferenceType* blkptr{reinterpret_cast<SgAsmDwarfReferenceType*>(SgAsmDwarfReferenceType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfReferenceType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfReferenceType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfReferenceType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfReferenceType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfReferenceType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfReferenceType::pool_size - 1};
    SgAsmDwarfReferenceType* testPointer{reinterpret_cast<SgAsmDwarfReferenceType*>(SgAsmDwarfReferenceType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfReferenceType::pool_size * (SgAsmDwarfReferenceType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfReferenceTypeStroageClass
 * from its counterpart of SgAsmDwarfReferenceType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfReferenceType::initializeStorageClassArray(SgAsmDwarfReferenceTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfReferenceType::pools.begin(); block != SgAsmDwarfReferenceType::pools.end(); ++block) {
        SgAsmDwarfReferenceType* pointer{reinterpret_cast<SgAsmDwarfReferenceType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfReferenceType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 192388 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfRestrictType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfRestrictType::SgAsmDwarfRestrictType(const SgAsmDwarfRestrictTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 192398 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfRestrictType*
SgAsmDwarfRestrictType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfRestrictType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfRestrictType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfRestrictType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfRestrictType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfRestrictType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfRestrictType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfRestrictType::pool_size};
        returnPointer = &(((SgAsmDwarfRestrictType*)(SgAsmDwarfRestrictType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfRestrictType*
SgAsmDwarfRestrictType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfRestrictType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfRestrictType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfRestrictType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfRestrictType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfRestrictType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfRestrictType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfRestrictType::pool_size};
        returnPointer = &(((SgAsmDwarfRestrictType*)(SgAsmDwarfRestrictType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfRestrictType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfRestrictType::pools.begin(); block != SgAsmDwarfRestrictType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfRestrictType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfRestrictType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfRestrictType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfRestrictType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfRestrictType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfRestrictType::pools.begin(); block != SgAsmDwarfRestrictType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfRestrictType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfRestrictType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfRestrictType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfRestrictType::clearMemoryPool() {
    SgAsmDwarfRestrictType* tempPointer{nullptr};
    if (SgAsmDwarfRestrictType::pools.empty() == false) {
        auto block{SgAsmDwarfRestrictType::pools.begin()};
        SgAsmDwarfRestrictType::next_node = reinterpret_cast<SgAsmDwarfRestrictType*>(*block);
        for (; block != SgAsmDwarfRestrictType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfRestrictType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfRestrictType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfRestrictType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfRestrictType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfRestrictType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfRestrictType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfRestrictType::next_node = nullptr;
    SgAsmDwarfRestrictType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfRestrictType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfRestrictType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfRestrictType)};
    for (size_t blockIndex = SgAsmDwarfRestrictType::pools.size(); (blockIndex * SgAsmDwarfRestrictType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfRestrictType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfRestrictType) = %" PRIuPTR " SgAsmDwarfRestrictType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfRestrictType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfRestrictType),SgAsmDwarfRestrictType::pool_size);
        }
#endif

        SgAsmDwarfRestrictType* pointer{static_cast<SgAsmDwarfRestrictType*>(ROSE_MALLOC(SgAsmDwarfRestrictType::pool_size * sizeof(SgAsmDwarfRestrictType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfRestrictType::pool_size * sizeof(SgAsmDwarfRestrictType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfRestrictType::pool_size * sizeof(SgAsmDwarfRestrictType));
#endif
        SgAsmDwarfRestrictType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfRestrictType::pool_size * sizeof(SgAsmDwarfRestrictType), V_SgAsmDwarfRestrictType));

        if (SgAsmDwarfRestrictType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfRestrictType* blkptr{reinterpret_cast<SgAsmDwarfRestrictType*>(SgAsmDwarfRestrictType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfRestrictType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfRestrictType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfRestrictType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfRestrictType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfRestrictType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfRestrictType::pool_size - 1};
    SgAsmDwarfRestrictType* testPointer{reinterpret_cast<SgAsmDwarfRestrictType*>(SgAsmDwarfRestrictType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfRestrictType::pool_size * (SgAsmDwarfRestrictType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfRestrictTypeStroageClass
 * from its counterpart of SgAsmDwarfRestrictType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfRestrictType::initializeStorageClassArray(SgAsmDwarfRestrictTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfRestrictType::pools.begin(); block != SgAsmDwarfRestrictType::pools.end(); ++block) {
        SgAsmDwarfRestrictType* pointer{reinterpret_cast<SgAsmDwarfRestrictType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfRestrictType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 192615 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfSetType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfSetType::SgAsmDwarfSetType(const SgAsmDwarfSetTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 192625 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfSetType*
SgAsmDwarfSetType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfSetType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSetType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSetType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSetType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSetType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSetType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSetType::pool_size};
        returnPointer = &(((SgAsmDwarfSetType*)(SgAsmDwarfSetType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfSetType*
SgAsmDwarfSetType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfSetType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSetType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSetType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSetType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfSetType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSetType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSetType::pool_size};
        returnPointer = &(((SgAsmDwarfSetType*)(SgAsmDwarfSetType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfSetType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfSetType::pools.begin(); block != SgAsmDwarfSetType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSetType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSetType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfSetType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfSetType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfSetType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfSetType::pools.begin(); block != SgAsmDwarfSetType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSetType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSetType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfSetType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfSetType::clearMemoryPool() {
    SgAsmDwarfSetType* tempPointer{nullptr};
    if (SgAsmDwarfSetType::pools.empty() == false) {
        auto block{SgAsmDwarfSetType::pools.begin()};
        SgAsmDwarfSetType::next_node = reinterpret_cast<SgAsmDwarfSetType*>(*block);
        for (; block != SgAsmDwarfSetType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfSetType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfSetType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfSetType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfSetType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfSetType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfSetType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfSetType::next_node = nullptr;
    SgAsmDwarfSetType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfSetType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSetType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSetType)};
    for (size_t blockIndex = SgAsmDwarfSetType::pools.size(); (blockIndex * SgAsmDwarfSetType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSetType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSetType) = %" PRIuPTR " SgAsmDwarfSetType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSetType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSetType),SgAsmDwarfSetType::pool_size);
        }
#endif

        SgAsmDwarfSetType* pointer{static_cast<SgAsmDwarfSetType*>(ROSE_MALLOC(SgAsmDwarfSetType::pool_size * sizeof(SgAsmDwarfSetType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfSetType::pool_size * sizeof(SgAsmDwarfSetType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfSetType::pool_size * sizeof(SgAsmDwarfSetType));
#endif
        SgAsmDwarfSetType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfSetType::pool_size * sizeof(SgAsmDwarfSetType), V_SgAsmDwarfSetType));

        if (SgAsmDwarfSetType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfSetType* blkptr{reinterpret_cast<SgAsmDwarfSetType*>(SgAsmDwarfSetType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfSetType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfSetType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfSetType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfSetType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfSetType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfSetType::pool_size - 1};
    SgAsmDwarfSetType* testPointer{reinterpret_cast<SgAsmDwarfSetType*>(SgAsmDwarfSetType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfSetType::pool_size * (SgAsmDwarfSetType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfSetTypeStroageClass
 * from its counterpart of SgAsmDwarfSetType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfSetType::initializeStorageClassArray(SgAsmDwarfSetTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfSetType::pools.begin(); block != SgAsmDwarfSetType::pools.end(); ++block) {
        SgAsmDwarfSetType* pointer{reinterpret_cast<SgAsmDwarfSetType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSetType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 192842 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfSharedType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfSharedType::SgAsmDwarfSharedType(const SgAsmDwarfSharedTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 192852 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfSharedType*
SgAsmDwarfSharedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfSharedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSharedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSharedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSharedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSharedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSharedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSharedType::pool_size};
        returnPointer = &(((SgAsmDwarfSharedType*)(SgAsmDwarfSharedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfSharedType*
SgAsmDwarfSharedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfSharedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSharedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSharedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSharedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfSharedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSharedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSharedType::pool_size};
        returnPointer = &(((SgAsmDwarfSharedType*)(SgAsmDwarfSharedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfSharedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfSharedType::pools.begin(); block != SgAsmDwarfSharedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSharedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSharedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfSharedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfSharedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfSharedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfSharedType::pools.begin(); block != SgAsmDwarfSharedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSharedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSharedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfSharedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfSharedType::clearMemoryPool() {
    SgAsmDwarfSharedType* tempPointer{nullptr};
    if (SgAsmDwarfSharedType::pools.empty() == false) {
        auto block{SgAsmDwarfSharedType::pools.begin()};
        SgAsmDwarfSharedType::next_node = reinterpret_cast<SgAsmDwarfSharedType*>(*block);
        for (; block != SgAsmDwarfSharedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfSharedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfSharedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfSharedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfSharedType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfSharedType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfSharedType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfSharedType::next_node = nullptr;
    SgAsmDwarfSharedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfSharedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSharedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSharedType)};
    for (size_t blockIndex = SgAsmDwarfSharedType::pools.size(); (blockIndex * SgAsmDwarfSharedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSharedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSharedType) = %" PRIuPTR " SgAsmDwarfSharedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSharedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSharedType),SgAsmDwarfSharedType::pool_size);
        }
#endif

        SgAsmDwarfSharedType* pointer{static_cast<SgAsmDwarfSharedType*>(ROSE_MALLOC(SgAsmDwarfSharedType::pool_size * sizeof(SgAsmDwarfSharedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfSharedType::pool_size * sizeof(SgAsmDwarfSharedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfSharedType::pool_size * sizeof(SgAsmDwarfSharedType));
#endif
        SgAsmDwarfSharedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfSharedType::pool_size * sizeof(SgAsmDwarfSharedType), V_SgAsmDwarfSharedType));

        if (SgAsmDwarfSharedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfSharedType* blkptr{reinterpret_cast<SgAsmDwarfSharedType*>(SgAsmDwarfSharedType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfSharedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfSharedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfSharedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfSharedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfSharedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfSharedType::pool_size - 1};
    SgAsmDwarfSharedType* testPointer{reinterpret_cast<SgAsmDwarfSharedType*>(SgAsmDwarfSharedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfSharedType::pool_size * (SgAsmDwarfSharedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfSharedTypeStroageClass
 * from its counterpart of SgAsmDwarfSharedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfSharedType::initializeStorageClassArray(SgAsmDwarfSharedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfSharedType::pools.begin(); block != SgAsmDwarfSharedType::pools.end(); ++block) {
        SgAsmDwarfSharedType* pointer{reinterpret_cast<SgAsmDwarfSharedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSharedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 193069 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfStringType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfStringType::SgAsmDwarfStringType(const SgAsmDwarfStringTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 193079 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfStringType*
SgAsmDwarfStringType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfStringType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfStringType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfStringType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfStringType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStringType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfStringType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfStringType::pool_size};
        returnPointer = &(((SgAsmDwarfStringType*)(SgAsmDwarfStringType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfStringType*
SgAsmDwarfStringType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfStringType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfStringType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfStringType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfStringType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfStringType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfStringType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfStringType::pool_size};
        returnPointer = &(((SgAsmDwarfStringType*)(SgAsmDwarfStringType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfStringType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfStringType::pools.begin(); block != SgAsmDwarfStringType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfStringType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfStringType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfStringType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfStringType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfStringType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfStringType::pools.begin(); block != SgAsmDwarfStringType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfStringType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfStringType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfStringType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfStringType::clearMemoryPool() {
    SgAsmDwarfStringType* tempPointer{nullptr};
    if (SgAsmDwarfStringType::pools.empty() == false) {
        auto block{SgAsmDwarfStringType::pools.begin()};
        SgAsmDwarfStringType::next_node = reinterpret_cast<SgAsmDwarfStringType*>(*block);
        for (; block != SgAsmDwarfStringType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfStringType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfStringType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfStringType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfStringType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfStringType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfStringType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfStringType::next_node = nullptr;
    SgAsmDwarfStringType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfStringType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStringType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfStringType)};
    for (size_t blockIndex = SgAsmDwarfStringType::pools.size(); (blockIndex * SgAsmDwarfStringType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStringType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfStringType) = %" PRIuPTR " SgAsmDwarfStringType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStringType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfStringType),SgAsmDwarfStringType::pool_size);
        }
#endif

        SgAsmDwarfStringType* pointer{static_cast<SgAsmDwarfStringType*>(ROSE_MALLOC(SgAsmDwarfStringType::pool_size * sizeof(SgAsmDwarfStringType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfStringType::pool_size * sizeof(SgAsmDwarfStringType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfStringType::pool_size * sizeof(SgAsmDwarfStringType));
#endif
        SgAsmDwarfStringType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfStringType::pool_size * sizeof(SgAsmDwarfStringType), V_SgAsmDwarfStringType));

        if (SgAsmDwarfStringType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfStringType* blkptr{reinterpret_cast<SgAsmDwarfStringType*>(SgAsmDwarfStringType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfStringType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfStringType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfStringType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfStringType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfStringType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfStringType::pool_size - 1};
    SgAsmDwarfStringType* testPointer{reinterpret_cast<SgAsmDwarfStringType*>(SgAsmDwarfStringType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfStringType::pool_size * (SgAsmDwarfStringType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfStringTypeStroageClass
 * from its counterpart of SgAsmDwarfStringType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfStringType::initializeStorageClassArray(SgAsmDwarfStringTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfStringType::pools.begin(); block != SgAsmDwarfStringType::pools.end(); ++block) {
        SgAsmDwarfStringType* pointer{reinterpret_cast<SgAsmDwarfStringType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfStringType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 193296 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfStructureType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfStructureType::SgAsmDwarfStructureType(const SgAsmDwarfStructureTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 193306 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfStructureType*
SgAsmDwarfStructureType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfStructureType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfStructureType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfStructureType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfStructureType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStructureType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfStructureType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfStructureType::pool_size};
        returnPointer = &(((SgAsmDwarfStructureType*)(SgAsmDwarfStructureType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfStructureType*
SgAsmDwarfStructureType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfStructureType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfStructureType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfStructureType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfStructureType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfStructureType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfStructureType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfStructureType::pool_size};
        returnPointer = &(((SgAsmDwarfStructureType*)(SgAsmDwarfStructureType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfStructureType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfStructureType::pools.begin(); block != SgAsmDwarfStructureType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfStructureType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfStructureType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfStructureType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfStructureType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfStructureType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfStructureType::pools.begin(); block != SgAsmDwarfStructureType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfStructureType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfStructureType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfStructureType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfStructureType::clearMemoryPool() {
    SgAsmDwarfStructureType* tempPointer{nullptr};
    if (SgAsmDwarfStructureType::pools.empty() == false) {
        auto block{SgAsmDwarfStructureType::pools.begin()};
        SgAsmDwarfStructureType::next_node = reinterpret_cast<SgAsmDwarfStructureType*>(*block);
        for (; block != SgAsmDwarfStructureType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfStructureType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfStructureType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfStructureType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfStructureType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfStructureType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfStructureType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfStructureType::next_node = nullptr;
    SgAsmDwarfStructureType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfStructureType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStructureType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfStructureType)};
    for (size_t blockIndex = SgAsmDwarfStructureType::pools.size(); (blockIndex * SgAsmDwarfStructureType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStructureType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfStructureType) = %" PRIuPTR " SgAsmDwarfStructureType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfStructureType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfStructureType),SgAsmDwarfStructureType::pool_size);
        }
#endif

        SgAsmDwarfStructureType* pointer{static_cast<SgAsmDwarfStructureType*>(ROSE_MALLOC(SgAsmDwarfStructureType::pool_size * sizeof(SgAsmDwarfStructureType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfStructureType::pool_size * sizeof(SgAsmDwarfStructureType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfStructureType::pool_size * sizeof(SgAsmDwarfStructureType));
#endif
        SgAsmDwarfStructureType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfStructureType::pool_size * sizeof(SgAsmDwarfStructureType), V_SgAsmDwarfStructureType));

        if (SgAsmDwarfStructureType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfStructureType* blkptr{reinterpret_cast<SgAsmDwarfStructureType*>(SgAsmDwarfStructureType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfStructureType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfStructureType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfStructureType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfStructureType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfStructureType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfStructureType::pool_size - 1};
    SgAsmDwarfStructureType* testPointer{reinterpret_cast<SgAsmDwarfStructureType*>(SgAsmDwarfStructureType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfStructureType::pool_size * (SgAsmDwarfStructureType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfStructureTypeStroageClass
 * from its counterpart of SgAsmDwarfStructureType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfStructureType::initializeStorageClassArray(SgAsmDwarfStructureTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfStructureType::pools.begin(); block != SgAsmDwarfStructureType::pools.end(); ++block) {
        SgAsmDwarfStructureType* pointer{reinterpret_cast<SgAsmDwarfStructureType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfStructureType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 193524 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfSubprogram that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfSubprogram::SgAsmDwarfSubprogram(const SgAsmDwarfSubprogramStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 193534 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));
    p_linkage_name = storageSource.storageOf_linkage_name.rebuildDataStoredInEasyStorageClass();
    p_is_main_subprogram = storageSource.storageOf_is_main_subprogram;
    p_is_const_expr = storageSource.storageOf_is_const_expr;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfSubprogram*
SgAsmDwarfSubprogram::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfSubprogram* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubprogram)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubprogram + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubprogram)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubprogram)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSubprogram::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSubprogram::pool_size};
        returnPointer = &(((SgAsmDwarfSubprogram*)(SgAsmDwarfSubprogram::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfSubprogram*
SgAsmDwarfSubprogram::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfSubprogram* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubprogram)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubprogram + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubprogram)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfSubprogram)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSubprogram::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSubprogram::pool_size};
        returnPointer = &(((SgAsmDwarfSubprogram*)(SgAsmDwarfSubprogram::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfSubprogram::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfSubprogram::pools.begin(); block != SgAsmDwarfSubprogram::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSubprogram*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubprogram::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfSubprogram*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfSubprogram::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfSubprogram* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfSubprogram::pools.begin(); block != SgAsmDwarfSubprogram::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSubprogram*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubprogram::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfSubprogram::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfSubprogram::clearMemoryPool() {
    SgAsmDwarfSubprogram* tempPointer{nullptr};
    if (SgAsmDwarfSubprogram::pools.empty() == false) {
        auto block{SgAsmDwarfSubprogram::pools.begin()};
        SgAsmDwarfSubprogram::next_node = reinterpret_cast<SgAsmDwarfSubprogram*>(*block);
        for (; block != SgAsmDwarfSubprogram::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfSubprogram*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfSubprogram::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfSubprogram::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfSubprogram::pool_size-1]);
        }
    }
}

void
SgAsmDwarfSubprogram::deleteMemoryPool() {
    for (auto p: SgAsmDwarfSubprogram::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfSubprogram::next_node = nullptr;
    SgAsmDwarfSubprogram::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfSubprogram::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubprogram) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubprogram)};
    for (size_t blockIndex = SgAsmDwarfSubprogram::pools.size(); (blockIndex * SgAsmDwarfSubprogram::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubprogram) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubprogram) = %" PRIuPTR " SgAsmDwarfSubprogram::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubprogram),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubprogram),SgAsmDwarfSubprogram::pool_size);
        }
#endif

        SgAsmDwarfSubprogram* pointer{static_cast<SgAsmDwarfSubprogram*>(ROSE_MALLOC(SgAsmDwarfSubprogram::pool_size * sizeof(SgAsmDwarfSubprogram)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfSubprogram::pool_size * sizeof(SgAsmDwarfSubprogram));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfSubprogram::pool_size * sizeof(SgAsmDwarfSubprogram));
#endif
        SgAsmDwarfSubprogram::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfSubprogram::pool_size * sizeof(SgAsmDwarfSubprogram), V_SgAsmDwarfSubprogram));

        if (SgAsmDwarfSubprogram::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfSubprogram* blkptr{reinterpret_cast<SgAsmDwarfSubprogram*>(SgAsmDwarfSubprogram::pools[blockIndex-1])};
                blkptr[SgAsmDwarfSubprogram::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfSubprogram::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfSubprogram::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfSubprogram::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfSubprogram::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfSubprogram::pool_size - 1};
    SgAsmDwarfSubprogram* testPointer{reinterpret_cast<SgAsmDwarfSubprogram*>(SgAsmDwarfSubprogram::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfSubprogram::pool_size * (SgAsmDwarfSubprogram::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfSubprogramStroageClass
 * from its counterpart of SgAsmDwarfSubprogram. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfSubprogram::initializeStorageClassArray(SgAsmDwarfSubprogramStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfSubprogram::pools.begin(); block != SgAsmDwarfSubprogram::pools.end(); ++block) {
        SgAsmDwarfSubprogram* pointer{reinterpret_cast<SgAsmDwarfSubprogram*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubprogram::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 193755 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfSubrangeType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfSubrangeType::SgAsmDwarfSubrangeType(const SgAsmDwarfSubrangeTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 193765 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfSubrangeType*
SgAsmDwarfSubrangeType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfSubrangeType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubrangeType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubrangeType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubrangeType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubrangeType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSubrangeType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSubrangeType::pool_size};
        returnPointer = &(((SgAsmDwarfSubrangeType*)(SgAsmDwarfSubrangeType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfSubrangeType*
SgAsmDwarfSubrangeType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfSubrangeType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubrangeType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubrangeType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubrangeType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfSubrangeType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSubrangeType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSubrangeType::pool_size};
        returnPointer = &(((SgAsmDwarfSubrangeType*)(SgAsmDwarfSubrangeType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfSubrangeType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfSubrangeType::pools.begin(); block != SgAsmDwarfSubrangeType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSubrangeType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubrangeType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfSubrangeType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfSubrangeType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfSubrangeType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfSubrangeType::pools.begin(); block != SgAsmDwarfSubrangeType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSubrangeType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubrangeType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfSubrangeType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfSubrangeType::clearMemoryPool() {
    SgAsmDwarfSubrangeType* tempPointer{nullptr};
    if (SgAsmDwarfSubrangeType::pools.empty() == false) {
        auto block{SgAsmDwarfSubrangeType::pools.begin()};
        SgAsmDwarfSubrangeType::next_node = reinterpret_cast<SgAsmDwarfSubrangeType*>(*block);
        for (; block != SgAsmDwarfSubrangeType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfSubrangeType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfSubrangeType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfSubrangeType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfSubrangeType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfSubrangeType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfSubrangeType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfSubrangeType::next_node = nullptr;
    SgAsmDwarfSubrangeType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfSubrangeType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubrangeType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubrangeType)};
    for (size_t blockIndex = SgAsmDwarfSubrangeType::pools.size(); (blockIndex * SgAsmDwarfSubrangeType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubrangeType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubrangeType) = %" PRIuPTR " SgAsmDwarfSubrangeType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubrangeType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubrangeType),SgAsmDwarfSubrangeType::pool_size);
        }
#endif

        SgAsmDwarfSubrangeType* pointer{static_cast<SgAsmDwarfSubrangeType*>(ROSE_MALLOC(SgAsmDwarfSubrangeType::pool_size * sizeof(SgAsmDwarfSubrangeType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfSubrangeType::pool_size * sizeof(SgAsmDwarfSubrangeType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfSubrangeType::pool_size * sizeof(SgAsmDwarfSubrangeType));
#endif
        SgAsmDwarfSubrangeType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfSubrangeType::pool_size * sizeof(SgAsmDwarfSubrangeType), V_SgAsmDwarfSubrangeType));

        if (SgAsmDwarfSubrangeType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfSubrangeType* blkptr{reinterpret_cast<SgAsmDwarfSubrangeType*>(SgAsmDwarfSubrangeType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfSubrangeType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfSubrangeType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfSubrangeType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfSubrangeType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfSubrangeType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfSubrangeType::pool_size - 1};
    SgAsmDwarfSubrangeType* testPointer{reinterpret_cast<SgAsmDwarfSubrangeType*>(SgAsmDwarfSubrangeType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfSubrangeType::pool_size * (SgAsmDwarfSubrangeType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfSubrangeTypeStroageClass
 * from its counterpart of SgAsmDwarfSubrangeType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfSubrangeType::initializeStorageClassArray(SgAsmDwarfSubrangeTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfSubrangeType::pools.begin(); block != SgAsmDwarfSubrangeType::pools.end(); ++block) {
        SgAsmDwarfSubrangeType* pointer{reinterpret_cast<SgAsmDwarfSubrangeType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubrangeType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 193982 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfSubroutineType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfSubroutineType::SgAsmDwarfSubroutineType(const SgAsmDwarfSubroutineTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 193992 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfSubroutineType*
SgAsmDwarfSubroutineType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfSubroutineType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubroutineType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubroutineType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfSubroutineType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubroutineType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSubroutineType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSubroutineType::pool_size};
        returnPointer = &(((SgAsmDwarfSubroutineType*)(SgAsmDwarfSubroutineType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfSubroutineType*
SgAsmDwarfSubroutineType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfSubroutineType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubroutineType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubroutineType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfSubroutineType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfSubroutineType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfSubroutineType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfSubroutineType::pool_size};
        returnPointer = &(((SgAsmDwarfSubroutineType*)(SgAsmDwarfSubroutineType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfSubroutineType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfSubroutineType::pools.begin(); block != SgAsmDwarfSubroutineType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSubroutineType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubroutineType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfSubroutineType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfSubroutineType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfSubroutineType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfSubroutineType::pools.begin(); block != SgAsmDwarfSubroutineType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfSubroutineType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubroutineType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfSubroutineType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfSubroutineType::clearMemoryPool() {
    SgAsmDwarfSubroutineType* tempPointer{nullptr};
    if (SgAsmDwarfSubroutineType::pools.empty() == false) {
        auto block{SgAsmDwarfSubroutineType::pools.begin()};
        SgAsmDwarfSubroutineType::next_node = reinterpret_cast<SgAsmDwarfSubroutineType*>(*block);
        for (; block != SgAsmDwarfSubroutineType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfSubroutineType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfSubroutineType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfSubroutineType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfSubroutineType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfSubroutineType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfSubroutineType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfSubroutineType::next_node = nullptr;
    SgAsmDwarfSubroutineType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfSubroutineType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubroutineType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubroutineType)};
    for (size_t blockIndex = SgAsmDwarfSubroutineType::pools.size(); (blockIndex * SgAsmDwarfSubroutineType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubroutineType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubroutineType) = %" PRIuPTR " SgAsmDwarfSubroutineType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfSubroutineType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfSubroutineType),SgAsmDwarfSubroutineType::pool_size);
        }
#endif

        SgAsmDwarfSubroutineType* pointer{static_cast<SgAsmDwarfSubroutineType*>(ROSE_MALLOC(SgAsmDwarfSubroutineType::pool_size * sizeof(SgAsmDwarfSubroutineType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfSubroutineType::pool_size * sizeof(SgAsmDwarfSubroutineType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfSubroutineType::pool_size * sizeof(SgAsmDwarfSubroutineType));
#endif
        SgAsmDwarfSubroutineType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfSubroutineType::pool_size * sizeof(SgAsmDwarfSubroutineType), V_SgAsmDwarfSubroutineType));

        if (SgAsmDwarfSubroutineType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfSubroutineType* blkptr{reinterpret_cast<SgAsmDwarfSubroutineType*>(SgAsmDwarfSubroutineType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfSubroutineType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfSubroutineType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfSubroutineType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfSubroutineType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfSubroutineType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfSubroutineType::pool_size - 1};
    SgAsmDwarfSubroutineType* testPointer{reinterpret_cast<SgAsmDwarfSubroutineType*>(SgAsmDwarfSubroutineType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfSubroutineType::pool_size * (SgAsmDwarfSubroutineType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfSubroutineTypeStroageClass
 * from its counterpart of SgAsmDwarfSubroutineType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfSubroutineType::initializeStorageClassArray(SgAsmDwarfSubroutineTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfSubroutineType::pools.begin(); block != SgAsmDwarfSubroutineType::pools.end(); ++block) {
        SgAsmDwarfSubroutineType* pointer{reinterpret_cast<SgAsmDwarfSubroutineType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfSubroutineType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 194210 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfTemplateTypeParameter that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfTemplateTypeParameter::SgAsmDwarfTemplateTypeParameter(const SgAsmDwarfTemplateTypeParameterStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 194220 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfTemplateTypeParameter*
SgAsmDwarfTemplateTypeParameter::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfTemplateTypeParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTemplateTypeParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTemplateTypeParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTemplateTypeParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateTypeParameter)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTemplateTypeParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTemplateTypeParameter::pool_size};
        returnPointer = &(((SgAsmDwarfTemplateTypeParameter*)(SgAsmDwarfTemplateTypeParameter::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfTemplateTypeParameter*
SgAsmDwarfTemplateTypeParameter::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfTemplateTypeParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTemplateTypeParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTemplateTypeParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTemplateTypeParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfTemplateTypeParameter)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTemplateTypeParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTemplateTypeParameter::pool_size};
        returnPointer = &(((SgAsmDwarfTemplateTypeParameter*)(SgAsmDwarfTemplateTypeParameter::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfTemplateTypeParameter::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfTemplateTypeParameter::pools.begin(); block != SgAsmDwarfTemplateTypeParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTemplateTypeParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTemplateTypeParameter::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfTemplateTypeParameter*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfTemplateTypeParameter::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfTemplateTypeParameter* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfTemplateTypeParameter::pools.begin(); block != SgAsmDwarfTemplateTypeParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTemplateTypeParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTemplateTypeParameter::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfTemplateTypeParameter::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfTemplateTypeParameter::clearMemoryPool() {
    SgAsmDwarfTemplateTypeParameter* tempPointer{nullptr};
    if (SgAsmDwarfTemplateTypeParameter::pools.empty() == false) {
        auto block{SgAsmDwarfTemplateTypeParameter::pools.begin()};
        SgAsmDwarfTemplateTypeParameter::next_node = reinterpret_cast<SgAsmDwarfTemplateTypeParameter*>(*block);
        for (; block != SgAsmDwarfTemplateTypeParameter::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfTemplateTypeParameter*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfTemplateTypeParameter::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfTemplateTypeParameter::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfTemplateTypeParameter::pool_size-1]);
        }
    }
}

void
SgAsmDwarfTemplateTypeParameter::deleteMemoryPool() {
    for (auto p: SgAsmDwarfTemplateTypeParameter::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfTemplateTypeParameter::next_node = nullptr;
    SgAsmDwarfTemplateTypeParameter::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfTemplateTypeParameter::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateTypeParameter) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTemplateTypeParameter)};
    for (size_t blockIndex = SgAsmDwarfTemplateTypeParameter::pools.size(); (blockIndex * SgAsmDwarfTemplateTypeParameter::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateTypeParameter) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTemplateTypeParameter) = %" PRIuPTR " SgAsmDwarfTemplateTypeParameter::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateTypeParameter),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTemplateTypeParameter),SgAsmDwarfTemplateTypeParameter::pool_size);
        }
#endif

        SgAsmDwarfTemplateTypeParameter* pointer{static_cast<SgAsmDwarfTemplateTypeParameter*>(ROSE_MALLOC(SgAsmDwarfTemplateTypeParameter::pool_size * sizeof(SgAsmDwarfTemplateTypeParameter)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfTemplateTypeParameter::pool_size * sizeof(SgAsmDwarfTemplateTypeParameter));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfTemplateTypeParameter::pool_size * sizeof(SgAsmDwarfTemplateTypeParameter));
#endif
        SgAsmDwarfTemplateTypeParameter::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfTemplateTypeParameter::pool_size * sizeof(SgAsmDwarfTemplateTypeParameter), V_SgAsmDwarfTemplateTypeParameter));

        if (SgAsmDwarfTemplateTypeParameter::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfTemplateTypeParameter* blkptr{reinterpret_cast<SgAsmDwarfTemplateTypeParameter*>(SgAsmDwarfTemplateTypeParameter::pools[blockIndex-1])};
                blkptr[SgAsmDwarfTemplateTypeParameter::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfTemplateTypeParameter::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfTemplateTypeParameter::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfTemplateTypeParameter::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfTemplateTypeParameter::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfTemplateTypeParameter::pool_size - 1};
    SgAsmDwarfTemplateTypeParameter* testPointer{reinterpret_cast<SgAsmDwarfTemplateTypeParameter*>(SgAsmDwarfTemplateTypeParameter::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfTemplateTypeParameter::pool_size * (SgAsmDwarfTemplateTypeParameter::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfTemplateTypeParameterStroageClass
 * from its counterpart of SgAsmDwarfTemplateTypeParameter. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfTemplateTypeParameter::initializeStorageClassArray(SgAsmDwarfTemplateTypeParameterStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfTemplateTypeParameter::pools.begin(); block != SgAsmDwarfTemplateTypeParameter::pools.end(); ++block) {
        SgAsmDwarfTemplateTypeParameter* pointer{reinterpret_cast<SgAsmDwarfTemplateTypeParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTemplateTypeParameter::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 194437 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfTemplateValueParameter that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfTemplateValueParameter::SgAsmDwarfTemplateValueParameter(const SgAsmDwarfTemplateValueParameterStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 194447 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfTemplateValueParameter*
SgAsmDwarfTemplateValueParameter::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfTemplateValueParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTemplateValueParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTemplateValueParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTemplateValueParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateValueParameter)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTemplateValueParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTemplateValueParameter::pool_size};
        returnPointer = &(((SgAsmDwarfTemplateValueParameter*)(SgAsmDwarfTemplateValueParameter::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfTemplateValueParameter*
SgAsmDwarfTemplateValueParameter::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfTemplateValueParameter* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTemplateValueParameter)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTemplateValueParameter + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTemplateValueParameter)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfTemplateValueParameter)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTemplateValueParameter::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTemplateValueParameter::pool_size};
        returnPointer = &(((SgAsmDwarfTemplateValueParameter*)(SgAsmDwarfTemplateValueParameter::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfTemplateValueParameter::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfTemplateValueParameter::pools.begin(); block != SgAsmDwarfTemplateValueParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTemplateValueParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTemplateValueParameter::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfTemplateValueParameter*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfTemplateValueParameter::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfTemplateValueParameter* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfTemplateValueParameter::pools.begin(); block != SgAsmDwarfTemplateValueParameter::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTemplateValueParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTemplateValueParameter::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfTemplateValueParameter::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfTemplateValueParameter::clearMemoryPool() {
    SgAsmDwarfTemplateValueParameter* tempPointer{nullptr};
    if (SgAsmDwarfTemplateValueParameter::pools.empty() == false) {
        auto block{SgAsmDwarfTemplateValueParameter::pools.begin()};
        SgAsmDwarfTemplateValueParameter::next_node = reinterpret_cast<SgAsmDwarfTemplateValueParameter*>(*block);
        for (; block != SgAsmDwarfTemplateValueParameter::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfTemplateValueParameter*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfTemplateValueParameter::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfTemplateValueParameter::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfTemplateValueParameter::pool_size-1]);
        }
    }
}

void
SgAsmDwarfTemplateValueParameter::deleteMemoryPool() {
    for (auto p: SgAsmDwarfTemplateValueParameter::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfTemplateValueParameter::next_node = nullptr;
    SgAsmDwarfTemplateValueParameter::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfTemplateValueParameter::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateValueParameter) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTemplateValueParameter)};
    for (size_t blockIndex = SgAsmDwarfTemplateValueParameter::pools.size(); (blockIndex * SgAsmDwarfTemplateValueParameter::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateValueParameter) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTemplateValueParameter) = %" PRIuPTR " SgAsmDwarfTemplateValueParameter::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTemplateValueParameter),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTemplateValueParameter),SgAsmDwarfTemplateValueParameter::pool_size);
        }
#endif

        SgAsmDwarfTemplateValueParameter* pointer{static_cast<SgAsmDwarfTemplateValueParameter*>(ROSE_MALLOC(SgAsmDwarfTemplateValueParameter::pool_size * sizeof(SgAsmDwarfTemplateValueParameter)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfTemplateValueParameter::pool_size * sizeof(SgAsmDwarfTemplateValueParameter));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfTemplateValueParameter::pool_size * sizeof(SgAsmDwarfTemplateValueParameter));
#endif
        SgAsmDwarfTemplateValueParameter::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfTemplateValueParameter::pool_size * sizeof(SgAsmDwarfTemplateValueParameter), V_SgAsmDwarfTemplateValueParameter));

        if (SgAsmDwarfTemplateValueParameter::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfTemplateValueParameter* blkptr{reinterpret_cast<SgAsmDwarfTemplateValueParameter*>(SgAsmDwarfTemplateValueParameter::pools[blockIndex-1])};
                blkptr[SgAsmDwarfTemplateValueParameter::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfTemplateValueParameter::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfTemplateValueParameter::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfTemplateValueParameter::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfTemplateValueParameter::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfTemplateValueParameter::pool_size - 1};
    SgAsmDwarfTemplateValueParameter* testPointer{reinterpret_cast<SgAsmDwarfTemplateValueParameter*>(SgAsmDwarfTemplateValueParameter::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfTemplateValueParameter::pool_size * (SgAsmDwarfTemplateValueParameter::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfTemplateValueParameterStroageClass
 * from its counterpart of SgAsmDwarfTemplateValueParameter. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfTemplateValueParameter::initializeStorageClassArray(SgAsmDwarfTemplateValueParameterStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfTemplateValueParameter::pools.begin(); block != SgAsmDwarfTemplateValueParameter::pools.end(); ++block) {
        SgAsmDwarfTemplateValueParameter* pointer{reinterpret_cast<SgAsmDwarfTemplateValueParameter*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTemplateValueParameter::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 194664 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfThrownType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfThrownType::SgAsmDwarfThrownType(const SgAsmDwarfThrownTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 194674 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfThrownType*
SgAsmDwarfThrownType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfThrownType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfThrownType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfThrownType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfThrownType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfThrownType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfThrownType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfThrownType::pool_size};
        returnPointer = &(((SgAsmDwarfThrownType*)(SgAsmDwarfThrownType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfThrownType*
SgAsmDwarfThrownType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfThrownType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfThrownType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfThrownType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfThrownType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfThrownType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfThrownType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfThrownType::pool_size};
        returnPointer = &(((SgAsmDwarfThrownType*)(SgAsmDwarfThrownType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfThrownType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfThrownType::pools.begin(); block != SgAsmDwarfThrownType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfThrownType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfThrownType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfThrownType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfThrownType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfThrownType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfThrownType::pools.begin(); block != SgAsmDwarfThrownType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfThrownType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfThrownType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfThrownType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfThrownType::clearMemoryPool() {
    SgAsmDwarfThrownType* tempPointer{nullptr};
    if (SgAsmDwarfThrownType::pools.empty() == false) {
        auto block{SgAsmDwarfThrownType::pools.begin()};
        SgAsmDwarfThrownType::next_node = reinterpret_cast<SgAsmDwarfThrownType*>(*block);
        for (; block != SgAsmDwarfThrownType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfThrownType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfThrownType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfThrownType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfThrownType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfThrownType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfThrownType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfThrownType::next_node = nullptr;
    SgAsmDwarfThrownType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfThrownType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfThrownType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfThrownType)};
    for (size_t blockIndex = SgAsmDwarfThrownType::pools.size(); (blockIndex * SgAsmDwarfThrownType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfThrownType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfThrownType) = %" PRIuPTR " SgAsmDwarfThrownType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfThrownType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfThrownType),SgAsmDwarfThrownType::pool_size);
        }
#endif

        SgAsmDwarfThrownType* pointer{static_cast<SgAsmDwarfThrownType*>(ROSE_MALLOC(SgAsmDwarfThrownType::pool_size * sizeof(SgAsmDwarfThrownType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfThrownType::pool_size * sizeof(SgAsmDwarfThrownType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfThrownType::pool_size * sizeof(SgAsmDwarfThrownType));
#endif
        SgAsmDwarfThrownType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfThrownType::pool_size * sizeof(SgAsmDwarfThrownType), V_SgAsmDwarfThrownType));

        if (SgAsmDwarfThrownType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfThrownType* blkptr{reinterpret_cast<SgAsmDwarfThrownType*>(SgAsmDwarfThrownType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfThrownType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfThrownType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfThrownType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfThrownType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfThrownType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfThrownType::pool_size - 1};
    SgAsmDwarfThrownType* testPointer{reinterpret_cast<SgAsmDwarfThrownType*>(SgAsmDwarfThrownType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfThrownType::pool_size * (SgAsmDwarfThrownType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfThrownTypeStroageClass
 * from its counterpart of SgAsmDwarfThrownType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfThrownType::initializeStorageClassArray(SgAsmDwarfThrownTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfThrownType::pools.begin(); block != SgAsmDwarfThrownType::pools.end(); ++block) {
        SgAsmDwarfThrownType* pointer{reinterpret_cast<SgAsmDwarfThrownType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfThrownType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 194891 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfTryBlock that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfTryBlock::SgAsmDwarfTryBlock(const SgAsmDwarfTryBlockStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 194901 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfTryBlock*
SgAsmDwarfTryBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfTryBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTryBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTryBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTryBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTryBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTryBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTryBlock::pool_size};
        returnPointer = &(((SgAsmDwarfTryBlock*)(SgAsmDwarfTryBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfTryBlock*
SgAsmDwarfTryBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfTryBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTryBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTryBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTryBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfTryBlock)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTryBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTryBlock::pool_size};
        returnPointer = &(((SgAsmDwarfTryBlock*)(SgAsmDwarfTryBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfTryBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfTryBlock::pools.begin(); block != SgAsmDwarfTryBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTryBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTryBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfTryBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfTryBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfTryBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfTryBlock::pools.begin(); block != SgAsmDwarfTryBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTryBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTryBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfTryBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfTryBlock::clearMemoryPool() {
    SgAsmDwarfTryBlock* tempPointer{nullptr};
    if (SgAsmDwarfTryBlock::pools.empty() == false) {
        auto block{SgAsmDwarfTryBlock::pools.begin()};
        SgAsmDwarfTryBlock::next_node = reinterpret_cast<SgAsmDwarfTryBlock*>(*block);
        for (; block != SgAsmDwarfTryBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfTryBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfTryBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfTryBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfTryBlock::pool_size-1]);
        }
    }
}

void
SgAsmDwarfTryBlock::deleteMemoryPool() {
    for (auto p: SgAsmDwarfTryBlock::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfTryBlock::next_node = nullptr;
    SgAsmDwarfTryBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfTryBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTryBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTryBlock)};
    for (size_t blockIndex = SgAsmDwarfTryBlock::pools.size(); (blockIndex * SgAsmDwarfTryBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTryBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTryBlock) = %" PRIuPTR " SgAsmDwarfTryBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTryBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTryBlock),SgAsmDwarfTryBlock::pool_size);
        }
#endif

        SgAsmDwarfTryBlock* pointer{static_cast<SgAsmDwarfTryBlock*>(ROSE_MALLOC(SgAsmDwarfTryBlock::pool_size * sizeof(SgAsmDwarfTryBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfTryBlock::pool_size * sizeof(SgAsmDwarfTryBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfTryBlock::pool_size * sizeof(SgAsmDwarfTryBlock));
#endif
        SgAsmDwarfTryBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfTryBlock::pool_size * sizeof(SgAsmDwarfTryBlock), V_SgAsmDwarfTryBlock));

        if (SgAsmDwarfTryBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfTryBlock* blkptr{reinterpret_cast<SgAsmDwarfTryBlock*>(SgAsmDwarfTryBlock::pools[blockIndex-1])};
                blkptr[SgAsmDwarfTryBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfTryBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfTryBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfTryBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfTryBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfTryBlock::pool_size - 1};
    SgAsmDwarfTryBlock* testPointer{reinterpret_cast<SgAsmDwarfTryBlock*>(SgAsmDwarfTryBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfTryBlock::pool_size * (SgAsmDwarfTryBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfTryBlockStroageClass
 * from its counterpart of SgAsmDwarfTryBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfTryBlock::initializeStorageClassArray(SgAsmDwarfTryBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfTryBlock::pools.begin(); block != SgAsmDwarfTryBlock::pools.end(); ++block) {
        SgAsmDwarfTryBlock* pointer{reinterpret_cast<SgAsmDwarfTryBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTryBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 195118 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfTypedef that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfTypedef::SgAsmDwarfTypedef(const SgAsmDwarfTypedefStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 195128 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfTypedef*
SgAsmDwarfTypedef::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfTypedef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTypedef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTypedef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfTypedef)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTypedef)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTypedef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTypedef::pool_size};
        returnPointer = &(((SgAsmDwarfTypedef*)(SgAsmDwarfTypedef::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfTypedef*
SgAsmDwarfTypedef::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfTypedef* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTypedef)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTypedef + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfTypedef)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfTypedef)};
        unsigned long positionInPool{localIndex % SgAsmDwarfTypedef::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfTypedef::pool_size};
        returnPointer = &(((SgAsmDwarfTypedef*)(SgAsmDwarfTypedef::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfTypedef::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfTypedef::pools.begin(); block != SgAsmDwarfTypedef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTypedef*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTypedef::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfTypedef*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfTypedef::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfTypedef* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfTypedef::pools.begin(); block != SgAsmDwarfTypedef::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfTypedef*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTypedef::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfTypedef::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfTypedef::clearMemoryPool() {
    SgAsmDwarfTypedef* tempPointer{nullptr};
    if (SgAsmDwarfTypedef::pools.empty() == false) {
        auto block{SgAsmDwarfTypedef::pools.begin()};
        SgAsmDwarfTypedef::next_node = reinterpret_cast<SgAsmDwarfTypedef*>(*block);
        for (; block != SgAsmDwarfTypedef::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfTypedef*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfTypedef::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfTypedef::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfTypedef::pool_size-1]);
        }
    }
}

void
SgAsmDwarfTypedef::deleteMemoryPool() {
    for (auto p: SgAsmDwarfTypedef::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfTypedef::next_node = nullptr;
    SgAsmDwarfTypedef::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfTypedef::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTypedef) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTypedef)};
    for (size_t blockIndex = SgAsmDwarfTypedef::pools.size(); (blockIndex * SgAsmDwarfTypedef::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTypedef) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTypedef) = %" PRIuPTR " SgAsmDwarfTypedef::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfTypedef),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfTypedef),SgAsmDwarfTypedef::pool_size);
        }
#endif

        SgAsmDwarfTypedef* pointer{static_cast<SgAsmDwarfTypedef*>(ROSE_MALLOC(SgAsmDwarfTypedef::pool_size * sizeof(SgAsmDwarfTypedef)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfTypedef::pool_size * sizeof(SgAsmDwarfTypedef));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfTypedef::pool_size * sizeof(SgAsmDwarfTypedef));
#endif
        SgAsmDwarfTypedef::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfTypedef::pool_size * sizeof(SgAsmDwarfTypedef), V_SgAsmDwarfTypedef));

        if (SgAsmDwarfTypedef::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfTypedef* blkptr{reinterpret_cast<SgAsmDwarfTypedef*>(SgAsmDwarfTypedef::pools[blockIndex-1])};
                blkptr[SgAsmDwarfTypedef::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfTypedef::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfTypedef::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfTypedef::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfTypedef::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfTypedef::pool_size - 1};
    SgAsmDwarfTypedef* testPointer{reinterpret_cast<SgAsmDwarfTypedef*>(SgAsmDwarfTypedef::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfTypedef::pool_size * (SgAsmDwarfTypedef::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfTypedefStroageClass
 * from its counterpart of SgAsmDwarfTypedef. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfTypedef::initializeStorageClassArray(SgAsmDwarfTypedefStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfTypedef::pools.begin(); block != SgAsmDwarfTypedef::pools.end(); ++block) {
        SgAsmDwarfTypedef* pointer{reinterpret_cast<SgAsmDwarfTypedef*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfTypedef::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 195345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfUnionType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfUnionType::SgAsmDwarfUnionType(const SgAsmDwarfUnionTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 195355 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_body = (SgAsmDwarfConstructList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_body));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfUnionType*
SgAsmDwarfUnionType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfUnionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnionType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnionType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnionType::pool_size};
        returnPointer = &(((SgAsmDwarfUnionType*)(SgAsmDwarfUnionType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfUnionType*
SgAsmDwarfUnionType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfUnionType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnionType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnionType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnionType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfUnionType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnionType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnionType::pool_size};
        returnPointer = &(((SgAsmDwarfUnionType*)(SgAsmDwarfUnionType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfUnionType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfUnionType::pools.begin(); block != SgAsmDwarfUnionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnionType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnionType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfUnionType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfUnionType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfUnionType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfUnionType::pools.begin(); block != SgAsmDwarfUnionType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnionType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnionType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfUnionType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfUnionType::clearMemoryPool() {
    SgAsmDwarfUnionType* tempPointer{nullptr};
    if (SgAsmDwarfUnionType::pools.empty() == false) {
        auto block{SgAsmDwarfUnionType::pools.begin()};
        SgAsmDwarfUnionType::next_node = reinterpret_cast<SgAsmDwarfUnionType*>(*block);
        for (; block != SgAsmDwarfUnionType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfUnionType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfUnionType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfUnionType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfUnionType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfUnionType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfUnionType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfUnionType::next_node = nullptr;
    SgAsmDwarfUnionType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfUnionType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnionType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnionType)};
    for (size_t blockIndex = SgAsmDwarfUnionType::pools.size(); (blockIndex * SgAsmDwarfUnionType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnionType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnionType) = %" PRIuPTR " SgAsmDwarfUnionType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnionType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnionType),SgAsmDwarfUnionType::pool_size);
        }
#endif

        SgAsmDwarfUnionType* pointer{static_cast<SgAsmDwarfUnionType*>(ROSE_MALLOC(SgAsmDwarfUnionType::pool_size * sizeof(SgAsmDwarfUnionType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfUnionType::pool_size * sizeof(SgAsmDwarfUnionType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfUnionType::pool_size * sizeof(SgAsmDwarfUnionType));
#endif
        SgAsmDwarfUnionType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfUnionType::pool_size * sizeof(SgAsmDwarfUnionType), V_SgAsmDwarfUnionType));

        if (SgAsmDwarfUnionType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfUnionType* blkptr{reinterpret_cast<SgAsmDwarfUnionType*>(SgAsmDwarfUnionType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfUnionType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfUnionType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfUnionType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfUnionType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfUnionType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfUnionType::pool_size - 1};
    SgAsmDwarfUnionType* testPointer{reinterpret_cast<SgAsmDwarfUnionType*>(SgAsmDwarfUnionType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfUnionType::pool_size * (SgAsmDwarfUnionType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfUnionTypeStroageClass
 * from its counterpart of SgAsmDwarfUnionType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfUnionType::initializeStorageClassArray(SgAsmDwarfUnionTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfUnionType::pools.begin(); block != SgAsmDwarfUnionType::pools.end(); ++block) {
        SgAsmDwarfUnionType* pointer{reinterpret_cast<SgAsmDwarfUnionType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnionType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 195573 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfUnknownConstruct that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfUnknownConstruct::SgAsmDwarfUnknownConstruct(const SgAsmDwarfUnknownConstructStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 195583 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfUnknownConstruct*
SgAsmDwarfUnknownConstruct::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfUnknownConstruct* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnknownConstruct)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnknownConstruct + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnknownConstruct)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnknownConstruct)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnknownConstruct::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnknownConstruct::pool_size};
        returnPointer = &(((SgAsmDwarfUnknownConstruct*)(SgAsmDwarfUnknownConstruct::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfUnknownConstruct*
SgAsmDwarfUnknownConstruct::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfUnknownConstruct* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnknownConstruct)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnknownConstruct + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnknownConstruct)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfUnknownConstruct)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnknownConstruct::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnknownConstruct::pool_size};
        returnPointer = &(((SgAsmDwarfUnknownConstruct*)(SgAsmDwarfUnknownConstruct::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfUnknownConstruct::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfUnknownConstruct::pools.begin(); block != SgAsmDwarfUnknownConstruct::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnknownConstruct*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnknownConstruct::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfUnknownConstruct*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfUnknownConstruct::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfUnknownConstruct* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfUnknownConstruct::pools.begin(); block != SgAsmDwarfUnknownConstruct::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnknownConstruct*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnknownConstruct::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfUnknownConstruct::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfUnknownConstruct::clearMemoryPool() {
    SgAsmDwarfUnknownConstruct* tempPointer{nullptr};
    if (SgAsmDwarfUnknownConstruct::pools.empty() == false) {
        auto block{SgAsmDwarfUnknownConstruct::pools.begin()};
        SgAsmDwarfUnknownConstruct::next_node = reinterpret_cast<SgAsmDwarfUnknownConstruct*>(*block);
        for (; block != SgAsmDwarfUnknownConstruct::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfUnknownConstruct*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfUnknownConstruct::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfUnknownConstruct::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfUnknownConstruct::pool_size-1]);
        }
    }
}

void
SgAsmDwarfUnknownConstruct::deleteMemoryPool() {
    for (auto p: SgAsmDwarfUnknownConstruct::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfUnknownConstruct::next_node = nullptr;
    SgAsmDwarfUnknownConstruct::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfUnknownConstruct::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnknownConstruct) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnknownConstruct)};
    for (size_t blockIndex = SgAsmDwarfUnknownConstruct::pools.size(); (blockIndex * SgAsmDwarfUnknownConstruct::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnknownConstruct) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnknownConstruct) = %" PRIuPTR " SgAsmDwarfUnknownConstruct::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnknownConstruct),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnknownConstruct),SgAsmDwarfUnknownConstruct::pool_size);
        }
#endif

        SgAsmDwarfUnknownConstruct* pointer{static_cast<SgAsmDwarfUnknownConstruct*>(ROSE_MALLOC(SgAsmDwarfUnknownConstruct::pool_size * sizeof(SgAsmDwarfUnknownConstruct)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfUnknownConstruct::pool_size * sizeof(SgAsmDwarfUnknownConstruct));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfUnknownConstruct::pool_size * sizeof(SgAsmDwarfUnknownConstruct));
#endif
        SgAsmDwarfUnknownConstruct::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfUnknownConstruct::pool_size * sizeof(SgAsmDwarfUnknownConstruct), V_SgAsmDwarfUnknownConstruct));

        if (SgAsmDwarfUnknownConstruct::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfUnknownConstruct* blkptr{reinterpret_cast<SgAsmDwarfUnknownConstruct*>(SgAsmDwarfUnknownConstruct::pools[blockIndex-1])};
                blkptr[SgAsmDwarfUnknownConstruct::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfUnknownConstruct::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfUnknownConstruct::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfUnknownConstruct::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfUnknownConstruct::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfUnknownConstruct::pool_size - 1};
    SgAsmDwarfUnknownConstruct* testPointer{reinterpret_cast<SgAsmDwarfUnknownConstruct*>(SgAsmDwarfUnknownConstruct::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfUnknownConstruct::pool_size * (SgAsmDwarfUnknownConstruct::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfUnknownConstructStroageClass
 * from its counterpart of SgAsmDwarfUnknownConstruct. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfUnknownConstruct::initializeStorageClassArray(SgAsmDwarfUnknownConstructStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfUnknownConstruct::pools.begin(); block != SgAsmDwarfUnknownConstruct::pools.end(); ++block) {
        SgAsmDwarfUnknownConstruct* pointer{reinterpret_cast<SgAsmDwarfUnknownConstruct*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnknownConstruct::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 195800 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfUnspecifiedParameters that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfUnspecifiedParameters::SgAsmDwarfUnspecifiedParameters(const SgAsmDwarfUnspecifiedParametersStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 195810 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfUnspecifiedParameters*
SgAsmDwarfUnspecifiedParameters::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfUnspecifiedParameters* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedParameters)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedParameters + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedParameters)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedParameters)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnspecifiedParameters::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnspecifiedParameters::pool_size};
        returnPointer = &(((SgAsmDwarfUnspecifiedParameters*)(SgAsmDwarfUnspecifiedParameters::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfUnspecifiedParameters*
SgAsmDwarfUnspecifiedParameters::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfUnspecifiedParameters* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnspecifiedParameters)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnspecifiedParameters + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnspecifiedParameters)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfUnspecifiedParameters)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnspecifiedParameters::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnspecifiedParameters::pool_size};
        returnPointer = &(((SgAsmDwarfUnspecifiedParameters*)(SgAsmDwarfUnspecifiedParameters::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfUnspecifiedParameters::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfUnspecifiedParameters::pools.begin(); block != SgAsmDwarfUnspecifiedParameters::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedParameters*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedParameters::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfUnspecifiedParameters*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfUnspecifiedParameters::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfUnspecifiedParameters* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfUnspecifiedParameters::pools.begin(); block != SgAsmDwarfUnspecifiedParameters::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedParameters*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedParameters::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfUnspecifiedParameters::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfUnspecifiedParameters::clearMemoryPool() {
    SgAsmDwarfUnspecifiedParameters* tempPointer{nullptr};
    if (SgAsmDwarfUnspecifiedParameters::pools.empty() == false) {
        auto block{SgAsmDwarfUnspecifiedParameters::pools.begin()};
        SgAsmDwarfUnspecifiedParameters::next_node = reinterpret_cast<SgAsmDwarfUnspecifiedParameters*>(*block);
        for (; block != SgAsmDwarfUnspecifiedParameters::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedParameters*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfUnspecifiedParameters::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfUnspecifiedParameters::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfUnspecifiedParameters::pool_size-1]);
        }
    }
}

void
SgAsmDwarfUnspecifiedParameters::deleteMemoryPool() {
    for (auto p: SgAsmDwarfUnspecifiedParameters::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfUnspecifiedParameters::next_node = nullptr;
    SgAsmDwarfUnspecifiedParameters::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfUnspecifiedParameters::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedParameters) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedParameters)};
    for (size_t blockIndex = SgAsmDwarfUnspecifiedParameters::pools.size(); (blockIndex * SgAsmDwarfUnspecifiedParameters::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedParameters) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedParameters) = %" PRIuPTR " SgAsmDwarfUnspecifiedParameters::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedParameters),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedParameters),SgAsmDwarfUnspecifiedParameters::pool_size);
        }
#endif

        SgAsmDwarfUnspecifiedParameters* pointer{static_cast<SgAsmDwarfUnspecifiedParameters*>(ROSE_MALLOC(SgAsmDwarfUnspecifiedParameters::pool_size * sizeof(SgAsmDwarfUnspecifiedParameters)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfUnspecifiedParameters::pool_size * sizeof(SgAsmDwarfUnspecifiedParameters));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfUnspecifiedParameters::pool_size * sizeof(SgAsmDwarfUnspecifiedParameters));
#endif
        SgAsmDwarfUnspecifiedParameters::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfUnspecifiedParameters::pool_size * sizeof(SgAsmDwarfUnspecifiedParameters), V_SgAsmDwarfUnspecifiedParameters));

        if (SgAsmDwarfUnspecifiedParameters::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfUnspecifiedParameters* blkptr{reinterpret_cast<SgAsmDwarfUnspecifiedParameters*>(SgAsmDwarfUnspecifiedParameters::pools[blockIndex-1])};
                blkptr[SgAsmDwarfUnspecifiedParameters::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfUnspecifiedParameters::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedParameters::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfUnspecifiedParameters::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfUnspecifiedParameters::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfUnspecifiedParameters::pool_size - 1};
    SgAsmDwarfUnspecifiedParameters* testPointer{reinterpret_cast<SgAsmDwarfUnspecifiedParameters*>(SgAsmDwarfUnspecifiedParameters::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfUnspecifiedParameters::pool_size * (SgAsmDwarfUnspecifiedParameters::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfUnspecifiedParametersStroageClass
 * from its counterpart of SgAsmDwarfUnspecifiedParameters. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfUnspecifiedParameters::initializeStorageClassArray(SgAsmDwarfUnspecifiedParametersStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfUnspecifiedParameters::pools.begin(); block != SgAsmDwarfUnspecifiedParameters::pools.end(); ++block) {
        SgAsmDwarfUnspecifiedParameters* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedParameters*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedParameters::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 196027 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfUnspecifiedType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfUnspecifiedType::SgAsmDwarfUnspecifiedType(const SgAsmDwarfUnspecifiedTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 196037 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfUnspecifiedType*
SgAsmDwarfUnspecifiedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfUnspecifiedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnspecifiedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnspecifiedType::pool_size};
        returnPointer = &(((SgAsmDwarfUnspecifiedType*)(SgAsmDwarfUnspecifiedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfUnspecifiedType*
SgAsmDwarfUnspecifiedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfUnspecifiedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnspecifiedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnspecifiedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUnspecifiedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfUnspecifiedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUnspecifiedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUnspecifiedType::pool_size};
        returnPointer = &(((SgAsmDwarfUnspecifiedType*)(SgAsmDwarfUnspecifiedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfUnspecifiedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfUnspecifiedType::pools.begin(); block != SgAsmDwarfUnspecifiedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfUnspecifiedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfUnspecifiedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfUnspecifiedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfUnspecifiedType::pools.begin(); block != SgAsmDwarfUnspecifiedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfUnspecifiedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfUnspecifiedType::clearMemoryPool() {
    SgAsmDwarfUnspecifiedType* tempPointer{nullptr};
    if (SgAsmDwarfUnspecifiedType::pools.empty() == false) {
        auto block{SgAsmDwarfUnspecifiedType::pools.begin()};
        SgAsmDwarfUnspecifiedType::next_node = reinterpret_cast<SgAsmDwarfUnspecifiedType*>(*block);
        for (; block != SgAsmDwarfUnspecifiedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfUnspecifiedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfUnspecifiedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfUnspecifiedType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfUnspecifiedType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfUnspecifiedType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfUnspecifiedType::next_node = nullptr;
    SgAsmDwarfUnspecifiedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfUnspecifiedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedType)};
    for (size_t blockIndex = SgAsmDwarfUnspecifiedType::pools.size(); (blockIndex * SgAsmDwarfUnspecifiedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedType) = %" PRIuPTR " SgAsmDwarfUnspecifiedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUnspecifiedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUnspecifiedType),SgAsmDwarfUnspecifiedType::pool_size);
        }
#endif

        SgAsmDwarfUnspecifiedType* pointer{static_cast<SgAsmDwarfUnspecifiedType*>(ROSE_MALLOC(SgAsmDwarfUnspecifiedType::pool_size * sizeof(SgAsmDwarfUnspecifiedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfUnspecifiedType::pool_size * sizeof(SgAsmDwarfUnspecifiedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfUnspecifiedType::pool_size * sizeof(SgAsmDwarfUnspecifiedType));
#endif
        SgAsmDwarfUnspecifiedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfUnspecifiedType::pool_size * sizeof(SgAsmDwarfUnspecifiedType), V_SgAsmDwarfUnspecifiedType));

        if (SgAsmDwarfUnspecifiedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfUnspecifiedType* blkptr{reinterpret_cast<SgAsmDwarfUnspecifiedType*>(SgAsmDwarfUnspecifiedType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfUnspecifiedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfUnspecifiedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfUnspecifiedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfUnspecifiedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfUnspecifiedType::pool_size - 1};
    SgAsmDwarfUnspecifiedType* testPointer{reinterpret_cast<SgAsmDwarfUnspecifiedType*>(SgAsmDwarfUnspecifiedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfUnspecifiedType::pool_size * (SgAsmDwarfUnspecifiedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfUnspecifiedTypeStroageClass
 * from its counterpart of SgAsmDwarfUnspecifiedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfUnspecifiedType::initializeStorageClassArray(SgAsmDwarfUnspecifiedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfUnspecifiedType::pools.begin(); block != SgAsmDwarfUnspecifiedType::pools.end(); ++block) {
        SgAsmDwarfUnspecifiedType* pointer{reinterpret_cast<SgAsmDwarfUnspecifiedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUnspecifiedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 196254 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfUpcRelaxedType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfUpcRelaxedType::SgAsmDwarfUpcRelaxedType(const SgAsmDwarfUpcRelaxedTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 196264 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfUpcRelaxedType*
SgAsmDwarfUpcRelaxedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfUpcRelaxedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcRelaxedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcRelaxedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcRelaxedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcRelaxedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUpcRelaxedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUpcRelaxedType::pool_size};
        returnPointer = &(((SgAsmDwarfUpcRelaxedType*)(SgAsmDwarfUpcRelaxedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfUpcRelaxedType*
SgAsmDwarfUpcRelaxedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfUpcRelaxedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcRelaxedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcRelaxedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcRelaxedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfUpcRelaxedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUpcRelaxedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUpcRelaxedType::pool_size};
        returnPointer = &(((SgAsmDwarfUpcRelaxedType*)(SgAsmDwarfUpcRelaxedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfUpcRelaxedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfUpcRelaxedType::pools.begin(); block != SgAsmDwarfUpcRelaxedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUpcRelaxedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcRelaxedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfUpcRelaxedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfUpcRelaxedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfUpcRelaxedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfUpcRelaxedType::pools.begin(); block != SgAsmDwarfUpcRelaxedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUpcRelaxedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcRelaxedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfUpcRelaxedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfUpcRelaxedType::clearMemoryPool() {
    SgAsmDwarfUpcRelaxedType* tempPointer{nullptr};
    if (SgAsmDwarfUpcRelaxedType::pools.empty() == false) {
        auto block{SgAsmDwarfUpcRelaxedType::pools.begin()};
        SgAsmDwarfUpcRelaxedType::next_node = reinterpret_cast<SgAsmDwarfUpcRelaxedType*>(*block);
        for (; block != SgAsmDwarfUpcRelaxedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfUpcRelaxedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfUpcRelaxedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfUpcRelaxedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfUpcRelaxedType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfUpcRelaxedType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfUpcRelaxedType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfUpcRelaxedType::next_node = nullptr;
    SgAsmDwarfUpcRelaxedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfUpcRelaxedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcRelaxedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcRelaxedType)};
    for (size_t blockIndex = SgAsmDwarfUpcRelaxedType::pools.size(); (blockIndex * SgAsmDwarfUpcRelaxedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcRelaxedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcRelaxedType) = %" PRIuPTR " SgAsmDwarfUpcRelaxedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcRelaxedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcRelaxedType),SgAsmDwarfUpcRelaxedType::pool_size);
        }
#endif

        SgAsmDwarfUpcRelaxedType* pointer{static_cast<SgAsmDwarfUpcRelaxedType*>(ROSE_MALLOC(SgAsmDwarfUpcRelaxedType::pool_size * sizeof(SgAsmDwarfUpcRelaxedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfUpcRelaxedType::pool_size * sizeof(SgAsmDwarfUpcRelaxedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfUpcRelaxedType::pool_size * sizeof(SgAsmDwarfUpcRelaxedType));
#endif
        SgAsmDwarfUpcRelaxedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfUpcRelaxedType::pool_size * sizeof(SgAsmDwarfUpcRelaxedType), V_SgAsmDwarfUpcRelaxedType));

        if (SgAsmDwarfUpcRelaxedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfUpcRelaxedType* blkptr{reinterpret_cast<SgAsmDwarfUpcRelaxedType*>(SgAsmDwarfUpcRelaxedType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfUpcRelaxedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfUpcRelaxedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfUpcRelaxedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfUpcRelaxedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfUpcRelaxedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfUpcRelaxedType::pool_size - 1};
    SgAsmDwarfUpcRelaxedType* testPointer{reinterpret_cast<SgAsmDwarfUpcRelaxedType*>(SgAsmDwarfUpcRelaxedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfUpcRelaxedType::pool_size * (SgAsmDwarfUpcRelaxedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfUpcRelaxedTypeStroageClass
 * from its counterpart of SgAsmDwarfUpcRelaxedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfUpcRelaxedType::initializeStorageClassArray(SgAsmDwarfUpcRelaxedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfUpcRelaxedType::pools.begin(); block != SgAsmDwarfUpcRelaxedType::pools.end(); ++block) {
        SgAsmDwarfUpcRelaxedType* pointer{reinterpret_cast<SgAsmDwarfUpcRelaxedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcRelaxedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 196481 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfUpcSharedType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfUpcSharedType::SgAsmDwarfUpcSharedType(const SgAsmDwarfUpcSharedTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 196491 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfUpcSharedType*
SgAsmDwarfUpcSharedType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfUpcSharedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcSharedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcSharedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcSharedType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcSharedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUpcSharedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUpcSharedType::pool_size};
        returnPointer = &(((SgAsmDwarfUpcSharedType*)(SgAsmDwarfUpcSharedType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfUpcSharedType*
SgAsmDwarfUpcSharedType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfUpcSharedType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcSharedType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcSharedType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcSharedType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfUpcSharedType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUpcSharedType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUpcSharedType::pool_size};
        returnPointer = &(((SgAsmDwarfUpcSharedType*)(SgAsmDwarfUpcSharedType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfUpcSharedType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfUpcSharedType::pools.begin(); block != SgAsmDwarfUpcSharedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUpcSharedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcSharedType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfUpcSharedType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfUpcSharedType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfUpcSharedType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfUpcSharedType::pools.begin(); block != SgAsmDwarfUpcSharedType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUpcSharedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcSharedType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfUpcSharedType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfUpcSharedType::clearMemoryPool() {
    SgAsmDwarfUpcSharedType* tempPointer{nullptr};
    if (SgAsmDwarfUpcSharedType::pools.empty() == false) {
        auto block{SgAsmDwarfUpcSharedType::pools.begin()};
        SgAsmDwarfUpcSharedType::next_node = reinterpret_cast<SgAsmDwarfUpcSharedType*>(*block);
        for (; block != SgAsmDwarfUpcSharedType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfUpcSharedType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfUpcSharedType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfUpcSharedType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfUpcSharedType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfUpcSharedType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfUpcSharedType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfUpcSharedType::next_node = nullptr;
    SgAsmDwarfUpcSharedType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfUpcSharedType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcSharedType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcSharedType)};
    for (size_t blockIndex = SgAsmDwarfUpcSharedType::pools.size(); (blockIndex * SgAsmDwarfUpcSharedType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcSharedType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcSharedType) = %" PRIuPTR " SgAsmDwarfUpcSharedType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcSharedType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcSharedType),SgAsmDwarfUpcSharedType::pool_size);
        }
#endif

        SgAsmDwarfUpcSharedType* pointer{static_cast<SgAsmDwarfUpcSharedType*>(ROSE_MALLOC(SgAsmDwarfUpcSharedType::pool_size * sizeof(SgAsmDwarfUpcSharedType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfUpcSharedType::pool_size * sizeof(SgAsmDwarfUpcSharedType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfUpcSharedType::pool_size * sizeof(SgAsmDwarfUpcSharedType));
#endif
        SgAsmDwarfUpcSharedType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfUpcSharedType::pool_size * sizeof(SgAsmDwarfUpcSharedType), V_SgAsmDwarfUpcSharedType));

        if (SgAsmDwarfUpcSharedType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfUpcSharedType* blkptr{reinterpret_cast<SgAsmDwarfUpcSharedType*>(SgAsmDwarfUpcSharedType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfUpcSharedType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfUpcSharedType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfUpcSharedType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfUpcSharedType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfUpcSharedType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfUpcSharedType::pool_size - 1};
    SgAsmDwarfUpcSharedType* testPointer{reinterpret_cast<SgAsmDwarfUpcSharedType*>(SgAsmDwarfUpcSharedType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfUpcSharedType::pool_size * (SgAsmDwarfUpcSharedType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfUpcSharedTypeStroageClass
 * from its counterpart of SgAsmDwarfUpcSharedType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfUpcSharedType::initializeStorageClassArray(SgAsmDwarfUpcSharedTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfUpcSharedType::pools.begin(); block != SgAsmDwarfUpcSharedType::pools.end(); ++block) {
        SgAsmDwarfUpcSharedType* pointer{reinterpret_cast<SgAsmDwarfUpcSharedType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcSharedType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 196708 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfUpcStrictType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfUpcStrictType::SgAsmDwarfUpcStrictType(const SgAsmDwarfUpcStrictTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 196718 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfUpcStrictType*
SgAsmDwarfUpcStrictType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfUpcStrictType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcStrictType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcStrictType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfUpcStrictType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcStrictType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUpcStrictType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUpcStrictType::pool_size};
        returnPointer = &(((SgAsmDwarfUpcStrictType*)(SgAsmDwarfUpcStrictType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfUpcStrictType*
SgAsmDwarfUpcStrictType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfUpcStrictType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcStrictType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcStrictType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfUpcStrictType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfUpcStrictType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfUpcStrictType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfUpcStrictType::pool_size};
        returnPointer = &(((SgAsmDwarfUpcStrictType*)(SgAsmDwarfUpcStrictType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfUpcStrictType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfUpcStrictType::pools.begin(); block != SgAsmDwarfUpcStrictType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUpcStrictType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcStrictType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfUpcStrictType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfUpcStrictType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfUpcStrictType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfUpcStrictType::pools.begin(); block != SgAsmDwarfUpcStrictType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfUpcStrictType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcStrictType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfUpcStrictType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfUpcStrictType::clearMemoryPool() {
    SgAsmDwarfUpcStrictType* tempPointer{nullptr};
    if (SgAsmDwarfUpcStrictType::pools.empty() == false) {
        auto block{SgAsmDwarfUpcStrictType::pools.begin()};
        SgAsmDwarfUpcStrictType::next_node = reinterpret_cast<SgAsmDwarfUpcStrictType*>(*block);
        for (; block != SgAsmDwarfUpcStrictType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfUpcStrictType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfUpcStrictType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfUpcStrictType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfUpcStrictType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfUpcStrictType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfUpcStrictType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfUpcStrictType::next_node = nullptr;
    SgAsmDwarfUpcStrictType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfUpcStrictType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcStrictType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcStrictType)};
    for (size_t blockIndex = SgAsmDwarfUpcStrictType::pools.size(); (blockIndex * SgAsmDwarfUpcStrictType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcStrictType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcStrictType) = %" PRIuPTR " SgAsmDwarfUpcStrictType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfUpcStrictType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfUpcStrictType),SgAsmDwarfUpcStrictType::pool_size);
        }
#endif

        SgAsmDwarfUpcStrictType* pointer{static_cast<SgAsmDwarfUpcStrictType*>(ROSE_MALLOC(SgAsmDwarfUpcStrictType::pool_size * sizeof(SgAsmDwarfUpcStrictType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfUpcStrictType::pool_size * sizeof(SgAsmDwarfUpcStrictType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfUpcStrictType::pool_size * sizeof(SgAsmDwarfUpcStrictType));
#endif
        SgAsmDwarfUpcStrictType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfUpcStrictType::pool_size * sizeof(SgAsmDwarfUpcStrictType), V_SgAsmDwarfUpcStrictType));

        if (SgAsmDwarfUpcStrictType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfUpcStrictType* blkptr{reinterpret_cast<SgAsmDwarfUpcStrictType*>(SgAsmDwarfUpcStrictType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfUpcStrictType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfUpcStrictType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfUpcStrictType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfUpcStrictType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfUpcStrictType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfUpcStrictType::pool_size - 1};
    SgAsmDwarfUpcStrictType* testPointer{reinterpret_cast<SgAsmDwarfUpcStrictType*>(SgAsmDwarfUpcStrictType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfUpcStrictType::pool_size * (SgAsmDwarfUpcStrictType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfUpcStrictTypeStroageClass
 * from its counterpart of SgAsmDwarfUpcStrictType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfUpcStrictType::initializeStorageClassArray(SgAsmDwarfUpcStrictTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfUpcStrictType::pools.begin(); block != SgAsmDwarfUpcStrictType::pools.end(); ++block) {
        SgAsmDwarfUpcStrictType* pointer{reinterpret_cast<SgAsmDwarfUpcStrictType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfUpcStrictType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 196935 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfVariable that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfVariable::SgAsmDwarfVariable(const SgAsmDwarfVariableStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 196945 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_linkage_name = storageSource.storageOf_linkage_name.rebuildDataStoredInEasyStorageClass();
    p_is_const_expr = storageSource.storageOf_is_const_expr;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfVariable*
SgAsmDwarfVariable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfVariable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariable)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVariable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVariable::pool_size};
        returnPointer = &(((SgAsmDwarfVariable*)(SgAsmDwarfVariable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfVariable*
SgAsmDwarfVariable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfVariable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfVariable)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVariable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVariable::pool_size};
        returnPointer = &(((SgAsmDwarfVariable*)(SgAsmDwarfVariable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfVariable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfVariable::pools.begin(); block != SgAsmDwarfVariable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVariable*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfVariable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfVariable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfVariable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfVariable::pools.begin(); block != SgAsmDwarfVariable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVariable*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfVariable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfVariable::clearMemoryPool() {
    SgAsmDwarfVariable* tempPointer{nullptr};
    if (SgAsmDwarfVariable::pools.empty() == false) {
        auto block{SgAsmDwarfVariable::pools.begin()};
        SgAsmDwarfVariable::next_node = reinterpret_cast<SgAsmDwarfVariable*>(*block);
        for (; block != SgAsmDwarfVariable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfVariable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfVariable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfVariable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfVariable::pool_size-1]);
        }
    }
}

void
SgAsmDwarfVariable::deleteMemoryPool() {
    for (auto p: SgAsmDwarfVariable::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfVariable::next_node = nullptr;
    SgAsmDwarfVariable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfVariable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariable)};
    for (size_t blockIndex = SgAsmDwarfVariable::pools.size(); (blockIndex * SgAsmDwarfVariable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariable) = %" PRIuPTR " SgAsmDwarfVariable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariable),SgAsmDwarfVariable::pool_size);
        }
#endif

        SgAsmDwarfVariable* pointer{static_cast<SgAsmDwarfVariable*>(ROSE_MALLOC(SgAsmDwarfVariable::pool_size * sizeof(SgAsmDwarfVariable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfVariable::pool_size * sizeof(SgAsmDwarfVariable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfVariable::pool_size * sizeof(SgAsmDwarfVariable));
#endif
        SgAsmDwarfVariable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfVariable::pool_size * sizeof(SgAsmDwarfVariable), V_SgAsmDwarfVariable));

        if (SgAsmDwarfVariable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfVariable* blkptr{reinterpret_cast<SgAsmDwarfVariable*>(SgAsmDwarfVariable::pools[blockIndex-1])};
                blkptr[SgAsmDwarfVariable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfVariable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfVariable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfVariable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfVariable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfVariable::pool_size - 1};
    SgAsmDwarfVariable* testPointer{reinterpret_cast<SgAsmDwarfVariable*>(SgAsmDwarfVariable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfVariable::pool_size * (SgAsmDwarfVariable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfVariableStroageClass
 * from its counterpart of SgAsmDwarfVariable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfVariable::initializeStorageClassArray(SgAsmDwarfVariableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfVariable::pools.begin(); block != SgAsmDwarfVariable::pools.end(); ++block) {
        SgAsmDwarfVariable* pointer{reinterpret_cast<SgAsmDwarfVariable*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 197164 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfVariant that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfVariant::SgAsmDwarfVariant(const SgAsmDwarfVariantStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 197174 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfVariant*
SgAsmDwarfVariant::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfVariant* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariant)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariant + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariant)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariant)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVariant::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVariant::pool_size};
        returnPointer = &(((SgAsmDwarfVariant*)(SgAsmDwarfVariant::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfVariant*
SgAsmDwarfVariant::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfVariant* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariant)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariant + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariant)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfVariant)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVariant::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVariant::pool_size};
        returnPointer = &(((SgAsmDwarfVariant*)(SgAsmDwarfVariant::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfVariant::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfVariant::pools.begin(); block != SgAsmDwarfVariant::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVariant*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariant::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfVariant*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfVariant::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfVariant* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfVariant::pools.begin(); block != SgAsmDwarfVariant::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVariant*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariant::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfVariant::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfVariant::clearMemoryPool() {
    SgAsmDwarfVariant* tempPointer{nullptr};
    if (SgAsmDwarfVariant::pools.empty() == false) {
        auto block{SgAsmDwarfVariant::pools.begin()};
        SgAsmDwarfVariant::next_node = reinterpret_cast<SgAsmDwarfVariant*>(*block);
        for (; block != SgAsmDwarfVariant::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfVariant*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfVariant::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfVariant::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfVariant::pool_size-1]);
        }
    }
}

void
SgAsmDwarfVariant::deleteMemoryPool() {
    for (auto p: SgAsmDwarfVariant::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfVariant::next_node = nullptr;
    SgAsmDwarfVariant::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfVariant::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariant) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariant)};
    for (size_t blockIndex = SgAsmDwarfVariant::pools.size(); (blockIndex * SgAsmDwarfVariant::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariant) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariant) = %" PRIuPTR " SgAsmDwarfVariant::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariant),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariant),SgAsmDwarfVariant::pool_size);
        }
#endif

        SgAsmDwarfVariant* pointer{static_cast<SgAsmDwarfVariant*>(ROSE_MALLOC(SgAsmDwarfVariant::pool_size * sizeof(SgAsmDwarfVariant)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfVariant::pool_size * sizeof(SgAsmDwarfVariant));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfVariant::pool_size * sizeof(SgAsmDwarfVariant));
#endif
        SgAsmDwarfVariant::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfVariant::pool_size * sizeof(SgAsmDwarfVariant), V_SgAsmDwarfVariant));

        if (SgAsmDwarfVariant::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfVariant* blkptr{reinterpret_cast<SgAsmDwarfVariant*>(SgAsmDwarfVariant::pools[blockIndex-1])};
                blkptr[SgAsmDwarfVariant::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfVariant::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfVariant::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfVariant::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfVariant::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfVariant::pool_size - 1};
    SgAsmDwarfVariant* testPointer{reinterpret_cast<SgAsmDwarfVariant*>(SgAsmDwarfVariant::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfVariant::pool_size * (SgAsmDwarfVariant::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfVariantStroageClass
 * from its counterpart of SgAsmDwarfVariant. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfVariant::initializeStorageClassArray(SgAsmDwarfVariantStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfVariant::pools.begin(); block != SgAsmDwarfVariant::pools.end(); ++block) {
        SgAsmDwarfVariant* pointer{reinterpret_cast<SgAsmDwarfVariant*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariant::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 197391 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfVariantPart that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfVariantPart::SgAsmDwarfVariantPart(const SgAsmDwarfVariantPartStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 197401 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfVariantPart*
SgAsmDwarfVariantPart::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfVariantPart* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariantPart)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariantPart + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVariantPart)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariantPart)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVariantPart::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVariantPart::pool_size};
        returnPointer = &(((SgAsmDwarfVariantPart*)(SgAsmDwarfVariantPart::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfVariantPart*
SgAsmDwarfVariantPart::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfVariantPart* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariantPart)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariantPart + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVariantPart)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfVariantPart)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVariantPart::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVariantPart::pool_size};
        returnPointer = &(((SgAsmDwarfVariantPart*)(SgAsmDwarfVariantPart::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfVariantPart::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfVariantPart::pools.begin(); block != SgAsmDwarfVariantPart::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVariantPart*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariantPart::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfVariantPart*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfVariantPart::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfVariantPart* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfVariantPart::pools.begin(); block != SgAsmDwarfVariantPart::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVariantPart*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariantPart::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfVariantPart::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfVariantPart::clearMemoryPool() {
    SgAsmDwarfVariantPart* tempPointer{nullptr};
    if (SgAsmDwarfVariantPart::pools.empty() == false) {
        auto block{SgAsmDwarfVariantPart::pools.begin()};
        SgAsmDwarfVariantPart::next_node = reinterpret_cast<SgAsmDwarfVariantPart*>(*block);
        for (; block != SgAsmDwarfVariantPart::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfVariantPart*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfVariantPart::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfVariantPart::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfVariantPart::pool_size-1]);
        }
    }
}

void
SgAsmDwarfVariantPart::deleteMemoryPool() {
    for (auto p: SgAsmDwarfVariantPart::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfVariantPart::next_node = nullptr;
    SgAsmDwarfVariantPart::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfVariantPart::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariantPart) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariantPart)};
    for (size_t blockIndex = SgAsmDwarfVariantPart::pools.size(); (blockIndex * SgAsmDwarfVariantPart::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariantPart) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariantPart) = %" PRIuPTR " SgAsmDwarfVariantPart::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVariantPart),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVariantPart),SgAsmDwarfVariantPart::pool_size);
        }
#endif

        SgAsmDwarfVariantPart* pointer{static_cast<SgAsmDwarfVariantPart*>(ROSE_MALLOC(SgAsmDwarfVariantPart::pool_size * sizeof(SgAsmDwarfVariantPart)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfVariantPart::pool_size * sizeof(SgAsmDwarfVariantPart));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfVariantPart::pool_size * sizeof(SgAsmDwarfVariantPart));
#endif
        SgAsmDwarfVariantPart::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfVariantPart::pool_size * sizeof(SgAsmDwarfVariantPart), V_SgAsmDwarfVariantPart));

        if (SgAsmDwarfVariantPart::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfVariantPart* blkptr{reinterpret_cast<SgAsmDwarfVariantPart*>(SgAsmDwarfVariantPart::pools[blockIndex-1])};
                blkptr[SgAsmDwarfVariantPart::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfVariantPart::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfVariantPart::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfVariantPart::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfVariantPart::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfVariantPart::pool_size - 1};
    SgAsmDwarfVariantPart* testPointer{reinterpret_cast<SgAsmDwarfVariantPart*>(SgAsmDwarfVariantPart::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfVariantPart::pool_size * (SgAsmDwarfVariantPart::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfVariantPartStroageClass
 * from its counterpart of SgAsmDwarfVariantPart. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfVariantPart::initializeStorageClassArray(SgAsmDwarfVariantPartStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfVariantPart::pools.begin(); block != SgAsmDwarfVariantPart::pools.end(); ++block) {
        SgAsmDwarfVariantPart* pointer{reinterpret_cast<SgAsmDwarfVariantPart*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVariantPart::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 197618 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfVolatileType that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfVolatileType::SgAsmDwarfVolatileType(const SgAsmDwarfVolatileTypeStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 197628 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfVolatileType*
SgAsmDwarfVolatileType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfVolatileType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVolatileType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVolatileType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfVolatileType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVolatileType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVolatileType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVolatileType::pool_size};
        returnPointer = &(((SgAsmDwarfVolatileType*)(SgAsmDwarfVolatileType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfVolatileType*
SgAsmDwarfVolatileType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfVolatileType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVolatileType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVolatileType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfVolatileType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfVolatileType)};
        unsigned long positionInPool{localIndex % SgAsmDwarfVolatileType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfVolatileType::pool_size};
        returnPointer = &(((SgAsmDwarfVolatileType*)(SgAsmDwarfVolatileType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfVolatileType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfVolatileType::pools.begin(); block != SgAsmDwarfVolatileType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVolatileType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVolatileType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfVolatileType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfVolatileType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfVolatileType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfVolatileType::pools.begin(); block != SgAsmDwarfVolatileType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfVolatileType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVolatileType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfVolatileType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfVolatileType::clearMemoryPool() {
    SgAsmDwarfVolatileType* tempPointer{nullptr};
    if (SgAsmDwarfVolatileType::pools.empty() == false) {
        auto block{SgAsmDwarfVolatileType::pools.begin()};
        SgAsmDwarfVolatileType::next_node = reinterpret_cast<SgAsmDwarfVolatileType*>(*block);
        for (; block != SgAsmDwarfVolatileType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfVolatileType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfVolatileType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfVolatileType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfVolatileType::pool_size-1]);
        }
    }
}

void
SgAsmDwarfVolatileType::deleteMemoryPool() {
    for (auto p: SgAsmDwarfVolatileType::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfVolatileType::next_node = nullptr;
    SgAsmDwarfVolatileType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfVolatileType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVolatileType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVolatileType)};
    for (size_t blockIndex = SgAsmDwarfVolatileType::pools.size(); (blockIndex * SgAsmDwarfVolatileType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVolatileType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVolatileType) = %" PRIuPTR " SgAsmDwarfVolatileType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfVolatileType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfVolatileType),SgAsmDwarfVolatileType::pool_size);
        }
#endif

        SgAsmDwarfVolatileType* pointer{static_cast<SgAsmDwarfVolatileType*>(ROSE_MALLOC(SgAsmDwarfVolatileType::pool_size * sizeof(SgAsmDwarfVolatileType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfVolatileType::pool_size * sizeof(SgAsmDwarfVolatileType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfVolatileType::pool_size * sizeof(SgAsmDwarfVolatileType));
#endif
        SgAsmDwarfVolatileType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfVolatileType::pool_size * sizeof(SgAsmDwarfVolatileType), V_SgAsmDwarfVolatileType));

        if (SgAsmDwarfVolatileType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfVolatileType* blkptr{reinterpret_cast<SgAsmDwarfVolatileType*>(SgAsmDwarfVolatileType::pools[blockIndex-1])};
                blkptr[SgAsmDwarfVolatileType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfVolatileType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfVolatileType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfVolatileType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfVolatileType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfVolatileType::pool_size - 1};
    SgAsmDwarfVolatileType* testPointer{reinterpret_cast<SgAsmDwarfVolatileType*>(SgAsmDwarfVolatileType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfVolatileType::pool_size * (SgAsmDwarfVolatileType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfVolatileTypeStroageClass
 * from its counterpart of SgAsmDwarfVolatileType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfVolatileType::initializeStorageClassArray(SgAsmDwarfVolatileTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfVolatileType::pools.begin(); block != SgAsmDwarfVolatileType::pools.end(); ++block) {
        SgAsmDwarfVolatileType* pointer{reinterpret_cast<SgAsmDwarfVolatileType*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfVolatileType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 197845 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfWithStmt that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfWithStmt::SgAsmDwarfWithStmt(const SgAsmDwarfWithStmtStorageClass& storageSource)  : SgAsmDwarfConstruct (storageSource) {

/* #line 197855 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfWithStmt*
SgAsmDwarfWithStmt::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfWithStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfWithStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfWithStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfWithStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfWithStmt)};
        unsigned long positionInPool{localIndex % SgAsmDwarfWithStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfWithStmt::pool_size};
        returnPointer = &(((SgAsmDwarfWithStmt*)(SgAsmDwarfWithStmt::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfWithStmt*
SgAsmDwarfWithStmt::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfWithStmt* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfWithStmt)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfWithStmt + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfWithStmt)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfWithStmt)};
        unsigned long positionInPool{localIndex % SgAsmDwarfWithStmt::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfWithStmt::pool_size};
        returnPointer = &(((SgAsmDwarfWithStmt*)(SgAsmDwarfWithStmt::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfWithStmt::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfWithStmt::pools.begin(); block != SgAsmDwarfWithStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfWithStmt*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfWithStmt::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfWithStmt*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfWithStmt::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfWithStmt* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfWithStmt::pools.begin(); block != SgAsmDwarfWithStmt::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfWithStmt*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfWithStmt::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfWithStmt::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfWithStmt::clearMemoryPool() {
    SgAsmDwarfWithStmt* tempPointer{nullptr};
    if (SgAsmDwarfWithStmt::pools.empty() == false) {
        auto block{SgAsmDwarfWithStmt::pools.begin()};
        SgAsmDwarfWithStmt::next_node = reinterpret_cast<SgAsmDwarfWithStmt*>(*block);
        for (; block != SgAsmDwarfWithStmt::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfWithStmt*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfWithStmt::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfWithStmt::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfWithStmt::pool_size-1]);
        }
    }
}

void
SgAsmDwarfWithStmt::deleteMemoryPool() {
    for (auto p: SgAsmDwarfWithStmt::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfWithStmt::next_node = nullptr;
    SgAsmDwarfWithStmt::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfWithStmt::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfWithStmt) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfWithStmt)};
    for (size_t blockIndex = SgAsmDwarfWithStmt::pools.size(); (blockIndex * SgAsmDwarfWithStmt::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfWithStmt) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfWithStmt) = %" PRIuPTR " SgAsmDwarfWithStmt::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfWithStmt),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfWithStmt),SgAsmDwarfWithStmt::pool_size);
        }
#endif

        SgAsmDwarfWithStmt* pointer{static_cast<SgAsmDwarfWithStmt*>(ROSE_MALLOC(SgAsmDwarfWithStmt::pool_size * sizeof(SgAsmDwarfWithStmt)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfWithStmt::pool_size * sizeof(SgAsmDwarfWithStmt));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfWithStmt::pool_size * sizeof(SgAsmDwarfWithStmt));
#endif
        SgAsmDwarfWithStmt::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfWithStmt::pool_size * sizeof(SgAsmDwarfWithStmt), V_SgAsmDwarfWithStmt));

        if (SgAsmDwarfWithStmt::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfWithStmt* blkptr{reinterpret_cast<SgAsmDwarfWithStmt*>(SgAsmDwarfWithStmt::pools[blockIndex-1])};
                blkptr[SgAsmDwarfWithStmt::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfWithStmt::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfWithStmt::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfWithStmt::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfWithStmt::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfWithStmt::pool_size - 1};
    SgAsmDwarfWithStmt* testPointer{reinterpret_cast<SgAsmDwarfWithStmt*>(SgAsmDwarfWithStmt::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfWithStmt::pool_size * (SgAsmDwarfWithStmt::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfWithStmtStroageClass
 * from its counterpart of SgAsmDwarfWithStmt. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfWithStmt::initializeStorageClassArray(SgAsmDwarfWithStmtStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfWithStmt::pools.begin(); block != SgAsmDwarfWithStmt::pools.end(); ++block) {
        SgAsmDwarfWithStmt* pointer{reinterpret_cast<SgAsmDwarfWithStmt*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfWithStmt::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 198072 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfConstructList that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfConstructList::SgAsmDwarfConstructList(const SgAsmDwarfConstructListStorageClass& storageSource)  : SgAsmDwarfInformation (storageSource) {

/* #line 198082 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_list = storageSource.storageOf_list.rebuildDataStoredInEasyStorageClass();
    SgAsmDwarfConstructPtrList::iterator i_list = p_list.begin();
    for ( ; i_list != p_list.end(); ++i_list) {
        (*i_list) = (SgAsmDwarfConstructPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfConstructList*
SgAsmDwarfConstructList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfConstructList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstructList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstructList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfConstructList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstructList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstructList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstructList::pool_size};
        returnPointer = &(((SgAsmDwarfConstructList*)(SgAsmDwarfConstructList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfConstructList*
SgAsmDwarfConstructList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfConstructList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstructList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstructList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfConstructList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfConstructList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfConstructList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfConstructList::pool_size};
        returnPointer = &(((SgAsmDwarfConstructList*)(SgAsmDwarfConstructList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfConstructList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfConstructList::pools.begin(); block != SgAsmDwarfConstructList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstructList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstructList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfConstructList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfConstructList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfConstructList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfConstructList::pools.begin(); block != SgAsmDwarfConstructList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfConstructList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstructList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfConstructList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfConstructList::clearMemoryPool() {
    SgAsmDwarfConstructList* tempPointer{nullptr};
    if (SgAsmDwarfConstructList::pools.empty() == false) {
        auto block{SgAsmDwarfConstructList::pools.begin()};
        SgAsmDwarfConstructList::next_node = reinterpret_cast<SgAsmDwarfConstructList*>(*block);
        for (; block != SgAsmDwarfConstructList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfConstructList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfConstructList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfConstructList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfConstructList::pool_size-1]);
        }
    }
}

void
SgAsmDwarfConstructList::deleteMemoryPool() {
    for (auto p: SgAsmDwarfConstructList::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfConstructList::next_node = nullptr;
    SgAsmDwarfConstructList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfConstructList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstructList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstructList)};
    for (size_t blockIndex = SgAsmDwarfConstructList::pools.size(); (blockIndex * SgAsmDwarfConstructList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstructList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstructList) = %" PRIuPTR " SgAsmDwarfConstructList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfConstructList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfConstructList),SgAsmDwarfConstructList::pool_size);
        }
#endif

        SgAsmDwarfConstructList* pointer{static_cast<SgAsmDwarfConstructList*>(ROSE_MALLOC(SgAsmDwarfConstructList::pool_size * sizeof(SgAsmDwarfConstructList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfConstructList::pool_size * sizeof(SgAsmDwarfConstructList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfConstructList::pool_size * sizeof(SgAsmDwarfConstructList));
#endif
        SgAsmDwarfConstructList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfConstructList::pool_size * sizeof(SgAsmDwarfConstructList), V_SgAsmDwarfConstructList));

        if (SgAsmDwarfConstructList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfConstructList* blkptr{reinterpret_cast<SgAsmDwarfConstructList*>(SgAsmDwarfConstructList::pools[blockIndex-1])};
                blkptr[SgAsmDwarfConstructList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfConstructList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfConstructList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfConstructList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfConstructList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfConstructList::pool_size - 1};
    SgAsmDwarfConstructList* testPointer{reinterpret_cast<SgAsmDwarfConstructList*>(SgAsmDwarfConstructList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfConstructList::pool_size * (SgAsmDwarfConstructList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfConstructListStroageClass
 * from its counterpart of SgAsmDwarfConstructList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfConstructList::initializeStorageClassArray(SgAsmDwarfConstructListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfConstructList::pools.begin(); block != SgAsmDwarfConstructList::pools.end(); ++block) {
        SgAsmDwarfConstructList* pointer{reinterpret_cast<SgAsmDwarfConstructList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfConstructList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 198304 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfLine that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfLine::SgAsmDwarfLine(const SgAsmDwarfLineStorageClass& storageSource)  : SgAsmDwarfInformation (storageSource) {

/* #line 198314 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_address = storageSource.storageOf_address;
    p_file_id = storageSource.storageOf_file_id;
    p_line = storageSource.storageOf_line;
    p_column = storageSource.storageOf_column;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfLine*
SgAsmDwarfLine::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfLine* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLine)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLine + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLine)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLine)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLine::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLine::pool_size};
        returnPointer = &(((SgAsmDwarfLine*)(SgAsmDwarfLine::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfLine*
SgAsmDwarfLine::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfLine* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLine)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLine + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLine)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfLine)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLine::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLine::pool_size};
        returnPointer = &(((SgAsmDwarfLine*)(SgAsmDwarfLine::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfLine::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfLine::pools.begin(); block != SgAsmDwarfLine::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLine*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLine::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfLine*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfLine::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfLine* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfLine::pools.begin(); block != SgAsmDwarfLine::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLine*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLine::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfLine::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfLine::clearMemoryPool() {
    SgAsmDwarfLine* tempPointer{nullptr};
    if (SgAsmDwarfLine::pools.empty() == false) {
        auto block{SgAsmDwarfLine::pools.begin()};
        SgAsmDwarfLine::next_node = reinterpret_cast<SgAsmDwarfLine*>(*block);
        for (; block != SgAsmDwarfLine::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfLine*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfLine::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfLine::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfLine::pool_size-1]);
        }
    }
}

void
SgAsmDwarfLine::deleteMemoryPool() {
    for (auto p: SgAsmDwarfLine::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfLine::next_node = nullptr;
    SgAsmDwarfLine::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfLine::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLine) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLine)};
    for (size_t blockIndex = SgAsmDwarfLine::pools.size(); (blockIndex * SgAsmDwarfLine::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLine) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLine) = %" PRIuPTR " SgAsmDwarfLine::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLine),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLine),SgAsmDwarfLine::pool_size);
        }
#endif

        SgAsmDwarfLine* pointer{static_cast<SgAsmDwarfLine*>(ROSE_MALLOC(SgAsmDwarfLine::pool_size * sizeof(SgAsmDwarfLine)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfLine::pool_size * sizeof(SgAsmDwarfLine));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfLine::pool_size * sizeof(SgAsmDwarfLine));
#endif
        SgAsmDwarfLine::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfLine::pool_size * sizeof(SgAsmDwarfLine), V_SgAsmDwarfLine));

        if (SgAsmDwarfLine::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfLine* blkptr{reinterpret_cast<SgAsmDwarfLine*>(SgAsmDwarfLine::pools[blockIndex-1])};
                blkptr[SgAsmDwarfLine::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfLine::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfLine::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfLine::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfLine::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfLine::pool_size - 1};
    SgAsmDwarfLine* testPointer{reinterpret_cast<SgAsmDwarfLine*>(SgAsmDwarfLine::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfLine::pool_size * (SgAsmDwarfLine::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfLineStroageClass
 * from its counterpart of SgAsmDwarfLine. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfLine::initializeStorageClassArray(SgAsmDwarfLineStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfLine::pools.begin(); block != SgAsmDwarfLine::pools.end(); ++block) {
        SgAsmDwarfLine* pointer{reinterpret_cast<SgAsmDwarfLine*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLine::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 198535 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfLineList that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfLineList::SgAsmDwarfLineList(const SgAsmDwarfLineListStorageClass& storageSource)  : SgAsmDwarfInformation (storageSource) {

/* #line 198545 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_line_list = storageSource.storageOf_line_list.rebuildDataStoredInEasyStorageClass();
    SgAsmDwarfLinePtrList::iterator i_line_list = p_line_list.begin();
    for ( ; i_line_list != p_line_list.end(); ++i_line_list) {
        (*i_line_list) = (SgAsmDwarfLinePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_line_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfLineList*
SgAsmDwarfLineList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfLineList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLineList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLineList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfLineList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLineList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLineList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLineList::pool_size};
        returnPointer = &(((SgAsmDwarfLineList*)(SgAsmDwarfLineList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfLineList*
SgAsmDwarfLineList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfLineList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLineList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLineList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfLineList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfLineList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfLineList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfLineList::pool_size};
        returnPointer = &(((SgAsmDwarfLineList*)(SgAsmDwarfLineList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfLineList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfLineList::pools.begin(); block != SgAsmDwarfLineList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLineList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLineList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfLineList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfLineList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfLineList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfLineList::pools.begin(); block != SgAsmDwarfLineList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfLineList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLineList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfLineList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfLineList::clearMemoryPool() {
    SgAsmDwarfLineList* tempPointer{nullptr};
    if (SgAsmDwarfLineList::pools.empty() == false) {
        auto block{SgAsmDwarfLineList::pools.begin()};
        SgAsmDwarfLineList::next_node = reinterpret_cast<SgAsmDwarfLineList*>(*block);
        for (; block != SgAsmDwarfLineList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfLineList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfLineList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfLineList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfLineList::pool_size-1]);
        }
    }
}

void
SgAsmDwarfLineList::deleteMemoryPool() {
    for (auto p: SgAsmDwarfLineList::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfLineList::next_node = nullptr;
    SgAsmDwarfLineList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfLineList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLineList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLineList)};
    for (size_t blockIndex = SgAsmDwarfLineList::pools.size(); (blockIndex * SgAsmDwarfLineList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLineList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLineList) = %" PRIuPTR " SgAsmDwarfLineList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfLineList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfLineList),SgAsmDwarfLineList::pool_size);
        }
#endif

        SgAsmDwarfLineList* pointer{static_cast<SgAsmDwarfLineList*>(ROSE_MALLOC(SgAsmDwarfLineList::pool_size * sizeof(SgAsmDwarfLineList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfLineList::pool_size * sizeof(SgAsmDwarfLineList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfLineList::pool_size * sizeof(SgAsmDwarfLineList));
#endif
        SgAsmDwarfLineList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfLineList::pool_size * sizeof(SgAsmDwarfLineList), V_SgAsmDwarfLineList));

        if (SgAsmDwarfLineList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfLineList* blkptr{reinterpret_cast<SgAsmDwarfLineList*>(SgAsmDwarfLineList::pools[blockIndex-1])};
                blkptr[SgAsmDwarfLineList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfLineList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfLineList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfLineList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfLineList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfLineList::pool_size - 1};
    SgAsmDwarfLineList* testPointer{reinterpret_cast<SgAsmDwarfLineList*>(SgAsmDwarfLineList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfLineList::pool_size * (SgAsmDwarfLineList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfLineListStroageClass
 * from its counterpart of SgAsmDwarfLineList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfLineList::initializeStorageClassArray(SgAsmDwarfLineListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfLineList::pools.begin(); block != SgAsmDwarfLineList::pools.end(); ++block) {
        SgAsmDwarfLineList* pointer{reinterpret_cast<SgAsmDwarfLineList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfLineList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 198767 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfMacro that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfMacro::SgAsmDwarfMacro(const SgAsmDwarfMacroStorageClass& storageSource)  : SgAsmDwarfInformation (storageSource) {

/* #line 198777 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_macro_string = storageSource.storageOf_macro_string.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfMacro*
SgAsmDwarfMacro::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfMacro* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMacro)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMacro + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMacro)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacro)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMacro::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMacro::pool_size};
        returnPointer = &(((SgAsmDwarfMacro*)(SgAsmDwarfMacro::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfMacro*
SgAsmDwarfMacro::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfMacro* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMacro)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMacro + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMacro)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfMacro)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMacro::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMacro::pool_size};
        returnPointer = &(((SgAsmDwarfMacro*)(SgAsmDwarfMacro::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfMacro::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfMacro::pools.begin(); block != SgAsmDwarfMacro::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMacro*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMacro::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfMacro*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfMacro::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfMacro* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfMacro::pools.begin(); block != SgAsmDwarfMacro::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMacro*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMacro::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfMacro::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfMacro::clearMemoryPool() {
    SgAsmDwarfMacro* tempPointer{nullptr};
    if (SgAsmDwarfMacro::pools.empty() == false) {
        auto block{SgAsmDwarfMacro::pools.begin()};
        SgAsmDwarfMacro::next_node = reinterpret_cast<SgAsmDwarfMacro*>(*block);
        for (; block != SgAsmDwarfMacro::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfMacro*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfMacro::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfMacro::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfMacro::pool_size-1]);
        }
    }
}

void
SgAsmDwarfMacro::deleteMemoryPool() {
    for (auto p: SgAsmDwarfMacro::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfMacro::next_node = nullptr;
    SgAsmDwarfMacro::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfMacro::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacro) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMacro)};
    for (size_t blockIndex = SgAsmDwarfMacro::pools.size(); (blockIndex * SgAsmDwarfMacro::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacro) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMacro) = %" PRIuPTR " SgAsmDwarfMacro::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacro),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMacro),SgAsmDwarfMacro::pool_size);
        }
#endif

        SgAsmDwarfMacro* pointer{static_cast<SgAsmDwarfMacro*>(ROSE_MALLOC(SgAsmDwarfMacro::pool_size * sizeof(SgAsmDwarfMacro)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfMacro::pool_size * sizeof(SgAsmDwarfMacro));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfMacro::pool_size * sizeof(SgAsmDwarfMacro));
#endif
        SgAsmDwarfMacro::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfMacro::pool_size * sizeof(SgAsmDwarfMacro), V_SgAsmDwarfMacro));

        if (SgAsmDwarfMacro::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfMacro* blkptr{reinterpret_cast<SgAsmDwarfMacro*>(SgAsmDwarfMacro::pools[blockIndex-1])};
                blkptr[SgAsmDwarfMacro::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfMacro::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfMacro::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfMacro::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfMacro::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfMacro::pool_size - 1};
    SgAsmDwarfMacro* testPointer{reinterpret_cast<SgAsmDwarfMacro*>(SgAsmDwarfMacro::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfMacro::pool_size * (SgAsmDwarfMacro::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfMacroStroageClass
 * from its counterpart of SgAsmDwarfMacro. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfMacro::initializeStorageClassArray(SgAsmDwarfMacroStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfMacro::pools.begin(); block != SgAsmDwarfMacro::pools.end(); ++block) {
        SgAsmDwarfMacro* pointer{reinterpret_cast<SgAsmDwarfMacro*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMacro::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 198995 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDwarfMacroList that takes its corresponding StorageClass as parameter
 */
SgAsmDwarfMacroList::SgAsmDwarfMacroList(const SgAsmDwarfMacroListStorageClass& storageSource)  : SgAsmDwarfInformation (storageSource) {

/* #line 199005 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_macro_list = storageSource.storageOf_macro_list.rebuildDataStoredInEasyStorageClass();
    SgAsmDwarfMacroPtrList::iterator i_macro_list = p_macro_list.begin();
    for ( ; i_macro_list != p_macro_list.end(); ++i_macro_list) {
        (*i_macro_list) = (SgAsmDwarfMacroPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_macro_list)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDwarfMacroList*
SgAsmDwarfMacroList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDwarfMacroList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMacroList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMacroList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDwarfMacroList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacroList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMacroList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMacroList::pool_size};
        returnPointer = &(((SgAsmDwarfMacroList*)(SgAsmDwarfMacroList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDwarfMacroList*
SgAsmDwarfMacroList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDwarfMacroList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMacroList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMacroList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDwarfMacroList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDwarfMacroList)};
        unsigned long positionInPool{localIndex % SgAsmDwarfMacroList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDwarfMacroList::pool_size};
        returnPointer = &(((SgAsmDwarfMacroList*)(SgAsmDwarfMacroList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDwarfMacroList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDwarfMacroList::pools.begin(); block != SgAsmDwarfMacroList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMacroList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMacroList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDwarfMacroList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDwarfMacroList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDwarfMacroList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDwarfMacroList::pools.begin(); block != SgAsmDwarfMacroList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDwarfMacroList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMacroList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDwarfMacroList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDwarfMacroList::clearMemoryPool() {
    SgAsmDwarfMacroList* tempPointer{nullptr};
    if (SgAsmDwarfMacroList::pools.empty() == false) {
        auto block{SgAsmDwarfMacroList::pools.begin()};
        SgAsmDwarfMacroList::next_node = reinterpret_cast<SgAsmDwarfMacroList*>(*block);
        for (; block != SgAsmDwarfMacroList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDwarfMacroList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDwarfMacroList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDwarfMacroList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDwarfMacroList::pool_size-1]);
        }
    }
}

void
SgAsmDwarfMacroList::deleteMemoryPool() {
    for (auto p: SgAsmDwarfMacroList::pools) {
        ROSE_FREE(p);
    }
    SgAsmDwarfMacroList::next_node = nullptr;
    SgAsmDwarfMacroList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDwarfMacroList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacroList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMacroList)};
    for (size_t blockIndex = SgAsmDwarfMacroList::pools.size(); (blockIndex * SgAsmDwarfMacroList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacroList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMacroList) = %" PRIuPTR " SgAsmDwarfMacroList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDwarfMacroList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDwarfMacroList),SgAsmDwarfMacroList::pool_size);
        }
#endif

        SgAsmDwarfMacroList* pointer{static_cast<SgAsmDwarfMacroList*>(ROSE_MALLOC(SgAsmDwarfMacroList::pool_size * sizeof(SgAsmDwarfMacroList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDwarfMacroList::pool_size * sizeof(SgAsmDwarfMacroList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDwarfMacroList::pool_size * sizeof(SgAsmDwarfMacroList));
#endif
        SgAsmDwarfMacroList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDwarfMacroList::pool_size * sizeof(SgAsmDwarfMacroList), V_SgAsmDwarfMacroList));

        if (SgAsmDwarfMacroList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDwarfMacroList* blkptr{reinterpret_cast<SgAsmDwarfMacroList*>(SgAsmDwarfMacroList::pools[blockIndex-1])};
                blkptr[SgAsmDwarfMacroList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDwarfMacroList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDwarfMacroList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDwarfMacroList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDwarfMacroList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDwarfMacroList::pool_size - 1};
    SgAsmDwarfMacroList* testPointer{reinterpret_cast<SgAsmDwarfMacroList*>(SgAsmDwarfMacroList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDwarfMacroList::pool_size * (SgAsmDwarfMacroList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDwarfMacroListStroageClass
 * from its counterpart of SgAsmDwarfMacroList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDwarfMacroList::initializeStorageClassArray(SgAsmDwarfMacroListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDwarfMacroList::pools.begin(); block != SgAsmDwarfMacroList::pools.end(); ++block) {
        SgAsmDwarfMacroList* pointer{reinterpret_cast<SgAsmDwarfMacroList*>(*block)};
        for (unsigned i = 0; i < SgAsmDwarfMacroList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 199227 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfDynamicEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfDynamicEntry::SgAsmElfDynamicEntry(const SgAsmElfDynamicEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 199237 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_d_tag = storageSource.storageOf_d_tag;
    p_d_val = storageSource.storageOf_d_val;
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));
    p_extra = storageSource.storageOf_extra.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfDynamicEntry*
SgAsmElfDynamicEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfDynamicEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfDynamicEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfDynamicEntry::pool_size};
        returnPointer = &(((SgAsmElfDynamicEntry*)(SgAsmElfDynamicEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfDynamicEntry*
SgAsmElfDynamicEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfDynamicEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfDynamicEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfDynamicEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfDynamicEntry::pool_size};
        returnPointer = &(((SgAsmElfDynamicEntry*)(SgAsmElfDynamicEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfDynamicEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfDynamicEntry::pools.begin(); block != SgAsmElfDynamicEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfDynamicEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfDynamicEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfDynamicEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfDynamicEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfDynamicEntry::pools.begin(); block != SgAsmElfDynamicEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfDynamicEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfDynamicEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfDynamicEntry::clearMemoryPool() {
    SgAsmElfDynamicEntry* tempPointer{nullptr};
    if (SgAsmElfDynamicEntry::pools.empty() == false) {
        auto block{SgAsmElfDynamicEntry::pools.begin()};
        SgAsmElfDynamicEntry::next_node = reinterpret_cast<SgAsmElfDynamicEntry*>(*block);
        for (; block != SgAsmElfDynamicEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfDynamicEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfDynamicEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfDynamicEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfDynamicEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfDynamicEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfDynamicEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfDynamicEntry::next_node = nullptr;
    SgAsmElfDynamicEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfDynamicEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicEntry)};
    for (size_t blockIndex = SgAsmElfDynamicEntry::pools.size(); (blockIndex * SgAsmElfDynamicEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicEntry) = %" PRIuPTR " SgAsmElfDynamicEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicEntry),SgAsmElfDynamicEntry::pool_size);
        }
#endif

        SgAsmElfDynamicEntry* pointer{static_cast<SgAsmElfDynamicEntry*>(ROSE_MALLOC(SgAsmElfDynamicEntry::pool_size * sizeof(SgAsmElfDynamicEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfDynamicEntry::pool_size * sizeof(SgAsmElfDynamicEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfDynamicEntry::pool_size * sizeof(SgAsmElfDynamicEntry));
#endif
        SgAsmElfDynamicEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfDynamicEntry::pool_size * sizeof(SgAsmElfDynamicEntry), V_SgAsmElfDynamicEntry));

        if (SgAsmElfDynamicEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfDynamicEntry* blkptr{reinterpret_cast<SgAsmElfDynamicEntry*>(SgAsmElfDynamicEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfDynamicEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfDynamicEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfDynamicEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfDynamicEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfDynamicEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfDynamicEntry::pool_size - 1};
    SgAsmElfDynamicEntry* testPointer{reinterpret_cast<SgAsmElfDynamicEntry*>(SgAsmElfDynamicEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfDynamicEntry::pool_size * (SgAsmElfDynamicEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfDynamicEntryStroageClass
 * from its counterpart of SgAsmElfDynamicEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfDynamicEntry::initializeStorageClassArray(SgAsmElfDynamicEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfDynamicEntry::pools.begin(); block != SgAsmElfDynamicEntry::pools.end(); ++block) {
        SgAsmElfDynamicEntry* pointer{reinterpret_cast<SgAsmElfDynamicEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 199458 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfDynamicEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmElfDynamicEntryList::SgAsmElfDynamicEntryList(const SgAsmElfDynamicEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 199468 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfDynamicEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfDynamicEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfDynamicEntryList*
SgAsmElfDynamicEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfDynamicEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfDynamicEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfDynamicEntryList::pool_size};
        returnPointer = &(((SgAsmElfDynamicEntryList*)(SgAsmElfDynamicEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfDynamicEntryList*
SgAsmElfDynamicEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfDynamicEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfDynamicEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfDynamicEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfDynamicEntryList::pool_size};
        returnPointer = &(((SgAsmElfDynamicEntryList*)(SgAsmElfDynamicEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfDynamicEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfDynamicEntryList::pools.begin(); block != SgAsmElfDynamicEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfDynamicEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfDynamicEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfDynamicEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfDynamicEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfDynamicEntryList::pools.begin(); block != SgAsmElfDynamicEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfDynamicEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfDynamicEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfDynamicEntryList::clearMemoryPool() {
    SgAsmElfDynamicEntryList* tempPointer{nullptr};
    if (SgAsmElfDynamicEntryList::pools.empty() == false) {
        auto block{SgAsmElfDynamicEntryList::pools.begin()};
        SgAsmElfDynamicEntryList::next_node = reinterpret_cast<SgAsmElfDynamicEntryList*>(*block);
        for (; block != SgAsmElfDynamicEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfDynamicEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfDynamicEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfDynamicEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfDynamicEntryList::pool_size-1]);
        }
    }
}

void
SgAsmElfDynamicEntryList::deleteMemoryPool() {
    for (auto p: SgAsmElfDynamicEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfDynamicEntryList::next_node = nullptr;
    SgAsmElfDynamicEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfDynamicEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicEntryList)};
    for (size_t blockIndex = SgAsmElfDynamicEntryList::pools.size(); (blockIndex * SgAsmElfDynamicEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicEntryList) = %" PRIuPTR " SgAsmElfDynamicEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicEntryList),SgAsmElfDynamicEntryList::pool_size);
        }
#endif

        SgAsmElfDynamicEntryList* pointer{static_cast<SgAsmElfDynamicEntryList*>(ROSE_MALLOC(SgAsmElfDynamicEntryList::pool_size * sizeof(SgAsmElfDynamicEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfDynamicEntryList::pool_size * sizeof(SgAsmElfDynamicEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfDynamicEntryList::pool_size * sizeof(SgAsmElfDynamicEntryList));
#endif
        SgAsmElfDynamicEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfDynamicEntryList::pool_size * sizeof(SgAsmElfDynamicEntryList), V_SgAsmElfDynamicEntryList));

        if (SgAsmElfDynamicEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfDynamicEntryList* blkptr{reinterpret_cast<SgAsmElfDynamicEntryList*>(SgAsmElfDynamicEntryList::pools[blockIndex-1])};
                blkptr[SgAsmElfDynamicEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfDynamicEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfDynamicEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfDynamicEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfDynamicEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfDynamicEntryList::pool_size - 1};
    SgAsmElfDynamicEntryList* testPointer{reinterpret_cast<SgAsmElfDynamicEntryList*>(SgAsmElfDynamicEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfDynamicEntryList::pool_size * (SgAsmElfDynamicEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfDynamicEntryListStroageClass
 * from its counterpart of SgAsmElfDynamicEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfDynamicEntryList::initializeStorageClassArray(SgAsmElfDynamicEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfDynamicEntryList::pools.begin(); block != SgAsmElfDynamicEntryList::pools.end(); ++block) {
        SgAsmElfDynamicEntryList* pointer{reinterpret_cast<SgAsmElfDynamicEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 199690 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfEHFrameEntryCI that takes its corresponding StorageClass as parameter
 */
SgAsmElfEHFrameEntryCI::SgAsmElfEHFrameEntryCI(const SgAsmElfEHFrameEntryCIStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 199700 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_version = storageSource.storageOf_version;
    p_augmentation_string = storageSource.storageOf_augmentation_string.rebuildDataStoredInEasyStorageClass();
    p_eh_data = storageSource.storageOf_eh_data;
    p_code_alignment_factor = storageSource.storageOf_code_alignment_factor;
    p_data_alignment_factor = storageSource.storageOf_data_alignment_factor;
    p_augmentation_data_length = storageSource.storageOf_augmentation_data_length;
    p_lsda_encoding = storageSource.storageOf_lsda_encoding;
    p_prh_encoding = storageSource.storageOf_prh_encoding;
    p_prh_arg = storageSource.storageOf_prh_arg;
    p_prh_addr = storageSource.storageOf_prh_addr;
    p_addr_encoding = storageSource.storageOf_addr_encoding;
    p_sig_frame = storageSource.storageOf_sig_frame;
    p_instructions = storageSource.storageOf_instructions.rebuildDataStoredInEasyStorageClass();
     p_fd_entries = (SgAsmElfEHFrameEntryFDList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_fd_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfEHFrameEntryCI*
SgAsmElfEHFrameEntryCI::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfEHFrameEntryCI* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCI)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCI + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCI)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCI)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryCI::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryCI::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryCI*)(SgAsmElfEHFrameEntryCI::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfEHFrameEntryCI*
SgAsmElfEHFrameEntryCI::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfEHFrameEntryCI* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryCI)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryCI + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryCI)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfEHFrameEntryCI)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryCI::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryCI::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryCI*)(SgAsmElfEHFrameEntryCI::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfEHFrameEntryCI::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfEHFrameEntryCI::pools.begin(); block != SgAsmElfEHFrameEntryCI::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCI*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCI::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfEHFrameEntryCI*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfEHFrameEntryCI::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfEHFrameEntryCI* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfEHFrameEntryCI::pools.begin(); block != SgAsmElfEHFrameEntryCI::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCI*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCI::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfEHFrameEntryCI::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfEHFrameEntryCI::clearMemoryPool() {
    SgAsmElfEHFrameEntryCI* tempPointer{nullptr};
    if (SgAsmElfEHFrameEntryCI::pools.empty() == false) {
        auto block{SgAsmElfEHFrameEntryCI::pools.begin()};
        SgAsmElfEHFrameEntryCI::next_node = reinterpret_cast<SgAsmElfEHFrameEntryCI*>(*block);
        for (; block != SgAsmElfEHFrameEntryCI::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCI*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfEHFrameEntryCI::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfEHFrameEntryCI::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfEHFrameEntryCI::pool_size-1]);
        }
    }
}

void
SgAsmElfEHFrameEntryCI::deleteMemoryPool() {
    for (auto p: SgAsmElfEHFrameEntryCI::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfEHFrameEntryCI::next_node = nullptr;
    SgAsmElfEHFrameEntryCI::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfEHFrameEntryCI::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCI) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCI)};
    for (size_t blockIndex = SgAsmElfEHFrameEntryCI::pools.size(); (blockIndex * SgAsmElfEHFrameEntryCI::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCI) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCI) = %" PRIuPTR " SgAsmElfEHFrameEntryCI::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCI),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCI),SgAsmElfEHFrameEntryCI::pool_size);
        }
#endif

        SgAsmElfEHFrameEntryCI* pointer{static_cast<SgAsmElfEHFrameEntryCI*>(ROSE_MALLOC(SgAsmElfEHFrameEntryCI::pool_size * sizeof(SgAsmElfEHFrameEntryCI)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfEHFrameEntryCI::pool_size * sizeof(SgAsmElfEHFrameEntryCI));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfEHFrameEntryCI::pool_size * sizeof(SgAsmElfEHFrameEntryCI));
#endif
        SgAsmElfEHFrameEntryCI::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfEHFrameEntryCI::pool_size * sizeof(SgAsmElfEHFrameEntryCI), V_SgAsmElfEHFrameEntryCI));

        if (SgAsmElfEHFrameEntryCI::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfEHFrameEntryCI* blkptr{reinterpret_cast<SgAsmElfEHFrameEntryCI*>(SgAsmElfEHFrameEntryCI::pools[blockIndex-1])};
                blkptr[SgAsmElfEHFrameEntryCI::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfEHFrameEntryCI::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCI::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfEHFrameEntryCI::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfEHFrameEntryCI::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfEHFrameEntryCI::pool_size - 1};
    SgAsmElfEHFrameEntryCI* testPointer{reinterpret_cast<SgAsmElfEHFrameEntryCI*>(SgAsmElfEHFrameEntryCI::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfEHFrameEntryCI::pool_size * (SgAsmElfEHFrameEntryCI::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfEHFrameEntryCIStroageClass
 * from its counterpart of SgAsmElfEHFrameEntryCI. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfEHFrameEntryCI::initializeStorageClassArray(SgAsmElfEHFrameEntryCIStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfEHFrameEntryCI::pools.begin(); block != SgAsmElfEHFrameEntryCI::pools.end(); ++block) {
        SgAsmElfEHFrameEntryCI* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCI*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCI::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 199931 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfEHFrameEntryCIList that takes its corresponding StorageClass as parameter
 */
SgAsmElfEHFrameEntryCIList::SgAsmElfEHFrameEntryCIList(const SgAsmElfEHFrameEntryCIListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 199941 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfEHFrameEntryCIPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfEHFrameEntryCIPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfEHFrameEntryCIList*
SgAsmElfEHFrameEntryCIList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfEHFrameEntryCIList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCIList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCIList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCIList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCIList)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryCIList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryCIList::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryCIList*)(SgAsmElfEHFrameEntryCIList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfEHFrameEntryCIList*
SgAsmElfEHFrameEntryCIList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfEHFrameEntryCIList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryCIList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryCIList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryCIList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfEHFrameEntryCIList)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryCIList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryCIList::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryCIList*)(SgAsmElfEHFrameEntryCIList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfEHFrameEntryCIList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfEHFrameEntryCIList::pools.begin(); block != SgAsmElfEHFrameEntryCIList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCIList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCIList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfEHFrameEntryCIList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfEHFrameEntryCIList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfEHFrameEntryCIList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfEHFrameEntryCIList::pools.begin(); block != SgAsmElfEHFrameEntryCIList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCIList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCIList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfEHFrameEntryCIList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfEHFrameEntryCIList::clearMemoryPool() {
    SgAsmElfEHFrameEntryCIList* tempPointer{nullptr};
    if (SgAsmElfEHFrameEntryCIList::pools.empty() == false) {
        auto block{SgAsmElfEHFrameEntryCIList::pools.begin()};
        SgAsmElfEHFrameEntryCIList::next_node = reinterpret_cast<SgAsmElfEHFrameEntryCIList*>(*block);
        for (; block != SgAsmElfEHFrameEntryCIList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCIList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfEHFrameEntryCIList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfEHFrameEntryCIList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfEHFrameEntryCIList::pool_size-1]);
        }
    }
}

void
SgAsmElfEHFrameEntryCIList::deleteMemoryPool() {
    for (auto p: SgAsmElfEHFrameEntryCIList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfEHFrameEntryCIList::next_node = nullptr;
    SgAsmElfEHFrameEntryCIList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfEHFrameEntryCIList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCIList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCIList)};
    for (size_t blockIndex = SgAsmElfEHFrameEntryCIList::pools.size(); (blockIndex * SgAsmElfEHFrameEntryCIList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCIList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCIList) = %" PRIuPTR " SgAsmElfEHFrameEntryCIList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryCIList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryCIList),SgAsmElfEHFrameEntryCIList::pool_size);
        }
#endif

        SgAsmElfEHFrameEntryCIList* pointer{static_cast<SgAsmElfEHFrameEntryCIList*>(ROSE_MALLOC(SgAsmElfEHFrameEntryCIList::pool_size * sizeof(SgAsmElfEHFrameEntryCIList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfEHFrameEntryCIList::pool_size * sizeof(SgAsmElfEHFrameEntryCIList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfEHFrameEntryCIList::pool_size * sizeof(SgAsmElfEHFrameEntryCIList));
#endif
        SgAsmElfEHFrameEntryCIList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfEHFrameEntryCIList::pool_size * sizeof(SgAsmElfEHFrameEntryCIList), V_SgAsmElfEHFrameEntryCIList));

        if (SgAsmElfEHFrameEntryCIList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfEHFrameEntryCIList* blkptr{reinterpret_cast<SgAsmElfEHFrameEntryCIList*>(SgAsmElfEHFrameEntryCIList::pools[blockIndex-1])};
                blkptr[SgAsmElfEHFrameEntryCIList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfEHFrameEntryCIList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCIList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfEHFrameEntryCIList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfEHFrameEntryCIList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfEHFrameEntryCIList::pool_size - 1};
    SgAsmElfEHFrameEntryCIList* testPointer{reinterpret_cast<SgAsmElfEHFrameEntryCIList*>(SgAsmElfEHFrameEntryCIList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfEHFrameEntryCIList::pool_size * (SgAsmElfEHFrameEntryCIList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfEHFrameEntryCIListStroageClass
 * from its counterpart of SgAsmElfEHFrameEntryCIList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfEHFrameEntryCIList::initializeStorageClassArray(SgAsmElfEHFrameEntryCIListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfEHFrameEntryCIList::pools.begin(); block != SgAsmElfEHFrameEntryCIList::pools.end(); ++block) {
        SgAsmElfEHFrameEntryCIList* pointer{reinterpret_cast<SgAsmElfEHFrameEntryCIList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryCIList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 200163 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfEHFrameEntryFD that takes its corresponding StorageClass as parameter
 */
SgAsmElfEHFrameEntryFD::SgAsmElfEHFrameEntryFD(const SgAsmElfEHFrameEntryFDStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 200173 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_begin_rva = storageSource.storageOf_begin_rva;
    p_size = storageSource.storageOf_size;
    p_augmentation_data = storageSource.storageOf_augmentation_data.rebuildDataStoredInEasyStorageClass();
    p_instructions = storageSource.storageOf_instructions.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfEHFrameEntryFD*
SgAsmElfEHFrameEntryFD::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfEHFrameEntryFD* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFD)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFD + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFD)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFD)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryFD::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryFD::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryFD*)(SgAsmElfEHFrameEntryFD::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfEHFrameEntryFD*
SgAsmElfEHFrameEntryFD::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfEHFrameEntryFD* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryFD)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryFD + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryFD)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfEHFrameEntryFD)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryFD::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryFD::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryFD*)(SgAsmElfEHFrameEntryFD::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfEHFrameEntryFD::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfEHFrameEntryFD::pools.begin(); block != SgAsmElfEHFrameEntryFD::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFD*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFD::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfEHFrameEntryFD*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfEHFrameEntryFD::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfEHFrameEntryFD* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfEHFrameEntryFD::pools.begin(); block != SgAsmElfEHFrameEntryFD::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFD*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFD::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfEHFrameEntryFD::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfEHFrameEntryFD::clearMemoryPool() {
    SgAsmElfEHFrameEntryFD* tempPointer{nullptr};
    if (SgAsmElfEHFrameEntryFD::pools.empty() == false) {
        auto block{SgAsmElfEHFrameEntryFD::pools.begin()};
        SgAsmElfEHFrameEntryFD::next_node = reinterpret_cast<SgAsmElfEHFrameEntryFD*>(*block);
        for (; block != SgAsmElfEHFrameEntryFD::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFD*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfEHFrameEntryFD::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfEHFrameEntryFD::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfEHFrameEntryFD::pool_size-1]);
        }
    }
}

void
SgAsmElfEHFrameEntryFD::deleteMemoryPool() {
    for (auto p: SgAsmElfEHFrameEntryFD::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfEHFrameEntryFD::next_node = nullptr;
    SgAsmElfEHFrameEntryFD::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfEHFrameEntryFD::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFD) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFD)};
    for (size_t blockIndex = SgAsmElfEHFrameEntryFD::pools.size(); (blockIndex * SgAsmElfEHFrameEntryFD::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFD) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFD) = %" PRIuPTR " SgAsmElfEHFrameEntryFD::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFD),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFD),SgAsmElfEHFrameEntryFD::pool_size);
        }
#endif

        SgAsmElfEHFrameEntryFD* pointer{static_cast<SgAsmElfEHFrameEntryFD*>(ROSE_MALLOC(SgAsmElfEHFrameEntryFD::pool_size * sizeof(SgAsmElfEHFrameEntryFD)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfEHFrameEntryFD::pool_size * sizeof(SgAsmElfEHFrameEntryFD));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfEHFrameEntryFD::pool_size * sizeof(SgAsmElfEHFrameEntryFD));
#endif
        SgAsmElfEHFrameEntryFD::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfEHFrameEntryFD::pool_size * sizeof(SgAsmElfEHFrameEntryFD), V_SgAsmElfEHFrameEntryFD));

        if (SgAsmElfEHFrameEntryFD::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfEHFrameEntryFD* blkptr{reinterpret_cast<SgAsmElfEHFrameEntryFD*>(SgAsmElfEHFrameEntryFD::pools[blockIndex-1])};
                blkptr[SgAsmElfEHFrameEntryFD::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfEHFrameEntryFD::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFD::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfEHFrameEntryFD::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfEHFrameEntryFD::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfEHFrameEntryFD::pool_size - 1};
    SgAsmElfEHFrameEntryFD* testPointer{reinterpret_cast<SgAsmElfEHFrameEntryFD*>(SgAsmElfEHFrameEntryFD::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfEHFrameEntryFD::pool_size * (SgAsmElfEHFrameEntryFD::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfEHFrameEntryFDStroageClass
 * from its counterpart of SgAsmElfEHFrameEntryFD. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfEHFrameEntryFD::initializeStorageClassArray(SgAsmElfEHFrameEntryFDStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfEHFrameEntryFD::pools.begin(); block != SgAsmElfEHFrameEntryFD::pools.end(); ++block) {
        SgAsmElfEHFrameEntryFD* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFD*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFD::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 200394 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfEHFrameEntryFDList that takes its corresponding StorageClass as parameter
 */
SgAsmElfEHFrameEntryFDList::SgAsmElfEHFrameEntryFDList(const SgAsmElfEHFrameEntryFDListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 200404 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfEHFrameEntryFDPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfEHFrameEntryFDPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfEHFrameEntryFDList*
SgAsmElfEHFrameEntryFDList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfEHFrameEntryFDList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFDList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFDList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFDList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFDList)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryFDList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryFDList::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryFDList*)(SgAsmElfEHFrameEntryFDList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfEHFrameEntryFDList*
SgAsmElfEHFrameEntryFDList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfEHFrameEntryFDList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryFDList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryFDList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameEntryFDList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfEHFrameEntryFDList)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameEntryFDList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameEntryFDList::pool_size};
        returnPointer = &(((SgAsmElfEHFrameEntryFDList*)(SgAsmElfEHFrameEntryFDList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfEHFrameEntryFDList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfEHFrameEntryFDList::pools.begin(); block != SgAsmElfEHFrameEntryFDList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFDList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFDList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfEHFrameEntryFDList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfEHFrameEntryFDList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfEHFrameEntryFDList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfEHFrameEntryFDList::pools.begin(); block != SgAsmElfEHFrameEntryFDList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFDList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFDList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfEHFrameEntryFDList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfEHFrameEntryFDList::clearMemoryPool() {
    SgAsmElfEHFrameEntryFDList* tempPointer{nullptr};
    if (SgAsmElfEHFrameEntryFDList::pools.empty() == false) {
        auto block{SgAsmElfEHFrameEntryFDList::pools.begin()};
        SgAsmElfEHFrameEntryFDList::next_node = reinterpret_cast<SgAsmElfEHFrameEntryFDList*>(*block);
        for (; block != SgAsmElfEHFrameEntryFDList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFDList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfEHFrameEntryFDList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfEHFrameEntryFDList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfEHFrameEntryFDList::pool_size-1]);
        }
    }
}

void
SgAsmElfEHFrameEntryFDList::deleteMemoryPool() {
    for (auto p: SgAsmElfEHFrameEntryFDList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfEHFrameEntryFDList::next_node = nullptr;
    SgAsmElfEHFrameEntryFDList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfEHFrameEntryFDList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFDList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFDList)};
    for (size_t blockIndex = SgAsmElfEHFrameEntryFDList::pools.size(); (blockIndex * SgAsmElfEHFrameEntryFDList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFDList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFDList) = %" PRIuPTR " SgAsmElfEHFrameEntryFDList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameEntryFDList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameEntryFDList),SgAsmElfEHFrameEntryFDList::pool_size);
        }
#endif

        SgAsmElfEHFrameEntryFDList* pointer{static_cast<SgAsmElfEHFrameEntryFDList*>(ROSE_MALLOC(SgAsmElfEHFrameEntryFDList::pool_size * sizeof(SgAsmElfEHFrameEntryFDList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfEHFrameEntryFDList::pool_size * sizeof(SgAsmElfEHFrameEntryFDList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfEHFrameEntryFDList::pool_size * sizeof(SgAsmElfEHFrameEntryFDList));
#endif
        SgAsmElfEHFrameEntryFDList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfEHFrameEntryFDList::pool_size * sizeof(SgAsmElfEHFrameEntryFDList), V_SgAsmElfEHFrameEntryFDList));

        if (SgAsmElfEHFrameEntryFDList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfEHFrameEntryFDList* blkptr{reinterpret_cast<SgAsmElfEHFrameEntryFDList*>(SgAsmElfEHFrameEntryFDList::pools[blockIndex-1])};
                blkptr[SgAsmElfEHFrameEntryFDList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfEHFrameEntryFDList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFDList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfEHFrameEntryFDList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfEHFrameEntryFDList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfEHFrameEntryFDList::pool_size - 1};
    SgAsmElfEHFrameEntryFDList* testPointer{reinterpret_cast<SgAsmElfEHFrameEntryFDList*>(SgAsmElfEHFrameEntryFDList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfEHFrameEntryFDList::pool_size * (SgAsmElfEHFrameEntryFDList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfEHFrameEntryFDListStroageClass
 * from its counterpart of SgAsmElfEHFrameEntryFDList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfEHFrameEntryFDList::initializeStorageClassArray(SgAsmElfEHFrameEntryFDListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfEHFrameEntryFDList::pools.begin(); block != SgAsmElfEHFrameEntryFDList::pools.end(); ++block) {
        SgAsmElfEHFrameEntryFDList* pointer{reinterpret_cast<SgAsmElfEHFrameEntryFDList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameEntryFDList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 200626 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfNoteEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfNoteEntry::SgAsmElfNoteEntry(const SgAsmElfNoteEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 200636 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_type = storageSource.storageOf_type;
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));
    p_payload = storageSource.storageOf_payload.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfNoteEntry*
SgAsmElfNoteEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfNoteEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfNoteEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfNoteEntry::pool_size};
        returnPointer = &(((SgAsmElfNoteEntry*)(SgAsmElfNoteEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfNoteEntry*
SgAsmElfNoteEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfNoteEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfNoteEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfNoteEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfNoteEntry::pool_size};
        returnPointer = &(((SgAsmElfNoteEntry*)(SgAsmElfNoteEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfNoteEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfNoteEntry::pools.begin(); block != SgAsmElfNoteEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfNoteEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfNoteEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfNoteEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfNoteEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfNoteEntry::pools.begin(); block != SgAsmElfNoteEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfNoteEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfNoteEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfNoteEntry::clearMemoryPool() {
    SgAsmElfNoteEntry* tempPointer{nullptr};
    if (SgAsmElfNoteEntry::pools.empty() == false) {
        auto block{SgAsmElfNoteEntry::pools.begin()};
        SgAsmElfNoteEntry::next_node = reinterpret_cast<SgAsmElfNoteEntry*>(*block);
        for (; block != SgAsmElfNoteEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfNoteEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfNoteEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfNoteEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfNoteEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfNoteEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfNoteEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfNoteEntry::next_node = nullptr;
    SgAsmElfNoteEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfNoteEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteEntry)};
    for (size_t blockIndex = SgAsmElfNoteEntry::pools.size(); (blockIndex * SgAsmElfNoteEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteEntry) = %" PRIuPTR " SgAsmElfNoteEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteEntry),SgAsmElfNoteEntry::pool_size);
        }
#endif

        SgAsmElfNoteEntry* pointer{static_cast<SgAsmElfNoteEntry*>(ROSE_MALLOC(SgAsmElfNoteEntry::pool_size * sizeof(SgAsmElfNoteEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfNoteEntry::pool_size * sizeof(SgAsmElfNoteEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfNoteEntry::pool_size * sizeof(SgAsmElfNoteEntry));
#endif
        SgAsmElfNoteEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfNoteEntry::pool_size * sizeof(SgAsmElfNoteEntry), V_SgAsmElfNoteEntry));

        if (SgAsmElfNoteEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfNoteEntry* blkptr{reinterpret_cast<SgAsmElfNoteEntry*>(SgAsmElfNoteEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfNoteEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfNoteEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfNoteEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfNoteEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfNoteEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfNoteEntry::pool_size - 1};
    SgAsmElfNoteEntry* testPointer{reinterpret_cast<SgAsmElfNoteEntry*>(SgAsmElfNoteEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfNoteEntry::pool_size * (SgAsmElfNoteEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfNoteEntryStroageClass
 * from its counterpart of SgAsmElfNoteEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfNoteEntry::initializeStorageClassArray(SgAsmElfNoteEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfNoteEntry::pools.begin(); block != SgAsmElfNoteEntry::pools.end(); ++block) {
        SgAsmElfNoteEntry* pointer{reinterpret_cast<SgAsmElfNoteEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 200856 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfNoteEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmElfNoteEntryList::SgAsmElfNoteEntryList(const SgAsmElfNoteEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 200866 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfNoteEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfNoteEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfNoteEntryList*
SgAsmElfNoteEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfNoteEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfNoteEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfNoteEntryList::pool_size};
        returnPointer = &(((SgAsmElfNoteEntryList*)(SgAsmElfNoteEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfNoteEntryList*
SgAsmElfNoteEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfNoteEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfNoteEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfNoteEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfNoteEntryList::pool_size};
        returnPointer = &(((SgAsmElfNoteEntryList*)(SgAsmElfNoteEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfNoteEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfNoteEntryList::pools.begin(); block != SgAsmElfNoteEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfNoteEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfNoteEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfNoteEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfNoteEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfNoteEntryList::pools.begin(); block != SgAsmElfNoteEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfNoteEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfNoteEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfNoteEntryList::clearMemoryPool() {
    SgAsmElfNoteEntryList* tempPointer{nullptr};
    if (SgAsmElfNoteEntryList::pools.empty() == false) {
        auto block{SgAsmElfNoteEntryList::pools.begin()};
        SgAsmElfNoteEntryList::next_node = reinterpret_cast<SgAsmElfNoteEntryList*>(*block);
        for (; block != SgAsmElfNoteEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfNoteEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfNoteEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfNoteEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfNoteEntryList::pool_size-1]);
        }
    }
}

void
SgAsmElfNoteEntryList::deleteMemoryPool() {
    for (auto p: SgAsmElfNoteEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfNoteEntryList::next_node = nullptr;
    SgAsmElfNoteEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfNoteEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteEntryList)};
    for (size_t blockIndex = SgAsmElfNoteEntryList::pools.size(); (blockIndex * SgAsmElfNoteEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteEntryList) = %" PRIuPTR " SgAsmElfNoteEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteEntryList),SgAsmElfNoteEntryList::pool_size);
        }
#endif

        SgAsmElfNoteEntryList* pointer{static_cast<SgAsmElfNoteEntryList*>(ROSE_MALLOC(SgAsmElfNoteEntryList::pool_size * sizeof(SgAsmElfNoteEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfNoteEntryList::pool_size * sizeof(SgAsmElfNoteEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfNoteEntryList::pool_size * sizeof(SgAsmElfNoteEntryList));
#endif
        SgAsmElfNoteEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfNoteEntryList::pool_size * sizeof(SgAsmElfNoteEntryList), V_SgAsmElfNoteEntryList));

        if (SgAsmElfNoteEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfNoteEntryList* blkptr{reinterpret_cast<SgAsmElfNoteEntryList*>(SgAsmElfNoteEntryList::pools[blockIndex-1])};
                blkptr[SgAsmElfNoteEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfNoteEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfNoteEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfNoteEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfNoteEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfNoteEntryList::pool_size - 1};
    SgAsmElfNoteEntryList* testPointer{reinterpret_cast<SgAsmElfNoteEntryList*>(SgAsmElfNoteEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfNoteEntryList::pool_size * (SgAsmElfNoteEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfNoteEntryListStroageClass
 * from its counterpart of SgAsmElfNoteEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfNoteEntryList::initializeStorageClassArray(SgAsmElfNoteEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfNoteEntryList::pools.begin(); block != SgAsmElfNoteEntryList::pools.end(); ++block) {
        SgAsmElfNoteEntryList* pointer{reinterpret_cast<SgAsmElfNoteEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 201088 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfRelocEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfRelocEntry::SgAsmElfRelocEntry(const SgAsmElfRelocEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 201098 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_r_offset = storageSource.storageOf_r_offset;
    p_r_addend = storageSource.storageOf_r_addend;
    p_sym = storageSource.storageOf_sym;
    p_type = storageSource.storageOf_type;
    p_extra = storageSource.storageOf_extra.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfRelocEntry*
SgAsmElfRelocEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfRelocEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfRelocEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfRelocEntry::pool_size};
        returnPointer = &(((SgAsmElfRelocEntry*)(SgAsmElfRelocEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfRelocEntry*
SgAsmElfRelocEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfRelocEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfRelocEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfRelocEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfRelocEntry::pool_size};
        returnPointer = &(((SgAsmElfRelocEntry*)(SgAsmElfRelocEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfRelocEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfRelocEntry::pools.begin(); block != SgAsmElfRelocEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfRelocEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfRelocEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfRelocEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfRelocEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfRelocEntry::pools.begin(); block != SgAsmElfRelocEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfRelocEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfRelocEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfRelocEntry::clearMemoryPool() {
    SgAsmElfRelocEntry* tempPointer{nullptr};
    if (SgAsmElfRelocEntry::pools.empty() == false) {
        auto block{SgAsmElfRelocEntry::pools.begin()};
        SgAsmElfRelocEntry::next_node = reinterpret_cast<SgAsmElfRelocEntry*>(*block);
        for (; block != SgAsmElfRelocEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfRelocEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfRelocEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfRelocEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfRelocEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfRelocEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfRelocEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfRelocEntry::next_node = nullptr;
    SgAsmElfRelocEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfRelocEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocEntry)};
    for (size_t blockIndex = SgAsmElfRelocEntry::pools.size(); (blockIndex * SgAsmElfRelocEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocEntry) = %" PRIuPTR " SgAsmElfRelocEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocEntry),SgAsmElfRelocEntry::pool_size);
        }
#endif

        SgAsmElfRelocEntry* pointer{static_cast<SgAsmElfRelocEntry*>(ROSE_MALLOC(SgAsmElfRelocEntry::pool_size * sizeof(SgAsmElfRelocEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfRelocEntry::pool_size * sizeof(SgAsmElfRelocEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfRelocEntry::pool_size * sizeof(SgAsmElfRelocEntry));
#endif
        SgAsmElfRelocEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfRelocEntry::pool_size * sizeof(SgAsmElfRelocEntry), V_SgAsmElfRelocEntry));

        if (SgAsmElfRelocEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfRelocEntry* blkptr{reinterpret_cast<SgAsmElfRelocEntry*>(SgAsmElfRelocEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfRelocEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfRelocEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfRelocEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfRelocEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfRelocEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfRelocEntry::pool_size - 1};
    SgAsmElfRelocEntry* testPointer{reinterpret_cast<SgAsmElfRelocEntry*>(SgAsmElfRelocEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfRelocEntry::pool_size * (SgAsmElfRelocEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfRelocEntryStroageClass
 * from its counterpart of SgAsmElfRelocEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfRelocEntry::initializeStorageClassArray(SgAsmElfRelocEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfRelocEntry::pools.begin(); block != SgAsmElfRelocEntry::pools.end(); ++block) {
        SgAsmElfRelocEntry* pointer{reinterpret_cast<SgAsmElfRelocEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 201320 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfRelocEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmElfRelocEntryList::SgAsmElfRelocEntryList(const SgAsmElfRelocEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 201330 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfRelocEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfRelocEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfRelocEntryList*
SgAsmElfRelocEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfRelocEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfRelocEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfRelocEntryList::pool_size};
        returnPointer = &(((SgAsmElfRelocEntryList*)(SgAsmElfRelocEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfRelocEntryList*
SgAsmElfRelocEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfRelocEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfRelocEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfRelocEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfRelocEntryList::pool_size};
        returnPointer = &(((SgAsmElfRelocEntryList*)(SgAsmElfRelocEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfRelocEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfRelocEntryList::pools.begin(); block != SgAsmElfRelocEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfRelocEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfRelocEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfRelocEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfRelocEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfRelocEntryList::pools.begin(); block != SgAsmElfRelocEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfRelocEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfRelocEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfRelocEntryList::clearMemoryPool() {
    SgAsmElfRelocEntryList* tempPointer{nullptr};
    if (SgAsmElfRelocEntryList::pools.empty() == false) {
        auto block{SgAsmElfRelocEntryList::pools.begin()};
        SgAsmElfRelocEntryList::next_node = reinterpret_cast<SgAsmElfRelocEntryList*>(*block);
        for (; block != SgAsmElfRelocEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfRelocEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfRelocEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfRelocEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfRelocEntryList::pool_size-1]);
        }
    }
}

void
SgAsmElfRelocEntryList::deleteMemoryPool() {
    for (auto p: SgAsmElfRelocEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfRelocEntryList::next_node = nullptr;
    SgAsmElfRelocEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfRelocEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocEntryList)};
    for (size_t blockIndex = SgAsmElfRelocEntryList::pools.size(); (blockIndex * SgAsmElfRelocEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocEntryList) = %" PRIuPTR " SgAsmElfRelocEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocEntryList),SgAsmElfRelocEntryList::pool_size);
        }
#endif

        SgAsmElfRelocEntryList* pointer{static_cast<SgAsmElfRelocEntryList*>(ROSE_MALLOC(SgAsmElfRelocEntryList::pool_size * sizeof(SgAsmElfRelocEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfRelocEntryList::pool_size * sizeof(SgAsmElfRelocEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfRelocEntryList::pool_size * sizeof(SgAsmElfRelocEntryList));
#endif
        SgAsmElfRelocEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfRelocEntryList::pool_size * sizeof(SgAsmElfRelocEntryList), V_SgAsmElfRelocEntryList));

        if (SgAsmElfRelocEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfRelocEntryList* blkptr{reinterpret_cast<SgAsmElfRelocEntryList*>(SgAsmElfRelocEntryList::pools[blockIndex-1])};
                blkptr[SgAsmElfRelocEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfRelocEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfRelocEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfRelocEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfRelocEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfRelocEntryList::pool_size - 1};
    SgAsmElfRelocEntryList* testPointer{reinterpret_cast<SgAsmElfRelocEntryList*>(SgAsmElfRelocEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfRelocEntryList::pool_size * (SgAsmElfRelocEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfRelocEntryListStroageClass
 * from its counterpart of SgAsmElfRelocEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfRelocEntryList::initializeStorageClassArray(SgAsmElfRelocEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfRelocEntryList::pools.begin(); block != SgAsmElfRelocEntryList::pools.end(); ++block) {
        SgAsmElfRelocEntryList* pointer{reinterpret_cast<SgAsmElfRelocEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 201552 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSectionTableEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfSectionTableEntry::SgAsmElfSectionTableEntry(const SgAsmElfSectionTableEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 201562 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_sh_name = storageSource.storageOf_sh_name;
    p_sh_type = storageSource.storageOf_sh_type;
    p_sh_link = storageSource.storageOf_sh_link;
    p_sh_info = storageSource.storageOf_sh_info;
    p_sh_flags = storageSource.storageOf_sh_flags;
    p_sh_addr = storageSource.storageOf_sh_addr;
    p_sh_offset = storageSource.storageOf_sh_offset;
    p_sh_size = storageSource.storageOf_sh_size;
    p_sh_addralign = storageSource.storageOf_sh_addralign;
    p_sh_entsize = storageSource.storageOf_sh_entsize;
    p_extra = storageSource.storageOf_extra.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSectionTableEntry*
SgAsmElfSectionTableEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSectionTableEntry::pool_size};
        returnPointer = &(((SgAsmElfSectionTableEntry*)(SgAsmElfSectionTableEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSectionTableEntry*
SgAsmElfSectionTableEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSectionTableEntry::pool_size};
        returnPointer = &(((SgAsmElfSectionTableEntry*)(SgAsmElfSectionTableEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSectionTableEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSectionTableEntry::pools.begin(); block != SgAsmElfSectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSectionTableEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSectionTableEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSectionTableEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSectionTableEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSectionTableEntry::pools.begin(); block != SgAsmElfSectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSectionTableEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSectionTableEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSectionTableEntry::clearMemoryPool() {
    SgAsmElfSectionTableEntry* tempPointer{nullptr};
    if (SgAsmElfSectionTableEntry::pools.empty() == false) {
        auto block{SgAsmElfSectionTableEntry::pools.begin()};
        SgAsmElfSectionTableEntry::next_node = reinterpret_cast<SgAsmElfSectionTableEntry*>(*block);
        for (; block != SgAsmElfSectionTableEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSectionTableEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSectionTableEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSectionTableEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSectionTableEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfSectionTableEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfSectionTableEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSectionTableEntry::next_node = nullptr;
    SgAsmElfSectionTableEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSectionTableEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTableEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSectionTableEntry)};
    for (size_t blockIndex = SgAsmElfSectionTableEntry::pools.size(); (blockIndex * SgAsmElfSectionTableEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTableEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSectionTableEntry) = %" PRIuPTR " SgAsmElfSectionTableEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTableEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSectionTableEntry),SgAsmElfSectionTableEntry::pool_size);
        }
#endif

        SgAsmElfSectionTableEntry* pointer{static_cast<SgAsmElfSectionTableEntry*>(ROSE_MALLOC(SgAsmElfSectionTableEntry::pool_size * sizeof(SgAsmElfSectionTableEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSectionTableEntry::pool_size * sizeof(SgAsmElfSectionTableEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSectionTableEntry::pool_size * sizeof(SgAsmElfSectionTableEntry));
#endif
        SgAsmElfSectionTableEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSectionTableEntry::pool_size * sizeof(SgAsmElfSectionTableEntry), V_SgAsmElfSectionTableEntry));

        if (SgAsmElfSectionTableEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSectionTableEntry* blkptr{reinterpret_cast<SgAsmElfSectionTableEntry*>(SgAsmElfSectionTableEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfSectionTableEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSectionTableEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSectionTableEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSectionTableEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSectionTableEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSectionTableEntry::pool_size - 1};
    SgAsmElfSectionTableEntry* testPointer{reinterpret_cast<SgAsmElfSectionTableEntry*>(SgAsmElfSectionTableEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSectionTableEntry::pool_size * (SgAsmElfSectionTableEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSectionTableEntryStroageClass
 * from its counterpart of SgAsmElfSectionTableEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSectionTableEntry::initializeStorageClassArray(SgAsmElfSectionTableEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSectionTableEntry::pools.begin(); block != SgAsmElfSectionTableEntry::pools.end(); ++block) {
        SgAsmElfSectionTableEntry* pointer{reinterpret_cast<SgAsmElfSectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSectionTableEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 201790 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSegmentTableEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfSegmentTableEntry::SgAsmElfSegmentTableEntry(const SgAsmElfSegmentTableEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 201800 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_index = storageSource.storageOf_index;
    p_type = storageSource.storageOf_type;
    p_flags = storageSource.storageOf_flags;
    p_offset = storageSource.storageOf_offset;
    p_vaddr = storageSource.storageOf_vaddr;
    p_paddr = storageSource.storageOf_paddr;
    p_filesz = storageSource.storageOf_filesz;
    p_memsz = storageSource.storageOf_memsz;
    p_align = storageSource.storageOf_align;
    p_extra = storageSource.storageOf_extra.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSegmentTableEntry*
SgAsmElfSegmentTableEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSegmentTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSegmentTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSegmentTableEntry::pool_size};
        returnPointer = &(((SgAsmElfSegmentTableEntry*)(SgAsmElfSegmentTableEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSegmentTableEntry*
SgAsmElfSegmentTableEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSegmentTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSegmentTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSegmentTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSegmentTableEntry::pool_size};
        returnPointer = &(((SgAsmElfSegmentTableEntry*)(SgAsmElfSegmentTableEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSegmentTableEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSegmentTableEntry::pools.begin(); block != SgAsmElfSegmentTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSegmentTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTableEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSegmentTableEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSegmentTableEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSegmentTableEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSegmentTableEntry::pools.begin(); block != SgAsmElfSegmentTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSegmentTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTableEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSegmentTableEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSegmentTableEntry::clearMemoryPool() {
    SgAsmElfSegmentTableEntry* tempPointer{nullptr};
    if (SgAsmElfSegmentTableEntry::pools.empty() == false) {
        auto block{SgAsmElfSegmentTableEntry::pools.begin()};
        SgAsmElfSegmentTableEntry::next_node = reinterpret_cast<SgAsmElfSegmentTableEntry*>(*block);
        for (; block != SgAsmElfSegmentTableEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSegmentTableEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSegmentTableEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSegmentTableEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSegmentTableEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfSegmentTableEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfSegmentTableEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSegmentTableEntry::next_node = nullptr;
    SgAsmElfSegmentTableEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSegmentTableEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntry)};
    for (size_t blockIndex = SgAsmElfSegmentTableEntry::pools.size(); (blockIndex * SgAsmElfSegmentTableEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntry) = %" PRIuPTR " SgAsmElfSegmentTableEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntry),SgAsmElfSegmentTableEntry::pool_size);
        }
#endif

        SgAsmElfSegmentTableEntry* pointer{static_cast<SgAsmElfSegmentTableEntry*>(ROSE_MALLOC(SgAsmElfSegmentTableEntry::pool_size * sizeof(SgAsmElfSegmentTableEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSegmentTableEntry::pool_size * sizeof(SgAsmElfSegmentTableEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSegmentTableEntry::pool_size * sizeof(SgAsmElfSegmentTableEntry));
#endif
        SgAsmElfSegmentTableEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSegmentTableEntry::pool_size * sizeof(SgAsmElfSegmentTableEntry), V_SgAsmElfSegmentTableEntry));

        if (SgAsmElfSegmentTableEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSegmentTableEntry* blkptr{reinterpret_cast<SgAsmElfSegmentTableEntry*>(SgAsmElfSegmentTableEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfSegmentTableEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSegmentTableEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSegmentTableEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSegmentTableEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSegmentTableEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSegmentTableEntry::pool_size - 1};
    SgAsmElfSegmentTableEntry* testPointer{reinterpret_cast<SgAsmElfSegmentTableEntry*>(SgAsmElfSegmentTableEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSegmentTableEntry::pool_size * (SgAsmElfSegmentTableEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSegmentTableEntryStroageClass
 * from its counterpart of SgAsmElfSegmentTableEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSegmentTableEntry::initializeStorageClassArray(SgAsmElfSegmentTableEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSegmentTableEntry::pools.begin(); block != SgAsmElfSegmentTableEntry::pools.end(); ++block) {
        SgAsmElfSegmentTableEntry* pointer{reinterpret_cast<SgAsmElfSegmentTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTableEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 202027 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSegmentTableEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmElfSegmentTableEntryList::SgAsmElfSegmentTableEntryList(const SgAsmElfSegmentTableEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 202037 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfSegmentTableEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfSegmentTableEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSegmentTableEntryList*
SgAsmElfSegmentTableEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSegmentTableEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSegmentTableEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSegmentTableEntryList::pool_size};
        returnPointer = &(((SgAsmElfSegmentTableEntryList*)(SgAsmElfSegmentTableEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSegmentTableEntryList*
SgAsmElfSegmentTableEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSegmentTableEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTableEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTableEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTableEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSegmentTableEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSegmentTableEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSegmentTableEntryList::pool_size};
        returnPointer = &(((SgAsmElfSegmentTableEntryList*)(SgAsmElfSegmentTableEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSegmentTableEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSegmentTableEntryList::pools.begin(); block != SgAsmElfSegmentTableEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSegmentTableEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTableEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSegmentTableEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSegmentTableEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSegmentTableEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSegmentTableEntryList::pools.begin(); block != SgAsmElfSegmentTableEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSegmentTableEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTableEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSegmentTableEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSegmentTableEntryList::clearMemoryPool() {
    SgAsmElfSegmentTableEntryList* tempPointer{nullptr};
    if (SgAsmElfSegmentTableEntryList::pools.empty() == false) {
        auto block{SgAsmElfSegmentTableEntryList::pools.begin()};
        SgAsmElfSegmentTableEntryList::next_node = reinterpret_cast<SgAsmElfSegmentTableEntryList*>(*block);
        for (; block != SgAsmElfSegmentTableEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSegmentTableEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSegmentTableEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSegmentTableEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSegmentTableEntryList::pool_size-1]);
        }
    }
}

void
SgAsmElfSegmentTableEntryList::deleteMemoryPool() {
    for (auto p: SgAsmElfSegmentTableEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSegmentTableEntryList::next_node = nullptr;
    SgAsmElfSegmentTableEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSegmentTableEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntryList)};
    for (size_t blockIndex = SgAsmElfSegmentTableEntryList::pools.size(); (blockIndex * SgAsmElfSegmentTableEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntryList) = %" PRIuPTR " SgAsmElfSegmentTableEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTableEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTableEntryList),SgAsmElfSegmentTableEntryList::pool_size);
        }
#endif

        SgAsmElfSegmentTableEntryList* pointer{static_cast<SgAsmElfSegmentTableEntryList*>(ROSE_MALLOC(SgAsmElfSegmentTableEntryList::pool_size * sizeof(SgAsmElfSegmentTableEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSegmentTableEntryList::pool_size * sizeof(SgAsmElfSegmentTableEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSegmentTableEntryList::pool_size * sizeof(SgAsmElfSegmentTableEntryList));
#endif
        SgAsmElfSegmentTableEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSegmentTableEntryList::pool_size * sizeof(SgAsmElfSegmentTableEntryList), V_SgAsmElfSegmentTableEntryList));

        if (SgAsmElfSegmentTableEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSegmentTableEntryList* blkptr{reinterpret_cast<SgAsmElfSegmentTableEntryList*>(SgAsmElfSegmentTableEntryList::pools[blockIndex-1])};
                blkptr[SgAsmElfSegmentTableEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSegmentTableEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSegmentTableEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSegmentTableEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSegmentTableEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSegmentTableEntryList::pool_size - 1};
    SgAsmElfSegmentTableEntryList* testPointer{reinterpret_cast<SgAsmElfSegmentTableEntryList*>(SgAsmElfSegmentTableEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSegmentTableEntryList::pool_size * (SgAsmElfSegmentTableEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSegmentTableEntryListStroageClass
 * from its counterpart of SgAsmElfSegmentTableEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSegmentTableEntryList::initializeStorageClassArray(SgAsmElfSegmentTableEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSegmentTableEntryList::pools.begin(); block != SgAsmElfSegmentTableEntryList::pools.end(); ++block) {
        SgAsmElfSegmentTableEntryList* pointer{reinterpret_cast<SgAsmElfSegmentTableEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTableEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 202259 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymbolList that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymbolList::SgAsmElfSymbolList(const SgAsmElfSymbolListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 202269 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_symbols = storageSource.storageOf_symbols.rebuildDataStoredInEasyStorageClass();
    SgAsmElfSymbolPtrList::iterator i_symbols = p_symbols.begin();
    for ( ; i_symbols != p_symbols.end(); ++i_symbols) {
        (*i_symbols) = (SgAsmElfSymbolPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_symbols)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymbolList*
SgAsmElfSymbolList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymbolList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbolList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbolList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbolList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymbolList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymbolList::pool_size};
        returnPointer = &(((SgAsmElfSymbolList*)(SgAsmElfSymbolList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymbolList*
SgAsmElfSymbolList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymbolList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbolList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbolList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbolList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymbolList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymbolList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymbolList::pool_size};
        returnPointer = &(((SgAsmElfSymbolList*)(SgAsmElfSymbolList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymbolList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymbolList::pools.begin(); block != SgAsmElfSymbolList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbolList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymbolList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymbolList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymbolList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymbolList::pools.begin(); block != SgAsmElfSymbolList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbolList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymbolList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymbolList::clearMemoryPool() {
    SgAsmElfSymbolList* tempPointer{nullptr};
    if (SgAsmElfSymbolList::pools.empty() == false) {
        auto block{SgAsmElfSymbolList::pools.begin()};
        SgAsmElfSymbolList::next_node = reinterpret_cast<SgAsmElfSymbolList*>(*block);
        for (; block != SgAsmElfSymbolList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymbolList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymbolList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymbolList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymbolList::pool_size-1]);
        }
    }
}

void
SgAsmElfSymbolList::deleteMemoryPool() {
    for (auto p: SgAsmElfSymbolList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymbolList::next_node = nullptr;
    SgAsmElfSymbolList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymbolList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbolList)};
    for (size_t blockIndex = SgAsmElfSymbolList::pools.size(); (blockIndex * SgAsmElfSymbolList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbolList) = %" PRIuPTR " SgAsmElfSymbolList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbolList),SgAsmElfSymbolList::pool_size);
        }
#endif

        SgAsmElfSymbolList* pointer{static_cast<SgAsmElfSymbolList*>(ROSE_MALLOC(SgAsmElfSymbolList::pool_size * sizeof(SgAsmElfSymbolList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymbolList::pool_size * sizeof(SgAsmElfSymbolList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymbolList::pool_size * sizeof(SgAsmElfSymbolList));
#endif
        SgAsmElfSymbolList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymbolList::pool_size * sizeof(SgAsmElfSymbolList), V_SgAsmElfSymbolList));

        if (SgAsmElfSymbolList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymbolList* blkptr{reinterpret_cast<SgAsmElfSymbolList*>(SgAsmElfSymbolList::pools[blockIndex-1])};
                blkptr[SgAsmElfSymbolList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymbolList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymbolList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymbolList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymbolList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymbolList::pool_size - 1};
    SgAsmElfSymbolList* testPointer{reinterpret_cast<SgAsmElfSymbolList*>(SgAsmElfSymbolList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymbolList::pool_size * (SgAsmElfSymbolList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymbolListStroageClass
 * from its counterpart of SgAsmElfSymbolList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymbolList::initializeStorageClassArray(SgAsmElfSymbolListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymbolList::pools.begin(); block != SgAsmElfSymbolList::pools.end(); ++block) {
        SgAsmElfSymbolList* pointer{reinterpret_cast<SgAsmElfSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbolList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 202491 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverDefinedAux that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverDefinedAux::SgAsmElfSymverDefinedAux(const SgAsmElfSymverDefinedAuxStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 202501 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverDefinedAux*
SgAsmElfSymverDefinedAux::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverDefinedAux* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAux)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAux + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAux)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAux)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedAux::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedAux::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedAux*)(SgAsmElfSymverDefinedAux::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverDefinedAux*
SgAsmElfSymverDefinedAux::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverDefinedAux* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedAux)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedAux + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedAux)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverDefinedAux)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedAux::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedAux::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedAux*)(SgAsmElfSymverDefinedAux::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverDefinedAux::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverDefinedAux::pools.begin(); block != SgAsmElfSymverDefinedAux::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedAux*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedAux::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverDefinedAux*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverDefinedAux::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverDefinedAux* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverDefinedAux::pools.begin(); block != SgAsmElfSymverDefinedAux::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedAux*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedAux::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverDefinedAux::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverDefinedAux::clearMemoryPool() {
    SgAsmElfSymverDefinedAux* tempPointer{nullptr};
    if (SgAsmElfSymverDefinedAux::pools.empty() == false) {
        auto block{SgAsmElfSymverDefinedAux::pools.begin()};
        SgAsmElfSymverDefinedAux::next_node = reinterpret_cast<SgAsmElfSymverDefinedAux*>(*block);
        for (; block != SgAsmElfSymverDefinedAux::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedAux*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverDefinedAux::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverDefinedAux::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverDefinedAux::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverDefinedAux::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverDefinedAux::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverDefinedAux::next_node = nullptr;
    SgAsmElfSymverDefinedAux::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverDefinedAux::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAux) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAux)};
    for (size_t blockIndex = SgAsmElfSymverDefinedAux::pools.size(); (blockIndex * SgAsmElfSymverDefinedAux::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAux) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAux) = %" PRIuPTR " SgAsmElfSymverDefinedAux::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAux),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAux),SgAsmElfSymverDefinedAux::pool_size);
        }
#endif

        SgAsmElfSymverDefinedAux* pointer{static_cast<SgAsmElfSymverDefinedAux*>(ROSE_MALLOC(SgAsmElfSymverDefinedAux::pool_size * sizeof(SgAsmElfSymverDefinedAux)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverDefinedAux::pool_size * sizeof(SgAsmElfSymverDefinedAux));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverDefinedAux::pool_size * sizeof(SgAsmElfSymverDefinedAux));
#endif
        SgAsmElfSymverDefinedAux::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverDefinedAux::pool_size * sizeof(SgAsmElfSymverDefinedAux), V_SgAsmElfSymverDefinedAux));

        if (SgAsmElfSymverDefinedAux::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverDefinedAux* blkptr{reinterpret_cast<SgAsmElfSymverDefinedAux*>(SgAsmElfSymverDefinedAux::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverDefinedAux::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverDefinedAux::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverDefinedAux::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverDefinedAux::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverDefinedAux::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverDefinedAux::pool_size - 1};
    SgAsmElfSymverDefinedAux* testPointer{reinterpret_cast<SgAsmElfSymverDefinedAux*>(SgAsmElfSymverDefinedAux::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverDefinedAux::pool_size * (SgAsmElfSymverDefinedAux::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverDefinedAuxStroageClass
 * from its counterpart of SgAsmElfSymverDefinedAux. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverDefinedAux::initializeStorageClassArray(SgAsmElfSymverDefinedAuxStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverDefinedAux::pools.begin(); block != SgAsmElfSymverDefinedAux::pools.end(); ++block) {
        SgAsmElfSymverDefinedAux* pointer{reinterpret_cast<SgAsmElfSymverDefinedAux*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedAux::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 202719 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverDefinedAuxList that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverDefinedAuxList::SgAsmElfSymverDefinedAuxList(const SgAsmElfSymverDefinedAuxListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 202729 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfSymverDefinedAuxPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfSymverDefinedAuxPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverDefinedAuxList*
SgAsmElfSymverDefinedAuxList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverDefinedAuxList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAuxList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAuxList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAuxList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAuxList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedAuxList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedAuxList::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedAuxList*)(SgAsmElfSymverDefinedAuxList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverDefinedAuxList*
SgAsmElfSymverDefinedAuxList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverDefinedAuxList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedAuxList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedAuxList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedAuxList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverDefinedAuxList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedAuxList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedAuxList::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedAuxList*)(SgAsmElfSymverDefinedAuxList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverDefinedAuxList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverDefinedAuxList::pools.begin(); block != SgAsmElfSymverDefinedAuxList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedAuxList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedAuxList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverDefinedAuxList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverDefinedAuxList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverDefinedAuxList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverDefinedAuxList::pools.begin(); block != SgAsmElfSymverDefinedAuxList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedAuxList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedAuxList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverDefinedAuxList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverDefinedAuxList::clearMemoryPool() {
    SgAsmElfSymverDefinedAuxList* tempPointer{nullptr};
    if (SgAsmElfSymverDefinedAuxList::pools.empty() == false) {
        auto block{SgAsmElfSymverDefinedAuxList::pools.begin()};
        SgAsmElfSymverDefinedAuxList::next_node = reinterpret_cast<SgAsmElfSymverDefinedAuxList*>(*block);
        for (; block != SgAsmElfSymverDefinedAuxList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedAuxList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverDefinedAuxList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverDefinedAuxList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverDefinedAuxList::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverDefinedAuxList::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverDefinedAuxList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverDefinedAuxList::next_node = nullptr;
    SgAsmElfSymverDefinedAuxList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverDefinedAuxList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAuxList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAuxList)};
    for (size_t blockIndex = SgAsmElfSymverDefinedAuxList::pools.size(); (blockIndex * SgAsmElfSymverDefinedAuxList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAuxList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAuxList) = %" PRIuPTR " SgAsmElfSymverDefinedAuxList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedAuxList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedAuxList),SgAsmElfSymverDefinedAuxList::pool_size);
        }
#endif

        SgAsmElfSymverDefinedAuxList* pointer{static_cast<SgAsmElfSymverDefinedAuxList*>(ROSE_MALLOC(SgAsmElfSymverDefinedAuxList::pool_size * sizeof(SgAsmElfSymverDefinedAuxList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverDefinedAuxList::pool_size * sizeof(SgAsmElfSymverDefinedAuxList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverDefinedAuxList::pool_size * sizeof(SgAsmElfSymverDefinedAuxList));
#endif
        SgAsmElfSymverDefinedAuxList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverDefinedAuxList::pool_size * sizeof(SgAsmElfSymverDefinedAuxList), V_SgAsmElfSymverDefinedAuxList));

        if (SgAsmElfSymverDefinedAuxList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverDefinedAuxList* blkptr{reinterpret_cast<SgAsmElfSymverDefinedAuxList*>(SgAsmElfSymverDefinedAuxList::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverDefinedAuxList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverDefinedAuxList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverDefinedAuxList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverDefinedAuxList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverDefinedAuxList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverDefinedAuxList::pool_size - 1};
    SgAsmElfSymverDefinedAuxList* testPointer{reinterpret_cast<SgAsmElfSymverDefinedAuxList*>(SgAsmElfSymverDefinedAuxList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverDefinedAuxList::pool_size * (SgAsmElfSymverDefinedAuxList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverDefinedAuxListStroageClass
 * from its counterpart of SgAsmElfSymverDefinedAuxList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverDefinedAuxList::initializeStorageClassArray(SgAsmElfSymverDefinedAuxListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverDefinedAuxList::pools.begin(); block != SgAsmElfSymverDefinedAuxList::pools.end(); ++block) {
        SgAsmElfSymverDefinedAuxList* pointer{reinterpret_cast<SgAsmElfSymverDefinedAuxList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedAuxList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 202951 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverDefinedEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverDefinedEntry::SgAsmElfSymverDefinedEntry(const SgAsmElfSymverDefinedEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 202961 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_version = storageSource.storageOf_version;
    p_flags = storageSource.storageOf_flags;
    p_index = storageSource.storageOf_index;
    p_hash = storageSource.storageOf_hash;
     p_entries = (SgAsmElfSymverDefinedAuxList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverDefinedEntry*
SgAsmElfSymverDefinedEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverDefinedEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedEntry::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedEntry*)(SgAsmElfSymverDefinedEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverDefinedEntry*
SgAsmElfSymverDefinedEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverDefinedEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverDefinedEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedEntry::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedEntry*)(SgAsmElfSymverDefinedEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverDefinedEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverDefinedEntry::pools.begin(); block != SgAsmElfSymverDefinedEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverDefinedEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverDefinedEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverDefinedEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverDefinedEntry::pools.begin(); block != SgAsmElfSymverDefinedEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverDefinedEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverDefinedEntry::clearMemoryPool() {
    SgAsmElfSymverDefinedEntry* tempPointer{nullptr};
    if (SgAsmElfSymverDefinedEntry::pools.empty() == false) {
        auto block{SgAsmElfSymverDefinedEntry::pools.begin()};
        SgAsmElfSymverDefinedEntry::next_node = reinterpret_cast<SgAsmElfSymverDefinedEntry*>(*block);
        for (; block != SgAsmElfSymverDefinedEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverDefinedEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverDefinedEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverDefinedEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverDefinedEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverDefinedEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverDefinedEntry::next_node = nullptr;
    SgAsmElfSymverDefinedEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverDefinedEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntry)};
    for (size_t blockIndex = SgAsmElfSymverDefinedEntry::pools.size(); (blockIndex * SgAsmElfSymverDefinedEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntry) = %" PRIuPTR " SgAsmElfSymverDefinedEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntry),SgAsmElfSymverDefinedEntry::pool_size);
        }
#endif

        SgAsmElfSymverDefinedEntry* pointer{static_cast<SgAsmElfSymverDefinedEntry*>(ROSE_MALLOC(SgAsmElfSymverDefinedEntry::pool_size * sizeof(SgAsmElfSymverDefinedEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverDefinedEntry::pool_size * sizeof(SgAsmElfSymverDefinedEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverDefinedEntry::pool_size * sizeof(SgAsmElfSymverDefinedEntry));
#endif
        SgAsmElfSymverDefinedEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverDefinedEntry::pool_size * sizeof(SgAsmElfSymverDefinedEntry), V_SgAsmElfSymverDefinedEntry));

        if (SgAsmElfSymverDefinedEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverDefinedEntry* blkptr{reinterpret_cast<SgAsmElfSymverDefinedEntry*>(SgAsmElfSymverDefinedEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverDefinedEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverDefinedEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverDefinedEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverDefinedEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverDefinedEntry::pool_size - 1};
    SgAsmElfSymverDefinedEntry* testPointer{reinterpret_cast<SgAsmElfSymverDefinedEntry*>(SgAsmElfSymverDefinedEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverDefinedEntry::pool_size * (SgAsmElfSymverDefinedEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverDefinedEntryStroageClass
 * from its counterpart of SgAsmElfSymverDefinedEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverDefinedEntry::initializeStorageClassArray(SgAsmElfSymverDefinedEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverDefinedEntry::pools.begin(); block != SgAsmElfSymverDefinedEntry::pools.end(); ++block) {
        SgAsmElfSymverDefinedEntry* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 203183 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverDefinedEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverDefinedEntryList::SgAsmElfSymverDefinedEntryList(const SgAsmElfSymverDefinedEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 203193 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfSymverDefinedEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfSymverDefinedEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverDefinedEntryList*
SgAsmElfSymverDefinedEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverDefinedEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedEntryList::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedEntryList*)(SgAsmElfSymverDefinedEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverDefinedEntryList*
SgAsmElfSymverDefinedEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverDefinedEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverDefinedEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedEntryList::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedEntryList*)(SgAsmElfSymverDefinedEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverDefinedEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverDefinedEntryList::pools.begin(); block != SgAsmElfSymverDefinedEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverDefinedEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverDefinedEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverDefinedEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverDefinedEntryList::pools.begin(); block != SgAsmElfSymverDefinedEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverDefinedEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverDefinedEntryList::clearMemoryPool() {
    SgAsmElfSymverDefinedEntryList* tempPointer{nullptr};
    if (SgAsmElfSymverDefinedEntryList::pools.empty() == false) {
        auto block{SgAsmElfSymverDefinedEntryList::pools.begin()};
        SgAsmElfSymverDefinedEntryList::next_node = reinterpret_cast<SgAsmElfSymverDefinedEntryList*>(*block);
        for (; block != SgAsmElfSymverDefinedEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverDefinedEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverDefinedEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverDefinedEntryList::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverDefinedEntryList::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverDefinedEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverDefinedEntryList::next_node = nullptr;
    SgAsmElfSymverDefinedEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverDefinedEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntryList)};
    for (size_t blockIndex = SgAsmElfSymverDefinedEntryList::pools.size(); (blockIndex * SgAsmElfSymverDefinedEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntryList) = %" PRIuPTR " SgAsmElfSymverDefinedEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedEntryList),SgAsmElfSymverDefinedEntryList::pool_size);
        }
#endif

        SgAsmElfSymverDefinedEntryList* pointer{static_cast<SgAsmElfSymverDefinedEntryList*>(ROSE_MALLOC(SgAsmElfSymverDefinedEntryList::pool_size * sizeof(SgAsmElfSymverDefinedEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverDefinedEntryList::pool_size * sizeof(SgAsmElfSymverDefinedEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverDefinedEntryList::pool_size * sizeof(SgAsmElfSymverDefinedEntryList));
#endif
        SgAsmElfSymverDefinedEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverDefinedEntryList::pool_size * sizeof(SgAsmElfSymverDefinedEntryList), V_SgAsmElfSymverDefinedEntryList));

        if (SgAsmElfSymverDefinedEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverDefinedEntryList* blkptr{reinterpret_cast<SgAsmElfSymverDefinedEntryList*>(SgAsmElfSymverDefinedEntryList::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverDefinedEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverDefinedEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverDefinedEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverDefinedEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverDefinedEntryList::pool_size - 1};
    SgAsmElfSymverDefinedEntryList* testPointer{reinterpret_cast<SgAsmElfSymverDefinedEntryList*>(SgAsmElfSymverDefinedEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverDefinedEntryList::pool_size * (SgAsmElfSymverDefinedEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverDefinedEntryListStroageClass
 * from its counterpart of SgAsmElfSymverDefinedEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverDefinedEntryList::initializeStorageClassArray(SgAsmElfSymverDefinedEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverDefinedEntryList::pools.begin(); block != SgAsmElfSymverDefinedEntryList::pools.end(); ++block) {
        SgAsmElfSymverDefinedEntryList* pointer{reinterpret_cast<SgAsmElfSymverDefinedEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 203415 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverEntry::SgAsmElfSymverEntry(const SgAsmElfSymverEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 203425 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_value = storageSource.storageOf_value;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverEntry*
SgAsmElfSymverEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverEntry::pool_size};
        returnPointer = &(((SgAsmElfSymverEntry*)(SgAsmElfSymverEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverEntry*
SgAsmElfSymverEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverEntry::pool_size};
        returnPointer = &(((SgAsmElfSymverEntry*)(SgAsmElfSymverEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverEntry::pools.begin(); block != SgAsmElfSymverEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverEntry::pools.begin(); block != SgAsmElfSymverEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverEntry::clearMemoryPool() {
    SgAsmElfSymverEntry* tempPointer{nullptr};
    if (SgAsmElfSymverEntry::pools.empty() == false) {
        auto block{SgAsmElfSymverEntry::pools.begin()};
        SgAsmElfSymverEntry::next_node = reinterpret_cast<SgAsmElfSymverEntry*>(*block);
        for (; block != SgAsmElfSymverEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverEntry::next_node = nullptr;
    SgAsmElfSymverEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverEntry)};
    for (size_t blockIndex = SgAsmElfSymverEntry::pools.size(); (blockIndex * SgAsmElfSymverEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverEntry) = %" PRIuPTR " SgAsmElfSymverEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverEntry),SgAsmElfSymverEntry::pool_size);
        }
#endif

        SgAsmElfSymverEntry* pointer{static_cast<SgAsmElfSymverEntry*>(ROSE_MALLOC(SgAsmElfSymverEntry::pool_size * sizeof(SgAsmElfSymverEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverEntry::pool_size * sizeof(SgAsmElfSymverEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverEntry::pool_size * sizeof(SgAsmElfSymverEntry));
#endif
        SgAsmElfSymverEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverEntry::pool_size * sizeof(SgAsmElfSymverEntry), V_SgAsmElfSymverEntry));

        if (SgAsmElfSymverEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverEntry* blkptr{reinterpret_cast<SgAsmElfSymverEntry*>(SgAsmElfSymverEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverEntry::pool_size - 1};
    SgAsmElfSymverEntry* testPointer{reinterpret_cast<SgAsmElfSymverEntry*>(SgAsmElfSymverEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverEntry::pool_size * (SgAsmElfSymverEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverEntryStroageClass
 * from its counterpart of SgAsmElfSymverEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverEntry::initializeStorageClassArray(SgAsmElfSymverEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverEntry::pools.begin(); block != SgAsmElfSymverEntry::pools.end(); ++block) {
        SgAsmElfSymverEntry* pointer{reinterpret_cast<SgAsmElfSymverEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 203643 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverEntryList::SgAsmElfSymverEntryList(const SgAsmElfSymverEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 203653 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfSymverEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfSymverEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverEntryList*
SgAsmElfSymverEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverEntryList::pool_size};
        returnPointer = &(((SgAsmElfSymverEntryList*)(SgAsmElfSymverEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverEntryList*
SgAsmElfSymverEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverEntryList::pool_size};
        returnPointer = &(((SgAsmElfSymverEntryList*)(SgAsmElfSymverEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverEntryList::pools.begin(); block != SgAsmElfSymverEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverEntryList::pools.begin(); block != SgAsmElfSymverEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverEntryList::clearMemoryPool() {
    SgAsmElfSymverEntryList* tempPointer{nullptr};
    if (SgAsmElfSymverEntryList::pools.empty() == false) {
        auto block{SgAsmElfSymverEntryList::pools.begin()};
        SgAsmElfSymverEntryList::next_node = reinterpret_cast<SgAsmElfSymverEntryList*>(*block);
        for (; block != SgAsmElfSymverEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverEntryList::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverEntryList::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverEntryList::next_node = nullptr;
    SgAsmElfSymverEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverEntryList)};
    for (size_t blockIndex = SgAsmElfSymverEntryList::pools.size(); (blockIndex * SgAsmElfSymverEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverEntryList) = %" PRIuPTR " SgAsmElfSymverEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverEntryList),SgAsmElfSymverEntryList::pool_size);
        }
#endif

        SgAsmElfSymverEntryList* pointer{static_cast<SgAsmElfSymverEntryList*>(ROSE_MALLOC(SgAsmElfSymverEntryList::pool_size * sizeof(SgAsmElfSymverEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverEntryList::pool_size * sizeof(SgAsmElfSymverEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverEntryList::pool_size * sizeof(SgAsmElfSymverEntryList));
#endif
        SgAsmElfSymverEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverEntryList::pool_size * sizeof(SgAsmElfSymverEntryList), V_SgAsmElfSymverEntryList));

        if (SgAsmElfSymverEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverEntryList* blkptr{reinterpret_cast<SgAsmElfSymverEntryList*>(SgAsmElfSymverEntryList::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverEntryList::pool_size - 1};
    SgAsmElfSymverEntryList* testPointer{reinterpret_cast<SgAsmElfSymverEntryList*>(SgAsmElfSymverEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverEntryList::pool_size * (SgAsmElfSymverEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverEntryListStroageClass
 * from its counterpart of SgAsmElfSymverEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverEntryList::initializeStorageClassArray(SgAsmElfSymverEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverEntryList::pools.begin(); block != SgAsmElfSymverEntryList::pools.end(); ++block) {
        SgAsmElfSymverEntryList* pointer{reinterpret_cast<SgAsmElfSymverEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 203875 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverNeededAux that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverNeededAux::SgAsmElfSymverNeededAux(const SgAsmElfSymverNeededAuxStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 203885 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_hash = storageSource.storageOf_hash;
    p_flags = storageSource.storageOf_flags;
    p_other = storageSource.storageOf_other;
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverNeededAux*
SgAsmElfSymverNeededAux::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverNeededAux* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededAux)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededAux + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededAux)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAux)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededAux::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededAux::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededAux*)(SgAsmElfSymverNeededAux::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverNeededAux*
SgAsmElfSymverNeededAux::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverNeededAux* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededAux)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededAux + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededAux)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverNeededAux)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededAux::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededAux::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededAux*)(SgAsmElfSymverNeededAux::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverNeededAux::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverNeededAux::pools.begin(); block != SgAsmElfSymverNeededAux::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededAux*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededAux::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverNeededAux*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverNeededAux::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverNeededAux* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverNeededAux::pools.begin(); block != SgAsmElfSymverNeededAux::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededAux*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededAux::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverNeededAux::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverNeededAux::clearMemoryPool() {
    SgAsmElfSymverNeededAux* tempPointer{nullptr};
    if (SgAsmElfSymverNeededAux::pools.empty() == false) {
        auto block{SgAsmElfSymverNeededAux::pools.begin()};
        SgAsmElfSymverNeededAux::next_node = reinterpret_cast<SgAsmElfSymverNeededAux*>(*block);
        for (; block != SgAsmElfSymverNeededAux::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverNeededAux*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverNeededAux::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverNeededAux::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverNeededAux::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverNeededAux::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverNeededAux::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverNeededAux::next_node = nullptr;
    SgAsmElfSymverNeededAux::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverNeededAux::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAux) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededAux)};
    for (size_t blockIndex = SgAsmElfSymverNeededAux::pools.size(); (blockIndex * SgAsmElfSymverNeededAux::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAux) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededAux) = %" PRIuPTR " SgAsmElfSymverNeededAux::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAux),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededAux),SgAsmElfSymverNeededAux::pool_size);
        }
#endif

        SgAsmElfSymverNeededAux* pointer{static_cast<SgAsmElfSymverNeededAux*>(ROSE_MALLOC(SgAsmElfSymverNeededAux::pool_size * sizeof(SgAsmElfSymverNeededAux)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverNeededAux::pool_size * sizeof(SgAsmElfSymverNeededAux));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverNeededAux::pool_size * sizeof(SgAsmElfSymverNeededAux));
#endif
        SgAsmElfSymverNeededAux::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverNeededAux::pool_size * sizeof(SgAsmElfSymverNeededAux), V_SgAsmElfSymverNeededAux));

        if (SgAsmElfSymverNeededAux::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverNeededAux* blkptr{reinterpret_cast<SgAsmElfSymverNeededAux*>(SgAsmElfSymverNeededAux::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverNeededAux::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverNeededAux::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverNeededAux::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverNeededAux::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverNeededAux::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverNeededAux::pool_size - 1};
    SgAsmElfSymverNeededAux* testPointer{reinterpret_cast<SgAsmElfSymverNeededAux*>(SgAsmElfSymverNeededAux::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverNeededAux::pool_size * (SgAsmElfSymverNeededAux::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverNeededAuxStroageClass
 * from its counterpart of SgAsmElfSymverNeededAux. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverNeededAux::initializeStorageClassArray(SgAsmElfSymverNeededAuxStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverNeededAux::pools.begin(); block != SgAsmElfSymverNeededAux::pools.end(); ++block) {
        SgAsmElfSymverNeededAux* pointer{reinterpret_cast<SgAsmElfSymverNeededAux*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededAux::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 204106 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverNeededAuxList that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverNeededAuxList::SgAsmElfSymverNeededAuxList(const SgAsmElfSymverNeededAuxListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 204116 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfSymverNeededAuxPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfSymverNeededAuxPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverNeededAuxList*
SgAsmElfSymverNeededAuxList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverNeededAuxList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededAuxList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededAuxList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededAuxList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAuxList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededAuxList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededAuxList::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededAuxList*)(SgAsmElfSymverNeededAuxList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverNeededAuxList*
SgAsmElfSymverNeededAuxList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverNeededAuxList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededAuxList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededAuxList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededAuxList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverNeededAuxList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededAuxList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededAuxList::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededAuxList*)(SgAsmElfSymverNeededAuxList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverNeededAuxList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverNeededAuxList::pools.begin(); block != SgAsmElfSymverNeededAuxList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededAuxList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededAuxList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverNeededAuxList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverNeededAuxList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverNeededAuxList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverNeededAuxList::pools.begin(); block != SgAsmElfSymverNeededAuxList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededAuxList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededAuxList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverNeededAuxList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverNeededAuxList::clearMemoryPool() {
    SgAsmElfSymverNeededAuxList* tempPointer{nullptr};
    if (SgAsmElfSymverNeededAuxList::pools.empty() == false) {
        auto block{SgAsmElfSymverNeededAuxList::pools.begin()};
        SgAsmElfSymverNeededAuxList::next_node = reinterpret_cast<SgAsmElfSymverNeededAuxList*>(*block);
        for (; block != SgAsmElfSymverNeededAuxList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverNeededAuxList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverNeededAuxList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverNeededAuxList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverNeededAuxList::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverNeededAuxList::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverNeededAuxList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverNeededAuxList::next_node = nullptr;
    SgAsmElfSymverNeededAuxList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverNeededAuxList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAuxList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededAuxList)};
    for (size_t blockIndex = SgAsmElfSymverNeededAuxList::pools.size(); (blockIndex * SgAsmElfSymverNeededAuxList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAuxList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededAuxList) = %" PRIuPTR " SgAsmElfSymverNeededAuxList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededAuxList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededAuxList),SgAsmElfSymverNeededAuxList::pool_size);
        }
#endif

        SgAsmElfSymverNeededAuxList* pointer{static_cast<SgAsmElfSymverNeededAuxList*>(ROSE_MALLOC(SgAsmElfSymverNeededAuxList::pool_size * sizeof(SgAsmElfSymverNeededAuxList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverNeededAuxList::pool_size * sizeof(SgAsmElfSymverNeededAuxList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverNeededAuxList::pool_size * sizeof(SgAsmElfSymverNeededAuxList));
#endif
        SgAsmElfSymverNeededAuxList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverNeededAuxList::pool_size * sizeof(SgAsmElfSymverNeededAuxList), V_SgAsmElfSymverNeededAuxList));

        if (SgAsmElfSymverNeededAuxList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverNeededAuxList* blkptr{reinterpret_cast<SgAsmElfSymverNeededAuxList*>(SgAsmElfSymverNeededAuxList::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverNeededAuxList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverNeededAuxList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverNeededAuxList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverNeededAuxList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverNeededAuxList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverNeededAuxList::pool_size - 1};
    SgAsmElfSymverNeededAuxList* testPointer{reinterpret_cast<SgAsmElfSymverNeededAuxList*>(SgAsmElfSymverNeededAuxList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverNeededAuxList::pool_size * (SgAsmElfSymverNeededAuxList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverNeededAuxListStroageClass
 * from its counterpart of SgAsmElfSymverNeededAuxList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverNeededAuxList::initializeStorageClassArray(SgAsmElfSymverNeededAuxListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverNeededAuxList::pools.begin(); block != SgAsmElfSymverNeededAuxList::pools.end(); ++block) {
        SgAsmElfSymverNeededAuxList* pointer{reinterpret_cast<SgAsmElfSymverNeededAuxList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededAuxList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 204338 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverNeededEntry that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverNeededEntry::SgAsmElfSymverNeededEntry(const SgAsmElfSymverNeededEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 204348 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_version = storageSource.storageOf_version;
     p_fileName = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_fileName));
     p_entries = (SgAsmElfSymverNeededAuxList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverNeededEntry*
SgAsmElfSymverNeededEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverNeededEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededEntry::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededEntry*)(SgAsmElfSymverNeededEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverNeededEntry*
SgAsmElfSymverNeededEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverNeededEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverNeededEntry)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededEntry::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededEntry*)(SgAsmElfSymverNeededEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverNeededEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverNeededEntry::pools.begin(); block != SgAsmElfSymverNeededEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverNeededEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverNeededEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverNeededEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverNeededEntry::pools.begin(); block != SgAsmElfSymverNeededEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverNeededEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverNeededEntry::clearMemoryPool() {
    SgAsmElfSymverNeededEntry* tempPointer{nullptr};
    if (SgAsmElfSymverNeededEntry::pools.empty() == false) {
        auto block{SgAsmElfSymverNeededEntry::pools.begin()};
        SgAsmElfSymverNeededEntry::next_node = reinterpret_cast<SgAsmElfSymverNeededEntry*>(*block);
        for (; block != SgAsmElfSymverNeededEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverNeededEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverNeededEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverNeededEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverNeededEntry::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverNeededEntry::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverNeededEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverNeededEntry::next_node = nullptr;
    SgAsmElfSymverNeededEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverNeededEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntry)};
    for (size_t blockIndex = SgAsmElfSymverNeededEntry::pools.size(); (blockIndex * SgAsmElfSymverNeededEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntry) = %" PRIuPTR " SgAsmElfSymverNeededEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntry),SgAsmElfSymverNeededEntry::pool_size);
        }
#endif

        SgAsmElfSymverNeededEntry* pointer{static_cast<SgAsmElfSymverNeededEntry*>(ROSE_MALLOC(SgAsmElfSymverNeededEntry::pool_size * sizeof(SgAsmElfSymverNeededEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverNeededEntry::pool_size * sizeof(SgAsmElfSymverNeededEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverNeededEntry::pool_size * sizeof(SgAsmElfSymverNeededEntry));
#endif
        SgAsmElfSymverNeededEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverNeededEntry::pool_size * sizeof(SgAsmElfSymverNeededEntry), V_SgAsmElfSymverNeededEntry));

        if (SgAsmElfSymverNeededEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverNeededEntry* blkptr{reinterpret_cast<SgAsmElfSymverNeededEntry*>(SgAsmElfSymverNeededEntry::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverNeededEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverNeededEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverNeededEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverNeededEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverNeededEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverNeededEntry::pool_size - 1};
    SgAsmElfSymverNeededEntry* testPointer{reinterpret_cast<SgAsmElfSymverNeededEntry*>(SgAsmElfSymverNeededEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverNeededEntry::pool_size * (SgAsmElfSymverNeededEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverNeededEntryStroageClass
 * from its counterpart of SgAsmElfSymverNeededEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverNeededEntry::initializeStorageClassArray(SgAsmElfSymverNeededEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverNeededEntry::pools.begin(); block != SgAsmElfSymverNeededEntry::pools.end(); ++block) {
        SgAsmElfSymverNeededEntry* pointer{reinterpret_cast<SgAsmElfSymverNeededEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 204568 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverNeededEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverNeededEntryList::SgAsmElfSymverNeededEntryList(const SgAsmElfSymverNeededEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 204578 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmElfSymverNeededEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmElfSymverNeededEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverNeededEntryList*
SgAsmElfSymverNeededEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverNeededEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededEntryList::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededEntryList*)(SgAsmElfSymverNeededEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverNeededEntryList*
SgAsmElfSymverNeededEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverNeededEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverNeededEntryList)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededEntryList::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededEntryList*)(SgAsmElfSymverNeededEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverNeededEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverNeededEntryList::pools.begin(); block != SgAsmElfSymverNeededEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverNeededEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverNeededEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverNeededEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverNeededEntryList::pools.begin(); block != SgAsmElfSymverNeededEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverNeededEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverNeededEntryList::clearMemoryPool() {
    SgAsmElfSymverNeededEntryList* tempPointer{nullptr};
    if (SgAsmElfSymverNeededEntryList::pools.empty() == false) {
        auto block{SgAsmElfSymverNeededEntryList::pools.begin()};
        SgAsmElfSymverNeededEntryList::next_node = reinterpret_cast<SgAsmElfSymverNeededEntryList*>(*block);
        for (; block != SgAsmElfSymverNeededEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverNeededEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverNeededEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverNeededEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverNeededEntryList::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverNeededEntryList::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverNeededEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverNeededEntryList::next_node = nullptr;
    SgAsmElfSymverNeededEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverNeededEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntryList)};
    for (size_t blockIndex = SgAsmElfSymverNeededEntryList::pools.size(); (blockIndex * SgAsmElfSymverNeededEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntryList) = %" PRIuPTR " SgAsmElfSymverNeededEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededEntryList),SgAsmElfSymverNeededEntryList::pool_size);
        }
#endif

        SgAsmElfSymverNeededEntryList* pointer{static_cast<SgAsmElfSymverNeededEntryList*>(ROSE_MALLOC(SgAsmElfSymverNeededEntryList::pool_size * sizeof(SgAsmElfSymverNeededEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverNeededEntryList::pool_size * sizeof(SgAsmElfSymverNeededEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverNeededEntryList::pool_size * sizeof(SgAsmElfSymverNeededEntryList));
#endif
        SgAsmElfSymverNeededEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverNeededEntryList::pool_size * sizeof(SgAsmElfSymverNeededEntryList), V_SgAsmElfSymverNeededEntryList));

        if (SgAsmElfSymverNeededEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverNeededEntryList* blkptr{reinterpret_cast<SgAsmElfSymverNeededEntryList*>(SgAsmElfSymverNeededEntryList::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverNeededEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverNeededEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverNeededEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverNeededEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverNeededEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverNeededEntryList::pool_size - 1};
    SgAsmElfSymverNeededEntryList* testPointer{reinterpret_cast<SgAsmElfSymverNeededEntryList*>(SgAsmElfSymverNeededEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverNeededEntryList::pool_size * (SgAsmElfSymverNeededEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverNeededEntryListStroageClass
 * from its counterpart of SgAsmElfSymverNeededEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverNeededEntryList::initializeStorageClassArray(SgAsmElfSymverNeededEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverNeededEntryList::pools.begin(); block != SgAsmElfSymverNeededEntryList::pools.end(); ++block) {
        SgAsmElfSymverNeededEntryList* pointer{reinterpret_cast<SgAsmElfSymverNeededEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 204800 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericDLL that takes its corresponding StorageClass as parameter
 */
SgAsmGenericDLL::SgAsmGenericDLL(const SgAsmGenericDLLStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 204810 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));
    p_symbols = storageSource.storageOf_symbols.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericDLL*
SgAsmGenericDLL::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericDLL* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericDLL)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericDLL + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericDLL)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLL)};
        unsigned long positionInPool{localIndex % SgAsmGenericDLL::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericDLL::pool_size};
        returnPointer = &(((SgAsmGenericDLL*)(SgAsmGenericDLL::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericDLL*
SgAsmGenericDLL::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericDLL* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericDLL)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericDLL + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericDLL)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericDLL)};
        unsigned long positionInPool{localIndex % SgAsmGenericDLL::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericDLL::pool_size};
        returnPointer = &(((SgAsmGenericDLL*)(SgAsmGenericDLL::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericDLL::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericDLL::pools.begin(); block != SgAsmGenericDLL::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericDLL*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericDLL::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericDLL*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericDLL::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericDLL* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericDLL::pools.begin(); block != SgAsmGenericDLL::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericDLL*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericDLL::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericDLL::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericDLL::clearMemoryPool() {
    SgAsmGenericDLL* tempPointer{nullptr};
    if (SgAsmGenericDLL::pools.empty() == false) {
        auto block{SgAsmGenericDLL::pools.begin()};
        SgAsmGenericDLL::next_node = reinterpret_cast<SgAsmGenericDLL*>(*block);
        for (; block != SgAsmGenericDLL::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericDLL*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericDLL::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericDLL::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericDLL::pool_size-1]);
        }
    }
}

void
SgAsmGenericDLL::deleteMemoryPool() {
    for (auto p: SgAsmGenericDLL::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericDLL::next_node = nullptr;
    SgAsmGenericDLL::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericDLL::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLL) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericDLL)};
    for (size_t blockIndex = SgAsmGenericDLL::pools.size(); (blockIndex * SgAsmGenericDLL::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLL) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericDLL) = %" PRIuPTR " SgAsmGenericDLL::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLL),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericDLL),SgAsmGenericDLL::pool_size);
        }
#endif

        SgAsmGenericDLL* pointer{static_cast<SgAsmGenericDLL*>(ROSE_MALLOC(SgAsmGenericDLL::pool_size * sizeof(SgAsmGenericDLL)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericDLL::pool_size * sizeof(SgAsmGenericDLL));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericDLL::pool_size * sizeof(SgAsmGenericDLL));
#endif
        SgAsmGenericDLL::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericDLL::pool_size * sizeof(SgAsmGenericDLL), V_SgAsmGenericDLL));

        if (SgAsmGenericDLL::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericDLL* blkptr{reinterpret_cast<SgAsmGenericDLL*>(SgAsmGenericDLL::pools[blockIndex-1])};
                blkptr[SgAsmGenericDLL::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericDLL::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericDLL::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericDLL::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericDLL::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericDLL::pool_size - 1};
    SgAsmGenericDLL* testPointer{reinterpret_cast<SgAsmGenericDLL*>(SgAsmGenericDLL::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericDLL::pool_size * (SgAsmGenericDLL::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericDLLStroageClass
 * from its counterpart of SgAsmGenericDLL. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericDLL::initializeStorageClassArray(SgAsmGenericDLLStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericDLL::pools.begin(); block != SgAsmGenericDLL::pools.end(); ++block) {
        SgAsmGenericDLL* pointer{reinterpret_cast<SgAsmGenericDLL*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericDLL::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 205029 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericDLLList that takes its corresponding StorageClass as parameter
 */
SgAsmGenericDLLList::SgAsmGenericDLLList(const SgAsmGenericDLLListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 205039 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_dlls = storageSource.storageOf_dlls.rebuildDataStoredInEasyStorageClass();
    SgAsmGenericDLLPtrList::iterator i_dlls = p_dlls.begin();
    for ( ; i_dlls != p_dlls.end(); ++i_dlls) {
        (*i_dlls) = (SgAsmGenericDLLPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_dlls)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericDLLList*
SgAsmGenericDLLList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericDLLList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericDLLList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericDLLList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericDLLList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLLList)};
        unsigned long positionInPool{localIndex % SgAsmGenericDLLList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericDLLList::pool_size};
        returnPointer = &(((SgAsmGenericDLLList*)(SgAsmGenericDLLList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericDLLList*
SgAsmGenericDLLList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericDLLList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericDLLList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericDLLList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericDLLList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericDLLList)};
        unsigned long positionInPool{localIndex % SgAsmGenericDLLList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericDLLList::pool_size};
        returnPointer = &(((SgAsmGenericDLLList*)(SgAsmGenericDLLList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericDLLList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericDLLList::pools.begin(); block != SgAsmGenericDLLList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericDLLList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericDLLList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericDLLList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericDLLList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericDLLList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericDLLList::pools.begin(); block != SgAsmGenericDLLList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericDLLList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericDLLList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericDLLList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericDLLList::clearMemoryPool() {
    SgAsmGenericDLLList* tempPointer{nullptr};
    if (SgAsmGenericDLLList::pools.empty() == false) {
        auto block{SgAsmGenericDLLList::pools.begin()};
        SgAsmGenericDLLList::next_node = reinterpret_cast<SgAsmGenericDLLList*>(*block);
        for (; block != SgAsmGenericDLLList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericDLLList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericDLLList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericDLLList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericDLLList::pool_size-1]);
        }
    }
}

void
SgAsmGenericDLLList::deleteMemoryPool() {
    for (auto p: SgAsmGenericDLLList::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericDLLList::next_node = nullptr;
    SgAsmGenericDLLList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericDLLList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLLList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericDLLList)};
    for (size_t blockIndex = SgAsmGenericDLLList::pools.size(); (blockIndex * SgAsmGenericDLLList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLLList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericDLLList) = %" PRIuPTR " SgAsmGenericDLLList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericDLLList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericDLLList),SgAsmGenericDLLList::pool_size);
        }
#endif

        SgAsmGenericDLLList* pointer{static_cast<SgAsmGenericDLLList*>(ROSE_MALLOC(SgAsmGenericDLLList::pool_size * sizeof(SgAsmGenericDLLList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericDLLList::pool_size * sizeof(SgAsmGenericDLLList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericDLLList::pool_size * sizeof(SgAsmGenericDLLList));
#endif
        SgAsmGenericDLLList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericDLLList::pool_size * sizeof(SgAsmGenericDLLList), V_SgAsmGenericDLLList));

        if (SgAsmGenericDLLList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericDLLList* blkptr{reinterpret_cast<SgAsmGenericDLLList*>(SgAsmGenericDLLList::pools[blockIndex-1])};
                blkptr[SgAsmGenericDLLList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericDLLList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericDLLList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericDLLList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericDLLList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericDLLList::pool_size - 1};
    SgAsmGenericDLLList* testPointer{reinterpret_cast<SgAsmGenericDLLList*>(SgAsmGenericDLLList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericDLLList::pool_size * (SgAsmGenericDLLList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericDLLListStroageClass
 * from its counterpart of SgAsmGenericDLLList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericDLLList::initializeStorageClassArray(SgAsmGenericDLLListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericDLLList::pools.begin(); block != SgAsmGenericDLLList::pools.end(); ++block) {
        SgAsmGenericDLLList* pointer{reinterpret_cast<SgAsmGenericDLLList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericDLLList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 205261 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericFile that takes its corresponding StorageClass as parameter
 */
SgAsmGenericFile::SgAsmGenericFile(const SgAsmGenericFileStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 205271 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_dwarfInfo = (SgAsmDwarfCompilationUnitList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dwarfInfo));
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
    p_fd = storageSource.storageOf_fd;
    p_sb = storageSource.storageOf_sb;
    p_data = storageSource.storageOf_data.rebuildDataStoredInEasyStorageClass();
     p_headers = (SgAsmGenericHeaderList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_headers));
     p_holes = (SgAsmGenericSectionList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_holes));
    p_truncateZeros = storageSource.storageOf_truncateZeros;
    p_trackingReferences = storageSource.storageOf_trackingReferences;
    p_referencedExtents = storageSource.storageOf_referencedExtents.rebuildDataStoredInEasyStorageClass();
    p_neuter = storageSource.storageOf_neuter;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericFile*
SgAsmGenericFile::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFile)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFile)};
        unsigned long positionInPool{localIndex % SgAsmGenericFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericFile::pool_size};
        returnPointer = &(((SgAsmGenericFile*)(SgAsmGenericFile::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericFile*
SgAsmGenericFile::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericFile* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFile)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFile + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFile)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericFile)};
        unsigned long positionInPool{localIndex % SgAsmGenericFile::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericFile::pool_size};
        returnPointer = &(((SgAsmGenericFile*)(SgAsmGenericFile::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericFile::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericFile::pools.begin(); block != SgAsmGenericFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericFile*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFile::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericFile*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericFile::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericFile* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericFile::pools.begin(); block != SgAsmGenericFile::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericFile*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFile::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericFile::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericFile::clearMemoryPool() {
    SgAsmGenericFile* tempPointer{nullptr};
    if (SgAsmGenericFile::pools.empty() == false) {
        auto block{SgAsmGenericFile::pools.begin()};
        SgAsmGenericFile::next_node = reinterpret_cast<SgAsmGenericFile*>(*block);
        for (; block != SgAsmGenericFile::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericFile*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericFile::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericFile::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericFile::pool_size-1]);
        }
    }
}

void
SgAsmGenericFile::deleteMemoryPool() {
    for (auto p: SgAsmGenericFile::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericFile::next_node = nullptr;
    SgAsmGenericFile::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericFile::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFile) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFile)};
    for (size_t blockIndex = SgAsmGenericFile::pools.size(); (blockIndex * SgAsmGenericFile::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFile) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFile) = %" PRIuPTR " SgAsmGenericFile::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFile),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFile),SgAsmGenericFile::pool_size);
        }
#endif

        SgAsmGenericFile* pointer{static_cast<SgAsmGenericFile*>(ROSE_MALLOC(SgAsmGenericFile::pool_size * sizeof(SgAsmGenericFile)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericFile::pool_size * sizeof(SgAsmGenericFile));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericFile::pool_size * sizeof(SgAsmGenericFile));
#endif
        SgAsmGenericFile::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericFile::pool_size * sizeof(SgAsmGenericFile), V_SgAsmGenericFile));

        if (SgAsmGenericFile::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericFile* blkptr{reinterpret_cast<SgAsmGenericFile*>(SgAsmGenericFile::pools[blockIndex-1])};
                blkptr[SgAsmGenericFile::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericFile::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericFile::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericFile::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericFile::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericFile::pool_size - 1};
    SgAsmGenericFile* testPointer{reinterpret_cast<SgAsmGenericFile*>(SgAsmGenericFile::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericFile::pool_size * (SgAsmGenericFile::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericFileStroageClass
 * from its counterpart of SgAsmGenericFile. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericFile::initializeStorageClassArray(SgAsmGenericFileStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericFile::pools.begin(); block != SgAsmGenericFile::pools.end(); ++block) {
        SgAsmGenericFile* pointer{reinterpret_cast<SgAsmGenericFile*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFile::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 205499 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericFormat that takes its corresponding StorageClass as parameter
 */
SgAsmGenericFormat::SgAsmGenericFormat(const SgAsmGenericFormatStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 205509 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_family = storageSource.storageOf_family;
    p_purpose = storageSource.storageOf_purpose;
    p_sex = storageSource.storageOf_sex;
    p_version = storageSource.storageOf_version;
    p_isCurrentVersion = storageSource.storageOf_isCurrentVersion;
    p_abi = storageSource.storageOf_abi;
    p_abiVersion = storageSource.storageOf_abiVersion;
    p_wordSize = storageSource.storageOf_wordSize;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericFormat*
SgAsmGenericFormat::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericFormat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFormat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFormat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFormat)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFormat)};
        unsigned long positionInPool{localIndex % SgAsmGenericFormat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericFormat::pool_size};
        returnPointer = &(((SgAsmGenericFormat*)(SgAsmGenericFormat::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericFormat*
SgAsmGenericFormat::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericFormat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFormat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFormat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFormat)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericFormat)};
        unsigned long positionInPool{localIndex % SgAsmGenericFormat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericFormat::pool_size};
        returnPointer = &(((SgAsmGenericFormat*)(SgAsmGenericFormat::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericFormat::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericFormat::pools.begin(); block != SgAsmGenericFormat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericFormat*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFormat::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericFormat*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericFormat::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericFormat* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericFormat::pools.begin(); block != SgAsmGenericFormat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericFormat*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFormat::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericFormat::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericFormat::clearMemoryPool() {
    SgAsmGenericFormat* tempPointer{nullptr};
    if (SgAsmGenericFormat::pools.empty() == false) {
        auto block{SgAsmGenericFormat::pools.begin()};
        SgAsmGenericFormat::next_node = reinterpret_cast<SgAsmGenericFormat*>(*block);
        for (; block != SgAsmGenericFormat::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericFormat*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericFormat::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericFormat::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericFormat::pool_size-1]);
        }
    }
}

void
SgAsmGenericFormat::deleteMemoryPool() {
    for (auto p: SgAsmGenericFormat::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericFormat::next_node = nullptr;
    SgAsmGenericFormat::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericFormat::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFormat) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFormat)};
    for (size_t blockIndex = SgAsmGenericFormat::pools.size(); (blockIndex * SgAsmGenericFormat::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFormat) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFormat) = %" PRIuPTR " SgAsmGenericFormat::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFormat),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFormat),SgAsmGenericFormat::pool_size);
        }
#endif

        SgAsmGenericFormat* pointer{static_cast<SgAsmGenericFormat*>(ROSE_MALLOC(SgAsmGenericFormat::pool_size * sizeof(SgAsmGenericFormat)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericFormat::pool_size * sizeof(SgAsmGenericFormat));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericFormat::pool_size * sizeof(SgAsmGenericFormat));
#endif
        SgAsmGenericFormat::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericFormat::pool_size * sizeof(SgAsmGenericFormat), V_SgAsmGenericFormat));

        if (SgAsmGenericFormat::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericFormat* blkptr{reinterpret_cast<SgAsmGenericFormat*>(SgAsmGenericFormat::pools[blockIndex-1])};
                blkptr[SgAsmGenericFormat::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericFormat::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericFormat::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericFormat::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericFormat::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericFormat::pool_size - 1};
    SgAsmGenericFormat* testPointer{reinterpret_cast<SgAsmGenericFormat*>(SgAsmGenericFormat::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericFormat::pool_size * (SgAsmGenericFormat::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericFormatStroageClass
 * from its counterpart of SgAsmGenericFormat. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericFormat::initializeStorageClassArray(SgAsmGenericFormatStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericFormat::pools.begin(); block != SgAsmGenericFormat::pools.end(); ++block) {
        SgAsmGenericFormat* pointer{reinterpret_cast<SgAsmGenericFormat*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFormat::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 205734 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericHeaderList that takes its corresponding StorageClass as parameter
 */
SgAsmGenericHeaderList::SgAsmGenericHeaderList(const SgAsmGenericHeaderListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 205744 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_headers = storageSource.storageOf_headers.rebuildDataStoredInEasyStorageClass();
    SgAsmGenericHeaderPtrList::iterator i_headers = p_headers.begin();
    for ( ; i_headers != p_headers.end(); ++i_headers) {
        (*i_headers) = (SgAsmGenericHeaderPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_headers)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericHeaderList*
SgAsmGenericHeaderList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericHeaderList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericHeaderList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericHeaderList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericHeaderList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeaderList)};
        unsigned long positionInPool{localIndex % SgAsmGenericHeaderList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericHeaderList::pool_size};
        returnPointer = &(((SgAsmGenericHeaderList*)(SgAsmGenericHeaderList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericHeaderList*
SgAsmGenericHeaderList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericHeaderList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericHeaderList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericHeaderList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericHeaderList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericHeaderList)};
        unsigned long positionInPool{localIndex % SgAsmGenericHeaderList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericHeaderList::pool_size};
        returnPointer = &(((SgAsmGenericHeaderList*)(SgAsmGenericHeaderList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericHeaderList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericHeaderList::pools.begin(); block != SgAsmGenericHeaderList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericHeaderList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericHeaderList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericHeaderList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericHeaderList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericHeaderList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericHeaderList::pools.begin(); block != SgAsmGenericHeaderList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericHeaderList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericHeaderList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericHeaderList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericHeaderList::clearMemoryPool() {
    SgAsmGenericHeaderList* tempPointer{nullptr};
    if (SgAsmGenericHeaderList::pools.empty() == false) {
        auto block{SgAsmGenericHeaderList::pools.begin()};
        SgAsmGenericHeaderList::next_node = reinterpret_cast<SgAsmGenericHeaderList*>(*block);
        for (; block != SgAsmGenericHeaderList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericHeaderList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericHeaderList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericHeaderList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericHeaderList::pool_size-1]);
        }
    }
}

void
SgAsmGenericHeaderList::deleteMemoryPool() {
    for (auto p: SgAsmGenericHeaderList::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericHeaderList::next_node = nullptr;
    SgAsmGenericHeaderList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericHeaderList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeaderList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericHeaderList)};
    for (size_t blockIndex = SgAsmGenericHeaderList::pools.size(); (blockIndex * SgAsmGenericHeaderList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeaderList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericHeaderList) = %" PRIuPTR " SgAsmGenericHeaderList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeaderList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericHeaderList),SgAsmGenericHeaderList::pool_size);
        }
#endif

        SgAsmGenericHeaderList* pointer{static_cast<SgAsmGenericHeaderList*>(ROSE_MALLOC(SgAsmGenericHeaderList::pool_size * sizeof(SgAsmGenericHeaderList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericHeaderList::pool_size * sizeof(SgAsmGenericHeaderList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericHeaderList::pool_size * sizeof(SgAsmGenericHeaderList));
#endif
        SgAsmGenericHeaderList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericHeaderList::pool_size * sizeof(SgAsmGenericHeaderList), V_SgAsmGenericHeaderList));

        if (SgAsmGenericHeaderList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericHeaderList* blkptr{reinterpret_cast<SgAsmGenericHeaderList*>(SgAsmGenericHeaderList::pools[blockIndex-1])};
                blkptr[SgAsmGenericHeaderList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericHeaderList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericHeaderList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericHeaderList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericHeaderList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericHeaderList::pool_size - 1};
    SgAsmGenericHeaderList* testPointer{reinterpret_cast<SgAsmGenericHeaderList*>(SgAsmGenericHeaderList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericHeaderList::pool_size * (SgAsmGenericHeaderList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericHeaderListStroageClass
 * from its counterpart of SgAsmGenericHeaderList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericHeaderList::initializeStorageClassArray(SgAsmGenericHeaderListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericHeaderList::pools.begin(); block != SgAsmGenericHeaderList::pools.end(); ++block) {
        SgAsmGenericHeaderList* pointer{reinterpret_cast<SgAsmGenericHeaderList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericHeaderList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 205966 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericSection that takes its corresponding StorageClass as parameter
 */
SgAsmGenericSection::SgAsmGenericSection(const SgAsmGenericSectionStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 205976 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_file = (SgAsmGenericFile*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_file));
     p_header = (SgAsmGenericHeader*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_header));
    p_size = storageSource.storageOf_size;
    p_offset = storageSource.storageOf_offset;
    p_fileAlignment = storageSource.storageOf_fileAlignment;
    p_data = storageSource.storageOf_data.rebuildDataStoredInEasyStorageClass();
    p_purpose = storageSource.storageOf_purpose;
    p_synthesized = storageSource.storageOf_synthesized;
    p_id = storageSource.storageOf_id;
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));
    p_shortName = storageSource.storageOf_shortName.rebuildDataStoredInEasyStorageClass();
    p_mappedPreferredRva = storageSource.storageOf_mappedPreferredRva;
    p_mappedSize = storageSource.storageOf_mappedSize;
    p_mappedAlignment = storageSource.storageOf_mappedAlignment;
    p_mappedReadPermission = storageSource.storageOf_mappedReadPermission;
    p_mappedWritePermission = storageSource.storageOf_mappedWritePermission;
    p_mappedExecutePermission = storageSource.storageOf_mappedExecutePermission;
    p_containsCode = storageSource.storageOf_containsCode;
    p_mappedActualVa = storageSource.storageOf_mappedActualVa;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericSection*
SgAsmGenericSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSection)};
        unsigned long positionInPool{localIndex % SgAsmGenericSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSection::pool_size};
        returnPointer = &(((SgAsmGenericSection*)(SgAsmGenericSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericSection*
SgAsmGenericSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericSection)};
        unsigned long positionInPool{localIndex % SgAsmGenericSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSection::pool_size};
        returnPointer = &(((SgAsmGenericSection*)(SgAsmGenericSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericSection::pools.begin(); block != SgAsmGenericSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSection*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericSection::pools.begin(); block != SgAsmGenericSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSection*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericSection::clearMemoryPool() {
    SgAsmGenericSection* tempPointer{nullptr};
    if (SgAsmGenericSection::pools.empty() == false) {
        auto block{SgAsmGenericSection::pools.begin()};
        SgAsmGenericSection::next_node = reinterpret_cast<SgAsmGenericSection*>(*block);
        for (; block != SgAsmGenericSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericSection::pool_size-1]);
        }
    }
}

void
SgAsmGenericSection::deleteMemoryPool() {
    for (auto p: SgAsmGenericSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericSection::next_node = nullptr;
    SgAsmGenericSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSection)};
    for (size_t blockIndex = SgAsmGenericSection::pools.size(); (blockIndex * SgAsmGenericSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSection) = %" PRIuPTR " SgAsmGenericSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSection),SgAsmGenericSection::pool_size);
        }
#endif

        SgAsmGenericSection* pointer{static_cast<SgAsmGenericSection*>(ROSE_MALLOC(SgAsmGenericSection::pool_size * sizeof(SgAsmGenericSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericSection::pool_size * sizeof(SgAsmGenericSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericSection::pool_size * sizeof(SgAsmGenericSection));
#endif
        SgAsmGenericSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericSection::pool_size * sizeof(SgAsmGenericSection), V_SgAsmGenericSection));

        if (SgAsmGenericSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericSection* blkptr{reinterpret_cast<SgAsmGenericSection*>(SgAsmGenericSection::pools[blockIndex-1])};
                blkptr[SgAsmGenericSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericSection::pool_size - 1};
    SgAsmGenericSection* testPointer{reinterpret_cast<SgAsmGenericSection*>(SgAsmGenericSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericSection::pool_size * (SgAsmGenericSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericSectionStroageClass
 * from its counterpart of SgAsmGenericSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericSection::initializeStorageClassArray(SgAsmGenericSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericSection::pools.begin(); block != SgAsmGenericSection::pools.end(); ++block) {
        SgAsmGenericSection* pointer{reinterpret_cast<SgAsmGenericSection*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 206212 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCliHeader that takes its corresponding StorageClass as parameter
 */
SgAsmCliHeader::SgAsmCliHeader(const SgAsmCliHeaderStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 206222 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_cb = storageSource.storageOf_cb;
    p_majorRuntimeVersion = storageSource.storageOf_majorRuntimeVersion;
    p_minorRuntimeVersion = storageSource.storageOf_minorRuntimeVersion;
    p_metaData = storageSource.storageOf_metaData;
    p_flags = storageSource.storageOf_flags;
    p_entryPointToken = storageSource.storageOf_entryPointToken;
    p_resources = storageSource.storageOf_resources;
    p_strongNameSignature = storageSource.storageOf_strongNameSignature;
    p_codeManagerTable = storageSource.storageOf_codeManagerTable;
    p_vTableFixups = storageSource.storageOf_vTableFixups;
    p_exportAddressTableJumps = storageSource.storageOf_exportAddressTableJumps;
    p_managedNativeHeader = storageSource.storageOf_managedNativeHeader;
     p_metadataRoot = (SgAsmCilMetadataRoot*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_metadataRoot));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCliHeader*
SgAsmCliHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCliHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCliHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCliHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCliHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCliHeader)};
        unsigned long positionInPool{localIndex % SgAsmCliHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCliHeader::pool_size};
        returnPointer = &(((SgAsmCliHeader*)(SgAsmCliHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCliHeader*
SgAsmCliHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCliHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCliHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCliHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCliHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCliHeader)};
        unsigned long positionInPool{localIndex % SgAsmCliHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCliHeader::pool_size};
        returnPointer = &(((SgAsmCliHeader*)(SgAsmCliHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCliHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCliHeader::pools.begin(); block != SgAsmCliHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCliHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmCliHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCliHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCliHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCliHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCliHeader::pools.begin(); block != SgAsmCliHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCliHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmCliHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCliHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCliHeader::clearMemoryPool() {
    SgAsmCliHeader* tempPointer{nullptr};
    if (SgAsmCliHeader::pools.empty() == false) {
        auto block{SgAsmCliHeader::pools.begin()};
        SgAsmCliHeader::next_node = reinterpret_cast<SgAsmCliHeader*>(*block);
        for (; block != SgAsmCliHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCliHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCliHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCliHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCliHeader::pool_size-1]);
        }
    }
}

void
SgAsmCliHeader::deleteMemoryPool() {
    for (auto p: SgAsmCliHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmCliHeader::next_node = nullptr;
    SgAsmCliHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCliHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCliHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCliHeader)};
    for (size_t blockIndex = SgAsmCliHeader::pools.size(); (blockIndex * SgAsmCliHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCliHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCliHeader) = %" PRIuPTR " SgAsmCliHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCliHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCliHeader),SgAsmCliHeader::pool_size);
        }
#endif

        SgAsmCliHeader* pointer{static_cast<SgAsmCliHeader*>(ROSE_MALLOC(SgAsmCliHeader::pool_size * sizeof(SgAsmCliHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCliHeader::pool_size * sizeof(SgAsmCliHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCliHeader::pool_size * sizeof(SgAsmCliHeader));
#endif
        SgAsmCliHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCliHeader::pool_size * sizeof(SgAsmCliHeader), V_SgAsmCliHeader));

        if (SgAsmCliHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCliHeader* blkptr{reinterpret_cast<SgAsmCliHeader*>(SgAsmCliHeader::pools[blockIndex-1])};
                blkptr[SgAsmCliHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCliHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCliHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCliHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCliHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCliHeader::pool_size - 1};
    SgAsmCliHeader* testPointer{reinterpret_cast<SgAsmCliHeader*>(SgAsmCliHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCliHeader::pool_size * (SgAsmCliHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCliHeaderStroageClass
 * from its counterpart of SgAsmCliHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCliHeader::initializeStorageClassArray(SgAsmCliHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCliHeader::pools.begin(); block != SgAsmCliHeader::pools.end(); ++block) {
        SgAsmCliHeader* pointer{reinterpret_cast<SgAsmCliHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmCliHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 206452 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCoffSymbolTable that takes its corresponding StorageClass as parameter
 */
SgAsmCoffSymbolTable::SgAsmCoffSymbolTable(const SgAsmCoffSymbolTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 206462 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_strtab = (SgAsmGenericSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_strtab));
     p_symbols = (SgAsmCoffSymbolList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbols));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCoffSymbolTable*
SgAsmCoffSymbolTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCoffSymbolTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbolTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbolTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbolTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolTable)};
        unsigned long positionInPool{localIndex % SgAsmCoffSymbolTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffSymbolTable::pool_size};
        returnPointer = &(((SgAsmCoffSymbolTable*)(SgAsmCoffSymbolTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCoffSymbolTable*
SgAsmCoffSymbolTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCoffSymbolTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbolTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbolTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbolTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCoffSymbolTable)};
        unsigned long positionInPool{localIndex % SgAsmCoffSymbolTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffSymbolTable::pool_size};
        returnPointer = &(((SgAsmCoffSymbolTable*)(SgAsmCoffSymbolTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCoffSymbolTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCoffSymbolTable::pools.begin(); block != SgAsmCoffSymbolTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffSymbolTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbolTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCoffSymbolTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCoffSymbolTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCoffSymbolTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCoffSymbolTable::pools.begin(); block != SgAsmCoffSymbolTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffSymbolTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbolTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCoffSymbolTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCoffSymbolTable::clearMemoryPool() {
    SgAsmCoffSymbolTable* tempPointer{nullptr};
    if (SgAsmCoffSymbolTable::pools.empty() == false) {
        auto block{SgAsmCoffSymbolTable::pools.begin()};
        SgAsmCoffSymbolTable::next_node = reinterpret_cast<SgAsmCoffSymbolTable*>(*block);
        for (; block != SgAsmCoffSymbolTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCoffSymbolTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCoffSymbolTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCoffSymbolTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCoffSymbolTable::pool_size-1]);
        }
    }
}

void
SgAsmCoffSymbolTable::deleteMemoryPool() {
    for (auto p: SgAsmCoffSymbolTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmCoffSymbolTable::next_node = nullptr;
    SgAsmCoffSymbolTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCoffSymbolTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbolTable)};
    for (size_t blockIndex = SgAsmCoffSymbolTable::pools.size(); (blockIndex * SgAsmCoffSymbolTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbolTable) = %" PRIuPTR " SgAsmCoffSymbolTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbolTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbolTable),SgAsmCoffSymbolTable::pool_size);
        }
#endif

        SgAsmCoffSymbolTable* pointer{static_cast<SgAsmCoffSymbolTable*>(ROSE_MALLOC(SgAsmCoffSymbolTable::pool_size * sizeof(SgAsmCoffSymbolTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCoffSymbolTable::pool_size * sizeof(SgAsmCoffSymbolTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCoffSymbolTable::pool_size * sizeof(SgAsmCoffSymbolTable));
#endif
        SgAsmCoffSymbolTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCoffSymbolTable::pool_size * sizeof(SgAsmCoffSymbolTable), V_SgAsmCoffSymbolTable));

        if (SgAsmCoffSymbolTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCoffSymbolTable* blkptr{reinterpret_cast<SgAsmCoffSymbolTable*>(SgAsmCoffSymbolTable::pools[blockIndex-1])};
                blkptr[SgAsmCoffSymbolTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCoffSymbolTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCoffSymbolTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCoffSymbolTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCoffSymbolTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCoffSymbolTable::pool_size - 1};
    SgAsmCoffSymbolTable* testPointer{reinterpret_cast<SgAsmCoffSymbolTable*>(SgAsmCoffSymbolTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCoffSymbolTable::pool_size * (SgAsmCoffSymbolTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCoffSymbolTableStroageClass
 * from its counterpart of SgAsmCoffSymbolTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCoffSymbolTable::initializeStorageClassArray(SgAsmCoffSymbolTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCoffSymbolTable::pools.begin(); block != SgAsmCoffSymbolTable::pools.end(); ++block) {
        SgAsmCoffSymbolTable* pointer{reinterpret_cast<SgAsmCoffSymbolTable*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbolTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 206681 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDOSExtendedHeader that takes its corresponding StorageClass as parameter
 */
SgAsmDOSExtendedHeader::SgAsmDOSExtendedHeader(const SgAsmDOSExtendedHeaderStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 206691 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_e_res1 = storageSource.storageOf_e_res1;
    p_e_oemid = storageSource.storageOf_e_oemid;
    p_e_oeminfo = storageSource.storageOf_e_oeminfo;
    p_e_res2 = storageSource.storageOf_e_res2;
    p_e_res3 = storageSource.storageOf_e_res3;
    p_e_res4 = storageSource.storageOf_e_res4;
    p_e_res5 = storageSource.storageOf_e_res5;
    p_e_res6 = storageSource.storageOf_e_res6;
    p_e_lfanew = storageSource.storageOf_e_lfanew;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDOSExtendedHeader*
SgAsmDOSExtendedHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDOSExtendedHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDOSExtendedHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDOSExtendedHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDOSExtendedHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSExtendedHeader)};
        unsigned long positionInPool{localIndex % SgAsmDOSExtendedHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDOSExtendedHeader::pool_size};
        returnPointer = &(((SgAsmDOSExtendedHeader*)(SgAsmDOSExtendedHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDOSExtendedHeader*
SgAsmDOSExtendedHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDOSExtendedHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDOSExtendedHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDOSExtendedHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDOSExtendedHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDOSExtendedHeader)};
        unsigned long positionInPool{localIndex % SgAsmDOSExtendedHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDOSExtendedHeader::pool_size};
        returnPointer = &(((SgAsmDOSExtendedHeader*)(SgAsmDOSExtendedHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDOSExtendedHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDOSExtendedHeader::pools.begin(); block != SgAsmDOSExtendedHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDOSExtendedHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmDOSExtendedHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDOSExtendedHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDOSExtendedHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDOSExtendedHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDOSExtendedHeader::pools.begin(); block != SgAsmDOSExtendedHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDOSExtendedHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmDOSExtendedHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDOSExtendedHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDOSExtendedHeader::clearMemoryPool() {
    SgAsmDOSExtendedHeader* tempPointer{nullptr};
    if (SgAsmDOSExtendedHeader::pools.empty() == false) {
        auto block{SgAsmDOSExtendedHeader::pools.begin()};
        SgAsmDOSExtendedHeader::next_node = reinterpret_cast<SgAsmDOSExtendedHeader*>(*block);
        for (; block != SgAsmDOSExtendedHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDOSExtendedHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDOSExtendedHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDOSExtendedHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDOSExtendedHeader::pool_size-1]);
        }
    }
}

void
SgAsmDOSExtendedHeader::deleteMemoryPool() {
    for (auto p: SgAsmDOSExtendedHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmDOSExtendedHeader::next_node = nullptr;
    SgAsmDOSExtendedHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDOSExtendedHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSExtendedHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDOSExtendedHeader)};
    for (size_t blockIndex = SgAsmDOSExtendedHeader::pools.size(); (blockIndex * SgAsmDOSExtendedHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSExtendedHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDOSExtendedHeader) = %" PRIuPTR " SgAsmDOSExtendedHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSExtendedHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDOSExtendedHeader),SgAsmDOSExtendedHeader::pool_size);
        }
#endif

        SgAsmDOSExtendedHeader* pointer{static_cast<SgAsmDOSExtendedHeader*>(ROSE_MALLOC(SgAsmDOSExtendedHeader::pool_size * sizeof(SgAsmDOSExtendedHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDOSExtendedHeader::pool_size * sizeof(SgAsmDOSExtendedHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDOSExtendedHeader::pool_size * sizeof(SgAsmDOSExtendedHeader));
#endif
        SgAsmDOSExtendedHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDOSExtendedHeader::pool_size * sizeof(SgAsmDOSExtendedHeader), V_SgAsmDOSExtendedHeader));

        if (SgAsmDOSExtendedHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDOSExtendedHeader* blkptr{reinterpret_cast<SgAsmDOSExtendedHeader*>(SgAsmDOSExtendedHeader::pools[blockIndex-1])};
                blkptr[SgAsmDOSExtendedHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDOSExtendedHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDOSExtendedHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDOSExtendedHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDOSExtendedHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDOSExtendedHeader::pool_size - 1};
    SgAsmDOSExtendedHeader* testPointer{reinterpret_cast<SgAsmDOSExtendedHeader*>(SgAsmDOSExtendedHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDOSExtendedHeader::pool_size * (SgAsmDOSExtendedHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDOSExtendedHeaderStroageClass
 * from its counterpart of SgAsmDOSExtendedHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDOSExtendedHeader::initializeStorageClassArray(SgAsmDOSExtendedHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDOSExtendedHeader::pools.begin(); block != SgAsmDOSExtendedHeader::pools.end(); ++block) {
        SgAsmDOSExtendedHeader* pointer{reinterpret_cast<SgAsmDOSExtendedHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmDOSExtendedHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 206917 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfSection::SgAsmElfSection(const SgAsmElfSectionStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 206927 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_linkedSection = (SgAsmElfSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_linkedSection));
     p_sectionEntry = (SgAsmElfSectionTableEntry*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sectionEntry));
     p_segmentEntry = (SgAsmElfSegmentTableEntry*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_segmentEntry));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSection*
SgAsmElfSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSection::pool_size};
        returnPointer = &(((SgAsmElfSection*)(SgAsmElfSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSection*
SgAsmElfSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSection::pool_size};
        returnPointer = &(((SgAsmElfSection*)(SgAsmElfSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSection::pools.begin(); block != SgAsmElfSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSection::pools.begin(); block != SgAsmElfSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSection::clearMemoryPool() {
    SgAsmElfSection* tempPointer{nullptr};
    if (SgAsmElfSection::pools.empty() == false) {
        auto block{SgAsmElfSection::pools.begin()};
        SgAsmElfSection::next_node = reinterpret_cast<SgAsmElfSection*>(*block);
        for (; block != SgAsmElfSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSection::pool_size-1]);
        }
    }
}

void
SgAsmElfSection::deleteMemoryPool() {
    for (auto p: SgAsmElfSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSection::next_node = nullptr;
    SgAsmElfSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSection)};
    for (size_t blockIndex = SgAsmElfSection::pools.size(); (blockIndex * SgAsmElfSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSection) = %" PRIuPTR " SgAsmElfSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSection),SgAsmElfSection::pool_size);
        }
#endif

        SgAsmElfSection* pointer{static_cast<SgAsmElfSection*>(ROSE_MALLOC(SgAsmElfSection::pool_size * sizeof(SgAsmElfSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSection::pool_size * sizeof(SgAsmElfSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSection::pool_size * sizeof(SgAsmElfSection));
#endif
        SgAsmElfSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSection::pool_size * sizeof(SgAsmElfSection), V_SgAsmElfSection));

        if (SgAsmElfSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSection* blkptr{reinterpret_cast<SgAsmElfSection*>(SgAsmElfSection::pools[blockIndex-1])};
                blkptr[SgAsmElfSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSection::pool_size - 1};
    SgAsmElfSection* testPointer{reinterpret_cast<SgAsmElfSection*>(SgAsmElfSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSection::pool_size * (SgAsmElfSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSectionStroageClass
 * from its counterpart of SgAsmElfSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSection::initializeStorageClassArray(SgAsmElfSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSection::pools.begin(); block != SgAsmElfSection::pools.end(); ++block) {
        SgAsmElfSection* pointer{reinterpret_cast<SgAsmElfSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 207147 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfDynamicSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfDynamicSection::SgAsmElfDynamicSection(const SgAsmElfDynamicSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 207157 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_entries = (SgAsmElfDynamicEntryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfDynamicSection*
SgAsmElfDynamicSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfDynamicSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfDynamicSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicSection)};
        unsigned long positionInPool{localIndex % SgAsmElfDynamicSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfDynamicSection::pool_size};
        returnPointer = &(((SgAsmElfDynamicSection*)(SgAsmElfDynamicSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfDynamicSection*
SgAsmElfDynamicSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfDynamicSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfDynamicSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfDynamicSection)};
        unsigned long positionInPool{localIndex % SgAsmElfDynamicSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfDynamicSection::pool_size};
        returnPointer = &(((SgAsmElfDynamicSection*)(SgAsmElfDynamicSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfDynamicSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfDynamicSection::pools.begin(); block != SgAsmElfDynamicSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfDynamicSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfDynamicSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfDynamicSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfDynamicSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfDynamicSection::pools.begin(); block != SgAsmElfDynamicSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfDynamicSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfDynamicSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfDynamicSection::clearMemoryPool() {
    SgAsmElfDynamicSection* tempPointer{nullptr};
    if (SgAsmElfDynamicSection::pools.empty() == false) {
        auto block{SgAsmElfDynamicSection::pools.begin()};
        SgAsmElfDynamicSection::next_node = reinterpret_cast<SgAsmElfDynamicSection*>(*block);
        for (; block != SgAsmElfDynamicSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfDynamicSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfDynamicSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfDynamicSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfDynamicSection::pool_size-1]);
        }
    }
}

void
SgAsmElfDynamicSection::deleteMemoryPool() {
    for (auto p: SgAsmElfDynamicSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfDynamicSection::next_node = nullptr;
    SgAsmElfDynamicSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfDynamicSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicSection)};
    for (size_t blockIndex = SgAsmElfDynamicSection::pools.size(); (blockIndex * SgAsmElfDynamicSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicSection) = %" PRIuPTR " SgAsmElfDynamicSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfDynamicSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfDynamicSection),SgAsmElfDynamicSection::pool_size);
        }
#endif

        SgAsmElfDynamicSection* pointer{static_cast<SgAsmElfDynamicSection*>(ROSE_MALLOC(SgAsmElfDynamicSection::pool_size * sizeof(SgAsmElfDynamicSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfDynamicSection::pool_size * sizeof(SgAsmElfDynamicSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfDynamicSection::pool_size * sizeof(SgAsmElfDynamicSection));
#endif
        SgAsmElfDynamicSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfDynamicSection::pool_size * sizeof(SgAsmElfDynamicSection), V_SgAsmElfDynamicSection));

        if (SgAsmElfDynamicSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfDynamicSection* blkptr{reinterpret_cast<SgAsmElfDynamicSection*>(SgAsmElfDynamicSection::pools[blockIndex-1])};
                blkptr[SgAsmElfDynamicSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfDynamicSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfDynamicSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfDynamicSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfDynamicSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfDynamicSection::pool_size - 1};
    SgAsmElfDynamicSection* testPointer{reinterpret_cast<SgAsmElfDynamicSection*>(SgAsmElfDynamicSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfDynamicSection::pool_size * (SgAsmElfDynamicSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfDynamicSectionStroageClass
 * from its counterpart of SgAsmElfDynamicSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfDynamicSection::initializeStorageClassArray(SgAsmElfDynamicSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfDynamicSection::pools.begin(); block != SgAsmElfDynamicSection::pools.end(); ++block) {
        SgAsmElfDynamicSection* pointer{reinterpret_cast<SgAsmElfDynamicSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfDynamicSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 207375 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfEHFrameSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfEHFrameSection::SgAsmElfEHFrameSection(const SgAsmElfEHFrameSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 207385 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_ci_entries = (SgAsmElfEHFrameEntryCIList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_ci_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfEHFrameSection*
SgAsmElfEHFrameSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfEHFrameSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfEHFrameSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameSection)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameSection::pool_size};
        returnPointer = &(((SgAsmElfEHFrameSection*)(SgAsmElfEHFrameSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfEHFrameSection*
SgAsmElfEHFrameSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfEHFrameSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfEHFrameSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfEHFrameSection)};
        unsigned long positionInPool{localIndex % SgAsmElfEHFrameSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfEHFrameSection::pool_size};
        returnPointer = &(((SgAsmElfEHFrameSection*)(SgAsmElfEHFrameSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfEHFrameSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfEHFrameSection::pools.begin(); block != SgAsmElfEHFrameSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfEHFrameSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfEHFrameSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfEHFrameSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfEHFrameSection::pools.begin(); block != SgAsmElfEHFrameSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfEHFrameSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfEHFrameSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfEHFrameSection::clearMemoryPool() {
    SgAsmElfEHFrameSection* tempPointer{nullptr};
    if (SgAsmElfEHFrameSection::pools.empty() == false) {
        auto block{SgAsmElfEHFrameSection::pools.begin()};
        SgAsmElfEHFrameSection::next_node = reinterpret_cast<SgAsmElfEHFrameSection*>(*block);
        for (; block != SgAsmElfEHFrameSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfEHFrameSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfEHFrameSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfEHFrameSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfEHFrameSection::pool_size-1]);
        }
    }
}

void
SgAsmElfEHFrameSection::deleteMemoryPool() {
    for (auto p: SgAsmElfEHFrameSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfEHFrameSection::next_node = nullptr;
    SgAsmElfEHFrameSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfEHFrameSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameSection)};
    for (size_t blockIndex = SgAsmElfEHFrameSection::pools.size(); (blockIndex * SgAsmElfEHFrameSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameSection) = %" PRIuPTR " SgAsmElfEHFrameSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfEHFrameSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfEHFrameSection),SgAsmElfEHFrameSection::pool_size);
        }
#endif

        SgAsmElfEHFrameSection* pointer{static_cast<SgAsmElfEHFrameSection*>(ROSE_MALLOC(SgAsmElfEHFrameSection::pool_size * sizeof(SgAsmElfEHFrameSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfEHFrameSection::pool_size * sizeof(SgAsmElfEHFrameSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfEHFrameSection::pool_size * sizeof(SgAsmElfEHFrameSection));
#endif
        SgAsmElfEHFrameSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfEHFrameSection::pool_size * sizeof(SgAsmElfEHFrameSection), V_SgAsmElfEHFrameSection));

        if (SgAsmElfEHFrameSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfEHFrameSection* blkptr{reinterpret_cast<SgAsmElfEHFrameSection*>(SgAsmElfEHFrameSection::pools[blockIndex-1])};
                blkptr[SgAsmElfEHFrameSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfEHFrameSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfEHFrameSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfEHFrameSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfEHFrameSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfEHFrameSection::pool_size - 1};
    SgAsmElfEHFrameSection* testPointer{reinterpret_cast<SgAsmElfEHFrameSection*>(SgAsmElfEHFrameSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfEHFrameSection::pool_size * (SgAsmElfEHFrameSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfEHFrameSectionStroageClass
 * from its counterpart of SgAsmElfEHFrameSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfEHFrameSection::initializeStorageClassArray(SgAsmElfEHFrameSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfEHFrameSection::pools.begin(); block != SgAsmElfEHFrameSection::pools.end(); ++block) {
        SgAsmElfEHFrameSection* pointer{reinterpret_cast<SgAsmElfEHFrameSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfEHFrameSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 207603 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfNoteSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfNoteSection::SgAsmElfNoteSection(const SgAsmElfNoteSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 207613 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_entries = (SgAsmElfNoteEntryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfNoteSection*
SgAsmElfNoteSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfNoteSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfNoteSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteSection)};
        unsigned long positionInPool{localIndex % SgAsmElfNoteSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfNoteSection::pool_size};
        returnPointer = &(((SgAsmElfNoteSection*)(SgAsmElfNoteSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfNoteSection*
SgAsmElfNoteSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfNoteSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfNoteSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfNoteSection)};
        unsigned long positionInPool{localIndex % SgAsmElfNoteSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfNoteSection::pool_size};
        returnPointer = &(((SgAsmElfNoteSection*)(SgAsmElfNoteSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfNoteSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfNoteSection::pools.begin(); block != SgAsmElfNoteSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfNoteSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfNoteSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfNoteSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfNoteSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfNoteSection::pools.begin(); block != SgAsmElfNoteSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfNoteSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfNoteSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfNoteSection::clearMemoryPool() {
    SgAsmElfNoteSection* tempPointer{nullptr};
    if (SgAsmElfNoteSection::pools.empty() == false) {
        auto block{SgAsmElfNoteSection::pools.begin()};
        SgAsmElfNoteSection::next_node = reinterpret_cast<SgAsmElfNoteSection*>(*block);
        for (; block != SgAsmElfNoteSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfNoteSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfNoteSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfNoteSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfNoteSection::pool_size-1]);
        }
    }
}

void
SgAsmElfNoteSection::deleteMemoryPool() {
    for (auto p: SgAsmElfNoteSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfNoteSection::next_node = nullptr;
    SgAsmElfNoteSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfNoteSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteSection)};
    for (size_t blockIndex = SgAsmElfNoteSection::pools.size(); (blockIndex * SgAsmElfNoteSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteSection) = %" PRIuPTR " SgAsmElfNoteSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfNoteSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfNoteSection),SgAsmElfNoteSection::pool_size);
        }
#endif

        SgAsmElfNoteSection* pointer{static_cast<SgAsmElfNoteSection*>(ROSE_MALLOC(SgAsmElfNoteSection::pool_size * sizeof(SgAsmElfNoteSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfNoteSection::pool_size * sizeof(SgAsmElfNoteSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfNoteSection::pool_size * sizeof(SgAsmElfNoteSection));
#endif
        SgAsmElfNoteSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfNoteSection::pool_size * sizeof(SgAsmElfNoteSection), V_SgAsmElfNoteSection));

        if (SgAsmElfNoteSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfNoteSection* blkptr{reinterpret_cast<SgAsmElfNoteSection*>(SgAsmElfNoteSection::pools[blockIndex-1])};
                blkptr[SgAsmElfNoteSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfNoteSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfNoteSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfNoteSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfNoteSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfNoteSection::pool_size - 1};
    SgAsmElfNoteSection* testPointer{reinterpret_cast<SgAsmElfNoteSection*>(SgAsmElfNoteSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfNoteSection::pool_size * (SgAsmElfNoteSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfNoteSectionStroageClass
 * from its counterpart of SgAsmElfNoteSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfNoteSection::initializeStorageClassArray(SgAsmElfNoteSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfNoteSection::pools.begin(); block != SgAsmElfNoteSection::pools.end(); ++block) {
        SgAsmElfNoteSection* pointer{reinterpret_cast<SgAsmElfNoteSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfNoteSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 207831 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfRelocSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfRelocSection::SgAsmElfRelocSection(const SgAsmElfRelocSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 207841 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_usesAddend = storageSource.storageOf_usesAddend;
     p_targetSection = (SgAsmElfSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_targetSection));
     p_entries = (SgAsmElfRelocEntryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfRelocSection*
SgAsmElfRelocSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfRelocSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfRelocSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocSection)};
        unsigned long positionInPool{localIndex % SgAsmElfRelocSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfRelocSection::pool_size};
        returnPointer = &(((SgAsmElfRelocSection*)(SgAsmElfRelocSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfRelocSection*
SgAsmElfRelocSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfRelocSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfRelocSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfRelocSection)};
        unsigned long positionInPool{localIndex % SgAsmElfRelocSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfRelocSection::pool_size};
        returnPointer = &(((SgAsmElfRelocSection*)(SgAsmElfRelocSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfRelocSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfRelocSection::pools.begin(); block != SgAsmElfRelocSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfRelocSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfRelocSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfRelocSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfRelocSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfRelocSection::pools.begin(); block != SgAsmElfRelocSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfRelocSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfRelocSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfRelocSection::clearMemoryPool() {
    SgAsmElfRelocSection* tempPointer{nullptr};
    if (SgAsmElfRelocSection::pools.empty() == false) {
        auto block{SgAsmElfRelocSection::pools.begin()};
        SgAsmElfRelocSection::next_node = reinterpret_cast<SgAsmElfRelocSection*>(*block);
        for (; block != SgAsmElfRelocSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfRelocSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfRelocSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfRelocSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfRelocSection::pool_size-1]);
        }
    }
}

void
SgAsmElfRelocSection::deleteMemoryPool() {
    for (auto p: SgAsmElfRelocSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfRelocSection::next_node = nullptr;
    SgAsmElfRelocSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfRelocSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocSection)};
    for (size_t blockIndex = SgAsmElfRelocSection::pools.size(); (blockIndex * SgAsmElfRelocSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocSection) = %" PRIuPTR " SgAsmElfRelocSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfRelocSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfRelocSection),SgAsmElfRelocSection::pool_size);
        }
#endif

        SgAsmElfRelocSection* pointer{static_cast<SgAsmElfRelocSection*>(ROSE_MALLOC(SgAsmElfRelocSection::pool_size * sizeof(SgAsmElfRelocSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfRelocSection::pool_size * sizeof(SgAsmElfRelocSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfRelocSection::pool_size * sizeof(SgAsmElfRelocSection));
#endif
        SgAsmElfRelocSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfRelocSection::pool_size * sizeof(SgAsmElfRelocSection), V_SgAsmElfRelocSection));

        if (SgAsmElfRelocSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfRelocSection* blkptr{reinterpret_cast<SgAsmElfRelocSection*>(SgAsmElfRelocSection::pools[blockIndex-1])};
                blkptr[SgAsmElfRelocSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfRelocSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfRelocSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfRelocSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfRelocSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfRelocSection::pool_size - 1};
    SgAsmElfRelocSection* testPointer{reinterpret_cast<SgAsmElfRelocSection*>(SgAsmElfRelocSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfRelocSection::pool_size * (SgAsmElfRelocSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfRelocSectionStroageClass
 * from its counterpart of SgAsmElfRelocSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfRelocSection::initializeStorageClassArray(SgAsmElfRelocSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfRelocSection::pools.begin(); block != SgAsmElfRelocSection::pools.end(); ++block) {
        SgAsmElfRelocSection* pointer{reinterpret_cast<SgAsmElfRelocSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfRelocSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 208061 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfStringSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfStringSection::SgAsmElfStringSection(const SgAsmElfStringSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 208071 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_strtab = (SgAsmElfStrtab*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_strtab));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfStringSection*
SgAsmElfStringSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfStringSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfStringSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfStringSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfStringSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStringSection)};
        unsigned long positionInPool{localIndex % SgAsmElfStringSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfStringSection::pool_size};
        returnPointer = &(((SgAsmElfStringSection*)(SgAsmElfStringSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfStringSection*
SgAsmElfStringSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfStringSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfStringSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfStringSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfStringSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfStringSection)};
        unsigned long positionInPool{localIndex % SgAsmElfStringSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfStringSection::pool_size};
        returnPointer = &(((SgAsmElfStringSection*)(SgAsmElfStringSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfStringSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfStringSection::pools.begin(); block != SgAsmElfStringSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfStringSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfStringSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfStringSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfStringSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfStringSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfStringSection::pools.begin(); block != SgAsmElfStringSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfStringSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfStringSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfStringSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfStringSection::clearMemoryPool() {
    SgAsmElfStringSection* tempPointer{nullptr};
    if (SgAsmElfStringSection::pools.empty() == false) {
        auto block{SgAsmElfStringSection::pools.begin()};
        SgAsmElfStringSection::next_node = reinterpret_cast<SgAsmElfStringSection*>(*block);
        for (; block != SgAsmElfStringSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfStringSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfStringSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfStringSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfStringSection::pool_size-1]);
        }
    }
}

void
SgAsmElfStringSection::deleteMemoryPool() {
    for (auto p: SgAsmElfStringSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfStringSection::next_node = nullptr;
    SgAsmElfStringSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfStringSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStringSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfStringSection)};
    for (size_t blockIndex = SgAsmElfStringSection::pools.size(); (blockIndex * SgAsmElfStringSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStringSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfStringSection) = %" PRIuPTR " SgAsmElfStringSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStringSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfStringSection),SgAsmElfStringSection::pool_size);
        }
#endif

        SgAsmElfStringSection* pointer{static_cast<SgAsmElfStringSection*>(ROSE_MALLOC(SgAsmElfStringSection::pool_size * sizeof(SgAsmElfStringSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfStringSection::pool_size * sizeof(SgAsmElfStringSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfStringSection::pool_size * sizeof(SgAsmElfStringSection));
#endif
        SgAsmElfStringSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfStringSection::pool_size * sizeof(SgAsmElfStringSection), V_SgAsmElfStringSection));

        if (SgAsmElfStringSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfStringSection* blkptr{reinterpret_cast<SgAsmElfStringSection*>(SgAsmElfStringSection::pools[blockIndex-1])};
                blkptr[SgAsmElfStringSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfStringSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfStringSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfStringSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfStringSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfStringSection::pool_size - 1};
    SgAsmElfStringSection* testPointer{reinterpret_cast<SgAsmElfStringSection*>(SgAsmElfStringSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfStringSection::pool_size * (SgAsmElfStringSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfStringSectionStroageClass
 * from its counterpart of SgAsmElfStringSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfStringSection::initializeStorageClassArray(SgAsmElfStringSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfStringSection::pools.begin(); block != SgAsmElfStringSection::pools.end(); ++block) {
        SgAsmElfStringSection* pointer{reinterpret_cast<SgAsmElfStringSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfStringSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 208289 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymbolSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymbolSection::SgAsmElfSymbolSection(const SgAsmElfSymbolSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 208299 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_isDynamic = storageSource.storageOf_isDynamic;
     p_symbols = (SgAsmElfSymbolList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbols));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymbolSection*
SgAsmElfSymbolSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymbolSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbolSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbolSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbolSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymbolSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymbolSection::pool_size};
        returnPointer = &(((SgAsmElfSymbolSection*)(SgAsmElfSymbolSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymbolSection*
SgAsmElfSymbolSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymbolSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbolSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbolSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbolSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymbolSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymbolSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymbolSection::pool_size};
        returnPointer = &(((SgAsmElfSymbolSection*)(SgAsmElfSymbolSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymbolSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymbolSection::pools.begin(); block != SgAsmElfSymbolSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymbolSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbolSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymbolSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymbolSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymbolSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymbolSection::pools.begin(); block != SgAsmElfSymbolSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymbolSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbolSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymbolSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymbolSection::clearMemoryPool() {
    SgAsmElfSymbolSection* tempPointer{nullptr};
    if (SgAsmElfSymbolSection::pools.empty() == false) {
        auto block{SgAsmElfSymbolSection::pools.begin()};
        SgAsmElfSymbolSection::next_node = reinterpret_cast<SgAsmElfSymbolSection*>(*block);
        for (; block != SgAsmElfSymbolSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymbolSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymbolSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymbolSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymbolSection::pool_size-1]);
        }
    }
}

void
SgAsmElfSymbolSection::deleteMemoryPool() {
    for (auto p: SgAsmElfSymbolSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymbolSection::next_node = nullptr;
    SgAsmElfSymbolSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymbolSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbolSection)};
    for (size_t blockIndex = SgAsmElfSymbolSection::pools.size(); (blockIndex * SgAsmElfSymbolSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbolSection) = %" PRIuPTR " SgAsmElfSymbolSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbolSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbolSection),SgAsmElfSymbolSection::pool_size);
        }
#endif

        SgAsmElfSymbolSection* pointer{static_cast<SgAsmElfSymbolSection*>(ROSE_MALLOC(SgAsmElfSymbolSection::pool_size * sizeof(SgAsmElfSymbolSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymbolSection::pool_size * sizeof(SgAsmElfSymbolSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymbolSection::pool_size * sizeof(SgAsmElfSymbolSection));
#endif
        SgAsmElfSymbolSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymbolSection::pool_size * sizeof(SgAsmElfSymbolSection), V_SgAsmElfSymbolSection));

        if (SgAsmElfSymbolSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymbolSection* blkptr{reinterpret_cast<SgAsmElfSymbolSection*>(SgAsmElfSymbolSection::pools[blockIndex-1])};
                blkptr[SgAsmElfSymbolSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymbolSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymbolSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymbolSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymbolSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymbolSection::pool_size - 1};
    SgAsmElfSymbolSection* testPointer{reinterpret_cast<SgAsmElfSymbolSection*>(SgAsmElfSymbolSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymbolSection::pool_size * (SgAsmElfSymbolSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymbolSectionStroageClass
 * from its counterpart of SgAsmElfSymbolSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymbolSection::initializeStorageClassArray(SgAsmElfSymbolSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymbolSection::pools.begin(); block != SgAsmElfSymbolSection::pools.end(); ++block) {
        SgAsmElfSymbolSection* pointer{reinterpret_cast<SgAsmElfSymbolSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbolSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 208518 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverDefinedSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverDefinedSection::SgAsmElfSymverDefinedSection(const SgAsmElfSymverDefinedSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 208528 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_entries = (SgAsmElfSymverDefinedEntryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverDefinedSection*
SgAsmElfSymverDefinedSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverDefinedSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverDefinedSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedSection::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedSection*)(SgAsmElfSymverDefinedSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverDefinedSection*
SgAsmElfSymverDefinedSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverDefinedSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverDefinedSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverDefinedSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverDefinedSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverDefinedSection::pool_size};
        returnPointer = &(((SgAsmElfSymverDefinedSection*)(SgAsmElfSymverDefinedSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverDefinedSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverDefinedSection::pools.begin(); block != SgAsmElfSymverDefinedSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverDefinedSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverDefinedSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverDefinedSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverDefinedSection::pools.begin(); block != SgAsmElfSymverDefinedSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverDefinedSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverDefinedSection::clearMemoryPool() {
    SgAsmElfSymverDefinedSection* tempPointer{nullptr};
    if (SgAsmElfSymverDefinedSection::pools.empty() == false) {
        auto block{SgAsmElfSymverDefinedSection::pools.begin()};
        SgAsmElfSymverDefinedSection::next_node = reinterpret_cast<SgAsmElfSymverDefinedSection*>(*block);
        for (; block != SgAsmElfSymverDefinedSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverDefinedSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverDefinedSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverDefinedSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverDefinedSection::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverDefinedSection::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverDefinedSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverDefinedSection::next_node = nullptr;
    SgAsmElfSymverDefinedSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverDefinedSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedSection)};
    for (size_t blockIndex = SgAsmElfSymverDefinedSection::pools.size(); (blockIndex * SgAsmElfSymverDefinedSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedSection) = %" PRIuPTR " SgAsmElfSymverDefinedSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverDefinedSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverDefinedSection),SgAsmElfSymverDefinedSection::pool_size);
        }
#endif

        SgAsmElfSymverDefinedSection* pointer{static_cast<SgAsmElfSymverDefinedSection*>(ROSE_MALLOC(SgAsmElfSymverDefinedSection::pool_size * sizeof(SgAsmElfSymverDefinedSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverDefinedSection::pool_size * sizeof(SgAsmElfSymverDefinedSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverDefinedSection::pool_size * sizeof(SgAsmElfSymverDefinedSection));
#endif
        SgAsmElfSymverDefinedSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverDefinedSection::pool_size * sizeof(SgAsmElfSymverDefinedSection), V_SgAsmElfSymverDefinedSection));

        if (SgAsmElfSymverDefinedSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverDefinedSection* blkptr{reinterpret_cast<SgAsmElfSymverDefinedSection*>(SgAsmElfSymverDefinedSection::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverDefinedSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverDefinedSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverDefinedSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverDefinedSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverDefinedSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverDefinedSection::pool_size - 1};
    SgAsmElfSymverDefinedSection* testPointer{reinterpret_cast<SgAsmElfSymverDefinedSection*>(SgAsmElfSymverDefinedSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverDefinedSection::pool_size * (SgAsmElfSymverDefinedSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverDefinedSectionStroageClass
 * from its counterpart of SgAsmElfSymverDefinedSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverDefinedSection::initializeStorageClassArray(SgAsmElfSymverDefinedSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverDefinedSection::pools.begin(); block != SgAsmElfSymverDefinedSection::pools.end(); ++block) {
        SgAsmElfSymverDefinedSection* pointer{reinterpret_cast<SgAsmElfSymverDefinedSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverDefinedSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 208746 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverNeededSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverNeededSection::SgAsmElfSymverNeededSection(const SgAsmElfSymverNeededSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 208756 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_entries = (SgAsmElfSymverNeededEntryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverNeededSection*
SgAsmElfSymverNeededSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverNeededSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverNeededSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededSection::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededSection*)(SgAsmElfSymverNeededSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverNeededSection*
SgAsmElfSymverNeededSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverNeededSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverNeededSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverNeededSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverNeededSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverNeededSection::pool_size};
        returnPointer = &(((SgAsmElfSymverNeededSection*)(SgAsmElfSymverNeededSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverNeededSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverNeededSection::pools.begin(); block != SgAsmElfSymverNeededSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverNeededSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverNeededSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverNeededSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverNeededSection::pools.begin(); block != SgAsmElfSymverNeededSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverNeededSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverNeededSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverNeededSection::clearMemoryPool() {
    SgAsmElfSymverNeededSection* tempPointer{nullptr};
    if (SgAsmElfSymverNeededSection::pools.empty() == false) {
        auto block{SgAsmElfSymverNeededSection::pools.begin()};
        SgAsmElfSymverNeededSection::next_node = reinterpret_cast<SgAsmElfSymverNeededSection*>(*block);
        for (; block != SgAsmElfSymverNeededSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverNeededSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverNeededSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverNeededSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverNeededSection::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverNeededSection::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverNeededSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverNeededSection::next_node = nullptr;
    SgAsmElfSymverNeededSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverNeededSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededSection)};
    for (size_t blockIndex = SgAsmElfSymverNeededSection::pools.size(); (blockIndex * SgAsmElfSymverNeededSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededSection) = %" PRIuPTR " SgAsmElfSymverNeededSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverNeededSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverNeededSection),SgAsmElfSymverNeededSection::pool_size);
        }
#endif

        SgAsmElfSymverNeededSection* pointer{static_cast<SgAsmElfSymverNeededSection*>(ROSE_MALLOC(SgAsmElfSymverNeededSection::pool_size * sizeof(SgAsmElfSymverNeededSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverNeededSection::pool_size * sizeof(SgAsmElfSymverNeededSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverNeededSection::pool_size * sizeof(SgAsmElfSymverNeededSection));
#endif
        SgAsmElfSymverNeededSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverNeededSection::pool_size * sizeof(SgAsmElfSymverNeededSection), V_SgAsmElfSymverNeededSection));

        if (SgAsmElfSymverNeededSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverNeededSection* blkptr{reinterpret_cast<SgAsmElfSymverNeededSection*>(SgAsmElfSymverNeededSection::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverNeededSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverNeededSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverNeededSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverNeededSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverNeededSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverNeededSection::pool_size - 1};
    SgAsmElfSymverNeededSection* testPointer{reinterpret_cast<SgAsmElfSymverNeededSection*>(SgAsmElfSymverNeededSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverNeededSection::pool_size * (SgAsmElfSymverNeededSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverNeededSectionStroageClass
 * from its counterpart of SgAsmElfSymverNeededSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverNeededSection::initializeStorageClassArray(SgAsmElfSymverNeededSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverNeededSection::pools.begin(); block != SgAsmElfSymverNeededSection::pools.end(); ++block) {
        SgAsmElfSymverNeededSection* pointer{reinterpret_cast<SgAsmElfSymverNeededSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverNeededSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 208974 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymverSection that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymverSection::SgAsmElfSymverSection(const SgAsmElfSymverSectionStorageClass& storageSource)  : SgAsmElfSection (storageSource) {

/* #line 208984 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_entries = (SgAsmElfSymverEntryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entries));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymverSection*
SgAsmElfSymverSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymverSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymverSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverSection::pool_size};
        returnPointer = &(((SgAsmElfSymverSection*)(SgAsmElfSymverSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymverSection*
SgAsmElfSymverSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymverSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymverSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymverSection)};
        unsigned long positionInPool{localIndex % SgAsmElfSymverSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymverSection::pool_size};
        returnPointer = &(((SgAsmElfSymverSection*)(SgAsmElfSymverSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymverSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymverSection::pools.begin(); block != SgAsmElfSymverSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymverSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymverSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymverSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymverSection::pools.begin(); block != SgAsmElfSymverSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymverSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymverSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymverSection::clearMemoryPool() {
    SgAsmElfSymverSection* tempPointer{nullptr};
    if (SgAsmElfSymverSection::pools.empty() == false) {
        auto block{SgAsmElfSymverSection::pools.begin()};
        SgAsmElfSymverSection::next_node = reinterpret_cast<SgAsmElfSymverSection*>(*block);
        for (; block != SgAsmElfSymverSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymverSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymverSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymverSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymverSection::pool_size-1]);
        }
    }
}

void
SgAsmElfSymverSection::deleteMemoryPool() {
    for (auto p: SgAsmElfSymverSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymverSection::next_node = nullptr;
    SgAsmElfSymverSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymverSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverSection)};
    for (size_t blockIndex = SgAsmElfSymverSection::pools.size(); (blockIndex * SgAsmElfSymverSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverSection) = %" PRIuPTR " SgAsmElfSymverSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymverSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymverSection),SgAsmElfSymverSection::pool_size);
        }
#endif

        SgAsmElfSymverSection* pointer{static_cast<SgAsmElfSymverSection*>(ROSE_MALLOC(SgAsmElfSymverSection::pool_size * sizeof(SgAsmElfSymverSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymverSection::pool_size * sizeof(SgAsmElfSymverSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymverSection::pool_size * sizeof(SgAsmElfSymverSection));
#endif
        SgAsmElfSymverSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymverSection::pool_size * sizeof(SgAsmElfSymverSection), V_SgAsmElfSymverSection));

        if (SgAsmElfSymverSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymverSection* blkptr{reinterpret_cast<SgAsmElfSymverSection*>(SgAsmElfSymverSection::pools[blockIndex-1])};
                blkptr[SgAsmElfSymverSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymverSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymverSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymverSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymverSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymverSection::pool_size - 1};
    SgAsmElfSymverSection* testPointer{reinterpret_cast<SgAsmElfSymverSection*>(SgAsmElfSymverSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymverSection::pool_size * (SgAsmElfSymverSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymverSectionStroageClass
 * from its counterpart of SgAsmElfSymverSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymverSection::initializeStorageClassArray(SgAsmElfSymverSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymverSection::pools.begin(); block != SgAsmElfSymverSection::pools.end(); ++block) {
        SgAsmElfSymverSection* pointer{reinterpret_cast<SgAsmElfSymverSection*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymverSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 209202 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSectionTable that takes its corresponding StorageClass as parameter
 */
SgAsmElfSectionTable::SgAsmElfSectionTable(const SgAsmElfSectionTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 209212 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSectionTable*
SgAsmElfSectionTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTable)};
        unsigned long positionInPool{localIndex % SgAsmElfSectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSectionTable::pool_size};
        returnPointer = &(((SgAsmElfSectionTable*)(SgAsmElfSectionTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSectionTable*
SgAsmElfSectionTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSectionTable)};
        unsigned long positionInPool{localIndex % SgAsmElfSectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSectionTable::pool_size};
        returnPointer = &(((SgAsmElfSectionTable*)(SgAsmElfSectionTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSectionTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSectionTable::pools.begin(); block != SgAsmElfSectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSectionTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSectionTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSectionTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSectionTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSectionTable::pools.begin(); block != SgAsmElfSectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSectionTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSectionTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSectionTable::clearMemoryPool() {
    SgAsmElfSectionTable* tempPointer{nullptr};
    if (SgAsmElfSectionTable::pools.empty() == false) {
        auto block{SgAsmElfSectionTable::pools.begin()};
        SgAsmElfSectionTable::next_node = reinterpret_cast<SgAsmElfSectionTable*>(*block);
        for (; block != SgAsmElfSectionTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSectionTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSectionTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSectionTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSectionTable::pool_size-1]);
        }
    }
}

void
SgAsmElfSectionTable::deleteMemoryPool() {
    for (auto p: SgAsmElfSectionTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSectionTable::next_node = nullptr;
    SgAsmElfSectionTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSectionTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSectionTable)};
    for (size_t blockIndex = SgAsmElfSectionTable::pools.size(); (blockIndex * SgAsmElfSectionTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSectionTable) = %" PRIuPTR " SgAsmElfSectionTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSectionTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSectionTable),SgAsmElfSectionTable::pool_size);
        }
#endif

        SgAsmElfSectionTable* pointer{static_cast<SgAsmElfSectionTable*>(ROSE_MALLOC(SgAsmElfSectionTable::pool_size * sizeof(SgAsmElfSectionTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSectionTable::pool_size * sizeof(SgAsmElfSectionTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSectionTable::pool_size * sizeof(SgAsmElfSectionTable));
#endif
        SgAsmElfSectionTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSectionTable::pool_size * sizeof(SgAsmElfSectionTable), V_SgAsmElfSectionTable));

        if (SgAsmElfSectionTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSectionTable* blkptr{reinterpret_cast<SgAsmElfSectionTable*>(SgAsmElfSectionTable::pools[blockIndex-1])};
                blkptr[SgAsmElfSectionTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSectionTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSectionTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSectionTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSectionTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSectionTable::pool_size - 1};
    SgAsmElfSectionTable* testPointer{reinterpret_cast<SgAsmElfSectionTable*>(SgAsmElfSectionTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSectionTable::pool_size * (SgAsmElfSectionTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSectionTableStroageClass
 * from its counterpart of SgAsmElfSectionTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSectionTable::initializeStorageClassArray(SgAsmElfSectionTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSectionTable::pools.begin(); block != SgAsmElfSectionTable::pools.end(); ++block) {
        SgAsmElfSectionTable* pointer{reinterpret_cast<SgAsmElfSectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSectionTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 209429 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSegmentTable that takes its corresponding StorageClass as parameter
 */
SgAsmElfSegmentTable::SgAsmElfSegmentTable(const SgAsmElfSegmentTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 209439 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSegmentTable*
SgAsmElfSegmentTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSegmentTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSegmentTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTable)};
        unsigned long positionInPool{localIndex % SgAsmElfSegmentTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSegmentTable::pool_size};
        returnPointer = &(((SgAsmElfSegmentTable*)(SgAsmElfSegmentTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSegmentTable*
SgAsmElfSegmentTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSegmentTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSegmentTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSegmentTable)};
        unsigned long positionInPool{localIndex % SgAsmElfSegmentTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSegmentTable::pool_size};
        returnPointer = &(((SgAsmElfSegmentTable*)(SgAsmElfSegmentTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSegmentTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSegmentTable::pools.begin(); block != SgAsmElfSegmentTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSegmentTable*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSegmentTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSegmentTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSegmentTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSegmentTable::pools.begin(); block != SgAsmElfSegmentTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSegmentTable*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSegmentTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSegmentTable::clearMemoryPool() {
    SgAsmElfSegmentTable* tempPointer{nullptr};
    if (SgAsmElfSegmentTable::pools.empty() == false) {
        auto block{SgAsmElfSegmentTable::pools.begin()};
        SgAsmElfSegmentTable::next_node = reinterpret_cast<SgAsmElfSegmentTable*>(*block);
        for (; block != SgAsmElfSegmentTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSegmentTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSegmentTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSegmentTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSegmentTable::pool_size-1]);
        }
    }
}

void
SgAsmElfSegmentTable::deleteMemoryPool() {
    for (auto p: SgAsmElfSegmentTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSegmentTable::next_node = nullptr;
    SgAsmElfSegmentTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSegmentTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTable)};
    for (size_t blockIndex = SgAsmElfSegmentTable::pools.size(); (blockIndex * SgAsmElfSegmentTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTable) = %" PRIuPTR " SgAsmElfSegmentTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSegmentTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSegmentTable),SgAsmElfSegmentTable::pool_size);
        }
#endif

        SgAsmElfSegmentTable* pointer{static_cast<SgAsmElfSegmentTable*>(ROSE_MALLOC(SgAsmElfSegmentTable::pool_size * sizeof(SgAsmElfSegmentTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSegmentTable::pool_size * sizeof(SgAsmElfSegmentTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSegmentTable::pool_size * sizeof(SgAsmElfSegmentTable));
#endif
        SgAsmElfSegmentTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSegmentTable::pool_size * sizeof(SgAsmElfSegmentTable), V_SgAsmElfSegmentTable));

        if (SgAsmElfSegmentTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSegmentTable* blkptr{reinterpret_cast<SgAsmElfSegmentTable*>(SgAsmElfSegmentTable::pools[blockIndex-1])};
                blkptr[SgAsmElfSegmentTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSegmentTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSegmentTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSegmentTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSegmentTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSegmentTable::pool_size - 1};
    SgAsmElfSegmentTable* testPointer{reinterpret_cast<SgAsmElfSegmentTable*>(SgAsmElfSegmentTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSegmentTable::pool_size * (SgAsmElfSegmentTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSegmentTableStroageClass
 * from its counterpart of SgAsmElfSegmentTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSegmentTable::initializeStorageClassArray(SgAsmElfSegmentTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSegmentTable::pools.begin(); block != SgAsmElfSegmentTable::pools.end(); ++block) {
        SgAsmElfSegmentTable* pointer{reinterpret_cast<SgAsmElfSegmentTable*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSegmentTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 209656 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericHeader that takes its corresponding StorageClass as parameter
 */
SgAsmGenericHeader::SgAsmGenericHeader(const SgAsmGenericHeaderStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 209666 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_executableFormat = (SgAsmGenericFormat*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_executableFormat));
    p_magic = storageSource.storageOf_magic.rebuildDataStoredInEasyStorageClass();
    p_isa = storageSource.storageOf_isa;
    p_baseVa = storageSource.storageOf_baseVa;
    p_entryRvas = storageSource.storageOf_entryRvas.rebuildDataStoredInEasyStorageClass();
     p_dlls = (SgAsmGenericDLLList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dlls));
     p_sections = (SgAsmGenericSectionList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sections));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericHeader*
SgAsmGenericHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeader)};
        unsigned long positionInPool{localIndex % SgAsmGenericHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericHeader::pool_size};
        returnPointer = &(((SgAsmGenericHeader*)(SgAsmGenericHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericHeader*
SgAsmGenericHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericHeader)};
        unsigned long positionInPool{localIndex % SgAsmGenericHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericHeader::pool_size};
        returnPointer = &(((SgAsmGenericHeader*)(SgAsmGenericHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericHeader::pools.begin(); block != SgAsmGenericHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericHeader::pools.begin(); block != SgAsmGenericHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericHeader::clearMemoryPool() {
    SgAsmGenericHeader* tempPointer{nullptr};
    if (SgAsmGenericHeader::pools.empty() == false) {
        auto block{SgAsmGenericHeader::pools.begin()};
        SgAsmGenericHeader::next_node = reinterpret_cast<SgAsmGenericHeader*>(*block);
        for (; block != SgAsmGenericHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericHeader::pool_size-1]);
        }
    }
}

void
SgAsmGenericHeader::deleteMemoryPool() {
    for (auto p: SgAsmGenericHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericHeader::next_node = nullptr;
    SgAsmGenericHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericHeader)};
    for (size_t blockIndex = SgAsmGenericHeader::pools.size(); (blockIndex * SgAsmGenericHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericHeader) = %" PRIuPTR " SgAsmGenericHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericHeader),SgAsmGenericHeader::pool_size);
        }
#endif

        SgAsmGenericHeader* pointer{static_cast<SgAsmGenericHeader*>(ROSE_MALLOC(SgAsmGenericHeader::pool_size * sizeof(SgAsmGenericHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericHeader::pool_size * sizeof(SgAsmGenericHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericHeader::pool_size * sizeof(SgAsmGenericHeader));
#endif
        SgAsmGenericHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericHeader::pool_size * sizeof(SgAsmGenericHeader), V_SgAsmGenericHeader));

        if (SgAsmGenericHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericHeader* blkptr{reinterpret_cast<SgAsmGenericHeader*>(SgAsmGenericHeader::pools[blockIndex-1])};
                blkptr[SgAsmGenericHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericHeader::pool_size - 1};
    SgAsmGenericHeader* testPointer{reinterpret_cast<SgAsmGenericHeader*>(SgAsmGenericHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericHeader::pool_size * (SgAsmGenericHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericHeaderStroageClass
 * from its counterpart of SgAsmGenericHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericHeader::initializeStorageClassArray(SgAsmGenericHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericHeader::pools.begin(); block != SgAsmGenericHeader::pools.end(); ++block) {
        SgAsmGenericHeader* pointer{reinterpret_cast<SgAsmGenericHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 209890 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDOSFileHeader that takes its corresponding StorageClass as parameter
 */
SgAsmDOSFileHeader::SgAsmDOSFileHeader(const SgAsmDOSFileHeaderStorageClass& storageSource)  : SgAsmGenericHeader (storageSource) {

/* #line 209900 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_e_last_page_size = storageSource.storageOf_e_last_page_size;
    p_e_total_pages = storageSource.storageOf_e_total_pages;
    p_e_nrelocs = storageSource.storageOf_e_nrelocs;
    p_e_header_paragraphs = storageSource.storageOf_e_header_paragraphs;
    p_e_minalloc = storageSource.storageOf_e_minalloc;
    p_e_maxalloc = storageSource.storageOf_e_maxalloc;
    p_e_ss = storageSource.storageOf_e_ss;
    p_e_sp = storageSource.storageOf_e_sp;
    p_e_cksum = storageSource.storageOf_e_cksum;
    p_e_ip = storageSource.storageOf_e_ip;
    p_e_cs = storageSource.storageOf_e_cs;
    p_e_overlay = storageSource.storageOf_e_overlay;
    p_e_relocs_offset = storageSource.storageOf_e_relocs_offset;
    p_e_res1 = storageSource.storageOf_e_res1;
     p_relocs = (SgAsmGenericSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_relocs));
     p_rm_section = (SgAsmGenericSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rm_section));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDOSFileHeader*
SgAsmDOSFileHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDOSFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDOSFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDOSFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDOSFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmDOSFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDOSFileHeader::pool_size};
        returnPointer = &(((SgAsmDOSFileHeader*)(SgAsmDOSFileHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDOSFileHeader*
SgAsmDOSFileHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDOSFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDOSFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDOSFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDOSFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDOSFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmDOSFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDOSFileHeader::pool_size};
        returnPointer = &(((SgAsmDOSFileHeader*)(SgAsmDOSFileHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDOSFileHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDOSFileHeader::pools.begin(); block != SgAsmDOSFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDOSFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmDOSFileHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDOSFileHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDOSFileHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDOSFileHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDOSFileHeader::pools.begin(); block != SgAsmDOSFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDOSFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmDOSFileHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDOSFileHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDOSFileHeader::clearMemoryPool() {
    SgAsmDOSFileHeader* tempPointer{nullptr};
    if (SgAsmDOSFileHeader::pools.empty() == false) {
        auto block{SgAsmDOSFileHeader::pools.begin()};
        SgAsmDOSFileHeader::next_node = reinterpret_cast<SgAsmDOSFileHeader*>(*block);
        for (; block != SgAsmDOSFileHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDOSFileHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDOSFileHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDOSFileHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDOSFileHeader::pool_size-1]);
        }
    }
}

void
SgAsmDOSFileHeader::deleteMemoryPool() {
    for (auto p: SgAsmDOSFileHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmDOSFileHeader::next_node = nullptr;
    SgAsmDOSFileHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDOSFileHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSFileHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDOSFileHeader)};
    for (size_t blockIndex = SgAsmDOSFileHeader::pools.size(); (blockIndex * SgAsmDOSFileHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSFileHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDOSFileHeader) = %" PRIuPTR " SgAsmDOSFileHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDOSFileHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDOSFileHeader),SgAsmDOSFileHeader::pool_size);
        }
#endif

        SgAsmDOSFileHeader* pointer{static_cast<SgAsmDOSFileHeader*>(ROSE_MALLOC(SgAsmDOSFileHeader::pool_size * sizeof(SgAsmDOSFileHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDOSFileHeader::pool_size * sizeof(SgAsmDOSFileHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDOSFileHeader::pool_size * sizeof(SgAsmDOSFileHeader));
#endif
        SgAsmDOSFileHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDOSFileHeader::pool_size * sizeof(SgAsmDOSFileHeader), V_SgAsmDOSFileHeader));

        if (SgAsmDOSFileHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDOSFileHeader* blkptr{reinterpret_cast<SgAsmDOSFileHeader*>(SgAsmDOSFileHeader::pools[blockIndex-1])};
                blkptr[SgAsmDOSFileHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDOSFileHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDOSFileHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDOSFileHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDOSFileHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDOSFileHeader::pool_size - 1};
    SgAsmDOSFileHeader* testPointer{reinterpret_cast<SgAsmDOSFileHeader*>(SgAsmDOSFileHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDOSFileHeader::pool_size * (SgAsmDOSFileHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDOSFileHeaderStroageClass
 * from its counterpart of SgAsmDOSFileHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDOSFileHeader::initializeStorageClassArray(SgAsmDOSFileHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDOSFileHeader::pools.begin(); block != SgAsmDOSFileHeader::pools.end(); ++block) {
        SgAsmDOSFileHeader* pointer{reinterpret_cast<SgAsmDOSFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmDOSFileHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 210133 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfFileHeader that takes its corresponding StorageClass as parameter
 */
SgAsmElfFileHeader::SgAsmElfFileHeader(const SgAsmElfFileHeaderStorageClass& storageSource)  : SgAsmGenericHeader (storageSource) {

/* #line 210143 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_e_ident_file_class = storageSource.storageOf_e_ident_file_class;
    p_e_ident_data_encoding = storageSource.storageOf_e_ident_data_encoding;
    p_e_ident_file_version = storageSource.storageOf_e_ident_file_version;
    p_e_ident_padding = storageSource.storageOf_e_ident_padding.rebuildDataStoredInEasyStorageClass();
    p_e_type = storageSource.storageOf_e_type;
    p_e_machine = storageSource.storageOf_e_machine;
    p_e_flags = storageSource.storageOf_e_flags;
    p_e_ehsize = storageSource.storageOf_e_ehsize;
    p_phextrasz = storageSource.storageOf_phextrasz;
    p_e_phnum = storageSource.storageOf_e_phnum;
    p_shextrasz = storageSource.storageOf_shextrasz;
    p_e_shnum = storageSource.storageOf_e_shnum;
    p_e_shstrndx = storageSource.storageOf_e_shstrndx;
     p_sectionTable = (SgAsmElfSectionTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sectionTable));
     p_segmentTable = (SgAsmElfSegmentTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_segmentTable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfFileHeader*
SgAsmElfFileHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmElfFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfFileHeader::pool_size};
        returnPointer = &(((SgAsmElfFileHeader*)(SgAsmElfFileHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfFileHeader*
SgAsmElfFileHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmElfFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfFileHeader::pool_size};
        returnPointer = &(((SgAsmElfFileHeader*)(SgAsmElfFileHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfFileHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfFileHeader::pools.begin(); block != SgAsmElfFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmElfFileHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfFileHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfFileHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfFileHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfFileHeader::pools.begin(); block != SgAsmElfFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmElfFileHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfFileHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfFileHeader::clearMemoryPool() {
    SgAsmElfFileHeader* tempPointer{nullptr};
    if (SgAsmElfFileHeader::pools.empty() == false) {
        auto block{SgAsmElfFileHeader::pools.begin()};
        SgAsmElfFileHeader::next_node = reinterpret_cast<SgAsmElfFileHeader*>(*block);
        for (; block != SgAsmElfFileHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfFileHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfFileHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfFileHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfFileHeader::pool_size-1]);
        }
    }
}

void
SgAsmElfFileHeader::deleteMemoryPool() {
    for (auto p: SgAsmElfFileHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfFileHeader::next_node = nullptr;
    SgAsmElfFileHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfFileHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfFileHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfFileHeader)};
    for (size_t blockIndex = SgAsmElfFileHeader::pools.size(); (blockIndex * SgAsmElfFileHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfFileHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfFileHeader) = %" PRIuPTR " SgAsmElfFileHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfFileHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfFileHeader),SgAsmElfFileHeader::pool_size);
        }
#endif

        SgAsmElfFileHeader* pointer{static_cast<SgAsmElfFileHeader*>(ROSE_MALLOC(SgAsmElfFileHeader::pool_size * sizeof(SgAsmElfFileHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfFileHeader::pool_size * sizeof(SgAsmElfFileHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfFileHeader::pool_size * sizeof(SgAsmElfFileHeader));
#endif
        SgAsmElfFileHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfFileHeader::pool_size * sizeof(SgAsmElfFileHeader), V_SgAsmElfFileHeader));

        if (SgAsmElfFileHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfFileHeader* blkptr{reinterpret_cast<SgAsmElfFileHeader*>(SgAsmElfFileHeader::pools[blockIndex-1])};
                blkptr[SgAsmElfFileHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfFileHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfFileHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfFileHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfFileHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfFileHeader::pool_size - 1};
    SgAsmElfFileHeader* testPointer{reinterpret_cast<SgAsmElfFileHeader*>(SgAsmElfFileHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfFileHeader::pool_size * (SgAsmElfFileHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfFileHeaderStroageClass
 * from its counterpart of SgAsmElfFileHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfFileHeader::initializeStorageClassArray(SgAsmElfFileHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfFileHeader::pools.begin(); block != SgAsmElfFileHeader::pools.end(); ++block) {
        SgAsmElfFileHeader* pointer{reinterpret_cast<SgAsmElfFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmElfFileHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 210375 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmFileHeader that takes its corresponding StorageClass as parameter
 */
SgAsmJvmFileHeader::SgAsmJvmFileHeader(const SgAsmJvmFileHeaderStorageClass& storageSource)  : SgAsmGenericHeader (storageSource) {

/* #line 210385 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_minor_version = storageSource.storageOf_minor_version;
    p_major_version = storageSource.storageOf_major_version;
    p_access_flags = storageSource.storageOf_access_flags;
    p_this_class = storageSource.storageOf_this_class;
    p_super_class = storageSource.storageOf_super_class;
     p_constant_pool = (SgAsmJvmConstantPool*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_constant_pool));
    p_interfaces = storageSource.storageOf_interfaces.rebuildDataStoredInEasyStorageClass();
     p_field_table = (SgAsmJvmFieldTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_field_table));
     p_method_table = (SgAsmJvmMethodTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_method_table));
     p_attribute_table = (SgAsmJvmAttributeTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_attribute_table));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmFileHeader*
SgAsmJvmFileHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmJvmFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmFileHeader::pool_size};
        returnPointer = &(((SgAsmJvmFileHeader*)(SgAsmJvmFileHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmFileHeader*
SgAsmJvmFileHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmJvmFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmFileHeader::pool_size};
        returnPointer = &(((SgAsmJvmFileHeader*)(SgAsmJvmFileHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmFileHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmFileHeader::pools.begin(); block != SgAsmJvmFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmFileHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmFileHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmFileHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmFileHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmFileHeader::pools.begin(); block != SgAsmJvmFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmFileHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmFileHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmFileHeader::clearMemoryPool() {
    SgAsmJvmFileHeader* tempPointer{nullptr};
    if (SgAsmJvmFileHeader::pools.empty() == false) {
        auto block{SgAsmJvmFileHeader::pools.begin()};
        SgAsmJvmFileHeader::next_node = reinterpret_cast<SgAsmJvmFileHeader*>(*block);
        for (; block != SgAsmJvmFileHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmFileHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmFileHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmFileHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmFileHeader::pool_size-1]);
        }
    }
}

void
SgAsmJvmFileHeader::deleteMemoryPool() {
    for (auto p: SgAsmJvmFileHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmFileHeader::next_node = nullptr;
    SgAsmJvmFileHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmFileHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFileHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmFileHeader)};
    for (size_t blockIndex = SgAsmJvmFileHeader::pools.size(); (blockIndex * SgAsmJvmFileHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFileHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmFileHeader) = %" PRIuPTR " SgAsmJvmFileHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFileHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmFileHeader),SgAsmJvmFileHeader::pool_size);
        }
#endif

        SgAsmJvmFileHeader* pointer{static_cast<SgAsmJvmFileHeader*>(ROSE_MALLOC(SgAsmJvmFileHeader::pool_size * sizeof(SgAsmJvmFileHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmFileHeader::pool_size * sizeof(SgAsmJvmFileHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmFileHeader::pool_size * sizeof(SgAsmJvmFileHeader));
#endif
        SgAsmJvmFileHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmFileHeader::pool_size * sizeof(SgAsmJvmFileHeader), V_SgAsmJvmFileHeader));

        if (SgAsmJvmFileHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmFileHeader* blkptr{reinterpret_cast<SgAsmJvmFileHeader*>(SgAsmJvmFileHeader::pools[blockIndex-1])};
                blkptr[SgAsmJvmFileHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmFileHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmFileHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmFileHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmFileHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmFileHeader::pool_size - 1};
    SgAsmJvmFileHeader* testPointer{reinterpret_cast<SgAsmJvmFileHeader*>(SgAsmJvmFileHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmFileHeader::pool_size * (SgAsmJvmFileHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmFileHeaderStroageClass
 * from its counterpart of SgAsmJvmFileHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmFileHeader::initializeStorageClassArray(SgAsmJvmFileHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmFileHeader::pools.begin(); block != SgAsmJvmFileHeader::pools.end(); ++block) {
        SgAsmJvmFileHeader* pointer{reinterpret_cast<SgAsmJvmFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmFileHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 210612 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLEFileHeader that takes its corresponding StorageClass as parameter
 */
SgAsmLEFileHeader::SgAsmLEFileHeader(const SgAsmLEFileHeaderStorageClass& storageSource)  : SgAsmGenericHeader (storageSource) {

/* #line 210622 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_e_byte_order = storageSource.storageOf_e_byte_order;
    p_e_word_order = storageSource.storageOf_e_word_order;
    p_e_format_level = storageSource.storageOf_e_format_level;
    p_e_cpu_type = storageSource.storageOf_e_cpu_type;
    p_e_os_type = storageSource.storageOf_e_os_type;
    p_e_module_version = storageSource.storageOf_e_module_version;
    p_e_flags = storageSource.storageOf_e_flags;
    p_e_eip_section = storageSource.storageOf_e_eip_section;
    p_e_esp_section = storageSource.storageOf_e_esp_section;
    p_e_last_page_size = storageSource.storageOf_e_last_page_size;
    p_e_page_offset_shift = storageSource.storageOf_e_page_offset_shift;
    p_e_fixup_sect_cksum = storageSource.storageOf_e_fixup_sect_cksum;
    p_e_loader_sect_cksum = storageSource.storageOf_e_loader_sect_cksum;
    p_e_secttab_nentries = storageSource.storageOf_e_secttab_nentries;
    p_e_rsrctab_nentries = storageSource.storageOf_e_rsrctab_nentries;
    p_e_fmtdirtab_nentries = storageSource.storageOf_e_fmtdirtab_nentries;
    p_e_import_modtab_nentries = storageSource.storageOf_e_import_modtab_nentries;
    p_e_preload_npages = storageSource.storageOf_e_preload_npages;
    p_e_nonresnametab_size = storageSource.storageOf_e_nonresnametab_size;
    p_e_nonresnametab_cksum = storageSource.storageOf_e_nonresnametab_cksum;
    p_e_auto_ds_section = storageSource.storageOf_e_auto_ds_section;
    p_e_debug_info_size = storageSource.storageOf_e_debug_info_size;
    p_e_num_instance_preload = storageSource.storageOf_e_num_instance_preload;
    p_e_num_instance_demand = storageSource.storageOf_e_num_instance_demand;
    p_e_heap_size = storageSource.storageOf_e_heap_size;
    p_e_npages = storageSource.storageOf_e_npages;
    p_e_eip = storageSource.storageOf_e_eip;
    p_e_esp = storageSource.storageOf_e_esp;
    p_e_page_size = storageSource.storageOf_e_page_size;
    p_e_fixup_sect_size = storageSource.storageOf_e_fixup_sect_size;
    p_e_loader_sect_size = storageSource.storageOf_e_loader_sect_size;
    p_e_secttab_rfo = storageSource.storageOf_e_secttab_rfo;
    p_e_pagetab_rfo = storageSource.storageOf_e_pagetab_rfo;
    p_e_iterpages_offset = storageSource.storageOf_e_iterpages_offset;
    p_e_rsrctab_rfo = storageSource.storageOf_e_rsrctab_rfo;
    p_e_resnametab_rfo = storageSource.storageOf_e_resnametab_rfo;
    p_e_entrytab_rfo = storageSource.storageOf_e_entrytab_rfo;
    p_e_fmtdirtab_rfo = storageSource.storageOf_e_fmtdirtab_rfo;
    p_e_fixup_pagetab_rfo = storageSource.storageOf_e_fixup_pagetab_rfo;
    p_e_fixup_rectab_rfo = storageSource.storageOf_e_fixup_rectab_rfo;
    p_e_import_modtab_rfo = storageSource.storageOf_e_import_modtab_rfo;
    p_e_import_proctab_rfo = storageSource.storageOf_e_import_proctab_rfo;
    p_e_ppcksumtab_rfo = storageSource.storageOf_e_ppcksumtab_rfo;
    p_e_data_pages_offset = storageSource.storageOf_e_data_pages_offset;
    p_e_nonresnametab_offset = storageSource.storageOf_e_nonresnametab_offset;
    p_e_debug_info_rfo = storageSource.storageOf_e_debug_info_rfo;
     p_dos2Header = (SgAsmDOSExtendedHeader*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dos2Header));
     p_sectionTable = (SgAsmLESectionTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sectionTable));
     p_pageTable = (SgAsmLEPageTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_pageTable));
     p_residentNameTable = (SgAsmLENameTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_residentNameTable));
     p_nonresidentNameTable = (SgAsmLENameTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonresidentNameTable));
     p_entryTable = (SgAsmLEEntryTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entryTable));
     p_relocationTable = (SgAsmLERelocTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_relocationTable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLEFileHeader*
SgAsmLEFileHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLEFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmLEFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEFileHeader::pool_size};
        returnPointer = &(((SgAsmLEFileHeader*)(SgAsmLEFileHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLEFileHeader*
SgAsmLEFileHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLEFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLEFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmLEFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEFileHeader::pool_size};
        returnPointer = &(((SgAsmLEFileHeader*)(SgAsmLEFileHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLEFileHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLEFileHeader::pools.begin(); block != SgAsmLEFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmLEFileHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLEFileHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLEFileHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLEFileHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLEFileHeader::pools.begin(); block != SgAsmLEFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmLEFileHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLEFileHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLEFileHeader::clearMemoryPool() {
    SgAsmLEFileHeader* tempPointer{nullptr};
    if (SgAsmLEFileHeader::pools.empty() == false) {
        auto block{SgAsmLEFileHeader::pools.begin()};
        SgAsmLEFileHeader::next_node = reinterpret_cast<SgAsmLEFileHeader*>(*block);
        for (; block != SgAsmLEFileHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLEFileHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLEFileHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLEFileHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLEFileHeader::pool_size-1]);
        }
    }
}

void
SgAsmLEFileHeader::deleteMemoryPool() {
    for (auto p: SgAsmLEFileHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmLEFileHeader::next_node = nullptr;
    SgAsmLEFileHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLEFileHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEFileHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEFileHeader)};
    for (size_t blockIndex = SgAsmLEFileHeader::pools.size(); (blockIndex * SgAsmLEFileHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEFileHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEFileHeader) = %" PRIuPTR " SgAsmLEFileHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEFileHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEFileHeader),SgAsmLEFileHeader::pool_size);
        }
#endif

        SgAsmLEFileHeader* pointer{static_cast<SgAsmLEFileHeader*>(ROSE_MALLOC(SgAsmLEFileHeader::pool_size * sizeof(SgAsmLEFileHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLEFileHeader::pool_size * sizeof(SgAsmLEFileHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLEFileHeader::pool_size * sizeof(SgAsmLEFileHeader));
#endif
        SgAsmLEFileHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLEFileHeader::pool_size * sizeof(SgAsmLEFileHeader), V_SgAsmLEFileHeader));

        if (SgAsmLEFileHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLEFileHeader* blkptr{reinterpret_cast<SgAsmLEFileHeader*>(SgAsmLEFileHeader::pools[blockIndex-1])};
                blkptr[SgAsmLEFileHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLEFileHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLEFileHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLEFileHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLEFileHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLEFileHeader::pool_size - 1};
    SgAsmLEFileHeader* testPointer{reinterpret_cast<SgAsmLEFileHeader*>(SgAsmLEFileHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLEFileHeader::pool_size * (SgAsmLEFileHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLEFileHeaderStroageClass
 * from its counterpart of SgAsmLEFileHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLEFileHeader::initializeStorageClassArray(SgAsmLEFileHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLEFileHeader::pools.begin(); block != SgAsmLEFileHeader::pools.end(); ++block) {
        SgAsmLEFileHeader* pointer{reinterpret_cast<SgAsmLEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmLEFileHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 210892 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNEFileHeader that takes its corresponding StorageClass as parameter
 */
SgAsmNEFileHeader::SgAsmNEFileHeader(const SgAsmNEFileHeaderStorageClass& storageSource)  : SgAsmGenericHeader (storageSource) {

/* #line 210902 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_e_linker_major = storageSource.storageOf_e_linker_major;
    p_e_linker_minor = storageSource.storageOf_e_linker_minor;
    p_e_checksum = storageSource.storageOf_e_checksum;
    p_e_flags1 = storageSource.storageOf_e_flags1;
    p_e_autodata_sn = storageSource.storageOf_e_autodata_sn;
    p_e_bss_size = storageSource.storageOf_e_bss_size;
    p_e_stack_size = storageSource.storageOf_e_stack_size;
    p_e_csip = storageSource.storageOf_e_csip;
    p_e_sssp = storageSource.storageOf_e_sssp;
    p_e_nsections = storageSource.storageOf_e_nsections;
    p_e_nmodrefs = storageSource.storageOf_e_nmodrefs;
    p_e_nnonresnames = storageSource.storageOf_e_nnonresnames;
    p_e_nmovable_entries = storageSource.storageOf_e_nmovable_entries;
    p_e_sector_align = storageSource.storageOf_e_sector_align;
    p_e_nresources = storageSource.storageOf_e_nresources;
    p_e_exetype = storageSource.storageOf_e_exetype;
    p_e_flags2 = storageSource.storageOf_e_flags2;
    p_e_res1 = storageSource.storageOf_e_res1;
    p_e_winvers = storageSource.storageOf_e_winvers;
    p_e_entrytab_rfo = storageSource.storageOf_e_entrytab_rfo;
    p_e_entrytab_size = storageSource.storageOf_e_entrytab_size;
    p_e_sectab_rfo = storageSource.storageOf_e_sectab_rfo;
    p_e_rsrctab_rfo = storageSource.storageOf_e_rsrctab_rfo;
    p_e_resnametab_rfo = storageSource.storageOf_e_resnametab_rfo;
    p_e_modreftab_rfo = storageSource.storageOf_e_modreftab_rfo;
    p_e_importnametab_rfo = storageSource.storageOf_e_importnametab_rfo;
    p_e_nonresnametab_offset = storageSource.storageOf_e_nonresnametab_offset;
    p_e_fastload_sector = storageSource.storageOf_e_fastload_sector;
    p_e_fastload_nsectors = storageSource.storageOf_e_fastload_nsectors;
     p_dos2Header = (SgAsmDOSExtendedHeader*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dos2Header));
     p_sectionTable = (SgAsmNESectionTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sectionTable));
     p_residentNameTable = (SgAsmNENameTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_residentNameTable));
     p_nonresidentNameTable = (SgAsmNENameTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_nonresidentNameTable));
     p_moduleTable = (SgAsmNEModuleTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_moduleTable));
     p_entryTable = (SgAsmNEEntryTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_entryTable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNEFileHeader*
SgAsmNEFileHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNEFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmNEFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEFileHeader::pool_size};
        returnPointer = &(((SgAsmNEFileHeader*)(SgAsmNEFileHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNEFileHeader*
SgAsmNEFileHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNEFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNEFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmNEFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEFileHeader::pool_size};
        returnPointer = &(((SgAsmNEFileHeader*)(SgAsmNEFileHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNEFileHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNEFileHeader::pools.begin(); block != SgAsmNEFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmNEFileHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNEFileHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNEFileHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNEFileHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNEFileHeader::pools.begin(); block != SgAsmNEFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmNEFileHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNEFileHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNEFileHeader::clearMemoryPool() {
    SgAsmNEFileHeader* tempPointer{nullptr};
    if (SgAsmNEFileHeader::pools.empty() == false) {
        auto block{SgAsmNEFileHeader::pools.begin()};
        SgAsmNEFileHeader::next_node = reinterpret_cast<SgAsmNEFileHeader*>(*block);
        for (; block != SgAsmNEFileHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNEFileHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNEFileHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNEFileHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNEFileHeader::pool_size-1]);
        }
    }
}

void
SgAsmNEFileHeader::deleteMemoryPool() {
    for (auto p: SgAsmNEFileHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmNEFileHeader::next_node = nullptr;
    SgAsmNEFileHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNEFileHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEFileHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEFileHeader)};
    for (size_t blockIndex = SgAsmNEFileHeader::pools.size(); (blockIndex * SgAsmNEFileHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEFileHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEFileHeader) = %" PRIuPTR " SgAsmNEFileHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEFileHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEFileHeader),SgAsmNEFileHeader::pool_size);
        }
#endif

        SgAsmNEFileHeader* pointer{static_cast<SgAsmNEFileHeader*>(ROSE_MALLOC(SgAsmNEFileHeader::pool_size * sizeof(SgAsmNEFileHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNEFileHeader::pool_size * sizeof(SgAsmNEFileHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNEFileHeader::pool_size * sizeof(SgAsmNEFileHeader));
#endif
        SgAsmNEFileHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNEFileHeader::pool_size * sizeof(SgAsmNEFileHeader), V_SgAsmNEFileHeader));

        if (SgAsmNEFileHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNEFileHeader* blkptr{reinterpret_cast<SgAsmNEFileHeader*>(SgAsmNEFileHeader::pools[blockIndex-1])};
                blkptr[SgAsmNEFileHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNEFileHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNEFileHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNEFileHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNEFileHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNEFileHeader::pool_size - 1};
    SgAsmNEFileHeader* testPointer{reinterpret_cast<SgAsmNEFileHeader*>(SgAsmNEFileHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNEFileHeader::pool_size * (SgAsmNEFileHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNEFileHeaderStroageClass
 * from its counterpart of SgAsmNEFileHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNEFileHeader::initializeStorageClassArray(SgAsmNEFileHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNEFileHeader::pools.begin(); block != SgAsmNEFileHeader::pools.end(); ++block) {
        SgAsmNEFileHeader* pointer{reinterpret_cast<SgAsmNEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmNEFileHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 211154 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEFileHeader that takes its corresponding StorageClass as parameter
 */
SgAsmPEFileHeader::SgAsmPEFileHeader(const SgAsmPEFileHeaderStorageClass& storageSource)  : SgAsmGenericHeader (storageSource) {

/* #line 211164 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_e_cpu_type = storageSource.storageOf_e_cpu_type;
    p_e_nsections = storageSource.storageOf_e_nsections;
    p_e_time = storageSource.storageOf_e_time;
    p_e_coff_symtab = storageSource.storageOf_e_coff_symtab;
    p_e_nt_hdr_size = storageSource.storageOf_e_nt_hdr_size;
    p_e_coff_nsyms = storageSource.storageOf_e_coff_nsyms;
    p_e_flags = storageSource.storageOf_e_flags;
    p_e_opt_magic = storageSource.storageOf_e_opt_magic;
    p_e_lmajor = storageSource.storageOf_e_lmajor;
    p_e_lminor = storageSource.storageOf_e_lminor;
    p_e_code_size = storageSource.storageOf_e_code_size;
    p_e_data_size = storageSource.storageOf_e_data_size;
    p_e_bss_size = storageSource.storageOf_e_bss_size;
    p_e_code_rva = storageSource.storageOf_e_code_rva;
    p_e_data_rva = storageSource.storageOf_e_data_rva;
    p_e_section_align = storageSource.storageOf_e_section_align;
    p_e_file_align = storageSource.storageOf_e_file_align;
    p_e_os_major = storageSource.storageOf_e_os_major;
    p_e_os_minor = storageSource.storageOf_e_os_minor;
    p_e_user_major = storageSource.storageOf_e_user_major;
    p_e_user_minor = storageSource.storageOf_e_user_minor;
    p_e_subsys_major = storageSource.storageOf_e_subsys_major;
    p_e_subsys_minor = storageSource.storageOf_e_subsys_minor;
    p_e_reserved9 = storageSource.storageOf_e_reserved9;
    p_e_image_size = storageSource.storageOf_e_image_size;
    p_e_header_size = storageSource.storageOf_e_header_size;
    p_e_file_checksum = storageSource.storageOf_e_file_checksum;
    p_e_subsystem = storageSource.storageOf_e_subsystem;
    p_e_dll_flags = storageSource.storageOf_e_dll_flags;
    p_e_stack_reserve_size = storageSource.storageOf_e_stack_reserve_size;
    p_e_stack_commit_size = storageSource.storageOf_e_stack_commit_size;
    p_e_heap_reserve_size = storageSource.storageOf_e_heap_reserve_size;
    p_e_heap_commit_size = storageSource.storageOf_e_heap_commit_size;
    p_e_loader_flags = storageSource.storageOf_e_loader_flags;
    p_e_num_rvasize_pairs = storageSource.storageOf_e_num_rvasize_pairs;
     p_rvaSizePairs = (SgAsmPERVASizePairList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rvaSizePairs));
     p_sectionTable = (SgAsmPESectionTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sectionTable));
     p_coffSymbolTable = (SgAsmCoffSymbolTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_coffSymbolTable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEFileHeader*
SgAsmPEFileHeader::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmPEFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEFileHeader::pool_size};
        returnPointer = &(((SgAsmPEFileHeader*)(SgAsmPEFileHeader::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEFileHeader*
SgAsmPEFileHeader::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEFileHeader* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEFileHeader)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEFileHeader + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEFileHeader)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEFileHeader)};
        unsigned long positionInPool{localIndex % SgAsmPEFileHeader::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEFileHeader::pool_size};
        returnPointer = &(((SgAsmPEFileHeader*)(SgAsmPEFileHeader::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEFileHeader::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEFileHeader::pools.begin(); block != SgAsmPEFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmPEFileHeader::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEFileHeader*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEFileHeader::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEFileHeader* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEFileHeader::pools.begin(); block != SgAsmPEFileHeader::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmPEFileHeader::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEFileHeader::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEFileHeader::clearMemoryPool() {
    SgAsmPEFileHeader* tempPointer{nullptr};
    if (SgAsmPEFileHeader::pools.empty() == false) {
        auto block{SgAsmPEFileHeader::pools.begin()};
        SgAsmPEFileHeader::next_node = reinterpret_cast<SgAsmPEFileHeader*>(*block);
        for (; block != SgAsmPEFileHeader::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEFileHeader*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEFileHeader::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEFileHeader::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEFileHeader::pool_size-1]);
        }
    }
}

void
SgAsmPEFileHeader::deleteMemoryPool() {
    for (auto p: SgAsmPEFileHeader::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEFileHeader::next_node = nullptr;
    SgAsmPEFileHeader::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEFileHeader::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEFileHeader) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEFileHeader)};
    for (size_t blockIndex = SgAsmPEFileHeader::pools.size(); (blockIndex * SgAsmPEFileHeader::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEFileHeader) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEFileHeader) = %" PRIuPTR " SgAsmPEFileHeader::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEFileHeader),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEFileHeader),SgAsmPEFileHeader::pool_size);
        }
#endif

        SgAsmPEFileHeader* pointer{static_cast<SgAsmPEFileHeader*>(ROSE_MALLOC(SgAsmPEFileHeader::pool_size * sizeof(SgAsmPEFileHeader)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEFileHeader::pool_size * sizeof(SgAsmPEFileHeader));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEFileHeader::pool_size * sizeof(SgAsmPEFileHeader));
#endif
        SgAsmPEFileHeader::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEFileHeader::pool_size * sizeof(SgAsmPEFileHeader), V_SgAsmPEFileHeader));

        if (SgAsmPEFileHeader::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEFileHeader* blkptr{reinterpret_cast<SgAsmPEFileHeader*>(SgAsmPEFileHeader::pools[blockIndex-1])};
                blkptr[SgAsmPEFileHeader::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEFileHeader::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEFileHeader::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEFileHeader::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEFileHeader::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEFileHeader::pool_size - 1};
    SgAsmPEFileHeader* testPointer{reinterpret_cast<SgAsmPEFileHeader*>(SgAsmPEFileHeader::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEFileHeader::pool_size * (SgAsmPEFileHeader::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEFileHeaderStroageClass
 * from its counterpart of SgAsmPEFileHeader. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEFileHeader::initializeStorageClassArray(SgAsmPEFileHeaderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEFileHeader::pools.begin(); block != SgAsmPEFileHeader::pools.end(); ++block) {
        SgAsmPEFileHeader* pointer{reinterpret_cast<SgAsmPEFileHeader*>(*block)};
        for (unsigned i = 0; i < SgAsmPEFileHeader::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 211419 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmAttributeTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmAttributeTable::SgAsmJvmAttributeTable(const SgAsmJvmAttributeTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 211429 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_attributes = storageSource.storageOf_attributes.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmAttributePtrList::iterator i_attributes = p_attributes.begin();
    for ( ; i_attributes != p_attributes.end(); ++i_attributes) {
        (*i_attributes) = (SgAsmJvmAttributePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_attributes)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmAttributeTable*
SgAsmJvmAttributeTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmAttributeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAttributeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAttributeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAttributeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttributeTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmAttributeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmAttributeTable::pool_size};
        returnPointer = &(((SgAsmJvmAttributeTable*)(SgAsmJvmAttributeTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmAttributeTable*
SgAsmJvmAttributeTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmAttributeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAttributeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAttributeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAttributeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmAttributeTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmAttributeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmAttributeTable::pool_size};
        returnPointer = &(((SgAsmJvmAttributeTable*)(SgAsmJvmAttributeTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmAttributeTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmAttributeTable::pools.begin(); block != SgAsmJvmAttributeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmAttributeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAttributeTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmAttributeTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmAttributeTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmAttributeTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmAttributeTable::pools.begin(); block != SgAsmJvmAttributeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmAttributeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAttributeTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmAttributeTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmAttributeTable::clearMemoryPool() {
    SgAsmJvmAttributeTable* tempPointer{nullptr};
    if (SgAsmJvmAttributeTable::pools.empty() == false) {
        auto block{SgAsmJvmAttributeTable::pools.begin()};
        SgAsmJvmAttributeTable::next_node = reinterpret_cast<SgAsmJvmAttributeTable*>(*block);
        for (; block != SgAsmJvmAttributeTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmAttributeTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmAttributeTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmAttributeTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmAttributeTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmAttributeTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmAttributeTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmAttributeTable::next_node = nullptr;
    SgAsmJvmAttributeTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmAttributeTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttributeTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAttributeTable)};
    for (size_t blockIndex = SgAsmJvmAttributeTable::pools.size(); (blockIndex * SgAsmJvmAttributeTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttributeTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAttributeTable) = %" PRIuPTR " SgAsmJvmAttributeTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttributeTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAttributeTable),SgAsmJvmAttributeTable::pool_size);
        }
#endif

        SgAsmJvmAttributeTable* pointer{static_cast<SgAsmJvmAttributeTable*>(ROSE_MALLOC(SgAsmJvmAttributeTable::pool_size * sizeof(SgAsmJvmAttributeTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmAttributeTable::pool_size * sizeof(SgAsmJvmAttributeTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmAttributeTable::pool_size * sizeof(SgAsmJvmAttributeTable));
#endif
        SgAsmJvmAttributeTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmAttributeTable::pool_size * sizeof(SgAsmJvmAttributeTable), V_SgAsmJvmAttributeTable));

        if (SgAsmJvmAttributeTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmAttributeTable* blkptr{reinterpret_cast<SgAsmJvmAttributeTable*>(SgAsmJvmAttributeTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmAttributeTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmAttributeTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmAttributeTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmAttributeTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmAttributeTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmAttributeTable::pool_size - 1};
    SgAsmJvmAttributeTable* testPointer{reinterpret_cast<SgAsmJvmAttributeTable*>(SgAsmJvmAttributeTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmAttributeTable::pool_size * (SgAsmJvmAttributeTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmAttributeTableStroageClass
 * from its counterpart of SgAsmJvmAttributeTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmAttributeTable::initializeStorageClassArray(SgAsmJvmAttributeTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmAttributeTable::pools.begin(); block != SgAsmJvmAttributeTable::pools.end(); ++block) {
        SgAsmJvmAttributeTable* pointer{reinterpret_cast<SgAsmJvmAttributeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAttributeTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 211651 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmConstantPool that takes its corresponding StorageClass as parameter
 */
SgAsmJvmConstantPool::SgAsmJvmConstantPool(const SgAsmJvmConstantPoolStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 211661 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmConstantPoolEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmJvmConstantPoolEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmConstantPool*
SgAsmJvmConstantPool::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmConstantPool* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmConstantPool)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmConstantPool + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmConstantPool)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPool)};
        unsigned long positionInPool{localIndex % SgAsmJvmConstantPool::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmConstantPool::pool_size};
        returnPointer = &(((SgAsmJvmConstantPool*)(SgAsmJvmConstantPool::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmConstantPool*
SgAsmJvmConstantPool::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmConstantPool* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmConstantPool)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmConstantPool + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmConstantPool)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmConstantPool)};
        unsigned long positionInPool{localIndex % SgAsmJvmConstantPool::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmConstantPool::pool_size};
        returnPointer = &(((SgAsmJvmConstantPool*)(SgAsmJvmConstantPool::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmConstantPool::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmConstantPool::pools.begin(); block != SgAsmJvmConstantPool::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmConstantPool*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmConstantPool::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmConstantPool*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmConstantPool::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmConstantPool* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmConstantPool::pools.begin(); block != SgAsmJvmConstantPool::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmConstantPool*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmConstantPool::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmConstantPool::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmConstantPool::clearMemoryPool() {
    SgAsmJvmConstantPool* tempPointer{nullptr};
    if (SgAsmJvmConstantPool::pools.empty() == false) {
        auto block{SgAsmJvmConstantPool::pools.begin()};
        SgAsmJvmConstantPool::next_node = reinterpret_cast<SgAsmJvmConstantPool*>(*block);
        for (; block != SgAsmJvmConstantPool::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmConstantPool*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmConstantPool::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmConstantPool::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmConstantPool::pool_size-1]);
        }
    }
}

void
SgAsmJvmConstantPool::deleteMemoryPool() {
    for (auto p: SgAsmJvmConstantPool::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmConstantPool::next_node = nullptr;
    SgAsmJvmConstantPool::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmConstantPool::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPool) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmConstantPool)};
    for (size_t blockIndex = SgAsmJvmConstantPool::pools.size(); (blockIndex * SgAsmJvmConstantPool::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPool) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmConstantPool) = %" PRIuPTR " SgAsmJvmConstantPool::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPool),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmConstantPool),SgAsmJvmConstantPool::pool_size);
        }
#endif

        SgAsmJvmConstantPool* pointer{static_cast<SgAsmJvmConstantPool*>(ROSE_MALLOC(SgAsmJvmConstantPool::pool_size * sizeof(SgAsmJvmConstantPool)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmConstantPool::pool_size * sizeof(SgAsmJvmConstantPool));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmConstantPool::pool_size * sizeof(SgAsmJvmConstantPool));
#endif
        SgAsmJvmConstantPool::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmConstantPool::pool_size * sizeof(SgAsmJvmConstantPool), V_SgAsmJvmConstantPool));

        if (SgAsmJvmConstantPool::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmConstantPool* blkptr{reinterpret_cast<SgAsmJvmConstantPool*>(SgAsmJvmConstantPool::pools[blockIndex-1])};
                blkptr[SgAsmJvmConstantPool::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmConstantPool::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmConstantPool::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmConstantPool::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmConstantPool::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmConstantPool::pool_size - 1};
    SgAsmJvmConstantPool* testPointer{reinterpret_cast<SgAsmJvmConstantPool*>(SgAsmJvmConstantPool::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmConstantPool::pool_size * (SgAsmJvmConstantPool::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmConstantPoolStroageClass
 * from its counterpart of SgAsmJvmConstantPool. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmConstantPool::initializeStorageClassArray(SgAsmJvmConstantPoolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmConstantPool::pools.begin(); block != SgAsmJvmConstantPool::pools.end(); ++block) {
        SgAsmJvmConstantPool* pointer{reinterpret_cast<SgAsmJvmConstantPool*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmConstantPool::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 211883 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmFieldTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmFieldTable::SgAsmJvmFieldTable(const SgAsmJvmFieldTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 211893 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_fields = storageSource.storageOf_fields.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmFieldPtrList::iterator i_fields = p_fields.begin();
    for ( ; i_fields != p_fields.end(); ++i_fields) {
        (*i_fields) = (SgAsmJvmFieldPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_fields)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmFieldTable*
SgAsmJvmFieldTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmFieldTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmFieldTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmFieldTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmFieldTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFieldTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmFieldTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmFieldTable::pool_size};
        returnPointer = &(((SgAsmJvmFieldTable*)(SgAsmJvmFieldTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmFieldTable*
SgAsmJvmFieldTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmFieldTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmFieldTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmFieldTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmFieldTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmFieldTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmFieldTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmFieldTable::pool_size};
        returnPointer = &(((SgAsmJvmFieldTable*)(SgAsmJvmFieldTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmFieldTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmFieldTable::pools.begin(); block != SgAsmJvmFieldTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmFieldTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmFieldTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmFieldTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmFieldTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmFieldTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmFieldTable::pools.begin(); block != SgAsmJvmFieldTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmFieldTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmFieldTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmFieldTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmFieldTable::clearMemoryPool() {
    SgAsmJvmFieldTable* tempPointer{nullptr};
    if (SgAsmJvmFieldTable::pools.empty() == false) {
        auto block{SgAsmJvmFieldTable::pools.begin()};
        SgAsmJvmFieldTable::next_node = reinterpret_cast<SgAsmJvmFieldTable*>(*block);
        for (; block != SgAsmJvmFieldTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmFieldTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmFieldTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmFieldTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmFieldTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmFieldTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmFieldTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmFieldTable::next_node = nullptr;
    SgAsmJvmFieldTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmFieldTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFieldTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmFieldTable)};
    for (size_t blockIndex = SgAsmJvmFieldTable::pools.size(); (blockIndex * SgAsmJvmFieldTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFieldTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmFieldTable) = %" PRIuPTR " SgAsmJvmFieldTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmFieldTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmFieldTable),SgAsmJvmFieldTable::pool_size);
        }
#endif

        SgAsmJvmFieldTable* pointer{static_cast<SgAsmJvmFieldTable*>(ROSE_MALLOC(SgAsmJvmFieldTable::pool_size * sizeof(SgAsmJvmFieldTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmFieldTable::pool_size * sizeof(SgAsmJvmFieldTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmFieldTable::pool_size * sizeof(SgAsmJvmFieldTable));
#endif
        SgAsmJvmFieldTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmFieldTable::pool_size * sizeof(SgAsmJvmFieldTable), V_SgAsmJvmFieldTable));

        if (SgAsmJvmFieldTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmFieldTable* blkptr{reinterpret_cast<SgAsmJvmFieldTable*>(SgAsmJvmFieldTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmFieldTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmFieldTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmFieldTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmFieldTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmFieldTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmFieldTable::pool_size - 1};
    SgAsmJvmFieldTable* testPointer{reinterpret_cast<SgAsmJvmFieldTable*>(SgAsmJvmFieldTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmFieldTable::pool_size * (SgAsmJvmFieldTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmFieldTableStroageClass
 * from its counterpart of SgAsmJvmFieldTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmFieldTable::initializeStorageClassArray(SgAsmJvmFieldTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmFieldTable::pools.begin(); block != SgAsmJvmFieldTable::pools.end(); ++block) {
        SgAsmJvmFieldTable* pointer{reinterpret_cast<SgAsmJvmFieldTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmFieldTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 212115 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmMethodTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmMethodTable::SgAsmJvmMethodTable(const SgAsmJvmMethodTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 212125 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_methods = storageSource.storageOf_methods.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmMethodPtrList::iterator i_methods = p_methods.begin();
    for ( ; i_methods != p_methods.end(); ++i_methods) {
        (*i_methods) = (SgAsmJvmMethodPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_methods)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmMethodTable*
SgAsmJvmMethodTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmMethodTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethodTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethodTable::pool_size};
        returnPointer = &(((SgAsmJvmMethodTable*)(SgAsmJvmMethodTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmMethodTable*
SgAsmJvmMethodTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmMethodTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmMethodTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethodTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethodTable::pool_size};
        returnPointer = &(((SgAsmJvmMethodTable*)(SgAsmJvmMethodTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmMethodTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmMethodTable::pools.begin(); block != SgAsmJvmMethodTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethodTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmMethodTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmMethodTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmMethodTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmMethodTable::pools.begin(); block != SgAsmJvmMethodTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethodTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmMethodTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmMethodTable::clearMemoryPool() {
    SgAsmJvmMethodTable* tempPointer{nullptr};
    if (SgAsmJvmMethodTable::pools.empty() == false) {
        auto block{SgAsmJvmMethodTable::pools.begin()};
        SgAsmJvmMethodTable::next_node = reinterpret_cast<SgAsmJvmMethodTable*>(*block);
        for (; block != SgAsmJvmMethodTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmMethodTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmMethodTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmMethodTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmMethodTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmMethodTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmMethodTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmMethodTable::next_node = nullptr;
    SgAsmJvmMethodTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmMethodTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodTable)};
    for (size_t blockIndex = SgAsmJvmMethodTable::pools.size(); (blockIndex * SgAsmJvmMethodTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodTable) = %" PRIuPTR " SgAsmJvmMethodTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodTable),SgAsmJvmMethodTable::pool_size);
        }
#endif

        SgAsmJvmMethodTable* pointer{static_cast<SgAsmJvmMethodTable*>(ROSE_MALLOC(SgAsmJvmMethodTable::pool_size * sizeof(SgAsmJvmMethodTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmMethodTable::pool_size * sizeof(SgAsmJvmMethodTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmMethodTable::pool_size * sizeof(SgAsmJvmMethodTable));
#endif
        SgAsmJvmMethodTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmMethodTable::pool_size * sizeof(SgAsmJvmMethodTable), V_SgAsmJvmMethodTable));

        if (SgAsmJvmMethodTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmMethodTable* blkptr{reinterpret_cast<SgAsmJvmMethodTable*>(SgAsmJvmMethodTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmMethodTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmMethodTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmMethodTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmMethodTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmMethodTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmMethodTable::pool_size - 1};
    SgAsmJvmMethodTable* testPointer{reinterpret_cast<SgAsmJvmMethodTable*>(SgAsmJvmMethodTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmMethodTable::pool_size * (SgAsmJvmMethodTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmMethodTableStroageClass
 * from its counterpart of SgAsmJvmMethodTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmMethodTable::initializeStorageClassArray(SgAsmJvmMethodTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmMethodTable::pools.begin(); block != SgAsmJvmMethodTable::pools.end(); ++block) {
        SgAsmJvmMethodTable* pointer{reinterpret_cast<SgAsmJvmMethodTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 212347 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLEEntryTable that takes its corresponding StorageClass as parameter
 */
SgAsmLEEntryTable::SgAsmLEEntryTable(const SgAsmLEEntryTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 212357 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_bundle_sizes = storageSource.storageOf_bundle_sizes.rebuildDataStoredInEasyStorageClass();
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmLEEntryPointPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmLEEntryPointPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLEEntryTable*
SgAsmLEEntryTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLEEntryTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEEntryTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEEntryTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEEntryTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryTable)};
        unsigned long positionInPool{localIndex % SgAsmLEEntryTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEEntryTable::pool_size};
        returnPointer = &(((SgAsmLEEntryTable*)(SgAsmLEEntryTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLEEntryTable*
SgAsmLEEntryTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLEEntryTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEEntryTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEEntryTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEEntryTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLEEntryTable)};
        unsigned long positionInPool{localIndex % SgAsmLEEntryTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEEntryTable::pool_size};
        returnPointer = &(((SgAsmLEEntryTable*)(SgAsmLEEntryTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLEEntryTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLEEntryTable::pools.begin(); block != SgAsmLEEntryTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEEntryTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLEEntryTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLEEntryTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLEEntryTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLEEntryTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLEEntryTable::pools.begin(); block != SgAsmLEEntryTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEEntryTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLEEntryTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLEEntryTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLEEntryTable::clearMemoryPool() {
    SgAsmLEEntryTable* tempPointer{nullptr};
    if (SgAsmLEEntryTable::pools.empty() == false) {
        auto block{SgAsmLEEntryTable::pools.begin()};
        SgAsmLEEntryTable::next_node = reinterpret_cast<SgAsmLEEntryTable*>(*block);
        for (; block != SgAsmLEEntryTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLEEntryTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLEEntryTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLEEntryTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLEEntryTable::pool_size-1]);
        }
    }
}

void
SgAsmLEEntryTable::deleteMemoryPool() {
    for (auto p: SgAsmLEEntryTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmLEEntryTable::next_node = nullptr;
    SgAsmLEEntryTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLEEntryTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEEntryTable)};
    for (size_t blockIndex = SgAsmLEEntryTable::pools.size(); (blockIndex * SgAsmLEEntryTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEEntryTable) = %" PRIuPTR " SgAsmLEEntryTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEEntryTable),SgAsmLEEntryTable::pool_size);
        }
#endif

        SgAsmLEEntryTable* pointer{static_cast<SgAsmLEEntryTable*>(ROSE_MALLOC(SgAsmLEEntryTable::pool_size * sizeof(SgAsmLEEntryTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLEEntryTable::pool_size * sizeof(SgAsmLEEntryTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLEEntryTable::pool_size * sizeof(SgAsmLEEntryTable));
#endif
        SgAsmLEEntryTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLEEntryTable::pool_size * sizeof(SgAsmLEEntryTable), V_SgAsmLEEntryTable));

        if (SgAsmLEEntryTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLEEntryTable* blkptr{reinterpret_cast<SgAsmLEEntryTable*>(SgAsmLEEntryTable::pools[blockIndex-1])};
                blkptr[SgAsmLEEntryTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLEEntryTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLEEntryTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLEEntryTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLEEntryTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLEEntryTable::pool_size - 1};
    SgAsmLEEntryTable* testPointer{reinterpret_cast<SgAsmLEEntryTable*>(SgAsmLEEntryTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLEEntryTable::pool_size * (SgAsmLEEntryTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLEEntryTableStroageClass
 * from its counterpart of SgAsmLEEntryTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLEEntryTable::initializeStorageClassArray(SgAsmLEEntryTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLEEntryTable::pools.begin(); block != SgAsmLEEntryTable::pools.end(); ++block) {
        SgAsmLEEntryTable* pointer{reinterpret_cast<SgAsmLEEntryTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLEEntryTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 212580 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLENameTable that takes its corresponding StorageClass as parameter
 */
SgAsmLENameTable::SgAsmLENameTable(const SgAsmLENameTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 212590 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_names = storageSource.storageOf_names.rebuildDataStoredInEasyStorageClass();
    p_ordinals = storageSource.storageOf_ordinals.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLENameTable*
SgAsmLENameTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLENameTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLENameTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLENameTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLENameTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLENameTable)};
        unsigned long positionInPool{localIndex % SgAsmLENameTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLENameTable::pool_size};
        returnPointer = &(((SgAsmLENameTable*)(SgAsmLENameTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLENameTable*
SgAsmLENameTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLENameTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLENameTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLENameTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLENameTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLENameTable)};
        unsigned long positionInPool{localIndex % SgAsmLENameTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLENameTable::pool_size};
        returnPointer = &(((SgAsmLENameTable*)(SgAsmLENameTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLENameTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLENameTable::pools.begin(); block != SgAsmLENameTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLENameTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLENameTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLENameTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLENameTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLENameTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLENameTable::pools.begin(); block != SgAsmLENameTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLENameTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLENameTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLENameTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLENameTable::clearMemoryPool() {
    SgAsmLENameTable* tempPointer{nullptr};
    if (SgAsmLENameTable::pools.empty() == false) {
        auto block{SgAsmLENameTable::pools.begin()};
        SgAsmLENameTable::next_node = reinterpret_cast<SgAsmLENameTable*>(*block);
        for (; block != SgAsmLENameTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLENameTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLENameTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLENameTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLENameTable::pool_size-1]);
        }
    }
}

void
SgAsmLENameTable::deleteMemoryPool() {
    for (auto p: SgAsmLENameTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmLENameTable::next_node = nullptr;
    SgAsmLENameTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLENameTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLENameTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLENameTable)};
    for (size_t blockIndex = SgAsmLENameTable::pools.size(); (blockIndex * SgAsmLENameTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLENameTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLENameTable) = %" PRIuPTR " SgAsmLENameTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLENameTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLENameTable),SgAsmLENameTable::pool_size);
        }
#endif

        SgAsmLENameTable* pointer{static_cast<SgAsmLENameTable*>(ROSE_MALLOC(SgAsmLENameTable::pool_size * sizeof(SgAsmLENameTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLENameTable::pool_size * sizeof(SgAsmLENameTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLENameTable::pool_size * sizeof(SgAsmLENameTable));
#endif
        SgAsmLENameTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLENameTable::pool_size * sizeof(SgAsmLENameTable), V_SgAsmLENameTable));

        if (SgAsmLENameTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLENameTable* blkptr{reinterpret_cast<SgAsmLENameTable*>(SgAsmLENameTable::pools[blockIndex-1])};
                blkptr[SgAsmLENameTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLENameTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLENameTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLENameTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLENameTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLENameTable::pool_size - 1};
    SgAsmLENameTable* testPointer{reinterpret_cast<SgAsmLENameTable*>(SgAsmLENameTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLENameTable::pool_size * (SgAsmLENameTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLENameTableStroageClass
 * from its counterpart of SgAsmLENameTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLENameTable::initializeStorageClassArray(SgAsmLENameTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLENameTable::pools.begin(); block != SgAsmLENameTable::pools.end(); ++block) {
        SgAsmLENameTable* pointer{reinterpret_cast<SgAsmLENameTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLENameTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 212809 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLEPageTable that takes its corresponding StorageClass as parameter
 */
SgAsmLEPageTable::SgAsmLEPageTable(const SgAsmLEPageTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 212819 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmLEPageTableEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmLEPageTableEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLEPageTable*
SgAsmLEPageTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLEPageTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEPageTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEPageTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEPageTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTable)};
        unsigned long positionInPool{localIndex % SgAsmLEPageTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEPageTable::pool_size};
        returnPointer = &(((SgAsmLEPageTable*)(SgAsmLEPageTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLEPageTable*
SgAsmLEPageTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLEPageTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEPageTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEPageTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEPageTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLEPageTable)};
        unsigned long positionInPool{localIndex % SgAsmLEPageTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEPageTable::pool_size};
        returnPointer = &(((SgAsmLEPageTable*)(SgAsmLEPageTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLEPageTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLEPageTable::pools.begin(); block != SgAsmLEPageTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEPageTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLEPageTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLEPageTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLEPageTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLEPageTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLEPageTable::pools.begin(); block != SgAsmLEPageTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEPageTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLEPageTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLEPageTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLEPageTable::clearMemoryPool() {
    SgAsmLEPageTable* tempPointer{nullptr};
    if (SgAsmLEPageTable::pools.empty() == false) {
        auto block{SgAsmLEPageTable::pools.begin()};
        SgAsmLEPageTable::next_node = reinterpret_cast<SgAsmLEPageTable*>(*block);
        for (; block != SgAsmLEPageTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLEPageTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLEPageTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLEPageTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLEPageTable::pool_size-1]);
        }
    }
}

void
SgAsmLEPageTable::deleteMemoryPool() {
    for (auto p: SgAsmLEPageTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmLEPageTable::next_node = nullptr;
    SgAsmLEPageTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLEPageTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEPageTable)};
    for (size_t blockIndex = SgAsmLEPageTable::pools.size(); (blockIndex * SgAsmLEPageTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEPageTable) = %" PRIuPTR " SgAsmLEPageTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEPageTable),SgAsmLEPageTable::pool_size);
        }
#endif

        SgAsmLEPageTable* pointer{static_cast<SgAsmLEPageTable*>(ROSE_MALLOC(SgAsmLEPageTable::pool_size * sizeof(SgAsmLEPageTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLEPageTable::pool_size * sizeof(SgAsmLEPageTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLEPageTable::pool_size * sizeof(SgAsmLEPageTable));
#endif
        SgAsmLEPageTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLEPageTable::pool_size * sizeof(SgAsmLEPageTable), V_SgAsmLEPageTable));

        if (SgAsmLEPageTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLEPageTable* blkptr{reinterpret_cast<SgAsmLEPageTable*>(SgAsmLEPageTable::pools[blockIndex-1])};
                blkptr[SgAsmLEPageTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLEPageTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLEPageTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLEPageTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLEPageTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLEPageTable::pool_size - 1};
    SgAsmLEPageTable* testPointer{reinterpret_cast<SgAsmLEPageTable*>(SgAsmLEPageTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLEPageTable::pool_size * (SgAsmLEPageTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLEPageTableStroageClass
 * from its counterpart of SgAsmLEPageTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLEPageTable::initializeStorageClassArray(SgAsmLEPageTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLEPageTable::pools.begin(); block != SgAsmLEPageTable::pools.end(); ++block) {
        SgAsmLEPageTable* pointer{reinterpret_cast<SgAsmLEPageTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLEPageTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 213041 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLERelocTable that takes its corresponding StorageClass as parameter
 */
SgAsmLERelocTable::SgAsmLERelocTable(const SgAsmLERelocTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 213051 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmLERelocEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmLERelocEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLERelocTable*
SgAsmLERelocTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLERelocTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLERelocTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLERelocTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLERelocTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLERelocTable)};
        unsigned long positionInPool{localIndex % SgAsmLERelocTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLERelocTable::pool_size};
        returnPointer = &(((SgAsmLERelocTable*)(SgAsmLERelocTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLERelocTable*
SgAsmLERelocTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLERelocTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLERelocTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLERelocTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLERelocTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLERelocTable)};
        unsigned long positionInPool{localIndex % SgAsmLERelocTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLERelocTable::pool_size};
        returnPointer = &(((SgAsmLERelocTable*)(SgAsmLERelocTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLERelocTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLERelocTable::pools.begin(); block != SgAsmLERelocTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLERelocTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLERelocTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLERelocTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLERelocTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLERelocTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLERelocTable::pools.begin(); block != SgAsmLERelocTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLERelocTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLERelocTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLERelocTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLERelocTable::clearMemoryPool() {
    SgAsmLERelocTable* tempPointer{nullptr};
    if (SgAsmLERelocTable::pools.empty() == false) {
        auto block{SgAsmLERelocTable::pools.begin()};
        SgAsmLERelocTable::next_node = reinterpret_cast<SgAsmLERelocTable*>(*block);
        for (; block != SgAsmLERelocTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLERelocTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLERelocTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLERelocTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLERelocTable::pool_size-1]);
        }
    }
}

void
SgAsmLERelocTable::deleteMemoryPool() {
    for (auto p: SgAsmLERelocTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmLERelocTable::next_node = nullptr;
    SgAsmLERelocTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLERelocTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLERelocTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLERelocTable)};
    for (size_t blockIndex = SgAsmLERelocTable::pools.size(); (blockIndex * SgAsmLERelocTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLERelocTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLERelocTable) = %" PRIuPTR " SgAsmLERelocTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLERelocTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLERelocTable),SgAsmLERelocTable::pool_size);
        }
#endif

        SgAsmLERelocTable* pointer{static_cast<SgAsmLERelocTable*>(ROSE_MALLOC(SgAsmLERelocTable::pool_size * sizeof(SgAsmLERelocTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLERelocTable::pool_size * sizeof(SgAsmLERelocTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLERelocTable::pool_size * sizeof(SgAsmLERelocTable));
#endif
        SgAsmLERelocTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLERelocTable::pool_size * sizeof(SgAsmLERelocTable), V_SgAsmLERelocTable));

        if (SgAsmLERelocTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLERelocTable* blkptr{reinterpret_cast<SgAsmLERelocTable*>(SgAsmLERelocTable::pools[blockIndex-1])};
                blkptr[SgAsmLERelocTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLERelocTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLERelocTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLERelocTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLERelocTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLERelocTable::pool_size - 1};
    SgAsmLERelocTable* testPointer{reinterpret_cast<SgAsmLERelocTable*>(SgAsmLERelocTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLERelocTable::pool_size * (SgAsmLERelocTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLERelocTableStroageClass
 * from its counterpart of SgAsmLERelocTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLERelocTable::initializeStorageClassArray(SgAsmLERelocTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLERelocTable::pools.begin(); block != SgAsmLERelocTable::pools.end(); ++block) {
        SgAsmLERelocTable* pointer{reinterpret_cast<SgAsmLERelocTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLERelocTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 213273 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLESection that takes its corresponding StorageClass as parameter
 */
SgAsmLESection::SgAsmLESection(const SgAsmLESectionStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 213283 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_sectionTableEntry = (SgAsmLESectionTableEntry*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sectionTableEntry));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLESection*
SgAsmLESection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLESection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESection)};
        unsigned long positionInPool{localIndex % SgAsmLESection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLESection::pool_size};
        returnPointer = &(((SgAsmLESection*)(SgAsmLESection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLESection*
SgAsmLESection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLESection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLESection)};
        unsigned long positionInPool{localIndex % SgAsmLESection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLESection::pool_size};
        returnPointer = &(((SgAsmLESection*)(SgAsmLESection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLESection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLESection::pools.begin(); block != SgAsmLESection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLESection*>(*block)};
        for (unsigned i = 0; i < SgAsmLESection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLESection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLESection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLESection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLESection::pools.begin(); block != SgAsmLESection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLESection*>(*block)};
        for (unsigned i = 0; i < SgAsmLESection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLESection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLESection::clearMemoryPool() {
    SgAsmLESection* tempPointer{nullptr};
    if (SgAsmLESection::pools.empty() == false) {
        auto block{SgAsmLESection::pools.begin()};
        SgAsmLESection::next_node = reinterpret_cast<SgAsmLESection*>(*block);
        for (; block != SgAsmLESection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLESection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLESection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLESection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLESection::pool_size-1]);
        }
    }
}

void
SgAsmLESection::deleteMemoryPool() {
    for (auto p: SgAsmLESection::pools) {
        ROSE_FREE(p);
    }
    SgAsmLESection::next_node = nullptr;
    SgAsmLESection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLESection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESection)};
    for (size_t blockIndex = SgAsmLESection::pools.size(); (blockIndex * SgAsmLESection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESection) = %" PRIuPTR " SgAsmLESection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESection),SgAsmLESection::pool_size);
        }
#endif

        SgAsmLESection* pointer{static_cast<SgAsmLESection*>(ROSE_MALLOC(SgAsmLESection::pool_size * sizeof(SgAsmLESection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLESection::pool_size * sizeof(SgAsmLESection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLESection::pool_size * sizeof(SgAsmLESection));
#endif
        SgAsmLESection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLESection::pool_size * sizeof(SgAsmLESection), V_SgAsmLESection));

        if (SgAsmLESection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLESection* blkptr{reinterpret_cast<SgAsmLESection*>(SgAsmLESection::pools[blockIndex-1])};
                blkptr[SgAsmLESection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLESection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLESection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLESection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLESection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLESection::pool_size - 1};
    SgAsmLESection* testPointer{reinterpret_cast<SgAsmLESection*>(SgAsmLESection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLESection::pool_size * (SgAsmLESection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLESectionStroageClass
 * from its counterpart of SgAsmLESection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLESection::initializeStorageClassArray(SgAsmLESectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLESection::pools.begin(); block != SgAsmLESection::pools.end(); ++block) {
        SgAsmLESection* pointer{reinterpret_cast<SgAsmLESection*>(*block)};
        for (unsigned i = 0; i < SgAsmLESection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 213501 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLESectionTable that takes its corresponding StorageClass as parameter
 */
SgAsmLESectionTable::SgAsmLESectionTable(const SgAsmLESectionTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 213511 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLESectionTable*
SgAsmLESectionTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLESectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTable)};
        unsigned long positionInPool{localIndex % SgAsmLESectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLESectionTable::pool_size};
        returnPointer = &(((SgAsmLESectionTable*)(SgAsmLESectionTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLESectionTable*
SgAsmLESectionTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLESectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLESectionTable)};
        unsigned long positionInPool{localIndex % SgAsmLESectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLESectionTable::pool_size};
        returnPointer = &(((SgAsmLESectionTable*)(SgAsmLESectionTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLESectionTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLESectionTable::pools.begin(); block != SgAsmLESectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLESectionTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLESectionTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLESectionTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLESectionTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLESectionTable::pools.begin(); block != SgAsmLESectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLESectionTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLESectionTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLESectionTable::clearMemoryPool() {
    SgAsmLESectionTable* tempPointer{nullptr};
    if (SgAsmLESectionTable::pools.empty() == false) {
        auto block{SgAsmLESectionTable::pools.begin()};
        SgAsmLESectionTable::next_node = reinterpret_cast<SgAsmLESectionTable*>(*block);
        for (; block != SgAsmLESectionTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLESectionTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLESectionTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLESectionTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLESectionTable::pool_size-1]);
        }
    }
}

void
SgAsmLESectionTable::deleteMemoryPool() {
    for (auto p: SgAsmLESectionTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmLESectionTable::next_node = nullptr;
    SgAsmLESectionTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLESectionTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESectionTable)};
    for (size_t blockIndex = SgAsmLESectionTable::pools.size(); (blockIndex * SgAsmLESectionTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESectionTable) = %" PRIuPTR " SgAsmLESectionTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESectionTable),SgAsmLESectionTable::pool_size);
        }
#endif

        SgAsmLESectionTable* pointer{static_cast<SgAsmLESectionTable*>(ROSE_MALLOC(SgAsmLESectionTable::pool_size * sizeof(SgAsmLESectionTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLESectionTable::pool_size * sizeof(SgAsmLESectionTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLESectionTable::pool_size * sizeof(SgAsmLESectionTable));
#endif
        SgAsmLESectionTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLESectionTable::pool_size * sizeof(SgAsmLESectionTable), V_SgAsmLESectionTable));

        if (SgAsmLESectionTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLESectionTable* blkptr{reinterpret_cast<SgAsmLESectionTable*>(SgAsmLESectionTable::pools[blockIndex-1])};
                blkptr[SgAsmLESectionTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLESectionTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLESectionTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLESectionTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLESectionTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLESectionTable::pool_size - 1};
    SgAsmLESectionTable* testPointer{reinterpret_cast<SgAsmLESectionTable*>(SgAsmLESectionTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLESectionTable::pool_size * (SgAsmLESectionTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLESectionTableStroageClass
 * from its counterpart of SgAsmLESectionTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLESectionTable::initializeStorageClassArray(SgAsmLESectionTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLESectionTable::pools.begin(); block != SgAsmLESectionTable::pools.end(); ++block) {
        SgAsmLESectionTable* pointer{reinterpret_cast<SgAsmLESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmLESectionTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 213728 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNEEntryTable that takes its corresponding StorageClass as parameter
 */
SgAsmNEEntryTable::SgAsmNEEntryTable(const SgAsmNEEntryTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 213738 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_bundle_sizes = storageSource.storageOf_bundle_sizes.rebuildDataStoredInEasyStorageClass();
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmNEEntryPointPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmNEEntryPointPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNEEntryTable*
SgAsmNEEntryTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNEEntryTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEEntryTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEEntryTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEEntryTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryTable)};
        unsigned long positionInPool{localIndex % SgAsmNEEntryTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEEntryTable::pool_size};
        returnPointer = &(((SgAsmNEEntryTable*)(SgAsmNEEntryTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNEEntryTable*
SgAsmNEEntryTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNEEntryTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEEntryTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEEntryTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEEntryTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNEEntryTable)};
        unsigned long positionInPool{localIndex % SgAsmNEEntryTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEEntryTable::pool_size};
        returnPointer = &(((SgAsmNEEntryTable*)(SgAsmNEEntryTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNEEntryTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNEEntryTable::pools.begin(); block != SgAsmNEEntryTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEEntryTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEEntryTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNEEntryTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNEEntryTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNEEntryTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNEEntryTable::pools.begin(); block != SgAsmNEEntryTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEEntryTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEEntryTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNEEntryTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNEEntryTable::clearMemoryPool() {
    SgAsmNEEntryTable* tempPointer{nullptr};
    if (SgAsmNEEntryTable::pools.empty() == false) {
        auto block{SgAsmNEEntryTable::pools.begin()};
        SgAsmNEEntryTable::next_node = reinterpret_cast<SgAsmNEEntryTable*>(*block);
        for (; block != SgAsmNEEntryTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNEEntryTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNEEntryTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNEEntryTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNEEntryTable::pool_size-1]);
        }
    }
}

void
SgAsmNEEntryTable::deleteMemoryPool() {
    for (auto p: SgAsmNEEntryTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmNEEntryTable::next_node = nullptr;
    SgAsmNEEntryTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNEEntryTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEEntryTable)};
    for (size_t blockIndex = SgAsmNEEntryTable::pools.size(); (blockIndex * SgAsmNEEntryTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEEntryTable) = %" PRIuPTR " SgAsmNEEntryTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEEntryTable),SgAsmNEEntryTable::pool_size);
        }
#endif

        SgAsmNEEntryTable* pointer{static_cast<SgAsmNEEntryTable*>(ROSE_MALLOC(SgAsmNEEntryTable::pool_size * sizeof(SgAsmNEEntryTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNEEntryTable::pool_size * sizeof(SgAsmNEEntryTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNEEntryTable::pool_size * sizeof(SgAsmNEEntryTable));
#endif
        SgAsmNEEntryTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNEEntryTable::pool_size * sizeof(SgAsmNEEntryTable), V_SgAsmNEEntryTable));

        if (SgAsmNEEntryTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNEEntryTable* blkptr{reinterpret_cast<SgAsmNEEntryTable*>(SgAsmNEEntryTable::pools[blockIndex-1])};
                blkptr[SgAsmNEEntryTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNEEntryTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNEEntryTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNEEntryTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNEEntryTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNEEntryTable::pool_size - 1};
    SgAsmNEEntryTable* testPointer{reinterpret_cast<SgAsmNEEntryTable*>(SgAsmNEEntryTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNEEntryTable::pool_size * (SgAsmNEEntryTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNEEntryTableStroageClass
 * from its counterpart of SgAsmNEEntryTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNEEntryTable::initializeStorageClassArray(SgAsmNEEntryTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNEEntryTable::pools.begin(); block != SgAsmNEEntryTable::pools.end(); ++block) {
        SgAsmNEEntryTable* pointer{reinterpret_cast<SgAsmNEEntryTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEEntryTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 213961 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNEModuleTable that takes its corresponding StorageClass as parameter
 */
SgAsmNEModuleTable::SgAsmNEModuleTable(const SgAsmNEModuleTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 213971 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_strtab = (SgAsmNEStringTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_strtab));
    p_nameOffsets = storageSource.storageOf_nameOffsets.rebuildDataStoredInEasyStorageClass();
    p_names = storageSource.storageOf_names.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNEModuleTable*
SgAsmNEModuleTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNEModuleTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEModuleTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEModuleTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEModuleTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEModuleTable)};
        unsigned long positionInPool{localIndex % SgAsmNEModuleTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEModuleTable::pool_size};
        returnPointer = &(((SgAsmNEModuleTable*)(SgAsmNEModuleTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNEModuleTable*
SgAsmNEModuleTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNEModuleTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEModuleTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEModuleTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEModuleTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNEModuleTable)};
        unsigned long positionInPool{localIndex % SgAsmNEModuleTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEModuleTable::pool_size};
        returnPointer = &(((SgAsmNEModuleTable*)(SgAsmNEModuleTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNEModuleTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNEModuleTable::pools.begin(); block != SgAsmNEModuleTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEModuleTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEModuleTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNEModuleTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNEModuleTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNEModuleTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNEModuleTable::pools.begin(); block != SgAsmNEModuleTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEModuleTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEModuleTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNEModuleTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNEModuleTable::clearMemoryPool() {
    SgAsmNEModuleTable* tempPointer{nullptr};
    if (SgAsmNEModuleTable::pools.empty() == false) {
        auto block{SgAsmNEModuleTable::pools.begin()};
        SgAsmNEModuleTable::next_node = reinterpret_cast<SgAsmNEModuleTable*>(*block);
        for (; block != SgAsmNEModuleTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNEModuleTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNEModuleTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNEModuleTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNEModuleTable::pool_size-1]);
        }
    }
}

void
SgAsmNEModuleTable::deleteMemoryPool() {
    for (auto p: SgAsmNEModuleTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmNEModuleTable::next_node = nullptr;
    SgAsmNEModuleTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNEModuleTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEModuleTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEModuleTable)};
    for (size_t blockIndex = SgAsmNEModuleTable::pools.size(); (blockIndex * SgAsmNEModuleTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEModuleTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEModuleTable) = %" PRIuPTR " SgAsmNEModuleTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEModuleTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEModuleTable),SgAsmNEModuleTable::pool_size);
        }
#endif

        SgAsmNEModuleTable* pointer{static_cast<SgAsmNEModuleTable*>(ROSE_MALLOC(SgAsmNEModuleTable::pool_size * sizeof(SgAsmNEModuleTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNEModuleTable::pool_size * sizeof(SgAsmNEModuleTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNEModuleTable::pool_size * sizeof(SgAsmNEModuleTable));
#endif
        SgAsmNEModuleTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNEModuleTable::pool_size * sizeof(SgAsmNEModuleTable), V_SgAsmNEModuleTable));

        if (SgAsmNEModuleTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNEModuleTable* blkptr{reinterpret_cast<SgAsmNEModuleTable*>(SgAsmNEModuleTable::pools[blockIndex-1])};
                blkptr[SgAsmNEModuleTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNEModuleTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNEModuleTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNEModuleTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNEModuleTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNEModuleTable::pool_size - 1};
    SgAsmNEModuleTable* testPointer{reinterpret_cast<SgAsmNEModuleTable*>(SgAsmNEModuleTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNEModuleTable::pool_size * (SgAsmNEModuleTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNEModuleTableStroageClass
 * from its counterpart of SgAsmNEModuleTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNEModuleTable::initializeStorageClassArray(SgAsmNEModuleTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNEModuleTable::pools.begin(); block != SgAsmNEModuleTable::pools.end(); ++block) {
        SgAsmNEModuleTable* pointer{reinterpret_cast<SgAsmNEModuleTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEModuleTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 214191 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNENameTable that takes its corresponding StorageClass as parameter
 */
SgAsmNENameTable::SgAsmNENameTable(const SgAsmNENameTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 214201 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_names = storageSource.storageOf_names.rebuildDataStoredInEasyStorageClass();
    p_ordinals = storageSource.storageOf_ordinals.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNENameTable*
SgAsmNENameTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNENameTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNENameTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNENameTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNENameTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNENameTable)};
        unsigned long positionInPool{localIndex % SgAsmNENameTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNENameTable::pool_size};
        returnPointer = &(((SgAsmNENameTable*)(SgAsmNENameTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNENameTable*
SgAsmNENameTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNENameTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNENameTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNENameTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNENameTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNENameTable)};
        unsigned long positionInPool{localIndex % SgAsmNENameTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNENameTable::pool_size};
        returnPointer = &(((SgAsmNENameTable*)(SgAsmNENameTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNENameTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNENameTable::pools.begin(); block != SgAsmNENameTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNENameTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNENameTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNENameTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNENameTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNENameTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNENameTable::pools.begin(); block != SgAsmNENameTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNENameTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNENameTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNENameTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNENameTable::clearMemoryPool() {
    SgAsmNENameTable* tempPointer{nullptr};
    if (SgAsmNENameTable::pools.empty() == false) {
        auto block{SgAsmNENameTable::pools.begin()};
        SgAsmNENameTable::next_node = reinterpret_cast<SgAsmNENameTable*>(*block);
        for (; block != SgAsmNENameTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNENameTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNENameTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNENameTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNENameTable::pool_size-1]);
        }
    }
}

void
SgAsmNENameTable::deleteMemoryPool() {
    for (auto p: SgAsmNENameTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmNENameTable::next_node = nullptr;
    SgAsmNENameTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNENameTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNENameTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNENameTable)};
    for (size_t blockIndex = SgAsmNENameTable::pools.size(); (blockIndex * SgAsmNENameTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNENameTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNENameTable) = %" PRIuPTR " SgAsmNENameTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNENameTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNENameTable),SgAsmNENameTable::pool_size);
        }
#endif

        SgAsmNENameTable* pointer{static_cast<SgAsmNENameTable*>(ROSE_MALLOC(SgAsmNENameTable::pool_size * sizeof(SgAsmNENameTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNENameTable::pool_size * sizeof(SgAsmNENameTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNENameTable::pool_size * sizeof(SgAsmNENameTable));
#endif
        SgAsmNENameTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNENameTable::pool_size * sizeof(SgAsmNENameTable), V_SgAsmNENameTable));

        if (SgAsmNENameTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNENameTable* blkptr{reinterpret_cast<SgAsmNENameTable*>(SgAsmNENameTable::pools[blockIndex-1])};
                blkptr[SgAsmNENameTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNENameTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNENameTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNENameTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNENameTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNENameTable::pool_size - 1};
    SgAsmNENameTable* testPointer{reinterpret_cast<SgAsmNENameTable*>(SgAsmNENameTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNENameTable::pool_size * (SgAsmNENameTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNENameTableStroageClass
 * from its counterpart of SgAsmNENameTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNENameTable::initializeStorageClassArray(SgAsmNENameTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNENameTable::pools.begin(); block != SgAsmNENameTable::pools.end(); ++block) {
        SgAsmNENameTable* pointer{reinterpret_cast<SgAsmNENameTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNENameTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 214420 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNERelocTable that takes its corresponding StorageClass as parameter
 */
SgAsmNERelocTable::SgAsmNERelocTable(const SgAsmNERelocTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 214430 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmNERelocEntryPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmNERelocEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNERelocTable*
SgAsmNERelocTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNERelocTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNERelocTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNERelocTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNERelocTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocTable)};
        unsigned long positionInPool{localIndex % SgAsmNERelocTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNERelocTable::pool_size};
        returnPointer = &(((SgAsmNERelocTable*)(SgAsmNERelocTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNERelocTable*
SgAsmNERelocTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNERelocTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNERelocTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNERelocTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNERelocTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNERelocTable)};
        unsigned long positionInPool{localIndex % SgAsmNERelocTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNERelocTable::pool_size};
        returnPointer = &(((SgAsmNERelocTable*)(SgAsmNERelocTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNERelocTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNERelocTable::pools.begin(); block != SgAsmNERelocTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNERelocTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNERelocTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNERelocTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNERelocTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNERelocTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNERelocTable::pools.begin(); block != SgAsmNERelocTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNERelocTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNERelocTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNERelocTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNERelocTable::clearMemoryPool() {
    SgAsmNERelocTable* tempPointer{nullptr};
    if (SgAsmNERelocTable::pools.empty() == false) {
        auto block{SgAsmNERelocTable::pools.begin()};
        SgAsmNERelocTable::next_node = reinterpret_cast<SgAsmNERelocTable*>(*block);
        for (; block != SgAsmNERelocTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNERelocTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNERelocTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNERelocTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNERelocTable::pool_size-1]);
        }
    }
}

void
SgAsmNERelocTable::deleteMemoryPool() {
    for (auto p: SgAsmNERelocTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmNERelocTable::next_node = nullptr;
    SgAsmNERelocTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNERelocTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNERelocTable)};
    for (size_t blockIndex = SgAsmNERelocTable::pools.size(); (blockIndex * SgAsmNERelocTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNERelocTable) = %" PRIuPTR " SgAsmNERelocTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNERelocTable),SgAsmNERelocTable::pool_size);
        }
#endif

        SgAsmNERelocTable* pointer{static_cast<SgAsmNERelocTable*>(ROSE_MALLOC(SgAsmNERelocTable::pool_size * sizeof(SgAsmNERelocTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNERelocTable::pool_size * sizeof(SgAsmNERelocTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNERelocTable::pool_size * sizeof(SgAsmNERelocTable));
#endif
        SgAsmNERelocTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNERelocTable::pool_size * sizeof(SgAsmNERelocTable), V_SgAsmNERelocTable));

        if (SgAsmNERelocTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNERelocTable* blkptr{reinterpret_cast<SgAsmNERelocTable*>(SgAsmNERelocTable::pools[blockIndex-1])};
                blkptr[SgAsmNERelocTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNERelocTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNERelocTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNERelocTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNERelocTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNERelocTable::pool_size - 1};
    SgAsmNERelocTable* testPointer{reinterpret_cast<SgAsmNERelocTable*>(SgAsmNERelocTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNERelocTable::pool_size * (SgAsmNERelocTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNERelocTableStroageClass
 * from its counterpart of SgAsmNERelocTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNERelocTable::initializeStorageClassArray(SgAsmNERelocTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNERelocTable::pools.begin(); block != SgAsmNERelocTable::pools.end(); ++block) {
        SgAsmNERelocTable* pointer{reinterpret_cast<SgAsmNERelocTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNERelocTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 214652 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNESection that takes its corresponding StorageClass as parameter
 */
SgAsmNESection::SgAsmNESection(const SgAsmNESectionStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 214662 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_sectionTableEntry = (SgAsmNESectionTableEntry*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_sectionTableEntry));
     p_relocationTable = (SgAsmNERelocTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_relocationTable));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNESection*
SgAsmNESection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNESection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESection)};
        unsigned long positionInPool{localIndex % SgAsmNESection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNESection::pool_size};
        returnPointer = &(((SgAsmNESection*)(SgAsmNESection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNESection*
SgAsmNESection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNESection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNESection)};
        unsigned long positionInPool{localIndex % SgAsmNESection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNESection::pool_size};
        returnPointer = &(((SgAsmNESection*)(SgAsmNESection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNESection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNESection::pools.begin(); block != SgAsmNESection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNESection*>(*block)};
        for (unsigned i = 0; i < SgAsmNESection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNESection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNESection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNESection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNESection::pools.begin(); block != SgAsmNESection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNESection*>(*block)};
        for (unsigned i = 0; i < SgAsmNESection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNESection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNESection::clearMemoryPool() {
    SgAsmNESection* tempPointer{nullptr};
    if (SgAsmNESection::pools.empty() == false) {
        auto block{SgAsmNESection::pools.begin()};
        SgAsmNESection::next_node = reinterpret_cast<SgAsmNESection*>(*block);
        for (; block != SgAsmNESection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNESection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNESection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNESection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNESection::pool_size-1]);
        }
    }
}

void
SgAsmNESection::deleteMemoryPool() {
    for (auto p: SgAsmNESection::pools) {
        ROSE_FREE(p);
    }
    SgAsmNESection::next_node = nullptr;
    SgAsmNESection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNESection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESection)};
    for (size_t blockIndex = SgAsmNESection::pools.size(); (blockIndex * SgAsmNESection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESection) = %" PRIuPTR " SgAsmNESection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESection),SgAsmNESection::pool_size);
        }
#endif

        SgAsmNESection* pointer{static_cast<SgAsmNESection*>(ROSE_MALLOC(SgAsmNESection::pool_size * sizeof(SgAsmNESection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNESection::pool_size * sizeof(SgAsmNESection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNESection::pool_size * sizeof(SgAsmNESection));
#endif
        SgAsmNESection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNESection::pool_size * sizeof(SgAsmNESection), V_SgAsmNESection));

        if (SgAsmNESection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNESection* blkptr{reinterpret_cast<SgAsmNESection*>(SgAsmNESection::pools[blockIndex-1])};
                blkptr[SgAsmNESection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNESection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNESection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNESection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNESection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNESection::pool_size - 1};
    SgAsmNESection* testPointer{reinterpret_cast<SgAsmNESection*>(SgAsmNESection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNESection::pool_size * (SgAsmNESection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNESectionStroageClass
 * from its counterpart of SgAsmNESection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNESection::initializeStorageClassArray(SgAsmNESectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNESection::pools.begin(); block != SgAsmNESection::pools.end(); ++block) {
        SgAsmNESection* pointer{reinterpret_cast<SgAsmNESection*>(*block)};
        for (unsigned i = 0; i < SgAsmNESection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 214881 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNESectionTable that takes its corresponding StorageClass as parameter
 */
SgAsmNESectionTable::SgAsmNESectionTable(const SgAsmNESectionTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 214891 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_flags = storageSource.storageOf_flags;
    p_sector = storageSource.storageOf_sector;
    p_physicalSize = storageSource.storageOf_physicalSize;
    p_virtualSize = storageSource.storageOf_virtualSize;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNESectionTable*
SgAsmNESectionTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNESectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTable)};
        unsigned long positionInPool{localIndex % SgAsmNESectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNESectionTable::pool_size};
        returnPointer = &(((SgAsmNESectionTable*)(SgAsmNESectionTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNESectionTable*
SgAsmNESectionTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNESectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNESectionTable)};
        unsigned long positionInPool{localIndex % SgAsmNESectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNESectionTable::pool_size};
        returnPointer = &(((SgAsmNESectionTable*)(SgAsmNESectionTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNESectionTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNESectionTable::pools.begin(); block != SgAsmNESectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNESectionTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNESectionTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNESectionTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNESectionTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNESectionTable::pools.begin(); block != SgAsmNESectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNESectionTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNESectionTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNESectionTable::clearMemoryPool() {
    SgAsmNESectionTable* tempPointer{nullptr};
    if (SgAsmNESectionTable::pools.empty() == false) {
        auto block{SgAsmNESectionTable::pools.begin()};
        SgAsmNESectionTable::next_node = reinterpret_cast<SgAsmNESectionTable*>(*block);
        for (; block != SgAsmNESectionTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNESectionTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNESectionTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNESectionTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNESectionTable::pool_size-1]);
        }
    }
}

void
SgAsmNESectionTable::deleteMemoryPool() {
    for (auto p: SgAsmNESectionTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmNESectionTable::next_node = nullptr;
    SgAsmNESectionTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNESectionTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESectionTable)};
    for (size_t blockIndex = SgAsmNESectionTable::pools.size(); (blockIndex * SgAsmNESectionTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESectionTable) = %" PRIuPTR " SgAsmNESectionTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESectionTable),SgAsmNESectionTable::pool_size);
        }
#endif

        SgAsmNESectionTable* pointer{static_cast<SgAsmNESectionTable*>(ROSE_MALLOC(SgAsmNESectionTable::pool_size * sizeof(SgAsmNESectionTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNESectionTable::pool_size * sizeof(SgAsmNESectionTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNESectionTable::pool_size * sizeof(SgAsmNESectionTable));
#endif
        SgAsmNESectionTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNESectionTable::pool_size * sizeof(SgAsmNESectionTable), V_SgAsmNESectionTable));

        if (SgAsmNESectionTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNESectionTable* blkptr{reinterpret_cast<SgAsmNESectionTable*>(SgAsmNESectionTable::pools[blockIndex-1])};
                blkptr[SgAsmNESectionTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNESectionTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNESectionTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNESectionTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNESectionTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNESectionTable::pool_size - 1};
    SgAsmNESectionTable* testPointer{reinterpret_cast<SgAsmNESectionTable*>(SgAsmNESectionTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNESectionTable::pool_size * (SgAsmNESectionTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNESectionTableStroageClass
 * from its counterpart of SgAsmNESectionTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNESectionTable::initializeStorageClassArray(SgAsmNESectionTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNESectionTable::pools.begin(); block != SgAsmNESectionTable::pools.end(); ++block) {
        SgAsmNESectionTable* pointer{reinterpret_cast<SgAsmNESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNESectionTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 215112 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNEStringTable that takes its corresponding StorageClass as parameter
 */
SgAsmNEStringTable::SgAsmNEStringTable(const SgAsmNEStringTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 215122 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNEStringTable*
SgAsmNEStringTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNEStringTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEStringTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEStringTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEStringTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEStringTable)};
        unsigned long positionInPool{localIndex % SgAsmNEStringTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEStringTable::pool_size};
        returnPointer = &(((SgAsmNEStringTable*)(SgAsmNEStringTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNEStringTable*
SgAsmNEStringTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNEStringTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEStringTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEStringTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEStringTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNEStringTable)};
        unsigned long positionInPool{localIndex % SgAsmNEStringTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEStringTable::pool_size};
        returnPointer = &(((SgAsmNEStringTable*)(SgAsmNEStringTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNEStringTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNEStringTable::pools.begin(); block != SgAsmNEStringTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEStringTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEStringTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNEStringTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNEStringTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNEStringTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNEStringTable::pools.begin(); block != SgAsmNEStringTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEStringTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEStringTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNEStringTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNEStringTable::clearMemoryPool() {
    SgAsmNEStringTable* tempPointer{nullptr};
    if (SgAsmNEStringTable::pools.empty() == false) {
        auto block{SgAsmNEStringTable::pools.begin()};
        SgAsmNEStringTable::next_node = reinterpret_cast<SgAsmNEStringTable*>(*block);
        for (; block != SgAsmNEStringTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNEStringTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNEStringTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNEStringTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNEStringTable::pool_size-1]);
        }
    }
}

void
SgAsmNEStringTable::deleteMemoryPool() {
    for (auto p: SgAsmNEStringTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmNEStringTable::next_node = nullptr;
    SgAsmNEStringTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNEStringTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEStringTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEStringTable)};
    for (size_t blockIndex = SgAsmNEStringTable::pools.size(); (blockIndex * SgAsmNEStringTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEStringTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEStringTable) = %" PRIuPTR " SgAsmNEStringTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEStringTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEStringTable),SgAsmNEStringTable::pool_size);
        }
#endif

        SgAsmNEStringTable* pointer{static_cast<SgAsmNEStringTable*>(ROSE_MALLOC(SgAsmNEStringTable::pool_size * sizeof(SgAsmNEStringTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNEStringTable::pool_size * sizeof(SgAsmNEStringTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNEStringTable::pool_size * sizeof(SgAsmNEStringTable));
#endif
        SgAsmNEStringTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNEStringTable::pool_size * sizeof(SgAsmNEStringTable), V_SgAsmNEStringTable));

        if (SgAsmNEStringTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNEStringTable* blkptr{reinterpret_cast<SgAsmNEStringTable*>(SgAsmNEStringTable::pools[blockIndex-1])};
                blkptr[SgAsmNEStringTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNEStringTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNEStringTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNEStringTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNEStringTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNEStringTable::pool_size - 1};
    SgAsmNEStringTable* testPointer{reinterpret_cast<SgAsmNEStringTable*>(SgAsmNEStringTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNEStringTable::pool_size * (SgAsmNEStringTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNEStringTableStroageClass
 * from its counterpart of SgAsmNEStringTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNEStringTable::initializeStorageClassArray(SgAsmNEStringTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNEStringTable::pools.begin(); block != SgAsmNEStringTable::pools.end(); ++block) {
        SgAsmNEStringTable* pointer{reinterpret_cast<SgAsmNEStringTable*>(*block)};
        for (unsigned i = 0; i < SgAsmNEStringTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 215339 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPESection that takes its corresponding StorageClass as parameter
 */
SgAsmPESection::SgAsmPESection(const SgAsmPESectionStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 215349 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_section_entry = (SgAsmPESectionTableEntry*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_section_entry));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPESection*
SgAsmPESection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPESection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESection)};
        unsigned long positionInPool{localIndex % SgAsmPESection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPESection::pool_size};
        returnPointer = &(((SgAsmPESection*)(SgAsmPESection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPESection*
SgAsmPESection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPESection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPESection)};
        unsigned long positionInPool{localIndex % SgAsmPESection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPESection::pool_size};
        returnPointer = &(((SgAsmPESection*)(SgAsmPESection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPESection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPESection::pools.begin(); block != SgAsmPESection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPESection*>(*block)};
        for (unsigned i = 0; i < SgAsmPESection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPESection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPESection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPESection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPESection::pools.begin(); block != SgAsmPESection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPESection*>(*block)};
        for (unsigned i = 0; i < SgAsmPESection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPESection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPESection::clearMemoryPool() {
    SgAsmPESection* tempPointer{nullptr};
    if (SgAsmPESection::pools.empty() == false) {
        auto block{SgAsmPESection::pools.begin()};
        SgAsmPESection::next_node = reinterpret_cast<SgAsmPESection*>(*block);
        for (; block != SgAsmPESection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPESection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPESection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPESection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPESection::pool_size-1]);
        }
    }
}

void
SgAsmPESection::deleteMemoryPool() {
    for (auto p: SgAsmPESection::pools) {
        ROSE_FREE(p);
    }
    SgAsmPESection::next_node = nullptr;
    SgAsmPESection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPESection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESection)};
    for (size_t blockIndex = SgAsmPESection::pools.size(); (blockIndex * SgAsmPESection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESection) = %" PRIuPTR " SgAsmPESection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESection),SgAsmPESection::pool_size);
        }
#endif

        SgAsmPESection* pointer{static_cast<SgAsmPESection*>(ROSE_MALLOC(SgAsmPESection::pool_size * sizeof(SgAsmPESection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPESection::pool_size * sizeof(SgAsmPESection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPESection::pool_size * sizeof(SgAsmPESection));
#endif
        SgAsmPESection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPESection::pool_size * sizeof(SgAsmPESection), V_SgAsmPESection));

        if (SgAsmPESection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPESection* blkptr{reinterpret_cast<SgAsmPESection*>(SgAsmPESection::pools[blockIndex-1])};
                blkptr[SgAsmPESection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPESection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPESection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPESection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPESection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPESection::pool_size - 1};
    SgAsmPESection* testPointer{reinterpret_cast<SgAsmPESection*>(SgAsmPESection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPESection::pool_size * (SgAsmPESection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPESectionStroageClass
 * from its counterpart of SgAsmPESection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPESection::initializeStorageClassArray(SgAsmPESectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPESection::pools.begin(); block != SgAsmPESection::pools.end(); ++block) {
        SgAsmPESection* pointer{reinterpret_cast<SgAsmPESection*>(*block)};
        for (unsigned i = 0; i < SgAsmPESection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 215567 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEExportSection that takes its corresponding StorageClass as parameter
 */
SgAsmPEExportSection::SgAsmPEExportSection(const SgAsmPEExportSectionStorageClass& storageSource)  : SgAsmPESection (storageSource) {

/* #line 215577 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_exportDirectory = (SgAsmPEExportDirectory*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_exportDirectory));
     p_exports = (SgAsmPEExportEntryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_exports));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEExportSection*
SgAsmPEExportSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEExportSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportSection)};
        unsigned long positionInPool{localIndex % SgAsmPEExportSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportSection::pool_size};
        returnPointer = &(((SgAsmPEExportSection*)(SgAsmPEExportSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEExportSection*
SgAsmPEExportSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEExportSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEExportSection)};
        unsigned long positionInPool{localIndex % SgAsmPEExportSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportSection::pool_size};
        returnPointer = &(((SgAsmPEExportSection*)(SgAsmPEExportSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEExportSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEExportSection::pools.begin(); block != SgAsmPEExportSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEExportSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEExportSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEExportSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEExportSection::pools.begin(); block != SgAsmPEExportSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEExportSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEExportSection::clearMemoryPool() {
    SgAsmPEExportSection* tempPointer{nullptr};
    if (SgAsmPEExportSection::pools.empty() == false) {
        auto block{SgAsmPEExportSection::pools.begin()};
        SgAsmPEExportSection::next_node = reinterpret_cast<SgAsmPEExportSection*>(*block);
        for (; block != SgAsmPEExportSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEExportSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEExportSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEExportSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEExportSection::pool_size-1]);
        }
    }
}

void
SgAsmPEExportSection::deleteMemoryPool() {
    for (auto p: SgAsmPEExportSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEExportSection::next_node = nullptr;
    SgAsmPEExportSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEExportSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportSection)};
    for (size_t blockIndex = SgAsmPEExportSection::pools.size(); (blockIndex * SgAsmPEExportSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportSection) = %" PRIuPTR " SgAsmPEExportSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportSection),SgAsmPEExportSection::pool_size);
        }
#endif

        SgAsmPEExportSection* pointer{static_cast<SgAsmPEExportSection*>(ROSE_MALLOC(SgAsmPEExportSection::pool_size * sizeof(SgAsmPEExportSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEExportSection::pool_size * sizeof(SgAsmPEExportSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEExportSection::pool_size * sizeof(SgAsmPEExportSection));
#endif
        SgAsmPEExportSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEExportSection::pool_size * sizeof(SgAsmPEExportSection), V_SgAsmPEExportSection));

        if (SgAsmPEExportSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEExportSection* blkptr{reinterpret_cast<SgAsmPEExportSection*>(SgAsmPEExportSection::pools[blockIndex-1])};
                blkptr[SgAsmPEExportSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEExportSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEExportSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEExportSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEExportSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEExportSection::pool_size - 1};
    SgAsmPEExportSection* testPointer{reinterpret_cast<SgAsmPEExportSection*>(SgAsmPEExportSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEExportSection::pool_size * (SgAsmPEExportSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEExportSectionStroageClass
 * from its counterpart of SgAsmPEExportSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEExportSection::initializeStorageClassArray(SgAsmPEExportSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEExportSection::pools.begin(); block != SgAsmPEExportSection::pools.end(); ++block) {
        SgAsmPEExportSection* pointer{reinterpret_cast<SgAsmPEExportSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 215796 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEImportSection that takes its corresponding StorageClass as parameter
 */
SgAsmPEImportSection::SgAsmPEImportSection(const SgAsmPEImportSectionStorageClass& storageSource)  : SgAsmPESection (storageSource) {

/* #line 215806 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_importDirectories = (SgAsmPEImportDirectoryList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_importDirectories));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEImportSection*
SgAsmPEImportSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEImportSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportSection)};
        unsigned long positionInPool{localIndex % SgAsmPEImportSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportSection::pool_size};
        returnPointer = &(((SgAsmPEImportSection*)(SgAsmPEImportSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEImportSection*
SgAsmPEImportSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEImportSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEImportSection)};
        unsigned long positionInPool{localIndex % SgAsmPEImportSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportSection::pool_size};
        returnPointer = &(((SgAsmPEImportSection*)(SgAsmPEImportSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEImportSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEImportSection::pools.begin(); block != SgAsmPEImportSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEImportSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEImportSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEImportSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEImportSection::pools.begin(); block != SgAsmPEImportSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEImportSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEImportSection::clearMemoryPool() {
    SgAsmPEImportSection* tempPointer{nullptr};
    if (SgAsmPEImportSection::pools.empty() == false) {
        auto block{SgAsmPEImportSection::pools.begin()};
        SgAsmPEImportSection::next_node = reinterpret_cast<SgAsmPEImportSection*>(*block);
        for (; block != SgAsmPEImportSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEImportSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEImportSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEImportSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEImportSection::pool_size-1]);
        }
    }
}

void
SgAsmPEImportSection::deleteMemoryPool() {
    for (auto p: SgAsmPEImportSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEImportSection::next_node = nullptr;
    SgAsmPEImportSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEImportSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportSection)};
    for (size_t blockIndex = SgAsmPEImportSection::pools.size(); (blockIndex * SgAsmPEImportSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportSection) = %" PRIuPTR " SgAsmPEImportSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportSection),SgAsmPEImportSection::pool_size);
        }
#endif

        SgAsmPEImportSection* pointer{static_cast<SgAsmPEImportSection*>(ROSE_MALLOC(SgAsmPEImportSection::pool_size * sizeof(SgAsmPEImportSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEImportSection::pool_size * sizeof(SgAsmPEImportSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEImportSection::pool_size * sizeof(SgAsmPEImportSection));
#endif
        SgAsmPEImportSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEImportSection::pool_size * sizeof(SgAsmPEImportSection), V_SgAsmPEImportSection));

        if (SgAsmPEImportSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEImportSection* blkptr{reinterpret_cast<SgAsmPEImportSection*>(SgAsmPEImportSection::pools[blockIndex-1])};
                blkptr[SgAsmPEImportSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEImportSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEImportSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEImportSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEImportSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEImportSection::pool_size - 1};
    SgAsmPEImportSection* testPointer{reinterpret_cast<SgAsmPEImportSection*>(SgAsmPEImportSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEImportSection::pool_size * (SgAsmPEImportSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEImportSectionStroageClass
 * from its counterpart of SgAsmPEImportSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEImportSection::initializeStorageClassArray(SgAsmPEImportSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEImportSection::pools.begin(); block != SgAsmPEImportSection::pools.end(); ++block) {
        SgAsmPEImportSection* pointer{reinterpret_cast<SgAsmPEImportSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 216024 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEStringSection that takes its corresponding StorageClass as parameter
 */
SgAsmPEStringSection::SgAsmPEStringSection(const SgAsmPEStringSectionStorageClass& storageSource)  : SgAsmPESection (storageSource) {

/* #line 216034 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_strtab = (SgAsmCoffStrtab*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_strtab));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEStringSection*
SgAsmPEStringSection::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEStringSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEStringSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEStringSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEStringSection)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEStringSection)};
        unsigned long positionInPool{localIndex % SgAsmPEStringSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEStringSection::pool_size};
        returnPointer = &(((SgAsmPEStringSection*)(SgAsmPEStringSection::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEStringSection*
SgAsmPEStringSection::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEStringSection* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEStringSection)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEStringSection + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEStringSection)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEStringSection)};
        unsigned long positionInPool{localIndex % SgAsmPEStringSection::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEStringSection::pool_size};
        returnPointer = &(((SgAsmPEStringSection*)(SgAsmPEStringSection::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEStringSection::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEStringSection::pools.begin(); block != SgAsmPEStringSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEStringSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEStringSection::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEStringSection*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEStringSection::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEStringSection* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEStringSection::pools.begin(); block != SgAsmPEStringSection::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEStringSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEStringSection::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEStringSection::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEStringSection::clearMemoryPool() {
    SgAsmPEStringSection* tempPointer{nullptr};
    if (SgAsmPEStringSection::pools.empty() == false) {
        auto block{SgAsmPEStringSection::pools.begin()};
        SgAsmPEStringSection::next_node = reinterpret_cast<SgAsmPEStringSection*>(*block);
        for (; block != SgAsmPEStringSection::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEStringSection*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEStringSection::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEStringSection::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEStringSection::pool_size-1]);
        }
    }
}

void
SgAsmPEStringSection::deleteMemoryPool() {
    for (auto p: SgAsmPEStringSection::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEStringSection::next_node = nullptr;
    SgAsmPEStringSection::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEStringSection::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEStringSection) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEStringSection)};
    for (size_t blockIndex = SgAsmPEStringSection::pools.size(); (blockIndex * SgAsmPEStringSection::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEStringSection) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEStringSection) = %" PRIuPTR " SgAsmPEStringSection::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEStringSection),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEStringSection),SgAsmPEStringSection::pool_size);
        }
#endif

        SgAsmPEStringSection* pointer{static_cast<SgAsmPEStringSection*>(ROSE_MALLOC(SgAsmPEStringSection::pool_size * sizeof(SgAsmPEStringSection)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEStringSection::pool_size * sizeof(SgAsmPEStringSection));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEStringSection::pool_size * sizeof(SgAsmPEStringSection));
#endif
        SgAsmPEStringSection::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEStringSection::pool_size * sizeof(SgAsmPEStringSection), V_SgAsmPEStringSection));

        if (SgAsmPEStringSection::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEStringSection* blkptr{reinterpret_cast<SgAsmPEStringSection*>(SgAsmPEStringSection::pools[blockIndex-1])};
                blkptr[SgAsmPEStringSection::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEStringSection::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEStringSection::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEStringSection::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEStringSection::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEStringSection::pool_size - 1};
    SgAsmPEStringSection* testPointer{reinterpret_cast<SgAsmPEStringSection*>(SgAsmPEStringSection::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEStringSection::pool_size * (SgAsmPEStringSection::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEStringSectionStroageClass
 * from its counterpart of SgAsmPEStringSection. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEStringSection::initializeStorageClassArray(SgAsmPEStringSectionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEStringSection::pools.begin(); block != SgAsmPEStringSection::pools.end(); ++block) {
        SgAsmPEStringSection* pointer{reinterpret_cast<SgAsmPEStringSection*>(*block)};
        for (unsigned i = 0; i < SgAsmPEStringSection::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 216252 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPESectionTable that takes its corresponding StorageClass as parameter
 */
SgAsmPESectionTable::SgAsmPESectionTable(const SgAsmPESectionTableStorageClass& storageSource)  : SgAsmGenericSection (storageSource) {

/* #line 216262 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPESectionTable*
SgAsmPESectionTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPESectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTable)};
        unsigned long positionInPool{localIndex % SgAsmPESectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPESectionTable::pool_size};
        returnPointer = &(((SgAsmPESectionTable*)(SgAsmPESectionTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPESectionTable*
SgAsmPESectionTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPESectionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESectionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESectionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESectionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPESectionTable)};
        unsigned long positionInPool{localIndex % SgAsmPESectionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPESectionTable::pool_size};
        returnPointer = &(((SgAsmPESectionTable*)(SgAsmPESectionTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPESectionTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPESectionTable::pools.begin(); block != SgAsmPESectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmPESectionTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPESectionTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPESectionTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPESectionTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPESectionTable::pools.begin(); block != SgAsmPESectionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmPESectionTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPESectionTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPESectionTable::clearMemoryPool() {
    SgAsmPESectionTable* tempPointer{nullptr};
    if (SgAsmPESectionTable::pools.empty() == false) {
        auto block{SgAsmPESectionTable::pools.begin()};
        SgAsmPESectionTable::next_node = reinterpret_cast<SgAsmPESectionTable*>(*block);
        for (; block != SgAsmPESectionTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPESectionTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPESectionTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPESectionTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPESectionTable::pool_size-1]);
        }
    }
}

void
SgAsmPESectionTable::deleteMemoryPool() {
    for (auto p: SgAsmPESectionTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmPESectionTable::next_node = nullptr;
    SgAsmPESectionTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPESectionTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESectionTable)};
    for (size_t blockIndex = SgAsmPESectionTable::pools.size(); (blockIndex * SgAsmPESectionTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESectionTable) = %" PRIuPTR " SgAsmPESectionTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESectionTable),SgAsmPESectionTable::pool_size);
        }
#endif

        SgAsmPESectionTable* pointer{static_cast<SgAsmPESectionTable*>(ROSE_MALLOC(SgAsmPESectionTable::pool_size * sizeof(SgAsmPESectionTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPESectionTable::pool_size * sizeof(SgAsmPESectionTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPESectionTable::pool_size * sizeof(SgAsmPESectionTable));
#endif
        SgAsmPESectionTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPESectionTable::pool_size * sizeof(SgAsmPESectionTable), V_SgAsmPESectionTable));

        if (SgAsmPESectionTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPESectionTable* blkptr{reinterpret_cast<SgAsmPESectionTable*>(SgAsmPESectionTable::pools[blockIndex-1])};
                blkptr[SgAsmPESectionTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPESectionTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPESectionTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPESectionTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPESectionTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPESectionTable::pool_size - 1};
    SgAsmPESectionTable* testPointer{reinterpret_cast<SgAsmPESectionTable*>(SgAsmPESectionTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPESectionTable::pool_size * (SgAsmPESectionTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPESectionTableStroageClass
 * from its counterpart of SgAsmPESectionTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPESectionTable::initializeStorageClassArray(SgAsmPESectionTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPESectionTable::pools.begin(); block != SgAsmPESectionTable::pools.end(); ++block) {
        SgAsmPESectionTable* pointer{reinterpret_cast<SgAsmPESectionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmPESectionTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 216479 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericSectionList that takes its corresponding StorageClass as parameter
 */
SgAsmGenericSectionList::SgAsmGenericSectionList(const SgAsmGenericSectionListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 216489 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_sections = storageSource.storageOf_sections.rebuildDataStoredInEasyStorageClass();
    SgAsmGenericSectionPtrList::iterator i_sections = p_sections.begin();
    for ( ; i_sections != p_sections.end(); ++i_sections) {
        (*i_sections) = (SgAsmGenericSectionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_sections)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericSectionList*
SgAsmGenericSectionList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericSectionList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSectionList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSectionList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSectionList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSectionList)};
        unsigned long positionInPool{localIndex % SgAsmGenericSectionList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSectionList::pool_size};
        returnPointer = &(((SgAsmGenericSectionList*)(SgAsmGenericSectionList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericSectionList*
SgAsmGenericSectionList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericSectionList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSectionList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSectionList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSectionList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericSectionList)};
        unsigned long positionInPool{localIndex % SgAsmGenericSectionList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSectionList::pool_size};
        returnPointer = &(((SgAsmGenericSectionList*)(SgAsmGenericSectionList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericSectionList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericSectionList::pools.begin(); block != SgAsmGenericSectionList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSectionList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSectionList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericSectionList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericSectionList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericSectionList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericSectionList::pools.begin(); block != SgAsmGenericSectionList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSectionList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSectionList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericSectionList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericSectionList::clearMemoryPool() {
    SgAsmGenericSectionList* tempPointer{nullptr};
    if (SgAsmGenericSectionList::pools.empty() == false) {
        auto block{SgAsmGenericSectionList::pools.begin()};
        SgAsmGenericSectionList::next_node = reinterpret_cast<SgAsmGenericSectionList*>(*block);
        for (; block != SgAsmGenericSectionList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericSectionList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericSectionList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericSectionList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericSectionList::pool_size-1]);
        }
    }
}

void
SgAsmGenericSectionList::deleteMemoryPool() {
    for (auto p: SgAsmGenericSectionList::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericSectionList::next_node = nullptr;
    SgAsmGenericSectionList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericSectionList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSectionList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSectionList)};
    for (size_t blockIndex = SgAsmGenericSectionList::pools.size(); (blockIndex * SgAsmGenericSectionList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSectionList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSectionList) = %" PRIuPTR " SgAsmGenericSectionList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSectionList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSectionList),SgAsmGenericSectionList::pool_size);
        }
#endif

        SgAsmGenericSectionList* pointer{static_cast<SgAsmGenericSectionList*>(ROSE_MALLOC(SgAsmGenericSectionList::pool_size * sizeof(SgAsmGenericSectionList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericSectionList::pool_size * sizeof(SgAsmGenericSectionList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericSectionList::pool_size * sizeof(SgAsmGenericSectionList));
#endif
        SgAsmGenericSectionList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericSectionList::pool_size * sizeof(SgAsmGenericSectionList), V_SgAsmGenericSectionList));

        if (SgAsmGenericSectionList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericSectionList* blkptr{reinterpret_cast<SgAsmGenericSectionList*>(SgAsmGenericSectionList::pools[blockIndex-1])};
                blkptr[SgAsmGenericSectionList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericSectionList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericSectionList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericSectionList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericSectionList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericSectionList::pool_size - 1};
    SgAsmGenericSectionList* testPointer{reinterpret_cast<SgAsmGenericSectionList*>(SgAsmGenericSectionList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericSectionList::pool_size * (SgAsmGenericSectionList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericSectionListStroageClass
 * from its counterpart of SgAsmGenericSectionList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericSectionList::initializeStorageClassArray(SgAsmGenericSectionListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericSectionList::pools.begin(); block != SgAsmGenericSectionList::pools.end(); ++block) {
        SgAsmGenericSectionList* pointer{reinterpret_cast<SgAsmGenericSectionList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSectionList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 216711 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericString that takes its corresponding StorageClass as parameter
 */
SgAsmGenericString::SgAsmGenericString(const SgAsmGenericStringStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 216721 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericString*
SgAsmGenericString::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericString)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericString)};
        unsigned long positionInPool{localIndex % SgAsmGenericString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericString::pool_size};
        returnPointer = &(((SgAsmGenericString*)(SgAsmGenericString::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericString*
SgAsmGenericString::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericString)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericString)};
        unsigned long positionInPool{localIndex % SgAsmGenericString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericString::pool_size};
        returnPointer = &(((SgAsmGenericString*)(SgAsmGenericString::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericString::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericString::pools.begin(); block != SgAsmGenericString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericString*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericString::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericString*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericString::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericString* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericString::pools.begin(); block != SgAsmGenericString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericString*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericString::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericString::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericString::clearMemoryPool() {
    SgAsmGenericString* tempPointer{nullptr};
    if (SgAsmGenericString::pools.empty() == false) {
        auto block{SgAsmGenericString::pools.begin()};
        SgAsmGenericString::next_node = reinterpret_cast<SgAsmGenericString*>(*block);
        for (; block != SgAsmGenericString::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericString*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericString::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericString::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericString::pool_size-1]);
        }
    }
}

void
SgAsmGenericString::deleteMemoryPool() {
    for (auto p: SgAsmGenericString::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericString::next_node = nullptr;
    SgAsmGenericString::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericString::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericString) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericString)};
    for (size_t blockIndex = SgAsmGenericString::pools.size(); (blockIndex * SgAsmGenericString::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericString) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericString) = %" PRIuPTR " SgAsmGenericString::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericString),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericString),SgAsmGenericString::pool_size);
        }
#endif

        SgAsmGenericString* pointer{static_cast<SgAsmGenericString*>(ROSE_MALLOC(SgAsmGenericString::pool_size * sizeof(SgAsmGenericString)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericString::pool_size * sizeof(SgAsmGenericString));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericString::pool_size * sizeof(SgAsmGenericString));
#endif
        SgAsmGenericString::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericString::pool_size * sizeof(SgAsmGenericString), V_SgAsmGenericString));

        if (SgAsmGenericString::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericString* blkptr{reinterpret_cast<SgAsmGenericString*>(SgAsmGenericString::pools[blockIndex-1])};
                blkptr[SgAsmGenericString::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericString::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericString::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericString::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericString::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericString::pool_size - 1};
    SgAsmGenericString* testPointer{reinterpret_cast<SgAsmGenericString*>(SgAsmGenericString::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericString::pool_size * (SgAsmGenericString::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericStringStroageClass
 * from its counterpart of SgAsmGenericString. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericString::initializeStorageClassArray(SgAsmGenericStringStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericString::pools.begin(); block != SgAsmGenericString::pools.end(); ++block) {
        SgAsmGenericString* pointer{reinterpret_cast<SgAsmGenericString*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericString::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 216938 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBasicString that takes its corresponding StorageClass as parameter
 */
SgAsmBasicString::SgAsmBasicString(const SgAsmBasicStringStorageClass& storageSource)  : SgAsmGenericString (storageSource) {

/* #line 216948 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_string = storageSource.storageOf_string.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBasicString*
SgAsmBasicString::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBasicString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBasicString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBasicString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBasicString)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBasicString)};
        unsigned long positionInPool{localIndex % SgAsmBasicString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBasicString::pool_size};
        returnPointer = &(((SgAsmBasicString*)(SgAsmBasicString::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBasicString*
SgAsmBasicString::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBasicString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBasicString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBasicString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBasicString)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBasicString)};
        unsigned long positionInPool{localIndex % SgAsmBasicString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBasicString::pool_size};
        returnPointer = &(((SgAsmBasicString*)(SgAsmBasicString::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBasicString::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBasicString::pools.begin(); block != SgAsmBasicString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBasicString*>(*block)};
        for (unsigned i = 0; i < SgAsmBasicString::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBasicString*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBasicString::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBasicString* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBasicString::pools.begin(); block != SgAsmBasicString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBasicString*>(*block)};
        for (unsigned i = 0; i < SgAsmBasicString::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBasicString::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBasicString::clearMemoryPool() {
    SgAsmBasicString* tempPointer{nullptr};
    if (SgAsmBasicString::pools.empty() == false) {
        auto block{SgAsmBasicString::pools.begin()};
        SgAsmBasicString::next_node = reinterpret_cast<SgAsmBasicString*>(*block);
        for (; block != SgAsmBasicString::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBasicString*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBasicString::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBasicString::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBasicString::pool_size-1]);
        }
    }
}

void
SgAsmBasicString::deleteMemoryPool() {
    for (auto p: SgAsmBasicString::pools) {
        ROSE_FREE(p);
    }
    SgAsmBasicString::next_node = nullptr;
    SgAsmBasicString::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBasicString::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBasicString) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBasicString)};
    for (size_t blockIndex = SgAsmBasicString::pools.size(); (blockIndex * SgAsmBasicString::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBasicString) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBasicString) = %" PRIuPTR " SgAsmBasicString::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBasicString),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBasicString),SgAsmBasicString::pool_size);
        }
#endif

        SgAsmBasicString* pointer{static_cast<SgAsmBasicString*>(ROSE_MALLOC(SgAsmBasicString::pool_size * sizeof(SgAsmBasicString)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBasicString::pool_size * sizeof(SgAsmBasicString));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBasicString::pool_size * sizeof(SgAsmBasicString));
#endif
        SgAsmBasicString::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBasicString::pool_size * sizeof(SgAsmBasicString), V_SgAsmBasicString));

        if (SgAsmBasicString::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBasicString* blkptr{reinterpret_cast<SgAsmBasicString*>(SgAsmBasicString::pools[blockIndex-1])};
                blkptr[SgAsmBasicString::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBasicString::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBasicString::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBasicString::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBasicString::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBasicString::pool_size - 1};
    SgAsmBasicString* testPointer{reinterpret_cast<SgAsmBasicString*>(SgAsmBasicString::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBasicString::pool_size * (SgAsmBasicString::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBasicStringStroageClass
 * from its counterpart of SgAsmBasicString. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBasicString::initializeStorageClassArray(SgAsmBasicStringStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBasicString::pools.begin(); block != SgAsmBasicString::pools.end(); ++block) {
        SgAsmBasicString* pointer{reinterpret_cast<SgAsmBasicString*>(*block)};
        for (unsigned i = 0; i < SgAsmBasicString::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 217166 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmStoredString that takes its corresponding StorageClass as parameter
 */
SgAsmStoredString::SgAsmStoredString(const SgAsmStoredStringStorageClass& storageSource)  : SgAsmGenericString (storageSource) {

/* #line 217176 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_storage = (SgAsmStringStorage*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_storage));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmStoredString*
SgAsmStoredString::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmStoredString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStoredString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStoredString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStoredString)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStoredString)};
        unsigned long positionInPool{localIndex % SgAsmStoredString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStoredString::pool_size};
        returnPointer = &(((SgAsmStoredString*)(SgAsmStoredString::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmStoredString*
SgAsmStoredString::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmStoredString* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStoredString)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStoredString + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStoredString)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmStoredString)};
        unsigned long positionInPool{localIndex % SgAsmStoredString::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStoredString::pool_size};
        returnPointer = &(((SgAsmStoredString*)(SgAsmStoredString::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmStoredString::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmStoredString::pools.begin(); block != SgAsmStoredString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStoredString*>(*block)};
        for (unsigned i = 0; i < SgAsmStoredString::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmStoredString*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmStoredString::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmStoredString* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmStoredString::pools.begin(); block != SgAsmStoredString::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStoredString*>(*block)};
        for (unsigned i = 0; i < SgAsmStoredString::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmStoredString::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmStoredString::clearMemoryPool() {
    SgAsmStoredString* tempPointer{nullptr};
    if (SgAsmStoredString::pools.empty() == false) {
        auto block{SgAsmStoredString::pools.begin()};
        SgAsmStoredString::next_node = reinterpret_cast<SgAsmStoredString*>(*block);
        for (; block != SgAsmStoredString::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmStoredString*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmStoredString::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmStoredString::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmStoredString::pool_size-1]);
        }
    }
}

void
SgAsmStoredString::deleteMemoryPool() {
    for (auto p: SgAsmStoredString::pools) {
        ROSE_FREE(p);
    }
    SgAsmStoredString::next_node = nullptr;
    SgAsmStoredString::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmStoredString::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStoredString) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStoredString)};
    for (size_t blockIndex = SgAsmStoredString::pools.size(); (blockIndex * SgAsmStoredString::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStoredString) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStoredString) = %" PRIuPTR " SgAsmStoredString::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStoredString),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStoredString),SgAsmStoredString::pool_size);
        }
#endif

        SgAsmStoredString* pointer{static_cast<SgAsmStoredString*>(ROSE_MALLOC(SgAsmStoredString::pool_size * sizeof(SgAsmStoredString)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmStoredString::pool_size * sizeof(SgAsmStoredString));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmStoredString::pool_size * sizeof(SgAsmStoredString));
#endif
        SgAsmStoredString::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmStoredString::pool_size * sizeof(SgAsmStoredString), V_SgAsmStoredString));

        if (SgAsmStoredString::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmStoredString* blkptr{reinterpret_cast<SgAsmStoredString*>(SgAsmStoredString::pools[blockIndex-1])};
                blkptr[SgAsmStoredString::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmStoredString::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmStoredString::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmStoredString::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmStoredString::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmStoredString::pool_size - 1};
    SgAsmStoredString* testPointer{reinterpret_cast<SgAsmStoredString*>(SgAsmStoredString::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmStoredString::pool_size * (SgAsmStoredString::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmStoredStringStroageClass
 * from its counterpart of SgAsmStoredString. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmStoredString::initializeStorageClassArray(SgAsmStoredStringStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmStoredString::pools.begin(); block != SgAsmStoredString::pools.end(); ++block) {
        SgAsmStoredString* pointer{reinterpret_cast<SgAsmStoredString*>(*block)};
        for (unsigned i = 0; i < SgAsmStoredString::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 217394 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericStrtab that takes its corresponding StorageClass as parameter
 */
SgAsmGenericStrtab::SgAsmGenericStrtab(const SgAsmGenericStrtabStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 217404 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_container = (SgAsmGenericSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_container));
    p_storageList = storageSource.storageOf_storageList.rebuildDataStoredInEasyStorageClass();
    SgAsmGenericStrtab::referenced_t::iterator i_storageList = p_storageList.begin();
    for ( ; i_storageList != p_storageList.end(); ++i_storageList) {
        (*i_storageList) = (SgAsmGenericStrtab::referenced_t::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_storageList)) );
    }
    p_freeList = storageSource.storageOf_freeList.rebuildDataStoredInEasyStorageClass();
     p_dontFree = (SgAsmStringStorage*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dontFree));
    p_numberFreed = storageSource.storageOf_numberFreed;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericStrtab*
SgAsmGenericStrtab::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericStrtab* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericStrtab)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericStrtab + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericStrtab)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericStrtab)};
        unsigned long positionInPool{localIndex % SgAsmGenericStrtab::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericStrtab::pool_size};
        returnPointer = &(((SgAsmGenericStrtab*)(SgAsmGenericStrtab::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericStrtab*
SgAsmGenericStrtab::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericStrtab* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericStrtab)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericStrtab + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericStrtab)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericStrtab)};
        unsigned long positionInPool{localIndex % SgAsmGenericStrtab::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericStrtab::pool_size};
        returnPointer = &(((SgAsmGenericStrtab*)(SgAsmGenericStrtab::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericStrtab::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericStrtab::pools.begin(); block != SgAsmGenericStrtab::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericStrtab::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericStrtab*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericStrtab::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericStrtab* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericStrtab::pools.begin(); block != SgAsmGenericStrtab::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericStrtab::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericStrtab::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericStrtab::clearMemoryPool() {
    SgAsmGenericStrtab* tempPointer{nullptr};
    if (SgAsmGenericStrtab::pools.empty() == false) {
        auto block{SgAsmGenericStrtab::pools.begin()};
        SgAsmGenericStrtab::next_node = reinterpret_cast<SgAsmGenericStrtab*>(*block);
        for (; block != SgAsmGenericStrtab::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericStrtab*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericStrtab::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericStrtab::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericStrtab::pool_size-1]);
        }
    }
}

void
SgAsmGenericStrtab::deleteMemoryPool() {
    for (auto p: SgAsmGenericStrtab::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericStrtab::next_node = nullptr;
    SgAsmGenericStrtab::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericStrtab::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericStrtab) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericStrtab)};
    for (size_t blockIndex = SgAsmGenericStrtab::pools.size(); (blockIndex * SgAsmGenericStrtab::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericStrtab) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericStrtab) = %" PRIuPTR " SgAsmGenericStrtab::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericStrtab),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericStrtab),SgAsmGenericStrtab::pool_size);
        }
#endif

        SgAsmGenericStrtab* pointer{static_cast<SgAsmGenericStrtab*>(ROSE_MALLOC(SgAsmGenericStrtab::pool_size * sizeof(SgAsmGenericStrtab)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericStrtab::pool_size * sizeof(SgAsmGenericStrtab));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericStrtab::pool_size * sizeof(SgAsmGenericStrtab));
#endif
        SgAsmGenericStrtab::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericStrtab::pool_size * sizeof(SgAsmGenericStrtab), V_SgAsmGenericStrtab));

        if (SgAsmGenericStrtab::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericStrtab* blkptr{reinterpret_cast<SgAsmGenericStrtab*>(SgAsmGenericStrtab::pools[blockIndex-1])};
                blkptr[SgAsmGenericStrtab::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericStrtab::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericStrtab::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericStrtab::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericStrtab::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericStrtab::pool_size - 1};
    SgAsmGenericStrtab* testPointer{reinterpret_cast<SgAsmGenericStrtab*>(SgAsmGenericStrtab::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericStrtab::pool_size * (SgAsmGenericStrtab::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericStrtabStroageClass
 * from its counterpart of SgAsmGenericStrtab. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericStrtab::initializeStorageClassArray(SgAsmGenericStrtabStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericStrtab::pools.begin(); block != SgAsmGenericStrtab::pools.end(); ++block) {
        SgAsmGenericStrtab* pointer{reinterpret_cast<SgAsmGenericStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericStrtab::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 217630 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCoffStrtab that takes its corresponding StorageClass as parameter
 */
SgAsmCoffStrtab::SgAsmCoffStrtab(const SgAsmCoffStrtabStorageClass& storageSource)  : SgAsmGenericStrtab (storageSource) {

/* #line 217640 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCoffStrtab*
SgAsmCoffStrtab::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCoffStrtab* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffStrtab)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffStrtab + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffStrtab)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffStrtab)};
        unsigned long positionInPool{localIndex % SgAsmCoffStrtab::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffStrtab::pool_size};
        returnPointer = &(((SgAsmCoffStrtab*)(SgAsmCoffStrtab::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCoffStrtab*
SgAsmCoffStrtab::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCoffStrtab* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffStrtab)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffStrtab + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffStrtab)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCoffStrtab)};
        unsigned long positionInPool{localIndex % SgAsmCoffStrtab::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffStrtab::pool_size};
        returnPointer = &(((SgAsmCoffStrtab*)(SgAsmCoffStrtab::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCoffStrtab::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCoffStrtab::pools.begin(); block != SgAsmCoffStrtab::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffStrtab::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCoffStrtab*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCoffStrtab::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCoffStrtab* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCoffStrtab::pools.begin(); block != SgAsmCoffStrtab::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffStrtab::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCoffStrtab::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCoffStrtab::clearMemoryPool() {
    SgAsmCoffStrtab* tempPointer{nullptr};
    if (SgAsmCoffStrtab::pools.empty() == false) {
        auto block{SgAsmCoffStrtab::pools.begin()};
        SgAsmCoffStrtab::next_node = reinterpret_cast<SgAsmCoffStrtab*>(*block);
        for (; block != SgAsmCoffStrtab::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCoffStrtab*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCoffStrtab::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCoffStrtab::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCoffStrtab::pool_size-1]);
        }
    }
}

void
SgAsmCoffStrtab::deleteMemoryPool() {
    for (auto p: SgAsmCoffStrtab::pools) {
        ROSE_FREE(p);
    }
    SgAsmCoffStrtab::next_node = nullptr;
    SgAsmCoffStrtab::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCoffStrtab::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffStrtab) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffStrtab)};
    for (size_t blockIndex = SgAsmCoffStrtab::pools.size(); (blockIndex * SgAsmCoffStrtab::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffStrtab) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffStrtab) = %" PRIuPTR " SgAsmCoffStrtab::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffStrtab),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffStrtab),SgAsmCoffStrtab::pool_size);
        }
#endif

        SgAsmCoffStrtab* pointer{static_cast<SgAsmCoffStrtab*>(ROSE_MALLOC(SgAsmCoffStrtab::pool_size * sizeof(SgAsmCoffStrtab)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCoffStrtab::pool_size * sizeof(SgAsmCoffStrtab));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCoffStrtab::pool_size * sizeof(SgAsmCoffStrtab));
#endif
        SgAsmCoffStrtab::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCoffStrtab::pool_size * sizeof(SgAsmCoffStrtab), V_SgAsmCoffStrtab));

        if (SgAsmCoffStrtab::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCoffStrtab* blkptr{reinterpret_cast<SgAsmCoffStrtab*>(SgAsmCoffStrtab::pools[blockIndex-1])};
                blkptr[SgAsmCoffStrtab::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCoffStrtab::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCoffStrtab::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCoffStrtab::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCoffStrtab::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCoffStrtab::pool_size - 1};
    SgAsmCoffStrtab* testPointer{reinterpret_cast<SgAsmCoffStrtab*>(SgAsmCoffStrtab::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCoffStrtab::pool_size * (SgAsmCoffStrtab::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCoffStrtabStroageClass
 * from its counterpart of SgAsmCoffStrtab. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCoffStrtab::initializeStorageClassArray(SgAsmCoffStrtabStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCoffStrtab::pools.begin(); block != SgAsmCoffStrtab::pools.end(); ++block) {
        SgAsmCoffStrtab* pointer{reinterpret_cast<SgAsmCoffStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffStrtab::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 217857 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfStrtab that takes its corresponding StorageClass as parameter
 */
SgAsmElfStrtab::SgAsmElfStrtab(const SgAsmElfStrtabStorageClass& storageSource)  : SgAsmGenericStrtab (storageSource) {

/* #line 217867 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfStrtab*
SgAsmElfStrtab::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfStrtab* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfStrtab)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfStrtab + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfStrtab)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStrtab)};
        unsigned long positionInPool{localIndex % SgAsmElfStrtab::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfStrtab::pool_size};
        returnPointer = &(((SgAsmElfStrtab*)(SgAsmElfStrtab::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfStrtab*
SgAsmElfStrtab::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfStrtab* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfStrtab)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfStrtab + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfStrtab)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfStrtab)};
        unsigned long positionInPool{localIndex % SgAsmElfStrtab::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfStrtab::pool_size};
        returnPointer = &(((SgAsmElfStrtab*)(SgAsmElfStrtab::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfStrtab::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfStrtab::pools.begin(); block != SgAsmElfStrtab::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmElfStrtab::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfStrtab*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfStrtab::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfStrtab* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfStrtab::pools.begin(); block != SgAsmElfStrtab::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmElfStrtab::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfStrtab::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfStrtab::clearMemoryPool() {
    SgAsmElfStrtab* tempPointer{nullptr};
    if (SgAsmElfStrtab::pools.empty() == false) {
        auto block{SgAsmElfStrtab::pools.begin()};
        SgAsmElfStrtab::next_node = reinterpret_cast<SgAsmElfStrtab*>(*block);
        for (; block != SgAsmElfStrtab::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfStrtab*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfStrtab::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfStrtab::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfStrtab::pool_size-1]);
        }
    }
}

void
SgAsmElfStrtab::deleteMemoryPool() {
    for (auto p: SgAsmElfStrtab::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfStrtab::next_node = nullptr;
    SgAsmElfStrtab::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfStrtab::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStrtab) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfStrtab)};
    for (size_t blockIndex = SgAsmElfStrtab::pools.size(); (blockIndex * SgAsmElfStrtab::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStrtab) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfStrtab) = %" PRIuPTR " SgAsmElfStrtab::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfStrtab),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfStrtab),SgAsmElfStrtab::pool_size);
        }
#endif

        SgAsmElfStrtab* pointer{static_cast<SgAsmElfStrtab*>(ROSE_MALLOC(SgAsmElfStrtab::pool_size * sizeof(SgAsmElfStrtab)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfStrtab::pool_size * sizeof(SgAsmElfStrtab));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfStrtab::pool_size * sizeof(SgAsmElfStrtab));
#endif
        SgAsmElfStrtab::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfStrtab::pool_size * sizeof(SgAsmElfStrtab), V_SgAsmElfStrtab));

        if (SgAsmElfStrtab::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfStrtab* blkptr{reinterpret_cast<SgAsmElfStrtab*>(SgAsmElfStrtab::pools[blockIndex-1])};
                blkptr[SgAsmElfStrtab::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfStrtab::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfStrtab::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfStrtab::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfStrtab::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfStrtab::pool_size - 1};
    SgAsmElfStrtab* testPointer{reinterpret_cast<SgAsmElfStrtab*>(SgAsmElfStrtab::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfStrtab::pool_size * (SgAsmElfStrtab::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfStrtabStroageClass
 * from its counterpart of SgAsmElfStrtab. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfStrtab::initializeStorageClassArray(SgAsmElfStrtabStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfStrtab::pools.begin(); block != SgAsmElfStrtab::pools.end(); ++block) {
        SgAsmElfStrtab* pointer{reinterpret_cast<SgAsmElfStrtab*>(*block)};
        for (unsigned i = 0; i < SgAsmElfStrtab::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 218084 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericSymbol that takes its corresponding StorageClass as parameter
 */
SgAsmGenericSymbol::SgAsmGenericSymbol(const SgAsmGenericSymbolStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 218094 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_definitionState = storageSource.storageOf_definitionState;
    p_binding = storageSource.storageOf_binding;
    p_type = storageSource.storageOf_type;
    p_value = storageSource.storageOf_value;
    p_size = storageSource.storageOf_size;
     p_bound = (SgAsmGenericSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_bound));
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericSymbol*
SgAsmGenericSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbol)};
        unsigned long positionInPool{localIndex % SgAsmGenericSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSymbol::pool_size};
        returnPointer = &(((SgAsmGenericSymbol*)(SgAsmGenericSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericSymbol*
SgAsmGenericSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericSymbol)};
        unsigned long positionInPool{localIndex % SgAsmGenericSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSymbol::pool_size};
        returnPointer = &(((SgAsmGenericSymbol*)(SgAsmGenericSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericSymbol::pools.begin(); block != SgAsmGenericSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericSymbol::pools.begin(); block != SgAsmGenericSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericSymbol::clearMemoryPool() {
    SgAsmGenericSymbol* tempPointer{nullptr};
    if (SgAsmGenericSymbol::pools.empty() == false) {
        auto block{SgAsmGenericSymbol::pools.begin()};
        SgAsmGenericSymbol::next_node = reinterpret_cast<SgAsmGenericSymbol*>(*block);
        for (; block != SgAsmGenericSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericSymbol::pool_size-1]);
        }
    }
}

void
SgAsmGenericSymbol::deleteMemoryPool() {
    for (auto p: SgAsmGenericSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericSymbol::next_node = nullptr;
    SgAsmGenericSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSymbol)};
    for (size_t blockIndex = SgAsmGenericSymbol::pools.size(); (blockIndex * SgAsmGenericSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSymbol) = %" PRIuPTR " SgAsmGenericSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSymbol),SgAsmGenericSymbol::pool_size);
        }
#endif

        SgAsmGenericSymbol* pointer{static_cast<SgAsmGenericSymbol*>(ROSE_MALLOC(SgAsmGenericSymbol::pool_size * sizeof(SgAsmGenericSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericSymbol::pool_size * sizeof(SgAsmGenericSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericSymbol::pool_size * sizeof(SgAsmGenericSymbol));
#endif
        SgAsmGenericSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericSymbol::pool_size * sizeof(SgAsmGenericSymbol), V_SgAsmGenericSymbol));

        if (SgAsmGenericSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericSymbol* blkptr{reinterpret_cast<SgAsmGenericSymbol*>(SgAsmGenericSymbol::pools[blockIndex-1])};
                blkptr[SgAsmGenericSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericSymbol::pool_size - 1};
    SgAsmGenericSymbol* testPointer{reinterpret_cast<SgAsmGenericSymbol*>(SgAsmGenericSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericSymbol::pool_size * (SgAsmGenericSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericSymbolStroageClass
 * from its counterpart of SgAsmGenericSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericSymbol::initializeStorageClassArray(SgAsmGenericSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericSymbol::pools.begin(); block != SgAsmGenericSymbol::pools.end(); ++block) {
        SgAsmGenericSymbol* pointer{reinterpret_cast<SgAsmGenericSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 218318 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCoffSymbol that takes its corresponding StorageClass as parameter
 */
SgAsmCoffSymbol::SgAsmCoffSymbol(const SgAsmCoffSymbolStorageClass& storageSource)  : SgAsmGenericSymbol (storageSource) {

/* #line 218328 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_st_name = storageSource.storageOf_st_name.rebuildDataStoredInEasyStorageClass();
    p_st_name_offset = storageSource.storageOf_st_name_offset;
    p_st_section_num = storageSource.storageOf_st_section_num;
    p_st_type = storageSource.storageOf_st_type;
    p_st_storage_class = storageSource.storageOf_st_storage_class;
    p_st_num_aux_entries = storageSource.storageOf_st_num_aux_entries;
    p_auxiliaryData = storageSource.storageOf_auxiliaryData.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCoffSymbol*
SgAsmCoffSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCoffSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCoffSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbol)};
        unsigned long positionInPool{localIndex % SgAsmCoffSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffSymbol::pool_size};
        returnPointer = &(((SgAsmCoffSymbol*)(SgAsmCoffSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCoffSymbol*
SgAsmCoffSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCoffSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCoffSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCoffSymbol)};
        unsigned long positionInPool{localIndex % SgAsmCoffSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCoffSymbol::pool_size};
        returnPointer = &(((SgAsmCoffSymbol*)(SgAsmCoffSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCoffSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCoffSymbol::pools.begin(); block != SgAsmCoffSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCoffSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCoffSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCoffSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCoffSymbol::pools.begin(); block != SgAsmCoffSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCoffSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCoffSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCoffSymbol::clearMemoryPool() {
    SgAsmCoffSymbol* tempPointer{nullptr};
    if (SgAsmCoffSymbol::pools.empty() == false) {
        auto block{SgAsmCoffSymbol::pools.begin()};
        SgAsmCoffSymbol::next_node = reinterpret_cast<SgAsmCoffSymbol*>(*block);
        for (; block != SgAsmCoffSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCoffSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCoffSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCoffSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCoffSymbol::pool_size-1]);
        }
    }
}

void
SgAsmCoffSymbol::deleteMemoryPool() {
    for (auto p: SgAsmCoffSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAsmCoffSymbol::next_node = nullptr;
    SgAsmCoffSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCoffSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbol)};
    for (size_t blockIndex = SgAsmCoffSymbol::pools.size(); (blockIndex * SgAsmCoffSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbol) = %" PRIuPTR " SgAsmCoffSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCoffSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCoffSymbol),SgAsmCoffSymbol::pool_size);
        }
#endif

        SgAsmCoffSymbol* pointer{static_cast<SgAsmCoffSymbol*>(ROSE_MALLOC(SgAsmCoffSymbol::pool_size * sizeof(SgAsmCoffSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCoffSymbol::pool_size * sizeof(SgAsmCoffSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCoffSymbol::pool_size * sizeof(SgAsmCoffSymbol));
#endif
        SgAsmCoffSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCoffSymbol::pool_size * sizeof(SgAsmCoffSymbol), V_SgAsmCoffSymbol));

        if (SgAsmCoffSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCoffSymbol* blkptr{reinterpret_cast<SgAsmCoffSymbol*>(SgAsmCoffSymbol::pools[blockIndex-1])};
                blkptr[SgAsmCoffSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCoffSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCoffSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCoffSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCoffSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCoffSymbol::pool_size - 1};
    SgAsmCoffSymbol* testPointer{reinterpret_cast<SgAsmCoffSymbol*>(SgAsmCoffSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCoffSymbol::pool_size * (SgAsmCoffSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCoffSymbolStroageClass
 * from its counterpart of SgAsmCoffSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCoffSymbol::initializeStorageClassArray(SgAsmCoffSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCoffSymbol::pools.begin(); block != SgAsmCoffSymbol::pools.end(); ++block) {
        SgAsmCoffSymbol* pointer{reinterpret_cast<SgAsmCoffSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmCoffSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 218552 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmElfSymbol that takes its corresponding StorageClass as parameter
 */
SgAsmElfSymbol::SgAsmElfSymbol(const SgAsmElfSymbolStorageClass& storageSource)  : SgAsmGenericSymbol (storageSource) {

/* #line 218562 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_st_info = storageSource.storageOf_st_info;
    p_st_res1 = storageSource.storageOf_st_res1;
    p_st_shndx = storageSource.storageOf_st_shndx;
    p_st_size = storageSource.storageOf_st_size;
    p_extra = storageSource.storageOf_extra.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmElfSymbol*
SgAsmElfSymbol::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmElfSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmElfSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbol)};
        unsigned long positionInPool{localIndex % SgAsmElfSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymbol::pool_size};
        returnPointer = &(((SgAsmElfSymbol*)(SgAsmElfSymbol::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmElfSymbol*
SgAsmElfSymbol::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmElfSymbol* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbol)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbol + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmElfSymbol)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmElfSymbol)};
        unsigned long positionInPool{localIndex % SgAsmElfSymbol::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmElfSymbol::pool_size};
        returnPointer = &(((SgAsmElfSymbol*)(SgAsmElfSymbol::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmElfSymbol::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmElfSymbol::pools.begin(); block != SgAsmElfSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbol::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmElfSymbol*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmElfSymbol::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmElfSymbol* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmElfSymbol::pools.begin(); block != SgAsmElfSymbol::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmElfSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbol::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmElfSymbol::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmElfSymbol::clearMemoryPool() {
    SgAsmElfSymbol* tempPointer{nullptr};
    if (SgAsmElfSymbol::pools.empty() == false) {
        auto block{SgAsmElfSymbol::pools.begin()};
        SgAsmElfSymbol::next_node = reinterpret_cast<SgAsmElfSymbol*>(*block);
        for (; block != SgAsmElfSymbol::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmElfSymbol*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmElfSymbol::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmElfSymbol::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmElfSymbol::pool_size-1]);
        }
    }
}

void
SgAsmElfSymbol::deleteMemoryPool() {
    for (auto p: SgAsmElfSymbol::pools) {
        ROSE_FREE(p);
    }
    SgAsmElfSymbol::next_node = nullptr;
    SgAsmElfSymbol::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmElfSymbol::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbol) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbol)};
    for (size_t blockIndex = SgAsmElfSymbol::pools.size(); (blockIndex * SgAsmElfSymbol::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbol) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbol) = %" PRIuPTR " SgAsmElfSymbol::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmElfSymbol),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmElfSymbol),SgAsmElfSymbol::pool_size);
        }
#endif

        SgAsmElfSymbol* pointer{static_cast<SgAsmElfSymbol*>(ROSE_MALLOC(SgAsmElfSymbol::pool_size * sizeof(SgAsmElfSymbol)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmElfSymbol::pool_size * sizeof(SgAsmElfSymbol));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmElfSymbol::pool_size * sizeof(SgAsmElfSymbol));
#endif
        SgAsmElfSymbol::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmElfSymbol::pool_size * sizeof(SgAsmElfSymbol), V_SgAsmElfSymbol));

        if (SgAsmElfSymbol::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmElfSymbol* blkptr{reinterpret_cast<SgAsmElfSymbol*>(SgAsmElfSymbol::pools[blockIndex-1])};
                blkptr[SgAsmElfSymbol::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmElfSymbol::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmElfSymbol::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmElfSymbol::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmElfSymbol::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmElfSymbol::pool_size - 1};
    SgAsmElfSymbol* testPointer{reinterpret_cast<SgAsmElfSymbol*>(SgAsmElfSymbol::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmElfSymbol::pool_size * (SgAsmElfSymbol::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmElfSymbolStroageClass
 * from its counterpart of SgAsmElfSymbol. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmElfSymbol::initializeStorageClassArray(SgAsmElfSymbolStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmElfSymbol::pools.begin(); block != SgAsmElfSymbol::pools.end(); ++block) {
        SgAsmElfSymbol* pointer{reinterpret_cast<SgAsmElfSymbol*>(*block)};
        for (unsigned i = 0; i < SgAsmElfSymbol::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 218784 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericSymbolList that takes its corresponding StorageClass as parameter
 */
SgAsmGenericSymbolList::SgAsmGenericSymbolList(const SgAsmGenericSymbolListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 218794 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_symbols = storageSource.storageOf_symbols.rebuildDataStoredInEasyStorageClass();
    SgAsmGenericSymbolPtrList::iterator i_symbols = p_symbols.begin();
    for ( ; i_symbols != p_symbols.end(); ++i_symbols) {
        (*i_symbols) = (SgAsmGenericSymbolPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_symbols)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericSymbolList*
SgAsmGenericSymbolList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericSymbolList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSymbolList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSymbolList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericSymbolList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbolList)};
        unsigned long positionInPool{localIndex % SgAsmGenericSymbolList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSymbolList::pool_size};
        returnPointer = &(((SgAsmGenericSymbolList*)(SgAsmGenericSymbolList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericSymbolList*
SgAsmGenericSymbolList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericSymbolList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSymbolList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSymbolList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericSymbolList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericSymbolList)};
        unsigned long positionInPool{localIndex % SgAsmGenericSymbolList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericSymbolList::pool_size};
        returnPointer = &(((SgAsmGenericSymbolList*)(SgAsmGenericSymbolList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericSymbolList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericSymbolList::pools.begin(); block != SgAsmGenericSymbolList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSymbolList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericSymbolList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericSymbolList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericSymbolList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericSymbolList::pools.begin(); block != SgAsmGenericSymbolList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSymbolList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericSymbolList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericSymbolList::clearMemoryPool() {
    SgAsmGenericSymbolList* tempPointer{nullptr};
    if (SgAsmGenericSymbolList::pools.empty() == false) {
        auto block{SgAsmGenericSymbolList::pools.begin()};
        SgAsmGenericSymbolList::next_node = reinterpret_cast<SgAsmGenericSymbolList*>(*block);
        for (; block != SgAsmGenericSymbolList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericSymbolList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericSymbolList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericSymbolList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericSymbolList::pool_size-1]);
        }
    }
}

void
SgAsmGenericSymbolList::deleteMemoryPool() {
    for (auto p: SgAsmGenericSymbolList::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericSymbolList::next_node = nullptr;
    SgAsmGenericSymbolList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericSymbolList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbolList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSymbolList)};
    for (size_t blockIndex = SgAsmGenericSymbolList::pools.size(); (blockIndex * SgAsmGenericSymbolList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbolList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSymbolList) = %" PRIuPTR " SgAsmGenericSymbolList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericSymbolList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericSymbolList),SgAsmGenericSymbolList::pool_size);
        }
#endif

        SgAsmGenericSymbolList* pointer{static_cast<SgAsmGenericSymbolList*>(ROSE_MALLOC(SgAsmGenericSymbolList::pool_size * sizeof(SgAsmGenericSymbolList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericSymbolList::pool_size * sizeof(SgAsmGenericSymbolList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericSymbolList::pool_size * sizeof(SgAsmGenericSymbolList));
#endif
        SgAsmGenericSymbolList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericSymbolList::pool_size * sizeof(SgAsmGenericSymbolList), V_SgAsmGenericSymbolList));

        if (SgAsmGenericSymbolList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericSymbolList* blkptr{reinterpret_cast<SgAsmGenericSymbolList*>(SgAsmGenericSymbolList::pools[blockIndex-1])};
                blkptr[SgAsmGenericSymbolList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericSymbolList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericSymbolList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericSymbolList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericSymbolList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericSymbolList::pool_size - 1};
    SgAsmGenericSymbolList* testPointer{reinterpret_cast<SgAsmGenericSymbolList*>(SgAsmGenericSymbolList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericSymbolList::pool_size * (SgAsmGenericSymbolList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericSymbolListStroageClass
 * from its counterpart of SgAsmGenericSymbolList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericSymbolList::initializeStorageClassArray(SgAsmGenericSymbolListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericSymbolList::pools.begin(); block != SgAsmGenericSymbolList::pools.end(); ++block) {
        SgAsmGenericSymbolList* pointer{reinterpret_cast<SgAsmGenericSymbolList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericSymbolList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 219016 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLEEntryPoint that takes its corresponding StorageClass as parameter
 */
SgAsmLEEntryPoint::SgAsmLEEntryPoint(const SgAsmLEEntryPointStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 219026 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmLEEntryPointPtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmLEEntryPointPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }
    p_flags = storageSource.storageOf_flags;
    p_objnum = storageSource.storageOf_objnum;
    p_entry_type = storageSource.storageOf_entry_type;
    p_res1 = storageSource.storageOf_res1;
    p_entry_offset = storageSource.storageOf_entry_offset;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLEEntryPoint*
SgAsmLEEntryPoint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLEEntryPoint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEEntryPoint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEEntryPoint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEEntryPoint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryPoint)};
        unsigned long positionInPool{localIndex % SgAsmLEEntryPoint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEEntryPoint::pool_size};
        returnPointer = &(((SgAsmLEEntryPoint*)(SgAsmLEEntryPoint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLEEntryPoint*
SgAsmLEEntryPoint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLEEntryPoint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEEntryPoint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEEntryPoint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEEntryPoint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLEEntryPoint)};
        unsigned long positionInPool{localIndex % SgAsmLEEntryPoint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEEntryPoint::pool_size};
        returnPointer = &(((SgAsmLEEntryPoint*)(SgAsmLEEntryPoint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLEEntryPoint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLEEntryPoint::pools.begin(); block != SgAsmLEEntryPoint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmLEEntryPoint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLEEntryPoint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLEEntryPoint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLEEntryPoint* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLEEntryPoint::pools.begin(); block != SgAsmLEEntryPoint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmLEEntryPoint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLEEntryPoint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLEEntryPoint::clearMemoryPool() {
    SgAsmLEEntryPoint* tempPointer{nullptr};
    if (SgAsmLEEntryPoint::pools.empty() == false) {
        auto block{SgAsmLEEntryPoint::pools.begin()};
        SgAsmLEEntryPoint::next_node = reinterpret_cast<SgAsmLEEntryPoint*>(*block);
        for (; block != SgAsmLEEntryPoint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLEEntryPoint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLEEntryPoint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLEEntryPoint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLEEntryPoint::pool_size-1]);
        }
    }
}

void
SgAsmLEEntryPoint::deleteMemoryPool() {
    for (auto p: SgAsmLEEntryPoint::pools) {
        ROSE_FREE(p);
    }
    SgAsmLEEntryPoint::next_node = nullptr;
    SgAsmLEEntryPoint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLEEntryPoint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryPoint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEEntryPoint)};
    for (size_t blockIndex = SgAsmLEEntryPoint::pools.size(); (blockIndex * SgAsmLEEntryPoint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryPoint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEEntryPoint) = %" PRIuPTR " SgAsmLEEntryPoint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEEntryPoint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEEntryPoint),SgAsmLEEntryPoint::pool_size);
        }
#endif

        SgAsmLEEntryPoint* pointer{static_cast<SgAsmLEEntryPoint*>(ROSE_MALLOC(SgAsmLEEntryPoint::pool_size * sizeof(SgAsmLEEntryPoint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLEEntryPoint::pool_size * sizeof(SgAsmLEEntryPoint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLEEntryPoint::pool_size * sizeof(SgAsmLEEntryPoint));
#endif
        SgAsmLEEntryPoint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLEEntryPoint::pool_size * sizeof(SgAsmLEEntryPoint), V_SgAsmLEEntryPoint));

        if (SgAsmLEEntryPoint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLEEntryPoint* blkptr{reinterpret_cast<SgAsmLEEntryPoint*>(SgAsmLEEntryPoint::pools[blockIndex-1])};
                blkptr[SgAsmLEEntryPoint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLEEntryPoint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLEEntryPoint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLEEntryPoint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLEEntryPoint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLEEntryPoint::pool_size - 1};
    SgAsmLEEntryPoint* testPointer{reinterpret_cast<SgAsmLEEntryPoint*>(SgAsmLEEntryPoint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLEEntryPoint::pool_size * (SgAsmLEEntryPoint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLEEntryPointStroageClass
 * from its counterpart of SgAsmLEEntryPoint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLEEntryPoint::initializeStorageClassArray(SgAsmLEEntryPointStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLEEntryPoint::pools.begin(); block != SgAsmLEEntryPoint::pools.end(); ++block) {
        SgAsmLEEntryPoint* pointer{reinterpret_cast<SgAsmLEEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmLEEntryPoint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 219253 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLEPageTableEntry that takes its corresponding StorageClass as parameter
 */
SgAsmLEPageTableEntry::SgAsmLEPageTableEntry(const SgAsmLEPageTableEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 219263 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_pageno = storageSource.storageOf_pageno;
    p_flags = storageSource.storageOf_flags;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLEPageTableEntry*
SgAsmLEPageTableEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLEPageTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEPageTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEPageTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLEPageTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmLEPageTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEPageTableEntry::pool_size};
        returnPointer = &(((SgAsmLEPageTableEntry*)(SgAsmLEPageTableEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLEPageTableEntry*
SgAsmLEPageTableEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLEPageTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEPageTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEPageTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLEPageTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLEPageTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmLEPageTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLEPageTableEntry::pool_size};
        returnPointer = &(((SgAsmLEPageTableEntry*)(SgAsmLEPageTableEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLEPageTableEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLEPageTableEntry::pools.begin(); block != SgAsmLEPageTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEPageTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmLEPageTableEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLEPageTableEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLEPageTableEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLEPageTableEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLEPageTableEntry::pools.begin(); block != SgAsmLEPageTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLEPageTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmLEPageTableEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLEPageTableEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLEPageTableEntry::clearMemoryPool() {
    SgAsmLEPageTableEntry* tempPointer{nullptr};
    if (SgAsmLEPageTableEntry::pools.empty() == false) {
        auto block{SgAsmLEPageTableEntry::pools.begin()};
        SgAsmLEPageTableEntry::next_node = reinterpret_cast<SgAsmLEPageTableEntry*>(*block);
        for (; block != SgAsmLEPageTableEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLEPageTableEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLEPageTableEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLEPageTableEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLEPageTableEntry::pool_size-1]);
        }
    }
}

void
SgAsmLEPageTableEntry::deleteMemoryPool() {
    for (auto p: SgAsmLEPageTableEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmLEPageTableEntry::next_node = nullptr;
    SgAsmLEPageTableEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLEPageTableEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTableEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEPageTableEntry)};
    for (size_t blockIndex = SgAsmLEPageTableEntry::pools.size(); (blockIndex * SgAsmLEPageTableEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTableEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEPageTableEntry) = %" PRIuPTR " SgAsmLEPageTableEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLEPageTableEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLEPageTableEntry),SgAsmLEPageTableEntry::pool_size);
        }
#endif

        SgAsmLEPageTableEntry* pointer{static_cast<SgAsmLEPageTableEntry*>(ROSE_MALLOC(SgAsmLEPageTableEntry::pool_size * sizeof(SgAsmLEPageTableEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLEPageTableEntry::pool_size * sizeof(SgAsmLEPageTableEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLEPageTableEntry::pool_size * sizeof(SgAsmLEPageTableEntry));
#endif
        SgAsmLEPageTableEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLEPageTableEntry::pool_size * sizeof(SgAsmLEPageTableEntry), V_SgAsmLEPageTableEntry));

        if (SgAsmLEPageTableEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLEPageTableEntry* blkptr{reinterpret_cast<SgAsmLEPageTableEntry*>(SgAsmLEPageTableEntry::pools[blockIndex-1])};
                blkptr[SgAsmLEPageTableEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLEPageTableEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLEPageTableEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLEPageTableEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLEPageTableEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLEPageTableEntry::pool_size - 1};
    SgAsmLEPageTableEntry* testPointer{reinterpret_cast<SgAsmLEPageTableEntry*>(SgAsmLEPageTableEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLEPageTableEntry::pool_size * (SgAsmLEPageTableEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLEPageTableEntryStroageClass
 * from its counterpart of SgAsmLEPageTableEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLEPageTableEntry::initializeStorageClassArray(SgAsmLEPageTableEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLEPageTableEntry::pools.begin(); block != SgAsmLEPageTableEntry::pools.end(); ++block) {
        SgAsmLEPageTableEntry* pointer{reinterpret_cast<SgAsmLEPageTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmLEPageTableEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 219482 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmLESectionTableEntry that takes its corresponding StorageClass as parameter
 */
SgAsmLESectionTableEntry::SgAsmLESectionTableEntry(const SgAsmLESectionTableEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 219492 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_flags = storageSource.storageOf_flags;
    p_pageMapIndex = storageSource.storageOf_pageMapIndex;
    p_pageMapNEntries = storageSource.storageOf_pageMapNEntries;
    p_res1 = storageSource.storageOf_res1;
    p_mappedSize = storageSource.storageOf_mappedSize;
    p_baseAddr = storageSource.storageOf_baseAddr;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmLESectionTableEntry*
SgAsmLESectionTableEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmLESectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmLESectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmLESectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLESectionTableEntry::pool_size};
        returnPointer = &(((SgAsmLESectionTableEntry*)(SgAsmLESectionTableEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmLESectionTableEntry*
SgAsmLESectionTableEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmLESectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmLESectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmLESectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmLESectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmLESectionTableEntry::pool_size};
        returnPointer = &(((SgAsmLESectionTableEntry*)(SgAsmLESectionTableEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmLESectionTableEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmLESectionTableEntry::pools.begin(); block != SgAsmLESectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmLESectionTableEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmLESectionTableEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmLESectionTableEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmLESectionTableEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmLESectionTableEntry::pools.begin(); block != SgAsmLESectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmLESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmLESectionTableEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmLESectionTableEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmLESectionTableEntry::clearMemoryPool() {
    SgAsmLESectionTableEntry* tempPointer{nullptr};
    if (SgAsmLESectionTableEntry::pools.empty() == false) {
        auto block{SgAsmLESectionTableEntry::pools.begin()};
        SgAsmLESectionTableEntry::next_node = reinterpret_cast<SgAsmLESectionTableEntry*>(*block);
        for (; block != SgAsmLESectionTableEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmLESectionTableEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmLESectionTableEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmLESectionTableEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmLESectionTableEntry::pool_size-1]);
        }
    }
}

void
SgAsmLESectionTableEntry::deleteMemoryPool() {
    for (auto p: SgAsmLESectionTableEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmLESectionTableEntry::next_node = nullptr;
    SgAsmLESectionTableEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmLESectionTableEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTableEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESectionTableEntry)};
    for (size_t blockIndex = SgAsmLESectionTableEntry::pools.size(); (blockIndex * SgAsmLESectionTableEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTableEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESectionTableEntry) = %" PRIuPTR " SgAsmLESectionTableEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmLESectionTableEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmLESectionTableEntry),SgAsmLESectionTableEntry::pool_size);
        }
#endif

        SgAsmLESectionTableEntry* pointer{static_cast<SgAsmLESectionTableEntry*>(ROSE_MALLOC(SgAsmLESectionTableEntry::pool_size * sizeof(SgAsmLESectionTableEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmLESectionTableEntry::pool_size * sizeof(SgAsmLESectionTableEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmLESectionTableEntry::pool_size * sizeof(SgAsmLESectionTableEntry));
#endif
        SgAsmLESectionTableEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmLESectionTableEntry::pool_size * sizeof(SgAsmLESectionTableEntry), V_SgAsmLESectionTableEntry));

        if (SgAsmLESectionTableEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmLESectionTableEntry* blkptr{reinterpret_cast<SgAsmLESectionTableEntry*>(SgAsmLESectionTableEntry::pools[blockIndex-1])};
                blkptr[SgAsmLESectionTableEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmLESectionTableEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmLESectionTableEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmLESectionTableEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmLESectionTableEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmLESectionTableEntry::pool_size - 1};
    SgAsmLESectionTableEntry* testPointer{reinterpret_cast<SgAsmLESectionTableEntry*>(SgAsmLESectionTableEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmLESectionTableEntry::pool_size * (SgAsmLESectionTableEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmLESectionTableEntryStroageClass
 * from its counterpart of SgAsmLESectionTableEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmLESectionTableEntry::initializeStorageClassArray(SgAsmLESectionTableEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmLESectionTableEntry::pools.begin(); block != SgAsmLESectionTableEntry::pools.end(); ++block) {
        SgAsmLESectionTableEntry* pointer{reinterpret_cast<SgAsmLESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmLESectionTableEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 219715 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNEEntryPoint that takes its corresponding StorageClass as parameter
 */
SgAsmNEEntryPoint::SgAsmNEEntryPoint(const SgAsmNEEntryPointStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 219725 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_flags = storageSource.storageOf_flags;
    p_int3f = storageSource.storageOf_int3f;
    p_sectionIndex = storageSource.storageOf_sectionIndex;
    p_sectionOffset = storageSource.storageOf_sectionOffset;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNEEntryPoint*
SgAsmNEEntryPoint::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNEEntryPoint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEEntryPoint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEEntryPoint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNEEntryPoint)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryPoint)};
        unsigned long positionInPool{localIndex % SgAsmNEEntryPoint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEEntryPoint::pool_size};
        returnPointer = &(((SgAsmNEEntryPoint*)(SgAsmNEEntryPoint::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNEEntryPoint*
SgAsmNEEntryPoint::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNEEntryPoint* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEEntryPoint)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEEntryPoint + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNEEntryPoint)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNEEntryPoint)};
        unsigned long positionInPool{localIndex % SgAsmNEEntryPoint::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNEEntryPoint::pool_size};
        returnPointer = &(((SgAsmNEEntryPoint*)(SgAsmNEEntryPoint::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNEEntryPoint::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNEEntryPoint::pools.begin(); block != SgAsmNEEntryPoint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmNEEntryPoint::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNEEntryPoint*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNEEntryPoint::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNEEntryPoint* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNEEntryPoint::pools.begin(); block != SgAsmNEEntryPoint::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNEEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmNEEntryPoint::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNEEntryPoint::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNEEntryPoint::clearMemoryPool() {
    SgAsmNEEntryPoint* tempPointer{nullptr};
    if (SgAsmNEEntryPoint::pools.empty() == false) {
        auto block{SgAsmNEEntryPoint::pools.begin()};
        SgAsmNEEntryPoint::next_node = reinterpret_cast<SgAsmNEEntryPoint*>(*block);
        for (; block != SgAsmNEEntryPoint::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNEEntryPoint*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNEEntryPoint::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNEEntryPoint::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNEEntryPoint::pool_size-1]);
        }
    }
}

void
SgAsmNEEntryPoint::deleteMemoryPool() {
    for (auto p: SgAsmNEEntryPoint::pools) {
        ROSE_FREE(p);
    }
    SgAsmNEEntryPoint::next_node = nullptr;
    SgAsmNEEntryPoint::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNEEntryPoint::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryPoint) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEEntryPoint)};
    for (size_t blockIndex = SgAsmNEEntryPoint::pools.size(); (blockIndex * SgAsmNEEntryPoint::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryPoint) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEEntryPoint) = %" PRIuPTR " SgAsmNEEntryPoint::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNEEntryPoint),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNEEntryPoint),SgAsmNEEntryPoint::pool_size);
        }
#endif

        SgAsmNEEntryPoint* pointer{static_cast<SgAsmNEEntryPoint*>(ROSE_MALLOC(SgAsmNEEntryPoint::pool_size * sizeof(SgAsmNEEntryPoint)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNEEntryPoint::pool_size * sizeof(SgAsmNEEntryPoint));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNEEntryPoint::pool_size * sizeof(SgAsmNEEntryPoint));
#endif
        SgAsmNEEntryPoint::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNEEntryPoint::pool_size * sizeof(SgAsmNEEntryPoint), V_SgAsmNEEntryPoint));

        if (SgAsmNEEntryPoint::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNEEntryPoint* blkptr{reinterpret_cast<SgAsmNEEntryPoint*>(SgAsmNEEntryPoint::pools[blockIndex-1])};
                blkptr[SgAsmNEEntryPoint::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNEEntryPoint::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNEEntryPoint::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNEEntryPoint::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNEEntryPoint::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNEEntryPoint::pool_size - 1};
    SgAsmNEEntryPoint* testPointer{reinterpret_cast<SgAsmNEEntryPoint*>(SgAsmNEEntryPoint::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNEEntryPoint::pool_size * (SgAsmNEEntryPoint::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNEEntryPointStroageClass
 * from its counterpart of SgAsmNEEntryPoint. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNEEntryPoint::initializeStorageClassArray(SgAsmNEEntryPointStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNEEntryPoint::pools.begin(); block != SgAsmNEEntryPoint::pools.end(); ++block) {
        SgAsmNEEntryPoint* pointer{reinterpret_cast<SgAsmNEEntryPoint*>(*block)};
        for (unsigned i = 0; i < SgAsmNEEntryPoint::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 219946 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNERelocEntry that takes its corresponding StorageClass as parameter
 */
SgAsmNERelocEntry::SgAsmNERelocEntry(const SgAsmNERelocEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 219956 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_src_type = storageSource.storageOf_src_type;
    p_modifier = storageSource.storageOf_modifier;
    p_tgt_type = storageSource.storageOf_tgt_type;
    p_flags = storageSource.storageOf_flags;
    p_src_offset = storageSource.storageOf_src_offset;
    p_iref = storageSource.storageOf_iref;
    p_iord = storageSource.storageOf_iord;
    p_iname = storageSource.storageOf_iname;
    p_osfixup = storageSource.storageOf_osfixup;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNERelocEntry*
SgAsmNERelocEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNERelocEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNERelocEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNERelocEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNERelocEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocEntry)};
        unsigned long positionInPool{localIndex % SgAsmNERelocEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNERelocEntry::pool_size};
        returnPointer = &(((SgAsmNERelocEntry*)(SgAsmNERelocEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNERelocEntry*
SgAsmNERelocEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNERelocEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNERelocEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNERelocEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNERelocEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNERelocEntry)};
        unsigned long positionInPool{localIndex % SgAsmNERelocEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNERelocEntry::pool_size};
        returnPointer = &(((SgAsmNERelocEntry*)(SgAsmNERelocEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNERelocEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNERelocEntry::pools.begin(); block != SgAsmNERelocEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNERelocEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmNERelocEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNERelocEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNERelocEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNERelocEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNERelocEntry::pools.begin(); block != SgAsmNERelocEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNERelocEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmNERelocEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNERelocEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNERelocEntry::clearMemoryPool() {
    SgAsmNERelocEntry* tempPointer{nullptr};
    if (SgAsmNERelocEntry::pools.empty() == false) {
        auto block{SgAsmNERelocEntry::pools.begin()};
        SgAsmNERelocEntry::next_node = reinterpret_cast<SgAsmNERelocEntry*>(*block);
        for (; block != SgAsmNERelocEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNERelocEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNERelocEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNERelocEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNERelocEntry::pool_size-1]);
        }
    }
}

void
SgAsmNERelocEntry::deleteMemoryPool() {
    for (auto p: SgAsmNERelocEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmNERelocEntry::next_node = nullptr;
    SgAsmNERelocEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNERelocEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNERelocEntry)};
    for (size_t blockIndex = SgAsmNERelocEntry::pools.size(); (blockIndex * SgAsmNERelocEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNERelocEntry) = %" PRIuPTR " SgAsmNERelocEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNERelocEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNERelocEntry),SgAsmNERelocEntry::pool_size);
        }
#endif

        SgAsmNERelocEntry* pointer{static_cast<SgAsmNERelocEntry*>(ROSE_MALLOC(SgAsmNERelocEntry::pool_size * sizeof(SgAsmNERelocEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNERelocEntry::pool_size * sizeof(SgAsmNERelocEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNERelocEntry::pool_size * sizeof(SgAsmNERelocEntry));
#endif
        SgAsmNERelocEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNERelocEntry::pool_size * sizeof(SgAsmNERelocEntry), V_SgAsmNERelocEntry));

        if (SgAsmNERelocEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNERelocEntry* blkptr{reinterpret_cast<SgAsmNERelocEntry*>(SgAsmNERelocEntry::pools[blockIndex-1])};
                blkptr[SgAsmNERelocEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNERelocEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNERelocEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNERelocEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNERelocEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNERelocEntry::pool_size - 1};
    SgAsmNERelocEntry* testPointer{reinterpret_cast<SgAsmNERelocEntry*>(SgAsmNERelocEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNERelocEntry::pool_size * (SgAsmNERelocEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNERelocEntryStroageClass
 * from its counterpart of SgAsmNERelocEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNERelocEntry::initializeStorageClassArray(SgAsmNERelocEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNERelocEntry::pools.begin(); block != SgAsmNERelocEntry::pools.end(); ++block) {
        SgAsmNERelocEntry* pointer{reinterpret_cast<SgAsmNERelocEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmNERelocEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 220182 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNESectionTableEntry that takes its corresponding StorageClass as parameter
 */
SgAsmNESectionTableEntry::SgAsmNESectionTableEntry(const SgAsmNESectionTableEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 220192 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_flags = storageSource.storageOf_flags;
    p_sector = storageSource.storageOf_sector;
    p_physicalSize = storageSource.storageOf_physicalSize;
    p_virtualSize = storageSource.storageOf_virtualSize;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNESectionTableEntry*
SgAsmNESectionTableEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNESectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNESectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmNESectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNESectionTableEntry::pool_size};
        returnPointer = &(((SgAsmNESectionTableEntry*)(SgAsmNESectionTableEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNESectionTableEntry*
SgAsmNESectionTableEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNESectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNESectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNESectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmNESectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNESectionTableEntry::pool_size};
        returnPointer = &(((SgAsmNESectionTableEntry*)(SgAsmNESectionTableEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNESectionTableEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNESectionTableEntry::pools.begin(); block != SgAsmNESectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmNESectionTableEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNESectionTableEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNESectionTableEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNESectionTableEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNESectionTableEntry::pools.begin(); block != SgAsmNESectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmNESectionTableEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNESectionTableEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNESectionTableEntry::clearMemoryPool() {
    SgAsmNESectionTableEntry* tempPointer{nullptr};
    if (SgAsmNESectionTableEntry::pools.empty() == false) {
        auto block{SgAsmNESectionTableEntry::pools.begin()};
        SgAsmNESectionTableEntry::next_node = reinterpret_cast<SgAsmNESectionTableEntry*>(*block);
        for (; block != SgAsmNESectionTableEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNESectionTableEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNESectionTableEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNESectionTableEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNESectionTableEntry::pool_size-1]);
        }
    }
}

void
SgAsmNESectionTableEntry::deleteMemoryPool() {
    for (auto p: SgAsmNESectionTableEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmNESectionTableEntry::next_node = nullptr;
    SgAsmNESectionTableEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNESectionTableEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTableEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESectionTableEntry)};
    for (size_t blockIndex = SgAsmNESectionTableEntry::pools.size(); (blockIndex * SgAsmNESectionTableEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTableEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESectionTableEntry) = %" PRIuPTR " SgAsmNESectionTableEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNESectionTableEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNESectionTableEntry),SgAsmNESectionTableEntry::pool_size);
        }
#endif

        SgAsmNESectionTableEntry* pointer{static_cast<SgAsmNESectionTableEntry*>(ROSE_MALLOC(SgAsmNESectionTableEntry::pool_size * sizeof(SgAsmNESectionTableEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNESectionTableEntry::pool_size * sizeof(SgAsmNESectionTableEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNESectionTableEntry::pool_size * sizeof(SgAsmNESectionTableEntry));
#endif
        SgAsmNESectionTableEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNESectionTableEntry::pool_size * sizeof(SgAsmNESectionTableEntry), V_SgAsmNESectionTableEntry));

        if (SgAsmNESectionTableEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNESectionTableEntry* blkptr{reinterpret_cast<SgAsmNESectionTableEntry*>(SgAsmNESectionTableEntry::pools[blockIndex-1])};
                blkptr[SgAsmNESectionTableEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNESectionTableEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNESectionTableEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNESectionTableEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNESectionTableEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNESectionTableEntry::pool_size - 1};
    SgAsmNESectionTableEntry* testPointer{reinterpret_cast<SgAsmNESectionTableEntry*>(SgAsmNESectionTableEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNESectionTableEntry::pool_size * (SgAsmNESectionTableEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNESectionTableEntryStroageClass
 * from its counterpart of SgAsmNESectionTableEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNESectionTableEntry::initializeStorageClassArray(SgAsmNESectionTableEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNESectionTableEntry::pools.begin(); block != SgAsmNESectionTableEntry::pools.end(); ++block) {
        SgAsmNESectionTableEntry* pointer{reinterpret_cast<SgAsmNESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmNESectionTableEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 220413 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEExportDirectory that takes its corresponding StorageClass as parameter
 */
SgAsmPEExportDirectory::SgAsmPEExportDirectory(const SgAsmPEExportDirectoryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 220423 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_res1 = storageSource.storageOf_res1;
    p_timestamp = storageSource.storageOf_timestamp;
    p_vmajor = storageSource.storageOf_vmajor;
    p_vminor = storageSource.storageOf_vminor;
    p_name_rva = storageSource.storageOf_name_rva;
    p_ord_base = storageSource.storageOf_ord_base;
    p_expaddr_n = storageSource.storageOf_expaddr_n;
    p_nameptr_n = storageSource.storageOf_nameptr_n;
    p_expaddr_rva = storageSource.storageOf_expaddr_rva;
    p_nameptr_rva = storageSource.storageOf_nameptr_rva;
    p_ordinals_rva = storageSource.storageOf_ordinals_rva;
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEExportDirectory*
SgAsmPEExportDirectory::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEExportDirectory* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportDirectory)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportDirectory + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportDirectory)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportDirectory)};
        unsigned long positionInPool{localIndex % SgAsmPEExportDirectory::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportDirectory::pool_size};
        returnPointer = &(((SgAsmPEExportDirectory*)(SgAsmPEExportDirectory::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEExportDirectory*
SgAsmPEExportDirectory::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEExportDirectory* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportDirectory)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportDirectory + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportDirectory)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEExportDirectory)};
        unsigned long positionInPool{localIndex % SgAsmPEExportDirectory::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportDirectory::pool_size};
        returnPointer = &(((SgAsmPEExportDirectory*)(SgAsmPEExportDirectory::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEExportDirectory::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEExportDirectory::pools.begin(); block != SgAsmPEExportDirectory::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportDirectory*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportDirectory::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEExportDirectory*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEExportDirectory::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEExportDirectory* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEExportDirectory::pools.begin(); block != SgAsmPEExportDirectory::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportDirectory*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportDirectory::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEExportDirectory::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEExportDirectory::clearMemoryPool() {
    SgAsmPEExportDirectory* tempPointer{nullptr};
    if (SgAsmPEExportDirectory::pools.empty() == false) {
        auto block{SgAsmPEExportDirectory::pools.begin()};
        SgAsmPEExportDirectory::next_node = reinterpret_cast<SgAsmPEExportDirectory*>(*block);
        for (; block != SgAsmPEExportDirectory::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEExportDirectory*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEExportDirectory::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEExportDirectory::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEExportDirectory::pool_size-1]);
        }
    }
}

void
SgAsmPEExportDirectory::deleteMemoryPool() {
    for (auto p: SgAsmPEExportDirectory::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEExportDirectory::next_node = nullptr;
    SgAsmPEExportDirectory::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEExportDirectory::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportDirectory) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportDirectory)};
    for (size_t blockIndex = SgAsmPEExportDirectory::pools.size(); (blockIndex * SgAsmPEExportDirectory::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportDirectory) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportDirectory) = %" PRIuPTR " SgAsmPEExportDirectory::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportDirectory),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportDirectory),SgAsmPEExportDirectory::pool_size);
        }
#endif

        SgAsmPEExportDirectory* pointer{static_cast<SgAsmPEExportDirectory*>(ROSE_MALLOC(SgAsmPEExportDirectory::pool_size * sizeof(SgAsmPEExportDirectory)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEExportDirectory::pool_size * sizeof(SgAsmPEExportDirectory));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEExportDirectory::pool_size * sizeof(SgAsmPEExportDirectory));
#endif
        SgAsmPEExportDirectory::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEExportDirectory::pool_size * sizeof(SgAsmPEExportDirectory), V_SgAsmPEExportDirectory));

        if (SgAsmPEExportDirectory::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEExportDirectory* blkptr{reinterpret_cast<SgAsmPEExportDirectory*>(SgAsmPEExportDirectory::pools[blockIndex-1])};
                blkptr[SgAsmPEExportDirectory::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEExportDirectory::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEExportDirectory::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEExportDirectory::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEExportDirectory::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEExportDirectory::pool_size - 1};
    SgAsmPEExportDirectory* testPointer{reinterpret_cast<SgAsmPEExportDirectory*>(SgAsmPEExportDirectory::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEExportDirectory::pool_size * (SgAsmPEExportDirectory::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEExportDirectoryStroageClass
 * from its counterpart of SgAsmPEExportDirectory. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEExportDirectory::initializeStorageClassArray(SgAsmPEExportDirectoryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEExportDirectory::pools.begin(); block != SgAsmPEExportDirectory::pools.end(); ++block) {
        SgAsmPEExportDirectory* pointer{reinterpret_cast<SgAsmPEExportDirectory*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportDirectory::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 220652 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEExportEntry that takes its corresponding StorageClass as parameter
 */
SgAsmPEExportEntry::SgAsmPEExportEntry(const SgAsmPEExportEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 220662 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));
    p_ordinal = storageSource.storageOf_ordinal;
    p_exportRva = storageSource.storageOf_exportRva;
     p_forwarder = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_forwarder));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEExportEntry*
SgAsmPEExportEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEExportEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntry)};
        unsigned long positionInPool{localIndex % SgAsmPEExportEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportEntry::pool_size};
        returnPointer = &(((SgAsmPEExportEntry*)(SgAsmPEExportEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEExportEntry*
SgAsmPEExportEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEExportEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEExportEntry)};
        unsigned long positionInPool{localIndex % SgAsmPEExportEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportEntry::pool_size};
        returnPointer = &(((SgAsmPEExportEntry*)(SgAsmPEExportEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEExportEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEExportEntry::pools.begin(); block != SgAsmPEExportEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEExportEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEExportEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEExportEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEExportEntry::pools.begin(); block != SgAsmPEExportEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEExportEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEExportEntry::clearMemoryPool() {
    SgAsmPEExportEntry* tempPointer{nullptr};
    if (SgAsmPEExportEntry::pools.empty() == false) {
        auto block{SgAsmPEExportEntry::pools.begin()};
        SgAsmPEExportEntry::next_node = reinterpret_cast<SgAsmPEExportEntry*>(*block);
        for (; block != SgAsmPEExportEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEExportEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEExportEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEExportEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEExportEntry::pool_size-1]);
        }
    }
}

void
SgAsmPEExportEntry::deleteMemoryPool() {
    for (auto p: SgAsmPEExportEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEExportEntry::next_node = nullptr;
    SgAsmPEExportEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEExportEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportEntry)};
    for (size_t blockIndex = SgAsmPEExportEntry::pools.size(); (blockIndex * SgAsmPEExportEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportEntry) = %" PRIuPTR " SgAsmPEExportEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportEntry),SgAsmPEExportEntry::pool_size);
        }
#endif

        SgAsmPEExportEntry* pointer{static_cast<SgAsmPEExportEntry*>(ROSE_MALLOC(SgAsmPEExportEntry::pool_size * sizeof(SgAsmPEExportEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEExportEntry::pool_size * sizeof(SgAsmPEExportEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEExportEntry::pool_size * sizeof(SgAsmPEExportEntry));
#endif
        SgAsmPEExportEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEExportEntry::pool_size * sizeof(SgAsmPEExportEntry), V_SgAsmPEExportEntry));

        if (SgAsmPEExportEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEExportEntry* blkptr{reinterpret_cast<SgAsmPEExportEntry*>(SgAsmPEExportEntry::pools[blockIndex-1])};
                blkptr[SgAsmPEExportEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEExportEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEExportEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEExportEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEExportEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEExportEntry::pool_size - 1};
    SgAsmPEExportEntry* testPointer{reinterpret_cast<SgAsmPEExportEntry*>(SgAsmPEExportEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEExportEntry::pool_size * (SgAsmPEExportEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEExportEntryStroageClass
 * from its counterpart of SgAsmPEExportEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEExportEntry::initializeStorageClassArray(SgAsmPEExportEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEExportEntry::pools.begin(); block != SgAsmPEExportEntry::pools.end(); ++block) {
        SgAsmPEExportEntry* pointer{reinterpret_cast<SgAsmPEExportEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 220883 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEExportEntryList that takes its corresponding StorageClass as parameter
 */
SgAsmPEExportEntryList::SgAsmPEExportEntryList(const SgAsmPEExportEntryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 220893 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_exports = storageSource.storageOf_exports.rebuildDataStoredInEasyStorageClass();
    SgAsmPEExportEntryPtrList::iterator i_exports = p_exports.begin();
    for ( ; i_exports != p_exports.end(); ++i_exports) {
        (*i_exports) = (SgAsmPEExportEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_exports)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEExportEntryList*
SgAsmPEExportEntryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEExportEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEExportEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntryList)};
        unsigned long positionInPool{localIndex % SgAsmPEExportEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportEntryList::pool_size};
        returnPointer = &(((SgAsmPEExportEntryList*)(SgAsmPEExportEntryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEExportEntryList*
SgAsmPEExportEntryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEExportEntryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportEntryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportEntryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEExportEntryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEExportEntryList)};
        unsigned long positionInPool{localIndex % SgAsmPEExportEntryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEExportEntryList::pool_size};
        returnPointer = &(((SgAsmPEExportEntryList*)(SgAsmPEExportEntryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEExportEntryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEExportEntryList::pools.begin(); block != SgAsmPEExportEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportEntryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEExportEntryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEExportEntryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEExportEntryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEExportEntryList::pools.begin(); block != SgAsmPEExportEntryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEExportEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportEntryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEExportEntryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEExportEntryList::clearMemoryPool() {
    SgAsmPEExportEntryList* tempPointer{nullptr};
    if (SgAsmPEExportEntryList::pools.empty() == false) {
        auto block{SgAsmPEExportEntryList::pools.begin()};
        SgAsmPEExportEntryList::next_node = reinterpret_cast<SgAsmPEExportEntryList*>(*block);
        for (; block != SgAsmPEExportEntryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEExportEntryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEExportEntryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEExportEntryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEExportEntryList::pool_size-1]);
        }
    }
}

void
SgAsmPEExportEntryList::deleteMemoryPool() {
    for (auto p: SgAsmPEExportEntryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEExportEntryList::next_node = nullptr;
    SgAsmPEExportEntryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEExportEntryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportEntryList)};
    for (size_t blockIndex = SgAsmPEExportEntryList::pools.size(); (blockIndex * SgAsmPEExportEntryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportEntryList) = %" PRIuPTR " SgAsmPEExportEntryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEExportEntryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEExportEntryList),SgAsmPEExportEntryList::pool_size);
        }
#endif

        SgAsmPEExportEntryList* pointer{static_cast<SgAsmPEExportEntryList*>(ROSE_MALLOC(SgAsmPEExportEntryList::pool_size * sizeof(SgAsmPEExportEntryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEExportEntryList::pool_size * sizeof(SgAsmPEExportEntryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEExportEntryList::pool_size * sizeof(SgAsmPEExportEntryList));
#endif
        SgAsmPEExportEntryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEExportEntryList::pool_size * sizeof(SgAsmPEExportEntryList), V_SgAsmPEExportEntryList));

        if (SgAsmPEExportEntryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEExportEntryList* blkptr{reinterpret_cast<SgAsmPEExportEntryList*>(SgAsmPEExportEntryList::pools[blockIndex-1])};
                blkptr[SgAsmPEExportEntryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEExportEntryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEExportEntryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEExportEntryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEExportEntryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEExportEntryList::pool_size - 1};
    SgAsmPEExportEntryList* testPointer{reinterpret_cast<SgAsmPEExportEntryList*>(SgAsmPEExportEntryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEExportEntryList::pool_size * (SgAsmPEExportEntryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEExportEntryListStroageClass
 * from its counterpart of SgAsmPEExportEntryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEExportEntryList::initializeStorageClassArray(SgAsmPEExportEntryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEExportEntryList::pools.begin(); block != SgAsmPEExportEntryList::pools.end(); ++block) {
        SgAsmPEExportEntryList* pointer{reinterpret_cast<SgAsmPEExportEntryList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEExportEntryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 221115 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEImportDirectory that takes its corresponding StorageClass as parameter
 */
SgAsmPEImportDirectory::SgAsmPEImportDirectory(const SgAsmPEImportDirectoryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 221125 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_dllName = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_dllName));
    p_dllNameRva = storageSource.storageOf_dllNameRva;
    p_dll_name_nalloc = storageSource.storageOf_dll_name_nalloc;
    p_time = storageSource.storageOf_time;
    p_forwarder_chain = storageSource.storageOf_forwarder_chain;
    p_ilt_rva = storageSource.storageOf_ilt_rva;
    p_ilt_nalloc = storageSource.storageOf_ilt_nalloc;
    p_iat_rva = storageSource.storageOf_iat_rva;
    p_iat_nalloc = storageSource.storageOf_iat_nalloc;
     p_imports = (SgAsmPEImportItemList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_imports));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEImportDirectory*
SgAsmPEImportDirectory::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEImportDirectory* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportDirectory)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportDirectory + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportDirectory)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectory)};
        unsigned long positionInPool{localIndex % SgAsmPEImportDirectory::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportDirectory::pool_size};
        returnPointer = &(((SgAsmPEImportDirectory*)(SgAsmPEImportDirectory::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEImportDirectory*
SgAsmPEImportDirectory::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEImportDirectory* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportDirectory)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportDirectory + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportDirectory)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEImportDirectory)};
        unsigned long positionInPool{localIndex % SgAsmPEImportDirectory::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportDirectory::pool_size};
        returnPointer = &(((SgAsmPEImportDirectory*)(SgAsmPEImportDirectory::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEImportDirectory::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEImportDirectory::pools.begin(); block != SgAsmPEImportDirectory::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportDirectory*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportDirectory::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEImportDirectory*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEImportDirectory::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEImportDirectory* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEImportDirectory::pools.begin(); block != SgAsmPEImportDirectory::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportDirectory*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportDirectory::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEImportDirectory::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEImportDirectory::clearMemoryPool() {
    SgAsmPEImportDirectory* tempPointer{nullptr};
    if (SgAsmPEImportDirectory::pools.empty() == false) {
        auto block{SgAsmPEImportDirectory::pools.begin()};
        SgAsmPEImportDirectory::next_node = reinterpret_cast<SgAsmPEImportDirectory*>(*block);
        for (; block != SgAsmPEImportDirectory::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEImportDirectory*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEImportDirectory::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEImportDirectory::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEImportDirectory::pool_size-1]);
        }
    }
}

void
SgAsmPEImportDirectory::deleteMemoryPool() {
    for (auto p: SgAsmPEImportDirectory::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEImportDirectory::next_node = nullptr;
    SgAsmPEImportDirectory::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEImportDirectory::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectory) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportDirectory)};
    for (size_t blockIndex = SgAsmPEImportDirectory::pools.size(); (blockIndex * SgAsmPEImportDirectory::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectory) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportDirectory) = %" PRIuPTR " SgAsmPEImportDirectory::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectory),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportDirectory),SgAsmPEImportDirectory::pool_size);
        }
#endif

        SgAsmPEImportDirectory* pointer{static_cast<SgAsmPEImportDirectory*>(ROSE_MALLOC(SgAsmPEImportDirectory::pool_size * sizeof(SgAsmPEImportDirectory)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEImportDirectory::pool_size * sizeof(SgAsmPEImportDirectory));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEImportDirectory::pool_size * sizeof(SgAsmPEImportDirectory));
#endif
        SgAsmPEImportDirectory::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEImportDirectory::pool_size * sizeof(SgAsmPEImportDirectory), V_SgAsmPEImportDirectory));

        if (SgAsmPEImportDirectory::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEImportDirectory* blkptr{reinterpret_cast<SgAsmPEImportDirectory*>(SgAsmPEImportDirectory::pools[blockIndex-1])};
                blkptr[SgAsmPEImportDirectory::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEImportDirectory::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEImportDirectory::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEImportDirectory::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEImportDirectory::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEImportDirectory::pool_size - 1};
    SgAsmPEImportDirectory* testPointer{reinterpret_cast<SgAsmPEImportDirectory*>(SgAsmPEImportDirectory::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEImportDirectory::pool_size * (SgAsmPEImportDirectory::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEImportDirectoryStroageClass
 * from its counterpart of SgAsmPEImportDirectory. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEImportDirectory::initializeStorageClassArray(SgAsmPEImportDirectoryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEImportDirectory::pools.begin(); block != SgAsmPEImportDirectory::pools.end(); ++block) {
        SgAsmPEImportDirectory* pointer{reinterpret_cast<SgAsmPEImportDirectory*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportDirectory::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 221352 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEImportDirectoryList that takes its corresponding StorageClass as parameter
 */
SgAsmPEImportDirectoryList::SgAsmPEImportDirectoryList(const SgAsmPEImportDirectoryListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 221362 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_vector = storageSource.storageOf_vector.rebuildDataStoredInEasyStorageClass();
    SgAsmPEImportDirectoryPtrList::iterator i_vector = p_vector.begin();
    for ( ; i_vector != p_vector.end(); ++i_vector) {
        (*i_vector) = (SgAsmPEImportDirectoryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_vector)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEImportDirectoryList*
SgAsmPEImportDirectoryList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEImportDirectoryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportDirectoryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportDirectoryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportDirectoryList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectoryList)};
        unsigned long positionInPool{localIndex % SgAsmPEImportDirectoryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportDirectoryList::pool_size};
        returnPointer = &(((SgAsmPEImportDirectoryList*)(SgAsmPEImportDirectoryList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEImportDirectoryList*
SgAsmPEImportDirectoryList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEImportDirectoryList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportDirectoryList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportDirectoryList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportDirectoryList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEImportDirectoryList)};
        unsigned long positionInPool{localIndex % SgAsmPEImportDirectoryList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportDirectoryList::pool_size};
        returnPointer = &(((SgAsmPEImportDirectoryList*)(SgAsmPEImportDirectoryList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEImportDirectoryList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEImportDirectoryList::pools.begin(); block != SgAsmPEImportDirectoryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportDirectoryList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportDirectoryList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEImportDirectoryList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEImportDirectoryList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEImportDirectoryList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEImportDirectoryList::pools.begin(); block != SgAsmPEImportDirectoryList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportDirectoryList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportDirectoryList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEImportDirectoryList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEImportDirectoryList::clearMemoryPool() {
    SgAsmPEImportDirectoryList* tempPointer{nullptr};
    if (SgAsmPEImportDirectoryList::pools.empty() == false) {
        auto block{SgAsmPEImportDirectoryList::pools.begin()};
        SgAsmPEImportDirectoryList::next_node = reinterpret_cast<SgAsmPEImportDirectoryList*>(*block);
        for (; block != SgAsmPEImportDirectoryList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEImportDirectoryList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEImportDirectoryList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEImportDirectoryList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEImportDirectoryList::pool_size-1]);
        }
    }
}

void
SgAsmPEImportDirectoryList::deleteMemoryPool() {
    for (auto p: SgAsmPEImportDirectoryList::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEImportDirectoryList::next_node = nullptr;
    SgAsmPEImportDirectoryList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEImportDirectoryList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectoryList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportDirectoryList)};
    for (size_t blockIndex = SgAsmPEImportDirectoryList::pools.size(); (blockIndex * SgAsmPEImportDirectoryList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectoryList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportDirectoryList) = %" PRIuPTR " SgAsmPEImportDirectoryList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportDirectoryList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportDirectoryList),SgAsmPEImportDirectoryList::pool_size);
        }
#endif

        SgAsmPEImportDirectoryList* pointer{static_cast<SgAsmPEImportDirectoryList*>(ROSE_MALLOC(SgAsmPEImportDirectoryList::pool_size * sizeof(SgAsmPEImportDirectoryList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEImportDirectoryList::pool_size * sizeof(SgAsmPEImportDirectoryList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEImportDirectoryList::pool_size * sizeof(SgAsmPEImportDirectoryList));
#endif
        SgAsmPEImportDirectoryList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEImportDirectoryList::pool_size * sizeof(SgAsmPEImportDirectoryList), V_SgAsmPEImportDirectoryList));

        if (SgAsmPEImportDirectoryList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEImportDirectoryList* blkptr{reinterpret_cast<SgAsmPEImportDirectoryList*>(SgAsmPEImportDirectoryList::pools[blockIndex-1])};
                blkptr[SgAsmPEImportDirectoryList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEImportDirectoryList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEImportDirectoryList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEImportDirectoryList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEImportDirectoryList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEImportDirectoryList::pool_size - 1};
    SgAsmPEImportDirectoryList* testPointer{reinterpret_cast<SgAsmPEImportDirectoryList*>(SgAsmPEImportDirectoryList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEImportDirectoryList::pool_size * (SgAsmPEImportDirectoryList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEImportDirectoryListStroageClass
 * from its counterpart of SgAsmPEImportDirectoryList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEImportDirectoryList::initializeStorageClassArray(SgAsmPEImportDirectoryListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEImportDirectoryList::pools.begin(); block != SgAsmPEImportDirectoryList::pools.end(); ++block) {
        SgAsmPEImportDirectoryList* pointer{reinterpret_cast<SgAsmPEImportDirectoryList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportDirectoryList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 221584 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEImportItem that takes its corresponding StorageClass as parameter
 */
SgAsmPEImportItem::SgAsmPEImportItem(const SgAsmPEImportItemStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 221594 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_by_ordinal = storageSource.storageOf_by_ordinal;
    p_ordinal = storageSource.storageOf_ordinal;
    p_hint = storageSource.storageOf_hint;
     p_name = (SgAsmGenericString*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_name));
    p_hintname_rva = storageSource.storageOf_hintname_rva;
    p_hintname_nalloc = storageSource.storageOf_hintname_nalloc;
    p_bound_rva = storageSource.storageOf_bound_rva;
    p_iat_written = storageSource.storageOf_iat_written;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEImportItem*
SgAsmPEImportItem::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEImportItem* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportItem)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportItem + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportItem)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItem)};
        unsigned long positionInPool{localIndex % SgAsmPEImportItem::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportItem::pool_size};
        returnPointer = &(((SgAsmPEImportItem*)(SgAsmPEImportItem::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEImportItem*
SgAsmPEImportItem::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEImportItem* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportItem)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportItem + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportItem)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEImportItem)};
        unsigned long positionInPool{localIndex % SgAsmPEImportItem::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportItem::pool_size};
        returnPointer = &(((SgAsmPEImportItem*)(SgAsmPEImportItem::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEImportItem::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEImportItem::pools.begin(); block != SgAsmPEImportItem::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportItem*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportItem::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEImportItem*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEImportItem::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEImportItem* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEImportItem::pools.begin(); block != SgAsmPEImportItem::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportItem*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportItem::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEImportItem::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEImportItem::clearMemoryPool() {
    SgAsmPEImportItem* tempPointer{nullptr};
    if (SgAsmPEImportItem::pools.empty() == false) {
        auto block{SgAsmPEImportItem::pools.begin()};
        SgAsmPEImportItem::next_node = reinterpret_cast<SgAsmPEImportItem*>(*block);
        for (; block != SgAsmPEImportItem::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEImportItem*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEImportItem::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEImportItem::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEImportItem::pool_size-1]);
        }
    }
}

void
SgAsmPEImportItem::deleteMemoryPool() {
    for (auto p: SgAsmPEImportItem::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEImportItem::next_node = nullptr;
    SgAsmPEImportItem::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEImportItem::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItem) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportItem)};
    for (size_t blockIndex = SgAsmPEImportItem::pools.size(); (blockIndex * SgAsmPEImportItem::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItem) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportItem) = %" PRIuPTR " SgAsmPEImportItem::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItem),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportItem),SgAsmPEImportItem::pool_size);
        }
#endif

        SgAsmPEImportItem* pointer{static_cast<SgAsmPEImportItem*>(ROSE_MALLOC(SgAsmPEImportItem::pool_size * sizeof(SgAsmPEImportItem)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEImportItem::pool_size * sizeof(SgAsmPEImportItem));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEImportItem::pool_size * sizeof(SgAsmPEImportItem));
#endif
        SgAsmPEImportItem::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEImportItem::pool_size * sizeof(SgAsmPEImportItem), V_SgAsmPEImportItem));

        if (SgAsmPEImportItem::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEImportItem* blkptr{reinterpret_cast<SgAsmPEImportItem*>(SgAsmPEImportItem::pools[blockIndex-1])};
                blkptr[SgAsmPEImportItem::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEImportItem::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEImportItem::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEImportItem::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEImportItem::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEImportItem::pool_size - 1};
    SgAsmPEImportItem* testPointer{reinterpret_cast<SgAsmPEImportItem*>(SgAsmPEImportItem::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEImportItem::pool_size * (SgAsmPEImportItem::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEImportItemStroageClass
 * from its counterpart of SgAsmPEImportItem. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEImportItem::initializeStorageClassArray(SgAsmPEImportItemStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEImportItem::pools.begin(); block != SgAsmPEImportItem::pools.end(); ++block) {
        SgAsmPEImportItem* pointer{reinterpret_cast<SgAsmPEImportItem*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportItem::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 221819 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPEImportItemList that takes its corresponding StorageClass as parameter
 */
SgAsmPEImportItemList::SgAsmPEImportItemList(const SgAsmPEImportItemListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 221829 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_vector = storageSource.storageOf_vector.rebuildDataStoredInEasyStorageClass();
    SgAsmPEImportItemPtrList::iterator i_vector = p_vector.begin();
    for ( ; i_vector != p_vector.end(); ++i_vector) {
        (*i_vector) = (SgAsmPEImportItemPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_vector)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPEImportItemList*
SgAsmPEImportItemList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPEImportItemList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportItemList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportItemList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPEImportItemList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItemList)};
        unsigned long positionInPool{localIndex % SgAsmPEImportItemList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportItemList::pool_size};
        returnPointer = &(((SgAsmPEImportItemList*)(SgAsmPEImportItemList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPEImportItemList*
SgAsmPEImportItemList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPEImportItemList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportItemList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportItemList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPEImportItemList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPEImportItemList)};
        unsigned long positionInPool{localIndex % SgAsmPEImportItemList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPEImportItemList::pool_size};
        returnPointer = &(((SgAsmPEImportItemList*)(SgAsmPEImportItemList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPEImportItemList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPEImportItemList::pools.begin(); block != SgAsmPEImportItemList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportItemList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportItemList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPEImportItemList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPEImportItemList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPEImportItemList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPEImportItemList::pools.begin(); block != SgAsmPEImportItemList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPEImportItemList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportItemList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPEImportItemList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPEImportItemList::clearMemoryPool() {
    SgAsmPEImportItemList* tempPointer{nullptr};
    if (SgAsmPEImportItemList::pools.empty() == false) {
        auto block{SgAsmPEImportItemList::pools.begin()};
        SgAsmPEImportItemList::next_node = reinterpret_cast<SgAsmPEImportItemList*>(*block);
        for (; block != SgAsmPEImportItemList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPEImportItemList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPEImportItemList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPEImportItemList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPEImportItemList::pool_size-1]);
        }
    }
}

void
SgAsmPEImportItemList::deleteMemoryPool() {
    for (auto p: SgAsmPEImportItemList::pools) {
        ROSE_FREE(p);
    }
    SgAsmPEImportItemList::next_node = nullptr;
    SgAsmPEImportItemList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPEImportItemList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItemList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportItemList)};
    for (size_t blockIndex = SgAsmPEImportItemList::pools.size(); (blockIndex * SgAsmPEImportItemList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItemList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportItemList) = %" PRIuPTR " SgAsmPEImportItemList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPEImportItemList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPEImportItemList),SgAsmPEImportItemList::pool_size);
        }
#endif

        SgAsmPEImportItemList* pointer{static_cast<SgAsmPEImportItemList*>(ROSE_MALLOC(SgAsmPEImportItemList::pool_size * sizeof(SgAsmPEImportItemList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPEImportItemList::pool_size * sizeof(SgAsmPEImportItemList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPEImportItemList::pool_size * sizeof(SgAsmPEImportItemList));
#endif
        SgAsmPEImportItemList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPEImportItemList::pool_size * sizeof(SgAsmPEImportItemList), V_SgAsmPEImportItemList));

        if (SgAsmPEImportItemList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPEImportItemList* blkptr{reinterpret_cast<SgAsmPEImportItemList*>(SgAsmPEImportItemList::pools[blockIndex-1])};
                blkptr[SgAsmPEImportItemList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPEImportItemList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPEImportItemList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPEImportItemList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPEImportItemList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPEImportItemList::pool_size - 1};
    SgAsmPEImportItemList* testPointer{reinterpret_cast<SgAsmPEImportItemList*>(SgAsmPEImportItemList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPEImportItemList::pool_size * (SgAsmPEImportItemList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPEImportItemListStroageClass
 * from its counterpart of SgAsmPEImportItemList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPEImportItemList::initializeStorageClassArray(SgAsmPEImportItemListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPEImportItemList::pools.begin(); block != SgAsmPEImportItemList::pools.end(); ++block) {
        SgAsmPEImportItemList* pointer{reinterpret_cast<SgAsmPEImportItemList*>(*block)};
        for (unsigned i = 0; i < SgAsmPEImportItemList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 222051 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPERVASizePair that takes its corresponding StorageClass as parameter
 */
SgAsmPERVASizePair::SgAsmPERVASizePair(const SgAsmPERVASizePairStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 222061 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_e_rva = storageSource.storageOf_e_rva;
    p_e_size = storageSource.storageOf_e_size;
     p_section = (SgAsmGenericSection*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_section));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPERVASizePair*
SgAsmPERVASizePair::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPERVASizePair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPERVASizePair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPERVASizePair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPERVASizePair)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePair)};
        unsigned long positionInPool{localIndex % SgAsmPERVASizePair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPERVASizePair::pool_size};
        returnPointer = &(((SgAsmPERVASizePair*)(SgAsmPERVASizePair::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPERVASizePair*
SgAsmPERVASizePair::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPERVASizePair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPERVASizePair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPERVASizePair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPERVASizePair)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPERVASizePair)};
        unsigned long positionInPool{localIndex % SgAsmPERVASizePair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPERVASizePair::pool_size};
        returnPointer = &(((SgAsmPERVASizePair*)(SgAsmPERVASizePair::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPERVASizePair::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPERVASizePair::pools.begin(); block != SgAsmPERVASizePair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPERVASizePair*>(*block)};
        for (unsigned i = 0; i < SgAsmPERVASizePair::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPERVASizePair*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPERVASizePair::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPERVASizePair* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPERVASizePair::pools.begin(); block != SgAsmPERVASizePair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPERVASizePair*>(*block)};
        for (unsigned i = 0; i < SgAsmPERVASizePair::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPERVASizePair::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPERVASizePair::clearMemoryPool() {
    SgAsmPERVASizePair* tempPointer{nullptr};
    if (SgAsmPERVASizePair::pools.empty() == false) {
        auto block{SgAsmPERVASizePair::pools.begin()};
        SgAsmPERVASizePair::next_node = reinterpret_cast<SgAsmPERVASizePair*>(*block);
        for (; block != SgAsmPERVASizePair::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPERVASizePair*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPERVASizePair::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPERVASizePair::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPERVASizePair::pool_size-1]);
        }
    }
}

void
SgAsmPERVASizePair::deleteMemoryPool() {
    for (auto p: SgAsmPERVASizePair::pools) {
        ROSE_FREE(p);
    }
    SgAsmPERVASizePair::next_node = nullptr;
    SgAsmPERVASizePair::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPERVASizePair::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePair) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPERVASizePair)};
    for (size_t blockIndex = SgAsmPERVASizePair::pools.size(); (blockIndex * SgAsmPERVASizePair::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePair) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPERVASizePair) = %" PRIuPTR " SgAsmPERVASizePair::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePair),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPERVASizePair),SgAsmPERVASizePair::pool_size);
        }
#endif

        SgAsmPERVASizePair* pointer{static_cast<SgAsmPERVASizePair*>(ROSE_MALLOC(SgAsmPERVASizePair::pool_size * sizeof(SgAsmPERVASizePair)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPERVASizePair::pool_size * sizeof(SgAsmPERVASizePair));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPERVASizePair::pool_size * sizeof(SgAsmPERVASizePair));
#endif
        SgAsmPERVASizePair::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPERVASizePair::pool_size * sizeof(SgAsmPERVASizePair), V_SgAsmPERVASizePair));

        if (SgAsmPERVASizePair::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPERVASizePair* blkptr{reinterpret_cast<SgAsmPERVASizePair*>(SgAsmPERVASizePair::pools[blockIndex-1])};
                blkptr[SgAsmPERVASizePair::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPERVASizePair::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPERVASizePair::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPERVASizePair::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPERVASizePair::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPERVASizePair::pool_size - 1};
    SgAsmPERVASizePair* testPointer{reinterpret_cast<SgAsmPERVASizePair*>(SgAsmPERVASizePair::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPERVASizePair::pool_size * (SgAsmPERVASizePair::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPERVASizePairStroageClass
 * from its counterpart of SgAsmPERVASizePair. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPERVASizePair::initializeStorageClassArray(SgAsmPERVASizePairStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPERVASizePair::pools.begin(); block != SgAsmPERVASizePair::pools.end(); ++block) {
        SgAsmPERVASizePair* pointer{reinterpret_cast<SgAsmPERVASizePair*>(*block)};
        for (unsigned i = 0; i < SgAsmPERVASizePair::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 222281 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPERVASizePairList that takes its corresponding StorageClass as parameter
 */
SgAsmPERVASizePairList::SgAsmPERVASizePairList(const SgAsmPERVASizePairListStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 222291 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_pairs = storageSource.storageOf_pairs.rebuildDataStoredInEasyStorageClass();
    SgAsmPERVASizePairPtrList::iterator i_pairs = p_pairs.begin();
    for ( ; i_pairs != p_pairs.end(); ++i_pairs) {
        (*i_pairs) = (SgAsmPERVASizePairPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_pairs)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPERVASizePairList*
SgAsmPERVASizePairList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPERVASizePairList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPERVASizePairList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPERVASizePairList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPERVASizePairList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePairList)};
        unsigned long positionInPool{localIndex % SgAsmPERVASizePairList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPERVASizePairList::pool_size};
        returnPointer = &(((SgAsmPERVASizePairList*)(SgAsmPERVASizePairList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPERVASizePairList*
SgAsmPERVASizePairList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPERVASizePairList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPERVASizePairList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPERVASizePairList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPERVASizePairList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPERVASizePairList)};
        unsigned long positionInPool{localIndex % SgAsmPERVASizePairList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPERVASizePairList::pool_size};
        returnPointer = &(((SgAsmPERVASizePairList*)(SgAsmPERVASizePairList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPERVASizePairList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPERVASizePairList::pools.begin(); block != SgAsmPERVASizePairList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPERVASizePairList*>(*block)};
        for (unsigned i = 0; i < SgAsmPERVASizePairList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPERVASizePairList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPERVASizePairList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPERVASizePairList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPERVASizePairList::pools.begin(); block != SgAsmPERVASizePairList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPERVASizePairList*>(*block)};
        for (unsigned i = 0; i < SgAsmPERVASizePairList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPERVASizePairList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPERVASizePairList::clearMemoryPool() {
    SgAsmPERVASizePairList* tempPointer{nullptr};
    if (SgAsmPERVASizePairList::pools.empty() == false) {
        auto block{SgAsmPERVASizePairList::pools.begin()};
        SgAsmPERVASizePairList::next_node = reinterpret_cast<SgAsmPERVASizePairList*>(*block);
        for (; block != SgAsmPERVASizePairList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPERVASizePairList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPERVASizePairList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPERVASizePairList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPERVASizePairList::pool_size-1]);
        }
    }
}

void
SgAsmPERVASizePairList::deleteMemoryPool() {
    for (auto p: SgAsmPERVASizePairList::pools) {
        ROSE_FREE(p);
    }
    SgAsmPERVASizePairList::next_node = nullptr;
    SgAsmPERVASizePairList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPERVASizePairList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePairList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPERVASizePairList)};
    for (size_t blockIndex = SgAsmPERVASizePairList::pools.size(); (blockIndex * SgAsmPERVASizePairList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePairList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPERVASizePairList) = %" PRIuPTR " SgAsmPERVASizePairList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPERVASizePairList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPERVASizePairList),SgAsmPERVASizePairList::pool_size);
        }
#endif

        SgAsmPERVASizePairList* pointer{static_cast<SgAsmPERVASizePairList*>(ROSE_MALLOC(SgAsmPERVASizePairList::pool_size * sizeof(SgAsmPERVASizePairList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPERVASizePairList::pool_size * sizeof(SgAsmPERVASizePairList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPERVASizePairList::pool_size * sizeof(SgAsmPERVASizePairList));
#endif
        SgAsmPERVASizePairList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPERVASizePairList::pool_size * sizeof(SgAsmPERVASizePairList), V_SgAsmPERVASizePairList));

        if (SgAsmPERVASizePairList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPERVASizePairList* blkptr{reinterpret_cast<SgAsmPERVASizePairList*>(SgAsmPERVASizePairList::pools[blockIndex-1])};
                blkptr[SgAsmPERVASizePairList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPERVASizePairList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPERVASizePairList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPERVASizePairList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPERVASizePairList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPERVASizePairList::pool_size - 1};
    SgAsmPERVASizePairList* testPointer{reinterpret_cast<SgAsmPERVASizePairList*>(SgAsmPERVASizePairList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPERVASizePairList::pool_size * (SgAsmPERVASizePairList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPERVASizePairListStroageClass
 * from its counterpart of SgAsmPERVASizePairList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPERVASizePairList::initializeStorageClassArray(SgAsmPERVASizePairListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPERVASizePairList::pools.begin(); block != SgAsmPERVASizePairList::pools.end(); ++block) {
        SgAsmPERVASizePairList* pointer{reinterpret_cast<SgAsmPERVASizePairList*>(*block)};
        for (unsigned i = 0; i < SgAsmPERVASizePairList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 222513 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPESectionTableEntry that takes its corresponding StorageClass as parameter
 */
SgAsmPESectionTableEntry::SgAsmPESectionTableEntry(const SgAsmPESectionTableEntryStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 222523 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
    p_virtual_size = storageSource.storageOf_virtual_size;
    p_rva = storageSource.storageOf_rva;
    p_physical_size = storageSource.storageOf_physical_size;
    p_physical_offset = storageSource.storageOf_physical_offset;
    p_coff_line_nums = storageSource.storageOf_coff_line_nums;
    p_n_relocs = storageSource.storageOf_n_relocs;
    p_n_coff_line_nums = storageSource.storageOf_n_coff_line_nums;
    p_flags = storageSource.storageOf_flags;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPESectionTableEntry*
SgAsmPESectionTableEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPESectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPESectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmPESectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPESectionTableEntry::pool_size};
        returnPointer = &(((SgAsmPESectionTableEntry*)(SgAsmPESectionTableEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPESectionTableEntry*
SgAsmPESectionTableEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPESectionTableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESectionTableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESectionTableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPESectionTableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPESectionTableEntry)};
        unsigned long positionInPool{localIndex % SgAsmPESectionTableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPESectionTableEntry::pool_size};
        returnPointer = &(((SgAsmPESectionTableEntry*)(SgAsmPESectionTableEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPESectionTableEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPESectionTableEntry::pools.begin(); block != SgAsmPESectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmPESectionTableEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPESectionTableEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPESectionTableEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPESectionTableEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPESectionTableEntry::pools.begin(); block != SgAsmPESectionTableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmPESectionTableEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPESectionTableEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPESectionTableEntry::clearMemoryPool() {
    SgAsmPESectionTableEntry* tempPointer{nullptr};
    if (SgAsmPESectionTableEntry::pools.empty() == false) {
        auto block{SgAsmPESectionTableEntry::pools.begin()};
        SgAsmPESectionTableEntry::next_node = reinterpret_cast<SgAsmPESectionTableEntry*>(*block);
        for (; block != SgAsmPESectionTableEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPESectionTableEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPESectionTableEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPESectionTableEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPESectionTableEntry::pool_size-1]);
        }
    }
}

void
SgAsmPESectionTableEntry::deleteMemoryPool() {
    for (auto p: SgAsmPESectionTableEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmPESectionTableEntry::next_node = nullptr;
    SgAsmPESectionTableEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPESectionTableEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTableEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESectionTableEntry)};
    for (size_t blockIndex = SgAsmPESectionTableEntry::pools.size(); (blockIndex * SgAsmPESectionTableEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTableEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESectionTableEntry) = %" PRIuPTR " SgAsmPESectionTableEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPESectionTableEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPESectionTableEntry),SgAsmPESectionTableEntry::pool_size);
        }
#endif

        SgAsmPESectionTableEntry* pointer{static_cast<SgAsmPESectionTableEntry*>(ROSE_MALLOC(SgAsmPESectionTableEntry::pool_size * sizeof(SgAsmPESectionTableEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPESectionTableEntry::pool_size * sizeof(SgAsmPESectionTableEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPESectionTableEntry::pool_size * sizeof(SgAsmPESectionTableEntry));
#endif
        SgAsmPESectionTableEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPESectionTableEntry::pool_size * sizeof(SgAsmPESectionTableEntry), V_SgAsmPESectionTableEntry));

        if (SgAsmPESectionTableEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPESectionTableEntry* blkptr{reinterpret_cast<SgAsmPESectionTableEntry*>(SgAsmPESectionTableEntry::pools[blockIndex-1])};
                blkptr[SgAsmPESectionTableEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPESectionTableEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPESectionTableEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPESectionTableEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPESectionTableEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPESectionTableEntry::pool_size - 1};
    SgAsmPESectionTableEntry* testPointer{reinterpret_cast<SgAsmPESectionTableEntry*>(SgAsmPESectionTableEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPESectionTableEntry::pool_size * (SgAsmPESectionTableEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPESectionTableEntryStroageClass
 * from its counterpart of SgAsmPESectionTableEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPESectionTableEntry::initializeStorageClassArray(SgAsmPESectionTableEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPESectionTableEntry::pools.begin(); block != SgAsmPESectionTableEntry::pools.end(); ++block) {
        SgAsmPESectionTableEntry* pointer{reinterpret_cast<SgAsmPESectionTableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmPESectionTableEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 222749 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmStringStorage that takes its corresponding StorageClass as parameter
 */
SgAsmStringStorage::SgAsmStringStorage(const SgAsmStringStorageStorageClass& storageSource)  : SgAsmExecutableFileFormat (storageSource) {

/* #line 222759 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_strtab = (SgAsmGenericStrtab*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_strtab));
    p_string = storageSource.storageOf_string.rebuildDataStoredInEasyStorageClass();
    p_offset = storageSource.storageOf_offset;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmStringStorage*
SgAsmStringStorage::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmStringStorage* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStringStorage)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStringStorage + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStringStorage)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStringStorage)};
        unsigned long positionInPool{localIndex % SgAsmStringStorage::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStringStorage::pool_size};
        returnPointer = &(((SgAsmStringStorage*)(SgAsmStringStorage::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmStringStorage*
SgAsmStringStorage::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmStringStorage* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStringStorage)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStringStorage + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStringStorage)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmStringStorage)};
        unsigned long positionInPool{localIndex % SgAsmStringStorage::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStringStorage::pool_size};
        returnPointer = &(((SgAsmStringStorage*)(SgAsmStringStorage::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmStringStorage::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmStringStorage::pools.begin(); block != SgAsmStringStorage::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStringStorage*>(*block)};
        for (unsigned i = 0; i < SgAsmStringStorage::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmStringStorage*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmStringStorage::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmStringStorage* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmStringStorage::pools.begin(); block != SgAsmStringStorage::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStringStorage*>(*block)};
        for (unsigned i = 0; i < SgAsmStringStorage::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmStringStorage::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmStringStorage::clearMemoryPool() {
    SgAsmStringStorage* tempPointer{nullptr};
    if (SgAsmStringStorage::pools.empty() == false) {
        auto block{SgAsmStringStorage::pools.begin()};
        SgAsmStringStorage::next_node = reinterpret_cast<SgAsmStringStorage*>(*block);
        for (; block != SgAsmStringStorage::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmStringStorage*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmStringStorage::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmStringStorage::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmStringStorage::pool_size-1]);
        }
    }
}

void
SgAsmStringStorage::deleteMemoryPool() {
    for (auto p: SgAsmStringStorage::pools) {
        ROSE_FREE(p);
    }
    SgAsmStringStorage::next_node = nullptr;
    SgAsmStringStorage::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmStringStorage::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStringStorage) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStringStorage)};
    for (size_t blockIndex = SgAsmStringStorage::pools.size(); (blockIndex * SgAsmStringStorage::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStringStorage) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStringStorage) = %" PRIuPTR " SgAsmStringStorage::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStringStorage),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStringStorage),SgAsmStringStorage::pool_size);
        }
#endif

        SgAsmStringStorage* pointer{static_cast<SgAsmStringStorage*>(ROSE_MALLOC(SgAsmStringStorage::pool_size * sizeof(SgAsmStringStorage)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmStringStorage::pool_size * sizeof(SgAsmStringStorage));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmStringStorage::pool_size * sizeof(SgAsmStringStorage));
#endif
        SgAsmStringStorage::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmStringStorage::pool_size * sizeof(SgAsmStringStorage), V_SgAsmStringStorage));

        if (SgAsmStringStorage::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmStringStorage* blkptr{reinterpret_cast<SgAsmStringStorage*>(SgAsmStringStorage::pools[blockIndex-1])};
                blkptr[SgAsmStringStorage::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmStringStorage::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmStringStorage::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmStringStorage::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmStringStorage::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmStringStorage::pool_size - 1};
    SgAsmStringStorage* testPointer{reinterpret_cast<SgAsmStringStorage*>(SgAsmStringStorage::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmStringStorage::pool_size * (SgAsmStringStorage::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmStringStorageStroageClass
 * from its counterpart of SgAsmStringStorage. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmStringStorage::initializeStorageClassArray(SgAsmStringStorageStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmStringStorage::pools.begin(); block != SgAsmStringStorage::pools.end(); ++block) {
        SgAsmStringStorage* pointer{reinterpret_cast<SgAsmStringStorage*>(*block)};
        for (unsigned i = 0; i < SgAsmStringStorage::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 222979 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmExpression that takes its corresponding StorageClass as parameter
 */
SgAsmExpression::SgAsmExpression(const SgAsmExpressionStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 222989 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_type = (SgAsmType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_type));
    p_comment = storageSource.storageOf_comment.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmExpression*
SgAsmExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExpression)};
        unsigned long positionInPool{localIndex % SgAsmExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmExpression::pool_size};
        returnPointer = &(((SgAsmExpression*)(SgAsmExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmExpression*
SgAsmExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmExpression)};
        unsigned long positionInPool{localIndex % SgAsmExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmExpression::pool_size};
        returnPointer = &(((SgAsmExpression*)(SgAsmExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmExpression::pools.begin(); block != SgAsmExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmExpression::pools.begin(); block != SgAsmExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmExpression::clearMemoryPool() {
    SgAsmExpression* tempPointer{nullptr};
    if (SgAsmExpression::pools.empty() == false) {
        auto block{SgAsmExpression::pools.begin()};
        SgAsmExpression::next_node = reinterpret_cast<SgAsmExpression*>(*block);
        for (; block != SgAsmExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmExpression::pool_size-1]);
        }
    }
}

void
SgAsmExpression::deleteMemoryPool() {
    for (auto p: SgAsmExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmExpression::next_node = nullptr;
    SgAsmExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExpression)};
    for (size_t blockIndex = SgAsmExpression::pools.size(); (blockIndex * SgAsmExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExpression) = %" PRIuPTR " SgAsmExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExpression),SgAsmExpression::pool_size);
        }
#endif

        SgAsmExpression* pointer{static_cast<SgAsmExpression*>(ROSE_MALLOC(SgAsmExpression::pool_size * sizeof(SgAsmExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmExpression::pool_size * sizeof(SgAsmExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmExpression::pool_size * sizeof(SgAsmExpression));
#endif
        SgAsmExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmExpression::pool_size * sizeof(SgAsmExpression), V_SgAsmExpression));

        if (SgAsmExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmExpression* blkptr{reinterpret_cast<SgAsmExpression*>(SgAsmExpression::pools[blockIndex-1])};
                blkptr[SgAsmExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmExpression::pool_size - 1};
    SgAsmExpression* testPointer{reinterpret_cast<SgAsmExpression*>(SgAsmExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmExpression::pool_size * (SgAsmExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmExpressionStroageClass
 * from its counterpart of SgAsmExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmExpression::initializeStorageClassArray(SgAsmExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmExpression::pools.begin(); block != SgAsmExpression::pools.end(); ++block) {
        SgAsmExpression* pointer{reinterpret_cast<SgAsmExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 223208 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmAarch32Coprocessor that takes its corresponding StorageClass as parameter
 */
SgAsmAarch32Coprocessor::SgAsmAarch32Coprocessor(const SgAsmAarch32CoprocessorStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 223218 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmAarch32Coprocessor*
SgAsmAarch32Coprocessor::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmAarch32Coprocessor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmAarch32Coprocessor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmAarch32Coprocessor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmAarch32Coprocessor)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmAarch32Coprocessor)};
        unsigned long positionInPool{localIndex % SgAsmAarch32Coprocessor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmAarch32Coprocessor::pool_size};
        returnPointer = &(((SgAsmAarch32Coprocessor*)(SgAsmAarch32Coprocessor::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmAarch32Coprocessor*
SgAsmAarch32Coprocessor::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmAarch32Coprocessor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmAarch32Coprocessor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmAarch32Coprocessor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmAarch32Coprocessor)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmAarch32Coprocessor)};
        unsigned long positionInPool{localIndex % SgAsmAarch32Coprocessor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmAarch32Coprocessor::pool_size};
        returnPointer = &(((SgAsmAarch32Coprocessor*)(SgAsmAarch32Coprocessor::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmAarch32Coprocessor::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmAarch32Coprocessor::pools.begin(); block != SgAsmAarch32Coprocessor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmAarch32Coprocessor*>(*block)};
        for (unsigned i = 0; i < SgAsmAarch32Coprocessor::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmAarch32Coprocessor*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmAarch32Coprocessor::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmAarch32Coprocessor* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmAarch32Coprocessor::pools.begin(); block != SgAsmAarch32Coprocessor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmAarch32Coprocessor*>(*block)};
        for (unsigned i = 0; i < SgAsmAarch32Coprocessor::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmAarch32Coprocessor::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmAarch32Coprocessor::clearMemoryPool() {
    SgAsmAarch32Coprocessor* tempPointer{nullptr};
    if (SgAsmAarch32Coprocessor::pools.empty() == false) {
        auto block{SgAsmAarch32Coprocessor::pools.begin()};
        SgAsmAarch32Coprocessor::next_node = reinterpret_cast<SgAsmAarch32Coprocessor*>(*block);
        for (; block != SgAsmAarch32Coprocessor::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmAarch32Coprocessor*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmAarch32Coprocessor::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmAarch32Coprocessor::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmAarch32Coprocessor::pool_size-1]);
        }
    }
}

void
SgAsmAarch32Coprocessor::deleteMemoryPool() {
    for (auto p: SgAsmAarch32Coprocessor::pools) {
        ROSE_FREE(p);
    }
    SgAsmAarch32Coprocessor::next_node = nullptr;
    SgAsmAarch32Coprocessor::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmAarch32Coprocessor::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmAarch32Coprocessor) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmAarch32Coprocessor)};
    for (size_t blockIndex = SgAsmAarch32Coprocessor::pools.size(); (blockIndex * SgAsmAarch32Coprocessor::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmAarch32Coprocessor) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmAarch32Coprocessor) = %" PRIuPTR " SgAsmAarch32Coprocessor::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmAarch32Coprocessor),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmAarch32Coprocessor),SgAsmAarch32Coprocessor::pool_size);
        }
#endif

        SgAsmAarch32Coprocessor* pointer{static_cast<SgAsmAarch32Coprocessor*>(ROSE_MALLOC(SgAsmAarch32Coprocessor::pool_size * sizeof(SgAsmAarch32Coprocessor)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmAarch32Coprocessor::pool_size * sizeof(SgAsmAarch32Coprocessor));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmAarch32Coprocessor::pool_size * sizeof(SgAsmAarch32Coprocessor));
#endif
        SgAsmAarch32Coprocessor::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmAarch32Coprocessor::pool_size * sizeof(SgAsmAarch32Coprocessor), V_SgAsmAarch32Coprocessor));

        if (SgAsmAarch32Coprocessor::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmAarch32Coprocessor* blkptr{reinterpret_cast<SgAsmAarch32Coprocessor*>(SgAsmAarch32Coprocessor::pools[blockIndex-1])};
                blkptr[SgAsmAarch32Coprocessor::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmAarch32Coprocessor::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmAarch32Coprocessor::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmAarch32Coprocessor::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmAarch32Coprocessor::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmAarch32Coprocessor::pool_size - 1};
    SgAsmAarch32Coprocessor* testPointer{reinterpret_cast<SgAsmAarch32Coprocessor*>(SgAsmAarch32Coprocessor::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmAarch32Coprocessor::pool_size * (SgAsmAarch32Coprocessor::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmAarch32CoprocessorStroageClass
 * from its counterpart of SgAsmAarch32Coprocessor. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmAarch32Coprocessor::initializeStorageClassArray(SgAsmAarch32CoprocessorStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmAarch32Coprocessor::pools.begin(); block != SgAsmAarch32Coprocessor::pools.end(); ++block) {
        SgAsmAarch32Coprocessor* pointer{reinterpret_cast<SgAsmAarch32Coprocessor*>(*block)};
        for (unsigned i = 0; i < SgAsmAarch32Coprocessor::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 223435 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryExpression that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryExpression::SgAsmBinaryExpression(const SgAsmBinaryExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 223445 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_lhs = (SgAsmExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_lhs));
     p_rhs = (SgAsmExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_rhs));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryExpression*
SgAsmBinaryExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryExpression)};
        unsigned long positionInPool{localIndex % SgAsmBinaryExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryExpression::pool_size};
        returnPointer = &(((SgAsmBinaryExpression*)(SgAsmBinaryExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryExpression*
SgAsmBinaryExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryExpression)};
        unsigned long positionInPool{localIndex % SgAsmBinaryExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryExpression::pool_size};
        returnPointer = &(((SgAsmBinaryExpression*)(SgAsmBinaryExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryExpression::pools.begin(); block != SgAsmBinaryExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryExpression::pools.begin(); block != SgAsmBinaryExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryExpression::clearMemoryPool() {
    SgAsmBinaryExpression* tempPointer{nullptr};
    if (SgAsmBinaryExpression::pools.empty() == false) {
        auto block{SgAsmBinaryExpression::pools.begin()};
        SgAsmBinaryExpression::next_node = reinterpret_cast<SgAsmBinaryExpression*>(*block);
        for (; block != SgAsmBinaryExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryExpression::pool_size-1]);
        }
    }
}

void
SgAsmBinaryExpression::deleteMemoryPool() {
    for (auto p: SgAsmBinaryExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryExpression::next_node = nullptr;
    SgAsmBinaryExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryExpression)};
    for (size_t blockIndex = SgAsmBinaryExpression::pools.size(); (blockIndex * SgAsmBinaryExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryExpression) = %" PRIuPTR " SgAsmBinaryExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryExpression),SgAsmBinaryExpression::pool_size);
        }
#endif

        SgAsmBinaryExpression* pointer{static_cast<SgAsmBinaryExpression*>(ROSE_MALLOC(SgAsmBinaryExpression::pool_size * sizeof(SgAsmBinaryExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryExpression::pool_size * sizeof(SgAsmBinaryExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryExpression::pool_size * sizeof(SgAsmBinaryExpression));
#endif
        SgAsmBinaryExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryExpression::pool_size * sizeof(SgAsmBinaryExpression), V_SgAsmBinaryExpression));

        if (SgAsmBinaryExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryExpression* blkptr{reinterpret_cast<SgAsmBinaryExpression*>(SgAsmBinaryExpression::pools[blockIndex-1])};
                blkptr[SgAsmBinaryExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryExpression::pool_size - 1};
    SgAsmBinaryExpression* testPointer{reinterpret_cast<SgAsmBinaryExpression*>(SgAsmBinaryExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryExpression::pool_size * (SgAsmBinaryExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryExpressionStroageClass
 * from its counterpart of SgAsmBinaryExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryExpression::initializeStorageClassArray(SgAsmBinaryExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryExpression::pools.begin(); block != SgAsmBinaryExpression::pools.end(); ++block) {
        SgAsmBinaryExpression* pointer{reinterpret_cast<SgAsmBinaryExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 223664 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryAdd that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryAdd::SgAsmBinaryAdd(const SgAsmBinaryAddStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 223674 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryAdd*
SgAsmBinaryAdd::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryAdd* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAdd)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAdd + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAdd)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAdd)};
        unsigned long positionInPool{localIndex % SgAsmBinaryAdd::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryAdd::pool_size};
        returnPointer = &(((SgAsmBinaryAdd*)(SgAsmBinaryAdd::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryAdd*
SgAsmBinaryAdd::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryAdd* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAdd)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAdd + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAdd)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryAdd)};
        unsigned long positionInPool{localIndex % SgAsmBinaryAdd::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryAdd::pool_size};
        returnPointer = &(((SgAsmBinaryAdd*)(SgAsmBinaryAdd::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryAdd::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryAdd::pools.begin(); block != SgAsmBinaryAdd::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryAdd*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAdd::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryAdd*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryAdd::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryAdd* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryAdd::pools.begin(); block != SgAsmBinaryAdd::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryAdd*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAdd::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryAdd::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryAdd::clearMemoryPool() {
    SgAsmBinaryAdd* tempPointer{nullptr};
    if (SgAsmBinaryAdd::pools.empty() == false) {
        auto block{SgAsmBinaryAdd::pools.begin()};
        SgAsmBinaryAdd::next_node = reinterpret_cast<SgAsmBinaryAdd*>(*block);
        for (; block != SgAsmBinaryAdd::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryAdd*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryAdd::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryAdd::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryAdd::pool_size-1]);
        }
    }
}

void
SgAsmBinaryAdd::deleteMemoryPool() {
    for (auto p: SgAsmBinaryAdd::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryAdd::next_node = nullptr;
    SgAsmBinaryAdd::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryAdd::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAdd) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAdd)};
    for (size_t blockIndex = SgAsmBinaryAdd::pools.size(); (blockIndex * SgAsmBinaryAdd::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAdd) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAdd) = %" PRIuPTR " SgAsmBinaryAdd::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAdd),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAdd),SgAsmBinaryAdd::pool_size);
        }
#endif

        SgAsmBinaryAdd* pointer{static_cast<SgAsmBinaryAdd*>(ROSE_MALLOC(SgAsmBinaryAdd::pool_size * sizeof(SgAsmBinaryAdd)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryAdd::pool_size * sizeof(SgAsmBinaryAdd));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryAdd::pool_size * sizeof(SgAsmBinaryAdd));
#endif
        SgAsmBinaryAdd::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryAdd::pool_size * sizeof(SgAsmBinaryAdd), V_SgAsmBinaryAdd));

        if (SgAsmBinaryAdd::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryAdd* blkptr{reinterpret_cast<SgAsmBinaryAdd*>(SgAsmBinaryAdd::pools[blockIndex-1])};
                blkptr[SgAsmBinaryAdd::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryAdd::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryAdd::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryAdd::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryAdd::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryAdd::pool_size - 1};
    SgAsmBinaryAdd* testPointer{reinterpret_cast<SgAsmBinaryAdd*>(SgAsmBinaryAdd::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryAdd::pool_size * (SgAsmBinaryAdd::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryAddStroageClass
 * from its counterpart of SgAsmBinaryAdd. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryAdd::initializeStorageClassArray(SgAsmBinaryAddStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryAdd::pools.begin(); block != SgAsmBinaryAdd::pools.end(); ++block) {
        SgAsmBinaryAdd* pointer{reinterpret_cast<SgAsmBinaryAdd*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAdd::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 223891 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryAsr that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryAsr::SgAsmBinaryAsr(const SgAsmBinaryAsrStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 223901 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryAsr*
SgAsmBinaryAsr::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryAsr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAsr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAsr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryAsr)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAsr)};
        unsigned long positionInPool{localIndex % SgAsmBinaryAsr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryAsr::pool_size};
        returnPointer = &(((SgAsmBinaryAsr*)(SgAsmBinaryAsr::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryAsr*
SgAsmBinaryAsr::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryAsr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAsr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAsr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryAsr)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryAsr)};
        unsigned long positionInPool{localIndex % SgAsmBinaryAsr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryAsr::pool_size};
        returnPointer = &(((SgAsmBinaryAsr*)(SgAsmBinaryAsr::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryAsr::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryAsr::pools.begin(); block != SgAsmBinaryAsr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryAsr*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAsr::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryAsr*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryAsr::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryAsr* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryAsr::pools.begin(); block != SgAsmBinaryAsr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryAsr*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAsr::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryAsr::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryAsr::clearMemoryPool() {
    SgAsmBinaryAsr* tempPointer{nullptr};
    if (SgAsmBinaryAsr::pools.empty() == false) {
        auto block{SgAsmBinaryAsr::pools.begin()};
        SgAsmBinaryAsr::next_node = reinterpret_cast<SgAsmBinaryAsr*>(*block);
        for (; block != SgAsmBinaryAsr::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryAsr*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryAsr::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryAsr::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryAsr::pool_size-1]);
        }
    }
}

void
SgAsmBinaryAsr::deleteMemoryPool() {
    for (auto p: SgAsmBinaryAsr::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryAsr::next_node = nullptr;
    SgAsmBinaryAsr::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryAsr::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAsr) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAsr)};
    for (size_t blockIndex = SgAsmBinaryAsr::pools.size(); (blockIndex * SgAsmBinaryAsr::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAsr) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAsr) = %" PRIuPTR " SgAsmBinaryAsr::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryAsr),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryAsr),SgAsmBinaryAsr::pool_size);
        }
#endif

        SgAsmBinaryAsr* pointer{static_cast<SgAsmBinaryAsr*>(ROSE_MALLOC(SgAsmBinaryAsr::pool_size * sizeof(SgAsmBinaryAsr)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryAsr::pool_size * sizeof(SgAsmBinaryAsr));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryAsr::pool_size * sizeof(SgAsmBinaryAsr));
#endif
        SgAsmBinaryAsr::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryAsr::pool_size * sizeof(SgAsmBinaryAsr), V_SgAsmBinaryAsr));

        if (SgAsmBinaryAsr::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryAsr* blkptr{reinterpret_cast<SgAsmBinaryAsr*>(SgAsmBinaryAsr::pools[blockIndex-1])};
                blkptr[SgAsmBinaryAsr::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryAsr::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryAsr::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryAsr::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryAsr::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryAsr::pool_size - 1};
    SgAsmBinaryAsr* testPointer{reinterpret_cast<SgAsmBinaryAsr*>(SgAsmBinaryAsr::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryAsr::pool_size * (SgAsmBinaryAsr::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryAsrStroageClass
 * from its counterpart of SgAsmBinaryAsr. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryAsr::initializeStorageClassArray(SgAsmBinaryAsrStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryAsr::pools.begin(); block != SgAsmBinaryAsr::pools.end(); ++block) {
        SgAsmBinaryAsr* pointer{reinterpret_cast<SgAsmBinaryAsr*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryAsr::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 224118 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryConcat that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryConcat::SgAsmBinaryConcat(const SgAsmBinaryConcatStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 224128 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryConcat*
SgAsmBinaryConcat::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryConcat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryConcat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryConcat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryConcat)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryConcat)};
        unsigned long positionInPool{localIndex % SgAsmBinaryConcat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryConcat::pool_size};
        returnPointer = &(((SgAsmBinaryConcat*)(SgAsmBinaryConcat::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryConcat*
SgAsmBinaryConcat::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryConcat* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryConcat)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryConcat + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryConcat)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryConcat)};
        unsigned long positionInPool{localIndex % SgAsmBinaryConcat::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryConcat::pool_size};
        returnPointer = &(((SgAsmBinaryConcat*)(SgAsmBinaryConcat::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryConcat::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryConcat::pools.begin(); block != SgAsmBinaryConcat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryConcat*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryConcat::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryConcat*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryConcat::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryConcat* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryConcat::pools.begin(); block != SgAsmBinaryConcat::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryConcat*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryConcat::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryConcat::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryConcat::clearMemoryPool() {
    SgAsmBinaryConcat* tempPointer{nullptr};
    if (SgAsmBinaryConcat::pools.empty() == false) {
        auto block{SgAsmBinaryConcat::pools.begin()};
        SgAsmBinaryConcat::next_node = reinterpret_cast<SgAsmBinaryConcat*>(*block);
        for (; block != SgAsmBinaryConcat::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryConcat*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryConcat::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryConcat::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryConcat::pool_size-1]);
        }
    }
}

void
SgAsmBinaryConcat::deleteMemoryPool() {
    for (auto p: SgAsmBinaryConcat::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryConcat::next_node = nullptr;
    SgAsmBinaryConcat::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryConcat::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryConcat) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryConcat)};
    for (size_t blockIndex = SgAsmBinaryConcat::pools.size(); (blockIndex * SgAsmBinaryConcat::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryConcat) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryConcat) = %" PRIuPTR " SgAsmBinaryConcat::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryConcat),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryConcat),SgAsmBinaryConcat::pool_size);
        }
#endif

        SgAsmBinaryConcat* pointer{static_cast<SgAsmBinaryConcat*>(ROSE_MALLOC(SgAsmBinaryConcat::pool_size * sizeof(SgAsmBinaryConcat)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryConcat::pool_size * sizeof(SgAsmBinaryConcat));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryConcat::pool_size * sizeof(SgAsmBinaryConcat));
#endif
        SgAsmBinaryConcat::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryConcat::pool_size * sizeof(SgAsmBinaryConcat), V_SgAsmBinaryConcat));

        if (SgAsmBinaryConcat::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryConcat* blkptr{reinterpret_cast<SgAsmBinaryConcat*>(SgAsmBinaryConcat::pools[blockIndex-1])};
                blkptr[SgAsmBinaryConcat::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryConcat::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryConcat::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryConcat::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryConcat::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryConcat::pool_size - 1};
    SgAsmBinaryConcat* testPointer{reinterpret_cast<SgAsmBinaryConcat*>(SgAsmBinaryConcat::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryConcat::pool_size * (SgAsmBinaryConcat::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryConcatStroageClass
 * from its counterpart of SgAsmBinaryConcat. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryConcat::initializeStorageClassArray(SgAsmBinaryConcatStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryConcat::pools.begin(); block != SgAsmBinaryConcat::pools.end(); ++block) {
        SgAsmBinaryConcat* pointer{reinterpret_cast<SgAsmBinaryConcat*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryConcat::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 224345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryDivide that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryDivide::SgAsmBinaryDivide(const SgAsmBinaryDivideStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 224355 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryDivide*
SgAsmBinaryDivide::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryDivide* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryDivide)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryDivide + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryDivide)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDivide)};
        unsigned long positionInPool{localIndex % SgAsmBinaryDivide::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryDivide::pool_size};
        returnPointer = &(((SgAsmBinaryDivide*)(SgAsmBinaryDivide::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryDivide*
SgAsmBinaryDivide::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryDivide* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryDivide)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryDivide + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryDivide)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryDivide)};
        unsigned long positionInPool{localIndex % SgAsmBinaryDivide::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryDivide::pool_size};
        returnPointer = &(((SgAsmBinaryDivide*)(SgAsmBinaryDivide::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryDivide::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryDivide::pools.begin(); block != SgAsmBinaryDivide::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryDivide*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryDivide::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryDivide*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryDivide::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryDivide* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryDivide::pools.begin(); block != SgAsmBinaryDivide::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryDivide*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryDivide::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryDivide::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryDivide::clearMemoryPool() {
    SgAsmBinaryDivide* tempPointer{nullptr};
    if (SgAsmBinaryDivide::pools.empty() == false) {
        auto block{SgAsmBinaryDivide::pools.begin()};
        SgAsmBinaryDivide::next_node = reinterpret_cast<SgAsmBinaryDivide*>(*block);
        for (; block != SgAsmBinaryDivide::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryDivide*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryDivide::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryDivide::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryDivide::pool_size-1]);
        }
    }
}

void
SgAsmBinaryDivide::deleteMemoryPool() {
    for (auto p: SgAsmBinaryDivide::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryDivide::next_node = nullptr;
    SgAsmBinaryDivide::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryDivide::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDivide) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryDivide)};
    for (size_t blockIndex = SgAsmBinaryDivide::pools.size(); (blockIndex * SgAsmBinaryDivide::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDivide) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryDivide) = %" PRIuPTR " SgAsmBinaryDivide::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryDivide),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryDivide),SgAsmBinaryDivide::pool_size);
        }
#endif

        SgAsmBinaryDivide* pointer{static_cast<SgAsmBinaryDivide*>(ROSE_MALLOC(SgAsmBinaryDivide::pool_size * sizeof(SgAsmBinaryDivide)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryDivide::pool_size * sizeof(SgAsmBinaryDivide));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryDivide::pool_size * sizeof(SgAsmBinaryDivide));
#endif
        SgAsmBinaryDivide::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryDivide::pool_size * sizeof(SgAsmBinaryDivide), V_SgAsmBinaryDivide));

        if (SgAsmBinaryDivide::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryDivide* blkptr{reinterpret_cast<SgAsmBinaryDivide*>(SgAsmBinaryDivide::pools[blockIndex-1])};
                blkptr[SgAsmBinaryDivide::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryDivide::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryDivide::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryDivide::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryDivide::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryDivide::pool_size - 1};
    SgAsmBinaryDivide* testPointer{reinterpret_cast<SgAsmBinaryDivide*>(SgAsmBinaryDivide::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryDivide::pool_size * (SgAsmBinaryDivide::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryDivideStroageClass
 * from its counterpart of SgAsmBinaryDivide. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryDivide::initializeStorageClassArray(SgAsmBinaryDivideStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryDivide::pools.begin(); block != SgAsmBinaryDivide::pools.end(); ++block) {
        SgAsmBinaryDivide* pointer{reinterpret_cast<SgAsmBinaryDivide*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryDivide::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 224572 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryLsl that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryLsl::SgAsmBinaryLsl(const SgAsmBinaryLslStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 224582 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryLsl*
SgAsmBinaryLsl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryLsl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryLsl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryLsl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryLsl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsl)};
        unsigned long positionInPool{localIndex % SgAsmBinaryLsl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryLsl::pool_size};
        returnPointer = &(((SgAsmBinaryLsl*)(SgAsmBinaryLsl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryLsl*
SgAsmBinaryLsl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryLsl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryLsl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryLsl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryLsl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryLsl)};
        unsigned long positionInPool{localIndex % SgAsmBinaryLsl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryLsl::pool_size};
        returnPointer = &(((SgAsmBinaryLsl*)(SgAsmBinaryLsl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryLsl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryLsl::pools.begin(); block != SgAsmBinaryLsl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryLsl*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryLsl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryLsl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryLsl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryLsl* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryLsl::pools.begin(); block != SgAsmBinaryLsl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryLsl*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryLsl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryLsl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryLsl::clearMemoryPool() {
    SgAsmBinaryLsl* tempPointer{nullptr};
    if (SgAsmBinaryLsl::pools.empty() == false) {
        auto block{SgAsmBinaryLsl::pools.begin()};
        SgAsmBinaryLsl::next_node = reinterpret_cast<SgAsmBinaryLsl*>(*block);
        for (; block != SgAsmBinaryLsl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryLsl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryLsl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryLsl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryLsl::pool_size-1]);
        }
    }
}

void
SgAsmBinaryLsl::deleteMemoryPool() {
    for (auto p: SgAsmBinaryLsl::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryLsl::next_node = nullptr;
    SgAsmBinaryLsl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryLsl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryLsl)};
    for (size_t blockIndex = SgAsmBinaryLsl::pools.size(); (blockIndex * SgAsmBinaryLsl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryLsl) = %" PRIuPTR " SgAsmBinaryLsl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryLsl),SgAsmBinaryLsl::pool_size);
        }
#endif

        SgAsmBinaryLsl* pointer{static_cast<SgAsmBinaryLsl*>(ROSE_MALLOC(SgAsmBinaryLsl::pool_size * sizeof(SgAsmBinaryLsl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryLsl::pool_size * sizeof(SgAsmBinaryLsl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryLsl::pool_size * sizeof(SgAsmBinaryLsl));
#endif
        SgAsmBinaryLsl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryLsl::pool_size * sizeof(SgAsmBinaryLsl), V_SgAsmBinaryLsl));

        if (SgAsmBinaryLsl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryLsl* blkptr{reinterpret_cast<SgAsmBinaryLsl*>(SgAsmBinaryLsl::pools[blockIndex-1])};
                blkptr[SgAsmBinaryLsl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryLsl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryLsl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryLsl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryLsl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryLsl::pool_size - 1};
    SgAsmBinaryLsl* testPointer{reinterpret_cast<SgAsmBinaryLsl*>(SgAsmBinaryLsl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryLsl::pool_size * (SgAsmBinaryLsl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryLslStroageClass
 * from its counterpart of SgAsmBinaryLsl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryLsl::initializeStorageClassArray(SgAsmBinaryLslStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryLsl::pools.begin(); block != SgAsmBinaryLsl::pools.end(); ++block) {
        SgAsmBinaryLsl* pointer{reinterpret_cast<SgAsmBinaryLsl*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryLsl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 224799 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryLsr that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryLsr::SgAsmBinaryLsr(const SgAsmBinaryLsrStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 224809 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryLsr*
SgAsmBinaryLsr::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryLsr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryLsr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryLsr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryLsr)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsr)};
        unsigned long positionInPool{localIndex % SgAsmBinaryLsr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryLsr::pool_size};
        returnPointer = &(((SgAsmBinaryLsr*)(SgAsmBinaryLsr::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryLsr*
SgAsmBinaryLsr::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryLsr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryLsr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryLsr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryLsr)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryLsr)};
        unsigned long positionInPool{localIndex % SgAsmBinaryLsr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryLsr::pool_size};
        returnPointer = &(((SgAsmBinaryLsr*)(SgAsmBinaryLsr::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryLsr::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryLsr::pools.begin(); block != SgAsmBinaryLsr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryLsr*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryLsr::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryLsr*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryLsr::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryLsr* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryLsr::pools.begin(); block != SgAsmBinaryLsr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryLsr*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryLsr::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryLsr::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryLsr::clearMemoryPool() {
    SgAsmBinaryLsr* tempPointer{nullptr};
    if (SgAsmBinaryLsr::pools.empty() == false) {
        auto block{SgAsmBinaryLsr::pools.begin()};
        SgAsmBinaryLsr::next_node = reinterpret_cast<SgAsmBinaryLsr*>(*block);
        for (; block != SgAsmBinaryLsr::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryLsr*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryLsr::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryLsr::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryLsr::pool_size-1]);
        }
    }
}

void
SgAsmBinaryLsr::deleteMemoryPool() {
    for (auto p: SgAsmBinaryLsr::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryLsr::next_node = nullptr;
    SgAsmBinaryLsr::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryLsr::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsr) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryLsr)};
    for (size_t blockIndex = SgAsmBinaryLsr::pools.size(); (blockIndex * SgAsmBinaryLsr::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsr) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryLsr) = %" PRIuPTR " SgAsmBinaryLsr::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryLsr),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryLsr),SgAsmBinaryLsr::pool_size);
        }
#endif

        SgAsmBinaryLsr* pointer{static_cast<SgAsmBinaryLsr*>(ROSE_MALLOC(SgAsmBinaryLsr::pool_size * sizeof(SgAsmBinaryLsr)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryLsr::pool_size * sizeof(SgAsmBinaryLsr));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryLsr::pool_size * sizeof(SgAsmBinaryLsr));
#endif
        SgAsmBinaryLsr::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryLsr::pool_size * sizeof(SgAsmBinaryLsr), V_SgAsmBinaryLsr));

        if (SgAsmBinaryLsr::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryLsr* blkptr{reinterpret_cast<SgAsmBinaryLsr*>(SgAsmBinaryLsr::pools[blockIndex-1])};
                blkptr[SgAsmBinaryLsr::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryLsr::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryLsr::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryLsr::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryLsr::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryLsr::pool_size - 1};
    SgAsmBinaryLsr* testPointer{reinterpret_cast<SgAsmBinaryLsr*>(SgAsmBinaryLsr::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryLsr::pool_size * (SgAsmBinaryLsr::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryLsrStroageClass
 * from its counterpart of SgAsmBinaryLsr. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryLsr::initializeStorageClassArray(SgAsmBinaryLsrStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryLsr::pools.begin(); block != SgAsmBinaryLsr::pools.end(); ++block) {
        SgAsmBinaryLsr* pointer{reinterpret_cast<SgAsmBinaryLsr*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryLsr::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 225026 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryMod that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryMod::SgAsmBinaryMod(const SgAsmBinaryModStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 225036 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryMod*
SgAsmBinaryMod::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryMod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMod)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMod)};
        unsigned long positionInPool{localIndex % SgAsmBinaryMod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryMod::pool_size};
        returnPointer = &(((SgAsmBinaryMod*)(SgAsmBinaryMod::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryMod*
SgAsmBinaryMod::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryMod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMod)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryMod)};
        unsigned long positionInPool{localIndex % SgAsmBinaryMod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryMod::pool_size};
        returnPointer = &(((SgAsmBinaryMod*)(SgAsmBinaryMod::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryMod::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryMod::pools.begin(); block != SgAsmBinaryMod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryMod*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMod::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryMod*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryMod::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryMod* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryMod::pools.begin(); block != SgAsmBinaryMod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryMod*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMod::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryMod::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryMod::clearMemoryPool() {
    SgAsmBinaryMod* tempPointer{nullptr};
    if (SgAsmBinaryMod::pools.empty() == false) {
        auto block{SgAsmBinaryMod::pools.begin()};
        SgAsmBinaryMod::next_node = reinterpret_cast<SgAsmBinaryMod*>(*block);
        for (; block != SgAsmBinaryMod::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryMod*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryMod::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryMod::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryMod::pool_size-1]);
        }
    }
}

void
SgAsmBinaryMod::deleteMemoryPool() {
    for (auto p: SgAsmBinaryMod::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryMod::next_node = nullptr;
    SgAsmBinaryMod::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryMod::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMod) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMod)};
    for (size_t blockIndex = SgAsmBinaryMod::pools.size(); (blockIndex * SgAsmBinaryMod::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMod) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMod) = %" PRIuPTR " SgAsmBinaryMod::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMod),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMod),SgAsmBinaryMod::pool_size);
        }
#endif

        SgAsmBinaryMod* pointer{static_cast<SgAsmBinaryMod*>(ROSE_MALLOC(SgAsmBinaryMod::pool_size * sizeof(SgAsmBinaryMod)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryMod::pool_size * sizeof(SgAsmBinaryMod));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryMod::pool_size * sizeof(SgAsmBinaryMod));
#endif
        SgAsmBinaryMod::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryMod::pool_size * sizeof(SgAsmBinaryMod), V_SgAsmBinaryMod));

        if (SgAsmBinaryMod::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryMod* blkptr{reinterpret_cast<SgAsmBinaryMod*>(SgAsmBinaryMod::pools[blockIndex-1])};
                blkptr[SgAsmBinaryMod::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryMod::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryMod::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryMod::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryMod::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryMod::pool_size - 1};
    SgAsmBinaryMod* testPointer{reinterpret_cast<SgAsmBinaryMod*>(SgAsmBinaryMod::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryMod::pool_size * (SgAsmBinaryMod::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryModStroageClass
 * from its counterpart of SgAsmBinaryMod. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryMod::initializeStorageClassArray(SgAsmBinaryModStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryMod::pools.begin(); block != SgAsmBinaryMod::pools.end(); ++block) {
        SgAsmBinaryMod* pointer{reinterpret_cast<SgAsmBinaryMod*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMod::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 225253 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryMsl that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryMsl::SgAsmBinaryMsl(const SgAsmBinaryMslStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 225263 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryMsl*
SgAsmBinaryMsl::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryMsl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMsl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMsl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMsl)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMsl)};
        unsigned long positionInPool{localIndex % SgAsmBinaryMsl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryMsl::pool_size};
        returnPointer = &(((SgAsmBinaryMsl*)(SgAsmBinaryMsl::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryMsl*
SgAsmBinaryMsl::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryMsl* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMsl)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMsl + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMsl)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryMsl)};
        unsigned long positionInPool{localIndex % SgAsmBinaryMsl::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryMsl::pool_size};
        returnPointer = &(((SgAsmBinaryMsl*)(SgAsmBinaryMsl::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryMsl::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryMsl::pools.begin(); block != SgAsmBinaryMsl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryMsl*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMsl::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryMsl*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryMsl::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryMsl* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryMsl::pools.begin(); block != SgAsmBinaryMsl::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryMsl*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMsl::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryMsl::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryMsl::clearMemoryPool() {
    SgAsmBinaryMsl* tempPointer{nullptr};
    if (SgAsmBinaryMsl::pools.empty() == false) {
        auto block{SgAsmBinaryMsl::pools.begin()};
        SgAsmBinaryMsl::next_node = reinterpret_cast<SgAsmBinaryMsl*>(*block);
        for (; block != SgAsmBinaryMsl::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryMsl*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryMsl::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryMsl::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryMsl::pool_size-1]);
        }
    }
}

void
SgAsmBinaryMsl::deleteMemoryPool() {
    for (auto p: SgAsmBinaryMsl::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryMsl::next_node = nullptr;
    SgAsmBinaryMsl::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryMsl::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMsl) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMsl)};
    for (size_t blockIndex = SgAsmBinaryMsl::pools.size(); (blockIndex * SgAsmBinaryMsl::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMsl) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMsl) = %" PRIuPTR " SgAsmBinaryMsl::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMsl),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMsl),SgAsmBinaryMsl::pool_size);
        }
#endif

        SgAsmBinaryMsl* pointer{static_cast<SgAsmBinaryMsl*>(ROSE_MALLOC(SgAsmBinaryMsl::pool_size * sizeof(SgAsmBinaryMsl)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryMsl::pool_size * sizeof(SgAsmBinaryMsl));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryMsl::pool_size * sizeof(SgAsmBinaryMsl));
#endif
        SgAsmBinaryMsl::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryMsl::pool_size * sizeof(SgAsmBinaryMsl), V_SgAsmBinaryMsl));

        if (SgAsmBinaryMsl::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryMsl* blkptr{reinterpret_cast<SgAsmBinaryMsl*>(SgAsmBinaryMsl::pools[blockIndex-1])};
                blkptr[SgAsmBinaryMsl::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryMsl::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryMsl::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryMsl::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryMsl::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryMsl::pool_size - 1};
    SgAsmBinaryMsl* testPointer{reinterpret_cast<SgAsmBinaryMsl*>(SgAsmBinaryMsl::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryMsl::pool_size * (SgAsmBinaryMsl::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryMslStroageClass
 * from its counterpart of SgAsmBinaryMsl. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryMsl::initializeStorageClassArray(SgAsmBinaryMslStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryMsl::pools.begin(); block != SgAsmBinaryMsl::pools.end(); ++block) {
        SgAsmBinaryMsl* pointer{reinterpret_cast<SgAsmBinaryMsl*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMsl::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 225480 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryMultiply that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryMultiply::SgAsmBinaryMultiply(const SgAsmBinaryMultiplyStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 225490 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryMultiply*
SgAsmBinaryMultiply::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryMultiply* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMultiply)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMultiply + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryMultiply)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMultiply)};
        unsigned long positionInPool{localIndex % SgAsmBinaryMultiply::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryMultiply::pool_size};
        returnPointer = &(((SgAsmBinaryMultiply*)(SgAsmBinaryMultiply::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryMultiply*
SgAsmBinaryMultiply::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryMultiply* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMultiply)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMultiply + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryMultiply)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryMultiply)};
        unsigned long positionInPool{localIndex % SgAsmBinaryMultiply::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryMultiply::pool_size};
        returnPointer = &(((SgAsmBinaryMultiply*)(SgAsmBinaryMultiply::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryMultiply::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryMultiply::pools.begin(); block != SgAsmBinaryMultiply::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryMultiply*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMultiply::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryMultiply*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryMultiply::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryMultiply* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryMultiply::pools.begin(); block != SgAsmBinaryMultiply::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryMultiply*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMultiply::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryMultiply::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryMultiply::clearMemoryPool() {
    SgAsmBinaryMultiply* tempPointer{nullptr};
    if (SgAsmBinaryMultiply::pools.empty() == false) {
        auto block{SgAsmBinaryMultiply::pools.begin()};
        SgAsmBinaryMultiply::next_node = reinterpret_cast<SgAsmBinaryMultiply*>(*block);
        for (; block != SgAsmBinaryMultiply::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryMultiply*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryMultiply::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryMultiply::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryMultiply::pool_size-1]);
        }
    }
}

void
SgAsmBinaryMultiply::deleteMemoryPool() {
    for (auto p: SgAsmBinaryMultiply::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryMultiply::next_node = nullptr;
    SgAsmBinaryMultiply::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryMultiply::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMultiply) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMultiply)};
    for (size_t blockIndex = SgAsmBinaryMultiply::pools.size(); (blockIndex * SgAsmBinaryMultiply::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMultiply) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMultiply) = %" PRIuPTR " SgAsmBinaryMultiply::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryMultiply),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryMultiply),SgAsmBinaryMultiply::pool_size);
        }
#endif

        SgAsmBinaryMultiply* pointer{static_cast<SgAsmBinaryMultiply*>(ROSE_MALLOC(SgAsmBinaryMultiply::pool_size * sizeof(SgAsmBinaryMultiply)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryMultiply::pool_size * sizeof(SgAsmBinaryMultiply));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryMultiply::pool_size * sizeof(SgAsmBinaryMultiply));
#endif
        SgAsmBinaryMultiply::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryMultiply::pool_size * sizeof(SgAsmBinaryMultiply), V_SgAsmBinaryMultiply));

        if (SgAsmBinaryMultiply::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryMultiply* blkptr{reinterpret_cast<SgAsmBinaryMultiply*>(SgAsmBinaryMultiply::pools[blockIndex-1])};
                blkptr[SgAsmBinaryMultiply::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryMultiply::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryMultiply::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryMultiply::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryMultiply::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryMultiply::pool_size - 1};
    SgAsmBinaryMultiply* testPointer{reinterpret_cast<SgAsmBinaryMultiply*>(SgAsmBinaryMultiply::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryMultiply::pool_size * (SgAsmBinaryMultiply::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryMultiplyStroageClass
 * from its counterpart of SgAsmBinaryMultiply. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryMultiply::initializeStorageClassArray(SgAsmBinaryMultiplyStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryMultiply::pools.begin(); block != SgAsmBinaryMultiply::pools.end(); ++block) {
        SgAsmBinaryMultiply* pointer{reinterpret_cast<SgAsmBinaryMultiply*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryMultiply::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 225707 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryPostupdate that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryPostupdate::SgAsmBinaryPostupdate(const SgAsmBinaryPostupdateStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 225717 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryPostupdate*
SgAsmBinaryPostupdate::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryPostupdate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryPostupdate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryPostupdate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryPostupdate)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPostupdate)};
        unsigned long positionInPool{localIndex % SgAsmBinaryPostupdate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryPostupdate::pool_size};
        returnPointer = &(((SgAsmBinaryPostupdate*)(SgAsmBinaryPostupdate::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryPostupdate*
SgAsmBinaryPostupdate::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryPostupdate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryPostupdate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryPostupdate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryPostupdate)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryPostupdate)};
        unsigned long positionInPool{localIndex % SgAsmBinaryPostupdate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryPostupdate::pool_size};
        returnPointer = &(((SgAsmBinaryPostupdate*)(SgAsmBinaryPostupdate::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryPostupdate::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryPostupdate::pools.begin(); block != SgAsmBinaryPostupdate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryPostupdate*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryPostupdate::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryPostupdate*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryPostupdate::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryPostupdate* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryPostupdate::pools.begin(); block != SgAsmBinaryPostupdate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryPostupdate*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryPostupdate::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryPostupdate::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryPostupdate::clearMemoryPool() {
    SgAsmBinaryPostupdate* tempPointer{nullptr};
    if (SgAsmBinaryPostupdate::pools.empty() == false) {
        auto block{SgAsmBinaryPostupdate::pools.begin()};
        SgAsmBinaryPostupdate::next_node = reinterpret_cast<SgAsmBinaryPostupdate*>(*block);
        for (; block != SgAsmBinaryPostupdate::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryPostupdate*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryPostupdate::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryPostupdate::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryPostupdate::pool_size-1]);
        }
    }
}

void
SgAsmBinaryPostupdate::deleteMemoryPool() {
    for (auto p: SgAsmBinaryPostupdate::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryPostupdate::next_node = nullptr;
    SgAsmBinaryPostupdate::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryPostupdate::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPostupdate) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryPostupdate)};
    for (size_t blockIndex = SgAsmBinaryPostupdate::pools.size(); (blockIndex * SgAsmBinaryPostupdate::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPostupdate) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryPostupdate) = %" PRIuPTR " SgAsmBinaryPostupdate::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPostupdate),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryPostupdate),SgAsmBinaryPostupdate::pool_size);
        }
#endif

        SgAsmBinaryPostupdate* pointer{static_cast<SgAsmBinaryPostupdate*>(ROSE_MALLOC(SgAsmBinaryPostupdate::pool_size * sizeof(SgAsmBinaryPostupdate)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryPostupdate::pool_size * sizeof(SgAsmBinaryPostupdate));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryPostupdate::pool_size * sizeof(SgAsmBinaryPostupdate));
#endif
        SgAsmBinaryPostupdate::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryPostupdate::pool_size * sizeof(SgAsmBinaryPostupdate), V_SgAsmBinaryPostupdate));

        if (SgAsmBinaryPostupdate::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryPostupdate* blkptr{reinterpret_cast<SgAsmBinaryPostupdate*>(SgAsmBinaryPostupdate::pools[blockIndex-1])};
                blkptr[SgAsmBinaryPostupdate::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryPostupdate::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryPostupdate::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryPostupdate::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryPostupdate::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryPostupdate::pool_size - 1};
    SgAsmBinaryPostupdate* testPointer{reinterpret_cast<SgAsmBinaryPostupdate*>(SgAsmBinaryPostupdate::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryPostupdate::pool_size * (SgAsmBinaryPostupdate::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryPostupdateStroageClass
 * from its counterpart of SgAsmBinaryPostupdate. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryPostupdate::initializeStorageClassArray(SgAsmBinaryPostupdateStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryPostupdate::pools.begin(); block != SgAsmBinaryPostupdate::pools.end(); ++block) {
        SgAsmBinaryPostupdate* pointer{reinterpret_cast<SgAsmBinaryPostupdate*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryPostupdate::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 225934 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryPreupdate that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryPreupdate::SgAsmBinaryPreupdate(const SgAsmBinaryPreupdateStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 225944 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryPreupdate*
SgAsmBinaryPreupdate::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryPreupdate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryPreupdate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryPreupdate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryPreupdate)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPreupdate)};
        unsigned long positionInPool{localIndex % SgAsmBinaryPreupdate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryPreupdate::pool_size};
        returnPointer = &(((SgAsmBinaryPreupdate*)(SgAsmBinaryPreupdate::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryPreupdate*
SgAsmBinaryPreupdate::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryPreupdate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryPreupdate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryPreupdate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryPreupdate)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryPreupdate)};
        unsigned long positionInPool{localIndex % SgAsmBinaryPreupdate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryPreupdate::pool_size};
        returnPointer = &(((SgAsmBinaryPreupdate*)(SgAsmBinaryPreupdate::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryPreupdate::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryPreupdate::pools.begin(); block != SgAsmBinaryPreupdate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryPreupdate*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryPreupdate::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryPreupdate*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryPreupdate::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryPreupdate* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryPreupdate::pools.begin(); block != SgAsmBinaryPreupdate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryPreupdate*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryPreupdate::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryPreupdate::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryPreupdate::clearMemoryPool() {
    SgAsmBinaryPreupdate* tempPointer{nullptr};
    if (SgAsmBinaryPreupdate::pools.empty() == false) {
        auto block{SgAsmBinaryPreupdate::pools.begin()};
        SgAsmBinaryPreupdate::next_node = reinterpret_cast<SgAsmBinaryPreupdate*>(*block);
        for (; block != SgAsmBinaryPreupdate::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryPreupdate*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryPreupdate::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryPreupdate::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryPreupdate::pool_size-1]);
        }
    }
}

void
SgAsmBinaryPreupdate::deleteMemoryPool() {
    for (auto p: SgAsmBinaryPreupdate::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryPreupdate::next_node = nullptr;
    SgAsmBinaryPreupdate::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryPreupdate::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPreupdate) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryPreupdate)};
    for (size_t blockIndex = SgAsmBinaryPreupdate::pools.size(); (blockIndex * SgAsmBinaryPreupdate::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPreupdate) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryPreupdate) = %" PRIuPTR " SgAsmBinaryPreupdate::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryPreupdate),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryPreupdate),SgAsmBinaryPreupdate::pool_size);
        }
#endif

        SgAsmBinaryPreupdate* pointer{static_cast<SgAsmBinaryPreupdate*>(ROSE_MALLOC(SgAsmBinaryPreupdate::pool_size * sizeof(SgAsmBinaryPreupdate)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryPreupdate::pool_size * sizeof(SgAsmBinaryPreupdate));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryPreupdate::pool_size * sizeof(SgAsmBinaryPreupdate));
#endif
        SgAsmBinaryPreupdate::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryPreupdate::pool_size * sizeof(SgAsmBinaryPreupdate), V_SgAsmBinaryPreupdate));

        if (SgAsmBinaryPreupdate::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryPreupdate* blkptr{reinterpret_cast<SgAsmBinaryPreupdate*>(SgAsmBinaryPreupdate::pools[blockIndex-1])};
                blkptr[SgAsmBinaryPreupdate::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryPreupdate::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryPreupdate::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryPreupdate::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryPreupdate::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryPreupdate::pool_size - 1};
    SgAsmBinaryPreupdate* testPointer{reinterpret_cast<SgAsmBinaryPreupdate*>(SgAsmBinaryPreupdate::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryPreupdate::pool_size * (SgAsmBinaryPreupdate::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryPreupdateStroageClass
 * from its counterpart of SgAsmBinaryPreupdate. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryPreupdate::initializeStorageClassArray(SgAsmBinaryPreupdateStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryPreupdate::pools.begin(); block != SgAsmBinaryPreupdate::pools.end(); ++block) {
        SgAsmBinaryPreupdate* pointer{reinterpret_cast<SgAsmBinaryPreupdate*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryPreupdate::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 226161 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinaryRor that takes its corresponding StorageClass as parameter
 */
SgAsmBinaryRor::SgAsmBinaryRor(const SgAsmBinaryRorStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 226171 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinaryRor*
SgAsmBinaryRor::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinaryRor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryRor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryRor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinaryRor)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryRor)};
        unsigned long positionInPool{localIndex % SgAsmBinaryRor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryRor::pool_size};
        returnPointer = &(((SgAsmBinaryRor*)(SgAsmBinaryRor::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinaryRor*
SgAsmBinaryRor::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinaryRor* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryRor)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryRor + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinaryRor)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinaryRor)};
        unsigned long positionInPool{localIndex % SgAsmBinaryRor::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinaryRor::pool_size};
        returnPointer = &(((SgAsmBinaryRor*)(SgAsmBinaryRor::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinaryRor::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinaryRor::pools.begin(); block != SgAsmBinaryRor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryRor*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryRor::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinaryRor*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinaryRor::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinaryRor* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinaryRor::pools.begin(); block != SgAsmBinaryRor::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinaryRor*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryRor::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinaryRor::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinaryRor::clearMemoryPool() {
    SgAsmBinaryRor* tempPointer{nullptr};
    if (SgAsmBinaryRor::pools.empty() == false) {
        auto block{SgAsmBinaryRor::pools.begin()};
        SgAsmBinaryRor::next_node = reinterpret_cast<SgAsmBinaryRor*>(*block);
        for (; block != SgAsmBinaryRor::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinaryRor*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinaryRor::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinaryRor::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinaryRor::pool_size-1]);
        }
    }
}

void
SgAsmBinaryRor::deleteMemoryPool() {
    for (auto p: SgAsmBinaryRor::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinaryRor::next_node = nullptr;
    SgAsmBinaryRor::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinaryRor::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryRor) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryRor)};
    for (size_t blockIndex = SgAsmBinaryRor::pools.size(); (blockIndex * SgAsmBinaryRor::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryRor) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryRor) = %" PRIuPTR " SgAsmBinaryRor::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinaryRor),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinaryRor),SgAsmBinaryRor::pool_size);
        }
#endif

        SgAsmBinaryRor* pointer{static_cast<SgAsmBinaryRor*>(ROSE_MALLOC(SgAsmBinaryRor::pool_size * sizeof(SgAsmBinaryRor)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinaryRor::pool_size * sizeof(SgAsmBinaryRor));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinaryRor::pool_size * sizeof(SgAsmBinaryRor));
#endif
        SgAsmBinaryRor::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinaryRor::pool_size * sizeof(SgAsmBinaryRor), V_SgAsmBinaryRor));

        if (SgAsmBinaryRor::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinaryRor* blkptr{reinterpret_cast<SgAsmBinaryRor*>(SgAsmBinaryRor::pools[blockIndex-1])};
                blkptr[SgAsmBinaryRor::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinaryRor::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinaryRor::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinaryRor::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinaryRor::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinaryRor::pool_size - 1};
    SgAsmBinaryRor* testPointer{reinterpret_cast<SgAsmBinaryRor*>(SgAsmBinaryRor::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinaryRor::pool_size * (SgAsmBinaryRor::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinaryRorStroageClass
 * from its counterpart of SgAsmBinaryRor. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinaryRor::initializeStorageClassArray(SgAsmBinaryRorStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinaryRor::pools.begin(); block != SgAsmBinaryRor::pools.end(); ++block) {
        SgAsmBinaryRor* pointer{reinterpret_cast<SgAsmBinaryRor*>(*block)};
        for (unsigned i = 0; i < SgAsmBinaryRor::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 226388 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBinarySubtract that takes its corresponding StorageClass as parameter
 */
SgAsmBinarySubtract::SgAsmBinarySubtract(const SgAsmBinarySubtractStorageClass& storageSource)  : SgAsmBinaryExpression (storageSource) {

/* #line 226398 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBinarySubtract*
SgAsmBinarySubtract::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBinarySubtract* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinarySubtract)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinarySubtract + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBinarySubtract)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinarySubtract)};
        unsigned long positionInPool{localIndex % SgAsmBinarySubtract::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinarySubtract::pool_size};
        returnPointer = &(((SgAsmBinarySubtract*)(SgAsmBinarySubtract::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBinarySubtract*
SgAsmBinarySubtract::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBinarySubtract* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinarySubtract)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinarySubtract + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBinarySubtract)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBinarySubtract)};
        unsigned long positionInPool{localIndex % SgAsmBinarySubtract::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBinarySubtract::pool_size};
        returnPointer = &(((SgAsmBinarySubtract*)(SgAsmBinarySubtract::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBinarySubtract::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBinarySubtract::pools.begin(); block != SgAsmBinarySubtract::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinarySubtract*>(*block)};
        for (unsigned i = 0; i < SgAsmBinarySubtract::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBinarySubtract*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBinarySubtract::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBinarySubtract* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBinarySubtract::pools.begin(); block != SgAsmBinarySubtract::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBinarySubtract*>(*block)};
        for (unsigned i = 0; i < SgAsmBinarySubtract::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBinarySubtract::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBinarySubtract::clearMemoryPool() {
    SgAsmBinarySubtract* tempPointer{nullptr};
    if (SgAsmBinarySubtract::pools.empty() == false) {
        auto block{SgAsmBinarySubtract::pools.begin()};
        SgAsmBinarySubtract::next_node = reinterpret_cast<SgAsmBinarySubtract*>(*block);
        for (; block != SgAsmBinarySubtract::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBinarySubtract*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBinarySubtract::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBinarySubtract::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBinarySubtract::pool_size-1]);
        }
    }
}

void
SgAsmBinarySubtract::deleteMemoryPool() {
    for (auto p: SgAsmBinarySubtract::pools) {
        ROSE_FREE(p);
    }
    SgAsmBinarySubtract::next_node = nullptr;
    SgAsmBinarySubtract::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBinarySubtract::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinarySubtract) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinarySubtract)};
    for (size_t blockIndex = SgAsmBinarySubtract::pools.size(); (blockIndex * SgAsmBinarySubtract::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinarySubtract) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinarySubtract) = %" PRIuPTR " SgAsmBinarySubtract::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBinarySubtract),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBinarySubtract),SgAsmBinarySubtract::pool_size);
        }
#endif

        SgAsmBinarySubtract* pointer{static_cast<SgAsmBinarySubtract*>(ROSE_MALLOC(SgAsmBinarySubtract::pool_size * sizeof(SgAsmBinarySubtract)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBinarySubtract::pool_size * sizeof(SgAsmBinarySubtract));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBinarySubtract::pool_size * sizeof(SgAsmBinarySubtract));
#endif
        SgAsmBinarySubtract::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBinarySubtract::pool_size * sizeof(SgAsmBinarySubtract), V_SgAsmBinarySubtract));

        if (SgAsmBinarySubtract::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBinarySubtract* blkptr{reinterpret_cast<SgAsmBinarySubtract*>(SgAsmBinarySubtract::pools[blockIndex-1])};
                blkptr[SgAsmBinarySubtract::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBinarySubtract::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBinarySubtract::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBinarySubtract::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBinarySubtract::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBinarySubtract::pool_size - 1};
    SgAsmBinarySubtract* testPointer{reinterpret_cast<SgAsmBinarySubtract*>(SgAsmBinarySubtract::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBinarySubtract::pool_size * (SgAsmBinarySubtract::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBinarySubtractStroageClass
 * from its counterpart of SgAsmBinarySubtract. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBinarySubtract::initializeStorageClassArray(SgAsmBinarySubtractStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBinarySubtract::pools.begin(); block != SgAsmBinarySubtract::pools.end(); ++block) {
        SgAsmBinarySubtract* pointer{reinterpret_cast<SgAsmBinarySubtract*>(*block)};
        for (unsigned i = 0; i < SgAsmBinarySubtract::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 226615 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmByteOrder that takes its corresponding StorageClass as parameter
 */
SgAsmByteOrder::SgAsmByteOrder(const SgAsmByteOrderStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 226625 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmByteOrder*
SgAsmByteOrder::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmByteOrder* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmByteOrder)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmByteOrder + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmByteOrder)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmByteOrder)};
        unsigned long positionInPool{localIndex % SgAsmByteOrder::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmByteOrder::pool_size};
        returnPointer = &(((SgAsmByteOrder*)(SgAsmByteOrder::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmByteOrder*
SgAsmByteOrder::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmByteOrder* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmByteOrder)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmByteOrder + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmByteOrder)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmByteOrder)};
        unsigned long positionInPool{localIndex % SgAsmByteOrder::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmByteOrder::pool_size};
        returnPointer = &(((SgAsmByteOrder*)(SgAsmByteOrder::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmByteOrder::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmByteOrder::pools.begin(); block != SgAsmByteOrder::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmByteOrder*>(*block)};
        for (unsigned i = 0; i < SgAsmByteOrder::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmByteOrder*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmByteOrder::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmByteOrder* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmByteOrder::pools.begin(); block != SgAsmByteOrder::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmByteOrder*>(*block)};
        for (unsigned i = 0; i < SgAsmByteOrder::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmByteOrder::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmByteOrder::clearMemoryPool() {
    SgAsmByteOrder* tempPointer{nullptr};
    if (SgAsmByteOrder::pools.empty() == false) {
        auto block{SgAsmByteOrder::pools.begin()};
        SgAsmByteOrder::next_node = reinterpret_cast<SgAsmByteOrder*>(*block);
        for (; block != SgAsmByteOrder::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmByteOrder*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmByteOrder::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmByteOrder::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmByteOrder::pool_size-1]);
        }
    }
}

void
SgAsmByteOrder::deleteMemoryPool() {
    for (auto p: SgAsmByteOrder::pools) {
        ROSE_FREE(p);
    }
    SgAsmByteOrder::next_node = nullptr;
    SgAsmByteOrder::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmByteOrder::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmByteOrder) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmByteOrder)};
    for (size_t blockIndex = SgAsmByteOrder::pools.size(); (blockIndex * SgAsmByteOrder::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmByteOrder) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmByteOrder) = %" PRIuPTR " SgAsmByteOrder::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmByteOrder),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmByteOrder),SgAsmByteOrder::pool_size);
        }
#endif

        SgAsmByteOrder* pointer{static_cast<SgAsmByteOrder*>(ROSE_MALLOC(SgAsmByteOrder::pool_size * sizeof(SgAsmByteOrder)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmByteOrder::pool_size * sizeof(SgAsmByteOrder));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmByteOrder::pool_size * sizeof(SgAsmByteOrder));
#endif
        SgAsmByteOrder::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmByteOrder::pool_size * sizeof(SgAsmByteOrder), V_SgAsmByteOrder));

        if (SgAsmByteOrder::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmByteOrder* blkptr{reinterpret_cast<SgAsmByteOrder*>(SgAsmByteOrder::pools[blockIndex-1])};
                blkptr[SgAsmByteOrder::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmByteOrder::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmByteOrder::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmByteOrder::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmByteOrder::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmByteOrder::pool_size - 1};
    SgAsmByteOrder* testPointer{reinterpret_cast<SgAsmByteOrder*>(SgAsmByteOrder::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmByteOrder::pool_size * (SgAsmByteOrder::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmByteOrderStroageClass
 * from its counterpart of SgAsmByteOrder. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmByteOrder::initializeStorageClassArray(SgAsmByteOrderStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmByteOrder::pools.begin(); block != SgAsmByteOrder::pools.end(); ++block) {
        SgAsmByteOrder* pointer{reinterpret_cast<SgAsmByteOrder*>(*block)};
        for (unsigned i = 0; i < SgAsmByteOrder::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 226842 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCommonSubExpression that takes its corresponding StorageClass as parameter
 */
SgAsmCommonSubExpression::SgAsmCommonSubExpression(const SgAsmCommonSubExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 226852 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_subexpression = (SgAsmExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_subexpression));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCommonSubExpression*
SgAsmCommonSubExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCommonSubExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCommonSubExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCommonSubExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCommonSubExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCommonSubExpression)};
        unsigned long positionInPool{localIndex % SgAsmCommonSubExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCommonSubExpression::pool_size};
        returnPointer = &(((SgAsmCommonSubExpression*)(SgAsmCommonSubExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCommonSubExpression*
SgAsmCommonSubExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCommonSubExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCommonSubExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCommonSubExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCommonSubExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCommonSubExpression)};
        unsigned long positionInPool{localIndex % SgAsmCommonSubExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCommonSubExpression::pool_size};
        returnPointer = &(((SgAsmCommonSubExpression*)(SgAsmCommonSubExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCommonSubExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCommonSubExpression::pools.begin(); block != SgAsmCommonSubExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCommonSubExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmCommonSubExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCommonSubExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCommonSubExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCommonSubExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCommonSubExpression::pools.begin(); block != SgAsmCommonSubExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCommonSubExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmCommonSubExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCommonSubExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCommonSubExpression::clearMemoryPool() {
    SgAsmCommonSubExpression* tempPointer{nullptr};
    if (SgAsmCommonSubExpression::pools.empty() == false) {
        auto block{SgAsmCommonSubExpression::pools.begin()};
        SgAsmCommonSubExpression::next_node = reinterpret_cast<SgAsmCommonSubExpression*>(*block);
        for (; block != SgAsmCommonSubExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCommonSubExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCommonSubExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCommonSubExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCommonSubExpression::pool_size-1]);
        }
    }
}

void
SgAsmCommonSubExpression::deleteMemoryPool() {
    for (auto p: SgAsmCommonSubExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmCommonSubExpression::next_node = nullptr;
    SgAsmCommonSubExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCommonSubExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCommonSubExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCommonSubExpression)};
    for (size_t blockIndex = SgAsmCommonSubExpression::pools.size(); (blockIndex * SgAsmCommonSubExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCommonSubExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCommonSubExpression) = %" PRIuPTR " SgAsmCommonSubExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCommonSubExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCommonSubExpression),SgAsmCommonSubExpression::pool_size);
        }
#endif

        SgAsmCommonSubExpression* pointer{static_cast<SgAsmCommonSubExpression*>(ROSE_MALLOC(SgAsmCommonSubExpression::pool_size * sizeof(SgAsmCommonSubExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCommonSubExpression::pool_size * sizeof(SgAsmCommonSubExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCommonSubExpression::pool_size * sizeof(SgAsmCommonSubExpression));
#endif
        SgAsmCommonSubExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCommonSubExpression::pool_size * sizeof(SgAsmCommonSubExpression), V_SgAsmCommonSubExpression));

        if (SgAsmCommonSubExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCommonSubExpression* blkptr{reinterpret_cast<SgAsmCommonSubExpression*>(SgAsmCommonSubExpression::pools[blockIndex-1])};
                blkptr[SgAsmCommonSubExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCommonSubExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCommonSubExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCommonSubExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCommonSubExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCommonSubExpression::pool_size - 1};
    SgAsmCommonSubExpression* testPointer{reinterpret_cast<SgAsmCommonSubExpression*>(SgAsmCommonSubExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCommonSubExpression::pool_size * (SgAsmCommonSubExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCommonSubExpressionStroageClass
 * from its counterpart of SgAsmCommonSubExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCommonSubExpression::initializeStorageClassArray(SgAsmCommonSubExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCommonSubExpression::pools.begin(); block != SgAsmCommonSubExpression::pools.end(); ++block) {
        SgAsmCommonSubExpression* pointer{reinterpret_cast<SgAsmCommonSubExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmCommonSubExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 227070 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmControlFlagsExpression that takes its corresponding StorageClass as parameter
 */
SgAsmControlFlagsExpression::SgAsmControlFlagsExpression(const SgAsmControlFlagsExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 227080 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_bitFlags = storageSource.storageOf_bitFlags;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmControlFlagsExpression*
SgAsmControlFlagsExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmControlFlagsExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmControlFlagsExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmControlFlagsExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmControlFlagsExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmControlFlagsExpression)};
        unsigned long positionInPool{localIndex % SgAsmControlFlagsExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmControlFlagsExpression::pool_size};
        returnPointer = &(((SgAsmControlFlagsExpression*)(SgAsmControlFlagsExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmControlFlagsExpression*
SgAsmControlFlagsExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmControlFlagsExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmControlFlagsExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmControlFlagsExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmControlFlagsExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmControlFlagsExpression)};
        unsigned long positionInPool{localIndex % SgAsmControlFlagsExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmControlFlagsExpression::pool_size};
        returnPointer = &(((SgAsmControlFlagsExpression*)(SgAsmControlFlagsExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmControlFlagsExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmControlFlagsExpression::pools.begin(); block != SgAsmControlFlagsExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmControlFlagsExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmControlFlagsExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmControlFlagsExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmControlFlagsExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmControlFlagsExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmControlFlagsExpression::pools.begin(); block != SgAsmControlFlagsExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmControlFlagsExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmControlFlagsExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmControlFlagsExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmControlFlagsExpression::clearMemoryPool() {
    SgAsmControlFlagsExpression* tempPointer{nullptr};
    if (SgAsmControlFlagsExpression::pools.empty() == false) {
        auto block{SgAsmControlFlagsExpression::pools.begin()};
        SgAsmControlFlagsExpression::next_node = reinterpret_cast<SgAsmControlFlagsExpression*>(*block);
        for (; block != SgAsmControlFlagsExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmControlFlagsExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmControlFlagsExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmControlFlagsExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmControlFlagsExpression::pool_size-1]);
        }
    }
}

void
SgAsmControlFlagsExpression::deleteMemoryPool() {
    for (auto p: SgAsmControlFlagsExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmControlFlagsExpression::next_node = nullptr;
    SgAsmControlFlagsExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmControlFlagsExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmControlFlagsExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmControlFlagsExpression)};
    for (size_t blockIndex = SgAsmControlFlagsExpression::pools.size(); (blockIndex * SgAsmControlFlagsExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmControlFlagsExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmControlFlagsExpression) = %" PRIuPTR " SgAsmControlFlagsExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmControlFlagsExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmControlFlagsExpression),SgAsmControlFlagsExpression::pool_size);
        }
#endif

        SgAsmControlFlagsExpression* pointer{static_cast<SgAsmControlFlagsExpression*>(ROSE_MALLOC(SgAsmControlFlagsExpression::pool_size * sizeof(SgAsmControlFlagsExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmControlFlagsExpression::pool_size * sizeof(SgAsmControlFlagsExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmControlFlagsExpression::pool_size * sizeof(SgAsmControlFlagsExpression));
#endif
        SgAsmControlFlagsExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmControlFlagsExpression::pool_size * sizeof(SgAsmControlFlagsExpression), V_SgAsmControlFlagsExpression));

        if (SgAsmControlFlagsExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmControlFlagsExpression* blkptr{reinterpret_cast<SgAsmControlFlagsExpression*>(SgAsmControlFlagsExpression::pools[blockIndex-1])};
                blkptr[SgAsmControlFlagsExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmControlFlagsExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmControlFlagsExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmControlFlagsExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmControlFlagsExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmControlFlagsExpression::pool_size - 1};
    SgAsmControlFlagsExpression* testPointer{reinterpret_cast<SgAsmControlFlagsExpression*>(SgAsmControlFlagsExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmControlFlagsExpression::pool_size * (SgAsmControlFlagsExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmControlFlagsExpressionStroageClass
 * from its counterpart of SgAsmControlFlagsExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmControlFlagsExpression::initializeStorageClassArray(SgAsmControlFlagsExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmControlFlagsExpression::pools.begin(); block != SgAsmControlFlagsExpression::pools.end(); ++block) {
        SgAsmControlFlagsExpression* pointer{reinterpret_cast<SgAsmControlFlagsExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmControlFlagsExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 227298 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmExprListExp that takes its corresponding StorageClass as parameter
 */
SgAsmExprListExp::SgAsmExprListExp(const SgAsmExprListExpStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 227308 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_expressions = storageSource.storageOf_expressions.rebuildDataStoredInEasyStorageClass();
    SgAsmExpressionPtrList::iterator i_expressions = p_expressions.begin();
    for ( ; i_expressions != p_expressions.end(); ++i_expressions) {
        (*i_expressions) = (SgAsmExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_expressions)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmExprListExp*
SgAsmExprListExp::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmExprListExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExprListExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExprListExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmExprListExp)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExprListExp)};
        unsigned long positionInPool{localIndex % SgAsmExprListExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmExprListExp::pool_size};
        returnPointer = &(((SgAsmExprListExp*)(SgAsmExprListExp::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmExprListExp*
SgAsmExprListExp::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmExprListExp* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExprListExp)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExprListExp + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmExprListExp)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmExprListExp)};
        unsigned long positionInPool{localIndex % SgAsmExprListExp::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmExprListExp::pool_size};
        returnPointer = &(((SgAsmExprListExp*)(SgAsmExprListExp::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmExprListExp::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmExprListExp::pools.begin(); block != SgAsmExprListExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmExprListExp*>(*block)};
        for (unsigned i = 0; i < SgAsmExprListExp::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmExprListExp*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmExprListExp::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmExprListExp* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmExprListExp::pools.begin(); block != SgAsmExprListExp::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmExprListExp*>(*block)};
        for (unsigned i = 0; i < SgAsmExprListExp::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmExprListExp::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmExprListExp::clearMemoryPool() {
    SgAsmExprListExp* tempPointer{nullptr};
    if (SgAsmExprListExp::pools.empty() == false) {
        auto block{SgAsmExprListExp::pools.begin()};
        SgAsmExprListExp::next_node = reinterpret_cast<SgAsmExprListExp*>(*block);
        for (; block != SgAsmExprListExp::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmExprListExp*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmExprListExp::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmExprListExp::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmExprListExp::pool_size-1]);
        }
    }
}

void
SgAsmExprListExp::deleteMemoryPool() {
    for (auto p: SgAsmExprListExp::pools) {
        ROSE_FREE(p);
    }
    SgAsmExprListExp::next_node = nullptr;
    SgAsmExprListExp::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmExprListExp::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExprListExp) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExprListExp)};
    for (size_t blockIndex = SgAsmExprListExp::pools.size(); (blockIndex * SgAsmExprListExp::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExprListExp) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExprListExp) = %" PRIuPTR " SgAsmExprListExp::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmExprListExp),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmExprListExp),SgAsmExprListExp::pool_size);
        }
#endif

        SgAsmExprListExp* pointer{static_cast<SgAsmExprListExp*>(ROSE_MALLOC(SgAsmExprListExp::pool_size * sizeof(SgAsmExprListExp)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmExprListExp::pool_size * sizeof(SgAsmExprListExp));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmExprListExp::pool_size * sizeof(SgAsmExprListExp));
#endif
        SgAsmExprListExp::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmExprListExp::pool_size * sizeof(SgAsmExprListExp), V_SgAsmExprListExp));

        if (SgAsmExprListExp::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmExprListExp* blkptr{reinterpret_cast<SgAsmExprListExp*>(SgAsmExprListExp::pools[blockIndex-1])};
                blkptr[SgAsmExprListExp::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmExprListExp::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmExprListExp::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmExprListExp::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmExprListExp::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmExprListExp::pool_size - 1};
    SgAsmExprListExp* testPointer{reinterpret_cast<SgAsmExprListExp*>(SgAsmExprListExp::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmExprListExp::pool_size * (SgAsmExprListExp::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmExprListExpStroageClass
 * from its counterpart of SgAsmExprListExp. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmExprListExp::initializeStorageClassArray(SgAsmExprListExpStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmExprListExp::pools.begin(); block != SgAsmExprListExp::pools.end(); ++block) {
        SgAsmExprListExp* pointer{reinterpret_cast<SgAsmExprListExp*>(*block)};
        for (unsigned i = 0; i < SgAsmExprListExp::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 227530 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmMemoryReferenceExpression that takes its corresponding StorageClass as parameter
 */
SgAsmMemoryReferenceExpression::SgAsmMemoryReferenceExpression(const SgAsmMemoryReferenceExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 227540 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_address = (SgAsmExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_address));
     p_segment = (SgAsmExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_segment));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmMemoryReferenceExpression*
SgAsmMemoryReferenceExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmMemoryReferenceExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmMemoryReferenceExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmMemoryReferenceExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmMemoryReferenceExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMemoryReferenceExpression)};
        unsigned long positionInPool{localIndex % SgAsmMemoryReferenceExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmMemoryReferenceExpression::pool_size};
        returnPointer = &(((SgAsmMemoryReferenceExpression*)(SgAsmMemoryReferenceExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmMemoryReferenceExpression*
SgAsmMemoryReferenceExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmMemoryReferenceExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmMemoryReferenceExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmMemoryReferenceExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmMemoryReferenceExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmMemoryReferenceExpression)};
        unsigned long positionInPool{localIndex % SgAsmMemoryReferenceExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmMemoryReferenceExpression::pool_size};
        returnPointer = &(((SgAsmMemoryReferenceExpression*)(SgAsmMemoryReferenceExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmMemoryReferenceExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmMemoryReferenceExpression::pools.begin(); block != SgAsmMemoryReferenceExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmMemoryReferenceExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmMemoryReferenceExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmMemoryReferenceExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmMemoryReferenceExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmMemoryReferenceExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmMemoryReferenceExpression::pools.begin(); block != SgAsmMemoryReferenceExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmMemoryReferenceExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmMemoryReferenceExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmMemoryReferenceExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmMemoryReferenceExpression::clearMemoryPool() {
    SgAsmMemoryReferenceExpression* tempPointer{nullptr};
    if (SgAsmMemoryReferenceExpression::pools.empty() == false) {
        auto block{SgAsmMemoryReferenceExpression::pools.begin()};
        SgAsmMemoryReferenceExpression::next_node = reinterpret_cast<SgAsmMemoryReferenceExpression*>(*block);
        for (; block != SgAsmMemoryReferenceExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmMemoryReferenceExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmMemoryReferenceExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmMemoryReferenceExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmMemoryReferenceExpression::pool_size-1]);
        }
    }
}

void
SgAsmMemoryReferenceExpression::deleteMemoryPool() {
    for (auto p: SgAsmMemoryReferenceExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmMemoryReferenceExpression::next_node = nullptr;
    SgAsmMemoryReferenceExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmMemoryReferenceExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMemoryReferenceExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmMemoryReferenceExpression)};
    for (size_t blockIndex = SgAsmMemoryReferenceExpression::pools.size(); (blockIndex * SgAsmMemoryReferenceExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMemoryReferenceExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmMemoryReferenceExpression) = %" PRIuPTR " SgAsmMemoryReferenceExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMemoryReferenceExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmMemoryReferenceExpression),SgAsmMemoryReferenceExpression::pool_size);
        }
#endif

        SgAsmMemoryReferenceExpression* pointer{static_cast<SgAsmMemoryReferenceExpression*>(ROSE_MALLOC(SgAsmMemoryReferenceExpression::pool_size * sizeof(SgAsmMemoryReferenceExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmMemoryReferenceExpression::pool_size * sizeof(SgAsmMemoryReferenceExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmMemoryReferenceExpression::pool_size * sizeof(SgAsmMemoryReferenceExpression));
#endif
        SgAsmMemoryReferenceExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmMemoryReferenceExpression::pool_size * sizeof(SgAsmMemoryReferenceExpression), V_SgAsmMemoryReferenceExpression));

        if (SgAsmMemoryReferenceExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmMemoryReferenceExpression* blkptr{reinterpret_cast<SgAsmMemoryReferenceExpression*>(SgAsmMemoryReferenceExpression::pools[blockIndex-1])};
                blkptr[SgAsmMemoryReferenceExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmMemoryReferenceExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmMemoryReferenceExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmMemoryReferenceExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmMemoryReferenceExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmMemoryReferenceExpression::pool_size - 1};
    SgAsmMemoryReferenceExpression* testPointer{reinterpret_cast<SgAsmMemoryReferenceExpression*>(SgAsmMemoryReferenceExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmMemoryReferenceExpression::pool_size * (SgAsmMemoryReferenceExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmMemoryReferenceExpressionStroageClass
 * from its counterpart of SgAsmMemoryReferenceExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmMemoryReferenceExpression::initializeStorageClassArray(SgAsmMemoryReferenceExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmMemoryReferenceExpression::pools.begin(); block != SgAsmMemoryReferenceExpression::pools.end(); ++block) {
        SgAsmMemoryReferenceExpression* pointer{reinterpret_cast<SgAsmMemoryReferenceExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmMemoryReferenceExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 227759 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmRegisterNames that takes its corresponding StorageClass as parameter
 */
SgAsmRegisterNames::SgAsmRegisterNames(const SgAsmRegisterNamesStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 227769 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_registers = storageSource.storageOf_registers.rebuildDataStoredInEasyStorageClass();
    SgAsmRegisterReferenceExpressionPtrList::iterator i_registers = p_registers.begin();
    for ( ; i_registers != p_registers.end(); ++i_registers) {
        (*i_registers) = (SgAsmRegisterReferenceExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_registers)) );
    }
    p_mask = storageSource.storageOf_mask;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmRegisterNames*
SgAsmRegisterNames::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmRegisterNames* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRegisterNames)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRegisterNames + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRegisterNames)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterNames)};
        unsigned long positionInPool{localIndex % SgAsmRegisterNames::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmRegisterNames::pool_size};
        returnPointer = &(((SgAsmRegisterNames*)(SgAsmRegisterNames::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmRegisterNames*
SgAsmRegisterNames::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmRegisterNames* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRegisterNames)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRegisterNames + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRegisterNames)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmRegisterNames)};
        unsigned long positionInPool{localIndex % SgAsmRegisterNames::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmRegisterNames::pool_size};
        returnPointer = &(((SgAsmRegisterNames*)(SgAsmRegisterNames::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmRegisterNames::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmRegisterNames::pools.begin(); block != SgAsmRegisterNames::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmRegisterNames*>(*block)};
        for (unsigned i = 0; i < SgAsmRegisterNames::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmRegisterNames*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmRegisterNames::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmRegisterNames* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmRegisterNames::pools.begin(); block != SgAsmRegisterNames::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmRegisterNames*>(*block)};
        for (unsigned i = 0; i < SgAsmRegisterNames::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmRegisterNames::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmRegisterNames::clearMemoryPool() {
    SgAsmRegisterNames* tempPointer{nullptr};
    if (SgAsmRegisterNames::pools.empty() == false) {
        auto block{SgAsmRegisterNames::pools.begin()};
        SgAsmRegisterNames::next_node = reinterpret_cast<SgAsmRegisterNames*>(*block);
        for (; block != SgAsmRegisterNames::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmRegisterNames*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmRegisterNames::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmRegisterNames::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmRegisterNames::pool_size-1]);
        }
    }
}

void
SgAsmRegisterNames::deleteMemoryPool() {
    for (auto p: SgAsmRegisterNames::pools) {
        ROSE_FREE(p);
    }
    SgAsmRegisterNames::next_node = nullptr;
    SgAsmRegisterNames::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmRegisterNames::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterNames) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRegisterNames)};
    for (size_t blockIndex = SgAsmRegisterNames::pools.size(); (blockIndex * SgAsmRegisterNames::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterNames) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRegisterNames) = %" PRIuPTR " SgAsmRegisterNames::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterNames),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRegisterNames),SgAsmRegisterNames::pool_size);
        }
#endif

        SgAsmRegisterNames* pointer{static_cast<SgAsmRegisterNames*>(ROSE_MALLOC(SgAsmRegisterNames::pool_size * sizeof(SgAsmRegisterNames)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmRegisterNames::pool_size * sizeof(SgAsmRegisterNames));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmRegisterNames::pool_size * sizeof(SgAsmRegisterNames));
#endif
        SgAsmRegisterNames::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmRegisterNames::pool_size * sizeof(SgAsmRegisterNames), V_SgAsmRegisterNames));

        if (SgAsmRegisterNames::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmRegisterNames* blkptr{reinterpret_cast<SgAsmRegisterNames*>(SgAsmRegisterNames::pools[blockIndex-1])};
                blkptr[SgAsmRegisterNames::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmRegisterNames::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmRegisterNames::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmRegisterNames::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmRegisterNames::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmRegisterNames::pool_size - 1};
    SgAsmRegisterNames* testPointer{reinterpret_cast<SgAsmRegisterNames*>(SgAsmRegisterNames::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmRegisterNames::pool_size * (SgAsmRegisterNames::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmRegisterNamesStroageClass
 * from its counterpart of SgAsmRegisterNames. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmRegisterNames::initializeStorageClassArray(SgAsmRegisterNamesStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmRegisterNames::pools.begin(); block != SgAsmRegisterNames::pools.end(); ++block) {
        SgAsmRegisterNames* pointer{reinterpret_cast<SgAsmRegisterNames*>(*block)};
        for (unsigned i = 0; i < SgAsmRegisterNames::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 227992 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmRegisterReferenceExpression that takes its corresponding StorageClass as parameter
 */
SgAsmRegisterReferenceExpression::SgAsmRegisterReferenceExpression(const SgAsmRegisterReferenceExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 228002 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_descriptor = storageSource.storageOf_descriptor;
    p_adjustment = storageSource.storageOf_adjustment;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmRegisterReferenceExpression*
SgAsmRegisterReferenceExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmRegisterReferenceExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRegisterReferenceExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRegisterReferenceExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRegisterReferenceExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterReferenceExpression)};
        unsigned long positionInPool{localIndex % SgAsmRegisterReferenceExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmRegisterReferenceExpression::pool_size};
        returnPointer = &(((SgAsmRegisterReferenceExpression*)(SgAsmRegisterReferenceExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmRegisterReferenceExpression*
SgAsmRegisterReferenceExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmRegisterReferenceExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRegisterReferenceExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRegisterReferenceExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRegisterReferenceExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmRegisterReferenceExpression)};
        unsigned long positionInPool{localIndex % SgAsmRegisterReferenceExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmRegisterReferenceExpression::pool_size};
        returnPointer = &(((SgAsmRegisterReferenceExpression*)(SgAsmRegisterReferenceExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmRegisterReferenceExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmRegisterReferenceExpression::pools.begin(); block != SgAsmRegisterReferenceExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmRegisterReferenceExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmRegisterReferenceExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmRegisterReferenceExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmRegisterReferenceExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmRegisterReferenceExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmRegisterReferenceExpression::pools.begin(); block != SgAsmRegisterReferenceExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmRegisterReferenceExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmRegisterReferenceExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmRegisterReferenceExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmRegisterReferenceExpression::clearMemoryPool() {
    SgAsmRegisterReferenceExpression* tempPointer{nullptr};
    if (SgAsmRegisterReferenceExpression::pools.empty() == false) {
        auto block{SgAsmRegisterReferenceExpression::pools.begin()};
        SgAsmRegisterReferenceExpression::next_node = reinterpret_cast<SgAsmRegisterReferenceExpression*>(*block);
        for (; block != SgAsmRegisterReferenceExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmRegisterReferenceExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmRegisterReferenceExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmRegisterReferenceExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmRegisterReferenceExpression::pool_size-1]);
        }
    }
}

void
SgAsmRegisterReferenceExpression::deleteMemoryPool() {
    for (auto p: SgAsmRegisterReferenceExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmRegisterReferenceExpression::next_node = nullptr;
    SgAsmRegisterReferenceExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmRegisterReferenceExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterReferenceExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRegisterReferenceExpression)};
    for (size_t blockIndex = SgAsmRegisterReferenceExpression::pools.size(); (blockIndex * SgAsmRegisterReferenceExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterReferenceExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRegisterReferenceExpression) = %" PRIuPTR " SgAsmRegisterReferenceExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRegisterReferenceExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRegisterReferenceExpression),SgAsmRegisterReferenceExpression::pool_size);
        }
#endif

        SgAsmRegisterReferenceExpression* pointer{static_cast<SgAsmRegisterReferenceExpression*>(ROSE_MALLOC(SgAsmRegisterReferenceExpression::pool_size * sizeof(SgAsmRegisterReferenceExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmRegisterReferenceExpression::pool_size * sizeof(SgAsmRegisterReferenceExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmRegisterReferenceExpression::pool_size * sizeof(SgAsmRegisterReferenceExpression));
#endif
        SgAsmRegisterReferenceExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmRegisterReferenceExpression::pool_size * sizeof(SgAsmRegisterReferenceExpression), V_SgAsmRegisterReferenceExpression));

        if (SgAsmRegisterReferenceExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmRegisterReferenceExpression* blkptr{reinterpret_cast<SgAsmRegisterReferenceExpression*>(SgAsmRegisterReferenceExpression::pools[blockIndex-1])};
                blkptr[SgAsmRegisterReferenceExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmRegisterReferenceExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmRegisterReferenceExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmRegisterReferenceExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmRegisterReferenceExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmRegisterReferenceExpression::pool_size - 1};
    SgAsmRegisterReferenceExpression* testPointer{reinterpret_cast<SgAsmRegisterReferenceExpression*>(SgAsmRegisterReferenceExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmRegisterReferenceExpression::pool_size * (SgAsmRegisterReferenceExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmRegisterReferenceExpressionStroageClass
 * from its counterpart of SgAsmRegisterReferenceExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmRegisterReferenceExpression::initializeStorageClassArray(SgAsmRegisterReferenceExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmRegisterReferenceExpression::pools.begin(); block != SgAsmRegisterReferenceExpression::pools.end(); ++block) {
        SgAsmRegisterReferenceExpression* pointer{reinterpret_cast<SgAsmRegisterReferenceExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmRegisterReferenceExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 228221 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmDirectRegisterExpression that takes its corresponding StorageClass as parameter
 */
SgAsmDirectRegisterExpression::SgAsmDirectRegisterExpression(const SgAsmDirectRegisterExpressionStorageClass& storageSource)  : SgAsmRegisterReferenceExpression (storageSource) {

/* #line 228231 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmDirectRegisterExpression*
SgAsmDirectRegisterExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmDirectRegisterExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDirectRegisterExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDirectRegisterExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmDirectRegisterExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDirectRegisterExpression)};
        unsigned long positionInPool{localIndex % SgAsmDirectRegisterExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDirectRegisterExpression::pool_size};
        returnPointer = &(((SgAsmDirectRegisterExpression*)(SgAsmDirectRegisterExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmDirectRegisterExpression*
SgAsmDirectRegisterExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmDirectRegisterExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDirectRegisterExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDirectRegisterExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmDirectRegisterExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmDirectRegisterExpression)};
        unsigned long positionInPool{localIndex % SgAsmDirectRegisterExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmDirectRegisterExpression::pool_size};
        returnPointer = &(((SgAsmDirectRegisterExpression*)(SgAsmDirectRegisterExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmDirectRegisterExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmDirectRegisterExpression::pools.begin(); block != SgAsmDirectRegisterExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDirectRegisterExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmDirectRegisterExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmDirectRegisterExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmDirectRegisterExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmDirectRegisterExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmDirectRegisterExpression::pools.begin(); block != SgAsmDirectRegisterExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmDirectRegisterExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmDirectRegisterExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmDirectRegisterExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmDirectRegisterExpression::clearMemoryPool() {
    SgAsmDirectRegisterExpression* tempPointer{nullptr};
    if (SgAsmDirectRegisterExpression::pools.empty() == false) {
        auto block{SgAsmDirectRegisterExpression::pools.begin()};
        SgAsmDirectRegisterExpression::next_node = reinterpret_cast<SgAsmDirectRegisterExpression*>(*block);
        for (; block != SgAsmDirectRegisterExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmDirectRegisterExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmDirectRegisterExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmDirectRegisterExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmDirectRegisterExpression::pool_size-1]);
        }
    }
}

void
SgAsmDirectRegisterExpression::deleteMemoryPool() {
    for (auto p: SgAsmDirectRegisterExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmDirectRegisterExpression::next_node = nullptr;
    SgAsmDirectRegisterExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmDirectRegisterExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDirectRegisterExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDirectRegisterExpression)};
    for (size_t blockIndex = SgAsmDirectRegisterExpression::pools.size(); (blockIndex * SgAsmDirectRegisterExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDirectRegisterExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDirectRegisterExpression) = %" PRIuPTR " SgAsmDirectRegisterExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmDirectRegisterExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmDirectRegisterExpression),SgAsmDirectRegisterExpression::pool_size);
        }
#endif

        SgAsmDirectRegisterExpression* pointer{static_cast<SgAsmDirectRegisterExpression*>(ROSE_MALLOC(SgAsmDirectRegisterExpression::pool_size * sizeof(SgAsmDirectRegisterExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmDirectRegisterExpression::pool_size * sizeof(SgAsmDirectRegisterExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmDirectRegisterExpression::pool_size * sizeof(SgAsmDirectRegisterExpression));
#endif
        SgAsmDirectRegisterExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmDirectRegisterExpression::pool_size * sizeof(SgAsmDirectRegisterExpression), V_SgAsmDirectRegisterExpression));

        if (SgAsmDirectRegisterExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmDirectRegisterExpression* blkptr{reinterpret_cast<SgAsmDirectRegisterExpression*>(SgAsmDirectRegisterExpression::pools[blockIndex-1])};
                blkptr[SgAsmDirectRegisterExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmDirectRegisterExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmDirectRegisterExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmDirectRegisterExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmDirectRegisterExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmDirectRegisterExpression::pool_size - 1};
    SgAsmDirectRegisterExpression* testPointer{reinterpret_cast<SgAsmDirectRegisterExpression*>(SgAsmDirectRegisterExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmDirectRegisterExpression::pool_size * (SgAsmDirectRegisterExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmDirectRegisterExpressionStroageClass
 * from its counterpart of SgAsmDirectRegisterExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmDirectRegisterExpression::initializeStorageClassArray(SgAsmDirectRegisterExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmDirectRegisterExpression::pools.begin(); block != SgAsmDirectRegisterExpression::pools.end(); ++block) {
        SgAsmDirectRegisterExpression* pointer{reinterpret_cast<SgAsmDirectRegisterExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmDirectRegisterExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 228448 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmIndirectRegisterExpression that takes its corresponding StorageClass as parameter
 */
SgAsmIndirectRegisterExpression::SgAsmIndirectRegisterExpression(const SgAsmIndirectRegisterExpressionStorageClass& storageSource)  : SgAsmRegisterReferenceExpression (storageSource) {

/* #line 228458 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_stride = storageSource.storageOf_stride;
    p_offset = storageSource.storageOf_offset;
    p_index = storageSource.storageOf_index;
    p_modulus = storageSource.storageOf_modulus;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmIndirectRegisterExpression*
SgAsmIndirectRegisterExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmIndirectRegisterExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIndirectRegisterExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIndirectRegisterExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIndirectRegisterExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIndirectRegisterExpression)};
        unsigned long positionInPool{localIndex % SgAsmIndirectRegisterExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmIndirectRegisterExpression::pool_size};
        returnPointer = &(((SgAsmIndirectRegisterExpression*)(SgAsmIndirectRegisterExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmIndirectRegisterExpression*
SgAsmIndirectRegisterExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmIndirectRegisterExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIndirectRegisterExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIndirectRegisterExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIndirectRegisterExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmIndirectRegisterExpression)};
        unsigned long positionInPool{localIndex % SgAsmIndirectRegisterExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmIndirectRegisterExpression::pool_size};
        returnPointer = &(((SgAsmIndirectRegisterExpression*)(SgAsmIndirectRegisterExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmIndirectRegisterExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmIndirectRegisterExpression::pools.begin(); block != SgAsmIndirectRegisterExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmIndirectRegisterExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmIndirectRegisterExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmIndirectRegisterExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmIndirectRegisterExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmIndirectRegisterExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmIndirectRegisterExpression::pools.begin(); block != SgAsmIndirectRegisterExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmIndirectRegisterExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmIndirectRegisterExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmIndirectRegisterExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmIndirectRegisterExpression::clearMemoryPool() {
    SgAsmIndirectRegisterExpression* tempPointer{nullptr};
    if (SgAsmIndirectRegisterExpression::pools.empty() == false) {
        auto block{SgAsmIndirectRegisterExpression::pools.begin()};
        SgAsmIndirectRegisterExpression::next_node = reinterpret_cast<SgAsmIndirectRegisterExpression*>(*block);
        for (; block != SgAsmIndirectRegisterExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmIndirectRegisterExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmIndirectRegisterExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmIndirectRegisterExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmIndirectRegisterExpression::pool_size-1]);
        }
    }
}

void
SgAsmIndirectRegisterExpression::deleteMemoryPool() {
    for (auto p: SgAsmIndirectRegisterExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmIndirectRegisterExpression::next_node = nullptr;
    SgAsmIndirectRegisterExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmIndirectRegisterExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIndirectRegisterExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIndirectRegisterExpression)};
    for (size_t blockIndex = SgAsmIndirectRegisterExpression::pools.size(); (blockIndex * SgAsmIndirectRegisterExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIndirectRegisterExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIndirectRegisterExpression) = %" PRIuPTR " SgAsmIndirectRegisterExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIndirectRegisterExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIndirectRegisterExpression),SgAsmIndirectRegisterExpression::pool_size);
        }
#endif

        SgAsmIndirectRegisterExpression* pointer{static_cast<SgAsmIndirectRegisterExpression*>(ROSE_MALLOC(SgAsmIndirectRegisterExpression::pool_size * sizeof(SgAsmIndirectRegisterExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmIndirectRegisterExpression::pool_size * sizeof(SgAsmIndirectRegisterExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmIndirectRegisterExpression::pool_size * sizeof(SgAsmIndirectRegisterExpression));
#endif
        SgAsmIndirectRegisterExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmIndirectRegisterExpression::pool_size * sizeof(SgAsmIndirectRegisterExpression), V_SgAsmIndirectRegisterExpression));

        if (SgAsmIndirectRegisterExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmIndirectRegisterExpression* blkptr{reinterpret_cast<SgAsmIndirectRegisterExpression*>(SgAsmIndirectRegisterExpression::pools[blockIndex-1])};
                blkptr[SgAsmIndirectRegisterExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmIndirectRegisterExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmIndirectRegisterExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmIndirectRegisterExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmIndirectRegisterExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmIndirectRegisterExpression::pool_size - 1};
    SgAsmIndirectRegisterExpression* testPointer{reinterpret_cast<SgAsmIndirectRegisterExpression*>(SgAsmIndirectRegisterExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmIndirectRegisterExpression::pool_size * (SgAsmIndirectRegisterExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmIndirectRegisterExpressionStroageClass
 * from its counterpart of SgAsmIndirectRegisterExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmIndirectRegisterExpression::initializeStorageClassArray(SgAsmIndirectRegisterExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmIndirectRegisterExpression::pools.begin(); block != SgAsmIndirectRegisterExpression::pools.end(); ++block) {
        SgAsmIndirectRegisterExpression* pointer{reinterpret_cast<SgAsmIndirectRegisterExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmIndirectRegisterExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 228679 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmRiscOperation that takes its corresponding StorageClass as parameter
 */
SgAsmRiscOperation::SgAsmRiscOperation(const SgAsmRiscOperationStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 228689 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_riscOperator = storageSource.storageOf_riscOperator;
     p_operands = (SgAsmExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operands));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmRiscOperation*
SgAsmRiscOperation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmRiscOperation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRiscOperation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRiscOperation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmRiscOperation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRiscOperation)};
        unsigned long positionInPool{localIndex % SgAsmRiscOperation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmRiscOperation::pool_size};
        returnPointer = &(((SgAsmRiscOperation*)(SgAsmRiscOperation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmRiscOperation*
SgAsmRiscOperation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmRiscOperation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRiscOperation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRiscOperation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmRiscOperation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmRiscOperation)};
        unsigned long positionInPool{localIndex % SgAsmRiscOperation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmRiscOperation::pool_size};
        returnPointer = &(((SgAsmRiscOperation*)(SgAsmRiscOperation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmRiscOperation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmRiscOperation::pools.begin(); block != SgAsmRiscOperation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmRiscOperation*>(*block)};
        for (unsigned i = 0; i < SgAsmRiscOperation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmRiscOperation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmRiscOperation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmRiscOperation* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmRiscOperation::pools.begin(); block != SgAsmRiscOperation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmRiscOperation*>(*block)};
        for (unsigned i = 0; i < SgAsmRiscOperation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmRiscOperation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmRiscOperation::clearMemoryPool() {
    SgAsmRiscOperation* tempPointer{nullptr};
    if (SgAsmRiscOperation::pools.empty() == false) {
        auto block{SgAsmRiscOperation::pools.begin()};
        SgAsmRiscOperation::next_node = reinterpret_cast<SgAsmRiscOperation*>(*block);
        for (; block != SgAsmRiscOperation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmRiscOperation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmRiscOperation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmRiscOperation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmRiscOperation::pool_size-1]);
        }
    }
}

void
SgAsmRiscOperation::deleteMemoryPool() {
    for (auto p: SgAsmRiscOperation::pools) {
        ROSE_FREE(p);
    }
    SgAsmRiscOperation::next_node = nullptr;
    SgAsmRiscOperation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmRiscOperation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRiscOperation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRiscOperation)};
    for (size_t blockIndex = SgAsmRiscOperation::pools.size(); (blockIndex * SgAsmRiscOperation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRiscOperation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRiscOperation) = %" PRIuPTR " SgAsmRiscOperation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmRiscOperation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmRiscOperation),SgAsmRiscOperation::pool_size);
        }
#endif

        SgAsmRiscOperation* pointer{static_cast<SgAsmRiscOperation*>(ROSE_MALLOC(SgAsmRiscOperation::pool_size * sizeof(SgAsmRiscOperation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmRiscOperation::pool_size * sizeof(SgAsmRiscOperation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmRiscOperation::pool_size * sizeof(SgAsmRiscOperation));
#endif
        SgAsmRiscOperation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmRiscOperation::pool_size * sizeof(SgAsmRiscOperation), V_SgAsmRiscOperation));

        if (SgAsmRiscOperation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmRiscOperation* blkptr{reinterpret_cast<SgAsmRiscOperation*>(SgAsmRiscOperation::pools[blockIndex-1])};
                blkptr[SgAsmRiscOperation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmRiscOperation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmRiscOperation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmRiscOperation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmRiscOperation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmRiscOperation::pool_size - 1};
    SgAsmRiscOperation* testPointer{reinterpret_cast<SgAsmRiscOperation*>(SgAsmRiscOperation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmRiscOperation::pool_size * (SgAsmRiscOperation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmRiscOperationStroageClass
 * from its counterpart of SgAsmRiscOperation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmRiscOperation::initializeStorageClassArray(SgAsmRiscOperationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmRiscOperation::pools.begin(); block != SgAsmRiscOperation::pools.end(); ++block) {
        SgAsmRiscOperation* pointer{reinterpret_cast<SgAsmRiscOperation*>(*block)};
        for (unsigned i = 0; i < SgAsmRiscOperation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 228908 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmStackExpression that takes its corresponding StorageClass as parameter
 */
SgAsmStackExpression::SgAsmStackExpression(const SgAsmStackExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 228918 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_stackPosition = storageSource.storageOf_stackPosition;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmStackExpression*
SgAsmStackExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmStackExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStackExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStackExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStackExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStackExpression)};
        unsigned long positionInPool{localIndex % SgAsmStackExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStackExpression::pool_size};
        returnPointer = &(((SgAsmStackExpression*)(SgAsmStackExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmStackExpression*
SgAsmStackExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmStackExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStackExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStackExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStackExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmStackExpression)};
        unsigned long positionInPool{localIndex % SgAsmStackExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStackExpression::pool_size};
        returnPointer = &(((SgAsmStackExpression*)(SgAsmStackExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmStackExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmStackExpression::pools.begin(); block != SgAsmStackExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStackExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmStackExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmStackExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmStackExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmStackExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmStackExpression::pools.begin(); block != SgAsmStackExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStackExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmStackExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmStackExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmStackExpression::clearMemoryPool() {
    SgAsmStackExpression* tempPointer{nullptr};
    if (SgAsmStackExpression::pools.empty() == false) {
        auto block{SgAsmStackExpression::pools.begin()};
        SgAsmStackExpression::next_node = reinterpret_cast<SgAsmStackExpression*>(*block);
        for (; block != SgAsmStackExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmStackExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmStackExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmStackExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmStackExpression::pool_size-1]);
        }
    }
}

void
SgAsmStackExpression::deleteMemoryPool() {
    for (auto p: SgAsmStackExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmStackExpression::next_node = nullptr;
    SgAsmStackExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmStackExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStackExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStackExpression)};
    for (size_t blockIndex = SgAsmStackExpression::pools.size(); (blockIndex * SgAsmStackExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStackExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStackExpression) = %" PRIuPTR " SgAsmStackExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStackExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStackExpression),SgAsmStackExpression::pool_size);
        }
#endif

        SgAsmStackExpression* pointer{static_cast<SgAsmStackExpression*>(ROSE_MALLOC(SgAsmStackExpression::pool_size * sizeof(SgAsmStackExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmStackExpression::pool_size * sizeof(SgAsmStackExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmStackExpression::pool_size * sizeof(SgAsmStackExpression));
#endif
        SgAsmStackExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmStackExpression::pool_size * sizeof(SgAsmStackExpression), V_SgAsmStackExpression));

        if (SgAsmStackExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmStackExpression* blkptr{reinterpret_cast<SgAsmStackExpression*>(SgAsmStackExpression::pools[blockIndex-1])};
                blkptr[SgAsmStackExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmStackExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmStackExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmStackExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmStackExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmStackExpression::pool_size - 1};
    SgAsmStackExpression* testPointer{reinterpret_cast<SgAsmStackExpression*>(SgAsmStackExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmStackExpression::pool_size * (SgAsmStackExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmStackExpressionStroageClass
 * from its counterpart of SgAsmStackExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmStackExpression::initializeStorageClassArray(SgAsmStackExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmStackExpression::pools.begin(); block != SgAsmStackExpression::pools.end(); ++block) {
        SgAsmStackExpression* pointer{reinterpret_cast<SgAsmStackExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmStackExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 229136 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUnaryExpression that takes its corresponding StorageClass as parameter
 */
SgAsmUnaryExpression::SgAsmUnaryExpression(const SgAsmUnaryExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 229146 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_operand = (SgAsmExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operand));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUnaryExpression*
SgAsmUnaryExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUnaryExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryExpression)};
        unsigned long positionInPool{localIndex % SgAsmUnaryExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryExpression::pool_size};
        returnPointer = &(((SgAsmUnaryExpression*)(SgAsmUnaryExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUnaryExpression*
SgAsmUnaryExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUnaryExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUnaryExpression)};
        unsigned long positionInPool{localIndex % SgAsmUnaryExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryExpression::pool_size};
        returnPointer = &(((SgAsmUnaryExpression*)(SgAsmUnaryExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUnaryExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUnaryExpression::pools.begin(); block != SgAsmUnaryExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUnaryExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUnaryExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUnaryExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUnaryExpression::pools.begin(); block != SgAsmUnaryExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUnaryExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUnaryExpression::clearMemoryPool() {
    SgAsmUnaryExpression* tempPointer{nullptr};
    if (SgAsmUnaryExpression::pools.empty() == false) {
        auto block{SgAsmUnaryExpression::pools.begin()};
        SgAsmUnaryExpression::next_node = reinterpret_cast<SgAsmUnaryExpression*>(*block);
        for (; block != SgAsmUnaryExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUnaryExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUnaryExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUnaryExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUnaryExpression::pool_size-1]);
        }
    }
}

void
SgAsmUnaryExpression::deleteMemoryPool() {
    for (auto p: SgAsmUnaryExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmUnaryExpression::next_node = nullptr;
    SgAsmUnaryExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUnaryExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryExpression)};
    for (size_t blockIndex = SgAsmUnaryExpression::pools.size(); (blockIndex * SgAsmUnaryExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryExpression) = %" PRIuPTR " SgAsmUnaryExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryExpression),SgAsmUnaryExpression::pool_size);
        }
#endif

        SgAsmUnaryExpression* pointer{static_cast<SgAsmUnaryExpression*>(ROSE_MALLOC(SgAsmUnaryExpression::pool_size * sizeof(SgAsmUnaryExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUnaryExpression::pool_size * sizeof(SgAsmUnaryExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUnaryExpression::pool_size * sizeof(SgAsmUnaryExpression));
#endif
        SgAsmUnaryExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUnaryExpression::pool_size * sizeof(SgAsmUnaryExpression), V_SgAsmUnaryExpression));

        if (SgAsmUnaryExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUnaryExpression* blkptr{reinterpret_cast<SgAsmUnaryExpression*>(SgAsmUnaryExpression::pools[blockIndex-1])};
                blkptr[SgAsmUnaryExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUnaryExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUnaryExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUnaryExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUnaryExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUnaryExpression::pool_size - 1};
    SgAsmUnaryExpression* testPointer{reinterpret_cast<SgAsmUnaryExpression*>(SgAsmUnaryExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUnaryExpression::pool_size * (SgAsmUnaryExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUnaryExpressionStroageClass
 * from its counterpart of SgAsmUnaryExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUnaryExpression::initializeStorageClassArray(SgAsmUnaryExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUnaryExpression::pools.begin(); block != SgAsmUnaryExpression::pools.end(); ++block) {
        SgAsmUnaryExpression* pointer{reinterpret_cast<SgAsmUnaryExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 229364 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUnaryMinus that takes its corresponding StorageClass as parameter
 */
SgAsmUnaryMinus::SgAsmUnaryMinus(const SgAsmUnaryMinusStorageClass& storageSource)  : SgAsmUnaryExpression (storageSource) {

/* #line 229374 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUnaryMinus*
SgAsmUnaryMinus::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUnaryMinus* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryMinus)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryMinus + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryMinus)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryMinus)};
        unsigned long positionInPool{localIndex % SgAsmUnaryMinus::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryMinus::pool_size};
        returnPointer = &(((SgAsmUnaryMinus*)(SgAsmUnaryMinus::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUnaryMinus*
SgAsmUnaryMinus::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUnaryMinus* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryMinus)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryMinus + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryMinus)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUnaryMinus)};
        unsigned long positionInPool{localIndex % SgAsmUnaryMinus::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryMinus::pool_size};
        returnPointer = &(((SgAsmUnaryMinus*)(SgAsmUnaryMinus::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUnaryMinus::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUnaryMinus::pools.begin(); block != SgAsmUnaryMinus::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryMinus*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryMinus::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUnaryMinus*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUnaryMinus::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUnaryMinus* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUnaryMinus::pools.begin(); block != SgAsmUnaryMinus::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryMinus*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryMinus::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUnaryMinus::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUnaryMinus::clearMemoryPool() {
    SgAsmUnaryMinus* tempPointer{nullptr};
    if (SgAsmUnaryMinus::pools.empty() == false) {
        auto block{SgAsmUnaryMinus::pools.begin()};
        SgAsmUnaryMinus::next_node = reinterpret_cast<SgAsmUnaryMinus*>(*block);
        for (; block != SgAsmUnaryMinus::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUnaryMinus*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUnaryMinus::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUnaryMinus::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUnaryMinus::pool_size-1]);
        }
    }
}

void
SgAsmUnaryMinus::deleteMemoryPool() {
    for (auto p: SgAsmUnaryMinus::pools) {
        ROSE_FREE(p);
    }
    SgAsmUnaryMinus::next_node = nullptr;
    SgAsmUnaryMinus::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUnaryMinus::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryMinus) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryMinus)};
    for (size_t blockIndex = SgAsmUnaryMinus::pools.size(); (blockIndex * SgAsmUnaryMinus::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryMinus) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryMinus) = %" PRIuPTR " SgAsmUnaryMinus::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryMinus),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryMinus),SgAsmUnaryMinus::pool_size);
        }
#endif

        SgAsmUnaryMinus* pointer{static_cast<SgAsmUnaryMinus*>(ROSE_MALLOC(SgAsmUnaryMinus::pool_size * sizeof(SgAsmUnaryMinus)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUnaryMinus::pool_size * sizeof(SgAsmUnaryMinus));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUnaryMinus::pool_size * sizeof(SgAsmUnaryMinus));
#endif
        SgAsmUnaryMinus::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUnaryMinus::pool_size * sizeof(SgAsmUnaryMinus), V_SgAsmUnaryMinus));

        if (SgAsmUnaryMinus::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUnaryMinus* blkptr{reinterpret_cast<SgAsmUnaryMinus*>(SgAsmUnaryMinus::pools[blockIndex-1])};
                blkptr[SgAsmUnaryMinus::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUnaryMinus::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUnaryMinus::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUnaryMinus::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUnaryMinus::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUnaryMinus::pool_size - 1};
    SgAsmUnaryMinus* testPointer{reinterpret_cast<SgAsmUnaryMinus*>(SgAsmUnaryMinus::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUnaryMinus::pool_size * (SgAsmUnaryMinus::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUnaryMinusStroageClass
 * from its counterpart of SgAsmUnaryMinus. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUnaryMinus::initializeStorageClassArray(SgAsmUnaryMinusStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUnaryMinus::pools.begin(); block != SgAsmUnaryMinus::pools.end(); ++block) {
        SgAsmUnaryMinus* pointer{reinterpret_cast<SgAsmUnaryMinus*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryMinus::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 229591 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUnaryPlus that takes its corresponding StorageClass as parameter
 */
SgAsmUnaryPlus::SgAsmUnaryPlus(const SgAsmUnaryPlusStorageClass& storageSource)  : SgAsmUnaryExpression (storageSource) {

/* #line 229601 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUnaryPlus*
SgAsmUnaryPlus::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUnaryPlus* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryPlus)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryPlus + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryPlus)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryPlus)};
        unsigned long positionInPool{localIndex % SgAsmUnaryPlus::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryPlus::pool_size};
        returnPointer = &(((SgAsmUnaryPlus*)(SgAsmUnaryPlus::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUnaryPlus*
SgAsmUnaryPlus::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUnaryPlus* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryPlus)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryPlus + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryPlus)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUnaryPlus)};
        unsigned long positionInPool{localIndex % SgAsmUnaryPlus::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryPlus::pool_size};
        returnPointer = &(((SgAsmUnaryPlus*)(SgAsmUnaryPlus::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUnaryPlus::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUnaryPlus::pools.begin(); block != SgAsmUnaryPlus::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryPlus*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryPlus::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUnaryPlus*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUnaryPlus::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUnaryPlus* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUnaryPlus::pools.begin(); block != SgAsmUnaryPlus::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryPlus*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryPlus::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUnaryPlus::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUnaryPlus::clearMemoryPool() {
    SgAsmUnaryPlus* tempPointer{nullptr};
    if (SgAsmUnaryPlus::pools.empty() == false) {
        auto block{SgAsmUnaryPlus::pools.begin()};
        SgAsmUnaryPlus::next_node = reinterpret_cast<SgAsmUnaryPlus*>(*block);
        for (; block != SgAsmUnaryPlus::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUnaryPlus*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUnaryPlus::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUnaryPlus::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUnaryPlus::pool_size-1]);
        }
    }
}

void
SgAsmUnaryPlus::deleteMemoryPool() {
    for (auto p: SgAsmUnaryPlus::pools) {
        ROSE_FREE(p);
    }
    SgAsmUnaryPlus::next_node = nullptr;
    SgAsmUnaryPlus::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUnaryPlus::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryPlus) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryPlus)};
    for (size_t blockIndex = SgAsmUnaryPlus::pools.size(); (blockIndex * SgAsmUnaryPlus::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryPlus) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryPlus) = %" PRIuPTR " SgAsmUnaryPlus::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryPlus),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryPlus),SgAsmUnaryPlus::pool_size);
        }
#endif

        SgAsmUnaryPlus* pointer{static_cast<SgAsmUnaryPlus*>(ROSE_MALLOC(SgAsmUnaryPlus::pool_size * sizeof(SgAsmUnaryPlus)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUnaryPlus::pool_size * sizeof(SgAsmUnaryPlus));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUnaryPlus::pool_size * sizeof(SgAsmUnaryPlus));
#endif
        SgAsmUnaryPlus::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUnaryPlus::pool_size * sizeof(SgAsmUnaryPlus), V_SgAsmUnaryPlus));

        if (SgAsmUnaryPlus::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUnaryPlus* blkptr{reinterpret_cast<SgAsmUnaryPlus*>(SgAsmUnaryPlus::pools[blockIndex-1])};
                blkptr[SgAsmUnaryPlus::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUnaryPlus::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUnaryPlus::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUnaryPlus::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUnaryPlus::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUnaryPlus::pool_size - 1};
    SgAsmUnaryPlus* testPointer{reinterpret_cast<SgAsmUnaryPlus*>(SgAsmUnaryPlus::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUnaryPlus::pool_size * (SgAsmUnaryPlus::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUnaryPlusStroageClass
 * from its counterpart of SgAsmUnaryPlus. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUnaryPlus::initializeStorageClassArray(SgAsmUnaryPlusStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUnaryPlus::pools.begin(); block != SgAsmUnaryPlus::pools.end(); ++block) {
        SgAsmUnaryPlus* pointer{reinterpret_cast<SgAsmUnaryPlus*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryPlus::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 229818 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUnaryRrx that takes its corresponding StorageClass as parameter
 */
SgAsmUnaryRrx::SgAsmUnaryRrx(const SgAsmUnaryRrxStorageClass& storageSource)  : SgAsmUnaryExpression (storageSource) {

/* #line 229828 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUnaryRrx*
SgAsmUnaryRrx::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUnaryRrx* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryRrx)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryRrx + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryRrx)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryRrx)};
        unsigned long positionInPool{localIndex % SgAsmUnaryRrx::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryRrx::pool_size};
        returnPointer = &(((SgAsmUnaryRrx*)(SgAsmUnaryRrx::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUnaryRrx*
SgAsmUnaryRrx::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUnaryRrx* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryRrx)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryRrx + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryRrx)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUnaryRrx)};
        unsigned long positionInPool{localIndex % SgAsmUnaryRrx::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryRrx::pool_size};
        returnPointer = &(((SgAsmUnaryRrx*)(SgAsmUnaryRrx::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUnaryRrx::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUnaryRrx::pools.begin(); block != SgAsmUnaryRrx::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryRrx*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryRrx::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUnaryRrx*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUnaryRrx::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUnaryRrx* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUnaryRrx::pools.begin(); block != SgAsmUnaryRrx::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryRrx*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryRrx::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUnaryRrx::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUnaryRrx::clearMemoryPool() {
    SgAsmUnaryRrx* tempPointer{nullptr};
    if (SgAsmUnaryRrx::pools.empty() == false) {
        auto block{SgAsmUnaryRrx::pools.begin()};
        SgAsmUnaryRrx::next_node = reinterpret_cast<SgAsmUnaryRrx*>(*block);
        for (; block != SgAsmUnaryRrx::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUnaryRrx*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUnaryRrx::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUnaryRrx::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUnaryRrx::pool_size-1]);
        }
    }
}

void
SgAsmUnaryRrx::deleteMemoryPool() {
    for (auto p: SgAsmUnaryRrx::pools) {
        ROSE_FREE(p);
    }
    SgAsmUnaryRrx::next_node = nullptr;
    SgAsmUnaryRrx::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUnaryRrx::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryRrx) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryRrx)};
    for (size_t blockIndex = SgAsmUnaryRrx::pools.size(); (blockIndex * SgAsmUnaryRrx::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryRrx) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryRrx) = %" PRIuPTR " SgAsmUnaryRrx::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryRrx),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryRrx),SgAsmUnaryRrx::pool_size);
        }
#endif

        SgAsmUnaryRrx* pointer{static_cast<SgAsmUnaryRrx*>(ROSE_MALLOC(SgAsmUnaryRrx::pool_size * sizeof(SgAsmUnaryRrx)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUnaryRrx::pool_size * sizeof(SgAsmUnaryRrx));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUnaryRrx::pool_size * sizeof(SgAsmUnaryRrx));
#endif
        SgAsmUnaryRrx::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUnaryRrx::pool_size * sizeof(SgAsmUnaryRrx), V_SgAsmUnaryRrx));

        if (SgAsmUnaryRrx::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUnaryRrx* blkptr{reinterpret_cast<SgAsmUnaryRrx*>(SgAsmUnaryRrx::pools[blockIndex-1])};
                blkptr[SgAsmUnaryRrx::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUnaryRrx::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUnaryRrx::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUnaryRrx::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUnaryRrx::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUnaryRrx::pool_size - 1};
    SgAsmUnaryRrx* testPointer{reinterpret_cast<SgAsmUnaryRrx*>(SgAsmUnaryRrx::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUnaryRrx::pool_size * (SgAsmUnaryRrx::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUnaryRrxStroageClass
 * from its counterpart of SgAsmUnaryRrx. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUnaryRrx::initializeStorageClassArray(SgAsmUnaryRrxStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUnaryRrx::pools.begin(); block != SgAsmUnaryRrx::pools.end(); ++block) {
        SgAsmUnaryRrx* pointer{reinterpret_cast<SgAsmUnaryRrx*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryRrx::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 230045 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUnarySignedExtend that takes its corresponding StorageClass as parameter
 */
SgAsmUnarySignedExtend::SgAsmUnarySignedExtend(const SgAsmUnarySignedExtendStorageClass& storageSource)  : SgAsmUnaryExpression (storageSource) {

/* #line 230055 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUnarySignedExtend*
SgAsmUnarySignedExtend::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUnarySignedExtend* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnarySignedExtend)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnarySignedExtend + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnarySignedExtend)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnarySignedExtend)};
        unsigned long positionInPool{localIndex % SgAsmUnarySignedExtend::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnarySignedExtend::pool_size};
        returnPointer = &(((SgAsmUnarySignedExtend*)(SgAsmUnarySignedExtend::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUnarySignedExtend*
SgAsmUnarySignedExtend::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUnarySignedExtend* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnarySignedExtend)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnarySignedExtend + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnarySignedExtend)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUnarySignedExtend)};
        unsigned long positionInPool{localIndex % SgAsmUnarySignedExtend::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnarySignedExtend::pool_size};
        returnPointer = &(((SgAsmUnarySignedExtend*)(SgAsmUnarySignedExtend::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUnarySignedExtend::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUnarySignedExtend::pools.begin(); block != SgAsmUnarySignedExtend::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnarySignedExtend*>(*block)};
        for (unsigned i = 0; i < SgAsmUnarySignedExtend::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUnarySignedExtend*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUnarySignedExtend::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUnarySignedExtend* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUnarySignedExtend::pools.begin(); block != SgAsmUnarySignedExtend::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnarySignedExtend*>(*block)};
        for (unsigned i = 0; i < SgAsmUnarySignedExtend::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUnarySignedExtend::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUnarySignedExtend::clearMemoryPool() {
    SgAsmUnarySignedExtend* tempPointer{nullptr};
    if (SgAsmUnarySignedExtend::pools.empty() == false) {
        auto block{SgAsmUnarySignedExtend::pools.begin()};
        SgAsmUnarySignedExtend::next_node = reinterpret_cast<SgAsmUnarySignedExtend*>(*block);
        for (; block != SgAsmUnarySignedExtend::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUnarySignedExtend*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUnarySignedExtend::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUnarySignedExtend::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUnarySignedExtend::pool_size-1]);
        }
    }
}

void
SgAsmUnarySignedExtend::deleteMemoryPool() {
    for (auto p: SgAsmUnarySignedExtend::pools) {
        ROSE_FREE(p);
    }
    SgAsmUnarySignedExtend::next_node = nullptr;
    SgAsmUnarySignedExtend::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUnarySignedExtend::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnarySignedExtend) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnarySignedExtend)};
    for (size_t blockIndex = SgAsmUnarySignedExtend::pools.size(); (blockIndex * SgAsmUnarySignedExtend::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnarySignedExtend) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnarySignedExtend) = %" PRIuPTR " SgAsmUnarySignedExtend::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnarySignedExtend),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnarySignedExtend),SgAsmUnarySignedExtend::pool_size);
        }
#endif

        SgAsmUnarySignedExtend* pointer{static_cast<SgAsmUnarySignedExtend*>(ROSE_MALLOC(SgAsmUnarySignedExtend::pool_size * sizeof(SgAsmUnarySignedExtend)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUnarySignedExtend::pool_size * sizeof(SgAsmUnarySignedExtend));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUnarySignedExtend::pool_size * sizeof(SgAsmUnarySignedExtend));
#endif
        SgAsmUnarySignedExtend::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUnarySignedExtend::pool_size * sizeof(SgAsmUnarySignedExtend), V_SgAsmUnarySignedExtend));

        if (SgAsmUnarySignedExtend::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUnarySignedExtend* blkptr{reinterpret_cast<SgAsmUnarySignedExtend*>(SgAsmUnarySignedExtend::pools[blockIndex-1])};
                blkptr[SgAsmUnarySignedExtend::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUnarySignedExtend::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUnarySignedExtend::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUnarySignedExtend::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUnarySignedExtend::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUnarySignedExtend::pool_size - 1};
    SgAsmUnarySignedExtend* testPointer{reinterpret_cast<SgAsmUnarySignedExtend*>(SgAsmUnarySignedExtend::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUnarySignedExtend::pool_size * (SgAsmUnarySignedExtend::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUnarySignedExtendStroageClass
 * from its counterpart of SgAsmUnarySignedExtend. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUnarySignedExtend::initializeStorageClassArray(SgAsmUnarySignedExtendStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUnarySignedExtend::pools.begin(); block != SgAsmUnarySignedExtend::pools.end(); ++block) {
        SgAsmUnarySignedExtend* pointer{reinterpret_cast<SgAsmUnarySignedExtend*>(*block)};
        for (unsigned i = 0; i < SgAsmUnarySignedExtend::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 230272 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUnaryTruncate that takes its corresponding StorageClass as parameter
 */
SgAsmUnaryTruncate::SgAsmUnaryTruncate(const SgAsmUnaryTruncateStorageClass& storageSource)  : SgAsmUnaryExpression (storageSource) {

/* #line 230282 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUnaryTruncate*
SgAsmUnaryTruncate::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUnaryTruncate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryTruncate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryTruncate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryTruncate)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryTruncate)};
        unsigned long positionInPool{localIndex % SgAsmUnaryTruncate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryTruncate::pool_size};
        returnPointer = &(((SgAsmUnaryTruncate*)(SgAsmUnaryTruncate::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUnaryTruncate*
SgAsmUnaryTruncate::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUnaryTruncate* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryTruncate)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryTruncate + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryTruncate)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUnaryTruncate)};
        unsigned long positionInPool{localIndex % SgAsmUnaryTruncate::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryTruncate::pool_size};
        returnPointer = &(((SgAsmUnaryTruncate*)(SgAsmUnaryTruncate::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUnaryTruncate::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUnaryTruncate::pools.begin(); block != SgAsmUnaryTruncate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryTruncate*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryTruncate::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUnaryTruncate*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUnaryTruncate::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUnaryTruncate* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUnaryTruncate::pools.begin(); block != SgAsmUnaryTruncate::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryTruncate*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryTruncate::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUnaryTruncate::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUnaryTruncate::clearMemoryPool() {
    SgAsmUnaryTruncate* tempPointer{nullptr};
    if (SgAsmUnaryTruncate::pools.empty() == false) {
        auto block{SgAsmUnaryTruncate::pools.begin()};
        SgAsmUnaryTruncate::next_node = reinterpret_cast<SgAsmUnaryTruncate*>(*block);
        for (; block != SgAsmUnaryTruncate::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUnaryTruncate*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUnaryTruncate::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUnaryTruncate::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUnaryTruncate::pool_size-1]);
        }
    }
}

void
SgAsmUnaryTruncate::deleteMemoryPool() {
    for (auto p: SgAsmUnaryTruncate::pools) {
        ROSE_FREE(p);
    }
    SgAsmUnaryTruncate::next_node = nullptr;
    SgAsmUnaryTruncate::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUnaryTruncate::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryTruncate) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryTruncate)};
    for (size_t blockIndex = SgAsmUnaryTruncate::pools.size(); (blockIndex * SgAsmUnaryTruncate::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryTruncate) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryTruncate) = %" PRIuPTR " SgAsmUnaryTruncate::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryTruncate),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryTruncate),SgAsmUnaryTruncate::pool_size);
        }
#endif

        SgAsmUnaryTruncate* pointer{static_cast<SgAsmUnaryTruncate*>(ROSE_MALLOC(SgAsmUnaryTruncate::pool_size * sizeof(SgAsmUnaryTruncate)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUnaryTruncate::pool_size * sizeof(SgAsmUnaryTruncate));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUnaryTruncate::pool_size * sizeof(SgAsmUnaryTruncate));
#endif
        SgAsmUnaryTruncate::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUnaryTruncate::pool_size * sizeof(SgAsmUnaryTruncate), V_SgAsmUnaryTruncate));

        if (SgAsmUnaryTruncate::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUnaryTruncate* blkptr{reinterpret_cast<SgAsmUnaryTruncate*>(SgAsmUnaryTruncate::pools[blockIndex-1])};
                blkptr[SgAsmUnaryTruncate::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUnaryTruncate::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUnaryTruncate::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUnaryTruncate::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUnaryTruncate::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUnaryTruncate::pool_size - 1};
    SgAsmUnaryTruncate* testPointer{reinterpret_cast<SgAsmUnaryTruncate*>(SgAsmUnaryTruncate::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUnaryTruncate::pool_size * (SgAsmUnaryTruncate::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUnaryTruncateStroageClass
 * from its counterpart of SgAsmUnaryTruncate. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUnaryTruncate::initializeStorageClassArray(SgAsmUnaryTruncateStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUnaryTruncate::pools.begin(); block != SgAsmUnaryTruncate::pools.end(); ++block) {
        SgAsmUnaryTruncate* pointer{reinterpret_cast<SgAsmUnaryTruncate*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryTruncate::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 230499 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUnaryUnsignedExtend that takes its corresponding StorageClass as parameter
 */
SgAsmUnaryUnsignedExtend::SgAsmUnaryUnsignedExtend(const SgAsmUnaryUnsignedExtendStorageClass& storageSource)  : SgAsmUnaryExpression (storageSource) {

/* #line 230509 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUnaryUnsignedExtend*
SgAsmUnaryUnsignedExtend::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUnaryUnsignedExtend* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryUnsignedExtend)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryUnsignedExtend + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUnaryUnsignedExtend)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryUnsignedExtend)};
        unsigned long positionInPool{localIndex % SgAsmUnaryUnsignedExtend::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryUnsignedExtend::pool_size};
        returnPointer = &(((SgAsmUnaryUnsignedExtend*)(SgAsmUnaryUnsignedExtend::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUnaryUnsignedExtend*
SgAsmUnaryUnsignedExtend::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUnaryUnsignedExtend* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryUnsignedExtend)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryUnsignedExtend + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUnaryUnsignedExtend)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUnaryUnsignedExtend)};
        unsigned long positionInPool{localIndex % SgAsmUnaryUnsignedExtend::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUnaryUnsignedExtend::pool_size};
        returnPointer = &(((SgAsmUnaryUnsignedExtend*)(SgAsmUnaryUnsignedExtend::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUnaryUnsignedExtend::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUnaryUnsignedExtend::pools.begin(); block != SgAsmUnaryUnsignedExtend::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryUnsignedExtend*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryUnsignedExtend::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUnaryUnsignedExtend*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUnaryUnsignedExtend::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUnaryUnsignedExtend* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUnaryUnsignedExtend::pools.begin(); block != SgAsmUnaryUnsignedExtend::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUnaryUnsignedExtend*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryUnsignedExtend::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUnaryUnsignedExtend::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUnaryUnsignedExtend::clearMemoryPool() {
    SgAsmUnaryUnsignedExtend* tempPointer{nullptr};
    if (SgAsmUnaryUnsignedExtend::pools.empty() == false) {
        auto block{SgAsmUnaryUnsignedExtend::pools.begin()};
        SgAsmUnaryUnsignedExtend::next_node = reinterpret_cast<SgAsmUnaryUnsignedExtend*>(*block);
        for (; block != SgAsmUnaryUnsignedExtend::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUnaryUnsignedExtend*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUnaryUnsignedExtend::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUnaryUnsignedExtend::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUnaryUnsignedExtend::pool_size-1]);
        }
    }
}

void
SgAsmUnaryUnsignedExtend::deleteMemoryPool() {
    for (auto p: SgAsmUnaryUnsignedExtend::pools) {
        ROSE_FREE(p);
    }
    SgAsmUnaryUnsignedExtend::next_node = nullptr;
    SgAsmUnaryUnsignedExtend::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUnaryUnsignedExtend::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryUnsignedExtend) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryUnsignedExtend)};
    for (size_t blockIndex = SgAsmUnaryUnsignedExtend::pools.size(); (blockIndex * SgAsmUnaryUnsignedExtend::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryUnsignedExtend) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryUnsignedExtend) = %" PRIuPTR " SgAsmUnaryUnsignedExtend::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUnaryUnsignedExtend),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUnaryUnsignedExtend),SgAsmUnaryUnsignedExtend::pool_size);
        }
#endif

        SgAsmUnaryUnsignedExtend* pointer{static_cast<SgAsmUnaryUnsignedExtend*>(ROSE_MALLOC(SgAsmUnaryUnsignedExtend::pool_size * sizeof(SgAsmUnaryUnsignedExtend)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUnaryUnsignedExtend::pool_size * sizeof(SgAsmUnaryUnsignedExtend));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUnaryUnsignedExtend::pool_size * sizeof(SgAsmUnaryUnsignedExtend));
#endif
        SgAsmUnaryUnsignedExtend::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUnaryUnsignedExtend::pool_size * sizeof(SgAsmUnaryUnsignedExtend), V_SgAsmUnaryUnsignedExtend));

        if (SgAsmUnaryUnsignedExtend::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUnaryUnsignedExtend* blkptr{reinterpret_cast<SgAsmUnaryUnsignedExtend*>(SgAsmUnaryUnsignedExtend::pools[blockIndex-1])};
                blkptr[SgAsmUnaryUnsignedExtend::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUnaryUnsignedExtend::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUnaryUnsignedExtend::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUnaryUnsignedExtend::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUnaryUnsignedExtend::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUnaryUnsignedExtend::pool_size - 1};
    SgAsmUnaryUnsignedExtend* testPointer{reinterpret_cast<SgAsmUnaryUnsignedExtend*>(SgAsmUnaryUnsignedExtend::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUnaryUnsignedExtend::pool_size * (SgAsmUnaryUnsignedExtend::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUnaryUnsignedExtendStroageClass
 * from its counterpart of SgAsmUnaryUnsignedExtend. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUnaryUnsignedExtend::initializeStorageClassArray(SgAsmUnaryUnsignedExtendStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUnaryUnsignedExtend::pools.begin(); block != SgAsmUnaryUnsignedExtend::pools.end(); ++block) {
        SgAsmUnaryUnsignedExtend* pointer{reinterpret_cast<SgAsmUnaryUnsignedExtend*>(*block)};
        for (unsigned i = 0; i < SgAsmUnaryUnsignedExtend::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 230726 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmValueExpression that takes its corresponding StorageClass as parameter
 */
SgAsmValueExpression::SgAsmValueExpression(const SgAsmValueExpressionStorageClass& storageSource)  : SgAsmExpression (storageSource) {

/* #line 230736 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_unfoldedExpression = (SgAsmValueExpression*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_unfoldedExpression));
    p_bitOffset = storageSource.storageOf_bitOffset;
    p_bitSize = storageSource.storageOf_bitSize;
     p_symbol = (SgSymbol*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbol));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmValueExpression*
SgAsmValueExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmValueExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmValueExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmValueExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmValueExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmValueExpression)};
        unsigned long positionInPool{localIndex % SgAsmValueExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmValueExpression::pool_size};
        returnPointer = &(((SgAsmValueExpression*)(SgAsmValueExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmValueExpression*
SgAsmValueExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmValueExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmValueExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmValueExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmValueExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmValueExpression)};
        unsigned long positionInPool{localIndex % SgAsmValueExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmValueExpression::pool_size};
        returnPointer = &(((SgAsmValueExpression*)(SgAsmValueExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmValueExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmValueExpression::pools.begin(); block != SgAsmValueExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmValueExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmValueExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmValueExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmValueExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmValueExpression::pools.begin(); block != SgAsmValueExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmValueExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmValueExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmValueExpression::clearMemoryPool() {
    SgAsmValueExpression* tempPointer{nullptr};
    if (SgAsmValueExpression::pools.empty() == false) {
        auto block{SgAsmValueExpression::pools.begin()};
        SgAsmValueExpression::next_node = reinterpret_cast<SgAsmValueExpression*>(*block);
        for (; block != SgAsmValueExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmValueExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmValueExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmValueExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmValueExpression::pool_size-1]);
        }
    }
}

void
SgAsmValueExpression::deleteMemoryPool() {
    for (auto p: SgAsmValueExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmValueExpression::next_node = nullptr;
    SgAsmValueExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmValueExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmValueExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmValueExpression)};
    for (size_t blockIndex = SgAsmValueExpression::pools.size(); (blockIndex * SgAsmValueExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmValueExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmValueExpression) = %" PRIuPTR " SgAsmValueExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmValueExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmValueExpression),SgAsmValueExpression::pool_size);
        }
#endif

        SgAsmValueExpression* pointer{static_cast<SgAsmValueExpression*>(ROSE_MALLOC(SgAsmValueExpression::pool_size * sizeof(SgAsmValueExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmValueExpression::pool_size * sizeof(SgAsmValueExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmValueExpression::pool_size * sizeof(SgAsmValueExpression));
#endif
        SgAsmValueExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmValueExpression::pool_size * sizeof(SgAsmValueExpression), V_SgAsmValueExpression));

        if (SgAsmValueExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmValueExpression* blkptr{reinterpret_cast<SgAsmValueExpression*>(SgAsmValueExpression::pools[blockIndex-1])};
                blkptr[SgAsmValueExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmValueExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmValueExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmValueExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmValueExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmValueExpression::pool_size - 1};
    SgAsmValueExpression* testPointer{reinterpret_cast<SgAsmValueExpression*>(SgAsmValueExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmValueExpression::pool_size * (SgAsmValueExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmValueExpressionStroageClass
 * from its counterpart of SgAsmValueExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmValueExpression::initializeStorageClassArray(SgAsmValueExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmValueExpression::pools.begin(); block != SgAsmValueExpression::pools.end(); ++block) {
        SgAsmValueExpression* pointer{reinterpret_cast<SgAsmValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmValueExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 230957 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmConstantExpression that takes its corresponding StorageClass as parameter
 */
SgAsmConstantExpression::SgAsmConstantExpression(const SgAsmConstantExpressionStorageClass& storageSource)  : SgAsmValueExpression (storageSource) {

/* #line 230967 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_bitVector = storageSource.storageOf_bitVector.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmConstantExpression*
SgAsmConstantExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmConstantExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmConstantExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmConstantExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmConstantExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmConstantExpression)};
        unsigned long positionInPool{localIndex % SgAsmConstantExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmConstantExpression::pool_size};
        returnPointer = &(((SgAsmConstantExpression*)(SgAsmConstantExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmConstantExpression*
SgAsmConstantExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmConstantExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmConstantExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmConstantExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmConstantExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmConstantExpression)};
        unsigned long positionInPool{localIndex % SgAsmConstantExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmConstantExpression::pool_size};
        returnPointer = &(((SgAsmConstantExpression*)(SgAsmConstantExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmConstantExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmConstantExpression::pools.begin(); block != SgAsmConstantExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmConstantExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmConstantExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmConstantExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmConstantExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmConstantExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmConstantExpression::pools.begin(); block != SgAsmConstantExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmConstantExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmConstantExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmConstantExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmConstantExpression::clearMemoryPool() {
    SgAsmConstantExpression* tempPointer{nullptr};
    if (SgAsmConstantExpression::pools.empty() == false) {
        auto block{SgAsmConstantExpression::pools.begin()};
        SgAsmConstantExpression::next_node = reinterpret_cast<SgAsmConstantExpression*>(*block);
        for (; block != SgAsmConstantExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmConstantExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmConstantExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmConstantExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmConstantExpression::pool_size-1]);
        }
    }
}

void
SgAsmConstantExpression::deleteMemoryPool() {
    for (auto p: SgAsmConstantExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmConstantExpression::next_node = nullptr;
    SgAsmConstantExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmConstantExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmConstantExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmConstantExpression)};
    for (size_t blockIndex = SgAsmConstantExpression::pools.size(); (blockIndex * SgAsmConstantExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmConstantExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmConstantExpression) = %" PRIuPTR " SgAsmConstantExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmConstantExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmConstantExpression),SgAsmConstantExpression::pool_size);
        }
#endif

        SgAsmConstantExpression* pointer{static_cast<SgAsmConstantExpression*>(ROSE_MALLOC(SgAsmConstantExpression::pool_size * sizeof(SgAsmConstantExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmConstantExpression::pool_size * sizeof(SgAsmConstantExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmConstantExpression::pool_size * sizeof(SgAsmConstantExpression));
#endif
        SgAsmConstantExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmConstantExpression::pool_size * sizeof(SgAsmConstantExpression), V_SgAsmConstantExpression));

        if (SgAsmConstantExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmConstantExpression* blkptr{reinterpret_cast<SgAsmConstantExpression*>(SgAsmConstantExpression::pools[blockIndex-1])};
                blkptr[SgAsmConstantExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmConstantExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmConstantExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmConstantExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmConstantExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmConstantExpression::pool_size - 1};
    SgAsmConstantExpression* testPointer{reinterpret_cast<SgAsmConstantExpression*>(SgAsmConstantExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmConstantExpression::pool_size * (SgAsmConstantExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmConstantExpressionStroageClass
 * from its counterpart of SgAsmConstantExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmConstantExpression::initializeStorageClassArray(SgAsmConstantExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmConstantExpression::pools.begin(); block != SgAsmConstantExpression::pools.end(); ++block) {
        SgAsmConstantExpression* pointer{reinterpret_cast<SgAsmConstantExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmConstantExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 231185 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmFloatValueExpression that takes its corresponding StorageClass as parameter
 */
SgAsmFloatValueExpression::SgAsmFloatValueExpression(const SgAsmFloatValueExpressionStorageClass& storageSource)  : SgAsmConstantExpression (storageSource) {

/* #line 231195 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmFloatValueExpression*
SgAsmFloatValueExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmFloatValueExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFloatValueExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFloatValueExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFloatValueExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatValueExpression)};
        unsigned long positionInPool{localIndex % SgAsmFloatValueExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmFloatValueExpression::pool_size};
        returnPointer = &(((SgAsmFloatValueExpression*)(SgAsmFloatValueExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmFloatValueExpression*
SgAsmFloatValueExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmFloatValueExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFloatValueExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFloatValueExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFloatValueExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmFloatValueExpression)};
        unsigned long positionInPool{localIndex % SgAsmFloatValueExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmFloatValueExpression::pool_size};
        returnPointer = &(((SgAsmFloatValueExpression*)(SgAsmFloatValueExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmFloatValueExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmFloatValueExpression::pools.begin(); block != SgAsmFloatValueExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmFloatValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmFloatValueExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmFloatValueExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmFloatValueExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmFloatValueExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmFloatValueExpression::pools.begin(); block != SgAsmFloatValueExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmFloatValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmFloatValueExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmFloatValueExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmFloatValueExpression::clearMemoryPool() {
    SgAsmFloatValueExpression* tempPointer{nullptr};
    if (SgAsmFloatValueExpression::pools.empty() == false) {
        auto block{SgAsmFloatValueExpression::pools.begin()};
        SgAsmFloatValueExpression::next_node = reinterpret_cast<SgAsmFloatValueExpression*>(*block);
        for (; block != SgAsmFloatValueExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmFloatValueExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmFloatValueExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmFloatValueExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmFloatValueExpression::pool_size-1]);
        }
    }
}

void
SgAsmFloatValueExpression::deleteMemoryPool() {
    for (auto p: SgAsmFloatValueExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmFloatValueExpression::next_node = nullptr;
    SgAsmFloatValueExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmFloatValueExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatValueExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFloatValueExpression)};
    for (size_t blockIndex = SgAsmFloatValueExpression::pools.size(); (blockIndex * SgAsmFloatValueExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatValueExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFloatValueExpression) = %" PRIuPTR " SgAsmFloatValueExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatValueExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFloatValueExpression),SgAsmFloatValueExpression::pool_size);
        }
#endif

        SgAsmFloatValueExpression* pointer{static_cast<SgAsmFloatValueExpression*>(ROSE_MALLOC(SgAsmFloatValueExpression::pool_size * sizeof(SgAsmFloatValueExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmFloatValueExpression::pool_size * sizeof(SgAsmFloatValueExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmFloatValueExpression::pool_size * sizeof(SgAsmFloatValueExpression));
#endif
        SgAsmFloatValueExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmFloatValueExpression::pool_size * sizeof(SgAsmFloatValueExpression), V_SgAsmFloatValueExpression));

        if (SgAsmFloatValueExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmFloatValueExpression* blkptr{reinterpret_cast<SgAsmFloatValueExpression*>(SgAsmFloatValueExpression::pools[blockIndex-1])};
                blkptr[SgAsmFloatValueExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmFloatValueExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmFloatValueExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmFloatValueExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmFloatValueExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmFloatValueExpression::pool_size - 1};
    SgAsmFloatValueExpression* testPointer{reinterpret_cast<SgAsmFloatValueExpression*>(SgAsmFloatValueExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmFloatValueExpression::pool_size * (SgAsmFloatValueExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmFloatValueExpressionStroageClass
 * from its counterpart of SgAsmFloatValueExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmFloatValueExpression::initializeStorageClassArray(SgAsmFloatValueExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmFloatValueExpression::pools.begin(); block != SgAsmFloatValueExpression::pools.end(); ++block) {
        SgAsmFloatValueExpression* pointer{reinterpret_cast<SgAsmFloatValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmFloatValueExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 231412 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmIntegerValueExpression that takes its corresponding StorageClass as parameter
 */
SgAsmIntegerValueExpression::SgAsmIntegerValueExpression(const SgAsmIntegerValueExpressionStorageClass& storageSource)  : SgAsmConstantExpression (storageSource) {

/* #line 231422 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_baseNode = (SgNode*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_baseNode));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmIntegerValueExpression*
SgAsmIntegerValueExpression::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmIntegerValueExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIntegerValueExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIntegerValueExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIntegerValueExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerValueExpression)};
        unsigned long positionInPool{localIndex % SgAsmIntegerValueExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmIntegerValueExpression::pool_size};
        returnPointer = &(((SgAsmIntegerValueExpression*)(SgAsmIntegerValueExpression::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmIntegerValueExpression*
SgAsmIntegerValueExpression::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmIntegerValueExpression* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIntegerValueExpression)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIntegerValueExpression + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIntegerValueExpression)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmIntegerValueExpression)};
        unsigned long positionInPool{localIndex % SgAsmIntegerValueExpression::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmIntegerValueExpression::pool_size};
        returnPointer = &(((SgAsmIntegerValueExpression*)(SgAsmIntegerValueExpression::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmIntegerValueExpression::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmIntegerValueExpression::pools.begin(); block != SgAsmIntegerValueExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmIntegerValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmIntegerValueExpression::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmIntegerValueExpression*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmIntegerValueExpression::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmIntegerValueExpression* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmIntegerValueExpression::pools.begin(); block != SgAsmIntegerValueExpression::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmIntegerValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmIntegerValueExpression::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmIntegerValueExpression::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmIntegerValueExpression::clearMemoryPool() {
    SgAsmIntegerValueExpression* tempPointer{nullptr};
    if (SgAsmIntegerValueExpression::pools.empty() == false) {
        auto block{SgAsmIntegerValueExpression::pools.begin()};
        SgAsmIntegerValueExpression::next_node = reinterpret_cast<SgAsmIntegerValueExpression*>(*block);
        for (; block != SgAsmIntegerValueExpression::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmIntegerValueExpression*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmIntegerValueExpression::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmIntegerValueExpression::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmIntegerValueExpression::pool_size-1]);
        }
    }
}

void
SgAsmIntegerValueExpression::deleteMemoryPool() {
    for (auto p: SgAsmIntegerValueExpression::pools) {
        ROSE_FREE(p);
    }
    SgAsmIntegerValueExpression::next_node = nullptr;
    SgAsmIntegerValueExpression::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmIntegerValueExpression::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerValueExpression) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIntegerValueExpression)};
    for (size_t blockIndex = SgAsmIntegerValueExpression::pools.size(); (blockIndex * SgAsmIntegerValueExpression::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerValueExpression) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIntegerValueExpression) = %" PRIuPTR " SgAsmIntegerValueExpression::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerValueExpression),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIntegerValueExpression),SgAsmIntegerValueExpression::pool_size);
        }
#endif

        SgAsmIntegerValueExpression* pointer{static_cast<SgAsmIntegerValueExpression*>(ROSE_MALLOC(SgAsmIntegerValueExpression::pool_size * sizeof(SgAsmIntegerValueExpression)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmIntegerValueExpression::pool_size * sizeof(SgAsmIntegerValueExpression));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmIntegerValueExpression::pool_size * sizeof(SgAsmIntegerValueExpression));
#endif
        SgAsmIntegerValueExpression::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmIntegerValueExpression::pool_size * sizeof(SgAsmIntegerValueExpression), V_SgAsmIntegerValueExpression));

        if (SgAsmIntegerValueExpression::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmIntegerValueExpression* blkptr{reinterpret_cast<SgAsmIntegerValueExpression*>(SgAsmIntegerValueExpression::pools[blockIndex-1])};
                blkptr[SgAsmIntegerValueExpression::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmIntegerValueExpression::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmIntegerValueExpression::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmIntegerValueExpression::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmIntegerValueExpression::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmIntegerValueExpression::pool_size - 1};
    SgAsmIntegerValueExpression* testPointer{reinterpret_cast<SgAsmIntegerValueExpression*>(SgAsmIntegerValueExpression::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmIntegerValueExpression::pool_size * (SgAsmIntegerValueExpression::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmIntegerValueExpressionStroageClass
 * from its counterpart of SgAsmIntegerValueExpression. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmIntegerValueExpression::initializeStorageClassArray(SgAsmIntegerValueExpressionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmIntegerValueExpression::pools.begin(); block != SgAsmIntegerValueExpression::pools.end(); ++block) {
        SgAsmIntegerValueExpression* pointer{reinterpret_cast<SgAsmIntegerValueExpression*>(*block)};
        for (unsigned i = 0; i < SgAsmIntegerValueExpression::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 231640 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmGenericFileList that takes its corresponding StorageClass as parameter
 */
SgAsmGenericFileList::SgAsmGenericFileList(const SgAsmGenericFileListStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 231650 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_files = storageSource.storageOf_files.rebuildDataStoredInEasyStorageClass();
    SgAsmGenericFilePtrList::iterator i_files = p_files.begin();
    for ( ; i_files != p_files.end(); ++i_files) {
        (*i_files) = (SgAsmGenericFilePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_files)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmGenericFileList*
SgAsmGenericFileList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmGenericFileList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFileList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFileList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmGenericFileList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFileList)};
        unsigned long positionInPool{localIndex % SgAsmGenericFileList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericFileList::pool_size};
        returnPointer = &(((SgAsmGenericFileList*)(SgAsmGenericFileList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmGenericFileList*
SgAsmGenericFileList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmGenericFileList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFileList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFileList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmGenericFileList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmGenericFileList)};
        unsigned long positionInPool{localIndex % SgAsmGenericFileList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmGenericFileList::pool_size};
        returnPointer = &(((SgAsmGenericFileList*)(SgAsmGenericFileList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmGenericFileList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmGenericFileList::pools.begin(); block != SgAsmGenericFileList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericFileList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFileList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmGenericFileList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmGenericFileList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmGenericFileList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmGenericFileList::pools.begin(); block != SgAsmGenericFileList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmGenericFileList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFileList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmGenericFileList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmGenericFileList::clearMemoryPool() {
    SgAsmGenericFileList* tempPointer{nullptr};
    if (SgAsmGenericFileList::pools.empty() == false) {
        auto block{SgAsmGenericFileList::pools.begin()};
        SgAsmGenericFileList::next_node = reinterpret_cast<SgAsmGenericFileList*>(*block);
        for (; block != SgAsmGenericFileList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmGenericFileList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmGenericFileList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmGenericFileList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmGenericFileList::pool_size-1]);
        }
    }
}

void
SgAsmGenericFileList::deleteMemoryPool() {
    for (auto p: SgAsmGenericFileList::pools) {
        ROSE_FREE(p);
    }
    SgAsmGenericFileList::next_node = nullptr;
    SgAsmGenericFileList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmGenericFileList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFileList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFileList)};
    for (size_t blockIndex = SgAsmGenericFileList::pools.size(); (blockIndex * SgAsmGenericFileList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFileList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFileList) = %" PRIuPTR " SgAsmGenericFileList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmGenericFileList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmGenericFileList),SgAsmGenericFileList::pool_size);
        }
#endif

        SgAsmGenericFileList* pointer{static_cast<SgAsmGenericFileList*>(ROSE_MALLOC(SgAsmGenericFileList::pool_size * sizeof(SgAsmGenericFileList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmGenericFileList::pool_size * sizeof(SgAsmGenericFileList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmGenericFileList::pool_size * sizeof(SgAsmGenericFileList));
#endif
        SgAsmGenericFileList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmGenericFileList::pool_size * sizeof(SgAsmGenericFileList), V_SgAsmGenericFileList));

        if (SgAsmGenericFileList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmGenericFileList* blkptr{reinterpret_cast<SgAsmGenericFileList*>(SgAsmGenericFileList::pools[blockIndex-1])};
                blkptr[SgAsmGenericFileList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmGenericFileList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmGenericFileList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmGenericFileList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmGenericFileList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmGenericFileList::pool_size - 1};
    SgAsmGenericFileList* testPointer{reinterpret_cast<SgAsmGenericFileList*>(SgAsmGenericFileList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmGenericFileList::pool_size * (SgAsmGenericFileList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmGenericFileListStroageClass
 * from its counterpart of SgAsmGenericFileList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmGenericFileList::initializeStorageClassArray(SgAsmGenericFileListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmGenericFileList::pools.begin(); block != SgAsmGenericFileList::pools.end(); ++block) {
        SgAsmGenericFileList* pointer{reinterpret_cast<SgAsmGenericFileList*>(*block)};
        for (unsigned i = 0; i < SgAsmGenericFileList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 231872 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmInstructionList that takes its corresponding StorageClass as parameter
 */
SgAsmInstructionList::SgAsmInstructionList(const SgAsmInstructionListStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 231882 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_instructions = storageSource.storageOf_instructions.rebuildDataStoredInEasyStorageClass();
    SgAsmInstructionPtrList::iterator i_instructions = p_instructions.begin();
    for ( ; i_instructions != p_instructions.end(); ++i_instructions) {
        (*i_instructions) = (SgAsmInstructionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_instructions)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmInstructionList*
SgAsmInstructionList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmInstructionList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInstructionList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInstructionList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInstructionList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstructionList)};
        unsigned long positionInPool{localIndex % SgAsmInstructionList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInstructionList::pool_size};
        returnPointer = &(((SgAsmInstructionList*)(SgAsmInstructionList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmInstructionList*
SgAsmInstructionList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmInstructionList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInstructionList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInstructionList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInstructionList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmInstructionList)};
        unsigned long positionInPool{localIndex % SgAsmInstructionList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInstructionList::pool_size};
        returnPointer = &(((SgAsmInstructionList*)(SgAsmInstructionList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmInstructionList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmInstructionList::pools.begin(); block != SgAsmInstructionList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInstructionList*>(*block)};
        for (unsigned i = 0; i < SgAsmInstructionList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmInstructionList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmInstructionList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmInstructionList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmInstructionList::pools.begin(); block != SgAsmInstructionList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInstructionList*>(*block)};
        for (unsigned i = 0; i < SgAsmInstructionList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmInstructionList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmInstructionList::clearMemoryPool() {
    SgAsmInstructionList* tempPointer{nullptr};
    if (SgAsmInstructionList::pools.empty() == false) {
        auto block{SgAsmInstructionList::pools.begin()};
        SgAsmInstructionList::next_node = reinterpret_cast<SgAsmInstructionList*>(*block);
        for (; block != SgAsmInstructionList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmInstructionList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmInstructionList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmInstructionList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmInstructionList::pool_size-1]);
        }
    }
}

void
SgAsmInstructionList::deleteMemoryPool() {
    for (auto p: SgAsmInstructionList::pools) {
        ROSE_FREE(p);
    }
    SgAsmInstructionList::next_node = nullptr;
    SgAsmInstructionList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmInstructionList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstructionList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInstructionList)};
    for (size_t blockIndex = SgAsmInstructionList::pools.size(); (blockIndex * SgAsmInstructionList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstructionList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInstructionList) = %" PRIuPTR " SgAsmInstructionList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstructionList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInstructionList),SgAsmInstructionList::pool_size);
        }
#endif

        SgAsmInstructionList* pointer{static_cast<SgAsmInstructionList*>(ROSE_MALLOC(SgAsmInstructionList::pool_size * sizeof(SgAsmInstructionList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmInstructionList::pool_size * sizeof(SgAsmInstructionList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmInstructionList::pool_size * sizeof(SgAsmInstructionList));
#endif
        SgAsmInstructionList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmInstructionList::pool_size * sizeof(SgAsmInstructionList), V_SgAsmInstructionList));

        if (SgAsmInstructionList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmInstructionList* blkptr{reinterpret_cast<SgAsmInstructionList*>(SgAsmInstructionList::pools[blockIndex-1])};
                blkptr[SgAsmInstructionList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmInstructionList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmInstructionList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmInstructionList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmInstructionList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmInstructionList::pool_size - 1};
    SgAsmInstructionList* testPointer{reinterpret_cast<SgAsmInstructionList*>(SgAsmInstructionList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmInstructionList::pool_size * (SgAsmInstructionList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmInstructionListStroageClass
 * from its counterpart of SgAsmInstructionList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmInstructionList::initializeStorageClassArray(SgAsmInstructionListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmInstructionList::pools.begin(); block != SgAsmInstructionList::pools.end(); ++block) {
        SgAsmInstructionList* pointer{reinterpret_cast<SgAsmInstructionList*>(*block)};
        for (unsigned i = 0; i < SgAsmInstructionList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 232104 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmInterpretation that takes its corresponding StorageClass as parameter
 */
SgAsmInterpretation::SgAsmInterpretation(const SgAsmInterpretationStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 232114 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_headers = (SgAsmGenericHeaderList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_headers));
     p_globalBlock = (SgAsmBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_globalBlock));
    p_coverageComputed = storageSource.storageOf_coverageComputed;
    p_percentageCoverage = storageSource.storageOf_percentageCoverage;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmInterpretation*
SgAsmInterpretation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmInterpretation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInterpretation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInterpretation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInterpretation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretation)};
        unsigned long positionInPool{localIndex % SgAsmInterpretation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInterpretation::pool_size};
        returnPointer = &(((SgAsmInterpretation*)(SgAsmInterpretation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmInterpretation*
SgAsmInterpretation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmInterpretation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInterpretation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInterpretation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInterpretation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmInterpretation)};
        unsigned long positionInPool{localIndex % SgAsmInterpretation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInterpretation::pool_size};
        returnPointer = &(((SgAsmInterpretation*)(SgAsmInterpretation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmInterpretation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmInterpretation::pools.begin(); block != SgAsmInterpretation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInterpretation*>(*block)};
        for (unsigned i = 0; i < SgAsmInterpretation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmInterpretation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmInterpretation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmInterpretation* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmInterpretation::pools.begin(); block != SgAsmInterpretation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInterpretation*>(*block)};
        for (unsigned i = 0; i < SgAsmInterpretation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmInterpretation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmInterpretation::clearMemoryPool() {
    SgAsmInterpretation* tempPointer{nullptr};
    if (SgAsmInterpretation::pools.empty() == false) {
        auto block{SgAsmInterpretation::pools.begin()};
        SgAsmInterpretation::next_node = reinterpret_cast<SgAsmInterpretation*>(*block);
        for (; block != SgAsmInterpretation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmInterpretation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmInterpretation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmInterpretation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmInterpretation::pool_size-1]);
        }
    }
}

void
SgAsmInterpretation::deleteMemoryPool() {
    for (auto p: SgAsmInterpretation::pools) {
        ROSE_FREE(p);
    }
    SgAsmInterpretation::next_node = nullptr;
    SgAsmInterpretation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmInterpretation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInterpretation)};
    for (size_t blockIndex = SgAsmInterpretation::pools.size(); (blockIndex * SgAsmInterpretation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInterpretation) = %" PRIuPTR " SgAsmInterpretation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInterpretation),SgAsmInterpretation::pool_size);
        }
#endif

        SgAsmInterpretation* pointer{static_cast<SgAsmInterpretation*>(ROSE_MALLOC(SgAsmInterpretation::pool_size * sizeof(SgAsmInterpretation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmInterpretation::pool_size * sizeof(SgAsmInterpretation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmInterpretation::pool_size * sizeof(SgAsmInterpretation));
#endif
        SgAsmInterpretation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmInterpretation::pool_size * sizeof(SgAsmInterpretation), V_SgAsmInterpretation));

        if (SgAsmInterpretation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmInterpretation* blkptr{reinterpret_cast<SgAsmInterpretation*>(SgAsmInterpretation::pools[blockIndex-1])};
                blkptr[SgAsmInterpretation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmInterpretation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmInterpretation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmInterpretation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmInterpretation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmInterpretation::pool_size - 1};
    SgAsmInterpretation* testPointer{reinterpret_cast<SgAsmInterpretation*>(SgAsmInterpretation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmInterpretation::pool_size * (SgAsmInterpretation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmInterpretationStroageClass
 * from its counterpart of SgAsmInterpretation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmInterpretation::initializeStorageClassArray(SgAsmInterpretationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmInterpretation::pools.begin(); block != SgAsmInterpretation::pools.end(); ++block) {
        SgAsmInterpretation* pointer{reinterpret_cast<SgAsmInterpretation*>(*block)};
        for (unsigned i = 0; i < SgAsmInterpretation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 232335 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmInterpretationList that takes its corresponding StorageClass as parameter
 */
SgAsmInterpretationList::SgAsmInterpretationList(const SgAsmInterpretationListStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 232345 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_interpretations = storageSource.storageOf_interpretations.rebuildDataStoredInEasyStorageClass();
    SgAsmInterpretationPtrList::iterator i_interpretations = p_interpretations.begin();
    for ( ; i_interpretations != p_interpretations.end(); ++i_interpretations) {
        (*i_interpretations) = (SgAsmInterpretationPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_interpretations)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmInterpretationList*
SgAsmInterpretationList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmInterpretationList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInterpretationList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInterpretationList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInterpretationList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretationList)};
        unsigned long positionInPool{localIndex % SgAsmInterpretationList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInterpretationList::pool_size};
        returnPointer = &(((SgAsmInterpretationList*)(SgAsmInterpretationList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmInterpretationList*
SgAsmInterpretationList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmInterpretationList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInterpretationList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInterpretationList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInterpretationList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmInterpretationList)};
        unsigned long positionInPool{localIndex % SgAsmInterpretationList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInterpretationList::pool_size};
        returnPointer = &(((SgAsmInterpretationList*)(SgAsmInterpretationList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmInterpretationList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmInterpretationList::pools.begin(); block != SgAsmInterpretationList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInterpretationList*>(*block)};
        for (unsigned i = 0; i < SgAsmInterpretationList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmInterpretationList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmInterpretationList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmInterpretationList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmInterpretationList::pools.begin(); block != SgAsmInterpretationList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInterpretationList*>(*block)};
        for (unsigned i = 0; i < SgAsmInterpretationList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmInterpretationList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmInterpretationList::clearMemoryPool() {
    SgAsmInterpretationList* tempPointer{nullptr};
    if (SgAsmInterpretationList::pools.empty() == false) {
        auto block{SgAsmInterpretationList::pools.begin()};
        SgAsmInterpretationList::next_node = reinterpret_cast<SgAsmInterpretationList*>(*block);
        for (; block != SgAsmInterpretationList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmInterpretationList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmInterpretationList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmInterpretationList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmInterpretationList::pool_size-1]);
        }
    }
}

void
SgAsmInterpretationList::deleteMemoryPool() {
    for (auto p: SgAsmInterpretationList::pools) {
        ROSE_FREE(p);
    }
    SgAsmInterpretationList::next_node = nullptr;
    SgAsmInterpretationList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmInterpretationList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretationList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInterpretationList)};
    for (size_t blockIndex = SgAsmInterpretationList::pools.size(); (blockIndex * SgAsmInterpretationList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretationList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInterpretationList) = %" PRIuPTR " SgAsmInterpretationList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInterpretationList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInterpretationList),SgAsmInterpretationList::pool_size);
        }
#endif

        SgAsmInterpretationList* pointer{static_cast<SgAsmInterpretationList*>(ROSE_MALLOC(SgAsmInterpretationList::pool_size * sizeof(SgAsmInterpretationList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmInterpretationList::pool_size * sizeof(SgAsmInterpretationList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmInterpretationList::pool_size * sizeof(SgAsmInterpretationList));
#endif
        SgAsmInterpretationList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmInterpretationList::pool_size * sizeof(SgAsmInterpretationList), V_SgAsmInterpretationList));

        if (SgAsmInterpretationList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmInterpretationList* blkptr{reinterpret_cast<SgAsmInterpretationList*>(SgAsmInterpretationList::pools[blockIndex-1])};
                blkptr[SgAsmInterpretationList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmInterpretationList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmInterpretationList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmInterpretationList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmInterpretationList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmInterpretationList::pool_size - 1};
    SgAsmInterpretationList* testPointer{reinterpret_cast<SgAsmInterpretationList*>(SgAsmInterpretationList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmInterpretationList::pool_size * (SgAsmInterpretationList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmInterpretationListStroageClass
 * from its counterpart of SgAsmInterpretationList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmInterpretationList::initializeStorageClassArray(SgAsmInterpretationListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmInterpretationList::pools.begin(); block != SgAsmInterpretationList::pools.end(); ++block) {
        SgAsmInterpretationList* pointer{reinterpret_cast<SgAsmInterpretationList*>(*block)};
        for (unsigned i = 0; i < SgAsmInterpretationList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 232567 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmNode that takes its corresponding StorageClass as parameter
 */
SgAsmJvmNode::SgAsmJvmNode(const SgAsmJvmNodeStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 232577 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmNode*
SgAsmJvmNode::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmNode)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmNode)};
        unsigned long positionInPool{localIndex % SgAsmJvmNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmNode::pool_size};
        returnPointer = &(((SgAsmJvmNode*)(SgAsmJvmNode::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmNode*
SgAsmJvmNode::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmNode* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmNode)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmNode + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmNode)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmNode)};
        unsigned long positionInPool{localIndex % SgAsmJvmNode::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmNode::pool_size};
        returnPointer = &(((SgAsmJvmNode*)(SgAsmJvmNode::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmNode::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmNode::pools.begin(); block != SgAsmJvmNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmNode*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmNode::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmNode*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmNode::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmNode* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmNode::pools.begin(); block != SgAsmJvmNode::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmNode*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmNode::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmNode::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmNode::clearMemoryPool() {
    SgAsmJvmNode* tempPointer{nullptr};
    if (SgAsmJvmNode::pools.empty() == false) {
        auto block{SgAsmJvmNode::pools.begin()};
        SgAsmJvmNode::next_node = reinterpret_cast<SgAsmJvmNode*>(*block);
        for (; block != SgAsmJvmNode::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmNode*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmNode::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmNode::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmNode::pool_size-1]);
        }
    }
}

void
SgAsmJvmNode::deleteMemoryPool() {
    for (auto p: SgAsmJvmNode::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmNode::next_node = nullptr;
    SgAsmJvmNode::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmNode::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmNode) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmNode)};
    for (size_t blockIndex = SgAsmJvmNode::pools.size(); (blockIndex * SgAsmJvmNode::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmNode) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmNode) = %" PRIuPTR " SgAsmJvmNode::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmNode),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmNode),SgAsmJvmNode::pool_size);
        }
#endif

        SgAsmJvmNode* pointer{static_cast<SgAsmJvmNode*>(ROSE_MALLOC(SgAsmJvmNode::pool_size * sizeof(SgAsmJvmNode)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmNode::pool_size * sizeof(SgAsmJvmNode));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmNode::pool_size * sizeof(SgAsmJvmNode));
#endif
        SgAsmJvmNode::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmNode::pool_size * sizeof(SgAsmJvmNode), V_SgAsmJvmNode));

        if (SgAsmJvmNode::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmNode* blkptr{reinterpret_cast<SgAsmJvmNode*>(SgAsmJvmNode::pools[blockIndex-1])};
                blkptr[SgAsmJvmNode::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmNode::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmNode::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmNode::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmNode::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmNode::pool_size - 1};
    SgAsmJvmNode* testPointer{reinterpret_cast<SgAsmJvmNode*>(SgAsmJvmNode::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmNode::pool_size * (SgAsmJvmNode::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmNodeStroageClass
 * from its counterpart of SgAsmJvmNode. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmNode::initializeStorageClassArray(SgAsmJvmNodeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmNode::pools.begin(); block != SgAsmJvmNode::pools.end(); ++block) {
        SgAsmJvmNode* pointer{reinterpret_cast<SgAsmJvmNode*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmNode::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 232794 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmAttribute that takes its corresponding StorageClass as parameter
 */
SgAsmJvmAttribute::SgAsmJvmAttribute(const SgAsmJvmAttributeStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 232804 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_attribute_name_index = storageSource.storageOf_attribute_name_index;
    p_attribute_length = storageSource.storageOf_attribute_length;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmAttribute*
SgAsmJvmAttribute::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttribute)};
        unsigned long positionInPool{localIndex % SgAsmJvmAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmAttribute::pool_size};
        returnPointer = &(((SgAsmJvmAttribute*)(SgAsmJvmAttribute::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmAttribute*
SgAsmJvmAttribute::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmAttribute)};
        unsigned long positionInPool{localIndex % SgAsmJvmAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmAttribute::pool_size};
        returnPointer = &(((SgAsmJvmAttribute*)(SgAsmJvmAttribute::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmAttribute::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmAttribute::pools.begin(); block != SgAsmJvmAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAttribute::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmAttribute*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmAttribute::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmAttribute* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmAttribute::pools.begin(); block != SgAsmJvmAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAttribute::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmAttribute::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmAttribute::clearMemoryPool() {
    SgAsmJvmAttribute* tempPointer{nullptr};
    if (SgAsmJvmAttribute::pools.empty() == false) {
        auto block{SgAsmJvmAttribute::pools.begin()};
        SgAsmJvmAttribute::next_node = reinterpret_cast<SgAsmJvmAttribute*>(*block);
        for (; block != SgAsmJvmAttribute::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmAttribute*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmAttribute::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmAttribute::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmAttribute::pool_size-1]);
        }
    }
}

void
SgAsmJvmAttribute::deleteMemoryPool() {
    for (auto p: SgAsmJvmAttribute::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmAttribute::next_node = nullptr;
    SgAsmJvmAttribute::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmAttribute::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttribute) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAttribute)};
    for (size_t blockIndex = SgAsmJvmAttribute::pools.size(); (blockIndex * SgAsmJvmAttribute::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttribute) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAttribute) = %" PRIuPTR " SgAsmJvmAttribute::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAttribute),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAttribute),SgAsmJvmAttribute::pool_size);
        }
#endif

        SgAsmJvmAttribute* pointer{static_cast<SgAsmJvmAttribute*>(ROSE_MALLOC(SgAsmJvmAttribute::pool_size * sizeof(SgAsmJvmAttribute)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmAttribute::pool_size * sizeof(SgAsmJvmAttribute));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmAttribute::pool_size * sizeof(SgAsmJvmAttribute));
#endif
        SgAsmJvmAttribute::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmAttribute::pool_size * sizeof(SgAsmJvmAttribute), V_SgAsmJvmAttribute));

        if (SgAsmJvmAttribute::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmAttribute* blkptr{reinterpret_cast<SgAsmJvmAttribute*>(SgAsmJvmAttribute::pools[blockIndex-1])};
                blkptr[SgAsmJvmAttribute::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmAttribute::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmAttribute::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmAttribute::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmAttribute::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmAttribute::pool_size - 1};
    SgAsmJvmAttribute* testPointer{reinterpret_cast<SgAsmJvmAttribute*>(SgAsmJvmAttribute::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmAttribute::pool_size * (SgAsmJvmAttribute::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmAttributeStroageClass
 * from its counterpart of SgAsmJvmAttribute. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmAttribute::initializeStorageClassArray(SgAsmJvmAttributeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmAttribute::pools.begin(); block != SgAsmJvmAttribute::pools.end(); ++block) {
        SgAsmJvmAttribute* pointer{reinterpret_cast<SgAsmJvmAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAttribute::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 233023 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmAnnotationDefault that takes its corresponding StorageClass as parameter
 */
SgAsmJvmAnnotationDefault::SgAsmJvmAnnotationDefault(const SgAsmJvmAnnotationDefaultStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 233033 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_default_value = (SgAsmJvmRuntimeAnnotationValue*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_default_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmAnnotationDefault*
SgAsmJvmAnnotationDefault::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmAnnotationDefault* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAnnotationDefault)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAnnotationDefault + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmAnnotationDefault)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAnnotationDefault)};
        unsigned long positionInPool{localIndex % SgAsmJvmAnnotationDefault::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmAnnotationDefault::pool_size};
        returnPointer = &(((SgAsmJvmAnnotationDefault*)(SgAsmJvmAnnotationDefault::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmAnnotationDefault*
SgAsmJvmAnnotationDefault::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmAnnotationDefault* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAnnotationDefault)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAnnotationDefault + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmAnnotationDefault)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmAnnotationDefault)};
        unsigned long positionInPool{localIndex % SgAsmJvmAnnotationDefault::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmAnnotationDefault::pool_size};
        returnPointer = &(((SgAsmJvmAnnotationDefault*)(SgAsmJvmAnnotationDefault::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmAnnotationDefault::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmAnnotationDefault::pools.begin(); block != SgAsmJvmAnnotationDefault::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmAnnotationDefault*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAnnotationDefault::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmAnnotationDefault*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmAnnotationDefault::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmAnnotationDefault* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmAnnotationDefault::pools.begin(); block != SgAsmJvmAnnotationDefault::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmAnnotationDefault*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAnnotationDefault::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmAnnotationDefault::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmAnnotationDefault::clearMemoryPool() {
    SgAsmJvmAnnotationDefault* tempPointer{nullptr};
    if (SgAsmJvmAnnotationDefault::pools.empty() == false) {
        auto block{SgAsmJvmAnnotationDefault::pools.begin()};
        SgAsmJvmAnnotationDefault::next_node = reinterpret_cast<SgAsmJvmAnnotationDefault*>(*block);
        for (; block != SgAsmJvmAnnotationDefault::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmAnnotationDefault*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmAnnotationDefault::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmAnnotationDefault::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmAnnotationDefault::pool_size-1]);
        }
    }
}

void
SgAsmJvmAnnotationDefault::deleteMemoryPool() {
    for (auto p: SgAsmJvmAnnotationDefault::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmAnnotationDefault::next_node = nullptr;
    SgAsmJvmAnnotationDefault::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmAnnotationDefault::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAnnotationDefault) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAnnotationDefault)};
    for (size_t blockIndex = SgAsmJvmAnnotationDefault::pools.size(); (blockIndex * SgAsmJvmAnnotationDefault::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAnnotationDefault) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAnnotationDefault) = %" PRIuPTR " SgAsmJvmAnnotationDefault::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmAnnotationDefault),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmAnnotationDefault),SgAsmJvmAnnotationDefault::pool_size);
        }
#endif

        SgAsmJvmAnnotationDefault* pointer{static_cast<SgAsmJvmAnnotationDefault*>(ROSE_MALLOC(SgAsmJvmAnnotationDefault::pool_size * sizeof(SgAsmJvmAnnotationDefault)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmAnnotationDefault::pool_size * sizeof(SgAsmJvmAnnotationDefault));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmAnnotationDefault::pool_size * sizeof(SgAsmJvmAnnotationDefault));
#endif
        SgAsmJvmAnnotationDefault::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmAnnotationDefault::pool_size * sizeof(SgAsmJvmAnnotationDefault), V_SgAsmJvmAnnotationDefault));

        if (SgAsmJvmAnnotationDefault::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmAnnotationDefault* blkptr{reinterpret_cast<SgAsmJvmAnnotationDefault*>(SgAsmJvmAnnotationDefault::pools[blockIndex-1])};
                blkptr[SgAsmJvmAnnotationDefault::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmAnnotationDefault::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmAnnotationDefault::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmAnnotationDefault::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmAnnotationDefault::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmAnnotationDefault::pool_size - 1};
    SgAsmJvmAnnotationDefault* testPointer{reinterpret_cast<SgAsmJvmAnnotationDefault*>(SgAsmJvmAnnotationDefault::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmAnnotationDefault::pool_size * (SgAsmJvmAnnotationDefault::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmAnnotationDefaultStroageClass
 * from its counterpart of SgAsmJvmAnnotationDefault. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmAnnotationDefault::initializeStorageClassArray(SgAsmJvmAnnotationDefaultStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmAnnotationDefault::pools.begin(); block != SgAsmJvmAnnotationDefault::pools.end(); ++block) {
        SgAsmJvmAnnotationDefault* pointer{reinterpret_cast<SgAsmJvmAnnotationDefault*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmAnnotationDefault::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 233251 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmBootstrapMethods that takes its corresponding StorageClass as parameter
 */
SgAsmJvmBootstrapMethods::SgAsmJvmBootstrapMethods(const SgAsmJvmBootstrapMethodsStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 233261 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_bootstrap_methods = storageSource.storageOf_bootstrap_methods.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmBootstrapMethodPtrList::iterator i_bootstrap_methods = p_bootstrap_methods.begin();
    for ( ; i_bootstrap_methods != p_bootstrap_methods.end(); ++i_bootstrap_methods) {
        (*i_bootstrap_methods) = (SgAsmJvmBootstrapMethodPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_bootstrap_methods)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmBootstrapMethods*
SgAsmJvmBootstrapMethods::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmBootstrapMethods* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethods)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethods + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethods)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethods)};
        unsigned long positionInPool{localIndex % SgAsmJvmBootstrapMethods::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmBootstrapMethods::pool_size};
        returnPointer = &(((SgAsmJvmBootstrapMethods*)(SgAsmJvmBootstrapMethods::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmBootstrapMethods*
SgAsmJvmBootstrapMethods::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmBootstrapMethods* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmBootstrapMethods)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmBootstrapMethods + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmBootstrapMethods)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmBootstrapMethods)};
        unsigned long positionInPool{localIndex % SgAsmJvmBootstrapMethods::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmBootstrapMethods::pool_size};
        returnPointer = &(((SgAsmJvmBootstrapMethods*)(SgAsmJvmBootstrapMethods::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmBootstrapMethods::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmBootstrapMethods::pools.begin(); block != SgAsmJvmBootstrapMethods::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmBootstrapMethods*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmBootstrapMethods::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmBootstrapMethods*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmBootstrapMethods::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmBootstrapMethods* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmBootstrapMethods::pools.begin(); block != SgAsmJvmBootstrapMethods::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmBootstrapMethods*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmBootstrapMethods::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmBootstrapMethods::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmBootstrapMethods::clearMemoryPool() {
    SgAsmJvmBootstrapMethods* tempPointer{nullptr};
    if (SgAsmJvmBootstrapMethods::pools.empty() == false) {
        auto block{SgAsmJvmBootstrapMethods::pools.begin()};
        SgAsmJvmBootstrapMethods::next_node = reinterpret_cast<SgAsmJvmBootstrapMethods*>(*block);
        for (; block != SgAsmJvmBootstrapMethods::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmBootstrapMethods*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmBootstrapMethods::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmBootstrapMethods::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmBootstrapMethods::pool_size-1]);
        }
    }
}

void
SgAsmJvmBootstrapMethods::deleteMemoryPool() {
    for (auto p: SgAsmJvmBootstrapMethods::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmBootstrapMethods::next_node = nullptr;
    SgAsmJvmBootstrapMethods::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmBootstrapMethods::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethods) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethods)};
    for (size_t blockIndex = SgAsmJvmBootstrapMethods::pools.size(); (blockIndex * SgAsmJvmBootstrapMethods::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethods) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethods) = %" PRIuPTR " SgAsmJvmBootstrapMethods::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethods),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethods),SgAsmJvmBootstrapMethods::pool_size);
        }
#endif

        SgAsmJvmBootstrapMethods* pointer{static_cast<SgAsmJvmBootstrapMethods*>(ROSE_MALLOC(SgAsmJvmBootstrapMethods::pool_size * sizeof(SgAsmJvmBootstrapMethods)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmBootstrapMethods::pool_size * sizeof(SgAsmJvmBootstrapMethods));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmBootstrapMethods::pool_size * sizeof(SgAsmJvmBootstrapMethods));
#endif
        SgAsmJvmBootstrapMethods::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmBootstrapMethods::pool_size * sizeof(SgAsmJvmBootstrapMethods), V_SgAsmJvmBootstrapMethods));

        if (SgAsmJvmBootstrapMethods::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmBootstrapMethods* blkptr{reinterpret_cast<SgAsmJvmBootstrapMethods*>(SgAsmJvmBootstrapMethods::pools[blockIndex-1])};
                blkptr[SgAsmJvmBootstrapMethods::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmBootstrapMethods::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmBootstrapMethods::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmBootstrapMethods::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmBootstrapMethods::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmBootstrapMethods::pool_size - 1};
    SgAsmJvmBootstrapMethods* testPointer{reinterpret_cast<SgAsmJvmBootstrapMethods*>(SgAsmJvmBootstrapMethods::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmBootstrapMethods::pool_size * (SgAsmJvmBootstrapMethods::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmBootstrapMethodsStroageClass
 * from its counterpart of SgAsmJvmBootstrapMethods. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmBootstrapMethods::initializeStorageClassArray(SgAsmJvmBootstrapMethodsStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmBootstrapMethods::pools.begin(); block != SgAsmJvmBootstrapMethods::pools.end(); ++block) {
        SgAsmJvmBootstrapMethods* pointer{reinterpret_cast<SgAsmJvmBootstrapMethods*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmBootstrapMethods::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 233483 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmCodeAttribute that takes its corresponding StorageClass as parameter
 */
SgAsmJvmCodeAttribute::SgAsmJvmCodeAttribute(const SgAsmJvmCodeAttributeStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 233493 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_max_stack = storageSource.storageOf_max_stack;
    p_max_locals = storageSource.storageOf_max_locals;
    p_code_length = storageSource.storageOf_code_length;
     p_code = const_cast<char* >( storageSource.storageOf_code.rebuildDataStoredInEasyStorageClass());
     p_exception_table = (SgAsmJvmExceptionTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_exception_table));
     p_attribute_table = (SgAsmJvmAttributeTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_attribute_table));
    p_code_offset = storageSource.storageOf_code_offset;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmCodeAttribute*
SgAsmJvmCodeAttribute::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmCodeAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmCodeAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmCodeAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmCodeAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmCodeAttribute)};
        unsigned long positionInPool{localIndex % SgAsmJvmCodeAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmCodeAttribute::pool_size};
        returnPointer = &(((SgAsmJvmCodeAttribute*)(SgAsmJvmCodeAttribute::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmCodeAttribute*
SgAsmJvmCodeAttribute::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmCodeAttribute* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmCodeAttribute)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmCodeAttribute + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmCodeAttribute)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmCodeAttribute)};
        unsigned long positionInPool{localIndex % SgAsmJvmCodeAttribute::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmCodeAttribute::pool_size};
        returnPointer = &(((SgAsmJvmCodeAttribute*)(SgAsmJvmCodeAttribute::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmCodeAttribute::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmCodeAttribute::pools.begin(); block != SgAsmJvmCodeAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmCodeAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmCodeAttribute::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmCodeAttribute*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmCodeAttribute::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmCodeAttribute* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmCodeAttribute::pools.begin(); block != SgAsmJvmCodeAttribute::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmCodeAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmCodeAttribute::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmCodeAttribute::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmCodeAttribute::clearMemoryPool() {
    SgAsmJvmCodeAttribute* tempPointer{nullptr};
    if (SgAsmJvmCodeAttribute::pools.empty() == false) {
        auto block{SgAsmJvmCodeAttribute::pools.begin()};
        SgAsmJvmCodeAttribute::next_node = reinterpret_cast<SgAsmJvmCodeAttribute*>(*block);
        for (; block != SgAsmJvmCodeAttribute::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmCodeAttribute*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmCodeAttribute::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmCodeAttribute::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmCodeAttribute::pool_size-1]);
        }
    }
}

void
SgAsmJvmCodeAttribute::deleteMemoryPool() {
    for (auto p: SgAsmJvmCodeAttribute::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmCodeAttribute::next_node = nullptr;
    SgAsmJvmCodeAttribute::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmCodeAttribute::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmCodeAttribute) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmCodeAttribute)};
    for (size_t blockIndex = SgAsmJvmCodeAttribute::pools.size(); (blockIndex * SgAsmJvmCodeAttribute::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmCodeAttribute) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmCodeAttribute) = %" PRIuPTR " SgAsmJvmCodeAttribute::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmCodeAttribute),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmCodeAttribute),SgAsmJvmCodeAttribute::pool_size);
        }
#endif

        SgAsmJvmCodeAttribute* pointer{static_cast<SgAsmJvmCodeAttribute*>(ROSE_MALLOC(SgAsmJvmCodeAttribute::pool_size * sizeof(SgAsmJvmCodeAttribute)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmCodeAttribute::pool_size * sizeof(SgAsmJvmCodeAttribute));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmCodeAttribute::pool_size * sizeof(SgAsmJvmCodeAttribute));
#endif
        SgAsmJvmCodeAttribute::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmCodeAttribute::pool_size * sizeof(SgAsmJvmCodeAttribute), V_SgAsmJvmCodeAttribute));

        if (SgAsmJvmCodeAttribute::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmCodeAttribute* blkptr{reinterpret_cast<SgAsmJvmCodeAttribute*>(SgAsmJvmCodeAttribute::pools[blockIndex-1])};
                blkptr[SgAsmJvmCodeAttribute::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmCodeAttribute::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmCodeAttribute::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmCodeAttribute::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmCodeAttribute::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmCodeAttribute::pool_size - 1};
    SgAsmJvmCodeAttribute* testPointer{reinterpret_cast<SgAsmJvmCodeAttribute*>(SgAsmJvmCodeAttribute::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmCodeAttribute::pool_size * (SgAsmJvmCodeAttribute::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmCodeAttributeStroageClass
 * from its counterpart of SgAsmJvmCodeAttribute. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmCodeAttribute::initializeStorageClassArray(SgAsmJvmCodeAttributeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmCodeAttribute::pools.begin(); block != SgAsmJvmCodeAttribute::pools.end(); ++block) {
        SgAsmJvmCodeAttribute* pointer{reinterpret_cast<SgAsmJvmCodeAttribute*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmCodeAttribute::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 233717 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmEnclosingMethod that takes its corresponding StorageClass as parameter
 */
SgAsmJvmEnclosingMethod::SgAsmJvmEnclosingMethod(const SgAsmJvmEnclosingMethodStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 233727 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_class_index = storageSource.storageOf_class_index;
    p_method_index = storageSource.storageOf_method_index;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmEnclosingMethod*
SgAsmJvmEnclosingMethod::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmEnclosingMethod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmEnclosingMethod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmEnclosingMethod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmEnclosingMethod)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmEnclosingMethod)};
        unsigned long positionInPool{localIndex % SgAsmJvmEnclosingMethod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmEnclosingMethod::pool_size};
        returnPointer = &(((SgAsmJvmEnclosingMethod*)(SgAsmJvmEnclosingMethod::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmEnclosingMethod*
SgAsmJvmEnclosingMethod::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmEnclosingMethod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmEnclosingMethod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmEnclosingMethod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmEnclosingMethod)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmEnclosingMethod)};
        unsigned long positionInPool{localIndex % SgAsmJvmEnclosingMethod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmEnclosingMethod::pool_size};
        returnPointer = &(((SgAsmJvmEnclosingMethod*)(SgAsmJvmEnclosingMethod::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmEnclosingMethod::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmEnclosingMethod::pools.begin(); block != SgAsmJvmEnclosingMethod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmEnclosingMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmEnclosingMethod::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmEnclosingMethod*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmEnclosingMethod::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmEnclosingMethod* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmEnclosingMethod::pools.begin(); block != SgAsmJvmEnclosingMethod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmEnclosingMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmEnclosingMethod::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmEnclosingMethod::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmEnclosingMethod::clearMemoryPool() {
    SgAsmJvmEnclosingMethod* tempPointer{nullptr};
    if (SgAsmJvmEnclosingMethod::pools.empty() == false) {
        auto block{SgAsmJvmEnclosingMethod::pools.begin()};
        SgAsmJvmEnclosingMethod::next_node = reinterpret_cast<SgAsmJvmEnclosingMethod*>(*block);
        for (; block != SgAsmJvmEnclosingMethod::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmEnclosingMethod*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmEnclosingMethod::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmEnclosingMethod::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmEnclosingMethod::pool_size-1]);
        }
    }
}

void
SgAsmJvmEnclosingMethod::deleteMemoryPool() {
    for (auto p: SgAsmJvmEnclosingMethod::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmEnclosingMethod::next_node = nullptr;
    SgAsmJvmEnclosingMethod::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmEnclosingMethod::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmEnclosingMethod) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmEnclosingMethod)};
    for (size_t blockIndex = SgAsmJvmEnclosingMethod::pools.size(); (blockIndex * SgAsmJvmEnclosingMethod::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmEnclosingMethod) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmEnclosingMethod) = %" PRIuPTR " SgAsmJvmEnclosingMethod::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmEnclosingMethod),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmEnclosingMethod),SgAsmJvmEnclosingMethod::pool_size);
        }
#endif

        SgAsmJvmEnclosingMethod* pointer{static_cast<SgAsmJvmEnclosingMethod*>(ROSE_MALLOC(SgAsmJvmEnclosingMethod::pool_size * sizeof(SgAsmJvmEnclosingMethod)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmEnclosingMethod::pool_size * sizeof(SgAsmJvmEnclosingMethod));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmEnclosingMethod::pool_size * sizeof(SgAsmJvmEnclosingMethod));
#endif
        SgAsmJvmEnclosingMethod::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmEnclosingMethod::pool_size * sizeof(SgAsmJvmEnclosingMethod), V_SgAsmJvmEnclosingMethod));

        if (SgAsmJvmEnclosingMethod::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmEnclosingMethod* blkptr{reinterpret_cast<SgAsmJvmEnclosingMethod*>(SgAsmJvmEnclosingMethod::pools[blockIndex-1])};
                blkptr[SgAsmJvmEnclosingMethod::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmEnclosingMethod::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmEnclosingMethod::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmEnclosingMethod::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmEnclosingMethod::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmEnclosingMethod::pool_size - 1};
    SgAsmJvmEnclosingMethod* testPointer{reinterpret_cast<SgAsmJvmEnclosingMethod*>(SgAsmJvmEnclosingMethod::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmEnclosingMethod::pool_size * (SgAsmJvmEnclosingMethod::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmEnclosingMethodStroageClass
 * from its counterpart of SgAsmJvmEnclosingMethod. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmEnclosingMethod::initializeStorageClassArray(SgAsmJvmEnclosingMethodStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmEnclosingMethod::pools.begin(); block != SgAsmJvmEnclosingMethod::pools.end(); ++block) {
        SgAsmJvmEnclosingMethod* pointer{reinterpret_cast<SgAsmJvmEnclosingMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmEnclosingMethod::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 233946 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmIndexedAttr that takes its corresponding StorageClass as parameter
 */
SgAsmJvmIndexedAttr::SgAsmJvmIndexedAttr(const SgAsmJvmIndexedAttrStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 233956 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_attribute_type = storageSource.storageOf_attribute_type;
    p_index = storageSource.storageOf_index;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmIndexedAttr*
SgAsmJvmIndexedAttr::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmIndexedAttr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmIndexedAttr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmIndexedAttr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmIndexedAttr)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexedAttr)};
        unsigned long positionInPool{localIndex % SgAsmJvmIndexedAttr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmIndexedAttr::pool_size};
        returnPointer = &(((SgAsmJvmIndexedAttr*)(SgAsmJvmIndexedAttr::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmIndexedAttr*
SgAsmJvmIndexedAttr::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmIndexedAttr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmIndexedAttr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmIndexedAttr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmIndexedAttr)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmIndexedAttr)};
        unsigned long positionInPool{localIndex % SgAsmJvmIndexedAttr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmIndexedAttr::pool_size};
        returnPointer = &(((SgAsmJvmIndexedAttr*)(SgAsmJvmIndexedAttr::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmIndexedAttr::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmIndexedAttr::pools.begin(); block != SgAsmJvmIndexedAttr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmIndexedAttr*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmIndexedAttr::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmIndexedAttr*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmIndexedAttr::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmIndexedAttr* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmIndexedAttr::pools.begin(); block != SgAsmJvmIndexedAttr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmIndexedAttr*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmIndexedAttr::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmIndexedAttr::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmIndexedAttr::clearMemoryPool() {
    SgAsmJvmIndexedAttr* tempPointer{nullptr};
    if (SgAsmJvmIndexedAttr::pools.empty() == false) {
        auto block{SgAsmJvmIndexedAttr::pools.begin()};
        SgAsmJvmIndexedAttr::next_node = reinterpret_cast<SgAsmJvmIndexedAttr*>(*block);
        for (; block != SgAsmJvmIndexedAttr::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmIndexedAttr*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmIndexedAttr::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmIndexedAttr::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmIndexedAttr::pool_size-1]);
        }
    }
}

void
SgAsmJvmIndexedAttr::deleteMemoryPool() {
    for (auto p: SgAsmJvmIndexedAttr::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmIndexedAttr::next_node = nullptr;
    SgAsmJvmIndexedAttr::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmIndexedAttr::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexedAttr) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmIndexedAttr)};
    for (size_t blockIndex = SgAsmJvmIndexedAttr::pools.size(); (blockIndex * SgAsmJvmIndexedAttr::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexedAttr) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmIndexedAttr) = %" PRIuPTR " SgAsmJvmIndexedAttr::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexedAttr),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmIndexedAttr),SgAsmJvmIndexedAttr::pool_size);
        }
#endif

        SgAsmJvmIndexedAttr* pointer{static_cast<SgAsmJvmIndexedAttr*>(ROSE_MALLOC(SgAsmJvmIndexedAttr::pool_size * sizeof(SgAsmJvmIndexedAttr)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmIndexedAttr::pool_size * sizeof(SgAsmJvmIndexedAttr));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmIndexedAttr::pool_size * sizeof(SgAsmJvmIndexedAttr));
#endif
        SgAsmJvmIndexedAttr::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmIndexedAttr::pool_size * sizeof(SgAsmJvmIndexedAttr), V_SgAsmJvmIndexedAttr));

        if (SgAsmJvmIndexedAttr::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmIndexedAttr* blkptr{reinterpret_cast<SgAsmJvmIndexedAttr*>(SgAsmJvmIndexedAttr::pools[blockIndex-1])};
                blkptr[SgAsmJvmIndexedAttr::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmIndexedAttr::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmIndexedAttr::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmIndexedAttr::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmIndexedAttr::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmIndexedAttr::pool_size - 1};
    SgAsmJvmIndexedAttr* testPointer{reinterpret_cast<SgAsmJvmIndexedAttr*>(SgAsmJvmIndexedAttr::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmIndexedAttr::pool_size * (SgAsmJvmIndexedAttr::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmIndexedAttrStroageClass
 * from its counterpart of SgAsmJvmIndexedAttr. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmIndexedAttr::initializeStorageClassArray(SgAsmJvmIndexedAttrStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmIndexedAttr::pools.begin(); block != SgAsmJvmIndexedAttr::pools.end(); ++block) {
        SgAsmJvmIndexedAttr* pointer{reinterpret_cast<SgAsmJvmIndexedAttr*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmIndexedAttr::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 234175 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmIndexTableAttr that takes its corresponding StorageClass as parameter
 */
SgAsmJvmIndexTableAttr::SgAsmJvmIndexTableAttr(const SgAsmJvmIndexTableAttrStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 234185 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_attribute_type = storageSource.storageOf_attribute_type;
    p_table = storageSource.storageOf_table.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmIndexTableAttr*
SgAsmJvmIndexTableAttr::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmIndexTableAttr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmIndexTableAttr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmIndexTableAttr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmIndexTableAttr)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexTableAttr)};
        unsigned long positionInPool{localIndex % SgAsmJvmIndexTableAttr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmIndexTableAttr::pool_size};
        returnPointer = &(((SgAsmJvmIndexTableAttr*)(SgAsmJvmIndexTableAttr::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmIndexTableAttr*
SgAsmJvmIndexTableAttr::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmIndexTableAttr* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmIndexTableAttr)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmIndexTableAttr + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmIndexTableAttr)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmIndexTableAttr)};
        unsigned long positionInPool{localIndex % SgAsmJvmIndexTableAttr::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmIndexTableAttr::pool_size};
        returnPointer = &(((SgAsmJvmIndexTableAttr*)(SgAsmJvmIndexTableAttr::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmIndexTableAttr::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmIndexTableAttr::pools.begin(); block != SgAsmJvmIndexTableAttr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmIndexTableAttr*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmIndexTableAttr::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmIndexTableAttr*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmIndexTableAttr::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmIndexTableAttr* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmIndexTableAttr::pools.begin(); block != SgAsmJvmIndexTableAttr::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmIndexTableAttr*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmIndexTableAttr::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmIndexTableAttr::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmIndexTableAttr::clearMemoryPool() {
    SgAsmJvmIndexTableAttr* tempPointer{nullptr};
    if (SgAsmJvmIndexTableAttr::pools.empty() == false) {
        auto block{SgAsmJvmIndexTableAttr::pools.begin()};
        SgAsmJvmIndexTableAttr::next_node = reinterpret_cast<SgAsmJvmIndexTableAttr*>(*block);
        for (; block != SgAsmJvmIndexTableAttr::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmIndexTableAttr*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmIndexTableAttr::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmIndexTableAttr::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmIndexTableAttr::pool_size-1]);
        }
    }
}

void
SgAsmJvmIndexTableAttr::deleteMemoryPool() {
    for (auto p: SgAsmJvmIndexTableAttr::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmIndexTableAttr::next_node = nullptr;
    SgAsmJvmIndexTableAttr::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmIndexTableAttr::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexTableAttr) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmIndexTableAttr)};
    for (size_t blockIndex = SgAsmJvmIndexTableAttr::pools.size(); (blockIndex * SgAsmJvmIndexTableAttr::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexTableAttr) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmIndexTableAttr) = %" PRIuPTR " SgAsmJvmIndexTableAttr::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmIndexTableAttr),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmIndexTableAttr),SgAsmJvmIndexTableAttr::pool_size);
        }
#endif

        SgAsmJvmIndexTableAttr* pointer{static_cast<SgAsmJvmIndexTableAttr*>(ROSE_MALLOC(SgAsmJvmIndexTableAttr::pool_size * sizeof(SgAsmJvmIndexTableAttr)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmIndexTableAttr::pool_size * sizeof(SgAsmJvmIndexTableAttr));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmIndexTableAttr::pool_size * sizeof(SgAsmJvmIndexTableAttr));
#endif
        SgAsmJvmIndexTableAttr::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmIndexTableAttr::pool_size * sizeof(SgAsmJvmIndexTableAttr), V_SgAsmJvmIndexTableAttr));

        if (SgAsmJvmIndexTableAttr::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmIndexTableAttr* blkptr{reinterpret_cast<SgAsmJvmIndexTableAttr*>(SgAsmJvmIndexTableAttr::pools[blockIndex-1])};
                blkptr[SgAsmJvmIndexTableAttr::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmIndexTableAttr::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmIndexTableAttr::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmIndexTableAttr::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmIndexTableAttr::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmIndexTableAttr::pool_size - 1};
    SgAsmJvmIndexTableAttr* testPointer{reinterpret_cast<SgAsmJvmIndexTableAttr*>(SgAsmJvmIndexTableAttr::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmIndexTableAttr::pool_size * (SgAsmJvmIndexTableAttr::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmIndexTableAttrStroageClass
 * from its counterpart of SgAsmJvmIndexTableAttr. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmIndexTableAttr::initializeStorageClassArray(SgAsmJvmIndexTableAttrStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmIndexTableAttr::pools.begin(); block != SgAsmJvmIndexTableAttr::pools.end(); ++block) {
        SgAsmJvmIndexTableAttr* pointer{reinterpret_cast<SgAsmJvmIndexTableAttr*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmIndexTableAttr::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 234404 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmInnerClasses that takes its corresponding StorageClass as parameter
 */
SgAsmJvmInnerClasses::SgAsmJvmInnerClasses(const SgAsmJvmInnerClassesStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 234414 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_classes = storageSource.storageOf_classes.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmInnerClasses*
SgAsmJvmInnerClasses::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmInnerClasses* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmInnerClasses)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmInnerClasses + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmInnerClasses)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInnerClasses)};
        unsigned long positionInPool{localIndex % SgAsmJvmInnerClasses::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmInnerClasses::pool_size};
        returnPointer = &(((SgAsmJvmInnerClasses*)(SgAsmJvmInnerClasses::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmInnerClasses*
SgAsmJvmInnerClasses::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmInnerClasses* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmInnerClasses)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmInnerClasses + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmInnerClasses)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmInnerClasses)};
        unsigned long positionInPool{localIndex % SgAsmJvmInnerClasses::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmInnerClasses::pool_size};
        returnPointer = &(((SgAsmJvmInnerClasses*)(SgAsmJvmInnerClasses::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmInnerClasses::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmInnerClasses::pools.begin(); block != SgAsmJvmInnerClasses::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmInnerClasses*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmInnerClasses::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmInnerClasses*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmInnerClasses::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmInnerClasses* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmInnerClasses::pools.begin(); block != SgAsmJvmInnerClasses::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmInnerClasses*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmInnerClasses::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmInnerClasses::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmInnerClasses::clearMemoryPool() {
    SgAsmJvmInnerClasses* tempPointer{nullptr};
    if (SgAsmJvmInnerClasses::pools.empty() == false) {
        auto block{SgAsmJvmInnerClasses::pools.begin()};
        SgAsmJvmInnerClasses::next_node = reinterpret_cast<SgAsmJvmInnerClasses*>(*block);
        for (; block != SgAsmJvmInnerClasses::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmInnerClasses*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmInnerClasses::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmInnerClasses::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmInnerClasses::pool_size-1]);
        }
    }
}

void
SgAsmJvmInnerClasses::deleteMemoryPool() {
    for (auto p: SgAsmJvmInnerClasses::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmInnerClasses::next_node = nullptr;
    SgAsmJvmInnerClasses::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmInnerClasses::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInnerClasses) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmInnerClasses)};
    for (size_t blockIndex = SgAsmJvmInnerClasses::pools.size(); (blockIndex * SgAsmJvmInnerClasses::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInnerClasses) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmInnerClasses) = %" PRIuPTR " SgAsmJvmInnerClasses::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInnerClasses),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmInnerClasses),SgAsmJvmInnerClasses::pool_size);
        }
#endif

        SgAsmJvmInnerClasses* pointer{static_cast<SgAsmJvmInnerClasses*>(ROSE_MALLOC(SgAsmJvmInnerClasses::pool_size * sizeof(SgAsmJvmInnerClasses)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmInnerClasses::pool_size * sizeof(SgAsmJvmInnerClasses));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmInnerClasses::pool_size * sizeof(SgAsmJvmInnerClasses));
#endif
        SgAsmJvmInnerClasses::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmInnerClasses::pool_size * sizeof(SgAsmJvmInnerClasses), V_SgAsmJvmInnerClasses));

        if (SgAsmJvmInnerClasses::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmInnerClasses* blkptr{reinterpret_cast<SgAsmJvmInnerClasses*>(SgAsmJvmInnerClasses::pools[blockIndex-1])};
                blkptr[SgAsmJvmInnerClasses::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmInnerClasses::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmInnerClasses::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmInnerClasses::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmInnerClasses::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmInnerClasses::pool_size - 1};
    SgAsmJvmInnerClasses* testPointer{reinterpret_cast<SgAsmJvmInnerClasses*>(SgAsmJvmInnerClasses::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmInnerClasses::pool_size * (SgAsmJvmInnerClasses::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmInnerClassesStroageClass
 * from its counterpart of SgAsmJvmInnerClasses. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmInnerClasses::initializeStorageClassArray(SgAsmJvmInnerClassesStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmInnerClasses::pools.begin(); block != SgAsmJvmInnerClasses::pools.end(); ++block) {
        SgAsmJvmInnerClasses* pointer{reinterpret_cast<SgAsmJvmInnerClasses*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmInnerClasses::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 234632 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmLineNumberTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmLineNumberTable::SgAsmJvmLineNumberTable(const SgAsmJvmLineNumberTableStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 234642 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_line_number_table = storageSource.storageOf_line_number_table.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmLineNumberTable*
SgAsmJvmLineNumberTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmLineNumberTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLineNumberTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLineNumberTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLineNumberTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLineNumberTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmLineNumberTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLineNumberTable::pool_size};
        returnPointer = &(((SgAsmJvmLineNumberTable*)(SgAsmJvmLineNumberTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmLineNumberTable*
SgAsmJvmLineNumberTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmLineNumberTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLineNumberTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLineNumberTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLineNumberTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmLineNumberTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmLineNumberTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLineNumberTable::pool_size};
        returnPointer = &(((SgAsmJvmLineNumberTable*)(SgAsmJvmLineNumberTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmLineNumberTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmLineNumberTable::pools.begin(); block != SgAsmJvmLineNumberTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLineNumberTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLineNumberTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmLineNumberTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmLineNumberTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmLineNumberTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmLineNumberTable::pools.begin(); block != SgAsmJvmLineNumberTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLineNumberTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLineNumberTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmLineNumberTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmLineNumberTable::clearMemoryPool() {
    SgAsmJvmLineNumberTable* tempPointer{nullptr};
    if (SgAsmJvmLineNumberTable::pools.empty() == false) {
        auto block{SgAsmJvmLineNumberTable::pools.begin()};
        SgAsmJvmLineNumberTable::next_node = reinterpret_cast<SgAsmJvmLineNumberTable*>(*block);
        for (; block != SgAsmJvmLineNumberTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmLineNumberTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmLineNumberTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmLineNumberTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmLineNumberTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmLineNumberTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmLineNumberTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmLineNumberTable::next_node = nullptr;
    SgAsmJvmLineNumberTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmLineNumberTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLineNumberTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLineNumberTable)};
    for (size_t blockIndex = SgAsmJvmLineNumberTable::pools.size(); (blockIndex * SgAsmJvmLineNumberTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLineNumberTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLineNumberTable) = %" PRIuPTR " SgAsmJvmLineNumberTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLineNumberTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLineNumberTable),SgAsmJvmLineNumberTable::pool_size);
        }
#endif

        SgAsmJvmLineNumberTable* pointer{static_cast<SgAsmJvmLineNumberTable*>(ROSE_MALLOC(SgAsmJvmLineNumberTable::pool_size * sizeof(SgAsmJvmLineNumberTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmLineNumberTable::pool_size * sizeof(SgAsmJvmLineNumberTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmLineNumberTable::pool_size * sizeof(SgAsmJvmLineNumberTable));
#endif
        SgAsmJvmLineNumberTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmLineNumberTable::pool_size * sizeof(SgAsmJvmLineNumberTable), V_SgAsmJvmLineNumberTable));

        if (SgAsmJvmLineNumberTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmLineNumberTable* blkptr{reinterpret_cast<SgAsmJvmLineNumberTable*>(SgAsmJvmLineNumberTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmLineNumberTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmLineNumberTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmLineNumberTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmLineNumberTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmLineNumberTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmLineNumberTable::pool_size - 1};
    SgAsmJvmLineNumberTable* testPointer{reinterpret_cast<SgAsmJvmLineNumberTable*>(SgAsmJvmLineNumberTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmLineNumberTable::pool_size * (SgAsmJvmLineNumberTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmLineNumberTableStroageClass
 * from its counterpart of SgAsmJvmLineNumberTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmLineNumberTable::initializeStorageClassArray(SgAsmJvmLineNumberTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmLineNumberTable::pools.begin(); block != SgAsmJvmLineNumberTable::pools.end(); ++block) {
        SgAsmJvmLineNumberTable* pointer{reinterpret_cast<SgAsmJvmLineNumberTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLineNumberTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 234860 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmLocalVariableTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmLocalVariableTable::SgAsmJvmLocalVariableTable(const SgAsmJvmLocalVariableTableStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 234870 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_local_variable_table = storageSource.storageOf_local_variable_table.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmLocalVariableEntryPtrList::iterator i_local_variable_table = p_local_variable_table.begin();
    for ( ; i_local_variable_table != p_local_variable_table.end(); ++i_local_variable_table) {
        (*i_local_variable_table) = (SgAsmJvmLocalVariableEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_local_variable_table)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmLocalVariableTable*
SgAsmJvmLocalVariableTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmLocalVariableTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableTable::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableTable*)(SgAsmJvmLocalVariableTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmLocalVariableTable*
SgAsmJvmLocalVariableTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmLocalVariableTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmLocalVariableTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableTable::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableTable*)(SgAsmJvmLocalVariableTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmLocalVariableTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmLocalVariableTable::pools.begin(); block != SgAsmJvmLocalVariableTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmLocalVariableTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmLocalVariableTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmLocalVariableTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmLocalVariableTable::pools.begin(); block != SgAsmJvmLocalVariableTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmLocalVariableTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmLocalVariableTable::clearMemoryPool() {
    SgAsmJvmLocalVariableTable* tempPointer{nullptr};
    if (SgAsmJvmLocalVariableTable::pools.empty() == false) {
        auto block{SgAsmJvmLocalVariableTable::pools.begin()};
        SgAsmJvmLocalVariableTable::next_node = reinterpret_cast<SgAsmJvmLocalVariableTable*>(*block);
        for (; block != SgAsmJvmLocalVariableTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmLocalVariableTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmLocalVariableTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmLocalVariableTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmLocalVariableTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmLocalVariableTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmLocalVariableTable::next_node = nullptr;
    SgAsmJvmLocalVariableTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmLocalVariableTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTable)};
    for (size_t blockIndex = SgAsmJvmLocalVariableTable::pools.size(); (blockIndex * SgAsmJvmLocalVariableTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTable) = %" PRIuPTR " SgAsmJvmLocalVariableTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTable),SgAsmJvmLocalVariableTable::pool_size);
        }
#endif

        SgAsmJvmLocalVariableTable* pointer{static_cast<SgAsmJvmLocalVariableTable*>(ROSE_MALLOC(SgAsmJvmLocalVariableTable::pool_size * sizeof(SgAsmJvmLocalVariableTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmLocalVariableTable::pool_size * sizeof(SgAsmJvmLocalVariableTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmLocalVariableTable::pool_size * sizeof(SgAsmJvmLocalVariableTable));
#endif
        SgAsmJvmLocalVariableTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmLocalVariableTable::pool_size * sizeof(SgAsmJvmLocalVariableTable), V_SgAsmJvmLocalVariableTable));

        if (SgAsmJvmLocalVariableTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmLocalVariableTable* blkptr{reinterpret_cast<SgAsmJvmLocalVariableTable*>(SgAsmJvmLocalVariableTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmLocalVariableTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmLocalVariableTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmLocalVariableTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmLocalVariableTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmLocalVariableTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmLocalVariableTable::pool_size - 1};
    SgAsmJvmLocalVariableTable* testPointer{reinterpret_cast<SgAsmJvmLocalVariableTable*>(SgAsmJvmLocalVariableTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmLocalVariableTable::pool_size * (SgAsmJvmLocalVariableTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmLocalVariableTableStroageClass
 * from its counterpart of SgAsmJvmLocalVariableTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmLocalVariableTable::initializeStorageClassArray(SgAsmJvmLocalVariableTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmLocalVariableTable::pools.begin(); block != SgAsmJvmLocalVariableTable::pools.end(); ++block) {
        SgAsmJvmLocalVariableTable* pointer{reinterpret_cast<SgAsmJvmLocalVariableTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 235092 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmLocalVariableTypeTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmLocalVariableTypeTable::SgAsmJvmLocalVariableTypeTable(const SgAsmJvmLocalVariableTypeTableStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 235102 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_local_variable_type_table = storageSource.storageOf_local_variable_type_table.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmLocalVariableTypeEntryPtrList::iterator i_local_variable_type_table = p_local_variable_type_table.begin();
    for ( ; i_local_variable_type_table != p_local_variable_type_table.end(); ++i_local_variable_type_table) {
        (*i_local_variable_type_table) = (SgAsmJvmLocalVariableTypeEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_local_variable_type_table)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmLocalVariableTypeTable*
SgAsmJvmLocalVariableTypeTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmLocalVariableTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableTypeTable::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableTypeTable*)(SgAsmJvmLocalVariableTypeTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmLocalVariableTypeTable*
SgAsmJvmLocalVariableTypeTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmLocalVariableTypeTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTypeTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTypeTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTypeTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmLocalVariableTypeTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableTypeTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableTypeTable::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableTypeTable*)(SgAsmJvmLocalVariableTypeTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmLocalVariableTypeTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmLocalVariableTypeTable::pools.begin(); block != SgAsmJvmLocalVariableTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmLocalVariableTypeTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmLocalVariableTypeTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmLocalVariableTypeTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmLocalVariableTypeTable::pools.begin(); block != SgAsmJvmLocalVariableTypeTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmLocalVariableTypeTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmLocalVariableTypeTable::clearMemoryPool() {
    SgAsmJvmLocalVariableTypeTable* tempPointer{nullptr};
    if (SgAsmJvmLocalVariableTypeTable::pools.empty() == false) {
        auto block{SgAsmJvmLocalVariableTypeTable::pools.begin()};
        SgAsmJvmLocalVariableTypeTable::next_node = reinterpret_cast<SgAsmJvmLocalVariableTypeTable*>(*block);
        for (; block != SgAsmJvmLocalVariableTypeTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmLocalVariableTypeTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmLocalVariableTypeTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmLocalVariableTypeTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmLocalVariableTypeTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmLocalVariableTypeTable::next_node = nullptr;
    SgAsmJvmLocalVariableTypeTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmLocalVariableTypeTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeTable)};
    for (size_t blockIndex = SgAsmJvmLocalVariableTypeTable::pools.size(); (blockIndex * SgAsmJvmLocalVariableTypeTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeTable) = %" PRIuPTR " SgAsmJvmLocalVariableTypeTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeTable),SgAsmJvmLocalVariableTypeTable::pool_size);
        }
#endif

        SgAsmJvmLocalVariableTypeTable* pointer{static_cast<SgAsmJvmLocalVariableTypeTable*>(ROSE_MALLOC(SgAsmJvmLocalVariableTypeTable::pool_size * sizeof(SgAsmJvmLocalVariableTypeTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmLocalVariableTypeTable::pool_size * sizeof(SgAsmJvmLocalVariableTypeTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmLocalVariableTypeTable::pool_size * sizeof(SgAsmJvmLocalVariableTypeTable));
#endif
        SgAsmJvmLocalVariableTypeTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmLocalVariableTypeTable::pool_size * sizeof(SgAsmJvmLocalVariableTypeTable), V_SgAsmJvmLocalVariableTypeTable));

        if (SgAsmJvmLocalVariableTypeTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmLocalVariableTypeTable* blkptr{reinterpret_cast<SgAsmJvmLocalVariableTypeTable*>(SgAsmJvmLocalVariableTypeTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmLocalVariableTypeTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmLocalVariableTypeTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmLocalVariableTypeTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmLocalVariableTypeTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmLocalVariableTypeTable::pool_size - 1};
    SgAsmJvmLocalVariableTypeTable* testPointer{reinterpret_cast<SgAsmJvmLocalVariableTypeTable*>(SgAsmJvmLocalVariableTypeTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmLocalVariableTypeTable::pool_size * (SgAsmJvmLocalVariableTypeTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmLocalVariableTypeTableStroageClass
 * from its counterpart of SgAsmJvmLocalVariableTypeTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmLocalVariableTypeTable::initializeStorageClassArray(SgAsmJvmLocalVariableTypeTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmLocalVariableTypeTable::pools.begin(); block != SgAsmJvmLocalVariableTypeTable::pools.end(); ++block) {
        SgAsmJvmLocalVariableTypeTable* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 235324 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmMethodParameters that takes its corresponding StorageClass as parameter
 */
SgAsmJvmMethodParameters::SgAsmJvmMethodParameters(const SgAsmJvmMethodParametersStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 235334 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_parameters = storageSource.storageOf_parameters.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmMethodParametersEntryPtrList::iterator i_parameters = p_parameters.begin();
    for ( ; i_parameters != p_parameters.end(); ++i_parameters) {
        (*i_parameters) = (SgAsmJvmMethodParametersEntryPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_parameters)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmMethodParameters*
SgAsmJvmMethodParameters::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmMethodParameters* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodParameters)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodParameters + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodParameters)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParameters)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethodParameters::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethodParameters::pool_size};
        returnPointer = &(((SgAsmJvmMethodParameters*)(SgAsmJvmMethodParameters::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmMethodParameters*
SgAsmJvmMethodParameters::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmMethodParameters* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodParameters)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodParameters + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodParameters)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmMethodParameters)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethodParameters::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethodParameters::pool_size};
        returnPointer = &(((SgAsmJvmMethodParameters*)(SgAsmJvmMethodParameters::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmMethodParameters::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmMethodParameters::pools.begin(); block != SgAsmJvmMethodParameters::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethodParameters*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodParameters::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmMethodParameters*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmMethodParameters::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmMethodParameters* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmMethodParameters::pools.begin(); block != SgAsmJvmMethodParameters::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethodParameters*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodParameters::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmMethodParameters::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmMethodParameters::clearMemoryPool() {
    SgAsmJvmMethodParameters* tempPointer{nullptr};
    if (SgAsmJvmMethodParameters::pools.empty() == false) {
        auto block{SgAsmJvmMethodParameters::pools.begin()};
        SgAsmJvmMethodParameters::next_node = reinterpret_cast<SgAsmJvmMethodParameters*>(*block);
        for (; block != SgAsmJvmMethodParameters::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmMethodParameters*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmMethodParameters::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmMethodParameters::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmMethodParameters::pool_size-1]);
        }
    }
}

void
SgAsmJvmMethodParameters::deleteMemoryPool() {
    for (auto p: SgAsmJvmMethodParameters::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmMethodParameters::next_node = nullptr;
    SgAsmJvmMethodParameters::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmMethodParameters::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParameters) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodParameters)};
    for (size_t blockIndex = SgAsmJvmMethodParameters::pools.size(); (blockIndex * SgAsmJvmMethodParameters::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParameters) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodParameters) = %" PRIuPTR " SgAsmJvmMethodParameters::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParameters),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodParameters),SgAsmJvmMethodParameters::pool_size);
        }
#endif

        SgAsmJvmMethodParameters* pointer{static_cast<SgAsmJvmMethodParameters*>(ROSE_MALLOC(SgAsmJvmMethodParameters::pool_size * sizeof(SgAsmJvmMethodParameters)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmMethodParameters::pool_size * sizeof(SgAsmJvmMethodParameters));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmMethodParameters::pool_size * sizeof(SgAsmJvmMethodParameters));
#endif
        SgAsmJvmMethodParameters::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmMethodParameters::pool_size * sizeof(SgAsmJvmMethodParameters), V_SgAsmJvmMethodParameters));

        if (SgAsmJvmMethodParameters::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmMethodParameters* blkptr{reinterpret_cast<SgAsmJvmMethodParameters*>(SgAsmJvmMethodParameters::pools[blockIndex-1])};
                blkptr[SgAsmJvmMethodParameters::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmMethodParameters::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmMethodParameters::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmMethodParameters::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmMethodParameters::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmMethodParameters::pool_size - 1};
    SgAsmJvmMethodParameters* testPointer{reinterpret_cast<SgAsmJvmMethodParameters*>(SgAsmJvmMethodParameters::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmMethodParameters::pool_size * (SgAsmJvmMethodParameters::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmMethodParametersStroageClass
 * from its counterpart of SgAsmJvmMethodParameters. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmMethodParameters::initializeStorageClassArray(SgAsmJvmMethodParametersStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmMethodParameters::pools.begin(); block != SgAsmJvmMethodParameters::pools.end(); ++block) {
        SgAsmJvmMethodParameters* pointer{reinterpret_cast<SgAsmJvmMethodParameters*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodParameters::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 235556 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmModule that takes its corresponding StorageClass as parameter
 */
SgAsmJvmModule::SgAsmJvmModule(const SgAsmJvmModuleStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 235566 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_module_name_index = storageSource.storageOf_module_name_index;
    p_module_flags = storageSource.storageOf_module_flags;
    p_module_version_index = storageSource.storageOf_module_version_index;
    p_requires = storageSource.storageOf_requires.rebuildDataStoredInEasyStorageClass();
    p_exports = storageSource.storageOf_exports.rebuildDataStoredInEasyStorageClass();
    p_opens = storageSource.storageOf_opens.rebuildDataStoredInEasyStorageClass();
    p_uses_index = storageSource.storageOf_uses_index.rebuildDataStoredInEasyStorageClass();
    p_provides = storageSource.storageOf_provides.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmModule*
SgAsmJvmModule::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmModule)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmModule)};
        unsigned long positionInPool{localIndex % SgAsmJvmModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmModule::pool_size};
        returnPointer = &(((SgAsmJvmModule*)(SgAsmJvmModule::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmModule*
SgAsmJvmModule::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmModule* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmModule)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmModule + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmModule)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmModule)};
        unsigned long positionInPool{localIndex % SgAsmJvmModule::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmModule::pool_size};
        returnPointer = &(((SgAsmJvmModule*)(SgAsmJvmModule::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmModule::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmModule::pools.begin(); block != SgAsmJvmModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmModule*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmModule::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmModule*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmModule::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmModule* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmModule::pools.begin(); block != SgAsmJvmModule::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmModule*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmModule::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmModule::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmModule::clearMemoryPool() {
    SgAsmJvmModule* tempPointer{nullptr};
    if (SgAsmJvmModule::pools.empty() == false) {
        auto block{SgAsmJvmModule::pools.begin()};
        SgAsmJvmModule::next_node = reinterpret_cast<SgAsmJvmModule*>(*block);
        for (; block != SgAsmJvmModule::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmModule*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmModule::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmModule::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmModule::pool_size-1]);
        }
    }
}

void
SgAsmJvmModule::deleteMemoryPool() {
    for (auto p: SgAsmJvmModule::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmModule::next_node = nullptr;
    SgAsmJvmModule::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmModule::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmModule) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmModule)};
    for (size_t blockIndex = SgAsmJvmModule::pools.size(); (blockIndex * SgAsmJvmModule::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmModule) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmModule) = %" PRIuPTR " SgAsmJvmModule::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmModule),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmModule),SgAsmJvmModule::pool_size);
        }
#endif

        SgAsmJvmModule* pointer{static_cast<SgAsmJvmModule*>(ROSE_MALLOC(SgAsmJvmModule::pool_size * sizeof(SgAsmJvmModule)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmModule::pool_size * sizeof(SgAsmJvmModule));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmModule::pool_size * sizeof(SgAsmJvmModule));
#endif
        SgAsmJvmModule::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmModule::pool_size * sizeof(SgAsmJvmModule), V_SgAsmJvmModule));

        if (SgAsmJvmModule::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmModule* blkptr{reinterpret_cast<SgAsmJvmModule*>(SgAsmJvmModule::pools[blockIndex-1])};
                blkptr[SgAsmJvmModule::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmModule::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmModule::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmModule::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmModule::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmModule::pool_size - 1};
    SgAsmJvmModule* testPointer{reinterpret_cast<SgAsmJvmModule*>(SgAsmJvmModule::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmModule::pool_size * (SgAsmJvmModule::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmModuleStroageClass
 * from its counterpart of SgAsmJvmModule. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmModule::initializeStorageClassArray(SgAsmJvmModuleStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmModule::pools.begin(); block != SgAsmJvmModule::pools.end(); ++block) {
        SgAsmJvmModule* pointer{reinterpret_cast<SgAsmJvmModule*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmModule::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 235791 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmRuntimeInvisibleAnnotations that takes its corresponding StorageClass as parameter
 */
SgAsmJvmRuntimeInvisibleAnnotations::SgAsmJvmRuntimeInvisibleAnnotations(const SgAsmJvmRuntimeInvisibleAnnotationsStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 235801 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_annotations = storageSource.storageOf_annotations.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmRuntimeInvisibleAnnotations*
SgAsmJvmRuntimeInvisibleAnnotations::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmRuntimeInvisibleAnnotations* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeInvisibleAnnotations)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeInvisibleAnnotations + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeInvisibleAnnotations)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeInvisibleAnnotations)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeInvisibleAnnotations::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeInvisibleAnnotations::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeInvisibleAnnotations*)(SgAsmJvmRuntimeInvisibleAnnotations::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmRuntimeInvisibleAnnotations*
SgAsmJvmRuntimeInvisibleAnnotations::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmRuntimeInvisibleAnnotations* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeInvisibleAnnotations)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeInvisibleAnnotations + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeInvisibleAnnotations)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmRuntimeInvisibleAnnotations)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeInvisibleAnnotations::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeInvisibleAnnotations::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeInvisibleAnnotations*)(SgAsmJvmRuntimeInvisibleAnnotations::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmRuntimeInvisibleAnnotations::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmRuntimeInvisibleAnnotations::pools.begin(); block != SgAsmJvmRuntimeInvisibleAnnotations::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeInvisibleAnnotations::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmRuntimeInvisibleAnnotations*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmRuntimeInvisibleAnnotations::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmRuntimeInvisibleAnnotations* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmRuntimeInvisibleAnnotations::pools.begin(); block != SgAsmJvmRuntimeInvisibleAnnotations::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeInvisibleAnnotations::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmRuntimeInvisibleAnnotations::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmRuntimeInvisibleAnnotations::clearMemoryPool() {
    SgAsmJvmRuntimeInvisibleAnnotations* tempPointer{nullptr};
    if (SgAsmJvmRuntimeInvisibleAnnotations::pools.empty() == false) {
        auto block{SgAsmJvmRuntimeInvisibleAnnotations::pools.begin()};
        SgAsmJvmRuntimeInvisibleAnnotations::next_node = reinterpret_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(*block);
        for (; block != SgAsmJvmRuntimeInvisibleAnnotations::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmRuntimeInvisibleAnnotations::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmRuntimeInvisibleAnnotations::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmRuntimeInvisibleAnnotations::pool_size-1]);
        }
    }
}

void
SgAsmJvmRuntimeInvisibleAnnotations::deleteMemoryPool() {
    for (auto p: SgAsmJvmRuntimeInvisibleAnnotations::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmRuntimeInvisibleAnnotations::next_node = nullptr;
    SgAsmJvmRuntimeInvisibleAnnotations::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmRuntimeInvisibleAnnotations::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeInvisibleAnnotations) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeInvisibleAnnotations)};
    for (size_t blockIndex = SgAsmJvmRuntimeInvisibleAnnotations::pools.size(); (blockIndex * SgAsmJvmRuntimeInvisibleAnnotations::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeInvisibleAnnotations) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeInvisibleAnnotations) = %" PRIuPTR " SgAsmJvmRuntimeInvisibleAnnotations::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeInvisibleAnnotations),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeInvisibleAnnotations),SgAsmJvmRuntimeInvisibleAnnotations::pool_size);
        }
#endif

        SgAsmJvmRuntimeInvisibleAnnotations* pointer{static_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(ROSE_MALLOC(SgAsmJvmRuntimeInvisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeInvisibleAnnotations)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmRuntimeInvisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeInvisibleAnnotations));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmRuntimeInvisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeInvisibleAnnotations));
#endif
        SgAsmJvmRuntimeInvisibleAnnotations::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmRuntimeInvisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeInvisibleAnnotations), V_SgAsmJvmRuntimeInvisibleAnnotations));

        if (SgAsmJvmRuntimeInvisibleAnnotations::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmRuntimeInvisibleAnnotations* blkptr{reinterpret_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(SgAsmJvmRuntimeInvisibleAnnotations::pools[blockIndex-1])};
                blkptr[SgAsmJvmRuntimeInvisibleAnnotations::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmRuntimeInvisibleAnnotations::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmRuntimeInvisibleAnnotations::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmRuntimeInvisibleAnnotations::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmRuntimeInvisibleAnnotations::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmRuntimeInvisibleAnnotations::pool_size - 1};
    SgAsmJvmRuntimeInvisibleAnnotations* testPointer{reinterpret_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(SgAsmJvmRuntimeInvisibleAnnotations::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmRuntimeInvisibleAnnotations::pool_size * (SgAsmJvmRuntimeInvisibleAnnotations::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmRuntimeInvisibleAnnotationsStroageClass
 * from its counterpart of SgAsmJvmRuntimeInvisibleAnnotations. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmRuntimeInvisibleAnnotations::initializeStorageClassArray(SgAsmJvmRuntimeInvisibleAnnotationsStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmRuntimeInvisibleAnnotations::pools.begin(); block != SgAsmJvmRuntimeInvisibleAnnotations::pools.end(); ++block) {
        SgAsmJvmRuntimeInvisibleAnnotations* pointer{reinterpret_cast<SgAsmJvmRuntimeInvisibleAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeInvisibleAnnotations::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 236019 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmRuntimeVisibilityParamAnnotations that takes its corresponding StorageClass as parameter
 */
SgAsmJvmRuntimeVisibilityParamAnnotations::SgAsmJvmRuntimeVisibilityParamAnnotations(const SgAsmJvmRuntimeVisibilityParamAnnotationsStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 236029 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_parameter_annotations = storageSource.storageOf_parameter_annotations.rebuildDataStoredInEasyStorageClass();
    p_isVisible = storageSource.storageOf_isVisible;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmRuntimeVisibilityParamAnnotations*
SgAsmJvmRuntimeVisibilityParamAnnotations::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmRuntimeVisibilityParamAnnotations* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibilityParamAnnotations)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibilityParamAnnotations + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibilityParamAnnotations)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibilityParamAnnotations)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeVisibilityParamAnnotations*)(SgAsmJvmRuntimeVisibilityParamAnnotations::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmRuntimeVisibilityParamAnnotations*
SgAsmJvmRuntimeVisibilityParamAnnotations::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmRuntimeVisibilityParamAnnotations* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeVisibilityParamAnnotations)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeVisibilityParamAnnotations + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeVisibilityParamAnnotations)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmRuntimeVisibilityParamAnnotations)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeVisibilityParamAnnotations*)(SgAsmJvmRuntimeVisibilityParamAnnotations::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmRuntimeVisibilityParamAnnotations::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmRuntimeVisibilityParamAnnotations::pools.begin(); block != SgAsmJvmRuntimeVisibilityParamAnnotations::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmRuntimeVisibilityParamAnnotations*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmRuntimeVisibilityParamAnnotations::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmRuntimeVisibilityParamAnnotations* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmRuntimeVisibilityParamAnnotations::pools.begin(); block != SgAsmJvmRuntimeVisibilityParamAnnotations::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmRuntimeVisibilityParamAnnotations::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmRuntimeVisibilityParamAnnotations::clearMemoryPool() {
    SgAsmJvmRuntimeVisibilityParamAnnotations* tempPointer{nullptr};
    if (SgAsmJvmRuntimeVisibilityParamAnnotations::pools.empty() == false) {
        auto block{SgAsmJvmRuntimeVisibilityParamAnnotations::pools.begin()};
        SgAsmJvmRuntimeVisibilityParamAnnotations::next_node = reinterpret_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(*block);
        for (; block != SgAsmJvmRuntimeVisibilityParamAnnotations::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size-1]);
        }
    }
}

void
SgAsmJvmRuntimeVisibilityParamAnnotations::deleteMemoryPool() {
    for (auto p: SgAsmJvmRuntimeVisibilityParamAnnotations::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmRuntimeVisibilityParamAnnotations::next_node = nullptr;
    SgAsmJvmRuntimeVisibilityParamAnnotations::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmRuntimeVisibilityParamAnnotations::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibilityParamAnnotations) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibilityParamAnnotations)};
    for (size_t blockIndex = SgAsmJvmRuntimeVisibilityParamAnnotations::pools.size(); (blockIndex * SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibilityParamAnnotations) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibilityParamAnnotations) = %" PRIuPTR " SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibilityParamAnnotations),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibilityParamAnnotations),SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size);
        }
#endif

        SgAsmJvmRuntimeVisibilityParamAnnotations* pointer{static_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(ROSE_MALLOC(SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibilityParamAnnotations)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibilityParamAnnotations));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibilityParamAnnotations));
#endif
        SgAsmJvmRuntimeVisibilityParamAnnotations::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibilityParamAnnotations), V_SgAsmJvmRuntimeVisibilityParamAnnotations));

        if (SgAsmJvmRuntimeVisibilityParamAnnotations::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmRuntimeVisibilityParamAnnotations* blkptr{reinterpret_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(SgAsmJvmRuntimeVisibilityParamAnnotations::pools[blockIndex-1])};
                blkptr[SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmRuntimeVisibilityParamAnnotations::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmRuntimeVisibilityParamAnnotations::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size - 1};
    SgAsmJvmRuntimeVisibilityParamAnnotations* testPointer{reinterpret_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(SgAsmJvmRuntimeVisibilityParamAnnotations::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size * (SgAsmJvmRuntimeVisibilityParamAnnotations::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmRuntimeVisibilityParamAnnotationsStroageClass
 * from its counterpart of SgAsmJvmRuntimeVisibilityParamAnnotations. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmRuntimeVisibilityParamAnnotations::initializeStorageClassArray(SgAsmJvmRuntimeVisibilityParamAnnotationsStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmRuntimeVisibilityParamAnnotations::pools.begin(); block != SgAsmJvmRuntimeVisibilityParamAnnotations::pools.end(); ++block) {
        SgAsmJvmRuntimeVisibilityParamAnnotations* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibilityParamAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 236248 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmRuntimeVisibleAnnotations that takes its corresponding StorageClass as parameter
 */
SgAsmJvmRuntimeVisibleAnnotations::SgAsmJvmRuntimeVisibleAnnotations(const SgAsmJvmRuntimeVisibleAnnotationsStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 236258 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_annotations = storageSource.storageOf_annotations.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmRuntimeVisibleAnnotations*
SgAsmJvmRuntimeVisibleAnnotations::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmRuntimeVisibleAnnotations* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibleAnnotations)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibleAnnotations + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibleAnnotations)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibleAnnotations)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeVisibleAnnotations::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeVisibleAnnotations::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeVisibleAnnotations*)(SgAsmJvmRuntimeVisibleAnnotations::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmRuntimeVisibleAnnotations*
SgAsmJvmRuntimeVisibleAnnotations::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmRuntimeVisibleAnnotations* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeVisibleAnnotations)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeVisibleAnnotations + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeVisibleAnnotations)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmRuntimeVisibleAnnotations)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeVisibleAnnotations::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeVisibleAnnotations::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeVisibleAnnotations*)(SgAsmJvmRuntimeVisibleAnnotations::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmRuntimeVisibleAnnotations::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmRuntimeVisibleAnnotations::pools.begin(); block != SgAsmJvmRuntimeVisibleAnnotations::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibleAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibleAnnotations::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmRuntimeVisibleAnnotations*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmRuntimeVisibleAnnotations::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmRuntimeVisibleAnnotations* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmRuntimeVisibleAnnotations::pools.begin(); block != SgAsmJvmRuntimeVisibleAnnotations::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibleAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibleAnnotations::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmRuntimeVisibleAnnotations::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmRuntimeVisibleAnnotations::clearMemoryPool() {
    SgAsmJvmRuntimeVisibleAnnotations* tempPointer{nullptr};
    if (SgAsmJvmRuntimeVisibleAnnotations::pools.empty() == false) {
        auto block{SgAsmJvmRuntimeVisibleAnnotations::pools.begin()};
        SgAsmJvmRuntimeVisibleAnnotations::next_node = reinterpret_cast<SgAsmJvmRuntimeVisibleAnnotations*>(*block);
        for (; block != SgAsmJvmRuntimeVisibleAnnotations::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibleAnnotations*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmRuntimeVisibleAnnotations::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmRuntimeVisibleAnnotations::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmRuntimeVisibleAnnotations::pool_size-1]);
        }
    }
}

void
SgAsmJvmRuntimeVisibleAnnotations::deleteMemoryPool() {
    for (auto p: SgAsmJvmRuntimeVisibleAnnotations::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmRuntimeVisibleAnnotations::next_node = nullptr;
    SgAsmJvmRuntimeVisibleAnnotations::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmRuntimeVisibleAnnotations::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibleAnnotations) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibleAnnotations)};
    for (size_t blockIndex = SgAsmJvmRuntimeVisibleAnnotations::pools.size(); (blockIndex * SgAsmJvmRuntimeVisibleAnnotations::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibleAnnotations) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibleAnnotations) = %" PRIuPTR " SgAsmJvmRuntimeVisibleAnnotations::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeVisibleAnnotations),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeVisibleAnnotations),SgAsmJvmRuntimeVisibleAnnotations::pool_size);
        }
#endif

        SgAsmJvmRuntimeVisibleAnnotations* pointer{static_cast<SgAsmJvmRuntimeVisibleAnnotations*>(ROSE_MALLOC(SgAsmJvmRuntimeVisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibleAnnotations)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmRuntimeVisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibleAnnotations));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmRuntimeVisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibleAnnotations));
#endif
        SgAsmJvmRuntimeVisibleAnnotations::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmRuntimeVisibleAnnotations::pool_size * sizeof(SgAsmJvmRuntimeVisibleAnnotations), V_SgAsmJvmRuntimeVisibleAnnotations));

        if (SgAsmJvmRuntimeVisibleAnnotations::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmRuntimeVisibleAnnotations* blkptr{reinterpret_cast<SgAsmJvmRuntimeVisibleAnnotations*>(SgAsmJvmRuntimeVisibleAnnotations::pools[blockIndex-1])};
                blkptr[SgAsmJvmRuntimeVisibleAnnotations::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmRuntimeVisibleAnnotations::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibleAnnotations::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmRuntimeVisibleAnnotations::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmRuntimeVisibleAnnotations::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmRuntimeVisibleAnnotations::pool_size - 1};
    SgAsmJvmRuntimeVisibleAnnotations* testPointer{reinterpret_cast<SgAsmJvmRuntimeVisibleAnnotations*>(SgAsmJvmRuntimeVisibleAnnotations::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmRuntimeVisibleAnnotations::pool_size * (SgAsmJvmRuntimeVisibleAnnotations::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmRuntimeVisibleAnnotationsStroageClass
 * from its counterpart of SgAsmJvmRuntimeVisibleAnnotations. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmRuntimeVisibleAnnotations::initializeStorageClassArray(SgAsmJvmRuntimeVisibleAnnotationsStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmRuntimeVisibleAnnotations::pools.begin(); block != SgAsmJvmRuntimeVisibleAnnotations::pools.end(); ++block) {
        SgAsmJvmRuntimeVisibleAnnotations* pointer{reinterpret_cast<SgAsmJvmRuntimeVisibleAnnotations*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeVisibleAnnotations::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 236476 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmStackMapTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmStackMapTable::SgAsmJvmStackMapTable(const SgAsmJvmStackMapTableStorageClass& storageSource)  : SgAsmJvmAttribute (storageSource) {

/* #line 236486 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_entries = storageSource.storageOf_entries.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmStackMapFramePtrList::iterator i_entries = p_entries.begin();
    for ( ; i_entries != p_entries.end(); ++i_entries) {
        (*i_entries) = (SgAsmJvmStackMapFramePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_entries)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmStackMapTable*
SgAsmJvmStackMapTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmStackMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmStackMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmStackMapTable::pool_size};
        returnPointer = &(((SgAsmJvmStackMapTable*)(SgAsmJvmStackMapTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmStackMapTable*
SgAsmJvmStackMapTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmStackMapTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmStackMapTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmStackMapTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmStackMapTable::pool_size};
        returnPointer = &(((SgAsmJvmStackMapTable*)(SgAsmJvmStackMapTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmStackMapTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmStackMapTable::pools.begin(); block != SgAsmJvmStackMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmStackMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmStackMapTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmStackMapTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmStackMapTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmStackMapTable::pools.begin(); block != SgAsmJvmStackMapTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmStackMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmStackMapTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmStackMapTable::clearMemoryPool() {
    SgAsmJvmStackMapTable* tempPointer{nullptr};
    if (SgAsmJvmStackMapTable::pools.empty() == false) {
        auto block{SgAsmJvmStackMapTable::pools.begin()};
        SgAsmJvmStackMapTable::next_node = reinterpret_cast<SgAsmJvmStackMapTable*>(*block);
        for (; block != SgAsmJvmStackMapTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmStackMapTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmStackMapTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmStackMapTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmStackMapTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmStackMapTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmStackMapTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmStackMapTable::next_node = nullptr;
    SgAsmJvmStackMapTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmStackMapTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapTable)};
    for (size_t blockIndex = SgAsmJvmStackMapTable::pools.size(); (blockIndex * SgAsmJvmStackMapTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapTable) = %" PRIuPTR " SgAsmJvmStackMapTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapTable),SgAsmJvmStackMapTable::pool_size);
        }
#endif

        SgAsmJvmStackMapTable* pointer{static_cast<SgAsmJvmStackMapTable*>(ROSE_MALLOC(SgAsmJvmStackMapTable::pool_size * sizeof(SgAsmJvmStackMapTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmStackMapTable::pool_size * sizeof(SgAsmJvmStackMapTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmStackMapTable::pool_size * sizeof(SgAsmJvmStackMapTable));
#endif
        SgAsmJvmStackMapTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmStackMapTable::pool_size * sizeof(SgAsmJvmStackMapTable), V_SgAsmJvmStackMapTable));

        if (SgAsmJvmStackMapTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmStackMapTable* blkptr{reinterpret_cast<SgAsmJvmStackMapTable*>(SgAsmJvmStackMapTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmStackMapTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmStackMapTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmStackMapTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmStackMapTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmStackMapTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmStackMapTable::pool_size - 1};
    SgAsmJvmStackMapTable* testPointer{reinterpret_cast<SgAsmJvmStackMapTable*>(SgAsmJvmStackMapTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmStackMapTable::pool_size * (SgAsmJvmStackMapTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmStackMapTableStroageClass
 * from its counterpart of SgAsmJvmStackMapTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmStackMapTable::initializeStorageClassArray(SgAsmJvmStackMapTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmStackMapTable::pools.begin(); block != SgAsmJvmStackMapTable::pools.end(); ++block) {
        SgAsmJvmStackMapTable* pointer{reinterpret_cast<SgAsmJvmStackMapTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 236708 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmBootstrapMethod that takes its corresponding StorageClass as parameter
 */
SgAsmJvmBootstrapMethod::SgAsmJvmBootstrapMethod(const SgAsmJvmBootstrapMethodStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 236718 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_bootstrap_method_ref = storageSource.storageOf_bootstrap_method_ref;
    p_bootstrap_arguments = storageSource.storageOf_bootstrap_arguments.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmBootstrapMethod*
SgAsmJvmBootstrapMethod::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmBootstrapMethod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethod)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethod)};
        unsigned long positionInPool{localIndex % SgAsmJvmBootstrapMethod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmBootstrapMethod::pool_size};
        returnPointer = &(((SgAsmJvmBootstrapMethod*)(SgAsmJvmBootstrapMethod::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmBootstrapMethod*
SgAsmJvmBootstrapMethod::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmBootstrapMethod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmBootstrapMethod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmBootstrapMethod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmBootstrapMethod)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmBootstrapMethod)};
        unsigned long positionInPool{localIndex % SgAsmJvmBootstrapMethod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmBootstrapMethod::pool_size};
        returnPointer = &(((SgAsmJvmBootstrapMethod*)(SgAsmJvmBootstrapMethod::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmBootstrapMethod::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmBootstrapMethod::pools.begin(); block != SgAsmJvmBootstrapMethod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmBootstrapMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmBootstrapMethod::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmBootstrapMethod*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmBootstrapMethod::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmBootstrapMethod* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmBootstrapMethod::pools.begin(); block != SgAsmJvmBootstrapMethod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmBootstrapMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmBootstrapMethod::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmBootstrapMethod::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmBootstrapMethod::clearMemoryPool() {
    SgAsmJvmBootstrapMethod* tempPointer{nullptr};
    if (SgAsmJvmBootstrapMethod::pools.empty() == false) {
        auto block{SgAsmJvmBootstrapMethod::pools.begin()};
        SgAsmJvmBootstrapMethod::next_node = reinterpret_cast<SgAsmJvmBootstrapMethod*>(*block);
        for (; block != SgAsmJvmBootstrapMethod::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmBootstrapMethod*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmBootstrapMethod::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmBootstrapMethod::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmBootstrapMethod::pool_size-1]);
        }
    }
}

void
SgAsmJvmBootstrapMethod::deleteMemoryPool() {
    for (auto p: SgAsmJvmBootstrapMethod::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmBootstrapMethod::next_node = nullptr;
    SgAsmJvmBootstrapMethod::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmBootstrapMethod::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethod) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethod)};
    for (size_t blockIndex = SgAsmJvmBootstrapMethod::pools.size(); (blockIndex * SgAsmJvmBootstrapMethod::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethod) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethod) = %" PRIuPTR " SgAsmJvmBootstrapMethod::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmBootstrapMethod),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmBootstrapMethod),SgAsmJvmBootstrapMethod::pool_size);
        }
#endif

        SgAsmJvmBootstrapMethod* pointer{static_cast<SgAsmJvmBootstrapMethod*>(ROSE_MALLOC(SgAsmJvmBootstrapMethod::pool_size * sizeof(SgAsmJvmBootstrapMethod)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmBootstrapMethod::pool_size * sizeof(SgAsmJvmBootstrapMethod));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmBootstrapMethod::pool_size * sizeof(SgAsmJvmBootstrapMethod));
#endif
        SgAsmJvmBootstrapMethod::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmBootstrapMethod::pool_size * sizeof(SgAsmJvmBootstrapMethod), V_SgAsmJvmBootstrapMethod));

        if (SgAsmJvmBootstrapMethod::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmBootstrapMethod* blkptr{reinterpret_cast<SgAsmJvmBootstrapMethod*>(SgAsmJvmBootstrapMethod::pools[blockIndex-1])};
                blkptr[SgAsmJvmBootstrapMethod::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmBootstrapMethod::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmBootstrapMethod::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmBootstrapMethod::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmBootstrapMethod::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmBootstrapMethod::pool_size - 1};
    SgAsmJvmBootstrapMethod* testPointer{reinterpret_cast<SgAsmJvmBootstrapMethod*>(SgAsmJvmBootstrapMethod::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmBootstrapMethod::pool_size * (SgAsmJvmBootstrapMethod::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmBootstrapMethodStroageClass
 * from its counterpart of SgAsmJvmBootstrapMethod. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmBootstrapMethod::initializeStorageClassArray(SgAsmJvmBootstrapMethodStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmBootstrapMethod::pools.begin(); block != SgAsmJvmBootstrapMethod::pools.end(); ++block) {
        SgAsmJvmBootstrapMethod* pointer{reinterpret_cast<SgAsmJvmBootstrapMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmBootstrapMethod::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 236937 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmClass that takes its corresponding StorageClass as parameter
 */
SgAsmJvmClass::SgAsmJvmClass(const SgAsmJvmClassStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 236947 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmClass*
SgAsmJvmClass::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmClass)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmClass)};
        unsigned long positionInPool{localIndex % SgAsmJvmClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmClass::pool_size};
        returnPointer = &(((SgAsmJvmClass*)(SgAsmJvmClass::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmClass*
SgAsmJvmClass::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmClass* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmClass)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmClass + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmClass)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmClass)};
        unsigned long positionInPool{localIndex % SgAsmJvmClass::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmClass::pool_size};
        returnPointer = &(((SgAsmJvmClass*)(SgAsmJvmClass::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmClass::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmClass::pools.begin(); block != SgAsmJvmClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmClass*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmClass::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmClass*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmClass::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmClass* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmClass::pools.begin(); block != SgAsmJvmClass::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmClass*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmClass::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmClass::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmClass::clearMemoryPool() {
    SgAsmJvmClass* tempPointer{nullptr};
    if (SgAsmJvmClass::pools.empty() == false) {
        auto block{SgAsmJvmClass::pools.begin()};
        SgAsmJvmClass::next_node = reinterpret_cast<SgAsmJvmClass*>(*block);
        for (; block != SgAsmJvmClass::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmClass*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmClass::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmClass::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmClass::pool_size-1]);
        }
    }
}

void
SgAsmJvmClass::deleteMemoryPool() {
    for (auto p: SgAsmJvmClass::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmClass::next_node = nullptr;
    SgAsmJvmClass::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmClass::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmClass) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmClass)};
    for (size_t blockIndex = SgAsmJvmClass::pools.size(); (blockIndex * SgAsmJvmClass::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmClass) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmClass) = %" PRIuPTR " SgAsmJvmClass::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmClass),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmClass),SgAsmJvmClass::pool_size);
        }
#endif

        SgAsmJvmClass* pointer{static_cast<SgAsmJvmClass*>(ROSE_MALLOC(SgAsmJvmClass::pool_size * sizeof(SgAsmJvmClass)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmClass::pool_size * sizeof(SgAsmJvmClass));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmClass::pool_size * sizeof(SgAsmJvmClass));
#endif
        SgAsmJvmClass::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmClass::pool_size * sizeof(SgAsmJvmClass), V_SgAsmJvmClass));

        if (SgAsmJvmClass::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmClass* blkptr{reinterpret_cast<SgAsmJvmClass*>(SgAsmJvmClass::pools[blockIndex-1])};
                blkptr[SgAsmJvmClass::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmClass::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmClass::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmClass::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmClass::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmClass::pool_size - 1};
    SgAsmJvmClass* testPointer{reinterpret_cast<SgAsmJvmClass*>(SgAsmJvmClass::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmClass::pool_size * (SgAsmJvmClass::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmClassStroageClass
 * from its counterpart of SgAsmJvmClass. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmClass::initializeStorageClassArray(SgAsmJvmClassStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmClass::pools.begin(); block != SgAsmJvmClass::pools.end(); ++block) {
        SgAsmJvmClass* pointer{reinterpret_cast<SgAsmJvmClass*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmClass::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 237164 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmConstantPoolEntry that takes its corresponding StorageClass as parameter
 */
SgAsmJvmConstantPoolEntry::SgAsmJvmConstantPoolEntry(const SgAsmJvmConstantPoolEntryStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 237174 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_tag = storageSource.storageOf_tag;
    p_bytes = storageSource.storageOf_bytes;
    p_hi_bytes = storageSource.storageOf_hi_bytes;
    p_low_bytes = storageSource.storageOf_low_bytes;
    p_bootstrap_method_attr_index = storageSource.storageOf_bootstrap_method_attr_index;
    p_class_index = storageSource.storageOf_class_index;
    p_descriptor_index = storageSource.storageOf_descriptor_index;
    p_name_index = storageSource.storageOf_name_index;
    p_name_and_type_index = storageSource.storageOf_name_and_type_index;
    p_reference_index = storageSource.storageOf_reference_index;
    p_reference_kind = storageSource.storageOf_reference_kind;
    p_string_index = storageSource.storageOf_string_index;
    p_length = storageSource.storageOf_length;
     p_utf8_bytes = const_cast<char* >( storageSource.storageOf_utf8_bytes.rebuildDataStoredInEasyStorageClass());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmConstantPoolEntry*
SgAsmJvmConstantPoolEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmConstantPoolEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmConstantPoolEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmConstantPoolEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmConstantPoolEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPoolEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmConstantPoolEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmConstantPoolEntry::pool_size};
        returnPointer = &(((SgAsmJvmConstantPoolEntry*)(SgAsmJvmConstantPoolEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmConstantPoolEntry*
SgAsmJvmConstantPoolEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmConstantPoolEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmConstantPoolEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmConstantPoolEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmConstantPoolEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmConstantPoolEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmConstantPoolEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmConstantPoolEntry::pool_size};
        returnPointer = &(((SgAsmJvmConstantPoolEntry*)(SgAsmJvmConstantPoolEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmConstantPoolEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmConstantPoolEntry::pools.begin(); block != SgAsmJvmConstantPoolEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmConstantPoolEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmConstantPoolEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmConstantPoolEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmConstantPoolEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmConstantPoolEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmConstantPoolEntry::pools.begin(); block != SgAsmJvmConstantPoolEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmConstantPoolEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmConstantPoolEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmConstantPoolEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmConstantPoolEntry::clearMemoryPool() {
    SgAsmJvmConstantPoolEntry* tempPointer{nullptr};
    if (SgAsmJvmConstantPoolEntry::pools.empty() == false) {
        auto block{SgAsmJvmConstantPoolEntry::pools.begin()};
        SgAsmJvmConstantPoolEntry::next_node = reinterpret_cast<SgAsmJvmConstantPoolEntry*>(*block);
        for (; block != SgAsmJvmConstantPoolEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmConstantPoolEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmConstantPoolEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmConstantPoolEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmConstantPoolEntry::pool_size-1]);
        }
    }
}

void
SgAsmJvmConstantPoolEntry::deleteMemoryPool() {
    for (auto p: SgAsmJvmConstantPoolEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmConstantPoolEntry::next_node = nullptr;
    SgAsmJvmConstantPoolEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmConstantPoolEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPoolEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmConstantPoolEntry)};
    for (size_t blockIndex = SgAsmJvmConstantPoolEntry::pools.size(); (blockIndex * SgAsmJvmConstantPoolEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPoolEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmConstantPoolEntry) = %" PRIuPTR " SgAsmJvmConstantPoolEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmConstantPoolEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmConstantPoolEntry),SgAsmJvmConstantPoolEntry::pool_size);
        }
#endif

        SgAsmJvmConstantPoolEntry* pointer{static_cast<SgAsmJvmConstantPoolEntry*>(ROSE_MALLOC(SgAsmJvmConstantPoolEntry::pool_size * sizeof(SgAsmJvmConstantPoolEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmConstantPoolEntry::pool_size * sizeof(SgAsmJvmConstantPoolEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmConstantPoolEntry::pool_size * sizeof(SgAsmJvmConstantPoolEntry));
#endif
        SgAsmJvmConstantPoolEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmConstantPoolEntry::pool_size * sizeof(SgAsmJvmConstantPoolEntry), V_SgAsmJvmConstantPoolEntry));

        if (SgAsmJvmConstantPoolEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmConstantPoolEntry* blkptr{reinterpret_cast<SgAsmJvmConstantPoolEntry*>(SgAsmJvmConstantPoolEntry::pools[blockIndex-1])};
                blkptr[SgAsmJvmConstantPoolEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmConstantPoolEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmConstantPoolEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmConstantPoolEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmConstantPoolEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmConstantPoolEntry::pool_size - 1};
    SgAsmJvmConstantPoolEntry* testPointer{reinterpret_cast<SgAsmJvmConstantPoolEntry*>(SgAsmJvmConstantPoolEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmConstantPoolEntry::pool_size * (SgAsmJvmConstantPoolEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmConstantPoolEntryStroageClass
 * from its counterpart of SgAsmJvmConstantPoolEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmConstantPoolEntry::initializeStorageClassArray(SgAsmJvmConstantPoolEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmConstantPoolEntry::pools.begin(); block != SgAsmJvmConstantPoolEntry::pools.end(); ++block) {
        SgAsmJvmConstantPoolEntry* pointer{reinterpret_cast<SgAsmJvmConstantPoolEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmConstantPoolEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 237405 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmExceptionHandler that takes its corresponding StorageClass as parameter
 */
SgAsmJvmExceptionHandler::SgAsmJvmExceptionHandler(const SgAsmJvmExceptionHandlerStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 237415 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_start_pc = storageSource.storageOf_start_pc;
    p_end_pc = storageSource.storageOf_end_pc;
    p_handler_pc = storageSource.storageOf_handler_pc;
    p_catch_type = storageSource.storageOf_catch_type;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmExceptionHandler*
SgAsmJvmExceptionHandler::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmExceptionHandler* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmExceptionHandler)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmExceptionHandler + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmExceptionHandler)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionHandler)};
        unsigned long positionInPool{localIndex % SgAsmJvmExceptionHandler::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmExceptionHandler::pool_size};
        returnPointer = &(((SgAsmJvmExceptionHandler*)(SgAsmJvmExceptionHandler::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmExceptionHandler*
SgAsmJvmExceptionHandler::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmExceptionHandler* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmExceptionHandler)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmExceptionHandler + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmExceptionHandler)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmExceptionHandler)};
        unsigned long positionInPool{localIndex % SgAsmJvmExceptionHandler::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmExceptionHandler::pool_size};
        returnPointer = &(((SgAsmJvmExceptionHandler*)(SgAsmJvmExceptionHandler::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmExceptionHandler::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmExceptionHandler::pools.begin(); block != SgAsmJvmExceptionHandler::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmExceptionHandler*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmExceptionHandler::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmExceptionHandler*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmExceptionHandler::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmExceptionHandler* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmExceptionHandler::pools.begin(); block != SgAsmJvmExceptionHandler::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmExceptionHandler*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmExceptionHandler::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmExceptionHandler::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmExceptionHandler::clearMemoryPool() {
    SgAsmJvmExceptionHandler* tempPointer{nullptr};
    if (SgAsmJvmExceptionHandler::pools.empty() == false) {
        auto block{SgAsmJvmExceptionHandler::pools.begin()};
        SgAsmJvmExceptionHandler::next_node = reinterpret_cast<SgAsmJvmExceptionHandler*>(*block);
        for (; block != SgAsmJvmExceptionHandler::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmExceptionHandler*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmExceptionHandler::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmExceptionHandler::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmExceptionHandler::pool_size-1]);
        }
    }
}

void
SgAsmJvmExceptionHandler::deleteMemoryPool() {
    for (auto p: SgAsmJvmExceptionHandler::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmExceptionHandler::next_node = nullptr;
    SgAsmJvmExceptionHandler::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmExceptionHandler::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionHandler) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmExceptionHandler)};
    for (size_t blockIndex = SgAsmJvmExceptionHandler::pools.size(); (blockIndex * SgAsmJvmExceptionHandler::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionHandler) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmExceptionHandler) = %" PRIuPTR " SgAsmJvmExceptionHandler::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionHandler),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmExceptionHandler),SgAsmJvmExceptionHandler::pool_size);
        }
#endif

        SgAsmJvmExceptionHandler* pointer{static_cast<SgAsmJvmExceptionHandler*>(ROSE_MALLOC(SgAsmJvmExceptionHandler::pool_size * sizeof(SgAsmJvmExceptionHandler)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmExceptionHandler::pool_size * sizeof(SgAsmJvmExceptionHandler));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmExceptionHandler::pool_size * sizeof(SgAsmJvmExceptionHandler));
#endif
        SgAsmJvmExceptionHandler::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmExceptionHandler::pool_size * sizeof(SgAsmJvmExceptionHandler), V_SgAsmJvmExceptionHandler));

        if (SgAsmJvmExceptionHandler::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmExceptionHandler* blkptr{reinterpret_cast<SgAsmJvmExceptionHandler*>(SgAsmJvmExceptionHandler::pools[blockIndex-1])};
                blkptr[SgAsmJvmExceptionHandler::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmExceptionHandler::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmExceptionHandler::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmExceptionHandler::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmExceptionHandler::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmExceptionHandler::pool_size - 1};
    SgAsmJvmExceptionHandler* testPointer{reinterpret_cast<SgAsmJvmExceptionHandler*>(SgAsmJvmExceptionHandler::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmExceptionHandler::pool_size * (SgAsmJvmExceptionHandler::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmExceptionHandlerStroageClass
 * from its counterpart of SgAsmJvmExceptionHandler. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmExceptionHandler::initializeStorageClassArray(SgAsmJvmExceptionHandlerStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmExceptionHandler::pools.begin(); block != SgAsmJvmExceptionHandler::pools.end(); ++block) {
        SgAsmJvmExceptionHandler* pointer{reinterpret_cast<SgAsmJvmExceptionHandler*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmExceptionHandler::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 237636 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmExceptionTable that takes its corresponding StorageClass as parameter
 */
SgAsmJvmExceptionTable::SgAsmJvmExceptionTable(const SgAsmJvmExceptionTableStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 237646 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_handlers = storageSource.storageOf_handlers.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmExceptionHandlerPtrList::iterator i_handlers = p_handlers.begin();
    for ( ; i_handlers != p_handlers.end(); ++i_handlers) {
        (*i_handlers) = (SgAsmJvmExceptionHandlerPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_handlers)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmExceptionTable*
SgAsmJvmExceptionTable::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmExceptionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmExceptionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmExceptionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmExceptionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmExceptionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmExceptionTable::pool_size};
        returnPointer = &(((SgAsmJvmExceptionTable*)(SgAsmJvmExceptionTable::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmExceptionTable*
SgAsmJvmExceptionTable::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmExceptionTable* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmExceptionTable)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmExceptionTable + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmExceptionTable)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmExceptionTable)};
        unsigned long positionInPool{localIndex % SgAsmJvmExceptionTable::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmExceptionTable::pool_size};
        returnPointer = &(((SgAsmJvmExceptionTable*)(SgAsmJvmExceptionTable::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmExceptionTable::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmExceptionTable::pools.begin(); block != SgAsmJvmExceptionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmExceptionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmExceptionTable::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmExceptionTable*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmExceptionTable::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmExceptionTable* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmExceptionTable::pools.begin(); block != SgAsmJvmExceptionTable::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmExceptionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmExceptionTable::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmExceptionTable::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmExceptionTable::clearMemoryPool() {
    SgAsmJvmExceptionTable* tempPointer{nullptr};
    if (SgAsmJvmExceptionTable::pools.empty() == false) {
        auto block{SgAsmJvmExceptionTable::pools.begin()};
        SgAsmJvmExceptionTable::next_node = reinterpret_cast<SgAsmJvmExceptionTable*>(*block);
        for (; block != SgAsmJvmExceptionTable::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmExceptionTable*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmExceptionTable::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmExceptionTable::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmExceptionTable::pool_size-1]);
        }
    }
}

void
SgAsmJvmExceptionTable::deleteMemoryPool() {
    for (auto p: SgAsmJvmExceptionTable::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmExceptionTable::next_node = nullptr;
    SgAsmJvmExceptionTable::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmExceptionTable::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionTable) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmExceptionTable)};
    for (size_t blockIndex = SgAsmJvmExceptionTable::pools.size(); (blockIndex * SgAsmJvmExceptionTable::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionTable) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmExceptionTable) = %" PRIuPTR " SgAsmJvmExceptionTable::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmExceptionTable),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmExceptionTable),SgAsmJvmExceptionTable::pool_size);
        }
#endif

        SgAsmJvmExceptionTable* pointer{static_cast<SgAsmJvmExceptionTable*>(ROSE_MALLOC(SgAsmJvmExceptionTable::pool_size * sizeof(SgAsmJvmExceptionTable)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmExceptionTable::pool_size * sizeof(SgAsmJvmExceptionTable));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmExceptionTable::pool_size * sizeof(SgAsmJvmExceptionTable));
#endif
        SgAsmJvmExceptionTable::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmExceptionTable::pool_size * sizeof(SgAsmJvmExceptionTable), V_SgAsmJvmExceptionTable));

        if (SgAsmJvmExceptionTable::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmExceptionTable* blkptr{reinterpret_cast<SgAsmJvmExceptionTable*>(SgAsmJvmExceptionTable::pools[blockIndex-1])};
                blkptr[SgAsmJvmExceptionTable::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmExceptionTable::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmExceptionTable::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmExceptionTable::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmExceptionTable::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmExceptionTable::pool_size - 1};
    SgAsmJvmExceptionTable* testPointer{reinterpret_cast<SgAsmJvmExceptionTable*>(SgAsmJvmExceptionTable::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmExceptionTable::pool_size * (SgAsmJvmExceptionTable::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmExceptionTableStroageClass
 * from its counterpart of SgAsmJvmExceptionTable. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmExceptionTable::initializeStorageClassArray(SgAsmJvmExceptionTableStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmExceptionTable::pools.begin(); block != SgAsmJvmExceptionTable::pools.end(); ++block) {
        SgAsmJvmExceptionTable* pointer{reinterpret_cast<SgAsmJvmExceptionTable*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmExceptionTable::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 237868 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmField that takes its corresponding StorageClass as parameter
 */
SgAsmJvmField::SgAsmJvmField(const SgAsmJvmFieldStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 237878 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_access_flags = storageSource.storageOf_access_flags;
    p_name_index = storageSource.storageOf_name_index;
    p_descriptor_index = storageSource.storageOf_descriptor_index;
     p_attribute_table = (SgAsmJvmAttributeTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_attribute_table));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmField*
SgAsmJvmField::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmField* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmField)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmField + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmField)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmField)};
        unsigned long positionInPool{localIndex % SgAsmJvmField::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmField::pool_size};
        returnPointer = &(((SgAsmJvmField*)(SgAsmJvmField::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmField*
SgAsmJvmField::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmField* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmField)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmField + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmField)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmField)};
        unsigned long positionInPool{localIndex % SgAsmJvmField::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmField::pool_size};
        returnPointer = &(((SgAsmJvmField*)(SgAsmJvmField::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmField::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmField::pools.begin(); block != SgAsmJvmField::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmField*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmField::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmField*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmField::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmField* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmField::pools.begin(); block != SgAsmJvmField::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmField*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmField::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmField::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmField::clearMemoryPool() {
    SgAsmJvmField* tempPointer{nullptr};
    if (SgAsmJvmField::pools.empty() == false) {
        auto block{SgAsmJvmField::pools.begin()};
        SgAsmJvmField::next_node = reinterpret_cast<SgAsmJvmField*>(*block);
        for (; block != SgAsmJvmField::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmField*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmField::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmField::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmField::pool_size-1]);
        }
    }
}

void
SgAsmJvmField::deleteMemoryPool() {
    for (auto p: SgAsmJvmField::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmField::next_node = nullptr;
    SgAsmJvmField::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmField::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmField) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmField)};
    for (size_t blockIndex = SgAsmJvmField::pools.size(); (blockIndex * SgAsmJvmField::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmField) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmField) = %" PRIuPTR " SgAsmJvmField::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmField),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmField),SgAsmJvmField::pool_size);
        }
#endif

        SgAsmJvmField* pointer{static_cast<SgAsmJvmField*>(ROSE_MALLOC(SgAsmJvmField::pool_size * sizeof(SgAsmJvmField)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmField::pool_size * sizeof(SgAsmJvmField));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmField::pool_size * sizeof(SgAsmJvmField));
#endif
        SgAsmJvmField::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmField::pool_size * sizeof(SgAsmJvmField), V_SgAsmJvmField));

        if (SgAsmJvmField::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmField* blkptr{reinterpret_cast<SgAsmJvmField*>(SgAsmJvmField::pools[blockIndex-1])};
                blkptr[SgAsmJvmField::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmField::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmField::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmField::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmField::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmField::pool_size - 1};
    SgAsmJvmField* testPointer{reinterpret_cast<SgAsmJvmField*>(SgAsmJvmField::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmField::pool_size * (SgAsmJvmField::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmFieldStroageClass
 * from its counterpart of SgAsmJvmField. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmField::initializeStorageClassArray(SgAsmJvmFieldStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmField::pools.begin(); block != SgAsmJvmField::pools.end(); ++block) {
        SgAsmJvmField* pointer{reinterpret_cast<SgAsmJvmField*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmField::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 238099 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmLocalVariableEntry that takes its corresponding StorageClass as parameter
 */
SgAsmJvmLocalVariableEntry::SgAsmJvmLocalVariableEntry(const SgAsmJvmLocalVariableEntryStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 238109 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_start_pc = storageSource.storageOf_start_pc;
    p_length = storageSource.storageOf_length;
    p_name_index = storageSource.storageOf_name_index;
    p_descriptor_index = storageSource.storageOf_descriptor_index;
    p_index = storageSource.storageOf_index;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmLocalVariableEntry*
SgAsmJvmLocalVariableEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmLocalVariableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableEntry::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableEntry*)(SgAsmJvmLocalVariableEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmLocalVariableEntry*
SgAsmJvmLocalVariableEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmLocalVariableEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmLocalVariableEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableEntry::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableEntry*)(SgAsmJvmLocalVariableEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmLocalVariableEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmLocalVariableEntry::pools.begin(); block != SgAsmJvmLocalVariableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmLocalVariableEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmLocalVariableEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmLocalVariableEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmLocalVariableEntry::pools.begin(); block != SgAsmJvmLocalVariableEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmLocalVariableEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmLocalVariableEntry::clearMemoryPool() {
    SgAsmJvmLocalVariableEntry* tempPointer{nullptr};
    if (SgAsmJvmLocalVariableEntry::pools.empty() == false) {
        auto block{SgAsmJvmLocalVariableEntry::pools.begin()};
        SgAsmJvmLocalVariableEntry::next_node = reinterpret_cast<SgAsmJvmLocalVariableEntry*>(*block);
        for (; block != SgAsmJvmLocalVariableEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmLocalVariableEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmLocalVariableEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmLocalVariableEntry::pool_size-1]);
        }
    }
}

void
SgAsmJvmLocalVariableEntry::deleteMemoryPool() {
    for (auto p: SgAsmJvmLocalVariableEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmLocalVariableEntry::next_node = nullptr;
    SgAsmJvmLocalVariableEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmLocalVariableEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableEntry)};
    for (size_t blockIndex = SgAsmJvmLocalVariableEntry::pools.size(); (blockIndex * SgAsmJvmLocalVariableEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableEntry) = %" PRIuPTR " SgAsmJvmLocalVariableEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableEntry),SgAsmJvmLocalVariableEntry::pool_size);
        }
#endif

        SgAsmJvmLocalVariableEntry* pointer{static_cast<SgAsmJvmLocalVariableEntry*>(ROSE_MALLOC(SgAsmJvmLocalVariableEntry::pool_size * sizeof(SgAsmJvmLocalVariableEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmLocalVariableEntry::pool_size * sizeof(SgAsmJvmLocalVariableEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmLocalVariableEntry::pool_size * sizeof(SgAsmJvmLocalVariableEntry));
#endif
        SgAsmJvmLocalVariableEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmLocalVariableEntry::pool_size * sizeof(SgAsmJvmLocalVariableEntry), V_SgAsmJvmLocalVariableEntry));

        if (SgAsmJvmLocalVariableEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmLocalVariableEntry* blkptr{reinterpret_cast<SgAsmJvmLocalVariableEntry*>(SgAsmJvmLocalVariableEntry::pools[blockIndex-1])};
                blkptr[SgAsmJvmLocalVariableEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmLocalVariableEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmLocalVariableEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmLocalVariableEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmLocalVariableEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmLocalVariableEntry::pool_size - 1};
    SgAsmJvmLocalVariableEntry* testPointer{reinterpret_cast<SgAsmJvmLocalVariableEntry*>(SgAsmJvmLocalVariableEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmLocalVariableEntry::pool_size * (SgAsmJvmLocalVariableEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmLocalVariableEntryStroageClass
 * from its counterpart of SgAsmJvmLocalVariableEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmLocalVariableEntry::initializeStorageClassArray(SgAsmJvmLocalVariableEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmLocalVariableEntry::pools.begin(); block != SgAsmJvmLocalVariableEntry::pools.end(); ++block) {
        SgAsmJvmLocalVariableEntry* pointer{reinterpret_cast<SgAsmJvmLocalVariableEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 238331 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmLocalVariableTypeEntry that takes its corresponding StorageClass as parameter
 */
SgAsmJvmLocalVariableTypeEntry::SgAsmJvmLocalVariableTypeEntry(const SgAsmJvmLocalVariableTypeEntryStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 238341 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_start_pc = storageSource.storageOf_start_pc;
    p_length = storageSource.storageOf_length;
    p_name_index = storageSource.storageOf_name_index;
    p_signature_index = storageSource.storageOf_signature_index;
    p_index = storageSource.storageOf_index;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmLocalVariableTypeEntry*
SgAsmJvmLocalVariableTypeEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmLocalVariableTypeEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableTypeEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableTypeEntry::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableTypeEntry*)(SgAsmJvmLocalVariableTypeEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmLocalVariableTypeEntry*
SgAsmJvmLocalVariableTypeEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmLocalVariableTypeEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTypeEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTypeEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmLocalVariableTypeEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmLocalVariableTypeEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmLocalVariableTypeEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmLocalVariableTypeEntry::pool_size};
        returnPointer = &(((SgAsmJvmLocalVariableTypeEntry*)(SgAsmJvmLocalVariableTypeEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmLocalVariableTypeEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmLocalVariableTypeEntry::pools.begin(); block != SgAsmJvmLocalVariableTypeEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmLocalVariableTypeEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmLocalVariableTypeEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmLocalVariableTypeEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmLocalVariableTypeEntry::pools.begin(); block != SgAsmJvmLocalVariableTypeEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmLocalVariableTypeEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmLocalVariableTypeEntry::clearMemoryPool() {
    SgAsmJvmLocalVariableTypeEntry* tempPointer{nullptr};
    if (SgAsmJvmLocalVariableTypeEntry::pools.empty() == false) {
        auto block{SgAsmJvmLocalVariableTypeEntry::pools.begin()};
        SgAsmJvmLocalVariableTypeEntry::next_node = reinterpret_cast<SgAsmJvmLocalVariableTypeEntry*>(*block);
        for (; block != SgAsmJvmLocalVariableTypeEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmLocalVariableTypeEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmLocalVariableTypeEntry::pool_size-1]);
        }
    }
}

void
SgAsmJvmLocalVariableTypeEntry::deleteMemoryPool() {
    for (auto p: SgAsmJvmLocalVariableTypeEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmLocalVariableTypeEntry::next_node = nullptr;
    SgAsmJvmLocalVariableTypeEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmLocalVariableTypeEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeEntry)};
    for (size_t blockIndex = SgAsmJvmLocalVariableTypeEntry::pools.size(); (blockIndex * SgAsmJvmLocalVariableTypeEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeEntry) = %" PRIuPTR " SgAsmJvmLocalVariableTypeEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmLocalVariableTypeEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmLocalVariableTypeEntry),SgAsmJvmLocalVariableTypeEntry::pool_size);
        }
#endif

        SgAsmJvmLocalVariableTypeEntry* pointer{static_cast<SgAsmJvmLocalVariableTypeEntry*>(ROSE_MALLOC(SgAsmJvmLocalVariableTypeEntry::pool_size * sizeof(SgAsmJvmLocalVariableTypeEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmLocalVariableTypeEntry::pool_size * sizeof(SgAsmJvmLocalVariableTypeEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmLocalVariableTypeEntry::pool_size * sizeof(SgAsmJvmLocalVariableTypeEntry));
#endif
        SgAsmJvmLocalVariableTypeEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmLocalVariableTypeEntry::pool_size * sizeof(SgAsmJvmLocalVariableTypeEntry), V_SgAsmJvmLocalVariableTypeEntry));

        if (SgAsmJvmLocalVariableTypeEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmLocalVariableTypeEntry* blkptr{reinterpret_cast<SgAsmJvmLocalVariableTypeEntry*>(SgAsmJvmLocalVariableTypeEntry::pools[blockIndex-1])};
                blkptr[SgAsmJvmLocalVariableTypeEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmLocalVariableTypeEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmLocalVariableTypeEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmLocalVariableTypeEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmLocalVariableTypeEntry::pool_size - 1};
    SgAsmJvmLocalVariableTypeEntry* testPointer{reinterpret_cast<SgAsmJvmLocalVariableTypeEntry*>(SgAsmJvmLocalVariableTypeEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmLocalVariableTypeEntry::pool_size * (SgAsmJvmLocalVariableTypeEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmLocalVariableTypeEntryStroageClass
 * from its counterpart of SgAsmJvmLocalVariableTypeEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmLocalVariableTypeEntry::initializeStorageClassArray(SgAsmJvmLocalVariableTypeEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmLocalVariableTypeEntry::pools.begin(); block != SgAsmJvmLocalVariableTypeEntry::pools.end(); ++block) {
        SgAsmJvmLocalVariableTypeEntry* pointer{reinterpret_cast<SgAsmJvmLocalVariableTypeEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmLocalVariableTypeEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 238563 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmMethod that takes its corresponding StorageClass as parameter
 */
SgAsmJvmMethod::SgAsmJvmMethod(const SgAsmJvmMethodStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 238573 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_access_flags = storageSource.storageOf_access_flags;
    p_name_index = storageSource.storageOf_name_index;
    p_descriptor_index = storageSource.storageOf_descriptor_index;
     p_attribute_table = (SgAsmJvmAttributeTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_attribute_table));
     p_instruction_list = (SgAsmInstructionList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_instruction_list));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmMethod*
SgAsmJvmMethod::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmMethod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethod)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethod)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethod::pool_size};
        returnPointer = &(((SgAsmJvmMethod*)(SgAsmJvmMethod::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmMethod*
SgAsmJvmMethod::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmMethod* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethod)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethod + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethod)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmMethod)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethod::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethod::pool_size};
        returnPointer = &(((SgAsmJvmMethod*)(SgAsmJvmMethod::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmMethod::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmMethod::pools.begin(); block != SgAsmJvmMethod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethod::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmMethod*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmMethod::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmMethod* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmMethod::pools.begin(); block != SgAsmJvmMethod::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethod::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmMethod::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmMethod::clearMemoryPool() {
    SgAsmJvmMethod* tempPointer{nullptr};
    if (SgAsmJvmMethod::pools.empty() == false) {
        auto block{SgAsmJvmMethod::pools.begin()};
        SgAsmJvmMethod::next_node = reinterpret_cast<SgAsmJvmMethod*>(*block);
        for (; block != SgAsmJvmMethod::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmMethod*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmMethod::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmMethod::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmMethod::pool_size-1]);
        }
    }
}

void
SgAsmJvmMethod::deleteMemoryPool() {
    for (auto p: SgAsmJvmMethod::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmMethod::next_node = nullptr;
    SgAsmJvmMethod::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmMethod::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethod) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethod)};
    for (size_t blockIndex = SgAsmJvmMethod::pools.size(); (blockIndex * SgAsmJvmMethod::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethod) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethod) = %" PRIuPTR " SgAsmJvmMethod::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethod),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethod),SgAsmJvmMethod::pool_size);
        }
#endif

        SgAsmJvmMethod* pointer{static_cast<SgAsmJvmMethod*>(ROSE_MALLOC(SgAsmJvmMethod::pool_size * sizeof(SgAsmJvmMethod)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmMethod::pool_size * sizeof(SgAsmJvmMethod));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmMethod::pool_size * sizeof(SgAsmJvmMethod));
#endif
        SgAsmJvmMethod::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmMethod::pool_size * sizeof(SgAsmJvmMethod), V_SgAsmJvmMethod));

        if (SgAsmJvmMethod::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmMethod* blkptr{reinterpret_cast<SgAsmJvmMethod*>(SgAsmJvmMethod::pools[blockIndex-1])};
                blkptr[SgAsmJvmMethod::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmMethod::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmMethod::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmMethod::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmMethod::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmMethod::pool_size - 1};
    SgAsmJvmMethod* testPointer{reinterpret_cast<SgAsmJvmMethod*>(SgAsmJvmMethod::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmMethod::pool_size * (SgAsmJvmMethod::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmMethodStroageClass
 * from its counterpart of SgAsmJvmMethod. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmMethod::initializeStorageClassArray(SgAsmJvmMethodStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmMethod::pools.begin(); block != SgAsmJvmMethod::pools.end(); ++block) {
        SgAsmJvmMethod* pointer{reinterpret_cast<SgAsmJvmMethod*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethod::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 238795 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmMethodParametersEntry that takes its corresponding StorageClass as parameter
 */
SgAsmJvmMethodParametersEntry::SgAsmJvmMethodParametersEntry(const SgAsmJvmMethodParametersEntryStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 238805 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name_index = storageSource.storageOf_name_index;
    p_access_flags = storageSource.storageOf_access_flags;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmMethodParametersEntry*
SgAsmJvmMethodParametersEntry::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmMethodParametersEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodParametersEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodParametersEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmMethodParametersEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParametersEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethodParametersEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethodParametersEntry::pool_size};
        returnPointer = &(((SgAsmJvmMethodParametersEntry*)(SgAsmJvmMethodParametersEntry::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmMethodParametersEntry*
SgAsmJvmMethodParametersEntry::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmMethodParametersEntry* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodParametersEntry)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodParametersEntry + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmMethodParametersEntry)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmMethodParametersEntry)};
        unsigned long positionInPool{localIndex % SgAsmJvmMethodParametersEntry::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmMethodParametersEntry::pool_size};
        returnPointer = &(((SgAsmJvmMethodParametersEntry*)(SgAsmJvmMethodParametersEntry::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmMethodParametersEntry::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmMethodParametersEntry::pools.begin(); block != SgAsmJvmMethodParametersEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethodParametersEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodParametersEntry::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmMethodParametersEntry*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmMethodParametersEntry::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmMethodParametersEntry* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmMethodParametersEntry::pools.begin(); block != SgAsmJvmMethodParametersEntry::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmMethodParametersEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodParametersEntry::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmMethodParametersEntry::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmMethodParametersEntry::clearMemoryPool() {
    SgAsmJvmMethodParametersEntry* tempPointer{nullptr};
    if (SgAsmJvmMethodParametersEntry::pools.empty() == false) {
        auto block{SgAsmJvmMethodParametersEntry::pools.begin()};
        SgAsmJvmMethodParametersEntry::next_node = reinterpret_cast<SgAsmJvmMethodParametersEntry*>(*block);
        for (; block != SgAsmJvmMethodParametersEntry::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmMethodParametersEntry*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmMethodParametersEntry::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmMethodParametersEntry::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmMethodParametersEntry::pool_size-1]);
        }
    }
}

void
SgAsmJvmMethodParametersEntry::deleteMemoryPool() {
    for (auto p: SgAsmJvmMethodParametersEntry::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmMethodParametersEntry::next_node = nullptr;
    SgAsmJvmMethodParametersEntry::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmMethodParametersEntry::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParametersEntry) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodParametersEntry)};
    for (size_t blockIndex = SgAsmJvmMethodParametersEntry::pools.size(); (blockIndex * SgAsmJvmMethodParametersEntry::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParametersEntry) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodParametersEntry) = %" PRIuPTR " SgAsmJvmMethodParametersEntry::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmMethodParametersEntry),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmMethodParametersEntry),SgAsmJvmMethodParametersEntry::pool_size);
        }
#endif

        SgAsmJvmMethodParametersEntry* pointer{static_cast<SgAsmJvmMethodParametersEntry*>(ROSE_MALLOC(SgAsmJvmMethodParametersEntry::pool_size * sizeof(SgAsmJvmMethodParametersEntry)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmMethodParametersEntry::pool_size * sizeof(SgAsmJvmMethodParametersEntry));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmMethodParametersEntry::pool_size * sizeof(SgAsmJvmMethodParametersEntry));
#endif
        SgAsmJvmMethodParametersEntry::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmMethodParametersEntry::pool_size * sizeof(SgAsmJvmMethodParametersEntry), V_SgAsmJvmMethodParametersEntry));

        if (SgAsmJvmMethodParametersEntry::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmMethodParametersEntry* blkptr{reinterpret_cast<SgAsmJvmMethodParametersEntry*>(SgAsmJvmMethodParametersEntry::pools[blockIndex-1])};
                blkptr[SgAsmJvmMethodParametersEntry::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmMethodParametersEntry::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmMethodParametersEntry::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmMethodParametersEntry::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmMethodParametersEntry::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmMethodParametersEntry::pool_size - 1};
    SgAsmJvmMethodParametersEntry* testPointer{reinterpret_cast<SgAsmJvmMethodParametersEntry*>(SgAsmJvmMethodParametersEntry::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmMethodParametersEntry::pool_size * (SgAsmJvmMethodParametersEntry::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmMethodParametersEntryStroageClass
 * from its counterpart of SgAsmJvmMethodParametersEntry. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmMethodParametersEntry::initializeStorageClassArray(SgAsmJvmMethodParametersEntryStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmMethodParametersEntry::pools.begin(); block != SgAsmJvmMethodParametersEntry::pools.end(); ++block) {
        SgAsmJvmMethodParametersEntry* pointer{reinterpret_cast<SgAsmJvmMethodParametersEntry*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmMethodParametersEntry::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 239024 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmRuntimeAnnotation that takes its corresponding StorageClass as parameter
 */
SgAsmJvmRuntimeAnnotation::SgAsmJvmRuntimeAnnotation(const SgAsmJvmRuntimeAnnotationStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 239034 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_type_index = storageSource.storageOf_type_index;
    p_element_value_pairs = storageSource.storageOf_element_value_pairs.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmRuntimeAnnotation*
SgAsmJvmRuntimeAnnotation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmRuntimeAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotation)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeAnnotation::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeAnnotation*)(SgAsmJvmRuntimeAnnotation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmRuntimeAnnotation*
SgAsmJvmRuntimeAnnotation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmRuntimeAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmRuntimeAnnotation)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeAnnotation::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeAnnotation*)(SgAsmJvmRuntimeAnnotation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmRuntimeAnnotation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmRuntimeAnnotation::pools.begin(); block != SgAsmJvmRuntimeAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotation*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmRuntimeAnnotation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmRuntimeAnnotation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmRuntimeAnnotation* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmRuntimeAnnotation::pools.begin(); block != SgAsmJvmRuntimeAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotation*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmRuntimeAnnotation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmRuntimeAnnotation::clearMemoryPool() {
    SgAsmJvmRuntimeAnnotation* tempPointer{nullptr};
    if (SgAsmJvmRuntimeAnnotation::pools.empty() == false) {
        auto block{SgAsmJvmRuntimeAnnotation::pools.begin()};
        SgAsmJvmRuntimeAnnotation::next_node = reinterpret_cast<SgAsmJvmRuntimeAnnotation*>(*block);
        for (; block != SgAsmJvmRuntimeAnnotation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmRuntimeAnnotation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmRuntimeAnnotation::pool_size-1]);
        }
    }
}

void
SgAsmJvmRuntimeAnnotation::deleteMemoryPool() {
    for (auto p: SgAsmJvmRuntimeAnnotation::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmRuntimeAnnotation::next_node = nullptr;
    SgAsmJvmRuntimeAnnotation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmRuntimeAnnotation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotation)};
    for (size_t blockIndex = SgAsmJvmRuntimeAnnotation::pools.size(); (blockIndex * SgAsmJvmRuntimeAnnotation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotation) = %" PRIuPTR " SgAsmJvmRuntimeAnnotation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotation),SgAsmJvmRuntimeAnnotation::pool_size);
        }
#endif

        SgAsmJvmRuntimeAnnotation* pointer{static_cast<SgAsmJvmRuntimeAnnotation*>(ROSE_MALLOC(SgAsmJvmRuntimeAnnotation::pool_size * sizeof(SgAsmJvmRuntimeAnnotation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmRuntimeAnnotation::pool_size * sizeof(SgAsmJvmRuntimeAnnotation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmRuntimeAnnotation::pool_size * sizeof(SgAsmJvmRuntimeAnnotation));
#endif
        SgAsmJvmRuntimeAnnotation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmRuntimeAnnotation::pool_size * sizeof(SgAsmJvmRuntimeAnnotation), V_SgAsmJvmRuntimeAnnotation));

        if (SgAsmJvmRuntimeAnnotation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmRuntimeAnnotation* blkptr{reinterpret_cast<SgAsmJvmRuntimeAnnotation*>(SgAsmJvmRuntimeAnnotation::pools[blockIndex-1])};
                blkptr[SgAsmJvmRuntimeAnnotation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmRuntimeAnnotation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmRuntimeAnnotation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmRuntimeAnnotation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmRuntimeAnnotation::pool_size - 1};
    SgAsmJvmRuntimeAnnotation* testPointer{reinterpret_cast<SgAsmJvmRuntimeAnnotation*>(SgAsmJvmRuntimeAnnotation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmRuntimeAnnotation::pool_size * (SgAsmJvmRuntimeAnnotation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmRuntimeAnnotationStroageClass
 * from its counterpart of SgAsmJvmRuntimeAnnotation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmRuntimeAnnotation::initializeStorageClassArray(SgAsmJvmRuntimeAnnotationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmRuntimeAnnotation::pools.begin(); block != SgAsmJvmRuntimeAnnotation::pools.end(); ++block) {
        SgAsmJvmRuntimeAnnotation* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotation*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 239253 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmRuntimeAnnotationPair that takes its corresponding StorageClass as parameter
 */
SgAsmJvmRuntimeAnnotationPair::SgAsmJvmRuntimeAnnotationPair(const SgAsmJvmRuntimeAnnotationPairStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 239263 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_element_name_index = storageSource.storageOf_element_name_index;
     p_value = (SgAsmJvmRuntimeAnnotationValue*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_value));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmRuntimeAnnotationPair*
SgAsmJvmRuntimeAnnotationPair::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmRuntimeAnnotationPair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationPair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationPair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationPair)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationPair)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeAnnotationPair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeAnnotationPair::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeAnnotationPair*)(SgAsmJvmRuntimeAnnotationPair::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmRuntimeAnnotationPair*
SgAsmJvmRuntimeAnnotationPair::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmRuntimeAnnotationPair* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotationPair)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotationPair + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotationPair)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmRuntimeAnnotationPair)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeAnnotationPair::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeAnnotationPair::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeAnnotationPair*)(SgAsmJvmRuntimeAnnotationPair::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmRuntimeAnnotationPair::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmRuntimeAnnotationPair::pools.begin(); block != SgAsmJvmRuntimeAnnotationPair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationPair*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationPair::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmRuntimeAnnotationPair*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmRuntimeAnnotationPair::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmRuntimeAnnotationPair* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmRuntimeAnnotationPair::pools.begin(); block != SgAsmJvmRuntimeAnnotationPair::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationPair*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationPair::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmRuntimeAnnotationPair::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmRuntimeAnnotationPair::clearMemoryPool() {
    SgAsmJvmRuntimeAnnotationPair* tempPointer{nullptr};
    if (SgAsmJvmRuntimeAnnotationPair::pools.empty() == false) {
        auto block{SgAsmJvmRuntimeAnnotationPair::pools.begin()};
        SgAsmJvmRuntimeAnnotationPair::next_node = reinterpret_cast<SgAsmJvmRuntimeAnnotationPair*>(*block);
        for (; block != SgAsmJvmRuntimeAnnotationPair::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationPair*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationPair::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmRuntimeAnnotationPair::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmRuntimeAnnotationPair::pool_size-1]);
        }
    }
}

void
SgAsmJvmRuntimeAnnotationPair::deleteMemoryPool() {
    for (auto p: SgAsmJvmRuntimeAnnotationPair::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmRuntimeAnnotationPair::next_node = nullptr;
    SgAsmJvmRuntimeAnnotationPair::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmRuntimeAnnotationPair::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationPair) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationPair)};
    for (size_t blockIndex = SgAsmJvmRuntimeAnnotationPair::pools.size(); (blockIndex * SgAsmJvmRuntimeAnnotationPair::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationPair) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationPair) = %" PRIuPTR " SgAsmJvmRuntimeAnnotationPair::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationPair),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationPair),SgAsmJvmRuntimeAnnotationPair::pool_size);
        }
#endif

        SgAsmJvmRuntimeAnnotationPair* pointer{static_cast<SgAsmJvmRuntimeAnnotationPair*>(ROSE_MALLOC(SgAsmJvmRuntimeAnnotationPair::pool_size * sizeof(SgAsmJvmRuntimeAnnotationPair)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmRuntimeAnnotationPair::pool_size * sizeof(SgAsmJvmRuntimeAnnotationPair));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmRuntimeAnnotationPair::pool_size * sizeof(SgAsmJvmRuntimeAnnotationPair));
#endif
        SgAsmJvmRuntimeAnnotationPair::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmRuntimeAnnotationPair::pool_size * sizeof(SgAsmJvmRuntimeAnnotationPair), V_SgAsmJvmRuntimeAnnotationPair));

        if (SgAsmJvmRuntimeAnnotationPair::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmRuntimeAnnotationPair* blkptr{reinterpret_cast<SgAsmJvmRuntimeAnnotationPair*>(SgAsmJvmRuntimeAnnotationPair::pools[blockIndex-1])};
                blkptr[SgAsmJvmRuntimeAnnotationPair::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmRuntimeAnnotationPair::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationPair::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmRuntimeAnnotationPair::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmRuntimeAnnotationPair::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmRuntimeAnnotationPair::pool_size - 1};
    SgAsmJvmRuntimeAnnotationPair* testPointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationPair*>(SgAsmJvmRuntimeAnnotationPair::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmRuntimeAnnotationPair::pool_size * (SgAsmJvmRuntimeAnnotationPair::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmRuntimeAnnotationPairStroageClass
 * from its counterpart of SgAsmJvmRuntimeAnnotationPair. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmRuntimeAnnotationPair::initializeStorageClassArray(SgAsmJvmRuntimeAnnotationPairStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmRuntimeAnnotationPair::pools.begin(); block != SgAsmJvmRuntimeAnnotationPair::pools.end(); ++block) {
        SgAsmJvmRuntimeAnnotationPair* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationPair*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationPair::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 239482 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmRuntimeAnnotationValue that takes its corresponding StorageClass as parameter
 */
SgAsmJvmRuntimeAnnotationValue::SgAsmJvmRuntimeAnnotationValue(const SgAsmJvmRuntimeAnnotationValueStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 239492 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_tag = storageSource.storageOf_tag;
    p_const_value_index = storageSource.storageOf_const_value_index;
    p_type_name_index = storageSource.storageOf_type_name_index;
    p_const_name_index = storageSource.storageOf_const_name_index;
    p_class_info_index = storageSource.storageOf_class_info_index;
    p_is_annotation_value = storageSource.storageOf_is_annotation_value;
     p_annotation_value = (SgAsmJvmRuntimeAnnotation*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_annotation_value));
    p_values = storageSource.storageOf_values.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmRuntimeAnnotationValue*
SgAsmJvmRuntimeAnnotationValue::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmRuntimeAnnotationValue* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationValue)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationValue + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationValue)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationValue)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeAnnotationValue::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeAnnotationValue::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeAnnotationValue*)(SgAsmJvmRuntimeAnnotationValue::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmRuntimeAnnotationValue*
SgAsmJvmRuntimeAnnotationValue::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmRuntimeAnnotationValue* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotationValue)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotationValue + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeAnnotationValue)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmRuntimeAnnotationValue)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeAnnotationValue::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeAnnotationValue::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeAnnotationValue*)(SgAsmJvmRuntimeAnnotationValue::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmRuntimeAnnotationValue::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmRuntimeAnnotationValue::pools.begin(); block != SgAsmJvmRuntimeAnnotationValue::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationValue*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationValue::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmRuntimeAnnotationValue*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmRuntimeAnnotationValue::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmRuntimeAnnotationValue* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmRuntimeAnnotationValue::pools.begin(); block != SgAsmJvmRuntimeAnnotationValue::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationValue*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationValue::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmRuntimeAnnotationValue::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmRuntimeAnnotationValue::clearMemoryPool() {
    SgAsmJvmRuntimeAnnotationValue* tempPointer{nullptr};
    if (SgAsmJvmRuntimeAnnotationValue::pools.empty() == false) {
        auto block{SgAsmJvmRuntimeAnnotationValue::pools.begin()};
        SgAsmJvmRuntimeAnnotationValue::next_node = reinterpret_cast<SgAsmJvmRuntimeAnnotationValue*>(*block);
        for (; block != SgAsmJvmRuntimeAnnotationValue::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationValue*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationValue::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmRuntimeAnnotationValue::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmRuntimeAnnotationValue::pool_size-1]);
        }
    }
}

void
SgAsmJvmRuntimeAnnotationValue::deleteMemoryPool() {
    for (auto p: SgAsmJvmRuntimeAnnotationValue::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmRuntimeAnnotationValue::next_node = nullptr;
    SgAsmJvmRuntimeAnnotationValue::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmRuntimeAnnotationValue::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationValue) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationValue)};
    for (size_t blockIndex = SgAsmJvmRuntimeAnnotationValue::pools.size(); (blockIndex * SgAsmJvmRuntimeAnnotationValue::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationValue) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationValue) = %" PRIuPTR " SgAsmJvmRuntimeAnnotationValue::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeAnnotationValue),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeAnnotationValue),SgAsmJvmRuntimeAnnotationValue::pool_size);
        }
#endif

        SgAsmJvmRuntimeAnnotationValue* pointer{static_cast<SgAsmJvmRuntimeAnnotationValue*>(ROSE_MALLOC(SgAsmJvmRuntimeAnnotationValue::pool_size * sizeof(SgAsmJvmRuntimeAnnotationValue)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmRuntimeAnnotationValue::pool_size * sizeof(SgAsmJvmRuntimeAnnotationValue));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmRuntimeAnnotationValue::pool_size * sizeof(SgAsmJvmRuntimeAnnotationValue));
#endif
        SgAsmJvmRuntimeAnnotationValue::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmRuntimeAnnotationValue::pool_size * sizeof(SgAsmJvmRuntimeAnnotationValue), V_SgAsmJvmRuntimeAnnotationValue));

        if (SgAsmJvmRuntimeAnnotationValue::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmRuntimeAnnotationValue* blkptr{reinterpret_cast<SgAsmJvmRuntimeAnnotationValue*>(SgAsmJvmRuntimeAnnotationValue::pools[blockIndex-1])};
                blkptr[SgAsmJvmRuntimeAnnotationValue::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmRuntimeAnnotationValue::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationValue::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmRuntimeAnnotationValue::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmRuntimeAnnotationValue::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmRuntimeAnnotationValue::pool_size - 1};
    SgAsmJvmRuntimeAnnotationValue* testPointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationValue*>(SgAsmJvmRuntimeAnnotationValue::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmRuntimeAnnotationValue::pool_size * (SgAsmJvmRuntimeAnnotationValue::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmRuntimeAnnotationValueStroageClass
 * from its counterpart of SgAsmJvmRuntimeAnnotationValue. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmRuntimeAnnotationValue::initializeStorageClassArray(SgAsmJvmRuntimeAnnotationValueStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmRuntimeAnnotationValue::pools.begin(); block != SgAsmJvmRuntimeAnnotationValue::pools.end(); ++block) {
        SgAsmJvmRuntimeAnnotationValue* pointer{reinterpret_cast<SgAsmJvmRuntimeAnnotationValue*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeAnnotationValue::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 239717 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmRuntimeParameterAnnotation that takes its corresponding StorageClass as parameter
 */
SgAsmJvmRuntimeParameterAnnotation::SgAsmJvmRuntimeParameterAnnotation(const SgAsmJvmRuntimeParameterAnnotationStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 239727 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_annotations = storageSource.storageOf_annotations.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmRuntimeParameterAnnotation*
SgAsmJvmRuntimeParameterAnnotation::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmRuntimeParameterAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeParameterAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeParameterAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmRuntimeParameterAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeParameterAnnotation)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeParameterAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeParameterAnnotation::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeParameterAnnotation*)(SgAsmJvmRuntimeParameterAnnotation::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmRuntimeParameterAnnotation*
SgAsmJvmRuntimeParameterAnnotation::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmRuntimeParameterAnnotation* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeParameterAnnotation)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeParameterAnnotation + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmRuntimeParameterAnnotation)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmRuntimeParameterAnnotation)};
        unsigned long positionInPool{localIndex % SgAsmJvmRuntimeParameterAnnotation::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmRuntimeParameterAnnotation::pool_size};
        returnPointer = &(((SgAsmJvmRuntimeParameterAnnotation*)(SgAsmJvmRuntimeParameterAnnotation::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmRuntimeParameterAnnotation::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmRuntimeParameterAnnotation::pools.begin(); block != SgAsmJvmRuntimeParameterAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeParameterAnnotation*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeParameterAnnotation::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmRuntimeParameterAnnotation*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmRuntimeParameterAnnotation::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmRuntimeParameterAnnotation* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmRuntimeParameterAnnotation::pools.begin(); block != SgAsmJvmRuntimeParameterAnnotation::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmRuntimeParameterAnnotation*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeParameterAnnotation::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmRuntimeParameterAnnotation::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmRuntimeParameterAnnotation::clearMemoryPool() {
    SgAsmJvmRuntimeParameterAnnotation* tempPointer{nullptr};
    if (SgAsmJvmRuntimeParameterAnnotation::pools.empty() == false) {
        auto block{SgAsmJvmRuntimeParameterAnnotation::pools.begin()};
        SgAsmJvmRuntimeParameterAnnotation::next_node = reinterpret_cast<SgAsmJvmRuntimeParameterAnnotation*>(*block);
        for (; block != SgAsmJvmRuntimeParameterAnnotation::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmRuntimeParameterAnnotation*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmRuntimeParameterAnnotation::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmRuntimeParameterAnnotation::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmRuntimeParameterAnnotation::pool_size-1]);
        }
    }
}

void
SgAsmJvmRuntimeParameterAnnotation::deleteMemoryPool() {
    for (auto p: SgAsmJvmRuntimeParameterAnnotation::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmRuntimeParameterAnnotation::next_node = nullptr;
    SgAsmJvmRuntimeParameterAnnotation::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmRuntimeParameterAnnotation::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeParameterAnnotation) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeParameterAnnotation)};
    for (size_t blockIndex = SgAsmJvmRuntimeParameterAnnotation::pools.size(); (blockIndex * SgAsmJvmRuntimeParameterAnnotation::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeParameterAnnotation) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeParameterAnnotation) = %" PRIuPTR " SgAsmJvmRuntimeParameterAnnotation::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmRuntimeParameterAnnotation),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmRuntimeParameterAnnotation),SgAsmJvmRuntimeParameterAnnotation::pool_size);
        }
#endif

        SgAsmJvmRuntimeParameterAnnotation* pointer{static_cast<SgAsmJvmRuntimeParameterAnnotation*>(ROSE_MALLOC(SgAsmJvmRuntimeParameterAnnotation::pool_size * sizeof(SgAsmJvmRuntimeParameterAnnotation)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmRuntimeParameterAnnotation::pool_size * sizeof(SgAsmJvmRuntimeParameterAnnotation));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmRuntimeParameterAnnotation::pool_size * sizeof(SgAsmJvmRuntimeParameterAnnotation));
#endif
        SgAsmJvmRuntimeParameterAnnotation::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmRuntimeParameterAnnotation::pool_size * sizeof(SgAsmJvmRuntimeParameterAnnotation), V_SgAsmJvmRuntimeParameterAnnotation));

        if (SgAsmJvmRuntimeParameterAnnotation::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmRuntimeParameterAnnotation* blkptr{reinterpret_cast<SgAsmJvmRuntimeParameterAnnotation*>(SgAsmJvmRuntimeParameterAnnotation::pools[blockIndex-1])};
                blkptr[SgAsmJvmRuntimeParameterAnnotation::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmRuntimeParameterAnnotation::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmRuntimeParameterAnnotation::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmRuntimeParameterAnnotation::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmRuntimeParameterAnnotation::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmRuntimeParameterAnnotation::pool_size - 1};
    SgAsmJvmRuntimeParameterAnnotation* testPointer{reinterpret_cast<SgAsmJvmRuntimeParameterAnnotation*>(SgAsmJvmRuntimeParameterAnnotation::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmRuntimeParameterAnnotation::pool_size * (SgAsmJvmRuntimeParameterAnnotation::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmRuntimeParameterAnnotationStroageClass
 * from its counterpart of SgAsmJvmRuntimeParameterAnnotation. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmRuntimeParameterAnnotation::initializeStorageClassArray(SgAsmJvmRuntimeParameterAnnotationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmRuntimeParameterAnnotation::pools.begin(); block != SgAsmJvmRuntimeParameterAnnotation::pools.end(); ++block) {
        SgAsmJvmRuntimeParameterAnnotation* pointer{reinterpret_cast<SgAsmJvmRuntimeParameterAnnotation*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmRuntimeParameterAnnotation::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 239945 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmStackMapFrame that takes its corresponding StorageClass as parameter
 */
SgAsmJvmStackMapFrame::SgAsmJvmStackMapFrame(const SgAsmJvmStackMapFrameStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 239955 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_stack = storageSource.storageOf_stack.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmStackMapVerificationTypePtrList::iterator i_stack = p_stack.begin();
    for ( ; i_stack != p_stack.end(); ++i_stack) {
        (*i_stack) = (SgAsmJvmStackMapVerificationTypePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_stack)) );
    }
    p_locals = storageSource.storageOf_locals.rebuildDataStoredInEasyStorageClass();
    SgAsmJvmStackMapVerificationTypePtrList::iterator i_locals = p_locals.begin();
    for ( ; i_locals != p_locals.end(); ++i_locals) {
        (*i_locals) = (SgAsmJvmStackMapVerificationTypePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_locals)) );
    }
    p_frame_type = storageSource.storageOf_frame_type;
    p_offset_delta = storageSource.storageOf_offset_delta;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmStackMapFrame*
SgAsmJvmStackMapFrame::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmStackMapFrame* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapFrame)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapFrame + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapFrame)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapFrame)};
        unsigned long positionInPool{localIndex % SgAsmJvmStackMapFrame::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmStackMapFrame::pool_size};
        returnPointer = &(((SgAsmJvmStackMapFrame*)(SgAsmJvmStackMapFrame::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmStackMapFrame*
SgAsmJvmStackMapFrame::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmStackMapFrame* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapFrame)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapFrame + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapFrame)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmStackMapFrame)};
        unsigned long positionInPool{localIndex % SgAsmJvmStackMapFrame::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmStackMapFrame::pool_size};
        returnPointer = &(((SgAsmJvmStackMapFrame*)(SgAsmJvmStackMapFrame::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmStackMapFrame::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmStackMapFrame::pools.begin(); block != SgAsmJvmStackMapFrame::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmStackMapFrame*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapFrame::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmStackMapFrame*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmStackMapFrame::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmStackMapFrame* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmStackMapFrame::pools.begin(); block != SgAsmJvmStackMapFrame::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmStackMapFrame*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapFrame::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmStackMapFrame::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmStackMapFrame::clearMemoryPool() {
    SgAsmJvmStackMapFrame* tempPointer{nullptr};
    if (SgAsmJvmStackMapFrame::pools.empty() == false) {
        auto block{SgAsmJvmStackMapFrame::pools.begin()};
        SgAsmJvmStackMapFrame::next_node = reinterpret_cast<SgAsmJvmStackMapFrame*>(*block);
        for (; block != SgAsmJvmStackMapFrame::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmStackMapFrame*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmStackMapFrame::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmStackMapFrame::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmStackMapFrame::pool_size-1]);
        }
    }
}

void
SgAsmJvmStackMapFrame::deleteMemoryPool() {
    for (auto p: SgAsmJvmStackMapFrame::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmStackMapFrame::next_node = nullptr;
    SgAsmJvmStackMapFrame::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmStackMapFrame::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapFrame) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapFrame)};
    for (size_t blockIndex = SgAsmJvmStackMapFrame::pools.size(); (blockIndex * SgAsmJvmStackMapFrame::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapFrame) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapFrame) = %" PRIuPTR " SgAsmJvmStackMapFrame::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapFrame),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapFrame),SgAsmJvmStackMapFrame::pool_size);
        }
#endif

        SgAsmJvmStackMapFrame* pointer{static_cast<SgAsmJvmStackMapFrame*>(ROSE_MALLOC(SgAsmJvmStackMapFrame::pool_size * sizeof(SgAsmJvmStackMapFrame)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmStackMapFrame::pool_size * sizeof(SgAsmJvmStackMapFrame));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmStackMapFrame::pool_size * sizeof(SgAsmJvmStackMapFrame));
#endif
        SgAsmJvmStackMapFrame::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmStackMapFrame::pool_size * sizeof(SgAsmJvmStackMapFrame), V_SgAsmJvmStackMapFrame));

        if (SgAsmJvmStackMapFrame::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmStackMapFrame* blkptr{reinterpret_cast<SgAsmJvmStackMapFrame*>(SgAsmJvmStackMapFrame::pools[blockIndex-1])};
                blkptr[SgAsmJvmStackMapFrame::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmStackMapFrame::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmStackMapFrame::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmStackMapFrame::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmStackMapFrame::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmStackMapFrame::pool_size - 1};
    SgAsmJvmStackMapFrame* testPointer{reinterpret_cast<SgAsmJvmStackMapFrame*>(SgAsmJvmStackMapFrame::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmStackMapFrame::pool_size * (SgAsmJvmStackMapFrame::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmStackMapFrameStroageClass
 * from its counterpart of SgAsmJvmStackMapFrame. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmStackMapFrame::initializeStorageClassArray(SgAsmJvmStackMapFrameStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmStackMapFrame::pools.begin(); block != SgAsmJvmStackMapFrame::pools.end(); ++block) {
        SgAsmJvmStackMapFrame* pointer{reinterpret_cast<SgAsmJvmStackMapFrame*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapFrame::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 240184 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmStackMapVerificationType that takes its corresponding StorageClass as parameter
 */
SgAsmJvmStackMapVerificationType::SgAsmJvmStackMapVerificationType(const SgAsmJvmStackMapVerificationTypeStorageClass& storageSource)  : SgAsmJvmNode (storageSource) {

/* #line 240194 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_tag = storageSource.storageOf_tag;
    p_cpool_index = storageSource.storageOf_cpool_index;
    p_offset = storageSource.storageOf_offset;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmStackMapVerificationType*
SgAsmJvmStackMapVerificationType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmStackMapVerificationType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapVerificationType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapVerificationType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmStackMapVerificationType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapVerificationType)};
        unsigned long positionInPool{localIndex % SgAsmJvmStackMapVerificationType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmStackMapVerificationType::pool_size};
        returnPointer = &(((SgAsmJvmStackMapVerificationType*)(SgAsmJvmStackMapVerificationType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmStackMapVerificationType*
SgAsmJvmStackMapVerificationType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmStackMapVerificationType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapVerificationType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapVerificationType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmStackMapVerificationType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmStackMapVerificationType)};
        unsigned long positionInPool{localIndex % SgAsmJvmStackMapVerificationType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmStackMapVerificationType::pool_size};
        returnPointer = &(((SgAsmJvmStackMapVerificationType*)(SgAsmJvmStackMapVerificationType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmStackMapVerificationType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmStackMapVerificationType::pools.begin(); block != SgAsmJvmStackMapVerificationType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmStackMapVerificationType*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapVerificationType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmStackMapVerificationType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmStackMapVerificationType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmStackMapVerificationType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmStackMapVerificationType::pools.begin(); block != SgAsmJvmStackMapVerificationType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmStackMapVerificationType*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapVerificationType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmStackMapVerificationType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmStackMapVerificationType::clearMemoryPool() {
    SgAsmJvmStackMapVerificationType* tempPointer{nullptr};
    if (SgAsmJvmStackMapVerificationType::pools.empty() == false) {
        auto block{SgAsmJvmStackMapVerificationType::pools.begin()};
        SgAsmJvmStackMapVerificationType::next_node = reinterpret_cast<SgAsmJvmStackMapVerificationType*>(*block);
        for (; block != SgAsmJvmStackMapVerificationType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmStackMapVerificationType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmStackMapVerificationType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmStackMapVerificationType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmStackMapVerificationType::pool_size-1]);
        }
    }
}

void
SgAsmJvmStackMapVerificationType::deleteMemoryPool() {
    for (auto p: SgAsmJvmStackMapVerificationType::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmStackMapVerificationType::next_node = nullptr;
    SgAsmJvmStackMapVerificationType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmStackMapVerificationType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapVerificationType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapVerificationType)};
    for (size_t blockIndex = SgAsmJvmStackMapVerificationType::pools.size(); (blockIndex * SgAsmJvmStackMapVerificationType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapVerificationType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapVerificationType) = %" PRIuPTR " SgAsmJvmStackMapVerificationType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmStackMapVerificationType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmStackMapVerificationType),SgAsmJvmStackMapVerificationType::pool_size);
        }
#endif

        SgAsmJvmStackMapVerificationType* pointer{static_cast<SgAsmJvmStackMapVerificationType*>(ROSE_MALLOC(SgAsmJvmStackMapVerificationType::pool_size * sizeof(SgAsmJvmStackMapVerificationType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmStackMapVerificationType::pool_size * sizeof(SgAsmJvmStackMapVerificationType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmStackMapVerificationType::pool_size * sizeof(SgAsmJvmStackMapVerificationType));
#endif
        SgAsmJvmStackMapVerificationType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmStackMapVerificationType::pool_size * sizeof(SgAsmJvmStackMapVerificationType), V_SgAsmJvmStackMapVerificationType));

        if (SgAsmJvmStackMapVerificationType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmStackMapVerificationType* blkptr{reinterpret_cast<SgAsmJvmStackMapVerificationType*>(SgAsmJvmStackMapVerificationType::pools[blockIndex-1])};
                blkptr[SgAsmJvmStackMapVerificationType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmStackMapVerificationType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmStackMapVerificationType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmStackMapVerificationType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmStackMapVerificationType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmStackMapVerificationType::pool_size - 1};
    SgAsmJvmStackMapVerificationType* testPointer{reinterpret_cast<SgAsmJvmStackMapVerificationType*>(SgAsmJvmStackMapVerificationType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmStackMapVerificationType::pool_size * (SgAsmJvmStackMapVerificationType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmStackMapVerificationTypeStroageClass
 * from its counterpart of SgAsmJvmStackMapVerificationType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmStackMapVerificationType::initializeStorageClassArray(SgAsmJvmStackMapVerificationTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmStackMapVerificationType::pools.begin(); block != SgAsmJvmStackMapVerificationType::pools.end(); ++block) {
        SgAsmJvmStackMapVerificationType* pointer{reinterpret_cast<SgAsmJvmStackMapVerificationType*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmStackMapVerificationType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 240414 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmOperandList that takes its corresponding StorageClass as parameter
 */
SgAsmOperandList::SgAsmOperandList(const SgAsmOperandListStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 240424 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_operands = storageSource.storageOf_operands.rebuildDataStoredInEasyStorageClass();
    SgAsmExpressionPtrList::iterator i_operands = p_operands.begin();
    for ( ; i_operands != p_operands.end(); ++i_operands) {
        (*i_operands) = (SgAsmExpressionPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_operands)) );
    }


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmOperandList*
SgAsmOperandList::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmOperandList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmOperandList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmOperandList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmOperandList)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOperandList)};
        unsigned long positionInPool{localIndex % SgAsmOperandList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmOperandList::pool_size};
        returnPointer = &(((SgAsmOperandList*)(SgAsmOperandList::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmOperandList*
SgAsmOperandList::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmOperandList* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmOperandList)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmOperandList + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmOperandList)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmOperandList)};
        unsigned long positionInPool{localIndex % SgAsmOperandList::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmOperandList::pool_size};
        returnPointer = &(((SgAsmOperandList*)(SgAsmOperandList::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmOperandList::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmOperandList::pools.begin(); block != SgAsmOperandList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmOperandList*>(*block)};
        for (unsigned i = 0; i < SgAsmOperandList::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmOperandList*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmOperandList::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmOperandList* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmOperandList::pools.begin(); block != SgAsmOperandList::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmOperandList*>(*block)};
        for (unsigned i = 0; i < SgAsmOperandList::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmOperandList::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmOperandList::clearMemoryPool() {
    SgAsmOperandList* tempPointer{nullptr};
    if (SgAsmOperandList::pools.empty() == false) {
        auto block{SgAsmOperandList::pools.begin()};
        SgAsmOperandList::next_node = reinterpret_cast<SgAsmOperandList*>(*block);
        for (; block != SgAsmOperandList::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmOperandList*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmOperandList::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmOperandList::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmOperandList::pool_size-1]);
        }
    }
}

void
SgAsmOperandList::deleteMemoryPool() {
    for (auto p: SgAsmOperandList::pools) {
        ROSE_FREE(p);
    }
    SgAsmOperandList::next_node = nullptr;
    SgAsmOperandList::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmOperandList::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOperandList) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmOperandList)};
    for (size_t blockIndex = SgAsmOperandList::pools.size(); (blockIndex * SgAsmOperandList::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOperandList) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmOperandList) = %" PRIuPTR " SgAsmOperandList::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmOperandList),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmOperandList),SgAsmOperandList::pool_size);
        }
#endif

        SgAsmOperandList* pointer{static_cast<SgAsmOperandList*>(ROSE_MALLOC(SgAsmOperandList::pool_size * sizeof(SgAsmOperandList)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmOperandList::pool_size * sizeof(SgAsmOperandList));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmOperandList::pool_size * sizeof(SgAsmOperandList));
#endif
        SgAsmOperandList::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmOperandList::pool_size * sizeof(SgAsmOperandList), V_SgAsmOperandList));

        if (SgAsmOperandList::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmOperandList* blkptr{reinterpret_cast<SgAsmOperandList*>(SgAsmOperandList::pools[blockIndex-1])};
                blkptr[SgAsmOperandList::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmOperandList::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmOperandList::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmOperandList::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmOperandList::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmOperandList::pool_size - 1};
    SgAsmOperandList* testPointer{reinterpret_cast<SgAsmOperandList*>(SgAsmOperandList::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmOperandList::pool_size * (SgAsmOperandList::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmOperandListStroageClass
 * from its counterpart of SgAsmOperandList. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmOperandList::initializeStorageClassArray(SgAsmOperandListStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmOperandList::pools.begin(); block != SgAsmOperandList::pools.end(); ++block) {
        SgAsmOperandList* pointer{reinterpret_cast<SgAsmOperandList*>(*block)};
        for (unsigned i = 0; i < SgAsmOperandList::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 240646 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmStatement that takes its corresponding StorageClass as parameter
 */
SgAsmStatement::SgAsmStatement(const SgAsmStatementStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 240656 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_address = storageSource.storageOf_address;
    p_comment = storageSource.storageOf_comment.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmStatement*
SgAsmStatement::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStatement)};
        unsigned long positionInPool{localIndex % SgAsmStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStatement::pool_size};
        returnPointer = &(((SgAsmStatement*)(SgAsmStatement::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmStatement*
SgAsmStatement::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmStatement* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStatement)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStatement + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStatement)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmStatement)};
        unsigned long positionInPool{localIndex % SgAsmStatement::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStatement::pool_size};
        returnPointer = &(((SgAsmStatement*)(SgAsmStatement::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmStatement::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmStatement::pools.begin(); block != SgAsmStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStatement*>(*block)};
        for (unsigned i = 0; i < SgAsmStatement::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmStatement*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmStatement::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmStatement* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmStatement::pools.begin(); block != SgAsmStatement::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStatement*>(*block)};
        for (unsigned i = 0; i < SgAsmStatement::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmStatement::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmStatement::clearMemoryPool() {
    SgAsmStatement* tempPointer{nullptr};
    if (SgAsmStatement::pools.empty() == false) {
        auto block{SgAsmStatement::pools.begin()};
        SgAsmStatement::next_node = reinterpret_cast<SgAsmStatement*>(*block);
        for (; block != SgAsmStatement::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmStatement*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmStatement::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmStatement::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmStatement::pool_size-1]);
        }
    }
}

void
SgAsmStatement::deleteMemoryPool() {
    for (auto p: SgAsmStatement::pools) {
        ROSE_FREE(p);
    }
    SgAsmStatement::next_node = nullptr;
    SgAsmStatement::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmStatement::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStatement) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStatement)};
    for (size_t blockIndex = SgAsmStatement::pools.size(); (blockIndex * SgAsmStatement::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStatement) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStatement) = %" PRIuPTR " SgAsmStatement::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStatement),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStatement),SgAsmStatement::pool_size);
        }
#endif

        SgAsmStatement* pointer{static_cast<SgAsmStatement*>(ROSE_MALLOC(SgAsmStatement::pool_size * sizeof(SgAsmStatement)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmStatement::pool_size * sizeof(SgAsmStatement));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmStatement::pool_size * sizeof(SgAsmStatement));
#endif
        SgAsmStatement::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmStatement::pool_size * sizeof(SgAsmStatement), V_SgAsmStatement));

        if (SgAsmStatement::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmStatement* blkptr{reinterpret_cast<SgAsmStatement*>(SgAsmStatement::pools[blockIndex-1])};
                blkptr[SgAsmStatement::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmStatement::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmStatement::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmStatement::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmStatement::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmStatement::pool_size - 1};
    SgAsmStatement* testPointer{reinterpret_cast<SgAsmStatement*>(SgAsmStatement::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmStatement::pool_size * (SgAsmStatement::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmStatementStroageClass
 * from its counterpart of SgAsmStatement. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmStatement::initializeStorageClassArray(SgAsmStatementStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmStatement::pools.begin(); block != SgAsmStatement::pools.end(); ++block) {
        SgAsmStatement* pointer{reinterpret_cast<SgAsmStatement*>(*block)};
        for (unsigned i = 0; i < SgAsmStatement::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 240875 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmBlock that takes its corresponding StorageClass as parameter
 */
SgAsmBlock::SgAsmBlock(const SgAsmBlockStorageClass& storageSource)  : SgAsmStatement (storageSource) {

/* #line 240885 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_id = storageSource.storageOf_id;
    p_reason = storageSource.storageOf_reason;
    p_statementList = storageSource.storageOf_statementList.rebuildDataStoredInEasyStorageClass();
    SgAsmStatementPtrList::iterator i_statementList = p_statementList.begin();
    for ( ; i_statementList != p_statementList.end(); ++i_statementList) {
        (*i_statementList) = (SgAsmStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_statementList)) );
    }
    p_successors = storageSource.storageOf_successors.rebuildDataStoredInEasyStorageClass();
    SgAsmIntegerValuePtrList::iterator i_successors = p_successors.begin();
    for ( ; i_successors != p_successors.end(); ++i_successors) {
        (*i_successors) = (SgAsmIntegerValuePtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_successors)) );
    }
    p_successorsComplete = storageSource.storageOf_successorsComplete;
     p_immediateDominator = (SgAsmBlock*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_immediateDominator));
    p_cachedVertex = storageSource.storageOf_cachedVertex;
    p_codeLikelihood = storageSource.storageOf_codeLikelihood;
    p_stackDeltaOut = storageSource.storageOf_stackDeltaOut;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmBlock*
SgAsmBlock::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBlock)};
        unsigned long positionInPool{localIndex % SgAsmBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBlock::pool_size};
        returnPointer = &(((SgAsmBlock*)(SgAsmBlock::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmBlock*
SgAsmBlock::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmBlock* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBlock)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBlock + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmBlock)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmBlock)};
        unsigned long positionInPool{localIndex % SgAsmBlock::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmBlock::pool_size};
        returnPointer = &(((SgAsmBlock*)(SgAsmBlock::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmBlock::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmBlock::pools.begin(); block != SgAsmBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmBlock::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmBlock*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmBlock::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmBlock* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmBlock::pools.begin(); block != SgAsmBlock::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmBlock::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmBlock::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmBlock::clearMemoryPool() {
    SgAsmBlock* tempPointer{nullptr};
    if (SgAsmBlock::pools.empty() == false) {
        auto block{SgAsmBlock::pools.begin()};
        SgAsmBlock::next_node = reinterpret_cast<SgAsmBlock*>(*block);
        for (; block != SgAsmBlock::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmBlock*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmBlock::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmBlock::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmBlock::pool_size-1]);
        }
    }
}

void
SgAsmBlock::deleteMemoryPool() {
    for (auto p: SgAsmBlock::pools) {
        ROSE_FREE(p);
    }
    SgAsmBlock::next_node = nullptr;
    SgAsmBlock::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmBlock::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBlock) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBlock)};
    for (size_t blockIndex = SgAsmBlock::pools.size(); (blockIndex * SgAsmBlock::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBlock) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBlock) = %" PRIuPTR " SgAsmBlock::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmBlock),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmBlock),SgAsmBlock::pool_size);
        }
#endif

        SgAsmBlock* pointer{static_cast<SgAsmBlock*>(ROSE_MALLOC(SgAsmBlock::pool_size * sizeof(SgAsmBlock)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmBlock::pool_size * sizeof(SgAsmBlock));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmBlock::pool_size * sizeof(SgAsmBlock));
#endif
        SgAsmBlock::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmBlock::pool_size * sizeof(SgAsmBlock), V_SgAsmBlock));

        if (SgAsmBlock::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmBlock* blkptr{reinterpret_cast<SgAsmBlock*>(SgAsmBlock::pools[blockIndex-1])};
                blkptr[SgAsmBlock::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmBlock::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmBlock::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmBlock::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmBlock::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmBlock::pool_size - 1};
    SgAsmBlock* testPointer{reinterpret_cast<SgAsmBlock*>(SgAsmBlock::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmBlock::pool_size * (SgAsmBlock::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmBlockStroageClass
 * from its counterpart of SgAsmBlock. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmBlock::initializeStorageClassArray(SgAsmBlockStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmBlock::pools.begin(); block != SgAsmBlock::pools.end(); ++block) {
        SgAsmBlock* pointer{reinterpret_cast<SgAsmBlock*>(*block)};
        for (unsigned i = 0; i < SgAsmBlock::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 241119 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmInstruction::SgAsmInstruction(const SgAsmInstructionStorageClass& storageSource)  : SgAsmStatement (storageSource) {

/* #line 241129 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_architectureId = storageSource.storageOf_architectureId;
    p_rawBytes = storageSource.storageOf_rawBytes.rebuildDataStoredInEasyStorageClass();
     p_operandList = (SgAsmOperandList*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_operandList));
     p_delaySlot = (SgAsmInstruction*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_delaySlot));
     p_semantics = (SgAsmExprListExp*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_semantics));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmInstruction*
SgAsmInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstruction)};
        unsigned long positionInPool{localIndex % SgAsmInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInstruction::pool_size};
        returnPointer = &(((SgAsmInstruction*)(SgAsmInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmInstruction*
SgAsmInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmInstruction)};
        unsigned long positionInPool{localIndex % SgAsmInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmInstruction::pool_size};
        returnPointer = &(((SgAsmInstruction*)(SgAsmInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmInstruction::pools.begin(); block != SgAsmInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmInstruction::pools.begin(); block != SgAsmInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmInstruction::clearMemoryPool() {
    SgAsmInstruction* tempPointer{nullptr};
    if (SgAsmInstruction::pools.empty() == false) {
        auto block{SgAsmInstruction::pools.begin()};
        SgAsmInstruction::next_node = reinterpret_cast<SgAsmInstruction*>(*block);
        for (; block != SgAsmInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmInstruction::pool_size-1]);
        }
    }
}

void
SgAsmInstruction::deleteMemoryPool() {
    for (auto p: SgAsmInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmInstruction::next_node = nullptr;
    SgAsmInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInstruction)};
    for (size_t blockIndex = SgAsmInstruction::pools.size(); (blockIndex * SgAsmInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInstruction) = %" PRIuPTR " SgAsmInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmInstruction),SgAsmInstruction::pool_size);
        }
#endif

        SgAsmInstruction* pointer{static_cast<SgAsmInstruction*>(ROSE_MALLOC(SgAsmInstruction::pool_size * sizeof(SgAsmInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmInstruction::pool_size * sizeof(SgAsmInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmInstruction::pool_size * sizeof(SgAsmInstruction));
#endif
        SgAsmInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmInstruction::pool_size * sizeof(SgAsmInstruction), V_SgAsmInstruction));

        if (SgAsmInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmInstruction* blkptr{reinterpret_cast<SgAsmInstruction*>(SgAsmInstruction::pools[blockIndex-1])};
                blkptr[SgAsmInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmInstruction::pool_size - 1};
    SgAsmInstruction* testPointer{reinterpret_cast<SgAsmInstruction*>(SgAsmInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmInstruction::pool_size * (SgAsmInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmInstructionStroageClass
 * from its counterpart of SgAsmInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmInstruction::initializeStorageClassArray(SgAsmInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmInstruction::pools.begin(); block != SgAsmInstruction::pools.end(); ++block) {
        SgAsmInstruction* pointer{reinterpret_cast<SgAsmInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 241351 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmCilInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmCilInstruction::SgAsmCilInstruction(const SgAsmCilInstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 241361 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmCilInstruction*
SgAsmCilInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmCilInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmCilInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInstruction)};
        unsigned long positionInPool{localIndex % SgAsmCilInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilInstruction::pool_size};
        returnPointer = &(((SgAsmCilInstruction*)(SgAsmCilInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmCilInstruction*
SgAsmCilInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmCilInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmCilInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmCilInstruction)};
        unsigned long positionInPool{localIndex % SgAsmCilInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmCilInstruction::pool_size};
        returnPointer = &(((SgAsmCilInstruction*)(SgAsmCilInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmCilInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmCilInstruction::pools.begin(); block != SgAsmCilInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmCilInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmCilInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmCilInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmCilInstruction::pools.begin(); block != SgAsmCilInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmCilInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmCilInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmCilInstruction::clearMemoryPool() {
    SgAsmCilInstruction* tempPointer{nullptr};
    if (SgAsmCilInstruction::pools.empty() == false) {
        auto block{SgAsmCilInstruction::pools.begin()};
        SgAsmCilInstruction::next_node = reinterpret_cast<SgAsmCilInstruction*>(*block);
        for (; block != SgAsmCilInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmCilInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmCilInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmCilInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmCilInstruction::pool_size-1]);
        }
    }
}

void
SgAsmCilInstruction::deleteMemoryPool() {
    for (auto p: SgAsmCilInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmCilInstruction::next_node = nullptr;
    SgAsmCilInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmCilInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInstruction)};
    for (size_t blockIndex = SgAsmCilInstruction::pools.size(); (blockIndex * SgAsmCilInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInstruction) = %" PRIuPTR " SgAsmCilInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmCilInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmCilInstruction),SgAsmCilInstruction::pool_size);
        }
#endif

        SgAsmCilInstruction* pointer{static_cast<SgAsmCilInstruction*>(ROSE_MALLOC(SgAsmCilInstruction::pool_size * sizeof(SgAsmCilInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmCilInstruction::pool_size * sizeof(SgAsmCilInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmCilInstruction::pool_size * sizeof(SgAsmCilInstruction));
#endif
        SgAsmCilInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmCilInstruction::pool_size * sizeof(SgAsmCilInstruction), V_SgAsmCilInstruction));

        if (SgAsmCilInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmCilInstruction* blkptr{reinterpret_cast<SgAsmCilInstruction*>(SgAsmCilInstruction::pools[blockIndex-1])};
                blkptr[SgAsmCilInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmCilInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmCilInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmCilInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmCilInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmCilInstruction::pool_size - 1};
    SgAsmCilInstruction* testPointer{reinterpret_cast<SgAsmCilInstruction*>(SgAsmCilInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmCilInstruction::pool_size * (SgAsmCilInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmCilInstructionStroageClass
 * from its counterpart of SgAsmCilInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmCilInstruction::initializeStorageClassArray(SgAsmCilInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmCilInstruction::pools.begin(); block != SgAsmCilInstruction::pools.end(); ++block) {
        SgAsmCilInstruction* pointer{reinterpret_cast<SgAsmCilInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmCilInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 241579 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmJvmInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmJvmInstruction::SgAsmJvmInstruction(const SgAsmJvmInstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 241589 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmJvmInstruction*
SgAsmJvmInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmJvmInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmJvmInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInstruction)};
        unsigned long positionInPool{localIndex % SgAsmJvmInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmInstruction::pool_size};
        returnPointer = &(((SgAsmJvmInstruction*)(SgAsmJvmInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmJvmInstruction*
SgAsmJvmInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmJvmInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmJvmInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmJvmInstruction)};
        unsigned long positionInPool{localIndex % SgAsmJvmInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmJvmInstruction::pool_size};
        returnPointer = &(((SgAsmJvmInstruction*)(SgAsmJvmInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmJvmInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmJvmInstruction::pools.begin(); block != SgAsmJvmInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmJvmInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmJvmInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmJvmInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmJvmInstruction::pools.begin(); block != SgAsmJvmInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmJvmInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmJvmInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmJvmInstruction::clearMemoryPool() {
    SgAsmJvmInstruction* tempPointer{nullptr};
    if (SgAsmJvmInstruction::pools.empty() == false) {
        auto block{SgAsmJvmInstruction::pools.begin()};
        SgAsmJvmInstruction::next_node = reinterpret_cast<SgAsmJvmInstruction*>(*block);
        for (; block != SgAsmJvmInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmJvmInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmJvmInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmJvmInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmJvmInstruction::pool_size-1]);
        }
    }
}

void
SgAsmJvmInstruction::deleteMemoryPool() {
    for (auto p: SgAsmJvmInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmJvmInstruction::next_node = nullptr;
    SgAsmJvmInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmJvmInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmInstruction)};
    for (size_t blockIndex = SgAsmJvmInstruction::pools.size(); (blockIndex * SgAsmJvmInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmInstruction) = %" PRIuPTR " SgAsmJvmInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmJvmInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmJvmInstruction),SgAsmJvmInstruction::pool_size);
        }
#endif

        SgAsmJvmInstruction* pointer{static_cast<SgAsmJvmInstruction*>(ROSE_MALLOC(SgAsmJvmInstruction::pool_size * sizeof(SgAsmJvmInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmJvmInstruction::pool_size * sizeof(SgAsmJvmInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmJvmInstruction::pool_size * sizeof(SgAsmJvmInstruction));
#endif
        SgAsmJvmInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmJvmInstruction::pool_size * sizeof(SgAsmJvmInstruction), V_SgAsmJvmInstruction));

        if (SgAsmJvmInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmJvmInstruction* blkptr{reinterpret_cast<SgAsmJvmInstruction*>(SgAsmJvmInstruction::pools[blockIndex-1])};
                blkptr[SgAsmJvmInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmJvmInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmJvmInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmJvmInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmJvmInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmJvmInstruction::pool_size - 1};
    SgAsmJvmInstruction* testPointer{reinterpret_cast<SgAsmJvmInstruction*>(SgAsmJvmInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmJvmInstruction::pool_size * (SgAsmJvmInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmJvmInstructionStroageClass
 * from its counterpart of SgAsmJvmInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmJvmInstruction::initializeStorageClassArray(SgAsmJvmInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmJvmInstruction::pools.begin(); block != SgAsmJvmInstruction::pools.end(); ++block) {
        SgAsmJvmInstruction* pointer{reinterpret_cast<SgAsmJvmInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmJvmInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 241807 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmM68kInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmM68kInstruction::SgAsmM68kInstruction(const SgAsmM68kInstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 241817 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;
    p_dataFormat = storageSource.storageOf_dataFormat;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmM68kInstruction*
SgAsmM68kInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmM68kInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmM68kInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmM68kInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmM68kInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmM68kInstruction)};
        unsigned long positionInPool{localIndex % SgAsmM68kInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmM68kInstruction::pool_size};
        returnPointer = &(((SgAsmM68kInstruction*)(SgAsmM68kInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmM68kInstruction*
SgAsmM68kInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmM68kInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmM68kInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmM68kInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmM68kInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmM68kInstruction)};
        unsigned long positionInPool{localIndex % SgAsmM68kInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmM68kInstruction::pool_size};
        returnPointer = &(((SgAsmM68kInstruction*)(SgAsmM68kInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmM68kInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmM68kInstruction::pools.begin(); block != SgAsmM68kInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmM68kInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmM68kInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmM68kInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmM68kInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmM68kInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmM68kInstruction::pools.begin(); block != SgAsmM68kInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmM68kInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmM68kInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmM68kInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmM68kInstruction::clearMemoryPool() {
    SgAsmM68kInstruction* tempPointer{nullptr};
    if (SgAsmM68kInstruction::pools.empty() == false) {
        auto block{SgAsmM68kInstruction::pools.begin()};
        SgAsmM68kInstruction::next_node = reinterpret_cast<SgAsmM68kInstruction*>(*block);
        for (; block != SgAsmM68kInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmM68kInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmM68kInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmM68kInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmM68kInstruction::pool_size-1]);
        }
    }
}

void
SgAsmM68kInstruction::deleteMemoryPool() {
    for (auto p: SgAsmM68kInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmM68kInstruction::next_node = nullptr;
    SgAsmM68kInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmM68kInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmM68kInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmM68kInstruction)};
    for (size_t blockIndex = SgAsmM68kInstruction::pools.size(); (blockIndex * SgAsmM68kInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmM68kInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmM68kInstruction) = %" PRIuPTR " SgAsmM68kInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmM68kInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmM68kInstruction),SgAsmM68kInstruction::pool_size);
        }
#endif

        SgAsmM68kInstruction* pointer{static_cast<SgAsmM68kInstruction*>(ROSE_MALLOC(SgAsmM68kInstruction::pool_size * sizeof(SgAsmM68kInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmM68kInstruction::pool_size * sizeof(SgAsmM68kInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmM68kInstruction::pool_size * sizeof(SgAsmM68kInstruction));
#endif
        SgAsmM68kInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmM68kInstruction::pool_size * sizeof(SgAsmM68kInstruction), V_SgAsmM68kInstruction));

        if (SgAsmM68kInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmM68kInstruction* blkptr{reinterpret_cast<SgAsmM68kInstruction*>(SgAsmM68kInstruction::pools[blockIndex-1])};
                blkptr[SgAsmM68kInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmM68kInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmM68kInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmM68kInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmM68kInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmM68kInstruction::pool_size - 1};
    SgAsmM68kInstruction* testPointer{reinterpret_cast<SgAsmM68kInstruction*>(SgAsmM68kInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmM68kInstruction::pool_size * (SgAsmM68kInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmM68kInstructionStroageClass
 * from its counterpart of SgAsmM68kInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmM68kInstruction::initializeStorageClassArray(SgAsmM68kInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmM68kInstruction::pools.begin(); block != SgAsmM68kInstruction::pools.end(); ++block) {
        SgAsmM68kInstruction* pointer{reinterpret_cast<SgAsmM68kInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmM68kInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 242036 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmMipsInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmMipsInstruction::SgAsmMipsInstruction(const SgAsmMipsInstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 242046 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmMipsInstruction*
SgAsmMipsInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmMipsInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmMipsInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmMipsInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmMipsInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMipsInstruction)};
        unsigned long positionInPool{localIndex % SgAsmMipsInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmMipsInstruction::pool_size};
        returnPointer = &(((SgAsmMipsInstruction*)(SgAsmMipsInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmMipsInstruction*
SgAsmMipsInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmMipsInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmMipsInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmMipsInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmMipsInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmMipsInstruction)};
        unsigned long positionInPool{localIndex % SgAsmMipsInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmMipsInstruction::pool_size};
        returnPointer = &(((SgAsmMipsInstruction*)(SgAsmMipsInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmMipsInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmMipsInstruction::pools.begin(); block != SgAsmMipsInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmMipsInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmMipsInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmMipsInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmMipsInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmMipsInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmMipsInstruction::pools.begin(); block != SgAsmMipsInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmMipsInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmMipsInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmMipsInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmMipsInstruction::clearMemoryPool() {
    SgAsmMipsInstruction* tempPointer{nullptr};
    if (SgAsmMipsInstruction::pools.empty() == false) {
        auto block{SgAsmMipsInstruction::pools.begin()};
        SgAsmMipsInstruction::next_node = reinterpret_cast<SgAsmMipsInstruction*>(*block);
        for (; block != SgAsmMipsInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmMipsInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmMipsInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmMipsInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmMipsInstruction::pool_size-1]);
        }
    }
}

void
SgAsmMipsInstruction::deleteMemoryPool() {
    for (auto p: SgAsmMipsInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmMipsInstruction::next_node = nullptr;
    SgAsmMipsInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmMipsInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMipsInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmMipsInstruction)};
    for (size_t blockIndex = SgAsmMipsInstruction::pools.size(); (blockIndex * SgAsmMipsInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMipsInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmMipsInstruction) = %" PRIuPTR " SgAsmMipsInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmMipsInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmMipsInstruction),SgAsmMipsInstruction::pool_size);
        }
#endif

        SgAsmMipsInstruction* pointer{static_cast<SgAsmMipsInstruction*>(ROSE_MALLOC(SgAsmMipsInstruction::pool_size * sizeof(SgAsmMipsInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmMipsInstruction::pool_size * sizeof(SgAsmMipsInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmMipsInstruction::pool_size * sizeof(SgAsmMipsInstruction));
#endif
        SgAsmMipsInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmMipsInstruction::pool_size * sizeof(SgAsmMipsInstruction), V_SgAsmMipsInstruction));

        if (SgAsmMipsInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmMipsInstruction* blkptr{reinterpret_cast<SgAsmMipsInstruction*>(SgAsmMipsInstruction::pools[blockIndex-1])};
                blkptr[SgAsmMipsInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmMipsInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmMipsInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmMipsInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmMipsInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmMipsInstruction::pool_size - 1};
    SgAsmMipsInstruction* testPointer{reinterpret_cast<SgAsmMipsInstruction*>(SgAsmMipsInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmMipsInstruction::pool_size * (SgAsmMipsInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmMipsInstructionStroageClass
 * from its counterpart of SgAsmMipsInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmMipsInstruction::initializeStorageClassArray(SgAsmMipsInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmMipsInstruction::pools.begin(); block != SgAsmMipsInstruction::pools.end(); ++block) {
        SgAsmMipsInstruction* pointer{reinterpret_cast<SgAsmMipsInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmMipsInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 242264 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmNullInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmNullInstruction::SgAsmNullInstruction(const SgAsmNullInstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 242274 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmNullInstruction*
SgAsmNullInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmNullInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNullInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNullInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmNullInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNullInstruction)};
        unsigned long positionInPool{localIndex % SgAsmNullInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNullInstruction::pool_size};
        returnPointer = &(((SgAsmNullInstruction*)(SgAsmNullInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmNullInstruction*
SgAsmNullInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmNullInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNullInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNullInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmNullInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmNullInstruction)};
        unsigned long positionInPool{localIndex % SgAsmNullInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmNullInstruction::pool_size};
        returnPointer = &(((SgAsmNullInstruction*)(SgAsmNullInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmNullInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmNullInstruction::pools.begin(); block != SgAsmNullInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNullInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmNullInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmNullInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmNullInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmNullInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmNullInstruction::pools.begin(); block != SgAsmNullInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmNullInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmNullInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmNullInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmNullInstruction::clearMemoryPool() {
    SgAsmNullInstruction* tempPointer{nullptr};
    if (SgAsmNullInstruction::pools.empty() == false) {
        auto block{SgAsmNullInstruction::pools.begin()};
        SgAsmNullInstruction::next_node = reinterpret_cast<SgAsmNullInstruction*>(*block);
        for (; block != SgAsmNullInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmNullInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmNullInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmNullInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmNullInstruction::pool_size-1]);
        }
    }
}

void
SgAsmNullInstruction::deleteMemoryPool() {
    for (auto p: SgAsmNullInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmNullInstruction::next_node = nullptr;
    SgAsmNullInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmNullInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNullInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNullInstruction)};
    for (size_t blockIndex = SgAsmNullInstruction::pools.size(); (blockIndex * SgAsmNullInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNullInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNullInstruction) = %" PRIuPTR " SgAsmNullInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmNullInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmNullInstruction),SgAsmNullInstruction::pool_size);
        }
#endif

        SgAsmNullInstruction* pointer{static_cast<SgAsmNullInstruction*>(ROSE_MALLOC(SgAsmNullInstruction::pool_size * sizeof(SgAsmNullInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmNullInstruction::pool_size * sizeof(SgAsmNullInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmNullInstruction::pool_size * sizeof(SgAsmNullInstruction));
#endif
        SgAsmNullInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmNullInstruction::pool_size * sizeof(SgAsmNullInstruction), V_SgAsmNullInstruction));

        if (SgAsmNullInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmNullInstruction* blkptr{reinterpret_cast<SgAsmNullInstruction*>(SgAsmNullInstruction::pools[blockIndex-1])};
                blkptr[SgAsmNullInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmNullInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmNullInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmNullInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmNullInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmNullInstruction::pool_size - 1};
    SgAsmNullInstruction* testPointer{reinterpret_cast<SgAsmNullInstruction*>(SgAsmNullInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmNullInstruction::pool_size * (SgAsmNullInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmNullInstructionStroageClass
 * from its counterpart of SgAsmNullInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmNullInstruction::initializeStorageClassArray(SgAsmNullInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmNullInstruction::pools.begin(); block != SgAsmNullInstruction::pools.end(); ++block) {
        SgAsmNullInstruction* pointer{reinterpret_cast<SgAsmNullInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmNullInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 242491 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPowerpcInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmPowerpcInstruction::SgAsmPowerpcInstruction(const SgAsmPowerpcInstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 242501 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPowerpcInstruction*
SgAsmPowerpcInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPowerpcInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPowerpcInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPowerpcInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPowerpcInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPowerpcInstruction)};
        unsigned long positionInPool{localIndex % SgAsmPowerpcInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPowerpcInstruction::pool_size};
        returnPointer = &(((SgAsmPowerpcInstruction*)(SgAsmPowerpcInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPowerpcInstruction*
SgAsmPowerpcInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPowerpcInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPowerpcInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPowerpcInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPowerpcInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPowerpcInstruction)};
        unsigned long positionInPool{localIndex % SgAsmPowerpcInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPowerpcInstruction::pool_size};
        returnPointer = &(((SgAsmPowerpcInstruction*)(SgAsmPowerpcInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPowerpcInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPowerpcInstruction::pools.begin(); block != SgAsmPowerpcInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPowerpcInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmPowerpcInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPowerpcInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPowerpcInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPowerpcInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPowerpcInstruction::pools.begin(); block != SgAsmPowerpcInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPowerpcInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmPowerpcInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPowerpcInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPowerpcInstruction::clearMemoryPool() {
    SgAsmPowerpcInstruction* tempPointer{nullptr};
    if (SgAsmPowerpcInstruction::pools.empty() == false) {
        auto block{SgAsmPowerpcInstruction::pools.begin()};
        SgAsmPowerpcInstruction::next_node = reinterpret_cast<SgAsmPowerpcInstruction*>(*block);
        for (; block != SgAsmPowerpcInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPowerpcInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPowerpcInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPowerpcInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPowerpcInstruction::pool_size-1]);
        }
    }
}

void
SgAsmPowerpcInstruction::deleteMemoryPool() {
    for (auto p: SgAsmPowerpcInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmPowerpcInstruction::next_node = nullptr;
    SgAsmPowerpcInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPowerpcInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPowerpcInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPowerpcInstruction)};
    for (size_t blockIndex = SgAsmPowerpcInstruction::pools.size(); (blockIndex * SgAsmPowerpcInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPowerpcInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPowerpcInstruction) = %" PRIuPTR " SgAsmPowerpcInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPowerpcInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPowerpcInstruction),SgAsmPowerpcInstruction::pool_size);
        }
#endif

        SgAsmPowerpcInstruction* pointer{static_cast<SgAsmPowerpcInstruction*>(ROSE_MALLOC(SgAsmPowerpcInstruction::pool_size * sizeof(SgAsmPowerpcInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPowerpcInstruction::pool_size * sizeof(SgAsmPowerpcInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPowerpcInstruction::pool_size * sizeof(SgAsmPowerpcInstruction));
#endif
        SgAsmPowerpcInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPowerpcInstruction::pool_size * sizeof(SgAsmPowerpcInstruction), V_SgAsmPowerpcInstruction));

        if (SgAsmPowerpcInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPowerpcInstruction* blkptr{reinterpret_cast<SgAsmPowerpcInstruction*>(SgAsmPowerpcInstruction::pools[blockIndex-1])};
                blkptr[SgAsmPowerpcInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPowerpcInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPowerpcInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPowerpcInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPowerpcInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPowerpcInstruction::pool_size - 1};
    SgAsmPowerpcInstruction* testPointer{reinterpret_cast<SgAsmPowerpcInstruction*>(SgAsmPowerpcInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPowerpcInstruction::pool_size * (SgAsmPowerpcInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPowerpcInstructionStroageClass
 * from its counterpart of SgAsmPowerpcInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPowerpcInstruction::initializeStorageClassArray(SgAsmPowerpcInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPowerpcInstruction::pools.begin(); block != SgAsmPowerpcInstruction::pools.end(); ++block) {
        SgAsmPowerpcInstruction* pointer{reinterpret_cast<SgAsmPowerpcInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmPowerpcInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 242719 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmUserInstruction that takes its corresponding StorageClass as parameter
 */
SgAsmUserInstruction::SgAsmUserInstruction(const SgAsmUserInstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 242729 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmUserInstruction*
SgAsmUserInstruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmUserInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUserInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUserInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmUserInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUserInstruction)};
        unsigned long positionInPool{localIndex % SgAsmUserInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUserInstruction::pool_size};
        returnPointer = &(((SgAsmUserInstruction*)(SgAsmUserInstruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmUserInstruction*
SgAsmUserInstruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmUserInstruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUserInstruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUserInstruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmUserInstruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmUserInstruction)};
        unsigned long positionInPool{localIndex % SgAsmUserInstruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmUserInstruction::pool_size};
        returnPointer = &(((SgAsmUserInstruction*)(SgAsmUserInstruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmUserInstruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmUserInstruction::pools.begin(); block != SgAsmUserInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUserInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmUserInstruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmUserInstruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmUserInstruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmUserInstruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmUserInstruction::pools.begin(); block != SgAsmUserInstruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmUserInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmUserInstruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmUserInstruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmUserInstruction::clearMemoryPool() {
    SgAsmUserInstruction* tempPointer{nullptr};
    if (SgAsmUserInstruction::pools.empty() == false) {
        auto block{SgAsmUserInstruction::pools.begin()};
        SgAsmUserInstruction::next_node = reinterpret_cast<SgAsmUserInstruction*>(*block);
        for (; block != SgAsmUserInstruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmUserInstruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmUserInstruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmUserInstruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmUserInstruction::pool_size-1]);
        }
    }
}

void
SgAsmUserInstruction::deleteMemoryPool() {
    for (auto p: SgAsmUserInstruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmUserInstruction::next_node = nullptr;
    SgAsmUserInstruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmUserInstruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUserInstruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUserInstruction)};
    for (size_t blockIndex = SgAsmUserInstruction::pools.size(); (blockIndex * SgAsmUserInstruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUserInstruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUserInstruction) = %" PRIuPTR " SgAsmUserInstruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmUserInstruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmUserInstruction),SgAsmUserInstruction::pool_size);
        }
#endif

        SgAsmUserInstruction* pointer{static_cast<SgAsmUserInstruction*>(ROSE_MALLOC(SgAsmUserInstruction::pool_size * sizeof(SgAsmUserInstruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmUserInstruction::pool_size * sizeof(SgAsmUserInstruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmUserInstruction::pool_size * sizeof(SgAsmUserInstruction));
#endif
        SgAsmUserInstruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmUserInstruction::pool_size * sizeof(SgAsmUserInstruction), V_SgAsmUserInstruction));

        if (SgAsmUserInstruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmUserInstruction* blkptr{reinterpret_cast<SgAsmUserInstruction*>(SgAsmUserInstruction::pools[blockIndex-1])};
                blkptr[SgAsmUserInstruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmUserInstruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmUserInstruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmUserInstruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmUserInstruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmUserInstruction::pool_size - 1};
    SgAsmUserInstruction* testPointer{reinterpret_cast<SgAsmUserInstruction*>(SgAsmUserInstruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmUserInstruction::pool_size * (SgAsmUserInstruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmUserInstructionStroageClass
 * from its counterpart of SgAsmUserInstruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmUserInstruction::initializeStorageClassArray(SgAsmUserInstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmUserInstruction::pools.begin(); block != SgAsmUserInstruction::pools.end(); ++block) {
        SgAsmUserInstruction* pointer{reinterpret_cast<SgAsmUserInstruction*>(*block)};
        for (unsigned i = 0; i < SgAsmUserInstruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 242947 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmX86Instruction that takes its corresponding StorageClass as parameter
 */
SgAsmX86Instruction::SgAsmX86Instruction(const SgAsmX86InstructionStorageClass& storageSource)  : SgAsmInstruction (storageSource) {

/* #line 242957 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_kind = storageSource.storageOf_kind;
    p_baseSize = storageSource.storageOf_baseSize;
    p_operandSize = storageSource.storageOf_operandSize;
    p_addressSize = storageSource.storageOf_addressSize;
    p_lockPrefix = storageSource.storageOf_lockPrefix;
    p_repeatPrefix = storageSource.storageOf_repeatPrefix;
    p_branchPrediction = storageSource.storageOf_branchPrediction;
    p_segmentOverride = storageSource.storageOf_segmentOverride;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmX86Instruction*
SgAsmX86Instruction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmX86Instruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmX86Instruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmX86Instruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmX86Instruction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmX86Instruction)};
        unsigned long positionInPool{localIndex % SgAsmX86Instruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmX86Instruction::pool_size};
        returnPointer = &(((SgAsmX86Instruction*)(SgAsmX86Instruction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmX86Instruction*
SgAsmX86Instruction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmX86Instruction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmX86Instruction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmX86Instruction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmX86Instruction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmX86Instruction)};
        unsigned long positionInPool{localIndex % SgAsmX86Instruction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmX86Instruction::pool_size};
        returnPointer = &(((SgAsmX86Instruction*)(SgAsmX86Instruction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmX86Instruction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmX86Instruction::pools.begin(); block != SgAsmX86Instruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmX86Instruction*>(*block)};
        for (unsigned i = 0; i < SgAsmX86Instruction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmX86Instruction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmX86Instruction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmX86Instruction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmX86Instruction::pools.begin(); block != SgAsmX86Instruction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmX86Instruction*>(*block)};
        for (unsigned i = 0; i < SgAsmX86Instruction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmX86Instruction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmX86Instruction::clearMemoryPool() {
    SgAsmX86Instruction* tempPointer{nullptr};
    if (SgAsmX86Instruction::pools.empty() == false) {
        auto block{SgAsmX86Instruction::pools.begin()};
        SgAsmX86Instruction::next_node = reinterpret_cast<SgAsmX86Instruction*>(*block);
        for (; block != SgAsmX86Instruction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmX86Instruction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmX86Instruction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmX86Instruction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmX86Instruction::pool_size-1]);
        }
    }
}

void
SgAsmX86Instruction::deleteMemoryPool() {
    for (auto p: SgAsmX86Instruction::pools) {
        ROSE_FREE(p);
    }
    SgAsmX86Instruction::next_node = nullptr;
    SgAsmX86Instruction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmX86Instruction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmX86Instruction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmX86Instruction)};
    for (size_t blockIndex = SgAsmX86Instruction::pools.size(); (blockIndex * SgAsmX86Instruction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmX86Instruction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmX86Instruction) = %" PRIuPTR " SgAsmX86Instruction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmX86Instruction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmX86Instruction),SgAsmX86Instruction::pool_size);
        }
#endif

        SgAsmX86Instruction* pointer{static_cast<SgAsmX86Instruction*>(ROSE_MALLOC(SgAsmX86Instruction::pool_size * sizeof(SgAsmX86Instruction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmX86Instruction::pool_size * sizeof(SgAsmX86Instruction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmX86Instruction::pool_size * sizeof(SgAsmX86Instruction));
#endif
        SgAsmX86Instruction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmX86Instruction::pool_size * sizeof(SgAsmX86Instruction), V_SgAsmX86Instruction));

        if (SgAsmX86Instruction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmX86Instruction* blkptr{reinterpret_cast<SgAsmX86Instruction*>(SgAsmX86Instruction::pools[blockIndex-1])};
                blkptr[SgAsmX86Instruction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmX86Instruction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmX86Instruction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmX86Instruction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmX86Instruction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmX86Instruction::pool_size - 1};
    SgAsmX86Instruction* testPointer{reinterpret_cast<SgAsmX86Instruction*>(SgAsmX86Instruction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmX86Instruction::pool_size * (SgAsmX86Instruction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmX86InstructionStroageClass
 * from its counterpart of SgAsmX86Instruction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmX86Instruction::initializeStorageClassArray(SgAsmX86InstructionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmX86Instruction::pools.begin(); block != SgAsmX86Instruction::pools.end(); ++block) {
        SgAsmX86Instruction* pointer{reinterpret_cast<SgAsmX86Instruction*>(*block)};
        for (unsigned i = 0; i < SgAsmX86Instruction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 243182 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmStaticData that takes its corresponding StorageClass as parameter
 */
SgAsmStaticData::SgAsmStaticData(const SgAsmStaticDataStorageClass& storageSource)  : SgAsmStatement (storageSource) {

/* #line 243192 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_rawBytes = storageSource.storageOf_rawBytes.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmStaticData*
SgAsmStaticData::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmStaticData* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStaticData)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStaticData + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmStaticData)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStaticData)};
        unsigned long positionInPool{localIndex % SgAsmStaticData::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStaticData::pool_size};
        returnPointer = &(((SgAsmStaticData*)(SgAsmStaticData::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmStaticData*
SgAsmStaticData::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmStaticData* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStaticData)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStaticData + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmStaticData)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmStaticData)};
        unsigned long positionInPool{localIndex % SgAsmStaticData::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmStaticData::pool_size};
        returnPointer = &(((SgAsmStaticData*)(SgAsmStaticData::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmStaticData::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmStaticData::pools.begin(); block != SgAsmStaticData::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStaticData*>(*block)};
        for (unsigned i = 0; i < SgAsmStaticData::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmStaticData*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmStaticData::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmStaticData* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmStaticData::pools.begin(); block != SgAsmStaticData::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmStaticData*>(*block)};
        for (unsigned i = 0; i < SgAsmStaticData::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmStaticData::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmStaticData::clearMemoryPool() {
    SgAsmStaticData* tempPointer{nullptr};
    if (SgAsmStaticData::pools.empty() == false) {
        auto block{SgAsmStaticData::pools.begin()};
        SgAsmStaticData::next_node = reinterpret_cast<SgAsmStaticData*>(*block);
        for (; block != SgAsmStaticData::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmStaticData*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmStaticData::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmStaticData::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmStaticData::pool_size-1]);
        }
    }
}

void
SgAsmStaticData::deleteMemoryPool() {
    for (auto p: SgAsmStaticData::pools) {
        ROSE_FREE(p);
    }
    SgAsmStaticData::next_node = nullptr;
    SgAsmStaticData::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmStaticData::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStaticData) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStaticData)};
    for (size_t blockIndex = SgAsmStaticData::pools.size(); (blockIndex * SgAsmStaticData::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStaticData) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStaticData) = %" PRIuPTR " SgAsmStaticData::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmStaticData),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmStaticData),SgAsmStaticData::pool_size);
        }
#endif

        SgAsmStaticData* pointer{static_cast<SgAsmStaticData*>(ROSE_MALLOC(SgAsmStaticData::pool_size * sizeof(SgAsmStaticData)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmStaticData::pool_size * sizeof(SgAsmStaticData));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmStaticData::pool_size * sizeof(SgAsmStaticData));
#endif
        SgAsmStaticData::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmStaticData::pool_size * sizeof(SgAsmStaticData), V_SgAsmStaticData));

        if (SgAsmStaticData::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmStaticData* blkptr{reinterpret_cast<SgAsmStaticData*>(SgAsmStaticData::pools[blockIndex-1])};
                blkptr[SgAsmStaticData::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmStaticData::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmStaticData::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmStaticData::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmStaticData::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmStaticData::pool_size - 1};
    SgAsmStaticData* testPointer{reinterpret_cast<SgAsmStaticData*>(SgAsmStaticData::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmStaticData::pool_size * (SgAsmStaticData::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmStaticDataStroageClass
 * from its counterpart of SgAsmStaticData. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmStaticData::initializeStorageClassArray(SgAsmStaticDataStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmStaticData::pools.begin(); block != SgAsmStaticData::pools.end(); ++block) {
        SgAsmStaticData* pointer{reinterpret_cast<SgAsmStaticData*>(*block)};
        for (unsigned i = 0; i < SgAsmStaticData::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 243410 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmSynthesizedDeclaration that takes its corresponding StorageClass as parameter
 */
SgAsmSynthesizedDeclaration::SgAsmSynthesizedDeclaration(const SgAsmSynthesizedDeclarationStorageClass& storageSource)  : SgAsmStatement (storageSource) {

/* #line 243420 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmSynthesizedDeclaration*
SgAsmSynthesizedDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmSynthesizedDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmSynthesizedDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmSynthesizedDeclaration::pool_size};
        returnPointer = &(((SgAsmSynthesizedDeclaration*)(SgAsmSynthesizedDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmSynthesizedDeclaration*
SgAsmSynthesizedDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmSynthesizedDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmSynthesizedDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmSynthesizedDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmSynthesizedDeclaration::pool_size};
        returnPointer = &(((SgAsmSynthesizedDeclaration*)(SgAsmSynthesizedDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmSynthesizedDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmSynthesizedDeclaration::pools.begin(); block != SgAsmSynthesizedDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmSynthesizedDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmSynthesizedDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmSynthesizedDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmSynthesizedDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmSynthesizedDeclaration::pools.begin(); block != SgAsmSynthesizedDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmSynthesizedDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmSynthesizedDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmSynthesizedDeclaration::clearMemoryPool() {
    SgAsmSynthesizedDeclaration* tempPointer{nullptr};
    if (SgAsmSynthesizedDeclaration::pools.empty() == false) {
        auto block{SgAsmSynthesizedDeclaration::pools.begin()};
        SgAsmSynthesizedDeclaration::next_node = reinterpret_cast<SgAsmSynthesizedDeclaration*>(*block);
        for (; block != SgAsmSynthesizedDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmSynthesizedDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmSynthesizedDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmSynthesizedDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmSynthesizedDeclaration::pool_size-1]);
        }
    }
}

void
SgAsmSynthesizedDeclaration::deleteMemoryPool() {
    for (auto p: SgAsmSynthesizedDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgAsmSynthesizedDeclaration::next_node = nullptr;
    SgAsmSynthesizedDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmSynthesizedDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedDeclaration)};
    for (size_t blockIndex = SgAsmSynthesizedDeclaration::pools.size(); (blockIndex * SgAsmSynthesizedDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedDeclaration) = %" PRIuPTR " SgAsmSynthesizedDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedDeclaration),SgAsmSynthesizedDeclaration::pool_size);
        }
#endif

        SgAsmSynthesizedDeclaration* pointer{static_cast<SgAsmSynthesizedDeclaration*>(ROSE_MALLOC(SgAsmSynthesizedDeclaration::pool_size * sizeof(SgAsmSynthesizedDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmSynthesizedDeclaration::pool_size * sizeof(SgAsmSynthesizedDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmSynthesizedDeclaration::pool_size * sizeof(SgAsmSynthesizedDeclaration));
#endif
        SgAsmSynthesizedDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmSynthesizedDeclaration::pool_size * sizeof(SgAsmSynthesizedDeclaration), V_SgAsmSynthesizedDeclaration));

        if (SgAsmSynthesizedDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmSynthesizedDeclaration* blkptr{reinterpret_cast<SgAsmSynthesizedDeclaration*>(SgAsmSynthesizedDeclaration::pools[blockIndex-1])};
                blkptr[SgAsmSynthesizedDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmSynthesizedDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmSynthesizedDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmSynthesizedDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmSynthesizedDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmSynthesizedDeclaration::pool_size - 1};
    SgAsmSynthesizedDeclaration* testPointer{reinterpret_cast<SgAsmSynthesizedDeclaration*>(SgAsmSynthesizedDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmSynthesizedDeclaration::pool_size * (SgAsmSynthesizedDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmSynthesizedDeclarationStroageClass
 * from its counterpart of SgAsmSynthesizedDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmSynthesizedDeclaration::initializeStorageClassArray(SgAsmSynthesizedDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmSynthesizedDeclaration::pools.begin(); block != SgAsmSynthesizedDeclaration::pools.end(); ++block) {
        SgAsmSynthesizedDeclaration* pointer{reinterpret_cast<SgAsmSynthesizedDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 243637 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmFunction that takes its corresponding StorageClass as parameter
 */
SgAsmFunction::SgAsmFunction(const SgAsmFunctionStorageClass& storageSource)  : SgAsmSynthesizedDeclaration (storageSource) {

/* #line 243647 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
    p_reason = storageSource.storageOf_reason;
    p_reasonComment = storageSource.storageOf_reasonComment.rebuildDataStoredInEasyStorageClass();
    p_functionKind = storageSource.storageOf_functionKind;
    p_mayReturn = storageSource.storageOf_mayReturn;
    p_nameMd5 = storageSource.storageOf_nameMd5.rebuildDataStoredInEasyStorageClass();
    p_statementList = storageSource.storageOf_statementList.rebuildDataStoredInEasyStorageClass();
    SgAsmStatementPtrList::iterator i_statementList = p_statementList.begin();
    for ( ; i_statementList != p_statementList.end(); ++i_statementList) {
        (*i_statementList) = (SgAsmStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_statementList)) );
    }
    p_dest = storageSource.storageOf_dest.rebuildDataStoredInEasyStorageClass();
    SgAsmStatementPtrList::iterator i_dest = p_dest.begin();
    for ( ; i_dest != p_dest.end(); ++i_dest) {
        (*i_dest) = (SgAsmStatementPtrList::value_type)(AST_FILE_IO::getSgClassPointerFromGlobalIndex ( (unsigned long)(*i_dest)) );
    }
    p_entryVa = storageSource.storageOf_entryVa;
     p_symbolTable = (SgSymbolTable*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_symbolTable));
    p_cachedVertex = storageSource.storageOf_cachedVertex;
    p_stackDelta = storageSource.storageOf_stackDelta;
    p_callingConvention = storageSource.storageOf_callingConvention.rebuildDataStoredInEasyStorageClass();


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmFunction*
SgAsmFunction::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmFunction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFunction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFunction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFunction)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFunction)};
        unsigned long positionInPool{localIndex % SgAsmFunction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmFunction::pool_size};
        returnPointer = &(((SgAsmFunction*)(SgAsmFunction::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmFunction*
SgAsmFunction::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmFunction* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFunction)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFunction + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFunction)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmFunction)};
        unsigned long positionInPool{localIndex % SgAsmFunction::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmFunction::pool_size};
        returnPointer = &(((SgAsmFunction*)(SgAsmFunction::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmFunction::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmFunction::pools.begin(); block != SgAsmFunction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmFunction*>(*block)};
        for (unsigned i = 0; i < SgAsmFunction::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmFunction*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmFunction::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmFunction* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmFunction::pools.begin(); block != SgAsmFunction::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmFunction*>(*block)};
        for (unsigned i = 0; i < SgAsmFunction::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmFunction::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmFunction::clearMemoryPool() {
    SgAsmFunction* tempPointer{nullptr};
    if (SgAsmFunction::pools.empty() == false) {
        auto block{SgAsmFunction::pools.begin()};
        SgAsmFunction::next_node = reinterpret_cast<SgAsmFunction*>(*block);
        for (; block != SgAsmFunction::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmFunction*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmFunction::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmFunction::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmFunction::pool_size-1]);
        }
    }
}

void
SgAsmFunction::deleteMemoryPool() {
    for (auto p: SgAsmFunction::pools) {
        ROSE_FREE(p);
    }
    SgAsmFunction::next_node = nullptr;
    SgAsmFunction::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmFunction::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFunction) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFunction)};
    for (size_t blockIndex = SgAsmFunction::pools.size(); (blockIndex * SgAsmFunction::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFunction) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFunction) = %" PRIuPTR " SgAsmFunction::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFunction),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFunction),SgAsmFunction::pool_size);
        }
#endif

        SgAsmFunction* pointer{static_cast<SgAsmFunction*>(ROSE_MALLOC(SgAsmFunction::pool_size * sizeof(SgAsmFunction)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmFunction::pool_size * sizeof(SgAsmFunction));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmFunction::pool_size * sizeof(SgAsmFunction));
#endif
        SgAsmFunction::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmFunction::pool_size * sizeof(SgAsmFunction), V_SgAsmFunction));

        if (SgAsmFunction::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmFunction* blkptr{reinterpret_cast<SgAsmFunction*>(SgAsmFunction::pools[blockIndex-1])};
                blkptr[SgAsmFunction::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmFunction::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmFunction::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmFunction::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmFunction::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmFunction::pool_size - 1};
    SgAsmFunction* testPointer{reinterpret_cast<SgAsmFunction*>(SgAsmFunction::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmFunction::pool_size * (SgAsmFunction::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmFunctionStroageClass
 * from its counterpart of SgAsmFunction. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmFunction::initializeStorageClassArray(SgAsmFunctionStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmFunction::pools.begin(); block != SgAsmFunction::pools.end(); ++block) {
        SgAsmFunction* pointer{reinterpret_cast<SgAsmFunction*>(*block)};
        for (unsigned i = 0; i < SgAsmFunction::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 243885 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmSynthesizedDataStructureDeclaration that takes its corresponding StorageClass as parameter
 */
SgAsmSynthesizedDataStructureDeclaration::SgAsmSynthesizedDataStructureDeclaration(const SgAsmSynthesizedDataStructureDeclarationStorageClass& storageSource)  : SgAsmSynthesizedDeclaration (storageSource) {

/* #line 243895 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmSynthesizedDataStructureDeclaration*
SgAsmSynthesizedDataStructureDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmSynthesizedDataStructureDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedDataStructureDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedDataStructureDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedDataStructureDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDataStructureDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmSynthesizedDataStructureDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmSynthesizedDataStructureDeclaration::pool_size};
        returnPointer = &(((SgAsmSynthesizedDataStructureDeclaration*)(SgAsmSynthesizedDataStructureDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmSynthesizedDataStructureDeclaration*
SgAsmSynthesizedDataStructureDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmSynthesizedDataStructureDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedDataStructureDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedDataStructureDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedDataStructureDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmSynthesizedDataStructureDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmSynthesizedDataStructureDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmSynthesizedDataStructureDeclaration::pool_size};
        returnPointer = &(((SgAsmSynthesizedDataStructureDeclaration*)(SgAsmSynthesizedDataStructureDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmSynthesizedDataStructureDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmSynthesizedDataStructureDeclaration::pools.begin(); block != SgAsmSynthesizedDataStructureDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmSynthesizedDataStructureDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedDataStructureDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmSynthesizedDataStructureDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmSynthesizedDataStructureDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmSynthesizedDataStructureDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmSynthesizedDataStructureDeclaration::pools.begin(); block != SgAsmSynthesizedDataStructureDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmSynthesizedDataStructureDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedDataStructureDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmSynthesizedDataStructureDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmSynthesizedDataStructureDeclaration::clearMemoryPool() {
    SgAsmSynthesizedDataStructureDeclaration* tempPointer{nullptr};
    if (SgAsmSynthesizedDataStructureDeclaration::pools.empty() == false) {
        auto block{SgAsmSynthesizedDataStructureDeclaration::pools.begin()};
        SgAsmSynthesizedDataStructureDeclaration::next_node = reinterpret_cast<SgAsmSynthesizedDataStructureDeclaration*>(*block);
        for (; block != SgAsmSynthesizedDataStructureDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmSynthesizedDataStructureDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmSynthesizedDataStructureDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmSynthesizedDataStructureDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmSynthesizedDataStructureDeclaration::pool_size-1]);
        }
    }
}

void
SgAsmSynthesizedDataStructureDeclaration::deleteMemoryPool() {
    for (auto p: SgAsmSynthesizedDataStructureDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgAsmSynthesizedDataStructureDeclaration::next_node = nullptr;
    SgAsmSynthesizedDataStructureDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmSynthesizedDataStructureDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDataStructureDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedDataStructureDeclaration)};
    for (size_t blockIndex = SgAsmSynthesizedDataStructureDeclaration::pools.size(); (blockIndex * SgAsmSynthesizedDataStructureDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDataStructureDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedDataStructureDeclaration) = %" PRIuPTR " SgAsmSynthesizedDataStructureDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedDataStructureDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedDataStructureDeclaration),SgAsmSynthesizedDataStructureDeclaration::pool_size);
        }
#endif

        SgAsmSynthesizedDataStructureDeclaration* pointer{static_cast<SgAsmSynthesizedDataStructureDeclaration*>(ROSE_MALLOC(SgAsmSynthesizedDataStructureDeclaration::pool_size * sizeof(SgAsmSynthesizedDataStructureDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmSynthesizedDataStructureDeclaration::pool_size * sizeof(SgAsmSynthesizedDataStructureDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmSynthesizedDataStructureDeclaration::pool_size * sizeof(SgAsmSynthesizedDataStructureDeclaration));
#endif
        SgAsmSynthesizedDataStructureDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmSynthesizedDataStructureDeclaration::pool_size * sizeof(SgAsmSynthesizedDataStructureDeclaration), V_SgAsmSynthesizedDataStructureDeclaration));

        if (SgAsmSynthesizedDataStructureDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmSynthesizedDataStructureDeclaration* blkptr{reinterpret_cast<SgAsmSynthesizedDataStructureDeclaration*>(SgAsmSynthesizedDataStructureDeclaration::pools[blockIndex-1])};
                blkptr[SgAsmSynthesizedDataStructureDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmSynthesizedDataStructureDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmSynthesizedDataStructureDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmSynthesizedDataStructureDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmSynthesizedDataStructureDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmSynthesizedDataStructureDeclaration::pool_size - 1};
    SgAsmSynthesizedDataStructureDeclaration* testPointer{reinterpret_cast<SgAsmSynthesizedDataStructureDeclaration*>(SgAsmSynthesizedDataStructureDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmSynthesizedDataStructureDeclaration::pool_size * (SgAsmSynthesizedDataStructureDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmSynthesizedDataStructureDeclarationStroageClass
 * from its counterpart of SgAsmSynthesizedDataStructureDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmSynthesizedDataStructureDeclaration::initializeStorageClassArray(SgAsmSynthesizedDataStructureDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmSynthesizedDataStructureDeclaration::pools.begin(); block != SgAsmSynthesizedDataStructureDeclaration::pools.end(); ++block) {
        SgAsmSynthesizedDataStructureDeclaration* pointer{reinterpret_cast<SgAsmSynthesizedDataStructureDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedDataStructureDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 244112 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmSynthesizedFieldDeclaration that takes its corresponding StorageClass as parameter
 */
SgAsmSynthesizedFieldDeclaration::SgAsmSynthesizedFieldDeclaration(const SgAsmSynthesizedFieldDeclarationStorageClass& storageSource)  : SgAsmSynthesizedDeclaration (storageSource) {

/* #line 244122 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_name = storageSource.storageOf_name.rebuildDataStoredInEasyStorageClass();
    p_offset = storageSource.storageOf_offset;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmSynthesizedFieldDeclaration*
SgAsmSynthesizedFieldDeclaration::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmSynthesizedFieldDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedFieldDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedFieldDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmSynthesizedFieldDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedFieldDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmSynthesizedFieldDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmSynthesizedFieldDeclaration::pool_size};
        returnPointer = &(((SgAsmSynthesizedFieldDeclaration*)(SgAsmSynthesizedFieldDeclaration::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmSynthesizedFieldDeclaration*
SgAsmSynthesizedFieldDeclaration::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmSynthesizedFieldDeclaration* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedFieldDeclaration)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedFieldDeclaration + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmSynthesizedFieldDeclaration)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmSynthesizedFieldDeclaration)};
        unsigned long positionInPool{localIndex % SgAsmSynthesizedFieldDeclaration::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmSynthesizedFieldDeclaration::pool_size};
        returnPointer = &(((SgAsmSynthesizedFieldDeclaration*)(SgAsmSynthesizedFieldDeclaration::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmSynthesizedFieldDeclaration::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmSynthesizedFieldDeclaration::pools.begin(); block != SgAsmSynthesizedFieldDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmSynthesizedFieldDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedFieldDeclaration::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmSynthesizedFieldDeclaration*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmSynthesizedFieldDeclaration::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmSynthesizedFieldDeclaration* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmSynthesizedFieldDeclaration::pools.begin(); block != SgAsmSynthesizedFieldDeclaration::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmSynthesizedFieldDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedFieldDeclaration::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmSynthesizedFieldDeclaration::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmSynthesizedFieldDeclaration::clearMemoryPool() {
    SgAsmSynthesizedFieldDeclaration* tempPointer{nullptr};
    if (SgAsmSynthesizedFieldDeclaration::pools.empty() == false) {
        auto block{SgAsmSynthesizedFieldDeclaration::pools.begin()};
        SgAsmSynthesizedFieldDeclaration::next_node = reinterpret_cast<SgAsmSynthesizedFieldDeclaration*>(*block);
        for (; block != SgAsmSynthesizedFieldDeclaration::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmSynthesizedFieldDeclaration*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmSynthesizedFieldDeclaration::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmSynthesizedFieldDeclaration::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmSynthesizedFieldDeclaration::pool_size-1]);
        }
    }
}

void
SgAsmSynthesizedFieldDeclaration::deleteMemoryPool() {
    for (auto p: SgAsmSynthesizedFieldDeclaration::pools) {
        ROSE_FREE(p);
    }
    SgAsmSynthesizedFieldDeclaration::next_node = nullptr;
    SgAsmSynthesizedFieldDeclaration::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmSynthesizedFieldDeclaration::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedFieldDeclaration) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedFieldDeclaration)};
    for (size_t blockIndex = SgAsmSynthesizedFieldDeclaration::pools.size(); (blockIndex * SgAsmSynthesizedFieldDeclaration::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedFieldDeclaration) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedFieldDeclaration) = %" PRIuPTR " SgAsmSynthesizedFieldDeclaration::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmSynthesizedFieldDeclaration),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmSynthesizedFieldDeclaration),SgAsmSynthesizedFieldDeclaration::pool_size);
        }
#endif

        SgAsmSynthesizedFieldDeclaration* pointer{static_cast<SgAsmSynthesizedFieldDeclaration*>(ROSE_MALLOC(SgAsmSynthesizedFieldDeclaration::pool_size * sizeof(SgAsmSynthesizedFieldDeclaration)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmSynthesizedFieldDeclaration::pool_size * sizeof(SgAsmSynthesizedFieldDeclaration));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmSynthesizedFieldDeclaration::pool_size * sizeof(SgAsmSynthesizedFieldDeclaration));
#endif
        SgAsmSynthesizedFieldDeclaration::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmSynthesizedFieldDeclaration::pool_size * sizeof(SgAsmSynthesizedFieldDeclaration), V_SgAsmSynthesizedFieldDeclaration));

        if (SgAsmSynthesizedFieldDeclaration::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmSynthesizedFieldDeclaration* blkptr{reinterpret_cast<SgAsmSynthesizedFieldDeclaration*>(SgAsmSynthesizedFieldDeclaration::pools[blockIndex-1])};
                blkptr[SgAsmSynthesizedFieldDeclaration::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmSynthesizedFieldDeclaration::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmSynthesizedFieldDeclaration::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmSynthesizedFieldDeclaration::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmSynthesizedFieldDeclaration::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmSynthesizedFieldDeclaration::pool_size - 1};
    SgAsmSynthesizedFieldDeclaration* testPointer{reinterpret_cast<SgAsmSynthesizedFieldDeclaration*>(SgAsmSynthesizedFieldDeclaration::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmSynthesizedFieldDeclaration::pool_size * (SgAsmSynthesizedFieldDeclaration::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmSynthesizedFieldDeclarationStroageClass
 * from its counterpart of SgAsmSynthesizedFieldDeclaration. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmSynthesizedFieldDeclaration::initializeStorageClassArray(SgAsmSynthesizedFieldDeclarationStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmSynthesizedFieldDeclaration::pools.begin(); block != SgAsmSynthesizedFieldDeclaration::pools.end(); ++block) {
        SgAsmSynthesizedFieldDeclaration* pointer{reinterpret_cast<SgAsmSynthesizedFieldDeclaration*>(*block)};
        for (unsigned i = 0; i < SgAsmSynthesizedFieldDeclaration::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 244341 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmType that takes its corresponding StorageClass as parameter
 */
SgAsmType::SgAsmType(const SgAsmTypeStorageClass& storageSource)  : SgAsmNode (storageSource) {

/* #line 244351 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmType*
SgAsmType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmType)};
        unsigned long positionInPool{localIndex % SgAsmType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmType::pool_size};
        returnPointer = &(((SgAsmType*)(SgAsmType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmType*
SgAsmType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmType)};
        unsigned long positionInPool{localIndex % SgAsmType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmType::pool_size};
        returnPointer = &(((SgAsmType*)(SgAsmType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmType::pools.begin(); block != SgAsmType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmType*>(*block)};
        for (unsigned i = 0; i < SgAsmType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmType::pools.begin(); block != SgAsmType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmType*>(*block)};
        for (unsigned i = 0; i < SgAsmType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmType::clearMemoryPool() {
    SgAsmType* tempPointer{nullptr};
    if (SgAsmType::pools.empty() == false) {
        auto block{SgAsmType::pools.begin()};
        SgAsmType::next_node = reinterpret_cast<SgAsmType*>(*block);
        for (; block != SgAsmType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmType::pool_size-1]);
        }
    }
}

void
SgAsmType::deleteMemoryPool() {
    for (auto p: SgAsmType::pools) {
        ROSE_FREE(p);
    }
    SgAsmType::next_node = nullptr;
    SgAsmType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmType)};
    for (size_t blockIndex = SgAsmType::pools.size(); (blockIndex * SgAsmType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmType) = %" PRIuPTR " SgAsmType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmType),SgAsmType::pool_size);
        }
#endif

        SgAsmType* pointer{static_cast<SgAsmType*>(ROSE_MALLOC(SgAsmType::pool_size * sizeof(SgAsmType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmType::pool_size * sizeof(SgAsmType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmType::pool_size * sizeof(SgAsmType));
#endif
        SgAsmType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmType::pool_size * sizeof(SgAsmType), V_SgAsmType));

        if (SgAsmType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmType* blkptr{reinterpret_cast<SgAsmType*>(SgAsmType::pools[blockIndex-1])};
                blkptr[SgAsmType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmType::pool_size - 1};
    SgAsmType* testPointer{reinterpret_cast<SgAsmType*>(SgAsmType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmType::pool_size * (SgAsmType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmTypeStroageClass
 * from its counterpart of SgAsmType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmType::initializeStorageClassArray(SgAsmTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmType::pools.begin(); block != SgAsmType::pools.end(); ++block) {
        SgAsmType* pointer{reinterpret_cast<SgAsmType*>(*block)};
        for (unsigned i = 0; i < SgAsmType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 244568 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmScalarType that takes its corresponding StorageClass as parameter
 */
SgAsmScalarType::SgAsmScalarType(const SgAsmScalarTypeStorageClass& storageSource)  : SgAsmType (storageSource) {

/* #line 244578 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_minorOrder = storageSource.storageOf_minorOrder;
    p_majorOrder = storageSource.storageOf_majorOrder;
    p_majorNBytes = storageSource.storageOf_majorNBytes;
    p_nBits = storageSource.storageOf_nBits;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmScalarType*
SgAsmScalarType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmScalarType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmScalarType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmScalarType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmScalarType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmScalarType)};
        unsigned long positionInPool{localIndex % SgAsmScalarType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmScalarType::pool_size};
        returnPointer = &(((SgAsmScalarType*)(SgAsmScalarType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmScalarType*
SgAsmScalarType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmScalarType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmScalarType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmScalarType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmScalarType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmScalarType)};
        unsigned long positionInPool{localIndex % SgAsmScalarType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmScalarType::pool_size};
        returnPointer = &(((SgAsmScalarType*)(SgAsmScalarType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmScalarType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmScalarType::pools.begin(); block != SgAsmScalarType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmScalarType*>(*block)};
        for (unsigned i = 0; i < SgAsmScalarType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmScalarType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmScalarType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmScalarType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmScalarType::pools.begin(); block != SgAsmScalarType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmScalarType*>(*block)};
        for (unsigned i = 0; i < SgAsmScalarType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmScalarType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmScalarType::clearMemoryPool() {
    SgAsmScalarType* tempPointer{nullptr};
    if (SgAsmScalarType::pools.empty() == false) {
        auto block{SgAsmScalarType::pools.begin()};
        SgAsmScalarType::next_node = reinterpret_cast<SgAsmScalarType*>(*block);
        for (; block != SgAsmScalarType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmScalarType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmScalarType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmScalarType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmScalarType::pool_size-1]);
        }
    }
}

void
SgAsmScalarType::deleteMemoryPool() {
    for (auto p: SgAsmScalarType::pools) {
        ROSE_FREE(p);
    }
    SgAsmScalarType::next_node = nullptr;
    SgAsmScalarType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmScalarType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmScalarType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmScalarType)};
    for (size_t blockIndex = SgAsmScalarType::pools.size(); (blockIndex * SgAsmScalarType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmScalarType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmScalarType) = %" PRIuPTR " SgAsmScalarType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmScalarType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmScalarType),SgAsmScalarType::pool_size);
        }
#endif

        SgAsmScalarType* pointer{static_cast<SgAsmScalarType*>(ROSE_MALLOC(SgAsmScalarType::pool_size * sizeof(SgAsmScalarType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmScalarType::pool_size * sizeof(SgAsmScalarType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmScalarType::pool_size * sizeof(SgAsmScalarType));
#endif
        SgAsmScalarType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmScalarType::pool_size * sizeof(SgAsmScalarType), V_SgAsmScalarType));

        if (SgAsmScalarType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmScalarType* blkptr{reinterpret_cast<SgAsmScalarType*>(SgAsmScalarType::pools[blockIndex-1])};
                blkptr[SgAsmScalarType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmScalarType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmScalarType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmScalarType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmScalarType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmScalarType::pool_size - 1};
    SgAsmScalarType* testPointer{reinterpret_cast<SgAsmScalarType*>(SgAsmScalarType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmScalarType::pool_size * (SgAsmScalarType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmScalarTypeStroageClass
 * from its counterpart of SgAsmScalarType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmScalarType::initializeStorageClassArray(SgAsmScalarTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmScalarType::pools.begin(); block != SgAsmScalarType::pools.end(); ++block) {
        SgAsmScalarType* pointer{reinterpret_cast<SgAsmScalarType*>(*block)};
        for (unsigned i = 0; i < SgAsmScalarType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 244799 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmFloatType that takes its corresponding StorageClass as parameter
 */
SgAsmFloatType::SgAsmFloatType(const SgAsmFloatTypeStorageClass& storageSource)  : SgAsmScalarType (storageSource) {

/* #line 244809 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_significandOffset = storageSource.storageOf_significandOffset;
    p_significandNBits = storageSource.storageOf_significandNBits;
    p_signBitOffset = storageSource.storageOf_signBitOffset;
    p_exponentOffset = storageSource.storageOf_exponentOffset;
    p_exponentNBits = storageSource.storageOf_exponentNBits;
    p_exponentBias = storageSource.storageOf_exponentBias;
    p_flags = storageSource.storageOf_flags;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmFloatType*
SgAsmFloatType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmFloatType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFloatType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFloatType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmFloatType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatType)};
        unsigned long positionInPool{localIndex % SgAsmFloatType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmFloatType::pool_size};
        returnPointer = &(((SgAsmFloatType*)(SgAsmFloatType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmFloatType*
SgAsmFloatType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmFloatType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFloatType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFloatType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmFloatType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmFloatType)};
        unsigned long positionInPool{localIndex % SgAsmFloatType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmFloatType::pool_size};
        returnPointer = &(((SgAsmFloatType*)(SgAsmFloatType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmFloatType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmFloatType::pools.begin(); block != SgAsmFloatType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmFloatType*>(*block)};
        for (unsigned i = 0; i < SgAsmFloatType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmFloatType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmFloatType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmFloatType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmFloatType::pools.begin(); block != SgAsmFloatType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmFloatType*>(*block)};
        for (unsigned i = 0; i < SgAsmFloatType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmFloatType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmFloatType::clearMemoryPool() {
    SgAsmFloatType* tempPointer{nullptr};
    if (SgAsmFloatType::pools.empty() == false) {
        auto block{SgAsmFloatType::pools.begin()};
        SgAsmFloatType::next_node = reinterpret_cast<SgAsmFloatType*>(*block);
        for (; block != SgAsmFloatType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmFloatType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmFloatType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmFloatType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmFloatType::pool_size-1]);
        }
    }
}

void
SgAsmFloatType::deleteMemoryPool() {
    for (auto p: SgAsmFloatType::pools) {
        ROSE_FREE(p);
    }
    SgAsmFloatType::next_node = nullptr;
    SgAsmFloatType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmFloatType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFloatType)};
    for (size_t blockIndex = SgAsmFloatType::pools.size(); (blockIndex * SgAsmFloatType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFloatType) = %" PRIuPTR " SgAsmFloatType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmFloatType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmFloatType),SgAsmFloatType::pool_size);
        }
#endif

        SgAsmFloatType* pointer{static_cast<SgAsmFloatType*>(ROSE_MALLOC(SgAsmFloatType::pool_size * sizeof(SgAsmFloatType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmFloatType::pool_size * sizeof(SgAsmFloatType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmFloatType::pool_size * sizeof(SgAsmFloatType));
#endif
        SgAsmFloatType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmFloatType::pool_size * sizeof(SgAsmFloatType), V_SgAsmFloatType));

        if (SgAsmFloatType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmFloatType* blkptr{reinterpret_cast<SgAsmFloatType*>(SgAsmFloatType::pools[blockIndex-1])};
                blkptr[SgAsmFloatType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmFloatType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmFloatType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmFloatType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmFloatType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmFloatType::pool_size - 1};
    SgAsmFloatType* testPointer{reinterpret_cast<SgAsmFloatType*>(SgAsmFloatType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmFloatType::pool_size * (SgAsmFloatType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmFloatTypeStroageClass
 * from its counterpart of SgAsmFloatType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmFloatType::initializeStorageClassArray(SgAsmFloatTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmFloatType::pools.begin(); block != SgAsmFloatType::pools.end(); ++block) {
        SgAsmFloatType* pointer{reinterpret_cast<SgAsmFloatType*>(*block)};
        for (unsigned i = 0; i < SgAsmFloatType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 245033 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmIntegerType that takes its corresponding StorageClass as parameter
 */
SgAsmIntegerType::SgAsmIntegerType(const SgAsmIntegerTypeStorageClass& storageSource)  : SgAsmScalarType (storageSource) {

/* #line 245043 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_isSigned = storageSource.storageOf_isSigned;


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmIntegerType*
SgAsmIntegerType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmIntegerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIntegerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIntegerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmIntegerType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerType)};
        unsigned long positionInPool{localIndex % SgAsmIntegerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmIntegerType::pool_size};
        returnPointer = &(((SgAsmIntegerType*)(SgAsmIntegerType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmIntegerType*
SgAsmIntegerType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmIntegerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIntegerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIntegerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmIntegerType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmIntegerType)};
        unsigned long positionInPool{localIndex % SgAsmIntegerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmIntegerType::pool_size};
        returnPointer = &(((SgAsmIntegerType*)(SgAsmIntegerType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmIntegerType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmIntegerType::pools.begin(); block != SgAsmIntegerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmIntegerType*>(*block)};
        for (unsigned i = 0; i < SgAsmIntegerType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmIntegerType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmIntegerType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmIntegerType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmIntegerType::pools.begin(); block != SgAsmIntegerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmIntegerType*>(*block)};
        for (unsigned i = 0; i < SgAsmIntegerType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmIntegerType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmIntegerType::clearMemoryPool() {
    SgAsmIntegerType* tempPointer{nullptr};
    if (SgAsmIntegerType::pools.empty() == false) {
        auto block{SgAsmIntegerType::pools.begin()};
        SgAsmIntegerType::next_node = reinterpret_cast<SgAsmIntegerType*>(*block);
        for (; block != SgAsmIntegerType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmIntegerType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmIntegerType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmIntegerType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmIntegerType::pool_size-1]);
        }
    }
}

void
SgAsmIntegerType::deleteMemoryPool() {
    for (auto p: SgAsmIntegerType::pools) {
        ROSE_FREE(p);
    }
    SgAsmIntegerType::next_node = nullptr;
    SgAsmIntegerType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmIntegerType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIntegerType)};
    for (size_t blockIndex = SgAsmIntegerType::pools.size(); (blockIndex * SgAsmIntegerType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIntegerType) = %" PRIuPTR " SgAsmIntegerType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmIntegerType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmIntegerType),SgAsmIntegerType::pool_size);
        }
#endif

        SgAsmIntegerType* pointer{static_cast<SgAsmIntegerType*>(ROSE_MALLOC(SgAsmIntegerType::pool_size * sizeof(SgAsmIntegerType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmIntegerType::pool_size * sizeof(SgAsmIntegerType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmIntegerType::pool_size * sizeof(SgAsmIntegerType));
#endif
        SgAsmIntegerType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmIntegerType::pool_size * sizeof(SgAsmIntegerType), V_SgAsmIntegerType));

        if (SgAsmIntegerType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmIntegerType* blkptr{reinterpret_cast<SgAsmIntegerType*>(SgAsmIntegerType::pools[blockIndex-1])};
                blkptr[SgAsmIntegerType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmIntegerType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmIntegerType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmIntegerType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmIntegerType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmIntegerType::pool_size - 1};
    SgAsmIntegerType* testPointer{reinterpret_cast<SgAsmIntegerType*>(SgAsmIntegerType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmIntegerType::pool_size * (SgAsmIntegerType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmIntegerTypeStroageClass
 * from its counterpart of SgAsmIntegerType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmIntegerType::initializeStorageClassArray(SgAsmIntegerTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmIntegerType::pools.begin(); block != SgAsmIntegerType::pools.end(); ++block) {
        SgAsmIntegerType* pointer{reinterpret_cast<SgAsmIntegerType*>(*block)};
        for (unsigned i = 0; i < SgAsmIntegerType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 245261 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmPointerType that takes its corresponding StorageClass as parameter
 */
SgAsmPointerType::SgAsmPointerType(const SgAsmPointerTypeStorageClass& storageSource)  : SgAsmScalarType (storageSource) {

/* #line 245271 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
     p_subtype = (SgAsmType *)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_subtype));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmPointerType*
SgAsmPointerType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmPointerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPointerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPointerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmPointerType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPointerType)};
        unsigned long positionInPool{localIndex % SgAsmPointerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPointerType::pool_size};
        returnPointer = &(((SgAsmPointerType*)(SgAsmPointerType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmPointerType*
SgAsmPointerType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmPointerType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPointerType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPointerType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmPointerType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmPointerType)};
        unsigned long positionInPool{localIndex % SgAsmPointerType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmPointerType::pool_size};
        returnPointer = &(((SgAsmPointerType*)(SgAsmPointerType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmPointerType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmPointerType::pools.begin(); block != SgAsmPointerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPointerType*>(*block)};
        for (unsigned i = 0; i < SgAsmPointerType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmPointerType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmPointerType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmPointerType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmPointerType::pools.begin(); block != SgAsmPointerType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmPointerType*>(*block)};
        for (unsigned i = 0; i < SgAsmPointerType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmPointerType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmPointerType::clearMemoryPool() {
    SgAsmPointerType* tempPointer{nullptr};
    if (SgAsmPointerType::pools.empty() == false) {
        auto block{SgAsmPointerType::pools.begin()};
        SgAsmPointerType::next_node = reinterpret_cast<SgAsmPointerType*>(*block);
        for (; block != SgAsmPointerType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmPointerType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmPointerType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmPointerType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmPointerType::pool_size-1]);
        }
    }
}

void
SgAsmPointerType::deleteMemoryPool() {
    for (auto p: SgAsmPointerType::pools) {
        ROSE_FREE(p);
    }
    SgAsmPointerType::next_node = nullptr;
    SgAsmPointerType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmPointerType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPointerType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPointerType)};
    for (size_t blockIndex = SgAsmPointerType::pools.size(); (blockIndex * SgAsmPointerType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPointerType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPointerType) = %" PRIuPTR " SgAsmPointerType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmPointerType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmPointerType),SgAsmPointerType::pool_size);
        }
#endif

        SgAsmPointerType* pointer{static_cast<SgAsmPointerType*>(ROSE_MALLOC(SgAsmPointerType::pool_size * sizeof(SgAsmPointerType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmPointerType::pool_size * sizeof(SgAsmPointerType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmPointerType::pool_size * sizeof(SgAsmPointerType));
#endif
        SgAsmPointerType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmPointerType::pool_size * sizeof(SgAsmPointerType), V_SgAsmPointerType));

        if (SgAsmPointerType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmPointerType* blkptr{reinterpret_cast<SgAsmPointerType*>(SgAsmPointerType::pools[blockIndex-1])};
                blkptr[SgAsmPointerType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmPointerType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmPointerType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmPointerType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmPointerType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmPointerType::pool_size - 1};
    SgAsmPointerType* testPointer{reinterpret_cast<SgAsmPointerType*>(SgAsmPointerType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmPointerType::pool_size * (SgAsmPointerType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmPointerTypeStroageClass
 * from its counterpart of SgAsmPointerType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmPointerType::initializeStorageClassArray(SgAsmPointerTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmPointerType::pools.begin(); block != SgAsmPointerType::pools.end(); ++block) {
        SgAsmPointerType* pointer{reinterpret_cast<SgAsmPointerType*>(*block)};
        for (unsigned i = 0; i < SgAsmPointerType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 245489 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmVectorType that takes its corresponding StorageClass as parameter
 */
SgAsmVectorType::SgAsmVectorType(const SgAsmVectorTypeStorageClass& storageSource)  : SgAsmType (storageSource) {

/* #line 245499 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());
    p_nElmts = storageSource.storageOf_nElmts;
     p_elmtType = (SgAsmType*)(AST_FILE_IO::getSgClassPointerFromGlobalIndex(storageSource.storageOf_elmtType));


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmVectorType*
SgAsmVectorType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmVectorType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmVectorType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmVectorType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmVectorType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVectorType)};
        unsigned long positionInPool{localIndex % SgAsmVectorType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmVectorType::pool_size};
        returnPointer = &(((SgAsmVectorType*)(SgAsmVectorType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmVectorType*
SgAsmVectorType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmVectorType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmVectorType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmVectorType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmVectorType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmVectorType)};
        unsigned long positionInPool{localIndex % SgAsmVectorType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmVectorType::pool_size};
        returnPointer = &(((SgAsmVectorType*)(SgAsmVectorType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmVectorType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmVectorType::pools.begin(); block != SgAsmVectorType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmVectorType*>(*block)};
        for (unsigned i = 0; i < SgAsmVectorType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmVectorType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmVectorType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmVectorType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmVectorType::pools.begin(); block != SgAsmVectorType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmVectorType*>(*block)};
        for (unsigned i = 0; i < SgAsmVectorType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmVectorType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmVectorType::clearMemoryPool() {
    SgAsmVectorType* tempPointer{nullptr};
    if (SgAsmVectorType::pools.empty() == false) {
        auto block{SgAsmVectorType::pools.begin()};
        SgAsmVectorType::next_node = reinterpret_cast<SgAsmVectorType*>(*block);
        for (; block != SgAsmVectorType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmVectorType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmVectorType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmVectorType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmVectorType::pool_size-1]);
        }
    }
}

void
SgAsmVectorType::deleteMemoryPool() {
    for (auto p: SgAsmVectorType::pools) {
        ROSE_FREE(p);
    }
    SgAsmVectorType::next_node = nullptr;
    SgAsmVectorType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmVectorType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVectorType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmVectorType)};
    for (size_t blockIndex = SgAsmVectorType::pools.size(); (blockIndex * SgAsmVectorType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVectorType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmVectorType) = %" PRIuPTR " SgAsmVectorType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVectorType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmVectorType),SgAsmVectorType::pool_size);
        }
#endif

        SgAsmVectorType* pointer{static_cast<SgAsmVectorType*>(ROSE_MALLOC(SgAsmVectorType::pool_size * sizeof(SgAsmVectorType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmVectorType::pool_size * sizeof(SgAsmVectorType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmVectorType::pool_size * sizeof(SgAsmVectorType));
#endif
        SgAsmVectorType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmVectorType::pool_size * sizeof(SgAsmVectorType), V_SgAsmVectorType));

        if (SgAsmVectorType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmVectorType* blkptr{reinterpret_cast<SgAsmVectorType*>(SgAsmVectorType::pools[blockIndex-1])};
                blkptr[SgAsmVectorType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmVectorType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmVectorType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmVectorType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmVectorType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmVectorType::pool_size - 1};
    SgAsmVectorType* testPointer{reinterpret_cast<SgAsmVectorType*>(SgAsmVectorType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmVectorType::pool_size * (SgAsmVectorType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmVectorTypeStroageClass
 * from its counterpart of SgAsmVectorType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmVectorType::initializeStorageClassArray(SgAsmVectorTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmVectorType::pools.begin(); block != SgAsmVectorType::pools.end(); ++block) {
        SgAsmVectorType* pointer{reinterpret_cast<SgAsmVectorType*>(*block)};
        for (unsigned i = 0; i < SgAsmVectorType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 245718 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */



/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */
/*
 * Constructor of the IR node SgAsmVoidType that takes its corresponding StorageClass as parameter
 */
SgAsmVoidType::SgAsmVoidType(const SgAsmVoidTypeStorageClass& storageSource)  : SgAsmType (storageSource) {

/* #line 245728 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */

    ASSERT_require(p_freepointer == AST_FileIO::IS_VALID_POINTER());


/* #line 5 "/workspace/generated/src/ROSETTA/Grammar/grammarSourceOfIRNodesAstFileIOSupport.macro" */

}

/*
 * Compute the pointer to the passed global index, specific within the working AST
 */
SgAsmVoidType*
SgAsmVoidType::getPointerFromGlobalIndex(unsigned long globalIndex) {
    SgAsmVoidType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmVoidType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmVoidType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfNewAst(V_SgAsmVoidType)
                                             + AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVoidType)};
        unsigned long positionInPool{localIndex % SgAsmVoidType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmVoidType::pool_size};
        returnPointer = &(((SgAsmVoidType*)(SgAsmVoidType::pools[memoryBlock]))[positionInPool]);
        ASSERT_not_null(returnPointer);
    }
    return returnPointer;
}

/*
 * Computing the pointer to the passed global index, specific for the AST with the index astIndex
 */
SgAsmVoidType*
SgAsmVoidType::getPointerFromGlobalIndex(AstSpecificDataManagingClass* astInPool, unsigned long globalIndex) {
    SgAsmVoidType* returnPointer{nullptr};
    if (globalIndex != 0) {
#if FILE_IO_EXTRA_CHECK
        ASSERT_require((AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmVoidType)) <= globalIndex);
        ASSERT_require(globalIndex < (AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmVoidType + 1)));
#endif
        unsigned long localIndex{globalIndex - AST_FILE_IO::getAccumulatedPoolSizeOfAst(astInPool, V_SgAsmVoidType)
                                             + AST_FILE_IO::getSizeOfMemoryPoolUpToAst (astInPool, V_SgAsmVoidType)};
        unsigned long positionInPool{localIndex % SgAsmVoidType::pool_size};
        unsigned long memoryBlock{(localIndex - positionInPool) / SgAsmVoidType::pool_size};
        returnPointer = &(((SgAsmVoidType*)(SgAsmVoidType::pools[memoryBlock]))[positionInPool]);
#if FILE_IO_EXTRA_CHECK
        ASSERT_not_null(returnPointer);
#endif
    }
    return returnPointer ;
}

/*
 * Traverse memory pool, set global ids and return accumulated pool size! We set for every valid object in the memory
 * pool the freepointer to the global index and increase the global index afterwards. For all the invalid objects
 * (means address ranges within the memory pool that were not returned by the new operator) the freepointer is set
 * to NULL, in order to distinguish valid from invalid objects.
 */
unsigned long
SgAsmVoidType::getNumberOfValidNodesAndSetGlobalIndexInFreepointer(unsigned long numberOfPreviousNodes) {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == false);
    unsigned long globalIndex{numberOfPreviousNodes};
    for (auto block = SgAsmVoidType::pools.begin(); block != SgAsmVoidType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmVoidType*>(*block)};
        for (unsigned i = 0; i < SgAsmVoidType::pool_size; ++i) {
            // In reading in multiple files, when the extendMemoryPoolForFileIO() function is called,
            // we have entries with pointer[i].get_freepointer() set to NULL at the end of any newly allocated memory pool.
            // These are rare and so are a problem that is difficult to reproduce except when the CLASSNAME::pool_size
            // is set to 1 to test the pool extention at the price of performance.  In this case we want to reset the
            // elements of the memory pool that have freepointer set to either AST_FileIO::IS_VALID_POINTER() or NULL.
            // This should not be a problem if the AST resd reset the freepointer to AST_FileIO::IS_VALID_POINTER()
            // properly; so this will have to be checked next.
            if (pointer[i].get_freepointer() == AST_FileIO::IS_VALID_POINTER()) {
                pointer[i].set_freepointer((SgAsmVoidType*)(globalIndex));
                globalIndex++;
            }
            else {
                pointer[i].set_freepointer(nullptr);
            }
        }
    }
    return globalIndex;
}

void
SgAsmVoidType::resetValidFreepointers() {
    ASSERT_require(AST_FILE_IO::areFreepointersContainingGlobalIndices() == true);
    SgAsmVoidType* pointerOfLinkedList{nullptr};
    for (auto block = SgAsmVoidType::pools.begin(); block != SgAsmVoidType::pools.end(); ++block) {
        auto* pointer{reinterpret_cast<SgAsmVoidType*>(*block)};
        for (unsigned i = 0; i < SgAsmVoidType::pool_size; ++i) {
            if (pointer[i].get_freepointer() != nullptr) {
                pointer[i].set_freepointer(AST_FileIO::IS_VALID_POINTER());
            }
            else {
                if (pointerOfLinkedList == nullptr) {
                    SgAsmVoidType::next_node = &(pointer[i]);
                }
                else {
                    pointerOfLinkedList->set_freepointer(&(pointer[i]));
                }
                pointerOfLinkedList = &(pointer[i]);
            }
        }
    }
    if (pointerOfLinkedList != nullptr) {
        pointerOfLinkedList->set_freepointer(nullptr);
    }
}

/*
 * Clear the memory pool. This method deletes all objects within the memory pool and resets the freepointers, in order to
 * achieve a linked list, that has no jumps and starts at the beginning! This function does not extend the memory pool,
 * since we do not delete any memory blocks, but delete the valid objects.
 */
void
SgAsmVoidType::clearMemoryPool() {
    SgAsmVoidType* tempPointer{nullptr};
    if (SgAsmVoidType::pools.empty() == false) {
        auto block{SgAsmVoidType::pools.begin()};
        SgAsmVoidType::next_node = reinterpret_cast<SgAsmVoidType*>(*block);
        for (; block != SgAsmVoidType::pools.end(); ++block) {
            auto* pointer{reinterpret_cast<SgAsmVoidType*>(*block)};
            if (tempPointer != nullptr) {
                tempPointer->set_freepointer(pointer);
            }
            for (unsigned i = 0; i < SgAsmVoidType::pool_size - 1; ++i) {
                pointer[i].set_freepointer(&(pointer[i+1]));
            }
            pointer[SgAsmVoidType::pool_size-1].set_freepointer(nullptr);
            tempPointer = &(pointer[SgAsmVoidType::pool_size-1]);
        }
    }
}

void
SgAsmVoidType::deleteMemoryPool() {
    for (auto p: SgAsmVoidType::pools) {
        ROSE_FREE(p);
    }
    SgAsmVoidType::next_node = nullptr;
    SgAsmVoidType::pools.clear();
}

// Traversal for the MemoryPool, used for AST file IO
void
SgAsmVoidType::extendMemoryPoolForFileIO() {
    size_t newPoolSize{AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVoidType) + AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmVoidType)};
    for (size_t blockIndex = SgAsmVoidType::pools.size(); (blockIndex * SgAsmVoidType::pool_size) < newPoolSize; ++blockIndex) {
#if ROSE_ALLOC_TRACE
        if (blockIndex > 0) {
            printf("blockIndex = %lu newPoolSize = %" PRIuPTR " AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVoidType) = %" PRIuPTR " AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmVoidType) = %" PRIuPTR " SgAsmVoidType::pool_size = %d\n",
                   blockIndex,newPoolSize,AST_FILE_IO::getSizeOfMemoryPool(V_SgAsmVoidType),AST_FILE_IO::getPoolSizeOfNewAst(V_SgAsmVoidType),SgAsmVoidType::pool_size);
        }
#endif

        SgAsmVoidType* pointer{static_cast<SgAsmVoidType*>(ROSE_MALLOC(SgAsmVoidType::pool_size * sizeof(SgAsmVoidType)))};
        ASSERT_not_null(pointer);
#if ROSE_ALLOC_MEMSET == 1
        memset(pointer, 0x00, SgAsmVoidType::pool_size * sizeof(SgAsmVoidType));
#elif ROSE_ALLOC_MEMSET == 2
        memset(pointer, 0xCC, SgAsmVoidType::pool_size * sizeof(SgAsmVoidType));
#endif
        SgAsmVoidType::pools.push_back(reinterpret_cast<unsigned char*>(pointer));
        SgNode::all_pools.push_back(std::tuple<unsigned char*, unsigned, VariantT>(reinterpret_cast<unsigned char*>(pointer), SgAsmVoidType::pool_size * sizeof(SgAsmVoidType), V_SgAsmVoidType));

        if (SgAsmVoidType::next_node != nullptr) {
            if (blockIndex > 0) {
                SgAsmVoidType* blkptr{reinterpret_cast<SgAsmVoidType*>(SgAsmVoidType::pools[blockIndex-1])};
                blkptr[SgAsmVoidType::pool_size-1].set_freepointer(pointer);
            }
        } else {
            SgAsmVoidType::next_node = pointer;
        }

        for (unsigned i = 0; i < SgAsmVoidType::pool_size-1; ++i) {
            pointer[i].set_freepointer(&(pointer[i+1]));
        }
        pointer[SgAsmVoidType::pool_size-1].set_freepointer(nullptr);
    }
}

/*
 * Delivers the last valid object within a memory pool. This could be used, to read new ASTs even, if the memory pools are
 * not compressed. However, that stuff is not yet implemented.
 */
unsigned long
SgAsmVoidType::getNumberOfLastValidPointer() {
    unsigned long localIndex{SgAsmVoidType::pool_size - 1};
    SgAsmVoidType* testPointer{reinterpret_cast<SgAsmVoidType*>(SgAsmVoidType::pools.back())};
    while (testPointer[localIndex].get_freepointer() != AST_FileIO::IS_VALID_POINTER()) {
        --localIndex;
    }
    return (localIndex + SgAsmVoidType::pool_size * (SgAsmVoidType::pools.size()-1));
}

/*
 * Steps through the valid IR nodes in the memory pool and initializes the data member in class SgAsmVoidTypeStroageClass
 * from its counterpart of SgAsmVoidType. The return value is just for checking that the whole StorageClassArray is initialized.
 */
unsigned long
SgAsmVoidType::initializeStorageClassArray(SgAsmVoidTypeStorageClass* storageArray) {
    unsigned long storageCounter{0};
    for (auto block = SgAsmVoidType::pools.begin(); block != SgAsmVoidType::pools.end(); ++block) {
        SgAsmVoidType* pointer{reinterpret_cast<SgAsmVoidType*>(*block)};
        for (unsigned i = 0; i < SgAsmVoidType::pool_size; ++i) {
            if (pointer->get_freepointer() != nullptr) {
                storageArray->pickOutIRNodeData(pointer);
                ++storageArray;
                ++storageCounter;
            }
            ++pointer;
        }
    }
    return storageCounter;
}
/* #line 245945 "/workspace/generated/src/frontend/SageIII//astFileIO//SourcesOfIRNodesAstFileIOSupport.C" */


